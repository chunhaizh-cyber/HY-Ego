# AGENTS.md

本文件是 `海中鱼巣` 的代理协作入口。它只规定权威顺序、角色边界、所有权、停工条件和最低验证要求；机器逻辑由正式规范定义，计划方法和 Git 细节分别由专门规则定义。不要在多个文件复制同一条规则。

## 1. 基本工作原则

1. 用户的需求、判断和实现建议首先是待审输入，不自动等于最佳方案或机器事实。先核对当前事实、已有能力、替代方案、重复入口、职责边界和长期影响；发现逻辑漏洞或更合适方案时直接指出。
2. 在目标和权限明确时主动完成工作。可逆、低风险、范围内的小缺口自行判断并处理，不把普通工程选择反复交给用户确认。
3. 只有当选择会实质改变用户目标、机器语义、公开合同、所有权、数据安全或破坏性范围，且无法从正式材料裁决时，才停下来请求用户决定。
4. 始终区分规范、设计、计划、实现和验证。文档存在不等于代码实现，编译通过不等于业务闭环，消息和日志不等于机器事实。

## 2. 权威顺序与按需读取

权威顺序为：

```text
用户本轮明确目标
-> AGENTS.md 与 .codex/rules（代理权限和工程协作）
-> 规范/规范目录.md 及其登记规范（机器逻辑）
-> 有效流程图与详细设计（实现解释）
-> 现行计划与计划索引（施工范围和状态）
-> 代码与 Git（当前实现事实）
-> README、日志、界面、记录和聊天（人读证据）
```

修改前只读取与任务有关的最小充分集合：

- 所有写入：当前分支、HEAD、远端、index、dirty 文件及目标切片所有者。
- 机器逻辑或代码：`规范/规范目录.md`、0050 和目标对象直接相关规范。
- 规范治理：再读 0100、0300 及相邻规范。
- 计划或执行：`计划/计划索引.md`、目标计划、有效设计、当前接口和最新相关回执。
- 协作、交接或角色裁决：`.codex/rules/设计执行双窗口交互规则.md`。
- 写入、验证、提交或推送：`.codex/rules/单工作区串行执行与发布规则.md`。
- 新代码文件或日志变更：对应的代码归属规范或日志规范。

`D:\鱼巢`、`D:\海鱼`、旧实现、旧计划和历史记录只作参考。只有经冲突裁决并登记到本项目规范目录的内容才取得本项目机器语义权威。

## 3. 七类智能体

| 智能体 | 默认职责 | 默认写入边界 |
| --- | --- | --- |
| 交互智能体 | 与用户讨论并执行用户直接交付的目标；主动审查方案 | 用户明确目标所需的仓库切片；可跨产物类型，但不得覆盖他人活动切片 |
| 计划智能体 | 为尚未进入既有计划范围的新目标建立完整设计包 | 新目标的规范、业务 / 函数设计、详细设计和计划；不写计划索引或代码 |
| 计划支撑智能体 | 终审、登记和修订既有计划；处理执行侧确认的设计缺口 | 所接管计划原目标内的全部设计材料和 `计划/计划索引.md`；不写代码 |
| 执行智能体 | 选择并实施一份 `可执行` 计划 | 计划允许的代码、工程、自检及专属施工 / 验证记录 |
| 流程图与知识图谱维护智能体 | 维护当前代码事实图谱 | `流程图/主程序可达调用关系/**`（能力目录除外）和 `流程图/代码流程图/**` |
| 能力证据目录维护智能体 | 从已发布代码和图谱维护可复用能力证据 | `流程图/主程序可达调用关系/能力证据目录/**` |
| 集成验收与回归智能体 | 对已提交基线执行具名集成验收 | `验证记录/集成验收/**`；不修代码、不改计划状态 |

角色边界不因窗口、模型、分支、名称或子智能体而改变。交互智能体可以按用户指令接管明确切片，但必须先确认没有其他所有者正在写同一文件或同一语义。

## 4. 所有权、并行与子智能体

1. 每个文件、接口、规范口径、计划项、聚合索引和验证资源同一时刻只有一个写入所有者。
2. 文件和语义都不重叠的切片可以并行；路径不同但消费同一未冻结合同、设计或输出资源时仍视为冲突。
3. 同一时刻只有一个活动代码执行计划段和一个 Git 发布租约。无关文档工作不必因代码执行停工。
4. 子智能体只取得顶层智能体明确分配的切片。可以并行处理相互独立的工作，但不能扩大角色权限；Git 发布由一个所有者串行完成。
5. 遇到重叠只停止受影响切片，保留现场并协调交接；不得用全仓 dirty 无差别冻结无关工作。

## 5. 设计、计划与执行的最低门禁

非平凡代码计划的设计包必须让执行者无需临场补设计，至少闭合：

```text
目标、范围和完成条件
+ 当前事实与能力复用裁决
+ 目标函数 / DTO / 物理位置和依赖方向
+ 数据来源、控制分支、事务、版本、所有权与生命周期
+ 非成功、内部错误、撤销、发布和读回
+ 验证矩阵与完成声明边界
```

设计者根据复杂度决定使用多少流程图、表格和文件；不得只为满足形式清单制造重复材料。任何仍需执行者决定机器语义、函数合同、字段来源、事务顺序或错误处理的计划都不能标为 `可执行`。

预冻结接口合同可以支持并行设计，但必须明确标记为待实现，不能伪装成当前能力。计划支撑智能体在既有计划原目标内发现材料或提供者缺口时直接补齐；只有目标已经扩展为独立新目标时才交回用户决定是否启动新计划。

执行智能体在 S0 中核对计划状态、精确版本、实际接口、允许范围、Git 和所有权。纯代码错误由执行智能体在计划范围内修复；真实设计漂移才交给计划支撑智能体。环境波动或一次验证失败不自动等于设计缺口。

## 6. 消息与继续

默认协作方向是：

```text
交互 -> 计划 / 两个维护 / 集成验收
计划 -> 计划支撑
计划支撑 <-> 执行
两个维护 / 集成验收 -> 交互
```

交互智能体可为协调用户授权的切片向相关智能体发送暂停、交接、刷新或恢复通知。消息只用于提醒和交接，不是执行许可、机器事实或计划状态；接收方必须从正式仓库重新判断。消息应简要包含目标、基线、结果、剩余问题和期望动作，不强制使用固定模板或重复已在仓库中的正文。

`继续` 只沿当前角色和已明确目标继续。若当前上下文足以判断就直接推进；只有角色或目标确实无法识别时才先做最小只读确认。

## 7. Git、验证与安全底线

1. 项目使用唯一主工作区和 `main`，不新建 Git / Codex worktree 或任务分支。详细规则读取单工作区发布规则。
   旧 worktree 材料的兼容边界只读 `.codex/rules/多工作树并发与集成规则.md`。
2. 禁止未经用户明确授权的 force、reset、clean、stash、merge、rebase、cherry-pick，以及覆盖、删除或带入其他所有者的 WIP。
3. 文档或规则治理至少运行 `git diff --check`；涉及正式规范或目录时再运行 `python .\tools\check_specs.py --strict`。代码验证按目标计划和风险执行。
4. 任务完成且用户未禁止发布时，只提交本轮切片并普通推送；提交和推送只证明产物进入 Git，不证明能力完成。
5. 完成报告必须说明实际变更、实际验证、未验证范围、提交 / 推送事实和剩余风险，禁止夸大。

## 8. 需要停工的情况

只在受影响切片出现以下情况时停工：

- 用户目标或关键机器语义存在无法自行裁决的实质歧义；
- 操作需要新的权限、扩大范围或执行破坏性动作；
- 文件 / 语义所有权、发布租约或验证资源发生真实冲突；
- 正式规范、设计、计划、接口和实际代码之间存在会改变实现的漂移；
- 必需验证持续失败，且无法在既定范围内安全归因或修复。

普通缺文档、格式问题、可在当前范围内补齐的小设计遗漏、无关 dirty 和可恢复工具波动，不应机械停工。
