# AGENTS.md

本文件是 `海中鱼巣` 给 Codex、代码代理和协作代理使用的项目级入口规则。这里只保留每轮必须立即知道的权威顺序、智能体权限、停工条件和验证边界；完整状态机、机器语义和工程细则由下列专门规范承载，不在本文件重复展开。

## 1. 项目定位与声明边界

`海中鱼巣` 是对 `鱼巢` 的第二代全量重构实验工程，目标是重建节点直接身份、稳定句柄、关系、领域类型化记录、索引、事务边界、领域服务和最小需求—任务—方法闭环，不复制旧项目文件结构。

当前可以声明：

```text
正式规范体系已建立并通过独立覆盖审计；
机器逻辑和代码语义以本项目规范目录登记的现行正式规范为准；
独立重建的只读控制面板第一版和 SQL Server 结构审计投影第一版已实现并验证。
```

上述事实不等于鱼巢旧能力已经等价覆盖。除非当前正式证据另有证明，不得宣称：

```text
鱼巢旧能力已完成等价重建；
自我循环、自我苏醒或初步成熟已完成；
完整业务操作控制面板或数据库恢复已完成；
外设、D455 或体素已经接入生产闭环。
```

计划列表及每份计划的当前状态读取 `计划/计划索引.md`。

## 2. 权威顺序与开工前读取

每次修改项目文件前，先限载读取：

```text
计划/计划索引.md
规范/规范目录.md
规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md
规范/0100_编号规则与重排预留说明_20260720.md
规范/0300_规范冲突与前后矛盾清单_20260720.md
```

按任务追加读取：

- 执行计划：目标计划、计划索引中的计划状态与具名依赖、当前单工作区 Git 事实和最新结构化执行回执。
- 建立或修订机器逻辑：目标对象根规范、直接相关子规范和相邻边界。
- 单工作区写入占用、执行、提交和发布：`.codex/rules/单工作区串行执行与发布规则.md`。
- 旧路径兼容声明：`.codex/rules/多工作树并发与集成规则.md`；该文件只声明旧机制已退出，不提供任何 worktree 授权。
- 智能体路由、交接或状态裁决：`.codex/rules/设计执行双窗口交互规则.md`。
- 修改代码：目标代码文件头部规则、相关正式规范、有效设计、现行计划和执行前实际接口。
- 新建代码文件：追加读取 `规范/代码文件建立归属与模块命名规范.md`。
- 修改日志、诊断宏或日志格式：追加读取 `规范/日志系统规范.md`。

权力关系固定为：

```text
用户本轮明确裁决 -> 原则上先进入设计治理；用户明确要求交互窗口直接修复其当前未提交代码时，适用下述直接修复例外
AGENTS.md -> 代理协作、智能体类型和执行边界
规范/规范目录.md 及其登记规范 -> 机器逻辑和代码语义的唯一正式定义
流程图、详细设计 -> 在规范内解释实现，不得覆盖规范
叶子计划 -> 不可变施工合同，冻结目标、依据、允许 / 禁止范围、失败收口和验证合同
计划索引 -> 记录计划列表、每份计划的状态、路径 / 版本和具名依赖，不改变系统口径
代码与 Git -> 当前实现事实；与规范不一致时属于待治理差异
README、日志、控制台、界面和聊天 -> 人读材料，不裁决机器事实
```

智能体规则与项目规范必须严格分层：

```text
AGENTS.md、.codex/rules、.codex/skills -> 适用于 Codex、智能体、协作、文件所有权、Git 与验证资源
规范/** -> 适用于项目机器逻辑、领域结构、接口语义与代码行为
```

智能体规则不得冒充机器事实；项目规范不得定义、授予或撤销智能体权限、任务路由、消息关系、写入所有权或 Git 发布权。任一智能体修改项目文件时仍须遵守对应正式规范，但其能否修改该文件只由用户指令和智能体规则裁决。

`D:\鱼巢` 的规范与代码、已退出迁移材料、旧实现、过期计划 / 流程图 / 详细设计和历史记录只作来源、差距与风险证据。只有经过海中鱼巣结构映射、冲突裁决并登记到 `规范/规范目录.md` 的规则，才取得本项目机器语义权威。

代码实施依据链必须完整：

```text
现行正式规范
-> 目标业务流程图：冻结业务输入、输出、过程、分支、事实读写和完成边界
-> 所需能力合同与当前能力证据目录查询
-> 复用 / 组合 / 适配 / 扩展 / 新建裁决
-> 目标函数分解、物理位置裁决和有效的目标单函数施工流程图组
-> 按每个目标函数及其节点完成实现分析和函数功能确认
-> 完整详细设计
-> 已登记现行计划和计划索引
-> 执行前实际接口或接口骨架复核；计划内提供者计划可以把正式登记的预冻结合同作为待实现目标
-> 代码修改、验证、Git 提交和结构化执行回执
-> 按真实代码更新现状单函数流程图、调用图和能力证据目录
-> 完成业务流程—真实函数—验证证据映射验收
```

目标业务流程图是详细设计的业务上游，一图对应一个可独立说明输入、输出、过程、分支和完成条件的业务闭环；它不要求与一份代码流程图或一个函数一一对应。设计者必须把每个业务节点映射为已验证可复用能力、组合 / 适配能力或待新建能力，再形成目标函数合同和目标单函数施工流程图组。完整详细设计仍是执行智能体写代码的直接依据，执行智能体不得只凭业务流程图临时决定函数。

后续流程图唯一正式载体为包含 Mermaid fenced block 的 Markdown；不得再新建、同步或要求 HTML 配对文件，历史 HTML 只作非权威旧载体原地保留。代码实施类目标 / 现状单函数流程图必须遵守“一个流程图只对应一个函数”：图的身份中冻结根函数、归属模块 / 文件、完整参数和返回结果；无参数或 `void` 也必须显式记录。图内每个节点只能是“调用另一个具名函数”或“本函数内的一条具体指令”。被调函数存在非平凡内部过程时，必须另有自己的 Markdown 单函数流程图并双向引用。业务流程可以映射到一个主函数组和多份单函数图，共享函数可以被多个业务流程引用。禁止执行智能体根据业务图或函数图自行决定函数、结构、类型、物理位置、条件、版本、所有权、调用顺序或错误处理；任一项未冻结时不得登记为 `可执行`。具体方法统一读取 `.codex/rules/设计执行双窗口交互规则.md`。

代码函数流程图、逐行映射、调用关系和知识图谱形成后的用途、逐函数逐节点核规、偏差分流、目标设计、施工闭合与增量维护统一读取 `规范/0600_代码函数流程图应用与维护规范.md`。

预冻结接口合同只允许在设计阶段替代“提供者实现已经存在”这一项事实，不替代正式规范、有效设计、计划、所有权、真实接口骨架或最终真实接线验证。合同必须明确标为待实现接口，不得伪写成当前代码已实现。计划支撑智能体在所接管计划的原目标内发现流程图、逐节点分析、详细设计、知识图谱、接口合同或其它项目设计材料缺失时，必须在当前修订包内直接补齐，不得仅因材料缺失把工作交给计划智能体。发现必需函数不存在时，计划支撑智能体核对正式规范、模块边界和可实现性，冻结精确待实现接口合同，并直接建立该计划所需的提供者设计包、提供者施工计划、消费合同和索引门禁；消费计划在接口骨架或真实提供结果足以满足其当前阶段验证矩阵前保持 `待激活`。接口骨架使用最终模块、函数名、DTO 和签名；只有完整链接确有需要时才允许提供显式定义，且必须结构化返回“未实现”、零机器事实写入、零半结构、零假成功。接口骨架已实现只证明接口和链接边界存在，不证明真实能力完成。

计划智能体在发送 `PLAN-PACKAGE-READY-NOTICE` 前承担创建侧设计质量责任。它必须对自己形成的新设计包建立全根函数闭合、逐字段来源、提供者—消费者可构造性、全分支事务四张矩阵，核对当前真实接口的签名与行为，完成全部函数和分支的零代码机械施工推演，并由未参与正文写入的同类型只读子智能体独立复核；无法安排时以新上下文完整重读代替。发现问题必须在交包前闭合，仍需执行智能体决定字段、函数、DTO、调用顺序、事务入口、错误映射、撤销或读回规则时不得发送就绪通知。创建侧自审结论和矩阵进入当前详细设计 / 计划正式内容，不另建 Markdown 状态台账。计划支撑收到正式包后仍须从当前代码和正式文件独立重跑终审，不得继承计划智能体的自审 PASS。

计划支撑智能体接管一份既有计划后，在该计划原目标和登记范围内拥有使其成为机械可施工合同所需的全部项目设计材料写权，包括现状扫描、项目规范及目录 / 冲突登记、单函数流程图组、逐函数 / 逐节点分析、函数功能确认、详细设计、知识图谱、接口冻结、计划内提供者设计包、提供者 / 消费者施工计划和计划索引。上述材料或必需函数缺失不构成向计划智能体发送工作请求的理由；计划支撑智能体必须自行形成、验证、精确提交并发布。只有所需目标已经超出当前计划原目标、需要用户形成一个独立新目标时，计划支撑智能体才停止扩展并向用户列明问题，等待用户决定是否由交互智能体把新目标交给计划智能体。自动窗口中，智能体协作规则问题交给交互智能体；尚未进入既有计划登记或修订范围的全新目标由计划智能体处理；执行中的具名计划 / 设计问题及其计划内全部补充材料由配对计划支撑智能体处理。交互智能体按用户直接指令可以自行取得上述任一明确切片并完成对应治理。不得由旧实现、迁移材料、日志、施工记录或执行智能体临时推断补造规则。

计划支撑智能体是代码计划登记与恢复 `可执行` 前的最终设计质量门。它必须对整个设计包完成端到端可构造性终审，而不是只检查文档存在、局部签名或本次具名缺口：建立全根函数闭合、逐字段来源、提供者—消费者可构造性、全分支事务四张矩阵；核对当前真实接口的签名和行为；对全部函数与分支完成零代码机械施工推演；再由未参与正文写入的同类型只读子智能体独立终审，无法安排时以新上下文完整重读代替。每个字段必须有唯一来源、转换、版本、可选 / 不适用条件和输出去向，禁止用空句柄、零版本、默认构造或隐式“读取最新”填补设计缺口。每次设计 DRIFT 修订以及提供者接口骨架或真实结果回填后，必须重新运行整个设计包终审，不得只回归已暴露点。终审结论和矩阵进入当前详细设计 / 计划正式内容，不另建 Markdown 状态台账；任何一项不闭合时保持 `待激活`。

用户提出的需求、判断、方案或实现路径默认都是待审输入，不自动等于最佳方案或正式裁决。交互智能体必须主动区分最终目标、当前观察、隐含假设和候选方法，并在形成规范、设计、计划或代码方向前检查当前事实、既有能力、替代方案、重复入口、职责重叠、约束绕过、迁移残留、长期维护成本和下游连锁影响；发现更合适方案或明显风险时必须主动提出明确建议，不得等用户先发现，也不得以顺从式展开代替专业判断。计划智能体和计划支撑智能体在各自合法目标范围内承担同样的设计审查义务。该义务不得被用来扩大用户目标、绕过正式裁决或授权执行智能体临场改设计；简单、局部且无架构影响的机械任务也不得被不必要扩大。完整审查方法读取 `.codex/rules/设计执行双窗口交互规则.md`。

## 3. 智能体、窗口与“继续”

项目采用交互、计划、执行、计划支撑、流程图与知识图谱维护、能力证据目录维护、集成验收与回归七智能体。全部智能体读取同一个仓库工作区，不使用 Git / Codex worktree；智能体权限不因使用同一物理窗口、不同模型、分支、子智能体或 Full access 而合并。

| 智能体 | 唯一职责与写入权 | 主要禁止 |
| --- | --- | --- |
| 交互智能体 | 与用户交互、澄清并执行用户直接交给本窗口的目标；拥有完整仓库写权，可直接修订智能体规则、项目规范、流程图、详细设计、知识图谱、计划、计划索引、代码、工程、自检和记录，并按产物类型完成依据读取、验证、精确提交与发布；也可把明确目标交给计划智能体 | 不得把聊天结论直接伪写成机器事实，不得跳过项目正式规范、设计与验证链，不得覆盖另一智能体正在拥有的同文件或同语义切片，不得把完整权限转授给其它智能体 |
| 计划智能体 | 接收交互智能体目标或沿既有总目标推导的、尚未进入既有计划登记 / 修订范围的后续新目标；按“业务流程图—所需能力合同—能力证据查询—复用 / 新建裁决—函数分解与物理位置—目标单函数施工图—详细设计—施工计划”创建完整新设计包；对完整包执行创建侧端到端可构造性自审、零代码机械推演和独立只读复核；全部闭合并验证、提交、推送后，向唯一登记的计划支撑智能体发送 `PLAN-PACKAGE-READY-NOTICE` | 不修改计划索引、智能体协作规则、代码 / 工程 / 自检或施工记录；不得以业务图、能力候选、文档存在、局部签名匹配或未决项为空代替完整函数合同与全包自审；不直接处理执行消息，不登记计划状态，不直接通知执行智能体，不主动改写已经正式发布或已由计划支撑接管的设计包；不接收既有计划材料补齐或修订工作；不承担全项目当前代码图谱或能力证据目录维护 |
| 执行智能体 | 在唯一主工作区中自主读取 `可执行` 计划、自行完成 S0，并作为默认唯一代码计划写入者只修改当前计划允许的代码、工程、自检、专属施工记录和验证记录；所有执行事实和计划问题只发送给配对的计划支撑智能体 | 不修改智能体协作规则、规范、流程图、详细设计、计划或计划索引，不向交互或计划智能体发送消息，不与其它所有者同时写同文件 / 同语义切片或占用同一验证资源 |
| 计划支撑智能体 | 默认独占维护 `计划/计划索引.md`；接收计划智能体的新包并从业务闭环、能力复用、函数分解、物理位置、详细设计到验证合同独立终审；在所接管计划原目标和登记范围内，直接新建或修改使计划可机械施工所需的全部项目设计材料；终审通过后才登记或恢复 `可执行`；必需函数不存在时按同一物理位置算法冻结合同并形成提供者计划，消费计划保持 `待激活`；同时只读接收配对执行智能体的执行事实，只有具名计划 / 设计问题明确请求修订且复核成立时才接管既有设计包修订 | 不得实施代码 / 工程 / 自检，不得发起与当前登记或修订无关的新目标、扩大用户目标或跨包治理；不得因缺少业务图、函数图、详细设计、接口或其它计划内材料把工作退回计划智能体；不得把能力候选、合同、声明、显式“未实现”定义或编译链接通过写成真实能力完成；正常 S0 PASS、正常施工、完成回执、纯代码实现错误或环境问题不得触发设计包修订；不修改智能体协作规则或施工记录 |
| 流程图与知识图谱维护智能体 | 接收交互智能体给出的当前代码事实维护目标，或沿已经正式移交的维护总目标继续；只读源码、工程和正式规范，独占维护 `流程图/主程序可达调用关系/**` 中除能力证据目录外的调用聚合事实，以及 `流程图/代码流程图/**` 中的当前可达函数身份、项目调用边、外部边界、未解析调用、现状单函数流程图、逐行映射、制图队列、覆盖统计和当前代码知识图谱；按批验证、精确提交并推送 | 不修改 `流程图/主程序可达调用关系/能力证据目录/**`、智能体协作规则、项目规范、详细设计、施工流程图、计划、计划索引、代码、工程、自检、施工 / 验证记录；不把现状图或图谱写成目标设计、施工许可、计划状态或能力完成；不直接处理执行或计划支撑消息，不接管具名计划设计包 |
| 能力证据目录维护智能体 | 接收交互智能体给出的能力目录建立或续批目标；只读已发布的当前函数身份、调用图、现状单函数流程图、源码、正式规范和验证证据，独占维护 `流程图/主程序可达调用关系/能力证据目录/**` 的能力条目、能力候选队列、去重关系、状态和覆盖统计；首批完成后按新代码基线和新流程图批次持续增量刷新 | 不修改其它调用图或代码流程图、智能体规则、项目规范、目标业务 / 施工流程图、详细设计、计划、计划索引、代码、工程、自检或记录；不裁决某个新业务必然复用，不把名称相似、函数存在、接口骨架、编译或局部自检写成真实能力完成；不直接处理执行、计划或计划支撑消息 |
| 集成验收与回归智能体 | 接收交互智能体给出的具名验收目标，固定已经提交并推送的 `main` 提交、正式规范 / 设计 / 计划身份和验收矩阵，只读项目事实并在取得验证资源租约后执行全工程构建、自检、跨业务闭环、性能、并发、恢复、长期运行、唯一入口与旧路径退出等被授权验证；独占写入 `验证记录/集成验收/**`，形成可回查证据并精确提交、推送，只向交互智能体返回结构化结果 | 不修改智能体规则、项目规范、流程图、详细设计、知识图谱、计划、计划索引、代码、工程、自检、施工记录或执行智能体专属验证记录；不验收未提交 WIP，不临场修复失败、补造验收标准、扩大验证目标或向计划 / 执行 / 支撑智能体发消息；PASS 不自动改变计划状态，也不单独证明项目完成 |

智能体协作规则修订默认只属于交互智能体。交互智能体的完整权限来自用户指令和智能体规则，不来自项目规范；其它六个智能体仍受表中默认职责约束。项目机器规范属于项目设计依据：计划智能体可以为新计划建立或修订，计划支撑智能体可以在其接管包内建立或修订，交互智能体也可以在用户直接要求时自行形成、登记和发布；两个维护智能体只能把正式规范作为当前代码事实分类与差异标注依据，不取得规范或目标设计写权；集成验收与回归智能体只消费已经提交的正式事实和具名验收合同，不取得设计、计划、状态或代码裁决权。新计划设计包在 `PLAN-PACKAGE-READY-NOTICE` 前由计划智能体拥有；通知后计划支撑智能体取得该包登记轮次的默认写权。交互智能体若需接管同一切片，必须先让原所有者停写并明确交接。登记后的精确版本及正常执行期间保持冻结；任何会改变活动执行计划依据的修订都必须先暂停受影响执行切片并重新 S0。

跨智能体消息只允许：

```text
交互智能体 -> 计划智能体
交互智能体 -> 流程图与知识图谱维护智能体
交互智能体 -> 能力证据目录维护智能体
交互智能体 -> 集成验收与回归智能体
计划智能体 -> 唯一登记的计划支撑智能体
执行智能体 -> 配对的计划支撑智能体
计划支撑智能体 -> 来源计划智能体（只允许登记结果 / 新目标写入释放，不得请求补齐既有计划材料）或配对的执行智能体
流程图与知识图谱维护智能体 -> 交互智能体
能力证据目录维护智能体 -> 交互智能体
集成验收与回归智能体 -> 交互智能体
```

用户直接向任一用户可见智能体窗口发送消息，以及智能体在自身窗口向用户报告，不属于跨智能体消息。任何消息、送达或聊天回执都不是执行许可、停止命令、计划选择、S0 接受或机器事实；接收方必须重读正式仓库与 Git 后自行裁决。流程图与知识图谱维护消息只交接当前代码事实维护范围、基线、队列、续点、差异或阻断，不得请求或裁决计划登记、设计修订或代码实施。`PLAN-PACKAGE-READY-NOTICE` 只说明计划智能体已经推送一份待登记的新计划设计包，必须携带创建侧代码基线、四矩阵 / 机械推演产物路径和独立复核摘要；创建侧 PASS 不等于计划支撑终审通过，计划支撑智能体必须从正式提交和届时当前代码独立复核后才能修改计划索引。计划支撑智能体不得以流程图、详细设计、知识图谱、接口或其它计划内材料缺失为由，向计划智能体发送提供材料或建立提供者包的工作请求。`PLAN-AVAILABLE-NOTICE` 只说明计划支撑智能体已经把合格计划登记为 `可执行` 并推送，用来唤醒执行任务重新扫描候选；`PLAN-SUPPORT-NOTICE` 只说明一份因具名执行问题退回的设计包可能已经正式修订。除非该执行任务已经由用户明确暂停，面向执行智能体的通知送达并产生新轮次后，执行智能体必须在同一轮重读正式状态：没有活动计划段、index clean 且目标代码 / 语义切片未被占用时立即扫描候选并完成 S0；仅有无关且所有者明确的 dirty 不构成阻断，未知、重叠或依赖冲突的 dirty 才以 `DIRTY-BLOCKED` 或 `SLICE-OCCUPIED` 收口；已有活动计划段时不得中断或切换，必须在该段合法收口并发布后把通知纳入继续门禁。

流程图与知识图谱维护智能体每个已发布批次只向交互智能体发送 `GRAPH-MAINTENANCE-PROGRESS`，携带基线、函数变更清单、覆盖统计和下一续点。交互智能体据此向能力证据目录维护智能体发送 `CAPABILITY-CATALOG-REFRESH-NOTICE`；能力目录维护智能体完成候选扫描、去重、条目与统计更新并发布后，只向交互智能体返回 `CAPABILITY-CATALOG-PROGRESS`。两类消息都不裁决计划、设计修订、代码实施或能力完成。

交互智能体只在验收目标、提交基线、验收合同和允许资源已经明确时发送 `INTEGRATION-ACCEPTANCE-REQUEST`。集成验收与回归智能体只返回 `INTEGRATION-ACCEPTANCE-PASS`、`INTEGRATION-ACCEPTANCE-FAIL`、`INTEGRATION-ACCEPTANCE-DRIFT` 或 `INTEGRATION-ACCEPTANCE-BLOCKED`；结果必须绑定提交、输入 blob、验证命令、退出码、证据路径和完成边界。失败或漂移只交给交互智能体分流，不自动请求计划修订、启动执行或改变索引。

顶层任务是平台关系中不属于任何协作代理后代的独立用户可见任务，包括用户直接打开或创建的任务，以及用户授权后由 Codex 任务工具创建并交由用户独立接管的任务。顶层身份按平台父子关系和协作代理关系裁决；任务标题、消息来源、分支、模型、智能体名称和文件路径都不改变该身份。一个顶层任务及其全部直接和递归协作代理共同构成一个“顶层任务树”。

顶层任务树遵守以下最小硬规则：

1. 计划、执行、计划支撑、流程图与知识图谱维护、能力证据目录维护和集成验收与回归任务树在同一时刻只拥有自身一种默认写类型；交互任务树可以按用户直接指令跨产物类型工作，不需要为了修改规范、设计、计划、索引或代码伪造类型切换。
2. 非交互任务树中有写入权的后代只能继承顶层任务树类型；交互任务树后代只能取得顶层交互智能体明确分配的切片。只读后代保持零修改、零构建、零运行、零 Git 写入、零活动计划段所有权和零发布租约。
3. 每个具体文件、结构、接口、验证或证据裁决切片同一时刻只有一个所有者；同类型不等于可以共同写同一切片。
4. 分支、模型、推理等级、名称和递归层级均不改变智能体类型、范围、所有权或许可。
5. 单工作区允许多个路径和语义均不重叠的活动写入切片；Git index、暂存、提交和推送只由一个发布租约所有者串行收口，同一时刻仍只有一个活动代码执行计划段。其它智能体可以继续不相关切片，但不得暂存、提交、推送或占用冲突的构建 / 运行资源。
6. 各顶层任务开工前必须声明文件边界、语义边界、所有者和基线。路径不同但输入规范、设计合同、接口、聚合索引或验证输出存在依赖时仍视为冲突；只停止受影响切片。
7. 发现未授权写入、同文件 / 同语义所有权重叠、发布租约冲突或类型规避时，停止受影响切片并保留 Git 事实；边界明确的无关切片可以继续。
8. 执行智能体的仓库写入上限是当前计划允许的代码、工程、自检，以及该计划唯一专属的施工记录和验证记录。施工记录只允许写入实际 Git 身份、S0、变更文件、验证命令与结果、提交、残余风险和完成边界，不得定义机器语义、修改计划状态、解除依赖或扩大白名单。执行完成、漂移、失败和计划修订请求仍只通过结构化消息交给配对的计划支撑智能体；施工记录不得替代消息、正式计划或 Git 事实。
9. 集成验收与回归智能体的仓库写入上限是 `验证记录/集成验收/**`。它只能验证已提交并推送的精确基线；其它所有者的 dirty 不得作为验收输入，所需源码、工程、配置或运行材料与目标提交不一致时停止，能够从目标提交只读取得且不参与运行的无关 dirty 文档不构成阻断。

存在两个以上边界明确、无真实依赖且所有权可隔离的子任务时，可以使用子智能体并行提速；写入子任务必须预先分配不重叠的文件和语义切片，Git 发布仍由顶层任务指定的唯一发布者串行完成。使用更多智能体不能扩大其它智能体的默认权限、正式计划、计划索引、执行或发布边界，也不能替代现行规范、有效设计、实际接口复核和正式验证。七智能体交互合同、文件所有权、消息矩阵、子智能体 S0、停止与升级统一见 `.codex/rules/设计执行双窗口交互规则.md`；单工作区切片与发布统一见 `.codex/rules/单工作区串行执行与发布规则.md`。

`继续` 是上下文指令：只沿当前已声明或正式登记的智能体类型继续，不自动等于从计划列表选中另一份计划，也不自动跨类型。窗口类型仍不明确时，只做必要的只读判断并说明推断。


计划、执行、计划支撑、流程图与知识图谱维护、能力证据目录维护或集成验收与回归窗口切换智能体类型时，先完成原类型验证与切片交接，再明确目标类型；切换后重新读取该类型的正式入口、Git、相关索引 / 队列、目标依据和实际代码、接口或验收基线。交互窗口按用户直接指令跨产物工作不视为类型切换。

执行智能体完成 S0 并确认 `main == origin/main`、index clean、计划白名单及其依赖语义没有其它所有者后，取得唯一活动代码计划段；无关文档或规则切片可以继续写入。计划段起点、当前 HEAD、远端、允许文件、冻结规范 / 设计 / 接口或所有者不一致时只停止受影响切片，禁止 reset、clean、stash 或覆盖现场。

七智能体状态机、消息协议、单窗口切换和模型升级细则统一见 `.codex/rules/设计执行双窗口交互规则.md`；单工作区写入占用、执行、验证、提交和发布细则统一见 `.codex/rules/单工作区串行执行与发布规则.md`。

## 4. 计划、依赖与并发

1. 交互智能体可以按用户直接指令完成智能体规则、项目规范、流程图、详细设计、知识图谱、计划、计划索引、代码、工程、自检和记录的建立或修订；应先声明切片、确认所有权和依据，再按产物类型验证与发布。用户也可以要求其只讨论、只读扫描或把目标交给计划智能体。
2. 讨论结论和用户裁决不自动成为机器事实。交互智能体直接落盘项目规范或设计时，必须完成证据核对、冲突裁决和正式登记；直接修改代码时仍须具备现行规范、有效设计、计划或用户明确授权的修复边界，不得以完整权限跳过项目语义依据。
3. 计划、目标业务流程图、目标单函数施工流程图、详细设计和验证必须范围一致并互相列明路径。业务流程图一图对应一个业务闭环，冻结输入、输出、前置、业务步骤、分支、事实读写、非成功结果和完成条件；它通过“业务节点—所需能力—候选能力—复用 / 新建裁决—目标函数组”矩阵映射到代码，不要求与一份单函数图一一对应。每份目标单函数施工流程图仍只对应一个具名函数，并冻结完整签名、物理位置、参数绑定、返回结果、稳定节点身份和完整分支。计划智能体或计划支撑智能体必须先完成业务闭环和能力裁决，再逐函数、逐节点形成详细设计；详细设计必须记录业务节点—函数组、函数—节点、数据结构、物理位置、所有权与生命周期、事务 / 版本 / 并发、逻辑内返回 / 内部错误、撤销 / 发布后读回、调用图和验证矩阵。缺失、撤回、未裁决或仍含需要执行智能体决定的设计项时不得登记执行。纯规则、只读扫描或确实不适用设计材料的文档计划，必须写明不适用原因和替代依据。
4. 依赖只允许来自尚未形成且尚未冻结合同的具名正式产物 / 接口，或无法通过拆分共享接线消除的结构与装配所有权先后。同主题、同总计划、同来源、生成 / 文档 / 目录 / 验收顺序均不自动构成依赖。已经冻结为精确接口合同的提供者—消费者关系不再构成“等待提供者实现完成”的开发启动门控。
5. 预冻结接口合同至少列明合同版本、模块与文件身份、公开 DTO / 规格 / 结果类型、完整签名或等价行为、ABI / 规则版本、输入输出所有权与生命周期、逻辑内返回、内部错误、提供计划、消费计划和漂移退回点。提供者拥有合同实现权，消费者只读消费；该合同可以消除设计阶段“等待提供者实现完成”的门控，但不能使消费计划在接口骨架不存在时成为 `可执行`。提供者与消费者仍在唯一主工作区按计划段串行施工，真实接线和完整验证前不得宣称能力已完成。
6. 计划内提供者需求只允许来自当前登记或修订包原目标内必需、当前代码确实不存在、正式规范足以裁决且可冻结为精确合同的函数或等价接口。计划支撑智能体负责记录需求身份、来源计划、调用理由、最终模块 / 文件、最终函数名与签名、DTO、所有权、返回 / 错误、骨架的零机器事实写入 / 零半结构 / 零假成功合同、提供者 / 消费者、验证和漂移点，并直接完成所需现状扫描、规范治理、流程图、逐节点分析、详细设计、知识图谱、提供者施工计划和索引登记。计划内材料缺失不转交计划智能体；超出当前计划原目标时停止并等待用户形成独立新目标。
7. 接口骨架是提供者计划拥有的真实代码切片，只允许使用最终身份。声明足以满足当前施工时不得增加定义；其验证覆盖最终声明、DTO / ABI、模块导入 / 头文件消费及不需要链接定义的编译边界。完整链接确需定义时，定义只能结构化返回“未实现”，不得返回假成功、有效句柄、默认业务对象或伪造数据，不得写仓库、关系、索引、数据库或其它机器事实，不得产生可读半结构；其验证追加全工程链接、调用返回“未实现”和零机器事实 / 结构变化。消费者必须显式传播或收口“未实现”，不得把它转换成成功；消费计划验证矩阵要求真实成功行为时不得由骨架解除待激活。骨架完成后仍不得宣称真实能力完成。
8. 无未合同化真实依赖的计划必须独立登记，并优先按业务对象、文件、结构、公开接口和装配面隔离；隔离后的设计切片可以并行形成，但执行智能体在唯一主工作区一次只消费一份代码计划，消费者不得自行补造同名接口或修改提供者文件。
9. 共享工程、入口、统一运行器和最终装配优先收归一个后继接线 / 切换计划唯一拥有，不能因为多个私有实现最终都要登记工程而把全部业务计划串行。接口合同未冻结、接口骨架尚未真实实现、同一文件 / 结构所有权无法拆分、最终原子切换或物理删除必须消费实际全量结果时，才保留具名门控。
10. 后续计划必须提前形成，不得等待前一个真实能力实现完成后才制定，也不得因生成先后伪造依赖。依赖门控计划必须列明具名依赖产物、无法合同化或隔离的原因、前置计划列表项、解除条件、复核命令和漂移处理；预期产物、预冻结合同和接口骨架不得写成真实能力完成。
11. 依赖未满足时只跳过受影响计划，不标记真实阻塞，也不冻结无关计划；合同批次的提供者或消费者漂移时只停止受影响接口所有者和消费切片，未受影响任务继续。
12. 流程图中的中途非成功结果只分两类：前置通过后内部结构结果异常属于“追根因解决”；协议允许且不产生可读半结构的拒绝、候选为空、等待、显式未实现或人读材料属于“逻辑内返回”。流程图、详细设计和代码必须一致。
13. 面向常规执行模型的详细设计和计划必须机械区分已验证前置事实、预冻结待实现合同、已验证接口骨架、待实施真实能力、候选证据、最终集成证据和具名前置成立后才能判断的退回条件；执行智能体只作当前接口复核和机械施工，不继续完成节点分析、函数设计或机器语义裁决。模型升级不改变智能体类型或文件权限。
14. 新计划的规范、目标业务流程图、业务节点—能力映射、能力复用裁决、目标函数分解与物理位置、目标单函数施工流程图组、逐函数 / 逐节点实现分析、详细设计、目标知识图谱和施工计划形成范围一致的设计包；为创建该计划而执行的目标现状扫描、接口冻结、局部调用图整理和计划拆分同属计划智能体职责，但 `计划/计划索引.md`、全项目当前代码图谱和当前能力证据目录不属于计划智能体写入范围。计划智能体应只读复用两个维护智能体已发布的当前事实；它必须为本包建立“所需能力合同—候选能力—合同差异—复用 / 组合 / 适配 / 扩展 / 新建裁决—验证证据”矩阵。新建或迁移函数必须按 `规范/代码文件建立归属与模块命名规范.md` 冻结权威事实所有者、唯一领域 / 结构所有者、精确文件 / 模块、函数形态、可见性、声明 / 定义位置、插入锚点、允许调用方和依赖方向，不能把物理位置留给执行智能体。发现缺口时可以在自己的设计包内记录具名证据，不得与维护智能体并发改写全项目聚合图谱。计划智能体完成创建侧四矩阵、真实接口行为核对、零代码机械施工推演和独立只读复核，闭合发现的问题并验证、精确提交、推送完整设计包后，才向唯一登记的计划支撑智能体发送一次幂等 `PLAN-PACKAGE-READY-NOTICE`。计划支撑智能体从正式提交和当前代码独立复核范围、版本、blob、具名依赖、能力复用裁决、物理位置、验证合同与全部终审项，不得继承创建侧 PASS；取得该包后，在原目标和登记范围内拥有全部项目设计材料的补建和修订权。若设计包内容发生变化，必须形成、验证、提交并推送新的精确版本 / blob，再独占登记最终版本；接口骨架缺失时，计划支撑智能体直接形成并登记所需提供者设计包 / 施工计划，消费计划保持 `待激活`，满足全部执行前事实时才登记为 `可执行`。叶子计划的每个已登记精确版本 / blob 都是不可变施工合同。
15. `计划/计划索引.md` 是唯一运行导航，默认由计划支撑智能体独占写入；交互智能体在用户直接要求时也可取得该文件的明确切片所有权。索引以列表形式记录计划路径、版本、计划状态和具名依赖；计划之间只登记具名依赖，不另建层级结构。计划状态只允许 `待激活 / 可执行 / 已退出`。`可执行`只证明精确计划版本和 blob 可以被执行智能体选中，不等于已经开始、S0 通过、接口真实能力已完成或取得活动计划段单写所有权；`待激活`表示该版本正在形成、需要修订，或仍等待计划内提供者的接口骨架 / 真实结果满足消费验证门禁，不得被执行智能体选中。任务过程不另建 Markdown 状态台账，以当前单工作区 Git 事实和结构化消息为准。
16. 执行智能体读取计划索引，根据每份计划的状态和具名依赖自主选中一份 `可执行` 且真实依赖已满足的计划，再按 `.codex/rules/` 与 Git 核对目标文件、冻结语义、发布租约和验证资源并完成 S0；计划索引不发布由其它智能体控制的执行序列。唯一执行占用只排斥第二活动代码计划段以及与本计划文件、语义或验证资源冲突的切片，不排斥无关文档写入。计划段选择、S0、完成、DRIFT、失败、WIP、计划修订请求和 `RULE-BLOCKED` 只发送给配对的计划支撑智能体；每条消息必须明确“是否请求设计包修订”。PASS、正常完成、纯代码实施错误和环境问题写 `否`，不转移设计包写权；只有具名计划 / 设计缺口写 `是`。PASS 后直接实施，DRIFT 时停止受影响计划；属于计划 / 设计缺口时发送修订请求。计划支撑只读确认缺口成立后，当前改动全部属于计划白名单且达到单工作区规则定义的可验证安全代码断点时，执行智能体可以不依赖叶子计划预先列出该场景而提交并推送 `DESIGN-DRIFT-WIP`；该提交不得宣称完成，发布后必须释放执行切片并等待计划支撑修订。每个计划段按一个 main 起点、一个允许范围和一个结果提交串行前进；正常结果或普通合法 WIP 收口后继续门禁，`DESIGN-DRIFT-WIP` 则进入具名等待，不切换其它计划。
17. 计划智能体按“逐份设计、逐份创建侧自审、逐份冻结、逐份发布、逐份交给计划支撑登记”创建尚未进入既有计划登记 / 修订范围的新计划及其前置设计产物：每份新设计包必须先闭合创建侧端到端可构造性自审和独立只读复核，才能取得发布租约、提交并推送、发送一次 `PLAN-PACKAGE-READY-NOTICE`；随后停止该包写入，不得同时形成下一份计划。计划智能体不得修改计划索引或直接把计划标为 `可执行`。计划支撑刷新正式仓库和当前代码后独立复核该包，不得把计划智能体的自审结论直接当成终审证据；可在原目标和登记范围内新建或修改全部必要项目设计材料，只有最终范围一致、验证通过、未决项为空且端到端可构造性终审与独立只读终审全部通过时才登记。不存在计划内提供者缺口时登记为 `可执行` 并通知执行智能体；存在缺口时由计划支撑直接形成提供者设计包 / 施工计划，把消费计划登记为 `待激活`，并在提供者计划具备执行前事实后将其登记为 `可执行`；提供者结果满足消费门禁后，消费计划仍须重跑全包终审才能恢复 `可执行`。计划支撑更新索引前只需确认索引文件、相关设计语义和 Git 发布租约无冲突；无关 dirty 或无关写入者不构成阻断。正式登记后的精确设计包保持冻结；执行问题修订仍由计划支撑按具名请求和复核结果处理，修订后必须全包回归而不是只验证具名缺口。
18. 执行智能体在计划段结果 / 普通合法 WIP 提交和推送后，不得把“单计划完成”当作“执行任务完成”。本计划文件和 index 收口后，必须重读正式计划索引、计划 blob、当前 `main` / `origin/main` 与已固定的计划段映射；只要仍有真实依赖满足的 `可执行` 计划，就禁止输出最终答复，必须以当前累计 HEAD 建立下一计划段并重新 S0。`DESIGN-DRIFT-WIP` 例外进入 `DESIGN-DRIFT-WAIT`，不选择其它计划，等待计划支撑修订并发送 `PLAN-SUPPORT-NOTICE`。先前成立的 `NO-CANDIDATE`、DRIFT 排除结论或 idle 状态在新的 `PLAN-AVAILABLE-NOTICE` 或 `PLAN-SUPPORT-NOTICE` 送达并唤醒任务后不得直接沿用，必须以新的正式提交、索引 blob 和计划 blob 重新计算。只有 `NO-CANDIDATE`、`USER-PAUSED`、`DESIGN-DRIFT-WAIT`、`DIRTY-BLOCKED`、`MAIN-DRIFT`、`SLICE-OCCUPIED`、`RELEASE-OCCUPIED`、`VALIDATION-OCCUPIED`、`RULE-BLOCKED` 或 `ENVIRONMENT-BLOCKED` 可以作为具名终止码；终止回执必须附计划索引 blob、HEAD / origin/main、dirty state、候选扫描与逐项排除依据。平台轮次或上下文被迫让出只能报告 `EXECUTION-YIELD` 续点，不得宣称执行任务已经停止或完成。

## 5. 机器逻辑与代码硬边界

机器语义以 0050 和目标对象正式规范为准；下列入口级禁令不得跳过：

1. 函数名、变量名和类内业务成员名必须使用中文。英文只用于 C++ 关键字、标准库、第三方 API、构建字段和不可避免的外部协议字段。
2. 机器逻辑必须由正式结构承载，包括节点直接字段、稳定句柄、关系、领域类型化记录、索引、特征、存在、场景、状态、动态、二次特征、特征值、因果、需求、任务和方法等；日志、注释、文本、显示、线程或锁状态不得承载机器事实。
3. 运行期仓库裁决当前机器事实；数据库和文件只承担快照、事件记录、恢复候选、审计和只读投影，恢复材料必须重新准入后发布。
4. 外部输入、外设采集、控制台、网络消息和数据库记录首先都是材料或请求，不直接成为世界事实。
5. 外部调用方不得绕过领域服务直接写仓库。领域服务公开业务入口不得暴露原始事务令牌、许可、仓库锁或仓库引用；跨仓库写入遵守专用数据操作层、不透明写入会话和最后发布边界。
6. 所有写入口先拒绝无效身份、错误类型、越权、版本漂移和不满足前置条件的输入，再写结构；禁止先写错误 / 半结构，再靠校验、扫描、日志、弹窗或显示修复。
7. 前置条件通过后，内部创建、绑定、写关系、写状态、记录动态、结算、读回或发布不符合预期，属于内部逻辑错误，必须停止依赖路径并追根因，不得静默返回失败后继续。
8. 诊断只做人读提醒，不改变返回值或机器事实。日志工程治理统一读取 `规范/日志系统规范.md`；日志写入 `D:\海中鱼巣\日志`，逻辑错误、运行和调试日志分离；容错 / 调试宏关闭时不得改变运行行为，宏开启后的错误按该入口与现行弹窗设计处理。
9. 线程不是动作来源；方法执行或领域服务写入才可成为动作来源候选。单个因果引用不等于稳定因果结论。
10. 普通 bool、枚举、函数返回码、缓存命中或索引命中不得独立成为一阶机器事实；长期身份使用稳定句柄或主键，指针只作运行期临时引用或缓存。
11. 基础信息服务遵守原子服务与聚合服务分层；特征值服务只由特征服务直接访问，高级服务通过正式基础信息服务取得材料，不新增不存在的 `高级信息` 节点类型。
12. 场景内实例状态和实例动态必须具备存在主体、场景归属和发生时间，不直接充当长期语素入口；长期抽象状态 / 动态及其它对象边界以对应正式规范为准。
13. 需求目标是目标状态，不是 I64；方法执行权限来自任务授权、承接和任务—方法关系，任务、需求和方法的完成 / 满足 / 结算分别裁决。
14. 不把旧项目双向链表模板或整块旧实现复制为新项目核心；当前兼容写入路径不得无计划全仓删除，新生产代码不得新增原始令牌穿透服务路径。

代码编辑同时遵守：

1. 搜索优先使用 `rg`，手工修改使用 `apply_patch`；先确认同功能入口，能扩展既有领域服务时不重复创建。
2. 新增结构必须说明用途、身份、写入方、读取方、生命周期和验证；不新增只为显示方便的机器字段。
3. 删除、重挂和关系变更必须走仓库或领域服务入口。节点移动统一视为普通父子关系重挂，领域服务只做前置拒绝并调用通用关系入口，不重复实现移动算法。
4. 现有目录和代码文件默认原地保留。数据结构文件使用同名 `功能.分类.h/.cpp`；其它新代码使用真实 `功能.分类.ixx` 模块，具备匹配的 `export module`、`ClCompile` 登记和 `import` 调用。
5. 新代码文件必须包含最小文件头部规则注释。生产模块与自检模块分离，自检不得进入生产依赖方向。
6. `入口.cpp` 只保留参数、模式、最小装配、顶层调用和退出码；完整领域实现、自检正文、并发压力和逐项验收输出留在对应模块。
7. 验收、自检、基准和专项检查产生的人读过程信息与结果展示只能通过统一日志系统输出，禁止使用 `std::cout`、`std::cerr`、`printf`、弹窗、界面文本或另建文件作为替代输出通道。对应日志调用必须受具名预编译宏控制，并允许在构建时完全关闭；宏关闭时不得求值仅供日志使用的参数，不得改变自检执行、结构化结果、失败计数、退出码、机器事实或生产行为。日志关闭不等于自检关闭，自检逻辑和验收裁决不得依赖日志内容或日志写入是否成功。
8. 接口骨架必须使用正式计划冻结的最终模块、函数名、DTO 和签名。显式占位定义只能返回结构化“未实现”并保持零机器事实变化；禁止临时同名实现、假成功、默认有效结果、伪数据或通过静态初始化 / 链接副作用绕过提供者所有权。接口骨架计划、真实能力计划和最终接线验证必须分别声明完成边界。

## 6. 验证、提交与推送

文档或规则治理至少运行：

```powershell
git diff --check
python .\tools\check_specs.py --strict
```

代码构建、程序、自检和运行只在当前智能体类型与计划明确授权时执行；构建命令和完整验证矩阵以目标计划为准，当前 Visual Studio 默认构建入口为：

```powershell
msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m
```

验证声明必须区分文档生成、代码编译、入口拒绝、最小闭环、目标能力验收和真实外设样本，不得用低级验证替代高级完成声明。

每轮产生文件改动且本轮范围已完成或按正式退回协议收口后，自动精确提交并非强制推送，不等待额外“提交 / 推送”指令：

```text
核对唯一主工作区、main、Git 状态和无关 WIP
-> 只暂存本轮文件
-> 检查 staged diff 与 diff check
-> 提交
-> 复核提交和剩余 dirty state
-> 核对远端与非快进风险
-> 非强制推送 origin/main
```

交互、计划、执行、计划支撑、流程图与知识图谱维护、能力证据目录维护、集成验收与回归七智能体全部使用唯一主工作区和 `main`。不同文件且语义、接口、聚合入口和验证资源均不冲突时可以并行写入；任一时刻只有一个 Git 发布租约。交互智能体可提交用户直接交给本窗口的任意明确切片；计划智能体默认只提交新计划及其相关设计文件，禁止提交计划索引和全项目当前代码图谱；计划支撑智能体默认提交计划索引及其接管包；执行智能体只提交当前计划允许的代码、工程、自检和专属记录；流程图与知识图谱维护智能体只提交当前代码图谱专属目录；能力证据目录维护智能体只提交其能力目录专属子目录；集成验收与回归智能体只提交 `验证记录/集成验收/**`。每轮必须验证、只暂存本切片、提交并非强制推送 `origin/main`；其它所有者的未暂存 WIP 可以保留，但不得带入提交，也不得宣称全工作区 clean。代码计划要求的计划内验证仍须在执行计划段提交前完成；跨计划、里程碑和最终系统验收由集成验收与回归智能体在目标提交发布后独立执行，不另建集成分支或 worktree。禁止 force / force-with-lease、未经用户明确要求的 merge / rebase / cherry-pick，以及把其它智能体、用户或任务的 WIP 带入提交。

当前不在 `main`、detached HEAD、远端前进、目标切片或发布租约已被占用、无法隔离的同文件 / 同语义改动、验证失败或推送失败时停止受影响操作，不得扩大范围。提交 / 推送只证明产物已进入 `main`，不证明代码或项目阶段完成。

## 7. 规范治理、同步与完成报告

1. `规范/规范目录.md` 是正式机器逻辑规范的唯一索引；未登记文档不取得正式规范身份。正式规范只保留当前有效逻辑，不承载来源整理、窗口调度、Git 状态、详细设计或实现进度。
2. `tools/check_specs.py --strict` 只验证规范入口、编号、链接和退出文件等治理一致性，不证明代码实现、生产接通或能力验收。
3. 与 ChatGPT「理论探讨」项目同步时使用 `.codex/skills/chatgpt-hai-zhong-yu-chao-theory`。理论讨论、同步摘要和草稿不得作为仓库事实或代码许可。
4. 最终回复和跨窗口摘要必须区分已完成、未完成、验证、风险与下一步；不得宣称未发生的构建、运行、提交、推送或验收。
5. 每个完成声明必须给出输入入口、核心处理、结构承载、输出变化、调用路径和验证方式。计划段提交、main 发布、只读复核和规范体系完成各自只证明对应层级；里程碑或最终完成前必须由集成验收与回归智能体对正式具名矩阵形成绑定精确提交的独立结果，并由对应事实所有者在自身权限内基于正式证据逐项裁决。集成验收 PASS 只证明该提交通过该矩阵，不自动证明其它范围或整个项目完成；跨越规则、计划、代码或发布边界的最终结论等待用户裁决。
