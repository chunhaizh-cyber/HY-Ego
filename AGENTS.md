# AGENTS.md

本文件是 `海中鱼巣` 给 Codex、代码代理和协作代理使用的项目级入口规则。这里只保留每轮必须立即知道的权威顺序、智能体权限、停工条件和验证边界；完整状态机、机器语义和工程细则由下列专门规范承载，不在本文件重复展开。

## 1. 项目定位与声明边界

`海中鱼巣` 是对 `鱼巢` 的第二代全量重构实验工程，目标是重建节点直接身份、稳定句柄、关系、领域类型化记录、索引、事务边界、领域服务和最小需求—任务—方法闭环，不复制旧项目文件结构。

当前可以声明：

```text
正式规范体系已建立并通过独立覆盖审计；
机器逻辑和代码语义以本项目规范目录登记的现行正式规范为准；
独立重建的只读控制面板第一版和 SQL Server 结构审计投影第一版已实现并验证。
```

上述事实不等于鱼巢旧能力已经等价覆盖。除非当前正式证据另有证明，不得宣称：

```text
鱼巢旧能力已完成等价重建；
自我循环、自我苏醒或初步成熟已完成；
完整业务操作控制面板或数据库恢复已完成；
外设、D455 或体素已经接入生产闭环。
```

计划列表及每份计划的当前状态读取 `计划/计划索引.md`。

## 2. 权威顺序与开工前读取

每次修改项目文件前，先限载读取：

```text
计划/计划索引.md
规范/规范目录.md
规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md
规范/0100_编号规则与重排预留说明_20260720.md
规范/0300_规范冲突与前后矛盾清单_20260720.md
```

按任务追加读取：

- 执行计划：目标计划、计划索引中的计划状态与具名依赖、当前单工作区 Git 事实和最新结构化执行回执。
- 建立或修订机器逻辑：目标对象根规范、直接相关子规范和相邻边界。
- 单工作区写入占用、执行、提交和发布：`.codex/rules/单工作区串行执行与发布规则.md`。
- 旧路径兼容声明：`.codex/rules/多工作树并发与集成规则.md`；该文件只声明旧机制已退出，不提供任何 worktree 授权。
- 智能体路由、交接或状态裁决：`.codex/rules/设计执行双窗口交互规则.md`。
- 修改代码：目标代码文件头部规则、相关正式规范、有效设计、现行计划和执行前实际接口。
- 新建代码文件：追加读取 `规范/代码文件建立归属与模块命名规范.md`。
- 修改日志、诊断宏或日志格式：追加读取 `规范/日志系统规范.md`。

权力关系固定为：

```text
用户本轮明确裁决 -> 原则上先进入设计治理；用户明确要求交互窗口直接修复其当前未提交代码时，适用下述直接修复例外
AGENTS.md -> 代理协作、智能体类型和执行边界
规范/规范目录.md 及其登记规范 -> 机器逻辑和代码语义的唯一正式定义
流程图、详细设计 -> 在规范内解释实现，不得覆盖规范
叶子计划 -> 不可变施工合同，冻结目标、依据、允许 / 禁止范围、失败收口和验证合同
计划索引 -> 记录计划列表、每份计划的状态、路径 / 版本和具名依赖，不改变系统口径
代码与 Git -> 当前实现事实；与规范不一致时属于待治理差异
README、日志、控制台、界面和聊天 -> 人读材料，不裁决机器事实
```

`D:\鱼巢` 的规范与代码、已退出迁移材料、旧实现、过期计划 / 流程图 / 详细设计和历史记录只作来源、差距与风险证据。只有经过海中鱼巣结构映射、冲突裁决并登记到 `规范/规范目录.md` 的规则，才取得本项目机器语义权威。

代码实施依据链必须完整：

```text
现行正式规范
-> 有效的单函数流程图组
-> 按每个流程图函数及其节点完成实现分析和函数功能确认
-> 完整详细设计
-> 已登记现行计划和计划索引
-> 执行前实际接口或接口骨架复核；派生需求的提供者计划可以把正式登记的预冻结合同作为待实现目标
-> 代码修改、验证、Git 提交和结构化执行回执
```

流程图是详细设计的上游依据，完整详细设计是执行智能体写代码的直接依据。代码实施类有效流程图必须遵守“一个流程图只对应一个函数”：图的身份中冻结根函数、归属模块 / 文件、完整参数和返回结果；无参数或 `void` 也必须显式记录。图内每个节点只能是“调用另一个具名函数”或“本函数内的一条具体指令”，模块、服务、仓库和抽象阶段只能作为归属 / 分组信息，不能冒充可执行节点。被调函数存在非平凡内部过程时，必须另有自己的单函数流程图并双向引用；跨多个函数的业务过程只能由流程图组和调用图表达，不得压成一张大图。禁止执行智能体根据流程图自行决定函数、结构、类型、条件、版本、所有权、调用顺序或错误处理；如果流程图函数合同、节点类型、参数 / 结果、节点实现分析或所需函数尚未冻结，或详细设计仍要求施工者作设计裁决，关联计划不得登记为 `可执行`。从流程图形成详细设计的逐函数、逐节点方法、必填内容、子智能体分工和 S0 门禁统一读取 `.codex/rules/设计执行双窗口交互规则.md`。

预冻结接口合同只允许在设计阶段替代“提供者实现已经存在”这一项事实，不替代正式规范、有效设计、计划、所有权、真实接口骨架或最终真实接线验证。合同必须明确标为待实现接口，不得伪写成当前代码已实现。计划支撑智能体在所接管计划的原目标内发现必需函数不存在时，可以先核对正式规范、模块边界和可实现性，冻结精确待实现接口合同，建立具名派生需求，并使当前消费设计继续按该合同推演；但消费计划在提供结果足以满足其当前阶段验证矩阵前只能登记为 `待激活`。接口骨架使用最终模块、函数名、DTO 和签名；只有完整链接确有需要时才允许提供显式定义，且必须结构化返回“未实现”、零机器事实写入、零半结构、零假成功。接口骨架已实现只证明接口和链接边界存在，不证明真实能力完成。

派生需求由计划支撑智能体在当前包内正式记录、验证、提交并推送后，通过带目标来源计划任务身份和幂等关联键的 `DERIVED-REQUIREMENT-NOTICE` 交给来源计划智能体；计划智能体只据此创建新的提供者设计包，不得改写消费包或计划索引。已经发布而尚未交回提供者设计包的派生需求优先于普通下一计划，不得同时再用 `PLAN-WRITE-RELEASE` 授权另一目标。任一环节缺失、失效、冲突或漂移时停止受影响切片：智能体协作规则问题等待用户交给交互智能体；新计划及为形成新计划所需的现状扫描、流程图、详细设计、接口冻结和计划拆分由计划智能体处理；只有已经存在的计划在执行过程中出现具名计划 / 设计问题并由执行智能体明确请求修订后，才由配对计划支撑智能体处理。不得由旧实现、迁移材料、日志、施工记录或执行智能体临时推断补造规则。

## 3. 智能体、窗口与“继续”

项目采用交互、计划、执行、计划支撑四智能体。全部智能体读取同一个仓库工作区，不使用 Git / Codex worktree；智能体权限不因使用同一物理窗口、不同模型、分支、子智能体或 Full access 而合并。

| 智能体 | 唯一职责与写入权 | 主要禁止 |
| --- | --- | --- |
| 交互智能体 | 与用户交互、澄清目标并独占修订智能体协作规则，包括 `AGENTS.md`、`.codex/rules/` 和 `.codex/skills/`；用户明确要求本窗口直接修复其当前未提交代码时，可在暂停其它写入者后独占修改该用户代码切片所需的代码、工程和自检并完成构建 / 运行验证；只有确实需要形成项目设计包时才向计划智能体发送目标 | 不修改项目机器规范、流程图、详细设计、计划、计划索引或施工记录，不向执行或计划支撑智能体发送消息；直接修复不得扩大为未获授权的新功能或机器语义设计 |
| 计划智能体 | 接收交互智能体目标、沿既有总目标推导的后续目标，或计划支撑智能体正式发布的 `DERIVED-REQUIREMENT-NOTICE`；只创建新的计划及其所需的现状扫描、规范治理、流程图、逐节点分析、详细设计、知识图谱、接口冻结和计划拆分；每份完整新设计包验证、提交并推送后，向唯一登记的计划支撑智能体发送 `PLAN-PACKAGE-READY-NOTICE` | 不修改计划索引、智能体协作规则、代码 / 工程 / 自检或施工记录；不直接处理执行消息，不登记计划状态，不直接通知执行智能体，不主动改写已经正式发布或已由计划支撑接管的设计包 |
| 执行智能体 | 在唯一主工作区中自主读取 `可执行` 计划、自行完成 S0，并作为唯一代码写入者只修改当前计划允许的代码、工程、自检、专属施工记录和验证记录；所有执行事实和计划问题只发送给配对的计划支撑智能体 | 不修改智能体协作规则、规范、流程图、详细设计、计划或计划索引，不向交互或计划智能体发送消息，不在另一智能体持有单工作区写入占用时施工 |
| 计划支撑智能体 | 独占维护 `计划/计划索引.md`；接收计划智能体的 `PLAN-PACKAGE-READY-NOTICE`，复核已推送的新计划设计包，并可在该计划原目标和登记范围内按需新建或修改必要设计文件；遇到当前目标必需但尚不存在的函数时，可冻结待实现接口合同、建立派生需求、把消费计划登记为 `待激活`，并在 clean 释放写入权后向来源计划智能体发送 `DERIVED-REQUIREMENT-NOTICE`；提供者实现后再按消费合同复核是否足以把消费计划转为 `可执行`；同时只读接收配对执行智能体的执行事实，只有具名计划 / 设计问题明确请求修订且复核成立时才接管既有设计包修订 | 不得独立设计或实施派生需求的提供者计划，不得发起与当前登记或修订无关的新计划、扩大用户目标或跨包治理；不得把合同、声明、显式“未实现”定义或编译链接通过写成真实能力完成；正常 S0 PASS、正常施工、完成回执、纯代码实现错误或环境问题不得触发设计包修订；不修改智能体协作规则、代码 / 工程 / 自检或施工记录 |

智能体协作规则修订只属于交互智能体；项目机器规范属于设计包，不属于智能体协作规则。计划智能体可以为新计划建立或修订项目机器规范；计划支撑智能体在接管新计划登记或执行问题修订后，可以在原目标不变且证据充分时新建或修订该设计包所需的项目机器规范及其它必要文件。新计划设计包在计划智能体发送 `PLAN-PACKAGE-READY-NOTICE` 前由计划智能体拥有；通知后计划支撑智能体只在完成写入占用复核后取得该包登记轮次的唯一写权。登记后的精确版本及正常执行期间保持冻结；具名执行问题修订请求成立后，受影响设计包的修订权才唯一转给配对计划支撑智能体。两者遇到需要改变用户目标、无法裁决的机器语义或智能体协作规则问题时，必须停止受影响切片，在自身用户可见窗口列出问题并等待用户直接消息；不得代改智能体协作规则或跨智能体转发。

跨智能体消息只允许：

```text
交互智能体 -> 计划智能体
计划智能体 -> 唯一登记的计划支撑智能体
执行智能体 -> 配对的计划支撑智能体
计划支撑智能体 -> 来源计划智能体或配对的执行智能体
```

用户直接向任一用户可见智能体窗口发送消息，以及智能体在自身窗口向用户报告，不属于跨智能体消息。任何消息、送达或聊天回执都不是执行许可、停止命令、计划选择、S0 接受或机器事实；接收方必须重读正式仓库与 Git 后自行裁决。`PLAN-PACKAGE-READY-NOTICE` 只说明计划智能体已经推送一份待登记的新计划设计包；计划支撑智能体必须复核正式提交后才能修改计划索引。`DERIVED-REQUIREMENT-NOTICE` 只说明计划支撑智能体已正式记录一项来源明确的派生需求并释放写入权，允许来源计划智能体为该需求创建一份新的提供者设计包；它不证明接口、骨架或能力已经实现。`PLAN-AVAILABLE-NOTICE` 只说明计划支撑智能体已经把合格计划登记为 `可执行` 并推送，用来唤醒执行任务重新扫描候选；`PLAN-SUPPORT-NOTICE` 只说明一份因具名执行问题退回的设计包可能已经正式修订。除非该执行任务已经由用户明确暂停，面向执行智能体的通知送达并产生新轮次后，执行智能体必须在同一轮重读正式状态：没有活动计划段、单工作区 / index clean 且没有其它写入占用时立即扫描候选并完成 S0；没有活动计划段但 dirty 时以 `DIRTY-BLOCKED` 收口；已有活动计划段时不得中断或切换，必须在该段合法收口、提交推送并恢复 clean 后把通知纳入继续门禁。

顶层任务是平台关系中不属于任何协作代理后代的独立用户可见任务，包括用户直接打开或创建的任务，以及用户授权后由 Codex 任务工具创建并交由用户独立接管的任务。顶层身份按平台父子关系和协作代理关系裁决；任务标题、消息来源、分支、模型、智能体名称和文件路径都不改变该身份。一个顶层任务及其全部直接和递归协作代理共同构成一个“顶层任务树”。

顶层任务树遵守以下最小硬规则：

1. 同一顶层任务树在同一时刻只能拥有交互、计划、执行、计划支撑中的一种写类型；顶层任务只读时整树只能只读。
2. 全部有写入权的后代只能继承顶层任务树的当前智能体类型；只读后代保持零修改、零构建、零运行、零 Git 写入、零活动计划段所有权和零发布占用。
3. 每个具体文件、结构、接口、验证或证据裁决切片同一时刻只有一个所有者；同类型不等于可以共同写同一切片。
4. 分支、模型、推理等级、名称和递归层级均不改变智能体类型、范围、所有权或许可。
5. 单工作区写入占用、Git index、暂存、提交、推送和活动计划段只由唯一指定智能体串行收口；其它智能体及其后代在占用期间停止写入。
6. 跨写类型默认交给独立顶层任务。只有用户明确启用同一物理窗口串行切换时，才可在全部后代停止、旧类型记录与验证完成、Git 和占用释放并正式重新路由后整树切换；规则修订无论如何都只能切换为交互智能体。
7. 发现一棵任务树存在两种写类型、未授权写入、所有权重叠或类型规避时，整树立即停止新增写入，保留现有 Git 事实，在自身用户可见窗口列出问题并等待用户裁决；不得以文件无交集、结果正确或最终可合并继续。
8. 执行智能体的仓库写入上限是当前计划允许的代码、工程、自检，以及该计划唯一专属的施工记录和验证记录。施工记录只允许写入实际 Git 身份、S0、变更文件、验证命令与结果、提交、残余风险和完成边界，不得定义机器语义、修改计划状态、解除依赖或扩大白名单。执行完成、漂移、失败和计划修订请求仍只通过结构化消息交给配对的计划支撑智能体；施工记录不得替代消息、正式计划或 Git 事实。

存在两个以上边界明确、无真实依赖且所有权可隔离的只读子任务时，应优先使用只读子智能体并行提速；写入任务即使同类型也必须在唯一主工作区串行，不得制造空任务或伪造并行。使用更多智能体不能扩大智能体权限、正式计划、计划索引、执行或发布边界，也不能替代现行规范、有效设计、实际接口复核和正式验证。四智能体交互合同、文件所有权、消息矩阵、子智能体 S0、停止与升级统一见 `.codex/rules/设计执行双窗口交互规则.md`；单工作区占用、提交和发布统一见 `.codex/rules/单工作区串行执行与发布规则.md`。

`继续` 是上下文指令：只沿当前已声明或正式登记的智能体类型继续，不自动等于从计划列表选中另一份计划，也不自动跨类型。窗口类型仍不明确时，只做必要的只读判断并说明推断。


同一物理窗口切换智能体类型时，先完成原类型验证、提交和推送，再明确目标类型；切换后重新读取该类型的正式入口、Git、计划索引、目标计划和实际接口。聊天中的口头切换不生效。

执行智能体完成 S0 并确认唯一主工作区 clean、`main == origin/main` 且没有其它写入者后，取得活动计划段写入占用；释放前其它智能体只能只读。计划段起点、当前 HEAD、远端、允许文件或所有者不一致时停止，禁止 reset、clean、stash 或覆盖现场。

四智能体状态机、消息协议、单窗口切换和模型升级细则统一见 `.codex/rules/设计执行双窗口交互规则.md`；单工作区写入占用、执行、验证、提交和发布细则统一见 `.codex/rules/单工作区串行执行与发布规则.md`。

## 4. 计划、依赖与并发

1. 交互智能体可以直接完成智能体协作规则修订、目标讨论、解释和只读事实扫描。用户明确要求本窗口直接修复其当前未提交代码时，交互智能体可以取得该精确代码切片的唯一写入权，保留用户修改意图并通过编译、链接和必要运行验证排除错误；不得用 reset、checkout、clean、stash 或整文件恢复覆盖用户现场。确实需要形成项目设计包时，交互智能体只向计划智能体发送目标，不代写项目机器规范、流程图、详细设计或计划。
2. 讨论结论、用户裁决、流程图或详细设计不自动成为代码执行项。涉及新功能、字段、函数、能力重建或机器结构变化时，必须先形成并登记计划，列明允许文件、禁止文件、入口拒绝、失败收口和验证方式。交互智能体直接修复例外只允许恢复当前用户代码切片的编译、链接和既有行为，不得据此发明新机器语义；需要设计裁决时停止并转回设计治理。
3. 计划、流程图和详细设计必须范围一致并互相列明路径。每份代码实施类流程图只对应一个具名函数，必须具备函数完整签名、参数绑定、返回结果、稳定节点身份和完整分支；图内节点只能标为被调函数或本函数内具体指令，被调函数节点记录完整调用与参数 / 结果传递，指令节点记录具体判断、顺序操作、循环控制、事务操作或返回，不得使用“处理 / 服务 / 模块 / 阶段”等抽象占位。计划智能体或计划支撑智能体必须逐函数、逐节点核对正式规范与当前代码，确认节点职责、条件、输入输出、事实读写、调用与事务、结果和验证，再形成详细设计。详细设计必须记录流程图函数—节点（被调函数 / 指令）双向映射、完整函数合同、数据结构、所有权与生命周期、事务 / 版本 / 并发、逻辑内返回 / 内部错误、撤销 / 发布后读回、代码文件与调用图和验证矩阵。缺失、撤回、未裁决、范围不一致或仍含需要执行智能体决定的设计项时不得登记执行。纯规则、只读扫描或确实不适用设计材料的文档计划，必须写明不适用原因和替代依据。
4. 依赖只允许来自尚未形成且尚未冻结合同的具名正式产物 / 接口，或无法通过拆分共享接线消除的结构与装配所有权先后。同主题、同总计划、同来源、生成 / 文档 / 目录 / 验收顺序均不自动构成依赖。已经冻结为精确接口合同的提供者—消费者关系不再构成“等待提供者实现完成”的开发启动门控。
5. 预冻结接口合同至少列明合同版本、模块与文件身份、公开 DTO / 规格 / 结果类型、完整签名或等价行为、ABI / 规则版本、输入输出所有权与生命周期、逻辑内返回、内部错误、提供计划、消费计划和漂移退回点。提供者拥有合同实现权，消费者只读消费；该合同可以消除设计阶段“等待提供者实现完成”的门控，但不能使消费计划在接口骨架不存在时成为 `可执行`。提供者与消费者仍在唯一主工作区按计划段串行施工，真实接线和完整验证前不得宣称能力已完成。
6. 派生需求只允许来自当前登记或修订包原目标内必需、当前代码确实不存在、正式规范足以裁决且可冻结为精确合同的函数或等价接口。计划支撑智能体负责记录需求身份、来源计划、调用理由、最终模块 / 文件、最终函数名与签名、DTO、所有权、返回 / 错误、骨架的零机器事实写入 / 零半结构 / 零假成功合同、提供者 / 消费者、验证和漂移点；不得自行完成提供者设计。记录发布并恢复 clean 后，计划支撑智能体用 `DERIVED-REQUIREMENT-NOTICE` 把新提供者设计目标交给来源计划智能体。
7. 接口骨架是提供者计划拥有的真实代码切片，只允许使用最终身份。声明足以满足当前施工时不得增加定义；其验证覆盖最终声明、DTO / ABI、模块导入 / 头文件消费及不需要链接定义的编译边界。完整链接确需定义时，定义只能结构化返回“未实现”，不得返回假成功、有效句柄、默认业务对象或伪造数据，不得写仓库、关系、索引、数据库或其它机器事实，不得产生可读半结构；其验证追加全工程链接、调用返回“未实现”和零机器事实 / 结构变化。消费者必须显式传播或收口“未实现”，不得把它转换成成功；消费计划验证矩阵要求真实成功行为时不得由骨架解除待激活。骨架完成后仍不得宣称真实能力完成。
8. 无未合同化真实依赖的计划必须独立登记，并优先按业务对象、文件、结构、公开接口和装配面隔离；隔离只用于明确所有权和施工顺序，不再授权并行写入。执行智能体在唯一主工作区一次只消费一份计划，消费者不得自行补造同名接口或修改提供者文件。
9. 共享工程、入口、统一运行器和最终装配优先收归一个后继接线 / 切换计划唯一拥有，不能因为多个私有实现最终都要登记工程而把全部业务计划串行。接口合同未冻结、接口骨架尚未真实实现、同一文件 / 结构所有权无法拆分、最终原子切换或物理删除必须消费实际全量结果时，才保留具名门控。
10. 后续计划必须提前形成，不得等待前一个真实能力实现完成后才制定，也不得因生成先后伪造依赖。依赖门控计划必须列明具名依赖产物、无法合同化或隔离的原因、前置计划列表项、解除条件、复核命令和漂移处理；预期产物、预冻结合同和接口骨架不得写成真实能力完成。
11. 依赖未满足时只跳过受影响计划，不标记真实阻塞，也不冻结无关计划；合同批次的提供者或消费者漂移时只停止受影响接口所有者和消费切片，未受影响任务继续。
12. 流程图中的中途非成功结果只分两类：前置通过后内部结构结果异常属于“追根因解决”；协议允许且不产生可读半结构的拒绝、候选为空、等待、显式未实现或人读材料属于“逻辑内返回”。流程图、详细设计和代码必须一致。
13. 面向常规执行模型的详细设计和计划必须机械区分已验证前置事实、预冻结待实现合同、已验证接口骨架、待实施真实能力、候选证据、最终集成证据和具名前置成立后才能判断的退回条件；执行智能体只作当前接口复核和机械施工，不继续完成节点分析、函数设计或机器语义裁决。模型升级不改变智能体类型或文件权限。
14. 新计划的规范、单函数流程图组、逐函数 / 逐节点实现分析、函数功能确认、详细设计、知识图谱和施工计划形成范围一致的设计包；为创建该计划而执行的现状扫描、接口冻结、调用图整理和计划拆分同属计划智能体职责，但 `计划/计划索引.md` 不属于计划智能体写入范围。计划智能体验证、精确提交并推送完整设计包后，向唯一登记的计划支撑智能体发送一次幂等 `PLAN-PACKAGE-READY-NOTICE`。计划支撑智能体从正式提交复核范围、版本、blob、具名依赖和验证合同；可以在该计划原目标和登记范围内按需新建或修改必要设计文件。若设计包内容发生变化，必须形成、验证、提交并推送新的精确版本 / blob，再独占登记最终版本；接口骨架缺失时登记为 `待激活` 并发布派生需求，满足全部执行前事实时登记为 `可执行`。叶子计划的每个已登记精确版本 / blob 都是不可变施工合同。
15. `计划/计划索引.md` 是唯一运行导航，并由计划支撑智能体独占写入，以列表形式记录计划路径、版本、计划状态和具名依赖；计划之间只登记具名依赖，不另建层级结构。计划状态只允许 `待激活 / 可执行 / 已退出`。`可执行`只证明精确计划版本和 blob 可以被执行智能体选中，不等于已经开始、S0 通过、接口真实能力已完成或取得活动计划段单写所有权；`待激活`表示该版本正在形成、需要修订，或仍等待派生需求的接口骨架真实实现，不得被执行智能体选中。任务过程不另建 Markdown 状态台账，以当前单工作区 Git 事实和结构化消息为准。
16. 执行智能体读取计划索引，根据每份计划的状态和具名依赖自主选中一份 `可执行` 且真实依赖已满足的计划，再按 `.codex/rules/` 与 Git 核对唯一主工作区、写入占用并完成 S0；计划索引不发布由其它智能体控制的执行序列。唯一执行占用具体表现为当前工作区没有第二写入者或第二活动计划段。计划段选择、S0、完成、DRIFT、失败、WIP、计划修订请求和 `RULE-BLOCKED` 只发送给配对的计划支撑智能体；每条消息必须明确“是否请求设计包修订”。PASS、正常完成、纯代码实施错误和环境问题写 `否`，不转移设计包写权；只有具名计划 / 设计缺口写 `是`。PASS 后直接实施，DRIFT 时停止受影响计划；属于计划 / 设计缺口时发送修订请求。每个计划段按一个 main 起点、一个允许范围和一个结果提交串行前进；计划正常收口或形成计划允许的 clean 代码断点后必须立即运行继续门禁并选择下一份，不需要逐计划重建工作区、分支、顶层任务或取得逐计划许可。
17. 计划智能体按“逐份设计、逐份冻结、逐份推送、逐份交给计划支撑登记”创建新计划及其前置设计产物：每完成一份新计划设计包就正式提交并推送，向唯一登记的计划支撑智能体发送一次 `PLAN-PACKAGE-READY-NOTICE`，随后立即停写，不得同时形成下一份计划。计划智能体不得修改计划索引或直接把计划标为 `可执行`。计划支撑智能体收到通知后刷新正式仓库；可在该计划原目标和登记范围内按需新建或修改必要设计文件，并对发生变化的设计包形成新的精确版本 / blob。只有最终范围一致、验证通过且未决项为空的精确版本才可登记；不存在派生接口缺口时登记为 `可执行` 并通知执行智能体；存在缺口时登记为 `待激活`，正式发布派生需求并把 clean 写入权交给来源计划智能体创建新的提供者计划。提供者计划可以直接实现真实能力，也可以在派生合同明确允许时先实现接口骨架。计划支撑智能体向来源计划智能体返回 `PLAN-INDEX-RESULT`，并在适用时追加 `DERIVED-REQUIREMENT-NOTICE`。提供者实施、验证、提交推送并 clean 收口后，计划支撑智能体先只读复核正式代码和消费合同；待执行智能体没有活动计划段且工作区 / index clean 后，真实能力已满足，或消费计划当前阶段明确允许结构化“未实现”并且骨架足以通过其验证矩阵时，才可取得索引写入占用，把消费计划转为 `可执行`、提交推送并发送 `PLAN-AVAILABLE-NOTICE`；否则保持 `待激活` 且不改索引。未消费的派生提供者授权阻止普通 `PLAN-WRITE-RELEASE`。正式登记后的精确设计包保持冻结；执行问题修订仍由计划支撑智能体按具名请求和复核结果处理，并可在受影响包内按需新建必要文件。
18. 执行智能体在计划段结果 / 合法 WIP 提交和推送后，不得把“单计划完成”当作“执行任务完成”。单工作区 / index 恢复 clean 后，必须重读正式计划索引、计划 blob、当前 `main` / `origin/main` 与已固定的计划段映射；只要仍有真实依赖满足的 `可执行` 计划，就禁止输出最终答复，必须以当前累计 HEAD 建立下一计划段并重新 S0。先前成立的 `NO-CANDIDATE`、DRIFT 排除结论或 idle 状态在新的 `PLAN-AVAILABLE-NOTICE` 或 `PLAN-SUPPORT-NOTICE` 送达并唤醒任务后不得直接沿用，必须以新的正式提交、索引 blob 和计划 blob 重新计算。只有 `NO-CANDIDATE`、`USER-PAUSED`、`DIRTY-BLOCKED`、`MAIN-DRIFT`、`WRITE-OCCUPIED`、`RULE-BLOCKED` 或 `ENVIRONMENT-BLOCKED` 可以作为具名终止码；终止回执必须附计划索引 blob、HEAD / origin/main、dirty state、候选扫描与逐项排除依据。平台轮次或上下文被迫让出只能报告 `EXECUTION-YIELD` 续点，不得宣称执行任务已经停止或完成。

## 5. 机器逻辑与代码硬边界

机器语义以 0050 和目标对象正式规范为准；下列入口级禁令不得跳过：

1. 函数名、变量名和类内业务成员名必须使用中文。英文只用于 C++ 关键字、标准库、第三方 API、构建字段和不可避免的外部协议字段。
2. 机器逻辑必须由正式结构承载，包括节点直接字段、稳定句柄、关系、领域类型化记录、索引、特征、存在、场景、状态、动态、二次特征、特征值、因果、需求、任务和方法等；日志、注释、文本、显示、线程或锁状态不得承载机器事实。
3. 运行期仓库裁决当前机器事实；数据库和文件只承担快照、事件记录、恢复候选、审计和只读投影，恢复材料必须重新准入后发布。
4. 外部输入、外设采集、控制台、网络消息和数据库记录首先都是材料或请求，不直接成为世界事实。
5. 外部调用方不得绕过领域服务直接写仓库。领域服务公开业务入口不得暴露原始事务令牌、许可、仓库锁或仓库引用；跨仓库写入遵守专用数据操作层、不透明写入会话和最后发布边界。
6. 所有写入口先拒绝无效身份、错误类型、越权、版本漂移和不满足前置条件的输入，再写结构；禁止先写错误 / 半结构，再靠校验、扫描、日志、弹窗或显示修复。
7. 前置条件通过后，内部创建、绑定、写关系、写状态、记录动态、结算、读回或发布不符合预期，属于内部逻辑错误，必须停止依赖路径并追根因，不得静默返回失败后继续。
8. 诊断只做人读提醒，不改变返回值或机器事实。日志工程治理统一读取 `规范/日志系统规范.md`；日志写入 `D:\海中鱼巣\日志`，逻辑错误、运行和调试日志分离；容错 / 调试宏关闭时不得改变运行行为，宏开启后的错误按该入口与现行弹窗设计处理。
9. 线程不是动作来源；方法执行或领域服务写入才可成为动作来源候选。单个因果引用不等于稳定因果结论。
10. 普通 bool、枚举、函数返回码、缓存命中或索引命中不得独立成为一阶机器事实；长期身份使用稳定句柄或主键，指针只作运行期临时引用或缓存。
11. 基础信息服务遵守原子服务与聚合服务分层；特征值服务只由特征服务直接访问，高级服务通过正式基础信息服务取得材料，不新增不存在的 `高级信息` 节点类型。
12. 场景内实例状态和实例动态必须具备存在主体、场景归属和发生时间，不直接充当长期语素入口；长期抽象状态 / 动态及其它对象边界以对应正式规范为准。
13. 需求目标是目标状态，不是 I64；方法执行权限来自任务授权、承接和任务—方法关系，任务、需求和方法的完成 / 满足 / 结算分别裁决。
14. 不把旧项目双向链表模板或整块旧实现复制为新项目核心；当前兼容写入路径不得无计划全仓删除，新生产代码不得新增原始令牌穿透服务路径。

代码编辑同时遵守：

1. 搜索优先使用 `rg`，手工修改使用 `apply_patch`；先确认同功能入口，能扩展既有领域服务时不重复创建。
2. 新增结构必须说明用途、身份、写入方、读取方、生命周期和验证；不新增只为显示方便的机器字段。
3. 删除、重挂和关系变更必须走仓库或领域服务入口。节点移动统一视为普通父子关系重挂，领域服务只做前置拒绝并调用通用关系入口，不重复实现移动算法。
4. 现有目录和代码文件默认原地保留。数据结构文件使用同名 `功能.分类.h/.cpp`；其它新代码使用真实 `功能.分类.ixx` 模块，具备匹配的 `export module`、`ClCompile` 登记和 `import` 调用。
5. 新代码文件必须包含最小文件头部规则注释。生产模块与自检模块分离，自检不得进入生产依赖方向。
6. `入口.cpp` 只保留参数、模式、最小装配、顶层调用和退出码；完整领域实现、自检正文、并发压力和逐项验收输出留在对应模块。
7. 验收、自检、基准和专项检查产生的人读过程信息与结果展示只能通过统一日志系统输出，禁止使用 `std::cout`、`std::cerr`、`printf`、弹窗、界面文本或另建文件作为替代输出通道。对应日志调用必须受具名预编译宏控制，并允许在构建时完全关闭；宏关闭时不得求值仅供日志使用的参数，不得改变自检执行、结构化结果、失败计数、退出码、机器事实或生产行为。日志关闭不等于自检关闭，自检逻辑和验收裁决不得依赖日志内容或日志写入是否成功。
8. 接口骨架必须使用正式计划冻结的最终模块、函数名、DTO 和签名。显式占位定义只能返回结构化“未实现”并保持零机器事实变化；禁止临时同名实现、假成功、默认有效结果、伪数据或通过静态初始化 / 链接副作用绕过提供者所有权。接口骨架计划、真实能力计划和最终接线验证必须分别声明完成边界。

## 6. 验证、提交与推送

文档或规则治理至少运行：

```powershell
git diff --check
python .\tools\check_specs.py --strict
```

代码构建、程序、自检和运行只在当前智能体类型与计划明确授权时执行；构建命令和完整验证矩阵以目标计划为准，当前 Visual Studio 默认构建入口为：

```powershell
msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m
```

验证声明必须区分文档生成、代码编译、入口拒绝、最小闭环、目标能力验收和真实外设样本，不得用低级验证替代高级完成声明。

每轮产生文件改动且本轮范围已完成或按正式退回协议收口后，自动精确提交并非强制推送，不等待额外“提交 / 推送”指令：

```text
核对唯一主工作区、main、Git 状态和无关 WIP
-> 只暂存本轮文件
-> 检查 staged diff 与 diff check
-> 提交
-> 复核提交和剩余 dirty state
-> 核对远端与非快进风险
-> 非强制推送 origin/main
```

交互、计划、执行、计划支撑四智能体全部使用唯一主工作区和 `main`，但任一时刻只有一个写入者。交互智能体提交协作规则，以及用户明确交给本窗口的当前未提交代码直接修复结果；计划智能体只提交新计划及其相关设计文件，禁止提交计划索引；计划支撑智能体独占提交计划索引登记，并可在当前登记包或执行侧具名请求且复核成立的既有设计包范围内提交按需新建或修改的必要文件；执行智能体只提交当前计划允许的代码、工程、自检和专属记录。每轮必须验证、精确暂存、提交并非强制推送 `origin/main`，工作区恢复 clean 后才释放占用。代码计划要求的集成级验证必须在执行计划段提交前完成，不再另建集成分支或 worktree。禁止 force / force-with-lease、未经用户明确要求的 merge / rebase / cherry-pick，以及把其它智能体、用户或任务的 WIP 带入提交。

当前不在 `main`、detached HEAD、远端前进、已有写入占用、无法隔离的同文件改动、验证失败或推送失败时停止并报告，不得扩大操作。提交 / 推送只证明产物已进入 `main`，不证明代码或项目阶段完成。

## 7. 规范治理、同步与完成报告

1. `规范/规范目录.md` 是正式机器逻辑规范的唯一索引；未登记文档不取得正式规范身份。正式规范只保留当前有效逻辑，不承载来源整理、窗口调度、Git 状态、详细设计或实现进度。
2. `tools/check_specs.py --strict` 只验证规范入口、编号、链接和退出文件等治理一致性，不证明代码实现、生产接通或能力验收。
3. 与 ChatGPT「理论探讨」项目同步时使用 `.codex/skills/chatgpt-hai-zhong-yu-chao-theory`。理论讨论、同步摘要和草稿不得作为仓库事实或代码许可。
4. 最终回复和跨窗口摘要必须区分已完成、未完成、验证、风险与下一步；不得宣称未发生的构建、运行、提交、推送或验收。
5. 每个完成声明必须给出输入入口、核心处理、结构承载、输出变化、调用路径和验证方式。计划段提交、main 发布、只读复核和规范体系完成各自只证明对应层级；里程碑或最终完成前仍须独立只读覆盖审计，并由对应事实所有者在自身权限内基于正式证据逐项裁决。跨越规则、计划、代码或发布边界的最终结论等待用户裁决。
