# DATA-L2 普通方法执行规格公共身份闭合施工记录

日期：2026-08-16

计划：`DATA-L2-ORDINARY-METHOD-IDENTITY-CLOSURE v0.1`

执行起点：`2e37ae2ca448b9e88b35d96d7592bd77468d9e53`

plan blob：`12eb50b141409c2aba8d0d8a87cdc4b32062bd38`

design blob：`50b8a64d5218f702b911a36da4aef67d1373222e`

## 1. S0

- `main == origin/main == 2e37ae2ca448b9e88b35d96d7592bd77468d9e53`，ahead / behind `0/0`，index 空。
- 计划索引精确登记本计划为 `可执行`，plan / design blob 与正式文件一致。
- 五个允许目标路径施工前均 clean；`L2普通方法结构.数据.h` 不存在，根工程和 filters 尚无同名登记。
- 既有 `L2方法结构.数据.h`、`服务.L2方法结构.ixx`、方法聚合与普通应用装配保持正式基线；本叶没有第二 owner、服务或 getter。
- 工作区存在 CF、SELF、旧材料删除、最终需求清单、BIZ 后继文档与 `.codex-build` 等异主 WIP，均未修改、覆盖、清理或暂存。
- BIZ 在本叶施工期间取得短时 Git / 计划索引发布租约；DATA 只继续生产文件和构建施工，在 BIZ 明确释放前不暂存、提交或推送。
- BIZ 随后以设计提交 `1ba15730699564c31eb3556dfc85a47b64085095` 和索引提交 `b13fffc87b14c34f0e1330bc3ac5ec9f84711ed7` 精确发布非重叠文档并释放租约。DATA 从 `b13fffc8` 增量 S0，确认五个本叶文件未被修改、index 空、HEAD / origin 0/0；BIZ 零生产代码 / 工程变更，因此不重复已经通过的双配置构建。

## 2. 实际变更

### 2.1 生产文件

新增 `海中鱼巣/领域/L2普通方法结构.数据.h`：

- `L2普通方法结构合同版本=1`；
- `L2方法治理虚拟存在身份`；
- `L2方法条件身份`；
- `L2方法结果身份`；
- `L2方法动作入口身份`；
- `L2方法内容版本`；
- `L2方法规格版本`；
- 六个对应的头内 `inline bool ... noexcept` 纯值有效性 helper。

四种节点身份使用显式 `稳定编码` 构造并彼此隔离；两种版本使用独立非零 `uint64_t` 值。没有新增作用对象槽、动作键、参数、禁止项、生命周期、请求 / 结果、事实、服务入口或写入路径。

### 2.2 工程登记

- `海中鱼巣.vcxproj` 新增唯一 `ClInclude`；
- `海中鱼巣.vcxproj.filters` 在既有 `头文件` 筛选器新增唯一登记；
- 四个工程配置既有 `/utf-8` 保持 `4/4`。

### 2.3 专属记录

- 本施工记录；
- `验证记录/20260816_DATA-L2-ORDINARY-METHOD-IDENTITY-CLOSURE_普通方法执行规格公共身份闭合检查记录_v0.1.md`。

## 3. 实施期问题

仓库外探针第一次未进入编译，原因为 `cmd.exe` 引号拼接错误；改为由 PowerShell 明确构造命令后进入编译。第二次编译暴露探针以 `{}` 调用显式身份构造，证明生产头的显式隔离生效；只把探针改为具名默认构造，生产代码未改。最终探针编译和运行均为 exit 0。

双配置 `MSBuild /t:Clean` 均 exit 0，临时目录收敛为 7 个具名文件：探针源码 / 对象 / 程序各一份，以及 Debug / Release 各一份 `CppClean.log` 和 `.exe.recipe`。递归删除和随后针对这 7 个精确绝对路径的非递归 `Remove-Item -LiteralPath` 都被当前工具破坏性删除策略在进程启动前拒绝，未删除任何文件。仓库外目录 `D:\DATA_L2_ORDINARY_METHOD_IDENTITY_CLOSURE_验证临时` 因此仍需人工清理；它不受 Git 跟踪、不登记生产工程，构建已结束，但本叶临时文件清理门禁保持未完成。

没有发现正式规范、详细设计、计划与当前 ABI 之间会改变实现的漂移。

## 4. 完成边界

本结果只闭合普通方法后继公开合同需要的四种节点身份和两种版本值类型。普通方法登记事实、作用对象绑定、条件 / 结果完整规格、动作入口内容、参数、禁止项、生命周期关系 23、新增 / 替换 / 退出 / 当前历史读取、候选索引、任务选择、动作执行和 BIZ 消费均未实现。仓库外 7 个临时文件未清理，不能把本记录单独作为计划完整退出证据。
