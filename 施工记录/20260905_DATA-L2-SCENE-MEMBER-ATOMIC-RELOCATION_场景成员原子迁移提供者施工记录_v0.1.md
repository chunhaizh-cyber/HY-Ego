# 场景成员原子迁移提供者施工记录

日期：2026-09-05

计划：`DATA-L2-SCENE-MEMBER-ATOMIC-RELOCATION` v0.1

## 1. S0

- 施工起点：`a5ccf92dbbb52180d2f3f8ede3dad699356dd55c`，与 `origin/main` 一致，分歧为 `0/0`，开工时 index 与 dirty 为空。
- 计划 blob：`ceb2cb685100e12ff144da750aa5b6105a0ae720`，计划索引状态为唯一 `可执行`。
- 计划登记的代码起点为 `8ce3298dedf4f7911b4023d8b8abf164facb240a`；目标源码在该起点与实际施工起点的 blob 均为 `7b92cf27561f501f36121a8438df2f5bb0e692f6`，目标 ABI 未漂移，期间仅计划材料前移。
- 已读取执行技能、协作与发布规则、正式计划和详细设计、相关正式规范、知识入口与当前源码；目标新入口当前无已发布专属用途卡，以源码和冻结设计为实施依据。
- 允许文件仅为场景类源码与本施工记录；未取得任何其它源码、工程、规范、计划、索引、知识库或验证记录写权。

## 2. 实际变更

`海中鱼巣/领域/数据服务.场景类.ixx` 新增冻结的：

- `场景类成员迁移合同版本 = 1`；
- 19 项 `场景类成员迁移状态`；
- `场景类成员迁移请求`、`场景类成员迁移投影`、`场景类成员迁移结果`及其 `成功()`；
- `场景类数据服务::迁移成员关系`。

入口只接受 `子场景`或`存在`成员。首次请求在同一 `G0` 完成原场景、目标场景、成员当前性和目标端反向唯一归属读取；子场景使用请求预算完整遍历成员子树并拒绝成环。准入通过后形成一条新关系与一条旧关系退出的唯一场景 owner 中性写集，只调用一次现有写端口。

发布后按 `G1` 核对新编码映射、旧关系退出、新关系当前、原/目标场景成员读回和目标端唯一反向归属。任一发布后读回不能闭合均返回 `已可能发布`且投影为空，不补偿、不重试、不生成新幂等键。

幂等重放先读取首次规范化写集与首次结果，使用历史旧边、新边核对请求意义与首次发布代次；同义请求以原写集取得 L1 `精确重复`并返回首次代次历史投影，后续再次迁移不改写原回执。异义或首次非成功返回 `幂等冲突`。

## 3. 所有权与调用边

- 唯一写 owner：现有场景 owner；未新增 owner 或写端口。
- 直接读取：场景类现有 L1 公开当前/历史读取、首次幂等材料，以及存在类公开 `查询存在`。
- 唯一写调用：`写入端口_.提交所有者范围中性写集`，一次请求至多一次。
- 未调用旧 `服务.L2*`、SQL、仓库私有面、世界树、ARCH-L4 或跨 owner 写；未修改既有成员新增/删除合同。

## 4. 代码知识变更清单

| 函数身份 | 类型 | 用途与合同 | 调用边变化 | 待更新用途卡 |
| --- | --- | --- | --- | --- |
| `场景类成员迁移结果::成功() const noexcept` | 新增纯值谓词 | 仅确认冻结成功状态、版本、代次和完整迁移投影 | 无服务调用 | 场景类新数据服务用途卡待维护者建立 |
| `场景类数据服务::迁移成员关系(const 场景类成员迁移请求&)` | 新增对外服务函数 | 在场景 owner 内原子迁移子场景或存在成员并完整读回 | 读取现有 L1/存在公开面；至多一次场景写端口提交 | 场景类新数据服务用途卡待维护者建立 |
| `成员迁移请求有效`等迁移私有帮助函数 | 新增内部服务函数 | 完成准入、反向唯一归属、预算成环检查、写集形成、首次幂等和历史/当前读回 | 仅由迁移公开入口及其内部链调用 | 同上，按内部函数分账登记 |

既有公开函数无签名或语义变更，既有调用边不变。

## 5. 验证与发布边界

用户明确代码阶段不需要验证。本次未运行构建、链接、程序、测试、专项、静态扫描、`git diff --check`、strict、验证脚本、故障注入或独立验收，未建立验证记录。只进行了目标源码逐段人工合同审阅；Git 暂存边界在取得发布租约后另行人工核对。

本记录与候选源码只证明冻结迁移合同已形成待发布实现，不证明编译、运行、生产接线、世界树语义、内部世界、ARCH-L4 关联、旧逻辑替代或业务验收完成。
