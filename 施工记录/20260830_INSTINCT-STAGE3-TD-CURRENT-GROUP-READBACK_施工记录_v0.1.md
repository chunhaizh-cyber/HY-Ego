# INSTINCT-STAGE3-TD-CURRENT-GROUP-READBACK 施工记录 v0.1

日期：2026-08-30

计划身份：`INSTINCT-STAGE3-TD-CURRENT-GROUP-READBACK`

## 1. S0 与范围

- 正式施工基线：`main == origin/main == 8c8ba074ca9810d04cffd09649acb181c1907af3`，ahead/behind `0/0`，index 空。
- `计划/计划索引.md` 唯一可执行项为本计划；计划 blob 为 `9640904519c3dca105a410ce3c37988cd0eb58c6`。
- 规范目录已登记 4260 v0.12；本叶直接消费其中 v0.10 冻结的 `按任务读取来源需求关系组` 合同与同名详细设计 v0.1。
- 两份目标生产文件在共享工作区存在更宽 SELF-GOVERNANCE WIP。本叶从正式 HEAD 导出隔离候选，只把本计划两文件最小差异写入精确 index，不覆盖、不暂存共享工作树对应文件。
- 当前生产定义保持版本 1、值域 `0..I64_MAX`、`L=2,767,011,611,056,432,742`、`H=7,378,697,629,483,820,645`；“浮动”只表示未来定义版本可调整，本叶没有运行期自动变化。

## 2. 实际改动

1. `L2任务结构.数据.h` 增加 `L2任务来源需求关系类型身份`、`L2任务正式存在关系类型身份`、五字段 `L2任务来源需求关系事实`、专属合同版本 1、请求/结果 DTO、请求有效性和结果自身成功谓词。
2. `服务.L2任务结构.ixx` 以独立幂等身份 `0x4C32'5452'434F'5245` 登记 `0x0301'2114/2115`，保持原十项任务类型登记请求不变，并逐项历史读回两项映射。
3. 首次任务融合写集以 `0x0301'2613` 同事务建立首个显式 `T→D`；22 项正式形状要求该关系存在。13/21 项仅保留历史形状识别，不取得新生产成功。
4. 首次融合正式读回和精确重复读回逐字段互证关系 owner、源任务、目标需求、类型、顺序 1、代次和生命周期；不把关系复制进任务事实或首次结果 DTO。
5. 新公开入口按任务和 G0 读取当前 `0/N` 显式关系，逐项调用需求 owner 正式身份读回，按需求身份、再关系编码排序，关系身份全组唯一，并执行读前/读后代次守卫。
6. `退出任务` 在原同代退出写集内收集、验证并退出全部当前 `T→D`；历史事实保留，当前组不返回退出关系。
7. 验证源、ABI consumer、IFC 映射、静态库、可执行文件和持久恢复根均位于 `D:/TEMP/海中鱼巣/INSTINCT-STAGE3-TD-CURRENT-GROUP-READBACK`；隔离候选为 `20260830-implementation-v1`，精确 staged tree 复验根为 `20260830-commit-tree-v1`，均不进入 Git。

## 3. 函数级代码知识变更清单

| 函数 / 谓词 | 变化 | 用途、非职责与直接调用边 | 待回写 |
| --- | --- | --- | --- |
| `L2按任务读取来源需求关系组请求有效` | 新增纯值谓词 | 检查专属版本、共同头、非零 G0 和任务身份；由新公开入口及 ABI consumer 消费，不读取事实 | 新建 DTO/谓词用途卡 |
| `L2按任务读取来源需求关系组结果::成功` | 新增纯值谓词 | 检查唯一成功形状、同截止、逐项有效性、规范排序和关系身份唯一；不捕获请求、不替代服务后置条件 | 新建 DTO/谓词用途卡 |
| `初始化任务后继核心类型登记` | 新增 module-private | 独立幂等登记并历史读回两项类型；由 `L2任务结构服务` 构造函数调用，不形成业务关系 | 更新 task owner 登记用途卡 |
| `L2任务结构服务::L2任务结构服务(...)` | 修改 | 在既有登记链中追加独立后继核心类型登记；原十项登记请求字节形状不变 | 更新服务构造用途卡 |
| `查找初次筹办融合写入编码映射` | 修改 module-private | 识别 13/21 历史形状与 22 项新正式形状，恢复 `T→D` 编码；不把历史形状升级为新成功 | 更新首次融合内部用途卡 |
| `形成初次筹办融合写集` | 修改 module-private | 在同一 owner 写集中追加首个 `T→D`，来源需求直接取首次需求裁决正式身份 | 更新首次融合内部用途卡 |
| `读回初次筹办融合首次结果` | 修改 module-private | 22 项映射下逐字段互证 `T→D`；由首次发布和精确重复收束调用 | 更新首次融合内部用途卡 |
| `读取任务当前投影` | 修改 module-private | 将来源需求关系组纳入一致读取形状但不复制进 `L2任务事实` | 标记现有任务读取用途卡待复核 |
| `验证任务退出事实闭包当前有效` | 修改 module-private | 允许 0/N 并验证全部当前 `T→D` 成员 | 更新退出内部用途卡 |
| `读回退出任务` | 修改 module-private | 逐项确认给定 `T→D` 已在同代退出 | 更新退出内部用途卡 |
| `L2任务结构服务::按任务读取来源需求关系组` | 新增公开入口 | 同 G0 完整读取当前显式 `0/N` 关系组；只读、`const noexcept`，当前生产消费者尚未接入 | 新建公开入口用途卡 |
| `L2任务结构服务::退出任务` | 修改 | 收集当前全部 `T→D` 并纳入原子退出写集；不删除历史事实 | 更新退出公开入口用途卡 |

退出函数：无。新增验证 helper 只存在仓库外临时测试投影，生产宏和正式提交中均无第二业务写入口。

## 4. 完成边界

提交树复验时的精确 staged tree 为 `e63a98b97933f9f0d60fb1766fb7ec37d79d80cf`；该树的 v143 x64 Debug / Release Rebuild，以及从该树 IFC/OBJ 形成的 Debug、Release `/GL + /LTCG` ABI consumer 均已通过。随后仅将这两份记录补写为最终证据，生产文件 blob 未改变。

本切片最多证明 task owner 能在生产首次任务融合中形成首个显式 `T→D`，并能按任务与 G0 完整读取当前 `0/N` 关系组。它没有接入服务活动消费者，没有实现安全门禁、被动维护事务、FRESH、阶段三或完整 `INSTINCT-ROUTE`。
