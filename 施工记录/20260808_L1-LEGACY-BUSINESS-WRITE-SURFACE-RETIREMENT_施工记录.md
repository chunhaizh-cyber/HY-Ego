# 旧业务标签、意图和执行证据写入口退出施工记录

日期：2026-08-08

计划：`L1-LEGACY-BUSINESS-WRITE-SURFACE-RETIREMENT v0.1`

plan blob：`fe6c2d6aaef2077ea219fa12801f26a544f724e6`

施工起点：`3b4620b1ee7cbf13cc810fe9865c0a6640bf384e`

## 1. 开工核对

- 开工时 `main == origin/main`，ahead / behind 为 `0 / 0`，Git index 为空；本计划是计划索引中唯一可执行项。
- 前置状态动态联合发布中性迁移结果 `67771214300067058128fb38d33caa33b6ad53d8` 已进入正式 main 并经交互侧接受。
- 全仓生产代码对旧 `提交写集` 为 0；旧幂等探测只剩核心服务到核心仓库的待删除定义内转发，没有领域消费者。
- 三个目标模块及只读核心数据头 blob 与详细设计基线逐项一致；目标文件无异主修改，既有 P0-FIX、路线、旧 WORLD-TREE 和 `.codex-build` 现场原样保留。

## 2. 实际施工

### 2.1 核心服务公开面

从 `海中鱼巣/核心/服务.L1事实基座.ixx` 删除四个旧 `提交写集` 重载和旧 `探测幂等`。与它们绑定的标签白名单、完整快照前置、业务意图和执行证据组装一并退出。

`提交中性写集`、中性精确读取、当前 / 历史 / 审计 / 完整快照读取、提供者身份读取以及恢复候选建立 / 确认 / 撤销入口均保留。

### 2.2 核心仓库旧写实现

从 `海中鱼巣/核心/仓库.L1事实基座.ixx` 删除：

- 旧幂等探测实现；
- 旧 `提交(const L1写集请求&)`；
- 只由旧提交使用的读回引用解析、代次匹配、发布后读回计划执行、旧结果摘要形成和审计追加 helper。

中性写集提交、legacy 幂等账冲突保护、历史账完整性校验、当前 / 历史 / 审计 / 快照读取、恢复材料导出和恢复候选事务全部保留。没有清空或转换历史账、审计、失败见证或恢复材料。

### 2.3 旧探测合同

从 `海中鱼巣/核心/合同.L1事实基座.ixx` 删除旧幂等探测请求、状态、首次结果副本、探测结果和结果映射函数。其它读取、审计、快照和恢复请求合同保持不变。

`海中鱼巣/核心/L1事实基座.数据.h` 零修改；P13 / P14 继续只形成纯值 `L1写集请求` 规格，不取得核心写权。

## 3. 验证

- Debug x64 完整重建通过：0 错误、1 条临时输出目录提示。
- Release x64 完整重建通过：0 错误、1 条临时输出目录提示。
- Debug / Release 编译器模块依赖扫描均为 `sources=136, modules=136, failed=0`，拓扑 136、循环 0、未解析需求 0。
- 旧核心 `提交写集`、旧幂等探测 DTO / 结果 / 映射、仓库旧提交 / 探测及五个专属 helper 在生产 C++ 源中零定义零引用。
- `tools/generate_target_function_data_matrix.py` 仍有两条用于旧目标函数目录展示的静态名称映射；它不是 C++ 定义或运行期调用，也不在本计划五文件白名单内，留待后续旧验收资产盘点处理。
- `提交中性写集`、中性读取、legacy 管理读取和恢复入口仍存在；核心数据头和目标外文件未纳入本施工修改。
- `python .\tools\check_specs.py --strict` 与五个白名单路径空白检查在发布前执行并记录于配套验证记录。

构建输出和日志位于系统临时目录 `C:\Users\zhchh\AppData\Local\Temp\hzy-l1-legacy-retire-b3716b3c94244ccf94bc338d0b9a66e1`。生成的生产程序没有运行。

## 4. 完成边界

本施工只证明无人消费的旧普通写入和预探测代码面已物理退出，正式核心普通写入口只剩业务中性提交。

本施工不证明历史 legacy 数据格式已删除、恢复已经运行、P13 / P14 规格 DTO 已中性化、其它读取已经迁移、任何第二至第五层业务场景已经运行、消费者集合已经闭合或第一层已经完善。
