# FEATURE-LONG-TERM-CONCEPT-A 特征长期区间提供者施工记录

日期：20260906
版本：v0.1
状态：候选源码已冻结，Debug/Release与22阶段真实验证已完成，待父级复核发布。

## 范围与起点

执行 `FEATURE-LONG-TERM-CONCEPT` v0.2 第一片 A。计划 blob `a2e369817c545eec56d4d032c00a2cf68f94150a`，详细设计 blob `e8bdb0a0ed31ebcf46897c8d6c056d8d8a089399`。计划原源码基线 `c4a20f6d4d62e63d77dd7cfd9decdd6c9add9802`，F 原 blob `534cb63597968cd859bdd224e7f8f37e7fa0acd1`；首次代码 S0 从正式索引提交 `091531b9d5fd7dc19d8462daa38b06ae82e806be` 进入。A-SUPPORT v0.2 增量 S0 对应 `262f6a4541e804bd821aff3a54f6f2de7e934cd0`。

实际代码所有者 `/root/feature_concept_execute`，唯一 Git 发布者为父级 `/root`。仅改 `海中鱼巣/领域/数据服务.特征类.ixx` 与本片两份记录。其它九份概念、应用、工程及旧记录 WIP 保护，不并入本片。没有新生产模块，没有修改 L1、新值、世界提供者、规范或共享知识。新增验证工程只在独占资源根 `D:/TEMP/海中鱼巣/FEATURE-LONG-TERM-CONCEPT/01a07099-20260906-provider`。

S0 已读取执行 skill、现行索引/计划/设计、全再用登记、Git/owner 状态、规范目录及相关施工规范、当前代码知识与代码知识使用规则。现知识条目不足以代替新 F/值公开接口事实，故逐项回读当前源码后复用，不把旧 L2 知识当本片依据。

## 实际实现

新增 12 个具名公开方法：登记/读取特征类型域、比较长期特征、建立/读取特征校正、建立/读取特征跟踪、积累特征观察、匹配/读取长期区间、读取长期观察、释放长期来源引用。新 DTO 与方法全部位于既有 F 模块，不通过旧 L2 结构或概念服务包装。

构造只校验外部真实 18 角色及格式锚点。类型域登记同 owner 原子建立永久域记录和真实 I64 输出 FT，自槽标记为 `0x4C54444946460001`；普通 F 原有自槽门继续拒绝把此 FT 当一次值写入。类型域以真实 FT 唯一，内容不可偷偷替换。

实际值先按实例历史入口证明唯一真实属性，再精确互证请求值事实和 FT。引用值仅经现新值提供者完整历史材料解析一层。I64 使用安全幅度差；二值格式完整核验 0/1 像素、尺寸、坐标、分辨率及原点位模式，按四邻提边与逐点整数平方根计算双向平均。私有 128 位整数溢出及最终 I64 不可表示明确返回状态 17。

校正只接受静止声明 true、同 E/FT 的至少两个互异帧，完整比较所有 i<j 样本对后加裕量。记录保存明确静止标记及全部定位。跟踪绑定固定类型域/校正，区间冻结首次代表与半径，积累一次原子发布观察和可选新区间，保存全部命中。统计从正式观察重建；跨校正第二去重键不允许同实际帧换规则再次累计。

原 key 优先读取 L1 首次正式材料，按首次 H 的自有结构重建规范写集、全部映射和真实节点/关系/属性槽；不重新匹配当前区间、不重新计算 world 历史样本。所有五类写请求都保留完整输入、首次 H 和发布分类；L1 实际精确重复不会误标新发布。释放仅退出真实 role16 来源当前边，记录及历史引用保留，显式槽变更为空；未来退出只在原 H 预期比较中投影，不抹掉公开历史事实。

结构读取与原证据审计分开。显式审计重读真实来源并核验当时完整区间命中；普通结构恢复不借当前 F 值重算。单次读取上下文仅缓存同 G 下 `(记录身份,H)` 的成功完整记录，失败不缓存、跨事务不复用。完整区间/观察组用真实 role13/14 反向关系定位；跨跟踪第二键用类型域 role11 反向定位后筛 E 并核完整观察。所有返回关系与记录反向互证，保留累计预算和尾 G 守卫，不增加持久索引。

## 施工中的修正与证据边界

A-SUPPORT 两项真实合同缺口已由计划支撑正式发布 v0.2 后实施：差异不可表示=17，以及静止声明 bool/固定 I64 索引2/false拒绝。普通代码问题在本片修复，包括实例唯一属性来源互证、数量预算状态映射、跨校正第二键以及 L1 精确重复回执分类。

首批 Debug 最小链已运行。build-11 遇 MSVC C1001，跨跟踪去重从大泛型 lambda 提取成同合同私有函数后 build-12 通过；未修改算法或工具链。legacy-01 的低属性预算为8仍足够读真实多属性节点，因此先返回内部不一致；测试将预算精确设6使类型域5属性之后只余1项，legacy-02 实际命中底层数量预算分支。该调整只修测试前置，不放宽生产错误分类。

最终构建、各模式及三进程事实以配套验证记录为准。独立源码阅读不是运行覆盖，早期/终止预检不抵扣冻结后的结果。

## 代码知识变更清单

新增可复用能力：真实 FT 完整域及输出类型登记、完整二值材料解析、安全整数差异、全样本对静止校正、固定首代表全匹配、永久观察统计、同 key 历史写集恢复、来源引用释放。既有普通 F 历史、新值材料历史、L1 原生关系反向历史读取为实际复用点。

后续知识维护应以本片发布后的精确 F blob 建立上述函数用途条目，并注明 E/成员字段仅为历史上下文定位，本提供者不证明应用 E→F 归属。当前未改共享知识索引。FC 去宿主/取消成熟、共享域并集及名称、EC/DC/scene 适配属于后续片，本片不宣称完成。

## 交付与资源

最终22阶段全部退出0，全部138项输入hash执行后保持一致；完整命令、每阶段断言/G/耗时、依赖和产物hash已嵌入配套验证记录。当前只完成第一片A，后继共享概念未施工。父级已只读复核；资源删除被自动审批拒绝，实际残留详见末节；没有stage/commit/push。
最终候选 F：blob `420aae7f95f81446c9dfe782b5da206632d38f1b`，SHA256 `39C03D6AC618425999941D0B63CFC3CB5B80591D2BE9703D3A78683008B11CCB`。本值对应全部22阶段共同冻结输入。


## 临时资源处理实际结果

父级已独立核对22阶段结果、冻结输入及首次H证据后授权清理。本轮唯一目标为 `D:\TEMP\海中鱼巣\FEATURE-LONG-TERM-CONCEPT\01a07099-20260906-provider`，清理所有者 `/root/feature_concept_execute`。

删除前在独立只读命令中实际确认：解析绝对路径与目标相同，495个文件、58个目录（含根）、742433960字节，0个重解析点、0个相关运行进程；对495个文件逐项使用 FileShare.None 独占只读打开，全部成功、0个占用阻挡。该探测已单独执行完成，不在后来被拒绝的删除命令中，也不宣称它提供永久无锁保证。

随后原生 PowerShell `Remove-Item -LiteralPath` 删除调用在启动前被自动审批以 `blocked by policy` 拒绝。实际删除0个文件、0个目录；立即停止且没有重试、改用其它工具或触及旧任务目录。拒绝后再次只读统计，仍残留495个文件、58个目录（含根）、742433960字节。残留为本片隔离源码、消费者、工程、构建产物、日志及合成持久数据；无本轮活动进程，按5.1.2作普通残留分账，不改变已取得的验证证据。

两份记录回填后保持停写；没有 stage、commit 或 push，最终发布由父级唯一所有者处理。临时 delivery-files.json 保留清理前记录哈希快照；清理回填后的最终三文件哈希以交接回执及发布输入为准。
