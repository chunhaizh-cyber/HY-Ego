# L2-SELF-E-ROLE 存在单例角色结构施工记录

计划身份：`L2-SELF-E-ROLE v0.1`

执行日期：2026-08-14

起点与结果形成前基线：`870aa66f579ae416aca0d446c15600d75cf38807`

plan blob：`dbd1c09c2680c36eba8987765d40ea44dab05af9`

详细设计 blob：`101bfdfe509d636eb82f16eef7646032f00aec75`

## 1. S0 与范围

- `main == origin/main == 870aa66f579ae416aca0d446c15600d75cf38807`，分叉 `0/0`，计划索引状态为 `可执行`。
- 世界树路线 S-ID、R1、A1、I1、I2 已退出；第四因果链概念根规范包不改变本叶存在 owner 内的业务中性 CRUD 合同。
- 本段只修改两个存在生产文件、三份独立临时专项和两份永久记录。流程图、路线输入与 `.codex-build` 为异主现场，未修改、暂存或带入本段。
- 未修改 L1、场景生产代码、A1、普通应用、启动、计划、索引、规范、流程图、根工程和 filters；专项没有登记入生产工程。

## 2. 生产实现

`海中鱼巣/领域/L2存在结构.数据.h` 增加详细设计冻结的单例角色键、登记、事实、角色化存在、绑定、替换、退出、当前/历史读取 DTO，以及请求有效性、完整性与成功形状函数。

`海中鱼巣/领域/服务.L2存在结构.ixx` 在唯一存在 owner 和服务实例内实现：

- 构造期在存在身份来源之后以固定本地键 `0x0202'1301U / 0x0202'1302U` 和固定幂等身份 `0x4C32'4552'4F4C'4501ULL` 建立角色登记锚点与关系类型，并 owner-aware 正式读回；
- 公开入口 `建立存在单例角色结构登记`、`读取当前存在单例角色结构登记`、`新增角色化存在`、`绑定存在单例角色`、`替换存在单例角色目标`、`退出存在单例角色`、`读取存在单例角色`；
- 存在节点、存在族归属和角色关系在一份 owner-scoped 写集中原子创建；绑定只建立角色关系，替换同事务建立新关系并退出旧关系，退出只关闭具名关系；
- 写前核对事实代次、角色唯一性和目标存在来源；首次与精确重复均按首次截止正式读回；当前与历史读取共同互证关系和目标存在来源；
- 业务请求继续复用 L1 owner 范围全局幂等域，存在退出自然受当前角色关系引用保护；登记缓存只作定位，每次业务读取仍使用正式事实。

没有把角色解释为“自我”，没有解除启动阶段 17，没有进入阶段 18、概念根或生产装配。

## 3. 实施期修正

- 首次根工程 Debug 的真实首错为 `L2存在结构.数据.h(402,13) C3861 L2存在事实完整`。原因是新增成功函数定义早于既有完整性函数定义；只把 `L2角色化存在写入结果::成功` 的定义机械移动到依赖定义之后，未新增公开前置声明或能力。
- 专项首次构建因临时 vcxproj 漏列当前正式模块 `海中鱼巣.核心.合同.L1所有者范围CRUD` 而出现 `C2230`；只补专项依赖清单。
- 专项随后因测试整体比较既有 `optional<L2存在事实>` 出现 `C2678`；改为专项内身份、生命周期与属性空集合逐字段比较，没有新增生产运算符或辅助。
- 裸 L1 专项最初把允许状态写窄。诊断确认有效绑定请求进入正式存在来源读取后返回 `入口拒绝`、空载荷且代次不变；专项改为精确断言该实际结构化拒绝，生产零修改。
- 初轮 20 项通过后复核详细设计发现尚缺正式 wrong-family 动态证据；专项增加场景服务正式建根，以场景族节点作为存在目标验证拒绝，最终双配置为 21 / 21。

## 4. 实际验证

- 根工程 Debug / Release x64 fresh `Rebuild`：均 PASS，分别生成 `x64/Debug/海中鱼巣.exe` 与 `x64/Release/海中鱼巣.exe`。
- 独立专项 Debug / Release x64 `Rebuild` 并运行：均为 21 / 21 PASS、失败数 0。
- `python .\tools\check_specs.py --strict`：113 / 113 PASS。
- 五个当前目标文件严格 UTF-8、无 BOM / `U+FFFD`：5 / 5 PASS；根工程、filters、专项工程 XML：3 / 3 PASS。
- 五路径 `git diff --check`、生产工程 / filters 零专项登记、两个生产文件验证专用痕迹扫描：PASS。
- 专项的 `MSB8029` 只提示系统临时目录不适于增量构建；每轮使用独立 GUID 目录并执行 `Rebuild`，实际编译、链接和运行退出码均为 0。

## 5. 完成边界

本结果只证明中性单例角色结构在当前进程内按本专项矩阵实现并可构建运行。真实分配耗尽、私有事实损坏、恢复、跨进程重放、生产消费者接线和独立集成验收均为 `NOT_RUN`。三份专项暂按计划保留，等待交互接受和索引正式退出后再以独立清理提交删除；当前不能宣称真实自我形成、启动阶段 17 解除或项目闭环完成。
