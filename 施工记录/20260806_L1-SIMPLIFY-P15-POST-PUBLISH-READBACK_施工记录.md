# L1-SIMPLIFY-P15-POST-PUBLISH-READBACK v0.29 施工记录

日期：2026-08-06

## 1. 计划与起点

- 计划：`L1-SIMPLIFY-P15-POST-PUBLISH-READBACK v0.29`
- 正式起点：`57620c808af8d0642fe9960d91920f98efd2b6ff`
- 设计提交：`05f7f17fbd7b16637a6d3715d90229240c4670c4`
- 计划 blob：`4707ef7719fddca89b46c155e9d9cba73535eabb`
- S0：`main == origin/main`、0/0、Git index 空；v0.29 为可执行，v0.28 已退出；正式增量只含 P15 v0.29 支撑材料和索引替代/激活，与 P15 代码 WIP 零路径重叠。
- 允许范围：25 个代码/模块/自检路径和本记录、验证记录，共 27 个文件。实际代码 diff 为其中 24 个路径；`海中鱼巣/核心/合同.L1可重建索引.ixx` 保持无改动。

## 2. 实际施工

1. 完成 L1 v2 请求意图、执行证据、规范化写集摘要、确定性首次结果、发布后读回、失败见证、隔离、恢复材料三副本互证和恢复拒绝矩阵。
2. 完成 G1—G8 provider 前正式幂等探测、固定业务标签、首次/同义/异义分支和既有领域 DTO 映射；G0 与领域意图摘要独立互证。
3. 完成 E01 领域结果见证计划 RP、见证 RW、结果摘要 v2 绑定；其它结果摘要保持 v1；同义账返回保存版本，恢复按保存版本重算。
4. 完成只读恢复材料服务入口、共享锁确定性导出、完整隔离允许导出、未确认候选不导出、损坏和见证矛盾拒绝。
5. 保持正式基线 16 文件/50 个 `.提交写集(` 表达式，完成目标为 16 文件/49 个表达式：删除 X01 同义重复物理提交，X02 保留唯一共享 `0x15000901U`，R01 既有表达式补 `0x15012001U`。
6. 按 v0.29 唯一修订将 `自检.L1场景结构.ixx` 第 7 项同键不同世界根期望改为 `世界结构状态::幂等冲突`，保持 provider/提交 0/0、零候选交换和零事实变化。
7. 收口全量自检揭示的两个计划内代码缺陷：
   - G7/G8 预探测未命中但服务已有唯一登记时，恢复原领域唯一性拒绝/幂等冲突语义，不再错误返回内部不一致。
   - `形成L1规范化写集摘要` 对节点、关系、值、属性槽变更和退出事实执行与仓库相同的确定性排序，消除退出写集在仓库规范化后账内材料摘要失配及交换后隔离。

## 3. 所有权与边界

- 未修改计划、计划索引、规范、流程图或启动顺序。
- 未触碰或夹带 P75、P74、P0-FIX、路线、旧 WORLD-TREE 及其它异主 WIP。
- `.codex-build` 仅作为未跟踪隔离构建和诊断现场；全部短时诊断已从正式源码和最终隔离源码清除。
- 本记录只证明 v0.29 白名单内施工事实，不证明代码已发布、独立集成验收完成、恢复治理闭环或 SELF-GOV 闭环。

## 4. Git 状态

- 记录形成时 Git index 为空，尚未取得代码发布租约，未 stage、commit 或 push。
- 后续只允许在租约内精确发布实际 24 个代码 diff 和本记录、验证记录，共 26 个变更文件；允许范围中的未改动合同文件不产生提交内容。
