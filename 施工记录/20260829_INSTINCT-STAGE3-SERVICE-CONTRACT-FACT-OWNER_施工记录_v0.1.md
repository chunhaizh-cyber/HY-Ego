# INSTINCT-STAGE3-SERVICE-CONTRACT-FACT-OWNER 施工记录 v0.1

## 1. 身份与边界

- 计划：`INSTINCT-STAGE3-SERVICE-CONTRACT-FACT-OWNER`
- 计划文件：`计划/20260829_INSTINCT-STAGE3-SERVICE-CONTRACT-FACT-OWNER_服务合同事实权威与完整集合代码实施计划_v0.1.md`
- 计划 blob：`8d92a30ccd5ebdb107bcbbf2671cafe1bd5e5a4b`
- 起点：`main@6b55a9ef500c9b6b9159185d721412240cd2cff6`
- 本记录只陈述本计划白名单内已发生的施工事实；没有修改 L1、运行期只读查询、治理函数骨架或 SELF-GOVERNANCE WIP。

## 2. 已实施内容

1. 新增 `服务合同事实权威.数据.h`：定义合同、当前状态、到期未满足事件、完整集合见证、读取请求/结果和结构化状态。
2. 新增 `服务.服务合同事实权威.ixx`：
   - 固定 owner 身份 `0x494E'5354'5343'4F57`；
   - 固定登记幂等身份 `0x494E'5354'5343'5247`；
   - 使用 `0x0307'3001..0x0307'3013` 登记锚点、索引根、关系类型和属性类型；
   - 构造期恢复首次规范化写集，并在真实当前截止读回登记；
   - 通过 L1 所有者范围一致关系类型闭包读取两类完整集合；
   - 合法空集合返回正式完整见证，所有非成功返回空载荷和截止 0；
   - 生产构造不建立合同、状态或到期事件，也没有公开生产写入口。
3. 新增 `端到端测试.服务合同事实权威.ixx`：验证宏下建立两阶段真实 L1 fixture；本地键只用于写集，业务载荷使用 L1 返回的真实稳定编码。
4. 普通应用增加唯一服务持有、const/mutable getter、owner 交付、服务构造及装配失败状态 94—96。
5. 工程和 filters 登记两个生产文件及一个专项；只有 `ServiceContractFactAuthorityValidation=true` 时定义 fixture 宏。

## 3. 函数级代码知识变更清单

| 函数 / 类型 | 变化 | 用途与合同 | 直接调用边 / 待更新用途卡 |
| --- | --- | --- | --- |
| `尝试形成服务合同事实权威所有者交付_v1` | 新增 | 验证固定 owner 身份、范围种类和 L1 同实例绑定后形成唯一写端口交付 | 普通应用、专项 `建立服务`；待新增用途卡 |
| `服务合同事实权威服务::服务合同事实权威服务` | 新增 | 消费唯一交付，恢复或首次登记结构并以当前截止正式读回 | 普通应用、专项；待新增用途卡 |
| `读取当前有效未满足服务合同完整集合_v1` | 新增 | 在请求 G0 读取自我所属全部当前有效未满足合同、当前状态与完整见证 | 后续 SERVICE-C1 consumer；当前生产调用为 0；待新增用途卡 |
| `读取到期未满足事件完整集合_v1` | 新增 | 在请求 G0 读取自我所属全部到期未满足事件与完整见证 | 后续服务根消费；当前生产调用为 0；待新增用途卡 |
| `普通应用上下文::取得服务合同事实权威服务` const / mutable | 新增 | 返回普通应用持有的唯一服务同实例引用 | 后续生产 consumer；待更新普通应用用途卡 |
| `ARCH_建立验证样本_v1` | 仅验证宏新增 | 两阶段建立真实节点与合同/状态/事件 fixture，验证精确重放 | 仅专项；非生产用途卡 |
| `ARCH_注入读中漂移一次`、`ARCH_注入资源失败一次` | 仅验证宏新增 | 注入 G0 漂移和资源失败分支 | 仅专项；非生产用途卡 |

没有退出既有生产函数。共享知识库未由本计划直接修改。

## 4. 完成边界与当前阻断

- P00—P10、Debug ABI consumer、Release `/GL + /LTCG` ABI consumer、持久恢复、strict 和 diff-check 已取得独立证据，详见同名验证记录。
- live 工作区根构建被异主 SELF 测试与普通应用 WIP 的既有接口不一致阻断。
- 从精确索引导出的干净候选中，Debug / Release Rebuild 均完成全部编译，但最终链接被 HEAD 基线既有 26 个 L2 跨模块未解析符号阻断；符号来自状态、动态、需求、方法、概念和任务筹办等本计划禁止修改模块。
- 因根工程 Rebuild 未通过，本计划当前只能形成安全 WIP 结果，不能标为完成、不能归档，也不能声明 SERVICE-C1、阶段三或 `INSTINCT-ROUTE` 完成。
