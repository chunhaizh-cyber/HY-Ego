# L1-SIMPLIFY-P10-EXISTENCE-FEATURE-VALUE v0.1 施工记录

## 1. 身份与S0

- 计划：`L1-SIMPLIFY-P10-EXISTENCE-FEATURE-VALUE v0.1`
- plan blob：`8c9b4d2fdcc2b5ee97579453d02c7e01eb03d5a4`
- 激活HEAD：`5711b230ed43bc191f20f302178779b057864594`
- 最终验证HEAD：`51a59f763635325f09a1dc55341d24a8526114bf`
- 白名单：实例特征数据头、服务、专项自检和本计划两份专属记录，共五文件。

S0确认P7/P8结果已退出并经交互侧接受，实际存在读取DTO、四参实例特征服务构造、建立/读取入口和事实形状与冻结合同一致；顺序320、三个代码文件和构建验证资源均已释放。P0-FIX、路线输入、P15/P29和旧WORLD-TREE现场未修改、未暂存。

## 2. 实际施工

1. 新增`实际存在I64实例特征当前值换代请求`、逐字段相等语义和有效性函数；P8原字段、状态枚举和值域不变。
2. 新增唯一公开`L1实例特征服务::换代实际存在I64实例特征当前值`。首次请求经P8公开读取、P2定义、L1来源节点和完整快照形成同代次已验证材料。
3. 把原场景单值换代见证迁移为场景/实际存在共用的`0x07`有序值式账；同数字键跨两种请求variant互斥，同variant逐字段相同才精确重复。
4. 唯一私有`提交I64当前值换代写集`固定提交节点0、关系0、值1、属性槽变更1、旧值退出1；相同材料使用新键仍形成新稳定值和新代次。
5. 发布后通过P8公开入口读取新当前，并从L1读取旧当前、旧历史和当前属性槽；任一矛盾结构化失败，不补写、不回滚已发布事实。
6. 顺序320原位从45项扩展至62项；新增夹具覆盖成功、精确重复、同键异义、跨宿主形状冲突、同材料新键、多轮历史、非法/越界/旧基线/错当前、未绑定provider、第二存在和两存在真实竞争。

## 3. 施工中的代码修正

- 首次构建命令误用了不存在的`.sln`，未进入编译；改用正式`海中鱼巣.slnx`。
- 首轮编译发现默认相等因嵌套读取DTO不可默认比较而删除；在同一数据头内补为逐字段相等实现，未改变DTO字段或函数签名。
- 专项初稿的组合服务构造参数顺序和一个局部变量重名导致编译错误；均在计划内机械修正。
- 为严格复用P8公开读取入口，服务互斥量改为递归互斥量；所有公开入口仍由同一服务锁串行，未增加公开控制面或第二服务。

## 4. 完成边界

本施工只形成P7实际存在的P8 I64实例槽当前值换代源码候选及顺序320专项证据。不包含第二关系19状态、关系20迁移动能、动作动态、因果、运行期装配、SQL/恢复、旧消费者迁移或集成验收。
