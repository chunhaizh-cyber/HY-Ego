# FEATURE-CONCEPT-MODEL-CUTOVER 施工记录

日期：2026-09-07
执行所有者：`/root/execute_feature_node_i64`
状态：2026-09-08 v0.5 迁移代码已完成同候选 Debug/Release x64 编译链接，待父线程最终复核与发布；未运行。当前事实见第 8 节，早期失败回执原样保留。

## 1. 冻结依据与 S0

计划 v0.1 / blob `3d9d47bf609abcc7de28422859146223071511d3`；详细设计 v0.1 / blob `c21836adc34f8de996d3ca4902dc32bbfeff00c9`。执行起点为 `13646c6ed97b69b91a15f5d1d23d2d126dda337e`，`main == origin/main`，index 为空。索引为可执行，无未满足的外部计划依赖。

已读回统一再用文件登记，本执行者独占计划 13 源码、两个必要工程文件及本片施工/验证记录。未与异主自我治理文档重叠；全部无关 dirty、未跟踪副本及 `.codex-build` 原样保留。未经主线程明确 Git 发布租约，不暂存、提交或推送。

初始 S0 时特征类仍为旧五字段实现；新标量消费者已存在而其提供者被旧重写移除。复用 L1 当前/历史事实、关系/属性组、原子写集和首次材料；复用值服务完整读取；不复用旧字段、宿主槽或长期跟踪。知识库旧 L2 基线落后，新特征类没有对应函数用途卡，已回读实际源码和消费者；只读协作盘点覆盖状态、动态、存在、需求、任务、方法及二次类，不分派并行代码。

S0 结论：允许在正式白名单内按设计串行施工。当前事实的缺失属于本计划已冻结的重建范围，不以缺失消费者接口假称当前能力存在。

旧 v1 治理子范围因原七角色和端口来源未冻结而具名暂停；支撑 v0.2 发布并由提交 `748e81c5fb61ff477921f60778fde68682067a19` 激活后，已读完整差异，核对计划 blob `1b2ffcb0c111b32b443636b430743a061832b326`、设计 blob `81755b11f6ce772a7ce34a43109c140503925722`。该子范围恢复，白名单、唯一执行者、隔离验证资源及记录路径均不变；后续隔离候选以发布时当前正式 HEAD 为底。

## 2. 实际改动

累计 11 个生产文件有本片修改，均在 13 源码白名单内：领域的特征、状态、动态、存在、需求、任务、方法、概念树；业务的特征概念、存在概念树、动态概念树。二次特征类已只读核对并进入编译；场景成员概念类因公开 DTO 缺口未修改。工程两个文件已包含全部实际模块，不造空编辑、不排除失败模块。

已落盘的主体包括：准确 F/FT 强身份、固定内联或 F 自有 I64 材料、两分区初始化、先天 I64 域及命中、合法标量 v2 DAG、旧 v1 独立交付与治理；24 角色新 C/类型观察/名称/命中/来源/用途及多父组织；E 已知集合与当前采用分账、四类采用写入和独立历史读回；特征概念新编排及 EC/DC 固定 F 消费。

状态重放读取原截止真实 F，不再构造 I64=0 占位快照。DC 保留绝对/相对时间，模板 H 与实际状态创建 H 分别传递；EC 使用同 H 当前采用而非已知全集。需求方向在真实已知叶之外另核本次当前 F 的采用关系。

最后静态收紧包含：标量 v1/v2 在首次材料读取前保留唯一原业务，未知结果阻挡不同新写；标量完整叶核 Gread/类型关系/完整值；Fapp 读取错误逐项映射、命名读取不再要求先枚举全部适用 C、缺形成规则时不先发布新 C。这些改动仅有源码及下述有限编译证据，不表示恢复或业务验证通过。

本片落盘的澄清项（记录父线程裁决，不产生新规范）：

- 存在历史结果枚举归属迁移为存在类自有 `存在历史读取状态`，1..10 数值及 L1 映射保持；不恢复特征兼容别名。原枚举取证于 `61bae536a6a62bc6e9e7b2a37aa0708ef9799cd7` 的特征值类。
- `存在类数据状态::数量预算不足=29` 尾增，原 1..28 不重排，完整组读取预算失败不截断。
- 创建新 C 的子父替代中，新父组表示本次新 C 以外的已发布父；provider 同笔隐式并入新 C，不使用零编码占位；独立组织仍传完整最终父组。
- 概念服务增加 `使用特征服务(const 特征类数据服务&)` 地址绑定核验，仅比较已注入实例，不增加数据事实。

## 3. 代码知识变更清单

以下是本轮已落盘的函数知识变化，不是完成/可复用能力宣告；关联用途卡全部待本片发布后由维护角色按最终提交更新。共享知识库本轮零写入。

| 物理文件 | 新增/修改的具名函数及用途、调用边 |
| --- | --- |
| `海中鱼巣/领域/数据服务.特征类.ixx` | `初始化特征定义结构`、`初始化准确特征结构`、`创建先天I64特征类型`、`创建准确特征`、`查询准确特征`、`读取准确特征事实`及按身份字段读取/保护删除；只经 L1 两既有端口与值服务。`读取先天I64特征类型事实`、`读取I64类型完整域`、`形成I64特征域`、`规范化I64特征域`、`判定I64域包含`、`判定准确特征命中域`显式 G/H。`建立标量派生定义`、`读取标量派生定义`、`退出标量派生定义`、`比较派生特征`及 `标量执行写/标量展开/标量复算`消费固定 F；`读取旧派生治理事实/退出旧派生治理定义/旧路由/旧读取闭包`仅解释原交付与原端口。`收敛待确认写入`不代替标量原请求恢复。 |
| `海中鱼巣/领域/数据服务.概念树类.ixx` | 新 24 角色构造/核验、`使用特征服务`、`观察事实/观察组`、`展开共享定义`、`准备父组/准备特征写入/特征执行/特征发布读回`、`取得特征概念写入头`、`名称组`、命中读写；`共享用途/共享用途组/准备共享用途/准备共享治理/共享执行/共享发布读回`实现同 owner 治理。`应用定义/应用图/应用模板/应用判定`及四个公开应用读取入口消费新 union/固定 F/双 H；`读取旧特征概念审计`仅输出原生关系和值。各结果 `成功`、`应用事实完整/新特征概念事实完整`同步新结构。通用治理新 C 转接仍未闭合，见第 5 节。 |
| `海中鱼巣/领域/数据服务.存在类.ixx` | `读取当前采用/变更当前采用/收敛当前采用`及 `读取采用组/准备采用/采用互证/采用读F`：同 E/FT 完整已知/采用组及原关系预期，真实 F 决定 FT；`查询存在/读取存在历史事实`、既有发布快照和删除接入采用组。`验证参数类型节点`经 FT provider，不再把普通 FT 当 L1 属性类型。`映射特征状态`及历史枚举/映射归属迁移。 |
| `海中鱼巣/领域/数据服务.状态类.ixx` | `新增状态/读取状态历史事实/读取当前状态/读取历史状态快照/读取历史已退出状态/读取快照特征组/尝试重放新增`与快照结果 `成功`消费完整固定 F；保留真实自有值事实和原状态截止。 |
| `海中鱼巣/领域/数据服务.动态类.ixx` | `读取动态历史事实`及变化 F/状态快照读取、历史结果 `成功`和特征错误映射迁移；不改动态时间类型职责。 |
| `海中鱼巣/领域/数据服务.需求类.ixx` | `计算临时需求方向/验证端点/查询需求记录/按所属存在和目标精确查询`及方向结果/记录闭合校验：派生身份 `.结点`、旧治理投影、同 H 当前采用与已知叶证据分账。 |
| `海中鱼巣/领域/数据服务.任务类.ixx` | `绑定任务方法实例参数/读取任务方法实例可执行参数组`及参数/目标方向结果 `成功`改读取完整固定 F、真实 FT；`读取任务目标方向`消费新派生身份。 |
| `海中鱼巣/领域/数据服务.方法类.ixx` | 结果方向项/结果 `成功`及方向绑定、读取、旧派生治理分支迁移新派生身份；特征状态映射改具名新错误，合法派生实际阶次不强制二阶。 |
| `海中鱼巣/业务/应用服务.特征概念类.ixx` | 重写 `处理类型观察/收敛类型观察/推进/写/接受/确保概念`；双 provider 绑定、根引用生命周期、准确 F 多匹配选择、观察→域→DAG→命中→名称逐层保留。`查找适用特征概念/读取命名特征/数据错误映射`消费新 F/C。尚未完成全部阶段预算累计和恢复路径复核。 |
| `海中鱼巣/业务/应用服务.存在概念树类.ixx` | `形成见证`仅取 E 当前采用并核 F；身份/父组 visitor、特征读取 helper、`消费`改新 union、完整叶和14项具名用量。多 FC 合取及通用治理还需后续复核。 |
| `海中鱼巣/业务/应用服务.动态概念树类.ixx` | `值域匹配/校验形成见证/共享概念/共享查找`及读取调用迁移完整 F、新 C 和14项具名用量；状态创建 H 为实际证据 H，模板 H 独立。 |

退出项：特征类旧可变五字段、setter、历史/域/细分、长期 E 跟踪生产入口及相关 DTO/helper；概念树旧共享 C 生产 DTO/创建/名称分支；特征概念应用旧生成/关联、三次步骤联合与继续材料。物理删除尚未全闭合：场景成员保留的 11 个命中行明确列于编译记录，不能宣称全仓或全13文件旧符号清零。

二次特征类无源码变更；其五方法及 `包装写`仍转交四个 provider，并保留真实失败/首次 H。工程文件无变更，全部模块保持编译登记。

## 4. 验证与边界

实际执行 4 次 Debug Rebuild 与 1 次 Release Rebuild，全部退出 1；最后错误集中在白名单外世界树类的旧存在历史枚举。13 模块均进入依赖扫描；世界树失败使上层四个概念应用未实际完成编译，不能外推其无编译错误。最终 Release 候选与白名单15文件 SHA256逐项一致，领域目标对象文件已产生；不存在整个工程成功结果。

13 源码指定退役模式扫描命中 11 行，均在暂停修改的场景成员概念类。精确本片 `git diff --check` 和 `git diff --cached --check` 通过；全仓 diff check 仅报异主 `AGENTS.md:12` 尾空格，不擅自修复。`python .\tools\check_specs.py --strict` 返回 0，101 份目录项通过。详细命令、日志和分轮差异见专属编译记录。

没有执行程序、测试、数据库、故障注入、恢复或集成验收；没有 stage、commit、push。编译日志不证明业务、运行、恢复或生产装配闭合。

## 5. 真实缺口与未完成施工

### 5.1 需计划支撑处理，执行者不自行裁决

1. 名称历史：v0.2 保留 `包含已退出`，但 L1 历史关系组只枚举指定 H 活动关系，没有枚举 H 前全部已退出关系的入口。7160 当前名称读取本可按显式 G/H 表达；正式设计尚未修订。当前 true 分支具名不支持，不能算完成或永久豁免。
2. 场景成员 DTO：新观察请求不含原场景子请求曾携带的 E、采用幂等键及完整预期关系；场景继续材料/分层结果仍引用退役 Fapp ABI。需冻结 E 组合采用输入、F/C/E 结果与唯一原请求恢复材料。整个场景成员文件保持未改。
3. EC 抽象依据承载：现有 EC→EC 目标关系用旧 `shared_->类型[15]`，新24角色没有对应 EC 目标角色。不能把旧16直接换为新16/17（新17关联值只定义 FT/观察/并集来源特征 C）。设计又要求旧21只审计与原EC能力保留；需明确合法交付/复用。该局部保留原代码、未擅自新增字段或角色。
4. 白名单漏消费者：`海中鱼巣/业务/应用服务.世界树类.ixx:550-569,694-696`仍消费旧 `特征引用读取状态`，应迁移存在自有历史状态，但不在13源码范围。已报父线程，未改、未恢复兼容别名、未从工程剔除。该项阻断完整编译。

### 5.2 仍是代码所有者后续施工/复核，不包装成设计缺口

- `执行应用概念写入`的通用上位/生命周期/退出/释放尚未完整转接新 C 同一内核，外层原请求和成功投影需补闭合；已有通用分支不能当作新 C 可用证据。
- 全图、EC/DC和Fapp跨阶段14项用量累计、成功谓词及所有历史生命周期投影还需全面复核；已修若干核心路径不代表预算/历史全量完成。
- Fapp、EC/DC未完成实际编译；恢复后须修其真实编译/逻辑错误，并完成场景替代。不能只消除世界树错误便宣告计划完成。
- 标量 v1/v2 原键确定冲突、全部失败原请求保留、初始化/端口生命周期和完整退出集合仍需按计划静态矩阵逐项复核。最后加固未运行恢复验证。
- E新旧写入口共同串行保护、外部关系owner/type生命周期与采用结果全部分支需继续复核。

## 6. 收口现场

HEAD最后核对仍为 `748e81c5fb61ff477921f60778fde68682067a19`。本片11个源码修改与本施工记录、专属编译记录保留；二次/场景及两工程文件未改；世界树、L1、特征值、规则规范、设计计划及所有异主 WIP均未改。本记录中的“安全收口”只表示保持范围/现场与不发布，绝不表示这组WIP已达到可提交的编译完好门禁。登记占用未释放；隔离输出未删除。后续先取得正式支撑修订与精确范围，再由同一执行者继续。

## 7. 2026-09-08 v0.3 继续施工（09:13 +08:00 中间回执）

本节按时间追加，不改写前述失败证据。正式 HEAD 为 `2339ce0724164137d26438a3dce4587e2b84f3c9`，当次 S0 的 main/origin 相等、index 空。计划 v0.3 blob `ff2854ff02effbf42397b8c8944a020a33223687`、设计 v0.3 blob `646d00f15a8f6d6d863c1254147c07c318201521`，继承未覆盖的 v0.2 合同。世界树已加入正式14源码白名单，并在 `/root` 占用模块登记本执行者后修改；现有其它登记保留。

新增/校正的函数用途：

- 世界树 `映射存在历史状态` 及读取检查的10处旧枚举迁移为存在自有历史枚举；不改业务映射。
- 特征 `标量来源形状有效` 按 variant 拒绝派生输出角色0；准入、展开、复算改按强类型分支。`标量叶完整` 同供读取和比较谓词，核 Gread/H、关系、完整值；允许内联 I64 同时回显真实 L1 属性值编码，不把它改作材料引用。
- `读准确→读类型` 传同一私有可选计数上下文；标量读取按真实不同 FT 归属/外设/单位/形成规则/参数关系及规格/误差值累计，不漏计、不同 F 复用同 FT 时不重复扣减。公开 DTO 没有增加字段。
- Fapp `消费待确认写入` 统一首次/恢复结果消费；确定失败清 pending、未知发布保留原请求。F 创建恢复亦先辨明结果再清标志。`推进` 先读原观察键，已有观察沿原 F 续办，不因等待期间新增同值 F 重开选择；原键、时间、指定 F 及准确值互证，已确认观察/概念/名称继续保留。
- 概念树 `执行应用概念写入` 对新 C 上位/生命周期/退出转 `准备共享写入/共享发布读回`；旧上位状态6/7/8不改，退出替代按原父子重连规则内部形成。通用结果谓词增加新 C 完整分支。`形成引用组` 按真实 FT 来源关系反查；FT 释放复用新共享释放准备/读回核验，混合旧/新引用仍是一笔完整写集；O/C不冒充旧世界引用，继续使用具名共享释放入口。
- `生命周期迁移有效` 复用四种合法迁移；新共享生命周期不允许活跃直接退役。类型全域锚点不是永久本体根，仍有保留细分时禁止退出。`application_pending_` 为私有原请求恢复材料，外层回显及 pending 一律保存原 input；规范化只用于写集。feature/shared/通用/EC等同 owner 写入口及取键检查活动 pending；明确首次未找到或确定未发布不制造永久等待。
- 存在 `新增存在/新增成员关系/删除成员关系/删除存在/变更当前采用` 使用同一个私有写入互斥量。此处是同实例调用串行保护，非跨进程恢复证明。
- 动态概念 `值域匹配` 使用既有入口拒绝表达非 I64 成功面，删除不存在的枚举成员；存在概念结果谓词按新联合的 `身份` 或既有 `概念` 取真实身份。

只读子智能体完成两轮标量/Fapp复核及两轮新 C 治理复核。复核发现的角色0、FT漏计、恢复误留pending、同值F重选、生命周期越级、类型锚点永久化、通用pending门禁缺口及规范化回显不一致已逐项修入；这不是运行测试或正式验收。

本段四轮隔离 Debug Rebuild 均 exit 1。第四轮前面所有领域模块及世界树、特征概念、动态概念、存在概念已实际编译；错误全部集中在仍暂停的场景成员旧 DTO，汇总0警告/82错误。新根为 `D:/TEMP/海中鱼巣/FEATURE-CONCEPT-MODEL-CUTOVER/compile-20260908-01/`；未运行任何二进制。本段未启动 Release，不能声称最终双配置通过。

剩余合同支撑：v0.3场景观察DTO与现有Fapp同名冲突、未定义预算/继续材料、场景组合投影及多项采用意图含义；名称基础预算与专项预算读取边界；EC独立交付进入构造的精确位置。已具名退回父线程，由计划支撑处理；没有自行新增公开字段/别名，没有修改场景文件或复活旧兼容DTO。Fapp整次编排还是每provider调用的预算上限需按冻结正文裁决，不能私增用量字段。

未提交、未推送、未释放占用；没有写入规范、规则、设计、计划、索引、L1、特征值类、装配或共享知识库。计划尚未完成，继续等待原目标内支撑后施工。

### 7.1 09:27 +08:00 静态收尾追加

存在采用在派发前校验写后完整已知/采用关系总数仍落在请求预算内，避免预算不足直到发布读回才暴露；`采用互证` 补合同头/查询编码；结果谓词补原 G、首次 H、请求预算与预期关系有效性。确定失败只清理匹配本请求的 pending，不误清另一请求。保持四分支、原 DTO 与既有状态数值不变。

旧 v1 治理 `旧读取闭包` 新增私有子图高度缓存，已完成共享节点按当前位置加子图高度核最大深度，避免浅路径缓存放过更长路径。根定义仍核原退出端口 owner；递归外部来源按真实节点 owner 核属性、关系和同 owner 注册，外部事实不进入根9/10退出集合。`旧治理投影完整` 同供旧读/写成功谓词，补宿主、角色配对和请求预算。

`退出旧派生治理定义` 对已绑定首次材料先核原 G、原键、纯退出式9/10集合和目标；请求误指集合内注册节点等确定结构不符归幂等冲突，不改报未知并永久锁住。历史材料不可用、资源、代次漂移等仍保留原恢复边界。

这些修复源自只读复核的具名问题，不新增正式字段或规则。Debug第五轮已实际编译上述主修复，Release第一轮进一步包含最后的旧目标结构冲突修复；两轮都只在场景旧 DTO 处失败，分别31.18秒/27.26秒、均0警告/82错误。最终源码尚无同一候选双配置全工程成功证据；等待 v0.4 正式支撑后继续场景/名称/EC部分。

## 8. 2026-09-08 v0.4/v0.5 迁移与最终编译回执

本节按时间追加；第 1–7 节是先前已发生的中间事实，其中“场景未修改”“旧符号未清零”“编译未闭合”等只代表当时现场。本节不改写失败日志，不将后来的通过倒填为早期成功。

### 8.1 重新 S0 和范围

v0.4 实施起点 HEAD 为 `9708a7beb5fb2dd60d07fa4ae3771f5f8ea0d664`，计划 blob `bebcee9089bd63df096b6b6ed411f8e3d1157eba`、设计 blob `b10fa4473683930bbca092ba04157af46f98ae9f`。v0.5 发布后再次完整读取正式计划/设计并核对索引：HEAD/origin 均为 `5ee25943bdf3dc4c68d48645062023be5437e775`，main、0/0、index 空；plan blob `0b82aff3ba358534212ec561c61f976e81ddf33b`，design blob `5230be393d1a6cdfefeb95de6d248c0508b7f54c`，索引可执行。v0.5 继承 v0.4 及未覆盖的 v0.2/v0.3 合同。

重新读取执行 skill、协作和单工作区/知识规则、知识三入口及对应规范、代码接口；本片新模块没有可直接沿用的新架构用途卡，以当前源码和正式设计为准。完整读取再用文件列表，18 项登记仍归同一执行者，无重叠。实际修改累计为 13 个源码及两份专属记录；二次特征类和两个工程文件已具备所需消费/登记，保持原字节。L1、特征值类、规范、规则、计划、索引、生产启动/装配、共享知识库与所有异主 WIP 未由本执行者修改。

### 8.2 本段实际修订

- 场景旧 v1 联合 DTO、生成/关联/存在概念联合流程整体删除，按 v0.4/v0.5 输出新值式 DTO。首次只经 Fapp 处理原观察；续行只经 Fapp 收敛同一完整观察。场景不保存概念原请求，不调用概念写入/收敛入口。
- 场景先在指定 G/H 证明 S 直接包含 E，再依次确认 F/C/名称，可选以存在 owner 精确请求关联已知并采用；派发前保存原请求，恢复逐字段校验 E/FT/F、原 G、幂等身份、预期和预算。最终按同一当前 G/G 重读成员、F/C/名称和可选采用；子结果的真实读取截止不被最终 G 覆盖。
- 最终特征一致性比较不把读取封套 G/H 当持久内容；比较身份、类型、准确值/完整值、类型关系、准确值事实与生命周期。所有成功投影仍核正式域命中回执，未以浅层结构检查代替比较算法。
- EC 四个旧共享 DTO 全部改为 EC 专用类型；全部公开签名、helper、状态和谓词同步。Fapp 删除这四个旧声明和未使用的旧共享预算 helper。
- DC 保留 1..14，新增 15..19 五项正式失败分账；正向及公开治理使用的反向映射均保留五类原原因。
- EC/DC 构造补齐基础八项非零预算，初始非法预算返回入口拒绝；多模板累计耗尽返回数量预算不足，不把零剩余改为 1。判定回执核外层/内层 G、模板 H、实际 H、F/FT、完整值、模板身份和命中；DC 实际 H 保持状态创建截止。
- 任务两处参数读取、状态原键重放、需求当前采用读取补齐实际身份/状态/G/H 互证。
- 特征服务构造核两个端口真实基座与 owner 互异；确定首次材料幂等冲突清除本次 pending，避免误留永久未知发布。Fapp 保留已确认 F，后续新增同值 F 不重开选择；先利用已有最细域，缺失或具有正式形成参数时才形成/插层。
- 概念名称窄读独立使用基础预算，不转旧共享名称枚举。EC 抽象依据使用独立显式交付，两个新 24 角色构造面接入且校验与全部角色互异；未交付只关闭对应能力。

准确引用值仍是设计 v0.2 §5 冻结的 F 信息 owner 自有真实 L1 值事实，完整 I64 经值服务读回；没有保留原外部材料身份的新增合同，也没有伪编码。后天派生保持四项正式 provider 和任务局部值式结果；未新增持久派生 F 入口。旧 v1 治理、普通任务参数槽、存在已知关系和 L1 G/H 历史见证均不属于本次误删集合。

### 8.3 代码知识变更清单补充

以下同第 3/7 节一起构成本次函数知识交付；结果提交尚待父线程发布租约，所有关联用途卡继续标为待发布后复核。本段不写共享知识。

| 模块/可见性 | 函数身份或签名 | 用途、来源、结果、边界与调用边 |
| --- | --- | --- |
| 场景成员概念，公开 | `处理场景成员概念(const 场景成员概念请求_v2&)`、`收敛场景成员概念(const 场景成员概念继续材料_v2&)` → `场景成员概念结果_v2` | 前者处理完整用户原观察，后者以同原观察和可选精确采用原请求恢复；组合 Fapp、概念只读、场景读取和存在采用 provider，不取得数据 owner、不做跨 owner 回滚。 |
| 场景成员概念，私有 | `验证入口`、`读取成员`、`补全特征`、`消费特征结果`、`处理采用`、`完成投影` | DTO/绑定/预算先验、唯一直接成员、正式判定与名称窄读、原结果/继续材料分报、存在 owner 原请求与最终同截止读回。与公开入口组成单一编排，不新增持久字段。 |
| 场景值式 DTO，公开 | 三个 `成功()/完整() const noexcept` | 验证 F/C/名称与可选采用逐项闭合、真实 G/H 和最终投影；不访问 L1，不自行计算值域。 |
| EC，公开/私有 | 构造、`形成或复用存在概念`、`继续存在概念操作`、`存在概念操作结果::成功`；`形成见证`、`核验材料`、`场景检查`、`可派发`、`消费` | EC 专用预算/场景/结果/阶段替换，不复用 Fapp 应用 DTO；保留原定义、支持、接边、恢复和场景能力。完整 F 与模板判定回执、初始/耗尽预算分账。 |
| DC，私有及公开治理消费 | `映射(概念树数据状态)`、`数据状态(动态概念应用状态)`、`校验形成见证`、构造 | 新五状态双向映射；真实状态创建 H 读取准确 F；完整回执核验、原预算非法与累计耗尽区分，不改变动态时间语义。 |
| Fapp，私有/公开入口消费 | `推进`、`消费待确认写入`、`确保概念` | 同一观察绑定固定 F，观察不存在且无原实例 pending 时恢复不支持；读取已有最细域、规则驱动形成、多父/并集、名称分步保留。 |
| 特征数据，构造/私有 | 构造、`收敛原请求` | 两端口与值服务共享 L1、不同 owner；首次材料确定异义清 pending 并报告幂等冲突，未知发布保留原请求。 |
| 概念数据，公开/私有 | 两个接受 `概念树存在扩展交付` 的新构造、`校验存在扩展`、`名称窄读`、`读取特征当前名称`、`读取概念当前名称` | 独立 EC 依据角色，不借新 C 形成依据角色；当前名称只读端点/关系、基础预算和真实生命周期，零写入。 |
| 任务/状态/需求，既有公开入口内部 | `绑定任务方法实例参数`、`读取任务方法实例可执行参数组`、`尝试重放新增`、`计算临时需求方向` | 既有固定 F 读取与当前采用回执增加返回状态/身份/G/H 核验；不改公开 DTO、字段或截止语义。 |

物理退出的函数/DTO：旧场景 `生成或关联场景特征/形成场景成员存在概念/继续场景成员概念/读取场景成员概念` 及其旧联合请求、投影和继续材料；Fapp 四个旧共享 DTO 与闲置 `共享预算` helper。之前已退出的旧 F 可变字段/值域历史/分裂/按 E 跟踪表面见第 3 节，最终四组精确扫描均零命中。

### 8.4 实际验证与剩余

最后独立只读复核已回传：场景三阶段、Fapp 唯一续行、采用原请求逐字段互证、最终读回与失败结果保留未发现现行合同内确定问题。此前八消费者四处互证缺口及 EC/DC 三处预算/反向映射缺口均已修入最终候选；只读复核本身没有构建或运行，不代替下述编译证据。

最终候选由正式 HEAD `5ee25943bdf3dc4c68d48645062023be5437e775` 导出，再只覆盖 14 源码及两工程输入；ZIP comment 实际确认该精确基底。隔离根为 `D:/TEMP/海中鱼巣/FEATURE-CONCEPT-MODEL-CUTOVER/compile-20260908-02/`。最终 16 项 SHA256 与当前工作区逐项相等。Debug-final 0 警告/0 错误、33.44 秒，Release-final 0 警告/0 错误、27.37 秒，两者 exit 0，全部 14 模块实际编译并完成链接。

`git diff --check -- <16项精确输入>` 与 `git diff --cached --check` 均 exit 0；全仓 diff check 仍只报异主 AGENTS.md:12 既有尾随空格，未擅自清理。`python .\tools\check_specs.py --strict` exit 0、101 项通过。零命中命令、合法保留项、分轮构建及最终哈希见专属编译记录第 7 节。

未运行程序、单元测试、数据库、故障注入、恢复或集成验收；不证明生产装配、跨进程 F pending、存量迁移或业务闭环。全部源码和记录目前仍是未发布 WIP，等待父线程最终复核和唯一 Git 发布租约；未 stage/commit/push，未释放占用或删除隔离产物。
