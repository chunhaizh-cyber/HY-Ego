# L1 中性历史属性值组提供者施工记录

日期：2026-08-11

计划：`L1-NEUTRAL-HISTORICAL-ATTRIBUTE-VALUE-GROUP-PROVIDER v0.1`

## 基线与范围

- S0 基线：`main` / `origin/main` / HEAD 均为 `031f9722fc8ca4207c8f2ff310776cffaaad1d42`，分歧 `0/0`，index 为空。
- 精确计划 blob：`26272edd1a662a5f7729bf4882b26c877a74022f`；计划登记提交：`9161fc99006d93d233936e912af70eb1a0352183`。
- 三个生产目标 blob 与计划登记一致：数据头 `81293855cffa1c9c99cc11b635e5f90aa998a129`、仓库 `c6ee0fef8d35ef8a6eb10131c9b710359bf749c3`、服务 `7b6162f4da605ead9b441c840aa9a537054f10f1`；合同导出模块仍整体导出数据头。
- 只写入计划允许的三个生产文件、三个生产外验证文件和本记录及同名验证记录，共八文件。
- 流程图维护目录、路线输入和 `.codex-build/` 的异主现场保持原样；未修改规范、计划 / 索引、合同导出模块、主工程 / filters、入口、L2、装配或日志。

## 实际实施

1. `L1中性CRUD.数据.h`
   - 增加历史属性值组读取状态、请求和结果 DTO。
   - 请求冻结所属节点和非零历史截止；结果回显请求条件、实际读取代次及完整属性值组。
2. `仓库.L1事实基座.ixx`
   - 增加 `[所属节点][属性类型] -> 有序值稳定编码组` 的全生命周期属性槽值候选索引；索引不复制材料、来源、属性槽或生命周期。
   - 只有成功属性槽变更采用新值时登记候选；普通值创建不登记，属性换代、显式值退出和所属节点退出均不删除候选。
   - 增加一次阻塞式共享锁内的历史属性值组查询；从 `当前值` 或 `历史` 唯一回读权威完整值事实，按 `[创建事实代次, 退出事实代次)` 过滤，并按属性类型、值编码稳定排序。
   - 候选矛盾、权威值事实矛盾、生命周期非法或同类型同截止多项有效均返回内部不一致，不任取一项。
   - `状态完整` 增加候选索引、权威值事实和永久成功幂等记录的三方双向互证。
3. `服务.L1事实基座.ixx`
   - 增加同名公开服务入口；纯值校验后只调用仓库一次。
   - `bad_alloc` / `length_error` 映射资源失败，其它异常映射内部不一致；不重试、不排序、不另取代次。
4. 生产外验证
   - 新建独立 Debug / Release x64、v145、C++20、Unicode、`/utf-8` 验证项目和临时目录包装脚本。
   - 验证入口只 import `海中鱼巣.核心.服务.L1事实基座`，仅通过公开服务构造与读取事实。

## 实际验证摘要

- 主工程 Debug / Release x64 `Rebuild`：均成功编译并链接。
- 外部验证 Debug（起始幂等键 `5101`）与 Release（起始幂等键 `6101`）：均为 12 项检查全通过、失败数 `0`。
- `python .\tools\check_specs.py --strict`：111 份目录项全部通过。
- 严格 UTF-8 解码与 XML 解析：目标生产文件、外验三文件及两个工程文件通过；主工程仍为生产模块 26、`ClInclude` 12、四配置 `/utf-8` 4 项。
- 唯一性 / 接线检查：DTO 各一处，仓库和服务函数各一处；验证入口只有一个公开服务 import；专项项目未登记主工程；主工程、filters、合同导出模块、入口和 L2 零差异。
- 源码边界检查：候选登记只存在槽变更调用和辅助定义两处，未出现候选删除入口；普通值路径无候选登记，退出路径无候选删除。
- 完整命令、矩阵结论和 MSBuild 临时目录警告见同名验证记录。

## 完成声明边界

本施工只证明单个进程内 L1 服务实例可经公开接口按所属节点和已发布历史截止有界读取曾进入属性槽、且在该截止有效的稳定有序完整属性值组，并在具名初始槽值、普通值隔离、换代、显式退出、节点退出和幂等矩阵下保持当前 / 历史边界。

未验证资源耗尽注入、故意破坏私有候选索引或幂等账、长时间并发、公平性、性能、崩溃 / 重启 / 跨进程恢复、生产消费者、S3 场景固定属性换代、场景退出、引用保护、完整历史场景、其它 L2 结构族或具名集成验收。

本执行角色未执行 `git add`、提交或推送；发布交由交互智能体在复核八文件范围和异主现场后串行完成。
