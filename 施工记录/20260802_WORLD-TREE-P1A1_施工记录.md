# WORLD-TREE-P1A1 施工记录

日期：2026-08-03

## 计划身份

- 计划：`WORLD-TREE-P1A1 v0.1`
- plan blob：`d6c102c67b3dd9f4d7807237cef818601c1be37a`
- S0 起点：`main == origin/main == 60f6285729e1fc16b323b3017db71444cc03617e`
- 施工中正式仓库前进：`6a8a06054bfb97dab38390886f9c7e4d496147d2`，仅为代码图谱发布；未触碰本计划白名单或冻结合同，复核后继续。

## 实际施工

1. 建立 `节点直接结构合同.数据.h`，集中声明关系稳定主键、节点/关系稳定身份见证、类型合同/类型化值/幂等稳定值、共享索引物理键及纯完整性谓词。
2. 从可重建索引仓移除 `索引物理键` 的重复声明并改为包含共享头。
3. 扩展正式关系仓：关系记录永久稳定主键、命名域高水位和历史占用、候选撤销恢复、发布后永久占用、稳定主键读取、源/目标/相关三向值式枚举及稳定排序、权威材料导出。
4. 会话创建关系入口按值转交计划稳定主键；既有调用方自检在进入会话前从仓库高水位形成测试计划键。
5. 新增类型合同、类型化值和事务幂等三个仓库模块，分别实现未发布候选值副本读回、确认、撤销、完成发布和普通读取隔离。
6. 类型化值仓实现初始版本、换代历史和退役；幂等仓将不可变业务记录与持久证据状态侧账物理分离。
7. 工程与 filters 登记一个共享头和三个新模块；在既有节点直接身份结构自检中加入 `WORLD-P1A1-A01—A15` 专项矩阵。

## 边界

- 未修改事务域、固定执行器、服务、SQL、恢复、发布代次、运行期装配、规范、详细设计、计划或计划索引。
- 未接入默认生产路径；本施工只形成 L1 仓库代码候选和仓库级自检。
- 其它所有者的规范、计划与流程图 WIP 原样保留，未纳入本计划暂存范围。
