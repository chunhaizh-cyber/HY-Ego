# FEATURE-NODE-I64 特征节点数据操作施工记录

日期：2026-09-07；版本：v0.1；执行所有者：`/root/execute_feature_node_i64`；统筹、复核、发布所有者：`/root`。
结论层级：计划首段源码实现和静态复核完成，不证明编译、运行或业务闭环。
结果提交：待主线程串行发布，本执行者未暂存、提交或推送。

## 1. 授权、基线与范围

- 执行计划：`计划/20260907_FEATURE-NODE-I64_特征节点数据操作实现计划_v0.1.md`。
- 计划 v0.1 blob：`5ce91e6a60ad7cca752bf21a8ab9a334b4715f41`。
- 代码计划起点：`edd3e81e72586b741f6b57637a92cceaac0213e0`；接管 HEAD：`63521142cad8a057abb27377b8c78b41f86873bb`。
- 两源码接管时已有用户清空后留下的 DTO/getter/helper WIP；未保留该 WIP 精确 blob，不补造。相对 HEAD 的所有旧代码删除不能全部计成本轮迁移，也不代表旧消费者已完成迁移。
- 唯一写入集合：`海中鱼巣/领域/数据服务.特征类.ixx`、`海中鱼巣/领域/数据服务.特征值类.ixx`和本施工记录；三文件已在在用文件列表登记。
- 只依赖标准库、L1、特征值类；未改 L1、消费者、工程、启动装配、规范或共享知识库，未处理其他所有者的 WIP。
- 正式依据为计划所引 1120 v0.7、1130 v0.11、2100 v0.13、4110 v0.13、4120 v0.7、4180 v0.8 及 4015/CODE-FILE-01；实现五字段模型，不采用未正式化的新聊天模型。

## 2. 实际实现

- 特征类 33 个结果入口均有真实 L1 实现，覆盖先天 I64 类型、独立整节点 CRUD/查询、字段操作、细分读取、初始化与原请求收敛；不是只有 DTO 操作或空声明。
- 特征信息保持身份、类型、准确当前值、历史、值域五字段；值信息保持身份、表示和完整内容三字段。未增加业务版本、代次、水位、宿主或继承布尔字段。
- 显式先天有序 I64 规格初始化；完整 I64 位型、允许区间并集及安全算术检查。引用 I64 经值服务完整读回后在固定截止复核，再以内联完整值保存。
- 准确当前值、历史、域分槽保存；全部命中域逐一计数，历史精确去重，满三次简化及共同建子。子从触发真实值初始化，继承域次数为零，本次不递归输入。
- 域改义新键、次数归零；历史引用、生成关系及父继承保护阻止悬空变更；同域换键不能绕过继承防重复。类型改义检查全部内容、单位和约分缩放解释。
- 所有写操作在同截止读取后形成一个 L1 写集；唯一入口锁串行，原请求 pending 不被新写覆盖。成功载荷提前构造，提交后无异常移动，不重复计数或建子。
- 值类新增按值身份读取，区别当前、已退出可读、材料清理、能力缺失与资源/内部错误；移除跨模块前置声明、friend 和 friend 写集 helper，不新建值写 owner。
- 终稿特征类 blob：`5b11516d63cf3e42635024b7a3e3b7885fbc820a`。
- 终稿特征值类 blob：`4d761c2661544c11c4412bb023714f92669c441a`。

## 3. 代码知识变更清单

新增函数身份以模块 `海中鱼巣.领域.数据服务.特征类`、类 `海中鱼巣::特征类数据服务` 和计划 §4 完整签名确定；物理文件为本片特征类文件，以下结果入口均公开。共同合同：参数由调用方提供，事实来自同实例 L1；返回完整成功值或结构化无载荷错误，单写集发布、同实例串行，不公开 owner/代次/幂等，不提供跨进程业务恢复。

| 函数组 | 本轮新增或修改的实际函数名、用途及调用边 |
| --- | --- |
| 类型与初始化 | `初始化`、`收敛待确认写入`、`创建先天I64特征类型`、`读取先天I64特征类型`；经初始化写集、类型读回和原请求收敛调用 L1。构造只保存依赖和独占写端口。 |
| 整节点 | `创建特征信息`、`读取特征信息`、`修改特征信息`、`删除特征信息`、`查询特征信息`；调用读类型、读节点、输入处理、共同写集及同截止类型关系召回；无宿主前置、不扩级联。 |
| 类型/准确值字段 | `读取特征类型`、`设置特征类型`、`移除特征类型`、`读取准确当前观察值`、`接收观察值`、`移除准确当前观察值`；独立槽操作，真实观察进入统一计数处理。 |
| 历史字段 | `读取历史值集合`、`添加历史值项`、`修改历史值项`、`删除历史值项`、`查询历史值项`；精确去重、本节点稳定域引用保护，不手工制造观察次数。 |
| 域字段 | `读取值域集合`、`读取值域项`、`添加值域项`、`修改值域项`、`删除值域项`、`读取值域代表值`、`修改值域代表值`、`读取值域范围`、`修改值域范围`、`读取值域命中次数`；统一域改义保护，只读次数。 |
| 细分 | `读取细分子节点`、`读取细分父节点`、`读取值域生成子节点`；读取正式关系并核对同类型和继承域，不提供手工重挂删除。 |
| 值读取 | 值模块/类的公开 `特征值读取结果 获取特征值(特征值身份) const` 新增；私有 `映射读取错误`、`投影读取事实`新增，`转换为特征值内容`保留私有并供新读取路径调用；仅调用 L1 当前/历史值事实，无写副作用。 |
| 私有读取/校验 | `保护执行`、`要求`、`检查读取状态`、`检查项目状态`、`取得投影`、`开始读取`、`开始写入`、`读类型`、`读节点`、`检查细分`、`读继承域`、`解析观察值`、`规范化历史项`；内部错误映射、固定截止和完整内容读取，不导出新 API。 |
| 私有封闭编码/规则 | `区间包含`、`集合包含`、`检查类型规格`、`整数域`、`内联值`、`位型`、`整数`、`编码历史`、`编码域`、`找域`、`有稳定覆盖`、`检查字段`、`检查相容`、`组成完整信息`、`新域键`、`相同域定义`；无独立持久账。 |
| 私有工作副本/写集 | `分配本地键`、`加节点`、`加关系`、`写字段`、`新写集`、`处理输入`、`保存节点字段`、`修改类型副本`、`检查域可改义`、`修改域副本`；先校验工作副本，再共同写入。 |
| 私有发布收敛 | `初始化写集`、`规范化写集`、`映射编码`、`解析引用`、`接受初始化映射`、`确认发布载荷`、`提交并读回`、`收敛原请求`；完整核对首次请求与映射，再新选当前截止读回，不要求原 G1 永远当前。 |

L1 直接调用边为：一致当前投影、当前值、历史事实、历史关系组、提交所有者范围中性写集、读取首次写入材料。值类不再通过 friend 向上层特征类提供写集 helper；其接管 WIP 原完整签名未保留，不补造。既有强类型、DTO、浅校验和纯值 getter 保留，不计为新增按节点服务。
相关用途卡应在结果发布后新增/更新；旧特征槽、旧材料服务、friend/helper 调用边应退出或标记待复核。完整签名与私有函数物理位置以以上终稿 blob 为准。共享知识维护待能力目录维护者处理，本片不宣称知识闭合。

## 4. 实际验证与审计

- 已完成源码逐字段、逐分支静态复核及 33 个公开结果入口声明/定义核对；主线程复核整节点/字段/自动处理，独立审计智能体复核 L1 依赖与发布错误边界。
- 审计发现“已发布后读回异常仍有 pending，却可能回普通错误”的问题，已修复 `收敛原请求`和`提交并读回`两层异常分类；pending 保留即返回发布未确认，成功结果提前分配并无异常移动。终稿源码已独立复核，无新增静态阻断。
- 主线程精确两源码 `git diff --check` 无差异错误；全仓检查仅发现异主 `AGENTS.md:12` 尾随空格，未修复。LF/CRLF 提示不等于差异错误，全仓不能写为通过。
- 本阶段没有编译、链接、测试、程序运行、故障注入、跨进程恢复或集成验收；静态阅读不是动态 PASS。暂存后检查、提交和普通推送由主线程随后处理。

## 5. 未覆盖与交付

- 未支持材料/向量类型初始化、独立材料保存解析、后天类型、结构域比较、复合特征、公开业务比较结果和完整细分迁移。
- 未实施宿主引用、深层输入路由、旧消费者迁移或装配启动，不保证现有全工程可编译。
- 未实现另一任务尚未正式化的“固定类型和准确值、当前/历史移至存在采用关系、类型共享积累器”模型，未据该讨论更改规范或本轮计划。
- 三文件交回主线程，执行者停止写入；不提交、不自行释放登记。发布事实和文件释放由主线程最终回执确认。
