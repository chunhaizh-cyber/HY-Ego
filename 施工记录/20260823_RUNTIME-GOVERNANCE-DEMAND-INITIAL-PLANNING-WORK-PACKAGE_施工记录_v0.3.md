# RUNTIME-GOVERNANCE-DEMAND-INITIAL-PLANNING-WORK-PACKAGE 施工记录 v0.3

日期：2026-08-23

## 1. 计划与起点

- 计划身份：`RUNTIME-GOVERNANCE-DEMAND-INITIAL-PLANNING-WORK-PACKAGE`。
- 执行计划：`计划/20260823_RUNTIME-GOVERNANCE-DEMAND-INITIAL-PLANNING-WORK-PACKAGE_需求初次筹办工作包形成代码实施切片_v0.3.md`。
- plan blob：`56ec429ad18cf18e4e2b3e7ffdaede8c52925998`。
- 设计提交：`86550f8b678b64e2c141905f204082c8aba64634`。
- v0.3 重新 S0 起点：`HEAD == origin/main == 4901d5b1a06d67f4e5a7f304d4e801d949691560`，index 为空，计划索引唯一登记本计划 v0.3 为`可执行`。
- v0.3 只修订 V01/V04 合法注入边界；复核确认 DTO、ABI、生产入口顺序、事务和允许文件未改变。执行侧既有候选原地保留后继续施工。

## 2. 实际施工结果

1. `海中鱼巣/领域/L2任务结构.数据.h`
   - 增加首次初次筹办准备记录强类型身份、关系类型身份组、完整请求材料、首次需求裁决材料、记录事实、融合写和两类读回 DTO。
   - 增加完整请求、裁决快照、记录、读写请求和结果完整性守卫；双幂等身份必须非零且数值相同，G1 必须晚于 G0，筹办轮次固定为 1。
2. `海中鱼巣/领域/服务.L2任务结构.ixx`
   - 登记记录锚点、四类记录关系和三类记录属性；增加完整请求与首次裁决固定编码/解码。
   - 用一个 owner-scoped 写集建立任务、任务虚拟存在、首次记录、七条关系和三个值；列表项当前任务 0/1 守卫与原任务入口共用 task owner 锁。
   - 增加按准备幂等身份跨生命周期读取、按记录身份当前/历史读取及 G1 首次投影重构。
   - 任务退出写集同时闭合记录到任务、记录到任务虚拟存在的强关系；记录节点和值保持历史可读。
3. `海中鱼巣/领域/内部治理/任务初次筹办.数据.h`
   - 增加外层准备幂等身份、完整请求字段、`幂等冲突`状态、首次记录身份和轮次 1 工作包成功守卫。
4. `海中鱼巣/领域/内部治理/服务.需求初次筹办准备.ixx`
   - 新增`需求初次筹办准备提供者`；固定为请求守卫、首次记录前置读、无记录才实时裁决、融合写、G1 组包和原键收敛顺序。
   - 原键原请求在任务退出、新当前任务或后继特征变化后从首次记录恢复 G0/G1 结果；provider 未调用旧`新增任务`入口。
5. `海中鱼巣/端到端测试.自我治理一致事实.ixx`
   - 接入新 provider 模块并增加 V01—V15 治理专项；动态覆盖合法成功、重放、四类合法异义、三路读回、并发、非成功、漂移、退出/新当前任务/特征变化及新旧入口竞争。
6. `海中鱼巣.vcxproj`、`海中鱼巣.vcxproj.filters`
   - 各登记新数据头和 provider 模块一次。

未修改规范、流程图、计划、计划索引、共享知识库或能力目录；未进入父回流、重筹办、执行结果、结算、结果共用/争用或成员分配优先级。

## 3. 施工期纯代码修复

- 首轮编译发现新增 task-owner helper 误落在导出命名空间，导致内部定位类型不可见；已机械移回`L2任务结构内部`。
- 初始化登记改为调用现有 L1 中性当前代次公开入口；未建立第二代次来源。
- provider 模块补齐`vector/variant`标准头，使值式材料比较模板可见。
- 前置键读取改为先跨生命周期查首次记录；只有未找到时由 provider 比较当前观察截止与请求 G0。该修复保证原键原请求不因 G2+、任务退出或新当前任务被提前裁决为漂移。

## 4. 函数级代码知识变更清单

| 函数/类型 | 变化 | 用途与合同 | 直接调用/事实边 | 待知识维护动作 |
| --- | --- | --- | --- | --- |
| `L2任务结构服务::新增任务与首次初次筹办准备记录` | 新增 | 校验完整结构材料并在 G0→G1 原子发布任务、虚拟存在和首次记录 | provider 唯一写入口；写 task owner 三节点、七关系、三值 | 新增用途卡；登记与原`新增任务`共用 0/1 守卫 |
| `L2任务结构服务::按初次筹办准备幂等身份读取首次记录` | 新增 | 按准备键跨生命周期读 0/1 首次记录及 G1 任务投影 | provider 第一业务读取；读 L1 首次写入材料和历史事实 | 新增用途卡；注明请求 G0 不限制已有记录读回 |
| `L2任务结构服务::读取首次初次筹办准备记录` | 新增 | 按记录身份提供当前/历史和具名截止读回 | 治理专项/后继合法消费者；不以当前事实补历史 | 新增用途卡 |
| `L2任务结构服务::退出任务` | 修改 | 在原任务核心退出写集中增加两条记录强关系闭合 | task owner 同代退出；记录节点和值不退出 | 更新用途卡，增加首次记录关系闭合副作用 |
| `需求初次筹办准备提供者::形成唯一任务与初次筹办工作包` | 新增 | 先查首次记录；无记录才裁决和融合写；形成轮次 1 工作包 | 调用运行期只读裁决与三个 task-owner 公开 ABI | 新增用途卡；注明非普通筹办消费入口 |
| `需求初次筹办准备请求有效`、相关完整性函数 | 新增/修改 | 固定双键、完整请求、首次裁决和首次结果值式守卫 | 纯值判断，不读写事实 | 新增 DTO/守卫用途说明 |
| `验证需求初次筹办工作包闭环` | 新增 | 执行 V01—V15 动态可注入矩阵和守恒断言 | 仅治理专项；通过正式公开入口 | 测试知识可登记为验证证据，不登记为生产能力 |

执行侧未修改共享知识索引；上述函数身份由能力证据目录/代码知识库维护者在结果发布后按提交基线更新。

## 5. 实际验证摘要

- 最终隔离根：`D:\TEMP\海中鱼巣\RUNTIME-GOVERNANCE-DEMAND-INITIAL-PLANNING-WORK-PACKAGE\20260823-v03-final-02`。
- Debug/Release x64 `Rebuild`：均退出 0，stderr 均为 0 字节。
- Debug/Release `--test-治理`：均退出 0，stderr 均为 0 字节，stdout 均含新增矩阵摘要和`[治理测试] 全部通过`。
- `git diff --check`通过；`python .\tools\check_specs.py --strict`为 116/116；七个代码/工程文件 UTF-8 严格解码通过；生产 provider 旧`新增任务`调用和禁项静态扫描零命中。
- 逐项矩阵与 NOT_RUN 边界见同名验证记录。

## 6. 发布边界

本记录形成时 index 仍为空，尚未提交、尚未推送、尚未取得 Git 发布租约。构建和治理专项通过只证明本计划具名候选，不证明普通筹办已经消费工作包，也不证明路径、实例、冻结、执行、父回流、重筹办、成员结算、结果共用/争用或成员分配优先级已经实现。
