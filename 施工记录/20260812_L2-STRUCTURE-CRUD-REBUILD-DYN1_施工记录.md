# L2-STRUCTURE-CRUD-REBUILD-DYN1 施工记录

日期：2026-08-12

## 基线与范围

- 计划：`L2-STRUCTURE-CRUD-REBUILD-DYN1` v0.2，plan blob `5bc7d450c875d0de063a1f7a9bf939c71489ea86`。
- 起点：`61167c5905a080e516f33d16b9112fbc2d22df8a`，当时 `HEAD == origin/main`、index 为空。
- 只修改计划允许的十条路径；图谱、路线输入与 `.codex-build/` 异主 WIP 原地保留。

## 实际施工

- 新建动态数据头与唯一动态服务，形成 move-only 动态 owner 交付、当前/历史身份来源、十一项强类型登记。
- 新增动态以 owner-scoped 单写集建立节点、族归属、主体、前状态、后状态、起始值、结束值七个事实；三个端点只消费存在/状态正式身份来源，不增加主体等式、状态比较或时间排序。
- 首次提交读回逐项核对七编码唯一、七事实同 owner / 同创建代次、节点和四关系源/目标/类型/角色、两值材料/类型/来源及两个属性槽精确指向起止值；失败不宣称成功载荷。
- 实现当前/历史完整读取、四关系投影及主体/前状态/后状态三条件查询。历史读取独立读回族归属，复核节点、族归属、三业务关系、两值的 owner 和结构字段；动态未来退出后以退出前一代选取结构，并要求节点、族归属、三业务关系和两值同代退出，拒绝半闭包历史投影。
- 并发推进时结构化收敛为事实代次漂移，不把条件快照后逐项来源读取观察到的正常代次推进包装成内部不一致。
- 普通应用追加状态 14—16、唯一动态 provider 和 const/non-const getter；根工程登记一个模块与一个头，生产数变为 30/16。
- 新建三份生产外临时专项文件；未登记根工程或 solution。

## 修正经过

- 首次 Debug Build 暴露机械骨架残留的内部字段命名错误，修正后通过。
- 独立审计发现三项实现证据不足：历史退出投影曾拒绝退出生命周期、首次提交读回未逐字段核验、历史完整读取未独立闭合族归属 / owner / 生命周期。施工在计划范围内补齐上述三项，并增加退出半闭包同代拒绝。
- barrier 专项曾复现条件关系组快照与逐项动态完整读取之间发生合法推进，端点来源返回漂移却被条件入口包装为内部不一致。根因是内部来源失败状态被统一降格；生产路径改为精确传播来源状态，并在同代矛盾前复核当前代次：已推进返回 `事实代次漂移`，稳定同代矛盾才返回 `内部不一致`。专项没有用重试掩盖首次半投影。
- 验证器补充 `<variant>` 并修正七项冲突请求容器构造顺序；这两项只修复专项自身编译，不改变生产合同。
- 格式检查发现编辑目标有 BOM；十份当前产物统一为严格 UTF-8 无 BOM、无 `U+FFFD`。

## 完成边界

本记录只证明 DYN1 具名代码切片已施工并完成下列验证记录中的生产外检查。DYN1 没有合法动态退出入口，因此“动态自身退出后历史完整读回”只完成生产路径静态审计和半闭包拒绝实现，运行验证仍由 DYN2 强制承担；本记录不证明 DYN2、因果、场景迁移、A1、生产消费者或独立集成验收。
