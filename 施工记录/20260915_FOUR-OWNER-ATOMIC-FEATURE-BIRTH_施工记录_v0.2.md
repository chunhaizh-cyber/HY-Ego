# FOUR-OWNER-ATOMIC-FEATURE-BIRTH 施工记录 v0.2

日期：2026-09-16
计划：`20260915_FOUR-OWNER-ATOMIC-FEATURE-BIRTH_四owner原子准确特征出生代码实施计划_v0.1`
起点：`5de0a83833aece60ff2ac13d1dd65143c6970797`；计划 blob：`a4be2f4fe2a49a749e9e2eef64635b3d396ed2aa`

## S0 与范围

- 开工时 `main == origin/main`，起点与远端计数为 `0 0`；index 为空。
- 已读取计划、详细设计、目标 L2/L3 接口、L1 v3 只读合同、代码知识规则及占用表。`D:/TEMP/再用文件/再用文件列表.md` 中的本切片文件均由 `/root/four_owner_feature_execute` 登记；异主 WIP 未改动。
- 只实现四 owner I64 原子准确特征出生；未改 L1、正式规范、计划、规则、线程或启动语义。

## 实际变更

核心实现十文件：

1. `海中鱼巣/领域/合同.相关概念添加参与.h`：发布 I64 概念组织全量读取 DTO 与成功判定，按 `FT.编码` 读取。
2. `海中鱼巣/领域/数据服务.定位特征.h`：重建四 owner 原子出生组合器、候选/完整读取；读取成功状态独立为 `已读取=16`，并修正 v3 重放引用转换。
3. `海中鱼巣/领域/数据服务.特征类.h`：提供 owner #1 的候选、出生片段和内容窄读取实现。
4. `海中鱼巣/领域/数据服务.概念树类.h`：声明概念组织读取和 owner #4 出生使用接口。
5. `海中鱼巣/领域/数据服务.概念树类.cpp`：实现全量 I64 概念读取、出生使用片段/读取及结构注入读回。
6. `海中鱼巣/业务/应用服务.特征概念类.h`：重建 I64 原子出生应用 ABI，并按 `特征类 → 概念树类` 的依赖方向包含领域头。
7. `海中鱼巣/业务/应用服务.特征概念类.cpp`：实现 FCv 精确复用、全量候选分类和仅零候选新建。
8. `海中鱼巣/业务/应用服务.场景成员概念类.h`：仅保留新 I64 应用服务转发入口。
9. `海中鱼巣/业务/应用服务.场景成员概念类.cpp`：转发新请求/结果，不保留旧观察分支。
10. `海中鱼巣/装配.普通应用.cpp`：按纯概念结构、出生使用结构、L2 原子服务、L3 服务顺序装配。

配套工程和提供者调整：`海中鱼巣.vcxproj`、`海中鱼巣.vcxproj.filters` 登记新 L3 源；`数据服务.存在类.h`、`数据服务.场景类.h` 接入 owner #2/#3 私有参与者接口；旧专项验证源、工程和脚本已在消费者迁移后物理删除。

## 编译修复

- 将 L1 所有者范围引用显式转换为 v3 原子引用，避免将两类 `std::variant` 直接写入 v3 写集。
- 修正 `FT` 身份字段名、`std::get_if` 三目表达式、中文标识符相邻三目运算符的词法拼接、概念树文件尾重复声明/命名空间闭合，以及 L3 相对 include 顺序。
- 上述修复不改变 v1.1 冻结的 DTO 字段、事务、owner 或发布语义。

## 旧链退出检查

新 L3 文件不再含 `特征概念观察请求`、`特征概念处理状态`、`定位特征创建请求`、`定位特征数据服务`、`处理类型观察`、`收敛类型观察` 或 `指定F`。旧专项验证项目/脚本同步删除，未保留旧验证入口。

## 函数级代码知识变更清单

| 函数或服务 | 变化 | 用途与调用边 |
| --- | --- | --- |
| `概念树类数据服务::读取当前I64特征概念` | 新增 | L3 FCv 形成读取同截止当前 I64 概念全集；由特征概念应用服务调用 |
| `原子I64特征出生数据服务::{查询精确候选,创建,读取}` | 重建 | L2 四 owner 候选、原子发布与完整读回；由 L3 调用 |
| `概念树类数据服务::{读取原子I64出生使用,准备原子I64出生片段}` | 新增 | owner #4 的出生使用读取和参与者写集；仅组合器友元调用 |
| `特征概念应用服务::处理I64原子准确特征出生` | 新增 | FCv 确保、候选 0/1/多分类及创建/复用；由场景成员转发服务调用 |
| `场景成员概念应用服务::处理I64原子准确特征出生` | 重建 | 不增业务判断的 L3 转发；由普通应用消费者使用 |

共享知识索引未在本切片改写；以上清单是后续知识库维护的输入，不代表运行或集成验收完成。

## 完成边界

本记录只证明代码施工及随后的编译验证。未运行任何 exe，未连接持久库，未证明四 owner 发布、幂等重放、恢复、运行时消费或业务闭环。
