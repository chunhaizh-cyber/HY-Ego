# 概念应用全局根引用编译运行补充记录

日期：20260906
版本：v0.1
身份：CONCEPT-GLOBAL-ROOT / SECONDARY-SCALAR-COMPILE-FIX
结果：相关 21 模块 Debug / Release 编译链接通过，真实消费者两配置各 116 项断言通过；正式验收未进行。

## 1. 本轮授权及范围

用户新要求“代码最少要保证能编译，能运行，验收可以后置”，取代本任务此前暂停验证的限制。此前未验证记录保留为当时历史，本补充记录给出后续实际结果，不倒填早轮通过。

起点 `439444e2b24efaa56b6cdab5c0dddb43a34fec32`，main=origin/main，index 空。根所有者保留三个应用的根调整候选及旧 WIP；新建独占构建运行目录，复制当前源码，旧验证目录只读，不使用旧二进制、数据库或 BMI。执行技能及当前共享工作区规则继续适用。

编译发现 `数据服务.二次特征类.ixx` 的 `宿主状态(存在类数据状态)` 引用不存在的 `存在类数据状态::数量预算不足`，产生 C2838/C2065/C2131/C2051。以本轮明确的编译修复授权登记直接局部切片 SECONDARY-SCALAR-COMPILE-FIX，唯一代码所有者 /root，允许文件仅此模块及本轮两记录；不改变公开 ABI、DTO、事务、生命周期、owner 或已有合法状态含义。核对真实存在服务枚举后删除这一无法成立的 case，保留其它实际枚举映射。无需新增提供者状态或修改存在类。

修复后模块 blob：`f745c2e41dfea340a58bbd9a3d896e816caacf6d`。同步修复到新快照后重新构建，原失败日志保留为 `Debug-build-attempt1.log`。三概念应用源码未因本次编译发生修改，仍为前轮记录的 FC/EC/DC blobs。

## 2. 实际构建与运行

资源根：`D:/TEMP/海中鱼巣/CONCEPT-GLOBAL-ROOT/01a07099-20260906-build-run`。

快照包含 171 个当前源码 / 头 / 实现片段文件；实际编译为项目明确列出的 21 个模块和消费者，不把复制文件数写成编译模块数。21 模块包含 L1 中性合同及基座、特征值 / 特征 / 状态 / 动态 / 存在 / 场景 / 概念数据服务、四个概念应用，以及二次特征 / 需求 / 方法 / 任务服务。最终快照与当前源码逐文件 SHA 相同；编译器为 VS 18 Professional、v145、C++20、x64。

消费者在新 TEMP 内适配存在 / 动态构造的根实参删除；使用原真实 L1/owner fixture，新增仅属于测试夹具的 bindApps 开关和 roots 模式。没有向生产模块添加测试接口或假根。两配置均实际建立独立数据服务与真实根，并运行全局根绑定、同 provider 多实例、异 provider / 异依赖拒绝、失败构造保持、末实例释放及重绑；确认这些管理动作不推进 L1 事实代次。随后执行 FC 实例形成 / 名称关联、EC 概念 / 双向支持读回、场景成员联合投影、原键重放，以及真实 F3→F7、状态 / 动态见证链上的动态概念形成与重放。

| 配置 | 编译 / 链接 | 实际运行 |
| --- | --- | --- |
| Debug | 退出 0 | roots 退出 0，PASS 116 root/business assertions |
| Release | 退出 0 | roots 退出 0，PASS 116 root/business assertions |

原生构建、时间、日志及二进制 SHA 见[编译运行记录](../验证记录/20260906_CONCEPT-GLOBAL-ROOT_概念应用全局根引用编译运行记录_v0.1.md)。三目标 diff-check 通过，规范目录 strict 检查 98 项通过；本轮记录和一行修复在发布前再做差异格式检查。

## 3. 代码知识变更清单

唯一生产函数变化：模块 `海中鱼巣.领域.数据服务.二次特征类` 中 private static `特征类标量状态 二次特征类数据服务::宿主状态(存在类数据状态) noexcept`，移除不存在的输入枚举 case。用途仍为包装写入的宿主读取失败状态映射；参数仍来自实际存在服务结果，已有结果、异常、事实副作用和生命周期均不变。直接调用边仍为 `包装写→宿主状态`，无新增调用；诊断责任仍随包装结果向上返回。与当前存在类枚举互证，编译证据证明此 case 修复。

三个根应用新增函数用途仍以[原施工记录](20260906_CONCEPT-GLOBAL-ROOT_概念应用全局根引用施工记录_v0.1.md)为准；本轮补充其编译及运行证据，不改合同。共享知识库未修改。

## 4. 交付边界

仅将一行编译修复及两补充记录精确发布，不夹带三个应用和其余六份旧 WIP。三应用虽已在当前本地组合中编译运行，仍为未提交候选；其依赖的大计划旧代码仍待修，不把局部消费者通过升级为原概念大计划完成。

未启动完整生产主程序 / UI，未做生产初始化接线、正式集成验收、并发压力、分配失败注入或重启恢复验证；也未运行每个二次标量业务分支。对外只声明相关模块及本次根 / 概念消费者已编译运行。验证材料暂保留在本轮独立资源根，支持后续候选复核；未删除旧 TEMP 或原 WIP。
