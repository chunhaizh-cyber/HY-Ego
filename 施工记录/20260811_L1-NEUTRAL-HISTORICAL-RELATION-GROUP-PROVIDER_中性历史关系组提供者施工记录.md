# L1 中性历史关系组提供者施工记录

日期：2026-08-11

计划：`L1-NEUTRAL-HISTORICAL-RELATION-GROUP-PROVIDER v0.1`

## 基线与范围

- S0 基线：`main` / `origin/main` / HEAD 均为 `c83da9d8226ea65380c48d2f4045ba333fea6176`，分歧 `0/0`，index 为空。
- 精确计划 blob：`55cfc7119b9a28b498b5529631441f1e0ae70092`。
- 三个生产目标 blob 与计划登记一致；五个生产外目标施工前不存在。
- 只写入计划允许的三个生产文件、三个生产外验证文件和本记录及同名验证记录，共八文件。
- 流程图维护目录、路线输入和 `.codex-build/` 的异主现场保持未触碰；未修改规范、计划 / 索引、合同导出模块、主工程 / filters、入口、L2、装配或日志。

## 实际实施

1. `L1中性CRUD.数据.h`
   - 增加源 / 目标端点方向、历史关系组读取状态、请求和结果 DTO。
   - 请求冻结端点方向、端点节点、关系类型节点和非零历史截止；结果回显请求条件、实际读取代次及完整关系组。
2. `仓库.L1事实基座.ixx`
   - 增加只保存关系稳定编码的历史源 / 目标全生命周期候选索引。
   - 关系创建时在候选状态有序登记当前双索引与历史双候选索引；关系退出只删除当前双索引，保留历史候选。
   - 增加一次阻塞式共享锁内的历史关系组查询，从 `当前关系` 或 `历史` 唯一回读完整权威关系，按 `[创建事实代次, 退出事实代次)` 过滤并保持稳定编码升序。
   - `状态完整` 增加历史双候选索引与全部当前 / 历史关系的端点、类型、编码、生命周期、唯一来源和覆盖互证。
3. `服务.L1事实基座.ixx`
   - 增加同名公开服务入口；纯值校验后只调用仓库一次。
   - `bad_alloc` / `length_error` 映射资源失败，其它异常映射内部不一致；不重试、不排序、不另取代次。
4. 生产外验证
   - 新建独立 Debug / Release x64、v145、C++20、Unicode、`/utf-8` 验证项目和临时目录包装脚本。
   - 验证入口只 import `海中鱼巣.核心.服务.L1事实基座`，仅通过公开服务构造与读取事实。

## 实际验证摘要

- 主工程 Debug / Release x64 `Rebuild`：均成功编译并链接。
- 外部验证 Debug（起始幂等键 `3101`）与 Release（起始幂等键 `4101`）：均为 14 项检查全通过、失败数 `0`。
- `python .\tools\check_specs.py --strict`：111 份目录项全部通过。
- 严格 UTF-8 解码与 XML 解析：主工程及外验工程通过；主工程仍为生产模块 26、`ClInclude` 12、四配置 `/utf-8` 4 项。
- 唯一性 / 接线检查：仓库和服务函数各一处；验证入口只有一个公开服务 import；主工程、filters、合同导出模块、入口和 L2 零差异。
- 完整命令与矩阵结论见同名验证记录。

## 完成声明边界

本施工只证明单个进程内 L1 服务实例可经公开接口按端点方向、端点、关系类型和已发布历史截止有界读取稳定有序完整关系组，并在具名创建、原子替换和退出矩阵下维持当前 / 历史索引边界。

未验证资源耗尽注入、故意破坏私有索引、长时间并发、公平性、性能、崩溃 / 重启 / 跨进程恢复、生产消费者、S2 场景关系 CRUD、完整历史场景、其它 L2 结构族或集成验收。

本执行角色未执行 `git add`、提交或推送；发布交由交互智能体在复核八文件范围和异主现场后串行完成。
