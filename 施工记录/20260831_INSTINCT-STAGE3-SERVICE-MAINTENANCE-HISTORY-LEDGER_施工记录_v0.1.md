# INSTINCT-STAGE3-SERVICE-MAINTENANCE-HISTORY-LEDGER 施工记录 v0.1

日期：2026-08-31

计划：`INSTINCT-STAGE3-SERVICE-MAINTENANCE-HISTORY-LEDGER v0.1`

起点：`main == origin/main == 0a34e3ba26099e6e74d8289f08cddad61d77afc6`，index 空。

结果提交：待发布。

## 1. S0 与范围

本段只修改计划允许的三份生产 / 专项文件，并新增本施工记录和专属验证记录：

```text
海中鱼巣/领域/服务合同事实权威.数据.h
海中鱼巣/领域/服务.服务合同事实权威.ixx
海中鱼巣/端到端测试.服务合同事实权威.ixx
施工记录/20260831_INSTINCT-STAGE3-SERVICE-MAINTENANCE-HISTORY-LEDGER_施工记录_v0.1.md
验证记录/20260831_INSTINCT-STAGE3-SERVICE-MAINTENANCE-HISTORY-LEDGER_验证记录_v0.1.md
```

共享工作区的 SELF-GOVERNANCE、规范、流程图、计划、普通应用、线程和工程 WIP 均未覆盖、删除、清理或暂存。本段未修改 L1、阈值、普通应用、启动、线程、工程与 filters。

固定材料保持：值域 `0..INT64_MAX`，`L=2767011611056432742`，`H=7378697629483820645`，安全根定义版本和主动安全结算规则版本均为 1；本段没有运行期阈值重算或自动浮动。

## 2. 实际施工

1. 在现有唯一 `服务合同事实权威服务` 内新增历史账登记幂等身份 `0x494E'5354'5348'5631`，建立 `0x0307'3050..305B` 的五类永久历史根、关系类型和版本登记；构造顺序位于五个既有登记之后，首次与恢复均逐项互证。
2. 新增五类历史投影、范围请求、完整集合见证、结构化状态、结果及强 `成功()` 复核。范围固定为 `(排除起始事实代次,G0]`，起点早于登记事实代次时拒绝，0 项必须有完整见证。
3. 扩展进展 / 准备 v2 首次和更新写集：新事实、当前成员和永久历史成员同一 owner 写集形成；登记后替换只退出旧当前成员，旧节点、载荷、业务引用和历史成员保留；登记前事实继续按旧完整闭包兼容退出而不补账。
4. 拆分精确重放复核：登记前按旧首次写集收敛；登记后额外互证首次写集中的历史成员，精确重复零新写、零第二历史关系。
5. 新增公开只读入口 `按事实代次范围读取服务维护历史事实完整组_v1`。同一 `G0` 读取五个永久历史闭包和进展 / 准备两个当前闭包，先验证整个 owner 账，再按自我与事实代次范围过滤、规范排序、预算和见证。
6. 专项首次发现旧 ARCH fixture 先建事实节点、后补历史成员，违反同写集形成。本段没有放宽完整性，而是新增合规的分阶段合同 / 状态 / 到期事件 fixture，并让生产发布专项使用该合规有效合同。
7. 专项进一步发现当前进展 / 准备缺历史成员时原读取器会静默遗漏。读取器现增加登记后当前根反查：每个活动当前事实必须恰有一个永久历史成员，否则整体 `集合不闭合`、零载荷。
8. 新增 H00—H06，覆盖登记边界、合同 / 两状态 / 到期事件、N 进展 / 准备、左开右闭、精确预算、坏载荷、漂移、资源失败、已退出 / 重复 / 无载荷历史成员；W05 扩展为五类历史持久恢复逐字段一致。

## 3. 代码知识变更清单

### 新增公开函数

```cpp
服务维护历史事实范围读取结果_v1
服务合同事实权威服务::按事实代次范围读取服务维护历史事实完整组_v1(
    const 服务维护历史事实范围读取请求_v1&) const noexcept;
```

- 模块 / 文件：`海中鱼巣.领域.服务.服务合同事实权威` / `海中鱼巣/领域/服务.服务合同事实权威.ixx`。
- 用途：在一个冻结 `G0` 下读取登记覆盖范围内五类服务维护永久历史，并返回规范投影和完整见证。
- 非职责：不发布合同 / 状态 / 到期事件，不形成单调秒段，不改变 A/V、状态、动态、阈值或游标，不缓存第二历史权威。
- 参数来源：调用者提供正式 L2 请求头、自我、排除起点和总数量预算。
- 结果 / 错误：仅 `已读取` 携带完整载荷；入口、覆盖、预算、漂移、集合、引用、资源和内部错误均零载荷。
- 事实读写：只读；在同一 L1 事实代次读取七个关系闭包，无业务写副作用。
- 直接调用边：当前仅专项 / ABI consumer；后继服务维护历史段 composer 待接。
- 用途卡动作：发布后新建用途卡。

### 修改内部函数

```text
初始化历史账结构登记
形成历史账登记写集
形成进展发布写集
形成准备发布写集
首次发布历史形状同义_已锁定
形成版本化退出组_已锁定
形成历史账读取请求
```

- 用途变化：构造恢复历史账；生产进展 / 准备同步永久入账；按登记前后选择退出和重放语义；历史读取增加两个当前根反查。
- 事务变化：登记后的新事实、当前成员和历史成员同写集；更新只退出旧当前关系。
- 生命周期变化：历史成员永久，当前关系退出代次成为投影边界。
- 用途卡动作：`形成进展发布写集`、`形成准备发布写集`及服务构造相关条目标记待复核；其余内部 helper 由维护者按需要建卡。

### 测试宏函数

`ARCH_建立历史到期事件样本_v1`、`ARCH_退出历史成员关系_v1`、`ARCH_追加重复进展历史成员关系_v1`、`ARCH_追加无载荷进展历史成员_v1` 仅存在于 `ARCH_INSTINCT_SERVICE_CONTRACT_FACT_AUTHORITY_VALIDATION`，用于形成合规样本和坏账；不属于生产 ABI，不得建立生产调用。

## 4. 完成边界

本段只实现“服务维护历史事实账与事实代次范围完整读回”。未实现历史到单调完整秒段的映射、合同预支 / 余款、准备补回、低位回升门禁、批量 composer、A/V 或状态 / 动态事务、游标推进、普通应用接线、阶段三或 `INSTINCT-ROUTE` 完成。
