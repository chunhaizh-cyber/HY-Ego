# RUNTIME-GOVERNANCE-INPUT-V3-ROUTING 施工记录

日期：2026-08-20

计划：`RUNTIME-GOVERNANCE-INPUT-V3-ROUTING v0.1`

计划 blob：`26503c1f29bd8b14e1491b238c605ba623d5213f`

施工起点：`1948d0a4ce6cc221c40fbbebe4aa5301e876bce3`

## S0

- 当前分支为 `main`，S0 时 `HEAD`、`origin/main` 均为施工起点，领先/落后为 `0/0`，Git index 为空。
- 本切片只写六个计划白名单源码和本记录、验证记录；工作区中其它未跟踪详细设计、计划和构建残留原地保留，未暂存、未读取为本切片事实或带入范围。
- 已复核有效计划、对应详细设计、相关源码接口和知识库的正式基线。知识库只绑定旧正式 HEAD，故不把本次 WIP 写成已发布知识，也不修改聚合知识索引。

## 实际变更

- `线程/自我治理消息协议.ixx`：协议升至 v3；九类消息改为唯一强类型 `variant` 载荷，增加运行代次、读取截止代次及治理私有工作项/承接身份；删除 legacy 句柄组并完成路由、身份与时效纯值复核。
- `线程/任务结果回执协议.ixx`：协议升至 v2；以七项强身份和最小 `任务结果回执来源执行绑定` 替代旧冻结请求；补齐逐字段拒绝、过期、幂等和同任务顺序/上下文分账。
- `线程/有界自我治理队列.ixx`：同任务内部索引仅从强类型任务身份的稳定编码形成，不保留 legacy 句柄键。
- `线程/路由.自我治理批次.ixx`：新增九类唯一后继、路由项/结果和 `复核分类并路由治理批次`；停止为唯一已路由截断，八类普通输入均保留为合法等待。
- `线程/创建.自我线程.ixx`：按路由项逐项收口，普通合法等待不调用 002-04/05/06，也不推进成功批次游标。
- `端到端测试.自我治理一致事实.ixx`：增加 v3 九载荷与唯一路由断言，并增加回执 v2 合法、时效、身份、来源绑定、幂等和上下文分账断言。

## 代码知识变更清单

| 文件 | 新增/修改函数或类型 | 用途、合同与调用边 | 待更新用途卡 |
| --- | --- | --- | --- |
| `线程/自我治理消息协议.ixx` | 九种载荷、`取得自我治理消息任务`、`复核自我治理消息`及辅助复核 | 纯值 v3 准入；由邮箱和批次路由静态消费；不调用领域服务 | `支持线程与自我治理/自我治理消息协议函数组.md` |
| `线程/任务结果回执协议.ixx` | `任务结果回执来源执行绑定`、`复核任务结果回执材料`及辅助复核 | 纯值 v2 非权威回执准入；当前无合法 v2 生产者接线 | 任务协议与运行宿主相关用途卡 |
| `线程/有界自我治理队列.ixx` | `构造准入上下文`、`记录提交索引` | 邮箱私有的稳定编码索引；只消费消息协议任务身份 | `支持线程与自我治理/有界自我治理邮箱函数组.md` |
| `线程/路由.自我治理批次.ixx` | `复核分类并路由治理批次` | 复用分类结果形成唯一后继；不调用后继 provider | 路由/自我治理批次用途卡 |
| `线程/创建.自我线程.ixx` | `运行治理循环`的 N08-N13 分流段 | 只处理停止或合法等待；不宣称业务消费 | 自我线程创建候选用途卡 |
| `端到端测试.自我治理一致事实.ixx` | `验证运行期提供者闭环`新增断言段 | 专项验证输入协议和路由；不形成生产业务消费 | 测试用途卡（如后续维护者发布图谱） |

## 完成边界

本施工只完成治理输入 v3 ABI、任务回执 v2 强身份准入、九类唯一路由与合法等待。任务实际结果、6340 承接、002-04/05/06 实际消费或成功、需求重判、任务筹办、任务执行和完整自我治理循环均为 `NOT_RUN`。
