# 实例特征结构首次与重放截止分流施工记录

- 目标`GOAL-IFR-INSTANCE-STRUCTURE-REPLAY-FIRST-WRITE-REPAIR`，起点`f6a793f44`；只修改`存在类数据服务::登记实例特征结构`。
- 重放继续以当前已核验`G0`读回；首次写入以新增写入结果的`H`读回。二者都保留首次H、DTO、映射、幂等、owner和写集布局，首次提交前的可能发布保护不变。
- 函数级知识变更清单：修改`存在类数据服务::登记实例特征结构`；用途和普通应用调用边不变，新增首次/重放截止分流。共享用途卡待结果发布后按新blob复核。
