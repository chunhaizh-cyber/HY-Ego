# 比较派生标量与结构观测值施工记录

身份：SECONDARY-SCALAR-CONTRACT
日期：20260906
版本：v0.1
状态：候选源码、未编译/未运行/未验收；等待唯一发布者精确发布

## 1. 授权、S0 与版本

用户明确要求先落实一次值保留原表示、全部正式二次至 n 次每项 I64、各分量不同真实类型及共同来源、真实阶次最高来源加一、量纲缩放范围和溢出明确。本切片仅实现已存在 I64 比较，FC 聚合选层及身份等修改不在此范围。用户明确本轮不需要验证，其优先级覆盖技能中的默认验证步骤。

源代码起点为 e736a5ea4bf131275df5e6afdd0e0f1176e70bb4，加受保护 F WIP blob eaebdb30e2ff94b9a33be996e212fd8e366849e1。正式包 33da38b190623edf4d9a3e0dd3e4218a0a924699，首次执行 S0 的 main/远端同为 e59177a233dc32967cf7044027727fcd9192a312；index 空，已知旧 WIP 由同一代码所有者保留。登记按全路径读回，五模块和本计划两记录明确归本切片，未取得旧八文件写权。

随后消费已发布 v0.2 退出修订：HEAD/远端 344419477f9e37948577d011cc53e2a1f5d6996e；详细设计 blob b4aaa1909b42fb4d9a5b193038d381f2c58433e8，计划 blob 2f14f19b217e71c997ef73029f2776ae7667d13b，索引 blob 912b90f0bfb28c0fef520d9d50bca2ced50cc132。退出不向退出节点再提交槽变更；完整事实退出后由 L1 保留历史原槽。比较来源回执明确为可空固定二项数组。

已读取执行技能、登记、索引、正式设计、目标规范及当前 F/二次/需求/任务/方法接口；读取代码知识规则与现有知识条目，条目基线滞后处以当前代码和实际调用者为准。没有将旧 L2、旧 TEMP、旧 DB 或历史测试日志作为本轮实现依据。S0 的 Git、源码读取与内容哈希只用于身份和权限定位，不是验证通过证据。

## 2. 实际候选修改

F 新增标量 v2 请求/结果、量化、角色、输出类型、来源和读取预算 DTO。建立在原 F owner 一次发布定义、注册、不同 I64 输出 FT、自槽格式标记、来源/宿主/输出关系；输出归属通过真实 native 关系和反向唯一关系重建。普通 F 新增/修改在原键恢复之后拒绝任何自槽 FT，七类型旧构造亦能识别，直接 I64/完整 VecI64/VecU64/独立材料原 ABI 保留。

v2 共用严格注册编码与解码，读取真实两来源 DAG、基础 F 自身直接 I64 值及 FT、同创建/退出生命周期、输出 marker 和归属。显式栈、灰色环检测、共享闭包、五预算和根到叶深度有界；比较按所需角色并集复算共享定义，失败不返回任何值或来源。排序、关系、差异均返回带不同真实 FT 的 I64 值式结果；不持久化计算实例、不创建根、不维护全局缓存。量化只核显式合同，不隐式换单位；差异先拒绝溢出再减法，范围失败使整结果无值。

建立/退出以原键首次材料和规范化写集优先恢复。首次 H 与当前 Gread 分开；发布后读回异常保留已知 H/正式回执及可能发布。v2 退出完整自有事实全局排序，零显式槽变更，其他 owner 的引用由 L1 原子闭包拒绝。v1 保留治理查询、退出和已绑定原键恢复，未绑定新建明确不支持；旧裸比较 DTO/入口退出公开面。旧 F 的状态17、首次 H 历史恢复及移动返回 WIP 完整保留。

二次五方法替换为新标量合同，构造核对 F/E 同一 L1；只转发和互证，不另算比较算法。新建时核对 optional E；旧原 G 恢复不以今天 E 活动性阻断。资源、内部及漂移分账，写后已知首次 H 不丢失。

需求修复 FT/F 身份混用及拒绝同 FT 不同 F 的旧条件，从根左右实际叶闭包与真实 E 成员核验新方向。任务透传预算和完整具名结果，核对 G、请求、定义及角色集合。方法新绑定要求 v2 输出类型和真实阶次大于1，存量 v1 只通过 F 旧治理入口读取。计算成功不代表需求满足、任务完成或方法执行。

## 3. 分工与保护范围

本切片代码所有者 /root/feature_concept_execute 负责 F、二次和最终整合。三个无共享文件的消费者短时登记给 /root/feature_concept_execute/scalar_consumers；该子执行者候选完成并停写后，根将实际 writer 转回父执行者，随后统一适配可空来源回执。整个过程仍一份复杂代码切片、一个发布者，没有并行修改同一 ABI 文件。

本轮仅五生产模块及两记录。其它八份原 FEATURE-EXISTENCE-SCENE-CONCEPT-APPLICATION WIP 保留，不修改、不带入本次提交；原测试暂停现场及全部旧临时残留不触碰。本轮未新建 TEMP/消费者/工程/DB，也没有清理行为。

## 4. 代码知识变更清单

| 文件/函数身份 | 参数、结果与用途变化 | 调用和事实边界 | 用途卡动作 |
| --- | --- | --- | --- |
| F `建立标量派生定义(const 特征类标量派生建立请求&)` | 新 v2 写结果，角色量化及真实输出 FT | F owner 原键→来源读取→单写集→首次 H 读回 | 新建 |
| F `读取标量派生定义(const 特征类标量派生读取请求&) const` | Gread/H/五预算，定义及左右叶投影 | L1 历史事实/关系＋F 精确历史读取，零写 | 新建 |
| F `退出标量派生定义(const 特征类标量派生退出请求&)` | 原键、完整自有退出集合、发布确定性 | L1 同 owner 原子退出，保留历史槽 | 新建 |
| F `比较派生特征(const 特征类标量比较请求&) const` | 替代裸左右输入与 enum/int8 输出；真实 FT/I64 结果组 | 读取 DAG→角色并集合并复算→末 G；零写 | 旧业务条目退出，新条目建立 |
| F 私有 `标量编码/标量解码/标量自有定义/标量读叶/标量展开/标量复算/标量执行写` | 严格 v2 布局、来源、预算、历史、异常分账 | 只复用现 L1 与 F owner，无新 owner | 新建内部用途证据 |
| F `新增派生特征定义`、普通 `新增特征/修改特征` 类型检查 | v1 新建关闭；普通 F 拒绝自槽派生 FT；保留原键前置 | 原键历史不受新普通类型准入误拒 | 更新 |
| 二次 `生成二次特征节点(const 二次标量特征生成请求&)`、`退出二次特征节点(const 特征类标量派生退出请求&)` | 返回标量派生写结果 | 同 L1，转 F 写，保留已知 H/回执 | 更新 |
| 二次 `查询二次特征节点(const 特征类标量派生读取请求&) const`、`读取二次特征概念材料(const 特征类标量派生读取请求&) const` | v2 定义/输出/左右叶闭包投影 | 只读 F，材料不等于概念节点 | 更新 |
| 二次 `计算二次特征(const 二次标量特征计算请求&) const` | 新标量比较结果 | 仅调用 F 比较及回显互证 | 更新 |
| 需求 `计算临时需求方向(const 需求类标量方向读取请求&) const` | 预算、真实当前/目标 F、v2 来源闭包 | E 直接成员＋F＋二次比较；不写满足状态 | 更新 |
| 任务 `读取任务目标方向(const 任务类标量目标方向读取请求&) const` | 新结果/预算完整传递 | 需求方向＋原记录互证；不写任务完成 | 更新 |
| 方法私有 `验证二次特征(特征类定义身份,uint64_t,bool)` | 新绑定 v2/阶次>1，存量 v1 治理分流 | 二次材料与 F 旧治理接口 | 更新 |

相关持久结构及公开业务 DTO 条目均待基于本次实际发布提交更新。共享知识库未写入。结果提交由根发布后另行记录，不预填未发生提交。

## 5. 源码身份与未验证边界

以下仅为候选文件内容身份，不是正确性或兼容性验证：

| 文件 | SHA-256 |
| --- | --- |
| `海中鱼巣/领域/数据服务.特征类.ixx` | `c4122b5d048a96321afb2cc7e98d89103e962a6b217a634fe13f193fe0ea8de4` |
| `海中鱼巣/领域/数据服务.二次特征类.ixx` | `2fc15e1bdb1467a7ff87089c208178a17a20f40efafe51b60ee05457a87dbbdb` |
| `海中鱼巣/领域/数据服务.需求类.ixx` | `d866712df72200a99d01368ed10269f0efecb993c4d2e97c5a758c01f1e49766` |
| `海中鱼巣/领域/数据服务.任务类.ixx` | `e98ba010cbebf9956a6bce7c4940c49c1d6ef9e9dad1ca597c8e37f7a71707d0` |
| `海中鱼巣/领域/数据服务.方法类.ixx` | `86c8ac24992b5f5719d550017f127bf3963cece6b59e6cab22f007619ca8b775` |

全部验证 NOT_RUN：未执行 diff --check/strict/其它检查脚本、编译、链接、运行、测试、DB、恢复、故障注入或验收。只做普通源码阅读、编辑、协作与产物身份读取。无退出码0或测试数量可用于能力声明。

当前结论仅为候选源码、未编译/未运行/未验收。历史兼容、整数边界、预算、原键、输出类型退出保护和高阶消费者均没有本轮运行证明；全局初始化、设备输入、轮廓/体素解析、其它比较算法、持久派生值和 FC 聚合不在完成声明中。
