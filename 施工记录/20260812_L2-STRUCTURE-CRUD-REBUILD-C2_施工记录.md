# L2-STRUCTURE-CRUD-REBUILD-C2 施工记录

日期：2026-08-12

## 基线与范围

- 计划身份：`L2-STRUCTURE-CRUD-REBUILD-C2` v0.3；计划提交 `ceb3ac8640944e3ffa97e5d835a7794c15d18efa`，plan blob `c49c78c6c20c524058424da9ea192d122bdd9298`。
- 本轮起点：`02cb8ea1005e94292813efc34cdf365fe48b1b1d`。重新 S0 时 `HEAD == origin/main`、index 空，计划索引状态为 `可执行`；现行 L1 owner ABI、C1 因果合同、七路径白名单、所有权和验证资源一致，无实施漂移。
- 停工期间的两份生产 WIP 与三份专项 WIP 原地复核并继续使用，没有丢弃或重造。异主流程图、路线输入和 `.codex-build/` 原地保留，未暂存、覆盖或清理。

## 实施

- `海中鱼巣/领域/L2因果结构.数据.h` 新增条件 / 结果角色枚举，以及状态引用组替换、动作引用替换、来源换代和因果退出四组请求 / 结果 DTO，共八个 DTO。
- `海中鱼巣/领域/服务.L2因果结构.ixx` 新增 `替换因果状态引用组`、`替换因果动作引用`、`换代因果来源`、`退出因果` 四个公开写入口。
- C2 复用 C1 的同一 owner、服务、类型登记、当前 / 历史完整读取和本地键；条件 / 结果整组替换、动作有 / 无替换、来源值换代及全自有事实退出都通过一个 owner-scoped 原子写集提交，不引入第二服务、仓库、索引或缓存。
- 四类 L1 当前引用闭包保护保持在 L1 公共写端口，C2 对 `引用冲突` 原样透传。C2 因果专项按当前公开 ABI 直接构造可达的跨 owner 外部关系引用，验证阻断后由外部 owner 退出关系，再以新代次、新幂等身份成功退出因果。
- 修正 C1 当前完整读取的退出分类顺序：先裁决节点 `已退出`，不再把退出后合法的空当前关系组误判为内部不一致。
- 历史完整读取按 v0.3 固定算法：先读取因果节点单事实；若退出代次不晚于公开截止，以 `退出代次 - 1` 作为结构发现截止；公开返回截止保持不变；再按公开截止读取单事实并互证生命周期。
- 新增未登记主工程的 C2 参数化专项项目、源码和运行脚本。并发矩阵使用独立当前因果、`std::barrier` 同起步和固定轮数，无 sleep。

## 施工期裁决

- v0.2 曾要求因果专项直接构造四类外部引用。按当前 owner ABI，独立外部 owner 可合法建立“外部源节点指向因果”的跨 owner 关系，却不能把因果 owner 节点作为所属节点直接写值，也不能取得因果 owner 私有写端口。为避免新增友元、测试钩子或扩大公开 ABI，v0.3 将 C2 直接矩阵收敛为可达的跨 owner 关系阻断与关闭后退出闭环。
- “所属值、值来源、独立材料引用因果”三类保护由既有 L1 通用七角色专项提供绑定证据，不在 C2 因果专项伪造不可达夹具，也不宣称这三类在 C2 专项端到端运行。
- 初次 Debug 包装命令的上层等待在 5 秒到期，但 MSBuild 已自然完成并生成产物；随后直接运行该产物通过，之后又重新执行完整 Debug Rebuild 加运行并通过。本记录不把超时请求本身记为验证通过。

## 完成边界

本记录只说明 C2 七路径内的实际施工。它不证明因果真实性、动作成立、A1 聚合、生产消费者、崩溃恢复、跨进程或独立集成验收成立；也不将 L1 通用专项的三类值引用保护升级为 C2 因果专项直接覆盖。

## v0.4 独立核验退回修复

- 独立核验拒绝结果提交 `c55b2e5ebd59b92470139f1ebbff6395364ff75e`，具名 BLOCKER 为 `DRIFT-C2-POSTPUBLISH-AND-HISTORY-G2-RESOURCE-STATUS-MISCLASSIFICATION`，具名 HIGH 为 `VERIFY-C2-V03-MATRIX-INCOMPLETE`。
- v0.4 计划提交 `158b926904988f84f8a3b865c854d82d78a4ffe1`、plan blob `2a74b6b8abe2d23699e84196370a3f18601c0f3f`；重新施工起点为 `7c796268446f2edb359778a9778f8db5f26c808c`。S0 时 `HEAD == origin/main`、index 空，原七路径自 c55b2e5e 后无代码差异，异主流程图、路线输入与 `.codex-build/` 仍原地保留。
- 在唯一因果服务内补齐 L1 历史读取状态的许可 / 资源 / 漂移 / 内部不一致穷尽映射；完整历史首探、尾探以及组 / 单事实读取不再把资源或入口状态误报为漂移或内部不一致。
- 四写发布后读回共用同一个中性代次探测解释：非成功精确映射，成功但早于首次写入代次才是内部不一致；发布后失败载荷为空，事实截止与变更代次保留 L1 首次写入代次。原请求重复仍由 L1 幂等先于旧期望收敛。
- 不改 C2 DTO、公开签名、写集、幂等域、owner、依赖和四类 L1 引用闭包保护；未修改 L1，未增加友元、计数器、故障入口、测试钩子或生产夹具。
- 将同一生产外专项扩展为 v0.4 九组逐叶矩阵和状态组 / 动作 / 来源 / 退出四个独立并发窗。仍只有原 cpp / vcxproj / ps1 三份临时文件，未进入主工程或 filters；运行脚本继续为每轮建立随机临时输出 / 中间目录。

## v0.4 施工期问题

1. 同键异义专项辅助最初刷新了期望代次，导致幂等冲突被专项自身改写；改为保留原请求期望后直接证明 L1 幂等先于漂移。
2. 退出五族矩阵最初复用已删除动作的因果并解引用空 optional；改为先经 C2 正式动作入口补齐动作，再逐项构造族归属、条件、动作、结果、来源值闭包。未降低断言或改变生产语义。
3. v0.3 的 24 / 24、构建和记录没有升级为 v0.4 证据；v0.4 仅记录本轮重新构建和直接运行的结果。

## v0.4 第二次发布前复核修复

- Git 发布租约申请前，交互侧逐入口复核发现首次修订只闭合了发布后代次探测，四写后续的完整历史重建与逐项旧历史互证仍把 L1 非成功状态和成功坏形状合并为 `内部不一致`；先前“补齐”声明因此不作为最终结论。
- 系统扫描并修复八类下游点：状态组、动作、来源、退出各一处发布后完整历史读回；状态组逐旧关系、动作旧关系、来源旧值、退出逐自有事实四类历史互证。每个读取均先映射非成功状态，再只对成功缺载荷、错事实种类、错字段或错退出代次返回 `内部不一致`。
- 发布后下游失败统一返回空载荷，事实截止与变更代次都保留首次写入代次；L1 入口、许可、未找到、已退出、漂移、资源按现有 L2 同名状态送出，`属性未设置` 与 L1 内部不一致仍为 L2 内部不一致。
- `映射历史失败` 改为 `constexpr`，并以九个 `static_assert` 锁定 L1 历史读取枚举的全部九个叶。没有修改公开 ABI、DTO、owner、依赖、写集或幂等域，也没有形成设计漂移。
- 修复后从新源码重新运行专项 Debug / Release Rebuild 加运行、根工程双配置 Rebuild 和 strict；没有复用修复前临时产物作为新代码证据。

v0.4 完成边界仍与上节一致。C2 公开 ABI 不提供资源故障注入或私有损坏入口，相关分支只形成生产穷尽映射并保守绑定 L1 证据，不宣称 C2 端到端运行。
