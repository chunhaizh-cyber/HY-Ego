# 完整自我中性单事务生产发布施工记录

日期：2026-08-08

计划身份：`L2-COMPLETE-SELF-NEUTRAL-PRODUCTION-PUBLISH`

计划版本：v0.2

plan blob：`c7838e365572604d832b70d4a3138c82125a633c`

设计提交：`62bdb6c47734bdceac85a2bf3e2cb72940d23556`

施工起点：`31bc5d684c4837a2df11d0cf955873a2d6e4637a`

## 1. S0

- 开工时 `main == origin/main == 31bc5d684c4837a2df11d0cf955873a2d6e4637a`，ahead / behind 为 0 / 0，Git index 为空。
- 计划索引中 v0.2 为唯一可执行计划；实际计划文件、plan blob、设计提交和十二路径白名单与索引一致。
- 当前生产上下文按默认构造拥有唯一第一层事实基座服务，启动路径没有恢复、导入或重放；关系 21 角色 1 / 2 没有其它合法写入者，本计划的数据操作是本业务唯一发布者。
- 私有中性幂等域 `0x1C` 在正式代码目标外零占用。十二个授权路径没有异主活动所有者。
- P0-FIX、路线、旧 WORLD-TREE 和 `.codex-build` 均为异主现场，原地保留，不在本施工所有权内。

## 2. 实际施工

### 2.1 领域合同与准入

- 新增完整自我发布请求、存在准入请求 / 规格 / 结果、场景准入请求 / 规格 / 结果和完整自我当前事实等值式合同。
- 新增存在域纯值准入服务，只批准自我存在资格、关系 21 角色 1 和固定本地键。
- 新增场景域纯值准入服务，只批准三个新场景、三个标记值、固定父 / 成员拓扑和关系 21 角色 2 成员组。
- 两个领域规格分别形成后由组合服务逐字段互证；成员资格必须来自真实当前读取，保持严格升序，并与服务、类型和世界根身份互异。

### 2.2 唯一第一层数据操作

- 新增唯一完整自我数据操作，以私有幂等域映射不透明键，规范化形成四节点、四值、五加成员数条关系、四项属性槽变更和零退出项。
- 首次生产成员数为一，实际写集形状为四节点、四值、六关系、四属性槽、零退出；本地键连续为 1 至 14。
- 写前只通过一次中性一致当前投影复核已知世界、三个服务身份、类型节点、成员资格和已知端点竞争；没有调用旧全量读取、审计、恢复或全局遍历。
- 中性提交集中为一个物理调用点。首次成功或精确重复后，按第一层分配的稳定编码重新形成一致当前投影并逐字段读回。
- 请求、两域规格和规范化写集在首次提交前完成可分配材料准备；提交后立即固定第一层已发布边界，再只移动既有材料。提交后映射或读回失败进入进程内隔离，不撤销、不补写、不换键。

### 2.3 组合与生产接线

- 新增唯一完整自我组合服务，固定读取当前世界登记、自我结构登记、实际存在写入规格和逐项成员资格，再顺序调用两域准入与数据操作；组合与准入服务不直接访问第一层。
- 运行期上下文拥有两个准入服务、一个数据操作和一个组合服务，只向生产启动暴露发布与当前读取薄入口。
- 固定生产配置新增完整自我幂等身份。生产启动在自我结构登记成功后发布完整自我，并经组合公开入口再次全等读回；任一失败都在旧系统角色初始化前停止。
- 上下文完整性新增完整自我当前读回门禁，并互证世界根、出生期成员和事实截止。
- 工程与 filters 登记一个数据头和四个模块，没有新增专项自检、验收程序、第二目标或本地重复日志。

## 3. 施工校正

- 首次 Debug 重建暴露数据头在模块 purview 中重复引入标准库的问题；按仓库既有数据头模式增加模块内 include 抑制，并把所需标准库头放在全局模块片段中。
- 修正一个固定节点数组的聚合初始化层级。
- 静态复核发现首次提交后复制动态材料可能在第一层已写入后抛出分配异常；改为提交前准备、提交后移动，并在成功 / 精确重复后立即保存第一层已发布边界。
- 补齐场景规格对成员和固定服务 / 类型身份互异的自洽检查，以及新增纯值 helper 的诊断责任标注。
- 为同时满足首次提交和同键精确重复收敛，将第一层中性提交集中到一个模块内部调用点；两个公开业务根仍各条路径至多提交一次。

## 4. 范围与声明边界

本计划实际修改十个生产 / 工程路径，并新增本记录和配对验证记录，共十二个授权路径。没有修改第一层核心、规范、详细设计、流程图、计划、计划索引或任何异主 WIP。

本施工只实现完整自我这一第二层生产初始化业务真实使用第一层的中性新增与正式读取。它不证明崩溃、重启、恢复、跨进程收敛、旧系统角色、实例槽、状态、动态、需求、任务、方法或治理闭环完成，也不构成第一层自证、第一层整体完善或正式集成验收结论。
