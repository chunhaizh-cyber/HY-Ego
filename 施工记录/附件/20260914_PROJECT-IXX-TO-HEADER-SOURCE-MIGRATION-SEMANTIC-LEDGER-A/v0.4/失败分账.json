{
  "schema": "PROJECT-IXX-TO-HEADER-SOURCE-MIGRATION-SEMANTIC-LEDGER-A-failure-account-v0.4",
  "status": "incomplete-evidence",
  "blockingContract": "v0.4 requires every actual cl command to carry one unique pair of /scanDependencies and /sourceDependencies outputs and also requires a successful Rebuild.",
  "observed": [
    {
      "result": "metadata is lost by SelectClCompile because it runs after BeforeTargets=ClCompile",
      "attempt": "before-target injection"
    },
    {
      "result": "with /scanDependencies, compiler produces dependency JSON but no object file; Rebuild exits 1 at LNK1104",
      "attempt": "after-SelectClCompile per-source AdditionalOptions"
    },
    {
      "result": "exit 0 but only one scan/source JSON is produced for 59 TU; no 59/59 closure",
      "attempt": "ClCompile-only"
    }
  ],
  "firstDiagnostic": "LINK : fatal error LNK1104: cannot open ...\\\\obj\\\\Debug-x64\\\\装配.普通应用.obj after /scanDependencies run.",
  "requiredResolution": "Revise the plan to separate scan-only dependency collection from object-producing source dependency compilation, and freeze an injection strategy that applies after SelectClCompile and yields one output per actual compiler invocation."
}
