# INSTINCT-STAGE3-MAINTENANCE-BASE-SNAPSHOT-COMPOSER 施工记录 v0.1

日期：2026-08-29

## 1. 施工身份与边界

- 计划：`INSTINCT-STAGE3-MAINTENANCE-BASE-SNAPSHOT-COMPOSER v0.1`
- plan blob：`d67780e98203264135ad8a793d17448098651fa1`
- 设计提交：`cee91f6ab331ed3b048b91894e36c20bcb411ee9`
- S0 起点：`5c6ec5519978314d201f37fc0b8f525b8eb432b5`
- 两配置与专项实际使用的代码候选 tree：`1e990f5bf7e68742034f798fc10d2b17064505e5`
- 隔离根：`D:/TEMP/海中鱼巣/INSTINCT-STAGE3-MAINTENANCE-BASE-SNAPSHOT-COMPOSER/20260829-215000-r2`

本次只实现同一 `G0` 下的本能被动维护基础快照只读组合器、强类型数据、专项和工程登记。未建立第二 owner，未写入 A/V/L/H，未推进游标，未调用单秒规则内核，未发布状态或动态，也未把尚未提供的服务合同、活动、到期或安全门禁解释成合法空集合。

安全根、服务根值域是 `0..INT64_MAX`。L/H 的“浮动”指正式定义可随后继版本变化，不是浮点数，也不是每秒自动变化；同一已发布定义版本内 L/H 确定。当前版本 1 的 L/H 是 `floor(INT64_MAX*30/100)` 与 `floor(INT64_MAX*80/100)`，组合器只读正式定义事实，不重算百分比、不缓存也不硬编码版本 1。

## 2. 实际变更

新增：

- `海中鱼巣/领域/本能被动维护基础快照.数据.h`
- `海中鱼巣/领域/组合.本能被动维护基础快照.ixx`
- `海中鱼巣/端到端测试.本能被动维护基础快照.ixx`

`海中鱼巣.vcxproj` 与 `海中鱼巣.vcxproj.filters` 只登记上述三个源码。两个混合工程文件的 index 由 `HEAD + 本计划三项登记` 构造，工作树其它 SELF-GOVERNANCE WIP 未进入候选。

组合器按计划顺序绑定本能根锚点、安全根正式定义与 A 当前值、服务根 V 当前值、被动维护游标、单调完整秒观察及 `0/N` 待维护区间。`已读取` 与 `无新完整秒` 都是完整成功快照；只有前者 `可维护()==true`。其它状态统一空快照、截止 0。

## 3. 函数级代码知识变更清单

| 函数/身份 | 类型 | 用途与合同 | 调用边与后继 |
| --- | --- | --- | --- |
| `本能被动维护基础快照完整_v1` | 新增 | 验证锚点、定义、A/V、游标、时间观察、0/N 区间和同截止形状 | 结果成功谓词和组合器返回前调用 |
| `本能被动维护基础快照读取结果_v1::成功` | 新增 | 只接受 `已读取/无新完整秒 + 完整快照 + 非零同截止` | 专项、ABI consumer 和后继完整集合提供者消费 |
| `本能被动维护基础快照读取结果_v1::可维护` | 新增 | 只接受 `已读取 + 成功 + 正完整秒区间` | 后继批量分段与事务写入前门禁 |
| `本能被动维护基础快照组合器::读取本能被动维护基础快照_v1` | 新增 | 在同一 G0 下只读组合四个已有权威服务并执行读前/读后互证 | 当前只有专项与仓库外 ABI consumer；后继完整集合 owner/provider 待实现 |
| `运行本能被动维护基础快照端到端测试` | 新增 | 执行 P00—P08 基础快照专项 | 仅隔离验证临时入口调用，未接生产入口 |

待能力证据目录/函数用途维护者在结果发布后按最终 source blob 建立用途卡；本记录不直接修改共享知识索引。

## 4. 施工异常与处理

1. 首次隔离构建发现组合器未显式导入本能根锚点的正式定义模块，并且强身份类型不允许默认构造。在计划范围内补显式 module import，并将快照改为聚合构造；未改变公开字段或机器语义。
2. 仓库外 consumer 首次编译发现 MSVC 不能只凭组合器自身 IFC 解析传递导入。从精确隔离树的已构建 IFC 生成仓库外只读 module-name 映射后，Debug/Release 编译、链接和运行通过；未修改生产代码。
3. 正式工程声明 v145，本机未安装。全部验证使用 VS 2022 Professional、MSVC 14.44.35207，并以 `PlatformToolset=v143` 覆盖；未修改工程声明冒充 v145 证据。

## 5. 完成边界

本次最多证明阶段三基础快照组合器可编译、可由独立 consumer 消费，并满足 P00—P08。服务合同、有效活动、到期未满足事件和安全低位回升门禁的正式完整集合、批量分段、游标推进、A/V 原子发布、状态/动态及生产循环接线仍未完成。
