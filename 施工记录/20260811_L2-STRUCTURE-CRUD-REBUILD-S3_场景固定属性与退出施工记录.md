# L2-STRUCTURE-CRUD-REBUILD-S3 场景固定属性与退出施工记录

日期：2026-08-11

计划：`L2-STRUCTURE-CRUD-REBUILD-S3 v0.2`

计划 blob：`1d1dc204822acc754bd91ab540354a7526327ade`

执行起点：`main == origin/main == 57cec91f23d595aa9ff7a74b206fffcf23d6b516`，分歧 `0/0`，Git index 为空。

## S0 与现场

- v0.2 是计划索引唯一 `可执行` 代码计划；S1、S2、关系类型登记、L1 历史关系组、L1 历史属性值组和 L1 当前引用闭包保护均为 `已退出`。
- L1 数据头、仓库、服务 blob 分别为 `22ff4b162fae1a97e8f008342acd0a1b4d157c22`、`e487679fafba528eec680c76b5bca5a8774ae822`、`1bdc35e24d6873cec00a55516a8aaddd061192ea`。
- 场景数据头和服务的 HEAD blob 分别为 `e6825830187682f39099beff8ee9ff997d32c1b6`、`dd43524901404dab26895a3b41b8203ca614842e`；4230 blob 为 `f00b4c746d93d10de18c2fd54af6bc36fe1a1b9d`。
- 接回 v0.1 停止时的两个生产文件和三个专项文件 WIP；两份记录在恢复时尚不存在。
- 流程图、路线输入和 `.codex-build/` 为异主 WIP，施工未修改、暂存或清理这些路径。

## 实际施工

### `海中鱼巣/领域/L2场景结构.数据.h`

- 增加固定属性新增、固定属性换代、固定属性写入结果、场景退出请求和场景退出结果五个 DTO，并补齐默认逐字段比较。
- 增加三个请求有效性、属性截止投影完整性和最终场景完整性五个纯值辅助。
- 将 `L2场景S2事实完整` 收敛为最终场景完整性兼容包装；S1 创建完整性继续只接受空属性和空关系。
- 增加固定属性写入和场景退出成功载荷检查，区分当前属性、同代次旧值退出和整场同代次退出形状。

### `海中鱼巣/领域/服务.L2场景结构.ixx`

- 三个 L1 写入状态 switch 显式覆盖 `引用冲突`；S2/S3 映射为 L2 引用冲突，S1 和关系类型登记映射为内部不一致。
- 增加本地键 `0x0201'0301U`、L1/L2 四类材料转换、属性事实投影和允许有序属性槽的私有场景节点骨架。
- 增加 `新增场景固定属性`、`换代场景固定属性`、`退出场景` 三个公开函数。
- 固定属性初始以一份 L1 写集建值并设槽；换代以一份写集建新值、切槽并退出具名旧值；成功和精确重复均按首次事实代次历史读回。
- 当前完整场景使用两次同期 L1 一致当前投影发现槽并取得值、父子关系和成员关系；可选父关系只供退出快照。
- 历史完整场景加入 L1 历史属性值组，只以历史截止有效值重建属性。
- 场景退出逐字段核对请求显式闭包，再以一份 L1 写集退出场景、父关系、子关系、成员关系和固定属性值；L1 跨结构引用冲突精确透传。
- `读取场景节点` 保留签名并透传同一个完整场景读取结果。

### 生产外验证

- 新建独立 Debug/Release x64、v145、C++20、Unicode、`/utf-8` 专项项目和 PowerShell 包装。
- 专项只 import L1 公开服务、L2 存在结构和 L2 场景结构；不进入主工程或 filters。
- v0.2 删除 v0.1 诊断期的 4096 次 `yield` 和固定 `NOT-OBSERVED` 文本；第 16 项无 sleep、无 yield、无忙循环，逐配置统计 `已读取 / 事实代次漂移 / 许可拒绝`。
- 补强场景退出后父、子、存在端点当前性，全部退出编码 L1 历史同代次读回，以及零身份、零类型、零来源、空材料、零独立材料引用、未排序和重复闭包入口拒绝。

## 实际验证摘要

- 主工程 Debug x64 Rebuild：PASS。
- 主工程 Release x64 Rebuild：PASS。
- 专项 Debug 1—16：PASS；并发计数 `已读取=0 / 事实代次漂移=0 / 许可拒绝=16`。
- 专项 Release 1—16：PASS；并发计数 `已读取=0 / 事实代次漂移=0 / 许可拒绝=16`。
- strict：`111/111`。
- UTF-8/XML：主工程 26 个模块、12 个头、4 项 `/utf-8`；专项 8 个模块、1 个入口、2 项 `/utf-8`；专项未进入主工程或 filters。

## 异常与边界

- v0.1 无人工让行的并发诊断曾观察到合法 `许可拒绝 + 截止0 + 空场景`，由 v0.2 修订验证预言机；含固定让行的旧 Debug/Release 结果未作为本记录通过证据。
- v0.2 要求清理系统临时专项根目录；该递归删除命令被当前环境策略拒绝。包装脚本每轮使用全新 GUID 输出/中间目录，本轮构建未读取旧目录，旧输出不计证据。
- 两配置本轮只观察到许可拒绝，未观察到并发已读取或并发漂移；公开结果不能定位推进是否恰好发生在当前两次投影或历史前后夹取之间，因此内部窗口未证明。
- 未验证生产装配、真实业务消费者、集成验收、长期公平竞争、资源故障注入、崩溃恢复、跨进程或其它 L2 结构。

## Git 边界

本执行段按上级指令停止于七文件 WIP，不暂存、不提交、不推送。构建和专项通过不等于生产装配或独立集成验收完成。
