# 实例特征场景专用旧接口物理退出施工记录

日期：2026-08-08

## 1. 施工身份与范围

- 正式起点：`ba0c3556dcf19d0bf851b6ebb5ad73d2fc5e1e06`
- 计划身份：`L1-DATA-LAYER-COMPLETE-P13-INSTANCE-FEATURE-LEGACY-SCENE-API-REMOVE`
- 计划 blob：`c24acebfc32ae500a50342e06bbaf5d6450e912e`
- 生产代码：`海中鱼巣/领域/L1实例特征.数据.h`、`海中鱼巣/领域/服务.L1实例特征.ixx`
- 配套记录：本记录与同名构建、生产链验证记录

施工前 `main == origin/main`，ahead / behind 为 0 / 0，Git index 为空。两个目标代码文件没有异主修改；P0-FIX、路线、旧 WORLD-TREE 和 `.codex-build` 现场原样保留，未修改、未暂存。

## 2. 实际变更

1. 删除初始场景宿主的建立、读取和当前值换代三个公开入口，不建立别名、兼容转发或替代入口。
2. 删除三个旧请求数据结构和两个旧请求有效性检查；正式实际存在宿主的建立、读取、换代请求与结果保持不变。
3. 删除只服务于旧入口的领域意图编码、旧幂等探测、旧写集形成与提交、执行证据提供者组装、完整快照唯一性扫描、首次建立缓存、旧换代账和专属精确读回辅助。
4. 删除由旧闭包独占的标准库依赖；保留正式中性读取仍使用的变体访问依赖。
5. 保留实例特征登记、实际存在宿主建立、实际存在宿主读取、实际存在当前值换代、三处中性写入、逐项权威读回、锁、定位、状态映射、构造函数和运行期装配形状。
6. 第一层核心、其它领域服务、运行期上下文、启动、工程、filters、自检、规范、设计、计划和计划索引均未修改。

## 3. 施工中修复

首次 Debug 完整重建发现，删除旧换代账时一并移除了正式中性读取仍需要的标准变体头文件，导致变体访问函数不可见。该问题属于允许文件内的机械依赖清理错误；恢复该必要头文件后，Debug 与 Release 完整重建均以零警告、零错误通过。没有恢复任何旧请求、旧入口、旧写入或旧验收机制。

## 4. 完成边界

本次施工只物理退出实例特征服务中无人调用的初始场景宿主旧表面及其旧第一层依赖，并证明保留的正式生产代码仍可完整构建和保持模块闭合。

本次没有建立第一层专项验收、外部补格消费者、双目标、日志判定或恢复验证，也没有运行后续数据服务。第一层是否足以支撑项目，只能由第二至第五层为完成自身业务而在实际运行中使用中性数据操作并完成各自合同后判断。
