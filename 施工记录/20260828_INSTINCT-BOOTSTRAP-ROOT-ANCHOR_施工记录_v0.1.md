# INSTINCT-BOOTSTRAP-ROOT-ANCHOR 施工记录 v0.1

日期：2026-08-28

计划身份：`INSTINCT-BOOTSTRAP-ROOT-ANCHOR`

执行计划：`计划/20260828_INSTINCT-BOOTSTRAP-ROOT-ANCHOR_本能根运行锚点代码实施计划_v0.4.md`

plan blob：`1872e84347f2392b6fbccee4e391c2829881dc92`

详细设计：`规范/详细设计/20260828_INSTINCT-BOOTSTRAP-ROOT-ANCHOR_本能根运行锚点详细设计_v0.3.md`

代码起点：`518d7b54b44caaf1fa6a0ea3b9bfbe6281490604`

隔离候选：`D:\TEMP\海中鱼巣\INSTINCT-ROOT-ANCHOR\candidate-final-01`

## 1. S0 与所有权

恢复施工时重新核对：

```text
branch = main
HEAD = origin/main = 518d7b54b44caaf1fa6a0ea3b9bfbe6281490604
ahead/behind = 0/0
index = empty
plan = v0.4 / blob 1872e84347f2392b6fbccee4e391c2829881dc92 / 可执行
```

直接前置 `DATA-L1-PERSISTENT-RECOVERY-MANAGEMENT-PLANE` 已由提交
`db65bc99c48310935509e3fdf05b22e71ae9b6b1` 发布。根工程仍为
`海中鱼巣.vcxproj`，本计划冻结 VS 2022 `v143` Debug/Release x64。

工作区存在 L4、规范、流程图和计划目录清理等异主 WIP。本计划不覆盖、不回退、
不整文件暂存共享文件；正式发布内容以“起点 HEAD + 隔离候选精确增量”为准。
`计划/计划索引.md` 和历史计划移动由计划目录清理窗口持有，不纳入本结果提交。

## 2. 既有能力复用裁决

| 目标能力 | 当前证据 | 裁决 |
| --- | --- | --- |
| 跨 owner 单 G1 | 现行 L1 双参与者原子运输、owner 写端口和首次账 | 直接复用；不新增 L1 入口 |
| 唯一自我、定义、实例、合同、需求读写 | 既有存在、特征、状态、需求四个 L2 服务 | 组合复用；provider 只注入这四个服务 |
| 根需求列表项定位 | 既有 `按精确目标查询需求列表项` | 直接复用；不建立第二索引 |
| 跨进程启动恢复 | 已发布 L1 持久证据和普通应用恢复门禁 | 作为启动前置复用；本计划不建立业务快照 |
| 首值、初态、当前选择单 G1 | 现有公开入口不能一次完成 | 在状态服务扩展具名组合 ABI |
| 目标合同按幂等首次账恢复 | 现有完整读取要求预知合同身份 | 在状态服务扩展具名读取 ABI |
| 双根冷启动编排与值式锚点 | 无现行 provider 或用途卡命中 | 新建业务 provider 和 DTO |

执行前查询 `知识库/函数用途/` 和能力证据目录，新函数均无现行用途卡命中；因此
没有把候选名称冒充既有能力。既有函数用途按当前源码、正式规范、详细设计和计划互证。

## 3. 实际代码变更

### 3.1 新增文件

- `海中鱼巣/业务/本能根运行初始化.数据.h`
  - 定义值式锚点、初始化请求/结果/状态、`I64_MAX` 与下中点常量。
  - 差值只由纯值函数按需形成，不取得身份、实例、事实代次、状态、动态或恢复入口。
- `海中鱼巣/业务/提供者.本能根运行初始化.ixx`
  - 固定安全/服务两套互异幂等身份。
  - 按定义、二次概念、实例、首值首态、比较注册、目标合同、根需求和列表项顺序形成或恢复。
  - 冻结 `Gfinal` 后逐项正式读回，闭合后才返回值式锚点。

### 3.2 修改文件

- `海中鱼巣/领域/L2状态结构.数据.h`
  - 增加目标合同幂等首次完整读取 DTO、状态、有效性和成功谓词。
  - 增加首值首态原子形成 DTO、状态、有效性和成功谓词。
- `海中鱼巣/领域/服务.L2特征结构.ixx`
  - 增加仅供状态服务使用的首值参与者预形成和编码映射核对。
  - 不增加公开写入口，不形成动态。
- `海中鱼巣/领域/服务.L2状态结构.ixx`
  - 特征服务构造依赖由 const 引用收紧为可协作引用，依赖方向仍为状态到特征到 L1。
  - 增加目标合同幂等首次完整读取。
  - 增加首值、初态、当前选择跨 owner 单 G1 形成和正式读回。
- `海中鱼巣/程序运行结果.数据.h`
  - 增加稳定失败阶段 `本能根运行初始化 = 21`，不改阶段 20。
- `海中鱼巣/启动.应用程序.ixx`
  - 方法登记根成功后、自我线程创建前执行本能根初始化。
  - 初始化非成功时 fail-closed；成功锚点进入线程创建请求。
- `海中鱼巣/线程/自我线程生产创建.数据.h`
  - 创建合同版本升为 2，请求增加完整本能根锚点。
- `海中鱼巣/线程/创建.自我线程.ixx`
  - 锚点进入请求有效性、语义相等和既有整请求锁定。
- `海中鱼巣.vcxproj`
- `海中鱼巣.vcxproj.filters`
  - 只登记两个新增业务文件。

### 3.3 白名单内未形成语义增量

`海中鱼巣/领域/L2特征结构.数据.h` 的隔离候选与 HEAD 只有工作树换行表示差异；
按 Git 归一化比较无语义差异，因此不纳入结果提交。

### 3.4 明确未修改

未修改 L1、L2需求服务、旧 `新增状态_v2`、动态/因果/任务/方法/结果结算、
`海中鱼巣/入口.cpp`、生产专项、自检、测试宏、故障注入和其它 L4 WIP。

## 4. 施工中修复

1. VS 2022 `v143` 早期候选编译发现类作用域内非法 namespace alias；已移到 namespace 作用域。
2. 后续候选编译发现 `L2特征实例完整读取结果` 不存在 `成功()`，以及调用了不存在的二参数
   `L2状态事实完整`；已改为核对正式结果头并显式核对生命周期。
3. 新 DATA 合同的底层端口/装配许可异常按冻结合同映射为 `内部不一致`，没有新增 `许可拒绝`。

以上修复没有扩大公开 ABI、文件白名单或业务目标。

## 5. 代码知识变更清单

当前知识库没有下列新增函数的现行用途卡。发布后由代码知识维护者以结果提交和本清单
为输入建立或更新用途卡；本记录不冒充知识库已经回写。

| 稳定函数身份 | 文件 | 变化 | 用途与直接调用边 | 验证状态 |
| --- | --- | --- | --- | --- |
| `L2目标状态合同幂等首次完整读取请求有效_v1` | `L2状态结构.数据.h` | 新增 | 纯值验证新读取请求 | 静态检查、双配置构建 PASS；运行 NOT_RUN |
| `L2目标状态合同幂等首次完整读取结果_v1::成功` | 同上 | 新增 | 只接受已读取、完整合同和非零截止 | 静态检查、双配置构建 PASS；运行 NOT_RUN |
| `L2特征首值初始状态原子形成请求有效_v1` | 同上 | 新增 | 纯值验证组合写请求 | 静态检查、双配置构建 PASS；运行 NOT_RUN |
| `L2特征首值初始状态原子形成结果_v1::成功` | 同上 | 新增 | 核对成功状态、三载荷、同 G1 与精确值 | 静态检查、双配置构建 PASS；运行 NOT_RUN |
| `L2特征结构服务::形成首值原子参与者写集_v1` | `服务.L2特征结构.ixx` | 新增 private | 为状态服务预形成特征 owner 首值写集 | 静态检查、双配置构建 PASS；运行 NOT_RUN |
| `L2特征结构服务::首值编码映射完整_v1` | 同上 | 新增 private | 核对首值稳定编码映射 | 静态检查、双配置构建 PASS；运行 NOT_RUN |
| `L2状态结构服务::L2状态结构服务` | `服务.L2状态结构.ixx` | 修改 | 特征服务引用从 const 收紧为具名原子协作引用 | 双配置构建 PASS |
| `L2状态结构服务::读取目标状态合同幂等首次完整事实_v1` | 同上 | 新增 public | 从首次账恢复身份并按请求 G0 完整读回 | 静态检查、双配置构建 PASS；运行 NOT_RUN |
| `L2状态结构服务::新增特征首值与初始状态_v1` | 同上 | 新增 public | 单次 L1 双参与者事务形成首值、初态和当前选择 | 静态检查、双配置构建 PASS；运行 NOT_RUN |
| `本能根运行锚点_v1::完整` | `本能根运行初始化.数据.h` | 新增 | 纯值核对锚点身份、角色分离和截止 | 静态检查、双配置构建 PASS |
| `形成本能根差值二次特征值_v1` | 同上 | 新增 | 按需计算 `I64_MAX - 当前值`，结果用完即弃 | 静态检查、双配置构建 PASS；边界运行 NOT_RUN |
| `本能根运行初始化结果_v1::成功` | 同上 | 新增 | 只接受已形成/已恢复和完整锚点 | 静态检查、双配置构建 PASS |
| `本能根运行初始化提供者::本能根运行初始化提供者` | `提供者.本能根运行初始化.ixx` | 新增 | 注入存在、特征、状态、需求四个 L2 服务 | 双配置构建 PASS |
| `本能根运行初始化提供者::初始化并读取本能根运行锚点_v1` | 同上 | 新增 public | 冷启动形成/恢复两根并执行 Gfinal 全量读回；由 `运行普通程序` 唯一调用 | 静态检查、双配置构建 PASS；业务运行 NOT_RUN |
| `本能根运行初始化提供者::{读取当前事实代次,读取并验证唯一自我,确保定义,确保实例,确保首值首态,确保比较注册,确保目标合同,确保根需求,验证根需求闭包,最终完整读回,最终读回一根,失败}` | 同上 | 新增 private | 分阶段组合既有 L2 服务并统一结构化失败 | 静态检查、双配置构建 PASS；业务运行 NOT_RUN |
| `本能根运行内部::{映射结构失败,当前读取头完整,写入成功状态,形成比较合同材料,定义符合规格,实例符合规格,比较注册符合规格,目标合同符合规格,需求符合目标,列表项符合目标,值态符合规格}` | 同上 | 新增 internal | 纯值映射和同截止闭包验证，不建立第二权威 | 静态检查、双配置构建 PASS |
| `运行普通程序` | `启动.应用程序.ixx` | 修改 | 在方法登记根后调用 provider，并把锚点交给唯一生产线程创建调用 | 静态检查、双配置构建 PASS；生产运行 NOT_RUN |
| `请求语义相同` | `创建.自我线程.ixx` | 修改 | 把锚点纳入整请求精确重复 | 双配置构建 PASS；运行 NOT_RUN |
| `请求有效` | 同上 | 修改 | 要求线程创建请求携带完整锚点 | 双配置构建 PASS；运行 NOT_RUN |

退出函数：无。

## 6. 完成边界

本施工结果只提供并接入线程创建前的阶段一本能根运行锚点。它不证明根需求展开、
具体需求、任务、方法、结果结算、业务运行、跨进程恢复或整体自我治理闭环已经通过验收。
