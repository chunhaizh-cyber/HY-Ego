# INSTINCT-STAGE3-UTC-TIME-EVIDENCE 施工记录

日期：2026-08-29

计划：`INSTINCT-STAGE3-UTC-TIME-EVIDENCE` v0.1

计划 blob：`5121995d25f5f9f9308284f9c4f15d9e2b7f06ec`

起点：`5b9ad7fa82e3ea4fd4c5eacb3831b6a8f909e89b`

结果提交：本记录与实现候选同提交形成；精确哈希以发布回传为准。

## 1. S0 与所有权

- 开工时位于 `main`，`HEAD == origin/main == 5b9ad7fa82e3ea4fd4c5eacb3831b6a8f909e89b`、ahead/behind `0/0`、index 空；计划索引唯一执行项为本计划 `可执行`，索引登记 blob 与当前计划 blob 相同。
- 共享工作区保留 SELF-GOVERNANCE 异主 WIP。本切片只写计划列出的 UTC 适配器、启动自有调用边、专项、两项工程登记和两份专属记录；不覆盖、删除或暂存异主增量。
- `启动.应用程序.ixx`、`海中鱼巣.vcxproj`、`海中鱼巣.vcxproj.filters` 是混合文件。计划前差异已固定；发布时以 `HEAD + 本计划增量` 构造 index，不整文件暂存。
- 当前仓库只有以 `steady_clock` 提供经过时间的单调时钟适配器，没有等价的公开强类型绝对 UTC 证据入口。直接复用 `system_clock` 的启动调用不具备版本、结构化状态和完整成功形状，因此按冻结设计新增适配器。

## 2. 实际施工结果

1. 新增版本 1 的 UTC 读取请求、读取状态、可信 UTC 证据、读取结果和完整成功谓词。
2. 新增无状态 `UTC时钟适配器::读取当前UTC时间证据_v1(...)`：校验合同和时间源版本，以 `system_clock` 读取 Unix epoch 纳秒，并对请求拒绝、版本漂移、计数不可表示和内部不一致结构化分账；所有非成功结果空载荷。
3. 适配器不缓存、不写事实、不修正墙钟回拨、不承诺严格单调，也不参与完整秒、衰减、维护游标或经过时间计算。
4. `运行普通程序(...)` 在本能根运行初始化前消费完整 UTC 证据；证据不完整时沿既有 `本能根运行初始化` 失败阶段退出，不创建自我线程。成功时只把正 UTC 纳秒交给既有初始化请求。
5. 新增 U00—U04 专项模块及工程登记；U05—U07 由两配置 ABI / 根工程、生产静态接线、strict、精确 index 和提交树门禁证明。
6. 仓库外专项工程和产物位于 `D:\TEMP\海中鱼巣\INSTINCT-STAGE3-UTC-TIME-EVIDENCE\20260829-01`。

## 3. 代码知识变更清单

| 模块 / 文件 | 稳定函数身份 | 用途、合同、非职责与调用边 | 用途卡动作 |
| --- | --- | --- | --- |
| `适配器.UTC时钟.ixx` | `UTC时间证据读取结果_v1::成功() const noexcept` | 只认可 `已读取 + 完整证据 + 合同版本1 + 正UTC纳秒 + 时间源版本1`；不以单一状态码或裸载荷宣称成功。 | 新建 |
| 同上 | `UTC时钟适配器::读取当前UTC时间证据_v1(const UTC时间证据读取请求_v1&) const noexcept` | 公开、无状态、无持久副作用地读取 Unix epoch UTC 纳秒并结构化分账；不提供单调时钟、定时、完整秒或业务事实写入。 | 新建 |
| `启动.应用程序.ixx` | `运行普通程序(...)` | 本能根初始化的绝对发生时间来源由裸 `system_clock::now()` 改为强类型 UTC 证据入口；新增调用边 `运行普通程序 -> UTC时钟适配器::读取当前UTC时间证据_v1`。既有运行代次兼容用法未改。 | 更新 |
| `端到端测试.本能UTC时间证据.ixx` | `运行本能UTC时间证据端到端测试() noexcept` | 通过公开 ABI 验证 U00—U04，不用等待或严格递增断言制造正确性。 | 新建测试用途卡或登记为测试函数 |

没有退出公开函数。共享知识库、函数用途卡、能力证据目录和调用图不在计划白名单；结果发布后由知识库维护者依据本节与结果提交新建或更新用途卡。

## 4. 完成边界

- 本记录最多证明绝对 UTC 发生时间证据提供者及生产启动调用边，不证明完整秒维护、A/V 变化、状态/动态、游标推进、L/H、合同/活动集合或完整阶段三。
- 无法在不引入测试时间源或新增 ABI 的前提下稳定触发 `计数不可表示`、系统时钟非正或底层时钟异常；这些生产防御分支保留静态证明，不虚构运行覆盖。
- 共享工作树其余 dirty 均不是本计划结果；发布时仍须以精确 index 和隔离提交树复验排除异主 WIP。
