# L2-STRUCTURE-CRUD-REBUILD-A1 六服务聚合与生产装配施工记录

日期：2026-08-13

## 基线与范围

- 起点：`main`，`HEAD == origin/main == 34ac1ac588aa86237853204ce10b92f5398f2349`，ahead / behind 为 `0 / 0`，index 为空。
- 计划：`计划/20260812_L2-STRUCTURE-CRUD-REBUILD-A1_六服务聚合与生产装配代码实施切片_v0.1.md`，plan blob `ea1d2bf9a5c9546ff7aa60c71e25f76eaf67d276`；索引状态为 `可执行`。
- S0：六服务、普通应用 ABI、状态最大值 22、31 个生产 `.ixx` / 17 个头、四配置 `/utf-8` 均与激活登记匹配；九条允许路径无异主写入，构建与验证资源无冲突，无 DRIFT。
- 异主 `流程图/**`、`计划/20260803_后续计划持续制定共识与路线输入.md` 与 `.codex-build/` WIP 原样保留，未写入、未暂存、未清理。

## 实际施工

1. 新建 `海中鱼巣/领域/服务.L2结构聚合.ixx`：唯一导出 `L2结构聚合服务`，只保存场景、存在、特征、状态、动态、因果六个非 owning 左值引用；禁止默认、复制和移动；提供十二个直接返回原引用的 const / 非 const getter。
2. 修改 `海中鱼巣/装配.普通应用.ixx`：导入聚合模块；在六服务成员之后追加唯一聚合 `unique_ptr`；保留六服务直接 getter；新增聚合 getter；场景服务成功后形成聚合；追加连续状态 `L2结构聚合服务构造失败 = 23`；成功时一次移动六服务与聚合进入上下文。
3. 修改根工程与 filters：各恰一次登记 `海中鱼巣\领域\服务.L2结构聚合.ixx`，不登记临时验证项目。
4. 新建生产工程外专项三件套。验证项目只消费正式模块和 `构造普通应用上下文`，输出与中间文件位于系统 `%TEMP%` 下 GUID 隔离目录。

正式代码没有验证实体、故障钩子、计数器、sleep、测试分支、根缓存、L1 写面、DTO/CRUD 包装或旧世界树入口。聚合分配失败未做侵入式故障注入，验证结论明确记为 `NOT_RUN`。

## 验证与问题处理

- 专项初次编译暴露既有若干默认 `operator==` 在 MSVC 下因 `optional` 内部比较约束不可用而被删除；仅在验证 CPP 中改为逐字段 helper。
- 专项第一次完整运行仅“六服务合法只读结果”失败，原因是验证 helper 错误假定普通应用装配后的类型登记必为空；修正为六服务分别逐字段比较。未改生产代码和公开 ABI。
- 最终专项 Debug / Release 构建与运行均 PASS。
- 根工程 Debug / Release x64 Rebuild 均 PASS。
- `python .\tools\check_specs.py --strict`：113 / 113 PASS。
- 严格 UTF-8、无 BOM / U+FFFD、三份工程 XML、工程计数、四配置 `/utf-8`、聚合六引用 / 十二 getter、禁止项、启动入口零 diff、生产验证痕迹扫描和白名单 diff 检查均 PASS。

## 完成边界

本施工只形成普通应用持有的透明六服务同实例聚合。它不建立、选择、猜测、改变或缓存世界树根，不进入 I1 / I2、自我初始化或启动业务消费，不证明完整 L2 业务闭环、生产运行或独立集成验收。临时三件套须在结果被交互侧接受且计划索引退出后，以独立清理提交删除。
