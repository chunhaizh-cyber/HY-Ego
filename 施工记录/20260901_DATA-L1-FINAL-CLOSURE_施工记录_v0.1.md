# DATA-L1-FINAL-CLOSURE 施工记录 v0.1

日期：2026-09-01

## 1. 施工身份与边界

- 计划：`DATA-L1-FINAL-CLOSURE v0.1`
- plan blob：`fad9e1bc26517fd2f8ef9a4b348a45636609c6fc`
- 设计提交：`e424d96a5fa7b79a289c472abc5c32d42ab3b1a0`
- 施工起点：`main@5e57055d053184822834f38d7f87d919e528be94`
- 起点与 `origin/main` 一致，ahead / behind 为 `0 / 0`，施工前 index 为空。
- DATA-L1 通用操作全集 / 目标公开合同状态为 `FROZEN`；整体能力状态继续保持 `PENDING_ACCEPTANCE`。

S0 已核对计划索引、公开服务与四个公共数据面、仓库恢复实现、两个专项、工程登记、Git 和异主 WIP。施工只取得计划白名单；根工程、filters、入口、普通应用装配、规范、计划、索引、知识库、流程图以及 L2 / L3 / 业务 WIP 均原样保留，未取得其 hunk。

## 2. 实际变更

### 2.1 冻结面与防扩张门禁

- 新增 `tools/l1_frozen_surface_manifest.json`：冻结 43 项公开操作、服务模块与四个公共数据面的规范行结束 SHA-256、双参与者 v1 的 9 个精确 legacy 例外、技术标识例外和领域禁词；`data_l1_status` 保持 `PENDING_ACCEPTANCE`。
- 新增 `tools/check_l1_frozen_surface.py`：核对清单 schema / 状态、43 项操作、五文件指纹、领域例外、仓库 import 唯一入口、`许可拒绝` 生产边界以及 4015 / 4070 / 4080 / 有限 N v3 目录摘要。
- 修改 `tools/check_specs.py`：严格规范检查固定调用冻结面检查，使接口和领域边界扩张成为正式规范门禁失败。

### 2.2 恢复实现与公开黑盒

- `海中鱼巣/核心/仓库.L1事实基座.ixx` 的私有权威载荷解码由布尔结果改为三态：`成功 / 格式不支持 / 材料非法`。未知 magic 或载荷版本映射到公开 `格式不支持`；已知格式内损坏继续映射到 `编码或所有者冲突`。未改变公开 DTO、函数签名、合同版本或事务语义。
- 扩展 `海中鱼巣/端到端测试.L1事实基座持久恢复.ixx`：经公开持久运行包、签发器、写端口和读取服务覆盖节点 / 关系 / 值的当前、历史与物理清理墓碑，属性槽，源 / 目标 / 历史组，一致投影，owner 重签，首次材料，精确重放，后继代次与编码不复用；追加清单、槽、长度、摘要、未知载荷版本、未知标签、存储占用和非活动槽诱导 fail-closed 矩阵。
- 扩展 `海中鱼巣/端到端测试.DATA-L1有限N分区原子事务.ixx`：V08 不再是 `NOT_RUN`，而是恢复格式 1 / 2 固定样本，验证三类事实与旧幂等账重放，继续首次 v3，并在再次启动后精确重放 v3。

### 2.3 固定历史材料

- 格式 1 来源：`db65bc99c48310935509e3fdf05b22e71ae9b6b1`；generator SHA-256：`be2c99945da20e7c999bd7f04f47ada2a0fb9720708dc898ebdf2f11fa8bb99d`。
- 格式 2 来源：`ff2366d97b662cfc6c8cf16917c06d5da182bf4b`；generator SHA-256：`c1f76956e6978b9b3904aa37b38064d60cfb128f6af4fcc7b285b8a493e47bb9`。
- 两组 fixture 均由对应历史提交树的公开 API 生成，并在未修改的同提交树独立恢复、公开读回后纳入 `海中鱼巣/测试材料/DATA-L1-FINAL-CLOSURE/**`；未调用私有编码器，未复制格式 3 后手改版本字节。

## 3. 4070 与恢复审计裁决

保留 4070 的启动持久恢复独立管理面：它负责清单 / 快照材料隔离、完整中性事实互证、恢复后内存权威发布和端口重新签发，不进入普通 CRUD 固定路径。

删除并禁止恢复“L1 内部恢复审计账”口径。现行生产源码、公开 DTO、持久载荷和专项均不存在 128 条环形恢复审计账或恢复审计 CRUD。20260828 旧施工记录中“审计 helper / 有界审计已实现”的表述不成立，只能作为历史文案保留，不能作为当前实现或验收证据。诊断由装配或日志层按日志规范记录人读结果，不进入 L1 权威事实。

## 4. 函数级代码知识变更清单

新增或实质修改：

- `L1事实基座仓库::解码权威状态(...)`：私有解码返回三态；未知 magic / 版本与已知格式材料非法分账。
- `L1事实基座仓库::初始化持久恢复实现(...)`：把三态解码精确映射到现有公开恢复状态，失败不交付运行包。
- `运行L1事实基座持久恢复端到端测试()`：扩展为三类事实、当前 / 历史 / 墓碑、组 / 一致投影、幂等和 fail-closed 统一恢复专项。
- `L1事实基座持久恢复测试内部::SHA256(...)`、`同步清单(...)`、`活动载荷路径(...)`、`复制根(...)`、`非成功且无运行包(...)`：生产外损坏注入与 fail-closed 辅助，不构成生产入口。
- `DATA_L1有限N分区原子事务测试内部::SHA256文件(...)`、`十六进制(...)`、`验证fixture来源(...)`：互证固定材料来源和文件摘要。
- `DATA_L1有限N分区原子事务测试内部::验证旧格式三类事实(...)`、`验证旧格式并继续v3(...)`：格式 1 / 2 恢复、旧账重放、首次 v3 与重启重放。
- `运行DATA_L1有限N分区原子事务端到端测试()`：V08 从未运行占位改为实际执行格式 1 / 2 兼容矩阵。
- `tools.check_l1_frozen_surface.检查冻结面()` 及其内部检查函数：生产外机器冻结门禁。
- `tools.check_specs.主函数()`：接入冻结面错误结果。

没有退出公开函数，没有新增生产公开函数，没有改变 L1 公开 ABI。待结果提交正式发布后，知识库维护者应按该提交刷新 L1 仓库、两个专项和规范工具用途知识；本计划不直接修改共享知识库。

## 5. 完成边界

本施工结果只形成待独立验收的 DATA-L1 收口候选。崩溃、断电、介质故障、资源耗尽和长时运行均为 `NOT_RUN`；代码提交、双配置构建、统一 driver 或恢复专项都不能单独把整体状态升级为 `FROZEN`。候选提交并推送后仍须由集成验收与回归智能体绑定同一精确 commit 完成独立验收；只有 `PASS` 后，规范 / 计划索引所有者才能第二次治理发布。
