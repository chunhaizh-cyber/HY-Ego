# FEATURE-TYPE-R-I64-NO-FCR-RULE-CORRECTION 施工记录 v0.1

计划：FEATURE-TYPE-R-I64-NO-FCR-RULE-CORRECTION v0.1。开工 HEAD：8f4e1628c035071a63e7b84c96a00834b0d97e30。

## 实际变更

- 数据服务.特征类.cpp 与 private 声明：添加 I64 默认 R规则固定为用途1归组、用途2代表，不再为带域形成的 I64 FT 新建用途3概念归并规则。
- 归并特征R概念：I64 路径仍校验请求截止和 FT，但不读取用途3、R项、成员、材料或 FCv，也不形成 I64域；所有这些未使用预算为零时返回已归并零输出和空项。
- 补齐I64默认R规则：只检查用途1/2。已有用途1/2完整时，即使历史用途3仍存在也零写成功；用途1/2半结构仍拒绝。
- 未修改 L1、特征值、存在、概念 tree、IF/R、普通应用或任何历史规则关系。

## 代码知识变更清单

| 函数/成员 | 当前用途 | 不负责 |
| --- | --- | --- |
| 特征类数据服务::归并特征R概念 | I64 R 的零输出终点；只读 FT 与截止，并要求全部归并专用预算为零。 | 不形成 FCr、不读取 F→FCv、不读取 R材料/成员、不写入。 |
| 特征类数据服务::添加I64默认R规则 | 为新建或全无补齐的 I64 FT 创建归组和代表规则。 | 不创建用途3或退出旧规则。 |
| 特征类数据服务::补齐I64默认R规则 | 验证用途1/2完整或原子补齐两条规则。 | 不清理旧用途3、不修改历史 H。 |

## 完成边界

本结果落实 I64 单点 R 零 FCr 裁决，只解除 IF/R 的直接规则语义门槛。未证明 IF/R 持久化、FCr历史清理、概念 owner 关系、跨进程恢复、完整工程构建或阶段21。
