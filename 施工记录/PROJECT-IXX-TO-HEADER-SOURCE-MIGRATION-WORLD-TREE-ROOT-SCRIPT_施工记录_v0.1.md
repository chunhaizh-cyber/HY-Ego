# 世界树根启动专项脚本迁移施工记录

计划身份：`PROJECT-IXX-TO-HEADER-SOURCE-MIGRATION-WORLD-TREE-ROOT-SCRIPT`

日期：2026-09-18

## 范围与 S0

- 施工起点：`a18809cf8e0ced04f4619bb1b4e2efeeb4eae075`，`main == origin/main`，index 未由本切片使用。
- 计划与详细设计：v0.1，索引状态为`可执行`。
- 唯一生产外修改：`验证工具/运行世界树根启动专项验证.ps1`。
- 当前物理输入已读回：`装配.普通应用.h/.cpp`、`合同.世界树根.h`、`数据服务.世界树根.h`及世界树根专项工程均存在；四个旧 `.ixx/.cppcpp` 路径是脚本中待迁移的活动文本探针。
- 工作区存在大量异主删除、修改和未跟踪 WIP；本切片未覆盖、删除、暂存或写入它们。

## 实际修改

脚本的四个文本读取路径改为当前 `.h/.cpp` 文件；构建前检查改为：

- 普通应用头拒绝 `module`、`import` 和 `构造普通应用上下文` 函数体；
- 普通应用源要求 `#include "装配.普通应用.h"` 及 `构造普通应用上下文` 定义，并拒绝已退役模块声明；
- 两份世界树根公开头拼接后拒绝 `发布世界树根节点`、`module`、`import`。

`$OutputRoot` 白名单、`/m:1`、隔离 `OutDir/IntDir`、配置、两个 EXE 调用和 JSON 字段均未修改。

## 代码知识变更清单

无 C++ 函数身份新增、修改或退出。本切片仅调整既有 PowerShell 验证脚本的构建前物理格式断言，不改变`构造普通应用上下文`或世界树根服务的签名、事实读写、调用边、事务或生命周期；共享函数用途卡无需由本切片更新。

## 当前收口

脚本 AST 和旧格式扫描通过；但 Debug 隔离 Rebuild 在运行验证程序前失败。错误位于计划禁止修改的专项 C++/工程输入：D455 RealSense 头缺失、专项 C++ 与当前世界树 v4 ABI 不匹配、概念树翻译单元缺 `/bigobj`。详细设计要求这种非零构建具名失败并停止，不得通过放宽断言或改生产/专项工程收口。

本记录不声明计划完成、专项 Debug/Release 成功或世界树根能力通过。
