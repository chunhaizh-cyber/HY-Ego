# D455 可选 SDK 条件编译提供者施工记录

计划身份：`D455-CONDITIONAL-COMPILE-PROVIDER`

施工基线：`052a4dafca5dd0b249b2b59b1ae0dbc1a67e89e1`

日期：2026-09-18

## S0 与范围

- 计划索引将本计划 v0.1 登记为可执行；`main == origin/main == 052a4daf`，本切片未使用既有 Git index。
- 允许生产文件 `海中鱼巣/适配/采集器.D455相机.cpp` 在开工前无 WIP；两份专属记录为新文件。工作区其余修改、删除和未跟踪文件均为异主现场，未覆盖、删除、暂存或检查为本切片输入。
- 主工程只在 `EnableD455RealSense=true` 时构建 D455 源；世界树根专项仍编译它。两个工程只在该开关为 true 时定义 `HY_EGO_ENABLE_D455_REALSENSE`。当前源无条件包含 RealSense C API 头，确认为专项默认关闭构建的直接阻断。
- 6350/6340、计划与详细设计一致：D455 SDK 边界不写世界事实；公开头和协议不暴露 SDK 类型。本轮不修改协议、头、工程、专项、脚本、装配或线程。

## 实际修改

`采集器.D455相机.cpp` 在公开头包含之后以 `#if defined(HY_EGO_ENABLE_D455_REALSENSE)` 围住既有 RealSense C API include、全部 `rs2_*` 类型/辅助、真实采集器和真实工厂定义。宏未定义时定义同一公开工厂：

- `D455采集配置有效(配置)==false`：返回空 `unique_ptr`，写入 `{false,false,配置无效}`；
- 有效配置：返回空 `unique_ptr`，写入 `{false,false,无设备}`；
- 两条关闭路径均不创建帧来源、不访问 SDK/硬件、不写缓存、日志、线程或领域事实。

真实宏开启分支的原有代码和失败映射未改变；未新增 DTO、拒绝枚举或公开声明。

## 代码知识变更清单

| 身份 | 文件与可见性 | 变化、用途与非职责 | 调用边/副作用/生命周期 | 知识维护 |
| --- | --- | --- | --- | --- |
| `创建并打开D455相机采集器(D455采集配置,D455操作结果&)` | `适配/采集器.D455相机.cpp`，模块外声明在既有公开头 | 增加关闭 SDK 构建的同签名实现；只报告配置无效或当前无可用 D455 提供者，绝不模拟帧或建立设备 | 关闭分支仅读取值式配置、写入调用方结果并返回空所有权；无事务、锁、线程、缓存、日志、硬件或世界事实；真实分支保持既有打开后持有采集器生命周期 | `知识库/函数用途/D455采样材料/D455相机采集器函数组.md` 仍绑定 `.ixx`/`04c1179c`，与当前 `.h/.cpp` 物理形态不一致，待知识库维护者在结果发布后重建/复核 |

未新增其它稳定函数、服务、公开 ABI 或调用方。

## 未完成边界

本记录不证明 SDK 启用编译、真实 D455 设备、持续采样、线程承接、世界树专项 v4、普通应用、持久恢复或业务闭环。专项工程还需其后继计划处理 v4 消费者和 `/bigobj`。
