# 初始场景当前读回一致投影迁移施工记录

日期：2026-08-08

计划：`L2-INITIAL-SCENE-CURRENT-READ-CONSISTENT-PROJECTION-MIGRATION v0.1`

plan blob：`b1595a32d9d8aa922271a0421fbcdc436279662f`

设计提交：`092c23a3410ba12d158b341a2842fe0698ed1cf0`

一致投影提供者结果：`a23dc8e5ad462832ac97245b5f9d8033b0ab3c85`

世界登记迁移结果：`8d0a2c9deb03a7d65cfb5a8551480cc6584897d5`

施工起点：`c9e2bdd0042c0343972dba9bbca9d7e91dfde4c6`

## 1. S0 与范围

- 开工时 `main == origin/main == c9e2bdd0042c0343972dba9bbca9d7e91dfde4c6`，ahead / behind 为 `0 / 0`，Git index 为空；索引 blob 为 `feb7e513d356921c9529b7e423cc4ecc66662309`，本计划是唯一 `可执行` 项。
- 目标 `海中鱼巣/领域/服务.L1场景结构.ixx` 起始 blob 为 `4f099b8576eae60450b44623845ec2aad4f31da0`；场景 DTO、世界登记、L1 中性一致当前 DTO / 服务、运行期上下文和生产运行期 blob 均与详细设计冻结值一致。
- 现行公开合同、初始场景固定一节点 + 一值 + 一槽 + 一关系写集、定位缓存和生产调用边未漂移。
- 本轮只取得场景结构服务文件及两份专属记录的所有权。P0-FIX、路线、异主 `.codex-build`、两份 WORLD-TREE 和其它 WIP 原样保留；施工后复核其 tracked blob 与未跟踪文件 SHA256 均未变化。

## 2. 实际施工

### 2.1 单次一致当前投影

把 `L1场景结构内部::读取实现` 从事实代次首读、节点读取、属性读取、值读取、关系读取和事实代次末读的多许可拼接，迁移为一个物理 `尝试读取中性一致当前投影` 调用点。

唯一请求固定形成：

- 期望代次取本轮世界登记的 `已验证事实代次`；
- 节点恰一项，为请求初始场景；
- 关系恰一项，为请求直接父关系；
- 值为空，不按场景标记编码执行第二次读取；
- 属性值恰一项，为初始场景 + 登记场景标记属性类型；
- 源关系组和目标关系组为空。

目标根没有循环、等待、sleep、`continue` 或局部重试。登记与请求入口校验失败时零一致投影调用；场景标记值只从同一许可内返回的属性值投影取得。

### 2.2 非成功分账

新增三个目标专用纯值 helper，不修改同文件既有 `读取状态(...)` helper：

- 非成功结果必须满足合同、期望代次回显和六组全空；漂移还必须回显非零且不同的实际代次，其它非成功必须保持读取代次为零；
- 合法事实代次漂移映射 `版本漂移`，许可拒绝映射 `许可拒绝`，资源失败保持 `资源失败`；
- 入口拒绝、内部不一致、坏失败形状、项目已退出和种类不匹配映射 `内部不一致`；节点 / 关系未找到及属性未找到 / 未设置映射 `未找到`。

所有非成功都返回空事实，不补读、不补写、不修改定位缓存。

### 2.3 成功业务形状

成功只接受合同、期望回显、非零共同读取代次和六组长度精确 `1/1/0/1/0/0` 的结果，再验证：

- 初始场景查询 / 事实编码一致，节点为普通、无属性类型表示、未退出且只有一个当前属性槽；
- 属性投影槽与节点当前槽逐字段相等，槽类型、当前值编码和请求场景标记值精确绑定；
- 值事实所属初始场景、类型为登记标记类型、来源为登记服务、材料为 `std::int64_t(1)` 且未退出；
- 直接父关系编码、世界根源端、初始场景目标端、登记关系类型、顺序 0 和未退出精确成立；
- 节点、值和关系创建代次共同、非零且不晚于读取代次。

成功继续按原公开 DTO 组装 `初始场景事实`。场景结构数据头、创建写集、其它读写根、启动、工程和调用者均未修改。

## 3. 验证资源与结果

Debug / Release 的中间目录、可执行文件、构建日志、模块扫描 JSON 和运行捕获位于系统临时目录 `C:\Users\zhchh\AppData\Local\Temp\hzy-initial-scene-consistent-7f2d8c49e63a4f9ba9243164b3c99a21`，没有使用、删除或覆盖仓库内异主 `.codex-build`。

- Debug / Release x64 串行完整 `Rebuild` 均退出 0、0 编译错误；每个配置各有一条临时目录 `MSB8029` 提示。
- Debug / Release 模块扫描均为 `sources=136, modules=136, failed=0`，拓扑 136、循环 0、外部需求 0。
- Debug / Release 新构建程序的正式 `--runtime-context` 均在 30 秒时间盒内自行退出 0，stdout / stderr 均为 0 字节；没有以持续运行代替通过。
- 正式规范 strict 为 110 / 110；三白名单路径空白检查和目标根精确静态门禁通过。
- 运行前只读发现一个来自其它世界登记验收临时目录、早于本段启动且无 `--runtime-context` 参数的异主生产进程；本段未终止、修改或计入它，只对本段精确 PID 应用超时安全退出边界。

## 4. 完成边界

本施工最多证明初始场景这一具名 L2 当前读回根已按冻结合同迁移到一次中性非阻塞一致当前投影，并通过双配置构建、模块扫描、strict、静态门禁和正式生产入口运行门禁。

公开租约连续两次结构化场景 / 登记读回未在本段独立执行。

生产退出码只证明正式入口没有拒绝该次启动，不证明初始场景消费者业务成功、消费者集成验收或 L1 完善。不得据此声明真实许可竞争已运行、其它消费者已迁移、关系组 / 历史 / 修改 / 退出已证明、历史正文闭合、L1 自证或第一层完善。
