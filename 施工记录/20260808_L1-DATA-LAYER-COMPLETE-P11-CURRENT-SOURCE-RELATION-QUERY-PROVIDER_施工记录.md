# 当前关系源端条件查询中性提供者施工记录

日期：2026-08-08

## 1. 施工身份与范围

- 正式起点：`f03d901d96290ab920a22ff1cada3a4d47f0702d`
- 计划身份：`L1-DATA-LAYER-COMPLETE-P11-CURRENT-SOURCE-RELATION-QUERY-PROVIDER`
- 计划 blob：`b89e709d484d2b347ebb9f5c2a3fc6264cdcc658`
- 生产代码：`海中鱼巣/核心/L1中性CRUD.数据.h`、`海中鱼巣/核心/仓库.L1事实基座.ixx`、`海中鱼巣/核心/服务.L1事实基座.ixx`
- 配套记录：本记录与同名构建验证记录

施工前 `main == origin/main`，ahead / behind 为 0 / 0，Git index 为空。目标五路径没有异主修改。P0-FIX、路线、旧 WORLD-TREE 和 `.codex-build` 现场原样保留，未修改、未暂存。

## 2. 实际变更

1. 在既有第一层中性数据头新增源节点与关系类型条件查询的请求和结果。结果返回调用条件、读取事实代次和完整当前关系有序组，不提升合同版本，不修改既有数据结构或合同模块。
2. 第一层事实仓库状态新增与既有目标端索引并列的非权威源端派生索引。两级键只含源节点和关系类型，桶内只保存严格升序、无重复的关系稳定编码，不复制关系事实或生命周期字段。
3. 中性提交与兼容提交两条候选路径均在关系创建后依次插入源端、目标端索引，在关系退出前依次从源端、目标端索引删除。任一索引维护失败只影响候选，不交换权威状态；成功仍沿用既有一次状态交换。
4. 状态完整性检查使用两个独立覆盖集合，分别验证源端索引和目标端索引都恰好覆盖全部当前关系；每项索引编码都必须回到端点、类型和生命周期全等的权威当前关系。
5. 既有状态材料转为当前状态后，只从已经形成的权威当前关系分别纯派生源端、目标端索引。没有修改恢复数据结构、材料格式、恢复入口、状态、SQL或恢复验证范围。
6. 仓库源端条件查询在一次共享锁内只访问请求对应的源节点和关系类型桶，并按桶内编码逐项回读权威当前关系。合法零命中返回成功空组；空桶、非升序、悬空编码、字段或生命周期矛盾返回内部不一致和空组。
7. 第一层事实基座服务新增唯一只读源端条件查询入口。无效请求直接拒绝；资源异常映射资源失败，其它异常映射内部不一致。
8. 普通源端查询没有调用完整快照、历史、审计、恢复、SQL、目标端查询、旧管理索引、日志、状态完整性检查或全量派生函数。没有新增第一层自检、独立验收、双目标、第二执行程序或生产内通过判定。

## 3. 权威与不变量

- 权威当前关系仍只属于既有第一层事实仓库；源端和目标端索引都只保存可重建的关系编码候选。
- 权威关系和两个索引始终在同一候选、同一次权威状态交换后可见，不存在第二关系仓或独立索引发布入口。
- 第一层只返回业务中性的源端关系事实组，不解释场景、子场景、成员资格、角色、单父、无环或世界归属。
- 第二至第五层真实服务负责自己的业务裁决，并通过正式第一层入口、结构化结果和最终权威读回累计证明实际使用的第一层能力。

## 4. 完成边界

本次施工只形成按源节点和关系类型读取当前关系有序组的中性提供者候选，以及与目标端索引同候选维护的源端派生索引。构建、模块扫描和静态门禁通过不等于第一层自证，也不等于第一层已经完善。

本叶没有建立或运行第一层独立验收。后继必须由真实第二层场景完整事实生产服务消费该入口，并在自身业务运行中核对结构化结果和权威状态。当前不证明第二层场景完整事实、关系替换、性能、并发压力、崩溃、重启、跨进程恢复、其它消费者或整个第一层完善。
