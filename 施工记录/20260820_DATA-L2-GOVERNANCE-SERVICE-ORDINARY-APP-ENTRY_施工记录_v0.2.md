# DATA-L2-GOVERNANCE-SERVICE-ORDINARY-APP-ENTRY 施工记录 v0.2

日期：2026-08-20

计划：`DATA-L2-GOVERNANCE-SERVICE-ORDINARY-APP-ENTRY v0.2`

计划段起点：`f03438aa`；plan blob：`c53f2783bea9864367d1722c31e7eee5d8066af9`。

## S0

- 计划索引只登记本计划为当前 `可执行` 项；
- 普通应用、需求 / 任务服务与需求端到端入口的构造签名、owner 建立身份和 DTO 与设计绑定事实一致；
- index 空；其它未跟踪规范、详细设计、邮箱计划及临时材料均为无关 WIP，未触碰；
- 当前知识库基线落后于正式 HEAD，已回读当前源码、普通应用生产调用点、工程登记和目标函数用途卡完成复用互证。

## 实际施工

1. `普通应用上下文`在同一 `L1事实基座运行包`中依次装配唯一需求服务与任务服务，追加 const / non-const 同实例 getter 和六个尾部装配失败状态。
2. 既有需求端到端入口删除手工 L1、owner 和 L2 服务构造，改为只经普通应用 getter 消费存在、特征、状态、需求和任务服务。
3. 在既有需求真实材料上增加任务核心新增、读取、按列表项反查、退出和退出后当前读回。
4. 首轮运行发现 `退出任务`写入成功后又用当前投影重新发现已退出关系，确定性返回 `内部不一致`；按 v0.2 把退出前已经验证的虚拟存在和三条关系编码值式传入内部历史读回，归零提交后的当前投影查询。

## 代码知识变更清单

| 函数 / 身份 | 模块与文件 | 变化与用途 | 直接调用边变化 | 待知识维护 |
| --- | --- | --- | --- | --- |
| `普通应用上下文::取得L2需求结构服务()` const / non-const | `海中鱼巣.装配.普通应用` / `海中鱼巣/装配.普通应用.ixx` | 新增；返回上下文唯一需求结构服务同实例引用，零失败、零写入 | 需求端到端消费者新增调用 | 新建用途卡或扩展服务取得入口组 |
| `普通应用上下文::取得L2任务结构服务()` const / non-const | 同上 | 新增；返回上下文唯一任务结构服务同实例引用，零失败、零写入 | 需求端到端消费者新增调用 | 同上 |
| `普通应用上下文::普通应用上下文(...)` | 同上 | 修改；新增需求 / 任务服务所有权参数和成员移动，维持依赖逆序析构 | `构造普通应用上下文`新增两服务移动 | 更新私有构造用途卡 |
| `构造普通应用上下文(const 普通应用配置&)` | 同上 | 修改；同一 L1 运行包建立需求 / 任务 owner、形成交付并依赖顺序构造唯一服务 | 新增调用 `尝试形成L2需求所有者交付`、`L2需求结构服务`构造、`尝试形成L2任务所有者交付`、`L2任务结构服务`构造 | 更新生产入口和普通应用装配用途卡 |
| `L2任务结构内部::读回退出任务(...)` | `海中鱼巣.领域.服务.L2任务结构` / `海中鱼巣/领域/服务.L2任务结构.ixx` | 修改内部签名；接收退出前冻结编码，只按历史入口读回退出事实，不再查询当前投影 | 删除两次 `尝试读取所有者范围一致当前投影`，保留五项历史读回 | 更新任务结构服务内部用途卡 |
| `L2任务结构服务::退出任务(L2任务退出请求)` | 同上 | 公开签名不变；把提交前已验证编码传给内部读回，修复成功写入被误报内部不一致 | 内部调用参数扩充 | 更新任务服务公开退出用途卡 |
| `运行需求服务端到端测试()` | `海中鱼巣.端到端测试.需求服务` / `海中鱼巣/端到端测试.需求服务.ixx` | 修改；从手工装配迁移为普通应用合法消费者，并增加任务核心 CRUD 读回 | 删除 L1 / owner / 服务手工构造，新增普通应用 getter 与任务服务调用 | 更新测试入口用途说明 |

没有新仓库、第二 owner、公开 DTO、工程登记或业务入口。

## 完成边界

本施工只闭合需求 / 任务结构服务普通应用入口、任务核心实际消费和任务退出历史读回缺陷；任务方法路径全矩阵、任务业务服务、任务筹办、线程调度、自我治理及真实治理未实施。
