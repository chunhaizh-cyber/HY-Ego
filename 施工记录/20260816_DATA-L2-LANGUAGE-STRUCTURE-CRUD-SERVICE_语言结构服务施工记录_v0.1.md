# DATA-L2 语言结构 CRUD 服务施工记录

日期：2026-08-16

计划身份：`DATA-L2-LANGUAGE-STRUCTURE-CRUD-SERVICE v0.1`

计划 blob：`279fa2b3a38cdb0f01047a1449de4f7da507f784`

设计提交：`7e2dfc28049bd0f86c171c0a76e727f7c105b6b6`

施工基线：`cc1db7c5dbc01010481ebc32a95359cb10b30db7`

## 1. 实际范围

- 新增 `海中鱼巣/领域/服务.L2语言结构.ixx`，实现唯一语言 owner 交付、固定四项私有登记和 `L2语言结构服务`。
- 最小修改 `海中鱼巣/领域/服务.不可变材料.ixx`，新增只比较构造时借入实例地址的 `使用同一L1事实基座实例(...) const noexcept`。
- 根工程和 filters 各唯一登记一次新模块。
- 新增本施工记录和配对验证记录。

没有修改语言 DTO、L1、普通应用、聚合、词条、语言记录、概念名称、默认汉语、语素、词性、最终需求清单或异主 WIP。

## 2. 实现结果

- owner 建立幂等身份固定为 `0x4C32'4C41'4E47'0001`；登记写幂等身份固定为 `0x4C32'4944'5352'4308`。
- 固定登记一次建立语言族锚点、族归属关系类型、登记键关系类型、来源关系类型；首次和精确重复均正式历史读回四个互异、同 owner、同代、活动节点。
- 语言服务构造同时验证 owner 写端口与不可变材料服务绑定同一 `L1事实基座服务` 对象地址。
- 新增一次建立语言节点及族归属、登记键、来源三关系；替换在同一写集中退出旧四事实并建立不同身份的新四事实；退出只退出语言四事实，并保持 L1 全局引用保护。
- 当前 / 历史、按身份 / 按登记键读取均使用具名截止、材料正式读回和前后当前代次守卫；登记键材料必须是同截止严格 UTF-8，来源材料保持通用格式。
- 三个写入口在语言写锁内先读 L1 owner 首次规范化写集；原完整请求重放从首次写集、首次结果和原截止历史投影恢复原结果，后继替换或退出不改变原截止。
- 非成功返回空载荷、零变更代次；公开幂等身份与 owner 幂等身份一对一映射，私有登记幂等身份在入口拒绝。

## 3. 施工边界

本切片没有普通应用装配、生产消费者、第二 owner、第二写入口、自检目标、测试模式或日志 PASS。代码结果只形成语言结构 CRUD 服务候选，不证明真实消费者闭环，也不证明完整 `DATA-EXT-10`。

仓库外专项源码、对象、程序和运行材料只用于施工验证；发布前实际文件清理归零，不登记生产工程。
