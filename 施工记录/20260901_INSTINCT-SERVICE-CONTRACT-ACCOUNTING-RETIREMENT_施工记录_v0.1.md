# INSTINCT-SERVICE-CONTRACT-ACCOUNTING-RETIREMENT 施工记录 v0.1

日期：2026-09-01

计划身份：`INSTINCT-SERVICE-CONTRACT-ACCOUNTING-RETIREMENT`

施工起点：`main == origin/main == d27c6ce685c52d1c42ac17c2535478e90bae9bc3`

执行期重新 S0 基线：`main == origin/main == 368009f86fe379ca60b438056797833e3f7f7d2d`；该提交只发布计划支撑修订，代码差异仍以 `d27c6ce685c52d1c42ac17c2535478e90bae9bc3` 为起点。

正式依据：

- plan blob：`8a4b00065c66dbec22930413f988f2ef6aa4fc52`
- design blob：`eb4dbb100c9b192953a76e6218fa90f7a84ae7ca`
- index blob：`1377057d9a3144a4278d0a84700384f182502ba6`
- 计划状态：`可执行`

## 1. S0 与所有权

施工前核对 `main`、HEAD / origin、空 index、dirty 文件、计划白名单和目标 hunk。独立只读复核确认：

1. 十二个专属文件的外部 import、getter、owner、构造与工程登记都落在计划白名单；
2. `治理批次处理阶段` 没有持久编码或跨进程消费者；
3. 旧九项安全定义唯一解码位置是 `服务.安全根定义与当前值.ixx::解码定义`；
4. 没有新的活动写入所有者或发布租约冲突；
5. 用户已授权接管目标重叠 WIP，但任务、自我治理等无关 WIP必须原地保留并保持在本提交之外；
6. `端到端测试.安全根定义与当前值.ixx` 只取得 `形成发布请求` 中删除退役位置实参的一个 hunk，既有 `配置.等待合同登记` 删除不属于本计划。
7. 第一次“HEAD + 本计划差异”隔离构建发现 `端到端测试.自我治理一致事实.ixx` 仍直接消费退役 ABI；计划支撑已在 `368009f` 把该文件的四类最小适配加入白名单。重新 S0 后只取得这些 hunk，不接管该文件其余任务 / 自我治理整体重构。

## 2. 实际施工

### 2.1 整模块退出

删除下列十二个专属文件，并删除其 `.vcxproj` / `.filters` 登记：

```text
海中鱼巣/领域/服务合同事实权威.数据.h
海中鱼巣/领域/服务.服务合同事实权威.ixx
海中鱼巣/端到端测试.服务合同事实权威.ixx
海中鱼巣/领域/本能单完整秒被动维护.数据.h
海中鱼巣/领域/算法.本能单完整秒被动维护.ixx
海中鱼巣/端到端测试.本能单完整秒被动维护.ixx
海中鱼巣/领域/本能被动维护游标.数据.h
海中鱼巣/领域/服务.本能被动维护游标.ixx
海中鱼巣/端到端测试.本能被动维护游标.ixx
海中鱼巣/领域/本能被动维护基础快照.数据.h
海中鱼巣/领域/组合.本能被动维护基础快照.ixx
海中鱼巣/端到端测试.本能被动维护基础快照.ixx
```

没有留下兼容壳、替代账、迁移服务或新游标。

### 2.2 启动、装配和治理线程

- 普通应用上下文退出服务合同事实权威、被动维护游标的 import、getter、owner、服务实例、构造参数和装配状态；保留同文件无关任务 / 自我治理增量。
- 启动退出变化账 Gseed、服务维护历史、维护纪元、游标恢复与读回链；UTC 启动时间和运行代次继续使用。
- 自我线程删除 N06 `维护服务与生存安全`，安全维护之后直接进入权限计算。
- `治理批次处理阶段` 删除 `服务维护` 并连续重编号；该枚举没有持久兼容消费者。

### 2.3 运行期只读查询与治理骨架

- `读取生存安全与服务维护当前快照` 收缩为 `读取本能根当前快照`。
- 新快照只在同一 `G0` 读取安全根 A/L/H、派生控制态，并读取服务根普通 I64 当前值。
- 删除完整秒、时间纪元、上一结算秒、四组数量预算，以及合同 / 准备 / 进展 / 到期四组重复状态读取。
- 自我治理一致事实改为消费 `本能根当前快照`。
- `端到端测试.自我治理一致事实.ixx` 相对 HEAD 仅把请求成员改为 `服务根特征定义`、删除四项退役预算、把结果改读 `本能根快照`，并把两处 `服务游标.服务值` 改为 `服务根当前值`；安全根、事实截止和右侧身份来源保持不变。
- 删除 `维护服务与生存安全` 和 `结算需求并形成服务结算输入` 的 DTO、函数及相应 TODO，不建立替代结算入口。

### 2.4 安全定义兼容边界

- `发布安全根定义请求_v1`、生产初始化 DTO 和匹配函数退出 `被动服务维护规则版本`。
- 新定义编码由九项缩为八项。
- 原 `解码定义` 同时接受八项与旧九项；旧九项只校验索引 7 为合法非零 `uint32`，不恢复、不暴露该值，来源从最后一项读取。
- 没有新增旧格式 reader、迁移写或测试专用生产入口。

## 3. 函数级代码知识变更清单

以下清单供后续能力证据目录 / 函数用途维护者基于结果提交更新；本计划不直接写共享知识索引。

### 3.1 退出函数和入口

- `服务合同事实权威服务` 全部公开发布、读取、历史边界、进展 / 准备、到期事件和 ARCH 验证入口：用途随专属模块整体退出；全部调用边已删除。
- `计算本能单完整秒被动维护候选_v1`、`计算本能多完整秒被动维护候选_v1` 及专属纯值辅助函数：合同驱动完整秒被动维护算法整体退出。
- `本能被动维护游标服务` 的登记、激活 / 恢复、提交、v1-v4 读回及 ARCH 注入入口：游标 owner 和调用边整体退出。
- 本能被动维护基础快照、需求活动读取、维护提交组合器及其辅助函数：合同 / 游标 / 完整秒组合链整体退出。
- `运行服务合同事实权威端到端测试`、`运行本能单完整秒被动维护端到端测试`、`运行本能被动维护游标端到端测试`、`运行本能被动维护基础快照端到端测试`：随退役模块退出。
- `维护服务与生存安全`、`结算需求并形成服务结算输入`：L2 治理骨架入口退出，不建立替代函数。
- `读取生存安全与服务维护当前快照`：公开名称和旧四组重复读取合同退出。
- 普通应用的 `取得本能被动维护游标服务`、`取得服务合同事实权威服务`：getter 及 owner / service 构造边退出。

### 3.2 新增或修改函数

- `运行应用程序`：删除 Gseed、服务维护历史、完整秒启动维护和游标恢复；保留 UTC 运行代次及后续启动链。
- `自我线程::运行治理批次`：删除 N06 服务被动维护分支，安全维护成功后直接进入权限计算。
- `运行期只读查询组合器::读取本能根当前快照`：新增现行公开入口；同 `G0` 两次当前值读取，返回安全根、控制态和服务根普通当前值。
- `装配运行期只读查询组合器`：自我治理一致事实改为请求并消费本能根快照。
- `安全根定义细节::编码定义`：固定写出八项现行载荷。
- `安全根定义细节::解码定义`：兼容八 / 九项，旧索引 7 只校验后忽略，来源取末项。
- `安全根定义与当前值服务::发布安全根定义_v1` 及其同义比较：删除退役字段守卫和比较。
- `安全根生产定义匹配_v1`、`安全根生产定义初始化结果_v1::成功`、`安全根生产定义初始化提供者_v1::确保生产定义`：删除退役规则字段依赖。
- `运行安全根生产定义初始化端到端测试`：所有现行请求改为八项 ABI；既有普通装配 WIP不计入本计划。
- `形成发布请求`（安全根定义与当前值专项）：只删除一个退役位置实参。
- `运行自我治理一致事实端到端测试`：只适配本能根快照与服务根当前值 ABI；专项的外来任务 / 自我治理整体重构不进入结果提交。

### 3.3 待更新用途卡

- `读取本能根当前快照`
- `安全根定义细节::编码定义`
- `安全根定义细节::解码定义`
- `运行应用程序`
- `自我线程::运行治理批次`
- 上述整模块退出的服务合同、被动维护游标、基础快照和单完整秒维护入口应标为“已退出”，不得继续作为可复用能力。

## 4. 施工边界

- 未物理删除用户持久数据，未补账、迁移、续期或重新发布旧材料。
- 未新增人类服务请求专用入口；普通需求、任务、方法、正式结果的机器语义不变。
- 服务根保留为普通长期根坐标，本计划没有增加自动写入者。
- 旧九项运行恢复没有现成业务公开构造入口，本轮未增加测试缝；运行证据记为 NOT_RUN，不能据此声明全部旧数据恢复。
- 候选由固定 `368009f` 的十二个 HEAD 混合文件、五个目标专属纯文件和十二项删除组成；在全新的 `D:\TEMP\海中鱼巣\INSTINCT-SERVICE-CONTRACT-ACCOUNTING-RETIREMENT\20260901-stage-01\snapshot-02` 中完成独立 Debug / Release Rebuild。仓库外脚本本身不提供无条件路径或基线保护，本轮只在已核对的全新 `D:\TEMP` 路径和固定 HEAD 下使用，不把脚本能力写成生产能力。
- 目标快照专项 runner 与纯 `368009f` 对照 runner 都在相同两个上游专项处退出 2；该对照只排除本计划引入新的专项失败，不把既有失败升级为 PASS。
- 结果提交与普通推送由本记录和代码的同一发布段形成；提交号以 Git 发布结果为准。
