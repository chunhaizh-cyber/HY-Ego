# DATA-L2 普通概念建立读取服务施工记录

身份：`DATA-L2-ORDINARY-CONCEPT-CREATE-READ-SERVICE`

版本：v0.1

日期：2026-08-16

## 1. 基线与范围

- 代码施工起点：`main == origin/main == 95ecc17075d6d10ae734c4f416ff8e9418626308`，index 空。
- BIZ 零重叠文档与索引发布期间，DATA 先后在 `7fd81e87c5ddc767b7c8402f9aca796e7099828b`、`c31093ebd629531191ad3d3ffa393ac8dbbd8a10` 增量复核并保持未暂存；`DEMAND-SOURCE-PATH-MERGE-CANDIDATE-COMPOSE v0.1` 两笔发布完成后的正式发布前基线为 `3352f986d34192c8b4e87a8ddd8e567e7fb3a460`。这些提交均未修改目标生产模块；等待期间 DATA 保持 index 空且不占 Git / 计划索引租约。
- 正式计划状态：`可执行`；plan blob `df01d29f32e142c19ce03f3bcd26f6f1a3a96e2c`，设计提交 `08fe0587f8d7b3d904fca70c79a6bb4f90b918fa`，详细设计 blob `5757ecc6ec2d8f73a63438af021bdb4bdc004027`。
- 实际代码范围仅为 `海中鱼巣/领域/服务.L2概念结构.ixx`；另新增本记录和同身份检查记录。
- 普通概念 DTO、四本体根 DTO、概念聚合、普通应用、工程 / filters、不可变材料和六类世界服务、最终需求清单、BIZ、SELF、流程图及异主 WIP 均未修改。

## 2. 实际实现

1. 在唯一 `L2概念结构服务` 中新增完整构造，借入不可变材料及场景、存在、特征、状态、动态、因果六类结构服务的七份 `const` 引用；根专用构造继续有效，普通入口在依赖未绑定时返回结构化 `未实现`。
2. 新增 `建立普通概念(...)` 与 `读取普通概念(...) const`，保留四本体根建立、按角色读取和全组读取三项公开 ABI；根建立和普通概念建立共用唯一服务级写锁及同一概念 owner 写端口。
3. 用独立登记幂等身份原子登记 13 项普通概念私有关系 / 属性类型，首次与精确重复均逐节点历史读回；三项概念内部登记幂等身份全部从公开幂等域排除。
4. 首次建立按固定本地键与十个互异高位分区形成一个 owner 写集，包含概念节点、族 / 根归属、签名规则、全部直接上位、来源证据、签名项、定义项、首次请求见证和初始活跃生命周期。
5. 建立前在同一非零 `G0` 经公开服务读边校验四根、直接上位、不可变材料、十类定义目标、特征值和十三类世界关系。状态 / 动态只消费其公开强类型事实，不读取其私有登记。
6. 当前 / 历史读取先从概念 owner 自有事实重建，再按同一截止逐项互证公开依赖；缺项、重复、混代、坏成功形状或关系端点不一致不被过滤成成功。
7. 首次写入材料、完整规范化写集和首次结果用于区分精确重复、幂等冲突及同签名异请求；发布结果未知、资源失败或发布后读回失败不建立第二结果入口，仍要求原完整请求重放。
8. 当前依赖读取对不可变材料、场景、存在、特征定义、特征实例、状态、动态和因果统一要求生命周期无退出；历史读取只接受在历史截止内已经发生的退出，防止公开 helper 的历史完整形状被误收为当前成功。

## 3. 构建与静态检查

- 根工程 x64 Debug fresh Rebuild：PASS，0 警告、0 错误。
- 根工程 x64 Release fresh Rebuild：PASS，0 警告、0 错误。
- `git diff --check`：PASS；换行转换提示不构成空白错误。
- `python .\tools\check_specs.py --strict`：PASS，113/113。
- 普通概念登记幂等身份与 13 个登记键在生产 C++ 中各唯一命中；固定写集键和十个高位分区在目标模块内互异。
- 两个普通概念入口、两个可用构造、根三入口、唯一 owner 写端口与唯一概念写锁均已静态核对；工程、filters 及禁止范围文件相对 HEAD 零变化。
- 目标生产模块未包含测试、mock、seam、断言、自检或验收入口。

## 4. 完成边界

本记录只证明普通概念原子建立与按身份当前 / 历史完整读取的生产实现已进入唯一概念服务，并完成根工程双配置编译及静态合同检查。没有建立专门验收或动态专项；真实首次 / 重复、当前 / 历史调用、并发、资源失败、私有坏形状、普通应用完整装配和合法消费者实际使用均为 `NOT_RUN`。

直接上下位、定义、世界支持关系和生命周期的独立 CRUD、不可逆退出、名称关系、DATA-L3 投影、`DATA-EXT-08` 整项及数据服务最终需求清单整体不在本叶完成声明内。
