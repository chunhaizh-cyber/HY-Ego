# FEATURE-DOMAIN-COMPARISON-PROVIDER 施工记录

版本：v0.1

基线：`dede03ba`

## 施工范围

- 新增 `特征值域比较数据服务`：只读组合概念树、特征类和特征值类。
- 概念树新增按已知 `FC` 的特征概念值域基础窄读，不扫描全部概念。
- 工程登记新头与源文件。

## 实际实现

- `读取特征值域` 对 I64 委托概念窄读、既有 I64 类型读回和规范化；域关系委托既有 `判定I64域包含`，实例命中委托既有 `判定准确特征命中域`。
- I64组、U64组与独立材料引用均有声明及紧邻定义的“待实现”标注，返回结构化 `未实现`，不含成功域、规则或关系载荷。
- 新服务不持有写端口，也没有写集、提交、登记或创建调用。

## 验证与边界

- 本切片目标文件 `git diff --check` 通过。
- Debug/Release x64 主工程构建均在新服务编译前被既有计划外合同错误阻断：`领域/合同.相关概念添加参与.h:729` 的默认 `operator==` 依赖不可比较的 `纯概念结构交付_v2`。该文件不在本计划允许范围，未修改。
- 因上述阻断，未获得新服务翻译单元的编译结果，未运行专项、运行或恢复验证。
- 全仓 `git diff --check` 同时受异主 `AGENTS.md:12` 既有尾随空白阻断；本切片目标文件不含空白错误。

## 代码知识变更清单

| 函数/类型 | 文件 | 用途与边界 |
| --- | --- | --- |
| `读取特征概念值域基础` | `数据服务.概念树类.h/.cpp` | 按已知 FC 窄读 I64 值域；非 I64 明确未实现，不比较、不写入。 |
| `读取特征值域` | `数据服务.特征值域比较类.h/.cpp` | 唯一 FC 值域只读组合入口；I64 复用既有特征类。 |
| `核验特征值域关系` | 同上 | I64 双向包含映射相等/左右包含/不包含；非成功清空关系。 |
| `核验实例值域命中` | 同上 | 准确 I64 特征相对于 FC 域的命中；不把节点或材料身份当值。 |
| 三个非 I64 适配器 | 同上 | 待实现、零写入、零假成功。 |

共享用途卡未在本切片修改；发布后应由知识库维护者基于结果提交复核。
