# INSTINCT-STAGE3-GSEED-ACTIVATION-CURSOR-COMPAT-MIGRATION 施工记录 v0.1

日期：2026-08-31

计划：`INSTINCT-STAGE3-GSEED-ACTIVATION-CURSOR-COMPAT-MIGRATION v0.2`

计划提交：`19114af4a96b8152feb0da6d5e9f3ba414f8ee14`

计划 blob：`08ba2aee3c352f95b8f4c9669af3f6085ebf3f30`

施工起点：`4b6320fcf5c5222a0f3d1705fea0d25407fb06f2`

结果提交：待发布；本记录当前只描述安全 WIP，不代表计划完成。

## 1. S0 与治理裁决

1. `main == origin/main == 4b6320fcf5c5222a0f3d1705fea0d25407fb06f2`，ahead / behind 为 `0 / 0`，施工前 index 为空。
2. 目标计划在正式计划索引中为`可执行`；计划 v0.2 冻结详细设计第 4—6 节为唯一机器合同。
3. 详细设计 v0.1 页眉仍保留“候选、未登记、未发布、不授权施工”的旧文字，与正式计划索引“详细设计已发布”存在治理元数据冲突。因正式索引、计划状态和冻结合同一致，且冲突不改变字段、枚举或 A/B/U 机器语义，本轮按正式索引继续施工；不把该旧页眉解释为机器合同漂移。
4. 知识库和调用图没有本能力的当前用途卡，且图谱基线落后；复用裁决以正式设计、当前源码和真实调用点为准。
5. 复用既有唯一游标 owner、同一 U64 当前槽、L1 中性写集、首次写入重放、当前值读回、主动安全永久账登记见证和服务历史 v2 边界快照；未建立第二 owner、第二游标槽或新工程文件。

## 2. 实际范围

本轮只修改计划白名单中的四份源码，并新增本施工记录和专属验证记录：

```text
海中鱼巣/领域/本能被动维护游标.数据.h
海中鱼巣/领域/服务.本能被动维护游标.ixx
海中鱼巣/启动.应用程序.ixx
海中鱼巣/端到端测试.本能被动维护游标.ixx
施工记录/20260831_INSTINCT-STAGE3-GSEED-ACTIVATION-CURSOR-COMPAT-MIGRATION_施工记录_v0.1.md
验证记录/20260831_INSTINCT-STAGE3-GSEED-ACTIVATION-CURSOR-COMPAT-MIGRATION_验证记录_v0.1.md
```

未修改工程、规范、详细设计、计划、计划索引、知识库、流程图、A/V 服务、状态 / 动态 owner、服务历史 owner 或秒序 composer。

## 3. 实施结果

1. 新增 v4 游标事实、11 项同槽持久材料、激活请求 / 状态 / 见证 / 结果和 v4 独立读取 DTO；字段名、顺序和枚举数值按冻结详细设计实现。
2. v4 解码要求 owner、节点、属性类型、来源锚点、创建 / 退出代次和 11 项材料共同闭合；`形成事实代次`取值事实创建代次，服务历史水位不得小于 `Gseed`。
3. v1/v2/v3 读取对 11 项 v4 提供字段受限投影；按 v4 实际索引读取完整秒 `[4]`、主动安全 `[5]`、服务历史 `[6]`和版本 `[9]/[10]`，没有把维护纪元 `[3]`误解释为完整秒。
4. 新增`显式激活或恢复本能被动维护纪元_v1`：
   - 请求强制绑定同一 self / G0，且服务历史见证必须满足 `左边界 == 包含结束 == 正式截止 == G0`；
   - A 只在 owner 登记完整、v4 当前槽未建立且该专属节点历史属性值为空时形成 11 项 v4；
   - v1/v2/v3 固定返回`旧格式成功维护历史不可证明`，完整 v4 且成功序号大于零固定返回`已有成功维护需完整迁移`；
   - 同键同材料走首次写入重放和 v4 当前值互证；同键异义返回幂等冲突；异键同义保留首次维护纪元并返回恢复；
   - `Gseed`不一致返回覆盖种子漂移；不确定发布或写后读回不闭合返回已可能发布；
   - 唯一写动作是游标 owner 的一个中性写集。
5. 普通启动删除 v1 覆盖起点 + v3 自动迁移消费，改为以 `Gstart=G0`、非零 `1024 / 1024`预算读取 v2 完整边界，调用显式激活，并用 v4 独立读回逐字段绑定维护纪元、三水位、Gseed、零成功序号和放弃边界。
6. 专项新增独立 G01—G05 测试段，源码覆盖 A、同键重放、同键异义、异键同义恢复、v1/v2/v3 兼容投影、持久重启和三种旧格式 U 零写；运行受既有根工程链接缺口阻断，见验证记录。

## 4. 被承接 WIP 保护

施工前基线 patch 保存在：

```text
D:\TEMP\HY-Ego\INSTINCT-STAGE3-GSEED-ACTIVATION-CURSOR-COMPAT-MIGRATION\20260831-execute-v4-8d312001
```

- `海中鱼巣/启动.应用程序.ixx` 的两个线程装配 hunk 仍完整存在：自我线程 L2 任务服务参数、任务管理运行包的动作 / 阶段 / 结果提供者装配均未覆盖。
- `海中鱼巣/端到端测试.本能被动维护游标.ixx` 的“删除等待合同登记”hunk仍完整存在，本轮没有恢复该行。
- 两个混合文件的本轮 Gseed 变更与异主基线语义可分离；当前尚未暂存任何内容。

## 5. 代码知识变更清单

### 5.1 新增或直接修改的稳定函数

- `本能被动维护游标事实完整_v4`
  - 用途：验证 v4 公开事实身份、纪元、三水位、Gseed、版本和形成代次完整性。
  - 非职责：不证明值来自正式 owner，不推断旧格式的成功维护序号。
- `本能被动维护纪元激活结果_v1::成功`
  - 用途：只接受已激活 / 已恢复 / 精确重复及闭合的 v4 游标、激活见证和读回截止。
- `本能被动维护游标读取结果_v4::成功`
  - 用途：只接受已读取、完整 v4 游标及形成代次不晚于正式截止。
- `本能被动维护游标服务::显式激活或恢复本能被动维护纪元_v1`
  - 用途：A 首次激活、同义恢复、精确重放，以及 B/U、漂移、冲突和不确定发布的 fail-closed 裁决。
  - 直接调用边：普通启动和游标专项；内部只写既有游标 owner。
- `本能被动维护游标服务::读取当前本能被动维护游标_v4`
  - 用途：同一当前槽的 v4 独立正式读回；6/7/8 项返回旧格式待裁决。
- `编码游标_v4`、`解码游标_v4`、`形成v4游标`、`v4游标与请求同义`、`形成激活成功结果`及 v4 状态映射函数
  - 用途：module-private 的 11 项材料编码、解码、投影、结果形成和错误映射。
- `解码游标`、`解码游标_v2`、`解码游标_v3`
  - 变化：对 11 项 v4 采用新索引做旧 ABI 受限投影；旧 6/7/8 项原索引保持不变。
- `运行Gseed显式激活专项`及其请求 / 读取辅助函数
  - 用途：验证源码中的 G01—G05 独立测试段；不形成生产调用边。

### 5.2 新增、删除或改变的直接调用边

- 普通启动新增：服务历史 v2 同点完整边界 -> v1 显式激活 -> v4 独立读回。
- 普通启动删除：v1 服务历史覆盖起点 -> v3 自动建立 / 迁移及 v1/v2 回落。
- 旧 v1/v2/v3 服务 ABI 保留，只改变其对 v4 当前事实的只读投影能力。

### 5.3 待知识库维护

安全 WIP 发布后，能力证据 / 知识库维护智能体应在取得已发布代码基线后新增或更新：

```text
本能被动维护游标事实完整_v4
本能被动维护纪元激活结果_v1::成功
本能被动维护游标读取结果_v4::成功
本能被动维护游标服务::显式激活或恢复本能被动维护纪元_v1
本能被动维护游标服务::读取当前本能被动维护游标_v4
解码游标 / 解码游标_v2 / 解码游标_v3 的 v4 兼容投影
```

## 6. 当前完成边界

本轮已经形成语法完整、两配置可编译的安全 WIP，但计划要求的 Debug / Release 完整链接、专项运行、B 成功序号 1/N 运行覆盖和零副作用运行证据尚未取得。因此当前不得声明代码结果完成、A/B/U 运行通过、阶段三完成或本能路线完成；待既有根工程模块链接缺口恢复后继续验证。
