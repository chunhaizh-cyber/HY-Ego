# SCENE-C-IDENTITY-ROLE-PHYSICAL-CONTRACT 场景角色与真实绑定施工记录

日期：2026-09-11

计划：`SCENE-C-IDENTITY-ROLE-PHYSICAL-CONTRACT` v0.8

起点：`6d5e35055ef11164a1df9757becb3cd5ab78ff32`（开工时 `main == origin/main`，index 为空）

执行所有者：`/root/scene_p1_execute_v2`

## 1. S0 与范围

计划索引登记为`可执行`。本轮按4204 v1.1、4203 v0.8、7191 v0.3及详细设计v0.7实施，只写计划六项白名单。`D:\TEMP\再用文件\再用文件列表.md`中的六项占用均属于本执行者；其它工作区修改、删除和未跟踪文件均作为异主WIP原样保留。

允许的生产变更为：新增场景角色组织合同，原地重建场景类，并在根工程及filters唯一登记新合同模块。未修改L1、E/S/D/因果、4203合同、世界树、概念树、普通应用、规范、详细设计、计划或计划索引。

## 2. 实际实现

- 新增`海中鱼巣.领域.合同.场景角色组织`：定义协议v2的C角色、四根、组织路径、写回执、历史读取DTO及窄只读provider。
- 将场景类从B空骨架重建为单一场景owner服务：登记六项元结构；启用同节点C角色并同笔创建四根及四根绑定；当前/历史读取；共同退出与幂等恢复。
- 将4203既有七个B公开入口全部接入真实L1：建立C/E/S三关系、当前/历史读取、三种反查和退出；保留`绑定于`及原公开签名。
- 实现S/D组织树：单父、无环、唯一归根、路径预算、当前/历史路径读取、引用保护和原键收敛。
- `组织动态实例`只确认provider绑定同一L1，使用固定五项预算调用真实`读取当前动态`，核对同G0/H及D身份后维护组织树。按4204 v1.1已物理删除`展开动态来源`、展开载荷检查、逐原子B.C/请求C比较及其专属失败分支。
- 完整处理L1写入结果头、失败状态、发布阶段、映射集合、首次材料、竞争精确重复和读回守卫；提交后不确定统一返回`已可能发布`，不伪造成功载荷。
- 将服务互斥范围收窄到`串行提交`：E/S/D provider、首次材料、前置结构读取及提交后读回均不持服务写锁；短临界区内由L1提交以严格G0和幂等身份完成最终原子裁决。
- 根工程和filters对新合同模块各登记一次；未移除或压制任何原工程项。

## 3. 旧逻辑物理退出

已删除旧`状态使用绑定结构交付`构造、`未实现创建结果`、`未实现读取结果`、`未实现组查询结果`三个辅助和七个B入口的空函数体。七个B签名、4203 DTO/provider以及`绑定于`保留并成为真实实现。生产扫描不再出现上述未实现辅助、运行时`未实现`回退、动态来源展开或B.C场景核验。

计划已明确排除的世界树、存在概念应用及概念树悬空旧调用未在本片修复，也没有恢复旧场景ABI适配它们。

## 4. 函数级代码知识变更清单

共享知识索引不在本计划写范围。下列内容应由代码知识维护者在结果提交后按最终blob更新用途卡：

- 新增合同谓词：场景当前/历史、组织历史、动态组织历史、结构登记、C启用/退出、S/D组织写结果的`成功`判断；只做DTO自洽和请求回显判断，不读取L1。
- 新增公开入口：`登记结构类型`、`确认当前场景角色`、`读取场景角色历史`、`启用场景角色`、`退出场景角色`、`组织状态实例`、`组织动态实例`、`读取实例组织历史`、`读取动态场景组织历史`。
- 重建公开入口：`绑定于`、`创建状态使用绑定`、`读取当前状态使用绑定`、`读取状态使用绑定历史`、三种`按...查询当前状态使用绑定组`、`退出状态使用绑定`。
- 新增内部读取与校验：`布局形状有效`、`登记材料匹配`、`查询关系`、`读节点`、`读关系`、`读角色`、`读B`、`查B组`、`读路径`、`读取当前代次`、`核验代次`。
- 新增内部写集与收敛：`形成启用写集`、`形成绑定写集`、`形成组织写集`、`组织`、`串行提交`、`写入头完整`、`写入失败头完整`、`写入映射完整`、`竞争精确重复材料完整`、`写集相同`、`映射编码`。
- 新增来源映射：`场景到B`、`存在到B`、`状态到B`、`状态到场景`、`动态到场景`、`存在到场景`、`映射场景写入状态`、`映射绑定写入状态`。
- 退出函数：旧构造、`未实现创建结果`、`未实现读取结果`、`未实现组查询结果`及七个B空函数体；4204 v1.1另退出场景侧动态来源展开及B.C核验调用边。

## 5. 完成边界

本记录只描述候选施工事实。验证见同名验证记录。即使目标模块及探针通过，也不证明根工程闭合、普通应用接线、数据库跨进程恢复、完整七字段CRUD、动态实际发生场景或来源一致性、完整观察、场景TTL、正式集成验收或业务闭环。
