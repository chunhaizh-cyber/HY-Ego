# 中性非阻塞一致当前投影施工记录

日期：2026-08-08

计划：`L1-NEUTRAL-NONBLOCKING-CONSISTENT-PROJECTION v0.1`

plan blob：`b4fb05384f9c4e74bd8efafb933d08f1053dfc90`

设计提交：`0dccacbdc19e089dc46a3650bd6ee53345ecca95`

施工起点：`00de5165eef92cf844388a8326d7218f9b269817`

## 1. 开工核对

- 开工时 `main == origin/main == 00de5165eef92cf844388a8326d7218f9b269817`，ahead / behind 为 `0 / 0`，Git index 为空；计划索引 blob 为 `9ee25236559b8d97e28533b033e87ad51b049021`，本计划是唯一可执行项。
- 三个代码目标起始 blob 依次为 `d8f2c9ba052458fd9f22f6d6357695188d040c41`、`f21cc54ed4eb354f988b92286cb286cf011f69dc`、`b16dce9a5e1172751c90a0e0382564a03142d4d2`，与计划冻结事实一致。
- 现有共享锁、当前节点 / 关系 / 值表、源 / 目标关系索引、单编码历史表和服务中性转换器均存在，无等价非阻塞一致当前 provider。
- 本计划取得五个白名单路径所有权；P0-FIX、路线、旧 WORLD-TREE 和 `.codex-build` 异主现场不在本施工范围。

## 2. 实际施工

### 2.1 中性公开合同

在 `海中鱼巣/核心/L1中性CRUD.数据.h` 增加 v1 一致当前读取合同：

- 固定顶层状态 `成功 / 入口拒绝 / 许可拒绝 / 事实代次漂移 / 资源失败 / 内部不一致`；
- 固定项目状态 `成功 / 未找到 / 已退出 / 属性未设置 / 种类不匹配`；
- 增加节点、关系、值、属性值、源关系组、目标关系组六类请求与结果 DTO；
- 关系组成员携带关系事实和同许可取得的对端节点事实；所有 DTO 为调用方拥有的值式结果。

没有修改或重排既有 `L1中性读取状态`。

### 2.2 仓库非阻塞一致读取根

在 `海中鱼巣/核心/仓库.L1事实基座.ixx` 增加模块内 legacy 值式内部结果和唯一仓库根 `尝试读取一致当前内部投影`：

- 锁外校验合同版本、非零期望代次、六组非全空、稳定编码和组合键唯一性；
- 合法请求恰一次 `std::try_to_lock`，未取得许可立即返回许可拒绝，不回退阻塞锁、不 sleep、不轮询、不重试；
- 在同一共享许可内直接核对隔离与事实代次，按请求顺序精确读取三类当前事实、属性当前值和源 / 目标关系组；
- 关系组只从源 / 目标索引取得候选，再逐项回读当前关系、端点、类型和对端节点，不扫描全仓关系；
- 对节点槽—值—属性类型、关系两端与关系类型、值所属 / 来源 / 属性类型及材料表示作局部互证；单编码缺失只精确查看对应历史项以区分未找到、已退出和种类不匹配；
- 顶层非成功统一返回六组全空；仅事实代次漂移携带实际非零读取代次。

新根不调用 `尝试读取当前事实代次`、`状态完整`或 `构造快照`，不遍历全部当前表、永久占用、历史、幂等账或审计账；不形成写集、事务、索引写、缓存、日志、审计、恢复、SQL 或材料外部调用。

### 2.3 服务薄入口与唯一转换

在 `海中鱼巣/核心/服务.L1事实基座.ixx` 增加公开入口 `尝试读取中性一致当前投影`。仓库许可释放后，服务复用既有 `转换中性节点`、`转换中性关系`和`转换中性值`形成六类中性深副本；没有新增第二套转换器，也没有向公开结果泄漏仓库内部 legacy 类型。

服务再次核对成功六组与请求同序等长、唯一读取代次，以及仓库非成功的六组全空和代次边界。分配 / 长度异常映射为资源失败，其它异常映射为内部不一致；所有服务侧失败结果六组全空。

## 3. 验证

- Debug x64 完整 Rebuild 通过：退出 0、0 编译错误、1 条临时输出目录 `MSB8029` 提示。
- Release x64 完整 Rebuild 通过：退出 0、0 编译错误、1 条临时输出目录 `MSB8029` 提示。
- Debug / Release 模块依赖扫描均为 `sources=136, modules=136, failed=0`，拓扑 136、循环 0、外部需求 0。
- 正式规范严格检查通过：110 / 110；白名单差异空白检查通过，仅有 Git 行尾策略提示。
- 静态函数体检查确认新仓库根 `std::try_to_lock` 恰 1；`状态完整`、`构造快照`、`尝试读取当前事实代次`、sleep 和重试均为 0；服务入口无锁、无写入或外部副作用。
- 基线和当前的 `转换中性节点`、`转换中性关系`、`转换中性值` 均各只有一个定义。
- 异主四个 tracked WIP Git blob 与开工值一致；两个旧 WORLD-TREE 文件 SHA256 与开工值一致。

构建输出、日志和模块扫描结果位于系统临时目录 `C:\Users\zhchh\AppData\Local\Temp\hzy-l1-neutral-projection-5d4eaefedce84729a81cc7966f85adee`。生成的生产程序未运行。

## 4. 完成边界

本施工只证明中性非阻塞一致当前投影的合同、一次 try-lock 仓库候选、六类受限选择、锁外唯一中性转换和构建 / 模块 / 静态施工门禁成立。

全部消费者业务场景均为“未运行”。本施工不证明许可竞争已真实运行，不证明任何消费者已迁移或成功，不证明历史正文缺口闭合，不构成 L1 专项测试、驱动或 PASS，也不证明第一层自证、第一层读取迁移完成或第一层完善。
