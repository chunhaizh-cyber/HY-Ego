# INSTINCT-STAGE3-MAINTENANCE-CURSOR-FOUNDATION 施工记录

日期：2026-08-29

计划：`INSTINCT-STAGE3-MAINTENANCE-CURSOR-FOUNDATION` v0.1

计划 blob：`980347f6f4b9315db186cf2960b5bc7efe56d211`

起点：`dd318c59f689479a3ae2bbd395b878094a0384c6`

结果提交：本记录与实现候选同提交形成；精确哈希以发布回传为准。

## 1. S0 与所有权

- 开工时 `main`、`HEAD == origin/main == dd318c59f689479a3ae2bbd395b878094a0384c6`、ahead/behind `0/0`、index 空；计划索引唯一登记项为本计划 `可执行`。
- 共享工作区保留 SELF-GOVERNANCE 异主 WIP。本切片只写计划列出的两个生产新文件、普通应用、启动、工程登记、一个专项和两份专属记录；未覆盖、删除或暂存异主增量。
- 四个混合文件开工工作树 blob：`装配.普通应用.ixx=4dbc6547...`、`启动.应用程序.ixx=43570775...`、`海中鱼巣.vcxproj=24edbf36...`、`海中鱼巣.vcxproj.filters=85987518...`。发布时只允许把本计划 hunk 构造进 index。
- 发布 index 由 `HEAD + 本计划增量` 独立投影形成；四个混合文件未带入 SELF-GOVERNANCE 构造参数、服务成员或工程登记。专项测试另恢复 HEAD 现行等待合同登记，消除对未发布配置删减的隐式依赖。
- 复用现有 L1 owner-scoped CRUD、L1 持久恢复、L2 特征实例正式读回、单调时钟适配器和完整秒时钟；没有新增 L1 ABI、第二时间权威或游标推进入口。

## 2. 实际施工结果

1. 新增版本 1 的游标事实、建立/恢复请求结果、读取请求结果和完整成功谓词；固定 owner 建立身份 `0x494E'5354'4355'5253` 与本地键段 `0x0307'1001..0x0307'1004`。
2. 新增唯一游标 owner 交付和服务；登记锚点、游标节点、属性类型与当前槽可从首次写入材料恢复，不扫描历史最大值认领当前。
3. 建立入口覆盖首次游标、同键重放、异键同义恢复、新纪元切换、同纪元推进拒绝、版本漂移、幂等冲突、当前性漂移和已可能发布；旧纪元值不退出，当前性只由当前属性槽表达。
4. 公开读取固定从当前属性槽定位值，执行 Gread 前后守卫并互证唯一自我；状态/动态、A/V、L/H、服务合同和活动集合均不在该服务内形成或推导。
5. 普通应用建立唯一 owner、形成唯一服务实例并提供 const / non-const getter；增加三个具名装配失败状态。
6. 启动在本能根锚点成功后、自我线程创建前构造单调时钟和完整秒服务，建立/恢复游标并独立读回。现有本次启动 UTC 计数只复用为运行代次/纪元身份和幂等身份；完整秒边界仍只来自稳态单调时钟。
7. 自我线程改为消费同一 `本次运行代次`，避免游标、完整秒时钟和自我线程形成三套运行代次。
8. 新增专项模块与工程登记；临时 runner、ABI consumer、可执行文件和隔离持久根全部位于 `D:\TEMP\海中鱼巣\INSTINCT-STAGE3-MAINTENANCE-CURSOR-FOUNDATION`。

## 3. 代码知识变更清单

| 模块 / 文件 | 稳定函数身份 | 用途、非职责与调用边 | 用途卡动作 |
| --- | --- | --- | --- |
| `本能被动维护游标.数据.h` | `本能被动维护游标事实完整_v1(...)`；`本能被动维护游标建立结果_v1::成功()`；`本能被动维护游标读取结果_v1::成功()` | 固定游标事实和两类成功形状；不裁决 A/V 或业务目标。 | 新建 |
| `服务.本能被动维护游标.ixx` | `尝试形成本能被动维护游标所有者交付(...)` | 将唯一 L1 owner 交付收窄为游标专用移动型写端口，并独立读回 owner 当前事实。 | 新建 |
| 同上 | `本能被动维护游标服务::建立或恢复本能被动维护游标_v1(...)` | 首次建立、同义恢复、新纪元切换及结构化拒绝；不提供同纪元推进入口。 | 新建 |
| 同上 | `本能被动维护游标服务::读取当前本能被动维护游标_v1(...)` | 只从唯一当前属性槽读取并执行 Gread 前后守卫；不从历史最大值猜当前。 | 新建 |
| `装配.普通应用.ixx` | `构造普通应用上下文(...)`；`取得本能被动维护游标服务()` | 建立唯一 owner 和服务并暴露同实例引用。普通应用不公开 L1 写端口。 | 更新 |
| `启动.应用程序.ixx` | `运行普通程序(...)` | 本能根完成后、创建自我线程前读取正式完整秒、建立/恢复并独立读回游标；失败阻止自我线程创建。 | 更新 |
| `端到端测试.本能被动维护游标.ixx` | `运行本能被动维护游标端到端测试()` 及 module-private fixture | 通过普通应用与公开服务验证 C00—C06；仅写 `D:\TEMP` 隔离持久根。 | 新建测试用途卡或登记为测试函数 |

新增直接调用边：`构造普通应用上下文` → `尝试形成本能被动维护游标所有者交付` → `本能被动维护游标服务`；`运行普通程序` → `完整秒时钟服务::读取当前完整秒边界` → `建立或恢复本能被动维护游标_v1` → `读取当前本能被动维护游标_v1`。没有删除公开函数。

## 4. 完成边界

- 本记录只证明本能被动维护初始时间游标基础，不证明同纪元完整秒推进、A/V 维护、状态/动态记录、双根自检或完整本能路线。
- 资源失败、内部故障和提交后读回失败没有获授权的生产故障注入口，验证记录保留 `NOT_RUN`；未用测试宏或私有状态出口伪造覆盖。
- 共享知识库、函数用途卡、调用图和能力证据目录不在本计划白名单；结果发布后由维护者依据本节清单更新。
