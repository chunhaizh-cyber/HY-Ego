# L1-DATA-LAYER-COMPLETE-P1-NEUTRAL-CRUD v0.2 施工记录

日期：2026-08-07

## 计划与基线

- 计划：`L1-DATA-LAYER-COMPLETE-P1-NEUTRAL-CRUD v0.2`
- plan blob：`ca8a1b80ba5caec0033cc213bd9714bd8d3baa4a`
- 设计 / 规范提交：`df5000c46969a3d25ddb5aed66dfedbf74ab4f9d`
- 施工起点：`01b3a92329bccf9f6eb8ddaed9730547045165da`
- S0：`main == origin/main`、ahead / behind `0/0`、Git index 空；v0.1 已退出，v0.2 为索引唯一可执行。
- 现场边界：P0-FIX、路线、旧 WORLD-TREE 和 `.codex-build` 均为异主 WIP，路径与本切片八项允许范围不重叠，原样保留。

## S0 机械事实

1. 4015 v0.5、4030 v1.3、4040 v1.4、4050 v1.2、ACCEPTANCE-01 v0.3 与 v0.2 详细设计 / 单函数图 / 计划一致。
2. 当前事实仓仍由一个 `shared_mutex`、一个仓库状态、一个事实代次和一个全局稳定编码分配器拥有三类事实。
3. 现有带操作标签 legacy 写调用保持 10 个生产文件、16 个调用点；本切片不修改这些文件。
4. 新中性 DTO、合同模块和两份专属记录在施工前均不存在；六个代码 / 工程文件没有异主差异。
5. 计划命令中的解决方案扩展名为 `.sln`，当前正式仓库实际文件为 `海中鱼巣.slnx`；构建使用同一正式解决方案的 `.slnx`，不改变配置或机器语义。

## 实际施工

### 中性合同

- 新增 `L1中性CRUD.数据.h`：合同版本 1；中性本地键 / 幂等键、事实引用、四种材料、节点 / 关系 / 值投影、写集、写入 / 读取状态、重试边界和七入口请求 / 结果 DTO。
- 新增 `合同.L1中性CRUD.ixx`，唯一导出 `海中鱼巣.核心.合同.L1中性CRUD`；只导出公共事实和中性 DTO。
- 新合同未携带业务操作标签、意图组、执行证据、领域结果见证、审计或恢复材料。

### 同仓事务与最小幂等

- 在既有仓库状态、既有锁和既有候选复制 / 最后交换内新增 `中性幂等账`；没有新增中性当前事实容器、事实代次、编码分配器或锁。
- 新增唯一 `L1事实基座仓库::提交中性写集`：锁前规范化 / 形状拒绝；锁内双账交叉占用、代次与引用复核；同一候选内分配编码、发布三类事实、退出旧事实和替换属性槽；候选完整性及机械读回后写入首次结果；一次交换；发布后按原请求、映射和代次复核。
- 中性与 legacy 幂等键交叉复用均结构化返回冲突；legacy 公开签名和 16 个消费者调用保持不变。
- 中性账非空时，legacy 恢复材料导出、候选建立和候选确认结构化拒绝；撤销只允许清除中性发布前已建立候选。本切片不声明恢复能力。

### 服务与工程

- `L1事实基座服务` 导出新合同并增加七个正式入口：提交、当前节点、当前关系、当前值、当前属性、历史事实和当前事实代次读取。
- 中性读取只调用既有精确仓库读取并形成值式 DTO；没有调用完整快照、审计、恢复、索引或日志。
- `海中鱼巣.vcxproj` 与 filters 对新模块和数据头各登记一次；未增加第二目标、自检模块、验收入口或专用驱动。
- 新增和直接修改的函数按 CODE-FILE-01 登记 `向上送出` 或 `无适用错误分支` 诊断责任。

## 施工边界

本提交只形成中性 CRUD 实现候选并完成施工门禁。不建立专用 L1 外部黑盒驱动、独立 L1 验收提交、生产内自检或日志 PASS；不证明 L1 行为正确、消费者充分性、legacy 迁移、旧仓退出、完整数据层完善、崩溃或跨进程恢复。
