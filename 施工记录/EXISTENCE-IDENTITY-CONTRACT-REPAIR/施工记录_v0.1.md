# EXISTENCE-IDENTITY-CONTRACT-REPAIR 施工记录 v0.1

## 1. 范围与 S0

- 计划：`EXISTENCE-IDENTITY-CONTRACT-REPAIR` v0.1 / blob `089f49f4cce8ddf5b04f7e911d1a34595449cff4`。
- 开工正式HEAD：`790158a26b39e977c4570a7143bda4d662fd3014`；`main`与`origin/main`一致，index为空。
- 允许生产文件：`海中鱼巣/领域/数据服务.存在类.h`。该文件开工前无工作区改动；根材料 WIP 只位于`数据服务.需求类.h`和`装配.普通应用.h/.cpp`，未读取、暂存、覆盖或纳入本切片。
- 复用裁决：`数据服务.特征类.h`中的`特征信息身份`采用“`稳定编码`字段 + 默认相等 + `有效`谓词”的值式强类型。当前`存在类`公开 IF/R DTO 已经以`存在信息身份`表达 E，但没有同名定义；按详细设计v0.11在存在类头同一命名空间采用该形状，不新建服务、owner、事实或事务。

## 2. 实际变更

在`存在类数据合同版本`之后、所有 IF/R 消费点之前新增`存在信息身份`：唯一字段为`稳定编码 编码{}`，默认`operator==`，以及只委托`有效(编码)`的纯值谓词。没有改动既有 DTO、函数体、调用点、读取/写入、缓存、工程登记或根材料 WIP。

## 3. 代码知识变更清单

| 身份 | 模块 / 文件 | 用途与非职责 | 调用边、状态和生命周期 | 知识维护动作 |
| --- | --- | --- | --- | --- |
| `bool operator==(const 存在信息身份&, const 存在信息身份&)` | `海中鱼巣/领域/数据服务.存在类.h`，公开非服务纯值函数 | 比较两个存在节点强类型包装的编码；不读写事实、不校验存在性或生命周期 | 新增值式默认比较；无直接调用边和副作用 | 结果发布后为存在类公开 DTO 新建或更新用途卡 |
| `bool 有效(存在信息身份) noexcept` | 同上，公开非服务纯值函数 | 判断包装的编码非零；不证明存在族、owner、当前性或业务资格 | 新增纯值谓词；无 I/O、锁、事务、缓存或生命周期变化 | 同上 |

没有服务函数新增、修改或退出；IF/R 既有 DTO 字段和特征概念应用的直接消费点仅从“未解析声明”变为可解析，未改其签名或调用边。

## 4. 未覆盖范围

本记录不证明 IF/R 行为、根材料 provider、根工程构建、启动、恢复、线程、集成验收或持续运行。根材料计划仍须消费本结果后重新S0并执行其自己的验证矩阵。
