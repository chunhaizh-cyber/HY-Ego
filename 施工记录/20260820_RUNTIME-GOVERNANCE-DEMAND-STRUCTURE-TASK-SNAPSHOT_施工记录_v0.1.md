# RUNTIME-GOVERNANCE-DEMAND-STRUCTURE-TASK-SNAPSHOT 施工记录 v0.1

日期：2026-08-20

计划：`RUNTIME-GOVERNANCE-DEMAND-STRUCTURE-TASK-SNAPSHOT v0.1`

计划 blob：`45ff8fa73a37ff96f7e7226b67e71119daa8d0d0`

起点：`1a8b2d560f3f457dc765709c70e17773859974d8`

## S0

- 正式索引中的计划身份、版本、blob、设计提交 `d8dcb6f50c5c3117df2bd8d4b091588a3202d8b9` 与本切片一致，状态为`可执行`。
- `main`、`HEAD`、`origin/main`均为起点提交，ahead/behind 为 `0/0`，Git index 未占用。
- 工作区存在无关未跟踪 WIP 和旧 `.codex-build/` 残留；均未改动、未暂存、未夹带。
- 复核了根需求组/当前父 DTO 草案、唯一 `L2需求结构服务`、任务当前读取、运行期只读组合器、父治理 N04 和既有隔离合法消费者。旧组合器固定返回材料缺失，未发现可直接复用的等价结构快照入口。

## 实际施工

- 给根需求组请求加入根数量预算，删除无语义的根父子占位组，并由成功谓词保留完整、当前、稳定排序约束。
- 在唯一需求服务加入根组和当前父两个公开 provider；共享 module-private 无二次加锁投影，避免同一非递归 mutex 重入。
- 用需求结构、当前父、列表成员、完整列表项和 optional 当前任务替换旧活动型 DTO 与固定材料缺失骨架；全部读取逐项验证同一 G0，非成功不返回部分快照。
- 父治理 DTO / N04 仅机械改为三个结构预算和新结果字段；未调整三子成功、错误传播或 G1 规则。
- 既有治理端到端消费者改为公开组合器读取合法空根快照和零预算拒绝，并把父读回改为同一空快照断言。

## 代码知识变更清单

| 身份 | 变化 | 用途、合同与调用边 | 待知识维护 |
| --- | --- | --- | --- |
| `L2需求结构内部::按子需求读取当前父需求当前投影` | 新增 | 持锁上下文内按子需求反向读取 0/1/>1 当前父；只供两个公开 provider 复用，不再加锁、不回调公开入口 | 新建用途卡 |
| `L2需求结构内部::按所属存在读取当前根需求组当前投影` | 新增 | 按所属存在反向读取完整当前需求、筛选无父根并执行根预算；不限制子孙候选扫描 | 新建用途卡 |
| `L2需求结构服务::按所属存在读取当前根需求组` | 新增 | public const 入口，单次锁、请求/G0 守卫后调用内部投影 | 更新需求服务用途卡 |
| `L2需求结构服务::按子需求读取当前父需求` | 新增 | public const 入口，单次锁、请求/G0 守卫后调用内部投影 | 更新需求服务用途卡 |
| `运行期只读查询组合器::读取需求结构与任务后继快照` | 新增并替换旧骨架 | 同 G0 BFS 组合根、需求、列表及 0/1 任务；只读、零活动性、失败零快照 | 更新组合器用途卡；旧活动骨架身份标记退出 |
| `读取自我治理一致事实` | 修改 | 仅构造新请求和接收新字段，仍不实现最终三子/G1 收口 | 更新治理骨架用途卡 |
| `运行自我治理一致事实端到端测试` | 修改 | 公开 L1 签发/L2 构造/组合器消费者路径验证合法空根与拒绝 | 测试身份待复核 |

结果提交：待填（发布后回填于验证记录）。

## 未实施边界

未实施需求活动、授权、权重、优先或满足；未实施父级三子完整、G1、治理判断、任务筹办、任务执行或生产普通应用装配。
