# ARCH-L4-INSTINCT-ROOT-DEMAND-PROVIDER 安全 WIP 施工记录

基线`dd0b0bdb8f4130f934fba35e514320250f3ffdd4`，计划v0.6/blob`0c856f92b70e5537c69226ebbbb971b9b9f53f3a`仍为可执行。只修改计划白名单的`需求类`数据头及普通应用装配头/源：同一需求 owner 原子登记普通4+根3+I64属性1共8类型；普通应用持有需求服务并只暴露受限借用取得；根入口按角色原幂等身份建立或读回`T/D/L`、`D->F/D->T/L->D`和`T=I64_MAX`属性槽。

根入口未被普通应用调用，静态检索未发现装配层调用`建立或读取本能根材料`。三处 WIP 均在本计划范围，未修改启动、线程、特征/存在服务、既有 owner 键或异主文件。

代码知识变更清单：新增/修改`需求类数据服务::登记需求结构`、构造函数、`建立或读取本能根材料`及其私有读回/写集辅助；新增`读取普通应用需求服务`。这些身份尚无当前用途卡，发布后须由知识库维护者按结果提交复核；本记录不修改共享知识库。

本提交是可编译安全 WIP，不是计划完成：双根首次/重放/未知发布/漂移/错误矩阵尚未形成独立运行驱动并验证。

## 续行 v0.2：重放收敛修复与外部阻断

在`c7e32efc52c4f4346c8fa936c1fc381b1d3a21a2`上，隔离登记探针证明首次结构写集保存首次`G0`，而后续请求必须使用当前代次守卫；原先完整比较会把仅用于本次窗口的`期望事实代次`误判为幂等冲突。仅在本计划白名单的`需求类数据服务`中新增私有比较：忽略该守卫字段，仍精确比较合同版本、幂等身份、节点、关系、值、属性槽变更和退出事实。该比较同时用于需求结构登记和根材料恢复，未改变任何公开DTO、写入布局或失败状态。

独立根 provider 驱动只位于`D:\TEMP\海中鱼巣\ARCH-L4-INSTINCT-ROOT-DEMAND-PROVIDER\20260920-s0\root-provider-probe\`，不进入工程或Git。它使用真实L1运行包、owner端口和现有服务构造路径验证根 provider；为了隔离该provider，驱动只以原始普通节点模拟上游4115已交付的实际`F`，不声称实现或验证4115。

发现计划外装配阻断：`存在类数据服务::登记实例特征结构`对合法v2请求返回`已登记`和交付，但结果的`版本`保留默认`1`，其`成功(request)`因要求`版本==2`返回假。普通应用在需求结构登记之前以该谓词失败；此函数不在本计划允许范围，未修改。根 provider 驱动为隔离其本体合同而仅消费已登记交付，不能证明普通应用已装配需求服务。

## 续行 v0.3：持久恢复的首次 H 与当前 G 分流

在目标账事务`TARGET-20260920-17`发布后的`964823faeaef259e0ddfec98255b8a98e9baef5a`重新S0。计划`ARCH-L4-INSTINCT-ROOT-DEMAND-PROVIDER`仍为v0.6/blob`0c856f92b70e5537c69226ebbbb971b9b9f53f3a`可执行，唯一计划内生产修改为`海中鱼巣/领域/数据服务.需求类.h`。

修正了两条持久重放读回边界：`登记需求结构`在恢复首次材料时，以本次请求当前`G0`核验读回及结果事实代次，同时保留首次写入事实代次作为物理创建事实；`从根写入读回`同样保留`H=写入.事实代次`供节点、关系和值的创建事实校验，恢复时以`G=请求.期望事实代次`核验当前读回、最终当前代次、结果事实代次和`材料.读取事实代次`。首次形成仍使用新写入`H`同时作为读回截止。未改公开DTO、持久格式、L1、装配、启动、线程或计划/规范。

代码知识变更清单：修改`需求类数据服务::登记需求结构`的首次/恢复读回截止选择，修改私有`需求类数据服务::从根写入读回`的创建代次/当前读回代次分流；二者的用途仍分别是需求owner元结构交付与根材料一致读回，调用边和公开合同不变。它们尚无对应当前用途卡，发布后由知识库维护者依据结果提交复核；本施工记录不修改共享知识库。
