# ARCH-L4-INSTINCT-ROOT-DEMAND-PROVIDER 安全 WIP 施工记录

基线`dd0b0bdb8f4130f934fba35e514320250f3ffdd4`，计划v0.6/blob`0c856f92b70e5537c69226ebbbb971b9b9f53f3a`仍为可执行。只修改计划白名单的`需求类`数据头及普通应用装配头/源：同一需求 owner 原子登记普通4+根3+I64属性1共8类型；普通应用持有需求服务并只暴露受限借用取得；根入口按角色原幂等身份建立或读回`T/D/L`、`D->F/D->T/L->D`和`T=I64_MAX`属性槽。

根入口未被普通应用调用，静态检索未发现装配层调用`建立或读取本能根材料`。三处 WIP 均在本计划范围，未修改启动、线程、特征/存在服务、既有 owner 键或异主文件。

代码知识变更清单：新增/修改`需求类数据服务::登记需求结构`、构造函数、`建立或读取本能根材料`及其私有读回/写集辅助；新增`读取普通应用需求服务`。这些身份尚无当前用途卡，发布后须由知识库维护者按结果提交复核；本记录不修改共享知识库。

本提交是可编译安全 WIP，不是计划完成：双根首次/重放/未知发布/漂移/错误矩阵尚未形成独立运行驱动并验证。
