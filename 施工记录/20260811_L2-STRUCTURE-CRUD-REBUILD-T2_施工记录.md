# L2-STRUCTURE-CRUD-REBUILD-T2 施工记录

日期：2026-08-12

## 1. 计划与现场

- 计划：`L2-STRUCTURE-CRUD-REBUILD-T2` v0.2。
- 精确 plan blob：`efc33aadd17119aa8bc9d94be066026ef6062474`；设计提交：`1a0086926ac8e42781e48cc0ef2b9926479eb67c`。
- S0：`PASS`。执行起点为 `ad7fdb85ea6e5922201a05ab26946dd21fa86419`，当时 `main == origin/main`、分歧 `0/0`、Git index 为空。
- 允许范围：两个状态生产文件、三份临时验证文件和两份永久记录，共七路径。
- 流程图、路线输入、`.codex-build/` 及其它异主 WIP 原地保留，未暂存、覆盖或带入本切片。

## 2. 实际施工

1. 在 `L2状态结构.数据.h` 增加引用对关系、引用替换、精确 / 时间值强类型换代和状态退出闭包 DTO，以及请求纯值有效性和成功结果形状检查。
2. 在唯一 `L2状态结构服务` 原位增加：
   - `替换状态引用`：主体 / 特征实例两关系在一份状态 owner 写集中同代退出和新建；新主体、实例及状态来源按同一截止核对正式来源与实例宿主；
   - `换代状态值`：精确 / 时间任一角色的新值、属性槽切换和旧值退出在一份写集中同代发布，来源只回显不换代；
   - `退出状态`：状态节点、族归属 / 主体 / 实例三关系、精确 / 时间两值共六事实在一份写集中同代退出，并由 L1 当前引用闭包保护外部引用。
3. 修正历史完整状态读取：状态在截止代次退出时，以退出前一代选择有效三关系和两值，再把节点、关系和值的退出生命周期投影到请求截止；当前读取仍返回 `已退出` 空载荷。
4. 三个成功 / 精确重复读回均从首次发布代次重建，逐字段证明关系源 / 目标 / 类型 / 角色 / 生命周期、值所属状态 / 类型 / 来源，以及未改节点、关系、值、端点与来源；矛盾结构化返回内部不一致，不补偿已发布事实。
5. 新建生产工程外临时专项三文件，只消费 L1、存在、特征和状态正式公开入口，不登记到主工程、filters 或 solution。

## 3. 施工裁决

- 历史关系 / 属性组在截止恰等于退出代次时不返回已退出项，因此状态退出历史读取使用 `退出事实代次 - 1` 选择有效结构，再保留完整退出生命周期；未增加第二历史入口。
- 替换后的关系和值换代后的值允许创建代次晚于状态节点，但不得早于节点或晚于读取截止；节点身份与创建代次不变。
- 精确重复的结果头状态为 `精确重复`，载荷和首次截止 / 变更代次与首次结果一致；不要求整个 DTO 与首次 `已提交` 结果逐字段相等。
- 独立审计发现的来源截止、成功读回逐字段证明和专项矩阵缺口均属于计划白名单内实现 / 验证问题，已原位补齐，没有改变公开合同或扩大文件范围。
- 生产代码未复用历史状态 / 动态登记，未提前 DYN、因果或 A1，未新增工程登记、装配、仓、缓存、索引、日志、测试入口或故障注入。

## 4. 临时文件生命周期与完成边界

三份临时验证源文件随本结果提交发布，只为让永久记录可追溯；计划结果和记录发布后，将以独立后续提交只删除这三份文件。专项构建产物位于系统 `%TEMP%\海中鱼巣_T2_<GUID>` 目录，不在仓库内，也不是需要 Git 清理的源文件。

本施工只形成 T2 的状态引用替换、精确 / 时间值换代、状态退出、外部当前引用保护和退出历史投影。它不实现 DYN1 / DYN2、C1 / C2、A1，不证明状态真实性、生产消费者、恢复、跨进程或独立集成验收。
