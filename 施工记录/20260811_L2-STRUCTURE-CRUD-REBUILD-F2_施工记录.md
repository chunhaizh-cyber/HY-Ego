# L2-STRUCTURE-CRUD-REBUILD-F2 施工记录

日期：2026-08-11

计划：`L2-STRUCTURE-CRUD-REBUILD-F2 v0.1`

计划文件：`计划/已完成计划/20260811_L2-STRUCTURE-CRUD-REBUILD-F2_通用特征实例新增查询与按存在当前枚举代码实施切片_v0.1.md`

计划 blob：`a257819b249468fd85750353f7d673d9b760f937`

设计提交：`c6a1b343c4c6b9ac4266576c937723dbb9e9406c`

施工起点：`main == origin/main == f619db3b75b7afea36aaf8562b04b92361bfd318`

## 1. S0 结论

S0 通过后开始施工：F2 是唯一 `可执行` 代码计划，F1 结果与清理已发布，F3 保持 `待激活`；Git index 为空，七条允许路径均干净或尚不存在，主工程、filters、L1、E2、计划、规范、流程图与其它验证资产不在本叶写入范围。

施工前核对的 F1 生产 blob：

- `海中鱼巣/领域/L2特征结构.数据.h`：`a9b4d827983097f9575d584b543503bcd12e8542`
- `海中鱼巣/领域/服务.L2特征结构.ixx`：`241943a6d3d8fc05089cc9dd45f67ea3c6052d97`
- `海中鱼巣.vcxproj`：`4a845eb328be550b5f1587326b6a88ebc40e4969`
- `海中鱼巣.vcxproj.filters`：`8c253c7874940781194d06ed44961ea28baaf2d9`

既有流程图、路线输入和 `.codex-build/` dirty 属于其它所有者，施工期间原地保留且未纳入 F2。

## 2. 实际变更

### 2.1 生产数据合同

在既有 `海中鱼巣/领域/L2特征结构.数据.h` 原位增加：

- 两类特征关系类型强身份、登记状态、请求、定位和结果；
- `L2特征值事实` 与 `L2特征实例事实`；
- 实例新增、完整读取和按存在当前枚举 DTO；
- 登记、请求、特征值、最终实例与 F2 空值实例的纯值完整性判断；
- 登记结果和枚举结果的纯值成功判断。

F2 成功实例固定 `当前值 == std::nullopt`；没有实现值写入、值历史或实例退出入口。

### 2.2 唯一生产服务

在既有 `海中鱼巣/领域/服务.L2特征结构.ixx` 和同一个 `L2特征结构服务` 原位增加：

- 固定私有幂等域及宿主 / 定义关系类型稳定登记；
- 构造演进为同时注入 `L1事实基座服务&` 和 `const L2存在结构服务&`，未保留旧单参数构造；
- `建立特征关系类型登记` 与 `读取当前特征关系类型登记`；
- `新增特征实例`：一份 L1 写集原子创建普通实例节点和两条具名端点关系；
- `读取完整特征实例`：当前一致投影和历史半开区间重建；
- `按存在读取全部当前特征`：目标关系组反向读取、逐项复用公开单项读取并按实例身份严格升序返回。

没有建立第二服务、仓库、索引、缓存、SQL、日志、sleep、非登记重试循环、验证注入或 F3 骨架；没有复用历史状态 / 动态登记代码。

### 2.3 临时生产外验证

新增三份按计划具有后续清理生命周期的临时文件：

- `验证工具/L2通用特征实例参数验证.cpp`
- `验证工具/L2通用特征实例参数验证.vcxproj`
- `验证工具/运行L2通用特征实例参数验证.ps1`

临时项目独立于主工程和 solution，Debug / Release x64、C++20、Unicode、v145、`/utf-8`，输出和中间目录进入每轮系统临时目录。

## 3. 施工边界

本次只形成 F2 的空值通用特征实例新增、单项当前 / 历史读取与按存在当前枚举。未形成 F3 特征值、实例退出公开入口、生产装配、生产消费者、状态 / 动态 / 因果、世界树聚合、长期回归或独立集成验收。

本记录写成时未暂存、未提交、未推送；Git 发布不属于本执行段当前授权动作。
