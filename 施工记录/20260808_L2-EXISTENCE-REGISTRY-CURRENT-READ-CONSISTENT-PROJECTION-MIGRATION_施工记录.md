# 实际存在登记当前读回一致投影迁移施工记录

日期：2026-08-08

计划：`L2-EXISTENCE-REGISTRY-CURRENT-READ-CONSISTENT-PROJECTION-MIGRATION v0.1`

plan blob：`fa1c0f2abe918d0b9c6ba464072f05f1cfcca13a`

设计提交：`02b1b6b504f2490de3c5f6359e15981cc21248dc`

一致投影提供者结果：`a23dc8e5ad462832ac97245b5f9d8033b0ab3c85`

初始场景迁移结果：`24be53a3f868df5b1e35710e86aa6e13057124f9`

施工起点：`0dfd72d012d9a2cca4d753b4dd8bd76781fb97b5`

## 1. S0 与范围

- 开工时 `main == origin/main == 0dfd72d012d9a2cca4d753b4dd8bd76781fb97b5`，ahead / behind 为 `0 / 0`，Git index 为空；索引 blob 为 `52b4fd45ffb994d29e54b492c9aace6be77ab9e7`，本计划是唯一 `可执行` 项。
- 目标 `海中鱼巣/领域/服务.L1实际存在.ixx` 起始 blob 为 `435dba167e10538bee442f2effbab14b02112fd8`；实际存在 DTO、L1 中性一致当前 DTO / 服务、世界登记、生产运行期和运行期上下文 blob 均与详细设计冻结值一致。
- 实际存在合同版本 2、规则版本 1、十个稳定状态、目标私有函数签名、固定两节点登记写集、定位缓存和生产调用边未漂移。定位事实截止仍可落后于 L1 当前代次，目标根没有组合其它领域旧截止，因此首轮合法漂移后允许完整重试一次。
- 本轮只取得实际存在服务文件及两份专属记录的所有权。P0-FIX、路线、异主 `.codex-build`、两份 WORLD-TREE 和其它 WIP 原样保留；施工后复核其 tracked blob 与未跟踪文件 SHA256 均未变化。

## 2. 实际施工

### 2.1 两节点一致当前投影

把 `L1实际存在服务::按登记读取权威事实` 从事实代次首读、服务身份节点读取、资格属性类型节点读取和事实代次末读的多许可拼接，迁移为一个物理 `尝试读取中性一致当前投影` 调用点。

每轮请求固定形成：

- 期望代次取本轮期望事实代次，首轮唯一来源是定位的 `事实截止代次`；
- 节点按顺序恰为服务身份、实际存在资格属性类型；
- 关系、值、属性值、源关系组和目标关系组全部为空。

没有为证明 L1 其它形状附加值、属性、关系或关系组请求，也没有调用完整快照、历史、恢复或旧分散读取补齐结果。

### 2.2 有界完整重试与非成功分账

目标根只有一个 `最大尝试次数 = 2` 的有界 `for`，一致投影物理调用点恰一。新增三个登记专用纯值 helper，不修改继续供资格根使用的既有 `映射读取状态(...)`：

- 顶层非成功必须满足合同、期望代次回显和六组全空；漂移还必须回显非零且不同的实际代次，其它非成功必须保持读取代次为零；
- 只有首轮合法事实代次漂移会丢弃本轮材料、把回显实际代次作为第二轮期望并完整重做一次；第二轮漂移返回 `版本漂移`，不存在第三次调用；
- 许可拒绝立即返回 `许可拒绝`，资源失败保持 `资源失败`；入口拒绝、内部不一致、坏失败形状返回 `内部不一致`；
- 节点未找到映射 `未找到`；已退出、种类不匹配、属性未设置、成功缺事实和非成功带事实映射 `内部不一致`，内部类错误优先于另一项目的未找到。

全部非成功返回空登记，不补写、不换编码、不修改定位缓存、不等待、不 `sleep`。

### 2.3 成功业务形状

成功只接受合同、期望回显、非零共同读取代次和六组长度精确 `2/0/0/0/0/0` 的结果，再验证：

- 两项目顺序、查询编码和事实编码分别与定位服务身份、资格属性类型精确相等；
- 服务节点为普通、无属性类型表示；资格节点为属性类型且表示精确为 `I64`；
- 两节点均无当前属性、未退出，创建事实代次共同、非零且不晚于读取代次；
- 可选期望创建代次存在时，共同创建代次必须精确等于该值。

成功复制定位，只把返回副本的 `事实截止代次` 更新为共同读取代次。登记写入、定位缓存发布、`读取资格实现`、规格和同文件其它根均未修改；目标根外的旧读取继续合法保留。

## 3. 验证资源与结果

Debug / Release 的中间目录、可执行文件、构建日志、模块扫描 JSON 和运行捕获位于系统临时目录 `C:\Users\zhchh\AppData\Local\Temp\hzy-existence-registry-consistent-2a84b9170f4e4a13a63bd97b9f307512`，没有使用、删除或覆盖仓库内异主 `.codex-build`。

- Debug / Release x64 串行完整 `Rebuild` 均退出 0、0 编译错误；每个配置各有一条临时目录 `MSB8029` 提示。
- Debug / Release 模块扫描均为 `sources=136, modules=136, failed=0`，拓扑 136、循环 0、外部需求 0。
- Debug / Release 新构建程序的正式 `--runtime-context` 均在 30 秒时间盒内自行退出 0，stdout / stderr 均为 0 字节；没有以持续运行代替通过。
- 正式规范 strict 为 110 / 110；三白名单路径空白检查和目标根精确静态门禁通过。
- 运行前只读发现一个来自其它世界登记验收临时目录、早于本段启动且无 `--runtime-context` 参数的异主生产进程；本段未终止、修改或计入它，只对本段精确 PID 应用超时安全退出边界。

## 4. 完成边界

本施工最多证明实际存在登记这一具名 L2 当前读回根已按冻结合同迁移到中性非阻塞一致当前投影，并通过双配置构建、模块扫描、strict、静态门禁和正式生产入口运行门禁。

公开租约连续两次结构化登记 / 场景 / 世界读回未在本段独立执行。

生产退出码只证明正式入口没有拒绝该次启动，不证明实际存在登记消费者业务成功、消费者集成验收或第一层完善。不得据此声明真实许可竞争或二次漂移已经运行、值 / 属性 / 关系 / 关系组 / 修改 / 退出 / 历史、其它消费者或资格读取已经迁移，也不得重复冒充新增两节点创建证明。
