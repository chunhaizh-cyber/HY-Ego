# SELF-WORLD-TREE-ROOT-CONSUME-I2 v0.2 施工记录

日期：2026-08-13

## 1. 身份与 S0

- 计划：`SELF-WORLD-TREE-ROOT-CONSUME-I2 v0.2`
- 起点：`HEAD == origin/main == 5dde1c47bf149d125dfd671970f58e428bed724b`
- 计划 blob：`2d35c4faa6a7dd34bbe2351145d7ae0c5596c9ab`
- 设计提交：`7f34bc2cf3f74a88fba71649c75b8c37b025a6d1`
- 设计发布提交：`9ccc1b293c58f4705357ff7164b6af97d47cd7ab`
- 索引状态：施工前为 `可执行`；旧 v0.1 只作历史，未消费。
- S0：I1 六字段、A1 getter、R1 两读、阶段链、真实自我 provider 缺失、十二路径、index、异主 WIP 及构建 / 发布资源均与 v0.2 对齐，判定 `PASS`。

## 2. 实际施工

1. 新增公开值式 DTO `业务/自我世界树根消费.数据.h`，冻结合同版本、请求、九状态、两来源、材料和结果。
2. 新增普通应用模块所属 `业务/消费.自我世界树根.inl`：请求校验、异请求读前冲突、每次 A1 getter、R1 登记与整树两读、逐字段互证、值式发布、失败旧值守恒和异常分账。
3. 普通应用上下文只增加 I2 发布锁和值式材料槽；公开函数为消费薄 wrapper 与按值读回；未建立第二 provider 或适配器 owner。
4. 启动链在两种模式的宿主分支前执行 I1 -> 同次成功材料形成 I2 请求 -> I2；失败阶段新增 14、16、17，阶段 15 保持永久缺号。当前真实自我 provider 缺失，因此 I2 成功后固定阶段 17。
5. 根工程与 filters 各唯一以 `ClInclude` 登记 DTO 和 `.inl`；`.inl` 不形成独立 TU、BMI 或公开模块实体。
6. 三件临时专项位于生产工程外，脚本服务实例化同一 `.inl` 模板；生产文件未增加验证宏、故障选择、计数器、公开比较运算符或其它验证辅助。

## 3. 施工期修正

首轮生产 Debug fresh Rebuild 直接通过。临时专项首次编译暴露三类纯专项问题：误用未定义 DTO `operator==`、非合同测试便利辅助触发既有 variant 默认比较删除、跨 TU 调用 I1 私有比较名。处理均局限于计划范围：移除生产 `.inl` 中未使用的测试便利辅助，在临时专项内实现私有逐字段深比较并显式 include 已有启动数据头；未扩大 ABI、白名单或机器语义。

计划 v0.2 重新登记后复核发现，原 21 项记录虽然覆盖 I2 脚本读取非成功，却没有把 I1 / I2 正式 wrapper 的 provider 非成功分别具名，也没有逐项闭合两个 L2 结果各自成功但彼此漂移的全部矩阵。补验只修改临时专项源和两份永久记录：

1. 增加真实 I1 精确重复及同次根材料检查；
2. 增加 L2 自身对坏截止、坏完整性结果形状的拒绝，并保留既有坏 optional 检查；
3. 增加两个 L2 结果各自成功但树身份、根场景、根标记、事实截止或结构登记彼此漂移时的 I2 拒绝；
4. 增加正式 I1 / I2 wrapper 各自接收合同版本为零请求时的具名非成功动态，逐项断言非成功状态、`成功()==false` 和全部成功载荷为空；
5. 专项最终由 21 项扩展为 30 项，Debug / Release 均 30/30 PASS。新增 provider 非成功调用不经过 `运行海中鱼巣`，不得解释为阶段 14 / 16 真实入口动态。

补验没有修改生产代码、ABI、计划、索引、专项工程或脚本，也没有增加故障注入、验证宏、生产测试入口或替代构造链。三件临时专项继续原地保留，等待交互核验后的独立清理提交。

## 4. 最终验证

- 专项 Debug / Release：均 30/30 PASS，失败 0；`MSB8029` 仅提示隔离输出位于系统临时目录，没有形成构建、链接或运行失败。
- 根工程 Debug / Release x64 fresh Rebuild：均 PASS，分别生成 `x64/Debug/海中鱼巣.exe` 与 `x64/Release/海中鱼巣.exe`。
- `python .\tools\check_specs.py --strict`：113/113 PASS。
- 目标三路径严格 UTF-8、三个工程 XML、目标三路径 `git diff --check`：PASS。
- 静态合同：启动源码保持 I1 -> 阶段 14 短路 -> I2 -> 阶段 16 短路 -> 阶段 17；阶段 15 零定义零复用；I2 零 `建立场景树根`；生产源码零本次验证标记。

## 5. 完成边界

本施工只实现 I2 正式消费链和阶段 14 / 16 / 17 接线。阶段 14 / 16 只由启动源码静态接线 / 短路与对应 I1 / I2 provider 非成功动态组成分层证据；两者真实入口动态均为 `NOT_RUN`。阶段 17 由普通控制面板和无窗口两种模式真实入口动态证明。真实分配耗尽、真实自我形成、阶段 18、概念维度根、方法、自我线程、投影、宿主长时运行、恢复、跨进程与独立集成验收均为 `NOT_RUN`。临时三专项须在交互接受和计划退出后由独立清理提交删除；永久代码和本记录不得随专项清理删除。
