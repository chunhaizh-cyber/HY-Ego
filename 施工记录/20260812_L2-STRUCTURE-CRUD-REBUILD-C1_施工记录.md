# L2-STRUCTURE-CRUD-REBUILD-C1 施工记录

日期：2026-08-12

## 基线与范围

- 首次结果提交：`61907e04356292d88b2eed3b097097e982ae4387`。交互核验退回后，计划支撑以提交 `20e3c31c2e03be43f141d588c9c6092fe8d87c24` 发布 `L2-STRUCTURE-CRUD-REBUILD-C1` v0.3，plan blob `54e2e8b843379405765a90519bad1d43bcac5fc8`。
- 本轮起点：`f98b0039fe802ff3284308ebb8547c0436a021ad`，重新 S0 时 `HEAD == origin/main`、index 空；两个安全 WIP blob 与 v0.3 记录一致。
- S0：4230 v0.5、G2、状态身份来源 ABI、L1 owner CRUD、普通应用状态 0—19、v0.3 同仓夹具裁决与十路径白名单一致，PASS；C2 仍为 `待激活`。
- 异主流程图、路线输入和 `.codex-build/` WIP 原地保留，未暂存、覆盖或清理。

## 实施

- 新增因果 DTO 数据头：四类型登记、身份来源、因果事实、原子新增、完整读取和三类当前查询。
- 新增因果服务：专属 move-only owner、身份来源、四类型登记、owner-scoped 原子写集、当前/历史完整读取和条件/动作/结果查询。
- 普通应用在动态后追加唯一因果 provider、同实例 const/非 const getter，以及状态 17—19。
- 主工程只追加一个生产模块和一个数据头；专项三文件未进入主工程。
- 交互核验退回后修复三项生产偏差：L1 当前代次探测的资源失败不再折叠为内部不一致；当前完整读取把 owner、节点、锚点、归属类型 / 关系、三业务关系组和来源属性合并为一次 owner-aware 一致投影；历史身份来源增加读取前后当前代次复核，并从历史事实裁剪 owner / 节点 / 锚点 / 类型 / 关系的实际生命周期。
- 专项按 v0.3 在独立同一 L1 运行包中逐族签发并构造存在、特征、状态、因果服务；存在 / 特征只满足状态正式入口前置，四个状态最终只由 `读取状态身份来源(当前)` 证明。动作 / 来源只经同包 L1 公共中性写入口建立；cpp 无动态 import、owner、服务或调用。
- 没有实现 C2 替换/换代/退出、A1 聚合面或任何因果真实性、概率、模板、主动/被动判断。

## 施工期问题

1. 首次 Debug 编译暴露 L1 事实引用 variant 与属性投影的聚合类型不匹配；在因果服务内按现行 L1 ABI 修正，未改 L1。
2. `optional<稳定编码>` 不能由现行 `稳定编码` 的比较集合生成 MSVC 默认等号；在因果数据头内对三个含 optional DTO 提供逐字段等号，保持计划值式相等语义。
3. 专项第一次业务失败来自验证请求错误地刷新精确重复的首次期望；改回原请求后验证了 L1 幂等先于漂移的冻结语义。
4. v0.2 专项只有 11 个汇总断言，不能逐项证明计划矩阵；交互退回后按 v0.3 扩展为 21 条输出断言，覆盖 16 项必过矩阵。本轮不复用旧 11/11、旧构建或超时诊断作为通过证据。
5. 专项首次同时持有五个 owner 活动写端口后再构造服务，运行期卡在 owner 交付生命周期；改为逐族“签发、保存 owner、形成交付、立即构造服务”后消除等待。只改专项局部顺序，未改变生产锁语义。

## 完成边界

本记录只说明 C1 白名单内实际施工。构建和专项通过不等于生产消费者、独立集成验收、C2/A1、恢复、跨进程或因果业务真实性成立。
