# 实际存在 I64 实例特征当前值修改真实消费者施工记录

日期：2026-08-07

## 1. 施工身份与范围

- 正式起点：`eecca2f91cf5f7f0fc865915c7f403518c95754c`
- 计划身份：实际存在 I64 实例特征当前值修改真实消费者中性改退历史闭环
- 计划 blob：`ae783e0540840ee08e60c647778d21b5b53c70ca`
- 代码文件：`海中鱼巣/领域/服务.L1实例特征.ixx`
- 配套记录：本记录与同名构建、生产链验证记录

施工前 `main == origin/main`、ahead / behind 为 0 / 0，Git index 为空。目标三文件没有异主修改。P0-FIX、路线、旧 WORLD-TREE 和 `.codex-build` 现场原样保留，未修改、未暂存。

## 2. 实际变更

1. 为 `换代实际存在I64实例特征当前值` 增加唯一 `const` 正式实现；非 `const` 入口只向该实现转发一次。
2. 新增实际存在当前值换代专属中性写集形成函数。写集固定为零节点、零关系、一个新 I64 值、一次当前属性槽切换和一个旧值退出，幂等域保持 `0x07`。
3. 删除本目标路径的领域意图、幂等预探测、完整快照、旧写集提交、提供者见证、执行证据和首次结果缓存。
4. 将实际存在换代的内部状态收敛为只记录已进入本入口的幂等身份。首次提交未形成成功类结果时删除暂定定位；成功、精确重复或发布后读回失败时保留定位。
5. 首次候选按登记、起始代次、写前实际存在实例、特征定义、新来源节点和结束代次完成共同截止与业务准入；已定位请求不再读取已经退出的旧当前值。
6. 首次与重复都重新形成同一中性写集并恰一次调用 `提交中性写集`。同义和异义最终由第一层对完整中性写集裁决，不从服务缓存直接返回结果。
7. 成功类结果固定重新读取新当前实例、新值事实、旧值当前退出状态、旧值历史、当前属性槽和首末事实代次；新值创建代次和旧值退出代次必须等于首次发布代次。
8. 未迁移的初始场景当前值换代继续使用原有账、意图和旧提交路径，本轮没有改变其语义。

## 3. 完成边界

本次施工只形成实际存在实例特征当前值修改的中性代码候选。它不建立第一层自检、验收入口、双目标、通过状态或日志判定，也不证明第一层已经完善。

代码发布后仍须由生产代码之外的合法第二层消费者，经正式生产会话取得实例特征服务，调用该领域入口并核对结构化结果和公开权威读回。消费者验收通过后，最多累计本消费者实际覆盖的值新建、属性修改、旧值退出、当前不可读、历史可回查、精确重复、同键异义和非法请求零变化。当前不覆盖崩溃、重启、断电或跨进程恢复。
