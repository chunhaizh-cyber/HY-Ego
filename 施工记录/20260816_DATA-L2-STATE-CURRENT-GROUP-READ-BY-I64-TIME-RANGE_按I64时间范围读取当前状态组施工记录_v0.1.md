# 按 I64 时间范围读取当前状态组施工记录 v0.1

日期：2026-08-16

计划身份：`DATA-L2-STATE-CURRENT-GROUP-READ-BY-I64-TIME-RANGE`

## 1. 施工基线

- 正式开工 HEAD / `origin/main`：`3267b0a074a5ec8dcc99ca0d5a8ecdc062ae01d2`；
- 发布前增量基线 HEAD / `origin/main`：`e60b8225e71b0456977d336b3486f7fa49b4a16d`；
- 计划状态：`可执行`；
- plan blob：`5785b91b688eed85586b91febf97f806bfd519f0`；
- 设计提交：`375d0956384d220b4831a9a32c87b6cf9bcaea8d`；
- 详细设计 blob：`dcc1978c02e7d783921824cb41682539cfc1d22e`；
- L1 provider 结果提交：`b9e03f984516cd4577e18d868533ad1ea4f5f11a`；退出提交：`5f54d56bd970202ac09c1ce1b4da663f265fdf88`；
- 开工与发布前 index 均为空。BIZ 在施工期间独立发布文档并推进正式基线，增量 S0 已证明其提交未触及本计划两份生产文件；流程图、SELF、旧规范清理、最终需求清单和 `.codex-build` 等异主 WIP 原地保留。

## 2. 实际变更

1. `L2状态结构.数据.h` 新增 `L2按I64时间范围当前状态读取请求`及纯值有效 helper，复用既有 `L2当前状态组读取结果`；
2. `服务.L2状态结构.ixx` 新增唯一 const 入口 `按I64时间范围读取全部当前状态`，只消费 L1 owner + 属性类型当前值组公开入口；
3. 候选发现固定使用状态 owner 和正式时间 I64 属性类型；`最大候选数量`约束全部当前 I64 时间值，区间外候选也计入预算；
4. 服务对闭区间命中项执行独立返回预算检查，按状态身份严格升序后逐项调用既有完整状态读取，并互证时间值编码、I64 材料、类型、生命周期和共同事实截止；
5. 请求无效返回入口拒绝；L1 漂移、预算和资源失败分别保留分账；其它私有登记、候选坏形状、重复状态、混代或互证失败收敛内部不一致；所有非成功结果保持空载荷；
6. 时间 I64 组、时间 U64 组和独立材料引用不参与本入口；没有新增统一时间排序、缓存、第二索引、L1 私有访问或生产故障注入 seam。

## 3. 实际构建与检查

- 根工程 x64 Debug Rebuild：临时专项清理前、清理后均成功，0 警告、0 错误；
- 根工程 x64 Release Rebuild：临时专项清理前、清理后均成功，0 警告、0 错误；
- 临时生产外公开入口检查：Debug、Release 各 `19/19 PASS`；
- 检查覆盖坏请求、空组、闭区间两端和单点、同时间多状态、身份排序、其它三类时间材料不参与、候选与返回双预算、区间外候选计预算、陈旧守卫、时间换代、状态退出、逐字段互证、既有五类读取保持和读取零写；
- 临时 `.cpp`、`.vcxproj`、`.vcxproj.filters`、exe、pdb 及专属生成文件均已删除，未登记生产工程；仓库内仅可能残留不受 Git 跟踪且不含文件的空输出目录。

## 4. 未运行边界

- 真实资源耗尽：`NOT_RUN`；
- 私有登记、索引或状态损坏注入：`NOT_RUN`；
- 共享锁长期公平和极端并发调度：`NOT_RUN`；
- 跨进程恢复：`NOT_RUN`；
- 正式 BIZ 消费：`NOT_RUN`。

没有为上述分支增加生产 seam、friend、故障选择器或专门验收入口。

## 5. 完成边界

本记录只证明 `L2状态结构服务` 的显式 I64 时间值闭区间当前状态组 provider 在本计划范围内完成施工。它不建立四类时间材料统一排序，不证明动态时间范围、`DATA-EXT-04` 整项、`DATA-EXT-05—14`或数据服务最终需求清单整体完成；适用性由后继合法消费者实际使用时判断。
