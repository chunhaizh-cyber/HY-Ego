# 按 I64 时间范围读取当前动态组施工记录 v0.1

日期：2026-08-16

计划身份：`DATA-L2-DYNAMIC-CURRENT-GROUP-READ-BY-I64-TIME-RANGE`

## 1. 施工基线

- 正式开工 HEAD / `origin/main`：`4105f9b63f817871cae958f430838f9e905647fa`；
- 计划状态：`可执行`；
- plan blob：`c6038c1ba091e2c5036f0160ec4d463f93da7a96`；
- 设计提交：`6da9a0e602f89a1e6026a3ca5633e289f7d8fb37`；
- 详细设计 blob：`53d6aa9726def4c1fa76fb850bd0caa41f780ce0`；
- L1 属性类型当前值组 provider 结果提交：`b9e03f984516cd4577e18d868533ad1ea4f5f11a`；退出提交：`5f54d56bd970202ac09c1ce1b4da663f265fdf88`；
- 开工 index 为空。流程图、SELF、旧规范清理、最终需求清单、BIZ 候选和 `.codex-build` 等异主 WIP 原地保留，未暂存、覆盖或清理。

## 2. 实际变更

1. `L2动态结构.数据.h` 新增 `L2按I64时间范围当前动态读取请求`及纯值有效 helper，复用既有 `L2当前动态组读取结果`；
2. `服务.L2动态结构.ixx` 新增唯一 const 入口 `按I64时间范围读取全部当前动态`，只消费现行 L1 owner + 属性类型当前值组公开入口；
3. 候选发现固定使用动态 owner 与正式起始 / 结束 I64 属性类型，同一非零守卫分别读取两端，`最大候选数量`独立约束每一端全部当前 I64 值，区间外候选仍计预算；
4. 两端候选按动态身份配对，只用 `起始I64 <= 查询最大`且`结束I64 >= 查询最小`机械筛选；逆序动态不被拒绝、交换或解释，任一端为 I64 组、U64 组或独立材料引用时合法不参与；
5. 返回预算在完整读回前整组检查；命中项按动态身份严格升序，并逐项调用现有完整动态读取，互证值编码、类型、I64 材料、生命周期、来源和共同事实截止；
6. 入口无效、事实代次漂移、数量预算不足和资源失败保持具名分账；私有登记 / owner / 类型、候选坏形状、重复动态、混代或完整读回矛盾收敛内部不一致；所有非成功结果保持空组；
7. 没有新增 L1 接口、缓存、分页、第二索引、第二动态服务、普通应用或生产工程改动，也没有引入生产故障注入 seam。

## 3. 实际构建与检查

- 根工程 x64 Debug Rebuild：专项清理前与清理后均成功；
- 根工程 x64 Release Rebuild：专项清理前与清理后均成功；
- 临时生产外公开入口检查：Debug、Release 各 `23/23 PASS`；
- 检查覆盖入口拒绝、空组、闭区间两端 / 单点 / 左右交 / 互相包含 / 两侧不交、多个动态、身份排序、其它三类材料不参与、逆序动态机械判断、两端候选预算、区间外候选计预算、返回预算、陈旧守卫、起始 / 结束换代、动态退出、完整读回互证、既有六类读取保持和读取零写；
- 临时专项首轮工程清单漏登记不可变材料模块及 `bcrypt.lib`，后续已只修正临时工程；首轮运行还把逆序动态 `[20,10]` 对点查询 `[10,10]`错误预期为命中，按正式双不等式纠正为不命中。两项均未要求修改生产代码；
- 临时 `.cpp`、`.vcxproj`、`.vcxproj.filters`及专属生成文件均已删除，未登记生产工程；
- `python .\tools\check_specs.py --strict`：`113/113 PASS`；生产文件 diff、UTF-8、BOM、尾空白、ABI 和生产零专项痕迹检查通过。

## 4. 未运行边界

- 真实资源耗尽：`NOT_RUN`；
- 私有登记、索引或动态损坏注入：`NOT_RUN`；
- 共享锁长期公平与极端并发调度：`NOT_RUN`；
- 跨进程恢复：`NOT_RUN`；
- 正式 BIZ 消费：`NOT_RUN`。

没有用构建、静态检查或生产外专项替代这些证据，也没有建立专门验收入口。

## 5. 完成边界

本记录只证明 `L2动态结构服务` 的双端 I64 当前值闭区间相交读取在本计划范围内完成施工。它不建立其它起止材料类型的统一排序，不证明历史时间范围、`DATA-EXT-05`整项、`DATA-EXT-06—14`或数据服务最终需求清单整体完成；实际适用性由合法消费者使用时判断。
