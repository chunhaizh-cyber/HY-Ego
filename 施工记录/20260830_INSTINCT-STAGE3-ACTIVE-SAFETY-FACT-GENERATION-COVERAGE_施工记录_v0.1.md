# INSTINCT-STAGE3-ACTIVE-SAFETY-FACT-GENERATION-COVERAGE 施工记录 v0.1

日期：2026-08-30

## 1. 计划与起点

- 计划：`INSTINCT-STAGE3-ACTIVE-SAFETY-FACT-GENERATION-COVERAGE v0.1`。
- 计划 blob：`940d7cce26f160f98231f26d3f3a268560c140e9`。
- 施工起点：`16f44a5dcfad9cfd87de111aeea6bc57078a7390`，当时 `HEAD==origin/main`、ahead/behind `0/0`、index 空。
- 现有 SELF-GOVERNANCE、工程、线程和其它未提交 WIP 原地保留；本轮只写计划六个源码和两份专属记录。
- 隔离验证根：`D:/TEMP/海中鱼巣/INSTINCT-STAGE3-ACTIVE-SAFETY-FACT-GENERATION-COVERAGE/20260830-compile1`。候选由正式 HEAD 导出后只覆盖本计划六个源码，仓库其它 WIP 未进入构建。

## 2. 实施结果

1. `L2状态动态原子发布.数据.h` 增加事实代次范围请求、完整集合见证、固定状态和结果。
2. `服务.L2状态动态原子发布.ixx` 把 UTC 入口原有全量账读取及完整性验证抽成同一 module-private 路径；UTC 与事实代次入口共用该路径。新增入口按 `(排除起点,G0]` 筛选并按形成事实代次、账身份排序；起点早于登记代次返回覆盖边界不可用。
3. `本能被动维护基础快照.数据.h` 增加事实代次覆盖业务请求、裁决、快照、状态、结果和完整谓词。
4. `组合.本能被动维护基础快照.ixx` 增加同 G0 的基础快照、来源规则、事实代次完整组和第二次基础快照组合。规则版本 1 只有完整空组成功；非空组返回来源未裁定。
5. 两份现有专项增加 provider 与直接业务消费者矩阵；原 UTC 范围及 UTC 窗口用例继续运行。
6. 本轮没有发布、修改或推进维护游标，没有写 A/V、状态、动态或来源规则；没有建立 UTC 映射或非空主动安全来源算法。

固定值保持：值域 `0..INT64_MAX`；`L=2767011611056432742`；`H=7378697629483820645`；安全根定义版本 1、主动安全来源规则版本 1。阈值的“浮动”仍只表示未来可发布新定义版本，当前运行期不自动变化。

## 3. 函数级代码知识变更清单

### 3.1 新增生产函数

| 稳定函数身份 | 模块 / 文件 / 可见性 | 用途与合同 | 事实、副作用与调用边 | 用途卡动作 |
| --- | --- | --- | --- | --- |
| `bool L2特征当前值变化事实代次组读取结果_v2::成功() const noexcept` | `海中鱼巣.领域.服务.L2状态动态原子发布` / `服务.L2状态动态原子发布.ixx` / public | 验证已读取结果、见证、范围、成员顺序和同 G0 | 纯读取结果校验，无事实写入 | 新建 |
| `完整变化账扫描结果_v2 L2状态动态原子发布服务::读取并验证完整变化账_v2(const L2结构请求头&, L2特征实例身份) const noexcept` | 同上 / private | 统一执行 G0 读前守卫、登记、全量账、参与者首次写入和全集完整性校验 | 只读 L1/L2；被 UTC 与事实代次两个公开入口调用 | 新建，private 说明 |
| `L2特征当前值变化事实代次组读取结果_v2 L2状态动态原子发布服务::按特征实例与事实代次范围读取完整变化组_v2(const L2特征当前值变化事实代次组读取请求_v2&) const noexcept` | 同上 / public | 形成 `(排除起点,G0]` 完整 `0/N` 读回、预算、排序、见证和读后守卫 | 只读永久账；不推进游标、不做业务来源裁决 | 新建 |
| `bool 本能主动安全事实代次覆盖裁决快照完整_v1(const 本能主动安全事实代次覆盖裁决快照_v1&) noexcept` | `本能被动维护基础快照.数据.h` / inline public | 验证基础快照、规则 1 空来源、事实代次空覆盖及所有截止 | 纯值校验 | 新建 |
| `bool 本能主动安全事实代次覆盖裁决读取结果_v1::成功() const noexcept` | 同上 / inline public | 只接受已读取、完整快照和相同截止 | 纯值校验 | 新建 |
| `本能主动安全事实代次覆盖裁决读取状态_v1 本能主动安全事实代次覆盖裁决内部::映射基础状态(...) noexcept` | `海中鱼巣.领域.组合.本能被动维护基础快照` / private namespace | 基础快照错误到业务状态的结构化映射 | 无副作用 | 新建，private 说明 |
| `...::映射规则状态(...) noexcept` | 同上 | 来源规则错误到业务状态的结构化映射 | 无副作用 | 新建，private 说明 |
| `...::映射永久账状态(...) noexcept` | 同上 | 事实代次永久账错误到业务状态的结构化映射 | 无副作用 | 新建，private 说明 |
| `本能主动安全事实代次覆盖裁决组合器::本能主动安全事实代次覆盖裁决组合器(...) noexcept` | 同上 / public | 绑定既有基础快照、规则和永久账 provider | 保存只读引用，不取得 owner | 新建 |
| `本能主动安全事实代次覆盖裁决读取结果_v1 本能主动安全事实代次覆盖裁决组合器::读取事实代次覆盖主动安全变化裁决_v1(...) const noexcept` | 同上 / public | 两次基础快照之间同 G0 读规则和事实代次完整组；空组成功，非空组 fail-closed | 全程只读；不写 A/V、游标、规则或永久账 | 新建 |

### 3.2 直接修改的生产函数

| 稳定函数身份 | 变化 | 调用边与非职责 | 用途卡动作 |
| --- | --- | --- | --- |
| `L2状态动态原子发布服务::按特征实例与UTC范围读取完整变化组_v2(...) const noexcept` | 删除重复的全量账校验正文，改调同一 `读取并验证完整变化账_v2`；UTC 半开范围、排序、预算、见证和结果保持 | 新增 private helper 调用边；仍不做业务来源裁决 | 更新 |

### 3.3 专项函数

- `运行安全根定义与当前值端到端测试()` 增加 S12F、S13F、S15F，直接消费新 L2 入口。
- `运行本能被动维护基础快照端到端测试()` 增加 A05、A06，直接消费新业务组合器并保留 A00—A04 UTC 回归。
- 新增的测试 helper 只形成请求、校验空失败和重试当前性漂移，不进入生产模块。

以上用途卡在结果提交形成后由代码知识维护者按最终源码 blob 新建或更新；当前标记为待复核。结果提交：待发布后回填于完成回传。

## 4. 验证与诊断

- VS 18 / MSVC 14.52 首轮隔离 Debug 在正式基线既有 `服务.需求初次筹办准备.ixx:251` 触发 C1001；错误不在计划文件，未修改该异主模块。
- 改用既有正式验证工具链 VS2022 Professional、MSVC 14.44、`PlatformToolset=v143` 后，Debug / Release x64 根工程 Rebuild 均通过。
- Debug / Release 两份专项均实际运行 exit 0；Release 完成全量代码生成。
- Debug / Release 独立 ABI consumer 均以 `/GL` 编译、`/LTCG` 链接并运行 exit 0；consumer 同时 import L2 provider 与业务组合模块并静态核对两个公开成员函数完整 `const noexcept` 签名。
- `python .\tools\check_specs.py --strict`：116 / 116 PASS。
- 六源码 `git diff --check`：PASS；仅有行尾转换提示。

## 5. 完成边界

本结果只闭合事实代次永久账完整读回与当前规则 1 的空覆盖业务裁决。维护游标尚未保存该水位，主动安全非空来源、低位回升、完整被动维护事务、阶段三和 `INSTINCT-ROUTE` 均未因此完成。
