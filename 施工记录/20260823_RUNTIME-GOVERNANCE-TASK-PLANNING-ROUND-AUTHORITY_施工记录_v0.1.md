# RUNTIME-GOVERNANCE-TASK-PLANNING-ROUND-AUTHORITY 施工记录 v0.1

日期：2026-08-23

## 1. 施工身份与边界

- 计划：`RUNTIME-GOVERNANCE-TASK-PLANNING-ROUND-AUTHORITY v0.1`
- 正式计划 blob：`298d5dfdfc8f4c5c23f1d9a78ea6b0d814d0ce1d`
- 施工起点：`main == origin/main == d110c866fb8eb4d31b9d7baadc27c0ab017c267a`
- S0：`PASS`。计划索引状态为`可执行`，具名依赖为无，index 为空，目标键区间无碰撞。
- 所有权：只修改计划允许的五个源码 / 测试文件和本计划两份专属记录。工作区其它未跟踪材料未读取为正式依据、未修改、未纳入本切片。
- 未实施：初次筹办生产接线、legacy 迁移 / 退役、子目标回流 consumer、筹办执行、路径 / 实例创建生产者、结算和结果共享 / 竞争。

## 2. 实际变更

1. 在 task owner 中加入版本 1 筹办轮次权威、后继准备记录、任务当前筹办准备关系、强类型分区和当前 / 历史读取 DTO。
2. 新初次融合写集同代建立任务、首次记录、版本 1 权威和轮次 1 当前关系；旧首次结果同键重放保持 legacy、零补写。
3. `提交目标未达成待重筹办迁移`改为统一 / legacy 强类型联合：统一分支在原 owner 锁和同一提交内发布正式治理状态、N+1 后继记录并换代当前关系；legacy 分支保留原写集且零统一写。
4. BIZ 在首态前读取分区；统一工作包轮次只从后继记录读回，legacy 仍只在显式 legacy 分支使用`实例.筹办轮次 + 1`。
5. 增加同键精确重复、同键异义、同触发换准备键已消费和已推进统一任务绑定恢复；修复旧 G 同键重放漏核对统一推进材料的问题。
6. 任务退出同步退出统一当前关系与权威到任务 / 虚拟存在的强关系；权威登记和后继记录历史仍保留。
7. 专项增加统一轮次 1、legacy 分区、统一 N 到 N+1、后继 / 权威历史读回、精确重复、同键异义、触发已消费和 legacy 零统一写断言。

## 3. 施工期问题与收口

- 早期编译发现轮次定位声明顺序错误、分区恢复块误置于首态函数、测试局部变量重复 / 缺失；均在白名单内修复。
- 早期 Debug 专项先后暴露任务退出后的历史重放、V15 BIZ 在 DATA 幂等恢复前被当前关系门禁拒绝、V16 DATA 旧 G 重放漏核对推进材料；均按冻结合同修复。
- 最终以 round13 全新隔离输出完成双配置 Rebuild 和治理专项；此前失败轮次不作为最终通过证据。

## 4. 函数级代码知识变更清单

| 函数 / 身份 | 变化与用途 | 输入输出 / 事实读写 / 事务 | 直接调用与生命周期 / 非成功 | 验证证据与待更新用途卡 |
| --- | --- | --- | --- | --- |
| `L2任务筹办轮次权威记录事实完整`、`L2任务后继筹办准备记录事实完整`、`L2任务当前筹办准备事实完整` | 新增强类型事实完整性谓词 | 校验权威版本、同源身份、单调轮次、首次锚、前驱、触发、G 和生命周期 | 由 DTO 结果成功谓词和 DATA 读写使用；非法组合返回 false | round13 双配置编译 / 专项；发布后更新`L2任务结构.数据.h`用途卡 |
| `L2按任务读取筹办轮次分区请求有效`、各新增读取结果`成功` | 新增分区及权威 / 后继读取合同 | 约束当前 / 历史 G 和结果形状，不产生写入 | 由 task owner 公开读取入口消费；非法请求入口拒绝 | V04/V05/V21/V22；更新数据头用途卡 |
| `L2提交统一轮次推进材料完整`、`L2提交目标未达成待重筹办迁移请求有效`、迁移结果`成功` | 扩展原迁移联合合同 | 统一必须携带完整推进材料，legacy 必须为空；结果按分区约束后继记录 | BIZ 形成请求，DATA 机械分区；非法联合零写 | V13/V14 静态门禁、V15-V17 动态；更新数据头用途卡 |
| `初始化任务筹办轮次登记` | 新增 owner 物理定位初始化 | 登记权威 / 后继锚点、18 个关系类型、6 个属性类型和写入键 | 由`L2任务结构服务`构造调用；登记失败构造失败 | S0 键扫描、双配置 Rebuild；更新服务内部用途卡 |
| `形成初次筹办融合写集`、`读回初次筹办融合` | 修改初次融合为统一轮次 1 原子建立和 legacy 重放分账 | 新建路径同 G 写权威和当前关系；旧结果零补写；读回分区及权威 | `新增任务与首次初次筹办准备记录`调用；冲突 / 不完整读回内部错误 | V01/V02/V03边界/V04；更新 task owner 初次融合用途卡 |
| `读取筹办轮次权威历史投影`、`读取后继筹办准备历史投影`、`读取筹办轮次分区投影` | 新增内部历史投影和强类型 population 裁决 | 从 L1 节点、关系、属性重构不可变事实和当前链；0/0/0 才是 legacy | 由四个公开读取和迁移事务调用；复义、断链、未知版本内部不一致且不 fallback | V04-V06、V21/V22；更新 task owner 读取用途卡 |
| `按任务读取筹办轮次分区`、`读取筹办轮次权威记录`、`读取后继筹办准备记录`、`按后继筹办准备幂等身份读取记录` | 新增公开 DATA 读取入口 | 当前 / 历史只读，返回强类型分区、权威或后继记录 | BIZ 与专项调用；未找到、入口拒绝、资源失败分账 | V04/V05/V21/V22；更新公开入口用途卡 |
| `形成统一目标未达成迁移写集`、`提交目标未达成待重筹办迁移` | 新增统一 N 到 N+1 写集并修改原事务分区 | 同一 owner 写集退出旧治理 / 路径 / 实例 / 当前筹办关系，发布正式状态、后继记录和新当前关系；legacy 原写集不变 | BIZ 单一入口调用；同键异义冲突、同触发换键已消费、G / 分区漂移零写 | V07-V17、V19/V20静态边界、round13专项；更新迁移事务用途卡 |
| `退出任务` | 修改任务退出清理统一当前关系和权威强关系 | 退出任务 / 虚拟存在相关当前边；保留权威 / 后继节点和历史关系 | 原任务退出入口调用；复义内部不一致 | V22 历史读回及专项回归；更新退出用途卡 |
| `读回并形成工作包`、`读取并互证当前任务绑定`、`提交结构并形成工作包` | 修改 BIZ 分区和组包 | 统一轮次来自后继记录；legacy 保留实例加一；形成联合 DATA 请求 | 由公开重筹办 provider 调用；读回不一致返回内部错误 | V07/V10/V11/V15/V17；更新 BIZ provider 用途卡 |
| `读取并互证已推进统一任务绑定` | 新增已推进任务的幂等恢复材料重构 | 按当前权威 / 后继与原任务、路径、实例、实际结果重构原 N；不创建事实 | V15/V16/V17 重放路径调用；找不到同源触发时当前性漂移 / 引用冲突 | round13 双配置专项；更新 BIZ provider 内部函数用途卡 |
| `任务目标未达成重筹办触发提供者::形成待重筹办与下一筹办工作包` | 修改公开 BIZ 入口以接受当前路径或已推进恢复路径 | 保持一个公开入口；成功返回工作包，触发已消费只返回后继身份 | 任务管理线程既有 consumer 不变；不新增生产接线 | V11、V15-V17、生产链回归；更新公开 provider 用途卡 |
| `验证需求初次筹办工作包闭环`、`验证目标未达成迁移` | 修改治理专项 | 使用真实普通应用装配和公开入口覆盖统一 / legacy 分账 | `--test-治理`调用；断言失败返回专项失败 | round13 Debug / Release；更新测试模块用途卡 |

共享知识索引未由执行侧修改。上表中的用途卡均为发布后由能力证据目录 / 代码知识库维护侧消费的待更新项；目标代码基线以本计划最终结果提交为准。

## 5. 验证与完成边界

- 最终验证详见配套验证记录。
- 本施工结果只证明统一筹办轮次权威、实际结果未达成 N 到 N+1、legacy 保留和分区不 fallback 的代码 / 专项边界。
- 不证明初次筹办已生产接线、legacy 已迁移或退役、子目标回流已实现、筹办已经执行，也不构成完整业务闭环或集成验收。
