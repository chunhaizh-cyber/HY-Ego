# 二次特征调用期 I64 计算与职责迁移施工记录

日期：2026-09-08（Asia/Shanghai）
计划身份：`SECONDARY-FEATURE-CALLTIME-I64-CUTOVER`
执行所有者：`/root/execute_feature_node_i64`
实施依据：计划与详细设计 v0.1—v0.6 合读，冲突以 v0.6 为准。
最终 S0 / 隔离基线：`29e3e11d350c3dd1390ecb741038c93a4ec9f264`（`main == origin/main`）。
计划 v0.6 blob：`0a7959484f669b49f82bf0d29e80837d20e81b2a`。
详细设计 v0.6 blob：`d079e163a53679deb801dc71fe93dde95d0306bb`。

本记录只记实现和证据，不定义机器语义或计划状态。代码结果提交是首次同时加入本记录与下表候选源码的提交；发布前待形成，发布后可用 `git log --diff-filter=A --format=%H -- 施工记录/20260908_SECONDARY-FEATURE-CALLTIME-I64-CUTOVER_二次特征调用期I64计算与职责迁移施工记录_v0.1.md` 精确定位。提交/推送事实另由发布回执给出。

## 1. S0 与范围

- 索引登记 v0.6 可执行；各次正式支撑修订发布后重新 S0，没有依据聊天自行增加字段、持久结构或 owner。
- 13 项文件占用登记在 `D:/TEMP/再用文件/再用文件列表.md` 的 `/root` 模块，实际所有者为本执行者；发布前不提前释放。
- 本片是四新模块、四个现有 DATA 模块、旧二次 DATA 门面删除、两工程文件和两份专属记录。未修改规范、设计、计划、索引、知识库、L1、特征值、存在、概念、线程、入口、装配、README、数据库或既有测试。
- AGENTS、自我治理材料、历史记录删除、未跟踪旧源码副本和其它 WIP 原地保留，不纳入隔离输入或提交。
- 复用裁决：复用现行 L1 读取/写入/首次材料恢复、特征及特征值准确读取、需求/任务数据记录和存在当前采用；按冻结合同新建 provider/计算/方向应用。知识聚合基线较旧，目标能力以当前源码及生产调用点复核，不从知识条目或旧文件名推定新 ABI 已存在。

## 2. 实际实现

| 产物 | 实际变化 |
| --- | --- |
| 算法.有序I64特征比较 | 纯值比较 provider，有序比较、无符号幅度容差、右减左与 I64 溢出检查；任一要求结果失败清全部输出；状态5仅历史枚举洞，无许可判定分支。 |
| 计算.二次特征 | 三来源、两类根、统一 DAG；一次批量预取所有直接保存定义根，共享 saved/leaves/current 缓存；六预算、悬空/环/每路径深度、固定 K 相容、完整来源回执、拓扑计算及多根原子返回。 |
| 应用服务.需求目标方向 | 从需求、宿主、当前/目标 F、成员、唯一当前采用和方向定义取得依据；逐结果位升序建根，核验左右闭包；原请求回显、失败清成功载荷。 |
| 应用服务.任务目标方向 | 读取任务和来源需求、互证所属存在，委托需求方向。按 v0.5，任务尾 G 漂移令顶层失败，保留完整方向子结果作不可消费诊断。 |
| 数据服务.特征类 | 独立四身份 I64 绑定初始化、建立/身份读/当前读/退出、原请求幂等与未知发布恢复；有界准确读、真实材料/分类计量、单根与批量完整闭包；退出旧比较、缓存和复算。 |
| 数据服务.需求类/任务类/方法类 | 移除旧计算/方向职责及二次门面依赖，保留数据记录、来源需求、任务虚拟存在和旧 v1 治理；新增只读同 L1 绑定核对，需求/方法直接读特征 owner 定义。 |
| 数据服务.二次特征类 | 整文件物理删除，无兼容转发；可从 Git 原 blob 恢复。 |
| vcxproj / filters | 四新一删双登记一致；44 ClCompile（43 .ixx + 入口.cpp），无缺失或重复。XML行序不是编译拓扑；真实 import 依赖和双配置 Rebuild 已核对，不把列表排列顺序作为生产调用证明。 |

关键保持项：旧初始化 `{1}` / 16 角色写集与 d_ 下标不变，扩展键 `0x4645'4154'4936'3442ULL` 独立。引用值通过 `values_.获取特征值` 完整核验，同编码重复核验不重复计费。材料总数包含结构角色、FT、单位、规则等全部真实 L1 事实，允许大于派生四分类和。定义基本准入后计定义、准确 F 完整准入后计叶；失败保留真实用量。跨键同定义幂等冲突、异定义注册不唯一；竞争失败方恰一次最新 G 重扫，不换键/换G重提交。

## 3. 源码身份

| 路径 | 基线 blob | 最终候选 blob |
| --- | --- | --- |
| `海中鱼巣/领域/算法.有序I64特征比较.ixx` | `新增` | `9cb2d1aaf26fc7c46462ff097ba1a9705c6e4e7f` |
| `海中鱼巣/业务/计算.二次特征.ixx` | `新增` | `72a8291c8bceb4c0d681555693bf29b25290680e` |
| `海中鱼巣/业务/应用服务.需求目标方向.ixx` | `新增` | `60f5e421b54978189f41d454d418b96fd87203df` |
| `海中鱼巣/业务/应用服务.任务目标方向.ixx` | `新增` | `d902f760c9afccaf2f0116167299d2585a9ad636` |
| `海中鱼巣/领域/数据服务.特征类.ixx` | `a54e424ffd4719e1490cbe2e17912b0d3c662135` | `515884d06f1be6c8481ded94b65d48ca5bb723ca` |
| `海中鱼巣/领域/数据服务.需求类.ixx` | `1df64f7235b73c317f7307b95c14a3c998b1f6c1` | `40fb93b4a4c610f591cbf7e16636c84ed278aae4` |
| `海中鱼巣/领域/数据服务.任务类.ixx` | `203fa94fb62fd89cee969d65cc09fb26bc601571` | `788dfdc7f607053788a2867b55623463006f5ba6` |
| `海中鱼巣/领域/数据服务.方法类.ixx` | `daa002e3cf7916a4cdf84b83d579802a90d71eb7` | `5c5830b4a5b5a45eadb7dfd672ef9ccad4464302` |
| `海中鱼巣.vcxproj` | `f727f735d002a53b9740042e4f92a52405c75843` | `3190a6cf0f5170df39ca31ce0069ad3470f77fd0` |
| `海中鱼巣.vcxproj.filters` | `a8b6a3d5745141b717ba04d5de0c3546a702c776` | `2346951d4895d2ef51ead297d529d7aafd512239` |
| `海中鱼巣/领域/数据服务.二次特征类.ixx` | `f745c2e41dfea340a58bbd9a3d896e816caacf6d` | 物理删除 |

最终隔离输入与 10 个现存候选文件 SHA256 逐一相同；编译后未再修改生产字节。SHA256 详见编译记录。Git 行尾规范化提示不属于编译警告。

## 4. 代码知识变更清单

以下身份均在 `海中鱼巣` 命名空间；完整身份由所属类/namespace、签名、路径和上节 blob 定位。export/public 除非另标 private。用途卡动作：新增身份新建；直接修改身份更新；删除身份标记退出；本记录不写共享索引。DTO 的 default equality 是值比较，无事实读写。

### 4.1 新计算与方向函数

| 稳定完整签名 | 用途、合同、边界与调用边 |
| --- | --- |
| `特征比较执行结果 有序I64特征比较提供者::比较(const 特征比较执行请求&) const noexcept` | 领域/算法.有序I64特征比较.ixx。只消费 K 快照与左右 I64；输出要求位对应结果。范围、量化、溢出或资源失败无部分输出，无 L1/owner/写入；被计算应用调用。 |
| `二次准确计算结果 二次特征计算应用服务::计算(const 二次计算请求&) const noexcept` | 业务/计算.二次特征.ixx。来源为原请求 G/H、DAG、根、上下文和六预算；调用特征批量定义读取、有界准确 F、当前绑定、L1 G 守卫及 provider。缓存只活于调用内，不持久化；失败清根/叶/图/结果四组。 |
| `需求目标方向结果 需求目标方向应用服务::读取并计算(const 需求目标方向请求&) const noexcept` | 业务/应用服务.需求目标方向.ixx。调用需求记录、存在宿主/当前采用、特征 F/定义与计算应用；不写需求；宿主关系在入口核验，成功成员只互证包内数据。 |
| `任务目标方向结果 任务目标方向应用服务::读取并计算(const 任务目标方向请求&) const noexcept` | 业务/应用服务.任务目标方向.ixx。调用任务、来源需求和需求方向，不写任务或复制比较核心；调用前失败无子结果，调用后失败保留完整子结果但顶层不可消费。 |
| `二次特征计算应用服务::二次特征计算应用服务(const L1事实基座服务&, const 特征类数据服务&, const 有序I64特征比较提供者&)` | 持有引用，构造校验同 L1，异源抛 invalid_argument；不创建事实。调用者负责依赖对象寿命。 |
| `需求目标方向应用服务::需求目标方向应用服务(const L1事实基座服务&, const 需求类数据服务&, const 存在类数据服务&, const 特征类数据服务&, const 二次特征计算应用服务&)` | 同上，全部 DATA/计算必须绑定同 L1。 |
| `任务目标方向应用服务::任务目标方向应用服务(const L1事实基座服务&, const 任务类数据服务&, const 需求类数据服务&, const 需求目标方向应用服务&)` | 同上，任务/需求/方向必须绑定同 L1。 |
| 三应用各自 `bool 绑定于(const L1事实基座服务&) const noexcept` | 只校验引用及下游绑定，无事实写入。 |
| `bool 特征比较执行结果::成功() const noexcept`；`bool 二次准确计算结果::成功() const noexcept`；`bool 需求目标方向结果::成功() const noexcept`；`bool 任务目标方向结果::成功() const noexcept` | 状态、来源、K、准确值、量化、上下文、输出和原请求自包含互证；无再次 L1 读取。需求分别验左当前 F 和右目标 F；任务互证派生请求。 |
| `bool 二次计算预算::有效() const noexcept` | 六上限非零；不代表实际读取通过。 |

新模块名为各路径对应的 `海中鱼巣.领域.算法.有序I64特征比较`、`海中鱼巣.业务.计算.二次特征`、`海中鱼巣.业务.应用服务.需求目标方向`、`海中鱼巣.业务.应用服务.任务目标方向`。

### 4.2 特征 DATA 函数

文件 `海中鱼巣/领域/数据服务.特征类.ixx`，模块 `海中鱼巣.领域.数据服务.特征类`；`R<T>` 为 `特征数据结果<T>`。

| 稳定完整签名 | 变化及调用、事务、生命周期 |
| --- | --- |
| `特征I64比较绑定结果 特征类数据服务::建立I64比较绑定(const 特征I64比较绑定建立请求&)` | 新增，委托绑定执行写；仅原键/G/定义/写集重放精确重复。正式首次回执与活动读回互证，未知发布保留完整 pending；跨键不借回执、不新建幂等账。 |
| `特征I64比较绑定结果 特征类数据服务::退出I64比较绑定(const 特征I64比较绑定退出请求&)` | 新增，只退出 binding 自有事实，不退输入/输出 FT，外部引用受 L1 保护。 |
| `特征I64比较绑定结果 特征类数据服务::读取I64比较绑定(const 特征I64比较绑定读取请求&) const` | 新增，Gread/H 下按身份完整读取及活动性检查，首尾 G 守卫；异常结构化映射。 |
| `特征I64比较绑定结果 特征类数据服务::读取当前I64比较绑定(const 特征I64当前比较绑定读取请求&) const` | 新增，同 FT+用途唯一查询，扫描候选/匹配数分账；全部实际扫描材料计费，零/多/预算/格式失败无事实载荷。 |
| `有界准确特征读取结果 特征类数据服务::读取有界准确特征事实(const 有界准确特征读取请求&) const noexcept` | 新增，真实节点/关系/属性值/材料上限，经既有准确与特征值合同核验；旧公开准确读取 ABI 不变。 |
| `特征类标量派生批量读取结果 特征类数据服务::批量读取标量派生定义(const 特征类标量派生批量读取请求&) const` | 新增，一 mutex/共享上下文、精确左右闭包/完整定义/联合叶/真实用量；任一根失败原子清载荷，无逐根回退。 |
| `特征类标量派生读取结果 特征类数据服务::读取标量派生定义(const 特征类标量派生读取请求&) const` | 修改，签名不变；完整定义与左右闭包、缓存、真实总材料计量；预算逐次准入，成功谓词只包内互证。 |
| `R<std::monostate> 特征类数据服务::初始化特征定义结构()` | 修改，旧初始化后独立扩展，原 16 角色写集不变。 |
| `特征类标量派生写结果 特征类数据服务::建立标量派生定义(const 特征类标量派生建立请求&)`及 private `template<class R> 特征类标量派生写结果 标量执行写(const R&)` | 修改委托实现：建立只做来源/结构/域准入，不再实际比较/缓存/复算；既有保存定义写入及旧治理保留。 |
| `bool 有效(特征I64比较绑定身份) noexcept`；`bool I64绑定定义完整(const 特征I64比较绑定定义&) noexcept`；`bool I64绑定事实完整(const 特征I64比较绑定事实&, std::uint64_t) noexcept` | 新增值谓词，检查身份、用途/角色/量化/关系编码、输出与活动性，无 L1。 |
| `bool 特征I64比较绑定结果::成功() const noexcept`；`bool 有界准确特征读取预算::有效() const noexcept`；`bool 有界准确特征读取结果::成功() const noexcept`；`bool 特征类标量派生批量读取结果::成功() const noexcept` | 新增包内互证：原请求/首次回执/编码映射，真实计量/预算，完整闭包。 |
| `bool 特征类标量派生读取结果::成功() const noexcept` | 修改，全定义可达、左右叶精确闭包、分类与总量互证，不从缺失原请求推测预算。 |

private 新增 `void 初始化I64扩展()`、`template<class BindingRequest> 特征I64比较绑定结果 绑定执行写(const BindingRequest&)`及绑定编解码、原写集、首次材料恢复 helper，只为四入口提供同一 owner 下的写入/读回。未知发布 pending 门禁纳入其它既有写入口；冲突请求不能移除异请求 pending。

private 准确读取签名增量（N/E/V为类内L1节点/关系/值别名）：

```cpp
L1所有者范围事实副本 原始事实(稳定编码, std::uint64_t, 读取计量*) const;
N 节点(稳定编码, std::uint64_t, std::uint64_t, std::optional<分区>, 读取计量*) const;
std::vector<E> 关系(稳定编码, 稳定编码, bool, std::uint64_t, std::uint64_t, 分区, 读取计量*) const;
std::vector<V> 属性(稳定编码, std::uint64_t, std::uint64_t, 分区, 读取计量*) const;
E 唯一关系(稳定编码, 稳定编码, std::uint64_t, std::uint64_t, 分区, 读取计量*) const;
void 结构就绪(分区, std::uint64_t, std::uint64_t, 读取计量*) const;
E 核对归属(稳定编码, std::uint64_t, std::uint64_t, 分区, 读取计量*) const;
先天I64特征类型信息 读类型(特征类型身份, std::uint64_t, std::uint64_t, 标量读取上下文*, 读取计量*) const;
准确特征读取事实 读准确(特征信息身份, std::uint64_t, std::uint64_t, 标量读取上下文*, 读取计量*) const;
std::int64_t 解析输入(const 特征准确值&, std::uint64_t, std::uint64_t, 读取计量*) const;
```

新增指针参数有默认空值，旧调用保持有效；共享本次事实/关系组/属性组缓存，先映射 L1 失败再查 G；读后格式失败保留计数。引用值保留对 `values_.获取特征值` 的完整核验，不用 raw 数字取代正式值合同。

计算私有辅助知识以 `二次计算内部` 的键/身份/来源/输出键/输出角色、角色位/上下文位/上下文有效/来源有效、整数/准确来源完整/快照完整/同量化/快照/固定相容定位，分别处理请求级节点、来源角色、准确值及 K/量化互证；均是值函数，完整签名以本轮源码 blob 制卡。三应用的 private 守卫/状态映射只负责 G 读取和错误转译，不产生新持久入口。

### 4.3 消费者与退出身份

| 稳定身份 | 修改和用途卡动作 |
| --- | --- |
| 需求/任务/方法 DATA 各自 `bool 绑定于(const L1事实基座服务&) const noexcept` | 新增只读地址互证，文件/模块为各自 DATA。 |
| 需求/方法 DATA 的依赖注入构造函数 | 删除 `const 二次特征类数据服务&` 参数/成员，其他参数保持顺序；构造合同用途卡按当前 blob 更新。 |
| 需求 private `查询方向定义`；方法 private `std::optional<方法类数据状态> 验证派生定义(特征类定义身份, std::uint64_t, bool) const` | 直接被调者改为特征 owner 的 `读取标量派生定义`；旧 `验证二次特征` 身份退出，旧 v1 治理回退保留。 |
| `特征类标量比较结果 特征类数据服务::比较派生特征(const 特征类标量比较请求&) const` | 退出；旧比较输入/输出回执、DTO、缓存/复算、专属差异 helper 同退。 |
| `需求类标量方向读取结果 需求类数据服务::计算临时需求方向(const 需求类标量方向读取请求&) const` | 退出；旧方向 DTO、比较互证/失败 helper 同退，数据记录不删。 |
| `任务类标量目标方向读取结果 任务类数据服务::读取任务目标方向(const 任务类标量目标方向读取请求&) const` | 退出；旧方向 DTO/失败 helper 同退，任务虚拟存在保留。 |
| `特征类标量派生写结果 二次特征类数据服务::生成二次特征节点(const 二次标量特征生成请求&)`；`特征类标量派生写结果 二次特征类数据服务::退出二次特征节点(const 特征类标量派生退出请求&)` | 随门面整文件退出；保存定义写入仍由特征 owner 提供。 |
| `特征类标量派生读取结果 二次特征类数据服务::查询二次特征节点(const 特征类标量派生读取请求&) const`；`特征类标量比较结果 二次特征类数据服务::计算二次特征(const 二次标量特征计算请求&) const`；`二次标量特征概念材料结果 二次特征类数据服务::读取二次特征概念材料(const 特征类标量派生读取请求&) const` | 随门面整文件退出，构造、包装写、当前G等私有实现和 DTO 同退，无兼容转发。 |

依据为计划/设计 v0.1—v0.6，证据为源码 blob、下节静态审查和编译。新应用没有接到程序入口；知识共享回写仍待维护者从结果提交处理，不声明知识全量闭合。

## 5. 验证及未验证边界

[编译记录](../验证记录/20260908_SECONDARY-FEATURE-CALLTIME-I64-CUTOVER_二次特征调用期I64计算与职责迁移编译记录_v0.1.md)给出 V01—V47 分账、命令、最终日志及哈希。

Debug x64 Rebuild：exit 0、0 警告、0 错误、31.04秒；Release x64 Rebuild：exit 0、0 警告、0 错误、27.72秒。主仓 strict：108 份目录项全部通过。精确本片 diff-check 通过；全仓 diff-check 的异主 AGENTS.md:12 尾空格未修改。

独立只读子审计最终对两目标 blob 及 11 项生产/工程范围核对通过；语义审查中已修复共享结果位绕过、来源/K漏检、局部图预算漏计、空当前采用映射、左右闭包、读取缓存/错误映射、pending与首次回执重试边界等本片问题。无尚未裁决的确定代码问题。

没有运行程序、单元/端到端测试、数据库、故障注入、跨进程恢复、真实外设、集成验收或线程业务链。竞争、pending恢复和预算场景只做静态分支核对，不能记作运行通过。最多声明代码、旧 ABI 静态迁移和双配置编译链接；不声明 RC、持久二次实例、生产可达或整体业务闭环。发布及计划退出由父交互智能体统筹，执行者不改索引。
