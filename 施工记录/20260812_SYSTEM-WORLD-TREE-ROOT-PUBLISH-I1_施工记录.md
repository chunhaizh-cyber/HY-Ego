# SYSTEM-WORLD-TREE-ROOT-PUBLISH-I1 施工记录

日期：2026-08-13

## 施工身份

- 现行计划：`SYSTEM-WORLD-TREE-ROOT-PUBLISH-I1 v0.6`
- 最终重新 S0 基线：`2355bc40119cc3beff21381926437d3bf97929a6`
- 计划 blob：`e8b260b62e5221291afdf5b18e76506cb92e01ba`
- v0.6 详细设计 blob：`e81412d046c813692ed6473a935f256094db24f4`
- 状态：结果候选；本记录不修改计划索引状态。

## 实际施工

1. 新增 `海中鱼巣/业务/初始化.系统世界树根.inl`，作为既有 `海中鱼巣.装配.普通应用` 模块所属模板实现片段。完整状态机和逐字段私有辅助只保留在该单源正文；文件零 `module`、`export`、`#include`、验证逻辑、测试宏、脚本、故障选择和计数器。
2. `海中鱼巣/装配.普通应用.ixx` 在 DTO 导出后唯一包含该 `.inl`；正式 wrapper 只经 A1 getter 取得同实例场景服务引用，并一次委托同源模板。公开 DTO、函数签名、状态枚举和 ABI 未改。
3. 根工程与 filters 各以唯一 `ClInclude` 登记 `.inl`，未把它登记为 `ClCompile`、模块、分区或独立翻译单元。
4. 生产外专项 `.cpp` 在标准库依赖后依次直接 import L2 公共合同、L2 场景服务和普通应用，再唯一包含同一 `.inl`；脚本 provider、调用计数、异常与坏样本只存在于专项翻译单元。生产与专项是不同 TU 中的不同模板实体，不宣称同一二进制实例。
5. 保留真实普通应用旧 10 项路径，并增加七组共 55 个动态用例，逐例输出输入脚本、调用序列、L2 成功先验、I1 状态、六 optional 格和两状态槽前后。
6. v0.6 的 G5B-12 证明既有发布材料的独立截止字段原先可被本轮读回纠正后成功。按正式裁决，在 `.inl` 既有发布互证分支补精确等式：旧发布独立截止同时等于旧正式树截止、本轮二次登记结果截止和本轮整树读取结果截止；坏旧值现返回 `根读回不一致` 且旧发布逐字段守恒。
7. 更新本施工记录和对应验证记录；三件专项按现行顺序随结果提交保留，等待交互接受、索引退出后的独立清理提交删除。永久 `.inl` 与生产修复不清理。

## 漂移处置

- v0.3 传统 `.h` 物理方案因文件分类规则失效，v0.4 改为普通应用模块所属 `.inl` 单源模板。
- v0.4 的“非成功枚举却令 L2 `成功()==true`”不可构造；v0.5 改为四阶段“非成功枚举 + 完整成功载荷 -> L2 成功 false -> I1 按阶段拒绝”。
- v0.5 又把根父关系等 L2 自身不可完整的坏样本宽泛归为 I1 跨结果命中；v0.6 将组 5B 逐项分为 L2 先拒绝、I1 跨结果互证和静态源码防御冗余，并授权修复 G5B-12 私有截止互证。
- 每次正式修订后均从新 HEAD、索引、plan/design blob 和保留 WIP 重新 S0；未修改计划、设计、规范、流程图、I2 或异主 WIP。

## 四层证据与完成边界

1. 专项 seam specialization：驱动七组 55 项分支、顺序、optional 和状态槽变化，只证明同一 `.inl` 源码在专项脚本结果下的动态行为。
2. 真实 R1 / 普通应用：证明首次、已发布重复、同请求并发、来源、值式 getter 与直接 getter/A1 getter 同址；不把专项脚本结果升级为真实资源耗尽。
3. 静态源码：证明 wrapper 只取 A1 引用并一次委托、A1 聚合服务零写、固定 I1 身份与四个保留身份全异、生产验证痕迹零命中。
4. 构建 / 工程：根工程和专项 Debug/Release fresh 构建、链接、运行通过；只证明两套完整依赖图可构建运行。

本候选最多证明 I1 provider 可经 A1 同实例 R1 建立或收敛根，并在正式登记和整树读回互证后发布值式材料。它不证明正式启动可达、阶段 14—18、I2、自我、概念维度根、恢复、跨进程、长时生产或独立集成验收。
