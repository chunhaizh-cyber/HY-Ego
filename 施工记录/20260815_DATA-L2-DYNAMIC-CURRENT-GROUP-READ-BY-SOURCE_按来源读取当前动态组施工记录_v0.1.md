# 按来源读取当前动态组施工记录 v0.1

日期：2026-08-15

计划身份：`DATA-L2-DYNAMIC-CURRENT-GROUP-READ-BY-SOURCE`

## 1. 施工基线

- 起点 HEAD / `origin/main`：`fe519668f861f6a4ba80d57c60bcfe4ac744c9dd`；
- 计划状态：`可执行`；
- plan blob：`6432a7df44ab810d436412e8148143b74f7fbdb6`；
- 设计提交：`9c6e9b2620c3bef5e6bd3aabf5273c4644dbcd74`；
- 详细设计 blob：`13e3906943ae70b9ede048789b275ff4e5280156`；
- L1 provider 结果提交：`8e0928671d3584d399ce4d796b890401073e5676`；
- 共同预算状态结果提交：`86d10082dcd8fa3ae2643ad14db19921d98da1ff`；
- 起点 index 为空，两个生产目标文件 clean；流程图、EVENT、SELF、规范、路线、概念候选、生产工程和 `.codex-build` 等异主 WIP 原地保留。
- 发布前正式 HEAD 前进到 `eed062c23f12f9bc4d72b4d4b67b320b12201734`；该提交只收敛语言 / 词条相关规范与最终清单编号，对本叶两个生产文件、L1 来源入口、计划索引行和动态来源语义零重叠，故在新基线上继续发布。

## 2. 实际变更

1. `L2动态结构.数据.h` 新增 `L2按来源当前动态读取请求` 与纯值有效 helper，复用既有 `L2当前动态组读取结果`；
2. `服务.L2动态结构.ixx` 新增唯一 const 入口 `按来源读取全部当前动态`，只消费 L1 owner-aware 来源当前值组入口；
3. 内部把公共动态预算饱和换算为 `2 * (最大数量 + 1)` 的 L1 值预算，逐项校验动态 owner、来源、起始 / 结束时间值角色、生命周期和共同截止；
4. 完整结果按动态身份稳定编码严格升序，空组成功；公共动态数超预算时复用状态 15 返回零载荷、零变更代次；
5. 没有扫描全仓、增加缓存、解释业务来源、比较时间范围、修改共同枚举或生产工程，也没有建立验证 seam。

## 3. 实际构建与检查

- 根工程 x64 Debug Rebuild：清理前、清理后均成功，退出码 0；
- 根工程 x64 Release Rebuild：清理前、清理后均成功，退出码 0；
- 临时生产外公开入口检查：Debug、Release 各 `13/13 PASS`；
- 专项覆盖成功空组、同来源多动态、不同来源隔离、严格升序和共同截止、精确 / 少一预算、坏请求、来源未找到 / 已退出、陈旧守卫、动态退出及历史保留、既有主体 / 前状态 / 后状态查询和读取零写；
- 临时检查首次编译暴露其仍使用旧特征定义测试调用，已只在临时源码内机械匹配当前 `新增统一特征定义` ABI；生产实现没有因此改向；
- 临时 `.cpp`、`.vcxproj`、exe、pdb 和专属中间文件已经删除，未登记生产工程。

## 4. 未运行边界

- 真实分配 / 长度耗尽：`NOT_RUN`；
- 私有 owner、索引或动态损坏注入：`NOT_RUN`；
- 共享锁长期公平与极端并发调度：`NOT_RUN`；
- 跨进程恢复：`NOT_RUN`；
- 时间范围查询：`NOT_RUN`；
- 正式 BIZ 消费：`NOT_RUN`。

没有为这些分支增加生产 seam、friend、故障选择器或专门验收入口。

## 5. 完成边界

本记录只证明 `L2动态结构服务` 的按来源当前动态组 provider 在本计划范围内完成施工。它不证明动态时间范围查询、因果来源查询、DATA-EXT-05 整项或数据服务最终需求清单整体完成；适用性由后继合法消费者实际使用时判断。
