# LEGACY-DATA-L2-PLUS-RETIREMENT 旧基础数据服务 L2 以上迁移删除施工记录 v0.1

日期：2026-09-06

施工身份：`LEGACY-DATA-L2-PLUS-RETIREMENT`

结果状态：候选删除源码、工程工作树视图与精确 index 双视图已完成，等待发布复核后提交推送

## 1. 正式输入与 S0

- 实际施工起点：`main == origin/main == cd5716a3b5ad31cf0370991d2677481f287e04f0`，ahead/behind `0/0`，开工时 index 为空；
- 详细设计 blob：`623e99bfe5c7641f4552c5cf8f8b86cfc8d9ba70`；
- 代码实施计划 blob：`305d74f44b622fc34b0cdf6ef48d4dc93b087899`；
- 精确删除清单 blob：`4e59f03ce06023027b9c868a97d04c80d3267a64`；
- 计划索引 blob：`2d081d7d3e7e9066da2426829b3b52a078bfc841`，已精确登记本计划为 `可执行`；
- 119 个物理删除目标的 HEAD source blob 与工作树 blob 均逐项等于清单；28 个保护路径工作树 blob 均逐项等于清单；两集合物理路径零交集；
- 旧 HEAD `应用服务.特征概念类.ixx` blob 为 `4412ae461029667fb7fb252cb4ac82959e577237`，外部重写版工作树 blob 为 `5c81203eff3fa1f667d116edb6e597228063b296`、SHA-256 为 `944808AC92B231AC1CBBF2B4D62F381022A699F0511CD9A524F0F91FF443FD68`；
- 工程交接工作树 blob 分别为 `88880fd1910a148aea830f4e0d32f77c00e336c2` 与 `45fdbc05263b610362c21d294fa3d492f838f909`，SHA-256 与正式计划一致；
- 统一登记已为本计划逐项占用 123 个发布路径。外部 FEATURE-SHARED-CONCEPT 六源码、两记录及验证资源保持安全暂停，本计划只取得工程两文件写权和旧特征概念 HEAD 版本的 index 删除权。

S0 结论：`PASS`。文档后继只包含本计划正式包与索引发布；目标 source blob、保护 blob、工程交接 blob、所有权和 index 均无漂移。

## 2. 实际物理删除

严格按精确删除清单第 2.1 节使用整文件补丁物理删除 119 个路径：

- 旧生产实现与引用代码 113 个：65 个 `.ixx`、45 个 `.h`、3 个 `.inl`；
- 专属验证文件 6 个：2 个 `.cpp`、2 个 `.vcxproj`、2 个 `.ps1`；
- 119 个目标施工后均已从工作树消失；未按文件名扩大或缩减集合；
- 未物理修改或删除 `海中鱼巣/业务/应用服务.特征概念类.ixx`，其外部重写版 blob 保持 `5c81203eff3fa1f667d116edb6e597228063b296`。

取得 Git 发布租约后，旧特征概念 HEAD source blob 已与全部迁移内容一次性从 index 退出；工作树重写版继续保留。

## 3. 工程双视图

在冻结交接的工程工作树上使用精确补丁完成：

- `海中鱼巣.vcxproj`：移除 113 个物理删除生产文件登记，结果工作树 blob 为 `f727f735d002a53b9740042e4f92a52405c75843`；
- `海中鱼巣.vcxproj.filters`：移除对应 113 个筛选器项，结果工作树 blob 为 `a8b6a3d5745141b717ba04d5de0c3546a702c776`；
- 两个工作树视图中的 113 个旧登记剩余数均为 0；
- `应用服务.特征概念类.ixx`、`应用服务.存在概念树类.ixx`、`应用服务.场景成员概念类.ixx` 的三条 `ClCompile` 与三个筛选器项原样保留；
- 发布时禁止整文件 `git add`。cached 工程投影必须相对 HEAD 删除 114 项（113 个物理删除登记加旧 HEAD 特征概念登记），且不得包含六个外部 XML 新增项。

实际 cached 工程投影由 HEAD 内容直接移除 114 项后写入 index，未整体暂存工作树工程文件：

- `海中鱼巣.vcxproj` cached blob：`640fcc5ef3bc8120d6d1ad6422a5c74d0989d340`；
- `海中鱼巣.vcxproj.filters` cached blob：`6a85fdfccdb4123926fe4a627fba43d499436b03`。

## 4. 保护与未触碰范围

- 清单第 4 节 28 个 L1/新数据服务/新应用服务保护路径均未进入物理删除；唯一版本例外只会把旧 HEAD 特征概念版本从 index 退出；
- 外部安全暂停的六源码、两记录和验证资源未修改、未删除、未暂存；
- 主入口、启动壳、数据库、持久事实、稳定编码和用户数据未处理；
- 未创建替代 DTO、兼容模块、空壳、重定向 import、旧数据 reader/writer 或迁移入口；
- 未修改计划索引、正式规范、知识库、流程图或历史记录。

## 5. 待代码知识维护清单

以下已发布代码事实将在本迁移提交后过期，交由能力证据目录/代码知识库维护者按新 HEAD 更新；本施工不直接改共享知识文件：

1. 23 个 `服务.L2*.ixx` 旧服务种子及其反向 import 消费链的函数用途、提供者与调用关系；
2. 22 个 `L2*.数据.h` 旧结构种子及其反向 include 消费链的 DTO/函数用途条目；
3. 清单列出的 12 个旧链专属身份、结果结算、运行期、自我线程、根初始化与根消费伴随文件；
4. `合同.L2结构公共.ixx`、`服务.不可变材料.ixx`、`不可变材料.数据.h` 及随闭包退出的旧共享材料用途；
5. 两个旧专项验证源、工程和运行脚本的验证入口与证据定位；
6. 任何 `知识库/函数用途/**` 或能力证据条目中以清单 120 个旧 HEAD 路径/source blob 为代码定位的记录。

## 6. 发布边界与未验证声明

已取得唯一 Git 发布租约并形成单一 staged 集合：120 个 `D`、两个工程 `M`、一个本记录 `A`，共 123 个路径。工程 cached 视图只包含相对 HEAD 的 114 项退出；外部特征概念重写版及其它外部 WIP 留在工作树。

用户明确本代码阶段不需要验证。本施工未编译、未运行、未测试、未做静态扫描、未执行 `git diff --check`、未执行 `check_specs.py`、未运行验证脚本，也未创建验证记录。边界与 blob/路径/计数读回仅用于防止越权发布，不构成代码验证或业务验收。

发布完成后，只能声明：`LEGACY-DATA-L2-PLUS-RETIREMENT` 候选删除源码及工程登记已发布；旧 L2 以上代码闭包按清单退出；代码未编译、未运行、未测试、未扫描、未验收，持久数据未清理。
