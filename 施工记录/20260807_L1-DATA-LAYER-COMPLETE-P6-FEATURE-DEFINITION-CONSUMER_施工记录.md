# I64 特征定义真实消费者中性幂等写读闭环施工记录

日期：2026-08-07

## 1. 施工身份

- 正式起点：`2f0e3ad5f1a3470d61fa9a29fdc423e87a608f72`
- 计划：`L1-DATA-LAYER-COMPLETE-P6-FEATURE-DEFINITION-CONSUMER v0.1`
- 计划 blob：`a11a0adbb80a1ee256113176478f6179bfdb8c80`
- 施工范围：一个既有领域服务和本计划两份专属记录。

## 2. 实际变更

1. 原位迁移 `L1特征定义服务`，保留模块、类、DTO、合同版本、规则版本和四个公开函数。
2. `建立登记` 固定形成四个节点、零关系、零值、零属性槽、零退出的中性写集，并恰一次调用中性提交。
3. `建立I64特征定义` 固定形成一个节点、三个值、三个属性槽、零关系、零退出的中性写集，并恰一次调用中性提交。
4. 删除领域意图编码、幂等预探测、业务操作标签、领域执行证据、完整快照和旧写集 / 旧读取入口。
5. 中性提交结果逐项核对合同版本、幂等键、首次发布 / 精确重复标志、重试边界以及四项唯一编码映射。
6. 登记只保存四个稳定编码作为非权威定位；成功或精确重复后按当前共同截止重新读取四个权威节点，完整读回后才保存定位。
7. 定义不保存请求、幂等键、事实或读取定位；每次通过正式节点、属性和值入口重新形成不可变领域结果。
8. 首次成功的节点和值创建代次必须等于首次发布代次；后续精确重复允许共同读取截止晚于首次发布代次。

## 3. 文件范围

本次只修改或新增：

- `海中鱼巣/领域/服务.L1特征定义.ixx`
- `施工记录/20260807_L1-DATA-LAYER-COMPLETE-P6-FEATURE-DEFINITION-CONSUMER_施工记录.md`
- `验证记录/20260807_L1-DATA-LAYER-COMPLETE-P6-FEATURE-DEFINITION-CONSUMER_构建与生产链验证.md`

P0-FIX、路线、旧 WORLD-TREE、`.codex-build` 和其它异主现场均未修改、未暂存、未夹带。

## 4. 完成边界

本施工只形成 I64 特征定义合法第二层消费者的中性代码候选。它不建立第一层自检、自证入口或独立第一层通过结论，也不证明实例特征、当前值修改、关系、退出 / 历史读取、其它消费者或整个第一层完善。崩溃、重启、断电和跨进程恢复不在当前范围。

## 5. v0.2 正式接线回修

- 回修起点：`038e06925c881c2955381fbd3015ba80b6524a93`
- 替代计划 blob：`ad160efd2659cef966a36eb8e590ae1912cdee7d`
- 来源：v0.1 独立验收提交 `b019b7e734b11e9b6ae2462708c71a17d144b03d` 固定的只读租约 C2662 漂移。

本次在同一服务中为 `建立登记` 与 `建立I64特征定义` 各增加一个正式 `const` 写重载；原非 `const` 重载只通过同对象的只读引用转发，不复制算法或另取锁。两种调用形态共享唯一 `mutable` 锁、唯一 `mutable` 四编码非权威定位和同一私有第一层依赖。运行期租约、上下文、DTO、第一层、工程和生产入口均未修改，也未使用 `const_cast` 或新增验收专用入口。

这里的 `const` 只约束服务对象和装配身份不被调用者替换；写副作用仍由有效运行期租约取得的唯一具名服务、强类型请求、服务准入和第一层中性事务共同约束。它不表示第一层已经验收，也不向消费者暴露可变上下文、仓、锁或事务能力。
