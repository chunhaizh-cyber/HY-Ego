# 按所有者与属性类型读取当前值组施工记录 v0.1

日期：2026-08-16

计划身份：`DATA-L1-OWNER-ATTRIBUTE-TYPE-CURRENT-VALUE-GROUP-READ`

## 1. 施工基线

- 起点 HEAD / `origin/main`：`4b04ca1b08d023c540861a16de8c6fdc0961f58c`；
- 计划状态：`可执行`；
- plan blob：`38650fd3275d71b263d25347ea7dae8ca28e7c36`；
- 设计提交：`a037ffae80706c8f035ea5506228d17a7ed6f2c5`；
- 详细设计 blob：`4f4afc6f845dc84d3bef8cdbb0eccc39e89c640b`；
- 起点 index 为空；流程图、SELF、BIZ、旧材料删除、路线和 `.codex-build` 等异主 WIP 原地保留。

## 2. 实际变更

1. `L1所有者范围CRUD.数据.h` 新增独立合同版本、八状态、请求 DTO 和结果 DTO；
2. `仓库.L1事实基座.ixx` 新增一次 shared lock 内的 owner + 属性类型当前值组读取：严格非零守卫、固定 owner 后属性类型生命周期分账、属性类型种类校验、现有当前节点引用索引筛选、稳定编码升序、预算整组拒绝和异常映射；
3. `服务.L1事实基座.ixx` 新增唯一 const 转发入口 `按属性类型读取所有者范围全部当前值`；
4. 没有修改生产工程、写端口、签发器、L2 或 BIZ，没有新增第二索引、缓存、全值扫描入口或属性业务解释。

## 3. 实际构建与检查

- 根工程 x64 Debug Rebuild：清理前、清理后均成功，0 警告、0 错误；
- 根工程 x64 Release Rebuild：清理前、清理后均成功，0 警告、0 错误；
- 临时生产外公开入口检查：Debug、Release 程序汇总均为 `15/15 PASS`；
- 两配置覆盖三个互异独占 owner、关系索引噪声、同属性类型多值、跨 owner / 跨属性类型隔离、成功空组、精确预算、超预算零载荷、陈旧守卫、owner / 属性类型未找到与已退出、普通节点拒绝、坏请求、既有读取入口不回归和读取零写；
- 临时 `.cpp`、`.vcxproj`、`.filters`、exe、pdb 和专属生成文件均已删除；未登记生产工程。

## 4. 未运行边界

- 真实分配 / 长度耗尽：`NOT_RUN`；
- 私有索引或权威事实损坏注入：`NOT_RUN`；
- 共享锁长期公平与极端并发调度：`NOT_RUN`；
- 跨进程恢复：`NOT_RUN`；
- 后继 L2 显式 I64 状态时间范围读取与正式 BIZ 消费：`NOT_RUN`。

没有为这些分支增加生产 seam、friend、故障选择器或验证入口。

## 5. 完成边界

本记录只证明本 L1 中性属性类型反向读取切片的实际施工过程。它不定义四类时间材料统一排序，不证明状态或动态时间范围查询、`DATA-EXT-04—14` 或数据服务最终需求清单整体完成。
