# INSTINCT-STAGE3-HARD-DENIAL-RULE-UNIVERSE 施工记录 v0.2

日期：2026-08-31

计划身份：`INSTINCT-STAGE3-HARD-DENIAL-RULE-UNIVERSE`

## 1. S0 与范围

- 起点：`main == origin/main == d46c43a14c3a1220f1a847d0f4030d6e2322d072`，ahead/behind `0/0`，index 空。
- 计划索引唯一可执行项为 v0.2，plan blob `1b941be8013a3f36154f150c1ffdbc88aeef258b`。
- 工作区存在大量异主 SELF-GOVERNANCE WIP；本切片只消费两个共享数据头的精确 ABI hunk及两个工程文件的五项安全登记，未覆盖、删除、暂存或提交其它 WIP。
- 当前生产定义固定为值域 `0..INT64_MAX`、`L=2767011611056432742`、`H=7378697629483820645`、安全根定义版本 1、主动安全结算规则版本 1；本切片零修改。“浮动”只允许未来发布新定义版本。
- 精确提交前验证树：`D:/TEMP/海中鱼巣/INSTINCT-STAGE3-HARD-DENIAL-RULE-UNIVERSE/20260831-072418`。

## 2. 实际改动

1. `L2普通方法结构.数据.h` 发布唯一 `L2方法动作作用范围` 枚举；`L2任务结构.数据.h` 发布三类任务执行身份、同型别名和 `任务冻结动作项`。安全目录只消费这些类型，不建立第二 DTO。
2. 新建唯一安全规则结构 owner，固定 owner / 登记幂等身份和 `0x0307'2001..200E` 键；`200F..201F` 保留且未使用。
3. 规则 owner 实现完整规则组规范化、同语义多来源归一、异义拒绝、首次 / 连续版本原子发布、旧闭包退出、首次幂等材料重放、历史读回与 Gnow 当前全集读回。
4. 全集读取只对现实改变动作互证单一共同作用场景、self 和直接作用主体成员；覆盖见证逐项保存版本、生命周期、self、场景和动作范围判定。
5. hard-veto v2 只消费完整强类型全集：零现实改变不适用，完整空 / 非空适用组分别为未命中 / 已命中，非完整材料 fail-closed。
6. 专项增加 P00—P08。P08 在同一受控持久根关闭第一会话、恢复第二会话，重新装配存在 / 场景 / 规则服务，逐字段互证旧版本历史重放、当前版本、当前全集和 hard-veto 命中。
7. 工程只登记两个安全数据头、两个生产模块和一个专项模块；未接普通应用、线程、FRESH 或真实规则发布者。

## 3. 函数级代码知识变更清单

| 函数 / 谓词 | 变化 | 用途、非职责与调用边 | 待回写 |
| --- | --- | --- | --- |
| `L2任务现实执行硬否决规则覆盖项_v2::适用`、`L2任务现实执行硬否决规则完整集合见证_v2::完整` | 新增 | 校验逐规则五项合取和规范身份覆盖形状；不读取或发布事实 | 新建规则数据 DTO 用途卡 |
| `L2任务现实执行硬否决规则宇宙发布结果_v2::成功`、`L2任务现实执行硬否决规则全集读取结果_v2::成功` | 新增 | 校验公开结果完整形状、同截止与成员数量；由服务后置检查、provider 和专项消费 | 新建规则数据 DTO 用途卡 |
| `尝试形成L2任务现实执行硬否决规则结构所有者交付` | 新增公开服务取得函数 | 验证固定 owner 建立结果、当前 owner 读回及同 L1 独占写端口后移动交付；不发布规则 | 新建规则结构服务用途卡 |
| `L2任务现实执行硬否决规则结构服务::L2任务现实执行硬否决规则结构服务` | 新增公开构造函数 | 绑定同一 L1 / 场景服务和唯一 owner 写端口，幂等建立结构登记；不自动发布空宇宙 | 新建规则结构服务用途卡 |
| `发布或读取任务现实执行硬否决规则宇宙_v2` | 新增公开入口 | 规范化完整规则组，在唯一 owner 写集中发布连续版本并正式历史读回；同键重放收敛，非成功零普通载荷 | 新建规则结构服务用途卡 |
| `读取任务现实执行硬否决规则全集_v2` | 新增公开入口 | 在请求 Gnow 互证共同场景和当前完整规则闭包，返回规范适用组与覆盖见证；零写、零缓存 | 新建规则结构服务用途卡 |
| `初始化结构登记`、`规范化规则组`、`形成发布写集`、`读取提交版本历史`、`读取当前宇宙_已锁定`、`验证共同作用场景`及相邻校验 / 映射辅助 | 新增 module-private 组 | 分别承接登记、规范化、写集、历史解码、当前闭包和场景互证；只服务同一 owner 公开入口，不形成第二公共能力 | 在规则结构服务用途卡中登记私有组 |
| `任务现实执行硬否决提供者::复核任务现实执行硬否决` | 新增公开纯消费者入口 | 校验强请求和正式全集，形成不适用 / 材料不足 / 未命中 / 已命中；不拥有、枚举、缓存或发布规则宇宙 | 新建 hard-veto 用途卡 |
| `形成作用范围证据`、`正式规则全集有效` | 新增类私有辅助 | 校验现实改变动作子集和全集逐字段闭合；只向公开 provider 送出 bool，不记录第二事实 | 在 hard-veto 用途卡中登记私有组 |
| `运行任务现实执行硬否决规则宇宙端到端测试`及测试私有夹具 | 新增专项 | 运行 P00—P08；fixture 只存在于专项，不是生产规则清单或发布者 | 新建专项用途卡 |

退出函数：无。两个共享数据头只新增值类型，没有直接修改既有函数。结果提交在本轮精确提交形成前标记为待填；完成回传以实际提交为准。

## 4. 完成边界

本施工结果最多证明最小冻结动作 ABI、唯一规则 owner 和 hard-veto v2 的独立服务闭环。没有方法动作 v2 / 任务冻结生产者、真实规则清单、生产宇宙、FRESH、最终复核和普通应用装配，不能声明阶段三、生产安全闭环或 INSTINCT-ROUTE 完成。
