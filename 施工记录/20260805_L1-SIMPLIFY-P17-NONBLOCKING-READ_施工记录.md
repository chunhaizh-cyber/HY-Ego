# L1-SIMPLIFY-P17-NONBLOCKING-READ v0.2 施工记录

日期：2026-08-07

计划身份：`L1-SIMPLIFY-P17-NONBLOCKING-READ`

计划版本：`v0.2`

plan blob：`8c830e9016553315f826d359e7f614af3d40d846`

设计提交：`eec7b35072635db98426366ba62b6b0fe9d215e3`

施工起点：`b0df2d72ee2d95278f2819a130dec8a8c4e3b8a9`

完成复核基线：`5868970df2ff9f4819263a4f1b9c37ed1dce4553`

## 1. S0与范围

施工前确认`main==origin/main==b0df2d72ee2d95278f2819a130dec8a8c4e3b8a9`、ahead/behind为`0/0`、Git index空，P17 v0.2为唯一可执行代码计划。此前基线增量精确为R5三份设计文档及一条待激活索引行，与P17七路径和机器合同零重叠。

施工完成复核时正式HEAD继续普通前进到`5868970df2ff9f4819263a4f1b9c37ed1dce4553`；增量精确为R6三份设计文档及一条待激活索引行，与P17七路径、实际接口和验证资源零重叠。P17仍为索引唯一可执行计划，故无需重做已稳定的双配置代码验证。

P15 v0.29代码结果`0184a0446d7ddc15cf0f2004e5b91ab70dda5f41`及独立验收`211f05cc0684715270037472734670d071c4d2b1`均为施工起点祖先；实际`L1事实基座合同版本`为2，唯一数据头、合同、仓库、服务和自检模块均无既有dirty。P15结论仍为`INTEGRATION-ACCEPTANCE-FAIL / 未正式接线`，本切片未将其升级为PASS。

实际代码变更仅在五个既有核心文件；本文件和配对验证记录为两份新增专属记录。工程、filters、启动、R5/R4、P0-FIX、路线、WORLD-TREE、`.codex-build`及其它WIP均未修改。

## 2. 实施结果

1. 在`L1读取状态`末尾追加稳定值`许可拒绝=9`，未重编号既有状态。
2. 新增纯值`L1事实代次读取请求/结果`，继续使用L1总合同版本2。
3. 在唯一`L1事实基座仓库`增加`尝试读取完整快照`与`尝试读取当前事实代次`：每次只执行一次`std::try_to_lock`共享许可，竞争立即结构化返回许可拒绝，不sleep、不循环重试。
4. 阻塞式旧快照入口和新非阻塞入口共同复用唯一私有`构造快照`与稳定排序实现；没有建立第二仓、旁路索引或第二套快照解释。
5. 空L1快照成功返回代次0；空L1代次读取返回未找到0；正常状态返回非零代次；隔离或损坏返回内部不一致；快照分配失败返回资源失败；结果均为调用方拥有的纯值副本。
6. 在唯一服务类增加两个版本校验后转发入口，不调用P15提交、幂等探测、恢复或领域结果接口。
7. 在原L1自检模块增加合同错、空/非零代次、稳定排序、重复读取、并发共享、独占竞争、退出后当前快照、隔离/损坏、资源失败和未知异常矩阵。Debug故障辅助只在`#ifdef _DEBUG`内存在，不进入Release合同。
8. 新增和直接修改函数按施工规范登记为结构化“向上送出”或“无适用错误分支”，未新增文本日志或第二诊断出口。

## 3. 构建资源

Debug/Release中间目录、生产可执行文件、自检输出和模块扫描JSON均位于系统临时目录`hzy-p17-v02-20260807`。仓库`.codex-build`未被读取、清理、覆盖或纳入结果。MSBuild仅出现将输出放在系统临时目录所对应的预期`MSB8029`警告；双配置均编译、链接成功。

## 4. 完成边界

本次结果只形成P17 L1组件的一次try-lock当前完整快照副本与当前事实代次读取候选。它不证明P15正式接线或正式独立读回，不证明指定根世界结构、存在/特征/状态/动态读回、完整世界快照、生产装配、崩溃/跨进程恢复、STEP-4或治理闭环完成。当前代码尚未暂存、提交或推送，仍须取得精确Git发布租约并在发布后接受独立验收。
