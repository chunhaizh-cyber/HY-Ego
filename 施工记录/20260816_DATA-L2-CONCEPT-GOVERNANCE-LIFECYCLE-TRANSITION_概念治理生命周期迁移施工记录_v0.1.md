# DATA-L2 概念治理生命周期迁移施工记录

身份：`DATA-L2-CONCEPT-GOVERNANCE-LIFECYCLE-TRANSITION`

版本：v0.1

日期：2026-08-16

## 1. 基线与范围

- 施工起点：`main == origin/main == 10fe8f5e9d84520aebc94b5470a498bb95de7a71`，ahead / behind 为 `0/0`，index 空。
- 正式计划状态：`可执行`；plan blob `94ce335c136c1fe12299f2e22cf95d0b2bd9f80b`，设计提交 `bfd3526a964781cc7ece424dce21a0d8afec30eb`，详细设计 blob `09d4529a259767986ec68fff64847d8bd5b0a84a`。
- 普通概念、直接上下位和世界事实支持关系结果 / 退出依赖均已位于施工起点祖先。
- 实际生产代码只修改 `海中鱼巣/领域/L2普通概念结构.数据.h` 与 `海中鱼巣/领域/服务.L2概念结构.ixx`；另新增本记录和同身份检查记录。
- 最终需求清单、计划索引、L1、普通应用、概念聚合、工程文件、不可变材料、六类世界服务、BIZ、SELF、流程图 / 知识图谱及其它异主 WIP 均未修改。

## 2. 实际实现

1. 在普通概念数据头增加生命周期合同版本、迁移 / 读取请求、写 / 读结果以及状态、迁移矩阵、请求和事实投影五项纯值 helper；既有枚举数值和 `L2普通概念事实` 字段顺序保持不变。
2. `L2普通概念事实完整(...)` 复用新的生命周期事实投影 helper；普通概念、四本体根、直接上下位和世界事实支持 DTO / 成功形状保持原公开入口。
3. 唯一 `L2概念结构服务` 新增 `迁移概念治理生命周期(...)` 与 `读取概念治理生命周期(...) const`；两个入口只消费概念 owner 自有的治理生命周期值，不依赖不可变材料或六类世界服务，因此根专用构造和完整构造复用同一实现。
4. 迁移矩阵仅包含 `活跃 -> 冷却`、`冷却 -> 活跃`、`冷却 -> 退役`、`退役 -> 活跃`；同状态、`活跃 -> 退役` 和 `退役 -> 冷却` 在纯值校验阶段拒绝。
5. 固定本地键 `0x020A'5001U` 的规范化 owner 写集恰含一项新 I64 生命周期值、一项具名旧值退出和一项同属性槽切换；节点、关系、签名、定义、上下位、支持和其它属性槽零变化。
6. 当前代次等于请求守卫时，先经不依赖外部材料的生命周期读取逐字段核对概念、旧稳定编码和旧状态；不匹配返回 `引用冲突`。守卫已经变化时不改写请求，直接向 L1 提交原规范化写集，由 L1 裁决精确重复、事实代次漂移或幂等冲突。
7. 首次写入材料绑定完整规范化写集和首次结果；首次提交与精确重复均在首次截止重建新当前值和同代退出旧值。坏编码映射、错 owner / 类型 / 来源、槽错位、混代或坏成功形状不被猜成成功。
8. 当前 / 历史读取先排除四个永久本体根，再从普通概念 owner 历史投影取得具名截止上恰一生命周期；成功返回非零单一截止、零变更和非空值，非成功为空载荷、零变更。

## 3. 构建与静态检查

- 仓库外 `D:\DATA_L2_CONCEPT_GOVERNANCE_LIFECYCLE_验证临时` 中完成根工程 x64 Debug / Release fresh Rebuild；两次模块扫描、编译和链接均通过，未输出源码警告或错误。
- `git diff --check`：PASS；换行转换提示不构成空白错误。
- `python .\tools\check_specs.py --strict`：PASS，113/113。
- 静态合同检查：公开 ABI 与五项 helper 均存在；合法迁移矩阵 4/4；固定键在生产 C++ 中唯一命中；迁移写集为 `value:1 / slot:1 / exit:1 / node:0 / relation:0`。
- 唯一概念 owner 交付、唯一概念写锁、唯一治理生命周期属性类型以及既有根、普通概念、直接上下位和支持入口均保持唯一并完成根工程编译回归。
- 两份目标生产文件严格 UTF-8、无 BOM；新增生产差异中测试、mock、seam、断言、自检或验收入口为 0。

## 4. 完成边界

本记录只证明唯一 `L2概念结构服务` 已增加普通概念治理生命周期当前 / 历史读取和四条合法原子迁移的生产实现，并通过根工程双配置编译及静态合同检查。

没有建立专门验收或动态专项。真实首次 / 重复、四条合法迁移、三类非法迁移、当前 / 历史、并发相反请求、资源失败、发布未知、坏私有事实、普通应用完整装配、合法消费者和跨进程恢复均为 `NOT_RUN`。

自动治理、定义约束 CRUD、普通概念不可逆退出、普通应用不可变材料根配置、DATA-L3 图投影、`DATA-EXT-08` 整项及数据服务最终需求清单整体不在本叶完成声明内。
