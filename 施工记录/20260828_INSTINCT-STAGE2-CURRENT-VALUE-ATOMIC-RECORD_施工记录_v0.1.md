# INSTINCT-STAGE2-CURRENT-VALUE-ATOMIC-RECORD 施工记录

日期：2026-08-29
计划：`INSTINCT-STAGE2-CURRENT-VALUE-ATOMIC-RECORD` v0.6
计划 blob：`7add0af4711998ee8c082101d1fd6232a912887d`
起点：`8a7696694652cfd62894fb1c357072298837825b`
结果提交：本记录与阶段二结果同提交形成；精确提交哈希以 Git 发布回传为准。

## 1. S0 与所有权

- `main`、`HEAD == origin/main == 8a7696694652cfd62894fb1c357072298837825b`、ahead/behind `0/0`、开工 index 空。
- 唯一执行项为计划索引登记的阶段二 v0.6；直接依赖的阶段一本能根锚点和 L1 三分区事务已在正式 Git。
- 工作区保留 SELF-GOVERNANCE 等异主 WIP。阶段二只取得计划第 10 节白名单；`服务.L2任务结构.ixx`、`服务.L2方法结构.ixx`、`装配.普通应用.ixx`、工程文件按阶段二 hunk 构造 index，禁止带入其它增量。
- 复用裁决：扩展现有 `L2特征结构服务`、`L2状态结构服务`、`L2动态结构服务` 与唯一 `L2状态动态原子发布服务`；普通应用仍只装配一份组合服务；没有新增第二 owner、第二写入口或 L1 ABI。

## 2. 实际施工结果

1. 增加特征当前值变化请求、结果和独立读回 DTO；三 owner 的当前值、状态和动态只由一次 L1 v2 三分区事务形成。
2. 状态 owner 增加关系 19 上下文及节点式/关系式当前选择统一读取；动态 owner 增加关系 20 上下文和状态迁移/动作致变分账。
3. 组合服务完成无变化、legacy 基线、正常迁移、精确重复、冲突、漂移、已可能发布和独立历史读回。
4. 特征 owner 增加按固定幂等身份读取实例首次完整事实的只读入口；本能根 provider 只有在首次账正式未找到时才新建实例。
5. 普通应用把组合服务构造移动到场景服务可用之后，注入特征/状态/动态三端口及存在、场景和三个同实例 L2 服务。
6. 构造期固定登记改为先读 L1 首次材料、复用首次 G0、以首次 G1 核对创建事实、以恢复 Gread 读取当前投影；同键异义 fail-closed。
7. `L2新增需求并建立列表项请求有效` 只删除跨角色四字段互异限制，继续逐字段验证身份和请求头。
8. 新增阶段二专项模块和工程登记；测试持久根全部位于 `D:\TEMP\海中鱼巣\INSTINCT-STAGE2-CURRENT-VALUE-ATOMIC-RECORD`。
9. 精确提交树测试显式补齐基线 `普通应用配置.等待合同登记`；该材料由既有 `形成普通应用任务筹办等待合同登记()` 产生，只服务阶段二测试构造，不带入共享工作树中 L4 对普通应用配置的未提交变更。

## 3. 代码知识变更清单

下表只列阶段二新增或直接修改的稳定函数身份；局部 lambda 不另立身份。相关共享用途卡均基于旧提交，结果发布后应标记待复核并由知识维护者按结果提交更新。

| 模块 / 文件 | 稳定函数身份 | 用途与非职责、事实及调用边变化 | 用途卡动作 |
| --- | --- | --- | --- |
| `L2状态动态原子发布.数据.h` / `服务.L2状态动态原子发布.ixx` | `L2特征当前值变化记录结果_v1::成功()`；`L2特征当前值变化记录读取结果_v1::成功()` | 固定两种成功形状；不裁决业务目标。 | 新建 |
| 同上 | `L1三分区原子事务凭证_v2::有效()`；`L1三分区原子事务凭证_v2::提交(...)`；`尝试形成L1三分区原子事务凭证_v2(...)` | 只收窄三个同 L1、互异 owner 写端口；提交仍调用 L1 v2 唯一入口，不缓存事实。 | 新建 |
| 同上 | `L2状态动态原子发布服务::L2状态动态原子发布服务(...)`；`事务凭证有效()` | 组合服务新增存在/场景/特征依赖和三分区凭证；旧迁移入口继续零写。 | 更新 |
| 同上 | `发布特征当前值变化与中性记录_v1(...)`；`读取特征当前值变化记录_v1(...)` | 新公开写入与独立读回；一次事务写三 owner，已可能发布只保留原请求重放边界。 | 新建 |
| `L2特征结构.数据.h` / `服务.L2特征结构.ixx` | `L2特征实例幂等首次完整读取请求有效_v1(...)`；`L2特征实例幂等首次完整读取结果_v1::成功()` | 首次账、首次 G1、当前 Gread 和完整实例闭合；只读、不重放。 | 新建 |
| 同上 | `形成特征实例幂等首次完整读取失败_v1(...)`；`映射特征实例幂等首次完整读取失败_v1(...)`；`按幂等身份读取特征实例首次完整事实_v1(...)` | 从 L1 首次材料唯一重建固定实例请求并互证当前实例；禁止扫描认领。 | 新建 |
| 同上 | `读取固定登记期望代次(...)`；`形成当前值变化三分区参与者_v1(...)`；`取得组合发布写入端口()` | 前者只复用固定登记首次 G0；后者只形成特征参与者写集，不直接提交；端口只供唯一组合服务装配。 | 新建 / 更新 |
| `L2状态结构.数据.h` / `服务.L2状态结构.ixx` | `L2中性状态上下文读取结果_v1::成功()`；`读取中性状态上下文_v1(...)` | 读取关系 19 强类型投影，不形成第二状态权威。 | 新建 |
| 同上 | `读取固定登记期望代次(...)`；`初始化状态上下文关系类型(...)`；`形成当前值记录状态参与者_v1(...)` | 固定登记恢复；形成 legacy/正常状态、关系式当前选择和退出闭包，返回参与者写集而不提交。 | 新建 |
| `L2动态结构.数据.h` / `服务.L2动态结构.ixx` | `L2中性动态上下文读取结果_v1::成功()`；`读取中性动态上下文_v1(...)` | 读取关系 20 强类型投影，区分迁移动能和动作致变。 | 新建 |
| 同上 | `读取固定登记期望代次(...)`；`初始化动态上下文关系类型(...)`；`形成当前值记录动态参与者_v1(...)` | 固定登记恢复；形成动态参与者写集，不直接提交。 | 新建 |
| `提供者.本能根运行初始化.ixx` | `映射结构失败(...)`；`值态符合规格(...)`；`确保实例(...)`；`读取并验证根运行事实(...)` | 恢复优先消费中性当前选择和首次幂等实例读回；只在未找到时新建，不从动态或实例组补造当前值。 | 更新 |
| 固定登记 L2 服务 | 各服务 module-private `读取固定登记期望代次(...)`；语言 `读取登记节点(...)` / `读取词条登记节点(...)`；存在服务构造函数；场景、因果、概念、概念名称、方法、任务各既有初始化函数 | 从 L1 首次材料恢复固定登记，G1 证明创建、Gread 证明当前；不改变业务 CRUD。任务/方法混合文件只提交 HEAD 既有初始化函数的恢复增量。 | 更新 |
| `L2需求结构.数据.h` | `L2新增需求并建立列表项请求有效(...)` | 允许所属存在等于目标宿主；不放宽各字段有效性、父关系或幂等合同。 | 更新 |
| `装配.普通应用.ixx` | `构造普通应用上下文(...)` | 将唯一组合服务改为三参与者装配并在场景服务后构造；getter 与上下文持有形状不新增第二实例。 | 更新 |
| `端到端测试.本能路线阶段二.ixx` | `运行本能路线阶段二端到端测试()` 及其 module-private fixture/helper | 覆盖 V01—V17、V01A、V16A；只写隔离临时持久根，不提供生产入口。 | 新建测试用途卡或登记为测试函数 |

直接调用边新增：普通应用 `构造普通应用上下文` → `尝试形成L1三分区原子事务凭证_v2` → `L2状态动态原子发布服务`；组合服务 → 三个 owner 的 module-private 参与者形成函数 → L1 `提交三分区原子事务_v2`；本能根 provider → `按幂等身份读取特征实例首次完整事实_v1`。没有删除公开函数。

## 4. 完成边界

- 本记录只证明阶段二基础能力的实现与专项目标，不证明阶段三被动维护、安全/服务根自检、任务执行或完整本能路线。
- V05 缺正式生产方法动作 fixture；V16A 的许可/资源/内部故障、首次账缺项和持久账同键异义缺专属 L1 恢复故障 fixture，均在验证记录中保留 `NOT_RUN`。
- 共享知识库、代码流程图和能力证据目录不在本计划白名单；发布后交由各自维护者消费本节清单。
