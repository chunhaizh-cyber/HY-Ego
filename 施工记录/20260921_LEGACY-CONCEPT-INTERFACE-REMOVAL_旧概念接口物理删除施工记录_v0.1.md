# LEGACY-CONCEPT-INTERFACE-REMOVAL 施工记录 v0.1

日期：2026-09-21

计划：`LEGACY-CONCEPT-INTERFACE-REMOVAL`

计划观察版本：v0.1

计划 blob：`857de5e4bdc2a0e0463d04859aae78a712dba031`

施工起点：`aa465854b210d9e0af01c64b314ea8fdf2fbd655`

结果提交：本记录与代码结果同一提交发布。

## 1. S0 与所有权

- 唯一主工作区为 `D:\海中鱼巣`，分支为 `main`；开工及发布前复核时 `HEAD == origin/main == aa465854b210d9e0af01c64b314ea8fdf2fbd655`，左右差为 `0 0`。
- `计划/计划索引.md`登记本计划为“可执行”，计划正文 blob 与索引一致。
- 本切片只写计划第3节允许的生产代码、验证工具、工程登记和本计划专属记录；没有修改规范、详细设计、计划、计划索引、普通应用装配、L1、RC/DC、共享知识索引或其它专项源码。
- 根目录 `%TEMP%staged.txt`、既有 `.obj`、`vc140.pdb` 与 `海中鱼巣/Debug/`为异主/历史材料，原地保留，未清理、未暂存、未作为验证输入。
- 复用裁决：保留并复用 pure v2、特征概念出生使用、存在概念两组、相关概念、L1 中性读取和生命周期投影；没有建立兼容层、第二概念 owner、第二读写入口或假成功对象。

## 2. 实际变更

### 2.1 旧概念公开面物理退出

- 从 `数据服务.概念树类.h/.cpp`删除四根/base 旧布局 DTO、旧读写头和结果、旧支持/来源/共享 FC/EC DTO、5 个接收旧布局的构造重载，以及旧概念 CRUD、根访问、共享应用和直接上位公开入口。
- 删除私有 `layout_`、`shared_`、`feature_layout_`、`existence_layout_`、`旧布局()`、旧 pending 状态及只由旧公开面可达的读取、规范化、写集、发布和共享应用实现。
- 保留当前 `纯概念结构交付_v2 + 特征概念出生使用结构交付 + 存在概念两组结构交付_v1`构造、相关概念构造及其现行 CRUD。
- 保留当前路径仍调用的中性 `映射(存在历史读取状态)`及 L1 节点、关系、属性、生命周期投影辅助，不恢复任何旧 DTO 或旧公开入口。

### 2.2 旧应用与工程登记退出

- 物理删除无生产消费者的 `应用服务.存在概念树类.h/.cpp`，包括其进程内根绑定、继续材料和旧存在概念形成/治理应用入口。
- 从根工程、filters、绑定存在专项、相关概念专项和世界树根专项删除旧应用文件登记。

### 2.3 验证消费者迁移

- `绑定存在创建专项验证.cpp`改用现行存在单例角色、实例特征结构、pure v2、FC 出生使用、存在两组结构和世界树 v4；保留直接子场景、场景成员、父存在组成、零写拒绝、概念重放、同类别多父与正式读回断言。
- 删除该专项越出本计划边界的“两组存在概念经纯概念入口退役/退出”断言，改为按创建结果 `H`与完整两组预算读取正式定义。
- `相关概念窄参与专项验证.cpp`删除 old-only 与新旧迁移兼容用例；夹具补齐现行存在单例角色和实例特征结构登记。
- `相关概念窄参与专项验证.vcxproj`删除已无测试调用边的普通应用、启动、相机、线程、二次特征、因果和场景成员应用编译单元，补入当前概念树直接依赖的特征值域比较单元，并为四个配置恢复 `/bigobj`。这只收敛专项工程闭包，不改变生产工程装配。

## 3. 实际文件

- `海中鱼巣.vcxproj`
- `海中鱼巣.vcxproj.filters`
- `海中鱼巣/领域/数据服务.概念树类.h`
- `海中鱼巣/领域/数据服务.概念树类.cpp`
- `海中鱼巣/业务/应用服务.存在概念树类.h`（删除）
- `海中鱼巣/业务/应用服务.存在概念树类.cpp`（删除）
- `验证工具/绑定存在创建专项验证.cpp`
- `验证工具/绑定存在创建专项验证.vcxproj`
- `验证工具/相关概念窄参与专项验证.cpp`
- `验证工具/相关概念窄参与专项验证.vcxproj`
- `验证工具/世界树根启动专项验证.vcxproj`
- 本施工记录与同名验证记录。

## 4. 函数级代码知识变更清单

共享用途卡和当前知识状态不由本执行切片修改；下列函数身份交由知识维护者按发布提交复核。

| 函数或函数族 | 变化 | 原用途与当前裁决 | 直接调用边与用途卡动作 |
| --- | --- | --- | --- |
| `概念树类数据服务`接收`概念树结构交付`及旧共享/特征/存在扩展交付的 5 个构造重载 | 退出 | 构造四根/base 旧布局或叠加旧共享应用扩展；当前生产无调用者 | 所有验证旧调用边已删除或迁移；标记退出 |
| `读取概念`、`查找完整定义`、`读取概念图`、`按概念读取支持`、`按世界读取支持`、`读取形成来源当前引用` | 退出 | 读取旧四根/base 概念、支持和形成引用 | 旧 DTO 同时退出；标记退出 |
| `创建概念`、`操作直接上位`、`建立世界支持`、`退出世界支持`、`迁移概念生命周期`、`退出概念`、`释放形成来源当前引用` | 退出 | 通过旧业务写请求执行旧概念写入 | 无生产调用；标记退出 |
| `共享应用已启用`、`特征根引用`、`存在根引用`、`动态根引用` | 退出 | 暴露旧共享扩展和四根身份 | 当前调用改用 pure v2/两组结构或不再需要根访问；标记退出 |
| `读取应用概念定义`、`读取应用概念图`、`读取应用特征模板`、`判定应用特征模板`、`读取应用概念关联`、`执行应用概念写入` | 退出 | 旧共享应用读写面 | 无生产调用；标记退出 |
| `登记类型观察`、`读取类型观察`、`查找特征概念`、`建立特征概念`、特征名称/命中/组织/写入头/收敛写入/旧审计入口 | 退出 | 旧共享 FC 接口 | 现行纯概念和 FC 出生使用入口保留；标记退出 |
| 共享概念使用、上位、生命周期、退出、引用释放入口，以及旧 `创建存在概念`、`释放存在抽象依据`、`治理存在概念` v2 入口 | 退出 | 旧共享 EC 及用途治理 | 现行存在两组 CRUD 保留；标记退出 |
| `存在概念树应用服务`构造、析构、根读取、形成/继续/上位/树读取/支持退出/治理/抽象依据释放函数族 | 整类退出 | 无生产消费者且依赖旧存在根和旧共享入口 | 两文件和全部工程登记物理删除；标记退出 |
| `概念树类数据服务(...纯概念结构交付_v2, 特征概念出生使用结构交付, 存在概念两组结构交付_v1)` | 保留 | 当前生产概念 owner 唯一完整构造 | 普通应用、绑定存在专项和世界树根专项继续调用；用途卡复核为当前 |
| `概念树类数据服务(...相关概念结构交付)` | 保留 | 相关概念窄参与专项构造 | 相关概念专项继续调用；用途卡复核为当前 |
| pure v2、FC 出生使用、存在两组、相关概念和原子 I64 公开函数族 | 保留 | 当前概念服务能力 | 构建及具名专项保持通过；用途卡只核对未改义 |
| `S 映射(存在历史读取状态)`及当前 L1 中性读取辅助 | 保留/恢复误删 | 当前纯概念/两组路径仍需要的状态映射 | 私有直接调用保持；用途卡核对为内部辅助 |

## 5. 完成边界

本施工证明旧生产接口、旧验证消费者和旧应用文件已物理退出，并证明当前构造在本轮构建和专项证据层闭合。它不实现 RC/DC，不迁移或物理删除旧持久数据，不证明跨进程恢复、全仓全部概念逻辑或业务目标完成。
