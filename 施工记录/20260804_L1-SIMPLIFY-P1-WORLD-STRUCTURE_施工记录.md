# L1-SIMPLIFY-P1-WORLD-STRUCTURE v0.4 施工记录

计划 blob：`cad5a569eb8fc875676a9465c05b580893116ec0`

## S0

- 正式施工基线：`bad8c7207e8861acea128605fda30e272050e86c`，`main == origin/main`，index 初始为空。
- v0.4 已替换可执行的 v0.3；`L1-SIMPLIFY-CONTRACT-F0 v0.1` 仍为待激活，本切片未消费。
- 仅接管七文件 v0.3 WIP，并在同一白名单内新增世界操作身份数据头；未修改核心 L1、仓库、运行期、旧世界/场景、恢复、SQL、材料、状态动态或计划索引。

## 施工

- 新增 `世界操作幂等身份` L2 纯值合同，世界登记与初始场景公开 DTO 改用业务身份，合同版本均升为 2；v1 请求固定入口拒绝。
- 世界登记服务在模块内以域 `0x01`、初始场景服务以域 `0x02` 映射私有 L1 写集幂等键；业务身份和 L1 键不向 L2 结果泄漏。
- 保留三本地键单写集、原子发布、幂等/冲突状态和独立节点/属性/值/关系同代次读回；动作成功事实仍由公开读取路径校准。
- 扩充世界登记与初始场景专项，覆盖身份值域、v1 拒绝、跨服务同值不碰撞、零变化拒绝及 12/14 项专项分支；完整自检顺序 290/300 与总数 25 保持不变。
- 首轮 Debug 自检发现 L1 专项无效请求误使用 v2 合同，已在计划范围内改为 v1 后复验通过。

## 完成边界

只证明隔离世界登记与唯一初始子场景的 L2 业务合同、私有幂等映射、原子写入、幂等/冲突和真实独立读写闭环；不证明 L1 合同/仓库隔离、运行期、持久恢复、存在、状态动态、旧入口删除、生产接线或集成验收。
