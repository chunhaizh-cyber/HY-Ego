# L2-FEATURE-COMPARISON-TARGET-CONTRACT-NEUTRAL-CRUD-MIGRATION v0.1 施工记录

日期：2026-08-08

## 1. 施工身份与起点

- 计划：`L2-FEATURE-COMPARISON-TARGET-CONTRACT-NEUTRAL-CRUD-MIGRATION v0.1`
- plan blob：`b706d19f3200fee3639f61598f085a8e80d8d0b9`
- 设计提交：`4a1565b9434849b8acc0a0d6a5f01a35185bb6da`
- 计划索引 blob：`c8ba41a469ea2c70f6a13e30d57a538bc793eb20`
- 目标服务起点 blob：`bb28ff0f2c2513b6c35928de3e906497245bf2e1`
- 正式施工起点：`dcfebdfd61d70ff62c3b251c90d0165dc851c631`

S0 复核时 `main == origin/main`，ahead / behind 为 `0 / 0`，Git index 为空，本计划是计划索引中唯一 `可执行` 项。P0-FIX 文档、路线输入、异主 `.codex-build/` 和两份 WORLD-TREE 计划 WIP 原样保留，不纳入本轮施工或发布范围。

## 2. 实际施工范围

本轮只修改或新增三个计划白名单路径：

1. `海中鱼巣/领域/服务.特征比较.ixx`
2. `施工记录/20260808_L2-FEATURE-COMPARISON-TARGET-CONTRACT-NEUTRAL-CRUD-MIGRATION_施工记录.md`
3. `验证记录/20260808_L2-FEATURE-COMPARISON-TARGET-CONTRACT-NEUTRAL-CRUD-MIGRATION_构建与目标判断比较合同验证.md`

生产代码实际变化：

- 保留 `0x09` 合同幂等域，新增 M02 专用中性幂等键映射；M03 的 legacy 幂等键映射和 `0x0E` 域保持不变。
- 新增 `目标判断合同锁_`，使 M02 登记与公开当前读取整段串行；两条根均按“专属锁后短暂取得现有缓存锁”的唯一顺序访问缓存。
- 保留 `合同请求_` / `合同_` 成组缓存。完整原请求才能进入预期精确重复；同键异义返回 `入口拒绝/注册不唯一`；缓存半结构或缓存与登记定位矛盾返回内部不一致。缓存只提供业务幂等和稳定定位，不直接形成成功。
- 首次路径以 M01 `读取当前登记()`、G0、定义节点、定义三值、八个关系目标节点和 G1 形成同代次输入束；只对首末代次竞争完整重试一次，不在提交前以 G0 与请求期望代次比较阻断请求。
- 固定形成 `1 节点 / 8 关系 / 1 值 / 1 属性槽变更 / 0 退出` 的中性写集；配置材料保持 20 项，关系角色保持 1—8，本地键保持 1—10。
- `登记I64目标判断比较合同` 删除 M02 的 `0x15000802U`、G8 凭证、幂等预探测、完整快照、legacy 写集和 `提交写集`；每个可形成完整写集的请求物理调用 `提交中性写集` 恰一次。
- 首次成功和精确重复分别核对路径、发布标志、重试边界、回显幂等键、非零事实代次和十映射完整唯一，再执行权威中性精确读回；成功后才成组更新缓存。
- M02 精确读回以 M01 当前登记、G0、注册节点、配置值、当前属性、八关系源组、八目标节点、定义三值和 G1 闭合，并核对固定材料、角色 1—8、十项共同创建代次及可选写入映射。
- `读取I64目标判断比较合同` 只复制缓存定位并调用同一中性精确读回，不再直接返回缓存或调用 M02 旧完整快照 helper；无调用者的 `读取I64目标判断比较合同内部` 已删除，runtime `解析合同` 保留不变。
- M01 未登记路径保持既有 `未注册/未注册`，没有误升级为版本漂移。

## 3. 排除范围与同一性

- M01 两个公开根相对目标服务起点逐字相同。
- 从 M03 登记根到公开区结束的 M03、状态迁移比较和运行比较公开段相对起点逐字相同。
- runtime `解析合同` 逐字相同；从 `解析迁移合同` 到成员区前的排除私有段逐字相同。
- 数据头、核心 L1、状态、动态、因果、启动、运行期上下文、工程、filters、自检、集成验收、规范、详细设计、流程图、计划和计划索引均未修改。
- 构建和模块扫描只使用系统临时目录，没有运行生成的 `海中鱼巣.exe` 或真实消费者。

## 4. 实际验证与声明边界

Debug / Release x64 完整 `Rebuild` 均退出 0；每个配置只有一条 `MSB8029` 临时输出目录不适合增量构建提示，本轮为完整重建，未依赖增量结果。双配置 MSVC 模块扫描均为 `sources=136, modules=136, failed=0`，拓扑 136、循环 0、外部需求 0。strict、目标差异检查和静态门禁结果记录于配套验证记录。

本施工只闭合 M02 目标判断比较合同登记与合法当前读取的中性 CRUD 代码候选。没有新增专项 L1 驱动，没有运行生产程序或真实消费者，因此不证明首次、精确重复、同键异义、旧期望、竞争或读回损坏分支已由真实业务运行覆盖；也不证明 M03、运行比较、状态动态联合发布、其它 legacy、核心 L1、集成验收或第一层完善。
