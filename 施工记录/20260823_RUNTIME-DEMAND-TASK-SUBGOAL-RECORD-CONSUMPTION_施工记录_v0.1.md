# RUNTIME-DEMAND-TASK-SUBGOAL-RECORD-CONSUMPTION 施工记录 v0.1

日期：2026-08-23

## 1. 施工身份与 S0

- 计划身份：`RUNTIME-DEMAND-TASK-SUBGOAL-RECORD-CONSUMPTION`。
- 起点：`main == HEAD == origin/main == 678529c09cd23ff16a67d5e0905eae36b54e0d62`；ahead / behind 为`0/0`；index 为空。
- 计划 blob：`654dd527dcd180f34acaf0e11c7684d879535d8d`；详细设计提交：`904ad552b4db0222cc94f1618eeb367add7848ef`；计划索引状态：`可执行`。
- 直接依赖`DATA-L2-TASK-SUBGOAL-ACCEPTANCE-WAITING-RECORD`已满足；S0 已核对计划、详细设计、函数图、正式规范、当前 DTO / 公开接口、代码知识、Git、所有权和验证资源。
- 未发现会改变实现的设计漂移。仓库内异主未跟踪材料原地保留，未进入本切片。

## 2. 实际变更

| 文件 | 实际变更 |
| --- | --- |
| `海中鱼巣/领域/L2需求结构.数据.h` | 类型登记规则升为 2；新增任务子目标记录引用、来源关系类型 / 身份 / 事实、三组原子写 DTO、按记录读取 DTO 及完整性、请求和结果谓词。 |
| `海中鱼巣/领域/服务.L2需求结构.ixx` | 登记来源关系类型键`0x0201'1817`和写入键`0x0201'1A33`；新增按记录反查；扩展新列表、新需求加入列表、既有需求三类 owner 原子写入口。 |
| `海中鱼巣/领域/服务.需求任务子目标承接.ixx` | 新增 DATA 外唯一业务服务；实现预读、一次整组漂移重读、三模式选择、部分发布恢复、需求 owner 后任务 owner 固定顺序及同 G 双向读回。 |
| `海中鱼巣/装配.普通应用.ixx` | 装配唯一业务服务实例，新增 mutable / const getter 和具名构造失败状态 55。 |
| `海中鱼巣/端到端测试.自我治理一致事实.ixx` | 经真实普通应用链新增 V01—V21 专项；动态覆盖可合法构造场景，并明确保留不可注入项。 |
| `海中鱼巣.vcxproj`、`海中鱼巣.vcxproj.filters` | 各以`ClCompile`登记新模块一次。 |

没有修改任务记录 owner、规范、详细设计、流程图、计划、计划索引或共享知识库；没有实现父需求重判、任务唤醒、重新筹办、成员结算、结果共用 / 争用或争用优先级分配。

## 3. 函数级代码知识变更清单

### 3.1 DTO 守卫和结果谓词

| 函数 | 用途与合同 | 直接调用 / 事实 | 依据与验证 |
| --- | --- | --- | --- |
| `L2需求任务子目标来源关系事实截止投影完整` | 验证来源关系当前投影、截止和强类型端点完整。 | 调用通用当前投影完整性；只读关系事实。 | 详细设计 5.1；V02—V18 成功读回和源码守恒。 |
| `L2新增需求并建立列表项及任务子目标来源请求有效` | 校验新列表模式全部强类型字段和两个来源字段一致。 | 只读请求。 | 详细设计 5.1 / 5.3；V01、V02。 |
| `L2新增需求并加入既有列表项及任务子目标来源请求有效` | 校验既有列表模式字段、顺序和来源。 | 只读请求。 | 详细设计 5.1 / 5.3；V03、V05。 |
| `L2为既有需求建立任务子目标来源请求有效` | 校验既有需求模式的子需求、列表项、父、顺序和来源。 | 只读请求。 | 详细设计 5.1 / 5.3；V04、V09—V11。 |
| `L2按任务子目标记录读取需求来源请求有效` | 要求合法请求头和非零记录引用。 | 只读请求。 | 详细设计 5.1；V11、V15、V18。 |
| `L2需求任务子目标来源写结果成功` | 统一验证三类写结果的结果头和父、子、列表、成员、父子、来源六份载荷。 | 调用各事实完整性谓词；只读写结果。 | 详细设计 5.4；V02—V12。 |
| `L2新增需求并建立列表项及任务子目标来源结果::成功` | 固定新列表模式成功形状。 | 调用统一写结果谓词。 | V02。 |
| `L2新增需求并加入既有列表项及任务子目标来源结果::成功` | 固定复用列表模式成功形状。 | 调用统一写结果谓词。 | V03、V05。 |
| `L2为既有需求建立任务子目标来源结果::成功` | 固定复用既有需求模式成功形状。 | 调用统一写结果谓词。 | V04、V09—V11。 |
| `L2按任务子目标记录读取需求来源结果::{成功, 空结果, 唯一结果}` | 分开表达合法读取、零关系和唯一关系；复义不伪装唯一。 | 只读结果头和来源关系组。 | 详细设计 5.1 / 5.4；V07、V11、V15、V18。 |

### 3.2 需求 owner 公开入口

| 函数签名 | 用途和直接调用边 | 事实读写与事务 | 依据与验证 |
| --- | --- | --- | --- |
| `L2按任务子目标记录读取需求来源结果 L2需求结构服务::按任务子目标记录读取需求来源(L2按任务子目标记录读取需求来源请求) const noexcept` | 调用需求 owner 当前投影反查 helper。 | 只读指定 G 的来源关系；不写。 | 详细设计 5.2 / 5.4；V11、V15、V18。 |
| `L2新增需求并建立列表项及任务子目标来源结果 L2需求结构服务::新增需求并建立列表项及任务子目标来源(L2新增需求并建立列表项及任务子目标来源请求) noexcept` | 锁内复用既有需求验证、编码映射和完整读回 helper。 | 一个 owner 写集原子发布需求、列表项、成员、父子和来源关系。 | 详细设计 5.3 模式 A；V02、V09、V10。 |
| `L2新增需求并加入既有列表项及任务子目标来源结果 L2需求结构服务::新增需求并加入既有列表项及任务子目标来源(L2新增需求并加入既有列表项及任务子目标来源请求) noexcept` | 锁内验证列表项和父域后提交。 | 一个 owner 写集原子发布需求、成员、父子和来源关系；不重建列表项。 | 详细设计 5.3 模式 B；V03、V05。 |
| `L2为既有需求建立任务子目标来源结果 L2需求结构服务::为既有需求建立任务子目标来源(L2为既有需求建立任务子目标来源请求) noexcept` | 锁内核对现有子、父、成员和列表项后提交。 | 一个 owner 写集只新增来源关系，随后同 G 读回六份需求侧材料。 | 详细设计 5.3 模式 C / 5.4；V04、V09—V11。 |

### 3.3 需求任务子目标承接业务服务

| 函数 | 用途和直接调用边 | 事实读写 | 依据与验证 |
| --- | --- | --- | --- |
| `请求有效` | 校验业务合同、G、记录、顺序、两个非零且不同幂等身份。 | 零服务调用、零写。 | 详细设计 6.3；V01。 |
| `映射状态` | 穷尽映射 DATA 入口、许可、漂移、幂等、引用和资源状态。 | 不读写事实。 | 详细设计 8 / 9；V10、V17、V21 源码守恒。 |
| `读取需求侧材料组` | 按同 G 读取子、父、父关系、成员和列表项并互证来源、父域、所属和目标。 | 只读六份需求侧材料。 | 详细设计 6.4 / 7；V07、V11、V14、V16—V18。 |
| `提取写结果` | 从三类成功 owner 写结果形成统一需求侧材料。 | 只读写结果。 | 三模式收敛；V02—V04。 |
| `形成等待` | 任务绑定漂移 / 资源失败时保留需求侧材料和原绑定请求。 | 不新增写；不撤销需求事实。 | 详细设计 7 / 8；V13 源码守恒。 |
| `最终读回` | 按绑定截止或预读共同 G 双读记录与来源，最多一次整组最新 G 重读，再读取完整父需求在内六份材料。 | 只读；不以日志或单写结果判成功。 | 详细设计 6.4 / 7；V14、V18。 |
| `需求任务子目标承接结果::成功` | 只接受已承接 / 精确重复且九类载荷、共同 G 和双向端点完整一致。 | 只读结果载荷。 | 详细设计 6.3；V02—V05、V11、V14、V18。 |
| `需求任务子目标承接服务::需求任务子目标承接服务` | 借用唯一需求服务和记录服务，不持有业务账。 | 保存两个服务引用。 | 详细设计 6.2；V19、V20。 |
| `需求任务子目标承接服务::承接任务子目标记录` | 预读记录 / 来源，恢复已有需求侧事实，选择三种需求结构模式，先需求 owner 后任务 owner，最后同 G 双读回。 | 最多一次需求 owner 原子发布和一次任务 owner 独立绑定；跨 owner 不伪装原子。 | 同名函数流程图及详细设计 6—9；V01—V18。 |

### 3.4 装配和专项

- `普通应用上下文::取得需求任务子目标承接服务()`两重载：返回同一服务地址；V19 Debug / Release 动态通过。
- `普通应用上下文`构造与`构造普通应用上下文`：在需求服务和记录服务形成后构造唯一业务服务；新增状态`需求任务子目标承接服务构造失败 = 55`。
- `验证需求消费任务子目标承接记录闭环`：经普通应用 getter 建立真实任务、记录、父需求、列表项和子需求材料；覆盖 V01—V21 的动态、静态和 NOT_RUN 分账。

上述新增 / 修改函数待代码发布后由能力证据目录与代码知识库维护智能体更新用途卡和当前性聚合；本执行切片未直接修改共享知识库。

## 4. 实际验证

- 最终验证根：`D:\TEMP\海中鱼巣\RUNTIME-DEMAND-TASK-SUBGOAL-RECORD-CONSUMPTION\20260823-FINAL3`。
- Debug / Release 隔离`Rebuild`退出码均为 0；构建日志分别为`Debug/rebuild.log`和`Release/rebuild.log`。
- Debug / Release `--test-治理`退出码均为 0；两份 stderr 均为 0 字节；新增专项按 V 项分账，完整治理链末行均为`[治理测试] 全部通过`。
- `python .\tools\check_specs.py --strict`：116 份目录项全部通过。
- UTF-8 严格解码、静态禁项和工程登记检查通过；`git diff --check`和`git diff --cached --check`通过。逐项证据见同名验证记录。

## 5. 发布与完成边界

本记录落盘时发布动作尚未发生；发布只允许暂存计划九个白名单文件并普通推送，最终提交、`HEAD == origin/main`、ahead / behind、index 和异主 WIP 保留事实以发布后 Git 回读及最终交接为准，不在动作发生前预写成功。

本切片完成只证明任务子目标承接记录的需求建立 / 复用、需求 owner 强类型来源、任务 owner 绑定和同 G 双向读回。逐需求完成后单独核算、父需求派生、任务虚拟存在中的中间信息、条件组形成、成员结算、结果可共用 / 争用裁决，以及争用时按正式优先级依次满足，均保持为后继范围。
