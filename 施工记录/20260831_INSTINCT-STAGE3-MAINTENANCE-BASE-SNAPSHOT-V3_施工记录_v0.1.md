# INSTINCT-STAGE3-MAINTENANCE-BASE-SNAPSHOT-V3 施工记录 v0.1

日期：2026-08-31

## 1. 施工身份与边界

- 计划：`INSTINCT-STAGE3-MAINTENANCE-BASE-SNAPSHOT-V3 v0.1`
- plan blob：`44a7b352106869017bd187241eb015f199784019`
- S0 起点：`4bc4eed084f2f8c2ca8a5754f03e32878bfef67a`
- 隔离验证根：`D:/TEMP/海中鱼巣/INSTINCT-STAGE3-MAINTENANCE-BASE-SNAPSHOT-V3`

本次只修改计划允许的两个生产文件、一个既有专项和本记录/验证记录。工作区中 SELF-GOVERNANCE、工程文件、启动、游标 owner、线程及规范 WIP 均原地保留，未清理、暂存、覆盖或带入本切片。

当前正式阈值保持：

```text
值域 = 0..INT64_MAX
L = 2767011611056432742
H = 7378697629483820645
安全根定义版本 = 1
主动安全结算规则版本 = 1
```

“浮动”只表示未来可发布新定义版本；组合器不按百分比重算、不原地修改版本 1，也不缓存 L/H。

## 2. 实际变更

1. 在 `本能被动维护基础快照.数据.h` 追加 v2 请求、快照、16 状态结果、逐字段相等、完整性、成功和可维护谓词；v1 原定义保持。
2. 在既有组合器追加 `读取本能被动维护基础快照_v2`：两次读取 v3 游标，绑定同一 G0 的安全根定义/A、服务值 V 和完整秒观察，并逐字段互证两个事实水位。
3. v2 只携带水位，不读取历史事实范围、不推进游标、不写入 A/V/状态/动态，也不调用 v1 游标读取。
4. 专项增加独立 v2 会话，覆盖未建立、待迁移、v3 成功、水位保留、定义缺失、无新秒、1/N 秒、运行代次/G0/纪元/时间源/倒退和坏实例；原 v1 P/A/Q 回归保持独立，避免 v2 的账登记污染 v1 前置矩阵。

## 3. 函数级代码知识变更清单

| 函数/身份 | 变化 | 用途、合同与调用边 | 待知识维护 |
| --- | --- | --- | --- |
| `本能被动维护基础快照完整_v2` | 新增 | 验证锚点、定义/A、V、v3 三水位、时间观察、0/N 区间和同 G0 形状；由 v2 成功谓词和组合器返回前调用 | 建立 v2 完整性用途卡 |
| `本能被动维护基础快照读取结果_v2::成功` | 新增 | 只接受已读取/无新完整秒、完整快照和同截止 | 建立结果谓词用途卡 |
| `本能被动维护基础快照读取结果_v2::可维护` | 新增 | 只接受已读取、成功且区间非空非零 | 建立维护门禁用途卡 |
| `稳定权威字段相同_v2` | 新增，module-private | 逐字段比较 v2 稳定权威材料，不比较缓存或重建值 | 记录为内部复用 helper |
| `本能被动维护基础快照组合器::读取本能被动维护基础快照_v2` | 新增 | 正式公开只读入口；调用 v3 游标两次及既有时钟、安全根、特征当前值服务 | 建立公开 provider 用途卡；后继历史事实段消费 |
| `读取服务历史覆盖起点`、`建立游标_v3`、`形成请求_v2`、`空失败_v2`、`运行v2基础快照场景` | 新增测试 helper | 只为专项建立合法 v3 fixture 和校验 v2 形状 | 标记测试专用，不登记生产能力 |
| `运行本能被动维护基础快照端到端测试` | 修改 | 保留 v1 P/A/Q 回归并追加隔离 V200-V209 | 更新专项用途说明 |

退出函数：无。共享知识库不在本计划白名单内，待能力证据/代码知识维护者按发布后 source blob 更新。

## 4. 施工异常与处理

1. 当前共享工作树全量编译被异主 SELF-GOVERNANCE WIP 中 `任务执行派发门控` 读取不存在的 `规则宇宙` 字段阻断。本切片未修改该文件，改用 HEAD 干净导出树覆盖三个候选源码验证。
2. 默认 v145 全链接仍出现当前基线既有跨模块未解析符号；同一干净候选的 Debug/Release 默认 `ClCompile` 均通过，按既有本能专项方法以 v143 完整 Rebuild、链接和运行均通过。未使用 `/FORCE:UNRESOLVED`。
3. 初版将 v2 fixture 放入 v1 会话，提前登记永久变化账，破坏 v1 A01 前置。最终改为独立 v2 会话，v1 全回归恢复通过。

## 5. 完成边界

本结果只证明 v3 三水位基础快照 v2 provider 可用，并保留 v1。尚未实现历史事实范围分段、完整维护事务、游标推进、A/V 被动变化或阶段三生产循环。
