# INSTINCT-STAGE3-SERVICE-ACTIVITY-TYPED-BINDING-V2 施工记录 v0.1

日期：2026-08-30

计划身份：`INSTINCT-STAGE3-SERVICE-ACTIVITY-TYPED-BINDING-V2`

## 1. S0 与范围

- 起点：`main == origin/main == 00d8114c78b0e64428c495e9d1b9eabfb02ebeea`，ahead/behind `0/0`，index 空。
- `计划/计划索引.md` 唯一可执行项与计划 blob `31111181c3928d4bedca66c4a4c2e0f0096e0019` 一致。
- 三个生产/专项目标文件在起点 clean；工作区其它 SELF-GOVERNANCE、工程与文档 WIP 原地保留。
- 当前生产安全根定义版本、`L/H`、A/V、游标、状态/动态和普通应用装配均未修改。

## 2. 实际改动

1. `服务合同事实权威.数据.h` 新增进展/准备 v2 DTO、强类型任务执行绑定、完整性、规范成员见证和结果成功谓词；v1 布局保持不变。
2. `服务.服务合同事实权威.ixx` 公开导入现有 L2任务结构模块，在同一 owner 内增加独立 v2 登记、`0x0307'3030..304F` 键段、编解码、闭包读回和两个公开入口。
3. v2 持久事实只保存 task owner 的选择记录、冻结材料、实例方法及筹办/执行轮次强类型引用；不保存安全门禁结果，不从 v1 三个不透明见证投影或回落。
4. 验证宏 fixture 增加 v2 0/N、精确重放、坏绑定、坏载荷、错端点、重复成员、登记版本损坏、漂移和资源失败材料；生产宏关闭时无写入口。
5. `端到端测试.服务合同事实权威.ixx` 保留 P00—P24、Q00—Q06，并增加 V00—V04（含 V02A/V02B）公开入口矩阵。

## 3. 函数级代码知识变更清单

| 函数 / 谓词 | 变化 | 用途与合同 | 当前调用边 / 待回写 |
| --- | --- | --- | --- |
| `服务活动任务执行绑定完整_v2` | 新增 | 三个 task owner 强类型身份及两个轮次必须有效 | v2 事实完整性、专项、ABI consumer；待新增用途卡 |
| `服务合同关联进展事实完整_v2` | 新增 | 验证 v2 进展完整形状、时间窗、版本、生命周期与执行绑定 | v2 结果成功谓词和解码器；待新增用途卡 |
| `服务准备当前事实完整_v2` | 新增 | 验证来源二选一、T→D 形状、完成形状、时间窗及执行绑定 | v2 结果成功谓词和解码器；待新增用途卡 |
| `服务进展完整集合读取结果_v2::成功` | 新增 | 只接受已读取、完整见证、同截止、规范身份和全部完整成员 | 两个公开入口后置检查、专项、ABI consumer；待新增用途卡 |
| `服务准备完整集合读取结果_v2::成功` | 新增 | 同上，面向准备事实 | 两个公开入口后置检查、专项、ABI consumer；待新增用途卡 |
| `初始化进展v2结构登记` / `初始化准备v2结构登记` | 新增 | 幂等建立并正式读回两个独立 v2 登记 | `服务合同事实权威服务` 构造函数；module-private |
| `编码进展_v2` / `解码进展_v2` | 新增 | 固定 23 项载荷并由事实代次恢复生命周期 | v2 fixture、v2 进展读取；module-private |
| `编码准备_v2` / `解码准备_v2` | 新增 | 固定 29 项载荷并保留来源、完成及强类型绑定形状 | v2 fixture、v2 准备读取；module-private |
| `读取当前服务合同关联进展完整集合_v2` | 新增公开入口 | 在 G0 读取独立登记、完整成员及全部具名关系并 fail-closed | 当前只有专项和 ABI consumer；后继有效活动组合器待接入、待制卡 |
| `读取当前服务准备完整集合_v2` | 新增公开入口 | 同上，面向准备集合 | 当前只有专项和 ABI consumer；后继有效活动组合器待接入、待制卡 |
| `ARCH_建立v2验证样本` / `ARCH_损坏v2结构登记` | 新增验证入口 | 仅验证宏内构造真实 v2 闭包与失败材料 | 服务合同专项；生产不可见 |
| `运行服务合同事实权威端到端测试` | 修改 | 增加 V00—V04，保留全部 v1/终态回归 | 仓库外专项入口；待更新专项用途卡 |

退出函数：无。v1 入口和三个 v1 不透明见证保留源码兼容，但没有被 v2 消费。

## 4. 实施边界

本切片只证明服务进展/准备 v2 可承载并完整读回 task owner 强类型执行绑定。没有实现生产发布者、真实执行互证、T→D 当前性、安全门禁、有效服务活动或阶段三闭环。
