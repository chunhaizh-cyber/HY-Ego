# INSTANCE-FEATURE-IF-R-CONCEPT-FOUNDATION 存在类 IFR 提供者施工记录 v0.1

计划：`INSTANCE-FEATURE-IF-R-CONCEPT-FOUNDATION` v0.9。施工起点：`04b254eec57f73653c36caba09f69e6aa66ce439`。

## 本段结果

仅在 `海中鱼巣/领域/数据服务.存在类.h` 落入实例特征结构交付 v2、v1 到 v2 初始化升级、IF/R 完整读取以及目标结构提交。R项材料由存在 owner 作为 R项所属/来源的单一 `U64组` 值事实读写；不同目标建立完整新版本及新 R项，旧版本关系退出。不存在 R 到 FCr 的调用或概念关系写入。

同键幂等比较只绑定实际决定 L1 规范化写集的持久写入请求；五项读回预算只约束本次读取。已发布未知由进程内待确认请求按同一持久写入语义收束；同键零写不会占用写入身份。

## 代码知识变更清单

- 新增 `存在类数据服务::读取实例特征IFR`：同截止投影 IF、F、当前 R集合版本、R项及其材料值事实。
- 新增 `存在类数据服务::提交实例特征IFR目标结构`：核验目标、执行版本换代、材料编码、同键重放与写后读回。
- 修改 `存在类数据服务::登记实例特征结构`：交付 v2 的第七项 `R项材料属性类型`，兼容已发布 v1 六关系布局的升级读回。
- 新增私有 `IFR持久请求相同`：仅用于已可能发布的同键请求收束，忽略读回预算。

本记录的初始发布后，继续落入下列 L3 观察段；普通应用装配仍未实施，本记录不代表计划完成。

## L3 观察段补充

`应用服务.特征概念类.h/.cpp` 增加观察请求、结果、结构诊断回调及 `处理实例特征R观察`。它只读已发布 IF/R，调用既有特征 R 归组；I64 形成新 R 时才调用既有原子准确特征出生服务，再以出生后的截止重新读取并提交完整目标。非 I64 与 FT 规则未启用均零写返回 `比较未启用`。R 发布后立即结束，不调用 `归并特征R概念`、不建立 FCr、不写概念上下位关系。

新增调用边：`处理实例特征R观察 -> 读取实例特征IFR / 归组特征R / 处理I64原子准确特征出生 / 提交实例特征IFR目标结构`。观察请求的 `R规则预算` 原样传给归组入口；首次空 R 仅将归组请求的 R 形状预算置零。


## 验证与边界

已运行 `git diff --check`。用户已明确取消构建、运行、专项驱动和行为夹具验证；未执行这些项目，也不据此声明编译、运行、恢复或业务闭环成立。

## 普通应用装配与代码完成

`装配.普通应用.cpp` 已以 `IFRSTRV2` 登记实例特征结构，并向五参 `特征概念应用服务` 注入存在类和私有结构诊断回调。诊断尽力先记录逻辑错误日志、再弹出错误窗口；异常不会改写观察结果。装配不预建 IF/R，也不调用观察入口。

本计划允许的四份生产文件均已完成修改。最终仅执行差异格式检查；构建、运行、恢复、专项驱动和行为夹具仍按用户指令未执行。因此本计划只在“代码已按冻结合同落入”的范围内完成，不证明工程可编译、普通应用可启动或业务闭环。
