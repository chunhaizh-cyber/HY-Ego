# L2-STRUCTURE-CRUD-REBUILD-DYN2 施工记录

日期：2026-08-12

## 施工身份

- 计划：`L2-STRUCTURE-CRUD-REBUILD-DYN2` v0.5
- 起点：`71c985eb6b784078e461a91917dd3bfd2b523ea2`
- plan blob：`993ea2a012a88dfc75a7ed53ff38c4d6c2cf70f7`
- 索引状态：`可执行`
- 施工范围：两个动态生产文件、三份临时专项文件和本记录及验证记录，共七路径。

## S0

重新核对 `main == origin/main`、ahead/behind `0/0`、Git index 空、七路径所有权、DYN1/E-ID/F-ID/T1/T2 公共 ABI、同一普通应用上下文四服务 getter、L1 owner-aware 一致当前投影及构建资源。异主流程图、规范、路线和其它计划 WIP 原地保留，未纳入本切片。

## 实际实施

1. 在 `L2动态结构.数据.h` 增加动态引用角色、强类型目标、引用替换、时间值换代和退出 DTO、纯值有效性与成功边界。
2. 在唯一 `L2动态结构服务` 增加：
   - `替换动态引用`：主体、前状态、后状态一次只替换一个角色；
   - `换代动态时间值`：起始或结束一次只换代一个值；
   - `退出动态`：动态节点及四关系、两值七事实同写退出。
3. 保持生产服务只 import L1、存在和状态服务，零 F-ID 生产依赖；F-ID 只用于生产外公共夹具。
4. 按 v0.5 删除 `读取当前动态结构类型登记` 及上述三条 DYN2 写函数中登记读取后的旧中性代次探测。登记读取使用缓存登记截止发起 owner-aware 一致当前投影，漂移时只使用投影回显截止再试一次；三写使用登记结果的 owner-aware 截止判断是否读取当前完整动态或直接提交原幂等写集。提交成功后的首次历史读回以 `max(登记结果.事实截止代次, 写入.事实代次)` 形成请求头，并仍以 `写入.事实代次` 为历史截止：首次提交取首次写入代次，后继推进后的精确重复取登记已读回的当前截止，因此只重建首次事实，不混入未来端点或未来值。
5. 新建未登记到生产工程的临时参数专项工程、验证源码和运行脚本。验证器只构造一个普通应用上下文，经 E-ID -> F-ID -> T1/T2 -> DYN1/DYN2 公共链建立夹具。

## 完成边界

本施工只实现 DYN2 单角色引用替换、时间值换代、七事实退出、当前/历史读回及生产对 L1 通用引用冲突的透传。运行矩阵只覆盖 F-ID 公共入口可达的值来源和独立材料两类外部引用；不证明外部关系、以动态为所属节点的外部值、C1/C2、A1、生产消费者、恢复、跨进程或独立集成验收。

三份临时专项文件继续保留，等待结果经交互接受且计划索引退出后由独立清理提交删除。
