# L2-FEATURE-COMPARISON-STATE-TRANSITION-CONTRACT-NEUTRAL-CRUD-MIGRATION v0.1 施工记录

日期：2026-08-08

## 1. 施工身份与起点

- 计划：`L2-FEATURE-COMPARISON-STATE-TRANSITION-CONTRACT-NEUTRAL-CRUD-MIGRATION v0.1`
- plan blob：`6633b5bec42c5cf6ffceafafcf16f634c62fcdad`
- 设计提交：`9300f710d2c976dcf67b48d07101ecbf2c20e280`
- 计划索引 blob：`1e9a44b2e3c05e7520f5f57781d79fdc2aa73e40`
- 目标服务起点 blob：`14dd1eb9804fa81d9c3c0b8bebd715354a29e1d9`
- 正式施工起点：`fdaf20cbf63fc22bab8d066f14f879b93ec083ec`

S0 复核时 `main == origin/main`，ahead / behind 为 `0 / 0`，Git index 为空，本计划是计划索引中唯一 `可执行` 项。计划、索引、目标服务、数据头和六个中性 L1 provider blob 均与详细设计冻结值一致。P0-FIX 文档、路线输入、异主 `.codex-build/` 和两份 WORLD-TREE 计划 WIP 原样保留，不纳入本轮施工或发布范围。

## 2. 实际施工范围

本轮只修改或新增三个计划白名单路径：

1. `海中鱼巣/领域/服务.特征比较.ixx`
2. `施工记录/20260808_L2-FEATURE-COMPARISON-STATE-TRANSITION-CONTRACT-NEUTRAL-CRUD-MIGRATION_施工记录.md`
3. `验证记录/20260808_L2-FEATURE-COMPARISON-STATE-TRANSITION-CONTRACT-NEUTRAL-CRUD-MIGRATION_构建与状态迁移比较合同验证.md`

生产代码实际变化：

- 保留 `0x0E` 迁移合同幂等域，新增 M03 专用中性幂等键映射；M01 `0x08` 和 M02 `0x09` 路径保持不变。
- 新增 M03 合同完整性、稳定定位、完整请求对应、固定配置材料、七关系目标、固定中性写集和合同见证 helper。
- 新增 `状态迁移合同锁_`，使 M03 登记与公开当前读取整段串行；两条根均按“专属锁后短暂取得现有缓存锁”的唯一顺序访问缓存。
- 保留 `迁移合同请求_` / `迁移合同_` 成组缓存。完整原请求才能进入预期精确重复；异义返回 `入口拒绝/注册不唯一`；缓存半结构、登记或合同定位矛盾返回内部不一致。缓存只提供业务幂等和稳定定位，不直接形成成功。
- 首次路径以 M01 `读取当前登记()`、G0、定义节点、定义三值、七个关系目标节点和 G1 形成同代次输入束；只对首末代次竞争完整重试一次，不在提交前以 G0 与请求期望代次比较阻断请求。
- 固定形成 `1 节点 / 7 关系 / 1 值 / 1 属性槽变更 / 0 退出` 的中性写集；配置材料保持 20 项，关系角色保持 1—7，本地键保持 1—9。
- `登记I64状态迁移比较合同` 删除 M03 的 `0x15000803U`、G8 凭证、幂等预探测、完整快照、legacy 写集和 `提交写集`；每个可形成完整写集的请求物理调用 `提交中性写集` 恰一次。
- 首次成功和精确重复分别核对路径、发布标志、重试边界、回显幂等键、非零事实代次和九映射完整唯一，再执行 M03 权威中性精确读回；成功后才成组更新缓存。
- M03 精确读回以完整登记请求和合同定位为见证，读取 M01 当前登记、G0、注册节点、配置值、当前属性、七关系源组、七目标节点、定义三值和 G1，并核对固定材料、角色 1—7、定义值属性类型、九项共同创建代次及可选写入映射。
- `读取I64状态迁移比较合同` 成组复制完整请求与合同定位，先核对缓存和公开请求，再调用同一中性精确读回，不再直接返回缓存或调用完整快照。
- 静态确认后删除只服务旧 M03 的三项 G8 字段编码、G8 凭证、legacy 幂等键 / 映射 / 状态 helper 和 `读取I64状态迁移比较合同内部`。runtime `解析合同`、`解析迁移合同` 与完整快照消费保留不变。

## 3. 排除范围与同一性

- M01 / M02 的全部公开段相对目标服务起点逐字相同。
- M01 / M02 的中性读取和精确读回 helper 相对起点逐函数相同。
- 从 `比较I64状态迁移` 到公开区结束的三个运行比较公开段相对起点逐字相同。
- runtime `解析合同` 与 `解析迁移合同` 逐函数相同；状态动态联合发布目标外文件未修改。
- 数据头、核心 L1、状态、动态、因果、启动、运行期上下文、工程、filters、自检、集成验收、规范、详细设计、流程图、计划和计划索引均未修改。
- 构建和模块扫描只使用系统临时目录，没有运行生成的 `海中鱼巣.exe` 或真实消费者。

## 4. 实际验证与声明边界

Debug / Release x64 完整 `Rebuild` 均退出 0；每个配置只有一条 `MSB8029` 临时输出目录不适合增量构建提示，本轮为完整重建，未依赖增量结果。双配置 MSVC 模块扫描均为 `sources=136, modules=136, failed=0`，拓扑 136、循环 0、外部需求 0。strict、目标差异检查和静态门禁结果记录于配套验证记录。

本施工只闭合 M03 状态迁移比较合同登记与合法当前读取的中性 CRUD 代码候选。没有新增专项 L1 测试或外部补格驱动，没有运行生产程序或真实消费者，因此不证明首次、精确重复、同键异义、旧期望、竞争或读回损坏分支已由真实业务运行覆盖；也不证明 M01 / M02 重验、三个运行比较、状态动态联合发布、其它 legacy、核心 L1、集成验收或第一层完善。
