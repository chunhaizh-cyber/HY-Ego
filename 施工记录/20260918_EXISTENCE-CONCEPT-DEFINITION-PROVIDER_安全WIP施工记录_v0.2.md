# 存在概念两组定义提供者安全 WIP 施工记录

计划：`EXISTENCE-CONCEPT-DEFINITION-PROVIDER-REBUILD` v0.7

起点：`b2c3b9034921c57f4020641f9f4c36f374b199fe`（`main` 与 `origin/main` 当时一致）。

状态：安全 WIP，**计划未完成**；本记录不改变计划、规范或索引状态。

## 本轮实际修改

- `海中鱼巣/领域/合同.相关概念添加参与.h`
  - 增加 `存在概念两组定义关系事实_v1`，使两组定义事实不再以旧 pure 定义关系事实 DTO 承载。
- `海中鱼巣/领域/数据服务.概念树类.cpp`
  - 五个两组入口及私有读回/规范化链仅在调用期接收同一 `特征值域比较数据服务` 与完整 `特征值域比较预算_v1`；先核验 `绑定于(l1_)`，再原样发起 `特征值域读取请求_v1`，未从两组预算补造比较预算。
  - 对自身 `FT→FC` 组分别拒绝重复 FT 和重复 FC。
  - 两组结构登记及特征概念出生使用登记在写入前以同 owner、同 `G0` 对 pure v2 的 15 节点、13 条历史类型登记关系和格式版本历史值进行读回核验；不读取 owner 空域。
- `海中鱼巣/装配.普通应用.cpp`
  - 调整登记顺序为 pure v2、特征概念出生使用、存在概念两组结构；概念树构造后构造特征值域比较服务，概念树不缓存其引用。

## 未完成及阻断

旧 `纯合取存在概念定义` / `通用存在概念定义` 的物理退出没有执行。全仓生产调用确认其仍被 `海中鱼巣/业务/应用服务.世界树类.h` 和 `海中鱼巣/业务/应用服务.自我形成.cpp` 使用；两文件属于世界树/存在首次形成专项，且为本计划禁止修改的异主消费者。删除合同 variant 或迁移这些消费者都会越出本计划白名单并改变其 ABI/语义，因此该子项已停止，等待计划支撑裁决。

未新增专项驱动；计划要求的八类可复跑运行断言、Release 构建、运行、恢复与集成验收均未完成。

## 代码知识变更清单

| 函数或类型 | 变化 | 用途与边界 |
| --- | --- | --- |
| `存在概念两组定义关系事实_v1` | 新增 | 两组结构的专属物理关系事实；不承载旧 pure/D/O/R 语义。 |
| `已发布纯概念结构_v2` | 新增/加强 | 对指定 owner、`G0` 的 pure v2 交付执行节点、历史关系与格式值读回；零写入，不扫描 owner 空域。 |
| `规范化两组定义内部` | 修改 | 调用期比较服务/预算贯通，并独立去重 FT、FC 与子 EC。 |
| 五个两组公开入口及 `读取两组定义内部` | 修改 | 拒绝跨 L1 比较服务；调用期传递完整比较预算，不保存引用。 |
| `登记特征概念出生使用结构` / `登记存在概念两组结构_v1` | 修改 | 使用 pure v2 真实读回作为写入前置；不证明全部结构重放/故障分支已经运行验证。 |
| 普通应用概念结构装配 | 修改 | 固定 pure→出生使用→两组结构→概念树→比较服务顺序；不建立业务调用者。 |

共享知识库索引未改；上述条目待后续按已发布代码基线回写。
