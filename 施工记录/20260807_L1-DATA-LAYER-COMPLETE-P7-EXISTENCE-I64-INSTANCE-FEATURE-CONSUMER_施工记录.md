# 实际存在 I64 实例特征真实消费者中性关系写读闭环施工记录

日期：2026-08-07

## 1. 施工身份

- 计划：`L1-DATA-LAYER-COMPLETE-P7-EXISTENCE-I64-INSTANCE-FEATURE-CONSUMER` v0.1
- 计划 blob：`29d305e5e3b5c0138e3764dc5dcacaab8bc50107`
- 起点：`94b77802cd615486c8e179183ffa8210b0cb2177`
- 施工范围：实例特征服务、运行期上下文和本计划两份专属记录。

S0 确认 `main == origin/main`、ahead / behind 为 0 / 0、Git index 空，计划索引中本计划是唯一可执行计划。P0-FIX、路线、旧 WORLD-TREE 与 `.codex-build` 为异主现场，本施工未修改、暂存或夹带这些路径。

## 2. 实际变更

1. `L1实例特征服务` 的生产构造收敛为事实基座、场景结构、特征定义和实际存在组合服务四个必需引用；运行期上下文追加既有实际存在组合服务引用。
2. 登记建立增加 `const` 正式写入口，非 `const` 入口只转发；登记改用四节点中性写集，只保留四个稳定编码定位，每次读取都经首末事实代次和四个中性节点重新形成登记。
3. 实际存在 I64 实例建立增加 `const` 正式写入口，非 `const` 入口只转发；首次写集固定为一个实例槽节点、两条关系、一个 I64 值、一个当前属性槽和零退出事实。
4. 最小幂等交给第一层按完整中性写集裁决。服务内只保留“幂等身份到读取定位”和“宿主定义组合到读取定位”两份非权威索引，不保存请求写集、首次领域事实或首次结果。
5. 实际存在实例正式读取改为：起始事实代次、当前登记、实际存在、特征定义、实例槽、两关系、当前属性槽、当前值、来源节点、结束事实代次；逐字段互证后才形成领域结果，不再通过完整快照扫描证明本路径成功。
6. 未迁移的初始场景实例和两个当前值换代入口继续使用原有业务路径；只把它们对登记事实副本的依赖改为同锁权威登记读回，未扩大本叶完成范围。

## 3. 施工边界

本施工没有新增第一层自检、验收专用入口、业务操作标签、请求意图组、领域结果见证或跨进程恢复材料。它不证明当前值修改、事实退出与历史读取、关系条件查询、其它第二至第五层消费者或整个第一层已经完善。

第一层能力不由本记录自证。代码精确发布后，仍须由生产代码之外的合法第二层消费者通过正式生产会话和有效租约调用实例特征服务，并用正式公开读取入口核对最终权威状态，才能形成该消费者实际覆盖范围内的验收结论。
