# INSTINCT-STAGE3-SERVICE-EXPIRY-CONTRACT-READBACK 施工记录 v0.1

## 1. 身份与边界

- 计划：`INSTINCT-STAGE3-SERVICE-EXPIRY-CONTRACT-READBACK v0.1`
- 计划文件：`计划/20260830_INSTINCT-STAGE3-SERVICE-EXPIRY-CONTRACT-READBACK_服务到期事件合同终态完整读回代码实施计划_v0.1.md`
- 计划 blob：`d04fbf5c982ea84bc5fbb89fa35eeb3f1b5143a6`
- 执行起点：`main@ff6683ea12d62dc791e3f233582dafbb8644f59c`
- 结果提交：本记录与代码同提交；以本文件所在 Git 提交为准。

本记录只陈述计划白名单内三个代码文件和两份专属记录的实际施工。未修改 L1、普通应用、工程文件、线程、任务 / 方法 / 安全 owner、正式规范、详细设计、流程图、计划 / 索引或异主 SELF-GOVERNANCE WIP。

当前生产定义版本保持不变：值域 `0..I64_MAX`，`L=2,767,011,611,056,432,742`，`H=7,378,697,629,483,820,645`。“浮动”仅表示未来可以发布新定义版本，本切片未建立运行期自变规则。

## 2. 已实施内容

1. 在既有 `服务合同事实权威.数据.h` 新增到期事件、原合同和合同当前终态三项值式投影、完整集合见证、读取结果及强完整性 / 成功谓词。请求继续复用既有 `服务到期事件完整集合读取请求_v1`，没有建立第二读取键。
2. 在既有唯一 `服务合同事实权威服务` 新增公开只读入口 `读取到期未满足事件合同终态完整集合_v1`：在同一非零 `G0` 读取到期事件成员、事件到原合同、合同到当前状态三类闭包，逐事件互证合同、自我、需求、提出者、冻结秒、已消费秒、已到期终态和终态版本，按事件身份规范排序后返回 0/N 完整投影及见证。
3. 原合同继续是时间纪元唯一来源；事件事实没有新增重复时间纪元字段。非到期状态、错秒数、错终态版本、错合同关系、缺失 / 重复成员、缺失 / 重复状态和损坏载荷均整体失败，不返回部分投影。
4. 补上事件成员组与事件到合同关系组的等量闭包校验，防止索引漏事件但孤立事件关系仍被忽略后形成假成功。合同状态闭包允许包含 owner 内其它合同状态，但每个事件引用的原合同必须恰有一个当前状态。
5. 只在验证宏内扩展真实 L1 fixture：可形成正式已到期终态，并可分别注入秒数、版本、成员、合同关系和状态关系损坏；生产构建不含这些入口和状态。

## 3. 函数级代码知识变更清单

| 函数 / 类型 | 变化 | 用途、合同与非职责 | 直接调用边 / 待更新用途卡 |
| --- | --- | --- | --- |
| `服务到期事件合同终态投影_v1`、`服务到期事件合同终态完整集合见证_v1`、`服务到期事件合同终态完整集合读取结果_v1` | 新增公开值类型 | 承载同 `G0` 的事件、原合同、当前已到期状态和集合覆盖见证；不裁决本秒到期、最长等待或服务活动 | provider 返回、专项和仓库外 consumer；待新增数据合同用途卡 |
| `服务到期事件合同终态投影完整_v1`、`服务到期事件合同终态完整集合见证完整_v1`、`服务到期事件合同终态完整集合读取结果_v1::成功` | 新增公开纯值函数 | 校验三事实完整性、跨事实字段一致性、规范身份组、截止和强成功形状；不读取或写入事实 | 新公开读取入口、专项与 consumer；待新增数据合同用途卡 |
| `服务合同事实权威服务::读取到期未满足事件合同终态完整集合_v1` | 新增公开只读函数 | 按指定自我和 `G0` 返回 0/N 完整规范事件合同终态投影；全部非成功空载荷、截止 0 | 当前生产调用为 0；后继服务维护时间裁决组合器；待新增服务用途卡 |
| `服务合同事实权威服务::形成事件合同终态读取请求` | 新增 module-private 函数 | 在既有事件读取闭包上追加原合同完整关系和合同当前状态闭包；不暴露 L1 请求或内部 helper | 仅由新公开入口调用；待更新服务内部用途卡 |
| `服务合同事实权威服务::ARCH_建立验证样本_v1`、`形成验证事实写集` | 验证宏内修改 | 形成已到期终态和七类闭包损坏输入，维持真实 L1 两阶段写入、重放和持久恢复 | 仅专项；生产用途卡不登记 |
| `运行服务合同事实权威端到端测试` | 修改 | 保留 P00—P24 回归，并以 Q00—Q06 覆盖新入口完整矩阵；P10 增加第五类集合持久恢复 | 仓库外专项入口；待更新专项用途卡 |

没有退出既有生产函数，没有新增 owner、结构登记、键段、生产写入口、线程、队列、缓存、自动空宇宙或 L/H 调整。共享知识库未由本计划直接修改。

## 4. 验证与完成边界

- 正式隔离候选：`D:/TEMP/海中鱼巣/INSTINCT-STAGE3-SERVICE-EXPIRY-CONTRACT-READBACK/final-20260830-01/source`；由起点 HEAD 展开并只覆盖本计划三个代码文件，三个 SHA-256 与工作区逐项一致。
- v143 x64 Debug / Release 根工程 Rebuild、Debug 专项 P00—P24 + Q00—Q06、Debug 独立 consumer、Release `/GL + /LTCG` 独立 consumer均通过。
- P10 已证明合同、事件、事件合同终态、进展和准备五类完整集合跨持久运行包恢复后逐字段一致。
- 本结果只证明到期事件、原合同和当前到期终态的同 `G0` 完整只读 provider。它不证明事件属于本秒，不裁决最长等待或有效服务活动，不写服务值 / 安全值，不完成阶段三或 `INSTINCT-ROUTE`。
