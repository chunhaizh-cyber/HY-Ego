# L2-SELF-S-HOST 场景宿主存在关系施工记录

计划身份：`L2-SELF-S-HOST v0.1`

执行日期：2026-08-14

起点基线：`cb6f7771275063f4f0f79ff2ce2de89af16b30a5`

plan blob：`2131b50349a11c393cb7477fabf0a2c53dec0fef`

详细设计 blob：`5541dcc6bf7f8daafdfad37f970543f73ada7738`

## 1. S0 与范围

- `main == origin/main == cb6f7771275063f4f0f79ff2ce2de89af16b30a5`，index 空，计划索引状态为 `可执行`。
- R1 与 E-ROLE 已实施、接受、索引退出并完成临时专项清理；冻结键 `0x0201'1601U` 和固定登记幂等身份在最终场景域无冲突。
- 生产只修改场景数据头与场景服务；专项和永久记录均使用计划白名单路径。未修改存在服务、L1、A1、普通应用、启动、计划、索引、规范或流程图。
- 流程图、业务图、路线输入、概念候选与 `.codex-build` 为异主现场，未暂存或带入本段。

## 2. 生产实现

`海中鱼巣/领域/L2场景结构.数据.h` 新增场景宿主关系类型、固定登记、关系事实、新增 / 替换 / 退出、按场景当前 / 历史与按存在反向当前读取 DTO，并扩展场景事实和场景退出闭包字段。全部新增值类型具备 `operator==`，请求有效性、事实完整性和成功载荷形状按详细设计闭合。

`海中鱼巣/领域/服务.L2场景结构.ixx` 在唯一场景 owner 与服务实例内实现：

- 树结构登记之后，以固定本地键 `0x0201'1601U` 和固定幂等身份建立一个普通关系类型节点；登记定位只作查找，每次 owner-aware 读回正式 owner 与类型节点；
- 宿主关系固定为 `正式树内场景 -> 正式存在`、角色 `1`，关系本地写入键为私有实现值 `0x0201'1602U`；
- 新增、替换和退出均沿用原期望代次提交 owner-scoped 写集；替换在同写集建立新关系并退出旧关系；
- 写前正式核对场景身份来源、场景树归属、存在身份来源、按场景单值和按存在反向单值；首次和精确重复均在首次截止正式读回；
- 当前 / 历史完整场景投影携带宿主关系；普通场景退出请求必须与权威快照的可选宿主关系一致，并在同一写集中退出，结果正式历史读回同代退出事实；
- 存在退出 DTO 未修改，当前宿主关系由 L1 当前引用保护自然拒绝目标存在退出。

成员、父子、树归属和宿主关系继续分账；生产代码不解释宿主为自我或内部世界，不解除启动阶段 17，不进入阶段 18。

## 3. 实施期修正

首次根工程 Debug 的真实首错为 `服务.L2场景结构.ixx(4838,25) C2039`：完整场景后置宿主投影误用了不存在的 `L2场景完整读取结果::成功()`。只在服务实现内改用既有结果头 `已读取` 与 `optional<场景>` 形状判断，未新增公开函数或 ABI；重跑 Debug 后 PASS。

## 4. 实际验证

- 根工程 Debug / Release x64 fresh `Rebuild`：均 PASS，分别生成 `x64/Debug/海中鱼巣.exe` 与 `x64/Release/海中鱼巣.exe`。
- 独立专项 Debug / Release x64 `Rebuild` 并运行：均为 19 / 19 PASS、失败数 0。
- `python .\tools\check_specs.py --strict`：113 / 113 PASS。
- 七个当前目标文件严格 UTF-8、无 BOM / `U+FFFD`：7 / 7 PASS；根工程、filters、专项工程 XML：3 / 3 PASS。
- 七路径 `git diff --check`、生产工程 / filters 零专项登记、两个生产文件验证专用痕迹扫描：PASS。
- 专项 `MSB8029` 仅提示 GUID 系统临时目录不适于增量生成；每轮执行 `Rebuild`，编译、链接与程序运行退出码均为 0。

## 5. 完成边界

本结果只证明中性 S-HOST 结构按当前进程专项矩阵实现并可构建运行。真实分配耗尽、私有事实损坏、恢复、跨进程重放、正式启动消费者、SELF-FORM 与独立集成验收均为 `NOT_RUN`。三份专项在交互接受和计划索引正式退出前继续保留；接受退出后必须独立删除并复验。
