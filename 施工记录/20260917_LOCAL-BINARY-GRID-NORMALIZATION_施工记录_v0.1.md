# LOCAL-BINARY-GRID-NORMALIZATION 施工记录

计划：`LOCAL-BINARY-GRID-NORMALIZATION v0.1`

起点：`529b304c945cd2bbf480186728605ac35e0283ab`

## 实际变更

- `数据服务.局部二值格解析类.h` 物理退出 v2 裸 `U64组` 请求和解析入口，建立 v3 强类型解析请求、规范化载荷和结果 DTO。
- `数据服务.局部二值格解析类.cpp` 在既有私有布局/OR/颜色核验职责内重构为：原始完整组规范化、全零三项描述输出，以及只消费规范化载荷的轮廓/体素解析。
- 不增加 L1、保存、装配、DATA-L3 消费者、日志或机器事实写入。

## 代码知识变更清单

| 身份 | 变化与用途 | 副作用/调用边 | 待知识维护 |
| --- | --- | --- | --- |
| `局部二值格解析数据服务::规范化局部二值格(const 局部二值格规范化请求_v1&)` | 新增唯一 DATA-L2 原始完整组核验及全零描述规范化入口。 | 仅读取借入向量；成功返回自有载荷；不写事实。 | 新建用途卡。 |
| `局部二值格解析数据服务::解析局部二值轮廓(const 局部二值格解析请求_v3&)` | v2 入口退出；仅解析轮廓规范化载荷。 | 无 L1/写入/外部调用者。 | 新建或更新用途卡。 |
| `局部二值格解析数据服务::解析局部二值体素(const 局部二值格解析请求_v3&)` | v2 入口退出；仅解析体素规范化载荷。 | 无 L1/写入/外部调用者。 | 新建或更新用途卡。 |
| 私有布局、OR、颜色核验函数 | 由 v2 裸组解析改为供规范化和 v3 解析共同复用。 | 翻译单元私有、零事实副作用。 | 标记为该服务内部实现。 |

本记录形成时尚无模块外生产调用点；知识库中也无本服务用途卡。结果提交形成后由知识维护者基于发布源码回写。

## 完成边界

本切片仅实现 DATA-L2 格式规范化和强类型解析，不证明保存接线、特征采用、DATA-L3 编排、设备输入、恢复或业务闭环。
