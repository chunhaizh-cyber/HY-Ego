# 旧四仓反向依赖闭包退役施工记录

日期：2026-08-10

计划身份：`L1-LEGACY-FOUR-STORE-REVERSE-CLOSURE-REMOVAL v0.1`

施工起点：`81d03e31a5fbd0b8120a0646fa9a83f20c8c6f7d`

## 1. 实际施工

1. 按详细设计第 4 节物理删除 40 个旧闭包文件，其中包括旧主信息、节点、关系、索引四仓八个源码文件，以及只为旧基座服务的世界、存在、场景、状态、动态、特征、概念、语素、需求、任务、方法、控制面板、初始化和旧自我线程链。
2. 从 `海中鱼巣.vcxproj` 删除 38 个仍登记的旧文件项目；从 `海中鱼巣.vcxproj.filters` 删除 36 个仍登记的旧文件过滤器项目。其余被删文件在施工前未登记于对应工程文件。
3. 将 `普通应用上下文` 收束为唯一持有 `L1事实基座服务 L1` 的最小空上下文；保留空配置校验和结构化装配结果，不建立默认世界、场景、自我或其它业务事实。
4. 删除控制面板宿主及旧投影、旧初始化调用。普通控制面板模式固定返回 `宿主失败 + 控制面板窗口`，不返回空成功。
5. 保留停止信号安装、租约恢复和无窗口驻留等待。无窗口宿主不接收应用上下文，也不建立业务事实。
6. `海中鱼巣/入口.cpp`、新 L1 实现以及不依赖旧基座的线程队列、协议、任务管理、运行宿主和外设线程基础模块均未修改。

## 2. 编译收束结果

删除冻结闭包并同步工程登记后，Debug x64 与 Release x64 首轮完整重建均直接通过，没有出现详细设计第 4 节之外的旧函数或旧文件编译依赖。因此没有扩大删除范围，也没有建立兼容类型、转发门面、默认有效事实或空成功。

## 3. 所有权边界

未修改、未暂存、未纳入本施工切片：

- `计划/20260803_后续计划持续制定共识与路线输入.md`
- `.codex-build/`

## 4. 完成边界

本施工只实现旧四仓及其冻结旧业务反向依赖闭包的物理退出，并使程序保留新 L1 最小空装配后可编译链接。不证明新 L1 数据逻辑正确、旧业务已迁移、控制面板可用、L2—L5 已完成或原有业务功能可运行。
