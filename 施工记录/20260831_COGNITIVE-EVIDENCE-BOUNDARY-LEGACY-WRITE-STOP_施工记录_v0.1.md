# COGNITIVE-EVIDENCE-BOUNDARY-LEGACY-WRITE-STOP 施工记录 v0.1

日期：20260831

计划：`COGNITIVE-EVIDENCE-BOUNDARY-LEGACY-WRITE-STOP v0.3`

起点：`4e631436c9e1f0ed4b29354ec2a841beb35eb9de`

## 1. S0 与隔离边界

- `main`、`HEAD`、`origin/main` 一致，ahead/behind 为 `0/0`，施工前 index 为空。
- 计划索引唯一可执行项为本计划，计划 blob 为 `741b36f9c0b6850bf7c4b28071e3ccd875cb9f9c`。
- 正式工作区的两个目标文件同时含有大量未提交 SELF/FRESH WIP；没有整文件暂存、覆盖、回退、stash、clean 或 checkout。
- 以正式 HEAD 导出隔离候选，候选根为：
  `D:\TEMP\海中鱼巣\COGNITIVE-EVIDENCE-BOUNDARY-LEGACY-WRITE-STOP\20260831T134500`。

## 2. 实际代码变更

### 2.1 `L2任务结构服务::新增执行结果待验证状态`

- 请求有效后无条件读取 owner 首次幂等账。
- 无首次账立即返回`入口拒绝`、截止 0、空载荷，不再读取 current 业务事实或形成 proof 写集。
- 已有账核对首次写集 G0、幂等身份、状态/证据本地键、历史投影和请求同义性。
- 只接受 L1 `精确重复`，并核对首次 G1 和完整新编码映射。
- 物理删除无账首次建立治理状态与目标裁决证据的旧路径；旧 DTO、编码、解码、helper 和历史重放保留。

### 2.2 `L2任务结构服务::提交目标未达成待重筹办迁移`

- 同样改为先读首次账；无账立即 fail-closed。
- 物理删除“无同键账但已消费”旁路和首次迁移写入路径。
- 已有账保留 legacy/统一轮次两种历史重放；统一轮次后继键存在但历史投影缺失现在返回`内部不一致`，字段异义仍返回`幂等冲突`。
- 只接受 L1 `精确重复`并核对首次 G1、完整新编码映射。

### 2.3 专项

- 新增`运行认知证据边界无账首次拒绝专项()`，通过普通应用唯一装配取得 task owner 和事实代次读取能力。
- 两个请求均使用 HEAD 既有 DTO 构造语法完整的非零身份材料，不建立旧 proof 测试后门。
- 分别核对首态调用前后、迁移调用前后事实代次不变，并核对状态、截止、变更代次和全部业务载荷为空。
- 正式`运行自我治理一致事实端到端测试()`先执行本专项；其余既有矩阵保持原顺序和语义。

## 3. 函数级代码知识变更清单

| 函数 | 变化 | 当前用途 / 合同 | 调用边变化 | 待更新用途知识 |
| --- | --- | --- | --- | --- |
| `L2任务结构服务::新增执行结果待验证状态` | 修改 | 仅重放已有 legacy 首次账；无账拒绝且零写 | 公开 ABI 不变；旧生产调用仍存在但会 fail-closed | 发布后重绑 task owner 对应用途卡并登记首次 proof 生产退出 |
| `L2任务结构服务::提交目标未达成待重筹办迁移` | 修改 | 仅重放已有 legacy 首次账；无账拒绝且零写 | 公开 ABI 不变；旧重筹办触发调用仍存在但会 fail-closed | 发布后重绑 task owner 对应用途卡并登记首次迁移生产退出 |
| `运行认知证据边界无账首次拒绝专项` | 新增测试函数 | 证明两个 legacy 入口无账拒绝和逐调用零代次变化 | 由正式自我治理一致事实测试根调用 | 测试用途卡登记专项范围，不得登记为生产能力 |
| `运行自我治理一致事实端到端测试` | 修改测试根 | 在其它旧矩阵前执行本专项 | 新增到上述专项的测试调用边 | 测试用途卡登记；全根既有失败边界保留 |

本计划未新增、删除或改变生产公开 DTO、module、数据头、工程登记及普通应用 getter。

## 4. 发布与完成边界

发布必须把隔离候选两个源码作为 blob 精确写入 index，再只加入本施工记录和同名验证记录；禁止 `git add` 工作区同名源码。

本结果只表示：

> 两个 legacy proof 写入口停止无账首次生产；已有账精确重放代码路径保留。

旧生产调用移除、v2 正式选择/current provider、执行后结算、current-value 比较、SELF、FRESH、学习和完整认知闭环均不属于本结果。
