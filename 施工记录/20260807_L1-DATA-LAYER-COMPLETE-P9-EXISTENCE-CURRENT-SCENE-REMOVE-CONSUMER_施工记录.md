# 实际存在当前场景移除真实消费者施工记录

日期：2026-08-07

## 1. 施工身份与范围

- 正式起点：`7ca719b64b3b4258e7dcc53891c5abcb9b54a204`
- 计划身份：实际存在当前场景移除真实消费者中性关系退出历史闭环
- 计划 blob：`dfafa3c37f7c92252e4787cda41ded733c440072`
- 生产代码：`海中鱼巣/领域/L1实际存在.数据.h`、`海中鱼巣/领域/服务.L1场景结构.ixx`、`海中鱼巣/领域/组合.L1实际存在场景.ixx`、`海中鱼巣/启动.运行期上下文.ixx`
- 配套记录：本记录与同名构建、生产链验证记录

施工前 `main == origin/main`、ahead / behind 为 0 / 0，Git index 为空。目标六路径没有异主修改。P0-FIX、路线、旧 WORLD-TREE 和 `.codex-build` 现场原样保留，未修改、未暂存。

## 2. 实际变更

1. 在既有场景结构服务模块内新增直接存在成员关系退出的强类型请求、事实和结果，不修改场景数据头，不建立第二个关系事实所有者。
2. 场景结构服务新增唯一关系退出入口。它在既有场景锁内核对世界登记、初始场景、当前存在节点和当前或历史归属关系，形成零节点、零新关系、零值、零属性槽和一条旧关系退出的中性写集。
3. 当前关系和仍需精确重复裁决的已退出关系都从同一物理提交点进入第一层；精确重复和同键异义由第一层幂等账裁决，场景服务不保存请求、写集或首次结果缓存。
4. 成功类结果重新读取当前关系已退出状态、同编码历史关系、原场景、当前存在节点和首末事实代次；历史退出代次必须等于首次发布代次。
5. 场景接纳读取只把请求中的精确归属关系已退出且无当前事实映射为未找到；场景、成员、关系类型和其它关系矛盾仍保持原有非成功语义。
6. 实际存在数据合同新增当前场景移除请求、已退出归属关系见证、移除事实和结果，以及对应纯值有效性和完整性判断。
7. 实际存在场景组合服务只持有初始场景实际存在原子发布服务、场景结构服务和实际存在服务三个非拥有引用。它完成写前组合事实与资格互证、恰一次场景退出委托、写后资格未变及正式领域未找到互证。
8. 组合服务没有导入或调用第一层，没有形成中性写集、幂等键、领域缓存或组合锁；既有创建入口增加唯一 `const` 实现，非 `const` 入口只透明转发。
9. 运行期上下文按既有对象顺序为组合服务追加场景服务和实际存在服务引用，并新增只读组合服务访问器；没有新增可写访问器。
10. 第一次 Debug 完整重建暴露组合服务缺少标准可选值头；仅在该允许文件的全局模块片段补齐头文件后，Debug 与 Release 完整重建均通过。

## 3. 完成边界

本次施工只形成合法第二层场景与实际存在消费者所需的一条关系退出、当前不可读和历史可回查生产代码候选。它不建立第一层自检、专用验收入口、双目标、日志判定、恢复材料或跨进程恢复。

代码发布后仍须由生产代码之外的合法后续层消费者，经正式生产会话取得只读组合服务，实际创建非启动锚定存在、移除其当前场景关系，并从正式领域入口核对结构化结果和权威读回。该消费者通过后，也只累计证明它实际覆盖的关系退出、当前查询、历史查询、精确重复、同键异义和适用非法请求零变化；不能据此宣称整个第一层已经完善。
