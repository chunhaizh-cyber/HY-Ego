# REMOVE-LEGACY-ACCEPTANCE-CODE v0.2 施工记录

日期：2026-08-07

## 施工身份

```text
计划：REMOVE-LEGACY-ACCEPTANCE-CODE v0.2
plan blob：e5d096f3c989aedd3e03ff51c508e811faf55d20
设计提交：f09a20a70bf37b66138db6bbab7d1a80c1c336d0
施工起点：7b9c6a2dc4ff46e0abae121de953e15bfadecbf1
分支：main
```

施工前确认 `main==origin/main`、ahead/behind 为 0/0、Git index 为空，计划索引中 v0.2 是唯一可执行计划。P0-FIX 四份 tracked WIP、路线输入、两份旧 WORLD-TREE 计划和 `.codex-build/` 属于异主现场，均未修改、暂存或夹带。

## S0 与精确范围

1. 详细设计列出的 72 个旧自检 / 验收源码全部存在；工程中有 71 个对应编译项，筛选器中有 67 个现有节点，4 个既有缺失节点未补造。
2. 详细设计允许修改的 35 个工程 / 生产文件全部存在，且与异主 dirty 零路径重叠。
3. 删除后机械比对为 72 个删除文件、35 个目标修改文件，设计表与实际范围的缺失项和额外项均为 0。

## 实际变更

1. 物理删除 72 个旧验收、自检、夹具和运行器源码；从工程删除 71 个编译项，从筛选器删除 67 个现有节点。
2. 删除生产入口中的 21 个自检 import、验收包装、登记、四类专项路由和四个旧启动参数；启动模式只保留普通控制面板、无窗口常驻和生产运行期。
3. 收敛程序运行结果为模式、状态和失败阶段；删除旧验收状态、专项类别、摘要和聚合字段。生产运行期启动失败映射为既有“初始化失败 + 生产运行期首发”。
4. 删除五类旧验收宏接线、四类自检日志切片和 21 个生产文件中的故障自检宏块，保留宏关闭时的生产候选、确认、撤销、发布和读回逻辑。
5. 删除 L1 仓库、服务、索引、节点直接混合原子写、事件日志路径和 R1/P14/P16 中具名的私有探针、故障注入、访问器和验收诊断注释；正式生产入口和结构化结果保留。

## 施工中处理

- `git diff --check` 首次发现 `核心/日志系统.h` 文件末尾多一个空行，已在原计划文件范围内删除；未发生其它代码修复。
- 计划命令示例引用 `.sln`，当前正式仓库实际解决方案文件是 `海中鱼巣.slnx`；构建按实时工程事实使用 `.slnx`，没有修改设计、计划或工程身份。
- 无窗口验证最初在启动后 3 秒发送停止信号，程序尚未完成初始化并安装停止处理器，操作系统以控制台中断状态结束；该两次结果未计为通过。等待 30 秒后按既有 SIGBREAK 停止租约重验，双配置均正常退出 0，产品代码未因验证过程调整。

## 完成边界

本施工只退出旧生产内验收体系并保持三个生产启动模式可构建、可启动和可正常收口。不实现新 L1 中性 CRUD，不证明外部黑盒 CRUD、生产消费者接线、服务集成、崩溃或跨进程恢复成立。
