# METHOD-REGISTRY-ROOT-EMPTY-DOMAIN-BUDGET-REPAIR 施工记录 v0.1

日期：2026-09-23

计划：`METHOD-REGISTRY-ROOT-EMPTY-DOMAIN-BUDGET-REPAIR` v0.1 / blob `3587326598e2f3c8912ed65158763116b864b80e`

执行起点：`8d89eeb6bd236c935a88a18674406f777b6e3ba6`

代码所有者：当前交互智能体，唯一拥有本计划四个生产文件、两个专项文件和本计划两份记录。

结果提交：待本切片提交形成；以本记录所在提交为准。

## 1. S0 与边界

- `main == origin/main == 8d89eeb6bd236c935a88a18674406f777b6e3ba6`，开工时 index 为空。
- `计划/计划索引.md`将本计划登记为`可执行`；阶段21计划为`待激活`。
- 读取并核对了修复详细设计 v0.1、方法登记根生产初始化详细设计 v0.4、4015/4080/8120的现行路由、CODE-FILE-01、0600、单工作区发布规则和代码知识规则。
- 工作区存在根工程、DATA-L1、普通应用、阶段21、RC-P1、流程图和其它记录的异主 WIP；本切片未修改、清理、暂存或复制这些 WIP 进入验证基线。
- 因共享工作区的DATA-L1头存在异主WIP，验证从正式HEAD导出到`D:\TEMP\海中鱼巣\METHOD-REGISTRY-ROOT-EMPTY-DOMAIN-BUDGET-REPAIR\R1-20260923-151728\source`，仅覆盖本计划四个生产文件和两个专项文件。

## 2. 实际变更

### 2.1 L2方法登记根

- `L2方法登记根初始化请求`末尾增加默认0的`最大空域核验事实数`；合同版本保持1。
- `方法登记根结构状态`末尾增加`数量预算不足`，未重排既有值。
- 初始化入口拒绝预算0；删除L1空域请求中的固定预算`1`，改为原样传递请求预算。
- L1 `数量预算不足`准确映射为L2同名状态，保持空根、`H=0`和零写。
- L2成功谓词要求请求回显中的预算非零。

### 2.2 生产provider

- 增加唯一具名默认`方法登记根生产默认最大空域核验事实数 = 1'048'576ULL`。
- `方法登记根生产初始化请求`末尾增加同名字段并使用该默认；现行`{1}`聚合初始化继续有效。
- `方法登记根生产初始化状态`末尾增加`数量预算不足`。
- provider入口拒绝预算0；首次建立分支将同一预算原样传至L2；L2预算不足准确映射为provider同名状态。
- provider成功谓词要求请求回显中的预算非零。

### 2.3 专项

- 新建独立专项源与工程；不加入根工程，不引用普通应用、启动或阶段21源码。
- 专项从夹具成功发布的4个当前节点和0个关系/值/历史/墓碑，经溢出保护求和得到预算4；用预算3证明不足零写，用预算4建立根。
- 根已存在后以预算1重复，证明预算不进入持久写集或幂等身份。
- 覆盖未知owner结构、旧G0漂移、两进程seed/recover，以及两个公开头的独立C++20翻译。

## 3. 代码知识变更清单

### 3.1 修改函数

1. `bool L2方法登记根初始化结果::成功() const noexcept`
   - 模块/文件：`海中鱼巣/领域/数据服务.方法登记根.cpp`，公开结果谓词。
   - 用途变化：成功条件追加“请求回显预算非零”；不负责选择、扩大或持久化预算。
   - 事实与副作用：纯值式判断，仍无事实读写、副作用、事务或生命周期变化。
   - 调用边：无新增或删除。
   - 用途卡：现行知识库没有可直接复用的完整现行用途卡；发布后应新建或更新该函数用途卡。

2. `L2方法登记根初始化结果 L2方法结构服务::建立或读取方法登记根(const L2方法登记根初始化请求 &)`
   - 模块/文件：`海中鱼巣/领域/数据服务.方法登记根.cpp`，公开L2写入口。
   - 用途变化：入口校验新增非零预算；空域读调用改为原样使用请求预算；新增预算不足准确映射。
   - 明确非职责：不读取全仓计数、不自动倍增、不选择生产默认、不把预算写入根或首次材料。
   - 事实与副作用：既有首次材料、空域读取、单节点写入和正式读回顺序不变；预算不足路径零写。
   - 调用边：仍只调用既有`L1事实基座服务::读取所有者范围空域`和既有owner写端口；无第二入口。
   - 用途卡：发布后应更新。

3. `bool 方法登记根生产初始化结果::成功() const noexcept`
   - 模块/文件：`海中鱼巣/业务/应用服务.方法登记根生产初始化.cpp`，公开结果谓词。
   - 用途变化：成功条件追加“请求回显预算非零”。
   - 事实与副作用：纯值式判断，仍无事实读写、副作用、事务或生命周期变化。
   - 调用边：无新增或删除。
   - 用途卡：发布后应新建或更新。

4. `方法登记根生产初始化结果 方法登记根生产初始化提供者::初始化(const 方法登记根生产初始化请求 &) noexcept`
   - 模块/文件：`海中鱼巣/业务/应用服务.方法登记根生产初始化.cpp`，阶段19生产provider入口。
   - 用途变化：入口校验新增非零预算；缺根时原样传至L2；新增预算不足准确映射。
   - 明确非职责：不自动重试、不覆盖普通应用参数、不修改DATA-L1或阶段21。
   - 事实与副作用：预读、建立、发布未知收敛、单根和最终全组读回顺序不变；预算不足零写返回。
   - 调用边：仍只经同一`L2方法结构聚合服务`取得同一L2服务；无新增provider或旁路。
   - 用途卡：发布后应更新。

### 3.2 新增、退出函数

- 生产函数无新增、无退出。
- 专项内部夹具和断言函数只服务独立验证，不是生产ABI，不建立共享函数用途卡。

### 3.3 DTO与枚举变化

- 修改`L2方法登记根初始化请求`、`方法登记根结构状态`、`方法登记根生产初始化请求`、`方法登记根生产初始化状态`。
- 两个请求均只在末尾追加字段；两个枚举均只在末尾追加状态；版本保持1。

## 4. 验证与偏差处理

- Debug/Release x64隔离Rebuild、内存专项和两进程seed/recover最终全部通过；详见同名验证记录。
- 第一次Debug恢复试跑失败于专项夹具重复提交填充事实时使用了新的G0，属于验证夹具幂等请求不一致。修订为恢复进程只重签owner并读回既有根后，Debug/Release完整复跑通过；生产代码未因该失败调整。
- 资源耗尽和私有损坏无合法注入，保持`NOT_RUN`，未增加生产测试seam。

## 5. 完成边界

本施工只实现方法登记根L2与生产provider的显式空域预算传递和准确状态映射。它不证明默认预算覆盖未来所有仓库规模，不证明根工程、正式启动阶段19、阶段21、自我线程启动或内部治理闭环完成。
