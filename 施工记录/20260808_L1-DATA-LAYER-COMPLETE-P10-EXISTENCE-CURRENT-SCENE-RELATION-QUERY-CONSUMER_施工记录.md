# 实际存在当前场景归属条件查询真实消费者施工记录

日期：2026-08-08

## 1. 施工身份与范围

- 正式起点：`67b0e2f638e9e8d6f70411669c76b90d713c002e`
- 计划身份：实际存在当前场景归属条件查询真实消费者
- 计划 blob：`f09f5d48c74b934d74ab874fc4050cb8b73dd711`
- 生产代码：`海中鱼巣/核心/L1中性CRUD.数据.h`、`海中鱼巣/核心/仓库.L1事实基座.ixx`、`海中鱼巣/核心/服务.L1事实基座.ixx`、`海中鱼巣/领域/服务.L1场景结构.ixx`
- 配套记录：本记录与同名构建、生产链验证记录

施工前 `main == origin/main`，ahead / behind 为 0 / 0，Git index 为空。目标六路径没有异主修改。P0-FIX、路线、旧 WORLD-TREE 和 `.codex-build` 现场原样保留，未修改、未暂存。

## 2. 实际变更

1. 在既有第一层中性数据头新增目标节点与关系类型条件查询的请求和结果。结果返回调用条件、读取事实代次和完整当前关系有序组，不提升合同版本，不修改既有 DTO 或合同模块。
2. 第一层事实仓库状态新增一个非权威派生索引。索引的两级键只含目标节点和关系类型，桶内只保存严格升序、无重复的关系稳定编码，不复制关系事实或生命周期字段。
3. 中性提交与兼容提交两条候选路径都在关系创建时插入索引、在关系退出时删除索引；任何索引维护失败都发生在候选状态内，不交换权威状态。成功仍通过既有一次状态交换同时发布关系事实和索引。
4. 状态完整性检查新增双向互证：每个当前关系恰好出现于自身目标和类型桶，每个索引编码都能回到字段全等的权威当前关系；空桶、零编码、重复、非升序和额外编码均拒绝。
5. 既有状态材料转为当前状态后，只从已经形成的权威当前关系在本进程内纯派生索引。没有修改恢复 DTO、材料格式、恢复入口、状态或验证范围。
6. 仓库条件查询在一次共享锁内只访问请求对应的目标和类型桶，并按桶内编码逐项回读权威当前关系。合法零命中返回成功空组；桶与权威关系矛盾返回内部不一致和空组。
7. 第一层事实基座服务新增唯一只读条件查询入口，保持仓库结构化结果；资源异常映射资源失败，其它异常映射内部不一致。
8. 第二层场景接纳内部实现改为恰一次调用条件关系组查询。零项返回未找到，一项逐字段核对请求归属，多项返回内部不一致；公开请求、结果、构造、锁和调用者均未变化。
9. 普通条件查询没有调用完整快照、历史、审计、恢复、SQL、旧管理索引、日志、状态完整性检查或全量派生函数。没有新增第一层自检、双目标、第二执行程序或生产内通过判定。

## 3. 仓库不变量

- 权威当前关系仍只属于既有第一层事实仓库；派生索引只提供候选编码。
- 关系事实和索引始终在同一候选、同一次权威状态交换后可见，不存在独立发布入口或第二关系仓。
- 第二层继续拥有“场景归属是否唯一、是否匹配”的业务解释；第一层只返回业务中性的条件关系组。
- 生产日志只供人观察，不参与零项、一项或多项判定，也不作为验收证据。

## 4. 完成边界

本次施工只形成按目标节点和关系类型读取当前关系有序组，以及一个真实第二层场景消费者使用该组的生产代码候选。构建、启动和静态门禁通过不等于第一层自证，也不等于第一层已经完善。

代码精确发布后，仍须由生产代码之外的合法后续层消费者，经正式会话和有效租约实际创建、读取、退出并再次读取；只根据公开结构化结果和权威读回累计证明本次覆盖的行为。当前不证明关系替换、源端查询、分页、性能、并发压力、节点或存在删除、崩溃、重启、跨进程恢复、其它消费者、旧栈删除或整个第一层完善。
