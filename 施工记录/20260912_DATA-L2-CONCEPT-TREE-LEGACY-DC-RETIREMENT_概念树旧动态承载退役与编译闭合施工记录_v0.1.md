# 概念树旧动态承载退役与编译闭合施工记录

日期：2026-09-12。切片：DATA-L2-CONCEPT-TREE-LEGACY-DC-RETIREMENT v0.2。执行所有者：`/root/feature_concept_execute`；唯一发布者：`/root`。本稿仅记录施工事实，不改变计划状态。

## 1. S0 与范围

执行起点 `5d420883fecaed2608248b6775a4761a5ac98e4b`；v0.2 增量 S0 的 HEAD/origin 为 `73e0a0f84beca1e6c261d0870634de4abe83ed83`，main、index 空。计划 blob `01688232c757403071646a3f9ad3154a288e1a1d`，设计 blob `b37d2e2b9a17e92665393dbaac9a268705d45ed8`，索引 blob `28cb3ca1c2cfa3792e7a770abfad86caac601744`。起点到此 HEAD 只改三份本计划文档，没有生产依赖变化。

已读 execute 技能、协作/单工作区/知识回写规则、规范目录及本对象直接规范、计划/设计、当前公开接口和调用点。五个生产目标在本片起点均与正式 HEAD 相同；动态概念应用已交接。登记文件 `D:/TEMP/再用文件/再用文件列表.md` 给本执行者七文件及独占验证根。工程仅删动态应用登记，未恢复暂停 feature 的定位模块登记。

知识库入口和现行状态已读。旧用途卡 `知识库/函数用途/L2概念结构服务/内部-直接关系与概念图组.md` 指向旧 `服务.L2概念结构.ixx`，不能作为当前实现依据；本次回读当前 `数据服务.概念树类.ixx` 和真实消费者。共享知识文件未修改，发布后由知识维护者更新。

## 2. 已落盘变更

概念 provider 删除旧动态引用/定义/扩展/时间槽/依据/审计/模板/支持 DTO 及其构造、成员、公开入口、私有解码与写入闭包。世界引用剩 E/F，形成引用保留 E/F/FT/scene；通用定义剩 FC/EC，应用模板剩 `variant<特征概念事实>`。持久种类与关系按具名分支，不把删除后的 variant 序号作为持久类别。L1 本地键/稳定编码及保留 E/F 的内部排序不改。

保留四根及动态根身份读取；旧持久 kind3 必需解码时返回旧格式不支持35、清空载荷。未请求动态根的特征/存在图不受旧节点影响。没有物理删除世界事实、没有创建新 RC/DC、没有补旧动态 import 或兼容 ABI。

SCENE-RANGE-READ-PROVIDER-SOURCE 已经正式 v0.2 裁决：保留 scene 引用/import/同底座谓词，不增加场景成员或构造。`验证世界`/`形成引用组` 的 scene 分支经头/编码/G 守卫返回不支持30；普通和应用释放在首次账读取/重放前拒绝。其它世界的正式 provider 调用保持。

实际消费者暴露统一关联查询的旧内部头冲突：公开形成引用查询要求 H=0，而通用共享读取封装要求具体 H。本片仅对这个强类型分支把内部截止解析为 Gread，仍核原请求 H=0，保留 G/预算守卫；scene 和非scene真实消费者均消费该路径。

EC 应用只机械删除旧动态模板 `holds_alternative` 拒绝行。动态概念应用文件物理删除，主工程/filters 分别删除一行/三行登记。EC 剩余旧 scene API 在正式 HEAD 已悬空，未越过白名单修复。

共用预算 `最大动态槽数/最大动态模板数` 和公开用量 `动态槽数/动态模板数` 保留：EC 225/235/272、FC 324/375 等当前消费者仍读取，符合设计3.2(7)。旧动态专属历史缓存删除，不借名称相似删公共 ABI。

## 3. 保留公开签名与候选身份

下列四个非DC构造施工前后签名相同；完整公开前/后清单和逐项差异保存在独占验证根 `public-before.txt`、`public-after.txt`、`public-signature-diff.txt`。其它保留公开入口参数与返回类型没有改签名；相关通用 variant 按本计划删旧分支。

```cpp
概念树类数据服务(const L1事实基座服务 &, const 特征类数据服务 &, const 存在类数据服务 &,
                     const 特征值类数据服务 &, L1所有者范围写端口 &&, const 概念树结构交付 &,
                     const 概念树共享结构交付 &);
概念树类数据服务(const L1事实基座服务&, const 特征类数据服务&, const 存在类数据服务&,
        const 特征值类数据服务&, L1所有者范围写端口&&, const 概念树结构交付&, const 特征概念结构交付&);
概念树类数据服务(const L1事实基座服务&, const 特征类数据服务&, const 存在类数据服务&,
        const 特征值类数据服务&, L1所有者范围写端口&&, const 概念树结构交付&,
        const 特征概念结构交付&, const 概念树存在扩展交付&);
概念树类数据服务(const L1事实基座服务 &, const 特征类数据服务 &, const 存在类数据服务 &,
                     const 特征值类数据服务 &, L1所有者范围写端口 &&, const 概念树结构交付 &);
```

保留 `使用同一场景基座(const 场景类数据服务&) const noexcept`、`动态根引用() const noexcept`、基础读写、特征读写、存在 v2 及共享入口。退役后的五生产目标如下；blob 是发布前工作区真实对象，不是提交完成声明。

| 路径 | 候选身份 |
| --- | --- |
| `海中鱼巣/领域/数据服务.概念树类.ixx` | `bb15b6c3cce62df102489bb5e034a1e5460f3322`；340354 bytes |
| `海中鱼巣/业务/应用服务.动态概念树类.ixx` | 删除；原 blob `dc8477ebbc1fae60d180b1bbba3a3cf289525001` |
| `海中鱼巣/业务/应用服务.存在概念树类.ixx` | `ac83f5fa2ba743c07fa4c18aeff4b958d4b00103`；47066 bytes |
| `海中鱼巣.vcxproj` | `5b5fa866750cc189ec82055282205692af6f21ab`；15062 bytes |
| `海中鱼巣.vcxproj.filters` | `8c1dade81f6b7284ab34944ca4a6054953289031`；8597 bytes |

生产差异完整文件 `retirement.patch` 的 SHA256：`1076ad8ff24fd792ab973dc0d43c944301c48a75d10751a7158ebc5312cb65af`。

## 4. 函数级代码知识变更清单

所有下列函数身份归属当前概念 provider，应用项另行注明。没有新公开函数、owner 或生产模块。

| 变更函数 | 用途、合同与调用边变化 | 知识维护交接 |
| --- | --- | --- |
| `使用动态世界服务`、`创建动态概念`、`释放动态形成依据`；全部带动态扩展的构造 | 旧公开入口物理退出，不转发、不返回假成功 | 退出旧能力目录项及调用边 |
| `校验动态扩展`、`动态定义完整`、`动态依据完整`、`动态模板`、`验证动态定义`、`验证动态依据`、`审计动态`、`核验动态支持依据`、`读取动态形成依据`、`读取动态特征模板` | 旧动态私有闭包及公开读入口删除 | 退出对应用途卡/图节点 |
| 旧动态结果 `成功()`、`映射(场景类数据状态)`、`映射(动态类数据状态)` | 退出旧结果和错误状态桥接 | 退出签名 |
| 四保留 `概念树类数据服务` 构造、`校验共享扩展`、`校验存在扩展`、`校验特征扩展` | 保持合法交付校验，仅删动态交付交叉项；构造不写根/事实 | 更新构造依赖边 |
| `支持关系类型`、`规范化定义`、`规范写请求`、`请求写头` | 剩余类型显式分派；不保留动态分支或索引类别 | 更新值式合同 |
| `验证世界`、`形成引用组` | E/F/FT 用真实提供者；scene 无提供者明确30；组不伪空成功 | 标注 scene 能力边界 |
| `展开概念`、`支持记录`、`支持组`、`图`、`查找完整定义` | 删除动态解码，kind3 明确35；其余根独立 | 更新直接关系/图组用途卡 |
| `概念完整`、`支持完整`、概念读取/写入/图及应用写入结果 `成功()` | 去旧动态载荷必要条件，保留 FC/EC 的真实载荷条件 | 更新结果判定条目 |
| `形成写集`、`发布结果`、`执行写入` | 非动态事务/原key保留；scene 在首次账之前拒绝 | 更新发布/重放调用边 |
| `应用模板`、`应用判定` | 只返回/判定现行特征事实，不再构造旧动态模板 | 更新模板消费合同 |
| `准备共享治理`、`准备存在写入`、`存在执行` | 去除旧动态分支依赖，保留现行共享/EC 链 | 更新保留调用边 |
| `执行应用概念写入`、`读取应用概念关联` | 删旧动态操作；scene前置30；形成查询H0在内部解析G | 更新统一包装边界 |
| EC `存在概念树应用服务::形成见证` | 只删除不再存在的动态模板拒绝判断；其余场景消费未改 | 用途卡只记机械变化 |
| `应用服务.动态概念树类.ixx` 中全部服务/DTO/方法 | 唯一旧应用整体物理退出及取消工程项 | 退出整模块用途卡/调用图 |

知识状态仍待维护，不把本清单当作共享知识已更新。

## 5. 实际修验与边界

详见同名验证记录。初期 probe01 删除尾 variant 的语法残留、probe02误删仍复用预算字段已在本文件范围修复。probe03 目标概念模块通过，EC248/250旧scene方法错误是范围外正式基线问题。消费者窄化为17模块，probe04/05/06/07/09记录夹具输入或编译问题，后续真实修验保留所有失败日志；未改生产校验迎合夹具。probe08为501/G42；补上普通FC原key和FT实证后的probe10为541/G46。

最终两配置及根隔离结果在验证记录按真实退出码封尾。未声称根工程总体、场景概念应用、feature定位计划、RC/DC新模型、数据库跨进程或正式验收完成。

## 6. 暂停 WIP 与资源

验证根：`D:/TEMP/海中鱼巣/DATA-L2-CONCEPT-TREE-LEGACY-DC-RETIREMENT/01a07099-20260912-code`，仅本owner使用。正式HEAD源码加本片差异形成隔离快照；暂停feature文件不拷入该快照，未复用其可执行文件/测试结果。初始 archive 来自5d420883，v0.2 HEAD73只变文档；最终未改源逐字节与HEAD核对。archive的CRLF差异已在最终冻结前转为HEAD原始字节并记录 `head-byte-normalization.json`；四个改动文件保持live字节一致。最终冻结后不改75输入。

下列12个暂停feature文件逐字节与其 `stable-files.json` 核对保持原样，不纳入本片发布：

- `海中鱼巣/领域/数据服务.定位特征.ixx`：SHA256 `9A6384C5AD6A42BFF558135BFB076665753AEBE0B665B227615C1B50A41EEBD2`。
- `海中鱼巣/领域/数据服务.特征类.ixx`：SHA256 `DDE58B36B68FF9B46BC9EDA8C54CFFCE9D3A2CA6CBFEE17D363C0AA6FA03011E`。
- `海中鱼巣/领域/数据服务.存在类.ixx`：SHA256 `2342A349F3D82AB2167A7C328D81C50EFA4D3E8F348345EFD695BB515CA6C903`。
- `海中鱼巣/领域/数据服务.场景类.ixx`：SHA256 `3BDCB53B4FF3CB11FAF19E061341925F95FB46602B22DC102A1DC2AF700FB175`。
- `海中鱼巣/业务/应用服务.特征概念类.ixx`：SHA256 `BFF92225128721DECEA5CBC5C7B4C703413BFAAA3E9B7B49D1C4A90185DE7E19`。
- `海中鱼巣/业务/应用服务.场景成员概念类.ixx`：SHA256 `B5ACBE7491FDE559553DAC6764FC081937750171E91FAAB4C3940BD0B340D7F0`。
- `验证工具/场景特征实例组织专项验证.cpp`：SHA256 `5EA2F77EA4FDEB0695A59CAB5961ABCE3DD483B9F39406538B3921A5E66E0854`。
- `验证工具/定位特征创建专项验证.cpp`：SHA256 `0D3D0AD3741B708A67319E13E0C0AB3C95BFC1D7A55023F85D3D4AC11CE38848`。
- `验证工具/定位特征创建专项验证.vcxproj`：SHA256 `CFA2F43565694D50BD425476B647AAB821A19987E582EC368B22783C6072CB91`。
- `验证工具/运行定位特征创建专项验证.ps1`：SHA256 `9ACF0C1D73DEADD7AD74E0E446FD041F8BE167AB97525D171028992A1F1D649E`。
- `施工记录/20260912_FEATURE-PARENT-BOUND-CREATION_准确特征定位创建与概念报告施工记录_v0.1.md`：SHA256 `A63E3A92BA16C43814FD0A30AFFBDD6E8DEF92047F7208D748ECED21F6353440`。
- `验证记录/20260912_FEATURE-PARENT-BOUND-CREATION_准确特征定位创建与概念报告验证记录_v0.1.md`：SHA256 `CE984F002271C9397525606FD0A6712AAD08ADFDF5D226AD8EF67D098E99F1BF`。

20260906受保护未跟踪两记录未写入或清理。当前验证根待root只读核验，尚未执行清理；不以临时文件残留冒充功能失败或完成。Git尚未暂存/提交/推送，待root精确发布七文件，计划状态不由执行侧变更。
