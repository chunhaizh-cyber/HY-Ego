# L1-SIMPLIFY-P0-FIX v0.2 施工记录

日期：2026-08-04

## 计划与 S0

- 计划：`L1-SIMPLIFY-P0-FIX v0.2`
- plan blob：`6dd9dc474a0facfa89bfcf3805e5df5935deef38`
- 起点：`main == origin/main == 39cd415b223e6dc117752bbd1f5cd0c53f8e3f0e`
- 白名单：`核心/L1事实基座.数据.h`、`核心/仓库.L1事实基座.ixx`、`核心/自检.L1事实基座.ixx`及本记录、对应验证记录。
- 保留其它所有者 dirty/WIP；未消费世界结构迁移待激活叶，未修改服务、启动、工程、旧基座、领域或 SQL。

## 实施

1. 在三类事实数据头中补齐属性类型表示、完整快照比较合同和审计查询幂等键字段。
2. 在 L1 仓库中实现规范化写集、单次状态交换发布、幂等/审计账、属性槽一致性、恢复候选建立/确认/撤销及 v0.2 恢复事实代次与错误分类。
3. 在值事实显式退出时于同一候选事务中移除指向该值的当前属性槽，保持当前快照完整性；不改变公开服务接口或稳定状态枚举。
4. 在专项自检中覆盖审计键回显、类型表示错配、悬空关系、实际恢复代次、材料不完整与恢复发布/漂移/撤销分支。

## 代码问题收口

首次 Debug 重建发现自检整快照默认比较在 MSVC 模块环境触发 `std::equal_to<void>` 模板错误；改为白名单内显式字段/optional/variant 比较帮助逻辑后恢复编译。该问题未改变机器合同，未形成设计漂移。

## 边界

未改变 11 个服务函数签名、稳定状态枚举、顺序270或顶层22项；未接入运行期生产入口，未迁移领域，未删除旧基座，未建立 SQL/材料持久化或兼容壳。
