# DATA-L2-CAUSAL-CLASS-INSTANCE-CRUD 施工记录

日期：2026-09-04

计划：`DATA-L2-CAUSAL-CLASS-INSTANCE-CRUD` v0.1  
计划 blob：`b927efb87ec948bd53a3de3f09f511bfe7e80989`  
详细设计 blob：`edf33cee86831f4bf1924d5e4c0c376163d2f3e5`  
生产代码基线：`998b0d5a04589df30f8836fc80bb3f377012f709`  
最终执行起点：`d1e48de829284db65a946d1f601cb24b0634dea4`

## 1. S0 与范围

最终收口前重新核对：当前分支为 `main`，HEAD 与 `origin/main` 均为
`d1e48de829284db65a946d1f601cb24b0634dea4`，分歧为 `0/0`；计划索引只登记本计划为当前
`可执行` 项，计划与详细设计 blob 分别和上列值一致。统一占用登记将本切片四个允许文件唯一
交给 `/root/causal_class_exec`，未发现文件、语义或发布资源冲突。

状态、动态、场景服务、L1 事实基座服务和 L1 owner-scoped 公开合同相对生产代码基线的 blob
逐项一致。计划支撑按用户“当前不需要验证”的最新裁决只修改了验证完成边界，没有改变 §2—§6
的机器语义、公开 ABI、owner、事务、字段来源、依赖方向或错误映射，现有代码现场无需随之改义。

## 2. 实际施工

1. 新建模块 `海中鱼巣.领域.数据服务.因果类`，只通过 `数据服务.场景类` 的公开 import 链取得
   L1 公共类型及状态、动态、场景三类公开服务；服务唯一持有一份因果 owner 写端口。
2. 实现冻结的合同版本、状态枚举、成员 / 请求 / 结果 DTO、闭合谓词、构造配置校验和七个公开
   操作：新增、删除、完整当前读取、按状态、按动态证据、按适用场景、按来源查询。
3. 新增入口规范化四个非空组，保留三个有序组的调用方顺序、规范化场景集合；在同一请求截止
   通过状态 / 动态 / 场景公开查询验证结构身份，动作和来源只经 L1 公开当前节点读取。
4. 一次 owner-scoped 写集原子建立因果根、四个值和全部关系；固定互异本地键，提交前失败零写，
   同键同义按首次代次历史读回为精确重复，同键异义返回幂等冲突；提交确认后不能闭合读回时
   返回带非零代次的 `已可能发布`，不伪造回滚或成功载荷。
5. 完整读取逐项互证 owner、类型、材料、角色、顺序、生命周期和计数；四类反向查询复用完整
   闭包、去重并按因果编码升序，预算不足返回空组。删除在一个写集中退出根、四值和全部自有
   关系，保留 L1 外部引用保护并从退出代次读回完整历史，不退出任何被引用目标。
6. 在 `海中鱼巣.vcxproj` 和 `海中鱼巣.vcxproj.filters` 的场景类条目之后各登记一次新模块，
   filters 继续使用相邻数据服务的 `领域` 分组。

## 3. 本轮完成边界与已发生附带事实

用户最新裁决为“当前不需要验证”。按最终计划，本轮没有新建验证记录，也没有把专项、构建、
静态扫描、`git diff --check`、strict 或故障注入作为完成前置。在该裁决正式发布前，执行现场曾
经外置临时驱动取得 `PASS 24 assertions`，并曾完成 Debug / Release x64 隔离构建、静态边界和
strict 检查；这些只记录为此前已发生的附带事实，没有在最终 S0 后重跑，也不据此扩大完成结论。

公开依赖没有合法、确定性的“提交成功后首次读回失败”注入端口，因此未动态注入该分支；代码
保留 `已可能发布` 及原请求幂等重放收束路径，但本记录不声明该分支动态验证通过。真实资源耗尽、
并发长时公平、崩溃 / 跨进程恢复、生产装配、合法业务消费者、旧 L2 迁移、因果真伪、概率推理
和独立服务验收同样未验证。

## 4. 精确文件清单

- `海中鱼巣/领域/数据服务.因果类.ixx`；
- `海中鱼巣.vcxproj`；
- `海中鱼巣.vcxproj.filters`；
- `施工记录/20260904_DATA-L2-CAUSAL-CLASS-INSTANCE-CRUD_施工记录_v0.1.md`。

没有创建本计划验证记录；仓库外临时驱动和隔离构建产物不进入 Git。

## 5. 代码知识变更清单

### 新增公开能力

- 模块：`海中鱼巣.领域.数据服务.因果类`；
- 服务：`因果类数据服务`；
- 七个公开函数：`新增因果`、`删除因果`、`查询因果`、`按状态查询因果`、
  `按动态证据查询因果`、`按适用场景查询因果`、`按来源查询因果`；
- 新增公开 DTO：因果成员、结点、四类查询请求、新增 / 删除 / 单读请求及单项 / 组结果；
- 结果成功谓词只承认闭合完整载荷，空组查询成功合法。

### 新增内部复用点

- 请求规范化、依赖状态映射、写入状态映射、当前 / 历史完整闭包读取；
- 固定本地键写集形成、首次幂等结果恢复、关系候选取得、候选去重排序和预算收口；
- 同截止状态 / 动态 / 场景公开验证，以及动作 / 来源 L1 公共节点验证。

### 变更与退出

- 没有修改或退出既有公开函数、DTO、L1、五个候选类、旧 `L2*` 服务或装配入口；
- 知识库未由本代码切片修改；上述清单交由代码知识库维护所有者在发布后消费。

## 6. 发布声明

本记录与源码、工程登记同一次精确提交并普通推送。该发布只证明实例因果候选数据模块进入 Git，
不证明生产装配、迁移、业务正确、因果业务闭环或独立服务验收完成。
