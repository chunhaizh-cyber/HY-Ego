# INSTINCT-STAGE3-PASSIVE-MAINTENANCE-RULE-KERNEL 施工记录 v0.1

日期：2026-08-29

## 1. 施工身份与边界

- 计划：`INSTINCT-STAGE3-PASSIVE-MAINTENANCE-RULE-KERNEL v0.1`
- plan blob：`ac58117d114aec3bdc8138f88e28fcd063b68b5b`
- 设计提交：`79fa3900edae98468178641bc824d1f340614e08`
- S0 起点：`5e3d3040aaa758cd6178b9da0a1d6ef83a325a99`
- 两配置与专项实际使用的代码候选 tree：`e6a69fb40440831d98f47c8776bec357025b4341`
- 隔离根：`D:/TEMP/海中鱼巣/INSTINCT-STAGE3-PASSIVE-MAINTENANCE-RULE-KERNEL/20260829-190037-r3`

本次只实现单完整秒纯值规则内核、专项和工程登记。未读取事实、时钟或线程，未推进游标，未发布安全值、服务值、状态或动态，也未接入普通应用生产消费。

生产根值域保持 `0..INT64_MAX`。L/H 由调用方按正式安全根定义版本传入；版本 1 当前为 `floor(INT64_MAX*30/100)` 与 `floor(INT64_MAX*80/100)`。算法不硬编码生产阈值，允许后继定义版本改变 L/H；同一已发布定义版本内保持不可变。

## 2. 实际变更

新增：

- `海中鱼巣/领域/本能单完整秒被动维护.数据.h`
- `海中鱼巣/领域/算法.本能单完整秒被动维护.ixx`
- `海中鱼巣/端到端测试.本能单完整秒被动维护.ixx`

工程文件只登记上述三个文件。`海中鱼巣.vcxproj` 与 `海中鱼巣.vcxproj.filters` 的其它工作树变化未进入候选 index。

实现包括：请求和结果强类型合同、三类服务需求形状、活动/主动安全/低位门禁裁决、服务值单秒衰减、30 天门禁、服务比例、生存安全根终止/主动优先/休眠/低位回升/区间保持/高位回落分支，以及检查算术和完整载荷谓词。

编译期发现 MSVC 对包含 `optional<载荷>` 的默认结果相等比较生成删除函数。计划内修复为载荷与结果的显式逐字段相等比较，没有改变公开字段、分支或成功语义。

## 3. 函数级代码知识变更清单

| 函数/身份 | 类型 | 用途与合同 | 调用边与后继 |
| --- | --- | --- | --- |
| `本能单完整秒被动维护载荷完整_v1` | 新增 | 纯值验证已形成载荷的 V/A、变化、比例、阈值、分支和版本闭合 | `本能单完整秒被动维护结果_v1::成功`、根函数返回前调用 |
| `本能单完整秒被动维护结果_v1::成功` | 新增 | 仅 `已形成 + 完整载荷` 成功；其它状态空载荷 | 专项及后继 consumer 调用 |
| `计算本能单完整秒被动维护候选_v1` | 新增 | 对完整请求确定计算一个完整秒的 V/P/A 候选；无事实读写和外部副作用 | 当前只有专项与仓库外 ABI consumer；后继聚合入口待实现 |
| `运行本能单完整秒被动维护端到端测试` | 新增 | 执行 P00—P07 纯值专项 | 仅隔离验证临时入口调用，未接生产入口 |

待能力证据目录/函数用途维护者在结果发布后按最终 source blob 建立用途卡；本记录不直接修改共享知识索引。

## 4. 施工异常与处理

1. 首次使用自定义 `IntDir` 时，MSBuild 模块依赖任务无法建立 tlog 路径；改为在隔离提交树使用工程默认输出目录，未修改正式工程。
2. 首次源码编译发现结果默认比较被删除；改为显式逐字段比较后，普通构建、专项和 ABI consumer 均通过。
3. 正式工程声明的 v145 工具集在本机不可用；全部验证使用已安装 VS 2022 Professional、MSVC 14.44.35207，并以 `/p:PlatformToolset=v143` 覆盖。未修改工程声明。

## 5. 完成边界

本次最多证明单完整秒纯值规则内核可编译、可由独立 consumer 调用并满足 P00—P08。合同/活动/到期/安全门禁的生产完整集合、批量分段、游标推进、A/V 原子发布、状态/动态和整个阶段三仍未完成。
