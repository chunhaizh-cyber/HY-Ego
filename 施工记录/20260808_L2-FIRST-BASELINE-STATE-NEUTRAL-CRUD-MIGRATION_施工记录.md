# L2-FIRST-BASELINE-STATE-NEUTRAL-CRUD-MIGRATION 施工记录

日期：2026-08-08

## 1. 施工身份与起点

- 计划：`L2-FIRST-BASELINE-STATE-NEUTRAL-CRUD-MIGRATION v0.1`
- 计划 blob：`dab405c0af9453e993021ea3f4eb99cdc2d950ab`
- 设计提交：`ed56d50cf1cffa8243d7ec206bf20b668e09a018`
- 施工起点：`ae747ddd3d6b0cccb43f2df3e6ece414dc8dc015`
- S0 时 `main == origin/main`、ahead/behind=`0/0`、index 为空；目标两份代码没有异主 WIP。
- 异主 P0-FIX、路线、`.codex-build` 与两份 WORLD-TREE WIP 均原样保留，未暂存、未覆盖、未清理。

## 2. 实际变更

1. `海中鱼巣/领域/L1状态.数据.h`
   - 新增唯一 `L1首个基准状态幂等域 = 0x0DULL`。
   - 原 `L1状态登记幂等域 = 0x0CULL` 保持不变。
2. `海中鱼巣/领域/服务.L1状态.ixx`
   - 删除只服务首态根的 G7 意图编码、`0x15000602U`、旧幂等预探测、legacy 写入映射和完整快照读回 helper。
   - 首态首次路径以当前事实代次 G 约束 P8、P7、当前值、目标关系组、相关源关系组和端点节点；完整既有首态拒绝，关系形状矛盾返回内部不一致。
   - 固定形成 `2 节点 / 5 关系 / 5 值 / 5 属性槽 / 0 退出` 的中性写集，本地键为 1—12，并物理调用 `提交中性写集` 一次。
   - `首态账_` 保存公开请求、完整中性写集、12 编码读取定位和首次完整结果；同键同请求复用写集再次到达 L1，异请求在 L2 返回幂等冲突。
   - 具名状态读回改为中性当前 / 历史精确读取、当前源关系组核对及 G0/G1 门禁；不再使用完整快照、legacy 当前 / 历史读取或 P7/P8 当前投影。

## 3. 排除项核对

- `建立登记`、`读取当前登记`、相邻前状态选择和后继状态规格形成函数相对施工起点逐字一致。
- 未修改核心 L1、P7、P8、其它领域服务、组合服务、启动、工程、规范、详细设计、流程图、计划或计划索引。
- 未实现第二状态、后继状态写入、状态动态、比较、动态或因果。

## 4. 施工中修正

- 精确重复的中性结果仍回显首次发布代次，而具名读回的 G0/G1 可以是后来推进的当前代次；因此重复读回要求当前读取代次不早于首次发布代次，不错误要求二者相等。
- 同键公开请求比较改用 DTO 默认逐字段相等，覆盖嵌套场景的世界规则版本等全部字段，避免旧手写比较遗漏字段。

## 5. 完成边界

本记录只证明计划白名单内代码已经按冻结设计施工并完成约定构建、扫描、strict 与静态检查。没有运行真实消费者，也没有建立专项驱动、伪 provider 或独立集成通过结论；生产接线、集成验收、第二状态、后继状态、动态、因果、其它 legacy 与核心 L1 完善均未由本切片证明。
