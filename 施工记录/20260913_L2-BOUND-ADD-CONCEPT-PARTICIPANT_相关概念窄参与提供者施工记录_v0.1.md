# L2-BOUND-ADD-CONCEPT-PARTICIPANT 相关概念窄参与提供者施工记录

日期：2026-09-13
计划版本：v0.5
施工起点：`8c6a2ba05cac16b4da99bb3c839f57311eb96bc7`
计划 blob：`03793013ca6d394477cb55b90a43a2ebaca352d2`

## 实际变更

- 新增 `海中鱼巣/领域/合同.相关概念添加参与.ixx`，迁移概念基础纯值类型并定义相关概念类别、方案、挂靠、预算、18 状态、准备片段、完整读回、受限组合提交和参与者接口。合同显式再导出 `海中鱼巣.核心.服务.L1事实基座`，`绑定于`保持强类型服务引用。
- `海中鱼巣/领域/数据服务.概念树类.ixx` 改为再导出新合同；概念服务公开继承窄参与者，增加纯新布局构造和旧/新迁移构造，旧、新布局分别保存在可选交付中。
- 新路径按世界强类型历史见证、类别与定义一致性、同义复用、同类上位和成环条件准备概念 owner 局部写集；存在/特征概念、定义、直接上位及形成挂接由同一片段形成。
- 增加 M=1..254 的受限组合提交。概念参与者固定为 M+1；外部端口只在调用期借用；概念私有端口仅在实现内部作为最后端口进入 L1 v3。
- 首次材料重放把 owner 规范化写集逐字段转换为 v3 参与写集：稳定引用保持稳定，本 owner 本地引用保持 plain 本地键，不生成带编号跨参与者引用；再反向还原并逐字段等值核验。
- 新参与入口缺新布局返回 `相关概念参与状态::旧格式不支持=18`；旧入口缺旧布局仍返回 `概念树数据状态::旧格式不支持=35`。
- 根工程与 filters 各唯一登记新合同模块；新增独立专项工程和验证程序，不接入普通应用入口。

## 施工中修正

- 首轮专项夹具先调用准确特征结构初始化，返回 `特征数据错误::未设置`；按现有特征服务合同改为先初始化特征定义结构，再初始化准确特征结构。
- 形成片段最初把内部节点数量与“最大概念数”比较。该预算表示逻辑概念数量，内部节点由关系/属性预算约束，因此删除该错误比较；专项仍通过关系预算验证数量拒绝。

## 知识变化清单

- 可复用新能力：`相关概念添加参与者` 提供准备、组合提交和按实际截止读回三入口；概念 owner 私有端口不向消费者暴露。
- 事务约束：前序参与者按位置连续编号 1..M，概念为 M+1；首次材料中的本 owner 本地引用进入 L1 v3 时保持 plain 本地键。
- 迁移约束：旧、新布局可由同一实例和同一 owner 端口承载，但两侧入口只读取各自布局。
- 本轮未修改共享知识库或函数用途索引；以上作为发布后知识维护输入。

## 范围边界

未修改六类数据服务、世界树应用、L1、普通装配、旧概念应用、数据库 schema、规范、计划、索引和知识库。未建立概念总根，也未接线后继六类添加入口。
