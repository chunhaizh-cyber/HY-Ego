# IFR-FEATURE-IDENTITY-CONSUMER-REPAIR 施工记录

## S0 与范围

- 直接代码切片：`GOAL-IFR-FEATURE-IDENTITY-CONSUMER-REPAIR`；正式基线`36406c909d46e0939c1a3b19bcacbf29f204ab69`，`main == origin/main`，index 空。
- 唯一生产文件在开工时为 clean：`海中鱼巣/业务/应用服务.特征概念类.cpp`。`装配.普通应用.{h,cpp}`和`领域/数据服务.需求类.h`为根材料目标 WIP，未读取为本结果、更未暂存或修改。
- 当前源码复核：`处理实例特征R观察(const 实例特征R观察请求&) noexcept`中的`构造目标`第四参数为`optional<特征信息身份>`；`原子I64特征出生事实.F`是`稳定编码`；`特征信息身份`是只含`稳定编码 编码`的显式值式包装。不存在可用的隐式转换，编译器 C2664 与源码共同唯一裁决本行修改。

## 实际修改

只修改`海中鱼巣/业务/应用服务.特征概念类.cpp`的一个调用实参：把`out.F出生->事实->F`改为`特征信息身份{out.F出生->事实->F}`。未改变`构造目标`、请求/结果 DTO、读写、事务、失败映射、所有权、生命周期或相邻分支。

## 代码知识变更清单

| 身份 | 变化 | 合同、调用边与知识维护 |
| --- | --- | --- |
| `实例特征R观察结果 特征概念应用服务::处理实例特征R观察(const 实例特征R观察请求&) noexcept` | 直接修改一处向本地`构造目标`传入新增 F 的实参 | 输入、结果、失败分支、事实读写、事务、并发和生命周期均不变；唯一调用边仍是该函数内部`构造目标(...)`，仅把裸编码显式构造成既有强类型。相关用途卡未找到，按当前源码、调用点、0600与CODE-FILE-01完成裁决；发布后应由知识库维护者将此函数标记待复核。 |

## 边界

本记录只证明强类型消费者修复已施工；不证明 IF/R 运行行为、根材料 provider、Release、恢复或集成能力。
