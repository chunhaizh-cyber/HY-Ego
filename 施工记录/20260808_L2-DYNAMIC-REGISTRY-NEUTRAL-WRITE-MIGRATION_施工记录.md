# L2-DYNAMIC-REGISTRY-NEUTRAL-WRITE-MIGRATION 施工记录

日期：2026-08-08

## 1. 施工身份与起点

- 计划：`L2-DYNAMIC-REGISTRY-NEUTRAL-WRITE-MIGRATION v0.2`
- plan blob：`bcdc0f1727d67e71da7f96b0b7db0423064947d7`
- 设计提交：`7e2b2c48a61f9ebfe96a4c17b1569f15aa7c524f`
- 正式施工起点：`7ee147e28ce476629de8b9a6389609ad6e42d0e7`
- 起点关系：`main == origin/main`，ahead / behind 为 `0 / 0`，Git index 为空。

v0.1 施工期间发现并具名退回 `DYNAMIC-REGISTRY-IDEMPOTENCY-ORDER`：提交前以旧期望代次拒绝会使 L1 精确重复不可达。计划支撑发布 v0.2 后重新完成 S0；当前 L1 仍按“精确重复、同键异义、legacy 键冲突、期望代次漂移”顺序裁决，`L1中性写集请求` 的逐字段相等仍包含期望事实代次，三个中性接口和结果字段未漂移。

## 2. 实际施工范围

仅修改 / 新增计划允许的四个路径：

1. `海中鱼巣/领域/L1动态.数据.h`
2. `海中鱼巣/领域/服务.L1动态.ixx`
3. `施工记录/20260808_L2-DYNAMIC-REGISTRY-NEUTRAL-WRITE-MIGRATION_施工记录.md`
4. `验证记录/20260808_L2-DYNAMIC-REGISTRY-NEUTRAL-WRITE-MIGRATION_构建与动态登记验证.md`

生产代码实际变化：

- 删除登记操作标签、意图组和入口变体；保留唯一 `0x0F` 登记幂等域及全部公开 DTO、构造和函数签名。
- 登记幂等映射改为 `L1中性写集幂等键`；删除登记专用领域意图、旧幂等探测、首次请求缓存、完整快照与 legacy 提交路径。
- 建立登记在公开请求通过后预读中性当前代次，只验证入口健康；不以预读值和请求期望值不等提前拒绝。固定形成 `10/0/0/0/0` 中性写集并物理调用 `提交中性写集` 恰一次。
- L1 最终状态优先：首次成功额外核对预读代次等于请求期望、发布标志和重试边界；精确重复允许当前代次已推进，复用首次十编码；冲突、漂移、资源、入口与内部状态按中性结果映射。
- 新增共享中性当前读回：G0、十编码逐节点读取、G1，只有首末和逐节点事实代次一致、节点形状完整时组装登记。`读取当前登记` 只消费缓存定位并复用该读回，不直接返回缓存。

## 3. 排除范围复核

- `形成状态迁移动能规格` 起的完整快照规格路径及后续其它状态动态逻辑与起点 HEAD 逐字相同。
- 核心 L1、其它领域服务、启动、工程、filters、自检、规则、规范、流程图、详细设计、计划和计划索引均未修改。
- P0-FIX、路线输入、异主 `.codex-build` 和两份 WORLD-TREE WIP 原样保留。
- 构建和模块扫描只使用系统临时目录，没有运行生成的生产二进制或真实消费者。

## 4. 实际验证与边界

- Debug / Release `Rebuild` 均成功：各 0 错误、1 个 MSBuild 临时目录增量构建警告；本次执行目标为完整重建，不依赖增量结果。
- Debug / Release MSVC 模块扫描均为 `sources=136, modules=136, failed=0`，拓扑 136、循环 0、外部需求 0。
- `python .\tools\check_specs.py --strict`：110 份目录项全部通过。
- 精确静态门禁、排除路径同一性和 diff check 通过；详见配套验证记录。

本施工只闭合动态结构登记这一具名 L2 入口的中性写入与当前读回。构建、模块扫描和静态检查不证明真实消费者已运行，不证明其它动态 / 因果入口完成，也不证明 L1 或第一层整体完善。
