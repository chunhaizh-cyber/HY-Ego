# L1-SIMPLIFY-METHOD-ROOT-R1-CURRENT-READ v0.1 施工记录

日期：2026-08-07

## 施工身份

```text
计划：L1-SIMPLIFY-METHOD-ROOT-R1-CURRENT-READ v0.1
plan blob：e2c8b9de92ce0a4c3701a17de9a474b407b008be
设计提交：9b9bda5bae99f19acd59c3cb907cb4bf6ff05c7d
施工起点：9f01335bac6de210921cd5b5d7f051e006f514ab
分支：main
```

施工前确认 `main==origin/main`、ahead/behind 为 0/0、Git index 为空。P0-FIX、路线、旧 WORLD-TREE、`.codex-build/` 和并行 R9 文档属于异主切片，均未修改、暂存或夹带。

## 实际变更

1. 新增方法登记根当前读取数据合同：强类型定位凭证、固定角色、请求、七态结果和完整事实副本。
2. 新增唯一 `L1方法登记根当前读取服务::读取当前方法登记根`：入口先验通过后恰调用一次 P17 非阻塞完整快照，按共同截止解释三个定位节点、唯一角色槽和值事实。
3. 新增同一生产程序内的顺序 583 专项自检，使用真实进程内 L1 事实覆盖入口拒绝、截止漂移、全无、半结构、完整结构、无槽、旧角色、错来源、错属性表示和重复确定性读取。
4. 在既有工程、筛选器和启动薄入口登记两个模块与一个数据头；完整自检登记数和执行数由 33 更新为 34。

服务只保存 `const L1事实基座服务&`，不保存快照、身份、许可、锁或缓存；没有新增写入、等待、重试、事务、日志机器事实或第二程序目标。

## 施工中修正

首次 Debug 自检显示 R1 专项 14 项中两项夹具预期错误：一个创建代次越过请求截止，应在入口拒绝；一个 I64 组属性类型仍使用 I64 标量材料，导致夹具写入未成立。修正测试预期和材料形状后，生产读取算法未变，双配置完整矩阵通过。

## 完成边界

本施工只形成从调用方强类型定位凭证和 P17 同截止快照读取一个方法登记根的组件候选。它不发布登记根、不形成定位凭证、不登记或执行方法，也不证明生产消费者、正式接线、崩溃恢复或治理闭环完成。
