# 世界登记当前读回一致投影迁移施工记录

日期：2026-08-08

计划：`L2-WORLD-REGISTRY-CURRENT-READ-CONSISTENT-PROJECTION-MIGRATION v0.1`

plan blob：`eabd44f3754337c38c1ea6b3b181280b16461ca1`

设计提交：`801734293eaae4b074f7c39f72712c5c1563bba2`

一致投影提供者结果：`a23dc8e5ad462832ac97245b5f9d8033b0ab3c85`

施工起点：`f96f27397cfcfd781bc886081e7e6d9ec00fd484`

## 1. S0 与范围

- 开工时 `main == origin/main == f96f27397cfcfd781bc886081e7e6d9ec00fd484`，ahead / behind 为 `0 / 0`，Git index 为空；索引 blob 为 `ffffd8f4148aeeaa13f5b11fb8d04be1d5291694`，本计划是唯一 `可执行` 项。
- 目标 `海中鱼巣/领域/服务.世界登记.ixx` 起始 blob 为 `72d656b69542dcc80f8b8b330c0f24d3d5d70782`；世界登记 DTO、L1 中性一致当前 DTO / 服务、运行期上下文、生产运行期和应用程序入口 blob 均与详细设计创建侧冻结值一致。
- 现行公开合同仍为世界登记合同版本 3、规则版本 1、十个稳定状态；目标私有函数、固定五节点 + 一值 + 一槽写入以及 `--runtime-context -> 运行期上下文::完整 -> 世界登记服务::读取当前登记` 生产调用边未漂移。
- 本轮只取得世界登记服务文件及两份专属记录的所有权。P0-FIX、路线、异主 `.codex-build`、两份 WORLD-TREE 和其它 WIP 原样保留。

## 2. 实际施工

### 2.1 一次一致当前投影

把 `世界登记服务::按登记读取权威事实` 从事实代次首读、五次节点读取、属性读取、值读取和事实代次末读的多许可拼接，迁移为一个物理 `尝试读取中性一致当前投影` 调用点。

每次完整尝试固定形成：

- 节点五项，顺序为服务身份、场景标记属性类型、直接父场景关系类型、直接成员关系类型、世界根场景；
- 关系、值、源关系组、目标关系组为空；
- 属性值恰一项，为世界根场景 + 场景标记属性类型；
- 第一次期望代次取输入登记的 `已验证事实代次`。

没有预读当前值编码，也没有调用旧当前事实代次、节点、属性、值或完整快照入口。

### 2.2 非成功与最多一次完整重试

新增私有纯值辅助判断，对非成功结果逐项核对合同版本、期望代次回显、六组全空和读取代次边界。

- 只有第一次合法 `事实代次漂移` 才以回显的非零且不同实际代次重建完整请求；
- 最大尝试次数固定为 2，第二次仍合法漂移返回世界登记 `事实代次漂移`；
- `许可拒绝` 立即映射为世界登记许可拒绝，零等待、零重试；
- `资源失败` 保持资源失败；入口拒绝、内部不一致、未知状态或坏失败形状收敛为内部不一致；
- 所有非成功均返回空登记，不修改缓存、不补写、不恢复、不回退旧入口。

### 2.3 成功业务形状

成功只接受合同、期望回显、非零读取代次和六组长度精确为 `5/0/0/1/0/0` 的结果，再逐项验证：

- 五节点查询编码、项目状态、事实编码、种类、属性类型表示、共同创建代次、未退出和当前属性数量；
- 前四节点无当前属性，世界根恰一项当前属性；
- 唯一属性投影的节点、属性类型、槽和根当前槽逐字段一致；
- 当前值事实编码、所属世界根、属性类型、来源服务、共同创建代次、未退出及 `std::int64_t(1)` 材料精确成立。

成功只复制输入登记并把副本的 `已验证事实代次` 更新为本次读取代次；输入登记和成员 `登记_` 不在目标根内修改。

世界登记公开 DTO / 状态 / 函数签名、`建立空世界` 的固定中性写集 / 提交 / 映射、缓存和全部生产调用者保持不变。

## 3. 验证资源与结果

Debug / Release 的构建输出、日志、模块扫描 JSON 和生产运行输出位于系统临时目录 `C:\Users\zhchh\AppData\Local\Temp\hzy-world-registry-consistent-269108f6fc624b45996964f8a99f15cb`，没有使用、删除或覆盖仓库内异主 `.codex-build`。

- Debug x64 完整 Rebuild 退出 0，0 编译错误，1 条临时输出目录 `MSB8029` 提示。
- Release x64 完整 Rebuild 退出 0，0 编译错误，1 条临时输出目录 `MSB8029` 提示。
- Debug / Release 模块扫描均为 `sources=136, modules=136, failed=0`，拓扑 136、循环 0、外部需求 0。
- Debug / Release 正式 `--runtime-context` 均退出 0。
- 正式规范严格检查通过 110 / 110；三白名单路径空白检查与精确静态门禁通过。

## 4. 完成边界

本施工最多证明世界登记这一具名 L2 当前读回根已迁移到中性非阻塞一致当前投影，并通过约定构建、模块、静态和正式生产入口运行门禁。

生产退出码只证明正式入口没有拒绝该次启动。公开运行期上下文租约连续两次结构化 `读取世界登记` 未在本段独立执行，因此本段不独立证明世界登记业务成功，也不形成集成验收结论。

不得据此声明真实许可竞争已运行、其它消费者或方法登记根已迁移、关系组 / 历史 / 修改 / 退出已证明、历史正文闭合、L1 自证或第一层完善。
