# INSTINCT-STAGE3-ACTIVE-SAFETY-SOURCE-CATALOG 施工记录 v0.1

日期：2026-08-30

计划：`INSTINCT-STAGE3-ACTIVE-SAFETY-SOURCE-CATALOG v0.1`

计划 blob：`6d03a5333ee57d988bae5e0f8f5b996c7662084f`

施工起点：`6e4f549bd923340d1d96465f681f2286ac6f9979`

结果提交：本记录所在提交；发布后以 Git 返回为准。

## 1. S0 与复用裁决

- `计划/计划索引.md` 中本计划是唯一 `可执行` 项，两项具名依赖均已完成。
- `main == origin/main == 6e4f549b`，开工时 index 空；六个生产文件属于计划白名单。
- 共享工作区其它 SELF-GOVERNANCE 代码、规范、流程图、工程文件和未跟踪材料均为异主 WIP，本切片未修改、暂存或清理。
- 当前代码没有等价的主动安全结算来源完整集合入口。实现复用现有 `安全根定义与当前值服务` owner、L1 owner-scoped 首次写入材料、独立登记、属性当前槽、历史事实、幂等重放和事实代次守卫，不建立第二 owner、第二 module 或工程登记。
- 当前知识库没有该新 provider 的用途卡；本记录给出函数级知识变更清单，供发布后知识维护消费。

## 2. 实际变更

1. 在现有数据头增加主动安全结算来源规则事实、发布 / 读取 DTO、结构化状态、成功谓词和方法组规范排序检查。
2. 在现有安全根 owner 内增加独立来源规则登记：固定登记身份 `0x494E'5354'5341'4602` 与键 `0x0307'1205..1208`；定义登记 `1201..1204` 未改变。
3. 实现规则版本 1 显式空集合的发布、当前读取、持久恢复、同键重放、同义异键零第二写入、同键异义拒绝、G0 双守卫和提交后读回不确定收束。
4. 扩展生产初始化：定义形成或恢复后补发 / 恢复来源规则，最终以一个新 `Gread` 分别独立读回定义和规则并互证自我、根实例和规则版本。
5. 扩展两份专项，覆盖未发布与合法空集分离、非法版本 / 非空组、幂等矩阵、漂移、提交后读回失败、持久恢复、定义存在但规则缺失补发、规则异义和下层失败映射。
6. 安全根值域、定义请求 ABI、定义版本 1、`L=2767011611056432742` 与 `H=7378697629483820645` 均保持不变。

## 3. 函数级代码知识变更清单

| 稳定函数或谓词 | 文件 | 用途与合同 | 直接调用边 | 变更 |
| --- | --- | --- | --- | --- |
| `有效(主动安全结算来源规则事实身份_v1)` | `安全根定义与当前值.数据.h` | 校验强类型规则事实身份；不解释方法语义 | 规则事实完整谓词 | 新增 |
| `主动安全结算来源方法组规范_v1` | 同上 | 校验方法身份有效、严格升序、无重复；空组合法 | 请求准入、事实完整、专项、ABI consumer | 新增 |
| `主动安全结算来源规则事实完整_v1` | 同上 | 校验规则事实完整形状；不证明定义版本绑定 | 发布 / 读取结果与生产匹配谓词 | 新增 |
| `主动安全结算来源规则发布结果_v1::成功` | 同上 | 只接受已发布 / 精确重复、完整事实、首次 G1 和正式截止 | 服务、专项、生产 provider | 新增 |
| `主动安全结算来源规则读取结果_v1::成功` | 同上 | 只接受已读取、完整事实和同截止 | 专项、生产 provider | 新增 |
| `安全根定义与当前值服务::发布主动安全结算来源规则_v1` | `服务.安全根定义与当前值.ixx` | 规则 1 空集唯一写入口；处理登记、重放、写入、读回和不确定提交 | 生产初始化与专项 | 新增 |
| `安全根定义与当前值服务::读取当前主动安全结算来源规则_v1` | 同上 | 以请求 G0 读取唯一当前规则，非成功空载荷 | 生产初始化与专项 | 新增 |
| `确保来源规则登记` / `确保来源规则登记定位只读` | 同上，module-private | 建立或恢复本 owner 独立登记；不发布业务规则 | 来源规则发布 / 读取 | 新增 |
| `重放来源规则材料` / `来源规则等于请求` | 同上，module-private | 逐字段互证首次规范写集和事实语义 | 来源规则发布 | 新增 |
| 来源规则请求准入、编码 / 解码及状态映射 helpers | 同上，module-private | 固定 v1 空集 U64 布局、异常分账；不提供第二公开入口 | 来源规则发布 / 读取 | 新增 |
| `安全根生产定义匹配_v1` | `安全根生产定义初始化.数据.h` | 既有定义匹配，L/H 与定义版本不变 | 生产初始化 | 未改语义 |
| `主动安全结算生产来源规则匹配_v1` | 同上 | 校验生产规则 1、空方法组和固定来源 | 生产初始化及专项 | 新增 |
| `安全根生产定义初始化结果_v1::成功` | 同上 | 成功增加定义 / 规则同身份、同实例、同版本和同截止约束 | 启动与专项 | 修改 |
| `安全根生产定义初始化提供者::初始化并读取安全根生产定义_v1` | `提供者.安全根生产定义初始化.ixx` | 定义后发布 / 恢复规则，最多两次收敛；不自动发布后继版本 | 启动与专项 | 修改 |
| `安全根生产定义初始化提供者::形成成功` | 同上，private | 在同一最终 Gread 独立读回并互证定义和规则 | 初始化入口 | 修改 |
| 来源规则读 / 写映射与转发 helpers | 同上，private | 生产转发及验证宏一次性失败注入；不进入普通生产 ABI | 初始化入口与专项 | 新增 |
| `形成来源规则发布请求` / `读取来源规则` | `端到端测试.安全根定义与当前值.ixx`，private | 构造 fixture 请求和独立读回 | S00、S17—S19、S11 | 新增 |
| 两个端到端测试入口 | 两份专项 | 扩展规则与生产联合矩阵 | 仓库外专项 main | 修改 |

发布后应刷新安全根定义服务、生产初始化 provider、两个数据头和两份专项的函数用途卡；不得把验证宏入口登记为生产业务入口。

## 4. 边界

本切片只闭合主动安全结算来源规则版本 1 的显式空完整集合 provider 和生产初始化。它没有实现主动安全结算方法、永久变化账时间窗口分类、低位回升门禁、UTC / 单调秒绑定、被动维护或完整阶段三。
