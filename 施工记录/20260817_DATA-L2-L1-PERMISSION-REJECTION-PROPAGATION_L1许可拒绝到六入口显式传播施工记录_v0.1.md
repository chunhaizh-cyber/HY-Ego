# DATA-L2 L1许可拒绝到六入口显式传播施工记录

日期：2026-08-17

计划：`DATA-L2-L1-PERMISSION-REJECTION-PROPAGATION v0.1`

plan blob：`1f0c3d1846843c1e51c7e5a11150d6342ec455f7`

设计 blob：`f8f30332f98317989fd2c8124929bd17b50151a4`

流程图 blob：`2fb657515655de3773e6fe54dbbb56208c3266f3`

施工基线：`04c1179ca5cc3b2945dc39fa3aa6f9e2b56f9088`

## 1. S0 与范围

- `main == origin/main == 04c1179ca5cc3b2945dc39fa3aa6f9e2b56f9088`，ahead / behind 为 `0 / 0`，Git index 为空。
- 计划索引中本计划为唯一可执行代码计划；计划、详细设计和流程图 blob 与派发身份一致。
- 十二个允许生产文件均与计划创建侧 blob 一致；仓库 `尝试读取当前事实代次` 仍以 `try_to_lock` 在锁竞争时返回 `L1读取状态::许可拒绝, 0`。
- 全仓扫描的相关生产文件为十二个允许文件及只提供底层结果的 `仓库.L1事实基座.ixx`；未发现白名单外可观察消费者。
- WORLD-SELF 六件 WIP、SELF / CF、知识库、最终需求清单、旧材料删除、路线、根工程 WIP 与 `.codex-build` 均保持异主现场，未修改、暂存或清理。

## 2. 实际施工

十二个生产文件全部发生计划内最小修改：

1. `L1中性CRUD.数据.h` 在现有 1—7 尾部追加 `L1中性读取状态::许可拒绝 = 8`；合同版本和 DTO 布局不变。
2. `服务.L1事实基座.ixx::映射中性读取状态` 将底层许可拒绝从资源失败中拆出，唯一映射为中性许可拒绝。
3. 场景与存在两个事实代次失败映射 helper 增加 `中性许可拒绝 -> L2结构状态::许可拒绝`。
4. 不可变材料、动态、状态、因果、特征、语言、概念、方法八个公共枚举直接消费者增加对应既有许可拒绝状态映射。
5. 六个公开入口的签名、结果 DTO 和失败形成函数未改；它们经现有空载荷失败路径返回许可拒绝、截止 0、空变更代次和空业务载荷。

未修改仓库锁、owner、写入口、事务、历史读取、工程 / filters、普通应用、启动、日志或生产装配；未增加等待、重试、无锁代次镜像、故障 seam、测试宏、计数器或专项痕迹。

## 3. 函数级代码知识变更清单

共享知识库本轮不直接修改；下列条目在结果发布后应由知识维护者按新 source blob 增量复核。

| 稳定函数身份 | 文件 / 模块 | 用途变化 | 调用边变化 | 诊断责任 | 验证状态 |
| --- | --- | --- | --- | --- | --- |
| `L1中性读取状态 映射中性读取状态(L1读取状态) noexcept` | `核心/服务.L1事实基座.ixx` | 底层许可拒绝不再降为资源失败，保留为中性许可拒绝 | 无 | 纯值映射，无适用错误分支 | 静态映射、双配置构建及六入口矩阵通过 |
| `L2结构状态 映射事实代次探测失败(L1中性读取状态) noexcept` | `领域/服务.L2场景结构.ixx` | 新增许可拒绝守恒映射 | 无 | 纯值映射，无适用错误分支 | 六入口中的场景四入口双配置命中 |
| `L2结构状态 映射存在事实代次探测失败(L1中性读取状态) noexcept` | `领域/服务.L2存在结构.ixx` | 新增许可拒绝守恒映射 | 无 | 纯值映射，无适用错误分支 | 六入口中的存在两入口双配置命中 |
| `L2结构状态 映射中性来源状态(L1中性读取状态) noexcept` | `领域/服务.不可变材料.ixx` | 新增材料服务既有许可拒绝映射 | 无 | 纯值映射，无适用错误分支 | 编译与枚举穷尽通过；动态资源耗尽 NOT_RUN |
| 动态 / 状态 / 因果服务各自的 `映射事实代次探测失败(...)` | 三个对应服务模块 | 新增各服务既有许可拒绝映射 | 无 | 纯值映射，无适用错误分支 | 双配置编译与静态闭包通过 |
| `特征事实代次读取结果 读取特征事实代次(const L1事实基座服务&) noexcept` | `领域/服务.L2特征结构.ixx` | 中性许可拒绝映射为 owner 许可拒绝 | 无 | 纯值转换，无适用错误分支 | 双配置编译与静态闭包通过 |
| 语言 / 概念 / 方法服务各自的 `读取当前事实代次(...)` | 三个对应服务模块 | 中性许可拒绝映射为本服务既有许可拒绝 | 无 | 纯值转换或向上送出，不新增日志 | 双配置编译与静态闭包通过 |

间接受影响但未改函数体的六个公开入口：

- `L2场景结构服务::读取场景树(...) const`
- `L2存在结构服务::读取存在单例角色(...) const`
- `L2存在结构服务::读取完整存在(...) const`
- `L2场景结构服务::读取存在当前场景成员引用(...) const`
- `L2场景结构服务::读取场景宿主存在关系(...) const`
- `L2场景结构服务::读取存在当前宿主场景(...) const`

这六个入口继续承担“向上送出”责任；许可拒绝在载荷解释前经既有失败形成函数收口，不新增生产日志。

## 4. 仓库外验证材料

唯一验证根：

```text
D:\TEMP\海中鱼巣\DATA-L2-L1-PERMISSION-REJECTION-PROPAGATION\20260817-001\
```

专项只使用普通应用正式装配、正式场景写入口和六个公开读取入口。首轮沿用旧漂移专项的单次并发探针时，许可拒绝已经命中且形状正确，但专项错误地要求同一 P2 窗口同时命中 P1 漂移，快速入口还可能错过锁窗口；该轮不作为通过证据。最终专项改为同步起跑、每轮最多 64 次公开读取探针和最多 160 轮，不使用 `sleep`、`yield`、私有锁、生产 seam 或生产故障宏。

## 5. 完成边界

本结果只形成 L1 中性许可拒绝到六个具名 DATA-L2 当前读取入口的候选传播闭包，并完成公共枚举已知直接消费者的兼容修正。它不证明 WORLD-SELF、BIZ 提供者拒绝、其它 DATA 并发面、真实资源耗尽、长时公平、崩溃 / 重启或跨进程恢复完成。
