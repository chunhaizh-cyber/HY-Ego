# DATA-L1-THREE-PARTITION-ATOMIC-V2 施工记录 v0.1

日期：2026-08-28

## 1. 施工身份

- 计划：`DATA-L1-THREE-PARTITION-ATOMIC-V2 v0.1`
- plan blob：`34fc06e67fc116ced17a198873734b5a22bcdef7`
- 起点：`main@4613d1a8d5f963b300f9682a0ca3807bdfb97564`
- 结果提交：`ff2366d97b662cfc6c8cf16917c06d5da182bf4b`
- 生产代码范围：三个 L1 核心文件

S0 确认本计划是计划索引唯一 `可执行` 项，`HEAD==origin/main`、ahead/behind `0/0`、index 空。三个生产文件仅承载本计划增量；其余大量 dirty/untracked 文件均为异主 WIP，本次未清理、覆盖或暂存。

## 2. 实际变更

修改：

- `海中鱼巣/核心/L1所有者范围CRUD.数据.h`
- `海中鱼巣/核心/服务.L1事实基座.ixx`
- `海中鱼巣/核心/仓库.L1事实基座.ixx`

实现结果：

1. 增加合同版本 2 的三分区参与者、引用、四类写项、写集、请求、固定九态和结果 DTO；持久引用标签固定为稳定编码 1、本地键 2、跨参与者引用 3。
2. 在 move-only `L1所有者范围写端口` 增加三端口正式入口，接收者和两个参数固定绑定参与者 1、2、3；只允许同一 L1 实例且 owner 逐项一致的三个有效端口。
3. 仓库按参与者 1→2→3 在私有候选副本中复用普通 owner 写入，跨参与者只允许 2→1、3→1/2；三次候选代次、事实生命周期及三份首次写入账统一为一个 G1。
4. 增加独立 v2 组合幂等账；精确重复先于 G0 漂移，返回首次 G1 和三份映射且零新应用；同组合身份异义返回幂等冲突。
5. 候选通过全局状态完整性与持久准备后才进入一次状态交换；交换边界异常映射为 `已可能发布`，交换前异常保持零正式发布。
6. 内部权威载荷格式升级为 2，解码兼容格式 1/2；格式 1 初始化空 v2 账，格式 2 固定读写新账。外层 manifest、公开持久恢复合同及 `L1事实基座核心持久恢复见证{1,...}` 均保持 v1。
7. `状态完整` 增加 v2 请求、首次结果、三份 owner 首次规范化写集、映射、共同 G1、局部键、跨分区引用及属性槽唯一性的逐项互证。

未修改 L2/L3、业务、内部治理、安全、自我、需求、任务、方法、线程、启动、装配、工程文件或生产端到端测试模块；没有增加生产测试入口、故障注入、业务参与者枚举或自动 consumer 接线。

## 3. 施工中修复

- 将引用闭包裁决移入仓库唯一写锁，使引用冲突按冻结矩阵回显锁内当前代次。
- 幂等冲突、内部不一致和局部写入失败改为明确重试边界；新的外层组合事务不接受既有内部幂等账的局部精确重复冒充新提交。
- 恢复完整性从只比较 owner 首次账的代次和映射，收紧为逐字段重建并比较三份规范化中性写集。
- 修复属性槽恢复校验：同一所属节点与属性类型即使新当前值不同，也不能出现第二项。

## 4. 函数级代码知识变更清单

新增或实质修改：

- `L1所有者范围写端口::提交三分区原子事务_v2(const L1三分区原子事务请求_v2&, const L1所有者范围写端口&, const L1所有者范围写端口&) noexcept`：新增受限对外写入口；负责三端口同实例、有效性和 owner 绑定，不负责选择 owner、业务许可或 L2 语义。
- `L1事实基座仓库::提交三分区原子事务_v2(const L1三分区原子事务请求_v2&) noexcept`：新增私有三分区事务；负责规范化、锁内幂等/G0、后向引用解析、候选应用、单 G1、持久准备和一次交换。
- `写三分区引用_v2`、`读三分区引用_v2` 及 v2 请求/结果 `写/读` 重载：新增固定数值标签的私有持久编解码，不形成第二公开 ABI。
- `是进程维护三分区原子事务_v2`：新增私有过滤 helper；防止进程生命周期维护账进入持久权威载荷。
- `编码权威状态`：内部载荷版本由 1 升为 2，并在旧双参与者账后写入规范有序 v2 账；公开恢复见证版本不变。
- `解码权威状态`：兼容内部格式 1/2；格式 1 不读取 v2 账，格式 2 读取并拒绝重复、非规范、维护身份或尾随材料。
- `状态完整`：增加 v2 组合账与三份 owner 首次账的完整互证、引用闭包和属性槽唯一性检查。

直接调用边：新增公开写端口到同一仓库私有入口；当前生产 L2/业务调用边为 0。旧 v1 `提交跨所有者原子事务` 及其 DTO、持久账和调用边保持不变。

待知识库维护者在结果正式发布后新建或更新：

- `知识库/函数用途/L1中性与所有者范围CRUD合同/**`
- `知识库/函数用途/L1事实基座公开服务/**`
- `知识库/函数用途/L1事实基座仓库/**`

上述用途卡应绑定最终结果提交；本计划未直接修改共享知识库。

## 5. 完成边界

本记录只支持“L1 三分区中性原子 provider 已形成，并通过专属 Debug/Release 公开入口矩阵与正式 v143 根工程双配置构建”。VS 2026 v145 仍在未修改的既有模块触发 C1001；本结果不证明 v145 工具链闭合，也不证明 DATA-L2 当前值变化入口、任何业务 consumer、路线阶段二或自我内部治理闭环完成。
