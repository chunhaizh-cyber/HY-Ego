# DATA-L2-SCENE-STATE-USE-BINDING-ABI-SKELETON 场景状态使用绑定ABI骨架施工记录

日期：2026-09-09

版本：v0.1

执行计划：v0.3，plan blob `fce5e752d65d69bb8d233df8e0d84298acf6bc5d`

详细设计：v0.3，blob `3b36eda15c8c51780983512a50c177d4534c97af`

最终 S0 基线：`0fb2d13fb1ef0f2ada5ae2ab145f760b3cd9c462`

## 1. S0 与范围

最终 S0 确认 `main == origin/main`、ahead/behind 为 `0/0`、index 为空，计划索引登记 v0.3 为 `可执行`。本轮只修改合同模块、场景模块、两处工程登记和本计划专属施工/验证记录。

v0.2 候选曾向存在类加入17行窄接口骨架。v0.3移除存在类后，该17行已精确撤出；存在类工作树与HEAD blob均为 `1510813c6bc43513503171692cf57d3d7d5e627b`，内容零变化，不属于本轮提交。

状态类、动态类、动态概念类、L1、普通应用、规则、规范、详细设计、计划和计划索引均未修改。其它工作区修改与删除属于其它所有者，未清理、覆盖或带入本切片。

## 2. 实际变更

1. 新增 `合同.场景状态使用绑定.ixx`，按4203 v0.3发布绑定身份、信息、事实、结构交付、请求/结果DTO、固定枚举、完整性守卫、成功谓词及两个抽象只读提供者。
2. 重建 `数据服务.场景类.ixx` 为最终ABI骨架：构造保存依赖；`绑定于`只比较L1地址；七个业务入口直接返回合同版本1、状态 `未实现`、截止0、无变更及空载荷。
3. 工程与筛选器各唯一登记一次新合同模块；不修改普通应用装配。
4. 七个业务入口不读取请求，不访问L1、写端口、provider、状态服务、幂等账、索引、时钟或日志。

## 3. 函数级代码知识变更清单

| 函数/类型 | 变化 | 用途与合同 | 调用边/后续知识卡 |
| --- | --- | --- | --- |
| `有效(状态使用绑定身份)` | 新增 | 校验绑定身份底层编码 | DTO守卫使用；发布后补用途卡 |
| `状态使用绑定事实完整` | 新增 | 校验截止、生命周期、B/C/E/S和三关系；允许C与E同码 | 四类成功谓词使用；发布后补用途卡 |
| 四类状态使用绑定结果 `成功()` | 新增 | 校验创建、读取、排序空组和退出成功形状；`未实现`恒为false | 公开DTO自检；发布后补用途卡 |
| `存在结构身份只读结果::成功` | 新增 | 校验存在族身份只读结果 | 后继具体provider使用；发布后补用途卡 |
| `存在结构身份只读提供者` | 新增抽象接口 | 隔离完整存在类，公开身份确认和历史见证 | 场景骨架持有；具体实现留后继 |
| `状态使用绑定只读提供者` | 新增抽象接口 | 只公开当前/历史绑定读取 | 场景类实现；后继动态消费 |
| `场景类数据服务` 构造与 `绑定于` | 替换 | 保存最终依赖并检查L1对象身份 | 无生产装配；发布后补用途卡 |
| 创建、当前读、历史读、按C/E/S反查、退出共七入口 | 新增/替换 | 最终签名的显式未实现骨架 | 后继在同签名中替换；发布后补用途卡 |
| 场景类旧七字段、树、成员、状态和动态组织逻辑 | 退出 | 不属于4203 ABI骨架，不保留兼容双轨 | 后继按正式合同重新实现 |

## 4. 验证事实

隔离目录：`D:\TEMP\海中鱼巣\DATA-L2-SCENE-STATE-USE-BINDING\abi-skeleton-v03`。

- Debug/Release x64隔离Rebuild：均退出0，合同与场景模块均形成IFC/OBJ。
- Debug/Release最小消费者运行：均退出0；验证未实现DTO版本、零截止、空变更、空载荷及 `成功()==false`。
- 独立静态复核：PASS；场景只import合同，七入口零依赖访问，工程登记无重复，存在类内容零变化。
- `git diff --check`：被异主 `AGENTS.md` 第12行行尾空白阻断；本切片六文件无该错误。
- `python .\tools\check_specs.py --strict`：113份目录项，1个错误；冻结清单指向的验收记录被其它所有者工作区删除。本计划不拥有该文件，未修复。
- 根工程两配置的目标模块均生成产物，之后被白名单外的空动态模块、存在类旧状态消费者及旧世界树场景消费者阻断；根工程不是v0.3门禁。

## 5. 声明边界

`NOT_IMPLEMENTED/NOT_RUN`：具体存在provider、合法C/E身份生产、真实B写读退、反查、恢复、动态消费、完整场景七字段、生产装配、数据库重启、故障注入、集成验收和业务闭环。

本轮只证明4203最终ABI和场景显式未实现骨架可隔离编译与安全失败。提交与推送事实由Git提交承载。
