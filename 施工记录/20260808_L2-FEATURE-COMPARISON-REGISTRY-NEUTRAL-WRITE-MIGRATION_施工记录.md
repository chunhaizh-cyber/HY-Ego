# L2-FEATURE-COMPARISON-REGISTRY-NEUTRAL-WRITE-MIGRATION v0.1 施工记录

日期：2026-08-08

## 1. 施工身份与起点

- 计划：`L2-FEATURE-COMPARISON-REGISTRY-NEUTRAL-WRITE-MIGRATION v0.1`
- plan blob：`310f9eacb1fef683acda16a2e868fd2e024d89eb`
- 设计提交：`20b6aef4dbf404a4cd1d234c979586f6b01943ab`
- 正式施工起点：`0a3464f72ce1263864108871a5759e701840974e`
- 起点关系：`main == origin/main`，ahead / behind 为 `0 / 0`，Git index 为空。

S0 复核确认计划 blob、两份目标生产代码和三个中性 L1 provider blob 均与详细设计冻结值一致；本计划是计划索引中唯一 `可执行` 项。P0-FIX、路线输入、异主 `.codex-build` 和两份 WORLD-TREE 计划 WIP 原样保留，不纳入本轮机器事实或发布范围。

## 2. 实际施工范围

本轮只修改或新增四个计划白名单路径：

1. `海中鱼巣/领域/特征比较.数据.h`
2. `海中鱼巣/领域/服务.特征比较.ixx`
3. `施工记录/20260808_L2-FEATURE-COMPARISON-REGISTRY-NEUTRAL-WRITE-MIGRATION_施工记录.md`
4. `验证记录/20260808_L2-FEATURE-COMPARISON-REGISTRY-NEUTRAL-WRITE-MIGRATION_构建与特征比较登记验证.md`

生产代码实际变化：

- 将唯一 `特征比较登记幂等域 = 0x08ULL` 移入数据头；M02 的 `0x09` 域和 M03 的 `0x0E` 域保持原位。
- 删除 M01 专用 G8 登记意图凭证、`0x15000801U`、旧幂等预探测、完整快照、legacy 写集 / 提交和首次登记请求缓存。
- `建立登记` 在公开请求校验后只健康预读中性当前事实代次，不在提交前用 P 与期望代次比较阻断请求；随后形成固定 `6/0/0/0/0` 中性写集并物理调用 `提交中性写集` 恰一次。
- L1 非成功状态优先映射；首次成功核对 P 等于请求期望、发布标志和不适用重试边界，精确重复允许当前代次已推进并要求零发布及 `原幂等键读回收敛`。
- 成功类结果核对合同、回显幂等键、非零首次发布代次和六项映射完整唯一，再执行 G0、六节点、G1 的中性当前精确读回。六节点必须未退出、当前属性为空、形状正确且共同创建代次等于 L1 首次发布代次。
- `读取当前登记` 只把 `登记_` 当作六编码定位缓存，复用同一权威读回，不直接返回缓存中的旧事实截止代次。

## 3. 排除范围与同一性

- M02 `登记I64目标判断比较合同`、M03 `登记I64状态迁移比较合同` 及从 M02 起至公开区结束的全部既有函数相对施工起点逐字一致。
- 从私有 `解析合同` 起至成员区前的运行比较、状态迁移比较和快照解析 helper 相对施工起点逐字一致。
- 核心 L1、状态、动态、因果、启动、工程、filters、自检、集成验收、规范、设计、流程图、计划和计划索引均未修改。
- 构建和模块扫描只使用系统临时目录，没有运行生成的生产二进制或真实消费者。

## 4. 实际验证与声明边界

Debug / Release x64 完整 `Rebuild` 均退出 0；每个配置各保留一条 `MSB8029` 临时输出目录不适合增量构建提示，本轮为完整重建，未依赖增量结果。双配置 MSVC 模块扫描均为 `sources=136, modules=136, failed=0`，拓扑 136、循环 0、外部需求 0。`python .\tools\check_specs.py --strict` 为 110 份目录项全部通过；目标差异检查和静态边界核验通过。

本施工只闭合特征比较第一结构登记根 M01 的中性写入与当前读回代码候选。没有新增专项 L1 驱动，没有运行生产程序或真实消费者，因此不证明首次成功、后继代次精确重复、同键异义、版本漂移或竞争分支已被真实业务运行触发；也不证明 M02/M03、运行期比较、状态动态联合发布、集成验收、核心 L1 或项目整体已经闭环。
