# INSTANCE-FEATURE-IFR-L2-LAYOUT-FOUNDATION 施工记录 v0.1

结果提交：`8d79cf4fb128a3e48b3254c71f3a6f4dea15087d`。

## 实际变更

- `实例特征结构交付` 和登记请求升级为 v2；新增 `R项材料属性类型`。
- v2 采用新幂等键 `0x4946525354525632`，首次写集建立六个普通关系类型节点和一个 `U64组` 属性类型节点；旧 v1 键未读、未写、未退出。
- 构造校验及登记后读回校验属性类型、表示、owner、创建代次与未退出状态。
- 普通应用改用 v2 请求和交付；没有装配期 IF/R 业务调用。

## 代码知识变更清单

- 修改 `存在类数据服务::登记实例特征结构`（`领域/数据服务.存在类.h`，公开静态服务）：v2 布局登记与同键重放读回；不负责 IF/R 投影、目标提交、当前采用或业务分类。
- 修改 `存在类数据服务` 构造布局核验：新增内部 `属性类型有效`，只核验同 owner 的 U64组属性类型。
- 修改 `建立上下文`（`装配.普通应用.cpp`，服务装配）：只登记并注入 v2 布局；不创建 IF/R 事实。
- 已发布结果需要知识库维护者将上述三个身份标记为待复核；本片不改共享知识索引。

## 未验证边界

完整应用工程的 MSBuild `ClCompile` 被项目级并行 PDB 配置稳定阻断为 C1041；隔离 MSVC Debug/Release 翻译已通过。未运行应用、旧 v1 持久库迁移、跨进程恢复、IF/R 读写或 L3。