# L1 中性当前引用闭包保护施工记录

日期：2026-08-11

计划：`L1-NEUTRAL-CURRENT-REFERENCE-CLOSURE-PREWRITE-GUARD v0.1`

## 基线与范围

- S0 基线：`main` / `origin/main` / HEAD 均为 `500869e1678e5b3c4ff1f3b43a48b2f282cd2fbb`，分歧 `0/0`，index 为空。
- 精确计划 blob：`f549d58a85627b7237663f03f354781bd9254a78`；计划登记提交：`41e07e8e8a15a5ebd085c3d67b86a93bb7dffd41`；索引中该计划是唯一 `可执行` 代码计划。
- 两个生产目标 blob 与计划登记一致：数据头 `a5467a55e71ad062d06cc1c4546543906fe7e567`、仓库 `c93400cc348cf4cfe1b9f452d5ba13c308c430c7`。
- 4015、4050、4230、总体详细设计、S2 计划、服务透传、合同整体导出及主工程 / filters blob 均与计划创建侧冻结事实一致。
- 只写入计划允许的两个生产文件、三个生产外验证文件和本记录及同名验证记录，共七文件。
- 流程图维护目录、路线输入和 `.codex-build/` 的异主现场保持原样；未修改服务、L2、规范、详细设计、计划 / 索引、主工程 / filters、入口、装配或日志。

## 实际实施

1. `L1中性CRUD.数据.h`
   - 保留既有 `L1中性写入状态` 数值 1—9，仅在末尾追加 `引用冲突 = 10`。
   - 未改变合同版本、写入结果字段、布局、成功判定或重试枚举。
2. `仓库.L1事实基座.ixx`
   - 在仓库私有状态增加 `当前节点引用索引`，只保存 `[被引用节点稳定编码] -> 有序当前关系 / 值稳定编码组`，不复制角色、材料、生命周期或完整事实。
   - 关系按源 / 目标 / 类型去重登记；值按所属 / 属性类型 / 来源 / 可选独立材料引用去重登记。创建时插入，显式退出和属性换代旧值退出时同步删除，组空时删除节点键。
   - 增加 `已闭合 / 引用冲突 / 内部不一致` 三值私有闭包裁决；命中候选后必须在当前关系或当前值中恰一回读并核对真实引用角色。
   - 在形成 `状态 候选 = 状态_` 前依次完成退出目标存在性、请求内新建事实 / 槽变更不得指向同请求退出节点、以及当前节点闭包检查。
   - 未闭合权威当前引用返回当前代次、零发布、空映射和 `修正请求后可重试`；索引与权威事实矛盾返回内部不一致。
   - `状态完整` 增加反向索引到权威当前关系 / 值、以及全部权威当前关系 / 值到索引的双向互证。
3. 生产外验证
   - 新建独立 Debug / Release x64、v145、C++20、Unicode、`/utf-8` 验证项目和临时目录包装脚本。
   - 验证入口只 import `海中鱼巣.核心.服务.L1事实基座`，仅通过公开服务构造、提交和读回事实。

## 实际验证摘要

- 主工程 Debug / Release x64 `Rebuild`：最终生产代码下均成功编译并链接。
- 外部验证 Debug（起始幂等键 `6101`）与 Release（起始幂等键 `8101`）：最终代码下均为 12 项检查全通过、失败数 `0`，最终事实代次 `23`。
- `python .\tools\check_specs.py --strict`：111 份目录项全部通过。
- 严格 UTF-8 与 XML：目标文件和两个工程通过；主工程仍为生产模块 26、`ClInclude` 12、四配置 `/utf-8` 4 项，专项项目未进入主工程 / filters。
- 唯一性 / 接线检查：`引用冲突 = 10`、私有三值枚举、索引字段和闭包函数各一处，插入 / 删除关系和值重载各两处；验证入口只有一个公开服务 import。
- 新增函数和变量使用中文名；生产新增代码未建立公开引用列表、L2 反向扫描、新仓库 / 锁 / 缓存 / SQL / 日志或等待逻辑。
- 完整命令、矩阵和一次验证辅助修正见同名验证记录。

## 完成声明边界

本施工只证明单个进程内 L1 服务可对当前节点的关系三角色和值四角色引用，在第一份可变候选形成前返回具名引用冲突，并在调用方显式给出目标节点和全部当前引用事实时原子同代次闭合；同时证明具名矩阵下显式退出、属性换代、自环 / 多角色去重和既有幂等优先级保持成立。

未注入私有索引损坏、真实分配失败或资源耗尽，未验证锁公平性、长时并发、性能、崩溃 / 重启 / 恢复、跨进程、生产消费者、L2 S3、完整场景退出、其它五类 L2 结构、A1 聚合面或具名独立集成验收。

本执行角色未执行 `git add`、提交或推送；发布交由交互智能体在复核七文件范围和异主现场后串行完成。
