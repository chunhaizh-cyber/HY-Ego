# 实例特征结构持久重放收敛施工记录

- 目标：`GOAL-IFR-INSTANCE-STRUCTURE-PERSISTENT-REPLAY-REPAIR`；起点`48f51807f6d39a7acaef104c08203fd66ce4462b`。
- S0：计划`ARCH-L4-INSTINCT-ROOT-DEMAND-PROVIDER`仍为可执行但其根材料持久验证在实例特征结构入口之前被阻断；真实持久L1首次材料包含完整7项映射，故问题是消费者把首次`H`误作当前读回`G`，而非持久材料缺失。
- 变更：`存在类数据服务::登记实例特征结构`在重放前读取并核验当前事实截止等于请求`G0`；重放直接复用首次持久写入结果，不重新提交旧写集；交付`Gread`使用当前截止，历史事实读回亦固定在该截止，`首次H`仍保留首次写入事实。首次写入仍通过端口提交，且提交前保留“可能已发布”保护。
- 函数级代码知识变更清单：修改`存在类数据服务::登记实例特征结构`。用途不变：存在 owner 下实例特征结构的首次登记、精确重放和同截止读回；新增的内部行为是跨进程重放收敛。生产调用边仍为普通应用装配。当前共享用途卡未覆盖该身份，结果发布后由知识库维护者按新blob增量复核；本记录不修改共享知识库。
- 明确未改：DTO、写集布局、幂等身份、owner、L1持久格式、需求服务、普通应用、启动、线程、规范与计划。
