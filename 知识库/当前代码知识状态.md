# 当前代码知识状态

状态日期：2026-09-23

状态：待按现行 `.h/.cpp` 重建

本文件只记录当前代码知识是否足以支持复用判断，不是能力完成清单，也不定义机器语义。

## 1. 当前结论

本次治理前既有的能力证据目录、主程序可达调用关系、知识图谱、代码流程图、逐行映射和候选队列已经全量退出。历史施工计划与施工记录已经删除。上述材料不得通过改扩展名、复制旧提交正文或改写路径的方式恢复为当前证据。

现阶段没有覆盖当前 HEAD 的完整能力目录、调用图或代码流程图。任何函数存在、调用关系、可达性、实现状态和复用结论，都必须直接回读现行 `.h/.cpp`、工程登记、生产调用点和正式合同。

## 2. 仍可使用的材料

- `规范/规范目录.md` 及其登记的现行规范：约束机器语义、物理归属和施工边界；
- `计划/计划索引.md` 及当前有效计划：约束当前复杂代码施工资格和范围；
- `流程图/目标业务流程图/`：描述目标业务合同，不证明当前代码已经实现；
- `知识库/函数用途/`：仅作候选导航，使用前必须核对绑定基线和当前源码；
- `验证记录/` 与 `验证记录/集成验收/`：只证明具名基线和具名范围内实际执行过的验证；
- Git 历史：只用于追溯，不直接授予当前施工或复用资格。

历史施工记录已经退出，不再承担迁移、物理退出、禁止恢复或当前实现证明。仍然有效的迁移、退出和禁止恢复约束必须从现行正式规范读取。

## 3. 当前复用门禁

复用任一函数或类型前至少核对：

1. 现行头文件中的完整声明、DTO、版本和所有权；
2. 现行源文件中的完整定义、成功与非成功分支、副作用和资源边界；
3. 当前工程登记、装配入口和全部生产调用点；
4. 直接调用者、直接被调者及其依赖方向；
5. 相关正式规范、有效设计和当前计划；
6. 具名验证是否绑定同一代码基线与同一合同版本。

只命中旧函数名、旧模块名、旧 `import`、旧源码 blob、旧行号、旧用途卡或 Git 历史，不足以证明当前可复用。用途卡与当前代码不一致时，立即标记过期并以当前源码重新建立结论。

## 4. 重建条件

后续重新建立能力目录、知识图谱或代码流程图时，必须同时满足：

- 明确冻结的当前提交；
- 从现行 `.h/.cpp` 和工程登记机械枚举源文件与函数身份；
- 逐项核对声明、定义、调用边、可达入口和非成功分支；
- 不从已删除材料复制旧模块身份、调用边、行号或完成结论；
- 将候选能力、静态实现、构建、运行、恢复、集成验收和业务闭环分层陈述；
- 发布前复核冻结提交至当前 HEAD 的差异，漂移时先刷新而不是继续沿用。

## 5. 当前完整度

| 知识项 | 当前状态 |
| --- | --- |
| 现行规范入口 | 可用 |
| 当前计划索引 | 可用 |
| 目标业务流程图 | 已去除旧 `.ixx` 物理证据；只表达目标合同 |
| 函数用途卡 | 候选导航；未完成全量现行头源复核 |
| 能力证据目录 | 已退出，待重建 |
| 主程序可达调用关系 | 已退出，待重建 |
| 知识图谱 | 已退出，待重建 |
| 代码流程图与逐行映射 | 已退出，待重建 |
| 历史施工计划与施工记录 | 已删除，不再作为证据入口 |

因此，当前允许的结论只有：旧模块证据已退出，现行代码事实必须直接从 `.h/.cpp`、工程登记和生产调用点取得。不得据此声明任何功能已经构建、运行、恢复、集成验收或完成业务闭环。
