# `普通应用上下文::读取系统世界树根读回`

- 完整签名：`std::optional<海中鱼巣::系统世界树根读回> 海中鱼巣::普通应用上下文::读取系统世界树根读回() const`
- 位置：`海中鱼巣/装配.普通应用.ixx:128`
- 模块：`海中鱼巣.装配.普通应用`
- 可见性：公开const成员
- 绑定提交：`12062f619b86aaa9a3ad585551f3bdef398326a8`
- 源码 blob：`2e7827a8721138e50be2e7e1ed38aac978311b64`
- 知识状态：当前
- 验证状态：静态锁和返回路径已核；并发与真实发布 `NOT_RUN`

## 用途

取得`系统世界树根发布锁_`后，按值复制并返回当前上下文已发布的可选`系统世界树根读回`。

## 事实与生命周期

- 返回的是I1调用方在本进程、同一上下文内保存的值式发布材料。
- 空值表示当前上下文尚未发布该材料。
- 返回副本离开锁后独立存在，不允许修改上下文内部值。
- 该材料不是R1场景owner中的根权威事实，也不提供跨进程恢复；需要根事实时仍须调用正式场景结构服务读回。

## 明确非职责

不触发I1、不建立根、不重新读取R1、不验证已保存材料是否仍对应当前事实代次，也不取得自我世界树根消费材料。

## 当前调用点

当前生产源码没有调用点。接口存在不能证明缓存式消费是合法的；I2正式路径按8120要求每次重新读取登记和整树。
