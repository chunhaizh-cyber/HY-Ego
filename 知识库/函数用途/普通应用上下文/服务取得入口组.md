# `普通应用上下文`服务取得入口组

- 稳定身份：下表18个完整签名
- 边界类别：服务取得函数
- 位置：`海中鱼巣/装配.普通应用.ixx:67-126`
- 模块：`海中鱼巣.装配.普通应用`
- 可见性：`普通应用上下文`公开成员
- 绑定提交：`12062f619b86aaa9a3ad585551f3bdef398326a8`
- 源码 blob：`2e7827a8721138e50be2e7e1ed38aac978311b64`
- 知识状态：当前
- 验证状态：静态源码和当前调用点已核；运行时引用身份 `NOT_RUN`

## 完整身份

| 服务 | 可写重载 | 只读重载 | 成员所有权 |
| --- | --- | --- | --- |
| 存在 | `L2存在结构服务& 普通应用上下文::取得L2存在结构服务() noexcept` | `const L2存在结构服务& 普通应用上下文::取得L2存在结构服务() const noexcept` | `unique_ptr<L2存在结构服务>` |
| 特征 | `L2特征结构服务& 普通应用上下文::取得L2特征结构服务() noexcept` | `const L2特征结构服务& 普通应用上下文::取得L2特征结构服务() const noexcept` | `unique_ptr<L2特征结构服务>` |
| 状态 | `L2状态结构服务& 普通应用上下文::取得L2状态结构服务() noexcept` | `const L2状态结构服务& 普通应用上下文::取得L2状态结构服务() const noexcept` | `unique_ptr<L2状态结构服务>` |
| 动态 | `L2动态结构服务& 普通应用上下文::取得L2动态结构服务() noexcept` | `const L2动态结构服务& 普通应用上下文::取得L2动态结构服务() const noexcept` | `unique_ptr<L2动态结构服务>` |
| 因果 | `L2因果结构服务& 普通应用上下文::取得L2因果结构服务() noexcept` | `const L2因果结构服务& 普通应用上下文::取得L2因果结构服务() const noexcept` | `unique_ptr<L2因果结构服务>` |
| 场景 | `L2场景结构服务& 普通应用上下文::取得L2场景结构服务() noexcept` | `const L2场景结构服务& 普通应用上下文::取得L2场景结构服务() const noexcept` | `unique_ptr<L2场景结构服务>` |
| 世界结构聚合 | `L2结构聚合服务& 普通应用上下文::取得L2结构聚合服务() noexcept` | `const L2结构聚合服务& 普通应用上下文::取得L2结构聚合服务() const noexcept` | `unique_ptr<L2结构聚合服务>` |
| 概念结构聚合 | `L2概念结构聚合服务& 普通应用上下文::取得L2概念结构聚合服务() noexcept` | `const L2概念结构聚合服务& 普通应用上下文::取得L2概念结构聚合服务() const noexcept` | `unique_ptr<L2概念结构聚合服务>` |
| 方法结构聚合 | `L2方法结构聚合服务& 普通应用上下文::取得L2方法结构聚合服务() noexcept` | `const L2方法结构聚合服务& 普通应用上下文::取得L2方法结构聚合服务() const noexcept` | `unique_ptr<L2方法结构聚合服务>` |

## 用途

向已获准消费者借出普通应用上下文持有的同一服务实例。非const重载允许调用服务公开写入口；const重载只允许调用服务只读公开面。

## 前置、不变量与生命周期

- 只能在`普通应用装配结果::成功()`成立且上下文仍存活时调用。
- 成功构造保证对应`unique_ptr`非空；getter自身不再检查空值。
- 返回引用不转移所有权，不得跨越上下文生命周期保存。
- 同一上下文的重复调用返回同一成员实例，不创建第二服务或第二owner。

## 当前调用点

当前生产源码中，I1/I2通过`取得L2结构聚合服务()`再取得同一场景服务。其它上下文直接getter以及概念/方法聚合getter当前没有生产调用点；接口存在不等于相应BIZ消费者已经接线。

## 明确非职责

不构造服务、不恢复服务、不校验业务资格、不取得锁、不读写事实。消费者仍须遵守各聚合和服务的公开合同；不得仅因存在直接getter就绕过目标设计要求的聚合边界。
