# 普通应用上下文特殊成员

- 稳定身份：5个显式删除函数
- 边界类别：非服务函数（生命周期约束）
- 模块：`海中鱼巣.装配.普通应用`
- 位置：`海中鱼巣/装配.普通应用.ixx:136-140`
- 绑定提交：`40a01afc450e296dcc65286d343218d4314454b4`
- 源码 blob：`377169094c789e42d1d802a36ff6f2d215e1bee0`
- 知识状态：当前

## 完整身份

- `普通应用上下文::普通应用上下文() = delete`
- `普通应用上下文::普通应用上下文(const 普通应用上下文&) = delete`
- `普通应用上下文& 普通应用上下文::operator=(const 普通应用上下文&) = delete`
- `普通应用上下文::普通应用上下文(普通应用上下文&&) = delete`
- `普通应用上下文& 普通应用上下文::operator=(普通应用上下文&&) = delete`

## 用途

禁止无依赖默认构造、复制或移动整个上下文，确保L1运行包、owner写端口、服务借用引用、锁和进程内发布材料在对象生命周期内保持同一地址与唯一所有权。

## 明确非职责

这些身份不可调用，不取得服务、不读写事实、不执行销毁。上下文正常析构仍由成员反向析构自动完成；“删除移动”也不证明内部单个服务不可移动。
