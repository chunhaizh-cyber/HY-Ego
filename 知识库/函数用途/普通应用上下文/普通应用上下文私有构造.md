# `普通应用上下文`私有构造函数

- 完整身份：`海中鱼巣::普通应用上下文::普通应用上下文(L1事实基座运行包&&, std::unique_ptr<不可变材料服务>&&, std::unique_ptr<L2语言结构服务>&&, std::unique_ptr<L2存在结构服务>&&, std::unique_ptr<L2特征结构服务>&&, std::unique_ptr<L2状态结构服务>&&, std::unique_ptr<L2需求结构服务>&&, std::unique_ptr<L2任务结构服务>&&, std::unique_ptr<L2任务子目标承接记录结构服务>&&, std::unique_ptr<需求任务子目标承接服务>&&, std::unique_ptr<L2动态结构服务>&&, std::unique_ptr<L2因果结构服务>&&, std::unique_ptr<L2场景结构服务>&&, std::unique_ptr<L2结构聚合服务>&&, std::unique_ptr<L2概念结构服务>&&, std::unique_ptr<L2概念结构聚合服务>&&, std::unique_ptr<L2概念名称结构服务>&&, std::unique_ptr<L2方法结构服务>&&, std::unique_ptr<L2方法结构聚合服务>&&, std::unique_ptr<L2任务方法路径结构服务>&&, std::unique_ptr<任务特征方法查找与条件提议提供者>&&) noexcept`
- 边界类别：服务装配函数
- 位置：`海中鱼巣/装配.普通应用.ixx:290-328`
- 模块：`海中鱼巣.装配.普通应用`
- 可见性：类私有；`构造普通应用上下文`为友元
- 绑定提交：`3a58b690f3b76a73b99ec02fcbdbaed7f90a2607`
- 源码 blob：`e31023e0bf4f672a78d63fee19c5f607e5a5803b`
- 知识状态：当前
- 验证状态：静态成员与初始化顺序已核；需求任务子目标承接服务及记录服务构造、getter同实例由治理专项覆盖；异常中途析构和生产非自检消费`NOT_RUN`

## 用途

在全部运行包和服务已经由装配函数成功形成后，把L1运行包、基础结构服务、任务子目标承接记录服务、需求任务子目标承接服务、任务方法路径与条件提议提供者及聚合服务所有权一次移动进上下文成员，并保持“下层所有者先声明、依赖者后声明；析构时反向释放”的结构顺序。

## 不变量和生命周期

- 默认、复制和移动构造均删除；上下文地址及内部引用关系在生命周期内稳定。
- 构造函数为`noexcept`，正文只移动已形成对象，不执行新的业务建立或事实写入。
- 聚合服务和上层服务借用的下层引用由成员声明顺序保证在其析构时仍有效。

## 明确非职责

不验证传入指针是否为空、不建立owner、不构造服务、不执行初始化或回滚。非空和依赖正确性由唯一友元装配函数负责。
