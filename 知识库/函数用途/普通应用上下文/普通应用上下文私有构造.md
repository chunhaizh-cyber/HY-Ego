# `普通应用上下文`私有构造函数

- 完整身份：`海中鱼巣::普通应用上下文::普通应用上下文(L1事实基座运行包&&, std::unique_ptr<不可变材料服务>&&, std::unique_ptr<L2语言结构服务>&&, std::unique_ptr<L2存在结构服务>&&, std::unique_ptr<L2特征结构服务>&&, std::unique_ptr<L2状态结构服务>&&, std::unique_ptr<L2动态结构服务>&&, std::unique_ptr<L2因果结构服务>&&, std::unique_ptr<L2场景结构服务>&&, std::unique_ptr<L2结构聚合服务>&&, std::unique_ptr<L2概念结构服务>&&, std::unique_ptr<L2概念结构聚合服务>&&, std::unique_ptr<L2方法结构服务>&&, std::unique_ptr<L2方法结构聚合服务>&&) noexcept`
- 边界类别：服务装配函数
- 位置：`海中鱼巣/装配.普通应用.ixx:230`
- 模块：`海中鱼巣.装配.普通应用`
- 可见性：类私有；`构造普通应用上下文`为友元
- 绑定提交：`40a01afc450e296dcc65286d343218d4314454b4`
- 源码 blob：`e941321b867bb1cd5e6526a54991ce832542d6f2`
- 知识状态：当前
- 验证状态：静态成员与初始化顺序已核；生产外上下文构造、getter和销毁已验证；异常中途析构 `NOT_RUN`

## 用途

在全部运行包和服务已经由装配函数成功形成后，把L1运行包、不可变材料、语言、六类世界结构服务及三个聚合/上层服务所有权一次移动进上下文成员，并保持“下层所有者先声明、依赖者后声明；析构时反向释放”的结构顺序。

## 不变量和生命周期

- 默认、复制和移动构造均删除；上下文地址及内部引用关系在生命周期内稳定。
- 构造函数为`noexcept`，正文只移动已形成对象，不执行新的业务建立或事实写入。
- 聚合服务和上层服务借用的下层引用由成员声明顺序保证在其析构时仍有效。

## 明确非职责

不验证传入指针是否为空、不建立owner、不构造服务、不执行初始化或回滚。非空和依赖正确性由唯一友元装配函数负责。
