# `普通应用配置::有效`

- 完整签名：`bool 海中鱼巣::普通应用配置::有效() const noexcept`
- 边界类别：非服务函数（值对象谓词）
- 位置：`海中鱼巣/装配.普通应用.ixx:68`
- 模块：`海中鱼巣.装配.普通应用`
- 可见性：公开成员
- 绑定提交：`40a01afc450e296dcc65286d343218d4314454b4`
- 源码 blob：`377169094c789e42d1d802a36ff6f2d215e1bee0`
- 知识状态：当前
- 验证状态：源码机械确认；坏合同、相对路径和点路径已由生产外合法消费者验证；真实生产目录故障 `NOT_RUN`

## 用途

作为普通应用装配入口的纯值配置前置判断。它要求不可变材料存储合同版本等于当前L2结构合同版本、受控根路径非空且为绝对路径，并拒绝任一路径分量为`.`或`..`。

## 明确非职责

不访问文件系统，不创建目录，不检查目标目录是否存在、可写或属于当前用户，也不检查语言、场景、线程或服务能力。返回`true`只证明配置字面形状合法，不证明运行环境完整。

## 当前调用点与边界

由`普通应用生产配置结果::成功()`复核已形成配置，也由`构造普通应用上下文`在任何L1或owner建立前再次调用。任一失败都不能继续装配。

依据：`施工记录/20260816_DATA-LANGUAGE-ORDINARY-APP-ASSEMBLY-READBACK_语言材料普通应用装配与首次权威读回施工记录_v0.1.md`。
