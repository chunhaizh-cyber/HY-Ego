# 语言结构服务特殊成员组

- 稳定身份：下表5个显式删除特殊成员
- 边界类别：非服务函数（唯一服务实例生命周期约束）
- 模块：`海中鱼巣.领域.服务.L2语言结构`
- 位置：`海中鱼巣/领域/服务.L2语言结构.ixx:1218-1222`
- 绑定提交：`9169937d75071de88cb47e9aaaa79f2f08459051`
- 源码 blob：`5f26f1fded5836498824d1e57b513098f0fc2483`
- 知识状态：当前
- 验证状态：编译期唯一实例约束与普通应用`unique_ptr`持有已核；真实生产运行 `NOT_RUN`

## 完整身份

| 完整签名 | 状态 |
| --- | --- |
| `L2语言结构服务::L2语言结构服务()` | `= delete` |
| `L2语言结构服务::L2语言结构服务(const L2语言结构服务&)` | `= delete` |
| `L2语言结构服务& L2语言结构服务::operator=(const L2语言结构服务&)` | `= delete` |
| `L2语言结构服务::L2语言结构服务(L2语言结构服务&&)` | `= delete` |
| `L2语言结构服务& L2语言结构服务::operator=(L2语言结构服务&&)` | `= delete` |

## 用途

禁止空造、复制和移动语言结构服务，强制实例在构造时一次绑定同一L1、不可变材料服务、语言owner与词条owner，并由普通应用上下文通过`unique_ptr`稳定持有。服务地址和内部两套写端口在生命周期内不因对象移动而改变。

## 生命周期和合法调用语境

唯一合法建立路径是具名公开构造；普通应用装配完成后，消费者只能经上下文getter取得同一服务引用。需要销毁时由普通应用上下文的RAII所有权整体释放，不允许用赋值替换内部依赖或把服务搬迁到第二上下文。

## 明确非职责

这些删除声明不证明全局只有一个进程实例，不提供服务定位器，也不执行owner登记、语言/词条CRUD、并发锁定或事实读回。唯一装配仍由普通应用流程和公开构造的依赖互证共同保证。

## 依据与证据

- `施工记录/20260816_DATA-L2-LANGUAGE-LEXEME-SERVICE-CLOSURE_词条公开服务闭合施工记录_v0.1.md`
- `施工记录/20260816_DATA-LANGUAGE-ORDINARY-APP-ASSEMBLY-READBACK_语言材料普通应用装配与首次权威读回施工记录_v0.1.md`
