# DTO默认比较与成功谓词组

绑定源码：`海中鱼巣/领域/L2概念名称结构.数据.h`，blob `a55e5a68dba8e94b0df6a5770f93bb451a480a61`

## 覆盖身份（14个）

### `operator==`（11个，全部`friend bool ... = default`）

| 所属结构体 | 行范围 | 比较语义 |
| --- | --- | --- |
| `L2概念名称关系身份` | 18-19 | 逐字段比较稳定编码 |
| `L2概念名称事实` | 36-37 | 逐字段比较身份、概念、词条、角色、语言、来源、幂等身份和生命周期 |
| `L2概念名称新增请求` | 49-50 | 逐字段比较请求头、概念、词条、角色、语言和来源 |
| `L2概念名称替换请求` | 63-64 | 逐字段比较请求头、关系身份、概念、词条、角色、语言和来源 |
| `L2概念名称退出请求` | 72-73 | 逐字段比较请求头和关系身份 |
| `L2概念名称写入结果` | 84-85 | 逐字段比较状态、截止和可选事实 |
| `L2概念名称读取请求` | 94-95 | 逐字段比较请求头、关系身份和截止 |
| `L2概念名称读取结果` | 107-108 | 逐字段比较状态、截止和可选事实 |
| `L2按概念名称组读取请求` | 116-117 | 逐字段比较请求头和概念身份 |
| `L2按词条概念名称候选组读取请求` | 125-126 | 逐字段比较请求头和词条身份 |
| `L2概念名称组读取结果` | 136-137 | 逐字段比较状态、截止和事实向量 |

### `成功()`成员函数（3个，`inline`，声明在结构体内，定义在文件尾部）

| 所属结构体 | 声明行 | 定义行 | 成功条件 |
| --- | --- | --- | --- |
| `L2概念名称写入结果` | 82 | 241-267 | 状态为`成功写入`或`精确重复`，且截止非零（写入结果必须有代次） |
| `L2概念名称读取结果` | 105 | 269-281 | 状态为`成功读取`，截止非零，且可选事实有值 |
| `L2概念名称组读取结果` | 134 | 283-293 | 状态为`成功读取`，截止非零 |

## 边界

- 所有`operator==`都是`= default`逐字段比较，只判断两个DTO结构是否完全相同，不判断请求是否合法或操作是否成功。
- `L2概念名称事实`的`operator==`比较全部字段包括幂等身份和生命周期；但服务内部冲突检测不使用此`operator==`，而是用内部helper按业务字段比较。
- `成功()`只校验返回结果的状态形状，不替代公开服务调用的业务语义。

## 当前结论

当前；纯值DTO比较和结果形状校验，与上列blob互证。
