# 方法登记根公开服务组

- 稳定身份：下表3个完整公开成员函数
- 边界类别：对外服务函数
- 模块：`海中鱼巣.领域.服务.L2方法结构`
- 位置：`海中鱼巣/领域/服务.L2方法结构.ixx:1509-1632`
- 绑定提交：`11e48a63c6ceeb59d0bbab186947c2ad352a9356`
- 源码 blob：`cc966503d0bd590369f87de102f3dc3152708b81`
- 知识状态：当前
- 实现状态：已实现并登记生产工程
- 验证状态：构建与静态合同PASS；真实首次/重复/并发、阶段19和当前服务独立集成验收`NOT_RUN`

| 行 | 完整签名 | 用途与结果 |
| ---: | --- | --- |
| 1509 | `L2方法登记根写入结果 L2方法结构服务::建立方法登记根(const L2方法登记根建立请求&)` | 在方法owner内原子建立唯一角色根及首次请求见证；同请求收敛，异请求返回幂等/引用冲突 |
| 1581 | `L2当前方法登记根读取结果 L2方法结构服务::按角色读取当前方法登记根(const L2当前方法登记根读取请求&) const` | 在非零守卫读取唯一角色根；缺根返回未找到 |
| 1620 | `L2当前方法登记根组读取结果 L2方法结构服务::读取全部当前方法登记根(const L2当前方法登记根组读取请求&) const` | 以零守卫选择调用时最新快照，或以非零守卫做同代0—1根组读 |

三个入口只管理方法登记根，不创建普通方法、方法首、条件、结果、动作入口或任务选择。已发布候选provider静态调用三入口，但未工程登记且无生产import，所以当前生产调用点为0；不得把源码调用文本升级为阶段19已接线。

非成功结果为空载荷、零变化；资源失败或发布未知时调用方必须保留原完整请求重放，不得换幂等身份猜测发布状态。当前新服务的真实根运行仍`NOT_RUN`，旧R1失败验收不适用于本ABI。
