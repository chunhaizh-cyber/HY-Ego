# L2状态内部共同状态映射、失败形成与写入边界组

- 稳定身份：下表24个内部服务函数
- 模块：`海中鱼巣.领域.服务.L2状态结构`
- 位置：`海中鱼巣/领域/服务.L2状态结构.ixx:105-374`
- 绑定提交：`614e4a32368a73ad81c274479fff4b0b12805fb1`
- 源码 blob：`80e2b71b219a25a548d23a297d90086396628b7e`
- 知识状态：当前
- 实现状态：已实现；模块内部使用
- 验证状态：各公开入口构建/专项覆盖正常映射；许可拒绝六映射静态PASS；真实资源与私损分支`NOT_RUN`

## 完整身份与用途

以下身份均位于`海中鱼巣::L2状态结构内部`，不是稳定消费ABI。

| 完整函数身份 | 用途 |
| --- | --- |
| `L1所有者范围写入幂等身份 映射状态结构类型登记幂等键(L2结构幂等身份) noexcept` | 将状态登记幂等身份放入固定域 |
| `L1所有者范围写入幂等身份 映射状态幂等键(L2结构幂等身份) noexcept` | 将状态CRUD幂等身份映射到L1键 |
| `L1所有者范围写入幂等身份 映射目标状态合同幂等键(L2结构幂等身份) noexcept` | 将目标合同CRUD幂等身份映射到独立域 |
| `L1所有者范围原始值材料 转换为L1材料(const L2原始值材料&)` | 将四类L2材料转为owner-scoped L1材料 |
| `L2原始值材料 转换为L2材料(const L1所有者范围原始值材料&)` | 将L1材料形成调用方拥有的L2值 |
| `L2结构状态 映射写入状态(L1所有者范围写入状态) noexcept` | 穷尽映射普通写状态，保留许可拒绝 |
| `L2结构状态 映射一致读取失败状态(L1所有者范围一致当前读取状态) noexcept` | 映射一致当前顶层失败 |
| `L2结构状态 映射当前项目状态(L1所有者范围一致当前读取项目状态) noexcept` | 映射一致投影单项状态 |
| `L2结构状态 映射历史读取状态(L1所有者范围读取状态) noexcept` | 映射历史单事实状态 |
| `L2结构状态 映射历史关系组失败状态(L1所有者范围读取状态) noexcept` | 映射历史关系组失败 |
| `L2结构状态 映射历史属性组失败状态(L1所有者范围读取状态) noexcept` | 映射历史属性组失败 |
| `L2结构状态 映射来源值组读取失败状态(L1所有者范围来源当前值组读取状态) noexcept` | 映射来源当前值组失败 |
| `L2结构状态 映射属性类型值组读取失败状态(L1所有者范围属性类型当前值组读取状态) noexcept` | 映射属性类型当前值组失败 |
| `L2状态结构类型登记状态 映射登记写入状态(L1所有者范围写入状态) noexcept` | 映射状态类型登记写结果 |
| `L2状态结构类型登记状态 映射登记读取失败状态(L1所有者范围一致当前读取状态) noexcept` | 映射状态登记一致读失败 |
| `L2结构状态 映射登记失败为结构状态(L2状态结构类型登记状态) noexcept` | 将登记失败收敛到公共结构状态 |
| `L2状态结构类型登记结果 形成登记失败(L2状态结构类型登记状态, std::uint64_t = 0) noexcept` | 形成空登记失败结果 |
| `L2状态写入结果 形成写入失败(L2结构状态, std::uint64_t = 0) noexcept` | 形成空状态新增失败结果 |
| `L2状态完整读取结果 形成完整读取失败(const L2状态完整读取请求&, L2结构状态, std::uint64_t = 0) noexcept` | 保留读取类别并形成空失败结果 |
| `L2当前状态组读取结果 形成组读取失败(L2结构状态, std::uint64_t = 0) noexcept` | 形成无成功前缀的空组失败 |
| `L2状态引用替换结果 形成引用替换失败(L2结构状态, std::uint64_t = 0, std::optional<std::uint64_t> = std::nullopt) noexcept` | 形成引用替换失败并可保留发布后代次 |
| `L2状态值换代结果 形成值换代失败(L2结构状态, std::uint64_t = 0, std::optional<std::uint64_t> = std::nullopt) noexcept` | 形成值换代失败并可保留发布后代次 |
| `L2状态退出结果 形成状态退出失败(L2结构状态, std::uint64_t = 0, std::optional<std::uint64_t> = std::nullopt) noexcept` | 形成退出失败并可保留发布后代次 |
| `bool 写入成功边界完整(const L1所有者范围写入结果&) noexcept` | 只在已提交/精确重复且owner、截止与首次载荷完整时确认已发布边界 |

## 明确非职责

本组不接受业务调用，不创建或读取完整状态，不决定重试策略。状态映射必须保留结构化失败；未知或矛盾形状归入内部不一致，不能降级为未找到或资源失败。

## 依据与证据

- `规范/4230_子规范_L2六类纯结构统一合同.md`第2.2、6节
- T1/T2及写许可拒绝保持记录
