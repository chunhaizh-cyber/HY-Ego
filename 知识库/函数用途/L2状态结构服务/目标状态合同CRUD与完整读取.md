# L2目标状态合同CRUD与完整读取

- 稳定身份：4个对外服务函数
- 模块：`海中鱼巣.领域.服务.L2状态结构`
- 位置：`海中鱼巣/领域/服务.L2状态结构.ixx:1364-1643`
- 绑定提交：`614e4a32368a73ad81c274479fff4b0b12805fb1`
- 源码 blob：`80e2b71b219a25a548d23a297d90086396628b7e`
- 知识状态：当前
- 实现状态：已实现并由方法服务静态消费读取入口
- 验证状态：双配置根构建与静态CRUD合同PASS；无独立专项，正式需求消费者`NOT_RUN`

## 完整身份与用途

| 完整函数身份 | 实际用途 |
| --- | --- |
| `L2目标状态合同新增结果 L2状态结构服务::新增目标状态合同(const L2目标状态合同新增请求&)` | 核对特征定义和目标判断I64比较注册，在状态owner一笔写集中建立合同节点、两关系、目标I64值和U64合同材料 |
| `L2目标状态合同替换结果 L2状态结构服务::替换目标状态合同(const L2目标状态合同替换请求&)` | 精确核对旧合同闭包，原子退出旧五事实并建立新合同闭包，历史读回新旧两份合同 |
| `L2目标状态合同退出结果 L2状态结构服务::退出目标状态合同(const L2目标状态合同退出请求&)` | 精确核对合同节点、两关系和两值闭包，原子退出并历史读回 |
| `L2目标状态合同完整读取结果 L2状态结构服务::读取完整目标状态合同(const L2目标状态合同完整读取请求&) const` | 按当前/历史分派，闭合合同事实并复核特征定义与目标判断比较注册 |

## 事实、副作用、事务和生命周期

三写入口都由状态owner形成一份完整不可变写集并恰一次提交；目标合同身份独立于实例状态身份。替换形成新合同身份并保留旧合同历史，退出只改变当前可见性。读取零写，当前投影与跨特征服务上下文必须共享请求守卫；发布结果未知只能用原完整请求收敛。

## 当前生产调用语境

`L2方法结构服务`在方法规格写前调用`读取完整目标状态合同`验证目标合同引用。新增、替换和退出没有模块外生产调用点；后继需求/目标设计引用该公开ABI不等于其代码已经消费。

## 明确非职责

目标合同保存特征、比较注册、目标I64、允许关系位和版本，不比较当前特征值，不判断目标满足，不创建需求、任务或方法。相同内容的不同合同身份不自动等价。

## 已知缺口

精确CRUD主要由详细设计冻结；5100只规定单需求绑定一个目标状态合同。真实合法消费者的增替退、满足判断、异常发布收敛、恢复和跨进程均`NOT_RUN`。

## 依据与证据

- `规范/5100_子规范_需求下级规则_20260720.md`
- `规范/详细设计/20260815_DATA-L2-ABSTRACT-TARGET-STATE-CONTRACT-CRUD_抽象目标状态合同CRUD详细设计_v0.1.md`
- `验证记录/20260815_DATA-L2-ABSTRACT-TARGET-STATE-CONTRACT-CRUD_抽象目标状态合同CRUD施工检查记录_v0.1.md`
