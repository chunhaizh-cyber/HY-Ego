# L2状态内部登记、投影与身份来源组

- 稳定身份：下表22个身份
- 边界类别：21个内部服务函数、1个服务装配函数
- 模块：`海中鱼巣.领域.服务.L2状态结构`
- 位置：`海中鱼巣/领域/服务.L2状态结构.ixx:377-732`
- 绑定提交：`a6b2c1d6b7662b16fe33e7f337694167a140514c`
- 源码 blob：`31567708934ada024b90d58bc020d43ea7b3084d`
- 知识状态：当前
- 实现状态：已实现；仅供本模块调用
- 验证状态：T1/T2静态与专项覆盖主要路径；私有损坏、真实异常和恢复`NOT_RUN`

## 完整身份与用途

除特别标注外均位于`海中鱼巣::L2状态结构内部`。

| 完整函数身份 | 类别 | 用途 |
| --- | --- | --- |
| `std::optional<L2状态关系退出事实> 形成关系退出事实(const L1所有者范围关系事实&, std::uint64_t) noexcept` | 内部 | 把指定截止关系形成退出投影 |
| `bool 历史关系匹配(const L1所有者范围关系事实&, 稳定编码, 稳定编码, 稳定编码, 稳定编码, L1结构所有者身份, std::uint64_t) noexcept` | 内部 | 精确核对历史关系编码、端点、类型、owner与退出代次 |
| `bool 状态未改字段相同(const L2状态事实&, const L2状态事实&) noexcept` | 内部 | 核对替换/换代不应改变的状态字段 |
| `std::optional<稳定编码> 查找编码映射(const L1所有者范围写入结果&, std::uint32_t) noexcept` | 内部 | 从首次写入本地键映射取得唯一新编码 |
| `std::array<稳定编码, 10> 登记编码组(const L2状态结构类型登记&) noexcept` | 内部 | 固定状态十类型编码顺序 |
| `std::optional<L2状态结构类型登记> 从写入形成登记(const L1所有者范围写入结果&, const L2状态结构类型登记请求&) noexcept` | 内部 | 从首次映射形成登记值 |
| `bool 登记节点历史完整(const L1事实基座服务&, 稳定编码, 节点种类, std::optional<L1所有者范围值表示种类>, std::uint64_t)` | 内部 | 核对单个登记节点历史形状 |
| `bool 登记历史完整(const L1事实基座服务&, const L2状态结构类型登记&)` | 内部 | 核对十登记节点同owner、表示与建立代次 |
| `稳定编码 选择状态值类型编码(const L2状态结构类型登记&, const L2原始值材料&, bool) noexcept` | 内部 | 按精确/时间角色和材料变体选择类型编码 |
| `bool 状态值类型与材料一致(const L2状态结构类型登记&, 稳定编码, const L1所有者范围原始值材料&, bool)` | 内部 | 核对值类型与材料表示一致 |
| `std::optional<L2属性事实> 形成截止属性事实(L1所有者范围值事实, const L2状态结构类型登记&, std::uint64_t, bool)` | 内部 | 夹持值生命周期并形成属性事实 |
| `std::optional<L2状态事实> 形成状态事实(L1所有者范围节点事实, L1所有者范围关系事实, L1所有者范围关系事实, L1所有者范围值事实, L1所有者范围值事实, const L2状态结构类型登记&, std::uint64_t)` | 内部 | 从节点、两关系和两值形成完整状态投影 |
| `bool 一致读取顶层结果::成功() const noexcept` | 内部 | 判断一致当前顶层已读取 |
| `一致读取顶层结果 解释一致读取顶层(const L1所有者范围一致当前读取结果&, std::uint64_t) noexcept` | 内部 | 统一解释空/漂移/截止和顶层状态 |
| `const L1所有者范围一致节点读取结果项* 查找唯一节点项(const L1所有者范围一致当前读取结果&, 稳定编码) noexcept` | 内部 | 查找唯一节点投影项 |
| `const L1所有者范围一致源关系组读取结果项* 查找唯一源关系组(const L1所有者范围一致当前读取结果&, 稳定编码, 稳定编码) noexcept` | 内部 | 查找唯一源端关系组 |
| `const L1所有者范围一致目标关系组读取结果项* 查找唯一目标关系组(const L1所有者范围一致当前读取结果&, 稳定编码, 稳定编码) noexcept` | 内部 | 查找唯一目标端关系组 |
| `const L1所有者范围一致属性值读取结果项* 查找唯一属性项(const L1所有者范围一致当前读取结果&, 稳定编码, 稳定编码) noexcept` | 内部 | 查找唯一节点属性值项 |
| `bool 普通当前节点完整(const L1所有者范围节点事实&, 稳定编码, std::uint64_t) noexcept` | 内部 | 核对引用端点是指定截止普通当前节点 |
| `L2结构状态 映射事实代次探测失败(L1中性读取状态) noexcept` | 内部 | 映射写前/历史前后代次探测失败 |
| `L2结构状态 检查端点结果头(const L2结构结果头&, std::uint64_t) noexcept` | 内部 | 把跨族端点公开结果约束到同一守卫 |
| `状态身份来源定位 初始化状态身份来源(const L1事实基座服务&, L1所有者范围写端口&)` | 服务装配 | 同owner建立/读回状态族锚点和归属关系类型，异常阻止服务公开 |

## 明确非职责

本组是状态公开CRUD的实现细节，不是可复用公共工具。它不解释主体、特征、值或时间的业务意义，也不允许其它模块直达L1重演状态身份来源。

## 依据与证据

- `规范/4230_子规范_L2六类纯结构统一合同.md`第3、4.4、5—7节
- T1/T2验证记录
