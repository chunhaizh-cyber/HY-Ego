# L2状态DTO身份构造与比较组

- 稳定身份：下表69个完整函数身份
- 边界类别：非服务函数（强类型值构造与逐字段比较）
- 模块：`海中鱼巣.领域.服务.L2状态结构`
- 位置：`海中鱼巣/领域/L2状态结构.数据.h:16-477`
- 绑定提交：`a6b2c1d6b7662b16fe33e7f337694167a140514c`
- 源码 blob：`3b62cfcedc4f6d9de0bca876b902939f9c578b07`
- 知识状态：当前
- 实现状态：已实现并登记生产工程
- 验证状态：69/69定义与字段形状已静态互证；T1/T2及目标合同记录覆盖部分真实比较语境，正式BIZ消费`NOT_RUN`

## 强类型构造与比较（30）

下表每种类型各有两个身份：`<类型>::<类型>(稳定编码 编码 = {}) noexcept`和`bool operator==(const <类型>&, const <类型>&)`。

| 强类型 | 用途 |
| --- | --- |
| `L2状态主体关系类型身份` | 包装状态主体关系类型编码并比较值 |
| `L2状态特征实例关系类型身份` | 包装状态特征实例关系类型编码并比较值 |
| `L2状态精确I64值类型身份` | 包装精确I64值类型编码并比较值 |
| `L2状态精确I64组值类型身份` | 包装精确I64组值类型编码并比较值 |
| `L2状态精确U64组值类型身份` | 包装精确U64组值类型编码并比较值 |
| `L2状态精确独立材料值类型身份` | 包装精确独立材料值类型编码并比较值 |
| `L2状态时间I64值类型身份` | 包装时间I64值类型编码并比较值 |
| `L2状态时间I64组值类型身份` | 包装时间I64组值类型编码并比较值 |
| `L2状态时间U64组值类型身份` | 包装时间U64组值类型编码并比较值 |
| `L2状态时间独立材料值类型身份` | 包装时间独立材料值类型编码并比较值 |
| `L2目标状态合同身份` | 包装独立目标状态合同编码并比较值，不得转换为实例状态身份 |
| `L2目标状态特征定义关系类型身份` | 包装目标合同到特征定义的关系类型编码并比较值 |
| `L2目标状态比较注册关系类型身份` | 包装目标合同到比较注册的关系类型编码并比较值 |
| `L2目标状态I64值类型身份` | 包装目标I64值类型编码并比较值 |
| `L2目标状态U64合同属性类型身份` | 包装目标合同材料属性类型编码并比较值 |

非零包装只提供C++类型隔离，不证明编码已登记、当前有效或属于正确owner。

## DTO逐字段比较（39）

下表每项完整函数形状均为`bool operator==(const <类型>&, const <类型>&)`，由编译器按公开字段逐项默认比较。

| DTO族 | DTO类型 |
| --- | --- |
| 状态类型登记 | `L2状态结构类型登记请求`、`L2状态结构类型登记`、`L2状态结构类型登记结果` |
| 目标合同登记与事实 | `L2目标状态合同结构类型登记请求`、`L2目标状态合同结构类型登记`、`L2目标状态合同结构类型登记结果`、`L2目标状态合同事实` |
| 目标合同请求 | `L2目标状态合同新增请求`、`L2目标状态合同替换请求`、`L2目标状态合同退出请求`、`L2目标状态合同完整读取请求` |
| 目标合同结果 | `L2目标状态合同新增结果`、`L2目标状态合同替换结果`、`L2目标状态合同退出结果`、`L2目标状态合同完整读取结果` |
| 状态事实与来源 | `L2状态事实`、`L2状态自有关系引用`、`L2状态身份来源读取请求`、`L2状态身份来源读取结果` |
| 状态新增与完整读取 | `L2状态新增请求`、`L2状态写入结果`、`L2状态完整读取请求`、`L2状态完整读取结果` |
| 状态组查询 | `L2按主体当前状态读取请求`、`L2按特征实例当前状态读取请求`、`L2按来源当前状态读取请求`、`L2按I64时间范围当前状态读取请求`、`L2当前状态组读取结果` |
| 引用替换 | `L2状态引用对关系引用`、`L2状态关系退出事实`、`L2状态引用对关系退出投影`、`L2状态自有关系退出投影`、`L2状态引用替换请求`、`L2状态引用替换结果` |
| 值换代与退出 | `L2状态值换代请求`、`L2状态值换代结果`、`L2状态退出闭包`、`L2状态退出请求`、`L2状态退出结果` |

## 用途、事实与合法调用语境

这些函数只构造或比较调用方已有的值对象，不访问L1、owner、锁或服务，不发布事实。它们适合请求重放、首次结果互证、当前/历史投影比较和验证断言；DTO相等只表示公开字段相等。

## 明确非职责

不得用构造成功或逐字段相等证明身份来源、请求有效、事实当前、幂等可共享、目标满足或结果成功；这些结论必须经守卫、成功谓词和正式服务读回取得。

## 依据与证据

- `规范/4230_子规范_L2六类纯结构统一合同.md`
- `规范/详细设计/20260815_DATA-L2-ABSTRACT-TARGET-STATE-CONTRACT-CRUD_抽象目标状态合同CRUD详细设计_v0.1.md`
- 绑定数据头中的两处宏展开和39个DTO比较定义
