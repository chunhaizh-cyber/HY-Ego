# L2状态新增、完整读取与身份来源

- 稳定身份：3个对外服务函数
- 模块：`海中鱼巣.领域.服务.L2状态结构`
- 位置：`海中鱼巣/领域/服务.L2状态结构.ixx:1647-1961`
- 绑定提交：`614e4a32368a73ad81c274479fff4b0b12805fb1`
- 源码 blob：`80e2b71b219a25a548d23a297d90086396628b7e`
- 知识状态：当前
- 实现状态：已实现；普通应用已装配
- 验证状态：T1两配置16/16；动态/因果/概念已有静态生产调用点；正式BIZ写入`NOT_RUN`

## 完整身份与用途

| 完整函数身份 | 实际用途 |
| --- | --- |
| `L2状态写入结果 L2状态结构服务::新增状态(const L2状态新增请求&)` | 同一守卫读取主体存在、特征实例及其宿主来源；在状态owner一笔写集中建立状态节点、族归属、主体/实例关系和精确/时间两值 |
| `L2状态完整读取结果 L2状态结构服务::读取完整状态(const L2状态完整读取请求&) const` | 按当前或历史分派，闭合状态节点、三关系、两值、主体/实例来源和生命周期 |
| `L2状态身份来源读取结果 L2状态结构服务::读取状态身份来源(const L2状态身份来源读取请求&) const` | 证明状态节点属于状态owner、经非自环族归属指向固定锚点，并返回当前/历史生命周期 |

## 事实、副作用、事务和生命周期

新增是唯一写入口：六个新事实同owner、同代次原子发布，主体存在必须等于特征实例公开读回的宿主。精确值和时间值各使用独立属性槽；材料可为I64、非空I64/U64组或独立材料引用。两个读取入口零写，当前读取使用一致投影，历史读取不借当前事实补项。

## 当前生产调用语境

动态与因果服务多处调用身份来源入口验证状态端点；概念服务调用完整读取验证世界事实引用。当前正式源码没有模块外`新增状态`调用点。

## 明确非职责

本组不判断状态真实、重要或当前最优，不比较值，不从特征当前值自动生成状态，也不形成动态、因果或目标满足。来源稳定编码只是不透明来源引用。

## 已知缺口

1160要求实例状态包含场景归属，当前4230和ABI不含场景字段；知识只登记冲突。真实BIZ新增、资源耗尽、私损、恢复、跨进程和独立集成验收`NOT_RUN`。

## 依据与证据

- `规范/4230_子规范_L2六类纯结构统一合同.md`第4.4、5—8节
- `规范/1160_根规范_状态节点_20260720.md`第2—4节
- `验证记录/20260811_L2-STRUCTURE-CRUD-REBUILD-T1_验证记录.md`
