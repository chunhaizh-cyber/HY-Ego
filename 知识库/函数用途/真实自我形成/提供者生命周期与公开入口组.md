# 提供者生命周期与公开入口组

绑定源码：`海中鱼巣/业务/提供者.真实自我形成.ixx`，blob `56a8b5e77d535999611331c73a47699afc02574b`

## 覆盖身份（9个）

### `真实自我形成提供者`特殊成员与构造（6个）

| 函数 | 访问 | 行 | 种类 |
| --- | --- | --- | --- |
| `explicit 真实自我形成提供者(L2结构聚合服务&) noexcept` | public | 35-37 | 用户定义构造 |
| `真实自我形成提供者()` | public | 39 | `= delete` |
| `真实自我形成提供者(const &)` | public | 40 | `= delete` |
| `operator=(const &)` | public | 41-42 | `= delete` |
| `真实自我形成提供者(&&)` | public | 43 | `= delete` |
| `operator=(&&)` | public | 44-45 | `= delete` |

提供者不可复制、不可移动、不可默认构造。只接受`L2结构聚合服务&`作为唯一依赖。

### `形成()`（1个公开服务函数）

```cpp
真实自我形成结果 形成(const 真实自我形成请求& 请求) noexcept
```
行47-53。**当前为桩实现**。注释描述五步状态机：
1. 根正式读回（验证已有自我）
2. 自我存在形成
3. 所在场景形成
4. 所在场景成员形成
5. 内部世界和内部宿主形成

当前桩始终返回`状态 = 真实自我形成状态::内部不一致`，不执行任何L2操作。

### `读取已发布自我()`（1个公开读取函数）

```cpp
std::optional<真实自我事实> 读取已发布自我() const
```
行55-58。线程安全读取。使用`std::lock_guard< std::mutex>`保护`发布锁_`，返回`已发布自我_`可选值的副本。

### `真实自我形成结果::成功()`（1个，定义在此模块）

```cpp
bool 成功() const noexcept
```
行23-31。声明在数据头`真实自我形成结果`结构体内，定义在此模块。返回`true`当且仅当`已形成自我`有值且`状态`为`已形成`或`精确重复`。

## 边界

- 提供者的`形成()`是桩实现，不执行任何真实L2操作；启动链调用它会得到`内部不一致`结果。
- `读取已发布自我()`使用`mutable std::mutex`，支持从`const`成员函数调用。
- 提供者import了`L2存在结构`、`L2场景结构`、`L2结构聚合`和`装配.普通应用`，但当前桩实现未使用这些依赖。
- 启动链（`启动.应用程序.ixx`）阶段17已接线到此提供者的`形成()`入口。

## 当前结论

当前；提供者生命周期和公开入口，`形成()`为桩实现；真实五步状态机和业务闭环`NOT_RUN`。
