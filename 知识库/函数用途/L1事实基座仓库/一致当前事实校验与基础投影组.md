# 一致当前事实校验与基础投影组

- 稳定身份：10个完整函数身份
- 边界类别：私有一致当前请求、局部完整性和基础投影
- 模块：`海中鱼巣.核心.仓库.L1事实基座`
- 位置：`海中鱼巣/核心/仓库.L1事实基座.ixx:2274-2549`
- 绑定提交：`8a88fefb20bcbd5bc1e53941c4642a20b51f5c99`
- 源码 blob：`4351bb0767533d4f099807f4ec23ce092e1665fb`
- 知识状态：当前
- 验证状态：10/10身份与v1/owner一致投影调用点静态互证；故障注入`NOT_RUN`

## 完整身份与用途

| 完整身份 | 用途 |
| --- | --- |
| `static bool L1事实基座仓库::一致当前请求有效(const L1中性一致当前读取请求& 请求)` | 校验v1合同版本、非零期望代次、各选择有效且全部具名编码跨组唯一 |
| `static bool L1事实基座仓库::一致编码唯一当前(const 状态& 值, std::uint64_t 编码, 一致当前事实种类 种类) noexcept` | 确认一个编码只在预期当前事实表命中，且没有历史或其它类型占用矛盾 |
| `static bool L1事实基座仓库::一致当前节点基本完整(const 状态& 值, std::uint64_t 键, const 节点事实& 事实) noexcept` | 核对当前节点自身编码、生命周期、节点种类、owner和属性排序基本不变量 |
| `static bool L1事实基座仓库::一致当前值局部完整(const 状态& 值, std::uint64_t 键, const 值事实& 事实) noexcept` | 核对值自身及所属、类型、来源、材料引用和owner的直接当前闭包 |
| `static bool L1事实基座仓库::一致当前节点局部完整(const 状态& 值, std::uint64_t 键, const 节点事实& 事实) noexcept` | 在节点基本完整上逐槽互证属性类型和当前值事实 |
| `static bool L1事实基座仓库::一致当前关系局部完整(const 状态& 值, std::uint64_t 键, const 关系事实& 事实) noexcept` | 核对关系、源/目标/类型节点、owner与当前三类索引的局部一致性 |
| `static std::optional<L1中性一致当前读取项目状态> L1事实基座仓库::一致当前缺失状态(const 状态& 值, 稳定编码 编码, 一致当前事实种类 期望种类) noexcept` | 对当前未命中编码判定未找到、已退出或种类不匹配；矛盾返回空optional |
| `static std::optional<L1事实基座仓库::一致具名事实内部结果项> L1事实基座仓库::读取一致具名当前事实(const 状态& 值, 稳定编码 编码, 一致当前事实种类 种类)` | 按种类执行局部完整性检查并形成一项事实副本或具名缺失状态 |
| `static std::optional<L1事实基座仓库::一致源关系组内部结果项> L1事实基座仓库::读取一致源关系组(const 状态& 值, const L1中性一致源关系组选择项& 选择)` | 从当前源索引形成有序完整关系与目标节点对端投影 |
| `static std::optional<L1事实基座仓库::一致目标关系组内部结果项> L1事实基座仓库::读取一致目标关系组(const 状态& 值, const L1中性一致目标关系组选择项& 选择)` | 从当前目标索引形成关系与源节点对端投影 |

## 边界

这些helper只在调用者已经持有同一仓库共享许可时读取传入`状态`，不自行选代次、不重试、不修索引。空optional表示内部结构无法互证，不等于业务未找到。

## 依据与缺口

- `规范/4015_子规范_L1简化事实基座.md:132-180,253`

未运行人为索引损坏、错种类和分配失败；私有局部检查不能替代`状态完整`的全仓不变量证明。
