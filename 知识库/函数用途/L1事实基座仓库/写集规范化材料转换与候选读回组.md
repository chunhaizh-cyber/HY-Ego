# 写集规范化材料转换与候选读回组

- 稳定身份：12个完整函数身份
- 边界类别：私有写事务分配、规范化、材料校验和候选读回
- 模块：`海中鱼巣.核心.仓库.L1事实基座`
- 位置：`海中鱼巣/核心/仓库.L1事实基座.ixx:2266-2271,3030-3238`
- 绑定提交：`8a88fefb20bcbd5bc1e53941c4642a20b51f5c99`
- 源码 blob：`4351bb0767533d4f099807f4ec23ce092e1665fb`
- 知识状态：当前
- 验证状态：12/12身份与共用写内核静态互证；完整写矩阵`NOT_RUN`

## 完整身份与用途

| 完整身份 | 用途 |
| --- | --- |
| `static std::optional<稳定编码> L1事实基座仓库::分配稳定编码(状态& 值) noexcept` | 跳过永久占用编码，从候选状态分配新的非零不可复用稳定编码并推进游标 |
| `static L1中性写入结果 L1事实基座仓库::中性写入结果(const L1中性写集请求& 请求, L1中性写入状态 状态值, std::uint64_t 代次, bool 已发布, L1中性重试边界 重试, std::vector<std::pair<L1中性写集本地键, 稳定编码>> 映射 = {})` | 统一形成v1内部写结果及确定编码映射 |
| `static std::uint64_t L1事实基座仓库::中性引用排序键(const L1中性事实引用& 引用)` | 给稳定引用和本地引用形成互不冲突的纯值排序键 |
| `static bool L1事实基座仓库::中性引用有效(const L1中性事实引用& 引用) noexcept` | 验证稳定编码或本地键非零 |
| `static std::optional<L1中性写集请求> L1事实基座仓库::规范化中性写集(const L1中性写集请求& 请求)` | 复制请求并按本地键、引用和退出编码确定排序，形成逐字段幂等比较形状 |
| `static bool L1事实基座仓库::中性请求结构有效(const L1中性写集请求& 请求, std::unordered_set<std::uint32_t>& 本地键)` | 校验五类写项的键唯一性、引用有效性、节点类型/表示一致、退出唯一和属性槽选择不重复 |
| `static std::optional<值表示种类> L1事实基座仓库::转换值表示种类(const std::optional<L1中性值表示种类>& 值) noexcept` | 把中性表示枚举转内部表示 |
| `static std::optional<原始值材料> L1事实基座仓库::转换原始材料(const L1中性原始值材料& 材料)` | 把四类中性材料转换成内部材料，独立材料引用保持编码 |
| `static std::optional<L1中性原始值材料> L1事实基座仓库::转换中性原始材料(const 原始值材料& 材料)` | 把内部材料复制回v1中性材料 |
| `static bool L1事实基座仓库::中性材料等于(const 原始值材料& 实际, const L1中性原始值材料& 期望) noexcept` | 跨内部/中性变体逐类型比较材料 |
| `static bool L1事实基座仓库::中性候选读回完整(const 状态& 值, const L1中性写集请求& 请求, const std::vector<std::pair<L1中性写集本地键, 稳定编码>>& 映射, std::uint64_t 发布代次, L1结构所有者身份 写入所有者) noexcept` | 按原本地键映射逐项读回新节点、关系、值、属性槽和退出事实，互证代次、owner和不可变字段 |
| `static bool L1事实基座仓库::表示匹配(值表示种类 表示, const 原始值材料& 材料)` | 校验属性类型表示与I64、I64组、U64组或独立材料引用形状精确匹配 |

## 边界

规范化只建立确定比较顺序，不改变请求语义；排序相同仍必须逐字段比较完整写集。分配只在候选状态发生，发布前失败不能泄漏编码为当前事实。候选读回是发布前和发布后精确互证，不产生第二结果或自动修复。

## 依据与缺口

- `规范/4015_子规范_L1简化事实基座.md:17-43,85-124`
- `规范/4040_子规范_不透明结构事务候选确认撤销与最后发布.md:17-65`

未运行编码耗尽、所有材料变体、同键异序同义和候选读回故障注入。
