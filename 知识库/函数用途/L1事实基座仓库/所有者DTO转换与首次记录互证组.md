# 所有者DTO转换与首次记录互证组

- 稳定身份：11个完整函数身份
- 边界类别：私有值式转换与owner幂等记录互证
- 模块：`海中鱼巣.核心.仓库.L1事实基座`
- 位置：`海中鱼巣/核心/仓库.L1事实基座.ixx:2001-2204`
- 绑定提交：`8a88fefb20bcbd5bc1e53941c4642a20b51f5c99`
- 源码 blob：`4351bb0767533d4f099807f4ec23ce092e1665fb`
- 知识状态：当前
- 验证状态：11/11身份与owner读取、首次写入材料入口静态互证；异常路径`NOT_RUN`

## 完整身份与用途

| 完整身份 | 用途 |
| --- | --- |
| `static L1所有者范围节点事实 L1事实基座仓库::转换所有者范围节点(const 节点事实& 事实)` | 复制节点、owner、生命周期和属性槽为owner-aware公共DTO |
| `static L1所有者范围关系事实 L1事实基座仓库::转换所有者范围关系(const 关系事实& 事实) noexcept` | 复制关系端点、类型、owner和生命周期 |
| `static L1所有者范围原始值材料 L1事实基座仓库::转换所有者范围材料(const 原始值材料& 材料)` | 把内部独立材料引用映射为owner合同引用，其余三类材料保持值式内容 |
| `static L1所有者范围值事实 L1事实基座仓库::转换所有者范围值(const 值事实& 事实)` | 复制值事实及其owner、来源、材料和生命周期 |
| `static L1所有者范围事实副本 L1事实基座仓库::转换所有者范围事实(const L1事实副本& 事实)` | 按节点/关系/值变体分派到对应owner-aware事实转换 |
| `static L1所有者范围事实引用 L1事实基座仓库::转换所有者范围写集引用(const L1中性事实引用& 引用)` | 把内部规范化稳定/本地引用转换为v2同构强类型引用 |
| `static std::optional<L1所有者范围值表示种类> L1事实基座仓库::转换所有者范围写集表示(std::optional<L1中性值表示种类> 表示) noexcept` | 映射可选值表示枚举 |
| `static L1所有者范围原始值材料 L1事实基座仓库::转换所有者范围写集材料(const L1中性原始值材料& 材料)` | 把首次规范化内部材料转换为v2公共材料 |
| `static L1所有者范围写集请求 L1事实基座仓库::转换所有者范围写集(const L1中性写集请求& 请求)` | 逐字段重建首次规范化v2五类写项，并把中性幂等键映射回owner幂等身份 |
| `static L1所有者范围写入结果 L1事实基座仓库::转换所有者范围写入结果(L1结构所有者身份 所有者, const L1中性写入结果& 内部)` | 把私有首次成功结果映射为含owner的v2公共结果 |
| `static bool L1事实基座仓库::所有者范围首次记录完整(const 状态& 值, L1结构所有者身份 所有者, L1所有者范围写入幂等身份 幂等身份, const 所有者范围幂等记录& 记录)` | 局部互证所选owner幂等记录的规范化写集、首次代次、编码映射和首次结果，不扫描其它owner账 |

## 边界

转换函数只改变DTO形状，不授权跨owner、不产生新事实、不推进代次。首次记录互证是读取前完整性门，不是恢复实现或幂等账修复器；失败由公开入口映射为内部不一致。

## 依据与缺口

- `规范/4015_子规范_L1简化事实基座.md:188-282`

未运行大容器分配失败、四类材料往返和损坏幂等记录隔离；内部转换不得作为公开适配ABI复用。
