# 内部：普通概念编码、写集与完整投影组

- 稳定身份：下表20个内部服务函数
- 边界类别：内部服务函数
- 模块：`海中鱼巣.领域.服务.L2概念结构`
- 位置：`海中鱼巣/领域/服务.L2概念结构.ixx:771-1678,1852-2619`
- 绑定提交：`11e48a63c6ceeb59d0bbab186947c2ad352a9356`
- 源码 blob：`996c2c63d9692b4b7e9bea7f4af4c7897d6d714e`
- 知识状态：当前

| 行 | 稳定身份 | 实际用途 |
| ---: | --- | --- |
| 771 | `是概念保留登记幂等身份(...)` | 隔离四项私有登记身份，禁止公开请求占用 |
| 779 | `映射概念幂等身份(...)` | 无损映射公开幂等身份到唯一owner域 |
| 784 | `形成普通概念写入失败(...)` | 形成空概念、零变更的写非成功结果 |
| 792 | `形成普通概念读取失败(...)` | 保留读取类别并清空概念负载 |
| 804 | `形成概念定义组读取失败(...)` | 保留类别/历史截止并清空合同与定义组 |
| 814 | `形成普通概念退出失败(...)` | 清空退出概念、退出边和替代边 |
| 823 | `映射历史读取状态(...)` | 映射L1历史读取状态到L2结构状态 |
| 842 | `编码概念定义目标(...)` | 把十类闭合定义目标编码为确定U64组 |
| 887 | `解码概念定义目标(...)` | 只解码合同允许的十类定义目标 |
| 931 | `编码世界事实引用(...)` | 复用定义编码并从类型上排除概念身份 |
| 938 | `解码世界事实引用(...)` | 拒绝概念分支和坏编码，恢复九类世界事实引用 |
| 950 | `映射普通概念材料到L1(...)` | 无损转换普通值材料到owner-scoped L1表示 |
| 970 | `普通概念写集数量可表示(...)` | 校验确定高位分区和完整写集数量可表示 |
| 988 | `读取普通概念节点映射(...)` | 校验首次映射覆盖本请求且编码互异 |
| 1043 | `形成普通概念写集(...)` | 形成节点、关系、值和属性槽的完整规范化owner写集 |
| 1197 | `历史关系组头完整(...)` | 校验历史组读头、方向、端点、类型和许可截止 |
| 1209 | `重建普通概念自有事实(...)` | 从同截止owner事实重建签名、合同、定义、父边、证据和生命周期 |
| 1852 | `校验普通概念公开引用(...)` | 模板化经材料与六类世界公开服务互证全部跨owner引用 |
| 2267 | `读取全部当前普通概念身份(...)` | 枚举当前根归属关系的源概念并稳定排序 |
| 2319 | `普通概念签名相同(...)` | 比较本体根、规则、值式项和定义角色/目标完整签名 |

本组是`建立普通概念`、`读取普通概念`、定义双向读取和退出的内部承载。闭合编码和固定分区是实现细节，不是外部ABI；上层不得直接构造L1写集、扫描owner关系或绕过公开端点互证。`普通概念签名相同`只比较确定签名，不负责DATA外同义或抽象成立裁决。
