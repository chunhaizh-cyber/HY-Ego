# 内部：直接关系与概念图组

- 稳定身份：下表9个内部服务函数
- 边界类别：内部服务函数
- 模块：`海中鱼巣.领域.服务.L2概念结构`
- 位置：`海中鱼巣/领域/服务.L2概念结构.ixx:2622-2869,3428-3555`
- 绑定提交：`11e48a63c6ceeb59d0bbab186947c2ad352a9356`
- 源码 blob：`996c2c63d9692b4b7e9bea7f4af4c7897d6d714e`
- 知识状态：当前

| 行 | 稳定身份 | 实际用途 |
| ---: | --- | --- |
| 2622 | `形成概念关系写入失败(...)` | 形成空当前/退出边、零变更的写失败结果 |
| 2629 | `形成概念关系组失败(...)` | 保留读取类别并形成空关系组 |
| 2637 | `读取直接关系事实(...)` | 当前优先、历史回退读取一条owner直接边 |
| 2674 | `形成直接关系写集(...)` | 形成新增、替换或退出的确定owner写集 |
| 2693 | `重建直接关系首次结果(...)` | 从首次映射与历史截止重建新边和退出边 |
| 2759 | `读取直接关系组核心(...)` | 按端点方向有界读取当前/历史活动直接边并排序 |
| 3428 | `当前概念图完整(...)` | 纯值校验同根、多父、无环和唯一根可达 |
| 3504 | `当前概念图存在路径(...)` | 在值式DAG中判断有向可达，用于避免冗余替代边 |
| 3525 | `形成普通概念退出写集(...)` | 关闭目标当前自有事实/关联边并建立确定替代边 |

本组只操作直接边和调用内值式图，不保存传递闭包、高度、坐标或显示树。`当前概念图存在路径`是一次事务候选的纯值辅助，不是可复用概念查询ABI。
