# 直接上下位关系增替退与读取组

- 稳定身份：下表5个公开成员函数
- 边界类别：对外服务函数
- 模块：`海中鱼巣.领域.服务.L2概念结构`
- 位置：`海中鱼巣/领域/服务.L2概念结构.ixx:4677-4855`
- 绑定提交：`11e48a63c6ceeb59d0bbab186947c2ad352a9356`
- 源码 blob：`996c2c63d9692b4b7e9bea7f4af4c7897d6d714e`
- 知识状态：当前
- 实现状态：已实现并登记生产工程
- 验证状态：具名检查与双配置构建证据存在；真实多父/成环/并发/资源失败和正式BIZ消费`NOT_RUN`

| 行 | 完整签名 | 实际用途 |
| ---: | --- | --- |
| 4677 | `L2概念直接上位写入结果 L2概念结构服务::新增概念直接上位关系(const L2概念直接上位新增请求&)` | 在值式当前概念图通过同根、无环、唯一根可达校验后原子建立直接边 |
| 4689 | `L2概念直接上位写入结果 L2概念结构服务::替换概念直接上位关系(const L2概念直接上位替换请求&)` | 同一写集中退出具名旧边并建立新边，保持下位概念至少一个父路径 |
| 4703 | `L2概念直接上位写入结果 L2概念结构服务::退出概念直接上位关系(const L2概念直接上位退出请求&)` | 退出具名直接边，并拒绝令普通概念失去全部父边或根可达性 |
| 4717 | `L2概念直接上位组读取结果 L2概念结构服务::读取概念直接上位关系组(const L2概念直接上位组读取请求&) const` | 按下位端点返回当前/历史直接上位边组，稳定排序且有界 |
| 4789 | `L2概念直接下位组读取结果 L2概念结构服务::读取概念直接下位关系组(const L2概念直接下位组读取请求&) const` | 按上位端点返回当前/历史直接下位边组，稳定排序且有界 |

方向固定为“上位概念 -> 下位概念”；只保存直接边，允许同根多父。传递闭包、祖先后代、高度、坐标、锥体和显示树均应由当前直接边重建，不能作为本组写入材料或第二事实来源。
