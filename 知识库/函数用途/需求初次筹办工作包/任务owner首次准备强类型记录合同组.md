# task owner 首次准备强类型记录合同组

- 稳定身份：首次准备区20个纯值函数身份
- 边界类别：非服务函数
- 模块：`海中鱼巣.领域.服务.L2任务结构`导出数据头
- 位置：`海中鱼巣/领域/L2任务结构.数据.h:443-716`
- 绑定提交：`17edb9d974be6d961d404247c88a064b8f73df58`
- 源码blob：`522e226e8bfa702492f2527a2c511df94ea0f23e`
- 知识状态：当前（只限首次准备增量区，不宣称全文件重新枚举）
- 实现状态：已实现强类型材料、记录、请求、结果和守卫
- 验证状态：计划内V01—V15分账；V04/V07具名子项与V09/V12/V15保持`NOT_RUN`

| 分组 | 函数身份 | 用途 |
| --- | --- | --- |
| 关系与完整请求 | 2个默认`operator==`、`L2任务初次筹办准备完整请求材料完整(...)` | 固定记录关系类型和双键完整请求的值式合同 |
| 首次裁决 | `L2任务初次筹办特征值事实相同(...)`、裁决材料`operator==`、`L2任务目标判断关系位(...)`、`L2任务初次筹办首次需求裁决材料完整(...)` | 保存并互证G0需求、成员、完整列表项、当前特征值、目标合同和比较证据 |
| 首次记录 | 记录事实`operator==`、`L2任务初次筹办准备记录事实完整(...)` | 要求任务、虚拟存在、列表项、轮次1、G0/G1与记录生命周期一致 |
| 融合写请求/结果 | 请求`operator==`、`L2新增任务与首次初次筹办准备记录请求有效(...)`、结果`成功()`和结果`operator==` | 核对融合写输入及`已提交/精确重复`的完整G1读回 |
| 按准备键读取 | 请求`operator==`、结果`成功()`和结果`operator==` | 允许空记录成功；有记录时必须同时返回首次任务和身份来源 |
| 按记录身份读取 | 请求`operator==`、结果`成功()`、结果`operator==`、`L2任务初次筹办准备记录读取请求有效(...)` | 固定当前/历史类别与历史截止，并要求完整三件套读回 |

## 非职责与使用边界

该数据头只定义强类型载荷和纯值守卫。它不提交事务、不读取L1、不裁决需求是否满足、不形成父回流、不创建新筹办轮次，也不定义成员分配优先级。调用者不得把`成功()`当作服务调用、生产接线或普通筹办消费证据。

依据：`17edb9d9`源码、施工知识变更清单、计划内验证及`172b6969`局部图谱。
