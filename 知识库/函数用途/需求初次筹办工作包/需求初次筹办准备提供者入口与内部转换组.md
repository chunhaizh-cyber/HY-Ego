# 需求初次筹办准备提供者入口与内部转换组

- 稳定身份：R1515、R1516及8个相邻成员/内部函数
- 边界类别：候选业务入口、内部服务函数、服务装配函数和显式删除
- 模块：`海中鱼巣.领域.内部治理.服务.需求初次筹办准备`
- 位置：`海中鱼巣/领域/内部治理/服务.需求初次筹办准备.ixx:23-246`
- 绑定提交：`17edb9d974be6d961d404247c88a064b8f73df58`
- 源码blob：`49d5c31745cdd82eccaf577626706ed22a393409`
- 知识状态：当前
- 实现状态：候选代码已实现；未正式接线
- 验证状态：计划内专项通过列明矩阵；独立验收`FAIL / 未正式接线`

| 身份/行 | 完整签名或分组 | 用途 |
| --- | --- | --- |
| 23 | `L2任务初次筹办准备完整请求材料 转换完整请求(const 需求初次筹办准备请求&) noexcept` | 值式转换完整外层请求 |
| 31 / 50 | `转换首次裁决(...)`、`重构首次裁决(...)` | 在外层裁决快照与task owner首次裁决材料间无损转换 |
| 69 / 94 | `映射读取失败(...)`、`映射裁决失败(...)` | 把下层具名状态映射到外层状态，不吞掉许可/资源/漂移 |
| R1516 / 109 | `需求初次筹办准备结果 形成首次结果(...)` | 从首次记录、任务和身份来源形成已形成/精确重复工作包 |
| 143—151 | 构造、删除复制构造、删除复制赋值 | 借用同一查询组合器和task owner服务，不取得事实所有权 |
| R1515 / 153 | `需求初次筹办准备结果 形成唯一任务与初次筹办工作包(需求初次筹办准备请求) noexcept` | 先读首次记录；无记录且G0当前才裁决与融合写；发布未知只按原键收敛 |

## 事实、副作用与并发

R1515自身不持有业务账。它先调用R1518；只有首次记录未找到时才调用`裁决需求当前满足`，未满足后调用R1517。事务和同列表项0/1当前任务竞争由task owner服务负责。异常映射为`资源失败/内部错误`，不形成日志式成功。

## 当前调用语境与缺口

唯一仓库内动态调用者是治理专项函数`验证需求初次筹办工作包闭环()`。普通应用上下文没有保存或暴露本provider，普通启动线程没有调用边；独立验收正式入口巡检为`0/5`，Release仍携带治理测试模块。父回流、新筹办轮次、工作包消费、路径/实例/冻结/执行、成员结算和结果分配均不属于本入口已实现职责。
