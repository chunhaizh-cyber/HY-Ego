# 支持线程候选租约与公开操作组

- 稳定身份：10个身份
- 边界类别：线程provider公开操作与生命周期函数
- 模块：`海中鱼巣.线程.候选.支持线程`
- 位置：`海中鱼巣/线程/候选.支持线程.ixx:56-103,376-552`
- 绑定提交：`40e85893ac04fc37b42f43ae0f996d3186af5b66`
- 源码 blob：`f3580ff4ce414c9ba93b4e9b2abfb78ce6304f93`
- 知识状态：当前
- 实现状态：已实现并由EVENT provider调用
- 验证状态：生产外专项双配置各161项PASS，连续20轮及早停命中20/20；真实资源失败和三个具体支持线程整组仍`NOT_RUN`

| 完整签名 | 用途 |
| --- | --- |
| `支持线程候选租约::支持线程候选租约(支持线程候选租约&&) noexcept` | 转移唯一控制块和jthread |
| `支持线程候选租约& 支持线程候选租约::operator=(支持线程候选租约&&) = delete` | 禁止移动赋值 |
| `支持线程候选租约::支持线程候选租约(const 支持线程候选租约&) = delete` | 禁止复制 |
| `支持线程候选租约& 支持线程候选租约::operator=(const 支持线程候选租约&) = delete` | 禁止复制赋值 |
| `支持线程候选租约::~支持线程候选租约()` | fallback安全停止并join |
| `支持线程候选见证 支持线程候选租约::读取见证() const noexcept` | 读取当前物理见证 |
| `支持线程候选租约::支持线程候选租约(std::shared_ptr<候选控制块>, std::jthread&&) noexcept` | 仅供内部访问器装配租约 |
| `支持线程候选创建结果 创建支持线程候选(const 支持线程候选创建请求&, 支持线程入口端口&, 线程生命周期发布端口&) noexcept` | 墓碑身份、创建控制块和物理jthread |
| `支持线程进入等待结果 等待支持线程进入见证(const 支持线程候选租约&, const 支持线程进入等待请求&) noexcept` | 有界等待内部进入或完成 |
| `支持线程候选回收结果 停止并连接支持线程启动候选(支持线程候选租约&&, const 支持线程候选回收请求&) noexcept` | 消费租约，停止、等待完成并无条件join |

公开操作只证明候选线程生命周期；不等于具体EVENT/缓存/证据线程已经正确处理材料。诊断预算超时后仍会继续等待并join，因此“超过预算”是诊断状态，不是detach许可。
