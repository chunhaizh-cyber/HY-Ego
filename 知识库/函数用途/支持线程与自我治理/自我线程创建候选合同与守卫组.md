# 自我线程创建合同与守卫组

- 稳定身份：10个；其中数据头5个身份、候选模块5个内部守卫；结果谓词声明/定义为同一身份
- 边界类别：非服务值式合同、预冻结生命周期端口与内部创建守卫
- 模块：`海中鱼巣.线程.创建.自我线程`
- 位置：`海中鱼巣/线程/自我线程生产创建.数据.h:9-136`、`海中鱼巣/线程/创建.自我线程.ixx:41-89`
- 绑定提交：`195964fcbf9436f1647b576e60234462d5443e53`
- 源码 blob：`b2b7402d6a5caaa7b40e11b999044123c6c03d1e`、`d7a3c496a9cbd280c92e08557177013fe49f7f88`
- 知识状态：当前（仅限 195964fc 基线）
- 实现状态：创建合同和守卫已实现；数据头新增治理退出/处理槽状态与冻结裁决字段
- 验证状态：身份/守卫静态互证；治理专项覆盖槽状态、裁决次数、冻结后继/裁决和重试；构建、普通应用长时运行、资源故障注入及跨进程恢复 `NOT_RUN`

## 数据头身份（当前 195964fc）

| 完整签名 | 用途 |
| --- | --- |
| `bool operator==(const 自我线程创建幂等身份&, const 自我线程创建幂等身份&) = default` | 比较创建幂等身份 |
| `bool operator==(const 自我线程停门见证&, const 自我线程停门见证&) = default` | 比较停门见证全部字段 |
| `bool operator==(const 任务结果治理后继输入&, const 任务结果治理后继输入&) = default` | 比较冻结后继输入的治理批次、引用和事实截止值 |
| `bool operator==(const 任务结果治理处理槽快照&, const 任务结果治理处理槽快照&) = default` | 比较进程内治理处理槽快照；不表示任务事实已写入 |
| `bool 自我线程生产创建结果::成功() const noexcept` | 要求已创建/精确重复且停门见证完整；不证明治理循环或任务完成 |

## 当前处理槽合同增量

本批未新增函数身份，但发布了治理处理状态和值字段：`自我治理循环退出结果::任务目标裁决已形成`、`任务结果治理处理槽状态::目标待裁决 / 不可比较终止 / 目标裁决已形成`、`任务结果治理处理槽快照::目标裁决次数`和`::目标裁决`。这些字段只记录进程内治理槽，不写任务生命周期或需求事实。

## 内部守卫

| 完整签名 | 用途 |
| --- | --- |
| `bool 根消费材料相同(const 自我世界树根消费材料&, const 自我世界树根消费材料&) noexcept` | 比较 I2 值式根材料 |
| `bool 请求语义相同(const 自我线程生产创建请求&, const 自我线程生产创建请求&) noexcept` | 比较重复创建的稳定业务选择 |
| `bool 请求有效(const 自我线程生产创建请求&) noexcept` | 核对合同、身份、代次、邮箱和 I2/SELF-FORM 根对应 |
| `bool 停门见证完整(const 自我线程停门见证&) noexcept` | 要求真实进入、后继停门、门关闭和零冻结批次 |
| `自我线程生产创建状态 合并启动失败与回收状态(自我线程生产创建状态, const 自我线程启动候选回收结果&) noexcept` | 合并原失败与候选回收结果 |

## 当前边界与证据

该合同仍依赖正式 SELF-FORM 材料和普通应用形态，但两文件已经登记工程并进入普通应用/治理专项接线。线程生命周期发布端口和逻辑身份已由通用线程投影模块拥有，不在本批数据头重复声明。生命周期端口为空时只代表观察投影不发布，不代表物理线程创建失败。治理专项未把资源故障注入、路径验证或任务生命周期迁移写成通过。

- 施工记录：`施工记录/20260821_RUNTIME-GOVERNANCE-TASK-RESULT-COMPLETION-ADJUDICATION_施工记录.md` §3.2、§4.4
- 验证记录：`验证记录/20260821_RUNTIME-GOVERNANCE-TASK-RESULT-COMPLETION-ADJUDICATION_验证记录.md` §3-§5
