# 自我线程创建候选合同与守卫组

- 稳定身份：11个；其中数据头6个身份、候选模块5个内部守卫；结果谓词声明/定义为同一身份
- 边界类别：候选非服务函数与预冻结端口合同
- 模块：候选`海中鱼巣.线程.创建.自我线程`
- 位置：`海中鱼巣/线程/自我线程生产创建.数据.h:9-110`、`海中鱼巣/线程/创建.自我线程.ixx:27-87,342-347`
- 绑定提交：`40e85893ac04fc37b42f43ae0f996d3186af5b66`
- 源码 blob：`1554eefc3a4617f3930b28ff828397865f825a91`、`b7bbea4da7d531b40bf00b0b47e9c8fbe8e9fc31`
- 知识状态：当前HEAD候选源码已核；工作区新版本待发布后重审
- 实现状态：两文件未登记工程、无生产import
- 验证状态：正式HEAD 11项身份/守卫静态互证；构建、阶段20、线程真实进入和失败回收均`NOT_RUN`

## 数据头身份

| 完整签名 | 用途 |
| --- | --- |
| `bool operator==(const 自我线程创建幂等身份&, const 自我线程创建幂等身份&) = default` | 比较创建幂等身份 |
| `bool operator==(const 自我线程逻辑身份&, const 自我线程逻辑身份&) = default` | 比较候选线程逻辑身份 |
| `virtual 自我线程生命周期发布端口::~自我线程生命周期发布端口() = default` | 预冻结端口生命周期 |
| `virtual 自我线程生命周期发布结果 自我线程生命周期发布端口::发布(const 自我线程生命周期发布请求&) noexcept = 0` | 预冻结生命周期发布合同 |
| `bool operator==(const 自我线程停门见证&, const 自我线程停门见证&) = default` | 比较停门见证全部字段 |
| `bool 自我线程生产创建结果::成功() const noexcept` | 要求已创建/精确重复且停门见证完整 |

## 内部守卫

| 完整签名 | 用途 |
| --- | --- |
| `bool 根消费材料相同(const 自我世界树根消费材料&, const 自我世界树根消费材料&) noexcept` | 比较I2值式根材料 |
| `bool 请求语义相同(const 自我线程生产创建请求&, const 自我线程生产创建请求&) noexcept` | 比较重复创建的稳定业务选择 |
| `bool 请求有效(const 自我线程生产创建请求&) noexcept` | 核对合同、身份、代次、邮箱和I2/SELF-FORM根对应 |
| `bool 停门见证完整(const 自我线程停门见证&) noexcept` | 要求真实进入、后继停门、门关闭和零冻结批次 |
| `自我线程生产创建状态 合并启动失败与回收状态(自我线程生产创建状态, const 自我线程启动候选回收结果&) noexcept` | 合并原失败与候选回收结果 |

此合同仍导入预冻结SELF-FORM及普通应用形态。源码存在不等于最终ABI已机械匹配；工作区另有未发布改动，必须等正式代码提交后重新制卡。
