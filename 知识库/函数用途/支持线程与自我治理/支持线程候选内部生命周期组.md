# 支持线程候选内部生命周期组

- 稳定身份：19个内部身份
- 边界类别：内部服务函数（物理线程、见证、投影和join内核）
- 模块：`海中鱼巣.线程.候选.支持线程`
- 位置：`海中鱼巣/线程/候选.支持线程.ixx:109-370`
- 绑定提交：`40e85893ac04fc37b42f43ae0f996d3186af5b66`
- 源码 blob：`f3580ff4ce414c9ba93b4e9b2abfb78ce6304f93`
- 知识状态：当前
- 实现状态：已实现；不是跨模块ABI
- 验证状态：19/19定义和父调用互证；专项覆盖早停、进入、停止、故障和join主矩阵

## 完整身份

| 完整签名 | 只负责 |
| --- | --- |
| `候选控制块::候选控制块(const 支持线程候选创建请求&, 支持线程入口端口&, 线程生命周期发布端口&)` | 借入入口和投影端口并保存选择 |
| `bool 请求有效(const 支持线程候选创建请求&) noexcept` | 只允许EVENT、缓存统计、持久证据三类配对 |
| `bool 等待毫秒有效(std::uint32_t, std::uint64_t) noexcept` | 核对合同和有界诊断等待 |
| `std::uint64_t 当前发生时间毫秒() noexcept` | 形成最佳努力系统时钟投影值 |
| `bool 已进入终态(线程生命周期投影) noexcept` | 识别已退出或异常退出 |
| `void 记录投影未接受(候选控制块&) noexcept` | 饱和累计8110投影未接受次数 |
| `void 发布生命周期已加锁(候选控制块&, 线程生命周期投影, std::uint64_t, std::optional<std::uint64_t>) noexcept` | 顺序形成并尽力发布生命周期消息 |
| `支持线程候选见证 读取见证值(const 候选控制块&) noexcept` | 锁内复制物理见证并附加投影失败计数 |
| `支持线程入口结果 复核入口结果(const 支持线程入口结果&, bool) noexcept` | 收窄入口返回与停止见证 |
| `void 发布异常终态链已加锁(候选控制块&, std::uint64_t, std::optional<std::uint64_t>) noexcept` | 尽力发布故障至异常退出链 |
| `void 发布正常终态链已加锁(候选控制块&, std::optional<std::uint64_t>) noexcept` | 尽力发布收尾至已退出链 |
| `void 运行入口包装(const std::shared_ptr<候选控制块>&, std::stop_token) noexcept` | 形成系统线程身份、进入见证、调用入口并闭合完成见证 |
| `bool 租约有效(const 支持线程候选租约&) noexcept` | 互证控制块和可连接线程 |
| `void 锁存停止并发布(候选控制块&, std::jthread&) noexcept` | 锁存停止、request_stop并尽力发布停止投影 |
| `void 安全连接(const std::shared_ptr<候选控制块>&, std::jthread&) noexcept` | 无detach地停止、join并锁存连接见证 |
| `支持线程候选租约 租约访问::形成(std::shared_ptr<候选控制块>, std::jthread&&) noexcept` | 内部形成唯一租约 |
| `const std::shared_ptr<候选控制块>& 租约访问::控制块(const 支持线程候选租约&) noexcept` | 内部读取控制块 |
| `std::jthread& 租约访问::线程(支持线程候选租约&) noexcept` | 内部取得物理线程 |
| `bool 租约访问::可连接(const 支持线程候选租约&) noexcept` | 内部查询joinable |

## 边界

物理见证由控制块、`std::jthread`和join形成；8110发布失败只累计观察计数，不推翻物理成功。进程级身份墓碑不回收，同运行代次/逻辑身份重用会拒绝。内部时钟只用于线程观察发生时间，不是业务时间或事实代次。
