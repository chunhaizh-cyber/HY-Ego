# 自我线程创建候选对象组

- 稳定身份：14个身份；13个类成员和1个结果谓词定义
- 边界类别：候选线程provider与内部生命周期函数
- 模块：候选`海中鱼巣.线程.创建.自我线程`
- 位置：`海中鱼巣/线程/创建.自我线程.ixx:91-347`
- 绑定提交：`40e85893ac04fc37b42f43ae0f996d3186af5b66`
- 源码 blob：`b7bbea4da7d531b40bf00b0b47e9c8fbe8e9fc31`
- 知识状态：当前HEAD候选源码已核；工作区新版本待发布后重审
- 实现状态：未登记工程、无生产import；阶段20计划未形成当前结果
- 验证状态：14/14身份静态互证；全部构建和运行范围`NOT_RUN`

| 完整签名 | 用途 |
| --- | --- |
| `explicit 自我线程::自我线程(自我线程生命周期发布端口*) noexcept` | 借入可空预冻结生命周期端口 |
| `自我线程::~自我线程() noexcept` | 锁存停止、唤醒并join候选线程 |
| `自我线程::自我线程(const 自我线程&) = delete` | 禁止复制 |
| `自我线程& 自我线程::operator=(const 自我线程&) = delete` | 禁止复制赋值 |
| `自我线程::自我线程(自我线程&&) = delete` | 禁止移动 |
| `自我线程& 自我线程::operator=(自我线程&&) = delete` | 禁止移动赋值 |
| `自我线程生产创建结果 自我线程::创建并使自我线程停在治理运行门(const 自我线程生产创建请求&) noexcept` | 锁定选择、建立邮箱/物理线程、等待真实停门或失败回收 |
| `自我线程启动候选回收结果 自我线程::收口启动失败候选(std::uint64_t) noexcept` | 公开串行回收未成功候选 |
| `std::optional<自我线程停门见证> 自我线程::读取停门见证() const` | 锁内复制已发布停门见证 |
| `自我线程启动候选回收结果 自我线程::收口启动失败候选内部(std::uint64_t) noexcept` | 锁存停止、等待完成、join并清理候选状态 |
| `自我线程生命周期发布结果 自我线程::发布生命周期(自我线程生命周期投影, std::optional<自我线程生命周期投影>, std::uint64_t) noexcept` | 经可空预冻结端口发布观察投影 |
| `void 自我线程::执行线程入口正文()` | 形成进入/停门序号并等待开放或停止；不运行治理循环 |
| `void 自我线程::线程入口() noexcept` | 捕获入口异常并形成原子完成见证 |
| `bool 自我线程生产创建结果::成功() const noexcept` | 结果纯值谓词，见合同卡 |

正式HEAD正文在运行门被开放时立即锁存停止，明确没有治理循环；这只能支撑“创建并停在关闭门”的候选设计。未登记工程、依赖预冻结ABI和未发布工作区改动共同决定它不能作为当前阶段20能力或可直接复用入口。
