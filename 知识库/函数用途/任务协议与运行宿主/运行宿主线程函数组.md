# 运行宿主线程函数组

- 稳定身份：12个身份
- 边界类别：领域运行消息线程壳
- 模块：`海中鱼巣.线程.运行宿主线程`
- 位置：`海中鱼巣/线程/运行宿主线程.ixx:42-164`
- 绑定提交：`04c1179ca5cc3b2945dc39fa3aa6f9e2b56f9088`
- 源码 blob：`5d881574bfe8a11e90147a018c8a6308e26103ca`
- 知识状态：当前
- 实现状态：已登记工程；无生产import
- 验证状态：12/12身份静态互证；当前普通应用启动、停止和异常资源路径`NOT_RUN`

| 完整签名 | 用途 |
| --- | --- |
| `explicit 运行宿主线程::运行宿主线程(std::uint64_t)` | 建立有界运行消息队列 |
| `运行宿主线程::~运行宿主线程()` | fallback锁存停止并join物理线程 |
| `运行宿主线程::运行宿主线程(const 运行宿主线程&) = delete` | 禁止复制 |
| `运行宿主线程& 运行宿主线程::operator=(const 运行宿主线程&) = delete` | 禁止复制赋值 |
| `运行宿主线程状态 运行宿主线程::当前状态() const` | 读取线程壳状态 |
| `bool 运行宿主线程::线程已进入() const` | 读取物理线程进入见证 |
| `bool 运行宿主线程::线程可收口() const` | 以joinable反向判断是否已收口 |
| `const 有界运行消息队列& 运行宿主线程::只读消息队列() const` | 暴露队列只读引用 |
| `运行宿主线程操作结果 运行宿主线程::启动(std::uint64_t, std::uint64_t)` | 发布调度周期材料并启动yield线程壳 |
| `运行宿主线程操作结果 运行宿主线程::请求停止并发送停止消息(std::uint64_t, std::uint64_t)` | 发布停止材料并锁存停止 |
| `运行宿主线程操作结果 运行宿主线程::收口等待()` | join并更新已停止状态 |
| `运行宿主线程操作结果 运行宿主线程::拒绝(运行宿主线程拒绝原因) const` | 形成线程壳拒绝结果 |

现行8120明确把该类型归为领域消息线程，不是程序运行宿主。它的调度和停止消息都标记为非机器事实；队列入队成功、线程进入或join不证明任何业务工作已调度完成。
