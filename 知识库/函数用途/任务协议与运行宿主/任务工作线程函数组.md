# 任务工作线程函数组

- 稳定身份：13个
- 边界类别：已形成任务工作结果的进程内有界尾部
- 模块：`海中鱼巣.线程.任务工作线程`
- 位置：`海中鱼巣/线程/任务工作线程.ixx:38-188`
- 绑定提交：`494824dcd63039860013cb7230016afdf0f16b6e`
- 源码 blob：`e24723a5d4f4856e977b6334d1851caa9886c02e`
- 知识状态：当前
- 实现状态：已登记工程；由任务结果生产运行包装配
- 验证状态：专项已覆盖接收、形成、上交、拒绝和停止排空；真实方法执行`NOT_RUN`

| 完整签名 | 只负责 |
| --- | --- |
| `bool 任务工作线程提交结果::成功() const noexcept` | 判断材料已接收 |
| `任务工作线程::任务工作线程(std::uint64_t, 任务管理线程&) noexcept` | 借入唯一管理线程并冻结有界容量 |
| `任务工作线程::~任务工作线程() noexcept` | 停止接收、排空并连接物理线程 |
| `任务工作线程::任务工作线程(const 任务工作线程&) = delete` | 禁止复制 |
| `任务工作线程& 任务工作线程::operator=(const 任务工作线程&) = delete` | 禁止复制赋值 |
| `任务工作线程::任务工作线程(任务工作线程&&) = delete` | 禁止移动 |
| `任务工作线程& 任务工作线程::operator=(任务工作线程&&) = delete` | 禁止移动赋值 |
| `任务工作线程操作结果 任务工作线程::启动() noexcept` | 启动结果尾部线程 |
| `任务工作线程提交结果 任务工作线程::提交已形成任务工作结果(const 任务工作结果材料&) noexcept` | 把已形成工作结果放入有界队列 |
| `任务工作线程操作结果 任务工作线程::停止接收并排空() noexcept` | 停止新材料并要求排空已接收材料 |
| `任务工作线程操作结果 任务工作线程::收口等待() noexcept` | 连接物理线程并锁存终态 |
| `任务工作线程快照 任务工作线程::读取快照() const noexcept` | 复制队列和形成、上交、拒绝计数 |
| `void 任务工作线程::运行结果尾部() noexcept` | 形成版本3回执并提交任务管理线程 |

本模块不执行任务方法，不生成状态或动态，不写任务事实，也不判定任务完成。它的输入必须已经是工作结果材料；外部真实工作结果来源仍未由当前专项证明。
