# 任务结果回执协议函数组

- 稳定身份：11个非服务函数
- 边界类别：版本3非权威工作结果材料的纯值形成、比较与准入
- 模块：`海中鱼巣.线程.任务结果回执协议`
- 位置：`海中鱼巣/线程/任务结果回执协议.ixx:81-380`
- 绑定提交：`494824dcd63039860013cb7230016afdf0f16b6e`
- 源码 blob：`fa6aaebb6632b05d6fe6096a2bfa32a5d5248537`
- 知识状态：当前
- 实现状态：已登记工程；由任务工作线程形成、由任务管理线程准入
- 验证状态：正式生产链专项已覆盖首次、重复、协议拒绝和同轮异义；真实方法执行`NOT_RUN`

| 完整签名 | 只负责 |
| --- | --- |
| `bool operator==(const 任务结果回执身份组&, const 任务结果回执身份组&)` | 比较七个强类型定位身份 |
| `bool operator==(const 任务结果回执来源执行绑定&, const 任务结果回执来源执行绑定&)` | 比较来源请求、序号、批次、幂等键与任务/方法/动作入口 |
| `bool operator==(const 任务工作结果材料&, const 任务工作结果材料&)` | 完整比较已形成工作结果材料 |
| `bool 任务结果回执形成结果::成功() const noexcept` | 判断已形成且含回执 |
| `bool 任务结果回执身份组完整(const 任务结果回执身份组&) noexcept` | 复核七个强类型定位身份 |
| `bool 任务结果回执来源执行绑定完整(const 任务结果回执来源执行绑定&) noexcept` | 复核来源执行绑定非零及身份形状 |
| `bool 任务结果回执材料相同(const 任务结果回执材料&, const 任务结果回执材料&) noexcept` | 比较完整版本3回执，含来源版本与结果写入幂等身份 |
| `任务结果回执准入结果 拒绝任务结果回执材料(任务结果回执拒绝原因) noexcept` | 形成协议拒绝 |
| `任务结果回执准入结果 标记任务结果回执内部不一致(任务结果回执拒绝原因) noexcept` | 形成内部不一致结果 |
| `任务结果回执准入结果 复核任务结果回执材料(const 任务结果回执材料&, const 任务结果回执准入上下文&) noexcept` | 复核版本、幂等、时效、身份、来源绑定和同任务顺序 |
| `任务结果回执形成结果 形成任务结果回执(const 任务工作结果材料&) noexcept` | 机械形成版本3回执并复用同一准入合同 |

回执材料仍明确标记为非权威。它不能提供正式执行轮次、任务完成或需求满足；上行桥必须从正式实例方法取得执行轮次，并从任务实际结果正式入口独立读回权威事实。
