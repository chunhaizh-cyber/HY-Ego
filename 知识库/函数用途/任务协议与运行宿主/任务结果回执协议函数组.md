# 任务结果回执协议函数组

- 稳定身份：7个非服务函数
- 边界类别：非权威执行结果材料的纯值比较与准入
- 模块：`海中鱼巣.线程.任务结果回执协议`
- 位置：`海中鱼巣/线程/任务结果回执协议.ixx:104-286`
- 绑定提交：`04c1179ca5cc3b2945dc39fa3aa6f9e2b56f9088`
- 源码 blob：`1b01e1133fe45bda4e253da1e7e32113c37fe6b1`
- 知识状态：当前
- 实现状态：已登记工程；无普通应用生产import
- 验证状态：7/7定义与内部调用静态互证；当前生产回执链`NOT_RUN`

| 完整签名 | 只负责 |
| --- | --- |
| `bool 任务结果回执句柄相同(const 任务结果回执节点句柄&, const 任务结果回执节点句柄&)` | 比较legacy节点句柄三字段 |
| `bool 任务结果回执句柄组相同(const 任务结果回执句柄组&, const 任务结果回执句柄组&)` | 比较回执中的七个定位句柄 |
| `bool 任务结果回执材料相同(const 任务结果回执材料&, const 任务结果回执材料&)` | 比较完整回执及可选来源冻结请求 |
| `bool 任务结果回执全部句柄形状有效(const 任务结果回执句柄组&)` | 复核全部可选句柄形状 |
| `bool 任务结果回执必填句柄齐全(const 任务结果回执句柄组&)` | 要求任务、状态、动态、方法、动作、场景和主体定位齐全 |
| `任务结果回执准入结果 拒绝任务结果回执材料(任务结果回执拒绝原因)` | 形成协议级拒绝结果 |
| `任务结果回执准入结果 复核任务结果回执材料(const 任务结果回执材料&, const 任务结果回执准入上下文&)` | 总控版本、幂等、时效、句柄、来源冻结请求和同任务顺序准入 |

回执材料明确标记为非权威，只能定位执行结果证据。`可准入`不等于任务完成、需求满足或业务事实已经提交；消费者仍须独立回读权威事实并通过领域提交入口处理。
