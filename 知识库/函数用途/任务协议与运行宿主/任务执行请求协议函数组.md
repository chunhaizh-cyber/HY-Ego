# 任务执行请求协议函数组

- 稳定身份：29个非服务函数
- 边界类别：任务执行冻结材料的纯值合同与一致性复核
- 模块：`海中鱼巣.线程.协议.任务执行请求`
- 位置：`海中鱼巣/线程/协议.任务执行请求.ixx:70-538`
- 绑定提交：`04c1179ca5cc3b2945dc39fa3aa6f9e2b56f9088`
- 源码 blob：`8f92feaf71fb26993e618a0efa9cf27615b24080`
- 知识状态：当前
- 实现状态：已登记工程；由任务管理线程和回执协议导入，无普通应用生产import
- 验证状态：29/29身份静态互证；生产冻结、排队、工作执行和回执闭环`NOT_RUN`

| 完整签名 | 只负责 |
| --- | --- |
| `bool 任务执行节点句柄相同(const 节点句柄&, const 节点句柄&) noexcept` | 比较节点句柄三字段 |
| `bool 任务执行主信息句柄相同(const 主信息句柄&, const 主信息句柄&) noexcept` | 比较主信息句柄三字段 |
| `bool 任务执行关系句柄相同(const 关系句柄&, const 关系句柄&) noexcept` | 比较关系句柄三字段 |
| `bool 任务执行调度请求::operator==(const 任务执行调度请求&) const noexcept` | 比较调度、时序、幂等及回执选择 |
| `bool 任务执行关系证据::完整() const noexcept` | 检查关系及两端句柄形状 |
| `bool 任务执行关系证据::当前有效() const noexcept` | 要求关系完整且记录状态有效 |
| `bool 任务执行关系证据::operator==(const 任务执行关系证据&) const noexcept` | 比较关系证据全部字段 |
| `bool 任务执行节点身份::当前可读() const noexcept` | 复核幂等键、节点和主信息句柄 |
| `bool 任务执行节点身份::operator==(const 任务执行节点身份&) const noexcept` | 比较节点身份全部字段 |
| `bool 任务执行生命周期材料::当前() const noexcept` | 复核当前生命周期关系、阶段、版本和时间 |
| `bool 任务执行生命周期材料::operator==(const 任务执行生命周期材料&) const noexcept` | 比较生命周期材料 |
| `bool 任务执行选择材料::完整() const noexcept` | 复核任务方法选择记录及来源版本 |
| `bool 任务执行选择材料::operator==(const 任务执行选择材料&) const noexcept` | 比较选择材料全部字段 |
| `bool 任务执行任务材料::完整承接() const noexcept` | 复核任务、来源需求、场景、目标状态和当前生命周期 |
| `bool 任务执行任务材料::operator==(const 任务执行任务材料&) const noexcept` | 比较任务材料全部字段 |
| `bool 任务执行方法材料::完整() const noexcept` | 复核方法登记、角色、虚拟存在和生命周期关系 |
| `bool 任务执行方法材料::operator==(const 任务执行方法材料&) const noexcept` | 比较方法材料全部字段 |
| `bool 任务执行动作材料::完整动作入口() const noexcept` | 复核动作入口角色、状态、场景和稳定动作键 |
| `bool 任务执行动作材料::operator==(const 任务执行动作材料&) const noexcept` | 比较动作材料全部字段 |
| `bool 任务执行冻结请求::operator==(const 任务执行冻结请求&) const noexcept` | 比较完整冻结请求 |
| `bool 任务执行请求准入结果::成功() const noexcept` | 判断协议准入状态是否可准入 |
| `bool 冻结任务执行请求相同(const 任务执行冻结请求&, const 任务执行冻结请求&) noexcept` | 公开完整冻结请求值比较 |
| `bool 任务执行任务材料静态相同(const 任务执行任务材料&, const 任务执行任务材料&) noexcept` | 比较任务静态材料和可选当前选择 |
| `bool 任务执行方法材料相同(const 任务执行方法材料&, const 任务执行方法材料&) noexcept` | 公开方法材料值比较 |
| `bool 任务执行动作材料相同(const 任务执行动作材料&, const 任务执行动作材料&) noexcept` | 公开动作材料值比较 |
| `任务执行请求准入结果 拒绝任务执行请求(任务执行请求拒绝原因) noexcept` | 形成协议拒绝结果 |
| `任务执行请求准入结果 复核任务执行调度请求(const 任务执行调度请求&) noexcept` | 复核版本、编号唯一性、任务句柄和时间顺序 |
| `bool 任务执行关系匹配(const 任务执行关系证据&, 关系类型, 节点句柄, 节点句柄, std::int64_t) noexcept` | 精确匹配当前关系证据的类型、两端和顺序号 |
| `bool 冻结任务执行请求完整(const 任务执行冻结请求&) noexcept` | 总控任务、方法、动作、选择和关系证据闭合 |

本模块只冻结和复核值式执行材料，不读取仓库、不取得执行权、不调用动作，也不提交任务状态。当前适配器枚举只接受“任务目标状态值变化”，不能据此推导其它方法或D455工作包已可执行。
