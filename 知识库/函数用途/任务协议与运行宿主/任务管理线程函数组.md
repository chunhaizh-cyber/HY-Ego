# 任务管理线程函数组

- 稳定身份：39个身份
- 边界类别：任务执行请求候选、两阶段有界队列与线程壳
- 模块：`海中鱼巣.线程.任务管理线程`
- 位置：`海中鱼巣/线程/任务管理线程.ixx:81-568`
- 绑定提交：`04c1179ca5cc3b2945dc39fa3aa6f9e2b56f9088`
- 源码 blob：`ebfc8e567294935773605fcd4126c8fea47a88fc`
- 知识状态：当前
- 实现状态：已登记工程；无普通应用生产import
- 验证状态：39/39身份静态互证；历史专项证明计划内强类型队列，当前生产任务闭环`NOT_RUN`

| 完整签名 | 只负责 |
| --- | --- |
| `强类型任务执行请求候选::强类型任务执行请求候选() = delete` | 禁止无主候选 |
| `强类型任务执行请求候选::强类型任务执行请求候选(const 强类型任务执行请求候选&) = delete` | 禁止复制候选 |
| `强类型任务执行请求候选& 强类型任务执行请求候选::operator=(const 强类型任务执行请求候选&) = delete` | 禁止复制赋值 |
| `强类型任务执行请求候选::强类型任务执行请求候选(强类型任务执行请求候选&&) noexcept` | 转移预留能力并清空来源 |
| `强类型任务执行请求候选& 强类型任务执行请求候选::operator=(强类型任务执行请求候选&&) noexcept` | 先撤销本地未决预留再转移来源 |
| `强类型任务执行请求候选::~强类型任务执行请求候选()` | fallback撤销未决预留 |
| `bool 强类型任务执行请求候选::有效() const noexcept` | 复核所有者、编号、代次和未决状态 |
| `std::uint64_t 强类型任务执行请求候选::读取请求编号() const noexcept` | 读取候选请求编号 |
| `std::uint64_t 强类型任务执行请求候选::读取预留代次() const noexcept` | 读取内部预留代次 |
| `强类型任务执行请求候选::强类型任务执行请求候选(任务管理线程*, std::uint64_t, std::uint64_t) noexcept` | 仅供所有者形成移动独占候选 |
| `void 强类型任务执行请求候选::置为已决定() noexcept` | 清空候选并锁存已决定 |
| `bool 强类型任务执行请求操作结果::成功() const noexcept` | 判断确认或撤销状态 |
| `explicit 任务管理线程::任务管理线程(std::uint64_t)` | 以同一容量建立旧队列和强类型队列 |
| `任务管理线程::任务管理线程(std::uint64_t, std::uint64_t)` | 分别配置两类队列容量 |
| `任务管理线程::~任务管理线程()` | 停止强类型准入、锁存停止并join |
| `任务管理线程::任务管理线程(const 任务管理线程&) = delete` | 禁止复制 |
| `任务管理线程& 任务管理线程::operator=(const 任务管理线程&) = delete` | 禁止复制赋值 |
| `任务管理线程状态 任务管理线程::当前状态() const` | 读取线程壳状态 |
| `bool 任务管理线程::线程已进入() const` | 读取进入见证 |
| `bool 任务管理线程::线程可收口() const` | 以joinable反向判断收口 |
| `const 有界运行消息队列& 任务管理线程::只读任务调度队列() const` | 读取旧运行消息队列 |
| `强类型任务执行请求准备结果 任务管理线程::准备强类型任务执行请求(const 任务执行冻结请求&)` | 复核完整请求并预留幂等、顺序和容量槽 |
| `强类型任务执行请求操作结果 任务管理线程::确认强类型任务执行请求(强类型任务执行请求候选&)` | 把本所有者未决预留转为可消费 |
| `强类型任务执行请求操作结果 任务管理线程::撤销强类型任务执行请求(强类型任务执行请求候选&)` | 精确撤销未决预留 |
| `强类型任务执行请求取出结果 任务管理线程::取出强类型任务执行请求()` | 取出一条已确认冻结请求 |
| `强类型任务执行请求队列快照 任务管理线程::读取强类型任务执行队列快照() const` | 复制容量、预留、可消费和索引计数 |
| `任务管理线程操作结果 任务管理线程::启动()` | 启动只等待停止的物理线程壳 |
| `任务管理线程操作结果 任务管理线程::发送任务执行请求(const 运行消息&)` | 兼容旧运行消息队列，只接受任务执行请求类型 |
| `出队结果 任务管理线程::取出任务执行请求()` | 从旧运行消息队列取出一条材料 |
| `任务管理线程操作结果 任务管理线程::请求停止并发送停止消息(std::uint64_t, std::uint64_t)` | 停止强类型准入并向旧队列发布停止材料 |
| `任务管理线程操作结果 任务管理线程::收口等待()` | join并更新已停止状态 |
| `std::size_t 强类型任务句柄哈希::operator()(const 节点句柄&) const noexcept` | 为任务句柄形成进程内哈希 |
| `bool 强类型任务句柄相等::operator()(const 节点句柄&, const 节点句柄&) const noexcept` | 比较任务句柄 |
| `任务管理线程操作结果 任务管理线程::拒绝(任务管理线程拒绝原因) const` | 形成线程壳拒绝 |
| `强类型任务执行请求准备结果 任务管理线程::拒绝强类型请求准备(强类型任务执行请求队列拒绝原因)` | 形成准备拒绝 |
| `强类型任务执行请求操作结果 任务管理线程::拒绝强类型请求操作(强类型任务执行请求队列拒绝原因) noexcept` | 形成确认/撤销拒绝 |
| `bool 任务管理线程::撤销强类型请求_已锁定(std::uint64_t, std::uint64_t)` | 锁内精确删除未决预留并修复同任务最新序号 |
| `void 任务管理线程::重新计算任务最新序号_已锁定(节点句柄)` | 从现存记录重算同任务顺序索引 |
| `void 任务管理线程::析构撤销强类型请求(std::uint64_t, std::uint64_t) noexcept` | 为候选析构提供锁内撤销入口 |

强类型队列只保管已冻结执行材料。确认候选不等于工作线程已经执行，取出请求也不等于任务状态已经推进。8100禁止任务管理线程直接写需求、任务、状态、动态、因果或价值事实；当前模块同样没有正式生产import，不能作为运行中任务管理能力的证据。
