# L2存在结构服务内部函数索引

- 边界类别：44个内部服务函数；1个服务装配函数
- 绑定提交：`b186b65f67eb24e3683c318c07bbb394e3f9015f`
- 绑定源码：`海中鱼巣/领域/服务.L2存在结构.ixx`
- 源码 blob：`658799f6fa37aac731a8f3a83cc6f522fd7e7318`
- 知识状态：当前枚举；45个函数身份均已逐项制卡

## 数量与物理边界

| 区域 | 位置 | 函数身份数 | 对外可用性 |
| --- | --- | ---: | --- |
| `海中鱼巣::L2存在结构内部`命名空间 | 76-1600 | 43 | 非导出内部服务实现 |
| `L2存在结构服务`私有区 | 2407-2497 | 2 | 1个内部服务函数；1个服务装配函数 |
| 合计 | — | 45 | 44个内部服务函数；1个服务装配函数；均不得列入对外服务接口 |

计数规则只统计具名函数定义起始行，不统计内部结构体的`成功()`成员、lambda、公开服务函数、声明或显式删除身份。完整签名用于区分重载，短函数名不能作为唯一身份。

## 责任分组

| 分组 | 行范围 | 数量 | 代表函数 | 只负责 |
| --- | --- | ---: | --- | --- |
| [基础状态、材料与截止投影](内部-基础状态材料与截止投影组.md) | 76-294 | 14 | `映射存在写入状态`、`形成E1存在事实`、`转换为L2材料` | 状态映射、编码映射、材料转换和截止生命周期投影 |
| [身份来源与完整存在读取](内部-身份来源与完整存在读取组.md) | 295-915 | 12 | `检查存在当前代次`、`读取当前存在身份来源`、`读取历史完整存在` | 在统一截止读取和互证存在来源、节点及固定属性 |
| [固定属性与退出提交读回](内部-固定属性与退出提交读回组.md) | 916-1065,1429-1527 | 6 | `检查存在固定属性前置`、`提交并读回存在固定属性写集`、`提交并读回存在退出写集` | 属性 / 退出前置、原子提交和正式读回 |
| [单例角色与角色化存在](内部-单例角色与角色化存在组.md) | 1068-1428 | 10 | `读取当前存在单例角色`、`提交并读回存在单例角色写集`、`提交并读回角色化存在写集` | 角色关系当前 / 历史读取、写入映射及提交读回 |
| [构造引导与私有装配](内部-构造引导与私有装配组.md) | 1530-1600,2407-2497 | 3 | `初始化存在身份来源`、`按定位读取当前存在单例角色结构登记`、`验证并移动交付` | 构造期来源登记、定位正式读回及owner交付移动 |

## 使用边界

44个内部服务函数只解释或支撑`L2存在结构服务`公开操作。上层BIZ、其它DATA服务和普通应用不得因为源码可见、名字相似或参数可构造而直接依赖它们。需要复用内部算法时，应先判断是否由现有对外入口承载，不能复制helper形成第二套owner、幂等、事务、读回或错误解释。

唯一服务装配函数`L2存在结构服务::验证并移动交付`只在构造期验证同实例owner写端口并移动所有权，不是对外服务函数，也不是内部CRUD helper。

当前45个函数身份均已逐项制卡：44个内部服务函数、1个服务装配函数，剩余0个仅分组未制卡身份。本结论只证明存在服务内部 / 私有函数知识已完整，不代表整个项目函数知识库已经完整。
