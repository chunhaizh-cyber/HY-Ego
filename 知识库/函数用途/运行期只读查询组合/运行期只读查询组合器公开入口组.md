# 运行期只读查询组合器公开入口组

- 稳定身份：下表 12 个当前完整签名（2 个构造、5 个显式删除、5 个只读入口）
- 边界类别：服务装配函数、显式删除和对外只读组合函数
- 函数种类和可见性：模块导出的 `class` 公开成员
- 模块：`海中鱼巣.领域.组合.运行期只读查询`
- 位置：`海中鱼巣/领域/组合.运行期只读查询.ixx:60-98,101-1128`
- 绑定代码提交：`37ced00ca7cc0ee18a4e901d70af6d672e99040f`
- 复核基线：`603a809799a3a2093d198cafc0f6ad2a35858d8a`
- 源码 blob：`e0335a442b792cf3ae28272565490be9ff07f747`
- 知识状态：当前（仅限上述复核基线）
- 实现状态：已实现；七服务兼容构造不提供目标裁决能力，八服务构造借入方法服务
- 验证状态：既有治理专项对需求/任务与目标裁决分支的历史分账不变；`37ced00c`目标差异独立快照 Debug / Release Rebuild 通过，相关 runner 分项通过但整体因既有上游初始化失败退出2，完整一致事实专项未通过；目标裁决阶段资源故障注入及普通应用长时运行 `NOT_RUN`

| 行 | 完整签名 | 用途 | 诊断责任 |
| ---: | --- | --- | --- |
| 62 | `explicit 运行期只读查询组合器(const L2需求结构服务&, const L2任务结构服务&, const L2状态结构服务&, const L2特征结构服务&, const L2存在结构服务&, const L2场景结构服务&, const L2动态结构服务&) noexcept` | 借用七个正式服务，保留既有读取入口；方法服务指针为空 | 无事实写入 |
| 77 | `运行期只读查询组合器(const L2需求结构服务&, const L2任务结构服务&, const L2方法结构服务&, const L2状态结构服务&, const L2特征结构服务&, const L2存在结构服务&, const L2场景结构服务&, const L2动态结构服务&) noexcept` | 借用同一应用上下文的八个正式服务，供任务目标裁决读取普通方法 | 无事实写入 |
| 94 | `运行期只读查询组合器() = delete` | 禁止无服务默认构造 | 无适用错误分支 |
| 95 | `运行期只读查询组合器(const 运行期只读查询组合器&) = delete` | 禁止复制构造，避免复制非 owning 服务借用 | 无适用错误分支 |
| 96 | `运行期只读查询组合器& operator=(const 运行期只读查询组合器&) = delete` | 禁止复制赋值 | 无适用错误分支 |
| 97 | `运行期只读查询组合器(运行期只读查询组合器&&) = delete` | 禁止移动构造 | 无适用错误分支 |
| 98 | `运行期只读查询组合器& operator=(运行期只读查询组合器&&) = delete` | 禁止移动赋值 | 无适用错误分支 |
| 101 | `需求当前满足裁决结果 运行期只读查询组合器::裁决需求当前满足(const 需求当前满足裁决请求&) const noexcept` | 在同一G0读取需求、当前成员、完整列表项、目标宿主当前特征、完整目标合同和比较注册，调用正式比较后返回已满足/未满足快照 | 向上送出证据不足、漂移、冲突、不可比较、许可/资源或内部状态 |
| 352 | `本能根当前快照结果 运行期只读查询组合器::读取本能根当前快照(const 本能根当前快照请求&) const noexcept` | 在同一G0定位唯一安全根和服务根特征实例，分别读取安全根三元I64与服务根I64当前值，派生控制态并形成只含两根当前值的快照 | 向上送出请求拒绝、尚无权威、材料缺失、当前性漂移、资源失败或内部不一致 |
| 444 | `需求任务快照结果 运行期只读查询组合器::读取需求结构与任务后继快照(const 需求任务快照请求&) const noexcept` | 有界展开当前需求结构，读取列表项及当前任务后继；同一 G0 下形成只读快照 | 向上送出结构化状态 |
| 647 | `任务实际结果独立读回结果 运行期只读查询组合器::读取任务本轮实际结果与独立读回(const 任务实际结果独立读回请求&) const noexcept` | 读取正式任务实际结果，并独立回读实例方法、路径、任务、状态、动态、场景、主体和需求成员，逐字段互证 | 向上送出等待、漂移、冲突、预算、资源或内部状态 |
| 838 | `任务目标达成裁决结果 运行期只读查询组合器::裁决任务目标达成(const 任务目标达成裁决请求&) const noexcept` | 按正式结果、独立读回、普通方法、完整特征实例、目标状态合同、比较注册和特征比较顺序互证；只形成目标已达成或目标未达成裁决快照 | 向上送出合法等待、当前性漂移、引用冲突、不可比较、入口拒绝、资源失败或内部错误 |

## 用途与依赖方向

五个只读入口均只借用服务引用，不持有DATA/L1所有权，不写事实、不提交事务、不等待重试。需求当前满足入口固定六次公开读取和一次正式特征比较共用请求G0；它不读取任务、路径、实例方法或实际结果。本能根入口要求两个不同且有效的特征定义，在同一G0读取唯一安全根与服务根实例及其当前值；安全根材料必须是三个I64且满足`L > 1 && L < H`，服务根材料必须是单个I64。该入口不读取状态服务，不形成服务游标，也不读取合同、准备、进展或到期状态组。其它入口继续支撑需求/任务、实际结果独立读回和任务目标裁决。

目标裁决入口的成功结果仅准备 `待验证后完成` 或 `目标未达成待重筹办`。七服务兼容构造的 `方法服务_` 为空，因此同一入口固定返回 `入口拒绝`，不能被误当成目标裁决能力。

## 当前生产调用语境

- `启动.应用程序::运行普通程序` 从同一普通应用上下文注入八服务；治理骨架保存非 owning 借用并通过专用转发入口调用。
- 治理骨架在同一一致事实截止下调用 `读取本能根当前快照`，成功时只把本能根快照并入一致事实结果；该接线不产生服务合同或维护账。
- `线程/创建.自我线程.ixx::运行治理循环` 通过治理骨架转发目标裁决，成功后把裁决冻结到进程内处理槽。
- `需求初次筹办准备提供者`调用需求当前满足入口，但该provider自身只有治理专项消费者，不能据此声明本入口已由普通筹办正式消费。
- 治理专项直接构造八服务组合器覆盖正常与非成功矩阵，并单独构造七服务组合器验证兼容拒绝。
- 当前证据只证明上述静态接线和专项路径，不证明普通应用长时间宿主、外部真实工作结果来源或任务生命周期迁移。

## 历史退出入口

旧提交 `29ed0cbe` 的 `读取共享截止世界与自我快照(...)` 及其专属 WORLD-SELF 结构/互证 helper 已从当前模块移除。`37ced00c` 已继续移除 `读取生存安全与服务维护当前快照(...)` 及其服务游标、合同、准备、进展和到期状态组读取形状。两类旧材料都只作历史证据，不能沿本卡当前入口复用。

## 依据与证据

- 施工记录：`施工记录/20260821_RUNTIME-GOVERNANCE-TASK-RESULT-COMPLETION-ADJUDICATION_施工记录.md` §2、§3.1、§4.1
- 验证记录：`验证记录/20260821_RUNTIME-GOVERNANCE-TASK-RESULT-COMPLETION-ADJUDICATION_验证记录.md` §2-§5
- 需求当前满足施工/验证：`施工记录/20260823_RUNTIME-DEMAND-CURRENT-SATISFACTION-ADJUDICATION_施工记录_v0.1.md`、`验证记录/20260823_RUNTIME-DEMAND-CURRENT-SATISFACTION-ADJUDICATION_验证记录_v0.1.md`
- 服务合同记账退役施工/验证：`施工记录/20260901_INSTINCT-SERVICE-CONTRACT-ACCOUNTING-RETIREMENT_施工记录_v0.1.md`、`验证记录/20260901_INSTINCT-SERVICE-CONTRACT-ACCOUNTING-RETIREMENT_验证记录_v0.1.md`
- 普通应用装配调用点：`海中鱼巣/启动.应用程序.ixx:193-201`
- 本能根快照治理消费：`海中鱼巣/领域/服务.L2治理函数骨架.ixx:114-123`

## 已知缺口

完整一致事实专项受既有上游初始化失败阻断，未由 `37ced00c` 证明通过；旧九项持久载荷运行恢复保持 `NOT_RUN`。需求当前满足V06、V07、V10、V11、V12A、V14、V18保持`NOT_RUN`；本入口不形成父回流、筹办、逐成员结算或结果分配。目标裁决资源失败注入、路径验证、任务生命周期迁移、真实方法动作和跨进程恢复也保持原`NOT_RUN`边界。
