# 运行期只读查询内部纯值映射组

- 稳定身份：下表 1 个当前完整签名；旧 WORLD-SELF 互证 helper 另列为历史退出
- 边界类别：内部非服务函数（目标关系位纯值映射）
- 函数种类和可见性：非导出命名空间 `海中鱼巣::运行期只读查询细节` 的 `constexpr` 自由函数
- 模块：`海中鱼巣.领域.组合.运行期只读查询`
- 位置：`海中鱼巣/领域/组合.运行期只读查询.ixx:33-50`
- 绑定提交：`195964fcbf9436f1647b576e60234462d5443e53`
- 源码 blob：`bc94ad33ccd9a62ac6a7be8c96eeb8a9d25a1f54`
- 知识状态：当前（仅限 195964fc 基线）
- 实现状态：已实现纯值映射；不是稳定消费 ABI
- 验证状态：三个编译期断言分别锁定 `0x01 / 0x02 / 0x04`；运行期资源故障和完整业务闭环 `NOT_RUN`

| 行 | 完整签名 | 用途 | 诊断责任 |
| ---: | --- | --- | --- |
| 35 | `constexpr std::optional<std::uint8_t> 目标判断关系位(L2特征比较具名关系) noexcept` | 将当前低于目标、当前达到目标、当前高于目标集中映射为 `0x01 / 0x02 / 0x04`；未知关系返回空 | 调用方将空映射为不可比较 |

## 用途与非职责

该函数只解释已经由 `L2特征结构服务::比较特征` 形成的具名关系位，不读取或写入事实、不执行比较、不选择目标合同、不改变任务或生命周期。`static_assert`（45-50 行）是合同锁定，不是运行期验证结果。

## 历史退出 helper

`29ed0cbe` 版本中的 WORLD-SELF 请求校验、失败映射、唯一查找和四组拓扑/关系互证 helper 已随旧共享截止入口移除。它们不属于当前 195964fc 组合器的可调用身份，旧用途说明只作历史证据。

## 依据与证据

- 施工记录：`施工记录/20260821_RUNTIME-GOVERNANCE-TASK-RESULT-COMPLETION-ADJUDICATION_施工记录.md` §4.2
- 验证记录：`验证记录/20260821_RUNTIME-GOVERNANCE-TASK-RESULT-COMPLETION-ADJUDICATION_验证记录.md` §2、§3（三关系位）
- 调用方：当前组合器 `裁决任务目标达成` 在 `海中鱼巣/领域/组合.运行期只读查询.ixx:919-922`
