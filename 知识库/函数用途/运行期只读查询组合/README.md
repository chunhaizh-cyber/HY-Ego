# 运行期只读查询组合函数用途索引

当前增量复核基线：`603a809799a3a2093d198cafc0f6ad2a35858d8a`（服务合同记账退役代码结果：`37ced00ca7cc0ee18a4e901d70af6d672e99040f`）

| 源码 | source blob | 当前显式身份 | 当前边界 | 用途卡 |
| --- | --- | ---: | --- | --- |
| `海中鱼巣/领域/运行期只读查询.数据.h` | `ecb6d5617fc0c8f737f12a7106ab73f74df8be59` | 10 | 纯值DTO/谓词，不产生事实；本能根快照只承载安全根、服务根当前值和同G0截止 | [DTO与谓词](世界自我共享截止快照DTO与谓词组.md) |
| `海中鱼巣/领域/组合.运行期只读查询.ixx` | `e0335a442b792cf3ae28272565490be9ff07f747` | 13 | 工程已登记；本能根当前快照已有治理骨架消费者，其余生产/自检分账不变 | [组合器公开入口](运行期只读查询组合器公开入口组.md)；[内部互证](运行期只读查询内部互证组.md) |

当前两源码共23个显式函数身份：数据头10个纯值身份；组合器包含2个构造、5个显式删除、5个公开只读入口和1个关系位helper。`37ced00c`没有改变身份总数，但已将旧生存服务快照DTO和服务维护读取入口原位收缩为本能根当前快照DTO与读取入口；当前快照不含服务游标、合同、准备、进展或到期状态组。

需求当前满足入口只按同一G0读取需求、当前成员、完整列表项、目标宿主当前特征、目标合同和比较注册，并调用正式特征比较。它不读取任务、路径、实例方法或实际结果，不写需求/任务/结算事实。`17edb9d9`新增初次筹办候选provider对该入口的调用，但provider自身仍只由治理专项消费，不能升级为普通筹办生产接线。

旧WORLD-SELF共享截止入口，以及旧生存服务快照、服务维护游标和四类合同状态组读取身份均已退出；历史材料只作证据，不能用于当前复用裁决。`37ced00c`目标差异快照的 Debug / Release Rebuild 通过，相关 runner 分项通过但整体因既有上游初始化失败退出2；完整一致事实专项、普通应用长时运行和跨进程恢复不能据此升级为通过。需求当前满足计划内V06、V07、V10、V11、V12A、V14、V18保持`NOT_RUN`，且没有独立服务导向验收。父回流、任务筹办消费、逐成员结算和结果共用/争用不属于本组合器已实现职责。
