# 运行期只读查询组合函数用途索引

复核输入基线：`b0c94d20`（开工时最新已推送正式 HEAD；至发布时正式源码对本账 blob 零漂移）

| 源码 | source blob | 显式身份 | 当前边界 | 用途卡 |
| --- | --- | ---: | --- | --- |
| `领域/组合.运行期只读查询.ixx` | `8050167c347ec0a78448c2fe057cc9bc7a041513` | 24 | 工程已登记；生产零import、零调用点 | [DTO与谓词](世界自我共享截止快照DTO与谓词组.md)；[组合器公开入口](运行期只读查询组合器公开入口组.md)；[内部互证](运行期只读查询内部互证组.md) |

本目录按源码覆盖24个显式函数身份：三个值式默认比较、一个成功谓词、四个请求 / 失败映射 helper、三个唯一查找 helper、两个纯值一致性 helper、四个业务互证 helper、一个构造、五个显式删除特殊成员和一个公开组合入口。身份口径与 `施工记录/20260815_WORLD-SELF-CURRENT-SNAPSHOT-CONSUME_v0.3_施工记录.md` 第3节函数级知识变更清单一致。

模块 `海中鱼巣.领域.组合.运行期只读查询` 由提交 `29ed0cbe61e18e06bcfb588e63d072f769801ef7` 重新进入正式树并登记 `海中鱼巣.vcxproj` 与 filters（该文件曾于更早历史存在并在旧结构治理中退出）。组合器只借用同一 `const L2结构聚合服务&`，不持有可变 DATA 状态；公开入口按同一非零事实截止代次顺序调用六个公开 DATA 读取入口（场景树、存在单例角色、完整存在、存在当前场景成员引用、场景宿主存在关系、存在当前宿主场景），全部成功后做结构互证并一次形成值式快照；任何非成功均返回零快照。DATA `许可拒绝` 映射为 `提供者拒绝`，事实代次漂移映射 `当前性漂移`，未找到 / 已退出映射 `材料缺失`。

生产边界：当前全仓除本模块自身外无任何 import 或调用点；普通应用未装配组合器，自我线程生产消费未接线。Debug / Release 专项三类结果（已形成、当前性漂移、提供者拒绝）通过；普通应用或自我线程生产消费、父级共享截止 provider、G0 / G1 完整业务闭环、真实资源耗尽、私有坏形状、集成验收、崩溃 / 重启和跨进程恢复仍为 `NOT_RUN`。
