# L2场景DTO默认比较组

- 稳定身份：下表63个默认`operator==`
- 边界类别：非服务纯值函数
- 模块：`海中鱼巣.领域.服务.L2场景结构`
- 位置：`海中鱼巣/领域/L2场景结构.数据.h:16-625`
- 绑定提交：`14c627187703b559cf4a80d82c1e98a94bce6454`
- 源码 blob：`0b23c16a240a76751a23ce9dcf42f4e400829041`
- 知识状态：当前
- 实现状态：已实现；数据头由场景服务模块导出并登记正式工程
- 验证状态：63/63个真实展开身份及逐字段定义已静态互证；既有场景专项覆盖部分比较语境，完整公开面真实BIZ消费`NOT_RUN`

每一项完整稳定身份均为`bool operator==(const <类型>&, const <类型>&)`。前四项由第16—25行宏真实展开；宏模板本身不是函数身份。

| 责任组 | DTO类型与定义行 | 数量 |
| --- | --- | ---: |
| 四类场景属性类型身份 | `L2场景I64属性类型身份`(22)、`L2场景I64组属性类型身份`(23)、`L2场景U64组属性类型身份`(24)、`L2场景独立材料属性类型身份`(25) | 4 |
| 属性类型登记 | `L2场景属性类型登记请求`(46)、`L2场景属性类型登记`(61)、`L2场景属性类型登记结果`(70) | 3 |
| 场景、父子、成员与关系类型 | `L2场景父子关系事实`(80)、`L2场景成员关系事实`(90)、`L2场景宿主存在关系类型身份`(96)、`L2场景事实`(222)、`L2场景父子关系类型身份`(229)、`L2场景成员关系类型身份`(235) | 6 |
| 场景宿主登记与关系 | `L2场景宿主结构登记请求`(108)、`L2场景宿主结构登记`(121)、`L2场景宿主结构登记结果`(129)、`L2场景宿主存在关系事实`(138)、`L2场景宿主存在新增请求`(147)、`L2场景宿主存在替换请求`(158)、`L2场景宿主存在退出请求`(168)、`L2场景宿主存在写入结果`(177)、`L2场景宿主存在读取请求`(186)、`L2场景宿主存在读取结果`(196)、`L2存在当前宿主场景读取请求`(203)、`L2存在当前宿主场景读取结果`(211) | 12 |
| 场景关系类型登记 | `L2场景关系类型登记请求`(256)、`L2场景关系类型登记`(269)、`L2场景关系类型登记结果`(279) | 3 |
| 场景树强类型 | `L2场景树身份`(285)、`L2场景树根标记关系类型身份`(296)、`L2场景树归属关系类型身份`(302) | 3 |
| 场景树结构登记 | `L2场景树结构登记请求`(314)、`L2场景树结构登记`(330)、`L2场景树结构登记结果`(338) | 3 |
| 场景树事实 | `L2场景树归属事实`(350)、`L2场景树节点事实`(358)、`L2场景树事实`(370) | 3 |
| 场景树建立与读取 | `L2场景树根建立请求`(377)、`L2树内场景新增请求`(387)、`L2场景树节点写入结果`(397)、`L2场景树归属读取请求`(406)、`L2场景树归属读取结果`(416)、`L2场景树读取请求`(425)、`L2场景树读取结果`(435)、`L2当前场景树组读取请求`(441)、`L2当前场景树组读取结果`(449) | 9 |
| 节点与身份来源读取 | `L2场景节点读取请求`(460)、`L2场景节点读取结果`(467)、`L2场景身份来源读取请求`(476)、`L2场景身份来源读取结果`(486) | 4 |
| 固定属性 | `L2场景固定属性新增请求`(495)、`L2场景固定属性换代请求`(505)、`L2场景固定属性写入结果`(514) | 3 |
| 场景退出 | `L2场景退出请求`(529)、`L2场景退出结果`(541) | 2 |
| 父子与成员关系写入 | `L2场景父子关系替换请求`(554)、`L2场景成员关系新增请求`(564)、`L2场景成员关系替换请求`(577)、`L2场景成员关系退出请求`(588)、`L2场景父子关系写入结果`(597)、`L2场景成员关系写入结果`(606) | 6 |
| 完整读取 | `L2场景完整读取请求`(615)、`L2场景完整读取结果`(624) | 2 |
| 合计 | 63个真实函数身份 | 63 |

## 用途与明确非职责

本组只对调用方已经持有的强类型身份、请求、结果和事实执行逐字段精确相等。它可用于幂等请求材料、首次结果读回、缓存键旁证和验证断言；不读取L1、owner或场景服务，不取得锁、不写事实、不推进事实代次。

默认相等不证明请求有效、场景属于当前owner、树归属闭合、父子结构无环、存在端点合法、结果成功或场景具有任何业务含义。使用者仍须调用对应`请求有效`、`事实完整`或`成功()`纯值函数，并经公开场景服务正式读回权威事实。

## 依据与证据

- `规范/1150_根规范_场景节点_20260720.md`
- `规范/4230_子规范_L2六类纯结构统一合同.md`第2、3、4.1、7节
- `验证记录/20260812_L2-STRUCTURE-CRUD-REBUILD-S-ID_场景身份来源提供者迁移验证记录.md`
- `验证记录/20260815_DATA-L2-SCENE-TREE-CURRENT-GROUP-READ_全部当前场景树结构读取施工检查记录_v0.2.md`
