# `L2场景结构服务`父子与成员关系组

- 完整身份：`替换场景父子关系`、`新增场景成员关系`、`替换场景成员关系`、`退出场景成员关系`、`读取存在当前场景成员引用`
- 边界类别：对外数据服务函数
- 位置：`海中鱼巣/领域/服务.L2场景结构.ixx:4332-4558,5166-5304`
- 模块：`海中鱼巣.领域.服务.L2场景结构`
- 绑定提交：`b186b65f67eb24e3683c318c07bbb394e3f9015f`
- 源码 blob：`a0b75ff333612f933595f33d780ed4e60a099319`
- 知识状态：当前
- 验证状态：静态前置、写集与反向读取已核；按`614e4a32`增量，`读取存在当前场景成员引用`函数体未改，许可拒绝经既有空载荷失败形成路径向上送出，六入口双配置并发矩阵命中许可拒绝且形状正确；真实资源耗尽、长时公平、跨进程恢复和真实消费者`NOT_RUN`

## 用途

- 父子替换在同一树内建立新直接父关系并退出具名旧关系；根节点不能被改成子节点。
- 成员新增把一个当前存在加入当前场景；替换以一份写集更换具名成员关系；退出只退出具名关系。
- `读取存在当前场景成员引用`从存在端反向枚举其全部当前场景成员引用，并按顺序、场景和关系身份稳定排序；关系登记截止必须先与请求`G0`一致，异截止返回事实代次漂移且不携带部分引用组。

## 一致性与边界

写入口使用关系类型登记、非零幂等身份和调用方期望事实代次，提交前互证场景树归属、存在事实及具名旧关系，提交后正式读回。成员关系不是宿主关系；本组不保证一个存在只能属于一个场景，也不建立存在节点。

## 当前调用点

绑定提交上本组五个函数均无外部生产调用点。六入口专项已覆盖反向读取的漂移传播、零载荷和按`614e4a32`的许可拒绝守恒，但不证明正式消费者已接线。后续消费者必须依据强类型结果和事实截止使用，不能直接操作 L1 关系或凭缓存退出关系。
