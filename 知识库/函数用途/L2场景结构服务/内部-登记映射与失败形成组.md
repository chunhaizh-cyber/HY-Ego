# 场景内部登记映射与失败形成函数组

- 边界类别：内部服务函数
- 完整身份：下列17个非导出函数
- 位置：`海中鱼巣/领域/服务.L2场景结构.ixx:107-373`
- 模块：`海中鱼巣.领域.服务.L2场景结构`内部实现
- 绑定提交：`b186b65f67eb24e3683c318c07bbb394e3f9015f`
- 源码 blob：`a0b75ff333612f933595f33d780ed4e60a099319`
- 知识状态：当前
- 验证状态：静态定义及直接调用点已核；异常注入和运行`NOT_RUN`

## 函数身份与用途

| 行 | 函数 | 用途 |
| ---: | --- | --- |
| 107 | `初始化场景身份来源(const L1事实基座服务&, L1所有者范围写端口&)` | 互证owner并幂等建立场景族锚点、族归属类型与关系，返回内部定位；失败抛出使服务构造终止 |
| 179 | `是保留登记幂等身份(L2结构幂等身份) noexcept` | 拒绝业务写请求复用四个内部登记保留身份 |
| 188 | `映射场景关系类型登记幂等键(L2结构幂等身份) noexcept` | 值式转换为L1 owner写入幂等身份 |
| 194 | `映射场景关系类型登记写入状态(L1所有者范围写入状态) noexcept` | 把L1写状态收窄为关系登记状态 |
| 221 | `映射场景关系类型登记读取失败状态(L1所有者范围一致当前读取状态) noexcept` | 把一致当前读取失败收窄为登记读取状态 |
| 240 | `场景关系类型一致读取失败形状完整(const L1所有者范围一致当前读取结果&, uint64_t) noexcept` | 校验非成功L1结果没有混入成功载荷且截止形状正确 |
| 257 | `查找场景关系类型编码映射(const L1所有者范围写入结果&, uint32_t) noexcept` | 按本地键查找唯一稳定编码映射 |
| 269 | `场景关系类型编码映射唯一(const L1所有者范围写入结果&) noexcept` | 证明父子、成员两类登记映射各一且互异 |
| 284 | `形成场景关系类型登记失败(L2场景关系类型登记状态, uint64_t) noexcept` | 形成零成功载荷的登记失败结果 |
| 291 | `映射场景幂等键(L2结构幂等身份) noexcept` | 把普通场景写请求身份值式映射到L1 |
| 296 | `映射场景写入状态(L1所有者范围写入状态) noexcept` | 把L1通用写状态收窄为L2结构状态 |
| 314 | `形成场景树登记失败(L2结构状态, uint64_t) noexcept` | 形成场景树登记失败结果 |
| 319 | `形成场景树归属读取失败(const L2场景树归属读取请求&, L2结构状态, uint64_t) noexcept` | 保留读取类别 / 历史截止回显并清空成功归属 |
| 326 | `形成场景树读取失败(const L2场景树读取请求&, L2结构状态, uint64_t) noexcept` | 保留请求读取语境并清空整树载荷 |
| 333 | `形成当前场景树组读取失败(L2结构状态, uint64_t) noexcept` | 形成当前树组的空失败结果 |
| 338 | `形成场景树节点写入失败(L2结构状态, uint64_t) noexcept` | 形成建根 / 新增场景共用的空失败结果 |
| 344 | `形成场景树归属事实(const L1所有者范围关系事实&, L1结构所有者身份, const L2场景树结构登记&, L2场景身份, L2场景树归属类别, uint64_t) noexcept` | 互证关系端点、类型、owner、角色和生命周期后投影强类型归属事实 |

## 内部边界

本组只服务场景公开登记、建树和读树入口。外部调用者不得直接复用状态映射或失败形成器，也不得复制它们建立第二套错误解释。需要同类结果时应调用对应对外服务函数并消费其强类型结果。
