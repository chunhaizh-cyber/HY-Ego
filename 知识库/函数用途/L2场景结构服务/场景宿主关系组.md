# `L2场景结构服务`场景—宿主存在关系组

- 完整身份：`读取场景宿主存在关系`、`读取存在当前宿主场景`、`新增场景宿主存在关系`、`替换场景宿主存在关系`、`退出场景宿主存在关系`
- 边界类别：对外数据服务函数
- 位置：`海中鱼巣/领域/服务.L2场景结构.ixx:4495-4878`
- 模块：`海中鱼巣.领域.服务.L2场景结构`
- 绑定提交：`e4a3f90f0dece248abfad21cbbe3bfa4bed40dbb`
- 源码 blob：`9d17590eb8cb4ecd6098d923accf29bdeb3ed3fb`
- 知识状态：当前
- 验证状态：静态正反向读取、写集和读回互证已核；真实消费者`NOT_RUN`

## 用途

两个读取入口分别从场景端读取宿主存在、从存在端读取当前宿主场景；三个写入口以具名关系完成新增、新关系替换旧关系和退出，并在提交前后互证场景、存在和正反向关系材料。

## 输入、输出和一致性

读请求显式区分当前 / 历史和历史截止。写请求要求有效场景、存在、关系身份、幂等身份和期望事实代次；替换 / 退出必须指明旧关系，不能按端点猜测。成功结果带正式事实截止和完整关系，引用冲突、代次漂移和内部矛盾结构化返回。

## 明确非职责与调用点

宿主关系不等于场景成员关系，也不建立或修改存在本体。绑定提交上五个公开函数均无外部生产调用点；正式 ABI 可复用不等于运行期宿主已经接线。
