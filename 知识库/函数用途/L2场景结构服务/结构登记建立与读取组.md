# `L2场景结构服务`四类结构登记建立与读取组

- 完整身份：宿主、关系类型、属性类型、场景树结构四组`建立...登记(const ...请求&)`与`读取当前...登记() const`，共8个函数
- 边界类别：对外数据服务函数（结构登记管理；调用语境受限）
- 位置：`海中鱼巣/领域/服务.L2场景结构.ixx:2783-3302`
- 模块：`海中鱼巣.领域.服务.L2场景结构`
- 绑定提交：`e4a3f90f0dece248abfad21cbbe3bfa4bed40dbb`
- 源码 blob：`9d17590eb8cb4ecd6098d923accf29bdeb3ed3fb`
- 知识状态：当前
- 验证状态：静态请求、锁、写入与读回路径已核；并发和损坏定位`NOT_RUN`

## 完整函数组

- `建立场景宿主结构登记(const L2场景宿主结构登记请求&)` / `读取当前场景宿主结构登记() const`
- `建立场景关系类型登记(const L2场景关系类型登记请求&)` / `读取当前场景关系类型登记() const`
- `建立场景属性类型登记(const L2场景属性类型登记请求&)` / `读取当前场景属性类型登记() const`
- `建立场景树结构登记(const L2场景树结构登记请求&)` / `读取当前场景树结构登记() const`

## 用途与事务

建立函数在各自登记锁内校验合同、规则、幂等身份和期望事实代次，以一份 owner 范围写集建立固定结构类型 / 锚点，随后正式读回并保存值式定位。精确重复必须与首次请求见证一致；异请求返回幂等冲突。读取函数只在锁内复制定位，再从 L1 正式读取权威事实，不把定位缓存冒充事实。

## 错误与生命周期

入口无效、事实代次漂移、幂等冲突、资源失败和内部不一致均由对应强类型结果返回。定位只在本服务进程生命周期内加速定位；跨进程恢复不由这些函数证明。

## 当前调用语境

构造函数调用四个建立入口；I1/I2当前还会显式读取场景树结构登记。其它登记公开入口没有外部生产调用点。调用者不得自行写死登记编码或绕过正式读回。
