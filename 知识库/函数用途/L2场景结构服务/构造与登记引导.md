# `L2场景结构服务`构造与登记引导

- 完整签名：`海中鱼巣::L2场景结构服务::L2场景结构服务(const L1事实基座服务&, const L2存在结构服务&, L2场景所有者交付&&)`
- 边界类别：服务装配函数（构造期会调用对外登记操作完成引导）
- 位置：`海中鱼巣/领域/服务.L2场景结构.ixx:2738-2779`
- 模块：`海中鱼巣.领域.服务.L2场景结构`
- 可见性：公开构造函数
- 绑定提交：`b186b65f67eb24e3683c318c07bbb394e3f9015f`
- 源码 blob：`a0b75ff333612f933595f33d780ed4e60a099319`
- 知识状态：当前
- 验证状态：静态构造路径已核；真实首次 / 重复装配和异常注入`NOT_RUN`

## 用途

验证并接收场景 owner 写端口，借用同一 L1事实基座服务和存在结构服务，初始化场景身份来源；随后依次读取非零当前事实代次并保证关系类型、属性类型、场景树结构和场景宿主四类登记已经建立且正式读回成功。

## 输入、输出和副作用

- L1服务与存在服务为非 owning 引用，生命周期必须覆盖本服务。
- 场景所有者交付被移动进服务并经 L1 互证；错误交付会使构造失败。
- 构造不是纯绑定：四类登记缺失时会经本 owner 写入固定登记事实。
- 任一当前代次不可用或登记失败时抛出`std::runtime_error`，由普通应用装配映射为场景服务构造失败。

## 明确非职责

不建立场景树根、业务场景、成员或宿主事实，不启动 I1/I2，不吞掉构造失败。登记成功也不证明任何业务场景已经存在。

## 显式删除身份与调用点

复制构造和复制赋值显式删除，防止复制 owner 写端口和锁状态。当前唯一生产构造点为`构造普通应用上下文`；默认构造不可用，移动语义不得从未声明推断为可用。
