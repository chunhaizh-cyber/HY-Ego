# L2场景结构服务内部函数索引

- 边界类别：内部服务函数
- 绑定提交：`fdca9930375e23ca5ed4705fa8a6a569c24f7828`
- 绑定源码：`海中鱼巣/领域/服务.L2场景结构.ixx`
- 源码 blob：`9d17590eb8cb4ecd6098d923accf29bdeb3ed3fb`
- 知识状态：当前枚举；逐函数用途待补

## 数量与物理边界

| 区域 | 位置 | 函数身份数 | 对外可用性 |
| --- | --- | ---: | --- |
| `海中鱼巣::L2场景结构内部`命名空间 | 107-2695 | 86 | 非导出内部实现 |
| `L2场景结构服务`私有区 | 5194-5454 | 4 | 仅类内部调用 |
| 合计 | — | 90 | 均不得列入对外服务接口 |

计数规则只统计函数定义起始行，不统计内部结构体的`成功()`成员、lambda、公开服务函数、声明或显式删除身份；后续逐身份制卡时必须继续以完整签名区分重载。

## 责任分组

| 分组 | 行范围 | 数量 | 代表函数 | 只负责 |
| --- | --- | ---: | --- | --- |
| [登记、幂等映射与失败结果形成](内部-登记映射与失败形成组.md) | 107-344 | 17 | `初始化场景身份来源`、`映射场景关系类型登记写入状态`、`形成场景树读取失败` | 已逐项登记；把L1材料映射为L2登记 / 失败形状 |
| 当前 / 历史读取、投影与材料转换 | 374-1204 | 40 | `读取当前场景身份来源`、`读取历史场景节点`、`转换为L2材料`、`形成截止成员关系事实` | 在指定截止解释L1读取并形成强类型值 |
| 写集提交与正式读回 | 1227-1455 | 4 | `提交并读回父子写集`、`提交并读回成员写集`、`提交并读回场景宿主写集`、`检查S2当前代次` | 复用对外写入口的原子提交和读回骨架 |
| 节点 / 关系查找与业务前置 | 1470-1930 | 17 | `查找唯一节点项`、`检查父子无环`、`检查成员替换前置`、`检查固定属性前置` | 校验端点、引用、无环、同树和当前性 |
| 固定属性提交与当前完整快照 | 1939-2219 | 3 | `提交并读回固定属性写集`、`读取当前场景快照`、`读取当前完整场景` | 组合属性写入读回或当前场景快照 |
| 退出闭包与历史完整读取 | 2235-2486 | 5 | `场景退出闭包一致`、`提交并读回场景退出写集`、`读取历史完整场景` | 校验退出闭包并形成退出 / 历史材料 |
| 私有登记定位与交付验证 | 5194-5454 | 4 | `按定位读取当前场景宿主结构登记`、`验证并移动交付` | 从进程内定位正式读回登记或验证owner交付 |

## 使用边界

这些函数只解释或支撑`L2场景结构服务`公开操作。上层 BIZ、其它 DATA 服务和普通应用不得因为源码可见、名字相似或参数可构造而直接依赖它们。需要复用内部算法时，先判断是否应由既有对外服务入口承载；不能复制 helper 形成第二套幂等、事务、读回或错误解释。

当前17个登记 / 映射 / 失败形成函数已逐项制卡；其余73个仍只有身份数量、物理范围和责任分组，不能宣称90个函数全部完成用途卡。
