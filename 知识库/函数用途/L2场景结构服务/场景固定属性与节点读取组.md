# `L2场景结构服务`固定属性与节点读取组

- 完整身份：`新增场景固定属性`、`换代场景固定属性`、`读取场景节点`
- 边界类别：对外数据服务函数
- 位置：`海中鱼巣/领域/服务.L2场景结构.ixx:4083-4264`
- 模块：`海中鱼巣.领域.服务.L2场景结构`
- 绑定提交：`b186b65f67eb24e3683c318c07bbb394e3f9015f`
- 源码 blob：`a0b75ff333612f933595f33d780ed4e60a099319`
- 知识状态：当前
- 验证状态：静态类型登记、属性槽和完整读取委托已核；真实消费者`NOT_RUN`

## 用途

`新增场景固定属性`把一项类型正确的固定属性值写入指定场景及属性槽；`换代场景固定属性`在同一写集中建立新值并退出具名旧值；`读取场景节点`复用`读取完整场景`，只投影结果头与场景事实。

## 输入、输出和事实

属性写入必须使用当前属性类型登记允许的 I64、I64组、U64组或独立材料类型，携带来源和非零幂等身份，并在指定事实代次下证明场景当前存在。成功返回实际写入和正式读回材料；非成功负载不得冒充当前属性。

## 明确非职责与调用点

不解释属性的业务含义，不自动选择属性类型，不更新场景树关系。绑定提交上本组三个函数均无外部生产调用点；公开 ABI 存在不等于固定属性业务已经接线。
