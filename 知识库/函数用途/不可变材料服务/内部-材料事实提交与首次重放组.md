# 内部-材料事实提交与首次重放组

- 稳定身份：3个完整函数身份
- 边界类别：内部服务函数
- 模块：`海中鱼巣.领域.服务.不可变材料`
- 位置：`海中鱼巣/领域/服务.不可变材料.ixx:768-964`
- 绑定提交：`c003a6f4547b6c2bbdaee73676f03d19c7e6af59`
- 源码 blob：`5382f87a57943fac39e8e69823fb9c0b7bf1daf9`
- 知识状态：当前
- 验证状态：3/3身份与四事实结构静态互证；写后读回、历史、精确重复和退出闭包黑盒已覆盖

## 完整身份与用途

| 完整身份 | 只负责 |
| --- | --- |
| `不可变材料服务::实现::材料读取内部结果 不可变材料服务::实现::读取材料截止(不可变材料身份, L2读取类别, std::uint64_t, std::optional<std::uint64_t>) const` | 在稳定前后代次内从材料节点三槽、三值和最终文件重建单一当前/历史材料及三值编码 |
| `不可变材料写入结果 不可变材料服务::实现::提交材料写集(const 不可变材料写入请求&, 稳定编码, 不可变材料格式身份, const std::filesystem::path&)` | 一份owner写集建立材料节点、格式/长度/摘要三值和三槽，再从首次截止正式读回完整结果 |
| `不可变材料写入结果 不可变材料服务::实现::重读首次结果(const 不可变材料写入结果&) const` | 缓存命中后按首次变更截止重读文件与L1，返回原身份/原截止的精确重复 |

## 事实、事务和重复调用

`读取材料截止`不依赖请求回显或内容索引，要求节点与三值同owner、同创建代次、同来源，三槽恰好对应登记中的三属性类型；当前/历史退出投影必须一致。最终文件缺失或不一致不能用L1事实或缓存补成功。

`提交材料写集`直接使用调用方幂等身份作为L1写入幂等身份，不散列或另造第二键。成功与精确重复都以L1首次截止和正式读回为准。`重读首次结果`只恢复已成功首次结果，不能把失败缓存升级为成功。

## 明确非职责

这些函数不选择候选、不解释来源、不协调并发、不公开vector历史，也不回滚已发布事实。外部模块不得直接依赖写集本地键、三槽布局或内部读回结果形成第二ABI。

## 依据与证据

- `计划/20260814_CONCEPT-SPACE-STRUCTURE-CRUD-REBUILD-M1_不可变材料结构代码实施切片_v0.3.md:260-327`
- `验证记录/20260814_CONCEPT-M1_不可变材料验证记录.md:28-40`
