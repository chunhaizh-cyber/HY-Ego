# 内部-结果形成与请求守卫组

- 稳定身份：7个完整函数身份
- 边界类别：内部服务函数
- 模块：`海中鱼巣.领域.服务.不可变材料`
- 位置：`海中鱼巣/领域/服务.不可变材料.ixx:487-584,531-541`
- 绑定提交：`c003a6f4547b6c2bbdaee73676f03d19c7e6af59`
- 源码 blob：`5382f87a57943fac39e8e69823fb9c0b7bf1daf9`
- 知识状态：当前
- 验证状态：7/7身份和失败空载荷/截止守卫静态互证；公开可达拒绝已覆盖

## 完整身份与用途

| 完整身份 | 只负责 |
| --- | --- |
| `bool 不可变材料服务::实现::内容键比较::operator()(const 内容键&, const 内容键&) const noexcept` | 按格式、长度、摘要字节形成内容索引确定严格序 |
| `不可变材料服务::实现::实现(const L1事实基座服务&, L1所有者范围写端口&&, L1结构所有者身份, 路径句柄事实&&, 路径句柄事实&&, 路径句柄事实&&, 路径句柄事实&&, const 不可变材料登记事实&, bool)` | 一次持有L1引用、唯一写端口、四个目录句柄事实、登记和首次发布标记 |
| `static 不可变材料写入结果 不可变材料服务::实现::写入失败(L2结构状态, std::uint64_t = 0) noexcept` | 形成无材料、无变更代次的写入失败结果 |
| `static 不可变材料退出结果 不可变材料服务::实现::退出失败(L2结构状态, std::uint64_t = 0) noexcept` | 形成无退出材料、无变更代次的退出失败结果 |
| `static 不可变材料读取结果 不可变材料服务::实现::读取失败(const 不可变材料读取请求&, L2结构状态, std::uint64_t = 0) noexcept` | 保留请求读取类别/历史截止并形成空材料失败结果 |
| `static bool 不可变材料服务::实现::读取请求有效(const 不可变材料读取请求&) noexcept` | 校验合同、非零守卫、身份及当前/历史截止互斥形状 |
| `static bool 不可变材料服务::实现::登记请求有效(const 不可变材料登记读取请求&) noexcept` | 校验登记读取合同、非零守卫及当前/历史截止形状 |

## 边界与非职责

内容键不含来源；来源在内容协调项中另行精确比较，因此同内容异来源不能收敛。实现构造只接收已验证依赖，不负责验证或发布。失败形成函数不回滚已发布事实，也不把日志或异常文本放入结果。读取守卫只检查请求值形状，不证明目标材料存在或截止当前。

这些函数均不是公开ABI；外部消费者不得复制内部结果形成逻辑替代公开服务结果。

## 依据与证据

- `计划/20260814_CONCEPT-SPACE-STRUCTURE-CRUD-REBUILD-M1_不可变材料结构代码实施切片_v0.3.md:252-264,288-327`
- `验证记录/20260814_CONCEPT-M1_不可变材料验证记录.md:28-40`
