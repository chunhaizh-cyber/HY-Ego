# 公开材料写入读取退出与登记组

- 稳定身份：下表4个完整签名
- 边界类别：对外服务函数
- 模块：`海中鱼巣.领域.服务.不可变材料`
- 位置：`海中鱼巣/领域/服务.不可变材料.ixx:448-452,1189-1224`
- 绑定提交：`c003a6f4547b6c2bbdaee73676f03d19c7e6af59`
- 源码 blob：`5382f87a57943fac39e8e69823fb9c0b7bf1daf9`
- 知识状态：当前
- 实现状态：已实现；读入口已有正式生产消费者
- 验证状态：M1公开黑盒59/59、普通应用合法消费者35/35；真实BIZ写入、资源耗尽、长并发和跨进程恢复`NOT_RUN`

## 完整身份与合同

| 完整签名 | 操作与成功载荷 |
| --- | --- |
| `不可变材料写入结果 不可变材料服务::写入不可变材料(const 不可变材料写入请求&)` | 校验来源、候选、格式、长度和摘要，发布摘要对象与材料四事实；成功/精确重复返回首次截止完整活动材料 |
| `不可变材料读取结果 不可变材料服务::读取不可变材料(const 不可变材料读取请求&) const` | 按身份读取当前或具名历史截止的单一完整材料；当前退出返回已退出空载荷，创建前历史返回未找到 |
| `不可变材料退出结果 不可变材料服务::退出不可变材料(const 不可变材料退出请求&)` | 在引用保护允许时一次退出材料节点及三值；成功返回同代完整历史材料，不删除物理对象 |
| `不可变材料登记读取结果 不可变材料服务::读取不可变材料登记(const 不可变材料登记读取请求&) const` | 当前/历史读取系统签发者、两个格式身份和三属性类型的同截止登记快照 |

## 事实、事务、并发和重复调用

- 写入先正式读取来源，再复读候选完整字节；文件对象收敛后才提交一份材料owner写集，建立节点、三值和三槽。
- 请求账与内容索引仅在服务实例内协调同键和同内容并发，不是事实；命中后仍复读文件和L1首次结果。
- 读取从L1节点三槽和三值重组材料，并在前后事实代次稳定时返回，不从路径、请求字段或缓存猜事实。
- 退出用当前三槽取得三值编码，一份写集退出四项事实；外部引用冲突保持零变化。
- 公开边界捕获资源异常并返回结构化状态，不泄漏半载荷、不伪造回滚。

## 参数来源与合法调用

调用方应经普通应用上下文取得同一服务。写入请求的期望代次来自正式L1/材料结果，幂等身份由调用方在自己业务合同内稳定提供，候选必须位于服务受控根的`候选/`下；来源只能是系统签发或已发布普通节点。读取/退出身份必须来自既有材料结果，不得用路径、摘要或日志反推身份。

## 当前生产调用点

- 语言结构服务在`服务.L2语言结构.ixx:443-525`调用材料读取和登记读取，互证语言/词条材料的同截止格式。
- 概念结构服务在`服务.L2概念结构.ixx:1851-1898,5508-5535`调用材料读取，校验普通概念定义与支持材料。
- 当前正式项目源码没有材料写入或退出的模块外生产调用点；两者只具备公开能力和已发布专项证据，不能写成真实BIZ已接线。

## 明确非职责

服务不解释UTF-8文本含义、规范化规则、签名、语言、词条或概念，不选择生产根，不回收物理对象，不提供vector历史、跨进程索引恢复或第二材料owner。上层不得直达内部helper或L1写端口复制事务。

## 依据与证据

- `规范/4240_子规范_语言词条与概念空间纯结构统一合同.md:45-61,146-166`
- `施工记录/20260814_CONCEPT-M1_不可变材料施工记录.md:40-56`
- `验证记录/20260814_CONCEPT-M1_不可变材料验证记录.md:24-55`
- `施工记录/20260816_DATA-LANGUAGE-ORDINARY-APP-ASSEMBLY-READBACK_语言材料普通应用装配与首次权威读回施工记录_v0.1.md:40-61`
