# 构造输入与服务特殊成员组

- 稳定身份：下表9个显式身份
- 边界类别：非服务函数（装配输入与服务生命周期约束）
- 模块：`海中鱼巣.领域.服务.不可变材料`
- 位置：`海中鱼巣/领域/服务.不可变材料.ixx:416-446,1182`
- 绑定提交：`c003a6f4547b6c2bbdaee73676f03d19c7e6af59`
- 源码 blob：`5382f87a57943fac39e8e69823fb9c0b7bf1daf9`
- 知识状态：当前
- 验证状态：move-only交付、具名构造和普通应用`unique_ptr`持有已静态/动态互证；真实生产常驻生命周期`NOT_RUN`

## 完整身份

| 完整签名 | 状态与用途 |
| --- | --- |
| `不可变材料服务构造输入::不可变材料服务构造输入()` | `= default`；形成空的装配承载值 |
| `不可变材料服务构造输入::不可变材料服务构造输入(const 不可变材料服务构造输入&)` | `= delete`；禁止复制owner交付 |
| `不可变材料服务构造输入& 不可变材料服务构造输入::operator=(const 不可变材料服务构造输入&)` | `= delete` |
| `不可变材料服务构造输入::不可变材料服务构造输入(不可变材料服务构造输入&&) noexcept` | `= default`；一次移动配置和owner交付 |
| `不可变材料服务构造输入& 不可变材料服务构造输入::operator=(不可变材料服务构造输入&&) noexcept` | `= default` |
| `不可变材料服务::不可变材料服务()` | `= delete`；禁止无依赖空造服务 |
| `不可变材料服务::不可变材料服务(const 不可变材料服务&)` | `= delete`；禁止复制服务及写端口 |
| `不可变材料服务& 不可变材料服务::operator=(const 不可变材料服务&)` | `= delete` |
| `不可变材料服务::~不可变材料服务()` | 释放实现、写端口、句柄和协调状态；不退出已发布owner或材料事实 |

服务没有显式移动构造/赋值身份；普通应用以`std::unique_ptr<不可变材料服务>`稳定持有实例。

## 明确非职责

这些特殊成员不建立目录、owner、登记或材料，不提供服务定位器，也不证明进程全局只有一个实例。服务销毁不执行材料物理回收、事实退出或跨进程恢复。

## 依据与证据

- `计划/20260814_CONCEPT-SPACE-STRUCTURE-CRUD-REBUILD-M1_不可变材料结构代码实施切片_v0.3.md:222-264`
- `施工记录/20260816_DATA-LANGUAGE-ORDINARY-APP-ASSEMBLY-READBACK_语言材料普通应用装配与首次权威读回施工记录_v0.1.md:40-61`
