# `启动选项解析结果::成功`结果谓词

- 稳定身份：`bool 海中鱼巣::启动选项解析结果::成功() const noexcept`
- 边界类别：非服务函数（启动解析结果纯值谓词）
- 函数种类和可见性：公开结构体内联const成员函数；`noexcept`
- 模块：无独立C++模块；由启动入口与程序入口源码直接包含
- 位置：`海中鱼巣/启动选项.数据.h:33-35`
- 绑定提交：`61dd2c7e2d7db2f7bf0dca6fa79f26656ca963c7`
- 源码 blob：`62ebdb879a47afc44ed589c9c3dd180cfd21406b`
- 知识状态：当前代码已核；正式三模式合同漂移待治理
- 实现状态：已实现
- 验证状态：1/1函数正文、两个直接调用点和8120合同已静态互证；真实参数矩阵`NOT_RUN`

## 用途

只判断`启动选项解析结果::状态`是否精确等于`启动选项解析状态::已接受`，供`main`决定是否进入顶层程序，并供退出码映射区分请求拒绝与已准入运行。

## 明确非职责

- 不重新解析参数，不检查原始参数指针或拒绝索引。
- 不验证`选项.模式`是否属于当前枚举，也不验证状态、模式和拒绝索引之间的组合一致性。
- 不启动程序、不写日志、不映射退出码，也不证明所选模式已经实现或运行成功。

## 输入、输出和错误

隐式输入是当前结果对象；无显式参数。状态为`已接受`返回`true`，其它六种状态返回`false`。函数无异常、无诊断、无结构化错误载荷。

## 事实、副作用、事务和生命周期

只读对象内一个枚举字段，零I/O、零事实读写、零锁、零事务，不保存对象引用。返回值仅描述解析状态，不产生运行期机器事实。

## 合法调用语境

允许命令行入口和退出码映射在已经取得完整`启动选项解析结果`后使用。手工构造、反序列化或未来新增模式的调用方不能只用本谓词替代DTO组合有效性检查。

## 当前生产调用点

- `海中鱼巣/入口.cpp`：拒绝结果直接映射退出码2，成功后才调用`运行海中鱼巣`。
- `海中鱼巣/启动.程序入口.ixx`中的`映射进程退出码`：再次检查解析结果，拒绝时返回2。

## 依据与证据

- `规范/8120_子规范_程序入口启动模式与运行宿主边界_20260726.md:19-57`
- `海中鱼巣/启动选项.数据.h:9-35`
- `海中鱼巣/入口.cpp:8-14`
- `海中鱼巣/启动.程序入口.ixx:49-61`

## 已知缺口

8120规定`普通控制面板`、`无窗口常驻`、`生产运行期`三种模式和`--runtime-context`参数；当前DTO只有前两种模式。谓词会接受任何状态为`已接受`的对象，不能掩盖模式枚举、解析器和路由尚未实现第三模式的正式漂移。未运行七种解析状态、拒绝索引和退出码矩阵。
