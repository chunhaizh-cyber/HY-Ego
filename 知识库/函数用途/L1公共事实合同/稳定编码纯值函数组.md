# 稳定编码纯值函数组

- 稳定身份：下表3个完整签名
- 边界类别：非服务函数（L1公共稳定编码纯值运算）
- 函数种类和可见性：公开头内`friend`默认比较、`friend noexcept`全序比较和命名空间`inline noexcept`守卫；经公共事实模块导出
- 模块：`海中鱼巣.核心.合同.L1公共事实`
- 位置：`海中鱼巣/核心/L1公共事实.数据.h:9-17`
- 绑定提交：`b055ebb180851b25db0cc5630eb4297b22717e29`
- 源码 blob：`d8e128e1ebf6a1d46c38051427b7e912a676cef2`
- 知识状态：当前
- 实现状态：已实现
- 验证状态：3/3个函数正文和正式规范已静态互证；真实事实服务调用`NOT_RUN`

## 完整身份与用途

| 完整签名 | 用途 |
| --- | --- |
| `bool operator==(const 稳定编码&, const 稳定编码&)` | 默认逐字段比较两个不透明编码的数值是否相同 |
| `bool operator<(const 稳定编码& 左, const 稳定编码& 右) noexcept` | 只按底层`uint64`值提供严格全序，用于确定排序、去重前置和有序容器 |
| `bool 有效(稳定编码 编码) noexcept` | 只判断底层数值是否非零 |

## 用途

本组为节点、关系、值和L1结构所有者共用的稳定编码空间提供最小纯值操作。`operator<`让结果可以按稳定编码形成确定顺序；`有效`让请求守卫拒绝零编码；默认相等可用于DTO比较、集合去重和读回互证。

## 明确非职责

- 不分配、预留、发布、退出或恢复编码，不判断编码是否曾被占用。
- 不从编码推导类型、名称、owner、容器位置、创建时间、版本、业务对象或节点族。
- 数值更小不表示创建更早、层级更低、优先级更高或业务顺序更前。
- 非零只说明纯值形状可进入后继检查，不证明事实存在、当前、可访问或归属正确。

## 输入、输出和错误

三个函数只接收值式`稳定编码`，返回`bool`。它们不产生结构化状态、诊断或异常分支；零编码可以参与比较，但`有效`返回`false`。

## 事实、副作用、事务和生命周期

只读参数，不访问仓库、服务、owner端口、锁、事务、SQL、缓存或时钟，不推进事实代次，不改变任何生命周期。

## 合法调用语境

允许L1—L3值式DTO守卫、严格排序、去重和精确读回比较。需要判断事实状态时必须继续调用正式L1服务；需要判断领域身份时必须调用目标结构服务，不能只看编码数值。

## 当前生产调用点

`有效(稳定编码)`被L1仓库、L1服务、各L2数据头/服务和生产初始化广泛重载解析调用；`operator<`直接用于属性槽、关系组和其它稳定投影的确定排序。调用方必须以完整参数类型区分本函数与其它同名`有效`重载。

## 依据与证据

- `规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md:15-27`
- `规范/4015_子规范_L1简化事实基座.md:21-28`
- `海中鱼巣/核心/L1公共事实.数据.h:9-17`
- 本知识批不绑定代码施工计划；用途结论只描述当前正式源码和正式合同

## 已知缺口

未运行编码分配、永久占用、并发发布或恢复矩阵；这些能力也不由本文件承载。当前调用图冻结基线落后，调用点以正式HEAD源码扫描为准。
