# L2动态请求守卫与成功谓词组

- 稳定身份：下表23个完整函数身份
- 边界类别：非服务函数（纯值守卫与结果谓词）
- 模块：`海中鱼巣.领域.服务.L2动态结构`
- 位置：`海中鱼巣/领域/L2动态结构.数据.h:302-582`
- 绑定提交：`90477df35f6d7f78beda33e6e59d57c120ea5f30`
- 源码 blob：`797a4f45d89706493649fc890a63be4c46ab7f3a`
- 知识状态：当前
- 实现状态：已实现并登记生产工程
- 验证状态：23/23定义及服务调用点已静态互证；DYN1/DYN2、来源和时间范围专项覆盖主要公开形状，其它见`NOT_RUN`

## 请求、事实与材料守卫（16）

| 完整函数身份 | 实际用途 |
| --- | --- |
| `bool L2动态结构类型登记请求有效(const L2动态结构类型登记请求&) noexcept` | 校验共同请求头、规则版本和固定登记幂等身份 |
| `bool L2动态结构类型登记完整(const L2动态结构类型登记&) noexcept` | 校验十一编码非零互异、版本、首次身份和建立/截止代次 |
| `bool L2动态原始值材料有效(const L2原始值材料&) noexcept` | 校验I64、非空I64/U64组或非零独立材料引用 |
| `bool L2动态属性角色与材料一致(const L2动态结构类型登记&, const L2属性事实&, bool) noexcept` | 核对起始/结束角色与四种专属类型及材料表示一致 |
| `bool L2动态事实完整(const L2动态事实&, const L2动态结构类型登记&, std::uint64_t) noexcept` | 核对身份、三端点、两时间属性、共同来源和生命周期截止 |
| `bool L2动态新增请求有效(const L2动态新增请求&) noexcept` | 校验幂等、三端点、来源及两份材料形状；不要求状态差异或时间有序 |
| `bool L2动态完整读取请求有效(const L2动态完整读取请求&) noexcept` | 分账当前零历史截止与历史非零且不越过期望代次 |
| `bool L2动态身份来源读取请求有效(const L2动态身份来源读取请求&) noexcept` | 校验动态身份及当前/历史来源读取守卫 |
| `bool L2按主体当前动态读取请求有效(const L2按主体当前动态读取请求&) noexcept` | 校验主体、非零守卫和数量预算 |
| `bool L2按前状态当前动态读取请求有效(const L2按前状态当前动态读取请求&) noexcept` | 校验前状态、非零守卫和数量预算 |
| `bool L2按后状态当前动态读取请求有效(const L2按后状态当前动态读取请求&) noexcept` | 校验后状态、非零守卫和数量预算 |
| `bool L2按来源当前动态读取请求有效(const L2按来源当前动态读取请求&) noexcept` | 校验来源、非零守卫和数量预算 |
| `bool L2按I64时间范围当前动态读取请求有效(const L2按I64时间范围当前动态读取请求&) noexcept` | 校验闭区间、两端候选预算和返回预算 |
| `bool L2动态引用替换请求有效(const L2动态引用替换请求&) noexcept` | 校验角色、旧关系、新强类型目标与幂等写前置 |
| `bool L2动态时间值换代请求有效(const L2动态时间值换代请求&) noexcept` | 校验起止角色、旧值、来源和新材料 |
| `bool L2动态退出请求有效(const L2动态退出请求&) noexcept` | 校验六项自有事实闭包严格升序、非零且不含动态节点自身 |

## 结果成功谓词（7）

| 完整函数身份 | 认可的成功形状 |
| --- | --- |
| `bool L2动态结构类型登记结果::成功() const noexcept` | 已提交/精确重复/已读取、非零截止及完整登记 |
| `bool L2动态写入结果::成功() const noexcept` | 成功写状态、同截止变更、动态与四个互异自有关系编码 |
| `bool L2动态身份来源读取结果::成功() const noexcept` | 已读取、无变更、正确动态族来源及当前/历史截止 |
| `bool L2当前动态组读取结果::成功() const noexcept` | 已读取、无变更、成员当前且按动态身份严格升序 |
| `bool L2动态引用替换结果::成功() const noexcept` | 同截止变更、当前动态/自有关系和具名已退出旧关系 |
| `bool L2动态时间值换代结果::成功() const noexcept` | 同截止变更、当前动态/自有关系和同代退出旧值 |
| `bool L2动态退出结果::成功() const noexcept` | 同截止变更、动态/两值同代退出及四个互异关系编码 |

## 边界

全组零事实读写、零锁、零诊断。守卫只检查公开值形状，不证明端点存在、前后状态构成变化、主体一致、时间有序或来源具有业务资格；`成功()`也不替代调用方读取结构化状态和具名验证证据。

## 正式缺口

有界组查询会返回代码中的`数量预算不足=15`，4230共同枚举尚未同步；1170要求的场景、被改变目标和来源动作不在当前DTO。此卡只暴露差异，不补造字段或裁决合同。
