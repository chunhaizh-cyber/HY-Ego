# L2当前动态组查询

- 稳定身份：5个对外服务函数
- 模块：`海中鱼巣.领域.服务.L2动态结构`
- 位置：`海中鱼巣/领域/服务.L2动态结构.ixx:1260-1648`
- 绑定提交：`614e4a32368a73ad81c274479fff4b0b12805fb1`
- 源码 blob：`f5b88b230c7244a4b744da58716e751b9655e6c2`
- 知识状态：当前
- 实现状态：已实现并登记生产工程
- 验证状态：DYN1三端点；来源两配置13/13；I64范围两配置23/23；端点预算静态合同；正式BIZ消费`NOT_RUN`

## 完整身份与用途

| 完整函数身份 | 实际用途 |
| --- | --- |
| `L2当前动态组读取结果 L2动态结构服务::按主体读取全部当前动态(const L2按主体当前动态读取请求& 请求) const` | 验证存在来源，按主体关系反查当前动态，预算后逐项完整读回 |
| `L2当前动态组读取结果 L2动态结构服务::按前状态读取全部当前动态(const L2按前状态当前动态读取请求& 请求) const` | 验证状态来源，按前状态关系反查当前动态 |
| `L2当前动态组读取结果 L2动态结构服务::按后状态读取全部当前动态(const L2按后状态当前动态读取请求& 请求) const` | 验证状态来源，按后状态关系反查当前动态 |
| `L2当前动态组读取结果 L2动态结构服务::按来源读取全部当前动态(const L2按来源当前动态读取请求& 请求) const` | 通过owner-aware来源当前值组定位起止值候选，去重动态并完整读回互证来源 |
| `L2当前动态组读取结果 L2动态结构服务::按I64时间范围读取全部当前动态(const L2按I64时间范围当前动态读取请求& 请求) const` | 分别读取起止I64当前值候选，按两个闭区间不等式求交，执行候选/返回预算并完整读回互证 |

## 读取、预算与并发

五入口零写，成功组共享请求的非零事实截止并按动态身份严格升序。三个端点入口在内部去重排序后、完整读回前执行数量预算；来源与时间范围另有候选预算。一次读取窗口中的内部不一致会重新探测代次，确认前移时收敛为事实代次漂移，不返回混代部分组。

I64时间范围只检查当前起止材料各自落入调用方闭区间，逆序动态仍是合法DATA结构；非I64材料不参与且不报错。该入口不是排序、持续时间、相邻状态或业务变化查询。

## 已知缺口

当前没有模块外生产调用点。4230共同状态枚举未登记代码使用的`数量预算不足=15`；端点预算没有独立运行专项。真实资源耗尽、私有损坏、长期公平、恢复、跨进程和正式BIZ消费`NOT_RUN`。
