# 动态私有提交读回与端点查询核心组

- 完整身份：下表6个类私有函数
- 边界类别：5个内部服务函数、1个服务装配函数
- 模块：`海中鱼巣.领域.服务.L2动态结构::L2动态结构服务`
- 位置：`海中鱼巣/领域/服务.L2动态结构.ixx:2001-2649`
- 绑定提交：`614e4a32368a73ad81c274479fff4b0b12805fb1`
- 源码 blob：`f5b88b230c7244a4b744da58716e751b9655e6c2`
- 知识状态：当前
- 实现状态：已实现
- 验证状态：6/6定义、控制分支和调用点已静态互证；DYN1/DYN2及查询专项覆盖主要路径，真实资源失败、私有损坏和恢复`NOT_RUN`

## 完整身份与用途

| 行 | 完整函数身份 | 类别与实际用途 |
| ---: | --- | --- |
| 2001 | `收敛并发条件读取(L2当前动态组读取结果, uint64_t) const` | 内部服务；遇内部不一致时探测当前代次，确认前移则收敛为漂移 |
| 2011 | `static 验证并移动交付(const L1事实基座服务&, L2动态所有者交付&) -> L1所有者范围写端口&&` | 服务装配；要求端口有效且绑定同一L1后移动 |
| 2019 | `提交并读回新增动态(const L1所有者范围写集请求&, const L2动态新增请求&, const L2动态结构类型登记&)` | 内部服务；恰一次提交七事实并从首次/重复边界正式读回完整结果 |
| 2190 | `读取当前完整动态(const L2动态完整读取请求&, const L2动态结构类型登记&) const` | 内部服务；两段一致投影闭合节点、来源、三端点、两属性和共同截止 |
| 2374 | `读取历史完整动态(const L2动态完整读取请求&, const L2动态结构类型登记&) const` | 内部服务；按历史截止重建结构、退出生命周期和三端点来源 |
| 2553 | `按端点读取动态组(const L2结构请求头&, 稳定编码, 动态端点角色, uint64_t) const` | 内部服务；按主体/前/后关系反查，去重排序、预算并逐项完整读回 |

## 事实、副作用、事务与生命周期

只有`提交并读回新增动态`写入权威事实；其余函数零写。当前读回的借用投影不越过局部结果生命周期，历史读回不使用当前事实补历史。端点组在预算通过后逐项调用公开完整读取并要求统一截止和角色匹配。

## 明确非职责

这些函数不是公开ABI，不供概念、BIZ、普通应用或验证代码直接调用。它们不判断状态变化真实性、主体一致、时间顺序或动作归因，也不提供递归图遍历、缓存或第二事实入口。
