# 所有者范围公开读取组

- 稳定身份：下表14个完整签名
- 边界类别：对外服务函数（owner-aware v2只读入口）
- 函数种类和可见性：`L1事实基座服务`公开`const`成员函数
- 模块：`海中鱼巣.核心.服务.L1事实基座`
- 位置：`海中鱼巣/核心/服务.L1事实基座.ixx:456-527`
- 绑定提交：`614e4a32368a73ad81c274479fff4b0b12805fb1`
- 源码 blob：`b9dfce530406875f6b696bb7e8a798f24ddf4a09`
- 知识状态：当前
- 实现状态：已实现
- 验证状态：14/14入口与仓库同名转发静态互证；公开读取矩阵`NOT_RUN`

## 完整身份

```text
L1所有者范围当前读取结果 L1事实基座服务::读取所有者范围当前节点(const L1所有者范围事实读取请求&) const
L1所有者范围当前读取结果 L1事实基座服务::读取所有者范围当前关系(const L1所有者范围事实读取请求&) const
L1所有者范围当前读取结果 L1事实基座服务::读取所有者范围当前值(const L1所有者范围事实读取请求&) const
L1所有者范围来源当前值组读取结果 L1事实基座服务::按来源读取所有者范围全部当前值(const L1所有者范围来源当前值组读取请求&) const
L1所有者范围属性类型当前值组读取结果 L1事实基座服务::按属性类型读取所有者范围全部当前值(const L1所有者范围属性类型当前值组读取请求&) const
L1所有者范围源关系组读取结果 L1事实基座服务::读取所有者范围当前源关系组(const L1所有者范围源关系组读取请求&) const
L1所有者范围目标关系组读取结果 L1事实基座服务::读取所有者范围当前目标关系组(const L1所有者范围目标关系组读取请求&) const
L1所有者范围历史关系组读取结果 L1事实基座服务::读取所有者范围历史关系组(const L1所有者范围历史关系组读取请求&) const
L1所有者范围历史属性值组读取结果 L1事实基座服务::读取所有者范围历史属性值组(const L1所有者范围历史属性值组读取请求&) const
L1所有者范围历史读取结果 L1事实基座服务::读取所有者范围历史事实(const L1所有者范围事实读取请求&) const
L1所有者范围一致当前读取结果 L1事实基座服务::尝试读取所有者范围一致当前投影(const L1所有者范围一致当前读取请求&) const
L1所有者范围一致关系类型闭包读取结果 L1事实基座服务::尝试读取所有者范围一致关系类型闭包投影(const L1所有者范围一致关系类型闭包读取请求&) const
L1结构所有者当前读取结果 L1事实基座服务::读取当前结构所有者(const L1结构所有者读取请求&) const
L1结构所有者历史读取结果 L1事实基座服务::读取历史结构所有者(const L1结构所有者读取请求&) const
```

## 用途

本组是L2结构服务读取同一owner事实、当前/历史节点关系值、有界反向值组和同截止投影的正式L1入口。服务层只转发同实例仓库，不暴露锁、索引、候选或raw端口。

## 明确非职责

不解释L2节点族、业务资格、单位或名称；不跨owner列举；不提供整库快照、递归图遍历或缓存权威。成功空组只表示具名条件下无成员，不能解释为目标业务不存在。

## 事实、副作用、事务和生命周期

全部只读；当前一致投影和关系类型闭包由仓库在同一共享锁/截止形成，服务不拼接第二代次材料。当前源码为直接转发，结构化状态和载荷由仓库裁决。

## 依据与证据

- `规范/4015_子规范_L1简化事实基座.md:140-180`
- `规范/4015_子规范_L1简化事实基座.md:213-253`
- `海中鱼巣/核心/服务.L1事实基座.ixx:456-527`

## 已知缺口

未运行零守卫最新快照、非零漂移、空组、超预算、历史截止和闭包一跳矩阵。函数存在不证明任一L2消费者已完整覆盖全部读取合同。
