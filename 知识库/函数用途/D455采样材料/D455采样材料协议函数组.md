# D455采样材料协议函数组

- 稳定身份：16个身份
- 边界类别：SDK无关配置、原始同步帧材料和帧来源端口
- 模块：`海中鱼巣.适配.协议.D455采样材料`
- 位置：`海中鱼巣/适配/协议.D455采样材料.ixx:160-354`
- 绑定提交：`04c1179ca5cc3b2945dc39fa3aa6f9e2b56f9088`
- 源码 blob：`f110508fd4e607469d69db1068bd1b15ee010d60`
- 知识状态：当前
- 实现状态：已登记工程；由采集器、缓存和桥静态消费
- 验证状态：16/16身份静态互证；当前真实硬件、断流和生产消费`NOT_RUN`

| 完整签名 | 用途 |
| --- | --- |
| `virtual D455帧来源::~D455帧来源() = default` | 允许经帧来源端口安全销毁实现 |
| `virtual D455采集结果 D455帧来源::采样一次(std::uint32_t) = 0` | 抽象一次有界等待采样 |
| `virtual void D455帧来源::请求停止() noexcept = 0` | 抽象停止请求 |
| `virtual void D455帧来源::关闭() noexcept = 0` | 抽象资源关闭 |
| `virtual D455采集状态快照 D455帧来源::读取状态快照() const = 0` | 抽象采集状态快照 |
| `D455采集配置 生成D455默认采集配置(std::uint32_t = 2000)` | 形成RGB、深度和双红外默认配置 |
| `bool D455流配置有效(const D455流配置&) noexcept` | 复核流类型、索引、格式、尺寸和帧率 |
| `bool D455采集配置有效(const D455采集配置&) noexcept` | 复核四流配置和采样超时 |
| `std::uint32_t D455格式每像素字节数(D455像素格式) noexcept` | 映射RGB8、Z16和Y8字节数 |
| `bool D455设备身份有效(const D455设备身份材料&) noexcept` | 要求名称、序列号、固件和产品编号 |
| `bool D455相机内参有效(const D455相机内参材料&) noexcept` | 复核尺寸、焦距和有限畸变系数 |
| `bool D455相机外参有效(const D455相机外参材料&) noexcept` | 复核流身份、有限矩阵和非零旋转 |
| `bool D455图像平面有效(const D455图像平面材料&) noexcept` | 复核格式、尺寸、步长、字节、时间、内参和深度比例 |
| `std::uint64_t 计算D455同步帧字节数(const D455同步帧材料&) noexcept` | 汇总四平面实际字节数 |
| `bool D455同步帧材料有效(const D455同步帧材料&) noexcept` | 复核设备、批次、四平面和三组外参闭合 |
| `std::uint64_t 计算D455材料摘要(const D455同步帧材料&) noexcept` | 从设备、批次、标定、帧号和字节数形成非密码摘要 |

原始同步帧只是外设材料，不是领域事实或四类产品。摘要只用于材料识别和幂等辅助，不是内容完整性证明，也不能替代完整值、设备代次或来源链。
