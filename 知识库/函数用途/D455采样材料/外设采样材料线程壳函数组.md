# 外设采样材料线程壳函数组

- 稳定身份：17个身份
- 边界类别：模拟/空外部材料运行消息线程壳
- 模块：`海中鱼巣.线程.外设采样材料线程`
- 位置：`海中鱼巣/线程/外设采样材料线程.ixx:67-229`
- 绑定提交：`04c1179ca5cc3b2945dc39fa3aa6f9e2b56f9088`
- 源码 blob：`bd7421b99136affac597e42bb4dced3781171eea`
- 知识状态：当前
- 实现状态：已登记工程；无生产import；文件合同禁止接真实D455
- 验证状态：17/17身份静态互证；当前生产线程启动、材料流和收口`NOT_RUN`

| 完整签名 | 用途 |
| --- | --- |
| `explicit 外设采样材料线程::外设采样材料线程(std::uint64_t)` | 建立有界运行消息队列 |
| `外设采样材料线程::~外设采样材料线程()` | fallback锁存停止并join |
| `外设采样材料线程::外设采样材料线程(const 外设采样材料线程&) = delete` | 禁止复制 |
| `外设采样材料线程& 外设采样材料线程::operator=(const 外设采样材料线程&) = delete` | 禁止复制赋值 |
| `外设采样材料线程状态 外设采样材料线程::当前状态() const` | 读取线程壳状态 |
| `bool 外设采样材料线程::线程已进入() const` | 读取进入见证 |
| `bool 外设采样材料线程::线程可收口() const` | 以joinable反向判断收口 |
| `const 有界运行消息队列& 外设采样材料线程::只读外部材料队列() const` | 暴露队列只读引用 |
| `外设采样材料线程操作结果 外设采样材料线程::启动()` | 启动只等待停止的yield线程壳 |
| `外设采样材料线程操作结果 外设采样材料线程::发送外部材料消息(const 运行消息&)` | 只接受外设采样来源的外部材料消息 |
| `外设采样材料线程消费结果 外设采样材料线程::消费外部材料一次()` | 从本线程队列取出一条外部材料消息 |
| `外设采样材料线程消费结果 外设采样材料线程::丢弃过期外部材料一次(std::uint64_t)` | 取出一条消息并按时间戳判断丢弃 |
| `外设采样材料线程操作结果 外设采样材料线程::请求停止并发送停止消息(std::uint64_t, std::uint64_t)` | 向本队列发布停止材料并锁存停止 |
| `外设采样材料线程操作结果 外设采样材料线程::收口等待()` | join并更新已停止状态 |
| `外设采样材料线程操作结果 外设采样材料线程::成功结果() const` | 形成无业务写入的成功结果 |
| `外设采样材料线程操作结果 外设采样材料线程::拒绝(外设采样材料线程拒绝原因) const` | 形成操作拒绝 |
| `外设采样材料线程消费结果 外设采样材料线程::消费拒绝(外设采样材料线程拒绝原因) const` | 形成消费拒绝 |

该类型同时拥有生产和消费同一队列的入口，是早期线程壳，不符合现行6340的任务域唯一消费边界，也不导入D455协议、采集器或缓存。不得把它与D455桥拼接为当前生产链；可复用的只有一般线程生命周期和队列调用形状。
