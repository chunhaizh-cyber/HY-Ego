# D455相机采集器函数组

- 稳定身份：46个身份
- 边界类别：RealSense SDK资源、设备选择、四流采集和SDK无关材料转换
- 模块：`海中鱼巣.适配.采集器.D455相机`
- 位置：`海中鱼巣/适配/采集器.D455相机.ixx:31-755`
- 绑定提交：`04c1179ca5cc3b2945dc39fa3aa6f9e2b56f9088`
- 源码 blob：`9009c0c875af65020d4e2edef14a5e33aa7cc948`
- 知识状态：当前
- 实现状态：已登记工程；无生产import
- 验证状态：46/46身份静态互证；当前真实设备打开、采样、断流、重连和生产装配`NOT_RUN`

| 完整签名 | 只负责 |
| --- | --- |
| `SDK错误槽::SDK错误槽() = default` | 初始化空SDK错误槽 |
| `SDK错误槽::SDK错误槽(const SDK错误槽&) = delete` | 禁止复制SDK错误所有权 |
| `SDK错误槽& SDK错误槽::operator=(const SDK错误槽&) = delete` | 禁止复制赋值 |
| `SDK错误槽::~SDK错误槽()` | 释放SDK错误对象 |
| `rs2_error** SDK错误槽::接收() noexcept` | 提供SDK错误输出地址 |
| `bool SDK错误槽::失败() const noexcept` | 判断SDK错误是否存在 |
| `void 上下文释放器::operator()(rs2_context*) const noexcept` | 释放SDK上下文 |
| `void 设备列表释放器::operator()(rs2_device_list*) const noexcept` | 释放设备列表 |
| `void 设备释放器::operator()(rs2_device*) const noexcept` | 释放设备 |
| `void 管线释放器::operator()(rs2_pipeline*) const noexcept` | 释放采集管线 |
| `void 配置释放器::operator()(rs2_config*) const noexcept` | 释放配置 |
| `void 管线配置释放器::operator()(rs2_pipeline_profile*) const noexcept` | 释放管线配置 |
| `void 流配置列表释放器::operator()(rs2_stream_profile_list*) const noexcept` | 释放流配置列表 |
| `void 传感器列表释放器::operator()(rs2_sensor_list*) const noexcept` | 释放传感器列表 |
| `void 传感器释放器::operator()(rs2_sensor*) const noexcept` | 释放传感器 |
| `void 帧释放器::operator()(rs2_frame*) const noexcept` | 释放SDK帧 |
| `std::string 读取设备信息(const rs2_device*, rs2_camera_info, bool&)` | 读取具名设备信息并反馈SDK调用状态 |
| `bool 是D455设备(const rs2_device*, bool&)` | 以名称含D455且产品线为D400筛选设备 |
| `D455设备身份材料 转换设备身份(const rs2_device*, bool&)` | 读取名称、序列号、固件和产品编号 |
| `rs2_stream 转换流类型(D455流类型) noexcept` | 映射协议流类型到SDK枚举 |
| `rs2_format 转换像素格式(D455像素格式) noexcept` | 映射协议像素格式到SDK枚举 |
| `D455时间戳域 转换时间戳域(rs2_timestamp_domain) noexcept` | 映射SDK时间戳域 |
| `D455相机内参材料 转换内参(const rs2_intrinsics&)` | 把SDK内参复制为值式材料 |
| `D455相机外参材料 转换外参(const rs2_extrinsics&, D455流类型, std::uint32_t, D455流类型, std::uint32_t)` | 把SDK外参和流身份复制为值式材料 |
| `std::uint64_t 当前单调时间戳微秒()` | 形成宿主steady clock微秒投影 |
| `bool 读取流配置(const rs2_stream_profile*, D455流配置&)` | 从SDK流配置读取类型、索引、格式、尺寸和帧率 |
| `bool 流配置相同(const D455流配置&, const D455流配置&) noexcept` | 比较完整流配置 |
| `bool 复核实际流配置(rs2_pipeline_profile*, const D455采集配置&)` | 枚举实际四流并与请求配置互证 |
| `bool 读取深度比例(const rs2_device*, float&)` | 遍历深度传感器读取深度单位 |
| `bool 读取帧流身份(const rs2_frame*, rs2_stream&, int&)` | 从帧配置读取SDK流类型和索引 |
| `bool 复制图像平面(const rs2_frame*, D455流类型, std::uint32_t, D455像素格式, float, D455图像平面材料&)` | 复制尺寸、字节、时间、内参和像素数据并复核 |
| `bool 读取外参(const rs2_frame*, const rs2_frame*, D455流类型, std::uint32_t, D455流类型, std::uint32_t, D455相机外参材料&)` | 读取两帧配置间外参并转换 |
| `std::uint64_t 计算标定摘要(const D455同步帧材料&)` | 从设备、尺寸和部分外参形成标定摘要 |
| `explicit D455相机采集器::D455相机采集器(D455采集配置)` | 保存请求配置 |
| `D455相机采集器::~D455相机采集器() override` | 关闭采集器和SDK资源 |
| `D455相机采集器::D455相机采集器(const D455相机采集器&) = delete` | 禁止复制 |
| `D455相机采集器& D455相机采集器::operator=(const D455相机采集器&) = delete` | 禁止复制赋值 |
| `D455相机采集器::D455相机采集器(D455相机采集器&&) = delete` | 禁止移动 |
| `D455相机采集器& D455相机采集器::operator=(D455相机采集器&&) = delete` | 禁止移动赋值 |
| `D455操作结果 D455相机采集器::打开()` | 选择唯一D455、配置四流、启动管线并复核设备与实际配置 |
| `D455采集结果 D455相机采集器::采样一次(std::uint32_t) override` | 等待frameset、分离四流、复制平面/外参并形成同步帧 |
| `void D455相机采集器::请求停止() noexcept override` | 锁存停止请求 |
| `void D455相机采集器::关闭() noexcept override` | 停止管线并释放SDK资源 |
| `D455采集状态快照 D455相机采集器::读取状态快照() const override` | 复制状态、设备、实际配置和批次数 |
| `D455操作结果 D455相机采集器::打开失败(D455采集拒绝原因, bool) noexcept` | 清理打开候选并形成拒绝结果 |
| `std::unique_ptr<D455帧来源> 创建并打开D455相机采集器(D455采集配置, D455操作结果&)` | 建立具体采集器并只在打开成功时返回端口所有权 |

采集器直接依赖RealSense C SDK，但正式HEAD没有生产import，不能据工程登记推导设备正在使用。`是D455设备`基于名称和产品线字符串筛选，标定摘要也不是密码学标识；生产设备占用、运行代次、断流恢复和验收必须由后继正式服务另行闭合。
