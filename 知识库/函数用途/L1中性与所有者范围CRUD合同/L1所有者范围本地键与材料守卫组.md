# L1所有者范围本地键与材料守卫组

- 稳定身份：下表6个完整签名
- 边界类别：非服务函数（owner-scoped v2身份、本地键与材料纯值守卫）
- 函数种类和可见性：公开头内`friend noexcept`比较与命名空间`inline noexcept`函数
- 模块：`海中鱼巣.核心.合同.L1所有者范围CRUD`
- 位置：`海中鱼巣/核心/L1所有者范围CRUD.数据.h:116-122,694-708`
- 绑定提交：`2fed1b048cfe770b92d18fa787983e9696212fbd`
- 源码 blob：`c08decabb9b00b0b26c6c44ccc242fbd13e1581d`
- 知识状态：当前
- 实现状态：已实现
- 验证状态：6/6函数正文及当前生产调用点已静态互证；端口与写读矩阵`NOT_RUN`

## 完整身份与用途

| 完整签名 | 用途 |
| --- | --- |
| `bool operator<(const L1所有者范围写集本地键& 左, const L1所有者范围写集本地键& 右) noexcept` | 按本地键数值提供严格全序 |
| `bool 有效(L1结构所有者身份 身份) noexcept` | 复用公共稳定编码守卫，判断owner编码非零 |
| `bool 有效(L1所有者范围建立幂等身份 身份) noexcept` | 判断owner建立/重入/退出幂等身份非零 |
| `bool 有效(L1所有者范围写入幂等身份 身份) noexcept` | 判断owner-scoped写集幂等身份非零 |
| `bool 有效(L1所有者范围写集本地键 键) noexcept` | 判断一份v2写集内的局部引用键非零 |
| `bool L1所有者范围原始材料完整(const L1所有者范围原始值材料& 材料) noexcept` | I64允许任意值，I64/U64组拒绝空组，独立材料引用要求编码非零 |

## 用途与调用语境

四个`有效`重载为仓库、服务、签发器和L2公开服务提供纯值入口守卫；本地键比较支持完整写集规范化和确定映射。当前正式生产源码没有`L1所有者范围原始材料完整`的直接调用点，仓库会先把v2请求规范化为中性形状并调用中性材料守卫，因此本函数当前是公开合同辅助而非已消费生产路径。

## 明确非职责

- 非零owner不证明owner事实存在、当前、属于本L1实例或仍持有唯一活动端口。
- 非零幂等身份不证明未绑定、同请求或可换请求重试；幂等域仍包含精确L1实例与owner。
- 本地键不是稳定编码或跨请求引用。
- 材料形状完整不证明材料节点存在、表示种类匹配、来源合法或业务值域正确。

## 事实、副作用、事务和生命周期

只读值式参数并返回`bool`，不访问仓库、端口状态、幂等账、锁或事实，不推进事实代次。正式写入仍由活动端口隐式注入owner并由仓库原子提交。

## 依据与证据

- `规范/4015_子规范_L1简化事实基座.md:94-124`
- `规范/4015_子规范_L1简化事实基座.md:313-343`
- `海中鱼巣/核心/L1所有者范围CRUD.数据.h:116-122,694-708`

## 已知缺口

`L1所有者范围原始材料完整`当前无生产直接消费者，不能据其存在宣称owner-scoped写集已独立验证。未运行端口许可、幂等域、材料类型和发布未知矩阵。
