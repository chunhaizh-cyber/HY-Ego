# L2特征DTO与强类型比较组

- 稳定身份：下表85个完整函数身份
- 边界类别：非服务函数（值对象逐字段比较）
- 模块：`海中鱼巣.领域.服务.L2特征结构`
- 位置：`海中鱼巣/领域/L2特征结构.数据.h:15-846`
- 绑定提交：`4241ab33a9ac66db57c369a68a3bdc10c2c08c09`
- 源码 blob：`dbd48d1e37899fe5d3af053bd1e77f606a0170cc`
- 知识状态：当前
- 实现状态：已实现并登记生产工程
- 验证状态：85/85定义与字段形状已静态互证；专项覆盖部分比较语境，正式BIZ消费`NOT_RUN`

## 逐字段相等（84）

下表每项完整函数形状均为`bool operator==(const <类型>&, const <类型>&)`。宏生成的八个身份按展开后的实际类型逐项计数，不把宏模板本身登记成可调用函数。

| DTO族 | 类型 |
| --- | --- |
| 关系和值类型登记 | `L2特征宿主关系类型身份`、`L2特征定义关系类型身份`、`L2特征关系类型登记请求`、`L2特征关系类型登记`、`L2特征关系类型登记结果`、`L2特征I64值类型身份`、`L2特征I64组值类型身份`、`L2特征U64组值类型身份`、`L2特征独立材料值类型身份`、`L2特征值类型登记请求`、`L2特征值类型登记`、`L2特征值类型登记结果` |
| 定义属性类型登记 | `L2特征定义I64属性类型身份`、`L2特征定义I64组属性类型身份`、`L2特征定义U64组属性类型身份`、`L2特征定义独立材料属性类型身份`、`L2特征定义属性类型登记请求`、`L2特征定义属性类型登记`、`L2特征定义属性类型登记结果` |
| 统一定义与固定属性 | `L2特征派生规则身份`、`L2特征派生规则提交项`、`L2特征直接来源提交项`、`L2特征直接来源事实`、`L2特征派生规则事实`、`L2统一特征定义事实`、`L2统一特征定义新增请求`、`L2统一特征定义写入结果`、`L2特征定义固定属性新增请求`、`L2特征定义固定属性换代请求`、`L2特征定义固定属性写入结果`、`L2特征定义身份来源读取请求`、`L2特征定义身份来源读取结果`、`L2统一特征定义完整读取请求`、`L2统一特征定义完整读取结果`、`L2当前统一特征定义组读取请求`、`L2当前统一特征定义组读取结果`、`L2统一特征定义退出请求`、`L2统一特征定义退出结果` |
| 实例和值 | `L2特征值事实`、`L2特征当前值读取请求`、`L2特征当前值读取结果`、`L2特征当前值新增请求`、`L2特征当前值换代请求`、`L2特征当前值写入结果`、`L2特征值历史读取请求`、`L2特征值历史读取结果`、`L2特征实例自有关系引用`、`L2特征实例旧引用退出投影`、`L2特征实例事实`、`L2特征实例引用替换请求`、`L2特征实例引用替换结果`、`L2特征当前值退出请求`、`L2特征当前值退出结果`、`L2特征实例身份来源读取请求`、`L2特征实例身份来源读取结果`、`L2特征实例退出请求`、`L2特征实例退出结果`、`L2特征实例新增请求`、`L2特征实例写入结果`、`L2特征实例完整读取请求`、`L2特征实例完整读取结果`、`L2按存在当前特征读取请求`、`L2按存在当前特征读取结果` |
| 比较登记与结果 | `L2特征比较归属关系类型身份`、`L2特征比较U64合同属性类型身份`、`L2特征比较I64合同属性类型身份`、`L2特征比较注册身份`、`L2特征比较结构类型登记请求`、`L2特征比较结构类型登记`、`L2特征比较结构类型登记结果`、`L2特征比较I64合同材料`、`L2特征比较注册事实`、`L2特征比较注册新增请求`、`L2特征比较注册替换请求`、`L2特征比较注册退出请求`、`L2当前特征比较注册读取请求`、`L2历史特征比较注册读取请求`、`L2特征比较注册写入结果`、`L2特征比较注册读取结果`、`L2特征比较输入材料`、`L2特征比较请求`、`L2特征比较输入版本回执`、`L2特征I64差异材料`、`L2特征比较结果` |

## 三向比较（1）

- `auto operator<=>(const L2特征派生规则身份&, const L2特征派生规则身份&)`（`L2特征结构.数据.h:202`）：只按稳定编码形成确定排序，供规则身份排序和确定投影使用。

## 用途、事实与合法调用语境

这些函数只比较调用方已有的值对象，不访问L1、owner、锁或服务，不发布事实。相等适用于请求重放、首次结果互证、读回比较和验证断言；三向比较只提供稳定编码顺序。

## 明确非职责

逐字段相等或排序不证明身份来源、请求有效、事实当前、比较合同有效、业务语义等价、目标满足或结果成功。以上结论必须经守卫、成功谓词和正式服务读回取得。

## 依据与证据

- `规范/4230_子规范_L2六类纯结构统一合同.md`
- `规范/4130_子规范_特征值三态比较底层逻辑_20260720.md`
- 绑定数据头中的76个显式DTO相等定义、两处宏的八个展开身份及一个三向比较定义
