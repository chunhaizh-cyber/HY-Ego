# L2特征内部定义固定属性与投影组

- 稳定身份：21个
- 边界类别：20个内部服务函数、1个服务装配函数
- 模块内部命名空间：`海中鱼巣::L2特征结构内部`
- 位置：`海中鱼巣/领域/服务.L2特征结构.ixx:154-620`
- 绑定提交：`614e4a32368a73ad81c274479fff4b0b12805fb1`
- 源码 blob：`f0b8407c7c6da68858bdc9c8abe2eb9206c2f9b4`
- 知识状态：当前
- 实现状态：已实现；不是对外ABI
- 验证状态：21/21静态互证；F1与统一定义专项覆盖主要调用链；许可拒绝映射经双配置编译与静态闭包验证（2026-08-17增量复核）

| 行 | 函数 | 用途 |
| ---: | --- | --- |
| 154 | `初始化特征定义派生结构(...)` | 构造期在定义owner登记阶次、规则见证和直接来源三种私有类型；服务装配函数 |
| 237 | `读取特征事实代次(...)` | 将L1中性当前事实代次投影为owner读取状态；中性许可拒绝映射为owner许可拒绝（纯值转换） |
| 257 | `映射特征定义幂等键(...)` | 纯值映射L2幂等身份到L1写入身份 |
| 262 | `映射特征定义写入状态(...)` | 完整映射L1写状态到L2结构状态 |
| 281 | `映射特征定义一致读取失败状态(...)` | 映射一致当前读取失败 |
| 297 | `映射特征定义当前项目状态(...)` | 映射当前投影单项状态 |
| 311 | `映射特征定义历史读取状态(...)` | 映射历史读取状态 |
| 329 | `特征定义预检结果::成功() const noexcept` | 只判断内部定义预检是否已读取 |
| 333 | `映射特征定义历史属性值组失败状态(...)` | 映射历史属性值组失败 |
| 348 | `转换为L1材料(...)` | 将L2原始值材料值式转换为L1材料 |
| 358 | `转换为L2材料(...)` | 将L1材料值式转换回L2材料 |
| 368 | `形成截止属性事实(...)` | 从L1值形成同截止L2属性事实 |
| 379 | `形成截止特征定义节点骨架(...)` | 形成定义身份、生命周期和空派生/属性骨架 |
| 405 | `解释特征定义一致读取顶层(...)` | 互证一致读取顶层合同和截止 |
| 430 | `查找唯一特征定义节点项(...)` | 在读取结果中定位唯一目标节点 |
| 442 | `检查特征定义当前代次(...)` | 当前代次预检定义存在、owner和形状 |
| 469 | `形成完整特征定义读取失败(...)` | 形成空载荷结构化读取失败 |
| 477 | `形成特征定义固定属性写入失败(...)` | 形成空载荷结构化写失败 |
| 484 | `检查特征定义固定属性前置(...)` | 校验定义、属性类型、旧值和截止 |
| 533 | `查找唯一特征定义固定属性编码映射(...)` | 定位提交返回的唯一新值编码 |
| 542 | `提交并读回特征定义固定属性写集(...)` | 恰一次提交并以正式读回收敛新/旧属性 |

以上`...`以绑定文件对应行的完整参数和限定组成稳定身份。它们只服务公开定义CRUD，外部模块不得直接调用、复制或形成第二投影路径。

## 完整签名清单

```text
154 特征定义派生结构定位 初始化特征定义派生结构(const L1事实基座服务&, L1所有者范围写端口&, std::uint64_t)
237 特征事实代次读取结果 读取特征事实代次(const L1事实基座服务&) noexcept
257 L1所有者范围写入幂等身份 映射特征定义幂等键(L2结构幂等身份) noexcept
262 L2结构状态 映射特征定义写入状态(L1所有者范围写入状态) noexcept
281 L2结构状态 映射特征定义一致读取失败状态(L1所有者范围一致当前读取状态) noexcept
297 L2结构状态 映射特征定义当前项目状态(L1所有者范围一致当前读取项目状态) noexcept
311 L2结构状态 映射特征定义历史读取状态(L1所有者范围读取状态) noexcept
329 bool 特征定义预检结果::成功() const noexcept
333 L2结构状态 映射特征定义历史属性值组失败状态(L1所有者范围读取状态) noexcept
348 L1所有者范围原始值材料 转换为L1材料(const L2原始值材料&)
358 L2原始值材料 转换为L2材料(const L1所有者范围原始值材料&)
368 std::optional<L2属性事实> 形成截止属性事实(L1所有者范围值事实, std::uint64_t)
379 std::optional<L2统一特征定义事实> 形成截止特征定义节点骨架(L1所有者范围节点事实, std::uint64_t) noexcept
405 特征定义预检结果 解释特征定义一致读取顶层(const L1所有者范围一致当前读取结果&, std::uint64_t) noexcept
430 const L1所有者范围一致节点读取结果项* 查找唯一特征定义节点项(const L1所有者范围一致当前读取结果&, 稳定编码) noexcept
442 特征定义预检结果 检查特征定义当前代次(const L1事实基座服务&, 稳定编码, std::uint64_t)
469 L2统一特征定义完整读取结果 形成完整特征定义读取失败(const L2统一特征定义完整读取请求&, L2结构状态, std::uint64_t = 0) noexcept
477 L2特征定义固定属性写入结果 形成特征定义固定属性写入失败(L2结构状态, std::uint64_t = 0) noexcept
484 特征定义预检结果 检查特征定义固定属性前置(const L1事实基座服务&, L2特征定义身份, 稳定编码, std::optional<稳定编码>, std::uint64_t)
533 std::optional<稳定编码> 查找唯一特征定义固定属性编码映射(const L1所有者范围写入结果&) noexcept
542 L2特征定义固定属性写入结果 提交并读回特征定义固定属性写集(const L1事实基座服务&, L1所有者范围写端口&, const L1所有者范围写集请求&, L2特征定义身份, const L2属性提交项&, std::optional<稳定编码>)
```

## 依据与证据

- 4230共同事务、幂等、读取和特征结构边界
- F1与统一特征定义施工检查记录
- `施工记录/20260817_DATA-L2-L1-PERMISSION-REJECTION-PROPAGATION_L1许可拒绝到六入口显式传播施工记录_v0.1.md`与`验证记录/20260817_DATA-L2-L1-PERMISSION-REJECTION-PROPAGATION_六入口并发传播检查记录_v0.1.md`（提交614e4a32增量复核）
