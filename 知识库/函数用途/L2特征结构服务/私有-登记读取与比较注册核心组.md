# L2特征私有登记读取与比较注册核心组

- 稳定身份：10个
- 边界类别：8个内部服务函数、2个服务装配函数
- 类：`L2特征结构服务`
- 位置：`海中鱼巣/领域/服务.L2特征结构.ixx:5117-5837`
- 绑定提交：`4241ab33a9ac66db57c369a68a3bdc10c2c08c09`
- 源码 blob：`72fb00b7fee9fdb034f036e3c3ee8228480ca714`
- 知识状态：当前
- 实现状态：已实现；`private`且不是对外ABI
- 验证状态：10/10静态互证；相关公开入口按各专项分账

| 行 | 完整身份起点 | 边界与用途 |
| ---: | --- | --- |
| 5117 | `static L1所有者范围写端口&& 验证并移动定义交付(...)` | 服务装配；只接受绑定同一L1的有效定义端口 |
| 5125 | `static L1所有者范围写端口&& 验证并移动实例交付(...)` | 服务装配；只接受绑定同一L1的有效实例端口 |
| 5134 | `L2结构结果头 检查特征比较合同上下文(...) const` | 当前同代核对单位、维度和分量角色节点 |
| 5183 | `特征比较首次写集读取值 读取特征比较首次写集(...) const` | 读回已绑定比较注册幂等请求的首次写集 |
| 5209 | `L2特征比较注册写入结果 提交并读回特征比较注册写集(...)` | 恰一次提交并收敛新旧注册完整事实 |
| 5280 | `L2特征比较注册读取结果 按键读取当前特征比较注册(...) const` | 按定义+用途定位唯一当前注册 |
| 5423 | `L2特征比较注册读取结果 按身份读取历史特征比较注册(...) const` | 按注册身份与截止重建历史注册 |
| 5593 | `L2特征比较结构类型登记结果 按定位读取当前特征比较结构类型登记(...) const` | 以缓存定位读回并校验四类比较结构类型 |
| 5664 | `L2特征值类型登记结果 按定位读取当前特征值类型登记(...) const` | 以缓存定位读回四类值类型 |
| 5751 | `L2特征关系类型登记结果 按定位读取当前特征关系类型登记(...) const` | 以缓存定位读回宿主/定义关系类型 |

## 完整签名清单

```text
5117 static L1所有者范围写端口&& 验证并移动定义交付(const L1事实基座服务&, L2特征定义所有者交付&)
5125 static L1所有者范围写端口&& 验证并移动实例交付(const L1事实基座服务&, L2特征实例所有者交付&)
5134 L2结构结果头 检查特征比较合同上下文(const L2结构请求头&, const L2特征比较I64合同材料&) const
5183 特征比较首次写集读取值 读取特征比较首次写集(L2结构幂等身份) const
5209 L2特征比较注册写入结果 提交并读回特征比较注册写集(const L1所有者范围写集请求&, std::optional<L2特征比较注册事实>, bool = true)
5280 L2特征比较注册读取结果 按键读取当前特征比较注册(const L2当前特征比较注册读取请求&, bool* = nullptr) const
5423 L2特征比较注册读取结果 按身份读取历史特征比较注册(const L2历史特征比较注册读取请求&) const
5593 L2特征比较结构类型登记结果 按定位读取当前特征比较结构类型登记(const L2特征比较结构类型登记&, std::optional<std::uint64_t>) const
5664 L2特征值类型登记结果 按定位读取当前特征值类型登记(const L2特征值类型登记&, std::optional<std::uint64_t>) const
5751 L2特征关系类型登记结果 按定位读取当前特征关系类型登记(const L2特征关系类型登记&, std::optional<std::uint64_t>) const
```

## 明确非职责

这些函数只能由同一服务公开入口和构造路径使用。任何外部消费者、其它L2服务或测试驱动不得取得私有端口、定位、写集或按键读取核心；不得复制形成第二比较注册服务。

## 依据与证据

- 4230双owner、单向依赖、共同事务和读取边界
- F-ID、F2/F3和I64比较施工检查记录
