# L2特征实例与当前值CRUD组

- 稳定身份：10个对外服务函数
- 模块：`海中鱼巣.领域.服务.L2特征结构`
- 位置：`海中鱼巣/领域/服务.L2特征结构.ixx:3713-4997`
- 绑定提交：`4241ab33a9ac66db57c369a68a3bdc10c2c08c09`
- 源码 blob：`72fb00b7fee9fdb034f036e3c3ee8228480ca714`
- 知识状态：当前
- 实现状态：已实现并登记生产工程
- 验证状态：F2/F3、引用替换21/21和当前值退出9/9有双配置专项；当前值具名读取运行`NOT_RUN`

| 完整签名 | 用途 |
| --- | --- |
| `L2特征实例写入结果 新增特征实例(const L2特征实例新增请求&)` | 在实例owner原子建立实例、族归属、唯一宿主和唯一定义关系 |
| `L2特征实例完整读取结果 读取完整特征实例(const L2特征实例完整读取请求&) const` | 当前或历史同截止重建实例、宿主、定义、可选当前值 |
| `L2特征当前值读取结果 读取特征当前值(const L2特征当前值读取请求&) const` | 委托完整实例读取并把未设置与已设置分账 |
| `L2特征实例引用替换结果 替换特征实例引用(const L2特征实例引用替换请求&)` | 同代替换宿主、定义或二者，当前值保持不变 |
| `L2按存在当前特征读取结果 按存在读取全部当前特征(const L2按存在当前特征读取请求&) const` | 经存在来源证明后返回宿主全部当前实例并稳定排序 |
| `L2特征当前值写入结果 新增特征当前值(const L2特征当前值新增请求&)` | 为尚无当前值的实例原子新增类型化值和当前属性槽 |
| `L2特征当前值写入结果 换代特征当前值(const L2特征当前值换代请求&)` | 同代退出具名旧当前值并建立新当前值 |
| `L2特征当前值退出结果 退出特征当前值(const L2特征当前值退出请求&)` | 只退出具名当前值及属性槽，实例、宿主和定义关系保持当前 |
| `L2特征值历史读取结果 读取特征历史值(const L2特征值历史读取请求&) const` | 按实例、值编码和历史截止重建已退出或当时有效值 |
| `L2特征实例退出结果 退出特征实例(const L2特征实例退出请求&)` | 闭合当前值及全部自有关系后同代退出实例和族归属 |

## 当前生产调用点

概念和状态服务实际静态消费完整实例；概念服务消费当前/历史值。其它写入口与按存在组读取在本基线没有模块外生产调用点。

## 明确非职责

宿主和定义引用只证明结构关系，不授予业务适用性或值域解释。当前值允许为空；服务不建立空值/墓碑值，不解释单位或业务意义，也不按名称创建专用CRUD。枚举值生产者合法性仍由DATA之外的合法消费者/规则证据裁决。

## 依据与证据

- `规范/4110_子规范_特征节点实现与字段边界_20260720.md`
- `规范/4140_子规范_枚举型实例特征值合法来源_20260720.md`
- `规范/4230_子规范_L2六类纯结构统一合同.md`第4.3节
- F2/F3、实例引用替换、当前值读取和当前值退出施工检查记录
