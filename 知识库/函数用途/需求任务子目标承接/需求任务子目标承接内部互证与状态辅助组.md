# 需求任务子目标承接内部互证与状态辅助组

- 稳定身份：R1507、R1508；另含6个无独立图的内部 helper。
- 模块：`海中鱼巣.领域.服务.需求任务子目标承接`内部命名空间。
- 位置：`海中鱼巣/领域/服务.需求任务子目标承接.ixx:94-310`。
- 绑定提交：`3a58b690f3b76a73b99ec02fcbdbaed7f90a2607`。
- 源码 blob：`d03f149888dca1ad51c2c841a28930f29aeb083d`。
- 知识状态：当前（仅限该已发布 blob）。
- 边界类别：内部守卫、映射、材料转换和只读互证；均不独立形成可复用业务能力。

| 行 | 函数 | 只负责 |
| ---: | --- | --- |
| 110 | `请求有效(...)` | 校验合同版本、请求头、非零G/记录/顺序和两个不同的合法幂等身份 |
| 120 | `映射状态(...)` | 穷尽映射结构入口拒绝、许可拒绝、漂移、幂等/引用冲突和资源失败；其它异常形状归内部不一致 |
| 136 | `失败(...)` | 形成无业务载荷的具名失败结果 |
| 143 | `目标相同(...)` | 比较目标宿主、目标特征和目标状态合同三元组 |
| 150 | `读取需求侧材料组(...)` | R1508；按同 G 读取并互证子、父、父关系、成员、列表项和来源端点 |
| 202 | `提取写结果(...)` | 从三类需求 owner 成功结果收敛为统一需求侧材料；不改变事实 |
| 210 | `形成等待(...)` | 保存已发布需求材料和原任务绑定请求；不执行隐藏重试 |
| 228 | `最终读回(...)` | R1507；双读任务记录和需求来源，必要时最多一次整组最新 G 重读，再完成六份需求材料互证 |

## 内部错误边界

- 成功头与载荷形状不一致、来源记录与任务记录端点不一致、父子/成员/列表项不一致、多个候选复义或最终共同截止不一致均归追根因对象。
- 一次重读后仍漂移时返回具名非成功，不循环等待。
- helper只使用调用栈内局部值和已发布事实，不建立BIZ私有账、缓存或永久唤醒记录。

## 验证边界

R1507/R1508已有配对现状图和逐行映射；其它helper由根函数图和源码静态覆盖。V13、V16、V17的资源/冲突/并发路径仍是源码守恒`NOT_RUN`，不能因helper分支存在而写成运行验证通过。
