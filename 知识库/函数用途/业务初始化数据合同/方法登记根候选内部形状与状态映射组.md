# 方法登记根候选内部形状与状态映射组

- 稳定身份：16个具名函数定义；`零代次根组读取调用形状完整`按两个不同参数类型区分重载
- 边界类别：非服务函数（候选阶段19请求、调用证据与失败状态守卫）
- 模块：候选模块`海中鱼巣.业务.提供者.方法登记根生产初始化`
- 位置：`海中鱼巣/业务/提供者.方法登记根生产初始化.ixx:22-189`
- 绑定提交：`058ddc6f50a983aef99a007fe61f0b93b7152f84`
- 源码 blob：`9c97faa8aa5b2911570972f814e3861a144f79ec`
- 知识状态：当前候选源码已核；非当前生产能力
- 实现状态：候选模块未登记工程、无生产import
- 验证状态：16/16定义静态互证；构建、专项、并发与正式阶段19均`NOT_RUN`

## 完整身份

| 完整签名 | 只负责 |
| --- | --- |
| `bool 请求有效(const 方法登记根生产初始化请求&) noexcept` | 版本和非零根幂等身份 |
| `const L2方法登记根事实* 查找登记根(const L2当前方法登记根组读取结果&) noexcept` | 查找唯一固定角色根 |
| `bool 根组形状完整(const L2当前方法登记根组读取结果&) noexcept` | 已读取、非零截止和0—1完整根 |
| `bool 零代次根组读取调用形状完整(const 方法登记根组读取调用证据&, std::uint64_t) noexcept` | 初始零守卫组读与截止不倒退 |
| `bool 零代次根组读取调用形状完整(const 方法登记根漂移重读证据&, std::uint64_t) noexcept` | 漂移零守卫重读与截止不倒退 |
| `bool 非零根组读取调用形状完整(const 方法登记根组读取调用证据&) noexcept` | 最终非零守卫组读精确同截止 |
| `bool 建立调用请求匹配(const 方法登记根建立调用证据&, L2结构幂等身份) noexcept` | 建立请求角色、截止和幂等匹配 |
| `bool 根写入形状完整(const 方法登记根建立调用证据&, L2结构幂等身份) noexcept` | 写成功、截止推进、变更回显和根事实完整 |
| `bool 是可重组漂移(const 方法登记根建立调用证据&, L2结构幂等身份) noexcept` | 明确零变化、空载荷漂移 |
| `bool 单根读取调用形状完整(const 方法登记根单根读取调用证据&, L2结构幂等身份) noexcept` | 非零单根读和首次身份完整 |
| `bool 组读取非成功形状完整(const L2当前方法登记根组读取结果&) noexcept` | 非成功组读空载荷 |
| `bool 单根读取非成功形状完整(const L2当前方法登记根读取结果&) noexcept` | 非成功单根读空载荷 |
| `bool 建立非成功形状完整(const L2方法登记根写入结果&) noexcept` | 非成功写空载荷 |
| `方法登记根生产初始化状态 映射组读取失败(const L2当前方法登记根组读取结果&, 方法登记根生产初始化状态, bool) noexcept` | 映射组读竞争、资源和普通失败 |
| `方法登记根生产初始化状态 映射单根读取失败(const L2当前方法登记根读取结果&) noexcept` | 映射单根竞争、资源和正式读回失败 |
| `方法登记根生产初始化状态 映射建立失败(const 方法登记根建立调用证据&) noexcept` | 映射建立拒绝、资源或内部不一致 |

## 边界

全部函数只读候选DTO并返回布尔值、借用指针或BIZ状态；零锁、零I/O、零事实写。它们不取得M1、不建立方法根、不决定方法业务，也不能把预冻结ABI变为已实现接口。当前只有同一未接线候选模块内部调用。

正式依据为8120阶段19；候选设计与计划仍待激活。任何最终M1字段、状态或调用形状变化都要求重新制卡，不能沿用当前候选结论施工。
