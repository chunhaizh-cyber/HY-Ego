# 四本体根候选内部请求与读取形状组

- 稳定身份：9个具名函数定义；模板按源码定义计一个身份
- 边界类别：非服务函数（候选阶段18请求、根组与单根读取证据守卫）
- 模块：候选模块`海中鱼巣.业务.提供者.四本体根生产初始化`
- 位置：`海中鱼巣/业务/初始化.四本体根.inl:3-108`
- 绑定提交：`058ddc6f50a983aef99a007fe61f0b93b7152f84`
- 源码 blob：`668c7d78fc423260077ee9b4763253642d5e7501`
- 知识状态：当前候选源码已核；非当前生产能力
- 实现状态：被候选provider包含；provider未工程登记、无生产import
- 验证状态：9/9定义静态互证；构建、专项、阶段18和真实读回均`NOT_RUN`

## 完整身份与用途

| 完整签名 | 只负责 |
| --- | --- |
| `bool 请求有效(const 四本体根生产初始化请求&) noexcept` | 版本、四个非零且互异幂等身份 |
| `L2结构幂等身份 取得角色幂等身份(const 四本体根生产初始化请求&, L2概念本体根角色) noexcept` | 四固定角色到请求身份映射 |
| `bool 根组形状完整(const L2当前概念本体根组读取结果&) noexcept` | 已读取、非零截止、至多四根、角色升序和身份互异 |
| `const L2概念本体根事实* 查找角色根(const L2当前概念本体根组读取结果&, L2概念本体根角色) noexcept` | 在线性根组中按角色返回借用指针 |
| `bool 单根读取形状完整(const L2当前概念本体根读取结果&, L2概念本体根角色, L2结构幂等身份, std::uint64_t) noexcept` | 单根角色、首次幂等和截止完整 |
| `template<class 根组读取证据> bool 零代次根组读取调用形状完整(const 根组读取证据&, std::uint64_t) noexcept` | 零守卫组读和截止不倒退 |
| `bool 非零根组读取调用形状完整(const 四本体根组读取调用证据&) noexcept` | 非零守卫组读精确同截止 |
| `bool 单根读取调用形状完整(const 四本体根单根读取调用证据&, L2概念本体根角色, L2结构幂等身份) noexcept` | 单根请求与结果匹配 |
| `bool 建立调用请求匹配(const 四本体根建立调用证据&, L2概念本体根角色, L2结构幂等身份) noexcept` | 建立请求版本、截止、角色和幂等匹配 |

## 边界与合法语境

本组不调用C1、不读写事实、不选择业务根，也不发布材料；它只判断候选编排已经持有的值式请求和调用证据。返回真不证明模块编译、provider唯一、阶段17成功或阶段18接线。

当前无合法生产调用者。正式依据为2220与8120阶段18；候选设计/计划仍待激活。后续只有最终C1 ABI、工程登记、普通应用唯一provider和正式入口全部发布后才能重新复核并用于生产。
