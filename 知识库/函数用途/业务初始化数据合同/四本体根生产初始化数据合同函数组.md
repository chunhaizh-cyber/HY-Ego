# 四本体根生产初始化数据合同函数组

- 稳定身份：下表7个完整签名
- 边界类别：非服务函数（候选BIZ DTO值比较与结果证据谓词）
- 函数种类和可见性：6个结构体内`friend`默认相等、1个公开结果const成员声明/模块定义
- 模块：候选模块`海中鱼巣.业务.提供者.四本体根生产初始化`
- 声明与内联位置：`海中鱼巣/业务/四本体根生产初始化.数据.h:16-110`
- 结果谓词定义位置：`海中鱼巣/业务/提供者.四本体根生产初始化.ixx:27-29`
- 绑定提交：`ea8d034748c69f282a48377a140cf4196ee2022f`
- 源码 blob：`1af4871a08ad122f558d29c838a096137d999e3d`
- 实现源码 blob：`f8644445280da160bd27ae8b0ddc361640abb135`
- 知识状态：当前候选源码已核；非当前生产能力
- 实现状态：候选源码存在；未登记`海中鱼巣.vcxproj`、无生产import、对应计划未登记计划索引
- 验证状态：7/7身份和结果证据检查静态互证；构建、专项、正式阶段18与真实读回均`NOT_RUN`

## 完整身份与用途

| 位置 | 完整签名 | 当前用途 |
| --- | --- | --- |
| 22-23 | `bool operator==(const 四本体根生产初始化请求&, const 四本体根生产初始化请求&)` | 逐字段比较合同版本和四个固定幂等身份 |
| 56-57 | `bool operator==(const 四本体根发布材料&, const 四本体根发布材料&)` | 比较选择、四个本体根事实和最终截止 |
| 65-66 | `bool operator==(const 四本体根漂移重读证据&, const 四本体根漂移重读证据&)` | 比较角色、调用前截止、请求和重读结果 |
| 72-73 | `bool operator==(const 四本体根组读取调用证据&, const 四本体根组读取调用证据&)` | 比较根组请求与结果 |
| 79-80 | `bool operator==(const 四本体根单根读取调用证据&, const 四本体根单根读取调用证据&)` | 比较单根请求与结果 |
| 86-87 | `bool operator==(const 四本体根建立调用证据&, const 四本体根建立调用证据&)` | 比较建立请求与写入结果 |
| 声明110；定义27-29 | `bool 四本体根生产初始化结果::成功() const noexcept` | 调用候选内部`结果证据完整`，互证四角色建立/漂移/读回/最终恰四根证据 |

## 用途

六个默认相等为候选编排中的请求锁定、原请求重放和证据逐字段互证提供纯值操作。结果谓词要求来源、当前根组预读、四个角色各自单根正式读回、可选建立/漂移重组路径、最终恰四根组读回和同一最终事实截止全部一致。

## 明确非职责

- 相等运算不判断请求合法、事实当前、根角色唯一或服务调用成功。
- 结果谓词不建立本体根、不调用C1、不取得普通应用服务，也不把候选模块接入生产入口。
- 代码文本、固定幂等身份和`成功()`定义不证明阶段18已实现、构建、运行或验收。
- 本合同不建立语言、词条、普通概念、场景概念根、二次特征或因果候选。

## 输入、输出和错误

相等函数接收同类型const引用并返回逐字段相等布尔值。结果谓词只读完整结果，任一必需optional缺失、来源/状态不匹配、根数量非4、单根与最终根组不一致、漂移重读数量/截止不一致或建立证据形状错误时返回`false`；无错误文本或部分成功载荷。

## 事实、副作用、事务和生命周期

全部函数只读值式DTO，零I/O、零锁、零事实写、零代次推进。默认比较可能递归调用嵌套DTO比较；结果谓词遍历已持有向量和optional，但不发布材料。DTO由调用方拥有，不保存引用。

## 合法调用语境

当前仅允许作为尚未进入生产工程的阶段18候选设计/源码证据。正式消费者必须等待计划进入正式索引、模块工程登记、普通应用同实例C1装配、provider调用和权威读回全部发布后，再按届时源码重新复核；不得在新代码中绕过这些门禁直接import候选模块。

## 当前生产调用点

正式HEAD的`海中鱼巣.vcxproj`不包含四本体根数据头、provider或`.inl`实现；项目源码没有import该provider模块，也没有生产构造/调用点。当前只有候选provider内部调用结果证据检查。

## 依据与证据

- `规范/8120_子规范_程序入口启动模式与运行宿主边界_20260726.md:78-99`
- `计划/过期计划/20260814_CONCEPT-ONTOLOGY-ROOTS-PRODUCTION-INIT_四个语言无关概念本体根生产初始化代码实施切片_v0.1.md`
- `海中鱼巣/业务/四本体根生产初始化.数据.h:5-110`
- `海中鱼巣/业务/初始化.四本体根.inl:442-571`
- `海中鱼巣/业务/提供者.四本体根生产初始化.ixx:25-70`
- 正式HEAD工程登记、import和计划索引只读扫描

## 已知缺口

对应计划文件存在但未登记`计划/计划索引.md`，候选模块未进入工程和普通应用，且没有施工/验证记录。计划文本和候选源码可能在正式实施前漂移；本卡只能作为候选代码知识，不能用于当前能力复用或解除阶段18。
