# L2因果结构服务内部函数索引

- 绑定提交：`614e4a32368a73ad81c274479fff4b0b12805fb1`
- 绑定源码：`海中鱼巣/领域/服务.L2因果结构.ixx`
- 源码 blob：`2345568a5f6025c5ee7f570e7ba647b37dfa85e9`
- 知识状态：当前枚举；37个内部/私有具名身份均已逐项制卡

## 数量与边界

| 分组 | 位置 | 数量 | 类别 |
| --- | --- | ---: | --- |
| [内部-状态映射失败形成与写入边界组.md](内部-状态映射失败形成与写入边界组.md) | 80-306 | 24 | 内部服务函数 |
| [内部-当前投影查找与身份来源初始化组.md](内部-当前投影查找与身份来源初始化组.md) | 311-386 | 8 | 7个内部服务函数、1个服务装配函数 |
| [私有-交付验证提交读回与完整查询核心组.md](私有-交付验证提交读回与完整查询核心组.md) | 1292-1622 | 5 | 4个内部服务函数、1个服务装配函数 |
| 合计 | — | 37 | 35个内部服务函数、2个服务装配函数 |

计数只统计具名函数定义，不统计局部lambda、内部结构体隐式特殊成员、公开函数、声明或显式删除身份。lambda只属于`读取因果身份来源`、`退出因果`、`读取历史完整`和`按端点读取`等父函数内部指令。

## 使用边界

35个内部服务函数只支撑同模块公开因果结构操作，不是稳定消费ABI；其它DATA服务、BIZ、普通应用和验证代码不得直接依赖这些名字或复制其owner、登记、状态映射、写集、历史夹持和读回算法形成第二实现。2个装配函数只在唯一服务构造期间收窄交付和初始化族来源。

结合公开/生命周期卡，`服务.L2因果结构.ixx`的60个显式函数身份已全量覆盖：13个对外服务、1个服务取得、4个服务装配、7个显式特殊成员、35个内部服务函数。全量制卡不证明真实业务运行、恢复或跨进程闭环。
