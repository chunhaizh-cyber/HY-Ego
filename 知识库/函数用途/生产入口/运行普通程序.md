# `运行普通程序`

- 完整签名：`海中鱼巣::程序运行结果 海中鱼巣::运行普通程序(海中鱼巣::启动模式 模式)`
- 边界类别：非服务函数（普通模式业务编排与装配）
- 位置：`海中鱼巣/启动.应用程序.ixx:31-188`
- 模块：`海中鱼巣.启动.应用程序`
- 可见性：模块内部函数
- 绑定提交：`195964fcbf9436f1647b576e60234462d5443e53`
- 源码 blob：`7273c7db40f2de721a2def64cec57575c8e37a3c`
- 知识状态：当前（仅限 195964fc 基线）
- 验证状态：治理专项真实工作线程/管理线程/结果上行/自我线程链路与双配置构建通过；普通程序长时宿主、外部真实工作结果来源、路径验证和完整生命周期迁移 `NOT_RUN`

## 用途

编排普通模式下的配置形成、普通应用上下文、I1/I2/SELF-FORM、四本体根、方法登记根、同一应用上下文的运行期只读查询组合器、自我线程和任务结果生产运行包，并在退出时按逆序停派发、排空结果上行、连接线程组。

## 当前装配顺序增量

1. 校验启动模式并形成普通应用生产配置。
2. 构造普通应用上下文，完成既有服务 owner / getter 取得。
3. 完成系统世界树根初始化发布、自我世界树根消费发布、真实自我形成、四本体根生产初始化和方法登记根生产初始化。
4. 在同一上下文中调用八服务 `装配运行期只读查询组合器(...)`（`启动.应用程序.ixx:94-102`），方法服务来自 `取得L2方法结构聚合服务().取得L2方法结构服务()`；不新建第二方法服务。
5. 构造自我线程、创建并停在治理运行门，随后开放治理运行门；再构造并启动唯一任务结果生产运行包。
6. 进入既有宿主运行骨架；退出时先停新派发、排空工作线程与结果上行，再允许自我线程析构。

## 当前边界

该函数只负责普通程序生命周期编排和 owner / 服务装配，不执行任务方法、不形成任务工作结果、不裁决任务目标完成、需求满足、结算、价值或学习。八服务组合器接线只证明依赖方向和静态装配；治理专项没有把 `运行普通程序` 本身的长时宿主路径写成通过。

## 副作用和生命周期

配置阶段可能建立固定不可变材料目录；成功装配形成 L1 运行包、L2 服务、自我线程和任务结果生产运行包。任务结果运行包晚于自我线程构造，离开作用域时先停止派发并排空结果链，再析构自我线程。治理裁决只冻结进程内槽，不写任务生命周期。

## 依据与证据

- 施工记录：`施工记录/20260821_RUNTIME-GOVERNANCE-TASK-RESULT-COMPLETION-ADJUDICATION_施工记录.md` §3.2、§4.5
- 验证记录：`验证记录/20260821_RUNTIME-GOVERNANCE-TASK-RESULT-COMPLETION-ADJUDICATION_验证记录.md` §2-§5
- 治理装配转发：`海中鱼巣/领域/服务.L2治理函数骨架.ixx:50-86,325-340`

## 已知缺口

普通模式真实启动、外部真实任务工作结果来源、目标裁决资源故障注入、路径验证、任务生命周期迁移、任务完成、需求结算和跨进程恢复均为 `NOT_RUN`；不能把治理专项成功写成普通应用全链路完成。
