# `接收程序停止信号`

- 完整签名：`void 海中鱼巣::接收程序停止信号(int)`
- 位置：`海中鱼巣/启动.程序运行宿主.ixx:89`
- 模块：`海中鱼巣.启动.程序运行宿主`
- 可见性：导出声明对应的处理函数
- 绑定提交：`12062f619b86aaa9a3ad585551f3bdef398326a8`
- 源码 blob：`a583f58fc8331a12c25e1d1f7eedce69a44832d5`
- 知识状态：当前
- 验证状态：静态源码已核；真实进程信号 `NOT_RUN`

## 用途

作为进程信号处理器，在存在当前停止信号租约时，仅把其`volatile std::sig_atomic_t 停止请求`置为1。

## 明确非职责

不解释信号编号，不分配内存，不加锁，不写日志，不调用服务，不销毁对象，不直接结束进程。参数当前未参与分支。

## 合法调用语境

由`安装程序停止信号`登记给进程信号机制。普通代码不应把它作为通用停止API；正常停止观察由持有租约的宿主读取标志。
