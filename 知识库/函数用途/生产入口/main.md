# `int main(int 参数数量, char* 参数组[])`

- 稳定身份：完整签名 + `海中鱼巣/入口.cpp`
- 位置：`海中鱼巣/入口.cpp:8`
- 模块：全局程序入口
- 绑定提交：`12062f619b86aaa9a3ad585551f3bdef398326a8`
- 源码 blob：`870fa4500eee2e013e60103ddc5d1a0b24cf299b`
- 知识状态：当前
- 验证状态：静态源码与8120薄入口合同互证；构建和运行 `NOT_RUN`

## 用途

接收操作系统传入的原始参数，调用唯一启动解析函数；解析成功后调用唯一顶层程序函数，最后通过具名函数映射进程退出码。

## 明确非职责

不解释领域业务，不构造仓库或服务，不初始化系统，不安装信号，不运行窗口或驻留循环，不记录验收 PASS。

## 输入、输出和错误

- 输入：原始参数数量和指针组，所有权属于进程启动环境。
- 输出：解析拒绝映射为退出码2；准入后运行失败映射为1；成功完成映射为0。
- 自身不捕获异常；下游公开合同承担结构化结果或异常边界。

## 调用语境与副作用

- 调用：`解析并验证启动选项`、`运行海中鱼巣`、`映射进程退出码`。
- 副作用只来自被调顶层程序；入口自身只保存局部值。
- 合法调用者：操作系统/运行时启动入口，不作为普通业务函数复用。

## 依据与缺口

- 正式规范：8120第2节、第4节。
- 当前实现符合薄入口四步。
- 运行参数矩阵和实际退出码未在本知识批次执行。
