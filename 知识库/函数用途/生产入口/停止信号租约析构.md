# `停止信号租约::~停止信号租约()`

- 完整签名：`海中鱼巣::停止信号租约::~停止信号租约()`
- 边界类别：非服务函数（信号租约生命周期辅助）
- 位置：`海中鱼巣/启动.程序运行宿主.ixx:34`
- 模块：`海中鱼巣.启动.程序运行宿主`
- 可见性：公开特殊成员
- 绑定提交：`12062f619b86aaa9a3ad585551f3bdef398326a8`
- 源码 blob：`a583f58fc8331a12c25e1d1f7eedce69a44832d5`
- 知识状态：当前
- 验证状态：静态源码已核；真实恢复与失败处理 `NOT_RUN`

## 用途

结束停止信号租约时，按SIGBREAK、SIGTERM、SIGINT的反安装顺序恢复各自原处理器，并在本对象仍是模块当前租约时清空静态指针。

## 生命周期和副作用

- 只恢复曾经成功安装的信号。
- 调用保存的安装函数执行恢复，忽略恢复返回值。
- 对象不可复制、不可移动，保证当前租约地址在生命周期内稳定。

## 明确非职责

不等待宿主退出，不保证恢复一定成功，不记录恢复失败，不管理业务线程或其它进程资源。

## 已知缺口

析构函数调用的`std::function`理论上可能抛出，而析构没有显式异常隔离；本批没有通过故障安装器验证该边界。
