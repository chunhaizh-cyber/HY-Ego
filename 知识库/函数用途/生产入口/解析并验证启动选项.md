# `解析并验证启动选项`

- 完整签名：`海中鱼巣::启动选项解析结果 海中鱼巣::解析并验证启动选项(int 参数数量, char* const 参数组[]) noexcept`
- 位置：`海中鱼巣/启动.程序入口.ixx:12`
- 模块：`海中鱼巣.启动.程序入口`
- 可见性：导出函数
- 绑定提交：`12062f619b86aaa9a3ad585551f3bdef398326a8`
- 源码 blob：`86d1862f9fb81cb12fea34d7dc73e39e62a17424`
- 知识状态：当前代码已核；正式合同漂移待治理
- 验证状态：静态源码、DTO和调用点已核；运行参数矩阵 `NOT_RUN`

## 用途

把原始命令行参数转换为强类型 `启动选项解析结果`。无额外参数时选择普通控制面板；当前只接受一次 `--headless` 并选择无窗口常驻。

## 明确非职责

不启动程序、不写日志、不改变业务结构、不探测运行环境，也不把拒绝转换为进程退出码。

## 输入、输出和拒绝

- `参数数量 < 1`：`参数数量无效`。
- 参数数组为空：`参数数组为空`。
- 任一参数指针为空：`参数指针为空`并回显索引。
- 非 `--headless`：`未知参数`并回显索引。
- 第二个模式参数：重复字符串为`重复模式参数`，其它已知模式组合原设计分支为`模式组合冲突`。
- 成功：`已接受`及强类型模式；函数为 `noexcept`、零状态写入。

当前只有一个可识别的显式模式字符串，因此`模式组合冲突`分支实际上不可达；第二个`--headless`只能形成重复，其它字符串会先作为未知参数返回。

## 合法调用语境

当前唯一生产调用点是 `main`。调用者必须先检查 `成功()`，不得在拒绝结果上运行顶层程序。

## 正式漂移

8120第3节还规定`生产运行期`和`--runtime-context`。当前 `启动模式` DTO只有普通控制面板、无窗口常驻两值，本函数也会把`--runtime-context`拒绝为未知参数。因此当前代码不能宣称已经实现8120完整三模式合同。
