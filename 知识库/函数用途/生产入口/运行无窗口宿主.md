# `运行无窗口宿主`

- 完整签名：`海中鱼巣::程序运行结果 海中鱼巣::运行无窗口宿主(海中鱼巣::停止信号租约& 信号)`
- 边界类别：非服务函数（运行宿主循环）
- 位置：`海中鱼巣/启动.程序运行宿主.ixx:131`
- 模块：`海中鱼巣.启动.程序运行宿主`
- 可见性：导出函数
- 绑定提交：`12062f619b86aaa9a3ad585551f3bdef398326a8`
- 源码 blob：`a583f58fc8331a12c25e1d1f7eedce69a44832d5`
- 知识状态：当前函数存在；普通程序当前不可达
- 验证状态：静态源码已核；真实驻留、信号唤醒和时序 `NOT_RUN`

## 用途

轮询借入停止信号租约的`停止请求`标志；未请求停止时每100毫秒休眠，收到停止后返回无窗口常驻模式的`已完成`结果。

## 输入、输出和副作用

- 输入：调用方持有且在整个等待期间有效的可变停止信号租约。
- 输出：只在停止标志非零后返回成功结果；没有超时或失败结果。
- 副作用：阻塞当前线程并周期休眠；不修改业务结构。

## 明确非职责

不安装信号、不拥有租约、不运行窗口、不调度业务线程、不执行健康检查或恢复。

## 当前可达性

`运行普通程序`中调用本函数的代码位于无条件阶段17返回之后，因此当前普通生产路径不能到达。函数存在不能证明无窗口模式已经进入常驻宿主。
