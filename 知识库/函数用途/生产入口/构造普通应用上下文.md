# `构造普通应用上下文`

- 完整签名：`海中鱼巣::普通应用装配结果 海中鱼巣::构造普通应用上下文(const 海中鱼巣::普通应用配置& 配置)`
- 边界类别：服务装配函数
- 位置：`海中鱼巣/装配.普通应用.ixx:337`
- 模块：`海中鱼巣.装配.普通应用`
- 可见性：导出函数
- 绑定提交：`40a01afc450e296dcc65286d343218d4314454b4`
- 源码 blob：`377169094c789e42d1d802a36ff6f2d215e1bee0`
- 知识状态：当前
- 验证状态：静态源码、根工程双配置和生产外合法消费者真实装配/销毁已核；真实启动、异常注入和资源失败 `NOT_RUN`

## 用途

建立一个独占`普通应用上下文`：先建立唯一L1事实基座运行包，再依次建立不可变材料、语言、词条及既有世界/概念/方法owner范围与强类型交付，构造材料服务、语言服务、六类世界服务及世界/概念/方法聚合服务。

## 输入与结果

- 输入：带不可变材料存储合同和绝对受控根的`普通应用配置`；入口再次调用`有效()`。
- 成功：`已装配`及非空`unique_ptr<普通应用上下文>`。
- 非成功：39个装配状态中除`已装配`外的38个状态，精确区分配置、owner范围、交付、服务或聚合构造失败；最外层其它异常映射`构造失败`。

## 所有权、事务和生命周期

- 上下文独占持有L1运行包和全部服务对象，不可复制、不可移动。
- 服务借用的下层引用由成员声明/析构顺序维持；聚合只引用同一上下文已有服务。
- 每个owner建立是独立步骤；本函数没有把全部装配包装成跨owner单事务。
- 任一步失败依靠局部对象析构回收已经形成的内存对象。

## 明确非职责

不建立系统世界树根、自我、概念本体根或方法登记根，不启动线程或宿主。材料、语言和词条服务已装配并经生产外消费者读回，但这不证明真实BIZ消费或完整数据需求清单闭环。

## 合法调用语境

当前生产调用点为`运行普通程序`。调用者必须检查`普通应用装配结果::成功()`后再解引用上下文。

依据：`施工记录/20260816_DATA-LANGUAGE-ORDINARY-APP-ASSEMBLY-READBACK_语言材料普通应用装配与首次权威读回施工记录_v0.1.md`。
