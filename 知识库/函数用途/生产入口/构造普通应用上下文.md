# `构造普通应用上下文`

- 完整签名：`海中鱼巣::普通应用装配结果 海中鱼巣::构造普通应用上下文(const 海中鱼巣::普通应用配置& 配置)`
- 位置：`海中鱼巣/装配.普通应用.ixx:230`
- 模块：`海中鱼巣.装配.普通应用`
- 可见性：导出函数
- 绑定提交：`12062f619b86aaa9a3ad585551f3bdef398326a8`
- 源码 blob：`2e7827a8721138e50be2e7e1ed38aac978311b64`
- 知识状态：当前
- 验证状态：静态源码和所有当前生产调用点已核；构建、异常注入和真实装配 `NOT_RUN`

## 用途

建立一个独占`普通应用上下文`：先建立唯一L1事实基座运行包，再为存在、特征定义、特征实例、状态、动态、因果、场景、概念和方法建立owner范围与强类型交付，依赖有序构造现有L2服务及世界、概念、方法聚合服务。

## 输入与结果

- 输入：`普通应用配置`；当前`有效()`固定返回true。
- 成功：`已装配`及非空`unique_ptr<普通应用上下文>`。
- 非成功：按31个装配状态精确区分配置、owner范围、交付、服务或聚合构造失败；最外层其它异常映射`构造失败`。

## 所有权、事务和生命周期

- 上下文独占持有L1运行包和全部服务对象，不可复制、不可移动。
- 服务借用的下层引用由成员声明/析构顺序维持；聚合只引用同一上下文已有服务。
- 每个owner建立是独立步骤；本函数没有把全部装配包装成跨owner单事务。
- 任一步失败依靠局部对象析构回收已经形成的内存对象。

## 明确非职责

不建立系统世界树根、自我、概念本体根或方法登记根，不启动线程或宿主，不证明服务已经通过真实消费者验收。当前也没有装配语言/词条服务。

## 合法调用语境

当前生产调用点为`运行普通程序`。调用者必须检查`普通应用装配结果::成功()`后再解引用上下文。
