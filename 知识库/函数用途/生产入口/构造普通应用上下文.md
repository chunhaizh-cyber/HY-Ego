# `构造普通应用上下文`

- 完整签名：`海中鱼巣::普通应用装配结果 海中鱼巣::构造普通应用上下文(const 海中鱼巣::普通应用配置& 配置)`
- 边界类别：服务装配函数
- 位置：`海中鱼巣/装配.普通应用.ixx:454`
- 模块：`海中鱼巣.装配.普通应用`
- 可见性：导出函数
- 绑定提交：`3a58b690f3b76a73b99ec02fcbdbaed7f90a2607`
- 源码 blob：`e31023e0bf4f672a78d63fee19c5f607e5a5803b`
- 知识状态：当前
- 验证状态：静态源码和计划内双配置已核；任务记录与需求任务承接服务由治理专项经真实普通应用上下文装配，独立验收仍为`FAIL / 未正式接线`

## 用途

建立一个独占`普通应用上下文`：先建立唯一L1事实基座运行包，再建立各结构owner范围和强类型交付，构造基础结构服务、任务方法路径服务、任务子目标承接记录结构服务、需求任务子目标承接服务、条件提议提供者及聚合服务。

## 输入与结果

- 输入：带不可变材料存储合同和绝对受控根的`普通应用配置`；入口再次调用`有效()`。
- 成功：`已装配`及非空`unique_ptr<普通应用上下文>`。
- 非成功：56个枚举值中除`已装配`外的55个状态，精确区分配置、owner范围、交付、写入端口、服务、provider或聚合构造失败；最外层其它异常映射`构造失败`。

## 所有权、事务和生命周期

- 上下文独占持有L1运行包和全部服务对象，不可复制、不可移动。
- 服务借用的下层引用由成员声明/析构顺序维持；聚合只引用同一上下文已有服务。
- 每个owner建立是独立步骤；本函数没有把全部装配包装成跨owner单事务。
- 任一步失败依靠局部对象析构回收已经形成的内存对象。

## 明确非职责

不建立系统世界树根、自我、概念本体根或方法登记根，不启动线程或宿主。装配任务记录和需求任务承接服务不等于存在生产调用边，也不证明父需求重判、成员结算或完整需求闭环。

## 合法调用语境

当前生产调用点为`运行普通程序`。调用者必须检查`普通应用装配结果::成功()`后再解引用上下文。

依据：`施工记录/20260816_DATA-LANGUAGE-ORDINARY-APP-ASSEMBLY-READBACK_语言材料普通应用装配与首次权威读回施工记录_v0.1.md`。
