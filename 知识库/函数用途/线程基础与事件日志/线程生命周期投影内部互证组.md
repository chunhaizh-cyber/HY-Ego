# 线程生命周期投影内部互证组

- 稳定身份：16个内部函数
- 边界类别：内部服务函数（键、请求、转换和历史链互证）
- 模块：`海中鱼巣.线程.投影.线程生命周期`
- 位置：`海中鱼巣/线程/投影.线程生命周期.ixx:21-274`
- 绑定提交：`595d4a67ad3b633cabeb6cb87a1bd179880cc9ba`
- 源码 blob：`9f74b91c1c140675eca1a3cb6b46806394fdc591`
- 知识状态：当前
- 实现状态：已实现；只供同模块投影服务调用
- 验证状态：16/16定义和父调用静态互证；完整乱序、容量、资源与真实线程运行矩阵`NOT_RUN`

## 完整身份

| 完整签名 | 只负责 |
| --- | --- |
| `bool operator<(const 投影键&, const 投影键&) noexcept` | 按运行代次、线程身份排序 |
| `bool operator==(const 投影键&, const 投影键&) = default` | 比较投影主键 |
| `bool operator<(const 消息键&, const 消息键&) noexcept` | 按投影键、事件序号排序 |
| `bool 投影有效(线程生命周期投影) noexcept` | 限定已知投影枚举 |
| `bool 线程类别有效(项目线程类别) noexcept` | 排除未知线程类别 |
| `bool 线程模块有效(项目线程模块) noexcept` | 排除未知线程模块 |
| `bool 诊断引用类别有效(线程诊断引用类别) noexcept` | 限定任务或工作项诊断引用 |
| `bool 终态(线程生命周期投影) noexcept` | 识别已退出和异常退出 |
| `bool 生命周期转换允许(线程生命周期投影, 线程生命周期投影) noexcept` | 核对相邻生命周期转换白名单 |
| `bool 配置有效(const 线程生命周期投影服务配置&) noexcept` | 核对代次、线程和消息容量上限 |
| `bool 请求有效(const 线程生命周期发布请求&) noexcept` | 核对版本、身份、类别、投影、时间与首事件形状 |
| `投影键 形成投影键(const 线程生命周期消息身份&) noexcept` | 从消息身份投影主键 |
| `消息键 形成消息键(const 线程生命周期消息身份&) noexcept` | 从消息身份投影历史账键 |
| `bool 相邻历史一致(const 消息账&, const 消息键&, const 线程生命周期发布请求&) noexcept` | 互证迟到消息与已存在前后相邻消息 |
| `bool 前置完整(const 消息账&, const 投影键&, std::uint64_t) noexcept` | 从事件1起检查连续序号和前一投影链 |
| `当前线程生命周期投影 形成当前投影(const 线程生命周期发布请求&, std::optional<std::uint64_t>) noexcept` | 形成新当前投影值 |

## 边界

这些函数不是公开ABI，不得跨模块调用或复制状态机。历史链完整只说明消息序列互证，不证明消息对应的业务动作发生；迟到消息可以补入历史但不得覆盖终态或当前消息。正式语义依据为8110，具体内存map和容量是实现选择。
