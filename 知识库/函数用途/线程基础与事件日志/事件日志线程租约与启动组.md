# 事件日志线程租约与启动组

- 稳定身份：13个公开/受限身份
- 边界类别：线程provider公开操作、服务装配与生命周期函数
- 模块：`海中鱼巣.线程.运行.事件日志线程`
- 位置：`海中鱼巣/线程/运行.事件日志线程.ixx:46-100,382-698`
- 绑定提交：`595d4a67ad3b633cabeb6cb87a1bd179880cc9ba`
- 源码 blob：`c8082fb70254485d65272dec58f3c6cfd04c4212`
- 知识状态：当前
- 实现状态：provider组件已实现并登记工程；普通应用和正式生产启动未接线
- 验证状态：生产外专项双配置各`20 PASS / 0 FAIL / 3 NOT_RUN`，根工程双配置通过；生产装配、可靠恢复和完整运行`NOT_RUN`

## 完整身份与用途

| 完整签名 | 用途 |
| --- | --- |
| `事件日志线程租约::事件日志线程租约(事件日志线程租约&&) noexcept` | 转移唯一队列、入口与SUPPORT租约 |
| `事件日志线程租约& 事件日志线程租约::operator=(事件日志线程租约&&) = delete` | 禁止移动赋值 |
| `事件日志线程租约::事件日志线程租约(const 事件日志线程租约&) = delete` | 禁止复制 |
| `事件日志线程租约& 事件日志线程租约::operator=(const 事件日志线程租约&) = delete` | 禁止复制赋值 |
| `事件日志线程租约::~事件日志线程租约()` | 未显式收口时保守停收并安全连接，异常终止 |
| `事件日志提交结果 事件日志线程租约::提交(事件日志摘要) noexcept` | 有界MPSC接受摘要并分配连续位置 |
| `事件日志停止接收结果 事件日志线程租约::停止接收并取得截止() noexcept` | 锁存唯一接受截止并唤醒入口 |
| `事件日志处理位置 事件日志线程租约::读取处理位置() const noexcept` | 读取值式队列处理位置 |
| `事件日志完成等待结果 事件日志线程租约::等待完成到截止(const 事件日志完成等待请求&) const noexcept` | 有界等待完成截止或返回故障/超时 |
| `事件日志未取出快照结果 事件日志线程租约::冻结并读取未取出快照(const 事件日志未取出快照请求&) noexcept` | 永久冻结新取出并复制未取出项 |
| `事件日志线程停止连接结果 事件日志线程租约::停止并连接(const 事件日志线程停止连接请求&) && noexcept` | 消费唯一租约，经SUPPORT停止、join并返回最终见证 |
| `事件日志线程租约::事件日志线程租约(std::unique_ptr<事件日志线程状态>, std::unique_ptr<事件日志生产写入适配器>, std::unique_ptr<事件日志线程入口>, 支持线程候选回收请求, 支持线程候选租约&&) noexcept` | 仅供启动友元按安全析构顺序装配拥有体 |
| `事件日志线程启动结果 启动事件日志线程(const 事件日志线程启动请求&, 线程生命周期发布端口&) noexcept` | 校验请求，创建SUPPORT候选，等待真实进入或失败后安全回收 |

## 合法语境与限制

调用者必须持有唯一移动租约并按“提交 -> 停收截止 -> 等待完成或冻结快照 -> 右值停止连接”收口。前置未闭合或坏停止请求仍要求安全join，但返回专门非正常状态。事件摘要不去重，相同摘要身份可以取得多个位置；这不是领域幂等。

EVENT计划验证只证明provider组件和计划内矩阵。当前没有普通应用构造/持有/调用点，因此这些入口属于已实现但未接线候选，不得写成正式启动阶段、可靠日志恢复或业务闭环已完成。
