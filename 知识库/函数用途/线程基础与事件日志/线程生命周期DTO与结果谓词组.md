# 线程生命周期DTO与结果谓词组

- 稳定身份：8个身份；其中2个在投影模块定义
- 边界类别：非服务函数（值式比较与发布结果解释）
- 模块：`海中鱼巣.线程.投影.线程生命周期`
- 位置：`海中鱼巣/线程/线程生命周期.数据.h:14-147`、`海中鱼巣/线程/投影.线程生命周期.ixx:524-534`
- 绑定提交：`595d4a67ad3b633cabeb6cb87a1bd179880cc9ba`
- 源码 blob：`71bd60686dafe9bd58aa2f7aab93e13c6ceffbfc`、`9f74b91c1c140675eca1a3cb6b46806394fdc591`
- 知识状态：当前
- 实现状态：已进入线程生命周期投影工程模块
- 验证状态：8/8身份、声明与定义静态互证；真实全生命周期矩阵`NOT_RUN`

## 完整身份与用途

| 完整签名 | 用途 |
| --- | --- |
| `bool operator==(const 线程逻辑身份&, const 线程逻辑身份&) = default` | 比较运行期逻辑线程主键 |
| `bool operator==(const 线程生命周期消息身份&, const 线程生命周期消息身份&) = default` | 比较运行代次、线程和事件序号 |
| `bool operator==(const 线程诊断引用&, const 线程诊断引用&) = default` | 比较诊断引用类别与身份 |
| `bool operator==(const 线程生命周期发布请求&, const 线程生命周期发布请求&) = default` | 比较完整生命周期发布材料 |
| `bool operator==(const 线程生命周期消息&, const 线程生命周期消息&) = default` | 比较发布请求和当前性 |
| `bool operator==(const 当前线程生命周期投影&, const 当前线程生命周期投影&) = default` | 比较完整值式线程投影 |
| `bool 线程生命周期发布结果::已接受() const noexcept` | 接受已发布、精确重复或迟到历史消息且要求消息和当前投影载荷齐全 |
| `bool 线程生命周期发布结果::已成为当前() const noexcept` | 在已接受基础上要求消息当前且等于当前投影的当前消息 |

## 边界

默认比较只做值相等；两个结果谓词只解释同一发布结果，不执行发布、读取或事实裁决。线程生命周期消息是8110规定的只读运行投影材料，任务或工作项引用只用于诊断定位，不能推出任务等待、完成、失败或需求满足。
