# G1-R1 D 正式规范层级、根子与外设唯一归属修复记录

日期：2026-07-21

状态：D 批已完成并通过独立只读终检

## 1. 范围

本批只治理正式规范层级、对象根身份、根子重复和外设规则唯一归属；不修改 C++、工程、自检、流程图或详细设计，不运行构建和程序，也不消费代码执行队列。

三条同角色设计切片按互斥文件范围并行：4xxx/5xxx 与需求任务方法根、6xxx 观察外设、7xxx/8xxx 与语素运行治理。顶层独占规范目录、检查器、跨切片重复、状态记录和串行 Git 收口。

## 2. 层级与根对象

- 56 份旧 `上级规范` 字段全部拆为 `直接上级规范`、`直接依赖规范` 和 `相关边界规范`。
- 4210/4220、5310/5320、6210—6370、7200/7210、8200/8210 的直接上级循环全部消除。
- 正式目录由 82 份编号规范、22 个对象根收束为 81 份编号规范、20 个对象根。
- 原 1175 等义迁入 4160 特征表达与判断收束主题子规范。
- 原 1195 并入 1190 因果本体；模板细则由 4310、4320 唯一承载。
- 检查器新增精确 20 根集合、旧关系字段退出、1175/1195 旧路径退出和直接上级无环门禁。

## 3. 唯一规则归属

| 规则 | 唯一归属 |
| --- | --- |
| 需求 OR / AND、父权重继承、组内平分与预算 | 5110 |
| 任务特征来源、状态矩阵、筹办闭合、任务级唯一执行权 | 5200、5210、5230 |
| 方法登记、召回、选择、运行账和本能边界 | 5300—5330 |
| 语素对应关系、概念追溯与场景绑定 | 7200 |
| 人类词性、信息入口类型、领域路由与词到词性投影 | 7210 |
| 像素簇与存在候选 | 6300 |
| 外设域、任务承接、自我认知与场景事实分层 | 6320 |
| 通用外设报告、队列、一次消费、任务绑定和任务域唯一消费权 | 6340 |
| 双目相机 L0—L3、三种外显模式和四类逻辑供给产品 | 6350 |
| 相机外设综合顺序 | 6360 |
| 图像稳定到空间静止的位姿补偿与量化门 | 6210 |
| 线程最小消息合同和只读投影 | 8110 |

3100、3200、3300、2210 根规范只保留各自对象身份、来源、写入权、生命周期和原则边界，通过子规范路由指向上述细则，不再复制枚举、公式、矩阵或完整步骤。

## 4. 独立复核修正

独立只读复核先后发现并促成两类阻断修正：

1. 6300、6320、8200 正文消费 6340/6350 合同但关系元数据漏登记；已补为直接依赖。
2. 6360 收束时误删“图像相对稳定不等于存在静止”的唯一逻辑；已迁入 6210，并把 6310 质量与坐标合同列为直接依赖。

修正后终检 `PASS`。这证明检查器的无环和目录检查不能替代正文语义复核；后续规范去重仍必须逐条建立“删除规则 -> 唯一保留归属”映射。

## 5. 验证与完成边界

- `git diff --check`：通过，仅有行尾转换提示。
- `python -B .\tools\check_specs.py --strict`：81 份目录项全部通过。
- 旧 `上级规范` 字段：0。
- 对象根：20 个；直接上级循环：0。
- 独立只读终检：`PASS`，零写入、零构建、零运行、零 Git 写入。

D 批只证明正式规范层级和唯一归属治理完成。它不证明 E 批的 26 份未登记旧规范已经迁入或退出，不证明 DD-05 已恢复，不证明代码、D455、外设生产链或自我内部循环已经实现。

当前没有满足全部门禁却遗漏派发的普通代码执行计划：#325—#327 仍有具名依赖，#328 接口漂移，#324 属独立主线发布后继。D 批是设计治理，不制造执行窗口。
