# PERSIST-S1-P0-B3 JY-495 代码构建静态固定点通过与共享验证派发

日期：2026-07-19

队列：#301 / DQ-193 / PERSIST-S1-P0-B3 v0.5

事件：JY-495 / E240

角色：WT-MAIN 设计角色

## 1. 输入事实

WT-301-R1 在冻结基线 `719943f720e32f02febd3b475d91962a8576de20` 上完成 E239 授权的 S1—S4，当前未暂存差异精确为：

```text
海中鱼巣/线程/自我治理领域路由.ixx
海中鱼巣/线程/自检.自我治理闭环.ixx
海中鱼巣/线程/自检.自我治理多轮.ixx
```

统计为 462 insertions / 515 deletions，index 为空。Debug / Release x64 Rebuild 均为 0 warning / 0 error，产品进程为零；未运行 self-test、未访问共享事件段目录、未创建专属实施记录、未暂存、提交或推送。

## 2. 代码边界裁决

1. `自我治理领域路由` 只持同一生产运行期租约，五处构造均改接该租约；治理、动作、结算、观察四阶段只经 B0 正式业务面写入。
2. 生产路由中的旧需求、任务、方法、存在、场景、特征、状态、动态、因果服务及索引仓库写路径、原始事务令牌、许可和写入会话引用均为零。
3. 多轮自检中的 B1 任务管理上行桥继续使用租约只读查询适配器，没有回改 #299。
4. 同一允许文件内的薄 PIMPL 用于把 B0 组合器的私有模块依赖留在模块实现面，避免 MSVC 把同名历史类型泄漏进导出 IFC；它不形成第二装配、第二业务接口或新的结构所有者。
5. 两个允许自检模块仅在全局模块片段内对旧 `方法服务.h` 的 `方法登记根材料`、`方法登记项材料` 作局部宏别名，并在包含后立即撤销。该适配只保护旧夹具与新模块 IFC 共存，不修改旧服务实现、生产接口或机器状态。

因此，当前实现不构成允许范围、业务合同或所有权漂移，可以进入运行验证。

## 3. 已通过固定点

```text
Debug x64 Rebuild：0 warning / 0 error
Release x64 Rebuild：0 warning / 0 error
strict：通过
git diff --check：通过
精确三文件：通过
禁止依赖：通过
传统写引用归零：通过
五处同租约构造：通过
四阶段 B0 调用：通过
请求 / 结果 variant：14 / 11
正式阶段 940 登记：0
产品进程：0
```

## 4. E240 共享验证授权

E240 发布后，WT-301-R1 可独占 `D:\海中鱼巣\日志\事件段\自检`，固定顺序为：

1. Debug x64 完整 self-test。
2. 仅 Debug 全通过后运行 Release x64 完整 self-test。
3. 仅两配置单轮全通过后，从第 1 轮开始全新运行 Debug 严格 20 / 20；每轮一个自然结束的产品进程，完整 stdout / stderr 先落盘，轮间等待 2 秒。

Debug 每个有效轮须机械确认：

```text
SELF-GOV-S4A：8 项，失败 0
SELF-GOV-S4B：8 项，失败 0
SELF-GOV-S4C：8 项，失败 0
SELF-GOV-S4D：8 项，失败 0
SELF-GOV-S5：12 项，失败 0
SERVICE-DATA-S5：38 项，失败 0
TASK-RESULT-S1：18 项，失败 0
PERSIST-S1-P0-A：18 项，失败 0
PERSIST-S1-P0-B0：20 项，失败 0
PERSIST-S1-P0-B3C：12 项，失败 0
```

解析格式必须按当前输出事实分别冻结：SELF-GOV、SERVICE-DATA-S5、TASK-RESULT-S1、PERSIST-S1-P0-A 使用 `: 失败`；PERSIST-S1-P0-B0、PERSIST-S1-P0-B3C 使用 `=失败`。Release 上述 Debug 专项输出均为零。每次运行后必须确认产品进程归零。

首个非零退出、行数或叶子不符、解析异常、日志截断、目录竞争、进程残留或工具异常，立即停止、释放共享目录、不补跑、不续算，并保留三文件 WIP 回告设计窗口。

## 5. 成功收口

只有全部运行验证通过，才允许：

1. 新建唯一专属记录 `实施记录/20260718_PERSIST-S1-P0-B3_自我治理权威写路径改接代码实施_Codex断点清单.md`。
2. 重跑 strict、diff、精确四文件、禁止依赖、Release 隔离和 staged diff。
3. 精确暂存三代码与该记录，提交到 `codex/301-p0b3-r1`。
4. 非强制推送 `origin/codex/301-p0b3-r1`，回告完整提交、唯一父、remote 0 / 0、clean和全部证据。

任务完成状态只到“分支完成待独立集成”。禁止推 main、force、merge、rebase、cherry-pick、创建集成 worktree、修改入口 / 工程 / 运行器 / 中央治理、登记 940或消费 #306 等后继。

## 6. 完成边界

JY-495 只接受 #301 的代码、构建和静态固定点并派发运行验证，不证明任务分支完成、独立集成完成或 #301 已归档。#306 继续等待 #301 正式集成与设计归档；940 仍由 #309 登记。
