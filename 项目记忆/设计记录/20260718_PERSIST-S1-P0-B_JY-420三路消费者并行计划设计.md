# PERSIST-S1-P0-B JY-420 三路消费者并行计划设计

日期：2026-07-18

设计裁决：JY-420

当前状态：历史设计 / 已由 JY-421 与消费者流程图 v0.5 纠偏 / 不再作为并行执行依据

涉及队列：#298、#299、#301、#302、#303、#309

设计队列：DQ-190、DQ-191、DQ-193、DQ-194、DQ-195、DQ-201

## 1. 当前事实

```text
主集成工作树：D:\海中鱼巢
分支：main
设计起点：3c3db689f5ffa6ad8352ed7cacad9072b6919f7e
main / origin/main：一致
WT-297-R1：codex/297-p0a-r1@9973083，clean，分支完成待集成
#297：尚未进入 main，#298 继续依赖门控
```

原 #299、#301、#302 均允许修改 `海中鱼巣.vcxproj`、`.filters` 和 `自检.运行器.ixx`；#302 又依赖 #299 提供查询适配。按 `规范/多工作树并发与集成规范.md`，这组计划不具备文件互斥、共享文件唯一所有者和接口无依赖条件，不能直接并行。

当前代码还确认专项自检登记实际由 `入口.cpp` 承担，`自检.运行器.ixx` 只拥有通用运行器合同。原计划中的统一运行器登记假定需要同步纠正。

## 2. 默认裁决

采用“串行公共提供者 -> 三路并行消费者 -> 串行汇合 -> 默认入口切换”：

```text
#297 集成归档
-> #298 v0.2 串行建立正式值式合同和公共只读查询门面
-> #299 v0.2、#301 v0.2、#302 v0.2 在同一 #298 归档基线上并行
-> 独立集成 worktree 固定顺序汇集
-> #309 v0.1 串行登记 920 / 940 / 950 并做同租约交叉验收
-> #303 v0.2 串行切默认入口
```

#302 的前置从 #299 改为 #298。#299 与 #302 共同消费 #298 唯一拥有的 `适配/查询.运行期只读.ixx`；#301 只消费 #298 B0 正式写业务面。三个消费者不互相提供接口。

## 3. 文件所有权裁决

| 任务 | 独占文件方向 | 不再拥有 |
| --- | --- | --- |
| #299 | 两个上行桥、既有概念命名治理 / 任务回执自检、专属记录 | 新查询模块、入口、工程、filters、运行器 |
| #301 | 自我治理领域路由、既有闭环 / 多轮自检、专属记录 | 入口、工程、filters、运行器 |
| #302 | 新投影 / 投影自检、控制面板、窗口、SQL 适配、既有 830 自检、专属记录 | 入口、运行器；工程 / filters 为本批唯一所有者 |
| #309 | 入口、新汇合自检、工程、filters、专属记录 | 三消费者生产文件和 B0 公共接口 |

并行任务不登记 920 / 940 / 950；这些阶段由 #309 在三路正式集成后统一登记。任务级仍须执行两配置 Rebuild、现有具名回归、连续 20 / 20、strict、diff 和精确范围。

## 4. 三件套一致性

已生成 / 修订：

```text
流程图/20260718_生产运行期消费者三路并行与串行汇合流程图_v0.4.md
流程图/20260718_生产运行期消费者三路并行与串行汇合流程图_v0.4.html
规范/详细设计/权威状态快照隔离恢复与运行期上下文一次发布详细设计.md 第 18 节
计划/#298、#299、#301、#302、#303 正文 v0.2
计划/20260718_PERSIST-S1-P0-BPAR_三路消费者并行汇合与阶段登记代码实施切片_v0.1.md
```

流程图、详细设计和计划一致于：#298 串行、三消费者依赖门控并行候选、#309 串行汇合、#303 串行切换；#300 / #214 保持暂停。

## 5. 并行资格边界

本轮没有 #298 正式归档提交，因此没有共同冻结基线，也没有创建 #299 / #301 / #302 worktree。本轮只登记“依赖门控并行候选”，不构成预授权并行批次或任务写入权。

#298 进入 main 并完成设计同步后，设计角色必须重新读取 Git、实际接口、三个计划和本记录，再把以下事实写入 `项目记忆/并行工作树登记表.md`：

```text
精确共同基线提交
三个任务 worktree 绝对路径、codex/* 分支和窗口所有者
完整文件 / 结构 / ABI 所有权
任务级和集成级验证矩阵
固定集成顺序 #299 -> #301 -> #302
独立集成 worktree 路径和分支
```

任一文件交集、接口漂移、消费者依赖或共享所有权无法唯一裁决时，取消并行资格并退回串行。

## 6. 新队列 #309

#309 / DQ-201 已生成并登记为依赖门控待执行。它不参与三任务并行，不负责汇集分支；只有三任务集成进入 main 后才执行统一阶段登记与交叉验收。#303 的正式前置改为 #309。

## 7. 完成边界

本设计轮只完成流程、详细设计、计划和路由治理；未修改 C++ / 工程，未创建新任务 worktree，未构建、未运行、未登记 910—960。不得宣称并行批次已冻结、#297 已集成、#298 已解锁、默认入口已切换或持久化恢复已完成。

## 8. 验证

```text
python tools/check_specs.py --strict
git diff --check
流程图 MD / HTML 同图核对
计划允许文件两两交集核对
计划 / 索引 / 队列 / 登记表 / 当前状态同步核对
```
