# REAL-GENERATION-LOOP-S1 R7 并发同义选择非成功返回二分审查表

日期：2026-07-20

| 节点 | 代码位置 | 返回条件 | 输入语境 | 是否上游保证有效 | 结构是否变化 | 设计是否允许 | 二分口径 | 追根因对象或逻辑返回理由 | 计划动作 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| A | R7 路由 88-93 | 配置/租约/上下文/B0 不完整 | 外部材料或正式宿主 | 混合 | 否 | 外部材料允许；正式宿主不允许 | 分语境：逻辑内返回 / 追根因解决 | 外部请求可拒绝；正式宿主合同失真追上下文发布根因 | 本次不扩范围，保留现状并在验收中保证入口 |
| A | R7 路由 98-100 | 两根无效、相同或角色不符 | 正式系统角色清单 | 是 | 否 | 否 | 追根因解决 | 已发布完整清单与路由读回矛盾 | 偏差记录，后继独立治理 |
| A/B | R7 路由 104-113 | 根读回不完整 / 根已满足 | 正式权威读回 | 是 | 否 | 内部矛盾不允许；已满足允许 | 分流：追根因解决 / 逻辑内返回 | 根结构矛盾追根因；已满足本轮无需筹办 | 保持 |
| B | R7 路由 120-130 | 创建任务非成功或成功后读回不完整 | 正式流程契约 | 是 | 可能 | 真实写前当前性允许；内部矛盾不允许 | 分流 | 内部不一致/成功后不完整追根因；真实当前性零半结构逻辑返回 | 保持 |
| C | R7 路由 137-160 | 生命周期迁移非成功或最终非筹办中 | 正式四字段请求 | 是 | 可能 | 完整历史同义/写前漂移允许；提交后矛盾不允许 | 分流 | 继续遵守完整历史幂等与写后读回口径 | 保持 |
| E | R7 路由 166-189 | 方法首/结果/动作非成功或方法读回不完整 | 正式方法基线 | 是 | 可能 | 真实写前当前性允许；内部矛盾不允许 | 分流 | R7 已证明目标竞态不在此段；内部不一致仍追根因 | 保持 |
| J1/L1 | R7 路由 198-200 + 召回 151-153 | 召回无候选且调用后任务仍无选择 | 非权威候选阶段 | 是 | 否 | 是 | 逻辑内返回 | 当前没有可执行候选，任务保持筹办中 | 修代码：读回后仍无选择才返回无候选 |
| J1/L2 | R7 路由 198-200 + 召回 155-158 | 召回非唯一且调用后任务仍无选择 | 非权威候选阶段 | 是 | 否 | 是 | 逻辑内返回 | 没有唯一建议，任务保持筹办中 | 修代码：读回后仍无选择才返回非唯一 |
| R1 | R7 路由 198-200 + 召回 98-100 | 召回内部不一致 | 正式流程已进入权威读回 | 是 | 否 | 否 | 追根因解决 | 特征/目标状态或内部承载矛盾；即使当前出现选择也不得掩盖 | 修代码：内部不一致优先返回并保留证据 |
| X0-X3 | 召回 89-92；组合 96-100；路由 198-200 | 召回重读发现当前选择而入口拒绝；路由未读回即当前性返回 | 两线程同义正式请求 | 是 | 另一线程已形成完整选择 | 现状不允许提前丢弃完整同义完成 | 先权威读回再二分 | R7 已证明本分支是 A11 间歇失败唯一返回点 | 修代码：路由重读任务和方法，完整全同义则幂等继续 |
| R2 | 召回 86-95 / 165-181 | 入口拒绝或适配失败后，权威任务/方法仍完整稳定且无选择 | 正式请求 | 是 | 否 | 否 | 追根因解决 | 有效输入和稳定当前性下没有设计允许理由 | 修代码：归内部不一致，不伪装当前性变化 |
| L0/L3 | 新施工分支 | 非成功后权威任务/方法完整但生命周期、活跃关系或选择来源字段异义 | 正式请求并发当前性 | 是 | 至多已有其它完整结构 | 是 | 逻辑内返回 | 权威事实已经漂移，不能采用异义选择 | 修代码：返回当前性变化，零新增结构 |
| R3 | 新施工分支 | 权威任务存在当前选择但选择材料不完整 | 正式权威读回 | 是 | 已有可读半结构 | 否 | 追根因解决 | 当前选择发布承载不完整 | 修代码：内部不一致并停止冻结 |
| P | 新施工分支 | 权威选择完整且记录主键、任务/方法、材料号、来源关系/版本、规则版本全同义 | 两线程同义正式请求 | 是 | 只有一个既有完整选择 | 是 | 逻辑内幂等读回，不是失败返回 | 采用权威完整选择继续冻结；不补写、不重试 | 修代码 + 自检 |
| K1 | R7 路由 204-208 | 调用后任务/选择不完整或方法不同 | 正式权威读回 | 是 | 可能已有选择 | 现状把全部归内部不一致过粗 | 分流 | 读回/选择半结构追根因；完整异义选择为当前性变化 | 修代码：复用统一全字段裁决 |
| O | R7 路由 234-243 | 冻结不完整或最终报告不完整 | 同义选择成立后的正式纯值阶段 | 是 | 无业务结构变化 | 仅真实当前性允许 | 分流 | 内部不一致追根因；过期/当前性可逻辑返回 | 保持 |

审查结论：`入口拒绝` 不是最终业务语义。R7 证据要求路由在调用后读回裁决；只有完整全同义选择可作为幂等完成。内部不一致、读回不完整和半结构必须追根因解决，无候选/非唯一及真实完整当前性漂移才是逻辑内返回。
