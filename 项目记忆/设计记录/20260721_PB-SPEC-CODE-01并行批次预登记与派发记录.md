# PB-SPEC-CODE-01 并行批次预登记与派发记录

日期：2026-07-21

状态：JY-559 正式派发与 S0 接收完成；两个任务并发执行中

## 1. 批次目标

把无真实依赖且所有权可隔离的两个施工计划放入独立任务 worktree 并发执行：

```text
#324 / DQ-216 / DD-01 D455材料成熟度与四类产品数据合同
#328 / DQ-220 / TASK-PREP-RIGHT任务级唯一筹办执行权
```

本记录只承载设计路由、冻结事实和派发证据，不授权 WT-MAIN 修改 C++。

## 2. 准入复核

1. 两计划在 JY-557 基线上都没有未满足业务前置。
2. 允许文件交集为 0。
3. #324 独占 `海中鱼巣.vcxproj`、`海中鱼巣.vcxproj.filters` 和 `海中鱼巣/入口.cpp`；#328 明确禁止这些文件。
4. #324 只拥有 D455 产品值式合同和阶段 842；#328 只拥有任务筹办执行权、召回、选择、冻结和任务执行请求门禁。
5. 两计划不存在提供者到消费者、初始化、发布、迁移或恢复先后依赖。
6. 固定集成顺序为 #324 -> #328；任务分支完成不等于进入 main。

## 3. 工作树身份

| 工作树 | 路径 | 分支 | 所有者 | 状态 |
| --- | --- | --- | --- | --- |
| WT-324-R1 | `C:\Users\zhchh\.codex\worktrees\324-dd01-r1\海中鱼巢` | `codex/324-dd01-d455-contract-r1` | `/root/pb_spec_code_01_dd01` | S0 PASS / S1—S4执行中 |
| WT-328-R1 | `C:\Users\zhchh\.codex\worktrees\328-task-prep-right-r1\海中鱼巢` | `codex/328-task-prep-right-r1` | `/root/pb_spec_code_01_task_prep` | S0 PASS / S1执行中 |

共同基线是 JY-557 发布提交 `80da244e10e1071f4f82561e249905599ac210f0`。两个工作树已经从该提交首次创建；聊天中的其它哈希或预期值不构成基线事实。

## 4. 验证矩阵

任务级验证分别完整执行各计划第 10 节。集成级验证固定包括：

```text
核对两个任务提交父链、文件范围和远端分支
按 #324 -> #328 汇集，无冲突才继续
Debug / Release Rebuild
两配置 --self-test-exit
python .\tools\check_specs.py --strict
git diff --check
DD01-A01—A12
TPR-A01—A12
两个计划的禁止依赖与禁止差异扫描
```

任一任务出现接口漂移、超范围、构建失败或验收失败，只退回对应任务；不得由另一个任务或集成角色代改。

## 5. 当前边界

E302 发布后已向两个唯一代理发送正式执行合同并读回 S0 PASS。PB-SPEC-CODE-01 当前正在并发执行；后续依次为：

```text
两任务分别执行、验证、提交并推送同名分支
-> 设计窗口核对固定提交和范围
-> 另立独立集成身份
```
