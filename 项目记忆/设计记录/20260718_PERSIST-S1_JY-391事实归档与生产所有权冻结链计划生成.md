# PERSIST-S1 JY-391 事实归档与生产所有权冻结链计划生成

日期：2026-07-18

设计裁决：JY-391

完成队列：#295 / DQ-187

新队列：#296—#307 / DQ-188—DQ-199

设计门控队列：#226 / DQ-118

窗口事件：WIN-0001-E132

状态：#295 已完成归档 / 恢复施工路由 v0.3 已形成 / #296—#307 已生成 / #226 v0.3 继续禁止执行 / E132 待本轮验证、提交与非强制推送

## 1. 正式接管

当前物理窗口从 `main@46e49c038c5c600a4d8a49e08156c4c331eb3acb == origin/main` 重新读取：

```text
计划索引、Codex 任务队列、窗口交互记录和 JY-390。
#295 计划、两份正式事实记录及提交范围。
20260718 恢复流程图 v0.2、恢复详细设计和 #226 v0.2。
#294 九段物理审计、#271 兼容矩阵、CODE-FILE-01、迁移台账。
入口、运行期上下文 / 宿主 / 租约、业务装配、概念活动、四仓库、特征值和索引当前接口。
```

#295 只新增两份允许记录；M0—M6、A01—A18 全部通过，未改代码、未构建、未运行、未登记阶段。执行角色已按正式回执交回设计角色。

## 2. 顺序裁决

v0.2 的 `P0-A -> P0-B -> P0-C` 会允许先发布没有概念活动私有权威的上下文，因此撤回。现行顺序固定：

```text
#296 P0-C 概念活动唯一所有者
-> #297 P0-A 排他生产首发
-> #298 B0 正式值式合同
-> #299 B1 / #301 B3 / #302 B4
-> #303 B5 默认切换
-> #304 A0 -> #305 A1 -> #306 A2 -> #307 A3
-> #226 PERSIST-S1-B
```

#300 / B2 继续绑定 #214 用户暂停。B5 只有在旧命名写路由于默认生产静态、运行期均不可达时，才可不等待 #300；不得据此恢复命名写业务。

## 3. 生产版本 1 与排他通道

稳定配置固定：

```text
域编号 = 1；仓库编号 = 1。
稳定主键前缀 = 0x4859'0001'0000'0000。
系统角色键 = 前缀 + 1…21；生命周期键 = 前缀 + 22…24。
安全当前 / 目标 = 0 / 1；服务当前 / 目标 = 0 / 1；动态根材料 = 1。
稳定动作键 = 0x4859'0002'0000'0001。
```

B5 前默认旧通道、显式 `--runtime-context` 新通道；一次进程只允许一个通道，失败不得互相回退。B5 后默认新通道；旧大段只保留 Debug 自检 / 兼容证据，Release 不增加旧生产逃生口。

## 4. 计划生成映射

| 队列 | DQ | 计划 | 状态 | 阶段 |
| --- | --- | --- | --- | ---: |
| #296 | DQ-188 | P0-C 概念活动唯一所有者 | 已派发待执行回执 / 当前唯一候选 | 890 |
| #297 | DQ-189 | P0-A 排他生产首发与租约持有 | 依赖门控 / 前置 #296 | 900 |
| #298 | DQ-190 | B0 关键路径值式公开合同 | 依赖门控 / 前置 #297 | 910 |
| #299 | DQ-191 | B1 纯只读上行桥 | 依赖门控 / 前置 #298 | 920 |
| #300 | DQ-192 | B2 概念命名写路径 | 用户暂停 / #214 重设计前非待执行 | 930 |
| #301 | DQ-193 | B3 自我治理权威写路径 | 依赖门控 / 前置 #298 | 940 |
| #302 | DQ-194 | B4 SQL / 面板值式投影 | 依赖门控 / 前置 #299 | 950 |
| #303 | DQ-195 | B5 默认入口切换 | 依赖门控 / 前置 #299、#301、#302 | 960 |
| #304 | DQ-196 | A0 索引所有者声明 | 依赖门控 / 前置 #303 | 970 |
| #305 | DQ-197 | A1 四仓库全状态导出 | 依赖门控 / 前置 #304 | 980 |
| #306 | DQ-198 | A2 侧表角色登记活动导出 | 依赖门控 / 前置 #305 | 990 |
| #307 | DQ-199 | A3 正式租约同域独占九段材料 | 依赖门控 / 前置 #306 | 1000 |

每份计划均区分已验证当前事实、待实施目标、实施后证据和只有前置成立后才判断的退回条件，并列明允许 / 禁止文件、新文件分类、模块名、工程登记、非成功返回二分、验证和完成声明边界。

## 5. #226 v0.3

`计划/20260718_PERSIST-S1-B_HYSNAP01规范编码与原子文件发布代码实施切片_v0.3.md` 只绑定 #296—#307 依赖并冻结编码 / 严格自读 / 同卷原子发布后段职责。它当前不授权任何 C++ / 工程修改；#307 正式完成后必须按实际九段 DTO、模块名、旧拒绝壳和自检接口再次修订才可派发。v0.2 移入已完成目录，状态为历史撤回 / 证据保留。

## 6. 三层一致性与迁移边界

```text
流程图 v0.3、恢复详细设计第 17 节和 #296—#307 范围一致。
MIG-18 仍是 S0 / 设计完成、代码未实现，不计旧能力迁移。
O1 只在 A0 登记现有索引合同、在 A2 登记无私有侧表且未接生产；不新增第十段。
SQL / 控制面板只读，HYEVSEG1 不重放，日志 / 显示不裁决恢复。
#214 / #300 继续暂停。
```

## 7. 单窗口切换

E132 随本设计提交成功推送后，当前物理窗口必须重新读取 Git、索引、队列、#296、JY-391、#295 正式证据、窗口记录和 #296 实际接口，才可切中等推理执行角色。目标是当前物理窗口，不发送自发自收消息。执行角色只实施 #296 允许文件；完成或异常先写专属断点、验证、提交和推送，再切回设计角色。

## 8. 完成声明六项证据

| 维度 | 本轮证据 |
| --- | --- |
| 输入入口 | `46e49c0`、#295 两份正式事实、v0.2 三件套、#226 v0.2、CODE-FILE-01、MIG-18 和当前接口 |
| 核心处理 | 归档 #295；修正 P0 顺序；固定生产配置 / 排他通道；生成 #296—#307；更新 #226 v0.3 |
| 结构承载 | v0.3 流程图 MD+HTML、详细设计第 17 节、12 份新计划、#226 v0.3、JY-391 与中央项目记忆 |
| 输出变化 | #296 唯一候选；后继依赖门控；#300 暂停；#226 继续禁止执行；E132 设计转执行待推送 |
| 调用路径 | `46e49c0/#295 -> JY-391 设计角色 -> E132 提交推送 -> 重读正式状态 -> #296 执行角色` |
| 验证方式 | Mermaid 同源、strict、diff、计划 / 索引 / 队列唯一状态、精确范围、C++ / 工程零修改、未构建未运行未登记阶段 |

## 9. 完成边界

本设计轮只证明 #295 已归档，v0.3 施工路由、#296—#307 完整计划和 #226 后段依赖门控已形成。不得声明阶段 890—1000、生产入口、概念活动所有者、九段冻结、HYSNAP01 编码 / 发布 / 恢复或旧持久化能力已经实现。
