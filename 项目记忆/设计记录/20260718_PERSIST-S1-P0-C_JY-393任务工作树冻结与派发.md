# PERSIST-S1-P0-C JY-393 任务工作树冻结与派发

日期：2026-07-18

计划：#296 / DQ-188 / v0.1

批次：WB-296-01

状态：中央登记待验证、提交和非强制推送 / 代码零修改 / 阶段 890 未登记

## 1. 当前事实

1. `main@ea904a189a8f51dcc569ec5dc4183dbcacd1ad94 == origin/main`，主工作树 clean。
2. Git 实际主工作树为 `D:\海中鱼巢`；`海中鱼巣` 是仓库内源码目录名。JY-392 的工作树路径字形需要定向纠正。
3. #296 是当前唯一依赖就绪代码候选，#297 以后继续依赖门控，#300 / #214 用户暂停。
4. Codex 应用已建立 `C:\Users\zhchh\.codex\worktrees\9c7d\海中鱼巢`，冻结 HEAD 为 `ea904a1`；候选任务只做只读接管，未修改、暂存、提交或推送文件。
5. 该 worktree 已切换到独立分支 `codex/296-persist-p0-c`，工作树 clean。

## 2. 派发裁决

1. 建立单任务隔离批次 WB-296-01。它不构成两个计划并行，不改变 #296 的依赖和允许范围。
2. WT-296 独占 #296 计划第 4 节全部十个允许文件及其中的概念活动 DTO、数据操作、业务服务、业务装配槽、上下文完整性门禁和阶段 890。
3. `海中鱼巣.vcxproj`、`.filters` 和 `海中鱼巣/自检.运行器.ixx` 在本批次内只归 WT-296；WT-MAIN 不并发修改这些共享代码面。
4. 任务工作树只推送同名远端任务分支。任务验证通过后状态为 `分支完成待集成`，不得直推 `origin/main` 或归档 #296。
5. 任务提交形成后另建 `codex/integration-wb-296-01` 集成 worktree，按单项顺序重新运行计划最严格验证；通过后才由 WT-MAIN 快进发布。
6. 本设计提交成功推送后，由设计任务向新执行任务发送正式授权。执行任务必须读取 `origin/main` 上的登记表、计划索引、队列、计划和本记录，再复核实际接口后才可写入。

## 3. 验证与退回

```text
git worktree list --porcelain
git status --short --branch
git diff --check
python .\tools\check_specs.py --strict
```

以下任一成立即停止：路径、分支或冻结基线不一致；计划外文件写入；公开接口需要禁止文件；结构出现第二所有者；共享文件被 WT-MAIN 或其它任务并发修改；任务 / 集成验证失败；远端 main 在集成发布前发生未复核前进。

## 4. 完成声明边界

本轮只可声明 #296 的独立任务 worktree、任务分支、冻结基线、文件 / 结构 / 接口所有权和验证矩阵已登记并派发。不得声明 #296 已实现、阶段 890 已登记、多个代码计划已经并发、概念活动所有者已形成、生产入口已切换或恢复能力已完成。
