# PERSIST-S1-P0-B JY-421 消费者并行资格与能力台账纠偏

日期：2026-07-18

设计裁决：JY-421

涉及队列：#297、#298、#299、#301、#302、#303、#304—#307、#309

设计队列：DQ-189—DQ-199、DQ-201

## 1. 当前事实

```text
主集成工作树：D:\海中鱼巢
分支：main
设计起点：6714872abb891f0f007affc816645569e8b83265
main / origin/main：一致
worktree / index：clean
WT-297-R1：codex/297-p0a-r1@9973083，分支完成待集成
#297：尚未进入 main，#298 继续依赖门控
本轮只读复核：未改 C++ / 工程，未构建、未运行
```

从旧扫描基线 `3c3db689f5ffa6ad8352ed7cacad9072b6919f7e` 到当前 `main` 的 `.cpp`、`.h`、`.ixx`、`.vcxproj` 和 `.filters` 没有内容差异，因此默认入口、八类同步业务请求、20 个服务模块中的 15 个运行期装配成员、快照恢复拒绝矩阵和传统原始令牌公开面等代码事实仍可沿用。#297 任务分支和其集成提交都不是当前 main 祖先，不能计入主线实现。

## 2. 能力结论纠偏

原“服务基础能力大量存在，但新分层服务尚未成为默认生产运行期”的总判断成立，但必须增加以下限定：

| 原表述风险 | 当前准确口径 |
| --- | --- |
| “存在服务缺生产装配” | 存在服务已经进入候选 `装配.运行期业务`；缺的是默认生产激活、完整生命周期、通用删除与恢复，不是候选装配缺失 |
| “通用任务执行、结果回执和需求结算未形成” | #224 / 600 与 #225 / 610 已完成归档；通用执行、强类型回执、任务结果治理和需求独立结算已在隔离新路径实现，尚未统一进入默认生产运行期和恢复链 |
| “动作只有固定样本执行” | 已有 #224 的单稳定动作键通用执行调度和强类型回执；仍缺多动作适配、外部动作、失败 / 取消生产编排、恢复与默认运行期接线 |
| “20 个服务只有 15 个装配” | 该计数只适用于 `领域/服务.*.ixx` 模块服务；装配还持有传统 `特征值服务` 等对象，不能把 20 / 15 扩大成全部服务总数 |
| “#297 -> #298 -> 三路并行 -> #303” | 当前正式路线必须包含 #309，且 JY-421 进一步改为 #299 / #302 两路并行、#301 后继串行 |

成熟度结论固定为：基础结构和多数服务第一轮能力已经形成；通用任务执行和结果结算已有隔离实现，但新的生产服务权力链、默认运行期接线、权威持久化恢复、学习晋级和完整外部交互仍未完成。

## 3. 并行资格复核

`海中鱼巣/线程/自检.自我治理多轮.ixx` 当前同时 import 并实例化：

```text
自我治理领域路由：约第 158 行。
任务管理上行桥：约第 169 行。
```

#299 会改变任务管理上行桥构造合同，#301 会改变自我治理领域路由构造合同。若 #299 不修改该自检，完整 Rebuild 无法证明新桥 ABI 已迁移；若该文件只归 #301，#299 集成后的旧桥调用会先失效。因此 JY-420 的“三路无消费者依赖”结论不成立。

当前裁决：

```text
#297 集成归档
-> #298 v0.3 串行公共合同
-> #299 v0.3 / #302 v0.3 两路并行
-> 两路集成发布 main 并设计同步
-> #301 v0.3 从新 main 串行执行与集成
-> #309 v0.2 统一登记 920 / 940 / 950
-> #303 v0.3 默认入口切换
```

#299 定向拥有多轮自检中的 B1 桥构造同步；#301 后继串行再次接管同一文件，只完成 B3 路由构造和权威写验证。两次修改不在同一批次，禁止保留旧构造重载、双读或旧服务兜底。

## 4. #302 自检接线纠偏

#302 禁止修改入口和统一运行器，而当前专项登记实际由入口承担。新 `自检.运行期只读投影.ixx` 固定由允许修改的既有 `自检.控制面板按需投影.ixx` import 并在阶段 830 内组合调用；工程文件只负责 `ClCompile` 登记，不形成第二阶段或第二中央调用点。

#302 任务级验证必须补 `--database-self-test-exit`，与详细设计的默认 / 数据库双模式保持一致。

## 5. 当前计划覆盖

| 系统缺口 | 当前承接计划 | 裁决 |
| --- | --- | --- |
| 唯一生产运行期与默认入口 | #297—#303、#309 | 计划已覆盖；按两路并行 + #301 串行路线推进 |
| 完整冻结材料与 HYSNAP01 前置 | #304—#307 | 计划已生成，继续依赖 #303 串行门控 |
| 编码、文件发布、解析、隔离恢复与一次发布 | #226—#230、#237—#239 | 继续按 #307 后实际 DTO / 接口复核，不新增平行恢复计划 |
| 通用任务执行与结果结算 | #224、#225、#286 已完成 | 更新能力台账，不重复生成“第一轮通用执行”计划 |
| 概念命名 S4—S5 | #214、#215 | 用户暂停，保持原计划和架构重设计门控 |
| 动态模式、因果模式、用途观察与学习 | #231—#235、#282—#293 及后继门控 | 已有纯值 / 运行期候选和权威用途观察基础；生产接线、统计、学习和恢复不得合并进 P0-B |
| 控制面板、SQL、D455 和外部动作 | #302 只做只读租约投影；其它专项后置 | 不把只读投影或材料入口扩大为完整业务操作和外部世界事实写入 |

本轮不新增重复队列编号。现有计划链已覆盖当前 P0 默认运行期与持久化前置；更深层生命周期、学习或外部交互必须分别经过新的当前事实、流程图、详细设计和窄实施计划，不能用一份“补全全部服务”计划直接入队。

## 6. 三件套与治理同步

新增 / 修订：

```text
流程图/20260718_生产运行期消费者两路并行与自我治理串行汇合流程图_v0.5.md
流程图/20260718_生产运行期消费者两路并行与自我治理串行汇合流程图_v0.5.html
恢复详细设计第 19 节
#298 / #299 / #301 / #302 / #303 正文 v0.3
#309 正文 v0.2
计划索引、任务队列、当前状态、并行工作树登记表、窗口交互和决策记录
旧能力迁移与新内核建设逐能力台账 MIG-07 / MIG-08 / MIG-11 / MIG-13 / MIG-18
```

v0.4 流程图和详细设计第 18 节保留为 JY-420 历史设计，不再作为当前并行执行依据。WB-P0B-01 当前仍未冻结、未创建 worktree、未授权写入。

## 7. 完成边界

本轮只完成当前能力口径、并行资格、计划三件套和中央路由治理。未修改 C++ / 工程，未构建、未运行，未登记 910—1000，未集成 #297，也未创建 #299 / #302 / #301 worktree。不得宣称默认运行期、消费者改接、HYSNAP01、恢复、自我循环、完整控制面板或旧能力迁移已经完成。

## 8. 验证

```text
python tools/check_specs.py --strict
git diff --check
流程图 MD / HTML Mermaid 同图检查
计划 / 索引 / 队列 / 当前状态 / 登记表 / 能力台账引用一致性检查
git status、main / origin/main 和提交范围检查
```
