# PERSIST-S1-P0-B0 JY-434 独立集成起点自包含修正

日期：2026-07-19

队列：#298 / DQ-190

集成编号：INT-WB-298-03-R1

状态：集成起点已修正 / 等待 E179 发布后正式派发

## 1. 修正原因

JY-433 / E178 在写入前核对到的 `main@3000e33` 是中央预留提交的父提交。E178 发布后，正式 `main` 已由中央治理推进；若集成分支仍从 `3000e33` 起步，集成结果与包含 E178 的 WT-MAIN 将形成分叉，不能满足 `merge --ff-only` 发布合同。

集成消息尚未发送，`C:\Users\zhchh\.codex\worktrees\integration-wb-298-03-r1\海中鱼巢`、`codex/integration-wb-298-03-r1` 本地 / 远端和 worktree 均未创建，因此当前没有需要失效保留的旧集成候选。

## 2. 正式裁决

1. `3000e33` 只保留为 JY-433 / E178 写入前父提交，不再作为集成起点。
2. INT-WB-298-03-R1 的正式起点为“JY-434 / E179 本记录所在 `origin/main` 发布提交”。
3. E179 推送成功后，派发消息必须给出该提交的完整哈希；集成角色从远端重读并核对 `main == origin/main == 消息哈希` 后才可创建 worktree。
4. 后续集成分支从该哈希起步，精确 cherry-pick `409417f`，写专属集成记录后，WT-MAIN 才可能对结果执行非强制快进。
5. JY-433 的任务输入、十五文件、验证矩阵、允许 / 禁止范围和失败收口保持不变。

## 3. 状态边界

本记录仍只建立集成派发前置，不表示集成已接管、主线发布占用已生效、阶段 910 已进入 main 或 #298 已完成。消息送达和集成角色重读前，#299 / #304 继续门控，WT-MAIN 仍由设计角色持有。
