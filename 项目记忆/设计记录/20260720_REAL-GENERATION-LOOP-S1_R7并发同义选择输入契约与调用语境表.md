# REAL-GENERATION-LOOP-S1 R7 并发同义选择输入契约与调用语境表

日期：2026-07-20

事实边界：`main@0747cce` 尚无 #315 路由；路由调用语境来自只读失效证据分支 `R7@4160652`。本表用于约束后继 R8 施工，不把 R7 视作可集成实现。

| 入口 | 调用方 | 输入字段 | 输入来源 | 上游是否保证有效 | 允许逻辑内返回 | 追根因触发条件 | 结构变化 | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `筹办生产根需求` | 后继生产治理宿主 / 当前 #315 自检 | 同一运行期租约、固定生产治理配置 | 已发布上下文 + 固定值式配置 | 生产流程是；外部调用未必 | 外部无效配置/租约、根已满足 | 正式宿主保证后仍无上下文/B0/系统角色或读回结构矛盾 | 经 B0 可能写任务、生命周期、方法和选择；冻结为纯值 | 路由签名、A01—A12、B0 权威读回 |
| `运行期上下文::读取候选业务操作` | 根需求筹办路由 | 同租约候选 B0 | 运行期上下文 | 是 | 租约失效发生在外部材料语境 | 正式有效租约却读不到完整 B0 | 无 | 同租约系统角色和 B0 完整性 |
| `创建已有需求承接任务` | 根需求筹办路由 | 任务主键、虚拟存在主键、初始状态主键、根需求、时间 | 固定配置 + 权威安全根 | 是 | 同义幂等读回、真实写前当前性 | 已提交后任务/承接结构不完整或同键异义半结构 | 可能写任务结构 | 首次、重复、并发单身份 |
| `迁移任务生命周期` | 根需求筹办路由 | 任务、目标阶段、幂等材料编号、发生时间戳 | 当前任务 + 固定配置 | 是 | 完整历史同义读回、同键异义冲突、目标不存在时写前当前性变化 | 完整历史重复/不完整，或提交后缺少唯一同义目标 | 可能写生命周期状态/关系 | 四字段合同、历史同义重入、相邻阶段并发 |
| `创建方法首/结果/动作入口` | 根需求筹办路由 | 方法稳定键、登记根、角色状态、场景、特征、目标值、动作键 | 固定配置 + 系统角色 | 是 | 同义幂等读回、真实当前性变化 | 已声明成功但方法登记/规格读回不完整 | 可能写方法结构 | A06 和方法专项回归 |
| `需求任务方法组合器::召回并提交唯一选择` | 根需求筹办路由 | 任务、登记根、排序版本、召回规则版本、选择记录主键、选择材料号 | 权威任务/方法基线 + 固定配置 | 是 | 无候选、非唯一、真实任务/方法当前性变化 | 内部不一致；有效稳定输入下无选择且不存在设计允许分支；选择半结构 | 可能发布选择记录和关系 | R7 失败复现；R8 A11 + 20/20 |
| `方法召回业务服务::召回` | 需求任务方法组合器 | 任务、登记根、排序规则、召回规则 | 路由固定请求 | 是 | 无候选、非唯一；任务/方法确已漂移 | 输入仍有效、任务/方法稳定且无选择时却入口拒绝；任何内部不一致 | 无，只重建非权威候选 | 状态、候选、建议与调用后权威读回联合复核 |
| `方法召回业务服务::适配唯一建议` | 需求任务方法组合器 | 完整召回结果、选择记录主键、选择材料号 | 同次召回 + 固定配置 | 唯一建议分支是 | 任务已有选择或任务/方法来源版本真实漂移 | 任务/方法稳定且无选择时仍无法形成请求 | 无 | 组合结果 + 权威任务/方法读回 |
| `任务业务服务::提交方法选择` | 需求任务方法组合器 | 完整选择写入请求及任务/方法来源关系版本 | 唯一建议适配 | 是 | 同义幂等读回、写前版本漂移 | 规格形成失败、内部不一致、已提交后当前选择缺失/半结构 | 写选择记录、方法投影关系和发布关系 | 任务专项自检 + 路由调用后读回 |
| `运行期业务操作组合器::读取任务` | 根需求筹办路由 | 任务句柄 | 本轮承接任务身份 | 是 | 任务已完整但生命周期真实改变时可当前性返回 | 未找到、类型不匹配、不完整承接或当前选择半结构 | 无 | `完整承接()`、来源需求、阶段、关系/版本、选择完整性 |
| `运行期业务操作组合器::读取方法登记项` | 根需求筹办路由 | 本轮方法句柄 | 本轮方法首身份 | 是 | 方法完整但生命周期/来源关系真实改变时可当前性返回 | 未找到、类型不匹配或不完整登记项 | 无 | `完整()`、身份、登记根、活跃生命周期关系/版本 |
| `选择与路由输入全字段同义判定` | 根需求筹办路由内部 | 权威任务、权威方法、当前选择、配置、目标根、预期任务/方法 | 同次 B0 读回 + 固定配置 | 是 | 完整但异义选择/来源版本为当前性变化 | 选择存在但不完整，或读回结构本身不完整 | 无 | 记录主键、任务/方法、材料号、两类来源关系/版本、两规则版本逐项断言 |
| `冻结任务执行请求` | 根需求筹办路由 | 调度值、权威任务和选择来源、动作材料 | 同义选择成立后的本地纯值构造 | 是 | 过期或真实当前性变化 | 已冻结结果缺字段、来源不一致或最终报告不完整 | 无，纯值冻结 | A08、A09、A11、相等性和批次 1100 |
| `运行根需求筹办自检` | 入口阶段 1100 | 固定配置、独立生产会话、单线程重复和两线程同义请求 | 自检宏内正式生产入口 | 是 | 无效配置专门用例 | 正常/重复/并发同义报告不完整或身份/来源字段不相等 | 只写自检会话内结构 | Debug/Release 单轮、Debug 连续 20/20、Release 输出隔离 |

契约裁决：组合调用非成功不能直接等同于“并发成功”，也不能一律等同于普通当前性变化。路由必须先保留内部不一致优先级，再用权威任务/方法读回区分同义完成、真实漂移、无候选/非唯一和意外内部矛盾。
