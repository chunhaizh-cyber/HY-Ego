# TASK-RESULT-STABILITY-S2 JY-472 集成归档与非空备份原子替换诊断修订

日期：2026-07-19

状态：正式设计记录 / v0.4 已归档 / v0.5 重新待执行 / WT-313-R5 已预留待创建

## 1. 集成事实

```text
集成起点：8956616719cd9accbc7b4d6d1543851c74a5442d
任务提交：464a6f2c1d5879bed8a65a159ad25ea48dde1d7e
cherry-pick：44fc20c6733279f1e9cb0f352b985ea7c937b9bc
main 发布：0a662d1ff6fb8e67fa756ea0ce64029f9e9883ea
任务记录 blob：4836253bbd4fa7b49dcd1f55f8fb13e9b39bc652
集成记录 blob：a5c700e0cbc088440c312c9fd2c516255110726f
```

INT-WB-DIAG-06-R1 已按修正后的精确起点完成纯文档集成。相对起点最终精确两份记录，C++、工程、入口、计划、索引、队列和中央治理差异为 0；strict、diff、父链、单文件、blob、remote 和 clean 通过。主线发布占用已释放，WT-MAIN、集成 worktree 和 WT-313-R4 均 clean。

## 2. v0.4 归档

第 43 轮产品本身自然退出 0，TASK / B3C / B0 为 18 / 12 / 20 且失败 0。非 Git脚本已经形成下一轮 44 的临时六键状态，但三参数：

```powershell
[System.IO.File]::Replace($TemporaryPath, $StatePath, $null)
```

抛出 `ArgumentException: The path is not of a legal form.`。脚本退出 1，正式游标仍为 43，因此第 43 轮不计有效；正式有效轮仍只有 v0.1 的 1—42。异常不是产品失败或 TASK-RESULT-S1 失败。该证据已进入 main，v0.4 可以归档。

## 3. 运行时二分事实

设计窗口只读核对正式脚本，并在系统临时目录用正式启动器做可丢弃文件探测：

```text
powershell.exe：5.1.26100.8875
CLR：4.0.30319.42000
File.Replace：三参数和四参数重载存在
```

二分结果：

```text
File.Replace(临时, 正式, $null)：复现同一 ArgumentException
File.Replace(临时, 正式, 同目录非空备份)：成功
成功后正式文件=新值
成功后备份文件=旧值
成功后临时源文件=不存在
```

因此缺口不是文件所在卷或正式 / 临时路径不合法，而是该 Windows PowerShell / CLR 运行时不接受空备份路径。

## 4. v0.5 裁决

v0.5 固定：

```text
R5 使用全新非 Git诊断目录，不复用 R4 状态、摘要或备份
只有一个状态替换辅助，样本预检和真实轮共用
File.Replace 第三参数必须是同目录、非空、按轮唯一备份路径
备份保留替换前正式六键状态，不在本轮删除或覆盖
产品启动前必须用同一辅助完成一次可丢弃样本替换
样本和真实轮都核对正式新值、备份旧值、临时源消失
替换调用返回后发生异常时报告三方事实，不自动回滚或补跑
43—60 仍为 18 次独立最小外层调用
```

这不是 v0.4 内部补跑。v0.4 已正式结束且有效游标未推进；R5 是基于同一冻结候选、全新计划版本、worktree 和证据目录，从正式历史下一轮 43 开始的续接。

## 5. 新批次预留

```text
批次：WB-DIAG-07
worktree：WT-313-R5
路径：C:\Users\zhchh\.codex\worktrees\313-task-result-subcondition-r5\海中鱼巢
分支：codex/313-task-result-subcondition-r5
冻结候选：0890464414a8ffd5dfc71dcfc80641d9e4f18181
非 Git目录：D:\海中鱼巣\日志\诊断\TASK-RESULT-STABILITY-S2-R5
最终记录：实施记录/20260719_TASK-RESULT-STABILITY-S2_任务结果结算非空备份原子替换独立单轮诊断_Codex断点清单.md
```

创建前目标路径、本地分支、远端同名分支和 worktree 登记命中均为 0。E217 发布后才创建并核对；创建、S0 消息、修改 / 构建 / 共享时隙和真实轮放行必须分别登记。预留不等于派发。

## 6. 三件套一致性

```text
流程图：流程图/20260715_任务结果完成与需求独立结算代码逻辑流程图_v0.1.md
详细设计：规范/详细设计/任务结果完成与需求独立结算详细设计.md
计划：计划/20260719_TASK-RESULT-STABILITY-S2_任务结果结算非空备份原子替换独立单轮诊断计划_v0.5.md
```

流程图和详细设计继续定义任务结果完成与需求独立结算业务语义；v0.5 不改变业务入口、结构、状态或验收表达式，只治理非 Git诊断脚本的状态替换。三者在业务范围和禁止扩大边界上一致，工具细节由正式实施记录和本设计记录替代新增业务流程图 / 详细设计。

## 7. 边界

本次不修改生产代码、工程或入口，不运行产品或构建，不修复 TASK-RESULT-S1，不解释候选稳定性，不恢复 #310，不解除 #305 / #301 / #306。流程图和业务详细设计仍由既有 TASK-RESULT-S1 产物定义；本轮只修订诊断脚本的文件替换合同。
