# PERSIST-S1-P0-C JY-410 第四次接口漂移发布与独立集成登记

日期：2026-07-18

设计裁决：JY-410

执行队列：#296

设计队列：DQ-188

批次：WB-296-04

## 1. 已验证输入

1. WT-296-R4 位于 `C:\Users\zhchh\.codex\worktrees\296-p0c-r4\海中鱼巢`，分支为 `codex/296-p0c-r4`。
2. 任务提交 `2f9e0bc521635f644d23daacedae1f7e97f22e0d` 的父提交精确为冻结基线 `038ecad93fecef358263846cd68c1b536224734e`。
3. 该提交只修改执行专属断点，没有 C++、工程、计划或中央治理差异。
4. 本地任务分支、同名远端和 HEAD 一致，worktree / index clean；提交推送过程没有人工命令授权。
5. 三文件机械改名草案已完整撤回，不作为实现完成证据。

## 2. 第四次漂移事实

v0.4 假定旧创建型 `概念根类别` 的显式引用闭合在数据操作、服务和专项自检三文件。实际 `海中鱼巣/自检.运行期组合分层.ixx` 直接 import 旧数据操作模块，并在第 199 行构造 `发布概念结构请求` 时写入 `概念根类别::存在`。

只改原三文件后，Debug x64 Rebuild 在该行稳定返回 C2653 / C2065。该调用属于旧创建型概念结构枚举，不是规范签名枚举；执行角色不能改用规范类型、别名、宏或转换绕过。由于第四文件不在 16 项允许范围内，正确收口是撤回草案并退回设计。

## 3. 独立集成登记

```text
工作树：INT-WB-296-04
路径：C:\Users\zhchh\.codex\worktrees\integration-wb-296-04\海中鱼巢
分支：codex/integration-wb-296-04
起点：本设计登记提交发布后的 origin/main
输入：origin/codex/296-p0c-r4@2f9e0bc521635f644d23daacedae1f7e97f22e0d
```

集成只允许任务断点和新建 `实施记录/20260718_WB-296-04_第四次接口漂移断点集成记录.md`。核对父链、单文件范围、C2653 / C2065 正文和草案撤回边界，运行 strict 与 diff。因为最终没有代码或工程差异，不构建、不运行、不登记 890，也不把断点集成声明为 #296 完成。

## 4. 后继设计门禁

断点发布 main 后原地升级 #296 v0.5：

1. 允许范围增加 `海中鱼巣/自检.运行期组合分层.ixx`，总数改为 17。
2. 新增文件只允许把旧请求中的 `概念根类别::存在` 机械同步为 `概念结构根类别::存在`；阶段 770 与其它逻辑不变。
3. S1 验证增加阶段 770，并保持 Debug / Release Rebuild、完整自检和 755。
4. 同步流程图、详细设计、计划和中央路由；使用新批次和新任务 worktree，不复用 WT-296-R4。

#297 和全部恢复后继继续依赖门控。

## 5. 本轮验证

```text
python tools/check_specs.py --strict
git diff --check
git diff --name-only
```

本轮只登记纯断点集成，不修改代码、不构建、不运行、不登记阶段 890。
