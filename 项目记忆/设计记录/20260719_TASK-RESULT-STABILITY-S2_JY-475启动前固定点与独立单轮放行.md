# TASK-RESULT-STABILITY-S2 JY-475 启动前固定点与独立单轮放行

日期：2026-07-19

状态：正式设计记录 / WT-313-R5 S1—S3通过 / 待第43—60轮独立调用

## 1. 启动前固定点

WT-313-R5 已按 E219 完成：

```text
临时差异=仅海中鱼巣/线程/自检.任务结果结算.ixx
临时blob=e817aefd1bd67cf314bdf52c3d16a0742ed1b122
index=空
Debug Rebuild=exit0 / 0 warning / 0 error
构建日志SHA-256=7385C7D73D1653D7EFBAD72A8DC3766F0FCBD0F696195240D4740311CBAAFA42
脚本AST错误=0
脚本SHA-256=FA918DF4376BBE5F07032DFC40F74878A48D583B2493709FCC8209A9F4A37795
File.Replace调用数=1
ValidateOnly=exit0
ATOMIC_REPLACE=通过
LF_CRLF=通过
PRODUCT_STARTED=0
PROCESS_COUNT=0
```

原 A14十四项、A18十一项、两组各100线程、请求、夹具、阶段610和执行顺序未改。

## 2. 原子替换后验

LF 与 CRLF 两份可丢弃样本均通过同一脚本和同一替换辅助：正式样本推进为下一轮44，备份保存旧值下一轮43，临时源消失。两备份路径均为非空、同目录、绝对、互异路径。

正式 `resume-state.tsv` 未参与样本替换，仍为精确六键：冻结候选 `0890464`、冻结源码 blob `74734d0`、历史有效轮 `1-42`、历史摘要 `4F5C5D7B...2EDE`、下一轮43、终止状态未终止。真实轮日志0、真实备份0、summary不存在，产品进程0。

## 3. 共享资源与真实轮授权

E220 发布后，`D:\海中鱼巣\日志\事件段\自检` 由 WT-313-R5 独占。#310、#305 和其它 worktree 禁止启动完整或连续自检，直到 R5 明确释放。

允许目标使用冻结脚本逐次执行第43—60轮。外层每次只允许：

```text
powershell.exe -NoProfile -ExecutionPolicy Bypass -File <run-one-round.ps1> -Round N
```

每次工具调用只运行一个轮号，脚本最多启动一个同步产品进程；禁止跨轮循环、后台进程、脚本自调用下一轮或运行中改脚本。

## 4. 推进与停止

只有产品 exit0、TASK / B3C / B0 行数18 / 12 / 20且失败0、摘要和日志哈希形成、同辅助非空备份原子替换成功、正式 / 备份 / 临时三方后验通过、脚本自身exit0时，正式游标才推进。

任一非零退出、失败叶子、行数不符、构建 / 工具 / 脚本 / 状态 / 备份 / 后验异常或异常终止，必须在首个异常立即停止，不补跑该轮、不启动后继轮。停止后立即释放共享目录，报告正式 / 备份 / 临时三方事实，反向恢复冻结源码 blob，并形成 R5 专属记录。

若43—60全部通过，则累计有效样本为1—60；结论仍只能写“本次冻结候选累计60个完整样本未复现”，不得写稳定或确定因果。

## 5. 边界

本事件不修改产品代码、工程或入口，不运行产品；只登记目标回执并放行后继运行。它不证明43—60通过、不修复TASK-RESULT-S1、不放行#310 / #305 / #301 / #306。
