# JY-436 WB-P0B-01 第一并行批次实际接口复核与正式冻结

日期：2026-07-19

角色：WT-MAIN 设计角色

## 1. 输入与共同前置

`#298 / DQ-190` 已由 INT-WB-298-03-R1 发布 `main@374e153`，JY-435 / E180 已完成归档。当前复核基线为 `main@8dfbfbc == origin/main`，工作树和 index clean。

## 2. #299 当前接口事实

1. `运行期只读查询适配器` 已正式公开租约值构造、`查询概念命名` 和 `复核任务结果`，只持 `运行期上下文租约`。
2. 概念命名桥当前仍持 `概念图服务 + 语素服务 + 邮箱`；任务管理桥当前仍持 `任务服务 + 方法服务 + 状态服务 + 动态服务 + 邮箱`。
3. 概念命名桥共有五个既有自检构造点；任务管理桥共有任务回执自检和多轮自检两个构造点；没有其它生产构造点。
4. 多轮自检只构造并停止任务管理桥，B3 自我治理领域路由仍由同文件独立构造；#299 可以只迁移 B1 构造而不改变 B3 路由语义。
5. 计划 v0.4 的五个代码文件和一个专属记录足以承载迁移；不需要入口、工程、B0 或 B3 文件。

## 3. #304 当前接口事实

1. `主键绑定记录` 当前只含主键和节点句柄；索引仓库与结构写入会话仍提供二参数绑定路径。
2. 显式新运行期生产数据操作共有十二个 `会话.绑定主键` 调用点：存在场景 3、状态动态 4、特征体系 1、语素基础 1、轻量因果 1、需求任务方法 1、用途观察 1。
3. 这些调用点全部位于 #304 允许的七个数据操作模块；系统角色初始化负责组合，但不直接增加第十三个生产绑定调用。
4. 传统服务、入口旧样例和既有自检继续走兼容路径，不属于“显式新运行期未知所有者为 0”的生产统计；#304 不需要扩大到计划禁止文件。
5. 新数据头、索引仓库 / 会话、七数据操作、新自检、工程和入口 970 均由 #304 独占。

## 4. 并行资格

```text
#299 允许文件集合 ∩ #304 允许文件集合 = 0
#299 只消费 #298 已发布 ABI
#304 不消费 #299 未发布 ABI
#299 阶段写入 = 0
#304 阶段写入 = Debug 970
工程 / filters / 入口所有者 = #304
固定集成顺序 = #299 -> #304
```

## 5. 裁决

WB-P0B-01 正式冻结。E181 推送后从同一精确提交创建 WT-299-R1 和 WT-304-R1；创建前路径、本地 / 远端分支与 worktree 登记必须不存在。创建核对后才允许向两个唯一执行任务正式派发。独立集成路径 / 分支先固定身份，精确起点在两个任务提交完成核对后从当时最新 `origin/main` 单独发布，当前不得创建集成 worktree。

## 6. 边界

本记录只证明第一批具备并行资格和创建条件，不证明 worktree 已创建、消息已送达、任务已实施或集成已发布；不解锁第二批和后继串行计划。
