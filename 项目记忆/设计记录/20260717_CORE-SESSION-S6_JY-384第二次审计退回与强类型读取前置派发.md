# CORE-SESSION-S6 JY-384 第二次审计退回与强类型读取前置派发

日期：2026-07-17

设计裁决：JY-384

窗口事件：WIN-0001-E123 / WIN-0001-E124

来源执行队列：#290 / DQ-182 / CAUSAL-USE-O1-S2-D1

新增执行队列：#292 / DQ-184 / CORE-SESSION-S6

状态：#290 第二次审计不通过已接管 / #292 待执行 / #290 v0.3 依赖门控 / 单窗口串行双角色 / 761、880 未登记

## 1. 接管

`87e94b7` 已把 #290 第二次审计按失败收口并推送，工作树干净。审计通过 A01—A06、A08、A12—A16，不通过 A07、A09—A11。失败不是 O1 业务合同冲突，而是两项通用会话能力不足。

## 2. 裁决

1. 新增独立 #292 / DQ-184；不重写已完成 #291 历史。
2. 节点 / 索引批量读取必须显式区分真实空、入口拒绝、资源失败和内部不一致。
3. 会话新增完整主信息值式读取，允许专用数据操作检查整个值容器而不取得仓库或原始令牌。
4. 核心不解释 15 槽、O1、FNV、角色或容量；业务合法性继续后置。
5. #290 保留编号和路径，升级 v0.3 并依赖 #292、761、JY-385；第二次审计证据保持历史只读。
6. #292 首次改变核心失败语义，当前单窗口执行使用高推理；完成后必须先回设计逐签名复核，不能直接消费 #290。

## 3. 三件套一致性

```text
流程图/20260717_不透明结构写入会话完整主信息与强类型投影失败归因流程图_v0.1.md
流程图/20260717_不透明结构写入会话完整主信息与强类型投影失败归因流程图_v0.1.html
规范/详细设计/不透明结构写入会话完整主信息与强类型投影失败归因详细设计.md
计划/20260717_CORE-SESSION-S6_完整主信息值式读取与强类型投影失败归因代码实施切片_v0.1.md
```

三者范围一致，不新建代码模块，不改工程，不扩大到 O1 或恢复。

## 4. 单窗口路由

E123 登记 `87e94b7` 的 #290 失败回执并把当前物理窗口切回设计角色。E124 的设计提交推送成功并重新读取 Git、计划索引、队列、#292、本记录、生成断点和实际接口后，本窗口切执行角色只消费 #292。执行完成或异常仍先提交推送，再切设计角色。

## 5. 完成边界

本记录只形成通用前置设计和执行许可；不证明代码已改、构建已通过、761 已登记、#290 已通过、O1-S2 或 HYSNAP01 已实现。
