# PERSIST-S1-P0-B0 JY-430 既有组合分层自检尺寸同步修订

日期：2026-07-19

角色：WT-MAIN 设计角色

对象：#298 / DQ-190 / PERSIST-S1-P0-B0

## 1. 输入事实

1. WT-298-R2 从冻结基线 `f43832ddcadd079d895765c1abd8e37da21b37e2` 完成 S0，并以纯断点提交 `ef45d2e02e31e2338aba0b3ed78c6daf4cf9ef06` 退回；该提交只新增执行断点，未修改 C++、工程或入口，910 未登记。
2. 当前 `线程/路由.运行期业务请求.ixx` 的请求 / 结果 variant 均为 9 个备选项；#298 目标是请求 14、结果 11。
3. 既有 `自检.运行期组合分层.ixx` 在 Debug 编译中以两条 `static_assert` 固定 9 / 9，同时逐项固定既有备选项 1—8 的类型、顺序、结果索引和原八类同步分派语义。
4. 全仓直接消费者只有生产路由、组合分层自检和 `自检.服务写入兼容边界.ixx`。后者不冻结 variant 总尺寸，不需要修改。

## 2. 裁决

1. #298 原地升为正文 v0.6，定向把 `海中鱼巣/自检.运行期组合分层.ixx` 加入允许文件。
2. 该文件只允许把请求尺寸断言 `9` 改为 `14`、结果尺寸断言 `9` 改为 `11`；两项修改必须与 S5 的正式 variant 扩展在同一步完成。
3. 该文件的备选项 1—8 类型、顺序、结果索引、八类场景正文和其它断言全部只读；A01—A20 新场景正文仍唯一归新建 `自检.运行期兼容请求.ixx`。
4. `自检.服务写入兼容边界.ixx` 不加入允许范围。S0 增加全消费者重扫，若再出现第四个冻结尺寸、类型或索引的消费者，必须再次退回设计。
5. R2 只读退回，不复用、不重置、不 cherry-pick。v0.6 等待全新 WB-298-03 / WT-298-R3 冻结和正式派发。

## 3. 三件套一致性

消费者流程图 v0.6 和恢复详细设计第 21 节已经固定 #298 的实施后请求 14 / 结果 11，以及既有八类语义不回归；它们没有冻结具体自检文件清单。因此本次不改变业务流程、结构所有权、公开 ABI 或上下游路线，只修计划层允许文件、机械步骤和验收门禁。

正式依据：

```text
流程图/20260718_生产运行期公共合同双批并行与值式投影串行汇合流程图_v0.6.md
规范/详细设计/权威状态快照隔离恢复与运行期上下文一次发布详细设计.md 第 21 节
计划/20260718_PERSIST-S1-P0-B0_运行期关键路径值式公开合同代码实施切片_v0.1.md 正文 v0.6
origin/codex/298-p0b0-r2@ef45d2e 执行断点
```

## 4. 验收与边界

本轮执行严格规范检查、`git diff --check`、全消费者静态扫描和中央治理范围检查；不修改 C++、工程或入口，不构建、不运行、不登记 910。

本记录只证明 #298 v0.6 的执行边界已覆盖当前已知编译消费者，不证明 #298 已实施、R3 已创建、后继已解锁或恢复能力形成。
