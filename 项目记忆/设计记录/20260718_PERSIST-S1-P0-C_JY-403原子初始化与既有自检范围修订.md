# PERSIST-S1-P0-C JY-403 原子初始化与既有自检范围修订

日期：2026-07-18

设计裁决：JY-403

执行队列：#296

设计队列：DQ-188

## 1. 正式输入

1. WB-296-02 的任务断点与独立集成记录已由 `889ff28cd6e922d426617e36247e55f0a8f2bcb7` 发布 `origin/main`，C++ 与工程零变化。
2. 阶段 660、680、690 当前均在只初始化系统角色后要求 `运行期上下文::完整()` 为真并发布，必须随 #296 一起迁移。
3. `领域/概念图服务.h` 已声明旧 `概念生命周期阶段`；新旧路径并存时重复同名声明会形成所有权和编译冲突。
4. `状态业务服务` 可形成抽象状态写入规格，`状态动态数据操作::写入抽象状态_会话` 可在既有不透明结构会话中写入；因此三角色和四条生命周期关系无需分成多个已提交事务。
5. `数据操作.概念图结构` 已有模块可达类型，与新数据头组合时存在枚举 / 类型暴露风险；#296 不需要反向 import 该模块即可完成同域重建。

## 2. JY-403 裁决

1. #296 原地升级为 v0.3，不另建替代队列号。
2. 新数据枚举精确命名为 `运行期概念生命周期阶段`，固定 `活跃=1、冷却=2、退役=3`；旧枚举保持旧路径隔离，二者不转换、不比较。
3. 初始化参数固定为版本 1 和活跃 / 冷却 / 退役三个非零、互异稳定主键；活动版本由生产合同固定为 1，不由调用方传入。
4. `概念活动业务服务` 只做业务准入、规格形成和私有状态发布；`概念活动数据操作` 通过一个不透明结构写入会话原子写入三个抽象状态、四条根到活跃状态关系并完整读回后一次提交。
5. `数据操作.概念活动` 不 import 旧 `概念图服务` 或 `数据操作.概念图结构`；先落固定模块骨架并通过 Debug / Release 编译，再迁移正文。
6. fresh 初始化与 restore 重建分离：fresh 不认领预存活动结构，restore 后续由 #228 独立适配完整签名和活动版本。
7. 允许文件增加 660、680、690 三个现有自检模块，固定“系统角色后不完整且发布拒绝 -> 显式活动初始化 -> 完整 -> 原发布 / 租约 / 并发”顺序。

## 3. 非成功返回

| 场景 | 分类 | 收口 |
| --- | --- | --- |
| 参数版本错误、稳定键为零或重复 | 逻辑内返回 | 写前拒绝，结构和私有状态不变 |
| 已发布同参数完整材料 | 逻辑内幂等 | 值式读回，不开启第二事务、不推进版本 |
| 已发布异参数材料 | 逻辑内冲突 | 写前拒绝，不建立第二所有者 |
| 正式 fresh 前置成立后发现角色或活动关系预存 | 追根因解决 | 不认领、不修复，停止 #297 依赖 |
| 会话内任一步写入或读回不一致 | 追根因解决 | 整会话回滚，不留下可读半结构 |
| 固定模块骨架无法按允许范围编译 | 接口漂移 | 写具名断点并退回，不扩文件或导入旧服务 |

## 4. 三层一致性

本轮原地修订：

```text
流程图/20260718_权威状态快照隔离恢复与运行期上下文一次发布流程图_v0.3.md
流程图/20260718_权威状态快照隔离恢复与运行期上下文一次发布流程图_v0.3.html
规范/详细设计/权威状态快照隔离恢复与运行期上下文一次发布详细设计.md 第 17.2、17.2B 节
计划/20260718_PERSIST-S1-P0-C_概念活动唯一所有者代码实施切片_v0.1.md 正文 v0.3
```

三者均限定 #296 只做 fresh 概念活动原子初始化、私有活动材料和上下文完整性门禁；不实现生产首发、快照编码、恢复、旧概念服务迁移或 #214。

## 5. 后继路由

本轮只完成设计修订。#296 状态为“重新待执行 / 待新批次冻结”，阶段 890 未登记；下一治理轮以本次正式提交冻结 WB-296-03，预留新的任务 worktree 和分支，不复用 WT-296-R2。#297 及全部恢复后继继续依赖门控。

## 6. 验证

```text
python tools/check_specs.py --strict
git diff --check
MD / HTML Mermaid 解码后逐字同源
```

本轮不修改 C++ 或工程，不构建、不运行、不登记阶段。
