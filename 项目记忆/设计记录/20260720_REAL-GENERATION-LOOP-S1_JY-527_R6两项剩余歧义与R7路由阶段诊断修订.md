# JY-527 #315 R6 两项剩余歧义与 R7 路由阶段诊断修订

日期：2026-07-20

## 1. R6 正式回执核对

- R6 正式提交：`08431c4dfd986fd4b3e020fe8413d832dec09cd5`；唯一父 `23ca426f924241b91404836a11b76da451a5b348`。
- 固定链：`ca514749 -> 11d83d1 -> 55e360c -> 9fbdc7d -> 23ca426 -> 08431c4`。
- 远端：`origin/codex/315-real-generation-loop-s1-r6@08431c4`；本地 / 远端 `0 / 0`，worktree / index clean。
- 最终提交只修改 #315 专属实施记录；生产和自检净代码差异为 0。
- Debug Rebuild 0 warning / 0 error；产品启动预算 27 / 60，有效完整轮 26，其中 25 轮通过、1 轮真实 A11 失败；另有 1 次平台工具中断。
- 首个真实失败发生在第 27 次启动；失败后新增产品启动 0。
- 证据目录：`D:\海中鱼巣\日志\诊断\REAL-GENERATION-LOOP-S1-R6\E271-WT-315-R6`；manifest 共 38 项，SHA-256 `BB4B67065500A8198BB6186DFCC7B0A0C1CB1D88FA513A0F16E72822A1FC6B8D`。
- 路由恢复 blob：`aa49f237d2ca1500dab31acd55a2c0c38ab50a9f`；自检恢复 blob：`989840db92aa7d6f0672921471f07574397be7e4`。
- 产品 / 构建进程 0，共享目录为空；R6 写入权释放并只读失效，不可集成，不解锁 #316。

## 2. 已排除事实与剩余歧义

首个失败报告为：会话和租约有效；报告 0 已冻结且完整；报告 1 为当前性变化且不完整；两份报告任务 / 方法身份都有效且相同；报告 1 没有选择和冻结请求。

因此已经排除：

```text
会话或租约失败
任务创建或生命周期提前返回
冻结阶段失败
两份完整报告仅值不同
```

仍不能唯一二分：

1. 方法结果或动作入口返回非成功，方法身份虽已写入报告，但方法基线后半段未完成。
2. 方法基线已经完成，召回或唯一选择提交因当前性变化返回非成功。

当前不得把“方法身份有效、选择缺失”直接解释为召回 / 选择竞态，更不得据此修改生产服务、数据操作或门禁。

## 3. 当前代码调用顺序

R6 正式树中的 `筹办生产根需求` 顺序为：

```text
创建方法首
-> 创建方法结果
-> 创建方法动作入口
-> 读取方法登记项并写入报告
-> 联合检查方法结果 / 动作入口 / 方法材料
-> 读取调用前任务当前选择
-> 必要时调用召回并提交唯一选择
-> 把召回结果写入报告并检查组合结果
-> 重新读取任务和当前选择
-> 冻结任务执行请求
```

`召回并提交唯一选择结果` 已公开既有 `召回` 和 `选择` 两个值式结果；`方法召回结果` 已有状态、候选组和唯一建议；任务读回已有 `当前选择`。因此 R7 无需修改服务、组合器或生产 ABI，只需在路由 / 自检既有宏内临时抄录这些现成局部事实。

## 4. 设计裁决

#315 保留队列号和 DQ-207，升为 v0.6 路由阶段诊断。R7 固定带入 R6 五提交，只临时修改：

```text
海中鱼巣/线程/路由.根需求筹办.ixx
海中鱼巣/线程/自检.根需求筹办.ixx
```

临时诊断必须全部处于既有 `HY_EGO_ENABLE_STRUCTURE_COMMIT_FAULT_SELF_TEST` 宏内，至少记录方法结果、动作入口、方法阶段联合校验、调用前当前选择、召回是否调用、召回状态 / 候选数 / 唯一建议、选择提交结果状态、调用后当前选择和最终路由状态。宏关闭时不得留下字段、赋值、输出、ABI 或行为差异。

A11 / A12 原判定、路由分支、调用顺序、返回值、状态映射和生产业务调用次数均不得改变。R7 仍是诊断分支；最终只提交专属实施记录，路由和自检必须恢复为 R6 blob，不进入集成。

## 5. 运行协议修订

R6 的一次长编排命令触发了 300 秒平台工具中断，但没有形成真实产品失败。R7 采用：

```text
总产品启动预算最多 60
每个工具编排批次最多 10 个独立产品进程
每批结束核对产品 / 构建进程为 0、共享目录为空
平台中断计入启动预算但不算有效完整轮
首个真实 A11 失败立即停止
```

该修订只约束验证编排，不改变 A11 / A12 业务语义或正式 20 / 20 门禁。

## 6. R7 预留

```text
批次：WB-REAL-LOOP-07
工作树：WT-315-R7
路径：C:\Users\zhchh\.codex\worktrees\315-real-generation-loop-s1-r7\海中鱼巢
分支：codex/315-real-generation-loop-s1-r7
冻结基线：E272 发布后的 origin/main
固定带入：11d83d1 -> 55e360c -> 9fbdc7d -> 23ca426 -> 08431c4
最终 Git 差异：只允许 #315 专属实施记录
```

E272 发布前不创建 R7。发布后先核对目标路径、本地 / 远端分支和 Git worktree 项均不存在，再从精确 `origin/main` 创建；独立执行代理登记和只读 S0 均须分别形成正式中央提交后才能派发。S0 接受前不 cherry-pick、不修改、不构建、不运行。

## 7. 上游一致性与禁止宣称

R6 没有推翻目标流程图、详细设计、四字段生命周期合同、完整历史幂等或非成功二分语义。本轮只补偏差 D16 和 #315 v0.6 诊断计划，不把未知根因写进目标流程；流程图 / HTML / 详细设计不改。

本记录不证明生产根因已经唯一、代码已经修复、R7 已创建或派发、20 / 20 已通过、任何 R5 / R6 / R7 分支可集成、#315 完成或 #316 解锁。
