# TASK-RESULT-STABILITY-S3 JY-481 基线回归标签漂移裁决与 v0.2 重验

日期：2026-07-19

队列：#314 / DQ-206

工作树：WT-314-R1

状态：验证合同已修订 / v0.2 重新待共享验证

## 1. 具名退回事实

WT-314-R1 在 E225 授权后的首个 Debug 单轮按门禁停止：

```text
产品退出：0
日志：D:\海中鱼巣\日志\诊断\TASK-RESULT-STABILITY-S3-WT314\debug-single.log
SHA-256：54FF94873DC552EF62076B2F1739CAA45F4C4E189CDA7F5CECE7CDCCF1686ED8
SERVICE-DATA-S5：38 行 / 失败 0，A33 / A34 通过
TASK-RESULT-S1：18 行 / 失败 0，A14 / A18 通过
PERSIST-S1-P0-B0：20 行 / 失败 0
P0-B3C / B3C：代码与日志 0 命中
```

执行角色没有启动 Release 单轮或 Debug 连续二十轮，没有补跑；共享目录已释放，进程零。任务 worktree 保留两份未暂存差异，index 空，无实施记录或提交。

## 2. 根因裁决

`P0-B3C` 属于 #310 / WT-310-R1 尚未集成的 WIP 自检，不存在于 #314 冻结基线 `dd9bf97`。#314 的目的又是先纠正同义并发结算归并，再从其集成后的新 main 重验 #310；若要求 #314 先出现 B3C，会形成循环依赖。

同时，当前基线真实 B0 前缀为 `PERSIST-S1-P0-B0-A..`，原验证脚本使用短前缀 `P0-B0-A..`，属于解析合同错误。产品退出零和已存在叶子均通过，不构成产品失败。

## 3. v0.2 修订

只修订 #314 计划的验证合同和同一允许自检文件内 A34 人读标签：

1. A34 标签由“八路同义结算”同步为“百路同义结算”，机器验收继续为局部一百路、一提交、九十九幂等、其它状态零。
2. Debug 任务级回归固定为：
   - `SERVICE-DATA-S5-A01` 至 `A38`：38 行 / 失败 0，A33 / A34 通过；
   - `TASK-RESULT-S1-A01` 至 `A18`：18 行 / 失败 0，A14 / A18 通过；
   - `PERSIST-S1-P0-A-A01` 至 `A18`：18 行 / 失败 0；
   - `PERSIST-S1-P0-B0-A01` 至 `A20`：20 行 / 失败 0。
3. `P0-B3C` 不属于 #314 任务分支验收；它在 #314 独立集成后生成的 #310 新基线重验中验证。
4. E225 的首个 Debug 单轮只作验证合同漂移证据，不计 v0.2 单轮或连续轮。v0.2 必须从新的 Debug 单轮重新开始。

流程图和详细设计没有冻结 B3C 输出，因此不需修改。生产实现、允许 / 禁止文件、ABI、机器结构和业务验收均不变。

## 4. 重新授权边界

E226 发布后可保留当前两文件差异，只在既有自检文件机械同步 A34 标签，然后重新取得共享时隙，执行新的 Debug 单轮、Release 单轮和 Debug 严格二十轮。首异常仍停止释放；全部通过后才写专属记录、精确提交并推送任务分支。

## 5. 完成边界

本裁决只修正不可满足的任务级验证合同，不把旧 Debug 单轮补算为完成证据，不预判 v0.2 验证通过，不放行 #310、#305 或 WB-P0B-02。
