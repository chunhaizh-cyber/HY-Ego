# 规范权威、协作与层级去重审查接管记录

日期：2026-07-21

状态：A—D 已完成；下一批为 E 未登记旧规范逐条映射与退出；DD-05 零写入暂停

## 1. 接管事实

只读检查窗口在 `75244903` 基线上完成三路零写入审查，并在 `main` 前进到 `7b814655d25c2c23602049b0cf4a2d1d6a840890` 后确认被审规则未发生实质变化。当前设计窗口复核到：

```text
main == origin/main == 7b814655d25c2c23602049b0cf4a2d1d6a840890
工作树 clean
DD-05 尚未创建任何新增文件或差异
三个 DD-05 只读子智能体均已零写入结束
```

因此，DD-05 停在可恢复的 P0 末尾。本轮先收敛规则权威、协作边界、规范层级、重复规则和技能越权；不得把规则修订与 DD-05 设计产物放入同一提交。

## 2. DD-05 安全续点

DD-05 已取得的只读结论继续有效，但暂不落成五件套：

```text
DD-05A：通用领域提交、任务验证与结构化回流
-> 复用现有任务结果治理、任务完成和需求独立结算底座
-> 不消费 D455 工作包，不整体依赖 DD-04

DD-05B：D455 工作包方法结果适配
-> 只读消费 DD-04 不可变工作包、三类材料视图、方法内容快照和合同版本
-> 真实依赖 #327 实现、集成和实际接口复核
```

唯一结果主链应保持：

```text
方法结果
-> 对应领域服务准入、正式提交和权威读回
-> 任务目标独立验证
-> 任务完成 / 重筹办 / 等待 / 结构化缺口
-> 任务管理形成唯一强类型上行材料
-> 自我请求需求域复核
-> 需求满足 / 保持未满足 / 下一轮重判
```

现有 `任务管理上行桥` 与 `任务结果治理路由` 不能竞争消费同一工作线程回执；`外部材料适配入口 -> 观察材料准入请求` 不得成为外设旁路。生产连续装配仍归 DD-06。

## 3. 规则治理批次

| 批次 | 唯一目标 | 主要所有权 | 完成门禁 |
| --- | --- | --- | --- |
| A 权威收敛 | AGENTS 统一代理协作权威；退役 0600、0000、0200、0400；稳定编号路径；0300 只留未决冲突；checker 增加直接上级无环检查 | AGENTS、规范目录、0xxx 治理规范、0500、四角色交互规范、checker | 每条退出规则有唯一保留归属；目录和 strict 通过；直接上级图无 SCC |
| B 协作与状态去重 | 四角色交互只管角色状态，多工作树只管 Git / 路径 / 冻结 / 回收；活动索引、队列、状态和窗口记录只留当前入口 | 交互规范、多工作树规范、计划索引、队列、当前状态、窗口记录 | 不再复制通用规则和历史事件流水；当前活动事实仍可完整读回 |
| C 技能修复 | 12 个项目技能只保留触发、角色与权威引用、专用步骤、专用产物和停止条件 | `.codex/skills/*/SKILL.md` | 无已退出路径、虚构脚本、硬编码旧仓库和跨角色写权限 |
| D 正式规范层级与业务去重 | 拆分直接上级、直接依赖、相关边界；修复 5 个 SCC；根规范只保留原则；外设规则唯一归属 | 相关 1xxx—8xxx 正式规范 | 上级图无 SCC；根对象具备独立身份；重复枚举、公式和流程只有一个所有者 |
| E 未登记旧规范迁移 | 先形成 A—D 四组逐条“唯一规则 -> 当前编号规范”映射，再迁入、退出或归档 | 26 份未登记规范及目标正式规范 | 未登记文档不再自称正式；每条唯一规则有正式归属；禁止先删后查 |

各批分别验证、提交并非强制推送。顶层窗口统一串行 Git；同角色子智能体只处理互不重叠切片。A—C 已分别完成并发布；D 批已经把目录收束为 81 份编号规范、20 个对象根，旧关系字段和直接上级循环归零，并通过独立只读终检。E 批尚未开始迁入或退出。

## 4. A 批最小语义保留映射

| 退出或纠偏对象 | 唯一保留归属 |
| --- | --- |
| 0600 的顶层任务树单写角色核心 | `AGENTS.md` |
| 0600 的派发合同、S0、角色切换和跨顶层交接细节 | `规范/设计执行双窗口交互规范.md` |
| 0000 的编号段与层级 | `规范/0100_编号规则与重排预留说明_20260720.md` 与 `规范/规范目录.md` |
| 0200 的覆盖矩阵 | 由目录和根规范派生；不作为正式规范权威 |
| 0400 的新增根对象准入 | `规范/0500_子规范_字段与结构新增准入约束_20260720.md` |
| C001—C012 的已裁决正文 | 对应 31xx、51xx、52xx、53xx、63xx、81xx、82xx 正式规范；0300 只保留编号和目标规范索引 |
| 全局权威顺序 | 仅 `AGENTS.md` 定义；计划和交互文件中的顺序分别只是计划读取顺序和交接证据读取顺序 |

## 5. 完成边界

本记录只证明只读审查已被设计窗口接管、DD-05 已安全暂停并形成 A—E 治理路线。它不证明 A—E 已全部完成，不证明 DD-05 已形成，更不证明任何代码缺口、D455 生产接通或自我内部循环已经完成。
