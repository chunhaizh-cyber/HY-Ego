# JY-522 #315 R5 间歇性并发失败与 R6 分层诊断修订

日期：2026-07-20

## 1. R5 正式回执核对

- R5 正式提交：`de05479a67910a6c7d5adbb7899ef0de25612dda`；唯一父 `855aa60bdf1e1270422e51df1b5a9bd12fb346bf`。
- 固定链：`1e5aefb -> 97a9ff3 -> a8d61b1 -> 855aa60 -> de05479`。
- 远端：`origin/codex/315-real-generation-loop-s1-r5@de05479`；本地 / 远端 `0 / 0`，worktree / index clean，无 upstream。
- 相对冻结基线精确十文件，`git diff --check` 通过。
- 实施记录：`实施记录/20260719_REAL-GENERATION-LOOP-S1_生产根需求承接与任务筹办代码实施_Codex断点清单.md` 第 10 节。
- 证据目录：`D:\海中鱼巣\日志\验证\REAL-GENERATION-LOOP-S1\E265-WT-315-R5`；manifest SHA-256 `23FAB6688717962A008562A9F9331CAA1FDD57C929AB2E1BF5757949E5F3D597`。
- Debug / Release Rebuild 和单轮通过；Debug 严格连续第 1—10 轮通过，第 11 轮 A40 与 A01—A10 通过、A11 / A12 失败；后续按首异常未启动。
- 共享目录空，产品 / 构建进程 0，R5 写入权释放并只读失效，不可集成，不解锁 #316。

## 2. 分层根因事实

现象层和直接生成层已经唯一：第 11 轮只有 A11 / A12 失败，A11 在 `海中鱼巣/线程/自检.根需求筹办.ixx` 把以下条件合并成一个布尔值：

```text
两份报告完整
任务身份相同
方法身份相同
选择记录身份相同
冻结请求完全相同
```

会话启动和租约成立又在 A11 之前作为条件控制是否启动线程，但没有独立输出。失败日志没有报告状态、部分材料或子条件。因此当前只能列出六类同层候选：会话 / 租约、任务 / 生命周期、方法基线、召回 / 选择、冻结、两份完整报告的值差异。

R5 的 A40、两配置单轮和连续前十轮已经证明完整历史幂等路径可成立，但不能证明第十一轮仍必然由同一生命周期点失败。现有 `运行.log` 只记录总自检失败，`事件.log` 无 #315 子条件；日志是症状证据，不能代替机器结构读回。

## 3. 设计裁决

当前不得再次修改任务服务、数据操作、路由或协议。否则会在六个候选之间猜修复，并可能放宽正确的入口门禁。

#315 保留原队列号和 DQ-207，升为 v0.5 分层诊断阶段。R6 只把 R5 正式树机械带入，在既有自检宏内临时拆分 A11 的会话、报告状态 / 完整性、任务、方法、选择和冻结请求子条件；A11 / A12 原判定不变。最多运行 60 个独立 Debug 进程，首个 A11 失败立即停止产品运行，随后恢复临时源码并只提交专属实施记录。

R6 是诊断分支，不是修复或集成输入。即使 60 / 60 未复现，也不能宣称稳定；必须回到设计窗口裁决下一步。

## 4. 上游材料一致性

目标业务语义、四字段生命周期请求、完整历史唯一同义读回和非成功二分口径没有被 R5 失败推翻，因此本轮不改目标流程图、HTML、详细设计、输入契约或二分表，避免把未知根因写成目标语义。

本轮只在 `项目记忆/设计记录/20260719_REAL-LOOP-S0_根需求任务筹办执行学习现状目标偏差清单.md` 新增 D15：现有 A11 聚合输出不能定位并发非成功子层。#315 v0.5、计划索引、任务队列、当前状态、工作树登记和窗口交互同步指向同一诊断范围。

## 5. R6 预留

```text
批次：WB-REAL-LOOP-06
工作树：WT-315-R6
路径：C:\Users\zhchh\.codex\worktrees\315-real-generation-loop-s1-r6\海中鱼巢
分支：codex/315-real-generation-loop-s1-r6
冻结基线：E267 发布后的 origin/main
固定带入：97a9ff3 -> a8d61b1 -> 855aa60 -> de05479
最终 Git 差异：只允许专属实施记录
```

E267 发布前不创建 R6。发布后先核对目标路径、本地 / 远端分支均不存在，再从精确 `origin/main` 创建；新执行代理登记和只读 S0 均须另行提交推送后才能派发。S0 接受前不 cherry-pick、不修改、不构建、不运行。

## 6. 六类证据与禁止宣称

- 输入入口：R5 提交、正式实施记录、第 11 轮日志、连续汇总和 manifest。
- 核心处理：从 A11 总失败回溯到不可区分的子条件，拒绝在证据不足时猜生产修复。
- 结构承载：偏差 D15、#315 v0.5、索引、队列、当前状态、工作树登记、窗口交互和本记录。
- 输出变化：R5 转验证失败只读失效；R6 只预留为分层诊断。
- 调用路径：并发自检 -> 两线程筹办路由 -> 报告部分材料 -> A11 聚合断言。
- 验证方式：临时自检宏分层输出、独立进程最多 60 轮、首失败停止、源码恢复、只提交诊断记录。

本记录不证明生产根因已经唯一、代码已经修正、R6 已创建或派发、20 / 20 通过、分支可集成、#315 完成或 #316 解锁。
