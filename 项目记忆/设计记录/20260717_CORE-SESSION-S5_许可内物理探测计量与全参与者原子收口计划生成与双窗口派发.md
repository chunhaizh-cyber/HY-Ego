# CORE-SESSION-S5 许可内物理探测计量与全参与者原子收口计划生成与双窗口派发设计记录

日期：2026-07-17

执行队列：#291

设计队列：DQ-183

设计裁决：JY-382

关联退回：#290 / DQ-182 / E115

窗口事件：WIN-0001-E116

状态：JY-382 双窗口修订完成 / E116 设计派发 / #291、DQ-183 已派发待执行回执 / #290 保持依赖门控待重审 / 目标为下一独立执行窗口 / C++ 与工程零修改 / 未构建未运行 / 759、880 均未登记

## 1. 接管基线

E115 已以 `b54cd383b58d05560270d3b3d5a18dc03ff659f9` 发布。设计角色接管时：

```text
main == origin/main == b54cd38。
工作树干净。
#290 / DQ-182 保留原路径、未归档，A07—A13 不通过。
旧执行任务 `019f6a26-ef76-73a3-be4f-60e95498dff9` 已由用户强制停止，状态复核为 `idle / interrupted`；本设计窗口独立接管遗留草案并按现行双窗口规则修订，不恢复旧任务、不继承其单窗口切角色口径。
节点 15、关系 18、阶段 880 均未登记。
#214 继续用户暂停；全部 stash 禁止触碰。
```

## 2. 主线程统一裁决

三个只读设计审查分别覆盖：

```text
许可内任意物理主键点查与指定类型—索引值式投影。
部分确认、真实参与者失败、全量反向撤销和撤销失败隔离。
#290、新前置队列、阶段与单窗口角色切换顺序。
```

JY-382 采用一个通用结构闭环前置 #291，而不把缺口塞入 O1-S2：查询与事务收口都属于同一不透明会话 / 执行器边界，必须在一个最终回归阶段 759 中用真实参与者证明。#291 内部分 S1—S5 实施，但不产生中途“部分底座已完成”的扩大声明。

## 3. 新产物

```text
流程图/20260717_不透明结构写入会话许可内探测计量与全参与者原子收口代码逻辑流程图_v0.1.md
流程图/20260717_不透明结构写入会话许可内探测计量与全参与者原子收口代码逻辑流程图_v0.1.html
规范/详细设计/不透明结构写入会话许可内探测计量与全参与者原子收口详细设计.md
计划/20260717_CORE-SESSION-S5_许可内物理探测计量与全参与者原子收口代码实施切片_v0.1.md
项目记忆/设计记录/20260717_CORE-SESSION-S5_许可内物理探测计量与全参与者原子收口计划生成与双窗口派发.md
```

流程图、详细设计和计划的范围一致：

```text
通用许可内点查与投影。
确认保持可逆。
真实参与者四段协议。
无失败最终发布区。
结构许可释放唯一可见点。
撤销失败事务域隔离。
隔离前已登记等待者获锁后二次复核并返回无效。
接域特征值服务读写统一为结构许可 -> 侧表锁，唯一运行期装配传入同一接线。
参与者准备资源 / 竞争拒绝与抛出 / 内部异常唯一分流。
F01—F09 / R01—R06。
不实施 O1、恢复或阶段 880。
```

## 4. #290 修订路由

#290 保留原编号和原计划路径，不归档。E115 的审计矩阵与退回断点保持历史证据，不覆盖、不改写。

本轮只把 #290 改为：

```text
依赖 #291 / DQ-183 / 759 正式完成。
依赖设计角色从 #291 正式提交逐签名复核并发布 JY-383。
在上述两项完成前不可重审。
重审时新增第二次审计矩阵与第二次审计断点，不覆盖 E115 文件。
A01—A16 全部通过前仍不生成 O1-S2 代码计划，不登记 880。
```

## 5. #291 核心合同

### 5.1 查询

```text
读取物理主键当前绑定：空闲 / 已绑定完整节点 / 入口拒绝 / 内部不一致。
读取有效节点与索引投影：指定类型完整节点组 + 指向它们的完整绑定组。
仓库只返回值式副本；会话在同一独占许可下跨仓库互证。
查询只在第一笔候选写入前；无第二许可、无许可外扫描、无仓库 / 令牌外泄。
```

### 5.2 原子收口

```text
节点 / 主信息和关系变更确认后、最终发布前仍可用原能力精确撤销。
执行器顺序：参与者准备 -> 会话确认 -> 参与者确认待发布 -> 无失败发布 -> 许可释放。
最终发布区不得分配、回调、日志、注入或返回失败。
撤销失败时在许可释放前登记协调器域隔离；新请求登记前拒绝，隔离前已登记等待者获锁后二次复核、释放并返回无效。
特征值服务接域生产创建 / 写入先取结构独占许可，七个公开读取先取结构共享许可；侧表锁内不得调用无令牌仓库入口，唯一运行期装配传入同一接线。
参与者准备只有资源 / 竞争拒绝且零可读半结构时可逻辑内返回；抛出、内部不一致或准备后材料异常必须追根因，二者均全撤销。
失败撤销不回拨编号 / 创建序号高水位，只证明可读结构和容量占用无残留。
```

## 6. 当前事实与预期接口边界

当前代码尚无 #291 接口。当前 `特征值服务` 存在侧表锁内无令牌仓库读取的反向锁序，协调器也尚未让隔离前已登记等待者在获锁后二次复核隔离。计划中的中文签名、会话状态机、参与者四段协议、接域特征值锁序、等待者隔离复核和阶段 759 都是预期正式产物，不得在本设计记录中写成已实现事实。

执行窗口接管前必须从包含 E116 的正式设计派发提交逐签名复核。若必须新建代码文件、修改工程 / filters、扩大到 O1 / 恢复 / 线程或改变锁序，执行角色不得修改代码，必须只写 `接口漂移待设计修订` 复核断点并退回 DQ-183。

## 7. 队列与角色

```text
#291 / DQ-183：计划 v0.1 已按双窗口规则修订并登记为 `已派发待执行回执`；目标是下一独立执行窗口，不绑定或恢复旧执行任务。
#290 / DQ-182：依赖门控待重审；不得在 E116 后直接消费。
759：只在 #291 A01—A24 全部通过后登记。
880：继续保留给未来 O1-S2，本轮零登记。
```

本轮由用户明确要求“梳理计划索引、修订计划”，因此设计窗口完成遗留草案审计、计划修订与派发治理。E116 只表示本次设计派发事件；执行权限来自已推送的计划、索引、队列、设计记录和实际接口共同一致，不来自聊天摘要。没有预授权执行批次，#291 完成或退回后必须先交回设计窗口。

#291 执行窗口只写允许清单内的生产代码、自检、入口最小登记及专属实施记录；本计划不授权工程文件。完成后标记 `执行完成待设计同步`，漂移时标记 `接口漂移待设计修订`，提交推送后停止。只有设计窗口读取正式证据并形成 JY-383，才可归档 #291 或重新派发 #290。

## 8. 验证门禁

本设计轮只运行文档验证：

```powershell
python .\tools\check_specs.py --strict
git diff --check
```

另须验证：

```text
新流程图 Markdown / HTML Mermaid 解码后逐字同源。
物理点查空闲 / 已绑定成功边与参与者准备两类非成功均在图中闭合。
#291 / DQ-183 / JY-382 为唯一依赖就绪候选并登记 `已派发待执行回执`；#290 与其它活动计划继续门控。
#290 保留原路径且只变为依赖门控待重审。
新三件套路径在计划、索引、队列、项目记忆和本断点一致。
C++、工程、入口、阶段 759 / 880 零差异。
计划允许 `特征值服务.h` 和 `装配.运行期业务.ixx` 只为接域锁序收口，禁止范围未被其它领域文件扩大。
E115 审计矩阵和退回断点零差异。
#214 与 stash 状态不变。
```

不运行 msbuild，不运行程序或自检。

## 9. 完成声明六项证据

| 维度 | 本轮证据 |
| --- | --- |
| 输入入口 | E115 审计矩阵、退回断点、JY-381 三件套和当前会话 / 执行器 / 仓库 / 参与者接口 |
| 核心处理 | 三项只读审查统一裁决，形成查询、可逆确认、原子发布、隔离和故障矩阵 |
| 结构承载 | 新流程图 MD+HTML、详细设计、#291 计划、本断点及修订后的 #290 治理状态 |
| 输出变化 | #291 v0.1 已按双窗口规则派发给下一独立执行窗口；#290 依赖门控待重审；旧执行任务保持停止 |
| 调用路径 | E115 -> 设计角色 JY-382 -> 强停旧执行任务 -> 设计窗口审计遗留草案 -> E116 双窗口派发 -> 独立执行窗口 #291 -> 设计窗口逐签名复核 -> 后继重审 #290 |
| 验证方式 | Mermaid 同源、strict、diff、唯一登记、范围与 C++ / 工程 / 阶段零差异 |

## 10. 禁止宣称

本轮只能声明通用前置三件套和 #291 v0.1 已完成双窗口修订、计划索引与队列已梳理并形成设计派发，#290 仍为依赖门控待重审。不得声明 #291 已实施、759 已登记、#290 已重新审计或通过、O1-S2 代码计划已生成、节点 15 / 关系 18 / 15 槽 / 容量 / 恢复已实现，或任何统计、学习、生产、自动行动和旧能力迁移已完成。
