# JY-541 #315 WT-315-R7 S6 回执接受与 R7 只读失效裁决

日期：2026-07-20

## 1. 前置

JY-540 / E285 已由 `c69f376a44d9215c93ec8ba815db2d6d3b458edf` 非强制发布到 `origin/main`。发布后，设计窗口只授权替代唯一代理 `/root/wt315_r7_execution_v2` 更新 #315 专属实施记录、运行静态门禁并非强制推送同名 R7 分支。

## 2. S6 正式结果

```text
正式提交：4160652079d7296264b902dfb98cc29eab855769
唯一父：d36cb4c6d8ee6b7e0ec9d78ac54ad41c030cbf2b
远端：origin/codex/315-real-generation-loop-s1-r7@4160652
本地 / 远端：0 / 0
worktree / index：clean
提交范围：只修改 #315 专属实施记录
strict：通过
产品 / 构建进程：0
共享目录项目数：0
```

专属实施记录新增第 12 节，完整记载五提交固定带入、S2 两 patch / 临时 blob、S3 Debug 0 warning / 0 error、S4 6 / 60 及唯一返回点、S5 两源码恢复、14 项证据 SHA、D01—D14 和完成边界。记录没有猜测生产修复。

## 3. 设计窗口独立复核

设计窗口独立 fetch 同名远端并确认：

- 本地与远端精确同为 `4160652079d7296264b902dfb98cc29eab855769`，ahead / behind 为 0 / 0。
- 唯一父为 `d36cb4c6d8ee6b7e0ec9d78ac54ad41c030cbf2b`。
- 提交只修改 `实施记录/20260719_REAL-GENERATION-LOOP-S1_生产根需求承接与任务筹办代码实施_Codex断点清单.md`，新增 156 行。
- task worktree / index clean，strict 未发现问题。
- 路由 / 自检保持恢复 blob `aa49f237...a9f` / `989840db...e4`，14 / 14 非 Git 证据与资源状态不变。

因此正式接受 S6。

## 4. R7 裁决

WT-315-R7 已完成 v0.6 路由阶段诊断，唯一返回点为：

```text
并发线程甲形成任务当前选择
-> 线程乙进入方法召回服务并重新读取同一任务
-> 命中任务已有当前选择入口门禁
-> 召回返回入口拒绝
-> 路由映射当前性变化并在调用后权威任务读回前返回
```

R7 写入权立即释放并登记为只读失效。R7 只保存诊断提交和非 Git 证据，不创建集成身份，不进入 `main`，不得继续修改、构建、运行或复用为生产修复 worktree。

该裁决不证明并发语义已经修复，不证明正式 Debug 20 / 20，不证明 #315 完成，不解除 #316 门控。

## 5. 后继设计路线

设计窗口继续完成：

1. 修订当前状态流程，明确旧路由在召回入口拒绝后未做调用后权威任务读回。
2. 修订目标流程，固定并发同义调用发现“另一线程已经形成同一任务 / 方法选择”时，必须权威读回、核对同义选择并继续形成等价完整报告；异义选择、任务 / 生命周期漂移或不完整读回仍按当前性变化或内部不一致收口。
3. 同步修订详细设计、逐行代码映射、输入契约 / 调用语境、非成功返回二分表和偏差清单。
4. 把 #315 升为新计划版本，明确允许 / 禁止文件、入口拒绝、失败收口、验证矩阵和全新 worktree 身份。
5. 从新 `origin/main` 冻结基线创建全新修复 worktree；不得在 R7 追改生产代码。

## 6. 六类证据

- 输入入口：E285 云端提交、R7 S6 正式提交 / 远端、专属实施记录和非 Git 证据。
- 核心处理：接受 S6 单记录分支收口，释放 R7 写入权，路由到生产修复设计。
- 结构承载：#315 计划、计划索引、任务队列、并行工作树登记表、当前状态、窗口交互记录和本设计记录。
- 输出变化：R7 从 S6 待收口推进为诊断证据完成 / 只读失效；#315 回到设计修订。
- 调用路径：E285 发布 -> S6 单记录提交 / 推送 -> 设计独立复核 -> JY-541 / E286 -> 并发同义收口三件套修订。
- 验证方式：提交父链、单文件范围、远端 0 / 0、strict、clean、恢复 blob、14 项证据和资源 0。

## 7. 完成边界

本记录不允许集成 R7，不完成 #315，不解除 #316。E286 后当前唯一工作是设计窗口修订生产并发同义收口的上游材料和新计划版本。
