# PERSIST-S1-P0-C JY-406 第三次接口漂移发布与独立集成登记

日期：2026-07-18

设计裁决：JY-406

执行队列：#296

设计队列：DQ-188

批次：WB-296-03

## 1. 已验证输入

1. WT-296-R3 位于 `C:\Users\zhchh\.codex\worktrees\296-p0c-r3\海中鱼巢`，分支为 `codex/296-p0c-r3`。
2. 任务提交 `8ff3c2bc153e71b18b2af639fa4208361c4b9fb4` 的父提交精确为冻结基线 `d18a1047df5fa3ab5202451af3b9cc8aef90b1c3`。
3. 该提交只修改 `实施记录/20260718_PERSIST-S1-P0-C_概念活动唯一所有者代码实施_Codex断点清单.md`，没有 C++、工程、计划或中央治理差异。
4. 本地任务分支、`origin/codex/296-p0c-r3` 和 HEAD 一致，worktree / index clean；提交推送过程没有人工命令授权。
5. 执行草案曾完成 S1 Debug / Release x64 Rebuild 0/0 和 S2 Debug 增量构建 0/0，随后在 S3 装配汇合点命中 IFC 冲突；这些草案已完整撤回，不作为实现完成证据。

## 2. 第三次漂移事实

#296 v0.3 要求新活动状态直接复用 `概念图算法.h` 唯一拥有的 `概念根类别` 与 `概念签名材料`，同时运行期业务装配必须继续拥有既有概念图结构服务。

当前导入图存在两个同名但不同实体：

```text
领域/概念图算法.h：enum class 概念根类别 : std::uint32_t
领域/数据操作.概念图结构.ixx：enum class 概念根类别 : std::uint8_t
```

新数据操作没有 import 旧概念图结构模块，能够独立编译；两条依赖在 `装配.运行期业务.ixx` 首次汇合后，MSVC 先报 C1117 同名符号冲突。仅隐藏显式签名后仍报 C4789，证明两个不同宽度实体已经通过 IFC 到达同一模块依赖图，不是补 include 或调整声明顺序可以修复的问题。

当前 v0.3 禁止修改旧概念结构数据操作、服务和专项自检。执行角色若继续，只能越权修改旧模块或放弃规范签名唯一所有者，因此正确停止在 S3 并完整撤回草案。

## 3. 独立集成登记

按多工作树规范登记：

```text
工作树：INT-WB-296-03
路径：C:\Users\zhchh\.codex\worktrees\integration-wb-296-03\海中鱼巢
分支：codex/integration-wb-296-03
起点：本设计登记提交发布后的 origin/main
输入：origin/codex/296-p0c-r3@8ff3c2bc153e71b18b2af639fa4208361c4b9fb4
```

集成只允许任务断点和新建 `实施记录/20260718_WB-296-03_第三次接口漂移断点集成记录.md`。集成角色核对父链、单文件范围、C1117 / C4789 漂移正文和草案撤回边界，运行 strict 与 diff。因为最终没有代码或工程差异，不构建、不运行、不登记阶段 890，也不把断点集成声明为 #296 完成。

## 4. 后继设计门禁

断点发布 main 后，设计角色必须主动复核旧模块全部引用，再原地升级 #296 v0.4：

1. 定向允许 `领域/数据操作.概念图结构.ixx`、`领域/服务.概念图结构.ixx`、`领域/自检.概念图结构分层.ixx` 做旧内部类型机械改名。
2. 旧内部类型固定改名为 `概念结构根类别`；底层值、字段语义、关系 9 / 10 / 11 写入读回和公开业务行为保持不变。
3. 旧内部类型不得转换、比较或合并为 `概念图算法.h::概念根类别`；规范签名仍只有 `概念图算法.h` 一个所有者。
4. 新增静态门禁：全仓不得再有第二个 `enum class 概念根类别` 定义；装配同时导入新旧生产依赖后 Debug / Release 均须生成 IFC。
5. 同步流程图 MD / HTML、详细设计、计划、索引、队列、登记表、当前状态、决策与窗口交互；使用新批次和新任务 worktree，不复用 WT-296-R3。

#297 和全部恢复后继继续依赖门控。

## 5. 本轮验证

```text
python tools/check_specs.py --strict
git diff --check
git diff --name-only
```

本轮只登记纯断点集成，不修改代码、不构建、不运行、不登记阶段 890。
