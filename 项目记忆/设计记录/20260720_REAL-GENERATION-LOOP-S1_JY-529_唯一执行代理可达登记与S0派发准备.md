# JY-529 #315 WT-315-R7 唯一执行代理可达登记与 S0 派发准备

日期：2026-07-20

## 1. 前置

JY-528 / E273 已由 `d2b24be56bde36e1e51d7985875b4a7e22be003d` 非强制发布到 `origin/main`。R7 已创建在冻结基线 `4993a6dd205222e2841fa678ce99639b6cf8b8c2`，分支、clean、无 upstream、远端不存在和资源门禁已经核对。

## 2. 候选窗口创建与回执

设计窗口创建独立候选代理 `/root/wt315_r7_execution`，只要求可达性回执，不授权读取仓库或运行工具。代理明确回复：

```text
canonical task name：/root/wt315_r7_execution
保持待命
尚未进入或读取 R7
未运行 Git / 构建 / 程序
未修改、暂存、提交或推送
未开始 S0 或消费 #315
```

因此本轮只把该代理登记为 R7 唯一目标。代理尚无 R7 写入权，也没有共享目录授权。E273 发布后 R7 相对新 `origin/main` 仅落后 1 个中央治理提交、领先 0；该中央差异不进入任务分支。

## 3. E274 后正式 S0 合同

E274 发布后才向唯一代理发送只读 S0。S0 必须固定在：

```text
C:\Users\zhchh\.codex\worktrees\315-real-generation-loop-s1-r7\海中鱼巢
```

核对：R7 HEAD / 父 / 分支 / clean / upstream / 远端 / 与冻结基线关系；R6 五提交父链和远端 `08431c4`；相对冻结基线的预期带入范围与中央治理零回退；正式实施记录第 11 节；R6 证据目录 38 项 manifest 及 SHA；失败字段与两项剩余歧义；路由 / 自检恢复 blob；路由中的方法结果 -> 动作入口 -> 方法校验 -> 召回选择 -> 任务读回 -> 冻结顺序；v0.6 临时 / 最终文件范围、宏关闭门禁、阶段 1100、进程 0 和共享目录未占用。

S0 不得 cherry-pick、修改、构建、运行、暂存、提交、推送或占用共享目录。只有 S0 PASS 经设计窗口读取接受并再次提交推送后，才可另行授予 S1—S6。

## 4. 六类证据

- 输入入口：E273 云端提交、R7 创建记录和代理可达回执。
- 核心处理：创建独立代理并确认 canonical 身份与零动作边界。
- 结构承载：计划、索引、队列、工作树登记、当前状态、窗口交互和本设计记录。
- 输出变化：R7 从已创建待目标代理推进为唯一目标已登记、S0 未派发。
- 调用路径：E273 发布 -> 创建候选代理 -> 代理零工具回执 -> E274 登记。
- 验证方式：canonical task name 与未进入 / 读取 / 运行 / 修改 / 提交 / 推送的明确回执。

本记录不证明 S0 已发送或通过，不授予 R7 写入权，不证明 R6 提交已带入、诊断代码、构建、运行、根因或集成，也不解锁 #316。
