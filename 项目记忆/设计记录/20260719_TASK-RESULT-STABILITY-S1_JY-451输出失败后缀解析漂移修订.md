# TASK-RESULT-STABILITY-S1 JY-451 输出失败后缀解析漂移修订

日期：2026-07-19

状态：正式设计记录 / #312 v0.2 重新待执行

## 1. 漂移事实

WT-312-R1 在纯只读 S0 中确认身份、父链、四文件范围、三组前缀和 18 / 12 / 20 行数结构均一致，但失败后缀存在两种正式格式：

```text
海中鱼巣/线程/自检.任务结果结算.ixx：TASK-RESULT-S1 使用 ": 通过/失败"
海中鱼巣/自检.运行期兼容请求.ixx：PERSIST-S1-P0-B0 / B3C 使用 "=通过/失败"
```

#312 v0.1 把三组行合并后统一按 `: 失败$` 解析，会漏记 B3C / B0 失败叶子，也无法形成计划要求的三组独立失败计数。非零退出虽可能停止循环，但不能替代完整证据合同。

## 2. 修订

#312 升为 v0.2，三组分别解析：

```text
TASK-RESULT：: 失败$
B3C：=失败$
B0：=失败$
```

摘要必须保存三组独立叶子数和失败数；停止条件逐组判断。行数前缀、60 轮上限、轮间 2 秒、完整输出先落盘、首个异常即停和其它边界不变。

## 3. 身份与重试

WT-312-R1 仍是 `0890464`、clean、无 upstream、远端同名不存在；本轮未构建、未运行、未写文件，因此不重建 worktree，不改变冻结候选。E196 推送后向同一目标任务发送 v0.2 重读消息；读回一致后再单独授予共享时隙。

## 4. 完成边界

本修订只修复诊断解析合同，不修改任何代码、自检输出或业务语义，不证明 TASK-RESULT 稳定或 #310 因果，不恢复 #310 正式验收，不解锁后继。
