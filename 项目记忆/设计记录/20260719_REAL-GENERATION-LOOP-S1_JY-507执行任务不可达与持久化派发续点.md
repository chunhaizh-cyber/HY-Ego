# JY-507 #315 执行任务不可达与持久化派发续点

日期：2026-07-19

## 1. 前置事实

- JY-506 / E251 已由 `7b6e941ff2cf0a7eeb7d9feb9d25c34e32d2ff5f` 推送 `origin/main`。
- WT-315-R2 已在新电脑重建为 `codex/315-real-generation-loop-s1-r2@0981e8a`，clean、无 upstream、远端 0/0、七文件一致。
- 本轮没有修改 R2，没有运行构建、自检或产品，没有占用共享目录。

## 2. 消息工具事实

设计窗口向登记任务 `019f46a8-7cd7-7622-88c4-fc45cc5ea016` 发送只读 S0 消息时，工具返回失败，没有消息 ID 或送达回执。

随后查询 REAL-GENERATION-LOOP-S1 相关线程，只返回当前设计窗口 `019f7a86-dd7e-7513-855b-e12d2e21f11d`；登记的旧执行任务未出现在当前电脑可达线程中。结合用户已切换电脑的说明，当前不能证明旧任务仍可接收消息。

## 3. 裁决

```text
WT-315-R2 已创建待派发
消息工具不可用 / 目标任务不可达
S0 未派发
非执行中
不可修改 / 构建 / 运行 / 提交 / 推送 / 集成
#316 继续门控
```

本记录作为消息不可用时的持久化替代续点，但不替代真实派发，不得写成 `已派发待执行回执`。

后继只有两条合法路径：

1. 用户打开或指定当前电脑可达的独立执行窗口后，设计窗口把 E251 的只读 S0 原样发送到该唯一目标并读回。
2. 用户明确要求当前物理窗口切换为执行角色，并完成项目规定的正式角色切换登记；裸 `继续` 或当前“设计窗口”声明不构成切换。

## 4. 完成声明六类证据

- 输入入口：E251、重建后的 WT-315-R2、登记任务 ID、线程消息与列表查询结果。
- 核心处理：尝试真实派发，核对失败后停止，不把持久化记录冒充送达回执。
- 结构承载：计划索引、任务队列、当前状态、并行工作树登记表、窗口交互记录和本设计记录。
- 输出变化：#315 保持已创建待派发，新增具名消息不可达续点；R2 和 #316 状态不变。
- 调用路径：E251 发布 -> send_message 失败 -> list_threads 只见当前设计窗口 -> E252 持久化替代回执。
- 验证方式：工具返回、线程列表、R2 Git 身份 / clean / 0/0、strict、diff 和 staged 范围检查。

## 5. 本轮边界

本轮只写消息不可达的设计治理记录；不创建新线程，不切换角色，不修改或运行 R2，不创建集成身份。
