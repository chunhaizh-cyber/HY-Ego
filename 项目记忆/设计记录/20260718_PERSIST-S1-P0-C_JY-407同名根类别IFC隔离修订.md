# PERSIST-S1-P0-C JY-407 同名根类别 IFC 隔离修订

日期：2026-07-18

设计裁决：JY-407

执行队列：#296

设计队列：DQ-188

计划版本：v0.4

## 1. 正式输入

1. WB-296-03 第三次接口漂移断点和集成记录已由 `ab5fbfce96be4a63b069fdc2a93e5b3d1c33dc0a` 发布 `origin/main`。
2. WT-296-R3 的代码、工程和三个新文件草案已全部撤回；当前 main 没有 #296 代码产物，阶段 890 未登记。
3. 执行证据证明新模块自身可完成 S1 / S2 编译，阻断只在旧概念结构依赖与新活动依赖于运行期业务装配汇合后出现。

## 2. 当前实体矩阵

| 所有者 | 类型 | 底层类型 | 显式引用范围 | 裁决 |
| --- | --- | --- | --- | --- |
| `领域/概念图算法.h` | `概念根类别` | `std::uint32_t` | 规范签名、初始化、旧概念服务、显示与安全删除等正式根语义 | 保持唯一规范所有者，不修改 |
| `领域/数据操作.概念图结构.ixx` | `概念根类别` | `std::uint8_t` | 旧创建型概念结构数据操作、服务和专项自检三文件 | 改名为 `概念结构根类别` |

对当前源码的定向 `rg` 证明，旧模块实体的显式类型名只需在以下三文件机械同步：

```text
海中鱼巣/领域/数据操作.概念图结构.ixx
海中鱼巣/领域/服务.概念图结构.ixx
海中鱼巣/领域/自检.概念图结构分层.ixx
```

装配、系统角色初始化、发布组合器和运行期组合自检只消费旧模块导出的对象 / DTO，不直接命名旧根类别，不进入改名范围。

## 3. v0.4 裁决

1. 保留 #296、DQ-188 和阶段 890，不拆出新的业务前置计划。
2. 第 4 节允许文件从 13 项扩为 16 项，仅增加上述三个旧概念结构文件。
3. 旧 `概念根类别` 统一机械改名为 `概念结构根类别`；底层 `uint8_t`、0—4 数值、字段语义、关系 9 / 10 / 11、顺序号、事务、读回、故障、并发和阶段 755 不变。
4. 禁止用 `using` 别名、宏、重复枚举、模板隐藏或转换层保留同名；全仓必须只剩 `概念图算法.h` 一个 `enum class 概念根类别` 定义。
5. 先完成旧类型改名并通过 Debug / Release Rebuild 与阶段 755，再建立新活动模块；任何旧专项行为变化都在新实现前追根因。
6. `概念结构根类别` 不进入新活动 DTO，不与规范 `概念根类别` 转换、比较或合并。

## 4. 三层同步

已同步：

```text
流程图/20260718_权威状态快照隔离恢复与运行期上下文一次发布流程图_v0.3.md
流程图/20260718_权威状态快照隔离恢复与运行期上下文一次发布流程图_v0.3.html
规范/详细设计/权威状态快照隔离恢复与运行期上下文一次发布详细设计.md 第 17.2C 节
计划/20260718_PERSIST-S1-P0-C_概念活动唯一所有者代码实施切片_v0.1.md 正文 v0.4
```

三层都固定先隔离旧内部类型，再实施 fresh-only 单会话原子初始化；均不迁移旧 `概念图服务.h`，不实现生产入口、restore 或快照编码。

## 5. 后继路由

#296 v0.4 为重新待执行，但本次修订提交不直接构成任务 worktree 执行许可。下一步另以 JY-408 从本提交冻结 WB-296-04、登记新 WT-296-R4 路径 / 分支 / 16 文件所有权；创建核对和正式派发必须再独立发布。旧 WT-296-R3 与 INT-WB-296-03 可回收但本轮不删除。

#297 及全部恢复后继继续依赖门控，#300 / #214 继续用户暂停。

## 6. 验证

```text
python tools/check_specs.py --strict
git diff --check
MD / HTML Mermaid 同源
```

本轮只修订设计、计划和中央治理，不修改 C++ / 工程，不构建、不运行、不登记阶段 890。
