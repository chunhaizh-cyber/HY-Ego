# JY-524 #315 WT-315-R6 唯一执行代理可达登记与 S0 派发准备

日期：2026-07-20

## 1. 前置

JY-523 / E268 已由 `3d921b0df42ce4ece07a54fa21eb5bdd480c9910` 非强制发布到 `origin/main`。R6 已创建在冻结基线 `ca514749fabcb11fdc0b60c56ec7f7f9eb7b86fb`，分支、clean、无 upstream、远端不存在和资源门禁已经核对。

## 2. 候选窗口创建与回执

设计窗口创建独立候选代理 `/root/wt315_r6_execution`，只要求可达性回执，不授权读取仓库或运行工具。代理明确回复：

```text
canonical task name：/root/wt315_r6_execution
保持待命
尚未进入或读取 R6
未运行 Git / 构建 / 程序
未修改、暂存、提交或推送
未开始 S0 或消费 #315
```

因此本轮只把该代理登记为 R6 唯一目标。代理尚无 R6 写入权，也没有共享目录授权。

## 3. E269 后正式 S0 合同

E269 发布后才向唯一代理发送只读 S0。S0 必须固定在：

```text
C:\Users\zhchh\.codex\worktrees\315-real-generation-loop-s1-r6\海中鱼巢
```

核对：R6 HEAD / 父 / 分支 / clean / upstream / 远端 / 与冻结基线关系；R5 四提交父链和相对 `1e5aefb` 的精确十文件；正式实施记录第 10 节；E265-WT-315-R5 二十份日志和 manifest；A40、两配置单轮、Debug连续1—10通过及第11轮A11/A12失败；`根需求筹办报告` 的状态、任务、方法、选择和冻结请求字段；`自检.根需求筹办.ixx` 既有宏内分层观测能力；v0.5临时/最终文件范围、阶段1100、进程0和共享目录未占用。

S0 不得 cherry-pick、修改、构建、运行、暂存、提交、推送或占用共享目录。只有 S0 PASS 经设计窗口读取接受并再次提交推送后，才可另行授予 S1—S6。

## 4. 六类证据

- 输入入口：E268 云端提交、R6 创建记录和代理可达回执。
- 核心处理：创建独立代理并确认 canonical 身份与零动作边界。
- 结构承载：计划、索引、队列、工作树登记、当前状态、窗口交互和本设计记录。
- 输出变化：R6 从已创建待目标代理推进为唯一目标已登记、S0 未派发。
- 调用路径：E268 发布 -> 创建候选代理 -> 代理零工具回执 -> E269 登记。
- 验证方式：canonical task name 与未进入 / 读取 / 运行 / 修改 / 提交 / 推送的明确回执。

本记录不证明 S0 已发送或通过，不授予 R6 写入权，不证明 R5 提交已带入、诊断代码、构建、运行、根因或集成，也不解锁 #316。
