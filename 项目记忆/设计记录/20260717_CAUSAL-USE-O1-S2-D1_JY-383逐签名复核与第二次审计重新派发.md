# CAUSAL-USE-O1-S2-D1 JY-383 逐签名复核与第二次审计重新派发

日期：2026-07-17

设计裁决：JY-383

窗口事件：WIN-0001-E122

来源执行队列：#291 / DQ-183 / CORE-SESSION-S5

恢复执行队列：#290 / DQ-182 / CAUSAL-USE-O1-S2-D1

状态：设计复核通过 / #291 已完成归档 / #290 v0.2 重新待执行 / 单窗口串行双角色 / 不登记 880

## 1. 接管事实

```text
main == origin/main == df31ea899206b3638cc6bbff3432a18264021085。
工作树在设计接管前干净。
#291 v0.6 实施记录状态为“执行完成待设计同步”。
阶段 759 / CORE-SESSION-S5 已在入口唯一登记。
A01—A24、协调器隔离 6 / 6、FEATURE-VALUE-TXN-S1-A15、Debug / Release、两配置完整自检、最终 Debug 20 / 20、strict、diff 与允许范围均有正式实施证据。
节点 15、关系 18、15 槽、O1 索引 / 容量、HYSNAP01 和 880 均未新增。
```

正式执行证据：

```text
实施记录/20260717_CORE-SESSION-S5_许可内物理探测计量与全参与者原子收口代码实施_Codex断点清单.md
计划/已完成计划/20260717_CORE-SESSION-S5_许可内物理探测计量与全参与者原子收口代码实施切片_v0.1.md
```

## 2. 逐签名复核

| JY-382 假定 | `df31ea8` 当前签名 / 结构 | 裁决 |
| --- | --- | --- |
| 同一会话按任意物理主键点查 | `会话.结构写入.ixx::读取物理主键当前绑定(std::uint64_t)` 返回强类型四态结果 | 一致 |
| 指定类型有效节点与索引值式投影 | `会话.结构写入.ixx::读取有效节点与索引投影(节点类型)`；节点仓库 `读取指定类型有效节点记录组`；索引仓库 `读取全部主键绑定组` / `读取节点主键组` | 一致 |
| 完整来源 / 目标关系材料 | 会话 `读取关系记录组` / `读取来源关系记录组` 通过同一令牌读取完整关系记录 | 一致 |
| 会话确认保持可逆 | 会话 `完成确认` 进入已确认待发布，`完成撤销` 接受写入中 / 确认中 / 已确认待发布 | 一致 |
| 无失败最终发布 | 会话 `完成发布() noexcept`；执行器在会话与参与者均确认后依次完成发布 | 一致 |
| 真实参与者四段 | 特征值原始材料参与者提供 `准备提交`、`确认待发布`、`完成发布() noexcept`、`完成撤销` | 一致 |
| 任一失败全量撤销 | 执行器汇总参与者撤销与会话撤销，固定继续尝试其余逆操作 | 一致 |
| 撤销失败域隔离 | 结构事务接线把 `标记撤销失败隔离` 纳入完整接域；执行器在许可析构前调用并返回内部不一致 | 一致 |
| 接域特征值锁序 | 特征值服务写入口先取独占许可，读入口先取共享许可，锁内仓库访问复用该令牌 | 一致 |
| 正式运行期接线 | `装配.运行期业务.ixx` 已把正式结构接线传入特征值服务 | 一致 |

## 3. 设计裁决

1. #291 的正式产物满足 #290 A07—A13 所需的通用接口前置；未发现需要修订 JY-382 通用合同的接口漂移。
2. #291 / DQ-183 移入已完成计划；759 保持正式登记。
3. #290 / DQ-182 原编号、原路径保留，内部版本升级为 v0.2，状态改为 `重新待执行`。
4. #290 仍是纯文档审计，只允许新增第二次审计矩阵和第二次审计断点；不得修改被审计三件套、代码、工程、E115 历史证据或登记 880。
5. JY-383 只解除前置门控，不预判 #290 A01—A16。任一审计项不通过仍须退回设计，不得以“大部分通过”生成 O1-S2 代码计划。

## 4. 单窗口与模型路由

当前物理窗口继续使用单窗口串行双角色模式。本轮为设计角色；E122 设计提交推送成功并重新读取 Git、计划索引、任务队列、#290 v0.2、本记录、#291 正式实施记录和实际接口后，才切换为执行角色。

#290 是成熟的纯文档检查表，可由中等推理执行模型机械执行。出现以下任一情况时升级回设计 / 高推理角色，不允许执行角色自行修订计划或扩大文件范围：

```text
正式签名相对本记录漂移。
三件套范围冲突或 Mermaid 不同源。
A01—A16 同一事实出现互斥结论。
需要修改两份允许审计记录以外的文件才能继续。
连续两轮不能按检查表形成唯一结论。
```

## 5. 三件套范围一致性

```text
流程图/20260717_权威用途观察结构承载与事务发布恢复流程图_v0.1.md
流程图/20260717_权威用途观察结构承载与事务发布恢复流程图_v0.1.html
规范/详细设计/权威用途观察结构承载与事务发布恢复详细设计.md
计划/20260717_CAUSAL-USE-O1-S2-D1_权威用途观察结构承载与事务发布恢复实施前设计审计计划_v0.1.md
```

四者范围一致：节点 15、关系 18、15 槽、单索引双证、FNV 有界探测、派生容量、专用数据操作、唯一许可释放发布点和 HYSNAP01 原地九段仍只是 O1 设计候选；#291 只提供通用底座；#290 只审计，不实施。

## 6. 设计轮验证

```text
python .\tools\check_specs.py --strict
git diff --check
```

本设计轮不运行 msbuild，不运行程序，不修改 C++ / 工程，不操作 stash。

## 7. 完成声明边界

本记录只证明 #291 正式接口与 JY-382 假定逐签名一致，并据此重新发布 #290 第二次纯文档审计。不得声明 #290 已通过、O1-S2 已实现、节点 15 / 关系 18 / 15 槽 / O1 索引 / 容量 / HYSNAP01 已落地、880 已登记，或统计、学习、生产接线、自动行动与旧能力迁移已经完成。
