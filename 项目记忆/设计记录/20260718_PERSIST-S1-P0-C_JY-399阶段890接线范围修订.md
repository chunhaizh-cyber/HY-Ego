# PERSIST-S1-P0-C JY-399 阶段 890 接线范围修订

日期：2026-07-18

状态：设计修订完成待提交 / #296 v0.2 重新待执行 / 阶段 890 未登记

## 1. 正式输入

1. `70534d7fd3e9531690d1ee1ee1113bf53a011afb` 已把 WB-296-01 的执行断点和独立集成记录发布到 `origin/main`。
2. `海中鱼巣/自检.运行器.ixx` 当前只导出通用 `自检运行器`、`阶段化自检总成` 和合同自检，不 import 或登记任何领域专项。
3. `海中鱼巣/入口.cpp` 当前统一拥有专项自检 import、值式结果槽、`登记自检(最终回归, 顺序, ...)` 和最终具名门禁表；660、680、690、880 均按该模式接线。
4. #296 v0.1 把阶段 890 修改权错误授予运行器并禁止入口，因此无法完成完整自检接入；生产接口、四仓库、状态业务服务、概念图结构数据操作和上下文目标合同未发现其它漂移。

## 2. 裁决

不新增自检聚合前置计划。#296 保留原队列编号并原地升级 v0.2：

```text
撤销：海中鱼巣/自检.运行器.ixx 修改权。
新增：海中鱼巣/入口.cpp 四点最小 Debug 接线权。
点 1：现有 HY_EGO_ENABLE_STRUCTURE_COMMIT_FAULT_SELF_TEST 条件内 import 新自检模块。
点 2：同一条件内增加一个 自检单元结果 值式结果槽。
点 3：880 后以 890 一次登记 PERSIST-S1-P0-C，并只调用独立自检真模块。
点 4：把登记、最终回归已执行和结果总通过并入现有最终具名门禁表。
```

入口不得承载 A01—A18 场景正文、并发循环或逐项输出；Release 不 import、不登记、不包含专项文本。生产模块不反向依赖自检；工程仍只按 ClCompile 登记真模块。

## 3. 三层一致性

已同步：

```text
流程图/20260718_权威状态快照隔离恢复与运行期上下文一次发布流程图_v0.3.md
流程图/20260718_权威状态快照隔离恢复与运行期上下文一次发布流程图_v0.3.html
规范/详细设计/权威状态快照隔离恢复与运行期上下文一次发布详细设计.md 第 17.2A 节
计划/20260718_PERSIST-S1-P0-C_概念活动唯一所有者代码实施切片_v0.1.md 正文 v0.2
```

范围仍只建立概念活动唯一所有者、确定性重建视图、上下文完整性和 Debug 890 验收；不接生产启动、不迁移旧概念图服务、不编码快照、不接 O1、不恢复 #214。

## 4. 批次与后继

WB-296-01 已完成纯断点集成并释放写入所有权；旧 WT-296 和 INT-WB-296-01 均 clean，可回收但本轮不删除。v0.2 不复用旧任务 worktree，必须在本设计提交发布后以其精确 `origin/main` 哈希登记 WB-296-02、新任务分支和新 worktree，再重新派发。#297 继续依赖门控。

## 5. 验证

```text
python tools/check_specs.py --strict
git diff --check
MD / HTML Mermaid 同源检查
入口与运行器接线静态核对
```

本轮不修改 C++ / 工程，不构建、不运行、不登记 890。
