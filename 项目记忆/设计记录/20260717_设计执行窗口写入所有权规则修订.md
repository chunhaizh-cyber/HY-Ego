# 设计执行窗口写入所有权规则修订记录

日期：2026-07-17

状态：规则已修订 / 本记录随本轮规则提交发布 / 不构成代码实施许可

## 1. 用户裁决

```text
“制定窗口”统一称为“设计窗口”。
设计窗口负责计划、计划索引和任务队列的读写。
执行窗口只读计划、计划索引和任务队列，负责按计划实施并写执行专属实施记录。
设计窗口读取实施记录后更新计划、计划索引、任务队列和归档状态。
执行窗口可以按正式状态计算下一计划，但不得只凭聊天上下文或消息顺序。
运行中收到的新计划消息排队到下一轮，不中断当前切片、不形成执行许可。
```

## 2. 本轮规则落点

```text
AGENTS.md
规范/设计执行双窗口交互规范.md
规范/000_项目规则总纲.md
规范/001_规则迁移清单.md
tools/check_specs.py
```

新增硬边界：

```text
设计窗口独占计划、计划索引、任务队列、窗口交互和设计治理状态写入。
执行窗口独占计划允许范围内的代码实施和本次执行专属实施记录写入。
新设计生成 / 审查记录写入 项目记忆/设计记录/。
旧 实施记录/*计划生成* 只保留历史事实。
默认逐计划交回设计同步；预授权执行批次必须显式登记。
模型选择不改变角色权限。
```

## 3. 中断任务隔离

原活动任务已被用户强制停止，Codex 任务状态已核对为 `idle / interrupted`。该任务遗留的 E116 / #291 计划、队列、流程图、详细设计和项目记忆差异全部保留原样，本轮不读取其 stash、不修改其内容、不暂存、不提交。

由于 `计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/当前状态.md`、`项目记忆/决策记录.md` 和 `项目记忆/窗口交互记录.md` 已含 E116 未提交差异，本轮只以本独立设计记录承载规则修订事实，待 E116 由其原范围另行恢复或裁决后，再由设计窗口按新规则同步中央汇总状态。

## 4. 验证

```text
python -B .\tools\check_specs.py --strict
git diff --check -- <本轮规则文件>
检查脚本 Python 语法校验
目标规则文件与 E116 遗留文件暂存范围隔离检查
```

本轮不修改 C++、工程或自检，不运行构建，不证明任何代码实施、业务闭环或旧能力迁移完成。
