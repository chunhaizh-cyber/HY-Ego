# CORE-SESSION-S5 已确认候选自检合同修订及第四次重新派发

日期：2026-07-17

执行队列：#291

设计队列：DQ-183

设计裁决：JY-382 v0.6

窗口事件：WIN-0001-E121

执行基线：`532e3e59020b4df2f0ef749f190eaafce98394fd`

用户裁决：继续由当前物理窗口串行承担设计和执行

状态：v0.5 M0—M2 已完成 / `CORE-UNPUBLISHED-S1` 具名失败已接管 / 同一 #291 原地升级 v0.6 / 当前设计角色完成后切回当前物理窗口执行角色 / 759、880 未登记 / #290 继续门控

## 1. 正式输入

执行角色已经提交并推送 `532e3e5`，正式证据为：

```text
实施记录/20260717_CORE-SESSION-S5_许可内物理探测计量与全参与者原子收口代码实施_Codex断点清单.md
海中鱼巣/入口.cpp
海中鱼巣/核心/自检.未发布身份候选.ixx
```

已验证事实：

```text
main == origin/main == 532e3e5，工作树干净。
入口旧程序通过长链共有 141 个叶子，新具名表共有 141 个值，逐项比较完全一致。
最终汇总和具名表均只有一份，失败时输出全部假门禁。
Debug x64 Rebuild 通过，0 warning / 0 error；git diff --check 通过。
完整 Debug 自检退出 1，顶层假门禁为“概念命名第一切片通过”。
中央失败编号为 CORE-UNPUBLISHED-S1。
759、880 未登记，#290 未消费。
```

因此 M0、M1、M2 已经完成；v0.6 从 M3 的具名来源修复继续，不重复改造入口具名表。

## 2. 根因裁决

`海中鱼巣/核心/自检.未发布身份候选.ixx:75-79` 仍断言：节点和主信息候选确认后，撤销必须返回 `已拒绝`，且记录继续可读。

#291 的现行生产合同已经固定：

```text
确认只把候选推进到已确认待发布，不清除精确撤销材料。
同一原许可在最终发布前仍可按原能力和完整句柄精确撤销。
最终发布关闭会话撤销入口；原许可释放后，后继许可不能复用旧能力。
```

现有失败是既有自检合同滞后，不是生产接口再次漂移。不得回退节点 / 主信息候选生产语义，也不得删除中央登记、强制门禁为真或削弱完整自检。

## 3. JY-382 v0.6 两候选机械合同

只允许修改 `自检.未发布身份候选.ixx` 第 3 项场景，固定步骤如下：

```text
候选甲：在同一原共享许可内创建节点 / 主信息候选并完成确认。
候选甲撤销：按节点 -> 主信息顺序使用原能力与原令牌撤销，两项均为已完成；两个完整句柄均不可读。

候选乙：在同一原共享许可内创建并确认，但不撤销；把节点能力、主信息能力、两个完整句柄和原许可序号保留到作用域外。
后继共享许可：证明许可序号不同；用后继令牌撤销候选乙两项能力均为已拒绝；两个记录仍逐句柄可读且内容不变。
最终清理：释放后继共享许可，再取得独占许可，按节点 -> 主信息普通删除候选乙并证明两个句柄不可读。
```

保持不变：

```text
其它 11 项验收表达式与语义。
std::array<bool, 12> 总数。
模块名和 CORE-UNPUBLISHED-S1 中央编号。
节点 / 主信息 / 关系仓库、会话、执行器和入口生产语义。
工程登记、阶段 759 和 A01—A24 总数。
```

## 4. 允许范围与模型边界

在 v0.5 允许范围上只新增：

```text
海中鱼巣/核心/自检.未发布身份候选.ixx
```

该文件只允许按第 3 节同步第 3 项。执行角色先复核 `CORE-UNPUBLISHED-S1`，再运行完整 Debug 自检；该项通过后继续 #291 其余 R01—R06、Debug / Release、两配置完整自检、最终 Debug 20 / 20、strict、范围验证和 759 收口。

这组步骤已机械化，可由中等推理执行模型按检查表完成；若实际签名不同、需要改生产代码才能让第 3 项符合合同、出现新的计划外具名失败、或同一失败按合同重复后根因仍不明，执行角色停止并退回高推理设计角色。模型升级不改变角色和允许范围。

## 5. 单窗口路由

用户再次明确由单窗口设计和执行。当前角色是从 `532e3e5` 正式退回记录切换后的设计角色。

```text
E121 设计提交推送成功
-> 当前物理窗口重新读取 Git、计划索引、任务队列、#291 v0.6、实施断点和实际接口
-> 切换为执行角色
-> 从 M3 两候选自检步骤继续 #291
-> 完成或再次异常时只写执行专属证据并提交推送
-> 再从正式记录切回当前物理窗口设计角色
```

不向当前任务发送自发自收消息。外部执行任务继续无写入权。#290 继续等待 #291、759 和 JY-383，不得消费；880 不登记。

## 6. 本设计轮边界

本轮只修改 CORE-SESSION-S5 流程图 MD / HTML、详细设计、同一 #291 计划、计划索引、任务队列、当前状态、待确认问题、决策、窗口交互、验收记录和本设计记录。

以下内容相对 `532e3e5` 保持零差异：

```text
海中鱼巣/**/*.h
海中鱼巣/**/*.cpp
海中鱼巣/**/*.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
实施记录/20260717_CORE-SESSION-S5_许可内物理探测计量与全参与者原子收口代码实施_Codex断点清单.md
```

设计角色不运行 msbuild 或程序，不登记 759 / 880，不归档 #291，不消费 #290。

## 7. 设计验证

```text
CORE-SESSION-S5 流程图 Markdown / HTML Mermaid 解码后逐字同源。
python -B .\tools\check_specs.py --strict
git diff --check
#291、DQ-183、JY-382 v0.6、E121、重新待执行和当前物理窗口目标角色在中央路由一致。
C++、工程和执行专属断点相对 532e3e5 零差异。
```

## 8. 完成声明六项证据

| 维度 | 本轮证据 |
| --- | --- |
| 输入入口 | `532e3e5`、v0.5 M0—M2 回执、`CORE-UNPUBLISHED-S1` 与旧第 3 项断言 |
| 核心处理 | 区分已确认待发布可逆合同与原许可释放后的跨许可拒绝合同，并拆成两候选检查表 |
| 结构承载 | CORE-SESSION-S5 流程图 / 详细设计、#291 v0.6、中央路由和本记录 |
| 输出变化 | 新增一份既有自检允许文件；同一 #291 / DQ-183 重新待执行；单窗口切回执行角色 |
| 调用路径 | `532e3e5` 退回 -> 设计接管 -> JY-382 v0.6 / E121 -> 正式推送 -> 当前窗口重读并执行 M3 |
| 验证方式 | Mermaid 同源、strict、diff、路由一致性和 C++ / 工程 / 执行断点零差异 |

## 9. 禁止宣称

本轮只证明具名失败已经获得精确自检合同和执行许可。不得声明 `CORE-UNPUBLISHED-S1` 已通过、#291 已完成、759 已登记、#290 已重审或通过、O1-S2 已获得代码许可，或任何旧能力迁移已经完成。
