# DD-05F0 v0.2 自检超时退回与 v0.3 诊断续作设计记录

日期：2026-07-22

状态：设计裁决；JY-575 / E316 派发准备范围

## 1. 输入事实

- 计划 / 队列：`#329 / DQ-221 / DD-05F0 v0.2`
- 任务提交：`f3dc981c20903f17f39f60934c4be49242e6e7e5`
- 分支：`codex/329-dd05f0-feature-current-txn-r2`
- 远端与本地提交一致，worktree / index clean，残留进程 0。
- Debug x64 Rebuild、strict 91 项、diff、静态门禁和禁止范围通过。
- Debug 完整自检固定 180 秒超时；Release 和完整 A01—A17 未执行。

## 2. 根因证据链

```text
L0 现象：Debug --self-test-exit 180 秒未退出
-> L1 直接阶段：入口阶段 740 调用 运行特征体系分层自检
-> L2 本地事实：约 11.965 秒进入该调用，剩余约 168 秒未返回
-> L3 候选：旧 CRUD / 故障、旧并发 join、新批次、双参与者、账重读与 A17 竞争
-> L4 生命周期：结构写入许可、候选、参与者准备 / 确认 / 发布或撤销
-> L5 合同：A16 要求完整自检 180 秒内退出码 0
```

已证实的第一违反点只有 A16 失败。当前没有证据证明具体死锁语句或业务根因。`SERVICE-DATA-S3` 和 `DD05F0-Axx` 输出集中、未强制刷新，强杀后的缺失行不能排除已经执行；旧自检中的无界 `thread::join()` 只是候选。

## 3. 设计裁决

1. 4170、详细设计、生产 MD / HTML 流程图和函数知识图谱的机器语义不因本次超时修改。
2. 计划验证合同存在确定缺口：只有整进程 180 秒边界，没有立即刷新的分段开始 / 完成、稳定复现和首个未返回符号门禁。
3. #329 升为 v0.3。先在 Debug 自检内建立 P0—P7 临时检查点，两轮粗分段稳定后只细分命中段，再以两轮相同结果证明首个未返回符号。
4. 未取得稳定叶子前禁止修改业务逻辑、延长超时或宣称死锁根因。稳定叶子位于原白名单且不改变正式合同，才允许定向修复。
5. 最终必须删除临时检查点并重跑 Debug / Release 完整 180 秒自检及原 A01—A17；局部诊断不能替代 A16。

## 4. R2 续作裁决

本次是验证失败，没有证明 R2 的路径、分支、父链、冻结接口或所有权失效。复用 `WT-329-R2@f3dc981c...`，不创建 R3，不允许任务分支自行 merge / rebase / cherry-pick main。

如果诊断需要扩大允许文件、改变正式接口 / 模块依赖、修改运行器 / 入口或接入新 main 基线，R2 停止并由设计窗口另行建立 WB-DD05F0-03 / WT-329-R3。当前 R2 WIP 不解除 #330 或 #333 门控，不进入集成。

## 5. 完成和禁止声明

当前只能声明 v0.2 已形成可编译白名单 WIP并命中 Debug 完整自检超时，设计已形成 v0.3 诊断续作合同。不得声明具体根因已确认、A01—A17 已通过、分支完成、main 已实现、#330 / #333 已解门或 DD-05 已接通。
