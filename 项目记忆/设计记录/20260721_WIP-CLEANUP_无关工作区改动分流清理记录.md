# WT-MAIN 无关工作区改动分流清理记录

日期：2026-07-21

状态：已完成

## 1. 用户授权与范围

用户明确授权对 WT-MAIN 的无关 WIP 进行提交、合并或删除。本次只处理清理前可见的以下六类状态，不扩大到 #323—#328 的代码实施、集成发布或其它 worktree：

```text
海中鱼巣/界面/控制面板窗口.cpp
规范/3200_根规范_任务_20260720.md
历史对话记录/20260717_每日任务复盘报告.md
历史对话记录/20260718_每日任务复盘报告.md
历史对话记录/20260719_每日任务复盘报告.md
日志/
```

## 2. 只读审计结论

1. 控制面板 C++ 为 `+150/-23` 的未登记、未构建、未运行草案。它过滤语素关系子项但仍把这些节点计入投影数量，并缺少现行详细设计要求的稳定句柄回退，不能直接并入 main。
2. 3200 根规范的规范化对象哈希与 `HEAD` 相同，正文无差异；显示修改仅来自 LF / CRLF 工作树状态漂移。
3. 三份每日复盘共 34,900 字节、582 行，均明确是人读治理材料；凭据扫描未发现私钥、令牌、密码或 JWT。仓库已有同系列 20260716 报告提交，可按同一边界归档。
4. `日志/` 共 86 个文件、30,584,952 字节。其中 78 个 `.log` 已被既有规则忽略，Git 可见的 3 个 `.patch` 与 5 个 `.txt` 是 #315 R6 / R7 非 Git 诊断证据。正式记录要求保留这些证据，不得删除或改写，也不宜提交包含源码补丁和本机路径的材料。

## 3. 分流处置

```text
控制面板未验证草案
-> 恢复到 HEAD，不形成代码提交

3200 换行伪差异
-> 恢复到 HEAD，不形成规范正文提交

20260717—20260719 三份每日复盘
-> 清理 20260718 第 3 行一处行尾空白
-> 作为人读历史归档精确提交

日志/
-> 保留物理目录和全部证据
-> .gitignore 新增 /日志/
-> 不提交、不删除、不改写日志内容
```

## 4. 验证与边界

本次实际验证结果：

1. `git status --short --untracked-files=all` 不再出现控制面板、3200 或 `日志/`；
2. `git diff --exit-code -- 海中鱼巣/界面/控制面板窗口.cpp 规范/3200_根规范_任务_20260720.md` 返回 0，两个工作树文件与 `HEAD` 无差异；
3. `git check-ignore -v` 证明诊断 `.patch` 与 `.txt` 由 `.gitignore` 的 `/日志/` 规则命中，物理文件未删除；
4. `git diff --check` 通过，`python .\tools\check_specs.py --strict` 返回 86 份目录项全部通过；
5. 不运行构建或产品程序，因为本设计治理切片不保留任何 C++ 修改；
6. 本次清理不证明 #324 已发布 main，也不解除 #328 的接口漂移或 #325—#327 的依赖门禁。
