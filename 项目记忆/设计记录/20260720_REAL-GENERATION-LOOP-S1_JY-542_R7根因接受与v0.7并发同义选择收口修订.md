# REAL-GENERATION-LOOP-S1 JY-542 R7 根因接受与 v0.7 并发同义选择收口修订

日期：2026-07-20

角色：WT-MAIN 设计窗口

对象：#315 / DQ-207 / REAL-GENERATION-LOOP-S1

## 1. 输入事实

```text
main == origin/main@0747cce491c3c6bbf37dbd49f61046b4097edee0
main 不含 路由.根需求筹办.ixx 和 自检.根需求筹办.ixx
R7@4160652079d7296264b902dfb98cc29eab855769
R7 远端 0/0、clean、资源 0、写入权释放、只读失效、不可集成
R7 源码已恢复：路由 aa49f237...50a9f；自检 989840db...7e4
R7 非 Git 证据：14 / 14 SHA 保持不变
```

R7 S4 使用 6 / 60 次启动：001—005 通过；006 退出 1，A01—A10 通过、A11/A12 失败；诊断行 1，失败后新增启动 0，平台中断 0。manifest SHA：

```text
A4D329C145E675A54034C3A2D1824AF9BF28500774534627F2E2A91A7061B41D
```

## 2. 根因裁决

唯一最窄返回点成立：

```text
线程乙路由旧任务快照没有当前选择
-> 进入召回并提交唯一选择组合器
-> 方法召回服务重新读取同一任务
-> 线程甲已发布当前选择
-> 召回因“任务已有当前选择”返回默认入口拒绝
-> 组合器无选择请求且整体非成功
-> 路由把入口拒绝映射为当前性变化
-> 在调用后任务权威读回之前提前返回
```

因此 `report1_post_choice_present=0` 不是权威任务读回；该字段未到赋值点，只保留默认值。

## 3. 设计裁决

最窄修复固定在路由层：

1. 不修改方法召回服务、组合器、任务服务或数据操作。
2. 不新增重试、等待、睡眠、第二次召回或第二次选择提交。
3. 组合内部不一致优先追根因，不能被并发选择掩盖。
4. 其它组合非成功后通过同一 B0 权威重读任务和方法。
5. 当前选择完整且记录主键、任务/方法、幂等材料、两类来源关系/版本、两规则版本和候选数量与本路由完全同义时，作为并发同义完成读回继续冻结。
6. 完整异义选择或任务/方法真实漂移返回当前性变化；选择半结构、读回不完整或稳定输入下入口拒绝后仍无选择按内部不一致追根因。
7. 无候选/非唯一只有在权威读回仍无选择时保留原逻辑返回。

## 4. 设计产物

```text
流程图/现状流程图/20260720_根需求任务筹办并发同义选择收口现状流程图_v0.2.md/.html
流程图/20260720_根需求任务筹办并发同义选择收口施工流程图_v0.2.md/.html
项目记忆/设计记录/20260720_REAL-GENERATION-LOOP-S1_R7并发同义选择逐行代码映射表.md
项目记忆/设计记录/20260720_REAL-GENERATION-LOOP-S1_R7并发同义选择输入契约与调用语境表.md
项目记忆/设计记录/20260720_REAL-GENERATION-LOOP-S1_R7并发同义选择非成功返回二分审查表.md
项目记忆/设计记录/20260720_REAL-GENERATION-LOOP-S1_R7并发同义选择现状施工偏差清单.md
规范/详细设计/根需求任务筹办执行与方法学习生产闭环详细设计.md v0.3
计划/20260719_REAL-GENERATION-LOOP-S1_生产根需求承接与任务筹办代码实施切片_v0.1.md 正文 v0.7
```

现状图明确 main/R7 双基线，施工图、详细设计和计划均只授权路由/自检语义变更，范围一致。

## 5. 固定源树与 R8

R8 必须从发布本设计包后的新 `origin/main` 建立，不能复用 R7。生产源树只按 `23ca426` 的十个具名 blob 恢复；恢复后七个非目标生产文件冻结，只有路由、自检和 #315 专属实施记录可继续修改。

预留身份：

```text
批次：WB-REAL-LOOP-08
worktree：WT-315-R8
分支：codex/315-real-generation-loop-s1-r8
路径：C:/Users/zhchh/.codex/worktrees/315-real-generation-loop-s1-r8/海中鱼巢
冻结基线：本设计包提交并推送后的 origin/main 精确 SHA，创建时回填
目标角色：新的独立执行窗口
```

预留不等于创建或派发。必须先发布本设计包，再核对路径、本地分支、远端同名分支和 worktree 项不存在，创建并回读身份；另行提交推送登记后创建唯一新执行窗口，取得待命回执；再次登记推送后才能派发 S0。

## 6. 当前状态

```text
#315：v0.7 设计修订完成待机械校验与 E287 发布
#316：继续依赖门控
R7：只读失效 / 不可集成
R8：仅预留，尚未创建，尚未派发
当前设计窗口：不修改 C++，不运行构建或产品
```

## 7. 完成声明边界

JY-542 只证明 R7 根因已转化为一致的流程图、详细设计和可执行计划。它不证明修复已实施、R8 已创建/派发、20/20 通过、任务分支完成、独立集成、#315 完成或 #316 解锁。
