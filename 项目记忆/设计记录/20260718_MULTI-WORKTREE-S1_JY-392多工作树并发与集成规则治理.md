# MULTI-WORKTREE-S1 JY-392 多工作树并发与集成规则治理

日期：2026-07-18

设计裁决：JY-392

队列：#308 / DQ-200

窗口事件：WIN-0001-E133

状态：规则治理完成 / #296 未消费 / 当前保持设计角色 / 待提交推送闭合

## 1. 输入事实

1. `main@f184007` 时 WIN-0001-E132 把当前窗口派向 #296 执行角色。
2. 用户在代码实施前明确授权修改规则并允许多 worktree。
3. `4a89c16` 已发布执行前改向断点；#296 代码、工程、自检和阶段 890 均未发生变化。
4. 旧规则把 `D:\海中鱼巣` 和 `main` 同时固定为唯一写入位置，无法合法建立 Git worktree 并发。

## 2. 裁决

```text
D:\海中鱼巣 + main = 主集成工作树与最终权威
codex/<队列号>-<主题> = 独立任务分支
独立目录 = 任务 worktree
codex/integration-<批次号> = 集成暂存分支 / worktree
任务验证 -> 分支完成待集成
集成验证 -> main 非强制发布
设计同步 -> 完成归档
```

并发准入必须同时证明依赖同基线满足、文件集合互斥、结构和接口所有权不冲突、共享文件唯一所有者和固定集成顺序。不能满足时继续串行。

## 3. 当前输出

1. 新增 `规范/多工作树并发与集成规范.md`。
2. 新增 `项目记忆/并行工作树登记表.md`，当前仅登记 WT-MAIN，无任务 worktree 或活动并行批次。
3. #308 / DQ-200 已作为纯规则治理计划完成归档。
4. #296 保持依赖就绪候选，但写入前必须新增任务 worktree、任务分支、冻结基线和文件所有权登记。
5. 本轮不创建分支或 worktree，不改 C++ / 工程，不构建、不运行、不登记 890。

## 4. 完成声明六项

| 维度 | 证据 |
| --- | --- |
| 输入入口 | 用户授权、`f184007`、`4a89c16`、E132、#296 和旧单工作树规则 |
| 核心处理 | 把单 worktree 改为主集成、任务和集成暂存三类；建立并行准入、冻结、集成、失败与回收合同 |
| 结构承载 | AGENTS、四份规范、四个项目技能、并行登记表、#308、JY-392 和中央项目记忆 |
| 输出变化 | 允许受控多 worktree；main 仍唯一最终权威；#296 等待正式工作树派发 |
| 调用路径 | `执行前用户改向 -> JY-392 设计治理 -> E133 提交推送 -> 后继设计角色登记实际并行批次` |
| 验证方式 | strict、diff、范围扫描；四技能 quick_validate 因两个 Python 环境均缺 PyYAML 未加载，按脚本规则人工 frontmatter 校验通过；C++ / 工程 / 流程图 / 领域详细设计零变化 |

## 5. 完成边界

本轮只建立并发治理能力。没有创建实际任务 worktree，没有开始 #296，没有证明任何迁移切片完成，也没有形成性能提升数据。
