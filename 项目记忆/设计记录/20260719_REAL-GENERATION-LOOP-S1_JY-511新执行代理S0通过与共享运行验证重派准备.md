# JY-511 #315 新执行代理 S0 通过与共享运行验证重派准备

日期：2026-07-19

## 1. 正式改派与 S0 回执

- JY-510 / E255 已由 `52c0b5365e0118a1ad93e51c9cad69b27250aa56` 发布 `origin/main`。
- 只读 S0 已正式派发给唯一代理 `/root/wt315_r2_execution`；代理没有继承旧线程结论，已独立返回 `PASS`。
- 旧线程 `019f7acd-cc6c-7342-a018-214880665da2` 继续保持后继所有权撤销和只读失效。

## 2. 独立 S0 证据

```text
路径：C:\Users\zhchh\.codex\worktrees\315-real-generation-loop-s1-r2\海中鱼巢
分支：codex/315-real-generation-loop-s1-r2
HEAD：0981e8a89ddb6ba1b853b3b68a65092037088ed1
父提交：57cacbc78cd4aa40e5cd85791baabb80a43e2c48
worktree / index：clean
upstream：无
远端任务分支：0/0
云端 main：52c0b5365e0118a1ad93e51c9cad69b27250aa56
产品 / 构建进程：0
共享事件段目录：未授时、未占用、未访问
```

新代理从 E255 中央路由独立确认：两根身份和首轮安全根消费、四字段生命周期请求、任务服务内部当前关系 / 版本封存、数据操作写前复核、其余 B0、召回四字段、稳定键隔离、工程模块顺序、1100 唯一和七文件范围全部一致；禁止依赖与计划外差异为 0。

本轮未修改、构建、自检、运行、写断点、暂存、提交、推送或创建集成身份。因此 #315 进入新代理 `已派发待执行回执`；#316 继续门控。

## 3. E256 后共享验证合同

E256 发布后才向 `/root/wt315_r2_execution` 发送验证消息。代码固定点 `0981e8a` 冻结，不修改六份代码 / 工程 / 入口。

```text
共享目录：D:\海中鱼巣\日志\事件段\自检
验证目录：D:\海中鱼巣\日志\验证\REAL-GENERATION-LOOP-S1\E256-WT-315-R2
```

消息送达到明确释放期间仅 WT-315-R2 独占共享目录。固定顺序：

1. 全新 Debug x64 Rebuild，退出0、0 warning、0 error。
2. Debug 单轮完整 self-test，退出0；`REAL-GENERATION-LOOP-S1-A01—A12` 恰好12项且全部通过。
3. 全新 Release x64 Rebuild，退出0、0 warning、0 error。
4. Release 单轮完整 self-test，退出0；`REAL-GENERATION-LOOP-S1` 专项输出0行。
5. Debug 从新第1轮严格连续20/20，轮间2秒；每轮独立进程、完整日志先落盘、退出0、A01—A12恰好12项且失败0。

首个构建 / 产品非零、标签或计数不符、日志截断、目录竞争或工具异常立即停止，不补跑，不修改代码或合同；释放目录、确认进程0，以专属断点具名回传。

## 4. 成功收口

全部通过后只允许更新既有 #315 专属断点，登记本轮构建、单轮、20/20、日志路径 / SHA-256、进程0和目录释放。随后运行 strict、diff、精确七文件、禁止依赖、1100唯一和 staged范围门禁；只暂存该断点的新差异，在 `0981e8a` 之上提交并非强制推送 `origin/codex/315-real-generation-loop-s1-r2`。状态只到分支完成待集成，不得推 main、创建集成身份或消费 #316。

## 5. 六类证据与边界

- 输入入口：E255、新代理独立 S0、R2 Git 与实际接口。
- 核心处理：接受新代理无漂移 S0，恢复唯一执行所有者，准备冻结共享验证。
- 结构承载：计划、索引、队列、当前状态、工作树登记、窗口交互和本设计记录。
- 输出变化：新代理从已创建待派发推进为已派发待执行回执；运行验证仍待 E256 消息。
- 调用路径：E255 发布 -> 新代理 S0 PASS -> E256 接受 -> 推送后共享验证。
- 验证方式：代理回执、Git、strict、diff 和 staged 范围。

本记录不预先证明构建、单轮或20/20通过，不声明任务分支完成、可集成或#316解锁。
