# CAUSAL-USE-O1-S2-D1 JY-385 逐签名复核与第三次审计重新派发

日期：2026-07-17

设计裁决：JY-385

窗口事件：WIN-0001-E125

来源执行队列：#292 / DQ-184 / CORE-SESSION-S6

恢复执行队列：#290 / DQ-182 / CAUSAL-USE-O1-S2-D1

状态：设计复核通过 / #292 已完成归档 / #290 v0.3 重新待执行 / 单窗口串行双角色 / 中等推理机械审计 / 不登记 880

## 1. 接管事实

```text
main == origin/main == 58e351b9a0657085bf3d1b442a2cdb8f551e7851。
工作树在设计接管前干净。
#292 实施记录状态为“执行完成待设计同步”。
阶段 761 / CORE-SESSION-S6 已在入口唯一登记。
A01—A18、Debug / Release Rebuild 0 warning / 0 error、两配置完整自检退出 0、Release 隔离、最终 Debug 20 / 20、strict、diff 与允许范围均有正式实施证据。
主信息仓库、工程、O1、恢复和中央治理文件未被 #292 修改；节点 15、关系 18、业务 15 槽、O1 索引 / 容量、HYSNAP01 和 880 均未新增。
```

正式执行证据：

```text
实施记录/20260717_CORE-SESSION-S6_完整主信息值式读取与强类型投影失败归因代码实施_Codex断点清单.md
计划/已完成计划/20260717_CORE-SESSION-S6_完整主信息值式读取与强类型投影失败归因代码实施切片_v0.1.md
```

## 2. 逐签名复核

| JY-384 假定 | `58e351b` 当前签名 / 结构 | 裁决 |
| --- | --- | --- |
| 节点批量读取四态值式结果 | `节点记录组读取状态` 固定 `已形成 / 入口拒绝 / 资源失败 / 内部不一致`；`节点记录组读取结果` 承载记录组；`节点仓库::读取指定类型有效节点记录组(节点类型, const 结构事务令牌&)` 返回该结果 | 一致 |
| 索引批量读取四态值式结果 | `主键绑定组读取状态` 固定同四态；`主键绑定组读取结果` 承载完整主键绑定记录；`索引仓库::读取全部主键绑定组(const 结构事务令牌&)` 返回该结果并正反互证 | 一致 |
| 真实空与失败互斥 | 两仓库仅以 `已形成 + 空集合` 表示真实零记录；入口、复制资源失败与记录矛盾均返回非已形成状态和空集合 | 一致 |
| 会话投影保持资源失败归因 | `结构写入会话::类型索引投影状态` 含 `资源失败`；`读取有效节点与索引投影(节点类型)` 只消费两仓库的 `已形成` 结果，资源失败保持强类型并锁存首次失败 | 一致 |
| 完整主信息值式读取 | `结构写入会话::完整主信息读取状态` 固定 `已读取 / 入口拒绝 / 资源失败 / 内部不一致`；`读取完整主信息(主信息句柄)` 返回含完整 `主信息记录` 的值式副本，记录内含整个值容器 | 一致 |
| 候选写后、请求提交前读回 | 完整读取使用 `可继续写入()` 门禁；成功后同时标记本会话主信息候选及对应 `候选I64写集_` 已读回 | 一致 |
| 读取失败阻止后继写入 | 资源失败、记录矛盾或复制异常登记首次失败；后继写入被拒绝，已有候选沿 #291 撤销合同收口 | 一致 |
| Debug 故障与 Release 隔离 | 三个一次性资源失败入口均在 `HY_EGO_ENABLE_STRUCTURE_COMMIT_FAULT_SELF_TEST` 内，Release 验证无阶段、符号或输出泄漏 | 一致 |
| 入口最小登记 | `入口.cpp` 仅登记阶段 761、一次自检调用和顶层具名门禁；完整 A01—A18 正文位于既有自检真模块 | 一致 |

## 3. 设计裁决

1. #292 的正式产物满足 #290 第二次审计暴露的 A07、A09—A11 通用接口前置；未发现需要修订 JY-384 的签名或语义漂移。
2. #292 / DQ-184 移入已完成计划；761 保持正式登记。
3. #290 / DQ-182 保留原编号、原路径和 v0.3，状态改为 `重新待执行 / 依赖已满足 / 唯一候选`。
4. #290 仍是纯文档审计，只允许新增第三次审计矩阵和第三次审计断点；不得修改被审计三件套、代码、工程、计划、索引、队列或历史审计，不登记 880。
5. JY-385 只解除第三次审计前置门控，不预判 #290 A01—A16；任一项不通过仍退回设计，不得生成 O1-S2 代码计划。

## 4. 单窗口与模型路由

当前物理窗口继续使用单窗口串行双角色模式。本轮为设计角色；E125 设计提交推送成功并重新读取 Git、计划索引、任务队列、#290 v0.3、本记录、#292 正式实施记录和实际接口后，才切换为执行角色。

#290 是成熟的纯文档检查表，可由中等推理执行模型机械执行。出现以下任一情况时立即停止并升级回高推理设计角色，不允许执行角色修改计划或扩大文件范围：

```text
正式签名相对本记录漂移。
三件套范围冲突或 Mermaid 不同源。
计划、索引、队列、允许 / 禁止文件互相冲突。
A01—A16 同一事实出现互斥结论或无法形成唯一结论。
需要修改两份允许审计记录以外的文件才能继续。
```

## 5. 三件套范围一致性

```text
流程图/20260717_权威用途观察结构承载与事务发布恢复流程图_v0.1.md
流程图/20260717_权威用途观察结构承载与事务发布恢复流程图_v0.1.html
规范/详细设计/权威用途观察结构承载与事务发布恢复详细设计.md
计划/20260717_CAUSAL-USE-O1-S2-D1_权威用途观察结构承载与事务发布恢复实施前设计审计计划_v0.1.md
```

四者范围一致：节点 15、关系 18、15 槽、单索引双证、FNV 有界探测、派生容量、专用数据操作、唯一许可释放发布点和 HYSNAP01 原地九段仍只是 O1 设计候选；#291 / #292 只提供通用底座；#290 只审计，不实施。

## 6. 设计轮验证

```text
python .\tools\check_specs.py --strict
git diff --check
```

本设计轮不运行 msbuild，不运行程序，不修改 C++ / 工程，不操作 stash。

## 7. 完成声明边界

本记录只证明 #292 正式接口与 JY-384 假定逐签名一致，并据此重新发布 #290 第三次纯文档审计。不得声明 #290 已通过、O1-S2 已实现、节点 15 / 关系 18 / 15 槽 / O1 索引 / 容量 / HYSNAP01 已落地、880 已登记，或统计、学习、生产接线、自动行动与旧能力迁移已经完成。
