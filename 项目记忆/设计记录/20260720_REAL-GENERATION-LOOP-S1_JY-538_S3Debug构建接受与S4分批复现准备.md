# JY-538 #315 WT-315-R7 S3 Debug 构建接受与 S4 分批复现准备

日期：2026-07-20

## 1. 前置与构建

JY-537 / E282 已由 `4ea8af75de4e556c45d0de698b1572dcaf37eb54` 非强制发布到 `origin/main`。发布后，设计窗口只授权替代唯一代理 `/root/wt315_r7_execution_v2` 执行一次 Debug x64 Rebuild，没有授权产品运行。

## 2. S3 结果

```text
执行次数：1
退出码：0
warning：0
error：0
```

证据目录：`D:\海中鱼巣\日志\诊断\REAL-GENERATION-LOOP-S1-R7\E281-WT-315-R7`

```text
04-debug-rebuild.log
SHA-256=5E72DE04793BBB306D6BE24EAC88CCE4B9E62E49FE880BA12090B765590918E1

05-s3-debug-build-summary.txt
SHA-256=3D2DAFEF46D06910D9C417BE9C13E03E25FF35B8A543DF8223E5283B64611AB0
```

构建后本轮产生的 `mspdbsrv` / `vctip` 已按精确 PID 释放；产品 / 构建进程 0、共享目录空。两源码 blob、两个 patch SHA 和 Git 两文件 dirty / index 空状态不变。代理未运行产品或执行 S4。

## 3. 设计窗口独立复核与裁决

设计窗口独立核对日志与 summary SHA、summary 内容、构建日志尾部 0 warning / 0 error、两源码 blob、Git 和资源状态，结果与代理一致，DRIFT 为 0。因此正式接受 S3。

## 4. E283 后 S4 合同

E283 发布后授权使用 Debug `--self-test-exit` 执行分批独立产品复现：

- 总产品启动预算最多 60，每个工具编排批次最多 10 个独立进程。
- 每次分配唯一尝试号，先保存完整 stdout / stderr，再记录退出码、A11、A12、R7 诊断行和共享目录计数。
- 每批结束确认产品 / 构建进程 0、共享目录为空，才可进入下一批。
- 平台工具中断计入启动预算，但不计有效完整轮；资源核对后从下一个未使用尝试号继续。
- 首个真实 A11 失败立即停止后续产品启动，不重试、不补跑。
- 60 次预算用尽仍无失败，只记录“本次未复现”，不得宣称稳定。
- 捕获失败时必须依据诊断行定位唯一最窄返回点；若仍有歧义，具名保留，不猜修复。

S4 期间不得修改源码、写实施记录、暂存、提交、推送或创建集成身份。S4 结果被设计窗口接受并形成 JY-539 / E284 前，不得恢复源码或进入 S5。

## 5. 六类证据

- 输入入口：E282云端提交、构建日志/summary和代理S3回执。
- 核心处理：接受唯一Debug构建，下一步授权有界分批复现。
- 结构承载：#315计划、计划索引、任务队列、并行工作树登记表、当前状态、窗口交互记录和本设计记录。
- 输出变化：S3从待构建推进为PASS已接受；S4待执行。
- 调用路径：E282发布 -> 唯一Debug Rebuild -> 代理PASS -> 设计独立复核 -> JY-538/E283 -> S4分批复现。
- 验证方式：构建退出码、warning/error、日志SHA、资源、Git、strict、diff和staged范围。

## 6. 完成边界

本记录不证明S4已运行或失败已定位，不授予S5、源码恢复、实施记录、暂存、提交或推送权限。R7不可集成，#316继续依赖门控。
