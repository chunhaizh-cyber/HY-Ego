# PERSIST-S1-P0-C JY-396 任务 Git 自动审查与断点发布门禁

日期：2026-07-18

计划：#296 / DQ-188 / v0.1

状态：默认沙箱复测通过 / S0 接口漂移已具名 / 断点待自动审查 Git 发布

## 1. 当前事实

1. Codex 桌面重启后，WT-296 在 `unelevated` 默认沙箱中成功核对 cwd、分支、冻结 HEAD 和 clean 状态，不再出现 1312，也没有人工审批弹窗。
2. S0 逐签名复核确认阶段 890 的专项 import、回调登记、结果槽和最终汇总实际位于计划禁止的 `海中鱼巣/入口.cpp`；`自检.运行器.ixx` 没有可供新专项接入的登记宿主。
3. WT-296 已只新增计划允许的执行专属断点，C++、工程和中央治理零变化；`git add` 因任务 worktree 的 Git 元数据位于主仓库 `.git/worktrees` 而被默认沙箱拒绝创建 `index.lock`。
4. 个人配置已改为 `windows.sandbox = "unelevated"`、`approval_policy = "on-request"`、`approvals_reviewer = "auto_review"`。现有桌面进程仍需重启后才使用自动审查策略。

## 2. 裁决

1. JY-395 的 `approval_policy = "never"` 退出；保留沙箱并把确需越界的权限请求交给自动审查代理，不再要求用户逐条确认。
2. WT-296 重启后的唯一动作是精确暂存执行专属断点、核对 staged diff、提交并非强制推送 `codex/296-p0c`；不得继续代码实施，不得触碰 `origin/main`。
3. 自动审查只改变审批承担者，不扩大 #296 文件、结构、接口、Git 目标或角色权限。删除、移动、强推、主工作树写入和 `origin/main` 推送继续禁止。
4. 断点提交推送后状态为“接口漂移待设计修订”，由后续独立集成角色把纯断点提交纳入 `main`，设计角色读取正式证据后再修订 #296 三件套。

## 3. 完成边界

本轮只形成自动审查 Git 发布门禁。不得声明断点已经提交、#296 已实施、阶段 890 已登记、构建已运行或任务分支完成待集成。
