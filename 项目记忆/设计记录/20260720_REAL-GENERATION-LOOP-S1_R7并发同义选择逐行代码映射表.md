# REAL-GENERATION-LOOP-S1 R7 并发同义选择逐行代码映射表

日期：2026-07-20

代码基线：`main@0747cce491c3c6bbf37dbd49f61046b4097edee0`；#315 路由与自检仅存在于只读失效证据分支 `R7@4160652079d7296264b902dfb98cc29eab855769`。组合器与召回服务在 main/R7 中 blob 相同；任务服务按 R7 已恢复生产源树读取。R7 不可集成，本表只映射证据代码。

覆盖说明：以下范围覆盖所列函数或代码片段全部非空、非纯格式行；纯括号和空行并入相邻范围。现状流程图节点对应 `流程图/现状流程图/20260720_根需求任务筹办并发同义选择收口现状流程图_v0.2.md`。

| 文件 | 行号 | 代码行为 | 流程图节点 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `R7:海中鱼巣/线程/路由.根需求筹办.ixx` | 1 | 文件头规则限制路由只消费同一租约系统角色和 B0 | E1/A | 注释 | 无 | 人读规则 | 不适用 | 保持，不扩服务/仓库访问 |
| 同上 | 2-9 | 模块全局片段、配置头、标准库和模块声明 | E1 | 预编译 / 模块声明 | 无 | 编译合同 | 不适用 | 固定从源 blob 恢复 |
| 同上 | 11-20 | 导入上下文、B0 所需服务/组合和执行请求协议 | E1/A | 模块导入 | 无 | 编译合同 | 不适用 | 不新增召回/任务服务写权限 |
| 同上 | 22-32 | 导出命名空间和七种根需求筹办状态 | E1 | 类型声明 | 无 | 值式报告合同 | 不适用 | 状态枚举不变 |
| 同上 | 34-57 | 报告承载两根、需求、任务、方法、召回、选择、冻结；`完整()` 复核全链 | K1/O | 结构承载 / 纯计算 | 无 | 值式权威读回报告 | 不完整属于追根因对象或明确逻辑分支 | 保持完整条件，不能放宽 A11 |
| 同上 | 59-72 | 把召回无候选/非唯一/内部不一致映射到路由状态，其它均映射当前性变化 | J1/X2 | 纯计算 | 无 | 临时局部映射 | 无候选/非唯一逻辑内；内部不一致应追根因 | v0.7 不得让内部不一致被现存选择掩盖 |
| 同上 | 74-80 | `任务读回匹配` 只检查完整承接、来源需求和目标阶段 | C/K1 | 纯计算 | 无 | 权威材料判定 | 不匹配需区分读回不完整与当前性漂移 | 新选择同义 helper 需逐字段，不扩此函数语义 |
| 同上 | 84-93 | 建报告；拒绝无效配置/租约/不完整上下文或 B0 | A | 入口拒绝 / 读取 / 返回 | 无 | 正式流程入口 | 外部无效材料逻辑内；上游正式保证后异常需追根因 | 保持入口拒绝 |
| 同上 | 95-100 | 取系统角色两根并复核目标根角色和句柄互异 | A | 读取 / 入口拒绝 | 无 | 已发布系统角色材料 | 正式清单不完整应追根因 | 现状仍直接返回默认报告，计划不扩大本次范围 |
| 同上 | 102-114 | B0 权威读取两根；读回矛盾为内部不一致，根已满足逻辑返回 | A/B | 读取 / 返回 | 无 | 权威需求事实 | 内部不一致追根因；根已满足逻辑内返回 | 保持 |
| 同上 | 116-131 | 创建或幂等读回承接任务，再权威读取并复核来源需求 | B | 调用 / 权威读回 / 返回 | 可能写任务结构 | 权威任务事实 | 已声明成功却读回不完整追根因 | 保持 |
| 同上 | 133-161 | 对已创建/已承接任务按四字段迁移并逐段读回，最终要求筹办中 | C | 调用 / 权威读回 / 返回 | 可能写生命周期 | 权威任务事实 | 内部不一致追根因；真实当前性变化逻辑内 | 保持 v0.4 完整历史幂等合同 |
| 同上 | 163-172 | 创建或幂等读回方法首并取得方法句柄 | D | 调用 / 返回 | 可能写方法结构 | 权威方法事实 | 内部不一致追根因；其它当前性变化 | 保持 |
| 同上 | 173-190 | 创建方法结果与动作入口，权威读方法登记项并联合复核 | D/E | 调用 / 权威读回 / 返回 | 可能写方法规格 | 权威方法事实 | 内部不一致追根因；其它当前性变化 | 保持；R7 已证明失败不在此段 |
| 同上 | 192-197 | 若旧任务快照无当前选择，调用组合器并保存召回结果 | F/G | 条件 / 调用 / 局部赋值 | 组合器可能发布选择 | 权威写入由 B0 决定 | 不适用 | 调用次数和顺序不变 |
| 同上 | 198-201 | 组合非成功时直接映射召回状态并返回 | J1/X1-X3 | 返回 | 可能已有其它线程完整选择 | 局部返回状态；尚未权威读回 | 无候选/非唯一可逻辑返回；内部不一致追根因；入口拒绝需读回裁决 | 核心偏差：先权威读回任务/方法再分类，不得一律提前返回 |
| 同上 | 202-203 | 只有组合成功才重新权威读取任务 | J2 | 权威读回 | 无 | 权威任务事实 | 读回不完整需追根因 | 扩展到组合非成功路径 |
| 同上 | 204-210 | 只检查任务完整、选择存在完整且方法相同，然后复制选择进报告 | K/K1 | 读取判定 / 局部赋值 / 返回 | 无 | 权威选择材料 | 半结构追根因；异义选择应当前性返回 | 增加记录主键、幂等材料、任务/方法来源关系版本和规则版本全字段同义判定 |
| 同上 | 212-232 | 从配置和系统角色构造纯值任务执行调度请求 | N | 纯值构造 | 无 | 请求材料 | 不适用 | 仅在同义选择成立后继续 |
| 同上 | 233-244 | 冻结请求；不完整时分类；完整则设已冻结并最终复核报告 | N/O/P | 调用 / 返回 | 无，纯值冻结 | 权威来源的值式请求 | 内部不一致追根因；真实当前性变化逻辑内 | 保持 |
| `main/R7:海中鱼巣/领域/组合.需求任务方法.ixx` | 35-47 | 定义召回+选择请求/结果；成功要求召回唯一建议且选择提交成功 | G/J1 | 类型声明 / 纯计算 | 无 | 值式组合结果 | 不适用 | 不修改组合器 |
| 同上 | 93-100 | 依次召回、适配唯一建议、存在请求才提交选择 | G/J/X1 | 调用 | 可能发布选择 | 权威写入由任务服务 | 无候选/非唯一逻辑内；适配失败需调用方结合权威读回裁决 | 不修改调用序列或成功条件 |
| `main/R7:海中鱼巣/领域/服务.方法召回.ixx` | 25-38 | 定义召回请求和五种状态，默认失败状态为入口拒绝 | G/H/X0 | 类型声明 | 无 | 值式请求/结果合同 | 不适用 | 状态不改 |
| 同上 | 40-71 | 定义候选和召回结果完整条件 | G/J | 结构承载 / 纯计算 | 无 | 非权威候选材料 | 不适用 | 不放宽候选完整性 |
| 同上 | 74-84 | 保存需求、任务、方法、特征和状态服务引用 | H | 构造 | 无 | 服务依赖 | 不适用 | 不改所有权 |
| 同上 | 86-88 | 召回入口拒绝无效任务/登记根/规则版本 | H | 入口拒绝 / 返回 | 无 | 正式请求材料 | 正式路由已保证字段有效；若仍命中应追根因 | 路由读回后无选择且任务/方法稳定时按内部不一致收口 |
| 同上 | 89-92 | 权威读取任务；任务不完整、非筹办中或已有当前选择均返回默认入口拒绝 | H/I/X0 | 权威读回 / 返回 | 无 | 权威任务事实 | 非筹办中是真实当前性；已有完整同义选择是并发完成候选；不完整追根因 | 不改召回服务；由路由权威读回细分 |
| 同上 | 93-101 | 权威读取需求、登记根、特征和目标状态；不完整返回入口拒绝或内部不一致 | J | 权威读回 / 返回 | 无 | 权威基础/高级信息 | 内部矛盾追根因 | 路由不得用并发选择掩盖内部不一致 |
| 同上 | 103-150 | 固定召回快照；扫描活跃方法，复判条件/结果/动作并确定性排序 | J | 读取 / 纯计算 | 无 | 非权威候选材料 | 无匹配允许继续形成无候选 | 不修改 |
| 同上 | 151-163 | 候选空返回无候选；首两名同分返回非唯一；否则形成唯一建议 | J/J1/L1/L2 | 纯计算 / 返回 | 无 | 非权威建议 | 无候选/非唯一逻辑内返回 | 不修改 |
| 同上 | 165-171 | `适配唯一建议` 拒绝非唯一建议或零幂等材料 | J/X1 | 入口拒绝 / 返回 | 无 | 值式请求适配 | 正式输入异常追根因；无候选/非唯一不调用提交 | 不修改 |
| 同上 | 172-181 | 再读任务/方法；任务已有选择或任务/方法版本漂移时不形成提交请求 | J/X1 | 权威读回 / 返回 | 无 | 权威当前性事实 | 当前性变化逻辑内；稳定读回仍拒绝需追根因 | 路由通过调用后权威读回裁决 |
| 同上 | 183-190 | 由召回快照和值式幂等材料形成提交任务方法选择请求 | J | 纯值构造 / 返回 | 无 | 请求材料 | 不适用 | 不修改 |
| 同上 | 192-200 | 服务私有依赖成员和类型收口 | J | 成员声明 | 无 | 服务所有权 | 不适用 | 不修改 |
| `R7:海中鱼巣/领域/服务.任务.ixx` | 168-176 | 提交选择入口拒绝零主键、无效句柄、零来源版本/规则/候选数 | J | 入口拒绝 / 返回 | 无 | 正式请求材料 | 上游适配已保证，意外拒绝需追根因 | 不修改入口 |
| 同上 | 177-184 | 形成选择写入规格并由数据操作提交；规格缺失映射内部不一致 | J | 调用 / 返回 | 可能写选择记录和关系 | 权威任务选择 | 内部不一致追根因；幂等读回成功 | 不修改任务服务 |
| `R7:海中鱼巣/线程/自检.根需求筹办.ixx` | 1-24 | 文件规则、宏控头/导入和自检模块声明 | E1/X5 | 注释 / 预编译 / 模块声明 | 无 | 自检合同 | 不适用 | 保持生产/自检分离 |
| 同上 | 28-38 | 建 12 项验收并检查路由签名、固定配置和动作键 | X5 | 自检 / 纯计算 | 无 | 人读验收材料 | 不适用 | 路由签名不变 |
| 同上 | 40-48 | 启动生产会话、读租约和系统角色材料 | X5 | 自检调用 / 读取 | 建测试运行期结构 | 自检事实 | 启动失败为自检失败 | 保持 |
| 同上 | 50-78 | 首次与重复同义筹办，验证根、任务、方法、选择、冻结和结构守恒 | K-N/P/X5 | 自检调用 / 读取 | 可写测试会话业务结构 | 自检事实 | 任一不完整为失败 | 增加选择全字段同义断言，不删除现有条件 |
| 同上 | 80-87 | 构造无效配置，验证入口拒绝和正常配置仍完整 | A/X5 | 自检调用 | 无有效新结构 | 自检事实 | 逻辑内入口拒绝 | 保持 |
| 同上 | 89-107 | 新会话两线程同义调用；要求两报告完整且任务/方法/选择记录/冻结相同 | X5 | 并发自检 / 读取 | 可并发写同一测试会话结构 | 自检事实 | 报告不完整为失败 | 保持 A11 目标并补全字段比较；严格连续 20/20 覆盖竞态 |
| 同上 | 108-130 | A12 汇总、逐项人读输出和自检结果 | X5 | 纯计算 / 输出 / 返回 | 无 | 人读诊断 | 不适用 | 不把输出当机器事实 |
| 同上 | 132-133 | 命名空间和自检宏收口 | X5 | 预编译 | 无 | 编译合同 | 不适用 | 保持 |

结论：当前偏差集中在路由 198-203 行。组合器、召回服务和任务服务已有足够的权威读回与强类型材料；最窄施工只需路由在非成功后读回并逐字段裁决，自检保留并收紧 A11。
