# PERSIST-S1-P0-C JY-402 第二次接口漂移发布与独立集成登记

日期：2026-07-18

设计裁决：JY-402

执行队列：#296

设计队列：DQ-188

批次：WB-296-02

## 1. 已验证输入

1. WT-296-R2 位于 `C:\Users\zhchh\.codex\worktrees\296-p0c-r2\海中鱼巢`，分支为 `codex/296-p0c-r2`。
2. 任务提交 `1e95d8440f4b7f1c05ffaad4b1126085ffd59421` 的父提交精确为冻结基线 `3fa286bc4b65183f297f571f82434d0c6f3a62b7`。
3. 该提交只修改 `实施记录/20260718_PERSIST-S1-P0-C_概念活动唯一所有者代码实施_Codex断点清单.md`，没有 C++、工程、计划或中央治理差异。
4. 本地任务分支、`origin/codex/296-p0c-r2` 和 HEAD 一致，worktree / index clean；提交推送过程未触发人工命令授权。

## 2. 第二次漂移事实

#296 v0.2 A10 要求：系统角色初始化后、概念活动显式初始化前，`运行期上下文::完整()` 必须为假；只有消费三项非零且互异的生命周期稳定键并完整读回后才允许首次发布。

当前三个已登记自检仍固定旧合同：

```text
阶段 660：自检.运行期上下文.ixx 只初始化系统角色后要求完整并发布
阶段 680：自检.主装配事务.ixx 只初始化系统角色后以完整() 形成候选
阶段 690：自检.系统角色初始化.ixx 只初始化系统角色后要求完整、发布和租约成功
```

这三个文件不在 v0.2 允许范围。阶段 890 无法修改前序回调的局部上下文，Release 也仍运行阶段 660。自动推导生命周期键、弱化 `完整()` 或增加测试专用生产分支都会违反既有设计合同，因此执行角色正确停止在 S0。

## 3. 独立集成登记

按多工作树规范登记：

```text
工作树：INT-WB-296-02
路径：C:\Users\zhchh\.codex\worktrees\integration-wb-296-02\海中鱼巢
分支：codex/integration-wb-296-02
起点：本设计登记提交发布后的 origin/main
输入：origin/codex/296-p0c-r2@1e95d8440f4b7f1c05ffaad4b1126085ffd59421
```

集成只允许任务断点和新建 `实施记录/20260718_WB-296-02_第二次接口漂移断点集成记录.md`。集成角色核对父链、单文件范围和 660 / 680 / 690 漂移正文，运行 strict 与 diff。因为没有代码或工程差异，不构建、不运行、不登记阶段 890，也不把断点集成声明为 #296 完成。

## 4. 后继设计门禁

断点发布 main 后，设计角色必须先做一次主动接口复核，再原地升级 #296 v0.3：

1. 定向允许 `自检.运行期上下文.ixx`、`自检.主装配事务.ixx`、`自检.系统角色初始化.ixx` 做完整性前置机械适配。
2. 固定每个夹具的顺序：系统角色初始化 -> 不完整且宿主拒绝 -> 显式概念活动初始化 -> 完整 -> 发布 / 租约。
3. 复核旧 `概念生命周期阶段` 声明与新数据所有者的命名冲突，禁止重复声明同名类型或反向依赖旧概念图服务。
4. 明确 `概念活动初始化参数` 的字段、fresh 初始化和 restore 重建边界，禁止生产路径从测试数值或算术偏移推导稳定键。
5. 同步流程图 MD / HTML、详细设计、计划、索引、队列、登记表、当前状态、决策与窗口交互；使用新批次和新任务 worktree，不复用 WT-296-R2。

#297 和全部恢复后继继续依赖门控。

## 5. 本轮验证

```text
python tools/check_specs.py --strict
git diff --check
git diff --name-only
```

本轮只登记集成，不修改代码、不构建、不运行、不登记阶段 890。
