# JY-506 #315 跨电脑 R2 重建核对与 S0 派发准备

日期：2026-07-19

## 1. 前置

JY-505 纠正提交 `8cc277225b22de5b12c53f126f5aa56033a74d6e` 已非强制推送 `origin/main`，并确认换电脑后本机分支 / worktree 缺失不构成远端 R2 身份漂移。

## 2. R3 回收事实

- 回收前 R3 精确为 `codex/315-real-generation-loop-s1-r3@0981e8a`，worktree / index clean、无 upstream、远端 R3 不存在，与远端 R2 对照 0/0。
- 已使用 Git worktree 入口移除 `C:\Users\zhchh\.codex\worktrees\315-real-generation-loop-s1-r3\海中鱼巢`，随后删除本地 R3 分支。
- 回收后 R3 路径、本地分支和远端分支均不存在。
- WIP 继续完整保存在 `origin/codex/315-real-generation-loop-s1-r2@0981e8a`，因此回收未丢失代码，可由远端 R2 恢复。

## 3. R2 重建事实

- 重建前 R2 目标路径和本地同名分支不存在；远端 R2 精确指向 `0981e8a89ddb6ba1b853b3b68a65092037088ed1`。
- 已从远端 R2 创建本地 `codex/315-real-generation-loop-s1-r2`，并在原登记路径 `C:\Users\zhchh\.codex\worktrees\315-real-generation-loop-s1-r2\海中鱼巢` 物化 WT-315-R2。
- 重建后 `HEAD=0981e8a`，唯一父为 `57cacbc78cd4aa40e5cd85791baabb80a43e2c48`，worktree / index clean、无 upstream，与远端 R2 对照 0/0。
- 相对冻结父精确七文件，`git diff --check` 通过；计划读回为 v0.2，专属断点读回身份为 WT-315-R2。
- 产品与 MSBuild 进程均为 0；未构建、未运行、自检共享目录未占用。

## 4. 当前状态与 S0 范围

```text
WB-REAL-LOOP-02 / WT-315-R2
跨电脑本地重建完成
已创建待派发
非执行中
不可修改 / 构建 / 运行 / 提交 / 推送 / 集成
#316 继续门控
```

E251 发布后，只向唯一任务 `019f46a8-7cd7-7622-88c4-fc45cc5ea016` 派发只读 S0：

1. 核对路径、分支、HEAD、父链、clean、无 upstream、远端 R2 0/0。
2. 核对七文件逐 blob、计划 v0.2、JY-500 生命周期合同、允许 / 禁止范围、实际接口和 1100 唯一登记。
3. 核对产品进程 0、共享目录未占用。
4. 不修改、不构建、不运行、不暂存、不提交、不推送、不创建集成身份。
5. 正式回告设计窗口；S0 通过后再由设计窗口单独授权运行验证和共享目录独占。

消息工具不可用、发送失败或未读回时，状态保持 `已创建待派发`，不得写成执行中或已派发待执行回执。

## 5. 完成声明六类证据

- 输入入口：用户换电脑说明、JY-505 / E250、远端 R2 WIP、R3 可回收门禁。
- 核心处理：精确回收误建 R3，并从远端 R2 在新电脑重建原路径 / 分支身份。
- 结构承载：Git worktree / 本地 R2 分支、计划索引、任务队列、当前状态、并行工作树登记、窗口交互和本设计记录。
- 输出变化：R3 已回收；R2 从跨电脑待重建推进为已创建待派发；未形成运行或集成授权。
- 调用路径：E250 发布 -> 回收 R3 -> 从远端 R2 物化本地身份 -> E251 设计回执发布 -> 后继只读 S0 消息。
- 验证方式：路径 / 分支不存在与存在性、HEAD / parent、0/0、clean、无 upstream、七文件、计划 / 断点读回、进程 0、strict、diff 和 staged 范围检查。

## 6. 本轮边界

本轮只回收误建 worktree、重建原任务 worktree 并同步中央设计治理；不修改 R2 文件，不运行构建、自检或产品，不推送任务分支，不发送运行验证授权。
