# NODE-TYPED-MIGRATION NT-P0 六包设计依赖所有权与一致性独立审计记录

日期：2026-07-22

身份：#338 设计治理审计记录；记录只读子智能体事实、DRIFT、修订与终审，不替代正式规范、详细设计、计划或 Git 事实

## 1. 审计基线与范围

基线：`main == origin/main == 1185e1b458b9c83244cd775dea3825931a134787`。审计对象为 NT-P1、P2A、P2B、P2C、P3、P4 六包现状 / 施工流程图、详细设计、函数结构知识图谱、#339—#352 叶子路线、文件 / 接口 / 工程所有权和真实依赖。三路审计全程零修改、零构建、零运行、零 Git 写入。

## 2. 初审结论：DRIFT

1. P1 关系仓被误限为关系 19—23，不能支持 P3 的关系 9—16 和 P4 的 0—23 完整冻结。
2. P2A -> P2B 的 `复核特征值归属`、P2A -> P2C 的槽位组、P2B -> P2C 的状态 / 动态事实组是真实接口消费，完整 P2 三叶不能并行。
3. P2C 与 P3A 对节点直接存在场景数据操作和存在 / 场景服务三个文件重叠。
4. P3D 缺 P3C 强类型记录退役依赖，FREEZE / RESTORE 适配边界可能回写上游固定文件。
5. P3A—P3C 和 P4 各叶新增模块都需要同一工程 XML / 统一运行器；在没有独立编译路径时不能一边宣称叶子并行 / 共享文件唯一后置，一边要求每叶完整构建。
6. P4 全新候选在流程图和图谱中直接取得已验证候选能力，绕过全量互证、投影重建和冻结证明。
7. P4-CUTOVER 未显式拥有 `核心/句柄.h`，统一运行器未精确到 `海中鱼巣/自检.运行器.ixx`。

## 3. 已落实修订

1. P1 关系仓改为完整支持正式 ABI 0—23；19—23 只表示本轮新增 ABI / 主题角色。
2. 固定 `#339 -> #340 -> #341 -> #342`；P3A—P3C 在独立验证路径形成前也按工程 XML / 运行器所有权串行。
3. P2C 保留存在场景基础模块唯一所有权；P3A 只读消费并只新建系统角色 / 增量模块。
4. P3D 依赖 P2A—P2C 与 P3A—P3C 全部退役接口；退役、冻结和装载均使用独立适配模块，不回写上游固定文件。
5. 工程 XML 和统一运行器按固定提交串行移交，每个叶子只登记自身模块 / 专属自检，阶段共享叶最终统一整理。
6. 全新候选与恢复候选分型但等强验证：两者均做全量互证和投影确定重建；全新形成规范化初始冻结，恢复另做再冻结与输入相等。
7. CUTOVER 显式拥有 `核心/句柄.h`、默认装配、启动、入口、工程、`海中鱼巣/自检.运行器.ixx` 和最终物理删除。
8. 12 对新流程图 MD / HTML 的 Mermaid 主体在初审时逐字一致；P4 施工图修订后必须在终审重新比较。

## 4. 真实依赖和并发裁决

```text
#339 P1
-> #340 P2A -> #341 P2B -> #342 P2C
-> #343 P3A -> #344 P3B -> #345 P3C
-> #346 P3D -> #347 P3E -> #348 P3-SHARED
-> #349 P4-FREEZE -> #350 P4-RESTORE -> #351 P4-LEGACY -> #352 P4-CUTOVER
```

P3A—P3C 的串行边只来自当前工程 / 运行器所有权和完整验证路径，不升级为业务语义依赖。将来若建立不修改共享工程 / 运行器的真实独立编译与自检路径，可以另行升版重算并行；不得只改状态文字。

## 5. 终审门禁

终审必须确认：上述 DRIFT 全部零残留；六包与十四份叶子范围一致；所有精确文件交集都有唯一串行所有者；P4 全新 / 恢复验证无旁路；12 对 Mermaid 主体一致；活动设计不引用归档旧设计；正式规范 strict 通过；代码目录零差异。终审 PASS 前 #339 不得晋级待执行或建立派发事实。

## 6. 修订后终审结论：PASS

两路独立只读终审和顶层路由复验确认：

1. 关系仓合同完整支持正式 ABI 0—23，19—23 只表示本轮新增 ABI；未知关系类型在写前拒绝且保持零写。
2. P1 图谱只把隔离新模块列为当前写所有权；旧核心仓库、旧会话、旧执行器和旧冻结材料均为只读来源。
3. P2 固定为 `#340 -> #341 -> #342`，工程 XML 和统一运行器随已集成且经设计接受的固定提交逐项移交，不另立共享接线叶子。
4. P3A 只读消费 P2C 已发布接口；P3A—P3C 与 P4 各叶的工程、运行器和最终 CUTOVER 所有权均由串行合同裁决。
5. #340、#341、#342 已分别冻结三个互异的专属实施记录路径；十四个叶子的业务生产文件不存在无控制重叠。
6. 全新候选与恢复候选均须全量互证和投影重建；恢复候选还须再冻结并与输入相等，不存在发布旁路。
7. 六包设计与 #339—#352 十四份叶子计划的对象范围、依赖、所有权和完成边界一致；十四个索引路径存在且计划 / 队列身份一致。
8. 12 对现状 / 施工流程图的 Markdown 与 HTML Mermaid 主体逐字一致；活动设计未把归档旧设计恢复为权威依据。
9. `python .\tools\check_specs.py --strict` 通过 94 份正式规范，`git diff --check` 通过；代码、工程和 staged 代码差异均为零。

终审边界：该 PASS 只证明 NT-P0 设计依据、叶子路线、依赖和所有权可以进入中央提交与 #339 派发治理；不证明代码已迁移、任务已派发、构建通过、恢复接通或旧结构已退役。

当前状态：终审 PASS；#339 可晋级 `待执行`，但只有中央提交推送、worktree / 分支 / 所有权登记和两阶段真实派发全部完成后才形成执行许可。
