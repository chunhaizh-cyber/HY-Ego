# JY-504 #315 恢复执行身份重建预留

日期：2026-07-19

## 1. 触发

用户把云端 `origin/main` 同步到当前本机后，明确本窗口为设计窗口并要求继续。JY-503 已规定：恢复 #315 前须由设计窗口重新核对暂停提交、专属断点、计划 v0.2 和当前主线，再单独授权剩余运行验证。

## 2. 当前事实

- 主集成工作树为 `D:\海中鱼巣`，当前 `main == origin/main == c72aac2c2e6b76e3c8f5aacf8431596ef222e83b`。
- 主工作树只有未跟踪的人读报告 `历史对话记录/20260717_每日任务复盘报告.md`；本轮不修改、不暂存该文件。
- `origin/codex/315-real-generation-loop-s1-r2` 精确指向暂停提交 `0981e8a89ddb6ba1b853b3b68a65092037088ed1`，唯一父提交为 `57cacbc78cd4aa40e5cd85791baabb80a43e2c48`。
- 暂停提交相对冻结父精确为六份代码 / 工程差异加一份 #315 专属断点，共七文件；`git diff --check 57cacbc..0981e8a` 通过。
- 当前本机 `git worktree list --porcelain` 只有主集成工作树；登记的 R2 路径 `C:\Users\zhchh\.codex\worktrees\315-real-generation-loop-s1-r2\海中鱼巢` 不存在，本地 `codex/315-real-generation-loop-s1-r2` 分支也不存在。
- 产品和 MSBuild 进程均为 0；共享事件段目录存在，但本轮未访问、未占用。
- 对登记任务 `019f46a8-7cd7-7622-88c4-fc45cc5ea016` 的列表查询和直接读取均连续超时，当前没有可证明的消息接收状态。

## 3. 裁决

R2 的远端暂停提交继续作为只读 WIP 来源保留，但当前本机不存在其登记的物理 worktree，不能把登记表历史路径解释为仍可直接恢复。建立新的恢复身份预留：

```text
批次：WB-REAL-LOOP-03
工作树：WT-315-R3
路径：C:\Users\zhchh\.codex\worktrees\315-real-generation-loop-s1-r3\海中鱼巢
分支：codex/315-real-generation-loop-s1-r3
恢复起点：0981e8a89ddb6ba1b853b3b68a65092037088ed1
计划：#315 / DQ-207 / REAL-GENERATION-LOOP-S1 v0.2
状态：已预留待创建 / 非派发 / 不可运行 / 不可集成
```

本次预留不改变计划业务合同，不重写 WIP，不创建集成身份，不解锁 #316。

## 4. 后继门禁

1. 本设计提交非强制推送 `origin/main` 后，才允许从精确 `0981e8a` 首次创建 R3 路径和本地分支。
2. 创建后须核对路径、分支、HEAD、worktree / index clean、无 upstream、远端 R3 同名分支不存在，以及相对 `57cacbc` 的七文件范围与暂停提交逐 blob 一致。
3. 创建事实形成新的设计回执并提交推送后，才向唯一执行任务派发只读 S0；S0 只核对 Git 身份、计划 v0.2、允许 / 禁止范围、实际接口、1100 唯一登记、七文件内容和进程 / 共享目录状态。
4. S0 通过并正式回告后，设计窗口再单独授权 Debug / Release 单轮 self-test、Release 隔离和 Debug 严格连续 20/20，共享事件段目录在该授权中独占。
5. 运行验证全部通过后，执行角色才能更新 #315 专属记录、精确提交并推送 R3 同名任务分支，状态只到分支完成待独立集成。
6. 任务工具仍不可用或消息未读回时，保持待派发并记录持久化替代回执，不得写成执行中或已派发待执行回执。

## 5. 完成声明六类证据

- 输入入口：JY-503、计划 v0.2、`origin/main@c72aac2`、远端 WIP `0981e8a`、当前本机 worktree / 分支 / 进程事实。
- 核心处理：对暂停 WIP、冻结父、七文件范围、本机物理身份和任务消息可达性逐项复核，裁决不得复用不存在的 R2 worktree。
- 结构承载：计划索引、任务队列、当前状态、并行工作树登记表、窗口交互记录和本设计记录。
- 输出变化：新增 R3 恢复身份预留；R2 转为只读远端 WIP 来源；#315 仍不可集成，#316 继续门控。
- 调用路径：设计窗口预留并发布 -> 创建核对 R3 -> 发布创建回执 -> 真实派发只读 S0 -> 另行授予共享运行验证。
- 验证方式：Git 提交 / 父链 / 七文件 / diff check、路径与分支不存在性、进程 0、严格规范检查、定向差异和 staged 范围检查。

## 6. 本轮边界

本轮只修改中央设计治理材料，不修改 C++、工程、入口、流程图、详细设计或计划正文，不创建 worktree，不运行构建、自检或产品，不发送“已派发”消息。
