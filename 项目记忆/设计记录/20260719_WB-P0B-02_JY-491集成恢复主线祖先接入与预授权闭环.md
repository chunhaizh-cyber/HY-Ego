# WB-P0B-02 JY-491 集成恢复主线祖先接入与预授权闭环

日期：2026-07-19

对象：INT-WB-P0B-02-R1 / JY-490 后继

## 1. 拓扑事实

JY-490 / E235 由设计窗口在集成角色正式释放主线发布占用后写入并发布，`main == origin/main == 4a0b96916898228640176a009827242dd26a0460`。现有集成分支仍停在 `f43b802e50d6c3c900b5713f7d5bbd24c27c632c`，它从 E234 汇入两个任务提交，因此 E235 不是该分支祖先。

若直接在 `f43b802` 上完成集成记录，WT-MAIN 无法从 E235 非强制快进到集成结果。重建 worktree、重做任务 cherry-pick 或 rebase 会丢失既有身份与验证边界，也没有必要。E235 相对 E234 只含 JY-490 的十三份中央设计治理文件，不含 C++、工程、入口、十八个任务文件或目标集成记录。

## 2. 裁决

E236 发布后，INT-WB-P0B-02-R1 必须先现场读取 E236 完整哈希并核对：

1. `origin/main` 与 WT-MAIN `main` 精确等于 E236，0 / 0、WT-MAIN clean。
2. 集成路径、分支、HEAD `f43b802`、worktree / index clean；两个任务树和十八任务 blob 仍一致。
3. E234 到 E236 的差异只含 JY-490 / JY-491 中央设计治理文件，C++、工程、入口、十八任务文件和目标集成记录差异为 0。
4. 产品进程为 0，共享目录未被其它已协调 worktree 占用。

全部一致后回告“集成恢复中”并取得主线发布占用。在现有集成分支执行一次普通非强制合并：

```powershell
git merge --no-ff --no-edit origin/main
```

只允许无冲突合并；冲突立即停止，不得手工改文件。合并后必须证明 E236 已成为 HEAD 祖先、相对 E236 精确只有十八个任务文件、十八个 blob 与两个输入一致、worktree / index clean。禁止 rebase、reset、重新 cherry-pick、删除或重建身份。

## 3. 单批预授权

为避免设计窗口在脚本固定点后再次推进 main，E236 把以下步骤冻结为同一集成预授权批次：

```text
接入 E236 祖先
-> 建立并预检唯一非 Git run-debug-round.ps1
-> 固定点全部通过后直接进入新的 Debug 20/20
-> 静态 / 范围 / blob / 阶段 / Release 隔离门禁
-> 唯一集成记录
-> 集成分支提交推送
-> main 非强制快进与推送
```

脚本固定点仍须逐项回告，但若 JY-490 第 3 节全部通过、脚本哈希已冻结、进程 0、共享目录可用，E236 已预先授权集成角色在不等待新的设计提交的情况下继续：取得共享目录独占，从新的第 1 轮执行 20 次独立固定外层调用。任一脚本、产品、判定、计数、叶子、manifest、哈希、进程或 Git 异常立即停止，释放共享目录和主线占用，不补跑。

## 4. 发布边界

20 / 20 全部通过后，继续 E234 / JY-490 的静态门禁。最终相对 E236 必须精确为十八任务文件加唯一集成记录，共十九文件；E235 / E236 中央治理通过祖先合并保留，不计入该十九文件差异。

集成分支和 main 只允许非强制推送。发布前 `origin/main` 必须仍精确为 E236，WT-MAIN clean；否则停止。发布成功或正式退回时明确释放主线占用。

本记录不归档 #310 / #305，不解锁 #301 / #306，也不修改业务计划、代码或验证等级。
