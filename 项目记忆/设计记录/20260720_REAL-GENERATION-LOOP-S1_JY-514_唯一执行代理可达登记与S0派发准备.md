# JY-514 #315 唯一执行代理可达登记与 S0 派发准备

日期：2026-07-20

## 1. 前置

JY-513 / E258 已由 `9e20f48115fa63a1eb61703d13a3d4cb07b44799` 非强制发布到 `origin/main`。WT-315-R4 已创建并通过路径、分支、HEAD、父链、clean、无 upstream、远端不存在和 main 0/0 身份门禁。

## 2. 唯一目标代理

设计窗口创建了唯一新执行代理：

```text
canonical task name：/root/wt315_r4_execution
目标角色：WT-315-R4 执行角色
当前权限：仅待命；尚未派发 S0；无代码写入权
```

代理已明确回执可达并保持待命，同时确认：

```text
未进入或读取 R4 worktree
未运行 Git、构建或程序
未修改、暂存、提交或推送任何文件
未开始 S0
未消费 #315
```

不存在第二个 R4 候选代理；R2 与历史 R3 均不拥有后继权。

## 3. E259 后只读 S0 合同

E259 发布前不得发送 S0。E259 发布后向 `/root/wt315_r4_execution` 发送并要求逐项回告：

1. 核对 R4 路径、分支、HEAD `aa75ae7`、父 `e109e0b`、clean、无 upstream、远端同名不存在。
2. 核对 `0981e8a^ == 57cacbc`、`b91d26c^ == 0981e8a`。
3. 核对 `0981e8a` 精确七文件、`b91d26c` 精确单记录，以及 E256 日志 SHA 与 A09—A12 失败事实。
4. 核对 `任务承接匹配` 当前实现、历史第1版生命周期材料读取入口与 v0.3 九文件范围。
5. 核对产品 / 构建进程为0、共享事件段目录为空。

S0 阶段禁止 cherry-pick、修改、构建、运行、写记录、暂存、提交和推送。S0 PASS 只形成只读回执；必须由设计窗口另行接受、登记、提交和推送后，才允许固定顺序带入两提交并实施根因修正。

## 4. 上游与完成边界

本轮是既有 #315 v0.3 的目标代理可达登记和派发门禁同步，不新增业务流程、接口或代码范围，因此流程图与详细设计不适用。替代依据为 `规范/000_项目规则总纲.md` 的真实派发规则、`规范/多工作树并发与集成规范.md`、#315 v0.3 计划、JY-512 根因裁决、JY-513 R4 创建事实和代理可达回执。

本记录不构成 S0 已派发、S0 PASS、WIP 已带入、代码写入权授予、构建运行、任务分支完成、可集成或 #316 解锁。

## 5. 六类证据

- 输入入口：E258 云端提交、R4 创建事实和代理创建调用。
- 核心处理：创建唯一代理、读取其明确待命回执并绑定到 R4。
- 结构承载：计划、索引、队列、工作树登记、当前状态、窗口交互和本设计记录。
- 输出变化：R4 从待目标代理推进为唯一代理已登记待 S0 派发。
- 调用路径：E258 发布 -> 创建代理 -> 代理回执可达且零操作 -> E259 登记。
- 验证方式：代理 canonical name / 回执、Git main 与 origin/main 对照、定向 diff 和严格规范检查。
