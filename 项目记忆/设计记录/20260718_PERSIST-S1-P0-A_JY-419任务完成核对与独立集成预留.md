# PERSIST-S1-P0-A JY-419 任务完成核对与独立集成预留

日期：2026-07-18

设计裁决：JY-419

执行队列：#297

设计队列：DQ-189

批次：WB-297-01 / INT-WB-297-01

## 1. 任务提交事实

```text
任务分支：codex/297-p0a-r1
任务提交：997308378caad88b91f33915cb2618b0598d413f
父提交：ac90efeae0407ab42ee9020ca9659cfdd9c68459
远端：origin/codex/297-p0a-r1 与任务提交一致
任务 worktree / index：clean
差异：计划允许的七文件
```

执行专属记录已登记生产版本 1 固定配置、不可复制生产运行期会话、启动互斥、唯一宿主 / 租约、入口旧栈前排他分流和阶段 900 双路径。任务级 A01—A18、Debug / Release x64 Rebuild 0 警告 0 错误、两配置默认 / 显式路径、四条冲突命令、Debug 默认 / 显式各连续 20 / 20、strict、diff、七文件范围和 Release 专项零文本通过。

## 2. 设计事实核对

任务实现保留计划边界：显式 `--runtime-context` 在首个旧仓库对象前分流，成功后因 P0-A 尚无生产消费者直接退出；默认入口仍走旧通道。启动互斥只属于生产运行期会话，不形成进程全局第二宿主，不修改 `启动.运行期上下文.ixx`、运行期业务装配或通用自检运行器。

执行记录如实保留早期探索运行的一次非最终异常，并明确该轮不能作为验收证据。互斥修正后已重新 Rebuild、具名运行并完成不间断最终 20 / 20；独立集成仍必须从提交树重新执行全部验证，不沿用探索性结果。

## 3. 集成预留

E161 推送并从 `origin/main` 重读后才允许创建：

```text
路径：C:\Users\zhchh\.codex\worktrees\integration-wb-297-01\海中鱼巢
分支：codex/integration-wb-297-01
起点：E161 发布提交
输入：origin/codex/297-p0a-r1@997308378caad88b91f33915cb2618b0598d413f
```

集成角色只能汇集该任务提交并新增 `实施记录/20260718_WB-297-01_排他生产运行期首发与租约持有集成记录.md`。不得修改业务语义、计划、索引、队列或其它中央治理来消除冲突。

## 4. 集成门禁

1. 核对任务提交唯一父为 `ac90efe`，相对父提交精确七文件。
2. 从 E161 main 起点 cherry-pick `9973083`；任何冲突、范围外差异或远端 main 前进都停止。
3. 重跑 Debug / Release x64 Rebuild、两配置默认 / 显式路径、四条冲突命令、Debug 默认 / 显式各连续 20 / 20、A01—A18、阶段 900、strict、diff、七文件范围和 Release 专项零文本。
4. 验证通过后提交并推送同名集成分支；随后只有 WT-MAIN 集成角色可把 main 非强制快进到集成提交并推送 `origin/main`。
5. main 发布后仍只进入已集成待设计同步；设计归档、计划索引 / 队列同步和 #298 依赖解除必须另行完成。

## 5. 本轮边界

本轮只登记集成，不修改 C++ / 工程，不构建、不运行。E161 推送成功前不得创建或写入 INT-WB-297-01。

## 6. 验证

```text
python tools/check_specs.py --strict
git diff --check
git diff --name-only
```
