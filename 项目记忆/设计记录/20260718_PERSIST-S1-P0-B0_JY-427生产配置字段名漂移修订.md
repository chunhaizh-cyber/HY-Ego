# JY-427 #298 生产配置字段名漂移修订

日期：2026-07-18

## 1. 输入事实

WT-298-R1 以 `1561f0ed40ddcd3601981781e8a936b1b93780af` 正式退回。该提交唯一父为 `b93f0711a081af1cb226e93b13b6102381581acc`，只新增 #298 执行断点；任务分支与远端 0 / 0，worktree / index clean，未改 C++、工程或入口，未构建，910 未登记。

当前 `海中鱼巣/生产运行期配置.数据.h` 的正式字段为：

```cpp
std::uint64_t 动作键 = 生产运行期稳定动作键;
```

#298 v0.4 第 3.1 节和第 9 节却冻结为 `配置.稳定动作键`，执行角色若满足字面合同必须修改禁止文件或伪报机械验收，因此退回成立。

## 2. 三层复核

消费者流程图 v0.6 只要求形成生产动作键传递；恢复详细设计第 21 节只要求生产稳定动作键唯一来自 `生产运行期配置` 并由生产启动显式传入。两者均未声明配置成员名，不存在上游语义漂移。

因此本次只修订计划层，不修改流程图或详细设计。替代依据是当前配置数据头、R1 具名断点和 JY-425 已确认的稳定动作键来源语义。

## 3. 裁决

1. #298 正文从 v0.4 升为 v0.5。
2. 第 3.1 节改为生产启动传入现行 `生产运行期配置.动作键`。
3. 第 9 节机械检查同步为 `配置.动作键`。
4. 三参数 `运行期上下文` 的第三参数语义仍为稳定动作键。
5. 不修改 `生产运行期配置.数据.h`，不新增别名，不扩大允许文件。
6. A01—A20、910、13 / 14 / 11 数量和后继路线不变。
7. WT-298-R1 保留只读证据，不在旧基线复用；从本设计提交后另建 WB-298-02 / WT-298-R2。

## 4. 完成边界

本记录只证明 #298 计划字段名已与当前正式 ABI 对齐，不证明代码已实施、910 已登记或 #299 / #304 已解锁。
