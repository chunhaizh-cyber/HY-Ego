# PERSIST-S1-P0-C JY-404 新批次冻结与任务工作树预留

日期：2026-07-18

设计裁决：JY-404

执行队列：#296

设计队列：DQ-188

批次：WB-296-03

## 1. 冻结输入

1. JY-403 已由 `e922c00ef25f7c3de3d79bdff79f36cbf068781c` 发布 `origin/main`，流程图、详细设计、#296 v0.3 和中央路由一致。
2. WB-296-02 的第二次纯断点已由 `889ff28` 发布 main，旧任务 / 集成 worktree 均 clean 且已释放，不得复用。
3. 当前没有其它活动代码任务；#296 是唯一依赖就绪业务候选，#297 和恢复后继继续门控。
4. 当前 Windows 多 worktree 使用 Full access + never，Git 命令不得申请人工授权；项目文件、角色、分支和远端边界不因此扩大。

## 2. 任务 worktree 预留

```text
工作树：WT-296-R3
路径：C:\Users\zhchh\.codex\worktrees\296-p0c-r3\海中鱼巢
分支：codex/296-p0c-r3
计划：#296 / DQ-188 / v0.3
设计预留基线：e922c00ef25f7c3de3d79bdff79f36cbf068781c
```

E146 推送后才可从最新 `origin/main` 创建 worktree。创建完成后必须由 JY-405 核对实际路径、分支、HEAD、worktree / index clean 和远端分支状态，并把 JY-404 发布提交固定为代码冻结基线。预留本身不构成执行许可。

## 3. 所有权冻结

WT-296-R3 只拥有 #296 第 4 节 13 项。共享装配文件 `入口.cpp`、vcxproj、filters 以及三个既有自检模块在 WB-296-03 中只归该任务；WT-MAIN 不并发修改。

生产结构所有权固定为：

```text
运行期概念生命周期阶段与概念活动值类型。
概念活动业务服务的唯一私有活动状态。
一个不透明结构会话内的三抽象状态、四条生命周期关系、重建、读回和一次提交。
运行期业务装配和上下文活动槽与完整性门禁。
660 / 680 / 690 完整性前置机械适配和 Debug 890 独立专项。
```

禁止修改旧生命周期枚举、旧概念图服务、概念图结构数据操作、状态服务 / 状态动态数据操作实现、运行器和中央治理。

## 4. 集成门禁

任务只推送 `origin/codex/296-p0c-r3`，完成状态只能为“分支完成待集成”。后继独立 INT-WB-296-03 必须从冻结父核对 13 文件范围，重跑 Debug / Release Rebuild、两配置完整自检、Debug 连续 20 / 20、strict、diff、模块 / 枚举 / 入口 / Release 静态门禁；全部通过后才能由 WT-MAIN 非强制发布 main。

## 5. 本轮边界

本轮只预留批次和 worktree，不创建代码、不构建、不运行、不登记 890、不解除 #297。E146 推送后继续创建 WT-296-R3，并以 JY-405 独立提交发布创建核对与派发状态。

## 6. 验证

```text
python tools/check_specs.py --strict
git diff --check
git diff --name-only
```
