# TASK-RESULT-STABILITY-S2 JY-471 集成精确起点派发修正

日期：2026-07-19

状态：正式设计记录 / 首次集成未创建 / 待 E216 重新派发

## 1. 偏差

INT-WB-DIAG-06-R1 首次委派声明起点为：

```text
0f684e9651663c017bc1e43f2b750c082f50bd03
```

该对象不存在。fetch 后中央真实状态为：

```text
main == origin/main == 0f684e90b36f48598d6d1eb879cd8677262d2167
WT-MAIN clean
```

偏差来自设计窗口手工写错完整提交哈希，不是 main 前进、任务输入变化或仓库身份漂移。

## 2. 集成窗口收口

集成窗口按“精确起点不一致即停止”门禁正确退回：

```text
目标路径：不存在
本地分支：不存在
远端同名分支：不存在
worktree：不存在
主线发布占用：未取得
```

任务输入 `464a6f2c1d5879bed8a65a159ad25ea48dde1d7e`、唯一父、单记录、blob、remote 0/0 和 WT-313-R4 clean 均未漂移。

## 3. 修正

INT-WB-DIAG-06-R1 的编号、路径、分支、输入、允许范围、禁止范围和纯文档验证矩阵保持不变。正式集成起点改为 E216 本记录发布后的 `origin/main` 精确提交。

E216 推送完成后，设计窗口必须执行 `git rev-parse origin/main` 并把工具返回的完整哈希原样发送，不得手工拼接或从短哈希扩写。目标窗口重新核对后才允许创建集成 worktree 并取得主线发布占用。

## 4. 边界

本记录不创建集成身份，不汇入 `464a6f2`，不构建、不运行、不修订 v0.5、不放行 #310 / #305 / #301 / #306。
