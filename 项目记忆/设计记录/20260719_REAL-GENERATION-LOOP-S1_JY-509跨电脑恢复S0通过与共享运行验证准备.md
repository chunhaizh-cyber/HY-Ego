# JY-509 #315 跨电脑恢复 S0 通过与共享运行验证准备

日期：2026-07-19

## 1. 正式派发与回执事实

- JY-508 / E253 已由 `c835ce4b8bfba500eb47761d359d0ce9642ffb8b` 发布 `origin/main`。
- 正式只读 S0 消息已送达执行窗口 `019f7acd-cc6c-7342-a018-214880665da2`；目标已按 R2 绝对路径接管并返回完整 `PASS`。
- WT-MAIN 始终只读；执行窗口没有修改、构建、运行、暂存、提交或推送。

## 2. S0 接受证据

```text
路径：C:\Users\zhchh\.codex\worktrees\315-real-generation-loop-s1-r2\海中鱼巢
分支：codex/315-real-generation-loop-s1-r2
HEAD：0981e8a89ddb6ba1b853b3b68a65092037088ed1
父提交：57cacbc78cd4aa40e5cd85791baabb80a43e2c48
dirty / index：clean
upstream：无
远端：0/0
中央路由：origin/main@c835ce4b8bfba500eb47761d359d0ce9642ffb8b
产品 / 构建进程：0
共享自检目录：存在但未访问、未占用
```

暂停 WIP 仍精确为六份代码 / 工程 / 入口加一份专属断点。计划 v0.2 与任务分支实施合同、接口、范围和验收矩阵一致；文件名保留 v0.1 只是历史命名，不构成版本漂移。

执行窗口逐项确认：

1. 安全根、服务根均来自正式系统角色清单，首轮只消费安全根，不创建第三根。
2. 生命周期公开请求精确为任务、目标阶段、幂等材料编号、发生时间戳四字段。
3. 任务服务内部读取当前任务并封存当前关系 / 版本，数据操作提交前再次复核。
4. 其余 B0、唯一召回、纯值执行冻结和同租约调用方向一致。
5. 23 个治理稳定键内部唯一，且与系统角色、概念活动和生产动作键无冲突。
6. 方法召回四字段、工程模块顺序、阶段 1100、允许 / 禁止范围均通过。

因此 #315 正式进入 `已派发待执行回执`；#316 继续门控。

## 3. 共享运行验证授权准备

E254 发布后，才向同一执行窗口发送以下授权。代码固定点冻结，不允许修改六份代码 / 工程 / 入口；验证使用当前 `0981e8a` 内容。

共享资源：

```text
D:\海中鱼巣\日志\事件段\自检
```

从正式消息送达到执行窗口明确释放期间，由 WT-315-R2 独占；其它任务不得运行完整或连续自检。验证 stdout / stderr 和机器判定摘要只能写入：

```text
D:\海中鱼巣\日志\验证\REAL-GENERATION-LOOP-S1\E254-WT-315-R2
```

固定顺序：

1. 全新 Debug x64 Rebuild，要求退出 0、0 warning、0 error。
2. Debug x64 单轮 `--self-test-exit`，要求退出 0；`REAL-GENERATION-LOOP-S1-A01` 至 `A12` 恰好 12 项且全部 `=通过`、失败 0。
3. 全新 Release x64 Rebuild，要求退出 0、0 warning、0 error。
4. Release x64 单轮 `--self-test-exit`，要求退出 0；`REAL-GENERATION-LOOP-S1` 专项输出为 0，证明 Release 隔离。
5. Debug x64 从新第 1 轮开始严格连续 20 / 20，轮间 2 秒；每轮退出 0、A01—A12 恰好 12 项且失败 0。

每次产品调用的 stdout / stderr 必须先完整落盘再判定。首个构建非零、产品非零、专项计数 / 标签失败、日志截断、目录竞争或工具异常必须立即停止，不补跑旧编号，不修改业务代码或验收合同；先释放共享目录、确认进程 0，再以专属断点回传设计窗口。

## 4. 成功收口

全部运行验证通过后，执行窗口才允许：

1. 更新既有专属断点，写入新电脑本轮构建、单轮、20 / 20、日志路径 / 哈希、进程 0 和共享目录释放证据。
2. 运行 `tools/check_specs.py --strict`、`git diff --check`、精确七文件、禁止依赖、阶段 1100 唯一和 staged 范围门禁。
3. 只暂存更新后的专属断点；形成 `0981e8a` 之上的新验证记录提交，非强制推送 `origin/codex/315-real-generation-loop-s1-r2`。
4. 状态只写 `分支完成待集成`，不得推送 main、创建集成身份、解锁或消费 #316。

## 5. 完成声明六类证据

- 输入入口：E253 正式消息、执行窗口完整 S0 回执、R2 Git 和实际接口。
- 核心处理：接受无漂移 S0，冻结代码固定点，准备共享运行验证和失败收口。
- 结构承载：计划正文、计划索引、任务队列、当前状态、并行工作树登记表、窗口交互记录和本设计记录。
- 输出变化：#315 从已创建待派发推进为已派发待执行回执；运行验证仍待 E254 消息送达。
- 调用路径：E253 发布 -> 正式 S0 -> 执行窗口 PASS -> E254 设计接受 -> 推送后共享验证。
- 验证方式：线程最终回执、R2 Git、strict、diff 和 staged 范围检查。

## 6. 当前边界

本记录不预先证明构建、单轮或 20 / 20 通过，不把历史构建当新电脑运行证据，不声明任务分支完成或可集成，不解锁 #316。
