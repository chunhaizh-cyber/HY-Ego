# JY-531 #315 WT-315-R7 替代唯一代理可达登记与 S0-A 派发准备

日期：2026-07-20

## 1. 前置

JY-530 / E275 已由 `4a87bdb55912391c7dc4dcb2c9727bae9f81a37f` 非强制发布到 `origin/main`。E275 已正式撤销首个代理 `/root/wt315_r7_execution` 的后继权，并把同一 v0.6 的 S0 拆为 S0-A—S0-D 四个逐轮机械门禁。

## 2. 替代代理创建与回执

E275 发布后，设计窗口创建替代代理 `/root/wt315_r7_execution_v2`，只要求可达性回执，不授权进入 R7 或运行工具。代理明确回复：

```text
canonical task name：/root/wt315_r7_execution_v2
保持待命
不读取工作树
不运行工具
不开始 S0-A
不消费 #315
等待设计窗口正式发布并派发
```

因此本轮只把该代理登记为 R7 替代唯一目标。首个代理仍为中断、后继权撤销状态，不存在两个活动目标。

## 3. R7 身份复核

```text
路径：C:\Users\zhchh\.codex\worktrees\315-real-generation-loop-s1-r7\海中鱼巢
分支：codex/315-real-generation-loop-s1-r7
HEAD：4993a6dd205222e2841fa678ce99639b6cf8b8c2
worktree / index：clean
upstream：无
远端同名分支：不存在
相对 origin/main：领先 0 / 落后 3
```

落后的三份提交是 E273—E275 中央治理，不进入任务分支。替代代理未读取该工作树，所以没有带入、修改、构建、运行、暂存、提交、推送或共享目录占用事实。

## 4. E276 后 S0-A 合同

E276 发布后才向替代唯一代理单独发送 S0-A。S0-A 只核对：

```text
绝对路径与分支
HEAD与E272冻结基线
唯一父
worktree / index clean
无upstream
远端同名分支不存在
相对origin/main仅落后中央治理提交
产品 / 构建进程为0
共享目录为空
```

代理必须立即只返回 S0-A PASS 或具名 DRIFT，不得检查 R6 父链、manifest、路由、范围或中央治理，不得 cherry-pick、修改、构建、运行、占用共享目录、暂存、提交或推送。

设计窗口读取 S0-A 回执并另行形成 JY-532 / E277 前，不得发送 S0-B。S0-A 通过也不授予 S1、写入、构建或运行权。

## 5. 六类证据

- 输入入口：E275 云端提交、替代代理 canonical 零工具待命回执和 R7 Git 只读状态。
- 核心处理：在 E275 后创建替代代理并登记为唯一目标，不合并派发。
- 结构承载：#315 计划、计划索引、任务队列、并行工作树登记表、当前状态、窗口交互记录和本设计记录。
- 输出变化：R7 从“替代代理待创建”推进为“替代唯一目标已登记、S0-A 未派发”。
- 调用路径：E275 发布 -> 创建替代代理 -> 零工具回执 -> JY-531 / E276 -> 单独派发 S0-A。
- 验证方式：canonical 回执、R7 HEAD / 分支 / clean / upstream / 远端、相对主线计数、strict、diff 和 staged 范围检查。

## 6. 完成边界

本记录不证明 S0-A 已发送或通过，不授予 R7 写入、构建、运行、共享目录、暂存、提交、推送或固定带入权限。R7 不可集成，#316 继续依赖门控。
