# JY-508 #315 新电脑独立执行窗口创建核对与正式派发准备

日期：2026-07-19

## 1. 前置事实

- JY-507 / E252 已由 `f5315e50ddb580488d06ab18e94b979a2a5f5bfc` 发布 `origin/main`。
- 用户明确授权设计窗口根据需要创建新对话窗口，并要求按现有规则继续。
- WT-315-R2 仍为 `codex/315-real-generation-loop-s1-r2@0981e8a`，clean、无 upstream、远端 0/0、七文件一致。

## 2. 新窗口创建与回执

设计窗口通过当前项目创建候选独立窗口：

```text
线程：019f7acd-cc6c-7342-a018-214880665da2
标题：#315 WT-315-R2 执行窗口
目标角色：WT-315-R2 执行角色
任务：#315 / DQ-207 / REAL-GENERATION-LOOP-S1 v0.2
```

候选窗口已按要求只读回复：

```text
候选执行窗口已接收，等待正式路由提交与只读 S0 派发。
```

该回执只证明线程可达，不证明正式派发。候选窗口没有开始 S0，没有修改文件，没有运行构建、自检或产品，没有暂存、提交或推送。

## 3. worktree 绑定边界

当前项目接口不支持在创建线程时直接选择既有 Git worktree，因此候选线程初始 cwd 为 `D:\海中鱼巣`。这不授予 WT-MAIN 写入权。

正式 S0 消息必须要求候选窗口：

1. 把所有任务检查和后续执行命令的工作目录固定到 `C:\Users\zhchh\.codex\worktrees\315-real-generation-loop-s1-r2\海中鱼巢`。
2. 核对分支 `codex/315-real-generation-loop-s1-r2`、HEAD `0981e8a89ddb6ba1b853b3b68a65092037088ed1`、父提交 `57cacbc78cd4aa40e5cd85791baabb80a43e2c48`、clean、无 upstream、远端 0/0 和七文件。
3. 对 WT-MAIN、计划、计划索引、任务队列、流程图、详细设计和中央项目记忆保持只读。
4. 只返回 S0 事实；在设计窗口另行授权前，不修改、构建、运行、记录、暂存、提交或推送。

## 4. 当前裁决

```text
候选执行窗口已创建并可达
正式目标已具名待登记
S0 尚未派发
非执行中
不可修改 / 构建 / 运行 / 提交 / 推送 / 集成
#316 继续门控
```

E253 推送成功后，设计窗口才向唯一目标发送 E251 的只读 S0，并通过线程读回核对接收状态。只有正式消息送达且目标从 `origin/main` 重新读取 E253 后，才可进入 `已派发待执行回执`。

## 5. 完成声明六类证据

- 输入入口：用户授权、JY-507 / E252、候选线程创建接口、WT-315-R2 当前 Git 事实。
- 核心处理：创建只读候选窗口，取得可达回执，固定目标线程和既有 R2 绝对路径。
- 结构承载：计划索引、任务队列、当前状态、并行工作树登记表、窗口交互记录和本设计记录。
- 输出变化：旧不可达任务替换为当前电脑可达的候选目标；#315 仍保持已创建待派发。
- 调用路径：用户授权 -> 创建候选线程 -> 候选只读回执 -> E253 中央登记 -> 推送后正式 S0。
- 验证方式：线程创建结果、线程读回、R2 Git 身份、strict、diff 和 staged 范围检查。

## 6. 本轮边界

本轮只登记新候选执行窗口和正式派发前置；不切换当前设计角色，不修改或运行 R2，不把候选回执冒充 S0 或执行回执，不创建集成身份。
