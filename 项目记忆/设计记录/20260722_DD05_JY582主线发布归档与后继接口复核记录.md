# DD-05 JY-582 主线发布归档与后继接口复核记录

日期：2026-07-22

状态：设计窗口正式同步；不构成 C++ 执行许可

## 1. 输入事实

- `main == origin/main == f7a7c75005194a61107d07353e643baf60e1b5bc`，WT-MAIN clean。
- #329 固定任务输入为 `f3dc981c... -> 3c967251...`，独立集成等价汇入为 `d875b9e4... -> 440ac5f4...`。
- 独立集成记录证明十四文件逐 blob 一致；Debug / Release Rebuild 与完整自检通过；DD05F0-A01—A17 17/17、CORE-A01—A10 10/10、具名失败 0；strict 91/91、静态和范围门禁通过。
- WT-MAIN、任务 worktree 与集成 worktree clean，任务和集成远端 0/0，产品进程 0，主线发布占用已释放。

权威证据：

```text
实施记录/20260722_DD05F0_v0.3独立集成_Codex断点清单.md
项目记忆/并行工作树登记表.md 11.40
Git main / origin/main 当前事实
```

## 2. #329 裁决

#329 / DQ-221 / DD-05F0 v0.3 转为 `已完成 / 已归档`。现行计划移入 `计划/已完成计划/`。WT-329-R2 与 INT-WB-DD05F0-02-R1 仅标记可回收，本轮不删除物理 worktree、本地或远端证据分支。

本裁决只证明核心多参与者、4170 运行期批次发布记录、多槽初始 / 换代 / 混合批次和同层参与包已经进入 main；不证明任务状态特征、4170 恢复、旧任务入口迁移或生产闭环完成。

## 3. #330 实际接口 DRIFT

逐签名复核确认：

1. 新任务完整句柄只在需求任务方法数据操作的结构写入会话内产生。
2. 当前特征批次规格要求预先携带有效宿主，特征业务服务还要求宿主在规格形成前已经当前可读。
3. 当前同层参与包只能从已经完整绑定宿主的规格形成，没有“同会话任务候选形成后一次性绑定宿主”的接口。
4. 当前 `特征批次身份` 只有单一 `uint64_t 编号`，没有可证明无碰撞的任务域复合身份或权威映射账。

因此 #330 v0.1 不能直接派发。后继必须先形成独立接口补正，明确候选宿主一次性绑定、绑定时点、同会话互证、复合批次身份和历史读回宿主互证，再形成 #330 v0.2。

## 4. #333 设计解门与计划 DRIFT

#329 实际侧账已经进入 main，#333 的设计依赖解除。但 v0.1 仍写等待 #329 v0.2，且未反映以下实际接口：

- 侧账由 `特征体系数据操作` 私有拥有，不是运行期上下文直接成员；
- 当前访问器只支持按批次身份读取，没有全量冻结导出；
- Debug 自检注入不是生产恢复入口；
- 当前运行期没有隔离恢复候选或完整上下文首次发布接线。

#333 必须先修订为 v0.2，再形成恢复流程图、详细设计、函数 / 结构知识图谱和分层施工计划。恢复候选不得原地注入当前账，必须与四仓、特征值原始材料和 4170 侧账共同互证后随完整隔离上下文首次发布。

## 5. 后继顺序

```text
JY-582 中央发布归档同步
-> 候选宿主绑定与复合批次身份接口补正设计
-> #330 v0.2

JY-582 中央发布归档同步
-> #333 v0.2 与恢复五件套
```

两条设计治理可并行。未来代码是否并行由实际文件、结构、公开接口和装配所有权裁决；不得为了并行复制第二侧账或让两个任务同时拥有 `装配.运行期业务.ixx`。

## 6. 当前验证边界

本记录只同步已经发生的集成发布并登记后继设计差距。本次不修改 C++、工程或自检，不运行构建或程序，不创建或派发执行 worktree。
