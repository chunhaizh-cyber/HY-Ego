# WB-P0B-02 JY-490 集成验证工具异常与独立单轮包装修订

日期：2026-07-19

对象：INT-WB-P0B-02-R1 / #310 / #305

## 1. 正式事实

INT-WB-P0B-02-R1 已从 E234 精确起点首次创建，并按固定顺序把 #310 `dac36f7` 与 #305 `aee7cc4` 无冲突汇入为集成 HEAD `f43b802e50d6c3c900b5713f7d5bbd24c27c632c`。十八个任务文件逐 blob 一致，Debug / Release x64 Rebuild 均为 0 警告 0 错误；集成树、WT-MAIN、两个任务树均 clean。

第一次 Debug 单轮的产品已同步运行并落盘，但外层解析函数调用缺少参数分隔空格，工具返回 1；该轮不计。离线复核随后确认六组真实输出格式不相同：PERSIST-S1-A1 使用数值 `=1 / =0`，SERVICE-DATA-S5 与 TASK-RESULT-S1 使用 `: 通过 / : 失败`，P0-A / B0 / B3C 使用 `=通过 / =失败`。

集成窗口已形成唯一非 Git 判定器 `judge-complete-log.ps1`，SHA-256 为 `3A73DF7552E32901C227B8A388723DD367995C466F031C8A6CEC8C1B940EB01D`，AST 错误数为 0，并对旧日志完成零进程预检。恢复后的全新 Debug 单轮、Release 单轮均通过；连续验证第 1 轮产品与冻结判定器也通过，日志 SHA-256 为 `D0AE5C0D97DF7D9DF8DA06CA03CA603F671092C8088E690A0F5DAEF71E707747`，JSON 摘要 SHA-256 为 `CB1E69E46366BEBD878301BB7FAD03AD2E4A95A8DA62D588DE2490032515F425`。随后外层 `Export-Csv` 参数缺少空格，路径被误绑定为 `Delimiter`，工具返回 1；该轮不计连续完成，第 2—20 轮未启动。

两次停止均发生在非产品工具编排层，不是代码、业务接口、集成冲突或产品自检失败。每次均按门禁立即停止，未补跑，释放共享目录和主线发布占用，确认进程 0；`main == origin/main == 76e26b406f7fe3d152d39e2980dd5a0021bfaf63`，集成 HEAD 仍为 `f43b802`，未写集成记录、提交、推送或发布。

## 2. 裁决

保留 INT-WB-P0B-02-R1 的既有路径、分支、集成 HEAD、两次 cherry-pick 与已通过的两配置 Rebuild、全新 Debug 单轮和 Release 单轮证据；不重建 R2，不 reset、delete、rebase、重新 cherry-pick，也不修改任务代码或业务语义。

连续验证不再由外层工具拼接产品启动、解析和 manifest 命令。集成窗口必须在全新非 Git 目录中建立唯一 `run-debug-round.ps1`，每次只执行一个 Debug 完整自检轮，内部完成：

```text
同步启动一个产品进程
-> 完整 stdout / stderr 先落盘
-> 调用冻结 judge-complete-log.ps1
-> 形成单轮 JSON 摘要
-> 以明确参数写入或追加 TSV manifest
-> 复核日志、摘要、manifest、判定器哈希和进程 0
-> 返回单轮退出码
```

脚本不得包含跨轮循环、后台启动、递归、自行启动下一轮、业务解析副本或产品补跑。外层真实调用固定为：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File <run-debug-round.ps1> -Round N
```

除轮号外不得变化。

## 3. 启动前门禁

真实产品启动前必须同时满足：

1. `run-debug-round.ps1` 经 PowerShell AST `ParseFile` 检查错误数为 0，并冻结 SHA-256；运行期间脚本与哈希不得变化。
2. 脚本只引用既有 `judge-complete-log.ps1`，并复核其 SHA-256 仍为 `3A73DF...B01D`。
3. 脚本的 `-ValidateOnly` 模式不得启动产品；必须使用现有连续第 1 轮 JSON 摘要的副本在可丢弃目录中完整演练 manifest 建立、TSV 分隔、固定表头、单行内容、路径和哈希后验。
4. `-ValidateOnly -Round 1` 必须退出 0，产品进程数为 0，正式连续轮日志与正式 manifest 不得被创建或修改。
5. 集成 HEAD、main / origin、四棵工作树 clean、十八任务 blob、冻结判定器哈希和产品进程 0 必须再次一致。

启动前门禁通过后单独回告固定点；设计窗口登记接受后才重新取得主线发布占用与共享目录独占，执行从新的第 1 轮开始的 Debug 严格 20/20。此前连续第 1 轮仍不计，不补跑旧编号；新 1—20 每轮必须由一次独立固定外层调用完成，轮间 2 秒，首异常立即停止、不补跑。

## 4. 完成边界

新的 20 / 20 全部通过后，才继续 E234 原定的 strict、diff、blob、阶段 910 / 980、禁止依赖、Release 隔离和十九文件范围门禁，再写唯一集成记录、提交集成分支并非强制发布 main。

本裁决不归档 #310 / #305，不解锁 #301 / #306，不消费后继，不改变 #300 / #214 暂停状态，也不把工具异常解释为产品稳定性证据。
