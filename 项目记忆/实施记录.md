# 实施记录

更新时间：2026-07-05

## 1. 本次实施

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求建立项目记忆层，并将云端同步改为每个计划切片完成后自动执行。 |
| 核心处理 | 新建项目记忆文件，更新协作规则和计划索引中的切片收尾动作。 |
| 结构承载 | `项目记忆/当前状态.md`、`项目记忆/决策记录.md`、`项目记忆/实施记录.md`、`项目记忆/待确认问题.md`、`项目记忆/Codex任务队列.md`、`项目记忆/验收记录.md`。 |
| 输出变化 | 项目有可追踪的当前状态、决策、实施、待确认、任务队列和验收记录入口。 |
| 调用路径 | 用户请求 -> 读取项目规则和计划 -> 新建项目记忆层 -> 更新自动同步规则 -> 提交并推送。 |
| 验证方式 | `git status`、`git diff --check`、提交记录和远端推送结果。 |

## 2. 本次追加实施：MC-01 到 MC-11 独立验收输出

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求继续按照计划索引执行。 |
| 核心处理 | 确认领域服务最小入口详细设计和最小闭环第一轮独立验收项，把控制台最小闭环样例拆成 MC-01 到 MC-11 独立输出。 |
| 结构承载 | `海中鱼巣/入口.cpp` 中的本地结构句柄、关系句柄、入口拒绝结果、事务失败边界和默认宏状态。 |
| 输出变化 | 控制台新增 `最小闭环独立验收项: 是` 以及 MC-01 到 MC-11 逐项通过输出。 |
| 调用路径 | 用户继续 -> 计划索引 HR-001 -> 领域服务详细设计和独立验收项确认 -> 控制台入口拆分输出 -> Debug x64 构建 -> 默认 exe 运行。 |
| 验证方式 | `msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m` 通过，运行 `.\x64\Debug\海中鱼巣.exe` 输出 MC-01 到 MC-11 均为通过。 |

## 3. 本次追加实施：基础信息服务分层文档落地

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求将基础信息服务分层、世界聚合服务、高级信息服务依赖基础服务、特征值只服务特征等口径落入文档。 |
| 核心处理 | 新增基础信息服务分层规范，更新项目规则、规则迁移清单、重构总纲、领域服务授权写入规范、领域服务详细设计、语素规范、计划索引和项目记忆。 |
| 结构承载 | `规范/基础信息服务分层规范.md`、`规范/000_项目规则总纲.md`、`规范/详细设计/领域服务最小入口详细设计.md`、`计划/计划索引.md`、`项目记忆/决策记录.md`。 |
| 输出变化 | 项目文档明确基础信息八个原子服务、世界聚合服务、高级信息服务依赖关系、特征值服务边界、实例状态 / 动态与抽象状态 / 动态分层。 |
| 调用路径 | 用户口径 -> 项目规则 -> 基础信息服务分层规范 -> 领域服务详细设计 -> 计划和项目记忆。 |
| 验证方式 | 文档一致性检索、`git diff --check`、提交和云端推送。 |
| 禁止扩大声明 | 本次只声明文档口径已落地，不声明代码已完成服务分层重构。 |

## 4. 本次追加实施：日志系统规范建立

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求增加日志相关规范：逻辑错误报错独立日志，运行日志独立文件记录系统运行情况，调试信息按需建日志，调试日志代码按切片宏控制，调试完成后关闭。 |
| 核心处理 | 新增日志系统规范，并同步更新项目规则总纲、规则迁移清单、计划索引、AGENTS 和项目记忆。 |
| 结构承载 | `规范/日志系统规范.md`、`规范/000_项目规则总纲.md`、`规范/001_规则迁移清单.md`、`计划/计划索引.md`、`AGENTS.md`、`项目记忆/决策记录.md`。 |
| 输出变化 | 明确逻辑错误日志、运行日志、调试日志分离；运行日志独立记录系统运行情况；调试日志切片宏默认关闭，调试完成后必须关闭对应切片调试日志代码。 |
| 调用路径 | 用户口径 -> 项目规则 -> 日志系统规范 -> 计划和项目记忆。 |
| 验证方式 | 文档一致性检索、`git diff --check`、提交和云端推送。 |
| 禁止扩大声明 | 本次只声明日志规范已建立，不声明日志系统代码已实现。 |

## 5. 完成声明边界

```text
文档已生成：本次可声明日志系统规范已落地。
代码已编译：本次未修改代码，未重新运行构建；只能沿用上一切片 Debug x64 构建记录。
入口前置拒绝通过：本次未修改代码，未重新运行入口样例；只能沿用上一切片入口拒绝记录。
最小闭环通过：本次未修改代码，不新增最小闭环验证声明；上一切片只声明第一轮结构样例和 MC-01 到 MC-11 独立输出通过。
旧能力迁移通过：禁止宣称。
真实外设样本通过：禁止宣称。
基础信息服务分层完整实现：禁止宣称；第一轮代码状态见第 9 节。
日志系统代码实现：禁止宣称。
```

## 6. 本次追加实施：第二轮最小闭环增强口径确认

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认第 10 项第二轮最小闭环增强的 6 条口径。 |
| 核心处理 | 新增第二轮增强切片计划，修正 MC-04 到 MC-10 验收边界，补充日志和弹窗诊断规则，更新计划索引和项目记忆。 |
| 结构承载 | `计划/20260705_第二轮最小闭环增强切片.md`、`规范/验收/最小闭环验收用例.md`、`规范/验收/最小闭环第一轮独立验收项.md`、`规范/日志系统规范.md`。 |
| 输出变化 | 第 10 项从待确认变为已确认 / 已建计划；下一步为执行第二轮最小闭环增强切片。 |
| 调用路径 | 用户确认 -> 计划切片 -> 验收口径 -> 项目记忆 -> 后续代码实施。 |
| 验证方式 | 文档一致性检索、`git diff --check`、提交和云端推送。 |
| 禁止扩大声明 | 本次只声明第二轮增强口径已确认，不声明代码已实现或最小闭环完整完成。 |

## 7. 本次追加实施：第二轮最小闭环增强代码实现

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求继续执行下一步，计划索引指向 `计划/20260705_第二轮最小闭环增强切片.md`。 |
| 核心处理 | 当时增加执行前授权结构，改造动态前后值材料和来源动态因果入口，增加仓库结构数量只读接口，更新 MC-04 到 MC-10 输出；该执行前授权口径已在第 13 节按用户后续确认修正。 |
| 结构承载 | `海中鱼巣/领域/方法服务.h`、`海中鱼巣/领域/因果服务.h`、`海中鱼巣/核心/*仓库*`、`海中鱼巣/入口.cpp`。 |
| 输出变化 | 当时默认入口输出 MC-04 选中前未执行目标匹配、MC-05 执行前授权、MC-06 前后值节点、MC-07 来源动态、MC-09 拒绝前后数量一致、MC-10 默认宏关闭；当前入口已改为任务方法关系、发生时间戳和轻量因果引用证据。 |
| 调用路径 | 计划索引 HR-001-A3 -> 方法服务 / 因果服务 / 核心仓库 -> 控制台只读验收输出。 |
| 验证方式 | `git diff --check`、Debug x64 默认 Rebuild、默认 exe 运行、临时宏开启隔离编译。 |
| 禁止扩大声明 | 本次只声明第二轮增强代码通过当前样例验证，不声明完整最小闭环、自我循环或旧能力迁移完成。 |

## 8. 本次追加实施：基础信息服务分层详细设计建立

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 计划队列第 12 项要求建立基础信息服务分层详细设计。 |
| 核心处理 | 新增存在、场景、状态、动态、二次特征和世界聚合服务 6 份详细设计，并补入旧项目详细设计和代码规范参照口径。 |
| 结构承载 | `规范/详细设计/存在服务详细设计.md`、`规范/详细设计/场景服务详细设计.md`、`规范/详细设计/状态服务详细设计.md`、`规范/详细设计/动态服务详细设计.md`、`规范/详细设计/二次特征服务详细设计.md`、`规范/详细设计/世界聚合服务详细设计.md`。 |
| 输出变化 | 基础信息服务分层已有代码重构前详细设计依据；代码重构仍需独立确认。 |
| 调用路径 | 基础信息服务分层规范 -> 6 份服务详细设计 -> 计划索引和项目记忆。 |
| 验证方式 | 文档一致性检索、`git diff --check`、提交和云端推送。 |
| 禁止扩大声明 | 本次只声明详细设计已建立，不声明基础信息服务分层代码已实现。 |

## 9. 本次追加实施：基础信息服务分层第一轮代码重构

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户选择 `1`，确认执行基础信息服务分层代码重构。 |
| 核心处理 | 新增存在、场景、状态、动态、二次特征服务；世界服务改为聚合存在 / 场景服务；任务、需求、方法状态写入改经状态服务；动态记录改经动态服务；语素入口拒绝实例状态和实例动态。 |
| 结构承载 | `海中鱼巣/领域/存在服务.h`、`海中鱼巣/领域/场景服务.h`、`海中鱼巣/领域/状态服务.h`、`海中鱼巣/领域/动态服务.h`、`海中鱼巣/领域/二次特征服务.h`、`海中鱼巣/入口.cpp`。 |
| 输出变化 | 控制台新增 `基础信息服务分层: 第一轮通过`；MC-09 输出实例状态和实例动态语素拒绝；拒绝前后结构数量保持不变。 |
| 调用路径 | 计划索引 HR-001 -> 基础信息服务分层规范 -> 6 份详细设计 -> 原子服务代码 -> 世界聚合服务 -> 控制台入口验收。 |
| 验证方式 | `git diff --check`、Debug x64 Rebuild、运行 `.\x64\Debug\海中鱼巣.exe`。 |
| 禁止扩大声明 | 本次只声明第一轮服务分层代码重构已验证，不声明基础信息服务分层完整完成、旧项目基础信息迁移完成或自我循环完成。 |

## 10. 本次追加实施：日志系统第一轮代码入口

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求继续执行，计划索引下一步为完整日志系统代码入口。 |
| 核心处理 | 新增日志系统详细设计和公共日志入口；容错检查改为调用公共逻辑错误日志入口；入口程序写运行日志；提供调试日志切片宏入口和事件日志独立文件入口。 |
| 结构承载 | `规范/详细设计/日志系统详细设计.md`、`海中鱼巣/核心/日志系统.h`、`海中鱼巣/核心/容错检查.h`、`海中鱼巣/入口.cpp`。 |
| 输出变化 | 默认运行生成 `D:\海中鱼巣\日志\运行.log`；默认运行不生成 `逻辑错误.log` 和 `调试_*.log`；MC-10 仍输出默认宏关闭。 |
| 调用路径 | 计划索引 HR-001 -> 日志系统规范 -> 日志系统详细设计 -> 公共日志入口 -> 容错检查和入口运行日志。 |
| 验证方式 | `git diff --check`、Debug x64 Rebuild、默认 exe 运行、日志文件检查、临时开启 `HY_EGO_ENABLE_FAULT_TOLERANCE_CHECK` 和 `HY_EGO_DEBUG_LOG_MIN_LOOP` 的隔离编译。 |
| 禁止扩大声明 | 本次只声明日志系统第一轮代码入口已验证，不声明事件日志持久化恢复完成，不声明日志参与机器事实或结构修复。 |

## 11. 本次追加实施：阶段 4 交互式迁移启动状态校准

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认“确认,继续”。 |
| 核心处理 | 将阶段 4 旧项目能力迁移入口从“进入前需独立确认”更新为“已按用户确认进入交互式迁移流程”，并建立主信息字段功能确认门禁。 |
| 结构承载 | `计划/计划索引.md`、`实施记录/20260705_阶段4交互式迁移启动与主信息字段确认_Codex断点清单.md`、`实施记录/20260705_主信息字段功能确认待确认清单.md`、`项目记忆/*`。 |
| 输出变化 | 当时下一步明确为确认主信息字段功能清单；当时门禁禁止新增主信息字段或迁移旧代码。当前状态见第 14 节。 |
| 调用路径 | 用户确认 -> 计划索引 HR-001 -> 阶段 4 迁移门禁 -> 主信息字段功能确认清单。 |
| 验证方式 | `git diff --check`、计划索引回读、提交和云端推送。 |
| 禁止扩大声明 | 本次只声明阶段 4 交互式流程已打开并登记下一门禁，不声明旧项目能力或主信息字段已迁移。 |

## 12. 本次追加实施：语素与基础信息字段口径确认

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认语素对应信息多节点、概念多级多路抽象、概念节点 / 概念模板为基础信息抽象节点，以及实例状态 / 实例动态必须带发生时间戳。 |
| 核心处理 | 更新项目规则总纲、规则迁移清单、基础信息服务分层规范、状态服务详细设计、动态服务详细设计和主信息字段功能确认清单。 |
| 结构承载 | `实施记录/20260705_语素与基础信息字段口径确认记录.md`、`规范/000_项目规则总纲.md`、`规范/基础信息服务分层规范.md`。 |
| 输出变化 | 明确实例状态 / 实例动态发生时间戳为必填机器事实字段；抽象状态 / 抽象动态本体不携带发生时间戳；语素对应信息和概念追溯由关系图承载。 |
| 调用路径 | 用户确认 -> 字段功能确认清单 -> 项目规则和详细设计同步。 |
| 验证方式 | 文档一致性检查、`git diff --check`、提交和云端推送。 |
| 禁止扩大声明 | 本次不声明状态 / 动态代码已按时间戳增强，不声明主信息字段或旧项目能力已迁移。 |

## 13. 本次追加实施：状态动态时间戳与权限因果口径同步

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认状态 / 动态发生时间戳代码增强必须第一批实现，并确认因果引用轻量化、方法执行不再需要执行前授权状态节点。 |
| 核心处理 | 主信息值容器补最小按索引 I64 读写；状态服务和动态服务入口增加发生时间戳必填材料和读取入口；方法服务移除 `形成I64写值授权`，执行入口改为任务节点、方法节点、任务方法关系和特征值节点前置拒绝；因果服务保持轻量引用，只用来源动态做准入证据，不写来源动态关系。 |
| 结构承载 | `海中鱼巣/核心/主信息仓库.*`、`海中鱼巣/核心/关系仓库.*`、`海中鱼巣/领域/状态服务.h`、`海中鱼巣/领域/动态服务.h`、`海中鱼巣/领域/方法服务.h`、`海中鱼巣/入口.cpp`。 |
| 输出变化 | 默认入口输出 MC-03 任务状态发生时间戳、MC-05 任务方法关系和无任务方法关系拒绝、MC-06 动态发生时间戳和缺时间戳拒绝、MC-07 来源动态准入且来源动态关系未持久化。 |
| 调用路径 | 用户口径 -> 项目规则 -> 领域服务详细设计 -> 代码入口 -> 默认 Debug x64 构建和入口运行。 |
| 验证方式 | `msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m` 通过，0 警告 0 错误；运行 `.\x64\Debug\海中鱼巣.exe` 退出码 0，MC-01 到 MC-11 均通过。 |
| 禁止扩大声明 | 本次只声明时间戳第一批代码增强、方法权限口径同步和轻量因果引用入口通过当前样例验证；不声明完整状态 / 动态系统、完整方法权限系统或稳定因果结论系统完成。 |

## 14. 本次追加实施：主信息字段功能全部确认与规范更新

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户回复“全部确认”。 |
| 核心处理 | 将 `实施记录/20260705_主信息字段功能确认待确认清单.md` 从待确认清单更新为第一轮已确认清单；把已确认字段处理口径同步到主信息系统规范和主信息系统详细设计；更新计划索引和项目记忆。 |
| 结构承载 | `实施记录/20260705_主信息字段功能确认待确认清单.md`、`规范/主信息系统规范.md`、`规范/详细设计/主信息系统详细设计.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | 主信息字段功能确认门禁已通过；下一步变为建立主信息迁移实施计划或字段迁移映射计划。 |
| 调用路径 | 用户确认 -> 主信息字段功能清单 -> 主信息规范 -> 主信息详细设计 -> 计划索引 -> 项目记忆。 |
| 验证方式 | 文档差异检查、确认清单状态检索、`git diff --check`、提交和云端推送。 |
| 禁止扩大声明 | 本次只声明主信息字段功能第一轮口径已确认并完成文档同步；不声明主信息字段已迁移、主信息代码已迁移或旧项目能力已迁移。 |

## 15. 本次追加实施：主信息迁移实施映射计划建立

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 计划索引 HR-001 下一步要求建立主信息迁移实施计划或字段迁移映射计划。 |
| 核心处理 | 基于主信息系统规范、主信息系统详细设计和已确认字段功能清单，新增主信息迁移实施映射计划，列出必须实现、禁止实现、后续待补、结构映射、代码入口映射、反走样清单、验证剧本和最小切片计划。 |
| 结构承载 | `计划/20260705_主信息迁移实施映射计划_v0.1.md`、`实施记录/20260705_主信息迁移实施映射计划_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | 当前下一步变为确认主信息迁移实施映射计划；确认前不执行 S0 只读代码事实扫描、不新增主信息字段、不迁移旧代码。 |
| 调用路径 | 计划索引 HR-001 -> 主信息规范和详细设计 -> 主信息字段功能确认清单 -> 主信息迁移实施映射计划 -> 项目记忆。 |
| 验证方式 | 文档差异检查、计划索引回读、`git diff --check`、提交和云端推送。 |
| 禁止扩大声明 | 本次只声明迁移实施映射计划已建立，不声明主信息迁移已开始、字段已迁移或旧项目能力已迁移。 |

## 16. 本次追加实施：主信息迁移实施映射计划确认

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户回复“确认”。 |
| 核心处理 | 将 `计划/20260705_主信息迁移实施映射计划_v0.1.md` 状态改为已确认，释放 S0 只读代码事实扫描门禁。 |
| 结构承载 | `计划/20260705_主信息迁移实施映射计划_v0.1.md`、`计划/计划索引.md`、`项目记忆/*`、`实施记录/20260705_主信息迁移实施映射计划确认_Codex断点清单.md`。 |
| 输出变化 | 当前下一步变为执行 S0 主信息迁移只读代码事实扫描。 |
| 调用路径 | 用户确认 -> 计划索引 HR-001 -> 主信息迁移实施映射计划 -> 项目记忆。 |
| 验证方式 | 文档一致性检索、`git diff --check`、提交和云端推送。 |
| 禁止扩大声明 | 本次只声明映射计划已确认，不声明 S0 已完成、代码已迁移或旧项目能力已迁移。 |
