# 实施记录

更新时间：2026-07-12

## 1. 本次实施

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求建立项目记忆层，并将云端同步改为每个计划切片完成后自动执行。 |
| 核心处理 | 新建项目记忆文件，更新协作规则和计划索引中的切片收尾动作。 |
| 结构承载 | `项目记忆/当前状态.md`、`项目记忆/决策记录.md`、`项目记忆/实施记录.md`、`项目记忆/待确认问题.md`、`项目记忆/Codex任务队列.md`、`项目记忆/验收记录.md`。 |
| 输出变化 | 项目有可追踪的当前状态、决策、实施、待确认、任务队列和验收记录入口。 |
| 调用路径 | 用户请求 -> 读取项目规则和计划 -> 新建项目记忆层 -> 更新自动同步规则 -> 提交并推送。 |
| 验证方式 | `git status`、`git diff --check`、提交记录和远端推送结果。 |

## 1.1 本次追加实施：仓库读写锁规则迁移

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求参考 `D:\鱼巢` 中锁规则，如可行则直接迁移到本项目。 |
| 核心处理 | 只迁移旧项目共享结构读写锁规则；不迁移线程调度、任务线程池、`std::async`、SQL 投影锁、控制面板线程或外设线程。 |
| 结构承载 | `海中鱼巣/核心/节点仓库.*`、`海中鱼巣/核心/主信息仓库.*`、`海中鱼巣/核心/关系仓库.*`、`海中鱼巣/核心/索引仓库.*`。 |
| 输出变化 | 四个核心仓库新增仓库级 `shared_mutex`；读入口使用共享锁并返回副本 / 句柄材料；写入口使用独占锁；关系仓库新增 `_已加锁` 内部辅助函数避免递归持锁。 |
| 调用路径 | 用户口径 -> 旧项目锁规则证据 -> #129 S0a 仓库锁子切片 -> 核心仓库入口 -> Debug x64 构建和默认入口运行。 |
| 验证方式 | `msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m` 通过；`.\x64\Debug\海中鱼巣.exe` 通过；`git diff --check` 无错误；`python .\tools\check_specs.py` 未发现问题；排除项扫描未命中。 |
| 禁止扩大声明 | 只声明核心仓库最小读写锁边界已实现并验证；不声明 #129 服务矩阵全部完成、真实多线程调度完成、并发压力测试完成、旧能力迁移完成。 |

## 1.2 本次追加实施：多线程防锁机制规范落地

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求将本项目多线程防锁机制落入规范。 |
| 核心处理 | 新增多线程防锁机制规范，明确锁只保护运行期内存结构一致性，不承载机器事实，不生成动作来源；锁定单仓库持锁优先、多仓库持锁顺序、禁止持锁调用外部等待 / 日志落盘 / 弹窗 / 上层服务和读锁升级写锁等规则。 |
| 结构承载 | `规范/多线程防锁机制规范.md`、`规范/000_项目规则总纲.md`、`规范/001_规则迁移清单.md`、`计划/计划索引.md`、`项目记忆/当前状态.md`、`项目记忆/决策记录.md`、`项目记忆/验收记录.md`。 |
| 输出变化 | 多线程防锁机制从 #129 S0a 实施记录和规则迁移清单中的初步口径，提升为可引用的项目级规范；轻量规范检查工具新增该规范文件和关键短语检查。 |
| 调用路径 | 用户口径 -> AGENTS 开工前必读 -> 现有仓库锁记录 -> 多线程防锁机制规范 -> 总纲 / 清单 / 计划索引 / 项目记忆同步。 |
| 验证方式 | `git diff --check`、`python .\tools\check_specs.py`、关键短语检索。 |
| 禁止扩大声明 | 本次只声明规范已确认和文档已生成；不声明 C++ 代码新增、真实线程调度完成、工作队列完成、`std::async` 接入、并发压力测试通过或旧能力迁移完成。 |

## 2. 本次追加实施：MC-01 到 MC-11 独立验收输出

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求继续按照计划索引执行。 |
| 核心处理 | 确认领域服务最小入口详细设计和最小闭环第一轮独立验收项，把控制台最小闭环样例拆成 MC-01 到 MC-11 独立输出。 |
| 结构承载 | `海中鱼巣/入口.cpp` 中的本地结构句柄、关系句柄、入口拒绝结果、事务失败边界和默认宏状态。 |
| 输出变化 | 控制台新增 `最小闭环独立验收项: 是` 以及 MC-01 到 MC-11 逐项通过输出。 |
| 调用路径 | 用户继续 -> 计划索引 HR-001 -> 领域服务详细设计和独立验收项确认 -> 控制台入口拆分输出 -> Debug x64 构建 -> 默认 exe 运行。 |
| 验证方式 | `msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m` 通过，运行 `.\x64\Debug\海中鱼巣.exe` 输出 MC-01 到 MC-11 均为通过。 |

## 3. 本次追加实施：基础信息服务分层文档落地

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求将基础信息服务分层、世界聚合服务、高级信息服务依赖基础服务、特征值只服务特征等口径落入文档。 |
| 核心处理 | 新增基础信息服务分层规范，更新项目规则、规则迁移清单、重构总纲、领域服务授权写入规范、领域服务详细设计、语素规范、计划索引和项目记忆。 |
| 结构承载 | `规范/基础信息服务分层规范.md`、`规范/000_项目规则总纲.md`、`规范/详细设计/领域服务最小入口详细设计.md`、`计划/计划索引.md`、`项目记忆/决策记录.md`。 |
| 输出变化 | 项目文档明确基础信息八个原子服务、世界聚合服务、高级信息服务依赖关系、特征值服务边界、实例状态 / 动态与抽象状态 / 动态分层。 |
| 调用路径 | 用户口径 -> 项目规则 -> 基础信息服务分层规范 -> 领域服务详细设计 -> 计划和项目记忆。 |
| 验证方式 | 文档一致性检索、`git diff --check`、提交和云端推送。 |
| 禁止扩大声明 | 本次只声明文档口径已落地，不声明代码已完成服务分层重构。 |

## 4. 本次追加实施：日志系统规范建立

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求增加日志相关规范：逻辑错误报错独立日志，运行日志独立文件记录系统运行情况，调试信息按需建日志，调试日志代码按切片宏控制，调试完成后关闭。 |
| 核心处理 | 新增日志系统规范，并同步更新项目规则总纲、规则迁移清单、计划索引、AGENTS 和项目记忆。 |
| 结构承载 | `规范/日志系统规范.md`、`规范/000_项目规则总纲.md`、`规范/001_规则迁移清单.md`、`计划/计划索引.md`、`AGENTS.md`、`项目记忆/决策记录.md`。 |
| 输出变化 | 明确逻辑错误日志、运行日志、调试日志分离；运行日志独立记录系统运行情况；调试日志切片宏默认关闭，调试完成后必须关闭对应切片调试日志代码。 |
| 调用路径 | 用户口径 -> 项目规则 -> 日志系统规范 -> 计划和项目记忆。 |
| 验证方式 | 文档一致性检索、`git diff --check`、提交和云端推送。 |
| 禁止扩大声明 | 本次只声明日志规范已建立，不声明日志系统代码已实现。 |

## 5. 完成声明边界

```text
文档已生成：本次可声明日志系统规范已落地。
代码已编译：本次未修改代码，未重新运行构建；只能沿用上一切片 Debug x64 构建记录。
入口前置拒绝通过：本次未修改代码，未重新运行入口样例；只能沿用上一切片入口拒绝记录。
最小闭环通过：本次未修改代码，不新增最小闭环验证声明；上一切片只声明第一轮结构样例和 MC-01 到 MC-11 独立输出通过。
旧能力迁移通过：禁止宣称。
真实外设样本通过：禁止宣称。
基础信息服务分层完整实现：禁止宣称；第一轮代码状态见第 9 节。
日志系统代码实现：禁止宣称。
```

## 6. 本次追加实施：第二轮最小闭环增强口径确认

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认第 10 项第二轮最小闭环增强的 6 条口径。 |
| 核心处理 | 新增第二轮增强切片计划，修正 MC-04 到 MC-10 验收边界，补充日志和弹窗诊断规则，更新计划索引和项目记忆。 |
| 结构承载 | `计划/已完成计划/20260705_第二轮最小闭环增强切片.md`、`规范/验收/最小闭环验收用例.md`、`规范/验收/最小闭环第一轮独立验收项.md`、`规范/日志系统规范.md`。 |
| 输出变化 | 第 10 项从待确认变为已确认 / 已建计划；下一步为执行第二轮最小闭环增强切片。 |
| 调用路径 | 用户确认 -> 计划切片 -> 验收口径 -> 项目记忆 -> 后续代码实施。 |
| 验证方式 | 文档一致性检索、`git diff --check`、提交和云端推送。 |
| 禁止扩大声明 | 本次只声明第二轮增强口径已确认，不声明代码已实现或最小闭环完整完成。 |

## 7. 本次追加实施：第二轮最小闭环增强代码实现

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求继续执行下一步，计划索引指向 `计划/已完成计划/20260705_第二轮最小闭环增强切片.md`。 |
| 核心处理 | 当时增加执行前授权结构，改造动态前后值材料和来源动态因果入口，增加仓库结构数量只读接口，更新 MC-04 到 MC-10 输出；该执行前授权口径已在第 13 节按用户后续确认修正。 |
| 结构承载 | `海中鱼巣/领域/方法服务.h`、`海中鱼巣/领域/因果服务.h`、`海中鱼巣/核心/*仓库*`、`海中鱼巣/入口.cpp`。 |
| 输出变化 | 当时默认入口输出 MC-04 选中前未执行目标匹配、MC-05 执行前授权、MC-06 前后值节点、MC-07 来源动态、MC-09 拒绝前后数量一致、MC-10 默认宏关闭；当前入口已改为任务方法关系、发生时间戳和轻量因果引用证据。 |
| 调用路径 | 计划索引 HR-001-A3 -> 方法服务 / 因果服务 / 核心仓库 -> 控制台只读验收输出。 |
| 验证方式 | `git diff --check`、Debug x64 默认 Rebuild、默认 exe 运行、临时宏开启隔离编译。 |
| 禁止扩大声明 | 本次只声明第二轮增强代码通过当前样例验证，不声明完整最小闭环、自我循环或旧能力迁移完成。 |

## 8. 本次追加实施：基础信息服务分层详细设计建立

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 计划队列第 12 项要求建立基础信息服务分层详细设计。 |
| 核心处理 | 新增存在、场景、状态、动态、二次特征和世界聚合服务 6 份详细设计，并补入旧项目详细设计和代码规范参照口径。 |
| 结构承载 | `规范/详细设计/存在服务详细设计.md`、`规范/详细设计/场景服务详细设计.md`、`规范/详细设计/状态服务详细设计.md`、`规范/详细设计/动态服务详细设计.md`、`规范/详细设计/二次特征服务详细设计.md`、`规范/详细设计/世界聚合服务详细设计.md`。 |
| 输出变化 | 基础信息服务分层已有代码重构前详细设计依据；代码重构仍需独立确认。 |
| 调用路径 | 基础信息服务分层规范 -> 6 份服务详细设计 -> 计划索引和项目记忆。 |
| 验证方式 | 文档一致性检索、`git diff --check`、提交和云端推送。 |
| 禁止扩大声明 | 本次只声明详细设计已建立，不声明基础信息服务分层代码已实现。 |

## 9. 本次追加实施：基础信息服务分层第一轮代码重构

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户选择 `1`，确认执行基础信息服务分层代码重构。 |
| 核心处理 | 新增存在、场景、状态、动态、二次特征服务；世界服务改为聚合存在 / 场景服务；任务、需求、方法状态写入改经状态服务；动态记录改经动态服务；语素入口拒绝实例状态和实例动态。 |
| 结构承载 | `海中鱼巣/领域/存在服务.h`、`海中鱼巣/领域/场景服务.h`、`海中鱼巣/领域/状态服务.h`、`海中鱼巣/领域/动态服务.h`、`海中鱼巣/领域/二次特征服务.h`、`海中鱼巣/入口.cpp`。 |
| 输出变化 | 控制台新增 `基础信息服务分层: 第一轮通过`；MC-09 输出实例状态和实例动态语素拒绝；拒绝前后结构数量保持不变。 |
| 调用路径 | 计划索引 HR-001 -> 基础信息服务分层规范 -> 6 份详细设计 -> 原子服务代码 -> 世界聚合服务 -> 控制台入口验收。 |
| 验证方式 | `git diff --check`、Debug x64 Rebuild、运行 `.\x64\Debug\海中鱼巣.exe`。 |
| 禁止扩大声明 | 本次只声明第一轮服务分层代码重构已验证，不声明基础信息服务分层完整完成、旧项目基础信息迁移完成或自我循环完成。 |

## 10. 本次追加实施：日志系统第一轮代码入口

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求继续执行，计划索引下一步为完整日志系统代码入口。 |
| 核心处理 | 新增日志系统详细设计和公共日志入口；容错检查改为调用公共逻辑错误日志入口；入口程序写运行日志；提供调试日志切片宏入口和事件日志独立文件入口。 |
| 结构承载 | `规范/详细设计/日志系统详细设计.md`、`海中鱼巣/核心/日志系统.h`、`海中鱼巣/核心/容错检查.h`、`海中鱼巣/入口.cpp`。 |
| 输出变化 | 默认运行生成 `D:\海中鱼巣\日志\运行.log`；默认运行不生成 `逻辑错误.log` 和 `调试_*.log`；MC-10 仍输出默认宏关闭。 |
| 调用路径 | 计划索引 HR-001 -> 日志系统规范 -> 日志系统详细设计 -> 公共日志入口 -> 容错检查和入口运行日志。 |
| 验证方式 | `git diff --check`、Debug x64 Rebuild、默认 exe 运行、日志文件检查、临时开启 `HY_EGO_ENABLE_FAULT_TOLERANCE_CHECK` 和 `HY_EGO_DEBUG_LOG_MIN_LOOP` 的隔离编译。 |
| 禁止扩大声明 | 本次只声明日志系统第一轮代码入口已验证，不声明事件日志持久化恢复完成，不声明日志参与机器事实或结构修复。 |

## 11. 本次追加实施：阶段 4 交互式迁移启动状态校准

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认“确认,继续”。 |
| 核心处理 | 将阶段 4 旧项目能力迁移入口从“进入前需独立确认”更新为“已按用户确认进入交互式迁移流程”，并建立主信息字段功能确认门禁。 |
| 结构承载 | `计划/计划索引.md`、`实施记录/20260705_阶段4交互式迁移启动与主信息字段确认_Codex断点清单.md`、`实施记录/20260705_主信息字段功能确认待确认清单.md`、`项目记忆/*`。 |
| 输出变化 | 当时下一步明确为确认主信息字段功能清单；当时门禁禁止新增主信息字段或迁移旧代码。当前状态见第 14 节。 |
| 调用路径 | 用户确认 -> 计划索引 HR-001 -> 阶段 4 迁移门禁 -> 主信息字段功能确认清单。 |
| 验证方式 | `git diff --check`、计划索引回读、提交和云端推送。 |
| 禁止扩大声明 | 本次只声明阶段 4 交互式流程已打开并登记下一门禁，不声明旧项目能力或主信息字段已迁移。 |

## 12. 本次追加实施：语素与基础信息字段口径确认

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认语素对应信息多节点、概念多级多路抽象、概念节点 / 概念模板为基础信息抽象节点，以及实例状态 / 实例动态必须带发生时间戳。 |
| 核心处理 | 更新项目规则总纲、规则迁移清单、基础信息服务分层规范、状态服务详细设计、动态服务详细设计和主信息字段功能确认清单。 |
| 结构承载 | `实施记录/20260705_语素与基础信息字段口径确认记录.md`、`规范/000_项目规则总纲.md`、`规范/基础信息服务分层规范.md`。 |
| 输出变化 | 明确实例状态 / 实例动态发生时间戳为必填机器事实字段；抽象状态 / 抽象动态本体不携带发生时间戳；语素对应信息和概念追溯由关系图承载。 |
| 调用路径 | 用户确认 -> 字段功能确认清单 -> 项目规则和详细设计同步。 |
| 验证方式 | 文档一致性检查、`git diff --check`、提交和云端推送。 |
| 禁止扩大声明 | 本次不声明状态 / 动态代码已按时间戳增强，不声明主信息字段或旧项目能力已迁移。 |

## 13. 本次追加实施：状态动态时间戳与权限因果口径同步

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认状态 / 动态发生时间戳代码增强必须第一批实现，并确认因果引用轻量化、方法执行不再需要执行前授权状态节点。 |
| 核心处理 | 主信息值容器补最小按索引 I64 读写；状态服务和动态服务入口增加发生时间戳必填材料和读取入口；方法服务移除 `形成I64写值授权`，执行入口改为任务节点、方法节点、任务方法关系和特征值节点前置拒绝；因果服务保持轻量引用，只用来源动态做准入证据，不写来源动态关系。 |
| 结构承载 | `海中鱼巣/核心/主信息仓库.*`、`海中鱼巣/核心/关系仓库.*`、`海中鱼巣/领域/状态服务.h`、`海中鱼巣/领域/动态服务.h`、`海中鱼巣/领域/方法服务.h`、`海中鱼巣/入口.cpp`。 |
| 输出变化 | 默认入口输出 MC-03 任务状态发生时间戳、MC-05 任务方法关系和无任务方法关系拒绝、MC-06 动态发生时间戳和缺时间戳拒绝、MC-07 来源动态准入且来源动态关系未持久化。 |
| 调用路径 | 用户口径 -> 项目规则 -> 领域服务详细设计 -> 代码入口 -> 默认 Debug x64 构建和入口运行。 |
| 验证方式 | `msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m` 通过，0 警告 0 错误；运行 `.\x64\Debug\海中鱼巣.exe` 退出码 0，MC-01 到 MC-11 均通过。 |
| 禁止扩大声明 | 本次只声明时间戳第一批代码增强、方法权限口径同步和轻量因果引用入口通过当前样例验证；不声明完整状态 / 动态系统、完整方法权限系统或稳定因果结论系统完成。 |

## 14. 本次追加实施：主信息字段功能全部确认与规范更新

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户回复“全部确认”。 |
| 核心处理 | 将 `实施记录/20260705_主信息字段功能确认待确认清单.md` 从待确认清单更新为第一轮已确认清单；把已确认字段处理口径同步到主信息系统规范和主信息系统详细设计；更新计划索引和项目记忆。 |
| 结构承载 | `实施记录/20260705_主信息字段功能确认待确认清单.md`、`规范/主信息系统规范.md`、`规范/详细设计/主信息系统详细设计.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | 主信息字段功能确认门禁已通过；下一步变为建立主信息迁移实施计划或字段迁移映射计划。 |
| 调用路径 | 用户确认 -> 主信息字段功能清单 -> 主信息规范 -> 主信息详细设计 -> 计划索引 -> 项目记忆。 |
| 验证方式 | 文档差异检查、确认清单状态检索、`git diff --check`、提交和云端推送。 |
| 禁止扩大声明 | 本次只声明主信息字段功能第一轮口径已确认并完成文档同步；不声明主信息字段已迁移、主信息代码已迁移或旧项目能力已迁移。 |

## 15. 本次追加实施：主信息迁移实施映射计划建立

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 计划索引 HR-001 下一步要求建立主信息迁移实施计划或字段迁移映射计划。 |
| 核心处理 | 基于主信息系统规范、主信息系统详细设计和已确认字段功能清单，新增主信息迁移实施映射计划，列出必须实现、禁止实现、后续待补、结构映射、代码入口映射、反走样清单、验证剧本和最小切片计划。 |
| 结构承载 | `计划/20260705_主信息迁移实施映射计划_v0.1.md`、`实施记录/20260705_主信息迁移实施映射计划_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | 当前下一步变为确认主信息迁移实施映射计划；确认前不执行 S0 只读代码事实扫描、不新增主信息字段、不迁移旧代码。 |
| 调用路径 | 计划索引 HR-001 -> 主信息规范和详细设计 -> 主信息字段功能确认清单 -> 主信息迁移实施映射计划 -> 项目记忆。 |
| 验证方式 | 文档差异检查、计划索引回读、`git diff --check`、提交和云端推送。 |
| 禁止扩大声明 | 本次只声明迁移实施映射计划已建立，不声明主信息迁移已开始、字段已迁移或旧项目能力已迁移。 |

## 16. 本次追加实施：主信息迁移实施映射计划确认

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户回复“确认”。 |
| 核心处理 | 将 `计划/20260705_主信息迁移实施映射计划_v0.1.md` 状态改为已确认，释放 S0 只读代码事实扫描门禁。 |
| 结构承载 | `计划/20260705_主信息迁移实施映射计划_v0.1.md`、`计划/计划索引.md`、`项目记忆/*`、`实施记录/20260705_主信息迁移实施映射计划确认_Codex断点清单.md`。 |
| 输出变化 | 当时下一步变为执行 S0 主信息迁移只读代码事实扫描；当前状态见第 17 节。 |
| 调用路径 | 用户确认 -> 计划索引 HR-001 -> 主信息迁移实施映射计划 -> 项目记忆。 |
| 验证方式 | 文档一致性检索、`git diff --check`、提交和云端推送。 |
| 禁止扩大声明 | 本次只声明映射计划已确认；当时不声明 S0 已完成、代码已迁移或旧项目能力已迁移。 |

## 17. 本次追加实施：主信息迁移 S0 只读代码事实扫描

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 主信息迁移实施映射计划已确认，计划索引下一步为执行 S0 只读代码事实扫描。 |
| 核心处理 | 只读扫描主信息仓库、关系仓库、特征值服务、语素服务、需求服务、方法服务、入口程序和函数说明书状态。 |
| 结构承载 | `实施记录/20260705_主信息迁移S0只读代码事实扫描_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | 记录主信息仓库最小函数已存在、按索引 I64 函数已存在、旧项目各类主信息类未命中；同时记录当时发现的 3 项缺口：语素对应信息 / 概念追溯当前单目标，需求服务仍直接写需求节点主信息值容器，函数功能说明文档当时不存在。当前缺口 3 已补函数功能说明清单并经用户确认。 |
| 调用路径 | 计划索引 HR-001 -> 主信息迁移实施映射计划 -> S0 只读扫描 -> S0 扫描记录 -> 项目记忆。 |
| 验证方式 | `rg` 只读扫描、关键文件读取、`git diff --check`、提交和云端推送。 |
| 禁止扩大声明 | 本次只声明 S0 当前代码事实扫描完成，不声明主信息迁移完成、不声明语素多对应实现完成、不声明需求目标值承载问题已修复。 |

## 18. 本次追加实施：S0 缺口处理顺序确认与语素多对应实现

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求按建议执行，计划索引 HR-001 下一步为确认 S0 缺口处理顺序。 |
| 核心处理 | 确认 S0 缺口处理顺序为先语素多对应 / 多路追溯，再需求目标状态承载专项，最后函数功能说明；同步修正关系仓库和语素系统详细设计中的单目标旧描述；新增关系目标集合读取和语素追加 / 集合读取入口。 |
| 结构承载 | `海中鱼巣/核心/关系仓库.*`、`海中鱼巣/领域/语素服务.h`、`海中鱼巣/入口.cpp`、`规范/详细设计/关系仓库详细设计.md`、`规范/详细设计/语素系统详细设计.md`。 |
| 输出变化 | 默认入口新增 `语素多对应追溯: 是 | 对应信息数=2 概念追溯数=2`；旧的单目标读取保留为兼容入口，但多目标事实必须通过集合读取。 |
| 调用路径 | 计划索引 HR-001 -> S0 缺口处理顺序确认 -> 关系仓库集合读取 -> 语素服务追加和集合读取 -> 控制台入口结构样例。 |
| 验证方式 | `git diff --check` 通过；Debug x64 Rebuild 通过，0 警告 0 错误；默认 exe 运行退出码 0，语素多对应追溯通过，MC-01 到 MC-11 全部通过。 |
| 禁止扩大声明 | 本次只声明 S0 缺口 1 在当前样例中验证通过；不声明完整语素系统、候选查询、冲突包、自然语言理解、主信息迁移或旧能力迁移完成。 |

## 19. 本次追加实施：需求目标承载专项建立（旧称需求目标值）

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 计划索引 HR-001 下一步要求建立需求目标承载专项，处理需求服务直接写需求节点主信息值容器的问题；该记录当时误称为需求目标值。 |
| 核心处理 | 只读扫描需求服务、状态服务、主信息详细设计、基础信息服务分层规范和最小闭环验收项；新增当时的需求目标承载详细设计和专项计划；将领域服务最小入口详细设计中的旧需求目标主信息承载标记为 S0 缺口 2。 |
| 结构承载 | 该记录对应文件已在后续第 20 节修正为 `规范/详细设计/需求目标状态承载详细设计.md`、`计划/已完成计划/20260705_需求目标状态承载专项_v0.1.md`。 |
| 输出变化 | 当时下一步变为确认需求目标承载专项；后续已按用户口径修正为需求目标状态并进入 DV-1。 |
| 调用路径 | 计划索引 HR-001 -> S0 缺口 2 -> 需求目标承载详细设计 -> 专项计划 -> 项目记忆。 |
| 验证方式 | 文档一致性检索、`git diff --check`、提交和云端推送。 |
| 禁止扩大声明 | 本次只声明专项文档已建立，不声明代码已改造、缺口已修复、主信息迁移完成或旧能力迁移完成。 |

## 20. 本次追加实施：需求目标状态承载和特征值访问边界改造

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认需求需要的是状态不是 I64，并确认特征值只有特征类能够直接访问；高级服务只允许直接依赖特征、存在、场景、二次特征、状态、动态和因果服务。 |
| 核心处理 | 将此前误称的需求目标值专项修正为需求目标状态专项；特征服务封装特征值创建、读取和写入；需求服务改为创建目标状态需求、读取需求目标状态、结算目标状态需求；方法服务改为通过特征服务执行特征状态写入。 |
| 结构承载 | `规范/详细设计/需求目标状态承载详细设计.md`、`计划/已完成计划/20260705_需求目标状态承载专项_v0.1.md`、`海中鱼巣/领域/特征服务.h`、`海中鱼巣/领域/需求服务.h`、`海中鱼巣/领域/方法服务.h`、`海中鱼巣/入口.cpp`。 |
| 输出变化 | 需求节点不再通过活动入口写入需求主信息目标值；需求通过模板关系回查目标状态；需求和方法服务不再直接包含或接收特征值服务。 |
| 调用路径 | 用户口径 -> 项目规则 -> 需求目标状态详细设计 -> 特征服务封口 -> 需求 / 方法服务改造 -> 默认入口验证。 |
| 验证方式 | `git diff --check` 通过；Debug x64 Rebuild 通过，0 警告 0 错误；默认入口运行退出码 0，MC-01 到 MC-11 全部通过，MC-02 输出目标状态节点和值。 |
| 禁止扩大声明 | 本次只声明需求目标状态承载 DV-1 当前入口验证通过；不声明完整需求系统、主信息迁移、旧能力迁移或完整特征值权限系统完成。 |

## 21. 本次追加实施：函数功能说明清单建立

| 项目 | 内容 |
| --- | --- |
| 输入入口 | S0 缺口 3 要求补函数功能说明清单；用户补充高级服务白名单中必须包含场景服务。 |
| 核心处理 | 新建当前函数功能说明文档和专项计划；按核心层、日志容错、领域服务、入口程序和私有辅助函数列出功能、用途、使用范围和对应规范；同步项目记忆和计划状态。 |
| 结构承载 | `说明书/函数功能说明文档.md`、`计划/已完成计划/20260705_函数功能说明清单专项_v0.1.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | S0 缺口 3 从“函数功能说明文档不存在”更新为“函数功能说明清单已建立 / 待确认”；高级服务白名单明确包含存在、场景、特征、二次特征、状态、动态和因果服务。 |
| 调用路径 | 计划索引 HR-001 -> HR-001-FD 函数说明专项 -> 函数说明文档 -> 项目记忆和实施记录。 |
| 验证方式 | 文档差异检查、白名单一致性检索、`git diff --check`、提交和云端推送。 |
| 禁止扩大声明 | 本次只声明函数说明清单已建立，不声明函数说明已确认、旧函数已迁移或旧项目能力已迁移。 |

## 22. 本次追加实施：关系仓库节点移动统一入口增强

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认不要每个服务重复写移动逻辑，按“普通父子移动统一走关系仓库重挂入口”的建议修改。 |
| 核心处理 | 将节点移动口径写入项目规则、规则迁移清单和关系仓库详细设计；增强 `关系仓库::重挂节点`，在入口拒绝移动到自身、移动到自身子孙、移动到当前父节点和缺当前父关系；入口样例补充重挂拒绝输出。 |
| 结构承载 | `海中鱼巣/核心/关系仓库.*`、`海中鱼巣/入口.cpp`、`规范/详细设计/关系仓库详细设计.md`、`计划/已完成计划/20260706_关系仓库节点移动统一入口专项_v0.1.md`。 |
| 输出变化 | 关系仓库成为普通父子移动唯一通用入口；各领域服务后续不得复制移动算法，只能封装本领域前置拒绝后调用通用入口。 |
| 调用路径 | 用户口径 -> 项目规则 -> 关系仓库详细设计 -> `关系仓库::重挂节点` -> 默认入口样例。 |
| 验证方式 | `git diff --check` 通过；Debug x64 Rebuild 通过，0 警告 0 错误；默认入口运行退出码 0，输出 `重挂拒绝: 自身=是 子孙=是 当前父=是`，MC-01 到 MC-11 均通过。 |
| 禁止扩大声明 | 本次只声明普通父子移动统一入口增强，不声明完整结构移动系统、领域移动语义或旧项目能力迁移完成。 |

## 23. 本次追加实施：函数功能说明文档确认

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户回复“全部确认”，按当前计划索引门禁解释为确认 `说明书/函数功能说明文档.md`。 |
| 核心处理 | 将函数说明文档、HR-001-FD 专项、计划索引和项目记忆中的函数说明状态从待确认推进为已确认；关闭 QR-017；下一步推进到 S1 主信息仓库最小函数复验。 |
| 结构承载 | `说明书/函数功能说明文档.md`、`计划/已完成计划/20260705_函数功能说明清单专项_v0.1.md`、`计划/20260705_主信息迁移实施映射计划_v0.1.md`、`计划/计划索引.md`、`实施记录/20260706_函数功能说明文档确认_Codex断点清单.md`、`项目记忆/*`。 |
| 输出变化 | 当前项目已有函数的功能、用途、使用范围和对应规范说明已确认；S0 缺口 3 门禁解除；下一步允许执行 S1 主信息仓库最小函数复验。 |
| 调用路径 | 用户确认 -> HR-001-FD 函数说明专项 -> 函数说明文档 -> 计划索引 HR-001 -> S1 主信息仓库最小函数复验。 |
| 验证方式 | `rg` 检查确认状态；`git diff --check`；文档切片未改代码，默认不运行构建。 |
| 禁止扩大声明 | 本次只声明当前项目函数说明清单已确认；不声明旧函数已迁移、主信息迁移完成或旧项目能力已迁移。 |

## 24. 本次追加实施：S1 主信息仓库最小函数复验

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 计划索引 HR-001 下一步要求执行 S1 主信息仓库最小函数复验；复验前不得迁移旧函数体。 |
| 核心处理 | 在默认入口增加主信息仓库最小函数复验，覆盖创建、读取、有效判断、单槽 / 分槽 I64、未写槽为空、删除后旧句柄失效、编号不复用和无效写入拒绝；修复 `std::vector<std::int64_t>` 无法区分未写槽位和已写入 0 的问题，改为可空槽位。 |
| 结构承载 | `海中鱼巣/核心/主信息仓库.*`、`海中鱼巣/入口.cpp`、`规范/详细设计/主信息系统详细设计.md`、`实施记录/20260706_主信息仓库最小函数复验_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | 默认入口新增 `主信息仓库复验: 是 | 单槽=42 分槽=84 未写槽为空=是`；`程序通过` 条件纳入主信息仓库最小函数复验。 |
| 调用路径 | 计划索引 HR-001 -> 主信息迁移实施映射计划 S1 -> 主信息仓库 -> 默认入口复验 -> 计划和项目记忆回写。 |
| 验证方式 | `git diff --check` 通过；Debug x64 Rebuild 通过，0 警告 0 错误；默认入口运行退出码 0，主信息仓库复验通过，MC-01 到 MC-11 全部通过。 |
| 禁止扩大声明 | 本次只声明统一主信息仓库最小函数通过当前入口验证；不声明主信息迁移完成、旧项目主信息类已移植或旧项目能力已迁移。 |

## 25. 本次追加实施：S4 后续专项登记

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户回复“全部确认”，计划索引 HR-001 下一步为进入 S4 后续专项登记。 |
| 核心处理 | 新增 S4 后续专项登记计划，把 MI-T2、MI-T3、MI-M1、MI-M2、MI-V2、MI-BI2、MI-D3 分流到任务系统、方法系统、动作入口、特征值索引、结构统计和需求结算非权威摘要等后续专项。 |
| 结构承载 | `计划/已完成计划/20260706_主信息迁移S4后续专项登记_v0.1.md`、`实施记录/20260706_主信息迁移S4后续专项登记_Codex断点清单.md`、`计划/20260705_主信息迁移实施映射计划_v0.1.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | 主信息迁移实施映射计划推进为 S4 已登记；计划索引下一步推进到 S5 主信息迁移收口验收；本切片未改代码、未新增主信息字段、未迁移旧函数体。 |
| 调用路径 | 计划索引 HR-001 -> 主信息迁移实施映射计划 S4 -> 后续专项登记计划 -> 项目记忆。 |
| 验证方式 | 文档一致性检索、`git diff --check`、提交和云端推送。 |
| 禁止扩大声明 | 本次只声明 S4 后续专项登记完成；不声明任务系统、方法系统、动作入口、缓存统计系统、主信息迁移或旧能力迁移完成。 |

## 26. 本次追加实施：S5 主信息迁移收口验收

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 计划索引 HR-001 下一步要求进入 S5 主信息迁移收口验收。 |
| 核心处理 | 汇总 S0 到 S4 证据，检查旧主信息类未回流，确认 S4 后续专项没有被误判为完成，并把下一步转为确认仓库逻辑空间与领域树结构规范草案。 |
| 结构承载 | `实施记录/20260706_主信息迁移S5收口验收_Codex断点清单.md`、`计划/20260705_主信息迁移实施映射计划_v0.1.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | 主信息迁移实施映射计划状态推进为 S5 收口验收已完成；当时 `规范/仓库逻辑空间与领域树结构规范.md` 仍处于未完成确认的草案状态，后续见第 27 节已确认。 |
| 调用路径 | 计划索引 HR-001 -> 主信息迁移实施映射计划 S5 -> 收口验收记录 -> 项目记忆。 |
| 验证方式 | `git diff --check` 通过；旧主信息类名在 `海中鱼巣` 代码目录未命中；S0-S4 状态一致性检索通过。本切片未改代码，未重新运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明主信息迁移第一轮门禁和证据收口完成；不声明主信息迁移完成、旧主信息类移植完成、旧项目能力迁移完成或 S4 后续专项完成。 |

## 27. 本次追加实施：仓库逻辑空间与领域树图结构规范确认

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户附加审查意见要求对 `规范/仓库逻辑空间与领域树结构规范.md` 小修后确认。 |
| 核心处理 | 修正规范标题和状态；补逻辑空间定位方式、跨空间关系准入表、索引命名空间、当前状态列和风险项；修正重复关系拒绝口径以兼容语素多对应 / 多路追溯。 |
| 结构承载 | `规范/仓库逻辑空间与领域树结构规范.md`、`实施记录/20260706_仓库逻辑空间与领域树图结构规范确认_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | 仓库逻辑空间与领域树图结构规范从待确认推进为已确认项目规范；下一步转为建立任务系统规范和详细设计。 |
| 调用路径 | 用户审查意见 -> 仓库逻辑空间规范修订 -> 计划索引 -> 项目记忆。 |
| 验证方式 | 文档一致性检索和 `git diff --check`；本切片未改代码，未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明规范已确认；不声明逻辑树图代码完整实现、任务系统完成、方法系统完成、主信息迁移完成或旧项目能力迁移完成。 |

## 28. 本次追加实施：计划执行确认门禁修正

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求修改计划执行逻辑：类似“建立任务系统规范和详细设计”的动作本身不需要用户确认，需要确认的是设计方案完成后进入执行前。 |
| 核心处理 | 将确认门禁拆为文档治理动作和落地执行动作；文档治理默认直接执行，代码实施、旧能力迁移、字段落地、函数迁移或改变机器结构前才等待用户确认。 |
| 结构承载 | `AGENTS.md`、`计划/计划索引.md`、`项目记忆/决策记录.md`、`项目记忆/当前状态.md`、`项目记忆/Codex任务队列.md`。 |
| 输出变化 | 后续看到“建立规范 / 详细设计 / 计划 / 验收项 / 只读扫描 / 断点清单 / 项目记忆同步”时不再停等用户确认；任务系统规范和详细设计可按计划索引直接建立。 |
| 调用路径 | 用户口径 -> AGENTS 执行规则 -> 计划索引确认门禁 -> 项目记忆队列。 |
| 验证方式 | 文档一致性检索和 `git diff --check`；本切片不改代码，不运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明执行规则已修正；不声明任务系统规范已建立、任务系统代码已实现或旧任务字段已迁移。 |

## 29. 本次追加实施：任务系统规范和详细设计建立

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 计划索引 HR-001 下一步要求建立任务系统规范和详细设计，覆盖 MI-T2、MI-T3、MI-T4；按执行确认门禁，建立文档本身不需要额外确认。 |
| 核心处理 | 参照旧项目任务根规范、任务根据需求初始化、需求任务单目标和任务筹办执行边界，改写为新项目任务系统规范和详细设计；明确旧任务主信息字段不迁移。 |
| 结构承载 | `规范/任务系统规范.md`、`规范/详细设计/任务系统详细设计.md`、`规范/仓库逻辑空间与领域树结构规范.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | 任务系统规范和详细设计在建立切片结束时待用户确认；后续已按审查意见小修后确认，当前下一步为任务系统第一轮代码实施。 |
| 调用路径 | 计划索引 HR-001 -> S4 后续专项登记 MI-T2 / MI-T3 / MI-T4 -> 任务系统规范 -> 任务系统详细设计 -> 项目记忆。 |
| 验证方式 | 文档一致性检索和 `git diff --check`；本切片未改代码，未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明任务系统规范和详细设计已建立；不声明已确认、代码已实现、任务状态机已完成、任务筹办 / 执行完成或旧任务字段已迁移。 |

## 30. 本次追加实施：等待任务确认期间详细设计补齐

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求在确认任务系统期间生成其它可以生成但还未生成的详细设计。 |
| 核心处理 | 根据 S4 后续专项登记，补齐任务以外四组规范和详细设计；建立当时均保持待确认状态，不进入代码。 |
| 结构承载 | `规范/方法系统规范.md`、`规范/详细设计/方法系统详细设计.md`、`规范/动作入口规范.md`、`规范/详细设计/动作入口详细设计.md`、`规范/非权威缓存与统计规范.md`、`规范/详细设计/非权威缓存与统计详细设计.md`、`规范/需求结算记录规范.md`、`规范/详细设计/需求结算记录详细设计.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | S4 中除任务系统外的可生成专项文档已建立；任务系统后续已小修确认，方法系统已在后续切片小修确认，动作入口也已在后续切片小修确认，非权威缓存与统计和需求结算记录也已在后续切片小修确认。 |
| 调用路径 | 用户请求 -> 计划索引执行确认门禁 -> S4 后续专项登记 -> 旧项目方法 / 动作 / 特征值 / 需求结算规范参照 -> 新项目规范和详细设计 -> 项目记忆。 |
| 验证方式 | 文档一致性检索和 `git diff --check`；本切片未改代码，未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明文档已生成；不声明相关规范已确认、代码已实现、方法系统完成、动作入口完成、缓存统计完成、需求结算完成或旧字段已迁移。 |

## 31. 本次追加实施：任务系统详细设计小修确认

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认任务系统详细设计方向可作为第一轮方案基础，但要求补齐 `按需求创建任务` 的任务目标状态关系、入口拒绝和逻辑错误追根因规则后再确认。 |
| 核心处理 | 修订任务系统规范和详细设计：补任务目标状态关系写入，补需求无目标状态 / 多目标 / 类型错误 / 关系写入失败拒绝规则，补任务服务专用读取入口，补第一轮无半结构策略；将入口通过后的内部不及预期统一为逻辑错误追根因规则。 |
| 结构承载 | `规范/任务系统规范.md`、`规范/详细设计/任务系统详细设计.md`、`AGENTS.md`、`规范/000_项目规则总纲.md`、`规范/001_规则迁移清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | 任务系统规范和详细设计从待确认推进为已确认 / 未改代码；下一步转为任务系统第一轮代码实施。 |
| 调用路径 | 用户审查意见 -> 任务系统规范小修 -> 任务系统详细设计小修 -> 项目通用逻辑错误规则 -> 计划索引和项目记忆同步。 |
| 验证方式 | 文档一致性检索和 `git diff --check`；本切片未改代码，未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明任务系统规范和详细设计已确认；不声明任务系统代码已实现、任务状态机完成、任务筹办 / 执行完成或旧任务字段已迁移。 |

## 32. 本次追加实施：方法系统详细设计小修确认

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认方法系统详细设计方向通过，但要求补齐方法角色状态绑定、条件 / 结果节点绑定口径、MI-M3 / MI-M4 函数占位和动作入口边界后再正式确认。 |
| 核心处理 | 修订方法系统规范和详细设计：方法角色状态第一轮采用抽象状态节点并绑定到方法节点；方法条件 / 结果节点创建时自动绑定方法首；MI-M3 来源任务、父方法、前置 / 后续方法和 MI-M4 条件判定索引、结果初始状态补为后续函数占位；补有效方法首 / 条件 / 结果判定。 |
| 结构承载 | `规范/方法系统规范.md`、`规范/详细设计/方法系统详细设计.md`、`规范/仓库逻辑空间与领域树结构规范.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | 该专项从待确认推进为已确认 / 未改代码；动作入口、非权威缓存与统计和需求结算记录后续已小修确认；当前下一步仍为任务系统第一轮代码实施。 |
| 调用路径 | 用户审查意见 -> 方法系统规范小修 -> 方法系统详细设计小修 -> 计划索引和项目记忆同步。 |
| 验证方式 | 文档一致性检索和 `git diff --check`；本切片未改代码，未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明方法系统规范和详细设计已确认；不声明方法系统代码已实现、动作入口已确认、方法查找完成、方法执行系统完成或旧方法字段已迁移。 |

## 33. 本次追加实施：动作入口详细设计小修确认

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认动作入口详细设计方向通过，但要求补齐 `来源动作` 指向、动作入口角色身份、稳定动作键承载和动作入口状态承载后再正式确认。 |
| 核心处理 | 修订动作入口规范和详细设计：第一轮 `来源动作` 指向动作入口角色节点；动作入口角色节点使用 `节点类型::方法` 并带动作入口角色抽象状态；方法节点不得自引用冒充动作入口；稳定动作键使用 `uint64` 动作入口索引键；动作入口状态使用抽象状态；补被动入口候选排除和动作动态 / 轻量因果边界。 |
| 结构承载 | `规范/动作入口规范.md`、`规范/详细设计/动作入口详细设计.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | 动作入口规范和详细设计由条件确认推进为已确认 / 未改代码；非权威缓存与统计和需求结算记录后续已小修确认；当前下一步仍为任务系统第一轮代码实施。 |
| 调用路径 | 用户审查意见 -> 动作入口规范小修 -> 动作入口详细设计小修 -> 计划索引和项目记忆同步。 |
| 验证方式 | 文档一致性检索和 `git diff --check`；本切片未改代码，未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明动作入口规范和详细设计已确认；不声明动作入口代码已实现、动作动态系统完成、方法动作迁移完成或旧动作句柄字段已迁移。 |

## 34. 本次追加实施：非权威缓存与统计详细设计小修确认

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认非权威缓存与统计详细设计方向通过，但要求补齐缓存承载位置、缓存命名空间、第一轮可实施范围和需求结算摘要依赖边界后再正式确认。 |
| 核心处理 | 修订非权威缓存与统计规范和详细设计：明确缓存不进入主信息权威字段；补索引仓库缓存项、统计服务运行期快照、需求结算摘要缓存和主信息非权威缓存区的承载边界；补缓存命名空间字段；将第一轮范围收窄到缓存命名空间、缓存记录结构、结构统计运行期快照和缓存清空 / 重建接口壳；把特征值哈希、命中缓存和需求结算摘要标为后续占位。 |
| 结构承载 | `规范/非权威缓存与统计规范.md`、`规范/详细设计/非权威缓存与统计详细设计.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | 非权威缓存与统计规范和详细设计由条件确认推进为已确认 / 未改代码；需求结算记录后续已小修确认；当前下一步仍为任务系统第一轮代码实施。 |
| 调用路径 | 用户审查意见 -> 非权威缓存与统计规范小修 -> 非权威缓存与统计详细设计小修 -> 计划索引和项目记忆同步。 |
| 验证方式 | 文档一致性检索和 `git diff --check`；本切片未改代码，未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明非权威缓存与统计规范和详细设计已确认；不声明缓存统计代码已实现、特征值哈希已实现、需求结算摘要已实现、缓存系统完成或旧缓存字段已迁移。 |

## 35. 本次追加实施：需求结算记录详细设计小修确认

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认需求结算记录详细设计方向通过，但要求补齐需求到结算记录关系、结算目标状态追溯、实际状态来源、最近结算规则和第一轮比较规则后再正式确认。 |
| 核心处理 | 修订需求结算记录规范和详细设计：第一轮以需求结算实例状态作为权威结算记录锚点；补需求结算记录关系、结算来源任务关系、结算目标状态关系、结算实际状态关系、动态证据和可选因果引用；拆分需求承接任务和结算来源任务；明确实际状态由调用方传入已有状态节点；定义最近结算记录读取规则和第一轮 I64 比较规则。 |
| 结构承载 | `规范/需求结算记录规范.md`、`规范/详细设计/需求结算记录详细设计.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | 需求结算记录规范和详细设计由条件确认推进为已确认 / 未改代码；当前下一步仍为任务系统第一轮代码实施。 |
| 调用路径 | 用户审查意见 -> 需求结算记录规范小修 -> 需求结算记录详细设计小修 -> 计划索引和项目记忆同步。 |
| 验证方式 | 文档一致性检索和 `git diff --check`；本切片未改代码，未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明需求结算记录规范和详细设计已确认；不声明需求结算记录代码已实现、需求结算系统完成、非权威摘要已实现或旧需求结算字段已迁移。 |

## 36. 本次追加实施：最小闭环验收用例小修确认

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认 `规范/验收/最小闭环验收用例.md` 方向基本通过，但要求先处理旧版同名文件冲突，并修正 MC-02、MC-08 和最小闭环设计中的旧目标值 / 比较方式口径。 |
| 核心处理 | 将旧版 `规范/最小闭环验收用例.md` 改为导航文件；把权威验收文件状态改为已确认；修正 MC-02 为目标状态需求和固定 DV-1 满足规则；补 MC-03 任务状态发生时间戳；修正 MC-05 方法执行拒绝项；修正 MC-08 为结算状态节点、发生时间戳、目标状态和当前特征状态材料追溯；同步修订 `规范/最小闭环设计.md` 和写入事务最小闭环事务表。 |
| 结构承载 | `规范/验收/最小闭环验收用例.md`、`规范/最小闭环验收用例.md`、`规范/最小闭环设计.md`、`规范/详细设计/写入事务详细设计.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | 最小闭环验收用例从已建立推进为已确认；当前权威验收入口唯一指向 `规范/验收/最小闭环验收用例.md`；旧版目标值和动态回查来源方法等旧口径不再作为验收依据。 |
| 调用路径 | 用户审查意见 -> 最小闭环验收文件小修 -> 最小闭环设计口径同步 -> 计划索引和项目记忆同步。 |
| 验证方式 | 文档一致性检索和 `git diff --check`；本切片未改代码，未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明验收用例文档已确认；不声明代码已实现、完整验收体系完成、最小闭环重新验证通过、旧项目能力迁移或自我循环完成。 |

## 37. 本次追加实施：节点类型与关系类型枚举规范小修确认

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认 `规范/节点类型与关系类型枚举规范.md` 方向通过，但要求补齐枚举数值锁定、抽象 / 实例状态动态口径、当前关系类型多目标 / 唯一性表和新增节点类型流程后再确认。 |
| 核心处理 | 按 `海中鱼巣/核心/句柄.h` 当前代码事实补节点类型和关系类型数值锁定表；明确枚举值不得重排、复用或改名复义；补基础信息类长期节点、抽象状态 / 抽象动态与实例状态 / 实例动态边界；补当前关系类型多目标 / 唯一性口径；补新增节点类型和关系类型准入流程；补枚举不裁决业务事实反例。 |
| 结构承载 | `规范/节点类型与关系类型枚举规范.md`、`计划/20260705_海中鱼巣全量重构总计划_v0.1.md`、`计划/计划索引.md`、`项目记忆/*`、`实施记录/20260706_节点类型与关系类型枚举规范确认_Codex断点清单.md`。 |
| 输出变化 | 节点类型与关系类型枚举规范从待确认推进为已确认 / 未改代码；后续新增节点类型、关系类型和代码枚举变更有明确门禁。 |
| 调用路径 | 用户审查意见 -> 枚举规范小修 -> 计划索引确认清单 -> 项目记忆同步。 |
| 验证方式 | 文档一致性检索和 `git diff --check`；本切片未改代码，未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明枚举规范已确认；不声明代码已修改、枚举持久化兼容已验证、关系系统完整完成、任务 / 方法 / 需求系统完成或旧项目能力迁移。 |

## 38. 本次追加实施：错误结果与入口拒绝规范小修确认

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认 `规范/错误结果与入口拒绝规范.md` 方向正确，但要求补齐错误分层、统一结果结构、领域扩展原因、多步写入失败边界和验收矩阵后再确认。 |
| 核心处理 | 将规范状态改为已确认；补入口拒绝、业务失败和逻辑错误三层；保留当前无效句柄、`std::nullopt`、局部 `bool` 和领域枚举轻量返回模型；补结构化错误结果设计口径；补当前领域服务扩展拒绝原因；补多步写入失败边界、拒绝后结构不变化检查范围、容错诊断边界和验收矩阵。 |
| 结构承载 | `规范/错误结果与入口拒绝规范.md`、`计划/20260705_海中鱼巣全量重构总计划_v0.1.md`、`计划/计划索引.md`、`项目记忆/*`、`实施记录/20260706_错误结果与入口拒绝规范确认_Codex断点清单.md`。 |
| 输出变化 | 错误结果与入口拒绝规范从待确认推进为已确认 / 未改代码；后续仓库、领域服务、多步写入和旧能力迁移有统一错误结果口径。 |
| 调用路径 | 用户审查意见 -> 错误结果与入口拒绝规范小修 -> 总计划和计划索引确认清单 -> 项目记忆同步。 |
| 验证方式 | 文档一致性检索和 `git diff --check`；本切片未改代码，未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明错误结果与入口拒绝规范已确认；不声明代码已改造为统一错误对象、所有入口拒绝测试已覆盖、所有多步写入已有完整回滚或旧项目能力已迁移。 |

## 39. 本次追加实施：已确认代码切片状态一致性收口

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求当前执行窗口继续执行已经确认的步骤；当前计划和项目记忆中仍残留“进入任务系统第一轮代码实施”的旧队尾。 |
| 核心处理 | 将任务系统、方法系统、动作入口、非权威缓存统计和需求结算记录第一轮代码实施状态同步为已完成 / 已验证；把当前下一步收口为等待设计窗口确认下一项代码实施。 |
| 结构承载 | `计划/计划索引.md`、`计划/20260705_海中鱼巣全量重构总计划_v0.1.md`、`计划/20260705_主信息迁移实施映射计划_v0.1.md`、`项目记忆/*`、`实施记录/20260706_已确认代码切片状态一致性收口_Codex断点清单.md`。 |
| 输出变化 | 后续继续执行时不再从已完成的任务系统第一轮代码实施重复起步；当前窗口只继续执行已确认的文档治理、只读扫描、断点清单或项目记忆同步。 |
| 调用路径 | 用户继续 -> 计划索引和项目记忆回读 -> 已提交代码切片验证记录核对 -> 状态一致性收口 -> 文档验证和同步。 |
| 验证方式 | `rg` 检查旧队尾残留；`git diff --check`；本切片未改代码，未重新运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明文档状态已与已验证代码切片对齐；不声明完整任务系统、方法系统、动作系统、缓存系统、需求结算系统或旧项目能力迁移完成。 |

## 40. 本次追加实施：领域服务授权写入规范小修确认

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认 `规范/领域服务授权写入规范.md` 方向正确，但要求补齐仓库入口 / 领域入口边界、跨服务依赖白名单、服务写入结构清单、越权判定方式和验收矩阵后再确认。 |
| 核心处理 | 将规范状态改为已确认；补仓库正式入口只负责最小结构、领域服务负责业务写入编排和准入；补高级服务直接依赖白名单，明确高级服务不得直接访问特征值服务；补语素、特征值、特征、存在、场景、状态、动态、二次特征、因果、世界、需求、任务、方法和非权威缓存 / 统计入口的写入矩阵；补越权写入判定方式、新增服务注册规则和 AW-01 到 AW-10 验收矩阵。 |
| 结构承载 | `规范/领域服务授权写入规范.md`、`计划/20260705_海中鱼巣全量重构总计划_v0.1.md`、`计划/计划索引.md`、`项目记忆/*`、`实施记录/20260706_领域服务授权写入规范确认_Codex断点清单.md`。 |
| 输出变化 | 领域服务授权写入规范从待确认推进为已确认 / 未改代码；后续任务系统、方法系统、动作入口、需求结算、缓存统计和旧能力迁移有统一授权门禁。 |
| 调用路径 | 用户审查意见 -> 领域服务授权写入规范小修 -> 总计划和计划索引确认清单 -> 项目记忆同步。 |
| 验证方式 | 文档一致性检索和 `git diff --check`；本切片未改代码，未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明领域服务授权写入规范已确认；不声明完整运行时权限系统已实现、所有越权路径已测试、所有领域服务依赖已代码化治理或旧项目能力已迁移。 |

## 41. 本次追加实施：计划索引双路执行路由修订

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求修订计划索引为两路执行，并明确确认过的方案和计划应提交给执行模块。 |
| 核心处理 | 将计划索引拆为落实执行路线和设计确认路线；落实执行路线承接确认过的方案和计划并持续执行到清空；设计确认路线继续分部设计和确认，确认后必须提交给落实执行路线。 |
| 结构承载 | `计划/计划索引.md`、`项目记忆/当前状态.md`、`项目记忆/Codex任务队列.md`、`项目记忆/决策记录.md`、`项目记忆/待确认问题.md`、`实施记录/20260706_计划索引双路执行路由修订_Codex断点清单.md`。 |
| 输出变化 | 后续“继续”时优先清空落实执行路线；设计确认结果不再停留在等待态，确认后必须登记给执行模块。 |
| 调用路径 | 用户路由规则 -> 计划索引双路执行路由 -> 项目记忆队列和决策同步 -> 文档验证和同步。 |
| 验证方式 | `rg` 检查双路路由、执行模块提交规则和当前下一步；`git diff --check`；本切片未改代码，未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明执行路由规则已修订；不声明新的代码实施项已存在，不声明旧能力迁移、字段落地或函数迁移已完成。 |

## 42. 本次追加实施：UTF-8 与中文标识符工程规范确认

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认 `规范/UTF8与中文标识符工程规范.md` 可以确认，并建议补充类型 / 命名空间 / 文件名优先中文、不得依赖系统代码页、控制台乱码边界和 BOM 口径。 |
| 核心处理 | 将规范状态改为已确认；补类名、结构名、枚举名、命名空间名和文件名优先中文；补不得依赖 Windows 本地代码页；补 Markdown / C++ UTF-8 BOM 口径和工程 XML `encoding="utf-8"`；补控制台乱码只作为显示层问题；记录当前 `海中鱼巣.vcxproj` 四个构建配置均含 `/utf-8`。 |
| 结构承载 | `规范/UTF8与中文标识符工程规范.md`、`计划/20260705_海中鱼巣全量重构总计划_v0.1.md`、`计划/计划索引.md`、`项目记忆/*`、`实施记录/20260706_UTF8与中文标识符工程规范确认_Codex断点清单.md`。 |
| 输出变化 | UTF-8 与中文标识符工程规范从待确认推进为已确认 / 未改代码；后续新增 C++ 文件、Markdown 文件和工程配置变更有统一工程边界。 |
| 调用路径 | 用户审查意见 -> UTF-8 与中文标识符工程规范确认 -> 工程 `/utf-8` 配置检索 -> 计划索引和项目记忆同步。 |
| 验证方式 | `rg -n "/utf-8|AdditionalOptions|CharacterSet" 海中鱼巣.vcxproj`、文档一致性检索和 `git diff --check`；本切片未改代码，未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明 UTF-8 与中文标识符工程规范已确认；不声明所有历史文件已逐个编码审计、所有终端环境中文显示都已验证、CI 已验证 UTF-8 或所有后续代码命名已自动符合规范。 |

## 43. 本次追加实施：函数迁移前置专项生成

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求生成“函数迁移前置专项”，并指定必须包含任务目标、依据文件、只读扫描范围、允许修改文件、禁止修改文件、旧函数功能说明模板、保留 / 改名 / 拆分 / 合并 / 删除 / 改写为领域服务入口的判定表、验收标准和禁止宣称。 |
| 核心处理 | 新建函数迁移前置专项，明确它只是旧函数进入迁移前的逐函数说明和判定确认门禁；同步计划索引、项目记忆和断点清单。 |
| 结构承载 | `计划/20260706_函数迁移前置专项_v0.1.md`、`实施记录/20260706_函数迁移前置专项生成_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | 函数迁移前置专项已建立，状态为待确认 / 未改代码 / 未迁移旧函数；确认前不得迁移旧函数体、不得新增旧主信息字段、不得声明旧项目能力迁移完成、不得接控制面板 / SQL / D455 / 体素。 |
| 调用路径 | 用户请求 -> 项目规则和计划索引回读 -> 函数功能说明基线回读 -> 函数迁移前置专项 -> 项目记忆同步 -> 文档验证和同步。 |
| 验证方式 | 文档一致性检索和 `git diff --check`；本切片未改代码，未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明专项文档已生成；不声明旧函数已迁移、旧项目能力已迁移、代码已编译、入口前置拒绝已重新验证或外设 / 体素 / 控制面板已接入。 |

## 44. 本次追加实施：函数迁移前置专项确认

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认 `计划/20260706_函数迁移前置专项_v0.1.md`。 |
| 核心处理 | 将函数迁移前置专项从待确认推进为已确认；同步计划索引和项目记忆；登记下一步只允许逐函数只读扫描和旧函数说明登记。 |
| 结构承载 | `计划/20260706_函数迁移前置专项_v0.1.md`、`计划/计划索引.md`、`项目记忆/*`、`实施记录/20260706_函数迁移前置专项确认_Codex断点清单.md`。 |
| 输出变化 | 函数迁移前置专项已确认 / 未改代码 / 未迁移旧函数；后续候选旧函数必须先按模板登记并再次确认迁移判定。 |
| 调用路径 | 用户确认 -> 函数迁移前置专项状态更新 -> 计划索引落实路线登记 -> 项目记忆同步 -> 文档验证和同步。 |
| 验证方式 | 文档一致性检索和 `git diff --check`；本切片未改代码，未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明专项确认完成；不声明旧函数已扫描、旧函数已迁移、代码已编译、入口前置拒绝已重新验证、旧项目能力迁移完成或控制面板 / SQL / D455 / 体素已接入。 |

## 45. 本次追加实施：逐函数只读扫描清单生成

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求“确认,生成 逐函数只读扫描清单”。 |
| 核心处理 | 基于已确认函数迁移前置专项，生成逐函数只读扫描清单；拆分 FS-00 到 FS-10 和 FS-X 排除项；建立逐函数登记模板和空槽位；同步计划索引和项目记忆。 |
| 结构承载 | `实施记录/20260706_逐函数只读扫描清单.md`、`实施记录/20260706_逐函数只读扫描清单生成_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | 后续旧函数扫描有统一批次和登记模板；当前仍未执行旧项目函数扫描、未形成逐函数迁移判定、未改代码。 |
| 调用路径 | 用户确认并要求生成清单 -> 函数迁移前置专项 -> 逐函数只读扫描清单 -> 计划索引和项目记忆同步 -> 文档验证和同步。 |
| 验证方式 | 文档一致性检索和 `git diff --check`；本切片未改代码，未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明逐函数只读扫描清单已生成；不声明旧函数已扫描、旧函数已迁移、代码已编译、入口前置拒绝已重新验证、旧项目能力迁移完成或控制面板 / SQL / D455 / 体素已接入。 |

## 46. 本次追加实施：FS-00 / FS-01 第一批逐函数只读扫描

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求继续执行已生成的逐函数只读扫描清单。 |
| 核心处理 | 复核当前项目和旧项目路径、当前项目 git 状态和旧项目只读边界；只读扫描 `D:\鱼巢` 中旧双向链表模板、基础数据类型、基础信息类和基础信息主信息类；登记 OF-001 到 OF-018 第一批旧核心结构候选函数。 |
| 结构承载 | `实施记录/20260706_FS00_FS01_逐函数只读扫描记录.md`、`实施记录/20260706_逐函数只读扫描清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-00 已复核；FS-01 已登记旧节点创建、父子挂载、主键查找、主信息替换、删除、重挂、校验、深拷贝、序列化和基础信息树入口第一批候选；FS-01 尚未完整扫描。 |
| 调用路径 | 用户继续 -> 逐函数只读扫描清单 -> 旧项目只读 `rg` / `Get-Content` -> FS-00 / FS-01 扫描记录 -> 计划索引和项目记忆同步。 |
| 验证方式 | 当前项目 `git status`、旧项目 `git status`、旧项目只读 `rg` 和文件读取、文档一致性检索、`git diff --check`；本切片未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明 FS-00 已复核、FS-01 第一批旧核心结构函数已登记；不声明 FS-01 已完整扫描、旧函数已迁移、旧函数体可以复制、旧主信息字段已迁移、旧项目能力迁移完成或控制面板 / SQL / D455 / 体素已接入。 |

## 47. 本次追加实施：FS-01 第二批旧世界树核心边界只读扫描

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求继续执行无需确认的步骤；逐函数只读扫描清单允许继续 FS-01 旧核心结构只读扫描。 |
| 核心处理 | 只读扫描 `D:\鱼巢\世界树类.h` 和 `D:\鱼巢\世界树类.cpp` 中旧世界树聚合入口、服务访问、主键解析、SQL / 控制面板投影边界、特征写入桥和默认世界 / 自我初始化入口；登记 OF-019 到 OF-031。 |
| 结构承载 | `实施记录/20260706_FS01_旧世界树核心边界只读扫描记录.md`、`实施记录/20260706_逐函数只读扫描清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-01 第二批已登记；旧 `世界树类` 被记录为聚合门面和服务定位器，不适合整体迁移；SQL / 控制面板、体素和外设相关内容只登记为 FS-X 或后续专项排除边界。 |
| 调用路径 | 用户继续 -> 逐函数只读扫描清单 -> 旧项目只读 `rg` / 文件读取 -> FS-01 第二批扫描记录 -> 计划索引和项目记忆同步。 |
| 验证方式 | 当前项目 `git status`、旧项目只读文件读取、文档一致性检索、`git diff --check`；本切片未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明 FS-01 第二批旧世界树核心边界函数已登记；不声明 FS-01 已完整扫描、旧函数已迁移、旧世界树已迁移、旧主信息字段已迁移、旧项目能力迁移完成或控制面板 / SQL / D455 / 体素已接入。 |

## 48. 本次追加实施：FS-01 第三批基础数据类型与主信息辅助边界只读扫描

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求无需确认的步骤持续执行；计划索引 HR-001-FM-SCAN 要求补旧核心结构中的主信息字段访问辅助函数和基础数据类型中句柄 / 值容器边界。 |
| 核心处理 | 只读扫描 `D:\鱼巢\基础数据类型.h`、`D:\鱼巢\基础信息主信息类.h` 和 `D:\鱼巢\基础信息主信息类.cpp`；登记 OF-032 到 OF-045，覆盖 I64 区间、比较量、可解析引用、统计缓存、Vec / 指针句柄、ZigZag 编码、主信息比较协议、特征值域、特征当前值、存在旧高级树根、状态 / 动态字段、二次特征模板和因果计数边界。 |
| 结构承载 | `实施记录/20260706_FS01_基础数据类型与主信息辅助边界只读扫描记录.md`、`实施记录/20260706_逐函数只读扫描清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-01 第三批已登记；当前第一轮核心边界已覆盖到可转 FS-02；下一旧函数编号从 OF-046 开始。 |
| 调用路径 | 用户继续 -> 逐函数只读扫描清单 -> 旧项目只读 `rg` / 文件读取 -> FS-01 第三批扫描记录 -> 计划索引和项目记忆同步。 |
| 验证方式 | 当前项目 `git status`、旧项目只读文件读取、文档一致性检索、`git diff --check`；本切片未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明 FS-01 第三批基础数据类型与主信息辅助边界函数已登记；不声明旧函数已扫描完成、旧函数已迁移、旧主信息字段可迁移、特征 / 二次特征 / 因果系统完成或控制面板 / SQL / D455 / 体素已接入。 |

## 49. 本次追加实施：FS-02 第一批语素入口与基础信息桥接只读扫描

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求不需要确定的步骤持续执行；计划索引 HR-001-FM-SCAN 要求进入 FS-02 旧项目语素 / 基础信息函数只读扫描。 |
| 核心处理 | 只读扫描 `D:\鱼巢\语素类.h`、`D:\鱼巢\语素类.cpp`、`D:\鱼巢\语素主信息类.h`、`D:\鱼巢\语素节点类型.h`、`D:\鱼巢\语素信息类型.h`、`D:\鱼巢\语言类_基础信息命名.cpp` 和 `D:\鱼巢\语言类.h`；登记 OF-046 到 OF-059，覆盖旧语素入口初始化、SQL 投影排除、词面 / 词性入口、已理解入口绑定、概念 / 实例入口、别名、候选查询、显示标题、同词信息入口、场景桥接、二次特征词、对应基础信息绑定和词性映射边界。 |
| 结构承载 | `实施记录/20260706_FS02_语素入口与基础信息桥接只读扫描记录.md`、`实施记录/20260706_FS02_语素入口与基础信息桥接只读扫描_Codex断点清单.md`、`实施记录/20260706_逐函数只读扫描清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-02 第一批已登记；下一旧函数编号从 OF-060 开始；下一步继续 FS-02 旧项目基础信息类、存在 / 场景 / 状态 / 动态 / 二次特征 / 因果相关函数只读扫描。 |
| 调用路径 | 用户继续 -> 逐函数只读扫描清单 -> 旧项目只读 `rg` / 文件读取 -> FS-02 第一批扫描记录 -> 计划索引和项目记忆同步。 |
| 验证方式 | 当前项目 `git status`、旧项目只读文件读取、文档一致性检索、`git diff --check`；本切片未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明 FS-02 第一批语素入口与基础信息桥接函数已登记；不声明旧语素类已迁移、旧语素树或旧语素主信息字段可迁移、候选查询 / 自然语言理解 / 显示层只读视图完成或控制面板 / SQL / D455 / 体素已接入。 |

## 50. 本次追加实施：FS-02 第二批基础信息模板入口只读扫描

| 项目 | 内容 |
| --- | --- |
| 输入入口 | FS-02 第一批已提交并推送；计划索引 HR-001-FM-SCAN 要求继续旧项目基础信息类、存在 / 场景 / 状态 / 动态 / 二次特征 / 因果相关函数只读扫描。 |
| 核心处理 | 只读扫描 `D:\鱼巢\基础信息类.h` 和 `D:\鱼巢\基础信息类.cpp`；登记 OF-060 到 OF-066，覆盖旧基础信息模板入口、默认根选择、入口引用检索、模板主信息创建、存在概念根兼容、显示名称和清理边界。 |
| 结构承载 | `实施记录/20260706_FS02_基础信息模板入口只读扫描记录.md`、`实施记录/20260706_FS02_基础信息模板入口只读扫描_Codex断点清单.md`、`实施记录/20260706_逐函数只读扫描清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-02 第二批已登记；下一旧函数编号从 OF-067 开始；下一步继续 FS-02 旧项目存在类、场景类、状态类、动态类、二次特征类和因果类第一批函数只读扫描。 |
| 调用路径 | 用户继续 -> 逐函数只读扫描清单 -> 旧项目只读 `rg` / 文件读取 -> FS-02 第二批扫描记录 -> 计划索引和项目记忆同步。 |
| 验证方式 | 当前项目 `git status`、旧项目只读文件读取、文档一致性检索、`git diff --check`；本切片未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明 FS-02 第二批基础信息模板入口函数已登记；不声明旧基础信息类已迁移、旧基础信息树或旧主信息字段可迁移、基础信息服务分层 / 世界聚合服务 / 存在根概念系统完整完成或控制面板 / SQL / D455 / 体素已接入。 |

## 51. 本次追加实施：FS-02 第三批存在与场景入口只读扫描

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求不需要确认的步骤持续执行；计划索引 HR-001-FM-SCAN 要求继续 FS-02 旧项目存在类、场景类、状态类、动态类、二次特征类和因果类第一批函数只读扫描。 |
| 核心处理 | 只读扫描 `D:\鱼巢\存在类.h`、`D:\鱼巢\存在类.cpp`、`D:\鱼巢\场景类.h` 和 `D:\鱼巢\场景类.cpp`；登记 OF-067 到 OF-079，覆盖旧存在身份、名称 / 类型、概念 / 实例、内部世界、观测坐标、场景身份、场景创建、场景子节点查询、体素候选排除边界、场景宿主和场景状态 / 动态 / 二次特征索引。 |
| 结构承载 | `实施记录/20260706_FS02_存在与场景入口只读扫描记录.md`、`实施记录/20260706_FS02_存在与场景入口只读扫描_Codex断点清单.md`、`实施记录/20260706_逐函数只读扫描清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-02 第三批已登记；下一旧函数编号从 OF-080 开始；下一步继续 FS-02 旧状态类、动态类、二次特征类和因果类第一批函数只读扫描。 |
| 调用路径 | 用户继续 -> 逐函数只读扫描清单 -> 旧项目只读 `rg` / 文件读取 -> FS-02 第三批扫描记录 -> 计划索引和项目记忆同步。 |
| 验证方式 | 当前项目 `git status`、旧项目只读文件读取、文档一致性检索、`git diff --check`；本切片未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明 FS-02 第三批存在与场景入口函数已登记；不声明旧存在类或旧场景类已迁移，不声明旧存在 / 场景主信息字段、旧观测缓存或旧事实索引可迁移，不接控制面板 / SQL / D455 / 体素。 |

## 52. 本次追加实施：FS-02 第四批状态与动态入口只读扫描

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求不需要确认的步骤持续执行；计划索引 HR-001-FM-SCAN 要求继续 FS-02 旧项目状态类、动态类、二次特征类和因果类第一批函数只读扫描。 |
| 核心处理 | 只读扫描 `D:\鱼巢\状态类.h`、`D:\鱼巢\状态类.cpp`、`D:\鱼巢\动态类.h` 和 `D:\鱼巢\动态类.cpp`；登记 OF-080 到 OF-093，覆盖旧状态身份、状态主体 / 特征 / 值材料、状态创建、状态索引清理、动态身份、动态因果输入、动作动态、方法动作动态、存在 / 场景动态、运动基元候选、SQL 显示排除和动态来源绑定。 |
| 结构承载 | `实施记录/20260706_FS02_状态与动态入口只读扫描记录.md`、`实施记录/20260706_FS02_状态与动态入口只读扫描_Codex断点清单.md`、`实施记录/20260706_逐函数只读扫描清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-02 第四批已登记；下一旧函数编号从 OF-094 开始；下一步继续 FS-02 旧二次特征类和因果类第一批函数只读扫描。 |
| 调用路径 | 用户继续 -> 逐函数只读扫描清单 -> 旧项目只读 `rg` / 文件读取 -> FS-02 第四批扫描记录 -> 计划索引和项目记忆同步。 |
| 验证方式 | 当前项目 `git status`、旧项目只读文件读取、文档一致性检索、`git diff --check`；本切片未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明 FS-02 第四批状态与动态入口函数已登记；不声明旧状态类或旧动态类已迁移，不声明旧状态 / 动态主信息字段、场景状态 / 动态索引、显示缓存或 SQL 控制面板镜像可迁移，不接控制面板 / SQL / D455 / 体素。 |

## 53. 本次追加实施：FS-02 第五批二次特征与因果入口只读扫描

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求不需要确认的步骤持续执行；计划索引 HR-001-FM-SCAN 要求继续 FS-02 旧项目二次特征类和因果类第一批函数只读扫描。 |
| 核心处理 | 只读扫描 `D:\鱼巢\二次特征类.h`、`D:\鱼巢\二次特征类.cpp`、`D:\鱼巢\因果类.h` 和 `D:\鱼巢\因果类.cpp`；登记 OF-094 到 OF-105，覆盖旧二次特征身份、状态迁移来源、方向签名、概念刻度链、生成语义、值材料、因果身份、因果证据、被动 / 主动因果治理、因果模板、证据动态样本、任务投影、模板匹配和自然语言显示边界。 |
| 结构承载 | `实施记录/20260706_FS02_二次特征与因果入口只读扫描记录.md`、`实施记录/20260706_FS02_二次特征与因果入口只读扫描_Codex断点清单.md`、`实施记录/20260706_逐函数只读扫描清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-02 第五批已登记；下一旧函数编号从 OF-106 开始；下一步继续 FS-03 旧特征 / 特征值函数第一批只读扫描。 |
| 调用路径 | 用户继续 -> 逐函数只读扫描清单 -> 旧项目只读 `rg` / 文件读取 -> FS-02 第五批扫描记录 -> 计划索引和项目记忆同步。 |
| 验证方式 | 当前项目 `git status`、旧项目只读文件读取、文档一致性检索、`git diff --check`；本切片未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明 FS-02 第五批二次特征与因果入口函数已登记；不声明旧二次特征类或旧因果类已迁移，不声明旧二次特征主信息、旧因果主信息或旧因果模板主信息字段可迁移，不声明完整二次特征系统、因果系统、因果模板系统、任务投影系统或方法学习系统完成，不接控制面板 / SQL / D455 / 体素。 |

## 54. 本次追加实施：FS-03 第一批特征与特征值入口只读扫描

| 项目 | 内容 |
| --- | --- |
| 输入入口 | FS-02 第五批已提交并推送；计划索引 HR-001-FM-SCAN 要求继续 FS-03 旧项目特征 / 特征值函数第一批只读扫描。 |
| 核心处理 | 只读扫描 `D:\鱼巢\特征类.h`、`D:\鱼巢\特征类.cpp`、`D:\鱼巢\特征值类.h` 和 `D:\鱼巢\特征值主信息类.h`；登记 OF-106 到 OF-117，覆盖旧特征身份、抽象特征、实例特征、当前值写入、值域匹配、稳态缓存、VecU / VecI64 值容器、内容哈希、命中统计、序列化和三维体素排除边界。 |
| 结构承载 | `实施记录/20260706_FS03_特征与特征值入口只读扫描记录.md`、`实施记录/20260706_FS03_特征与特征值入口只读扫描_Codex断点清单.md`、`实施记录/20260706_逐函数只读扫描清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-03 第一批已登记；下一旧函数编号从 OF-118 开始；下一步继续 FS-04 旧需求树函数第一批只读扫描。 |
| 调用路径 | 用户继续 -> 逐函数只读扫描清单 -> 旧项目只读 `rg` / 文件读取 -> FS-03 第一批扫描记录 -> 计划索引和项目记忆同步。 |
| 验证方式 | 当前项目 `git status`、旧项目只读文件读取、文档一致性检索、`git diff --check`；本切片未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明 FS-03 第一批特征与特征值入口函数已登记；不声明旧特征类或旧特征值类已迁移，不声明旧特征主信息、旧抽象特征主信息或旧特征值主信息字段可迁移，不声明 Vec 原始值容器、稳态系统、抽象特征系统、体素系统或缓存统计系统完成，不接控制面板 / SQL / D455 / 体素。 |

## 55. 本次追加实施：FS-04 第一批需求树入口只读扫描

| 项目 | 内容 |
| --- | --- |
| 输入入口 | FS-03 第一批已提交并推送；计划索引 HR-001-FM-SCAN 要求继续 FS-04 旧项目需求树函数第一批只读扫描。 |
| 核心处理 | 只读扫描 `D:\鱼巢\需求类.h`、`D:\鱼巢\需求类.cpp`、`D:\鱼巢\需求主信息类.h`、`D:\鱼巢\需求节点类型.h` 和 `D:\鱼巢\需求信息类型.h`；登记 OF-118 到 OF-127，覆盖旧需求目标语义、目标状态读取、SQL 投影排除、父子需求角色、需求结算摘要、需求方向、需求树更新指令、枚举目标校验、按主键解析和需求任务绑定边界。 |
| 结构承载 | `实施记录/20260706_FS04_需求树入口只读扫描记录.md`、`实施记录/20260706_FS04_需求树入口只读扫描_Codex断点清单.md`、`实施记录/20260706_逐函数只读扫描清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-04 第一批已登记；下一旧函数编号从 OF-128 开始；下一步继续 FS-05 旧项目任务管理函数第一批只读扫描。 |
| 调用路径 | 用户继续 -> 逐函数只读扫描清单 -> 旧项目只读 `rg` / 文件读取 -> FS-04 第一批扫描记录 -> 计划索引和项目记忆同步。 |
| 验证方式 | 当前项目 `git status`、旧项目只读文件读取、文档一致性检索、`git diff --check`；本切片未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明 FS-04 第一批需求树入口函数已登记；不声明旧需求类或旧需求树已迁移，不声明旧需求主信息字段可迁移，不声明需求树、任务管理、方法候选、需求结算或 SQL 显示系统完成，不接控制面板 / SQL / D455 / 体素。 |

## 56. 本次追加实施：FS-05 第一批任务管理入口只读扫描

| 项目 | 内容 |
| --- | --- |
| 输入入口 | FS-04 第一批已提交并推送；计划索引 HR-001-FM-SCAN 要求继续 FS-05 旧项目任务管理函数第一批只读扫描。 |
| 核心处理 | 只读扫描 `D:\鱼巢\任务主信息类.h`、`D:\鱼巢\任务类.h`、`D:\鱼巢\任务类.cpp`、`D:\鱼巢\任务节点类型.h` 和 `D:\鱼巢\任务信息类型.h`；登记 OF-128 到 OF-139，覆盖旧任务主信息、任务承接、任务虚拟存在、任务目标 / 结果状态、生命周期状态、任务方法关系、阶段角色、权重统计、特征账本、创建任务头结点和 SQL 投影排除边界。 |
| 结构承载 | `实施记录/20260706_FS05_任务管理入口只读扫描记录.md`、`实施记录/20260706_FS05_任务管理入口只读扫描_Codex断点清单.md`、`实施记录/20260706_逐函数只读扫描清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-05 第一批已登记；下一旧函数编号从 OF-140 开始；下一步继续 FS-05 第二批任务模块筹办 / 执行 / 管理线程边界扫描。 |
| 调用路径 | 用户继续 -> 逐函数只读扫描清单 -> 旧项目只读 `rg` / 文件读取 -> FS-05 第一批扫描记录 -> 计划索引和项目记忆同步。 |
| 验证方式 | 当前项目 `git status`、旧项目只读文件读取、文档一致性检索、`git diff --check`；本切片未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明 FS-05 第一批任务管理入口函数已登记；不声明旧任务类、旧任务树或旧任务管理系统已迁移，不声明旧任务主信息字段可迁移，不声明完整任务状态机、任务筹办、任务执行、调度线程或控制面板任务页完成，不接控制面板 / SQL / D455 / 体素。 |

## 57. 本次追加实施：FS-05 第二批任务模块筹办执行管理边界只读扫描

| 项目 | 内容 |
| --- | --- |
| 输入入口 | FS-05 第一批已提交并推送；计划索引 HR-001-FM-SCAN 要求继续 FS-05 第二批，扫描旧项目任务模块筹办 / 执行 / 管理线程边界。 |
| 核心处理 | 只读扫描 `D:\鱼巢\任务模块.筹办.ixx`、`D:\鱼巢\任务模块.执行.ixx`、`D:\鱼巢\任务模块.管理工作线程.ixx`、`D:\鱼巢\任务模块.运行包.ixx` 和 `D:\鱼巢\任务模块.治理协议.ixx`；登记 OF-140 到 OF-150，覆盖旧任务筹办回执、筹办主入口、因果缺口派生、安全风险树跳过边界、任务执行桥、方法调用动作动态、任务管理工作线程、线程快照、外设消息承接排除边界、运行包和治理协议函数族。 |
| 结构承载 | `实施记录/20260706_FS05_任务模块筹办执行管理边界只读扫描记录.md`、`实施记录/20260706_FS05_任务模块筹办执行管理边界只读扫描_Codex断点清单.md`、`实施记录/20260706_逐函数只读扫描清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-05 第二批已登记；下一旧函数编号从 OF-151 开始；下一步继续 FS-06 旧项目方法系统函数第一批只读扫描。 |
| 调用路径 | 用户继续 -> 逐函数只读扫描清单 -> 旧项目只读 `rg` / 文件读取 -> FS-05 第二批扫描记录 -> 计划索引和项目记忆同步。 |
| 验证方式 | 当前项目 `git status`、旧项目只读文件读取、文档一致性检索、`git diff --check`；本切片未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明 FS-05 第二批任务模块筹办 / 执行 / 管理边界函数已登记；不声明旧任务筹办、旧任务执行、旧任务管理线程或旧治理协议已迁移，不声明完整任务状态机、调度线程、任务管理系统、方法候选系统或动作执行系统完成，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 58. 本次追加实施：FS-06 第一批方法系统入口只读扫描

| 项目 | 内容 |
| --- | --- |
| 输入入口 | FS-05 第二批已提交并推送；计划索引 HR-001-FM-SCAN 要求继续 FS-06，扫描旧项目方法系统函数第一批。 |
| 核心处理 | 只读扫描 `D:\鱼巢\方法主信息类.h`、`D:\鱼巢\方法类.h`、`D:\鱼巢\方法类.cpp`、`D:\鱼巢\方法虚拟存在服务类.h`、`D:\鱼巢\方法虚拟存在服务类.cpp`、`D:\鱼巢\方法节点类型.h` 和 `D:\鱼巢\方法信息类型.h`；登记 OF-151 到 OF-161，覆盖旧方法主信息枚举和角色字段、方法首 / 条件 / 结果构造、方法查找注册、条件 / 结果场景、方法虚拟存在、来源任务、父 / 前置 / 后续方法、方法结构计数、条件结果对、方法动作动态和 SQL 显示排除边界。 |
| 结构承载 | `实施记录/20260706_FS06_方法系统入口只读扫描记录.md`、`实施记录/20260706_FS06_方法系统入口只读扫描_Codex断点清单.md`、`实施记录/20260706_逐函数只读扫描清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-06 第一批已登记；下一旧函数编号从 OF-162 开始；下一步继续 FS-07 旧项目动作 / 动态 / 因果函数第一批只读扫描。 |
| 调用路径 | 用户继续 -> 逐函数只读扫描清单 -> 旧项目只读 `rg` / 文件读取 -> FS-06 第一批扫描记录 -> 计划索引和项目记忆同步。 |
| 验证方式 | 当前项目 `git status`、旧项目只读文件读取、文档一致性检索、`git diff --check`；本切片未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明 FS-06 第一批方法系统入口函数已登记；不声明旧方法系统、旧方法主信息、旧方法虚拟存在服务或旧方法查找能力已迁移，不声明完整方法查找、候选排序、条件结果对、方法学习或动作执行系统完成，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 59. 本次追加实施：函数迁移逻辑迁移优先规则修订和 FS-02 逻辑包归并

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户提出迁移计划修订建议：迁移应是逻辑迁移，逐函数扫描只作为证据采集，函数不是最终迁移单位。 |
| 核心处理 | 修订 `计划/20260706_函数迁移前置专项_v0.1.md`，补充“函数 = 证据采集单位，逻辑能力 = 迁移判定单位，领域服务 / 结构闭环 = 代码实施单位”；修订 `实施记录/20260706_逐函数只读扫描清单.md`，补 `归属逻辑包` 字段和每批扫描后生成逻辑迁移包归并表的门禁；生成 `实施记录/20260706_FS02_状态动态二次特征因果逻辑迁移包归并表.md`。 |
| 结构承载 | 计划文件、逐函数扫描清单、FS-02 逻辑迁移包归并表、计划索引和项目记忆。 |
| 输出变化 | OF-080 到 OF-105 已从函数事实归并为 FS-02-A 到 FS-02-J 和 FS-02-X 逻辑迁移包；当前下一步为提交 FS-02 归并表给用户确认。 |
| 调用路径 | 用户建议 -> 迁移计划修订 -> 扫描清单模板修订 -> FS-02 归并表生成 -> 计划索引和项目记忆同步。 |
| 验证方式 | 文档一致性检查、`git diff --check`；本切片未改代码、未运行构建、未运行当前项目入口。 |
| 禁止扩大声明 | 本次只证明迁移计划已改为逻辑迁移优先并生成 FS-02 归并确认材料；不证明旧函数已迁移，不证明状态 / 动态 / 二次特征 / 因果代码已改造，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 60. 本次追加实施：FS-07 第一批动作动态因果入口只读扫描

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求不需要确认的步骤持续执行；计划索引 HR-001-FM-SCAN 已进入 FS-07 旧项目动作 / 动态 / 因果函数第一批只读扫描。 |
| 核心处理 | 只读扫描 `D:\鱼巢\动态类.h/.cpp`、`D:\鱼巢\因果类.h/.cpp`、`D:\鱼巢\本能方法类.h/.cpp`、`D:\鱼巢\本能动作类.ixx`、`D:\鱼巢\本能动作管理类.ixx`、`D:\鱼巢\动作验证报告类.h`、`D:\鱼巢\动作闭环特征定义.ixx`、`D:\鱼巢\动作线程类.ixx` 和 `D:\鱼巢\自我动作实现*.ixx`；登记 OF-162 到 OF-174，覆盖旧动态证据材料、动作动态、动态聚合、因果模板、证据动态样本、本能动作注册执行、动作线程、自我动作实现边界、动作验证报告和动作闭环特征名。 |
| 结构承载 | `实施记录/20260706_FS07_动作动态因果入口只读扫描记录.md`、`实施记录/20260706_FS07_动作动态因果入口只读扫描_Codex断点清单.md`、`实施记录/20260706_逐函数只读扫描清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-07 第一批已登记；下一旧函数编号从 OF-175 开始；下一步继续 FS-08 旧项目缓存 / 统计函数第一批只读扫描。 |
| 调用路径 | 用户继续 -> 逐函数只读扫描清单 -> 旧项目只读 `rg` / 文件读取 -> FS-07 第一批扫描记录 -> 计划索引和项目记忆同步。 |
| 验证方式 | 当前项目 `git status`、旧项目只读文件读取、文档一致性检索、`git diff --check`；本切片未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明 FS-07 第一批动作 / 动态 / 因果入口函数已登记；不声明旧动作系统、动作动态系统、因果服务、因果模板系统、本能动作注册表或动作线程调度已迁移，不新增旧动作句柄或旧因果模板主信息字段，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 61. 本次追加实施：FS-08 第一批缓存统计入口只读扫描

| 项目 | 内容 |
| --- | --- |
| 输入入口 | FS-07 第一批已提交并推送；计划索引 HR-001-FM-SCAN 要求继续 FS-08，扫描旧项目缓存 / 统计函数第一批。 |
| 核心处理 | 只读扫描 `D:\鱼巢\特征值主信息类.h`、`D:\鱼巢\特征值类.h`、`D:\鱼巢\需求主信息类.h`、`D:\鱼巢\需求类.h/.cpp`、`D:\鱼巢\世界树类.h/.cpp`、`D:\鱼巢\基础信息主信息类.h`、`D:\鱼巢\存在类.cpp`、`D:\鱼巢\二次特征类.cpp` 和显示 / 线程 / 体素统计摘要相关文件；登记 OF-175 到 OF-182，覆盖旧特征值内容哈希、命中观察缓存、哈希候选索引、需求统计、需求结算摘要、结构统计快照、存在观测缓存、二次特征签名缓存和显示 / 线程 / 体素统计排除边界。 |
| 结构承载 | `实施记录/20260706_FS08_缓存统计入口只读扫描记录.md`、`实施记录/20260706_FS08_缓存统计入口只读扫描_Codex断点清单.md`、`实施记录/20260706_逐函数只读扫描清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-08 第一批已登记；下一旧函数编号从 OF-183 开始；下一步继续 FS-09 旧项目事件日志 / 持久化恢复函数第一批只读扫描。 |
| 调用路径 | 用户继续 -> 逐函数只读扫描清单 -> 旧项目只读 `rg` / 文件读取 -> FS-08 第一批扫描记录 -> 计划索引和项目记忆同步。 |
| 验证方式 | 当前项目 `git status`、旧项目只读文件读取、文档一致性检索、`git diff --check`；本切片未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明 FS-08 第一批缓存 / 统计入口函数已登记；不声明旧缓存统计代码已迁移，不声明旧特征值哈希 / 命中字段、旧需求结算摘要或结构统计缓存已实现，不把缓存、统计、SQL 快照、日志摘要或控制面板显示作为业务事实，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 61. 本次追加实施：FS-02 逻辑迁移包归并表确认

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户明确确认 `实施记录/20260706_FS02_状态动态二次特征因果逻辑迁移包归并表.md`，并要求当前窗口作为确认和设计窗口。 |
| 核心处理 | 将 FS-02 归并表状态由待确认改为已确认；确认 FS-02-A 到 FS-02-F、FS-02-H、FS-02-I 进入后续规范 / 详细设计 / 计划补齐；确认 FS-02-G、FS-02-J 暂缓到后续专项，FS-02-X 排除本轮迁移。 |
| 结构承载 | `实施记录/20260706_FS02_状态动态二次特征因果逻辑迁移包归并表.md`、`计划/计划索引.md`、`项目记忆/当前状态.md`、`项目记忆/决策记录.md`、`项目记忆/待确认问题.md`、`项目记忆/Codex任务队列.md`、`项目记忆/验收记录.md`。 |
| 输出变化 | FS-02 逻辑包已从“归并生成 / 待确认”变为“已确认 / 未改代码 / 未迁移旧函数”；后续可由落实路线承接补规范、详细设计和计划。 |
| 调用路径 | 用户确认 -> FS-02 归并表确认状态更新 -> 计划索引和项目记忆同步 -> 后续落实路线补设计材料。 |
| 验证方式 | 文档一致性检查、`git diff --check`；本切片未修改 C++ 代码、未运行构建、未运行当前项目入口。 |
| 禁止扩大声明 | 本次只证明 FS-02 逻辑迁移包归并表已确认；不证明状态 / 动态 / 二次特征 / 因果代码已实现，不证明旧函数体已迁移，不证明旧项目能力已迁移，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 62. 本次追加实施：FS-09 第一批事件日志持久化恢复入口只读扫描

| 项目 | 内容 |
| --- | --- |
| 输入入口 | FS-08 第一批已提交并推送；计划索引 HR-001-FM-SCAN 要求继续 FS-09，扫描旧项目事件日志 / 持久化恢复函数第一批。 |
| 核心处理 | 只读扫描 `D:\鱼巢\日志模块.ixx`、`D:\鱼巢\日志接入.h`、`D:\鱼巢\数据库ADO模块.ixx/.cpp`、`D:\鱼巢\双向链表模板_序列化.inl`、`D:\鱼巢\说明书\双向链表模板序列化说明.md`、`D:\鱼巢\特征值主信息类.h`、`D:\鱼巢\世界树类.h/.cpp`、`D:\鱼巢\语素类.cpp`、`D:\鱼巢\需求类.cpp`、`D:\鱼巢\任务类.cpp`、`D:\鱼巢\方法类.cpp` 和线程 / 外设日志排除边界；登记 OF-183 到 OF-190，覆盖旧日志输出、ADO 字段恢复比对、SQL 控制面板运行态事件流水、世界树 / 领域树 SQL 快照、旧链表二进制保存恢复、特征值主信息序列化和线程 / 外设日志排除边界。 |
| 结构承载 | `实施记录/20260706_FS09_事件日志持久化恢复只读扫描记录.md`、`实施记录/20260706_FS09_事件日志持久化恢复只读扫描_Codex断点清单.md`、`实施记录/20260706_逐函数只读扫描清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-09 第一批已登记；下一旧函数编号从 OF-191 开始；下一步继续 FS-10 旧项目显示层只读候选函数第一批只读扫描。 |
| 调用路径 | 用户继续 -> 逐函数只读扫描清单 -> 旧项目只读 `rg` / 文件读取 -> FS-09 第一批扫描记录 -> 计划索引和项目记忆同步。 |
| 验证方式 | 当前项目 `git status`、旧项目只读文件读取、文档一致性检索、`git diff --check`；本切片未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明 FS-09 第一批事件日志 / 持久化恢复入口函数已登记；不声明事件日志持久化恢复已实现，不声明旧 SQL / ADO / 控制面板投影已迁移，不声明旧链表二进制保存恢复格式可作为新项目快照格式，不把日志、字段恢复比对、SQL 快照、控制面板事件流水、线程事件或外设快照作为运行期机器事实，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 63. 本次追加实施：FS-10 第一批显示层只读候选只读扫描

| 项目 | 内容 |
| --- | --- |
| 输入入口 | FS-09 第一批已提交并推送；计划索引 HR-001-FM-SCAN 要求继续 FS-10，扫描旧项目显示层只读候选函数第一批。 |
| 核心处理 | 只读扫描 `D:\鱼巢\语素类.cpp`、`D:\鱼巢\世界树类.h/.cpp`、`D:\鱼巢\控制面板类.cpp`、`D:\鱼巢\控制面板摘要线程模块.impl.cpp`、`D:\鱼巢\任务模块.管理界面线程.ixx/.impl.cpp` 和 `D:\鱼巢\鱼巢.cpp`；登记 OF-191 到 OF-198，覆盖旧语素和基础信息显示命名、世界树只读结构视图材料、SQL 控制面板世界显示镜像、控制面板快照 / JSON / 渲染输出、SQL / WebView2 / 窗口控制面板入口、控制面板摘要线程、任务管理界面线程和控制台摘要排除边界。 |
| 结构承载 | `实施记录/20260706_FS10_显示层只读候选只读扫描记录.md`、`实施记录/20260706_FS10_显示层只读候选只读扫描_Codex断点清单.md`、`实施记录/20260706_逐函数只读扫描清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-10 第一批已登记；下一旧函数编号从 OF-199 开始；下一步继续 FS-X 控制面板 / SQL / D455 / 体素 / 外设函数排除项汇总。 |
| 调用路径 | 用户继续 -> 逐函数只读扫描清单 -> 旧项目只读 `rg` / 文件读取 -> FS-10 第一批扫描记录 -> 计划索引和项目记忆同步。 |
| 验证方式 | 当前项目 `git status`、旧项目只读文件读取、文档一致性检索、`git diff --check`；本切片未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明 FS-10 第一批显示层只读候选函数已登记；不声明显示层只读视图已设计或已实现，不声明旧控制面板、SQL 读模型、WebView2、相机窗口、自我场景窗口、截图或控制台命令已迁移，不把显示标题、HTML、JSON、控制台摘要、线程快照、SQL 显示镜像或控制面板事件作为机器事实，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 64. 本次追加实施：FS-X 控制面板 SQL D455 体素外设排除项汇总

| 项目 | 内容 |
| --- | --- |
| 输入入口 | FS-10 第一批已提交并推送；计划索引 HR-001-FM-SCAN 要求继续 FS-X 控制面板 / SQL / D455 / 体素 / 外设函数排除项汇总。 |
| 核心处理 | 只读核对旧项目控制面板、WebView2、数据库 ADO、D455 相机、双目相机适配器、外设观察报告队列、D455 外设线程、场景体素、视觉观察融合和外设本能动作实现边界；登记 OF-199 到 OF-206 为本轮排除项 / 后续专项。 |
| 结构承载 | `实施记录/20260706_FSX_控制面板SQLD455体素外设排除项汇总记录.md`、`实施记录/20260706_FSX_控制面板SQLD455体素外设排除项汇总_Codex断点清单.md`、`实施记录/20260706_逐函数只读扫描清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-X 排除项已汇总；FS-00 至 FS-X 第一轮只读扫描已覆盖；下一旧函数编号从 OF-207 开始；下一步生成 FS-03 特征 / 特征值逻辑迁移包归并表。 |
| 调用路径 | 用户继续 -> 逐函数只读扫描清单 -> 旧项目只读 `rg` / 文件读取 -> FS-X 排除项汇总记录 -> 计划索引和项目记忆同步。 |
| 验证方式 | 当前项目 `git status`、旧项目只读文件读取、文档一致性检索、`git diff --check`；本切片未运行构建和默认入口。 |
| 禁止扩大声明 | 本次只声明控制面板 / SQL / D455 / 体素 / 外设相关旧函数族已登记为本轮排除项或后续专项材料；不声明旧控制面板、SQL / ADO、D455、体素、视觉融合、外设线程或外设本能动作已迁移，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 65. 本次追加实施：FS-03 特征特征值逻辑迁移包归并表生成

| 项目 | 内容 |
| --- | --- |
| 输入入口 | FS-X 排除项汇总已提交并推送；计划索引 HR-001-FM-SCAN 要求生成 FS-03 特征 / 特征值逻辑迁移包归并表。 |
| 核心处理 | 基于 `实施记录/20260706_FS03_特征与特征值入口只读扫描记录.md`，将 OF-106 到 OF-117 从函数事实归并为 FS-03-A 到 FS-03-I 和 FS-03-X 逻辑迁移包。 |
| 结构承载 | `实施记录/20260706_FS03_特征特征值逻辑迁移包归并表.md`、`实施记录/20260706_FS03_特征特征值逻辑迁移包归并表_Codex断点清单.md`、`实施记录/20260706_逐函数只读扫描清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-03 归并表生成时为待确认；特征身份、实例特征、I64 特征值写入读取、Vec 原始值容器、非权威缓存、值域匹配、稳态、序列化和体素排除边界已按逻辑包分流。当前确认状态见第 66 节。 |
| 调用路径 | 用户继续 -> 计划索引 HR-001-FM-SCAN -> FS-03 只读扫描记录 -> FS-03 逻辑迁移包归并表 -> 计划索引和项目记忆同步。 |
| 验证方式 | 文档一致性检索、`git diff --check`；本切片未修改 C++ 代码、未运行构建、未运行当前项目入口。 |
| 禁止扩大声明 | 第 65 节只声明 FS-03 逻辑迁移包归并表已生成；不声明旧特征类或旧特征值类已迁移，不声明旧特征主信息 / 抽象特征主信息 / 特征值主信息字段可直接迁移，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 66. 本次追加实施：FS-03 特征特征值逻辑迁移包归并表确认

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户明确确认 `实施记录/20260706_FS03_特征特征值逻辑迁移包归并表.md`。 |
| 核心处理 | 将 FS-03 归并表状态由待确认改为已确认；确认 FS-03-A、FS-03-C、FS-03-D 后续补特征服务相关规范 / 详细设计 / 计划；确认 FS-03-B、FS-03-E、FS-03-F、FS-03-G、FS-03-H、FS-03-I 转后续专项，FS-03-X 排除本轮迁移。 |
| 结构承载 | `实施记录/20260706_FS03_特征特征值逻辑迁移包归并表.md`、`实施记录/20260706_逐函数只读扫描清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-03 逻辑包已从“归并生成 / 待确认”变为“已确认 / 未改代码 / 未迁移旧函数”；后续可由落实路线承接补规范、详细设计和计划。 |
| 调用路径 | 用户确认 -> FS-03 归并表确认状态更新 -> 计划索引和项目记忆同步 -> 后续落实路线补设计材料。 |
| 验证方式 | 文档一致性检查、`git diff --check`；本切片未修改 C++ 代码、未运行构建、未运行当前项目入口。 |
| 禁止扩大声明 | 本次只证明 FS-03 逻辑迁移包归并表已确认；不证明特征 / 特征值代码已实现，不证明旧函数体已迁移，不证明 Vec 原始值容器、内容哈希、稳态、抽象特征、值域系统、体素或外设链路已实现，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 67. 本次追加实施：确认后加入可执行队列策略修订

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求修改策略：确认后应该加入可执行队列。 |
| 核心处理 | 修订计划索引双路执行路由，当时明确确认事项必须登记到 `项目记忆/Codex任务队列.md` 可执行队列；后续第 70 节已将该口径收窄为确认后的计划必须入队。将 FS-02 和 FS-03 已确认逻辑包承接项从待承接改为已入可执行队列。 |
| 结构承载 | `AGENTS.md`、`计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/当前状态.md`、`项目记忆/决策记录.md`、`项目记忆/验收记录.md`。 |
| 输出变化 | 后续确认不再只形成确认状态，必须形成可执行队列项；当前可执行队列包含 FS-02 和 FS-03 已确认逻辑包的规范 / 详细设计 / 计划补齐项。 |
| 调用路径 | 用户策略修订 -> 计划索引双路路由 -> 项目记忆任务队列 -> 决策和验收记录同步。 |
| 验证方式 | 文档一致性检查、`git diff --check`；本切片未修改 C++ 代码、未运行构建、未运行当前项目入口。 |
| 禁止扩大声明 | 确认后的计划加入可执行队列不等于允许代码实施；代码实施、旧能力迁移、字段落地、函数迁移或机器结构改变仍需独立切片确认允许文件、禁止文件和验证方式。 |

## 70. 本次追加实施：确认后入队对象收窄为计划

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户指出规则应修订为“确认后的计划”，不是“确认后的事项”。 |
| 核心处理 | 将当前规则入口中的“确认后的事项 / 确认事项”收窄为“确认后的计划”；补充详细设计确认、问题裁决和单个确认结论只更新对应状态，不自动进入可执行队列。 |
| 结构承载 | `AGENTS.md`、`计划/计划索引.md`、`项目记忆/当前状态.md`、`项目记忆/决策记录.md`、`项目记忆/实施记录.md`、`项目记忆/Codex任务队列.md`、`项目记忆/验收记录.md`。 |
| 输出变化 | 确认后入队规则从泛化事项收窄为计划；确认结论本身不等于代码实施许可，也不自动成为可执行队列项。 |
| 调用路径 | 用户修订规则 -> 规则入口修订 -> 计划索引和项目记忆同步 -> 文档验证。 |
| 验证方式 | 文档一致性检索、`git diff --check`；本切片未修改 C++ 代码、未运行构建、未运行当前项目入口。 |
| 禁止扩大声明 | 本次只证明规则口径已修订；不证明新的代码实施项存在，不证明 FS-03 专项计划已确认，不证明旧函数或旧能力已迁移。 |

## 68. 本次追加实施：FS-03 特征服务封口增强专项生成

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户给出当前状态并要求下一步生成 FS-03 特征服务封口增强专项；FS-03 逻辑迁移包归并表已确认，FS-03-A、FS-03-C、FS-03-D 可补特征服务相关规范 / 详细设计 / 计划。 |
| 核心处理 | 复核项目规则、计划索引、FS-03 归并表、基础信息服务分层规范、领域服务授权写入规范、错误结果与入口拒绝规范、领域服务最小入口详细设计，并只读核对当前 `特征服务.h`、`特征值服务.h` 和 `句柄.h`；生成特征服务封口增强详细设计、专项计划和断点清单。 |
| 结构承载 | `规范/详细设计/特征服务封口增强详细设计.md`、`计划/已完成计划/20260706_FS03_特征服务封口增强专项_v0.1.md`、`实施记录/20260706_FS03_特征服务封口增强专项生成_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-03-A、FS-03-C、FS-03-D 已形成特征身份读取、实例特征宿主槽位、I64 特征状态材料和特征值服务内部封口的设计与计划；状态为已生成 / 待确认 / 未改代码 / 未迁移旧函数。 |
| 调用路径 | 用户下一步 -> 计划索引 HR-001-FM-SCAN -> FS-03 已确认逻辑迁移包 -> 特征服务封口增强详细设计 -> FS-03 专项计划 -> 项目记忆同步。 |
| 验证方式 | 文档一致性检查、`git diff --check`、`tools/check_specs.py` 如存在则运行；本切片未修改 C++ 代码、未运行构建、未运行当前项目入口。 |
| 禁止扩大声明 | 本次只证明 FS-03 特征服务封口增强专项已生成待确认；不证明特征 / 特征值代码已实现，不证明旧函数体已迁移，不证明 Vec 原始值、内容哈希、稳态、抽象特征、值域系统、序列化恢复、体素或外设链路已实现。 |

## 69. 本次追加实施：FS-03 特征服务封口增强详细设计确认

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认 `规范/详细设计/特征服务封口增强详细设计.md`。 |
| 核心处理 | 将特征服务封口增强详细设计状态改为已确认；同步计划索引和项目记忆；生成确认断点清单；专项计划继续保持待确认。 |
| 结构承载 | `规范/详细设计/特征服务封口增强详细设计.md`、`实施记录/20260706_FS03_特征服务封口增强详细设计确认_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-03 特征服务封口增强详细设计已确认；该时点专项计划处于待确认状态；当前状态见第 74 节。 |
| 调用路径 | 用户确认详细设计 -> 更新详细设计状态 -> 更新计划索引 -> 更新项目记忆 -> 记录确认断点。 |
| 验证方式 | 文档一致性检查、`git diff --check`；本切片未修改 C++ 代码、未运行构建、未运行当前项目入口。 |
| 禁止扩大声明 | 本次只证明 FS-03 特征服务封口增强详细设计已确认；不证明专项计划已确认，不证明特征 / 特征值代码已实现，不证明旧函数体已迁移，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 71. 本次追加实施：FS-02 状态动态二次特征因果服务增强专项生成

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户要求继续推进设计窗口中已确认后的后续设计治理；FS-02 逻辑迁移包归并表已确认，FS-02-A 到 FS-02-F、FS-02-H、FS-02-I 可补规范 / 详细设计 / 计划。 |
| 核心处理 | 复核项目规则、计划索引、FS-02 归并表、基础信息服务分层规范、领域服务授权写入规范、状态服务 / 动态服务 / 二次特征服务 / 领域服务最小入口详细设计，并只读核对当前 `状态服务.h`、`动态服务.h`、`二次特征服务.h` 和 `因果服务.h`；生成 FS-02 服务增强详细设计、专项计划和断点清单。 |
| 结构承载 | `规范/详细设计/状态动态二次特征因果服务增强详细设计.md`、`计划/已完成计划/20260706_FS02_状态动态二次特征因果服务增强专项_v0.1.md`、`实施记录/20260706_FS02_状态动态二次特征因果服务增强专项生成_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-02-A 到 FS-02-F、FS-02-H、FS-02-I 已形成状态身份读取、实例状态创建与场景收纳、状态变化显式动态、动作动态来源绑定、二次特征组成读取和轻量因果动态准入的设计与计划；状态为已生成 / 待确认 / 未改代码 / 未迁移旧函数。 |
| 调用路径 | 用户继续推进 -> 计划索引 HR-001-FM-SCAN -> FS-02 已确认逻辑迁移包 -> 状态动态二次特征因果服务增强详细设计 -> FS-02 专项计划 -> 项目记忆同步。 |
| 验证方式 | 文档一致性检查、`git diff --check`、`tools/check_specs.py` 如存在则运行；本切片未修改 C++ 代码、未运行构建、未运行当前项目入口。 |
| 禁止扩大声明 | 本次只证明 FS-02 状态动态二次特征因果服务增强专项已生成待确认；不证明状态 / 动态 / 二次特征 / 因果代码已实现，不证明旧函数体已迁移，不证明动态聚合、运动基元、因果模板、因果任务化、显示只读视图、控制面板 / SQL / D455 / 体素 / 外设链路已实现。 |

## 72. 本次追加实施：FS-02 状态动态二次特征因果服务增强详细设计确认

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认 `规范/详细设计/状态动态二次特征因果服务增强详细设计.md`，并要求按文件内默认建议同步确认 FS02-Q1 到 FS02-Q4，同时将 `删除实例状态(状态)` 明确为后续 / 暂缓。 |
| 核心处理 | 将 FS-02 服务增强详细设计状态改为已确认；确认 FS02-Q1 第一轮只补读取和拒绝、不新增删除写入口；确认 FS02-Q2 来源动作复用动作入口角色节点口径；确认 FS02-Q3 状态变化动态要求前后状态同场景；确认 FS02-Q4 二次特征状态迁移只建立组成关系、不写差值材料；同步计划索引和项目记忆。 |
| 结构承载 | `规范/详细设计/状态动态二次特征因果服务增强详细设计.md`、`计划/已完成计划/20260706_FS02_状态动态二次特征因果服务增强专项_v0.1.md`、`实施记录/20260707_FS02_状态动态二次特征因果服务增强详细设计确认_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-02 状态动态二次特征因果服务增强详细设计已确认；该时点专项计划处于待确认状态；当前状态见第 73 节。 |
| 调用路径 | 用户确认详细设计 -> 更新详细设计状态和确认问题 -> 更新计划索引 -> 更新项目记忆 -> 记录确认断点。 |
| 验证方式 | 文档一致性检查、`git diff --check`；本切片未修改 C++ 代码、未运行构建、未运行当前项目入口。 |
| 禁止扩大声明 | 本次只证明 FS-02 服务增强详细设计已确认；不证明专项计划已确认，不证明状态 / 动态 / 二次特征 / 因果代码已实现，不证明旧函数体已迁移，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 73. 本次追加实施：FS-02 状态动态二次特征因果服务增强专项计划确认和代码实施切片生成

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认 `计划/已完成计划/20260706_FS02_状态动态二次特征因果服务增强专项_v0.1.md`，确认范围为 FS-02-A/B/C/D/E/F/H/I 的第一轮增强计划边界，并要求另建 FS02 第一轮代码实施切片。 |
| 核心处理 | 将 FS-02 服务增强专项计划状态改为已确认；保持 FS02-Q1 到 FS02-Q4 默认保守口径；生成 `计划/已完成计划/20260707_FS02_状态动态二次特征因果第一轮代码实施切片_v0.1.md`，明确允许文件、禁止文件、验证方式和代码实施确认门禁；同步计划索引和项目记忆。 |
| 结构承载 | `计划/已完成计划/20260706_FS02_状态动态二次特征因果服务增强专项_v0.1.md`、`计划/已完成计划/20260707_FS02_状态动态二次特征因果第一轮代码实施切片_v0.1.md`、`实施记录/20260707_FS02_状态动态二次特征因果第一轮代码实施切片生成_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-02 服务增强专项计划已确认；FS-02 第一轮代码实施切片已生成待确认；当前仍未进入 C++ 实施。 |
| 调用路径 | 用户确认专项计划 -> 更新专项计划状态 -> 生成第一轮代码实施切片 -> 更新计划索引 -> 更新项目记忆 -> 记录断点清单。 |
| 验证方式 | 文档一致性检查、`git diff --check`、`tools/check_specs.py` 如存在则运行；本切片未修改 C++ 代码、未运行构建、未运行当前项目入口。 |
| 禁止扩大声明 | 本次只证明 FS-02 专项计划已确认并生成代码实施切片；不证明代码实施切片已确认，不证明状态 / 动态 / 二次特征 / 因果代码已实现，不证明旧函数体已迁移，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 74. 本次追加实施：FS-03 特征服务封口增强专项计划确认和 S0 扫描切片生成

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户确认 `计划/已完成计划/20260706_FS03_特征服务封口增强专项_v0.1.md`，确认范围为 FS-03-A / FS-03-C / FS-03-D 的第一轮特征服务封口路线，并要求另建 S0 当前代码事实扫描切片。 |
| 核心处理 | 将 FS-03 特征服务封口增强专项计划状态改为已确认；生成 `计划/已完成计划/20260707_FS03_特征服务封口增强S0当前代码事实扫描切片_v0.1.md`，明确只读扫描范围、禁止修改代码、输出断点清单和后续代码实施门禁；同步计划索引和项目记忆。 |
| 结构承载 | `计划/已完成计划/20260706_FS03_特征服务封口增强专项_v0.1.md`、`计划/已完成计划/20260707_FS03_特征服务封口增强S0当前代码事实扫描切片_v0.1.md`、`实施记录/20260707_FS03_特征服务封口增强专项确认与S0扫描切片生成_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-03 特征服务封口增强专项计划已确认；S0 当前代码事实扫描切片已生成，后续 S0 扫描完成状态见第 75 节；当前仍未进入 C++ 实施。 |
| 调用路径 | 用户确认专项计划 -> 更新专项计划状态 -> 生成 S0 当前代码事实扫描切片 -> 更新计划索引 -> 更新项目记忆 -> 记录断点清单。 |
| 验证方式 | 文档一致性检查、`git diff --check`、`tools/check_specs.py` 如存在则运行；本切片未修改 C++ 代码、未运行构建、未运行当前项目入口。 |
| 禁止扩大声明 | 本次只证明 FS-03 专项计划已确认并生成 S0 只读扫描切片；不证明 S0 扫描已完成，不证明特征服务封口增强代码已实现，不证明旧函数体已迁移，不接 Vec / 哈希 / 稳态 / 值域 / 序列化 / 体素 / 外设 / 控制面板 / SQL。 |

## 75. 本次追加实施：FS-03 特征服务封口增强 S0 当前代码事实扫描

| 项 | 内容 |
| --- | --- |
| 输入入口 | `计划/已完成计划/20260707_FS03_特征服务封口增强S0当前代码事实扫描切片_v0.1.md` 已登记为可执行队列项并执行。 |
| 核心处理 | 只读扫描 `特征服务.h`、`特征值服务.h`、`句柄.h`、需求 / 任务 / 方法 / 动态服务、入口样例、函数说明书、FS-03 详细设计和专项计划；检索特征值服务、I64 特征值读写、特征 / 特征值节点类型和归属关系。 |
| 结构承载 | `实施记录/20260707_FS03_特征服务封口增强S0当前代码事实扫描_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-03 S0 当前代码事实扫描完成；当前未发现需求 / 任务 / 方法 / 动态直接依赖特征值服务；后续仍缺特征身份只读复核、特征语义类型读取、宿主实例特征槽位和 S1-S4 独立代码实施切片。 |
| 调用路径 | 用户要求执行可执行计划 -> 计划索引 HR-001-FM-SCAN -> FS-03 S0 当前代码事实扫描切片 -> 当前代码只读扫描 -> 断点清单和项目记忆同步。 |
| 验证方式 | `git diff --check`；`tools/check_specs.py` 不存在则跳过；`git status --short --branch`。 |
| 禁止扩大声明 | 本次只证明 S0 当前代码事实扫描已完成；不证明特征服务封口增强代码已实现，不证明旧特征 / 特征值函数体已迁移，不证明 C++ 构建或入口拒绝通过。 |

## 76. 本次追加实施：FS-03 S0 问题裁决与 FS-02 代码实施切片确认登记

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户确认 FS-03 S0 断点清单中的三个问题，并确认 `计划/已完成计划/20260707_FS02_状态动态二次特征因果第一轮代码实施切片_v0.1.md`。 |
| 核心处理 | 将 FS-03 S0 三个问题登记为已确认；将 FS-02 第一轮代码实施切片登记为已确认并进入落实执行队列。 |
| 结构承载 | `实施记录/20260707_FS03_特征服务封口增强S0当前代码事实扫描_Codex断点清单.md`、`计划/已完成计划/20260707_FS02_状态动态二次特征因果第一轮代码实施切片_v0.1.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-03 S1-S4 确认为拆分实施；特征值服务 public 入口第一轮保持现状；当前特征值唯一性第一轮只做归属复核和读取拒绝。FS-02 第一轮代码实施切片进入执行。 |
| 调用路径 | 用户确认 -> 更新 FS-03 S0 断点清单 -> 更新 FS-02 代码实施切片状态 -> 更新计划索引和项目记忆 -> 进入 FS-02 代码实施。 |
| 验证方式 | 文档一致性检索；后续 FS-02 代码实施完成后按切片执行 Debug x64 构建、默认入口运行和 `git diff --check`。 |
| 禁止扩大声明 | 本次确认登记不证明 FS-03 S1-S4 已允许实施，不证明 FS-02 代码已实现或已验证，不迁移旧函数体，不新增旧主信息字段，不接 SQL / 控制面板 / D455 / 体素 / 外设。 |

## 77. 本次追加实施：FS-02 状态动态二次特征因果第一轮代码实施

| 项 | 内容 |
| --- | --- |
| 输入入口 | `计划/已完成计划/20260707_FS02_状态动态二次特征因果第一轮代码实施切片_v0.1.md` 已由用户确认。 |
| 核心处理 | 补状态材料读取、动态材料读取、状态变化显式动态、来源动作入口角色准入、二次特征组成读取、状态迁移二次特征组成入口、因果动态材料准入和默认入口十项 FS-02 验收输出。 |
| 结构承载 | `海中鱼巣/领域/状态服务.h`、`海中鱼巣/领域/动态服务.h`、`海中鱼巣/领域/二次特征服务.h`、`海中鱼巣/领域/因果服务.h`、`海中鱼巣/入口.cpp`、`实施记录/20260707_FS02_状态动态二次特征因果第一轮代码实施_Codex断点清单.md`。 |
| 输出变化 | 默认入口新增 FS-02 十项输出：状态材料读取、状态读取拒绝、状态变化动态、动态材料读取、动态拒绝路径、来源动作准入、二次特征组成读取、二次特征非权威边界、轻量因果准入和因果轻量边界。 |
| 调用路径 | 用户确认代码实施切片 -> 状态 / 动态 / 二次特征 / 因果服务增强 -> 入口 FS-02 分项验收 -> Debug x64 构建 -> 默认入口运行 -> 项目记忆和断点清单同步。 |
| 验证方式 | `msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m` 成功，0 警告 0 错误；`.\x64\Debug\海中鱼巣.exe` 退出码 0，FS-02 十项均输出通过，MC-01 到 MC-11 均通过；`git diff --check` 无错误。 |
| 禁止扩大声明 | 本次只证明 FS-02-A/B/C/D/E/F/H/I 第一轮增强通过当前默认入口验证；不证明旧函数体已迁移，不证明旧主信息字段已迁移，不证明完整状态 / 动态 / 二次特征 / 因果系统完成，不进入 FS-02-G、FS-02-J、FS-02-X，不接 SQL / 控制面板 / D455 / 体素 / 外设。 |

## 78. 本次追加实施：FS-02 / FS-03 执行队列状态一致性收口

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求继续执行已确认步骤；当前计划索引和任务队列显示 FS-02 第一轮代码实施已完成，FS-03 S0 三个断点问题已确认。 |
| 核心处理 | 复核 `计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/当前状态.md`、FS-02 代码实施断点清单和 FS-03 S0 扫描断点清单；修正项目记忆中 FS-03 承接项旧“待确认”状态，并将当前下一步收束为执行队列无新的已确认代码实施项。 |
| 结构承载 | `项目记忆/Codex任务队列.md`、`项目记忆/当前状态.md`、`项目记忆/实施记录.md`、`实施记录/20260707_FS02_FS03_执行队列状态一致性收口_Codex断点清单.md`。 |
| 输出变化 | FS-02 明确为已完成并验证；FS-03 明确为专项计划已确认、S0 已完成、S0 三个断点问题已确认；S1 到 S4 仍需另建代码实施切片并确认允许文件、禁止文件和验证方式后才能进入 C++ 实施。 |
| 调用路径 | 用户继续 -> 计划索引 HR-001 / HR-001-FM-SCAN -> 项目记忆任务队列和当前状态复核 -> 项目记忆状态一致性修正。 |
| 验证方式 | `rg` 关键词复核待执行 / 待确认 / 下一步状态；`git diff --check`；本切片未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明项目记忆状态一致性已收口；不证明 FS-03 S1-S4 代码实施已获许可，不证明特征服务封口增强代码已实现，不迁移旧函数体，不新增旧主信息字段，不接 SQL / 控制面板 / D455 / 体素 / 外设。 |

## 79. 本次追加实施：FS-03 特征服务封口增强 S1-S4 代码实施切片生成

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户确认 FS-03 S1-S4 后续代码实施切片方向可以进入独立代码实施切片设计，但不能直接开写 C++。 |
| 核心处理 | 基于 FS-03 详细设计、专项计划、S0 当前代码事实扫描和三个已确认断点问题，生成 `计划/已完成计划/20260707_FS03_特征服务封口增强S1-S4代码实施切片_v0.1.md`，明确 S1 到 S4 拆分实施、特征值服务 public 入口保持现状、当前值唯一性第一轮只做归属复核和读取拒绝。 |
| 结构承载 | `计划/已完成计划/20260707_FS03_特征服务封口增强S1-S4代码实施切片_v0.1.md`、`实施记录/20260707_FS03_特征服务封口增强S1-S4代码实施切片生成_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/*`。 |
| 输出变化 | FS-03 S1-S4 代码实施切片当时已生成待确认；后续已由用户确认并由执行窗口完成代码实施验证。切片已明确允许文件、禁止文件、入口函数名、结构变化、验证命令、拒绝后结构不变化检查方式和完成声明边界。 |
| 调用路径 | 用户确认方向 -> FS-03 S0 断点裁决 -> 代码实施切片设计 -> 计划索引和项目记忆同步。 |
| 验证方式 | `git diff --check`；`tools/check_specs.py` 如存在则运行；本切片未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本节原始生成动作只证明 FS-03 S1-S4 代码实施切片已生成；后续执行记录见第 81 节。不得据此声明旧函数体迁移、旧主信息字段新增、Vec / 哈希 / 稳态 / 值域 / 序列化 / SQL / 控制面板 / D455 / 体素 / 外设已实现。 |

## 80. 本次追加实施：双窗口规则确认与 FS-03 执行收口入队

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户重新确认双窗口规则：一个窗口确认并登记可执行队列，另一个窗口持续执行；执行中需要确认的内容进入待确认队列。 |
| 核心处理 | 将确认窗口职责收束为确认、设计、入队和待确认登记；当前 worktree 已存在 FS-03 S1-S4 相关实施差异，确认窗口不接管 C++，只将执行收口登记给执行窗口。 |
| 结构承载 | `计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/待确认问题.md`、`项目记忆/当前状态.md`、`项目记忆/决策记录.md`、`实施记录/20260707_FS03_S1_特征身份读取代码实施切片确认_Codex断点清单.md`。 |
| 输出变化 | 执行窗口下一步承接 FS-03 S1-S4 实施差异验证、实施断点清单、项目记忆同步和云端同步；若验证失败、发现越权文件或需要改变切片边界，必须登记到待确认队列。 |
| 调用路径 | 用户确认规则 -> 计划索引确认路由更新 -> 任务队列登记 FS-03 S1-S4 执行收口项 -> 待确认问题登记执行中新边界问题入口 -> 新增规则确认断点清单。 |
| 验证方式 | 文档一致性复核、`git diff --check`、`tools/check_specs.py` 如存在则运行；本确认窗口不运行 C++ 构建，不运行默认入口。 |
| 禁止扩大声明 | 本次只证明双窗口规则已重新确认且 FS-03 执行收口已入队；不证明本确认窗口完成 C++ 实施，不证明本确认窗口完成构建或默认入口验证，不证明特征服务封口增强完整完成，不迁移旧函数体，不新增旧主信息字段，不接 Vec / 哈希 / 稳态 / 值域 / 序列化 / SQL / 控制面板 / D455 / 体素 / 外设。 |

## 81. 本次追加实施：FS-03 特征服务封口增强 S1-S4 代码实施

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户确认 `计划/已完成计划/20260707_FS03_特征服务封口增强S1-S4代码实施切片_v0.1.md`，并明确当前窗口按执行窗口承接已确认步骤。 |
| 核心处理 | `特征服务.h` 新增特征身份与语义类型只读入口、实例特征槽位入口、宿主 I64 特征状态材料读写入口和拒绝路径辅助；`入口.cpp` 新增 FS-03 S1-S4 默认验收输出。 |
| 结构承载 | S1 不新增结构；S2 用特征槽位节点、宿主到槽位 `归属` 关系、槽位到特征类型 `模板` 关系承载；S3 复用特征值节点和归属关系承载 I64 材料；S4 只做拒绝路径和依赖扫描验收。 |
| 输出变化 | 默认入口新增 `FS-03 S1 特征身份读取`、`FS-03 S1 特征语义类型读取`、`FS-03 S2 实例特征槽位创建`、`FS-03 S2 实例特征槽位拒绝`、`FS-03 S3 I64 写入读回`、`FS-03 S3 非归属写入拒绝`、`FS-03 S3 非归属读取拒绝`、`FS-03 S4 高级服务越权依赖复核`、`FS-03 S4 拒绝后结构不变`。 |
| 调用路径 | `入口.cpp` -> `特征服务` -> 节点仓库 / 关系仓库 / 特征值服务 / 主信息仓库；高级服务依赖由 `rg` 命令只读复核。 |
| 验证方式 | `msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m` 通过，0 警告，0 错误；运行 `.\x64\Debug\海中鱼巣.exe` 退出码 0，FS-03 S1-S4 输出均为通过；高级服务 `rg` 依赖扫描无命中；`git diff --check` 无错误；`tools/check_specs.py` 当前不存在。 |
| 禁止扩大声明 | 本次只证明 FS-03 S1-S4 第一轮代码实施通过当前默认入口验证；不声明旧特征 / 特征值能力已迁移；不声明特征服务封口增强完整完成；不声明特征值服务 public 入口治理、当前值唯一性治理、Vec / 哈希 / 稳态 / 值域 / 序列化、SQL / 控制面板 / D455 / 体素 / 外设已实现。 |

## 82. 本次追加实施：FS-05 任务服务承接材料与生命周期增强专项计划确认

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求将 FS-05 专项计划状态改为已确认，将 FS-05 S0 当前代码事实扫描登记到可执行队列，同步计划索引和项目记忆，并新增确认断点清单。 |
| 核心处理 | 将 `计划/20260707_FS05_任务服务承接材料与生命周期增强专项_v0.1.md` 状态改为已确认；将 QR-043 改为已确认；登记 `执行 FS-05 S0 当前代码事实扫描` 到 `项目记忆/Codex任务队列.md` 可执行队列。 |
| 结构承载 | `计划/20260707_FS05_任务服务承接材料与生命周期增强专项_v0.1.md`、`计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/待确认问题.md`、`项目记忆/当前状态.md`、`实施记录/20260707_FS05_任务服务承接材料与生命周期增强专项确认_Codex断点清单.md`。 |
| 输出变化 | FS-05-B/C/D/E 第一轮增强路线已确认；执行窗口可承接 FS-05 S0 当前代码事实扫描；S1 到 S4 仍需另建独立代码实施切片并确认允许文件、禁止文件和验证方式。 |
| 调用路径 | 用户确认 -> FS-05 专项计划状态更新 -> 待确认问题收口 -> 可执行队列登记 -> 计划索引和项目记忆同步。 |
| 验证方式 | 文档一致性检查；`git diff --check`；`tools/check_specs.py` 当前不存在则跳过；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明 FS-05 专项计划已确认并登记 S0 只读扫描；不证明 FS-05 代码已实现，不证明旧任务函数体或旧任务主信息字段已迁移，不证明完整任务状态机、任务筹办、任务执行、方法候选或调度线程完成，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 83. 本次追加实施：FS-04 需求服务目标状态与任务承接材料 S0 当前代码事实扫描

| 项 | 内容 |
| --- | --- |
| 输入入口 | `计划/20260707_FS04_需求服务目标状态与任务承接材料增强专项_v0.1.md` 已确认，`项目记忆/Codex任务队列.md` #86 已登记为可执行 S0 只读扫描。 |
| 核心处理 | 只读扫描需求服务、任务服务、状态 / 场景 / 存在服务、关系仓库和默认入口调用点；复核需求身份、目标状态、需求到任务承接关系、需求结算记录和高级服务依赖边界。 |
| 结构承载 | `实施记录/20260707_FS04_需求服务目标状态与任务承接材料S0当前代码事实扫描_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/当前状态.md`、`项目记忆/待确认问题.md`。 |
| 输出变化 | FS-04 S0 扫描完成；已记录统一需求承接材料、需求场景材料、主体 / 目标宿主角色区分和非权威摘要分流仍需后续切片。 |
| 调用路径 | 用户执行 -> 可执行队列 #86 -> 当前代码只读扫描 -> 断点清单 -> 项目记忆和计划索引同步。 |
| 验证方式 | `rg` 只读扫描；`git diff --check`；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明 FS-04 S0 当前代码事实扫描已完成；不证明 FS-04 代码已实现，不证明旧需求函数体或旧需求主信息字段已迁移，不证明完整需求树、任务筹办或需求结算系统完成。 |

## 84. 本次追加实施：FS-05 任务服务承接材料与生命周期 S0 当前代码事实扫描

| 项 | 内容 |
| --- | --- |
| 输入入口 | `计划/20260707_FS05_任务服务承接材料与生命周期增强专项_v0.1.md` 已确认，`项目记忆/Codex任务队列.md` #87 已登记为可执行 S0 只读扫描。 |
| 核心处理 | 只读扫描任务服务、需求服务、方法服务、存在 / 状态 / 场景服务、关系仓库和默认入口调用点；复核任务承接壳、虚拟存在、生命周期状态、任务方法关系和高级服务依赖边界。 |
| 结构承载 | `实施记录/20260707_FS05_任务服务承接材料与生命周期S0当前代码事实扫描_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/当前状态.md`、`项目记忆/待确认问题.md`。 |
| 输出变化 | FS-05 S0 扫描完成；已记录统一任务承接材料、任务过程账、任务实际结果状态、任务选择 / 当前 / 候选方法入口和完整任务状态机仍需后续切片。 |
| 调用路径 | 用户执行 -> 可执行队列 #87 -> 当前代码只读扫描 -> 断点清单 -> 项目记忆和计划索引同步。 |
| 验证方式 | `rg` 只读扫描；`git diff --check`；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明 FS-05 S0 当前代码事实扫描已完成；不证明 FS-05 代码已实现，不证明旧任务函数体或旧任务主信息字段已迁移，不证明完整任务状态机、任务筹办、任务执行、方法候选或调度线程完成。 |

## 85. 本次追加实施：FS-06 方法服务角色与条件结果读取 S0 当前代码事实扫描

| 项 | 内容 |
| --- | --- |
| 输入入口 | `计划/20260707_FS06_方法服务角色与条件结果读取增强专项_v0.1.md` 已确认，`项目记忆/Codex任务队列.md` #88 已登记为可执行 S0 只读扫描。 |
| 核心处理 | 只读扫描方法服务、存在 / 状态 / 场景服务、关系仓库和默认入口调用点；复核方法角色状态、方法首 / 条件 / 结果节点、条件 / 结果场景、方法虚拟存在和动作入口边界。 |
| 结构承载 | `实施记录/20260707_FS06_方法服务角色与条件结果读取S0当前代码事实扫描_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/当前状态.md`、`项目记忆/待确认问题.md`。 |
| 输出变化 | FS-06 S0 扫描完成；确认当前无独立 `动作入口服务.h`，动作入口仍在 `方法服务.h`；方法身份只读包装、条件结果对、来源 / 父 / 前置 / 后续方法关系、候选召回、结果能力和统计仍需后续切片。 |
| 调用路径 | 用户执行 -> 可执行队列 #88 -> 当前代码只读扫描 -> 断点清单 -> 项目记忆和计划索引同步。 |
| 验证方式 | `rg` 只读扫描；`git diff --check`；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明 FS-06 S0 当前代码事实扫描已完成；不证明 FS-06 代码已实现，不证明旧方法函数体或旧方法主信息字段已迁移，不证明完整方法查找、候选排序、条件结果对、方法学习或动作执行系统完成。 |

## 83. 本次追加实施：FS-06 方法服务角色与条件结果读取增强专项计划确认

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求确认 FS-06。 |
| 核心处理 | 将 `计划/20260707_FS06_方法服务角色与条件结果读取增强专项_v0.1.md` 状态改为已确认；将 QR-044 改为已确认；登记 `执行 FS-06 S0 当前代码事实扫描` 到 `项目记忆/Codex任务队列.md` 可执行队列。 |
| 结构承载 | `计划/20260707_FS06_方法服务角色与条件结果读取增强专项_v0.1.md`、`计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/待确认问题.md`、`项目记忆/当前状态.md`、`实施记录/20260707_FS06_方法服务角色与条件结果读取增强专项确认_Codex断点清单.md`。 |
| 输出变化 | FS-06-A/C/D 第一轮增强路线已确认；执行窗口可承接 FS-06 S0 当前代码事实扫描；S1 到 S4 仍需另建独立代码实施切片并确认允许文件、禁止文件和验证方式。 |
| 调用路径 | 用户确认 -> FS-06 专项计划状态更新 -> 待确认问题收口 -> 可执行队列登记 -> 计划索引和项目记忆同步。 |
| 验证方式 | 文档一致性检查；`git diff --check`；`tools/check_specs.py` 当前不存在则跳过；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明 FS-06 专项计划已确认并登记 S0 只读扫描；不证明 FS-06 代码已实现，不证明旧方法函数体或旧方法主信息字段已迁移，不证明完整方法查找、候选排序、条件结果对、方法学习或动作执行系统完成，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 86. 本次追加实施：FS-07 确认入队与 FS-08 / FS-09 专项计划生成

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户确认 FS-07 小修口径后继续要求生成下一个计划。 |
| 核心处理 | 将 FS-07 动作动态因果入口增强专项按用户小修口径登记为已确认，并把 `执行 FS-07 S0 当前代码事实扫描` 登记到可执行队列；生成 FS-09 事件日志持久化恢复逻辑迁移包归并表、专项计划和生成断点清单；同步 FS-08 / FS-09 生成状态。 |
| 结构承载 | `计划/20260707_FS07_动作动态因果入口增强专项_v0.1.md`、`实施记录/20260707_FS07_动作动态因果入口逻辑迁移包归并表.md`、`计划/20260707_FS08_缓存统计结构快照增强专项_v0.1.md`、`实施记录/20260707_FS08_缓存统计入口逻辑迁移包归并表.md`、`计划/20260707_FS09_事件日志持久化恢复增强专项_v0.1.md`、`实施记录/20260707_FS09_事件日志持久化恢复逻辑迁移包归并表.md`、`计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/当前状态.md`、`项目记忆/待确认问题.md`。 |
| 输出变化 | FS-07 已确认且只允许执行 S0 当前代码事实扫描；FS-08 和 FS-09 专项计划当时已生成，后续已确认并登记 S0，可执行状态见第 90 条。 |
| 调用路径 | 设计确认窗口 -> FS-07 确认状态同步 -> 可执行队列登记 FS-07 S0 -> FS-09 只读扫描材料归并 -> 生成 FS-09 专项计划 -> 项目记忆和计划索引同步。 |
| 验证方式 | 文档一致性检查；`git diff --check`；`tools/check_specs.py` 如存在则运行；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明 FS-07 计划已确认并登记 S0、FS-08 / FS-09 计划已生成；不证明 FS-07 / FS-08 / FS-09 代码已实现，不证明旧函数体或旧主信息字段已迁移，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 87. 本次追加实施：函数迁移服务级确认规则修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户提出迁移逐函数确认太慢，要求“不用再每个函数确认了，按照服务确认即可”。 |
| 核心处理 | 将 `计划/20260706_函数迁移前置专项_v0.1.md` 从逐函数确认口径修订为服务级确认口径；函数事实继续只读登记和归属服务包，但不再逐个等待用户确认；确认单位改为服务逻辑包、服务专项计划和服务代码实施切片。 |
| 结构承载 | `计划/20260706_函数迁移前置专项_v0.1.md`、`计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/当前状态.md`、`项目记忆/待确认问题.md`、`项目记忆/决策记录.md`、`实施记录/20260707_函数迁移服务级确认规则修订_Codex断点清单.md`。 |
| 输出变化 | 后续旧能力迁移不再逐函数确认；服务包确认后可登记 S0 或生成服务代码实施切片。执行中发现服务包外的新函数事实、新字段、新结构或新依赖时，写入待确认问题。 |
| 调用路径 | 用户调整策略 -> 读取项目规则和函数迁移前置专项 -> 修订服务级确认规则 -> 同步计划索引和项目记忆 -> 新增断点清单。 |
| 验证方式 | 文档一致性检查；`git diff --check`；`tools/check_specs.py` 如存在则运行；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明迁移确认粒度已调整为服务级；不证明旧函数体已迁移，不证明旧主信息字段可回流，不证明可以跳过服务专项计划或代码实施切片，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 88. 本次追加实施：计划目录状态分流

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求将生成的待确认计划放入 `D:\海中鱼巣\计划\待确认计划`，并要求计划确认完成后移回计划目录、已经完成的计划放入 `已完成计划`。 |
| 核心处理 | 将 FS-08、FS-09 待确认专项计划移动到 `计划/待确认计划/`；将已完成或已验证收口的计划切片移动到 `计划/已完成计划/`；补充计划文件目录流转规则。 |
| 结构承载 | `计划/待确认计划/*`、`计划/已完成计划/*`、`计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/*`、`实施记录/20260707_计划目录状态分流_Codex断点清单.md`。 |
| 输出变化 | 根目录 `计划/` 保留总计划、仍在承接中的已确认计划和计划索引；待确认计划和已完成计划按目录分流；相关路径引用已同步更新。 |
| 调用路径 | 用户目录规则 -> 创建计划索引锁 -> 移动计划文件 -> 同步路径引用 -> 补目录流转规则 -> 新增断点清单 -> 验证并同步云端。 |
| 验证方式 | `git diff --check`；`tools/check_specs.py` 如存在则运行；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明计划文件目录状态已分流；不证明 FS-08 / FS-09 已确认，不证明任何新增代码实施、旧函数体迁移、字段落地或外设 / SQL / 控制面板接入。 |

## 89. 本次追加实施：海中鱼巣本地技能转换

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求将 `D:\鱼巢` 的技能转换成 `D:\海中鱼巣` 的技能。 |
| 核心处理 | 基于旧 `D:\鱼巢\.codex\skills` 工作流，创建 9 个海中鱼巣专用本地技能；将路径、规则入口、计划索引、双路执行、计划目录流转、服务级迁移确认和禁止扩大声明改为当前仓库口径。 |
| 结构承载 | `.codex/skills/hai-zhong-yu-chao-*`、`.codex/skills/hai-zhong-digital-life-reasoning`、`实施记录/20260707_海中鱼巣本地技能转换_Codex断点清单.md`、`项目记忆/当前状态.md`、`项目记忆/实施记录.md`。 |
| 输出变化 | 当前仓库拥有执行、计划、当前事实扫描、实施前审查、流程图、根因追踪、讨论、理论草稿和数字生命推理 9 个海中鱼巣专用技能。 |
| 调用路径 | 用户要求转换技能 -> 读取旧技能和当前项目规则 -> 新建海中鱼巣技能文件 -> 轻量校验 frontmatter 与 agents 元数据 -> 写入断点清单和项目记忆。 |
| 验证方式 | 官方 `quick_validate.py` 因当前 Python 缺少 `yaml` 模块无法运行；已使用不依赖 PyYAML 的轻量校验确认 9 个技能目录、`SKILL.md` frontmatter、名称一致性、description 和 `agents/openai.yaml` 存在。后续收尾执行 `git diff --check`。 |
| 禁止扩大声明 | 本次只证明技能治理已完成；不证明任何 C++ 代码实现、旧函数迁移、旧字段落地、计划确认、执行队列消费、SQL / 控制面板 / D455 / 体素 / 外设接入。 |

## 90. 本次追加实施：FS-08 / FS-09 专项计划确认入队

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求确认两个待确认计划。 |
| 核心处理 | 将 `计划/待确认计划/20260707_FS08_缓存统计结构快照增强专项_v0.1.md` 和 `计划/待确认计划/20260707_FS09_事件日志持久化恢复增强专项_v0.1.md` 移回 `计划/`；状态改为已确认；QR-049、QR-050 改为已确认；登记 `执行 FS-08 S0 当前代码事实扫描` 和 `执行 FS-09 S0 当前代码事实扫描` 到可执行队列。 |
| 结构承载 | `计划/20260707_FS08_缓存统计结构快照增强专项_v0.1.md`、`计划/20260707_FS09_事件日志持久化恢复增强专项_v0.1.md`、`计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/当前状态.md`、`项目记忆/待确认问题.md`、`实施记录/20260707_FS08_FS09_专项计划确认_Codex断点清单.md`。 |
| 输出变化 | FS-08-E 缓存统计结构快照增强路线已确认；FS-09-A / FS-09-D 事件日志持久化恢复增强路线已确认；执行窗口可按队列执行 FS-08 S0 和 FS-09 S0 只读扫描。 |
| 调用路径 | 用户确认 -> 计划文件移回根计划目录 -> 状态更新 -> 待确认问题收口 -> 可执行队列登记 -> 计划索引和项目记忆同步。 |
| 验证方式 | 文档一致性检查；`git diff --check`；`tools/check_specs.py` 如存在则运行；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明 FS-08 / FS-09 专项计划已确认并登记 S0；不证明 FS-08 / FS-09 代码已实现，不证明缓存统计或事件日志持久化恢复系统完成，不迁移旧函数体或旧主信息字段，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 91. 本次追加实施：FS-10 显示层只读视图增强专项计划生成

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求按照新规则持续生成计划，直到遇到问题。 |
| 核心处理 | 基于 `实施记录/20260706_FS10_显示层只读候选只读扫描记录.md` 和 FS-X 排除项，生成 FS-10 显示层只读候选逻辑迁移包归并表；生成 `计划/20260707_FS10_显示层只读视图增强专项_v0.1.md`；登记 QR-051 为待确认。 |
| 结构承载 | `实施记录/20260707_FS10_显示层只读候选逻辑迁移包归并表.md`、`计划/20260707_FS10_显示层只读视图增强专项_v0.1.md`、`实施记录/20260707_FS10_显示层只读视图增强专项生成_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/当前状态.md`、`项目记忆/待确认问题.md`。 |
| 输出变化 | FS-10 专项计划已生成但仍待确认；未确认前不得登记 FS-10 S0，不得进入 C++ 实施。 |
| 调用路径 | 设计计划队列 DQ-07 -> FS-10 只读扫描记录 -> 逻辑迁移包归并表 -> 待确认专项计划 -> 计划索引和项目记忆同步。 |
| 验证方式 | 文档一致性检查；`git diff --check`；`tools/check_specs.py` 如存在则运行；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明 FS-10 逻辑迁移包归并表和待确认专项计划已生成；不证明显示层只读视图代码已实现，不证明旧控制面板、SQL、WebView2、窗口、截图、线程、D455、体素或外设已迁移。 |

## 92. 本次追加实施：FS-03 特征值系统第二轮专项计划生成

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求按照新规则持续生成计划，直到遇到问题。 |
| 核心处理 | 基于已确认 FS-03 逻辑迁移包归并表、特征服务封口第一轮边界和非权威缓存 / 主信息字段分流材料，生成 `计划/20260707_FS03_特征值系统第二轮专项_v0.1.md`；登记 QR-052 为待确认。 |
| 结构承载 | `计划/20260707_FS03_特征值系统第二轮专项_v0.1.md`、`实施记录/20260707_FS03_特征值系统第二轮专项生成_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/当前状态.md`、`项目记忆/待确认问题.md`。 |
| 输出变化 | FS-03 第二轮专项计划已生成但仍待确认；未确认前不得登记 FS-03 第二轮 S0，不得进入 C++ 实施。 |
| 调用路径 | 设计计划队列 DQ-08 -> 已确认 FS-03 归并表 -> FS-03 第二轮待确认专项计划 -> 计划索引和项目记忆同步。 |
| 验证方式 | 文档一致性检查；`git diff --check`；`tools/check_specs.py` 如存在则运行；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明 FS-03 第二轮专项待确认计划已生成；不证明 Vec 原始值、哈希、命中统计、稳态、值域、序列化恢复、旧特征值字段、控制面板 / SQL / D455 / 体素 / 外设已实现或迁移。 |

## 93. 本次追加实施：FS-02 动态聚合因果模板删除实例状态后续专项计划生成

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求按照新规则持续生成计划，直到遇到问题。 |
| 核心处理 | 基于已确认 FS-02 逻辑迁移包归并表、服务增强详细设计和第一轮代码实施切片的暂缓边界，生成 `计划/20260707_FS02_动态聚合因果模板删除实例状态后续专项_v0.1.md`；登记 QR-053 为待确认。 |
| 结构承载 | `计划/20260707_FS02_动态聚合因果模板删除实例状态后续专项_v0.1.md`、`实施记录/20260707_FS02_动态聚合因果模板删除实例状态后续专项生成_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/当前状态.md`、`项目记忆/待确认问题.md`。 |
| 输出变化 | FS-02 后续专项待确认计划已生成；未确认前不得登记 FS-02 后续专项 S0，不得进入 C++ 实施。 |
| 调用路径 | 设计计划队列 DQ-09 -> 已确认 FS-02 后续包边界 -> FS-02 后续专项待确认计划 -> 计划索引和项目记忆同步。 |
| 验证方式 | 文档一致性检查；`git diff --check`；`tools/check_specs.py` 如存在则运行；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明 FS-02 后续专项待确认计划已生成；不证明动态聚合、运动基元候选、因果模板、因果任务化、查询候选或删除实例状态写入口已实现；不迁移旧函数体或旧主信息字段，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 94. 本次追加实施：基础信息服务分层完整增强专项计划生成

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求按照新规则持续生成计划，直到遇到问题。 |
| 核心处理 | 基于基础信息服务分层规范和存在、场景、状态、动态、二次特征、世界聚合服务详细设计的当前缺口，生成 `计划/20260707_基础信息服务分层完整增强专项_v0.1.md`；登记 QR-054 为待确认。 |
| 结构承载 | `计划/20260707_基础信息服务分层完整增强专项_v0.1.md`、`实施记录/20260707_基础信息服务分层完整增强专项生成_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/当前状态.md`、`项目记忆/待确认问题.md`。 |
| 输出变化 | 基础信息服务分层完整增强专项待确认计划已生成；未确认前不得登记 S0，不得进入 C++ 实施。 |
| 调用路径 | 设计计划队列 DQ-10 -> 基础信息服务分层规范和详细设计 -> 待确认专项计划 -> 计划索引和项目记忆同步。 |
| 验证方式 | 文档一致性检查；`git diff --check`；`tools/check_specs.py` 如存在则运行；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明基础信息服务分层完整增强专项待确认计划已生成；不证明基础信息服务分层完整完成，不证明世界服务、抽象状态、抽象动态或实例生命周期治理已实现；不迁移旧函数体或旧主信息字段，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 95. 本次追加实施：DQ-11 事件日志持久化恢复专项去重

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求按照新规则持续生成计划，直到遇到问题。 |
| 核心处理 | 检查设计计划队列 DQ-11，确认其已被 `计划/20260707_FS09_事件日志持久化恢复增强专项_v0.1.md` 覆盖，且该计划已确认并登记 FS-09 S0 到可执行队列，因此不重复生成第二份事件日志持久化恢复计划。 |
| 结构承载 | `项目记忆/Codex任务队列.md`、`计划/计划索引.md`、`项目记忆/实施记录.md`。 |
| 输出变化 | DQ-11 状态改为已由 FS-09 覆盖；下一项转入 DQ-12 旧能力迁移专项检查。 |
| 调用路径 | 设计计划队列 DQ-11 -> 已确认 FS-09 专项计划 -> 去重登记 -> 继续 DQ-12。 |
| 验证方式 | 文档一致性检查；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明 DQ-11 计划生成需求已被 FS-09 覆盖；不证明事件日志持久化恢复代码已实现，不证明 S0 已执行，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 96. 本次追加实施：旧能力迁移服务级排期与门禁专项计划生成

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求按照新规则持续生成计划，直到遇到问题。 |
| 核心处理 | 基于函数迁移前置专项、逐函数只读扫描清单和服务级确认规则，生成 `计划/20260707_旧能力迁移服务级排期与门禁专项_v0.1.md`；登记 QR-055 为待确认。 |
| 结构承载 | `计划/20260707_旧能力迁移服务级排期与门禁专项_v0.1.md`、`实施记录/20260707_旧能力迁移服务级排期与门禁专项生成_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/当前状态.md`、`项目记忆/待确认问题.md`。 |
| 输出变化 | 旧能力迁移服务级排期与门禁专项待确认计划已生成；未确认前不得登记 S0，不得进入 C++ 实施。 |
| 调用路径 | 设计计划队列 DQ-12 -> 函数迁移前置专项服务级确认规则 -> 待确认专项计划 -> 计划索引和项目记忆同步。 |
| 验证方式 | 文档一致性检查；`git diff --check`；`tools/check_specs.py` 如存在则运行；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明旧能力迁移服务级排期与门禁专项待确认计划已生成；不证明旧能力、旧函数体或旧主信息字段已迁移；不证明任何未确认代码切片可执行，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 97. 本次追加实施：DQ-13 外部 / 显示后续专项阻塞登记

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求按照新规则持续生成计划，直到遇到问题。 |
| 核心处理 | 检查设计计划队列 DQ-13，确认控制面板、SQL、D455、体素和外设仍属暂缓 / 排除本轮，当前缺少已确认规范、详细设计和计划依据，因此不生成实施计划。 |
| 结构承载 | `项目记忆/Codex任务队列.md`、`计划/计划索引.md`、`项目记忆/实施记录.md`。 |
| 输出变化 | DQ-13 登记为阻塞 / 暂缓 / 排除本轮；连续生成计划流程在此停止。 |
| 调用路径 | 设计计划队列 DQ-13 -> 排除项规则复核 -> 阻塞登记。 |
| 验证方式 | 文档一致性检查；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明 DQ-13 当前没有可生成实施计划的依据；不证明外部 / 显示后续能力已设计或可实施，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 98. 本次追加实施：FS-07 / FS-08 / FS-09 S0 当前代码事实扫描

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求执行当前可执行队列；`项目记忆/Codex任务队列.md` 中 FS-07、FS-08、FS-09 S0 当前代码事实扫描为待执行 / 可执行队列。 |
| 核心处理 | 按已确认专项计划只读扫描动作入口 / 动态 / 因果入口、缓存统计结构快照入口、事件日志 / 核心仓库持久化恢复基础入口；记录当前代码事实、缺口和后续确认门禁。 |
| 结构承载 | `实施记录/20260707_FS07_动作动态因果入口增强S0当前代码事实扫描_Codex断点清单.md`、`实施记录/20260707_FS08_缓存统计结构快照增强S0当前代码事实扫描_Codex断点清单.md`、`实施记录/20260707_FS09_事件日志持久化恢复增强S0当前代码事实扫描_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/当前状态.md`、`项目记忆/待确认问题.md`、`项目记忆/验收记录.md`。 |
| 输出变化 | FS-07 / FS-08 / FS-09 S0 均从可执行队列待执行收口为已完成；当前可执行队列暂无新的待执行项。 |
| 调用路径 | 执行队列 -> 已确认 FS-07 / FS-08 / FS-09 专项计划 -> 当前代码事实扫描 -> S0 断点清单 -> 项目记忆和计划索引同步。 |
| 验证方式 | `rg` 当前代码事实扫描、缺失文件检查、`git diff --check`；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明 S0 当前代码事实扫描和文档收口完成；不证明 FS-07 / FS-08 / FS-09 代码增强已实现，不迁移旧函数体或旧主信息字段，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 99. 本次追加实施：FS-07 动作动态因果入口增强专项复核确认

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求修正 `20260707_FS07_动作动态因果入口增强专项_v0.1.md` 的确认文案错字，并确认其它全部内容。 |
| 核心处理 | 复核 FS-07 专项计划当前状态；确认原确认意见中的“先生成为”应修正为“先生成”，正确文案为“本文件先生成待确认计划”；在计划和断点清单中补充复核确认记录。 |
| 结构承载 | `计划/20260707_FS07_动作动态因果入口增强专项_v0.1.md`、`实施记录/20260707_FS07_动作动态因果入口增强专项生成_Codex断点清单.md`、`项目记忆/实施记录.md`。 |
| 输出变化 | FS-07 专项计划维持已确认状态；本次仅记录错字修正和确认复核，不新增代码实施许可。 |
| 调用路径 | 用户确认 -> FS-07 专项计划复核 -> 断点清单补记 -> 项目记忆实施记录补记。 |
| 验证方式 | `git diff --check`；`tools/check_specs.py` 如存在则运行；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明 FS-07 专项文案复核确认完成；不证明 FS-07 S1-S4 已确认，不证明动作入口、动作动态或因果系统代码已实现，不迁移旧函数体或旧主信息字段，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 100. 本次追加实施：待确认计划全部确认入队

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求“待确认计划全部确认”。 |
| 核心处理 | 将 `计划/待确认计划/` 下 5 个计划移回 `计划/`；确认 FS-10 显示层只读视图增强专项、FS-03 特征值系统第二轮专项、FS-02 动态聚合因果模板删除实例状态后续专项、基础信息服务分层完整增强专项、旧能力迁移服务级排期与门禁专项；按各计划确认后入队规则登记对应 S0。 |
| 结构承载 | `计划/20260707_FS10_显示层只读视图增强专项_v0.1.md`、`计划/20260707_FS03_特征值系统第二轮专项_v0.1.md`、`计划/20260707_FS02_动态聚合因果模板删除实例状态后续专项_v0.1.md`、`计划/20260707_基础信息服务分层完整增强专项_v0.1.md`、`计划/20260707_旧能力迁移服务级排期与门禁专项_v0.1.md`、`计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/当前状态.md`、`项目记忆/待确认问题.md`。 |
| 输出变化 | 5 个待确认计划均已确认；`计划/待确认计划/` 已清空；可执行队列新增 FS-10 S0、FS-03 第二轮 S0、FS-02 后续专项 S0、基础信息服务分层完整增强 S0、旧能力迁移服务包状态复核 S0。 |
| 调用路径 | 用户确认 -> 待确认计划移回计划目录 -> 状态改为已确认 -> QR-051 到 QR-055 收口 -> DQ-07 到 DQ-10、DQ-12 改为已确认可执行 S0 -> 可执行队列登记。 |
| 验证方式 | `git diff --check`；`tools/check_specs.py` 如存在则运行；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明计划已确认并登记 S0；不证明任何 S0 已执行，不证明 C++ 已实现，不迁移旧函数体或旧主信息字段，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 101. 本次追加实施：FS-10、FS-03 第二轮、FS-02 后续、基础信息完整增强和旧能力迁移排期 S0 扫描

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求执行当前可执行队列；`项目记忆/Codex任务队列.md` 中 #100 到 #104 为待执行 / 可执行队列。 |
| 核心处理 | 按已确认计划只读扫描 FS-10 显示层只读视图增强、FS-03 特征值系统第二轮、FS-02 动态聚合因果模板删除实例状态后续、基础信息服务分层完整增强，并复核旧能力迁移服务级排期状态。 |
| 结构承载 | `实施记录/20260707_FS10_显示层只读视图增强S0当前代码事实扫描_Codex断点清单.md`、`实施记录/20260707_FS03_特征值系统第二轮S0当前代码事实扫描_Codex断点清单.md`、`实施记录/20260707_FS02_动态聚合因果模板删除实例状态后续S0当前代码事实扫描_Codex断点清单.md`、`实施记录/20260707_基础信息服务分层完整增强S0当前代码事实扫描_Codex断点清单.md`、`实施记录/20260707_旧能力迁移服务级排期S0状态复核_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/当前状态.md`、`项目记忆/待确认问题.md`、`项目记忆/验收记录.md`。 |
| 输出变化 | #100 到 #104 均从可执行队列待执行收口为已完成；当前可执行队列清空；新增 QR-058 到 QR-062 作为后续设计 / 切片确认门禁。 |
| 调用路径 | 执行队列 -> 已确认专项计划 -> 当前代码事实扫描 / 服务包状态复核 -> S0 断点清单 -> 项目记忆和计划索引同步。 |
| 验证方式 | `rg` 当前代码事实扫描、文档状态复核、`git diff --check`；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明 S0 当前代码事实扫描和状态复核完成；不证明显示层只读视图、Vec / 哈希 / 值域 / 稳态 / 序列化、动态聚合、因果模板、删除实例状态、基础信息完整增强或旧能力迁移已实现；不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 101. 本次追加实施：下一批待确认计划生成

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求继续制定下一批计划。 |
| 核心处理 | 基于已完成 S0 当前代码事实扫描的 FS-04 至 FS-09 生成下一批待确认计划：FS-04 至 FS-08 生成 S1-S4 代码实施切片，FS-09 先生成事件日志持久化恢复详细设计专项和仓库快照格式与恢复拒绝矩阵专项。 |
| 结构承载 | `计划/20260707_FS04_需求服务目标状态与任务承接材料S1-S4代码实施切片_v0.1.md`、`计划/20260707_FS05_任务服务承接材料与生命周期S1-S4代码实施切片_v0.1.md`、`计划/20260707_FS06_方法服务角色与条件结果读取S1-S4代码实施切片_v0.1.md`、`计划/20260707_FS07_动作动态因果入口增强S1-S4代码实施切片_v0.1.md`、`计划/20260707_FS08_缓存统计结构快照增强S1-S4代码实施切片_v0.1.md`、`计划/20260707_FS09_事件日志持久化恢复详细设计专项_v0.1.md`、`计划/20260707_FS09_仓库快照格式与恢复拒绝矩阵专项_v0.1.md`、`实施记录/20260707_下一批待确认计划生成_Codex断点清单.md`。 |
| 输出变化 | 新增 7 个待确认计划；计划索引、设计计划队列、当前状态和待确认问题同步登记。 |
| 调用路径 | 设计窗口 -> 已完成 S0 断点清单 -> 待确认计划 -> 计划索引和项目记忆同步。 |
| 验证方式 | `git diff --check`；`tools/check_specs.py` 如存在则运行；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明下一批待确认计划已生成；不证明任何代码实施切片已确认或执行，不证明事件日志持久化恢复、仓库快照格式、恢复入口、旧函数体迁移、旧主信息字段迁移或外设 / SQL / 控制面板接入。 |

## 102. 本次追加实施：ChatGPT 同步能力迁移与状态修正

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求迁移鱼巢中用于和 ChatGPT 同步的能力，并在检查代码现状和已执行计划后修正不一致。 |
| 核心处理 | 基于旧鱼巢 `AGENTS.md` 的理论探讨同步规则和旧 `chatgpt-yu-chao-theory` 技能，按海中鱼巣项目记忆、规则总纲、计划索引和服务级迁移边界改写为 `chatgpt-hai-zhong-yu-chao-theory`；补充 AGENTS 同步规则；修正 `项目记忆/当前状态.md` 中 #100-#104 的过期执行状态。 |
| 结构承载 | `.codex/skills/chatgpt-hai-zhong-yu-chao-theory/SKILL.md`、`.codex/skills/chatgpt-hai-zhong-yu-chao-theory/agents/openai.yaml`、`AGENTS.md`、`计划/计划索引.md`、`项目记忆/当前状态.md`、`项目记忆/Codex任务队列.md`、`项目记忆/决策记录.md`、`项目记忆/验收记录.md`、`实施记录/20260707_ChatGPT同步能力迁移_Codex断点清单.md`。 |
| 输出变化 | ChatGPT 侧同步能力已迁移到海中鱼巣技能目录；同步状态入口从旧鱼巢 `docs/current-state.md` / `docs/decision-log.md` 改为海中鱼巣 `项目记忆/当前状态.md` / `项目记忆/决策记录.md`；当前可执行队列继续保持清空。 |
| 调用路径 | 用户修正请求 -> 旧鱼巢 ChatGPT 同步证据 -> 海中鱼巣规则和项目记忆回读 -> 技能治理和文档治理 -> 项目记忆同步。 |
| 验证方式 | 轻量技能校验；`git diff --check`；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明 ChatGPT 理论探讨同步能力已作为技能和协作规则迁移，并修正过期状态；不证明 C++ 能力变化、旧函数体迁移、旧能力迁移、构建通过或运行通过。 |

## 103. 本次追加实施：鱼巢工具与有效规则迁移治理

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求检查和梳理旧 `鱼巢` 项目中有用的工具和有效规则，并迁移到本项目。 |
| 核心处理 | 只读检查旧 `D:\鱼巢\tools`、旧 `AGENTS.md` 和旧实现验收审计规则；按海中鱼巣边界分类为已迁移、待确认、暂缓 / 禁止迁移；新增海中鱼巣版规范治理检查工具和工具规则迁移规范。 |
| 结构承载 | `tools/check_specs.py`、`规范/工具与规则迁移治理规范.md`、`AGENTS.md`、`规范/000_项目规则总纲.md`、`规范/001_规则迁移清单.md`、`计划/计划索引.md`、`项目记忆/当前状态.md`、`项目记忆/Codex任务队列.md`、`项目记忆/决策记录.md`、`项目记忆/验收记录.md`、`实施记录/20260707_鱼巢工具与规则迁移治理_Codex断点清单.md`。 |
| 输出变化 | 已迁移 `tools/check_specs.py` 的规范治理用途；已迁移限载读取、六证据审计、需求治理、构建运行不并发和函数复用检查等规则口径；函数说明生成工具和 debug-stack 只登记为待确认候选；SQL 投影和控制面板相关工具仍暂缓。 |
| 调用路径 | 用户迁移请求 -> 海中鱼巣规则入口回读 -> 旧鱼巢工具 / 规则只读梳理 -> 海中鱼巣规范和工具改写 -> 项目记忆同步。 |
| 验证方式 | `python tools/check_specs.py`、`python -m py_compile tools/check_specs.py`、`git diff --check`；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明工具治理和规则治理迁移完成；不证明旧函数体、旧能力、SQL 投影、控制面板、D455、体素、外设或 C++ 结构已迁移。 |

## 104. 本次追加实施：下一批待确认计划确认入队

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户给出 7 份待确认计划的确认表，其中 FS-04 小修后确认，其余 6 份直接确认。 |
| 核心处理 | 将 FS-04 至 FS-08 S1-S4 代码实施切片、FS-09 事件日志持久化恢复详细设计专项、FS-09 仓库快照格式与恢复拒绝矩阵专项从 `计划/待确认计划/` 移入 `计划/`；按用户意见小修 FS-04 的 FS04-IMP-Q1 / Q2；同步确认其余计划的待确认问题；修正待确认问题重复编号；登记可执行队列 #107 到 #113。 |
| 结构承载 | `计划/20260707_FS04_需求服务目标状态与任务承接材料S1-S4代码实施切片_v0.1.md`、`计划/20260707_FS05_任务服务承接材料与生命周期S1-S4代码实施切片_v0.1.md`、`计划/20260707_FS06_方法服务角色与条件结果读取S1-S4代码实施切片_v0.1.md`、`计划/20260707_FS07_动作动态因果入口增强S1-S4代码实施切片_v0.1.md`、`计划/20260707_FS08_缓存统计结构快照增强S1-S4代码实施切片_v0.1.md`、`计划/20260707_FS09_事件日志持久化恢复详细设计专项_v0.1.md`、`计划/20260707_FS09_仓库快照格式与恢复拒绝矩阵专项_v0.1.md`、`实施记录/20260707_下一批待确认计划确认_Codex断点清单.md`、`项目记忆/Codex任务队列.md`。 |
| 输出变化 | 7 份计划已确认并登记可执行队列；执行窗口可从 #107 到 #113 持续执行。 |
| 调用路径 | 用户确认表 -> 待确认计划小修 / 确认 -> 计划目录流转 -> 计划索引和项目记忆同步 -> 可执行队列登记。 |
| 验证方式 | `git diff --check`；`tools/check_specs.py` 如存在则运行；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明计划确认和入队完成；不证明 FS-04 至 FS-08 代码已实现，不证明事件日志持久化恢复、仓库快照格式或恢复入口已实现；不迁移旧函数体或旧主信息字段，不接控制面板 / SQL / D455 / 体素 / 外设。 |

## 105. 本次追加实施：下一批待确认计划生成与控制面板策略补入

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求继续生成下一批计划，并追加“控制面板用原样迁移，需要优化功能，然后重构”的新策略。 |
| 核心处理 | 基于 QR-058 到 QR-062 对应 S0 扫描结果生成 6 个后续待确认计划；同时新增控制面板原样迁移优化重构专项，将控制面板从长期排除调整为独立专项承接；按用户补充口径新增数据库主信息迁移后重构专项，将数据库重构登记为主信息迁移完成后的后置专项。 |
| 结构承载 | `计划/待确认计划/20260707_FS10_显示层只读视图详细设计与DTO格式计划_v0.1.md`、`计划/待确认计划/20260707_FS03_特征值系统第二轮拆分设计专项_v0.1.md`、`计划/待确认计划/20260707_FS02_动态聚合运动基元候选详细设计专项_v0.1.md`、`计划/待确认计划/20260707_FS02_因果模板查询候选与状态生命周期详细设计专项_v0.1.md`、`计划/待确认计划/20260707_基础信息服务分层完整增强拆分设计专项_v0.1.md`、`计划/待确认计划/20260707_旧能力迁移后续批次排期与门禁复核计划_v0.1.md`、`计划/待确认计划/20260707_控制面板原样迁移优化重构专项_v0.1.md`、`计划/待确认计划/20260707_数据库主信息迁移后重构专项_v0.1.md`、`实施记录/20260707_下一批待确认计划生成_控制面板策略补入_Codex断点清单.md`。 |
| 输出变化 | 新增 8 个待确认计划；当前可执行队列 #107-#113 不变；控制面板迁移策略改为“原样迁移隔离壳 -> 功能优化 -> 重构到只读视图边界”；数据库重构登记为主信息迁移完成后的后置专项。 |
| 调用路径 | 设计窗口 -> 已完成 S0 断点清单 -> 用户控制面板新策略 -> 待确认计划 -> 计划索引和项目记忆同步。 |
| 验证方式 | `git diff --check`；`tools/check_specs.py` 如存在则运行；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明下一批待确认计划已生成；不证明控制面板已迁移、已优化或已重构，不证明数据库已重构，不证明 SQL / ADO / WebView2 / D455 / 体素 / 外设已接入，不证明主信息迁移已完成。 |

## 106. 本次追加实施：FS-04 需求服务目标状态与任务承接材料 S1-S4 代码实施

| 项 | 内容 |
| --- | --- |
| 输入入口 | `项目记忆/Codex任务队列.md` #107；`计划/20260707_FS04_需求服务目标状态与任务承接材料S1-S4代码实施切片_v0.1.md`。 |
| 核心处理 | 新增需求主体、目标宿主、需求场景和统一需求承接材料读取入口；任务服务按需求创建任务前经需求服务读取承接材料；缺目标状态、主体、目标宿主或场景时拒绝创建任务承接壳；需求结算摘要仍不作为权威事实。 |
| 结构承载 | `海中鱼巣/领域/需求服务.h`、`海中鱼巣/领域/任务服务.h`、`海中鱼巣/入口.cpp`、`计划/已完成计划/20260707_FS04_需求服务目标状态与任务承接材料S1-S4代码实施切片_v0.1.md`、`实施记录/20260707_FS04_需求服务目标状态与任务承接材料S1-S4代码实施_Codex断点清单.md`。 |
| 输出变化 | 默认入口新增 `FS-04 需求承接材料读取`、`FS-04 需求承接材料拒绝`、`FS-04 需求结算摘要边界` 三项输出；#107 标记为已完成并验证；下一步转入 #108。 |
| 调用路径 | 执行窗口 -> 可执行队列 #107 -> FS-04 已确认代码实施切片 -> C++ 允许文件 -> 默认入口验证 -> 计划索引和项目记忆同步。 |
| 验证方式 | `msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m` 通过，0 警告 0 错误；`.\x64\Debug\海中鱼巣.exe` 退出码 0；`git diff --check` 通过；`rg -n -F "特征值服务" 海中鱼巣/领域/需求服务.h 海中鱼巣/领域/任务服务.h 海中鱼巣/领域/方法服务.h` 无命中；`python .\tools\check_specs.py` 未发现问题。 |
| 禁止扩大声明 | 本次只证明 FS-04 第一轮需求承接材料读取和拒绝边界已实现并验证；不证明完整需求树、任务筹办、方法候选、完整需求结算系统或旧需求能力迁移完成。 |

## 107. 本次追加实施：控制面板和数据库撤出迁移计划并生成后续门禁

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求删除 3 个待确认计划，并修订迁移计划：控制面板、数据库后续根据指令重建，不参与迁移，然后制定下一批计划。 |
| 核心处理 | 删除 FS-10 DTO、控制面板原样迁移优化重构、数据库主信息迁移后重构 3 个待确认计划；将 QR-070、QR-076、QR-077 和 DQ-21、DQ-27、DQ-28 改为已撤销；同步计划索引、当前状态、决策记录和规则迁移清单；新增执行队列消化后续计划生成门禁。 |
| 结构承载 | `计划/计划索引.md`、`规范/001_规则迁移清单.md`、`项目记忆/Codex任务队列.md`、`项目记忆/当前状态.md`、`项目记忆/待确认问题.md`、`项目记忆/决策记录.md`、`计划/待确认计划/20260707_执行队列消化后续计划生成门禁_v0.1.md`、`实施记录/20260707_控制面板数据库撤出迁移计划_Codex断点清单.md`。 |
| 输出变化 | 控制面板和数据库不再作为迁移计划对象；后续只按用户明确指令重建；当前待确认计划改为执行队列消化后续计划生成门禁。 |
| 调用路径 | 用户指令 -> 删除待确认计划 -> 修订迁移口径 -> 项目记忆同步 -> 新增后续门禁计划。 |
| 验证方式 | `git diff --check`；`tools/check_specs.py` 如存在则运行；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明文档治理和计划门禁已更新；不证明控制面板、数据库、显示层 DTO、SQL / ADO、WebView2 或任何 C++ 能力已实现。 |

## 108. 本次追加实施：执行队列消化后续计划生成门禁确认

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户确认《执行队列消化后续计划生成门禁 v0.1》。 |
| 核心处理 | 将门禁计划从 `计划/待确认计划/` 移入 `计划/`，状态改为已确认 / 已登记可执行队列；登记可执行队列 #119；同步 QR-078、DQ-29、计划索引、当前状态和决策记录。 |
| 结构承载 | `计划/已完成计划/20260707_执行队列消化后续计划生成门禁_v0.1.md`、`计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/待确认问题.md`、`项目记忆/当前状态.md`、`项目记忆/决策记录.md`、`实施记录/20260707_执行队列消化后续计划生成门禁确认_Codex断点清单.md`。 |
| 输出变化 | #119 成为可执行队列项；当前无待确认计划；当时口径为等待 #108 到 #118 的实际执行结果或真实阻塞记录，当前已由第 110 节修订为滚动循环。 |
| 调用路径 | 用户确认 -> 待确认计划流转 -> 可执行队列登记 -> 项目记忆和计划索引同步。 |
| 验证方式 | `git diff --check`；`tools/check_specs.py` 如存在则运行；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明门禁计划已确认并入队；不证明 #108 到 #118 已完成，不证明下一批代码实施切片已确认，不证明旧能力迁移完成。 |

## 109. 本次追加实施：FS-05 任务服务承接材料与生命周期 S1-S4 代码实施

| 项 | 内容 |
| --- | --- |
| 输入入口 | `项目记忆/Codex任务队列.md` #108；`计划/20260707_FS05_任务服务承接材料与生命周期S1-S4代码实施切片_v0.1.md`。 |
| 核心处理 | 新增统一任务承接材料读取入口；新增任务实际结果状态记录 / 读取入口；新增记录任务完成状态入口并要求完成前必须已有实际结果状态；新增任务选择方法和读取选择方法入口，继续使用任务服务封装的引用关系，不实现候选集合或完整状态机。 |
| 结构承载 | `海中鱼巣/领域/任务服务.h`、`海中鱼巣/入口.cpp`、`计划/已完成计划/20260707_FS05_任务服务承接材料与生命周期S1-S4代码实施切片_v0.1.md`、`实施记录/20260707_FS05_任务服务承接材料与生命周期S1-S4代码实施_Codex断点清单.md`。 |
| 输出变化 | 默认入口新增 `FS-05 任务承接材料复核`、`FS-05 生命周期读取`、`FS-05 实际结果状态`、`FS-05 任务方法关系`、`FS-05 完成前置拒绝` 五项输出；#108 标记为已完成并验证；下一步转入 #109。 |
| 调用路径 | 执行窗口 -> 可执行队列 #108 -> FS-05 已确认代码实施切片 -> C++ 允许文件 -> 默认入口验证 -> 计划索引和项目记忆同步。 |
| 验证方式 | `msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m` 通过，0 警告 0 错误；`.\x64\Debug\海中鱼巣.exe` 退出码 0；`git diff --check` 通过；`rg -n -F "特征值服务" 海中鱼巣/领域/任务服务.h 海中鱼巣/领域/需求服务.h 海中鱼巣/领域/方法服务.h` 无命中；`python .\tools\check_specs.py` 未发现问题。 |
| 禁止扩大声明 | 本次只证明 FS-05 第一轮任务承接材料、生命周期读取、实际结果状态和任务方法关系边界已实现并验证；不证明完整任务系统、完整任务状态机、任务筹办、任务执行桥、运行统计、治理协议或旧任务能力迁移完成。 |

## 110. 本次追加实施：执行队列滚动循环规则修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户指出整批等待无法形成循环，要求先按“任一队列项完成即可滚动生成后续计划”的口径修改规则。 |
| 核心处理 | 将执行队列后续计划生成门禁从“等待 #108 到 #118 全部完成或阻塞”修订为滚动循环：单个可执行项完成并有正式产物后，设计窗口可基于该项产物生成后续计划；跨项综合计划只等待明确依赖项。 |
| 结构承载 | `AGENTS.md`、`计划/已完成计划/20260707_执行队列消化后续计划生成门禁_v0.1.md`、`计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/待确认问题.md`、`项目记忆/当前状态.md`、`项目记忆/决策记录.md`、`实施记录/20260707_执行队列滚动循环规则修订_Codex断点清单.md`。 |
| 输出变化 | #119 保持文档治理队列项，但语义改为滚动复核已完成队列项；下一批计划生成不再默认等待整批可执行队列清空。 |
| 调用路径 | 用户口径 -> AGENTS 协作规则 -> 门禁计划 -> 计划索引 -> 项目记忆 -> 断点清单。 |
| 验证方式 | `git diff --check`；`tools/check_specs.py` 如存在则运行；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明规则和文档门禁已修订；不证明 #109 到 #119 已执行，不证明下一批计划已生成，不证明旧能力迁移完成。 |

## 111. 本次追加实施：基于 FS-04 / FS-05 完成产物生成下一批待确认计划

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求梳理并生成下一批计划；滚动循环规则允许基于已完成队列项生成后续计划。 |
| 核心处理 | 读取 #107 FS-04 和 #108 FS-05 已完成实施记录、已完成代码实施切片和当前队列状态，只基于这两个完成项生成 3 份待确认计划。 |
| 结构承载 | `计划/待确认计划/20260707_FS04_需求树后续结构拆分设计专项_v0.1.md`、`计划/待确认计划/20260707_FS05_任务状态机筹办执行桥拆分设计专项_v0.1.md`、`计划/待确认计划/20260707_FS04_FS05_需求任务承接闭环联动验收计划_v0.1.md`、`实施记录/20260707_FS04_FS05滚动下一批待确认计划生成_Codex断点清单.md`。 |
| 输出变化 | 下一批待确认计划从无变为 3 份；QR-079 到 QR-081 和 DQ-30 到 DQ-32 已登记；未登记可执行队列。 |
| 调用路径 | 已完成 #107 / #108 产物 -> 滚动循环规则 -> 待确认计划 -> 计划索引和项目记忆同步。 |
| 验证方式 | `git diff --check`；`tools/check_specs.py` 如存在则运行；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明下一批待确认计划已生成；不证明计划已确认、C++ 已实施、旧函数已迁移或系统级闭环完成。 |

## 112. 本次追加实施：FS-06 方法服务角色与条件结果读取 S1-S4 代码实施

| 项 | 内容 |
| --- | --- |
| 输入入口 | `项目记忆/Codex任务队列.md` #109；`计划/20260707_FS06_方法服务角色与条件结果读取S1-S4代码实施切片_v0.1.md`。 |
| 核心处理 | 在方法服务中新增方法节点身份复核、方法首复核、条件归属方法首读取和结果归属方法首读取；在默认入口增加方法身份、条件 / 结果节点、条件 / 结果场景、虚拟存在唯一读取和拒绝路径复核。 |
| 结构承载 | `海中鱼巣/领域/方法服务.h`、`海中鱼巣/入口.cpp`、`计划/已完成计划/20260707_FS06_方法服务角色与条件结果读取S1-S4代码实施切片_v0.1.md`、`实施记录/20260707_FS06_方法服务角色与条件结果读取S1-S4代码实施_Codex断点清单.md`。 |
| 输出变化 | 默认入口新增 `FS-06 方法身份复核`、`FS-06 条件结果节点读取`、`FS-06 条件结果场景读取`、`FS-06 虚拟存在读取`、`FS-06 拒绝路径和依赖复核` 五项输出；#109 标记为已完成并验证；下一步转入 #110。 |
| 调用路径 | 执行窗口 -> 可执行队列 #109 -> FS-06 已确认代码实施切片 -> C++ 允许文件 -> 默认入口验证 -> 计划索引和项目记忆同步。 |
| 验证方式 | `msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m` 通过，0 警告 0 错误；`.\x64\Debug\海中鱼巣.exe` 退出码 0；`git diff --check` 通过；`rg -n -F "特征值服务" 海中鱼巣/领域/方法服务.h 海中鱼巣/领域/任务服务.h 海中鱼巣/领域/需求服务.h` 无命中；`python .\tools\check_specs.py` 未发现问题。 |
| 禁止扩大声明 | 本次只证明 FS-06 第一轮方法身份、角色、条件 / 结果节点、场景和虚拟存在读取边界已实现并验证；不证明完整方法查找、候选召回、条件结果对、方法学习、动作执行系统或旧方法能力迁移完成。 |

## 113. 本次追加实施：FS-07 动作动态因果入口增强 S1-S4 代码实施

| 项 | 内容 |
| --- | --- |
| 输入入口 | `项目记忆/Codex任务队列.md` #110；`计划/20260707_FS07_动作动态因果入口增强S1-S4代码实施切片_v0.1.md`。 |
| 核心处理 | 在方法服务中新增动作入口身份、动作入口状态、动作入口归属方法首、稳定动作键候选和本能动作注册材料复核；在动态服务中新增来源动作入口、动态来源动作和动态证据材料完整性读取；在因果服务中新增轻量准入复核；默认入口增加 FS-07 五项验证输出。 |
| 结构承载 | `海中鱼巣/领域/方法服务.h`、`海中鱼巣/领域/动态服务.h`、`海中鱼巣/领域/因果服务.h`、`海中鱼巣/入口.cpp`、`计划/已完成计划/20260707_FS07_动作动态因果入口增强S1-S4代码实施切片_v0.1.md`、`实施记录/20260707_FS07_动作动态因果入口增强S1-S4代码实施_Codex断点清单.md`。 |
| 输出变化 | 默认入口新增 `FS-07 动作入口身份复核`、`FS-07 稳定动作键候选复核`、`FS-07 动作动态证据读取`、`FS-07 本能动作注册材料边界`、`FS-07 因果动作排除边界` 五项输出；#110 标记为已完成并验证；下一步转入 #111。 |
| 调用路径 | 执行窗口 -> 可执行队列 #110 -> FS-07 已确认代码实施切片 -> C++ 允许文件 -> 默认入口验证 -> 计划索引和项目记忆同步。 |
| 验证方式 | `msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m` 通过，0 警告 0 错误；`.\x64\Debug\海中鱼巣.exe` 退出码 0；`git diff --check` 通过；`rg -n "函数指针|std::async|thread|线程|D455|体素|控制面板|SQL|WebView" 海中鱼巣/领域 海中鱼巣/入口.cpp` 只命中既有文件规则注释中的线程禁用口径，未发现本轮新增禁用能力实现入口；`python .\tools\check_specs.py` 未发现问题。 |
| 禁止扩大声明 | 本次只证明 FS-07 第一轮动作入口身份、来源动作准入、动作动态材料和排除边界已实现并验证；不证明完整动作动态系统、因果模板系统、本能动作系统、自我动作实现、任务执行桥、动作执行桥或旧动作能力迁移完成。 |

## 114. 本次追加实施：FS-08 缓存统计结构快照增强 S1-S4 代码实施

| 项 | 内容 |
| --- | --- |
| 输入入口 | `项目记忆/Codex任务队列.md` #111；`计划/20260707_FS08_缓存统计结构快照增强S1-S4代码实施切片_v0.1.md`。 |
| 核心处理 | 在统计服务中新增缓存命名空间有效性、结构统计快照有效性、权威结构统计快照读取和缓存缺失仍不可裁决业务复核；默认入口增加 FS-08 五项验证输出，继续证明清空 / 重建不改变权威结构。 |
| 结构承载 | `海中鱼巣/领域/统计服务.h`、`海中鱼巣/入口.cpp`、`计划/已完成计划/20260707_FS08_缓存统计结构快照增强S1-S4代码实施切片_v0.1.md`、`实施记录/20260707_FS08_缓存统计结构快照增强S1-S4代码实施_Codex断点清单.md`。 |
| 输出变化 | 默认入口新增 `FS-08 缓存命名空间复核`、`FS-08 结构统计快照重建`、`FS-08 缓存清空权威不变`、`FS-08 缓存缺失业务不变`、`FS-08 后续缓存排除边界` 五项输出；#111 标记为已完成并验证；下一步转入 #112。 |
| 调用路径 | 执行窗口 -> 可执行队列 #111 -> FS-08 已确认代码实施切片 -> C++ 允许文件 -> 默认入口验证 -> 计划索引和项目记忆同步。 |
| 验证方式 | `msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m` 通过，0 警告 0 错误；`.\x64\Debug\海中鱼巣.exe` 退出码 0；`git diff --check` 通过；`rg -n "哈希|命中|需求结算摘要|存在观测|二次特征签名|SQL|控制面板|D455|体素|外设" 海中鱼巣/领域/统计服务.h 海中鱼巣/入口.cpp` 仅命中既有枚举名或计划要求中的后续排除词，未发现本轮新增后续缓存能力实现入口；`python .\tools\check_specs.py` 未发现问题。 |
| 禁止扩大声明 | 本次只证明 FS-08 第一轮结构统计运行期快照、清空 / 重建和缓存非权威边界已实现并验证；不证明完整缓存系统、特征值内容哈希、命中观察、Vec 原始值缓存、需求结算摘要刷新、存在观测缓存、二次特征签名缓存或旧缓存能力迁移完成。 |

## 115. 本次追加实施：FS-09 事件日志持久化恢复详细设计专项

| 项 | 内容 |
| --- | --- |
| 输入入口 | `项目记忆/Codex任务队列.md` #112；`计划/20260707_FS09_事件日志持久化恢复详细设计专项_v0.1.md`。 |
| 核心处理 | 生成 `规范/详细设计/事件日志持久化恢复详细设计.md`，明确事件日志定位、结构化事件材料字段、事件来源准入、写入方和读取方、生命周期、日志分类边界、写入失败拒绝、不裁决运行期事实声明、后续 C++ 实施切片入口和验收矩阵。 |
| 结构承载 | `规范/详细设计/事件日志持久化恢复详细设计.md`、`计划/已完成计划/20260707_FS09_事件日志持久化恢复详细设计专项_v0.1.md`、`实施记录/20260707_FS09_事件日志持久化恢复详细设计生成_Codex断点清单.md`。 |
| 输出变化 | #112 标记为已完成；DQ-19 标记为已完成；下一步转入 #113。 |
| 调用路径 | 执行窗口 -> 可执行队列 #112 -> FS-09 已确认文档治理计划 -> 详细设计 -> 计划索引和项目记忆同步。 |
| 验证方式 | `git diff --check` 通过；`python .\tools\check_specs.py` 未发现问题；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明事件日志持久化恢复详细设计已生成；不证明事件日志结构化材料、仓库快照格式、恢复入口或任何 C++ 代码已实现，不证明旧项目日志或旧链表二进制保存恢复已迁移。 |

## 116. 本次追加实施：基于 FS-06 至 FS-09 完成产物生成第二批滚动待确认计划

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求“继续生成”；滚动循环规则允许基于已完成队列项生成后续计划。 |
| 核心处理 | 读取 #109 FS-06、#110 FS-07、#111 FS-08 和 #112 FS-09 已完成产物；生成 4 份待确认计划；登记 QR-082 到 QR-085 和 DQ-33 到 DQ-36。 |
| 结构承载 | `计划/待确认计划/20260707_FS06_方法候选召回与条件结果对拆分设计专项_v0.1.md`、`计划/待确认计划/20260707_FS07_动作执行桥与本能动作治理拆分设计专项_v0.1.md`、`计划/待确认计划/20260707_FS08_非权威缓存二级材料拆分设计专项_v0.1.md`、`计划/待确认计划/20260707_FS09_事件日志详细设计确认后实施门禁计划_v0.1.md`、`实施记录/20260707_FS06_FS09滚动下一批待确认计划生成_Codex断点清单.md`。 |
| 输出变化 | 第二批滚动待确认计划从无变为 4 份；计划索引、当前状态、待确认问题和设计队列已同步；未登记可执行队列。 |
| 调用路径 | 已完成 #109-#112 产物 -> 滚动循环规则 -> 待确认计划 -> 计划索引和项目记忆同步。 |
| 验证方式 | `git diff --check`；`tools/check_specs.py` 如存在则运行；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明第二批滚动待确认计划已生成；不证明计划已确认、C++ 已实施、旧函数已迁移或事件日志详细设计已确认。 |

## 117. 本次追加实施：FS-09 仓库快照格式与恢复拒绝矩阵专项

| 项 | 内容 |
| --- | --- |
| 输入入口 | `项目记忆/Codex任务队列.md` #113；`计划/20260707_FS09_仓库快照格式与恢复拒绝矩阵专项_v0.1.md`。 |
| 核心处理 | 生成 `规范/详细设计/仓库快照格式与恢复拒绝矩阵详细设计.md`，明确快照头、节点段、主信息段、关系段、索引段、关系源 / 目标复核、恢复拒绝矩阵、恢复失败结构不变化要求和后续 C++ 实施切片草案。 |
| 结构承载 | `规范/详细设计/仓库快照格式与恢复拒绝矩阵详细设计.md`、`计划/已完成计划/20260707_FS09_仓库快照格式与恢复拒绝矩阵专项_v0.1.md`、`实施记录/20260707_FS09_仓库快照格式与恢复拒绝矩阵生成_Codex断点清单.md`。 |
| 输出变化 | #113 标记为已完成；DQ-20 标记为已完成；下一步转入 #114 FS-03 特征值系统第二轮拆分设计专项。 |
| 调用路径 | 执行窗口 -> 可执行队列 #113 -> FS-09 已确认文档治理计划 -> 快照格式与恢复拒绝矩阵详细设计 -> 计划索引和项目记忆同步。 |
| 验证方式 | `git diff --check`；`python .\tools\check_specs.py`；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明仓库快照格式与恢复拒绝矩阵详细设计已生成；不证明仓库快照服务、恢复入口、事件日志恢复或任何 C++ 代码已实现，不证明旧链表二进制保存恢复、SQL / ADO / 控制面板 / WebView / D455 / 体素 / 外设已迁移。 |

## 118. 本次追加实施：FS-03 特征值系统第二轮拆分设计专项

| 项 | 内容 |
| --- | --- |
| 输入入口 | `项目记忆/Codex任务队列.md` #114；`计划/20260707_FS03_特征值系统第二轮拆分设计专项_v0.1.md`。 |
| 核心处理 | 生成 Vec 原始值容器、特征值非权威缓存与内容哈希、特征值值域稳态与候选比较三份详细设计，并基于 FS-09 事件日志和仓库快照恢复边界生成特征值序列化恢复边界待确认计划。 |
| 结构承载 | `规范/详细设计/特征值Vec原始值容器详细设计.md`、`规范/详细设计/特征值非权威缓存与内容哈希详细设计.md`、`规范/详细设计/特征值值域稳态与候选比较详细设计.md`、`计划/待确认计划/20260707_FS03_特征值序列化恢复边界专项_v0.1.md`、`计划/已完成计划/20260707_FS03_特征值系统第二轮拆分设计专项_v0.1.md`、`实施记录/20260707_FS03_特征值系统第二轮拆分设计专项生成_Codex断点清单.md`。 |
| 输出变化 | #114 标记为已完成；DQ-22 标记为已完成；新增待确认问题 QR-086；下一步转入 #115 FS-02 动态聚合运动基元候选详细设计专项。 |
| 调用路径 | 执行窗口 -> 可执行队列 #114 -> FS-03 已确认文档治理计划 -> 三份详细设计和一份待确认计划 -> 计划索引和项目记忆同步。 |
| 验证方式 | `git diff --check`；`python .\tools\check_specs.py`；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明 FS-03 第二轮拆分设计产物已生成；不证明 Vec 原始值容器、内容哈希、命中统计、值域、复合比较、稳态、区间升格、序列化恢复或任何 C++ 代码已实现，不证明旧特征值主信息字段已迁移。 |

## 119. 本次追加实施：FS-02 动态聚合运动基元候选详细设计专项

| 项 | 内容 |
| --- | --- |
| 输入入口 | `项目记忆/Codex任务队列.md` #115；`计划/20260707_FS02_动态聚合运动基元候选详细设计专项_v0.1.md`。 |
| 核心处理 | 生成 `规范/详细设计/动态聚合与运动基元候选详细设计.md`，明确候选输入、候选结构、来源动态材料、评分 / 版本边界、入口拒绝矩阵、候选清空不改事实和后续代码切片草案。 |
| 结构承载 | `规范/详细设计/动态聚合与运动基元候选详细设计.md`、`计划/已完成计划/20260707_FS02_动态聚合运动基元候选详细设计专项_v0.1.md`、`实施记录/20260707_FS02_动态聚合运动基元候选详细设计专项生成_Codex断点清单.md`。 |
| 输出变化 | #115 标记为已完成；DQ-23 标记为已完成；下一步转入 #116 FS-02 因果模板查询候选与状态生命周期详细设计专项。 |
| 调用路径 | 执行窗口 -> 可执行队列 #115 -> FS-02 已确认文档治理计划 -> 动态聚合与运动基元候选详细设计 -> 计划索引和项目记忆同步。 |
| 验证方式 | `git diff --check`；`python .\tools\check_specs.py`；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明动态聚合与运动基元候选详细设计已生成；不证明动态聚合算法、运动基元识别算法、方法候选、二次特征候选、因果模板或任何 C++ 代码已实现。 |

## 120. 本次追加实施：FS-02 因果模板查询候选与状态生命周期详细设计专项

| 项 | 内容 |
| --- | --- |
| 输入入口 | `项目记忆/Codex任务队列.md` #116；`计划/20260707_FS02_因果模板查询候选与状态生命周期详细设计专项_v0.1.md`。 |
| 核心处理 | 生成 `规范/详细设计/因果模板与查询候选详细设计.md` 和 `规范/详细设计/实例状态生命周期治理详细设计.md`，明确动态证据集合、来源动作 / 来源方法、目标状态、实际状态、缺失证据、任务化拒绝、实例状态引用阻断、读取拒绝和关系清理边界。 |
| 结构承载 | `规范/详细设计/因果模板与查询候选详细设计.md`、`规范/详细设计/实例状态生命周期治理详细设计.md`、`计划/已完成计划/20260707_FS02_因果模板查询候选与状态生命周期详细设计专项_v0.1.md`、`实施记录/20260707_FS02_因果模板查询候选与状态生命周期详细设计专项生成_Codex断点清单.md`。 |
| 输出变化 | #116 标记为已完成；DQ-24 标记为已完成；QR-087 登记删除实例状态写入口是否允许进入后续独立代码实施切片；下一步转入 #117 基础信息服务分层完整增强拆分设计专项。 |
| 调用路径 | 执行窗口 -> 可执行队列 #116 -> FS-02 已确认文档治理计划 -> 两份详细设计和断点清单 -> 计划索引和项目记忆同步。 |
| 验证方式 | `git diff --check`；`python .\tools\check_specs.py`；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明因果模板与查询候选、实例状态生命周期治理详细设计已生成；不证明因果模板算法、因果查询、任务化、稳定因果结论、删除实例状态写入口或任何 C++ 代码已实现。 |

## 121. 本次追加实施：基础信息服务分层完整增强拆分设计专项

| 项 | 内容 |
| --- | --- |
| 输入入口 | `项目记忆/Codex任务队列.md` #117；`计划/20260707_基础信息服务分层完整增强拆分设计专项_v0.1.md`。 |
| 核心处理 | 生成 `规范/详细设计/世界服务兼容壳与依赖注入治理详细设计.md`、`规范/详细设计/抽象状态动态治理详细设计.md` 和 `规范/详细设计/实例状态动态生命周期治理详细设计.md`，明确 `创建基础信息()` 兼容壳治理、世界服务依赖注入、抽象候选 / 升格 / 衰减 / 淘汰边界，以及实例状态 / 动态生命周期引用阻断和读取拒绝边界。 |
| 结构承载 | `规范/详细设计/世界服务兼容壳与依赖注入治理详细设计.md`、`规范/详细设计/抽象状态动态治理详细设计.md`、`规范/详细设计/实例状态动态生命周期治理详细设计.md`、`计划/已完成计划/20260707_基础信息服务分层完整增强拆分设计专项_v0.1.md`、`实施记录/20260707_基础信息服务分层完整增强拆分设计专项生成_Codex断点清单.md`。 |
| 输出变化 | #117 标记为已完成；DQ-25 标记为已完成；QR-088 登记三份详细设计是否确认并允许生成后续独立代码实施切片候选；下一步转入 #118 旧能力迁移后续批次排期与门禁复核计划。 |
| 调用路径 | 执行窗口 -> 可执行队列 #117 -> 基础信息服务分层完整增强拆分设计专项 -> 三份详细设计和断点清单 -> 计划索引和项目记忆同步。 |
| 验证方式 | `git diff --check`；`python .\tools\check_specs.py`；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明基础信息服务分层完整增强拆分详细设计已生成；不证明基础信息服务分层完整完成、世界服务完整完成、`创建基础信息()` 已移除、抽象治理算法已实现或实例状态 / 动态删除写入口已实现。 |

## 122. 本次追加实施：旧能力迁移后续批次排期与门禁复核

| 项 | 内容 |
| --- | --- |
| 输入入口 | `项目记忆/Codex任务队列.md` #118；`计划/20260707_旧能力迁移后续批次排期与门禁复核计划_v0.1.md`。 |
| 核心处理 | 复核 #107 到 #117 当前完成状态、后续待确认项、控制面板 / 数据库撤销边界，以及当前工作区外部窗口未跟踪的 FS-02 待确认计划文件；本切片不接管这些外部文件。 |
| 结构承载 | `计划/已完成计划/20260707_旧能力迁移后续批次排期与门禁复核计划_v0.1.md`、`实施记录/20260707_旧能力迁移后续批次排期与门禁复核计划生成_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/当前状态.md`、`项目记忆/待确认问题.md`。 |
| 输出变化 | #118 标记为已完成；DQ-26 标记为已完成；下一步转入 #119 执行队列滚动循环计划生成门禁复核；未生成 C++ 实施许可。 |
| 调用路径 | 执行窗口 -> 可执行队列 #118 -> 旧能力迁移后续批次排期与门禁复核计划 -> 断点清单 -> 计划索引和项目记忆同步。 |
| 验证方式 | `git diff --check`；`python .\tools\check_specs.py`；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明旧能力迁移后续批次排期和门禁已复核；不证明旧能力、旧函数体、旧字段、控制面板、数据库、SQL、WebView2、D455、体素、外设或线程已迁移。 |

## 123. 本次追加实施：执行队列滚动循环计划生成门禁复核

| 项 | 内容 |
| --- | --- |
| 输入入口 | `项目记忆/Codex任务队列.md` #119；`计划/20260707_执行队列消化后续计划生成门禁_v0.1.md`。 |
| 核心处理 | 按滚动循环门禁复核已完成 #115 / #116 产物，基于动态聚合运动基元候选详细设计、因果模板与查询候选详细设计和实例状态生命周期治理详细设计生成三份 FS-02 后续代码实施候选待确认计划。 |
| 结构承载 | `计划/待确认计划/20260707_FS02_动态聚合运动基元候选代码实施切片_v0.1.md`、`计划/待确认计划/20260707_FS02_因果模板查询候选代码实施切片_v0.1.md`、`计划/待确认计划/20260707_FS02_实例状态生命周期读取阻断代码实施切片_v0.1.md`、`实施记录/20260707_FS02后续三份代码实施候选计划生成_Codex断点清单.md`、`实施记录/20260707_执行队列滚动循环计划生成门禁复核_Codex断点清单.md`。 |
| 输出变化 | #119 标记为已完成；DQ-29 标记为已完成；DQ-38 到 DQ-40 和 QR-089 到 QR-091 已登记为待确认；下一步转入 #120 FS-04 需求树后续结构拆分设计专项。 |
| 调用路径 | 执行窗口 -> 可执行队列 #119 -> 滚动循环门禁 -> #115 / #116 已完成产物 -> 三份待确认计划 -> 计划索引和项目记忆同步。 |
| 验证方式 | `git diff --check`；`python .\tools\check_specs.py`；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明三份 FS-02 后续代码实施候选计划已生成并等待确认；不证明计划已确认、C++ 已实施、旧函数已迁移、动态聚合算法、因果模板查询或实例状态删除写入口已完成。 |

## 118. 本次追加实施：确认 FS-04 / FS-05 三份滚动计划并登记可执行队列

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户确认 `计划/待确认计划/20260707_FS04_需求树后续结构拆分设计专项_v0.1.md`、`计划/待确认计划/20260707_FS05_任务状态机筹办执行桥拆分设计专项_v0.1.md`、`计划/待确认计划/20260707_FS04_FS05_需求任务承接闭环联动验收计划_v0.1.md`。 |
| 核心处理 | 将三份滚动计划从待确认目录移入 `计划/`，更新状态为已确认，登记可执行队列 #120-#122，并同步计划索引、设计队列、待确认问题和当前状态。 |
| 结构承载 | `计划/20260707_FS04_需求树后续结构拆分设计专项_v0.1.md`、`计划/20260707_FS05_任务状态机筹办执行桥拆分设计专项_v0.1.md`、`计划/20260707_FS04_FS05_需求任务承接闭环联动验收计划_v0.1.md`、`实施记录/20260707_FS04_FS05三份滚动计划确认入队_Codex断点清单.md`。 |
| 输出变化 | QR-079 到 QR-081 标记为已确认；DQ-30 到 DQ-32 标记为已确认并登记 #120-#122；新增执行队列项 #120、#121、#122；未修改 C++。 |
| 调用路径 | 设计确认窗口 -> 用户确认三份待确认计划 -> 计划目录流转 -> 可执行队列登记 -> 执行窗口后续承接。 |
| 验证方式 | `git diff --check`；`python .\tools\check_specs.py`；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明三份计划已确认并入队；不证明详细设计已生成、验收已执行、C++ 已实施、旧函数已迁移或需求 / 任务完整系统完成。 |

## 122. 本次追加实施：生成 FS-02 后续三份代码实施候选待确认计划

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求继续；滚动循环规则允许基于 #115 FS-02 动态聚合运动基元候选详细设计和 #116 FS-02 因果模板查询候选与状态生命周期详细设计产物生成后续候选计划。 |
| 核心处理 | 生成三份 FS-02 后续代码实施候选切片：动态聚合运动基元候选、因果模板查询候选、实例状态生命周期读取阻断；同步计划索引、设计计划队列、待确认问题和当前状态。 |
| 结构承载 | `计划/待确认计划/20260707_FS02_动态聚合运动基元候选代码实施切片_v0.1.md`、`计划/待确认计划/20260707_FS02_因果模板查询候选代码实施切片_v0.1.md`、`计划/待确认计划/20260707_FS02_实例状态生命周期读取阻断代码实施切片_v0.1.md`、`实施记录/20260707_FS02后续三份代码实施候选计划生成_Codex断点清单.md`。 |
| 输出变化 | 新增 DQ-38 到 DQ-40 待确认设计队列项；新增 QR-089 到 QR-091 待确认问题；计划索引新增“第四批滚动待确认计划”；未登记可执行队列。 |
| 调用路径 | 已完成 #115/#116 详细设计产物 -> 设计确认窗口滚动生成候选计划 -> 待确认计划目录 -> 计划索引和项目记忆同步。 |
| 验证方式 | `git diff --check`；`python .\tools\check_specs.py`；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明三份 FS-02 后续代码实施候选计划已生成并等待确认；不证明计划已确认、C++ 已实施、旧函数已迁移、动态聚合算法已实现、因果模板查询已完成或删除实例状态写入口已允许。 |

## 124. 本次追加实施：FS-04 需求树后续结构拆分设计专项

| 项 | 内容 |
| --- | --- |
| 输入入口 | `项目记忆/Codex任务队列.md` #120；`计划/20260707_FS04_需求树后续结构拆分设计专项_v0.1.md`。 |
| 核心处理 | 生成 `规范/详细设计/需求树后续结构详细设计.md`，明确父子需求角色、阻塞权重、逻辑组织需求、获取途径、目标语义、方向签名、枚举目标、方法候选准入和结构化更新指令的结构边界、入口拒绝和后续候选切片草案。 |
| 结构承载 | `规范/详细设计/需求树后续结构详细设计.md`、`计划/已完成计划/20260707_FS04_需求树后续结构拆分设计专项_v0.1.md`、`实施记录/20260707_FS04_需求树后续结构拆分设计_Codex断点清单.md`。 |
| 输出变化 | #120 标记为已完成；DQ-30 标记为已完成；新增 QR-092，等待确认是否基于本详细设计生成 FS-04 后续独立代码实施切片候选；下一步转入 #121 FS-05 任务状态机筹办执行桥拆分设计专项。 |
| 调用路径 | 执行窗口 -> 可执行队列 #120 -> FS-04 已确认文档治理计划 -> 需求树后续结构详细设计 -> 计划索引和项目记忆同步。 |
| 验证方式 | `git diff --check`；`python .\tools\check_specs.py`；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明 FS-04 需求树后续结构详细设计已生成；不证明完整需求树、父子需求运行时更新、方法候选算法、任务筹办、旧需求函数或旧需求主信息字段已迁移。 |

## 125. 本次追加实施：FS-05 任务状态机筹办执行桥拆分设计专项

| 项 | 内容 |
| --- | --- |
| 输入入口 | `项目记忆/Codex任务队列.md` #121；`计划/20260707_FS05_任务状态机筹办执行桥拆分设计专项_v0.1.md`。 |
| 核心处理 | 生成 `规范/详细设计/任务状态机筹办执行桥详细设计.md`，明确任务生命周期状态迁移、筹办回执、执行桥请求材料、运行统计、权重评分、错误码、运行包和治理协议分流边界。 |
| 结构承载 | `规范/详细设计/任务状态机筹办执行桥详细设计.md`、`计划/已完成计划/20260707_FS05_任务状态机筹办执行桥拆分设计专项_v0.1.md`、`实施记录/20260707_FS05_任务状态机筹办执行桥拆分设计_Codex断点清单.md`。 |
| 输出变化 | #121 标记为已完成；DQ-31 标记为已完成；新增 QR-093，等待确认是否基于本详细设计生成 FS-05 后续独立代码实施切片候选；下一步转入 #122 FS-04 / FS-05 需求任务承接闭环联动验收计划。 |
| 调用路径 | 执行窗口 -> 可执行队列 #121 -> FS-05 已确认文档治理计划 -> 任务状态机筹办执行桥详细设计 -> 计划索引和项目记忆同步。 |
| 验证方式 | `git diff --check`；`python .\tools\check_specs.py`；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明 FS-05 任务状态机筹办执行桥详细设计已生成；不证明完整任务状态机、任务筹办、任务执行桥、线程派发、运行统计代码、治理协议代码或旧任务模块能力已迁移。 |

## 126. 本次追加实施：FS-04 / FS-05 需求任务承接闭环联动验收

| 项 | 内容 |
| --- | --- |
| 输入入口 | `项目记忆/Codex任务队列.md` #122；`计划/20260707_FS04_FS05_需求任务承接闭环联动验收计划_v0.1.md`。 |
| 核心处理 | 执行 Debug x64 构建、默认入口运行、FS-04 / FS-05 输出复核、MC-03 / MC-04 / MC-05 / MC-08 / MC-09 输出复核、高级服务特征值服务依赖扫描、文档空白和规范治理检查。 |
| 结构承载 | `实施记录/20260707_FS04_FS05_需求任务承接闭环联动验收_Codex断点清单.md`、`计划/已完成计划/20260707_FS04_FS05_需求任务承接闭环联动验收计划_v0.1.md`。 |
| 输出变化 | #122 标记为已完成；DQ-32 标记为已完成；当前可执行队列清空，后续等待设计确认路线确认 QR-092、QR-093 或其它待确认计划后再入队。 |
| 调用路径 | 执行窗口 -> 可执行队列 #122 -> 构建 / 默认入口 / 依赖扫描 / 文档检查 -> 验收断点清单 -> 计划索引和项目记忆同步。 |
| 验证方式 | `msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m` 通过，0 警告 0 错误；`.\x64\Debug\海中鱼巣.exe` 输出 FS-04、FS-05 和 MC-03 / MC-04 / MC-05 / MC-08 / MC-09 均通过；`rg -n -F "特征值服务" ...` 无命中；`git diff --check` 通过；`python .\tools\check_specs.py` 未发现问题。 |
| 禁止扩大声明 | 本次只证明 FS-04 / FS-05 需求任务承接闭环联动验收通过；不证明完整需求树、完整任务状态机、任务筹办、任务执行桥、方法候选算法、动作入口后续能力或旧能力迁移完成。 |

## 127. 本次追加实施：确认五份滚动待确认计划并登记可执行队列

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户确认 `计划/待确认计划/20260707_FS06_方法候选召回与条件结果对拆分设计专项_v0.1.md`、`计划/待确认计划/20260707_FS07_动作执行桥与本能动作治理拆分设计专项_v0.1.md`、`计划/待确认计划/20260707_FS08_非权威缓存二级材料拆分设计专项_v0.1.md`、`计划/待确认计划/20260707_FS09_事件日志详细设计确认后实施门禁计划_v0.1.md`、`计划/待确认计划/20260707_FS03_特征值序列化恢复边界专项_v0.1.md`。 |
| 核心处理 | 将五份计划状态改为已确认并从 `计划/待确认计划/` 移入 `计划/`；登记可执行队列 #123 到 #127；同步计划索引、待确认问题、当前状态和断点清单。 |
| 结构承载 | `计划/20260707_FS06_方法候选召回与条件结果对拆分设计专项_v0.1.md`、`计划/20260707_FS07_动作执行桥与本能动作治理拆分设计专项_v0.1.md`、`计划/20260707_FS08_非权威缓存二级材料拆分设计专项_v0.1.md`、`计划/20260707_FS09_事件日志详细设计确认后实施门禁计划_v0.1.md`、`计划/20260707_FS03_特征值序列化恢复边界专项_v0.1.md`、`实施记录/20260707_五份滚动待确认计划确认入队_Codex断点清单.md`。 |
| 输出变化 | QR-082 到 QR-086 标记为已确认；DQ-33 到 DQ-37 标记为已确认并登记 #123 到 #127；执行窗口下一步可从 #123 开始继续文档治理执行。 |
| 调用路径 | 设计确认窗口 -> 用户确认五份待确认计划 -> 计划目录流转 -> 可执行队列登记 -> 执行窗口后续承接。 |
| 验证方式 | `git diff --check`；`python .\tools\check_specs.py`；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明五份计划已确认并入队；不证明详细设计已执行生成、C++ 已实施、旧函数已迁移、事件日志详细设计已确认、动作执行桥已实现或特征值序列化恢复已实现。 |

## 128. 本次追加实施：生成 FS-04 / FS-05 / 基础信息服务后续代码实施候选计划

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求继续生成计划；滚动循环规则允许基于 #120、#121 和 #117 已完成详细设计产物生成后续候选计划。 |
| 核心处理 | 生成三份后续代码实施候选切片：FS-04 需求树后续结构、FS-05 任务状态机筹办执行桥、基础信息服务分层完整增强后续；同步计划索引、设计计划队列、待确认问题、当前状态和断点清单。 |
| 结构承载 | `计划/待确认计划/20260707_FS04_需求树后续结构代码实施切片_v0.1.md`、`计划/待确认计划/20260707_FS05_任务状态机筹办执行桥代码实施切片_v0.1.md`、`计划/待确认计划/20260707_基础信息服务分层完整增强后续代码实施切片_v0.1.md`、`实施记录/20260707_FS04_FS05_BASE后续代码实施候选计划生成_Codex断点清单.md`。 |
| 输出变化 | 新增 DQ-41 到 DQ-43 待确认设计队列项；新增 QR-094 到 QR-096 待确认问题；计划索引新增“第五批滚动待确认计划”；未登记可执行队列。 |
| 调用路径 | 已完成详细设计产物 -> 设计确认窗口滚动生成候选计划 -> 待确认计划目录 -> 计划索引和项目记忆同步。 |
| 验证方式 | `git diff --check`；`python .\tools\check_specs.py`；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明三份后续代码实施候选计划已生成并等待确认；不证明计划已确认、C++ 已实施、旧函数已迁移、需求树后续结构已实现、任务状态机或执行桥已实现、基础信息服务分层完整增强已完成。 |

## 129. 本次追加实施：生成服务操作函数矩阵与流程图迁移策略调整计划

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户提出并确认策略口径：主信息已经建好后，优先生成每个服务对各自数据的操作函数，应用逻辑采用流程图迁移。 |
| 核心处理 | 生成 `计划/待确认计划/20260707_服务操作函数矩阵与流程图迁移策略调整计划_v0.1.md`，明确服务操作函数矩阵、流程图迁移模板、批次级代码实施包和现有 6 份待确认小切片的归并处理方式。 |
| 结构承载 | `计划/待确认计划/20260707_服务操作函数矩阵与流程图迁移策略调整计划_v0.1.md`、`实施记录/20260707_服务操作函数矩阵与流程图迁移策略调整计划生成_Codex断点清单.md`、`计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/待确认问题.md`、`项目记忆/当前状态.md`、`项目记忆/决策记录.md`。 |
| 输出变化 | 新增 DQ-44 待确认设计队列项；新增 QR-097 待确认问题；新增 JY-070 决策记录；计划索引新增策略调整待确认计划。 |
| 调用路径 | 用户策略口径 -> 设计确认窗口 -> 待确认计划目录 -> 计划索引和项目记忆同步。 |
| 验证方式 | `git diff --check`；`python .\tools\check_specs.py`；本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明策略调整计划已生成并等待确认；不证明策略计划已确认、服务操作函数矩阵已生成、流程图迁移包已生成、批次级代码实施包已生成、C++ 已实施或旧函数已迁移。 |

## 130. 本次追加实施：迁移路线权力分层修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户修订迁移口径：先生成服务操作函数矩阵；项目功能按旧项目功能流程图抽取、修改优化后在本项目重构；旧逐项迁移结果只作为证据或候选材料。 |
| 核心处理 | 新增迁移路线权力分层规范，明确证据层、服务矩阵层、流程图迁移层和批次级实施包层；将已被 #129 归并的 6 份小切片移出待确认目录并标记证据保留；将相关候选详细设计标记为进入 C++ 前必须经服务矩阵和流程图迁移包重审；同步计划索引、项目记忆、规范治理检查工具和路径引用。 |
| 结构承载 | `规范/迁移路线权力分层规范.md`、`规范/000_项目规则总纲.md`、`规范/001_规则迁移清单.md`、`tools/check_specs.py`、`计划/计划索引.md`、`项目记忆/当前状态.md`、`项目记忆/决策记录.md`、`项目记忆/待确认问题.md`、`项目记忆/验收记录.md`、`实施记录/20260708_迁移路线权力分层修订_Codex断点清单.md`。 |
| 输出变化 | 旧函数事实、旧逻辑迁移包、旧小切片和候选详细设计的执行权被降级为证据或候选；待确认目录只保留真实待确认计划；后续默认按服务操作函数矩阵、应用逻辑流程图迁移包和批次级代码实施包推进。 |
| 调用路径 | 用户新口径 -> 规范修订 -> 计划索引和项目记忆同步 -> 已归并计划归档 -> 候选详细设计状态标记 -> 断点清单。 |
| 验证方式 | `git diff --check`；`python .\tools\check_specs.py`；`python -m py_compile .\tools\check_specs.py`；关键短语和旧待确认路径检索。本次未修改 C++，未运行构建，未运行默认入口。 |
| 禁止扩大声明 | 本次只证明迁移路线权力分层和文档路由已修订；不证明服务矩阵全部完成、应用逻辑流程图迁移完成、C++ 新增、旧函数体迁移或旧项目能力迁移完成。 |

## 132. 本次追加实施：PANEL-RUN-S1 控制面板原生窗口

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户明确要求生成并运行控制面板，计划登记可执行队列 #165。 |
| 核心处理 | 新增原生 Win32 控制面板窗口、消息循环、六类树根、结构计数、只读详情、刷新、操作请求壳和正常关闭。 |
| 结构承载 | `海中鱼巣/界面/控制面板窗口.h/.cpp`、`海中鱼巣/入口.cpp`、`海中鱼巣.vcxproj`。 |
| 输出变化 | 默认运行打开控制面板；`--headless` 保留无窗口常驻；`--self-test-exit` 保持自检后退出。 |
| 调用路径 | `main -> 控制面板窗口::运行 -> 控制面板服务只读入口 -> Win32 控件`。 |
| 验证方式 | Debug x64 0 警告 0 错误；自检退出返回 0；可见窗口标题、WM_CLOSE 和退出码 0 验证通过。 |
| 禁止扩大声明 | 不证明完整领域树、业务写操作、WebView、数据库或外设能力完成。 |

## 133. 本次追加实施：DB-PANEL-S1 SQL Server 结构审计投影

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户明确要求本地 SQL Server 数据写入并从控制面板查看，计划登记可执行队列 #166。 |
| 核心处理 | 新增 ODBC SQL Server 适配器，初始化审计数据库和表，参数化追加结构统计审计并读回；控制面板只读显示最近记录。 |
| 结构承载 | 内存事实仍在四个核心仓库；数据库只承载 `海中鱼巣.dbo.结构统计审计`；适配代码位于 `海中鱼巣/适配/SQL数据库适配.h/.cpp`。 |
| 输出变化 | 新增 `--database-self-test-exit`、两个数据库环境覆盖字段和控制面板数据库审计列表。 |
| 调用路径 | `main -> 统计服务权威结构统计快照 -> SQL数据库适配写入/读回 -> 控制面板只读查询`。 |
| 验证方式 | SQL Server 连接、表结构、真实写入读回、不可用负例、Debug x64、控制面板列表与刷新、`git diff --check`、规范治理检查。 |
| 禁止扩大声明 | 不证明节点 / 主信息 / 关系 / 索引明细持久化、数据库恢复、旧表迁移、完整数据库系统或数据库成为运行期事实源。 |

## 134. 本次追加实施：INFO-MIG-S0 鱼巢信息源事实扫描

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户确认当前没有旧数据库可用，要求根据 `D:/鱼巢` 中的信息进行逻辑迁移。 |
| 核心处理 | 只读扫描鱼巢规范、说明书、初始化代码、149 个静态定义词、82 个默认本能方法种子和 15 条因果模板；建立保留、改写、暂缓、拒绝、待裁决矩阵。 |
| 结构承载 | 新增信息逻辑迁移流程图、详细设计、S0 迁移矩阵和 S0 断点清单；曾生成的 INFO-MIG-S1 隔离样本计划已于 2026-07-10 按用户指令删除。 |
| 输出变化 | 取消旧数据库和旧表扫描路线；原安全值、服务值隔离候选仓库路线已撤销，运行期初始化改由 SELF-INIT-S1 承接。 |
| 调用路径 | 用户纠正 -> S0 只读事实扫描 -> 迁移矩阵 -> 流程图 -> 详细设计 -> INFO-MIG-S1（后续撤销）-> SELF-INIT-S1。 |
| 验证方式 | `git diff --check`、`python .\tools\check_specs.py`、Markdown / HTML Mermaid 文本一致性检查；未运行 C++ 构建。 |
| 禁止扩大声明 | 不证明鱼巢定义词、本能方法、因果模板、运行实例、运行期仓库或完整数据库结构已经迁移。 |

## 135. 本次追加实施：THREAD-MOD-S1 每线程独立 ixx 模块拆分

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户明确要求每个线程一个文件、不得混放并使用 `.ixx`；计划登记可执行队列 #167。 |
| 核心处理 | 先验证中文 C++20 模块探针，再拆分共享消息协议、有界队列和运行宿主、任务管理、任务工作、缓存统计、事件日志、外设采样材料 6 个线程模块；删除旧混合头文件。 |
| 结构承载 | `海中鱼巣/线程/` 下 8 个模块接口；6 个线程文件各含 1 个线程类和 1 个 `std::thread` 成员，2 个共享模块无线程类。 |
| 输出变化 | `入口.cpp` 改用 `import`，任务管理 / 工作与缓存统计 / 事件日志分离生命周期和队列所有权；旧 `运行宿主.h`、`运行消息队列.h` 已删除。 |
| 调用路径 | `入口 -> import 协议 / 队列 / 六线程模块 -> 各线程独立启动 -> 正式队列入口 -> 各线程独立停止和 join`。 |
| 验证方式 | Debug x64 Rebuild 0 警告 0 错误；`--self-test-exit` 返回 0，THREAD-S1 至 S6 通过；模块、类、线程成员、项目登记、旧文件和禁止依赖静态扫描通过。 |
| 禁止扩大声明 | 不证明完整真实线程调度、真实闭环、自我循环、自我苏醒、真实外设接入或旧线程能力迁移完成。 |

## 136. 本次追加实施：SELF-INIT-S1 自我线程系统初始化计划生成与直接入队

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求在自我线程内初始化语素、世界树和安全 / 服务两个根需求，指定生成 `初始化.需求.ixx`；随后明确计划生成后直接加入计划索引等待执行，并要求删除鱼巢定义信息隔离样本计划。 |
| 核心处理 | 复核当前初始化入口、需求服务同主体同目标拒绝、线程模块边界和既有服务能力；生成自我线程系统初始化流程图、详细设计、默认裁决和代码实施切片；撤销 INFO-MIG-S1。 |
| 结构承载 | `流程图/20260710_自我线程系统初始化流程图_v0.1.md`、同名 HTML、`规范/详细设计/自我线程系统初始化详细设计.md`、`计划/已完成计划/20260710_SELF-INIT-S1_自我线程系统初始化代码实施切片_v0.1.md`、本切片断点清单、计划索引和可执行队列 #168。 |
| 输出变化 | SELF-INIT-S1 已登记 #168 待执行；执行目标明确为新增独立自我线程、`初始化.需求.ixx`、需求服务专用入口和入口接线。INFO-MIG-S1 计划文件已删除，DQ-57 已撤销。 |
| 调用路径 | 当前代码事实 -> 初始化流程图 -> 初始化详细设计 -> SELF-INIT-Q1 至 Q7 默认裁决 -> 完整计划 -> 计划索引 -> Codex 任务队列 #168。 |
| 验证方式 | Markdown / HTML Mermaid 文本一致性、路径存在性、计划索引 / 队列交叉引用、`git diff --check`、`python .\tools\check_specs.py`；本轮未运行 C++ 构建或程序。 |
| 禁止扩大声明 | 不证明 `初始化.需求.ixx`、自我线程或两个根需求已经实现，不证明自我治理、自我循环、自我苏醒、SQL / 外设接入或旧能力迁移完成。 |

## 138. 本次追加实施：START-TIME-S1 控制台启动步骤用时显示

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户明确要求在控制台实现启动步骤用时显示；#170 作为不依赖 #168 / #169 的人读可观测性插队切片。 |
| 核心处理 | 在入口新增基于 `std::chrono::steady_clock` 的 `启动用时显示器`，按既有启动阶段输出步骤毫秒数、累计毫秒数和模式总用时，使用局部字符串流隔离输出格式。 |
| 结构承载 | 唯一代码承载为 `海中鱼巣/入口.cpp` 的调用栈临时对象；流程图、详细设计、归档计划和两份断点清单承载人读治理证据。 |
| 输出变化 | 控制台新增 `[启动用时]` 和 `[启动总用时]` 行；不新增节点、主信息、关系、索引、缓存、SQL 字段、日志字段或业务状态。 |
| 调用路径 | `main -> 启动用时显示器 -> 既有启动步骤 -> 完成步骤 / 输出总用时 -> 控制台文本`。 |
| 验证方式 | Debug x64 Rebuild 0 警告 0 错误；自检和数据库模式退出码 0；步骤格式、非负值、累计不递减、总用时、静态边界、Mermaid 一致性和规范治理检查通过。 |
| 禁止扩大声明 | 不证明启动性能已优化、建立长期性能基线、精确测得控制面板窗口显示耗时、自我线程初始化完成或自我治理闭环完成。 |

## 139. 本次追加实施：SELF-INIT-S1 自我线程系统初始化代码实施

| 项 | 内容 |
| --- | --- |
| 输入入口 | 可执行队列 #168、已确认的自我线程系统初始化流程图、详细设计和代码实施切片。 |
| 核心处理 | 新增独立自我线程模块和需求初始化服务；在线程入口依次初始化基础语素、世界树、安全根需求和服务根需求；需求服务新增自我目标状态需求专用入口，入口改从初始化快照读取结果。 |
| 结构承载 | 自我线程快照承载进程期初始化结果；稳定语素、特征定义、实例槽位、当前值、需求和目标状态承载机器事实。自我目标宿主用带角色顺序号的反向引用解决关系仓库同端点唯一性，普通入口和核心规则保持不变。 |
| 输出变化 | 新增 `海中鱼巣/线程/自我线程.ixx`、`海中鱼巣/领域/初始化.需求.ixx` 和 SELF-INIT-A1 至 A6；安全 / 服务稳定键分别为 `0x0001'0001'0000'0001`、`0x0001'0001'0000'0002`，当前值为 1，目标值为 10。 |
| 调用路径 | `main -> 自我线程::启动 -> 语素初始化 -> 世界树初始化 -> 需求初始化服务::初始化两个根需求 -> 发布快照 -> main 读回 -> 既有自检 / 控制面板材料`。 |
| 验证方式 | Debug x64 构建 0 警告 0 错误；`--self-test-exit` 返回 0；SELF-INIT-A1 至 A6 和既有矩阵通过；`--headless` 进入常驻并经 CTRL_BREAK 受控停止返回 0；`git diff --check` 与 `py -3.14 .\tools\check_specs.py` 通过。 |
| 禁止扩大声明 | 不证明自我治理消息、治理 mailbox、治理队列、任务 / 方法运行、需求结算、自我循环、自我苏醒、SQL 恢复、外设或旧鱼巢初始化迁移完成。 |

## 140. 本次追加实施：SELF-GOV-S1 自我治理消息协议计划生成

| 项 | 内容 |
| --- | --- |
| 输入入口 | 自我治理循环承载边界流程图、自我治理循环详细设计、SELF-GOV 父计划、#169 S0 当前代码事实和 REAL-LOOP-S1 E01-E15 证据契约。 |
| 核心处理 | 生成 SG-S1 完整代码实施计划，固定八类消息白名单、稳定句柄组、目标特征定义 / 实例特征槽位分离、纯值准入、过期、幂等、顺序、停止优先和结构不变验收；固定后续全部依赖图。 |
| 结构承载 | `计划/已完成计划/20260710_SELF-GOV-S1_自我治理消息协议代码实施切片_v0.1.md`、计划索引、任务队列 #171、项目记忆和计划生成断点清单；未新增机器结构。 |
| 输出变化 | #171 已登记待执行；SG-S2 与 SG-S3A -> SG-S3B -> SG-S4A-D -> SG-S5 的生成条件已登记。 |
| 调用路径 | 本次只形成未来 `治理请求 -> 自我治理消息 -> 纯值准入结果` 的施工计划，不运行该调用路径。 |
| 验证方式 | `git diff --check` 与 `py -3.14 .\tools\check_specs.py`；不运行 C++ 构建或程序。 |
| 禁止扩大声明 | 不证明协议代码、mailbox、治理队列、任务桥、领域路由、自我治理闭环、自我循环或自我苏醒已实现。 |

## 141. 本次追加实施：依赖门控计划规则修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户取消等待前置计划完成后再生成下一计划的规则，并明确新口径“先生成依赖门控计划，执行前再复核实际接口”。 |
| 核心处理 | 分离计划生成许可与代码执行许可；允许提前生成和入队，增加当前事实 / 假定接口分栏、依赖编号、预期产物、执行前复核和漂移退回门禁。 |
| 结构承载 | AGENTS、000、001、计划 / 执行技能、计划索引、任务队列、SELF-GOV 活动计划、项目记忆和正式断点清单。 |
| 输出变化 | 后续 SELF-GOV 计划可提前登记为依赖门控待执行；执行窗口只消费依赖已满足且接口复核通过的项。 |
| 调用路径 | 设计计划窗口生成门控计划 -> 队列登记 -> 执行窗口检查依赖 -> 复核实际接口 -> 执行或退回修订。 |
| 验证方式 | 定向扫描、`git diff --check`、`py -3.14 .\tools\check_specs.py`；未运行 C++ 构建或程序。 |
| 禁止扩大声明 | 不证明后续 SELF-GOV 计划、协议代码、治理队列、任务桥、领域路由或治理闭环已实现。 |

## 142. 本次追加实施：SELF-GOV-S1 自我治理消息协议代码实施

| 项 | 内容 |
| --- | --- |
| 输入入口 | 可执行队列 #171、已确认自我治理循环流程图与详细设计、SELF-GOV 父计划和 #169 S0 当前事实。 |
| 核心处理 | 新增独立 C++20 自我治理消息协议模块，固定八类白名单、稳定句柄组、路由与必填矩阵，并实现公共字段、句柄形状、过期、停止、幂等、顺序和材料越权的纯值复核。 |
| 结构承载 | `自我治理消息`、固定只读句柄组、准入上下文与局部准入结果只承载非权威运行材料；模块不持有仓库、领域服务、队列、锁、线程或日志对象。 |
| 输出变化 | 新增 `海中鱼巣/线程/自我治理消息协议.ixx`，工程登记新模块，入口新增 SELF-GOV-S1-A1 至 A10；计划已移入 `计划/已完成计划/`。 |
| 调用路径 | `固定样例 / 后续显式生产者 -> 自我治理消息 -> 复核自我治理消息 -> 可准入 / 已拒绝 / 重复材料 / 内部不一致`，当前不进入 mailbox 或领域服务。 |
| 验证方式 | Debug x64 0 警告 0 错误；`--self-test-exit` 返回 0，A1-A10 全通过；三组禁止能力扫描 0 命中；`git diff --check` 与规范治理检查通过。 |
| 禁止扩大声明 | 不证明治理 mailbox、独立队列、冻结批次、任务上行桥、领域落账、自我治理闭环、自我循环或自我苏醒完成。 |

## 143. 本次追加实施：SELF-GOV-S2 至 S5 依赖门控计划批量生成

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求继续制定后续所有计划，并已明确“先生成依赖门控计划，执行前再复核实际接口”；输入包括 SELF-GOV 父计划、#169 S0、#171 正式产物、自我治理 / 真实闭环流程图与详细设计及 E01-E15 证据矩阵。 |
| 核心处理 | 一次生成 S2、S3A、S3B、S4A-D、S5 八份完整计划；逐份固定依赖、当前事实、假定接口、预期产物、执行前复核、漂移退回、允许 / 禁止文件、失败收口和验收矩阵。生成时 #171 已完成，因此 #172、#173 登记为依赖已满足待执行；#172、#173 后续已分别由本记录第 144、146 节完成。 |
| 结构承载 | 八份 SELF-GOV 后续计划、计划索引、任务队列 #172-#179、设计队列 DQ-63-DQ-70、项目记忆和 `实施记录/20260710_SELF-GOV-S2至S5依赖门控计划批量生成_Codex断点清单.md`；八份计划后续均已归档到 `计划/已完成计划/`。 |
| 输出变化 | SELF-GOV 当前定义的全部后续路线已计划化；生成时 #172、#173 同时依赖就绪，#174-#179 按 S3B -> S4A -> S4B -> S4C -> S4D -> S5 门控，后续均已完成。第一轮固定安全根需求和动作键，槽位感知结算断点已落入 #176 / #177。 |
| 调用路径 | 本次只规划未来 `S1 -> S2 / S3A -> S3B -> S4A -> S4B -> S4C -> S4D -> S5`，未运行该调用路径。 |
| 验证方式 | 上游路径与范围扫描、计划必备章节扫描、`git diff --check`、`py -3.14 .\tools\check_specs.py`；未运行 C++ 构建或程序。 |
| 禁止扩大声明 | 不证明治理 mailbox、强类型回执、上行桥、领域路由、固定根需求闭环、多轮治理、自我循环或自我苏醒已实现。 |

## 144. 本次追加实施：SELF-GOV-S2 治理 mailbox 独立队列与冻结批次代码实施

| 项 | 内容 |
| --- | --- |
| 输入入口 | 可执行队列 #172、#171 正式消息协议、自我治理与运行宿主流程图 / 详细设计及 SELF-GOV 父计划。 |
| 核心处理 | 新增独立有界治理邮箱模块，以私有队列、互斥量和条件变量实现唯一提交、S1 纯值准入、跨轮幂等 / 顺序、整批冻结、下一轮隔离和停止唤醒。 |
| 结构承载 | 私有待处理队列、已见幂等消息、同任务最新序号、冻结批次和邮箱快照只承载进程期非权威材料；不写节点、主信息、关系、索引或领域事实。 |
| 输出变化 | 新增 `海中鱼巣/线程/有界自我治理队列.ixx`，工程登记新模块，入口新增 SELF-GOV-S2-A1 至 A9 与 A6B；计划已移入 `计划/已完成计划/`。 |
| 调用路径 | `显式生产者 -> 有界自我治理邮箱::提交 -> S1 准入与队列本地复核 -> 私有队列 -> 等待并冻结下一批 -> 本轮只读冻结材料`，当前不进入任务桥或领域服务。 |
| 验证方式 | Debug x64 0 警告 0 错误；完整自检返回 0且 A1-A9 / A6B 全通过；连续 20 次并发自检通过；禁止依赖、并发原语、`git diff --check` 与规范治理检查通过。 |
| 禁止扩大声明 | 不证明强类型回执、治理消息桥、自我线程接线、领域落账、自我治理闭环、自我循环或自我苏醒完成。 |

## 145. 本次追加实施：FS-03 特征值系统第二轮第一批流程设计与代码实施计划生成

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户要求生成 FS-03 特征值系统第二轮第一批代码实施计划；输入包括 #101 S0 当前代码事实、#114 三份分项详细设计、#127 序列化恢复边界详细设计及当前特征值 / 特征服务接口。 |
| 核心处理 | 补建第一批专用流程图 Markdown / HTML 和汇总详细设计；采用“先稳定 Vec 原始值，再开放派生能力”的顺序，生成依赖门控计划并固定当前事实、假定接口、执行前复核、漂移退回、允许 / 禁止文件及 A1-A11 验收。 |
| 结构承载 | `流程图/20260710_FS03特征值系统第二轮第一批代码实施流程图_v0.1.md`、同名 HTML、`规范/详细设计/FS03特征值系统第二轮第一批代码实施详细设计.md`、`计划/20260710_FS03_特征值系统第二轮第一批代码实施切片_v0.1.md`、计划索引、任务队列 #180、项目记忆和正式生成断点清单；未新增机器结构。 |
| 输出变化 | #180 已登记待执行，#101、#114、#127 依赖已满足，执行前接口复核待完成；第一批只允许 VecI64 / VecU64 原始值、I64 / Vec 类型互斥、容器版本和特征服务封口。 |
| 调用路径 | 本次只规划未来 `特征服务请求 -> 归属校验 -> 特征值服务 Vec 前置拒绝 -> 完整记录整体发布 -> 副本读回`，未运行该调用路径。 |
| 验证方式 | 流程图双文件与 Mermaid 文本一致性、上游路径和计划章节扫描、`git diff --check`、`py -3.14 .\tools\check_specs.py`；本次不运行 C++ 构建或程序。 |
| 禁止扩大声明 | 不证明 Vec 原始值代码、内容哈希、缓存、值域、稳态、候选比较、序列化恢复、旧能力迁移或自我内部治理闭环已实现。 |

## 146. 本次追加实施：SELF-GOV-S3A 强类型任务结果回执与公开上行代码实施

| 项 | 内容 |
| --- | --- |
| 输入入口 | 可执行队列 #173、#171 正式自我治理协议、当前任务工作线程、运行宿主与任务回执流程图 / 详细设计及 SELF-GOV 父计划。 |
| 核心处理 | 新增独立强类型任务结果回执协议，固定七个稳定句柄槽位和纯值写前复核；任务工作线程以私有互斥队列、跨取出幂等 / 顺序索引和停止锁存提供唯一发布与公开值式取出。 |
| 结构承载 | 强类型回执、句柄组、准入上下文、私有队列和索引只承载非权威运行材料；不写节点、主信息、关系、索引或领域事实。 |
| 输出变化 | 新增 `海中鱼巣/线程/任务结果回执协议.ixx`，扩展任务工作线程，工程登记新模块，入口新增 SELF-GOV-S3A-A1 至 A7；计划已移入 `计划/已完成计划/`。 |
| 调用路径 | `执行方完整证据 -> 发布强类型任务结果回执 -> 纯值及队列本地复核 -> 私有队列 -> 取出一条任务结果回执 -> 后续 S3B 输入候选`，当前不做领域复核或消息转换。 |
| 验证方式 | Debug x64 0 警告 0 错误；完整自检返回 0且 A1-A7 全通过；最终代码连续 20 次并发自检通过；协议禁依赖、差异级权限词、`git diff --check` 与规范治理检查通过。 |
| 禁止扩大声明 | 不证明回执已通过领域复核、治理消息桥、领域落账、自我治理闭环、自我循环或自我苏醒完成。 |

## 147. 本次追加实施：SELF-GOV-S3B 回执复核许可预判与治理消息转换代码实施

| 项 | 内容 |
| --- | --- |
| 输入入口 | 可执行队列 #174、#171-#173 正式前置产物、任务 / 方法 / 状态 / 动态服务公开只读入口、自我治理与任务回执流程图 / 详细设计。 |
| 核心处理 | 新增任务管理上行桥；在 S3A 值式取出后锁外复核任务承接、选择方法、动作归属与状态、实际结果、动态证据、场景、主体和发生顺序；许可路径只产生“可请求正式入口复核”候选，消息无损转换后只经 S2 mailbox 提交。 |
| 结构承载 | 桥处理结果、许可候选、转换消息和桥生命周期状态只承载非权威运行材料；不新增节点、主信息、关系、索引、授权状态或领域结果。 |
| 输出变化 | 新增 `海中鱼巣/线程/任务管理上行桥.ixx`，工程登记新模块，入口新增 SELF-GOV-S3B-A1 至 A9；计划已移入 `计划/已完成计划/`，#175 依赖已满足。 |
| 调用路径 | `S3A 强类型回执 -> 任务管理上行桥 -> 领域公开只读复核 -> 许可候选 / 结果材料 -> 自我治理消息转换 -> S2 mailbox`，当前不进入领域写入口。 |
| 验证方式 | Debug x64 0 警告 0 错误；完整自检返回 0且 A1-A9 全通过；最终代码连续 20 次运行通过；四仓库依赖、权限结论赋值和领域写入口扫描均 0 命中；`git diff --check` 与规范治理检查通过。 |
| 禁止扩大声明 | 不证明任务已授权、方法已执行、任务已完成、需求已结算、自我治理闭环、自我循环或自我苏醒完成。 |

## 148. 本次追加实施：SELF-GOV-S4A 根需求任务固定方法与动作请求治理落账

| 项 | 内容 |
| --- | --- |
| 输入入口 | 可执行队列 #175、#174 正式前置、#168 初始化快照、S1 / S2 治理消息与冻结批次、E01-E06 证据契约及现有领域服务入口。 |
| 核心处理 | 新增自我治理领域路由；只接受固定安全根 `派生需求请求`，完整复核定义、实例槽位、当前值和目标状态后，依次建立任务承接、固定方法、主动动作入口、稳定键、输入 / 输出规格、唯一任务方法关系和动作请求材料。 |
| 结构承载 | 任务、方法、动作入口、规格、稳定索引和任务方法关系由正式领域服务承载；路由上下文只聚合同组权威句柄，不新增第二套事实。 |
| 输出变化 | 新增 `海中鱼巣/线程/自我治理领域路由.ixx`，工程登记新模块，入口新增 SELF-GOV-S4A-A1 至 A8；计划已移入 `计划/已完成计划/`，#176 依赖已满足。 |
| 调用路径 | `固定安全根请求 -> S2 冻结 -> 路由读回根材料 -> 创建任务 -> 建立方法 / 动作 / 规格 -> 任务选择方法 -> 读取动作执行桥请求 -> 完整上下文`。 |
| 验证方式 | Debug x64 0 警告 0 错误；完整自检返回 0且 A1-A8 全通过；最终代码连续 20 次运行通过；直接仓库调用、排除能力和后续落账扫描 0 命中；`git diff --check` 与规范治理检查通过。 |
| 禁止扩大声明 | 不证明动作已执行、实例槽位已改值、动态 / 因果已记录、任务完成、需求结算或自我治理闭环完成。 |

## 149. 本次追加实施：SELF-GOV-S4B 实例槽位动作动态与轻量因果治理落账

| 项 | 内容 |
| --- | --- |
| 输入入口 | 可执行队列 #176、#175 固定安全根完整上下文、E07-E09 证据契约及方法 / 特征 / 状态 / 动态 / 因果服务现有公开入口。 |
| 核心处理 | 扩展自我治理领域路由；写前复核任务创建时间、任务方法关系、主动动作请求、主体场景和实例槽位，经方法服务把当前值 1 写为 10，记录同一动作入口来源的完整实例动态，再用动态作轻量因果准入证据。 |
| 结构承载 | 实例槽位当前值、动作前后值材料状态、实例动态和轻量因果身份由正式领域服务承载；动作上下文只聚合同组权威句柄，因果引用不持久化来源动态关系。 |
| 输出变化 | `海中鱼巣/线程/自我治理领域路由.ixx` 新增动作处理与完整结果发布，入口新增 SELF-GOV-S4B-A1 至 A8；计划已移入 `计划/已完成计划/`，#177 依赖已满足。 |
| 调用路径 | `S4A 完整上下文 -> 写前领域复核 -> 方法服务写实例槽位 -> 方法服务记录动作动态 -> 动态完整读回 -> 因果轻量准入与身份创建 -> 单次发布动作上下文`。 |
| 验证方式 | Debug x64 0 警告 0 错误；完整自检返回 0且 A1-A8 全通过；最终代码连续 20 次运行通过；直接仓库调用和禁止能力扫描 0 命中；`git diff --check` 与规范治理检查通过。 |
| 禁止扩大声明 | 不证明任务实际结果、任务完成、需求结算、稳定因果结论、自我治理闭环、自我循环或自我苏醒完成。 |

## 150. 本次追加实施：SELF-GOV-S4C 任务结果完成与根需求结算治理落账

| 项 | 内容 |
| --- | --- |
| 输入入口 | 可执行队列 #177、#176 固定安全根动作上下文、E10-E11 证据契约及需求 / 任务 / 状态 / 动态 / 因果服务公开入口。 |
| 核心处理 | 在需求服务新增实例槽位感知结算预判和可选因果读回；扩展领域路由，依次创建任务实际结果、记录任务完成、创建需求实际状态并用同一动态与因果证据记录根需求结算。 |
| 结构承载 | 任务实际结果与完成状态、需求实际状态、结算状态及来源任务 / 目标 / 实际 / 动态 / 因果关系由正式领域服务承载；结算上下文只聚合同组权威句柄。 |
| 输出变化 | `需求服务.h` 新增槽位感知只读入口和因果读回，领域路由与入口新增 SELF-GOV-S4C-A1 至 A8；计划已移入 `计划/已完成计划/`，#178 依赖已满足。 |
| 调用路径 | `S4B 动作上下文 -> 槽位感知预判 -> 任务实际结果 -> 任务完成 -> 需求实际状态 -> 需求结算 -> 完整证据读回 -> 单次发布结算上下文`。 |
| 验证方式 | Debug x64 0 警告 0 错误；完整自检返回 0且 A1-A8 全通过；最终代码连续 20 次运行通过；需求服务越权依赖、路由仓库直调和 S4D / 外部能力扫描 0 命中；`git diff --check` 与规范治理检查通过。 |
| 禁止扩大声明 | 不证明结算后缓存 / 日志 / 显示观察、多轮自我治理、自我循环、自我苏醒或旧能力迁移完成。 |

## 151. 本次追加实施：SELF-GOV-S4D 结算后缓存日志显示观察

| 项 | 内容 |
| --- | --- |
| 输入入口 | 可执行队列 #178、#177 固定安全根结算上下文、E12-E14 证据契约及统计 / 日志 / 显示现有非权威入口。 |
| 核心处理 | 在统计服务增加需求结算摘要运行期缓存，在显示服务增加结算只读材料；扩展领域路由，先重读 E02-E11 权威材料，再并列生成同一结算来源的摘要缓存、人读结构事件和只读显示材料。 |
| 结构承载 | 摘要缓存只存在于统计服务运行期，事件日志只做人读审计，显示材料只读；观察上下文聚合三分支结果，不成为第二套结算事实。 |
| 输出变化 | `统计服务.h`、`显示服务.h`、领域路由与入口新增 SELF-GOV-S4D-A1 至 A8；计划已移入 `计划/已完成计划/`，#179 的 #171-#178 依赖全部满足。 |
| 调用路径 | `S4C 结算上下文 -> 权威服务完整重读 -> 摘要缓存 / 结构事件日志 / 只读显示并列观察 -> 来源一致后单次发布观察上下文`。 |
| 验证方式 | Debug x64 0 警告 0 错误；完整自检返回 0且 A1-A8 全通过；最终代码连续 20 次运行通过；缓存清空、日志裁决 / 恢复拒绝和显示越权拒绝前后 E02-E11 与结构数量不变；权威性反向赋值扫描 0 命中；`git diff --check` 与规范治理检查通过。 |
| 禁止扩大声明 | 不证明多轮自我治理、事件日志持久化恢复、缓存持久化、控制面板接入、自我循环、自我苏醒或旧能力迁移完成。 |

## 152. 本次追加实施：SELF-GOV-S5 自我线程多轮治理接线与停止收口

| 项 | 内容 |
| --- | --- |
| 输入入口 | 可执行队列 #179、#171-#178 正式协议 / mailbox / 回执 / 上行桥 / E01-E14 产物及自我线程既有初始化和停止入口。 |
| 核心处理 | 扩展自我线程，在完整初始化快照后等待并冻结治理批次，冻结返回后经强类型处理器锁外编排既有 S4A-D；分类完成、逻辑内返回和内部不一致，并管理多轮、异常和停止生命周期。 |
| 结构承载 | S2 冻结批次和自我线程原子读数只承载非权威运行材料；固定安全根 E01-E14 仍由既有领域路由和服务承载，自我线程不直接依赖领域服务或核心仓库。 |
| 输出变化 | `自我线程.ixx` 新增治理生命周期和锁外处理器，入口新增 SELF-GOV-S5-A1 至 A12；计划已移入 `计划/已完成计划/`，#171-#179 SELF-GOV 执行链全部形成正式记录。 |
| 调用路径 | `初始化快照 -> mailbox 等待 / 冻结 -> 锁外 S4A-D -> 第一批完整读回 -> 第二批隔离 -> 生产者 / 桥 / mailbox / 自我线程顺序停止与 join`。 |
| 验证方式 | Debug x64 0 警告 0 错误；完整自检返回 0且 A1-A12 全通过；最终并发自检连续 20 次通过；`--headless` 受控 CTRL_BREAK 停止返回 0；yield、领域服务 / 仓库直依赖和禁止能力扫描均 0 命中；`git diff --check` 与规范治理检查通过。 |
| 禁止扩大声明 | 只证明固定安全根样例的两轮治理、幂等、并发提交和停止收口；不证明持续自主需求、完整自我循环、自我苏醒、真实外设或旧能力迁移完成。 |

## 153. 本次追加实施：FS-03 特征值系统第二轮第一批代码实施

| 项 | 内容 |
| --- | --- |
| 输入入口 | 可执行队列 #180、#101 / #114 / #127 正式前置产物、第一批流程图 / 详细设计及当前特征值 / 特征服务接口。 |
| 核心处理 | 以完整特征值句柄建立运行期 VecI64 / VecU64 原始值记录；序列先锁外完整复制，锁内复核 I64 / Vec 类型互斥并整体发布，首次版本 1、同类型替换加 1；特征服务先校验节点和归属再调用。 |
| 结构承载 | Vec 原始值和独立容器版本只由特征值服务运行期线性记录表承载；I64 继续由主信息仓库承载，节点句柄生命周期版本不变。 |
| 输出变化 | 新增 VecI64 / VecU64 写读、原始类型和原始版本业务入口，入口新增 FS03-S1-A1 至 A11；计划移入 `计划/已完成计划/`。 |
| 调用路径 | `特征服务请求 -> 特征 / 特征值 / 归属前置校验 -> 特征值服务类型冲突复核 -> 完整记录发布 -> 值副本 / 类型 / 版本读回`。 |
| 验证方式 | Debug x64 0 警告 0 错误；完整自检 A1-A11 全通过并返回 0；最终代码连续 20 次运行通过；高级服务直接依赖和第一批排除能力扫描 0 命中；`git diff --check` 与规范治理检查通过。 |
| 禁止扩大声明 | 不证明内容哈希、缓存、值域、稳态、候选比较、当前值唯一性、序列化恢复、持久化、删除清理或旧能力迁移完成。 |

## 154. 本次追加实施：CONCEPT-S1 关系、根登记与概念图服务边界

| 项 | 内容 |
| --- | --- |
| 输入入口 | 可执行队列 #182、#181 当前代码事实扫描、概念图流程图 / 详细设计及关系仓库实际接口。 |
| 核心处理 | 追加关系 9 至 11；按目标返回来源节点和关系记录值副本；建立四槽运行期非名称根登记；在概念图服务内完成类别兼容、根实例排除、重复幂等、多父无环和定义材料白名单复核。 |
| 结构承载 | 三类专用关系由统一关系仓库承载；根类别、稳定非名称键和根句柄由概念图服务运行期登记组承载。 |
| 输出变化 | 新增 `概念图服务.h`、关系仓库三个反向读取入口和 CONCEPT-S1-A01 至 A12 自检；计划移入 `计划/已完成计划/`，#183 解锁。 |
| 调用路径 | `概念请求 -> 概念图服务写前拒绝 -> 关系仓库正式写入 -> 值式读回`；反向读取不解释业务事实。 |
| 验证方式 | Debug x64 0 警告 0 错误；完整自检返回 0；来源=2、多父=2、根登记=2；专用关系归属和禁止依赖扫描通过；`git diff --check` 与规范治理检查通过。 |
| 禁止扩大声明 | 不证明四根正式初始化、跨重启恢复、普通概念、签名固定点、活动图、树投影、命名、生命周期、安全删除或控制面板六类真实树完成。 |

## 155. 本次追加实施：CONCEPT-S2 四根与存在实例支持

| 项 | 内容 |
| --- | --- |
| 输入入口 | #183、#182 正式概念图服务边界及四类原子服务、语素服务和世界树自我存在。 |
| 核心处理 | 专用创建四根、固定非名称键登记、完整后根名绑定、重复初始化幂等、存在实例支持双向读回。 |
| 结构承载 | 根节点由四类原子服务创建，根身份由概念图服务登记，名称由语素关系承载，支持由实例支持概念关系承载。 |
| 输出变化 | 新增概念图初始化服务；四根和自我存在支持进入主启动；#184 解锁。 |
| 调用路径 | `入口 -> 概念图初始化 -> 原子服务 -> 根登记 -> 语素绑定`；`存在实例 -> 概念图服务 -> 存在根`。 |
| 验证方式 | Debug x64 0 警告 0 错误；完整自检 S2 根=4、存在支持=2；重复初始化结构不变；静态依赖和规范检查通过。 |
| 禁止扩大声明 | 不证明跨重启恢复、普通存在概念、其他三类实例支持、签名固定点、活动图、树投影或控制面板概念树完成。 |

## 156. 本次追加实施：CONCEPT-S3 动态、关系、因果实例支持

| 项 | 内容 |
| --- | --- |
| 输入入口 | #184、#182 / #183 正式概念图服务边界与四根，以及动态、二次特征、因果服务当前只读接口。 |
| 核心处理 | 三类原子服务形成值式实例完成材料；概念图服务按实例类别和根类别写或复用支持关系并双向读回；长期动态、空组成关系和错误来源动态写前拒绝。 |
| 结构承载 | 实例及业务关系继续由既有节点和仓库承载，完成材料只存在于调用期值对象，概念支持由实例支持概念关系承载；因果不保存来源动态关系。 |
| 输出变化 | 动态、关系、因果三类实例均可支持对应根，四类实例根支持第一轮闭合；#185 解锁。 |
| 调用路径 | `实例 -> 对应原子服务完成材料 -> 概念图服务类别复核 -> 实例支持概念关系 -> 双向读回`。 |
| 验证方式 | Debug x64 0 警告 0 错误；完整自检 S3 动态=1、关系=1、因果=1，A01-A12 全通过；来源动态关系、可靠性门槛、禁止能力与规范检查通过。 |
| 禁止扩大声明 | 不证明普通细分概念、签名、偏序、固定点、活动图、树投影、跨重启恢复、生命周期、安全删除或控制面板六类真实树完成。 |

## 157. 本次追加实施：CONCEPT-S4 签名、偏序与固定点候选

| 项 | 内容 |
| --- | --- |
| 输入入口 | #185、#180 精确 I64 / Vec 产物、#183 四根、#184 四类实例完成材料和根支持。 |
| 核心处理 | 特征服务整组读取结构化精确约束；纯值算法规范化签名、合并等价项、计算同类子集偏序、公共约束聚类、传递约简、受影响分量和预算受控固定点。 |
| 结构承载 | 签名、候选节点规格、来源实例、定义材料边、直接边和待重算请求均由调用期 DTO 承载；候选计算不写节点或概念关系。 |
| 输出变化 | 四类实例可形成确定性候选输入，调用方可取得收敛候选版本或结构化待重算材料；#186 解锁。 |
| 调用路径 | `四类实例完成材料 / 特征约束 -> 概念图服务构造输入 -> 概念图算法规范化与固定点 -> 候选版本值材料`。 |
| 验证方式 | Debug x64 0 警告 0 错误；完整自检 S4 候选=8、直接边=4、迭代=1，A01-A15 全通过；连续 10 次运行、结构快照和静态边界检查通过。 |
| 禁止扩大声明 | 不证明候选已发布为活动概念图，不证明普通概念节点、活动版本、抽象树投影、命名、生命周期、安全删除或控制面板六类真实树完成。 |

## 158. 本次追加实施：CONCEPT-S5 活动图发布与抽象树只读投影

| 项 | 内容 |
| --- | --- |
| 输入入口 | #186、#185 收敛候选版本、#183 四根和 #184 四类实例根支持。 |
| 核心处理 | 初始化基础活动版本；复核收敛、基准、签名、三类边和图约束；经四类原子服务创建普通概念，写入专用关系，完整读回后替换活动快照；内部失败清理本轮新增结构，旧关系后置失效；从四根生成多路径树投影。 |
| 结构承载 | 节点与关系继续由权威仓库承载；活动选择、版本和签名映射由概念图服务运行期快照承载；抽象树为调用期只读值材料。 |
| 输出变化 | 活动图从基础版本 1 发布到版本 2；11 个活动概念、39 条活动关系和 4 棵抽象树可读；XY 多父概念以同句柄投影两次；#187 解锁。 |
| 调用路径 | `S4 收敛候选 -> 发布门禁 -> 四类原子服务 / 概念专用关系 -> 活动快照 -> 四根抽象树只读投影`。 |
| 验证方式 | Debug x64 0 警告 0 错误；完整自检 S1-S5 返回 0，A01-A16 全通过；最终代码连续 10 次运行通过；结构快照、静态边界、差异和规范治理检查通过。 |
| 禁止扩大声明 | 不证明跨重启恢复、命名治理、用途、生命周期、安全删除或控制面板六类真实树完成。 |

## 159. 本次追加实施：PANEL-TREES-S1 控制面板六类真实树只读显示

| 项 | 内容 |
| --- | --- |
| 输入入口 | #187、世界 / 语素已验证 DTO、#186 活动概念四根投影、两个初始化根需求及领域公开只读入口。 |
| 核心处理 | 按需求反向发现任务、由任务选择发现方法、六类关系角色投影、确定性排序、重复标记、预算和完整性复核、候选一次发布、窗口原子替换与导航定位。 |
| 结构承载 | 权威事实仍由节点、关系和领域服务承载；六树是调用期值式 DTO，窗口只保存会话期完整快照，UI 分类根不携带领域句柄。 |
| 输出变化 | 单个原生 TreeView 同时显示世界、概念、语素、需求、任务、方法六类真实树；当前投影项为 3 / 13 / 66 / 19 / 9 / 6；#188 成为下一待执行项。 |
| 调用路径 | `已验证根 / 活动图 -> 领域公开只读入口 -> 六树完整候选 -> 窗口快照 -> 六分类 TreeView`。 |
| 验证方式 | Debug x64 0 警告 0 错误；A01-A18、连续 10 次自检、TreeView 跨进程读回、六次导航、刷新、两档视觉尺寸和退出码 0 通过。 |
| 禁止扩大声明 | 不证明需求父子写入、完整任务调度、通用方法召回、概念命名生命周期删除、业务写控制面板、SQL 事实源或旧控制面板迁移完成。 |

## 160. 本次追加实施：CONCEPT-S5 现状流程图与纠偏计划生成

| 项 | 内容 |
| --- | --- |
| 输入入口 | 当前 `海中鱼巣/领域/概念图服务.h`、#186 已完成计划与实施记录、概念图施工流程图和详细设计。 |
| 核心处理 | 逐行映射 S5 发布与树读取代码，建立任意请求/测试与正式流程调用语境，审查全部中途非成功返回，生成偏差清单和 #199 纠偏计划。 |
| 结构承载 | 现状流程图 MD/HTML、逐行代码映射表、输入契约与调用语境表、非成功返回二分审查表、现状施工偏差清单和计划文档。 |
| 输出变化 | #199、DQ-91、JY-216 已登记；未改变节点、关系、活动快照或 C++ 代码。 |
| 调用路径 | `当前代码 -> 现状流程图 -> 输入契约 -> 非成功二分审查 -> 偏差清单 -> 纠偏计划 -> 计划索引与任务队列`。 |
| 验证方式 | `tools/check_flowchart_correction.py` 严格产物与登记检查通过，`tools/check_specs.py` 通过；本轮未运行构建或程序。 |
| 禁止扩大声明 | 不宣称代码已纠偏，不宣称 S6-S8、生命周期、安全删除、持续自主循环或自我苏醒完成。 |

## 161. 本次追加实施：FS03-B2-S1 实例槽位当前值唯一性与统一原始版本

| 项 | 内容 |
| --- | --- |
| 输入入口 | #188、#180 I64 / Vec 原始值与 Vec 版本入口、#185 概念特征约束读取契约。 |
| 核心处理 | 完整枚举实例槽位归属值并分流零 / 唯一 / 多值；创建写锁复核顺序与并发重复请求；同一原始值锁内发布 I64 / Vec 值和统一版本并复制完整材料。 |
| 结构承载 | 特征值节点和归属关系继续由权威仓库承载；Vec 值与版本、I64 版本由特征值服务运行期记录承载；原始材料为调用期值 DTO。 |
| 输出变化 | 多值槽位不再返回第一个，唯一值可公开读回；I64 / Vec 均有首写 1、成功重写递增的统一版本，拒绝不递增；#189 解锁。 |
| 调用路径 | `宿主 / 特征 -> 特征服务槽位与归属复核 -> 特征值服务原始值锁 -> 值 / 统一版本发布或副本读取`。 |
| 验证方式 | Debug x64 0 警告 0 错误；A01-A10 与完整自检通过；连续 20 次运行、顺序 / 并发槽位创建、两个独立 I64 值并发写入、依赖与排除能力扫描通过。 |
| 禁止扩大声明 | 不证明历史值、候选值提升、真实哈希、缓存、值域、稳态、序列化、恢复、持久化或旧能力迁移完成。 |

## 162. 本次追加实施：FS03-B2-S2 内容哈希与非权威命中缓存

| 项 | 内容 |
| --- | --- |
| 输入入口 | #189、#188 统一原始值材料与版本、FS03 第二批流程图 / 详细设计及特征值非权威缓存专项详细设计。 |
| 核心处理 | 特征服务按固定小端规范字节、ZigZag64 和规则版本 1 的 FNV-1a 生成内容签名，重读完整原始值与版本复核当前性和相等性；统计服务并发安全记录同哈希候选和非权威命中观察。 |
| 结构承载 | 权威值继续由既有特征值结构承载；签名是调用期 DTO；哈希候选、命中次数和最近序号只由统计服务进程内可清空缓存承载，不持久化。 |
| 输出变化 | 可生成规则版本化真实内容哈希、排除旧版本候选、完整复核碰撞、读取命中观察并手动清空缓存；缺缓存仍可直接计算；#190 解锁。 |
| 调用路径 | `特征服务读取统一原始材料 -> 生成内容签名 -> 统计服务记录 / 读取候选 -> 特征服务复核当前版本与完整值 -> 逻辑内相同 / 不同 / 过期结果`。 |
| 验证方式 | Debug x64 0 警告 0 错误；A01-A10 与完整自检通过；`--self-test-exit` 连续 20 次通过；高级服务 / 统计服务越权依赖、排除能力、差异和规范治理检查通过。 |
| 禁止扩大声明 | 不证明哈希唯一、缓存持久化、缓存裁决业务事实、值域、稳态、序列化、恢复、完整特征值系统或旧能力迁移完成。 |

## 163. 本次追加实施：FS03-B2-S3 值域稳态与候选比较

| 项 | 内容 |
| --- | --- |
| 输入入口 | #190、#188 统一原始材料 / 版本、#189 完整内容复核及 FS03 第二批流程图 / 详细设计。 |
| 核心处理 | 显式样本去重并逐项读取同一特征原始材料；I64 生成三态比较和最小最大值域，Vec 只做完整序列等价；至少三个不同句柄全内容相等形成稳态候选；返回前再次复核来源版本。 |
| 结构承载 | 来源句柄组、版本组、原始类型、样本计数、规则版本、比较 / 稳态结论和结果状态只由调用期值式 DTO 承载，不进入任何权威仓库或高级业务结构。 |
| 输出变化 | 新增来源状态复核、精确比较、I64 值域和稳态候选入口；混合类型、样本不足、重复句柄、未知规则和版本漂移均逻辑内收口；#191 解锁。 |
| 调用路径 | `来源特征 / 显式样本 -> 归属与统一原始材料读取 -> 去重 / 同类型 / 样本数门禁 -> 精确计算 -> 来源版本复核 -> 待复判候选或结构化拒绝状态`。 |
| 验证方式 | Debug x64 0 警告 0 错误；A01-A10 与完整自检通过；连续 20 次运行通过；两个线程并发候选读通过；依赖、禁止升格、差异和规范治理检查通过。 |
| 禁止扩大声明 | 不证明容差比较、时间窗口稳态、长期区间、自动升格、高级业务裁决、序列化、恢复、完整特征值系统或旧能力迁移完成。 |

## 164. 本次追加实施：FS03-B2-S4 序列化材料与恢复候选

| 项 | 内容 |
| --- | --- |
| 输入入口 | #191、#188 统一原始材料 / 版本、#189 完整内容复核、#190 候选边界及 FS03 第二批流程图 / 详细设计。 |
| 核心处理 | 生成格式与枚举版本化结构材料，保存仓库上下文、材料序号、完整句柄、原始类型、统一版本、元素数量和 I64 / Vec 完整值；读后复核当前性；恢复候选只复核材料并复制规范化原始材料，不写仓库。 |
| 结构承载 | 权威值继续由既有特征值结构承载；序列化材料和恢复候选只由调用期值式 DTO 承载，不形成文件、数据库记录或运行期事实。 |
| 输出变化 | 新增材料生成、当前性复核和只读恢复候选入口；未知版本、上下文不符、错误归属、长度冲突、非权威恢复请求和版本漂移均结构化收口；#192 解锁。 |
| 调用路径 | `仓库上下文 / 完整句柄 / 材料序号 -> 归属与统一原始材料读取 -> 结构材料组装 -> 当前性复核 -> 可用或过期`；`目标上下文 / 结构材料 -> 静态字段与身份归属复核 -> 规范化原始材料副本 -> 只读候选或拒绝`。 |
| 验证方式 | Debug x64 0 警告 0 错误；A01-A10 与完整自检通过；连续 20 次运行及两个线程并发只读通过；四仓库 / 原始值不变、依赖、禁止能力、差异和规范治理检查通过。 |
| 禁止扩大声明 | 不证明文件序列化、反序列化写入、快照恢复、启动恢复、数据库持久化、旧格式迁移、完整特征值系统或旧能力迁移完成。 |

## 165. 本次追加实施：DEMAND-TREE-S1 需求父子原子挂载与重挂

| 项 | 内容 |
| --- | --- |
| 输入入口 | #192、FLOW-06 需求树只读材料、#187 控制面板需求树只读接口、需求父子流程图 / 详细设计及当前关系仓库 / 需求服务。 |
| 核心处理 | 关系仓库值式返回精确旧父、新父和父关系版本；两阶段验证端点后在同一独占锁内复核单父与父链，无父首次创建、有父复用原关系编号重挂、同父幂等；需求服务验证需求类型和子需求唯一目标状态后提交并读回。 |
| 结构承载 | 唯一权威事实是父需求到子需求的有效普通父子关系；关系版本表达原子操作线性化点；更新 DTO 只作调用期结果，不持久化为权重、完成或方法事实。 |
| 输出变化 | 新增通用挂载 / 重挂、需求父子提交、唯一父需求和稳定直接子需求组入口；自环、两级 / 多级成环、非需求端点和缺目标状态均写前拒绝；#193 解锁。 |
| 调用路径 | `子需求 / 新父需求 -> 需求与目标状态准入 -> 关系材料锁外验证 -> 关系锁内单父防环 -> 首次创建或原地重挂 -> 关系编号 / 版本 -> 直接父子读回`。 |
| 验证方式 | Debug x64 0 警告 0 错误；A01-A12 与完整自检通过；并发压力 50 次通过；不同父并发请求同一关系编号版本 1 -> 2；既有重挂、世界树、FLOW-06 和控制面板回归及排除扫描通过。 |
| 禁止扩大声明 | 不证明完整需求树、脱离 / 删除、阻塞权重、逻辑组织、方向签名、枚举目标、通用方法召回、旧需求能力迁移或外部系统接入完成。 |

## 166. 本次追加实施：METHOD-RECALL-S0 通用方法召回当前代码事实扫描

| 项 | 内容 |
| --- | --- |
| 输入入口 | #193、#190 值域稳态与候选比较正式产物、#192 需求父子正式产物、当前方法 / 需求 / 任务 / 场景 / 特征服务及核心仓库。 |
| 核心处理 | 只读扫描方法首发现、条件 / 结果规格、需求任务上下文、索引缓存、排序选择、生命周期、并发和恢复事实，归并七类矩阵与后续依赖链。 |
| 结构承载 | 正式断点清单只承载人读事实、默认建议和文件边界；未新增节点、主信息、关系、索引、状态、缓存或任务方法关系。 |
| 输出变化 | 确认当前只有已知方法首结构读取，缺权威登记发现、结构化规格、多值派生索引、生命周期版本、确定性排序和恢复重建；形成十项建议默认值及 S1-S7 与学习后置顺序；#194 解锁。 |
| 调用路径 | `#193 -> #190 / #192 前置复核 -> 当前代码与调用点扫描 -> 七类事实 / 缺口矩阵 -> 默认裁决 -> 后续依赖链 -> 治理同步`。 |
| 验证方式 | 定向 `rg` 与短文件读取；`git diff --check`、规范治理和 C++ / 工程差异零命中检查；按计划不运行构建或程序。 |
| 禁止扩大声明 | 不证明方法登记发现、结果能力索引、条件判定、候选排序、自动选择、任务授权、方法学习、完整方法系统、持久化恢复或旧方法能力迁移完成。 |

## 167. 本次追加文档治理：通用方法召回新三件套与连续计划

| 项 | 内容 |
| --- | --- |
| 输入入口 | #193 正式断点、`a2b4a85` 当前方法 / 需求 / 任务 / 特征 / 状态代码、既有方法规范与旧图漂移证据。 |
| 核心处理 | 生成登记、规格、派生索引、只读召回、条件复判、确定性排序和任务授权选择接线流程图 / 详细设计，并拆分 #203-#209。 |
| 结构承载 | 新流程图 MD+HTML、详细设计、七份计划和生成断点只承载设计与执行路由；未新增机器结构。 |
| 输出变化 | DQ-95 至 DQ-101、QR-166、JY-224 已登记；S1-S6 具备连续依赖计划，S7 通过更新后 S0 续接专用三件套，方法学习后置。 |
| 调用路径 | `#193 当前事实 -> 新流程图 -> 新详细设计 -> #203-#208 代码切片 -> #209 更新后 S0 -> 任务服务专用三件套`。 |
| 验证方式 | 流程图同图、路径与范围机械检查，`git diff --check`、规范治理检查和本批 C++ 零暂存；不运行构建或程序。 |
| 禁止扩大声明 | 不证明方法登记、生命周期、规格、索引、召回、复判、排序、授权、选择、学习或恢复已经实现。 |

## 168. 本次追加实施：CONCEPT-S6A 待命名指代与生命周期初态

| 项 | 内容 |
| --- | --- |
| 输入入口 | #194、#186 活动图正式产物、#187 六树只读投影、概念命名用途生命周期流程图 / 详细设计、状态服务与语素关系反查实际接口。 |
| 核心处理 | 追加概念生命周期关系 12；经状态服务创建并登记活跃、冷却、退役三个抽象状态；四根和新发布普通概念在活动快照发布前取得唯一活跃关系；名称反查按完整句柄排序去重；待命名与临时指代只返回活动版本约束的值式材料。 |
| 结构承载 | 生命周期事实由概念到已登记抽象状态的唯一关系承载；名称继续由语素概念追溯承载；待命名请求和临时指代不进入节点、主信息、关系、索引或缓存。 |
| 输出变化 | 新增生命周期状态登记 / 读取、四根初态、活动概念生命周期读取、概念名称入口反查、单个 / 全部待命名请求和临时指代入口；#195 解锁。 |
| 调用路径 | `概念图初始化 -> 状态服务创建三个抽象状态 -> 概念图登记 -> 四根活跃关系 -> 活动候选创建 -> 新概念活跃关系读回 -> 活动快照发布 -> 名称 / 待命名 / 临时指代只读材料`。 |
| 验证方式 | Debug x64 0 警告 0 错误；A01-A09 与完整自检通过；S1-S5、PANEL-TREES-S1 回归通过；四根中途失败和候选后段失败清理通过；规范、差异及禁止能力扫描通过。 |
| 禁止扩大声明 | 不证明命名需求、代词生成、用途统计、冷却 / 退役转换、物理删除、跨重启恢复、完整概念治理、自我循环或旧能力迁移完成。 |

## 169. 本次追加实施：CONCEPT-S6B 三类用途观察与生命周期复核候选

| 项 | 内容 |
| --- | --- |
| 输入入口 | #195、#194 活动生命周期正式产物、#189 非权威缓存模式、概念命名用途生命周期流程图 / 详细设计及非权威缓存规范 / 详细设计。 |
| 核心处理 | 以完整命名空间、概念句柄和用途类型键控一般 / 因果预测 / 因果行动观察；专用共享互斥量保护读取、原子累计和清空；概念图服务重读活动版本、签名和生命周期后提交，并组合活动支持、外部引用和三类观察形成非权威建议。 |
| 结构承载 | 用途观察只由统计服务进程内可清空哈希缓存承载；活动身份、支持和生命周期继续由活动快照及权威关系承载；复核建议只作调用期值式 DTO。 |
| 输出变化 | 新增概念用途记录 / 单项与组读取 / 清空、活动概念用途封口和生命周期复核候选入口；#196 解锁，#197 业务前置 #195 已满足。 |
| 调用路径 | `结构化用途调用 -> 活动版本 / 类别 / 生命周期准入 -> 统计缓存原子记录 -> 当前支持 / 外部引用与三类快照重读 -> 规则门禁 -> 非权威建议`。 |
| 验证方式 | Debug x64 0 警告 0 错误；A01-A08 与完整自检通过；两个线程不同键各 64 次无丢失；清空、时间 / 溢出拒绝、规则变化、支持 / 引用和权威结构不变检查通过；规范、差异及禁止能力扫描通过。 |
| 禁止扩大声明 | 不证明生命周期转换、自动淘汰、稳定因果、命名需求、物理删除、缓存持久化、跨重启恢复、完整概念治理、自我循环或旧能力迁移完成。 |

## 170. 本次追加扫描：CONCEPT-S6C 命名需求治理接线当前代码事实

| 项 | 内容 |
| --- | --- |
| 输入入口 | #196、#194 待命名正式产物、#195 用途候选、#192 需求父子、当前概念图 / 语素 / 需求 / 任务服务与自我治理协议、邮箱、线程和固定根路由。 |
| 核心处理 | 只读核对待命名 DTO、协议必填材料、邮箱与自我线程泛化边界、固定安全根路由、信息目标构造、父子归属、名称关系写入和需求结算证据；形成十二项默认裁决及五段依赖。 |
| 结构承载 | 正式扫描记录只承载当前事实、缺口、默认建议和候选文件；未新增节点、主信息、关系、索引、状态、动态、需求、任务、消息或缓存。 |
| 输出变化 | 确认命名需求不得复用服务值特征或把概念当存在宿主；默认以自我为主体 / 宿主、服务根为父需求、专用目标概念关系和共享信息目标状态承载，并按协议、目标建模、创建去重、绑定回执、正式结算继续拆分；#197 解锁。 |
| 调用路径 | `待命名请求 -> 专用非权威治理消息 -> 当前活动版本复核 -> 信息目标需求创建 / 唯一复用 -> 服务根挂载 -> 名称关系读回 -> 任务 / 动态证据 -> 正式需求结算`。 |
| 验证方式 | 定向 `rg` 与短文件读取；Git 基线 clean；`git diff --check`、规范治理和 C++ / 工程差异零命中检查；按计划未运行构建或程序。 |
| 禁止扩大声明 | 不证明命名需求、命名本能、名称选择算法、语言表达闭环、名称绑定、需求结算、自动命名或旧能力迁移完成。 |

## 171. 本次追加文档治理：概念支持失效前置与命名需求连续计划

| 项 | 内容 |
| --- | --- |
| 输入入口 | #196 正式命名接线事实、#197 执行窗口可达性漂移、#186 活动图、#195 生命周期候选、当前关系仓库 / 概念图 / 语素 / 需求 / 任务 / 方法与自我治理协议。 |
| 核心处理 | 追认普通概念全部有效支持导致 #197 前置不可达；设计不删原关系的已失效状态、审计读取、活动支持投影和版本推进，生成 #210 并修订 #197；同时生成命名协议、信息目标、唯一需求、绑定回执和正式结算五段流程图 / 详细设计及 #211-#215。 |
| 结构承载 | 两对流程图 MD+HTML、两份详细设计、六份计划和生成断点只承载设计与执行路由；未新增或修改机器结构。 |
| 输出变化 | #197 改依赖 #210；#210-#215、DQ-102-DQ-107、QR-167、JY-228 已登记。#210 完成后先回 #197；#214-#215 等待 #209 后继真实任务 / 方法接口。 |
| 调用路径 | `#197 不可达 -> #210 支持关系已失效 / 活动版本推进 -> #197 真实零支持生命周期验收`；`#196 -> #211 协议 -> #212 信息目标 -> #213 唯一需求 -> #214 名称绑定回执 -> #215 任务结果 / 需求结算`。 |
| 验证方式 | 当前事实定向读取、两对流程图同图检查、路径 / 范围 / 编号 / 章节机械检查、`git diff --check`、规范治理和精确 staged scope；按设计窗口规则不运行构建或程序。 |
| 禁止扩大声明 | 不证明支持失效、生命周期转换、命名协议、命名需求、名称绑定、任务结果、需求结算、自动命名、删除、恢复或旧能力迁移已经实现。 |

## 172. 本次追加文档治理：CONCEPT-S6D 双锁顺序文字漂移修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | #210 执行前实际接口复核报告、当前 `概念图服务.h` 三处双锁路径、支持失效流程图 / 详细设计 / 计划、`规范/多线程防锁机制规范.md`。 |
| 核心处理 | 复核确认当前 `初始化活动概念生命周期`、`初始化活动图基础版本`、`发布候选图版本` 均按 `活动图锁_ -> 图写锁_` 加锁；把原详细设计的反向文字、流程图未定序和计划缺失门禁统一修订，并把反向路径列为 ABBA 结构缺陷。 |
| 结构承载 | 流程图 MD+HTML、详细设计、#210 计划、计划索引与项目记忆只承载设计和治理事实；未修改 C++、仓库结构或运行期锁。 |
| 输出变化 | JY-229 已登记；#210 / DQ-102 编号和业务范围不变，执行窗口继续其余实际接口复核；QR-167 补记锁序，无新增待确认问题。 |
| 调用路径 | `执行前复核 -> 三处当前双锁路径 -> 固定活动图锁_后图写锁_ -> 支持失效与候选发布同序 -> 静态与交错并发验收`。 |
| 验证方式 | 定向代码读取、反向锁序残留扫描、MD / HTML 同图检查、`git diff --check`、规范检查和精确 staged scope；按设计窗口规则不运行构建或程序。 |
| 禁止扩大声明 | 不证明 #210 已实施、并发压力已通过、支持失效已实现、#197 已解锁、stash 已恢复或旧能力已迁移。 |

## 173. 本次追加文档治理：CONCEPT-S6D 根支持失效范围冲突修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | #210 执行前范围复核报告、四类候选输入的根支持准入、候选发布根支持保留、四根生命周期和支持失效三件套。 |
| 核心处理 | 区分同一 `实例支持概念` 类型下的根支持与普通概念支持：根支持承载实例进入对应概念域的准入，普通概念支持才是 #197 需要清零的活性依据；裁决 #210 只允许后者失效，四根目标写前拒绝。 |
| 结构承载 | 流程图 MD+HTML、详细设计、#210 计划、计划索引与项目记忆只承载设计和治理事实；未修改 C++、关系、活动快照或根生命周期。 |
| 输出变化 | JY-230 已登记；#210 / DQ-102 编号不变，新增 A13 根支持保护验收；QR-167 补记范围裁决，无新增待确认问题。 |
| 调用路径 | `失效请求 -> 当前活动普通概念准入 -> 根目标写前拒绝 / 普通目标进入失效 -> 根支持与候选准入保持`。 |
| 验证方式 | 当前代码定向读取、三件套冲突残留扫描、MD / HTML 同图、`git diff --check`、规范检查和精确 staged scope；按设计窗口规则不运行构建或程序。 |
| 禁止扩大声明 | 不证明 #210 已实施、A13 已运行通过、根支持生命周期、实例退役 / 删除、#197 解锁、stash 恢复或旧能力迁移完成。 |

## 174. 本次追加实施：CONCEPT-S6D 活动实例支持权威失效与投影推进

| 项 | 内容 |
| --- | --- |
| 输入入口 | #210、#186 活动图、#195 生命周期候选、JY-229 锁序修订、JY-230 根支持范围修订，以及隔离在命名 stash 中的 #197 六份草案。 |
| 核心处理 | 追加 `记录状态::已失效 = 3`；关系仓库新增值式失效、显式审计、值式重挂新句柄和节点相关有效关系读取；概念图服务在双锁前拒绝四根，按 `活动图锁_ -> 图写锁_` 复核精确支持、失效关系、移除活动投影、推进版本并清除固定点 `上一候选图_`。生命周期复核候选只作调用期值式 DTO，因活动版本变化而过期。 |
| 结构承载 | 关系仓库以同一关系编号的新版本承载失效历史；普通读取和活动快照只看有效关系，审计入口显式读取已失效记录；活动版本裁决候选当前性；请求和回执只作值式材料。 |
| 输出变化 | 当前活动普通概念支持可保留历史地失效并达到真实零有效支持；同端点后续发布创建新有效关系，旧审计保留；四根支持保持有效；#197 业务前置满足，待 #210 推送后恢复草案适配。 |
| 调用路径 | `显式请求 -> 根登记 / 类别拒绝 -> 活动图锁_ -> 图写锁_ -> 快照 / 登记 / 仓库重读 -> 预构造候选 -> 关系失效 -> 普通 / 审计读回 -> 快照交换发布 -> 活动版本加一`。 |
| 验证方式 | Debug x64 0 警告 0 错误；A01-A13 与完整自检通过；完整自检连续 20 轮均返回 0，每轮含同关系 50 次并发失效和失效 / 候选发布 50 次交错；静态反向锁序零命中，规范与差异检查通过。 |
| 禁止扩大声明 | 不证明四根支持失效、自动实例失效、实例退役 / 删除、生命周期转换、主动召回、物理删除、跨重启恢复、完整概念治理、自我循环或旧能力迁移完成。 |

## 175. 本次追加文档治理：CONCEPT-S7 验收编号与零支持前置链修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | #197 正式计划 A01-A08、生命周期详细设计旧验收矩阵、生命周期流程图、#210 正式实施记录，以及执行窗口治理前漂移报告。 |
| 核心处理 | 以 #197 计划为唯一验收编号基线，把详细设计 A01-A08 改为真实零支持、候选版本换代、状态转换、根 / 幂等、召回过滤和并发；流程图补入 #210 支持失效、活动版本推进、旧值式候选过期和当前候选重算链，并拆开写前拒绝 / 未达零支持的逻辑内返回与零支持后候选异常的追根因分支。明确每条 #210 请求独立原子：本次拒绝不写，已完成合法失效保持，不新增跨多关系回滚事务。 |
| 结构承载 | 流程图 MD+HTML、生命周期详细设计、JY-232 和治理索引只承载设计与执行路由；本窗口未修改 C++、执行窗口两份规范或 #197 计划。 |
| 输出变化 | #197 / DQ-89 编号不变，无新计划；三件套 A01-A08 现可逐项映射，执行窗口可基于修订提交继续治理收口。 |
| 调用路径 | `#210 正式支持失效 -> 真实普通概念零支持 -> 活动版本推进 -> 旧 #195 候选过期 -> 当前候选重算 -> S7 权威复核与生命周期转换`。 |
| 验证方式 | 正式计划 / 详细设计 A01-A08 逐项映射、MD / HTML 同图、前置链残留扫描、`git diff --check`、规范检查和精确 staged scope；按设计窗口规则不运行构建或程序。 |
| 禁止扩大声明 | 不证明 #197 已治理收口、A01-A08 已由本窗口运行、计划已归档、#198 已解锁、安全删除或旧概念能力迁移完成。 |

## 176. 本次追加实施：CONCEPT-S7 冷却退役与重新激活

| 项 | 内容 |
| --- | --- |
| 输入入口 | #197 正式计划、生命周期流程图与详细设计、#194 生命周期初态、#195 生命周期复核候选、#210 支持失效与活动版本推进正式接口，以及 JY-232 统一后的 A01-A08 前置链。 |
| 核心处理 | 新增调用期值式 `概念生命周期转换材料`、`转换概念生命周期` 和 `读取主动召回概念节点组`；在 `活动图锁_ -> 图写锁_` 内重读当前生命周期、活动版本、有效支持、外部正式引用和用途材料，复用原生命周期关系编号并以新版本重挂目标阶段。根离开活跃、非法跳转和同状态请求在写前拒绝或幂等返回；重新激活必须基于当前有效支持 / 引用 / 用途材料。 |
| 结构承载 | 唯一有效概念生命周期关系继续承载权威阶段，关系编号保持、版本加一；活动快照承载当前有效实例支持，统计服务只提供非权威观察材料，转换结果和候选只作调用期值式 DTO。冷却 / 退役节点、签名、名称、上下位和树投影均保留。 |
| 输出变化 | 普通概念可按 `活跃 -> 冷却 -> 退役` 合法推进，并可在正式新支持候选发布后从冷却 / 退役回到活跃；主动召回只返回活跃概念，活动全图与只读树仍包含冷却 / 退役概念。#198 前置已满足。 |
| 调用路径 | `#210 逐条支持失效 -> 活动版本推进 -> 旧候选过期 -> #195 重算当前候选 -> S7 双锁内权威复核 -> 生命周期关系值式重挂 -> 新句柄 / 版本 / 目标读回`；重新激活路径为 `正式候选发布新支持 -> 当前材料复核 -> 生命周期转活跃`。 |
| 验证方式 | CONCEPT-S7-A01 至 A08 全部通过；Debug x64 构建 0 警告 0 错误；完整 `--self-test-exit` 返回 0；最终代码连续 20 轮完整自检均返回 0，总用时约 21.5 秒；并发转换与支持发布保持可解释线性化结果；`git diff --check` 和 `tools/check_specs.py` 通过。 |
| 禁止扩大声明 | 只证明概念冷却、退役、重新激活和主动召回过滤第一轮；不证明自动定时治理、支持自动失效、根支持生命周期、实例退役 / 删除、安全物理删除、命名需求、跨重启恢复或旧能力迁移完成。 |

## 177. 本次追加扫描：CONCEPT-S8-S0 安全物理删除当前代码事实复核

| 项 | 内容 |
| --- | --- |
| 输入入口 | #198、#197 正式实施记录、概念图总设计与生命周期设计、写入事务详细设计、仓库快照恢复拒绝矩阵，以及 HEAD `023ce43` 的节点 / 主信息 / 关系 / 索引 / 写入事务 / 概念图服务 / 固定点算法实际代码。 |
| 核心处理 | 逐项扫描根保护、退役准入、实例支持、外部引用、下位重挂、关系失效、节点 / 主信息 / 索引清理、活动图发布、失败隔离和删除后固定点十类能力；区分“删除候选可计算”与“物理删除可原子提交”。额外确认关系创建和索引绑定在节点有效性检查后才取得各自仓库锁，和节点删除之间存在跨仓库 TOCTOU。 |
| 结构承载 | 本轮只生成值式事实矩阵和后续依赖建议，不新增机器结构。当前权威事实仍分别由节点、主信息、关系和生命周期关系承载；活动快照只承载概念图服务会话期投影，写入事务只承载状态壳，统计缓存和快照评估都不裁决删除。 |
| 输出变化 | JY-234 确认当前固定点、根 / 退役 / 支持 / 引用读取可作为只读删除候选基础，但没有端点冻结、跨仓库写集、回滚 / 补偿或统一读取隔离，也没有主信息 / 反向索引 / 服务登记原子清理。禁止直接进入物理删除代码；后续按删除候选、事务隔离与清理、删除提交、删除后固定点依赖链推进。 |
| 调用路径 | `退役普通概念 -> 活动版本 / 生命周期 / 支持 / 外部引用复核 -> 值式移除目标并重算传递约简候选 -> 事务与清理能力门禁`；当前只到门禁结论，未调用任何删除写入口。 |
| 验证方式 | 当前 HEAD 和 clean worktree 复核；定向读取各仓库、写入事务、概念图服务、固定点算法、四类原子服务、语素 / 需求 / 任务 / 方法 / 统计与恢复边界；`git diff --check` 和 `tools/check_specs.py` 用于治理收口；按计划未构建、未运行程序。 |
| 禁止扩大声明 | 不证明删除候选代码、跨仓库事务、补偿、读取隔离、原子清理、安全物理删除、恢复或旧能力迁移已实现。 |

## 178. 本次追加文档治理：概念安全删除专用三件套与连续计划生成

| 项 | 内容 |
| --- | --- |
| 输入入口 | #198 / JY-234 正式 S0、节点 / 主信息 / 关系 / 索引 / 写入事务 / 概念图 / 固定点当前代码事实，以及用户要求续接安全删除专用三件套与前置计划链。 |
| 核心处理 | 固定第一版非根退役零支持零外部关系准入；完整当前输入全量重算；单进程共享 / 独占结构事务域；同域令牌显式下传；结构所有者预构造清理包；唯一提交点无分配发布；活动快照最后发布；显式审计、幂等和等价新句柄重建。 |
| 结构承载 | 本轮只由流程图 MD+HTML、详细设计、五份依赖门控计划、计划索引和项目记忆承载设计 / 路由事实；没有新增机器结构。 |
| 输出变化 | #216-#220、DQ-108-DQ-112、QR-168 和 JY-235 已登记；执行当前项仍为 #199，#216 串行排在 #215 后。 |
| 调用路径 | `#198 S0 -> #216 完整删除候选 -> #217 结构事务域 -> #218 所有者清理适配 -> #219 安全删除提交 -> #220 后验固定点与等价重建验收`。 |
| 验证方式 | MD / HTML Mermaid 完全一致；详细设计与五份计划验收矩阵逐字映射；`git diff --check`、规范检查、治理编号与精确暂存范围收口；按设计窗口规则不运行构建或程序。 |
| 禁止扩大声明 | 不证明 #216-#220 已实施、端点冻结或原子清理已形成、安全物理删除已完成、崩溃 / 跨重启恢复、完整 ACID、高并发性能或旧能力迁移完成。 |

## 179. 本次追加实施：CONCEPT-S5 活动图发布与抽象树投影中途返回纠偏

| 项 | 内容 |
| --- | --- |
| 输入入口 | #199 正式计划、S5 现状流程图、总体施工流程图、概念图详细设计、输入契约、非成功二分审查、偏差清单，以及 #187 / #198 已完成实际接口。 |
| 核心处理 | 新增 `候选图发布状态` / `候选图发布结果` 和抽象树读取状态 / 结果；拆开过期、未收敛、空候选、正式候选内部错误、创建 / 生命周期 / 映射 / 关系 / 读回 / 清理异常；首次异常点追根因，清理异常返回残留句柄。活动快照替换是提交点，提交后旧边异常返回已生效版本和异常关系句柄。单树承接任意请求，全部四树承接正式契约。 |
| 结构承载 | 发布和树读取原因只由调用期值式 DTO 承载；活动图仍由服务内活动快照承载，节点 / 关系 / 生命周期权威结构不改所有权。兼容 `optional` 入口保留但不裁决失败原因。 |
| 输出变化 | #199 / DQ-91 / JY-236 已完成；A01-A11 全部通过并归档计划。#200 的串行前置已满足，成为当前下一项；#216 后续执行前可复核 #199 正式结果接口。 |
| 调用路径 | `S4 候选 -> 结构化发布入口 -> 基础图 / 基准 / 准入 -> 候选构造 -> 发布前读回 -> 活动快照提交点 -> 旧边收口 -> 发布结果`；树读取分为任意单树和正式全部四树两条入口。 |
| 验证方式 | Debug x64 构建 0 警告 0 错误；完整 `--self-test-exit` 返回 0；CONCEPT-S5-CORR-A01 至 A11 和 S6D-A11 回归通过；连续 20 轮自检 20/20 通过、用时 21.695 秒；两套 Mermaid 同图、专项流程图检查无 ERROR、规范检查通过。 |
| 禁止扩大声明 | 只证明 S5 中途返回、失败收口、提交点回执和树读取契约已纠偏；不证明 S6-S8 新行为、安全物理删除、跨仓库事务、恢复、完整概念系统、自我循环或自我苏醒完成。 |

## 180. 本次追加实施：EVENT-SEGMENT-S1 结构化事件段持久化与只读校验

| 项 | 内容 |
| --- | --- |
| 输入入口 | #200 正式计划、事件段流程图与详细设计、日志系统规范、现有 `事件材料` / 准入 / UTF-8 / 日志目录，以及事件线程和仓库快照只读事实。 |
| 核心处理 | 新增事件段配置、状态、拒绝原因、追加 / 读取 / 路径结果和独立核心服务；逐字段小端编解码，FNV-1a 64 校验，服务锁内连续序号，完整临时段写穿、严格读回、同卷替换和最终读回。 |
| 结构承载 | `日志/事件段/*.bin` 只承载持久化 / 恢复候选；运行期事实仍由节点、主信息、关系、索引和领域服务承载。事件段结果是调用期值式 DTO，不进入机器事实。 |
| 输出变化 | #200 / DQ-92 / QR-165 / JY-237 已完成；A01-A10 通过并归档计划。#201 的队列与业务前置均已满足，成为当前下一项；#202 的事件段业务前置已满足。 |
| 调用路径 | `事件材料 -> 入口准入 -> 服务锁内分配序号 -> 完整候选编码 -> .tmp 写穿 -> 临时严格读回 -> 同卷替换 -> 最终严格读回 -> 值式结果`；当前仅入口自检显式调用。 |
| 验证方式 | Debug x64 0 警告 0 错误；完整自检退出码 0；EVENT-SEGMENT-A01 至 A10 通过；最终代码连续 10 / 10 轮通过、用时 35.713 秒；规范、差异、禁止目录和自检残留检查通过。 |
| 禁止扩大声明 | 不证明事件日志线程接线、领域事件覆盖、事件裁决当前事实、快照、恢复、重放、轮换、压缩、加密、多进程写入、跨重启完整恢复或旧日志能力迁移完成。 |

## 181. 本次追加扫描：TASK-LIFECYCLE-S0 任务生命周期与通用执行当前代码事实复核

| 项 | 内容 |
| --- | --- |
| 输入入口 | #201 正式计划、#192 / #193 实际产物、任务系统规范与既有流程设计，以及当前任务 / 需求 / 状态 / 方法服务、任务线程、回执、上行桥、自我线程、固定安全根路由和入口调用点。 |
| 核心处理 | 只读登记入口 / 调用方、生命周期写读、当前态歧义、筹办召回选择断点、执行请求到回执 / 上行 / 落账断点、固定安全根差异和中途非成功返回二分；给出当前态、合法迁移、筹办、通用执行和完成结算五段建议。 |
| 结构承载 | 正式断点清单只承载人读代码事实与后续设计依据，没有新增机器结构。现有任务生命周期仍由实例状态节点和任务引用关系承载，线程 / 消息 / 回执状态均不承载机器事实。 |
| 输出变化 | #201 / DQ-93 / JY-238 已完成并归档计划；确认当前无唯一任务当前态、合法迁移和通用正式执行落账。#202 串行前置已满足并成为当前下一项；#209 的业务前置 #201 已满足。 |
| 调用路径 | `#201 -> 前置正式记录 -> 当前代码 / 调用点扫描 -> 权威与非权威边界 -> 当前态歧义 / 执行断点 -> 五段建议 -> 治理同步`。 |
| 验证方式 | 定向代码与正式记录扫描；`git diff --check`、规范治理和 C++ / 工程零差异检查；按计划不构建、不运行程序。 |
| 禁止扩大声明 | 不证明当前态投影、合法迁移、通用筹办、方法召回、任务授权、自动选择、通用执行、强类型回执自动生成、正式结果上行、通用完成结算或旧任务能力迁移完成。 |

## 182. 本次追加文档治理：任务生命周期通用执行新三件套与连续计划生成

| 项 | 内容 |
| --- | --- |
| 输入入口 | #201 / JY-238 正式事实记录、QR-169 默认裁决、任务 / 方法 / 状态 / 需求服务、管理 / 工作线程、强类型回执和上行桥当前代码，以及 #203-#220 已登记依赖。 |
| 核心处理 | 固定关系 14 的唯一当前态与版本审计、合法迁移和终态、只读建议到任务服务选择、强类型冻结请求与受控执行、非权威回执、结构化结果比较、任务完成与需求独立结算；同步消除 #216 对 #215 的错误串行依赖。 |
| 结构承载 | 本轮只由流程图 MD+HTML、详细设计、五份依赖门控计划、计划索引和项目记忆承载设计 / 路由事实；没有新增机器结构。 |
| 输出变化 | #221-#225、DQ-113-DQ-117 和 JY-239 已登记；QR-169 已收口。#223 是 #209 明确后继并精确解锁 #214，#225 精确供给 #215；#216 只依赖已完成 #198 / #199。 |
| 调用路径 | `#201 S0 -> #221 当前态 -> #222 合法迁移 -> #223 筹办 / 建议 / 选择 -> #224 调度 / 执行 / 回执 -> #225 结果 / 完成 / 结算 -> 回扫 #214 / #215`；#216-#220 可独立推进并为 #221 提供 #217。 |
| 验证方式 | MD / HTML Mermaid 同图、详细设计与五份计划验收逐字映射、依赖无环、计划依据 / 允许禁止范围检查、`git diff --check` 和规范检查；按设计窗口规则不构建、不运行程序。 |
| 禁止扩大声明 | 不证明 #221-#225 已实施、任务当前态 / 状态机、通用召回选择、线程真实执行、结果结算、安全删除、恢复、持续自我循环、自我苏醒或旧能力迁移完成。 |

## 183. 本次追加扫描：PERSIST-RECOVERY-S0 权威状态持久化恢复当前代码事实复核

| 项 | 内容 |
| --- | --- |
| 输入入口 | #202 正式计划、#191 / #197 / #198 / #200 实际产物、main d7d85a9、四仓库与事务 / 快照服务、特征值 / 概念图服务、初始化、事件 / 缓存 / 线程 / UI / SQL 和入口启动代码。 |
| 核心处理 | 只读枚举四仓库全状态、高水位、墓碑、特征值侧表、概念登记 / 活动版本、系统初始化角色、稳定键、非权威排除、版本映射、防复活、隔离上下文构建、并行边界、一次发布、失败二分和后续五段建议。 |
| 结构承载 | 正式断点清单只承载人读代码事实和恢复设计前置，没有新增机器结构；事件段、缓存、线程消息、控制面板和 SQL 继续不裁决恢复事实。 |
| 输出变化 | #202 / DQ-94 / JY-240 已完成并归档计划；#203 队列前置已满足。恢复专用流程图、详细设计和计划链须由设计计划窗口据本记录生成。 |
| 调用路径 | `#202 -> 前置正式产物 -> 四仓库 / 侧表 / 初始化 / 非权威扫描 -> 版本与防复活矩阵 -> 隔离构建 / 并行处理 / 一次发布草案 -> 五段建议 -> 治理同步`。 |
| 验证方式 | 定向代码与正式记录扫描；`git diff --check`、规范治理和 C++ / 工程零差异检查；按计划不构建、不运行程序，不生成或读取生产快照。 |
| 禁止扩大声明 | 不证明仓库快照、隔离恢复、领域重建、事件重放、启动恢复、任务续跑、安全物理删除、崩溃 / 断电恢复、自我循环、自我苏醒或旧持久化能力迁移完成。 |

## 184. 本次追加文档治理：权威状态持久化恢复专用三件套与连续计划生成

| 项 | 内容 |
| --- | --- |
| 输入入口 | #202 / JY-240 正式 S0、QR-170 默认边界、#191 / #198 / #201 实际记录、四仓库 / 快照 / 事务 / 特征值 / 概念图 / 初始化 / 线程 / UI / SQL 当前事实，以及 #203-#225、#217 / #220 已登记依赖。 |
| 核心处理 | 固定九段 `HYSNAP01`、显式小端和规范排序；#217 同域独占许可内快速值式复制、冻结外并行编码 / 解析和原子文件发布；按指定身份、高水位、墓碑构造隔离四仓库；恢复侧表 / 角色 / 所有者 / 领域登记并重建活动图；任务恢复中断迁移、跨段屏障、空宿主首次发布和防复活并发验收。 |
| 结构承载 | 本轮只由流程图 MD+HTML、详细设计、五份依赖门控计划、计划索引和项目记忆承载设计 / 路由事实；没有新增机器结构。 |
| 输出变化 | #226-#230、DQ-118-DQ-122 和 JY-241 已登记；QR-170 已收口。#226 硬门控 #217、#220、#203-#225 实际完成，后续四段串行依赖；当前执行项仍为 #203。 |
| 调用路径 | `#202 S0 -> #226 统一冻结 / 快照发布 -> #227 严格解析 / 隔离四仓库 -> #228 领域侧表 / 角色 / 活动图 -> #229 任务中断 / 屏障 / 上下文首发 -> #230 失败不变式 / 防复活 / 并发验收`。 |
| 验证方式 | MD / HTML Mermaid 同图；详细设计与五份计划 A01-A16、A01-A16、A01-A18、A01-A16、A01-A18 逐字映射；依赖无环、允许 / 禁止文件、入口拒绝、失败二分、实际接口复核和漂移退回检查；`git diff --check` 与规范检查；按设计窗口规则不构建、不运行程序。 |
| 禁止扩大声明 | 不证明 #226-#230 已实施、HYSNAP01 已发布或恢复、运行期上下文已首发、事件重放、在线热替换、跨版本迁移、强杀 / 断电耐久、任务恰好一次、自我苏醒或旧持久化能力迁移完成。 |

## 185. 本次追加实施：METHOD-RECALL-S1 方法首权威登记与生命周期基线

| 项 | 内容 |
| --- | --- |
| 输入入口 | #203 正式计划、通用方法召回流程图与详细设计、#193 / #202 正式记录，以及当前方法、状态、存在、关系和入口调用点。 |
| 核心处理 | 建立稳定非名称方法登记根和共享活跃 / 失效状态；方法首最后写根归属关系发布；失效原地重挂生命周期关系并推进版本；批量枚举稳定排序，写入口串行、读取返回副本、逻辑错误锁外诊断。 |
| 结构承载 | 方法角色根、根到共享状态模板关系、根到方法首归属关系、方法首生命周期模板关系和关系版本承载权威登记与生命周期；服务内根材料只作运行期定位。 |
| 输出变化 | #203 / DQ-95 / JY-242 已完成并归档计划；失效方法审计可读但不进入活跃发现、方法候选或动作读取。#204 前置已满足并成为当前下一项。 |
| 调用路径 | `入口初始化登记根 -> 创建方法首及活跃关系 -> 最终归属发布 -> 单项 / 全部 / 活跃读取 -> 显式失效 -> 审计读回`。 |
| 验证方式 | METHOD-RECALL-S1-A01 至 A12、Debug x64 0/0、完整自检退出码 0、最终连续 20 / 20 轮用时 82.514 秒、规范 / 差异 / 范围 / 锁边界扫描。 |
| 禁止扩大声明 | 不证明条件 / 结果规格、结果能力索引、通用召回、条件复判、确定性选择、任务授权、方法学习、物理删除、恢复、完整方法系统或旧能力迁移完成。 |

## 186. 本次追加实施：METHOD-RECALL-S2 条件结果结构化规格与版本读回

| 项 | 内容 |
| --- | --- |
| 输入入口 | #204 正式计划、通用方法召回流程图与详细设计、#203 正式登记 / 生命周期接口，以及当前方法、特征、状态、关系和入口调用点。 |
| 核心处理 | 固定条件 / 结果规格关系槽位和规则版本；验证特征值归属与抽象状态目标；以最终关系发布不可改义规格；声明全部满足 / 显式无条件策略；复核关系句柄、原始版本和状态材料当前性；同方法条带化串行、不同方法并发。 |
| 结构承载 | 方法首到策略抽象状态的模板关系、条件节点到特征 / 期望值的引用关系、结果节点到目标特征 / 抽象状态的引用关系，以及完整关系句柄和原始值版本。 |
| 输出变化 | #204 / DQ-96 / JY-243 已完成并归档计划；失效方法审计可读但不可新增规格，旧来源材料可区分当前 / 已过期 / 无效。#205 前置已满足并成为当前下一项。 |
| 调用路径 | `已登记方法 -> 创建条件 / 结果节点 -> 登记结构化规格 -> 声明条件策略 -> 读取当前材料 -> 复核来源版本`。 |
| 验证方式 | METHOD-RECALL-S2-A01 至 A12、Debug x64 0/0、完整自检退出码 0、最终连续 20 / 20 轮用时 82.593 秒、规范 / 差异 / 范围 / 依赖 / 锁边界扫描。 |
| 禁止扩大声明 | 不证明派生索引、通用召回、条件满足、结果达成、确定性排序 / 选择、任务授权、方法执行、方法学习、物理删除、恢复、完整方法系统或旧能力迁移完成。 |

## 187. 本次追加实施：METHOD-RECALL-S3 结果能力派生多值索引

| 项 | 内容 |
| --- | --- |
| 输入入口 | #205 正式计划、通用方法召回流程图与详细设计、#204 正式登记 / 生命周期 / 结果规格 / 来源当前性接口，以及当前方法服务、核心索引和稳定动作键调用点。 |
| 核心处理 | 新增规则版本化完整结果能力键、一键多方法 / 结果索引项、重建候选、状态 / 桶 / 快照材料；方法服务一次扫描登记根聚合活跃完整结果规格；候选双次观察，锁外排序 / 去重 / 分桶 / 校验，索引锁内原子交换桶、计数和发布版本。 |
| 结构承载 | 专用服务内桶映射、派生计数和发布版本只承载非权威快照；方法登记、生命周期和结果规格继续由节点 / 关系 / 主信息及方法服务承载，核心索引仓库和稳定动作键不改义。 |
| 输出变化 | #205 / DQ-97 / JY-244 已完成并归档计划；可按完整结果能力键读取多个值式候选、识别来源过期、发布空快照并清空重建。#206 前置已满足并成为当前下一项。 |
| 调用路径 | `方法权威登记 / 结果规格 -> 一次根扫描来源组 -> 双次当前性观察 -> 锁外完整分桶 -> 索引锁内 swap -> 值式桶 / 快照 -> 权威来源复核`。 |
| 验证方式 | METHOD-RECALL-S3-A01 至 A12、Debug x64 0/0、完整自检退出码 0、最终连续 20 / 20 轮用时 82.921 秒；每轮含 50 次清空、50 次重建和四线程读取；规范 / 差异 / 范围 / 依赖 / 锁边界扫描通过。 |
| 禁止扩大声明 | 不证明任务目标召回、运行上下文条件复判、排序 / 选择、任务授权、方法执行、方法学习、持久化、恢复、完整方法系统或旧能力迁移完成。 |

## 188. 本次追加文档治理：FS02 第三批动态模式因果候选与学习接线连续计划生成

| 项 | 内容 |
| --- | --- |
| 输入入口 | 当前 main `50f6c10`、迁移大纲、FS-02 逻辑迁移包与 7 月 7 日两份降权候选设计、动态 / 因果 / 特征 / 二次特征 / 统计 / 方法当前代码，以及 #224 / #225 假定接口。 |
| 核心处理 | 先形成第三批服务矩阵；把观察回合拆为显式片段，规范语义特征和动态步，以完整片段键跨回合聚类；再按同回合片段顺序统计原因机会、正负结果与 Q10000 概率；最后把概率和结果效用分开，后置用途观察与方法学习接线复核。 |
| 结构承载 | 本轮只由服务矩阵、流程图 MD+HTML、详细设计、五份依赖门控计划、迁移大纲、计划索引和项目记忆承载设计 / 路由事实；没有新增机器结构。全部未来候选均限定为可丢弃值式材料。 |
| 输出变化 | #231-#235、DQ-123-DQ-127 和 JY-245 已登记，QR-171 已收口。#231 等待 #225；#232-#234 逐段依赖；#235 等待 #234、#225。当前执行项仍为 #206。 |
| 调用路径 | `#225 -> #231 更新事实 -> #232 规范回合 / 片段窗口 -> #233 跨回合重复聚类 / 运动基元 -> #234 机会 / 结果概率 -> #235 用途 / 方法学习接线复核`。 |
| 验证方式 | MD / HTML Mermaid 同图；详细设计与五份计划 12 / 16 / 14 / 16 / 12 条验收逐字映射；依赖无环、允许 / 禁止文件、入口拒绝、失败二分和实际接口漂移退回检查；`git diff --check` 与规范检查。按设计窗口规则未构建、未运行程序。 |
| 禁止扩大声明 | 不证明 #231-#235 已执行、动态模式 / 运动基元 / 因果概率已形成、因果已证明好用、方法已学习、抽象节点已生成、候选已持久化或旧能力已迁移。 |

## 189. 本次追加实施：METHOD-RECALL-S4 任务目标只读候选召回

| 项 | 内容 |
| --- | --- |
| 输入入口 | #206 正式计划、通用方法召回流程图与详细设计、#205 正式结果能力索引，以及任务、需求、方法、状态和特征服务当前只读入口。 |
| 核心处理 | 连续两次读取并冻结任务 / 来源需求目标上下文；按完整结果能力键复制单桶；索引锁外逐项重读登记、生命周期、结果规格和来源版本；正常漂移过滤并建议重建，当前来源矛盾清空候选并追根因。 |
| 结构承载 | 新召回服务的请求、候选和候选组只承载调用期非权威值材料；权威任务、需求和方法事实仍由节点、关系、主信息及各领域服务承载。 |
| 输出变化 | #206 / DQ-98 / JY-246 已完成并归档计划；可按完整任务目标得到当前、无候选、需要重建、请求拒绝、索引不可用或内部矛盾的结构化候选组。#207 前置已满足并成为当前下一项。 |
| 调用路径 | `任务 / 来源需求 -> 双次上下文冻结 -> 完整结果能力键 -> 值式候选桶 -> 登记 / 生命周期 / 规格当前性复核 -> 稳定候选组`。 |
| 验证方式 | METHOD-RECALL-S4-A01 至 A12、Debug x64 0/0、完整自检退出码 0、最终连续 20 / 20 轮用时 83.268 秒；每轮含四线程各 50 次、共 200 次并发只读召回；规范 / 差异 / 范围 / 依赖 / 权威结构不变扫描通过。 |
| 禁止扩大声明 | 不证明运行上下文条件满足、语义排序 / 选择、任务授权、任务方法关系提交、方法执行、方法学习、持久化、恢复、完整方法系统或旧能力迁移完成。 |

## 190. 本次追加实施：METHOD-RECALL-S5 运行上下文条件复判

| 项 | 内容 |
| --- | --- |
| 输入入口 | #207 正式计划、通用方法召回流程图与详细设计、#206 正式请求 / 候选 / 来源版本，以及方法条件策略 / 规格和特征槽位 / 当前值公开只读入口。 |
| 核心处理 | 复核冻结请求和候选来源；按显式无条件 / 全部满足 / 未声明分流；逐项读取目标宿主槽位、唯一当前值和两侧原始版本，经特征服务完整比较；发布前后复核策略、条件节点、规格、槽位、当前值和版本。 |
| 结构承载 | 条件项、尚无完整规格的条件节点、整体结论和来源状态只承载非权威值材料；权威方法、任务、需求和特征事实继续由既有领域服务承载。 |
| 输出变化 | #207 / DQ-99 / JY-247 已完成并归档计划；S4 候选可区分全部满足、明确不满足、不可判定、材料过期或内部不一致，并可在 #208 使用前复核来源当前性。#208 前置已满足并成为当前下一项。 |
| 调用路径 | `S4 候选 -> 请求 / 方法来源复核 -> 条件策略 -> 目标宿主槽位 / 当前值 -> 完整内容比较 -> 来源再复核 -> 值式复判材料`。 |
| 验证方式 | METHOD-RECALL-S5-A01 至 A12、Debug x64 0/0、完整自检退出码 0、最终连续 20 / 20 轮用时 89.788 秒；每轮含四线程各 50 次、共 200 次并发复判；旧 FS-03 回归隔离和规范 / 差异 / 范围 / 依赖扫描通过。 |
| 禁止扩大声明 | 不证明语义排序、唯一选择建议、任务授权、任务方法关系提交、方法执行、方法学习、持久化、恢复、完整方法系统或旧能力迁移完成。 |

## 191. 本次追加实施：METHOD-RECALL-S6 确定性排序与只读选择建议

| 项 | 内容 |
| --- | --- |
| 输入入口 | #208 正式计划、通用方法召回流程图与详细设计、#206 完整候选组 / 索引发布版本，以及 #207 条件复判和来源当前性接口。 |
| 核心处理 | 重建完整候选批次，按候选身份 O(n log n) 归一 / 精确去重并逐项复核来源；按完整匹配、条件结论、同级策略证据形成语义键，最后以完整句柄稳定收口；按可建议项数量和首项语义输出无候选、唯一或并列建议。 |
| 结构承载 | 方法排序项、确定性排序材料、选择建议材料和来源状态只承载非权威值副本；权威任务、需求、方法、规格、特征和派生索引继续由既有领域服务承载。 |
| 输出变化 | #208 / DQ-100 / JY-248 已完成并归档计划；可输出稳定有序候选并复核来源，单可用候选给只读建议，语义并列不给建议且不写任务关系。#209 前置已满足并成为当前下一项。 |
| 调用路径 | `S4 完整候选组 + S5 全量复判组 -> 批次 / 来源复核 -> O(n log n) 归一去重 -> 语义排序 -> 来源再复核 -> 只读选择建议`。 |
| 验证方式 | METHOD-RECALL-S6-A01 至 A12、Debug x64 0/0、完整自检退出码 0、最终连续 20 / 20 轮用时 271.550 秒；每轮含 100 次随机排序和四线程共 100 次并发排序；规范 / 差异 / 范围 / 依赖扫描通过。 |
| 禁止扩大声明 | 不证明独立选择优先策略、策略唯一、任务当前态、任务授权、任务方法关系提交、方法执行、方法学习、持久化、恢复、完整方法系统或旧能力迁移完成。 |

## 192. 本次追加实施：METHOD-RECALL-S7-S0 任务授权选择接线当前代码事实复核

| 项 | 内容 |
| --- | --- |
| 输入入口 | #209 正式计划、#201 任务生命周期 S0、#203-#208 正式提交 / 实施记录、任务 / 方法 / 线程当前代码和 #223 假定契约。 |
| 核心处理 | 只读复核需求任务双向关系、任务历史状态与选择入口、S6 建议全部来源字段、方法失效和旧选择、动作执行桥、线程及固定安全根；形成状态、授权、字段映射、写前复核、旧选择和非成功二分矩阵。 |
| 结构承载 | 仅新增正式扫描记录并同步计划 / 项目治理；未新增节点、关系、状态、任务、方法、线程消息或其它机器结构。 |
| 输出变化 | #209 / DQ-101 / JY-249 已完成并归档计划。已确认双向关系结构真实存在但公开授权只读入口缺失，选择不读当前态，旧选择无关系版本 / 审计 / 换代，动作桥只认任意引用；S6 来源完整但任务服务反向依赖会成环。后续 JY-250 已完成 #223 无环扁平请求和当前选择准入设计修订；#211 进入 S0 后另发现接口漂移并停在写代码前。 |
| 调用路径 | `#201 + #203-#208 正式事实 -> 任务 / 方法 / 线程只读扫描 -> 授权 / 状态 / 建议 / 选择 / 动作桥矩阵 -> #223 设计修订`。 |
| 验证方式 | `git diff --check`、`tools/check_specs.py`、代码 / 工程差异为 0 和全调用点静态扫描；按计划未构建、未运行程序。 |
| 禁止扩大声明 | 不证明任务当前态、合法迁移、正式授权、选择提交、换代审计、通用执行、任务结果、需求结算、方法学习、持续自主循环或旧能力迁移完成。 |

## 193. 本次追加文档治理：TASK-PLANNING-S1 无环授权选择与动作桥接口修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | #209 / JY-249 正式 S0、任务生命周期与概念命名两套流程图 / 详细设计、#223、#214、#224 既有依赖门控计划，以及当前任务 / 方法头文件依赖事实。 |
| 核心处理 | 固定任务服务自有扁平选择请求和既有单向 include；双向授权返回两条关系完整句柄 / 版本；完整 S6 来源不得裁剪且只接受 `唯一可建议`；当前选择使用顺序号 30 关系完整句柄 / 版本并绑定筹办生命周期版本；补齐幂等、同版本冲突、正式重筹办换代审计和精确动作桥。 |
| 结构承载 | 本轮只由两对流程图 MD+HTML、方法系统规范、两份详细设计、三份既有计划、计划索引和项目记忆承载修订；没有新增节点、关系、任务、方法、协议、线程或其它机器结构。 |
| 输出变化 | #223 / DQ-115 已按 #209 修订，#214 / DQ-106 与 #224 / DQ-116 已同步精确接口；QR-172 与 JY-250 已登记。#223 继续等待 #221 / #222，不得提前实施。 |
| 调用路径 | `#208 完整 S6 建议 -> 方法召回侧重读 / 适配任务 DTO -> 任务服务重读双向授权 / 生命周期 / 全部来源 -> 当前选择关系 / 审计 -> 精确动作桥 -> #214 / #224`。 |
| 验证方式 | 两对 MD / HTML Mermaid 逐字比较；详细设计与 #223 / #224 验收表逐字比较；无环依赖、策略唯一、任意引用和来源裁剪定向扫描；`tools/check_specs.py`、`git diff --check` 与精确文件范围检查。按设计窗口规则未构建、未运行程序。 |
| 禁止扩大声明 | 不证明任务当前态、双向授权 DTO、选择提交 / 审计、动作桥、通用执行、名称绑定、结果结算或方法学习已经实现。 |

## 194. 本次追加文档治理：CONCEPT-NAMING-S1 语素只读依赖实际接口修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | #211 执行窗口 S0 漂移报告、当前 `概念图服务::读取待命名请求(节点句柄, const 语素服务&)`、`语素服务::读取概念名称入口组`、命名流程图 / 详细设计和 #211 计划。 |
| 核心处理 | 把桥依赖修订为概念图服务、语素服务和邮箱三项引用；沿现有双参入口重读名称为空条件；请求不存在时用语素反查区分已有名称与请求消失；保持协议编号、邮箱幂等、单请求边界和领域禁止修改不变。 |
| 结构承载 | 本轮只由命名流程图 MD+HTML、详细设计、#211 计划、断点清单、计划索引和项目记忆承载接口修订；没有新增节点、关系、消息、需求、任务、方法或其它机器结构。 |
| 输出变化 | #211 / DQ-103 已按当前接口修订；JY-251 与 QR-173 已登记。#211 前置满足，执行窗口可在复核正式提交后恢复代码实施。 |
| 调用路径 | `显式单请求 -> 读取待命名请求(概念, 语素) -> 存在则比较版本 / 根类别；不存在则反查名称入口组 -> 构造协议消息 -> 邮箱`。 |
| 验证方式 | MD / HTML Mermaid 逐字比较；双参读取、语素反查、领域文件禁止修改和无批量调用定向扫描；`tools/check_specs.py`、`git diff --check` 与精确暂存检查。按设计窗口规则未构建、未运行程序。 |
| 禁止扩大声明 | 不证明协议版本 2、命名上行桥、命名需求、名称绑定、任务结果或需求结算已经实现。 |

## 195. 本次追加实施：CONCEPT-S8-S1 删除候选与完整当前输入重建

| 项 | 内容 |
| --- | --- |
| 输入入口 | #216 正式计划、概念安全删除流程图与详细设计、#198 / #199 / #210 / #197 正式实现，以及当前概念图、生命周期、关系、索引、分类和统计接口。 |
| 核心处理 | 对退役普通概念执行请求、版本、预算、当前支持和外部正式引用门禁；同版本复制四根、全部签名、活动关系、生命周期和目标当前关系；锁外按完整输入并行重算直接边，校验删除后无环与四根可达，再次复核快照后形成只读影响候选。 |
| 结构承载 | 调用期值式完整输入、直接边、根可达摘要、待重算材料和结构所有者承载非权威候选；节点、主信息、关系、索引、生命周期和活动图继续承载权威事实，候选阶段固定为不可提交。 |
| 输出变化 | #216 / DQ-108 / JY-252 已完成并归档计划；可区分拒绝、待重算、内部不一致和领域影响已形成，并读回索引反查、分类原子服务、概念登记与统计缓存四类待补全所有者。#217 业务前置已满足，后由 JY-253 增加代码文件规则门控。 |
| 调用路径 | `退役普通概念请求 -> 写前门禁 -> 同版本完整当前输入复制 -> 锁外完整签名直接边重算 -> 删除后无环 / 四根可达 -> 当前性再复核 -> 不可提交影响候选`。 |
| 验证方式 | CONCEPT-S8-S1-A01 至 A10、Debug x64 0/0、完整自检退出码 0、最终连续 20 / 20 轮用时 277.3 秒；每轮四线程各 10 次候选读取，共 800 次并发只读；规范 / 差异 / 范围 / 禁止调用扫描通过。 |
| 禁止扩大声明 | 不证明删除事务、关系 / 主信息 / 节点物理清理、所有者补全、删除提交、失败回滚、快照恢复、完整概念系统、自我循环或旧能力迁移完成。 |

## 196. 本次追加文档治理：代码文件模块规则与后续计划安全修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户确认的目录、数据结构文件、真模块和 `功能.分类` 命名口径；执行窗口 #211 未提交草案；当前 `入口.cpp`、工程登记、#213 / #217 / #229 / #230、控制面板完成事实及迁移大纲。 |
| 核心处理 | 建立 CODE-FILE-01 规范及流程图 / 详细设计；把 #211 拆为生产桥与独立自检真模块；登记 #236 只读盘点并把 #217 置于盘点后设计修订门控；为 #213 增加 #217 直接前置和全公开读取前 / 后态隔离；把 RECOVERY-S3 拆成屏障许可、上下文首发、启动接线、自检验收四段；统一控制面板三分口径和逐能力迁移台账。 |
| 结构承载 | 本轮仅由规则、流程图、详细设计、计划、计划索引、项目记忆、逐能力台账和 JY-253 断点承载治理事实；未新增或修改节点、关系、状态、消息、模块代码、工程项或其它机器结构。 |
| 输出变化 | 新增 #236 / DQ-128、#237-#239 / DQ-129-DQ-131；#211 预留 JY-254 实施收口；#217 在 #236 与后续设计修订前不可执行；#213 直接依赖 #217；#230 直接依赖 #239。恢复链更新为 `#226 -> #227 -> #228 -> #229 -> #237 -> #238 -> #239 -> #230`。 |
| 调用路径 | `新文件需求 -> 数据结构 / 行为分类 -> 功能.分类 -> 真模块登记 / import -> 生产与自检分离 -> 入口最小调用`；`#211 -> #236 -> 设计修订 #217 -> 删除 / 任务 / 恢复后继`；`#228 -> #229 屏障许可 -> #237 首发 -> #238 接线 -> #239 验收 -> #230`。 |
| 验证方式 | 三对 MD / HTML Mermaid 逐字比较；RECOVERY-S3A-S3D 计划与详细设计验收矩阵逐字比较；队列编号、依赖、文件分类、控制面板三分口径和迁移台账字段定向扫描；`git diff --check` 与 `tools/check_specs.py`。按设计窗口规则未构建、未运行程序。 |
| 禁止扩大声明 | 不证明 #211 已按新模块路径完成、#236 已执行、入口已瘦身、#217 已修订或实现、#213 事务隔离已实现、RECOVERY-S3A-S3D 已实现、数据库恢复已完成或任何旧能力已经迁移。 |

## 197. 本次追加实施：CONCEPT-NAMING-S1 待命名治理协议与上行桥

| 项 | 内容 |
| --- | --- |
| 输入入口 | #211 正式计划、命名流程图 / 详细设计、JY-251 双服务只读接口、JY-253 CODE-FILE-01，以及当前概念图、语素、治理邮箱和自我线程公开接口。 |
| 核心处理 | 协议升级为版本 2并扩展目标概念 / 来源材料版本；单请求桥重读概念与语素当前状态，构造完整字段消息并提交唯一邮箱；独立自检模块承载正常、漂移、消失、已有名称、过期、重复、冲突和并发回归。 |
| 结构承载 | 协议消息值、目标概念和来源材料版本承载非权威治理材料；概念、名称和活动版本继续由既有领域结构承载。生产桥、自检分别由两个 `功能.分类` 真模块承载。 |
| 输出变化 | #211 / DQ-103 / JY-254 已完成并归档计划；协议版本 2 和单请求上行桥已实现，入口只保留最小上下文、一次调用和总结果。#236 已解锁为当前下一项。 |
| 调用路径 | `单个待命名请求 -> 概念图 + 语素双参重读 -> 名称入口反查 -> 协议消息 -> 有界治理邮箱`；自检从独立模块调用生产模块和现有公开入口。 |
| 验证方式 | CONCEPT-NAMING-S1-A01 至 A12、Debug x64 0/0、完整自检退出码 0、最终连续 20 / 20 轮用时 288.325 秒；模块 / 工程 / import、入口最小化、领域 0 修改、旧路径清零、差异与规范扫描通过。 |
| 禁止扩大声明 | 不证明命名需求、自动命名、目标状态、目标概念关系、名称绑定、任务结果、需求结算、自我循环或旧能力迁移完成。 |

## 198. 本次追加只读扫描：ENTRY-MOD-S0 入口与自检承载当前事实

| 项 | 内容 |
| --- | --- |
| 输入入口 | 正式 HEAD `db3a8ed`、`海中鱼巣/` 下全部 `.h/.cpp/.ixx`、工程登记、`入口.cpp`、CODE-FILE-01 和 #212-#235 计划。 |
| 核心处理 | 枚举 61 个源码和工程身份；区分真模块与历史 `.ixx`；按稳定锚点拆分入口；逐项复核活动计划的新文件分类、自检正文和入口用途。 |
| 结构承载 | `实施记录/20260711_ENTRY-MOD-S0_入口与自检承载当前代码事实复核_Codex断点清单.md`、归档计划、计划索引和项目记忆；没有机器结构变化。 |
| 输出变化 | #236 / DQ-128 / JY-256 已完成。确认 16 个真模块、4 个历史兼容初始化 `.ixx`；入口 15,903 行 / 1,055,402 字节；#212-#235 中 18 个旧代码计划必须修订，#213/#229/#230 保持方向，#231/#235 纯只读。 |
| 调用路径 | `正式 HEAD -> 文件 / 工程矩阵 -> 入口八段 -> 24 项计划冲突矩阵 -> 中央运行器 / 分批迁移 / 计划修订设计输入`。 |
| 验证方式 | ENTRY-MOD-S0-A01 至 A10；全路径计数、模块与工程扫描、入口指标、24 计划逐项读取、`git diff --check` 和 `tools/check_specs.py`。纯只读，未构建、未运行。 |
| 禁止扩大声明 | 不证明入口已瘦身、统一自检运行器已实现、旧初始化已模块化、#217 已修订或任何旧能力迁移完成。 |

## 199. 本次追加文档治理：FLOW-COVERAGE-S1 鱼巢逻辑与当前实现流程覆盖收口

| 项 | 内容 |
| --- | --- |
| 输入入口 | 旧鱼巢 `ef2cbbf` / dirty 差异、海中鱼巢 `70c73a7`、现有施工 / 现状图、#236 入口分段矩阵和实施记录。 |
| 核心处理 | 冻结两仓库证据；生成六条旧主链完整包；刷新 51 份当前生成包并校正实现状态；分类 121 个旧源码和 61 个当前源码；补 THREAD-S1 与当前总入口；形成最终收口矩阵。 |
| 结构承载 | 56 对现状图、51 份生成核查表、六条旧主链四表、THREAD-S1 四表、当前入口四表、源码分类与最终收口矩阵。 |
| 输出变化 | #240 / DQ-132 / JY-257 已完成并归档；施工图、当前事实、旧证据、排除 / 暂缓和未实施后继已经分开。 |
| 调用路径 | `Git / 源码 -> 现状图 -> 映射 / 契约 / 二分 -> 偏差 -> 文件分类 -> 最终覆盖矩阵`。 |
| 验证方式 | 6 / 6 旧包、51 / 51 生成包、THREAD-S1、当前入口与最终收口严格通过；56 对 MD/HTML 配对；规范、差异和禁止范围检查。未构建、未运行。 |
| 禁止扩大声明 | 不证明旧函数已全部迁移、代码已纠偏、入口已拆分、恢复、真实外设或旧能力迁移完成。 |

## 200. 本次追加文档治理：中央自检运行器与第一批领域自检下一批计划生成

| 项 | 内容 |
| --- | --- |
| 输入入口 | 用户“制定下一批计划”；#236 / #240 正式记录；当前 HEAD `aee3533` 的入口、工程与既有概念命名自检；线程、自我治理、概念生命周期、删除候选现状图；原 #217 计划和安全删除设计。 |
| 核心处理 | 先用现状图定位逻辑域，再从当前源码提取真实行为、149 项验收编号和共享夹具，以规范裁决迁移 / 禁止边界；形成逻辑提取引用矩阵、施工流程图、详细设计和 #241-#243 三份依赖门控计划；把 #217 改为生产 / 自检真模块并直接依赖 #243。 |
| 结构承载 | `实施记录/20260712_中央自检运行器与第一批领域自检逻辑提取引用矩阵.md`、流程图 MD+HTML、`规范/详细设计/中央自检运行器与第一批领域自检迁移详细设计.md`、三份计划、修订 #217、JY-258 断点、计划索引和项目记忆；没有新增机器结构。 |
| 输出变化 | #241 / DQ-133 成为下一可复核执行项；#242 / DQ-134 等待 #241；#243 / DQ-135 等待 #242；#217 / DQ-109 已修订并等待 #243。旧 `结构事务协调器.h/.cpp` 和入口 A01-A14 方案撤销。 |
| 调用路径 | `#236 / #240 -> 当前代码逻辑提取 -> #241 中央运行器 -> #242 线程 / 自我治理 119 项 -> #243 概念 18 项 -> #217 结构事务生产 / 自检模块 -> #218 后继`。 |
| 验证方式 | 流程图 MD / HTML Mermaid 同图；流程图 / 详细设计 / 逻辑矩阵 / 计划范围一致；编号和路径检查；`git diff --check` 与 `py -3 tools/check_specs.py --strict`。按设计窗口规则不构建、不运行程序。 |
| 禁止扩大声明 | 不证明中央运行器、领域自检迁移、入口瘦身、结构事务、安全删除、自我循环、恢复、真实外设或旧能力迁移已实现。 |

## 201. 本次追加实施：SELFTEST-RUNNER-S1 中央自检运行器基础

| 项 | 内容 |
| --- | --- |
| 输入入口 | #241 正式计划、#236 / #240 正式事实、概念命名自检值式上下文 / 报告、入口直接调用和工程模块登记。 |
| 核心处理 | 登记前拒绝非法材料；首次运行冻结并稳定排序；每个回调恰好执行一次且普通失败不短路；汇总登记、执行、通过、失败和失败编号组。 |
| 结构承载 | `海中鱼巣.自检.运行器` 真模块中的调用期登记组、值式单元结果和批次结果，不持久化、不写业务事实。 |
| 输出变化 | #241 / DQ-133 / JY-259 已完成并归档；概念命名 A01-A12 以顺序 400 接入；#242 已解锁为正式接口复核。 |
| 调用路径 | `入口最小上下文 -> 登记运行器合同 / 概念命名 -> 运行全部一次 -> 值式批次结果 -> 程序总结果`。 |
| 验证方式 | SELFTEST-RUNNER-S1-A01 至 A10、Debug x64 0 警告 0 错误、完整自检退出码 0、最终连续 20 / 20 轮退出码 0、规范和差异检查。 |
| 禁止扩大声明 | 不证明入口存量自检已全部迁出、完整自检体系、结构事务、安全删除、自我循环、自我苏醒、真实外设或旧能力迁移完成。 |

## 202. 本次追加文档治理：SELFTEST-MIGRATION-B1A 执行时序漂移与阶段化修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | `3baab3b` 的 #241 正式运行器与入口；执行窗口 #242 草案的两个新模块编译 0/0、THREAD 39/39、SELF-GOV 80/80、完整自检退出 1 和失败项回传。 |
| 核心处理 | 复核入口真实时序，确认 S4/S5 必须在面板与概念回归前形成真实结构、S3B 必须在后期领域材料形成后执行；把单一全局顺序修订为四阶段和阶段内顺序，并拆分迁移计划。 |
| 结构承载 | 修订流程图 MD+HTML、详细设计、逻辑提取矩阵、漂移断点、#244/#242/#245/#243 计划、JY-260、QR-175、计划索引和项目记忆；未新增机器结构。 |
| 输出变化 | #242 首次草案明确为已撤销未完成；新增 #244 / DQ-136 和 #245 / DQ-137；#242 缩为 66 项，#243 改为直接依赖 #245；正式路线改为 `#244 -> #242 -> #245 -> #243 -> #217`。 |
| 调用路径 | `完整自检失败 -> 当前源码锚点复核 -> 早期结构前置 / 中段线程夹具 / 后期领域材料分层 -> 阶段化总成 -> 依赖门控计划重排`。 |
| 验证方式 | 对照 `3baab3b` 行锚点和 149 项守恒；流程图 MD/HTML Mermaid 同图；计划 / 设计 / 矩阵数量、模块和依赖一致性；`git diff --check` 与 `tools/check_specs.py --strict`。设计窗口未运行 C++ 构建。 |
| 禁止扩大声明 | 不证明 #244/#242/#245/#243 已实现，不证明入口已瘦身、自我循环、结构事务、安全删除、恢复、真实外设或旧能力迁移完成。 |

## 203. 本次代码实施：SELFTEST-RUNNER-S2 阶段化自检总成

| 项 | 内容 |
| --- | --- |
| 输入入口 | #241 已实现的 `海中鱼巣.自检.运行器` 单批次合同、入口顺序 1 / 400 登记和 JY-260 阶段化修订材料。 |
| 核心处理 | 在既有模块追加四阶段强类型总成；阶段与阶段内顺序分离，支持渐进登记、全局唯一拒绝、严格单调一次执行、失败不短路、未执行登记不自动补跑和最终值式聚合。 |
| 结构承载 | `海中鱼巣.自检.运行器` 真模块中的调用期阶段登记组、阶段结果和总成结果；不持久化、不写业务事实。 |
| 输出变化 | #244 / DQ-136 / JY-261 已完成并归档；#241 单批次合同保持兼容；#242 解锁为正式接口复核。 |
| 调用路径 | `入口最小登记 -> S1 / S2 合同与概念命名自检 -> 单批次运行一次 -> 值式批次结果 -> 程序总结果`；阶段化总成供后继按真实锚点装配。 |
| 验证方式 | SELFTEST-RUNNER-S2-A01 至 A10、Debug x64 0 警告 0 错误、完整自检退出码 0、连续 20 / 20 轮退出码 0、规范与差异检查。 |
| 禁止扩大声明 | 不证明任何领域自检已迁移、入口已瘦身、结构事务、自我循环、自我苏醒、恢复、真实外设或旧能力迁移完成。 |

## 204. 本次追加文档治理：概念命名目标与安全删除后继阶段化修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | 两套概念施工流程图 / 详细设计、#236 冲突矩阵、`b33c602` 业务代码事实、`c924bf2` 四阶段总成事实与 JY-260。 |
| 核心处理 | 按逻辑流程提取共享命名目标、关系计数分流、结构所有者清理包、唯一删除提交点和删除后固定点；按 CODE-FILE-01 拆分生产 / 自检真模块，并把登记统一放入最终回归阶段 510-540。 |
| 结构承载 | 两套流程图 MD+HTML、两份详细设计、#212/#218/#219/#220 四份计划、逻辑提取矩阵、修订断点、JY-262、计划索引和任务队列；不新增机器结构。 |
| 输出变化 | #212 已解除旧设计修订门控并重新登记；#212、#218-#220 均为依赖门控待执行，正式交付路线为 `#212 -> #218 -> #219 -> #220`。 |
| 调用路径 | `规范 / 流程图 -> 详细设计 -> 当前代码与阶段接口事实 -> 逻辑提取矩阵 -> 文件归属 -> 依赖门控计划 -> 执行前实际接口复核`。 |
| 验证方式 | 四计划字段门禁通过；两套 MD/HTML Mermaid 逐字一致且含 Mermaid CDN；`git diff --check` 与 `tools/check_specs.py --strict` 通过。本批未运行 C++ 构建或程序。 |
| 禁止扩大声明 | #244 已完成不代表 #242、#245、#243、#217、#212 或 #218-#220 完成；不证明结构事务、安全删除、任务状态机、恢复、自我循环、真实外设或旧能力迁移完成。 |

## 205. 本次代码实施：SELFTEST-MIGRATION-B1A 线程协议阶段自检迁移

| 项 | 内容 |
| --- | --- |
| 输入入口 | `c924bf2` 的阶段化总成正式接口、入口现有 66 项和 JY-260 修订后的 100 / 200 / 300 / 400 顺序。 |
| 核心处理 | 建立运行线程与自我治理协议两个自检真模块；运行线程模块唯一拥有 THREAD-S3 夹具，自我治理 S3A 只读消费封存报告；入口改为阶段登记和结果装配。 |
| 结构承载 | 两个自检真模块中的调用期上下文、值式报告和测试夹具；不持久化、不写业务事实，生产线程与领域服务不变。 |
| 输出变化 | #242 / DQ-134 / JY-263 已完成并归档；线程协议阶段 66 项已迁移；#245 解锁为正式接口复核。 |
| 调用路径 | `入口上下文 -> 线程协议 100 自我治理前段 -> 200 运行线程前段 -> 300 自我治理 S3A -> 400 运行线程后段 -> 值式阶段报告 -> 最终总成`。 |
| 验证方式 | 66 项守恒、A01-A12、Debug x64 0 警告 0 错误、完整自检退出 0、PANEL-TREES / CONCEPT-S6D 回归、连续 20 / 20 轮退出 0。 |
| 禁止扩大声明 | 不证明 SELF-GOV 80 项全部迁移、入口存量自检全部迁出、结构事务、自我循环、自我苏醒、真实外设或旧能力迁移完成。 |

## 206. 本次追加文档治理：概念命名与任务生命周期后继承载修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | 两套 20260711 概念命名 / 任务生命周期施工流程图与详细设计、CODE-FILE-01、业务代码事实 `b33c602`、阶段化总成 `c924bf2`、#242 正式提交 `387f2bd` 和 JY-262 前批登记。 |
| 核心处理 | 按逻辑流程提取 #213-#215、#221-#225 的输入、准入、结构承载、输出和失败二分；区分真实业务依赖与批次顺序；裁决统一生产路由、新生产真模块、独立自检模块和最终回归阶段 550-620。 |
| 结构承载 | 两套流程图 MD+HTML、两份详细设计、八份依赖门控计划、逻辑提取引用矩阵、修订断点清单、JY-264、计划索引、任务队列、当前状态和逐能力台账；不新增机器结构。 |
| 输出变化 | #213-#215、#221-#225 已完成承载修订并正式登记；执行路线为 `#213/550 -> #221/560 -> #222/570 -> #223/580 -> #214/590 -> #224/600 -> #225/610 -> #215/620`，仍等待各自正式前置和执行前接口复核。 |
| 调用路径 | `正式流程图 / 详细设计 -> 冻结代码事实 -> 逻辑提取矩阵 -> 文件归属与阶段裁决 -> 依赖门控计划 -> 执行前实际接口复核 -> 执行窗口`。 |
| 验证方式 | 八计划完整门控字段和六列新文件分类通过；旧路径只位于历史 / 禁止范围；两个 Mermaid 图对逐字一致且含 CDN；`git diff --check` 与 `tools/check_specs.py --strict` 通过。本批未运行 C++ 构建或程序。 |
| 禁止扩大声明 | #242 已完成不代表 #245、#243、#212-#215、#217-#225 完成；不证明任务状态机、通用执行、结果结算、自我循环、恢复、真实外设或旧能力迁移完成。 |

## 207. 本次追加文档治理：#245 共享夹具实际接口漂移修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | `10e24cb` 当前入口、#242 正式阶段接口、执行窗口 #245 编译复核与完整回退回传、CODE-FILE-01、原施工流程图 / 详细设计 / #245 计划。 |
| 核心处理 | 逐项提取 S4A-D、S5、S3B 的真实依赖与所有权；把原两个最小上下文改为 S4 主装配借用语境、S5 模块独占完整隔离夹具、S3B 后段借用语境三段合同。 |
| 结构承载 | 聚焦现状流程图 MD+HTML、逐行映射、输入契约、非成功二分、偏差清单、JY-265 断点、修订施工图 / 详细设计 / #245、计划索引、任务队列、当前状态、决策和逐能力台账；未新增机器结构。 |
| 输出变化 | #245 / DQ-137 重新登记为依赖已满足待执行；三个预期真模块分别承载 S4A-D、S5、S3B，阶段顺序和 53 / 119 项守恒不变；#243 继续门控。 |
| 调用路径 | `执行前接口漂移 -> 完整回退 -> 当前代码扫描 -> 所有权拆分 -> 现状图与四表 -> 施工图 -> 详细设计 -> 修订计划 -> 再次执行前逐字段复核`。 |
| 验证方式 | 聚焦与施工两套 Mermaid 图对一致；流程图纠偏严格检查、规范严格检查、计划登记检查和 `git diff --check`。本批未运行 C++ 构建或程序。 |
| 禁止扩大声明 | 本批不证明 #245 已实现、53 项已迁移、入口已瘦身、#243 / #217 已解锁、自我循环、恢复、真实外设或旧能力迁移完成。 |

## 208. 本次追加文档治理：#245 S5 全局聚合证据边界漂移修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | `0cbce53` 当前入口、执行窗口第二次 #245 编译漂移与完整回退回传、JY-265 三种所有权合同。 |
| 核心处理 | 对照 S5 A01-A12、自身总结果、相邻输出、四项全局证据定义和最终 `程序通过`，确认机械文本切片误把并列证据当成 S5 输入；裁决按编号与依赖收紧边界。 |
| 结构承载 | 修订两套流程图、逻辑提取矩阵、逐行映射、输入契约、非成功二分、偏差清单、详细设计、#245、JY-266 断点、计划索引、任务队列、当前状态和决策；未新增机器结构。 |
| 输出变化 | S5 继续只接收两个值式参数，只迁移隔离场景、A01-A12、自身总结果与对应输出；四项全局聚合证据留在原所有者。#245 重新等待执行前逐字段逐编号复核。 |
| 调用路径 | `第二次编译漂移 -> 完整回退 -> 编号 / 依赖扫描 -> 并列证据裁决 -> 三件套再修订 -> 执行前逐字段逐编号复核`。 |
| 验证方式 | 流程图纠偏严格检查、规范严格检查、两套 Mermaid 同图、四项排除扫描和 `git diff --check`。本批未运行 C++ 构建或程序。 |
| 禁止扩大声明 | 不证明 #245 已实现，不把四项全局聚合证据归属 S5，不解锁 #243 / #217，不声明自我循环或旧能力迁移完成。 |

## 209. 本次代码实施：SELFTEST-MIGRATION-B1A2 自我治理闭环与领域回执阶段迁移

| 项 | 内容 |
| --- | --- |
| 输入入口 | `3289d6c`、#244 四阶段总成、#242 线程协议阶段、JY-265 三种所有权和 JY-266 S5 编号边界。 |
| 核心处理 | 新建三个独立自检真模块，迁移 S4A-D 32 项、S5 12 项和 S3B 9 项；入口仅保留语境装配、阶段登记、一次运行和报告汇总。 |
| 结构承载 | S4 借用主装配语境；S5 独占隔离仓库、服务、路由、桥、邮箱、统计与并发夹具；S3B 借用后段材料并独占临时线程、桥和邮箱。报告均为单写封存值。 |
| 输出变化 | 初始化闭环 100 / 200 位于控制面板前，领域回执 100 位于后段材料后；53 项与 #242 的 66 项守恒为 119 项；#243 解除为执行前复核。 |
| 调用路径 | `入口调用期语境 -> 初始化闭环 S4 100 / S5 200 -> 控制面板与后段领域材料 -> 领域回执 S3B 100 -> 最终回归只读报告`。 |
| 验证方式 | A01-A12、A03B、静态四项全局证据排除、Debug x64 0/0、完整自检退出 0、`tools/check_specs.py --strict`、连续 20 / 20 轮通过。 |
| 禁止扩大声明 | 不证明入口全部自检迁出、#243 / #217 已完成、结构事务、安全删除、恢复、自我循环、自我苏醒、真实外设或旧能力迁移完成。 |

## 210. 本次追加文档治理：#243 实际调用语境漂移修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | `05c0004` 当前入口、#245 正式阶段 / 报告接口、执行窗口 #243 实际复核与完整回退回传、CODE-FILE-01。 |
| 核心处理 | 逐项冻结 S7/S8 服务引用、前序值、实时支持读取和人读辅助；裁决 S7 完整非拥有语境、稳定身份报告，S8 同一图后 S7 实时读取，以及支持辅助 / 计时 / 输出所有权。 |
| 结构承载 | 聚焦现状图 MD+HTML、逐行映射、输入契约、非成功二分、偏差清单、JY-268 断点、修订施工图 / 详细设计 / 逻辑矩阵 / #243、计划索引、队列、当前状态、决策和逐能力台账；未新增机器结构。 |
| 输出变化 | #243 / DQ-135 重新登记为依赖已满足待执行；顺序 300 / 310 / 400 与 18 / 149 守恒不变；完成治理编号顺延 JY-269，#217 继续门控。 |
| 调用路径 | `实际接口漂移 -> 完整回退 -> 服务 / 值 / 实时读取分类 -> 现状图与四表 -> 施工图 -> 详细设计 -> 修订 #243 -> 再次执行前复核`。 |
| 验证方式 | 流程图纠偏严格检查、规范严格检查、两套 Mermaid 同图、计划登记和 `git diff --check`。本批未运行 C++ 构建或程序。 |
| 禁止扩大声明 | 不证明 #243 已实现、18 项已迁移、#217 已解锁、结构事务、安全删除、入口全部瘦身或旧能力迁移完成。 |

## 222. 本次代码实施：CONCEPT-NAMING-S2 信息目标状态与目标概念关系基线

| 项 | 内容 |
| --- | --- |
| 输入入口 | JY-280 修订合同、两个根需求初始化、关系仓库和最终回归 510。 |
| 核心处理 | 追加关系 13；根需求完整后创建共享抽象状态；区分生命周期活性计数和删除候选阻断计数。 |
| 结构承载 | 关系枚举、共享状态句柄、抽象状态节点和 `自检.概念命名目标` 真模块。 |
| 输出变化 | 初始化结果新增共享状态；最终回归新增 A01-A10 报告。 |
| 调用路径 | `需求初始化 -> 状态服务 -> 初始化结果`；`中央自检 510 -> 概念命名目标自检`。 |
| 验证方式 | Debug x64 0/0、完整自检退出 0、严格规范、`git diff --check`、连续 20/20。 |
| 禁止扩大声明 | 不证明普通命名需求、服务根挂载、名称绑定、任务结果、需求结算或命名闭环完成。 |

## 212. 本次追加实施治理补正：#243 概念自检迁移完成

| 项 | 内容 |
| --- | --- |
| 输入入口 | `e21affd`、JY-270 实施断点与验证证据。 |
| 核心处理 | 补正执行提交遗漏的计划归档、计划索引、任务队列、当前状态和 NEW-05 台账。 |
| 结构承载 | 两个概念自检真模块承载 18 项，入口保留 300 / 310 / 400 登记与报告汇总。 |
| 输出变化 | #243 / DQ-135 正式完成并归档；#217 解除业务依赖门控，进入设计主动复核后的执行前复核。 |
| 验证方式 | 沿用 JY-270：Debug x64 0/0、完整自检退出 0、严格规范、diff check、连续 20/20。补正批本身未重跑构建。 |
| 禁止扩大声明 | 不证明结构事务、安全删除、入口全部瘦身或旧能力迁移完成。 |

## 213. 本次追加文档治理：#217-#220 主动接口预审修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | `e21affd` 四仓库 / 写入事务 / 构造点、代码文件规则、安全删除流程与 #217-#220。 |
| 核心处理 | 主动提取模块类型桥、仓库许可包装、嵌套仓库锁和主装配接域边界；不等待执行窗口报错。 |
| 结构承载 | 聚焦现状图 MD+HTML、四张审查表、JY-271 断点、安全删除施工图 / 详细设计和修订 #217-#220；未新增机器结构。 |
| 输出变化 | #217 成为下一执行前复核项；#218-#220 保持依赖门控并消费正式值式令牌与固定模块骨架。 |
| 调用路径 | `传统仓库 -> 数据接线桥 -> 协调真模块 -> 隔离已接域仓库组 -> 自检 500 -> #218-#220 520-540`。 |
| 验证方式 | 流程纠偏严格检查、规范严格检查、Mermaid 同图和 `git diff --check`；未运行 C++ 构建。 |
| 禁止扩大声明 | 不证明 #217 已实现、入口主装配已接域、安全删除、回滚、恢复或旧能力迁移完成。 |

## 214. 本次追加文档治理：#217 阻断接口补修

| 项 | 内容 |
| --- | --- |
| 输入入口 | `9823507` 正式基线、执行窗口三个未提交骨架与首次 Debug x64 0/0、四仓库全部公开声明和关系 / 索引锁内节点调用点。 |
| 核心处理 | 补齐传统数据桥 ABI、移动许可 RAII、共享状态寿命、活动线程验证和无令牌重入规则；把普通操作与身份删除分为共享 / 独占许可；逐项裁决 46 个公开声明入口。 |
| 结构承载 | JY-272 ABI / 入口矩阵、阻断断点、现状图完整包、安全删除施工图 / 详细设计、修订 #217、计划索引和项目记忆；未新增正式机器结构。 |
| 输出变化 | #217 从“执行窗口自行定义 ABI 的阻断”恢复为设计已限定、等待从现有骨架继续；#212、#218-#220 仍等待 #217 正式完成。 |
| 调用路径 | `未提交骨架 -> 真实接口缺口 -> 固定数据桥 ABI -> 46 入口许可矩阵 -> 身份删除独占 -> 锁内复制 / 锁外同令牌复核 -> 恢复 #217`。 |
| 验证方式 | 流程图纠偏严格检查、规范严格检查、Mermaid 同源和 `git diff --check`；本治理未运行 C++ 构建，骨架 0/0 仅作输入证据。 |
| 禁止扩大声明 | 不证明数据桥 ABI 已编码、四仓库已接域、A01-A14 / 500 已通过、入口主装配已接域、安全删除或恢复已实现。 |

## 215. 本次追加计划治理：运行期主装配接域 S0

| 项 | 内容 |
| --- | --- |
| 输入入口 | JY-271 / JY-272 明确的主装配后置边界、`9823507` 入口与初始化 / 领域直接删除和失败清理初筛。 |
| 核心处理 | 新增依赖 #217 的纯只读 #246，要求全量登记主装配寿命、四仓库构造、直接删除 / 失效和失败补偿，并按六类所有权裁决。 |
| 结构承载 | #246 计划、计划索引、任务队列 DQ-138、当前状态、决策和 NEW-02 台账；没有机器结构变化。 |
| 输出变化 | #217 正式完成后，除 #212 / #218 外可并行执行 #246 文档扫描；其产物默认生成专用现状图、详细设计和分段实施计划。 |
| 调用路径 | `#217 正式接口 -> #246 S0 -> 主装配接域现状图 -> 详细设计 -> 主装配接线 / 失败清理 / 端到端验收计划`。 |
| 验证方式 | 计划完整性、依赖门控、替代上游依据、允许 / 禁止文件和 `git diff --check`；本批未运行构建或程序。 |
| 禁止扩大声明 | 不证明 #217 已完成、#246 已执行、主装配已接域、失败清理已迁移或安全删除 / 恢复完成。 |

## 216. 本次追加文档治理：命名目标与任务事务消费语境主动预审

| 项 | 内容 |
| --- | --- |
| 输入入口 | `43b7f5f` 正式基线、JY-272 数据桥 ABI、JY-274 / #246 主装配后置边界、#212 与 #221-#225 现有计划。 |
| 核心处理 | 区分 #212 阶段顺序与事务业务依赖；固定传统服务只消费数据桥 / 值式令牌，#221-#223 隔离自检和 #224-#225 生产真模块分别取得许可。 |
| 结构承载 | JY-275 预审矩阵 / 断点、任务流程图 MD+HTML、详细设计、六份计划、索引、队列、当前状态和决策；无机器结构变化。 |
| 输出变化 | #212 与 #221-#225 已提前补齐接口假定和漂移门禁；无接线兼容路径不参加事务验收，主装配整体接域继续等待 #246。 |
| 调用路径 | `#217 正式接口 -> 自检或生产真模块取得许可 -> 传统服务验证 / 下传令牌 -> 连接态验收 -> #246 主装配扫描`。 |
| 验证方式 | Mermaid 同源、严格规范与 `git diff --check`；未运行 C++ 构建或程序。 |
| 禁止扩大声明 | 不证明 #217、#212、#221-#225 或 #246 已执行，不证明主装配接域、任务闭环、恢复或旧能力迁移完成。 |

## 217. 本次追加文档治理：概念命名路由许可与结果委托主动预审

| 项 | 内容 |
| --- | --- |
| 输入入口 | `99da751` 正式基线、JY-272 数据桥 / 重入规则、JY-275 #225 许可边界、概念命名三件套和传统服务事实。 |
| 核心处理 | 固定 #213 独占许可后取分片锁、#214 共享许可同令牌贯穿且非原子、#215 无外层许可并锁外委托 #225。 |
| 结构承载 | JY-276 预审矩阵 / 断点、概念命名流程图 MD+HTML、详细设计、#213-#215、索引、队列、状态和决策；无机器结构变化。 |
| 输出变化 | #215 移除任务 / 需求服务修改权；三计划补齐许可所有者、锁序、DTO 禁止项和实际接口漂移门禁。 |
| 调用路径 | `#213 路由独占 -> 分片 -> 需求令牌链；#214 路由共享 -> 绑定 / 动态令牌链；#215 锁外 -> #225 两段许可`。 |
| 验证方式 | Mermaid 同源、严格规范和 `git diff --check`；未运行 C++ 构建或程序。 |
| 禁止扩大声明 | 不证明 #217、#213-#215、#223、#225 或 #246 已执行，不证明命名闭环、恢复、自我循环或旧能力迁移完成。 |

## 218. 本次追加文档治理：主装配接域与恢复链所有权门控

| 项 | 内容 |
| --- | --- |
| 输入入口 | `0e39b57` 正式基线、#217 隔离接域边界、#246 主装配 S0、恢复流程 / 详细设计与 #226-#230、#237-#239。 |
| 核心处理 | 识别 #226 运行期冻结来源不足和 #237 与主装配上下文双所有者风险；扩展 #246 并冻结恢复链代码消费。 |
| 结构承载 | JY-277 预审矩阵 / 断点、恢复流程图 MD+HTML、详细设计、#246、八个恢复计划、索引、队列、状态和决策；无机器结构变化。 |
| 输出变化 | #246 必须裁决恢复链复用 / 修订 / 替代；#226/#229/#237-#239/#230 设计复核门控，#227/#228 传递门控。 |
| 调用路径 | `#217 -> #246 S0 -> 主装配后继计划完成 -> 恢复链归属裁决 -> #226 -> #227 -> #228 -> #229 -> #237 -> #238 -> #239 -> #230`。 |
| 验证方式 | Mermaid 同源、严格规范和 `git diff --check`；未运行 C++ 构建或程序。 |
| 禁止扩大声明 | 不证明主装配、快照、恢复、上下文发布或旧持久化能力迁移完成。 |

## 219. 本次追加文档治理：FS-02 第三批事务语境与模块承载主动预审

| 项 | 内容 |
| --- | --- |
| 输入入口 | JY-245 第三批三件套、CODE-FILE-01、JY-272 结构事务 ABI、JY-277 唯一运行期上下文边界和 #231-#235。 |
| 核心处理 | 识别行为 `.h/.cpp` 与许可所有者缺口；固定 #232 一次共享许可同令牌读取、#233/#234 纯值后继及 630/640/650 自检承载。 |
| 结构承载 | JY-278 预审矩阵 / 断点、第三批流程图 MD+HTML、详细设计、五份计划、索引、队列、状态和决策；无机器结构变化。 |
| 输出变化 | #231 增加 #217 接口复核；#232-#234 改为真模块和独立自检；#235 增加 #246 未来写入所有权复核。 |
| 调用路径 | `#217 + #225 -> #231 -> #232(共享许可/630) -> #233(纯值/640) -> #234(纯值/650) -> #235`。 |
| 验证方式 | Mermaid 同源、严格规范和 `git diff --check`；未运行 C++ 构建或程序。 |
| 禁止扩大声明 | 不证明 #217、#225、#231-#235、主装配、用途学习或旧能力迁移完成。 |

## 220. 本次追加文档治理：当前正式路线剩余计划全量生成

| 项 | 内容 |
| --- | --- |
| 输入入口 | 正式规则、计划索引、能力台账、#236 入口事实、#246/JY-277、活动 #212-#255 路线。 |
| 核心处理 | 审计未计划缺口，生成主装配、初始化、第二批自检和实例状态事实门禁九份计划。 |
| 结构承载 | 三套流程图 MD+HTML、三份详细设计、#247-#255、JY-279 审计 / 断点及共享治理；无机器结构变化。 |
| 输出变化 | 当前可生成计划缺口归零；未知后继由 #235/#251/#255 承接，排除项和正式关闭路线不生成。 |
| 调用路径 | `#217/#246 -> #247-#250；#230/#235/#250 -> #251-#254；#220/#230/#234 -> #255`。 |
| 验证方式 | 三对 Mermaid 同源、严格规范、`git diff --check`；未运行 C++ 构建或程序。 |
| 禁止扩大声明 | 不证明任何新增计划已执行，不证明入口收口、状态删除、恢复或旧能力迁移完成。 |

## 221. 本次追加文档治理：#212 抽象状态读取合同漂移修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | #212 回退回传、`状态服务.h:149`、概念命名流程图 / 详细设计 / 计划和既有逻辑提取矩阵。 |
| 核心处理 | 删除“抽象状态材料可读”假定，固定读取状态值等于请求值且非实例状态的复核合同。 |
| 结构承载 | JY-280 断点、流程图 MD+HTML、详细设计、#212、索引、队列、状态和决策；无机器结构变化。 |
| 输出变化 | #212 重新成为依赖已满足待执行；状态服务继续禁止修改，不新增读取入口。 |
| 调用路径 | `创建抽象状态 -> 读取状态值 -> 非实例判定 -> 返回稳定句柄`。 |
| 验证方式 | Mermaid 同源、严格规范、`git diff --check`；本治理未运行 C++。 |
| 禁止扩大声明 | 不证明 #212、关系 13、命名需求或命名闭环已实现。 |

## 221. 本次代码实施：STRUCT-TXN-S1 端点冻结写集与统一读取隔离

| 项 | 内容 |
| --- | --- |
| 输入入口 | 隔离已接域四仓库组、最终回归 500 与既有中央自检阶段总成。 |
| 核心处理 | 实现 `结构事务接线.数据`、协调与自检真模块，按 46 个公开入口建立 41 条令牌路径，节点 / 主信息身份删除独占，普通路径共享。 |
| 结构承载 | 传统仓库保存值式接线；协调模块保存共享运行期状态、活动表和许可锁；自检模块独占 A01-A14 正文。 |
| 输出变化 | 最终回归 500 已登记；隔离接域组可验证端点冻结和令牌拒绝；入口主装配仍未接域。 |
| 调用路径 | `协调器 -> 接线 -> 仓库令牌入口 -> 自检 500`。 |
| 验证方式 | Debug x64 0/0、完整自检退出 0、严格规范、`git diff --check`、连续 20/20 通过。 |
| 禁止扩大声明 | 不证明全局统一读取隔离、主装配接域、安全删除提交、回滚、恢复或旧能力迁移完成。 |

## 211. 本次追加文档治理：#243 模块骨架与 S6D 支持辅助修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | `cdc96aa` 干净基线、执行窗口 #243 模块迁移首次构建失败与完整回退回传、JY-268 正式调用语境。 |
| 核心处理 | 区分实施形态错误与生产接口漂移；固定两个真模块的全局模块片段、导出声明、单向导入和公开接口顺序；冻结 S7 唯一支持辅助及 S6D/S7/S8 十一处接线。 |
| 结构承载 | JY-269 断点、聚焦现状图与四表、施工图、详细设计 10.6、逻辑矩阵 DRIFT-07、修订 #243、计划索引、队列、当前状态、决策和 NEW-05 台账；未新增机器结构。 |
| 输出变化 | #243 / DQ-135 重新登记为依赖已满足待执行；执行顺序固定为骨架公开接口先编译、十一处辅助改接、S7/S8 正文迁移、阶段验证；完成治理编号顺延 JY-270，#217 继续门控。 |
| 调用路径 | `实施形态错误 -> 完整回退 -> 固定模块骨架 -> S7 唯一辅助 -> S6D 六处 + S7 四处 + S8 一处改接 -> 正文迁移 -> 300/310 验证`。 |
| 验证方式 | 流程图纠偏严格检查、规范严格检查、两套 Mermaid 同图、计划登记和 `git diff --check`。本批未运行 C++ 构建或程序。 |
| 禁止扩大声明 | 不证明 #243 已实现、18 项已迁移、#217 已解锁、结构事务、安全删除、入口全部瘦身或旧能力迁移完成。 |

## 222. 本次追加文档治理：#218/#219 私有提交能力与编排授权合同修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | #218 执行前 P1 合同冲突回传、JY-273 正式结构事务 ABI、安全删除流程图 / 详细设计和 #218/#219 计划。 |
| 核心处理 | 分离结构事务许可与业务提交授权；固定四仓库私有提交面、唯一编排器友元、不可伪造提交会话和自检非友元通道。 |
| 结构承载 | JY-282 断点、安全删除流程图 MD+HTML、详细设计、#218/#219、索引、队列、当前状态、决策和 NEW-02 台账；无机器结构变化。 |
| 输出变化 | #218 从合同冲突恢复为前置依赖已满足待执行；#219 继续等待 #218 正式私有能力产物；#217 令牌业务语义保持不变。 |
| 调用路径 | `#217 独占结构许可 -> #219 编排器完整重读 / 预构造 -> 创建私有提交会话 -> 四仓库友元私有提交 -> 活动快照最后发布`。 |
| 验证方式 | Mermaid 同源、严格规范和 `git diff --check`；本治理未运行 C++ 构建或程序。 |
| 禁止扩大声明 | 不证明私有提交能力、安全删除、活动快照发布、主装配接域、恢复或旧能力迁移已经实现。 |

## 223. 本次纯只读实施：RUNTIME-TXN-S0 主装配接域与失败清理事实复核

| 项 | 内容 |
| --- | --- |
| 输入入口 | #217 正式 ABI、当前入口主装配、四仓库构造、删除 / 失效 / 清理调用和恢复计划。 |
| 核心处理 | 全量构造与清理扫描、六类归并、非成功二分和恢复单一所有权裁决。 |
| 结构承载 | S0 现状图 MD+HTML、四张事实表、恢复归属裁决、详细设计和修订计划；机器结构不变。 |
| 输出变化 | #247 解锁为执行前复核；#237 改为恢复适配，#226 与恢复链等待 #247-#249。 |
| 调用路径 | `#217 -> #246 -> #247 -> #248 -> #249 -> #226 / #229 / #237-#239`。 |
| 验证方式 | A01-A12、Mermaid 同图、严格规范、`git diff --check`、C++ / 工程差异为空；未构建、未运行。 |
| 禁止扩大声明 | 不证明主装配接域、清理迁移、快照、恢复、安全删除、ACID 或旧能力迁移完成。 |

## 224. 本次代码实施：CONCEPT-S8-S2 结构所有者原子清理适配

| 项 | 内容 |
| --- | --- |
| 输入入口 | JY-282 私有提交合同、#216 领域影响候选、#217 同域独占令牌、四仓库与最终回归 520。 |
| 核心处理 | 增加显式审计和反向主键；预构造四仓库、所有者、概念图与缓存清理包；形成结构写集补全候选与不可伪造提交能力。 |
| 结构承载 | 准备包、反向主键表、所有者授权、提交会话数据头和 `自检.概念清理适配` 真模块。 |
| 输出变化 | #218 / DQ-110 完成并归档；#219 的直接前置满足，可进入 530 实际接口复核。 |
| 调用路径 | `独占许可 -> 四仓库 / 服务准备 -> 概念图汇总`；`最终回归 520 -> 概念清理适配自检`。 |
| 验证方式 | A01-A12、Debug x64 0/0、完整自检退出 0、严格规范、私有边界静态检查和连续 20/20。 |
| 禁止扩大声明 | 不证明 #219 最终提交、物理删除、活动快照发布、恢复、主装配接域或旧能力迁移完成。 |

## 225. 本次追加文档治理：#219 精确缓存与完整快照合同漂移修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | #219 执行前回传、JY-284 正式代码、统计缓存键 / 清空入口、概念图清理包和提交会话阶段。 |
| 核心处理 | 识别目标级准备包与整命名空间写入口冲突、完整快照缺失及持独占阻塞等图锁风险；固定非阻塞图 / 缓存冻结、目标级精确提交、完整快照和扩展阶段。 |
| 结构承载 | JY-285 断点、安全删除流程图 MD+HTML、详细设计、#219、索引、队列、当前状态、决策和 NEW-02 台账；无机器结构变化。 |
| 输出变化 | #219 明确允许定向修改统计服务、概念图服务和提交能力数据头；前置已满足，可重新执行前复核。 |
| 调用路径 | `结构独占 -> 图 / 缓存冻结 -> 完整重读 / 预构造 -> 四仓库 -> 登记 -> 目标缓存 -> 完整快照 -> 释放`。 |
| 验证方式 | Mermaid 同源、严格规范和 `git diff --check`；本治理未运行 C++ 构建或程序。 |
| 禁止扩大声明 | 不证明 #219、安全删除、物理删除、活动快照发布、主装配接域、恢复或旧能力迁移已实现。 |

## 226. 本次追加文档治理：#219 删除端点关系审计合同漂移修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | #219 JY-285 草案运行回传、关系仓库普通 / 显式审计实现、A07 和 A11 失败事实。 |
| 核心处理 | 保留草案续作；分离删除端点历史审计与当前关系读取；把基准版本过期计为并发发布合法后态。 |
| 结构承载 | JY-286 断点、安全删除流程图 MD+HTML、详细设计、#219、索引、队列、状态、决策和 NEW-02 台账；本治理不改机器结构。 |
| 输出变化 | #219 允许定向修改关系仓库两个显式审计入口及令牌包装；A11 只校准自检计数。 |
| 调用路径 | `关系失效留审计 -> 节点删除 -> 普通关系空读 + 显式审计读回 -> 回执一致性`。 |
| 验证方式 | Mermaid 同源、严格规范和 `git diff --check`；执行草案既有 Debug 0/0，本治理未运行构建。 |
| 禁止扩大声明 | 不证明 A07/A11 或 #219 已通过，不放宽普通关系读取，不声明安全删除、恢复或旧能力迁移完成。 |

## 227. 本次代码实施：CONCEPT-S8-S3 安全删除提交与活动快照发布

| 项 | 内容 |
| --- | --- |
| 输入入口 | #216 完整删除影响候选、#217 结构事务域、#218 私有准备 / 提交能力及 JY-285/JY-286 裁决。 |
| 核心处理 | 结构独占下非阻塞冻结，锁内重读和完整预构造，固定八阶段无分配提交，活动快照最后发布。 |
| 结构承载 | 四仓库权威结构、不可伪造提交会话、概念登记、目标级统计缓存和完整活动快照。 |
| 输出变化 | 合法目标完成安全删除；普通读取为空、显式审计留存；目标缓存移除且活动版本 / 快照同步推进。 |
| 调用路径 | `最终回归 530 -> 自检.概念安全删除 -> 服务.概念安全删除 -> 四仓库 / 概念图 / 统计私有提交`。 |
| 验证方式 | A01-A12、Debug x64 0/0、完整自检退出 0、严格规范、静态边界和连续 20/20。 |
| 禁止扩大声明 | 不证明崩溃恢复、分布式或批量事务、入口主装配接域、完整物理回收或旧能力迁移完成。 |

## 228. 本次代码实施：CONCEPT-S8-S4 删除后固定点审计与等价重建验收

| 项 | 内容 |
| --- | --- |
| 输入入口 | #219 正式安全删除回执、完整候选、显式审计、活动图和统计只读入口。 |
| 核心处理 | 聚合结构后验，完整证据下幂等返回，重算直接边，并通过真实发布路径重建等价概念。 |
| 结构承载 | 权威仓库审计 / 反向主键、活动图、概念登记、目标缓存和值式后验报告。 |
| 输出变化 | 旧候选重复提交不写结构；等价概念获得新节点句柄，旧句柄和旧审计不复活。 |
| 调用路径 | `最终回归 540 -> 自检.概念删除后验 -> 服务.概念安全删除::复核删除审计 / 提交安全删除`。 |
| 验证方式 | A01-A12、Debug x64 0/0、完整自检退出 0、严格规范、静态边界和连续 20/20。 |
| 禁止扩大声明 | 不证明自动清理、恢复、分布式 / 批量事务、主装配接域、高并发性能或旧能力迁移完成。 |

## 229. 本次追加文档治理：#213 待命名复核令牌路径合同漂移修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | #213 执行窗口挂起与回退事实、JY-276 锁序合同、概念图 / 语素服务当前无令牌读取链和仓库正式令牌重载。 |
| 核心处理 | 否决许可前活动快照；把许可前阶段收窄为纯消息复核，并固定独占许可、概念分片、活动图、概念图 / 语素 / 仓库令牌重读和需求服务令牌链。 |
| 结构承载 | JY-289 断点、命名流程图 MD+HTML、详细设计、#213、JY-276 预审矩阵、索引、队列、当前状态、决策和 MIG-06 / NEW-03 台账；无机器结构变化。 |
| 输出变化 | #213 定向允许概念图与语素服务增加只读令牌重载；前置与 540 顺序已满足，重新登记为阶段 550 待执行。#214 继续等待 #213，#221 继续等待 550 批次顺序。 |
| 调用路径 | `纯消息复核 -> 结构独占 -> 概念分片 -> 活动图读锁 -> 概念图 / 语素 / 仓库令牌重读 -> 需求服务同令牌创建 / 复用`。 |
| 验证方式 | 两份 Mermaid 同源、计划 / 详细设计范围一致、严格规范和 `git diff --check`；本治理未运行 C++ 构建或程序。 |
| 禁止扩大声明 | 不证明 #213、阶段 550、命名需求闭环、入口主装配接域、自我循环、恢复或旧能力迁移完成。 |

## 230. 本次追加文档治理：#247/#248 上下文与领域服务最终所有权合同修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | 执行窗口对 #247/#248 的主动接口复核、#246 主装配事实、JY-283 恢复归属、主装配 / 恢复三件套和三份运行期计划。 |
| 核心处理 | 保持唯一上下文最终拥有服务；把 #247 收窄为不可生产发布的结构核心与宿主 / 租约骨架，授权 #248 回改上下文并加入最终服务成员。 |
| 结构承载 | JY-290 断点、两套流程图 MD+HTML、三份详细设计、#247-#249、#237、恢复归属 / 预审矩阵、索引、队列、状态、决策和 MIG-00 / NEW-02 台账；无机器结构变化。 |
| 输出变化 | #247 前置仍已满足，可按 660 执行；#248 等待 #247，并在 670 首次形成完整可发布上下文；#249 / #237 只消费 #248 后最终合同。 |
| 调用路径 | `#247 协调器 / 接线 / 四仓库核心 + 宿主骨架（不可发布） -> #248 服务装配 -> 回改唯一上下文 -> 完整首发 -> #249 / #237`。 |
| 验证方式 | 两套 Mermaid 同源、三件套范围一致、严格规范和 `git diff --check`；本治理未运行 C++ 构建或程序。 |
| 禁止扩大声明 | 不证明 #247/#248、660/670、主装配接域、完整上下文发布、恢复、快照、ACID 或旧能力迁移完成。 |

## 231. 本次代码实施：CONCEPT-NAMING-S3 命名需求唯一创建复用与服务根挂载

| 项 | 内容 |
| --- | --- |
| 输入入口 | 版本 2 命名治理消息、#217 同域结构事务接线、目标概念和当前待命名请求。 |
| 核心处理 | 许可前只复核消息值形状；许可后按结构独占、目标分片、活动图、概念图 / 语素 / 仓库同令牌路径重读，随后唯一创建或复用未完成命名需求，并在服务根关系最终发布。 |
| 结构承载 | 需求节点、需求主信息、主体 / 宿主 / 场景 / 目标状态 / 目标概念关系、服务根普通父子关系，以及值式治理结果报告。 |
| 输出变化 | 零个候选创建并完整发布，一个候选幂等复用，多个候选追根因；失败注入只逆序清理本次尚未发布候选。 |
| 调用路径 | `最终回归 550 -> 自检.概念命名需求 -> 路由.概念命名治理 -> 概念图 / 语素令牌重载 -> 需求服务令牌路径 -> 四仓库`。 |
| 验证方式 | A01-A14、同概念 / 不同概念并发各 50 次、公开读取交错、Debug x64 0/0、完整自检退出 0、严格规范、差异检查和连续 20/20。 |
| 禁止扩大声明 | 不证明名称候选选择 / 绑定、任务授权 / 执行、动态、结果结算、入口主装配接域、自我循环、恢复或旧能力迁移完成。 |

## 232. 本次追加文档治理：#221 存在服务同令牌创建合同漂移修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | #221 执行前复核、任务系统虚拟存在规则、任务 / 存在 / 状态服务当前调用链和 #217 仓库令牌入口。 |
| 核心处理 | 保留任务虚拟存在与存在原子服务所有权；为存在服务固定四个同令牌重载，并把失败候选清理纳入同一独占许可。 |
| 结构承载 | JY-292 断点、任务流程图 MD+HTML、详细设计、#221、JY-275 预审矩阵、索引、队列、状态、决策和 MIG-08 台账；无机器结构变化。 |
| 输出变化 | #221 允许文件增加存在服务；前置已满足，重新登记为阶段 560 待执行。#222 继续等待 #221，#223 继续等待 #222。 |
| 调用路径 | `自检独占许可 -> 任务服务 -> 存在服务 -> 状态服务 -> 仓库同令牌创建 / 读回 -> 完整发布或精确逆序清理`。 |
| 验证方式 | Mermaid 同源、三件套范围一致、严格规范和 `git diff --check`；本治理未运行 C++ 构建或程序。 |
| 禁止扩大声明 | 不证明 #221、任务当前态、主装配接域、完整任务闭环、恢复或旧能力迁移完成。 |

## 233. 本次代码实施：RUNTIME-TXN-S1 唯一运行期上下文与四仓库主装配接域

| 项 | 内容 |
| --- | --- |
| 输入入口 | #217 正式协调器 / 接线 ABI、#246 主装配事实和 JY-290 分阶段所有权合同。 |
| 核心处理 | 一份协调状态生成一份接线，按固定成员顺序构造四仓库结构核心；缺服务候选发布写前拒绝。 |
| 结构承载 | `运行期上下文`、宿主、租约及协调器 / 接线 / 四仓库成员。 |
| 输出变化 | 形成 #248 可原地扩展的唯一结构核心，宿主当前槽仍为空且无有效生产租约。 |
| 调用路径 | `最终回归 660 -> 自检.运行期上下文 -> 启动.运行期上下文 -> 协调.结构事务 -> 四仓库`。 |
| 验证方式 | A01-A12、Debug x64 0/0、完整自检退出 0、严格规范、静态边界与连续 20/20。 |
| 禁止扩大声明 | 不证明领域服务接线、完整上下文发布、入口生产对象切换、恢复、快照、ACID 或旧能力迁移完成。 |

## 234. 本次追加文档治理：#221 需求承接材料同令牌复核合同漂移修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | #221 第二次执行前复核、clean HEAD `22210c3`、任务 / 需求服务当前读取链、需求服务既有私有令牌辅助和 JY-292 存在同令牌合同。 |
| 核心处理 | 否决许可前值式快照；定向允许需求服务增加需求有效性与承接材料两个公开只读令牌重载，复用既有私有辅助，并固定第一笔任务写入前的许可内权威复核。 |
| 结构承载 | JY-294 断点、任务流程图 MD+HTML、详细设计、#221、JY-275 预审矩阵、索引、队列、状态、决策和 MIG-08 台账；无机器结构变化。 |
| 输出变化 | #221 调用链修订为 `任务 -> 需求只读 -> 存在 -> 状态 -> 仓库` 同一独占令牌；需求复核失败写前返回，#221 仍待执行 560。#248 的独立装配矩阵漂移已登记为退回设计。 |
| 调用路径 | `自检独占许可 -> 任务服务 -> 需求服务同令牌重读 -> 存在 / 状态同令牌创建 -> 仓库提交 -> 完整发布或精确逆序清理`。 |
| 验证方式 | Mermaid 同源、三件套范围一致、严格规范和 `git diff --check`；本治理未运行 C++ 构建或程序。 |
| 禁止扩大声明 | 不证明 #221 已实现，不证明完整领域服务装配、生产上下文发布、任务闭环、恢复或旧能力迁移完成。 |

## 235. 本次追加文档治理：#221 原子服务未发布候选撤销合同漂移修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | #221 第三次执行回传、JY-292/JY-294 正式合同、存在 / 状态创建链和任务顶层失败组合语境。 |
| 核心处理 | 否决任务服务直删原子结构；固定存在 / 状态不可伪造未发布候选能力、原服务确认 / 撤销入口、顶层成功 / 失败顺序和幂等边界。 |
| 结构承载 | JY-296 断点、任务流程图 MD+HTML、详细设计、#221、JY-275 预审矩阵、索引、队列、状态、决策和 MIG-08 台账；无机器结构变化。 |
| 输出变化 | #221 重新登记 560 待执行；第三轮代码草案已完整撤回。#222 继续等待 #221，#248 继续等待独立矩阵修订。 |
| 调用路径 | `需求同令牌复核 -> 任务节点 -> 存在候选 -> 状态候选 -> 任务关系 -> 完整读回 -> 原服务确认；失败逆序撤销`。 |
| 验证方式 | Mermaid 同源、三件套范围一致、严格规范和 `git diff --check`；本治理未运行构建或程序。 |
| 禁止扩大声明 | 不证明 #221 已实现，不证明任务当前态、完整运行期装配、任务闭环、恢复或旧能力迁移完成。 |

## 236. 本次追加文档治理：#248 服务装配完整集合与真实前置合同漂移修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | #248 执行前复核、HEAD `7157f9e` 入口生产构造顺序、#247 正式结构核心、#246 四表和任务 / 命名后继服务面。 |
| 核心处理 | 冻结 16 个长期服务、四个工厂局部初始化器、系统角色材料与发布后消费者排除；把“同域”拆为同仓库、必要值式接线和正式同令牌能力；硬前置改为 #214。 |
| 结构承载 | JY-295 矩阵 / 断点、主装配流程图 MD+HTML、三份详细设计、#248/#249、索引、队列、状态、决策和 MIG-00/NEW-02 台账；无机器结构变化。 |
| 输出变化 | #248 从退回设计恢复为依赖门控待执行 670，等待 #214；#249 继续等待 #248。 |
| 调用路径 | `#247 结构核心 + #214 稳定服务面 -> 16 服务装配 -> 四局部初始化器 -> 系统角色读回 -> 首次发布 -> #249`。 |
| 验证方式 | Mermaid 同源、三件套 / 矩阵范围一致、严格规范和 `git diff --check`；本治理未运行构建或程序。 |
| 禁止扩大声明 | 不证明 #214/#248 已实现，不证明入口切换、快照 / 恢复、控制面板接线、ACID 或旧能力迁移完成。 |

## 237. 本次追加文档治理：#221 关系枚举负例顺延回归修订

| 项 | 内容 |
| --- | --- |
| 输入入口 | #221 / JY-296 执行回传、Debug x64 0/0、完整自检退出 1、CONCEPT-NAMING-S2-A2 既有未知关系负例和关系 14 正式落位事实。 |
| 核心处理 | 把验证数据漂移与生产语义分开；只授权既有概念命名自检把未知关系负例从 14 顺延为 15，并保持关系 13 / 14 正式值断言。 |
| 结构承载 | JY-297 断点、任务详细设计、#221、索引、队列、状态、决策和 MIG-08 台账；无机器结构变化。 |
| 输出变化 | #221 保留现有未暂存代码草案，等待定向修改后继续完整自检、严格规范和连续 20 轮；未归档、未提交。#222 继续门控。 |
| 调用路径 | `最终回归 560 -> 关系 14 正式准入 -> CONCEPT-NAMING-S2-A2 复跑 -> 13 / 14 正式值检查 + 15 未知拒绝`。 |
| 验证方式 | 文档严格规范和 `git diff --check`；执行窗口已有 Debug x64 0/0、完整自检退出 1，本治理不重跑 C++ 构建或程序。 |
| 禁止扩大声明 | 不证明 #221 或 560 已通过，不修改 #212 业务语义，不证明任务当前态、完整任务闭环、恢复或旧能力迁移完成。 |

## 238. 本次代码实施：TASK-LIFECYCLE-S1 唯一当前态投影与历史读回

| 项 | 内容 |
| --- | --- |
| 输入入口 | #221 正式计划、JY-292/JY-294/JY-296/JY-297 修订、已完成需求承接材料和隔离结构独占许可。 |
| 核心处理 | 同令牌复核需求承接，创建任务、虚拟存在和已创建实例状态，提交版本 1 的唯一当前生命周期关系；成功确认原子候选，失败由原服务逆序撤销。 |
| 结构承载 | 任务节点 / 主信息、来源需求关系、虚拟存在、实例状态、任务生命周期关系、关系版本和历史审计 DTO。 |
| 输出变化 | #221 已完成并归档；形成任务初始生命周期唯一当前态与完整历史读回，#222 直接前置解除。 |
| 调用路径 | `最终回归 560 -> 自检.任务生命周期初态 -> 独占许可 -> 任务服务 -> 需求 / 存在 / 状态服务 -> 四仓库`。 |
| 验证方式 | A01-A13、CONCEPT-NAMING-S2-A2、Debug x64 0/0、完整自检退出 0、严格规范、560 唯一和连续 20/20。 |
| 禁止扩大声明 | 不证明合法迁移、筹办、方法选择、执行、结果结算、完整生产装配、恢复、自我苏醒或旧能力迁移完成。 |
