# 当前状态

更新时间：2026-07-22

本文件只保存当前仍影响设计、执行、集成和完成声明的应用结果；历史流水读取计划归档、实施记录、设计记录和 Git。

## 1. 当前治理状态

| 项目 | 当前状态 | 边界 / 依据 |
| --- | --- | --- |
| Git 基线 | 规范写入前 `main == origin/main == c9efe89bb783500b686145c0b45e6920cc382ec0`；工作树干净 | 本轮改动尚待独立终检、提交与推送 |
| 当前角色 | WT-MAIN 设计窗口 | 可以修订规范、设计、计划和中央状态；不修改 C++、工程、自检，不构建、不运行 |
| 正式规范 | 目录候选登记 93 份编号规范、20 份对象根规范 | 4020 改为领域类型化记录，新增 4070 完整恢复和 7130 自我持久结构；正式规范旧结构名称零命中 |
| 当前主路线 | #338 / DQ-230 / NODE-TYPED-MIGRATION | 当前执行 NT-P0 设计材料清点和五件套重建；代码 NT-P1—P4 均设计门控 |
| 主线发布占用 | 无 | 当前没有集成任务进入 `集成中`；设计窗口持有中央治理写权 |
| 旧 DD-05 WIP | `codex/dd05-design-wip-hold-20260722@9cb9834c...` 只读保护 | 未推送、未进入 main；不得恢复旧派发 |

## 2. 新规范结构

```text
节点：节点编号 + 稳定主键 + 节点类型 + 版本 + 记录状态 + 创建序号
关系：承载全部正式拓扑
领域类型化记录：承载关系无法表达的所属领域必要原始值
索引 / 缓存 / 列表：只加速或投影，必须可重建
完整读取材料：节点 + 关系 + 领域记录的非持久值式组合
恢复：隔离候选全量互证 -> 重建索引 / 投影 -> 完整上下文一次发布
```

自我是唯一当前稳定存在节点和聚合入口，恰有一个当前内部世界场景引用；子存在通过内部世界成员关系保存；实例槽通过宿主 / 模板 / 当前值关系成立；状态和动态列表只由主体关系与索引形成读取投影。

## 3. 当前代码事实与差距

- 最新扫描有 99 个 `.h/.cpp/.ixx` 引用旧结构名称，其中生产 55、自检 44。
- 当前节点仓库仍不直接承载新规范要求的稳定主键；现行四仓、领域服务和自检仍大量依赖旧附属句柄和值槽。
- 四仓具备权威材料导出，但没有指定身份导入或隔离装载；快照服务仍拒绝替换运行期仓库。
- 运行期上下文宿主只允许空槽首次发布，不允许在线热替换。新恢复设计保留这一正确边界。
- 4170 侧账已在 main 运行期发布链中，但没有全量冻结和恢复注入；不能单独证明恢复已接通。
- #329 历史代码已进入 main，这一事实不撤销；它与新规范的差异由 #338 治理，不是继续扩展旧结构的许可。

## 4. 当前计划状态

| 路由 | 当前状态 |
| --- | --- |
| #323 | 总治理继续；新基础结构转入 #338 |
| #338 | 设计治理进行中；NT-P0 当前可执行；NT-P1—P4 不可派发 |
| #324 | 旧集成结果只作证据；暂停主线发布 |
| #325—#328 | 旧代码许可失效；等待 #338 重建 |
| #329 | 历史已完成；当前代码作为迁移与回归事实 |
| #330—#334 | 旧接口、详细设计和依赖基础失效；全部停止派发 |

当前没有满足“现行规范、有效流程图、有效详细设计、知识图谱、实际接口、精确所有权和验证矩阵”的代码计划，因此没有漏派任务。

## 5. 下一步

1. 完成当前规范 WIP 的独立一致性终检、strict 和 diff check。
2. 精确提交并非强制推送规范、设计记录、#338 总 / 子计划和中央治理状态。
3. 在同一设计续程执行 NT-P0：逐份判断活动详细设计与流程图，及时移入 `过期设计` 或重绑现行规范。
4. 依次形成 NT-P1、P2A、P2B、P2C、P3、P4 的新流程图、详细设计、函数与结构知识图谱及精确叶子计划。
5. 对满足门禁的无依赖叶子优先建立不同任务 worktree 并真实派发；有真实接口依赖的只门控受影响计划。

## 6. 禁止宣称

不得声明：

```text
旧结构已从代码删除
I64、状态或动态已经迁移
自我跨重启恢复已经实现
旧快照已经兼容
#324 可以直接发布
#325—#334 已恢复执行
D455、外设、自我内部循环或最小生命闭环已经接通
```

当前最多声明规范定义和分阶段治理路线已经形成候选，代码仍存在明确迁移差距。
