# 当前状态

更新时间：2026-07-22

任务状态投影版本：JY-586/E327

本文件只保存面向人的派生摘要。任务生命周期投影来源为 `项目记忆/任务状态台账.md` 的上述版本；本文件可能滞后，不参与派发、S0、执行权、依赖解除或完成裁决。历史流水读取计划归档、实施记录、设计记录和 Git。

## 1. 当前治理状态

| 项目 | 当前状态 | 边界 / 依据 |
| --- | --- | --- |
| Git 基线 | 本投影随 JY-586/E327 治理提交发布；完整哈希由 Git 对具名提交解析 | JY-585 造成 #339 计划 blob 与 WT-339-R1 冻结基线分叉；本轮改为单一任务状态台账并撤销 R1 派发效力 |
| 当前角色 | WT-MAIN 设计窗口 | 可以修订规范、设计、计划和中央状态；不修改 C++、工程、自检，不构建、不运行 |
| 正式规范 | 目录正式登记 94 份编号规范、20 份对象根规范 | 4010—4070、7130、7140 已闭合节点直接身份、关系 0—23、类型化记录、自我、恢复和概念分账 |
| 当前主路线 | #338 / DQ-230 / NODE-TYPED-MIGRATION | 六个新设计包和 #339—#352 不可变叶子施工合同终审 PASS；任务生命周期只读台账 |
| 主线发布占用 | 无 | 当前没有集成任务进入 `集成中`；设计窗口持有中央治理写权 |
| 旧 DD-05 WIP | `codex/dd05-design-wip-hold-20260722@9cb9834c...` 只读保护 | 未推送、未进入 main；不得恢复旧派发 |

## 2. 新规范结构

```text
节点：节点编号 + 稳定主键 + 节点类型 + 版本 + 记录状态 + 创建序号
关系：承载全部正式拓扑
领域类型化记录：承载关系无法表达的所属领域必要原始值
索引 / 缓存 / 列表：只加速或投影，必须可重建
完整读取材料：节点 + 关系 + 领域记录的非持久值式组合
恢复：隔离候选全量互证 -> 重建索引 / 投影 -> 完整上下文一次发布
```

自我是唯一当前稳定存在节点和聚合入口，恰有一个当前内部世界场景引用；子存在通过内部世界成员关系保存；实例槽通过宿主 / 模板 / 当前值关系成立；状态和动态列表只由主体关系与索引形成读取投影。

## 3. 当前代码事实与差距

- 当前生产影响面分层扫描为 11 个现行数据操作、17 个现行现代服务、14 份旧兼容服务头；具体旧结构符号和调用点由各叶在固定上游提交重扫。
- 当前节点仓库仍不直接承载新规范要求的稳定主键；现行四仓、领域服务和自检仍大量依赖旧附属句柄和值槽。
- 四仓具备权威材料导出，但没有指定身份导入或隔离装载；快照服务仍拒绝替换运行期仓库。
- 运行期上下文宿主只允许空槽首次发布，不允许在线热替换。新恢复设计保留这一正确边界。
- 4170 侧账已在 main 运行期发布链中，但没有全量冻结和恢复注入；不能单独证明恢复已接通。
- #329 历史代码已进入 main，这一事实不撤销；它与新规范的差异由 #338 治理，不是继续扩展旧结构的许可。

## 4. 任务状态人读投影

下表按“任务状态投影版本”重放台账事件形成，不是状态源；现场裁决必须重新读取任务状态台账同任务最新行。

| 任务 | 计划版本 | 状态版本 | 生命周期投影 | 说明 |
| --- | --- | --- | --- | --- |
| #339 | v0.1 | JY-586/E327 | 待执行 | 原派发经 `DRIFT-STATE-AUTHORITY` 与 `DRIFT-PLAN-BLOB` 撤回；WT-339-R1 只读失效，WT-339-R2 尚未创建 |
| #340 | v0.1 | JY-586/E327 | 依赖门控待执行 | 等待 #339 具名接口进入 main 并逐签名接受 |
| #341 | v0.1 | JY-586/E327 | 依赖门控待执行 | 等待 #339、#340 具名接口 |
| #342 | v0.1 | JY-586/E327 | 依赖门控待执行 | 等待 #339—#341 具名接口 |
| #343 | v0.1 | JY-586/E327 | 依赖门控待执行 | 等待 #342 自我 / 成员值式接口 |
| #344 | v0.1 | JY-586/E327 | 依赖门控待执行 | 等待业务接口与 #343 工程所有权移交 |
| #345 | v0.1 | JY-586/E327 | 依赖门控待执行 | 等待业务接口与 #344 工程所有权移交 |
| #346 | v0.1 | JY-586/E327 | 依赖门控待执行 | 等待 #340—#345 固定退役接口 |
| #347 | v0.1 | JY-586/E327 | 依赖门控待执行 | 等待 #343—#346 固定服务 |
| #348 | v0.1 | JY-586/E327 | 依赖门控待执行 | 等待 #343—#347 固定提交与模块 |
| #349 | v0.1 | JY-586/E327 | 依赖门控待执行 | 等待 #348 新域候选图 |
| #350 | v0.1 | JY-586/E327 | 依赖门控待执行 | 等待 #349 固定快照与冻结接口 |
| #351 | v0.1 | JY-586/E327 | 依赖门控待执行 | 等待 #350 严格解析与恢复接口 |
| #352 | v0.1 | JY-586/E327 | 依赖门控待执行 | 等待 #347—#351；仍须升 v0.2 冻结最终删除集 |

技术事实投影：`WT-339-R1` 位于 JY-584、clean、无 upstream、远端同名分支不存在，但其计划 blob 与 JY-585 不同，已只读失效；旧准备消息和派发状态不再提供 S0 或代码写权。路径、分支、基线和 clean 状态的权威读取仍在工作树登记表。

## 5. 下一步

1. 发布 JY-586/E327 后从其完整哈希建立并核对 WT-339-R2；R1 保持只读，不物理回收。
2. 按两阶段协议重新派发 #339；JY-587 只能更新台账 #339 当前行并追加迁移事件，不再改叶子计划。
3. 继续逐份处置仍依赖旧基础结构的活动详细设计；有唯一业务逻辑者先迁入新设计再退出，不能批量删除。
4. #340—#352 只在具名前置实际接口进入 main 并逐签名接受后依次解除门控。

## 6. 禁止宣称

不得声明：

```text
旧结构已从代码删除
I64、状态或动态已经迁移
自我跨重启恢复已经实现
旧快照已经兼容
#324 可以直接发布
#325—#334 已恢复执行
D455、外设、自我内部循环或最小生命闭环已经接通
```

当前最多声明正式规范定义已发布、失效施工依据已归档、六包新设计与十四个不可变叶子施工合同终审 PASS、单一任务状态权威已建立；代码仍存在明确迁移差距，#339 的 R2 尚未完成重新派发或 S0。
