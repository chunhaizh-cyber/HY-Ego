# 当前状态

更新时间：2026-07-23

任务状态投影版本：JY-605/E346

本文件只保存面向人的派生摘要。任务生命周期投影来源为 `项目记忆/任务状态台账.md` 的上述版本；本文件可能滞后，不参与派发、S0、执行权、依赖解除或完成裁决。历史流水读取计划归档、实施记录、设计记录和 Git。

## 1. 当前治理状态

| 项目 | 当前状态 | 边界 / 依据 |
| --- | --- | --- |
| Git 基线 | JY-605/E346 发布 #339 R6 派发状态与 S0 合同；完整哈希由 Git 对具名提交解析 | R6 建立起点 JY-604，内容冻结基线仍为 JY-589，固定输入仍为 `16dd9410...` |
| 当前角色 | WT-MAIN 设计窗口 | 只修改规则、计划导航和任务治理文档；不修改 C++、工程、自检，不构建、不运行 |
| 正式规范 | 目录正式登记 94 份编号规范、20 份对象根规范 | 4010—4070、7130、7140 已闭合节点直接身份、关系 0—23、类型化记录、自我、恢复和概念分账 |
| 当前主路线 | #338 / DQ-230 / NODE-TYPED-MIGRATION v0.2 | #340—#351 按预冻结接口合同并行形成候选，#352 唯一完成共享接线、切换和完整验证；任务生命周期只读台账 |
| 主线发布占用 | 无 | R6 已建立并完成派发准备读回；JY-605 发布后只允许只读 S0，设计接受前不得 ff-only、汇入或取得占用 |
| 旧 DD-05 WIP | `codex/dd05-design-wip-hold-20260722@9cb9834c...` 只读保护 | 未推送、未进入 main；不得恢复旧派发 |

## 2. 新规范结构

```text
节点：节点编号 + 稳定主键 + 节点类型 + 版本 + 记录状态 + 创建序号
关系：承载全部正式拓扑
领域类型化记录：承载关系无法表达的所属领域必要原始值
索引 / 缓存 / 列表：只加速或投影，必须可重建
完整读取材料：节点 + 关系 + 领域记录的非持久值式组合
恢复：隔离候选全量互证 -> 重建索引 / 投影 -> 完整上下文一次发布
```

自我是唯一当前稳定存在节点和聚合入口，恰有一个当前内部世界场景引用；子存在通过内部世界成员关系保存；实例槽通过宿主 / 模板 / 当前值关系成立；状态和动态列表只由主体关系与索引形成读取投影。

## 3. 当前代码事实与差距

- 当前生产影响面分层扫描为 11 个现行数据操作、17 个现行现代服务、14 份旧兼容服务头；具体旧结构符号和调用点由各叶在固定上游提交重扫。
- 当前节点仓库仍不直接承载新规范要求的稳定主键；现行四仓、领域服务和自检仍大量依赖旧附属句柄和值槽。
- 四仓具备权威材料导出，但没有指定身份导入或隔离装载；快照服务仍拒绝替换运行期仓库。
- 运行期上下文宿主只允许空槽首次发布，不允许在线热替换。新恢复设计保留这一正确边界。
- 4170 侧账已在 main 运行期发布链中，但没有全量冻结和恢复注入；不能单独证明恢复已接通。
- #329 历史代码已进入 main，这一事实不撤销；它与新规范的差异由 #338 治理，不是继续扩展旧结构的许可。

## 4. 任务状态人读投影

下表按“任务状态投影版本”重放台账事件形成，不是状态源；现场裁决必须重新读取任务状态台账同任务最新行。

| 任务 | 计划版本 | 状态版本 | 生命周期投影 | 说明 |
| --- | --- | --- | --- | --- |
| #339 | v0.1 | JY-605/E346 | 已派发待集成回执 | R6 已从 JY-604 建立，派发准备已发送并读回；等待 JY-605 发布后完成全新只读 S0 |
| #340 | v0.2 | JY-591/E332 | 已派发待执行回执 | NT-C1 候选任务已派发；等待全新只读 S0，设计接受前不得写入 |
| #341 | v0.2 | JY-600/E341 | 分支完成待集成 | 固定候选 `e4645128...` 已推送；七个代码文件，完整验证后置 #352，等待 #340 前序候选 |
| #342 | v0.2 | JY-591/E332 | 已派发待执行回执 | 自我投影候选任务已派发；不等待 #339—#341 完成 |
| #343 | v0.2 | JY-600/E341 | 执行中 | S0 PASS 已接受；只按七文件白名单形成系统角色与存在场景候选 |
| #344 | v0.2 | JY-593/E334 | 执行中 | S0 PASS 已接受；形成语素、基础、因果和概念候选，无共享工程移交门控 |
| #345 | v0.2 | JY-600/E341 | 执行中 | S0 PASS 已接受；形成需求、任务、方法、用途和观察候选，唯一筹办规则保持冻结 |
| #346 | v0.2 | JY-600/E341 | 分支完成待集成 | 固定候选 `0651087d...` 已推送且 clean；等待 #340—#345 前序候选 |
| #347 | v0.2 | JY-592/E333 | 执行中 | S0 PASS 已接受；形成请求适配与逐调用点映射候选 |
| #348 | v0.2 | JY-600/E341 | 分支完成待集成 | 固定候选 `443347bf...` 已推送且 clean；等待 #340—#347 前序候选，共享接线仍归 #352 |
| #349 | v0.2 | JY-593/E334 | 执行中 | S0 PASS 已接受；形成 P4 当前格式候选，与 #350 / #351 并行 |
| #350 | v0.2 | JY-592/E333 | 执行中 | S0 PASS 已接受；形成严格解析、隔离恢复与确定重建候选，不等待 #349 完成 |
| #351 | v0.2 | JY-591/E332 | 已派发待执行回执 | 旧材料迁移 / 拒绝候选任务已派发，不等待 #349 / #350 完成 |
| #352 | v0.2 | JY-589/E330 | 依赖门控待执行 | 计划已提前完整冻结；等待 #339—#351 固定候选、#347 映射和最终删除集 |

技术事实投影：WT-339-R2 的任务结果仍为 `16dd9410...`、0/0、clean；集成 R1—R5 均只读失效，R6 位于 JY-604、clean、无 upstream、远端同名 0，当前主线发布占用为零。#341、#346、#348 已形成并推送固定候选；#343、#344、#345、#347、#349、#350 处于执行中；#340、#342、#351 等待 S0。远端当前至少存在 #339 R2、#341、#346、#348 四个待消费任务分支；路径、分支和基线权威仍读工作树登记表。

## 5. 下一步

1. 发布 JY-605 后通知 R6 读取正式仓库完成全新只读 S0；设计接受 PASS 并取得占用后，R6 先 ff-only 到接受提交，再机械汇入 `16dd9410...`。
2. #343、#344、#345、#347、#349、#350 继续在各自白名单内并行实施；#340、#342、#351 继续完成只读 S0，不等待 #339 集成。
3. #341、#346、#348 保持固定候选，不能越过缺失前序直接汇集；其它候选完成后按 `#339 -> #340 -> ... -> #351` 固定顺序逐合同接受。
4. #352 保持最终真实门控，待固定候选、#347 映射和删除集齐备后执行共享接线、切换、物理退役和完整验证。
5. 已进入 main 且无独立证据价值的分支 / clean worktree 经祖先与唯一提交核对后回收；仍执行、待 S0、未集成候选和失败证据继续保留。

## 6. 禁止宣称

不得声明：

```text
旧结构已从代码删除
I64、状态或动态已经迁移
自我跨重启恢复已经实现
旧快照已经兼容
#324 可以直接发布
#325—#334 已恢复执行
D455、外设、自我内部循环或最小生命闭环已经接通
```

当前最多声明正式规范定义已发布、六包设计与十四个叶子施工合同终审 PASS、执行窗口零 Markdown 与具名序列规则已发布、#341 / #346 / #348 三个固定候选已形成、#339 R5 已按 S0 DRIFT 回退且 R6 已建立并发布派发状态。尚未完成 R6 S0 / 集成、#340—#351 全量固定汇集、真实接线、完整验证或能力完成。
