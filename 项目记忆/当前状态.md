# 当前状态

更新时间：2026-07-23

任务状态投影版本：JY-611/E352

本文件只保存面向人的派生摘要。任务生命周期投影来源为 `项目记忆/任务状态台账.md` 的上述版本；本文件可能滞后，不参与派发、S0、执行权、依赖解除或完成裁决。历史流水读取计划归档、实施记录、设计记录和 Git。

## 1. 当前治理状态

| 项目 | 当前状态 | 边界 / 依据 |
| --- | --- | --- |
| Git 基线 | 当前中央治理基线由本文件所属 main 提交与 Git 现场解析 | #339 固定输入等价汇入为 `9140665b...`，集成记录和业务发布提交为 `897d3fd2...`，均为 main 祖先 |
| 当前角色 | WT-MAIN 设计窗口 | #339 主线发布占用已释放；只修改设计治理材料，不修改 C++、工程、自检，不构建、不运行 |
| 正式规范 | 目录正式登记 94 份编号规范、20 份对象根规范 | 4010—4070、7130、7140 已闭合节点直接身份、关系 0—23、类型化记录、自我、恢复和概念分账 |
| 当前主路线 | #338 / DQ-230 / NODE-TYPED-MIGRATION v0.3 | #340—#351 已按 #339 真实接口和 v0.3 预冻结合同完成解耦，不等待提供者实现完成并由永久执行窗口逐段施工；#352 唯一完成共享接线、切换和完整验证；任务生命周期只读台账 |
| 主线发布占用 | 无 | #339 / INT-WB-NODE-TYPED-01-R6 已完成发布与设计归档，集成写权和主线发布占用均已释放 |
| 旧 DD-05 WIP | `codex/dd05-design-wip-hold-20260722@9cb9834c...` 只读保护 | 未推送、未进入 main；不得恢复旧派发 |

## 2. 新规范结构

```text
节点：节点编号 + 稳定主键 + 节点类型 + 版本 + 记录状态 + 创建序号
关系：承载全部正式拓扑
领域类型化记录：承载关系无法表达的所属领域必要原始值
索引 / 缓存 / 列表：只加速或投影，必须可重建
完整读取材料：节点 + 关系 + 领域记录的非持久值式组合
恢复：隔离候选全量互证 -> 重建索引 / 投影 -> 完整上下文一次发布
```

自我是唯一当前稳定存在节点和聚合入口，恰有一个当前内部世界场景引用；子存在通过内部世界成员关系保存；实例槽通过宿主 / 模板 / 当前值关系成立；状态和动态列表只由主体关系与索引形成读取投影。

## 3. 当前代码事实与差距

- 当前生产影响面分层扫描为 11 个现行数据操作、17 个现行现代服务、14 份旧兼容服务头；具体旧结构符号和调用点由各叶在固定上游提交重扫。
- 当前节点仓库仍不直接承载新规范要求的稳定主键；现行四仓、领域服务和自检仍大量依赖旧附属句柄和值槽。
- 四仓具备权威材料导出，但没有指定身份导入或隔离装载；快照服务仍拒绝替换运行期仓库。
- 运行期上下文宿主只允许空槽首次发布，不允许在线热替换。新恢复设计保留这一正确边界。
- 4170 侧账已在 main 运行期发布链中，但没有全量冻结和恢复注入；不能单独证明恢复已接通。
- #329 历史代码已进入 main，这一事实不撤销；它与新规范的差异由 #338 治理，不是继续扩展旧结构的许可。

## 4. 任务状态人读投影

下表按“任务状态投影版本”重放台账事件形成，不是状态源；现场裁决必须重新读取任务状态台账同任务最新行。

| 任务 | 计划版本 | 状态版本 | 生命周期投影 | 说明 |
| --- | --- | --- | --- | --- |
| #339 | v0.1 | JY-607/E348 | 已完成 / 已归档 | 固定输入等价汇入 `9140665b...`，完整验证通过并以 `897d3fd2...` 发布 `origin/main`；R6 写权和主线占用已释放 |
| #340 | v0.3 | JY-611/E352 | 待执行 | 计划激活状态为 `可执行`；不再建立逐计划 R2，长期执行通道目标待建立；WT-340-R1 只作 v0.2 clean 基线证据 |
| #341 | v0.3 | JY-611/E352 | 待执行 | 计划激活状态为 `可执行`；长期执行通道目标待建立，旧提交 `e4645128...` 和未跟踪 Markdown 只作 v0.2 证据 |
| #342 | v0.3 | JY-611/E352 | 待执行 | 计划激活状态为 `可执行`；长期执行通道目标待建立，WT-342-R1 只读失效 |
| #343 | v0.3 | JY-611/E352 | 待执行 | 计划激活状态为 `可执行`；长期执行通道目标待建立，WT-343-R1 只读失效 |
| #344 | v0.3 | JY-611/E352 | 待执行 | 计划激活状态为 `可执行`；长期执行通道目标待建立，旧九个未跟踪代码文件只作 v0.2 证据 |
| #345 | v0.3 | JY-611/E352 | 待执行 | 计划激活状态为 `可执行`；长期执行通道目标待建立，WT-345-R1 只读失效 |
| #346 | v0.3 | JY-611/E352 | 待执行 | 计划激活状态为 `可执行`；长期执行通道目标待建立，旧提交 `0651087d...` 只作 v0.2 证据 |
| #347 | v0.3 | JY-611/E352 | 待执行 | 计划激活状态为 `可执行`；M01—M26 已冻结，长期执行通道目标待建立，旧提交 `3d87bdc...` 只作证据 |
| #348 | v0.3 | JY-611/E352 | 待执行 | 计划激活状态为 `可执行`；长期执行通道目标待建立，旧提交 `443347bf...` 只作 v0.2 证据 |
| #349 | v0.3 | JY-611/E352 | 待执行 | 计划激活状态为 `可执行`；长期执行通道目标待建立，旧提交 `88861f2a...` 和未跟踪 Markdown 只作证据 |
| #350 | v0.3 | JY-611/E352 | 待执行 | 计划激活状态为 `可执行`；长期执行通道目标待建立，旧七个暂存代码文件只作 v0.2 证据 |
| #351 | v0.3 | JY-611/E352 | 待执行 | 计划激活状态为 `可执行`；长期执行通道目标待建立，WT-351-R1 只读失效 |
| #352 | v0.3 | JY-610/E351 | 依赖门控待执行 | 127 个唯一修改白名单路径、39 文件删除集和唯一新建自检路径已冻结；仍等待 #339—#351 v0.3 固定候选和 #347 映射 |

技术事实投影：WT-339-R2 的任务结果仍为 `16dd9410...`、0/0、clean；集成 R1—R5 均只读失效。R6 的发布提交 `897d3fd2...` 已由 main 承载，物理 worktree、同名本地分支和远端分支均已回收；主线发布占用为无。WT-340-R1—WT-351-R1 全部绑定已经退出的 v0.2 合同，现只读失效；其中提交、暂存区、未跟踪文件和 clean 基线均原样保留为证据。#340—#351 不再建立逐计划 R2；永久窗口“执行当前计划任务”及其 worktree 已存在，但仍为 clean detached HEAD，目标长期执行通道 `EXEC-WB-CURRENT-PLAN-01-R1` 尚未形成结构化绑定；#352 也没有物理 worktree。路径、分支和基线权威仍读工作树登记表。

## 5. 下一步

1. 设计窗口只继续形成和修订计划，不向任何执行窗口发送开始、继续、停止、S0 接受或下一项指令。
2. JY-611/E352 发布后，把现有永久窗口 worktree 从该提交绑定到 `codex/execution-current-plan-r1`，核对后再以结构化登记把 #340—#351 共同绑定到该通道；不为每份计划建立 R2，旧 R1 不得重读新计划后继续写入。
3. 长期通道一次只允许一个活动计划段，段间以 clean 结果或计划允许的 clean 代码断点串行累计；最终终端提交按十二项计划段映射交给 #352 完整接线和验证，旧 v0.2 候选不直接进入该父链。
4. 所有失败证据、未集成候选和执行 WIP 原样保留；设计窗口不得清理、提交或覆盖执行现场。

## 6. 禁止宣称

不得声明：

```text
旧结构已从代码删除
I64、状态或动态已经迁移
自我跨重启恢复已经实现
旧快照已经兼容
#324 可以直接发布
#325—#334 已恢复执行
D455、外设、自我内部循环或最小生命闭环已经接通
```

当前最多声明正式规范定义已发布、五份受影响详细设计与 #340—#352 v0.3 施工合同已重建、执行窗口零 Markdown 与自主消费规则已发布、#339 已通过 R6 完整验证并发布主线归档、旧 v0.2 执行现场已作为只读证据隔离。长期执行通道规则与目标已经形成但技术通道尚未建立；尚未完成 #340—#351 v0.3 计划段候选、累计汇集、真实接线、完整验证或能力完成。
