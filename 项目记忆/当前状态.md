# 当前状态

更新时间：2026-07-22

本文件只保存当前仍影响设计、执行、集成和完成声明的应用结果；历史流水读取计划归档、实施记录、设计记录和 Git。

## 1. 当前治理状态

| 项目 | 当前状态 | 边界 / 依据 |
| --- | --- | --- |
| Git 基线 | `main == origin/main == 1185e1b458b9c83244cd775dea3825931a134787` / JY-583 | 当前 NT-P0 归档与新设计包属于尚未提交的设计 WIP，需独立复核后精确提交 |
| 当前角色 | WT-MAIN 设计窗口 | 可以修订规范、设计、计划和中央状态；不修改 C++、工程、自检，不构建、不运行 |
| 正式规范 | 目录正式登记 94 份编号规范、20 份对象根规范 | 4010—4070、7130、7140 已闭合节点直接身份、关系 0—23、类型化记录、自我、恢复和概念分账 |
| 当前主路线 | #338 / DQ-230 / NODE-TYPED-MIGRATION | 六个新设计包和 #339—#352 叶子路线终审 PASS；正在收口 #339 派发治理 |
| 主线发布占用 | 无 | 当前没有集成任务进入 `集成中`；设计窗口持有中央治理写权 |
| 旧 DD-05 WIP | `codex/dd05-design-wip-hold-20260722@9cb9834c...` 只读保护 | 未推送、未进入 main；不得恢复旧派发 |

## 2. 新规范结构

```text
节点：节点编号 + 稳定主键 + 节点类型 + 版本 + 记录状态 + 创建序号
关系：承载全部正式拓扑
领域类型化记录：承载关系无法表达的所属领域必要原始值
索引 / 缓存 / 列表：只加速或投影，必须可重建
完整读取材料：节点 + 关系 + 领域记录的非持久值式组合
恢复：隔离候选全量互证 -> 重建索引 / 投影 -> 完整上下文一次发布
```

自我是唯一当前稳定存在节点和聚合入口，恰有一个当前内部世界场景引用；子存在通过内部世界成员关系保存；实例槽通过宿主 / 模板 / 当前值关系成立；状态和动态列表只由主体关系与索引形成读取投影。

## 3. 当前代码事实与差距

- 当前生产影响面分层扫描为 11 个现行数据操作、17 个现行现代服务、14 份旧兼容服务头；具体旧结构符号和调用点由各叶在固定上游提交重扫。
- 当前节点仓库仍不直接承载新规范要求的稳定主键；现行四仓、领域服务和自检仍大量依赖旧附属句柄和值槽。
- 四仓具备权威材料导出，但没有指定身份导入或隔离装载；快照服务仍拒绝替换运行期仓库。
- 运行期上下文宿主只允许空槽首次发布，不允许在线热替换。新恢复设计保留这一正确边界。
- 4170 侧账已在 main 运行期发布链中，但没有全量冻结和恢复注入；不能单独证明恢复已接通。
- #329 历史代码已进入 main，这一事实不撤销；它与新规范的差异由 #338 治理，不是继续扩展旧结构的许可。

## 4. 当前计划状态

| 路由 | 当前状态 |
| --- | --- |
| #323 | 总治理继续；新基础结构转入 #338 |
| #338 | 六包新设计、图谱、成对流程图和十四个叶子计划已形成；独立终审 PASS |
| #339 | NT-P1 已晋级待执行；当前尚未派发 |
| #340—#342 | 固定 P2A -> P2B -> P2C 真实接口链，依赖门控 |
| #343—#348 | P3 业务面可隔离，但当前工程 XML / 运行器验证所有权固定串行，依赖门控 |
| #349—#352 | P4-FREEZE -> RESTORE -> LEGACY -> CUTOVER 固定串行；#352 还须按实际调用图升版 |
| #324 | 旧集成结果只作证据；暂停主线发布 |
| #325—#328 | 旧计划已归档、代码许可失效；等待 #338 重建 |
| #329 | 历史已完成；当前代码作为迁移与回归事实 |
| #330—#334 | 旧计划已归档；旧接口、详细设计和依赖基础失效；不得恢复派发 |

当前没有已经派发的代码计划。#339 已形成五件套、精确叶子计划并通过独立终审，但中央提交推送、worktree / 分支 / 所有权登记与两阶段消息尚未完成，因此仍不是派发事实。

## 5. 下一步

1. 精确提交并推送终审通过的六包设计、图谱、流程图、十四叶子计划和归档治理。
2. 以该提交为 #339 冻结基线，登记独立任务 worktree / 分支 / 所有权并完成两阶段真实派发。
3. 继续逐份处置仍依赖旧基础结构的活动详细设计；有唯一业务逻辑者先迁入新设计再退出，不能批量删除。
4. #340—#352 只在具名前置实际接口进入 main 并逐签名接受后依次解除门控。

## 6. 禁止宣称

不得声明：

```text
旧结构已从代码删除
I64、状态或动态已经迁移
自我跨重启恢复已经实现
旧快照已经兼容
#324 可以直接发布
#325—#334 已恢复执行
D455、外设、自我内部循环或最小生命闭环已经接通
```

当前最多声明正式规范定义已发布、失效施工依据已归档、六包新设计与十四个叶子路线终审 PASS；代码仍存在明确迁移差距，#339 尚未形成派发事实。
