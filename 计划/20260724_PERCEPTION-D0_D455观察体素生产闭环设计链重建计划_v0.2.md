# PERCEPTION-D0 D455—观察—体素生产闭环设计链重建计划 v0.2

编号：PERCEPTION-D0；路由：#360 / DQ-252；日期：2026-07-24

设计性质：正式设计治理计划，不授权修改代码、工程、自检或施工记录。计划状态唯一读取 `计划/计划索引.md`。

## 1. 目标与事实边界

本计划把现有 D455 L0 四流采样底座到任务承接、四方法、世界结构、体素和真实生产装配的施工依据链冻结为 `PER-C1—PER-C13 / ABI 2`。当前代码只实现原始同步采样、进程内缓存和上行桥；C1—C13 目标模块尚未实现。

完成本计划只允许声明：施工规范、四组流程图、四份详细设计、函数结构知识图谱和 #361—#373 施工合同已经范围一致地收口。不得声明 D455、外设、观察、体素或生产闭环已经实现、编译、接线或通过真实硬件验收。

## 2. 设计产物

- 五份受影响正式规范：6240、6250、6300、6340、6350。
- 四组配对流程图：`流程图/20260724_PERCEPTION-INGEST_*_v0.2.*`、`PERIPHERAL-TASK_*_v0.2.*`、`OBSERVE-VOXEL_*_v0.2.*`、`PERCEPTION-RUNTIME_*_v0.2.*`。
- 四份现行详细设计：D455 产品 / 报告、任务 / 方法、观察 / 体素、生产装配 / 恢复。
- 知识图谱：`规范/详细设计/函数结构知识图谱/20260724_PERCEPTION-D0_D455观察体素生产闭环函数结构知识图谱_v0.2.md`。
- #361—#373 v0.2 叶子计划与计划索引精确 plan blob。

## 3. 冻结裁决

1. L0 只含同步彩色、深度、左右红外、标定与采集元数据；点云从 L1 开始。
2. `C3 -> C4 -> C5` 只表示原始逐簇、稳定子集、扫描 / 跟踪之间的材料派生方向；三者各自形成不同的强类型产品候选，并分别按 `(C3 | C4 | C5) -> C1验证 -> C2发布` 收口。报告稳定身份是跨窗口、任务和代次的唯一消费键。
3. C7 只读 SELF-C2 筹办许可投影；C8 只读 SELF-C3 方法登记；C10 通过 SELF-C4 端口回流。
4. 不新增观察事实节点或体素节点类型。观察结果映射到存在、关系、状态 / 动态事实；体素模型、单元和消费记录是领域类型化权威记录。
5. C12 只读 C11 已发布模型和 C5 稳定观察子集，不消费原始帧、逐簇或泛化观察材料。
6. C13 只编排权威结构、报告治理、任务筹办和体素四类恢复提供者；全部确认后最后发布，失败逆序撤销。
7. #361—#372 按冻结合同形成隔离候选，不等待提供者代码实现；#373 唯一拥有工程、入口、运行器、生产宿主、线程、恢复和真实硬件验收。

## 4. 机械收口

1. 逐项审计现行规范、代码底座和 v0.1 设计；将现行事实与待实现合同分开。
2. 冻结 C1—C13 的模块 / 文件、字段、稳定枚举、公开函数、ABI、构造注入、所有权、生命周期、发布 / 撤销 / 恢复、逻辑内返回、内部错误和漂移点。
3. 校正四组流程图并保证 Markdown / HTML Mermaid 主体逐字一致。
4. 形成知识图谱和十三份互斥叶子合同；#373 保留真实固定结果门控。
5. 运行 `git diff --check`、`python .\tools\check_specs.py --strict`、流程图配对检查和独立只读覆盖审计。
6. 只暂存本设计包，提交并按规则推送 `origin/main`。

## 5. 完成门禁

- 正式规范、流程图、详细设计、知识图谱和计划范围一致；
- #361—#372 白名单互斥且共享面唯一归 #373；
- 每份计划具备 S0、步骤、验证、DRIFT / FAIL / WIP 收口和专属记录；
- #361—#372 的精确 plan blob 可被执行智能体独立选择；#373 仍由真实结果提交门控；
- 文档验证与独立只读一致性审计通过。
