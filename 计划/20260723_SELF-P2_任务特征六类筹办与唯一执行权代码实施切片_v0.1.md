# SELF-P2 任务特征、六类筹办与唯一执行权代码实施切片

编号：SELF-P2；路由：#355 / DQ-247；版本：v0.1；日期：2026-07-23

设计状态：范围草案。当前计划状态只读计划索引；#353 未冻结 SELF-C2 与精确施工边界前不得实施。

## 目标与依据

依据 3200、5200—5250、5300 和 8200，实现任务完整特征集合、阶段投影、可重复筹办、六类闭合结果，以及按任务稳定身份原子取得唯一筹办执行权。

六类结果必须由 #353 从 5210 / 5230 逐项冻结；不得用裸枚举、线程编号或“另一线程形成同一选择”替代。无候选方法时返回具名筹办结果，不自动创建方法首；任务筹办直接返回最小方法结果，不留下无法解决而回退的中间状态。

## 冻结前必须补齐

- SELF-C2 / v0.1 任务特征、筹办轮次、占用、选择、冻结和值式结果合同；
- 同一任务互斥、不同任务并行、同任务重复筹办的状态迁移；
- 任务域提交方法、回执与阶段特征的唯一写入权；
- 精确文件所有权和并发 / 故障验收矩阵。

## 候选步骤

1. S0 核对任务节点直接服务与 #339—#352 接口。
2. 实现任务特征组合读取和阶段投影。
3. 实现任务身份唯一筹办占用，失败者在召回前返回。
4. 实现召回、选择、冻结和六类闭合结果。
5. 覆盖重复筹办、同任务竞争、不同任务并发和读回不一致。

## 边界

本切片不执行方法、不结算任务 / 需求、不学习、不写生产线程或入口。第二路径越过筹办权进入召回属于内部并发错误，不得幂等收口。
