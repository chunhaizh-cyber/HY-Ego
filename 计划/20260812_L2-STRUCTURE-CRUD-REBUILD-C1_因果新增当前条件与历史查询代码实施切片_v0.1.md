# 因果新增当前条件与历史查询代码实施切片 v0.1

日期：2026-08-12

计划身份：`L2-STRUCTURE-CRUD-REBUILD-C1`

状态：`待激活`；4230 v0.3 与 G2 已冻结合同，但 T1 / T2 尚未发布状态身份来源和最终状态 ABI，且用户指定路线中的前序 DYN1 / DYN2 尚未完成、退出并释放单代码段与共享验证资源

## 1. 目标、范围与完成条件

在 4230 v0.3、G2 七族身份来源合同和 T1 / T2 最终 `L2状态结构服务` 之上，新建唯一 `L2因果结构服务`，完成因果 owner / 身份来源、结构类型登记、因果原子新增、单项当前 / 历史完整读取，以及按条件状态、动作和结果状态枚举全部当前因果。

本叶严格对应总体详细设计 C1。C2 才拥有条件 / 动作 / 结果引用替换、来源换代、因果退出和引用保护；A1 才拥有六服务聚合公开面。本叶不得提前形成这些 DTO、函数、装配聚合或验证。后继顺序仍为 `C1 -> C2 -> A1`。

旧因果 / 动态模式、历史状态 / 动态登记代码只作已删除反例和命名冲突证据，禁止复用、复制、适配、搬移或改名沿用其业务语义、DTO、函数体、概率、模板、计数、动态证据、主动 / 被动分类、动作归因或验证剧本。

完成必须同时满足：

1. 形成 G2 因果专属 move-only owner 交付、不可变族锚点、族归属关系类型和 `读取因果身份来源` 当前 / 历史 ABI；
2. 建立条件状态、动作、结果状态三种关系类型和一个独立材料引用表示的来源属性类型；
3. `新增因果` 以一份 owner-scoped L1 中性写集原子建立因果节点、族归属关系、非空条件关系组、可空动作关系、非空结果关系组及唯一来源属性值 / 当前槽；
4. 条件 / 结果请求均使用 `L2有序身份引用`，组非空，顺序和目标非零，按 `(顺序, 目标)` 严格升序且无重复；不同顺序可引用同一状态，同一顺序也可引用不同状态；
5. 每个条件 / 结果目标均以请求期望事实代次调用 T1 / T2 最终 `读取状态身份来源(当前)`，证明当前正式 `L2状态身份`；不比较状态内容、主体、特征、值、时间或条件 / 结果组间交集；
6. 动作为空合法；非空动作只是非零不透明稳定编码，只由 L1 同期望一致当前投影证明为当前普通节点，不依赖任务、方法、动态或其它业务服务，也不推断动作；
7. 来源稳定编码非零，作为 `L2独立材料引用` 写入因果来源属性，值事实的来源节点也等于该编码；L1 在同一写集中验证来源当前可引用，C1 不解释来源类别；
8. 单项当前 / 历史读取闭合因果身份来源、条件 / 动作 / 结果关系、来源属性和全部状态端点，所有项目共享同一事实截止；
9. 三类当前条件查询先验证查询端点，再返回按因果身份升序、无重复的完整当前因果组；合法端点无匹配返回已读取空组；
10. 同一幂等身份同完整请求返回首次完整结果且零推进；同键异任一组项、顺序、动作有无 / 编码或来源返回幂等冲突；旧期望新键返回漂移；
11. 入口、登记、许可、未找到、已退出、属性、漂移、幂等、资源和内部错误精确分账；提交前非成功为空载荷且零普通事实变化，发布后读回失败不补偿事实，只允许原请求收敛；
12. 生产代码零验证专用实体、接口、友元、状态出口、计数器、故障注入、sleep 或测试分支；临时验证三文件在结果与永久记录发布、计划退出后由独立清理提交删除；
13. T1 / T2 最终状态来源和 CRUD ABI 尚未形成，或用户指定路线中的 DYN1 / DYN2 尚未完成、退出并释放单代码段与共享验证资源时，C1 只能保持 `待激活`，不得施工或建立骨架；后两者只约束施工顺序与资源，不是 C1 的接口依赖。

C1 只保存调用方完整提交的纯结构。它不判断因果真实、必然、主动或被动，不要求条件与结果不同，不执行无环证明，不解释来源，不推断动作，不从状态、动态、历史统计或邻接事实自动生成因果，也不建立概率、计数、版本、证据动态、适用范围、模板或业务准入字段。

## 2. 正式依据、当前事实与激活门禁

正式依据：

- `AGENTS.md`
- `.codex/rules/设计执行双窗口交互规则.md`
- `.codex/rules/单工作区串行执行与发布规则.md`
- `规范/规范目录.md`
- `规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`
- `规范/4015_子规范_L1简化事实基座.md`
- `规范/4190_子规范_语义分层世界结构与状态动态因果边界.md`
- `规范/4200_子规范_世界树业务服务与同代次完整事实快照.md`
- `规范/4230_子规范_L2六类纯结构统一合同.md`
- `规范/详细设计/20260812_L2-STRUCTURE-CRUD-REBUILD-G2_七族节点身份来源与迁移顺序详细设计_v0.1.md`
- `规范/详细设计/20260810_L2-STRUCTURE-CRUD-REBUILD_旧链清理与六类结构CRUD分步重建详细设计_v0.1.md`
- T1、T2、DYN1、DYN2 现行计划

1190、4210、4220、4310—4330 的证据、主动 / 被动、模板、用途和业务准入属于因果业务层或后继计划，不能覆盖 4230 的 C1 纯结构合同。0050 中一次完整证据与主动 / 被动口径不授权 C1 裁决证据是否完整或动作是否成立。

### 2.1 创建侧当前事实

创建基线为 `e13f320197f7dd05dc7a0e82de8a40abf0fd3780`，创建时 `main == origin/main`、ahead / behind=`0 / 0`、Git index 为空：

- 4230 v0.3 blob=`3d788d5547369aff22dbdad0ad44f07d84466f01`，固定非空有序条件状态组、可空不透明动作、非空有序结果状态组、来源与生命周期；
- G2 详细设计 blob=`dd765cee252bfc7634fcca3ec8b9b9b05884448c`，固定因果 owner 常量 `0x4C31'4F57'4E45'5236`、来源登记幂等常量 `0x4C32'4944'5352'4306`、`L2因果所有者交付`、`读取因果身份来源` 和构造前缀 `const L1 + const 状态服务 + 因果 owner 交付`；
- 公共 L2 数据头 blob=`eecebc3f0dbd03d87b9f064fb569311dfb31f491`，已实现 `L2因果身份`、`L2有序身份引用`、共同结果状态和 `L2节点族身份来源事实`；
- L1 owner CRUD 数据头 / L1 数据头 / 服务 blob 分别为 `072ed11f1a7ea3dd8c4db981ce9f77291d9afc8d` / `37f54541f0c57cecf049d8e8fc14f79e49b73fa8` / `e53bb8b7fb893f270a2243502e9b800267907e72`，支持 owner-scoped 节点 / 关系 / 值 / 槽同写、当前一致投影、历史关系 / 属性值组、单事实历史、当前代次、幂等和引用冲突；
- 主工程 / filters blob=`b00118b5901e2625f453941cd2598ee1bf52186a` / `f3b1d0e6635949ec2d5d3823f2ee172de8d6880c`，正式基线为 29 个模块、14 个头；
- 普通应用 provider blob=`d65cfc107632d943e2ec342247dce0b141d560e9`；因果服务最终由同一 L1 运行包按 G2 固定构造顺序发布，不能新建第二 provider 或身份工厂；该构造顺序不使因果服务持有、import 或调用动态服务；
- T1 plan blob=`f0e248000220ec082b64cfca3ce994e07d59d0f4`、T2 plan blob=`2edd7dbe9463bedf0564e8dc5d870b0cbde57120`，均为 `待激活` 且无真实结果；当前两个未跟踪状态源码只是异主可逆草稿，不是 ABI；
- DYN1 plan blob=`a97f9cba457d0270a7b067ad61885041bb46ea2e`、DYN2 plan blob=`79a1a593932facbdb2fa6eccca802a780403b7c0`，均为 `待激活` 且无真实结果；
- 正式 HEAD 中 `L2状态结构.数据.h`、`服务.L2状态结构.ixx`、`L2动态结构.数据.h`、`服务.L2动态结构.ixx`、`L2因果结构.数据.h` 和 `服务.L2因果结构.ixx` 均不存在；因果生产符号零命中；
- D2 已物理删除旧因果 / 动态模式算法材料闭包，D3 已删除历史状态 / 动态服务 DTO 闭包，没有可原位扩展或兼容转发的旧因果实现。

能力裁决：复用 L1 owner / 写集 / 同截止当前历史读取和公共 L2 DTO；复用 T1 / T2 最终 `读取状态身份来源` 验证条件 / 结果；新建唯一因果数据头、服务、owner 交付、身份来源、结构登记和 C1 CRUD。C1 的直接服务、import、调用和机器语义依赖严格只有状态服务与 L1。DYN1 / DYN2 不向 C1 提供 ABI 或业务数据，只是用户指定路线的前序施工切片，并受同一活动代码段和共享验证资源串行约束。零动态服务依赖、零任务 / 方法依赖、零第二仓 / 服务 / 幂等账 / 缓存 / SQL / 日志。

### 2.2 激活门禁

C1 只有同时满足以下事实才能由索引所有者从 `待激活` 改为 `可执行`：

1. T1 / T2 最终结果已进入正式 `main` / `origin/main`，经交互接受、索引退出并由独立提交清理临时验证资源；
2. 状态数据头 / 服务正式提供 G2 `L2状态身份来源读取请求 / 结果`、`读取状态身份来源(当前 / 历史)`、完整状态读取和当前状态身份验证，wrong-family、legacy、裸节点、同 owner 无来源固定结构化拒绝；
3. 状态结果逐字段证明最终构造 `const L1、const 存在、const 特征、状态 owner 交付`，provider 在同一 L1 运行包发布唯一状态服务；
4. 用户指定路线的前序 DYN1 / DYN2 已完成、经交互接受、索引退出并清理临时资源，唯一活动代码段、主工程 / provider 写权和共享构建 / 验证资源已释放；本项只裁决施工顺序与资源可用性，不要求 C1 消费其 DTO、函数、结果或 ABI；
5. G2 因果 owner / 来源合同、L1 owner-scoped 写集、历史关系组、历史属性值组、当前一致投影和工程登记仍支持第 3—10 节；
6. C1 十路径无其它所有者，Git index、唯一代码执行段、发布租约和构建 / 验证资源通过新 HEAD S0；
7. 新 HEAD 重新计算状态最终结果 / blob、provider、主工程 / filters、生产模块 / 头数量，不用本计划的不存在状态、计划文本或未跟踪草稿代替结果事实。

任一状态最终 ABI / 状态映射、G2 因果构造合同或 L1 行为差异会改变 C1 DTO、写集、截止、读回或验证时，保持 `待激活` 并具名退回计划支撑；DYN1 / DYN2 实现差异本身不构成 C1 设计漂移，只在未释放重叠文件 / 单代码段 / 共享资源时继续阻止调度。执行侧不得增加适配层、第二 provider、动态依赖、业务动作解释或验证钩子。

## 3. 精确允许文件与临时生命周期

C1 实施只允许修改或新增：

1. `海中鱼巣/领域/L2因果结构.数据.h`
2. `海中鱼巣/领域/服务.L2因果结构.ixx`
3. `海中鱼巣/装配.普通应用.ixx`
4. `海中鱼巣.vcxproj`
5. `海中鱼巣.vcxproj.filters`
6. `验证工具/L2因果新增查询参数验证.cpp`
7. `验证工具/L2因果新增查询参数验证.vcxproj`
8. `验证工具/运行L2因果新增查询参数验证.ps1`
9. `施工记录/20260812_L2-STRUCTURE-CRUD-REBUILD-C1_施工记录.md`
10. `验证记录/20260812_L2-STRUCTURE-CRUD-REBUILD-C1_验证记录.md`

数据头是全部因果 DTO 和纯值完整性函数唯一所有者；服务模块是因果 owner、来源、类型登记、节点、关系和值唯一写入所有者；普通应用只按 G2 前缀形成因果 owner / 服务并提供同实例 const getter，不形成第二写入口。

临时三文件不进入生产工程 / solution，只经 L1、T1 / T2 最终状态服务和 C1 因果服务公开入口验证。结果与两份永久记录发布、计划退出后，由后续唯一清理所有者以独立提交只删除三份临时文件。

禁止修改 L1、公共 L2、存在 / 特征 / 状态 / 动态生产代码、规范、详细设计、路线输入、计划索引、流程图、能力目录、旧因果 / 动态模式、历史状态 / 动态代码、其它验证工具 / 记录和 `.codex-build/`。

## 4. 因果 owner、身份来源与结构类型登记

严格采用 G2：

```cpp
class L2因果所有者交付; // move-only，只由同模块形成函数构造

std::optional<L2因果所有者交付> 尝试形成L2因果所有者交付(
    const L1事实基座服务&, L1所有者范围交付&&);

explicit L2因果结构服务(
    const L1事实基座服务& 第一层读取服务,
    const L2状态结构服务& 状态服务,
    L2因果所有者交付&& 因果所有者交付);

L2因果身份来源读取结果 读取因果身份来源(
    const L2因果身份来源读取请求& 请求) const;
```

构造私有执行 `初始化因果身份来源()`，建立不可变族锚点与族归属关系类型；失败则构造失败，provider 不发布半构造服务。来源 DTO / 当前历史行为、wrong-family / legacy / 裸节点 / malformed 状态和生命周期严格复用 G2，不复制中央身份逻辑。

C1 另冻结四个结构类型登记节点：

```text
0x0206'0101U -> 普通关系类型 / 因果条件状态
0x0206'0102U -> 普通关系类型 / 因果动作
0x0206'0103U -> 普通关系类型 / 因果结果状态
0x0206'0201U -> 属性类型 / 因果来源引用 / 独立材料引用
```

`建立因果结构类型登记` 使用私有域 `0x22ULL`、公开登记幂等身份 `{1}`，其 L1 写入幂等值固定为 `0x2200'0000'0000'0001ULL`；一份 owner-scoped 写集建立四节点。成功 / 精确重复取得四个不同非零编码并按首次代次历史读回；`读取当前因果结构类型登记` 只按私有定位一致读取，允许一次完整漂移重读，不扫描全仓、不猜编码。登记写必须在 CRUD 并发前单线程完成。

因果身份来源登记幂等值 `0x4C32'4944'5352'4306ULL` 和结构登记值 `0x2200'0000'0000'0001ULL` 是本因果 owner 写域的保留值。`新增因果` 在纯值校验阶段拒绝把任一保留值作为公开 `L2结构幂等身份`，返回入口拒绝且零变化，避免调用方与私有初始化 / 登记共享幂等项。

四个类型只表达结构角色。来源属性采用独立材料引用，不把来源解释为动态、证据、方法、文件或业务对象。

## 5. C1 DTO 与公开函数

```cpp
struct L2因果事实 final {
    L2因果身份 身份;
    std::vector<L2有序身份引用> 条件状态;
    std::optional<稳定编码> 动作;
    std::vector<L2有序身份引用> 结果状态;
    稳定编码 来源稳定编码;
    L2生命周期 生命周期;
};

struct L2因果自有关系引用 final {
    稳定编码 族归属关系稳定编码;
    std::vector<稳定编码> 条件关系稳定编码;
    std::optional<稳定编码> 动作关系稳定编码;
    std::vector<稳定编码> 结果关系稳定编码;
    稳定编码 来源值稳定编码;
};

struct L2因果新增请求 final {
    L2结构请求头 请求头;
    L2结构幂等身份 幂等身份;
    std::vector<L2有序身份引用> 条件状态;
    std::optional<稳定编码> 动作;
    std::vector<L2有序身份引用> 结果状态;
    稳定编码 来源稳定编码;
};

struct L2因果写入结果 final {
    L2结构结果头 结果头;
    std::optional<L2因果事实> 因果;
    std::optional<L2因果自有关系引用> 自有事实;
    bool 成功() const noexcept;
};

struct L2因果完整读取请求 final {
    L2结构请求头 请求头;
    L2读取类别 读取类别 = L2读取类别::当前;
    L2因果身份 因果;
    std::uint64_t 历史截止事实代次 = 0;
};

struct L2因果完整读取结果 final {
    L2结构结果头 结果头;
    L2读取类别 读取类别 = L2读取类别::当前;
    std::uint64_t 历史截止事实代次 = 0;
    std::optional<L2因果事实> 因果;
    std::optional<L2因果自有关系引用> 自有事实;
};

struct L2按条件状态当前因果读取请求 final {
    L2结构请求头 请求头;
    L2状态身份 条件状态;
};
struct L2按动作当前因果读取请求 final {
    L2结构请求头 请求头;
    稳定编码 动作;
};
struct L2按结果状态当前因果读取请求 final {
    L2结构请求头 请求头;
    L2状态身份 结果状态;
};
struct L2当前因果组读取结果 final {
    L2结构结果头 结果头;
    std::vector<L2因果事实> 因果;
    bool 成功() const noexcept;
};
```

`L2因果自有关系引用` 是 C2 显式闭包材料：族归属、条件、可选动作、结果和来源值编码均非零，条件 / 结果编码组各非空、严格升序、无重复，所有编码全局两两不同；不公开关系类型、属性类型、本地键、owner 或内部定位。

新增请求要求合同、期望代次、非保留幂等身份、来源非零；条件 / 结果各非空并按 `(顺序, 目标)` 严格升序，每项顺序 / 目标非零；动作 optional 有值时非零。单项当前要求历史截止 0；历史要求 `0 < 历史截止 <= 期望代次`。条件 / 结果查询只接受非零状态身份；动作查询只接受非零编码。

公开函数固定为：

```cpp
L2因果结构类型登记结果 建立因果结构类型登记(...);
L2因果结构类型登记结果 读取当前因果结构类型登记() const;
L2因果写入结果 新增因果(const L2因果新增请求& 请求);
L2因果完整读取结果 读取完整因果(const L2因果完整读取请求& 请求) const;
L2当前因果组读取结果 按条件状态读取全部当前因果(...) const;
L2当前因果组读取结果 按动作读取全部当前因果(...) const;
L2当前因果组读取结果 按结果状态读取全部当前因果(...) const;
```

服务不可复制，成员顺序固定为 const L1、const 状态服务、因果 owner 写端口、来源定位、结构登记定位。全部业务标识符中文；调用 L1 / L2 的辅助向上送出，纯值排序 / 映射为无适用错误分支；分配失败映射资源失败，其它未处理异常映射内部不一致。

## 6. 新增因果的一份同代次写集

本地键固定：

```text
因果节点本地键       = 0x0206'1001U
因果族归属关系本地键 = 0x0206'1002U
条件关系本地键基值   = 0x0206'2000U + 项序号(1..n)
动作关系本地键       = 0x0206'3001U
结果关系本地键基值   = 0x0206'4000U + 项序号(1..n)
来源值本地键         = 0x0206'5001U
```

条件 / 结果单组项数各不得超过 `0x0FFF`，防止本地键域重叠；超过为入口拒绝。项序号只用于本写集本地键，关系的 `角色或顺序` 精确使用请求 `L2有序身份引用::顺序`。族归属角色固定 1，动作关系角色固定 1。

流程固定为：

```text
纯值校验 -> 读取当前因果结构登记 -> 形成完整不可变写集
-> 探测当前事实代次
-> 代次不同：恰一次直提原写集，让 L1 先裁决重复 / 冲突 / 漂移
-> 代次相同：逐项读取状态身份来源(当前)
-> 若有动作，以同一期望一次一致当前投影证明当前普通节点
-> 恰一次 owner-scoped 提交
-> 从首次代次历史来源、节点、关系、属性值和端点重建首次结果
```

写集固定为：

```text
节点 = [{因果节点本地键, 普通}]
关系 = [
  {族归属本地键, 源=因果节点, 目标=因果族锚点,
   类型=族归属关系类型, 角色=1},
  每个条件项 {条件本地键, 源=因果节点, 目标=项.目标,
               类型=条件关系类型, 角色=项.顺序},
  可选动作 {动作本地键, 源=因果节点, 目标=动作,
             类型=动作关系类型, 角色=1},
  每个结果项 {结果本地键, 源=因果节点, 目标=项.目标,
               类型=结果关系类型, 角色=项.顺序}
]
值 = [{来源值本地键, 所属=因果节点,
       类型=来源属性类型,
       材料=L2独立材料引用{来源}, 来源=来源}]
属性槽变更 = [{因果节点, 来源属性类型, 来源值本地键}]
退出事实 = []
```

节点、族归属、全部角色关系、来源值和槽在同一事实代次发布。来源不能等于新建因果本地节点，因为请求只接受既有稳定编码；动作 / 来源仍被 L1 引用闭包保护。C1 不要求动作等于方法或动态，不要求来源等于动作、状态、动态或证据。

成功 / 精确重复必须取得全部本地键的一一非零唯一映射。首次历史读回证明因果节点与族归属、全部条件 / 动作 / 结果关系、来源值同创建代次，关系端点 / 类型 / 顺序、来源材料和值来源精确匹配；以首次事实代次为历史截止逐项调用 `读取状态身份来源(历史)`。任一缺项、多项、错端点、错顺序、错类型、错来源或混代返回内部不一致，不二次提交。

## 7. 当前 / 历史完整读取

当前读取先取得登记，用一次 owner-aware 一致当前投影读取因果 owner、因果节点、锚点、归属类型、唯一归属关系、三类源关系组和来源属性值。要求：

- 因果身份来源完整；条件 / 结果关系组非空，动作关系组 0 或 1；
- 关系源均为因果，类型匹配；条件 / 结果角色为正，投影按 `(角色, 目标)` 严格升序无重复；动作角色 1；
- 来源槽唯一，值所属因果、类型为来源属性、材料是与值来源相同的非零独立材料引用；
- 以同一期望逐项调用 `读取状态身份来源(当前)`，并对可选动作 / 来源用 L1 当前事实证明可引用；
- 再读取同一因果投影，前后逐字段相等且截止等于请求期望。

历史读取固定为：

```text
读取当前代次并等于请求期望
-> 读取因果 owner / 节点 / 锚点 / 类型历史
-> 按因果为源读取归属、条件、动作、结果历史关系组(历史截止)
-> 读取因果历史属性值组(历史截止)
-> 逐项读取状态身份来源(历史截止)
-> 读取动作 / 来源历史事实
-> 再读当前代次并等于请求期望
-> 重建截止投影，隐藏晚于截止的未来退出
```

创建前返回未找到；当前入口已退出返回已退出；C1 未实现退出，因此首次实现的合法因果在创建及之后截止应完整。历史不得以当前关系、槽、状态、动作或来源补项。身份来源、关系、属性、端点或生命周期矛盾返回内部不一致；两次代次变化返回漂移；非成功载荷为空。

## 8. 三类当前条件查询

按条件 / 结果状态查询先调用 `读取状态身份来源(当前)` 验证请求端点，再以同一期望从对应关系类型的目标关系组取得源因果。按动作查询先用 L1 一致当前投影证明动作是当前普通节点，再读取动作类型目标关系组。

每条关系类型、目标、角色必须匹配；条件 / 结果角色必须正，动作角色固定 1。对每个唯一源调用 `读取完整因果(当前)`，要求同一截止且完整结果含对应查询项。按因果身份严格升序返回；重复源、关系与单项读回矛盾、部分成功或混代均内部不一致。

三个入口都允许合法空组，不返回“最可能”“最新”“必然”“主动 / 被动”或动作召回结果。空动作因果不会出现在任何动作条件查询中；C1 不提供“按无动作查询”。

## 9. 状态、幂等与异常收口

非成功优先级固定：纯值非法 -> 入口拒绝；登记未加载 -> 入口拒绝；登记其它失败 -> 同名；既有幂等 -> 精确重复 / 幂等冲突；新键旧期望 -> 漂移；条件 / 结果状态未找到、已退出或入口拒绝 -> 同名；动作 / 来源未找到或已退出 -> 同名；许可 / 资源 -> 同名；结构矛盾 -> 内部不一致。

普通 C1 幂等身份完整 64 位原样映射 L1 因果 owner 写域，不进入身份来源或登记私有常量。同键同完整请求返回首次节点、归属、关系、来源值和代次；同键异请求冲突。发布未知只能以原身份、原请求、原期望重提，不得换键或刷新期望。

提交前非成功零普通事实变化且无变更代次。L1 已发布后首次读回分配失败 / 矛盾返回空载荷资源失败 / 内部不一致，可回显安全取得的首次代次；事实已经形成，不得宣称零变化，不补偿回滚，只以原请求收敛。

C1 不增加锁、仓、缓存、索引、SQL、恢复、日志、材料解析器或第二幂等账。来源和动作均为不透明引用。

## 10. 临时生产外验证矩阵

临时 cpp 只 import L1、T1 / T2 最终状态服务和 C1 因果服务。状态夹具经状态正式入口建立；动作 / 来源夹具经 L1 公开入口建立。不得直接 import 存在 / 特征 / 动态以重演上游证明，不访问私有 owner / 端口 / 仓 / 锁 / 定位 / 幂等账，不解析日志。

参数固定：

```text
--幂等基值 <uint64，默认91000>
--条件一顺序 <uint64，默认10>
--条件二顺序 <uint64，默认20>
--结果一顺序 <uint64，默认10>
--结果二顺序 <uint64，默认20>
```

必过矩阵：

1. 因果 owner、锚点 / 归属类型、四结构类型登记和 provider 同实例 getter 完整；owner 与已发布各族两两不同；
2. 经 T1 / T2 正式入口建立至少四个状态，经 L1 建立动作和来源普通节点；C1 不直造状态；
3. 以两个条件、有动作、两个结果、来源新增因果，节点、归属、五角色关系、来源值 / 槽同代次；
4. 以一个条件、空动作、一个结果新增成功；空动作不补默认关系，不判断被动；
5. 不同顺序引用同一状态、同一顺序引用不同状态均合法；逐项完全重复、零顺序 / 目标、空条件 / 结果、组未排序和超过上限入口拒绝且零变化；
6. 条件与结果完全相同合法；不做真实性、必然性、主动 / 被动、时间或无环判断；
7. 原请求精确重复返回首次载荷且零推进；同键改顺序、目标、动作有无 / 编码或来源均幂等冲突；两个私有保留幂等值作为公开新增身份均入口拒绝且零推进；
8. 不存在 / 已退出 / wrong-family / legacy / 裸普通状态包装由 `读取状态身份来源` 精确拒绝；动作非普通、动作 / 来源不存在或已退出精确失败；
9. 当前完整读取返回身份来源、全部关系、来源属性和状态端点同截止；历史创建截止重建相同首次投影；
10. 按条件、动作、结果分别建立多个匹配因果，返回按因果身份升序完整组；空动作因果不出现在动作组；
11. 合法无匹配端点返回已读取空组；非法 / 已退出端点不得误报空成功；
12. 经 L1 推进后旧期望当前 / 历史 / 三条件查询均漂移且空载荷；
13. 已提交后推进代次，原请求仍精确重复首次结果；原身份异请求冲突；
14. wrong-family、同 owner 元数据、裸 L1 和 malformed 因果来源当前 / 历史按 G2 精确失败；
15. `std::barrier` 同起步、固定最大轮数、无 sleep 的新增与三类读者并发，只接受完整同截止已读取、许可或漂移，不接受部分组、混代或重复因果；
16. 根工程 Debug / Release x64、临时项目双配置、strict、UTF-8、工程隔离、范围与临时生命周期检查通过。

未覆盖 C2 替换 / 退出、A1、因果真实性、必然性、主动 / 被动、动作成立 / 可执行、动态证据、概率 / 计数 / 模板 / 适用范围、生产消费者、长期公平、真实分配失败、恢复、跨进程或独立集成验收。

## 11. 实施、验证与清理

1. 只有第 2.2 节全部成立后才激活；从新 HEAD、状态最终 ABI、L1 / G2、十路径所有权和 Git / 资源重新 S0，并确认前序 DYN1 / DYN2 已按用户路线退出且释放单代码段与共享资源；不得读取其 ABI 作为 C1 S0 输入。
2. 先实现 owner / 来源和类型登记，再 DTO / 新增，再当前 / 历史读和三条件查询，最后 provider 与工程登记；不得提前 C2 / A1。
3. 新建临时验证三文件和永久两记录，逐项记录矩阵、并发窗口和未覆盖边界。
4. 运行：

```powershell
git diff --check -- <十条允许路径>
python .\tools\check_specs.py --strict
MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /verbosity:minimal
MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Release /p:Platform=x64 /verbosity:minimal
powershell -ExecutionPolicy Bypass -File .\验证工具\运行L2因果新增查询参数验证.ps1 -配置 Debug -幂等基值 91000 -条件一顺序 10 -条件二顺序 20 -结果一顺序 10 -结果二顺序 20
powershell -ExecutionPolicy Bypass -File .\验证工具\运行L2因果新增查询参数验证.ps1 -配置 Release -幂等基值 95000 -条件一顺序 11 -条件二顺序 21 -结果一顺序 12 -结果二顺序 22
```

5. 严格 UTF-8 解码十文件并解析工程 XML；确认四配置 `/utf-8`、只增加一模块一头、临时项目未进主工程。
6. `rg` 确认 owner / 来源、四登记类型、七 C1 公开函数、DTO 与本地键唯一；生产零测试增量、零动态 / 任务 / 方法 import、零旧因果 / 动态模式和历史状态 / 动态复用、零第二服务 / provider / 仓 / 缓存 / SQL / 日志 / sleep。
7. 只暂存十路径，取得发布租约后精确提交、普通推送；交互接受后登记结果并退出。
8. C1 结果退出后，独立清理提交只删除临时三文件，永久记录保留。

若 4230、G2、状态来源 / 完整读取、L1 历史组 / 当前投影 / 引用行为或工程登记漂移会改变第 4—10 节，停止并返回计划支撑；DYN1 / DYN2 只做顺序和资源释放核对，不参与合同漂移判断。不得临场增加动态证据、动作方法证明、概率 / 模板、业务准入或扩大白名单。

## 12. 拟定索引登记与完成声明边界

发布后拟登记 `L2-STRUCTURE-CRUD-REBUILD-C1`、`v0.1`、精确 plan blob、设计提交 `待发布后回填`、状态 `待激活`。具名依赖绑定：

- 4230 v0.3 blob `3d788d5547369aff22dbdad0ad44f07d84466f01`、G2 详细设计 blob `dd765cee252bfc7634fcca3ec8b9b9b05884448c`；
- T1 v0.2 plan blob `f0e248000220ec082b64cfca3ce994e07d59d0f4`、T2 v0.1 plan blob `2edd7dbe9463bedf0564e8dc5d870b0cbde57120`；二者尚缺真实结果和最终状态来源 / CRUD / provider ABI；
- DYN1 plan blob `a97f9cba457d0270a7b067ad61885041bb46ea2e`、DYN2 plan blob `79a1a593932facbdb2fa6eccca802a780403b7c0`；二者只绑定用户指定施工顺序、唯一活动代码段与共享工程 / 验证资源释放，不绑定 C1 机器合同；
- C1 的直接服务、import、调用和机器语义依赖只有状态服务与 L1；不得把 DYN1 / DYN2 的计划、代码、ABI 或结果实现为 C1 动态消费边或激活事实；
- 所有前置结果进入正式基线后仍须从新 HEAD 重新 S0，不能自动激活。

本计划发布只证明 C1 因果 owner / 来源、类型登记、DTO、原子写集、当前 / 历史读取、三条件查询、依赖和验证合同预冻结；不证明 T1 / T2 / DYN1 / DYN2 已实现、C1 可施工、代码编译、运行或验收。

C1 实施完成只证明调用方提交的非空有序条件状态、可空不透明动作、非空有序结果状态和来源在具名生产外矩阵下完成同代次新增与当前 / 条件 / 历史读取。它不证明因果真实、必然、主动 / 被动、动作成立 / 可执行、动态证据、概率、模板、业务用途、生产装配、真实消费者、长期回归或独立集成验收完成。
