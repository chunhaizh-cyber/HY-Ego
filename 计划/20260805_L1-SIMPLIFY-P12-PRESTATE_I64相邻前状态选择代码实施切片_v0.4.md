# L1-SIMPLIFY-P12-PRESTATE I64 相邻前状态选择代码实施切片 v0.4

计划身份：`L1-SIMPLIFY-P12-PRESTATE`

版本：`v0.4`

修订基线：`5b68a6389cd6f1d5d15eb08d3497e6dfb289e090`

具名漂移：`DESIGN-DRIFT-P12-EXITED-ENDPOINT-CANDIDATE-UNREACHABLE`

## 1. 目标与收窄边界

在原位 L1 状态服务中，按同主体、同实例槽和严格更早业务发生时间选择唯一最大前状态，经 P9 同一入口返回完整前状态。候选枚举**仅来自 G0 当前关系19**；P9 历史入口只读回已选候选，不枚举历史关系19。后来退出端点不得再被声明为成功候选来源。

## 2. 精确范围

```text
海中鱼巣/领域/L1状态.数据.h
海中鱼巣/领域/服务.L1状态.ixx
海中鱼巣/领域/自检.L1状态.ixx
施工记录/20260805_L1-SIMPLIFY-P12-PRESTATE_施工记录_v0.4.md
验证记录/20260805_L1-SIMPLIFY-P12-PRESTATE_验证记录_v0.4.md
```

禁止修改 L1 权威不变量、启动/工程、P7/P8/P9、P10/P11/P34、第二状态、关系20、通用历史查询、其它计划和 WIP。

## 3. 机械步骤

1. 读取 G0；主体/实例槽必须当前可解析。
2. 只遍历 G0 当前关系19，拒绝缺角色、重复角色、已退出关系、端点非当前或其它不变量损坏；不从历史关系构造候选。
3. 严格早于拟发布时间筛选，等时冲突，唯一最大时间选择。
4. 用已知编码调用 P9 当前/历史入口完整读回并互证；历史材料不得改变候选集合。
5. G0/P9/G1 代次变化重试一次；非成功清空载荷。
6. 在既有顺序 360 的 `运行L1状态自检()` 中追加当前候选、从未存在、后来退出无当前关系、已退出关系不可达、L1拒绝和 P9 读回矩阵；不新增 370。

## 4. 状态映射

当前/历史均无记录或后来退出但没有当前关系19候选：`未找到`；只有端点当前合法且候选集合为空时才是 `无前状态`。当前/历史矛盾、生命周期损坏或违反 L1 不变量：`内部不一致`。不得伪造后来退出成功路径。

## 5. 待激活与验证

版本保持 `待激活`，执行前从新 HEAD 重做 S0，确认 P9 当前/历史入口、关系19生命周期、三个状态文件和顺序 360 仍匹配。验证覆盖当前候选成功、无候选、从未存在、后来退出不可达、退出关系拒绝、历史读回互证、冲突、许可/资源/漂移和非成功空载荷；不得扩展通用历史查询或第二状态。

## 6. 完成边界

最多声明当前关系19候选范围内的相邻前状态选择矩阵完成；不声明退出关系历史枚举、P9/P10/P11、第二状态、关系20、迁移动能、动态、装配、恢复、集成验收或治理闭环完成。
