# L1-DATA-LAYER-COMPLETE-P13-INSTANCE-FEATURE-LEGACY-SCENE-API-REMOVE 实例特征场景专用旧接口物理退出代码实施切片 v0.1

日期：2026-08-08

## 1. 目标与完成条件

依据配套详细设计，物理删除 `L1实例特征服务` 中无人调用且仍依赖第一层业务标签、旧幂等探测、完整快照和 legacy 提交的三条初始场景宿主旧入口及其专属代码闭包；保持正式实际存在宿主中性入口零语义变化。

完成条件是两份生产代码、两份专属记录的精确切片完成，旧表面与实例特征文件内 legacy 第一层依赖归零，Debug / Release 完整重建、两配置模块扫描、strict 和精确范围检查全部通过。

直接依据：

- `规范/详细设计/20260808_L1-DATA-LAYER-COMPLETE-P13-INSTANCE-FEATURE-LEGACY-SCENE-API-REMOVE_实例特征场景专用旧接口物理退出详细设计_v0.1.md`
- 4015 v0.6、4030 v1.4、ACCEPTANCE-01 v0.4、4110、4170
- P7 / P8 已发布的实际存在宿主中性实现

## 2. S0 与停止条件

执行前从正式新 HEAD 核对：

1. 本计划精确版本、blob 和计划索引状态；
2. 三个旧函数和三个旧 DTO 在当前生产代码仍只有定义、没有调用；
3. 实际存在宿主登记、建立、读取和换代仍使用中性 DTO / `提交中性写集`；
4. 两个目标代码文件无异主修改，Git index 空，构建 / 模块扫描资源可用；
5. P0-FIX、路线、旧 WORLD-TREE、`.codex-build` 和其它 WIP 不在允许范围。

若发现任一旧函数已有真实生产调用者，或删除要求改变实际存在宿主公开 DTO、幂等域、事务、读回、运行期构造或工程接线，立即停止并回到设计治理。普通未使用辅助清理、格式和编译错误在白名单内修复。

## 3. 精确允许范围

只允许修改或新增：

1. `海中鱼巣/领域/L1实例特征.数据.h`
2. `海中鱼巣/领域/服务.L1实例特征.ixx`
3. `施工记录/20260808_L1-DATA-LAYER-COMPLETE-P13-INSTANCE-FEATURE-LEGACY-SCENE-API-REMOVE_施工记录.md`
4. `验证记录/20260808_L1-DATA-LAYER-COMPLETE-P13-INSTANCE-FEATURE-LEGACY-SCENE-API-REMOVE_构建与生产链验证.md`

禁止修改第一层核心 / 仓库 / 合同、其它领域服务、运行期上下文、启动、工程、filters、自检、集成验收目录、规范、详细设计、计划、计划索引和异主 WIP。

## 4. 实施步骤

1. 删除详细设计 §3.1 的三个旧 DTO 和两个旧校验，不建立别名或兼容结构。
2. 删除三个旧公开函数。
3. 按符号引用闭包删除只服务于旧函数的领域意图编码、旧探测、legacy 写集、执行证据、完整快照扫描、首次请求 / 结果缓存、旧换代账和私有 helper。
4. 删除由本次清理变成未使用的 include；保留实际存在宿主中性路径需要的 helper 和字段。
5. 静态确认目标文件内旧表面、`探测幂等`、legacy `提交写集`、`读取完整快照`、领域意图和执行证据全部归零。
6. 静态确认 `提交中性写集` 仍恰三处，正式实际存在宿主的建立 / 读取 / 换代函数仍存在。
7. 写两份专属记录，只记录实际删除、构建、扫描和未证明范围。

## 5. 验证合同

至少执行：

```text
四路径 git diff --check
python .\tools\check_specs.py --strict
Debug x64 Rebuild
Release x64 Rebuild
Debug / Release 模块依赖扫描
staged 精确范围与 commit diff check
```

静态计数必须满足：

- 旧三个 DTO、两个校验、三个公开函数在 `海中鱼巣/` 中零命中；
- `服务.L1实例特征.ixx` 中旧 `探测幂等`、legacy `提交写集`、`读取完整快照`、领域意图和执行证据零命中；
- 同文件 `提交中性写集` 恰三处；
- 全领域 legacy `提交写集` 从基线 5 文件 9 处降为 4 文件 7 处；旧 `探测幂等` 从 5 文件 10 处降为 4 文件 8 处；
- 其它生产文件差异为零。

## 6. 完成声明边界

本叶只证明实例特征场景专用旧表面物理退出和正式实际存在宿主中性入口保持可构建。它不证明保留入口本轮实际运行、其它 legacy 写入已迁移、状态 / 动态 / 比较完成、第一层全部能力已被下游使用或第一层已经完善。
