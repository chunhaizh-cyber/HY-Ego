# FS-02 状态动态二次特征因果服务增强专项 v0.1

日期：2026-07-06
状态：已生成 / 待确认 / 文档治理 / 未改代码 / 未迁移旧函数

## 1. 任务目标

根据已确认的 `实施记录/20260706_FS02_状态动态二次特征因果逻辑迁移包归并表.md`，为 FS-02-A 到 FS-02-F、FS-02-H、FS-02-I 生成后续可实施的设计和计划边界。

本专项不迁移旧函数体，不新增旧主信息字段，不进入代码实施。

## 2. 范围

本轮纳入：

```text
FS-02-A 状态身份与状态读取
FS-02-B 实例状态创建与场景收纳
FS-02-C 状态主体 / 特征 / 值材料读取
FS-02-D 状态生命周期、场景读取与非权威清理
FS-02-E 状态变化触发实例动态
FS-02-F 动作动态、方法动作动态与来源绑定
FS-02-H 二次特征组成、来源和值材料
FS-02-I 轻量因果引用和动态证据准入
```

本轮排除：

```text
FS-02-G 动态聚合和运动基元候选。
FS-02-J 因果模板、任务化和查询候选。
FS-02-X SQL / 控制面板 / 显示镜像。
D455 / 体素 / 外设。
旧函数体和旧主信息字段。
```

## 3. 依据文件

```text
AGENTS.md
计划/计划索引.md
规范/000_项目规则总纲.md
规范/001_规则迁移清单.md
规范/基础信息服务分层规范.md
规范/领域服务授权写入规范.md
规范/详细设计/状态服务详细设计.md
规范/详细设计/动态服务详细设计.md
规范/详细设计/二次特征服务详细设计.md
规范/详细设计/领域服务最小入口详细设计.md
规范/详细设计/状态动态二次特征因果服务增强详细设计.md
实施记录/20260706_FS02_状态动态二次特征因果逻辑迁移包归并表.md
```

## 4. 后续代码切片建议

本专项确认后，如果进入代码实施，必须另建独立实施切片并再次确认允许文件、禁止文件和验证方式。

建议第一轮代码切片只允许修改：

```text
海中鱼巣/领域/状态服务.h
海中鱼巣/领域/动态服务.h
海中鱼巣/领域/二次特征服务.h
海中鱼巣/领域/因果服务.h
海中鱼巣/入口.cpp
必要的实施记录文件
必要的项目记忆文件
```

建议第一轮代码切片禁止修改：

```text
旧项目目录或旧项目源码。
主信息字段迁移相关结构。
SQL / 控制面板 / WebView / 显示写入代码。
D455 / 体素 / 外设代码。
构建系统和工程编码规则。
需求、任务、方法完整状态机或方法候选系统。
```

## 5. 建议实施顺序

| 顺序 | 切片 | 内容 | 完成条件 |
| --- | --- | --- | --- |
| 1 | FS02-CODE-1 | 补状态服务材料读取入口 | 有效实例状态可读主体、场景、值、时间戳；拒绝路径结构不变 |
| 2 | FS02-CODE-2 | 补动态服务材料读取和状态变化显式动态入口 | 有效状态变化生成动态；缺材料或主体 / 场景不一致拒绝 |
| 3 | FS02-CODE-3 | 补二次特征组成读取和状态迁移组成入口 | 二次特征组成可读；不写差值、签名、缓存为权威事实 |
| 4 | FS02-CODE-4 | 补因果服务来源动态材料准入验收 | 有效来源动态可创建轻量因果引用；不写来源动态关系 |
| 5 | FS02-CODE-5 | 入口样例和项目记忆收口 | 默认入口输出分项验收，不声明完整系统完成 |

## 6. 代码实施门禁

代码实施前必须确认：

```text
1. 本专项计划已由用户确认。
2. 另建代码实施切片。
3. 明确允许文件、禁止文件和验证命令。
4. 读取拟修改代码文件头部规则注释。
5. 不迁移旧函数体。
6. 不新增旧主信息字段。
7. 不接 SQL / 控制面板 / D455 / 体素 / 外设。
```

## 7. 验收方式

文档治理验收：

```text
详细设计文件已生成。
专项计划已生成。
计划索引和项目记忆已同步。
实施记录断点清单已生成。
git diff --check 通过或只存在换行提示。
本切片提交并推送到云端。
```

后续代码验收建议：

```text
Debug x64 构建通过。
默认入口输出 FS-02 分项验收结果。
拒绝路径验证结构数量不变。
状态、动态、二次特征和因果入口均能证明结构承载、输出变化和调用路径。
```

## 8. 待确认问题

本专项随 `规范/详细设计/状态动态二次特征因果服务增强详细设计.md` 一并提交确认。

待确认问题：

```text
FS02-Q1：状态生命周期删除第一轮是否只做读取和拒绝，不新增删除写入口。
FS02-Q2：动作动态来源动作是否复用动作入口角色节点当前口径。
FS02-Q3：状态变化动态第一轮是否要求前后状态同场景。
FS02-Q4：二次特征状态迁移第一轮是否只建立组成关系，不写差值材料。
```

默认建议均按保守第一轮执行。

## 9. 禁止宣称

本专项生成或确认后不得宣称：

```text
旧函数已迁移。
旧项目状态 / 动态 / 二次特征 / 因果能力已迁移。
状态系统、动态系统、二次特征系统或因果系统完整完成。
动态聚合、因果模板、任务化查询或显示只读视图已完成。
代码已编译或入口验收已通过。
```
