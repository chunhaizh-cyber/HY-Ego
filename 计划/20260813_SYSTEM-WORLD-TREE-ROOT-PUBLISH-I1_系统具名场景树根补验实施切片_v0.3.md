# SYSTEM-WORLD-TREE-ROOT-PUBLISH-I1 系统具名场景树根补验实施切片 v0.3

日期：2026-08-13
状态：可执行（唯一运行状态以 `计划/计划索引.md` 为准）

## 1. 目标与完成条件

从正式 `428c667b7127aaa8f86a5c3d2678d5815ebc3b3d` 修复 I1 验证注入边界：把现有生产 I1 状态机机械迁入非导出单源内部编排模板，正式入口继续使用 A1 同实例 final R1，生产外专项使用脚本化访问器驱动 v0.2 原强制矩阵。不得改变公开 ABI、机器语义、启动或 I2。

完成条件：生产和专项编译同一状态机正文；强制矩阵逐项 PASS；真实主路径双配置通过；永久记录准确区分确定性异常映射与真实资源耗尽 `NOT_RUN`；生产无验证逻辑；交互侧重新独立接受后方可退出和清理临时专项。

## 2. 正式依据与 S0

- `规范/详细设计/20260813_SYSTEM-WORLD-TREE-ROOT-PUBLISH-I1_系统具名场景树根验证注入边界详细设计_v0.3.md`
- I1 v0.2 详细设计中未被 v0.3 改写的全部合同。
- I1 实现 `34b9cbcc8e10429cfccba4d035df530f06c6ea51`、清理 `6a58962af041c55d0b801348f9e70efcb14f521a`、补验撤回 `428c667b7127aaa8f86a5c3d2678d5815ebc3b3d`。

执行者须从本计划正式登记后的最新 HEAD 重新读取索引、plan/design blob、实际 R1/A1/I1 ABI、Git/index/dirty 所有权和验证资源。任何公开字段、R1 三函数签名、上下文三状态槽或 A1 同实例关系漂移均具名退回。

## 3. 允许文件

只允许：

1. `海中鱼巣/业务/初始化系统世界树根.内部编排.h`（新增并永久保留）
2. `海中鱼巣/装配.普通应用.ixx`
3. `海中鱼巣.vcxproj`
4. `海中鱼巣.vcxproj.filters`
5. `验证工具/系统世界树根建立与正式读回发布参数验证.cpp`（从 Git 恢复并扩展，接受后删除）
6. `验证工具/系统世界树根建立与正式读回发布参数验证.vcxproj`（同上）
7. `验证工具/运行系统世界树根建立与正式读回发布参数验证.ps1`（同上）
8. `施工记录/20260812_SYSTEM-WORLD-TREE-ROOT-PUBLISH-I1_施工记录.md`
9. `验证记录/20260812_SYSTEM-WORLD-TREE-ROOT-PUBLISH-I1_验证记录.md`

DTO 头只读且不得修改。禁止修改 R1/A1/L1、六服务、启动、程序结果、8120、I2、自我、概念、流程图、其它计划/设计和异主 WIP。

## 4. 机械施工

1. 从 `34b9cbcc` 恢复三件临时专项，不恢复其它文件。
2. 建立详细设计冻结的内部头；把现有根函数除“A1 取服务与委托”之外的辅助比较和状态机正文机械迁入该头。
3. 正式入口经 A1 取得 `L2场景结构服务&`，把上下文锁、待收敛槽、发布槽、请求和真实服务交给模板，原样返回。不得 public 化状态、扩大 friend 或新增导出。
4. 在 import 与 DTO 导出之后、任何使用之前非导出包含内部头；根工程/filters 将其唯一登记为 `ClInclude`。不得新增翻译单元、模块、分区或手工模块依赖。
5. 专项 import 正式模块后包含同一内部头，建立只存在于专项的脚本化访问器、调用序列与计数；不得复制模板正文或 I1 比较 helper。
6. 对 v0.3 详细设计第 6 节逐项形成断言和独立输出；真实普通应用主路径与脚本化矩阵必须分账。
7. 更新永久记录，列出每一强制项 PASS/FAIL；只有真实资源耗尽等明确排除项可 `NOT_RUN`。

## 5. 验证

至少执行：

```powershell
git diff --check -- <本计划九条路径>
python .\tools\check_specs.py --strict
msbuild .\海中鱼巣.slnx /t:Rebuild /p:Configuration=Debug /p:Platform=x64
msbuild .\海中鱼巣.slnx /t:Rebuild /p:Configuration=Release /p:Platform=x64
powershell -ExecutionPolicy Bypass -File .\验证工具\运行系统世界树根建立与正式读回发布参数验证.ps1 -配置 Debug
powershell -ExecutionPolicy Bypass -File .\验证工具\运行系统世界树根建立与正式读回发布参数验证.ps1 -配置 Release
```

另做静态合同检查：

- 分别对 `海中鱼巣/业务/系统世界树根初始化.数据.h`、`海中鱼巣/启动.应用程序.ixx` 和 `海中鱼巣/程序运行结果.数据.h` 执行 `git diff 34b9cbcc -- <精确路径>`，证明这三条路径零变化；不得把全仓差异误写为零；
- 导出面只有既有 I1 DTO与根函数，内部头零 `export`；
- 正式入口仅 A1 取服务与一次委托，状态机正文唯一；
- 生产文件零测试宏、脚本、计数器、sleep、故障选择；
- 内部头工程唯一登记，独立 I1 实现文件仍为零；
- staged 文件精确等于允许清单。

脚本化 `std::bad_alloc` 只证明同一 catch 边界映射；真实分配器耗尽、启动/阶段14—18、恢复、跨进程、I2、自我、概念、长时生产和独立集成验收明确 `NOT_RUN`。

## 6. 发布、接受与清理

实现与记录作为一份结果提交普通推送。交互侧必须依据完整逐项输出独立复跑/核验；接受后由索引所有者另行登记退出，随后以独立提交只删除三件临时专项并复跑根工程与 strict。内部编排头是单源生产实现，必须永久保留，不属于清理对象。

## 7. 完成声明边界

本计划完成只补齐 I1 provider 的可确定验证边界及原 v0.2 强制矩阵，不证明正式启动、I2、自我、概念、恢复或完整系统闭环。计划可执行也不等于代码已实现或验证通过。
