# SELFTEST-MIGRATION-B2-S0 入口剩余领域自检当前事实复核计划 v0.1

日期：2026-07-12

队列编号：#251 / 设计队列：DQ-143

状态：依赖门控待执行 / 前置 #230、#235、#250 / 纯只读 / 未改 C++

## 1. 目标与依据

依据第二批自检流程图 MD+HTML、`规范/详细设计/第二批领域自检迁移与入口剩余责任收口详细设计.md`、#236 和第一批 149 项记录，扫描所有活动计划完成后的入口剩余正文，形成 #252/#253 的真实编号、夹具、阶段和文件矩阵。本计划不改变机器流程，施工图用于限定后继范围；自身只读。

## 2. 门控

当前事实：入口仍有大量验收，但 #212-#235、恢复和初始化会改变最终状态。假定前置完成后各新自检已独立模块化。依赖未满足不扫描；实际剩余域与本设计分组不一致时记录漂移并冻结 #252/#253。

## 3. 扫描范围与产物

只读 `入口.cpp`、工程文件、全部 `.h/.cpp/.ixx`、阶段注册表和前置实施记录。产出验收编号守恒、稳定锚点、共享夹具所有者、跨阶段报告、输出、并发、建议模块名、阶段占用、允许 / 禁止文件和 #252/#253 成立性矩阵。

## 4. 允许与禁止

只允许写本计划归档、实施记录和计划索引 / 项目记忆治理。禁止修改 `海中鱼巣/**`、工程、规范、流程图；禁止构建、运行或把建议模块写成实现事实。

## 5. 验收

A01-A12 覆盖全部剩余验收调用、并发构造和输出；证明无编号遗漏 / 重复；逐域给出 #252/#253 成立、漂移或删除结论。运行严格规范和 diff，提交推送。

## 6. 边界

只证明当前事实已复核，不证明第二批迁移或入口瘦身完成。
