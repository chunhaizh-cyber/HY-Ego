# L1-SIMPLIFY-P78-DYNAMIC-CAUSAL-INTENT-GROUP
# L1 动态因果候选领域意图组 G9 扩展代码实施切片 v0.1

日期：2026-08-06

状态：待激活

## 1. 目标与完成条件

在现有唯一`L1领域意图凭证完整`中把公共领域意图合法上限从硬编码8扩展为具名G9，保持
G0—G8兼容并拒绝G10+；通过同一生产程序专项验证确定编码、provider前幂等探测、双摘要
区分和正常进程内同义/异义边界。

完成只证明核心公共凭证边界为后继动态因果候选发布者提供合法G9入口；不证明生产G9请求
摘要、P76/P77、候选发布/读取、应用确认、安全图、装配或恢复完成。

## 2. 正式依据与创建侧事实

- 基线：`main == origin/main == 5265c8601a2502984e9557835e9802d49edf5067`，index空。
- P14 v0.3设计`f7303ea8d842727891065584575535f6d126133d`、plan blob
  `13d936c815f57becc612f55339bc12065a9e3592`、索引状态可执行；正式归G7变体3、标签
  `0x15000F01U`、域`0x0F`，明确不占G9。
- P77设计`98ceeec5eb0537f4156e06adedc96430bc03635c`、plan blob
  `33bfeeb7da5ab2c2f8b9707f9b1d45d1c1f8e739`，状态待激活。
- 当前核心验证器仍为`请求意图组<=8`；探测和提交消费该验证器，摘要已编码组号U8。
- 用户动态因果、单生产程序、日志观察、暂缓崩溃恢复裁决及正式规范4015、4030—4050、
  1190、4190、4210、4220、4310、P76/P77。

详细设计：
`规范/详细设计/20260806_L1-SIMPLIFY-P78-DYNAMIC-CAUSAL-INTENT-GROUP_L1动态因果候选领域意图组G9扩展详细设计_v0.1.md`

单函数流程图：
`流程图/20260806_L1-SIMPLIFY-P78-DYNAMIC-CAUSAL-INTENT-GROUP_校验L1领域意图凭证完整函数流程图_v0.1.md`

## 3. 精确允许文件

只允许修改或新增：

```text
海中鱼巣/核心/L1事实基座.数据.h
海中鱼巣/核心/自检.L1事实基座.ixx
海中鱼巣/启动.应用程序.ixx
施工记录/20260806_L1-SIMPLIFY-P78-DYNAMIC-CAUSAL-INTENT-GROUP_施工记录.md
验证记录/20260806_L1-SIMPLIFY-P78-DYNAMIC-CAUSAL-INTENT-GROUP_验证记录.md
```

禁止修改工程、filters、P14/P15/P16/P76/P77、P0-FIX、路线、旧WORLD-TREE、`.codex-build`、
恢复材料、生产装配和计划索引；禁止新增模块、exe、目标、第二链接或测试替身。

## 4. 目标接口与实现合同

在核心数据头新增：

```cpp
inline constexpr std::uint8_t L1动态因果候选领域意图组 = 9;
inline constexpr std::uint8_t L1领域意图组上限 = L1动态因果候选领域意图组;
```

只把现有根函数组号判断改为`凭证.请求意图组<=L1领域意图组上限`；格式版本精确、非零标签、
非零摘要三项门禁保持。G0—G8行为不变，G9完整凭证合法，G10+拒绝。

不得修改`形成L1写集摘要`字节格式、G0摘要或幂等算法，不得新增验证器、摘要器、账本、锁、
缓存、provider或提交入口。后继发布者的生产请求DTO、操作标签、幂等域和结果DTO不在本叶。

## 5. 自检接口、步骤与顺序

在现有核心自检模块新增：

```cpp
L1事实基座自检报告 运行L1动态因果候选领域意图组自检();
```

实施步骤：

1. 从激活时新HEAD重做S0，核对P14代码结果、当前验证器、G9/顺序578占用与五路径所有权。
2. 新增两个编译期常量并修改唯一验证器。
3. 新增专项根，使用自检专用标签`0x1501C00EU`、固定测试摘要和隔离L1服务；不得形成生产
   G9摘要或复用P14标签/域。
4. 启动层新增薄包装`运行L1动态因果候选领域意图组专项自检()`，登记身份
   `L1-SIMPLIFY-P78-S01`、标题`L1动态因果候选领域意图组G9`、顺序578。
5. 以激活S0真实总数为基线，登记数、执行数及成功/失败固定总数机械加1。
6. 写两份专属记录并完成静态、双配置、专项、白名单和strict验证。

## 6. 验证合同

专项578至少覆盖：

- 两常量均为9，G0—G8保持，G9合法，G10拒绝；
- G9错格式、零标签、零摘要拒绝且零事实变化；
- G7/G9仅组号不同的执行证据摘要不同，G0摘要不被覆盖；
- 合法G9预探测未找到，探测摘要与凭证摘要不等入口拒绝；
- 非空隔离L1基线下G9最小写集首次成功、同义重复不增代次、异义冲突快照不变；
- G10及缺陷凭证不能通过探测或提交；
- P14/P76/P77、动作、方法、恢复和跨进程入口调用0；
- 单一生产程序Debug/Release构建运行，日志人读核对专项身份、顺序、计数和拒绝；
- 零第二exe/目标/链接/装配、测试DTO、替身入口和调试状态出口；
- 原L1核心与G1—G8专项不退化。

执行验证至少包括：

```powershell
cmake --build .codex-build --config Debug --target 海中鱼巣 -- /m:1
cmake --build .codex-build --config Release --target 海中鱼巣 -- /m:1
& .\.codex-build\Debug\海中鱼巣.exe --self-test
& .\.codex-build\Release\海中鱼巣.exe --self-test
python .\tools\check_specs.py --strict
```

日志只作同一生产程序人读观察，不是机器事实、业务状态、持久材料或恢复证据。

## 7. 激活门禁与失败收口

计划保持待激活。P14 v0.3当前唯一可执行并拥有八路径，其中启动文件与P78重叠，同时占用
工程、filters、构建与完整自检资源。必须等待P14代码结果提交推送、经交互接受并释放八路径/
资源后，从新HEAD确认：G7变体3/标签/域未漂移；核心仍缺G9；P78五路径无异主WIP；顺序578
唯一；真实自检总数可机械加1。

以下情况具名退回计划支撑：P14代码改变G7身份或共享核心；已有等价G9边界；P15改变凭证
格式、组号编码或验证器；G9必须同步修改恢复材料或新增公开机器语义。普通包含、夹具、计数
和本范围内机械错误由执行侧修复。

## 8. 最大边界

完成后最多声明：核心公共凭证可合法区分G9动态因果候选与既有G0—G8，G10+拒绝，同一
正常生产进程中的确定编码、预探测和同义/异义矩阵通过。

禁止声明：动态因果候选已形成/发布/可读，生产G9摘要/标签/域已实现，已确认可应用、准备因
必得果、安全直接因果图、STEP-7、P16业务接线、生产装配、治理闭环、崩溃/重启/跨进程恢复
完成。
