# TASK-CURRENT-SOURCE-DEMAND-TARGET-MATERIAL-GROUP-CONSUME 候选任务全部来源需求目标材料组消费代码实施切片 v0.1

日期：2026-08-16
版本：v0.1

## 1. 目标与完成条件

实现 `BIZ-L4-004-02-02-06 v0.1`：新增 provider-only `任务来源目标事实提供者`，消费 02 完整成功结果，按全部来源需求稳定去重，并经同一个 `需求业务服务`在同一非零 `G0`逐唯一需求恰一次读取，形成完整来源需求目标引用材料组。

完成要求：每个唯一来源需求恰一次读取；共享来源跨任务只读一次；所有成功材料与 02 来源集合、身份、版本和 `G0`完整互证；任一失败零前缀；无目标正文读取、目标等价裁决或根选择。

## 2. 正式依据与当前事实

- 详细设计：`规范/详细设计/20260816_TASK-CURRENT-SOURCE-DEMAND-TARGET-MATERIAL-GROUP-CONSUME_候选任务全部来源需求目标材料组消费详细设计_v0.1.md`；
- 目标图：`流程图/目标业务流程图/L4/BIZ-L4-004-02-02-06_读取候选任务全部来源需求目标材料组_目标函数流程图_v0.1.md`；
- 依据规范：0050、3100、3200、5100、5200；
- 上游合同：`TASK-COMPLETE-ACCEPTANCE-LIFECYCLE-GROUP-CONSUME v0.1`、`DEMAND-CURRENT-PARENT-LIFECYCLE-CONSUME v0.1`；
- 当前无 `任务来源目标事实.数据.h`、`提供者.任务来源目标事实.ixx`或本函数；任务 / 需求 BIZ 服务和 DATA-EXT-12 / 13 最终生产 ABI 均未实现，因此状态只能 `待激活`。

## 3. 允许范围

生产文件：

1. 新增 `海中鱼巣/领域/任务来源目标事实.数据.h`；
2. 新增 `海中鱼巣/领域/提供者.任务来源目标事实.ixx`；
3. 修改 `海中鱼巣.vcxproj`；
4. 修改 `海中鱼巣.vcxproj.filters`。

允许新增本计划专属施工记录、验证记录和生产外临时专项三件套。

禁止修改 DATA-L1—L3、`服务.任务.ixx`、`服务.需求.ixx`、两个上游合同头、普通应用上下文、线程、目标业务图、规范、其它计划和计划索引。禁止新增 DATA 批量读取、第二需求业务服务、任务目标宿主 / 特征副本、缓存、L1 / owner / 仓库或写入口。

## 4. 实施合同

1. 按详细设计冻结中文 DTO、状态、固定配置、`任务来源目标事实提供者`构造和 `读取候选任务全部来源需求目标材料组(...) const noexcept`；
2. provider 只持一份 `const 需求业务服务&`和不可变配置；不得持 DATA 服务或改变上游服务所有权；
3. 完整验证 02 成功形状，从全部任务来源关系提取并稳定去重来源需求，checked 校验 `D <= 最大来源需求数量`；
4. 对 D 个唯一需求逐项从请求取得同一 `G0`和运行代次、从provider固定配置取得事实 / 需求范围与需求树 / 父关系 / 读取规则版本，构造下层请求；每项恰一次调用需求业务服务；
5. 逐项互证成功谓词、请求回显、需求身份 / 来源、生命周期、目标宿主 / 特征 / 合同、结构版本和 `G0`；
6. 只投影目标引用材料，按需求稳定身份一次形成完整候选；成功前不交换，任一失败清空局部候选；
7. 精确映射预算、漂移、提供者、资源和内部不一致；D超预算返回数量预算不足，同G0未找到 / 已退出、运行态未实现 / 未装配和未知状态升级内部不一致；
8. 全部异常收口，`noexcept`零终止；零重试、零截止重选、零 sleep、零跨调用缓存。

## 5. 验证

- 坏配置 / 请求 / 02结果、零G0 / 预算、空或坏来源、版本漂移、D超预算全部零调用；D超预算精确返回数量预算不足；
- 单 / 多任务、单 / 多来源、多任务共享同一来源和输入乱序成功，需求调用次数恰为D；
- 未找到 / 退出、漂移、许可、资源、运行态未实现 / 未装配和未知状态映射准确；未找到 / 退出、运行态未实现 / 未装配和未知状态升级内部不一致，任一失败零前缀；
- 错身份 / 来源 / G0 / 生命周期 / 目标三引用 / 版本和半结构拒绝；输出集合、排序与02唯一来源集合守恒；
- 静态证明零DATA直达、零目标正文 / 比较 / 等价、零根 / 拓扑 / 治理 / 当前值、零写 / 缓存 / retry / sleep；
- 根工程 x64 Debug / Release Rebuild，专项双配置，`python .\tools\check_specs.py --strict`，diff/XML/UTF-8/工程唯一登记和生产零专项痕迹。

真实资源耗尽、私有 DATA 损坏、跨进程恢复、目标等价、当前根、完整任务树、任务创建 / 迁移 / 调度 / 完成和系统闭环保持 `NOT_RUN`。

## 6. 完成声明边界

未来结果最多证明 `任务来源目标事实提供者::读取候选任务全部来源需求目标材料组`通过本计划矩阵。它不证明 DATA-EXT-12 / 13、上游任务 / 需求 BIZ 叶、目标等价、当前任务树或系统业务闭环完成。
