# RECOVERY-S1 严格解析与隔离四仓库候选代码实施切片 v0.1

日期：2026-07-11

队列编号：#227

设计队列：DQ-119

状态：已登记依赖门控 / JY-277 经 #226 传递主装配与恢复链设计门控 / 直接前置 #226 / 只构造隔离四仓库 / 不发布运行期 / 未改 C++ / 未构建 / 未运行程序

## 1. 目标

严格解析 HYSNAP01 外部文件，并按快照指定的仓库编号、记录身份、版本、状态和高水位构造隔离四仓库候选：

```text
只读文件
-> 总头 / 段表 / 边界 / 校验
-> 独立段值式解析
-> 主信息 -> 节点 -> 关系指定身份导入
-> 索引候选暂存
-> 隔离精确读回
```

本切片不恢复特征值侧表、系统角色、索引绑定、领域登记、概念活动图，不替换当前运行期，也不运行初始化或线程。

## 2. 依据与范围一致性

```text
流程图/20260711_权威状态快照隔离恢复与运行期上下文一次发布流程图_v0.1.md
流程图/20260711_权威状态快照隔离恢复与运行期上下文一次发布流程图_v0.1.html
规范/详细设计/权威状态快照隔离恢复与运行期上下文一次发布详细设计.md
计划/20260711_PERSIST-S1_统一冻结权威快照与原子文件发布代码实施切片_v0.1.md
```

流程图 Q1-Q5、详细设计第 7 节和本计划均止于隔离四仓库与索引值式候选，不提前进入领域恢复或发布。

## 3. 依赖门控

### 3.1 前置计划

```text
直接前置：#226 必须实际完成并形成 HYSNAP01 当前写入方和严格解析器。
传递前置：#217、#220、#203-#225 已由 #226 实施记录证明。
```

### 3.2 已验证当前事实

```text
当前四仓库普通创建入口自行分配身份，不能恢复指定编号 / 版本。
普通读取隐藏墓碑，关系审计又可能要求当前端点有效。
当前仓库构造器只接仓库编号，不接全记录、高水位或创建序号。
索引当前没有所有者复核，不能在本切片直接绑定。
```

### 3.3 假定接口契约

```text
#226 提供严格 HYSNAP01 解析值式结果和九段 DTO。
四仓库提供不公开给普通业务调用方的恢复专用候选构造入口。
恢复候选对象独立拥有主信息、节点、关系和索引候选，不引用当前运行期仓库。
仓库审计读取可在隔离候选中读取有效 / 失效 / 删除记录，不改变普通读取规则。
```

### 3.4 预期正式产物

```text
海中鱼巣/核心/隔离仓库恢复候选.h（需要时可有 .cpp）。
HYSNAP01 严格解析结果与结构化拒绝原因。
四仓库指定身份 / 状态 / 高水位恢复专用构造。
有效端点和历史墓碑端点两套关系复核。
实施记录/20260711_RECOVERY-S1_严格解析与隔离四仓库候选代码实施_Codex断点清单.md
```

## 4. 实施步骤

### S0：#226 实际格式复核

记录总头、九段、版本、校验、规范字节和文件发布实际接口。若 #226 格式或解析结果与详细设计不同，停止并退回修订 #227；不得自行兼容未知字段。

### S1：严格外部解析

先固定文件总边界，再验证段表唯一性、偏移 / 长度 / 计数、校验和摘要。所有长度乘法、内存预算和整数转换先拒绝溢出。边界确认后才允许并行解析段 2-5；输出不可变值式 DTO。

### S2：恢复专用仓库构造

主信息、节点、关系仓库新增候选构造器，直接接收已验证全记录和精确高水位。不得调用普通创建 / 删除 / 重挂入口逐条拼装，也不得从最大编号推导高水位。

### S3：端点和拓扑复核

有效关系要求精确当前有效端点与合法类型组合；历史关系按身份定位墓碑端点，端点版本不得大于当前墓碑版本。普通父子继续复核单父、自身和成环。历史关系不得进入普通父子 / 当前投影。

### S4：隔离读回

按审计入口逐记录比较指定身份、版本、状态、槽位、创建序号和高水位。索引段只暂存值式候选。外部坏材料逻辑拒绝；已严格验证的导入包无法精确读回追根因。

### S5：自检与治理

补齐 RECOVERY-S1-A01 至 A16、全拒绝矩阵、单 / 多线程解析一致性；完成规范、实施记录、计划归档、治理、提交和推送。

## 5. 允许文件

```text
海中鱼巣/核心/权威快照格式.h
海中鱼巣/核心/仓库快照服务.h
海中鱼巣/核心/仓库快照服务.cpp
海中鱼巣/核心/隔离仓库恢复候选.h
海中鱼巣/核心/隔离仓库恢复候选.cpp
海中鱼巣/核心/节点仓库.h
海中鱼巣/核心/节点仓库.cpp
海中鱼巣/核心/主信息仓库.h
海中鱼巣/核心/主信息仓库.cpp
海中鱼巣/核心/关系仓库.h
海中鱼巣/核心/关系仓库.cpp
海中鱼巣/核心/索引仓库.h
海中鱼巣/核心/索引仓库.cpp
海中鱼巣/入口.cpp
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
规范/仓库逻辑空间与领域树结构规范.md
规范/错误结果与入口拒绝规范.md
规范/领域服务授权写入规范.md
计划/20260711_RECOVERY-S1_严格解析与隔离四仓库候选代码实施切片_v0.1.md
计划/已完成计划/20260711_RECOVERY-S1_严格解析与隔离四仓库候选代码实施切片_v0.1.md
实施记录/20260711_RECOVERY-S1_严格解析与隔离四仓库候选代码实施_Codex断点清单.md
计划/计划索引.md
项目记忆/Codex任务队列.md
项目记忆/当前状态.md
项目记忆/实施记录.md
项目记忆/验收记录.md
项目记忆/决策记录.md
项目记忆/待确认问题.md
```

## 6. 禁止文件与排除项

```text
海中鱼巣/领域/**
海中鱼巣/线程/**
海中鱼巣/界面/**
海中鱼巣/适配/**
海中鱼巣/核心/事件日志段服务.h
海中鱼巣/核心/事件日志段服务.cpp
日志/**
快照/权威状态.hysnap
旧鱼巢源码和旧快照格式
```

禁止导入特征侧表、绑定索引、运行初始化、发布当前上下文、启动线程、事件重放、SQL / UI 修复或兼容未知格式。

## 7. 入口拒绝

```text
文件路径、魔数、总头 / 段表 / 快照版本或字节序不支持。
文件长度、段数量、段顺序、唯一性、偏移、重叠、边界、计数或校验错误。
记录身份为零 / 重复，版本 / 状态 / 枚举未知或数值溢出。
高水位为零、不高于已用身份或创建序号高水位错误。
节点主信息身份缺失 / 版本不允许。
有效关系端点非当前有效精确句柄或类型组合非法。
历史关系端点身份缺失、端点版本为零 / 大于墓碑版本。
普通父子多父、自身或成环。
```

全部外部材料拒绝发生在候选仓库构造前，或只改变隔离候选；当前运行期不变化。

## 8. 失败收口

| 场景 | 分类 | 收口 |
| --- | --- | --- |
| #226 接口漂移 | 依赖门控返回 | 不改代码，退回设计 |
| 外部文件格式 / 校验 / 身份 / 拓扑无效 | 逻辑内返回 | 丢弃解析材料，不构造当前结构 |
| 隔离候选构造预算不足 | 逻辑内返回 | 丢弃候选，当前运行期不变 |
| 已验证导入包无法按指定身份构造 / 读回 | 追根因解决 | 丢弃隔离候选，停止依赖路径 |
| 历史关系被普通读取或父子投影错误包含 | 追根因解决 | 不进入领域恢复，不扫描修补 |

## 9. 验收

| 编号 | 验收项 |
| --- | --- |
| RECOVERY-S1-A01 | 魔数、总头 / 段表 / 快照版本和显式小端逐项严格校验 |
| RECOVERY-S1-A02 | 总长度、段唯一性、偏移、重叠、边界、计数、段校验和跨段摘要完整校验 |
| RECOVERY-S1-A03 | 外部坏快照逻辑拒绝且不构造 / 修改当前运行期，也不回退全新初始化 |
| RECOVERY-S1-A04 | 主信息隔离候选按指定仓库编号、身份、版本、状态、槽位和高水位精确构造 |
| RECOVERY-S1-A05 | 节点隔离候选按指定身份、类型、主信息、版本、状态、创建序号和双高水位精确构造 |
| RECOVERY-S1-A06 | 关系隔离候选按指定身份、类型、端点、顺序、版本、状态和高水位精确构造 |
| RECOVERY-S1-A07 | 有效关系要求精确有效端点，历史关系允许同身份墓碑旧版本但拒绝缺失 / 未来端点 |
| RECOVERY-S1-A08 | 高水位严格高于已用值并按快照原值恢复，不用最大值加一重算 |
| RECOVERY-S1-A09 | 零身份、重复身份、版本 / 长度溢出和未知枚举均在导入前拒绝 |
| RECOVERY-S1-A10 | 普通父子单父、拒绝自身和无环规则在隔离候选中成立 |
| RECOVERY-S1-A11 | 索引段只进入值式候选，不在 RECOVERY-S1 绑定隔离索引 |
| RECOVERY-S1-A12 | 总边界确认后独立段可并行解析，完成顺序不改变候选顺序 |
| RECOVERY-S1-A13 | 已严格验证的导入包无法精确读回时进入追根因 |
| RECOVERY-S1-A14 | 恢复专用构造不调用普通业务创建入口猜身份或修复材料 |
| RECOVERY-S1-A15 | 本切片不恢复特征侧表、系统角色、领域登记、线程、UI 或 SQL |
| RECOVERY-S1-A16 | 全部拒绝矩阵、精确读回、单 / 多线程解析和既有仓库回归通过 |

## 10. 验证

```powershell
msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m
py -3.14 .\tools\check_specs.py
git diff --check
```

必须运行完整 `--self-test-exit`、A01-A16、格式 / 边界 / 身份 / 高水位 / 墓碑端点 / 普通父子拒绝矩阵，单 / 多线程解析一致性和最终连续 20 轮完整自检。运行产物必须清理且不入 Git。

## 11. 实际接口复核与漂移退回

执行前记录 #226 格式、解析 DTO、严格读回和当前四仓库全状态导出实际签名。若外部解析结果不能无损表达全部墓碑 / 高水位，恢复专用构造需要调用业务服务，或历史端点规则与 #220 实际审计冲突，停止修改并退回设计，不得放宽为“只恢复有效记录”。

## 12. 完成边界

完成后只可声明 HYSNAP01 严格解析和隔离四仓库候选第一版已实现。不得声明特征侧表 / 领域登记恢复、运行期发布、启动恢复、事件重放、热替换或断电恢复完成。
