# NODE-TYPED-MIGRATION NT-P3-SHARED 隔离装配候选代码实施切片 v0.4

身份：`#348 / DQ-240 / NT-P3-SHARED v0.4`

日期：2026-07-23

设计状态：终审 PASS

计划列表引用：#348 / 计划 v0.4；计划状态与具名依赖唯一读取 `计划/计划索引.md`。本文不维护执行过程。

共同合同基线：`NODE-TYPED-MIGRATION/v0.4`
合同来源：`计划/20260722_NODE-TYPED-MIGRATION_NT-P3_服务兼容删除审计收口子计划_v0.4.md` 第 2 节
消费合同：`NT-C3/v0.4::C3-A1—C3-E3`
提供合同：`NT-C3/v0.4::C3-A0/C3-F1—C3-F2`
合同性质：预冻结待实现；本文只登记 #348 的提供 / 消费边界，模块、文件、DTO、签名、ABI、所有权、生命周期、结果分类和漂移归属唯一读取合同来源。

共同依据：`计划/20260722_NODE-TYPED-MIGRATION_节点直接身份与领域类型化持久结构代码修订总计划_v0.4.md`、`计划/20260722_NODE-TYPED-MIGRATION_NT-P3_服务兼容删除审计收口子计划_v0.4.md`、`规范/详细设计/NODE-TYPED-MIGRATION_NT-P3_服务兼容删除审计迁移详细设计.md`、`流程图/20260722_NODE-TYPED-MIGRATION_NT-P3_服务兼容删除审计迁移施工流程图_v0.3.md`、`规范/详细设计/函数结构知识图谱/20260722_NODE-TYPED-MIGRATION_NT-P3_函数结构知识图谱.md`。

## 1. 目标与候选边界

本任务不承担业务语义，只按 #343—#347 预冻结模块和接口合同形成隔离节点直接运行期装配候选与隔离全图自检候选。旧默认装配和入口保持不变。

本任务不等待上游实现完成，不承担最终工程登记、统一自检运行器接线、构建、运行或完整验收。文件名沿用 `P3-SHARED` 路由兼容，职责已经从“共享工程接线”收紧为“隔离装配源码候选”。

旧 `WT-348-R1 / codex/348-nt-p3-shared-isolated-r1` 及 `443347bf...` 只作 v0.2 候选证据。v0.4 在现行长期执行通道中建立 #348 独立计划段，不复用旧路径、分支、暂存区或未跟踪现场，也不 reset、rebase、覆盖、续写或直接 cherry-pick 旧候选。

## 2. 预冻结待实现接口合同

唯一合同为 P3 v0.4 子计划第 2 节 `NT-C3/v0.4`。本任务独占 `C3-A0` 公共值式头并实现 `C3-F1—C3-F2`；消费 `C3-A1—C3-E3`，不复制各对象 DTO、签名或业务语义。

公共头只能定义 P3 公共版本、状态、请求头、结果头和错误材料；`节点直接身份记录`、`正式关系记录`、`可重建索引记录` 继续唯一来自 #339 真实模块，不得在公共头镜像、前置声明或包装成平行 ABI。关系业务角色只由 `正式关系记录.顺序号: std::int64_t` 表达；装配不得改写参与者 `完成发布() noexcept -> void` 的真实语义。

本任务向 #352 提供：

```text
海中鱼巣/装配.节点直接运行期业务.ixx
海中鱼巣/自检.节点直接运行期业务.ixx
```

前者只组合隔离新域生产候选；后者只组织 P3 隔离全图验收入口候选。两者不得进入当前默认装配或工程，不得成为“已经接通”的证据。

唯一装配方式按合同第 2.6 节固定：装配请求只持有 `C3-A1—C3-E3` 唯一 export 实体的 `std::reference_wrapper<const T>` 各一项；禁止 optional、裸指针及仓库 / 会话 / 令牌 / 锁。`节点直接运行期业务装配` 删除 public 默认构造和复制构造，只能由 `形成节点直接运行期业务装配` 创建，可移动不可复制。#352 最终持有全部服务实体并保证其生命周期严格长于装配。

## 3. 精确允许文件与所有权

```text
海中鱼巣/领域/节点直接P3合同.数据.h
海中鱼巣/装配.节点直接运行期业务.ixx
海中鱼巣/自检.节点直接运行期业务.ixx
```

#348 唯一拥有公共合同头并为两个目标模块提供公共定义；其余叶子只读包含。#339—#347 业务文件、`海中鱼巣.vcxproj`、`海中鱼巣.vcxproj.filters`、`海中鱼巣/自检.运行器.ixx`、`装配.运行期业务.ixx`、入口、启动、运行期宿主、旧服务 / 调用方、恢复和全部 Markdown 全部禁止修改。最终三个共享接线文件唯一交 #352。

## 4. 机械阶段、返回与漂移

S0 核对合同 blob、公共头和两个目标模块不存在第二所有者且文件所有权无交集；S1 只复核 `C3-A0`、`C3-F1—C3-F2`、import 图和唯一装配生命周期；S2 形成公共头与生产装配候选；S3 形成隔离全图自检候选；S4 完成静态、白名单和 Git 卫生收口。

装配入口的无效请求、业务前置拒绝和协议允许空组保持提供服务的逻辑内返回；提供服务已报告内部错误、合同不一致、新旧域互认或完整读回失败必须保持内部逻辑错误，不得由装配吞并、重试或降级。

缺少预冻结模块符号本身不是本阶段 DRIFT；出现同名异义模块、循环 import、第二实体所有者、必须修改提供者文件或必须接入默认装配才能表达结构时正式退回。

## 5. 检查、集成与完成边界

任务级只检查 import 图符合预冻结合同、生产不 import 自检、默认装配零可达、旧域零 import、三个允许代码文件之外零差异、白名单、UTF-8、`git diff --check` 和 clean；不构建、不运行，不写 Markdown。完成、漂移或失败只发结构化消息。

P3 固定集成顺序为 `#343 -> #344 -> #345 -> #346 -> #347 -> #348`。#352 汇集后唯一修改工程 XML 和统一运行器，并执行 Debug / Release、完整自检、真实联调和最终切换验收。本任务只证明隔离装配源码候选形成，不证明模块已登记、可编译、候选图已接通或默认生产切换。
