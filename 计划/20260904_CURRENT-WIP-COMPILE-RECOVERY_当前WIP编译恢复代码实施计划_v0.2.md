# 当前 WIP 编译恢复代码实施计划

身份：`CURRENT-WIP-COMPILE-RECOVERY`
版本：v0.2
设计：[当前 WIP 编译恢复详细设计 v0.2](../规范/详细设计/20260904_CURRENT-WIP-COMPILE-RECOVERY_当前WIP编译恢复详细设计_v0.2.md)
替代：v0.1；v0.1 不再构成执行许可。
计划基线：以本计划发布后的 `main` 为准，执行前重新 S0。
状态：可执行
依赖：无；文件交接、构建资源和发布租约属于 S0 门禁。

## 1. 目标与完成条件

保留已经删除的旧筹办服务和九个 E2E canonical 源文件，不恢复已停止的业务/退役工作；
在 v0.1 已实施的编译恢复范围内，修复本次由 SELF-FORM module 可见性造成的级联编译
错误。完成条件为 v0.1 §5 与 v0.2 详细设计 §4 的核对项均满足。

禁止：恢复 `服务.需求初次筹办准备.ixx` 或 `任务初次筹办.数据.h`；恢复/编译根目录
未跟踪 E2E 或普通应用副本；修改正式规范、规则、DTO、任务/自我形成机器语义；继续 C031
或退出的退役计划；处理本计划外的编译告警或错误。

## 2. S0、允许与禁止范围

执行前读取 AGENTS、规范目录、8120、5260、v0.1 与 v0.2 设计/计划、计划索引、当前
接口和调用点；核对 `main`、HEAD、`origin/main`、index、dirty、构建资源、发布租约和
`D:\TEMP\再用文件\再用文件列表.md`。为下表尚未登记的文件逐一登记；已有 v0.1 文件
仅由原执行者继续保护，不能借此覆盖异主 WIP。

| 文件 | 允许操作 |
| --- | --- |
| `海中鱼巣/业务/提供者.真实自我形成.ixx` | 仅恢复详细设计 §3 给出的普通应用 import。 |
| v0.1 已列出的八个生产/工程文件 | 仅保留并完成 v0.1 明定的恢复、旧 consumer 删除和函数尾段补全；不得扩大。 |
| v0.1 的施工、验证记录 | 按原记录合同补充本次首错、import 恢复与两配置构建证据。 |

明确禁止写：`海中鱼巣/业务/真实自我形成.数据.h`、`海中鱼巣/装配.普通应用.ixx`、
`海中鱼巣/启动.应用程序.ixx`、所有旧服务/旧 DTO/E2E canonical 文件，以及 v0.1 未列
文件。若 v0.1 允许集与此表不一致，以 v0.2 的禁止项优先。

## 3. 机械施工步骤

1. 保存 provider 当前 source blob 与 diff，确认其模块名仍为
   `海中鱼巣.业务.提供者.真实自我形成`，普通应用模块不反向 import SELF-FORM。
2. 只将 `import 海中鱼巣.装配.普通应用;` 恢复到三个 L2 import 之后、导出 DTO 头之前。
3. 重新运行 v0.2 §4 静态核对以及 v0.1 的旧服务/E2E 工程登记核对；随后分别在计划规定
   的隔离目录执行 Debug|x64、Release|x64 重建。
4. 若构建后产生新的首错，只在它完全属于 v0.1/v0.2 允许文件且不要求 DTO、服务合同或
   机器语义改变时修复；否则保存证据，停止该文件并具名退回计划支撑。

## 4. 验证、失败收口与完成声明

执行 v0.2 详细设计 §4 和 v0.1 §5 的完整矩阵，且仅精确暂存本计划实际拥有文件。运行
`git diff --check`、`python .\tools\check_specs.py --strict`；严格检查若仍复现于本计划
候选前的异主 WIP，必须分账，不得掩盖候选问题。

不得 reset、stash、checkout、clean、复制根目录副本、覆盖异主 WIP 或用新的 DTO/替代服务
回避首错。提交和推送只证明当前编译恢复切片发布，不证明旧筹办退役全链、SELF-FORM
运行或任何端到端业务验收。
