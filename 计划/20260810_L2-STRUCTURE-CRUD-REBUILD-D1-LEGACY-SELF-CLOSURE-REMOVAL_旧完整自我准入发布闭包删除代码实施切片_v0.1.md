# 旧完整自我准入发布闭包删除代码实施切片 v0.1

日期：2026-08-10

计划身份：`L2-STRUCTURE-CRUD-REBUILD-D1-LEGACY-SELF-CLOSURE-REMOVAL`

状态：创建侧验证后登记

## 1. 目标

作为 L2 结构 CRUD 重建的第一份代码叶，物理删除依赖旧世界 / 存在 / 场景合同的完整自我存在准入、场景准入、发布数据操作、组合服务及其唯一 DTO，解除后续旧 L2 自顶向下删除的最高层反向依赖。

本叶只删除旧代码，不实现新 L2，不迁移旧函数，不建立兼容壳。

## 2. 依据与当前事实

依据 0050、4015、4030、4190、4200、同名路线详细设计及用户“旧代码先行删除”的明确要求。

创建侧事实基线为 `f4dba8d14c5a93aec86da320cac4249bcdc1a005`：

- 正式 `普通应用上下文` 只持有新版 `L1事实基座服务`；目标五文件零启动 / 装配消费者；
- `L1完整自我.数据.h` 只被目标四模块消费；
- 四个目标模块的生产 import / 类型 / 函数引用闭包完全位于本叶五文件内；
- 目标工程登记各一项，filters 登记各一项；
- 当前计划索引无可执行计划，Git index 为空；无关路线输入与 `.codex-build` WIP 不属于本叶。

执行 S0 若发现任一集合外生产消费者、目标文件漂移、索引出现更早可执行计划或工程登记形状变化，停止并返回计划支撑，不扩大白名单。

## 3. 精确允许范围

删除：

- `海中鱼巣/领域/L1完整自我.数据.h`
- `海中鱼巣/领域/服务.L1完整自我存在准入.ixx`
- `海中鱼巣/领域/服务.L1完整自我场景准入.ixx`
- `海中鱼巣/领域/数据操作.L1完整自我发布.ixx`
- `海中鱼巣/领域/组合.L1完整自我.ixx`

修改：

- `海中鱼巣.vcxproj`
- `海中鱼巣.vcxproj.filters`

新增专属记录：

- `施工记录/20260810_L2-STRUCTURE-CRUD-REBUILD-D1-LEGACY-SELF-CLOSURE-REMOVAL_旧完整自我准入发布闭包删除施工记录.md`
- `验证记录/20260810_L2-STRUCTURE-CRUD-REBUILD-D1-LEGACY-SELF-CLOSURE-REMOVAL_旧完整自我准入发布闭包删除验证记录.md`

禁止修改或删除新版 L1、世界 / 场景 / 存在 / 特征 / 状态 / 动态 / 因果基础模块、启动与装配、计划索引、规范、设计、P0-FIX、路线、旧 WORLD-TREE、`.codex-build` 或其它异主 WIP。

## 4. 实施步骤

1. 从正式 HEAD 重新扫描五文件的 module / include / 类型 / 函数反向引用，确认集合外生产命中为零。
2. 删除五个目标文件。
3. 从工程和 filters 精确删除五项对应登记，不调整其它项目顺序或配置。
4. 全仓扫描下列身份在现行生产代码和工程登记中归零：
   - `海中鱼巣.领域.服务.L1完整自我存在准入`
   - `海中鱼巣.领域.服务.L1完整自我场景准入`
   - `海中鱼巣.领域.数据操作.L1完整自我发布`
   - `海中鱼巣.领域.组合.L1完整自我`
   - `L1完整自我.数据.h`
   - `L1完整自我存在准入服务`
   - `L1完整自我场景准入服务`
   - `L1完整自我发布数据操作`
   - `L1完整自我组合服务`
5. 记录实际删除、归零结果、构建结果和未覆盖范围。

不得把编译错误通过修改集合外源码、恢复旧接口、加入兼容声明或重命名模块解决；发现真实集合外依赖即停止。

## 5. 验证

本删除叶只做机械与构建验证，不运行 CRUD、生产程序、自检或深度验收：

1. 五目标路径不存在；
2. 上述模块、类型、函数与工程 / filters 登记在现行生产范围归零；
3. `海中鱼巣.vcxproj` 和 filters 仍为有效 UTF-8 XML，四配置 `/utf-8` 不变；
4. Debug x64 Rebuild 成功；
5. Release x64 Rebuild 成功；
6. `git diff --check` 通过；
7. `python .\tools\check_specs.py --strict` 通过；
8. 提交前 cached 范围只包含七个代码 / 工程文件和两份专属记录。

## 6. 完成声明边界

本叶完成只证明旧完整自我准入 / 发布闭包及工程登记物理退出，并且剩余工程继续编译链接。

本叶不证明：

- 其它旧 L2 模块已经删除；
- 新 L2 场景、存在、特征、状态、动态或因果 CRUD 已实现；
- 新版 L1 或任何 L2 服务功能正确；
- 生产运行期接线、业务真实性、并发、性能、崩溃或跨进程恢复成立。
