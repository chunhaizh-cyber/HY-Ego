# SYSTEM-WORLD-TREE-ROOT-PUBLISH-I1 系统具名场景树根补验实施切片 v0.5

日期：2026-08-13
状态：可执行（唯一运行状态以 `计划/计划索引.md` 为准）

## 1. 目标与替代关系

从正式 `877cb54727d2c675390d0a89031335abd687af3a` 修正 v0.4 组 5A 一项不可构造验证措辞。v0.5 同身份替代 v0.4 的执行合同；v0.4 的 `.inl`、公开 ABI、生产状态机、九路径、四层证据和除该措辞外全部矩阵继续有效。

精确替换为：登记、根建立、后二次登记、整树读取的非成功枚举即使携带完整成功载荷，对应 L2 `成功()` 仍为 false，I1 必须立即按阶段拒绝且保持正确 optional、调用短路和两状态槽守恒。不得要求构造 `非成功枚举 && 成功()==true`。

## 2. 正式依据与重新 S0

- `规范/详细设计/20260813_SYSTEM-WORLD-TREE-ROOT-PUBLISH-I1_系统具名场景树根补验可构造反例详细设计_v0.5.md`
- v0.4 详细设计 / 计划中未被 v0.5 替换的全部合同。
- 执行局部事实：组 5A 8/8、累计新增 28/28 已 PASS；生产代码未因该 DRIFT 修改，组 5B 尚未写，记录尚未更新。

执行者须从 v0.5 正式索引与最新 HEAD 重新核对 plan/design blob、当前九路径 WIP、真实 DTO `成功()` 实现和剩余矩阵。允许保留并复核既有局部 PASS，不得把消息数字直接当完成证据。

## 3. 白名单与实施

白名单仍精确沿用 v0.4 九条，不新增或删除路径，不授权修改 DTO、R1/A1、启动、程序结果、规范、I2 或其它设计 / 计划。

执行只需：

1. 保留现有生产 WIP，不为该 DRIFT 修改生产代码。
2. 在专项组 5A 中固定四阶段“非成功枚举 + 完整成功载荷”可构造反例，先断言 L2 `成功()==false`，再断言 I1 阶段拒绝、短路、optional 格和状态守恒。
3. 删除任何要求或描述 `非成功枚举 && 成功()==true` 的动态断言。
4. 继续完成 v0.4 其它未完成矩阵、双配置构建、专项、静态合同与永久记录。

## 4. 验证与成功门禁

构建命令、四层证据、七组动态矩阵、逐用例输出、严格 `NOT_RUN`、范围和清理门禁全部沿用 v0.4。

本次措辞专项成功还必须证明：

- 登记 / 根写 / 二次登记 / 整树四类结果至少各一项完整载荷配非成功枚举，L2 `成功()==false`；
- I1 对四阶段均不越过，结果 optional 只含已发生阶段；
- 根写非成功保留原待收敛请求；后二登 / 整树失败按首次空槽和已有发布重复路径分别证明守恒；
- 输出与记录不再宣称已构造逻辑上不可能的状态。

其它 v0.4 强制项不得因组 5A 已有局部 PASS 被跳过或降为 `NOT_RUN`。

## 5. 发布、接受与清理

执行结果仍按 v0.4 一份结果提交、交互独立核验、索引退出、再独立清理三件专项的顺序；永久 `.inl` 保留。I2 在重新接受、退出和清理全部正式完成前继续冻结。

## 6. 完成边界

v0.5 可执行只说明验证输入已可构造，不证明代码、完整矩阵或 I1 provider 已完成。最终声明边界与 v0.4 相同。
