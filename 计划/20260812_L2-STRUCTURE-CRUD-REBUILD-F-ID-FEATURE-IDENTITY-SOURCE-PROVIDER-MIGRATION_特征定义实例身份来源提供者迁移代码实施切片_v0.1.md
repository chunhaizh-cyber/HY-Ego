# L2-STRUCTURE-CRUD-REBUILD-F-ID-FEATURE-IDENTITY-SOURCE-PROVIDER-MIGRATION 特征定义 / 实例身份来源提供者迁移代码实施切片 v0.1

日期：2026-08-12
状态：待激活

## 1. 目标、依赖与完成条件

按同版详细设计，把现有 `L2特征结构服务` 从单一 v1 中性写面迁到特征定义 / 实例两个独占 owner，形成双来源、强引用证明、同截止读取、显式退出和唯一普通应用 provider。

正式依赖：G2 已发布；E-ID 设计提交 `973bc86f37351aaa16e9469de1a4a70e738a5997`、plan blob `0b8224215a2438267d6111b397afaca433ba33d4` 已冻结，但 E-ID 代码、清理提交和最终生产 ABI 尚未发布。因此本计划只能登记 `待激活`。激活前必须确认 E-ID 实现 / 清理进入正式 `main` / `origin/main`，共同 DTO、`读取存在身份来源`、存在交付、普通应用 0—5 状态逐字段匹配，三个生产文件和验证资源已释放，再从新 HEAD 重做 S0；不匹配即退回计划支撑修订。

完成要求：两强类型交付不可交换；两来源初始化；定义 owner 内四种属性类型正式登记；定义与归属同写；实例与归属 / 宿主 / 定义三关系同写；宿主经 E-ID、定义经 F-ID 来源证明；当前 / 历史 / 完整 / 枚举同截止；属性 / 值 / 退出闭合来源；精确拒绝矩阵、根工程和临时工程双配置通过；三临时文件独立清理。

## 2. 允许与禁止范围

实施提交只允许：

1. `海中鱼巣/领域/L2特征结构.数据.h`
2. `海中鱼巣/领域/服务.L2特征结构.ixx`
3. `海中鱼巣/装配.普通应用.ixx`
4. `验证工具/L2特征身份来源提供者迁移参数验证.cpp`
5. `验证工具/L2特征身份来源提供者迁移参数验证.vcxproj`
6. `验证工具/运行L2特征身份来源提供者迁移参数验证.ps1`
7. `施工记录/20260812_L2-STRUCTURE-CRUD-REBUILD-F-ID_特征身份来源提供者迁移施工记录.md`
8. `验证记录/20260812_L2-STRUCTURE-CRUD-REBUILD-F-ID_特征身份来源提供者迁移验证记录.md`

清理提交只允许删除 4—6。禁止修改 L1、公共 L2、存在服务、根工程 / filters、其它 L2 服务、T1、DYN2、S-ID、A1 和其它所有者 WIP；禁止恢复 v1、自环、adoption、缓存权威、第二仓 / 成员表、恢复扫描、测试友元或生产验证实体。

## 3. 当前事实与目标接口

基线 `d6207e16` 下数据头 / 服务 / 普通应用 blob 为 `c1217292...` / `a4c102af...` / `76bd7014...`。当前服务接收 `L1事实基座服务&` 并提交中性写集；定义 / 实例无 owner / 来源；普通应用未装配特征服务。根工程已登记目标模块，无工程修改。

新增 DTO：`L2特征定义身份来源读取请求 / 结果`、`L2特征实例身份来源读取请求 / 结果`；定义退出的归属字段放在定义身份后、属性闭包前，实例退出的归属字段放在实例身份后、自有关系前。另新增定义 I64 / I64组 / U64组 / 独立材料四种属性类型身份及 `L2特征定义属性类型登记请求 / 登记 / 结果 / 状态`，由定义端口固定登记；实例值类型不可代替。现有事实和完整结果形状保持。

新增强类型及形成函数：`L2特征定义所有者交付` / `尝试形成L2特征定义所有者交付`、`L2特征实例所有者交付` / `尝试形成L2特征实例所有者交付`。owner 建立身份固定 `...5232` / `...5233`，包装 move-only 且编译期不可交换；形成函数逐项镜像 E-ID 的合同版本、幂等回显、首次成功发布 / 不适用重试、精确重复不发布 / 原幂等收敛、owner 事实 / 代次 / 范围、端口 owner / 有效性 / L1 实例绑定。

服务构造固定为 const L1 读取、const 存在服务、定义交付、实例交付。公开新增 `读取特征定义身份来源`、`读取特征实例身份来源`。定义 / 实例来源登记幂等身份固定 `...4302` / `...4303`，本地键分别 `0x0203'2001—2003` / `0x0203'2101—2103`。构造期同步首次建立 / 重载定义属性类型、实例关系类型和实例值类型，形成完整内部保留编码集合；定位不完整时不得发布服务，来源读取不得退化为 owner + 普通形状。

## 4. 实施顺序

1. 落下 DTO、退出关系字段及纯值有效性 / 成功形状。
2. 落下两套交付、形成函数、两端口成员和新构造；定义来源先于实例来源初始化。
3. 用定义端口落下四属性类型固定登记和只读定位；属性写按材料表示只接受对应定义类型。
4. 迁移定义创建 / 属性 / 退出到定义端口，节点与归属关系同写。
5. 迁移关系类型、值类型、实例 / 两端点关系、当前值 / 退出到实例端口；实例创建同写第三条归属关系。
6. 实现两套当前 / 历史来源；定义 / 实例完整读取把本族来源纳入同一自身投影 / 历史夹取；实例历史读取对宿主 E-ID 与定义来源传同一个请求历史截止，禁止改用任一创建代次。
7. 实例写前经 E-ID 证明宿主、经定义来源证明定义；当前值和退出证明实例来源；具名历史值以值创建代次证明实例历史来源。
8. 改造按存在枚举：存在来源先验、实例批量来源与定义去重证明、末尾代次复核，合法空组与 wrong-family 分离。
9. 扩展普通应用 6—10 状态、两个 owner 建立 / 形成、唯一特征服务成员与双 getter。
10. 完成永久记录和生产外矩阵，发布 8 路径实施提交；随后只删除三临时文件并独立发布清理提交。

## 5. 失败与状态合同

目标当前已退出优先 `已退出`。目标节点 owner 错、非普通 / 属性类型、legacy、裸 / 未标记、锚点 / 类型等内部节点为 `入口拒绝`；来源定位后重复、错 owner / 锚点 / 类型 / 角色、混代、提前退出为 `内部不一致`。稳定编码未占用 / 截止前未创建为 `未找到`；期望代次不符为 `事实代次漂移`；许可 / 资源同名映射。非成功空来源 / 空部分载荷、零普通事实变化。

定义退出闭合节点、归属和全部当前属性；实例退出闭合节点、归属、宿主 / 定义关系和可选当前值。漏项 / 错关系为引用冲突；外部引用保护保持。私有损坏不可公开注入项记 `NOT_RUN`。

## 6. 验证

至少运行：

```powershell
git diff --check -- <8条允许路径>
git diff --cached --check
python .\tools\check_specs.py --strict
MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /verbosity:minimal
MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Release /p:Platform=x64 /verbosity:minimal
powershell -ExecutionPolicy Bypass -File .\验证工具\运行L2特征身份来源提供者迁移参数验证.ps1 -配置 Debug -起始幂等身份 51001
powershell -ExecutionPolicy Bypass -File .\验证工具\运行L2特征身份来源提供者迁移参数验证.ps1 -配置 Release -起始幂等身份 61001
```

成功条件：8 路径严格 UTF-8，临时 vcxproj XML 可解析且系统 `%TEMP%` GUID 隔离；两 owner / 两来源 / 不可交换交付通过；定义四属性类型登记及表示匹配通过；定义两事实与实例四事实同写 / 重复 / 冲突通过；宿主 / 定义来源及当前 / 历史 / 属性 / 值 / 枚举 / 退出后具名历史值通过；wrong-family / legacy / 裸节点 / internal / malformed 精确失败且零变化；普通应用重复 getter 同址；有界并发无混代 / 部分载荷；L1、公共 L2、存在服务、工程和禁止路径零 diff；生产零测试实体。

实施精确暂存 8 项后 cached check 通过且 staged 恰为 8；清理精确暂存三项删除后再次通过且 staged 恰为三项；最终三临时路径不存在、index 空。未运行生产业务、长期公平、分配故障、恢复、跨进程和独立集成验收必须如实记 `NOT_RUN`。

## 7. 记录与完成声明

施工记录列实际 DTO、函数、常量、owner 路由、写集和 provider 调用点；验证记录逐项列 PASS / FAIL / NOT_RUN、两配置结果、异主 WIP 和待清理三文件。记录不自指实现 / 清理提交哈希。

计划变为 `可执行` 只表示 E-ID 真门禁满足且 S0 可选择，不证明 F-ID 已实现。结果提交也只证明本切片通过矩阵，不授权 T1、DYN2、S-ID、A1、业务闭环或独立集成验收。
