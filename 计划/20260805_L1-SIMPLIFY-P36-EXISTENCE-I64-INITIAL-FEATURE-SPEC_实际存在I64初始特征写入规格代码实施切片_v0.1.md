# L1-SIMPLIFY-P36 实际存在 I64 初始特征写入规格代码实施切片 v0.1

日期：2026-08-05

状态：待激活

计划身份：`L1-SIMPLIFY-P36-EXISTENCE-I64-INITIAL-FEATURE-SPEC`

创建侧基线：`5b68a6389cd6f1d5d15eb08d3497e6dfb289e090`

## 1. 施工目标

在唯一 `L1实例特征服务` 原位新增：

```cpp
实际存在I64初始特征写入规格结果
形成实际存在I64初始特征写入规格(
    const 实际存在I64初始特征写入规格请求& 请求) const;
```

函数在请求的同一事实代次内互证宿主、I64定义、来源存在、P8登记和宿主+定义零当前槽，返回单项不可变纯值规格，全程零 L1 写入。完成后执行者无需决定 DTO、字段来源、错误映射、锁、排序、批次或后继发布边界。

## 2. 允许文件

只允许修改或新增：

```text
海中鱼巣/领域/L1实例特征.数据.h
海中鱼巣/领域/服务.L1实例特征.ixx
海中鱼巣/领域/自检.L1实例特征.ixx
施工记录/20260805_L1-SIMPLIFY-P36-EXISTENCE-I64-INITIAL-FEATURE-SPEC_施工记录.md
验证记录/20260805_L1-SIMPLIFY-P36-EXISTENCE-I64-INITIAL-FEATURE-SPEC_验证记录.md
```

前三个是唯一代码范围，后两个是本计划专属施工/验证证据。禁止修改工程、启动入口、P7/P2/L1、P15、P32-P35、旧特征栈、计划索引和其它文件。若实际编译证明 include 或模块接口需要第四个生产文件，立即退回计划支撑，不得临场扩大范围。

## 3. 实施顺序

1. S0 重核正式 HEAD、origin、index、dirty 所有者、计划版本/blob/状态和三个允许文件唯一所有权。
2. 逐项核对 P15 真实结果迁移后的 P8/P10 DTO、构造、锁、状态映射、登记、`检查宿主定义唯一性` 和自检结构；任何机器接口漂移退回计划支撑。
3. 在数据头追加详细设计冻结的两个版本常量、请求、规格、结果及完整性函数；全部字段中文命名、强类型、值语义。
4. 在 P8 服务原位实现单一公开函数；固定顺序调用 P7宿主、P2定义、P7来源、四登记节点、一次完整快照、唯一性 helper。
5. 确保所有依赖结果携带同一请求代次；来源与宿主同世界；初始值在闭区间；零匹配才构造规格。
6. 在既有 P8 专项自检中追加完整矩阵，不新增第二自检模块或启动接线。
7. 在两个专属记录路径逐项登记实际接口、调用计数、静态扫描、构建、自检、模块和 Git 范围证据。
8. 执行验证；只提交计划允许的五文件切片并普通推送。提交进入 Git 只证明源码候选发布，不证明完整批次能力。

## 4. 固定实现合同

### 4.1 调用计数与顺序

成功首次调用固定：

```text
P7读取宿主 1次
-> P2读取定义 1次
-> P7读取来源 1次
-> L1读取四登记节点
-> L1完整快照 1次
-> 检查宿主定义唯一性 1次
-> 提交写集 0次
```

不重试、不 sleep、不用普通缓存补齐。入口坏请求和登记未加载时所有依赖调用为0。

### 4.2 成功规格

结果逐字段携带：规格合同/规则版本、共同截止、原始项目顺序、完整宿主事实、完整 P2 定义事实和读取见证、初始值、完整来源存在事实、P8 服务/两关系类型/I64属性类型节点见证、固定零当前匹配槽数量。

非成功规格为空。已有恰一槽是 `幂等冲突`；重复组合、悬空端点、角色/类型/当前值/属性槽损坏是 `内部不一致`，不得互换。

### 4.3 禁止实现

```text
禁止形成批次身份、幂等键、两摘要、局部键或通用读回计划
禁止调用 L1 探测幂等 / 提交写集 / 稳定编码分配
禁止写 4170 批次账或 P8 进程内实例账
禁止逐项调用旧建立入口
禁止 SQL、材料、旧默认域、显示名、句柄、许可或事务外泄
禁止把项目顺序排序、去重或解释成完整批次成功
```

## 5. 验证门禁

至少完成并保存真实结果：

1. Debug/Release 两配置 Rebuild；
2. 两配置 `--self-test-exit` 完整自检均退出0；
3. P36 新矩阵覆盖详细设计第9节全部12类；
4. P8/P10 原专项不退化；
5. 静态扫描目标函数 `.提交写集(`、`探测幂等(`、SQL/材料/旧栈命中均为0；
6. 两配置 MSVC 模块扫描总数与基线增量机械闭合；本计划不新增模块，模块数应不变；
7. `git diff --check`；
8. `python .\tools\check_specs.py --strict`；
9. staged 精确仅三个代码文件与两个专属记录，提交前后 main/origin/index 和其它 WIP 原样。

验证失败先在本计划范围内归因。只有正式接口漂移、必须扩大文件或状态无法无损映射时退回计划支撑。

## 6. 激活门禁

保持 `待激活`，直至：

1. P15 v0.5 结果正式发布并经交互接受，且释放三个共享 P8 文件；
2. P15 后 P8/P10 实际接口与本计划机械匹配；
3. P7/P2 读取接口和接受边界未漂移；
4. 当前活动执行段释放代码、工程、自检、模块扫描和 Git 发布资源；
5. 执行侧从新 HEAD 重做完整 S0，确认本 plan blob、版本、允许文件、模块数、自检数和唯一候选。

任一门禁不满足不得施工，不得因计划文本“设计就绪”推断代码已实现。

## 7. 完成声明边界

允许声明：P36 源码候选可在同一事实截止下为一个当前实际存在、一个尚未建立的 I64 定义形成完整不可变初始特征单项写入规格，且零事实写入。

禁止声明：完整初始特征批次、4170 幂等账、P32 第二写门面、P15 能力、状态动态、因果、恢复装配、运行期接线、集成验收、STEP-4 或 STEP-5 已完成。
