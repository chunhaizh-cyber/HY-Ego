# L1-SIMPLIFY-P1-WORLD-STRUCTURE 世界结构事实迁移待激活叶 v0.1

计划身份：`L1-SIMPLIFY-P1-WORLD-STRUCTURE`

版本：v0.1

状态：待激活；本文件只登记后继目标和门禁，不是代码施工许可。

## 1. 最小目标

在新 L1 三类事实基座上形成首个世界结构领域写读闭环：由一个具名 L2 领域服务消费长寿命 `L1事实基座服务`，在已合法登记的现实世界结构内提交一个最小结构变化，并从另一读取途径重新取得权威当前事实，以重读状态而非动作返回预估裁决结果。

首叶只选择一个可独立验收的结构动作；不同时迁移首次引导、存在、移动、移除、特征、状态动态、生产装配或旧栈删除。精确动作、世界/场景资格身份、DTO、物理文件、登记提供者、事务和验证矩阵须在激活修订中基于 P0-FIX 正式结果重新冻结，本版本不预造。

## 2. 具名依赖与门禁

1. `L1-SIMPLIFY-P0-FIX v0.1` 必须先完成真实代码实施并进入正式 main，证明类型形状、完整引用、规范化幂等、提交异常安全和恢复一次替换合同。
2. 计划智能体必须从该结果重新读取 P0 最终 DTO、服务签名和实际行为，再完成本叶详细设计；不能沿用旧 P1A3/P1A3F DTO、索引、恢复流程、对象/关系/值版本或开工许可。
3. 世界结构资格和登记提供者若仍未形成正式合同与真实结果，本叶继续待激活；不得以固定编码、显示名、配置、旧句柄、自检替身或安全未实现骨架解除门禁。
4. 激活版本必须冻结业务输入输出、完整函数/DTO/物理位置、合法消费者、长寿命 L1 实例所有权、一次事务、动作后预计动态、另一途径权威重读、错误映射和验证矩阵。

## 3. 当前禁止范围与完成边界

本版本不允许任何代码、工程、自检、施工记录或验证记录写入，不登记允许文件白名单，不指定自检顺序。它只替代旧 `WORLD-TREE-P1A3 v0.3` 的后继路由，旧计划改为已退出历史证据。

本版本发布只证明世界结构迁移目标和依赖门禁已经登记；不证明计划可执行、P0 合同已补齐、世界结构接口已冻结或任何世界能力已经实现。
