# SELFTEST-MIGRATION-B2-S1 基础信息与仓库领域自检迁移代码实施切片 v0.1

日期：2026-07-12

队列编号：#252 / 设计队列：DQ-144

状态：JY-498 终止 / 纯自检搬迁不再占用生产闭环路线 / 不得执行

## 1. 依据与假定

依据第二批流程图 / 详细设计与 #251 预期记录。当前具体验收数、模块拆分和阶段顺序不是事实；假定 #251 证明仓库、语素及基础信息测试可按共享夹具所有权分组。漂移时不改代码，退回设计。

## 2. 范围

预期新增 `领域/自检.核心仓库.ixx`、`自检.语素基础信息.ixx`、`自检.特征状态动态.ixx`、`自检.存在场景因果.ixx` 真模块，均 `ClCompile + export module + import`；入口只登记回调。允许这四个预期模块、`入口.cpp`、工程文件和治理；#251 若裁决不同模块名 / 数量，先退回修订，不现场发明。禁止生产服务 / 核心仓库修改、测试改义、伪造样本、复制夹具和旧行为 `.h/.cpp`。

## 3. 步骤

S0 逐编号 / 逐字段复核；S1 先建公开报告和空骨架编译；S2 迁移共享夹具到唯一所有者；S3 迁移正文与输出；S4 登记阶段并删除入口对应正文；S5 验证编号、结果、结构、顺序和并发守恒。

## 4. 非成功二分

依赖 / 接口 / 阶段漂移为门控返回；原自检合法失败保持普通测试失败且不短路；迁移后结果、编号或结构变化归追根因。

## 5. 验收

A01-A14、Debug x64 0/0、完整自检、严格规范、diff、每个迁移编号恰好一次、最终连续 20 轮；精确提交推送。

## 6. 边界

只迁移既有自检承载，不新增领域能力或旧能力迁移完成度。
