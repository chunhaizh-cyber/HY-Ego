# DEMAND-SINGLE-TARGET-STRUCTURE-PUBLISH-CONSUME 单目标需求结构公开发布消费代码实施切片 v0.1

日期：2026-08-16
状态：待激活

## 1. 目标与完成条件

实现 BIZ-L4-004-02-01-05 v0.2：在唯一 `需求业务服务`内把已经完成准入、去重/合并和必要路径复核的候选转换为DATA-EXT-12公开强类型请求，恰一次提交，互证已发布/精确重复的首次请求见证和G1，并把结果交给06。同步机械落实BIZ-L3-004-02-01 v0.3的05参数/状态绑定。

完成必须证明零BIZ数据操作/事务/L1/owner/撤销/内部写集、零5150重复写入口、发布未知原请求守恒和成功/重复必经06；不得声明需求整体完成。

## 2. 激活门禁与当前事实

当前正式代码没有 `服务.需求.ixx`、本叶DTO或DATA-EXT-12最终服务。本计划必须保持待激活，直到：

- `DEMAND-SOURCE-ADMISSION-PROVIDER v0.2`及当前必需来源provider、`DEMAND-CURRENT-SAME-PARENT-TARGET-CONSUME v0.2`、`DEMAND-SOURCE-PATH-MERGE-CANDIDATE-COMPOSE v0.1`和04后继真实结果/退出/清理完成；
- DATA-EXT-12唯一服务、普通应用取得路径、`发布单目标需求结构`最终DTO/状态/首次请求见证/幂等/发布未知ABI发布；
- 上游合同头、`服务.需求.ixx`、工程、唯一代码段和构建资源释放；
- 从最新main重做S0，计划blob、详细设计blob、实际ABI和允许范围精确匹配。

接口声明、空函数、测试替身或“DATA能力未就绪”运行分支都不能解除真实接线门禁。

## 3. 允许文件

```text
海中鱼巣/领域/需求结构发布消费.数据.h
海中鱼巣/领域/服务.需求.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
施工记录/20260816_DEMAND-SINGLE-TARGET-STRUCTURE-PUBLISH-CONSUME_单目标需求结构发布消费施工记录_v0.1.md
验证记录/20260816_DEMAND-SINGLE-TARGET-STRUCTURE-PUBLISH-CONSUME_单目标需求结构发布消费检查记录_v0.1.md
验证工具/单目标需求结构发布消费验证.cpp
验证工具/单目标需求结构发布消费验证.vcxproj
验证工具/运行单目标需求结构发布消费验证.ps1
```

若目标文件已由上游计划创建，只允许在其正式释放后机械扩展。临时专项三文件必须独立清理，不留生产登记。

## 4. 禁止范围

禁止修改 DATA/L1/owner、DATA-EXT-12提供者、最终需求清单、规范、目标图、其它计划、计划索引、任务/方法/线程、SQL/恢复和异主WIP。禁止新增 `数据操作.需求.ixx`、第二需求服务、第二幂等账、BIZ事务/撤销、内部写集、缓存或运行态“DATA未就绪”旁路。

## 5. 实施步骤

1. 按详细设计建立BIZ发布模式、请求、状态、成功材料、结果和纯值helper；函数名/变量名使用中文。
2. 在同一 `需求业务服务`借入普通应用同实例唯一 `const L2需求结构服务&`；不得增加L1或owner引用。
3. 实现新建/补充模式矩阵和04见证门禁；非法组合零DATA调用。
4. 把BIZ候选逐字段转换为DATA中性目标、来源、路径、生命周期、G0、版本、预算和单一幂等身份；不传业务布尔/BIZ见证/内部写集。
5. 原完整DATA请求恰一次调用最终公开写入口；零重试、零截止重选、零预读幂等账。
6. 精确映射非成功；发布未知保留原BIZ请求、零发布材料。
7. 已发布/精确重复互证需求身份、首次请求见证、首次G0/幂等身份和G1；坏形状内部不一致。
8. 扩展父函数参数/状态映射：业务拒绝由父入口形成一次5150回执；成功/重复都把需求身份和G1交06。
9. 所有候选完整构造后无抛出交换；`bad_alloc/length_error`资源失败，其它异常内部不一致。

## 6. 验证

生产外专项至少覆盖详细设计§8十项矩阵，特别是：模式组合、零调用、DATA请求零业务材料、恰一调用、完整首次见证、精确重复原G1、发布未知原请求守恒、父5150唯一写入口以及成功/重复必经06。

验证必须执行：

```powershell
msbuild .\海中鱼巣.sln /t:Rebuild /p:Configuration=Debug /p:Platform=x64
msbuild .\海中鱼巣.sln /t:Rebuild /p:Configuration=Release /p:Platform=x64
python .\tools\check_specs.py --strict
git diff --check
```

并检查UTF-8无BOM、工程唯一登记、生产零专项痕迹、目标白名单和禁止import。专项替身只验证BIZ协议，不得宣称真实DATA发布、并发、结果未知收敛或06读回。

## 7. 失败与漂移

纯代码/编译/专项问题由执行者在白名单内修复。若最终DATA接口要求多个子幂等、BIZ内部写集/事务/撤销，缺完整首次请求见证或原G1，或上游04/06改变本叶请求/结果语义，固定以 `DRIFT-DEMAND-SINGLE-TARGET-STRUCTURE-PUBLISH-DATA-CONTRACT`零扩权退回计划支撑。

## 8. 完成声明边界

本计划未来完成只证明05经唯一DATA公开写入口提交并正确解释结果。DATA-EXT-12整项、06正式读回、单目标需求父链、任务化、结算、恢复及系统业务闭环均为 `NOT_RUN`。
