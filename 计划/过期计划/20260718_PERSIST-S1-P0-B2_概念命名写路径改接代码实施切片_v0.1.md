# PERSIST-S1-P0-B2 概念命名写路径改接代码实施切片 v0.1

日期：2026-07-18

执行队列：#300

设计队列：DQ-192

设计裁决：JY-391

计划阶段：930

状态：JY-498 终止当前计划版本 / 概念命名保留为独立后继 / 930 不登记 / 不得执行

## 1. 暂停裁决

恢复流程图 v0.3、详细设计第 17 节和 #295 事实矩阵只固定了本批的边界，没有撤销用户对 #214 的暂停。当前不得执行、构建本计划或借 B0 的只读 / 任务合同恢复概念命名写业务。

恢复执行资格必须同时满足：

```text
用户明确恢复 #214。
#296、#297、#298 已完成归档。
#214 重新形成当前流程图、详细设计和允许 / 禁止文件一致的施工三件套。
新三件套证明命名请求只经运行期租约和正式业务面，不取得独占许可、不直接调用旧概念图 / 需求 / 语素服务。
设计角色基于实际接口修订本计划到新版本并重新派发。
```

任一项缺失时，执行角色只判定“用户暂停”，不写代码、不写执行断点、不把它当真实阻塞。

## 2. 预冻结范围（非许可）

后继修订不得超出：

```text
海中鱼巣/线程/路由.概念命名治理.ixx
海中鱼巣/线程/自检.概念命名运行期改接.ixx       新建候选
海中鱼巣/自检.运行器.ixx                         仅登记 930 候选
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
后继执行专属实施记录
```

新自检候选必须为 `export module 海中鱼巣.线程.自检.概念命名运行期改接` 的 `ClCompile` 真模块，只由自检运行器 import。最终文件仍以恢复 #214 后的新三件套为准。

## 3. 预冻结目标与禁止项

目标只允许把命名写请求改为 B0 正式强类型业务面；不得改 B1 两桥、B3 自我治理、旧概念图服务或新概念活动所有者内部。禁止仓库、令牌、许可、会话、旧服务兜底、O1、SQL / 面板、快照恢复和默认入口切换。

## 4. 非成功返回

当前所有尝试均归依赖 / 用户暂停门控，不进入业务返回二分。未来版本中，无租约、请求无效、已命名或请求已变化属于逻辑内返回；正式写入前置通过后的提交 / 读回不一致属于追根因；实际接口与修订计划不一致属于接口漂移。

## 5. 未来验收下限

未来版本至少验证：运行期租约唯一、旧服务直接依赖为 0、无独占许可 / 令牌穿透、有效 / 幂等 / 已变化分流、阶段 930 Debug 唯一登记、Release 隔离、Debug / Release Rebuild、两配置完整自检、最终 Debug 20 / 20、strict、diff 和精确范围。

## 6. 完成边界

本 v0.1 只证明 #300 已作为暂停占位计划登记，防止执行窗口自行推断 #214 已恢复。它不证明任何代码许可、概念命名迁移、阶段 930、默认入口、快照或恢复。
