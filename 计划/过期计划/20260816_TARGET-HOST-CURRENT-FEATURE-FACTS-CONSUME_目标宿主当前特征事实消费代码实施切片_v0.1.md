# TARGET-HOST-CURRENT-FEATURE-FACTS-CONSUME 目标宿主当前特征事实消费代码实施切片 v0.1

日期：2026-08-16
状态：待激活

## 1. 目标与完成条件

实现 `BIZ-L4-004-01-01-02`：串行扩展同一 `需求目标事实提供者`，在同一非零 G0 下通过现有场景、存在和特征公开服务返回目标宿主的唯一当前特征实例和值式事实。

完成条件为：公开 DTO / 函数按详细设计落盘，固定四读、状态映射、失败守恒和 `noexcept` 成立；根工程与生产外专项按矩阵通过；结果、记录和临时专项清理正式发布。本计划不以设计、编译或单一成功路径宣称父业务闭环完成。

## 2. 正式依据与 S0

- 目标图：`BIZ-L3-004-01-01 v0.2`、`BIZ-L4-004-01-01-02 v0.1`。
- 详细设计：`规范/详细设计/20260816_TARGET-HOST-CURRENT-FEATURE-FACTS-CONSUME_目标宿主当前特征事实消费详细设计_v0.1.md`。
- 直接规范：4110、4160、4190、4200、4230、5100。
- 前驱：`TARGET-STATE-CONTRACT-DEFINITION-CONSUME v0.1`。

执行前重新 S0：确认索引精确版本、前驱结果 / 退出、两个生产文件已由前驱创建且无活动所有者、provider 仍只持 `const L2结构聚合服务&`、四个 DATA 函数与 DTO 未漂移、工程无需新增登记、唯一代码段 / 构建资源和 Git index 可用。

## 3. 允许文件

生产文件只允许：

1. `海中鱼巣/领域/需求目标事实.数据.h`
2. `海中鱼巣/领域/提供者.需求目标事实.ixx`

永久记录只允许：

3. `施工记录/20260816_TARGET-HOST-CURRENT-FEATURE-FACTS-CONSUME_目标宿主当前特征事实消费施工记录_v0.1.md`
4. `验证记录/20260816_TARGET-HOST-CURRENT-FEATURE-FACTS-CONSUME_目标宿主当前特征事实消费验证记录_v0.1.md`

生产工程外临时专项只允许：

5. `验证工具/目标宿主当前特征事实消费验证.cpp`
6. `验证工具/目标宿主当前特征事实消费验证.vcxproj`
7. `验证工具/运行目标宿主当前特征事实消费验证.ps1`

禁止修改根工程、filters、普通应用、计划索引、DATA 服务、`服务.需求.ixx`、父 L3 代码和其它 BIZ provider。临时专项不得登记到生产工程，结果退出后必须单独清理三路径并复验根工程。

## 4. 实施步骤

1. 在同一数据头追加合同 / 规则版本、状态、请求、材料、结果、请求 helper、材料完整 helper和成功谓词。
2. 在同一 provider 中追加 `读取目标宿主同截止当前特征事实(...) const noexcept`；不新增构造参数、成员、第二 provider 或专用 getter。
3. 请求无效零 DATA 调用；有效请求固定按“树归属 -> 完整场景 -> 完整存在 -> 按存在全部当前特征”执行，每次请求头使用同一 G0。
4. 按详细设计冻结 0/1/多成员、0/1/多同定义实例和空当前值分账；任何非成功材料为空。
5. 完成逐字段互证、异常映射和返回前完整候选构造；禁止状态 / 动态 / 因果、L1、owner、缓存或写入口。
6. 建立生产外专项和两份永久记录，执行验证矩阵；提交结果后由索引所有者登记退出，再独立删除三件临时专项。

## 5. 验证

至少执行：

```powershell
git diff --check -- <本计划精确文件>
python .\tools\check_specs.py --strict
msbuild .\海中鱼巣.sln /t:Rebuild /p:Configuration=Debug /p:Platform=x64
msbuild .\海中鱼巣.sln /t:Rebuild /p:Configuration=Release /p:Platform=x64
```

专项双配置必须覆盖：合法成功、请求拒绝、首笔场景 wrong-family、场景未找到 / 已退出、树不等、场景无成员、目标实例未找到、当前值未设置、当前性漂移、许可拒绝和可由公开 ABI 形成的并发分支。首笔 DATA 入口拒绝映射 BIZ 入口拒绝；同 G0 已证明身份后的入口拒绝 / 引用冲突须静态映射为内部不一致。重复实例坏形状、私有损坏和真实资源耗尽不可合法注入时保留静态防御并记 `NOT_RUN`，禁止生产 seam。

静态门禁：中文函数 / 变量名；四读顺序与相同 G0；同一聚合引用；两个生产文件外零改动；根工程零新增登记；零 L1 / owner / 仓库 / 缓存 / sleep / 状态 / 动态 / 因果服务和零 DATA 写。

## 6. 激活与声明边界

当前状态只能为待激活。前驱 `TARGET-STATE-CONTRACT-DEFINITION-CONSUME v0.1` 真实结果、退出及同文件释放前不得施工；设计不等待任何新增 DATA 能力。前驱闭合后只做最终 ABI / 所有权机械匹配即可激活。

代码结果只证明本 L4 provider 叶。父 L3 组合、目标差距裁决、需求 / 任务结构、普通应用持有 / getter / 装配、T02 接线、系统闭环、跨进程恢复和独立集成验收均为 `NOT_RUN`。
