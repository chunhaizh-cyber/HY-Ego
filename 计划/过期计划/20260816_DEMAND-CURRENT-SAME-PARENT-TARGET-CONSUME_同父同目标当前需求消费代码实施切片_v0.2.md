# DEMAND-CURRENT-SAME-PARENT-TARGET-CONSUME 同父同目标当前需求消费代码实施切片 v0.2

日期：2026-08-16
版本：v0.2
身份：`DEMAND-CURRENT-SAME-PARENT-TARGET-CONSUME`
状态：待激活

## 1. 目标与完成条件

按 BIZ-L4-004-02-01-02 v0.3 和同身份详细设计 v0.2，实现同父同目标当前需求读取。保持精确目标语义键和零 / 一 / 多裁决；唯一需求结果必须完整携带同一父需求下全部当前路径关系组，并分账需求、来源授权、路径关系三个数量预算。

本版替代未执行的 v0.1；旧版把当前路径冻结为单数，不得施工。

## 2. 正式依据与执行前事实

- 规范：0050、3100、4030、5100、5110、CODE-FILE-01；
- 目标图：`流程图/目标业务流程图/L4/BIZ-L4-004-02-01-02_读取同父同目标当前需求_目标函数流程图_v0.3.md`；
- 详细设计：`规范/详细设计/20260816_DEMAND-CURRENT-SAME-PARENT-TARGET-CONSUME_同父同目标当前需求消费详细设计_v0.2.md`；
- DATA-EXT-12 精确组读、完整路径组、三类预算及 C++ ABI 尚未发布；生产代码只有 `L2需求身份`和需求节点族；
- 同父同目标复用与多 OR / AND 路径共同要求一个需求身份可以保留多条当前路径关系；DATA 只保存结构，BIZ 后继裁决路径角色与活动结算。

S0 必须确认最终需求清单、DATA ABI、合法取得路径、首个 `需求业务服务`前驱、共享文件 / 工程 / 代码段 / 构建资源全部满足本设计门禁，且计划索引精确为 v0.2 可执行。

## 3. 允许与禁止文件

允许：

1. `海中鱼巣/领域/需求同父目标读取.数据.h`；
2. `海中鱼巣/领域/服务.需求.ixx`；
3. `海中鱼巣.vcxproj`；
4. `海中鱼巣.vcxproj.filters`；
5. `施工记录/20260816_DEMAND-CURRENT-SAME-PARENT-TARGET-CONSUME_同父同目标当前需求消费施工记录_v0.2.md`；
6. `验证记录/20260816_DEMAND-CURRENT-SAME-PARENT-TARGET-CONSUME_同父同目标当前需求消费验证记录_v0.2.md`；
7. `验证工具/同父同目标当前需求消费验证.cpp`；
8. `验证工具/同父同目标当前需求消费验证.vcxproj`；
9. `验证工具/运行同父同目标当前需求消费验证.ps1`。

禁止修改 DATA / L1 / owner、普通应用、其它 DEMAND DTO、正差距 / 来源 / 父路径服务、线程、正式规范、流程图、设计、计划和索引。禁止恢复 v0.1 单路径结果或新增 BIZ DATA 写入口。

## 4. 实施步骤

1. 新建 DTO 头，冻结版本、两份固定配置、请求、9个状态、结果、三个非零预算和完整当前路径关系值式组；只有唯一当前需求携带 optional 值。
2. 串行扩展唯一 `需求业务服务`，继续只持同一 DATA-EXT-12 const 引用和不可变配置；零第二 owner / 服务 / getter。
3. 构造主体 + 可空父 + 宿主 + 特征 + 目标合同引用 + 目标状态语义键 + G0 + 全部版本 + 三类预算的一次组读；运行代次不进入 DATA 请求。
4. 恰一次调用 DATA，互证回显、G0、版本、三类预算和全部当前需求。零 / 一 / 多分账，多项不任选。
5. 唯一项完整保留目标、来源 / 授权、父关系、生命周期和全部当前路径关系；按路径组身份、角色、顺序、关系身份稳定排序。路径差异不进入需求去重键，不折叠同端点互异路径。
6. 父需求为空时路径组必须为空，父需求非空时必须至少一项；违反任一基数条件均为内部不一致、零载荷、零截止。重复、错端点、同关系多当前版本、未知角色 / 当前性、坏生命周期、混代、截断和 checked 累计溢出同样为内部不一致。
7. 按设计映射请求拒绝、漂移、预算、许可、资源、未实现 / 未装配、坏形状和异常；零重试、缓存、截止重选和写入。
8. 工程唯一登记，完成专项与永久记录；结果 / 退出后独立删除临时专项并清理复验。

## 5. 验证与成功标准

- 根工程 x64 Debug / Release Rebuild PASS；
- 空 / 一 / 多需求分账；两个以上互异当前路径关系在唯一需求内全部保留且稳定排序；
- 同父同目标路径差异不产生第二需求，路径组不影响精确目标键；
- 三类预算、G0、版本、来源授权、路径端点 / 角色 / 当前性 / 生命周期和 optional 形状全部互证；父空则路径组必空，父非空则路径组必非空，反例统一内部不一致、零载荷、零截止；
- 只有已读取零 / 一 / 多回显 G0，其它状态零截止、零业务载荷；
- 源码恰一次 DATA 组读，零 DATA 写 / L1 / owner / 历史 / 缓存 / sleep / retry；
- DATA / 普通应用 / 其它 DEMAND 文件零差异，工程零重复登记，生产零专项痕迹；
- `git diff --check`、strict、UTF-8无BOM和精确白名单通过。

## 6. 激活、发布与边界

只有最终需求清单登记完整路径组和三类预算、DATA-EXT-12 最终 ABI / 取得路径发布、首个需求服务前驱退出并释放生产与构建资源后，索引所有者才能从最新 HEAD 机械匹配并把 v0.2 提升为可执行。

任何恢复单路径、改变精确键 / 路径多重性、让 DATA 选择活动路径或改变结果 optional 的漂移，固定具名 `DRIFT-DEMAND-CURRENT-SAME-PARENT-TARGET-PATH-GROUP-OR-ABI`。本结果不证明来源准入、父路径 / 防环、发布 / 合并、结算、任务树、T03或系统闭环。
