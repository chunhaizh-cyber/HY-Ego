# L1-SIMPLIFY-P9-BASELINE-STATE 实际存在 I64 首个关系19基准状态代码实施切片 v0.3

计划身份：`L1-SIMPLIFY-P9-BASELINE-STATE`

版本：`v0.3`

创建侧代码基线：`046cf19a650a9bb31715eabe4668e4662cd724e3`

替代关系：本计划完整替代尚未执行的 v0.2；v0.2 禁止施工。

## 1. 目标和完成条件

实现 STEP-3C 首个关系19基准状态，并保留 v0.2 已闭合的不可变当前/历史读取。删除结果 DTO 中错误的 `特征定义` 字段，确保被采样 P8 值属性类型不被冒充为 P2 抽象定义。

完成必须满足：

- 首态二节点、五值、五关系、五属性槽单写集及幂等合同不变；
- 读取入口按状态自身见证从 L1 当前/历史入口取得不可变快照；
- 结果返回角色3 P8实例槽及被采样值真实属性类型、材料、来源、创建/退出代次；
- 结果不输出、复制或推断 P2 抽象定义，不新增定义写项或第二读取入口；
- 后继只能用角色3实例槽与 P10/P8 新当前实例槽对齐，再消费 P10/P8 合法定义；
- 双配置、自检、strict、模块扫描和静态闭包通过。

唯一设计依据为同名 v0.3 详细设计、函数图及 4015、4190、4200、4210、P8 v0.1合同。v0.1/v0.2只作被替代差异证据。

## 2. 精确允许范围

只允许：

```text
海中鱼巣/领域/L1状态.数据.h
海中鱼巣/领域/服务.L1状态.ixx
海中鱼巣/领域/自检.L1状态.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
海中鱼巣/启动.应用程序.ixx
施工记录/20260805_L1-SIMPLIFY-P9-BASELINE-STATE_施工记录.md
验证记录/20260805_L1-SIMPLIFY-P9-BASELINE-STATE_验证记录.md
```

禁止修改 P7/P8/P10/P6、L1基座、P2定义服务、4180、全部L2合同、旧状态动态栈、运行期、SQL、材料、恢复、日志、其它设计或计划。

## 3. 待激活门禁

计划保持 `待激活`：

1. P7、P8正式结果分别经交互智能体接受；
2. P7/P8实际接口与发布前冻结合同匹配；
3. P7/P8释放代码、工程、启动、自检以及P9八文件/顺序360所有权；
4. 从新正式HEAD重做S0，复核L1当前/历史/快照入口、自检数、模块数和相邻顺序。

P6/P10不是首态机器依赖。P10式旧值退出可由专项夹具形成，不伪造激活依赖。

## 4. 机械施工

1. 按v0.3详细设计形成数据头；`实际存在I64基准状态事实`不得含`特征定义`。
2. 实现七节点登记和本地键1—12首态写集；写集不得出现P2定义复制项。
3. 发布前仍由P8/P7验证完整当前组合；P2定义只参与资格校验，不进入P9结果。
4. 发布后构造状态自身读取请求，调用同一公开读取入口。
5. 读取入口以首末完整快照包围逐项当前/历史读取，从关系19得到角色3实例槽，从引用取得被采样值。
6. 被采样值属性类型只验证I64表示并原样放在`被采样P8值.属性类型`；禁止赋给、重命名为或比较为P2定义。
7. 保留P10换代、P7场景移动、端点退出、混代重试及错误分账矩阵。
8. 原位新增顺序360专项并形成专属施工/验证记录。

## 5. 停工条件

若需要新增定义关系见证、扩大写集、调用P2重解释历史状态、新增第二状态读取入口、让比较服务解析关系19或修改白名单外文件，停止并退回设计漂移。

## 6. 验证合同

- 精确八文件，Markdown只允许两份专属记录；cached diff check通过；
- Debug/Release x64串行Rebuild及两配置完整自检通过；
- strict通过；MSVC模块扫描失败0、循环0、外部需求0；
- 首态写集、本地键1—12、关系19角色1—5、当前/历史读取与代次分账精确；
- `实际存在I64基准状态事实`中`特征定义`字段为零处；新定义节点/关系/值写项为零；
- P2定义编码与P8值属性类型编码故意不同时，状态成功返回且不混淆二者；
- 被采样值真实属性类型、材料、来源、创建/退出代次逐项读回；
- 读取函数P7/P8/P2/4180调用为零；
- 后继消费机械闭包只允许实例槽等式，不允许值属性类型充当定义；
- 关系20、动态、因果、生产装配、SQL、材料、旧栈和生产日志调用为零。

## 7. 完成边界

只证明首个关系19基准状态可按自身见证从L1当前/历史入口读取，并准确区分实例槽、值属性类型和后继才能取得的P2定义。

不得声明P7/P8/P10已实现、P9证明P2定义、当前/相邻状态选择、第二状态、迁移动能、动作动态、因果、生产装配、集成验收、STEP-3或治理闭环完成。
