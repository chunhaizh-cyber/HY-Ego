# DD-05F0R 特征批次发布记录快照恢复设计治理切片 v0.1

归档状态：因 C013 基础结构裁决失效；由 `#338 / NODE-TYPED-MIGRATION` 重新生成后继设计和计划；本文件仅作历史证据，不得恢复派发。

编号：DD-05F0R

日期：2026-07-22

版本：v0.1

状态：#333 / DQ-225；依赖门控设计治理；等待 #329 v0.2 实现、独立集成进入 main 并完成实际侧账接口复核；不授权 C++、构建、运行或执行 worktree

## 1. 目标与依据

本切片承接 `规范/4170_子规范_特征批次发布记录与幂等账.md` 的快照、恢复候选和恢复拒绝合同，补齐 #329 明确排除的持久化 / 恢复设计。依据包括：

- `规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`
- `规范/0500_子规范_字段与结构新增准入约束_20260720.md`
- `规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`
- `规范/4040_子规范_不透明结构事务候选确认撤销与最后发布.md`
- `规范/4170_子规范_特征批次发布记录与幂等账.md`
- `规范/代码文件建立归属与模块命名规范.md`
- `海中鱼巣/核心/仓库快照服务.h`
- `海中鱼巣/核心/自检.仓库权威导出.ixx`
- #329 v0.2 进入 main 后的实际 `参与者.特征批次发布记录`、侧账访问器和运行期上下文接口

## 2. 已验证当前事实

1. 4170 要求快照 / 恢复保留记录格式版本、批次身份、业务规则版本、完整有序项目和全部完整句柄，并拒绝未知格式、悬空引用、项目缺项、顺序不连续或引用结构不一致。
2. 当前 `核心/仓库快照服务.h` 的文件规则明确只承载恢复拒绝矩阵和隔离候选材料，不得写运行期仓库或执行恢复替换。
3. 当前仓库权威导出只覆盖主信息、节点、关系和索引；#329 前的 main 没有 4170 侧账类型、导出材料或恢复发布入口。
4. 因此 #329 可以完整实现运行期侧账，但不能在没有恢复架构设计和文件所有权的情况下宣称 4170 持久化 / 恢复完成。

## 3. 真实依赖与并发

具名前置为 `#329 / DQ-221 / DD-05F0 v0.2` 的实际主线结果。只有侧账记录类型、格式版本、访问器、参与者、生命周期和运行期拥有者进入 `origin/main` 后，才能确定导出 / 恢复边界和文件所有权；预期类型不得作为当前事实。

本切片不阻断 `#330 / DD-05F` 对 #329 运行期批次接口的消费。#330 解门只证明运行期接口可消费，不证明本切片或 4170 恢复能力完成。

## 4. 设计输出

依赖解除后，由设计窗口形成并互相绑定：

```text
4170 快照 / 恢复流程图 Markdown
与 Markdown Mermaid 主体一致的 HTML
有效详细设计中的 DD-05F0R 专节或独立详细设计
函数 / 结构清单与知识图谱
独立代码实施计划、允许 / 禁止文件、验证矩阵和任务队列登记
```

设计至少裁决：

```text
侧账快照格式标识、非零格式版本和大小 / 数量上限
完整记录字段、句柄版本和有序项目编码
导出时与四仓权威冻结材料的一致性边界
恢复候选隔离、未知格式、悬空句柄、缺项、重复和顺序缺口拒绝
恢复候选与主信息 / 节点 / 关系 / 索引 / 特征值原始材料的完整互证
恢复发布的唯一写入权、参与者、最后发布和失败清理
旧记录不可原地覆盖、当前值不由侧账恢复材料单独裁决
生产启动是否消费恢复候选，以及未授权时的明确拒绝
```

## 5. 机械阶段

### S0 实际接口复核

只读核对 #329 主线提交、侧账类型与拥有者、当前仓库导出 / 恢复拒绝底座、生产运行期边界、工程登记和全部相关调用点。返回 PASS 或具名 DRIFT；不修改代码、不构建、不运行。

### S1 设计与所有权裁决

形成流程图、详细设计和函数 / 结构知识图谱，明确是否扩展现有快照服务、建立特征领域专用快照模块或拆分导出与恢复参与者；不得把领域侧账塞入全局纯工具或让 SQL / 文件直接恢复运行期事实。

### S2 施工计划与登记

根据实际接口生成独立代码计划，列出新旧文件分类、唯一模块所有者、格式上限、拒绝矩阵、恢复发布边界、Debug / Release 验证、故障注入和完成声明；验证、提交并按设计执行交互规范及时派发。

## 6. 当前验证与完成边界

本设计治理切片当前至少验证：

```powershell
git diff --check
python .\tools\check_specs.py --strict
```

当前只可声明“4170 快照 / 恢复缺口已经具名登记并获得后继设计路由”。不得声明快照格式已经冻结、恢复入口已经建立、数据库可以恢复运行期事实、代码已经实施或 4170 已完整接通。
