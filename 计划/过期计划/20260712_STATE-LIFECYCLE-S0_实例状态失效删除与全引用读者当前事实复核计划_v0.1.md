# STATE-LIFECYCLE-S0 实例状态失效删除与全引用读者当前事实复核计划 v0.1

日期：2026-07-12

队列编号：#255 / 设计队列：DQ-147

状态：JY-498 移出迁移路线 / 独立能力积压 / #322 后按真实闭环读者重新生成 S0 / 当前版本不得执行

## 1. 目标与替代依据

扫描实例状态的写入、读取、动态前后状态、二次特征、因果候选、任务结果、需求结算、概念支持、快照 / 恢复和删除事务引用，决定后续失效 / 删除是否可形成新施工三件套。本计划不改变机器流程，故新流程图不适用；替代依据为 `规范/详细设计/实例状态生命周期治理详细设计.md`、#217-#220 事务设计、恢复设计和 #232-#234 正式产物。

关联 `MIG-03`、`MIG-12`、`MIG-15`、`MIG-18`；旧证据 FS-02 / FS-07 / FS-09。S0 不计旧迁移完成度。

## 2. 门控

当前删除写入口未实现。等待 #220 安全删除事务、#230 防复活恢复验收、#234 动态因果候选读取全部形成实际接口。依赖未满足保持门控；任何预期读者缺失都不得按计划猜测。

## 3. 扫描产物

状态身份 / 生命周期 / 墓碑 / 时间戳矩阵；所有直接和派生引用读者矩阵；失效与物理删除所有者；事务写集、锁序、快照段、恢复防复活、候选当前性和非成功二分；后续流程图 / 详细设计 / 计划是否成立结论。

## 4. 允许与禁止

只读代码、工程、规范、计划和实施记录；只写本计划归档、扫描记录和共享治理。禁止 C++ / 工程 / 规范 / 流程图修改，禁止构建运行，禁止实现删除、墓碑或级联解绑。

## 5. 验收

A01-A12 覆盖所有状态创建 / 读取 / 引用 / 恢复调用点，区分逻辑失效、物理删除和历史审计，给出后续三件套成立 / 漂移 / 禁止结论。严格规范、diff、提交推送。

## 6. 边界

只证明事实扫描完成，不证明实例状态可删除或生命周期治理实现。
