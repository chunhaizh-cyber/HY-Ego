# 节点直接身份与领域类型化持久结构代码修订总计划 v0.2

编号：NODE-TYPED-MIGRATION

日期：2026-07-22

版本：v0.2

路由：`#338 / DQ-230`

设计状态：终审 PASS

## 1. 目标与本次升版裁决

机器逻辑目标保持 v0.1 不变：把当前代码迁移为节点直接稳定身份、正式关系拓扑、领域类型化记录、可重建索引 / 组合投影，以及隔离恢复候选完成后的运行期上下文首次发布。

v0.2 只修订施工组织：不再等待前一个计划实现、集成和验收完成后才制定或开发下一个计划。后继消费条件先冻结成“预冻结待实现接口合同”，由具名提供计划实现；消费计划可以在同一计划合同基线上直接形成候选。预冻结合同不是当前代码事实，真实提供者接线、完整构建、运行和能力验收统一在最终汇合阶段完成。

## 2. 权威依据与设计材料

- 正式规范：0050、0500、4010—4070、4110、4170、4210、4220、4330、5160、5230、5300、5340、7130、7140。
- 六包现状 / 施工流程图、详细设计和函数结构知识图谱继续生效；本次不改变其机器逻辑，只把原“实现完成依赖”改为接口合同和最终集成依赖。
- `项目记忆/设计记录/20260722_NODE-TYPED-01_删除通用附属信息与自我持久结构规范映射记录.md`。
- 本计划 v0.2 以及 NT-P2 / P3 / P4 v0.2 子计划和 #340—#352 v0.2 叶子合同。

旧代码、v0.1 计划和当前不存在的目标模块只作差距证据。不得从预期模块名反推当前接口已经实现。

## 3. 已验证当前事实

1. #339 / NT-P1 v0.1 已独立派发并执行；本次不修改其不可变合同、worktree、分支或十二文件所有权。
2. 当前 main 仍没有 #339—#352 目标的新域模块；#340—#352 v0.1 把消费接口错误登记为逐计划完成门控。
3. #340—#351 的业务候选文件可以按计划隔离；造成广泛串行的共同文件是 `海中鱼巣.vcxproj`、`海中鱼巣.vcxproj.filters` 和 `海中鱼巣/自检.运行器.ixx`。
4. #352 还拥有入口、默认装配、启动、旧物理结构和最终删除集；这些动作必须与真实全量结果在最终切换提交内收口。
5. 现行代码影响面仍按 11 个数据操作、17 个现代服务和 14 份旧兼容服务头重扫；该数量只用于建立候选删除清单，不替代最终零消费者扫描。

## 4. 合同并行模型

### 4.1 合同最低字段

每项跨计划接口必须在提供者和消费者计划中用同一语义冻结：

```text
合同编号和版本
提供计划、消费计划
模块 / 文件身份
公开 DTO、规格、结果和值式材料
完整签名或等价行为
ABI / 规则版本
输入输出所有权与生命周期
逻辑内返回与内部逻辑错误
幂等、事务、撤销和可见性边界（适用时）
最终真实接线验证
漂移时退回的唯一接口所有者和受影响消费者
```

提供者拥有实现权，消费者只读消费。消费任务可以 import 计划中尚不存在的提供者模块并形成候选，但不得在本任务内补造同名模块、复制实现或修改提供者文件。

P2 并行批次的共同合同基线固定为：

```text
共同合同基线：NT-P2-CONTRACT/v0.2
唯一合同正文：计划/20260722_NODE-TYPED-MIGRATION_NT-P2_领域载荷与自我投影迁移子计划_v0.2.md 第 3 节
合同组：NT-C1/v0.2、NT-C2/v0.2
合同性质：预冻结待实现
提供范围：#339 提供 NT-C1/v0.2；#340—#342 分别提供 NT-C2/v0.2 的具名条目
消费范围：#340—#351 按各叶子登记的条目只读消费
最终接线：#352 逐模块、DTO、签名、ABI、结果分类和生命周期核对真实提供者
```

本总计划只登记合同基线、所有者和路由，不复制 P2 合同正文。#340—#342 叶子也只能引用上述唯一正文并登记自身提供 / 消费条目；同名 DTO、枚举、签名或 ABI 若与唯一正文不同，以合同漂移退回，不得在叶子或执行窗口重新定义。

### 4.2 合同分组

| 合同组 | 提供者 | 主要消费者 | 冻结主题 |
| --- | --- | --- | --- |
| NT-C1/v0.2 | #339 | #340—#351 | 节点直接身份、关系 0—23、索引、会话 / 执行器、参与者、冻结和跨域拒绝；P2 唯一定义见 NT-P2 v0.2 第 3 节 |
| NT-C2/v0.2 | #340—#342 | #341—#349 | 特征值归属 / 槽位组、主体状态 / 动态事实组、自我 / 内部世界 / 成员投影；P2 唯一定义见 NT-P2 v0.2 第 3 节 |
| NT-C3 | #343—#348 | #346—#352 | 系统角色、语素 / 因果 / 概念、需求任务方法用途、退役、请求适配、隔离装配候选 |
| NT-C4 | #349—#351 | #350—#352 | 快照段 ABI、规范化编码、隔离装载、候选验证、旧格式迁移 / 拒绝 |
| NT-C5 | #352 | 最终运行期 | 唯一工程登记、统一自检、全新 / 恢复互斥路由、默认切换、物理退役和首次发布 |

## 5. 开发并发与最终集成

开发候选批次固定为：

```text
#339 继续独立执行，不受本次升版影响

合同计划 v0.2 发布
-> #340—#351 可分别从同一治理基线建立任务 worktree
-> 各计划只形成自身实现候选并执行候选级验证
-> 不等待提供者实现完成，不修改共享工程 / 运行器

#352 最终汇合
-> 按固定顺序汇集 #339—#351
-> 唯一完成工程、统一运行器、入口、默认装配和删除集
-> 完整构建、运行、恢复、切换和覆盖审计
```

固定 Git 汇集顺序为：

```text
#339 -> #340 -> #341 -> #342
-> #343 -> #344 -> #345 -> #346 -> #347 -> #348
-> #349 -> #350 -> #351 -> #352
```

该顺序只用于确定汇集和根因定位，不是 #340—#351 的开发启动顺序。

## 6. 文件与结构所有权

1. #340—#351 各自只拥有 v0.2 叶子列出的生产 / 工具模块、专项自检和专属实施记录。
2. #348 只形成独立隔离装配候选与新域聚合自检候选，不写中央工程和统一运行器。
3. `海中鱼巣.vcxproj`、`海中鱼巣.vcxproj.filters`、`海中鱼巣/自检.运行器.ixx` 在新并行批次中只由 #352 拥有。
4. `入口.cpp`、默认装配、启动、运行期宿主、旧服务调用点、旧仓库 / 句柄 / 侧表 / 快照文件和最终工程删除项只由 #352 拥有。
5. #339 现有共享文件所有权不追溯修改；#352 只有从已含 #339 正式结果的最终集成基线执行，避免同文件并行写冲突。
6. v0.2 计划文件、索引、队列、台账、工作树登记和设计记录由设计窗口统一串行收口；执行任务不得修改。

## 7. 子计划与叶子路由

| 子计划 | 叶子 | 开发启动依赖 | 完成边界 |
| --- | --- | --- | --- |
| NT-P1 v0.1 | #339 | 保持原合同 | 实际底座任务分支与独立集成结果 |
| NT-P2 v0.2 | #340—#342 | 无未合同化实现依赖 | 三个并行领域候选，待最终真实接线 |
| NT-P3 v0.2 | #343—#348 | 无未合同化实现依赖 | 六个并行服务 / 退役 / 适配 / 装配候选 |
| NT-P4 v0.2 | #349—#351 | 无未合同化实现依赖 | 冻结、恢复和旧格式三个并行候选 |
| NT-P4-CUTOVER v0.2 | #352 | 必须消费 #339—#351 固定候选和最终删除集 | 唯一最终接线、切换、物理退役和全仓验收 |

计划路径由 `计划/计划索引.md` 登记。v0.1 计划保留为历史不可变合同，不再提供新派发许可。

## 8. 分级验证

### 8.1 #340—#351 候选级验证

- 冻结合同编号、版本、提供者 / 消费者和签名逐项一致；
- 实际差异只含本计划独占文件和专属记录；
- 禁止 import、旧结构、跨域访问、接口复制和共享文件写入静态扫描通过；
- 固定 DTO / 字节夹具 / 拒绝矩阵由叶子计划要求时完成；
- `git diff --check`、暂存白名单和提交后 clean 通过；
- 明确不运行项目 Debug / Release 构建和完整程序，不宣称编译或能力完成。

### 8.2 #352 最终验证

- 全部候选按固定顺序汇集，接口实际签名与合同逐项相等；
- 唯一工程登记、统一自检运行器和默认装配接线完整；
- Debug / Release x64 当轮 Rebuild 和两配置完整自检；
- 跨计划事务、故障撤销、关系 / 领域记录 / 历史 / 4170 全量互证；
- 进程甲冻结退出、进程乙仅恢复启动、规范化再冻结相等；
- 旧格式逐版本成功 / 拒绝、恢复失败宿主不变；
- 旧生产符号、调用点、工程项和物理结构零消费者 / 零命中；
- strict、diff、最终暂存白名单和独立覆盖审计。

完整验证后置只改变验证时点，不减少验收项目。任一合同漂移先退回提供者和受影响消费者；不得由 #352 临时发明接口消冲突。

## 9. 派发与状态规则

1. #340—#351 v0.2 发布后登记为无未合同化依赖的独立任务；设计窗口不等待 #339 或相邻计划执行完成才建立后续计划与执行身份。
2. S0 核对的是同一 v0.2 计划 blob、合同版本、所有权、共同治理基线和目标文件不存在 / 无冲突；不得因为提供者实现尚未出现在代码基线而单独返回 DRIFT。
3. 每个任务分支成功最多声明“按预冻结合同形成实现候选，分支完成待集成”；不声明项目已编译或业务能力通过。
4. #352 保留真实门控：必须从已含 #339 共享文件结果且可汇集 #340—#351 固定候选的基线执行，并拥有最终共享文件和删除集唯一所有权。
5. 派发、S0、状态和 worktree 仍遵守任务状态台账、交互规范和多工作树规范，不以聊天或本计划文本代替已发生事实。

## 10. 完成声明边界

本计划 v0.2 发布只证明接口合同驱动的并发施工路线、所有权和分级验证已经定义。#340—#351 候选完成不证明编译或接通；只有 #352 集成发布、独立覆盖审计并由设计窗口接受后，才可按正式证据声明节点直接新域成为唯一默认、当前格式完整恢复接通和旧通用附属结构物理退役。
