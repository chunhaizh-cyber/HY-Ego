# DATA-L2-SCENE-STATE-USE-BINDING-ABI-SKELETON 场景状态使用绑定ABI骨架代码实施计划

日期：20260909
版本：v0.2
状态：可执行候选
起点：`51991a4691c97c15828e5c22e51d3df9260bca74`

## 1. 目标

按4201/4203实现C/E/S状态使用绑定最终ABI及显式未实现骨架。完成后只证明DTO、守卫、两个窄接口和安全拒绝骨架可编译；不证明合法C/E身份生产、B事实CRUD、动态依赖满足、完整场景七字段、普通应用装配或业务运行完成。

## 2. 允许文件

生产文件：

1. `海中鱼巣/领域/合同.场景状态使用绑定.ixx`（新增）；
2. `海中鱼巣/领域/数据服务.场景类.ixx`；
3. `海中鱼巣/领域/数据服务.存在类.ixx`；
4. `海中鱼巣.vcxproj`；
5. `海中鱼巣.vcxproj.filters`。

记录文件：

6. `施工记录/20260909_DATA-L2-SCENE-STATE-USE-BINDING-ABI-SKELETON_场景状态使用绑定ABI骨架施工记录_v0.1.md`；
7. `验证记录/20260909_DATA-L2-SCENE-STATE-USE-BINDING-ABI-SKELETON_场景状态使用绑定ABI骨架验证记录_v0.1.md`。

允许在 `D:/TEMP/海中鱼巣/DATA-L2-SCENE-STATE-USE-BINDING/**` 建立隔离验证材料。禁止修改状态类、动态类、动态概念类、L1、普通应用、规则、规范、详细设计、计划索引及全部其它WIP。

## 3. 不变合同

- 不恢复P，不给状态增加C/E或其它字段；
- B是场景owner普通节点，不新增节点族或owner；
- 一份B恰有同一成员类型的三边，角色1=C、2=E、3=S；
- 场景类不直接依赖完整存在类，使用存在身份窄接口；
- 动态后继只消费绑定窄接口，不反向持有完整场景类；
- 旧散装关系零迁移、零解析、零双写；
- 不实现完整场景七字段或场景动态组织。

## 4. 目标接口

严格实现4203第3节全部DTO、成功谓词和状态数值。合同模块导出 `存在结构身份只读提供者` 与 `状态使用绑定只读提供者`。存在类只新增第一个窄接口的 `未实现` 骨架；场景类只实现下列最终签名的 `未实现` 骨架：

- 创建、当前读、历史读状态使用绑定；
- 按C、E、S三类当前反查；
- 退出状态使用绑定；
- 第二个窄接口。

## 5. 实施顺序

1. S0重新核对HEAD、计划blob、占用、index和三个生产源码WIP；存在类只做逐行窄增量，禁止覆盖。场景类空WIP按本计划重建本片范围。
2. 新建合同模块及值式守卫，不含事实写入。
3. 在存在类以窄增量实现两个身份读取函数的 `未实现` 骨架；不得读取旧存在结构或修改其它业务逻辑。
4. 在场景类实现构造、绑定检查和全部B操作最终签名；除纯绑定检查外，所有操作固定返回 `未实现`、零代次、空载荷，不读取任何provider或L1。
5. 更新工程登记；不修改普通应用装配。
6. 为隔离当前已被清空、但仍被既有代码 import 的外部模块，允许仅在 `D:/TEMP/海中鱼巣/DATA-L2-SCENE-STATE-USE-BINDING/**` 建立与当前被消费签名一致的编译替身。替身遵守4201零访问规则且不得写入生产源码或工程登记；它只服务于本片独立编译。
7. 建立专属施工与验证记录，包含函数级代码知识变更清单。
8. 执行验证、精确暂存、提交并普通推送；只发布本计划文件。

任一步发现必须改变4203字段、状态数值、owner、事务、公开函数或允许文件，停止并退回计划支撑；不得临场兼容。

## 6. 验证

最低要求：

- `git diff --check` 与 `git diff --cached --check`；
- 新合同、存在增量、场景绑定模块的Debug/Release隔离编译；
- 全部骨架入口返回 `未实现`，合同版本1、截止0、无变更、optional/vector载荷为空且 `成功()==false`；
- L1写、L1读、provider、幂等账、索引、时钟和日志调用计数均为0；
- import闭包不包含动态类或完整存在类反向调用；
- 合法首次/重复、B写读退、反查、恢复和动态消费均记录为 `NOT_IMPLEMENTED/NOT_RUN`。

若根工程读取当前空动态模块或异主WIP而失败，必须与目标模块结果分账；目标模块两配置未形成IFC/OBJ不得声明完成。运行、数据库重启、故障注入和正式集成验收未执行时明确记为 `NOT_RUN`。

## 7. 完成声明

完成只表示4203最终ABI及未实现骨架已经发布并可安全编译。场景C与存在E的合法族身份生产、4230当前/历史确认、B事实真实CRUD、恢复和动态消费仍未实现；动态类不得将本结果视为依赖满足。后继必须先闭合C/E身份生产，再建立真实绑定提供者计划；不得从本计划直接修改动态类或预先实现动态概念。
