# 旧状态动态特征比较服务 DTO 闭包删除代码实施切片 v0.1

日期：2026-08-11

计划身份：`L2-STRUCTURE-CRUD-REBUILD-D3-LEGACY-STATE-DYNAMIC-FEATURE-COMPARISON-CLOSURE-REMOVAL`

状态：创建侧验证后登记

## 1. 目标与完成条件

作为 L2 结构 CRUD 重建的第三份代码删除叶，物理删除旧 `L1状态`、旧 `L1动态` 和旧特征比较的三个服务模块及各自唯一 DTO 头，解除后续旧特征、世界、场景和存在服务删除前仍编译登记的状态 / 动态 / 比较业务合同残留。

本叶只删除旧代码和对应工程登记，不实现新 L2，不迁移函数，不建立兼容壳，也不删除下一叶负责的旧特征定义 / 实例特征或旧世界 / 场景 / 存在模块。

完成条件固定为：

1. 六个目标源码路径及其工程 / filters 登记全部退出；
2. 三个模块身份、三个服务类型和六文件公开 DTO / 函数身份在现行生产源码中归零；
3. 集合外生产源码无需修改，Debug / Release x64 继续完整编译链接；
4. 专属施工与验证记录准确保存实际结果和未覆盖范围。

## 2. 正式依据与当前事实

正式依据：0050、4015、4030、4190、4200、0600、CODE-FILE-01，以及：

- `规范/详细设计/20260810_L2-STRUCTURE-CRUD-REBUILD_旧链清理与六类结构CRUD分步重建详细设计_v0.1.md`
- `流程图/20260810_L2-STRUCTURE-CRUD-REBUILD_旧链清理与六类结构CRUD分步路线图_v0.1.md`
- `计划/过期计划/20260811_L2-STRUCTURE-CRUD-REBUILD-D2-LEGACY-CAUSAL-DYNAMIC-PATTERN-CLOSURE-REMOVAL_旧因果动态模式算法材料闭包删除代码实施切片_v0.1.md`

创建侧只读事实基线为 `fee54dca6ff8eeffcaa3a5dbda98a27f3dd32ba5`，该提交同时位于 `main` 与 `origin/main`：

- D2 已在该结果提交中删除五个旧动态 / 因果模式模块及对应工程登记；其六文件删除、两份专属记录和共享工程文件变更均未改变本叶六个源码 blob，也未建立本叶集合外消费者；
- 当前正式启动、装配和集合外生产源码均不 import `服务.L1状态`、`服务.L1动态` 或 `服务.特征比较`，也不构造 `L1状态服务`、`L1动态服务` 或 `特征比较服务`；
- 每个目标 DTO 头只由其同对服务模块通过 `export { #include ... }` 唯一公开：

```text
L1状态.数据.h      <- 服务.L1状态.ixx
L1动态.数据.h      <- 服务.L1动态.ixx
特征比较.数据.h    <- 服务.特征比较.ixx
```

- 三个服务只向下消费当前仍保留的提供者：新版 `L1事实基座`，以及状态 / 特征比较使用的旧实例特征、特征定义和实际存在公开面；删除本叶消费者不要求修改这些提供者；
- 六个冻结源码 blob 为：
  - `L1状态.数据.h`：`14815550e13aca9351b91288de8127be613cd192`
  - `服务.L1状态.ixx`：`df5b0c70aa9cb461d1d12d9824bece6a916e782a`
  - `L1动态.数据.h`：`0a0e4767f0934f84a2b3235476aa0ae0a006bc7d`
  - `服务.L1动态.ixx`：`31c7e24f388b2c0dfe6739da25a86f7f95a2e80c`
  - `特征比较.数据.h`：`90b920af652eabe1987475471336b48aa9b8aece`
  - `服务.特征比较.ixx`：`a0e57ae19b8a7ac70d8a3faa4d901e37239f52f9`
- `海中鱼巣.vcxproj` 对三个服务各有一项 `ClCompile`、对三个 DTO 头各有一项 `ClInclude`；filters 对六文件各有一个三行块；没有其它工程登记；
- 六文件当前无未提交差异；`流程图/主程序可达调用关系/**`、`流程图/代码流程图/**`、`计划/20260803_后续计划持续制定共识与路线输入.md` 和 `.codex-build/` 是异主现场，本叶原地保留且不得暂存。

复用裁决：

- `服务.L1状态` 除结构登记外还包含实际存在前置、首态形成和旧状态事实解释；
- `服务.L1动态` 与 DTO 固定旧动态登记形状；
- `服务.特征比较` 与 DTO 固定目标判断 / 状态迁移比较合同、算法族和角色解释；
- 这些旧合同不是 G1 尚待冻结的新 L2 调用方提交式纯结构 CRUD，且当前生产零消费者，因此整对物理删除，不复制函数体、DTO、枚举、缓存或幂等账，不以名称相近迁入新服务。

## 3. 具名依赖与状态门禁

D3 不消费 D2 的模块、DTO 或算法结果，D2 不提供 D3 所需能力；两者不存在接口或机器语义依赖。

但 D2 与 D3 都必须修改唯一共享的 `海中鱼巣.vcxproj` 和 `海中鱼巣.vcxproj.filters`，且项目同一时刻只有一个活动代码计划段和一个 Git 发布租约，因此曾存在不可并发的工程所有权 / 发布先后依赖。该门禁已经由 D2 结果提交 `fee54dca6ff8eeffcaa3a5dbda98a27f3dd32ba5` 满足：

```text
D2 完成代码、验证、提交和普通推送
-> D2 结果提交已进入 main 与 origin/main
-> D2 活动代码段与发布租约已经释放
-> D3 已从该结果 HEAD 复核六源码 blob、外部消费者和工程登记
-> 事实一致，D3 可以正式发布并登记为可执行
```

D3 当前无未满足的具名计划依赖。计划发布并登记为 `可执行` 后仍必须由执行智能体重新 S0；计划路径存在、创建侧检查通过或索引状态都不证明代码已经实施。若执行 S0 发现本叶目标源码、集合外消费者、工程形状或正式路线再次漂移，停止并回到计划支撑，不机械沿用本基线。

## 4. 精确允许范围

删除：

- `海中鱼巣/领域/L1状态.数据.h`
- `海中鱼巣/领域/服务.L1状态.ixx`
- `海中鱼巣/领域/L1动态.数据.h`
- `海中鱼巣/领域/服务.L1动态.ixx`
- `海中鱼巣/领域/特征比较.数据.h`
- `海中鱼巣/领域/服务.特征比较.ixx`

修改：

- `海中鱼巣.vcxproj`
- `海中鱼巣.vcxproj.filters`

新增专属记录：

- `施工记录/20260811_L2-STRUCTURE-CRUD-REBUILD-D3-LEGACY-STATE-DYNAMIC-FEATURE-COMPARISON-CLOSURE-REMOVAL_旧状态动态特征比较服务DTO闭包删除施工记录.md`
- `验证记录/20260811_L2-STRUCTURE-CRUD-REBUILD-D3-LEGACY-STATE-DYNAMIC-FEATURE-COMPARISON-CLOSURE-REMOVAL_旧状态动态特征比较服务DTO闭包删除验证记录.md`

## 5. 明确禁止与排除

禁止修改或删除：

- D2 的五个目标模块、施工 / 验证记录或正在形成的结果现场；
- 新版 L1 及其公开 DTO、服务、仓库和工程登记；
- `服务.L1特征定义.ixx`、`L1特征定义.数据.h`、`服务.L1实例特征.ixx`、`L1实例特征.数据.h`；
- 世界登记、场景、存在、原子发布和组合服务及其 DTO；
- 安全因果分层、任务、方法、启动、装配或上层业务模块；
- 规范、详细设计、路线图、计划索引、流程图维护专属目录及任何异主 WIP。

本叶不重命名、不搬移、不新增代码文件或函数，不改变函数可见性，不改变机器合同，不新建全局辅助函数。因只删除完整模块和专属 DTO，CODE-FILE-01 的新增 / 直接修改函数诊断责任分类不适用；不得借编译错误修改集合外函数。

## 6. 实施步骤与失败收口

1. 从 D2 已发布结果 HEAD 完成 S0，复核计划精确 blob、六源码 blob、D2 退出事实、Git index、异主 WIP、活动代码段和计划索引状态。
2. 以模块身份、头路径、服务类型和公开 DTO / 函数名扫描现行生产源码；确认全部集合外命中为零。
3. 物理删除六个目标源码。
4. 从 `海中鱼巣.vcxproj` 精确删除三个 `ClCompile` 和三个 `ClInclude`，从 filters 精确删除六个对应块；不调整其它项目顺序、筛选器或配置。
5. 扫描下列身份在现行生产源码和工程登记中归零：
   - `海中鱼巣.领域.服务.L1状态`、`L1状态.数据.h`、`L1状态服务`
   - `海中鱼巣.领域.服务.L1动态`、`L1动态.数据.h`、`L1动态服务`
   - `海中鱼巣.领域.服务.特征比较`、`特征比较.数据.h`、`特征比较服务`
   - 六文件唯一声明的旧状态 / 动态 / 比较请求、结果、登记和合同身份
6. 记录实际删除、归零、工程完整性、构建结果和未覆盖范围。

不得通过恢复旧模块、加入兼容声明、删除集合外消费者或修改集合外源码来处理失败。发现真实集合外消费者、冻结 blob 或工程形状漂移时，保留现场、停止本叶并具名返回计划支撑。

## 7. 验证合同

本叶没有新建或实现公开接口，用户要求的“接口实现后以生产外自定义参数轻量验证”在本删除叶不适用；不得为删除叶制造参数驱动、生产内自检或业务剧本。

实际验证固定为：

1. 六个目标路径逐项不存在；
2. 三个模块身份、服务类型、专属 DTO / 函数和十二项工程 / filters 登记在现行生产范围归零；
3. 禁止文件没有进入实际差异；
4. `海中鱼巣.vcxproj` 和 filters 均能按严格 UTF-8 解析为有效 XML，四配置 `/utf-8` 保持四项；
5. `MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /verbosity:minimal` 成功；
6. `MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Release /p:Platform=x64 /verbosity:minimal` 成功；
7. `git diff --check` 通过；
8. `python .\tools\check_specs.py --strict` 通过；
9. 提交前 cached 范围只包含八个代码 / 工程文件和两份专属记录。

明确不运行程序、CRUD、生产模式、自检、功能测试、参数黑盒或集成验收。

## 8. 完成声明边界

本叶完成只证明旧状态、动态和特征比较的三个服务 / DTO 对及工程登记物理退出，并且剩余工程继续编译链接。

本叶不证明：

- D2、D4、D5 或全部旧 L2 已经删除；
- 新 L2 场景、存在、特征、状态、动态或因果 CRUD 已设计或实现；
- 新版 L1 或任何数据结构服务功能正确；
- 生产运行期接线、业务真实性、并发、性能、崩溃、重启或跨进程恢复成立。
