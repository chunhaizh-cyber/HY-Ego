# REAL-GENERATION-LOOP-S2 任务管理与工作线程真实执行代码实施切片 v0.1

日期：2026-07-19

执行队列：#316
设计队列：DQ-208
设计裁决：JY-498
计划阶段：Debug 最终回归 1110
状态：依赖门控待执行 / 前置 #315 独立集成并归档

## 1. 目标与依据

让任务管理线程和任务工作线程在真实后台线程中自然完成确认请求出队、同租约 B0 执行和强类型回执发布，禁止最终验收由主测试线程直接调用受控消费入口代替。

依据为 #315 全部三件套、`规范/详细设计/根需求任务筹办执行与方法学习生产闭环详细设计.md` 第 5、7 节和任务执行调度三件套。

## 2. 已验证事实、假定接口与前置

当前两阶段队列和窄执行入口已实现，但两个线程体不做真实业务消费。假定 #315 导出完整冻结请求，现有任务执行适配器可由同租约构造。#315 未进入 main 时不得执行；执行前逐字段复核线程状态、停止锁存、队列所有权、执行适配器和回执 DTO。

前置计划：#315 必须已经独立集成、设计归档，且正式产物位于 `origin/main`。预期正式产物：生产任务执行宿主、管理线程自然出队、工作线程唯一执行调用、强类型回执和阶段 1110 报告。

执行前复核命令至少包含：

```powershell
rg -n "请求任务执行|确认任务执行请求|受控消费一条任务执行请求|任务执行回执" 海中鱼巣/线程
rg -n "冻结任务执行请求|运行期业务操作组合器" 海中鱼巣/领域 海中鱼巣/线程
rg -n "登记自检.*1110" 海中鱼巣
```

接口漂移处理：任一字段、线程所有权、停止语义或调用方向不一致时，保持代码与 index clean，写本计划专属复核断点并退回设计；不得复制 #315 DTO、增加测试后门或扩大允许文件。

## 3. 允许文件

```text
海中鱼巣/线程/任务管理线程.ixx
海中鱼巣/线程/任务工作线程.ixx
海中鱼巣/线程/宿主.任务执行.ixx 新建
海中鱼巣/线程/自检.生产任务执行线程.ixx 新建
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
海中鱼巣/入口.cpp 仅阶段1110最小登记
实施记录/20260719_REAL-GENERATION-LOOP-S2_任务管理与工作线程真实执行代码实施_Codex断点清单.md 新建
```

## 4. 禁止文件

禁止修改 #315 生产路由、B0/领域/核心/仓库、任务回执协议、上行桥、自我线程、运行期上下文、中央治理和阶段1100。禁止轮询自旋、后台脱管线程、睡眠判成功、测试专用业务后门和线程直接写仓库。

## 5. 状态机

S0 复核后，S1 为管理线程增加条件等待和确认后自然出队；S2 让工作线程体调用唯一窄执行适配器并发布强类型回执；S3 建宿主与反向停止排空；S4 验证真实线程 ID、空队列、容量、重复、过期、停止中请求、并发请求和异常收口；S5 构建、两配置、20/20和任务分支收口。

## 6. 入口拒绝与失败收口

队列满、停止、重复、过期和当前性漂移逻辑内返回。已确认请求丢失、同请求多执行、成功执行无回执、回执与权威结果矛盾或停止遗失已接受请求必须追根因。

## 7. 验收

A01-A04：#315 冻结请求唯一输入、真实管理/工作线程自然消费、线程不写仓库且不成为动作来源。A05-A08：每请求至多一次执行/回执、过期零新事实、并发顺序和停止排空。A09-A12：1110 唯一、两配置0/0、Release隔离、Debug20/20、strict/diff/范围。

## 8. 验证

沿用 #315 命令，并增加静态扫描：最终生产自检不得直接调用 `受控消费一条任务执行请求`，只能由工作线程体调用。

## 9. 完成声明边界

只证明任务排队、后台执行和强类型回执形成；回执仍非权威，不证明任务结果、需求结算、O1或学习。
