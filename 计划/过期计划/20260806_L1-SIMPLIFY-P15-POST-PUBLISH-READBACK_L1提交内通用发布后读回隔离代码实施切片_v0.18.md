# L1-SIMPLIFY-P15 G7/G8 代码实施授权修订代码实施切片 v0.18

计划身份：`L1-SIMPLIFY-P15-POST-PUBLISH-READBACK`

版本：`v0.18`

创建侧基线：`e6065af4b2d6c39a6928e1e644b691b2848ad8c2`

替代：P15 v0.17；具名漂移：`DESIGN-DRIFT-P15-V017-PLAN-PROHIBITS-CODE-IMPLEMENTATION`

## 1. 目标、执行授权和禁止范围

本计划是可执行的 P15 代码实施计划。执行侧通过正式 S0 后，允许在本计划精确 24 文件白名单内施工 G1—G8 确定性编码、provider 前探测、G0 独立互证、L1 v2 提交、首次 DTO、恢复、发布后读回、工程/模块登记、自检和专属施工/验证记录。

禁止范围为白名单外文件、P0-FIX、路线、旧 WORLD-TREE、P63/P64 及其它异主 WIP；禁止范围不包括本计划白名单内的 P15 代码实现。不得把计划文档通过当作代码已实现。

## 2. 施工合同

保留 v0.17 正确的 G7/G8 真实写入口矩阵：

```text
T01 L1状态登记请求                         -> 0x15000601 -> L1状态登记结果
T02 实际存在I64首个基准状态发布请求          -> 0x15000602 -> 实际存在I64基准状态结果
M01 特征比较登记请求                         -> 0x15000801 -> 特征比较登记结果
M02 I64目标判断比较合同登记请求               -> 0x15000802 -> I64目标判断比较合同结果
M03 I64状态迁移比较合同登记请求               -> 0x15000803 -> I64状态迁移比较合同结果
```

字段顺序、嵌套 DTO、固定域前缀、公共原语、测试向量、同义/异义、恢复重算、G1—G6、G0 互证、P-L1/P-I/P-S、32/18/50、16/50 和 24 文件范围均以 v0.17 设计为施工输入，不得临场改机器语义。

## 3. 精确施工步骤

1. S0 重核正式 HEAD、实际接口、所有权和精确 24 文件白名单；不满足即停在设计支撑。
2. 在核心唯一公共原语基础上实现 G1—G8 领域摘要，保持 G7/G8 五个真实写入口 DTO 和固定标签。
3. 将所有生产/自检点接成“真实请求编码→provider 前探测→未找到后读取→独立 G0 摘要→核心互证→一次提交”，核心不得覆盖调用方摘要。
4. 更新首次结果 DTO、执行证据、幂等账、审计和恢复重算；同义 provider/提交为 `0/0`，异义/缺字段/错标签拒绝，双账或发布后读回矛盾隔离。
5. 仅在 24 文件内完成对应工程登记、自检和专属记录，保持 32/32 生产、18/18 自检、32→50、16/50。

## 4. 允许文件与验证

允许文件集合就是 v0.17 已冻结的 24 文件，且本计划允许其中的代码、工程/模块登记、自检和专属施工/验证记录变更；不新增文件、不带入异主 WIP。执行侧必须运行 Debug/Release 编译、完整自检、provider 前探测顺序、G7/G8 首次/同义/异义、G0 互证、恢复和发布后读回验证。

创建侧门禁为三文件 no-index diff check、`git diff --check`、`python .\tools\check_specs.py --strict`、UTF-8/NUL/尾随空白、G7/G8 DTO/标签/字段/结果矩阵，以及 32/18/50、16/50、24 文件机械计数。

## 5. 完成边界与索引门禁

最多声明 v0.18 已消除权限文字冲突并形成可施工的 P15 代码计划。发布后把 v0.17 标记为被 v0.18 替代/禁止施工并新增 v0.18 `待激活` 行；不得自行激活。代码能力、构建、自检、恢复和生产接线只有执行侧完成并回报后才可分别声明。
