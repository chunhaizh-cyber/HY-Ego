# L1-SIMPLIFY-P12-PRESTATE I64 相邻前状态选择代码实施切片 v0.2

计划身份：`L1-SIMPLIFY-P12-PRESTATE`

版本：`v0.2`

修订基线：`cabe1794ab36226b10b3cecd1d03c8598b855793`

具名漂移：`DESIGN-DRIFT-P12-ORDER370-OUTSIDE-WHITELIST`

## 1. 目标与完成条件

在原位 L1 状态服务实现 `选择实际存在I64相邻前状态`，按同主体、同实例槽和严格更早业务发生时间选择唯一最大项，经 P9 同一公开入口返回完整前状态；合法零候选返回 `无前状态`。v0.1 的函数、DTO、错误、重试和读回合同不变。

专项验证不新增顶层顺序：在既有 `领域/自检.L1状态.ixx` 的 `运行L1状态自检()` 中追加 P12 矩阵，由启动已有顺序 360 统一执行。不得修改启动文件或声称存在顺序 370。

## 2. 精确允许范围

```text
海中鱼巣/领域/L1状态.数据.h
海中鱼巣/领域/服务.L1状态.ixx
海中鱼巣/领域/自检.L1状态.ixx
施工记录/20260805_L1-SIMPLIFY-P12-PRESTATE_施工记录_v0.2.md
验证记录/20260805_L1-SIMPLIFY-P12-PRESTATE_验证记录_v0.2.md
```

禁止修改 `海中鱼巣/启动.应用程序.ixx`、工程/filters、P7/P8/P9、P10/P11/P34、旧状态动态栈、SQL、材料、恢复、其它计划和 WIP。

## 3. 待激活门禁

计划保持 `待激活`，直到 P9 正式结果与接口形状被接受、释放三个状态代码文件和既有顺序 360 的自检资源，并从新 HEAD 重做 S0。执行前必须确认启动仍只有既有顺序 360，P12 追加项已由状态自检报告承载；若自检组织、顶层顺序或白名单再次变化，先退回设计。

## 4. 机械施工

1. 按详细设计追加规则常量、DTO 和唯一 `const` 选择函数；不引入前状态、事实截止、算法、注册、动态、许可或事务输入。
2. 读取 G0，校验主体/实例槽，按关系19角色、状态资格和发生时间形成候选；严格早于拟发布时间，等时多状态冲突，唯一最大时间选择。
3. 由快照构造 P9 读取请求，互证身份/时间/截止，G0/P9/G1 漂移完整重试一次；非成功清空载荷。
4. 在 `运行L1状态自检()` 既有报告中追加 P12 专项矩阵；不修改启动登记，不新增 370。
5. 形成 v0.2 专属施工/验证记录。

## 5. 验证合同

- 精确五路径，Markdown 仅两份专属记录；cached diff check、working diff check、strict 通过；
- Debug/Release x64 串行 Rebuild、两配置完整自检、MSVC 模块扫描通过；
- 顺序 360 承载的状态自检报告覆盖零候选、单候选、多候选乱序、等时冲突、未来/等时排除、跨主体/实例槽、关系19损坏、P9坏成功、许可/资源/漂移及所有错误映射；
- 启动文件新增/修改为零，顶层顺序 370 命中为零；
- 无事实写入、旧句柄/对象版本、SQL、缓存或第二状态服务路径。

## 6. 完成声明边界

最多声明 L1 状态相邻前状态选择候选及顺序 360 统一自检矩阵完成；不声明 P9/P10/P11、第二状态、关系20、迁移动能、动态、装配、恢复、集成验收或治理闭环完成。
