# L1-SIMPLIFY-P12-PRESTATE I64 相邻前状态选择代码实施切片 v0.3

计划身份：`L1-SIMPLIFY-P12-PRESTATE`

版本：`v0.3`

修订基线：`06012409536b52b0d79ce66d34640cde507f22da`

具名漂移：`DESIGN-DRIFT-P12-CURRENT-ENDPOINT-EXIT-CONTRADICTION`

## 1. 目标与完成条件

在原位 L1 状态服务中，按同主体、同实例槽、严格更早业务发生时间选择唯一最大前状态，并经 P9 同一入口返回完整前状态。主体/实例槽端点必须分别判定从未存在、当前存在、后来退出；合法无候选仍返回 `无前状态`，端点从未存在返回 `未找到`。P12 不新增启动顺序，继续由顺序 360 统一自检。

## 2. 精确范围

```text
海中鱼巣/领域/L1状态.数据.h
海中鱼巣/领域/服务.L1状态.ixx
海中鱼巣/领域/自检.L1状态.ixx
施工记录/20260805_L1-SIMPLIFY-P12-PRESTATE_施工记录_v0.3.md
验证记录/20260805_L1-SIMPLIFY-P12-PRESTATE_验证记录_v0.3.md
```

禁止修改启动、工程/filters、P7/P8/P9、P10/P11/P34、旧状态动态栈、SQL、其它计划和 WIP。

## 3. 机械步骤

1. 在现有 DTO/函数合同上新增端点三态内部解析：当前成功且 G0 代次一致为当前存在；当前已退出且历史副本、类型、查询键、当前有效标志和创建/退出代次互证为后来退出；当前与历史均明确未找到为从未存在；其它情况内部不一致。
2. 只允许当前节点读取、历史事实读取和生命周期字段互证；不扫描审计、SQL、日志、缓存或容器历史。
3. 当前存在/后来退出端点均参与关系19候选；严格时间、同时间冲突、P9 互证和 G0/P9/G1 重试规则保持不变。
4. 在既有 `运行L1状态自检()` 中追加三态、生命周期损坏和映射矩阵，由启动顺序 360 执行；不得修改启动或新增 370。

## 4. 待激活与停工

版本保持 `待激活`，执行前须从新 HEAD 重做 S0，确认 P9 当前/历史接口、三个状态文件和顺序 360 仍匹配。若需要新增通用历史查询、第二状态发布、关系20、启动白名单或改变 P9 语义，停止并退回设计。

## 5. 验证合同

- 精确五路径；cached/working diff check、strict 通过；
- Debug/Release Rebuild、完整自检、MSVC 模块扫描通过；
- 顺序 360 的状态自检覆盖从未存在、当前存在、后来退出、退出见证缺失、当前/历史矛盾、创建/退出代次错误、等时冲突、P9坏成功、许可/资源/漂移和非成功空载荷；
- 启动文件新增/修改为零，顺序 370 命中为零；
- 无第二状态、关系20、通用历史、SQL、缓存或旧句柄路径。

## 6. 完成边界

最多声明端点三态解析与相邻前状态选择候选矩阵完成；不声明 P9/P10/P11、第二状态、关系20、迁移动能、动态、装配、恢复、集成验收或治理闭环完成。
