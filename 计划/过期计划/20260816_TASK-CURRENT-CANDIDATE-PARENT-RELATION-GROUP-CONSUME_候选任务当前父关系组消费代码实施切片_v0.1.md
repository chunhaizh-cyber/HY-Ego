# 候选任务当前父关系组消费代码实施切片 v0.1

日期：2026-08-16

身份：`TASK-CURRENT-CANDIDATE-PARENT-RELATION-GROUP-CONSUME`

状态：设计就绪；以计划索引登记状态为准

## 1. 目标

按[详细设计](../规范/详细设计/20260816_TASK-CURRENT-CANDIDATE-PARENT-RELATION-GROUP-CONSUME_候选任务当前父关系组消费详细设计_v0.1.md)实现 BIZ-L4-004-02-02-05 v0.1：扩展01—04同一 `任务业务服务`，互证02完整成功结果后只经 DATA-EXT-13 恰一次批量读取候选任务逐项可空唯一当前父关系，形成供父级预选唯一零父候选的完整不可变材料；本叶不自行选根或读取整树。

## 2. 前置与状态

本计划登记为 `待激活`。只有详细设计第 6 节全部门禁成立并由计划索引正式改为 `可执行`后才能施工。DATA-EXT-13 最终清单能力、02待激活设计或预冻结接口均不能替代真实结果与最终 ABI。

## 3. 允许与禁止范围

生产允许新增：

```text
海中鱼巣/领域/候选任务当前父关系组.数据.h
```

生产允许机械扩展：

```text
海中鱼巣/领域/服务.任务.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
```

允许新增专属永久记录：

```text
施工记录/20260816_TASK-CURRENT-CANDIDATE-PARENT-RELATION-GROUP-CONSUME_候选任务当前父关系组消费施工记录_v0.1.md
验证记录/20260816_TASK-CURRENT-CANDIDATE-PARENT-RELATION-GROUP-CONSUME_候选任务当前父关系组消费施工检查记录_v0.1.md
```

允许生产外专属专项三件套，命名必须含计划身份；结果发布后按正式治理独立清理。

禁止修改 DATA-L1—L3、需求服务、普通应用、父级当前任务树、01—04、正式规范、目标图、详细设计、计划和计划索引。禁止新增第二任务服务或“根候选”DATA快捷入口、直达L1/owner/仓库、逐任务/分页/补读、缓存、重试、G0重选、历史拼装、根资格/目标等价/完整拓扑/治理存在/阶段/完成/调度/方法/权限判断、静默去重/截断或生产验证seam。

## 4. 实施顺序

1. S0 核对计划精确 blob、索引状态、02最终 DTO、DATA-EXT-13 最终批量父关系 ABI、普通应用合法取得路径、生产文件和资源所有权。
2. 新增 BIZ 值式合同头，落实固定配置、请求、逐任务可空父关系项、状态和结果；全部字段中文命名。
3. 扩展同一 `任务业务服务`和同一 DATA const 借用，校验02完整结果、G0、运行代次、版本和两预算。
4. 从02材料提取稳定候选组，恰一次调用 DATA，精确映射失败并逐字段互证回显、集合、optional同形、端点、生命周期、版本和G0。
5. 异常收口内完成 checked累计、稳定排序和候选构造，最后无抛出交换；全部非成功零载荷。
6. 精确登记工程、专项和两份永久记录，完成矩阵后形成结果提交；不得修改计划索引。

## 5. 验证

- 坏配置、坏02结果、零G0/预算、空/重复/超量候选、运行代次/版本漂移全部零DATA调用；
- 零父、单父、多候选混合、不同候选共享父任务、父在候选外和输入乱序成功；只把同一候选多父判为重复父属并分账；
- DATA只调用一次，逐任务结果等长同序；任务未找到/退出、预算、漂移、许可、资源、未实现、未知状态映射准确；
- optional半结构、反向/错端点、自父、同一候选多父、关系身份或候选任务身份全组重复、错来源/生命周期/版本/G0、超预算零前缀拒绝；
- 静态证明零根选择、完整拓扑/治理存在、目标/来源内容、阶段/完成/调度/方法/权限、L1/owner/仓库/DATA写/缓存/retry/sleep；
- 根工程 x64 Debug/Release Rebuild，专项双配置，`python .\tools\check_specs.py --strict`，diff/XML/UTF-8/工程唯一登记和生产零专项痕迹。

真实资源耗尽、DATA私有损坏、跨进程恢复、父级根裁决、目标等价、完整当前任务树、任务创建/迁移/调度/完成和系统业务闭环保持 `NOT_RUN`。

## 6. 完成声明边界

本计划未来完成最多证明 `任务业务服务::读取候选任务当前父关系组`真实消费 DATA-EXT-13 并通过本叶矩阵。它不证明 DATA-EXT-13 整项、候选中存在唯一根、完整拓扑/治理存在、目标等价、当前任务树、任务创建/迁移/调度/完成或系统业务闭环完成。
