# L2-FEATURE-COMPARISON-REGISTRY-NEUTRAL-WRITE-MIGRATION 特征比较结构登记中性写入迁移代码实施切片 v0.1

日期：2026-08-08

状态：可执行

## 1. 目标与依据

按配套详细设计机械实施：把 `特征比较服务` 的第一结构登记根 M01 及其当前登记读取迁移到现有 L1 中性 CRUD。保持公开 DTO、构造、函数签名、稳定状态值和 `0x08` 幂等域不变；删除 M01 的 `0x15000801U`、G8 登记意图、旧幂等预探测、完整快照、legacy 提交和首次请求缓存；形成固定六节点中性写集并经同代次六节点当前读回返回登记。

- 详细设计：`规范/详细设计/20260808_L2-FEATURE-COMPARISON-REGISTRY-NEUTRAL-WRITE-MIGRATION_特征比较结构登记中性写入迁移详细设计_v0.1.md`
- 施工流程图：`流程图/20260808_L2-FEATURE-COMPARISON-REGISTRY-NEUTRAL-WRITE-MIGRATION_建立登记函数流程图_v0.1.md`
- 正式依据：0050、4015 v0.6、4030 v1.4、4040 v1.4、4050 v1.2、4180 v0.2、0600 v0.4。

设计基线：`33c87af8eb57fea41d85ea3bfc58fc8676ce7071`。目标 blob：数据头 `e7472460163fb6b377dc2eb00ddf1dba5a3d0f6b`，服务 `94bb5c42cd8c9babf119a6e98ec87bed9046f78c`。中性 DTO、服务和仓库顺序 provider blob 分别为 `d8f2c9ba052458fd9f22f6d6357695188d040c41`、`d3b13e6081a2b17a25beb85a19cd89cb6a81b6fa`、`8a1c6c7a17c7b0412501bc6b588df0f8bd8d2218`。

## 2. 实施前 S0

执行者必须从正式 main 重新核对：

1. `main == origin/main`，HEAD 不早于本计划设计提交与索引提交，index 为空；确认唯一代码执行计划段和 Git 发布租约。
2. 读取计划索引本行、计划 blob、配套详细设计和流程图；核对本计划状态、版本、白名单和所有者。
3. 读取两份目标生产文件和三个 provider 的当前 HEAD blob；若目标根、公开 DTO、中性写读 DTO、L1 幂等裁决顺序或排除函数与设计基线发生会改变实施的漂移，停止并具名退回计划支撑。
4. 精确记录异主 dirty，只冻结与本计划四路径重叠的冲突；不得 stash、reset、clean、覆盖或带入异主 WIP。
5. 确认 M01/M02/M03 仍独立，`0x15000802U`、`0x15000803U` 及其函数不在修改范围；确认现有三个中性接口足够，无 provider 计划依赖。

## 3. 写入白名单

生产代码：

1. `海中鱼巣/领域/特征比较.数据.h`
2. `海中鱼巣/领域/服务.特征比较.ixx`

专属记录：

3. `施工记录/20260808_L2-FEATURE-COMPARISON-REGISTRY-NEUTRAL-WRITE-MIGRATION_施工记录.md`
4. `验证记录/20260808_L2-FEATURE-COMPARISON-REGISTRY-NEUTRAL-WRITE-MIGRATION_构建与特征比较登记验证.md`

除此之外零写入。禁止修改核心 L1、M02/M03、运行比较、状态/动态/因果、启动、运行期上下文、工程/filters、自检、集成验收、详细设计、流程图、计划、计划索引和异主 WIP。

## 4. 机械施工步骤

1. 在数据头建立唯一 `特征比较登记幂等域=0x08` 常量；保持全部公开 DTO、枚举值、字段与默认值不变。若服务内常量迁出，只删除同义 `0x08` 定义，保留 `0x09`、`0x0E`。
2. 删除 M01 唯一 `形成G8特征比较登记意图凭证`、`登记请求相同` 和类成员 `登记请求_`；不得删除 M02/M03 共用 G8、legacy 映射、快照或合同 helper。
3. 增加 M01 专用中性幂等键、状态映射、六映射完整唯一、六节点形状、登记定位相同和共享精确读回 helper；函数名、变量名使用中文。
4. 原位改写 `建立登记`：请求校验后预读 P，但不以 `P != 请求.期望事实代次` 提前返回；形成固定 `6/0/0/0/0` 中性写集并恰一次调用 `提交中性写集`。
5. 成功/精确重复严格核对合同、键、首次代次、发布标志、重试边界和六映射。首次成功才要求 P 等于请求期望；精确重复允许 P 高于原期望。同键异义保持映射为 `入口拒绝`，不得新增公开状态。
6. 按六编码执行 `G0 + 六次读取中性当前节点 + G1`；核对回显、代次、未退出、空当前属性、节点形状和共同创建代次。权威读回完整且与已有定位不冲突后，才更新 `登记_` 并返回 `已比较`。
7. 改写 `读取当前登记()`：缓存只提供六编码定位；缺失返回 `未注册`，存在则复用同一权威 helper，不得直接返回缓存登记或旧事实截止代次。
8. 对两份生产文件做精确范围审查：M01 旧标签/意图/探测/完整快照/legacy 提交/首次请求缓存归零；`提交中性写集` 在本目标根恰一个物理调用点；M02/M03 与全部排除根语义不变。
9. 填写施工与验证记录，逐项写明实际差异、验证、未运行场景、剩余风险和声明边界。

## 5. 验证命令与门禁

至少执行：

```powershell
git diff --check -- '海中鱼巣/领域/特征比较.数据.h' '海中鱼巣/领域/服务.特征比较.ixx' '施工记录/20260808_L2-FEATURE-COMPARISON-REGISTRY-NEUTRAL-WRITE-MIGRATION_施工记录.md' '验证记录/20260808_L2-FEATURE-COMPARISON-REGISTRY-NEUTRAL-WRITE-MIGRATION_构建与特征比较登记验证.md'
python .\tools\check_specs.py --strict
```

随后使用项目正式命令完成 Debug/Release x64 完整重建和双配置模块扫描。不得新建专项生产自检、测试驱动或 L1 独立验收入口。

静态门禁至少证明：

- M01 固定写集恰为 `6/0/0/0/0`，键 2 为属性类型 `I64组`，其余五节点为普通；
- M01 恰一次 `提交中性写集`，且不受 `P != 请求.期望事实代次` 提前返回支配；
- 成功和精确重复均进入六编码 `G0/G1` 权威读回；
- `0x15000801U`、M01 G8 意图、旧探测、完整快照、legacy 提交和 `登记请求_` 在目标根归零；
- `0x15000802U`、`0x15000803U` 及 M02/M03 函数差异为零；目标外生产代码差异为零；
- 公开 DTO、状态数值、两个构造和 `0x08` 数值不变。

验证记录必须按详细设计 §9 覆盖或明确标注未运行。现有合法入口无法直接制造的精确重复、同键异义和竞争漂移场景可保留为静态合同证据，但不得宣称已运行或已集成验收。

## 6. 提交、推送与完成声明

只暂存四个白名单路径，复核 staged diff 后普通提交并普通推送 main；禁止带入异主 WIP。结果提交进入正式 main 后，把提交、验证事实和声明边界交回交互智能体，由交互侧核验并更新计划索引；执行者不得自行修改本计划状态或索引。

完成最多证明特征比较第一结构登记根的中性 CRUD 迁移代码候选和约定施工门禁。不得宣称 M02/M03、运行期比较、状态动态联合发布、真实消费者、集成验收、其它 legacy、核心 L1、状态/动态/因果或完整项目闭环已经完成。
