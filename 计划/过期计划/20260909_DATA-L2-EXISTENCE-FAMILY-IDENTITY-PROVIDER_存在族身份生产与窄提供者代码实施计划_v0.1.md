# DATA-L2-EXISTENCE-FAMILY-IDENTITY-PROVIDER 存在族身份生产与窄提供者代码实施计划

日期：20260909
版本：v0.1
状态：可执行
起点：`38a0eaa28eb97593d1158cf2b16cb30e5a25b882`

## 1. 目标

按4203 v0.4、4230 v0.46及同名v0.1详细设计，实现存在E族锚点/归属生产、E节点+归属同写、v2当前无载荷确认、历史完整见证、退出/恢复读回，并退役存在类旧状态/动态成员和空动态依赖。

本计划不写场景C或B，不实现动态，不修改正式规范。

## 2. 允许文件

生产文件：

1. `海中鱼巣/领域/合同.存在结构身份只读.ixx`（新增）；
2. `海中鱼巣/领域/合同.场景状态使用绑定.ixx`；
3. `海中鱼巣/领域/数据服务.存在类.ixx`；
4. `海中鱼巣.vcxproj`；
5. `海中鱼巣.vcxproj.filters`。

记录文件：

6. `施工记录/20260909_DATA-L2-EXISTENCE-FAMILY-IDENTITY-PROVIDER_存在族身份生产与窄提供者施工记录_v0.1.md`；
7. `验证记录/20260909_DATA-L2-EXISTENCE-FAMILY-IDENTITY-PROVIDER_存在族身份生产与窄提供者验证记录_v0.1.md`。

允许在 `D:/TEMP/海中鱼巣/DATA-L2-EXISTENCE-FAMILY-IDENTITY-PROVIDER/**` 建立隔离编译和探针材料。禁止修改L1、场景类、状态类、动态类、动态概念类、因果类、任务/方法类、普通应用、规则、规范、详细设计、计划索引和其它WIP。

## 3. 不变合同

- 不恢复P，不增加状态本体字段；
- 存在窄协议只采用4203版本2，无第二当前载荷、别名、重载或兼容开关；
- owner由外部建立并移动注入，存在服务只私有生产族锚点和族归属关系类型；
- 首次装配与恢复分别固定使用4015签发器建立owner/重新签发既有owner端口，4070恢复先于重签发，owner当前读回先于服务构造；
- E节点和唯一 `E -> 锚点、类型=族归属类型、角色1` 关系同写同代、同退同代；
- 普通存在DTO不增加族来源字段，来源只内部读回或由历史窄见证返回；
- legacy裸存在零重释、零补边、零扫描、零保码迁移；
- 存在类零状态/动态成员与依赖；子存在、已知特征、当前采用及任务/方法helper的现行ABI和语义保持；
- 正式HEAD动态概念类中的三处存在旧字段读取是已知后续删除集，本计划不得修改或恢复其异主清空WIP；
- 不创建C/B，不修改场景骨架，不实现动态或下游模型。

## 4. 实施顺序

1. 重新S0：核对精确起点、计划blob、索引、WIP、index、允许文件、4203/4230版本及调用点；把存在类blob相同的行尾M当作噪声保留，不用checkout/reset清理。
2. 新建无状态依赖的存在窄合同模块，逐字实现4203 v0.4协议v2和成功谓词。
3. 迁移绑定合同模块为export-import该窄合同，删除旧共用结果；保持绑定版本1全部DTO和场景骨架ABI不变。
4. 按详细设计核对4015首次建立/4070恢复后重签发的外部owner前置，重构存在类构造为唯一签名，删除状态/动态依赖；构造内先核验端口和owner当前，再按固定键初始化并读回族锚点和类型。
5. 修改新增存在为节点+族关系原子同写，保持普通存在结果DTO无族字段；提交后同截止读回来源。
6. 实现v2当前确认和历史见证，历史固定增加一次 `读取历史结构所有者` 并核验owner在H有效；严格执行4203错误顺序与有界L1读取集合。
7. 让世界存在当前/历史内容操作和成员/采用写前校验复用族来源；裸legacy按冻结状态拒绝。
8. 修改删除存在，使E和族归属同退并以首次退出集合重放；恢复只读固定初始化原键和请求指定E。
9. 删除存在类内全部状态/动态成员、枚举名、关系类型、服务字段、路由与调用；记录正式HEAD动态概念类第274、580、591行的存在旧字段引用为禁止修改的后续删除集，不改其它生产文件。
10. 更新工程登记，建立隔离Debug/Release模块与探针验证；形成施工/验证记录和函数知识变更清单。
11. 验证后只暂存本计划允许文件和两份记录，精确提交并普通推送。

若发现必须新增/改变4203字段、owner、关系方向/角色、状态本体、L1接口、场景类，或发现上述三处以外的新存在旧字段消费者，停止并具名退回计划支撑，不得以空函数或扩大白名单绕过。

## 5. 验证与成功条件

- `git diff --check`、`git diff --cached --check`；
- `python .\tools\check_specs.py --strict`，已知异主删除的FROZEN acceptance record阻断单独记录，不修复、不带入提交；
- 静态检查：旧存在共用结果以及存在模块内的绑定/状态/动态import、状态/动态成员和调用零命中；外部存在旧字段命中只允许正式HEAD动态概念类第274、580、591行三处，并登记为后续删除集；
- 隔离Debug/Release分别生成新窄合同、4203合同、存在模块IFC/OBJ及探针exe；两配置探针运行成功；
- 探针覆盖详细设计第10节的初始化、创建、当前、历史、退出、幂等、漂移、来源错误、legacy和引用冲突矩阵；
- 根工程Debug/Release独立执行并分账；允许在已知动态概念旧消费者或异主清空模块处失败，不能用目标模块结果替代根工程成功，也不能把该失败归因本计划；
- staged路径和提交树严格等于允许生产文件及两份记录，不包含规范、计划、索引或其它WIP。

## 6. 完成声明与后继

完成只表示合法存在E身份生产与4203 v2存在窄provider已发布，旧存在状态/动态成员已在存在模块退役。它不表示场景C、B绑定、动态、场景动态组织、动作来源、动态概念、因果、真实重启恢复、普通应用装配、正式集成验收或业务闭环完成。

代码结果发布并由交互智能体核验后，下一合法叶为“场景C身份生产 + 4203真实B绑定”；不预登记更远动态计划。
