# L1-SIMPLIFY-P3-FEATURE-INSTANCE 初始场景实例特征槽与初始值代码实施切片 v0.1

计划身份：`L1-SIMPLIFY-P3-FEATURE-INSTANCE`

版本：`v0.1`

## 1. 目标和完成条件

按配套详细设计，在已验证初始场景上建立一个 I64 实例特征槽与初始值。完成必须证明登记、宿主/定义/来源第一写前复核、单写集原子发布、同宿主同定义唯一性、幂等和独立同代次读回全部闭合。

依赖：`L1-SIMPLIFY-P1-WORLD-STRUCTURE` 结果 `0041f974...`、`L1-SIMPLIFY-P2-FEATURE-DEFINITION` 结果 `263a658a...`、`L1-SIMPLIFY-CONTRACT-F0` 结果 `ed1f09b7...` 均进入正式 main。

## 2. 允许文件

```text
海中鱼巣/领域/L1实例特征.数据.h
海中鱼巣/领域/服务.L1实例特征.ixx
海中鱼巣/领域/自检.L1实例特征.ixx
海中鱼巣/启动.运行期上下文.ixx
海中鱼巣/自检.运行期上下文.ixx
海中鱼巣/启动.应用程序.ixx
海中鱼巣.vcxproj
施工记录/20260804_L1-SIMPLIFY-P3-FEATURE-INSTANCE_施工记录.md
验证记录/20260804_L1-SIMPLIFY-P3-FEATURE-INSTANCE_验证记录.md
```

禁止修改：规范、详细设计、流程图、计划、索引；P2 三文件；世界登记/场景结构生产模块；L1 核心合同/服务/仓库；旧特征体系；SQL、材料、恢复、关系22准入、批次、比较、状态动态和其它自检。

## 3. 实施合同

1. 新数据头声明详细设计第4节全部 DTO、状态和完整性函数；不得出现 L1 写集、事务、仓库或候选类型。
2. 新服务唯一拥有 `L1实例特征服务`；导出导入场景结构和特征定义服务以公开嵌套 DTO，普通导入 L1 服务，不导出 L1 完整合同。
3. 私有幂等域固定 `0x05/0x06`；登记写集精确四节点。
4. 实例写集精确一节点、两关系、一 I64 值、一属性槽；角色为 1/2，关系3/索引/退出事实为0。
5. 写前必须通过初始场景、I64 定义、来源当前节点、共同代次和同宿主同定义唯一性复核。
6. 成功或精确重复必须调用公开读取入口逐项重新形成事实；非成功返回零业务载荷。
7. 运行期在 P2 服务之后持有新服务，提供 const/非 const 访问器；不改变 `完整()`。
8. 新专项顺序 320，完整自检 `26 -> 27`；覆盖详细设计第9节矩阵。

## 4. S0 与失败收口

执行前重算计划 blob，确认索引版本/状态、HEAD/origin/index、九文件所有权、P1/P2 正式接口和总数26。普通编译、实现和自检错误在九文件内修复。

若需要新增宿主类别、改变 P1/P2 公开合同、修改 L1 写集语义、建立关系3/固定关系22、扩大白名单或决定新机器语义，具名退回 DESIGN DRIFT，不自行扩张。

## 5. 验证

```powershell
git diff --check -- <九个允许文件>
msbuild .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Debug /p:Platform=x64
.\x64\Debug\海中鱼巣.exe --self-test-exit
msbuild .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Release /p:Platform=x64
.\x64\Release\海中鱼巣.exe --self-test-exit
python .\tools\check_specs.py --strict
powershell -ExecutionPolicy Bypass -File .\tools\clang_ast\扫描.MSVC模块依赖.ps1 -配置 Debug -平台 x64
git diff --cached --check
```

静态门禁：

- 数据头对 `写集幂等键|写集本地键|L1写集请求|候选|事务令牌|仓库` 零命中；
- 新服务对场景结构/特征定义各有唯一 `export import`，L1 服务只有普通 `import`；
- 私有域 `0x05/0x06` 各唯一，`0x01`—`0x04` 文件零修改；
- 建立函数内节点1、关系2、值1、属性槽1、退出0；角色精确1/2；
- `关系22|角色或顺序 = 3|索引` 在新生产模块零命中；
- 场景、定义、来源和快照读回均在第一写前；成功后公开读取入口调用一次；
- 运行期成员顺序正确；顺序320唯一，登记/执行/摘要均为27；
- 三个新文件工程登记正确，新 `.ixx` 各有唯一 `export module`。

## 6. 记录和发布

记录只写实际 S0、变更、验证、提交和完成边界。全部门禁通过后只暂存九个允许文件，精确提交并普通推送；其它所有者 WIP 原样保留。

## 7. 完成声明边界

只允许声明初始场景 I64 实例槽和初始值的原子发布、幂等及独立读回源码候选完成。不声明其它宿主、值换代、批次、比较、状态动态、恢复、生产初始化、旧特征体系迁移/删除或集成验收完成。
