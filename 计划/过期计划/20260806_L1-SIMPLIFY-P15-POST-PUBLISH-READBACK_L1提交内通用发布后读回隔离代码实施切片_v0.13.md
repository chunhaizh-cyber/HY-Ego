# L1-SIMPLIFY-P15 L1提交内通用发布后读回、双摘要幂等与隔离代码实施切片 v0.13

计划身份：`L1-SIMPLIFY-P15-POST-PUBLISH-READBACK`
版本：`v0.13`
创建侧基线：`b3f19edd755b56b21b78726b8b36221397da543e`

具名漂移：`DESIGN-DRIFT-P15-V012-G1-G8-EXECUTION-EVIDENCE-SOURCE-MATRIX-MISSING`

## 1. 目标与替代

从 P15 v0.12 重新冻结 provider 前预探测、G1—G8 逐调用点来源矩阵、结构化证据组装、首次 DTO 映射和恢复拒绝。v0.12 标为被 v0.13 替代/禁止施工；v0.13 发布后先登记待激活。保留模式1/2/3、W01/D01 边界、共享业务标签、16 文件/50 点和 L1 v2 一次提交/发布后读回。

## 2. 现状、范围和白名单

正式 HEAD 已机械确认 16 个代码文件、50 个 `.提交写集(`；当前 50 点中领域 `.探测幂等(` 为0，provider/写前证据组装为0。代码 WIP 由执行侧持有，本计划不修改代码。允许执行侧改动仍为 v0.12 的 20 文件白名单；不得新增工程、filters、启动文件、顺序登记或触碰 P57/P58、P0-FIX、路线和旧 WORLD-TREE。

## 3. 实施合同

1. 按详细设计 §3 先编码领域公开请求意图，再调用 `L1事实基座服务::探测幂等`；精确重复只读首次结果，冲突/隔离零载荷，未找到才读 provider。
2. 按详细设计 §4.1—§4.2 的 50 行矩阵实现 provider 稳定身份、合同/规则版本、写前事实种类/身份、共同截止、数量、排序和首次领域 DTO 映射。矩阵中的 P/N 来源是唯一来源；不得采用首个快照事实、调用序号、默认版本或摘要反推。
3. 为 G1—G8 设计/实现结构化 `L1执行证据材料`，模式1/3 两组合法为空，除 W01/D01 的模式2 两组非空；材料不完整不提交。L1 现有 `L1写集请求` 不增加规则版本字段。
4. 成功写入后同锁完整读回，并把同一不可变材料副本写入幂等账和提交成功审计；首次结果逐点映射到现有领域 DTO。恢复按双账、当前/历史/审计/完整快照显式入口重算矩阵，缺字段、错版本、数量/排序/截止/摘要不一致分别返回 `材料不完整` 或 `内部不一致` 并拒绝候选。
5. 保持 C01/C02、C08/C10/C11、C09/C12/C13、X01/X02 的共享标签；禁止按调用点生成标签。

## 4. 机械实施步骤

1. 从本计划基线重扫 16 文件/50 点，逐点登记矩阵行；数量、路径、DTO 或标签变化立即退回支撑。
2. 在允许的 L1 数据/合同/服务文件中冻结前置探测请求、证据材料和恢复状态合同；在 16 个调用文件中按矩阵接入意图编码、前置探测和首次 DTO 映射。
3. 为每个 provider 记录稳定身份、合同版本、规则版本、共同截止和完整事实组；按矩阵排序后再形成材料摘要。
4. 实现同义/异义/隔离零载荷、一次提交、发布后读回和幂等账/审计双副本；禁止摘要反推原始材料。
5. 按模式1/2/3及恢复拒绝矩阵建立自检；未通过时保留执行断点，不放宽合同。

## 5. 验证合同

创建侧已完成的文档门禁：`git diff --check`、`python .\\tools\\check_specs.py --strict`、UTF-8/NUL/尾随空白、50 点唯一矩阵、16 文件计数、G1—G8 每点意图入口和 P/N 来源列齐全。执行侧必须另行验证 Debug/Release Rebuild、完整自检、provider 前调用边、材料数量排序、首次 DTO 映射、双摘要、同锁发布后读回和恢复拒绝；本计划不把这些结果预先宣称通过。

## 6. 激活与边界

发布后执行侧从新 HEAD 保留现有 P15 WIP 重做 S0，重新核对所有权、白名单、实际 DTO 和 50 点矩阵。v0.13 最多声明设计合同已闭合并可重新 S0；不声明代码实现、构建、自检、生产接线、恢复运行、P57/P58 或治理闭环完成。
