# STATE-STRUCTURE-OPERATION-CONTRACT 状态类结构与操作代码实施计划

日期：2026-09-09
版本：v0.1
状态：可执行
起点代码基线：`0c1c0da91b59d9e65a2fe1f88cf93c4ebf41519b`

## 1. 目标和完成条件

按4212 v0.1与同名详细设计 v0.1，把现行状态类从旧“时间+多特征快照”整体切换为状态身份、正式FT、固定准确值、强时间四字段合同；补齐特征类窄只读核验；把存在、场景、因果三个身份消费者迁移到窄入口。

完成条件：状态合同版本2、精确DTO和十一项公开函数实现；两种值表示、两种强时间及0成立；创建/重放、当前/历史读取、三项字段查询、退出、同步实例清理和共同身份读回闭合；旧v1生产声明/定义/调用零命中；根工程Debug/Release x64隔离Rebuild成功；施工记录包含知识变更清单。

## 2. 正式依据和已验证事实

- `规范/1160_根规范_状态节点_20260720.md` v0.17。
- `规范/4212_子规范_状态类结构与操作合同.md` v0.1。
- `规范/4230_子规范_L2六类纯结构统一合同.md` v0.42。
- `规范/详细设计/20260909_STATE-STRUCTURE-OPERATION-CONTRACT_状态类结构与操作合同详细设计_v0.1.md` v0.1。
- 当前状态文件约1192行、合同版本1，使用I64时间和U64交替快照；直接消费者仅存在、场景、因果三处身份核验；当前没有状态服务生产构造点。
- 特征类具备内部FT/准确值核验能力，L1具备本计划所需原子写集、当前/历史读取、组查询、首次幂等材料及物理清理入口；不修改L1或工程登记。

## 3. 允许和禁止范围

唯一代码切片所有者：执行智能体在S0取得本计划后登记。

允许修改：

1. `海中鱼巣/领域/数据服务.特征类.ixx`
2. `海中鱼巣/领域/数据服务.状态类.ixx`
3. `海中鱼巣/领域/数据服务.存在类.ixx`
4. `海中鱼巣/领域/数据服务.场景类.ixx`
5. `海中鱼巣/领域/数据服务.因果类.ixx`
6. `施工记录/20260909_STATE-STRUCTURE-OPERATION-CONTRACT_状态类结构与操作施工记录_v0.1.md`

禁止修改：规范、详细设计、本计划、计划索引、L1、特征值类、动态类及动态概念类异主WIP、工程文件、装配入口、世界树、C/E/P/S绑定、当前选择、动作来源、动态概念、因果业务语义和其它文件。不得恢复已删除旧L2文件，不得删除或转换存量旧状态事实。

## 4. 冻结接口和实现边界

4212第2—6、11节全部C++名称、字段、枚举数值、签名、成功谓词和构造参数逐字实施；执行者不得改名、增加字段或兼容 overload。

- 特征类新增 `特征类型准确值核验请求/事实` 与 const `核验正式特征类型准确值`，物理位置为现有特征模块公开准确读合同附近；只复用现有私有读类型、解析值、值域和G/H守卫，零写。
- 状态类原文件整体替换为版本2；物理布局固定为普通节点、族归属关系、FT关系、互斥准确值、互斥时间、首次形成UTC技术值。
- 存在/场景/因果只把旧 `查询状态` 调用改为 `确认当前状态结构身份`，不读取状态内容、不改变本类DTO或其它语义。
- 状态类公开函数全集和逐字段增查改退映射以详细设计第3.1、4节为准；不可变字段不建立失败setter。
- 所有新增/修改公开边界诊断责任为“向上送出”；不得加生产日志或测试入口。

## 5. 实施步骤

1. S0重新核对HEAD、origin、index、占用表、六个允许文件、4212/4230/详细设计精确版本和当前调用点；任一正式合同或目标代码漂移则只停本切片。
2. 在特征类加入窄核验DTO和函数，使用当前私有能力完成同截止FT和值相容核验。
3. 清除状态类v1公开DTO与快照实现，按v2重新实现结构交付、DTO、读写、查询、身份和清理；不保留双轨。
4. 创建采用“先重放/后首次核验”，写后独立读回；读取统一走一个私有完整投影；字段查询只召回后同G完整读回；退出和清理严格零级联。
5. 将三个消费者迁移到窄当前身份确认并核对结果头。
6. 静态扫描旧符号和禁止依赖；隔离构建Debug/Release x64；只在允许文件内修复代码错误。若必须改变4212字段、DTO、错误映射、L1布局、生命周期或文件范围，返回计划支撑，不临场设计。
7. 新建施工记录，列出实际函数知识变化、验证、未覆盖边界和结果提交；精确提交并普通推送。

## 6. 非成功和收口

- 坏版本、零G/身份/预算、负时间或未知语义：入口拒绝、零载荷、零写。
- FT/值来源问题：按4212映射；不得默认0或创建F补救。
- 事实代次漂移：交换前零写；调用方重读。
- 同键异请求：幂等冲突；同请求按首次材料和首次截止收敛。
- 提交已确认后读回失败：保留事实和原键，返回内部不一致，不补写第二身份。
- 旧v1形状：旧格式不支持；不回退、不转换、不删除。
- 外部引用：退出/清理引用冲突；不级联跨owner。
- 构建错误属于本切片则修复；需要触及禁止文件、改变公开合同或受异主WIP影响时返回具名DRIFT。

## 7. 验证合同

文档与代码检查：

```powershell
git diff --check
python .\tools\check_specs.py --strict
rg -n "状态类结点|状态类新增请求|特征值快照|快照属性类型|读取状态中的特征值快照|按特征值快照查询状态" 海中鱼巣
rg -n "\.查询状态\(" 海中鱼巣\领域\数据服务.存在类.ixx 海中鱼巣\领域\数据服务.场景类.ixx 海中鱼巣\领域\数据服务.因果类.ixx
```

前两项无错误；后两项目标旧生产符号零命中。再核对状态模块不 import/持有存在、场景、动态、概念、因果服务。

构建在 `D:\TEMP\海中鱼巣\STATE-STRUCTURE-OPERATION-CONTRACT\<本轮唯一标识>\Debug` 与 `Release` 隔离输出下执行根工程 x64 Rebuild；两配置均须编译和链接退出0。具体MSBuild路径与输出重定向参数由执行S0依据本机工具链确定，不改根工程。

静态矩阵逐项核对4212第12节；本轮不运行程序、专项测试、数据库/重启恢复、故障注入或业务验收。未实际运行的项目必须在施工记录标为未验证。

## 8. 专属记录和知识回写

唯一施工记录：`施工记录/20260909_STATE-STRUCTURE-OPERATION-CONTRACT_状态类结构与操作施工记录_v0.1.md`。

记录必须逐函数列出：新增的特征窄核验、状态v2十一入口、退出的v1入口、三个消费者调用边变化；每项给出模块、签名、用途/非职责、参数来源、结果、读写事实、副作用、事务/生命周期、正式依据、验证状态和结果提交。现有“L2状态结构服务”用途卡已过期且对应旧物理删除模块；共享知识库不在本计划白名单，发布后只向维护者交接待复核清单。

## 9. 完成声明边界

计划标为可执行只表示设计合同已冻结。执行完成和两配置构建成功最多证明候选源码实现、静态旧入口退出及编译接入；不证明运行行为、存量迁移、跨进程恢复、普通应用周期维护、C/E/P/S状态使用绑定、动态、动作来源、动态概念、因果或业务闭环。
