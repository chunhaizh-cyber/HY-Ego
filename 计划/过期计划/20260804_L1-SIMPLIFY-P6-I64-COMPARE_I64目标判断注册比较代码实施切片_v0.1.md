# L1-SIMPLIFY-P6-I64-COMPARE I64 目标判断注册比较代码实施切片 v0.1

计划身份：`L1-SIMPLIFY-P6-I64-COMPARE`

版本：`v0.1`

创建侧代码基线：`d5ecd0584708bb94070892a3a49bf070a964f5d4`

## 1. 目标和完成条件

实现 SELF-GOV-CLOSURE v0.2 STEP-2 的首个真实提供者：把 I64 目标判断比较合同正式登记到新 L1，以一次完整快照校准当前实例值和 P2 目标上界值，返回三态关系与安全 `右值 - 左值` 差异。

完成必须同时满足：

- 详细设计 v0.1 的全部 DTO、稳定数值、五个公开函数和 L1 注册结构精确形成；
- 注册、读回和 I64 比较都存在真实成功路径，不得固定未实现或包装旧比较函数；
- 比较只读取一次 `L1完整快照`，零机器事实写入；
- Debug/Release 构建、完整自检和专项矩阵通过。

唯一设计依据：

- `规范/详细设计/20260804_L1-SIMPLIFY-P6-I64-COMPARE_I64目标判断注册比较详细设计_v0.1.md`；
- `流程图/20260804_L1-SIMPLIFY-P6-I64-COMPARE_比较特征函数流程图_v0.1.md`；
- 4015、4020、4110、4130、4180 和代码文件归属规范。

## 2. 允许文件

```text
海中鱼巣/领域/特征比较.数据.h
海中鱼巣/领域/服务.特征比较.ixx
海中鱼巣/领域/自检.特征比较.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
海中鱼巣/启动.应用程序.ixx
施工记录/20260804_L1-SIMPLIFY-P6-I64-COMPARE_施工记录.md
验证记录/20260804_L1-SIMPLIFY-P6-I64-COMPARE_验证记录.md
```

禁止修改：

- 现行 L1 公共/事实基座/索引数据、合同、仓库和服务；
- P2/P3/P4 的 DTO、服务和专项自检；
- 全部 L2 需求 DTO 与服务；
- 运行期上下文、生产初始化、SQL、材料、恢复、日志和 P5 索引；
- 旧 `特征服务.h`、旧句柄、旧 WORLD-TREE 比较链；
- 规范、详细设计、流程图、其它计划正文或计划索引。

## 3. 机械施工步骤

1. 新建 `特征比较.数据.h`，精确实现详细设计 §4 全部常量、枚举、DTO、默认值、可选字段和相等比较；不得自行增删稳定状态或复用一个字段承载排序与差异。
2. 新建 `海中鱼巣.领域.服务.特征比较` 模块，导出数据头和 `特征比较服务`；只保存非拥有 L1 引用、互斥量及值式登记/合同见证。
3. 实现私有 `0x08` 服务登记写集：六节点原子提交、返回映射唯一校验、逐项同代次读回、幂等/冲突/漂移分账。
4. 实现私有 `0x09` I64 目标判断合同写集：从请求中的 P2 定义登记、定义读取见证和 P3 实例登记取得全部字段来源，形成一注册节点、一配置值、一当前属性槽、11 条具名角色关系；配置数组与关系目标严格按详细设计 §6.2。
5. 实现合同正式读取：只接受已加载登记和精确读取请求；以一次完整快照逐项读回配置、关系、P2 定义和值，拒绝缺失、重复、退出或错域。
6. 实现统一 `比较特征`：一次完整快照同时验证注册、P2/P3/P4 输入、事实截止和创建代次；排序用比较分支，差异用减法前可表示检查；只填请求结果位。
7. 所有公开函数捕获资源/未知异常并返回结构化状态；不得写日志、调用旧栈、SQL、材料、P5 索引或 L2。
8. 新建顺序340专项，覆盖详细设计 §10 全矩阵；并发夹具只验证允许结果集合与事实不变，不通过时序猜测证明漂移一定发生。
9. 工程精确登记一个头和两个模块；启动模块只增加 import、薄包装、顺序340登记，并把完整自检期望从28更新为29。
10. 形成本计划专属施工记录和验证记录，记录实际提交、命令、输出和未证明边界。

## 4. 并发、错误与停工条件

- 不得持服务锁调用 L1；登记采用见证复制/外部写读回/重新确认后发布。
- 比较函数生产源码对 `读取完整快照` 调用精确一次，对 L1 写入口调用为零。
- 差异不可表示且请求含差异时整项入口拒绝，排序/关系/差异载荷全部为空；只请求排序或关系时不得执行危险减法。
- 注册不唯一是内部不一致，不选择候选；未注册不回退旧比较。
- 需要修改禁止文件、引入运行期/恢复/类型兼容、改变 L2 合同、允许调用方提交裸目标值，或无法在单快照中闭合真实成功路径时，停止受影响切片并具名退回设计漂移。

## 5. 验证合同

- `git diff --check` 与 cached diff check；
- Debug/Release x64 串行 Rebuild，零警告零错误；
- Debug/Release `--self-test-exit` 均退出0，完整自检 `29/29`；
- `python .\tools\check_specs.py --strict` 通过；
- MSVC 模块依赖扫描覆盖正式模块，若执行基线无无关模块变化，预期由190增至192，失败0、循环0、外部需求0；
- 工程静态核对新模块/头唯一登记、filters 与 vcxproj 一致、`.ixx` 不被文本 include；
- 公开函数静态核对五个签名唯一，`比较特征` 的 L1 完整快照调用精确1、L1写入0；
- 依赖静态核对旧 `特征服务.h`、句柄、节点直接仓、SQL、材料、P5 索引、L2 命中0；
- 正式 L1/P2/P3/P4 数据头和服务 blob 与执行基线一致；
- 专项机械覆盖登记/读回、三态、位集合、极值、同域门禁、P4 换代后沿用注册、版本漂移、缺失/损坏、并发和比较前后完整快照相等。

## 6. 完成声明边界

只证明新 L1 上的 I64 目标判断注册比较真实提供者源码候选和本计划矩阵成立。不得声明其它算法族、显式类型兼容、任意目标值、运行期装配、生产恢复、旧消费者迁移、G05 目标差距收束、集成验收、旧栈删除、STEP-3 或整个治理闭环完成。
