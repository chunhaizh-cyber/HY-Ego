# NODE-TYPED-MIGRATION NT-P2A 特征值类型化记录与槽位关系代码实施切片 v0.1

编号：NT-P2A

身份：`#340 / DQ-232 / NT-P2A v0.1`

日期：2026-07-22

设计状态：终审 PASS

任务状态引用：#340 / 计划 v0.1 / 合同建立状态版本 JY-586/E327；当前生命周期只读取 `项目记忆/任务状态台账.md` 同任务最新状态版本，本文不维护派发、S0、执行或集成状态。

## 1. 依据链

- 正式规范：0050、4010—4050、4110、4170、2100。
- 现状 / 施工流程图：`流程图/20260722_NODE-TYPED-MIGRATION_NT-P2A_特征值类型化记录迁移现状流程图_v0.1.md/.html`、`流程图/20260722_NODE-TYPED-MIGRATION_NT-P2A_特征值类型化记录迁移施工流程图_v0.1.md/.html`。
- 详细设计：`规范/详细设计/NODE-TYPED-MIGRATION_NT-P2A_特征值类型化记录与当前关系迁移详细设计.md`。
- 图谱：`项目记忆/设计记录/20260722_NODE-TYPED-MIGRATION_NT-P2A_函数结构知识图谱.md`。
- 上级：`#338 / DQ-230` 与 NT-P2 子计划。

## 2. 已验证事实与目标

当前默认域的 I64 仍由主信息值槽承载，VecI64 / VecU64 位于独立侧表；现行特征数据操作与 4170 已具备多参与者和换代语义，但没有节点直接新域类型化记录。目标是在 #339 隔离新域中形成：

```text
特征值类型化记录 = I64 | VecI64 | VecU64 + 原始值版本
关系 22 角色 1 宿主槽、角色 2 定义模板、角色 3 当前值
4170 继续独立承载不可变批次发布语义
复核特征值归属 / 读取宿主实例特征槽位组 两个值式只读合同
```

本切片不修改现行默认特征模块，不切默认装配，不删除旧侧表或主信息结构。

## 3. 真实依赖与 S0

具名前置为 #339：节点直接仓库、关系 22 ABI、可重建索引、领域记录参与接口、结构写入会话 / 执行器和跨域拒绝。解除条件是 #339 结果进入 main、设计归档并逐签名确认。

执行 S0 必须复核上述符号、模块 import 方向、文件不存在 / 无冲突、工程登记格式、统一自检入口和冻结基线。任一漂移返回 `接口漂移待设计修订`，零修改、零构建、零运行。

## 4. 精确允许文件

```text
海中鱼巣/领域/特征值类型化记录.数据.h
海中鱼巣/领域/参与者.特征值类型化记录.ixx
海中鱼巣/领域/数据操作.节点直接特征体系.ixx
海中鱼巣/领域/服务.节点直接特征.ixx
海中鱼巣/领域/自检.节点直接特征体系.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
海中鱼巣/自检.运行器.ixx（仅唯一阶段登记）
实施记录/20260722_NODE-TYPED-MIGRATION_NT-P2A_特征值类型化记录与槽位关系代码实施_Codex断点清单.md
```

工程与运行器在本计划取得正式执行权的活动区间内只由 #340 唯一写；#341/#342 不得与本计划并发取得同一工程或运行器所有权。`入口.cpp` 禁止修改。

## 5. 禁止范围

- #339 核心文件；现行 `参与者.特征值原始材料.ixx`、`数据操作.特征体系.ixx`、`特征值服务.h`、`特征服务.h` 只读。
- 状态 / 动态、自我、需求任务方法、P3、恢复、默认装配、线程、外设、SQL、控制面板和日志。
- 禁止把 4170 字段并入特征值记录，禁止保存宿主 / 模板 / 当前关系 / 来源端点副本，禁止旧域回退或双写。

## 6. 机械阶段

| 阶段 | 待实施目标 | 实施后证据 / 退回 |
| --- | --- | --- |
| S0 | 角色、Git、#339 接口、文件与模块事实复核 | PASS 才进入写入；漂移立即退回 |
| S1 | 建立封闭载荷和值式记录 / 访问器 / 参与者 | 三种载荷互斥，记录无拓扑字段 |
| S2 | 建立节点直接特征数据操作与关系 22 | 初始、换代、历史、索引复判完整 |
| S3 | 4170 与值记录同事务编排 | 多槽初始 / 换代 / 混合批次共同发布 |
| S4 | 建立两个公开值式只读合同 | P2B / P2C 不接触记录容器或仓库 |
| S5 | 独立自检、工程与运行器最小登记 | 生产不 import 自检，默认装配不变 |
| S6 | 双配置、完整自检、静态与 Git 收口 | 任一失败按计划停止，不扩范围 |

## 7. 拒绝、内部错误与撤销

零主键、错域 / 类型 / 版本、空或多载荷、错宿主 / 模板 / 当前值、写前漂移和同键异义均在第一写前拒绝。前置通过后的节点、关系、记录、4170、索引读回或参与者阶段异常属于内部逻辑错误；按依赖逆序撤销，不能证明前态则在释放写入权前隔离事务域。最后发布前普通读取不可见候选。

## 8. 验证矩阵

1. I64 / VecI64 / VecU64 三态互斥、版本单调、错值写前拒绝。
2. 关系 22 三角色基数、换代失效历史、同义幂等和异义冲突。
3. 多槽三类批次与 4170 逐项互证；每个准备 / 确认故障点可撤销。
4. `复核特征值归属` 与 `读取宿主实例特征槽位组` 只返回不可变值式投影。
5. 新文件零命中主信息句柄 / 仓库、旧 I64 / Vec 侧表、拆分句柄和拓扑副本。
6. Debug / Release x64 当轮 Rebuild、两配置完整 `--self-test-exit`、strict、diff check、允许文件、暂存白名单和提交后 clean 全部通过。

## 9. 完成边界

任务分支成功最多形成 `分支完成待集成`。进入 main 并经设计接受后，只证明隔离新域特征值记录、关系 22、4170 同事务和两个值式只读合同成立；不证明 P2B/P2C、默认生产、恢复或旧结构退役完成。
