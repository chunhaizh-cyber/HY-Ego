# L1-DATA-LAYER-COMPLETE-P6-FEATURE-DEFINITION-CONSUMER I64特征定义真实消费者中性幂等写读闭环代码实施切片 v0.1

日期：2026-08-07

状态：待登记；登记后可执行

## 1. 目标、完成条件与依据

原位迁移 `L1特征定义服务` 的登记建立、I64 定义建立和两条公开读取路径，使其只使用 L1 中性 CRUD。完成必须同时具备：两个固定中性写集、非权威定位加权威读回、精确重复、同键异义冲突、非法值域零写入，以及生产代码外合法消费者可经正式运行期取得该服务。

依据：0050、1120、1130、2100、4015 v0.5、4030 v1.3、4110 v0.3、ACCEPTANCE-01 v0.3，以及：

- `规范/详细设计/20260807_L1-DATA-LAYER-COMPLETE-P6-FEATURE-DEFINITION-CONSUMER_I64特征定义真实消费者中性幂等写读闭环详细设计_v0.1.md`
- `流程图/20260807_L1-DATA-LAYER-COMPLETE-P6-FEATURE-DEFINITION-CONSUMER_建立并复用I64特征定义函数流程图_v0.1.md`

## 2. S0 与所有权

执行起点不得早于创建侧正式基线 `b5786ae00f5f5c106ed1ccc1782b9d8cff09fc69`。S0 必须确认：计划索引本版本状态为可执行；`main == origin/main`、index 空；目标服务仍由运行期上下文构造且四个公开签名和 DTO 与设计一致；中性合同版本 1、九写状态、七读状态和七个公开入口匹配；目标三路径没有异主 WIP；唯一活动代码段及构建、运行、模块扫描资源可用。

## 3. 精确允许范围

只允许修改或新增：

1. `海中鱼巣/领域/服务.L1特征定义.ixx`
2. `施工记录/20260807_L1-DATA-LAYER-COMPLETE-P6-FEATURE-DEFINITION-CONSUMER_施工记录.md`
3. `验证记录/20260807_L1-DATA-LAYER-COMPLETE-P6-FEATURE-DEFINITION-CONSUMER_构建与生产链验证.md`

禁止修改数据头、L1 核心/仓库、运行期上下文、生产启动、工程、filters、其它领域服务、自检、规范、详细设计、流程图、计划、计划索引或集成验收目录。禁止触碰或夹带 P0-FIX、路线、旧 WORLD-TREE、`.codex-build` 和其它 WIP。

## 4. 实施步骤

1. 删除目标服务私有的领域意图编码、预探测、legacy 写入状态和完整快照验证辅助；保留不透明幂等键域映射。
2. 增加中性写入/读取状态显式映射、四编码映射唯一校验和中性节点/值读取辅助；不得使用裸枚举转换。
3. 重写 `建立登记`：形成 4 节点中性写集并恰一次提交，让 L1 先裁决精确重复/冲突、再裁决首次新键的期望代次，以四编码映射做权威读回并只保存定位；禁止提交前代次检查阻断合法重复。
4. 重写 `读取当前登记`：每次执行首末代次和四节点中性读取，禁止直接返回缓存事实。
5. 重写 `建立I64特征定义`：读取当前登记，形成 1 节点+3 值+3 槽中性写集，恰一次提交，再调用正式定义读回。
6. 重写 `读取I64特征定义` 及私有读回：只用中性节点、属性、值和事实代次入口，逐字段核对三槽和值域。
7. 删除服务内首次请求和定义事实权威副本；只保留登记四编码定位。精确重复和冲突完全由 L1 完整中性写集裁决。
8. 写两份专属记录，记录实际变更、验证、未覆盖范围和精确提交；不得写第一层自证或整体完善。

正式接口、字段、所有权或中性状态语义发生会改变实现的漂移时停止并具名退回计划支撑；普通编译或局部代码错误在白名单内修复。

## 5. 验证合同

至少执行并记录：三路径 `git diff --check`、`python .\tools\check_specs.py --strict`、Debug/Release x64 串行 Rebuild、两配置正式 `--runtime-context`、两配置模块依赖扫描和 staged 范围核对。

静态门禁：

- `探测幂等`、legacy `提交写集`、`读取完整快照`、`L1领域意图凭证`、`L1提供者版本见证` 和业务操作标签调用均为 0。
- `提交中性写集` 恰 2 个物理调用点：登记 4 节点；定义 1 节点+3 值+3 槽；两者关系和退出均为 0。
- 节点、属性、值、历史以外的正式读回只使用中性入口；登记和定义都具备首末事实代次校准。
- 服务不保存 `特征定义登记` 或 `I64特征定义事实` 权威副本，只保存四编码定位。
- 目标数据头、运行期上下文、生产启动和工程文件差异为 0。

构建、生产入口退出码和静态扫描只证明施工候选。代码发布后另做生产代码外消费者验收：经生产会话、租约和具名服务依次执行登记首次、定义首次、定义精确重复、同键不同合法区间冲突、非法区间拒绝，并在每个非成功前后经正式定义读取逐字段比较权威状态；驱动不得导入或调用 L1，不解析日志。

## 6. 完成声明边界

施工完成最多证明特征定义领域的中性代码候选和工程门禁成立。消费者验收通过后，才可累计节点/值/槽新增与读取、两个写集原子性、精确重复、同键异义冲突以及冲突/非法请求零变化。

不得宣称实例特征、当前值修改、关系、退出/历史、其它消费者、旧栈删除或整个 L1 完善；当前不覆盖崩溃和跨进程恢复。
