# NODE-TYPED-MIGRATION NT-P2B 状态动态完整事实代码实施切片 v0.5

编号：NT-P2B

身份：`#341 / DQ-233 / NT-P2B v0.5`

日期：2026-07-23

设计状态：终审 PASS

计划列表引用：#341 / 计划 v0.5；本文不维护执行过程。

执行角色边界：只允许修改本文列出的代码 / 自检文件并以结构化消息交接结果；禁止新建、修改、暂存或提交任何 Markdown。v0.2 分支及其未跟踪实施记录只作合同漂移证据，不得继承为 v0.5 结果。

共同合同基线：`NT-P2-CONTRACT/v0.4`

合同来源：`计划/20260722_NODE-TYPED-MIGRATION_NT-P2_领域载荷与自我投影迁移子计划_v0.4.md` 第 3、9 节

消费合同：`NT-C1/v0.3`、`NT-C1Q/v0.1`、`NT-C2/v0.4::复核特征值归属`

提供合同：`NT-C2/v0.4` 的状态 / 动态事实、主体事实组、退役条目和同代次全量冻结提供者

合同性质：预冻结待实现；本文只登记 #341 的提供 / 消费边界，不重复定义 DTO、枚举、签名或 ABI。

## 1. 依据与版本边界

- 正式规范：0050、1160、1170、4010—4050、4210、4220。
- 成对流程图：`流程图/20260722_NODE-TYPED-MIGRATION_NT-P2B_状态动态完整事实迁移现状流程图_v0.2.md/.html`、`流程图/20260722_NODE-TYPED-MIGRATION_NT-P2B_状态动态完整事实迁移施工流程图_v0.2.md/.html`；其中角色与合同版本由本 v0.5 叶子和 AGENTS 现行规则覆盖。
- 详细设计：`规范/详细设计/NODE-TYPED-MIGRATION_NT-P2B_状态动态完整事实类型化迁移详细设计.md`。
- 图谱：`规范/详细设计/函数结构知识图谱/20260722_NODE-TYPED-MIGRATION_NT-P2B_函数结构知识图谱.md`。
- 上级：`#338 / DQ-230` 与 NT-P2 v0.4。

本版不改变状态/动态业务语义。相对 v0.1，只取消等待 #339/#340 完成的候选开发门控，以预冻结合同开发消费端，移除共享接线写权，并把构建、运行和真实提供者联调后置 #352。

## 2. 目标和候选完成边界

状态/动态领域候选固定为：

```text
完整状态事实 = 状态节点 + 关系 19 角色 1—5 + 状态域类型化记录
完整动态事实 = 动态节点 + 关系 20 角色 1—9 + 动态域类型化记录
主体事实组 = 关系主体角色反向候选 + 逐项完整权威读回形成的调用期投影
退役准备 = 状态/动态退役规格 + 对应退役材料
```

候选完成不表示 #339/#340 已实现、当前分支可编译或事实组已接入生产。

## 3. 开发启动与最终集成

候选开发启动依赖：无。S0 核对本计划 blob、共同合同、七文件所有权和同名模块，并逐项核对 #339 当前真实类型与签名等于 `NT-C1/v0.3`；#340 预冻结提供的 `海中鱼巣/领域/节点直接领域合同.数据.h` 和特征值式合同只读消费。v0.2 候选不是当前接口成立证据。

最终汇合顺序：`#339 -> #340 -> #341`。#339 或 #340 实际合同漂移时分别退回其提供计划；#341 不得直接读取核心仓库以替代特征业务解释，也不得修改提供者模块适配漂移。

## 4. 接口合同引用与所有权

唯一合同正文为 NT-P2 v0.4 第 3.1、3.2、3.4、3.6 节。#341 必须完整实现其中 `NT-C2/v0.4` 的状态 / 动态服务签名、全部 DTO 字段、结果枚举、关系 19 / 20 与记录格式 ABI、值式所有权和调用期生命周期；不得在本叶用简写函数、额外模式或兼容返回形成第二版合同。

| 条目 | #341 职责 | 只读消费者 | 漂移退回 |
| --- | --- | --- | --- |
| `NT-C1/v0.3` 节点 / 关系 / 索引 / 会话 / 执行器 | 只读消费并形成 P2B 数据操作调用 | #341 内部 | 退回 #339；#341 不复制核心实现 |
| `NT-C2/v0.4::复核特征值归属` | 只读消费当前 / 历史审计值式材料 | #341 内部 | 退回 #340 / #341 |
| `NT-C2/v0.4` 状态 / 动态写读和主体事实组 | 在两个具名服务模块实现 | #342、#344、#345 | 退回 #341 和实际消费者 |
| `NT-C2/v0.4` 状态 / 动态退役规格和材料 | 同上；只形成准入 / 审计值式材料，不执行删除 | #346 | 退回 #341 / #346 |

值式 DTO 只使用唯一合同正文定义的自有值，不保存主信息、持久列表、关系仓库、索引、记录容器、令牌、锁、引用、指针或可写对象。

## 5. 精确允许文件

```text
海中鱼巣/领域/状态动态类型化记录.数据.h
海中鱼巣/领域/参与者.状态动态类型化记录.ixx
海中鱼巣/领域/数据操作.节点直接状态动态.ixx
海中鱼巣/领域/服务.节点直接状态.ixx
海中鱼巣/领域/服务.节点直接动态.ixx
海中鱼巣/领域/组合.节点直接状态动态.ixx
海中鱼巣/领域/自检.节点直接状态动态.ixx
```

| 文件 | `export module` / 分类 | 唯一职责 |
| --- | --- | --- |
| `状态动态类型化记录.数据.h` | 头文件 | 状态/动态强类型记录、请求、规格和值式结果 DTO |
| `参与者.状态动态类型化记录.ixx` | `海中鱼巣.领域.参与者.状态动态类型化记录` | 两类记录参与者与只读访问器 |
| `数据操作.节点直接状态动态.ixx` | `海中鱼巣.领域.数据操作.节点直接状态动态` | 关系 19/20、记录、索引与核心事务编排 |
| `服务.节点直接状态.ixx` | `海中鱼巣.领域.服务.节点直接状态` | 状态准入、写读和状态组/退役合同 |
| `服务.节点直接动态.ixx` | `海中鱼巣.领域.服务.节点直接动态` | 动态准入、写读和动态组/退役合同 |
| `组合.节点直接状态动态.ixx` | `海中鱼巣.领域.组合.节点直接状态动态` | 同一事务内组织状态迁移和动态候选 |
| `自检.节点直接状态动态.ixx` | `海中鱼巣.领域.自检.节点直接状态动态` | P2B 专项源码，生产不得反向 import |

## 6. 禁止范围

- #339、P2A/P2C/P3/P4 文件和现行默认状态/动态服务、兼容头全部只读。
- 禁止修改 `海中鱼巣.vcxproj`、`.filters`、`海中鱼巣/自检.运行器.ixx` 和 `入口.cpp`；统一接线归 #352。
- 记录不得复制主体、场景、特征、值、来源、目标、前后状态、动作或低层动态端点。
- 禁止直接读取 P2A 记录容器，禁止旧 I64/主信息双写，禁止持久状态/动态列表。

## 7. 机械阶段

1. S0：核对身份、计划 blob、七文件白名单、#339 真实接口、#340 预冻结合同、文件独占和同名模块；漂移时停止并发送结构化修订请求，不修改 Markdown。
2. S1：建立状态/动态封闭记录、请求、规格、值式结果、访问器和参与者。
3. S2：按 #339 合同编写关系 19 角色 1—5 的状态候选、读回和主体组消费端。
4. S3：按 #339 合同编写关系 20 角色 1—9 的四类动态候选、读回和主体组消费端。
5. S4：只通过 #340 `复核特征值归属` 编写特征/值相容复核；不得自行实现替身业务判断。
6. S5：实现 NT-P2 v0.4 第 3.4 节退役规格 / 材料和值式事实组；编写专项自检源码。
7. S6：只做合同签名、静态 import、禁止范围、白名单、`git diff --check`、`git diff --cached --check` 和当前长期执行通道分支卫生；不构建、不运行，只通过结构化消息交接。

## 8. 逻辑内返回与内部错误

无效身份、错类型/版本/时间/来源、特征值不相容、同键异义、许可竞争和写前漂移均在第一写前结构化返回。同键同义完整结构零写读回；合法无主体事实返回空组。基准状态完整发布但无相邻前状态，返回具名“状态已发布、未形成动态”。

前置通过后，节点、关系 19/20、类型化记录、索引、参与者阶段、逆序撤销、最后发布或事务外完整读回异常均为内部逻辑错误；不得变成空组、等待、学习缺口或部分成功。撤销不闭合时在释放许可前隔离事务域。

## 9. 任务级验证

- 本文第 4 节登记条目与 NT-P2 v0.4 第 3 节唯一合同的接口、DTO 和 import 静态一致；#340 合同只消费不重定义。
- 关系 19/20 角色组和记录字段在源码/专项自检中完整覆盖；记录零拓扑副本、零主信息、零旧侧表。
- 主体组读取保留合法空组和坏项整体失败语义；公开签名零仓库、记录容器、令牌、锁。
- 七文件白名单、禁止范围、暂存范围、`git diff --check`、`git diff --cached --check`、提交后 clean 和当前长期执行通道分支推送目标通过。
- 本任务不登记工程/运行器，不构建 Debug/Release，不运行程序或业务自检。

完整 A01—A18、关系 19/20 与 #340 联调、故障注入、双配置和完整自检由 #352 执行。

## 10. 最终集成退回点

- #339 核心合同漂移：退回 #339；#341 不复制核心实现。
- #340 `复核特征值归属` 名称、DTO、当前/历史语义或错误分类漂移：退回 #340。
- #341 需要旧请求默认补特征/值/来源、复制拓扑或双写旧域才能成立：退回 #341 设计。
- #342/P3 要求部分列表、持久数组或内部记录访问：退回消费者设计。
- 只有工程登记/统一运行器缺失：保留候选，退回 #352 接线。

## 11. 完成声明

任务分支最多声明：P2B 状态动态完整事实实现候选已按预冻结合同形成并通过静态边界和文件卫生，等待固定顺序汇合。不得声明已编译、已运行自检、已接通 #339/#340、已进入 main、P2C 投影已成立或恢复/旧域退役完成。

## 12. v0.5 精确增量

1. 全部状态 / 动态读取使用 `NT-C1Q/v0.1` 按目标、相关节点和类型角色枚举；禁止全仓扫描。
2. `状态事实值式材料` 固定返回主体、场景、特征、值、来源五个具名单项关系证据；`动态事实值式材料` 固定返回主体、场景、被改变目标、前状态、后状态、来源动作和低层动态具名证据。
3. 导出可移动不可复制的 `状态记录退役参与包`、`动态记录退役参与包`，只覆盖一次执行器调用。
4. 导出 `状态动态冻结提供者/v1`：按同一冻结域 / 代次返回状态、动态当前 / 历史 / 失效 / 删除记录的自有值式全量材料，不暴露仓或容器。
5. 验证必须覆盖跨代次拒绝、历史不丢、重复读取同义、坏项整体失败、发布前逆序撤销和发布后读回失败不否认已发布事实。

本计划白名单仍为第 5 节七文件；不得新增共享文件。
