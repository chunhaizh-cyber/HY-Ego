# NODE-TYPED-MIGRATION NT-P2B 状态动态完整事实代码实施切片 v0.1

编号：NT-P2B

身份：`#341 / DQ-233 / NT-P2B v0.1`

日期：2026-07-22

设计状态：终审 PASS

任务状态引用：#341 / 计划 v0.1 / 合同建立状态版本 JY-586/E327；当前生命周期只读取 `项目记忆/任务状态台账.md` 同任务最新状态版本，本文不维护派发、S0、执行或集成状态。

## 1. 依据链

- 正式规范：0050、1160、1170、4010—4050、4210、4220。
- 成对流程图：`20260722_NODE-TYPED-MIGRATION_NT-P2B_状态动态完整事实迁移现状流程图_v0.1.md/.html`、对应施工流程图 `.md/.html`。
- 详细设计：`规范/详细设计/NODE-TYPED-MIGRATION_NT-P2B_状态动态完整事实类型化迁移详细设计.md`。
- 图谱：`规范/详细设计/函数结构知识图谱/20260722_NODE-TYPED-MIGRATION_NT-P2B_函数结构知识图谱.md`。

## 2. 目标

在隔离新域形成完整状态 / 动态事实：关系 19 / 20 承载全部端点拓扑，状态域 / 动态域类型化记录只承载发生时间、接收时间、模式、动态种类和聚合规则版本等非拓扑值。P2B 通过 #340 的 `复核特征值归属` 验证状态特征和值，不读取特征记录容器。

本切片不改现行默认状态 / 动态模块、兼容头、生产调用方和恢复装配。

## 3. 真实依赖与 S0

```text
#339 -> 节点直接身份、关系 19 / 20、事务和记录参与接口
#340 -> 服务.节点直接特征::复核特征值归属
```

两项均须进入 main 并由设计逐签名接受。S0 核对实际函数、返回类型、模块方向、工程登记、文件所有权和冻结基线；漂移只退回 #341，不冻结其它计划。

## 4. 精确允许文件

```text
海中鱼巣/领域/状态动态类型化记录.数据.h
海中鱼巣/领域/参与者.状态动态类型化记录.ixx
海中鱼巣/领域/数据操作.节点直接状态动态.ixx
海中鱼巣/领域/服务.节点直接状态.ixx
海中鱼巣/领域/服务.节点直接动态.ixx
海中鱼巣/领域/组合.节点直接状态动态.ixx
海中鱼巣/领域/自检.节点直接状态动态.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
海中鱼巣/自检.运行器.ixx（仅本计划阶段登记）
实施记录/20260722_NODE-TYPED-MIGRATION_NT-P2B_状态动态完整事实代码实施_Codex断点清单.md
```

## 5. 禁止范围

P2A 生产文件只读，尤其禁止访问其参与者 / 记录容器；P2C、需求任务方法、现行状态 / 动态服务、兼容头、默认装配、线程、外设、快照和旧主信息仓库均禁止修改。记录不得复制关系端点或原始特征值。

## 6. 机械阶段

1. S0 当前接口与所有权复核。
2. 建立状态 / 动态封闭类型化记录、只读访问器和事务参与者。
3. 建立关系 19 角色 1—5 的状态写入 / 读取 / 主体组读取。
4. 建立关系 20 角色 1—9 的迁移动能、动作致变、观察 / 外部和聚合动态。
5. 通过 #340 值式服务复核特征值归属，禁止直接跨域读记录。
6. 覆盖幂等、并发、故障注入、候选不可见和投影清空重建。
7. 工程登记、双配置完整验证、精确提交并非强制推送任务分支。

## 7. 非成功与失败收口

无效身份、错域 / 类型、时间不合法、关系前置不满足、值归属不一致、同键异义、写前漂移和许可竞争在第一写前结构化返回。前置通过后的角色缺失 / 重复、记录不完整、节点 / 关系 / 索引读回不一致和事务阶段异常属于内部逻辑错误；逆序撤销，失败则隔离事务域。

## 8. 验证矩阵

- A01—A18 按详细设计逐项覆盖；关系 19 / 20 每个角色端点、基数和组合准确。
- 同义幂等覆盖全部端点与记录字段；不同状态 / 动态身份可并发。
- 状态 / 动态记录零拓扑副本、零主信息、零旧侧表；主体组读取合法空组，坏项不返回部分组。
- Debug / Release Rebuild、两配置完整自检、strict、diff、静态依赖、暂存白名单和禁止范围全过。

## 9. 完成边界

进入 main 后只证明隔离新域完整状态 / 动态事实及主体读取接口成立；不证明 P2C 聚合投影、兼容服务迁移、默认生产或跨重启恢复完成。
