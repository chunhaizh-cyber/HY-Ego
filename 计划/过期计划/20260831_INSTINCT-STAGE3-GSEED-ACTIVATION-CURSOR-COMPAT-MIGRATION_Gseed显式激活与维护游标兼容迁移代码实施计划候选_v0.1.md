# INSTINCT-STAGE3-GSEED-ACTIVATION-CURSOR-COMPAT-MIGRATION Gseed 显式激活与维护游标兼容迁移代码实施计划候选 v0.1

日期：2026-08-31

状态：候选；未发布、未登记、不可执行

基线：`4477a44e7bf9fe7a9ec4c9294f0ed1a6bb7fa708`

详细设计：`规范/详细设计/20260831_INSTINCT-STAGE3-GSEED-ACTIVATION-CURSOR-COMPAT-MIGRATION_Gseed显式激活与维护游标兼容迁移详细设计_v0.1.md`

## 1. 目标与完成条件

实现详细设计中的 A 分支生产激活、持久恢复和独立读回；对已有成功维护 B 分支及旧格式 U 分支返回结构化 fail-closed，保证三个水位不动。不得宣称或实现 B 的完整历史迁移。

完成必须同时满足：v4 权威事实和公开 ABI 完整；启动只消费 v2 边界快照并使用新激活入口；A/B/U、幂等、漂移、已可能发布和重启验证闭合；A/V、状态、动态零写。

## 2. 当前事实与复用

复用既有游标 owner、当前槽、L1 中性写集、主动安全登记见证、服务历史边界 v2、锚点绑定与持久读回。当前 v3 自动迁移缺成功维护权威，必须停止作为生产激活入口。旧 v1/v2/v3 公开读取保留兼容，但不得推导零成功。

## 3. 精确文件白名单

允许修改：

```text
海中鱼巣/领域/本能被动维护游标.数据.h
海中鱼巣/领域/服务.本能被动维护游标.ixx
海中鱼巣/启动.应用程序.ixx
海中鱼巣/端到端测试.本能被动维护游标.ixx
施工记录/20260831_INSTINCT-STAGE3-GSEED-ACTIVATION-CURSOR-COMPAT-MIGRATION_施工记录_v0.1.md
验证记录/20260831_INSTINCT-STAGE3-GSEED-ACTIVATION-CURSOR-COMPAT-MIGRATION_验证记录_v0.1.md
```

禁止修改：计划索引、规范、路线、其它设计 / 计划、工程文件、A/V 服务、服务历史 owner、秒序 composer、其它测试和所有异主 WIP。不得新建节点类型、第二 owner 或新工程模块。

## 4. 所有权与当前阻断

`海中鱼巣/启动.应用程序.ixx` 和 `海中鱼巣/端到端测试.本能被动维护游标.ixx` 在本候选形成时已有异主 dirty；它们是本计划必改生产 / 验证文件，因此存在真实文件所有权冲突。当前处理固定为：

```text
不改目标代码
不登记计划索引
不标记可执行或待激活
等待异主形成安全断点并明确交接 / 释放
-> 重新 S0
-> 按新 HEAD 复核接口、dirty、index、计划 blob 和验证资源
-> 计划智能体修订 / 发布并登记后，执行智能体才可施工
```

无交接时不得以分块编辑、覆盖 dirty 或只暂存局部差异绕过。

## 5. 目标接口和实施步骤

目标 DTO、枚举数值、函数签名、11 项持久材料和 A/B/U 算法以详细设计第 4—6 节为唯一施工合同。

施工顺序：

1. 在数据头增加 v4 游标、激活请求 / 结果 / 见证和 v4 读取合同；保留旧 ABI；
2. 在既有服务同一当前槽实现 11 项 v4 编解码、完整性和读取；v4 读取旧长度返回旧格式待裁决，旧 v1/v2/v3 读取对 v4 提供字段受限兼容投影；
3. 实现显式激活入口、首次写集重放、同义恢复、漂移、已可能发布和持久恢复；
4. 固定 A 仅限全新无旧游标，B/U 零写 fail-closed；
5. 替换启动的 v1 覆盖起点 / v3 自动迁移路径，以 `Gstart=G0` 取得 v2 完整边界并调用新入口；
6. 扩充专项测试，验证 A/B/U、零副作用、幂等、故障和重启；
7. 形成专属施工 / 验证记录，列出代码知识变更清单；不直接改共享知识索引。

## 6. 失败收口

- 纯代码、编译或专项测试错误：在白名单内修复；
- 正式接口与设计不一致且改变字段 / 事务 / 错误语义：停止受影响切片，具名退回计划支撑；
- 已可能发布：保留现场，以原幂等键和独立 v4 读回收束，禁止换键；
- 所有权、index、发布租约或验证资源冲突：只停止本切片，不处理异主现场；
- B 完整迁移需求出现：保持结构化 fail-closed，不扩展秒序后继。

## 7. 重新 S0 与验证

所有权释放后必须重新执行：

```powershell
git rev-parse --show-toplevel
git branch --show-current
git status --short --branch
git rev-parse HEAD
git rev-parse origin/main
git rev-list --left-right --count HEAD...origin/main
```

并核对正式计划索引、详细设计 / 计划精确 blob、目标四文件 diff 所有者、index 空闲、旧 / 新 ABI、服务历史 v2 和主动安全登记接口、`D:\TEMP` 专属验证根。

验证至少包括：专项 Debug / Release；全新 A、v1/v2/v3 U、v4 序号 1/N B；同键重复 / 异义；G0/Gseed/self/时间漂移；已可能发布；进程重启；A/V 与状态动态零写；普通启动消费新入口；旧读取 ABI 回归；外部 ABI consumer；`git diff --check`；`python .\tools\check_specs.py --strict`。具体构建入口须在 S0 枚举 `*.sln/*.slnx/*.vcxproj` 后选择，不预设不存在的 solution。

## 8. 完成声明边界

最多声明：A 生产激活及恢复完成，B/U 安全拒绝完成。不得声明已有成功维护实例完成兼容迁移、秒序完成、事实段完成、A/V 维护事务接通、阶段三或本能路线完成。

本候选不进入 `计划/计划索引.md`；当前无执行许可。
