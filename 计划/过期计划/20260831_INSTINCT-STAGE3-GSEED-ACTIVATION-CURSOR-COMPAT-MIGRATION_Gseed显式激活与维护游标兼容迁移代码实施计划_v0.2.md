# INSTINCT-STAGE3-GSEED-ACTIVATION-CURSOR-COMPAT-MIGRATION Gseed 显式激活与维护游标兼容迁移代码实施计划 v0.2

日期：2026-08-31

状态：正式代码实施计划；当前执行状态只由 `计划/计划索引.md` 的精确 blob 登记决定

计划身份：`INSTINCT-STAGE3-GSEED-ACTIVATION-CURSOR-COMPAT-MIGRATION`

冻结起点：`26ebb965cfb0e6dcb713903b032e94d0fef3ac0b`

## 1. 正式依据

- 路线 v0.4：`计划/20260831_INSTINCT-ROUTE_本能路线后续计划制定方案_v0.4.md`，冻结起点 blob `cba2cb28dc18196c3f11a73f6fd567cb03271b75`；
- 详细设计 v0.1：`规范/详细设计/20260831_INSTINCT-STAGE3-GSEED-ACTIVATION-CURSOR-COMPAT-MIGRATION_Gseed显式激活与维护游标兼容迁移详细设计_v0.1.md`，冻结起点 blob `5987f8ad475bd4afcf78bd4c5163762322ef615a`；
- 0050、6170 第 503—528 行和当前代码；
- 候选 v0.1 blob `8c6e2a5c20ccd8e2c3ef984d578beb5a48291294` 仅为形成记录，本计划以 v0.2 取代其施工候选身份。

## 2. 目标与完成边界

实现：

```text
A：全新实例有权威零成功证明
   -> 显式建立 v4 维护纪元、持久恢复和独立读回
B：v4 成功维护提交序号 > 0
   -> 返回需完整迁移，三水位不动
U：v1/v2/v3 或坏格式不能证明成功历史
   -> 结构化 fail-closed，原事实不动
```

完成必须证明 v4 DTO、公开激活 / 读取 ABI、11 项持久材料、同 owner 写集、启动消费者和专项消费者闭合；A/V、状态、动态及服务历史 owner 零写。

不实现或宣称：B 完整迁移、五类事实秒序、同 G0 事实段、A/V 候选或维护原子事务、阶段三完成、本能路线完成。

## 3. 当前事实、能力复用与文件

复用既有游标 owner、当前槽、L1 中性写集、首次写入重放、主动安全登记见证、服务历史边界 v2、本能根锚点和持久读回。旧 v1/v2/v3 读取继续对 v4 提供字段受限投影，但生产启动不再调用旧迁移。

允许修改：

```text
海中鱼巣/领域/本能被动维护游标.数据.h
海中鱼巣/领域/服务.本能被动维护游标.ixx
海中鱼巣/启动.应用程序.ixx
海中鱼巣/端到端测试.本能被动维护游标.ixx
施工记录/20260831_INSTINCT-STAGE3-GSEED-ACTIVATION-CURSOR-COMPAT-MIGRATION_施工记录_v0.1.md
验证记录/20260831_INSTINCT-STAGE3-GSEED-ACTIVATION-CURSOR-COMPAT-MIGRATION_验证记录_v0.1.md
```

禁止修改：计划索引以外的计划治理文件、规范、路线、详细设计、候选 v0.1、工程文件、A/V 服务、服务历史 owner、秒序 composer、其它测试和其它工作区 WIP。计划状态更新另由计划索引所有者按正式 blob 登记，不属于代码执行白名单。

## 4. 被承接静态 WIP 基线

起点 HEAD、index 和所有权复核结果：`main == origin/main == 26ebb965...`，index 为空；当前交互智能体已承接下列静态 WIP，没有其它活动 writer。它们不是本计划结果，不得进入 Gseed 提交。

### 4.1 启动文件

```text
路径：海中鱼巣/启动.应用程序.ixx
起点 diff 内容哈希：142e57d5eec79389469f5ed42b3e041705ea7558
起点 numstat：+12/-3
起点 hunks：HEAD 约第 334 行、369 行
内容：自我线程新增 L2任务结构服务；任务管理线程替换为任务动作 / 阶段治理 / 结果收束等提供者
```

Gseed 目标改动位于此前的维护游标启动初始化段。执行者必须保留上述两个基线 hunk，不把其中任何行暂存或提交。

### 4.2 游标专项测试

```text
路径：海中鱼巣/端到端测试.本能被动维护游标.ixx
起点 diff 内容哈希：58b3ab4b90a1ed74b67930f5ab632ef7a94864eb
起点 numstat：+0/-1
起点 hunk：HEAD 约第 211 行
内容：删除 `配置.等待合同登记 = 形成普通应用任务筹办等待合同登记();`
```

本计划新增用例必须落在独立测试段；不得把该删除行作为 Gseed 变更或恢复它。

若执行开始前两份起点 diff 的语义、hunk 内容或所有者再次变化，立即停止并重新 S0；不得把哈希不一致机械解释为允许覆盖。行号变化只作提示，内容和所有权才是裁决依据。

## 5. 冻结接口与物理合同

详细设计第 4—6 节为唯一机器合同，执行不得改名、改字段、改枚举数值或另选 owner。目标公开能力是：

```cpp
本能被动维护纪元激活结果_v1
本能被动维护游标服务::显式激活或恢复本能被动维护纪元_v1(
    const 显式激活或恢复本能被动维护纪元请求_v1&) noexcept;

本能被动维护游标读取结果_v4
本能被动维护游标服务::读取当前本能被动维护游标_v4(
    const 本能被动维护游标读取请求_v4&) const noexcept;
```

v4 事实 12 个公开字段中，`形成事实代次`取值事实创建代次，其余 11 项进入既有当前游标 U64 组槽。成功维护提交权威仍属于该游标 owner，不建立第二 owner。

## 6. 实施步骤

1. 增加 v4 游标、激活请求 / 状态 / 见证 / 结果和 v4 读取 DTO；保留旧 ABI；
2. 在同一当前槽实现 11 项 v4 编解码、完整性、v4 读取和 v1/v2/v3 受限投影；
3. 实现显式激活入口的全新实例证明、A 写集、B/U 零写拒绝、幂等、漂移、已可能发布和恢复；
4. 启动以 `Gstart=G0` 消费 v2 完整边界，调用新激活入口并独立读回 v4；删除生产 v3 自动迁移消费，不删除旧 ABI；
5. 专项验证 A/B/U、零副作用、幂等、故障、旧投影和重启；
6. 形成专属施工 / 验证记录和知识变更清单；不直接修改共享知识索引。

## 7. 混合文件暂存与发布门禁

代码实施前在 `D:\TEMP` 专属任务目录保存两份起点 patch 和哈希；不得写仓库内临时构建根。实施后：

1. clean 文件和新记录按精确路径暂存；
2. 两个混合文件只能按 hunk 精确暂存 Gseed 新增改动，可使用 `git add -p` 或等价的精确 index patch；
3. `git diff --cached --name-only` 只能含本计划白名单；
4. `git diff --cached` 必须逐 hunk 审计，启动文件不得含线程装配基线内容，专项测试不得含等待合同登记删除；
5. 暂存后未暂存 diff 必须仍保留第 4 节两份基线 WIP 的全部语义；
6. 若新旧改动形成无法安全拆分的同一 hunk，停止发布并回计划支撑，不得夹带旧 WIP，也不得 reset / checkout / stash；
7. 提交后再次核对提交内容仅为 Gseed 切片，工作区仍保留被承接基线 WIP。

以上是共享工作区发布保护，不改变机器语义。

## 8. 失败收口

- 代码、编译或专项测试错误：在白名单内修复；
- 正式设计 / ABI 漂移：停止受影响切片并具名退回计划支撑；
- 已可能发布：只用原幂等键和 v4 独立读回收束，不换键；
- B 完整迁移需求：维持结构化 fail-closed，不扩展秒序；
- index、发布租约或验证资源冲突：只停止本计划；
- 静态 WIP 基线改变或无法分离：保留现场并返回所有权 / 发布门禁问题。

## 9. 验证合同

执行 S0 必须重新枚举 `*.sln`、`*.slnx`、`*.vcxproj` 后选择真实入口，并在 `D:\TEMP` 使用专属构建 / 运行根。至少验证：

```text
专项 Debug / Release
全新 A；v1/v2/v3 U；v4 成功序号 1/N B
同键重复 / 异义、异键同义恢复
G0/Gseed/self/锚点/时间/规则漂移
已可能发布与同键收束
持久重启逐字段恢复
A/V、状态、动态、服务历史 owner 零写
普通启动只消费新入口
v1/v2/v3 受限读取投影回归
外部 ABI consumer
git diff --cached --check
python .\tools\check_specs.py --strict
```

构建、自检和静态审计分别记账；未运行的外部资源 / 旧持久样本场景必须明确 `NOT_RUN`，不得升级为业务通过。

## 10. 施工记录与完成声明

施工记录：`施工记录/20260831_INSTINCT-STAGE3-GSEED-ACTIVATION-CURSOR-COMPAT-MIGRATION_施工记录_v0.1.md`。

验证记录：`验证记录/20260831_INSTINCT-STAGE3-GSEED-ACTIVATION-CURSOR-COMPAT-MIGRATION_验证记录_v0.1.md`。

最多声明：A 生产激活 / 恢复与 B/U 安全拒绝已按本矩阵完成。计划发布、索引可执行、编译或局部探针都不等于代码结果、阶段三或本能路线完成。
