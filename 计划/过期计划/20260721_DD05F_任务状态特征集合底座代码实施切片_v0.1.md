# DD-05F 任务状态特征集合底座代码实施切片 v0.1

归档状态：因 C013 基础结构裁决失效；由 `#338 / NODE-TYPED-MIGRATION` 重新生成后继设计和计划；本文件仅作历史证据，不得恢复派发。

编号：DD-05F

日期：2026-07-21

版本：v0.1

状态：#330 / DQ-222；依赖门控待执行；等待 #329 / DD-05F0 经 v0.3 完整验证、独立集成、进入 main 并完成实际接口复核；未派发、未建 worktree

## 1. 权威依据

- `规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`
- `规范/3200_根规范_任务_20260720.md`
- `规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`
- `规范/4030_子规范_基础信息服务分层与领域写授权.md`
- `规范/4040_子规范_不透明结构事务候选确认撤销与最后发布.md`
- `规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`
- `规范/5200_子规范_任务根据需求初始化_20260720.md`
- `规范/5210_子规范_任务状态特征集合与阶段关联_20260720.md`
- `规范/5230_子规范_任务筹办与执行边界_20260720.md`
- `规范/详细设计/领域提交任务验证与自我回流详细设计.md`
- `流程图/20260721_DD05_领域提交任务验证与自我回流流程图_v0.1.md`
- `流程图/20260721_DD05_领域提交任务验证与自我回流流程图_v0.1.html`
- `项目记忆/设计记录/20260721_DD05_领域提交任务验证自我回流函数结构清单与知识图谱.md`
- `计划/20260721_SPEC-CODE-GOV_规范代码审计与差距治理总计划_v0.1.md`
- `规范/4170_子规范_特征批次发布记录与幂等账.md`
- `计划/20260722_DD05F0_特征批次当前值换代与同会话参与底座代码实施切片_v0.3.md`
- `计划/过期计划/20260722_DD05F0_特征批次当前值换代与同会话参与底座代码实施切片_v0.2.md`（只读失败证据）

范围一致性：本切片消费 DD-05F0 经 v0.3 完整验证和独立集成后的核心多参与者收口、4170 运行期批次发布记录、通用特征批次规格与同会话参与合同，建立命名模块任务域中的固定特征目录、强类型任务状态特征快照、阶段 / 状态段矩阵复核，并收束允许文件内的新分层任务创建、迁移、完成和装配入口。范围外旧 `任务服务.h` 及其调用点本切片只要求保持编译和既有回归，不构成正式映射完成证据；旧写入口迁移与退役由 `#334 / DQ-226 / DD-05F1` 在本切片实际接口进入 main 后独立设计。本切片不重新实现通用特征事务或批次账，不实现 4170 快照 / 恢复，不实现任务级唯一筹办执行权的召回—选择—冻结传递，不治理任务结果回执，不修改需求结算、自我消息、D455 或连续生产调度。

## 2. 已验证当前事实

1. 当前任务权威材料以 `分层任务生命周期阶段`、生命周期关系和少量选择 / 结果关系为主，尚无 5200 第 4 章完整固定特征目录和实例快照。
2. 当前 `迁移任务生命周期` 主要按当前阶段、目标阶段、幂等编号和迁移证据提交，尚未在同一结构会话中同时形成目标特征候选、业务状态段和生命周期投影。
3. 当前 `任务权威材料`、生命周期历史读取和任务服务已经提供可复用的任务身份、来源需求、治理存在、场景、目标状态、关系版本和审计历史。
4. 当前存在两套互不委托的任务写入口：命名模块 `任务业务服务` 经 `需求任务方法数据操作` 写入；旧 `任务服务.h` 直接持有仓库并写任务结构。入口、线程路由和多份旧自检仍绑定旧类，且旧构造与请求缺少正式入口要求的索引、数据操作和幂等材料，不能在本切片允许范围内等义变成委托。该代码差距登记 #334，本切片不得把完整回归解释为旧入口已经迁移。
5. 当前特征服务只公开单槽位 / 初始值式入口，不能让任务数据操作在遵守特征服务唯一写权的同时与任务宿主共同发布；该通用缺口由 #329 / DD-05F0 前置解决。
6. #328 也需要修改本切片的任务数据操作、任务服务、任务组合、运行期业务操作组合器和多份自检。两者存在不可消除的文件和公开接口所有权先后，必须由 #330 先完成、独立集成并进入 main，#328 再按实际接口修订。
7. 本切片不依赖 #324—#327 或 D455 业务接口，但依赖 #329 的通用特征事务接口，当前不能派发。

## 3. 依赖、所有权和执行前复核

### 3.1 具名依赖

具名前置：`#329 / DQ-221 / DD-05F0`。

待消费产物：核心执行器的确定顺序多参与者收口；4170 运行期不可变发布记录与完整有序幂等读回；特征业务服务形成的多槽初始 / 当前值换代 / 混合批次不可变规格；特征数据操作形成的不透明同层参与包；发布前旧完整状态、发布后新完整状态的权威读回；同义幂等、异义冲突、许可 / 读取分类、来源 / 当前值版本漂移和撤销失败隔离合同。

无法隔离原因：任务、初始生命周期和初始固定特征必须共同发布；任务迁移也必须让生命周期、迁移证据和特征换代参加同一结构会话。绕开 #329 只能直接穿透特征数据操作或形成两次提交，分别违反 4030 和 4040。

解除条件：#329 经 v0.3 完整验证后的任务提交由独立集成进入 `origin/main`，设计窗口逐签名复核实际多参与者入口、4170 运行期侧账、批次规格、参与包、授权桥、发布与撤销合同，并确认本计划文件所有权无活动冲突。R1 `f01a370f...`、R2 退回提交 `f3dc981c...` 或其局部实现均不能解除本门禁。`DD-05F0R` 的快照 / 恢复设计不是本切片运行期消费前置，也不得因 #330 解门而伪装成已经完成。

#328 / DQ-220 和 #331 / DQ-223 是本切片的后继消费者。两者都必须等待本切片进入 main；#328 还须按实际重叠调用点修订后重新派发。

### 3.2 唯一文件所有权

允许修改：

```text
海中鱼巣/领域/数据操作.需求任务方法.ixx
海中鱼巣/领域/服务.任务.ixx
海中鱼巣/领域/组合.需求任务方法.ixx
海中鱼巣/领域/组合.任务执行.ixx
海中鱼巣/领域/组合.任务结果结算.ixx
海中鱼巣/领域/组合.运行期业务操作.ixx
海中鱼巣/装配.运行期业务.ixx
海中鱼巣/领域/自检.需求任务方法分层.ixx
海中鱼巣/领域/自检.动态模式.ixx
海中鱼巣/线程/自检.任务执行调度.ixx
海中鱼巣/线程/自检.任务结果结算.ixx
海中鱼巣/线程/自检.自我治理回执.ixx
实施记录/20260721_DD05F_任务状态特征集合底座代码实施_Codex断点清单.md
```

禁止修改：

```text
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
海中鱼巣/入口.cpp
海中鱼巣/自检.运行期组合分层.ixx
海中鱼巣/领域/服务.方法召回.ixx
海中鱼巣/领域/数据操作.特征体系.ixx
海中鱼巣/领域/服务.特征.ixx
海中鱼巣/领域/参与者.特征值原始材料.ixx
海中鱼巣/领域/自检.特征体系分层.ixx
海中鱼巣/领域/自检.特征值原始材料事务.ixx
海中鱼巣/线程/任务结果回执协议.ixx
海中鱼巣/线程/路由.任务结果治理.ixx
海中鱼巣/线程/任务管理上行桥.ixx
海中鱼巣/线程/自我治理消息协议.ixx
海中鱼巣/线程/自我治理领域路由.ixx
海中鱼巣/适配/**
海中鱼巣/核心/**
海中鱼巣/数据库/**
海中鱼巣/界面/**
全部规范、流程图、计划和项目记忆
```

任务 worktree 只允许新增本计划专属实施记录。允许文件覆盖命名模块任务域正式实现和本切片自检，但不覆盖旧 `任务服务.h` 及其范围外调用点。S0 必须把调用点分类为“本计划允许范围内的新分层写入口”或“#334 旧写入口迁移范围”；范围外旧入口必须保持编译和既有回归，但不得作为本切片正式映射证明。若 #330 的新接口改动迫使范围外调用修改才能编译，则以 `接口漂移待设计修订` 退回，不得扩改入口、线程或范围外自检。

### 3.3 S0 当前接口复核

```powershell
function 断言原生命令成功([string]$步骤) {
    if ($LASTEXITCODE -ne 0) { throw "$步骤 失败，退出码 $LASTEXITCODE" }
}

git status --short --branch
断言原生命令成功 '读取 Git 状态'
git rev-parse --show-toplevel
断言原生命令成功 '读取 Git 顶层'
git branch --show-current
断言原生命令成功 '读取当前分支'
git rev-parse HEAD
断言原生命令成功 '读取当前 HEAD'
git rev-parse origin/main
断言原生命令成功 '读取 origin/main'
rg -n "创建任务|迁移任务生命周期|提交实际结果并完成|读取任务生命周期历史" .\海中鱼巣\入口.cpp .\海中鱼巣\领域 .\海中鱼巣\线程 .\海中鱼巣\装配.运行期业务.ixx .\海中鱼巣\自检.运行期兼容请求.ixx
断言原生命令成功 '扫描任务创建迁移完成与历史入口'
rg -n "特征批次|当前值换代|结构参与包" .\海中鱼巣\领域\数据操作.特征体系.ixx .\海中鱼巣\领域\服务.特征.ixx
断言原生命令成功 '扫描特征批次依赖合同'
rg -n "#328|#329|#330|DQ-220|DQ-221|DQ-222" .\计划\计划索引.md .\项目记忆\Codex任务队列.md .\项目记忆\并行工作树登记表.md
断言原生命令成功 '扫描计划与队列登记'
python .\tools\check_specs.py --strict
断言原生命令成功 '严格规范检查'
```

S0 必须确认：路径、分支、冻结基线、执行顶层任务、全部允许路径、仓库写权限、远端同名分支和工作树 clean 均与派发状态提交一致；未通过不得写入、构建或运行。

### 3.4 接口漂移退回

出现下列任一情况停止代码并只写计划允许的复核断点：

```text
#329 的批次规格、参与包或发布 / 撤销语义与本计划假定合同不一致。
必须新增节点类型、关系类型、第二任务状态仓库、文本状态表或进程局部权威缓存。
55 类固定特征无法用现行正式结构表达类型、值域、来源、版本和当前性。
本切片的新接口改动迫使旧 `任务服务.h` 或范围外调用点修改才能保持编译与既有回归。
必须修改特征底座、工程、入口、统一运行器、任务结果协议、自我生产协议、D455 或其它禁止文件。
允许文件被另一个活动 worktree 占用，或冻结后的 main / 接口发生漂移。
```

## 4. 实施目标

```text
创建任务时形成任务宿主、初始生命周期、初始固定特征、状态段和来源证据
-> 通过 #329 参与包与任务结构共同确认和最后发布，不暴露裸任务
-> 后续读取任务结构锚点、当前生命周期和完整特征快照
-> 复核预期生命周期关系、版本和特征快照版本
-> 按目标阶段计算必需、可选和禁止特征
-> 在同一不透明结构会话形成全部特征值、状态段、生命周期和迁移证据候选
-> 完整读回候选
-> 最后发布新生命周期关系和特征快照当前性
-> 旧状态段、旧特征值和旧生命周期保留审计
```

生命周期阶段只作路由投影。任何业务判断必须从任务身份、结构锚点、固定和开放特征、业务状态段、来源、版本与当前性共同形成的快照得出。

## 5. 固定机器合同

### 5.1 55 类固定任务特征

施工必须逐项实现 5200 第 4 章的全部固定类型，不得只选择本轮自检用到的子集：

```text
承接、路径、权重和授权（12）
任务承接角色、目标当前性、授权当前性、活动 OR 路径状态、所属 AND 组状态、继承父权重、AND 任务份额、冻结结算预算、调度优先级、时限状态、当前业务状态段、状态段当前性

筹办治理（9）
筹办轮次、筹办触发类型、筹办占用状态、筹办读取当前性、路径查找状态、候选集合完备状态、方法选择当前性、方法特征契约当前性、筹办结果类型

就绪、条件和输入闭合（8）
前置条件满足状态、输入闭合状态、参数绑定状态、禁止项命中状态、验证合同状态、归因合同状态、执行就绪裁决、执行冻结当前性

等待与重新触发（4）
等待类型、等待条件当前性、重新触发状态、等待版本

执行、结果和完成（17）
执行轮次、执行输入当前性、方法运行当前性、执行结果类型、目标达成裁决、本轮推进裁决、本轮推进量、剩余差距、验证裁决、归因状态、禁止结果状态、附带结果路由状态、结果当前性、目标达成比例、完成当前性、结算候选状态、结算状态

失败、取消和终止（5）
业务失败分类、失败证据当前性、取消当前性、动作停止确认、终止版本
```

每个目录项至少具备：固定类型身份、稳定特征定义身份、值种类 / 值域、允许来源角色、首次可出现阶段、允许当前阶段集合、目标阶段必需性、是否允许空值和验证规则版本。固定类型目录由任务域唯一解释；普通调用方不得临时创建固定任务业务特征。

### 5.2 10 个生命周期投影

逐项落实 5210 的最低必需、可选和禁止矩阵：

```text
已创建、已承接、筹办中、等待中、排队中、执行中、待重筹办、已完成、失败、取消
```

每个阶段都必须回读任务身份、来源需求、唯一运行态治理存在、场景、任务目标、当前生命周期关系 / 版本 / 时间、承接角色、目标当前性、授权当前性、业务状态段和状态段当前性。任何缺失不得由阶段枚举兜底。

### 5.3 20 个正式业务状态段

逐项落实 5210 第 4 章：

```text
承接初态
待初次筹办
筹办权已取得
路径形成中
候选形成中
候选已形成待选择
方法已选择待契约复核
方法契约复核中
等待子目标
等待能力补齐
等待条件或输入
等待外部事实或授权
可执行已冻结
执行动作中
执行结果待验证
目标未达成待重筹办
当前性失效待重筹办
任务完成
业务失败终止
授权取消终止
```

状态段身份和区分特征必须同时成立；同状态段下开放方法特征、值、来源或版本不同，仍是不同完整任务状态。

### 5.4 强类型值和快照

至少形成以下值式材料，具体中文符号名以不扩大现有模块边界为准：

```text
任务固定特征类型
任务固定特征目录项
任务固定特征值材料
任务开放特征挂载材料
任务状态特征快照
任务状态迁移请求
任务状态迁移结果
任务特征集合复核结果
任务状态段历史材料
```

分类值、单调编号、稳定身份 / 版本引用和有量纲数值必须分别强类型表达。不得用裸 I64、字符串、日志、线程编号、普通 bool 或生命周期枚举代替完整任务特征。

### 5.5 任务幂等材料到特征批次身份

任务域必须确定形成 #329 强类型特征批次身份，不把调用方裸编号直接当作全局批次键：

| 操作 | 强类型批次身份组成 | 完整语义还必须比较 |
| --- | --- | --- |
| 创建 | `任务创建` 操作种类 + 任务幂等主键 + 来源需求完整句柄 | 虚拟存在 / 初始状态幂等主键、发生时间、初始固定特征、来源、状态段和治理锚点 |
| 普通迁移 | `任务状态迁移` 操作种类 + 任务完整句柄 + 任务域幂等材料编号 | 前驱生命周期关系 / 版本、目标阶段、发生时间、全部特征变更、来源、状态段和迁移证据 |
| 完成 | `任务完成` 操作种类 + 任务完整句柄 + 完成状态幂等主键 | 前驱、实际状态、完整完成特征、来源动态 / 因果、时间和证据 |
| 失败 / 取消 | 对应操作种类 + 任务完整句柄 + 对应任务域幂等材料编号 | 前驱、终态特征、失败 / 取消来源、动作停止证据和时间 |

不同任务或不同操作种类使用同一数值不冲突。同一复合身份下任一完整语义字段变化必须成为异义冲突；只有完整同义和已发布结构互证才允许读回。v0.1 只使用任务特征映射规则版本 1，并把该版本传入 #329 业务规则版本；4170 记录格式版本不得被任务域解释为业务规则版本。

## 6. 机械实施阶段

### S0 依赖、身份、范围和调用点复核

执行第 3.3 节；确认 #329 已进入 main 且实际接口与假定合同一致，列出仓库内全部任务创建、生命周期迁移、方法选择、任务完成、装配和历史读取入口，并逐项标记“本计划允许范围内的新分层写入口”或“#334 旧写入口迁移范围”。任何 #330 改动迫使范围外调用必须改签名 / 改语义才能保持编译时返回 DRIFT。S0 只读，返回 PASS / DRIFT，等待设计窗口接受 PASS 后才能进入 S1。

### S1 固定目录和值式材料

在数据操作模块建立 55 类固定目录和强类型值式材料。目录初始化必须确定性、可重复读回、同义幂等、同键异义冲突；不得运行期按显示名称自由扩表。

### S2 完整快照读取和复核

扩展 `任务权威材料` 或建立其窄值式组成，使读取同时返回结构锚点、生命周期、固定特征、当前业务状态段、开放挂载摘要、来源、版本和当前性。缺失、重复、错类型、错来源或版本冲突必须返回结构不完整 / 内部不一致，不以生命周期兜底。

### S3 任务创建共同发布和迁移

任务创建必须让任务宿主、初始生命周期、初始固定特征、状态段和来源证据共同确认并最后发布；禁止先发布裸任务，再调用独立“初始化固定特征”补写。承接和生命周期迁移通过 #329 参与包，在同一不透明结构会话形成新特征候选、状态段、目标生命周期和关系 15 迁移证据，完整读回后最后发布。旧特征、状态段和生命周期仅失效当前性并保留历史。

创建、迁移、完成、失败和取消分别按第 5.5 节形成强类型复合批次身份。精确重复必须复核完整前驱关系、目标阶段、发生时间、全部特征值、来源、状态段和迁移证据；同义读回既有结果，异义冲突。

### S4 服务入口收束

在任务服务提供详细设计规定的语义能力：

```text
读取任务状态特征快照
创建任务并发布初始特征
提交任务状态迁移
复核任务阶段与特征集合
读取任务状态段历史
```

既有 `迁移生命周期` 只能委托任务业务服务的正式映射：从当前完整任务快照、目标阶段、来源证据、发生时间和任务域幂等身份确定生成完整特征变更。调用方不提供 55 类默认特征，服务也不得在材料缺失时猜测；证明无法安全兼容时退回设计，不在执行窗口扩大接口。

### S5 允许范围适配与全调用回归

只改第 3.2 节允许范围内的新分层创建、迁移、完成、组合和装配实现。范围外旧调用点保持原代码并随完整 `--self-test-exit` 回归，但该回归只证明兼容未破坏，不证明已经委托正式映射；不得据此扩改 `入口.cpp`、旧 `任务服务.h`、自我治理领域路由或范围外自检。在允许的既有自检中加入 DD05F-A01—A14，覆盖本计划第 7 节；不新增入口、阶段或统一运行器接线。

### S6 完整验证和任务分支收口

运行第 8 节命令，只暂存第 3.2 节全部允许路径，形成任务提交并非强制推送同名 `codex/*` 分支。完成状态最多为 `分支完成待集成`，不得自行集成或推送 main。

## 7. 验收矩阵

| 编号 | 必须证明 |
| --- | --- |
| DD05F-A01 | 55 类固定目录逐项存在，身份、值域、来源、阶段和规则版本可确定读回 |
| DD05F-A02 | 10 个生命周期阶段逐项拒绝缺失必需特征和仍当前的禁止特征 |
| DD05F-A03 | 20 个业务状态段逐项由状态段身份和区分特征共同成立 |
| DD05F-A04 | 同阶段 / 同状态段的异义开放特征或值不被合并 |
| DD05F-A05 | 缺任务锚点、来源、特征定义、槽位、值、版本或当前性时不得以阶段兜底 |
| DD05F-A06 | 首个任务创建后立即可读完整“已创建”状态；任务、初始生命周期、初始特征、状态段和来源证据共同发布，失败零裸任务 / 零半结构 |
| DD05F-A07 | 迁移在同一结构会话形成目标特征、状态段、生命周期和证据，最后一次发布 |
| DD05F-A08 | 精确同义重复读回既有版本；任一特征值、来源、时间或证据异义均冲突 |
| DD05F-A09 | 旧特征、旧状态段和旧生命周期完整保留审计，只有一个当前快照 |
| DD05F-A10 | 有量纲数值、身份 / 版本、分类值和单调编号不退化为裸 I64 或文本 |
| DD05F-A11 | 既有任务创建、承接、选择、迁移、完成和历史自检保持编译与回归成立；不把旧入口回归当作正式映射证明 |
| DD05F-A12 | 工程、入口、运行器、线程、D455、规范和中央治理文件相对基线零差异 |
| DD05F-A13 | 本计划允许文件内、经 `import 海中鱼巣.领域.服务.任务` 的新分层创建、承接、选择、迁移和完成写路径，全部经过 `任务业务服务 -> 需求任务方法数据操作 -> #329 同会话特征参与合同`；范围外旧 `任务服务.h` 调用仅保持编译与回归并登记 #334，不得宣称全仓已收束 |
| DD05F-A14 | #329 特征批次参与包只被同层任务数据操作消费，任务服务和高级组合器不穿透特征数据操作、会话或执行器 |

入口拒绝发生在第一写之前。完整前置通过后，候选创建、读回、撤销或最后发布异常属于内部逻辑错误，必须停止依赖路径；不得包装成业务失败、等待或普通 bool。

## 8. 验证命令与成功条件

```powershell
function 断言原生命令成功([string]$步骤) {
    if ($LASTEXITCODE -ne 0) { throw "$步骤 失败，退出码 $LASTEXITCODE" }
}

function 断言预期零命中([string]$步骤) {
    if ($LASTEXITCODE -eq 0) { throw "$步骤 出现禁止命中" }
    if ($LASTEXITCODE -ne 1) { throw "$步骤 无法完成扫描，退出码 $LASTEXITCODE" }
}

$冻结基线 = '<派发状态提交登记的完整冻结基线>'
if ($冻结基线.StartsWith('<')) { throw '必须先从正式派发状态读取并填写完整冻结基线' }

function 运行有界完整自检([string]$程序路径, [string]$配置标签) {
    $既有进程 = @(Get-Process -Name '海中鱼巣' -ErrorAction SilentlyContinue)
    if ($既有进程.Count -ne 0) { throw "$配置标签 自检开始前存在海中鱼巣.exe 进程" }
    $标识 = [Guid]::NewGuid().ToString('N')
    $标准输出 = Join-Path ([IO.Path]::GetTempPath()) ("DD05F-$配置标签-$标识.stdout.log")
    $错误输出 = Join-Path ([IO.Path]::GetTempPath()) ("DD05F-$配置标签-$标识.stderr.log")
    $进程 = Start-Process -FilePath $程序路径 -ArgumentList '--self-test-exit' -PassThru -WindowStyle Hidden -RedirectStandardOutput $标准输出 -RedirectStandardError $错误输出
    if (-not $进程.WaitForExit(180000)) {
        Stop-Process -Id $进程.Id -Force
        $进程.WaitForExit()
        if (@(Get-Process -Name '海中鱼巣' -ErrorAction SilentlyContinue).Count -ne 0) { throw "$配置标签 超时清理后仍有海中鱼巣.exe 残留" }
        throw "$配置标签 完整自检超过固定 180 秒；已只清理 PID $($进程.Id)"
    }
    if (@(Get-Process -Name '海中鱼巣' -ErrorAction SilentlyContinue).Count -ne 0) { throw "$配置标签 自检结束后仍有海中鱼巣.exe 残留" }
    if ($进程.ExitCode -ne 0) { throw "$配置标签 完整自检退出码 $($进程.ExitCode)；证据：$标准输出 / $错误输出" }
    return @($标准输出, $错误输出)
}

msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m:1
断言原生命令成功 'Debug x64 Rebuild'
$Debug自检日志 = 运行有界完整自检 '.\x64\Debug\海中鱼巣.exe' 'Debug'
msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Release /p:Platform=x64 /m:1
断言原生命令成功 'Release x64 Rebuild'
$Release自检日志 = 运行有界完整自检 '.\x64\Release\海中鱼巣.exe' 'Release'
python .\tools\check_specs.py --strict
断言原生命令成功 '严格规范检查'
git diff --check
断言原生命令成功 '工作树 diff check'
rg -n "任务固定特征|任务状态特征快照|任务状态段|复核任务阶段与特征集合" .\海中鱼巣\领域 .\海中鱼巣\线程 .\海中鱼巣\装配.运行期业务.ixx
断言原生命令成功 '任务特征正式入口扫描'

$桥导入文件 = @(rg -l "^\s*import\s+海中鱼巣\.领域\.桥接\.特征批次同层授权\s*;" .\海中鱼巣 -g "*.ixx")
断言原生命令成功 '扫描授权桥 import 方'
$桥导入绝对路径 = @($桥导入文件 | ForEach-Object { (Resolve-Path -LiteralPath $_).Path.ToLowerInvariant() } | Sort-Object -Unique)
$预期桥导入绝对路径 = @(
    (Resolve-Path -LiteralPath '.\海中鱼巣\领域\数据操作.特征体系.ixx').Path.ToLowerInvariant(),
    (Resolve-Path -LiteralPath '.\海中鱼巣\领域\数据操作.需求任务方法.ixx').Path.ToLowerInvariant()
) | Sort-Object -Unique
if (@(Compare-Object $预期桥导入绝对路径 $桥导入绝对路径).Count -ne 0) { throw '授权桥 import 方不精确等于两个生产数据操作模块' }

$记录参与者导入文件 = @(rg -l "^\s*import\s+海中鱼巣\.领域\.参与者\.特征批次发布记录\s*;" .\海中鱼巣 -g "*.ixx")
断言原生命令成功 '扫描批次记录参与者 import 方'
$记录参与者导入绝对路径 = @($记录参与者导入文件 | ForEach-Object { (Resolve-Path -LiteralPath $_).Path.ToLowerInvariant() } | Sort-Object -Unique)
$预期记录参与者导入绝对路径 = @((Resolve-Path -LiteralPath '.\海中鱼巣\领域\数据操作.特征体系.ixx').Path.ToLowerInvariant())
if (@(Compare-Object $预期记录参与者导入绝对路径 $记录参与者导入绝对路径).Count -ne 0) { throw '批次记录参与者 import 方不精确等于特征体系数据操作' }

rg -n "^\s*import\s+海中鱼巣\.(领域|核心)\." .\海中鱼巣\领域\桥接.特征批次同层授权.ixx
断言预期零命中 '授权桥项目模块依赖扫描'
rg -n "^\s*(class|struct)\s+需求任务方法数据操作\b|friend\s+(class\s+)?需求任务方法数据操作\b" .\海中鱼巣\领域\数据操作.特征体系.ixx
断言预期零命中 '跨模块前置声明与错误 friend 扫描'

git diff --exit-code $冻结基线 -- .\海中鱼巣.vcxproj .\海中鱼巣.vcxproj.filters .\海中鱼巣\入口.cpp .\海中鱼巣\自检.运行期组合分层.ixx .\海中鱼巣\领域\任务服务.h .\海中鱼巣\领域\数据操作.特征体系.ixx .\海中鱼巣\领域\服务.特征.ixx .\海中鱼巣\领域\参与者.特征值原始材料.ixx .\海中鱼巣\领域\自检.特征体系分层.ixx .\海中鱼巣\领域\自检.特征值原始材料事务.ixx .\海中鱼巣\线程\任务结果回执协议.ixx .\海中鱼巣\线程\路由.任务结果治理.ixx .\海中鱼巣\线程\任务管理上行桥.ixx .\海中鱼巣\线程\自我治理消息协议.ixx .\海中鱼巣\线程\自我治理领域路由.ixx .\海中鱼巣\适配 .\海中鱼巣\核心 .\海中鱼巣\数据库 .\海中鱼巣\界面 .\规范 .\流程图 .\计划 .\项目记忆
断言原生命令成功 '禁止范围差异检查'

$允许暂存 = @(
    '海中鱼巣/领域/数据操作.需求任务方法.ixx',
    '海中鱼巣/领域/服务.任务.ixx',
    '海中鱼巣/领域/组合.需求任务方法.ixx',
    '海中鱼巣/领域/组合.任务执行.ixx',
    '海中鱼巣/领域/组合.任务结果结算.ixx',
    '海中鱼巣/领域/组合.运行期业务操作.ixx',
    '海中鱼巣/装配.运行期业务.ixx',
    '海中鱼巣/领域/自检.需求任务方法分层.ixx',
    '海中鱼巣/领域/自检.动态模式.ixx',
    '海中鱼巣/线程/自检.任务执行调度.ixx',
    '海中鱼巣/线程/自检.任务结果结算.ixx',
    '海中鱼巣/线程/自检.自我治理回执.ixx',
    '实施记录/20260721_DD05F_任务状态特征集合底座代码实施_Codex断点清单.md'
)
git add -- $允许暂存
断言原生命令成功 '精确暂存'
git diff --cached --check
断言原生命令成功 '暂存 diff check'
$已暂存 = @(git diff --cached --name-only)
断言原生命令成功 '读取暂存文件'
$越界暂存 = @($已暂存 | Where-Object { $_ -notin $允许暂存 })
if ($越界暂存.Count -ne 0) { throw "存在计划外暂存文件：$($越界暂存 -join ', ')" }
git diff --cached --name-status
断言原生命令成功 '读取暂存差异'

# 完成任务提交后执行；提交动作本身也必须检查退出码。
$提交后状态 = @(git status --porcelain=v1)
断言原生命令成功 '读取提交后状态'
if ($提交后状态.Count -ne 0) { throw '提交后任务 worktree 或 index 不干净' }
$分支文件 = @(git diff --name-only "$冻结基线...HEAD")
断言原生命令成功 '读取任务分支文件范围'
$越界分支文件 = @($分支文件 | Where-Object { $_ -notin $允许暂存 })
if ($越界分支文件.Count -ne 0) { throw "任务分支存在计划外文件：$($越界分支文件 -join ', ')" }
```

成功条件：Debug / Release x64 均为当轮 Rebuild 产物；每次完整自检开始前残留进程为零、固定 180 秒内退出码 0，超时时只清理本次精确 PID 并立即退回；DD05F-A01—A14 全部通过；所有原生命令逐项检查退出码；strict、正命中扫描、工作树 diff、staged diff、暂存白名单和禁止文件零差异；授权桥 import 方精确等于两个生产数据操作模块，批次记录参与者只由特征体系数据操作 import，桥自身项目领域 / 核心模块 import、特征数据操作中的跨模块需求类前置声明和错误 friend 均为零命中，且零命中扫描只接受 `rg` 退出码 1；提交后任务 worktree / index clean，任务分支文件集合是允许文件和专属实施记录白名单的子集。

## 9. 实施后证据和完成边界

实施记录必须写明：派发准备 / 状态提交、冻结基线、实际签名、修改文件、55 类目录覆盖、10 阶段 / 20 状态段覆盖、事务失败点、幂等 / 冲突结果、两配置构建和自检结果、提交 / 推送、残余 dirty state 和完成边界。

任务分支完成、独立集成、main 发布和设计归档全部成立后，最多声明：

```text
命名模块任务域中的固定特征目录、完整任务状态特征快照、十阶段 / 二十状态段复核和同会话原子迁移底座已经实现并验证；生命周期阶段只作投影。
```

不得声明旧 `任务服务.h` 及范围外调用已经迁移或退役，也不得声明任务级唯一筹办执行权、完整任务筹办、任务结果治理、自我回流、D455 适配、生产装配或自我内部循环已经完成。
