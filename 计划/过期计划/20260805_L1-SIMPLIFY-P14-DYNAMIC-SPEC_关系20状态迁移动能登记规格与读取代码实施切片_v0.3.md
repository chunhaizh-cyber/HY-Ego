# L1-SIMPLIFY-P14-DYNAMIC-SPEC 关系20状态迁移动能登记、规格与读取代码实施切片 v0.3

计划身份：`L1-SIMPLIFY-P14-DYNAMIC-SPEC`

版本：`v0.3`

创建侧基线：`5c549f2adf9f19b9f1f495231c4694aba63c8a77`

替代：P14 v0.2

具名漂移：`DESIGN-DRIFT-P14-V02-L1-V2-INTENT-PREPROBE-CONTRACT-MISSING`

## 1. 目标与唯一身份

在 P14 原目标内把关系20登记接入当前 L1 v2；不实现第二状态、生产动态、最终跨域
发布或恢复。P14 登记属于 G7 状态/状态动态意图族，唯一入口变体3、操作标签
`0x15000F01U`、私有幂等域 `0x0F`。G7 T01/T02 标签不复用；G9 明确保留给 P78
动态因果领域，不修改或占用 G9。

P13 v0.2 结果提交 `b79544472e268bac87c24be8ec12494d7bf47abf`；P12 v0.4 结果链
为 `df0e0b1b7aeabd700c3289a266db7ebe24207fe8` 与 `de0009c624391ff1c76fd0f55fbfff78d9fd7f61`。
两者专项顺序360，P14 自有顺序390。

## 2. 精确白名单与禁止项

只允许 v0.2 原有八路径：

```text
海中鱼巣/领域/L1动态.数据.h
海中鱼巣/领域/服务.L1动态.ixx
海中鱼巣/领域/自检.L1动态.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
海中鱼巣/启动.应用程序.ixx
施工记录/20260805_L1-SIMPLIFY-P14-DYNAMIC-SPEC_施工记录.md
验证记录/20260805_L1-SIMPLIFY-P14-DYNAMIC-SPEC_验证记录.md
```

禁止修改核心 L1 数据头/服务、P15、P78、P16、P12/P13/P9/P10/P11、P0-FIX、路线、
旧 WORLD-TREE、SQL、恢复材料、`.codex-build`、计划索引和其它 WIP。不得新增第二生产
目标、第二链接、测试替身或旁路写入口。

## 3. 机械实施合同

1. 在 `L1动态.数据.h` 定义 P14 领域 DTO、状态和登记映射；领域合同/规则版本为1。
2. 登记入口先校验合同、规则、身份、非零期望代次，再按 `0x0F` 形成写集幂等键和
   `HZY-L1G7 → U32(1) → U32(0x15000F01) → U8(3) → U32合同1 → U32规则1 → U64身份 → U64代次`
   意图摘要；凭证组7、变体3、结果计划摘要全零。
3. 入口必须先调用 `L1事实基座服务::探测幂等` 一次；同义返回首次登记、异义返回
   幂等冲突，二者 provider/提交均为0。只有未找到才读取非空完整 L1 快照并构造十节点
   写集，L1 v2 合同固定为2，关系/值/槽/退出全空。
4. 首次材料固定为 `事实互证`：L1 核心 provider 版本见证一次、写前事实见证取稳定
   排序首项、共同截止等于快照代次；P14 外部 provider 调用为0。空仓纯G0、空仓领域首次
   和 G0 冒充领域摘要均禁止。
5. 仅允许一次 `提交写集`；成功/精确重复后同一代次逐项读回十项非零互异映射，形成
   `L1动态登记结果`。交换后任何矛盾返回内部不一致并隔离，不补写、不重试提交。
6. `形成状态迁移动能规格` 只消费 P13 值式结果，纯值形成 `0x44` 一节点/八值/六关系/
   八槽；`读取状态迁移动能` 只读十五个既有编码并做 G0/G1 一次完整重试，二者 L1写入、
   provider、幂等和意图均为0。

## 4. DTO、字段和结果映射门禁

登记公开请求必须只有：合同版本、规则版本、动态操作幂等身份、期望事实代次；不得
携带稳定编码、L1本地键、provider值、锁、许可、事务或结果字段。十节点本地键1—10及
对象/表示类型必须逐字符合详细设计；成功和精确重复结果必须带十项登记、首次身份和
事实代次；异义/拒绝/漂移/资源失败/内部不一致结果登记为空。

P13/P12 输入只允许通过 v0.2 已冻结值式结果进入规格函数；不得重新调用 P12/P9/P11、
不得由日志/缓存/请求回显补字段。关系20角色精确1—5/9，角色6—8严格为空，角色5
单向引用 `0x53000001`。

## 5. 自检、工程和顺序390

在同一生产程序既有自检运行器中登记顺序390；不得新增 exe/目标/装配。矩阵至少覆盖：

- G7 组/变体3/`0x15000F01` 摘要向量、乱改字段异义、同义/异义 `0/0`；
- 非空 L1 基线下 `探测1/提交1` 首次十节点登记、十项同代次读回和结果映射；
- 候选前版本/身份/代次错误、探测已隔离、坏快照、坏映射、读回矛盾的固定状态；
- 规格函数无前态/等价/禁止/非成功零规格及增加/减少 `0x44` 完整形状；
- 读取十五键、端点退出、G0/G1 漂移一次重试和角色6—8为空；
- P14 外部 provider/P12/P9/P11/SQL/缓存/日志/恢复/因果调用均为0；
- 八路径、工程/filters/启动、顺序390、单生产程序和正式日志观察门禁。

## 6. 激活与验证

保持 `待激活`，执行前必须从激活后新 HEAD 完整重做 S0，确认 P13/P12 实际 DTO、截止、
迁移证据、G7 变体3/标签/域、L1 v2 合同和八路径所有权均未漂移。创建侧只做文档、路径、
Mermaid、diff-check、strict 和字段/计数静态验证；不构建、不运行、不修改代码、不宣称
P14 已实现或已接线。

执行完成条件是：代码、工程、自检和两条既有记录全部在八路径内；Debug/Release Rebuild、
完整自检、MSVC 模块扫描、strict110/110、cached diff-check 和顺序390矩阵通过。结果最多
声明 P14 动态登记、关系20纯值规格和独立读取候选闭合，不声明生产动态发布、最终跨域事务、
恢复、动作/因果、STEP-3F 或治理闭环。
