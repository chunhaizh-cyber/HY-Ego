# WORLD-TREE-P1C-F 统一世界树业务服务最终合同骨架代码实施切片 v0.1

计划身份：WORLD-TREE-P1C-F

版本：v0.1

创建侧当前代码基线：`3ea95e2667c565d1fe7ea69e340189dcfb90b8f0`

状态：待激活

## 1. 目标与完成条件

建立 `世界树业务服务` 最终模块、门面专属 DTO、五引用构造和九个固定结构化未实现根，并以正式专项入口证明参数读取0、成员读取0、依赖调用0、机器事实变化0。

本计划只允许取得 `编译接入通过` 与 `入口巡检通过`，不允许真实读写、快照、事务、幂等、运行期接线、旧入口删除或服务验收。

## 2. 唯一设计链与激活依赖

```text
流程图/20260803_WORLD-TREE-P1C-F_统一世界树业务服务最终合同骨架业务流程图_v0.1.md
流程图/20260803_世界树业务服务查询存在完整事实_合同骨架函数流程图_v0.1.md
流程图/20260803_世界树业务服务创建存在并接纳到场景_合同骨架函数流程图_v0.1.md
规范/详细设计/20260803_WORLD-TREE-P1C-F_统一世界树业务服务最终合同骨架详细设计_v0.1.md
规范/详细设计/函数结构知识图谱/20260803_WORLD-TREE-P1C-F_统一世界树业务服务最终合同骨架函数结构知识图谱_v0.1.md
```

具名激活依赖：

1. P1A3F v0.3 已发布代码保持现行存在/场景类及移动/移除请求 ABI。
2. FEATURE-W-F v0.1 真实代码结果发布最终特征写入类、批次请求/读回。
3. P1B-F v0.2 真实代码结果发布最终动态类、状态动态请求/读回。
4. P1C-QF v0.1 真实代码结果发布业务状态1—11、三读 DTO 和快照服务。
5. 五个 provider 实例释放目标模块、工程、启动聚合和自检验证资源；顺序260未占用。

任一依赖只有设计、WIP、消息或旧大计划时保持待激活。P1C-Q、FEATURE-W、P1B-W 的真实成功行为不是本骨架编译依赖，但分别是门面真实读写替换的硬依赖。

## 3. S0

1. 核对本计划精确版本/blob已登记并在依赖满足后转为可执行；核对 main/origin、index、活动代码段和发布租约。
2. 逐字段/逐签名核对五个 provider 实例的正式代码结果与详细设计 §§2—4；任何类型、`noexcept`、构造或所有权漂移均零代码返回。
3. 确认正式代码无 `服务.世界树.ixx`、门面 DTO、门面类或九根第二所有者；旧 `世界服务.h` 不是复用候选。
4. 确认允许文件无其它所有者 WIP；记录顶层自检数量 N；顺序260未被占用，顺序250保留给已登记计划。

失败码统一记录为 `DRIFT-WORLD-TREE-P1C-F-PROVIDER-ABI`、`DRIFT-WORLD-TREE-P1C-F-OWNER` 或 `DRIFT-WORLD-TREE-P1C-F-SLOT-260`；不得建立兼容壳或临时 DTO。

## 4. 精确允许文件

```text
海中鱼巣/领域/服务.世界树.ixx
海中鱼巣/领域/自检.世界树业务服务合同骨架.ixx
海中鱼巣/启动.应用程序.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
施工记录/20260803_WORLD-TREE-P1C-F_施工记录.md
验证记录/20260803_WORLD-TREE-P1C-F_验证记录.md
```

其它文件全部禁止，尤其禁止修改 provider、共享事实头、规范、流程图、详细设计、计划、计划索引、旧世界服务、运行期组合/路由、线程、仓库、事务、SQL、日志和当前代码图谱。不得删除旧入口或真实接线。

## 5. 机械施工

1. 新建 `服务.世界树.ixx`，按详细设计 §2 固定 include/import 顺序。
2. 逐字声明创建请求、写后 variant、统一写结果及五引用不可复制移动类。
3. 逐字声明九根完整 `const noexcept` 签名；为九根分别建立完整成员函数指针静态断言。
4. 按详细设计 §5 落实三个读返回表达式和六个相同写返回表达式；九个形参均不命名。
5. 新建专项自检，复用 provider 正式合法构造链形成五引用门面；以默认/非法/完整/边界请求逐一调用九根。
6. 静态断言状态11、DTO字段/variant、构造约束和九根签名；运行期逐字段断言固定结果。
7. 调用前后逐值比较节点、关系、索引、类型合同、类型化值、事务幂等六仓及事务域代次；证明依赖调用0、成功后继0。
8. 在启动聚合只登记 `WORLD-TREE-P1C-F-S01 / 统一世界树业务服务最终合同骨架`、顺序260；顶层登记/执行/摘要从 N 到 N+1。
9. 登记一个生产模块、一个自检模块，形成专属施工/验证记录。

执行者不得决定真实状态映射、创建组合、特征初始/换代校验、状态动态映射或读写消费者接线。

## 6. 验证矩阵

| 编号 | 场景 | 必须结果 |
| --- | --- | --- |
| F01 | 九根身份/签名 | 定义各1、重载0、完整成员指针与noexcept通过 |
| F02 | DTO ABI | 字段/类型/default/variant顺序精确 |
| F03 | 构造 | 五引用正例；默认/指针/缺参/复制移动反例 |
| F04 | 默认/非法/完整/边界请求 | 每根四类输入结果逐字段一致 |
| F05 | 三读 | 未实现11、默认回执、空快照/缺项 |
| F06 | 六写 | 未实现11、0、不适用、四optional空、缺项空 |
| F07 | 静态调用闭包 | 参数引用0、成员读取0、项目调用/未解析调用0 |
| F08 | 权威事实 | 六仓及事务域代次调用前后相等 |
| F09 | 消费者 | 未实现后成功/回源/旧入口后继0 |
| F10 | 聚合 | 仅S01/260；N到N+1 |

## 7. 验证与失败收口

```powershell
git diff --check
python .\tools\check_specs.py --strict
msbuild .\海中鱼巣.slnx /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m:1
.\x64\Debug\海中鱼巣.exe --self-test-exit
msbuild .\海中鱼巣.slnx /t:Rebuild /p:Configuration=Release /p:Platform=x64 /m:1
.\x64\Release\海中鱼巣.exe --self-test-exit
rg -n "世界树创建存在请求|世界树写后事实|世界树写入结果|class 世界树业务服务|WORLD-TREE-P1C-F-S01" 海中鱼巣
```

再用现行 MSVC 模块依赖与可达目标模块 AST/libclang 或正式等价源码闭包证明九根定义唯一、参数/成员读取0、项目/未解析调用0。工具兼容失败必须保留真实诊断，不得冒充 PASS；双配置构建、完整自检和源码闭包仍必须成立。

任一 ABI、构建、自检、调用边、事实不变、聚合、strict 或 diff 失败时保留 WIP，不提交、不升级声明；不得删除依赖、自检或字段来求通过。

## 8. 完成边界

允许声明：`WORLD-TREE-P1C-F 九根最终合同骨架已编译接入并通过安全入口巡检`。

禁止声明：真实三读六写、统一快照、领域提供者、事务、幂等、恢复、生产路由、旧入口删除、局部可继续或服务验收完成。
