# NODE-TYPED-MIGRATION NT-P2A 特征值类型化记录与槽位关系代码实施切片 v0.3

编号：NT-P2A

身份：`#340 / DQ-232 / NT-P2A v0.3`

日期：2026-07-23

设计状态：终审 PASS

任务状态引用：#340 / 计划 v0.3 / 合同建立状态版本 JY-610/E351；当前生命周期只读取 `项目记忆/任务状态台账.md` 同任务最新状态版本，本文不维护派发、S0、执行或集成状态。

执行角色边界：只允许修改本文列出的代码 / 自检文件并以结构化消息交接结果；禁止新建、修改、暂存或提交任何 Markdown。v0.2 的实施记录路径已退出执行白名单。

共同合同基线：`NT-P2-CONTRACT/v0.3`

合同来源：`计划/20260722_NODE-TYPED-MIGRATION_NT-P2_领域载荷与自我投影迁移子计划_v0.3.md` 第 3 节

消费合同：`NT-C1/v0.3`

提供合同：`NT-C2/v0.3` 的特征值归属、宿主槽位组和特征值退役条目

合同性质：预冻结待实现；本文只登记 #340 的提供 / 消费边界，不重复定义 DTO、枚举、签名或 ABI。

## 1. 依据与版本边界

- 正式规范：0050、2100、4010—4050、4110、4130、4140、4170。
- 现状/施工流程图：`流程图/20260722_NODE-TYPED-MIGRATION_NT-P2A_特征值类型化记录迁移现状流程图_v0.2.md/.html`、`流程图/20260722_NODE-TYPED-MIGRATION_NT-P2A_特征值类型化记录迁移施工流程图_v0.2.md/.html`；其中角色与合同版本由本 v0.3 叶子和 AGENTS 现行规则覆盖。
- 详细设计：`规范/详细设计/NODE-TYPED-MIGRATION_NT-P2A_特征值类型化记录与当前关系迁移详细设计.md`。
- 图谱：`项目记忆/设计记录/20260722_NODE-TYPED-MIGRATION_NT-P2A_函数结构知识图谱.md`。
- 上级：`#338 / DQ-230` 与 NT-P2 v0.3。

本版不改变 P2A 业务语义。相对 v0.1，只取消等待 #339 完成的开发启动门控，冻结 #339 应提供的接口合同，移除共享工程/运行器写权，并把完整构建和联调后置 #352。

## 2. 目标和候选完成边界

形成隔离新域候选：

```text
特征值类型化记录 = I64 | VecI64 | VecU64 + 原始值版本
关系 22 = 宿主槽 + 定义模板 + 当前值
4170 = 独立不可变批次账
跨域只读 = 复核特征值归属 + 读取宿主实例特征槽位组
退役准备 = 特征值退役规格 + 特征值退役材料
```

候选完成只表示本叶生产模块和专项自检源码按合同形成；不表示当前分支可编译、接口已联调或能力已进入 main。

## 3. 开发启动与最终集成

候选开发启动依赖：无。执行 S0 核对本计划 blob、独占文件、同名模块冲突、Git 身份，并逐项核对 #339 当前模块、真实类型和会话签名等于 `NT-C1/v0.3`；不得沿用 `NT-C1/v0.2` 的预期 DTO。

最终汇合顺序：`#339 -> #340`。#339 实际接口必须逐项符合 NT-P2 v0.3 第 3.2 节；不符合时退回 #339 提供计划，P2A 候选只读保留，不在 P2A 内复制核心仓库或改写合同。

## 4. 接口合同引用与所有权

唯一合同正文为 NT-P2 v0.3 第 3.1—3.3、3.6 节。#340 必须完整实现其中 `NT-C2/v0.3` 的四个特征服务签名、全部 DTO 字段、结果枚举、关系 22 / 记录格式 ABI、值式所有权和调用期生命周期；不得在本叶改名、删字段、增加仓库能力或形成第二版合同。

| 条目 | #340 职责 | 只读消费者 | 漂移退回 |
| --- | --- | --- | --- |
| `NT-C1/v0.3` 节点 / 关系 / 索引 / 会话 / 执行器 | 只读消费并形成 P2A 数据操作调用 | #340 内部 | 退回 #339；#340 不补造核心接口 |
| `NT-C2/v0.3::复核特征值归属` | 在 `海中鱼巣.领域.服务.节点直接特征` 实现 | #341、#344、#345 | 退回 #340 和实际消费者 |
| `NT-C2/v0.3::读取宿主实例特征槽位组` | 同上 | #342、#343 | 退回 #340 和实际消费者 |
| `NT-C2/v0.3::形成特征值退役规格 / 读取特征值退役材料` | 同上；只形成准入 / 审计值式材料，不执行删除 | #346 | 退回 #340 / #346 |

公开接口只返回唯一合同正文定义的自有值式结果，不暴露特征值记录容器、参与者、关系 / 索引仓库、事务令牌、锁、引用、指针或可写对象。

## 5. 精确允许文件

```text
海中鱼巣/领域/特征值类型化记录.数据.h
海中鱼巣/领域/节点直接领域合同.数据.h
海中鱼巣/领域/参与者.特征值类型化记录.ixx
海中鱼巣/领域/数据操作.节点直接特征体系.ixx
海中鱼巣/领域/服务.节点直接特征.ixx
海中鱼巣/领域/自检.节点直接特征体系.ixx
```

新生产模块归属：

| 文件 | `export module` | 唯一职责 |
| --- | --- | --- |
| `特征值类型化记录.数据.h` | 头文件，不声明模块 | 封闭三态载荷、记录和值式 DTO |
| `参与者.特征值类型化记录.ixx` | `海中鱼巣.领域.参与者.特征值类型化记录` | 本域记录参与者与只读访问器 |
| `数据操作.节点直接特征体系.ixx` | `海中鱼巣.领域.数据操作.节点直接特征体系` | 关系 22、记录、4170 与核心会话共同编排 |
| `服务.节点直接特征.ixx` | `海中鱼巣.领域.服务.节点直接特征` | 业务准入和 NT-P2 v0.3 第 3.3 节值式合同 |
| `自检.节点直接特征体系.ixx` | `海中鱼巣.领域.自检.节点直接特征体系` | P2A 专项源码，不进入生产依赖 |

## 6. 禁止范围

- 禁止修改 #339 核心文件、P2B/P2C/P3/P4 文件和现行默认特征路径。
- `海中鱼巣.vcxproj`、`海中鱼巣.vcxproj.filters`、`海中鱼巣/自检.运行器.ixx`、`海中鱼巣/入口.cpp` 全部禁止；登记和统一接线归 #352。
- 禁止把 4170 字段并入特征值记录，禁止保存宿主/模板/当前关系/来源端点副本。
- 禁止旧域回退、主信息/旧侧表双写、通用值容器和跨域仓库访问。

## 7. 机械阶段

| 阶段 | 待实施目标 | 任务级证据 / 退回 |
| --- | --- | --- |
| S0 | 核对身份、计划 blob、合同基线、同名文件和六文件白名单 | 真实接口、计划 blob、基线或所有权不等同即停止并发送结构化修订请求；不修改 Markdown |
| S1 | 建立封闭 I64/VecI64/VecU64 载荷、记录、值式 DTO 和参与者 | 三种载荷互斥，记录无拓扑字段 |
| S2 | 按预冻结 #339 合同编写关系 22 初始、换代、历史和索引消费端 | 不复制核心仓库算法，不写兼容分支 |
| S3 | 编写记录与 4170 同事务候选编排 | 4170 保持独立，撤销顺序明确 |
| S4 | 实现 NT-P2 v0.3 第 3.3 节四组值式读取 / 退役合同 | 公开边界零仓库、零令牌、零容器 |
| S5 | 编写专项自检源码与具名检查点 | 不登记统一运行器，不运行自检 |
| S6 | 静态合同、白名单、staged diff 和分支卫生收口 | 不构建、不运行；只提交六个允许代码 / 自检文件并以结构化消息交接 |

## 8. 逻辑内返回、内部错误与撤销

零主键、错域/类型/版本、空或多载荷、错宿主/模板/当前值、同键异义、写前漂移和许可竞争均在第一写前结构化返回。合法宿主无槽返回空组。

前置通过后的节点、关系、记录、4170、索引读回、参与者阶段、候选可见性或逆序撤销异常属于内部逻辑错误；必须停止新增写入，按依赖逆序撤销，不能证明前态时在释放许可前隔离事务域。不得回退旧主信息或把坏项过滤成部分结果。

## 9. 任务级验证

1. 本文第 4 节登记条目与 NT-P2 v0.3 第 3 节唯一合同的接口名、DTO 字段和 import 方向静态一致；预冻结提供者接口只被消费，不被本叶重新定义。
2. 三态载荷互斥；类型化记录零拓扑副本、零主信息、零 4170 业务字段。
3. P2B/P2C 只能通过服务值式合同消费；公开签名零仓库、令牌、会话、参与者和锁。
4. 六文件白名单、禁止范围、暂存范围、`git diff --check`、`git diff --cached --check` 和提交后 clean 通过；推送目标只能是当前同名 `codex/*` 分支。
5. 本任务明确不修改工程/运行器，不构建 Debug/Release，不运行程序或业务自检。

完整关系 22/4170 联调、故障注入、Debug/Release、专项/完整自检和运行期可见性验证由 #352 在固定汇合链上执行。

## 10. 最终集成退回点

- #339 实际模块、类型、关系 / 索引 / 事务语义不符合 NT-P2 v0.3 第 3.2 节：停止本切片并发送结构化修订请求；#340 不补造核心兼容 DTO。
- P2A 为成立需要暴露记录容器、复制拓扑、合并 4170 或双写旧结构：退回 #340 设计。
- P2B/P2C/P3 要求超出 NT-P2 v0.3 第 3.3 节的跨域能力：退回消费者设计，不扩 P2A 为通用服务。
- 仅缺工程登记或运行器接线：保留 #340 候选，退回 #352 共享接线阶段。

## 11. 完成声明

任务分支最多声明：P2A 隔离新域实现候选已按预冻结合同形成，文件所有权与静态边界通过，等待固定顺序汇合。不得声明代码已编译、专项自检已运行、接口已与 #339 联调、能力已进入 main、状态动态/自我已接通或旧结构已退役。
