# INSTINCT-ROOT-RUNTIME-ANCHOR 本能根运行锚点代码实施计划 v0.1

状态：待激活，等待 `INSTANCE-FEATURE-IF-R-CONCEPT-FOUNDATION` 代码发布及ARCH-L4物理合同。退回基线：`b48e3826e8288cf31041c037611d895b1481658f`。

目标：在机器语义重新冻结后实施8121阶段21，形成或恢复安全/服务双根的同截止值式锚点；启动在失败时阶段21短路。完成不证明线程、治理循环或业务闭环。

## 激活门禁

执行 S0 已具名退回，不能靠代码裁决。只有下列两项完成正式冻结、上游提供者已发布并同步更新8121 / 详细设计后，本计划才可重新标为`可执行`：

1. `INSTANCE-FEATURE-IF-R-CONCEPT-FOUNDATION` 的合同与实现发布：4115已冻结准确F不可变、IF/R由存在 owner 管理、概念汇聚由概念 owner 记录。该计划必须发布存在类“按存在持有特征变更并返回实际F”提供者，闭合F值、位置、概念、IF/R、既有当前采用、内部事务、幂等、发布未知和读回；阶段21只取得实际F，绝不模拟这些机制。
2. 目标合同、独立根 D、根 L 与成员的 ARCH-L4 DTO、关系、原子发布、恢复和读回；该合同不得为根 D 添加所属存在、目标宿主、自我字段、关系或 owner。

已裁决边界：安全 / 服务值的表示、区间语义和变更机制由特征类决定；阶段21只调用已发布的“按存在持有特征变更并返回实际F”提供者。阶段21不创建`M-X`、二次实例、状态或动态，需求服务不是特征类的替代者。安全 / 服务双根 D 是独立根节点，只具有独立身份、根特征、根目标合同、父空、顺序0与根列表闭包；阶段21入口和最终锚点表达其逻辑自我归属，根 D 不得有自我物理绑定，且阶段21不处理任务实体或任务物理关系。

允许：详细设计列出的特征、状态、需求、业务provider、普通应用、启动、工程和专属施工/验证记录。禁止：线程文件、L1、世界树、自我形成、方法根、动态/因果、旧`.ixx`、配置根编码。

## 原实施步骤（仅在激活门禁满足后适用）

1. 复用已发布的上游特征类提供者；阶段21只请求每根的实际F并严格处理其结果，所有非成功空锚点。
2. 实现独立根D/根列表成员及目标合同的固定键收敛与读回，不定义或改变根F内部事实。
3. 实现业务DTO/provider，最终Gfinal互证、双根锚点形成及发布未知原请求重放。
4. 装配需求服务和provider，增加普通应用唯一入口；启动阶段19成功后调用并映射21，仍不创建线程。
5. 编写施工/验证记录，运行严格规范检查、隔离业务驱动、Debug/Release Rebuild；仅提交本切片。

验收：仅经上游提供者取得实际F、阶段21未绕过提供者操作F或模拟其内部机制、根D首次形成 / 精确重复 / 部分前缀续行、任一异义/退出/未知/漂移失败及零线程副作用；双根实际F不交叉且最终G一致。根F自身的表示、变更、重启和内部事务由上游提供者验收；未覆盖真实长时运行、跨进程故障恢复、线程及后续治理。
