# INSTINCT-ROOT-RUNTIME-ANCHOR 本能根运行锚点代码实施计划 v0.1

状态：待激活，等待剩余物理合同裁决。退回基线：`5e50fb99ac7d190c7f71c7dc4a5a1f4627fa0c05`。

目标：在机器语义重新冻结后实施8121阶段21，形成或恢复安全/服务双根的同截止值式锚点；启动在失败时阶段21短路。完成不证明线程、治理循环或业务闭环。

## 激活门禁

执行 S0 已具名退回，不能靠代码裁决。只有下列四项进入8121并同步更新详细设计后，本计划才可重新标为`可执行`：

1. 4110 / 4205 所需的两根 F 的`C/P/主holder/FC`，以及十类固定幂等身份的命名域和完整规范化请求；
2. 首值、初态和当前状态选择的唯一 owner、参与者、关系、写集、本地映射、原请求重放和最终读回；
3. 目标合同、独立根 D、根 L 与成员的 ARCH-L4 DTO、关系、原子发布、恢复和读回；该合同不得为根 D 添加所属存在、目标宿主、自我字段、关系或 owner；
4. 首次初态 UTC 的唯一来源、双根是否共用、完整首次材料和失败映射。

已裁决边界：保留 `M-X` 定义和瞬时差值投影；删除旧方向差值的持久化生产路径。需求服务不是二次概念的替代者。安全 / 服务双根 D 是独立根节点，只具有独立身份、根特征、根目标合同、父空、顺序0与根列表闭包；阶段21入口和最终锚点表达其逻辑自我归属，根 D 不得有自我物理绑定，且阶段21不处理任务实体或任务物理关系。

允许：详细设计列出的特征、状态、需求、业务provider、普通应用、启动、工程和专属施工/验证记录。禁止：线程文件、L1、世界树、自我形成、方法根、动态/因果、旧`.ixx`、配置根编码。

## 原实施步骤（仅在激活门禁满足后适用）

1. 在ARCH-L2/ARCH-L4分别实现8121的公开请求、结果、首次读回和跨owner原子入口；所有非成功空载荷。
2. 实现根定义、二次概念、独立根特征信息、首值初态、比较合同、独立根D/根列表成员的固定键收敛与读回。
3. 实现业务DTO/provider，最终Gfinal互证、双根锚点形成及发布未知原请求重放。
4. 装配需求服务和provider，增加普通应用唯一入口；启动阶段19成功后调用并映射21，仍不创建线程。
5. 编写施工/验证记录，运行严格规范检查、隔离业务驱动、Debug/Release Rebuild；仅提交本切片。

验收：首次形成、精确重复、重启不重置、部分前缀续行、任一异义/单边/退出/未知/漂移失败及零线程副作用；双根身份不交叉且最终G一致。未覆盖真实长时运行、跨进程故障恢复、线程及后续治理。
