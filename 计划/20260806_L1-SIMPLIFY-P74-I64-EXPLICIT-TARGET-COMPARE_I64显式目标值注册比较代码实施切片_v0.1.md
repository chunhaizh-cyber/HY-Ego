# L1-SIMPLIFY-P74 I64 显式目标值注册比较代码实施切片 v0.1

计划身份：`L1-SIMPLIFY-P74-I64-EXPLICIT-TARGET-COMPARE`

计划版本：v0.1

创建基线：`cf31e58424e31c461d8c14d43343ee664412c917`

初始状态：待激活

启动聚合顺序：574

## 1. 目标、依据与完成条件

按以下唯一设计包，在现有特征比较域实现 I64 显式目标值合同登记、正式读回和同截止只读比较：

- `规范/详细设计/20260806_L1-SIMPLIFY-P74-I64-EXPLICIT-TARGET-COMPARE_I64显式目标值注册比较详细设计_v0.1.md`
- `流程图/20260806_L1-SIMPLIFY-P74-I64-EXPLICIT-TARGET-COMPARE_比较显式I64目标函数流程图_v0.1.md`
- 3100、4130、4180、5100、6130 及代码文件归属规范

完成必须同时成立：

1. 三个公开入口及 DTO、枚举数值、版本、const/noexcept 边界与详细设计一致；
2. 新 `0x17` 注册域、`0x50337401` 操作标签、九关系、配置材料、同义/异义和多合同唯一路由闭合；
3. P6 上界专用合同和 P11 迁移合同 ABI/行为不变，三类合同可并存；
4. 比较函数只读一次同截止 L1 快照，返回正式三态/关系/可选差异并零写入；
5. Debug/Release Rebuild、完整自检、专项矩阵、strict110/110 和模块扫描通过；
6. 专属施工与验证记录准确限定证明边界。

本计划不得标记可执行，直至第 6 节全部激活门禁由交互侧确认。

## 2. 允许文件

```text
海中鱼巣/领域/特征比较.数据.h
海中鱼巣/领域/服务.特征比较.ixx
海中鱼巣/领域/自检.特征比较.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
海中鱼巣/启动.应用程序.ixx
施工记录/20260806_L1-SIMPLIFY-P74-I64-EXPLICIT-TARGET-COMPARE_施工记录.md
验证记录/20260806_L1-SIMPLIFY-P74-I64-EXPLICIT-TARGET-COMPARE_验证记录.md
```

禁止修改 P15、P17—P40、P53—P73、L1 核心、需求/6170/装配、运行期、仓库、SQL、恢复、旧 WORLD-TREE 和计划索引。若现有接口漂移导致需要修改禁止文件，具名退回计划支撑，不得扩白名单。

## 3. 实施步骤

1. 从正式 main 重做 S0：核对目标计划版本、P6/P11/P15/P65/P69/P72/P73 真实接口、允许文件所有权、dirty、Git index、HEAD/origin 与顺序574。
2. 在 `特征比较.数据.h` 增加显式合同登记/读取、显式目标见证和比较请求 DTO；保持既有类型字段顺序和数值不变。
3. 在 `特征比较服务` 增加按特征定义索引的显式合同集合；不得覆盖 P6 `合同_` 或 P11 `迁移合同_`。
4. 实现登记前纯值校验、`0x17` 幂等映射、`0x50337401` 规范摘要、一次 P15/L1 v2 空参与者提交、九关系/配置形成及同锁读回。
5. 实现正式合同读取；零项、失效、多项、版本漂移和结构损坏按详细设计映射。
6. 实现 `比较显式I64目标`：请求校验后复制唯一合同，一次读取完整快照，互证注册、左当前事实和右目标状态事实，形成三态/关系/可选安全差异。
7. 在专项中建立安全/服务两个定义和目标状态值事实，覆盖双合同并存、P6/P11 回归、全部非成功、极值、并发与零写边界。
8. 工程与 filters 只作既有文件所需登记核对；启动模块增加唯一薄适配和顺序574登记，登记/执行/摘要期望数同步加一。
9. 形成专属施工记录和验证记录，逐项记录实际命令、输出、提交、未验证范围和最大声明。

## 4. 机器合同门禁

- 新合同唯一键是特征定义、目标判断用途、标量有序比较与安全差异、算法版本1；业务调用方不能选择注册。
- 显式目标值由目标状态当前属性槽承载，所属节点、属性类型、来源服务、创建代次和预计值全部互证；不得伪装为 P2 上界。
- 左值固定为当前事实，右值固定为目标状态，排序为 `compare(左,右)`，差异固定 `右-左`。
- 比较成功只返回方向材料；安全“大于”和服务“等于”模板命中、当前满足和持久化全部为0。
- 登记是唯一写路径；比较/读取中 P15、P37、P54 和任意需求 provider 调用均为0。
- 不自建账、参与者、稳定编码器、缓存、锁外权威路由或第二快照。

任一项不能成立即停止，不得用测试夹具、日志、名称、显示文本或调用方 bool 补足机器语义。

## 5. 验证命令与证据

执行者按实际工程入口运行并保存完整输出，至少包括：

```powershell
git status --short
git diff --check
python .\tools\check_specs.py --strict
rg -n "I64显式目标判断比较|比较显式I64目标|0x50337401|顺序574" 海中鱼巣
rg -n "满足|父需求|路径|6170|任务|装配" 海中鱼巣/领域/特征比较.数据.h 海中鱼巣/领域/服务.特征比较.ixx
```

另须完成：

1. Debug 和 Release Rebuild；
2. 两配置 `--self-test-exit` 与完整登记/执行/摘要计数一致；
3. P74 专项覆盖详细设计 §8 全矩阵；
4. 两配置模块声明/编译产物扫描按正式施工规范通过；
5. 允许文件外 diff 为0，Git index 只含本计划精确结果切片。

不得把文档检查、编译、自检或比较成功单独升级为当前满足、6170 闭环或生产可用证明。

## 6. 激活、资源与接口匹配

激活必须等待：

1. P2、P3/P4、P6、P11 与 P15 最终真实代码结果进入正式 main 并经交互接受；
2. P65、P69、P72、P73 真实结果进入正式 main，实际 DTO 和本设计消费字段逐项匹配；
3. 创建基线中的 P15 v0.28 已替代并激活为可执行计划，但仍须由其未来真实代码结果或正式后继证明可承载本叶注册操作，并释放共享代码、工程、构建、自检、模块扫描和 Git 发布资源；其内部 DTO 不进入本计划；
4. 新 HEAD 确认无等价显式目标注册比较 provider，`0x17`、`0x50337401`、顺序574和三个公开入口唯一；
5. 八个允许目标与异主 WIP 零路径/零语义重叠，Git index 空且取得唯一活动代码计划段。

若上游只存在设计或待激活接口，不得把它宣称为已实现；只能保留本计划待激活。P15 再次具名漂移时只等待其正式替代结果，不复制或预判支撑语义。

## 7. 非成功、回滚与停工

登记、读取和比较的状态/拒绝原因必须按详细设计逐项映射。提交成功后读回失败不得重写或回滚已提交事实，应保留发布代次并结构化返回内部不一致；同义重试不得推进代次。比较期间任何注册或输入版本变化只使本轮结果失效，不写补偿事实。

以下情况具名停工并退回设计/计划支撑：实际 P6/P11 ABI 与冻结合同不符；P15 不能承载新注册操作；P65/P69/P72/P73 缺少必需稳定身份或见证；同定义唯一路由无法在现有 L1 事实中表达；必须修改禁止文件；或专项持续失败且无法在本计划范围归因。

## 8. 完成声明边界

允许声明：`P74 I64显式目标值注册比较源码候选已按正式注册在同一L1截止形成三态、具名关系和可选安全差异，并通过本计划验证矩阵。`

禁止声明：P15/P65/P69/P72/P73 已由本计划实现，安全/服务当前满足、需求满足事实、父子/路径、6170维护、差距、条件性子需求、任务、完整自我连接、初始化/生产装配、恢复、旧栈删除、正式集成验收、STEP-5 或治理闭环完成。
