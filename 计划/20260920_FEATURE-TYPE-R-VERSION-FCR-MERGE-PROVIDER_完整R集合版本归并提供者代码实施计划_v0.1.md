# FEATURE-TYPE-R-VERSION-FCR-MERGE-PROVIDER 代码实施计划 v0.1

状态：已退出。基线：`6c05c0c40694aaeb61971a4a16110478cbc9efe5`。代码结果：`78fcfeebe`。

## 目标与完成条件

把已发布 `特征类数据服务::归并特征R概念` 从单 R项输入收敛为4115 §4要求的完整 R集合版本输入、该版本完整 FCr 定义组输出。I64 规则仅在全版本形状、成员和预算均核验时输出零个或多个规范化定义；单项 ABI、兼容重载、别名和调用路径零源码命中。本计划只提供规则 ABI，不保存 IF/R 或 FCr。

## 依据、范围与禁止

依据为4115 §4、4120 §3.3、7150、7160及[详细设计 v0.1](../规范/详细设计/20260920_FEATURE-TYPE-R-VERSION-FCR-MERGE-PROVIDER_完整R集合版本归并提供者详细设计_v0.1.md)。特征类型定义 owner 唯一拥有本切片。允许 `海中鱼巣/领域/数据服务.特征类.h/.cpp`、确有必要工程登记和专属施工/验证记录；禁止存在类、概念树类、特征概念应用、普通应用、L1、R规则物理结构、非 I64、所有 F/IF/R/FCv/FCr/概念关系写入、回执、恢复、线程和阶段21。

## 冻结施工与验证

按详细设计的版本2 DTO和唯一 `noexcept` 签名替换当前版本1单项请求；完整遍历并核验版本，逐项域形成后全量规范化输出，任一异常零部分输出；删除旧单项路径。用户于 2026-09-20 正式取消完整版本行为夹具验证，不再建立、运行或要求该夹具。保留已完成的隔离 Debug/Release x64 编译事实及翻译探针；strict 已取消，不运行或替代。通过不证明 IF/R、FCr持久写入、概念关系、恢复、装配、运行或业务验收。

## 后继

无上游依赖。`INSTANCE-FEATURE-IF-R-CONCEPT-FOUNDATION` 是唯一直接待激活后继；只有本计划精确代码结果发布后才可重新 S0，旧 `FEATURE-TYPE-R-INTERVAL-RULE-PROVIDER` 单项 ABI 不解除门禁。
