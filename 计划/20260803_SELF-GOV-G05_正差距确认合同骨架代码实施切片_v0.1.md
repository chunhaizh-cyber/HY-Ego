# SELF-GOV-G05 正差距确认合同骨架代码实施切片 v0.1

计划身份：SELF-GOV-G05-F0

版本：v0.1

创建侧当前代码基线：`4b80f1617dd70ec7a19e1a34efa9da768dce8927`

## 1. 目标、依据与完成条件

在 COMPARE-F 最终比较类型真实进入正式代码后，于现有 `需求业务服务` 中建立 `确认当前正差距` 的最终需求包装 DTO、稳定状态和固定 `未实现` 成员函数；复用既有需求任务方法分层自检完成安全入口巡检。只允许取得 `编译接入通过` 与 `入口巡检通过`。

直接依据：

```text
规范/3100_根规范_需求_20260720.md
规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md
规范/4180_子规范_特征值域差异算子与比较算法注册.md
规范/5090_子规范_需求任务方法运行闭环与独立结果回读.md
规范/6180_子规范_自我稳定身份项目资格与具名事实范围.md
规范/8300_子规范_自我内部循环最小生命闭环验收_20260720.md
规范/运行期服务导向代码验收与全函数入口巡检规范.md
规范/详细设计/20260803_WORLD-TREE-COMPARE-F_特征比较最终合同骨架详细设计_v0.1.md
规范/详细设计/20260803_SELF-GOV-G05_正差距确认合同骨架详细设计_v0.1.md
流程图/20260803_需求业务服务确认当前正差距_函数流程图_v0.1.md
```

完成条件：最终 ABI 精确进入正式模块；函数对任意输入固定未实现；专属自检实际进入并证明全字段、零调用依赖和零事实变化；要求的构建、完整自检与静态门禁通过。

## 2. S0 门禁

执行前从正式 Git 确认：

```text
服务.需求.ixx 仍唯一拥有 需求业务服务，构造仍为两个引用依赖；尚无同名函数/DTO。
核心/节点直接结构合同.数据.h 的复用稳定身份类型未漂移。
海中鱼巣.领域.服务.特征比较 已进入正式代码，完整 特征比较算法族/特征比较结果 与 COMPARE-F 详细设计逐字段一致，且其结果提交已释放模块类型所有权。
自检.需求任务方法分层.ixx 仍 import 服务.需求、构造 需求业务服务，并由正式完整自检进入。
目标两代码文件无其它活动写入所有者；Git index 和唯一代码计划段可取得。
```

任一不成立，零代码修改返回具名设计漂移；不得建立同义新模块、重载或兼容包装。

## 3. 允许与禁止文件

允许：

```text
海中鱼巣/领域/服务.需求.ixx
海中鱼巣/领域/自检.需求任务方法分层.ixx
施工记录/20260803_SELF-GOV-G05_施工记录.md
验证记录/20260803_SELF-GOV-G05_验证记录.md
```

其它全部禁止，尤其禁止修改 vcxproj、filters、启动、自我线程、世界树/比较模块、仓库、事务、SQL、规范、流程图、计划和计划索引。

## 4. 机械施工

1. 在 `服务.需求.ixx` global module fragment 增加 `../核心/节点直接结构合同.数据.h`；import `海中鱼巣.领域.服务.特征比较`，保持现有 include/import 和两个成员引用不变。
2. 逐字实现详细设计 §3 的合同版本、枚举和四个需求结构，字段顺序、底层类型、默认值不得改变；直接使用比较模块的完整类型，禁止复制比较子结构。
3. 在 `判定目标状态达成` 前增加详细设计 §3.3 的唯一公开成员；函数体只能 `return {};`，保持已移交的 `const` 签名，不得新增异常说明符、读取参数或调用依赖。
4. 在既有需求任务方法分层自检中增加：枚举数值与 DTO/函数指针类型 `static_assert`；一次空请求实际调用；调用前后结构数量相等；结果精确为未实现、零请求身份、0、空载荷、false、false。新增一项具名验收并同步数组/名称数量，不覆盖既有项目。
5. 形成专属施工和验证记录；不得在记录中升级业务完成声明。

## 5. 验证

```powershell
git diff --check
python .\tools\check_specs.py --strict
msbuild .\海中鱼巣.vcxproj /m /p:Configuration=Debug /p:Platform=x64
msbuild .\海中鱼巣.vcxproj /m /p:Configuration=Release /p:Platform=x64
.\x64\Debug\海中鱼巣.exe --self-test-exit
.\x64\Release\海中鱼巣.exe --self-test-exit
```

另以 Clang AST 或等价源码结构检查证明：完整限定函数唯一；函数体只有固定返回；生产直接调用方为 0；专属自检调用方为 1；被调项目函数为 0。静态检索必须确认函数体不触及数据操作、状态服务、世界树、比较、SQL、仓库、许可、事务、日志和时钟。

任何构建、自检、全字段、计数、调用边或零事实变化失败都保留 WIP，不声明完成。

创建侧独立只读复核第二轮 PASS：本计划直接复用 COMPARE-F 完整结果、保持真实类型依赖待激活，字段来源无 G05 自循环，固定 `return {}`、现有自检 L1 路径、白名单和弱完成声明均可机械实施。

## 6. 依赖与后继

本骨架具名依赖 WORLD-TREE-COMPARE-F v0.1 的代码结果提供最终比较类型；在该结果进入正式 main 前，本计划必须登记为 `待激活`。它不依赖 COMPARE-R、WORLD-TREE-P1A3、P1B 或 P1C 的真实成功行为。下列后继仍保持待激活：

```text
读取需求目标当前事实 提供者
COMPARE-R 按注册比较合同裁决目标差距 提供者
确认当前正差距 真实实现原位替换
G06 差距准入需求及生产自我治理消费者
```

接口存在、编译通过或入口巡检均不能满足这些真实依赖。

## 7. 完成声明边界

允许：SELF-GOV-G05-F0 最终合同骨架已通过编译接入与安全未实现入口巡检。

禁止：真实正差距已计算、消费者局部可继续、G01—G06 完成、需求层闭合、自我内部治理闭环或服务验收通过。
