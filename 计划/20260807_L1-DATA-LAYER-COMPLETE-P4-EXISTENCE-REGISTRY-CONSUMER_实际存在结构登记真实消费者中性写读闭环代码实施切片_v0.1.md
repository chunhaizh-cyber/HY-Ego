# L1-DATA-LAYER-COMPLETE-P4-EXISTENCE-REGISTRY-CONSUMER 实际存在结构登记真实消费者中性写读闭环代码实施切片 v0.1

日期：2026-08-07

状态：待登记；登记后可执行

## 1. 目标、完成条件与依据

按配套详细设计机械实施：将现有 L2 实际存在登记根迁移到 P1 中性 CRUD，并接入正式生产启动。完成必须同时具备：一个两节点中性写集恰一次提交、结构化成功/失败、正式两节点权威读回、生产调用及候选完整性门。

依据：4015 v0.5、4030 v1.3、4200 v1.2、ACCEPTANCE-01 v0.3、P1 中性合同、P2/P3 已发布消费者合同，以及：

- `规范/详细设计/20260807_L1-DATA-LAYER-COMPLETE-P4-EXISTENCE-REGISTRY-CONSUMER_实际存在结构登记真实消费者中性写读闭环详细设计_v0.1.md`
- `流程图/20260807_L1-DATA-LAYER-COMPLETE-P4-EXISTENCE-REGISTRY-CONSUMER_建立实际存在登记函数流程图_v0.1.md`

## 2. S0 与激活门禁

登记后可标为 `可执行`。创建侧已确认 P3 代码、消费者验收和计划退出均已普通推送并经交互接受；P1 中性合同版本 1、九写状态、七读状态和正式入口机械匹配；实际存在登记生产调用当前为零；本计划五个代码文件无异主 WIP；唯一活动代码段、Git index、构建、运行和模块扫描资源可用。执行前仍须从登记后的正式新 HEAD 重做 S0，不得把日志、退出码或消息当作门禁完成。

## 3. 精确允许范围

只允许修改：

1. `海中鱼巣/领域/L1实际存在.数据.h`
2. `海中鱼巣/领域/服务.L1实际存在.ixx`
3. `海中鱼巣/生产运行期配置.数据.h`
4. `海中鱼巣/启动.运行期上下文.ixx`
5. `海中鱼巣/启动.生产运行期.ixx`
6. `施工记录/20260807_L1-DATA-LAYER-COMPLETE-P4-EXISTENCE-REGISTRY-CONSUMER_施工记录.md`
7. `验证记录/20260807_L1-DATA-LAYER-COMPLETE-P4-EXISTENCE-REGISTRY-CONSUMER_构建与生产链验证.md`

禁止修改计划、索引、规范、流程图、工程/filters、L1 核心/仓库、场景组合、实际存在创建/资格读取、特征、状态、动态、旧系统角色实现、自检或集成验收目录。禁止触碰或夹带 P0-FIX、路线、旧 WORLD-TREE、`.codex-build` 和其它 WIP。

## 4. 实施步骤

1. 在实际存在数据头升合同 v2，规则保持 1，新增登记请求有效性和结果成功判断；现有 DTO 字段和状态枚举值不重排，登记事实截止明确为本次当前读截止。
2. 重写 `建立登记`：删除 G6 编码、领域结果见证计划/反建、预探测、完整快照、legacy 提交和首次请求缓存；形成 2节点+0关系+0值+0槽+0退出的中性写集，域前缀 0x0A，恰一次提交中性写集并完整校验结构化结果。
3. 新增私有两节点精确读回：首末事实代次夹取，读取普通服务身份和 I64 属性类型节点，验证同一创建代次、当前性和字段；缓存只保存首次幂等身份与两编码定位。`读取当前登记()` 必须走该读回并返回当前截止。
4. 保留 `形成实际存在写入规格`、`读取实际存在资格` 和 `L1实际存在场景组合服务` 的公开合同及既有路径；不得把本叶静态门禁扩大到整个存在模块。
5. 在生产配置增加固定实际存在登记幂等身份；在运行期上下文导入、构造并暴露唯一 `L1实际存在服务`，增加初始化/当前读/完整性转发，把世界登记、初始场景和实际存在登记同截止纳入 `完整()`。
6. 在生产启动结果增加末尾状态值 12 和实际存在登记业务状态；初始场景成功后构造完整登记请求并恰一次初始化，失败立即停止，成功后才进入旧系统角色。
7. 写两份专属记录，逐项记录实际变更、命令、结果和未证明范围。不得写 L1 PASS、日志 PASS 或整个数据层完善。

任一步出现正式接口、字段、所有权或启动顺序漂移且会改变上述机器语义时，保留现场并具名退回计划支撑；普通编译错误在白名单内修复。

## 5. 验证合同

至少执行并记录：

```powershell
git diff --check -- <七个白名单路径>
python .\tools\check_specs.py --strict
MSBuild.exe .\海中鱼巣.slnx /t:Rebuild /p:Configuration=Debug /p:Platform=x64
MSBuild.exe .\海中鱼巣.slnx /t:Rebuild /p:Configuration=Release /p:Platform=x64
<Debug生产程序> --runtime-context
<Release生产程序> --runtime-context
python .\tools\check_msvc_module_dependencies.py --configuration Debug
python .\tools\check_msvc_module_dependencies.py --configuration Release
```

实际工具参数以仓库正式脚本帮助为准，不得为了命令字面一致绕过正式入口。成功条件：两配置重建和生产入口退出 0；strict 110/110；两配置模块扫描全数通过；精确白名单无额外 staged 路径。

静态/源码门禁：在 `建立登记` 与当前登记精确读回路径内，`探测幂等`、完整快照、legacy `提交写集`、`L1领域意图凭证`、`L1领域结果见证计划`、`0x15000301U`、G6 摘要为 0；`提交中性写集` 物理调用点恰 1；生产登记初始化调用点恰 1 且晚于初始场景、早于系统角色；读回中两次事实代次和两个节点入口齐全。

`形成实际存在写入规格`、`读取实际存在资格` 和 `L1实际存在场景组合服务` 不属于本叶生产登记根调用链，允许保留其现有 legacy 读取/写入，留待后续实际存在创建消费者自己的中性迁移计划；本叶不得删除公开资格或场景接纳合同。构建、退出码和静态扫描只证明施工门禁，不是消费者合同验收。

代码发布后另由集成验收智能体绑定精确提交，经生产正式入口、结构化启动结果、租约公开实际存在登记读回以及世界登记/初始场景同截止结果验收；不得直调 L1、建立专用驱动或用日志代替结果。

## 6. 完成声明边界

计划施工完成最多证明：实际存在登记消费者的中性代码候选、生产接线和施工门禁成立。独立消费者验收接受后，才可累计两个节点的新增/查询/原子性实际覆盖。

不得宣称精确重复、非法请求零变化、关系、值、属性槽、修改、退出/历史、场景中实际存在创建、特征、状态、动态、全部消费者、旧栈删除或整个 L1 完善；不得考虑崩溃或跨进程恢复。
