# L1-SIMPLIFY-P1-WORLD-STRUCTURE 世界结构事实迁移待激活叶 v0.2

计划身份：`L1-SIMPLIFY-P1-WORLD-STRUCTURE`

版本：v0.2

状态：待激活；本文件只刷新 P0-FIX 依赖，不是代码施工许可。

## 1. 最小目标

在新 L1 三类事实基座上形成首个世界结构领域写读闭环：由具名 L2 领域服务消费长寿命 `L1事实基座服务`，提交一个最小世界结构变化，并从另一读取途径重新取得权威当前事实，以重读状态而非动作返回预估裁决结果。

本版本不细化节点、世界身份、资格、DTO、物理文件、事务或自检顺序；这些只能在依赖结果进入正式代码后重新设计。

## 2. 具名门禁

1. `L1-SIMPLIFY-P0-FIX v0.2` 必须先形成真实代码结果，证明审计查询键、恢复结果代次、错误分类、类型形状、规范化幂等、提交异常安全和恢复一次替换。
2. 计划智能体从该正式结果重新读取 P0 DTO、服务签名和实际行为后，建立新的完整可执行版本；不得沿用本待激活文件或旧 P1A3/P1A3F 开工许可。
3. 世界结构资格/登记提供者、长寿命 L1 实例、动作预计动态和另一途径权威重读未冻结前保持待激活；不得用固定编码、显示名、配置、旧句柄、自检替身或空骨架解除门禁。

## 3. 完成边界

本版本不允许代码或工程写入，不登记白名单。发布只证明最小后继目标和 P0-FIX v0.2 依赖已登记；不证明世界结构计划可执行或任何世界能力已实现。

旧 v0.1 plan blob `f8dc986590e0b5cadc54f4bea9abdac312dda1df` 只作历史路线证据。
