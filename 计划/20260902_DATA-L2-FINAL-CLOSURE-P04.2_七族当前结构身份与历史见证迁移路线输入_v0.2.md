# DATA-L2-FINAL-CLOSURE-P04.2 世界七族当前结构身份与历史见证迁移路线输入 v0.2

日期：2026-09-02

材料身份：`DATA-L2-FINAL-CLOSURE-P04.2-WORLD-SEVEN-MIGRATION-ROUTE-INPUT`

状态：设计治理输入 / 路线校正。本文件替代 v0.1 中已经被后续发布事实推翻的前置状态；v0.1 的旧调用位置清单只保留为其形成时基线的观察记录。本文件不是正式规范、代码实施计划、计划索引项、待激活库存或施工许可。

## 1. 已核实的状态校正

1. 当前正式 Git 基线为 `main == origin/main == 50ada2898aaa339324bd24a93bf4fadc0e606e3b`；任何后续代码计划仍须按届时 `HEAD`、index、调用点、所有权和验证资源重新 S0。
2. `DATA-L2-FINAL-CLOSURE-P04.1.1-HISTORICAL-CUTOFF-SELF-PROOF` 已由代码提交 `df66cab60e588ea9794eda1b6c0db853202cd26a` 落实，并在现行 `计划/计划索引.md` 的历史条目中登记退出。它只闭合状态、动态、因果三族旧历史成功谓词的同截止自证；不提供 P04.2 的新公开 ABI，也不证明 DATA-L2 已冻结。
3. 用户已确认 P04.2-2：七个旧 `读取{族}身份来源` 的请求、结果、公开声明、定义和调用必须在同一消费者迁移切片完成后物理删除；不得保留转发、别名、拒绝壳、兼容开关、隐藏 overload 或长期双入口。
4. P04.2-1A/B 已确认：当前读取以 `G0` 为一致性边界，由 provider 内部证明目标及其结构来源均当前且不公开生命周期；适用的历史、退出、恢复和审计读取以 `Gread` 和非零 `H <= Gread` 取得完整历史见证，成功时 `H == 结果头.事实截止代次`。

## 2. 尚未冻结的最小问题

P04.2-1C 仍待用户裁决：当前读取的公开 payload 是否只回显既有 `L2{族}身份`（或只返回成功结果头），还是需要另一份结构投影。不得因为旧完整见证去掉生命周期后恰好剩六项，就把六项投影伪称为新的必需数据字段。

当前代码的共同 `L2节点族身份来源事实` 已有六个结构字段和四份生命周期，且被七族以外的完整读取复用；它不能被就地收缩。当前端点究竟返回哪些字段，必须先按“调用方是否真的需要该字段”裁决。该决定未形成前，`L2世界七族结构身份` 只是不具施工许可的候选名称，不能写入正式规范、代码计划或代码。

未随 P04.2-2 一并裁决的事项还包括：新 request/result/函数的精确名称、当前与历史的成功谓词和可观察失败映射、P08 的状态/动态退出细节、4070 恢复格式，以及 DATA-L3 / 语言 / ARCH-L4 的独立 ABI。它们均不得借“旧入口删除”被默认改变。

## 3. 后续形成单一代码计划的门禁

1. 用户完成 P04.2-1C 及其余必要公开合同裁决；合法设计所有者据此将确认项同步至正式规范、规范目录和详细设计，并排除未确认草案内容。
2. 基于新正式基线重新枚举全部旧入口消费者：当前用途迁移至当前合同，历史 / 重放用途迁移至历史见证或既有专属退出路径；状态—动态原子发布必须保留首次 `G0` 与非首次 `H` 的分支。
3. `服务.L2需求结构.ixx`、`端到端测试.自我治理一致事实.ixx`、`装配.普通应用.ixx` 等异主 WIP 完成、交接或被明确排除；DATA-L2 不覆盖它们的现场。
4. 形成一份具名、完整、可验证的代码实施计划后，才由交互智能体统筹登记到 `计划/计划索引.md`；执行智能体仍须在启动时 S0，索引状态本身不替代该检查。

## 4. 完成声明边界

本文件只校正 P04.2 的设计输入和下一步门禁。它不证明正式规范已同步、任何 P04.2 代码已修改或构建、历史恢复可用、旧入口已删除、消费者已迁移、服务已验收，亦不把 DATA-L2 标记为 `FROZEN`。
