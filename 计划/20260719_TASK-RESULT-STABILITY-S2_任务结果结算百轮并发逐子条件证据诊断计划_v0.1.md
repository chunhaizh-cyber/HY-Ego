# TASK-RESULT-STABILITY-S2 任务结果结算百轮并发逐子条件证据诊断计划 v0.1

日期：2026-07-19

执行队列：#313

设计队列：DQ-205

设计裁决：JY-454

状态：纯诊断 / WB-DIAG-03 / WT-313-R1 已预留待创建 / 冻结候选 `0890464` / 临时自检证据改动最终必须恢复 / 不登记自检阶段

## 1. 目标与依据

本计划只回答一个问题：冻结候选 `0890464414a8ffd5dfc71dcfc80641d9e4f18181` 再次出现 TASK-RESULT-S1 A14 或 A18 失败时，究竟是哪一个已存在判定子条件为假，以及两组百线程结果分别落入了哪些治理状态、任务提交状态、需求结算状态和拒绝原因。

#312 已证明第 17 轮直接观察到 A14、A18 失败，但现有 A01—A18 输出只给出聚合布尔。现有证据不能区分：

```text
完成并发任务提交数量是否不是 1
完成并发需求结算提交数量是否不是 1
完成并发 100 个结果是否存在非收口状态
预先完成是否失败
独立结算提交数量是否不是 1
独立结算 100 个结果是否存在非收口状态
两组正式结算读回的完整性、来源任务或动态证据是否漂移
A18 的夹具、权威读回、未达成分支或共享收口计数是否另有失败
外层 catch 是否吞掉了异常
```

本计划不改变业务流程、机器结构、公开接口、验收语义、并发数量或阶段登记。新增流程图和详细设计不适用；替代正式依据为：

```text
流程图/20260715_任务结果完成与需求独立结算代码逻辑流程图_v0.1.md
规范/详细设计/任务结果完成与需求独立结算详细设计.md
计划/已完成计划/20260711_TASK-RESULT-S1_任务结果完成与需求独立结算代码实施切片_v0.1.md
计划/已完成计划/20260719_TASK-RESULT-STABILITY-S0_任务结果结算间歇失败基线复现与叶子证据采集诊断计划_v0.1.md
计划/已完成计划/20260719_TASK-RESULT-STABILITY-S1_任务结果结算间歇失败候选对照与叶子证据采集诊断计划_v0.2.md
实施记录/20260719_TASK-RESULT-STABILITY-S1_任务结果结算间歇失败候选对照诊断_Codex断点清单.md
实施记录/20260719_TASK-RESULT-STABILITY-S1_任务结果结算间歇失败候选对照诊断独立集成记录.md
海中鱼巣/线程/自检.任务结果结算.ixx
```

流程图、详细设计和原 TASK-RESULT-S1 计划继续定义业务与验收语义；#312 正式记录和当前源码定义本诊断的事实范围。三者没有业务范围冲突。本计划只增加失败后的观察输出，不能用输出承载机器逻辑或修改验收结论。

## 2. 已验证当前事实

```text
main == origin/main == e198c6ec722ed202e67df9f7efd2a7f39c761c23，WT-MAIN clean。
#312 任务记录和独立集成记录已经进入 main。
冻结候选 0890464 的唯一父是 1d95aa5，精确包含 #310 三份代码草案和一份 WIP 记录。
任务结果结算自检文件在 1d95aa5 与 0890464 的 blob 均为 74734d038e3baa0be12a46ee1f275273305b7bf5。
#312 第 1—16 轮通过，第 17 轮 exit 1；TASK A14、A18 失败，B3C / B0 无失败。
第 17 轮完整日志 SHA-256 为 638A2623F691993ACE67FE2B9762AFE8B76F8469FC52431F130FE5C38B6FACEA。
A14 当前由 14 个提交数量、收口数量、预先完成和正式结算读回条件合取形成。
A18 当前由 11 个夹具、权威读回和收口数量条件合取形成。
两组 100 线程均已在读取统计和验收前 join；因此失败后输出可放在 join 之后，不改变并发窗口。
固定 D:\海中鱼巣\日志\事件段\自检 是跨 worktree 共享写入路径，完整自检必须串行独占。
```

## 3. 身份、范围与所有权

```text
批次：WB-DIAG-03
任务 worktree：WT-313-R1
预留路径：C:\Users\zhchh\.codex\worktrees\313-task-result-subcondition-r1\海中鱼巢
预留分支：codex/313-task-result-subcondition-r1
冻结候选提交：0890464414a8ffd5dfc71dcfc80641d9e4f18181
候选父提交：1d95aa5975057965fcfa179c56521ad69e5b5b6a
冻结自检 blob：74734d038e3baa0be12a46ee1f275273305b7bf5
共享运行资源：D:\海中鱼巣\日志\事件段\自检，仅在设计窗口明确授予时隙后独占
```

运行期间允许临时修改：

```text
海中鱼巣/线程/自检.任务结果结算.ixx
```

永久 Git 写入只允许：

```text
实施记录/20260719_TASK-RESULT-STABILITY-S2_任务结果结算百轮并发逐子条件证据诊断_Codex断点清单.md
```

非 Git 原始证据只允许写入：

```text
D:\海中鱼巣\日志\诊断\TASK-RESULT-STABILITY-S2\**
```

临时自检文件在运行结束后必须用执行窗口自身的反向 `apply_patch` 恢复，并用 `git hash-object` 证明 blob 精确回到 `74734d038e3baa0be12a46ee1f275273305b7bf5`。最终任务提交不得包含该文件。

禁止修改：

```text
海中鱼巣/** 中除上述临时自检文件外的全部文件
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
海中鱼巣/入口.cpp
AGENTS.md
规范/**
流程图/**
计划/**
项目记忆/**
WT-310-R1、WT-305-R1、WT-312-R1 及其分支
```

禁止修改原 A01—A18 布尔表达式、失败语义、100 线程数量、请求、夹具、调度顺序、阶段 610 或顶层汇总。禁止增加锁、睡眠、重试、随机种子、提前输出、线程内输出或任何提高通过率的逻辑。

## 4. 临时逐子条件证据合同

临时输出只能在两组 100 线程全部 join、现有读回完成、现有 `验收[13]` 和 `验收[17]` 已按原表达式计算后执行。只有 A14 或 A18 为假时输出；通过轮不得输出 TASK-RESULT-STABILITY-S2 专项行。

### 4.1 A14 十四项

按固定顺序输出真 / 假和值式计数：

```text
完成提交数量 == 1
完成结算提交数量 == 1
完成收口数量 == 100
预先完成.成功()
独立结算提交数量 == 1
独立结算收口数量 == 100
完成并发需求正式结算存在
完成并发需求正式结算完整
完成并发需求来源任务一致
完成并发需求动态证据一致
结算并发需求正式结算存在
结算并发需求正式结算完整
结算并发需求来源任务一致
结算并发需求动态证据一致
```

可选值必须先判断 `has_value()`，不得为诊断输出引入空值解引用。

### 4.2 A18 十一项

按固定顺序输出：

```text
夹具完整
达成任务读回完整承接
达成需求读回完整目标状态需求
达成需求正式结算存在
恢复结算需求正式结算存在
完成并发需求正式结算存在
结算并发需求正式结算存在
未达成需求正式结算不存在
完成收口数量 == 100
未达成收口数量 + 未达成漂移数量 == 100
独立结算收口数量 == 100
```

### 4.3 两组百线程结果

失败轮必须对 `完成并发结果` 和 `结算并发结果` 各输出 100 条固定索引记录。每条只输出下列枚举的底层整数值：

```text
任务结果治理状态
任务提交.状态
需求结算.状态
任务结果治理拒绝原因
```

输出发生在 join 后，不参与任何验收值计算。外部脚本再按这 200 条记录形成分组计数，不在 C++ 中发明新的业务分类。

### 4.4 异常捕获

允许把现有外层 `catch (...)` 临时拆成 `catch (const std::exception&)` 和 `catch (...)`，只输出是否捕获、异常类别和 `what()` 人读文本；不得改变异常后的验收默认值，不得吞掉后继续启动下一轮。为此临时增加的标准库 include 也必须随源码恢复。

## 5. 阶段状态机

### S0 身份与接口复核

1. 从设计消息给出的精确 `origin/main` 重读 AGENTS、计划索引、队列、登记表、#313 和 JY-454。
2. 核对路径、分支、HEAD=`0890464`、clean、无 upstream、远端同名不存在。
3. 核对自检 blob、A14 / A18 当前表达式、两组结果类型和输出字段均与第 4 节一致。
4. 任何字段、成员、作用域或枚举无法按计划只读访问时停止，不修改代码，回传接口漂移。

### S1 临时证据补丁

1. 用 `apply_patch` 只修改允许的自检文件。
2. 保持原验收表达式和并发正文逐字语义不变；输出只放在 join 和验收计算之后。
3. 运行 `git diff --check`，人工核对没有生产文件和其它自检变化。

### S2 构建

执行：

```powershell
msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m
```

完整构建输出先写入 S2 诊断目录。只有退出 0、0 警告、0 错误后才进入 S3；否则停止、释放资源、恢复源码并记录构建失败。

### S3 最多六十轮候选诊断

每轮从全新进程运行：

```powershell
.\x64\Debug\海中鱼巣.exe --self-test-exit
```

规则：

1. 每轮完整 stdout / stderr 先写 `subcondition-round-XX.log`，再解析。
2. 解析 TASK-RESULT 18 行、B3C 12 行、B0 20 行及全局退出码。
3. 轮间等待 2 秒；最多 60 轮。
4. 首个非零退出、任一专项失败、行数不符或异常终止立即停止，不启动下一轮。
5. 若 A14 或 A18 失败，必须核对 14 项、11 项和两组各 100 条结果证据完整；缺任一组即登记诊断证据不完整，不补跑。
6. 若首个异常不含 A14 / A18，也按首个异常停止并原样登记，不把它解释为本计划目标根因。
7. 60 / 60 通过只表示本次带失败后输出的候选样本未复现，不证明稳定或 #310 无关。

### S4 释放与源码恢复

1. 运行结束立即确认没有残留 `海中鱼巣.exe` 并释放固定共享目录。
2. 用执行窗口自身的反向 `apply_patch` 撤回全部临时自检变化。
3. 核对自检文件 blob 精确等于 `74734d038e3baa0be12a46ee1f275273305b7bf5`。
4. 核对 `git diff --name-only` 不含任何代码、工程、入口、计划或项目记忆文件。

恢复失败、blob 不一致或存在计划外差异时不得提交；保留 worktree 并退回设计窗口，不使用 reset、checkout、rebase 或 force 清理。

### S5 记录与任务分支提交

专属诊断记录必须包含：

```text
构建退出码、警告、错误、日志哈希
逐轮退出码、三组行数 / 失败数和日志哈希
首个异常轮的 14 项、11 项和两组 100 条原始证据摘要
外层异常捕获证据
共享目录释放与进程 0
临时自检 blob 恢复证据
最终 Git 精确单文件范围
不确定因果和禁止放行声明
```

运行 `git diff --check` 和严格规范检查。只暂存专属诊断记录，提交并非强制推送 `codex/313-task-result-subcondition-r1`。状态只能是“诊断完成待设计裁决 / 分支完成待集成”或具名退回状态。

## 6. 验收项

| 编号 | 验收 |
| --- | --- |
| A01 | 身份、冻结候选、父链、自检 blob、clean、upstream 和远端状态一致。 |
| A02 | 临时差异只含任务结果结算自检文件，原 A01—A18 语义、100 线程和阶段 610 不变。 |
| A03 | A14 14 项和 A18 11 项只在失败后输出，且可选值安全判断。 |
| A04 | 完成并发与结算并发各 100 条结果包含四个固定枚举值。 |
| A05 | Debug Rebuild 退出 0、0 警告、0 错误。 |
| A06 | 每轮完整输出先落盘，行数和失败后专项证据按合同解析。 |
| A07 | 首个异常立即停止；未启动后续轮次；共享目录立即释放。 |
| A08 | 异常捕获只增加诊断，不改变验收默认值或运行流程。 |
| A09 | 临时自检文件恢复到冻结 blob，最终提交不含代码 / 工程 / 入口。 |
| A10 | 最终任务提交精确一份专属诊断记录，远端同名分支一致且 worktree / index clean。 |
| A11 | 结论不声明确定因果、稳定性、#310 完成或后继解锁。 |

## 7. 结果分支与完成边界

### 7.1 观察到完整逐子条件失败证据

设计窗口必须先独立集成诊断记录，再根据首个为假的子条件、对应 100 结果分布和服务调用链形成最小根因修复或更窄诊断计划。不得让执行窗口自行修改生产服务。

### 7.2 观察到异常但证据不完整

停止并记录缺失项。不得补跑、猜测或把聚合 A14 / A18 当作根因；由设计窗口修订同一诊断计划。

### 7.3 六十轮未复现

只登记“本次带失败后输出样本未复现”。#310 保持暂停，设计窗口另行裁决是否需要更高轮数、调度诊断或候选差分隔离。

本计划不修复 TASK-RESULT-S1，不完成 #310 / #305，不创建 WB-P0B-02 集成，不解除 #301 / #306，不证明运行期迁移、持久化恢复、自我治理闭环或旧能力迁移完成。
