# EXIST-SCENE-S1 存在场景首组分层垂直样例代码实施切片 v0.1

日期：2026-07-13

队列编号：#265 / 设计队列：DQ-157

状态：依赖门控待执行 / 前置 #264、710 / 最终回归 720 / 未改 C++

## 1. 当前事实与假定契约

当前存在服务三类写路径并存，场景仅无令牌，世界组合失败无统一撤销。假定 #264 提供不透明会话和窄结构操作能力，#262 已冻结所有存在 / 场景调用点。接口不符时退回设计。

## 2. 核心处理

实现实际存在、虚拟存在、场景、接纳已有存在、创建并接纳五条无原始令牌业务路径。存在 / 场景业务服务负责类型、来源、重复和幂等准入；数据操作层同会话创建候选、关系、最后发布、精确撤销和确认；组合器定义创建并接纳的一次事务。

旧 `存在服务.h / 场景服务.h / 世界服务.h` 保留为兼容入口，本切片只登记替代关系，不直接删除。

## 3. 新文件分类

| 文件 | 分类 | 工程登记 |
| --- | --- | --- |
| `领域/数据操作.存在场景.ixx` | 生产真模块 | `ClCompile` |
| `领域/服务.存在.ixx` | 生产真模块 | `ClCompile` |
| `领域/服务.场景.ixx` | 生产真模块 | `ClCompile` |
| `领域/组合.存在场景.ixx` | 生产真模块 | `ClCompile` |
| `领域/自检.存在场景分层.ixx` | 自检真模块 | `ClCompile` |

模块名与路径逐段一致，使用方只 `import`；生产和自检分离。

## 4. 允许文件

上述新模块；既有三个服务头只允许增加兼容归属注释或窄只读适配，不允许删除入口；#264 模块、工程 / filters、中央自检登记、入口最小汇总、实施记录和治理文件。

## 5. 禁止与失败收口

禁止任务、方法、需求、状态、动态、特征、线程、`#214/#257`。虚拟来源关系、归属关系或读回失败必须逆序撤销本次精确关系 / 候选；故障注入后四仓库数量和可读结构回到前态。最后发布后异常追根因，不静默删除。

## 6. 验收

A01-A16 覆盖五条路径、无效来源、重复接纳、错误类型、幂等冲突、每个写点故障注入、候选确认 / 撤销、兼容入口回归、公开模块无原始令牌 / 仓库引用。Debug x64 0/0、完整自检、连续 20 轮并发、严格规范、diff、提交推送。

## 7. 完成边界

只证明首组垂直样例，不证明所有基础信息服务或世界系统完成。
