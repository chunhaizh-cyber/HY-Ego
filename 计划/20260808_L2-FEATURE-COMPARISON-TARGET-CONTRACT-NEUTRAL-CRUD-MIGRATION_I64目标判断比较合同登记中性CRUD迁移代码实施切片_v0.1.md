# L2-FEATURE-COMPARISON-TARGET-CONTRACT-NEUTRAL-CRUD-MIGRATION I64 目标判断比较合同登记中性 CRUD 迁移代码实施切片 v0.1

日期：2026-08-08

状态：可执行

## 1. 目标与正式输入

按配套详细设计机械实施：只把 `特征比较服务` 的 M02 目标判断比较合同登记和合法当前读取迁移到现有中性 CRUD。保持公开 DTO、构造、稳定状态 / 拒绝原因、`0x09` 幂等域、完整业务请求缓存和运行比较合同不变；退出 M02 的 `0x15000802U`、G8、预探测、完整快照和 legacy 提交；形成固定 `1/8/1/1/0` 中性写集并完成合同自身的中性精确当前读回。

- 详细设计：`规范/详细设计/20260808_L2-FEATURE-COMPARISON-TARGET-CONTRACT-NEUTRAL-CRUD-MIGRATION_I64目标判断比较合同登记中性CRUD迁移详细设计_v0.1.md`
- 施工流程图：`流程图/20260808_L2-FEATURE-COMPARISON-TARGET-CONTRACT-NEUTRAL-CRUD-MIGRATION_登记I64目标判断比较合同函数流程图_v0.1.md`
- 正式依据：0050、4015 v0.6、4030 v1.4、4040 v1.4、4050 v1.2、4180 v0.2、0600 v0.4。

设计基线：`bb34700b5ea4a652e15ae09e9b106a7cb32084b5`。目标服务 blob=`bb28ff0f2c2513b6c35928de3e906497245bf2e1`；数据头只读基线 blob=`d4303044148faf3fa0e4e2019d849f6a7cb93b75`；中性 DTO / 服务 / 仓库 provider blob 分别为 `d8f2c9ba052458fd9f22f6d6357695188d040c41`、`d3b13e6081a2b17a25beb85a19cd89cb6a81b6fa`、`8a1c6c7a17c7b0412501bc6b588df0f8bd8d2218`。

## 2. 实施前 S0

1. 确认 `main == origin/main`，HEAD 不早于设计与索引提交，index 为空；取得唯一代码执行计划段和 Git 发布租约。
2. 读取计划索引本行、真实 plan blob、详细设计、单根流程图和现行 4015 / 4030；确认本计划是唯一合法可执行项。
3. 重新读取目标服务、只读数据头、M01 结果提交和六个中性入口；若公开 DTO、M01 当前登记、M02 写集、M03 / 运行比较边界或 L1 幂等顺序发生会改变施工的漂移，停止并具名退回计划支撑。
4. 精确记录异主 dirty；只冻结与本计划三个白名单路径真实重叠的冲突。不得 stash、reset、clean、覆盖或带入异主 WIP。
5. 确认无需核心 L1、数据头、新 provider、启动、工程或测试入口修改；否则停止，不得自行扩大。

## 3. 写入白名单

1. `海中鱼巣/领域/服务.特征比较.ixx`
2. `施工记录/20260808_L2-FEATURE-COMPARISON-TARGET-CONTRACT-NEUTRAL-CRUD-MIGRATION_施工记录.md`
3. `验证记录/20260808_L2-FEATURE-COMPARISON-TARGET-CONTRACT-NEUTRAL-CRUD-MIGRATION_构建与目标判断比较合同验证.md`

除此之外零写入。禁止修改数据头、核心 L1、M01、M03、运行比较、状态 / 动态 / 因果、启动、运行期上下文、工程 / filters、自检、集成验收、规范、详细设计、流程图、计划、计划索引和异主 WIP。

## 4. 机械施工步骤

1. 保留 `合同幂等域=0x09`，新增 M02 专用中性键映射和带 `比较拒绝原因` 的中性结果映射；保留 M03 使用的 legacy 键映射与所有 G8 helper。
2. 新增私有 `mutable std::mutex 目标判断合同锁_`，使 M02 登记与公开读取整段串行；固定锁序为专属锁后短暂取得现有缓存锁。保留并收紧 `合同请求_` / `合同_` 成组缓存：完整请求相同才允许预期精确重复，不同返回 `入口拒绝/注册不唯一`，半结构返回内部不一致。不得删除缓存、反向取锁或把缓存当事实权威。
3. 实现详细设计 §5 登记输入读取：调用 M01 `读取当前登记()`，以中性当前代次、节点和值精确读取形成同代次输入束；只因首末竞争允许完整重试一次。不得以当前 G 与请求期望不等在提交前返回。
4. 实现固定写集形成 helper：1 普通注册节点、8 角色关系、1 个固定 20 项 I64组配置值、1 个当前属性槽、0 退出；合同版本、期望代次和 `0x09` 中性幂等键完整设置。
5. 原位改写 `登记I64目标判断比较合同`：删除 M02 G8 凭证、`0x15000802U`、旧探测、完整快照、`L1写集请求` 和 `提交写集`；每个可形成完整写集的请求恰一次调用 `提交中性写集`。
6. 首次成功 / 精确重复按详细设计 §6 核对路径、输入代次、发布标志、重试边界、回显键、非零事实代次和十映射完整唯一；任何矛盾返回内部不一致。
7. 新增 M02 中性精确读回：读取 M01 当前登记、注册节点、配置值、当前属性、同源同类型八关系组、八目标节点、定义标记 / 下界 / 上界值，并以 G0 / G1 同代闭合；竞争只完整重试一次。
8. 改写 `读取I64目标判断比较合同`：缓存只提供完整定位见证；请求匹配后调用新中性 helper，不直接返回缓存，不调用完整快照。
9. 保留 runtime `解析合同` 及其完整快照消费，因为 `比较特征` 明确排除；保留 M03 的全部 G8、探测、legacy 提交、读取和解析路径。对排除区与施工起点执行逐字同一性检查。
10. 填写施工和验证记录，只记录实际差异、构建 / 静态结果、明确未运行项和声明边界。

## 5. 验证门禁

至少执行：

```powershell
git diff --check -- '海中鱼巣/领域/服务.特征比较.ixx' '施工记录/20260808_L2-FEATURE-COMPARISON-TARGET-CONTRACT-NEUTRAL-CRUD-MIGRATION_施工记录.md' '验证记录/20260808_L2-FEATURE-COMPARISON-TARGET-CONTRACT-NEUTRAL-CRUD-MIGRATION_构建与目标判断比较合同验证.md'
python .\tools\check_specs.py --strict
```

随后用项目正式命令完成 Debug / Release x64 完整重建和双配置模块扫描。不得新增专项 L1 测试、外部补格驱动、双目标、生产验收运行器或日志 PASS 判定。

静态门禁必须证明：

- M02 固定写集恰为 `1/8/1/1/0`，配置 20 项和关系角色 1—8 精确；
- M02 `提交中性写集` 恰一个物理调用点，旧 `0x15000802U` / G8 / 探测 / 完整快照 / legacy 提交在该根归零；
- 预读当前代次不在提交前阻断新键漂移或同义精确重复；
- 成功类十映射完整，当前读取执行 M01 登记 + G0 + 节点 / 值 / 属性 / 源关系组 + G1；
- `合同请求_` / `合同_` 保留且只作业务幂等 / 定位缓存，不直接形成成功；
- M02 两条公开根由专属锁串行，锁序唯一，合法同义竞争在首次缓存完成后进入精确重复；
- M01、M03、`比较特征`、显式目标比较、状态迁移比较、`解析合同`、`解析迁移合同` 与施工起点逐字相同；
- 目标外文件差异为零。

真实首次、精确重复、同键异义、旧期望、竞争和读回损坏场景只有在现有合法业务入口实际可触发时才可报告运行结果；否则验证记录必须逐项标记 `未运行`。构建、静态顺序、模块扫描和日志都不能冒充运行期业务覆盖或第一层证明。

## 6. 提交、推送与完成声明

只暂存三个白名单路径，复核 staged diff 后普通提交并普通推送 main。结果进入正式 main 后交回交互智能体核验并更新计划索引；执行者不得自行修改本计划或索引状态。

完成最多证明 M02 目标判断比较合同登记和合法当前读取的中性 CRUD 代码候选及约定施工门禁。不证明真实运行、消费者验收、M03、运行比较、状态动态联合发布、其它 legacy、核心 L1 或第一层完善。第一层只由 L2—L5 后续为完成自身合同而发生的实际运行自然证明。
