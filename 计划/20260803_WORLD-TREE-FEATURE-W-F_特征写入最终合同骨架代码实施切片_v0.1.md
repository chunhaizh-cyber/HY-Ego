# WORLD-TREE-FEATURE-W-F 特征写入最终合同骨架代码实施切片 v0.1

计划身份：WORLD-TREE-FEATURE-W-F

版本：v0.1

创建侧代码基线：`57e1a526cfba595df1d033c2d33a9962b769476b`

状态：待激活。

## 1. 目标与完成条件

按详细设计机械建立特征初始/换代写业务的最终 DTO、最终模块、最终三引用构造、唯一公开函数和固定未实现骨架。完成候选必须对任意请求返回 `未实现(9)、发布代次0、持久不适用、读回空、缺项空`，且零依赖调用、零机器事实变化。

完成最多证明编译接入和入口巡检候选；不实现真实特征写入。

## 2. 激活门禁与精确依据

本计划保持待激活，直至：

1. 本设计包同步修订的4201 v0.5仍正式冻结 `节点直接特征批次提交状态` 完整1—9和值 `未实现=9`；
2. P1BF v0.4或正式后继的代码结果提供最终 `世界树特征事实`；
3. FEATURE-W-DP正式结果提供最终 `节点直接特征批次数据操作` 类型；
4. 当前正式代码仍不存在并行特征写模块/同名DTO；
5. 世界事实头、工程、启动自检聚合和顺序230写权全部释放。

精确设计依据：

```text
流程图/20260803_WORLD-TREE-FEATURE-W-F_特征写入最终合同骨架业务流程图_v0.1.md
流程图/20260803_节点直接特征写入服务提交世界树特征批次_函数流程图_v0.1.md
规范/详细设计/20260803_WORLD-TREE-FEATURE-W-F_特征写入最终合同骨架详细设计_v0.1.md
规范/详细设计/函数结构知识图谱/20260803_WORLD-TREE-FEATURE-W-F_特征写入最终合同骨架函数结构知识图谱_v0.1.md
```

未发布 FEATURE-W-DP WIP、旧NT-P2A、旧特征栈和聊天均不构成能力事实。

## 3. 允许与禁止文件

依赖满足后唯一允许代码/工程文件：

```text
海中鱼巣/领域/世界树事实.数据.h
海中鱼巣/领域/服务.节点直接特征写入.ixx
海中鱼巣/领域/自检.节点直接特征写入骨架.ixx
海中鱼巣/启动.应用程序.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
施工记录/20260803_WORLD-TREE-FEATURE-W-F_施工记录.md
验证记录/20260803_WORLD-TREE-FEATURE-W-F_验证记录.md
```

禁止修改查询、FEATURE-W-DP、F0/R1、L1提交、仓库、执行器、SQL、日志、旧特征栈、世界门面、状态动态、线程、入口.cpp、规范、流程图、详细设计、知识图谱、其它计划和计划索引。禁止真实写、兼容壳、重载或第二自检根。

## 4. 目标接口与物理合同

完全实施详细设计§3—§6：

```cpp
节点直接特征写入服务(
    节点直接特征查询服务& 查询,
    节点直接特征批次数据操作& 批次数据操作,
    节点直接类型化结构提交服务& 提交) noexcept;

节点直接特征批次提交结果 提交世界树特征批次(
    const 世界树特征批次请求& 请求) const noexcept;
```

全部 DTO 字段、枚举值、默认值、所有权、生命周期和模块位置不得由执行者修改。根函数不得命名/读取请求，不得调用三项保存引用。

## 5. 机械施工步骤

1. S0核对计划精确blob、当前HEAD、五项激活门禁、目标文件所有权、Git/index/dirty和唯一活动代码计划段；任一失败零代码收口。
2. 在共享头只追加 `世界树特征批次读回`，不移动或复制既有类型。
3. 新建写服务模块，按详细设计逐字建立合同版本、枚举、DTO和类；直接依赖显式完整。
4. 构造只绑定查询、批次数据操作、提交三引用；不得新增默认、指针、optional、setter、运行期替换或兼容重载入口，也不得以复制/移动对象替代该三引用构造路径。
5. 根函数只保留编译期无抛断言和固定prvalue返回。
6. 新建专项自检，登记唯一身份 `WORLD-TREE-FEATURE-W-F-S01`、顺序230；S0顶层数量N精确变为N+1。
7. 更新工程/filters和启动自检聚合，只登记本模块和一个专项根。
8. 完成矩阵、构建、自检、静态扫描和精确diff；不进入真实写实现。

## 6. 验证矩阵

| 编号 | 场景 | 必须结果 |
| --- | --- | --- |
| F01 | 稳定数值和DTO默认值 | 逐字段等于设计 |
| F02 | 默认请求 | 固定未实现9和空载荷 |
| F03 | 每字段非法请求 | 与F02完全相同 |
| F04 | 完整初始请求 | 与F02完全相同 |
| F05 | 完整换代请求 | 与F02完全相同 |
| F06 | 大项目组/边界整数 | 与F02完全相同，无读取无分配 |
| F07 | 三依赖调用计数 | 全部0 |
| F08 | 六仓/幂等/批次账/代次前后 | 逐值相等 |
| F09 | SQL/日志/旧栈/时钟 | 命中0 |
| F10 | 消费者分支 | 未实现后成功后继0 |
| F11 | AST | 一根、签名唯一、参数引用0、项目内调用0 |
| F12 | 自检聚合 | 顺序230唯一；登记/执行N+1 |
| F13 | 工程和模块依赖 | 零缺项、零重复、零环 |
| F14 | scoped/cached diff | 仅允许文件和专属记录 |

执行期至少使用以下可形成命令：

```powershell
git diff --check -- '海中鱼巣/领域/世界树事实.数据.h' '海中鱼巣/领域/服务.节点直接特征写入.ixx' '海中鱼巣/领域/自检.节点直接特征写入骨架.ixx' '海中鱼巣/启动.应用程序.ixx' '海中鱼巣.vcxproj' '海中鱼巣.vcxproj.filters' '施工记录/20260803_WORLD-TREE-FEATURE-W-F_施工记录.md' '验证记录/20260803_WORLD-TREE-FEATURE-W-F_验证记录.md'
python .\tools\check_specs.py --strict
msbuild .\海中鱼巣.slnx /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m:1
.\x64\Debug\海中鱼巣.exe --self-test-exit
msbuild .\海中鱼巣.slnx /t:Rebuild /p:Configuration=Release /p:Platform=x64 /m:1
.\x64\Release\海中鱼巣.exe --self-test-exit
powershell -ExecutionPolicy Bypass -File .\tools\clang_ast\扫描.MSVC模块依赖.ps1 -配置 Debug -平台 x64
```

再以当前仓内源码闭包/AST能力扫描根函数；必须证明一处完整定义、唯一签名、参数引用0、成员读取0、direct/member/callback/unresolved项目调用0。Clang工具若因既有传递PCM不可用而未到达目标模块，只能记录为工具候选不可用，不得冒充AST PASS，也不得覆盖双配置构建、完整自检、MSVC模块扫描和源码闭包结论。

## 7. 失败与DRIFT收口

| 身份 | 条件 | 收口 |
| --- | --- | --- |
| `DRIFT-WORLD-TREE-FEATURE-W-F-SPEC` | 4201 v0.5未正式保留未实现9 | 零代码，保持待激活 |
| `DRIFT-WORLD-TREE-FEATURE-W-F-P1BF` | 最终特征事实类型未进入代码或漂移 | 零代码 |
| `DRIFT-WORLD-TREE-FEATURE-W-F-DP` | 批次数据操作最终类型未进入代码或漂移 | 零代码，不删除构造依赖 |
| `DRIFT-WORLD-TREE-FEATURE-W-F-230` | 顺序230被其它正式身份占用 | 零代码，退回设计 |
| `DRIFT-WORLD-TREE-FEATURE-W-F-OWNERSHIP` | 任一允许文件有其它活动所有者 | 保留现场，仅停止重叠切片 |
| `DRIFT-WORLD-TREE-FEATURE-W-F-SCOPE` | 需要修改禁止文件或实现真实写 | 零扩面退回 |

普通编译/格式错误只在允许文件内修复；不得借错误处理补业务语义。

## 8. 创建侧自审与完成声明

创建侧必须证明：业务图B01—B07全部映射；单函数N01—N03零隐藏调用；DTO字段来源和唯一物理所有者闭合；三构造依赖生命周期闭合；全部逻辑路径只有固定未实现；零代码机械推演无需执行者裁决。

计划完成声明只能是：“WORLD-TREE-FEATURE-W-F 最终特征写ABI安全骨架候选按本计划形成并通过具名矩阵。”不得声明 FEATURE-W、P1C-W、生产世界树或服务验收完成。
