# SERVICE-COMPOSER-S1 运行期组合器与线程路由去令牌代码实施切片 v0.1

日期：2026-07-13

队列编号：#270 / 设计队列：DQ-162

状态：设计资料与旧计划归并门控 / 计划骨架保留 / 不可执行 / 等待专项流程图、详细设计和归并裁决 / 预留最终回归 770 / 未改 C++

## 0. 设计与归并门禁

本范围与 `#248/#249/#250` 的运行期服务装配、主装配验收和初始化真模块迁移直接重叠，也会改变 `#214/#224` 当前由路由或生产真模块取得并下传原始令牌的假定。设计计划窗口必须先裁决这些旧计划是修订、归并还是由本计划替代，并生成运行期组合器专项流程图和详细设计。本文件在该裁决前不构成代码实施许可，`DQ-162` 不得执行。

## 1. 范围与假定

迁移初始化、世界树 / 语素 / 概念初始化、自我治理领域路由、概念命名治理路由和运行期组合调用，使生产调用方只提交业务请求，不取得、读取、保存或传递原始结构令牌。假定 #265-#269 新服务公开面已经覆盖所有生产写入。

## 2. 新模块与允许文件

新增 `领域/组合.运行期业务操作.ixx` 和 `领域/自检.运行期组合分层.ixx` 真模块。允许按 #262 矩阵修改明确的初始化 `.ixx`、线程路由 `.ixx`、`启动.运行期上下文.ixx` 和最小装配；模块清单漂移时退回设计。

## 3. 禁止

禁止线程 / 消息保存会话或令牌，禁止恢复 `#214`，禁止改线程为动作来源，禁止在入口新增领域正文，禁止直接写仓库。

## 4. 失败与验收

组合操作失败由结构化结果返回，线程只传业务材料；许可释放由执行器保证。A01-A16、静态扫描生产 `取得许可/读取令牌/结构事务令牌` 外泄为零、启动 / 停止 / 初始化回归、Debug x64 0/0、完整自检、连续 20 轮、严格规范、提交推送。

## 5. 完成边界

不证明兼容入口已删除、#214 已恢复或完整运行期业务已完成。
