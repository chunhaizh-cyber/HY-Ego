# 场景、二次关系概念与动态概念精确设计交付及实施衔接

日期：2026-09-10
版本：v0.2
身份：设计交付与后继施工输入；不是机器规范、代码完成回执或执行队列。
目标来源：用户要求“请完成设计”。
执行优先级：用户追加“旧结构优先清理掉”；按本文件第5节先处理已被替代的结构和入口。
初次设计事实基线：`7169524932ce2e01eaa5f9f9353361912e7dea20`；创建侧范围纠偏基线：`b6533fe4ab7dc680c24639eec9f6a1faa154c8df`。
整合所有者：交互智能体 `梳理新数据架构 /root`。

## 1. 交付范围

初版针对上一轮列明的三个精确合同缺口交付以下设计；v0.2进一步修正场景范围，并保留第5.1节未解除的旧格式合同缺口。不修改生产代码、不恢复旧计划，也不从设计发布推定实际提供者已接通或整个场景包已可执行。

| 对象 | 正式合同 | 设计交付 |
| --- | --- | --- |
| 场景 C 与真实 B | [4204](../../规范/4204_子规范_场景同节点角色与状态绑定生产合同.md)，同步4202/4203/4230 | 同节点角色、四根、身份与组织读取、真实B、失败/重放/退出/恢复，以及最早详细设计和实施计划 |
| 二次关系概念 RC | [4242](../../规范/4242_子规范_二次关系概念精确结构与操作合同.md)，同步4151/4240/4241 | 原子/AND结构、D/FC/EC/K、唯一owner操作、固定F或B/S来源求值、形成和治理 |
| 动态概念 DC | [7191](../../规范/7191_子规范_动态概念精确结构与算法合同.md)，同步7190/4240 | 角色/位置/条件/时间、确定规范化、有界提取和匹配、关键条件简化、结构发布及旧消费者替换合同 |

场景最早实施包：

- [详细设计](../../规范/详细设计/20260910_SCENE-C-IDENTITY-ROLE-PHYSICAL-CONTRACT_场景角色与真实绑定详细设计_v0.1.md)。
- [实施计划](../20260910_SCENE-C-IDENTITY-ROLE-PHYSICAL-CONTRACT_场景角色与真实绑定实施计划_v0.1.md)。

本文件保留基线、代码映射、复用裁决和发布验证等过程信息；正式规范只保存稳定机器合同。规范目录是规范现行性的权威，本文件不能替代它。

## 2. 关键设计裁决

1. C就是既有E节点的场景角色。存在owner生产对象；场景owner原子生产角色边、四根、四根绑定。两个阶段分别读回；后阶段失败保留E，不虚构跨服务回滚。
   C退出共同退出四根和四绑定八项，保留不可变角色登记边供按C找到首次布局及核验退出；登记边本身不证明当前角色。它继续保护E，E删除需后继具名协调解除，不能把这一限制藏成“全生命周期已闭合”。
2. B是4203既定的场景owner普通关联节点和三边，不增加节点族。S本体仍中性；B成功、S组织成功和完整观察成功分账。
3. 动态组织写调用时传入4213只读提供者；场景构造不反向持有动态服务，避免场景B→动态→场景构造环。动态概念只读场景组织和动态来源，不能自行解析旧世界快照。
4. RC与DC扩展同一概念owner、同一概念服务；基础纯值类型只机械搬移一次。新定义种类5/6不改义旧1..4，不把新定义塞入旧应用定义联合。
   初始化登记保留核心1..28，额外专属角色4242/7191指向各自扩展锚点；基础读回必须同次分账，不保留“总数恰28”阻断合法扩展，不复用来源关系作初始化。
5. 固定状态端点通过B→S取得准确值，使用4242独立强类型求值入口，不修改4151原三分支计算图来源，不为适配造F。差异算法复用公开I64比较，按左B右A形成A−B。
6. 公开结构证据不带L1写入所有者；内部仍完整校验owner。对象本体不增业务版本、代次、幂等或生命周期字段，调用可靠性和概念治理合同继续有效。
7. 同一事实截止H的组织链不证明事件UTC发生期包含，不能拿当前成员关系、B创建代次、D时间包络或调用方声明补证。缺该真实来源时，要求完整观察的形成/支持出口具名非成功。

## 3. RC基线复用与后继修改定位

以下是上述HEAD的设计扫描结论，不是未来工作区事实。执行前须重新扫描并保护他人WIP。

| 基线实体 | 复用/替换裁决 |
| --- | --- |
| `海中鱼巣/领域/数据服务.概念树类.ixx`的概念身份、四根、FC/EC、图及owner写入 | 扩展唯一服务；完整RC由专属读取提供，不能把旧FT加域或形成宿主定义当RC |
| `特征类数据服务::读取I64比较绑定/读取当前I64比较绑定` | 复用节点型K、固定输出和当前用途路由；不将旧比较规则身份与K互换 |
| `有序I64特征比较提供者::比较` | 复用纯计算；来源、FC/EC、路由与参照资格由求值应用核验 |
| `二次特征计算应用服务::计算` | 保留已有F/已存定义/当次上游输出与高阶复算，不向其variant暗加状态分支 |
| 4203 B历史读取、4212 S历史内容及准确值核验 | 组合B/S适配；不根据数值相等猜F，不用存在当前采用覆盖过去S |
| `概念树存在定义::特征模板组` | v1仅实现受支持I64域FC的合取；复杂EC具名拒绝 |

后继RC代码包需闭合基础类型搬移、外部交付初始化、服务专属操作、求值及形成应用、真实import/工程登记、退出闭包及验证。不能把DTO、服务与消费者拆成互不负责的三个“已完成”片段。

## 4. DC旧消费者映射

位置为上述HEAD源码证据，后继S0重新取得精确函数和调用清单；行号不是删除授权。

| HEAD对象及定位 | 现状 | 目标 |
| --- | --- | --- |
| `海中鱼巣/领域/数据服务.概念树类.ixx:99—118` | 旧动态时间槽/定义含具体C/E | 删除旧生产DTO分支，换概念化定义；旧存储拒绝，不重新解释 |
| 同文件`:246—262、428—465` | 旧动态扩展及建立/释放/支持请求 | 换7191交付与封闭写联合；FC/EC不改义 |
| 同文件`:954—973、1008—1027` | 多套旧构造、完整world依赖 | 换窄provider，删除旧动态扩展构造及`使用动态世界服务` |
| 同文件`:1800—1870` | 旧宿主模板及动态历史事实读取 | 4213原子B/S展开与4204组织互证 |
| 同文件`:2100—2200、2900—2960、3200—3250` | 旧时间槽解码/写集/通用分派 | 同次替换新布局；新损坏和旧格式不支持分账 |
| `海中鱼巣/业务/应用服务.动态概念树类.ixx:22—167、211—261` | 两套共享/非共享DTO、继续材料、十八个重载 | 换7191七个业务入口；删除旧状态机和纯CRUD转发 |
| 同文件`:553—595、967、1556` | 旧动态状态组、E/场景状态组及快照取值 | 只走新D/B/S来源和RC求值，不由孤立S猜观察 |
| 同文件`:1500—1641` | 共享门面、旧时间槽成功谓词 | 新专属完整返回，不保留首槽0/至少两槽的旧前提 |

该HEAD扫描未发现动态概念应用类及其旧形成/上位请求在应用文件外的普通消费者；这证明不了生产接线。后继新增或变更的调用者必须进入同次迁移闭包。

## 5. 实施顺序与真实剩余项

清理优先是明确的施工顺序：先按第3/4节及场景设计枚举旧定义、DTO、构造、函数、解析/适配分支和全部调用方，区分“本片将删除的现存提供者”与“提供者已在基线退出、调用方仍遗留的悬空调用”。无有效消费者的退役入口优先物理删除；本片删除的现存入口若仍有合法调用，必须与调用方在同一允许切片、同次发布内切换并删除，不保留别名、兼容空壳、默认成功、运行时新旧开关或第二写入路径。不能由本片新造破损消费者，也不能把基线已有悬空调用误报为本片新增破损或为修编译恢复已退出接口。

创建侧v0.2逐符号复核：`数据服务.场景类.ixx`当前只有B骨架，世界树所调用的`使用存在服务/查询场景树/接纳未归属存在/创建并接纳场景/迁移成员关系`及概念消费者所调用的`读取场景历史事实`在该提供者已不存在。它们是本片开工前的遗留缺口，不属于本片现存函数删除集。场景包仅替换旧构造、删除三个`未实现*结果`私有辅助、将七个B公开入口换为真实函数体，并保留加强`绑定于`；详细设计v0.2中的逐符号表是精确范围依据。旧成员/子场景/世界树及概念消费者后继须在其机器合同和所有权闭合后另行退役或迁移，本片既不恢复这些接口，也不声称其消费者已经清理、根工程已经可编译。

初版已经从正式规范移出旧代码基线/过程描述，并以专属新合同取代旧待冻结总括；本次v0.2不再修改正式规范。实际旧代码清理必须服从精确计划及文件所有权；不删除L1存量事实，不顺带清仓、恢复旧验收删除或覆盖他人WIP。被替代格式不重新解释为新结构，具体识别及相交拒绝分支尚须按第5.1节解除门禁。

| 清理类别 | 精确对象与处理 |
| --- | --- |
| 源码 | 场景骨架函数体及辅助、DC旧时间槽解码/快照适配/共享门面，按4204/7191同次物理删除；新RC不能以改名旧类冒充 |
| 公开ABI | 删除旧场景构造、旧DC DTO/构造/重载/继续材料；保留4203 B版本1和存在窄版本2原义、FC/EC有效公开面，基础纯值类型机械搬移而非复制 |
| 调用点 | 重新枚举每个本片被删现存符号的直接调用及import；若有调用须在允许范围内同次切换，否则退回范围治理。b6533fe4已悬空的旧世界/概念消费者按场景详细设计v0.2单独列账，不恢复、不暗纳本片完成条件；未接线仍报告未接线，不用空函数消除报错 |
| 工程登记 | 根`海中鱼巣.vcxproj`与`.filters`去除实际已删除文件项并登记真实新模块；不删除无关编译项以掩盖失败，不从本设计推定现在已有旧独立模块可删 |
| 持久格式 | C/B旧双身份或散装格式按4203/4204拒绝；RC旧FT加域/宿主布局按4242拒绝；DC旧时间槽种类保留原编号但不被新种类6解释，按7191拒绝。没有转换合同即零转换，L1历史事实不物理擦除，也不恢复业务兼容写入口 |

代码执行者须把上述各类对象落到最新HEAD的精确删除/修改路径和符号清单，不能用“旧结构全部删除”代替范围核对。当前已形成但未登记执行的场景包只规划自己的允许文件；RC/DC同owner清理属于其后继完整代码包，不越权夹带执行。

最早复杂代码目标是场景C角色、四根、真实B和状态/动态组织读回。该包经独立设计复核和正式发布后，由计划所有者依据最新现场登记索引。本轮不启动执行，不将远端RC/DC全塞入队列。根工程诊断与该模块公开探针分别报告：不删除工程中的旧消费者编译项、不改其源码或用兼容壳掩盖错误；本片不得宣称全仓旧结构已经清理。若S0发现本片新删符号有未纳入的实际调用，仍须停受影响范围，不能套用“基线遗留”豁免新回归。

RC/DC共享概念owner、基础类型、定义分派与退出闭包，代码阶段必须由一个owner整合；不能因为规范由两个作者并行完成就并行改同一生产服务。场景成果发布后再按实际提供者闭合相邻代码包，复用本次合同和映射，不恢复旧计划。

下列范围没有随本次设计自动完成：

- 场景成员/子场景的全部CRUD、跨owner包含替换、协调组织清理。L1已有有限N分区原子事务，缺的是上层具名协调合同，不是底层原子能力。
- C角色退出后登记保留及E对象删除的协调终结。现有L1历史关系组只召回H时有效边，不能假设其可按C发现全部已退出边；本版用登记边回溯固定八项，未提供解除登记的独立入口。
- `OBSERVATION-OCCURRENCE-CONTAINMENT-SOURCE`：事件发生期包含及观察完整来源。现有创建/退出事实代次不是UTC有效区间；完整观察、世界动态概念首次形成和新增支持不能以结构成功替代。
- 持久二次实例：0300 C038仍未解除；关系根真实为空不证明关系实例已经可持久化。
- 真实生产初始化、旧世界树/概念消费者集成、动作来源、因果应用和业务闭环。

因此“精确合同已有可实现的受限成功范围”不同于“所有生产路径已可成功”。机械匹配只能报告机械命中，不夸大现实识别和可靠性。

### 5.1 创建侧仍未解除的旧格式合同门禁

`SCENE-LEGACY-IDENTIFICATION-ORDER`：场景详细设计和计划v0.2修正了删除闭包及既有消费者分账，但目前仍不得登记执行。4204六项新交付未提供旧锚点/旧关系类型来源；普通同owner节点、角色1或边数量不能唯一识别旧场景。4203对旧散装仍规定“旧格式不支持或入口拒绝”，4204的“明确旧格式证据”也没有冻结本地识别来源以及与错族、已接受新族残缺、退出同时出现时的精确优先级。4230第2.4节要求由各类专属合同冻结，详细设计不能补造这个机器裁决。

现行合同足以确定合法E无新登记且无新根绑定为未启用、非E/裸未知且无明确旧证据为入口拒绝、已接受新结构残缺为内部不一致、八项共同退出为目标已退出；这不等于旧格式所有相交分支已闭合。具体可判/不可判表见场景详细设计v0.2。不得扫描或硬编码旧关系来制造识别，不得把`旧格式不支持`擅自改为恒不可达，也不得用默认错误跳过该门禁。

最小下一步是取得用户对4203/4204相关正式条款修订的确认，冻结旧格式证据是否只从具名提供者承接、不做本地旧结构解析，以及相交分支的先后；本轮不修改这些规范，不增兼容仓/旧字段/转换入口。待正式合同和设计同步发布后，计划创建侧才能重新判定索引资格。

## 6. 审查及验证记录

以下初版审查结论仅覆盖当时已核对范围；v0.2创建侧进一步发现第5.1节门禁，初版PASS不能继续解释为整个场景包可执行。当前判定为：消费者范围纠偏已明确，旧格式来源/精确优先级DRIFT，计划不登记。

本轮由三个独立作者分别完成场景、RC、DC；主线核对跨合同签名、依赖方向、G/H与UTC、owner及完成边界。场景作者独立复审4242与7191均为PASS，限定各自受限成功/拒绝边界；DC作者复审RC后指出重复来源零写复用成功谓词缺项，修正后定点复核PASS。主线复核并修正了公开owner泄漏、初始化缺口、基础类型登记扩展、C退出后发现路径和空U64不可发布等问题；不以格式检查代替这些语义核对。

RC作者对4204及场景详细设计/计划独立复审PASS；发现并修正根工程路径、仅以B角色1保护C退出两项问题。三方复核均未将代码、生产来源或业务完成作为设计PASS的一部分。

初版发布验证：8份已跟踪文档及6份新增文档逐文件差异检查；以正式HEAD归档加该次精确14件覆盖进行规范严格检查，118份目录项全部通过。隔离副本不消费其它切片WIP；Git只用于读取正式历史与L1既有验收blob，不创建工作树或分支。v0.2只修订场景详细设计、场景计划和本衔接文件三件；发布前执行精确差异检查、b6533fe4正式快照叠加三件的隔离strict及暂存清单核验，不把检查通过当第5.1节语义门禁解除。

工作区规范检查发现原有`验证记录/集成验收/DATA-L1-FINAL-CLOSURE/manifest_v0.3.json`被其它切片删除，导致L1 FROZEN清单失败。该文件在设计基线HEAD存在；本轮不恢复、不暂存或修改它。最终规范验证使用正式HEAD加本轮精确文件的独占临时副本，并与工作区结果分账。

全仓`git diff --check`另发现他人`AGENTS.md:12`现有修改含行尾空格；本轮不修改规则或夹带修复。本轮14件单独检查和精确暂存检查与该工作区问题分别报告，不能宣称整个工作区通过或干净。

本轮未执行C++构建、运行、恢复、生产接线或业务验收。文档/目录检查、提交和推送不证明代码能力。

## 7. 发布与交接

初版设计14件已由`b6533fe4ab7dc680c24639eec9f6a1faa154c8df`发布。v0.2仅提交第6节三件已登记设计纠偏，不修改正式规范、代码或计划索引，不带入他人454项删除、业务应用改动或未跟踪材料。发布后发送精确提交、文件blob、未解除门禁和剩余来源给另一交互窗口；由该窗口向用户请求最小规范修改确认。接收方重新读取正式仓库，不把消息当执行许可。

本文件随设计提交保存，不写入尚未存在的自身提交号；实际提交和远端事实以Git及交接回执为准。
