# SELF-P1 根特征自检、需求生长与任务意图代码实施切片

编号：SELF-P1；路由：#354 / DQ-246；版本：v0.1；日期：2026-07-23

设计状态：范围草案。当前计划状态只读 `计划/计划索引.md`；在 #353 冻结 SELF-C1、流程图、详细设计、知识图谱、实际接口和精确文件前，不构成代码施工合同。

## 目标与依据

依据 5100—5160、6130、7130、8200 和 8300，实现“根特征合法自检缺口 → 需求目标与路径 / 权重 → 主需求 → 可承接任务意图”的节点直接生产候选。AND 必须处于一个 OR 路径内；同层不同 OR 是不同路径；每个 OR 可继承全部父权重并在内部 AND 间平分。

## 冻结前必须补齐

- SELF-C1 / v0.1 的 DTO、签名、版本、所有权、拒绝和内部错误；
- 自检缺口不是普通方法执行失败，缺口只由合法自检动作形成；
- 需求、任务身份分离，任务意图不是已承接任务；
- 精确允许文件、禁止文件、入口拒绝、回滚 / 隔离和验收矩阵。

## 候选步骤

1. S0 读取 #353 精确设计和当前节点直接需求 / 任务接口。
2. 实现根特征读取、自检动作来源和缺口结果。
3. 实现需求路径、权重、目标状态和防重复需求生长候选。
4. 形成主需求和任务意图，发布前完整读回。
5. 定向自检负权重、空路径、非法 AND / OR、重复缺口和版本漂移。

## 边界

本切片不承接任务、不筹办、不创建方法、不执行动作、不结算需求，不修改共享入口、工程、统一运行器或生产装配。任何允许写文件尚未冻结时一律禁止代码写入。完成候选也不得声明自我闭环成立。
