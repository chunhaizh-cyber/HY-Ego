# L1-SIMPLIFY-P4-FEATURE-VALUE 实例特征当前 I64 值换代代码实施切片 v0.1

计划身份：`L1-SIMPLIFY-P4-FEATURE-VALUE`
版本：`v0.1`
创建侧代码基线：`fa3cb1401e8c1cc2cf71ff704590404fbcc22889`

## 1. 目标与完成条件

在 P3 正式结果之上，为一个已建立的 I64 实例槽新增当前值换代入口。完成条件是旧值退出、新值发布、当前属性槽切换和事实代次推进由一个 L1 写集原子完成，并以新当前事实、旧值历史及当前槽的独立再次读取校准后返回。

正式依据：4015、P4 v0.1 详细设计与函数流程图、P3 v0.2 正式结果 `e610246d7380eb45d3f2843e226af0a72f5fb367`。

## 2. 允许文件

```text
海中鱼巣/领域/L1实例特征.数据.h
海中鱼巣/领域/服务.L1实例特征.ixx
海中鱼巣/领域/自检.L1实例特征.ixx
施工记录/20260804_L1-SIMPLIFY-P4-FEATURE-VALUE_施工记录.md
验证记录/20260804_L1-SIMPLIFY-P4-FEATURE-VALUE_验证记录.md
```

禁止修改 L1 核心合同/仓库、P1/P2 服务、运行期初始化、工程登记、顶层自检顺序/数量、SQL、材料、恢复、旧特征体系及其它领域服务。不得新建代码模块。

## 3. 实施合同

1. 在现有数据头新增 `I64实例特征当前值换代请求` 及其有效性函数；现有 DTO、枚举值和签名不变。
2. 在现有服务类新增 `换代I64实例特征当前值`；使用私有 `0x07` 幂等域，锁内先读回当前实例、定义、新来源和完整快照。
3. 写集精确为一新值、一属性槽变更、一旧值退出；节点/关系均为空。新值必须在定义区间内，来源必须为同代次当前普通节点。
4. 成功或精确重复后不得直接采用提交返回的预计形状；必须独立读取新当前事实、旧值当前/历史和当前属性槽，并以再次读取到的状态校准最终结果。
5. 合法拒绝、未找到、幂等冲突和代次漂移零业务载荷且不写错误日志；资源失败和内部不一致结构化向上送出。
6. 扩展现有 P3 专项自检，完整自检顺序 320、登记数量 27 保持不变。

## 4. 验证合同

- 静态：根函数定义唯一；`0x07` 域唯一；写集节点0、关系0、值1、槽变更1、退出1；无 SQL/材料/旧仓调用。
- 专项：成功换代、旧值退出与历史、新槽、独立同代次读回、精确重复、同键异义、旧基线竞争、错误当前值、错误来源、越界和未登记零写入。
- 构建：Debug/Release x64 Rebuild 均通过。
- 运行：Debug/Release `--self-test-exit` 均退出 0，完整自检仍为 27/27。
- 工程：`python .\tools\check_specs.py --strict`、MSVC 模块依赖扫描、目标 diff check 和 cached diff check 通过。

若实现需要改变 P3 既有公开 DTO/枚举数值、L1 核心写集语义、P2/世界结构合同、运行期接线或允许文件，停止并具名退回设计漂移。

## 5. 完成声明边界

只允许声明一个已建立 I64 实例槽的当前值原子换代、幂等和独立校准源码候选完成；不声明其它宿主或值类型、关系 22、特征批次、比较、状态动态、恢复、生产初始化、旧体系删除或集成验收完成。
