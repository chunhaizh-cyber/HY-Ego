# 场景角色与真实绑定实施计划

计划身份：SCENE-C-IDENTITY-ROLE-PHYSICAL-CONTRACT
版本：v0.8（取消动态组织来源场景核验；沿用原文件路径）
日期：2026-09-11
所属层：DATA-L2 / ARCH-L2
当前状态：只由[计划索引](计划索引.md)登记裁决；本文件不自行宣布执行资格。
计划支撑范围：P0已完成；保留P1真实生产目标。依用户裁决同步4204 v1.1，取消场景组织时展开D来源及逐原子B.C核验；公开ABI、生产白名单、真实D当前读取、组织结构和其它核心行为不变，不触碰执行者生产WIP。

## 1. 目标与完成条件

原地替换场景类未实现骨架，真实实现同节点C角色、原子四根、B及CES三关系、S/D唯一实例组织路径和相应登记/写入/当前历史/退出/恢复读回。真实E、S、D必须通过正式服务产生，不能由替身成功、裸节点或恒定拒绝构成核心完成证据。

完整完成时必须满足4204全部本版公开入口及详细设计§9.1验证矩阵，物理删除旧骨架构造、三个未实现辅助及七B入口旧函数体，保留七B公开签名和`绑定于`并实现真实行为，目标模块在独立Debug/Release x64验证工程中编译链接并运行公开探针。P0已删除的B旧枚举保持退出，B合同不得修改。场景七字段完整CRUD、直接成员/子场景、跨owner包含迁移、完整旧场景消费者退役、全场景TTL、UTC发生期包含、RC/DC、普通应用和正式集成验收不在本计划；根工程既有悬空调用分账，不以全项目编译通过作为本片完成条件。

## 2. 正式依据与当前起点

- [4204 v1.1](../规范/4204_子规范_场景同节点角色与状态绑定生产合同.md)：精确物理/DTO/接口/事务、新结构准入、动态当前读取及L1发布结果全量映射。
- [4203 v0.8](../规范/4203_子规范_场景状态使用绑定物理合同.md)：B版本1、E窄版本2、E/S端点全量映射；B旧格式枚举14保持退出。
- [详细设计 v0.7](../规范/详细设计/20260910_SCENE-C-IDENTITY-ROLE-PHYSICAL-CONTRACT_场景角色与真实绑定详细设计_v0.1.md)：本计划唯一实现解释及A/B/C分阶段验证矩阵；路径保持原名。
- 0050、1140、1150、4015、4070、4202 v1.6、4212、4213、4230与代码文件建立归属规范。

v0.4创建侧复核基线为`main == origin/main == ada402943a4f849a29c820eedf2e12813ada9b26`，index为空。P0结果提交`e153314a4595523cef7ad314ea75564e06e644a7`已由关闭提交`ad2c2ab2`退出当前候选；B合同当前blob为`1b777530a7456af4a0d50b8f22a7b0f2659e2d8f`。E真实提供者发布于`d42cd5219c246135c388c131bf4cba4636e5445d`，S发布于`71f5114e3351fe8de0c0d4497b940b70991df447`，D发布于`293fd34ffff575a40a2391658ca4020338823045`；场景仍是blob `59be46413e399c5e86a1a9b113bb89e8a435a225`的95行骨架。L1 v3多owner原子能力已存在，但本切片只使用场景owner一次写集，不改底座。

详细设计§2.1已按当前HEAD重新确认本片删除/保留集和基线旧调用：世界树的`使用存在服务`、`查询场景树`、`接纳未归属存在`、`创建并接纳场景`、`迁移成员关系`，存在概念应用的`使用存在服务`、`读取场景历史事实`及概念树数据服务的`读取场景历史事实`在场景provider/基类中均不存在。因此它们是基线悬空调用，不是本片删除引入的破损；本片不恢复、不清理这些调用，不据“无外部旧构造调用”推导“无旧消费者”。动态/因果的B读取和概念树的`绑定于`为现存合法调用，保留其提供者签名。

P0已从B合同删除`旧格式不支持=14`；当前生产扫描无该成员、别名case或错误映射。动态/因果的`映射绑定`已有default内部不一致，不需改源码。当前没有可列入删除集的场景旧证据provider、parser或import；P1不虚构旧解析清理对象，也不修改B合同。4203 v0.8保持B协议，4204 v1.1保持场景角色组织协议v2并取消场景组织对D来源的展开核验。

### 2.1 执行期验证DRIFT裁决（2026-09-11）

旧的“公开DTO缺少状态/动态组织类型见证，必须补字段才能自证”已由4204重建撤销。当前口径仍是服务内部真实核验，轻量判断只查状态/载荷/请求对应；普通组织写回执不含祖先路径，显式历史查询保留完整路径。

执行侧具名确认：4204、详细设计v0.5和本计划v0.6把G01写成运行探针覆盖“L1写入来源全部枚举及未知值”，但生产场景服务依法直接持有具体`const L1事实基座服务&`和final写端口，合法公开L1不能产生未定义枚举值；为了命中它而增加测试后门、修改L1、重解释内存或复制私有仓库状态都会越过本计划范围。该问题不改变`default -> 内部不一致`机器语义，也不要求新增公开ABI。

验证证据继续按详细设计v0.7分账：全部现行具名枚举和未知值的映射完整性由G01-M机械提取与静态穷尽检查证明；正式公开接口真实可达的来源状态由G01-R运行探针证明。真实C/B/S/D/O/P成功、生命周期、恢复和保护反例仍是硬运行门禁，不能由替身或静态检查替代。现有探针明显小于§9.1完整矩阵时，执行者在仓库外拆成多个有界探针并汇总，不要求一个巨型探针，也不得缩减矩阵。

本次规范重建不修改生产代码，不取得他人WIP所有权。代码所有者在设计包完成后重新S0；原B协议v1/存在窄v2不变，场景角色组织v1不保留兼容重载。

执行S0读取当前计划blob和正式发布依据、当前代码/调用点、所有权/发布/资源及目标知识条目。当前知识条目基线早于这些实现，先以源代码核验；不要把全仓异主dirty当本计划代码或为验证回退它们。

## 3. 精确允许文件

| 允许路径 | 权限及内容 |
| --- | --- |
| `海中鱼巣/领域/合同.场景角色组织.ixx` | 新建4204精确模块/DTO/谓词/provider |
| `海中鱼巣/领域/数据服务.场景类.ixx` | 原地实现唯一owner；替换旧构造和7个B骨架 |
| `海中鱼巣.vcxproj` | 仓库根工程，仅登记新合同ClCompile，保留其它项 |
| `海中鱼巣.vcxproj.filters` | 仓库根工程，对应唯一工程项归属 |
| `施工记录/20260910_SCENE-C-IDENTITY-ROLE-PHYSICAL-CONTRACT_场景角色与真实绑定施工记录_v0.1.md` | 专属实施事实、知识变更清单、未覆盖项 |
| `验证记录/20260910_SCENE-C-IDENTITY-ROLE-PHYSICAL-CONTRACT_场景角色与真实绑定验证记录_v0.1.md` | 构建/公开探针/恢复及失败矩阵证据 |

临时验证产物仅在`D:/TEMP/海中鱼巣/SCENE-C-IDENTITY-ROLE-PHYSICAL-CONTRACT/<本轮唯一标识>`；验证源码/工程/运行脚本不进入生产仓库。按现行施工规则处置临时验证代码，保留可核对的人读证据和路径。

P1生产范围严格为上述4件；`海中鱼巣/领域/合同.场景状态使用绑定.ixx`及P0两份记录均为只读前置证据，不在白名单。执行者不得重复改写B合同，也不得把其它依赖编译文件纳入写范围。

禁止修改其它生产文件，尤其L1、E/S/D/因果合同和实现、B原DTO/交付/provider、主程序入口、世界/概念树、动态概念、二次关系概念、SELF、普通应用和所有foreign WIP；B合同及其原DTO、交付和provider在P1绝对只读。禁止借本计划改规范/共享知识索引。旧构造未发现本类之外的生产构造调用，三个private helper只有类内旧B函数体消费；七B公开签名和`绑定于`必须保留。S0若发现本片实际删除集的新外部消费者或保留签名发生漂移，具名退回范围问题；第2节已列的基线悬空调用不触发自动扩展，不恢复旧API或拓宽白名单。

## 4. 冻结施工合同

1. 同节点C/E，C本体只由存在owner写；角色登记边+4根+4绑定九项一笔场景owner创建。登记边不单独证明当前角色；对象已有包含不变，无第二父边。
2. 4204角色组织协议v2全部DTO在新合同模块，具名轻量写回执与完整历史路径分开；场景服务只依赖E窄provider和状态类，动态provider仅作为组织动态的const参数，避免循环构造。
3. B保持4203版本1和三个CES角色；真实创建/当前历史/三反查/退出，不自动组织S、不补造观察来源。
4. S/D组织单独一笔新叶边写，目标无同类入边/出边，已有父归根结论在受控追加前提下复用，不能改父；普通回执只返回直接组织，历史路径按需完整读取；D只经现有4213当前读取确认，不展开来源或核验B.C。
5. 当前身份无载荷，历史身份完整；Gr/H和首次发布分账，既有B首次快照回执按4204§6归一化为版本1已冻结形状，不能误用首发G1作当前守卫；该可靠重放不是旧格式解析，不删除。
6. 同键原写集通过L1首次材料还原；无第二应用DTO账。前置失败零写；提交结果未知和读回失败只原键收敛，不回滚。
7. 退出C仅4根+4绑定八项，登记边与E保留，有B/根引用拒绝；通过登记创建时历史组发现原八项并确认退出，禁止同E重启，E删除受登记引用保护。退出B不改CES并受D引用保护。无组织删除或协调清理入口。
8. 元节点登记6项，全部结构编码、固定本地键、角色和顺序按4204；不增加节点值字段、owner参数、通用编辑器或第二数据仓。
9. P0已使B枚举名14退出；P1只读核验1..13、15..23和底层uint8不变。新建场景角色合同无旧格式状态19，19不复用；其它owner的独立同名状态不改。
10. E/S到B、动态当前读取到场景组织及L1写入到场景结果必须分别逐项实现4203 v0.8第5节和4204 v1.1第5.2/6节的全量映射；每个来源枚举覆盖全部现行成员及`default -> 内部不一致`，不得把`依赖未实现`、旧格式、发布未知或矛盾成功载荷折成入口成功。场景组织不调用动态来源展开入口。

新结构错误顺序按4203§5/4204§5.4及详细设计§5、§7.1：C先E身份后新角色，出现新布局先完整核验再判共同退出；B先目标存在/退出，命中新B成员类型出边即完整核验，包括节点非空/元节点/错owner等损坏，不降格未知；无新B类型出边才入口拒绝。所有路径不消费旧证据，无旧识别provider/parser/import或专属错误，保留L1既有事实与引用保护。

## 5. 实施步骤

1. S0确认代码和正式接口；形成精确施工快照、文件登记和验证资源登记，源代码核对已有复用能力；按详细设计§2.1只读确认P0死枚举保持退出，并分别列出旧构造/三个helper/七B旧函数体的实际删除集、七B与`绑定于`保留集和基线悬空旧消费者，不混称全部为“待删除旧入口”。
2. 物理删除获准旧构造与三个helper，保留七B签名并替换旧函数体，加强但不删除`绑定于`，写新合同、请求互证谓词及真实生产构造，并最小登记工程。实际删除集若新发现外部调用，先具名返还范围治理后与获准调用点同次替换；不修改第2节基线悬空消费者。不保留别名、兼容壳、转发重载或运行时回退，不以只删DTO冒充闭合，也不得只停在新的编译骨架。
3. 实现私有同截止读取内核、登记和C九事实启用/历史/八事实退出，真实E验证成功链；结构登记保留技术键1，构造从首次材料互证唯一交付。
4. 替换全部B骨架，完成CES准入、反查、退出、幂等和恢复读回。
5. 实现S/D组织及路径读取，校验全局唯一入边和路径预算；D只执行真实当前读取，不展开来源、不核验B.C且不变更D本体。
6. 完成异常/发布结果未知/重放、新结构准入顺序和模块依赖检查；按详细设计v0.7§9.1依次完成A阶段生产ABI与G01-M机械穷尽、B阶段真实公开成功/生命周期、C阶段合法公开失败/结构破坏；无旧证据识别或错误状态回退。
7. 完成专属施工/验证记录、知识变更清单与精确发布。失败仅修本计划代码；真实合同/范围漂移返还设计，不以默认值和固定拒绝规避。

旧结构清理不授权删除事实数据、用户数据、旧记录文件或他人WIP，也不突破第3节白名单。基线旧消费者仍存在属于明确排除且未修复的既有缺陷，不是允许新提供者留兼容旧模型；完成报告必须分开说明这两个边界。不能为让根工程通过而恢复旧方法、加空函数、删旧消费者或从根工程移除其模块。

## 6. 验证方法与命令边界

文档/差异最低命令：

```powershell
git diff --check
python .\tools\check_specs.py --strict
```

工程XML检查新模块唯一ClCompile/Filter、四配置UTF-8不变，无对新ixx的include。静态检查生产`数据服务.场景类.ixx`不再包含`未实现创建结果`、`未实现读取结果`、`未实现组查询结果`或返回未实现；旧以`状态使用绑定结构交付`为第三参数的构造完全删除；七B签名和`绑定于`保留，新角色合同无旧状态19。B合同作为只读输入核验枚举14未恢复、1..13和15..23不变。另以提交基线扫描第2节旧调用，仅记录其未修复状态，不误列为本片删除失败。

完整包构建在正式起点HEAD归档加本计划4份生产文件精确覆盖的隔离物理副本执行；B合同直接取起点blob且不得覆盖。输入hash逐项与正式blob/本片覆盖一致，不消费异主WIP。仓库外独立探针工程仅编译该副本目标模块及真实消费者的传递import闭包，使用独占IntDir/OutDir；允许字节一致的机械快照，禁止裁剪、修改生产源码或删除正式工程/副本根工程的原有ClCompile项掩盖错误。分别使用当前可定位MSBuild对该外置验证工程执行`/t:Rebuild /p:Configuration=Debug /p:Platform=x64`与Release同参。执行时记录完整命令、工具版本、输入HEAD、精确覆盖集、全部输入源码路径/hash、工程依赖清单、IFC/OBJ/EXE、返回码和输出；本设计不假定固定VS安装目录。

验证按详细设计v0.7§9.1分成可独立执行和记录的A/B/C三阶段，两配置均须通过所有适用编译/运行探针，不能以单一现有烟测代表完整矩阵：

1. A阶段运行仓库外机械检查，从本轮精确依赖blob提取E/S/D与L1读写来源枚举，逐项核对生产映射分支、4203/4204目标状态和终端`default -> 内部不一致`。必须保存“来源成员 + 前置/载荷/发布阶段条件→目标状态或继续核验→生产行”清单，覆盖同一成员按正式条件形成的全部合法分流并排除无依据重复。未知底层值只检查生产default，不运行非法注入；不得增加生产后门、修改L1/端口、重解释对象内存、越界写或复制脱离生产的映射函数。
2. B阶段必须使用真实E/S/D和真实公开L1完成C01、B01、O01-S、O02、R01、I01、X01、P01。O02证明原子D和组合D均经真实当前读取进入纯结构组织，不运行来源展开或B.C一致性检查。核心成功、退出、引用保护、原键收敛和同进程隔离恢复不能由替身或静态证明代替。
3. C阶段完成C02、B02、O01-F、F01、G01-R。E窄provider与动态provider替身只验证其公开虚接口失败面并单独列证；构造保存的真实状态服务和具体L1/写端口不增加注入缝。L1读写中由合法请求、并发、预算、引用保护、幂等或公开恢复可达的状态必须实际运行；不可由公开合同形成的具名状态以G01-M闭合。

每一来源状态记录`RUN_PASS`、`RUN_FAIL`或`UNREACHABLE_BY_PUBLIC_CONTRACT + G01-M证据位置`；后者不是跳过，而是对不可合法运行分支的具名证据闭合。不得用笼统`NOT_RUN`、仅见catch/default、或人工声称穷尽通过门禁。发布结果未知在正式公开L1确实可达时必须验证原键重放收敛；若公开L1不能合法形成该底层状态，仍须实际验证可达的提交后读回失败/首次材料异常收敛路径，并由G01-M证明不可达状态的映射。

L1隔离恢复使用现行公开恢复入口，不修改仓库私有持久文件或引入测试专用生产函数。若恢复基础设施真实不可用，P01失败并具名报告，不将内存对象重建冒充恢复。

根工程已存在第2节的悬空调用，其全量Rebuild不作为本片完成条件；若另行运行须单列实际失败，未运行为NOT_RUN，不能从源码扫描虚构构建日志。根工程结果、目标模块编译、探针链接/运行、L1隔离恢复、数据库跨进程恢复分账；不能通过删除根工程原有模块、压制错误或省略目标必需依赖获得本计划通过。目标必需依赖内若存在无法在本片唯一裁决的错误则具名报告。strict若仅被异主删除的既有记录阻断，记录完整证据并保护其WIP，由发布所有者裁决，不补造记录或谎报严格检查通过。

## 7. 完成报告与剩余边界

4204重建已解除旧自证字段阻断，依赖状态映射和B合同继续保持。P0完成；v0.7关闭执行期验证可实现性DRIFT，v0.8按用户裁决取消场景组织的D来源展开及逐原子B.C核验。发布后索引仍登记P1为`可执行`，但执行者必须按新plan/design blob重新S0并重做受影响的验证清单；既有生产WIP不因本修订自动获得完成结论。

P1报告本包4件生产文件、两份专属记录、公开行为、A/B/C分阶段证据、构建/运行/恢复证据、原键结果收敛与保护反例，以及提交/推送事实。必须明确区分运行实际命中、公开合同不可达但G01-M闭合、以及真正未验证；不得把静态映射证据写成运行命中。计划完成只证明C角色/四根/B/组织路径切片；P0旧枚举保持退出，旧构造/三个helper/七B旧函数体已删除，七B签名/`绑定于`保留；第2节旧消费者仍未修复、根工程未闭合。不能据目标探针通过宣称全项目编译通过、旧消费者迁移完成、完整观察、发生期包含、全场景CRUD、场景TTL、动态概念生产、正式集成验收或业务闭环。

本计划v0.8不声明当前代码与v2相符、验证通过或可发布。执行者须核对新索引、精确plan/design blob、HEAD、所有权及验证资源后继续；G01-M、B阶段真实链和C阶段合法失败任一未闭合时计划不得完成。
