# FS-09 事件日志持久化恢复详细设计专项 v0.1

日期：2026-07-07
状态：已确认 / 文档治理计划 / 未改代码 / 未迁移旧函数

## 1. 依据

```text
计划/20260707_FS09_事件日志持久化恢复增强专项_v0.1.md
实施记录/20260707_FS09_事件日志持久化恢复增强S0当前代码事实扫描_Codex断点清单.md
规范/日志系统规范.md
规范/详细设计/日志系统详细设计.md
规范/错误结果与入口拒绝规范.md
规范/领域服务授权写入规范.md
```

S0 已确认：当前只有通用 `记录事件日志` 文件入口，尚无结构化事件调用点、仓库快照服务或恢复入口。因此本计划先补详细设计，不直接进入 C++ 实施。

## 2. 确认范围

确认后允许执行文档治理：

```text
生成 `规范/详细设计/事件日志持久化恢复详细设计.md`。
明确结构化事件材料字段、写入方、读取方、生命周期和不裁决运行期事实边界。
明确事件日志写入失败不得改变业务结构。
明确事件日志只能记录已由领域服务或仓库正式入口确认的结构事件。
```

## 3. 允许文件

```text
规范/详细设计/事件日志持久化恢复详细设计.md
实施记录/20260707_FS09_事件日志持久化恢复详细设计生成_Codex断点清单.md
计划/计划索引.md
项目记忆/*
```

## 4. 禁止范围

```text
禁止修改 C++。
禁止接 SQL Server ADO。
禁止实现恢复入口。
禁止把日志、SQL 快照、字段恢复比对、控制面板事件流水、线程事件或外设快照作为运行期事实。
禁止迁移旧函数体或旧主信息字段。
```

## 5. 文档要求

详细设计至少包含：

```text
事件日志定位。
结构化事件材料字段。
事件来源准入。
写入方和读取方。
生命周期。
与运行日志、逻辑错误日志、调试日志的边界。
写入失败和空内容拒绝。
不裁决运行期事实声明。
后续 C++ 实施切片入口。
验收矩阵。
```

## 6. 验证方式

```powershell
git diff --check
if (Test-Path .\tools\check_specs.py) { python .\tools\check_specs.py }
```

## 7. 完成声明边界

完成后只能声明事件日志持久化恢复详细设计已生成并待确认。不得声明事件日志结构化材料、仓库快照格式、恢复入口或任何 C++ 代码已实现。

## 8. 待确认问题

```text
FS09-DES-Q1：已确认。结构化事件材料第一轮只记录节点 / 主信息 / 关系 / 索引句柄和入口名，不记录旧 SQL 字段。
FS09-DES-Q2：已确认。事件日志只作为后续恢复材料候选，不作为当前运行期事实源。
```
