# 完整存在概念定义提供者代码实施计划

身份：`EXISTENCE-CONCEPT-COMPLETE-DEFINITION-PROVIDER`
版本：v0.6
状态：可执行
日期：2026-09-16

## 1. 目标与完成条件

实现 7100 v1.8 §2.1 的独立完整存在概念定义四入口，并把其内部定义落入既有 pure 概念 owner 的第四 variant 分支。完整定义的 D/O/R 物理类型不改 pure v2 初始结构；由同 owner、独立版本、独立原键恢复的结构增量提供者形成并读回。成功时完整独立事实返回 O/R 节点、关系、属性值及其生命周期。

不建第二 owner、第二概念族或第二物理概念身份；不改 pure v2 的 15 类型、29映射、写集、交付、原键恢复或旧三分支语义；不做 E、真实事实、使用、支持、名称、上下位、主动归纳或下游世界树施工。

## 2. 依据、S0 与已冻结合同

- 7100 v1.8 §2.1；7110 v1.8 §2、§7；7140 v1.12 §2—§5。
- 同名详细设计 v0.6。

执行 S0 必须确认本计划 blob、现有 pure v2 的固定 15类型/29映射恢复链、合同头、概念树 owner/构造、普通应用白名单、占用表和 Git。v0.4、v0.5 已被执行期 DRIFT 取代；v0.6 已冻结无精确数量的独立公共 ABI、同 owner 增量结构登记、U64稳定编码的 L1 I64 位模式表示、局部键/映射数量、恢复、O/R回传与失败收口，不存在上游提供者门禁。

执行者必须保留当前未发布 WIP 的现场，但在恢复施工前逐项删除本轮新增的“精确数量” DTO字段、属性类型/值、增量结构节点、登记映射、首次材料、规范化、查询、创建和完整读回代码；不得将其转换为兼容字段、保留旧格式分支或恢复 pure v2。该回退只针对本计划施工 WIP，不能触碰其他所有者文件或任一已发布 pure v2 结构。

## 3. 允许与禁止文件

允许：

- `海中鱼巣/领域/合同.相关概念添加参与.h`
- `海中鱼巣/领域/数据服务.概念树类.h`
- `海中鱼巣/领域/数据服务.概念树类.cpp`
- `海中鱼巣/装配.普通应用.h`（仅构造白名单确有必要时）
- `海中鱼巣/装配.普通应用.cpp`（仅构造白名单确有必要时）
- `施工记录/20260916_EXISTENCE-CONCEPT-COMPLETE-DEFINITION-PROVIDER_完整存在概念定义提供者施工记录_v0.1.md`

禁止工程、L1、存在/世界树/特征类、正式规范、计划索引、测试/验证工程，以及任何旧 pure v2 结构或旧分支语义变更。

## 4. 机械施工步骤

1. 在合同头新增独立完整定义、四请求/结果/状态/完整事实 DTO及四公开成员；完整定义加入 pure variant 第四分支，但任何完整公共 DTO 不暴露 pure DTO。
2. 新增 `完整存在概念结构登记请求/结果/交付/首次材料` v1 与静态登记函数。精确实现 9 节点、8关系、1值、18映射的增量写集，纯v2交付作为输入；同键首次材料比对、精确重放、冲突和可能发布完全独立于 pure v2 原键。组成角色、关系类型均以 `std::bit_cast<int64_t>` 写入其 U64 位模式，并以逆向 `bit_cast` 读回。
3. 构造函数接收增量交付并保存同 owner `完整定义布局_`；缺增量交付时完整四入口明确旧格式不支持，旧 pure 入口不受影响。
4. 实现第四分支规范化、D/O/R 写集、同截止完整读回和独立事实映射；定义种类为4，O/R属性仅写入各节点。
5. 实现四公开入口、独立原键恢复/结果映射、零载荷失败、精确重复和可能发布收口；旧三分支函数、结果谓词与写集不修改语义。
6. 仅在现有装配白名单要求时最小传递增量交付；不新建服务或消费者。
7. 写施工记录，含函数级代码知识变更清单、S0、实际文件、未实施范围和免测边界。

## 5. 验证与发布边界

用户明确不构建、运行或测试，不新增测试。只运行 `git diff --check`、`git diff --cached --check`；`check_specs.py --strict` 的异主删除 WIP 缺文件失败如实记录、不处理。提交只证明代码和施工记录发布，不证明编译、运行、恢复、装配消费者或世界存在闭环。

## 修订记录

| 版本 | 日期 | 内容 |
| --- | --- | --- |
| v0.6 | 2026-09-16 | 用户取消精确数量；移除其全部物理/DTO/读回链，增量固定为9节点/8关系/1值/18映射；冻结 U64 稳定编码的 L1 I64 位模式表示，执行者须从保留的 WIP 定向回退重做并重新 S0。 |
| v0.5 | 2026-09-16 | 按用户选择A：pure v2不变，新增同owner版本化完整定义结构增量提供者与O/R属性事实回传；合同闭合，恢复可执行。 |
| v0.4 | 2026-09-16 | 已因pure v2固定初始结构恢复链发生DRIFT。 |
