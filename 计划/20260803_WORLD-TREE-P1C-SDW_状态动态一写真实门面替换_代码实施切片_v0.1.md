# WORLD-TREE-P1C-SDW 状态动态一写真实门面替换代码实施切片 v0.1

计划身份：WORLD-TREE-P1C-SDW

版本：v0.1

创建侧当前代码基线：`0371103724e2d6cb7eda79a6060be15d7d22ef54`

状态：待激活

## 1. 目标与完成条件

在 P1C-F 原模块、原类和原签名中，只把 `发布状态并整理动态` 从固定未实现骨架替换为对 P1B 动态事实所有者的一次真实委托及统一结果映射。其它八根按 S0 基线原样保持。

完成只允许声明该门面根真实消费 P1B-W0 当前能力：首状态成功/幂等及已有前状态未实现；不得宣称后继状态、迁移动能或完整状态动态写入已完成。

## 2. 唯一设计链

```text
流程图/20260803_WORLD-TREE-P1C-SDW_状态动态一写真实门面替换_业务流程图_v0.1.md
流程图/20260803_WORLD-TREE-P1C-SDW_状态动态一写真实门面替换_目标函数流程图_v0.1.md
规范/详细设计/20260803_WORLD-TREE-P1C-SDW_状态动态一写真实门面替换_详细设计_v0.1.md
规范/详细设计/函数结构知识图谱/20260803_WORLD-TREE-P1C-SDW_状态动态一写真实门面替换_函数结构知识图谱_v0.1.md
```

旧 P1C 大设计/计划和旧六写图禁止作为施工依据。

## 3. 激活依赖

1. P1C-F v0.1 代码结果进入正式 main，提供最终门面、五引用构造、目标骨架根和 `WORLD-TREE-P1C-F-S01`。
2. P1B-W0 v0.1 代码结果进入正式 main，在 P1B-F 最终 ABI 内真实提供首状态已提交/幂等及已有前状态未实现；骨架或消息不能替代。
3. P1B-W0 的真实前置 P1B-F、P1B-Q、FEATURE-Q 和 L1混合原子写结果均保持正式兼容；门面不直接调用这些提供者。
4. `服务.世界树.ixx`、同一自检模块和验证资源已释放，无活动语义所有者。
5. P1C-SW、特征两写新版叶和本 P1C-SDW 均已正式发布登记，旧六写图与旧 P1C 大设计/计划已由原所有者丢弃或正式退役。

任一项不满足保持待激活。P1B-W0 后继状态未实现不阻止本门面对其现有真实合同做正确映射，但限制完成声明。

## 4. S0 与允许文件

S0 核对精确计划版本/blob、索引状态、main/origin/index、唯一活动代码段和所有权；逐签名核对 P1C-F、P1B-F、P1B-Q、P1B-W0 正式结果；确认目标根仍是未实现或尚未由同目标替换；保存其它八根及 P1C-F-S01 当前源码证据。

允许文件：

```text
海中鱼巣/领域/服务.世界树.ixx
海中鱼巣/领域/自检.世界树业务服务合同骨架.ixx
施工记录/20260803_WORLD-TREE-P1C-SDW_施工记录.md
验证记录/20260803_WORLD-TREE-P1C-SDW_验证记录.md
```

其它文件全部禁止，特别是 provider、共享 DTO、工程、启动、规范、流程图、详细设计、计划、索引、旧世界服务、运行期路由、线程、SQL、仓库、事务、日志和当前代码图谱。本叶不新增模块、工程项、顶层自检身份或顺序。

## 5. 机械施工

1. 保持模块 include/import、DTO、类、构造、成员顺序、九根声明和静态签名断言不变。
2. 按详细设计 §3 原位实现目标根：原请求调用 `状态动态_.发布状态并整理动态` 恰一次。
3. 按 §4 完整映射十四状态和未知值；不得新增状态枚举、辅助公开函数或第二入口。
4. 按 §5 校验成功代次、缺项、后状态、五关系、时间事实和可选迁移动能；合法则包装原读回，矛盾则结构化内部不一致。
5. 全函数日志调用0，不新增日志 include/helper；不查询、不重试、不补写、不形成第二事务。
6. 原位扩充 P1C-F-S01：真实 provider 只覆盖可达运行路径；不可达坏形状用源码闭包，禁止伪 provider、subclass、坏结果工厂或测试 seam。
7. 逐根证明其它八根和既有专项矩阵前后相等；形成专属施工/验证记录。

## 6. 验证矩阵

| ID | 场景 | 必须结果 |
| --- | --- | --- |
| SDW01 | 默认/非法请求真实入口 | provider恰1；入口拒绝；门面额外写0 |
| SDW02 | 首个基准状态 | 已提交、非零代次、目标=后状态、关系空、原读回包装 |
| SDW03 | 同义重试/异义冲突 | 原幂等读回/幂等冲突；不重算不补写 |
| SDW04 | 已有前状态 | provider未实现14映射门面未实现11；成功后继0 |
| SDW05 | P1B可达许可/资源/内部失败 | 精确映射；只内部不一致保留provider缺项 |
| SDW06 | 十四状态+未知值源码闭包 | 映射唯一；比较未注册->资源失败；三类业务拒绝->入口拒绝 |
| SDW07 | E02—E04源码闭包 | 保留provider代次/持久状态；业务载荷和缺项清空 |
| SDW08 | 调用顺序 | 门面provider恰1；查询/特征/比较/状态/数据操作/提交/快照0 |
| SDW09 | 原子/持久/幂等 | 门面不开事务；provider发布前失败0变化；原结果无损映射 |
| SDW10 | 日志与旁路 | 门面日志、SQL、材料、缓存、时钟、旧入口调用0 |
| SDW11 | 其它八根 | S0与完工源码/行为逐根一致 |
| SDW12 | 聚合 | 原P1C-F-S01/260、既有矩阵和顶层数量不变 |

## 7. 验证命令与失败收口

```powershell
git diff --check -- 海中鱼巣/领域/服务.世界树.ixx 海中鱼巣/领域/自检.世界树业务服务合同骨架.ixx
python .\tools\check_specs.py --strict
msbuild .\海中鱼巣.slnx /t:Rebuild /m:1 /p:Configuration=Debug /p:Platform=x64
.\x64\Debug\海中鱼巣.exe --self-test-exit
msbuild .\海中鱼巣.slnx /t:Rebuild /m:1 /p:Configuration=Release /p:Platform=x64
.\x64\Release\海中鱼巣.exe --self-test-exit
powershell -ExecutionPolicy Bypass -File .\tools\clang_ast\扫描.MSVC模块依赖.ps1 -配置 Debug -平台 x64
```

再用 AST 或正式等价源码闭包证明完整限定签名、唯一函数体、provider调用恰1、其它依赖与日志调用0、十四状态/未知值唯一映射、E02→E03→E04顺序和其它八根保持。创建侧或执行侧不得把工具依赖 PCM 不可用冒充目标 AST PASS；必须保存真实工具状态，并以双配置构建、自检、MSVC模块依赖和等价源码闭包共同收口。

任一 ABI、状态、读回、调用次数、保持根、构建、自检、strict、diff 或所有权漂移，返回 `DRIFT-WORLD-TREE-P1C-SDW-*` 并保留 WIP；禁止建立兼容壳、复制 DTO、伪造 provider 结果、回退旧栈或放宽读回。

## 8. 专属记录与完成边界

专属记录固定为允许范围内两文件。记录实际依赖提交/plan blob、S0/完工 HEAD、命令、退出码、可达运行与不可达源码闭包分账、其它八根保持证据和未执行项。

允许声明：`WORLD-TREE-P1C-SDW 单一状态动态写门面真实消费 P1B-W0 provider，并通过具名矩阵`。

禁止声明：后继状态、状态迁移动能、完整 P1B-W、其它八根、生产路由、旧入口删除、完整世界树业务服务或服务验收完成。
