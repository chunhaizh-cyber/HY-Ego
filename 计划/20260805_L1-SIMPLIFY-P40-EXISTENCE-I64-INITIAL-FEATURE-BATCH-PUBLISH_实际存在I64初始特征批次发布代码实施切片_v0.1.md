# L1-SIMPLIFY-P40 实际存在 I64 初始特征批次发布代码实施切片 v0.1

日期：2026-08-05

状态：待激活

计划身份：`L1-SIMPLIFY-P40-EXISTENCE-I64-INITIAL-FEATURE-BATCH-PUBLISH`

创建侧基线：`b79544472e268bac87c24be8ec12494d7bf47abf`

## 1. 施工目标

在实例特征领域新增 `实际存在I64初始特征批次发布服务::发布实际存在I64初始特征批次`，从一个公开完整有序请求先完成 P15/P38 provider 前幂等收口，再逐项消费 P36 规格，形成一个确定性 L1 v2 写集、通用读回计划和 P39 草案，经 P37/P39 在一个事实代次共同发布全部初始槽、关系、I64值、属性槽和4170记录。

本叶不实现 P32 第二写门面，不支持换代/混合批次，不进入状态/动作动态、因果、任务、结算、恢复装配、运行期接线、旧入口删除、集成验收或 STEP-5。

## 2. 允许文件与专属记录

只允许修改：

```text
海中鱼巣/领域/L1实例特征.数据.h
海中鱼巣/领域/服务.L1实例特征.ixx
海中鱼巣/领域/自检.L1实例特征.ixx
施工记录/20260805_L1-SIMPLIFY-P40-EXISTENCE-I64-INITIAL-FEATURE-BATCH-PUBLISH_施工记录.md
验证记录/20260805_L1-SIMPLIFY-P40-EXISTENCE-I64-INITIAL-FEATURE-BATCH-PUBLISH_验证记录.md
```

禁止修改 L1 核心、P36—P39设计/计划、P38/P39批次账三个文件、P32、工程/filters/启动、运行期上下文、规范、流程图、其它计划或计划索引。若真实模块 import、P15 首次读回或 P39 DTO 无法在三代码文件内消费，退回计划支撑，不得扩白名单。

## 3. 唯一施工依据与依赖

```text
规范/详细设计/20260805_L1-SIMPLIFY-P40-EXISTENCE-I64-INITIAL-FEATURE-BATCH-PUBLISH_实际存在I64初始特征批次发布详细设计_v0.1.md
流程图/20260805_L1-SIMPLIFY-P40-EXISTENCE-I64-INITIAL-FEATURE-BATCH-PUBLISH_发布实际存在I64初始特征批次函数流程图_v0.1.md
规范/4015_子规范_L1简化事实基座.md
规范/4030_子规范_基础信息服务分层与领域写授权.md
规范/4040_子规范_不透明结构事务候选确认撤销与最后发布.md
规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md
规范/4170_子规范_特征批次发布记录与幂等账.md
规范/4200_子规范_世界树业务服务与同代次完整事实快照.md
P15 v0.6、P36 v0.1、P37 v0.1、P38 v0.1、P39 v0.1 正式冻结合同
```

P15/P36/P37/P38/P39 均须先有正式接受结果。计划或设计文本存在不等于代码接口已实现。

## 4. 固定公开合同

精确实现详细设计第4—10节 DTO、映射、摘要、集合准入和根函数：

```cpp
class 实际存在I64初始特征批次发布服务 final {
public:
    实际存在I64初始特征批次发布服务(
        L1事实基座服务& L1,
        L1实例特征服务& 实例特征,
        L1实例特征批次数据操作& 批次数据操作) noexcept;

    实际存在I64初始特征批次发布结果 发布实际存在I64初始特征批次(
        const 实际存在I64初始特征批次发布请求& 请求);
};
```

- 批次身份唯一映射0x0F L1键，禁止第二公开幂等身份；
- 请求项目是 P36 规格请求，不是 provider 后规格；
- 只有成功/幂等读回携带完整项目和4170记录；
- 持久证据状态在 v1 固定不适用；
- 新类只持三个非拥有引用，无 nullable provider、缓存、账、锁或 setter。

## 5. 机械实施顺序

1. 从新正式 HEAD 重做 S0，核对计划状态/blob、五个依赖真实结果、三个目标文件、P13/届时活动代码所有权、顺序320和共享验证资源。
2. 在数据头加入 P40 请求、项目、状态、持久状态和结果 DTO，以及完整性 helper；不改 P36/P8 既有 DTO 数值。
3. 在服务模块 import P38/P39 实际数据操作模块，新增独立发布类；保留现有 `L1实例特征服务` 构造和旧入口签名。
4. 实现0x0F键映射和 P15 确定性 SHA-256 编码；先探测 P15，再预读 P38，严格实现双账矩阵。
5. 仅双未找到时逐项调用 P36，校验完整集合，按 `4*i+1..4` 形成写集、读回计划、执行证据摘要和初始草案。
6. 恰一次调用 P39；机械映射首次、并发精确重复、发布前失败和发布后隔离，成功形成完整0..N-1业务结果。
7. 原位扩展顺序320自检，加入详细设计第11节矩阵；不新增启动根或工程模块。
8. 写专属施工/验证记录，执行全部门禁，只提交三个代码文件和两份记录。

## 6. 禁止实现

```text
禁止公开请求携带第二 L1 幂等身份或调用方摘要
禁止 P15 探测前访问 P36、P38以外可变 provider
禁止 P15/P38 单边存在时扫描、猜测旧键、补账或继续首次路径
禁止排序修复、去重丢项、跨宿主拼批或部分成功
禁止逐项调用 P8旧建立入口、逐项L1提交、第二事实代次或事务后补4170记录
禁止直接使用公开 L1提交、参与者、锁、候选或 P37私有桥
禁止复制4170账、缓存规格、保存调用方引用或从地址/容器顺序形成摘要
禁止发布后删除、回滚、重试或把内部不一致降级为普通拒绝
禁止顺带形成 P32 门面、换代批次、生产装配或运行期接线
```

## 7. 验证门禁

至少执行并记录：

```powershell
git diff --check -- <五个白名单文件>
python .\tools\check_specs.py --strict
msbuild .\海中鱼巣.sln /t:Rebuild /p:Configuration=Debug /p:Platform=x64
msbuild .\海中鱼巣.sln /t:Rebuild /p:Configuration=Release /p:Platform=x64
.\x64\Debug\海中鱼巣.exe --self-test-exit
.\x64\Release\海中鱼巣.exe --self-test-exit
python .\tools\scan_msvc_modules.py --project .\海中鱼巣.vcxproj
```

成功条件：

1. 详细设计第11节12类矩阵具名通过，P36/P8既有顺序320矩阵不退化；
2. Debug/Release Rebuild零警告零错误，两配置完整自检退出0；strict、模块扫描和工程/filters一致性通过；
3. 静态扫描 P15/P38已收口时 P36/P39为0次，首次路径 P36每项1次、P39总计1次，公开L1提交和P8旧单项建立0次；
4. N=1/N>1 均为4N稳定映射、一个事实代次、一条4170记录、完整非夹带结果；
5. staged 精确仅三个代码文件和两份专属记录，其它所有者 WIP 原样。

## 8. 激活门禁

保持 `待激活`，直至：

1. P15 v0.6、P36、P37、P38、P39 真实结果均正式发布并经交互侧接受，plan blob 与实际接口匹配；
2. P36/P39 共享接口及三个目标文件释放，P13及届时唯一活动代码段释放构建、自检和模块扫描资源；
3. 4015/4030—4050/4170/4200 未发生改变批次身份、双摘要、单事务、读回或失败语义的修订；
4. 执行侧从新 HEAD 重做完整 S0，确认0x0F域仍唯一、无等价发布者、旧栈隔离、白名单和顺序320可施工。

若任一依赖的真实结果改变 DTO、模块依赖、首次完整读回、批次—L1键绑定或参与失败分账，退回计划支撑，不得临场新增身份、缓存、第二账、直接历史扫描或扩大目标。

## 9. 完成声明边界

允许声明：源码候选具备实际存在 I64 初始特征完整批次的 provider 前双账收口、P36规格聚合、确定性双摘要/本地键/读回计划、P37/P39单事务发布和完整有序业务结果。

禁止声明：P32第二写门面、换代或混合批次、状态/动作动态、因果、任务/结算、持久恢复、生产装配、运行期接线、旧入口退出、集成验收、STEP-4、STEP-5或治理闭环完成。
