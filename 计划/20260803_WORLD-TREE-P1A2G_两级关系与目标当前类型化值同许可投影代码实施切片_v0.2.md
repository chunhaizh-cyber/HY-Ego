# WORLD-TREE-P1A2G 两级关系与目标当前类型化值同许可投影代码实施切片 v0.2

计划身份：WORLD-TREE-P1A2G

版本：v0.2

修订侧正式代码基线：`440fd54feacf2f215ea8dff5f0d269da26f6ae6f`

来源问题：`VALIDATION-TOOLING-P1A2G-CLANG-PCM-TRANSITIVE-FRIEND`

## 1. 目标、依赖与本版修订

目标不变：在既有 L1 `节点直接结构查询服务` 中形成一次共享读取许可内的业务中性两级关系与
目标当前类型化值投影，只返回一个非零事实截止代次，不解释关系22或特征业务语义，不写机器事实。

具名依赖仍为 WORLD-TREE-P1A2 v0.3 正式结果
`65166e1ce979841502f8420204f6163f588c35e8`；本计划不依赖 P1A2Q、P1A3F、P1BF、SQL 或 Clang PCM 成功。

v0.1 的代码合同保持，但其 Clang 命令参数错误，且把已登记的 Clang/MSVC 命名模块兼容失败设成必过门禁。
v0.2 唯一修订是验证合同：以 MSVC Debug/Release、完整自检、MSVC 模块依赖扫描和目标函数源码闭包
作为必过证据；Clang AST 只作可选候选诊断。目标模块未被解析的传递 PCM 失败不归因 P1A2G。

直接施工依据：

```text
规范/详细设计/20260803_WORLD-TREE-P1A2G_两级关系与目标当前类型化值同许可投影详细设计_v0.1.md
规范/详细设计/20260803_WORLD-TREE-P1A2G_验证补充详细设计_v0.1.md
流程图/20260803_节点直接结构查询服务读取两级关系与目标当前类型化值_函数流程图_v0.1.md
流程图/20260803_节点直接结构查询服务形成当前关系见证_函数流程图_v0.1.md
```

## 2. 允许文件与现场边界

```text
海中鱼巣/核心/节点直接结构合同.数据.h
海中鱼巣/核心/服务.节点直接结构.ixx
海中鱼巣/核心/自检.节点直接身份结构写入.ixx
施工记录/20260803_WORLD-TREE-P1A2G_施工记录.md
验证记录/20260803_WORLD-TREE-P1A2G_验证记录.md
```

禁止修改仓库、事务域、执行器、会话、SQL/材料端口、工具脚本、工程/启动、规范、流程图、详细设计、
其它计划和计划索引。执行侧现有三代码文件 WIP 可在重新 S0 后续作；不得把既有构建结果自动继承为新版本验证。

## 3. 冻结代码合同

DTO、字段顺序、公开根、私有见证助手、完整性谓词、许可顺序、关系合并、值分组、错误映射和自检矩阵
完全继承主详细设计 v0.1。唯一公开根仍为：

```cpp
节点直接两级关系值投影结果 读取两级关系与目标当前类型化值(
    const 节点直接两级关系值投影请求& 请求) const noexcept;
```

根函数只读 `事务域_、节点_、关系_、类型化值_`，只取得一次事务域许可；
SQL、材料、索引仓、类型合同仓和全部写入/提交/撤销/发布入口零调用。
关系和值分别以关系稳定主键和值记录稳定身份去重；版本只参与排序和同身份冲突检查。
全部非成功结果不得携带部分业务载荷。

## 4. 机械施工与失败收口

1. S0 核对本 plan blob、P1A2 v0.3 真实结果、三文件签名/所有权、index 和发布租约。
2. 从当前三文件 WIP逐项对照主详细设计和两张函数图；代码错误在白名单内修复。
3. 完成主详细设计 §8 全部专项验收，保持顺序170与正式自检总数不变。
4. 按验证补充设计 §4 固定目标函数精确行区间、源码摘要、成员调用清单、许可命中数、禁止调用和失败载荷证据。
5. 运行第5节全部必过命令。Clang 工具只按第6节可选分流，不得修改白名单外模块追求通过。
6. 形成专属施工/验证记录，仅暂存五个允许文件并普通发布。

若 MSVC 构建、自检、MSVC 模块扫描或源码闭包证明失败，执行侧必须在三代码文件范围内归因并修复；
若目标接口或机器合同本身无法形成，再退回计划支撑。既有依赖 PCM 失败不是代码 DRIFT。

## 5. 必过验证

```powershell
git diff --check
python .\tools\check_specs.py --strict
msbuild .\海中鱼巣.vcxproj /m /p:Configuration=Debug /p:Platform=x64 /t:Rebuild
.\x64\Debug\海中鱼巣.exe --self-test-exit
msbuild .\海中鱼巣.vcxproj /m /p:Configuration=Release /p:Platform=x64 /t:Rebuild
powershell -ExecutionPolicy Bypass -File .\tools\clang_ast\扫描.MSVC模块依赖.ps1 -配置 Debug -平台 x64
```

成功条件：

- Debug/Release x64 0 error；Debug 完整自检成功且 P1-A13 包含本计划专项；
- strict 全通过；MSVC 依赖扫描全部工程 `.ixx` 成功，源数=模块数、failed=0、循环0、仓内缺失提供者0；
- 根函数签名唯一且 `noexcept`，源码闭包内许可取得恰一次；
- 根函数源码闭包内写入、提交、撤销、确认、发布、推进代次、SQL、材料、索引仓、类型合同仓调用为0；
- 关系/值身份分组键不含版本，所有非成功结果零部分载荷；
- 施工提交只含五个允许文件，staged diff check 通过。

验证记录必须保存函数行区间和源码证据，不得用全文件 `rg` 命中数替代目标函数闭包核对。

## 6. Clang 候选工具可选分流

若执行者选择补充运行 Clang，必须按工具 README 的真实参数先生成依赖 JSON，再以：

```powershell
powershell -ExecutionPolicy Bypass -File .\tools\clang_ast\扫描.Clang模块候选流程图.ps1 `
  -模块依赖路径 <MSVC依赖JSON> `
  -配置 Debug -平台 x64 `
  -目标模块 海中鱼巣.核心.服务.节点直接结构
```

只在“全部依赖 PCM 已成功且目标模块自身报告源代码错误”时阻断本计划。
既有依赖 PCM 失败、目标依赖不可用、PowerShell/stderr 工具异常只记录为候选工具不适用，
不得覆盖 MSVC、运行自检和源码闭包结论，也不得修改执行器、会话或工具脚本。

## 7. 完成条件与声明边界

全部必过验证、源码闭包证据和专属记录通过，精确五文件提交普通推送后，
最多声明 P1A2G 的 L1 同许可业务中性投影源码候选通过本计划矩阵。

不得声明 Clang 全项目兼容、关系22业务完整、FEATURE-Q、特征写入、状态动态、世界快照、生产接线或世界树服务验收通过。
v0.1 及其 plan blob 只作不可变历史，不再构成执行入口。
