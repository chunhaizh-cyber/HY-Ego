# NODE-TYPED-MIGRATION NT-P2C 自我内部世界与事实投影代码实施切片 v0.8

编号：NT-P2C

身份：`#342 / DQ-234 / NT-P2C v0.8`

日期：2026-07-25

设计状态：终审 PASS；本文是不可变复合施工合同，不维护执行过程。

基础合同：

```text
计划/20260722_NODE-TYPED-MIGRATION_NT-P2C_自我内部世界与事实投影代码实施切片_v0.7.md
blob 01f5f81fb0f17de3681bfd7020c9234bd042d40d
```

执行时必须完整读取 v0.7、其基础 v0.6 blob 与本文。本文只裁决 `节点直接存在场景数据操作` 构造 ABI；其它目标、九文件白名单、历史审计、空 P2C 关系写集、幂等、机械阶段、验证、DRIFT 和完成边界全部继承 v0.7。

唯一构造签名固定为：

```cpp
节点直接存在场景数据操作(
    节点直接身份结构事务域& 事务域,
    节点直接身份仓库& 节点仓库,
    正式关系仓库& 关系仓库,
    可重建索引仓库& 索引仓库) noexcept;
```

四项依赖均为非拥有、非空引用。不得增加对应指针重载、默认构造、可空包装或运行期空检查分支。四个被借用对象必须先构造，并覆盖数据操作、退役提供者及其全部参与包的完整生命周期；销毁顺序反向。

专项源码自检必须证明：

- 四引用构造可用；
- 默认构造不可用；
- 任一指针签名和 `nullptr` 装配路径零命中；
- 数据操作、提供者和包均不可逃逸依赖生命周期。

若现有真实类型无法以引用装配，返回 `DRIFT-P2C-DATA-OP-CONSTRUCTION-LIFETIME`，不得恢复指针签名或同时实现两套 ABI。

本计划不构建、不运行程序或自检二进制。完成声明仍以 v0.7 为上限。
