# NT-P4 恢复退役与全仓验收子计划

日期：2026-07-22

版本：v0.1

上级：`#338 / DQ-230 / NODE-TYPED-MIGRATION`

状态：依赖 NT-P3；不可派发

## 目标

形成节点、关系、全部领域类型化记录、失效关系、历史事实和 4170 侧账的统一冻结、严格解析、隔离装载、索引重建和单一运行期上下文首次发布；裁决旧快照策略，最后删除旧仓库、旧句柄、旧字段和工程登记。

## 候选允许文件

- `核心/权威冻结材料.数据.h`
- `核心/仓库快照服务.h`
- 节点 / 关系 / 索引仓库的隔离恢复内部入口
- 各领域记录和 4170 的有界全量冻结 / 隔离候选适配
- `装配.运行期业务.ixx`
- `启动.运行期上下文.ixx`
- `启动.生产运行期.ixx`
- `入口.cpp` 的全新 / 恢复互斥参数路由
- 专属恢复、自我投影重建和全仓退役自检
- `主信息仓库.h/.cpp` 及其工程登记只允许在最终零消费者证明后删除
- 工程文件和 filters。

## 禁止文件

禁止在线热替换、逐仓发布、恢复失败自动全新启动、运行时猜测旧槽位、长期双格式双写、通过普通公开服务注入恢复记录，以及用 SQL、日志或显示补齐候选。

## 旧快照策略

只允许：具名离线迁移器无歧义转换后按 4070 重新准入，或明确拒绝并提供只读审计。不得为兼容旧快照恢复已退出的通用容器。

## 失败收口

解析、分配、装载、关系互证、领域记录互证、索引重建或发布前读回任一步失败，都销毁候选并保持宿主和当前上下文不变。恢复失败不回退全新；已有宿主拒绝第二次发布。

## 验证

未知格式、数量上限、重复身份、悬空关系、错版本、错领域记录、缺历史 / 失效关系、4170 与特征值不一致全部拒绝；导出到再导出值式相等；索引和四类自我投影可清空重建；全新 / 恢复互斥；旧符号、旧仓库文件、工程项、拆分句柄和无类型槽全仓零命中；Debug / Release、完整自检、strict、diff、静态白名单和独立覆盖审计通过。

## 完成边界

只有本阶段和前置全部集成发布、独立审计并由设计窗口接受后，才可声明旧结构物理退役和完整运行期恢复接通。自我跨重启恢复还须以 7130 的唯一自我、内部世界、成员、槽位及状态 / 动态投影全部读回为证据。
