# CONCEPT-S8-S1 删除候选与完整当前输入重建代码实施切片

编号：CONCEPT-S8-S1

队列编号：#216

设计队列：DQ-108

日期：2026-07-11

状态：已生成 / 依赖门控待执行 / 未改 C++ / 未构建 / 未运行程序

## 1. 目标

在不写任何权威结构的前提下，为当前活动图中的退役普通概念形成值式删除影响候选。候选必须使用同一活动版本的完整当前输入，复用固定点共享直接边与传递约简算法全量重算删除后结构，并明确停在 `领域影响已形成`。

## 2. 正式依据与范围一致性

```text
流程图/20260711_概念安全删除候选事务清理与后验固定点流程图_v0.1.md
流程图/20260711_概念安全删除候选事务清理与后验固定点流程图_v0.1.html
规范/详细设计/概念安全删除候选事务清理与后验固定点详细设计.md
实施记录/20260711_CONCEPT-S8-S0_安全物理删除当前代码事实复核_Codex断点清单.md
```

本计划只覆盖流程图 A 至 G、详细设计第 4 节、11.1 和 12.1。三者都只允许只读候选、完整输入与纯值重算，不授予仓库删除、事务提交、活动快照发布或服务登记清理权限。

## 3. 前置与执行顺序

```text
业务前置：#198 已完成并形成正式 S0。
直接接口前置：#199 完成后形成的概念图正式流程纠偏接口。
队列串行前置：#215；因此 #199-#215 均须按既有队列完成或形成正式替代裁决。
后继：#217 STRUCT-TXN-S1。
```

依赖未满足时保持依赖门控并跳过，不标记真实阻塞。

## 4. 已验证当前事实

```text
概念图服务已有四根、活动版本、活动节点 / 关系 / 签名、生命周期、当前支持、节点相关有效关系和受影响分量只读材料。
概念图算法已有签名规范化、更一般比较、固定点候选、直接边无环与传递约简能力。
当前没有删除专用候选，也没有保证同一活动版本的完整当前输入重建入口。
当前删除能力不足，本切片不得调用节点、主信息、关系或索引删除。
```

## 5. 假定接口契约

以下是执行计划假定，不是当前实现事实：

```text
概念删除候选请求：目标概念、预期活动版本、候选数量预算、直接边计算预算。
概念图服务::读取概念删除影响候选(请求)。
概念图算法::重算完整签名直接边(完整签名组, 预算)。
概念删除影响候选携带基准版本、完整目标身份、生命周期 / 相关关系、完整剩余签名、删除后直接边和待补全所有者。
候选完备阶段固定为 领域影响已形成。
```

实际接口命名可按既有代码风格小修；值式边界、完整输入、同一版本、全量重算和不可提交边界不得改变。

## 6. 实施步骤

1. 逐项复核 #199 正式产物以及 #200-#215 是否改变活动快照、签名、生命周期或固定点接口；形成假定接口对照表。
2. 在概念图算法中提取现有创建固定点与删除候选共同使用的纯值直接边 / 传递约简函数，保持既有创建路径复用同一实现。
3. 在概念图服务内生成同一活动版本的完整值式输入副本，覆盖四根、全部活动概念、规范签名、内部概念关系、生命周期与目标相关关系。
4. 写前拒绝四根、非活动图节点、活跃 / 冷却节点、版本漂移、仍有支持或任一外部有效关系的目标。
5. 从完整当前集合移除目标及其概念内部边，按类别全量重算剩余直接边、无环、四根可达和传递约简。
6. 形成 `领域影响已形成` 候选，显式列出索引反查、原子服务、概念登记和统计缓存等待补全所有者。
7. 在入口自检中覆盖准入、完整性、预算、内部矛盾、纯只读和现有固定点创建回归。

## 7. 允许文件

```text
海中鱼巣/领域/概念图算法.h
海中鱼巣/领域/概念图服务.h
海中鱼巣/入口.cpp
规范/仓库逻辑空间与领域树结构规范.md
规范/领域服务授权写入规范.md
计划/20260711_CONCEPT-S8-S1_删除候选与完整当前输入重建代码实施切片_v0.1.md
实施记录/本切片正式实施记录
计划索引和项目记忆治理文件
```

只在实际接口复核证明现有声明必须同步时修改两份规范；不得借规范修改扩大实现范围。

## 8. 禁止文件与能力

```text
节点仓库、主信息仓库、关系仓库、索引仓库和写入事务实现。
存在、动态、二次特征、因果、需求、任务、方法、语素和统计服务写入口。
控制面板、SQL、快照恢复、事件日志、D455、体素和外设。
任何节点 / 主信息 / 关系 / 索引删除、活动快照发布、生命周期转换或服务登记清理。
局部父子重挂算法、写后扫描修补、候选持久化或新增第四生命周期阶段。
```

## 9. 入口拒绝与失败收口

```text
无效句柄、四根、非活动图普通概念、非退役、仍有支持 / 外部引用：逻辑内返回，不生成候选。
活动版本漂移、预算不足或允许的当前材料不足：逻辑内返回拒绝 / 待重算材料，不写结构。
完整当前输入产生环、根不可达、签名矛盾或和活动快照自相矛盾：追根因解决，停止依赖路径。
固定点共享函数与现有创建路径结果漂移：追根因解决，不保留两套算法。
```

## 10. 验收矩阵

| 编号 | 验收项 |
| --- | --- |
| CONCEPT-S8-S1-A01 | 四根、活跃 / 冷却概念、非活动句柄和版本漂移拒绝，结构不变化 |
| CONCEPT-S8-S1-A02 | 退役普通概念仍有当前支持或任一外部有效关系时不生成候选 |
| CONCEPT-S8-S1-A03 | 候选完整携带基准活动版本、目标节点 / 主信息、生命周期关系和全部当前相关关系 |
| CONCEPT-S8-S1-A04 | 完整当前输入覆盖四根、全部活动概念、签名和概念内部关系，材料均来自同一活动版本 |
| CONCEPT-S8-S1-A05 | 删除目标后使用与现有固定点相同的共享直接边和传递约简函数全量重算 |
| CONCEPT-S8-S1-A06 | 删除后直接边无环、四根可达且目标不再出现 |
| CONCEPT-S8-S1-A07 | 预算不足只返回待重算材料，不缩小为局部分量或写结构 |
| CONCEPT-S8-S1-A08 | 完整输入内部矛盾进入追根因，不用局部重挂或扫描修复 |
| CONCEPT-S8-S1-A09 | 候选阶段固定为领域影响已形成，明确列出待补全结构所有者，不可提交 |
| CONCEPT-S8-S1-A10 | 读取候选不改变节点、主信息、关系、索引、活动版本、生命周期或缓存 |

## 11. 验证

```text
Debug x64 构建 0 警告 0 错误。
CONCEPT-S8-S1-A01 至 A10 全部通过。
完整 --self-test-exit 返回 0，最终代码连续 20 轮均返回 0。
既有 CONCEPT-S1 至 S7、PANEL-TREES-S1 与固定点创建回归通过。
静态扫描确认仓库删除、事务提交、活动快照发布、局部移动和禁止依赖 0 新增命中。
git diff --check 与 tools/check_specs.py 通过。
```

## 12. 实际接口复核与漂移退回

执行前必须读取 #199 实施记录、最新概念图算法 / 服务和本计划全部允许文件，逐项对比第 5 节。若完整当前输入无法在本范围内形成、#199 改变活动版本发布语义、共享算法提取会改变既有固定点结果，或需要触碰禁止文件，停止修改并退回设计计划窗口修订，不得在执行窗口扩大范围。

## 13. 完成边界

完成后只可声明删除影响候选和完整当前输入纯值重算已实现并验证。不得声明结构事务、端点冻结、原子清理、安全删除、删除后审计、崩溃恢复或旧能力迁移完成。
