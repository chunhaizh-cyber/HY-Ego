# L2-INITIAL-SCENE-CURRENT-READ-CONSISTENT-PROJECTION-MIGRATION 初始场景当前读回一致投影迁移代码实施切片 v0.1

日期：2026-08-08

状态：待登记；三文档正式发布并取得本文件精确 blob 后，由计划智能体登记为唯一 `可执行`。

## 1. 身份、目标与完成条件

计划身份：`L2-INITIAL-SCENE-CURRENT-READ-CONSISTENT-PROJECTION-MIGRATION`

目标：把 `L1场景结构内部::读取实现` 迁移到现有中性非阻塞一致当前投影，在不改变场景结构公开 DTO、状态、写入、定位缓存和生产调用者的前提下，使初始场景这一合法 L2 生产消费者为完成自身权威当前场景读回而真实消费新投影。

完成条件：一个生产服务文件与两份专属记录精确完成；目标根只调用一次 `尝试读取中性一致当前投影`，固定请求为一节点 + 一关系 + 一个属性值投影；顶层漂移直接返回版本漂移而不局部重试；节点、标记槽 / 值、父关系和世界登记共同截止全部验证；旧分散事实代次 / 节点 / 属性 / 值 / 关系读取在目标根归零；双配置完整重建、双配置模块扫描、strict、静态门禁和正式 Debug / Release `--runtime-context` 运行通过。

直接依据：

- `规范/详细设计/20260808_L2-INITIAL-SCENE-CURRENT-READ-CONSISTENT-PROJECTION-MIGRATION_初始场景当前读回一致投影迁移详细设计_v0.1.md`
- `流程图/20260808_L2-INITIAL-SCENE-CURRENT-READ-CONSISTENT-PROJECTION-MIGRATION_读取实现函数流程图_v0.1.md`
- 0050、4015 v0.6、4030 v1.4、4050 v1.2、4200、ACCEPTANCE-01 v0.4、0600
- 当前已发布 `L1中性一致当前读取请求/结果`、`尝试读取中性一致当前投影` 和世界登记一致当前读回结果

计划可执行只表示设计和当前接口足以机械施工，不证明代码已实施、构建、运行、结构化消费者成功或 L1 已完善。

## 2. 执行前 S0 与停止条件

执行智能体从登记后的正式新 HEAD 重做 S0：

1. 核对本计划 v0.1 是计划索引中唯一 `可执行` 行，核对精确 plan blob、设计提交和索引提交；不得从聊天或本文件“待登记”文字开工。
2. 核对创建侧起点 `e0b1d40bc3fadb53944157cb7cee02e9cbc48b53` 之后正式增量只包含本三文档与索引登记；若 HEAD 继续前进，按新 HEAD 重新确认目标 blob、世界登记结果和生产调用边。
3. 核对一致当前请求仍含六组选择、非零期望代次；成功与请求同序等长；顶层非成功六组空；漂移回显非零实际代次。
4. 核对世界登记 `读取当前登记` 仍返回本次权威当前截止；场景 helper 必须以该截止精确请求，不得按漂移回显局部重试。
5. 核对场景合同版本 3、十个状态数值、请求 / 事实完整性、目标函数签名、固定一节点 / 一值 / 一关系 / 一槽写入和生产调用边未漂移；目标生产文件无异主 WIP。
6. 核对 Git index 为空，唯一活动代码段、发布租约、构建、模块扫描和生产运行资源可用。P0-FIX、路线、异主 `.codex-build`、两份 WORLD-TREE 和其它 WIP 均不得触碰、暂存或带入验证输出。

若一致投影 DTO / 状态、世界登记截止语义、场景公开合同、目标函数或生产调用边出现会改变实施的漂移，或实现必须触碰第 3 节之外路径，停止受影响切片并具名退回计划支撑。普通 include 清理、编译错误和白名单内机械实现问题由执行智能体自行修复。

## 3. 精确允许与禁止范围

只允许修改或新增：

1. `海中鱼巣/领域/服务.L1场景结构.ixx`
2. `施工记录/20260808_L2-INITIAL-SCENE-CURRENT-READ-CONSISTENT-PROJECTION-MIGRATION_施工记录.md`
3. `验证记录/20260808_L2-INITIAL-SCENE-CURRENT-READ-CONSISTENT-PROJECTION-MIGRATION_构建与生产消费验证.md`

禁止修改场景数据头、世界登记、核心 L1、其它领域服务、组合服务、启动、运行期上下文、配置、工程、filters、自检、集成验收、规范、详细设计、流程图、计划、计划索引和异主 WIP。

严格排除 `创建实现` 的写入与幂等路径、`读取接纳实现`、`读取完整结构实现`、直接存在成员退出和同文件其它读取根。目标根外现存完整快照、事实代次、单项当前读取和关系组调用必须原样保留；不得按文件级归零删除其它业务合同。禁止为验证新增 L1 自检、专项驱动、第二 exe、生产 PASS、日志解析或私有探针。

## 4. 目标函数、请求和所有权

- 唯一目标根：`L1场景结构内部::读取实现(const L1事实基座服务&, const 世界登记结果&, const 初始场景读取请求&)`。
- 公开创建 / 读取函数、DTO、状态数值、服务构造和定位缓存保持不变。
- 世界登记结果提供本轮世界根、场景标记类型、父关系类型、服务来源和权威截止；场景 helper 不缓存或更新登记。
- L1 唯一拥有共享 try-lock、事实代次和中性当前投影；场景服务唯一解释初始场景节点、标记和父关系业务形状。
- 可在现有内部命名空间新增目标专用顶层 / 项目状态映射和失败形状校验，全部中文命名、最小可见性；不得改变供其它根使用的既有 `读取状态` 映射。

固定请求六组：节点 `{初始场景}`；关系 `{直接父关系}`；属性值 `{初始场景, 场景标记属性类型}`；值、源关系组和目标关系组为空；期望代次为登记本次已验证截止。

## 5. 实施步骤

1. 保持登记成功 / 完整性、读取请求有效和世界根一致校验；失败零一致投影调用。
2. 新增目标专用一致读取顶层 / 项目状态映射和顶层失败形状校验，不改现有旧读取状态 helper。
3. 以登记已验证事实代次形成详细设计第 4 节固定 `1/1/0/1/0/0` 请求。
4. 物理调用 `尝试读取中性一致当前投影` 恰一次。事实代次漂移映射版本漂移，不使用回显实际代次重试；许可拒绝立即返回，不等待、不重试。
5. 成功要求合同、期望回显、读取代次和六组长度精确；读取代次必须等于登记已验证截止。
6. 项目未找到或属性未设置映射未找到；已退出、种类不匹配、成功无事实 / 投影和坏失败形状映射内部不一致。
7. 验证初始场景普通节点、唯一标记槽、投影当前值等于请求场景标记值、来源服务、登记属性类型和 `std::int64_t(1)`。
8. 验证直接父关系编码、世界根源端、初始场景目标端、登记关系类型、顺序零和未退出。
9. 要求节点、值、关系创建事实代次共同、非零且不晚于读取代次；按现有字段组装 `初始场景事实`，返回已读取。
10. 删除目标根对 `读取中性当前事实代次`、`读取中性当前节点`、`读取中性当前属性`、`读取中性当前值` 和 `读取中性当前关系` 的全部调用；其它函数零差异。
11. 写两份专属记录，如实记录静态范围、正式生产运行、结构化消费者未独立验收范围和 L1 不自证边界。

## 6. 固定结果与失败收口

| 场景 | 固定结果 |
| --- | --- |
| 登记非成功 / 不完整 | 按既有登记状态映射；零一致投影调用 |
| 读取请求无效或世界根不一致 | 入口拒绝；零一致投影调用 |
| 一致读取同截止成功 | 完整验证后已读取 |
| 顶层事实代次漂移 | 版本漂移；不按实际代次局部重试 |
| 顶层许可拒绝 | 许可拒绝；零等待零重试 |
| 顶层资源失败 | 资源失败 |
| 顶层入口拒绝 / 内部不一致 / 坏失败形状 | 内部不一致 |
| 节点 / 关系未找到，或属性未找到 / 未设置 | 未找到 |
| 项目已退出 / 异种 / 成功缺事实，或业务字段矛盾 | 内部不一致；无部分事实 |

读回失败不得修改定位缓存、补写、换编码、调用历史 / 恢复或伪装成功。

## 7. 验证合同

至少执行：

```powershell
git diff --check -- '海中鱼巣/领域/服务.L1场景结构.ixx' '施工记录/20260808_L2-INITIAL-SCENE-CURRENT-READ-CONSISTENT-PROJECTION-MIGRATION_施工记录.md' '验证记录/20260808_L2-INITIAL-SCENE-CURRENT-READ-CONSISTENT-PROJECTION-MIGRATION_构建与生产消费验证.md'
python .\tools\check_specs.py --strict
MSBuild.exe .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m:1
MSBuild.exe .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Release /p:Platform=x64 /m:1
pwsh -NoProfile -File .\tools\clang_ast\扫描.MSVC模块依赖.ps1 -项目根目录 . -配置 Debug -平台 x64 -输出路径 <系统临时目录\Debug模块依赖.json>
pwsh -NoProfile -File .\tools\clang_ast\扫描.MSVC模块依赖.ps1 -项目根目录 . -配置 Release -平台 x64 -输出路径 <系统临时目录\Release模块依赖.json>
.\x64\Debug\海中鱼巣.exe --runtime-context
.\x64\Release\海中鱼巣.exe --runtime-context
```

尖括号表示执行者在系统临时目录中创建的本轮唯一绝对路径，不是字面参数。构建和扫描不得使用、删除、覆盖或枚举异主 `.codex-build`；模块扫描不得写正式图谱输出。双配置 Rebuild 和两个生产运行必须退出 0；双配置扫描必须所有候选成功、拓扑完整、循环 0、外部未解析需求 0；strict 必须全部现行目录项通过。

静态门禁：

- `读取实现` 内 `尝试读取中性一致当前投影` 物理与逻辑调用均恰 1；不存在重试循环、第二次调用、阻塞回退或 sleep。
- `读取实现` 内 `读取中性当前事实代次`、`读取中性当前节点`、`读取中性当前属性`、`读取中性当前值`、`读取中性当前关系`、`读取完整快照` 和 `尝试读取完整快照` 均为 0。
- 固定请求六组长度为 `1/1/0/1/0/0`；标记值编码不进入值组；期望代次来自登记已验证截止。
- 顶层事实代次漂移直接映射版本漂移；许可拒绝零重试；入口拒绝 / 坏失败形状映射内部不一致。
- 成功验证节点、槽、值、关系共同截止和创建代次、值来源 / 类型 / I64(1)、关系端点 / 类型 / 顺序。
- 非成功不组装部分事实、不修改定位缓存；公开 DTO / 枚举 / 函数签名零差异。
- `读取实现` 之外的 `创建实现`、`读取接纳实现`、`读取完整结构实现`、退出及其它场景函数相对施工起点行为零变化；它们的旧读取调用不得因本叶归零。
- 世界登记、核心 L1、启动、运行期上下文、生产会话、工程、filters 和自检零修改。
- staged 与提交范围只含第 3 节三路径；P0-FIX、路线、`.codex-build`、两份 WORLD-TREE 和其它 WIP 零暂存、零修改。

生产运行退出 0 只证明正式入口没有拒绝启动，不单独证明结构化初始场景业务成功。若本代码施工段不含独立生产代码外消费者验收，验证记录必须明确写“公开租约连续两次结构化场景 / 登记读回未在本段独立执行”。后续集成验收只能经有效运行期上下文租约连续两次调用 `读取初始场景` 和 `读取世界登记`，要求结构化已读取、逐字段一致且共同截止。

## 8. 完成声明边界

本计划施工完成最多证明初始场景这一具名 L2 当前读回根已迁移到中性非阻塞一致当前投影，并通过约定构建、静态和正式生产入口运行门禁。只有合法消费者实际取得结构化已读取，并从公开场景 / 登记入口核对完整终态，才可累计“一节点 + 一关系 + 一属性值一致当前投影”的本消费者真实使用证据。

初始场景创建已经由既有验收覆盖，不得在本叶重复冒充新增证明。不得声明 L1 自证、第一层已完善、关系组、修改、退出、历史、其它消费者或完整场景结构已经迁移，或构建 / 静态检查 / 生产退出码等于业务成功。
