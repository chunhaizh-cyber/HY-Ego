# RUNTIME-TXN-S1 唯一运行期上下文与四仓库主装配接域代码实施切片 v0.1

日期：2026-07-12

队列编号：#247 / 设计队列：DQ-139

状态：已按 #246 当前事实复核 / 依赖门控待执行 / 前置 #217、#246 / 假定最终回归顺序 660 / 未改 C++

## 1. 依据与范围

流程图：`流程图/20260712_运行期主装配结构事务接域与恢复链单一所有权流程图_v0.1.md` 及同名 HTML。详细设计：`规范/详细设计/运行期主装配结构事务接域与恢复链单一所有权详细设计.md`。关联能力 `NEW-02`、`MIG-00`，不计旧能力迁移完成度。

## 2. 依赖门控

已验证事实：#217 只在自检模块形成接域隔离组；当前入口有一组生产主仓库和十二组历史自检仓库，全部未接域；当前无生产上下文、宿主或租约。假定合同：#217 提供正式接线 ABI；#246 已确认全新 / 恢复为互斥分支并共用一个上下文类型、一个发布槽和一个租约源。预期产物：一个生产上下文类型、一份协调状态、一份正式接线、四个同身份仓库和空宿主一次发布合同。实际接口漂移时不改代码，退回设计。

## 3. 文件分类与范围

| 文件 | 类型 | 登记 / 调用 |
| --- | --- | --- |
| `海中鱼巣/启动.运行期上下文.ixx` | 生产真模块 | `ClCompile` / `import 海中鱼巣.启动.运行期上下文` |
| `海中鱼巣/自检.运行期上下文.ixx` | 自检真模块 | `ClCompile` / 入口登记预期 660 |

允许文件：上述两个新模块、`海中鱼巣/入口.cpp`、`海中鱼巣.vcxproj`、`.filters`、本计划 / 实施记录 / 计划索引 / 项目记忆。四仓库和 `结构事务接线.数据.h` 只允许包含 / 构造，不允许修改；若 #246 证明必须改构造签名，本计划先退回修订允许清单。禁止：`海中鱼巣/核心/**` 修改、恢复算法、快照格式、SQL / 面板 / 外设、第二协调器、入口验收正文、旧行为 `.h/.cpp`。

## 4. 实施步骤

S0 读取 #246 逐字段裁决和实际阶段表；S1 建固定模块骨架并单独编译；S2 用一个协调状态生成接线并按“协调器、接线、四仓库、服务”顺序构造，析构逆序；S3 建立共用于全新 / 恢复互斥分支的空宿主一次发布和租约合同，入口全新分支只顶层调用；S4 独立自检域 / 纪元 / 状态一致、重复发布拒绝、析构顺序和并发租约。恢复候选适配仍由 #237 消费 #229 许可后调用同一发布合同。

## 5. 入口拒绝与失败收口

域编号零、接线不完整、仓库混装、重复发布、上下文未初始化写前拒绝。前置通过后仓库身份或读回不一致归追根因；不得扫描修复或另建上下文。

## 6. 验收与验证

A01-A12 覆盖唯一所有者、四仓同身份、发布一次、入口最小化、660 唯一登记和结构不变拒绝。执行 Debug x64、完整自检、严格规范、`git diff --check`、并发租约 50 轮和最终连续 20 轮；完成后提交推送。

## 7. 完成边界

只证明主装配四仓库接域，不证明全部领域服务接线、恢复、快照或 ACID。
