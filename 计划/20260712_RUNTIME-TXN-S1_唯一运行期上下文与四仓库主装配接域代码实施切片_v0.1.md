# RUNTIME-TXN-S1 唯一运行期上下文与四仓库主装配接域代码实施切片 v0.1

日期：2026-07-12

队列编号：#247 / 设计队列：DQ-139

状态：已按 JY-290 修订 / 前置 #217、#246 已完成 / 待执行 / 假定最终回归顺序 660 / 只形成结构核心与宿主租约骨架 / 缺领域服务时禁止生产发布 / 未改 C++

## 1. 依据与范围

流程图：`流程图/20260712_运行期主装配结构事务接域与恢复链单一所有权流程图_v0.1.md` 及同名 HTML。详细设计：`规范/详细设计/运行期主装配结构事务接域与恢复链单一所有权详细设计.md`。关联能力 `NEW-02`、`MIG-00`，不计旧能力迁移完成度。

## 2. 依赖门控

已验证事实：#217 只在自检模块形成接域隔离组；当前入口有一组生产主仓库和十二组历史自检仓库，全部未接域；当前无生产上下文、宿主或租约。假定合同：#217 提供正式接线 ABI；#246 已确认全新 / 恢复为互斥分支并共用一个上下文类型、一个发布槽和一个租约源。JY-290 明确本切片只形成协调器、接线、四仓库结构核心和宿主 / 租约骨架；领域服务类型仍由 #248 建立，因此 #247 结构核心不得成功生产发布，也不是恢复链可消费的最终上下文 ABI。预期产物：一份协调状态、一份正式接线、四个同身份仓库、不可生产发布的结构核心和空宿主 / 租约骨架。实际接口漂移时不改代码，退回设计。

## 3. 文件分类与范围

| 文件 | 类型 | 登记 / 调用 |
| --- | --- | --- |
| `海中鱼巣/启动.运行期上下文.ixx` | 生产真模块 | `ClCompile` / `import 海中鱼巣.启动.运行期上下文` |
| `海中鱼巣/自检.运行期上下文.ixx` | 自检真模块 | `ClCompile` / 入口登记预期 660 |

允许文件：上述两个新模块、`海中鱼巣/入口.cpp`、`海中鱼巣.vcxproj`、`.filters`、本计划 / 实施记录 / 计划索引 / 项目记忆。四仓库和 `结构事务接线.数据.h` 只允许包含 / 构造，不允许修改；若 #246 证明必须改构造签名，本计划先退回修订允许清单。禁止：`海中鱼巣/核心/**` 修改、恢复算法、快照格式、SQL / 面板 / 外设、第二协调器、入口验收正文、旧行为 `.h/.cpp`。

## 4. 实施步骤

S0 读取 #246 逐字段裁决、#217 实际构造依赖和阶段表；S1 建固定模块骨架并单独编译；S2 用一个协调状态生成接线，按“协调器、接线、主信息、节点、关系、索引”顺序构造结构核心并验证析构逆序；S3 建立共用于全新 / 恢复的空宿主 / 租约骨架，但生产发布入口必须因领域服务装配缺失而写前拒绝，不得把结构核心写入当前槽；S4 独立自检域 / 纪元 / 状态一致、不完整发布拒绝、析构顺序和租约骨架。完整首发、重复首发与并发首发成功 / 拒绝转由 #248/670；恢复候选只在 #248 最终类型形成且 #249 验收后由 #237 消费。

## 5. 入口拒绝与失败收口

域编号零、接线不完整、仓库混装、领域服务装配缺失、上下文未初始化均写前拒绝。#247 不存在成功生产发布路径；若结构核心进入当前槽，归追根因。前置通过后仓库身份或读回不一致归追根因；不得扫描修复或另建上下文。

## 6. 验收与验证

A01-A12 覆盖唯一结构所有者、四仓同身份、协调器 / 接线 / 主信息 / 节点 / 关系 / 索引构造与逆序析构、宿主 / 租约骨架、缺服务不可发布、入口最小化、660 唯一登记和结构不变拒绝。执行 Debug x64、完整自检、严格规范、`git diff --check` 和最终连续 20 轮；完成后提交推送。不得用假服务、无类型槽或自检专用发布后门制造成功首发。

## 7. 完成边界

只证明主装配四仓库结构核心接域及宿主 / 租约骨架；不证明完整生产上下文可发布、领域服务接线、恢复、快照或 ACID。#248 完成后才可形成最终上下文所有权声明。
