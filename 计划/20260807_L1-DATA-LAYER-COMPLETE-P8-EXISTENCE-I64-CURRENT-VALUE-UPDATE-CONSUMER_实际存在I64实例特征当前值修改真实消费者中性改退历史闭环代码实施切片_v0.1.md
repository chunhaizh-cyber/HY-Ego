# 实际存在 I64 实例特征当前值修改真实消费者中性改退历史闭环代码实施切片 v0.1

日期：2026-08-07

状态：待登记；登记后待激活

计划身份：`L1-DATA-LAYER-COMPLETE-P8-EXISTENCE-I64-CURRENT-VALUE-UPDATE-CONSUMER`

## 1. 目标、完成条件与依据

原位迁移唯一 `L1实例特征服务::换代实际存在I64实例特征当前值`，使合法第二层消费者通过中性写集完成一个新 I64 值发布、当前属性槽切换和旧当前值退出，并经正式入口重新读取新当前、旧历史和当前槽。完成必须同时具备：const 写入口、0 节点 + 0 关系 + 1 值 + 1 槽变更 + 1 退出的唯一中性写集、L1 最小幂等、首次与重复权威读回、旧值当前不可读而历史可回查，以及全部非成功零变化。

依据：0050、2100、4015 v0.5、4030 v1.3、4110 v0.7、ACCEPTANCE-01 v0.3，以及：

- `规范/详细设计/20260807_L1-DATA-LAYER-COMPLETE-P8-EXISTENCE-I64-CURRENT-VALUE-UPDATE-CONSUMER_实际存在I64实例特征当前值修改真实消费者中性改退历史闭环详细设计_v0.1.md`
- `流程图/20260807_L1-DATA-LAYER-COMPLETE-P8-EXISTENCE-I64-CURRENT-VALUE-UPDATE-CONSUMER_换代实际存在I64实例特征当前值函数流程图_v0.1.md`

本叶只让真实消费者使用第一层改、退和历史查，不建立 L1 自检或独立通过结论。旧值退出不是物理删除；历史事实必须保留。

## 2. 激活门禁、S0 与所有权

本计划创建侧基线为 `65344c892dfe1441c197b2bee15c5bb69c25c786`。P7 代码候选 `07771fd4346d687ad58476cc80484eb86ce6be8e` 与消费者验收 `65344c892dfe1441c197b2bee15c5bb69c25c786` 已进入正式 main，验收结论为第二层消费者公开黑盒局部通过，P7 接口和矩阵成立。登记状态仍固定为 `待激活`，只有以下剩余事实全部成立后才可由计划所有者改为可执行：

1. P7 计划正式退出，且实际 `L1实例特征服务` 四引用构造、登记定位、实际存在实例建立、正式读取和 const 访问接口与本计划继续机械匹配；
2. 实例特征服务文件、构建、运行、模块扫描和固定验证输出资源可用；
3. `main == origin/main`、Git index 空，目标三文件没有异主 WIP，唯一活动代码段和 Git 发布租约可用。

执行 S0 必须从届时正式新 HEAD 重读计划索引、精确计划 blob、详细设计、流程图、目标函数、P7 结果、L1 中性提交精确重复合同和全部共享资源。P7 验收消息、未提交目录或当前候选代码不能替代正式门禁。

## 3. 精确允许范围

只允许修改或新增：

1. `海中鱼巣/领域/服务.L1实例特征.ixx`
2. `施工记录/20260807_L1-DATA-LAYER-COMPLETE-P8-EXISTENCE-I64-CURRENT-VALUE-UPDATE-CONSUMER_施工记录.md`
3. `验证记录/20260807_L1-DATA-LAYER-COMPLETE-P8-EXISTENCE-I64-CURRENT-VALUE-UPDATE-CONSUMER_构建与生产链验证.md`

禁止修改数据头、L1 核心 / 仓库 / 中性合同、运行期上下文、其它领域服务、生产启动、工程、filters、自检、规范、详细设计、流程图、计划、计划索引或集成验收目录。禁止触碰或夹带 P7 验收现场、P0-FIX、路线、旧 WORLD-TREE、`.codex-build` 和其它 WIP。

## 4. 实施步骤

1. 为 `换代实际存在I64实例特征当前值` 增加 const 重载；非 const 重载只转发 const 实现，所有业务分支只保留一份。
2. 新增专属中性写集纯值 helper：域 0x07，固定形成 0 节点、0 关系、1 个 I64 值、1 个当前属性槽变更和 1 个旧值退出；不接收或生成业务标签、意图、摘要、见证、审计或恢复材料。
3. 新增专属中性历史值读回 helper 和成功合同检查，显式映射中性状态；复用 P7 已有中性代次、节点、值、属性、映射和实例正式读取 helper，不恢复 legacy 读取。
4. 把当前完整换代账对本目标函数的职责替换为仅含已进入本入口幂等身份的同锁路由定位；不保存请求、写集、旧事实、验证材料、首次发布结果或新值事实。未迁移的初始场景换代继续使用原 legacy 账，不改变其语义。
5. 身份未定位时先建立暂定定位，再按详细设计顺序读取登记、起始代次、P7 当前实例、P6 定义、新来源中性节点和结束代次；登记与其余材料必须共同等于请求期望事实代次，逐字段互证并在提交前完成值域、来源和共同截止准入。
6. 身份已定位时不读取已经退出的旧当前值；直接由本次请求和登记形成完整中性写集。无论首次还是已定位路径都恰一次调用 `提交中性写集`，由 L1 唯一裁决精确重复、同键异义和期望代次。
7. 成功类结果必须回显唯一新值编码和首次发布代次。首次提交非成功时删除暂定定位；成功、精确重复或发布后读回失败时保留定位，以允许原请求再次经 L1 收敛。
8. 从映射形成新的实际存在实例读取请求，固定执行：读回起始代次、新当前正式读取、旧值中性当前已退出、旧值中性历史完整、当前属性指向新值、读回结束代次；逐字段验证新值创建代次和旧值退出代次等于首次发布代次。
9. 删除本目标路径的 G4 意图、幂等预探测、provider 见证、执行证据、业务标签、完整快照、legacy `提交写集`、事实缓存和首次结果直返。只删除确认不再被其它未迁移函数引用的专属 helper；共享 legacy helper 留待其所有消费者迁移后再删。
10. 写两份专属施工 / 验证记录，记录真实改动、验证、P7 依赖、消费者验收待办和未覆盖范围；不得写“L1 自证通过”或“整个 L1 已完善”。

实际接口、P7 DTO、L1 中性精确重复映射、历史读回字段或共享 helper 所有权发生会改变实现的漂移时停止并具名退回计划支撑；普通编译和局部代码错误在白名单内修复。

## 5. 验证合同

至少执行并记录：三路径 `git diff --check`、`python .\tools\check_specs.py --strict`、Debug / Release x64 串行 Rebuild、两配置正式生产入口、两配置模块依赖扫描和 staged 精确范围核对。

静态门禁：

- 目标函数只有一份 const 业务实现，非 const 入口只原样转发。
- 目标路径中 `探测幂等`、领域意图、执行证据、provider 见证、业务操作标签、legacy `提交写集` 和 `读取完整快照` 调用均为 0。
- `提交中性写集` 在目标路径恰一个物理调用点；写集固定 0 节点 + 0 关系 + 1 值 + 1 属性槽变更 + 1 退出。
- 成功和精确重复均要求一个本地键 1 到同一首次新值编码的映射；精确重复不形成新权威发布。
- 目标幂等定位只保存幂等身份，不保存请求、写集、首次结果、旧 / 新领域事实或首次发布材料。
- 新当前读回经 P7 正式入口；旧当前、旧历史和当前属性只经中性公开入口；首末中性事实代次一致。
- 未迁移的 `换代I64实例特征当前值`、初始场景实例路径、P7 登记 / 建立 / 读取、运行期上下文、数据头、L1 核心 / 仓库 / 合同和其它服务差异均为 0。
- 生产工程中不新增自检、验收运行器、双目标、PASS 聚合、专用入口或第二程序。

构建、生产入口退出码和静态扫描只证明代码候选。代码发布后另做生产代码外合法消费者验收：通过正式生产会话和有效租约取得 const 实例服务，在同一合法链内取得 P7 当前实例；执行合法换代，再经领域正式读取入口独立核对新当前。换代入口只有在内部完成旧当前已退出、旧历史完整和当前属性指向新值三项中性权威读回后才能返回成功。随后执行精确重复、同键异义冲突、新值越界、新来源无效和写前漂移；每个非成功前后都经领域正式读取入口比较适用权威状态零变化。驱动不得导入或调用 L1，不解析日志。

## 6. 完成声明边界

施工完成最多证明实际存在实例特征当前值换代的中性代码候选和 const 生产调用边界成立。消费者验收通过后，才可累计本消费者实际使用的一个值新建、属性修改、旧值退出、当前不可读、历史可回查、原子性、精确重复、同键异义和非法请求零变化。

不得宣称节点 / 关系删除、关系替换、其它消费者、恢复、旧栈删除或整个 L1 已完善；当前不覆盖崩溃和跨进程恢复。
