# D455-P3 对齐、滤波、点云、分割与原始逐簇形成代码实施切片 v0.2

编号：D455-P3；路由：#363 / DQ-255；合同：PER-C3 / ABI 2；日期：2026-07-24

设计状态：终审 PASS。当前状态、版本和 plan blob 唯一读取 `计划/计划索引.md`。

## 合同与边界

- 依据：6300、6310、6350、6360；`规范/详细设计/D455成熟度产品受控材料与报告队列详细设计.md` v0.2；`流程图/20260724_PERCEPTION-INGEST_D455成熟度产品受控材料报告队列施工流程图_v0.2.md`；`规范/详细设计/函数结构知识图谱/20260724_PERCEPTION-D0_D455观察体素生产闭环函数结构知识图谱_v0.2.md`。
- 已实现事实：现有 L0 同步彩色、深度、左右红外、标定和采集元数据可只读；C1 为待实现合同。
- 待实现：`D455逐簇处理ABI版本 = 2`；`D455逐簇处理结果 形成D455原始逐簇产品(const D455同步帧材料&, const D455逐簇规格&, const D455产品身份&)`。
- 点云是 L1 派生物；坐标 / 尺寸用 I64 微米，质量用 I64 百万分倍率；不得输出存在句柄。

## 文件所有权

允许：`海中鱼巣/适配/处理.D455逐簇产品.ixx`、`海中鱼巣/适配/自检.D455逐簇产品.ixx`、`实施记录/20260724_D455-P3_PER-C3逐簇处理代码实施_Codex断点清单.md`。

禁止：采集器、缓存、上行桥、C1 / C2 / C4—C13、工程、入口、运行器和全部设计文件。

## 施工与 S0

1. S0 核对 plan blob、现有 L0 精确字段、C1 DTO / ABI 2 待实现合同和两代码文件所有权。
2. S1 建立对齐、滤波、点云、分割规格和确定性逐簇 DTO；保留设备、生产代次、帧范围、标定与规则版本。
3. S2 实现纯处理入口，具名区分空帧、标定不匹配、深度无效和无合法簇。
4. S3 自检源码覆盖单位边界、空 / 越界、来源继承、稳定排序和重放同义。
5. S4 核对静态合同、白名单、禁止 import 和 diff。
6. S5 只提交允许代码与专属记录，推送执行通道分支并恢复 clean。

## 机械验证命令

S0 记录 `$计划段起点 = (git rev-parse HEAD).Trim()`；S4 的两个 diff check 均须退出 0。S5 后运行：

```powershell
$期望 = @('海中鱼巣/适配/处理.D455逐簇产品.ixx','海中鱼巣/适配/自检.D455逐簇产品.ixx','实施记录/20260724_D455-P3_PER-C3逐簇处理代码实施_Codex断点清单.md') | Sort-Object
$实际 = @(git -c core.quotepath=false diff --name-only "$计划段起点..HEAD") | Sort-Object
if (Compare-Object $期望 $实际) { exit 1 }
if ((@(rg -n "D455逐簇处理ABI版本\s*=\s*2" '海中鱼巣/适配/处理.D455逐簇产品.ixx')).Count -ne 1) { exit 1 }
if ((@(rg -n "形成D455原始逐簇产品" '海中鱼巣/适配/处理.D455逐簇产品.ixx')).Count -lt 1) { exit 1 }
```

`git diff --check`、`git diff --cached --check` 和上段命令均须退出 0；ABI 精确 1 处。

## 验证与收口

- 只做静态和专项源码验证，不构建、不运行，不登记工程。
- 现有 L0 字段不足返回 `DRIFT-PER-CURRENT-INTERFACE`；C1 类型漂移返回 `DRIFT-PER-CONTRACT`；需要第三代码文件返回 `DRIFT-PER-FILE-OWNERSHIP`；出现反向依赖返回 `DRIFT-PER-IMPORT-CYCLE`。
- 前置通过后坐标、来源或产品版本矛盾为 `FAIL-PER-INTERNAL-READBACK`；其它为 `FAIL-PER-VALIDATION`。
- 完成只证明 PER-C3 候选，不证明算法质量、编译、报告发布或世界事实。
