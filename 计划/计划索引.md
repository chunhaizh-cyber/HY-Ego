# 计划索引

日期：2026-09-02

## 1. 索引职责

本文件是当前计划的唯一运行导航，只登记当前有效计划的精确路径、版本、状态和具名依赖。计划之间只登记具名依赖，不建立计划树，不记录窗口、智能体、分支、活动计划段、执行过程或历史叙述。

计划状态只允许：

- `待激活`：计划正在形成、需要修订或真实门禁尚未满足，执行智能体不得选择。
- `可执行`：精确计划版本与 blob 已完成创建侧设计验证并满足执行前门禁，执行智能体可以在完成 S0 后选择。
- `已退出`：计划不再作为当前执行候选。

计划状态只裁决计划是否可被选择，不证明代码已经实施、S0 已通过、活动计划段已经取得、接口真实能力已经完成或验证已经通过。

## 2. 登记与清理规则

1. 计划必须先形成现行正式文件并完成要求的设计、创建侧验证、提交和推送，才能登记其精确路径、版本、blob、状态和具名依赖。
2. 新计划的索引登记和正常状态维护由计划智能体负责；计划支撑智能体只修改执行侧具名退回计划的受影响索引项；交互智能体在用户直接要求时可以取得明确切片并修改。
3. 执行智能体只能从“当前有效计划”中选择状态为 `可执行` 且具名依赖已经满足的计划；消息、聊天回执、施工记录、旧索引或文件名都不能代替正式登记。
4. 计划文件被删除、移入 `计划/已完成计划/` 或 `计划/过期计划/` 时，必须同步删除其当前计划登记和依赖引用。归档文件只作历史证据，不构成执行候选。
5. 不存在于当前正式仓库的计划路径不得登记；已删除计划不得由索引、旧消息或历史材料恢复。
6. 新计划必须以新的正式计划文件和不可变版本重新登记；不得复用已删除计划的旧状态、旧 blob 或旧执行许可。
7. 当前有效计划列表为空时，执行智能体必须保持待命，不得从归档目录、历史消息或旧提交自行选择计划。
9. 自 2026-08-15 起，新的 DATA 计划只允许按现行三层架构登记：`DATA-L1` 纯事实内核、`DATA-L2` 带结构类型语义的强类型结构 CRUD、`DATA-L3` 世界树与概念空间结构聚合；`DATA-L4`、`DATA-L5` 当前未定义。DATA 计划不得拥有业务准入、资格、真实性、目标、动作、完成性、成功性、状态 / 动态自动生成、因果真伪或自我证明。已删除的旧 DATA 计划和详细设计不得由旧索引、消息、提交或同名版本恢复；后继必须从现行规范和代码事实建立新身份、新文件与新登记。
10. 2026-08-20 有限执行队列迁移后，迁移前的 `可执行`和`待激活`库存统一退出当前候选；其计划文件和原门禁正文只作路线或历史证据。当前索引只保留当前 `可执行` 项和至多一个紧邻 `待激活` 后继；当前可以为空。已完成与失效计划通过归档目录和实施清单导航，不在当前队列表重复登记。

## 3. 当前有效计划

当前执行队列只保留当前最早可执行代码闭环；当前可以为空。根目录中尚未完成唯一裁决的计划仍是未登记材料，不具备执行资格。

| 计划身份 | 精确计划 | 版本 / blob | 状态 | 具名依赖与门禁 |
| --- | --- | --- | --- | --- |
| `RUNTIME-GOVERNANCE-SERVICE-REQUEST-INITIAL-PLANNING-WORK-PACKAGE` | [真实服务请求材料到服务根初次筹办工作包代码实施计划 v0.4](20260902_RUNTIME-GOVERNANCE-SERVICE-REQUEST-INITIAL-PLANNING-WORK-PACKAGE_真实服务请求材料到服务根初次筹办工作包代码实施计划_v0.4.md) | v0.4 / `4dec89c9784cf7e0b761e4328e98db97fa829322` | 可执行 | 直接依赖：已发布 P01 工作包结果消息链；修订基线 `9947a378c9140ca91f98195f073159f4b56f95d2`。门禁：E/M/D 同 G0 强类型读回、服务根任务初次准备记录 / R1 绑定；初次和后继 DATA-L2 规范化来源都必须显式携带 task owner 正式任务轮次，后继任务轮次只来自 manager 在工作包迁移后 G 调用`按任务读取当前任务与轮次`的同 G 正式读回，不得从原工作包、槽或裸整数推断。每个已经开始的筹办逻辑形成一个且仅一个五类结果，工作线程通过唯一强类型消息交 manager；manager 已接收或确认精确重复后本工作包处理才正常结束，随后按消息任务 / 任务轮次 / G 独立读回。两份正式测试消费者只允许统一 DTO/API 适配及直接断言 hunk；施工前后工作区文件内容哈希必须一致，禁止带入当前 P01 专项删除或自我治理全文件重写 WIP。不得据此声明具体需求/方法执行、现实动作、路径验证、目标达成、任务完成、需求结算、价值/安全/服务值、学习、跨进程恢复、运行期业务矩阵或完整路线闭环。 |

`INSTINCT-ROUTE-P01-PLANNING-WORK-RESULT-MESSAGE` v0.1（plan blob `8ab1738f9d1a0bd8f74f202149da9bfdb2ad1308`）已由代码结果提交`d792cd7a79ceee133494c132c82b4f9c02fca92d`完成施工发布，并由施工 / 验证记录提交`fda42126ed8a1bfab2b461703eb2474f95355ee3`完成记录发布，现退出当前候选。该结果只证明正常运行和正常收口条件下的进程内“一个已消费筹办工作包—一个强类型结果消息—任务管理线程幂等接收”链及 P01 专项编译结果；专项运行未执行，不证明 P02 子目标准入、P03 子需求后继、跨进程恢复、整条本能路线或业务验收。

`INSTINCT-SERVICE-CONTRACT-ACCOUNTING-RETIREMENT` v0.1（plan blob `8a4b00065c66dbec22930413f988f2ef6aa4fc52`）已由结果提交 `37ced00ca7cc0ee18a4e901d70af6d672e99040f` 完成目标核验并退出当前候选。

`INSTINCT-ROUTE-P00-TASK-ROUND-PROVIDER` v0.1（plan blob `487761b4b68724b6e944c5a0230bd0319ad85097`）已由结果提交 `1fce5c9e817bc88f3fba750ceeaee5994a5d9ff1`发布 task owner 的新任务 R1 生产与同 G0 读回，并退出当前候选；该结果不证明 P01 消息链或更远后继。

`INSTINCT-STAGE3-GSEED-ACTIVATION-CURSOR-COMPAT-MIGRATION` 已由结果提交 `64f589c9285e77439216b8a19b09542480c674d6` 完成 v0.3 验证闭合并退出当前候选。该旧结果只作历史证据；Gseed、服务维护游标和合同驱动被动维护已由当前规范退役，不得恢复为后继依赖。

历史导航：
- `DATA-L1-FINAL-CLOSURE` v0.3（plan blob `c6de76077fb0c85939352f67c5df9160f6d917ca`）已由候选 `f8805cb3d0142aaaacce87b6351b4b7cac1c22c1` 完成修复，并由 R2 提交 `e0d51d30aad0be92fd61bb9efb71ca6490e2cad3` 的 `验证记录/集成验收/DATA-L1-FINAL-CLOSURE/manifest_v0.3.json`（blob `ac838dbf53a21a881cc3bce35cac2fd98e0e3d69`）绑定为 `服务验收通过`；整体 `FROZEN`，已退出当前执行队列。
- 已完成施工证据：`计划/已完成计划/`（只读归档，不提供执行许可）。
- 已失效历史方案：`计划/过期计划/`（只读归档，不得原地恢复）。
- 本次目录分层与逐文件裁决：`实施记录/20260821_计划目录清理清单.md`。
- 2026-08-31 增量裁决：`实施记录/20260831_计划目录第三轮增量清理清单.md`。
- 结果证据目录：`施工记录/`、`验证记录/`。
