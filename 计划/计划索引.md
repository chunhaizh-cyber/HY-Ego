# 计划索引

日期：2026-09-23

## 1. 索引职责

本文件是当前复杂代码计划的唯一运行导航，只登记当前有效计划的精确路径、版本、状态和具名依赖；不保存历史叙述、施工过程或完成结论。历史施工计划和 `施工记录/` 已经退出；已发布差异与验证历史只从 Git、验证记录、集成验收记录及仍有效的 `实施记录/` 追溯，二者不得混同。

计划状态只允许：

- `待激活`：计划正在形成、需要修订或真实门禁尚未满足，执行智能体不得选择。
- `可执行`：精确计划版本与 blob 已完成创建侧设计验证，计划内静态合同和具名上游依赖已经满足；执行前仍须完成 S0。
- `已退出`：计划不再作为当前执行候选。

计划状态只裁决计划能否被选择，不证明代码实施、构建、运行、恢复、验收或业务闭环已经完成。用户直接且符合 `AGENTS.md` 直接代码切片门禁的局部实施不登记本文件。

## 2. 登记与清理规则

1. 计划须先形成现行正式文件并完成要求的设计、创建侧验证、提交和推送，才能登记精确路径、版本、blob、状态和具名依赖。
2. 计划完成、替代、漂移、撤回或失效后，删除其现行计划文件并同步删除索引项；不再移入 `已完成计划` 或 `过期计划`，也不以历史文件保留执行入口。
3. 当前有效计划为空时，执行智能体不得从历史消息、旧提交或已删除计划身份选择计划。
4. 当前索引只保留可执行计划和至多一份合同已冻结、只等待直接上游的紧邻待激活后继；更远下游只保留在目标、路线输入或设计材料中。
5. 历史归档裁决读取 `实施记录/20260821_计划目录清理清单.md`、`实施记录/20260831_计划目录第三轮增量清理清单.md` 和 `实施记录/20260921_计划目录第四轮清理清单.md`。

## 3. 当前有效计划

| 计划身份 | 计划 | 版本 / blob | 状态 | 具名依赖 |
| --- | --- | --- | --- | --- |
| `SECONDARY-RELATION-CONCEPT-STRUCTURE-P1` | [二次关系概念结构所有者闭环代码实施计划](20260921_SECONDARY-RELATION-CONCEPT-STRUCTURE-P1_二次关系概念结构所有者闭环代码实施计划_v0.1.md) | v0.3 / `77d895aa2375f060214adb53da2747b6604f3bb4` | 可执行 | 4015 v2.6（机器合同不变，仅对齐头源承载和证据治理）、4180 v0.15、4182 v0.3、4240 v0.43、4242 v0.4、RC差距设计v0.5及RC-P1 v0.3；执行侧必须基于当前规范和该精确计划blob重新S0，保留既有RC WIP并按新provider合同继续。 |
| `INSTINCT-ROOT-RUNTIME-ANCHOR-PROVIDER` | [本能根运行锚点组合提供者代码实施计划](20260923_INSTINCT-ROOT-RUNTIME-ANCHOR-PROVIDER_本能根运行锚点组合提供者代码实施计划_v0.1.md) | v0.1 / `e6bd60b499b14bcceec3d37b5377262c0f31859a` | 可执行 | 8121 v0.6、8120 v0.16、6130 v0.17、4115 v0.9、7130 v0.19、8100 v1.12、8200 v1.9及详细设计v0.1；已按执行侧具名DRIFT修正一次尝试provider和验证分账：真实栈只证成功主链与两进程恢复，正式非成功、部分前缀及逐守卫漂移由内部脚本验证同一组合边界，禁止复制私有合同、绕过provider或直达L1；执行侧须废止旧blob并从当前HEAD重新S0。 |

## 4. 路线输入（不是当前执行候选）

- `INSTINCT-ROOT-RUNTIME-ANCHOR` 旧 v0.1 已删除：其绑定旧接口和旧事实，具名 DRIFT；当前组合provider由上表新计划承担，禁止恢复旧计划身份或旧 blob。普通应用、启动阶段21和self停门消费仍是代码结果发布后的紧邻后继，不是当前执行候选。
- `WORLD-TREE-STRUCTURE-B` 旧施工计划已随退役模块证据清理删除；用户暂停状态不变，它不是当前执行候选。恢复时必须基于现行 `.h/.cpp`、有效详细设计和当前接口重新设计并登记新版本，不能恢复旧计划身份或旧 blob。
- `DYNAMIC-4213-CONFORMANCE-CLOSURE`已在动态概念总实现方案中确认缺口，4213物理定位已治理为现行`.h`，但7150组成发生期物理提供者及4213精确消费ABI尚未闭合；它与当前RC-P1不共享机器语义，当前仅作路线输入，不提前登记第二份计划。

## 5. 历史追溯边界

- 历史施工计划和施工记录已经删除，不再提供目录导航或恢复入口。
- `LEGACY-CONCEPT-INTERFACE-REMOVAL` 的已发布代码结果为 `18239d93c`；`SELF-THREAD-G1-ISOLATED-SHELL` 的已发布代码结果为 `1445f4250`，后者只证明单对象隔离生命周期专项。
- 需要追溯其它退出计划时，只能读取 Git 提交、仍保留的验证 / 集成验收记录和现行正式规范；历史材料不得恢复为当前执行候选。
- 本索引不重复保存历史完成结论，也不从已删除计划或施工记录补造完成范围。
