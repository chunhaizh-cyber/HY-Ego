# RUNTIME-TXN-S2 领域服务接线与直接删除失败清理代码实施切片 v0.1

日期：2026-07-12

队列编号：#248 / 设计队列：DQ-140

状态：已按 #246 逐调用点矩阵复核 / 依赖门控待执行 / 前置 #247 / 假定最终回归顺序 670 / 未改 C++

## 1. 依据

同 #247 流程图与详细设计；必须消费 #246 的删除 / 失败清理矩阵。关联 `NEW-02`、`MIG-00`，不计旧能力迁移。

## 2. 门控事实与假定

当前生产服务引用一组未接域仓库；世界树初始化、语素初始化、语素服务和概念图服务存在创建期补偿、已发布变更和内部不一致清理，入口七处直接删除均为测试材料。假定 #247 导出唯一上下文和接线只读入口；#246 已把清理点分为六类。预期产物为 `装配.领域服务` 和 `自检.领域接线` 真模块、服务值式接线、调用期同令牌链及已发布删除正式路由。接口或文件归属漂移时退回设计。

## 3. 文件与范围

| 文件 | 分类 | 登记 / 调用 |
| --- | --- | --- |
| `海中鱼巣/装配.领域服务.ixx` | 生产真模块 | `ClCompile` / `import 海中鱼巣.装配.领域服务` |
| `海中鱼巣/自检.领域接线.ixx` | 自检真模块 | `ClCompile` / 入口登记 670 |

允许修改：上述模块、`入口.cpp`、工程文件，以及 `领域/初始化.世界树.ixx`、`初始化.语素.ixx`、`语素服务.h`、`特征服务.h`、`存在服务.h`、`场景服务.h`、`状态服务.h`、`动态服务.h`、`二次特征服务.h`、`因果服务.h`、`需求服务.h`、`任务服务.h`、`方法服务.h`、`概念图服务.h` 中 #246 矩阵明确命中的接线 / 清理入口。禁止其它核心仓库、线程 / 界面、恢复 / SQL / 外设文件，禁止仓库裸写、令牌进入 DTO / 消息 / 成员许可或复制清理算法。入口七处历史直接删除只随后续自检迁移搬离，不得改接为生产清理路由。

## 4. 步骤

S0 复核 #247 上下文和 #246 矩阵；S1 固定服务装配模块；S2 为传统服务注入同一值式接线；S3 分别改接世界树 / 语素创建期补偿、概念图已发布变更和内部不一致清理，保持同一许可与追根因语义；S4 自检写前拒绝、补偿不可读、候选丢弃、内部异常追根因和并发读写。安全删除逐仓库提交继续等待 #218/#219 补齐不可伪造业务授权，结构独占令牌不得冒充该授权。

## 5. 非成功收口

请求无效、准备未完成、候选未发布为逻辑内返回且结构不变；完成写入后读回、关系、索引、计数异常归追根因。禁止用日志、扫描或空读兜底。

## 6. 验收

A01-A14 覆盖全部 #246 清理点、无遗漏直接删除、服务同接线、调用期许可、锁序、670 登记和入口无正文。Debug x64、完整自检、严格规范、diff、并发 50 轮、最终 20 轮，随后提交推送。

## 7. 边界

不证明恢复、跨进程事务或所有未来服务自动接域。
