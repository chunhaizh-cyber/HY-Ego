# 函数功能说明清单专项

编号：HR-001-FD
日期：2026-07-05
状态：v0.1 / 已建立 / 已确认 / 未迁移旧函数

## 1. 目标

处理 S0 缺口 3：

```text
说明书/函数功能说明文档.md 不存在，无法满足函数迁移前函数事实登记和服务级确认门禁。
```

本专项只建立当前项目函数功能说明基线，不迁移旧项目函数。

## 2. 输出

```text
说明书/函数功能说明文档.md
```

说明文档必须覆盖：

```text
核心仓库函数
日志和容错函数
领域服务公开函数
入口程序局部函数
私有辅助函数清单
```

## 3. 门禁规则

```text
没有函数功能说明，不迁移旧函数。
没有说明函数用途、使用范围和对应规范，不迁移旧函数。
没有说明保留行为、删除行为和新增约束，不迁移旧函数。
没有归入已确认服务逻辑包、服务专项计划或服务代码实施切片，不迁移旧函数。
高级服务白名单必须写完整：只允许直接依赖存在、场景、特征、二次特征、状态、动态和因果服务；不得直接依赖特征值服务或把世界服务当作写入捷径。
```

## 4. 当前状态

```text
第一轮当前函数说明清单已建立并经用户确认。
说明状态为已确认。
本确认只覆盖当前项目已有函数说明，不代表旧项目函数已迁移。
迁移旧能力前仍必须登记相关旧函数功能说明、保留行为、删除行为和新增约束，并归入服务逻辑包；不再逐函数确认。
```

## 5. 禁止宣称

```text
不得声明旧项目函数已迁移。
不得声明旧项目函数说明已经全部覆盖。
不得声明旧项目能力迁移完成。
```
