# WORLD-TREE-FEATURE-HQ 具名已发布特征值材料真实读取代码实施切片 v0.1

计划身份：WORLD-TREE-FEATURE-HQ

版本：v0.1

创建侧代码基线：`466d1ab0272ebb122203354a2a1e7be893db964a`

## 1. 目标、状态和完成条件

在 P1BF v0.4 第二公开骨架原位实现具名已发布特征值材料真实读取。合法入口直接且恰调用一次 L1 当前类型化值组读取；只在唯一精确当前记录、材料和存在来源全部完整时返回非零代次材料，其余按冻结状态失败且无部分载荷。

本计划创建时 P1BF v0.4 只有正式设计/可执行计划，代码尚无结果，因此状态必须为待激活。P1BF v0.4 代码结果、最终 plan blob/接口、顺序200/21项自检和写权释放全部进入正式Git后，计划智能体才可复核激活。

当前正式 P1BF v0.4 plan blob 为 `55eb168b6212b839fed99451363b06cf62d6e3fc`，设计提交为 `2602343b56487f6099b89c54eb22c3a420be5c29`；二者只冻结合同，不替代代码结果。

## 2. 唯一施工依据

```text
流程图/20260803_WORLD-TREE-FEATURE-HQ_具名已发布特征值材料真实读取业务流程图_v0.1.md
流程图/20260803_节点直接特征查询服务读取具名已发布特征值材料_函数流程图_v0.2.md
规范/详细设计/20260803_WORLD-TREE-FEATURE-HQ_具名已发布特征值材料真实读取详细设计_v0.1.md
规范/详细设计/函数结构知识图谱/20260803_WORLD-TREE-FEATURE-HQ_具名已发布特征值材料真实读取函数结构知识图谱_v0.1.md
规范/详细设计/20260803_WORLD-TREE-P1BF_特征服务合同骨架详细设计_v0.3.md
规范/4020_子规范_领域类型化数据记录与组合读取投影边界.md
规范/4170_子规范_特征批次发布记录与幂等账.md
规范/4200_子规范_世界树业务服务与同代次完整事实快照.md
规范/4201_子规范_世界树合同骨架与未实现结果.md
```

## 3. 激活门禁和DRIFT

S0必须确认：P1BF v0.4 真实结果已进入正式main；共享材料事实、请求、结果、服务类、构造和完整签名逐字一致；骨架仍在目标函数中；L1签名/状态/结果字段一致；唯一S01/200和总数21存在；两个允许代码文件写权释放。

任一不满足零代码修改：

```text
DRIFT-WORLD-TREE-FEATURE-HQ-P1BF-PROVIDER
DRIFT-WORLD-TREE-FEATURE-HQ-L1-INTERFACE
DRIFT-WORLD-TREE-FEATURE-HQ-DTO
DRIFT-WORLD-TREE-FEATURE-HQ-OWNERSHIP
```

## 4. 精确允许文件

```text
海中鱼巣/领域/服务.节点直接特征查询.ixx
海中鱼巣/领域/自检.节点直接特征查询合同骨架.ixx
施工记录/20260803_WORLD-TREE-FEATURE-HQ_施工记录.md
验证记录/20260803_WORLD-TREE-FEATURE-HQ_验证记录.md
```

其它文件全部禁止。尤其禁止共享头、DTO、工程、启动、P1BF计划、L1、4170、SQL、旧特征栈、规范、流程图、详细设计、其它计划、计划索引和当前代码图谱。

## 5. 唯一函数和机械施工步骤

```cpp
节点直接具名特征值材料读取结果
节点直接特征查询服务::读取具名已发布特征值材料(
    const 节点直接具名特征值材料读取请求& 请求) const noexcept;
```

1. 记录HEAD、plan blob、P1BF结果提交、完整签名、DTO字段和顶层自检数量21。
2. 在服务文件全局模块片段按详细设计§3补充 `<algorithm>`、`<new>`、`<stdexcept>`，不依赖传递包含。
3. 按函数图N01—N02实现合同版本1、完整见证和特征值类型准入；非法入口返回入口拒绝且L1调用0。
4. 合法入口直接且仅调用一次 `结构_.读取所属身份当前类型化值组(请求.特征值.稳定主键)`；不得调用第二provider。
5. 按详细设计§4映射L1状态；成功代次0映射内部不一致。
6. 按§5要求值组恰一、所属稳定主键相等、完整身份精确相等和当前=true；同键异身份版本漂移，其它基数/键矛盾内部不一致。
7. 按§6逐项复核合同、记录、材料、前一版本、两个代次和存在来源；禁止服务来源、SQL或4170补全。
8. 按§7逐字段形成十字段事实并返回L1非零代次；不得变换材料或补默认值。
9. 按§8实现无抛异常映射；不增加日志、重试或诊断依赖。
10. 原位扩充S01/顺序200专项自检，不新增顶层项，数量仍为21。
11. 形成专属施工/验证记录；全部矩阵通过后才声明本叶代码候选完成。

## 6. 验证矩阵

- 非法合同、残缺见证、错节点类型：入口拒绝，L1调用0；
- L1七状态逐项映射，非成功均0/nullopt；
- L1成功代次0；值组0/1/2；同键异版本；错稳定键；当前=false；
- 合同身份/版本、记录身份/版本、前一版本每一反例；
- 各材料variant正反例；首次/当前状态/L1截止代次边界；
- 来源为完整存在节点正例，服务来源、不完整、错类型反例；
- 十字段逐项相等和深拷贝；
- bad_alloc、length_error、其它异常；
- SQL、4170批次、旧特征栈、缓存、日志、时钟调用边0；
- 调用前后权威仓、幂等账、持久侧账和事实代次逐值相等；
- S01/200唯一，登记/执行总数21。

静态闭包必须证明目标完整签名定义1；非法路径L1调用0、合法路径恰1；实参精确为请求特征值稳定主键；宿主组函数调用0；仓库/事务/SQL/4170/旧栈调用0。

## 7. 验证命令和失败收口

```powershell
git diff --check
python .\tools\check_specs.py --strict
msbuild .\海中鱼巣.vcxproj /m /p:Configuration=Debug /p:Platform=x64
.\x64\Debug\海中鱼巣.exe --self-test-exit
msbuild .\海中鱼巣.vcxproj /m /p:Configuration=Release /p:Platform=x64
.\x64\Release\海中鱼巣.exe --self-test-exit
rg -n "读取具名已发布特征值材料|读取所属身份当前类型化值组|WORLD-TREE-P1BF-S01" 海中鱼巣
```

MSVC模块依赖与目标函数源码闭包必过；Clang只作现行规则允许的候选诊断。任一失败保留WIP，不扩大白名单、不修改L1/P1BF合同、不更新索引为完成。

## 8. 记录与完成边界

```text
施工记录/20260803_WORLD-TREE-FEATURE-HQ_施工记录.md
验证记录/20260803_WORLD-TREE-FEATURE-HQ_验证记录.md
```

完成只允许声明具名已发布特征值材料读取在本矩阵内形成真实代码候选。不证明FEATURE-Q、4170批次引用、特征写入、比较、状态动态、世界快照、生产接线或服务验收完成。
