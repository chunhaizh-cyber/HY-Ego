# SERVICE-DATA-S5 需求任务方法服务分层迁移代码实施切片 v0.1

日期：2026-07-13

队列编号：#269 / 设计队列：DQ-161

状态：依赖门控待执行 / 前置 #268、750 / 最终回归 760 / 未改 C++

## 1. 范围与假定

迁移需求、任务、方法和方法召回的创建、目标状态、承接授权、生命周期、筹办选择、方法角色和动作桥读取路径。业务服务继续拥有授权 / 状态机 / 选择语义，仓库和数据操作层不得裁决。假定全部基础服务无令牌公开面稳定。

## 2. 新模块

`领域/数据操作.需求任务方法.ixx`、`领域/服务.需求任务方法.ixx`、`领域/组合.需求任务方法.ixx`、`领域/自检.需求任务方法分层.ixx`。#262 若要求按领域拆分，执行前修订为多个真模块和切片。

## 3. 允许与禁止

允许上述模块、既有需求 / 任务 / 方法 / 方法召回头的兼容适配、基础服务只读端口、工程 / 自检 / 治理。禁止改变需求目标状态、任务授权、任务方法选择、终态封口和方法执行权；禁止实施 `#214/#224/#257`。

## 4. 失败与验收

逐业务入口验证前置拒绝、幂等异义、选择换代、生命周期和动作桥；多仓库失败精确撤销。A01-A20、公开面无令牌 / 仓库、兼容回归、Debug x64 0/0、完整自检、连续 20 轮、严格规范、提交推送。

## 5. 完成边界

不证明完整任务执行、结果结算、方法学习或概念命名闭环完成。
