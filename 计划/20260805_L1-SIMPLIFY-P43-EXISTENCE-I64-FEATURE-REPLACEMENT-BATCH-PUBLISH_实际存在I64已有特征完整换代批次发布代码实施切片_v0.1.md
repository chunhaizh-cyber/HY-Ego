# L1-SIMPLIFY-P43 实际存在 I64 已有特征完整换代批次发布代码实施切片 v0.1

日期：2026-08-05

状态：待激活

计划身份：`L1-SIMPLIFY-P43-EXISTENCE-I64-FEATURE-REPLACEMENT-BATCH-PUBLISH`

创建侧基线：`3047ceb04b4b78e3570c27b79ee27a1677e6b5f2`

## 1. 施工目标与完成条件

在实例特征领域新增 `实际存在I64已有特征换代批次发布服务` 和唯一 `发布实际存在I64已有特征换代批次`。它必须在 provider 前完成 P15/P38 双账收口，消费完整有序 P42 规格，以 `N值 + N属性槽切换 + N旧值退出` 的一个 L1 v2 写集和一条 P39/P38 全换代记录共同发布，并返回本批次完整稳定结果。

完成条件：全组同宿主/同截止/顺序连续且定义、槽、旧值唯一；0x0F 批次身份跨P40/P43唯一；同批同义幂等、异义冲突；首次只推进一次代次；失败零部分结果；P42/P39调用顺序与次数精确；完整验证通过。

## 2. 设计包与代码白名单

设计包：

```text
规范/详细设计/20260805_L1-SIMPLIFY-P43-EXISTENCE-I64-FEATURE-REPLACEMENT-BATCH-PUBLISH_实际存在I64已有特征完整换代批次发布详细设计_v0.1.md
流程图/20260805_L1-SIMPLIFY-P43-EXISTENCE-I64-FEATURE-REPLACEMENT-BATCH-PUBLISH_发布实际存在I64已有特征换代批次函数流程图_v0.1.md
计划/20260805_L1-SIMPLIFY-P43-EXISTENCE-I64-FEATURE-REPLACEMENT-BATCH-PUBLISH_实际存在I64已有特征完整换代批次发布代码实施切片_v0.1.md
```

代码白名单：

```text
海中鱼巣/领域/L1实例特征.数据.h
海中鱼巣/领域/服务.L1实例特征.ixx
海中鱼巣/领域/自检.L1实例特征.ixx
施工记录/20260805_L1-SIMPLIFY-P43-EXISTENCE-I64-FEATURE-REPLACEMENT-BATCH-PUBLISH_施工记录.md
验证记录/20260805_L1-SIMPLIFY-P43-EXISTENCE-I64-FEATURE-REPLACEMENT-BATCH-PUBLISH_验证记录.md
```

不得修改 P15/P37/P38/P39/P40/P42、世界树门面、工程/filters/启动、旧特征栈或其它所有者 WIP。

## 3. 固定接口与所有权

按详细设计第4节新增两版本、请求/项目/状态/结果 DTO 和相等比较；不改既有枚举值、P40 DTO 或 P41 provider 类型。新增类、三引用构造和根函数：

```cpp
实际存在I64已有特征换代批次发布服务(
    L1事实基座服务& L1,
    L1实例特征服务& 实例特征,
    L1实例特征批次数据操作& 批次数据操作) noexcept;

实际存在I64已有特征换代批次发布结果
发布实际存在I64已有特征换代批次(
    const 实际存在I64已有特征换代批次发布请求& 请求);
```

类只保存三个非拥有引用，无互斥量、缓存或账。只允许调用 P15探测、P38读取、P42规格和P39提交四个根。

## 4. 机械施工顺序

### S0：接口、0x0F域和所有权重绑定

1. 核对正式 HEAD、plan blob、可执行状态、唯一活动代码段和五文件白名单。
2. 逐字段读取 P15/P37/P38/P39/P40/P42 真实接口，确认摘要、首次读回、换代草案、映射和状态可无损消费。
3. 确认 P40 与本叶共同使用0x0F且操作标签分别0x00010F01/02；同一批次身份不能跨入口双义成功。
4. 确认顺序520未占用、三个共享文件和构建/自检/模块扫描资源释放、无等价发布者。

任一漂移停止并退回计划支撑。

### S1：DTO、双账和规格集合

1. 追加 DTO/类，不改 P40/P42 公开合同。
2. 按详细设计第5—6节实现纯值准入、0x0F键、意图摘要、P15探测和P38读取；P42必须在双未找到前调用0。
3. 实现全换代历史幂等互证；不得重跑P42或按当前快照替代首次读回。
4. 首次路径按容器顺序逐项调用P42；任一失败销毁规格并返回，P39调用0。
5. 一次校验同截止/宿主/登记、连续顺序和定义/槽/旧值/关系唯一。

### S2：写集、草案和提交

1. 本地键固定 `i+1`；形成N值、N槽、N退出，节点/关系0。
2. 精确形成新值/旧历史/属性槽/必要既有见证读回计划。
3. 形成全换代P39草案和执行证据摘要；四者一一覆盖。
4. 调用P39恰一次；按详细设计第9节映射首次、重复和各项非成功；`已退出` 不得合并为 `未找到`，发布前 `L1发布已存在=false`，发布后矛盾固定为 true 且保留非零代次。
5. 成功项目只来自本批次，保持0..N-1，不扫描宿主其它槽。

### S3：专项和记录

1. 在实例特征自检追加顺序520专项，完整覆盖详细设计第10节。
2. 保持P3/P4/P8/P10、P36/P40/P42及P38/P39回归。
3. 形成专属施工/验证记录，登记调用计数、标准向量、写集/读回/草案覆盖、并发和发布后失败边界。

## 5. 禁止实现

```text
禁止调用P10逐项换代、公开L1提交或事务后补4170账
禁止为换代另建批次身份、L1幂等域、第二4170账或摘要算法
禁止接受空组、初始项目或初始/换代混合批次
禁止排序、去重后静默丢项或返回宿主其它槽
禁止重算历史同义时的P42规格或用当前事实替代首次读回
禁止新建节点/关系、退出槽/关系或夹带批次外值
禁止发布后补写、删除、撤销或返回部分成功
禁止修改P40/P41、第三写门面、状态动态、装配或STEP-5
```

## 6. 激活门禁

保持 `待激活`，直至 P15 v0.6、P37、P38、P39、P40、P42 真实结果进入正式 main 并经交互接受；实际接口、0x0F域、摘要、首次读回、换代草案和N映射机械匹配；全部共享文件、顺序520、构建、自检和模块扫描资源释放；执行侧从新 HEAD 重做完整 S0，确认本计划为唯一可执行代码段。

## 7. 验证

至少执行：

```powershell
git diff --check -- <本计划白名单>
python .\tools\check_specs.py --strict
cmake --build --preset windows-msvc-debug --target 海中鱼巣 --clean-first
cmake --build --preset windows-msvc-release --target 海中鱼巣 --clean-first
& .\out\build\windows-msvc-debug\Debug\海中鱼巣.exe --self-test-exit
& .\out\build\windows-msvc-release\Release\海中鱼巣.exe --self-test-exit
pwsh -NoProfile -Command '$PSNativeCommandUseErrorActionPreference=$false; & ".\tools\clang_ast\扫描.MSVC模块依赖.ps1" -配置 Debug -平台 x64'
pwsh -NoProfile -Command '$PSNativeCommandUseErrorActionPreference=$false; & ".\tools\clang_ast\扫描.MSVC模块依赖.ps1" -配置 Release -平台 x64'
```

成功条件：P43专项完整通过；首次/幂等/冲突/并发/发布后失败边界成立；写集、读回计划、草案和记录一一覆盖；旧回归不退化；双配置构建、自检、strict、模块扫描和目标/cached diff check通过。

## 8. 停工与完成声明

若 P15首次读回缺旧历史、P39缺N映射、P42字段不足、P40的0x0F语义不兼容、需要扩大白名单或验证持续无法归因，停止并退回计划支撑。

完成后只可声明：P43白名单内源码候选能以一个L1 v2事务发布完整实际存在I64已有特征换代批次，并形成同代次4170记录和完整有序结果。

不得声明：世界树第三写、混合批次、状态/动态、生产装配、运行期接线、持久恢复、旧入口退出、集成验收、STEP-4、STEP-5或治理闭环完成。
