# L1-SIMPLIFY-METHOD-ROOT-R7-CURRENT-ROOT-ADMISSION
# 方法登记根当前唯一准入代码实施切片 v0.1

更新时间：2026-08-07

## 1. 目标与完成条件

唯一实施根：

```cpp
方法登记根当前唯一准入结果 L1方法登记根当前唯一准入服务::判定方法登记根当前唯一准入(
    const 方法登记根当前唯一准入请求& 请求) const;
```

恰一次消费R6按G10/格式/标签读取的全部首次结果组，恰一次消费P17完整快照，在同一非零截止
形成方法登记根0/1/N当前资格。完成只证明零写资格源码候选，不形成发布者。

## 2. 正式依据与当前事实

- 3300、5300：方法登记根和方法登记写入权只属于方法领域；
- 4015、4030：L1业务中性，当前事实、幂等账和领域解释分离；
- R1/R2：唯一根结构为3节点、1个I64角色值、1个槽，定位只能来自发布结果；
- R4/R5：G10标签`0x15001101U`，幂等域`0x19`；不同键不证明业务唯一；
- R6：按领域意图完整读取全部成功首次结果，0项合法；
- P17：一次try-lock完整当前快照与许可拒绝。

详细设计：
`规范/详细设计/20260807_L1-SIMPLIFY-METHOD-ROOT-R7-CURRENT-ROOT-ADMISSION_方法登记根当前唯一准入详细设计_v0.1.md`

单函数流程图：
`流程图/20260807_L1-SIMPLIFY-METHOD-ROOT-R7-CURRENT-ROOT-ADMISSION_判定方法登记根当前唯一准入函数流程图_v0.1.md`

## 3. 精确范围

允许八路径：

```text
海中鱼巣/领域/L1方法登记根当前唯一准入.数据.h
海中鱼巣/领域/服务.L1方法登记根当前唯一准入.ixx
海中鱼巣/领域/自检.L1方法登记根当前唯一准入.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
海中鱼巣/启动.应用程序.ixx
施工记录/20260807_L1-SIMPLIFY-METHOD-ROOT-R7-CURRENT-ROOT-ADMISSION_施工记录.md
验证记录/20260807_L1-SIMPLIFY-METHOD-ROOT-R7-CURRENT-ROOT-ADMISSION_验证记录.md
```

禁止修改R1—R6/P17/P15合同或代码、P17五文件WIP、P0-FIX、路线、旧WORLD-TREE、
`.codex-build`及其它路径。禁止增加第二exe、第二服务解释者或第二装配。

## 4. 机械实施合同

1. 新增唯一DTO头、准入服务和自检模块；服务仅保存一个const L1服务引用；
2. 入口互证本叶/R4/R2版本、56位身份、非零截止和R5已形成`0x19`键；
3. 固定调用R6一次，再调用P17一次；任一非成功立即映射，零写；
4. 三个截止必须相等；不等待、不sleep、不重试、不跨provider持锁；
5. 每个R6记录精确验证G10/标签、R2 `3/0/1/1/0/5`、映射1—4、五项读回和摘要；
6. 用映射形成R1强类型定位，再与P17快照四事实和槽逐字段互证；
7. 坏记录内部不一致，结果与快照矛盾具名分账；不得跳过或扫描裸值；
8. 0项返回可首次发布，1项返回完整唯一候选和同/异身份，2+返回多个候选且无唯一载荷；
9. 新增顺序589专项并在同一生产程序既有薄入口登记。

## 5. 并发与零写边界

R7不持有跨调用许可。后继只有使用R7返回的同一事实截止作为P15期望代次才可提交。两个不同
身份在截止C同时取得零候选时，P15独占提交最多一笔推进代次；另一笔漂移并必须从新截止重做
R7，随后看到唯一候选。不得把R7结果绕过P15代次门或缓存成长期发布授权。

R1—R5函数、P15探测/提交/事务/读回、执行证据、稳定编码分配、事实写入、审计、恢复、SQL、
索引和日志机器事实调用均为0。

## 6. 验证矩阵

专项`L1-SIMPLIFY-METHOD-ROOT-R7-S01`、顺序589至少覆盖：

1. 全部入口版本、身份、截止、R5状态/键/反解；非法时R6/P17调用0；
2. R6/P17状态、坏非成功、坏成功、调用次数和固定顺序；
3. 三截止一致及调用间事实推进漂移；
4. 0项、1项同身份、1项异身份、2/N项；
5. G10/标签、R2形状、映射、读回和摘要逐项损坏；
6. 快照四事实/槽缺失、退出、重复、错种类、错材料、错来源和半结构；
7. 多候选不选择、坏项不跳过、裸I64/名称/日志不参与；
8. 不同身份截止C并发合同：最多一笔后继提交可成功，其余漂移后重做；
9. R1—R5、P15探测/提交/事务/读回、稳定编码和全部写入0；
10. 同一生产程序Debug/Release构建、既有自检薄入口、模块扫描和正式日志人读核对；
11. 零第二exe/独立自检目标/第二装配/测试DTO/替身入口/调试出口；
12. 八路径、顺序589、专项身份唯一，diff check与strict110/110。

## 7. 激活门禁与完成声明

计划登记待激活。等待R1—R6及P17真实代码结果进入main、经交互接受且实际DTO/状态/签名
机械匹配；P17和活动代码段释放八路径、工程/filters、启动、构建、自检、模块扫描、固定输出
和顺序589资源；执行从新HEAD完整S0。上游组件通过不得升级为生产服务接线。

完成后最多声明：源码候选可在同一进程内零写形成方法登记根0/1/N当前资格，并为合法后继的
同截止提交阻止不同幂等身份同时创建第二根。

不得声明：P15探测/提交、原子发布、稳定编码分配、方法根发布/读回、方法登记/执行、生产接线、
服务候选、安全、因果、崩溃/跨进程恢复或治理闭环完成。
