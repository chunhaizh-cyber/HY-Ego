# REAL-GENERATION-LOOP-S8 后继筹办新代际可见与真实闭环验收切片 v0.1

日期：2026-07-19

执行队列：#322
设计队列：DQ-214
设计裁决：JY-498
计划阶段：Debug 最终回归 1170
状态：依赖门控待执行 / 前置 #321 独立集成并归档

## 1. 目标与依据

以显式真实生成闭环生产模式运行两轮：第一轮从固定根需求经真实线程执行、结算和学习形成新方法代际；第二轮后继任务筹办能够召回该新代际，同时第一轮选择/执行快照保持旧代际。完成独立覆盖审计前不切换默认入口。

依据为共同流程图、详细设计第 13、15、17 节和 #315-#321 正式产物。

## 2. 已验证事实、假定接口与预期产物

已验证当前事实：当前主线只有分散的同步能力、自检直调和线程骨架，没有两轮真实生产消费者闭环，也没有默认入口切换许可。假定接口：#315-#321 已分别导出根筹办报告、线程执行报告、治理/O1、O2、M0、M1和 M2 报告；生产治理宿主可通过同一租约连续处理两轮。前置计划：#321 必须已经独立集成并归档。

预期正式产物：显式 `--generation-loop` 生产模式、两轮同运行期闭环自检、阶段 1170 报告、独立集成记录和后继只读覆盖审计输入。执行前至少运行：

```powershell
rg -n "根需求筹办|生产任务执行|生产治理回执|用途统计|方法内容版本|方法学习候选|方法代际" 海中鱼巣
rg -n "受控消费一条任务执行请求|--generation-loop|登记自检.*1170" 海中鱼巣
git worktree list --porcelain
```

接口漂移处理：任一报告仍依赖自检局部仓库、主测试线程直接受控消费、手工调用线程正文或跨运行期夹具时，保持代码与 index clean，写专属复核断点并退回对应上游；不得在 #322 复制夹具或改默认入口。

## 3. 允许文件

```text
海中鱼巣/线程/宿主.生产治理闭环.ixx
海中鱼巣/线程/自检.真实生成闭环.ixx 新建
海中鱼巣/入口.cpp 仅新增显式 --generation-loop 模式、阶段1170最小登记和退出码
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
实施记录/20260719_REAL-GENERATION-LOOP-S8_后继筹办新代际可见与真实闭环验收_Codex断点清单.md 新建
```

## 4. 禁止文件

禁止修改 #315-#321 领域语义和机器结构、默认无参数入口、旧生产所有者、SQL/面板、恢复、外设、中央治理。禁止主测试线程直接调用任务受控消费、治理处理器或晋级写入口来伪造闭环。

## 5. 端到端状态机

S0 逐报告核对同一上下文身份；S1 启动显式生产治理宿主和三个真实线程；S2 第一轮根需求到旧方法执行/结算/O1/O2/M0/M1/M2；S3 第二轮创建后继待办并重新召回；S4 验证新代际仅第二轮可见、第一轮快照不变；S5 验证空队列、重复唤醒、过期、无授权、停止排空、并发和两轮幂等；S6 两配置、显式模式、完整自检、严格20/20和任务分支收口；独立集成后派发只读覆盖审计。

## 6. 非成功返回

根已满足、无新候选、未授权、后继无待办属于逻辑内结束。任一线程声称完成但权威读回缺失、跨运行期身份混用、新代际回写第一轮快照、第二轮召回看不到已发布活跃代际或停止遗失已接受请求必须追根因。

## 7. 验收矩阵

| 编号 | 验收 |
| --- | --- |
| A01 | 两轮全部材料属于同一生产运行期身份。 |
| A02 | 只消费正式安全根/服务根，不出现第三根。 |
| A03 | 管理、工作、自我治理三个后台线程自然消费。 |
| A04 | 第一轮任务、选择、执行、回执、结果、结算和 O1 完整。 |
| A05 | O2 可重建，M0 快照不可变，M1 不自动写事实。 |
| A06 | 独立授权后 M2 新代际完整发布。 |
| A07 | 第一轮仍引用旧代际，第二轮才可召回新代际。 |
| A08 | 未授权、过期、重复和无候选均不产生半结构。 |
| A09 | 停止顺序排空已接受请求，进程自然退出。 |
| A10 | `--generation-loop` Debug/Release 行为明确，默认入口零差异。 |
| A11 | 1170唯一，Release专项隔离。 |
| A12 | 两配置0/0、完整自检、严格Debug20/20、strict/diff/范围通过。 |
| A13 | 独立集成后任务代码逐blob一致，main非强制发布。 |
| A14 | 独立只读覆盖审计确认计划、队列、代码、记录、worktree和origin一致。 |

## 8. 验证

沿用两配置 Rebuild、`--self-test-exit`，并新增两配置 `--generation-loop`。连续20轮逐轮保存完整输出；共享事件段目录独占；最终进程数0。

## 9. 完成声明边界与后继

完成后只允许声明“固定生产根需求到新方法代际可见性的真实生成闭环第一轮形成”。默认入口切换、旧所有者退出、持久化恢复、状态生命周期、概念命名、控制面板和外设均另行重新设计；不得恢复旧 #303/#226-#239 直接执行。
