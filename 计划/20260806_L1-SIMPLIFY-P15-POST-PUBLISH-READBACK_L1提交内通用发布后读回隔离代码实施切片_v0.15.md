# L1-SIMPLIFY-P15 非 L1 provider 身份与证据计数代码实施切片 v0.15

计划身份：`L1-SIMPLIFY-P15-POST-PUBLISH-READBACK`

版本：`v0.15`

创建侧基线：`c7366e7b77842cd2a65af866509dfc3081ea269f`

具名漂移：`DESIGN-DRIFT-P15-V014-NON-L1-PROVIDER-STABLE-IDENTITIES-UNBOUND`

## 1. 目标与范围

替代并禁止施工 P15 v0.14 中 P-I/P-S 身份未绑定的部分；保留 L1 身份/合同2、模式、共享标签、预探测、一次提交、发布后读回和 32/18/50、16 文件调用映射。执行侧 P15 代码 WIP 所有权不转移，本计划只冻结可施工接口、材料编码、验证和恢复拒绝。

## 2. 允许文件与合同

基础白名单沿用 v0.14 的 20 文件，新增四个文件，白名单总数固定为 24。完整路径集合与详细设计 §5 相同；新增文件的职责为：`核心/L1可重建索引.数据.h`、`核心/合同.L1可重建索引.ixx`、`核心/索引.L1事实基座.ixx` 承载 P-I DTO/声明/实现，`领域/L1场景结构.数据.h` 承载 P-S DTO。`领域/服务.L1场景结构.ixx` 已在原 16 个调用文件内，仅扩展其身份读取入口。

冻结 `读取提供者身份() const noexcept` 的完整签名、身份常量、合同/规则版本、实例生命周期和只读边界。不得用索引键、节点/关系/值、场景标记、世界登记或调用序号替代身份，不得新增模块、工程、filters、启动文件或顺序登记。

## 3. 施工顺序

1. 从本计划正式 HEAD 重扫，确认 P-I/P-S 两个身份常量未与已发布身份冲突；若冲突退回支撑，不自行改值。
2. 先实现两个 DTO/只读入口及 `(1,1)/(2,1)` 校验，再接入 I02/F02/X02 的 provider 前探测后首次读取；L1 `(2,0)` 保持 v0.14 合同。
3. 按逐点矩阵保留 provider 实际调用次数，按稳定身份升序生成去重版本见证；I02=`2/2`、F02=`3/3`、X02=`2/2`，其余点和 32/18/50 不变。
4. 将身份、版本、调用次数、去重数、共同截止写入首次执行证据摘要、幂等账和成功审计；恢复从真实入口重算，缺字段/错版本/伪造来源/计数矛盾拒绝，双账或读回矛盾隔离。

## 4. 创建侧与执行侧验证

创建侧必须通过 `git diff --check`、`python .\tools\check_specs.py --strict`、UTF-8/NUL/尾随空白、P-I/P-S/L1 身份互异且非零、I02/F02/X02 计数、32/32+18/18→50/50、16 文件/50 物理调用、24 文件白名单和恢复拒绝矩阵。执行侧再验证真实 DTO/接口、Debug/Release、完整自检、provider 前探测、首次结果 DTO、双账恢复和发布后读回；计划不预先宣称通过。

## 5. 激活与边界

发布后执行侧必须从新 HEAD 保留现有 WIP 重做 S0，重核 P-I/P-S 真实接口和白名单。v0.15 最多声明设计合同闭合并可重新 S0；不声明代码实现、构建、自检、生产接线、恢复运行、P61/P62 或治理闭环完成。v0.14 在索引中标记被 v0.15 替代/禁止施工，v0.15 初始登记为待激活。
