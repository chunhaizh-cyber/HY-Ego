# OBSERVE-P1 像素簇、存在候选、识别对应与场景提交代码实施切片 v0.2

编号：OBSERVE-P1；路由：#369 / DQ-261；合同：PER-C9 / ABI 2；日期：2026-07-24

设计状态：终审 PASS。当前状态、版本和 plan blob 唯一读取 `计划/计划索引.md`。

## 合同与边界

- 依据：1140—1170、6300、6370、7100；`规范/详细设计/观察世界事实与体素融合详细设计.md` v0.2；`流程图/20260724_OBSERVE-VOXEL_观察世界事实与体素融合施工流程图_v0.2.md`；`规范/详细设计/函数结构知识图谱/20260724_PERCEPTION-D0_D455观察体素生产闭环函数结构知识图谱_v0.2.md`。
- 已实现事实：存在 / 场景 / 状态入口可只读复核但仍需 #373 节点直接适配；C7 / C8 是待实现合同。
- 待实现：`观察存在提交ABI版本 = 2`；像素簇、存在候选、识别对应、新存在需求、结构提交 / 读回；`形成观察存在候选`、`形成识别派生需求`、`接收识别结果`、`提交并读回观察存在`。
- 不建立观察事实节点；正式结果只写存在、场景成员关系和适用状态事实。空识别只形成复验 / 新存在承接需求，不由本计划直接创建存在。

## 文件所有权

允许：`海中鱼巣/领域/合同.观察存在提交.ixx`、`海中鱼巣/领域/组合.观察存在提交.ixx`、`海中鱼巣/领域/自检.观察存在提交.ixx`、`实施记录/20260724_OBSERVE-P1_PER-C9观察存在提交代码实施_Codex断点清单.md`。

禁止：C7 共享类型定义文件、C8 方法执行、7100 / 现行存在场景状态服务、C10—C13、工程、入口、运行器和设计文件。

## 施工与 S0

1. S0 核对 plan blob、C7 共享类型 import、C8 结果合同、7100 与存在 / 场景 / 状态只读适配端口和三文件所有权。
2. S1 建立候选、识别派生、新存在需求和结构提交 DTO；#369 只 import `识别派生需求材料` / `合法外设当前数据项` 并形成值。
3. S2 实现观察回合与后继识别回合的分段入口；不得同方法直调。
4. S3 完整前置后通过依赖端口提交并读回存在、场景成员关系和状态事实，再形成合法当前数据项投影；专项自检覆盖空句柄、冲突、复验、新存在等待、幂等、字段缺失和读回。
5. S4 核对白名单、import 方向、ABI 和 diff。
6. S5 只提交允许代码与专属记录，推送执行通道分支并恢复 clean。

## 机械验证命令

S0 记录 `$计划段起点 = (git rev-parse HEAD).Trim()`；S4 的 `git diff --check` 与 `git diff --cached --check` 均须退出 0。S5 后运行：

```powershell
$期望 = @('海中鱼巣/领域/合同.观察存在提交.ixx','海中鱼巣/领域/组合.观察存在提交.ixx','海中鱼巣/领域/自检.观察存在提交.ixx','实施记录/20260724_OBSERVE-P1_PER-C9观察存在提交代码实施_Codex断点清单.md') | Sort-Object
$实际 = @(git -c core.quotepath=false diff --name-only "$计划段起点..HEAD") | Sort-Object
if (Compare-Object $期望 $实际) { exit 1 }
if ((@(rg -n "观察存在提交ABI版本\s*=\s*2" '海中鱼巣/领域/合同.观察存在提交.ixx')).Count -ne 1) { exit 1 }
if ((@(rg -n "形成观察存在候选|形成识别派生需求|接收识别结果|提交并读回观察存在" '海中鱼巣/领域')).Count -lt 4) { exit 1 }
if ((@(rg -n "观察事实节点|export.*观察事实句柄" '海中鱼巣/领域/合同.观察存在提交.ixx' '海中鱼巣/领域/组合.观察存在提交.ixx')).Count -ne 0) { exit 1 }
```

全部命令须退出 0；ABI 精确 1 处、四入口至少 4 处、独立观察事实本体零命中。

## 验证与收口

- 静态 / 专项源码验证，不构建、不运行。
- C7 / C8 / 7100 端口或事实字段漂移返回 `DRIFT-PER-CONTRACT`；现行结构入口不能适配返回 `DRIFT-PER-CURRENT-INTERFACE`；需要表外文件返回 `DRIFT-PER-FILE-OWNERSHIP`；共享类型形成反向依赖返回 `DRIFT-PER-IMPORT-CYCLE`。
- 方法直调、提交后关系 / 事实不能完整读回为 `FAIL-PER-INTERNAL-READBACK`；其它验证失败为 `FAIL-PER-VALIDATION`。
- 完成只证明 PER-C9 候选，不证明存在创建、世界事实或识别闭环已接通。
