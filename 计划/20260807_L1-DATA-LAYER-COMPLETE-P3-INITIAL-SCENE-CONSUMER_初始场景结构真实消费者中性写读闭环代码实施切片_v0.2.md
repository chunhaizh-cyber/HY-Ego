# L1-DATA-LAYER-COMPLETE-P3-INITIAL-SCENE-CONSUMER 初始场景结构真实消费者中性写读闭环代码实施切片 v0.2

日期：2026-08-07

状态：待登记；登记后可执行

## 1. 目标、完成条件与依据

按配套详细设计机械实施：将现有 L2 初始场景创建根迁移到 P1 中性 CRUD，并接入正式生产启动；修正世界登记当前读回对后继事实代次的兼容。完成必须同时具备：一个中性写集恰一次提交、结构化成功/失败、正式精确权威读回、生产调用及候选完整性门。

依据：4015 v0.5、4030 v1.3、4200 v1.2、ACCEPTANCE-01 v0.3、P1 中性合同、P2 已发布世界登记合同，以及：

- `规范/详细设计/20260807_L1-DATA-LAYER-COMPLETE-P3-INITIAL-SCENE-CONSUMER_初始场景结构真实消费者中性写读闭环详细设计_v0.2.md`
- `流程图/20260807_L1-DATA-LAYER-COMPLETE-P3-INITIAL-SCENE-CONSUMER_创建初始场景函数流程图_v0.1.md`

## 2. S0 与激活门禁

登记为 `可执行`。发布前已确认 P2 代码和消费者验收均已普通推送、经交互接受且来源计划退出；P1 中性合同版本 1、九写状态、七读状态、七公开入口和字段机械匹配；本计划六代码文件无异主 WIP；唯一活动代码段、Git index、构建、运行和模块扫描资源可用。执行前仍须从登记后的正式新 HEAD 重做 S0，不得把日志、退出码或消息当作门禁完成。

## 3. 精确允许范围

只允许修改：

1. `海中鱼巣/领域/L1场景结构.数据.h`
2. `海中鱼巣/领域/服务.L1场景结构.ixx`
3. `海中鱼巣/领域/服务.世界登记.ixx`
4. `海中鱼巣/生产运行期配置.数据.h`
5. `海中鱼巣/启动.运行期上下文.ixx`
6. `海中鱼巣/启动.生产运行期.ixx`
7. `施工记录/20260807_L1-DATA-LAYER-COMPLETE-P3-INITIAL-SCENE-CONSUMER_施工记录.md`
8. `验证记录/20260807_L1-DATA-LAYER-COMPLETE-P3-INITIAL-SCENE-CONSUMER_构建与生产链验证.md`

禁止修改计划、索引、规范、流程图、工程/filters、L1 核心/仓库、存在/特征/状态/动态、旧系统角色实现、自检或集成验收目录。禁止触碰或夹带 P0-FIX、路线、旧 WORLD-TREE、`.codex-build` 和其它 WIP。

## 4. 实施步骤

1. 在场景数据头升合同 v3，新增创建/读取请求有效性和结果成功判断；把长期当前事实的创建代次约束改为非零且不晚于当前截止，枚举稳定值不变。
2. 在世界登记服务按详细设计第 4 节修正当前截止：首末当前代次一致，登记事实创建代次同一且不晚于当前，成功返回副本的 `已验证事实代次` 为本次截止；签名、合同版本、首次写集不变。
3. 重写 `创建初始场景`：删除 G2 编码、预探测、完整快照、legacy 提交和首次事实缓存；形成 1 节点+1值+1关系+1槽+0退出的中性写集，域前缀 0x02，恰一次提交中性写集并完整校验结果。
4. 把读实现改为世界登记提供者加中性精确读；首末代次夹取，读取 1 节点、1 属性、1 值、1 关系，验证同一创建代次和全部业务字段。缓存只保存唯一定位，新增无参数当前读入口。
5. 在生产配置增加固定初始场景幂等身份；在运行期上下文增加初始化/当前读/完整性转发，并把世界登记与初始场景同截止纳入 `完整()`。
6. 在生产启动结果增加末尾状态值 11 和初始场景业务状态；世界登记成功后构造完整请求并恰一次初始化初始场景，失败立即停止，成功后才进入旧系统角色。
7. 写两份专属记录，逐项记录实际变更、命令、结果和未证明范围。不得写 L1 PASS、日志 PASS 或整个数据层完善。

任一步出现正式接口、字段、所有权或启动顺序漂移且会改变上述机器语义时，保留现场并具名退回计划支撑；普通编译错误在白名单内修复。

## 5. 验证合同

至少执行并记录：

```powershell
git diff --check -- <八个白名单路径>
python .\tools\check_specs.py --strict
MSBuild.exe .\海中鱼巣.slnx /t:Rebuild /p:Configuration=Debug /p:Platform=x64
MSBuild.exe .\海中鱼巣.slnx /t:Rebuild /p:Configuration=Release /p:Platform=x64
<Debug生产程序> --runtime-context
<Release生产程序> --runtime-context
python .\tools\check_msvc_module_dependencies.py --configuration Debug
python .\tools\check_msvc_module_dependencies.py --configuration Release
```

实际工具参数以仓库正式脚本帮助为准，不得为了命令字面一致绕过正式入口。成功条件：两配置重建和生产入口退出 0；strict 110/110；两配置模块扫描全数通过；精确白名单无额外 staged 路径。

静态/源码门禁：在 `创建初始场景` 与其共享的初始场景精确读回路径内，`探测幂等`、完整快照、legacy `提交写集`、`L1领域意图凭证`、`0x15000201U`、G2 摘要为 0；`提交中性写集` 物理调用点恰 1；生产初始场景初始化调用点恰 1 且早于系统角色；读回中两次事实代次及 1 节点、1 属性、1 值、1 关系入口齐全。

`读取初始场景接纳` 不属于本叶生产调用链。它为 P7 既有实际存在消费者保留同一完整快照中的单归属基数校验，在其自身中性迁移前允许且只允许该函数保留一个 legacy `读取完整快照` 调用；本叶不得删去基数校验或把单条关系精确读取当成唯一归属证明。构建、退出码和静态扫描只证明施工门禁，不是消费者合同验收。

代码发布后另由集成验收智能体绑定精确提交，经生产正式入口、结构化启动结果、租约公开初始场景读回和世界登记同截止结果验收；不得直调 L1、建立专用驱动或用日志代替结果。

## 6. 完成声明边界

计划施工完成最多证明：初始场景消费者的中性代码候选、生产接线和施工门禁成立。独立消费者验收接受后，才可累计一节点、一值、一槽、一关系的新增/查询/原子性实际覆盖。

不得宣称幂等、非法请求零变化、修改、退出/历史、存在、特征、状态、动态、全部消费者、旧栈删除或整个 L1 完善；不得考虑崩溃或跨进程恢复。

## 7. v0.2 修订说明

v0.2 只收窄 v0.1 的静态检查范围，不改变代码白名单和初始场景机器合同。执行期确认后续实际存在接纳读取仍有独立的单归属完整快照责任；该 legacy 调用留待它自己的消费者迁移计划退出。
