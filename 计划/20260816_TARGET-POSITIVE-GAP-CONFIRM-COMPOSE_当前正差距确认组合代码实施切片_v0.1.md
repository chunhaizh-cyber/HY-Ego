# TARGET-POSITIVE-GAP-CONFIRM-COMPOSE 当前正差距确认组合代码实施切片 v0.1

日期：2026-08-16
版本：v0.1
身份：`TARGET-POSITIVE-GAP-CONFIRM-COMPOSE`
状态：待激活

## 1. 目标与边界

按 BIZ-L2-004-01 v0.2 和同身份详细设计，新建 `需求目标裁决服务`，在一个非零 G0 下恰一次组合 `读取需求目标当前事实`与 `按注册比较合同裁决目标差距`，形成完整 `需求正差距结果`。

不实现需求 CRUD、来源授权、父路径、任务树、普通应用持有 / getter / 装配或 T02 接线；不修改 DATA、需求业务服务或两个子服务。

## 2. 执行前事实与允许文件

执行前必须确认本计划可执行；`TARGET-CURRENT-FACTS-COMPOSE v0.1`和 `TARGET-GAP-REGISTERED-COMPARE-CONSUME v0.1`结果 / 退出 / 清理均在祖先链，最终 DTO / ABI与详细设计机械一致，生产文件、工程、唯一代码段和构建资源已释放。

生产 / 工程：

1. `海中鱼巣/领域/需求正差距.数据.h`；
2. `海中鱼巣/领域/服务.需求目标裁决.ixx`；
3. `海中鱼巣.vcxproj`；
4. `海中鱼巣.vcxproj.filters`。

永久记录：

5. `施工记录/20260816_TARGET-POSITIVE-GAP-CONFIRM-COMPOSE_当前正差距确认组合施工记录_v0.1.md`；
6. `验证记录/20260816_TARGET-POSITIVE-GAP-CONFIRM-COMPOSE_当前正差距确认组合验证记录_v0.1.md`。

临时专项：

7. `验证工具/当前正差距确认组合验证.cpp`；
8. `验证工具/当前正差距确认组合验证.vcxproj`；
9. `验证工具/运行当前正差距确认组合验证.ps1`。

禁止修改 `服务.需求.ixx`、父 / 比较 provider文件、普通应用、全部 DATA / L1、线程、规范、流程图、设计、计划和计划索引。

## 3. 实施步骤

1. 新建传统 DTO 头，按 CODE-FILE-01 以自有 `NO_INCLUDES`宏支持普通包含和模块 export 包装；实现22个稳定状态、请求 helper和业务裁决成功谓词。
2. 新建 `海中鱼巣.领域.服务.需求目标裁决`模块；构造期从同一 `const L2结构聚合服务&`按固定顺序形成一个目标事实 provider和一个判断收束服务，不持有需求 owner或第二聚合。
3. 校验父请求后构造当前事实请求并恰一次调用 provider；按详细设计映射所有状态，失败零证据。
4. 第一叶成功后以完整结果和原比较请求身份构造比较请求，恰一次调用判断服务；比较协议内非成功保留两份完整结构化证据，第二叶入口拒绝或坏形状收敛内部不一致并清空证据。
5. 四类业务裁决逐字段互证后一次形成候选；保持零 DATA / L1 直达、零写、零锁 / 等待、零重试、零缓存、零截止重选。
6. 唯一登记两个生产文件，完成根工程与专项验证、永久记录；结果退出后独立删除临时专项并清理复验。

## 4. 验证与激活

- 根工程 x64 Debug / Release Rebuild及专项双配置通过；
- 父版本 / G0 / 六身份逐项拒绝在首个子调用前；
- 第一叶所有公开状态、第二叶所有公开状态及四类业务裁决精确映射；
- 成功时两个子结果、G0、合同、定义、注册、当前值和完整比较证据逐字段相等；
- 第一叶失败零证据，第二叶协议非成功保留两份完整证据，坏互证零证据；
- 源码恰两个 BIZ 子调用，零 DATA服务函数名 / L1 / owner / 需求服务 / 写入 / sleep / retry / cache；
- 工程唯一登记、生产零专项痕迹、diff、strict、UTF-8与精确白名单通过。

本计划待激活不授权施工。若子 DTO / ABI、构造顺序、状态、模块依赖或工程所有权漂移，固定 `DRIFT-TARGET-POSITIVE-GAP-CONFIRM-ABI-OR-OWNERSHIP`退回计划支撑。代码结果最多证明本父组合叶；需求建立、任务树、T02和系统闭环继续 `NOT_RUN`。
