# DEMAND-SOURCE-PATH-MERGE-CANDIDATE-COMPOSE 需求来源路径合并候选组合代码实施切片 v0.1

日期：2026-08-16
版本：v0.1
身份：`DEMAND-SOURCE-PATH-MERGE-CANDIDATE-COMPOSE`
状态：待激活

## 1. 目标与完成条件

按 BIZ-L4-004-02-01-03 v0.2 和详细设计实现纯值 `需求业务服务::合并来源与当前路径材料`：消费02完整当前需求和01已准入来源授权见证，形成无需补充、仅来源补充或必须经04复核的新增路径候选。零DATA调用、零事实写入。

## 2. 依据与执行前事实

- 0050、3100、4030、5100、5110、5130、CODE-FILE-01；
- BIZ-L3-004-02-01 v0.2、BIZ-L4-004-02-01-03 v0.2；
- 同名详细设计 v0.1；
- 02 v0.2已登记待激活，01来源授权DTO / provider尚未冻结；本计划不得越门施工。

S0须确认01和02真实结果 / 退出 / 清理，两个上游DTO与G0合同机械匹配，`服务.需求.ixx`和工程释放，索引已把本计划提升为可执行。

## 3. 允许与禁止文件

允许：`海中鱼巣/领域/需求来源路径合并.数据.h`、`海中鱼巣/领域/服务.需求.ixx`、根工程 / filters、专属施工 / 验证记录和三件生产外临时专项。

禁止：DATA / L1 / owner、最终需求清单、普通应用、01 / 02 DTO实现、04—06代码、线程、正式规范、流程图、设计、计划和索引。禁止新增DATA服务、事务参与者、幂等账读取或私有写集。

## 4. 实施步骤

1. 新建传统DTO头，冻结请求、8状态、结果和只含值式意图的补充候选；复用01 / 02最终类型，不复制同义结构。
2. 串行扩展唯一需求服务，新增 const noexcept 纯值函数；不增加服务成员或构造依赖。
3. 校验02唯一结果、01准入见证、G0 / 主体 / 目标 / 版本和非零后继幂等身份。
4. 按来源稳定身份比较完整来源 / 授权；形成空、来源追加或授权换代意图，不覆盖历史；02组自身重复 / 异义为内部不一致，新见证与既有不可变来源冲突为来源冲突。
5. 父非空时按父子端点 + 路径组 + 角色 + 顺序比较完整当前路径组；零命中形成必须经04的路径意图，一命中无需补充，多命中内部不一致。
6. 父空则路径组和意图路径字段都空，并直接无需路径补充；父非空则路径组至少一项且意图完整。候选只保存值式输入、补充意图、原幂等身份、G0和版本。
7. 完成专项、根工程双配置、永久记录；结果 / 退出后独立删除临时专项并清理复验。

## 5. 验证与成功标准

- 无需补充、仅来源补充、需要路径复核三态及全部非成功精确覆盖；
- 根需求全空路径不形成新增路径意图，不调用04，最终状态只由来源补充决定；
- 新来源 / 授权换代不改目标、既有来源或历史；新路径不使旧路径退出；
- 新路径必标记04复核，父图不得从03直接绕过04进入05；
- 后继幂等身份原样保留，03不访问幂等账，不宣称DATA精确重复；
- 源码零DATA / L1 / owner / 写入 / 锁 / 时间 / 缓存 / retry；
- 只有三类完整结果回显G0；失败截止0、optional空；
- Debug / Release、生产外专项、diff、strict、UTF-8、白名单和工程唯一登记通过。

## 6. 激活与边界

本计划必须等待01来源授权见证和02 v0.2最终代码结果，机械匹配后才能激活。若上游改变来源稳定身份、授权换代、路径组基数 / 语义键、G0或optional形状，固定具名 `DRIFT-DEMAND-SOURCE-PATH-MERGE-CANDIDATE-BOUNDARY`。

代码完成最多证明纯值候选形成；01准入、04防环、05发布、06读回、需求整体建立、任务树和系统闭环均为 `NOT_RUN`。
