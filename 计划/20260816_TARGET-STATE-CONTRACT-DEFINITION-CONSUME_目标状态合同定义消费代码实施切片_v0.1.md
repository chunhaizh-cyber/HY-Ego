# 目标状态合同定义消费代码实施切片

日期：2026-08-16
版本：v0.1
身份：`TARGET-STATE-CONTRACT-DEFINITION-CONSUME`
状态：待激活

## 1. 目标与完成条件

按 BIZ-L4-004-01-01-01 v0.2 和同身份详细设计，实现并接入 `需求目标事实提供者::读取目标状态合同与值域定义(...) const noexcept`。完成条件是普通应用合法消费者可在同一非零 `G0` 下通过真实 DATA 状态、特征公开 ABI 得到逐字段互证的完整 I64 目标合同定义材料，结果回显合同版本、规则版本和 `G0`，且整个调用零事实写入。

本叶不实现宿主当前事实读取、目标差距比较、目标等价、需求 / 任务 CRUD 或父级组合函数。

## 2. 正式依据与执行前事实

- 规范：0050、3100、4030、4180、4230、5100；
- 目标图：`流程图/目标业务流程图/L4/BIZ-L4-004-01-01-01_读取目标状态合同与值域定义_目标函数流程图_v0.2.md`；
- 详细设计：`规范/详细设计/20260816_TARGET-STATE-CONTRACT-DEFINITION-CONSUME_目标状态合同定义消费详细设计_v0.1.md`；
- DATA 统一特征定义结果 / 退出：`d0949ebcde1e4ddc243c4668fe380ea742067f21` / `31bcd894`；
- DATA I64 比较结果 / 退出：`0a0d75e15b1f1cf1e68d6bc1741bea36153162c0` / `9ff8ffb3b542d8829ea0db36b606cc354d7a9217`；
- DATA 目标状态合同结果 / 退出：`b5d8af3216df2459148a29e551bdf02c12c1d004` / `4a6c77adc2f419067ff4f28848e74d826eb95b5d`。

执行前重新 S0：确认计划索引精确版本和状态、上述提交在当前祖先链、三个 DATA 读取函数签名与 DTO 未漂移、两个新文件仍不存在、四个生产文件无重叠所有者、概念本体根 C1 结果与退出仍在祖先链、当前 DATA 代码段及普通应用 / 根工程 / 构建资源已释放、Git index 空。当前只匹配 I64 原始类型与“标量有序比较与安全差异”算法族；不得把现有枚举中的向量、角度、空间、集合或时间序列项解释为已闭合能力。

## 3. 允许文件

生产：

1. `海中鱼巣/领域/需求目标事实.数据.h`；
2. `海中鱼巣/领域/提供者.需求目标事实.ixx`；
3. `海中鱼巣.vcxproj`；
4. `海中鱼巣.vcxproj.filters`。

永久记录：

5. `施工记录/20260816_TARGET-STATE-CONTRACT-DEFINITION-CONSUME_目标状态合同定义消费施工记录_v0.1.md`；
6. `验证记录/20260816_TARGET-STATE-CONTRACT-DEFINITION-CONSUME_目标状态合同定义消费验证记录_v0.1.md`。

生产外临时专项：

7. `验证工具/目标状态合同定义消费验证.cpp`；
8. `验证工具/目标状态合同定义消费验证.vcxproj`；
9. `验证工具/运行目标状态合同定义消费验证.ps1`。

禁止修改 L1、全部 DATA 生产文件、`L2结构聚合服务`、`海中鱼巣/装配.普通应用.ixx`、需求 / 任务 owner、线程、正式规范、流程图、详细设计、计划和计划索引。

## 4. 实施步骤

1. 新增 BIZ DTO、纯值请求 / 材料 / 结果完整性 helper 和稳定状态数值；请求包含独立合同版本、规则版本、非零 `G0`、目标合同身份和目标特征定义身份；结果回显合同版本、规则版本和最终 `G0`，只有已读取带 optional。
2. 新增只读 provider，构造时持 `const L2结构聚合服务&`，函数内按固定顺序执行三次 BIZ 显式同 `G0` 正式读取；不得对 DATA 内部读取次数作假设。
3. 每个成功结果先互证 `已读取`、`成功()`、截止为 `G0`、空变更代次、当前读取、历史截止为0和当前生命周期，再逐字段互证目标合同、特征定义、目标判断比较注册的身份链、用途、I64 原始类型、标量算法、左右角色、右减左、单位、维度、分量角色、目标允许关系位1—7、注册允许结果位包含具名关系、算法版本、边界、量化和误差合同；完整候选形成后一次值式发布。
4. 第一笔目标合同未找到 / 已退出分别映射具名目标材料状态；第一笔成功后第二 / 三笔缺失或退出映射内部不一致；请求目标特征与合同引用不符映射目标上下文引用冲突。捕获 `std::bad_alloc`、`std::length_error` 为资源失败，其它异常为内部不一致；零内部重试、零部分材料、零 DATA 写、零 L1 直达。
5. 登记新头和 provider 模块到工程 / filters；合法消费者通过现有 `普通应用上下文::取得L2结构聚合服务() const`在调用作用域构造 provider。普通应用持有、专用 getter和装配状态全部保持后继 `NOT_RUN`。
6. 通过临时专项用正式公开 DATA 写入建立合法材料，再从普通应用现有 const 聚合 getter构造 provider 并调用本函数；动态覆盖版本 / G0 / 身份入口拒绝、首笔未找到与已退出、目标引用冲突和代次漂移。状态服务在第一笔成功前已于同一G0内部互证定义与注册，公开 ABI 无法构造“第一笔成功、后继缺失 / 退出”，该内部不一致防御分支只做静态检查并记 `NOT_RUN`，不得增加生产 seam。
7. 填写永久记录；结果提交后由索引所有者登记退出，再独立删除三件临时专项并重做根工程 / strict 清理后验证。

## 5. 验证与成功标准

- 根工程 x64 Debug / Release Rebuild PASS；
- 临时专项 Debug / Release 的具名用例全部 PASS，失败为 0，真实未发生分支保持 `NOT_RUN`；
- 合法调用回显的合同版本、规则版本、三份事实和 `G0` 与三个 DATA 正式读回逐字段一致；
- 错请求零 DATA 调用；首笔未找到 / 已退出动态分账准确；公开 ABI 不可构造的后两笔缺失 / 退出防御分支静态存在并记 `NOT_RUN`；引用冲突、漂移和全部非成功均零部分载荷；调用前后共享事实代次不变；
- 完整特征定义复制导致的内存 / 长度异常不得越过 `noexcept`，其它异常也必须结构化收口；
- provider 只经同一 `L2结构聚合服务`消费状态、特征服务，源码无 L1 / owner / 仓库 / DATA 写 / 缓存 / sleep / 日志成功裁决；普通应用文件零差异；
- 工程登记唯一，生产零专项引用 / seam；
- `git diff --check`、`python .\tools\check_specs.py --strict`、UTF-8 无 BOM和精确白名单通过。

## 6. 激活、发布与边界

概念本体根 C1 已以结果提交 `73ef1172ae468246c1ec71613f60e42a832c4d8d` 发布并由 `16b3e8a7426ab68fcbd846612436d3731450c5c7` 登记退出；本计划不再等待 C1。当前仅因 DATA 执行角色正在实施索引中可执行的 `DATA-L3-CONCEPT-SPACE-AGGREGATE-ASSEMBLY v0.1`，占用唯一代码段、普通应用 / 根工程和构建资源而保持待激活；该活动段完成或安全释放后，计划索引所有者只在最终三项 DATA ABI、现有普通应用 const 聚合 getter、四个生产文件所有权和工程资源机械匹配时提升为可执行。任何真实 ABI、状态形状、getter 或文件所有权漂移必须具名退回计划支撑。

代码结果和构建通过只证明本叶。宿主当前事实、差距判断、多个需求目标等价、需求 / 任务 CRUD、治理循环和系统闭环保持 `NOT_RUN`。
