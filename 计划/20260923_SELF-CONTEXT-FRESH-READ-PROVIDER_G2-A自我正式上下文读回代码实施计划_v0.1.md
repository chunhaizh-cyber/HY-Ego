# SELF-CONTEXT-FRESH-READ-PROVIDER G2-A自我正式上下文读回代码实施计划

计划身份：`SELF-CONTEXT-FRESH-READ-PROVIDER`

版本：v0.1

日期：2026-09-23

状态：可执行候选；以`计划/计划索引.md`登记为准

代码起点：`f11d94000322e8fa24f89d0c63384c7ff35525f7`

## 1. 目标与完成条件

按G2-A详细设计完成纯读双根材料入口、正式上下文上层provider及普通应用生产所有权，使世界根、唯一self/当前所在场景、双根闭包和唯一方法登记根可在同一fresh截止被完整核验。

完成必须同时满足：纯读缺根零写；provider返回现行值式投影；预算DTO完成分项迁移；普通上下文持有provider；治理门仍关闭；专项和根工程双配置通过；只发布允许文件及专属记录。

## 2. 正式依据与当前事实

- 正式依据：8100 v1.13、8120 v0.17、8121 v0.6、8200 v2.0、7130 v0.19及`20260923_SELF-CONTEXT-FRESH-READ-PROVIDER_G2-A自我正式上下文fresh读回详细设计_v0.1.md`。双根D的精确独立物理边界和读回失败以8121为直接依据，不以5170目录摘要版本裁决本叶ABI。
- 起点`f11d94000`已经发布阶段21生产消费和阶段20停门；旧计划完成证据由同提交施工/验证记录、self专项29 PASS、生产消费专项33 PASS及根工程双配置0错误0警告构成。
- 当前上下文端口只有接口，无provider；现有根入口可能写；当前门开放入口仍固定依赖未就绪。
- 当前三份流程图dirty属于异主WIP，禁止修改、暂存或带入验证结论。

## 3. 唯一所有权与文件范围

本计划是一个垂直代码切片，唯一所有者为选择本计划的执行智能体。

允许修改：

- `海中鱼巣/线程/线程_自我.h`
- `海中鱼巣/线程/线程_自我.cpp`
- `海中鱼巣/领域/数据服务.需求类.h`
- `海中鱼巣/装配.普通应用.cpp`
- `海中鱼巣.vcxproj`
- `海中鱼巣.vcxproj.filters`
- 新建`海中鱼巣/业务/应用服务.自我线程正式上下文.h/.cpp`
- 新建`验证工具/自我线程正式上下文提供者专项验证.cpp/.vcxproj`
- 新建本计划专属施工记录和验证记录。

禁止修改：

- `海中鱼巣/线程/线程_自我.h/.cpp`中除上下文预算DTO完整性外的生命周期、门、邮箱和停止逻辑；
- `海中鱼巣/启动.应用程序.cpp`、`海中鱼巣/装配.普通应用.h`、阶段21 provider、世界树/自我形成/方法结构现有公开ABI；
- 正式规范、目标账、计划索引、流程图、共享知识文件及其它计划/异主WIP；
- G3、G4-A、任务管理、安全/服务治理和控制面板窗口。

## 4. 精确施工合同

### P1：纯读根材料

在需求服务新增`const`纯读入口，复用既有请求/结果。它只读取固定首次材料及当前闭包，成功为`已读取`；缺失零写并返回`根材料未闭合`。不得调用写入口或复制第二套根读回算法；必要时把现有私有完整读回拆成共享只读辅助，但不得改变写入口现有成功/恢复语义。成功结果及材料读取代次必须等于请求截止；provider末次复用现有公开`核验当前事实代次(std::uint64_t) const noexcept`，禁止新增事实代次getter。

### P2：预算DTO

将上下文预算三字段替换为详细设计七字段并同步`完整()`。合同版本和所有枚举值不变。全仓扫描迁移所有旧字段消费者；当前基线预期仅声明/完整性实现，无实际构造调用，若出现新消费者则必须在允许范围内同步，否则具名DRIFT退回计划支撑。

### P3：上层provider

按详细设计精确类签名、七步算法和第5节完整状态映射矩阵实现。所有公开调用`noexcept`；不得合并、猜测或补造矩阵之外的状态语义。非成功空投影且`写业务事实=false`。不得向self头暴露底层服务类型以外的新依赖。

### P4：生产所有权

普通上下文按声明/析构顺序持有provider并在self服务、需求服务及方法结构服务就绪后构造。provider不注入self，不新增getter/包装入口，不改变启动、阶段和宿主。

### P5：工程与验证

根工程唯一登记新生产头源；专项不登记根工程。专项以真实服务栈验证首次、恢复、零写、同截止、移动场景及失败分支。验证临时根固定为`D:\TEMP\海中鱼巣\SELF-CONTEXT-FRESH-READ-PROVIDER\<本轮唯一标识>`。

## 5. 失败收口与DRIFT

- 纯读实现若必须修改根持久语义、状态枚举或调用写入口才能完成，停止并退回计划支撑。
- 如果现行真实自我读取不能在不新增机器事实的情况下证明当前场景/角色闭包，停止provider消费部分，不改规范。
- 如果方法根被要求进入公开投影，或当前特征值被要求提前进入G2-A，视为目标扩张，停止受影响部分。
- 普通工程错误、专项夹具错误和计划范围内状态映射错误由执行者修复，不据此改设计。

## 6. 验证命令与成功条件

1. `rg`确认旧预算三字段、根读入口调用写入口、provider到self注入及门开放新增代码均为零。
2. 专项Debug/Release x64隔离`Rebuild`并运行，全部断言通过；真实持久根seed/recover用独立子进程和受控临时根。
3. 根工程Debug/Release x64隔离`Rebuild`，0错误；警告必须逐项归因，不把既有噪声冒充新增通过。
4. 运行`git diff --check`、`git diff --cached --check`和精确允许/禁止范围审计。

成功证据写入：

- `施工记录/20260923_SELF-CONTEXT-FRESH-READ-PROVIDER_G2-A自我正式上下文读回施工记录_v0.1.md`
- `验证记录/20260923_SELF-CONTEXT-FRESH-READ-PROVIDER_G2-A自我正式上下文读回验证记录_v0.1.md`

## 7. 完成声明边界

本计划完成只证明G2-A只读provider及生产所有权已实现。它不证明provider已被self调用、治理门开放、G2整体完成、G3消息循环、G4-A根需求复核、当前特征值比较、任务线程组、跨进程业务恢复或自我治理闭环完成。
