# L2-EXISTENCE-REGISTRY-CURRENT-READ-CONSISTENT-PROJECTION-MIGRATION 实际存在登记当前读回一致投影迁移代码实施切片 v0.1

日期：2026-08-08

状态：待登记；三文档正式发布并取得本文件精确 blob 后，由计划智能体登记为唯一 `可执行`。

## 1. 身份、目标与完成条件

计划身份：`L2-EXISTENCE-REGISTRY-CURRENT-READ-CONSISTENT-PROJECTION-MIGRATION`

目标：把 `L1实际存在服务::按登记读取权威事实` 迁移到现有中性非阻塞一致当前投影，在不改变实际存在公开 DTO、状态、登记写入、定位缓存和生产调用者的前提下，使实际存在登记这一合法 L2 生产消费者为完成自身权威当前登记读回而真实消费新投影。

完成条件：一个生产服务文件与两份专属记录精确完成；目标根固定请求两个节点且其它五组为空；一致投影只有一个物理调用点和最多两个完整逻辑轮次；首轮代次漂移按回显实际代次完整重试一次，第二轮漂移返回版本漂移；许可拒绝零等待零重试；服务身份、资格属性类型、创建代次和当前截止完整验证；旧分散事实代次 / 节点读取在目标根归零；双配置完整重建、双配置模块扫描、strict、静态门禁和正式 Debug / Release `--runtime-context` 运行通过。

直接依据：

- `规范/详细设计/20260808_L2-EXISTENCE-REGISTRY-CURRENT-READ-CONSISTENT-PROJECTION-MIGRATION_实际存在登记当前读回一致投影迁移详细设计_v0.1.md`
- `流程图/20260808_L2-EXISTENCE-REGISTRY-CURRENT-READ-CONSISTENT-PROJECTION-MIGRATION_按登记读取权威事实函数流程图_v0.1.md`
- 0050、4015 v0.6、4030 v1.4、4050 v1.2、4200、ACCEPTANCE-01 v0.4、0600
- 当前已发布 `L1中性一致当前读取请求/结果`、`尝试读取中性一致当前投影`，以及世界登记和初始场景的一致当前读回结果

计划可执行只表示设计和当前接口足以机械施工，不证明代码已实施、构建、运行、结构化消费者成功或 L1 已完善。

## 2. 执行前 S0 与停止条件

执行智能体从登记后的正式新 HEAD 重做 S0：

1. 核对本计划 v0.1 是计划索引中唯一 `可执行` 行，核对精确 plan blob、设计提交和索引提交；不得从聊天或本文件“待登记”文字开工。
2. 核对创建侧起点 `4ba783408607c2e73b468d2c00236547610365cc` 之后正式增量只包含本三文档与索引登记；若 HEAD 继续前进，按新 HEAD 重新确认目标 blob、前置结果和生产调用边。
3. 核对一致当前请求仍含六组选择、非零期望代次；成功与请求同序等长；顶层非成功六组空；漂移回显非零实际代次。
4. 核对实际存在合同版本 2、规则版本 1、十个状态数值、登记完整性、目标函数签名、固定两节点写集和生产调用边未漂移；目标生产文件无异主 WIP。
5. 核对定位截止仍可落后于当前事实代次，目标根没有组合其它领域的同截止输入；只有在此前提下允许首轮漂移后完整重试一次。
6. 核对 Git index 为空，唯一活动代码段、发布租约、构建、模块扫描和生产运行资源可用。P0-FIX、路线、异主 `.codex-build`、两份 WORLD-TREE 和其它 WIP 均不得触碰、暂存或带入验证输出。

若一致投影 DTO / 状态、实际存在公开合同、目标函数、定位截止语义或生产调用边出现会改变实施的漂移，或实现必须触碰第 3 节之外路径，停止受影响切片并具名退回计划支撑。普通 include 清理、编译错误和白名单内机械实现问题由执行智能体自行修复。

## 3. 精确允许与禁止范围

只允许修改或新增：

1. `海中鱼巣/领域/服务.L1实际存在.ixx`
2. `施工记录/20260808_L2-EXISTENCE-REGISTRY-CURRENT-READ-CONSISTENT-PROJECTION-MIGRATION_施工记录.md`
3. `验证记录/20260808_L2-EXISTENCE-REGISTRY-CURRENT-READ-CONSISTENT-PROJECTION-MIGRATION_构建与生产消费验证.md`

禁止修改实际存在数据头、核心 L1、世界登记、场景结构、初始场景实际存在原子发布、组合服务、启动、运行期上下文、配置、工程、filters、自检、集成验收、规范、详细设计、流程图、计划、计划索引和异主 WIP。

严格排除 `建立登记` 的写集 / 幂等 / 定位缓存、`读取资格实现`、`形成实际存在写入规格` 和同文件其它读写根。目标根外现存事实代次和单项当前读取必须原样保留；不得按文件级归零删除其它业务合同。禁止为验证新增 L1 自检、专项驱动、第二 exe、生产 PASS、日志解析或私有探针。

## 4. 目标函数、请求和所有权

- 唯一目标根：`L1实际存在服务::按登记读取权威事实(const 实际存在结构登记&, std::optional<std::uint64_t>) const`。
- 公开建立 / 读取函数、DTO、状态数值、服务构造、互斥锁和定位缓存保持不变。
- 定位提供首次幂等身份、服务身份、资格属性类型和起始事实截止；可选 `期望创建代次` 只约束两个登记节点的共同创建代次。
- L1 唯一拥有共享 try-lock、事实代次和中性当前投影；实际存在服务唯一解释两个节点的业务种类和返回登记。
- 在现有内部命名空间新增目标专用顶层 / 项目状态映射和失败形状校验，全部中文命名、最小可见性；不得改变供资格根使用的既有 `映射读取状态`。

固定请求六组：节点 `{服务身份, 实际存在资格属性类型}`；关系、值、属性值、源关系组和目标关系组为空；首轮期望代次为定位截止。

## 5. 实施步骤

1. 保持定位完整性校验；失败零一致投影调用。
2. 新增目标专用一致读取顶层 / 项目状态映射和顶层失败形状校验，不改现有旧读取状态 helper。
3. 以定位截止形成详细设计第 4 节固定 `2/0/0/0/0/0` 请求。
4. 保持 `尝试读取中性一致当前投影` 一个物理调用点。首轮合法代次漂移改用回显实际代次完整重试一次；第二轮漂移返回版本漂移；许可拒绝立即返回，不等待、不重试。
5. 成功要求合同、期望回显、读取代次和六组长度精确；读取代次必须等于本轮期望代次。
6. 项目未找到映射未找到；已退出、种类不匹配、属性未设置、成功无事实 / 非成功带事实和坏失败形状映射内部不一致；内部错误优先于未找到。
7. 验证服务身份普通节点、无类型表示、无当前槽、未退出；资格属性类型为属性类型节点、I64 表示、无当前槽、未退出。
8. 要求两个节点创建事实代次共同、非零且不晚于读取代次；可选期望创建代次有值时必须等于共同创建代次。
9. 复制定位，把返回副本事实截止更新为读取代次，返回已读取；非成功返回空登记且不修改定位缓存。
10. 删除目标根对 `读取中性当前事实代次` 和 `读取中性当前节点` 的全部调用；其它函数零行为差异。
11. 写两份专属记录，如实记录静态范围、正式生产运行、结构化消费者未独立验收范围和 L1 不自证边界。

## 6. 固定结果与失败收口

| 场景 | 固定结果 |
| --- | --- |
| 定位不完整 | 内部不一致；零一致投影调用 |
| 当前代次等于定位截止 | 首轮完整成功；已读取 |
| 首轮事实代次漂移 | 丢弃首轮材料；按回显实际代次完整重试一次 |
| 第二轮事实代次漂移 | 版本漂移；无第三次调用 |
| 顶层许可拒绝 | 许可拒绝；零等待零重试 |
| 顶层资源失败 | 资源失败 |
| 顶层入口拒绝 / 内部不一致 / 坏失败形状 | 内部不一致 |
| 任一节点未找到且无内部类失败 | 未找到 |
| 项目已退出 / 异种 / 属性未设置 / 成功缺事实，或业务字段矛盾 | 内部不一致；无部分登记 |

读回失败不得修改定位缓存、补写、换编码、调用历史 / 恢复或伪装成功。

## 7. 验证合同

至少执行：

```powershell
git diff --check -- '海中鱼巣/领域/服务.L1实际存在.ixx' '施工记录/20260808_L2-EXISTENCE-REGISTRY-CURRENT-READ-CONSISTENT-PROJECTION-MIGRATION_施工记录.md' '验证记录/20260808_L2-EXISTENCE-REGISTRY-CURRENT-READ-CONSISTENT-PROJECTION-MIGRATION_构建与生产消费验证.md'
python .\tools\check_specs.py --strict
MSBuild.exe .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m:1
MSBuild.exe .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Release /p:Platform=x64 /m:1
pwsh -NoProfile -File .\tools\clang_ast\扫描.MSVC模块依赖.ps1 -项目根目录 . -配置 Debug -平台 x64 -输出路径 <系统临时目录\Debug模块依赖.json>
pwsh -NoProfile -File .\tools\clang_ast\扫描.MSVC模块依赖.ps1 -项目根目录 . -配置 Release -平台 x64 -输出路径 <系统临时目录\Release模块依赖.json>
.\x64\Debug\海中鱼巣.exe --runtime-context
.\x64\Release\海中鱼巣.exe --runtime-context
```

尖括号表示执行者在系统临时目录中创建的本轮唯一绝对路径，不是字面参数。构建和扫描不得使用、删除、覆盖或枚举异主 `.codex-build`；模块扫描不得写正式图谱输出。双配置 Rebuild 和两个生产运行必须退出 0；双配置扫描必须所有候选成功、拓扑完整、循环 0、外部未解析需求 0；strict 必须全部现行目录项通过。

静态门禁：

- `按登记读取权威事实` 内 `尝试读取中性一致当前投影` 物理调用点恰 1；逻辑调用最多 2；只存在一个有界两轮控制结构。
- 只有首轮合法事实代次漂移进入第二轮；第二轮漂移、许可拒绝、资源失败、入口拒绝、内部不一致和坏失败形状均不重试；不存在第三次调用、阻塞回退或 `sleep`。
- `按登记读取权威事实` 内 `读取中性当前事实代次`、`读取中性当前节点`、`读取中性当前属性`、`读取中性当前值`、`读取中性当前关系`、`读取完整快照` 和 `尝试读取完整快照` 均为 0。
- 固定请求六组长度为 `2/0/0/0/0/0`，顺序为服务身份、资格属性类型；首轮期望代次来自定位事实截止。
- 成功验证两节点种类、类型表示、空槽、生命周期、共同创建代次和可选期望创建代次；返回登记事实截止等于共同读取代次。
- 非成功不组装部分登记、不修改定位缓存；公开 DTO / 枚举 / 函数签名零差异。
- `按登记读取权威事实` 之外的 `建立登记` 写面、`读取资格实现`、规格和其它资格函数相对施工起点行为零变化；它们的旧读取调用不得因本叶归零。
- 核心 L1、世界登记、场景、原子发布、组合服务、启动、运行期上下文、工程、filters 和自检零修改。
- staged 与提交范围只含第 3 节三路径；P0-FIX、路线、`.codex-build`、两份 WORLD-TREE 和其它 WIP 零暂存、零修改。

生产运行退出 0 只证明正式入口没有拒绝启动，不单独证明结构化实际存在登记业务成功。若本代码施工段不含独立生产代码外消费者验收，验证记录必须明确写“公开租约连续两次结构化登记 / 场景 / 世界读回未在本段独立执行”。后续集成验收只能经有效运行期上下文租约连续两次调用 `读取实际存在登记`、`读取初始场景` 和 `读取世界登记`，要求结构化已读取、逐字段一致且共同截止。

## 8. 完成声明边界

本计划施工完成最多证明实际存在登记这一具名 L2 当前读回根已迁移到中性非阻塞一致当前投影，并通过约定构建、静态和正式生产入口运行门禁。只有合法消费者实际取得结构化已读取，并从公开登记 / 场景 / 世界入口核对完整终态，才可累计“两节点一致当前投影及一次完整代次收敛”的本消费者真实使用证据。

实际存在登记两节点创建已经由既有 P4 验收覆盖，不得在本叶重复冒充新增证明。不得声明 L1 自证、第一层已完善、值、属性、关系、关系组、修改、退出、历史、其它消费者或资格读取已经迁移，或构建 / 静态检查 / 生产退出码等于业务成功。
