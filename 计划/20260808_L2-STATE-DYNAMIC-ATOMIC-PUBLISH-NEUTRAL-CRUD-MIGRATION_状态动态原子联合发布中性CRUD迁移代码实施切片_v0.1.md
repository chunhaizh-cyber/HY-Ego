# L2-STATE-DYNAMIC-ATOMIC-PUBLISH-NEUTRAL-CRUD-MIGRATION 状态动态原子联合发布中性 CRUD 迁移代码实施切片 v0.1

日期：2026-08-08  
状态：创建侧设计已闭合；登记后可执行

## 1. 目标与完成条件

在现有 `状态动态原子发布服务` 对象内，把唯一 `发布实际存在I64后继状态与迁移动能` 从旧幂等探测、业务意图 / 执行证据和带标签提交迁移为一次业务中性写集提交，并建立服务专属普通互斥锁、多项完整请求账和状态 / 动态领域权威读回。

完成必须同时满足：

- 公开 DTO 升为 v2，所有无中性来源的旧证据字段物理删除；
- 首次只运行 P13 与按需 P14，同义重复不重跑上游规格；
- 首次和同义重复都恰一次调用 `提交中性写集`，缓存不得直接返回成功；
- 同键异请求在第二层冲突；
- L1 已发布后发生映射或读回失败时保留首次完整写集和映射，不丢失已发布事实；
- 状态和可选动态必须经各自正式领域读取入口权威读回；
- 本模块旧探测、旧提交、业务标签、意图和执行证据调用归零。

## 2. 施工依据

- 详细设计：`规范/详细设计/20260808_L2-STATE-DYNAMIC-ATOMIC-PUBLISH-NEUTRAL-CRUD-MIGRATION_状态动态原子联合发布中性CRUD迁移详细设计_v0.1.md`
- 函数流程图：`流程图/20260808_L2-STATE-DYNAMIC-ATOMIC-PUBLISH-NEUTRAL-CRUD-MIGRATION_发布实际存在I64后继状态与迁移动能函数流程图_v0.1.md`
- 正式规范：4015、4030、4040、4050、4190。
- 前置结果：`L2-STATE-DYNAMIC-ATOMIC-PUBLISH-SERVICE-OWNER v0.1` 结果提交 `76c2408ca52dcd81fa12876e1c7ac05b501deb4a`。

执行前必须从正式新 HEAD 重做 S0，机械核对中性 DTO、P13 / P14 写入规格、状态 / 动态读回请求形成函数和本计划文件白名单。

## 3. 精确允许范围

只允许修改：

1. `海中鱼巣/领域/服务.状态动态原子发布.ixx`
2. `海中鱼巣/领域/状态动态原子发布.数据.h`
3. `施工记录/20260808_L2-STATE-DYNAMIC-ATOMIC-PUBLISH-NEUTRAL-CRUD-MIGRATION_施工记录.md`
4. `验证记录/20260808_L2-STATE-DYNAMIC-ATOMIC-PUBLISH-NEUTRAL-CRUD-MIGRATION_构建与静态验证.md`

禁止修改核心 L1、P13、P14、运行期上下文、世界树门面、工程 / filters / 启动文件、旧验收、P0-FIX、路线、旧 WORLD-TREE 和其它 WIP。不得新增自检模块、验收运行器、专项 L1 驱动或第二发布入口。

## 4. 实施步骤

1. 在数据头将合同 / 规则升为 v2；删除公开业务标签 / 意图常量和全部旧 L1 证据字段；建立首次见证和 v2 内部原因。
2. 在服务模块删除请求摘要编码、领域意图、执行证据材料、旧写集完成、旧探测映射和旧完整读回处理。
3. 建立旧 P13 / P14 写项到中性节点、关系、值、属性槽和退出项的一一转换；冻结状态在前、动态在后的唯一顺序和私有幂等键映射。
4. 为服务新增普通 `std::mutex`、多项 `std::map` 发布账和提交阶段；不得使用递归锁。
5. 实现首次预占、一次中性提交、发布标志分账和已发布记录不可丢失边界。
6. 实现同键完整请求比较；同义重复原样重提缓存写集并只接受精确重复，同键异请求返回冲突。
7. 先固化中性映射，再形成状态 / 动态读回请求；首次和重复都重新调用领域所有者并逐字段形成结果。
8. 建立专属施工 / 验证记录，只记录已发生事实，不写业务通过声明。

任何需要改变 P13 / P14 公开规格、核心中性合同、运行期所有权或世界树门面签名的问题都属于设计漂移，必须停止受影响切片；不得扩大白名单。

## 5. 验证

至少执行：

```powershell
git diff --check -- '海中鱼巣/领域/服务.状态动态原子发布.ixx' '海中鱼巣/领域/状态动态原子发布.数据.h' '施工记录/20260808_L2-STATE-DYNAMIC-ATOMIC-PUBLISH-NEUTRAL-CRUD-MIGRATION_施工记录.md' '验证记录/20260808_L2-STATE-DYNAMIC-ATOMIC-PUBLISH-NEUTRAL-CRUD-MIGRATION_构建与静态验证.md'
python .\tools\check_specs.py --strict
```

并按仓库现行构建入口完成 Debug / Release x64 完整重建和两配置模块扫描。静态门禁至少核对：

- 目标模块旧幂等探测调用数为 0；
- 目标模块旧 `提交写集` 调用数为 0；
- 目标模块领域意图和执行证据材料构造数为 0；
- 目标入口 `提交中性写集` 调用点恰 1；
- 公开数据头旧摘要、旧完整读回和旧失败见证字段为 0；
- 普通互斥锁和多项发布账各有唯一所有者；
- 本计划之外无新增暂存文件。

不运行或新建 L1 专项验收。若当前没有合法生产消费者，业务场景明确记录为未运行。

## 6. 完成声明边界

本计划只证明状态与动态联合发布领域入口的代码已迁移为中性提交并可从领域所有者读回。构建和静态检查不证明真实业务调用、生产装配、世界树第四写、其它旧读取接口迁移、旧核心写面删除、第二至第五层服务集合完成或 L1 已完善。

