# ENTRY-MOD-S1 入口剩余责任与第二批自检迁移收口复核计划 v0.1

日期：2026-07-12

队列编号：#254 / 设计队列：DQ-146

状态：依赖门控待执行 / 前置 #253 / 纯只读收口 / 未改 C++

## 1. 目标与依据

依据第二批流程图 / 详细设计、#236 和 #251-#253 实施记录，只读确认入口是否只保留参数、模式、唯一装配、初始化、阶段登记、顶层运行和退出码。

## 2. 扫描与产物

枚举入口函数 / 类型 / lambda / 并发 / 输出 / include / import、工程中模块身份和自检编号归属；形成剩余责任矩阵、模块依赖图、重复 / 遗漏编号和下一具体残留逻辑包。若仍有残留，登记为后续设计事实，不在本计划修改代码。

## 3. 允许与禁止

只写归档计划、实施记录、索引和项目记忆。禁止 C++、工程、规范、流程图修改，禁止构建运行，禁止因行数下降宣称入口完成。

## 4. 验收

A01-A10 覆盖入口全部责任、全部自检模块、阶段注册、历史初始化 include 清零和生产 / 自检依赖方向。严格规范、diff、提交推送。

## 5. 边界

若矩阵归零才可声明达到当前入口边界；不代表未来功能无需新增顶层登记。
