# FS-10 显示层只读视图详细设计与 DTO 格式计划 v0.1

日期：2026-07-07
状态：待确认 / 未改代码 / 未迁移旧函数 / S0 后续计划

## 1. 依据

```text
AGENTS.md
计划/计划索引.md
规范/000_项目规则总纲.md
规范/001_规则迁移清单.md
计划/20260707_FS10_显示层只读视图增强专项_v0.1.md
实施记录/20260707_FS10_显示层只读视图增强S0当前代码事实扫描_Codex断点清单.md
实施记录/20260707_FS10_显示层只读候选逻辑迁移包归并表.md
```

## 2. 确认范围

本计划只确认 FS-10 S0 之后的文档治理路线：

```text
生成显示层只读视图详细设计。
生成只读视图 DTO / JSON 格式计划。
生成只读前后结构不变化验收矩阵。
形成后续第一轮代码实施切片候选，但不直接实施。
与控制面板原样迁移专项建立边界：FS-10 先提供只读视图材料，控制面板迁移另走独立专项。
```

## 3. 明确排除项

```text
不迁移旧显示函数体。
不新增显示标题、HTML、JSON、控制台摘要或 SQL 投影机器字段。
本计划不接控制面板、SQL、WebView2、窗口、截图、线程、D455、体素或外设；控制面板如按用户新策略迁移，必须走独立控制面板专项。
不把显示标题、JSON 或控制台输出作为机器事实。
不在本计划中修改 C++。
```

## 4. 目标产物

| 产物 | 作用 | 状态 |
| --- | --- | --- |
| `规范/详细设计/显示层只读视图详细设计.md` | 定义只读视图入口、读取材料、失败分流和结构不变化约束 | 待生成 |
| `计划/待确认计划/20260707_FS10_显示层只读视图DTO_JSON格式专项_v0.1.md` | 定义 DTO / JSON 字段边界、只读输出和后续代码切片入口 | 待生成 |
| `实施记录/20260707_FS10_显示层只读视图详细设计与DTO格式计划生成_Codex断点清单.md` | 记录本计划生成和后续门禁 | 待生成 |

## 5. 实施步骤

| 步骤 | 内容 | 是否允许改代码 |
| --- | --- | --- |
| S1 | 生成显示层只读视图详细设计，明确显示标题只读派生、世界结构只读 DTO、失败空视图和结构不变验收 | 否 |
| S2 | 生成 DTO / JSON 格式专项计划，明确节点句柄、节点类型、显示标题、子项摘要和错误视图边界 | 否 |
| S3 | 登记待确认问题，确认是否允许后续新建只读视图服务或仅扩展世界服务只读入口 | 否 |
| S4 | 若 S1 / S2 获确认，再另建代码实施切片，列出允许文件、禁止文件和验证命令 | 否 |

## 6. 允许文件

```text
规范/详细设计/显示层只读视图详细设计.md
计划/待确认计划/20260707_FS10_显示层只读视图DTO_JSON格式专项_v0.1.md
实施记录/20260707_FS10_显示层只读视图详细设计与DTO格式计划生成_Codex断点清单.md
计划/计划索引.md
项目记忆/当前状态.md
项目记忆/待确认问题.md
项目记忆/Codex任务队列.md
项目记忆/实施记录.md
```

## 7. 禁止文件

```text
海中鱼巣/领域/*.h
海中鱼巣/核心/*.h
海中鱼巣/入口.cpp
海中鱼巣.vcxproj
旧项目源码
SQL / 控制面板 / WebView / D455 / 体素 / 外设相关文件
```

## 8. 验收方式

```powershell
git diff --check
if (Test-Path .\tools\check_specs.py) { python .\tools\check_specs.py }
```

不运行 C++ 构建，不运行默认入口。

## 9. 待确认问题

| 编号 | 问题 | 默认建议 |
| --- | --- | --- |
| FS10-DES-Q1 | 第一轮显示只读入口是否允许新建独立只读视图服务。 | 先在详细设计中比较独立服务与世界服务只读入口，不在本计划确认时决定代码文件。 |
| FS10-DES-Q2 | DTO / JSON 是否可以作为后续控制面板输入。 | 可以作为后续控制面板重构阶段的只读材料候选；本计划阶段不直接接控制面板或 WebView2。 |
| FS10-DES-Q3 | 控制面板原样迁移是否并入 FS-10。 | 不并入；FS-10 只做只读视图和 DTO / JSON，控制面板另建原样迁移优化重构专项。 |

## 10. 完成声明边界

确认本计划后只能声明：

```text
FS-10 S0 后续显示层只读视图详细设计与 DTO 格式计划已确认。
允许执行文档治理，生成详细设计和 DTO / JSON 格式计划。
```

不得声明显示层只读视图代码已实现，不得声明控制面板、SQL、WebView2 或外设已接入。
