# FS-04 需求树后续结构拆分设计专项 v0.1

日期：2026-07-07
状态：待确认 / 滚动循环生成 / 文档治理计划 / 未改代码 / 未迁移旧函数

## 1. 依据

```text
实施记录/20260707_FS04_需求服务目标状态与任务承接材料S1-S4代码实施_Codex断点清单.md
计划/已完成计划/20260707_FS04_需求服务目标状态与任务承接材料S1-S4代码实施切片_v0.1.md
计划/20260707_FS04_需求服务目标状态与任务承接材料增强专项_v0.1.md
实施记录/20260706_FS04_需求树入口只读扫描记录.md
规范/详细设计/需求目标状态承载详细设计.md
规范/详细设计/任务系统详细设计.md
规范/领域服务授权写入规范.md
```

FS-04 第一轮已经实现并验证需求身份复核、目标状态读取、主体 / 目标宿主 / 场景承接材料读取、任务承接拒绝和需求结算摘要边界。本专项只承接 FS-04 第一轮明确排除的需求树后续结构，不基于未完成的 FS-06、FS-07 或后续队列项预设结果。

## 2. 确认范围

确认后只允许生成需求树后续结构详细设计和后续实施切片候选，范围包括：

```text
FS-04-E：父子需求角色、阻塞与权重读取。
FS-04-F：逻辑组织需求、获取途径与目标语义分流。
FS-04-G：需求方向签名、枚举目标和方法候选准入边界。
FS-04-H：需求树结构化更新指令。
```

本专项不直接进入 C++ 实施。若后续需要修改 C++，必须另建代码实施切片并再次确认允许文件、禁止文件和验证方式。

## 3. 目标结构和服务边界

```text
目标服务：需求服务、任务服务。
目标结构：需求父子关系、需求阻塞关系、需求权重材料、需求逻辑组织关系、目标语义材料、需求方向签名材料、方法候选准入材料、需求树更新指令材料。
```

第一轮设计必须继续保持：

```text
需求目标是目标状态，不是 I64。
需求主体、目标宿主和场景材料仍由需求服务读取。
方法候选不得绕过任务授权、任务承接和任务方法关系。
需求方向 bool、目标语义文本、获取途径枚举和显示摘要不得作为机器事实。
```

## 4. 明确排除项

```text
不修改 C++。
不迁移旧函数体。
不新增旧需求主信息字段。
不实现完整需求树运行时更新。
不实现方法候选算法。
不把方向 bool、文本说明、显示标题、日志或控制台输出作为机器事实。
不接控制面板 / SQL / ADO / WebView / D455 / 体素 / 外设。
```

## 5. 实施步骤

```text
S0：复核 FS-04 第一轮完成产物和旧需求树只读扫描证据。
S1：生成父子需求角色、阻塞和权重读取详细设计。
S2：生成逻辑组织需求、获取途径和目标语义分流详细设计。
S3：生成需求方向签名、枚举目标和方法候选准入边界设计。
S4：生成需求树结构化更新指令设计和拒绝矩阵。
S5：登记后续代码实施候选，但不自动确认代码实施。
```

## 6. 允许文件

```text
规范/详细设计/需求树后续结构详细设计.md
项目记忆/*
实施记录/20260707_FS04_需求树后续结构拆分设计_Codex断点清单.md
```

## 7. 禁止文件

```text
海中鱼巣/**/*.h
海中鱼巣/**/*.cpp
海中鱼巣.vcxproj
旧项目源码
SQL / 控制面板 / WebView / D455 / 体素 / 外设文件
```

## 8. 验收方式

```powershell
git diff --check
if (Test-Path .\tools\check_specs.py) { python .\tools\check_specs.py }
```

验收时必须能说明：

```text
每个后续结构的用途、写入方、读取方、生命周期和拒绝路径。
哪些内容进入后续代码实施切片，哪些内容继续暂缓。
本专项不证明任何 C++ 实现。
```

## 9. 完成声明边界

完成后只能声明 FS-04 需求树后续结构详细设计已生成或待确认。不得声明需求树完整实现、方法候选完成、旧需求能力迁移完成或控制面板 / SQL / 外设接入。

## 10. 待确认问题

```text
FS04-NEXT-Q1：是否确认本专项作为 FS-04 第一轮完成后的需求树后续结构设计计划？
FS04-NEXT-Q2：方法候选准入设计是否只定义需求侧准入材料，不等待 FS-06 / FS-07 代码结果？
```
