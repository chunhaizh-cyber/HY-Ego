# FS-02 实例状态生命周期读取阻断代码实施切片 v0.1

日期：2026-07-07
状态：待确认 / 代码实施切片候选 / 详细设计待同步确认 / 不实现删除写入口 / 未改代码 / 未迁移旧函数

## 1. 依据

```text
规范/详细设计/实例状态生命周期治理详细设计.md
实施记录/20260707_FS02_因果模板查询候选与状态生命周期详细设计专项生成_Codex断点清单.md
实施记录/20260707_FS02_动态聚合因果模板删除实例状态后续S0当前代码事实扫描_Codex断点清单.md
规范/详细设计/状态服务详细设计.md
规范/详细设计/状态动态二次特征因果服务增强详细设计.md
规范/错误结果与入口拒绝规范.md
```

## 2. 确认范围

本切片确认后，只允许实现实例状态生命周期第一轮读取和阻断复核：

```text
读取实例状态引用阻断原因(状态)
实例状态是否可失效(状态)
读取实例状态材料时拒绝不完整或已失效旧句柄
```

同步确认 `规范/详细设计/实例状态生命周期治理详细设计.md` 中“读取拒绝、引用阻断和结构不变化”部分作为第一轮代码依据。

## 3. 明确排除项

```text
不实现 删除实例状态(状态) 写入口。
不实现级联清理。
不删除节点、主信息、关系或索引。
不修改核心仓库删除语义。
不迁移旧状态删除函数体。
不接 SQL / 控制面板 / D455 / 体素 / 外设。
```

删除写入口仍保持 QR-087 待确认。

## 4. 目标服务和结构

目标服务：

```text
状态服务
动态服务，限只读阻断复核。
二次特征服务，限只读阻断复核。
```

目标结构：

```text
实例状态读取拒绝结果
实例状态引用阻断原因
结构不变化验收材料
```

## 5. 允许文件

```text
海中鱼巣/领域/状态服务.h
海中鱼巣/领域/动态服务.h
海中鱼巣/领域/二次特征服务.h
海中鱼巣/入口.cpp
计划/计划索引.md
项目记忆/*
实施记录/20260707_FS02_实例状态生命周期读取阻断代码实施_Codex断点清单.md
```

## 6. 禁止文件

```text
海中鱼巣/核心/*.h
海中鱼巣/领域/需求服务.h
海中鱼巣/领域/任务服务.h
海中鱼巣/领域/方法服务.h
旧项目源码
SQL / 控制面板 / D455 / 体素 / 外设相关文件
工程文件，除非构建失败证明必须修正
```

## 7. 实施步骤

```text
S0：读取状态、动态、二次特征服务当前代码事实，复核实例状态材料读取入口。
S1：收紧实例状态材料读取，拒绝无主体、无场景、无值或无时间戳材料。
S2：新增实例状态引用阻断原因只读入口，第一轮只复核状态被动态和二次特征使用的阻断边界。
S3：新增实例状态是否可失效只读入口，不执行删除。
S4：默认入口补有效状态可读、不完整状态拒绝、动态引用阻断、二次特征引用阻断、拒绝后结构不变化验收输出。
S5：同步计划索引、项目记忆和实施断点清单。
```

## 8. 验收方式

```powershell
msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m
.\x64\Debug\海中鱼巣.exe
git diff --check
python .\tools\check_specs.py
rg -n "删除实例状态|级联|Remove|Delete|SQL|控制面板|D455|体素|外设" 海中鱼巣/领域/状态服务.h 海中鱼巣/领域/动态服务.h 海中鱼巣/领域/二次特征服务.h 海中鱼巣/入口.cpp
```

## 9. 完成声明边界

完成后只能声明：

```text
实例状态生命周期第一轮读取拒绝和引用阻断只读入口已实现并验证。
删除拒绝和读取拒绝后结构不变化已验证。
```

不得声明：

```text
删除实例状态写入口已实现。
状态生命周期完整治理已完成。
级联清理已实现。
旧状态删除能力已迁移。
```

## 10. 待确认问题

```text
FS02-STATE-LIFE-IMP-Q1：是否确认本切片同步确认实例状态生命周期治理详细设计的读取拒绝和引用阻断部分。
FS02-STATE-LIFE-IMP-Q2：是否确认第一轮继续不实现 删除实例状态(状态) 写入口，只做只读阻断复核。
```
