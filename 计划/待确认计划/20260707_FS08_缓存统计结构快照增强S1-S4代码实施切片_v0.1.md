# FS-08 缓存统计结构快照增强 S1-S4 代码实施切片 v0.1

日期：2026-07-07
状态：待确认 / 代码实施门禁 / 未改代码 / 未迁移旧函数

## 1. 依据

```text
计划/20260707_FS08_缓存统计结构快照增强专项_v0.1.md
实施记录/20260707_FS08_缓存统计结构快照增强S0当前代码事实扫描_Codex断点清单.md
规范/非权威缓存与统计规范.md
规范/详细设计/非权威缓存与统计详细设计.md
规范/需求结算记录规范.md
规范/详细设计/需求结算记录详细设计.md
```

S0 已确认：当前没有独立 `非权威缓存统计服务.h`，缓存统计能力仍在 `统计服务.h`；已有结构统计运行期快照、清空 / 重建和非权威裁决拒绝入口。

## 2. 确认范围

本切片确认后，只允许执行 FS-08 S1-S4 第一轮代码实施：

```text
S1：缓存命名空间和统计快照结构复核。
S2：结构统计重建和清空复核。
S3：缓存缺失业务不变复核。
S4：后续缓存排除边界复核。
```

第一轮不实现特征值哈希、命中观察、需求结算摘要、存在观测缓存或二次特征签名缓存。

## 3. 允许文件

```text
海中鱼巣/领域/统计服务.h
海中鱼巣/入口.cpp
项目记忆/*
实施记录/20260707_FS08_缓存统计结构快照增强S1-S4代码实施_Codex断点清单.md
```

本切片默认不新建 `非权威缓存统计服务.h`。如用户确认要拆分服务文件，必须先修改本切片允许文件。

## 4. 禁止文件和禁止范围

```text
禁止修改旧项目源码。
禁止修改核心仓库、工程文件、SQL / 控制面板 / D455 / 体素 / 外设文件。
禁止新增旧主信息缓存字段。
禁止迁移旧缓存 / 统计函数体。
禁止实现特征值内容哈希、特征值命中观察、Vec 原始值缓存、需求结算摘要刷新、存在观测缓存和二次特征签名缓存。
禁止自动删除、修复或补写关系。
禁止让需求、任务、方法依赖缓存完成判断。
```

## 5. 实施要求

```text
缓存命名空间只定位、清空、失效和重建缓存项，不表达业务事实。
结构统计运行期快照只读取权威节点、关系和索引结构。
重建结构统计缓存必须从权威仓库数量入口读取。
清空缓存不得改变节点、关系、索引、状态、动态、需求、任务或方法事实。
缓存可裁决业务事实必须保持 false。
拒绝路径必须证明权威结构不变化。
```

## 6. 验证方式

```powershell
msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m
.\x64\Debug\海中鱼巣.exe
git diff --check
rg -n "哈希|命中|需求结算摘要|存在观测|二次特征签名|SQL|控制面板|D455|体素|外设" 海中鱼巣/领域/统计服务.h 海中鱼巣/入口.cpp
```

默认入口至少输出或覆盖：

```text
缓存命名空间复核通过。
结构统计快照重建通过。
清空缓存后权威节点 / 关系 / 索引数量不变。
缓存缺失不影响需求 / 任务 / 方法事实。
缓存业务裁决拒绝。
后续缓存能力未进入本轮。
```

## 7. 完成声明边界

完成后只能声明 FS-08 第一轮结构统计运行期快照、清空 / 重建和缓存非权威边界已实现并验证。不得声明完整缓存系统、特征值哈希、需求结算摘要、存在观测缓存或旧缓存能力迁移完成。

## 8. 待确认问题

```text
FS08-IMP-Q1：本切片是否确认不新建 `非权威缓存统计服务.h`，继续使用 `统计服务.h` 承载第一轮缓存统计能力。
FS08-IMP-Q2：是否确认本轮只复核已有结构统计快照，不新增孤立关系诊断报告结构。
```
