# D455-P1 成熟度、三模式与四产品强类型协议代码实施切片 v0.2

编号：D455-P1；路由：#361 / DQ-253；合同：PER-C1 / ABI 2；日期：2026-07-24

设计状态：终审 PASS。当前状态、版本和 plan blob 唯一读取 `计划/计划索引.md`。

## 合同与边界

- 依据：6300、6310、6350、6360；`规范/详细设计/D455成熟度产品受控材料与报告队列详细设计.md` v0.2；`流程图/20260724_PERCEPTION-INGEST_D455成熟度产品受控材料报告队列施工流程图_v0.2.md`；`规范/详细设计/函数结构知识图谱/20260724_PERCEPTION-D0_D455观察体素生产闭环函数结构知识图谱_v0.2.md`。
- 已实现事实：现有 L0 同步采样协议可只读；目标协议模块不存在。
- 待实现：`D455观察供给协议ABI版本 = 2`，稳定枚举、`D455产品描述`、四种强类型负载、`D455产品分类结果 验证D455产品分类(const D455产品描述&) noexcept`。
- L0 不含点云；成熟度、外显模式和产品类型三轴独立；原始逐簇、稳定子集、扫描变化、目标跟踪负载必须与产品类型唯一匹配。

## 文件所有权

允许：`海中鱼巣/适配/协议.D455观察供给.ixx`、`海中鱼巣/适配/自检.D455观察供给合同.ixx`、`实施记录/20260724_D455-P1_PER-C1强类型协议代码实施_Codex断点清单.md`。

禁止：其它代码、工程、filters、入口、统一运行器、生产装配、规范 / 流程图 / 详细设计 / 计划 / 索引。不得读取设备、队列、仓库或领域服务。

## 施工与 S0

1. S0 核对精确 plan blob、执行通道 clean、两代码文件不存在或仅有本计划同义断点，详细设计字段 / 枚举 / 签名无漂移。
2. S1 建立中文命名值式 DTO、稳定枚举与 ABI 常量；实现无副作用验证。
3. S2 覆盖字段缺失、非法组合、类型 / 负载错配、时间倒置、来源和规则版本。
4. S3 专项自检源码只验证协议，不登记工程或运行器。
5. S4 核对白名单、import 方向、中文命名、`git diff --check` 与 staged diff。
6. S5 只提交允许代码与专属记录，推送当前执行通道分支并恢复 clean。

## 机械验证命令

S0 记录 `$计划段起点 = (git rev-parse HEAD).Trim()`。S4 运行 `git diff --check`、`git diff --cached --check`，均须退出 0。S5 提交后运行：

```powershell
$期望 = @('海中鱼巣/适配/协议.D455观察供给.ixx','海中鱼巣/适配/自检.D455观察供给合同.ixx','实施记录/20260724_D455-P1_PER-C1强类型协议代码实施_Codex断点清单.md') | Sort-Object
$实际 = @(git -c core.quotepath=false diff --name-only "$计划段起点..HEAD") | Sort-Object
if (Compare-Object $期望 $实际) { exit 1 }
if ((@(rg -n "D455观察供给协议ABI版本\s*=\s*2" '海中鱼巣/适配/协议.D455观察供给.ixx')).Count -ne 1) { exit 1 }
if ((@(rg -n "验证D455产品分类" '海中鱼巣/适配/协议.D455观察供给.ixx')).Count -lt 1) { exit 1 }
```

全部命令须退出 0；ABI 定义精确 1 处，目标函数至少 1 处。

## 验证与收口

- 静态验证：精确两个代码文件、ABI=2、四负载分支、禁止依赖零命中。
- 候选验证：同身份同规则同义；非法组合全部具名拒绝；不构建、不运行。
- `DRIFT-PER-CONTRACT`：字段 / 枚举 / 签名不符；`DRIFT-PER-CURRENT-INTERFACE`：L0 事实不符；`DRIFT-PER-FILE-OWNERSHIP`：需要第三代码文件；`DRIFT-PER-IMPORT-CYCLE`：出现反向模块依赖。
- 前置通过后值式冻结变化为 `FAIL-PER-INTERNAL-READBACK`；其它验证失败为 `FAIL-PER-VALIDATION`。仅允许按计划形成 clean WIP 提交，不扩大范围。
- 完成只证明 PER-C1 源码候选固定；不证明编译、接线或真实 D455 能力。
