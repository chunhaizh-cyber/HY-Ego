# L1-SIMPLIFY-P13-STATE-SPEC 实际存在 I64 后继关系19状态写入规格代码实施切片 v0.2

计划身份：`L1-SIMPLIFY-P13-STATE-SPEC`

版本：`v0.2`

创建侧基线：`a3a346ea0fc79fb1870fe95fd5710a17ce5da313`

具名漂移：`DESIGN-DRIFT-P13-P12-VERSION-AND-ORDER-STALE`

## 1. 目标与替代关系

v0.2 完整替代并禁止施工 v0.1。目标仍是从 P10/P8 当前事实、P12 相邻前状态和 P11 比较形成 P9 同形后继状态纯值规格，不写 L1、不分配稳定编码、不发布第二状态或关系20。P12 依赖改为 v0.4，结果链 `df0e0b1b7aeabd700c3289a266db7ebe24207fe8` + `de0009c624391ff1c76fd0f55fbfff78d9fd7f61`；P12 自检为既有顺序360，不再引用 v0.1 或顺序370。

## 2. 精确白名单与所有权

```text
海中鱼巣/领域/L1状态.数据.h
海中鱼巣/领域/服务.L1状态.ixx
海中鱼巣/领域/组合.后继状态规格.ixx
海中鱼巣/领域/自检.L1状态.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
施工记录/20260805_L1-SIMPLIFY-P13-STATE-SPEC_施工记录.md
验证记录/20260805_L1-SIMPLIFY-P13-STATE-SPEC_验证记录.md
```

不修改启动、P12/P9/P10/P11、L1事实基座、P0-FIX、路线、P15、其它计划或 WIP。状态服务拥有状态规格 DTO/形成函数；组合模块拥有跨领域请求、编排和结果投影；自检拥有顺序360内的专项追加。

## 3. 机械施工合同

1. 在状态数据头追加 v0.1 已冻结的后继状态规格 DTO，不导入比较领域类型。
2. 在状态服务新增唯一 `形成已校准实际存在I64后继状态规格(const 已校准实际存在I64后继状态材料&)`；只验证值式材料和构造二节点、五值、五关系19、五属性槽。
3. 新建组合模块，固定 `G0 -> P10/P8 -> P12 v0.4 -> P11 -> G1 -> 状态规格形成`。P12 请求只填合同/规则/选择规则版本、业务查询身份、主体、实例槽、拟发布后状态发生时间；P12 `已选择` 的完整前状态和选择证据直接作为 P11 左输入，禁止再次调用 P9、历史枚举或首态入口。
4. P12 `无前状态` 返回 P13 `无前状态`，P12 `未找到/冲突/版本漂移/许可拒绝/资源失败/内部不一致`映射为对应非成功；所有非成功清空规格、迁移证据和摘要。
5. P10/P8 当前作为 P11 右输入；校准主体、场景、实例槽、P2定义、来源、值、业务时间、P12证据、P11注册/算法/三态/等价/差异和共同截止。G0/provider/G1不一致完整重试一次，仍漂移返回 `版本漂移`。
6. 等价形成规格并禁止动态；增加/减少形成同形规格并允许后继动态规格消费。固定 `0x53000001..0x5300000C` 本地键，值不作关系端点，不复制 P2 定义。
7. 在现有 `自检.L1状态` 中追加 P13 专项；继续由既有顺序360执行，不修改启动、不新增370。

## 4. 待激活门禁

保持 `待激活`。执行前必须从新正式 HEAD 重做 S0，确认 P12 v0.4 实际函数/DTO/结果字段、P10/P8/P11接口、状态/比较/实例特征文件、工程/filters、顺序360和验证资源均匹配；任何接口变化先退回计划支撑，不临场改语义。

## 5. 验证合同

- 三份专属设计/流程/计划治理文档与八路径施工白名单精确闭合；diff-check、cached diff-check、strict通过；
- Debug/Release x64 Rebuild、完整自检、MSVC模块扫描通过；P12专项仍证明顺序360而非370；
- P12当前候选成功、合法无前态、从未存在、后来退出不可达、退出关系拒绝、历史读回互证、冲突、漂移和全非成功零载荷逐项映射；
- P10/P8/P11等价、增加、减少、坏成功、跨场景、时间/来源/截止漂移；二节点、五值、五关系19、五槽、`0x53000001..0x5300000C`唯一；同义摘要确定性和零 L1写入；
- 状态服务不导入比较服务，组合器不反向被 provider 导入；关系20、动态发布、事务、SQL、恢复、旧句柄和缓存调用为零。

## 6. 完成边界

只证明 P13 源码候选能从同截止上游材料形成后继关系19状态纯值规格；不证明上游实现、第二状态/关系20/动态发布、最终原子提交、撤销、生产装配、持久恢复、集成验收或 SELF-GOV-CLOSURE 完成。
