# DATA-L2 普通方法公开服务闭合代码实施切片

日期：2026-08-16

版本：v0.1

身份：`DATA-L2-ORDINARY-METHOD-SERVICE-CLOSURE`

状态：设计就绪；以计划索引为准

## 1. 目标

按[详细设计](../规范/详细设计/20260816_DATA-L2-ORDINARY-METHOD-SERVICE-CLOSURE_普通方法公开服务闭合详细设计_v0.1.md)，在现有唯一方法 owner / 服务 / 聚合 / 普通应用路径一次实现普通方法完整公开 DTO、新增、新身份替换、退出、当前 / 历史读取和按结果规格候选读取，并以生产外合法普通应用消费者完成权威读回。

本计划默认合并 DTO、服务、装配和首次真实读回，不再为身份 wrapper、helper、getter或单个 CRUD 另立后继计划。

## 2. 正式依据与 S0

执行前读取 0050、3300、4010、4015、4230、5300 v0.6、最终需求清单 DATA-EXT-14、本详细设计、当前公共头 / 服务 / 普通应用和索引。确认 `DATA-L2-ORDINARY-METHOD-IDENTITY-CLOSURE`、`DATA-L2-ORDINARY-METHOD-SPEC-REFERENCE-CLOSURE`、方法登记根与聚合装配结果仍在正式主线，目标文件 clean，HEAD / origin / index 和唯一代码段 / 构建资源可用。

## 3. 精确允许范围

生产文件：

```text
海中鱼巣/领域/L2普通方法结构.数据.h
海中鱼巣/领域/服务.L2方法结构.ixx
海中鱼巣/装配.普通应用.ixx
```

专属记录：

```text
施工记录/20260816_DATA-L2-ORDINARY-METHOD-SERVICE-CLOSURE_普通方法公开服务闭合施工记录_v0.1.md
验证记录/20260816_DATA-L2-ORDINARY-METHOD-SERVICE-CLOSURE_普通方法公开服务闭合检查记录_v0.1.md
```

工程文件原则上只读；现有头、模块和装配模块均已登记。只有 S0 证明登记缺失且计划支撑明确修订范围后才能修改工程。禁止修改 L1、其它 L2 服务、聚合类、最终需求清单、BIZ、SELF、CF、正式规范、计划索引和异主 WIP。

## 4. 实施步骤

1. 在普通方法头实现详细设计冻结的事实、请求、结果、结果规格键、纯值完整性和成功形状 helper。
2. 在现有方法服务私有登记普通方法结构，加入三个只读结构依赖，实施新增、替换、退出、当前 / 历史读取和候选读取。
3. 普通应用只调整现有方法服务构造参数；owner 顺序、聚合类型和 getter ABI 不变。
4. 建立仓库外生产外专项，经普通应用 getter 覆盖详细设计矩阵；不得登记生产工程。
5. 根工程双配置 fresh Rebuild，运行 strict、diff、UTF-8、XML、白名单、生产零测试痕迹检查并清理专项实际文件。
6. 写入专属记录，只暂存本计划文件，普通提交和推送；结果不自动升级任务选择、方法执行或 BIZ 闭环。

## 5. 失败收口

普通编译、实现或专项错误在允许范围修复。若当前 L1 owner / 属性类型组读 ABI、世界结构公开读、5300 v0.6 或方法服务构造事实与详细设计发生会改变公开合同的漂移，停止受影响代码并具名退回计划支撑；不得拆回多个微型计划掩盖冲突。

发布未知时只读核对远端，禁止 force、reset、stash、merge、rebase、cherry-pick。无关 dirty 原地保留。

## 6. 完成声明

只有公开 DTO、五个服务函数、普通应用合法取得路径、权威读回专项和根工程验证均成立，才可声明本切片结果完成。完整 DATA-EXT-14 是否满足还须对照最终需求清单逐项复核；真实任务冻结、候选优劣、动作执行、结果回读、学习、恢复和 BIZ 消费均为 `NOT_RUN`。
