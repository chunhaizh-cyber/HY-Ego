# WORLD-TREE-P1A2Q 按精确索引键读取当前目标代码实施切片 v0.4

计划身份：WORLD-TREE-P1A2Q

版本：v0.4

创建侧当前代码基线：`3242c16640da898b546f294199c00c08032ba49c`

## 1. 具名依赖与目标

两个具名结果提交已经进入正式代码；P1A3F 的索引状态由本计划发布批次的第二笔索引提交同步闭合：

1. WORLD-TREE-P1A2 v0.3 结果提交 `65166e1ce979841502f8420204f6163f588c35e8` 已提供本计划读取的查询服务、共享合同和索引仓底座。
2. WORLD-TREE-P1A3F v0.3 结果提交 `3242c16640da898b546f294199c00c08032ba49c` 已以顺序180登记 `WORLD-TREE-P1A3F-S01`，正式完整自检基线为19项；其索引行须在本 v0.4 取得可执行状态的同一发布批次先登记为已退出。结果未包含允许但非完成必需的专属施工/验证记录，不影响九个代码/工程/自检结果路径与本计划基线，但不得据此宣称记录已形成。

本计划在现有索引仓增加能区分未找到与内部矛盾的结构化精确读取，把旧 optional 普通重载收敛为兼容包装；再在 `节点直接结构查询服务` 增加按精确索引物理键读取当前节点/关系稳定见证的值式函数。完成时公开函数在一个共享许可内返回恰一当前索引或具名非成功，零机器事实写入。

直接施工依据：

```text
规范/详细设计/20260803_WORLD-TREE-P1A2Q_按精确索引键读取当前目标详细设计_v0.2.md
规范/详细设计/20260803_WORLD-TREE-P1A2Q_故障注入补充详细设计_v0.1.md
流程图/目标业务流程图/业务节点到能力与目标函数映射.md（“世界事实维护”领域归属）
流程图/20260802_WORLD-TREE-P1_世界树业务服务业务流程图_v0.1.md（B01/B02）
流程图/20260803_节点直接结构查询服务读取当前索引_函数流程图_v0.2.md
流程图/20260803_可重建索引仓库读取索引物理键结构化_函数流程图_v0.2.md
```

## 2. 允许文件

```text
海中鱼巣/核心/服务.节点直接结构.ixx
海中鱼巣/核心/仓库.可重建索引.ixx
海中鱼巣/核心/自检.节点直接结构查询.ixx
海中鱼巣/启动.应用程序.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
施工记录/20260803_WORLD-TREE-P1A2Q_施工记录.md
验证记录/20260803_WORLD-TREE-P1A2Q_验证记录.md
```

共享合同头、其它仓库、执行器、数据操作、提交/恢复服务和 SQL 均为只读依赖，不得修改。索引仓只允许增加详细设计 §2.1 的内部状态/结果/函数，并把现有无事务序号 optional 重载原位收敛为包装；候选生命周期和带事务序号重载不得修改。

## 3. 禁止范围

禁止新公开服务 DTO/枚举、第二索引仓、缓存、SQL回源、恢复包装改造、全表反向扫描、仓库锁外泄、向 L2 返回内部句柄、旧栈适配和生产路由接线。除详细设计 §2.1 的一个内部结构化结果外不得增加通用值仓或第二读取合同。禁止把未找到变成空成功或把零代次变成成功。

## 4. 机械施工

1. S0 核对精确 plan blob、两个具名结果提交、当前查询类/索引仓签名、顺序180和顶层19项自检基线；任一不符零代码修改返回具名漂移。
2. 严格按详细设计 §2.1 在索引仓增加结构化精确读取；旧无事务序号 optional 重载只包装该结果，带事务序号重载及候选生命周期零变化。
3. 严格按详细设计 §2.2—§3 在查询服务增加唯一函数；复用现有 `节点直接索引读取结果` 和 `形成关系见证_`，捕获并结构化全部异常。
4. 严格按故障注入补充详细设计，在既有专项宏内为索引仓根和查询服务根分别建立实例私有、一次性资源/其它异常注入；宏关闭时零字段、零符号、零分支，禁止执行者另选测试接口。
5. 新建专项自检模块，覆盖详细设计 v0.2 §5 和补充设计 §3 矩阵；以 `WORLD-TREE-P1A2Q-S01` 顺序190登记，把完整自检登记/执行期望从19精确更新为20。顺序180/19项基线不成立返回 `DRIFT-P1A3F-SELFTEST-BASELINE`；顺序190被占用返回 `DRIFT-SELFTEST-ORDER-190`，不得临场改号。
6. 更新工程和 filters，形成专属施工/验证记录，完成 Debug/Release 和正式完整自检。

## 5. 验证与失败收口

```powershell
git diff --check
python .\tools\check_specs.py --strict
msbuild .\海中鱼巣.vcxproj /m /p:Configuration=Debug /p:Platform=x64
msbuild .\海中鱼巣.vcxproj /m /p:Configuration=Release /p:Platform=x64
.\x64\Debug\海中鱼巣.exe --self-test-exit
rg -n "SQL|读取目标当前索引组|节点句柄|关系句柄" 海中鱼巣/核心/服务.节点直接结构.ixx 海中鱼巣/核心/自检.节点直接结构查询.ixx
rg -n "读取索引物理键结构化|可重建索引精确读取状态|可重建索引精确读取结果" 海中鱼巣/核心/仓库.可重建索引.ixx
```

新增公开函数体和专项自检不得出现 SQL、全表反向索引扫描或公开内部句柄。结构化仓读取必须证明正向条目、反向唯一绑定和权威目标三者一致；索引仓锁内不得调用节点/关系仓。若必须修改共享合同、其它仓库或 P1A2 其它执行切片，停止并返回设计漂移。

## 6. 不可变历史与完成边界

v0.1—v0.3 文件及其 plan blob 只作历史证据，不再构成执行入口。现行执行者只消费本 v0.4、详细设计 v0.2 和两张 v0.2 函数图；不得把旧目标图引用、P1A2 v0.2、P1A3F v0.1/v0.2 或旧自检数量重新解释为当前门禁。

完成只声明按精确索引键读取当前稳定目标的 L1 查询候选通过本计划矩阵；不声明 P1A3、世界根、恢复或 L2 世界树服务完成。
