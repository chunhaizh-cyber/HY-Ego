# 相关概念窄参与提供者代码实施计划

计划身份：`L2-BOUND-ADD-CONCEPT-PARTICIPANT`

日期：2026-09-13
版本：v0.1
起点 HEAD：`0a30fa46599d2f3b0dab824d504b2b413464c0e5`
状态：待激活
具名依赖：无

## 1. 目标和完成条件

实现概念唯一 owner 的无总根窄参与合同，使后继场景/存在/特征 L2 添加函数能够提交存在或特征概念方案并取得概念 owner 的原子参与片段，再按实际截止独立读回概念及世界挂接。状态只引用已有特征概念，动态等待7191专属定义，因果沿4340独立合同；该叶不修改六类数据类和世界树应用。

完成必须同时满足：新合同可编译；概念服务实现准备/读回；创建与复用均不依赖四总根；请求缺挂靠、类别冲突、签名冲突、跨类上位、成环、漂移、预算和幂等分支有结构化结果；专项 Debug/Release 通过；根工程两配置至少完成构建差异归因。

## 2. 正式依据和当前事实

- 0050 v3.20、2220 v1.18、4230 v0.51、4240 v0.37及`L2-BOUND-ADD-CONCEPT-REFERENCE`详细设计。
- 当前 `数据服务.概念树类.ixx` 是概念唯一 owner，但生产 API 仍带旧四根；六类没有可复用的无总根参与者。
- L1 有限 N v3 已实现，复用其实际公开 DTO 和提交规则；本计划不改 L1。
- 4240 第2节的 `不可变材料身份` 是语言/外置材料的逻辑类型，当前代码没有同名 C++ 声明；本叶不实现该通用材料服务，概念同义签名只使用现有 `概念树定义` 的确定规范化与逐字段相等。

## 3. 允许和禁止文件

允许生产文件：

- `海中鱼巣/领域/合同.相关概念添加参与.ixx`（新建；接收从概念树数据模块原名迁移的基础纯值类型并定义参与 ABI）
- `海中鱼巣/领域/数据服务.概念树类.ixx`
- `海中鱼巣.vcxproj`
- `海中鱼巣.vcxproj.filters`

允许验证文件：

- `验证工具/相关概念窄参与专项验证.cpp`
- `验证工具/相关概念窄参与专项验证.vcxproj`
- `施工记录/20260913_L2-BOUND-ADD-CONCEPT-PARTICIPANT_相关概念窄参与提供者施工记录_v0.1.md`
- `验证记录/20260913_L2-BOUND-ADD-CONCEPT-PARTICIPANT_相关概念窄参与提供者验证记录_v0.1.md`

禁止修改六类数据服务、世界树应用、L1、普通应用入口、旧概念应用、数据库 schema 和存量数据。禁止恢复或新增概念总根。

## 4. 目标接口和实现

严格实现详细设计第5节的 `相关概念类别`、方案、挂靠、预算、状态、请求、片段、完整读回及 `相关概念添加参与者`。C++ 精确字段顺序按详细设计；签名和定义使用当前真实 `概念树定义`，概念和直接上位使用 `概念树概念身份`，挂靠使用 `概念树形成世界引用`，不得引用不存在的材料身份。参与片段固定使用 `L1有限N分区原子参与者写集_v3`，不得改变语义或新建事务协议。

把详细设计具名的基础纯值声明从 `数据服务.概念树类.ixx` 移到新合同模块；数据模块改为 `export import` 新合同，所有原名、字段、分派和值语义保持不变。新合同只导入 `海中鱼巣.核心.合同.L1所有者范围CRUD`，从而不形成六类数据服务与概念实现的模块环。

`概念树类数据服务`公开继承参与者并实现：

```cpp
相关概念参与片段 准备相关概念片段(
    const 相关概念参与请求&, std::uint64_t Gread) const noexcept override;
相关概念参与读回 读取相关概念结果(
    const 相关概念参与请求&, std::uint64_t Gread,
    std::uint64_t H) const noexcept override;
```

准备顺序固定为：入口/预算 -> 当前 G -> 世界挂靠强类型历史见证 -> 类别与定义联合一致 -> 复用目标或按规范化 `概念树定义` 查找 -> 上位同类及无环 -> 形成概念 owner 局部写集。精确复用不建立新概念，只在缺少相同挂接时准备挂接；完整相同返回无写集的 `精确重复`。创建方案命中同义概念时收敛为复用，未命中才使用局部新编码；顶层允许零直接上位，非顶层保留调用方提交的完整父组；禁止从名称、旧根或遍历位置选择类别。

读取按 `Gread/H` 返回请求对应的概念完整事实和该请求世界事实对应的唯一当前形成挂接；H 必须非零且不晚于 Gread。提交产生写集时使用 L1 返回的实际 H；`精确重复`无提交，使用 `H=Gread`。所有直接上位边稳定排序并全部回显，顶层空组是完整成功。失败载荷为空。

新 `相关概念结构交付` 的字段、21项类型登记角色及 owner 写入映射严格采用详细设计第5节：`概念族锚点 -> C`、`C -> 定义记录`、`上位C -> 下位C`、`C -> 形成记录 -> 世界端点`及类别/定义/证据/生命周期值。每项使用交付中的具名类型编码，禁止复用旧根归属关系表达类别。概念、定义、直接上位和首次形成挂接必须进入同一概念 owner 的 `L1有限N分区原子参与者写集_v3`；精确复用缺挂接时该片段只新增形成记录及其关系和值。所有新身份由参与写集本地键产生，调用方不得预分配稳定编码。

`概念树类数据服务`新增详细设计第5节冻结的纯新构造与迁移构造。纯新构造接收 L1、特征、存在、特征值、场景、唯一概念 owner 端口和新交付；迁移构造再依次接收旧 `概念树结构交付` 与新交付。旧布局、新布局均在内部保存为 `optional`：纯新实例只含新布局，迁移实例两者均含，保留旧构造只含旧布局。新参与方法没有新布局时返回 `旧格式不支持`，旧入口没有旧布局时返回同状态；两侧不得读对方布局。这样同一个服务实例和同一个概念 owner 端口承载迁移两侧，不建立第二服务、第二端口或第二概念身份来源。

## 5. 非成功、事务与并发

执行者按详细设计枚举逐项映射，不得把未找到、读取失败和格式不支持合并。准备函数零提交，不持有跨调用裸指针；结果写集只属于概念 owner 且不暴露端口。读回只读。

专项测试用测试内受控协调器把概念片段与一个假世界 owner 片段交给现有 L1 v3；生产模块不得新增协调器。覆盖同键同请求、同键异请求、同 G 竞争、提交前失败、提交后读回、原键重放。真实发布未知和故障注入不能执行时明确 `NOT_RUN`，不得记通过。

## 6. 机械施工步骤

1. S0 核对 HEAD、计划 blob、工作区、L1 v3 精确类型名和概念服务当前构造/旧根路径；发现公开合同漂移才退回计划支撑。
2. 新建合同模块，迁移详细设计列出的原名基础纯值类型并加入存在/特征参与 DTO；只依赖 L1 公共/owner-scoped 合同，不依赖不可变材料或六类数据服务实现。
3. 在概念服务加入 `相关概念结构交付`、新/迁移构造路径、无总根准备和读回；逐字段实现本节写入映射。旧入口只走可选旧布局，新入口只走新布局，两者共享同一端口、互斥和首次材料恢复。
4. 在 `海中鱼巣.vcxproj` 的领域合同模块组登记新 ixx，在 `海中鱼巣.vcxproj.filters` 登记同路径/筛选器；新增独立专项工程并以项目引用或与主工程相同的模块清单编译，不加入普通应用运行路径。
5. 形成施工/验证记录，记录精确文件、命令、结果、未运行项和知识变更清单。

## 7. 验证矩阵

| 组 | 场景 | 成功条件 |
| --- | --- | --- |
| V1 | 创建存在、特征概念各一项 | 片段 owner 唯一，无总根；提交后类别/定义/挂接完整读回 |
| V2 | 精确复用及缺挂接补充 | 不重复概念；仅必要挂接片段；最终同义唯一 |
| V3 | 空挂靠、端点退出、跨截止 | 提交前结构化拒绝，当前 G 不变 |
| V4 | 类别/签名冲突、跨类上位、成环 | 精确状态，零片段 |
| V5 | 同键同请求、同键异请求、同 G 竞争 | 精确重复/幂等冲突/漂移分账，无双写 |
| V6 | 预算与资源/读回异常 | 预算拒绝；可注入异常按阶段返回；未注入项记 NOT_RUN |
| V7 | 旧根隔离 | 新合同和实现符号不读写四根；不产生新根 |
| V8 | 新布局独立构造与迁移构造 | 无旧根交付可准备/提交/读回新概念；迁移构造共享同一 owner 端口且旧、新入口互不读取对方布局 |
| V9 | 工程登记 | 新模块在 vcxproj/filters 唯一登记；专项 Debug/Release 均实际运行 |

验证命令至少包括：

```powershell
msbuild .\验证工具\相关概念窄参与专项验证.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64
.\验证工具\x64\Debug\相关概念窄参与专项验证.exe
msbuild .\验证工具\相关概念窄参与专项验证.vcxproj /t:Rebuild /p:Configuration=Release /p:Platform=x64
.\验证工具\x64\Release\相关概念窄参与专项验证.exe
msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64
msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Release /p:Platform=x64
python .\tools\check_specs.py --strict
git diff --check
```

若实际输出目录不同，按 vcxproj 的 `TargetPath` 运行，不得靠猜测路径跳过运行。

## 8. 完成声明边界

本计划完成只证明存在/特征概念窄参与提供者可供后继消费，且新路径可不带旧根交付独立构造。它不证明六类添加入口、动态概念专属定义、世界树应用接线、旧概念应用迁移、存量四根迁移、普通生产装配、持久/跨进程恢复或正式集成验收。计划保持 `待激活`，须在本设计包提交并推送、索引填入该提交下精确计划 blob 后才能改为 `可执行`。
