# FS-05 任务服务承接材料与生命周期 S1-S4 代码实施切片 v0.1

日期：2026-07-07
状态：已确认 / 代码实施门禁已打开 / 未改代码 / 未迁移旧函数

## 1. 依据

```text
计划/20260707_FS05_任务服务承接材料与生命周期增强专项_v0.1.md
实施记录/20260707_FS05_任务服务承接材料与生命周期S0当前代码事实扫描_Codex断点清单.md
规范/任务系统规范.md
规范/详细设计/任务系统详细设计.md
规范/详细设计/方法系统详细设计.md
规范/领域服务授权写入规范.md
```

S0 已确认当前缺口：统一任务承接材料对象、任务过程账、任务实际结果状态、任务选择 / 当前 / 候选方法封装入口和完整任务状态机。

## 2. 确认范围

本切片确认后，只允许执行 FS-05 S1-S4 第一轮代码实施：

```text
S1：任务承接材料读取复核。
S2：任务虚拟存在和生命周期读取增强。
S3：任务实际结果状态与完成前置拒绝。
S4：任务选择方法关系读取复核。
```

第一轮不实现完整任务状态机，不实现任务筹办和任务执行桥。

## 3. 允许文件

```text
海中鱼巣/领域/任务服务.h
海中鱼巣/领域/需求服务.h
海中鱼巣/领域/方法服务.h
海中鱼巣/入口.cpp
项目记忆/*
实施记录/20260707_FS05_任务服务承接材料与生命周期S1-S4代码实施_Codex断点清单.md
```

如实施中需要修改核心仓库、工程文件、线程 / 调度文件或外设文件，必须停止并登记待确认问题。

## 4. 禁止文件和禁止范围

```text
禁止修改旧项目源码。
禁止新增旧任务主信息字段。
禁止迁移旧函数体。
禁止实现任务筹办主入口、任务执行桥、工作线程、std::async、派发、取消、超时和外设等待。
禁止实现任务运行统计、权重评分、错误码治理、运行包、治理协议和恢复快照。
禁止让任务服务直接访问特征值服务。
```

## 5. 实施要求

```text
任务必须来源于有效需求。
任务目标状态必须来自需求目标状态。
任务虚拟存在必须唯一可读。
生命周期状态仍通过状态服务记录并携带发生时间戳。
任务实际结果状态必须作为状态节点关系读取，不写旧主信息字段。
任务选择方法关系只建立最小选择 / 读取 / 拒绝边界，不实现候选集合。
拒绝路径必须证明节点、关系、状态和方法关系不变化。
```

## 6. 验证方式

```powershell
msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m
.\x64\Debug\海中鱼巣.exe
git diff --check
rg -n -F "特征值服务" 海中鱼巣/领域/任务服务.h 海中鱼巣/领域/需求服务.h 海中鱼巣/领域/方法服务.h
```

默认入口至少输出或覆盖：

```text
任务承接材料复核通过。
缺目标状态或缺场景拒绝。
任务虚拟存在唯一读取。
任务实际结果状态读取通过。
未达成完成前置条件时拒绝完成。
缺任务方法关系不得执行方法。
拒绝后结构数量不变。
```

## 7. 完成声明边界

完成后只能声明 FS-05 第一轮任务承接材料、生命周期读取、实际结果状态和任务方法关系边界已实现并验证。不得声明完整任务系统、完整状态机、任务筹办、任务执行或旧任务能力迁移完成。

## 8. 待确认问题

```text
FS05-IMP-Q1：已确认。第一轮只复用任务虚拟存在和生命周期状态读取，不新增过程账锚点。
FS05-IMP-Q2：已确认。第一轮任务方法关系继续使用 `引用` 关系，并由任务服务封装选择 / 读取入口。
```
