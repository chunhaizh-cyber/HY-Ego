# ARCH-L1-L2-STATE-HISTORY-CURRENT-SELECTION-REFERENCE-CLOSURE

## 状态历史、当前选择与引用闭包代码实施切片 v0.1

状态：设计就绪候选；第一笔发布后登记为唯一可执行。
设计基线：`main@c6e2431e612a13a4e8aa277cd94b31e06a12431c`

## 1. 目标与完成条件

### 目标

为现有 ARCH-L2 状态/动态两参与者原子 provider 补齐中性底座闭环：

1. L1 在全部参与者候选写集应用并统一事实代次后，交换正式仓库和写幂等账前执行全局当前节点/关系端点闭包检查；
2. 状态事实及其主体、特征、值、时间、来源和版本材料永久保留；状态 owner 以唯一 `L2特征实例→L2状态` 当前选择关系表达当前性；
3. 首态、迁移、失败零写、幂等和独立读回均通过公开中性结构合同验证。

### 完成条件

- 无悬空当前关系、当前值槽或独立材料引用可以进入正式仓库；参与者应用顺序任一交换均拒绝悬空候选。
- 迁移后旧状态事实与旧选择历史可读，新选择唯一当前，动态前/后状态关系分别指向旧/新永久状态。
- 所有非成功路径不交换正式仓库、不推进事实代次、不写跨 owner 幂等账，并返回结构化失败结果。
- 首态无动态；重复首态/重复迁移返回同义幂等结果，异义请求零写冲突。
- Debug/Release 根工程和 `D:\TEMP` 中性 harness 验证矩阵通过；独立读回与失败账断言均有记录。

## 2. 范围与明确排除

### 允许范围

只允许修改下列生产文件：

```text
海中鱼巣/核心/仓库.L1事实基座.ixx
海中鱼巣/领域/L2状态结构.数据.h
海中鱼巣/领域/服务.L2状态结构.ixx
海中鱼巣/领域/L2状态动态原子发布.数据.h
海中鱼巣/领域/服务.L2状态动态原子发布.ixx
```

专属施工/验证记录可写入 `施工记录/` 与 `验证记录/` 的本计划同名路径；仓库外专项 harness 仅置于 `D:\TEMP` 的本计划隔离目录，不纳入生产目标。

### 禁止范围

不得修改任务、方法、需求、普通应用、入口、工程文件、端到端测试、L4 状态/动态第二权威、P0-FIX、路线、`.codex-build` 或其它 owner 的 WIP。不得加入材料化状态身份、第二见证节点、历史关系特例或第三 owner 事务。

本叶不实施任务轮次、任务生命周期、目标裁决、路径验证、需求满足、需求结算、学习、价值回流、现实授权或生产消费者；`TASK-ROUND-INITIAL-STATE-ARCH-L2-MIGRATION v0.3` 只能在本叶发布并由执行侧取得真实结果后重新 S0。

## 3. 正式依据与当前事实

- `规范/1160_根规范_状态节点_20260720.md`：状态节点与状态事实的身份、历史和关系边界。
- `规范/4015_子规范_L1简化事实基座.md`：L1 跨 owner 原子候选、代次、闭包、幂等和零写边界。
- `规范/4230_子规范_L2六类纯结构统一合同.md`：L2 状态/动态纯结构 CRUD、引用和 owner 分账。
- `规范/规范目录.md`：现行 ARCH-L1/L2 映射、版本和本叶入口。
- `规范/详细设计/20260824_ARCH-L1-L2-STATE-HISTORY-CURRENT-SELECTION-REFERENCE-CLOSURE_状态历史与当前选择引用闭包详细设计_v0.1.md`：本叶的字段、时序和边界设计。

当前代码复用裁决：扩展现有 `核心/仓库.L1事实基座.ixx` 的跨 owner 提交路径和引用闭包检查；扩展现有状态结构 DTO/服务与状态动态原子发布 DTO/服务，不新建第二 L1 ABI、第二状态 owner 或新服务层。

## 4. 机器合同

### 4.1 L1 全局闭包

在 `核心/仓库.L1事实基座.ixx` 增加内部无异常检查函数：

```cpp
当前节点引用闭包检查结果 检查跨所有者原子候选全局引用闭包(
    const 状态& 候选仓库) noexcept;
```

调用顺序固定为：获取锁、校验 G0/幂等/参与者、复制候选、应用状态写集、应用动态写集并解析局部引用、统一候选事实代次、全局闭包检查、形成结果、写幂等账、交换正式仓库。

闭包检查覆盖当前关系三端点、当前值所属/类型/来源/材料端点、当前属性槽和候选索引一致性；历史事实不因退出当前而被当作悬空。请求悬空映射 `引用冲突`，候选索引/不变量损坏映射 `内部不一致`；G0 漂移、许可拒绝、资源失败和幂等冲突保持现有分账。闭包失败时零载荷、零写、零代次推进、零幂等账。

### 4.2 状态当前选择与永久事实

新增状态 owner 专属关系类型 `L2状态当前选择关系类型身份`，方向为：

```text
源：L2特征实例身份
目标：L2状态身份
唯一键：(特征实例身份, 当前选择关系类型)
```

宿主存在与状态主体在同一 G0 互证；当前范围等同于特征实例，不新增 L3 范围身份。状态节点、主体/特征关系、值、时间、来源、版本事实永久有效，当前性只由选择关系决定。

`L2状态结构.数据.h` 至少提供：当前选择关系引用、当前选择读取请求/结果、状态原子参与者局部键和旧/新选择证据。`服务.L2状态结构.ixx` 提供首态建立、当前选择切换、状态身份历史读和当前选择读；缺选择关系的旧数据返回 `待迁移`，不推断、不写入。

### 4.3 两参与者原子迁移

首态：状态 owner 建立永久状态事实与唯一当前选择关系，无动态。
迁移：状态 owner 退出旧选择关系、建立新永久状态事实和新选择；动态 owner 建立动态主体、前态→旧状态、后态→新状态；两方候选写集由 L1 在同一 G0 全局闭包检查后一次交换。

旧状态节点保持普通关系可引用；A1 和动态前态都可长期引用旧状态。状态 owner 不解释业务成功性，动态 owner 不写状态选择关系。

## 5. DTO/API 与错误合同

### 公开结构 DTO

- `L2状态当前选择关系引用`：关系稳定编码、特征实例身份、当前状态身份。
- `L2状态当前选择读取请求`：L2 请求头、读取类别、特征实例身份、历史截止代次。
- `L2状态当前选择读取结果`：L2 结果头、当前/历史选择状态、可选选择关系引用。
- 状态原子参与者 DTO：当前选择关系局部键、旧选择稳定编码（首态为空）、新选择及退出证据。
- 状态动态原子结果：新选择和旧选择退出证据、动态前/后状态稳定身份。

### 错误与幂等

同一特征实例首态/迁移请求的同义重试返回首次成功结果；异义请求返回幂等冲突。G0 变化返回事实代次漂移。当前选择已被其它状态占用、局部引用无法在最终候选解析或候选闭包损坏分别返回引用冲突/内部不一致。所有非成功结果为零载荷和零写；成功后独立读回不得用缓存、日志或方法返回值代替正式仓库读。

## 6. 实施顺序

1. 在 L1 既有跨 owner 候选交换前插入最终全局闭包函数，保留现有错误分账与公开 ABI/合同版本。
2. 在状态数据 DTO/服务增加当前选择关系、首态/切换、当前选择读和待迁移拒绝；保持状态事实历史读。
3. 在状态动态原子 DTO/服务接入旧/新选择证据，确保两参与者局部引用最终闭合；不增加第三 owner。
4. 用 `D:\TEMP\ARCH-L1-L2-STATE-HISTORY-CURRENT-SELECTION-REFERENCE-CLOSURE\` 中性 harness 依公开入口覆盖正反矩阵。
5. 生成施工记录、验证记录，完成 Debug/Release 与独立读回；回传结果后再由计划智能体更新索引状态。

## 7. 验证合同

### 构建与专项

- Debug/Release 根工程 fresh Rebuild PASS。
- harness Debug/Release 覆盖：首态、迁移、状态→动态顺序、动态→状态顺序、参与者顺序反转拒绝、悬空引用拒绝、G0 漂移、许可拒绝、坏引用、精确重复、异义冲突。

### G1 五组正式读回

1. 旧状态事实和历史关系仍可读，且失去当前性不退出；
2. 当前选择关系唯一指向新状态，旧选择已退出当前；
3. 动态主体、前态→旧状态、后态→新状态关系端点与特征实例/主体一致；
4. 共同截止下参与者结果和事实代次一致，重复请求不产生第二代；
5. 每个失败分支正式仓库不变、代次不变、幂等账无新条目。

独立读回必须通过公开状态身份/历史 API、当前选择 API、动态 API 和 L1 代次/幂等查询互证；不得以日志、缓存、退出码或测试专用假对象替代。

## 8. 记录、提交与完成边界

施工记录和验证记录仅记录本计划实际代码与命令证据；不得将文档发布、编译 PASS 或中性 harness PASS 升级为 L4 任务消费者、任务轮次或五层总体完成。设计发布提交只证明本叶合同和计划已冻结，代码结果和 G1 生产闭环必须另由执行/集成验收确认。

计划身份：`ARCH-L1-L2-STATE-HISTORY-CURRENT-SELECTION-REFERENCE-CLOSURE`。
版本：`v0.1`。
