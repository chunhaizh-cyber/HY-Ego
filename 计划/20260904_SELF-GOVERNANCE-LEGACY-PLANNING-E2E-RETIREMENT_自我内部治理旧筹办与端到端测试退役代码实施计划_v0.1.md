# 自我内部治理旧筹办与端到端测试退役代码实施计划

身份：`SELF-GOVERNANCE-LEGACY-PLANNING-E2E-RETIREMENT`
版本：v0.1
状态：可执行（执行仍须 S0）
详细设计：`规范/详细设计/20260904_SELF-GOVERNANCE-LEGACY-PLANNING-E2E-RETIREMENT_自我内部治理旧筹办与端到端测试退役详细设计_v0.1.md`

## 1. 目标、范围和来源

同次完成旧需求初次筹办提供者、全部九个生产端到端测试和其工程登记的物理退役；清除三处编译消费者；补全保留的子目标回流后继筹办提交函数；以通用主阶段治理请求保留已存在任务的合法后继筹办链。

本计划来源是用户明确要求删除全部端到端测试和旧服务，以及执行现场的缺模块、缺类型和缺返回值诊断。机器语义依据为 3100、3200、5260、8100、8200 与规范目录第 232 行；不得借任何已废除历史材料决定接口或恢复范围。

允许文件为详细设计第 4、5 节枚举的所有生产、工程和测试文件，以及本计划专属施工/验证记录。禁止修改正式规范、规则、计划索引以外的计划、P04.2 任何材料、或创建任何新的端到端测试源。

## 2. S0（不可跳过）

1. 重新读取本计划、详细设计、计划索引、正式依据、`D:\\TEMP\\再用文件\\再用文件列表.md`、当前 Git/index/远端及全部目标文件的实际 WIP。
2. 取得完整删除集合、普通应用装配、两个线程入口、L2任务结构服务和回流重判服务的唯一所有权；其中 `海中鱼巣.vcxproj`、`.filters`、`任务管理线程.ixx`和 `服务.L2任务结构.ixx`为共同 ABI/工程入口，不能只取得局部文本所有权。
3. 对现有异主删除 WIP 做逐文件内容核对。只有明确交接后才能将其作为本计划候选；不得还原、覆盖、暂存或混入未交接的改动。
4. 确认独立 Debug/Release 输出目录和非 E2E 专项验证入口未被占用。未满足任一项时保持计划可执行但停止施工。

## 3. 施工步骤

1. 在 `L2任务结构`中补全 `提交任务子目标回流后继筹办准备`：沿既有写集 helper 完成当前准备读取、同键幂等重放、提交、后继记录历史读回和全部返回分支。保持 task owner 唯一写入，且不引入旧初次 DTO。
2. 对 `服务.任务子目标回流重判.ixx`做符号复核：其只消费回流记录、当前任务、轮次权威和后继记录；删除任何剩余旧 import、状态映射、构造注入或旧工作包引用。
3. 删除 `任务初次筹办.数据.h` 和 `服务.需求初次筹办准备.ixx`，以及它们的所有外部消费者。不得删除当前回流链实际读取的 L2轮次权威、首次记录或后继记录结构；这些结构的后续语义迁移不在本次退役范围，不能借本计划擅自重写。
4. 从普通应用装配删除旧 provider 的 import、所有权、getter、构造参数、创建步骤和错误枚举；保留并重新校验通用阶段端口和主阶段治理 provider 的装配顺序。
5. 从任务管理线程及运行包任务结果生产删除以旧结果为参数的重载和 import；普通 `任务主阶段治理请求` 转发和队列语义不变。
6. 删除九个工程测试文件、仓库根目录同名九份未跟踪副本，以及工程和 filters 中的每一登记；逐项检查 XML 不再引用已删除文件。
7. 运行旧符号归零和隔离编译；修复只在本计划允许范围内出现的机械编译错误。若需改变 self 任务承接端口、正式任务创建语义、现实授权、需求来源、结算或增加新公开 DTO，停止并退回计划支撑，不在本计划扩张。

## 4. 验证和完成声明

依次执行详细设计第 6 节的静态归零、工程结构、Debug/Release、非 E2E 专项与变更卫生验证。专属记录路径为：

- `施工记录/SELF-GOVERNANCE-LEGACY-PLANNING-E2E-RETIREMENT_施工记录_v0.1.md`
- `验证记录/SELF-GOVERNANCE-LEGACY-PLANNING-E2E-RETIREMENT_验证记录_v0.1.md`

完成只声明：旧服务、旧 DTO、九个 E2E、其消费者和工程登记已物理退役；保留的已存在任务回流后继链可编译、重放并读回。不得声明完整自我治理、任务首次承接、实际执行、结果结算、恢复或端到端业务验收已经完成。
