# L1-SIMPLIFY-P9-BASELINE-STATE 实际存在 I64 首个关系19基准状态代码实施切片 v0.2

计划身份：`L1-SIMPLIFY-P9-BASELINE-STATE`

版本：`v0.2`

创建侧代码基线：`2857874c6af2135bf831a6105a7996acb3f78ebc`

替代关系：本计划完整替代尚未执行的 v0.1；v0.1 禁止施工。

## 1. 目标和完成条件

实现 SELF-GOV-CLOSURE STEP-3C：以一个 L1 写集发布首个完整关系19基准状态，并使现有读取入口按状态自身稳定身份和完整见证读取不可变状态快照。

完成必须同时满足：

- 保留 v0.1 的登记、二节点/五值/五关系/五属性槽单写集、幂等和冲突合同；
- 读取请求不再携带 P7/P8 当前组合请求，不新增第二状态读取入口；
- 当前状态自有事实走 L1 当前入口，已退出事实走 L1 历史入口；
- P10 换代后旧 P8 值可从历史入口互证，场景移动后仍返回关系19冻结场景；
- 读取截止代次与采样截止代次分列，首末完整快照拒绝混代；
- 双配置构建/自检、strict、模块扫描和静态闭包通过。

唯一设计依据为本计划同名 v0.2 详细设计和函数流程图，以及 1160、4015、4190、4200、4210。v0.1 只作漂移证据。

## 2. 精确允许范围

只允许：

```text
海中鱼巣/领域/L1状态.数据.h
海中鱼巣/领域/服务.L1状态.ixx
海中鱼巣/领域/自检.L1状态.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
海中鱼巣/启动.应用程序.ixx
施工记录/20260805_L1-SIMPLIFY-P9-BASELINE-STATE_施工记录.md
验证记录/20260805_L1-SIMPLIFY-P9-BASELINE-STATE_验证记录.md
```

禁止修改 P7/P8/P10/P6 DTO、服务或自检，现行 L1 基座，全部 L2 合同，旧状态/动态/句柄/仓库/WORLD-TREE 实现，运行期、SQL、材料、恢复、日志、规范、详细设计、流程图、其它计划或计划索引。

## 3. 待实现提供者与激活门禁

本计划登记为 `待激活`，必须同时满足：

1. P7 v0.1、P8 v0.1 正式执行结果分别经交互智能体接受；
2. P7/P8 实际公开接口和事实形状匹配本设计发布前所消费的冻结合同；
3. P7/P8 释放代码、工程、启动和自检所有权；P9 八文件、顺序360及 Git 发布资源无其它所有者；
4. 从新正式 HEAD 重做 S0，复核 L1 当前/历史/完整快照入口、P7/P8状态、自检总数、模块数和相邻顺序后仍可机械施工。

P6、P9后继和P10均不是首态发布的机器语义前置。P10 只用于验证历史可读性，可由专项故障/换代夹具形成等价 L1 退出事实；不得因此伪造 P10 激活依赖。

## 4. 机械施工步骤

1. 新建数据头，形成 v0.2 详细设计冻结的常量、状态、登记、类型化见证、发布请求、修订读取请求、状态事实和结果 DTO。
2. 新建服务模块，构造仍保存 L1/P7/P8 三个长寿命引用；P7/P8只供发布前使用。
3. 实现 `0x0C` 七节点登记和 `0x0D` 首态单写集，逐项读回映射。
4. 发布入口经 P8/P7/L1 当前读取取得同代次采样材料，拒绝已有首态，提交本地键1—12。
5. 发布成功或精确重复后构造只含状态自身见证编码的读取请求，调用同一公开读取入口。
6. 实现私有“当前或历史事实读取”机械辅助：当前成功使用当前副本；当前 `已退出` 才按同编码读历史；不扫描、不猜测。
7. 读取入口以首末完整快照代次包围全部读取，从五关系解释角色，从五值解释资格/时间/引用/材料，再按引用编码从当前或历史入口互证 P8 被采样值。
8. 校验采样时生命周期：端点和被采样值创建不晚于采样截止，退出为空或晚于采样截止；状态自有二节点、五值和五关系创建代次统一等于采样截止加一。后来退出合法，不与 P7/P8 当前投影比较。
9. 新建顺序360专项，覆盖 v0.2 详细设计 §11；工程和启动只登记本计划文件与唯一自检。
10. 形成专属施工/验证记录，记录真实命令、结果、提交和未证明边界。

## 5. 事务、错误和停工条件

- 发布前失败零普通可读变化；发布后读回失败不补写、不回滚、不删除。
- 非成功业务载荷为空；错误严格按 v0.2 详细设计 §10 映射。
- 同主体+实例槽并发不同键只有一个首态成功；同键同义只发布一次。
- 若实现必须新增第二状态读取入口、重新依赖 P7/P8 当前投影、让比较服务解释关系19、修改白名单外文件，或改变关系19/写集结构，停止并具名退回设计漂移。

## 6. 验证合同

- 目标与 cached diff check 精确八文件；Markdown 只允许两份专属记录；
- Debug/Release x64 串行 Rebuild，零警告零错误；
- 两配置 `--self-test-exit` 退出0，完整自检总数以执行 S0 正式值加1；
- `python .\tools\check_specs.py --strict` 通过；
- MSVC模块扫描失败0、循环0、外部需求0，模块数以执行 S0 正式值加2；
- 四个公开函数、两个幂等域和顺序360定义唯一，中文命名，依赖无环；
- 发布写集、本地键1—12、关系19角色1—5、五属性槽和状态值见证逐项精确；
- 读取函数源码中 P7/P8 调用为零，第二公开状态读取入口为零；
- 全当前读回、P8值退出后历史读回、场景改变后原场景读回、其它端点后来退出历史读回通过；
- 当前/历史错种类、缺历史、生命周期非法、引用/材料/来源矛盾和持续代次漂移按矩阵拒绝；
- 关系20、动态、因果、P6、SQL、材料、旧栈和生产日志调用为零。

## 7. 完成声明边界

只证明：新 L1 状态服务发布首个关系19基准状态，并可在被采样值换代、场景改变或相关事实后来退出后，从 L1 当前/历史入口读取同一不可变状态快照的源码候选与本计划矩阵。

不得声明 P7/P8/P10 已实现、当前/相邻状态选择、第二状态、迁移动能、动作动态、因果、生产装配、持久恢复、旧消费者迁移、集成验收、STEP-3 或治理闭环完成。
