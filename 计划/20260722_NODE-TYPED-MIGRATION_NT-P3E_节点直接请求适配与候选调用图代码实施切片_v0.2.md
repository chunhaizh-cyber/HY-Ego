# NODE-TYPED-MIGRATION NT-P3E 节点直接请求适配与候选调用图代码实施切片 v0.2

身份：`#347 / DQ-239 / NT-P3E v0.2`

日期：2026-07-22

设计状态：终审 PASS

任务状态引用：#347 / 计划 v0.2 / 合同建立状态版本 JY-589/E330；当前生命周期只读取 `项目记忆/任务状态台账.md` 同任务最新状态版本，本文不维护派发、S0、执行或集成状态。

共同合同基线：`NODE-TYPED-MIGRATION/v0.2`
合同来源：`计划/20260722_NODE-TYPED-MIGRATION_NT-P3_服务兼容删除审计收口子计划_v0.2.md` 第 2 节
消费合同：`NT-C3/v0.2::C3-A0—C3-D2`
提供合同：`NT-C3/v0.2::C3-E1—C3-E3`、`CUTOVER-MAP/v1` 候选
合同性质：预冻结待实现；本文只登记 #347 的提供 / 消费边界，模块、文件、DTO、签名、ABI、所有权、生命周期、结果分类和漂移归属唯一读取合同来源。

共同依据：`计划/20260722_NODE-TYPED-MIGRATION_节点直接身份与领域类型化持久结构代码修订总计划_v0.2.md`、`计划/20260722_NODE-TYPED-MIGRATION_NT-P3_服务兼容删除审计收口子计划_v0.2.md`、`规范/详细设计/NODE-TYPED-MIGRATION_NT-P3_服务兼容删除审计迁移详细设计.md`、`流程图/20260722_NODE-TYPED-MIGRATION_NT-P3_服务兼容删除审计迁移施工流程图_v0.2.md`、`项目记忆/设计记录/20260722_NODE-TYPED-MIGRATION_NT-P3_函数结构知识图谱.md`。

## 1. 目标与候选边界

业务目标与 v0.1 相同：只为隔离新域形成值式请求适配、业务 / 只读候选组合和逐旧调用点 P4 切换映射。适配层不访问仓库、令牌、会话或参与者，不修改旧调用方、默认装配、入口和启动。

本任务不等待 #343—#346 实现完成，直接按合同形成源码和映射库存候选；不负责工程登记、构建、运行或最终验收。

## 2. 预冻结待实现接口合同

唯一合同为 P3 v0.2 子计划第 2 节 `NT-C3/v0.2`；本任务只实现 `C3-E1—C3-E3`，消费 `C3-A1—C3-D2` 和 `C3-A0`。对每个具名请求类型分别提供合同第 2.5 节规定的强类型重载集合；不得保留通用请求体、`variant`、字节载荷或兼容重载。本文不复制 DTO、签名或 ABI 定义。

所有调用只使用稳定值类型和结构化结果，保持同键同义、冲突、许可拒绝、版本漂移、逻辑内返回和内部逻辑错误可区分。

本任务向 #348 / #352 提供：

```text
节点直接值式业务请求适配入口
节点直接运行期业务操作候选组合
节点直接运行期只读查询候选组合
节点直接切换映射记录
```

`节点直接切换映射记录` 至少包含调用点文件 / 模块 / 类 / 函数，旧目标模块 / 入口，目标新模块 / 入口与待冻结签名，请求字段和身份映射，结果 / 拒绝 / 内部错误语义映射，结构依赖，当前是否生产可达，替换后的静态零命中模式，#352 最终动作，以及无法无歧义映射时的具名退回原因。它是切换库存，不是当前接线事实。

提供者最终接口漂移时退回精确提供计划和本任务；本任务不得发明兼容重载、旧域回退或双写。

## 3. 精确允许文件与所有权

```text
海中鱼巣/领域/节点直接切换映射.数据.h
海中鱼巣/领域/适配.节点直接运行期业务请求.ixx
海中鱼巣/领域/组合.节点直接运行期业务操作.ixx
海中鱼巣/领域/组合.节点直接运行期只读查询.ixx
海中鱼巣/领域/自检.节点直接候选调用图.ixx
实施记录/20260722_NODE-TYPED-MIGRATION_NT-P3E_节点直接请求适配与候选调用图代码实施_v0.2_Codex断点清单.md
```

#343—#346 文件、十四份旧服务头、11 个现行数据操作、17 个现行现代服务、线程、入口、默认装配、启动、恢复、工程 XML、`海中鱼巣/自检.运行器.ixx` 和旧物理文件全部只读。

本任务全部生产 / 自检文件必须包含 `海中鱼巣/领域/节点直接P3合同.数据.h`；该头由 #348 唯一拥有，本任务不得修改、复制或重定义。

## 4. 机械阶段、返回与漂移

S0 核对合同 blob、旧调用点只读扫描边界和文件所有权；S1 只复核 `C3-E1—C3-E3` 强类型重载与映射记录合同；S2 形成值式适配与两个候选组合；S3 形成逐调用点库存和独立自检候选；S4 完成静态、白名单和 Git 卫生收口。

请求缺失、类型错误、命名域未知、旧域 / 跨域句柄、业务前置拒绝、同键同义 / 异义、第一写前版本漂移和协议允许未找到属于逻辑内返回。前置通过后的提供服务内部错误、结构读回不一致或新旧域互认属于内部逻辑错误，不得压成 `bool false` 或普通等待。

旧调用点不能无歧义映射、需要双写、出现循环依赖或目标签名偏离合同时正式退回；不得修改真实调用点消除差异。

## 5. 检查、集成与完成边界

任务级只检查请求零仓库能力、映射库存字段完整、目标入口无重复 / 漏项、默认入口与旧调用方零差异、旧域零回退、生产依赖方向、白名单、UTF-8、`git diff --check` 和 clean；不构建、不运行。

最终按 `#343 -> #344 -> #345 -> #346 -> #347 -> #348` 汇集，真实调用点复核、工程接线和运行验证由 #352 完成。本任务只证明候选源码与逐调用点库存形成，不证明接口已实现、可编译、候选图可达或切换已经执行。
