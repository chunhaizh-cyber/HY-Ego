# PERSIST-S1-B HYSNAP01 规范编码与原子文件发布代码实施切片 v0.4

日期：2026-07-18

执行队列：#226

设计队列：DQ-118

设计裁决：JY-391、JY-422

状态：设计复核门控 / 当前禁止执行 / 等待 #296—#307 与 #309 正式完成 / 本文件只冻结后段职责，不构成 C++ 许可

## 1. v0.4 修订结论

#295 已由 `46e49c0` 完成，JY-391 已把前置链固定为：

```text
#296 P0-C
-> #297 P0-A
-> #298 B0
-> #299 B1 / #302 B4 两路并行
-> #301 B3 串行
-> #309 汇合阶段登记
-> #303 B5（#300 保持暂停且默认生产不可达）
-> #304 A0 -> #305 A1 -> #306 A2 -> #307 A3
-> #226 PERSIST-S1-B
```

v0.2 中尚未编号的 P0 / A 假定已被上述完整计划替代；JY-421 又把消费者路径纠正为两路并行、B3 后继串行和 #309 汇合。上述前置当前尚未全部形成 main 正式产物，因此 #226 继续禁止执行；只有 #307 完成后，设计角色按实际 DTO / 模块签名再修订 #226 并明确允许文件，才可恢复代码许可。

## 2. 正式依据与范围一致性

```text
流程图/20260718_权威状态快照隔离恢复与运行期上下文一次发布流程图_v0.3.md
流程图/20260718_权威状态快照隔离恢复与运行期上下文一次发布流程图_v0.3.html
规范/详细设计/权威状态快照隔离恢复与运行期上下文一次发布详细设计.md 第 17 / 20 节
实施记录/20260718_PERSIST-S1-P0_生产运行期所有权与概念活动承载当前事实矩阵.md
计划/20260718_PERSIST-S1-A3_正式租约同域独占九段冻结材料代码实施切片_v0.1.md 当前正文 v0.2
规范/代码文件建立归属与模块命名规范.md
规范/迁移路线权力分层规范.md
```

三层一致：上游拥有生产所有权和冻结材料；#226 只消费不可变九段 DTO，完成规范编码、当前写入方严格值式自读和同卷原子文件发布，不回头修改仓库、领域服务、上下文或入口生产所有权。

来源性质为 MIG-18 重新实现 / 新增约束 / 全新能力，OF-183—OF-190 只作证据；即使完成也不自动计入旧持久化能力迁移。

## 3. 依赖门控

| 前置 | 必须形成的正式证据 |
| --- | --- |
| #296 | 概念活动三角色、签名、版本唯一所有者与上下文完整性 |
| #297 | 排他的新运行期首发通道、生产版本 1 和同身份租约 |
| #298 | 当前关键路径强类型值式公开合同与公共只读查询门面 |
| #299 / #302 | 从同一冻结基线并行改接两组文件互斥消费者，并正式集成进入 main |
| #301 / #309 / #303 | B3 从新 main 串行改接，#309 汇合登记 920 / 940 / 950，随后默认入口切换；#300 默认不可达 |
| #304 | 索引所有者、命名域、版本、探测和键保留合同；入口 Debug 最小登记 970，运行器不变 |
| #305 | 四仓库全部状态与精确高水位 DTO；入口 Debug 最小登记 980，运行器不变 |
| #306 | 特征值侧表、系统角色、领域登记和概念活动导出；入口 Debug 最小登记 990，运行器不变 |
| #307 | 正式租约下同域独占、不可变九段冻结材料与精确读回；入口 Debug 最小登记 1000，运行器不变 |

任一项未完成、仍为待设计同步、阶段未通过或实际签名未归档时，#226 只返回依赖门控，不构建、不运行、不创建实施草稿。

## 4. 后继假定接口（非当前事实）

```text
核心/权威冻结材料.数据.h 是九段 DTO 的唯一稳定所有者。
一个不可变权威冻结材料值绑定非零运行期上下文身份、事务域、冻结许可身份和快照生成身份。
段 1—9 均完整；段 5 所有者集合与绑定集合精确相等；段 9 来自新概念活动所有者。
DTO 不含指针、锁、span、对象地址或可写引用，可逐值精确比较。
```

任何实际差异必须退回设计，不得让编码器“补齐”上游字段。

## 5. 后段职责

未来正式版本只允许：

```text
HYSNAP01 总头、段表和九段显式小端规范编码。
按稳定完整身份规范排序；单 / 多线程同材料字节完全一致。
当前格式严格解码为值式 DTO，并与输入逐字段比较；不构造仓库。
CREATE_NEW 临时文件、写穿、严格自读、同卷替换和最终严格读回。
路径 / 版本 / 预算 / 空间等逻辑内拒绝，以及写入方内部不一致具名追根因。
生产真模块、自检真模块，以及入口 Debug 路径的最小 import、结果槽、阶段登记和具名汇总；通用 `自检.运行器.ixx` 保持不变。
```

禁止生产旧栈迁移、仓库 / 侧表导出、冻结编排、外部恢复、指定身份导入、线程、SQL、面板、事件重放、在线热替换、压缩、加密、断电耐久和 O1 生产接线。

## 6. 文件分类门禁（非许可）

后继正式版本必须按 #307 实际产物逐文件确认，至少满足：

| 责任 | 分类 | 工程登记 / 调用 | 约束 |
| --- | --- | --- | --- |
| 九段 DTO | 复用 `核心/权威冻结材料.数据.h` | `ClInclude` / `#include` | #226 不重复声明 |
| 规范编码与严格值式自读 | `功能.分类.ixx` 生产真模块 | `ClCompile` / `import` | 不声明仓库 / 服务 / 上下文所有权 |
| 原子文件发布 | `功能.分类.ixx` 生产真模块 | `ClCompile` / `import` | 只消费规范字节和值式结果 |
| 发布验收 | `自检.分类.ixx` 自检真模块 | `ClCompile` / `import` | 不进入生产依赖方向 |

当前 v0.4 不允许修改任何 C++、工程或实施记录；未来文件名、模块名、工程顺序、入口最小接线和旧 `仓库快照服务.h` 拒绝壳处理方式必须在 #307 后续修订中唯一裁决，且不得把专项正文或专项登记所有权迁入通用运行器。

## 7. 非成功返回二分

| 场景 | 分类 | 收口 |
| --- | --- | --- |
| #296—#307 任一未完成 | 依赖门控 | #226 保持禁止执行 |
| #307 DTO / 段 / 所有者与第 4 节不一致 | 接口漂移 | 退回设计修订，不扩大文件 |
| 路径、版本、生成序号或预算无效 | 后继逻辑内返回 | 不创建候选、不改变旧正式文件 |
| 临时文件创建、权限、空间或提交点前替换失败 | 后继逻辑内返回 | 清理本次候选，旧文件保持 |
| 当前编码器无法严格读回自己的规范字节 | 后继追根因 | 不替换旧文件，停止恢复依赖 |
| 提交后最终严格读回不一致 | 后继追根因 | 不宣称发布成功 |

## 8. 恢复执行资格

设计角色只有读取 #307 正式提交并逐签名确认以下事实后，才可生成 #226 后续可执行版本：九段 DTO 唯一所有者、生产模块名、工程依赖顺序、规范编码 / 自读输入、文件发布结果类型、旧拒绝壳去留、自检真模块承载、入口 Debug 最小接线和通用运行器零差异。修订后仍须重新派发；本 v0.4 永不直接转为执行。

## 9. 验证与完成边界

本设计版本只接受 `python .\tools\check_specs.py --strict`、Mermaid 同源检查和 `git diff --check`；不构建、不运行、不登记阶段。

本 v0.4 只证明 #226 已绑定 #296—#309 的正式路由、#304—#307 的阶段登记所有权和后段职责。不得声明 HYSNAP01 已实现、冻结材料已形成、生产入口已迁移、快照可恢复、断电耐久或旧持久化能力已迁移。
