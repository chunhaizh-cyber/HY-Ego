# TASK-RESULT-S1 任务结果完成与需求独立结算代码实施切片 v0.1

日期：2026-07-11

队列编号：#225

设计队列：DQ-117

状态：已生成 / 已登记依赖门控待执行 / 直接前置 #224、#222 / 完成后提供 #215 精确前置 / 结果与结算分界固定 / 实际接口复核后实施 / 未改 C++ / 未构建 / 未运行程序

## 1. 目标

把 #224 强类型非权威回执经正式领域复核转为任务结果候选，结构化比较任务目标与实际状态，目标达成时原子提交任务结果和完成，再独立调用需求结算：

```text
强类型回执
-> 正式重读任务 / 方法 / 动作 / 状态 / 动态
-> 状态服务比较目标与实际
-> 唯一任务结果 + 执行中到已完成
-> 独立需求结算与可重试待结算材料
```

本计划形成 #215 命名任务结果与需求结算可复用的通用契约；不实现命名专用绑定，不修改方法召回或选择。

## 2. 依据与范围一致性

```text
流程图/20260711_任务生命周期筹办通用执行与结果结算流程图_v0.1.md
流程图/20260711_任务生命周期筹办通用执行与结果结算流程图_v0.1.html
规范/详细设计/任务生命周期筹办通用执行与结果结算详细设计.md
规范/需求结算记录规范.md
规范/详细设计/需求结算记录详细设计.md
规范/详细设计/任务回执实际结果状态结果回写代码逻辑详细设计.md
```

流程图 S5、详细设计第 9 节和本计划均固定回执非权威、结构化目标比较、任务完成与需求结算两个边界。

## 3. 依赖门控

### 3.1 前置计划

```text
直接前置：#224 必须形成完整强类型回执和真实状态 / 动态证据。
生命周期前置：#222 必须形成执行中当前态和专用完成入口预留边界。
结构前置：#217 必须继续提供结果 + 生命周期同域独占提交能力。
后续关系：本计划完成后 #215 的通用任务结果接口门禁满足；#215 仍须等待 #214。
```

### 3.2 已验证当前事实

```text
上行桥能重读任务、方法、动作、状态和动态，但只提交非权威治理消息。
任务服务有一次性实际结果关系和完成状态入口，但不比较目标 / 实际或合法当前态。
需求服务已有正式结算入口，要求来源任务、目标 / 实际状态、结果、场景、主体和动作动态。
状态服务可读取结构化状态材料，但当前没有正式目标 / 实际比较结果 DTO。
任务完成与需求结算在固定安全根样例中由入口显式串行，不是通用路由。
```

### 3.3 假定接口契约

```text
#224 回执包含完整任务、生命周期、选择、方法、动作、实际状态、动态、主体、场景和来源版本。
#222 提供执行中到已完成的专用结果入口，并拒绝失败 / 取消终态复活。
#217 可把唯一任务实际结果与生命周期换代放入同域独占可见边界。
状态服务可新增不写结构的目标 / 实际比较入口。
需求服务现有正式结算可接收任务完成材料并保持幂等读回。
```

### 3.4 预期正式产物

```text
海中鱼巣/领域/任务结果服务.h（若项目模式要求可有对应 .cpp）。
海中鱼巣/线程/任务结果治理路由.ixx。
状态目标比较结果、任务结果提交材料、任务完成材料和待结算材料 DTO。
幂等正式任务结果 / 完成提交和独立需求结算调用链。
#215 可复用的通用结果、完成、结算函数与失败收口。
实施记录/20260711_TASK-RESULT-S1_任务结果完成与需求独立结算代码实施_Codex断点清单.md
```

## 4. 实施步骤

### S0：实际接口复核

读取 #224 强类型回执、#222 完成边界、#217 同域令牌和当前任务 / 需求 / 状态 / 方法 / 动态服务。记录现有任务结果、完成和需求结算入口是否会产生部分结构；若无法在允许范围内形成提交前完整准备，停止并退回设计。

### S1：正式结果路由重读

上行桥保持只读。新增结果治理路由或服务接收强类型回执后，再次逐项重读当前任务版本、选择、方法登记 / 规格、动作入口、实际状态、动作动态、主体和场景。过期、允许拒绝、精确重复在写前返回；同当前版本结构矛盾追根因。

### S2：状态结构化比较

状态服务新增只读比较结果，区分已达成、未达成、材料不足和内部不一致。比较使用目标 / 实际状态定义、主体与场景，不让任务服务通过裸 I64 相等直接裁决业务目标。

### S3：任务结果与完成

未达成时保留状态 / 动态证据，按 #222 迁移等待中或待重筹办，不写最终结果。已达成时取得 #217 同域独占许可，重读执行中当前态，写唯一实际结果并经专用入口迁移到已完成；完整读回后才返回完成材料。重复同结果幂等，冲突结果追根因。

### S4：独立需求结算

任务完成读回后再调用需求服务。结算允许拒绝或暂不可执行时，保留任务已完成，返回可重试待结算材料；不得回滚动作、实际状态、任务结果或生命周期。失败 / 取消任务不得结算为已满足。

### S5：#215 接口与治理收口

固定 #215 可复用的结果 / 完成 / 结算接口，补齐 A01-A14、迟到 / 重复 / 冲突 / 未达成 / 结算重试 / 并发测试，完成规范、工程登记、实施记录、归档、治理、提交和推送。

## 5. 允许文件

```text
海中鱼巣/领域/任务结果服务.h
海中鱼巣/领域/任务结果服务.cpp
海中鱼巣/领域/任务服务.h
海中鱼巣/领域/需求服务.h
海中鱼巣/领域/状态服务.h
海中鱼巣/领域/方法服务.h
海中鱼巣/领域/动态服务.h
海中鱼巣/线程/任务结果治理路由.ixx
海中鱼巣/线程/任务管理上行桥.ixx
海中鱼巣/线程/任务结果回执协议.ixx
海中鱼巣/入口.cpp
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
规范/任务系统规范.md
规范/需求结算记录规范.md
规范/多线程防锁机制规范.md
规范/领域服务授权写入规范.md
计划/20260711_TASK-RESULT-S1_任务结果完成与需求独立结算代码实施切片_v0.1.md
计划/已完成计划/20260711_TASK-RESULT-S1_任务结果完成与需求独立结算代码实施切片_v0.1.md
实施记录/20260711_TASK-RESULT-S1_任务结果完成与需求独立结算代码实施_Codex断点清单.md
计划/计划索引.md
项目记忆/Codex任务队列.md
项目记忆/当前状态.md
项目记忆/实施记录.md
项目记忆/验收记录.md
项目记忆/决策记录.md
项目记忆/待确认问题.md
```

新 `.cpp` 仅在项目编译模式需要时创建。`任务管理上行桥.ixx` 只允许保持 / 扩展只读投递材料，不得写任务或需求。

## 6. 禁止文件与排除项

```text
海中鱼巣/核心/**
海中鱼巣/界面/**
海中鱼巣/领域/方法召回服务.h
海中鱼巣/领域/方法召回索引.h
海中鱼巣/线程/任务管理线程.ixx
海中鱼巣/线程/任务工作线程.ixx
数据库、SQL、日志、统计学习、D455、体素和外设文件
```

禁止修改方法召回 / 选择、线程执行、回执生产、命名绑定、方法学习、因果稳定性、结果回滚或把显示 / 日志当结算事实。

## 7. 入口拒绝

```text
回执协议版本、任务 / 需求 / 生命周期 / 选择 / 方法 / 动作 / 状态 / 动态句柄无效。
任务不是回执对应的当前执行中版本，或已经失败 / 取消。
任务来源需求、目标状态、主体或场景与回执不一致。
当前选择、方法登记 / 规格、动作入口或动作动态来源版本漂移。
实际状态不属于当前主体 / 场景，或动作动态未完整读回。
状态比较材料不足、回执过期、重复冲突或已有不同任务结果。
需求结算证据与任务完成材料不一致。
```

## 8. 失败收口

| 场景 | 分类 | 收口 |
| --- | --- | --- |
| #224 / #222 / #217 接口漂移 | 依赖门控返回 | 不改代码，退回设计并保持 #215 门控 |
| 回执过期、允许拒绝或精确重复 | 逻辑内返回 | 不新增任务结果或版本 |
| 目标未达成 | 设计内分支 | 保留状态 / 动态，迁移等待 / 待重筹办 |
| 同当前版本回执与权威材料矛盾 | 追根因解决 | 不落账，不由协议兜底 |
| 结果 / 完成写后读回不一致 | 追根因解决 | 停止需求结算，不伪造完成材料 |
| 需求结算允许拒绝 / 暂不可执行 | 逻辑内返回 | 保留任务已完成，返回待结算材料 |
| 需求结算写后结构不一致 | 追根因解决 | 不回滚任务结果或动作事实，停止后续依赖 |

## 9. 验收

| 编号 | 验收项 |
| --- | --- |
| TASK-RESULT-S1-A01 | 上行桥继续只读，正式结果入口再次重读全部权威材料 |
| TASK-RESULT-S1-A02 | 过期、允许拒绝和精确重复回执不新增任务结果或生命周期版本 |
| TASK-RESULT-S1-A03 | 状态服务结构化比较目标与实际，任务服务不直接解释裸 I64 |
| TASK-RESULT-S1-A04 | 目标未达成时保留状态 / 动态证据，不写最终结果或完成 |
| TASK-RESULT-S1-A05 | 目标达成时唯一实际结果与执行中到已完成在 #217 同域独占许可中提交 |
| TASK-RESULT-S1-A06 | 结果、完成关系和生命周期历史全部读回一致后才返回完成材料 |
| TASK-RESULT-S1-A07 | 精确重复结果幂等，同任务同版本冲突结果进入追根因 |
| TASK-RESULT-S1-A08 | 已失败或已取消任务不能由迟到回执改成已完成 |
| TASK-RESULT-S1-A09 | 当前版本不变但回执与权威结构矛盾进入追根因，不由协议兜底 |
| TASK-RESULT-S1-A10 | 需求结算在任务完成后独立调用，不与动作 / 结果提交合并回滚 |
| TASK-RESULT-S1-A11 | 结算允许拒绝时任务保持已完成并返回可重试待结算材料 |
| TASK-RESULT-S1-A12 | 失败 / 取消任务不会被需求服务解释为目标已满足 |
| TASK-RESULT-S1-A13 | 形成 #215 可复用的幂等任务结果、完成和正式需求结算接口 |
| TASK-RESULT-S1-A14 | TASK、DEMAND、METHOD、SELF-GOV、PANEL、固定安全根和 S1-S4 回归通过 |

## 10. 验证

```powershell
msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m
py -3.14 .\tools\check_specs.py
git diff --check
```

必须运行完整 `--self-test-exit`、A01-A14、重复 / 迟到 / 冲突 / 未达成 / 失败终态 / 取消终态 / 结算重试 / 同结果并发至少 50 次，以及最终连续 20 轮完整自检。静态扫描确认线程写事实、召回选择、结果回滚、日志 / 显示结算和方法学习 0 新增命中。

## 11. 实际接口复核与漂移退回

执行前记录 #224 回执实际字段、#222 完成专用入口、#217 同域令牌、状态比较与需求结算现有函数。若实际任务结果和生命周期无法形成统一可见提交，目标比较需要未设计业务规则，或需求结算必须和任务完成做跨服务回滚，停止并退回设计；不得在路由中复制任务状态机或用裸 I64 / bool 代替。

## 12. 完成边界

完成后只可声明通用任务结果复核、结构化目标比较、唯一结果与完成提交、独立可重试需求结算第一版已实现，并为 #215 提供精确前置。不得声明命名完成、方法学习、任意动作执行、崩溃恢复、跨重启恰好一次或完整自我循环完成。
