# 语言概念聚合与生产装配代码实施切片 v0.1

日期：2026-08-12

计划身份：`CONCEPT-SPACE-STRUCTURE-CRUD-REBUILD-A1`

状态：`待激活`；设计完成，不等于实施完成

## 1. 目标与依赖

新建零写入 `L2语言概念结构聚合服务`，透明引用材料、语言、概念和概念名称四个唯一服务，并在普通应用最终装配。依赖本链 M1、L1、C1—C4、N1 全部真实结果，以及现有六类世界 `L2-STRUCTURE-CRUD-REBUILD-A1` 最终结果；两个聚合并列，不互相包含。

## 2. 允许范围

新增 `海中鱼巣/领域/服务.L2语言概念结构聚合.ixx`；修改普通应用、根工程 / filters；新增三份 `L2语言概念聚合装配参数验证` 临时文件和两份 CONCEPT-A1 记录，共九路径。禁止修改四服务、现有 `服务.L2结构聚合.ixx`、启动入口或业务消费者。

## 3. 公开合同

聚合类型不可默认构造、复制或移动，私有成员恰为四个非 owning 引用，构造 `noexcept`，提供四组 const / 非 const 一行 getter。禁止复制 CRUD、模板 dispatch、服务枚举、字符串路由、L1 import、owner、事务、锁、缓存或状态。

普通应用在四服务形成后构造唯一聚合，追加一个精确失败状态、一个 owning 成员和 const / 非 const getter；四服务既有直接 getter 不删除。聚合先于所引用服务析构。最终状态值和 provider ABI 在激活时绑定真实前置结果，不由当前设计猜数值。

## 4. 验证与声明

经正式普通应用构造，核对聚合和直接 getter 四服务逐一同址、const / 非 const 同址、重复同址，并对每服务调用一个只读公开入口证明透明转发。根工程双配置与专项通过。不得声明启动入口消费、概念业务闭环或现有六世界聚合职责扩大。
