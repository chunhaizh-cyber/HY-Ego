# OBSERVE-P2 扫描跟踪状态动态提交与任务回流代码实施切片 v0.2

编号：OBSERVE-P2；路由：#370 / DQ-262；合同：PER-C10 / ABI 2；日期：2026-07-24

设计状态：终审 PASS。当前状态、版本和 plan blob 唯一读取 `计划/计划索引.md`。

## 合同与边界

- 依据：1160、1170、4210、4220；`规范/详细设计/观察世界事实与体素融合详细设计.md` v0.2；`流程图/20260724_OBSERVE-VOXEL_观察世界事实与体素融合施工流程图_v0.2.md`；`规范/详细设计/函数结构知识图谱/20260724_PERCEPTION-D0_D455观察体素生产闭环函数结构知识图谱_v0.2.md`。
- 已实现事实：状态 / 动态现行入口和 SELF-C4 实现尚需节点直接迁移 / 适配；本计划消费预冻结端口。
- 待实现：`观察状态动态提交ABI版本 = 2`；状态 / 动态规格、事实读回、任务结果回流；`提交扫描跟踪事实`。
- 首次扫描只建立当前状态基准；只有可比基准和可验证差异成立才提交动态。世界事实发布后才回流任务，两者不得互相冒充。

## 文件所有权

允许：`海中鱼巣/领域/合同.观察状态动态提交.ixx`、`海中鱼巣/领域/组合.观察状态动态提交.ixx`、`海中鱼巣/领域/自检.观察状态动态提交.ixx`、`实施记录/20260724_OBSERVE-P2_PER-C10状态动态回流代码实施_Codex断点清单.md`。

禁止：状态 / 动态现行提供者、SELF-C4 提供者、C1—C9 / C11—C13、工程、入口、运行器和设计文件。

## 施工与 S0

1. S0 核对 plan blob、C8 结果合同、状态 / 动态事实提交端口、SELF-C4 回流端口和三个代码文件。
2. S1 建立精确规格、读回和结果 DTO；字段包含主体、特征、值、场景、时间、来源，动态另含被改变目标和前后状态。
3. S2 实现首次基准与变化提交分支；事实完整读回后才调用任务回流。
4. S3 自检源码覆盖首次基准、无变化、差异不足、主体未知、版本漂移、事实成功但回流失败。
5. S4 核对白名单、ABI、禁止第二事实仓和 diff。
6. S5 只提交允许代码与专属记录，推送执行通道分支并恢复 clean。

## 机械验证命令

S0 记录 `$计划段起点 = (git rev-parse HEAD).Trim()`；S4 的 `git diff --check` 与 `git diff --cached --check` 均须退出 0。S5 后运行：

```powershell
$期望 = @('海中鱼巣/领域/合同.观察状态动态提交.ixx','海中鱼巣/领域/组合.观察状态动态提交.ixx','海中鱼巣/领域/自检.观察状态动态提交.ixx','实施记录/20260724_OBSERVE-P2_PER-C10状态动态回流代码实施_Codex断点清单.md') | Sort-Object
$实际 = @(git -c core.quotepath=false diff --name-only "$计划段起点..HEAD") | Sort-Object
if (Compare-Object $期望 $实际) { exit 1 }
if ((@(rg -n "观察状态动态提交ABI版本\s*=\s*2" '海中鱼巣/领域/合同.观察状态动态提交.ixx')).Count -ne 1) { exit 1 }
if ((@(rg -n "提交扫描跟踪事实" '海中鱼巣/领域/组合.观察状态动态提交.ixx')).Count -lt 1) { exit 1 }
```

全部命令须退出 0；ABI 精确 1 处，提交入口至少 1 处。

## 验证与收口

- 静态 / 专项源码验证，不构建、不运行。
- C8 / 事实 / SELF-C4 端口漂移返回 `DRIFT-PER-CONTRACT`；现行事实入口不能适配返回 `DRIFT-PER-CURRENT-INTERFACE`；需要表外文件返回 `DRIFT-PER-FILE-OWNERSHIP`；反向依赖返回 `DRIFT-PER-IMPORT-CYCLE`。
- 前置通过后状态 / 动态不可读，或回流引用指向不存在事实，为 `FAIL-PER-INTERNAL-READBACK`；其它验证失败为 `FAIL-PER-VALIDATION`；已发布事实不得因回流失败被伪回滚。
- 完成只证明 PER-C10 候选，不证明事实提供者、任务回流或自我循环已接通。
