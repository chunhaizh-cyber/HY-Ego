# WORLD-TREE-P1B-M 关系目标反向源同许可投影代码实施切片 v0.1

计划身份：WORLD-TREE-P1B-M

版本：v0.1

创建侧代码基线：`7ba6e151eec74cb87ed02c69720596e1838acad9`

## 1. 目标、依赖和完成条件

在既有 `节点直接结构查询服务` 中增加一次共享许可的目标角色反向源投影，使 P1B-Q 能从已知主体/场景发现关系19/20源，并获得每个源全部同类型关系及源当前类型化值。

完成条件：真实函数按稳定身份、单代次形成完整业务中性投影；全部成功/空/拒绝/异常分支闭合；零机器事实变化；顺序170自检原位扩充且顶层总数不变。

具名依赖：

1. WORLD-TREE-P1A2 v0.3 结果提交 `65166e1ce979841502f8420204f6163f588c35e8` 已进入正式代码。
2. WORLD-TREE-P1A2G v0.1 必须先有真实成功代码并释放 `节点直接结构合同.数据.h`、`服务.节点直接结构.ixx`、`自检.节点直接身份结构写入.ixx`；本计划复用其唯一关系见证和类型化值完整性实现。

P1A2G只有计划/WIP/消息或实际助手签名不同，本计划保持待激活，S0零代码修改返回 `DRIFT-WORLD-TREE-P1B-M-P1A2G-PROVIDER`。本计划不依赖 P1A2Q、P1A3F、P1BF、COMPARE-F、P1B-F 或 SQL。

唯一施工依据：

```text
规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md v1.2 §6.1
规范/4020_子规范_领域类型化数据记录与组合读取投影边界.md
规范/详细设计/20260803_WORLD-TREE-P1B-M_关系目标反向源同许可投影详细设计_v0.1.md
规范/详细设计/函数结构知识图谱/20260803_WORLD-TREE-P1B-M_关系目标反向源同许可投影函数结构知识图谱_v0.1.md
流程图/20260803_节点直接结构查询服务读取目标角色命中源的当前关系与类型化值_函数流程图_v0.1.md
```

## 2. 允许文件

```text
海中鱼巣/核心/节点直接结构合同.数据.h
海中鱼巣/核心/服务.节点直接结构.ixx
海中鱼巣/核心/自检.节点直接身份结构写入.ixx
施工记录/20260803_WORLD-TREE-P1B-M_施工记录.md
验证记录/20260803_WORLD-TREE-P1B-M_验证记录.md
```

其它路径均禁止。不修改工程、启动、仓库、事务域、执行器、数据操作、SQL/材料、领域服务、规范、设计、其它计划或索引。

## 3. 禁止范围

- 禁止修改 P1A2G DTO、根函数或助手语义，禁止给它增加方向参数/重载。
- 禁止用多个公开读取函数事后拼代次；禁止返回内部句柄、许可、仓库记录或引用。
- 禁止在 L1 解释关系19/20角色、基数、状态/动态种类、发生时间、特征值或领域排序。
- 禁止读取索引、类型合同、SQL、材料、旧栈、世界结构、特征、比较、状态或动态服务。
- 禁止写事实、取得独占许可、提交事务、分配身份、修改顶层自检数量或建立新自检槽位。

## 4. 机械施工步骤

1. S0核对精确 plan blob、P1A2结果、P1A2G真实函数/两个私有助手、三共享文件释放、顺序170，并把届时正式顶层自检数量记录为 `N`；不得从本计划正文预设固定数量。
2. 在共享合同头逐字追加详细设计 §3 DTO；不得改已有字段或稳定值。
3. 在既有类追加唯一根函数，严格按函数图和详细设计 §5—§7实现；不新增生产构造或成员。
4. 复用 P1A2G 的 `形成当前关系见证_` 与 `类型化值记录完整_`；不得复制算法。
5. 在既有专项宏下增加本根独立一次性资源失败标志；原位扩充 `WORLD-TREE-P1A2-S01` 顺序170；施工后顶层自检数量仍须等于 S0 记录的 `N`。
6. 形成专属施工/验证记录，完成全部构建、自检、AST和零副作用矩阵。

## 5. 验证和失败收口

```powershell
git diff --check
python .\tools\check_specs.py --strict
msbuild .\海中鱼巣.vcxproj /m /p:Configuration=Debug /p:Platform=x64
msbuild .\海中鱼巣.vcxproj /m /p:Configuration=Release /p:Platform=x64
.\x64\Debug\海中鱼巣.exe --self-test-exit
```

使用现行 Clang AST 工具证明：根函数唯一；`取得读取许可`恰一次；目标关系组/源关系组/源值组调用方向正确；索引、类型合同、SQL、材料、旧栈及 L2 调用边为0。静态扫描生产函数不得出现主体、场景、特征、前/后状态或动态种类的业务分支词。

自检至少覆盖详细设计 §8。调用前后六仓、幂等账、持久侧账和发布代次逐值相等；关系19/20、零命中、空值组、未找到、漂移、许可、资源、内部矛盾和稳定排序全部有具名验收。

P1A2G助手、当前类成员、正式关系仓结果或完整自检基线漂移时停止，不复制助手或建立兼容壳。任一验证失败保留 WIP，不声明完成。

## 6. 完成声明边界

完成只声明 P1B-M 的 L1 目标角色反向源同许可投影通过本计划矩阵。不得声明关系19/20领域完整、状态/动态读取、P1B发布、世界快照、生产接线或服务验收完成。
