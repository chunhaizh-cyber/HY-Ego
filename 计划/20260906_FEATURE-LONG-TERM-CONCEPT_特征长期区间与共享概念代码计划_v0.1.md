# FEATURE-LONG-TERM-CONCEPT 特征长期区间与共享概念代码计划

日期：20260906
版本：v0.1
计划身份：FEATURE-LONG-TERM-CONCEPT
本版本执行目标：第一片A，特征长期区间提供者；当前状态只由计划索引维护。

## 1. 正式依据与完成目标

采用[详细设计](../规范/详细设计/20260906_FEATURE-LONG-TERM-CONCEPT_特征长期区间与共享概念详细设计_v0.1.md) v0.1，精确blob `cfa3b843cc77b8b6a2ebcfddca1f01333b3c481f`，仅第1—5节第一片施工合同；第6节后续路线不是本次代码白名单或已冻结API。正式语义依详细设计列出的九份新规范。用户明确先列文件 / 函数、再依次实施，并要求代码必须编译且运行；早期不验证要求不适用于本计划。

第一片完成真实类型完整域、二值 / I64差异、静止多帧校正、固定首代表长期区间、全部匹配 / 旧形态复用和正式跨进程恢复。独立F消费者只能证明特征结构上下文，不能替应用证明E成员归属。FC身份去宿主、取消成熟代码、并集上位、名称及EC/DC/scene适配仍须在A结果后接续，不能把A完成当整体目标完成。

## 2. 起点与精确允许范围

起点HEAD `c4a20f6d4d62e63d77dd7cfd9decdd6c9add9802`。唯一生产输入 `海中鱼巣/领域/数据服务.特征类.ixx` blob `534cb63597968cd859bdd224e7f8f37e7fa0acd1`，含已发布普通F历史原key和具名I64派生合同，不覆盖回旧版本。

只允许以下三文件：

1. `海中鱼巣/领域/数据服务.特征类.ixx`。
2. `施工记录/20260906_FEATURE-LONG-TERM-CONCEPT_特征长期区间提供者施工记录_v0.1.md`。
3. `验证记录/20260906_FEATURE-LONG-TERM-CONCEPT_特征长期区间提供者验证记录_v0.1.md`。

禁止修改L1、新值、其它world、conceptprovider、FC/EC/DC/scene、project、filters、任何全局根及旧L2；不得新增模块、私有持久仓或相机接线。现九份概念/应用/工程/旧记录WIP各有原所有者，本次只读保护，不带入提交。根应用当前本地blob FC `5c81203eff3fa1f667d116edb6e597228063b296`、EC `3992422e3f297769609402c3ec8c858b751a8b1b`、DC `18e5e98484d8ed44c7ca0004249082d1382c246b` 保持。

## 3. 复用与准确施工次序

先S0核对实际F入口、所有者、原key和值历史接口。新材料历史只复用新值`按结点读取不可变材料历史事实` / `读取不可变材料历史事实`；不得用只读当前引用校验替代H，也不得错value后fallback。F既有普通历史及标量输出FT自槽拒绝必须保留。

顺序完成：

1. 新具名DTO、状态、18角色外部交付构造与真实类型 / 输出FT登记。旧构造未启用新方法，普通和既有派生能力不变。
2. 二值U64组解析、整数边界距离、安全I64绝对差和全样本对校正。算法v1与旧形状比较入口隔离，不加旧注册允许项。
3. 跟踪 / 区间 / 观察记录、全部匹配、双重去重、固定代表与真实统计，按照详细设计固定写集和来源引用状态原子提交。
4. 原key优先规范写集恢复、只读完整历史组、source释放、预算及发布失败分账。多校正读取完整组，业务不得取最新代替显式身份。
5. Debug / Release实际构建链接、独立进程消费、原能力回归，记录准确完成与未覆盖边界。

新增12个public成员方法：登记特征类型域、读取特征类型域、比较长期特征、建立特征校正、读取特征校正、建立特征跟踪、读取特征跟踪、积累特征观察、匹配长期区间、读取长期区间、读取长期观察、释放长期来源引用；完整请求 / 结果与永久结构见设计3—4节。私有辅助可按职责拆分，不新增泛型ABI或回调。构造只校验外部18类型 / 锚点，零本体根建立。

## 4. 验证资源、命令与证据

资源根 `D:/TEMP/海中鱼巣/FEATURE-LONG-TERM-CONCEPT/01a07099-20260906-provider`，Debug/Release隔离src/build/data，实际执行前登记唯一资源。消费者和生成的局部.vcxproj只在该根，不改变canonical工程。使用当前MSVC C++模块工具链和实际新L1 / 值 / F依赖；最小8模块及独立consumer，若实际装配依赖更多须如实补编并记清单。

每配置建立快照与局部工程后运行等价明确命令：`MSBuild.exe <局部工程> /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m:1`、Release同形；每配置消费者分别运行`seed <独立data目录>`、`continue <同data目录>`、`audit <同data目录>`，每次是新进程，全部exit0且设计5节真实事实断言通过。验证记录填写实际绝对命令 / 工具版本，不能用这里的模板命令冒充已运行。

覆盖设计5节全部矩阵：材料 / 类型 / 输出FT、固定整数数值、校正反序和极值、全区间多命中、慢变和旧形态返回、同义 / 异义原key、预算失败、正常源退出、历史清理保护与跨进程统计一致。保存所有输入SHA、结果和权威G / 原key证据。资源 / 尾G竞态等不可稳定注入的分支NOT_RUN，不将静态审阅当动态通过。

## 5. 失败与完成声明

错误不能当零差异、零区间或零历史。发布后失败保留原完整请求 / 首次H / 可能发布；不换key补偿。纯代码错误在本片修复；实际DTO / owner / 写集漂移具名退回原目标，不自行扩文件。

本计划关闭需要F源码、两专属记录精确发布及Debug/Release真实编译运行证据；只有规范发布不能关闭。正式集成验收、主程序 / 相机接线、共享FC及应用尚未完成必须明确。A代码结果回报root后沿同一用户目标冻结B，最多一份实际可执行计划，不新建远端下游库存。
