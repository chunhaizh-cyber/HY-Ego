# DATA-L2-FINAL-CLOSURE-P04.2 世界七族当前结构身份与历史见证迁移路线输入 v0.1

日期：2026-09-02

材料身份：`DATA-L2-FINAL-CLOSURE-P04.2-WORLD-SEVEN-MIGRATION-ROUTE-INPUT`

代码 / 调用观察基线（形成时）：`1653e6f47d000cd0fb8085c3a5d3a9977f3780ea`；下列行号和调用分类只对该提交树有效。本轮 live S0 的当前 `main == origin/main == ccf90b034d1fa47cd2d9b38ed3fbe7c0daf92552`；`1653..ccf` 未修改 L2 代码、但已发布 C031 正式材料，故 1653 不是当前正式基线，未来仍须重新 S0。

状态：用户已确认 P04.2-A。本文件是未来重新设计时的迁移路线输入，**不是代码实施计划**、不是计划索引项、不是待激活库存、不是施工许可；当前静态复核结论为 `DRIFT`。

关联详细设计：`规范/详细设计/20260902_DATA-L2-FINAL-CLOSURE-P04.2_七族当前结构身份与历史见证迁移详细设计_v0.1.md`。

## 1. 可进入未来计划前的硬门禁

| 门禁 | 必须满足的事实 | 本轮状态 |
| --- | --- | --- |
| P04.1.1 前置 | 状态、动态、因果三个既有历史成功谓词已经单独发布；不得同 P04.2 合并或并行修改共同头文件链 | 未登记 / 未施工 |
| 正式合同 | 合法治理方已按 `0050 -> 4030 -> 4230 -> 4240 -> 0300 -> 规范目录` 同步并发布当前 / 适用历史见证、失败、旧入口退役、P08 隔离、L1 / 4070 旧格式边界 | ccf 已发布 C031 相关正式材料，但 P04.2 ABI 尚未同步 |
| 当前基线 | `main`、`origin/main`、index、目标规范 blob、目标头 / 服务、消费者和 WIP 全部重新 S0 | 只能使用本轮 HEAD 证据 |
| 执行资格 | 当前计划索引为空，且 P04.2-A 未登记；未来只有在形成一份合法计划、活动代码段、构建 / 验证资源和发布租约均可用时才可能施工 | 当前不满足；空队列不构成施工许可 |
| 当前消费者 | 所有表 2 的当前调用均有确切迁移归属；所有表 3 的历史调用均有显式见证归属 | 仅完成静态清单 |
| 异主所有权 | `服务.L2需求结构.ixx`、`装配.普通应用.ixx` 等异主 WIP 已完成、交接或被合法排除；不允许 DATA-L2 切片覆盖 | 当前不满足 |

任一门禁缺失时，不得以“已有详细设计”推断可以创建计划或修改一部分消费者。

## 2. 当前调用完整白名单：必须逐处按类别分流

| 所在文件与 HEAD 位置 | 当前调用 | 未来分流要求 |
| --- | --- | --- |
| `海中鱼巣/领域/服务.L2场景结构.ixx:1663, 4747, 4786, 5218` | 存在 / 场景当前读取 | 改用相应 `读取当前{族}结构身份` |
| `海中鱼巣/领域/服务.L2场景结构.ixx:4613, 4707` | 按 `L2读取类别` 的场景 / 存在读取 | 逐分支拆为当前结构身份或历史见证，禁止机械替换 |
| `海中鱼巣/领域/服务.L2存在结构.ixx:2348` | 存在当前读取 | 改用 `读取当前存在结构身份` |
| `海中鱼巣/领域/服务.L2特征结构.ixx:3848, 4277, 4279, 4578, 4716, 4810, 4878, 5168, 5263` | 存在、特征定义、特征实例当前读取 | 分别改用对应当前结构身份入口 |
| `海中鱼巣/领域/服务.L2特征结构.ixx:3350, 4132` | 特征定义 / 实例按类别读取 | 逐分支拆为当前结构身份或历史见证 |
| `海中鱼巣/领域/服务.L2状态结构.ixx:2431, 2931, 3255, 3290, 3627, 3629, 3631, 4025, 4028, 6412, 6544, 6546` | 存在、特征实例、状态当前读取 | 改用相应当前入口；`2931` 虽包在历史状态选择外层，仍要求实例当下有效，不得误改为历史见证 |
| `海中鱼巣/领域/服务.L2动态结构.ixx:1475, 1483, 1491, 1726, 1750, 1774, 2156, 2159, 2161, 2655, 2724, 2726, 2728, 2807, 2809, 2811, 3140` | 存在、状态、动态当前读取 | 改用对应当前入口 |
| `海中鱼巣/领域/服务.L2因果结构.ixx:760, 765, 803, 830, 973, 1459, 1465` | 状态当前读取 | 改用 `读取当前状态结构身份` |
| `海中鱼巣/领域/服务.L2状态动态原子发布.ixx:708, 710, 712` | 主体存在、来源存在、场景身份 | `692–694` 已区分首次候选 `Gnow == G0` 的当前分支和非首次候选的 H 重放；必须保留此分流，不能把 replay 误改为当前 |
| `海中鱼巣/领域/服务.L2需求结构.ixx:2946, 2957` | 存在、特征定义当前读取 | 只有新 provider 同 G0 证明四项均当前后，才由该文件所有者裁决是否删除直接 `节点生命周期.退出事实代次` 防御；本工作区该文件是异主 dirty WIP，不可直接改 |

这张表包括 HEAD 中全部七族旧身份来源的当前 / 按类别调用。未来扫描若出现新调用点，说明基线已漂移，必须停止受影响范围、更新清单和重新 S0，而不是遗漏后继续施工。

## 3. 显式历史见证保留清单

以下位置是本基线已核实的历史见证 / 重放使用点，不应迁移到瘦当前 DTO。只有各族已有或未来经正式明确的这些历史见证及专属退出 / 恢复 / 审计路径，才使用改名后的 `读取{族}身份来源见证`、非零 `H` 和完整共同见证，且成功满足 `H == 结果头.事实截止代次`；普通退出结果不因此被要求统一携带共同 DTO：

| 所在文件 | 已核实历史 / 重放位置 |
| --- | --- |
| `服务.L2场景结构.ixx` | 2676，以及 4613 / 4707 的历史支 |
| `服务.L2特征结构.ixx` | 3350 / 4132 的历史支，3943 / 3945、4420 / 4422、5129 |
| `服务.L2状态结构.ixx` | 6377 / 6379 / 6561 |
| `服务.L2动态结构.ixx` | 2626 / 2628 / 2630、2839、2980 / 2983 / 2986 |
| `服务.L2因果结构.ixx` | 1499、1562 / 1568 |
| `服务.L2状态动态原子发布.ixx` | 非首次候选的 H 重放支 |

P04.1.1 是状态、动态、因果的必要先行修正；只有其发布后，以上三族历史见证的成功谓词才具备一致的 H 自证。历史、退出、恢复和审计的保留 / 清理不是本路线统一改写的对象，状态 / 动态细节继续隔离 P08。

## 4. 异主 WIP、装配与世界树边界

| 对象 | 本基线事实 | 对未来迁移的要求 |
| --- | --- | --- |
| `海中鱼巣/领域/服务.L2需求结构.ixx` | 已 dirty，且是 HEAD 已发布当前消费者 | 取得文件和语义所有权的交接后重新 S0；不得借 DATA-L2 计划覆盖其 WIP，不能在未证实新 provider 合同前删去其生命周期防御 |
| `海中鱼巣/装配.普通应用.ixx` | 已 dirty；HEAD 未直接调用七族旧入口 | 仍属于最终装配 / 可达性语义。不得假定可改；须由所有者完成、交接或明确排除后重新 S0 |
| `海中鱼巣/业务/初始化.系统世界树根.inl` | HEAD 当前根读回比较 DATA-L3 `L2场景树` 投影所嵌完整 `场景身份来源`，不直接调用七族入口 | **P04.2 排除，不迁移。** 它不是旧当前身份 API 的消费者；不得以修改初始化比较或就地收缩场景树 DTO 取代七族迁移。若 P12 未来重设计当前树投影，另建独立范围、所有权交接、消费者清单和重新 S0 |
| `海中鱼巣/业务/消费.自我世界树根.inl`、系统 / 自我世界树根相关数据头 | 同为 DATA-L3 场景树聚合消费链 | 本项不改；未来树投影变更须与初始化同一独立范围处理 |

这一区分是必要的：要求“当前 API 不公开生命周期”不等于可以删除 DATA-L3 当前聚合中用于根一致性见证的生命周期。后者是否迁移是 P12 的独立语义 / ABI 问题，不能被 P04.2 暗中扩大。

## 5. 未来唯一切换的不可变约束

未来合法计划只能实现下列已经确认的终态，不得以短期便利改写：

```text
当前：读取当前{族}结构身份
      + 同 G0 四项来源均当前的 provider 证明
      + 瘦结果没有生命周期 / owner

历史：读取{族}身份来源见证
      + 仅各族已有或未来经正式明确的历史见证 / 专属退出、恢复、审计路径适用
      + 非零 H == 结果截止
      + 在适用路径返回完整共同见证

旧 读取{族}身份来源：不保留当前分支、转发壳或长期双入口
```

非成功必须空 payload：目标节点已退出为 `已退出`；锚点、关系类型、内部 owner、归属关系异常以及关系重复均为 `内部不一致`；调用 / 返回族不匹配为 `入口拒绝`。当前 success 必须蕴含四项来源同 G0 均未退出；瘦 DTO 的纯值谓词不能代替该 provider 证明。

任何旧格式解码、恢复或拒绝只可走 DATA-L1 / 4070 的合法恢复边界；本路线不允许引入 DTO 持久化、消息兼容、长期旧当前入口或第二当前投影。语言、词条、概念、不可变材料、概念名称和 ARCH-L4 兼容 ABI 均不在本路线范围。

## 6. 本路线的完成声明边界

本文件仅提供已发布 HEAD 的调用 / 所有权清单和未来门禁。它不证明正式规范完成同步、P04.1.1 已发布、异主 WIP 已交接、代码计划存在、任何代码已构建或验证、历史恢复可用，亦不把 DATA-L2 标记为 `FROZEN`。
