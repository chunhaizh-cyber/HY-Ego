# INSTINCT-STAGE3-SERVICE-HISTORY-BOUNDARY-SNAPSHOT-V2 服务维护历史兼容锚点与左边界读回代码实施计划 v0.1

日期：2026-08-31

计划身份：`INSTINCT-STAGE3-SERVICE-HISTORY-BOUNDARY-SNAPSHOT-V2`

版本：v0.1

计划状态：可执行候选；以计划索引正式登记为准

## 1. 目标与完成条件

在现有唯一 `服务合同事实权威服务` 内建立不可变服务维护历史兼容锚点，并实现从显式覆盖起点 `Gseed` 到任意 `Gstart/G0` 的左边界与区间变化完整读回。

完成必须同时满足：

1. `0x0307'3060..306B` 以同 owner 独立幂等建立 / 恢复五类 v2 历史根，首次写集把当时当前合同+状态、进展 v2、准备 v2 冻结为覆盖种子；
2. v2 历史成员关系永久保留目标节点，进展 / 准备更新对种子 incumbent 只退出当前关系并同步写入 v1/v2 历史；
3. 新入口从锚点正向重放到 `Gstart`，把左边界四类当前投影、`(Gstart,G0]` 五类变化、范围和成员完整性收束为一个不可拆分的低层完整范围快照；
4. `Gstart<Gseed`、不可恢复、漏 / 重 / 坏引用、任一预算不足、漂移和资源失败全部结构化非成功、零快照；
5. 现有 v1 历史范围 ABI 与回归保持；
6. Debug / Release、专项、ABI consumer、持久恢复、diff-check、strict 通过；
7. 形成专属施工 / 验证记录并精确提交、普通推送。

## 2. 正式依据与创建基线

正式依据：0050、4015、4040、4050、6160、6170，以及：

```text
规范/详细设计/20260831_INSTINCT-STAGE3-SERVICE-MAINTENANCE-HISTORY-LEDGER_服务维护历史事实账与完整范围读回详细设计_v0.1.md
规范/详细设计/20260831_INSTINCT-STAGE3-SERVICE-HISTORY-BOUNDARY-SNAPSHOT-V2_服务维护历史兼容锚点与左边界读回详细设计_v0.1.md
流程图/20260831_INSTINCT-STAGE3-SERVICE-HISTORY-BOUNDARY-SNAPSHOT-V2_服务维护历史边界读取函数流程图_v0.1.md
计划/20260828_INSTINCT-ROUTE_本能路线后续计划制定方案_v0.1.md
```

创建基线：`main == origin/main == 086dc43b37ff43158f58e57baf5dd4e4acead41a`，index 空。共享工作区存在无关 SELF-GOVERNANCE WIP，目标三源码当前干净。

固定生产定义保持：

```text
值域 = 0..INT64_MAX
L = 2767011611056432742
H = 7378697629483820645
安全根定义版本 = 1
主动安全结算规则版本 = 1
```

## 3. 精确白名单

允许修改：

```text
海中鱼巣/领域/服务合同事实权威.数据.h
海中鱼巣/领域/服务.服务合同事实权威.ixx
海中鱼巣/端到端测试.服务合同事实权威.ixx
施工记录/20260831_INSTINCT-STAGE3-SERVICE-HISTORY-BOUNDARY-SNAPSHOT-V2_施工记录_v0.1.md
验证记录/20260831_INSTINCT-STAGE3-SERVICE-HISTORY-BOUNDARY-SNAPSHOT-V2_验证记录_v0.1.md
```

禁止修改：全部 L1 源码、基础快照、游标、A/V 状态动态、批量算法、普通应用、启动、线程、工程、规范、详细设计、流程图、计划、索引及 SELF-GOVERNANCE WIP。编译闭包要求扩大时停止并具名退回。

## 4. 目标 DTO 与函数

module-private 持久工程类型与常量，只在服务实现内定义：

```text
服务维护历史覆盖结构登记版本_v2
服务维护历史覆盖成员_v2
0x0307'3060..0x0307'306B
0x494E'5354'5348'5632 登记幂等身份
```

低层维护工程 ABI，位于既有数据模块，只允许 maintenance composer 与专项 / ABI consumer 使用：

```text
服务维护历史边界合同版本_v2
服务维护历史边界读取请求_v2
服务维护历史左边界合同投影_v1
服务维护历史边界完整范围快照_v2
服务维护历史边界读取状态_v2
服务维护历史边界读取结果_v2
```

目标公开函数：

```cpp
服务维护历史边界读取结果_v2
服务合同事实权威服务::按事实代次边界读取服务维护历史完整快照_v2(
    const 服务维护历史边界读取请求_v2& 请求) const noexcept;
```

函数是同 owner 正式只读工程边界。允许后继 maintenance composer 与专项 / ABI consumer 调用；不允许线程、自我、需求比较、学习、日志、UI 或缓存把结果变成第二历史权威或认知事实。公开 `成功()`负责验证范围、成员身份、八个向量中的九类事实投影和 G0 终态闭合；消费者不得复制枚举、排序、归组或完整性算法。

## 5. 实施步骤

1. 候选常量全仓零命中；module-private 建立结构登记和历史成员类型；公开新增低层工程 DTO、严格升序 / 完整性 / `成功()`谓词。
2. 增加 v2 五根登记定位、数据依赖首次写集、首次恢复和构造顺序；首次写集读取当前四类完整集合并形成规范种子。恢复从首次规范写集 / 映射重建种子，不按重启当前集合重算。
3. 修改进展 / 准备 v2 写集分账：旧事实必须有唯一 v2 历史成员；只退出当前关系并保留历史材料；新事实同步写入 v1/v2 历史。
4. 实现 v2 五根和当前根一次 G0 一致读回；从 Gseed 正向重放到 Gstart，再收集变化并互证 G0 终态。
5. 扩展测试宏内 fixture，覆盖锚点前 incumbent、后继更新、坏锚点、预算、漂移、资源和恢复；生产无 fixture 出口。
6. 形成施工记录中的函数级代码知识变更清单和验证记录。

## 6. 失败收口

- v2 覆盖登记首次建立 / 恢复损坏：服务构造失败，不降级 v1 或空 v2 账。
- `Gstart<Gseed`：覆盖边界不可用；不得补算种子前历史。
- 左边界流缺唯一前驱 / 后继或 G0 重放不一致：左边界不可恢复 / 集合不闭合。
- 数量预算不足：整体失败，不截断。
- G0 漂移：丢弃候选并返回当前性漂移。
- 资源 / 异常：资源失败 / 内部错误；快照为空、截止 0。

## 7. 验证

至少执行：

1. `git diff --check`、精确 staged `git diff --cached --check`；
2. `python .\tools\check_specs.py --strict`；
3. v143 x64 Debug / Release 根工程 Rebuild；
4. 新专项覆盖详细设计第 7 节；
5. 仓库外 Debug ABI consumer 和 Release `/GL + /LTCG` ABI consumer；
6. 持久恢复：锚点 G、成员、关系、左边界、变化和唯一完整范围快照逐字段一致；
7. v1 范围读取完整回归；
8. 静态扫描无第二 owner、无生产 fixture、无阈值重算、无游标 / A/V / 状态动态写入；`3060..306B`、结构登记版本、登记幂等身份和内部历史成员没有从公开数据模块导出；
9. ABI 静态检查确认上层认知、自检和学习结果均不含 `Gseed/Gstart/G0`、预算、历史成员身份组或完整性字段。

隔离目录：

```text
D:\TEMP\海中鱼巣\INSTINCT-STAGE3-SERVICE-HISTORY-BOUNDARY-SNAPSHOT-V2\<本轮唯一标识>
```

## 8. 记录与知识交接

施工记录必须列出新增 / 修改函数、完整签名、用途、非职责、直接调用边、锚点写集和恢复边界、结果提交与待更新用途卡。共享知识库不在本计划白名单内。

## 9. 完成声明边界

本计划完成只可声明“服务维护历史自显式 v2 覆盖种子起可形成正式左边界，并以低层工程快照完整交付区间变化”。不得声明形成了新的认知证据类型，也不得声明种子前历史已恢复、游标已迁移、正式秒序、有效活动、低位门禁、事实段、批量维护、A/V 写入、状态动态因果、普通应用接线、阶段三或 `INSTINCT-ROUTE` 完成。
