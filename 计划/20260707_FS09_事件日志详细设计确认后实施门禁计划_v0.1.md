# FS-09 事件日志详细设计后实施门禁计划 v0.1

日期：2026-07-07
状态：已确认 / 已登记可执行队列 / 滚动生成 / 未改代码 / 未迁移旧函数

## 1. 依据

```text
项目记忆/Codex任务队列.md #112 已完成结果
计划/已完成计划/20260707_FS09_事件日志持久化恢复详细设计专项_v0.1.md
规范/详细设计/事件日志持久化恢复详细设计.md
实施记录/20260707_FS09_事件日志持久化恢复详细设计生成_Codex断点清单.md
实施记录/20260707_FS09_事件日志持久化恢复增强S0当前代码事实扫描_Codex断点清单.md
```

`规范/详细设计/事件日志持久化恢复详细设计.md` 当前状态为“已生成详细设计 / 未改代码 / 未迁移旧函数”。按最新规则，详细设计生成后可直接生成待确认计划，不需要单独等待详细设计确认；但本计划只建立详细设计后的实施门禁，不直接生成 C++ 实施许可。

## 2. 确认范围

本计划确认后，下一步可根据事件日志详细设计直接生成以下待确认计划候选：

```text
FS-09-E1：事件材料结构和事件类别枚举第一轮代码实施切片。
FS-09-E2：事件日志追加写入入口和空内容拒绝代码实施切片。
FS-09-E3：写入失败不改变结构验收切片。
FS-09-E4：事件日志只读审计和恢复候选材料边界切片。
```

## 3. 明确排除项

```text
不因详细设计生成直接放开 C++ 实施许可。
不实现仓库快照恢复入口。
不接 SQL / ADO / 控制面板 / WebView / D455 / 体素 / 外设。
不迁移旧日志函数体。
不迁移旧链表二进制保存恢复格式。
不把事件日志作为当前运行期事实源。
```

## 4. 目标服务和目标结构

| 项 | 目标口径 |
| --- | --- |
| 日志系统 | 事件日志作为结构事件审计和后续恢复候选材料 |
| 仓库入口 | 后续只在结构写入确认后记录事件，不让事件日志反向补写结构 |
| 领域服务 | 后续只在领域结构写入完成后记录领域结构事件 |
| 持久化适配 | 后续读取事件日志作为候选材料，不裁决当前事实 |
| 验收入口 | 验证空内容拒绝、未知类别拒绝和写入失败不改结构 |

## 5. 允许文件

确认后允许后续文档治理修改：

```text
规范/详细设计/事件日志持久化恢复详细设计.md
计划/待确认计划/20260707_FS09_事件日志第一轮代码实施切片_v0.1.md
实施记录/20260707_FS09_事件日志详细设计确认后实施门禁_Codex断点清单.md
项目记忆/*
计划/计划索引.md
```

## 6. 禁止文件

```text
海中鱼巣/**/*.h
海中鱼巣/**/*.cpp
海中鱼巣.vcxproj
旧项目源码
SQL / 控制面板 / WebView / D455 / 体素 / 外设文件
```

## 7. 实施步骤

```text
S0：复核事件日志详细设计是否已生成且仍未发生代码实施。
S1：根据事件日志详细设计生成待确认计划候选，不再等待单独详细设计确认。
S2：待确认计划经用户确认后，才登记可执行队列并进入后续代码实施切片门禁。
S3：若 #113 仓库快照格式与恢复拒绝矩阵仍未完成，则代码切片只覆盖事件日志写入，不覆盖恢复。
S4：同步计划索引、项目记忆和待确认问题。
```

## 8. 验收方式

本计划自身只做文档治理：

```powershell
git diff --check
python .\tools\check_specs.py
```

不运行 C++ 构建，不运行默认入口。

## 9. 完成声明边界

完成后只可声明：

```text
FS-09 事件日志详细设计后的实施门禁已建立。
```

不得声明：

```text
事件日志代码实施许可已放开。
事件日志结构化材料已实现。
仓库快照或恢复入口已实现。
旧日志 / 旧持久化能力已迁移。
```

## 10. 待确认问题

```text
FS09-NEXT-Q1：是否根据 `规范/详细设计/事件日志持久化恢复详细设计.md` 生成事件日志第一轮待确认计划候选。
FS09-NEXT-Q2：事件日志第一轮待确认计划是否只覆盖事件材料和追加写入，不覆盖恢复入口。
FS09-NEXT-Q3：是否等待 #113 仓库快照格式与恢复拒绝矩阵完成后，再生成恢复相关切片。
```
