# L2-SELF-E-ROLE 存在单例角色结构代码实施切片 v0.1

日期：2026-08-13

状态：待激活

## 1. 目标与依赖

按同名详细设计在最终 E-ID 存在 owner 中实现业务中性单例角色 CRUD 和原子角色化存在创建。本叶依赖当前存在结构最终实现；与 S-ID 无代码依赖，但同一时刻只允许一个代码施工段。

激活还必须等待当前世界树路线 `S-ID -> R1 -> A1 -> I1 -> I2` 全部实施、接受、索引退出并清理临时源码，使本叶成为 I2 后最早代码缺口；随后从新 HEAD 重做 S0。计划设计完成不改变该队列。

## 2. 允许与禁止文件

实施只允许：两个存在生产文件、三份具名临时专项、两份具名永久记录。清理提交只删除三份临时专项。禁止修改 L1、场景、A1、普通应用、启动、旧自我登记、根工程与其它服务；临时项目不得登记入生产工程。

## 3. 机械施工合同

1. 按详细设计完整实现 DTO、完整性函数与结果形状；
2. S0 扫描最终存在服务固定键和幂等保留域，只在无冲突时冻结详细设计预留 `0x0101'13xx` 键；冲突属于设计漂移；
3. 构造期在存在来源之后原子登记角色锚点 / 类型并 owner-aware 读回；
4. 实现存在 + 族归属 + 角色关系三项原子新增；
5. 实现既有绑定、目标替换、退出和当前 / 历史读取；
6. 角色唯一性、存在来源、期望代次、幂等和写后读回全部闭合；
7. 不改变普通存在 CRUD，只让存在退出自然受当前角色引用保护；
8. 形成永久记录，完成双配置根工程与专项矩阵。

## 4. 验证和退出

至少运行 Debug / Release 根工程 Rebuild、临时专项双配置、严格 UTF-8 / XML、范围扫描、`git diff --check` 和 `python .\tools\check_specs.py --strict`。真实分配失败、私有损坏、恢复、跨进程、生产消费者和独立集成验收未运行时记 `NOT_RUN`。

发布时精确暂存允许路径；交互接受并把本计划登记 `已退出` 后独立删除临时三文件。生产代码不得出现测试角色常量、验证分支、测试友元、故障注入或计数器。

## 5. 激活与完成声明

只有 I2 已退出、当前代码段和共享验证资源释放、最终存在 ABI 与详细设计无漂移时可登记 `可执行`。本叶实施完成不删除阶段 17，也不证明真实自我形成。
