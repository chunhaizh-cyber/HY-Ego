# SERVICE-DATA-S3 特征体系服务分层迁移代码实施切片 v0.1

日期：2026-07-13

队列编号：#267 / 设计队列：DQ-159

状态：设计资料门控 / 计划骨架保留 / 不可执行 / 等待 #262 实际矩阵和本服务族专项流程图、详细设计 / 预留最终回归 740 / 未改 C++

## 0. 设计资料门禁

当前总分层材料没有冻结特征值、特征、二次特征各自的数据操作所有权、公开只读端口和写入失败矩阵。本文件不构成代码实施许可。只有特征体系专项流程图与详细设计形成并按 #262 实际调用点修订本计划后，`DQ-159` 才可重新登记为依赖门控待执行。

## 1. 范围与假定

迁移特征值、特征和二次特征的创建、槽位、当前值、原始值版本、Vec / I64 和特征状态材料路径。特征值服务继续只服务特征系统，高级服务不得直连。假定 #266 的状态服务新公开面稳定。

## 2. 新模块

`领域/数据操作.特征体系.ixx`、`领域/服务.特征.ixx`、`领域/组合.特征状态.ixx`、`领域/自检.特征体系分层.ixx`，均为真模块；特征值数据操作只在模块内部可见，不导出给高级服务。

## 3. 允许与禁止

允许上述模块、既有特征值 / 特征 / 二次特征头的兼容适配、必要状态只读端口、工程 / 自检 / 治理。禁止需求、任务、方法直接访问特征值，禁止改哈希 / 缓存权力、旧主信息字段、`#214/#257`。

## 4. 失败与验收

槽位、主信息、节点、关系、原始值任一失败均精确撤销；当前值唯一性和原始版本保持。A01-A16、I64 / Vec、并发唯一当前值、故障注入、兼容回归、公开面无令牌、Debug x64 0/0、完整自检、连续 20 轮、严格规范、提交推送。

## 5. 完成边界

不证明完整特征值系统、值域稳态、缓存或恢复完成。
