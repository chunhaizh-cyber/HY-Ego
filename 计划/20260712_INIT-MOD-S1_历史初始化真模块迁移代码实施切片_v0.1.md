# INIT-MOD-S1 历史初始化真模块迁移代码实施切片 v0.1

日期：2026-07-12

队列编号：#250 / 设计队列：DQ-142

状态：JY-336 后置重设计门控 / 保留原 #250 / DQ-142 / 前置 #270、770 / 原流程图与详细设计待重做 / 690 当前不实施 / 不得执行本文件

> JY-336：本计划不并入 #270。#270 不修改四个历史初始化文件，只形成运行期业务装配和候选期无令牌路由。#270 正式完成后，设计窗口必须基于实际装配接口重做初始化流程图、详细设计和本计划：先通过新业务服务形成并读回系统角色材料，再迁移入口、线程和历史自检的兼容 include 调用。下文“#249 先提供唯一上下文”和直接原地真模块化假定均已撤回。

## 1. 依据与台账

流程图：`流程图/20260712_历史初始化真模块迁移与唯一上下文装配流程图_v0.1.md` 及 HTML；详细设计：`规范/详细设计/历史初始化真模块迁移与唯一上下文装配详细设计.md`。关联 `NEW-05`，属于工程治理，不计旧迁移。

## 2. 当前事实与假定

#236 证明四个初始化 `.ixx` 为历史兼容 include 形态；第一批自检模块仍消费部分历史初始化。当前顺序改为 #270 先提供运行期业务装配，#250 再形成新服务驱动的初始化路径，#249 最后验收上下文首次发布。执行前必须重做调用图和三件套；不得直接按本文件旧清单改代码。

## 3. 文件分类

| 文件 | 分类 | 登记 / 调用 |
| --- | --- | --- |
| `领域/初始化.概念图.ixx`、`初始化.世界树.ixx`、`初始化.语素.ixx`、`初始化.需求.ixx` | 生产真模块 | 原地改为 `ClCompile + export module + import` |
| `海中鱼巣/初始化.系统总成.ixx` | 生产总成真模块 | `ClCompile` / 入口 import |
| `海中鱼巣/自检.初始化模块.ixx` | 自检真模块 | `ClCompile` / 入口登记 690 |

允许上述六文件、`入口.cpp`、工程文件，以及当前已验证 include 调用方 `领域/自检.概念生命周期.ixx`、`线程/自我线程.ixx`、`线程/自我治理领域路由.ixx`、`线程/自检.自我治理多轮.ixx`、`自检.自我治理回执.ixx`、`自检.自我治理闭环.ixx` 的 import 改接和治理文件。禁止其它领域业务服务 / 仓库语义修改、双轨旧文件、入口初始化正文和第二套仓库；执行前出现新调用方即接口漂移，先修订允许清单。

## 4. 步骤与失败

S0 逐调用点复核；S1 先建模块骨架并分别编译；S2 按实际 DAG 改接 import；S3 接唯一上下文并移除 ClInclude/include；S4 自检顺序、幂等、失败不发布和 690。无效上下文逻辑内返回；前置通过后根 / 关系 / 索引读回异常追根因。

## 5. 验收

A01-A14 覆盖四文件真实模块、无 include、无循环、结果守恒、入口最小、生产 / 自检单向、完整自检与连续 20 轮。Debug x64、严格规范、diff，提交推送。

## 6. 边界

只迁移工程承载，不新增初始化事实或恢复能力。
