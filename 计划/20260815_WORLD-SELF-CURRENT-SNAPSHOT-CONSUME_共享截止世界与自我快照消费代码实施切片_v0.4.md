# WORLD-SELF-CURRENT-SNAPSHOT-CONSUME 共享截止世界与自我快照消费代码实施切片 v0.4

日期：2026-08-17

执行资格：只读 `计划/计划索引.md`；本文件不维护当前状态

替代关系：同身份 v0.4 替代已发布 v0.3；v0.3 保留为历史证据，不得原位改写或恢复。

## 1. 目标与完成条件

在唯一 `海中鱼巣/领域/组合.运行期只读查询.ixx` 内实现共享截止世界—自我只读组合：一个非零 G0 贯穿现实世界整树、自我角色、完整存在、成员和宿主正反向读取，全部成功后逐字段互证并一次返回完整快照。

本版本只收紧 DATA 失败边界。完成条件仍包括单一组合器、六入口同截止、成功完整、非成功零快照、无等待 / 无重试和三类合法并发结果；不得把当前已复现的 DATA 资源失败当作合法第四结果。

## 2. 当前阻断与独立前置

正式基线：`main == origin/main == 36ce5804b47238ba4b66210d13cd8e98b758acb8`。执行侧已安全停工，WIP 未暂存。

并发失败链为：L1 锁竞争 `许可拒绝` → `服务.L1事实基座.ixx::映射中性读取状态` `资源失败` → `服务.L2场景结构.ixx::映射事实代次探测失败` DATA `资源失败` → BIZ `资源失败`。WORLD-SELF 不吞错、不重试、不猜漂移；该链只能作为 DATA 前置缺口证据。

具名独立前置：`DATA-L2-L1-PERMISSION-REJECTION-PROPAGATION v0.1`。由计划智能体另立并发布，未完成前本计划保持`待激活`，不得施工。

前置必须真实证明 L1 许可拒绝经 L2 六个当前读取入口显式传播为 L2 许可拒绝与零载荷；不得新增状态、镜像、等待、重试、日志裁决或 BIZ 特例。真实资源失败仍保留公开状态 6，但并发专项不得再把锁竞争归入资源失败。

## 3. 施工范围

永久生产 / 工程文件：

```text
海中鱼巣/领域/组合.运行期只读查询.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
```

生产工程外专项：

```text
验证工具/共享截止世界与自我快照消费参数验证.cpp
验证工具/共享截止世界与自我快照消费参数验证.vcxproj
验证工具/运行共享截止世界与自我快照参数验证.ps1
```

永久记录沿用 v0.3 两份专属施工 / 验证记录。禁止修改全部 DATA / L1、P0-FIX、路线、旧 WORLD-TREE、`.codex-build`、验证工具之外的工具、知识库及其它 WIP；不得把 DATA 前置代码带入本计划。

## 4. 物理实现与状态合同

模块声明、imports、DTO、组合器不可复制 / 不可移动形态、六项调用顺序和函数签名沿用 v0.3。函数名、变量名和业务标识符使用中文；组合器只借用同一 `const L2结构聚合服务&`。

状态映射固定：

```text
未找到 / 已退出 -> 材料缺失
事实代次漂移或已发布 G1 != G0 -> 当前性漂移
许可拒绝 / 入口拒绝 / 引用冲突 / 预算拒绝 -> 提供者拒绝
资源失败 -> 资源失败（零快照；并发锁竞争命中时为 DATA 前置失败）
内部不一致、坏成功、错截止、复义、拓扑矛盾 -> 内部不一致
```

所有非成功零快照。BIZ 不取得 L1 锁、不解释 DATA 内部状态、不把 `Gobs == G0`或日志改写为漂移。

## 5. 实施与失败收口

执行前必须等待并核对 DATA 前置提交、接受、资源释放、DTO / ABI 和六入口逐项结果；然后从新 HEAD 完整 S0。实施顺序为：请求准入 → 同实例服务取得 → 六项同 G0 读取 → 状态 / 截止 / 载荷检查 → 树、成员、宿主、角色、存在互证 → 一次构造 → 双配置构建与专项验证。

任一 DATA 资源失败、许可拒绝未传播、截止 / ABI / 所有权漂移均立即停止并返回计划支撑；不得在 WORLD-SELF 范围内修 DATA、添加重试或重分类。

## 6. 验证矩阵

必须验证：坏请求零 DATA 调用；六项原样同 G0；完整树、两父子边、成员恰一、宿主正反向、角色 / 来源 / 存在；已形成、明确当前性漂移、L2 许可拒绝三类并发结果；所有非成功零快照；零等待、零重试、零部分载荷。

若并发专项出现 DATA 资源失败（包括既知 Debug `46=40+6` / Release `83=70+13`形状），记录为 DATA 前置未满足并失败，不扩展矩阵、不宣称可执行门禁通过。真实资源耗尽、私有坏形状、普通应用、正式生产消费者、跨进程恢复和集成验收保持 `NOT_RUN`。

## 7. 状态与完成边界

本 v0.4 发布后索引状态为`待激活`。只有独立 DATA 前置真实结果进入 main、经接受、实际接口匹配且并发专项不再将锁竞争映射为资源失败，索引所有者才可将本计划恢复为可执行；执行侧必须从新 HEAD 重做 S0。

可执行或完成仅表示 WORLD-SELF 组合合同与验证边界成立，不证明 DATA provider、生产消费者、正式接线、日志观察、跨进程恢复或系统业务闭环完成。
