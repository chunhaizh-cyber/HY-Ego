# L1-SIMPLIFY-METHOD-ROOT-R8-FIRST-PUBLISH-PROVIDER-EVIDENCE
# 方法登记根首次发布准入提供者证据代码实施切片 v0.1

更新时间：2026-08-07

## 1. 目标与完成条件

唯一实施根：

```cpp
方法登记根首次发布准入提供者证据结果
L1方法登记根首次发布证据服务::形成方法登记根首次发布准入提供者证据(
    const 方法登记根首次发布准入提供者证据请求& 请求) const;
```

恰一次调用 R7；仅在 `可首次发布` 时从同一 L1 恰一次取得最终 P17 完整快照；形成固定 provider
身份、直接调用计数、共同截止、全量当前事实身份与同索引完整副本。完成只证明零写强类型证据
源码候选，不形成最终 L1 执行材料或发布者。

本根只允许由后继领域发布调用方在现有 P15 单键探测返回 `未找到` 后调用。探测非 `未找到`
时 R8 调用0；本叶不重复探测、不新增探测 DTO 或可伪造许可令牌。

## 2. 正式依据与当前事实

- 0050、4015：运行期内存已发布事实代次权威、三类事实和原子发布边界；
- 4030：方法领域拥有业务准入，跨服务合同必须具名、版本化、强类型；
- 3300、5300：方法登记根及其唯一写入门属于方法领域；
- R2/R4/R5：完整本地写集、G10标签意图和 `0x19` 最终幂等键；
- R6/R7：同截止完整首次结果组与方法根0/1/N当前准入；
- P17：当前 L1 非阻塞完整快照组件与验收结果已进入 main；验收仅证明组件局部可继续、未正式接线；
- P15/PRE1/PRE2：执行证据必须使用真实 provider 身份和同截止完整事实副本，不能取默认身份或首项。

详细设计：
`规范/详细设计/20260807_L1-SIMPLIFY-METHOD-ROOT-R8-FIRST-PUBLISH-PROVIDER-EVIDENCE_方法登记根首次发布准入提供者证据详细设计_v0.1.md`

单函数流程图：
`流程图/20260807_L1-SIMPLIFY-METHOD-ROOT-R8-FIRST-PUBLISH-PROVIDER-EVIDENCE_形成方法登记根首次发布准入提供者证据函数流程图_v0.1.md`

## 3. 精确允许文件

```text
海中鱼巣/领域/L1方法登记根首次发布证据.数据.h
海中鱼巣/领域/服务.L1方法登记根首次发布证据.ixx
海中鱼巣/领域/自检.L1方法登记根首次发布证据.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
海中鱼巣/启动.应用程序.ixx
施工记录/20260807_L1-SIMPLIFY-METHOD-ROOT-R8-FIRST-PUBLISH-PROVIDER-EVIDENCE_施工记录.md
验证记录/20260807_L1-SIMPLIFY-METHOD-ROOT-R8-FIRST-PUBLISH-PROVIDER-EVIDENCE_验证记录.md
```

禁止修改 R1—R7、P15、P17 既有计划/设计/代码/记录，禁止 P0-FIX、路线、旧 WORLD-TREE、
`.codex-build`、服务候选、安全、因果、恢复及任何其它 WIP。不得新增第二 exe、独立自检目标、
第二装配、测试 DTO、调试出口或替身生产入口。

## 4. 目标合同

1. 新增固定 provider 身份 `0x4C314D5200000001ULL`，合同/规则1/1，以及无参只读
   `读取方法登记根首次发布准入证据提供者身份() noexcept`；
2. 新增版本化请求、状态、写前事实种类/身份、provider证据凭证和结果 DTO；
3. 服务只持非拥有 `const L1事实基座服务&`，用同一引用构造临时 R7 并取得最终 P17 快照；
4. 成功固定直接调用次数1、去重 provider 数量1、共同截止非零；R7/P17/R2截止相等；
5. 成功凭证携带完整 R7 准入结果、最终 `L1完整快照`、全量当前事实身份和同索引
   `L1事实副本`，三者数量机械一致；
6. 非成功凭证全空，不返回可拼接半材料；
7. 不形成 `L1执行证据材料`、写前事实摘要、请求/执行/规范化/材料摘要或通用读回计划。

## 5. 机械实施顺序

1. 新增数据头，冻结身份、版本、DTO、默认状态和逐字段相等；
2. 新增服务模块，构造函数只保存 `L1_` 非拥有引用；
3. 在任何 provider 前互证 R8/R7/R4/R2、公开身份、R5键和非零截止；
4. 用同一 `L1_` 构造 R7，恰调用一次并按详细设计映射全部状态；
5. 仅在 R7 可首次且0候选时，恰一次调用 P17 非阻塞完整快照；
6. 互证 R7/P17/R2 共同截止、P17成功形状、当前三类事实排序及永久占用覆盖；
7. 按节点、关系、值及各自编码升序全量形成身份组和同索引完整副本组；
8. 当前事实总数0返回事实材料不足；组长、数量、种类或副本矛盾返回内部不一致；
9. 一次形成固定身份、直接调用1、provider数量1、共同截止、准入和快照的完整凭证；
10. 新增专项 `L1-SIMPLIFY-METHOD-ROOT-R8-S01`，由同一生产程序顺序590薄入口执行；
11. 形成专属施工和验证记录，不修改聚合机器事实或其它计划状态。

## 6. 调用、并发与空组边界

- 非法入口：R7/P17均0；
- 合法入口：R7恰1；只有R7可首次时最终P17恰1，其它分支0；
- 不跨R7/P17持锁，不等待、不sleep、不循环重试；漂移由调用方重做完整R8；
- R8成功不是可转移许可，后继提交仍必须使用同一截止作为P15期望代次；
- 当前事实总数0返回事实材料不足。不得把非零截止状态伪装成P15空仓模式；
- R1—R6、P15探测/提交/审计/恢复、SQL、缓存、日志和全部事实写入为0。

## 7. 验证矩阵

专项至少覆盖：

1. 全部版本、身份、R5键、截止入口正反例及provider调用0；
2. 固定身份/合同/规则、服务生命周期、无锁无分配身份读取；
3. R7可首次、唯一、多个、具名非成功、坏成功和未知状态；
4. 最终P17成功/许可/资源/内部不一致/坏成功及三截止漂移；
5. 节点/关系/值0/1/N组合、各组排序、跨种类顺序和永久占用覆盖；
6. 身份/副本/数量等长、对象种类和编码逐项相等，重复/乱序/半结构拒绝；
7. 非零截止但当前事实0返回事实材料不足，历史/占用/日志/默认身份均不补造；
8. 成功直接调用1、provider数量1；R7传递调用不重复登记；
9. 后继 provider 前探测非未找到时R8调用0；本叶最终执行材料、四类摘要、探测、稳定编码、
   P15提交、事务、发布、读回均为0；
10. 同一生产程序 Debug/Release、自检、模块扫描及正式日志人读观察；
11. 既有专项不退化，零第二目标/装配/入口；
12. 八路径、顺序590、专项身份、provider身份唯一，diff check与strict110/110。

## 8. 激活门禁与失败收口

P17 独立验收门已满足，但计划仍保持待激活，直至 R7 真实代码结果进入 main 并经交互接受，
R1—R7 传递门禁和实际 DTO/签名机械匹配；活动计划释放八路径、工程/filters、启动、构建、自检、
模块扫描、固定输出和顺序590资源；执行从新 HEAD 完整 S0。

出现以下任一项须具名退回计划支撑，不得临场补语义：

- 固定 provider 身份已有正式等价占用；
- R7 实际结果不能区分可首次/唯一/多个或不携带共同截止；
- P17 实际快照不是完整、排序、同截止值式副本；
- 必须修改 R1—R7/P15/P17，或必须形成摘要、探测、事务、发布才能完成本叶；
- 顺序590出现当前真实代码占用，或白名单/资源发生重叠。

旧 #214 过期计划的590仅为已终止且未实施历史预留，不构成当前代码占用；本叶不得恢复或修改
该历史计划。

## 9. 完成声明

最多声明：R8 源码候选能够把 R7 同截止可首次资格转换为固定 provider 身份和全量当前 L1
事实的强类型、排序、一一对应证据凭证。

不得声明：R1—R7 已实现、P17 生产接线/服务验收通过、最终执行材料或摘要已形成、幂等已探测、
稳定编码已分配、P15 已提交、事务参与者/发布者已形成、方法登记根已发布/读回、方法已登记/执行、
服务候选、安全、因果、崩溃/跨进程恢复或治理闭环完成。
