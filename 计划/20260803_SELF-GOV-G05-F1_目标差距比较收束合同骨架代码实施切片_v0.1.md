# SELF-GOV-G05-F1 目标差距比较收束合同骨架代码实施切片 v0.1

计划身份：SELF-GOV-G05-F1

版本：v0.1

创建侧当前代码基线：`dce077a4d82b543654482f5cb3524f8819e2e5a9`

状态：待激活

## 1. 目标与完成条件

新建目标差距比较收束最终模块、稳定 DTO、单引用服务和唯一固定 `未实现` 根函数，并以专属自检证明安全入口可巡检。

完成条件：对任意请求固定返回 `未实现/零请求身份/零事实截止代次/空比较结果`；参数引用、COMPARE-F调用和其它项目调用均为0；权威机器事实与发布代次不变；所列构建、自检和静态门禁全部通过。

本计划只允许取得 `编译接入通过` 和 `入口巡检通过`。

## 2. 正式依据与依赖

唯一设计链：

```text
流程图/20260803_SELF-GOV-G05-F1_目标差距比较收束合同骨架业务流程图_v0.1.md
流程图/20260803_特征判断收束服务按注册比较合同裁决目标差距_函数流程图_v0.1.md
规范/详细设计/20260803_SELF-GOV-G05-F1_目标差距比较收束合同骨架详细设计_v0.1.md
规范/详细设计/函数结构知识图谱/20260803_SELF-GOV-G05-F1_目标差距比较收束合同骨架函数结构知识图谱_v0.1.md
```

具名激活依赖：WORLD-TREE-COMPARE-F v0.1 的真实代码结果必须把 `海中鱼巣.领域.服务.特征比较`、完整最终 DTO、`特征比较业务服务::比较特征` 和其专项自检基线发布到正式 main，并释放模块、工程和启动聚合写权。

COMPARE-F 仅有设计、计划、骨架 WIP或消息时，本计划保持待激活。COMPARE-R 不是本骨架编译依赖，但是真实函数体替换的硬依赖。

SELF-GOV-G05-F0 与本计划没有代码文件依赖；二者都在 COMPARE-F 结果后施工，可以按唯一活动代码段和共享工程所有权串行。不得为了文档顺序伪造结果依赖。

## 3. S0 门禁

执行前必须从正式 Git 复核：

1. 本计划精确版本/blob已登记，且计划索引已在依赖满足后把状态从 `待激活` 更新为 `可执行`；HEAD/main/origin、index、活动代码计划段和发布租约满足规则。
2. COMPARE-F 结果提交真实存在；最终状态、请求身份、算法族、输入、结果、服务类和 `noexcept` 函数与本详细设计引用一致。
3. 目标生产模块和专项自检不存在同义正式实现；允许文件没有其它所有者活动 WIP。
4. 记录届时顶层自检登记/执行数量 `N`；顺序250未被其它正式身份占用。顺序220—240是否连续不是依赖。
5. 现有代码没有 `特征判断收束服务`、`目标差距比较请求/结果` 或同名根的第二所有者。

任一不成立，零代码修改并返回具名漂移；不得复制 COMPARE-F 类型、改用旧比较入口或增加兼容壳。

## 4. 精确允许文件

```text
海中鱼巣/领域/服务.特征判断收束.ixx
海中鱼巣/领域/自检.特征判断收束合同骨架.ixx
海中鱼巣/启动.应用程序.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
施工记录/20260803_SELF-GOV-G05-F1_施工记录.md
验证记录/20260803_SELF-GOV-G05-F1_验证记录.md
```

其它文件全部禁止，尤其禁止修改 COMPARE-F、COMPARE-R、需求服务、世界树服务、目标业务图、规范、设计、计划、计划索引、仓库、事务、SQL和当前代码图谱。

启动和工程文件只允许登记本计划一个生产模块、一个自检模块及唯一顶层自检项。

## 5. 机械施工

1. 新建 `服务.特征判断收束.ixx`，import COMPARE-F；按详细设计 §3 逐字声明常量、枚举、两个 DTO 和服务类。
2. 构造精确保存一个 `特征比较业务服务&`；删除复制和移动；不得增加默认构造、setter、指针或可选依赖。
3. 唯一根参数不命名，函数体逐字 `return {};`；添加完整成员函数指针和 `noexcept` 静态断言。
4. 新建专属自检模块，唯一导出详细设计 §9 的报告和 `运行特征判断收束合同骨架自检()`；构造 COMPARE-F 所需的合法依赖链及本服务，但不得调用仓库写入、恢复、SQL或材料。
5. 自检以默认、非法、全字段和边界整数请求调用根；逐字段断言全部为固定未实现结果。对状态数值、DTO字段类型/顺序、构造与完整函数指针做静态断言。
6. 自检调用前后比较节点、关系、索引、类型合同、类型化值及事务域已发布代次的权威导出；必须逐值相等。记录 COMPARE-F 服务调用数为0。
7. 在启动聚合建立唯一 `运行特征判断收束合同骨架合同自检()` 适配，并以 `SELF-GOV-G05-F1-S01`、名称“目标差距比较收束合同骨架”、顺序250登记；登记、执行、摘要期望总数从S0的 `N` 精确变为 `N+1`。
8. 登记两个新模块到工程和 filters，形成专属施工/验证记录。

执行者不得决定真实注册、算法、正差距方向、错误映射或消费者接线。

## 6. 验证矩阵

| 编号 | 场景 | 必须结果 |
| --- | --- | --- |
| G05-F1-A01 | 默认/非法/完整/边界请求 | 输出逐字段完全相同 |
| G05-F1-A02 | 状态与 DTO ABI | 状态1—11、字段顺序和底层类型稳定 |
| G05-F1-A03 | 构造 | 单引用正例；默认/指针/缺参/复制移动反例 |
| G05-F1-A04 | 根签名 | 完整成员函数指针匹配且 `noexcept` |
| G05-F1-A05 | 函数体 | 参数引用0；直接/成员/回调/未解析调用0 |
| G05-F1-A06 | COMPARE-F | 保存引用但调用次数0 |
| G05-F1-A07 | 权威事实 | 调用前后全部导出和发布代次逐值相等 |
| G05-F1-A08 | 外部边界 | SQL、材料、缓存、日志和时钟不改变结果 |
| G05-F1-A09 | 消费者门禁 | 未实现后成功后继调用数0 |
| G05-F1-A10 | 聚合 | 仅S01/顺序250一项；总数N→N+1 |

## 7. 验证命令与失败收口

```powershell
git diff --check
python .\tools\check_specs.py --strict
msbuild .\海中鱼巣.slnx /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m:1
.\x64\Debug\海中鱼巣.exe --self-test-exit
msbuild .\海中鱼巣.slnx /t:Rebuild /p:Configuration=Release /p:Platform=x64 /m:1
.\x64\Release\海中鱼巣.exe --self-test-exit
rg -n "目标差距比较收束合同版本|目标差距比较状态|目标差距比较请求|目标差距比较结果|class 特征判断收束服务|按注册比较合同裁决目标差距|SELF-GOV-G05-F1-S01" 海中鱼巣
```

使用现行 MSVC 模块依赖检查和可到达目标模块的 AST/libclang 或正式允许的等价源码结构检查，证明完整限定定义唯一、参数引用0、项目调用边0和禁止边0。工具无法到达时保留真实失败并按现行验收规则处理，不得用文本命中冒充 AST PASS。

任一构建、自检、strict、diff、ABI、调用边、事实不变或聚合数量失败时保留 WIP，不提交、不升级完成声明。

## 8. 后继与完成边界

后继真实替换必须另立不可变计划，并依赖 COMPARE-R 真实注册和算法结果；该计划须冻结各算法族的已满足、严格正差距、约束冲突和不可比较映射。G05/G11 消费者必须等待真实替换结果，不能消费本骨架继续形成需求、任务或结算。

允许声明：`SELF-GOV-G05-F1 目标差距比较收束最终合同骨架通过编译接入和安全入口巡检`。

禁止声明：注册比较已实现、真实正差距已裁决、消费者局部可继续、G05/G06/G11完成或自我内部治理闭环已通过服务验收。
