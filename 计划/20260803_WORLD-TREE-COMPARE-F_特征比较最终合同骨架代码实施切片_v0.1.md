# WORLD-TREE-COMPARE-F 特征比较最终合同骨架代码实施切片 v0.1

计划身份：WORLD-TREE-COMPARE-F

版本：v0.1

创建侧代码复核基线：`0f7e2840498a984f2b16dc831d41f929fd40137d`

## 1. 目标、完成条件和依赖

建立唯一特征比较模块、最终强类型DTO、服务构造和固定未实现骨架，使消费者能按最终ABI编译并安全停止。

完成条件：任意请求固定返回 `状态=未实现8、拒绝原因=未实现20` 和全空载荷；零依赖调用、零机器事实变化；顺序210自检从21项精确增加到22项。

具名依赖：

1. P1A2正式代码已提供 `节点直接结构查询服务` 与共享身份/类型化值DTO；本骨架不要求P1A2查询、提交、恢复或SQL真实成功。
2. P1A3F v0.2或其P1A3原位替换、P1A2Q v0.2、P1BF v0.2的成功代码已分别占用顺序180、190、200，正式自检基线为21项；顺序210未占用。
3. 4180 v0.2和4201 v0.3已正式发布。

任一依赖只有设计/WIP/消息或签名、顺序、计数不同，本计划保持待激活并以零代码修改返回 `DRIFT-COMPARE-F-ABI-OR-SLOT`。

唯一直接施工依据：

```text
规范/4180_子规范_特征值域差异算子与比较算法注册.md v0.2
规范/4201_子规范_世界树合同骨架与未实现结果.md v0.3
规范/详细设计/20260803_WORLD-TREE-COMPARE-F_特征比较最终合同骨架详细设计_v0.1.md
流程图/20260803_特征比较业务服务比较特征_目标函数流程图_v0.1.md
```

## 2. 允许文件

```text
海中鱼巣/领域/服务.特征比较.ixx
海中鱼巣/领域/自检.特征比较合同骨架.ixx
海中鱼巣/启动.应用程序.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
施工记录/20260803_WORLD-TREE-COMPARE-F_施工记录.md
验证记录/20260803_WORLD-TREE-COMPARE-F_验证记录.md
```

其它路径均禁止。特别禁止修改仓库/查询/提交/恢复、旧特征/状态/动态、SQL/材料端口、规范、设计、流程图、其它计划和索引。

## 3. 机械施工

1. S0核对三项依赖、共享类型、顺序170—200、21项基线和顺序210空闲；不成立零修改收口。
2. 按详细设计§2—§5逐字建立模块、完整枚举、DTO字段顺序、唯一构造、删除复制/移动和公开函数。
3. 根函数不读取请求、不调用保存引用，逐字等价地返回详细设计§5固定prvalue；保留 `static_assert(noexcept(...))`。
4. 建立唯一自检 `WORLD-TREE-COMPARE-F-S01`，顺序210，从21项增加到22项。
5. 更新工程/filters并完成专属施工、验证记录。

不得删字段、改数值、换成JSON/variant/裸I64、默认成功、第二入口、临时构造或动态“最大顺序+10”。

## 4. 验证和失败收口

```powershell
git diff --check
python .\tools\check_specs.py --strict
msbuild .\海中鱼巣.vcxproj /m /p:Configuration=Debug /p:Platform=x64
msbuild .\海中鱼巣.vcxproj /m /p:Configuration=Release /p:Platform=x64
.\x64\Debug\海中鱼巣.exe --self-test-exit
```

另用正式Clang AST工具证明根函数项目调用边和未解析边均为0；静态扫描服务文件对SQL、材料、旧栈、仓库、许可、日志和算法命中为0。自检逐项覆盖详细设计§8。

接口、模块、共享头、顺序、计数或稳定数值漂移时停止，不建立兼容壳。任一验证失败保留WIP，不声明完成。

## 5. 完成边界

完成只声明 COMPARE-F 合同骨架编译接入和入口巡检通过；不得声明注册、算法、比较、P1B、状态动态或世界树真实能力完成。
