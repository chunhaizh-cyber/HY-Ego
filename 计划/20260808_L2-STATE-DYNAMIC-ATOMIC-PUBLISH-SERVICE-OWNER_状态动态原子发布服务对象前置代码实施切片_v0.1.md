# L2-STATE-DYNAMIC-ATOMIC-PUBLISH-SERVICE-OWNER 状态动态原子发布服务对象前置代码实施切片 v0.1

日期：2026-08-08

状态：可执行

## 1. 目标与正式输入

按配套详细设计机械实施最早所有权前置叶：只把 `海中鱼巣.领域.服务.状态动态原子发布` 的现有六参数自由函数收束为不可复制的 `状态动态原子发布服务` 对象方法，五个服务依赖改由构造一次绑定并保存为非拥有引用。现有请求/结果 DTO、legacy 幂等探测、G7 意图、执行证据、`提交写集(..., 0x15000603U)`、P13/P14 分支和领域读回语义保持不变。

- 详细设计：`规范/详细设计/20260808_L2-STATE-DYNAMIC-ATOMIC-PUBLISH-SERVICE-OWNER_状态动态原子发布服务对象前置详细设计_v0.1.md`
- 施工流程图：`流程图/20260808_L2-STATE-DYNAMIC-ATOMIC-PUBLISH-SERVICE-OWNER_发布实际存在I64后继状态与迁移动能函数流程图_v0.1.md`
- 正式依据：0050、4015 v0.6、4030 v1.4、4040 v1.4、4050 v1.2、4190 v0.3、4200 v1.2、0600 v0.4。

设计基线：`9b4f16d9d3d3fc379184333caac4597db880e03f`。目标服务 blob=`6bed60a3dfff42928225d93323ed63fc5bdbc9a2`；只读数据头 blob=`24c697ded03a0e572cce8f5454bdcf64f379df65`；计划索引 blob=`3e85580e82befc3f8fddaa2c9e56f01e4338f4c6`。

## 2. 实施前 S0

1. 确认 `main == origin/main`，HEAD 不早于设计与索引提交，index 为空；取得唯一活动代码计划段和 Git 发布租约。
2. 读取计划索引本行、真实 plan blob、详细设计、单根流程图、现行 4015 / 4030 / 4200 和单工作区发布规则；确认本计划是唯一合法 `可执行` 项，索引列顺序为 `plan blob` 后 `设计提交`。
3. 重读目标服务和数据头，确认仍只有一个六参数自由函数定义且当前代码调用为 0；确认模块、DTO、P13/P14/P15、五个依赖服务类型和现有 helper 签名与设计基线兼容。
4. 核对当前 `运行期上下文` 尚未装配状态、动态、比较、P16 或世界树业务服务；本叶不得以此为由扩大到运行期、门面、启动或工程。
5. 精确记录异主 dirty；只冻结与本计划三个白名单路径真实重叠的冲突。不得 stash、reset、clean、覆盖或带入异主 WIP。
6. 若出现真实代码调用点、同名服务对象、公开 ABI 消费或必须修改数据头/helper/依赖模块的漂移，停止并具名退回计划支撑，不得自行建立兼容壳或扩大白名单。

## 3. 写入白名单

1. `海中鱼巣/领域/服务.状态动态原子发布.ixx`
2. `施工记录/20260808_L2-STATE-DYNAMIC-ATOMIC-PUBLISH-SERVICE-OWNER_施工记录.md`
3. `验证记录/20260808_L2-STATE-DYNAMIC-ATOMIC-PUBLISH-SERVICE-OWNER_构建与服务对象前置验证.md`

除此之外零写入。禁止修改数据头、核心 L1、P13、P14、状态、动态、比较、运行期上下文、世界树门面、启动、工程 / filters、自检、集成验收、规范、详细设计、流程图、计划、计划索引和异主 WIP。

## 4. 机械施工步骤

1. 在现有 `export namespace 海中鱼巣` 中新增并只导出 `class 状态动态原子发布服务 final`。
2. 新增 `noexcept` 五引用构造，按 `L1_、实例特征_、状态_、比较_、动态_` 顺序绑定对应非拥有引用；删除复制构造和复制赋值。不得新增全局/静态对象、共享指针、仓库、锁、缓存或事务成员。
3. 把现有 `发布实际存在I64后继状态与迁移动能` 的请求参数保留为唯一显式参数，并将整段函数体迁入同名公有非 `const` 方法。
4. 按详细设计映射只替换五个服务形参：所有 L1 探测/执行证据/提交使用 `L1_`，P13 使用 `L1_、实例特征_、状态_、比较_`，P14 使用 `动态_`，领域读回使用 `状态_、动态_`。
5. 删除旧六参数自由函数，不保留重载、包装、兼容转发壳、函数内静态服务或临时对象调用。
6. 对方法正文执行施工起点同一性核对：请求校验、摘要、最终键、G7、两次 legacy 探测位置、P13/P14 分支、写集/材料形成、单次 legacy 提交、失败见证、领域读回、首次证据和异常映射不得发生其它差异。
7. 填写专属施工和验证记录，只记录实际代码差异、构建/扫描事实、明确未运行项和声明边界；不得新增业务场景、专项 L1 测试或日志 PASS 判定。

## 5. 验证门禁

至少执行：

```powershell
git diff --check -- '海中鱼巣/领域/服务.状态动态原子发布.ixx' '施工记录/20260808_L2-STATE-DYNAMIC-ATOMIC-PUBLISH-SERVICE-OWNER_施工记录.md' '验证记录/20260808_L2-STATE-DYNAMIC-ATOMIC-PUBLISH-SERVICE-OWNER_构建与服务对象前置验证.md'
python .\tools\check_specs.py --strict
```

随后用项目正式命令完成 Debug / Release x64 完整重建和双配置模块扫描。不得新增专项自检、生产外驱动、双目标、集成验收或运行期装配。

静态门禁必须证明：

- `状态动态原子发布服务 final`、五引用 `noexcept` 构造、删除复制操作和五个私有引用成员精确存在；
- 公开发布方法只接受 `const 状态动态原子发布请求&`，旧六参数自由函数和任何兼容壳为 0；
- 当前代码调用仍为 0，没有临时构造或第二对象所有者；
- 相对施工起点，方法正文只发生类作用域和五参数到成员的一一替换；legacy 探测、G7、执行证据、`0x15000603U` 提交、P13/P14、领域读回和异常映射保持；
- 数据头、helper 签名、核心 L1、P13/P14、状态/动态/比较、运行期、门面、工程和目标外文件差异为 0；
- 未新增 `mutex`、缓存容器、静态状态、中性提交、审计/恢复读取、生产日志或验证入口。

当前没有合法生产消费者，故构造、首次、重复、异义、P13/P14 非成功、提交和读回场景均必须在验证记录标记 `未运行`。编译和模块扫描只证明源码可构建，不证明服务对象已实例化或业务行为已经运行。

## 6. 后继门禁与完成声明

本计划结果进入正式 main 并经交互接受后，交互智能体通知计划侧从新 HEAD 建立“状态与动态原子联合发布入口业务中性 CRUD 迁移”消费叶。消费叶必须在本对象内设计 P16 专属串行锁和按幂等键保存的多项完整请求/首次规范化中性写集/读回定位缓存；不得恢复自由函数、只缓存最后请求、直接从缓存返回成功或调用 L1 审计/恢复入口补写集。

现行 P45 v0.1 按旧自由函数和五底层引用设计，继续保持待激活。后继计划必须把它重做为只消费 `状态动态原子发布服务&`；本计划执行者不得修改 P45、计划索引或运行期装配。

只暂存三个白名单路径，复核 staged diff 后普通提交并普通推送 main。结果进入正式 main 后交回交互智能体核验并更新计划索引；执行者不得自行修改本计划或索引状态。

本叶完成最多证明：P16 legacy 发布正文已有唯一可实例化、不可复制的服务类型承载位置，未来锁与首次写集缓存的对象所有权已冻结。它不证明运行期实例、真实调用、中性 CRUD、精确重复、世界树门面、生产接线、集成验收、其它状态动态入口、核心 L1 或第一层完善。
