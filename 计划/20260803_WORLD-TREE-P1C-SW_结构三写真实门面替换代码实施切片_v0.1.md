# WORLD-TREE-P1C-SW 结构三写真实门面替换代码实施切片 v0.1

计划身份：WORLD-TREE-P1C-SW

版本：v0.1

创建侧当前代码基线：`488cb76512254ebb81e7ee4ed02dbf9756e4caae`

状态：待激活

## 1. 目标和依赖

在 P1C-F 原模块/类/签名中真实替换创建、移动、移除三个骨架根，消费 P1A3 真实提供者并无损映射结构化结果。其它六根保持未实现。

激活依赖：P1C-F v0.1 代码结果进入正式 main；P1A3 v0.3 真实代码结果原位替换存在/场景骨架并通过其专项矩阵；两者释放 `服务.世界树.ixx`、同一P1C-F自检模块和验证资源。骨架、设计、消息或旧栈实现均不满足。

## 2. 唯一设计链

```text
流程图/20260803_WORLD-TREE-P1C-SW_结构三写真实门面替换业务流程图_v0.1.md
流程图/20260803_世界树业务服务创建存在并接纳到场景_P1C-SW目标函数流程图_v0.1.md
流程图/20260803_世界树业务服务移动世界树成员_P1C-SW目标函数流程图_v0.1.md
流程图/20260803_世界树业务服务移除世界树存在_P1C-SW目标函数流程图_v0.1.md
规范/详细设计/20260803_WORLD-TREE-P1C-SW_结构三写真实门面替换详细设计_v0.1.md
规范/详细设计/函数结构知识图谱/20260803_WORLD-TREE-P1C-SW_结构三写真实门面替换函数结构知识图谱_v0.1.md
```

旧 P1C 大包和旧六写图禁止作为施工依据。

## 3. S0 与允许文件

S0 核对精确计划/blob和可执行状态、main/origin/index/活动代码段；逐签名核对 P1C-F 与 P1A3；确认目标三根仍为固定未实现且无第二入口；确认其它六根与 DTO 不变；确认文件所有权和 P1C-F-S01 顶层身份仍唯一。

允许文件：

```text
海中鱼巣/领域/服务.世界树.ixx
海中鱼巣/领域/自检.世界树业务服务合同骨架.ixx
施工记录/20260803_WORLD-TREE-P1C-SW_施工记录.md
验证记录/20260803_WORLD-TREE-P1C-SW_验证记录.md
```

禁止其它全部文件，特别是规范、设计、计划、索引、provider、共享DTO、工程、启动、旧世界服务、路由、SQL、仓库、事务和图谱。本叶不新增模块或顶层自检，因此不得修改工程/filters/启动聚合。

## 4. 机械实施

1. 保持原模块全局模块片段、include/import、DTO、类、构造、九根声明和静态签名断言不变；不得新增日志 include、日志调用或包装函数。
2. 按详细设计 §§2—5 原位替换三个函数体；状态映射、成功读回校验、代次/持久状态保留逐项落实。
3. E01—E03 只作为防御分支标识；门面返回结构化内部不一致且日志调用0，E02 优先于 E03。最终日志责任归P1D正式运行期入口。
4. 扩充原 P1C-F 自检，只用隔离仓和真实 P1A3 provider 覆盖合法可达路径；不可达坏结果形状按详细设计 §8 做等价源码闭包，不得新增伪provider、第二入口、坏结果工厂、测试seam、顶层身份、顺序或工程模块。
5. 验证其它六根源码、调用边和结果与 P1C-F 骨架逐值一致。
6. 形成专属记录并精确验证。

## 5. 验证矩阵

| 编号 | 场景 | 结果 |
| --- | --- | --- |
| SW01 | 创建规格真实可达状态 + 五状态源码闭包 | 真实入口只覆盖合法可达路径；源码逐项冻结仅已形成+有规格调用场景 |
| SW02 | 场景真实可达状态 + 九状态/未知值源码闭包 | 真实入口覆盖合法路径；源码逐项映射，且仅 provider 内部不一致保留原缺项组 |
| SW03 | 创建成功/幂等 | 目标/新归属/读回/代次完整 |
| SW04 | 移动存在/子场景 | 关系类型、端点、旧新关系完整 |
| SW05 | 移除 | 仅失效归属，存在其它事实保留 |
| SW06 | E01—E03不可达坏形状源码闭包 | 不伪造运行覆盖；内部不一致且保留provider代次/持久状态，门面缺项组空，日志调用0 |
| SW07 | 调用边 | 创建存在1+场景0/1；移动/移除场景各1；其它边0 |
| SW08 | 幂等 | 同义返回原代次/读回；异义冲突 |
| SW09 | 失败原子性 | provider零发布或原已发布事实不被门面改写 |
| SW10 | 其它六根 | 仍固定未实现、调用0、事实变化0 |
| SW11 | 聚合 | 原P1C-F-S01/260及顶层数量不变 |

## 6. 验证与失败收口

```powershell
git diff --check
python .\tools\check_specs.py --strict
msbuild .\海中鱼巣.slnx /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m:1
.\x64\Debug\海中鱼巣.exe --self-test-exit
msbuild .\海中鱼巣.slnx /t:Rebuild /p:Configuration=Release /p:Platform=x64 /m:1
.\x64\Release\海中鱼巣.exe --self-test-exit
```

再以 MSVC 模块扫描及 AST/等价源码闭包证明精确调用边、三根定义唯一、其它六根调用0、日志调用0、五/九状态与未知值唯一映射、E02先于E03以及不可达坏形状的结构化收口。任一 ABI、provider、构造、调用次数、读回、幂等、构建、自检或聚合漂移时保留WIP，返回 `DRIFT-WORLD-TREE-P1C-SW-*`，不得建立第二入口、复制DTO、伪造provider结果、回退旧栈或放宽结果匹配。

## 7. 旧材料与完成边界

P1C-SW、后继特征两写、状态动态一写新版设计/计划全部发布登记后，且在任一真实门面写计划转为可执行前，由旧材料原所有者丢弃未发布旧六写图和旧P1C大设计/计划；若已提交则另作纯文档退役提交。本计划不删除他人WIP。

允许声明：三个结构写门面根真实消费P1A3并通过专项矩阵。禁止声明其它六根、运行期路由、旧入口删除、完整世界树业务服务或服务验收完成。
