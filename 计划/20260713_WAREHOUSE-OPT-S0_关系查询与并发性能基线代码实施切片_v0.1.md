# WAREHOUSE-OPT-S0 关系查询与并发性能基线代码实施切片 v0.1

日期：2026-07-13

队列编号：#257 / 设计队列：DQ-149

状态：待执行 / 独立隔离基线 / 阶段 700 / 不修改生产仓库逻辑 / 未改 C++

## 1. 输入入口

```text
流程图/20260713_数据仓库关系查询二级索引与性能治理流程图_v0.1.md/.html
规范/详细设计/数据仓库关系查询二级索引与性能治理详细设计.md
规范/仓库逻辑空间与领域树结构规范.md
规范/多线程防锁机制规范.md
关系仓库、索引仓库、结构事务和中央自检运行器当前代码
```

本计划是性能事实建立切片，不迁移旧函数，不改变生产关系语义。

## 2. 核心处理

1. 新建真模块 `海中鱼巣/核心/自检.关系仓库性能基线.ixx`，只在隔离仓库夹具中生成 1,000 / 10,000 / 100,000 条可复现关系。
2. 固定随机种子和稀疏 / 密集键分布，覆盖正向、反向、存在、节点相关、审计、创建、失效、重挂、删除。
3. 预热 20 次，正式至少 200 次；记录中位数、P95、P99、吞吐和等待代理值。
4. 用隔离全表参考模型逐项比对关系句柄、版本、状态、端点和顺序。
5. 阶段 700 只登记一次基线自检；入口只导入、登记和汇总，不放基准正文。
6. 形成正式基线记录，明确机器、配置、CPU、电源模式、线程数、种子、提交号和样本分布。

## 3. 结构承载与输出变化

结构承载仅为自检模块的运行期局部对象和值式报告，不进入生产节点、主信息、关系或索引。输出新增性能基线实施记录和阶段 700 自检结果；不新增机器事实。

调用路径：

```text
中央自检运行器 -> 阶段 700 -> 隔离仓库夹具 -> 当前关系仓库公开入口
-> 值式测量结果 -> 全表参考模型复核 -> 人读基线记录
```

## 4. 允许文件

```text
海中鱼巣/核心/自检.关系仓库性能基线.ixx
海中鱼巣/入口.cpp（只允许 import、最小登记和总结果汇总）
海中鱼巣/海中鱼巣.vcxproj
海中鱼巣/海中鱼巣.vcxproj.filters
实施记录/20260713_WAREHOUSE-OPT-S0_关系查询与并发性能基线代码实施_Codex断点清单.md
计划/计划索引.md
项目记忆/Codex任务队列.md
项目记忆/当前状态.md
```

若中央运行器实际登记点不在入口，执行前必须退回设计修订允许文件，不得现场扩权。

## 5. 禁止文件与行为

禁止修改四个生产仓库、结构事务 ABI、领域服务、控制面板、SQL、线程工作队列和 `#214` 任何代码。禁止新增生产缓存、性能状态节点或日志机器事实；禁止用 Debug 日志计时改变运行行为。

## 6. 失败收口

无效夹具参数、规模为零或样本分布不完整在创建前拒绝。构建失败、计时源异常、结果不一致或连续三轮波动超过 15% 均停止基线发布并追根因；不得选择最好一轮作为正式结果。隔离夹具销毁后不得影响默认自检的仓库数量。

## 7. 验收

| 编号 | 验收 |
| --- | --- |
| A01 | 模块为 `ClCompile + export module + import` 真模块 |
| A02 | 1k / 10k / 100k 三档规模和固定种子可重复 |
| A03 | 正向 / 反向 / 存在 / 相关 / 审计查询均有稀疏与密集样本 |
| A04 | 多根、多父共享节点、图关系与普通父子单父边界分开 |
| A05 | 全部结果与参考模型一致 |
| A06 | P50 / P95 / P99、吞吐和等待代理值字段完整 |
| A07 | 明确等待代理值不是内部精确锁等待 |
| A08 | 阶段 700 只登记一次，入口无基准正文 |
| A09 | Debug x64 0 警告 0 错误，完整自检退出 0 |
| A10 | 连续 3 轮基线波动门槛满足，保留原始结果摘要 |
| A11 | 生产仓库、事务、领域和 #214 文件零差异 |
| A12 | 严格规范、`git diff --check`、提交和推送通过 |

## 8. 完成边界

只证明指定机器、提交和隔离夹具的当前基线已建立；不证明生产运行饱和、关系二级索引已实现、令牌优化或分片有必要。
