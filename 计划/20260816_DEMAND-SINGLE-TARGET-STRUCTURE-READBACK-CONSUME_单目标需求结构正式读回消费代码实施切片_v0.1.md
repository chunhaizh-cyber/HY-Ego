# DEMAND-SINGLE-TARGET-STRUCTURE-READBACK-CONSUME 单目标需求结构正式读回消费代码实施切片 v0.1

日期：2026-08-16
状态：待激活

## 1. 目标与完成条件

实现 BIZ-L4-004-02-01-06 v0.2：接收05已发布 / 精确重复的需求身份和原G1，先经既有运行期事实版本服务取得调用时当前守卫G读，再以G读守卫、G1历史截止恰一次调用DATA-EXT-12完整历史读，返回一个G1值式完整需求供父函数与05首次请求见证互证。

完成必须证明G读与G1分账、完整目标 / 来源 / 授权 / 可空父 / 全部路径 / 生命周期 / 版本一次读回、非成功零部分载荷，以及零L1、owner、私有幂等账、DATA写、缓存、重试和5150回执写。不得声明父提交整体或需求树闭环。

## 2. 激活门禁与当前事实

当前正式代码只有`L2需求身份`和需求节点族，没有`L2需求结构服务`、需求DTO、普通应用取得路径、`服务.需求.ixx`或本叶函数。本计划必须保持待激活，直到：

- `RUNTIME-FACT-CURRENT-CUTOFF-CONSUME v0.1`真实结果 / 退出 / 清理并释放最终DTO；
- `DEMAND-CURRENT-SAME-PARENT-TARGET-CONSUME v0.2`和`DEMAND-SINGLE-TARGET-STRUCTURE-PUBLISH-CONSUME v0.1`真实结果 / 退出 / 清理并释放共享完整需求类型、05结果和生产文件；
- DATA-EXT-12唯一服务、普通应用取得路径、以独立G读守卫读取原G1完整历史结构的最终DTO / 状态 / 预算 / 排序ABI发布；
- 04 / 05及DATA-EXT-12正式合同补齐当前状态材料、满足关系、路径权重、AND份额 / 余量版本和预算来源，05首次请求见证与06历史投影可以逐字段互证；
- 从最新main重做S0，计划blob、详细设计blob、实际ABI、允许范围和资源精确匹配。

接口声明、未实现骨架、测试替身或“DATA能力未就绪”运行分支不能解除真实接线门禁。

## 3. 允许文件

```text
海中鱼巣/领域/需求结构正式读回.数据.h
海中鱼巣/领域/服务.需求.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
施工记录/20260816_DEMAND-SINGLE-TARGET-STRUCTURE-READBACK-CONSUME_单目标需求结构正式读回消费施工记录_v0.1.md
验证记录/20260816_DEMAND-SINGLE-TARGET-STRUCTURE-READBACK-CONSUME_单目标需求结构正式读回消费检查记录_v0.1.md
验证工具/单目标需求结构正式读回消费验证.cpp
验证工具/单目标需求结构正式读回消费验证.vcxproj
验证工具/运行单目标需求结构正式读回消费验证.ps1
```

只有上述路径可由本计划修改。执行前若02 / 05已在相同合同头形成共享DTO，必须机械复用，不得复制同义类型；必要的最终物理合并必须由正式设计版本先裁决，不能由执行者临场选文件。

## 4. 禁止范围

禁止修改DATA、L1、owner、运行期事实版本服务、最终需求清单、规范、目标图、其它计划、计划索引、任务 / 方法 / 线程、SQL / 恢复和异主WIP。禁止新增第二需求服务、第二当前截止provider、公开幂等账读取、DATA转发壳、缓存、自动重试或运行态“DATA未就绪”旁路。

## 5. 实施步骤

1. 按详细设计建立06请求、状态、成功材料、结果和纯值helper；函数名 / 变量名使用中文。
2. 扩展同一`需求业务服务`，借入既有`运行期事实版本服务`和普通应用同实例唯一`const L2需求结构服务&`；构造零事实读取。
3. 坏合同、运行 / 范围版本、需求、G1或预算在任何下层调用前拒绝。
4. 调用运行期事实版本服务取得非零G读；互证运行 / 范围版本及`G读>=G1`。
5. 形成DATA历史读请求：请求守卫G读、历史截止G1、需求身份、完整版本和两个独立预算。
6. 恰一次调用DATA-EXT-12完整历史读；零DATA写、零重试、零截止重选。
7. 精确映射漂移、未找到、已退出、预算、许可、资源和未实现；坏状态 / optional / 截止 / 版本收敛内部不一致。
8. 完整互证需求族身份来源、单目标、当前状态材料与满足关系、可空父、全部来源 / 授权 / 路径及其权重 / 分配 / 预算、生命周期、稳定排序和每项G1后一次值式构造结果。
9. 为父BIZ保留05首次请求见证与06结构的逐字段互证接口；父级首次发布先验证`G0 < UINT64_MAX && G1 == G0 + 1`，精确重复验证沿用原G0 / G1；06 DATA请求本身不得携带BIZ见证。
10. 所有异常按详细设计收口，任何失败零部分需求投影。

## 6. 验证

生产外专项至少覆盖详细设计§8矩阵，特别是：G读=G1、G读>G1、精确重复旧G1、G读取得后漂移、G读<G1、两个预算、完整多来源 / 多路径、父空 / 非空路径基数、坏排序 / 重复 / 混代、非成功optional守恒，以及父级05—06建立 / 补充 / 精确重复互证。

验证必须执行：

```powershell
msbuild .\海中鱼巣.slnx /t:Rebuild /p:Configuration=Debug /p:Platform=x64
msbuild .\海中鱼巣.slnx /t:Rebuild /p:Configuration=Release /p:Platform=x64
python .\tools\check_specs.py --strict
git diff --check
```

并检查UTF-8无BOM、工程唯一登记、生产零专项痕迹、目标白名单和禁止import。专项替身还必须覆盖当前状态材料 / 满足关系、路径权重 / AND份额 / 余量版本 / 预算来源以及首次`G1=G0+1`和精确重复原G0/G1守恒；它只验证BIZ协议，不能宣称真实DATA历史读、真实并发漂移、05生产接线或业务闭环。

## 7. 失败与漂移

纯代码、编译和专项问题由执行者在白名单内修复。若最终DATA接口把原G1当当前守卫、不能使用独立G读读取G1完整历史结构、把全部路径退化为单数、截断来源 / 授权 / 路径，或要求BIZ读取L1幂等账 / owner / 内部事务，固定以`DRIFT-DEMAND-SINGLE-TARGET-READBACK-DATA-CONTRACT`零扩权退回计划支撑。

## 8. 完成声明边界

本计划未来完成只证明06能可靠读取05原G1对应的完整需求结构，并把独立值式投影交父函数互证。04路径角色 / 防环、父`提交或复用单目标需求`整体、DATA-EXT-12整项、需求树、任务化、结算、恢复和系统业务闭环均为`NOT_RUN`。
