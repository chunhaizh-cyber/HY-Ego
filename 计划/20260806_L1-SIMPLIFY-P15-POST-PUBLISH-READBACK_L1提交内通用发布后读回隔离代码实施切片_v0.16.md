# L1-SIMPLIFY-P15 G1—G8 领域意图确定性编码代码实施切片 v0.16

计划身份：`L1-SIMPLIFY-P15-POST-PUBLISH-READBACK`

版本：`v0.16`

创建侧基线：`9ec7a17dbacc0912b9bd31c2f3168a54c53d6709`

替代：P15 v0.15；具名漂移：`DESIGN-DRIFT-P15-V015-G1-G8-PREPROBE-INTENT-BYTE-ENCODING-UNBOUND`

## 1. 目标与禁止范围

只实现详细设计和流程图冻结的 G1—G8 领域意图摘要编码、provider 前探测输入、G0 独立互证和恢复拒绝。保留 P15 v0.15 的 P-L1/P-I/P-S 身份、合同2、模式、调用/去重、32/18/50、16/50 和 24 文件白名单。不得修改 P62/P63、P0-FIX、路线、旧 WORLD-TREE、其它 WIP 或新增第二状态/关系20/通用历史查询。

计划不把当前代码的 G0 覆盖行为当作已实现；执行者必须按本版本重做 S0 后再施工。

## 2. 精确白名单和接口

只允许 v0.15 的 24 文件白名单。公共 `写U8/写U32/写U64/写I64/写稳定编码/写引用/写可选/写变体/写向量/写数组/写字段头/哈希` 只在 `核心/L1事实基座.数据.h`；G1—G8 函数物理模块分别为世界登记、场景结构、特征定义、实例特征、实际存在、状态、特征比较服务模块，G0/核心互证在核心数据头和 `核心/服务.L1事实基座.ixx`。不新增文件、工程、filters、启动或顺序登记。

`L1写集请求` 需增加/冻结 `请求意图组(U8)`、`请求意图格式版本(U32)`、`请求意图操作标签(U32)` 值式字段并保留 32 字节摘要；`准备L1写集v2` 接受调用方 `L1领域意图凭证`，不得从写集形成或覆盖领域摘要。`写写集` 不编码这四个互证字段，G0 摘要必须与领域摘要分离。

## 3. 实施步骤

1. 从正式 HEAD 重扫 DTO 和白名单，逐字段对照 v0.16 详细设计 §4；发现字段/标签/物理位置不一致时退回计划支撑，不临场改机器语义。
2. 在核心实现唯一公共原语和字段帧；所有整数大端、长度 U32、optional/variant/vector/array 显式标记和数量，稳定编码/事实见证使用固定字段序列。
3. 在 G1—G8 指定服务模块实现唯一摘要函数；函数不接受自由操作标签，自检调用复用生产函数；G5 继续只形成中性 G0 写集摘要。
4. 改造所有生产/自检调用点为“领域 DTO 编码→provider 前探测→未找到后读取 provider→独立 G0 摘要→核心互证→一次提交”。调用点保持 32/18/50 和既有标签，不把 provider/结果/执行证据写入请求意图。
5. 核心验证组号、格式版本、固定标签、幂等键、领域摘要非零和 G0 独立一致；任何 G0 覆盖、摘要不匹配、错误端序或缺字段均入口拒绝、零写。
6. 更新首次执行证据、幂等账、审计和恢复重算，使领域摘要与 G0 摘要分别可验证；同义重复 provider/提交为 `0/0`，异义/失败/隔离无业务载荷。

## 4. 矩阵与测试向量门禁

必须逐点保留 v0.15 的 32 个生产/业务场景、18 个自检调用行和 50 个物理 `.提交写集(` 调用点；执行侧不得新增或删除调用点。机械门禁分别报告 `32/32`、`18/18`、`32→50`、`16/50`、`24 文件`，不能写成“50 行矩阵”。

编码门禁必须覆盖：

- G1—G8 每个 DTO 的字段号、类型、顺序、固定域前缀/组号/操作标签/格式版本；
- U8/U32/U64/I64 大端、稳定编码、嵌套、optional、variant、vector、array、事实见证；
- `G1-min` 期望 SHA-256 `e8a069b8d3bb3da75aed2d92b16b22932c2682c7686312b4c882deec6f7c7b0b`，G1 规则字段突变摘要必须不同；
- G2 nested、G4 optional/vector、G8 optional/variant、G0 与领域摘要不匹配拒绝；
- 同义请求摘要相等，任一公开字段差异摘要不等，核心不覆盖调用方摘要；
- 恢复缺字段、错误组/版本/标签、错误端序、长度溢出、双账或读回矛盾拒绝/隔离。

## 5. 验证、施工记录和完成边界

创建侧在发布前运行 `git diff --check`、`python .\tools\check_specs.py --strict`、UTF-8/NUL/尾随空白/占位词检查、三文档 no-index diff check，并机械核对 8 组字段矩阵、公共原语唯一、物理位置、测试向量、32/18/50、16/50 和 24 文件白名单。执行侧必须另行运行 Debug/Release 编译、完整自检、provider 前探测、首次/同义/异义、恢复和发布后读回；这些不由计划预先宣称通过。

专属施工和验证记录仍由执行侧在其 P15 WIP 归属路径维护；本计划不接管代码 WIP。

最多声明：v0.16 设计合同已冻结并可重新 S0；不声明代码实现、构建、自检、生产接线、恢复运行、P62/P63 或治理闭环完成。发布时 P15 v0.15 在索引中标记被 v0.16 替代/禁止施工，v0.16 登记为待激活；不得由本计划自行激活。
