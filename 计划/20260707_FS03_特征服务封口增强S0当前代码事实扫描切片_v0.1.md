# FS-03 特征服务封口增强 S0 当前代码事实扫描切片 v0.1

日期：2026-07-07
状态：已完成 / 只读扫描 / 未改代码 / 未迁移旧函数

## 1. 任务定位

本切片承接已确认的 `计划/20260706_FS03_特征服务封口增强专项_v0.1.md`，只执行 FS-03 特征服务封口增强进入代码实施前的当前代码事实扫描。

本切片不是代码实施切片，不允许修改 C++，不允许迁移旧函数体，不允许新增旧主信息字段。扫描完成后，若要进入 S1 到 S4 的代码实施，必须另建代码实施切片并再次确认允许文件、禁止文件和验证方式。

## 2. 扫描目标

本切片必须只读确认以下事实：

```text
特征服务当前公开入口和内部依赖。
特征值服务当前公开入口和可被直接依赖的风险。
节点类型和关系类型中与特征、特征值、归属关系相关的枚举事实。
需求、任务、方法、动态等调用点是否直接依赖特征值服务。
当前入口样例中是否已有特征服务封口相关验收输出。
```

## 3. 允许读取范围

```text
海中鱼巣/领域/特征服务.h
海中鱼巣/领域/特征值服务.h
海中鱼巣/核心/句柄.h
海中鱼巣/领域/需求服务.h
海中鱼巣/领域/任务服务.h
海中鱼巣/领域/方法服务.h
海中鱼巣/领域/动态服务.h
海中鱼巣/入口.cpp
说明书/函数功能说明文档.md
规范/详细设计/特征服务封口增强详细设计.md
计划/20260706_FS03_特征服务封口增强专项_v0.1.md
```

可使用 `rg` 检索以下模式：

```text
特征值服务
写入I64特征值
读取I64特征值
创建特征值
特征值属于特征
节点类型::特征
节点类型::特征值
关系类型::归属
```

## 4. 禁止范围

```text
禁止修改任何 C++ 文件。
禁止修改工程文件、解决方案文件或构建配置。
禁止读取后直接补函数实现。
禁止迁移旧函数体。
禁止新增旧特征主信息、旧抽象特征主信息或旧特征值主信息字段。
禁止让需求、任务、方法直接访问特征值服务。
禁止接 Vec / 哈希 / 稳态 / 值域 / 序列化 / 体素 / 外设 / 控制面板 / SQL。
禁止把日志、控制台输出、注释或说明文本当作机器事实。
```

## 5. 输出文件

扫描完成后已生成：

```text
实施记录/20260707_FS03_特征服务封口增强S0当前代码事实扫描_Codex断点清单.md
```

该断点清单至少包含：

```text
已读取文件。
当前已有入口。
缺失入口。
当前调用点依赖事实。
直接依赖特征值服务的风险点。
能否进入 S1 代码实施切片设计。
仍需用户确认的问题。
禁止扩大声明。
```

## 6. 验证方式

本切片只做文档和只读扫描验证：

```powershell
git diff --check
if (Test-Path tools/check_specs.py) { python .\tools\check_specs.py }
git status --short --branch
```

本切片不运行 C++ 构建，不运行默认入口。只有后续代码实施切片才允许按确认范围运行：

```powershell
msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m
.\x64\Debug\海中鱼巣.exe
```

## 7. 完成声明边界

完成本切片后只允许声明：

```text
FS-03 特征服务封口增强 S0 当前代码事实扫描已完成。
已获得后续代码实施切片所需的当前代码事实。
未修改 C++。
未迁移旧函数体。
未新增旧主信息字段。
```

禁止声明：

```text
特征服务封口增强代码已实现。
旧特征 / 特征值能力已迁移。
Vec 原始值、内容哈希、稳态、抽象特征、值域系统、序列化恢复、体素或外设链路已实现。
C++ 构建或入口拒绝已通过本切片验证。
```

## 8. 执行结果

```text
S0 当前代码事实扫描已完成。
输出文件：实施记录/20260707_FS03_特征服务封口增强S0当前代码事实扫描_Codex断点清单.md
后续进入 S1 到 S4 前，仍需先裁决 S0 断点清单中的待确认问题，并另建代码实施切片。
```
