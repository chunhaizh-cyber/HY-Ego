# 概念生命周期退出与投影代码实施切片 v0.1

日期：2026-08-12

计划身份：`CONCEPT-SPACE-STRUCTURE-CRUD-REBUILD-C4`

状态：`待激活`；设计完成，不等于实施完成

## 1. 目标与依赖

原位扩展概念服务，完成活跃 / 冷却 / 退役迁移、不可逆退出、中间概念必要替代边、生命周期历史和有界维度投影。依赖 C2、C3、N1 的真实结果；N1 必须先提供零当前名称证明，C3 提供零支持证明。

## 2. 允许范围

只改概念数据头 / 服务，新增三份 `L2概念生命周期退出投影参数验证` 临时文件和两份 CONCEPT-C4 记录，共七路径。不得修改名称 / 世界 / 语言服务、普通应用、工程或索引。

## 3. 接口和事务

公开函数固定为：

```text
迁移概念生命周期
不可逆退出概念
读取概念生命周期历史
读取概念维度有限展开投影
```

迁移请求显式携带旧生命周期事实编码和新状态，只允许 7140 四条合法边；维度根拒绝。退出只接受退役普通概念，并逐项证明零当前支持、零当前名称、零概念结构外引用和完整当前签名 / 定义 / 上下位 / 生命周期。在当前图副本中删除目标，针对每个上位 × 下位组合仅在无其它可达路径时形成替代直接边；一份概念 owner 写集建立替代边并退出目标的签名、定义、上下位、生命周期、节点和族归属。世界事实、词条、名称历史不退出。投影为有界值式读取，不持久化。

## 4. 验证与声明

覆盖全部合法 / 非法迁移、根不可退、名称 / 支持 / 外部引用阻断、中间节点替代边、有替代路径时不增冗余边、历史完整、清空投影后等价。不得声明物理删除、概念业务判断或旧模型迁移完成。
