# SERVICE-DATA-S2 状态动态服务分层迁移代码实施切片 v0.1

日期：2026-07-13

计划编号：#266

队列编号：DQ-158

最终回归阶段：730

状态：依赖门控待执行 / JY-320 / 前置 #272、DQ-164、725 / #265、720 已满足 / 未改 C++

## 1. 依赖门控

### 1.1 已满足前置

```text
#263 / CORE-LAYER-S1 / 700：已完成并归档
#264 / SERVICE-DATA-S1 / 710：已完成并归档
#265 / EXIST-SCENE-S1 / 720：已完成并归档
```

### 1.2 未满足前置

```text
#272 / DQ-164 / CORE-SESSION-S2 / 725
计划/20260713_CORE-SESSION-S2_不透明结构写入会话材料能力扩展代码实施切片_v0.1.md
预期正式产物：实施记录/20260713_CORE-SESSION-S2_不透明结构写入会话材料能力扩展代码实施_Codex断点清单.md
```

`#272` 完成前，本计划只登记为依赖门控待执行。执行窗口必须跳过 `#266`，先实施 `#272`。

## 2. 上游范围一致性

### 2.1 流程图

```text
流程图/20260713_状态动态服务分层迁移代码逻辑流程图_v0.1.md
流程图/20260713_状态动态服务分层迁移代码逻辑流程图_v0.1.html
```

### 2.2 详细设计

```text
规范/详细设计/状态动态服务分层迁移详细设计.md
```

### 2.3 当前事实

```text
实施记录/20260713_ARCH-LAYER-S0_仓库服务双路径与调用点事实复核_Codex断点清单.md
实施记录/20260713_EXIST-SCENE-S1_存在场景首组垂直样例代码实施_Codex断点清单.md
海中鱼巣/领域/状态服务.h
海中鱼巣/领域/动态服务.h
```

流程图、详细设计和本计划均只覆盖五条隔离新路径，不改接兼容生产调用，不实施删除、提升、聚合或稳定因果。

## 3. 已验证当前事实

1. `#265` 已证明 `业务服务 / 组合器 -> 服务专用数据操作层 -> #264 执行器` 的隔离模式可用。
2. 当前状态服务使用主信息槽位 0/1/2/3 承载值、时间戳和幂等编号双槽。
3. 当前实例状态使用运行期临时和引用关系承载场景、主体和场景材料。
4. 当前实例动态使用运行期临时、引用和有序因果来源关系承载场景、主体、目标、前后状态和可选来源动作。
5. 当前动态服务仍有直接创建裸值材料状态的兼容路径；新分层不得复用。
6. 当前 `#264` 会话尚无候选值写入和同许可结构材料读取；由 `#272` 先补。

## 4. 假定接口契约

执行 `#266` 时假定 `#272` 已实际提供并验证：

```cpp
结构写入结果 写入候选I64值(主信息句柄, std::uint64_t, std::int64_t);
bool 候选I64值匹配(主信息句柄, std::uint64_t, std::int64_t);
std::optional<节点类型> 读取节点类型(节点句柄) const;
std::optional<主信息句柄> 读取节点主信息(节点句柄) const;
std::optional<std::int64_t> 读取I64值(主信息句柄, std::uint64_t) const;
std::vector<关系记录> 读取关系记录组(节点句柄, 关系类型) const;
```

这些是依赖契约，不是当前实现事实。若 `#272` 的实际名称、返回状态、可见性、值写集读回或撤销语义不一致，不得修改 `#266` 代码，必须退回设计窗口修订流程图、详细设计和计划。

## 5. 实施范围

固定五条路径：

```text
1. 创建抽象状态。
2. 创建完整实例状态。
3. 创建抽象动态。
4. 使用两个已有完整实例状态记录状态变化动态。
5. 由状态业务服务形成前后状态规格，在同一会话内创建两状态并记录实例动态。
```

## 6. 新模块分类

| 文件 | 类型 | 工程登记 | 调用方式 |
| --- | --- | --- | --- |
| `海中鱼巣/领域/数据操作.状态动态.ixx` | 生产真模块 | `ClCompile` | 唯一生产模块导入 `海中鱼巣.核心.执行器.结构写入` |
| `海中鱼巣/领域/服务.状态.ixx` | 生产真模块 | `ClCompile` | `import 海中鱼巣.领域.数据操作.状态动态` |
| `海中鱼巣/领域/服务.动态.ixx` | 生产真模块 | `ClCompile` | 导入状态业务服务和状态动态数据操作 |
| `海中鱼巣/领域/组合.状态动态.ixx` | 生产真模块 | `ClCompile` | 导入状态业务服务和状态动态数据操作，不导入执行器 |
| `海中鱼巣/领域/自检.状态动态分层.ixx` | 自检真模块 | `ClCompile` | 隔离夹具与 A01-A18；不进入生产依赖 |

`入口.cpp` 只增加自检导入、一个值式结果、阶段 730 登记和总汇总。

## 7. 允许文件

### 7.1 C++ 与工程

```text
海中鱼巣/领域/数据操作.状态动态.ixx
海中鱼巣/领域/服务.状态.ixx
海中鱼巣/领域/服务.动态.ixx
海中鱼巣/领域/组合.状态动态.ixx
海中鱼巣/领域/自检.状态动态分层.ixx
海中鱼巣/入口.cpp
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
```

### 7.2 收尾治理

```text
本计划及其已完成归档路径
计划/计划索引.md
项目记忆/Codex任务队列.md
项目记忆/当前状态.md
项目记忆/决策记录.md
项目记忆/验收记录.md
项目记忆/窗口交互记录.md
实施记录/20260713_SERVICE-DATA-S2_状态动态服务分层迁移代码实施_Codex断点清单.md
```

## 8. 禁止文件

```text
海中鱼巣/核心/会话.结构写入.ixx
海中鱼巣/核心/执行器.结构写入.ixx
海中鱼巣/核心/主信息仓库.h
海中鱼巣/核心/主信息仓库.cpp
海中鱼巣/核心/节点仓库.h
海中鱼巣/核心/节点仓库.cpp
海中鱼巣/核心/关系仓库.h
海中鱼巣/核心/关系仓库.cpp
海中鱼巣/核心/索引仓库.h
海中鱼巣/核心/索引仓库.cpp
海中鱼巣/核心/结果.结构写入.h
海中鱼巣/核心/结构事务接线.数据.h
海中鱼巣/核心/协调.结构事务.ixx
海中鱼巣/领域/状态服务.h
海中鱼巣/领域/动态服务.h
海中鱼巣/领域/数据操作.存在场景.ixx
海中鱼巣/领域/服务.存在.ixx
海中鱼巣/领域/服务.场景.ixx
海中鱼巣/领域/组合.存在场景.ixx
海中鱼巣/线程/**
海中鱼巣/界面/**
海中鱼巣/持久化/**
```

不得把 `#272` 的会话修改夹带进本切片；发现缺口必须退回设计。

## 9. 实施步骤

### S0 执行前接口复核

1. 读取 `#272` 已完成计划和正式实施记录。
2. 逐签名比对第 4 节假定接口。
3. 重读 `状态服务.h`、`动态服务.h` 的槽位、关系类型和顺序号。
4. 复核五个新模块名、工程项和阶段 730 未占用。
5. 确认禁止文件相对基线无待合并修改。

任一不一致时保持代码零修改并退回设计。

### S1 状态数据操作和业务服务

实现抽象状态、完整实例状态、权威材料读取、实例状态写入规格和幂等映射。

### S2 动态数据操作和业务服务

实现抽象动态、已有状态变化动态、目标 / 来源动作准入和权威材料读回。禁止调用或复制裸值材料状态创建逻辑。

### S3 状态动态组合器

由状态业务服务形成两个规格；数据操作层在同一会话创建两个完整状态和一个实例动态。

### S4 自检

实现 A01-A18，包含每个写点失败注入、数量快照、并发竞争、版本漂移和上游完整回归。

### S5 入口与工程

登记五个 `ClCompile` 真模块。入口只增加最小装配、一次自检调用、阶段 730 和总汇总。

### S6 收尾

验证通过后写正式实施记录、归档计划、同步项目记忆、提交并推送。不得顺带实施 `#267`。

## 10. 失败收口

```text
入口拒绝：第一笔写入前返回，结构不变化。
许可内当前性复核失败：第一笔写入前返回版本漂移。
写入或读回失败：禁止请求提交，由 #272 会话精确逆序撤销。
并发冲突：撤销并释放许可后权威重读；完全同一事实才返回幂等读回。
撤销证明：主信息、节点、关系、索引和数量快照回前态。
无法证明：停止 #266，登记断点；#267-#271 继续门控。
```

内部不一致必须使用项目公共追根因检查路径，不能只返回空句柄继续推进。

## 11. 验收

执行详细设计 A01-A18，并运行：

```powershell
msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m
.\x64\Debug\海中鱼巣.exe --self-test-exit
python .\tools\check_specs.py --strict
git diff --check
```

最终连续 20/20 完整自检通过后才可登记 730。排除项扫描至少包含：

```text
结构事务令牌
结构事务许可
std::thread
std::async
工作队列
控制面板
SQL
ADO
WebView
D455
体素
外设
```

`std::thread` 只允许出现在独立自检并发夹具或既有依赖中，不得进入四个新生产模块。

## 12. 完成声明

完成时必须报告：

```text
输入入口：五类无令牌状态动态业务请求。
核心处理：入口准入、状态规格、一次独占会话、多仓库写入、读回和撤销。
结构承载：现有主信息槽位、状态 / 动态节点、权威关系和主键索引。
输出变化：五个真模块、入口最小登记和阶段 730。
调用路径：业务服务 / 组合器 -> 数据操作.状态动态 -> #272 执行器 / 会话 -> 四仓库。
验证方式：A01-A18、Debug x64、完整自检、严格规范、禁止范围扫描和连续 20/20。
```

本计划完成不证明既有生产调用迁移、兼容路径收口、状态动态全部能力或旧能力迁移完成。`#214` 继续用户暂停，`#257` 继续等待 `#271`。
