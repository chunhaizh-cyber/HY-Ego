# SERVICE-DATA-S2 状态动态服务分层迁移代码实施切片 v0.1

日期：2026-07-13

队列编号：#266 / 设计队列：DQ-158

状态：依赖门控待执行 / 前置 #265、720 / 最终回归 730 / 未改 C++

## 1. 范围与假定

按 #262 矩阵迁移状态 / 动态的实例与抽象创建、时间戳、场景归属、前后值、幂等、来源动作 / 方法、候选确认撤销。假定存在场景新服务提供无令牌业务材料，#265 正式接口稳定；漂移退回设计。

## 2. 新模块

`领域/数据操作.状态动态.ixx`、`领域/服务.状态.ixx`、`领域/服务.动态.ixx`、`领域/组合.状态动态.ixx`、`领域/自检.状态动态分层.ixx`，均为 `ClCompile + export module + import` 真模块。

## 3. 允许与禁止

允许上述模块、既有状态 / 动态服务头的兼容适配、必要的存在场景只读端口、工程 / 自检 / 治理。禁止特征、需求、任务、方法、概念命名、线程、`#214/#257`；禁止改变时间戳、实例 / 抽象边界和轻量因果口径。

## 4. 失败与验收

最后发布前任何主信息、节点、关系、状态值或动态材料失败必须无残留；最后发布后读回异常追根因。A01-A16、逐写点故障注入、并发唯一性、旧入口回归、公开面无令牌 / 仓库、Debug x64 0/0、完整自检、连续 20 轮、严格规范、提交推送。

## 5. 完成边界

不证明完整状态 / 动态 / 因果系统完成。
