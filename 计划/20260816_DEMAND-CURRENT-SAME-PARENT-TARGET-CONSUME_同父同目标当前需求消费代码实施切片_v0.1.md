# DEMAND-CURRENT-SAME-PARENT-TARGET-CONSUME 同父同目标当前需求消费代码实施切片 v0.1

日期：2026-08-16
版本：v0.1
身份：`DEMAND-CURRENT-SAME-PARENT-TARGET-CONSUME`
状态：待激活

## 1. 目标与完成条件

按 BIZ-L4-004-02-01-02 v0.2 与同身份详细设计，在唯一 `需求业务服务`中实现同父同目标当前需求读取：以需求主体、可空父、目标宿主、目标特征定义、权威目标状态合同引用和目标状态语义键构造一次 DATA-EXT-12 有界组读，按同一非零 G0 逐字段互证并形成不存在、唯一当前需求或多个当前同目标。

本计划不复核正差距或来源资格，不写需求，不实现父路径、防环、合并、任务树或普通应用最终装配。

## 2. 正式依据与执行前事实

- 规范：0050、3100、4030、5100、5110、CODE-FILE-01；
- 目标图：`流程图/目标业务流程图/L4/BIZ-L4-004-02-01-02_读取同父同目标当前需求_目标函数流程图_v0.2.md`；
- 详细设计：`规范/详细设计/20260816_DEMAND-CURRENT-SAME-PARENT-TARGET-CONSUME_同父同目标当前需求消费详细设计_v0.1.md`；
- DATA-EXT-12 当前只冻结服务族边界；本叶需要的“按主体父需求与目标结构材料有界读取当前需求组”、跨目标合同 / 比较注册身份的同语义键组读及语义键回显尚未进入数据服务最终需求清单，最终需求结构 DTO / 状态 / C++ ABI 也未发布；
- `L2目标状态合同身份`已实现，需求结构 owner / 服务与本 BIZ 服务尚未实现；
- 拓扑第一叶候选准入的来源权威合同仍待后继裁决，不影响本只读叶独立实现。

执行前重新 S0：核对本计划状态；数据服务最终需求清单已登记 DATA-EXT-12 中性组读操作及请求 / 结果语义；最终组读、空组 / 预算 / G0 和完整需求 DTO已发布，且正式支持权威目标合同 / 比较注册引用互证与跨引用身份的同语义键完整组读；普通应用存在同一 L1 运行包的合法取得路径；首次建立 `服务.需求.ixx`的前驱已结果 / 退出 / 清理；共享文件、工程、唯一代码段和构建资源已释放。

## 3. 允许文件

生产与工程：

1. `海中鱼巣/领域/需求同父目标读取.数据.h`；
2. `海中鱼巣/领域/服务.需求.ixx`；
3. `海中鱼巣.vcxproj`；
4. `海中鱼巣.vcxproj.filters`。

永久记录：

5. `施工记录/20260816_DEMAND-CURRENT-SAME-PARENT-TARGET-CONSUME_同父同目标当前需求消费施工记录_v0.1.md`；
6. `验证记录/20260816_DEMAND-CURRENT-SAME-PARENT-TARGET-CONSUME_同父同目标当前需求消费验证记录_v0.1.md`。

生产外临时专项：

7. `验证工具/同父同目标当前需求消费验证.cpp`；
8. `验证工具/同父同目标当前需求消费验证.vcxproj`；
9. `验证工具/运行同父同目标当前需求消费验证.ps1`。

禁止修改 DATA 生产文件、L1、owner、普通应用、其它 DEMAND DTO、正差距 / 来源 / 父路径服务、线程、正式规范、流程图、详细设计、计划和计划索引。

## 4. 实施步骤

1. 按 CODE-FILE-01 新建传统 DTO 头，冻结版本、固定配置、请求、9个稳定状态、结果和纯值 helper；只有唯一当前需求携带完整 optional 值。
2. 串行扩展同一 `海中鱼巣.领域.服务.需求`模块；服务继续只持唯一 DATA-EXT-12 const 引用和不可变配置，不新增第二 owner / 服务 / getter。
3. 复用根组固定配置并增加不重复共同字段的同父目标结构规则配置；扩展 `需求业务服务`构造签名，校验两配置、请求和全部版本匹配。按详细设计构造主体 + 可空父 + 宿主 + 特征 + 权威目标合同引用 + 目标状态语义键的精确组读请求；运行代次只参与 BIZ 根组固定配置匹配，不进入 DATA 请求。
4. 恰一次调用 DATA 组读，互证请求回显、G0、全部范围 / 结构规则版本、预算、optional和每项当前完整形状；零项 / 一项 / 多项分别形成三个稳定结果，其中多项是供父级精确收敛的具名内部矛盾，不是普通成功。
5. 目标状态语义键固定为完整 `L2特征比较I64合同材料`、目标 I64、允许关系位和目标状态合同规则版本；目标合同与比较注册身份都只作权威引用。不同引用身份但同一语义键必须进入同一候选组。唯一项完整保留权威引用与语义键、目标场景、身份来源、来源 / 授权、父关系、生命周期和结构版本；不以来源、时间或场景二次筛选，不在本叶裁决区间 / 宽泛方向细化、近似裸值或文本等价。
6. 按详细设计映射漂移、预算、许可、资源、首笔请求拒绝 / 引用冲突、运行态未实现 / 未装配、坏形状和异常；首笔请求拒绝 / 引用冲突返回 BIZ 请求拒绝，运行态未实现 / 未装配收敛内部不一致，不暴露正常“未实现”业务状态。全部非唯一结果零业务载荷，零重试 / 缓存 / 截止重选。
7. 工程唯一登记生产文件；以生产外专项覆盖矩阵，填写两份永久记录。结果发布 / 索引退出后独立删除三件临时专项并重做根工程和 strict 清理验证。

## 5. 验证与成功标准

- 根工程 x64 Debug / Release Rebuild PASS；
- 空组、唯一项和多个当前匹配项精确分账，多个时不任选第一项且父级只能收敛为内部不一致；
- 来源、时间和场景差异不进入精确键；不同目标合同 / 比较注册身份但同一目标状态语义键必须同组，不同语义键不合并；语义键不是第二权威；
- 唯一项完整携带权威目标合同引用与语义键、目标场景、来源 / 授权、父关系、生命周期、身份来源、全部结构版本和同一 G0；
- 根组配置唯一拥有运行 / 范围 / 读取公共字段，同父目标配置只拥有六个结构规则版本；请求与两配置逐字段匹配闭合。请求拒绝、漂移、预算、许可、资源、运行态未实现 / 未装配、内部不一致及异常守恒按详细设计闭合；只有完整已读取零 / 一 / 多分支回显 G0，其余状态事实截止为0；无法由公开 ABI 构造的坏形状保持 `NOT_RUN`；
- 源码只有一次 DATA 组读，零 DATA 写入 / L1 / owner / 仓库 / 历史拼装 / 缓存 / sleep / retry；
- 工程登记唯一，DATA / 普通应用 / 其它 DEMAND 文件零差异，生产零专项引用和验证痕迹；
- `git diff --check`、`python .\tools\check_specs.py --strict`、UTF-8无BOM和精确白名单通过。

## 6. 激活、发布与边界

本计划只能在数据服务最终需求清单登记中性组读操作、DATA-EXT-12 最终组读 ABI（含权威目标合同 / 比较注册引用互证与跨引用身份的同语义键完整组读）、普通应用合法取得路径和首个 `需求业务服务`前驱真实发布后，由索引所有者从最新正式 HEAD 机械匹配并提升为可执行。待激活不授权施工。

若最终接口改变精确复用键、目标场景非身份语义、空组 / 预算、零 / 一 / 多分支、完整需求形状、服务归属或结果 optional，固定具名 `DRIFT-DEMAND-CURRENT-SAME-PARENT-TARGET-KEY-OR-ABI`并退回计划支撑；执行者不得临场增加目标等价算法、按场景分裂需求、直达 L1或复制 DATA CRUD。

代码结果与构建通过最多证明本只读定位叶。候选准入、来源授权、父路径、防环、需求发布 / 合并、任务树、T03 接线和系统业务闭环保持 `NOT_RUN`。
