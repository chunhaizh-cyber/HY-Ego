# 特征概念生成复用与长期沉淀代码实施计划

身份：`FEATURE-CONCEPT-APPLICATION`；版本：v0.4；日期：2026-09-05。沿用原文件路径，专属代码/记录白名单不变。

当前状态只由 [计划索引](计划索引.md) 的精确版本登记维护。本计划与 [详细设计](../规范/详细设计/20260905_FEATURE-CONCEPT-APPLICATION_特征概念生成复用与长期沉淀详细设计_v0.1.md) 共同构成一份代码闭环，不拆成 DTO、provider 和应用三个库存计划。

## 1. 目标与完成条件

按用户指令创建 `海中鱼巣/业务/应用服务.特征概念类.ixx`，真实实现特征概念生成、匹配复用、规则驱动值域细分候选、已发布实例的名称字段节点关联、独立观测记录、长期成熟评估及生命周期迁移。

概念 owner 复用现有 `L2概念结构服务`，概念为现有特征根下的独立普通概念，不是应用map对象或词条文本。未成熟概念可发布并立即复用，成熟记录独立于活跃/冷却/退役；历史实例和值证据按 Gobs 读回，名称字段方向为实例到概念节点。

完成必须同时满足：新模块及真实provider实现、合法真实provider消费者验证、匹配/观测/成熟/退出引用测试、代码与工程发布、施工记录形成函数级知识变更清单。构建成功不单独证明完成。

## 2. 正式依据与代码基线

正式依据：0050、1120、1130、2100、2220、4120、4230、4240、7140；本轮新增口径已由规范提交 `cb89b5c60b172becb662650608d4aa198a587399` 发布。代码起点为世界树切片发布后的 `f61aee960aad54b6cef1c5f05693c1ccd6b79c34`；执行S0允许后续已发布且不改变本合同的独立提交，须记录实际HEAD。

当前已有普通概念独立身份、四根、定义、上下位、治理生命周期与同owner写集，复用而不替代。新特征及存在provider不能强转为旧L2世界身份；需新增具名引用与历史只读合同。新应用不消费普通世界初始赋值或值变化提供者，不创建正式当前槽或初态，不修改已有特征实际值。1130同宿主/定义唯一槽及首次值+初态原子合同保持原样。

## 3. 允许文件与禁止范围

允许生产文件：

1. `海中鱼巣/领域/L2普通概念结构.数据.h`：DTO、wrapper、编码和成功谓词。
2. `海中鱼巣/领域/服务.L2概念结构.ixx`：现有概念owner专用创建/读取/名称字段/观测/成熟、按实际引用检查provider以及不可逆退出引用保护。
3. `海中鱼巣/领域/数据服务.特征类.ixx`：类型确认、具名历史读取、同L1装配确认；原始写入及重放语义不变。
4. `海中鱼巣/领域/数据服务.存在类.ixx`：具名历史存在/特征成员读取、同L1装配确认；原始写接口不变。
5. `海中鱼巣/领域/数据服务.特征值类.ixx`：具名历史不可变值读取、同L1装配确认；原始写接口不变。
6. `海中鱼巣/业务/应用服务.特征概念类.ixx`：新模块，函数及算法依详细设计。
7. `海中鱼巣.vcxproj`、`海中鱼巣.vcxproj.filters`：只登记新应用模块。

专属工程证据文件：`施工记录/20260905_FEATURE-CONCEPT-APPLICATION_特征概念生成复用与长期沉淀施工记录_v0.1.md`、`验证记录/20260905_FEATURE-CONCEPT-APPLICATION_特征概念局部服务验证记录_v0.1.md`。

禁止：修改L1 owner签发/事务实现、重建第二概念owner、改变根初始化ABI、重写旧七服务、改世界值/状态/动态写入、自动轮廓距离算法、自动区间聚类拆合、自然语言词条写入、其它概念根、普通应用生产装配、启动线程、源码或历史材料物理删除、共享知识索引回写。

## 4. 冻结施工合同

完整DTO、接口、来源、规则、同截止读回和异常分账唯一服从详细设计第3至8节，不让执行者自行决定机器语义。

关键裁决：

- root-only C1 配置不可变材料及三新类即可运行；不用另一套旧特征owner。所有依赖同一L1且实例完全相同，按实际引用检查缺provider。
- 形成规则与成熟规则分开，阈值不进入概念签名。精确值支持I64/VecI64/VecU64，区间只I64显式闭区间；相同签名唯一，重叠完整歧义，禁止首项猜选。
- 规则唯一身份就是不可变规则材料身份，`稳定编码 规则身份`只冗余回显`规则材料.值`且必须相等；通过真实材料provider同G读回后才成立。既有签名规则材料关系承载身份，版本/分区值式保存，不加标签15或规则节点，不把身份塞进I64/U64。首次写前，同类材料身份/版本的完整规则须与已发布承载记录一致，含历史成熟记录；异内容零写冲突。
- 分区候选根据明确规则和真实观测形成；没有观测的分区不自动发布概念，没有算法的表示明确不支持。
- 成熟采用独立观测键、UTC纳秒时间、最少数量、跨度及间隔；贪心序列末项替换为最后观测避免遗漏最大跨度。规则/证据正式保存，读失败不等于未成熟。
- 名称字段节点由概念owner保存，当前至多一项，显式替换同事务保留历史；清理校验可使用绑定时H的历史端点见证，正常退出仍须先解除当前引用，当前引用阻止概念不可逆退出。
- 正常实例退出先由概念owner按期望完整清单原子释放本合同名称/观测外部当前引用，再由外部合法世界owner处理其余前置；不绕L1保护、不宣称释放=世界可退出。观测记录及成熟证据保留，外部关系按Grecord读历史、世界事实按Gobs读历史；防御性历史清理不是制造不可达状态的依据。
- 观测保留Gobs、宿主成员见证及精确值引用，后续用历史H核验。应用无第二权威数据仓。
- 标签13裸实例引用使用原feature owner新增 `按实例读取特征历史事实`：请求为版本1/Gread/H/实例/非零最大属性值数，基于已有L1历史属性值组确定唯一真实值后复用精确读。标签14继续严格指定值，不用optional或自动fallback。完整组超预算不截断，零项历史材料不可用，预算内多项内部不一致；具体优先级与生命周期见详细设计第6.2节。
- 特征值结点引用的历史材料读取使用原值owner新增 `按结点读取不可变材料历史事实`：版本1/Gread/H/结点/非零最大属性值数，按已有L1历史属性值组取得唯一真实值后复用精确入口；概念provider预算固定为1，H沿世界观测截止，应用/概念零跨owner L1枚举。精确材料值引用不回退，类型/来源/生命周期及错误优先级见详细设计第6.4节。
- 每个应用写操作至多一个概念owner写集；完整准备请求可由调用方保存，原G/原键重放，未知发布不换键、不假称零写。
- 类只创建/治理概念并关联已发布实例；正式世界首实例/初态/值变化由外部合法提供者负责，本轮不实现这些生产链。

## 5. 实施顺序与失败收口

1. 重新S0：读取本计划和详细设计精确blob、最新规范、HEAD/远端/index/dirty、知识入口及用途卡，核对唯一代码所有者和工程占用。
2. 完成新世界provider的最小历史只读/同库检查与引用DTO；不能直接包装拒绝后来退出的旧“首次历史”helper。
3. 在既有概念owner内登记专用结构，完整创建/签名查找/读回，追加新引用标签，保留旧编码与根构造ABI。
4. 完成名称字段关联/替换/退出、观测/历史读取、成熟程度与证据同事务、生命周期复用和退出保护；补完整当前引用清单及同owner释放，准备/重放variant纳入释放原请求。
5. 实现新应用的确定匹配、分区候选、生成复用、关联、观测、成熟算法及完整原请求重放，补工程登记。
6. 执行下节验证、修复本切片实现错误、记录函数级知识变更，取得发布租约后只提交允许文件。

内部实现方式、私有函数命名和局部缓存投影可以由执行者在冻结合同内选择；公开语义/所有权/历史模型/原子边界改变必须具名退回本计划。临时环境或一次构建失败不自动转设计问题。不可实现的成功分支不得以未实现壳或测试容器代替。

## 6. 验证合同

隔离验证根：`D:\TEMP\FEATURE-CONCEPT-APPLICATION\<执行任务标识>`。仅本切片临时文件、构建输入、对象、日志及运行事实；不得消费其它任务WIP或改共享输出。临时脚本及生成探针置于此根，施工记录保存精确命令和结果，结束按工程规则核验路径后清理或登记保留原因。

必须运行：

```powershell
git diff --check
python .\tools\check_specs.py --strict
```

代码验证为隔离 Debug/Release x64 构建以及真实L1+唯一概念owner+不可变材料+三新类的局部消费者测试；用工程已配置的MSVC工具链，记录实际命令与退出码。测试源由本切片在隔离根形成，不修改主程序和既有自检注册。

业务矩阵执行详细设计第9节全部条目，尤其：root-only新依赖可运行、错库拒绝、零世界写入、首次未成熟概念、精确复用、多区间/向量/歧义、观测重放不计数、强时间与0/10/19边界、阈值改变不新概念、历史端点退出后证据可读、名称字段阻止删除、逐写操作未知发布原请求重放，以及重建应用后仅靠provider等价读回。

v0.2补充必测：规则回显与材料不等/材料不存在/异库拒绝；同材料版本完整规则复用；同键异分区或成熟参数冲突；历史成熟记录仍约束同键一致性；不同材料身份不因相同值域合并；规则身份从真实材料关系重建而无裸码副本或tag15。

v0.3补充必测：裸实例在当前槽清空后仍按既定H读取唯一真实历史值；G/H漂移、生命周期两端、零/不足预算、零/多值状态分账；标签13正确路由新只读入口，标签14错值严格失败不猜替代；概念owner零跨owner L1枚举。

同一v0.3还须覆盖可达的引用释放路径：有当前引用时世界退出受阻，概念侧先原子释放专用引用，外部世界owner处理其它引用后再退出，历史观测/成熟仍可读。释放空清单零写、完整清单同键重放、异清单冲突、预算不足零写及发布未知保留完整原请求；不能通过绕L1或强制退出构造“当前外部边仍在而实例已退出”。完整DTO、成功谓词与Grecord/Gobs分账见详细设计第6.3节。

v0.4补充必测：材料结点当前槽清空及合法退出后仍按Gobs读回真实唯一历史值，引用值观测/候选/成熟分支闭合；零/不足预算、零/多值、错误owner/类型/来源、G/H及生命周期两端、错精确值不回退。直接值分支保持行为，原始世界写接口不改，具体可达前置见详细设计第9节第17项。

只用真实provider，不以mock/map证明正式节点写入或历史；普通应用生产可达、真实设备、长期真实时间采样、跨进程持久恢复、完整世界初态和值变化均不在本轮验证声明中。

## 7. 发布与完成声明

代码所有者在施工记录列明新增/修改函数的完整签名、物理模块、调用边、数据权威、语义依据、知识条目基线和实际验证。只发布本轮代码与专属证据，普通push；不得带入其它切片或自行改共享知识库。

完成报告分别说明真实功能、验证结果、未验证范围、提交/推送及剩余风险。本模块交付不代表世界实例产生流程或全部概念树能力已经完成。

## 8. 执行退回修订

v0.2对应执行起点`42167004c6cc7cfc053c42f588e2a2a4beec1e25`的具名设计缺口：原v0.1规则身份字段未明确其真实provider，不能同时要求独立裸码参与概念唯一性。计划支撑按4240第3/5.1节及当前`普通概念签名相同`材料比较合同收敛为材料身份冗余回显，具体处理见详细设计第4.1节。

本次不扩大用户目标、生产文件或代码所有权，不修改正式规范。未涉及的历史只读实现保留；争议签名/DTO在修订正文和索引精确blob正式发布后重新S0，不使用旧v0.1口径继续。

v0.3对应执行侧第二次具名退回：裸实例历史引用无精确值身份来源。已在`ce8368282a90990145cbc0b0529ba621a4ba2da0`核验现L1历史属性值组公开API及真实仓库H过滤，可由原feature owner新增专用只读入口合法闭合；原精确值合同及代码/记录白名单不变，不新增L1或跨owner读取。受影响标签13路径在v0.3正文及索引精确版本发布后重新S0，其它独立已施工内容保留。

v0.3未发布期间合并当前引用生命周期退回：原先“实例已退出但名称/观测外部关系仍当前”的正常测试违反L1闭包。按既有7140第4.5节补概念owner先释放后由世界owner独立退出，保留观测/成熟记录及关系历史；不改正式规范或生产白名单。受影响名称/观测事务在完整v0.3发布后重新S0。

v0.4对应执行侧材料结点历史值身份来源退回。在已发布v0.3基线 `61ac0293f4bb743bb7c50c3330595f377f512c03` 按既有L1历史属性值组补原值owner具名结点/H读取，完整合同见详细设计第6.4节。代码/记录白名单不变，不新建计划、不改L1或世界写；仅受影响引用材料历史分支在本正文及索引精确版本发布后重新S0。
