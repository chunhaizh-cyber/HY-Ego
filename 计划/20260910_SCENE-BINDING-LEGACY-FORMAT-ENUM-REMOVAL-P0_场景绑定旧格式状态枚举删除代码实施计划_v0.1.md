# 场景绑定旧格式状态枚举删除代码实施计划

计划身份：`SCENE-BINDING-LEGACY-FORMAT-ENUM-REMOVAL-P0`
版本：v0.1
日期：2026-09-10
起点：`bd2c04b312a30cf471ec8329cf21977c71af3a8c`

## 1. 目标与完成条件

按[详细设计](../规范/详细设计/20260910_SCENE-BINDING-LEGACY-FORMAT-ENUM-REMOVAL-P0_场景绑定旧格式状态枚举删除详细设计_v0.1.md)，从 B 合同枚举中删除唯一孤立成员 `旧格式不支持=14`，永久停用数值14，并证明其余公开源码 ABI 形状、显式数值和合法消费者编译不变。

完成条件是：唯一生产差异成立；静态、数值、ABI形状、Debug/Release外置编译链接运行探针和差异检查满足；施工/验证记录如实发布。完成不要求真实 C/B 或根工程闭合。

## 2. S0 与依赖

无具名代码上游依赖。执行者必须从计划索引核对本计划精确版本/blob和 `可执行` 状态，并重新确认：

1. `main`、HEAD、`origin/main`、ahead/behind、index、dirty和WIP登记；
2. 起点正式材料及生产合同 blob 与详细设计第2节一致；
3. 全仓仍不存在该枚举成员的生产消费者、别名、case或数值14专属分支；
4. 允许生产文件未被其它所有者占用；
5. 当前 B 合同、场景骨架、动态及因果的实际 import 闭包；
6. 目标用途知识若落后，以当前源码和调用点为准并在施工记录形成知识变更清单。

若枚举已被合法消费者显式使用、数值14已被复用、目标合同发生其它形状漂移或生产文件有真实占用冲突，停止受影响范围并具名退回；无关 dirty 不阻断。

## 3. 精确允许文件

生产文件：

- `海中鱼巣/领域/合同.场景状态使用绑定.ixx`

记录文件：

- `施工记录/20260910_SCENE-BINDING-LEGACY-FORMAT-ENUM-REMOVAL-P0_场景绑定旧格式状态枚举删除施工记录_v0.1.md`
- `验证记录/20260910_SCENE-BINDING-LEGACY-FORMAT-ENUM-REMOVAL-P0_场景绑定旧格式状态枚举删除验证记录_v0.1.md`

只允许在生产文件中删除 `旧格式不支持=14`。记录只记实际 S0、改动、验证、Git、风险和函数级知识变更清单；本次无函数变化时明确写“无”。

## 4. 禁止范围

不得修改 `数据服务.场景类.ixx`、新建 `合同.场景角色组织.ixx`、修改任何工程文件、动态/因果/状态/存在/二次特征/世界树/概念树源码，或修改规范、详细设计、原场景P1计划、统一衔接、计划索引和共享知识索引。

不得增设别名、兼容壳、占位枚举14或回退逻辑；不得重编号15..23；不得删除或转换L1事实；不得夹带异主WIP。

## 5. 机械实施步骤

1. 在WIP表登记三个允许写文件，固定S0证据与外置验证目录。
2. 用最小补丁删除 B 合同中的 `旧格式不支持=14,`，不格式化相邻代码。
3. 扫描删除闭包并执行显式数值、底层类型、DTO/接口/谓词形状探针。
4. 从真实 import 递归形成外置验证工程，使用当前可定位的 MSBuild、v145、C++20、UTF-8 和 `bcrypt.lib`；在 `D:/TEMP/海中鱼巣/SCENE-BINDING-LEGACY-FORMAT-ENUM-REMOVAL-P0/<run-id>` 使用独占 IntDir/OutDir，Debug/Release x64 `/t:Rebuild /m:1 /v:minimal`，链接并运行 ABI/数值探针。
5. 核对生产 diff 严格只有一个枚举成员删除，工程零差异；运行 diff/strict 检查并分账范围外既有失败。
6. 写施工/验证记录，精确暂存三份允许交付文件，执行 cached diff/check，申请唯一Git发布租约后提交并普通推送。
7. 回读远端提交、目标 blob、index和剩余dirty；释放WIP登记并通知交互智能体。

## 6. 验证要求

- 静态：目标枚举的旧成员、引用、别名case及14复用均为零。
- 数值：22个合法枚举保持1..13、15..23，底层为uint8。
- ABI形状：合同版本1、DTO字段顺序/类型、结构交付三字段、只读provider、七个B入口及成功谓词不变。
- 构建：外置正式依赖闭包 Debug/Release x64 Rebuild、链接、探针运行均成功；记录工具版本、命令、输入hash和产物。
- 差异：生产差异仅一行枚举成员删除；工程零差异；`git diff --check`与`git diff --cached --check`通过。
- strict：实际运行并记录。已知范围外缺失L1验收记录不得通过补造、恢复或暂存异主删除来掩盖。

根工程当前受世界树/概念树旧场景悬空调用影响，不是本P0完成门禁；若执行者额外运行，必须按实际结果单列。场景骨架 `未实现` 不影响本P0探针通过。

## 7. 提交与完成声明

提交只包含目标合同、施工记录、验证记录。普通push成功且远端回读一致后才可交付；计划索引状态退出由其所有者后续处理。

报告只能声明本计划的死枚举删除、数值/形状保持和实际编译证据。不得升级为完整场景实现、根工程通过、运行恢复、集成验收或业务闭环。
