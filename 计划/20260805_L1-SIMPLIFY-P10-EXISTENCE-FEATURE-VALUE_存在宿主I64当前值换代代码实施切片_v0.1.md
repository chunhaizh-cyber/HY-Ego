# L1-SIMPLIFY-P10-EXISTENCE-FEATURE-VALUE 存在宿主 I64 当前值换代代码实施切片 v0.1

计划身份：`L1-SIMPLIFY-P10-EXISTENCE-FEATURE-VALUE`

版本：`v0.1`

创建侧代码基线：`4cf49c19ae403e47f4a74d808f247dc4603a9114`

## 1. 目标与完成条件

实现 SELF-GOV-CLOSURE STEP-3D：原位扩展现有 `L1实例特征服务`，为 P7 实际存在的 P8 I64 实例槽形成真实当前值换代；一个 L1 写集同时建立新值、退出旧值并切换当前属性槽，成功或精确重复后以 P8 新当前、L1 旧历史和当前槽的再次读取校准最终结果。

完成必须满足详细设计 v0.1 全部合同；执行侧不得临场改变相同材料语义、DTO、函数、状态映射、幂等域、写集、前一值链表达或错误映射。

唯一设计依据：

- `规范/详细设计/20260805_L1-SIMPLIFY-P10-EXISTENCE-FEATURE-VALUE_存在宿主I64当前值换代详细设计_v0.1.md`；
- `流程图/20260805_L1-SIMPLIFY-P10-EXISTENCE-FEATURE-VALUE_换代实际存在I64实例特征当前值函数流程图_v0.1.md`；
- 0050、1130、2100、4015、4020、4040、4110、4190、4200、4210；
- P4 正式结果和 P7/P8 v0.1 冻结合同。

## 2. 精确允许范围

只允许：

```text
海中鱼巣/领域/L1实例特征.数据.h
海中鱼巣/领域/服务.L1实例特征.ixx
海中鱼巣/领域/自检.L1实例特征.ixx
施工记录/20260805_L1-SIMPLIFY-P10-EXISTENCE-FEATURE-VALUE_施工记录.md
验证记录/20260805_L1-SIMPLIFY-P10-EXISTENCE-FEATURE-VALUE_验证记录.md
```

禁止修改 P7/P9/P6 DTO、服务或自检，P2 定义合同，L1事实基座，工程文件、filters、启动聚合、运行期上下文、全部 L2 需求合同、旧句柄/仓库/WORLD-TREE、SQL、材料、恢复、日志、规范、详细设计、流程图、其它计划或计划索引。不新增模块、不改顶层自检顺序；只原位扩展顺序320专项。

## 3. 待实现提供者与激活门禁

本计划登记为 `待激活`，必须同时满足：

1. P7 v0.1 正式执行结果已由交互智能体按 STEP-3A 目标明确接受；
2. P8 v0.1 正式执行结果已由交互智能体按 STEP-3B 目标明确接受；
3. 正式 P7 `实际存在读取请求/事实/结果` 和 `读取实际存在` 与 v0.1 冻结合同一致；
4. 正式 P8 `实际存在I64实例特征读取请求`、两个校验函数、四参构造、`建立实际存在I64实例特征`、`读取实际存在I64实例特征` 的字段、状态和事实形状与 v0.1 冻结合同一致；
5. P8 结果已释放本计划三个代码文件和顺序320专项所有权；其它活动切片不占用同一文件、语义或验证资源；
6. 从新正式 HEAD 重做 S0，重新读取完整自检数、模块数、现有 P4/P8 函数和统一账可落地性，计划仍可机械施工。

P9 不是换代机器语义依赖，P6 比较也不是写入前置；不得把二者结果接受伪造为激活条件。共享 Git 租约只构成发布串行等待。

## 4. 机械施工步骤

1. 在 `L1实例特征.数据.h` 追加 `实际存在I64实例特征当前值换代请求`、默认值、相等比较和有效性函数；现行场景 DTO、P8 DTO 和枚举数值不变。
2. 在现有类追加 `换代实际存在I64实例特征当前值(const 实际存在I64实例特征当前值换代请求&)`；返回仍为 `I64实例特征结果`。
3. 按详细设计 §6 新增 `I64当前值换代已验证材料` 和唯一私有 `提交I64当前值换代写集`；场景与存在入口均调用该核心，禁止复制平行写集逻辑。
4. 把现有四个单值换代见证机械迁移为场景/存在共用的有序值式账；键为幂等身份数值，值保存具名 variant 请求、规范化写集、新读取见证、旧事实和首次发布代次。
5. 新入口只通过 P8 `读取实际存在I64实例特征` 取得宿主、定义、实例槽、两关系、当前值和当前槽同代次材料；P8 内部消费 P7，3D 不再直接二次读取 P7。
6. 读取 P2 定义校验闭区间，读取 L1 新来源节点和完整快照，第一写前复核旧当前槽、结构见证和期望代次。
7. 以 `0x07` 提交零节点、零关系、一新值、一槽变更、一旧值退出；相同材料仍按详细设计 §3 发布新代次，完整同请求才幂等读回。
8. 成功/精确重复后调用 P8 公开入口读新当前，并用 L1 当前/历史/属性入口逐项互证旧退出、新当前槽和原结构；不得以缓存或提交返回估计事实。
9. 原位扩展 `自检.L1实例特征.ixx` 覆盖详细设计 §11；现行场景建立、读取、换代和 P8 建立/读取矩阵必须保持通过。
10. 形成两份专属记录，只写实际命令、结果、提交身份和未证明边界。

## 5. 固定状态、事务与停工条件

- 入口非法/值域非法/来源类型非法为 `入口拒绝`；登记/P8登记未加载为 `登记未加载`；事实缺失为 `未找到`；代次不一致为 `事实代次漂移`；同键异义为 `幂等冲突`；分配失败为 `资源失败`；坏成功、结构或读回矛盾为 `内部不一致`。
- 所有非成功零业务载荷。提交前失败零普通可读事实；发布后读回失败不补写、不删除、不回滚。
- “前一值链”只由同一 L1 写集、旧历史退出代次、新值创建代次、当前槽和审计互证，不新增 L1 字段、关系或领域侧权威账。
- 多轮调用每轮必须以 P8 最新当前见证和最新事实代次发起；同一旧当前的不同键竞争至多一个提交。
- 若正式 P7/P8 接口与冻结合同不同、`0x07` 域发生冲突、必须修改 L1 核心或白名单外文件、或相同材料语义无法保持 P4 合同，停止并具名退回设计漂移。

## 6. 验证合同

- 目标与 cached diff 精确五文件，Markdown 只允许两份专属记录；
- 新 DTO、校验函数、公开根函数和私有共享核心定义唯一，中文命名，依赖方向无环；
- 写集静态形状为节点0、关系0、值1、槽变更1、退出1，幂等域仍为 `0x07`；
- 存在入口只消费 P8 当前读取、P2 定义和 L1 公开读取/提交，不直接消费初始场景槽、P7归属关系、P9状态见证、P6、旧仓、SQL或材料；
- 成功、新旧同材料新代次、完整重复、同键异义、跨宿主同键、多轮历史、不同存在并发、旧基线、坏当前/来源/值域、provider失败、资源失败、映射损坏和发布后漂移矩阵通过；
- 新当前、旧历史、当前槽、宿主/定义/实例槽/两关系均由权威入口独立读回；
- 现行场景入口、P4 DTO/签名/状态及 P8 两入口保持兼容；
- Debug/Release x64 串行 Rebuild 均零警告零错误；两配置 `--self-test-exit` 退出0；
- `python .\tools\check_specs.py --strict`、MSVC 模块依赖扫描、目标 diff check 和 cached diff check 通过；完整自检数和模块数以执行 S0 正式基线为准。

## 7. 最大完成声明

只可声明：P7 已验证实际存在的 P8 I64 实例特征当前值换代、幂等/冲突、多轮历史和权威独立读回源码候选与具名矩阵成立。

不得声明：P7/P8 当前已实现、第二关系19状态、关系20迁移动能、动作动态、来源动作、因果、结算、生产装配、SQL/恢复、旧消费者迁移、集成验收、STEP-3 或整个治理闭环完成。
