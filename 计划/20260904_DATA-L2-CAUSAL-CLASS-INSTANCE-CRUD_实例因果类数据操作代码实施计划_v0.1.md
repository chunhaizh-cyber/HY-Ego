# 实例因果类数据操作代码实施计划

身份：`DATA-L2-CAUSAL-CLASS-INSTANCE-CRUD`

版本：v0.1

设计：[实例因果类数据操作详细设计 v0.1](../规范/详细设计/20260904_DATA-L2-CAUSAL-CLASS-INSTANCE-CRUD_实例因果类数据操作详细设计_v0.1.md)

状态：可执行

生产代码起点：`998b0d5a04589df30f8836fc80bb3f377012f709`

## 1. 目标、依赖与完成条件

按详细设计机械建立 `海中鱼巣.领域.数据服务.因果类`，实现实例因果的新增、当前读取、
按状态 / 动态证据 / 场景 / 来源查询和显式退出，并登记到 canonical 工程。

具名直接上游 `CURRENT-WIP-COMPILE-RECOVERY` 已由提交
`998b0d5a04589df30f8836fc80bb3f377012f709` 完成并普通推送。独立激活复核确认该提交只新增上游
施工 / 验证记录；状态、动态、场景、L1 公开合同和工程登记相对原设计基线 blob 全部未变，
上游隔离 Debug / Release x64 Rebuild 均为 0 警告、0 错误。因此具名依赖已经满足，本计划可由
索引登记为当前唯一 `可执行` 项。

完成条件：新模块和两份工程登记发布；专项矩阵、Debug / Release x64 编译、静态边界检查通过；
施工 / 验证记录绑定精确结果提交。完成不包括装配消费者、旧 L2 迁移、因果真伪裁决、概率推理、
跨进程恢复或服务验收。

## 2. S0 与允许范围

执行者必须读取 AGENTS、统一占用登记、单工作区发布规则、本计划和设计、规范目录、0050、1190、
4230、知识库三份入口和目标函数用途条目，并核对：

- 当前为 `main`，HEAD 与 `origin/main` 同步且 index 可用；生产依赖文件必须仍与
  `998b0d5a04589df30f8836fc80bb3f377012f709` 的对应 blob 一致，HEAD 在该提交之后只允许包含本计划
  详细设计、计划与索引的激活发布；
- 本计划在索引中为当前唯一 `可执行` 项，精确 plan blob 与索引一致，具名上游已经满足；
- 下列文件和语义由本切片唯一拥有，工程 / 构建 / 临时目录资源无冲突；
- 状态、动态、场景服务的 DTO 和公开函数仍与设计一致；L1 owner-scoped 公开合同未漂移。

允许文件严格为：

1. `海中鱼巣/领域/数据服务.因果类.ixx`；
2. `海中鱼巣.vcxproj`；
3. `海中鱼巣.vcxproj.filters`；
4. `施工记录/20260904_DATA-L2-CAUSAL-CLASS-INSTANCE-CRUD_施工记录_v0.1.md`；
5. `验证记录/20260904_DATA-L2-CAUSAL-CLASS-INSTANCE-CRUD_验证记录_v0.1.md`。

禁止修改详细设计 §8 所列范围，以及计划 / 索引和知识库。专项驱动只可写入
`D:\TEMP\DATA-L2-CAUSAL-CLASS-INSTANCE-CRUD\`，不得加入 Git。

## 3. 施工顺序

1. 新建模块，逐字实现详细设计 §2—§3 的常量、枚举、DTO、成功谓词、服务构造器和七个公开函数。
2. 实现请求规范化、跨类同截止公开读取、动作 / 来源 L1 公共读取、状态映射和严格零部分结果。
3. 实现一份 owner-scoped 新增写集、固定本地键、幂等重放、提交后完整读回和 `已可能发布`。
4. 实现完整当前读取和四类条件查询；全部复用同一闭包读取，组结果按因果编码稳定升序。
5. 实现删除写集、引用保护状态映射、退出后历史读回；不得删除或修改任何引用目标。
6. 在 `.vcxproj` 的现有数据服务链中于 `数据服务.场景类.ixx` 后增加唯一 `ClCompile`；filters
   在同组同顺序增加唯一条目，Filter 与相邻数据服务一致。
7. 建立专项驱动并执行 §4 验证；记录完整命令、退出码、结果、未覆盖范围和精确文件清单。

任何需要改变公开 DTO、事务、owner、字段来源、依赖方向、错误映射、工程入口或完成边界的问题
均属于设计漂移，停止受影响范围并退回计划支撑。当前文件内普通 C++ / 编译错误由执行者修复。

## 4. 验证合同

按详细设计 §7 的八组矩阵验证。至少执行：

```powershell
rg -n "仓库|服务\.L2|内部::|提交所有者范围中性写集" 海中鱼巣/领域/数据服务.因果类.ixx
rg -n "数据服务\.因果类\.ixx" 海中鱼巣.vcxproj 海中鱼巣.vcxproj.filters
git diff --check
python .\tools\check_specs.py --strict
```

其中 `提交所有者范围中性写集` 只允许出现在本服务自有 `写入端口_` 上；`内部::` 只允许本文件
自己的 `因果类数据内部`，不得命中其它服务内部。再使用隔离输出分别执行 canonical
`Debug|x64`、`Release|x64` 的 `/m:1 /t:Rebuild`，两者退出码必须为 0；专项驱动的可达断言全部
通过。详细设计 §7 第 6 组按以下三笔独立记证，不能用其中一笔冒充另一笔：

1. `STATIC_PASS`：逐函数审查新增提交、删除提交及其两个重放分支，确认成功 / 精确重复提交后的
   结果形状异常和读回失败统一映射为 `已可能发布`，保留非零代次、清空业务载荷，且不回滚、
   不提交第二份不同写集；
2. 动态通过：公开黑盒专项先首次提交，再以相同幂等键和全部原始请求字段精确重放，断言返回
   首次提交代次和同义结果；本轮已有的 24 项可达断言须全部通过；
3. `NOT_RUN`：确定性触发“提交已成功、随后读回失败”。当前允许的 L1、状态类、动态类、场景类
   公开面没有故障注入、暂停或替身 seam，因此不得用并发竞速、私有仓库访问、依赖替换或新增
   生产测试钩子补造。该项不阻断候选发布，但必须作为未动态验证边界保留，不能报告为已通过。

构建或专项若暴露上游模块、正式接口或共享资源问题，不得扩大允许集。只记录第一处完整诊断并
具名返回交互智能体；一次工具波动先在原范围内复核，不能把失败包装成成功。

## 5. 发布与报告

暂停新增写入后取得唯一 Git 发布租约，fetch 并核对远端；只暂存允许五文件，检查 staged diff，
运行最终验证，精确提交并普通推送 `origin main`。不得带入其它 dirty 或临时驱动。

报告必须说明激活后起点、实际变更、专项与两配置编译、静态 / 规范检查、提交 / 推送、剩余 dirty、
未验证边界和知识变更清单。发布只证明候选代码进入 Git，不证明生产装配、迁移、业务正确或独立
服务验收。
