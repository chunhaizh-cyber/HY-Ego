# PERIPHERAL-P2 外设方法冻结工作包与只读筹办许可接线代码实施切片 v0.2

编号：PERIPHERAL-P2；路由：#367 / DQ-259；合同：PER-C7 / ABI 2；日期：2026-07-24

设计状态：终审 PASS。当前状态、版本和 plan blob 唯一读取 `计划/计划索引.md`。

## 合同与边界

- 依据：3200、3300、5230；`规范/详细设计/D455任务唯一消费冻结工作包与观察方法详细设计.md` v0.2；`流程图/20260724_PERIPHERAL-TASK_任务唯一消费冻结工作包观察方法施工流程图_v0.2.md`；`规范/详细设计/函数结构知识图谱/20260724_PERCEPTION-D0_D455观察体素生产闭环函数结构知识图谱_v0.2.md`。
- 已实现事实：SELF-C2 具体适配器尚未实现；本计划只实现预冻结只读端口消费，不等待提供者代码。
- 待实现：`任务感知方法工作包ABI版本 = 2`；`任务筹办许可快照`、D455 / 识别 / 扫描三类工作包及冻结结果；三个 `冻结*工作包` 精确签名以详细设计 v0.2 为准。
- `识别派生需求材料`、`合法外设当前数据项`、`扫描输入材料` 唯一归属本合同模块，#369 只 import 并形成值。
- 本计划不创建、取得、迁移或恢复筹办占用；只读 SELF-C2 已发布许可投影。

## 文件所有权

允许：`海中鱼巣/线程/协议.任务感知方法工作包.ixx`、`海中鱼巣/领域/数据操作.任务感知方法工作包.ixx`、`海中鱼巣/领域/服务.任务感知方法工作包.ixx`、`海中鱼巣/领域/适配.任务感知方法材料.ixx`、`海中鱼巣/领域/组合.任务感知方法工作包.ixx`、`海中鱼巣/领域/自检.任务感知方法工作包.ixx`，以及 `实施记录/20260724_PERIPHERAL-P2_PER-C7工作包代码实施_Codex断点清单.md`。

禁止：SELF-C2 提供者、C6 / C8—C13、任务 / 方法现行服务、工程、入口、运行器、生产装配和设计文件。

## 施工与 S0

1. S0 核对 plan blob、C6 引用、SELF-C2 只读端口、三类共享类型所有权和六文件无反向 import。
2. S1 建立 DTO、值式只读端口、工作包数据操作与冻结入口；任务 + 筹办轮次 + 冻结版本为幂等键。
3. S2 核对许可、任务、方法、工作项、材料分支和版本；冻结后全字段不可变。
4. S3 自检源码覆盖许可失效、许可中的任务 / 筹办轮次与请求不一致、材料失效、方法错配、同义重放和冲突重放。
5. S4 核对白名单、ABI、`#367 <- #369` 零反向依赖和 diff。
6. S5 只提交允许代码与专属记录，推送执行通道分支并恢复 clean。

## 机械验证命令

S0 记录 `$计划段起点 = (git rev-parse HEAD).Trim()`；S4 的 `git diff --check` 与 `git diff --cached --check` 均须退出 0。S5 后运行：

```powershell
$期望 = @('海中鱼巣/线程/协议.任务感知方法工作包.ixx','海中鱼巣/领域/数据操作.任务感知方法工作包.ixx','海中鱼巣/领域/服务.任务感知方法工作包.ixx','海中鱼巣/领域/适配.任务感知方法材料.ixx','海中鱼巣/领域/组合.任务感知方法工作包.ixx','海中鱼巣/领域/自检.任务感知方法工作包.ixx','实施记录/20260724_PERIPHERAL-P2_PER-C7工作包代码实施_Codex断点清单.md') | Sort-Object
$实际 = @(git -c core.quotepath=false diff --name-only "$计划段起点..HEAD") | Sort-Object
if (Compare-Object $期望 $实际) { exit 1 }
if ((@(rg -n "任务感知方法工作包ABI版本\s*=\s*2" '海中鱼巣/线程/协议.任务感知方法工作包.ixx')).Count -ne 1) { exit 1 }
if ((@(rg -n "冻结D455方法工作包|冻结识别任务工作包|冻结扫描任务工作包" '海中鱼巣/领域')).Count -lt 3) { exit 1 }
if ((@(rg -n "尝试取得任务筹办执行权|写入任务筹办占用" '海中鱼巣/线程/协议.任务感知方法工作包.ixx' '海中鱼巣/领域/数据操作.任务感知方法工作包.ixx' '海中鱼巣/领域/服务.任务感知方法工作包.ixx' '海中鱼巣/领域/适配.任务感知方法材料.ixx' '海中鱼巣/领域/组合.任务感知方法工作包.ixx')).Count -ne 0) { exit 1 }
```

全部命令须退出 0；ABI 精确 1 处、三冻结入口至少 3 处、筹办占用写入口零命中。

## 验证与收口

- 静态 / 专项源码验证，不构建、不运行。
- SELF-C2 端口 / 共享 DTO / C6 引用漂移返回 `DRIFT-PER-CONTRACT`；现行只读许可无法适配返回 `DRIFT-PER-CURRENT-INTERFACE`；需要表外文件返回 `DRIFT-PER-FILE-OWNERSHIP`；循环依赖返回 `DRIFT-PER-IMPORT-CYCLE`。
- 同任务两路径进入召回、冻结后字段改变或前置通过后不可读为 `FAIL-PER-INTERNAL-READBACK`；其它验证失败为 `FAIL-PER-VALIDATION`。
- 完成只证明 PER-C7 隔离候选，不证明取得任务筹办权、方法执行或生产接通。
