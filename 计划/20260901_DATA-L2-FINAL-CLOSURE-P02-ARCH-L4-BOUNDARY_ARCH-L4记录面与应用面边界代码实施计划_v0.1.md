# DATA-L2-FINAL-CLOSURE-P02-ARCH-L4-BOUNDARY 代码实施计划 v0.1

日期：2026-09-01

计划身份：`DATA-L2-FINAL-CLOSURE-P02-ARCH-L4-BOUNDARY`

状态：`用户已裁决 / 当前交互智能体直接实施`

设计与施工起点：`e0d6402cc138b4d009cd2176ce8bcb640aa29e77`

本计划不登记 `计划/计划索引.md`；它是 DATA-L2 逐项收口中由用户直接裁决并由当前交互智能体连续落实的 P02 专属计划。

## 1. 目标与完成条件

把用户确认的需求、任务、方法两面分离口径落实为正式边界和编译期兼容门禁：

```text
DATA-L1：节点、关系和值的通用事实操作
    ↓
ARCH-L4 结构记录面：机械保存、读取和校验已经由调用方裁决的治理记录
    ↓
ARCH-L4 业务应用面：裁决为什么建立、选择、推进、结束或判定
```

完成必须同时满足：

1. 方法 `11`、需求 `12`、任务 `13` 永久保留为 ARCH-L4 兼容记录面编码，禁止删除、重排、改值或复用。
2. 三者继续被当前 DATA-L2 活跃分类拒绝，也不进入后继 DATA-L2 frozen manifest。
3. 现有 `L2需求*`、`L2任务*`、`L2方法*` 类型、服务、getter 和文件名只作兼容实现名，不取得 DATA-L2 架构权威。
4. 兼容记录面只负责身份、结构关系、生命周期、幂等、事实代次、当前 / 历史读取、原子发布、引用闭合和结构索引；不得裁决需求成立 / 当前满足、任务目标达成 / 轮次结算 / 生命周期结束、方法适用 / 授权 / 执行成功。
5. 全面 ARCH-L4 代码拆分后置为独立整改；P02 不修改当前异主需求 / 任务 / 方法 DTO、服务、装配、启动或消费者 WIP。
6. dirty WIP 新增的公开 L1 owner 提交见证归 P03；通用虚拟存在与状态 / 动态唯一权威归 P07 / P08；最终 manifest 排除和公开面禁增归 P11 / P12。
7. 规范、目录、裁决记录、公共代码分类和静态断言一致，隔离 Debug / Release 构建与 strict 检查通过。

## 2. 当前事实与复用裁决

1. P01 已在 `L2结构公共.数据.h` 提供 `L2节点族属于当前DATA_L2` 和 `L2节点族属于ARCH_L4兼容`，并固定 11—13 的数值；直接复用而不建立第二分类表。
2. 需求、任务、方法三个兼容服务已经进入普通应用装配并被 ARCH-L4 消费者使用，不能在 P02 机械删除或改名。
3. 当前活跃 DATA-L2 十一节点 owner 服务未发现对三个兼容服务的反向依赖；方法 HEAD 也未发现第二状态 / 动态写权威。
4. 需求、任务、方法服务及 DTO 正由另一活动自我治理任务修改，其中任务 / 方法切片约 6900 行 WIP；本计划与其文件所有权和语义施工分离。
5. 当前函数用途知识绑定旧提交，未覆盖 P01 新增分类函数；本计划以当前源码和 P01 施工 / 验证记录为事实，发布后把相关用途卡列为待复核，不直接修改共享知识库。

复用裁决：保留既有枚举和旧分类函数；新增一个更精确的纯值分类名作为正式记录面表达，并让旧函数委托它，零 I/O、零事实写入、零运行期新入口。

## 3. 冻结代码合同

唯一生产代码文件：

`海中鱼巣/领域/L2结构公共.数据.h`

新增公开纯值函数：

```cpp
inline constexpr bool L2节点族属于ARCH_L4兼容记录面(
    L2节点族 节点族) noexcept;
```

合同：只对方法、需求、任务返回 `true`；所有当前 DATA-L2 活跃族及未知枚举值返回 `false`。

既有函数保持原签名和行为：

```cpp
inline constexpr bool L2节点族属于ARCH_L4兼容(
    L2节点族 节点族) noexcept;
```

其实现改为委托新的精确分类函数，作为旧源码 ABI 兼容别名。`L2节点族身份来源事实完整` 使用精确分类名与当前 DATA-L2 分类的并集；这只保持既有兼容 DTO 可读，不把三者重新归入 DATA-L2。

静态断言至少固定：

- 方法 / 需求 / 任务分别为 `11 / 12 / 13`；
- 三者进入 ARCH-L4 兼容记录面并被当前 DATA-L2 拒绝；
- 旧兼容分类函数与新精确函数对三者、活跃族及未知值完全一致；
- 不可变材料和未知值不进入 ARCH-L4 兼容记录面。

## 4. 允许与禁止范围

允许修改：

```text
.codex/rules/层级命名空间规则.md
规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md
规范/0300_规范冲突与前后矛盾清单_20260720.md
规范/4030_子规范_基础信息服务分层与领域写授权.md
规范/3300_根规范_方法.md
规范/4250_子规范_L2需求树需求列表与对外接口统一合同_v0.4.md
规范/4270_子规范_L2方法核心结构与对外接口统一合同_v0.1.md
规范/规范目录.md
计划/20260901_DATA-L2-FINAL-CLOSURE-ADJUDICATION_逐项收口裁决输入_v0.1.md
本计划
海中鱼巣/领域/L2结构公共.数据.h
P02 专属施工 / 验证记录
```

禁止修改：

```text
需求 / 任务 / 方法 DTO、服务、聚合、装配、启动、线程和治理消费者
海中鱼巣.vcxproj、海中鱼巣.vcxproj.filters
任何 L1 生产文件
计划/计划索引.md
共享知识库、流程图或能力证据目录
P03—P12 尚未裁决的字段、生命周期、恢复、迁移和最终公开操作
```

## 5. 实施步骤

1. 同步 0050、4030、3300、4250、4270 和目录，明确 ARCH-L4 记录面 / 应用面分账；3100、3200、4260 已有“只执行已裁决结构操作、不作业务判断”正文且是异主当前语义依赖，本计划只引用、不修改。
2. 登记 C029 已裁决路由，并把历史 `DATA-L2 需求 / 任务 / 方法` 当前措辞改为 ARCH-L4 兼容记录面；历史修订记录不改义。
3. 在公共头新增精确分类函数，旧兼容分类函数改为别名委托；加入等价、排除和未知值静态断言。
4. 更新逐项裁决记录，把 P02 标为已落实，并登记 A 类跨层问题到 P03 / P07 / P08 / P11 / P12，B 类问题后置到 ARCH-L4 整改。
5. 形成施工记录中的函数级代码知识变更清单和验证记录。
6. 用 `D:\TEMP\海中鱼巣\DATA-L2-FINAL-CLOSURE-P02-ARCH-L4-BOUNDARY\<唯一轮次>` 的隔离候选执行 strict 和 Debug / Release x64 Rebuild。
7. 取得唯一发布租约后只暂存本计划精确文件，运行 staged 检查，普通提交并推送。

## 6. 验证合同

至少执行：

```text
git diff --check -- <P02 精确文件>
python .\tools\check_specs.py --strict（隔离候选）
海中鱼巣.vcxproj Debug|x64 Rebuild（隔离候选）
海中鱼巣.vcxproj Release|x64 Rebuild（隔离候选）
git diff --cached --check
```

定向静态检查必须证明：

1. 11—13 数值没有变化，14 未被复用；
2. 当前 DATA-L2 与 ARCH-L4 兼容记录面分类互斥；
3. 旧兼容分类函数与新精确函数等价；
4. 当前规范正文不再把需求、任务、方法或其兼容服务声明为 DATA-L2 活跃 owner / 公开面；
5. staged 文件不含任何异主 WIP。

## 7. 失败与完成声明边界

- 若外部活动任务占用同一正式规范、公共头或 Git 发布租约，本计划停止对应写入 / 发布并等待交接，不覆盖现场。
- 若 strict 或构建发现 P02 精确改动错误，在本计划范围内修复；不得修改 ARCH-L4 WIP 换取通过。
- 若发现兼容服务新增公开 L1 owner、第二状态 / 动态 owner 或 DATA-L2 反向依赖，具名路由到 P03 / P07 / P08 / P11，不在 P02 重写异主代码。
- P02 完成只证明 ARCH-L4 两面分离、11—13 永久兼容编码和 DATA-L2 排除门禁已正式化；不证明三个 ARCH-L4 服务内部合规，不证明 P03—P12 完成，也不证明 DATA-L2 已 `FROZEN`。
