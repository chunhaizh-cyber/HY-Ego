# WORLD-TREE-P1A3F 世界结构合同骨架代码实施切片 v0.2

计划身份：WORLD-TREE-P1A3F

版本：v0.2

创建侧当前代码基线：`23161473f49f5f20591534680889ea3dcc18b3aa`

## 1. 目标、依赖与替代关系

本计划原位取代 WORLD-TREE-P1A3F v0.1 计划；不新增第二个 P1A3F 身份。以 v0.1 plan blob `de4eebdba859201dd82a5ea29f3e7e01d9aa8fda` 的全部施工边界为基础，只用本计划冻结的四依赖构造、同实例装配和补充验证替换其中三依赖构造相关条目。

唯一直接施工依据是：

- `规范/详细设计/20260803_WORLD-TREE-P1A3F_世界结构合同骨架详细设计_v0.1.md` blob `e208c2bbb5ecfbc1ba06ad784eca0fa5136e7a24` 中未被 v0.2 替换的全部合同；
- `规范/详细设计/20260803_WORLD-TREE-P1A3F_世界结构合同骨架详细设计_v0.2.md` 全文。

具名依赖门禁仍是 WORLD-TREE-P1A2 v0.2 的最终查询、提交、恢复类型和真实公开签名已随成功结果进入正式代码。只有计划、WIP 或消息时保持待激活。

## 2. 允许和禁止范围

允许文件完全沿用 v0.1 §3。本修订直接影响的最小文件是：

```text
海中鱼巣/领域/服务.节点直接世界引导.ixx
海中鱼巣/领域/自检.节点直接世界结构骨架.ixx
海中鱼巣/启动.应用程序.ixx
```

工程文件仅沿用 v0.1 已冻结的模块登记，不因新增依赖新建模块。P1A2/P1A2Q 查询服务、合同头、自检和代码为只读依赖，禁止修改。v0.1 §4 的其它禁止行为全部继续有效。

## 3. 机械施工步骤

1. S0 核对 P1A2 三个服务和 `节点直接结构查询服务` 实际存在；任一签名漂移则零代码修改返回 `DRIFT-P1A3F-V02-CONSTRUCTOR-DEPENDENCY`。
2. 在 `服务.节点直接世界引导.ixx` 显式 import 查询服务模块，将引导服务构造原位替换为详细设计 v0.2 §3 的四参数签名、成员和初始化顺序；删除三参数构造，不保留转发重载。
3. 骨架自检中使用同一查询服务对象构造世界结构服务和引导服务；修正所有构造点，不修改骨架自检身份、顺序或总数。
4. 七个骨架函数仍只返回安全 `未实现`。新增 `结构_` 不得在函数体内被调用。
5. 完成 v0.1 §6 全部验证和本计划 §4 补充矩阵，写入 v0.1 已冻结的专属施工/验证记录。

## 4. 补充验证矩阵

| 编号 | 输入/检查 | 成功条件 |
| --- | --- | --- |
| F08 | `std::is_constructible_v<节点直接世界引导服务, 恢复服务&, 提交服务&, 查询服务&, 世界结构服务&>` | 为真 |
| F09 | 原二参数、三参数构造组合 | 均为假，无兼容重载 |
| F10 | 骨架自检装配 | 世界结构服务与引导服务持有同一查询服务实例的引用 |
| F11 | 调用七骨架前后导出事务域、六仓、幂等账和持久侧账 | 逐值完全相等 |
| F12 | 引导骨架函数体静态调用边 | `恢复_`/`提交_`/`结构_`/`世界结构_` 调用为零；import 和成员引用声明不计为调用 |

验证命令完全沿用 v0.1 §6。完成声明不变：最多为 `编译接入通过` / `入口巡检通过`，不证明任何真实世界行为。

## 5. 失败收口与索引登记

- 构造变化需要修改 P1A2/P1A2Q 文件、增加转发壳或暴露仓库/许可：失败收口，不扩大范围。
- 无法使两个世界服务共用同一查询实例：返回装配漂移，不构造第二实例补救。
- 骨架产生下层调用、非零代次、非空业务载荷或非 `未实现` 结果：本计划失败，不得提交。

计划索引只保留一行 WORLD-TREE-P1A3F，原位更新为 v0.2 和新的精确 plan/design blob；在 P1A2 v0.2 成功代码依赖正式成立前状态仍为 `待激活`。P1A2Q 不得仅修改索引说明来重解释其不可变 v0.1 plan blob；必须同批发布 `WORLD-TREE-P1A2Q v0.2` 计划补充，以精确新 plan blob 把依赖改为 P1A3F v0.2。P1A2Q 顺序 190、代码范围、状态和其它门禁不变。
