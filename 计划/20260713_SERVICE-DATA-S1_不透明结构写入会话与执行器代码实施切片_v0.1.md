# SERVICE-DATA-S1 不透明结构写入会话与执行器代码实施切片 v0.1

日期：2026-07-13

队列编号：#264 / 设计队列：DQ-156

状态：依赖门控待执行 / 前置 #263、700 / 最终回归 710 / 未改 C++

## 1. 当前事实与假定契约

当前结构事务接线公开许可和原始令牌，领域 / 路由可直接读取并传递。假定 #263 保持仓库令牌 ABI，允许新执行器内部适配且不修改许可强度、重入拒绝和锁序。漂移时退回设计。

## 2. 核心处理

新增不透明 `结构写入会话` 和 `结构写入执行器`。执行器内部取得 / 释放一次许可；会话不可默认构造、不可复制、移动后失效，不导出原始令牌。只向登记的数据操作模块提供窄仓库操作能力。第一轮在隔离自检验证，不接业务生产路由。

## 3. 新文件分类

| 文件 | 分类 | 工程登记 | 调用 |
| --- | --- | --- | --- |
| `核心/会话.结构写入.ixx` | 生产真模块 | `ClCompile` | 数据操作模块 `import` |
| `核心/执行器.结构写入.ixx` | 生产真模块 | `ClCompile` | 组合器 `import` |
| `核心/自检.结构写入会话.ixx` | 自检真模块 | `ClCompile` | 中央运行器 `import` |

## 4. 允许文件

上述三模块、必要的 `协调.结构事务.ixx` 窄内部接线、#263 结果头、工程 / filters、中央自检登记、入口最小汇总、实施记录和治理文件。

## 5. 禁止与失败收口

禁止四仓库语义改动、领域服务、线程路由、运行期主装配、`#214/#257`。禁止导出 `读取令牌`、保存会话到消息 / 队列 / 长期对象、跨线程移动会话。许可取得失败结构不变；回调异常必须释放许可并返回结构化内部错误。

## 6. 验收

A01-A12 覆盖独占 / 共享、无效接线、移动语义、同线程重入、异常释放、跨线程拒绝、原始令牌静态不可见和旧协调自检回归。Debug x64 0/0、完整自检、连续 20 轮、严格规范、diff、提交推送。

## 7. 完成边界

只证明会话基础可用，不证明领域服务已经迁移或业务事务完成。
