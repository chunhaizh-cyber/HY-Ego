# FOUR-OWNER-ATOMIC-FEATURE-BIRTH 代码实施计划 v1.4

状态：已退出。生产 I64 四 owner 结果由 `ea195241d7901e8bd1aee3d2b18142361fc8edaf` 发布；旧专项物理删除收口由 `097cbe48a7608997936fa0483a8fc4ec2fb11cd7` 发布。v0.3 已因结构登记输入缺失退出选择面；v0.4 冻结唯一已装配输入；v0.5 冻结出生使用结构登记结果；v0.6 冻结概念预算与 owner #4 ABI；v0.7 冻结 owner #1—#3 同 H 窄读取与完整读回顺序；v0.8 将候选搜索、逐项读回、0/1/多候选分类以及创建/复用决策上移并限定给 L3，L2 不再自行分类或据候选数决定写入；v0.9 补齐出生请求与候选查询的独立组织路径预算，使 owner #3 能在新建前按明确上限核验 C→…→P，且不能借用其它预算或默认值；v1.0 冻结 L3 的原子 I64 输入/结果、FCv 原键和显式组织来源、服务注入与消费者迁移，并将旧观察—定位创建链纳入迁移后的物理删除；v1.1 新增按 FT 限域、全量同截止的纯 I64 概念组织读取提供者，作为 FCv 最邻近父组计算前提，完整允许域继续复用既有特征类接口。v1.2 以 `ea195241` 为唯一已发布实现基线，确认四 owner 生产代码不在本轮改动范围。用户已明确取消专项测试；v1.3 只收口 HEAD 中旧专项验证工程的物理删除及限定旧链符号零命中；v1.4 记录该收口已正式完成并退出执行队列。

## v1.4 退出依据

`097cbe48` 从正式 Git 删除 HEAD 中的 `验证工具/定位特征创建专项验证.cpp` 与 `验证工具/定位特征创建专项验证.vcxproj`。以该提交为输入复核，限定的生产源码、`验证工具/`、工程/筛选器/解决方案/脚本范围内，这两个旧专项文件名及 `特征概念观察请求`、`特征概念处理状态`、`特征概念处理结果`、`定位特征创建请求`、`定位特征数据服务::创建或复用` 五个列明旧链符号均为零命中；`git diff --check 097cbe48^ 097cbe48` 通过。按用户指令没有构建、运行、新增专项或测试覆盖。本计划的 I64 上游结果现已满足 `INSTANCE-FEATURE-IF-R-CONCEPT-FOUNDATION` 的唯一直接依赖；后继执行者仍须重新 S0，且本退出不证明其 IF/R 能力。

## 目标与完成条件

实现 **I64 准确 F** 的四 owner 原子出生。`ea195241` 已发布一个新 I64 F 在同一 `G0→H` 同时读回 I64 内容、真实 holder 已知 F、`P→F` 和 `F→已发布 I64 单值域 FCv` 使用的生产实现；v1.3 仅删除 HEAD 中旧 `定位特征创建专项验证.cpp/.vcxproj`，并对本段列明的旧观察—定位创建链执行限定符号零命中检查。FCv 必须先独立发布；不得出现临时无 FC F。

这只是 1130 / 4110 / 7160 正式能力的 I64 实施阶段。I64组、U64组和独立材料引用的完整值读回、FC 单值域编码/规范化/匹配、概念 owner 核验、定位读回及四 owner 出生均未实现，明确不在本计划；完成不得称为通用准确 F 完成。

`L1有限N分区原子事务_v3` 已支持四参与者，本计划不修改 L1 合同、仓库或服务。`INSTANCE-FEATURE-IF-R-CONCEPT-FOUNDATION` 保持待激活，唯一直接依赖改为本计划的 **I64** 已发布结果；其执行者届时必须重新 S0。

## 允许范围

| 类别 | 文件 |
| --- | --- |
| 四 owner I64 组合合同 | `海中鱼巣/领域/数据服务.定位特征.h`；L2 提供不分类的精确候选查询（每项仅 `{F,出生H}`）、具名完整读取和只新建的四 owner 事务。候选、组织路径、概念读取预算彼此独立；候选查询、出生请求与读取请求均在需要 C/P 路径处显式包含非零组织路径预算，创建路径将该字段原样传递给 owner #3；读取请求显式包含 C/P、H、路径预算与概念读取预算；四参与者均声明私有窄读取接口，概念参与者另有退出片段接口 |
| 四个 owner I64 窄片段 | `海中鱼巣/领域/数据服务.特征类.h/.cpp`、`数据服务.存在类.h`、`数据服务.场景类.h`、`数据服务.概念树类.h/.cpp`；概念树以已读回的`纯概念结构交付_v2`为唯一输入新建独立 F→FCv 出生使用结构登记。登记结果必须公开原请求、状态/发布、Gread、首次 H、交付和四项首次材料的同 H 读回；仅其成功谓词允许装配注入。继续实现片段、窄读和退出保护，不得复用名称或共享用途，不读取未装配旧扩展 |
| FCv 父组最小读取提供者 | `海中鱼巣/领域/合同.相关概念添加参与.h`、`数据服务.概念树类.h/.cpp`；按 FT、Gread/H、概念树预算返回全部当前纯 I64 特征概念完整事实，零项成功、预算不足零结果不截断；不改 L1，不复用待删除旧特征概念枚举 |
| I64 概念前置、候选分类及消费者迁移 | `海中鱼巣/业务/应用服务.特征概念类.h/.cpp`、`应用服务.场景成员概念类.h/.cpp`；L3 公开 I64 原子请求/结果，独占 FCv 确保、候选搜索、每项同截止完整读回、0/1/多分类及创建/复用决定；普通应用以原子服务而非旧定位服务注入 |
| 装配和工程登记 | `海中鱼巣/装配.普通应用.h/.cpp`、`海中鱼巣.vcxproj` |
| 旧专项物理删除与静态收口 | 删除 HEAD 中的 `验证工具/定位特征创建专项验证.cpp`、`验证工具/定位特征创建专项验证.vcxproj`，并从工程/筛选器/脚本引用中退出；只对本计划列明的旧观察—定位创建链执行限定零命中检查。不得新建替代专项、运行脚本、施工/验证记录或任何测试资产。 |

禁止：`核心/L1所有者范围CRUD.数据.h`、`核心/服务.L1事实基座.h`、`核心/仓库.L1事实基座.h`、正式规范、I64组/U64组/独立材料出生或概念域实现、IF/R 六关系、当前采用、F 原地值、概念汇聚、线程/启动/阶段21及任何异主 WIP。

## 冻结实施步骤

1. S0：核对本计划精确 blob、`main == origin/main`、index 为空、范围文件无其它登记；读取详细设计、1130/4110/7160、L1 v3、现有 I64 特征/概念接口及目标函数知识。若基线、owner、L1 v3、正式规范或 I64 提供者漂移，停止并交计划支撑。
2. 在 `装配.普通应用.h/.cpp` 先执行既有`登记纯概念结构_v2`并保留其成功读回的`纯概念结构交付_v2`；随后在同一概念 owner 端口调用新的出生使用结构登记。后者只接收该交付、G0、原键和预算，新建独立锚点、无值关系类型、以纯交付`格式版本`写入的格式值，以及以纯交付`类型登记`写入的登记边；不得修改纯锚点的固定13条登记边，也不得扫描/读取旧特征概念或存在扩展。
3. 在`数据服务.概念树类.h/.cpp`发布登记结果 DTO 与成功谓词：原请求、具名状态/发布、Gread、首次 H、交付和四项首次材料读回缺一不可。按原键先读 L1 首次材料：仅匹配预期写集的既有成功材料可重放，首次提交只接受成功、重放只接受精确重复；二者都必须在同 H 逐项读回锚点、关系类型、类型登记关系和格式值。已进入提交后的任一不确定读回收敛为`已可能发布/可能发布`且清空交付/材料；确认未发布失败也清空交付/材料/首次 H。不得改 L1 或由 L1 承担结构性业务判断。
4. 仅在纯结构和出生使用结构的成功谓词均成立时，构造`概念树类数据服务(…纯概念结构交付_v2, 特征概念出生使用结构交付)`。任一失败、可能发布或读回不完整均停止装配，不构造假提供者。
5. 在 `数据服务.定位特征.h` 用 I64 专用候选查询、出生请求/结果、读取请求和四个窄参与接口替换旧三 owner 创建合同。候选查询只返回全部精确 `{F,出生H}`，不得排序裁决、过滤为单项、将读取异常视为零候选，亦不得写入；候选查询、出生请求和完整读取各自携带独立组织路径预算，最大路径长度为零即入口拒绝。出生入口改为只新建，不接受指定复用 F、不搜索或分类；组合器将出生请求路径预算不改写地传给 owner #3 准备片段，超过上限为`预算不足`，不得借候选/概念预算或默认值。请求、内容见证、事实和读回都使用 `std::int64_t`；候选、路径、三字段概念读取预算分别独立，调用方原样传递；概念 owner 仅映射为既有`概念树预算`的最大概念/关系/特征属性三项，其余为零。删除伪泛化 `variant` 入口及所有可创建无 FC F 的符号。固定参与者顺序 `1/2/3/4`，固定组合、内容、已知、组织、概念使用五把不同幂等键。
6. 为四个 owner 实现 I64 片段、同截止见证、首次材料恢复与逐事实读回。特征、存在、场景各以 L1 历史事实读取提供内容、holder、C/P 路径及 P→F 的私有窄结果，不返回或调用旧业务 DTO；场景 owner 在候选查询、出生片段准备和完整读取三个路径均只消费其收到的独立组织路径预算，零值入口拒绝、超限预算不足、路径不通位置冲突，任一失败零片段/零事实；概念 owner 以独立概念预算提供出生使用读回。四个读取都在同一 Gread 前后守卫，且各自返回 H 时完整、唯一、未退出的事实。概念 owner 经注入的纯概念结构核验当前特征类别`纯I64特征概念定义`、同 FT 和 I64 单点域 FCv，只写一条 `F→FCv`出生使用边；为既有四 owner F 叶退出组合器实现 owner #4 的退出片段：按 G0 读取唯一出生使用边并仅退出此边，不退出 FCv；不得在本计划中临时新增公共 F 退出服务或其它三 owner 退出 ABI。L2 不判定 IF/R 首次/后续或结构异常，不弹窗、不写 UI 日志，名称和共享用途零影响。
7. 实现组合器的精确候选召回、v3 新建调用、精确重放、已可能发布的原键收敛和四 owner 完整读回。读回固定为`内容→holder→组织→概念使用`：请求的 C/P 分别绑定 holder 及组织输入，四个 F、H、关系、生命周期和 FCv 必须交叉一致；仅四项全成功才形成返回事实。组合器不解释候选数、不得选择 F、不得将候选读取失败降格为零候选；F 叶退出同时退出 owner #4 出生使用边并保护仍被引用的 FCv。
8. 在`合同.相关概念添加参与.h`发布 `I64特征概念组织读取请求/结果`，在概念树类声明/实现 `读取当前I64特征概念`。请求固定版本2、Gread/H、FT和概念树预算；最大概念/关系/特征属性为零入口拒绝。成功返回同一 H 全部当前、类别为特征、定义为纯 I64 且同 FT 的完整纯概念事实，稳定编码排序去重；零项是成功空集。超过任一预算返回数量预算不足并清空结果，绝不截断；代次、历史、退出、资源和内部错误具名返回并清空。不得读取或改造待删除的旧`特征概念事实/查找特征概念`路径，不写 L1。
9. 在特征概念应用服务实现公开`I64原子准确特征出生应用请求/结果`。请求必须提供有效 G0、C/P、FT、I64、彼此互异的 FCv 概念键与五把 F 出生键、非零候选/组织/出生概念读取预算及 `概念树预算`的最大概念/关系/特征属性。先按完整 `FT + [I64,I64]` 确保并读回 FCv：同义当前概念复用；仅确认未找到时才以同一 Gread/H 调用既有`读取I64类型完整域`和新的`读取当前I64特征概念`，计算直接上位。完整允许域相等时显式顶层且空上位组；否则上位组为所有严格包含且不存在更近中间域的当前概念，稳定排序去重。任一读取失败、空而非顶层、父来源不可证明或预算不足均零 F 写；不得默认特征根、空上位或任一首项。以 FCv 原键、显式组织、上位组和原样概念预算调用纯概念创建/原键收敛，完整读回后才重新取得 G0。
10. L3 以非零候选和组织路径预算调用候选查询，并以每项返回的出生 H 及非零组织/概念预算逐项调用完整读取。L3 将空候选判为可创建、唯一完整候选判为复用、多项完整候选判为`需选择F`且零写；只有空候选才以同一 G0 和非零组织/概念预算调用 L2 新建入口。任一查询或读取失败保持结构化具名失败，不能作空候选；FCv 已发布而 F 失败时保留 FCv，不回滚。
11. 普通应用只在纯概念结构交付、出生使用交付和四 owner 原子服务都成功后，以`(特征类, 概念树类, 原子I64特征出生数据服务)`构造特征概念应用服务；场景成员概念类只转发新 L3 请求/结果。迁移已由 `ea195241` 发布；v1.3 不再改生产消费者。以 `ea195241` 的 HEAD 为删除起点，物理删除残留的 `定位特征创建专项验证.cpp/.vcxproj`；不得把现工作区中已删除但未发布的同路径 WIP 误当作完成。
12. 删除前后对限定范围执行零命中检查：生产源码、`验证工具/`、`*.vcxproj`、`*.vcxproj.filters`、`*.sln` 与 `*.ps1` 中的旧专项文件名，以及`特征概念观察请求`、`特征概念处理状态`、`特征概念处理结果`、`定位特征创建请求`、`定位特征数据服务::创建或复用`五个旧链符号。文件名必须为零引用；符号必须为零声明、定义、构造与调用命中。此检查不扩展为通用扫描、构建、运行或测试覆盖；不得以空文件、别名、双写或旧到新适配壳替代物理删除。
13. 不新增或修改专项工程、运行脚本、施工记录、验证记录、生产代码或正式规范。仅暂存本计划允许的旧专项删除路径；取得发布租约后提交并普通推送。完成门禁为：两份旧专项文件均从正式 Git 删除、限定文件名与五个符号均零命中、`git diff --check`及暂存后`git diff --cached --check`通过。用户明确本轮不运行构建、链接、程序、专项或测试覆盖；`check_specs.py --strict`既不构成本轮门禁，也不得为其修改全局检查器或恢复旧 `.ixx`。发布后释放占用，通知交互智能体；只有上述门禁全部满足，IF/R 执行者才可重新 S0。

## 必须保持的非成功合同

- FCv 缺失、错 FT、已退出或概念读回失败：不进入四 owner 提交，零 F/关系写。
- 纯概念交付、出生使用结构、交付、端点、角色、唯一性或生命周期不符：拒绝或内部不一致，零本次 F 出生写；名称为空、名称替换/解除及共享用途不能改变该结论；未装配旧特征概念结构或概念树存在扩展不得成为拒绝理由。
- 出生使用结构登记：仅同时返回原请求、确认发布、Gread、`G0+1`首次 H、交付及四项同 H 首次材料读回才可装配；已进入提交后的读回不确定性必须保留原键并返回`已可能发布/可能发布`，交付和材料均为空；确认未发布失败同样零交付、零材料、零首次 H。
- 概念读取预算：最大概念数、最大关系数、最大特征属性数任一为零或概念读取失败，均在 owner #4 片段准备前返回具名失败和零片段；不得消耗候选/路径预算补值，也不得以候选预算不足解释概念读取失败。
- 组织路径预算：候选查询、出生请求或完整读取中最大路径长度为零，均入口拒绝；创建组合器必须原样把出生请求的该预算传给 owner #3。路径超过该请求上限返回`预算不足`，路径断裂或 C/P 不符返回`位置冲突`；不得从候选/概念预算补值、使用默认上限或在片段准备后才发现缺失。
- F 退出：owner #4 只为已确定的 F 退出组合产生一项出生使用关系退出片段；关系不存在、重复、端点/类型/角色/生命周期不符或 FCv 读回失败时零片段。该片段不构成完整 F 叶退出成功，更不授权退出 FCv。
- 完整读取：内容、holder、组织、概念使用任一窄读取失败或四项读回的 F/H/C/P/关系/FCv 交叉不一致时，组合器清空结果事实；不得以任一旧定位业务 DTO 补齐、从候选搜索推断位置，或返回部分成功。
- 输入、C/P、holder、路径、任一独立预算、候选查询、候选历史读取、候选歧义、旧无 FC、片段或端口异常：保留具名状态，零本次写。只有 L3 可以解释候选数并产生复用或`需选择F`；L2 不得产生这些业务分类。
- `事实代次漂移`：L2 返回具名漂移；L3 以新快照重新查询、逐项读取和分类，不换原键伪造重试。
- `已可能发布`：只允许按原五键读回/重试收敛；不得声明零写或重新建立。
- 概念已有而 F 出生失败：保留 FCv，结果不得伪称全流程成功。
- 非 I64 不得由本入口伪装为`特征值身份`、默认拒绝或成功；它们没有本计划内 ABI 和生产能力。
- FCv 确保：完整 I64 单点定义的查询、创建、重放和读回都只使用 `FCv概念键`；该键不得与五把出生键混用。纯概念预算不足、组织未指定、直接上位空而非显式顶层、父不当前、父不严格包含、存在更近中间域或无法证明包含时，停止在 FCv 阶段并零 F 写；不得默认根、顶层或任意父。
- FCv 父组读取：`读取当前I64特征概念`与既有`读取I64类型完整域`必须以同一 Gread/H 成功；前者只返回全量同 FT 当前纯 I64 概念，空集仍成功，超预算绝不截断。任一失败、截止不一致、旧 DTO 误用或部分结果均停止 FCv 创建并零 F 写；不得以精确单概念查询、旧`查找特征概念`或临时缓存替代。
- 旧链删除：在新 L3 请求/结果、原子服务注入及场景成员消费者均已迁移前，旧`特征概念观察请求`、旧定位 DTO/创建调用和相关状态机必须保留以维持消费者可编译；迁移后必须以生产、验证、工程和脚本中的声明、定义、调用与引用全量零命中作为物理删除前提。HEAD 中的旧 `定位特征创建专项验证.cpp/.vcxproj` 是当前明确残留，任一残留命中都阻止本计划退出；不以空函数、别名、双写、旧到新适配壳或未发布删除 WIP代替删除。

## 验证和完成声明

本轮只执行静态收口检查：删除前后在步骤 12 的限定路径集合中确认旧专项文件名与五个旧链符号均零命中，随后运行`git diff --check`；在暂存本切片后再运行`git diff --cached --check`。不运行构建、链接、程序、专项、测试覆盖或`check_specs.py --strict`，也不新建或修改施工/验证记录。检查通过只证明删除差异没有空白错误，并证明限定的残留搜索范围内不存在被列出的旧链；它不证明生产代码、四 owner 合同、运行结果或业务能力。

完成只证明 I64 四 owner 原子出生的 HEAD 旧专项验证工程已物理删除，且限定旧链已完成静态零命中收口；`ea195241` 的已发布生产实现没有在本轮被重验。它不证明构建、链接、运行、测试覆盖、I64组/U64组/独立材料、IF/R、R 比较、概念汇聚、运行诊断可见性、长期运行/恢复或本能根初始化完成。
