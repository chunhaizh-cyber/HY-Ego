# TASK-LIFECYCLE-S0 任务生命周期与通用执行当前代码事实复核计划 v0.1

日期：2026-07-11

状态：已生成 / 已登记 #201 依赖门控待执行 / 队列串行前置 #200 / 业务前置 #192、#193 / 纯只读复核 / 未改 C++ / 未构建 / 未运行程序

## 1. 目标

在需求父子写入和通用方法召回 S0 形成正式事实后，重新扫描任务生命周期、筹办、方法选择、任务工作线程、强类型回执、自我治理固定样例、任务结果和需求结算的当前调用链，回答：

```text
当前任务状态如何确定。
哪些入口可任意写生命周期状态。
哪些调用直接从已创建跳到已完成。
任务筹办、方法召回与任务选择之间有哪些正式接口。
通用执行请求、工作线程回执和正式结果落账在哪里断开。
现有固定安全根样例哪些行为不能推广为通用任务状态机。
下一轮应如何拆分当前态投影、合法迁移、筹办接线、执行回执和完成结算。
```

本计划只生成更新后的代码事实记录，不修改代码，不生成状态机算法。

## 2. 正式依据

```text
规范/任务系统规范.md
规范/详细设计/任务状态机筹办执行桥详细设计.md
流程图/20260708_任务状态机筹办执行桥请求代码逻辑流程图_v0.1.md
规范/详细设计/任务状态机筹办执行桥请求代码逻辑详细设计.md
规范/详细设计/运行宿主与多线程消息队列详细设计.md
规范/详细设计/自我治理循环详细设计.md
计划/20260711_METHOD-RECALL-S0_通用方法召回当前代码事实扫描计划_v0.1.md
计划/20260711_DEMAND-TREE-S1_需求父子原子挂载与重挂代码实施切片_v0.1.md
```

本项是纯只读 S0，不新增机器结构，因此不新建流程图和详细设计。上述已确认任务流程、详细设计、#192 / #193 正式产物以及最新线程和自我治理代码作为替代依据。S0 完成后，任何任务状态机代码实施必须再生成范围匹配的新流程图、详细设计和计划。

## 3. 依赖门控

```text
队列串行前置：#200。
业务前置：#192 需求父子实际接口、#193 方法召回 S0 正式记录。
```

#200 不是任务业务依赖。#192 / #193 未完成时保持门控并跳过，不标记真实阻塞。

## 4. 已验证当前事实

截至计划生成时：

```text
任务生命周期枚举已有已创建至取消十个值。
记录任务生命周期状态入口可直接写任意非未定义值，没有合法迁移矩阵。
读取生命周期迁移请求材料把当前状态固定读取为已创建状态，不是最新状态。
任务生命周期状态作为多个实例状态节点长期引用，尚无正式当前态投影规则。
记录任务完成状态只要求存在实际结果状态和时间戳，不在入口内比较目标状态。
自我治理固定安全根路径可直接记录任务完成状态。
任务工作线程、强类型回执、上行桥和自我治理领域路由已有局部链，但不能等同通用任务执行。
```

这些只是生成时事实；执行必须读取 #192 / #193 后最新代码重新确认。

## 5. 假定前置产物

假定 #192 形成：

```text
需求普通父子的首次挂载 / 原子重挂。
需求服务直接父子只读。
关系仓库锁内防环实际入口。
```

假定 #193 形成：

```text
方法发现入口、条件 / 结果规格、多值索引、排序、失效和恢复边界的当前事实表。
下一轮方法召回三件套建议，不伪造未实现接口。
```

实际产物不同则按真实接口扫描，并在结论中登记漂移；不得为满足本计划假定修改代码。

## 6. 扫描范围

### 6.1 任务权威结构

```text
海中鱼巣/领域/任务服务.h
海中鱼巣/领域/需求服务.h
海中鱼巣/领域/状态服务.h
海中鱼巣/领域/方法服务.h
```

逐入口登记：输入、前置拒绝、写入结构、读取结构、时间戳、当前态语义、幂等、失败收口和调用方。

### 6.2 通用线程与回执

```text
海中鱼巣/线程/任务管理线程.ixx
海中鱼巣/线程/任务工作线程.ixx
海中鱼巣/线程/任务结果回执协议.ixx
海中鱼巣/线程/任务管理上行桥.ixx
海中鱼巣/线程/运行宿主.ixx
海中鱼巣/核心/运行消息协议.ixx
海中鱼巣/核心/运行消息队列.ixx
```

区分请求材料、调度信号、回执候选和权威状态写入，不把线程状态或消息 bool 当任务事实。

### 6.3 固定安全根样例

```text
海中鱼巣/线程/自我治理领域路由.ixx
海中鱼巣/线程/自我线程.ixx
海中鱼巣/入口.cpp
```

登记固定根直接完成、结果回写和结算的真实调用顺序，并明确哪些固定上下文、幂等键和特例不能推广。

### 6.4 正式记录

读取 #137、#158、#173-#179、#192、#193 的实施记录和计划归档，区分已验证入口与候选设计。

## 7. 必须输出

生成：

```text
实施记录/20260711_TASK-LIFECYCLE-S0_任务生命周期与通用执行当前代码事实复核_Codex断点清单.md
```

记录至少包含：

```text
入口 / 调用方矩阵。
生命周期状态写入与读取矩阵。
当前态候选规则与歧义表。
所有直接完成、失败、取消和任意状态写入调用点。
任务筹办到方法召回 / 选择的断点。
执行请求到工作线程、回执、上行桥和正式落账的断点。
固定安全根与通用任务的差异表。
中途非成功返回二分审查。
建议拆分：当前态投影、合法迁移、筹办接线、通用执行、完成结算。
每个建议切片的允许 / 禁止文件候选和依赖编号。
```

## 8. 允许文件

```text
实施记录/20260711_TASK-LIFECYCLE-S0_任务生命周期与通用执行当前代码事实复核_Codex断点清单.md
计划/20260711_TASK-LIFECYCLE-S0_任务生命周期与通用执行当前代码事实复核计划_v0.1.md
计划/已完成计划/20260711_TASK-LIFECYCLE-S0_任务生命周期与通用执行当前代码事实复核计划_v0.1.md
计划/计划索引.md
项目记忆/Codex任务队列.md
项目记忆/当前状态.md
项目记忆/实施记录.md
项目记忆/决策记录.md
项目记忆/待确认问题.md
```

## 9. 禁止文件与排除项

```text
海中鱼巣/**
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
规范/**
流程图/**
日志/**
旧鱼巢源码
```

禁止运行构建或程序，禁止创建状态、需求、任务、方法、动态或关系，禁止把建议接口写成当前实现。

## 10. 执行步骤

```text
读取依赖正式记录与最新 HEAD。
确认工作树并区分并行未提交材料。
按扫描范围登记全部声明和调用点。
核对状态服务的实例状态值、时间戳和读取能力。
核对任务服务当前态、任意状态写入和完成入口。
核对任务线程、回执和上行桥是否调用领域服务。
核对自我治理固定样例与通用入口差异。
形成二分审查、差距表和下一轮拆分建议。
更新治理、归档计划、提交推送。
```

## 11. 漂移与失败收口

```text
依赖未完成：保持门控并跳过。
实际接口不同：按实际事实记录，不改代码；范围扩大时退回设计窗口修订本 S0。
发现当前态已有正式唯一投影：登记并避免重复规划。
发现多状态歧义、时间倒退或固定样例绕过状态机：登记为待设计问题，不在扫描中修复。
扫描无法证明调用关系：标记“未证明”，不得猜测。
```

## 12. 验证

```powershell
git diff --check
py -3.14 .\tools\check_specs.py
git diff --name-only -- .\海中鱼巣 .\海中鱼巣.vcxproj .\海中鱼巣.vcxproj.filters
rg -n "任务生命周期状态|记录任务生命周期状态|记录任务完成状态|读取生命周期迁移请求材料" .\海中鱼巣
rg -n "任务结果回执|任务管理上行桥|自我治理领域路由" .\海中鱼巣\线程 .\海中鱼巣\入口.cpp
```

C++ / 工程差异必须为 0。本计划不得以任何构建或程序运行作为完成证据。

## 13. 完成声明边界

完成后只证明任务生命周期与通用执行最新代码事实、断点和下一轮设计拆分已经形成。不得声明当前态投影、合法迁移、通用筹办、方法召回、通用执行、完成结算或旧任务能力已经实现。

登记目标：#201 依赖门控待执行；完成后由设计计划窗口生成新的任务生命周期 / 通用执行三件套。
