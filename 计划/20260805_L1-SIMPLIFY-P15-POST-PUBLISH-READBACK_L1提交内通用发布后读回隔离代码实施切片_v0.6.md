# L1-SIMPLIFY-P15 L1提交内通用发布后读回、双摘要幂等与隔离代码实施切片 v0.6

计划身份：`L1-SIMPLIFY-P15-POST-PUBLISH-READBACK`

版本：`v0.6`

创建侧基线：`973aa24a189bd548b22774e051ba9eeca044ec81`

具名漂移：`DESIGN-DRIFT-P15-SUBMIT-WRITESET-CALL-COUNT-STALE-AFTER-P13`

## 1. 目标与替代关系

v0.6 完整替代并禁止施工 v0.5。机器目标不变：将全部真实 `.提交写集(...)` 调用迁移至 L1 v2 的双摘要、provider 前探测、首次结果、发布后权威读回、失败见证/隔离、审计/恢复互证合同。当前正式事实为 16 个代码文件、50 处调用点；相对 v0.5 只新增 `领域/自检.L1状态.ixx` 的 4 处调用。

## 2. 精确白名单

代码白名单保持 v0.5 的 20 个代码文件，另有 v0.6 专属施工/验证记录各一份，共 22 路径；不新增工程、filters、启动或模块所有权。16 文件/50 调用点完整清单以同版本详细设计和流程图为准，执行前必须重新枚举。

禁止修改 P6/P9/P11/P12/P13/P34、P0-FIX、路线、旧 WORLD-TREE、工程/启动或其它 WIP。

## 3. 机械迁移步骤

1. 从正式 HEAD 限定 `.ixx/.h` 扫描并确认 16 文件/50 调用点；流程图文本不计数，路径或数量变化先停工修订。
2. 保持 L1 核心六文件现有 v2 摘要、探测、提交、首次结果、审计/恢复互证和隔离实现。
3. 迁移 16 个调用文件的全部 50 点；P12 新增的 4 个状态自检调用与其它调用使用同一合同，不另造状态语义。
4. 每处调用严格执行：公开请求意图编码 -> `探测幂等` -> 仅未找到时 provider/事实读取 -> 执行证据编码 -> L1 v2 `提交写集`。同义、异义、已隔离、内部不一致均不得重复 provider 或提交。
5. 记录逐文件逐调用迁移结果、双摘要、发布后读回、失败见证和隔离证据；不得修改其它计划或设计。

## 4. 待激活门禁

保持 `待激活`。执行前从最新正式 HEAD 重做 S0，核对 P15 plan blob、16 文件/50 调用点、22 路径白名单、活动代码段、共享构建/自检/模块扫描资源和唯一 Git 所有权。若数量/路径变化，先回到计划支撑，不临场扩大。

## 5. 验证合同

- 精确 22 路径、50 个调用点、Markdown 仅两份专属记录；diff-check、cached diff-check、strict通过；
- Debug/Release Rebuild、完整自检、MSVC模块扫描通过；流程图命中不计入调用数量；
- 50 点逐一证明 provider 前双摘要/探测、首次确定性结果、同锁发布后权威读回、失败见证/隔离和审计/恢复互证；
- `领域/自检.L1状态.ixx` 新增4点不得遗漏，L1 v2机器合同不漂移；
- 无 P6/P9/P11/P12/P13 业务语义扩展、关系20、动态发布、SQL生产持久、恢复装配或旧入口旁路。

## 6. 完成边界

最多声明 L1 v2 双摘要幂等/通用发布后读回/隔离源码候选及当前 16 文件/50 调用点迁移矩阵完成；不声明 P6/P9/P11/P12/P13/P34、后状态/动态、生产装配、持久恢复、集成验收、STEP-3F、STEP-4或治理闭环完成。
