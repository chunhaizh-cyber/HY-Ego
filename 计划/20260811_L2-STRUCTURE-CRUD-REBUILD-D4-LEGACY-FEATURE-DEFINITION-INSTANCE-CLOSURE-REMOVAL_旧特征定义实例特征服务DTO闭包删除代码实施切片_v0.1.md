# 旧特征定义实例特征服务 DTO 闭包删除代码实施切片 v0.1

日期：2026-08-11

计划身份：`L2-STRUCTURE-CRUD-REBUILD-D4-LEGACY-FEATURE-DEFINITION-INSTANCE-CLOSURE-REMOVAL`

状态：创建侧验证后登记

## 1. 目标与完成条件

作为 L2 结构 CRUD 重建的第四份旧代码删除叶，在 D3 正式删除旧状态、动态和特征比较闭包后，物理删除旧 `L1特征定义` 与旧 `L1实例特征` 的两个服务模块及各自专属 DTO 头，为 D5 删除旧世界、场景和存在服务继续解除反向依赖。

本叶只删除旧代码和现存工程登记，不实现或设计新 L2 特征 CRUD，不迁移函数，不建立兼容壳，也不删除 D5 负责的旧世界、场景、存在、原子发布或组合服务。

完成条件固定为：

1. 四个目标源码路径及其四项现存 `海中鱼巣.vcxproj` 登记全部退出；
2. 两个模块身份、两个服务类型和四文件公开 DTO / 函数身份在现行生产源码中归零；
3. 集合外生产源码无需修改，Debug / Release x64 继续完整编译链接；
4. 专属施工与验证记录准确保存实际结果和未覆盖范围。

## 2. 正式依据与当前事实

正式依据：0050、4015、4030、4190、4200、0600、CODE-FILE-01，以及：

- `规范/详细设计/20260810_L2-STRUCTURE-CRUD-REBUILD_旧链清理与六类结构CRUD分步重建详细设计_v0.1.md`
- `流程图/20260810_L2-STRUCTURE-CRUD-REBUILD_旧链清理与六类结构CRUD分步路线图_v0.1.md`
- `计划/20260811_L2-STRUCTURE-CRUD-REBUILD-D3-LEGACY-STATE-DYNAMIC-FEATURE-COMPARISON-CLOSURE-REMOVAL_旧状态动态特征比较服务DTO闭包删除代码实施切片_v0.1.md`

创建侧正式事实基线为 `033cad5338231f313cadad4b29076a2dc850cd04`，该提交同时位于 `main` 与 `origin/main`：

- 正式 HEAD 中，`服务.L1实例特征.ixx` 是 `服务.L1特征定义.ixx` 的唯一闭包内生产消费者；实例特征服务向下消费旧场景结构、旧实际存在组合服务和新版 `L1事实基座`，删除消费者不要求修改这些提供者；
- D3 结果提交已删除 `服务.L1状态.ixx`、`服务.特征比较.ixx` 及其 DTO 和工程登记，正式生产源码中曾由它们形成的 D4 外部消费已经归零；
- 除四个 D4 闭包文件外，正式 `海中鱼巣/**` 生产源码无目标模块 import、数据头 include、服务类型构造或公开 DTO / 函数消费；
- 两个 DTO 头分别只由同对服务通过 `export { #include ... }` 公开，闭包关系为：

```text
L1特征定义.数据.h <- 服务.L1特征定义.ixx
                         ^
                         |
L1实例特征.数据.h <- 服务.L1实例特征.ixx
```

- 四个冻结源码 blob 为：
  - `L1特征定义.数据.h`：`11b6c074f7836217f1b6c49889e593e24903b2ea`
  - `服务.L1特征定义.ixx`：`8ebdd0200d902bd538e393ee03aea6df89462bee`
  - `L1实例特征.数据.h`：`dc14013c10ec7fa014b34b4d35de5b291a4e9ef1`
  - `服务.L1实例特征.ixx`：`b83e3725993542eacfd11f693d6595747e83e153`
- `海中鱼巣.vcxproj` 对两个服务各有一项 `ClCompile`、对两个 DTO 头各有一项 `ClInclude`；正式 `海中鱼巣.vcxproj.filters` 对四文件均无登记，因此本叶不得机械修改 filters；
- 四个源码当前无未提交差异；流程图目录、异主路线输入和 `.codex-build/` 继续原样保留且不得暂存。

复用裁决：旧特征定义和实例特征代码包含旧登记、领域命名、实际存在前置、值域准入、特征实例建立 / 读取 / 换代和结果解释，且服务及 DTO 仍以 `L1*` 命名。它们不是 G1 尚待冻结的新 L2 调用方提交式纯结构合同；旧待激活计划和详细设计中的文本引用不是生产消费者，也不能恢复旧接口执行许可。因此整对物理删除，不复制函数体、DTO、枚举、登记缓存或幂等定位账，不在本叶设计新合同。

## 3. 具名依赖、激活与发布门禁

D4 不消费 D3 的状态、动态或比较结果，D3 也不提供 D4 所需能力；两者不存在接口能力依赖。但正式 HEAD 中 D3 的两个目标服务仍是 D4 的集合外消费者，且 D3 / D4 都要修改唯一 `海中鱼巣.vcxproj`、使用唯一活动代码计划段和 Git 发布租约，因此 D3 结果构成真实删除顺序与共享资源门禁。

该门禁已经由 D3 结果提交 `033cad5338231f313cadad4b29076a2dc850cd04` 满足：

```text
D3 v0.1 按 plan blob 852e730204df63cc82deb89c9e87fcf4b20835a8 完成
-> 结果提交 033cad5338231f313cadad4b29076a2dc850cd04 已进入 main 与 origin/main
-> D3 活动代码段、工程文件所有权和 Git 发布租约已经释放
-> D4 已从该结果 HEAD 复核四源码 blob 与工程登记
-> 目标模块、服务类型和 DTO 在 D4 四文件外的生产消费者为零
-> D4 可以发布并登记为可执行
```

D4 发布并登记为 `可执行` 后仍必须由执行智能体重新 S0；计划文件、创建侧验证和索引状态都不证明代码已经删除。若执行 S0 发现新消费者、四源码 blob、工程形状或正式路线漂移，停止并返回计划支撑，不扩大 D4 白名单。

## 4. 精确允许范围

删除：

- `海中鱼巣/领域/L1特征定义.数据.h`
- `海中鱼巣/领域/服务.L1特征定义.ixx`
- `海中鱼巣/领域/L1实例特征.数据.h`
- `海中鱼巣/领域/服务.L1实例特征.ixx`

修改：

- `海中鱼巣.vcxproj`

新增专属记录：

- `施工记录/20260811_L2-STRUCTURE-CRUD-REBUILD-D4-LEGACY-FEATURE-DEFINITION-INSTANCE-CLOSURE-REMOVAL_旧特征定义实例特征服务DTO闭包删除施工记录.md`
- `验证记录/20260811_L2-STRUCTURE-CRUD-REBUILD-D4-LEGACY-FEATURE-DEFINITION-INSTANCE-CLOSURE-REMOVAL_旧特征定义实例特征服务DTO闭包删除验证记录.md`

## 5. 明确禁止与排除

禁止修改或删除：

- D3 的施工 / 验证记录或已经发布的结果提交；
- `海中鱼巣.vcxproj.filters`，因为当前四目标文件无 filters 登记；
- 新版 L1 及其公开 DTO、服务、仓库和工程登记；
- 世界登记、场景结构、实际存在、原子发布、组合服务及其 DTO；
- 新 L2 六类结构合同或场景、存在、特征、状态、动态、因果 CRUD；
- 安全因果分层、任务、方法、启动、装配或上层业务模块；
- 规范、详细设计、路线图、计划索引、流程图维护目录、异主路线输入及任何异主 WIP。

本叶不重命名、不搬移、不新增代码函数，不改变机器合同，不建立兼容声明或全局辅助函数。因只删除完整模块和专属 DTO，CODE-FILE-01 的新增 / 直接修改函数诊断责任分类不适用；不得借编译错误修改集合外函数。

## 6. 实施步骤与失败收口

1. 从 D3 已发布结果 HEAD 完成 S0，复核本计划精确 blob、四源码 blob、D3 结果提交、Git index、异主 WIP、活动代码段和索引状态。
2. 以模块身份、数据头路径、服务类型和公开 DTO / 函数名扫描现行生产源码，确认 D4 四文件外命中为零。
3. 物理删除四个目标源码。
4. 从 `海中鱼巣.vcxproj` 精确删除两个 `ClCompile` 和两个 `ClInclude`；不得调整其它项目顺序、筛选器或配置，不修改 filters。
5. 扫描下列身份在现行生产源码和工程登记中归零：
   - `海中鱼巣.领域.服务.L1特征定义`、`L1特征定义.数据.h`、`L1特征定义服务`
   - `海中鱼巣.领域.服务.L1实例特征`、`L1实例特征.数据.h`、`L1实例特征服务`
   - 四文件唯一声明的旧特征定义 / 实例特征请求、结果、登记、事实、状态和有效性函数
6. 记录实际删除、归零、工程完整性、构建结果和未覆盖范围。

不得通过恢复旧模块、加入兼容声明、删除集合外消费者或修改集合外源码来处理失败。发现真实集合外消费者、冻结 blob 或工程形状漂移时，保留现场、停止本叶并具名返回计划支撑。

## 7. 验证合同

本叶没有新建或实现公开接口，生产外自定义参数轻量验证不适用；不得为删除叶制造参数驱动、生产内自检或业务剧本。

实际验证固定为：

1. 四个目标路径逐项不存在；
2. 两个模块身份、服务类型、专属 DTO / 函数和四项工程登记在现行生产范围归零；
3. 禁止文件没有进入实际差异，`海中鱼巣.vcxproj.filters` 无变化；
4. `海中鱼巣.vcxproj` 与 filters 均能按严格 UTF-8 解析为有效 XML，四配置 `/utf-8` 保持四项；
5. `MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /verbosity:minimal` 成功；
6. `MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Release /p:Platform=x64 /verbosity:minimal` 成功；
7. `git diff --check` 通过；
8. `python .\tools\check_specs.py --strict` 通过；
9. 提交前 cached 范围只包含四个代码删除、一个工程文件和两份专属记录。

明确不运行程序、CRUD、生产模式、自检、功能测试、参数黑盒或集成验收。

## 8. 完成声明边界

本计划发布并登记只证明 D4 施工合同已经形成且创建侧门禁成立，不证明 D4 代码已删除、构建或功能正确。

D4 实施完成只证明旧特征定义与实例特征两个服务 / DTO 对及四项工程登记物理退出，并且剩余工程继续编译链接；不证明 D5 或全部旧 L2 已删除，不证明新 L2 特征或其它结构 CRUD 已设计 / 实现，也不证明新版 L1、生产运行期、业务真实性、并发、性能、崩溃、重启或跨进程恢复成立。
