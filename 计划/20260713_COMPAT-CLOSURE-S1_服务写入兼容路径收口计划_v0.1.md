# COMPAT-CLOSURE-S1 服务写入兼容路径收口计划 v0.1

日期：2026-07-13

队列编号：#271 / 设计队列：DQ-163

状态：依赖门控待执行 / 前置 #270、770 / 最终回归 780 / 未改 C++

## 1. 目标

消费 #262 完整矩阵和 #263-#270 正式产物，逐入口证明无令牌写、领域带令牌写和服务候选写的生产调用点已经替代，再裁决保留、私有化、弃用或删除。四仓库内部带令牌 ABI和不透明会话适配不属于本轮删除目标。

## 2. 实施规则

每个入口必须同时满足：生产调用点为零；自检已迁移；新入口行为 / 拒绝 / 故障注入覆盖；恢复与历史计划不依赖；删除后没有反向依赖。任一条件不满足就保留并登记后继，不得全仓批删。

## 3. 允许与禁止

允许 #262 明确列出的兼容服务头 / 模块、对应自检、工程、说明和治理文件。精确文件清单必须在执行前由 #262 产物回填；未回填前 C++ 禁止修改。禁止核心仓库 ABI、事务许可语义、`#214/#257`、业务行为改写和旧能力迁移。

## 4. 验收

A01-A16：矩阵逐项闭合；生产领域公开签名无原始令牌；生产路由无许可操作；外部写仓库为零；兼容保留项有所有者和后继；完整旧 / 新自检、Debug x64 0/0、连续 20 轮、严格规范、diff、提交推送。

## 5. 后继解锁

#271 完成后才解除 `#257` 的架构前置门控。`#214` 仍保持用户暂停，不能因本计划完成自动恢复。

## 6. 完成边界

只证明服务写入兼容路径按矩阵收口，不证明全部历史头文件、仓库兼容 ABI或旧能力迁移已删除完成。
