# DATA-L2 需求任务公共身份闭合代码实施切片

日期：2026-08-15
版本：v0.1
身份：`DATA-L2-DEMAND-TASK-IDENTITY-CLOSURE`
状态：设计就绪；以计划索引登记状态为准

## 1. 目标

依据[详细设计](../规范/详细设计/20260815_DATA-L2-DEMAND-TASK-IDENTITY-CLOSURE_需求任务公共身份闭合详细设计_v0.1.md)，在共同 L2 值式合同中追加独立需求 / 任务强类型身份与节点族，并扩展共同身份来源完整性 helper，为 DATA-EXT-12 / 13 后继结构 CRUD 提供前置。

## 2. 允许范围

生产修改只允许：

- `海中鱼巣/领域/L2结构公共.数据.h`。

允许新增永久记录：

- `施工记录/20260815_DATA-L2-DEMAND-TASK-IDENTITY-CLOSURE_需求任务公共身份闭合施工记录_v0.1.md`；
- `验证记录/20260815_DATA-L2-DEMAND-TASK-IDENTITY-CLOSURE_需求任务公共身份闭合施工检查记录_v0.1.md`。

禁止修改 L1、任何服务、聚合、普通应用、工程、BIZ、线程、规范、流程图、最终需求清单和异主 WIP。禁止建立临时专项或生产验证逻辑。

## 3. 施工步骤

1. 从索引绑定的最新正式 HEAD 重做 S0，确认计划状态、方法身份结果已发布、公共头无异主修改、index 空、唯一代码段与构建资源可用。
2. 机械追加 `L2需求身份`、`L2任务身份`、节点族 12 / 13 和 helper 两个闭合分支；不改其它代码。
3. 静态核对既有节点族 1—11、状态 1—15、合同版本和其它 ABI 零变化。
4. 使用仓库外专属输出执行根工程 x64 Debug / Release Rebuild；不修改或评价暂停 EVENT 工程 WIP。
5. 运行 diff、strict、UTF-8、白名单和生产零验证痕迹检查；清理本叶专属构建输出文件。
6. 形成两份永久记录，只提交本计划三文件并普通推送，随后单独登记退出。

## 4. 成功条件

- `L2需求身份`、`L2任务身份` 各自逐字存在且保持显式稳定编码包装；
- `L2节点族::需求=12`、`L2节点族::任务=13`，已有 1—11 不变；
- 共同身份来源 helper 接受十三族闭集，不接受未知值；
- 根工程 Debug / Release Rebuild 和静态门禁通过；
- 专属输出清理归零；
- 不出现需求 / 任务 owner、节点、CRUD、业务判断、万能服务或第二写入口。

本计划完成不证明 DATA-EXT-12 / 13 已有 owner、CRUD、事实或消费者。后继需求和任务结构服务必须从本结果重新 S0，不得复制身份或再次修改既有节点族数值。
