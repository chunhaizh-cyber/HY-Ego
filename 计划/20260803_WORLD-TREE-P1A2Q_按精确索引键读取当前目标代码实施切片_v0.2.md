# WORLD-TREE-P1A2Q 按精确索引键读取当前目标代码实施切片 v0.2

计划身份：WORLD-TREE-P1A2Q

版本：v0.2

创建侧当前代码基线：`23161473f49f5f20591534680889ea3dcc18b3aa`

## 1. 唯一修订与现行身份

本计划以不可变 v0.1 plan blob `c6d84847cd110b6dc70a74366e0398eec213be5f` 为基础，只替换 v0.1 的 P1A3F 具名版本依赖。v0.1 的目标、详细设计、两函数流程图、允许/禁止文件、机械步骤、顺序 190、验证矩阵、失败收口和完成声明全部继续有效。冲突时以本 v0.2 为准，执行者不得继续把 P1A3F v0.1 解释为开工门禁。

## 2. 具名依赖替换

P1A2Q 的依赖固定为：

1. WORLD-TREE-P1A2 v0.2 成功结果已进入当前正式代码，最终查询服务、索引仓和相关结构与详细设计一致。
2. WORLD-TREE-P1A3F v0.2 成功结果已进入当前正式代码：唯一 `WORLD-TREE-P1A3F-S01` 仍为顺序 180，成功后自检总数仍为 19，七骨架仍零事实写入并返回安全 `未实现`；唯一变化是引导服务构造预留了同一 L1 查询服务引用。

P1A3F v0.2 在 P1A2Q 消费面上与 v0.1 相容：P1A2Q 不构造、调用或修改世界引导服务，也不消费其依赖数量；它只在 P1A3F 合同类型已进入工程后扩展 L1 索引读取。因此不需要修改 P1A2Q 详细设计、流程图或代码白名单。

## 3. 索引、验证与完成边界

计划索引中 WORLD-TREE-P1A2Q 只保留一行，原位更新为 v0.2 及本文件的精确 plan blob；design blob 继续指向 v0.1 详细设计提交。P1A3F v0.2 或 P1A2 v0.2 真实成功结果未进入当前正式代码时，P1A2Q 仍为 `待激活`。

本修订不新增施工文件或验证项，执行仍必须完成 v0.1 全矩阵。完成仍最多证明按精确索引键读取当前 L1 目标的具名能力，不证明世界引导、世界结构读取或任何 P1A3 真实业务。
