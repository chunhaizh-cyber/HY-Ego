# TARGET-GAP-REGISTERED-COMPARE-CONSUME 注册比较目标差距消费代码实施切片 v0.1

日期：2026-08-16
版本：v0.1
身份：`TARGET-GAP-REGISTERED-COMPARE-CONSUME`
状态：待激活

## 1. 目标与完成条件

按 BIZ-L3-004-01-02 v0.2 和同身份详细设计，新建 provider-only `特征判断收束服务`：接收非零比较请求身份和完整 `需求目标当前事实结果`，构造一次结果位6的 `L2特征比较请求`，恰一次调用 `L2特征结构服务::比较特征`，逐字段互证后形成已满足、存在可处理正差距、方向相反或约束冲突及完整比较证据。

本计划不读取目标材料、不创建需求 / 任务、不写 DATA / 世界 / 业务事实、不修改普通应用，也不实现父 BIZ-L2 正差距组合。

## 2. 正式依据与执行前事实

- 规范：0050、3100、4160、4180、5100、CODE-FILE-01；
- 目标图：`流程图/目标业务流程图/L3/BIZ-L3-004-01-02_按注册比较合同裁决目标差距_目标函数流程图_v0.2.md`；
- 详细设计：`规范/详细设计/20260816_TARGET-GAP-REGISTERED-COMPARE-CONSUME_注册比较目标差距消费详细设计_v0.1.md`；
- 前驱：`TARGET-STATE-CONTRACT-DEFINITION-CONSUME v0.1`、`TARGET-HOST-CURRENT-FEATURE-FACTS-CONSUME v0.1`、`TARGET-CURRENT-FACTS-COMPOSE v0.1`；
- DATA `L2特征结构服务::比较特征`已实现并退出，当前正式 ABI 支持具名关系与差异材料组合请求。

执行前重新 S0：核对本计划精确状态；三个前驱真实结果 / 退出 / 清理均在祖先链，父 DTO 和两个子材料字段与设计一致；DATA 请求 / 结果 / 状态 / 拒绝原因、位掩码、I64安全差异和 const 服务 ABI 未漂移；目标生产文件、工程、唯一代码段、根工程和构建资源已释放。

## 3. 允许文件

生产与工程：

1. `海中鱼巣/领域/目标差距比较.数据.h`；
2. `海中鱼巣/领域/服务.特征判断收束.ixx`；
3. `海中鱼巣.vcxproj`；
4. `海中鱼巣.vcxproj.filters`。

永久记录：

5. `施工记录/20260816_TARGET-GAP-REGISTERED-COMPARE-CONSUME_注册比较目标差距消费施工记录_v0.1.md`；
6. `验证记录/20260816_TARGET-GAP-REGISTERED-COMPARE-CONSUME_注册比较目标差距消费验证记录_v0.1.md`。

生产外临时专项：

7. `验证工具/注册比较目标差距消费验证.cpp`；
8. `验证工具/注册比较目标差距消费验证.vcxproj`；
9. `验证工具/运行注册比较目标差距消费验证.ps1`。

禁止修改父 provider / DTO、普通应用、其它 BIZ 服务、全部 DATA 生产文件、L1、需求 / 任务结构、线程、正式规范、流程图、详细设计、计划和计划索引。

## 4. 实施步骤

1. 按 CODE-FILE-01 新建传统 DTO 头，以自有 `NO_INCLUDES` 宏同时支持普通头直接包含父 DTO / L2 比较 DTO和模块 export 包装，冻结版本、12个稳定状态、请求 / 结果、请求 helper和 `已形成业务裁决()`；入口拒绝在 DATA 调用前零比较证据，其它调用后状态保留完整原始比较结果。
2. 新建 `海中鱼巣.领域.服务.特征判断收束` 模块，export import 父 provider 模块和 `海中鱼巣.领域.服务.L2特征结构`后 export 包装自己的数据头；`特征判断收束服务`构造期只借入一个 `const L2特征结构服务&`，不借入聚合、需求服务、普通应用、L1、owner或第二服务。
3. 纯值互证父结果、G0、目标合同、比较注册、目标特征、当前实例和值生命周期；按详细设计形成左当前事实、右目标状态、目标判断、注册算法族 / 版本及结果位6请求。
4. 恰一次调用 `比较特征`。已比较结果必须互证请求身份、G0、注册 / 算法、实际位6、空排序、关系 / 差异、单位 / 维度 / 分量角色、角色 / 方向、误差合同和左右输入回执。
5. 按低1、等2、高4映射允许关系位；先判命中，再按详细设计收束低、高和等于但不允许的三个分支。完整 DATA 结果原样进入 BIZ 结果，不重算差值。
6. 按详细设计映射 DATA 非成功；差异不可表示、结果位6不支持和值域类拒绝为不可比较，已闭合父材料仍发生结构冲突为内部不一致。捕获资源与其它异常并保持零重试 / 零截止重选。
7. 在根工程唯一登记两个生产文件；以生产外专项覆盖验证矩阵，填写两份永久记录。结果发布 / 索引退出后独立删除三件临时专项并重做根工程和 strict 清理验证。

## 5. 验证与成功标准

- 根工程 x64 Debug / Release Rebuild PASS；
- 临时专项 Debug / Release 具名用例全部 PASS、失败为0，无法由公开 ABI 构造的真实许可 / 资源 / 私有坏形状保持 `NOT_RUN`；
- 全部允许关系位1—7与低 / 等 / 高关系组合精确裁决，等于且允许位为1、4、5分账成立；
- 实际比较请求结果位恰为6，只有具名关系和差异材料，完整结果 / 元数据 / 左右回执与直接 DATA 调用逐字段相等；
- I64差异不可表示、注册不允许差异位和值域拒绝为不可比较，不降级仅关系成功；
- 源码只有一次 `比较特征`，零注册预读 / 算法直调 / DATA写入 / L1 / owner / 仓库 / 缓存 / sleep / retry；
- 两个生产文件工程登记各唯一一次，父 provider / DTO、普通应用和 DATA 文件零差异，生产零专项引用 / 验证痕迹；
- `git diff --check`、`python .\tools\check_specs.py --strict`、UTF-8无BOM和精确白名单通过。

## 6. 激活、发布与边界

本计划只能在三个前驱真实结果发布、索引退出、临时专项清理及生产文件释放后，由索引所有者从最新正式 HEAD 机械匹配最终 ABI并提升为可执行。待激活不授权施工。

若父 DTO / 成功形状、DATA 比较 DTO / 状态 / 拒绝原因、结果位6、I64差异语义、物理归属或工程所有权漂移，固定具名 `DRIFT-TARGET-GAP-REGISTERED-COMPARE-ABI-OR-RESULT-SHAPE`并退回计划支撑；执行者不得降级为仅关系、增加注册预读、算法直调、简化比较 DTO或第二服务临场吸收差异。

代码结果与构建通过最多证明本 BIZ 比较叶。父正差距组合、需求建立、活动重判、任务树、普通应用持有 / getter / 装配、T02、跨进程恢复和系统闭环保持 `NOT_RUN`。
