# INSTINCT-P2 观察、识别、扫描、跟踪本能方法集代码实施切片 v0.2

编号：INSTINCT-P2；路由：#368 / DQ-260；合同：PER-C8 / ABI 2；日期：2026-07-24

设计状态：终审 PASS。当前状态、版本和 plan blob 唯一读取 `计划/计划索引.md`。

## 合同与边界

- 依据：3300、5300、6370；`规范/详细设计/D455任务唯一消费冻结工作包与观察方法详细设计.md` v0.2；`流程图/20260724_PERIPHERAL-TASK_任务唯一消费冻结工作包观察方法施工流程图_v0.2.md`；`规范/详细设计/函数结构知识图谱/20260724_PERCEPTION-D0_D455观察体素生产闭环函数结构知识图谱_v0.2.md`。
- 已实现事实：SELF-C3 方法登记提供者尚未实现；C7 是待实现工作包合同。
- 待实现：`感知本能方法ABI版本 = 2`；方法登记材料、四类结果和统一结果；`执行观察方法`、`执行识别方法`、`执行扫描方法`、`执行跟踪方法`。
- 四方法只读匹配工作包，零直接互调，不访问报告队列、受控材料域或事实仓。

## 文件所有权

允许：`海中鱼巣/领域/合同.感知本能方法.ixx`、`海中鱼巣/领域/服务.感知本能方法.ixx`、`海中鱼巣/领域/自检.感知本能方法.ixx`、`实施记录/20260724_INSTINCT-P2_PER-C8感知本能方法代码实施_Codex断点清单.md`。

禁止：SELF-C3 提供者、C1—C7 / C9—C13、需求 / 任务 / 世界事实写入、工程、入口、运行器和设计文件。

## 施工与 S0

1. S0 核对 plan blob、C7 三工作包合同、SELF-C3 登记只读端口、动作入口稳定键和三个代码文件。
2. S1 建立登记 / 结果 DTO 与依赖倒置端口；服务构造只注入端口和四个内部纯执行器。
3. S2 令四入口分别核对登记、工作包分支、任务 / 方法 / 冻结版本和动作入口键。
4. S3 自检源码覆盖错工作包、登记失效、空观察、空识别、首次扫描、无变化、目标丢失和四方法零直调。
5. S4 核对白名单、ABI、禁止队列 / 仓库依赖和 diff。
6. S5 只提交允许代码与专属记录，推送执行通道分支并恢复 clean。

## 机械验证命令

S0 记录 `$计划段起点 = (git rev-parse HEAD).Trim()`；S4 的 `git diff --check` 与 `git diff --cached --check` 均须退出 0。S5 后运行：

```powershell
$期望 = @('海中鱼巣/领域/合同.感知本能方法.ixx','海中鱼巣/领域/服务.感知本能方法.ixx','海中鱼巣/领域/自检.感知本能方法.ixx','实施记录/20260724_INSTINCT-P2_PER-C8感知本能方法代码实施_Codex断点清单.md') | Sort-Object
$实际 = @(git -c core.quotepath=false diff --name-only "$计划段起点..HEAD") | Sort-Object
if (Compare-Object $期望 $实际) { exit 1 }
if ((@(rg -n "感知本能方法ABI版本\s*=\s*2" '海中鱼巣/领域/合同.感知本能方法.ixx')).Count -ne 1) { exit 1 }
if ((@(rg -n "执行观察方法|执行识别方法|执行扫描方法|执行跟踪方法" '海中鱼巣/领域/服务.感知本能方法.ixx')).Count -lt 4) { exit 1 }
```

全部命令须退出 0；ABI 精确 1 处，四入口合计至少 4 处。

## 验证与收口

- 只做静态 / 专项源码验证，不构建、不运行。
- C7 DTO / SELF-C3 端口 / 动作入口漂移返回 `DRIFT-PER-CONTRACT`；现行登记材料不能适配返回 `DRIFT-PER-CURRENT-INTERFACE`；需要表外文件返回 `DRIFT-PER-FILE-OWNERSHIP`；反向依赖返回 `DRIFT-PER-IMPORT-CYCLE`。
- 方法互调、前置通过后结果版本 / 负载矛盾为 `FAIL-PER-INTERNAL-READBACK`；其它为 `FAIL-PER-VALIDATION`。
- 完成只证明 PER-C8 候选，不证明方法已登记、任务已执行或世界事实已提交。
