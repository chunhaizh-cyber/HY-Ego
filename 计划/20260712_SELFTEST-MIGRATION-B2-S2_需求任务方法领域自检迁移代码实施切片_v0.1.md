# SELFTEST-MIGRATION-B2-S2 需求任务方法领域自检迁移代码实施切片 v0.1

日期：2026-07-12

队列编号：#253 / 设计队列：DQ-145

状态：依赖门控待执行 / 前置 #252 / 阶段与编号由 #251 冻结 / 未改 C++

## 1. 依据与假定

依据第二批流程图 / 详细设计和 #251 正式矩阵。假定剩余需求、任务、方法、召回、动作、结果 / 结算自检与 #212-#235 新建模块可明确去重。任何重复所有权或新接口漂移均退回设计。

## 2. 范围

预期新增 `领域/自检.需求任务.ixx`、`自检.方法召回.ixx`、`自检.动作结果结算.ixx` 真模块，均 `ClCompile + export module + import`。允许这三个预期模块、`入口.cpp`、工程文件和治理；#251 裁决不同则先修订。禁止修改生产状态机 / 授权语义、重复迁移 #212-#235 自检、固定样例替代通用路径、复制方法召回索引或任务夹具。

## 3. 步骤

S0 建去重矩阵；S1 固定真模块骨架和单向 import；S2 迁移夹具与值式报告；S3 迁移正文 / 并发 / 输出；S4 删除入口对应段并登记；S5 全量回归。

## 4. 失败与验收

门控漂移不改代码；普通自检失败不短路；结果、编号、授权关系、任务状态或结算读回变化归追根因。A01-A14、Debug 0/0、完整自检、严格规范、diff、最终 20 轮，提交推送。

## 5. 边界

只迁移承载，不证明任务 / 方法 / 结算完整系统或旧迁移完成。
