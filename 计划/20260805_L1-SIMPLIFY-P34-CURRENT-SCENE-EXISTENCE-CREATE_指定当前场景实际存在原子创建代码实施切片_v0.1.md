# L1-SIMPLIFY-P34-CURRENT-SCENE-EXISTENCE-CREATE 指定当前场景实际存在原子创建代码实施切片 v0.1

日期：2026-08-05

状态：待激活

创建侧正式复核基线：`57b895eb87bb89e88ba1ae371e056a28722abdab`

`cabe1794..57b895eb` 只发布 P12 v0.2 三文档及其索引登记，与 P34 未来代码白名单和机器合同零重叠。

## 1. 目标与完成条件

按配套详细设计新增 `L1当前场景实际存在发布服务`，消费 P7 实际存在写入规格和 P33 当前场景接纳规格，使用 P15 v0.5 的 provider 前双摘要探测与 L1 v2 通用读回，以一个写集在指定当前场景原子发布实际存在身份、资格值/槽和唯一直接成员关系。

完成必须同时满足：

- 同义已发布在 provider 前机械返回首次完整结果；
- 首次路径固定 P7一次、P33一次、L1提交一次；
- 写集精确1节点+1值+1关系+1属性槽，事实代次只推进一次；
- L1 独占许可内读回精确覆盖节点、关系、值、属性槽四项；
- 发布者只从公开请求、首次映射和通用读回映射 `实际存在事实`；
- 发布前失败零普通事实变化；发布后矛盾保留代次、失败见证和隔离，不补写；
- 新能力属于具名领域发布者，不扩写旧 P7 组合服务的 L1 直连职责；
- 专项、完整自检、Debug/Release Rebuild、strict、模块扫描和精确白名单全部通过。

## 2. 正式设计包与未来代码白名单

设计包：

```text
规范/详细设计/20260805_L1-SIMPLIFY-P34-CURRENT-SCENE-EXISTENCE-CREATE_指定当前场景实际存在原子创建详细设计_v0.1.md
流程图/20260805_L1-SIMPLIFY-P34-CURRENT-SCENE-EXISTENCE-CREATE_指定当前场景实际存在原子创建函数流程图_v0.1.md
计划/20260805_L1-SIMPLIFY-P34-CURRENT-SCENE-EXISTENCE-CREATE_指定当前场景实际存在原子创建代码实施切片_v0.1.md
```

未来代码白名单：

```text
海中鱼巣/领域/L1实际存在.数据.h
海中鱼巣/领域/发布.L1当前场景实际存在.ixx
海中鱼巣/领域/自检.L1当前场景实际存在发布.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
海中鱼巣/启动.应用程序.ixx
施工记录/20260805_L1-SIMPLIFY-P34-CURRENT-SCENE-EXISTENCE-CREATE_施工记录.md
验证记录/20260805_L1-SIMPLIFY-P34-CURRENT-SCENE-EXISTENCE-CREATE_验证记录.md
```

不得修改 P7/P15/P33 生产实现、现有 `组合.L1实际存在场景.ixx`、`服务.世界树.ixx`、旧入口、其它设计/计划、P0-FIX、路线输入或旧 WORLD-TREE WIP。若实际 P15/P33 接口要求修改其所有者文件，视为设计漂移，退回计划支撑。

## 3. 施工步骤

### S0：重新绑定正式事实

1. 核对 `main==origin/main`、0/0、index空和全部 dirty 所有者。
2. 核对本计划精确版本、plan blob、状态为 `可执行`，并确认 P15 v0.5、P33 v0.1 真实结果已接受。
3. 机械读取 P15 实际 L1 v2 DTO、探测/提交状态、领域操作标签登记和四对象读回形状；读取 P7/P33 实际签名及字段。
4. 确认 `0x0B` 创建域保持唯一，`0x50333401` 操作标签无冲突；确认白名单与活动代码段、工程/启动/自检资源无所有权重叠。
5. 读取正式自检总数、相邻顺序和 MSVC 模块数；不得照抄本计划猜测数值。

任一接口、标签、状态、读回计划、所有权或白名单漂移，立即停止本切片并具名退回计划支撑。

### S1：DTO 与发布模块

1. 在 `L1实际存在.数据.h` 精确追加两个版本常量和 `当前场景实际存在创建请求`，字段、顺序、默认值和相等比较不得改变。
2. 新建 `海中鱼巣.领域.发布.L1当前场景实际存在` 模块和 final 服务类，只保存 L1/P7/P33 三个非拥有引用。
3. 固定 `0x0B` 幂等域和 `0x50333401` 操作标签；复用 P15 已发布的确定性编码/SHA-256能力，不复制第二算法实现。
4. 入口校验后先形成请求意图摘要并调用 `L1事实基座服务::探测幂等`；只有 `未找到` 才依次调用 `L1实际存在服务::形成实际存在写入规格` 与 `L1场景结构服务::形成当前场景接纳规格`。
5. 按详细设计形成执行证据、固定写集和四项计划，调用 L1 v2 恰一次。
6. 只从 L1 首次映射/读回机械形成结果；删除任何本地 `锁_`、`已提交_`、重复提交或事务外普通读回方案。

### S2：专项与工程登记

1. 新建专项自检，覆盖详细设计第10节全部矩阵。
2. 工程和 filters 只登记两个新模块；启动聚合只增加模块 import、自检薄包装和一个由 S0 裁决的唯一顺序项。
3. 不装配生产线程，不给 P32 增加写函数，不迁移旧调用方。
4. 形成两份专属记录，逐项记载实际接口、调用计数、写集、读回、并发、故障注入、构建和扫描证据。

## 4. 激活门禁

本计划当前保持 `待激活`。只有以下条件全部成立，交互/执行侧才可单独登记 `可执行`：

1. P15 v0.5 结果已接受，实际 L1 v2 探测、双摘要、通用读回、失败见证、隔离及全部现有调用点迁移成立；
2. P33 v0.1 结果已接受，实际接纳规格接口、目标当前性、成员关系类型和截止合同匹配；
3. P7 接受结果和实际存在规格/结果接口保持匹配；
4. P15/P33/P7 已释放白名单、工程、启动、自检、构建和模块扫描资源；
5. 若 P32 已进入代码执行，其 `服务.世界树.ixx` 与本叶零路径重叠，且本叶仍不修改门面；
6. 从新正式 HEAD 重做完整 S0 后只有本计划一个唯一可执行代码段。

待激活不证明任何代码、编译、原子发布、接线或业务能力存在。

## 5. 验证命令与证据矩阵

执行侧从正式工程入口按当前施工规范运行，至少包括：

```powershell
git diff --check -- <本计划白名单>
python .\tools\check_specs.py --strict
cmake --build --preset windows-msvc-debug --target 海中鱼巣 --clean-first
cmake --build --preset windows-msvc-release --target 海中鱼巣 --clean-first
& .\out\build\windows-msvc-debug\Debug\海中鱼巣.exe --self-test-exit
& .\out\build\windows-msvc-release\Release\海中鱼巣.exe --self-test-exit
pwsh -NoProfile -Command '$PSNativeCommandUseErrorActionPreference=$false; & ".\tools\clang_ast\扫描.MSVC模块依赖.ps1" -配置 Debug -平台 x64'
pwsh -NoProfile -Command '$PSNativeCommandUseErrorActionPreference=$false; & ".\tools\clang_ast\扫描.MSVC模块依赖.ps1" -配置 Release -平台 x64'
```

随后运行 Debug/Release `--self-test-exit`、P34 专项、P7/P33/P15 回归、MSVC 双配置模块扫描和精确 staged 白名单检查。命令若因正式工程入口调整而变化，只能按现行施工规范替换命令，不得降低验证语义。

验证记录必须逐项证明：入口前拒绝、摘要标准向量、探测短路、provider 调用计数、共同截止、精确写集、精确四项读回、同义/异义/跨入口幂等、代次竞争、发布前资源失败、发布后隔离、异常映射、无旁路静态扫描和两配置回归。

## 6. 停工条件与完成声明

以下情况停工并保留现场：P15/P33/P7 实际合同不匹配；`0x0B` 或操作标签冲突；需修改白名单外 provider/L1/门面；组合服务直连 L1 才能实现；发布后读回不能覆盖四项；验证持续失败且无法在本计划范围归因；Git/所有权/发布租约冲突。

完成后只可声明：P34 白名单内源码与验证证明，指定当前场景实际存在发布者能消费 P7/P33 规格，经 P15 v0.5 双摘要和一次 L1 v2 提交原子形成身份、资格和直接成员关系，并机械映射同锁读回。

禁止声明：P32 写门面或治理调用已接线、特征批写、状态动态、动作动态、因果、任务/结算、恢复装配、生产线程、旧入口退出、集成验收、STEP-4、STEP-5 或治理闭环完成。
