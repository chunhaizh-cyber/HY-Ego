# EXISTENCE-CONCEPT-APPLICATION 存在概念生成复用与逐层抽象代码实施计划

日期：20260905
版本：v0.1
计划身份：EXISTENCE-CONCEPT-APPLICATION
状态：仅由[计划索引](计划索引.md)登记；本文不是可执行状态或实现完成证明。
目标来源：用户“现在生成存在概念树”；世界具体存在为底部引用，提取部分特征后继续抽象，最终到存在根。

## 1. 起点、目标与依据

当前设计代码基线 `488652215e6ca54b962301b04185b0f2c7d1ae60`，前置特征概念应用结果 `616a58a9` 已发布。施工 S0 仍须核对实际 HEAD、正式文档版本、index、允许WIP、唯一所有者和验证资源。HEAD 非语义相关前进不自动作废设计。

完整合同唯一见[详细设计](../规范/详细设计/20260905_EXISTENCE-CONCEPT-APPLICATION_存在概念生成复用与逐层抽象详细设计_v0.1.md) v0.1。机器依据为0050、2220 v1.7、7110 v1.2、7100 v1.2、7140 v1.3、4240 v0.14；具体API、DTO、定义编码、原键恢复、来源历史修复、预算和验证矩阵不在计划重复。

完成目标：真实已发布存在与逐项特征见证可生成/精确复用存在概念，建立双向支持；按严格子集逐层形成上位，替换根直连同时保留多父，空约束复用唯一存在根；读回可重建业务树；源特征概念宿主退出后抽象持续可用。代码、隔离验证和专属记录精确提交并普通推送后方可收口。

## 2. 唯一施工切片

由交互智能体登记唯一执行所有者；本计划整体为一个复杂代码切片，不拆 provider/app/工程并行写。以下六个文件是最终允许范围：

1. 新建 `海中鱼巣/业务/应用服务.存在概念类.ixx`。
2. 修改 `海中鱼巣/领域/服务.L2概念结构.ixx`：仅详细设计第8节的已发布FC形成来源历史核验及必要私有调用点；不改 generic 其它定义引用、持久编码、公开 ABI 或成熟规则。
3. `海中鱼巣.vcxproj`：只登记新模块。
4. `海中鱼巣.vcxproj.filters`：只登记新模块，保留编码/BOM及其它项目行。
5. `施工记录/20260905_EXISTENCE-CONCEPT-APPLICATION_存在概念生成复用与逐层抽象施工记录_v0.1.md`。
6. `验证记录/20260905_EXISTENCE-CONCEPT-APPLICATION_存在概念局部服务验证记录_v0.1.md`。

不写世界存在/特征/特征值owner、特征概念app、L1、共享DTO、主程序/装配、UI、共享知识索引、其它计划或规范。必须修改范围之外代码或改变本合同机器语义时仅停止受影响部分并具名退回设计修订。

## 3. 实施顺序与复用

1. S0核对owner当前接口、详细设计与7110版本，读知识入口并从当前源复核用途；确认所有者和专属验证根。
2. 先修FC来源历史核验，使公开模板读取与generic创建/改边全图使用同一合法口径；源概念结构当前、形成来源历史，不能整体放宽引用。
3. 新增存在业务DTO、显式模板合取/实例见证/严格子集私有辅助，复用owner创建/支持/直接边与完整组读回。构造验证同库同实例。
4. 实现只读准备、值式自校验、当前重新准备与过期原key重放，保守分账派发后可能发布；再接便捷API与业务树投影。
5. 仅登记新模块到工程。使用真实服务fixture执行详细设计矩阵；修复本切片代码错误后以最终源码验证。
6. 施工/验证记录写完整命令、精确输入hash、成功与NOT_RUN范围、实际函数签名、旧接口回归、临时材料保留和知识变更清单。停写交回根唯一发布，不自行扩大Git范围。

来源材料和world事实已经有真实提供者，不依赖新世界创建计划；本业务接收已发布实例。若其它复杂代码计划正使用同owner/工程/验证资源，等待唯一切片串行交接，不能以可执行登记绕过所有权。

## 4. 验证与收口

必须完成：

```powershell
git diff --check
python .\tools\check_specs.py --strict
```

代码验证为专属隔离C++20模块closure的Debug/Release构建与实际运行，使用详细设计第10节完整矩阵；测试断言Release不失效。保留最终生产输入hash、fixture、project/runner和实际日志，不把较早源码测试结果绑定到最终源码。提供者历史修复必须回归既有特征概念行为与generic非FC引用保护。

验证资源为 `D:\TEMP\EXISTENCE-CONCEPT-APPLICATION\海中鱼巣-01a07099-existence_concept_execute`。不使用共享默认build输出、设备、持久数据库或其他任务测试目录。新日志在专属根，失败不删除现场，完成后保留理由与位置写验证记录。

只证明显式模板组合的精确复用、真实实例支持、严格子集抽象、来源历史独立性及业务值式DAG投影；不证明跨模板语义等价自动归并、自动学习/成熟、自然语言、主程序设备接线、世界创建闭环、跨进程恢复或独立集成验收。实现记录与代码发布不自动改变计划索引，由计划所有者按精确结果完成登记收口。
