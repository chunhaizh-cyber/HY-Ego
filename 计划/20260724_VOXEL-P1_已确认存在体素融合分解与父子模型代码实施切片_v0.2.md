# VOXEL-P1 已确认存在体素融合、分解与父子模型代码实施切片 v0.2

编号：VOXEL-P1；路由：#371 / DQ-263；合同：PER-C11 / ABI 2；日期：2026-07-24

设计状态：终审 PASS。当前状态、版本和 plan blob 唯一读取 `计划/计划索引.md`。

## 合同与边界

- 依据：1140、1150、6240、6300；`规范/详细设计/观察世界事实与体素融合详细设计.md` v0.2；`流程图/20260724_OBSERVE-VOXEL_观察世界事实与体素融合施工流程图_v0.2.md`；`规范/详细设计/函数结构知识图谱/20260724_PERCEPTION-D0_D455观察体素生产闭环函数结构知识图谱_v0.2.md`。
- 已实现事实：节点直接仓、正式关系和事务执行器候选可作底座；C9 与正式类型化记录适配尚未接线。
- 待实现：`存在体素融合ABI版本 = 2`；观察体素材、模型身份、体素单元 / 模型记录、消费记录、融合规格 / 结果；`融合已确认存在体素材`。
- 不新增体素节点类型；模型、单元和消费记录是归属于存在 / 场景的领域类型化权威记录。材料一次消费键为材料身份 + 新增确认关系 + 消费版本。

## 文件所有权

允许：`海中鱼巣/领域/合同.存在体素融合.ixx`、`海中鱼巣/领域/数据操作.存在体素融合.ixx`、`海中鱼巣/领域/服务.存在体素融合.ixx`、`海中鱼巣/领域/自检.存在体素融合.ixx`、`实施记录/20260724_VOXEL-P1_PER-C11体素融合代码实施_Codex断点清单.md`。

禁止：C9 提供者、节点 / 关系 / 事务底座、C12 / C13、工程、入口、运行器、显示网格和设计文件。

## 施工与 S0

1. S0 核对 plan blob、C9 观察体素材、节点 / 关系 / 类型化记录 / 执行器只读接口和四文件所有权。
2. S1 建立 I64 微米坐标、百万分占据值、模型 / 单元 / 消费记录 DTO 与稳定状态。
3. S2 锁外形成融合候选；会话内复核存在、场景、父模型、材料未消费和最外边界为空。
4. S3 写模型 / 单元 / 适用父子关系，完整读回，最后写消费记录并发布；撤销逆序；专项自检覆盖重复消费、越界、质量、坐标、父子模型、撤销和幂等。
5. S4 核对白名单、ABI、import 方向和 diff。
6. S5 只提交允许代码与专属记录，推送执行通道分支并恢复 clean。

## 机械验证命令

S0 记录 `$计划段起点 = (git rev-parse HEAD).Trim()`；S4 的 `git diff --check` 与 `git diff --cached --check` 均须退出 0。S5 后运行：

```powershell
$期望 = @('海中鱼巣/领域/合同.存在体素融合.ixx','海中鱼巣/领域/数据操作.存在体素融合.ixx','海中鱼巣/领域/服务.存在体素融合.ixx','海中鱼巣/领域/自检.存在体素融合.ixx','实施记录/20260724_VOXEL-P1_PER-C11体素融合代码实施_Codex断点清单.md') | Sort-Object
$实际 = @(git -c core.quotepath=false diff --name-only "$计划段起点..HEAD") | Sort-Object
if (Compare-Object $期望 $实际) { exit 1 }
if ((@(rg -n "存在体素融合ABI版本\s*=\s*2" '海中鱼巣/领域/合同.存在体素融合.ixx')).Count -ne 1) { exit 1 }
if ((@(rg -n "融合已确认存在体素材" '海中鱼巣/领域')).Count -lt 1) { exit 1 }
if ((@(rg -n "体素节点类型|创建体素节点" '海中鱼巣/领域/合同.存在体素融合.ixx' '海中鱼巣/领域/数据操作.存在体素融合.ixx')).Count -ne 0) { exit 1 }
```

全部命令须退出 0；ABI 精确 1 处、融合入口至少 1 处、体素节点本体零命中。

## 验证与收口

- 静态 / 专项源码验证，不构建、不运行。
- C9 / 空间类型合同漂移返回 `DRIFT-PER-CONTRACT`；节点 / 关系 / 事务底座不能适配返回 `DRIFT-PER-CURRENT-INTERFACE`；需要表外文件返回 `DRIFT-PER-FILE-OWNERSHIP`；反向 import 返回 `DRIFT-PER-IMPORT-CYCLE`。
- 前置通过后空间、父子、消费或发布读回不一致为 `FAIL-PER-INTERNAL-READBACK`；其它验证失败为 `FAIL-PER-VALIDATION`。
- 完成只证明 PER-C11 候选，不证明体素接入生产或场景先验可用。
