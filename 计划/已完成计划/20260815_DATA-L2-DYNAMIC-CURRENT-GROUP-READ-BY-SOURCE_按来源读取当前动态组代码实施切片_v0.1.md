# 按来源读取当前动态组代码实施切片 v0.1

日期：2026-08-15

计划身份：`DATA-L2-DYNAMIC-CURRENT-GROUP-READ-BY-SOURCE`

状态：设计就绪；不等于代码已实现

## 1. 目标与完成条件

消费已发布的 L1 owner-aware 来源当前值组 provider，在唯一 `L2动态结构服务` 内实现按来源稳定编码读取有界当前完整动态组。

完成必须满足：请求 DTO、有效 helper 和服务函数逐字实现；来源发现只经 L1 正式入口；起始 / 结束时间值角色配对；完整动态组同截止、按动态身份严格升序、空组成功、超预算零载荷拒绝；根工程双配置与风险相称的生产外临时检查通过；临时检查在结果提交前清理；永久记录如实列出 `NOT_RUN`。

## 2. 正式依据与依赖

- 0050、4015、4030、4190、4230；
- `计划/数据服务最终需求清单.md` 的 DATA-EXT-05；
- [详细设计](../规范/详细设计/20260815_DATA-L2-DYNAMIC-CURRENT-GROUP-READ-BY-SOURCE_按来源读取当前动态组详细设计_v0.1.md)；
- L1 provider 结果提交 `8e0928671d3584d399ce4d796b890401073e5676`；
- 共同预算状态结果提交 `86d10082dcd8fa3ae2643ad14db19921d98da1ff`。

执行 S0 若发现 L1 正式入口、动态 owner、起止时间双角色、完整动态读取或两个目标生产文件发生实质漂移，具名返回 `DRIFT-DATA-L2-DYNAMIC-SOURCE-READ-BASELINE-CHANGED`；不得回退全仓扫描、两次最新读取、缓存或 L2 私有仓访问。

## 3. 精确范围

生产代码白名单：

1. `海中鱼巣/领域/L2动态结构.数据.h`；
2. `海中鱼巣/领域/服务.L2动态结构.ixx`。

永久记录：

1. `施工记录/20260815_DATA-L2-DYNAMIC-CURRENT-GROUP-READ-BY-SOURCE_按来源读取当前动态组施工记录_v0.1.md`；
2. `验证记录/20260815_DATA-L2-DYNAMIC-CURRENT-GROUP-READ-BY-SOURCE_按来源读取当前动态组施工检查记录_v0.1.md`。

允许临时建立、最终必须删除且不提交：

1. `验证工具/L2按来源读取当前动态组参数检查.cpp`；
2. `验证工具/L2按来源读取当前动态组参数检查.vcxproj`；
3. `验证工具/L2按来源读取当前动态组参数检查.vcxproj.filters`。

不修改 L1、共同枚举、生产工程、普通应用、世界树聚合、状态、因果、BIZ、EVENT、SELF、概念候选、流程图、路线、历史删除或 `.codex-build`。

## 4. 实施步骤

1. 从最新正式 HEAD 重做 Git、index、目标文件、接口和所有权 S0。
2. 在动态数据头实现详细设计第 3.1 节的请求 DTO 和纯值有效 helper；复用现有动态组结果。
3. 在动态服务实现唯一 const 来源查询：读取登记、按 `2 * (最大数量 + 1)` 饱和换算 L1 值预算、调用正式 L1 来源入口、验证起始 / 结束角色配对、按动态身份去重排序、逐项同守卫完整读回后执行公共预算整组拒绝。
4. 根工程 x64 Debug Rebuild 通过后运行 Release Rebuild。
5. 建立临时生产外公开入口检查，覆盖第 5 节矩阵；失败只在本计划范围内修复。
6. 运行 strict、diff、UTF-8、ABI、范围和生产零验证痕迹检查。
7. 删除临时三件套及专属二进制 / 中间文件，重跑根工程双配置和静态清理门禁。
8. 写两份永久记录，只提交两个生产文件和两份记录，普通 push 并远端读回。
9. 单独登记计划退出；从清理 HEAD 继续最终需求清单中最早且语义已冻结的独立缺口。

## 5. 检查矩阵

1. 错合同、零守卫、零来源、零预算分别入口拒绝且零载荷；
2. 当前来源存在但无动态时成功空组；
3. 同来源一个 / 多个动态完整读回并按动态身份严格升序；
4. 不同来源严格隔离；
5. 每个候选的起始 / 结束时间值角色配对、所属动态和来源逐字段互证；
6. 动态预算等于数量成功，少一返回数量预算不足且零载荷；
7. 来源未找到、已退出与陈旧守卫分账；
8. 动态退出后不再命中，既有历史读取仍保持；
9. 读取前后事实代次不推进；
10. 既有按主体 / 前状态 / 后状态当前动态组读取和单动态当前 / 历史读取保持。

真实资源耗尽、私有 owner / 索引 / 动态损坏、共享锁长期公平、跨进程恢复、时间范围查询和正式 BIZ 消费没有合法稳定注入时如实 `NOT_RUN`，不得增加生产故障 seam。

## 6. 完成声明

本计划完成只证明 `L2动态结构服务` 的当前来源查询 provider 已实现和发布，不证明时间范围查询、因果来源查询、DATA-EXT-05 整项或数据服务最终需求清单整体完成。适用性由后继合法消费者实际使用时判断。
