# ARCH-LAYER-S0 仓库服务双路径与调用点事实复核计划 v0.1

日期：2026-07-13

队列编号：#262 / 设计队列：DQ-154

状态：已完成 / 已形成四张正式事实矩阵 / 纯只读代码事实复核 / 不登记自检阶段 / 未改 C++ / 已归档

## 1. 依据

仓库服务分层规范、两份 JY-316 流程图、分层纠偏详细设计和 `实施记录/20260713_仓库与服务分层纠偏S0当前代码事实扫描_Codex断点清单.md`。流程图、详细设计与本计划范围一致。

## 2. 核心处理

逐函数和逐调用点冻结四张矩阵：四仓库生命周期入口；协调 / 会话 / 仓库权力；领域文件原始令牌与直接仓库引用；无令牌 / 带令牌 / 候选写替代顺序。每项登记生产、自检、兼容、禁止分类，当前许可所有者、最后发布点、失败清理和后继计划。

重点覆盖存在、场景、世界、状态、动态、特征值、特征、语素、概念、因果、需求、任务、方法、初始化和线程路由。文本命中只能作为入口，最终矩阵必须落到真实函数签名和调用点。

## 3. 允许与禁止

允许只读全部代码、规范、计划和实施记录；只写本计划归档、正式 S0 矩阵、断点、计划索引、队列和当前状态。禁止修改 C++、工程、流程图、详细设计和 `#214/#257`，禁止构建运行，禁止提前删除兼容入口。

## 4. 产物与验证

正式产物：`实施记录/20260713_ARCH-LAYER-S0_仓库服务双路径与调用点事实复核_Codex断点清单.md`。A01-A12 要求所有写入口和调用点可追溯到唯一替代计划，未识别调用点必须列为断点而非猜测。执行严格规范、diff、提交和推送。

## 5. 完成边界

只证明当前调用矩阵冻结；不证明仓库、会话、服务或兼容路径已修改。
