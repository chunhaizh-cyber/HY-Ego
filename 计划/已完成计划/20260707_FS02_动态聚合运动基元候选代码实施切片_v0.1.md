# FS-02 动态聚合运动基元候选代码实施切片 v0.1

日期：2026-07-07
状态：已归并关闭 / 证据保留 / 已移出待确认目录 / 不再单独确认 / 不单独入队 / 未改代码 / 未迁移旧函数

路线修订标记：本切片已被 `计划/20260707_服务操作函数矩阵第一批代码实施包_v0.1.md` 归并，后续只能作为服务矩阵和流程图迁移包的证据材料，不再作为独立 C++ 实施入口。

## 1. 依据

```text
规范/详细设计/动态聚合与运动基元候选详细设计.md
实施记录/20260707_FS02_动态聚合运动基元候选详细设计专项生成_Codex断点清单.md
实施记录/20260707_FS02_动态聚合因果模板删除实例状态后续S0当前代码事实扫描_Codex断点清单.md
规范/详细设计/状态动态二次特征因果服务增强详细设计.md
规范/错误结果与入口拒绝规范.md
规范/领域服务授权写入规范.md
```

## 2. 确认范围

本切片确认后，允许在动态服务内实现第一轮只读候选入口：

```text
读取动态聚合候选(来源动态集合)
读取运动基元候选(来源动态集合)
动态候选是否可用(候选)
```

同步确认 `规范/详细设计/动态聚合与运动基元候选详细设计.md` 作为本切片第一轮代码依据。

## 3. 明确排除项

```text
不实现动态聚合算法。
不实现运动基元识别算法。
不创建抽象动态、二次特征结论、方法候选或因果模板。
不把候选评分写入需求、任务、方法、状态或因果事实。
不接 D455、体素、外设、控制面板、SQL、WebView2 或线程。
不迁移旧函数体，不新增旧主信息字段。
```

## 4. 目标服务和结构

目标服务：

```text
动态服务
```

目标结构为运行期只读候选材料：

```text
动态聚合候选
运动基元候选
候选拒绝原因
```

候选默认可丢弃、可重建，不作为长期机器事实。

## 5. 允许文件

```text
海中鱼巣/领域/动态服务.h
海中鱼巣/入口.cpp
计划/计划索引.md
项目记忆/*
实施记录/20260707_FS02_动态聚合运动基元候选代码实施_Codex断点清单.md
```

## 6. 禁止文件

```text
海中鱼巣/核心/*.h
海中鱼巣/领域/方法服务.h
海中鱼巣/领域/因果服务.h
海中鱼巣/领域/任务服务.h
海中鱼巣/领域/需求服务.h
旧项目源码
SQL / 控制面板 / D455 / 体素 / 外设相关文件
工程文件，除非构建失败证明必须修正
```

## 7. 实施步骤

```text
S0：读取动态服务当前代码事实，复核现有动态材料入口。
S1：新增动态聚合候选只读结构和入口前置拒绝。
S2：新增运动基元候选只读入口，要求来源动态同场景、有主体、有时间戳、有目标和前后值。
S3：默认入口补候选可用、跨场景拒绝、缺时间戳拒绝、外部材料排除和候选不裁决事实验收输出。
S4：同步计划索引、项目记忆和实施断点清单。
```

## 8. 验收方式

```powershell
msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m
.\x64\Debug\海中鱼巣.exe
git diff --check
python .\tools\check_specs.py
rg -n "D455|体素|控制面板|SQL|WebView|std::thread|std::async|线程" 海中鱼巣/领域/动态服务.h 海中鱼巣/入口.cpp
```

## 9. 完成声明边界

完成后只能声明：

```text
动态聚合和运动基元第一轮只读候选入口已实现并验证。
候选拒绝路径不改变权威结构。
```

不得声明：

```text
动态聚合算法完成。
运动基元识别完成。
方法候选、二次特征候选或因果模板已接入。
旧动态能力已迁移。
```

## 10. 待确认问题

```text
FS02-DYN-IMP-Q1：是否确认本切片同步确认动态聚合与运动基元候选详细设计。
FS02-DYN-IMP-Q2：第一轮是否只允许动态服务内部只读候选结构，不持久化候选节点或关系。
```
