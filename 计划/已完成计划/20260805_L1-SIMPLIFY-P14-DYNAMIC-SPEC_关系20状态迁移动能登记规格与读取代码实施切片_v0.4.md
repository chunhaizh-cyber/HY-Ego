# L1-SIMPLIFY-P14-DYNAMIC-SPEC 关系20状态迁移动能登记、规格与读取代码实施切片 v0.4

计划身份：`L1-SIMPLIFY-P14-DYNAMIC-SPEC`

版本：`v0.4`

状态：可执行

创建侧支撑基线：`a300acd47465e62b1df0366d600ec6eda878d0fe`

替代：P14 v0.3

具名漂移：`INTEGRATION-ACCEPTANCE-FAIL-P14-V03-PRODUCTION-BOUNDARY-LOG-DIAGNOSTIC-CONTRACT`

## 1. 目标、事实与最小裁决

P14 v0.3 代码提交 `bfae0a2a2a1e72a68fe0e611e88ada7f9446b73b` 的八文件结果保留；本计划
只补齐逐函数诊断责任、同一生产程序正式日志观察和验收适用性，不把 P16 或其它后继生产
消费者接入 P14。

正式验收已确认：P14 四公开入口当前只有顺序390自检调用，生产运行期调用0；Debug/Release
自检退出0但 `日志/运行.log` 无 P14 专项命中；32个可执行函数均无 CODE-FILE-01 /
ACCEPTANCE-01 诊断责任登记；Release 仍接受 `--self-test-exit`。现行单一生产目标规则
只禁止第二 exe、第二目标和第二装配，允许同一目标的受控自检模式，因此不得关闭该参数；
应修订验收适用性，不能把它当成正式构建失败。

P14 正式接线和正式读回继续是当前原目标之外的后继消费者门禁，P16 不得进入本计划。P14
仍保持 G7/变体3/`0x15000F01U`、L1 v2、十节点、事实互证、`0x44`、十五键、顺序390、
单生产程序以及排除崩溃/重启/跨进程恢复。

## 2. 精确白名单与禁止项

继续只允许 v0.3 原八路径：

```text
海中鱼巣/领域/L1动态.数据.h
海中鱼巣/领域/服务.L1动态.ixx
海中鱼巣/领域/自检.L1动态.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
海中鱼巣/启动.应用程序.ixx
施工记录/20260805_L1-SIMPLIFY-P14-DYNAMIC-SPEC_施工记录.md
验证记录/20260805_L1-SIMPLIFY-P14-DYNAMIC-SPEC_验证记录.md
```

不新增文件、不修改 `海中鱼巣/核心/日志系统.h`、不新增 exe/工程目标/装配/链接/自检解释者，
不修改 P16/P15/P12/P13/P9/P10/P11、核心 L1、P81、P0-FIX、路线、旧 WORLD-TREE、
`.codex-build`、计划索引或其它 WIP。日志目录是运行期观察输出，不是提交白名单文件。

## 3. 机械施工合同（仅执行侧使用，本轮不实施）

1. 在 `自检.L1动态.ixx` 的非权威 `L1动态自检报告` 增加 `未分类异常` 布尔字段；
   `catch (...)` 只设置该字段并保持报告失败计数，不吞掉结构化失败，不写日志。
2. 在四个代码文件的函数合同附近逐项登记详细设计 §4 的 32/32 诊断责任；纯值谓词、
   optional 查找、编码和哈希函数使用 `无适用错误分支`，内部/服务结果使用 `向上送出`，
   `运行L1动态专项自检` 使用 `本地记录`，`运行完整自检模式` 使用 `向上送出`。
3. 在现有 `启动.应用程序.ixx::运行L1动态专项自检()` 中复用 `记录运行日志` 恰一次，
   固定模块/入口/观察标识/28项数量/失败数量/未分类异常/正式接线不适用/正式读回不适用；
   `未分类异常=1` 时再调用 `记录逻辑错误日志` 恰一次。日志失败不能改变返回状态、写集、
   事实代次或任何机器事实。
   两个 API 的固定签名均为
   `bool(std::wstring_view 模块, std::wstring_view 入口, std::wstring_view 内容)`；返回值仅是
   写入观察，不进入机器结果。
4. 保持 `--self-test-exit` 为同一 `海中鱼巣.exe` 的受控模式；不得删除、复制、改成第二
   入口或新建 Release 专用目标。`运行生产运行期模式()` 继续不调用 P14。
5. 施工记录和验证记录只记录上述实现、日志和验收适用性；不得把自检结果写成生产接线或
   正式读回完成。

## 4. 验证矩阵与验收适用性

创建侧不构建、不运行、不修改代码，只验证文档、路径、责任32/32、单目标和日志字段。
执行侧从激活后新 HEAD 重做 S0，并至少完成：

- Debug/Release 单一 `海中鱼巣.exe` Rebuild；保留并运行受控 `--self-test-exit`；
- 28项 P14 自检、未分类异常传播和同一生产程序 `日志/运行.log` 的 S01 唯一命中；
- 逻辑异常时 `日志/逻辑错误.log` 恰一次，合法拒绝/重复/漂移/资源失败不误记逻辑错误；
- 逐函数诊断责任32/32、物理文件归属、工程/filters/顺序390和无第二目标静态门禁；
- 新独立验收清单按 `ACCEPTANCE-01`：只确认单一目标的组件构建与自检可观察性；正式接线和正式读回
  缺失，`NOT_APPLICABLE` 仅作未覆盖分类，不计入服务集成 PASS，结论最多 `自检通过`/`局部可继续`；
- P16、生产动态发布、跨域原子事务、崩溃/重启/跨进程恢复均列未覆盖，不能升级结论。

日志只作人读观察和逻辑诊断证据；验收仍须区分结构化自检结果、L1 权威事实、正式消费者
结果和独立读回，不能以日志文本或退出码替代机器事实。

## 5. 完成边界、状态与交接

P14 v0.3 已有代码和验收失败证据不被抹除；索引发布时应由 v0.3
`可执行` 转为 `已退出`，并将 v0.4 直接登记为唯一 `可执行` 计划，避免再次提交状态激活。
执行侧仍须从该索引提交后的新 HEAD 完整重做 S0；不得直接激活 P16 或其它后继代码计划。
只有 v0.4 代码提交、独立验收和交互侧重新接受后，才可另行裁决 P16 是否满足真实依赖。

最多声明：P14 诊断责任和同一生产目标日志观察合同已具备可施工设计。不得声明 P14 有
生产消费者、正式接线、正式独立读回、生产动态发布、P16 原子发布、恢复、动作/因果或
治理闭环完成。
