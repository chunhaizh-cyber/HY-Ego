# L1-LEGACY-BUSINESS-WRITE-SURFACE-RETIREMENT 旧业务标签意图执行证据写入口物理退出代码实施切片 v0.1

日期：2026-08-08

状态：待激活

## 1. 目标与依赖

按配套详细设计，在状态动态原子联合发布中性 CRUD 迁移结果进入正式 main 后，机械删除核心 L1 无消费者的旧 `提交写集`、旧 `探测幂等`、仓库实现和旧探测合同。只退出普通生产 legacy 写通道，不删除 P13/P14 纯规格或历史幂等账、审计、快照和恢复数据合同。

- 详细设计：`规范/详细设计/20260808_L1-LEGACY-BUSINESS-WRITE-SURFACE-RETIREMENT_旧业务标签意图执行证据写入口物理退出详细设计_v0.1.md`
- 具名依赖：`L2-STATE-DYNAMIC-ATOMIC-PUBLISH-NEUTRAL-CRUD-MIGRATION`，plan blob=`2a86422a99d8314c00c0f0593b0fe72ff1565f83`；必须绑定其尚未产生的真实结果提交。
- 设计基线：`a99ba19f004c4d9314c6803e30eb6de43a61dde7`；目标 blob：服务=`d3b13e6081a2b17a25beb85a19cd89cb6a81b6fa`，仓库=`8a1c6c7a17c7b0412501bc6b588df0f8bd8d2218`，合同=`cadb06062ccc4c231946b976e36fedccebef2619`，只读核心数据头=`04a345d009b1849142c4d175e00cb4004eff6dd6`。

## 2. 激活前 S0

1. 确认 main/origin 同步、index 为空、取得唯一活动代码段和发布租约；保留异主 WIP。
2. 从计划索引读取依赖结果提交、结果 blob、接受结论和本计划精确 plan blob；依赖未退出时停止。
3. 全仓扫描旧核心入口。除目标定义外，`L1事实基座服务::提交写集`、`L1事实基座服务::探测幂等`、仓库旧 `提交/探测幂等` 和旧探测 DTO 消费必须为 0。
4. 重算三个目标模块和核心数据头 blob。若中性迁移修改目标模块或出现新的消费者，具名退回计划支撑，不得按旧行号删除。
5. 确认 P13/P14 仍只形成 `L1写集请求` 规格，不调用旧提交；确认管理/恢复仍消费 legacy 账、审计、快照和恢复材料。

## 3. 写入白名单

1. `海中鱼巣/核心/服务.L1事实基座.ixx`
2. `海中鱼巣/核心/仓库.L1事实基座.ixx`
3. `海中鱼巣/核心/合同.L1事实基座.ixx`
4. `施工记录/20260808_L1-LEGACY-BUSINESS-WRITE-SURFACE-RETIREMENT_施工记录.md`
5. `验证记录/20260808_L1-LEGACY-BUSINESS-WRITE-SURFACE-RETIREMENT_构建与旧入口退出验证.md`

核心数据头、P13/P14、状态动态原子发布、运行期、工程、索引、恢复/读取调用方、规范、设计、计划、计划索引和异主 WIP均禁止修改。

## 4. 机械施工

1. 按详细设计从服务删除 4 个 `提交写集` 重载和 1 个 `探测幂等`，不改变中性、读取或恢复方法。
2. 从仓库删除旧 `提交`、旧 `探测幂等` 及仅属旧提交的 `解析读回引用/代次匹配/执行读回计划/形成结果摘要/记录审计`。
3. 从核心合同删除旧探测枚举、请求、首次结果副本、结果和映射函数；保留其它合同。
4. 编译修正只允许处理上述删除造成的目标三模块内无用 import/include 或私有死声明；任何公共数据头、P13/P14、恢复结构或中性接口变更都停止并退回。
5. 记录实际删除符号、保留结构和验证；不新增替代壳、弃用壳、模板旁路或第二写入口。

## 5. 验证与完成边界

至少执行：

```powershell
git diff --check -- <五个白名单路径>
python .\tools\check_specs.py --strict
```

并完成 Debug/Release x64 完整重建和双配置模块扫描。静态门禁必须证明：旧核心入口/探测 DTO 零定义零引用；`提交中性写集` 唯一保留；中性读取、legacy 当前/历史/审计/快照和恢复入口仍存在；核心数据头、P13/P14、状态动态迁移结果和目标外文件零差异。

不建立 L1 专项自检、生产外驱动、覆盖补格或运行场景。当前计划只证明旧普通写面退出，不证明恢复运行、状态动态生产运行、P13/P14 DTO 迁移、全部 legacy 数据删除、消费者集合闭合或 L1 已完善。

只暂存五个白名单路径并普通提交、普通推送。结果进入 main 后交回交互侧核验；执行者不得自行修改计划索引。
