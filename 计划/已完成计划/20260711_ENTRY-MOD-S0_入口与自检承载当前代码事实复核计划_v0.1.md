# ENTRY-MOD-S0 入口与自检承载当前代码事实复核计划 v0.1

日期：2026-07-11

状态：已完成 / 已归档 / JY-256 / ENTRY-MOD-S0-A01 至 A10 通过 / 纯只读 / 未改 C++、工程、规范或流程图 / 未构建 / 未运行程序

## 1. 目标

在 #211 按新文件规则正式完成后，只读盘点当前代码文件、`入口.cpp` 责任分段和全部未执行代码计划，形成后续自检运行器、入口拆分和计划修订的正式依据。

本计划不修改代码，不移动文件，不重命名既有文件，不直接修订 #212-#235。

## 2. 依据与范围一致性

```text
规范/代码文件建立归属与模块命名规范.md
流程图/20260711_代码文件建立归属模块命名与入口自检承载流程图_v0.1.md
流程图/20260711_代码文件建立归属模块命名与入口自检承载流程图_v0.1.html
规范/详细设计/代码文件建立归属模块命名与入口自检承载详细设计.md
```

流程图、详细设计和本计划一致覆盖文件分类、真实模块、入口分段和活动计划冲突；不覆盖实际拆分代码。

## 3. 依赖门控

### 3.1 前置计划 / 队列编号

```text
#211 CONCEPT-NAMING-S1
```

#236 必须读取 #211 正式提交后的新生产模块、自检模块和入口过渡调用，不以当前未提交草案作为基线。执行优先级固定为 #211 完成后、#217 之前。

### 3.2 已验证当前事实

```text
正式 HEAD 7733d08 的入口.cpp 已超过 1.5 万行、约 1.04 MB；不同文本读取方式会不会计空行，正式记录必须同时写明统计命令，不能只留一个无口径行数。
现有线程目录已有真实模块，领域初始化 .ixx 仍有 ClInclude / include 历史形态。
当前活动计划普遍允许修改入口.cpp，但尚无正式逐计划冲突矩阵。
JY-253 已建立以后新文件和入口不恶化规则。
```

### 3.3 假定接口契约

```text
#211 将正式产生 上行桥.概念命名治理.ixx 与 自检.概念命名治理.ixx。
#211 入口只保留最小自检调用，不保留 A01-A12 正文。
海中鱼巣.vcxproj 和 filters 可读取全部工程登记类型。
当前计划根目录继续承载 #212-#235 未执行计划。
```

### 3.4 预期正式产物

```text
实施记录/20260711_ENTRY-MOD-S0_入口与自检承载当前代码事实复核_Codex断点清单.md
```

记录必须包含代码文件矩阵、入口分段矩阵、活动计划冲突矩阵和建议依赖顺序。

## 4. 实施步骤

### S0：正式基线复核

```powershell
git status --short --branch
git log -5 --oneline
rg -n "^(export )?module |^import |#include .*\.ixx" .\海中鱼巣
rg -n "<ClCompile Include=.*\.ixx|<ClInclude Include=.*\.ixx" .\海中鱼巣.vcxproj
```

确认 #211 已完成并且工作树没有被当作正式接口。

### S1：代码文件矩阵

枚举 `海中鱼巣/` 下 `.h`、`.cpp` 和 `.ixx`，逐项记录路径、职责、工程登记、调用方式、真实模块状态、现有兼容状态和后续迁移需求。

### S2：入口分段矩阵

按稳定符号和功能主题定位：

```text
参数与启动观察。
仓库 / 服务构造。
初始化。
各领域 Axx 自检。
并发压力。
人读输出。
SQL / 控制面板。
停止与运行分支。
```

记录跨段共享变量、服务依赖、可迁移边界和建议 `功能.分类` 模块名。

### S3：活动计划冲突矩阵

逐项读取 #212-#235：

```text
允许 / 禁止文件。
新文件路径。
入口修改用途。
自检正文位置。
是否符合 CODE-FILE-01。
需要修订、执行前复核或不适用。
```

不得只按 `入口.cpp` 是否出现在允许文件判断；必须区分最小调用和正文堆积。

### S4：后续依赖建议

输出：

```text
中央 自检.运行器 基础切片。
优先迁移的领域自检组。
初始化 / 运行接线迁移组。
必须在执行前修订的活动计划组。
可以保持原计划但受全局规则约束的计划组。
```

不得直接生成 C++ 实施许可；后续由设计窗口依据正式记录生成流程图、详细设计和分段计划。

### S5：治理收口

归档本计划，更新计划索引、任务队列、当前状态、决策 / 实施 / 待确认记录，提交并推送。

## 5. 允许文件

```text
实施记录/20260711_ENTRY-MOD-S0_入口与自检承载当前代码事实复核_Codex断点清单.md
计划/20260711_ENTRY-MOD-S0_入口与自检承载当前代码事实复核计划_v0.1.md
计划/已完成计划/20260711_ENTRY-MOD-S0_入口与自检承载当前代码事实复核计划_v0.1.md
计划/计划索引.md
项目记忆/Codex任务队列.md
项目记忆/当前状态.md
项目记忆/实施记录.md
项目记忆/决策记录.md
项目记忆/待确认问题.md
```

## 6. 禁止文件与排除项

```text
AGENTS.md
规范/**
流程图/**
海中鱼巣/**
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
tools/**
```

禁止移动或重命名文件，禁止修改入口，禁止运行格式化，禁止构建和运行程序，禁止把建议模块名写成已实现事实。

## 7. 非成功收口

| 场景 | 分类 | 收口 |
| --- | --- | --- |
| #211 未完成或正式产物与假定接口不同 | 逻辑内返回 | 不扫描未提交草案，退回设计修订 #236 假定接口 |
| 文件职责无法只读判断 | 逻辑内返回 | 标为待 S0 深读，不猜分类 |
| 活动计划范围与上游设计不一致 | 逻辑内返回 | 记录为必须修订，不改计划 |
| 同一路径在工程中出现互相矛盾的登记 / 模块身份 | 追根因解决 | 记录精确证据和影响链，不用统计数字掩盖 |

## 8. 验收

| 编号 | 验收项 |
| --- | --- |
| A01 | 正式 HEAD、工作树和 #211 产物边界分开记录 |
| A02 | 所有 `.h/.cpp/.ixx` 均进入代码文件矩阵 |
| A03 | 所有 `.ixx` 均区分真实模块、历史兼容或异常登记 |
| A04 | 入口主要段均有稳定锚点、依赖和建议归属 |
| A05 | 自检正文、输出、并发压力和生产接线分开登记 |
| A06 | #212-#235 全部进入活动计划冲突矩阵 |
| A07 | 每个活动计划给出需要修订 / 执行前复核 / 不适用结论 |
| A08 | 后续中央运行器和分批迁移顺序明确，但没有代码许可 |
| A09 | C++、工程、规范和流程图差异为 0 |
| A10 | `git diff --check` 与 `tools/check_specs.py` 通过 |

## 9. 完成声明边界

完成后只能声明入口、自检、模块和活动计划冲突的当前事实已盘点。不得声明入口已瘦身、活动计划已修订、统一自检运行器已实现、旧文件已模块化或构建 / 自检已通过。

登记目标：#236 / DQ-128；完成后由设计窗口生成实际拆分与批量计划修订。
