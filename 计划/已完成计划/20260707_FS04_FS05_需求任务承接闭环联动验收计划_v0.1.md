# FS-04 / FS-05 需求任务承接闭环联动验收计划 v0.1

日期：2026-07-07
状态：已完成 / 验收通过 / 未改代码 / 未迁移旧函数

## 1. 依据

```text
实施记录/20260707_FS04_需求服务目标状态与任务承接材料S1-S4代码实施_Codex断点清单.md
实施记录/20260707_FS05_任务服务承接材料与生命周期S1-S4代码实施_Codex断点清单.md
计划/已完成计划/20260707_FS04_需求服务目标状态与任务承接材料S1-S4代码实施切片_v0.1.md
计划/已完成计划/20260707_FS05_任务服务承接材料与生命周期S1-S4代码实施切片_v0.1.md
规范/详细设计/需求目标状态承载详细设计.md
规范/详细设计/任务系统详细设计.md
规范/验收/最小闭环验收用例.md
```

FS-04 和 FS-05 均已完成并验证，具备生成联动验收计划的依据。本计划只验证需求到任务承接材料的结构闭环，不等待 FS-06 方法服务或 FS-07 动作入口结果。

## 2. 确认范围

确认后允许执行一个验收型文档 / 运行复核切片：

```text
复核需求目标状态、需求主体、目标宿主和场景材料。
复核按需求创建任务时读取需求承接材料，而不是调用方隐式补齐事实。
复核任务承接材料读取、任务目标状态、任务虚拟存在和创建生命周期状态。
复核任务实际结果状态和完成前置拒绝。
复核拒绝路径结构数量不变。
形成 FS-04 / FS-05 联动验收矩阵和后续断点问题。
```

## 3. 允许文件

```text
实施记录/20260707_FS04_FS05_需求任务承接闭环联动验收_Codex断点清单.md
项目记忆/*
```

本计划确认后允许执行构建和默认入口运行复核，但不允许修改 C++。若验收发现必须改 C++，必须停止并登记待确认问题。

## 4. 禁止文件和禁止范围

```text
禁止修改海中鱼巣/**/*.h。
禁止修改海中鱼巣/**/*.cpp。
禁止修改工程文件。
禁止迁移旧函数体。
禁止新增旧需求或旧任务主信息字段。
禁止实现完整需求树、完整任务状态机、任务筹办、任务执行桥、方法候选或动作入口。
禁止接控制面板 / SQL / ADO / WebView / D455 / 体素 / 外设。
```

## 5. 验收步骤

```text
S0：读取 FS-04 和 FS-05 已完成实施记录，列出联动验收项。
S1：运行 Debug x64 构建。
S2：运行默认入口，复核 FS-04、FS-05、MC-03、MC-04、MC-05、MC-08、MC-09 输出。
S3：复核高级服务不得直接依赖特征值服务。
S4：运行 `git diff --check` 和 `tools/check_specs.py`。
S5：生成联动验收断点清单和后续待确认问题。
```

## 6. 验证命令

```powershell
msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m
.\x64\Debug\海中鱼巣.exe
rg -n -F "特征值服务" 海中鱼巣/领域/需求服务.h 海中鱼巣/领域/任务服务.h 海中鱼巣/领域/方法服务.h
git diff --check
if (Test-Path .\tools\check_specs.py) { python .\tools\check_specs.py }
```

## 7. 完成声明边界

完成后只能声明 FS-04 / FS-05 需求任务承接闭环联动验收通过或失败，并记录证据。不得声明完整需求树、完整任务系统、方法执行系统、自我循环或旧能力迁移完成。

## 8. 待确认问题

```text
FS04-FS05-VERIFY-Q1：是否确认本计划作为 FS-04 / FS-05 第一轮完成后的联动验收计划？
FS04-FS05-VERIFY-Q2：若默认入口已有覆盖，是否仍需要补独立验收断点清单作为后续设计依据？
```
