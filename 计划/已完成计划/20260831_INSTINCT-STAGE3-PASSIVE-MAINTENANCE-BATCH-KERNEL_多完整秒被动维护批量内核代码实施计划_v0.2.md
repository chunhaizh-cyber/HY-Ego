# INSTINCT-STAGE3-PASSIVE-MAINTENANCE-BATCH-KERNEL 多完整秒被动维护批量内核代码实施计划 v0.2

日期：2026-08-31

## 1. 身份、目标与完成条件

计划身份：`INSTINCT-STAGE3-PASSIVE-MAINTENANCE-BATCH-KERNEL`

版本：`v0.2`

目标：在现有本能单完整秒被动维护纯算法 owner 内增加多完整秒批量候选入口，使无外部 A/V 调整的最大被动区间在规范有限事实段覆盖下与逐秒调用现有单秒内核等价，且执行复杂度不依赖完整秒总数。合同预支 / 余款、准备补回等有限外部变化由后继 composer 在区间之间先形成候选，不属于本函数。

完成必须同时具备：

```text
批量 DTO / 状态 / 成功谓词
+ 规范事实段完整性验证
+ 服务衰减地板和与边界跳跃
+ 服务比例和 A 回归有界跳跃
+ 规范审计段
+ 单秒参考等价专项
+ 大跨度非逐秒证明
+ Debug / Release / ABI consumer
```

当前生产值域、L/H、定义版本和规则版本保持不变；本计划不实现运行期阈值自动变化。

## 2. 正式依据和当前基线

- `规范/6170_子规范_服务值时间维护生存安全回归与任务门控.md`：完整秒参考顺序、批量等价、地板和、分段、服务比例与安全回归；
- `规范/详细设计/20260831_INSTINCT-STAGE3-PASSIVE-MAINTENANCE-BATCH-KERNEL_多完整秒被动维护批量内核详细设计_v0.2.md`：本计划唯一实现合同；
- `海中鱼巣/领域/本能单完整秒被动维护.数据.h` 与 `算法.本能单完整秒被动维护.ixx`：现有单秒参考入口；
- 代码基线：计划创建时 `main@2cf7ce902f81938db624ee6f4903c1fb648afd0a`；
- 当前 `计划/计划索引.md` 执行队列为空。

## 3. 精确施工范围

允许修改：

```text
海中鱼巣/领域/本能单完整秒被动维护.数据.h
海中鱼巣/领域/算法.本能单完整秒被动维护.ixx
海中鱼巣/端到端测试.本能单完整秒被动维护.ixx
```

新增专属记录：

```text
施工记录/20260831_INSTINCT-STAGE3-PASSIVE-MAINTENANCE-BATCH-KERNEL_施工记录_v0.2.md
验证记录/20260831_INSTINCT-STAGE3-PASSIVE-MAINTENANCE-BATCH-KERNEL_验证记录_v0.2.md
```

禁止修改：

- 6170、详细设计、本计划和计划索引；
- L1、L2 特征 / 状态 / 动态、维护游标、服务合同、任务和安全 owner；
- 普通应用、启动、线程、工程项目 / filters；
- 当前固定 L/H、值域、定义版本和规则版本；
- 任意 SELF-GOVERNANCE WIP。

## 4. 目标接口

在现有数据头按详细设计第 4 节增加：

```text
本能批量维护需求来源形态_v1
本能被动维护有限事实段_v1
本能多完整秒被动维护请求_v1
本能被动维护批量审计段_v1
本能多完整秒被动维护状态_v1
本能多完整秒被动维护载荷_v1
本能多完整秒被动维护结果_v1
```

在现有算法模块新增：

```cpp
本能多完整秒被动维护结果_v1 计算本能多完整秒被动维护候选_v1(
    const 本能多完整秒被动维护请求_v1&) noexcept;
```

不得新增第二算法模块、第二规则常量 owner、时钟读取、事实读取或持久写入。

## 5. 实施步骤

1. 增加 DTO、固定算法边界、完整性函数、状态和成功谓词；所有非成功结果载荷为空。`事实分段键`固定为模块内非零 `std::uint64_t` 不透明关联键，不 import DATA、不取得持久身份语义。
2. 实现事实段规范化验证：完整覆盖、无重叠 / 空洞、形状、分段键唯一、相邻可合并拒绝；合同 v1 只接受主动安全规则版本 1 的无主动变化形状。
3. 实现服务累计衰减：无需求饱和减法、最长未满足地板和、30 天门禁、活动 1 保护；只用检查整数、饱和值和固定次数二分 / 欧几里得算法。
4. 实现服务比例边界跳跃，最多跨 99 个比例区间；禁止按完整秒数量循环。
5. 实现低位 / 高位安全递推的固定上界收敛；到达 A 的 0/1/L/H 后跳过剩余区间。
6. 形成并验证规范审计段，按详细设计规定的强制语义边界切段；预算不足零载荷。
7. 扩展现有专项：短区间逐秒参考等价、全部关键边界、非法矩阵和大跨度运行次数上界。
8. 形成专属施工 / 验证记录，不把纯算法结果升级为生产维护完成。

## 6. 失败与收口

- 入口、版本、分段、预算、材料、算术和内部错误按详细设计枚举返回；
- 不允许用 fallback 逐秒循环规避地板和或跳跃实现错误；
- 不允许把输入事实段当作正式历史事实或持久权威；
- 不允许在本计划内实现准备补回、合同终态、低位门禁 provider、游标推进或原子事务；低位门禁材料只有实际进入低位分支时才消费，其它分支必须向单秒参考派生`不适用`；
- 如当前代码证明详细设计字段无法无歧义实现，保持代码零扩面并具名退回计划支撑，不得自行改变机器语义。

## 7. 验证

执行侧先枚举根构建入口与实际工具集；正式门禁至少包括：

```powershell
msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /p:PlatformToolset=v143
msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Release /p:Platform=x64 /p:PlatformToolset=v143
```

并完成：

- Debug / Release `运行本能单完整秒被动维护端到端测试()`，覆盖详细设计第 7 节；
- Debug / Release 外部 ABI consumer 同时调用单秒与批量入口；
- 用有界短区间逐秒调用单秒入口作为 reference oracle，比较最终 A/V 和逐秒分支投影；
- 用接近 I64 边界的大跨度用例记录输入事实段数、比例边界数、二分次数和安全收敛次数，证明不随 N 线性增长；
- `git diff --check`、`git diff --cached --check`；
- `python .\tools\check_specs.py --strict`。

隔离验证根使用：

```text
D:\TEMP\海中鱼巣\INSTINCT-STAGE3-PASSIVE-MAINTENANCE-BATCH-KERNEL\<本轮唯一标识>
```

## 8. 完成声明边界

通过全部门禁后只可声明“多完整秒纯批量候选内核已实现并与单秒参考等价”。不得声明历史事实材料、四 owner 原子事务、生产接线、阶段三或 INSTINCT-ROUTE 完成。
