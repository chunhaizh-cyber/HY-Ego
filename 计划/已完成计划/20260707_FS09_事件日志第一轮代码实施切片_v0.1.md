# FS-09 事件日志第一轮代码实施切片 v0.1

日期：2026-07-09
状态：已确认计划 / 已登记可执行队列 #153 / 代码实施候选 / 未执行 / 未改 C++ / 未构建 / 未迁移旧函数

## 1. 依据

```text
规范/详细设计/事件日志持久化恢复详细设计.md
规范/详细设计/事件日志审计材料代码逻辑详细设计.md
规范/详细设计/日志系统详细设计.md
实施记录/20260707_FS09_事件日志持久化恢复增强S0当前代码事实扫描_Codex断点清单.md
实施记录/20260707_FS09_事件日志持久化恢复详细设计生成_Codex断点清单.md
实施记录/20260708_FLOW-17_事件日志审计材料代码实施_Codex断点清单.md
计划/已完成计划/20260707_FS09_事件日志详细设计确认后实施门禁计划_v0.1.md
```

本计划已确认并登记可执行队列 #153；执行前仍必须读取本计划、对应详细设计、对应流程图、允许文件、禁止文件和验证命令。

## 2. 当前事实

```text
当前已有 `海中鱼巣/核心/日志系统.h` 中的通用 `记录事件日志(...)` 文件入口。
当前 FLOW-17 已验证事件日志审计材料路径、空内容拒绝和事件日志只做人读审计材料。
当前仍未实现结构化事件材料、事件类别枚举、仓库 / 领域结构写入后事件材料组装、恢复入口或仓库回放。
```

## 3. 目标

执行本切片时，只补 FS-09 结构化事件日志第一轮验收入口：

```text
事件材料结构和事件类别枚举。
结构化事件材料入口拒绝：空入口、未知类别、完全空结构材料、未确认写入结果。
事件日志追加写入入口复用现有事件日志文件边界。
写入失败不补写、不删除、不回滚已确认业务结构。
事件日志只作为审计和后续恢复候选材料，不裁决当前运行期事实。
事件日志只读审计材料和恢复候选材料边界。
```

## 4. 允许文件

```text
海中鱼巣/核心/日志系统.h
海中鱼巣/入口.cpp
实施记录/20260707_FS09_事件日志第一轮代码实施_Codex断点清单.md
计划/计划索引.md
项目记忆/Codex任务队列.md
项目记忆/当前状态.md
项目记忆/决策记录.md
项目记忆/待确认问题.md
项目记忆/验收记录.md
```

## 5. 禁止文件和禁止能力

```text
海中鱼巣/核心/节点仓库.h
海中鱼巣/核心/主信息仓库.h
海中鱼巣/核心/关系仓库.h
海中鱼巣/核心/索引仓库.h
海中鱼巣/核心/仓库快照服务.h
海中鱼巣/领域/**
海中鱼巣.vcxproj
海中鱼巣.sln
旧项目源码
SQL / ADO / 控制面板 / WebView / D455 / 体素 / 外设文件
```

禁止实现：

```text
仓库快照恢复入口。
运行期仓库回放。
SQL / ADO 审计或恢复。
控制面板事件流水。
旧链表二进制保存恢复格式。
特征值 Vec 序列化或反序列化。
线程事件、外设日志或体素快照。
旧日志 / 旧持久化函数体迁移。
```

## 6. 实施步骤候选

```text
S0：复核当前 `记录事件日志(...)`、FLOW-17 默认入口样例和事件日志文件边界。
S1：在日志系统中补事件材料结构、事件类别枚举和中文业务命名的入口拒绝辅助。
S2：补结构化事件日志追加入口，复用事件日志文件，不反向写业务结构。
S3：在默认入口补 FS-09 第一轮验收：空入口拒绝、未知类别拒绝、完全空材料拒绝、成功写入和写入失败不改结构。
S4：补事件日志不裁决运行期事实和恢复候选边界验收。
S5：补排除项扫描、断点清单、计划索引和项目记忆同步。
```

## 7. 验证命令候选

```powershell
git diff --check
python .\tools\check_specs.py
msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m
.\x64\Debug\海中鱼巣.exe
rg -n "SQL|ADO|控制面板|WebView|D455|体素|外设|旧链表|std::thread|std::async|工作队列" 海中鱼巣/核心/日志系统.h 海中鱼巣/入口.cpp
rg -n "记录结构事件日志|事件材料|事件类别" 海中鱼巣/核心/日志系统.h 海中鱼巣/入口.cpp
```

## 8. 完成声明边界候选

执行完成并通过验证后，只能声明：

```text
FS-09 事件日志第一轮结构化事件材料和追加写入入口已实现并验证。
```

不得声明：

```text
事件日志持久化恢复完成。
仓库快照或恢复入口完成。
运行期仓库回放完成。
SQL / ADO / 控制面板 / WebView / D455 / 体素 / 外设接入完成。
旧日志、旧持久化或旧链表二进制保存恢复能力已迁移。
```
