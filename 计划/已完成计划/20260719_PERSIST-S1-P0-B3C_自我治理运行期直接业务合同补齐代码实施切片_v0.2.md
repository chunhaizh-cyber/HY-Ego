# PERSIST-S1-P0-B3C 自我治理运行期直接业务合同补齐代码实施切片 v0.3

日期：2026-07-19

执行队列：#310

设计队列：DQ-202

设计裁决：JY-391 / JY-425 / JY-440—JY-445 / JY-477—JY-491

计划阶段：沿用既有 Debug 910 自检承载；940 仍由后继 #309 登记

状态：已完成 / 已归档 / `dac36f7 -> 3adc881 -> main@df58858` / WB-P0B-02 独立集成验证通过 / 沿用阶段 910

## 0. JY-483 新基线裁决

`#314 / TASK-RESULT-STABILITY-S3` 已经独立集成并发布到 `main@f1fb54d90f03a6b86b2e29f95ab8670611604406`。版本漂移与并发占用现共用权威当前需求、来源任务、正式结算读回和唯一匹配器；两配置、任务级单轮和 Debug 严格 20/20 已通过。

旧 `WT-310-R1 / 0890464414a8ffd5dfc71dcfc80641d9e4f18181` 仍是不可集成 WIP。它以旧父提交 `1d95aa5` 为基线，专属记录还保留旧失败状态，且没有在 #314 正式归并后的主线重新验证。因此：

```text
禁止 cherry-pick、merge、rebase 或直接发布 0890464。
禁止复用 WT-310-R1、旧分支和旧实施记录作为本轮执行身份。
允许只读提取 1d95aa5..0890464 的三个代码文件候选差异。
必须从 E228 发布后的新 main 创建 WT-310-R2，在当前接口上重新核对、机械落地并全新验证。
```

当前设计 S0 已验证：三个候选代码文件的旧差异可对 `main@f1fb54d` 通过 `git apply --check`；11 个现有业务服务方法、14 / 11 variant、B0 自检和阶段 910 均仍存在。该检查只证明候选差异可机械重放，不代替执行窗口逐签名复核、构建或运行证据。

## 1. 目标与正式依据

本切片只补齐 #301 所需、当前 #298 未提供的 B3 强类型直接合同：在唯一 `运行期业务操作组合器` 上增加 7 写、4 读共 11 个直接委托；不迁移自我治理领域路由，不改变线程请求 / 结果 variant，不新增线程分派，不登记 940。

三件套范围一致：

```text
流程图/20260718_生产运行期消费者两路并行与自我治理串行汇合流程图_v0.5.md
流程图/20260718_生产运行期消费者两路并行与自我治理串行汇合流程图_v0.5.html
规范/详细设计/权威状态快照隔离恢复与运行期上下文一次发布详细设计.md 第 22 节
计划/20260719_PERSIST-S1-P0-B3C_自我治理运行期直接业务合同补齐代码实施切片_v0.2.md
```

流程图和详细设计的业务范围、服务所有权、并行文件互斥及 `#310 -> #305` 集成顺序均未改变；本版只修订执行基线、WIP 消费边界、真实回归组和新任务身份。来源性质为 MIG-18 重新实现 / 新增约束，不计旧能力迁移完成。

## 2. 已验证当前事实与固定合同

已验证事实：

```text
main@f1fb54d 已包含 #314 正式归并与独立集成记录，WT-MAIN clean。
运行期业务操作组合器当前有 13 类线程可路由业务入口，请求 / 结果 variant 为 14 / 11。
运行期业务装配唯一拥有需求、任务、方法、轻量因果服务。
服务.需求 / 任务 / 方法 / 轻量因果的 11 个目标方法仍存在。
旧 WIP 三代码文件候选差异可在当前 main 上通过 git apply --check。
当前 main 不含 P0-B3C A01—A12；它必须由本切片重新形成。
```

目标入口固定为：

| 外层入口 | 唯一现有委托 | 返回类型 |
| --- | --- | --- |
| 创建完整目标状态需求 | `需求业务服务::创建完整目标状态需求` | `需求提交结果` |
| 创建已有需求承接任务 | `任务业务服务::创建承接任务` | `任务提交结果` |
| 迁移任务生命周期 | `任务业务服务::迁移生命周期` | `任务提交结果` |
| 创建方法首 | `方法业务服务::创建方法首` | `方法提交结果` |
| 创建方法结果 | `方法业务服务::创建结果` | `方法提交结果` |
| 创建方法动作入口 | `方法业务服务::创建动作入口` | `方法提交结果` |
| 创建轻量因果 | `轻量因果业务服务::创建轻量因果` | `轻量因果业务结果` |
| 读取需求 | `需求业务服务::读取需求` | `需求权威材料` |
| 读取任务 | `任务业务服务::读取任务` | `任务权威材料` |
| 读取方法登记项 | `方法业务服务::读取登记项` | `方法登记项材料` |
| 读取轻量因果 | `轻量因果业务服务::读取轻量因果` | `轻量因果值式材料` |

全部复用现有请求 / 结果 DTO；7 个写入口只做值式入口拒绝和唯一委托，4 个读入口只返回值式副本。不得接触仓库、接线、令牌、许可、执行器或会话。

## 3. 新任务身份与允许文件

预留身份：

```text
任务工作树：C:\Users\zhchh\.codex\worktrees\310-p0b3c-r2\海中鱼巢
任务分支：codex/310-p0b3c-r2
冻结基线：E228 `e1ec3193534e36b399e8d6a86816ee594412a1c5`
旧证据：0890464，只读，不作为父提交或提交输入
```

允许文件精确为：

```text
海中鱼巣/领域/组合.运行期业务操作.ixx
海中鱼巣/装配.运行期业务.ixx
海中鱼巣/自检.运行期兼容请求.ixx
实施记录/20260719_PERSIST-S1-P0-B3C_自我治理运行期直接业务合同补齐新基线重验_Codex断点清单.md 新建
```

既有自检只允许保留 P0-B0 A01—A20 原语义并追加 P0-B3C A01—A12；入口、工程和阶段 910 登记零修改。

## 4. 禁止范围

禁止修改 `运行期业务请求` / `运行期业务结果` variant、`路由.运行期业务请求.ixx`、运行期上下文 / 生产启动、仓库、数据操作、领域业务服务实现、B1 桥、B3 路由、入口、工程、统一运行器、SQL / 面板、快照恢复和中央治理。

禁止复制旧实施记录，禁止把旧 WIP 状态改名为成功，禁止 cherry-pick / merge / rebase `0890464`，禁止新增 DTO、公开原始事务能力、复制业务服务算法、增加重试或为 #301 保留传统服务兜底。

## 5. 机械阶段状态机

| 阶段 | 已验证前置事实 | 待实施目标 | 实施后证据 | 退回条件 |
| --- | --- | --- | --- | --- |
| S0 | E228 新 worktree 身份正确；旧候选补丁检查通过 | 逐签名复核 11 个请求 / 结果、四服务装配、14 / 11 variant、910 登记和 #314 当前代码 | 当前接口与第 2 节逐项一致；old WIP 只读 | 任一方法、DTO、构造顺序或自检所有权漂移；需修改禁止文件 |
| S1 | S0 一致 | 只向三个代码文件机械落地候选语义 | 组合器只新增四服务引用和 11 委托；装配唯一传入；B3C A01—A12 形成 | 候选补丁不能干净落地，或落地后必须修改业务算法 / variant / 路由 |
| S2 | S1 完成 | 两配置全量 Rebuild | Debug / Release 均 exit0、0 warning / 0 error | 任一构建失败或需计划外文件 |
| S3 | S2 通过 | 两配置全新单轮完整自检 | Debug 五组全部通过；Release 五组专项输出为 0 | 非零退出、行数 / 叶子不符、日志截断或目录竞争 |
| S4 | S3 通过且取得共享时隙 | 从第1轮全新 Debug 严格20/20 | 每轮完整日志先落盘、五组通过、进程归零、轮间2秒 | 首个异常立即停止释放；不得补跑或续算旧轮 |
| S5 | S4 全部通过 | strict、diff、范围和任务分支收口 | 精确四文件，任务分支完成待集成 | 超范围、ABI / 阶段漂移、仍有失败证据 |

S1 可以把 `1d95aa5..0890464` 的三个代码文件差异作为机械候选补丁，但应用前必须通过 `git apply --check`，应用后必须保持 index 为空并逐文件审查；候选补丁不是正式接口依据，当前代码与本计划才是依据。

## 6. 非成功返回

空句柄、零幂等键、非法生命周期或不完整请求为逻辑内返回，零写。正式业务服务返回许可拒绝、版本漂移或入口拒绝时保持原强类型状态。完整请求已被业务服务接受后，返回材料与请求身份、阶段、方法规格、动态或因果互证矛盾为追根因。必须修改禁止文件才可形成合同属于接口漂移。

计划外 TASK-RESULT-S1 失败不再沿用旧诊断推测：本基线必须以 #314 后的 SERVICE-DATA-S5 / TASK-RESULT-S1 直接叶子和完整日志判定。首异常保存完整输出、停止并退回设计，不得丢失叶子后继续运行。

## 7. 验收矩阵

| 编号 | 验收项 |
| --- | --- |
| P0-B3C-A01 | 11 个直接入口请求 / 结果签名与现有服务逐项一致 |
| P0-B3C-A02 | 顶层组合器只新增需求 / 任务 / 方法 / 轻量因果四服务引用 |
| P0-B3C-A03 | 7 个写入口只做准入和唯一委托，无算法复制、双写或兜底 |
| P0-B3C-A04 | 4 个读入口只返回值式材料，不泄漏引用或内部对象 |
| P0-B3C-A05 | 已有需求可承接任务并迁移 `已创建 -> 已承接 -> 筹办中` |
| P0-B3C-A06 | 方法首、结果、唯一动作入口可在系统方法登记根下幂等建立 |
| P0-B3C-A07 | 既有召回与选择路径可消费上述方法材料 |
| P0-B3C-A08 | 既有冻结、排队和执行合同形成实际状态与动态证据 |
| P0-B3C-A09 | 轻量因果只由正式动态创建并可值式读回 |
| P0-B3C-A10 | variant 仍为14 / 11，13类线程路由类型、顺序、索引和语义不变 |
| P0-B3C-A11 | P0-B0 A01—A20、B1桥和B3路由生产文件零差异 |
| P0-B3C-A12 | Debug / Release Rebuild、两配置单轮、Debug严格20/20、strict、diff、四文件范围和Release隔离通过 |

Debug 每个有效轮必须精确核对：

```text
SERVICE-DATA-S5：38 行，失败 0，A33 / A34 通过
TASK-RESULT-S1：18 行，失败 0，A14 / A18 通过
PERSIST-S1-P0-A：18 行，失败 0
PERSIST-S1-P0-B0：20 行，失败 0
PERSIST-S1-P0-B3C：12 行，失败 0
```

Release 上述五组专项输出均为 0。连续 20 轮必须使用逐轮完整日志脚本，不能使用丢弃 stdout 的一行循环。

四条指定叶子的当前正式输出分隔符为冒号，判定器必须分别精确匹配：

```text
SERVICE-DATA-S5-A33 ...: 通过
SERVICE-DATA-S5-A34 ...: 通过
TASK-RESULT-S1-A14 ...: 通过
TASK-RESULT-S1-A18 ...: 通过
```

不得使用 `=通过` 冒充上述四条叶子的格式。修订后的判定器在启动任何新产品进程前，必须先对 E231 已落盘日志做只读预检，证明四条叶子各唯一命中、五组行数为 38 / 18 / 18 / 20 / 12、失败 0 且进程 0；预检通过后才可从全新 Debug 单轮重启完整 S3—S4，旧轮不计入新验收。

## 8. 执行前复核与验证命令

```powershell
git status --short --branch
git rev-parse HEAD
git diff --binary 1d95aa5975057965fcfa179c56521ad69e5b5b6a..0890464414a8ffd5dfc71dcfc80641d9e4f18181 -- `
  '海中鱼巣/领域/组合.运行期业务操作.ixx' `
  '海中鱼巣/装配.运行期业务.ixx' `
  '海中鱼巣/自检.运行期兼容请求.ixx' | git apply --check --verbose
msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m
.\x64\Debug\海中鱼巣.exe --self-test-exit
msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Release /p:Platform=x64 /m
.\x64\Release\海中鱼巣.exe --self-test-exit
py -3.14 .\tools\check_specs.py --strict
git diff --check
git diff --name-only
rg -n "variant_size_v<运行期业务请求> == 14|variant_size_v<运行期业务结果> == 11" .\海中鱼巣
```

完整 / 连续自检只能在设计窗口明确授予 `D:\海中鱼巣\日志\事件段\自检` 独占时隙后运行。执行窗口必须先形成代码 / 构建 / 静态固定点，再申请共享时隙。

## 9. 完成边界与后继

只有新任务分支完成、独立集成按 `#310 -> #305` 顺序发布且设计归档后，才证明 B3C 直接强类型合同进入正式运行期业务面。当前计划重启不等于代码完成，不自动把旧 `0890464` 变成可集成提交，不解除 #305 / #301 / #306 门控，不证明自我治理路由迁移、940 登记、默认入口切换、快照恢复或自我循环完成。
