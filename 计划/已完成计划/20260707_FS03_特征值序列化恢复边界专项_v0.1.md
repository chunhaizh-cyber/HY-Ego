# FS-03 特征值序列化恢复边界专项 v0.1

日期：2026-07-07
状态：已完成 / 文档治理 / 已归档计划 / 未改代码 / 未迁移旧函数

## 1. 依据

```text
规范/详细设计/事件日志持久化恢复详细设计.md
规范/详细设计/仓库快照格式与恢复拒绝矩阵详细设计.md
规范/详细设计/特征值Vec原始值容器详细设计.md
规范/详细设计/特征值非权威缓存与内容哈希详细设计.md
实施记录/20260707_FS03_特征值系统第二轮S0当前代码事实扫描_Codex断点清单.md
实施记录/20260706_FS03_特征特征值逻辑迁移包归并表.md
```

FS-09 已生成事件日志持久化恢复详细设计和仓库快照格式与恢复拒绝矩阵详细设计，但当前仍未实现恢复入口。FS-03 特征值序列化恢复必须在恢复入口前置拒绝矩阵之下设计，不能绕过快照版本、枚举版本、句柄版本和关系端点复核。

## 2. 确认范围

确认后允许执行文档治理：

```text
定义特征值 I64 / Vec 原始值的序列化材料边界。
定义特征值序列化材料进入仓库快照或事件日志的限制。
定义反序列化前置拒绝矩阵。
定义缓存字段不恢复为业务事实。
定义后续 C++ 实施切片候选，但不授予代码实施许可。
```

## 3. 禁止范围

```text
禁止修改 C++。
禁止实现序列化、反序列化或恢复入口。
禁止复用旧特征值主信息序列化格式作为新项目格式。
禁止把内容哈希、命中次数、最后命中或缓存字段恢复为业务事实。
禁止接 SQL / ADO / 控制面板 / WebView / D455 / 体素 / 外设。
禁止让需求、任务、方法直接依赖特征值服务。
```

## 4. 计划产物

确认后生成：

```text
规范/详细设计/过期设计/特征值序列化恢复边界详细设计.md
实施记录/20260707_FS03_特征值序列化恢复边界专项生成_Codex断点清单.md
```

## 5. 文档要求

详细设计至少包含：

```text
特征值序列化格式版本。
I64 / VecI64 / VecU64 原始值材料字段。
特征值节点身份和特征归属复核。
内容哈希和命中统计的非权威恢复边界。
反序列化拒绝矩阵。
恢复失败结构不变化要求。
后续代码实施切片允许文件、禁止文件和验证剧本草案。
```

## 6. 验证方式

```powershell
git diff --check
if (Test-Path .\tools\check_specs.py) { python .\tools\check_specs.py }
```

## 7. 完成声明边界

本计划确认后只能声明特征值序列化恢复边界文档治理路线已确认。不得声明特征值序列化恢复代码已实现、仓库快照恢复已实现或旧格式已迁移。

## 8. 待确认问题

| 编号 | 问题 | 默认建议 |
| --- | --- | --- |
| FS03-SER-Q1 | 特征值序列化恢复是否先做详细设计，不直接写 C++。 | 是。 |
| FS03-SER-Q2 | 缓存字段是否允许随特征值原始值一起恢复为事实。 | 否，缓存字段只能作为非权威候选或丢弃重建。 |
