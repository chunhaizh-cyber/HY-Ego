# SYSTEM-WORLD-TREE-ROOT-PUBLISH-I1 系统具名场景树根补验实施切片 v0.6

日期：2026-08-13
状态：可执行（唯一运行状态以 `计划/计划索引.md` 为准）

## 1. 目标与替代关系

从正式 `32fad8089fcda7d31ada1ec6676b000bb247c034` 修正 v0.5 组 5B 坏形状的层级误归。v0.6 同身份替代 v0.5 的执行合同；v0.4 的 `.inl` 永久单源、公开 ABI、生产状态机、九条白名单、四层证据和七组矩阵，以及 v0.5 的可构造非成功枚举合同继续有效。

本版本要求执行者机械完成两类动态证据：

1. 根标记、根父关系和同对象错截止等坏样本先证明公开 L2 `成功()==false`，再证明 I1 阶段拒绝、短路与状态守恒；
2. 树身份、根场景 / 树归属生命周期、二登—整树截止和旧发布独立截止等反例保持参与对象各自 L2 成功，再由 I1 跨结果互证拒绝。

不得为了命中 I1 冗余防御而绕过或修改 L2 成功谓词。不得改变机器语义、公开 ABI、provider-only、A1 同实例、启动、阶段 15 或 I2。

## 2. 正式依据与重新 S0

- `规范/详细设计/20260813_SYSTEM-WORLD-TREE-ROOT-PUBLISH-I1_系统具名场景树根补验坏形状归层详细设计_v0.6.md`；
- v0.4 / v0.5 详细设计与计划中未被 v0.6 替换的全部合同；
- 当前公开 `L2场景树归属事实完整()`、`L2场景树节点事实完整()`、`L2场景树事实完整()` 及三类结果 `成功()` 实现。

执行现场事实：执行者已停在 G5B-06 修改后且未重跑，未进入组 6，未更新记录、暂存或提交；生产与专项九路径 WIP 原样保留。此前回传的 `32/32` 局部动态 PASS 可以从新正式版本复核，不得直接升级为完成证据。

执行者必须从 v0.6 正式索引、精确 plan / design blob 与最新 HEAD 重新 S0，核对九路径 WIP、L2 谓词、G5B-12 当前生产实现和构建 / 验证资源。消息和旧局部数字不代替正式事实。

## 3. 白名单与禁止范围

白名单仍精确沿用 v0.4 九条：

1. `海中鱼巣/业务/初始化.系统世界树根.inl`；
2. `海中鱼巣/装配.普通应用.ixx`；
3. `海中鱼巣.vcxproj`；
4. `海中鱼巣.vcxproj.filters`；
5. `验证工具/系统世界树根建立与正式读回发布参数验证.cpp`；
6. `验证工具/系统世界树根建立与正式读回发布参数验证.vcxproj`；
7. `验证工具/运行系统世界树根建立与正式读回发布参数验证.ps1`；
8. `施工记录/20260812_SYSTEM-WORLD-TREE-ROOT-PUBLISH-I1_施工记录.md`；
9. `验证记录/20260812_SYSTEM-WORLD-TREE-ROOT-PUBLISH-I1_验证记录.md`。

DTO、L2 / R1 / A1、六服务、启动、程序结果、8120、I2、自我、概念、流程图、路线输入、其它计划 / 设计及异主 WIP禁止修改。不得新增公开 `operator==`、字段、setter、验证入口或生产测试分支。

## 4. 机械实施

1. 保留现有九路径 WIP，从 v0.6 详细设计第 4 节逐项重整组 5B 标题、构造、L2 先验和预期层级。
2. G5B-02—06、09—10 固定为 L2 成功 / 完整性先拒绝；先验必须为 false，I1 仍按详细设计冻结的状态、optional、短路和两状态槽结果拒绝。
3. G5B-01、07—08、11 固定为 I1 跨结果反例；参与的根写、登记和整树读取对象必须按用例各自 `成功()==true`。
4. G5B-11 必须把二登成功截止 N 与内部自洽的整树成功截止 N+1 同时传给 I1，不得构造后丢弃或误传原对象。
5. G5B-12 固定为 I1 已发布材料跨结果反例：预置旧发布独立截止错值，二登与整树读取均成功，I1 必须返回 `根读回不一致`、根写零调用、待收敛空且旧发布逐字段守恒。
6. 若 G5B-12 复跑证明当前实现用本轮新读回纠正坏旧值后成功发布，按原计划范围内代码缺陷处理：只在九路径内补齐私有逐字段截止互证，不改 ABI、状态枚举、机器语义或 L2。
7. 继续完成 v0.4—v0.5 未完成的组 6—7、双配置构建 / 专项、静态合同、四层证据和永久施工 / 验证记录。

I1 中根父关系等不可独立动态到达的防御代码可以保留；记录只能把它登记为静态源码防御冗余，不得写成已动态命中。

## 5. 验证命令与成功门禁

构建与专项命令继续沿用 v0.4：

```powershell
git diff --check -- <九条白名单>
python .\tools\check_specs.py --strict
msbuild .\海中鱼巣.slnx /t:Rebuild /p:Configuration=Debug /p:Platform=x64
msbuild .\海中鱼巣.slnx /t:Rebuild /p:Configuration=Release /p:Platform=x64
powershell -ExecutionPolicy Bypass -File .\验证工具\运行系统世界树根建立与正式读回发布参数验证.ps1 -配置 Debug
powershell -ExecutionPolicy Bypass -File .\验证工具\运行系统世界树根建立与正式读回发布参数验证.ps1 -配置 Release
```

成功必须同时满足：

- v0.6 十二项逐项输出并按详细设计归层，L2 先验与归层一致；
- G5B-12 动态 PASS，旧发布逐字段守恒；若修复生产代码，双配置根工程和完整专项必须从 fresh 输出全量重做；
- v0.4—v0.5 七组动态强制矩阵、四层证据、静态 / 工程合同、范围与生产零验证逻辑全部 PASS；
- 根工程和含完整真实模块依赖图的专项 Debug / Release 重建、链接、运行 PASS；
- 永久记录诚实区分 seam 动态、真实 R1、静态源码和构建工程，不宣称不可到达 I1 分支已动态命中；
- `git diff --check`、strict、UTF-8 / XML、九路径范围和 staged 精确检查 PASS。

真实分配器耗尽、正式启动、阶段 14—18、恢复 / 跨进程、长时生产、I2、真实自我、概念维度根和独立集成验收可 `NOT_RUN`；不得覆盖本计划或继承合同的动态强制项。

## 6. 发布、接受与清理

实现、专项扩展与永久记录形成一份结果提交并普通推送。交互侧按 v0.6 两层动态证据、一个静态冗余层及四层总证据重新独立核验；接受后由索引所有者登记退出，再以独立提交只删除三件专项并复跑根工程与 strict。永久 `.inl` 和计划内生产缺陷修复不得清理，正式生产文件不得保留任何验证专用代码。

I2 在 I1 重新接受、索引退出和专项独立清理全部正式完成前继续冻结。

## 7. 完成边界

v0.6 可执行只表示坏形状归层和 G5B-12 一致性门禁已可机械施工，不证明当前代码已修复、完整矩阵已通过或 I1 provider 已接受。最终结果最多证明 I1 provider 经 A1 同实例 R1 建立 / 收敛并正式读回发布；不证明启动、I2、自我、概念、恢复或完整系统闭环。
