# WAREHOUSE-OPT-S2 令牌计数批量读取证据门控优化计划 v0.1

日期：2026-07-13

队列编号：#259 / 设计队列：DQ-151

状态：JY-357 / 已完成证据裁决 / 正式不实施 / 810 不登记 / 未改 C++ / 计划归档

## 0. 当前裁决

#258 已正式不实施、不发布，阶段 800 不登记，因此不存在本计划要求的 S1 后正式指标。按第 2 节“证据不足即不实施”合同，#259 直接完成为正式不实施；不得用失败 WIP 或 S0 全表扫描指标替代 S1 后证据。

## 1. 输入入口

消费 #257 基线、#258 S1 后同夹具指标、结构事务实际接口和调用链剖分。预期正式产物必须包含每类查询总 P95、许可取得 / 验证占比、有效数量占比、重复单项读取次数和批量候选复制占比。

## 2. 证据门槛

连续三轮满足任一项才允许进入代码实施：许可路径占目标查询 P95 的 20% 以上；计数 / 多次单项读取占 20% 以上；同一许可内同桶重复读取至少 8 次且成本占 20% 以上。否则本计划只生成“不实施”裁决记录并完成，不修改 C++，阶段 810 不登记。

## 3. 条件核心处理

达到门槛后，只对被证据指明的一个热点形成最小改动：

```text
计数：优先维护同锁权威派生计数并在读取时复核，不用日志统计裁决。
批量读取：一次许可内复制一个或多个明确关系桶，逐关系复核后返回值式批次。
许可活动表：只优化查找数据结构或只读快照，不改变许可强度、活动身份和重入拒绝。
```

若三个方向中有多个同时达到门槛，必须拆新计划，不在本切片混改。

## 4. 结构承载与输出

机器事实仍由仓库和关系承载；批次、计数快照和性能指标只是值式读取 / 诊断材料。输出为证据裁决记录，达到门槛时另含最小代码、阶段 810 和回归记录。

## 5. 允许文件

默认只允许读取全部代码并写实施记录、计划索引、队列和当前状态。只有执行前证据矩阵明确选中一个热点后，才允许把该热点的精确生产文件及对应独立自检模块写入断点清单；在设计窗口修订本计划允许文件并提交后方可改代码。

## 6. 禁止文件与行为

在未完成允许文件二次冻结前，全部 C++ 和工程文件禁止修改。始终禁止修改 `#214`，禁止改变核心事务 ABI、共享 / 独占许可语义、同线程重入拒绝、令牌生命周期、普通删除独占和固定锁序；禁止把许可保存在消息、线程、队列或长期对象中。

## 7. 失败收口与验收

指标缺失、不可重复或未达门槛属于逻辑内“不实施”，结构不变。达到门槛后接口漂移、许可语义不闭合或优化结果与 S1 不一致则追根因并退回设计。

代码实施时至少要求 A01-A12、Debug x64 0/0、完整自检、连续 20 轮并发、目标热点 P95 再下降 20%、非目标查询和纯写 P95 稳定回归不超过 10%、严格规范、diff、提交推送。

## 8. 完成边界

本计划已按“证据不足，正式不实施”完成并归档，阶段 810 不登记，正式 C++ 和工程零修改。不证明事务协调整体完成、令牌热点得到优化或分片必要。
