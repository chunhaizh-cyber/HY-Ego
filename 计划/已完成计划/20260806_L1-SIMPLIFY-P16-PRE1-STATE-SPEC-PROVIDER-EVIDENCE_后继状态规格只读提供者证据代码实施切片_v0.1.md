# L1-SIMPLIFY-P16-PRE1-STATE-SPEC-PROVIDER-EVIDENCE 后继状态规格只读提供者证据代码实施切片 v0.1

计划身份：`L1-SIMPLIFY-P16-PRE1-STATE-SPEC-PROVIDER-EVIDENCE`

版本：`v0.1`

创建侧代码基线：`8ee42f97861de342b9c09da778cb59763c3a78ab`

计划状态只由 `计划/计划索引.md` 维护；本文件发布前不得施工。

## 1. 目标与完成条件

在 P13 唯一后继状态规格所有者原位增加真实只读提供者证据合同。P13 成功结果必须同时返回固定提供者身份、直接调用次数1、去重后提供者见证数量1、最终成功轮次共同截止、五类强类型来源、排序去重写前事实身份组、逐项完整 `L1事实副本` 及精确数量；非成功保持全空载荷。

本叶只闭合 P13。P14 属于独立动态所有者，下一叶具名 `L1-SIMPLIFY-P16-PRE2-DYNAMIC-SPEC-PROVIDER-EVIDENCE`；不得在本次预分配其身份或接口。

完成不包括 P15统一规范化/摘要/通用读回重建、P16发布、P14证据、生产装配、因果或崩溃恢复。

## 2. 正式依据与当前事实

唯一实施依据为同名 v0.1 详细设计和函数流程图，以及 0050、4015、4190、4200、4210、P13 v0.2正式代码结果和当前 P15 执行证据 DTO。

当前 P13 已实现 G0→后当前→P12→P11→G1→状态规格形成，成功只返回规格与迁移证据；当前无 P13 provider 身份或证据凭证。P16 v0.2 在索引中保持待激活，具名门禁禁止默认 L1 身份、首个快照事实或业务编码冒充 P13/P14 provider。

## 3. 精确白名单

只允许修改：

```text
海中鱼巣/领域/L1状态.数据.h
海中鱼巣/领域/服务.L1状态.ixx
海中鱼巣/领域/组合.后继状态规格.ixx
海中鱼巣/领域/自检.L1状态.ixx
施工记录/20260806_L1-SIMPLIFY-P16-PRE1-STATE-SPEC-PROVIDER-EVIDENCE_施工记录.md
验证记录/20260806_L1-SIMPLIFY-P16-PRE1-STATE-SPEC-PROVIDER-EVIDENCE_验证记录.md
```

禁止修改 P14/P15/P16 代码或三文档、计划索引、工程、filters、启动、P0-FIX、路线、旧 WORLD-TREE、`.codex-build` 和其它 WIP。不得新增 exe、自检目标、装配、缓存、仓库或日志通道。

P12 的公开 DTO、结果和实现零修改。历史副本只由 P13 使用自身已有 `L1事实基座服务&` 读取，现有 P12 强类型前状态见证只负责限定允许回退的身份与生命周期。

## 4. 目标合同

1. 在组合模块定义 `后继状态规格提供者身份凭证`，固定身份 `0x4C31535000000001ULL`、合同1、规则1；公开 `读取后继状态规格提供者身份() noexcept`。
2. 扩展状态规格形成结果，使 `已形成` 同锁返回规格与 `L1状态结构登记` 证据，非成功两项空。
3. 定义 `后继状态规格写前事实种类/副本来源/身份` 与 `后继状态规格提供者证据凭证`，包含详细设计冻结的五类来源、共同截止、调用/数量字段、写前身份组和同索引完整事实副本组。
4. 扩展 `实际存在I64后继状态规格结果`，成功同时返回状态规格、迁移证据、提供者证据；非成功三项空。
5. P13内部传递provider不冒充P16直接provider；P16侧唯一直接见证固定为P13一项、调用次数1。
6. P13不计算L1事实摘要、执行证据摘要或通用读回计划。

## 5. 机械实施顺序

1. 在 `L1状态.数据.h` 为规格形成结果追加可选状态登记证据。
2. 在 `服务.L1状态.ixx` 的同锁成功分支返回本次采用的不可变登记副本；所有失败分支保持规格/登记双空。
3. 在组合模块增加固定身份、只读身份函数及证据 DTO。
4. 保留现有最多两轮完整漂移重试；只使用最终成功轮次材料。
5. 按详细设计 §8 从登记、后当前、前状态、选择和比较五类来源抽取事实身份，按 `(种类,身份)` 排序去重，校验覆盖最终 `0x53` 写集全部非本地引用。
6. 为每项身份绑定同索引完整事实副本：优先最终G1当前容器；只有P12强类型前状态明确已退出的身份允许恰一次历史读取，禁止从见证字段重建事实。
7. 历史读取后取末次G2；成功互证G1/G2共同截止、调用次数1、provider数量1、身份组/副本组/数量等长且非零；任一矛盾返回内部不一致全空载荷。
8. 在既有 `自检.L1状态.ixx` 追加专项；不改启动，继续由同一生产程序唯一顺序360的 L1 状态自检入口执行，不分配顺序582或任何新顺序。
9. 形成本计划专属施工与验证记录。

## 6. 验证矩阵

- 静态：固定身份全仓唯一；签名、DTO字段、依赖方向、六路径白名单成立；P12/P14/P15/P16及索引零修改；既有顺序360登记恰一且不新增顺序。
- 成功：等价、增加、减少均返回身份 `(0x4C31535000000001,1,1)`、直接调用1、provider数量1、同一非零截止、五类来源完整、身份/副本组严格排序一一对应且数量精确；覆盖G1当前副本和P12已退出历史副本。
- 非成功：无前态、入口拒绝、未找到、冲突、不可比较、许可拒绝、资源失败均为全空成功载荷边界。
- 漂移：首轮漂移丢弃全部证据并完整重试；第二轮漂移返回空载荷。
- 坏事实：错身份、错版本、缺登记、缺来源、缺副本、错种类、当前/历史双命中、P12未列出却历史回退、乱序、重复、数量不等、写集非本地引用未覆盖均内部不一致。
- 资源/并发：分配失败资源失败；并发写只导致完整重试或漂移，无部分凭证。
- 回归：P9/P11/P12/P13/P14/P15既有专项不退化；P13生产函数L1写/P14/P15/P16调用为0。
- 工程：Debug/Release x64 串行 Rebuild；同一生产程序既有受控自检两配置退出0；strict110/110；双配置模块扫描失败/循环/外部需求均0；目标与cached diff check通过。
- 日志：只核对同一生产程序正式日志中的专项身份、计数、状态和拒绝分支，日志不作为身份、事实、发布或完成证明。

## 7. 停工条件

出现任一项具名退回：

- `0x4C31535000000001` 已有正式等价占用；
- 必须从业务事实、局部键、日志、对象地址或默认 L1 身份生成 P13 provider 身份；
- 状态规格形成函数无法在同锁成功结果中回显登记证据；
- 五类来源不能唯一覆盖最终规格所依赖的写前事实身份，或任一身份无法取得同截止完整事实副本；
- 必须由 P13 计算 P15 摘要/通用读回，或修改 P14/P15/P16；
- 需要白名单外代码、第二目标、装配、持久/恢复或因果语义。

普通编译、模块导入和夹具问题在白名单内修复，不自动扩大合同。

## 8. 完成声明边界

只可声明：P13 后继状态规格成功结果具有真实、只读、版本化的提供者身份和同轮强类型证据凭证，可作为后继 P15 统一证据规范化的输入。

不得声明：P14 provider证据、P15后继接口、P16原子发布、正式接线、因果、崩溃恢复、STEP-3F或治理闭环完成。计划进入Git或待激活也不证明代码已实现。
