# FS-04 需求服务目标状态与任务承接材料 S1-S4 代码实施切片 v0.1

日期：2026-07-07
状态：已完成 / 代码已实现 / 验证已通过 / 未迁移旧函数

## 1. 依据

```text
计划/20260707_FS04_需求服务目标状态与任务承接材料增强专项_v0.1.md
实施记录/20260707_FS04_需求服务目标状态与任务承接材料S0当前代码事实扫描_Codex断点清单.md
规范/详细设计/需求目标状态承载详细设计.md
规范/详细设计/任务系统详细设计.md
规范/详细设计/需求结算记录详细设计.md
规范/领域服务授权写入规范.md
```

S0 已确认当前缺口：统一需求承接材料读取入口、需求场景材料、主体 / 目标宿主角色区分和非权威摘要分流。旧函数仍只作为证据锚点，不作为迁移单位。

## 2. 确认范围

本切片确认后，只允许执行 FS-04 S1-S4 第一轮代码实施：

```text
S1：补需求身份与目标状态读取复核。
S2：补需求主体 / 目标宿主 / 场景承接材料读取。
S3：补任务承接材料复核，缺材料不得创建有效任务承接壳。
S4：复核需求结算摘要分流边界，摘要不得成为权威事实。
```

## 3. 允许文件

```text
海中鱼巣/领域/需求服务.h
海中鱼巣/领域/任务服务.h
海中鱼巣/入口.cpp
项目记忆/*
实施记录/20260707_FS04_需求服务目标状态与任务承接材料S1-S4代码实施_Codex断点清单.md
```

如实施中发现必须修改状态服务、场景服务、存在服务、核心仓库或工程文件，必须停止并登记待确认问题。

## 4. 禁止文件和禁止范围

```text
禁止修改旧项目源码。
禁止修改核心仓库、工程文件、SQL / 控制面板 / WebView / D455 / 体素 / 外设文件。
禁止新增旧需求主信息字段。
禁止迁移旧函数体。
禁止实现父子需求角色、逻辑组织需求、获取途径枚举、方向签名、方法候选准入或需求树结构化更新指令。
禁止把需求结算摘要、显示文本、日志文本或方向 bool 作为机器事实。
```

## 5. 实施要求

```text
新增读取入口必须先复核需求节点类型。
需求目标必须仍读取为状态节点，不得降格为 I64。
需求主体、目标宿主和场景材料缺失时，承接材料整体失败。
任务服务按需求创建任务时必须经需求服务读取承接材料，不得由调用方隐式补齐机器事实。
拒绝路径必须证明节点、关系、状态和任务结构数量不变。
```

## 6. 验证方式

```powershell
msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m
.\x64\Debug\海中鱼巣.exe
git diff --check
rg -n -F "特征值服务" 海中鱼巣/领域/需求服务.h 海中鱼巣/领域/任务服务.h 海中鱼巣/领域/方法服务.h
```

默认入口至少输出或覆盖：

```text
需求承接材料读取通过。
缺需求目标状态拒绝。
缺主体 / 目标宿主 / 场景材料拒绝。
拒绝后结构数量不变。
需求结算权威记录仍可读，摘要不作为权威事实。
```

## 7. 完成声明边界

完成后只能声明 FS-04 第一轮需求承接材料读取和拒绝边界已实现并验证。不得声明完整需求树、任务筹办、方法候选、需求结算系统或旧需求能力迁移完成。

## 8. 待确认问题

```text
FS04-IMP-Q1：已确认。第一轮可复用现有 `引用` 关系，但必须由需求服务按“主体 / 目标宿主”角色过滤并做唯一性复核。
FS04-IMP-Q2：已确认。第一轮允许新增需求到场景的 `引用` 关系作为需求场景材料，不允许由调用方隐式补齐场景事实。
```
