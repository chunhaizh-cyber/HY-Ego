# FS-09 仓库快照格式与恢复拒绝矩阵专项 v0.1

日期：2026-07-07
状态：已完成 / 已归档计划 / 仓库快照格式与恢复拒绝矩阵详细设计已生成 / 未改代码 / 未迁移旧函数

## 1. 依据

```text
计划/20260707_FS09_事件日志持久化恢复增强专项_v0.1.md
实施记录/20260707_FS09_事件日志持久化恢复增强S0当前代码事实扫描_Codex断点清单.md
规范/节点类型与关系类型枚举规范.md
规范/错误结果与入口拒绝规范.md
规范/领域服务授权写入规范.md
规范/详细设计/节点仓库详细设计.md
规范/详细设计/主信息系统详细设计.md
规范/详细设计/关系仓库详细设计.md
规范/详细设计/索引仓库详细设计.md
```

S0 已确认：核心仓库有快照设计可参考的基础身份、读取和数量入口，但没有仓库快照服务、快照格式或恢复入口。因此本计划先锁定格式与恢复拒绝矩阵，不直接进入 C++ 实施。

## 2. 确认范围

确认后允许执行文档治理：

```text
生成仓库快照格式计划或详细设计。
锁定快照版本、枚举版本、仓库编号、本地编号、句柄版本和关系目标复核矩阵。
明确恢复入口前置拒绝项。
明确恢复失败不得留下半结构。
```

## 3. 允许文件

```text
计划/待确认计划/20260707_FS09_仓库快照格式与恢复拒绝矩阵代码实施前置计划_v0.1.md
规范/详细设计/仓库快照格式与恢复拒绝矩阵详细设计.md
实施记录/20260707_FS09_仓库快照格式与恢复拒绝矩阵计划生成_Codex断点清单.md
计划/计划索引.md
项目记忆/*
```

如果最终选择只生成计划或只生成详细设计，必须在断点清单中说明原因。

## 4. 禁止范围

```text
禁止修改 C++。
禁止迁移旧双向链表二进制保存恢复格式。
禁止接 SQL Server ADO、控制面板、WebView、D455、体素或外设。
禁止新增旧主信息字段。
禁止实现恢复入口。
禁止把字段恢复比对作为运行期事实源。
```

## 5. 文档要求

计划或详细设计至少包含：

```text
快照格式版本。
节点类型枚举版本。
关系类型枚举版本。
仓库编号、本地编号、句柄版本。
节点段、主信息段、关系段、索引段。
关系源 / 目标复核。
无效句柄、悬空关系、错误节点类型和越权恢复拒绝矩阵。
恢复失败结构不变化要求。
后续代码实施切片允许文件、禁止文件和验证剧本草案。
```

## 6. 验证方式

```powershell
git diff --check
if (Test-Path .\tools\check_specs.py) { python .\tools\check_specs.py }
```

## 7. 完成声明边界

完成后只能声明仓库快照格式和恢复拒绝矩阵计划 / 详细设计已生成并待确认。不得声明仓库快照服务、恢复入口、SQL 适配、旧格式兼容或 C++ 代码已实现。

## 8. 待确认问题

```text
FS09-SNAP-Q1：已确认。第一轮快照只覆盖节点仓库、主信息仓库、关系仓库和索引仓库，不覆盖特征值 Vec / 哈希 / 序列化。
FS09-SNAP-Q2：已确认。恢复入口必须在完整拒绝矩阵确认后另建 C++ 实施切片。
```
