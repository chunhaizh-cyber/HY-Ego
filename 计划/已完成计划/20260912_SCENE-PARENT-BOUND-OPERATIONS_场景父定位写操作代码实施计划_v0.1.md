# 场景父定位写操作代码实施计划

身份：SCENE-PARENT-BOUND-OPERATIONS；版本：v0.2；日期：2026-09-12。

绑定详细设计：`../规范/详细设计/20260912_SCENE-PARENT-BOUND-OPERATIONS_场景父定位写操作详细设计_v0.1.md`，v0.2 blob `6ae04f38105c2c9cf811915f6f0c026cdf5dbf38`。状态只由计划索引维护。

## 1. 目标

删除无父普通场景启用和沿普通存在组成链猜测上游场景的启用路径；保留独立根引导和显式父场景启用。普通非根场景退出升级为显式父定位，并在一个场景owner事务中共同退出直接父关系与角色内部结构。

## 2. 范围

生产白名单：`海中鱼巣/领域/合同.场景角色组织.ixx`、`海中鱼巣/领域/数据服务.场景类.ixx`。验证白名单：两个现有专项的最小迁移；新增本计划施工与验证记录。禁止修改L1、存在类、绑定存在、世界树、工程登记和其它合同。

## 3. 步骤

1. 重新核对HEAD、调用点、用途卡与正式规范。
2. 退出无父启用与沿组成链启用的公开声明、定义和DTO生产资格；把内部启用写集改为参数化private helper。
3. 给退出请求增加父场景并升级该请求版本；实现父关系与树归属证明定位、依赖检查、十事实共同退出、历史读回和幂等冲突。
4. 修订专项并完成详细设计第7节矩阵；形成知识变更清单。
5. 精确暂存、提交并普通推送；结果核验后从当前计划索引退出。

## 4. 验证与停工

验证使用`D:/TEMP/海中鱼巣/SCENE-PARENT-BOUND-OPERATIONS/<唯一轮次>`隔离根。至少运行两个专项Debug/Release、根工程Debug/Release、旧符号扫描、diff检查与strict规范检查。若发现生产调用者、必须修改L1/绑定存在/世界树公开合同、或无法以单场景owner事务共同退出父关系和角色结构，停止受影响切片并修订设计。

## 5. 完成边界

完成只证明当前代码公开面和验证范围内的场景父定位规则；不证明生产根初始化、历史迁移、跨进程恢复、主程序可达或正式集成验收。
