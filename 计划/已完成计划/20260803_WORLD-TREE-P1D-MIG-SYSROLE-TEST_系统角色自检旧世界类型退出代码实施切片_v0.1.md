# WORLD-TREE-P1D-MIG-SYSROLE-TEST 系统角色自检旧世界类型退出代码实施切片 v0.1

计划身份：WORLD-TREE-P1D-MIG-SYSROLE-TEST

版本：v0.1

创建侧当前代码基线：`b8404079aaed5b6012fbe459526f5491fb8921af`

直接代码依据：`规范/详细设计/20260803_WORLD-TREE-P1D-MIG-SYSROLE-TEST_系统角色自检旧世界类型退出详细设计_v0.1.md`。

## 1. 目标与完成条件

从系统角色初始化专项自检中删除旧世界初始化模块 import 和旧结果默认构造断言，使该消费者完全退出旧世界类型依赖，同时保持根函数签名、参数、22项运行时矩阵、顶层登记和返回结果不变。

完成只证明一个自检消费者退出。`海中鱼巣/领域/世界服务.h`、旧初始化服务、其它直接/间接消费者和生产装配全部保持原样；旧头文件只能在最终生产原子切换叶直接删除，不得在本计划删除或建立兼容转发壳。

## 2. 具名依赖与建议状态

本计划不依赖 P1C统一门面、P1A3真实世界结构、特征、状态动态或快照真实行为。当前目标模块的系统角色稳定身份自检已经直接消费运行期上下文和系统角色数据操作；旧世界类型只用于一条无运行时语义的默认构造断言。

建议登记状态：`可执行`，但只有本不可变计划和设计包正式提交、推送并由计划智能体登记精确 plan blob 后才成立。聊天、未跟踪WIP和本文件存在不构成执行许可。

S0若发现目标文件有活动所有者、旧类型出现第二用途、正式HEAD改变目标函数结构或本计划尚未登记，必须零修改停止。

## 3. 施工依据

```text
流程图/20260803_WORLD-TREE-P1D-MIG-SYSROLE-TEST_系统角色自检旧世界类型退出业务流程图_v0.1.md
流程图/20260803_运行系统角色初始化自检退出旧世界类型依赖_函数流程图_v0.1.md
规范/详细设计/20260803_WORLD-TREE-P1D-MIG-SYSROLE-TEST_系统角色自检旧世界类型退出详细设计_v0.1.md
规范/详细设计/函数结构知识图谱/20260803_WORLD-TREE-P1D-MIG-SYSROLE-TEST_系统角色自检旧世界类型退出函数结构知识图谱_v0.1.md
```

## 4. 精确允许文件

```text
海中鱼巣/自检.系统角色初始化.ixx
施工记录/20260803_WORLD-TREE-P1D-MIG-SYSROLE-TEST_施工记录.md
验证记录/20260803_WORLD-TREE-P1D-MIG-SYSROLE-TEST_验证记录.md
```

其它代码、工程、规范、设计、流程图、知识图谱、计划和计划索引全部禁止修改。目标模块、函数和自检已经登记，不修改 `海中鱼巣.vcxproj`、filters或`启动.应用程序.ixx`。

## 5. S0

1. 核对 HEAD/main/origin、计划版本/blob、索引状态和目标文件所有权。
2. 复算目标文件基线 blob；若不再是 `c08e2fbc22fdb8adcad524388eee32abe5753f83`，逐项重核本计划而非机械拒绝。
3. 确认目标文件中 `初始化.世界树` 仅一条 import，`世界树初始化结果` 仅一条默认构造断言；不存在运行时变量、参数、返回、成员、调用或分支用途。
4. 记录顶层自检数量、`INIT-MOD-S1` 身份、目标函数签名和22项数量。
5. 确认允许文件无其它所有者WIP，index和验证资源可用。

不满足时返回 `DRIFT-P1D-MIG-SYSROLE-TEST-OLD-WORLD-USE-OR-OWNERSHIP`，不得新增兼容壳、扩大到其它消费者或删除旧头文件。

## 6. 机械施工

1. 从 import 区精确删除 `import 海中鱼巣.领域.初始化.世界树;`。
2. 从 `运行系统角色初始化自检` 编译断言区精确删除 `static_assert(std::is_default_constructible_v<世界树初始化结果>);`。
3. 不重排、不改写其它 import、断言、函数体、验收数组、输出、返回和模块宏。
4. 形成专属施工/验证记录；记录基线、精确两处删除、验证结果和未覆盖范围。

## 7. 禁止范围

- 禁止删除或修改 `世界服务.h`、`初始化.世界树.ixx` 或其它旧消费者。
- 禁止新增旧类型别名、前置声明、包装结果、转换函数、兼容重载或转发壳。
- 禁止引入世界树统一门面或任何固定未实现生产函数；本叶没有需要冻结的新公开服务。
- 禁止改变 `历史兼容回归通过` 的使用、22项验收、`INIT-MOD-S1` 身份、顶层数量或诊断输出。
- 禁止修改规范、计划索引、工程、启动聚合和当前代码图谱。

## 8. 验证

```powershell
git diff --check -- 海中鱼巣/自检.系统角色初始化.ixx
python .\tools\check_specs.py --strict
msbuild .\海中鱼巣.slnx /t:Rebuild /m:1 /p:Configuration=Debug /p:Platform=x64
.\x64\Debug\海中鱼巣.exe --self-test-exit
msbuild .\海中鱼巣.slnx /t:Rebuild /m:1 /p:Configuration=Release /p:Platform=x64
.\x64\Release\海中鱼巣.exe --self-test-exit
powershell -ExecutionPolicy Bypass -File .\tools\clang_ast\扫描.MSVC模块依赖.ps1 -配置 Debug -平台 x64
rg -n "初始化\.世界树|世界树初始化结果|世界服务\.h|\b世界服务\b|创建基础信息|创建存在|创建场景|接纳存在" 海中鱼巣/自检.系统角色初始化.ixx
rg -n "运行系统角色初始化自检|INIT-MOD-S1|std::array<bool, 22>" 海中鱼巣/自检.系统角色初始化.ixx
```

成功条件：第一组旧词扫描退出码1且零输出；第二组精确命中根函数、身份和22项数组；模块依赖无目标旧边；Debug/Release构建与两配置完整自检成功；strict通过；scoped diff只含两处删除；顶层登记/执行数量不变。

## 9. 失败收口和完成声明

任何构建、自检、strict、模块依赖、零命中或精确diff失败时保留WIP，不提交结果、不扩大白名单、不修改设计或索引。

完成只允许声明“WORLD-TREE-P1D-MIG-SYSROLE-TEST 代码候选使系统角色初始化专项自检退出旧世界类型纯编译依赖并通过本计划矩阵”。不得声明生产世界服务迁移、旧头删除、统一门面接线、P1D切换或端到端服务验收完成。
