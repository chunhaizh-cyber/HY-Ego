# SELFTEST-MIGRATION-B1A 线程协议阶段自检迁移代码实施切片 v0.1

编号：SELFTEST-MIGRATION-B1A

队列编号：#242

设计队列：DQ-134

日期：2026-07-12

状态：已完成 / JY-263 / A01-A12 通过 / Debug x64 0 警告 0 错误 / 完整自检与连续 20 轮通过 / 待归档

## 1. 目标

把当前 `入口.cpp` 中线程协议阶段的 SELF-GOV-S1 / S2 / S3A 和 THREAD-S1-S6 迁入两个独立真模块，并通过 #244 阶段化总成在“线程协议”阶段按当前源码相对顺序执行。

本切片迁移 66 项：SELF-GOV 27 项、THREAD 39 项。SELF-GOV-S4A-D / S5 的 44 项和 S3B 的 9 项明确留给 #245，不再尝试由一个统一顺序 200 回调承载全部 80 项。

## 2. 正式依据与范围一致性

```text
流程图/20260712_中央自检运行器与第一批领域自检迁移流程图_v0.1.md
流程图/20260712_中央自检运行器与第一批领域自检迁移流程图_v0.1.html
规范/详细设计/中央自检运行器与第一批领域自检迁移详细设计.md
实施记录/20260712_中央自检运行器与第一批领域自检逻辑提取引用矩阵.md
实施记录/20260712_SELFTEST-MIGRATION-B1A_执行时序漂移与阶段化修订_Codex断点清单.md
实施记录/20260711_ENTRY-MOD-S0_入口与自检承载当前代码事实复核_Codex断点清单.md
规范/代码文件建立归属与模块命名规范.md
计划/20260712_SELFTEST-RUNNER-S2_阶段化自检总成代码实施切片_v0.1.md
```

本计划只覆盖流程图“线程协议阶段”及详细设计第 5.2、8、9.2、10.1、11.2、13、14.2 节。流程图、详细设计、矩阵和本计划均排除 S4/S5、S3B、概念迁移和结构事务，范围一致。

## 3. 能力台账

```text
能力编号：NEW-05。
来源性质：新增约束 / 全新工程治理。
现有行为来源：当前入口 SELF-GOV-S1/S2/S3A 与 THREAD-S1-S6。
新结构与写入方：两个自检模块只写测试控制材料；正式领域写入仍只经现有服务入口。
是否计入旧能力迁移完成度：不计入。
```

## 4. 依赖门控状态

| 前置 | 要求 | 当前状态 |
| --- | --- | --- |
| #241 SELFTEST-RUNNER-S1 | 单批次运行器正式接口 | 已完成 / `3baab3b` |
| #244 SELFTEST-RUNNER-S2 | 阶段化总成、阶段合同和最终聚合正式接口 | 未执行，直接门控 |

#244 未完成时，本计划保持依赖门控，执行窗口跳过，不标记真实阻塞。

## 5. 已验证当前事实

1. 当前正式代码基线为 `3baab3b`；#242 首次草案的两个模块、入口和工程差异已全部撤销。
2. SELF-GOV-S1 / S2 当前位于入口约 2337-2919 行，共 20 项。
3. THREAD-S1-S3 当前位于约 2920-3146 行及共享片段，共 20 项。
4. SELF-GOV-S3A 当前位于约 3148-3503 行，共 7 项，并依赖 THREAD-S3 强类型回执。
5. THREAD-S4-S6 当前位于约 3505-3806 行，共 19 项。
6. 当前真实相对顺序是 `S1/S2 -> THREAD-S1-S3 -> S3A -> THREAD-S4-S6`，不是“全部 THREAD -> 全部 SELF-GOV”。
7. S4A-D/S5 位于更早锚点，S3B 位于更晚锚点；二者不属于本切片。
8. #244 的正式阶段接口尚未形成，本计划只声明假定合同。

## 6. 假定接口契约

### 6.1 #244 预期接口

```text
模块：海中鱼巣.自检.运行器。
阶段：初始化闭环、线程协议、领域回执、最终回归。
登记：阶段 + 阶段内顺序 + 全局唯一编号 + 名称 + 回调。
执行：阶段单调、每阶段一次、普通失败不阻断后续阶段。
完成：聚合全部已执行阶段；不自动补跑已登记未执行单元。
```

### 6.2 本切片阶段合同

| 阶段内顺序 | 单元 | 项数 |
| ---: | --- | ---: |
| 100 | SELF-GOV-S1 / S2 | 20 |
| 200 | THREAD-S1-S3 | 20 |
| 300 | SELF-GOV-S3A | 7 |
| 400 | THREAD-S4-S6 | 19 |

`线程协议/400` 与 `最终回归/400` 属于不同阶段，不构成顺序冲突。

### 6.3 夹具合同

```text
自检.运行线程 唯一拥有 THREAD-S3 任务管理 / 工作线程夹具和强类型回执槽。
THREAD-S1-S3 回调写入一次并封存。
自检.自我治理协议 只取得常量读取能力，S3A 不复制线程控制逻辑。
协议阶段报告单写封存 S1 白名单、消息组与 S2 邮箱冻结结果，供最终概念命名只读消费。
```

具体类型名按 #244 实际接口和模块内部风格确定，但 66 项、四段顺序、唯一夹具所有权和报告边界不得改变。

## 7. 新文件分类

| 文件 | 分类 | `export module` | 工程登记 | 调用方式 |
| --- | --- | --- | --- | --- |
| `海中鱼巣/线程/自检.运行线程.ixx` | 非数据结构 / 线程自检真模块 | `海中鱼巣.线程.自检.运行线程` | `ClCompile` | 入口 `import`，向线程协议阶段登记前段 / 后段回调 |
| `海中鱼巣/线程/自检.自我治理协议.ixx` | 非数据结构 / 自我治理协议自检真模块 | `海中鱼巣.线程.自检.自我治理协议` | `ClCompile` | `import` 运行线程只读回执接口，入口登记 S1/S2 与 S3A 回调 |

两个新文件均须包含最小文件头规则。依赖方向固定为：`自我治理协议自检 -> 运行线程自检的只读回执类型`；禁止反向包含和生产依赖。

## 8. 允许文件

```text
海中鱼巣/线程/自检.运行线程.ixx
海中鱼巣/线程/自检.自我治理协议.ixx
海中鱼巣/入口.cpp
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
计划/20260712_SELFTEST-MIGRATION-B1A_线程与自我治理自检迁移代码实施切片_v0.1.md
实施记录/20260712_SELFTEST-MIGRATION-B1A_线程协议阶段自检迁移代码实施_Codex断点清单.md
实施记录/20260711_旧能力迁移与新内核建设逐能力台账.md
计划/计划索引.md
项目记忆/Codex任务队列.md
项目记忆/当前状态.md
项目记忆/决策记录.md
项目记忆/实施记录.md
```

`海中鱼巣/自检.运行器.ixx`、概念命名自检和全部生产模块只读消费，禁止顺手修改。

## 9. 禁止文件与能力

```text
全部核心仓库、领域服务、生产线程、自我治理消息协议、上行桥和概念命名治理生产 / 自检模块。
海中鱼巣/自检.运行器.ixx。
SELF-GOV-S4A-D、S5、S3B 的入口正文和输出。
SQL、控制面板、日志、快照、恢复、外设和体素实现。
把全部 THREAD 固定到 100、全部 SELF-GOV 固定到 200。
复制 THREAD-S3 夹具、使用全局可写槽或前置失败后重跑前置。
在入口新增或保留本批 66 项完整场景、并发循环和逐项输出正文。
```

## 10. 实施步骤

1. 确认 #244 已完成，读取实际阶段枚举、登记、运行、完成和结果接口。
2. 重新冻结当前 66 项编号集合与四段稳定锚点，确认首次草案残留为零。
3. 新建运行线程自检真模块，迁入 THREAD-S1-S3 前段和 THREAD-S4-S6 后段；模块唯一拥有 THREAD-S3 夹具。
4. 新建自我治理协议自检真模块，迁入 SELF-GOV-S1/S2 前段和 S3A 后段；S3A 只读消费线程模块封存回执。
5. 在入口初始化后建立阶段总成和最小报告槽；按 100 / 200 / 300 / 400 登记并在原逻辑区域运行“线程协议”阶段一次。
6. 从入口删除对应 66 项场景、局部辅助、并发循环和逐项输出；S4/S5、S3B 原位保持。
7. 最终锚点将 #241 S1 合同、#244 S2 合同和概念命名登记到“最终回归”阶段，运行一次并完成总成。
8. 概念命名上下文改为读取协议阶段报告字段，同时继续读取入口原位 S3B 和 S5 结果。
9. 比较迁移前后 66 项编号、结果、夹具所有权、控制面板和概念回归，执行完整自检及连续 20 轮。

## 11. 入口拒绝与失败收口

| 场景 | 分类 | 动作 |
| --- | --- | --- |
| #244 未完成或实际接口与假定不一致 | 逻辑内返回 | 不改代码，退回设计窗口 |
| 66 项编号、四段锚点或共享夹具边界漂移 | 逻辑内返回 | 更新事实和计划，不机械搬移行号 |
| 自检上下文缺少必需只读材料 | 逻辑内返回 | 不登记对应单元，不进入场景 |
| THREAD-S3 回执槽未完整封存 | 设计内自检合同失败 | S3A 仍执行一次，只返回前置材料不完整 |
| 单项按既有语义不通过 | 设计内结果 | 保留原编号，继续其它单元和后续阶段 |
| 66 项缺失、重复、结果改变或阶段错位 | 追根因解决 | 停止 #245，定位首次偏差 |
| 共有夹具被复制、线程未 join、回调不止一次 | 追根因解决 | 修正所有权和停止收口 |
| PANEL-TREES / CONCEPT-S6D 或概念命名回归变化 | 追根因解决 | 不以本批 66 项通过掩盖完整回归 |

## 12. 验收矩阵

| 编号 | 验收项 |
| --- | --- |
| SELFTEST-MIGRATION-B1A-A01 | 两个新文件名、模块名、`ClCompile`、`export module`、`import` 与依赖方向一致 |
| SELFTEST-MIGRATION-B1A-A02 | SELF-GOV-S1/S2 的 20 项完整、唯一且结果不变 |
| SELFTEST-MIGRATION-B1A-A03 | THREAD-S1-S3 的 20 项完整、唯一且结果不变 |
| SELFTEST-MIGRATION-B1A-A04 | SELF-GOV-S3A 的 7 项完整、唯一且只读消费 THREAD-S3 回执 |
| SELFTEST-MIGRATION-B1A-A05 | THREAD-S4-S6 的 19 项完整、唯一且结果不变 |
| SELFTEST-MIGRATION-B1A-A06 | 线程协议阶段内顺序固定 100 / 200 / 300 / 400，并保持当前源码相对时序 |
| SELFTEST-MIGRATION-B1A-A07 | THREAD-S3 夹具只有一个所有者；槽位单写封存、后继只读、非全局且不进入运行器通用 API |
| SELFTEST-MIGRATION-B1A-A08 | 入口不再承载 66 项正文；S4/S5 与 S3B 仍原位且未改变 |
| SELFTEST-MIGRATION-B1A-A09 | 生产线程、协议、上行桥、领域服务、运行器和概念命名模块无行为修改 |
| SELFTEST-MIGRATION-B1A-A10 | 66 项报告可供最终概念命名读取，PANEL-TREES 与 CONCEPT-S6D 无回归 |
| SELFTEST-MIGRATION-B1A-A11 | 完整自检返回 0，阶段总成计数与 66 项迁移结果一致 |
| SELFTEST-MIGRATION-B1A-A12 | Debug x64 0/0，最终代码连续 20 轮返回 0 |

## 13. 验证

```powershell
msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m
.\x64\Debug\海中鱼巣.exe --self-test-exit
git diff --check
py -3 .\tools\check_specs.py --strict
```

还必须静态比较 66 项迁移前后编号集合，确认入口仍保留 S4/S5、S3B，两个新文件为 `ClCompile`，不存在新 `.ixx` 的 `#include`，并扫描 PANEL-TREES-S1 与 CONCEPT-S6D-A11 逐项通过。

## 14. 执行前实际接口复核

```powershell
git status --short --branch
rg -n "自检阶段|阶段化自检总成|登记阶段自检|运行阶段|完成总成" .\海中鱼巣\自检.运行器.ixx .\海中鱼巣\入口.cpp
rg -n "SELF-GOV-S[12]-|SELF-GOV-S3A-|THREAD-S[1-6]-" .\海中鱼巣\入口.cpp
rg -n "SELF-GOV-S4|SELF-GOV-S5|SELF-GOV-S3B|PANEL-TREES-S1|CONCEPT-S6D-A11" .\海中鱼巣\入口.cpp
rg -n "自检\.运行线程|自检\.自我治理协议" .\海中鱼巣 .\海中鱼巣.vcxproj .\海中鱼巣.vcxproj.filters
```

若 #244 接口、四段锚点、编号集合、夹具所有权或工程模块方式任一不一致，不得修改代码，退回修订。

## 15. 预期正式产物与后继

```text
正式 自检.运行线程 真模块及工程登记。
正式 自检.自我治理协议 真模块及工程登记。
线程协议阶段四个单元和 66 项迁移对照。
入口阶段总成最小装配、线程协议执行与最终聚合。
#245 的实际模块、报告和阶段接口前置。
```

后继：#245 SELFTEST-MIGRATION-B1A2。

## 16. 完成边界

完成后只可声明线程协议阶段 66 项已迁入独立模块并经阶段化总成验证。不得声明 SELF-GOV 80 项全部迁移、入口存量自检全部迁出、自我治理业务闭环、生产线程能力扩大、旧能力迁移或结构事务实现。

## 17. 实施结果

2026-07-12 按 JY-263 完成。新增 `自检.运行线程` 与 `自检.自我治理协议` 两个真模块，按线程协议阶段 100 / 200 / 300 / 400 迁移 66 项；THREAD-S3 夹具由运行线程模块唯一拥有，自我治理 S3A 只读消费封存报告。入口删除对应场景、并发循环和逐项输出正文，仅保留上下文、报告、登记、阶段执行及尚待 #245 迁移的 S3B 共享观察辅助。Debug x64 0 警告 0 错误，完整自检退出 0，连续 20 轮全部退出 0。
