# FS-03 特征服务封口增强 S1-S4 代码实施切片 v0.1

日期：2026-07-07
状态：已确认 / 代码已实现 / Debug x64 构建通过 / 默认入口通过 / 未迁移旧函数

## 1. 切片来源

本切片承接：

```text
实施记录/20260706_FS03_特征特征值逻辑迁移包归并表.md
规范/详细设计/特征服务封口增强详细设计.md
计划/已完成计划/20260706_FS03_特征服务封口增强专项_v0.1.md
计划/已完成计划/20260707_FS03_特征服务封口增强S0当前代码事实扫描切片_v0.1.md
实施记录/20260707_FS03_特征服务封口增强S0当前代码事实扫描_Codex断点清单.md
```

S0 当前代码事实扫描已完成，且三个断点问题已确认：

```text
S1 到 S4 不合并，拆分实施。
特征值服务 public 入口第一轮保持现状，通过规则、依赖检查和验收禁止高级服务直接调用。
当前特征值唯一性治理第一轮只做归属复核和读取拒绝，不新增当前值唯一性约束。
```

本文件作为 S1 到 S4 的独立代码实施切片设计。用户确认本文件后，执行窗口已按本切片完成代码实施与验证。

## 2. 设计条款清单

必须实现：

```text
S1：特征身份与语义类型只读入口。
S2：实例特征槽位与宿主关系。
S3：I64 特征值写入读取封口。
S4：拒绝路径和越权依赖复核。
所有新增写入口必须先做入口前置拒绝，再写节点、主信息或关系。
拒绝路径必须证明节点、主信息、关系、索引和既有 I64 特征状态材料不变。
高级服务仍只能通过特征服务取得特征状态材料。
```

禁止实现：

```text
不迁移旧函数体。
不新增旧特征主信息、旧抽象特征主信息、旧特征值主信息字段。
不接 VecU / VecI64 原始值容器。
不实现内容哈希、命中统计、稳态、局部区间、抽象区间升格。
不实现特征值域匹配、复合比较或候选判断。
不实现特征值序列化、反序列化和恢复。
不接 D455、体素、外设、控制面板或 SQL 实时投影。
不允许需求、任务、方法直接依赖特征值服务。
不收窄特征值服务 public 入口作为本轮目标。
不新增当前值唯一性约束。
```

后续待补：

```text
FS-03-B 抽象特征和值域专项。
FS-03-E Vec 原始值容器专项。
FS-03-F 非权威缓存与哈希命中专项。
FS-03-G 值域匹配和复合比较专项。
FS-03-H 稳态和区间升格专项。
FS-03-I 事件日志持久化恢复专项。
FS-03-X 外设 / 体素专项。
特征值服务 public 入口治理专项。
当前特征值唯一性治理专项。
```

## 3. 项目结构映射表

| 业务概念 | 现有项目结构 | 值类型 / 身份结构 | 写入方 | 读取方 | 不新增字段的表达方式 | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- |
| 特征身份 | `节点类型::特征` | `节点句柄` | 特征服务既有创建入口 | 特征服务、高级服务经特征服务 | 节点句柄 + 节点类型复核 | 有效特征可读回；无效句柄和错误类型返回空 |
| 特征语义类型 | 特征节点、语素对应信息关系、索引候选复核 | 节点句柄 / 索引键 | 特征服务、语素服务授权入口 | 特征服务 | 只读复核关系 / 索引结果，不新增旧字段 | 索引或语素命中后类型不符不得返回特征事实 |
| 实例特征槽位 | 宿主节点到特征槽位节点的关系，特征槽位到特征类型的关系 | 关系句柄、特征节点句柄 | 特征服务 | 特征服务 | 用既有关系仓库表达宿主槽位，不使用旧子链字段 | 无效宿主、无效特征类型拒绝且结构不变 |
| 当前 I64 特征值 | 特征节点到特征值节点的 `归属` 关系 | 特征值节点句柄、I64 | 特征服务经内部特征值服务 | 特征服务 | 继续由特征到特征值归属关系准入，不新增当前值唯一关系 | 非归属值写入 / 读取拒绝，原值不变 |
| 服务封口 | 领域服务依赖关系 | 头文件包含和构造依赖 | 特征服务持有内部特征值服务依赖 | 需求 / 任务 / 方法经特征服务 | 通过依赖扫描和入口验收禁止越权 | `rg` 复核高级服务不包含、接收或调用特征值服务 |

## 4. 代码入口映射

| 切片 | 设计步骤 | 入口函数名 | 调用方 | 返回结构 | 失败分流 |
| --- | --- | --- | --- | --- | --- |
| S1 | 特征身份复核 | `特征服务::读取特征身份(节点句柄 特征节点) const` | 语素复核路径、高级服务经特征服务 | 特征节点句柄或空 | 无效句柄、已删除句柄、错误节点类型返回空，不写结构 |
| S1 | 特征类型判断 | `特征服务::节点是特征(节点句柄 特征节点) const` | 特征服务内部、入口验收 | 成功 / 失败 | 无效句柄或错误类型返回 false，不写结构 |
| S1 | 特征语义类型读取 | `特征服务::读取特征语义类型(节点句柄 特征节点) const` | 特征查找 / 初始化复核 | 语义材料或空 | 无效特征、关系目标不属于允许信息节点、索引命中后类型不符返回空 |
| S2 | 创建实例特征槽位 | `特征服务::创建实例特征槽位(节点句柄 宿主节点, 节点句柄 特征类型)` | 存在 / 任务 / 方法等经授权调用方 | 特征槽位节点句柄或无效句柄 | 无效宿主、宿主类型不允许、无效特征类型、关系写入失败时结构不变 |
| S2 | 读取宿主特征槽位 | `特征服务::读取宿主特征槽位(节点句柄 宿主节点, 节点句柄 特征类型) const` | 特征服务、入口验收 | 特征槽位节点句柄或空 | 无效宿主、无效特征类型、缺槽位关系返回空 |
| S2 | 读取宿主特征集合 | `特征服务::读取宿主特征集合(节点句柄 宿主节点) const` | 特征服务、后续高级服务 | 特征节点集合 | 无效宿主返回空集合；目标类型不符不得作为特征返回 |
| S3 | 宿主当前 I64 写入 | `特征服务::写入宿主I64特征状态材料(节点句柄 宿主节点, 节点句柄 特征类型, int64_t 值)` | 方法服务经特征服务、入口验收 | 成功 / 失败 | 宿主无槽位、特征值不归属、内部写入失败返回 false 且原值不变 |
| S3 | 宿主当前 I64 读取 | `特征服务::读取宿主I64特征状态材料(节点句柄 宿主节点, 节点句柄 特征类型) const` | 需求服务经特征服务、动态服务、入口验收 | I64 或空 | 宿主无槽位、无归属特征值、未写值、非归属值返回空 |
| S3 | 既有特征值归属复核 | `特征服务::特征值属于特征(节点句柄 特征节点, 节点句柄 特征值节点) const` | 特征服务内部 | 成功 / 失败 | 无效特征、无效特征值、缺归属关系返回 false |
| S4 | 越权依赖复核 | 无新增业务入口；使用依赖扫描和入口验收 | 执行窗口 | 依赖检查结果 | 发现高级服务直接依赖特征值服务时停止并回到设计确认，不以本切片静默修复 |

实施前必须重新扫描 `海中鱼巣/领域/特征服务.h`、`海中鱼巣/领域/特征值服务.h`、`海中鱼巣/核心/句柄.h` 和 `海中鱼巣/入口.cpp`。若当前代码已有等价中文入口，应优先扩展既有入口，不重复造同义函数。

## 5. 允许文件

本切片经用户确认后，S1 到 S4 第一轮代码实施只允许修改：

```text
海中鱼巣/领域/特征服务.h
海中鱼巣/领域/特征值服务.h
海中鱼巣/入口.cpp
项目记忆/当前状态.md
项目记忆/Codex任务队列.md
项目记忆/决策记录.md
项目记忆/实施记录.md
项目记忆/待确认问题.md
项目记忆/验收记录.md
实施记录/20260707_FS03_特征服务封口增强S1代码实施_Codex断点清单.md
实施记录/20260707_FS03_特征服务封口增强S2代码实施_Codex断点清单.md
实施记录/20260707_FS03_特征服务封口增强S3代码实施_Codex断点清单.md
实施记录/20260707_FS03_特征服务封口增强S4代码实施_Codex断点清单.md
```

`海中鱼巣/领域/特征值服务.h` 只允许在 S3 中作为内部 I64 材料读写和归属拒绝配合点使用，不允许把 public 入口收窄作为本轮完成目标。

## 6. 禁止文件

本切片禁止修改：

```text
旧项目目录或旧项目源码。
海中鱼巣/核心/节点仓库.*
海中鱼巣/核心/主信息仓库.*
海中鱼巣/核心/关系仓库.*
海中鱼巣/核心/索引仓库.*
海中鱼巣/核心/句柄.h
海中鱼巣/领域/需求服务.h
海中鱼巣/领域/任务服务.h
海中鱼巣/领域/方法服务.h
海中鱼巣/领域/动态服务.h
海中鱼巣.vcxproj
海中鱼巣.slnx
SQL / 控制面板 / WebView / 显示写入相关文件。
D455 / 体素 / 外设相关文件。
```

若 S4 依赖复核发现高级服务已经直接依赖 `特征值服务`，必须停止并回到设计确认路线，不得在本切片内临时修改高级服务文件绕过门禁。

## 7. 结构变化

S1：

```text
只新增特征服务只读入口。
不创建节点。
不写主信息。
不写关系。
不写索引。
```

S2：

```text
允许通过特征服务创建实例特征槽位节点。
允许写入宿主到实例特征槽位的关系。
允许写入实例特征槽位到特征类型的关系。
不得新增节点类型、关系类型或旧主信息字段。
若现有关系语义无法无歧义承载宿主槽位和特征类型关系，必须停止并回到设计确认。
```

S3：

```text
允许通过特征服务复用既有特征值节点和特征到特征值归属关系。
允许经内部特征值服务写入和读取 I64 特征状态材料。
只做归属复核和读取拒绝。
不新增当前值唯一性约束。
不新增 Vec、哈希、命中统计、稳态、值域或序列化结构。
```

S4：

```text
不新增机器结构。
只做入口拒绝验收、结构不变验收和高级服务越权依赖复核。
```

## 8. 流程状态表

| 输入条件 | 判断条件 | 输出状态 | 失败分流 | 谁继续承接 |
| --- | --- | --- | --- | --- |
| 特征句柄 | 句柄有效、节点类型为特征 | 特征身份可读 | 返回空，不写结构 | 特征服务 |
| 特征句柄 + 语义候选 | 特征有效，候选目标属于允许信息节点 | 特征语义类型可读 | 返回空，不裁决事实 | 特征服务 / 语素服务只读路径 |
| 宿主节点 + 特征类型 | 宿主有效、类型允许、特征类型有效 | 实例特征槽位建立 | 返回无效句柄，节点 / 主信息 / 关系 / 索引数量不变 | 特征服务 |
| 宿主节点 + 特征类型 + I64 | 宿主槽位存在，特征值归属目标特征 | I64 特征状态材料写入 | 返回 false，原 I64 材料不变 | 特征服务 |
| 宿主节点 + 特征类型 | 宿主槽位存在，特征值归属目标特征且已写值 | I64 特征状态材料可读 | 返回空，不生成默认事实 | 特征服务 |
| 高级服务需要特征状态材料 | 只允许依赖特征服务 | 通过特征服务读取 / 写入 | 直接依赖特征值服务视为越权 | 需求 / 任务 / 方法服务 |

## 9. 拒绝后结构不变化检查方式

代码实施完成后，默认入口必须至少构造以下拒绝样例，并在拒绝前后读取结构数量和关键材料：

```text
无效特征句柄读取身份。
错误节点类型读取特征身份。
无效宿主创建实例特征槽位。
错误宿主类型创建实例特征槽位。
无效特征类型创建实例特征槽位。
宿主无槽位读取 I64 特征状态材料。
非归属特征值写入 I64。
非归属特征值读取 I64。
未写值读取 I64。
高级服务直接依赖特征值服务的 rg 检索。
```

每个拒绝样例至少比较：

```text
节点数量不变。
主信息数量不变。
关系数量不变。
索引数量不变。
拒绝前已有 I64 特征状态材料不变。
```

如果当前仓库只读数量接口不足以覆盖某一项，不得用日志或控制台文本替代机器证据；必须在实施前回到设计确认，补齐可验证入口或调整切片边界。

## 10. 反走样清单

实施中任一命中即视为切片偏离：

```text
新增旧特征主信息字段。
新增旧抽象特征主信息字段。
新增旧特征值主信息字段。
用日志、控制台、说明文本或临时 JSON 表达特征事实。
用索引命中替代节点类型复核。
用旧宿主子链替代关系仓库表达宿主槽位。
让需求、任务、方法直接包含、接收或调用特征值服务。
把 I64 当作需求目标对外语义。
把 VecU、VecI64、体素、哈希、命中统计、稳态或值域塞入第一轮 I64 入口。
把非权威缓存命中当作特征值事实。
把旧函数编号当作新函数迁移单位。
```

## 11. 验证剧本

代码实施确认并完成后必须执行：

```powershell
msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m
.\x64\Debug\海中鱼巣.exe
rg -n "特征值服务" "海中鱼巣\领域\需求服务.h" "海中鱼巣\领域\任务服务.h" "海中鱼巣\领域\方法服务.h" "海中鱼巣\领域\动态服务.h"
git diff --check
if (Test-Path tools\check_specs.py) { python .\tools\check_specs.py }
```

默认入口至少输出或由独立验收汇总证明：

```text
FS-03 S1 特征身份读取: 通过
FS-03 S1 特征语义类型读取: 通过
FS-03 S2 实例特征槽位创建: 通过
FS-03 S2 实例特征槽位拒绝: 通过
FS-03 S3 I64 写入读回: 通过
FS-03 S3 非归属写入拒绝: 通过
FS-03 S3 非归属读取拒绝: 通过
FS-03 S4 高级服务越权依赖复核: 通过
FS-03 S4 拒绝后结构不变: 通过
```

`rg` 命令在高级服务文件中命中 `特征值服务` 时，不得声明 S4 通过。

## 12. 最小切片计划

| 顺序 | 切片 | 内容 | 允许文件 | 验证 |
| --- | --- | --- | --- | --- |
| S1 | 特征身份与语义类型只读入口 | 新增或扩展特征身份复核、节点类型判断和语义类型只读入口 | `特征服务.h`、`入口.cpp` | FS03-S1 身份读取、错误类型返回空、语义候选复核 |
| S2 | 实例特征槽位与宿主关系 | 经特征服务建立宿主槽位关系，读取宿主特征槽位和集合 | `特征服务.h`、`入口.cpp` | FS03-S2 有效槽位建立、无效宿主拒绝、结构不变 |
| S3 | I64 特征值写入读取封口 | 经特征服务写入 / 读取宿主当前 I64 特征状态材料，保持特征值服务 public 现状 | `特征服务.h`、必要时 `特征值服务.h`、`入口.cpp` | FS03-S3 写入读回、非归属写入 / 读取拒绝、原值不变 |
| S4 | 拒绝路径和越权依赖复核 | 补拒绝样例、依赖扫描、完成声明边界 | `入口.cpp`、项目记忆、实施记录 | FS03-S4 结构不变、依赖扫描无越权、`git diff --check` |

S1、S2、S3、S4 必须按顺序实施和记录；不得把 S2 到 S4 提前合并成一个无中间断点的大改动。

## 13. 当前确认门禁

本文件当前状态为：

```text
已确认。
代码已实现。
Debug x64 构建通过。
默认入口 FS-03 S1-S4 验收通过。
高级服务越权依赖扫描无命中。
未迁移旧函数体。
未新增旧主信息字段。
```

本轮实际修改 C++ 仅限：

```text
海中鱼巣/领域/特征服务.h
海中鱼巣/入口.cpp
```

未修改 `海中鱼巣/领域/特征值服务.h`、核心仓库、工程文件、高级服务、旧项目源码、SQL / 控制面板 / D455 / 体素 / 外设文件。

## 14. 完成声明边界

本切片完成后只允许声明：

```text
FS-03 S1-S4 第一轮代码实施已按确认切片完成。
特征服务已新增特征身份与语义类型只读入口、实例特征槽位入口、宿主 I64 特征状态材料读写入口和拒绝路径验收。
Debug x64 构建通过。
默认入口 FS-03 S1-S4 验收输出均为通过。
高级服务文件未直接依赖特征值服务。
未迁移旧函数体。
未新增旧主信息字段。
```

禁止声明：

```text
旧特征 / 特征值能力已完整迁移。
特征服务封口增强完整完成。
特征值服务 public 入口治理已完成。
当前值唯一性治理已完成。
旧特征 / 特征值能力已迁移。
Vec / 哈希 / 稳态 / 值域 / 序列化 / 体素 / 外设 / 控制面板 / SQL 已接入。
```
