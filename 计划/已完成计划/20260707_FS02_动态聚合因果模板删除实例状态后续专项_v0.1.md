# FS-02 动态聚合因果模板删除实例状态后续专项 v0.1

日期：2026-07-07
状态：已完成 / 已归档计划 / S0-S4 文档治理范围已收口 / 三类详细设计已生成 / 代码候选已由 #129 批次包归并 / 未实现删除写入口 / 未迁移旧函数

## 1. 依据

```text
AGENTS.md
计划/计划索引.md
规范/000_项目规则总纲.md
规范/001_规则迁移清单.md
实施记录/20260706_FS02_状态动态二次特征因果逻辑迁移包归并表.md
规范/详细设计/状态动态二次特征因果服务增强详细设计.md
计划/已完成计划/20260706_FS02_状态动态二次特征因果服务增强专项_v0.1.md
计划/已完成计划/20260707_FS02_状态动态二次特征因果第一轮代码实施切片_v0.1.md
实施记录/20260707_FS02_状态动态二次特征因果第一轮代码实施_Codex断点清单.md
```

本计划承接 FS-02 第一轮已经明确暂缓或排除的后续能力。旧函数编号只作为证据锚点，迁移判定单位仍是服务逻辑包和领域服务结构闭环。

```text
函数 = 证据采集单位
服务逻辑包 = 迁移确认单位
领域服务 / 结构闭环 = 代码实施单位
```

## 2. 确认范围

本计划仅生成后续专项路线，等待用户确认。确认后也只允许登记并执行 S0 当前代码事实扫描，不直接进入 C++ 实施。

纳入范围：

```text
FS-02-G：动态聚合和运动基元候选。
FS-02-J：因果模板、任务化和查询候选。
FS-02-D 后续项：删除实例状态生命周期治理。
```

本专项目标不是补旧函数体，而是判断这些后续能力在新项目中应由哪些服务和结构承载。

## 3. 明确排除项

本计划不允许：

```text
不迁移旧函数体。
不新增旧状态 / 动态 / 二次特征 / 因果主信息字段。
不直接实现删除实例状态写入口。
不直接实现动态聚合算法、运动基元算法、因果模板生成算法或任务化自动生成。
不把候选查询、显示文本、自然语言说明、证据计数或日志标签当作机器事实。
不接控制面板、SQL、WebView2、窗口、截图、线程、D455、体素或外设。
不改变 FS-02 第一轮已经验证的状态、动态、二次特征、因果入口语义。
```

## 4. 目标服务和目标结构

| 后续能力 | 目标服务 | 目标结构 | 第一轮计划口径 |
| --- | --- | --- | --- |
| 动态聚合候选 | 动态服务、二次特征服务、方法服务后续增强 | 动态候选集合、来源动态材料、二次特征候选、非权威统计 | 先只读扫描当前动态材料和候选承载缺口，不生成稳定动态结论 |
| 运动基元候选 | 动态服务、二次特征服务、方法服务后续增强 | 状态变化片段、动态序列、目标 / 前值 / 后值 / 时间戳材料 | 仅作为候选治理路线，不接外设、体素或控制面板观察链 |
| 因果模板 | 因果服务后续增强、动态服务 | 动态证据集合、来源动作 / 来源方法、目标状态、实际状态、评分或版本候选 | 不把单个因果引用升级为稳定因果结论 |
| 因果任务化和查询候选 | 因果服务、需求服务、任务服务、方法服务 | 因果候选、任务候选、方法候选、缺失证据材料 | 查询只返回候选和缺失证据，不新增需求、任务、方法或结算事实 |
| 删除实例状态生命周期治理 | 状态服务、动态服务、二次特征服务、关系仓库 | 状态生命周期、引用关系、场景临时关系、后续动态 / 二次特征引用检查 | S0 先扫描当前引用路径和失效口径；代码写入口另建切片确认 |

## 5. 实施步骤

| 步骤 | 名称 | 内容 | 产物 | 是否允许改代码 |
| --- | --- | --- | --- | --- |
| S0 | 当前代码事实扫描 | 只读扫描状态、动态、二次特征、因果、任务、方法、关系仓库和入口样例中与后续能力相关的现状 | `实施记录/20260707_FS02_动态聚合因果模板删除实例状态后续S0当前代码事实扫描_Codex断点清单.md` | 否 |
| S1 | 动态聚合候选详细设计 | 基于 S0 判断动态候选集合、运动基元候选、二次特征候选和方法候选的边界 | 待生成详细设计或实施切片 | 否，除非后续独立确认代码切片 |
| S2 | 因果模板和任务化候选详细设计 | 基于 S0 判断因果模板、动态证据集合、任务化候选和查询候选的边界 | 待生成详细设计或实施切片 | 否，除非后续独立确认代码切片 |
| S3 | 删除实例状态生命周期详细设计 | 基于 S0 判断删除准入、引用阻断、失效后读取拒绝和关系清理边界 | 待生成详细设计或实施切片 | 否，除非后续独立确认代码切片 |
| S4 | 计划索引和项目记忆收口 | 写入后续待确认问题、可执行队列或设计队列 | 项目记忆、计划索引、断点清单 | 否 |

## 6. 允许文件

本计划生成阶段只允许修改：

```text
计划/待确认计划/20260707_FS02_动态聚合因果模板删除实例状态后续专项_v0.1.md
实施记录/20260707_FS02_动态聚合因果模板删除实例状态后续专项生成_Codex断点清单.md
计划/计划索引.md
项目记忆/Codex任务队列.md
项目记忆/当前状态.md
项目记忆/待确认问题.md
项目记忆/实施记录.md
```

本计划确认后，若执行 S0 当前代码事实扫描，仍只读代码和文档，另行写入 S0 断点清单。

## 7. 禁止文件

本计划生成阶段禁止修改：

```text
海中鱼巣/领域/*.h
海中鱼巣/核心/*.h
海中鱼巣/入口.cpp
海中鱼巣.vcxproj
海中鱼巣.slnx
旧项目源码
SQL / 控制面板 / WebView / 显示写入相关文件
D455 / 体素 / 外设相关文件
```

## 8. 验收方式

计划生成验收：

```text
待确认计划已写入 计划/待确认计划/。
断点清单已写入 实施记录/。
计划索引和项目记忆已登记为待确认计划。
未修改 C++。
git diff --check 通过。
tools/check_specs.py 如存在则通过。
```

本计划确认后默认下一步：

```text
登记并执行 FS-02 后续专项 S0 当前代码事实扫描。
S0 完成后如发现边界问题，写入 项目记忆/待确认问题.md。
S1 到 S3 任何代码实施必须另建独立代码实施切片，并再次确认允许文件、禁止文件和验证方式。
```

## 9. 待确认问题

| 编号 | 问题 | 默认建议 |
| --- | --- | --- |
| FS02-FOLLOW-Q1 | 是否确认本计划作为 FS-02-G、FS-02-J 和删除实例状态后续专项路线。 | 确认后只登记 S0 当前代码事实扫描，不直接进入代码。 |
| FS02-FOLLOW-Q2 | 动态聚合和运动基元候选是否可与因果模板候选共用 S0 扫描。 | 可以共用 S0，因为二者都依赖动态证据材料和候选边界；后续实施切片再拆分。 |
| FS02-FOLLOW-Q3 | 删除实例状态生命周期治理是否进入本专项同一 S0。 | 可以同扫引用路径，但不得与候选算法实施合并；删除写入口后续单独设计和确认。 |

## 10. 完成声明边界

确认本计划后只能声明：

```text
FS-02 后续专项计划已确认。
允许登记 FS-02 后续专项 S0 当前代码事实扫描。
```

不得声明：

```text
动态聚合已实现。
运动基元候选已实现。
因果模板、因果任务化或查询候选已实现。
删除实例状态写入口已实现。
旧状态 / 动态 / 二次特征 / 因果函数体已迁移。
旧主信息字段已迁移。
控制面板 / SQL / D455 / 体素 / 外设已接入。
```
