# INSTINCT-STAGE3-TD-CURRENT-GROUP-READBACK 任务来源需求关系组正式读回代码实施计划 v0.1

日期：2026-08-30

计划身份：`INSTINCT-STAGE3-TD-CURRENT-GROUP-READBACK`

## 1. 目标与完成条件

在现有 task owner 内实现生产首次任务融合的首个显式 `T→D` 原子写入、任务退出同步收口，以及 `按任务读取来源需求关系组` 的同 `G0` 完整 `0/N` 读取。

完成必须同时满足：

1. 生产 `新增任务与首次初次筹办准备记录` 成功 / 精确重复都能由公开读取取得逐字段一致的首个 `T→D`；
2. 兼容 `新增任务` 可返回合法 0 项，不从列表项补造关系；
3. `0/N` 组规范排序、关系身份唯一、读前 / 读后 G0 守卫和全部非成功空载荷成立；
4. 任务退出同代退出全部当前 `T→D`；
5. 不改变既有十项任务类型登记请求，新增两项登记可首次、重放和恢复；
6. Debug / Release、专项、ABI consumer、strict 和两类 diff-check 通过；
7. 当前生产 L/H 和服务事实 owner 零改动。

## 2. 正式依据与创建事实

直接依据：4260 v0.10 与：

```text
规范/详细设计/20260830_INSTINCT-STAGE3-TD-CURRENT-GROUP-READBACK_任务来源需求关系组正式读回详细设计_v0.1.md
```

创建基线：`main == origin/main == 319577490148b3a62b00376a5cdce59b3505401c`，index 空。

已发布 task owner 有当前方法选择联合读回，但没有 `T→D` 关系类型、生产写入或完整组读取。工作区目标两文件存在更大范围异主历史 WIP；本计划必须从 `HEAD` 构造候选并精确合成 index，只发布本计划差异。

## 3. 实施白名单

允许修改：

```text
海中鱼巣/领域/L2任务结构.数据.h
海中鱼巣/领域/服务.L2任务结构.ixx
施工记录/20260830_INSTINCT-STAGE3-TD-CURRENT-GROUP-READBACK_施工记录_v0.1.md
验证记录/20260830_INSTINCT-STAGE3-TD-CURRENT-GROUP-READBACK_验证记录_v0.1.md
```

验证候选允许在 `D:\TEMP\海中鱼巣\INSTINCT-STAGE3-TD-CURRENT-GROUP-READBACK\<本轮>` 临时覆盖既有测试模块或建立 driver；临时文件不得进入 Git。

禁止修改 / 发布：普通应用、工程文件、需求 owner、服务合同 owner、状态 / 动态、线程、SELF-GOVERNANCE 其它 WIP、生产 L/H、安全门禁、FRESH、流程图、其它规范 / 设计 / 计划。

## 4. 目标函数和物理合同

数据头新增 `L2任务来源需求关系事实`、专属版本、请求、结果和成功谓词。服务模块新增：

```cpp
L2按任务读取来源需求关系组结果
L2任务结构服务::按任务读取来源需求关系组(
    L2按任务读取来源需求关系组请求) const noexcept;
```

登记、写入、退出、读取、错误映射和版本合同完全以详细设计第 3—7 节为准；执行者不得更换键、从列表项推导需求或把关系复制进任务事实。

## 5. 实施步骤

1. 从计划起点提交导出两个目标代码文件，在隔离候选中实现 DTO 和结果谓词。
2. 扩展 task owner 类型定位，以独立幂等身份登记 `0x0301'2114/2115`，保持既有十项登记请求字节级不变。
3. 给首次融合写集、编码映射和正式读回追加 `0x0301'2613` 的首个 `T→D`。
4. 给任务退出闭包追加 `0/N` 当前 `T→D` 的完整验证与同代退出。
5. 实现 public 完整组读取、排序、唯一性、双守卫和失败收口。
6. 仅在验证宏中加入建立 / 损坏 / 退出关系样本和漂移 / 资源失败注入；生产宏关闭时不得出现第二业务写入口。
7. 在隔离候选执行两配置、专项、ABI consumer 和回归；形成施工 / 验证记录。
8. 把 `HEAD + 本计划差异` 精确写入 index；工作区其余 WIP保持 unstaged。提交、普通推送后归档计划并清空索引。

## 6. 验证矩阵

- 登记首次、精确重复、持久恢复和同键异义 fail-closed；
- 生产首次融合 1 项、兼容任务 0 项；
- 逆序 N 项排序、重复身份 / 乱序结果谓词拒绝；
- 单关系退出、任务退出、历史不污染当前组；
- 坏专属版本、坏共同头、零 G0、坏任务、未找到、已退出；
- 读前 / 读后漂移、许可 / 资源 / 内部映射；
- Debug / Release 根工程；
- Debug 与 Release `/GL + /LTCG` ABI consumer；
- `python .\tools\check_specs.py --strict`、`git diff --check`、`git diff --cached --check`；
- 静态确认生产 L/H、服务合同事实 owner、FRESH 和安全门禁零改动。

不能稳定构造的故障注入必须明确记录 `NOT_RUN`，不得以静态搜索或编译成功替代运行证据。

## 7. 记录和完成声明

施工记录必须包含函数级代码知识变更清单；验证记录逐项分账 PASS / FAIL / NOT_RUN、隔离候选根和提交树边界。

本计划完成最多声明 task owner 的生产首个 `T→D` 和同 G0 当前完整组读取闭合。不得声明生产服务活动、安全门禁、被动维护事务、阶段三或完整本能路线完成。
