# INSTINCT-ROUTE-P00 任务轮次正式生产与同 G0 读回代码实施计划 v0.1

计划身份：`INSTINCT-ROUTE-P00-TASK-ROUND-PROVIDER`

形成日期：2026-09-01

正文状态：创建候选。计划运行状态只由`计划/计划索引.md`维护；发布前不得执行。

## 1. 目标与完成条件

按`规范/详细设计/20260901_INSTINCT-ROUTE-P00_任务轮次正式生产与同G0读回详细设计_v0.1.md`，在 task owner 内完成一个最小闭环：

```text
新任务核心原子建立 T/T→D/T→E/R1/T→当前R1
-> 按建立幂等身份完整读回
-> 按任务在同 G0 读取当前 T 与 R
```

完成条件是上述生产来源和两个读回入口均由结构化结果闭合。P00 不处理线程消息；每次筹办结束形成唯一结果并通过消息回送任务管理线程，仍由 P01 单独实施。

## 2. 依据与基线

- 详细设计：`规范/详细设计/20260901_INSTINCT-ROUTE-P00_任务轮次正式生产与同G0读回详细设计_v0.1.md`；
- 正式机器逻辑：0050、3200、4260；
- 直接后继：`规范/详细设计/20260901_INSTINCT-ROUTE-P01_筹办工作包与结果消息详细设计_v0.1.md`；
- 代码基线：`a741364eea8951d5ba482eae369a05eaaaa97356`。

执行 S0 必须重新核对 HEAD、origin/main、index、目标文件 dirty hunk 和所有权。共享工作树中两份生产文件已有大范围异主 WIP；只允许抽取逐项符合本计划的最小 hunk，不得整文件覆盖、暂存或提交。

## 3. 允许范围

生产白名单：

```text
海中鱼巣/领域/L2任务结构.数据.h
海中鱼巣/领域/服务.L2任务结构.ixx
```

验证与记录白名单：

```text
海中鱼巣/端到端测试.本能路线阶段二.ixx
施工记录/20260901_INSTINCT-ROUTE-P00-TASK-ROUND-PROVIDER_施工记录_v0.1.md
验证记录/20260901_INSTINCT-ROUTE-P00-TASK-ROUND-PROVIDER_验证记录_v0.1.md
```

未列路径全部禁止。不改线程、消息协议、筹办 provider、运行包、工程、filters、现有规范、流程图或知识索引。

## 4. 机械实施顺序

1. 重新执行 S0：从正式 HEAD 读取两生产文件和专项，记录各自 blob；对共享 dirty 逐 hunk 标出 P00 可机械复用项与异主项。若目标语义已经被其它所有者占用，只停止重叠切片并交回协调，不得覆盖。
2. 在数据文件严格按详细设计 §3—§5 补齐全部 DTO、专属版本、enum 数值、相等与完整性守卫；`成功()`自闭合检查公共/专属合同版本、状态、载荷、截止、变更代次、R1 序号和 T/L/D/E/R 互证，不从 WIP 猜字段。
3. 在服务文件建立独立`任务轮次核心定位`、`初始化任务轮次核心登记`和成员`任务轮次核心定位_`；服务构造期间初始化并读回，仅登记 `0x0301'2180..2184`五项。以静态断言固定数量、范围和互异，不复用或扩大`初次筹办治理定位_`，不宣称其余十项已登记。
4. 按冻结物理名实现`形成任务轮次核心写集`、`任务轮次核心写入编码映射`、`查找任务轮次核心写入编码映射`和`读回任务轮次核心`；固定`新任务首次来源需求关系写入本地键 = 0x0301'2613`承载 T→D、`新任务正式存在关系写入本地键 = 0x0301'2614`承载 T→E，R1 使用 `0x0301'2660..2664`。以`static_assert`固定这些键的精确值、数量，并与彼此及同写集 T、族归属、T→L 等其它键两两互异；随后单次原子写入完整任务核心并按建立幂等身份读回。
5. 实现`按建立幂等身份读取任务轮次核心`，只从首次规范化写集恢复同一任务核心，严格分账未找到、已退出、许可、漂移、引用、资源和内部错误。
6. 实现`当前任务轮次结构化投影`和`读取当前任务轮次结构化投影`，再由 public const `按任务读取当前任务与轮次`单次映射；0 条当前关系返回公共内部不一致/专用当前轮次未找到，1 条完整成功，>1 条公共内部不一致/专用关系多义；L1 owner-aware 许可拒绝机械映射，读前读后代次均须等于 G0。
7. P00 只由既有本能路线阶段二专项 harness 消费正式写/读入口，不迁移任何白名单外业务调用点。若专项必须修改白名单外文件，作为范围漂移退回；普通任务创建生产接线留给路线后继，P01 激活门禁仅消费 P00 provider 成功专项证据与正式接口。
8. 在既有本能路线阶段二专项加入详细设计 §8 矩阵；形成施工/验证记录和函数级知识变更清单，但不直接修改共享知识索引。

## 5. 明确禁止

- 只增加读取入口而不增加 R1 和 `T→当前R1`生产来源；
- 把一个当前任务缺当前轮次关系降级为合法未找到；
- 用筹办轮次、执行轮次、任务序号、线程槽或普通整数猜测任务轮次；
- 把当前共享大文件 WIP 整包接纳；
- 将治理函数、治理动作、P1、主阶段、结算、后继决议或 FRESH 带入 P00；
- 新增消息、回执账、outbox、线程、队列、缓存或第二轮次事实；
- 修改旧入口形成第二条本路线生产链。

## 6. 候选树隔离与验证

由于共享工作树两生产文件含大范围异主 WIP，验证必须分两层：

1. 在主工作区仅形成 P00 精确 hunk，不暂存、不覆盖异主 hunk；导出只含生产白名单、专项和本计划记录的 P00 补丁。
2. 用`git archive HEAD`建立非 Git、非 worktree 的临时候选树，将 P00 精确补丁应用到候选树；不得复制主工作区整文件覆盖候选树。补丁若不能干净应用到 HEAD，视为设计/基线漂移。
3. 在候选树运行源码断言、专项、Debug/Release 构建、严格规范检查和`git diff --check`等价检查；候选树结果只证明 P00 补丁相对正式基线，不吸收共享 WIP。
4. 候选验证通过后回到主工作区，重新核对目标 hunk所有权；发布所有者只精确暂存 P00 白名单。候选树、临时输出和异主 WIP不得进入 index。

## 7. 验证与交付

实现完成后按风险顺序执行：

1. 精确源码断言和专项矩阵；
2. Debug/Release 全量构建；
3. `python .\tools\check_specs.py --strict`；
4. `git diff --check`；
5. 施工/验证记录绑定实际提交、读回和失败矩阵。

用户要求先推进整条链路，不以 P00 单叶验证作为顶层停点；但执行者仍须完成本叶最低工程自检后立即交回顶层继续推进 P01。具名集成验收留到消息链与后继链接通后统一进行。

## 8. 完成声明上限

允许声明：task owner 的新任务 R1 正式生产来源和同 G0 当前任务/轮次读回已经闭合，可供 P01 绑定工作包来源。

禁止声明：筹办工作包已派发、每次筹办结果已通过消息返回任务管理线程、P01/P02/P03、现实动作、需求满足或 INSTINCT-ROUTE 全链完成。
