# DATA-L2 状态动态因果写许可拒绝保持代码实施切片

日期：2026-08-15
版本：v0.1
身份：`DATA-L2-WORLD-WRITE-PERMISSION-STATUS-PRESERVE`
状态：设计就绪；不等于代码已实现

## 1. 目标

依据[详细设计](../规范/详细设计/20260815_DATA-L2-WORLD-WRITE-PERMISSION-STATUS-PRESERVE_状态动态因果写许可拒绝保持详细设计_v0.1.md)，只在状态、动态、因果三个 L2 服务内补齐六个 L1 `许可拒绝` 到现有 L2 同名状态的机械映射，避免明确许可失败被误报为内部不一致。

## 2. 允许范围

生产修改只允许：

1. `海中鱼巣/领域/服务.L2状态结构.ixx`；
2. `海中鱼巣/领域/服务.L2动态结构.ixx`；
3. `海中鱼巣/领域/服务.L2因果结构.ixx`。

允许新增：

- `施工记录/20260815_DATA-L2-WORLD-WRITE-PERMISSION-STATUS-PRESERVE_状态动态因果写许可拒绝保持施工记录_v0.1.md`；
- `验证记录/20260815_DATA-L2-WORLD-WRITE-PERMISSION-STATUS-PRESERVE_状态动态因果写许可拒绝保持施工检查记录_v0.1.md`。

禁止修改公共头、L1、场景、存在、特征、聚合、普通应用、工程、最终需求清单、BIZ、线程、流程图和异主 WIP。

## 3. 施工步骤

1. 从索引绑定的正式 HEAD 重做 S0，确认三目标服务无异主修改且 index 空。
2. 在详细设计列出的六个 switch 中各追加唯一同名映射 case；不改 default 和其它分支。
3. 静态核对六处映射、三文件差异、无公开 ABI 变化及生产零验证痕迹。
4. 以仓库外专属输出执行根工程 x64 Debug / Release Rebuild；不建立专项验收或故障 seam。
5. 删除临时构建文件，形成两份永久记录，只提交本计划五文件并普通推送。
6. 单独登记计划退出；随后继续扫描最终需求清单下一缺口。

## 4. 成功条件

- 六个映射逐项等值成立；
- 其它状态映射及未知枚举收口不变；
- 根工程 Debug / Release Rebuild 均通过；
- diff、strict、UTF-8、白名单、生产零验证痕迹和临时资源清理通过；
- 真实许可拒绝动态分支如实 `NOT_RUN`，没有为了验证而增加生产代码。

本计划完成只证明三服务写许可状态不丢失，不证明状态、动态、因果服务整项、DATA-L1—L3 或最终需求清单整体完成。
