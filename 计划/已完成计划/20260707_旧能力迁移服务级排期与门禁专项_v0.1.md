# 旧能力迁移服务级排期与门禁专项 v0.1

日期：2026-07-07
状态：已完成 / 已归档计划 / S0-S4 服务级排期与门禁复核已收口 / 未改代码 / 未迁移旧函数

## 1. 依据

```text
AGENTS.md
计划/计划索引.md
规范/000_项目规则总纲.md
规范/001_规则迁移清单.md
计划/20260706_函数迁移前置专项_v0.1.md
实施记录/20260706_逐函数只读扫描清单.md
项目记忆/Codex任务队列.md
项目记忆/待确认问题.md
```

本计划只建立旧能力迁移的服务级排期和门禁，不迁移旧函数体，不新增旧主信息字段，不修改 C++。

## 2. 确认范围

确认本计划后，只允许登记并执行 S0 服务包状态复核，复核对象是已经生成或确认的服务级计划、逻辑迁移包和 S0 断点清单。

确认粒度：

```text
服务逻辑包。
服务增强专项计划。
服务代码实施切片。
```

不再逐函数确认，但函数事实仍是证据锚点。

## 3. 当前服务包状态分层

| 层级 | 当前对象 | 当前状态 | 后续门禁 |
| --- | --- | --- | --- |
| 已验证第一轮代码 | FS-02 第一轮、FS-03 S1-S4、任务 / 方法 / 动作入口 / 缓存统计 / 需求结算第一轮 | 已按各自切片验证 | 不扩大声明为完整系统或旧能力迁移完成 |
| 已确认计划待执行 S0 | FS-07、FS-08、FS-09 | 已登记可执行 S0 | 执行窗口先消费 S0，只读扫描后再生成 S1-Sn 切片 |
| 已完成 S0 但待后续切片 | FS-04、FS-05、FS-06 | S0 已完成 / 发现缺口 | 后续必须另建代码实施切片并确认 |
| 已生成待确认计划 | FS-10、FS-03 第二轮、FS-02 后续、基础信息服务分层完整增强 | 待确认 | 用户确认后才可登记 S0 |
| 排除 / 暂缓 | FS-X、控制面板、SQL、D455、体素、外设 | 本轮排除 | 必须另建规范、详细设计和计划 |

## 4. 明确排除项

本专项不允许：

```text
不复制旧函数体。
不把旧函数编号当作迁移单位。
不新增旧主信息字段。
不把日志、显示、SQL、控制面板、线程、外设材料当作机器事实。
不跳过服务专项计划、S0 当前代码事实扫描或代码实施切片。
不接控制面板、SQL、D455、体素或外设。
```

## 5. 实施步骤

| 步骤 | 名称 | 内容 | 产物 | 是否允许改代码 |
| --- | --- | --- | --- | --- |
| S0 | 服务包状态复核 | 复核当前已确认、待确认、待 S0、S0 已完成、待代码切片和排除项状态 | `实施记录/20260707_旧能力迁移服务级排期S0状态复核_Codex断点清单.md` | 否 |
| S1 | 已完成 S0 的代码切片候选排序 | 从 FS-04、FS-05、FS-06 等已完成 S0 的服务包中挑选可生成代码实施切片的对象 | 待确认切片清单 | 否 |
| S2 | 待确认计划入队规则复核 | 确认 FS-10、FS-03 第二轮、FS-02 后续、基础信息完整增强确认后各自登记哪个 S0 | 待确认问题或队列更新 | 否 |
| S3 | 排除项复核 | 确认 FS-X 和外部 / 显示后续边界仍不得进入本轮代码实施 | 排除项清单 | 否 |
| S4 | 计划索引和项目记忆收口 | 同步排期表、待确认问题、禁止扩大声明 | 项目记忆、计划索引、断点清单 | 否 |

## 6. 允许文件

本计划生成阶段只允许修改：

```text
计划/待确认计划/20260707_旧能力迁移服务级排期与门禁专项_v0.1.md
实施记录/20260707_旧能力迁移服务级排期与门禁专项生成_Codex断点清单.md
计划/计划索引.md
项目记忆/Codex任务队列.md
项目记忆/当前状态.md
项目记忆/待确认问题.md
项目记忆/实施记录.md
```

## 7. 禁止文件

本计划生成阶段禁止修改：

```text
海中鱼巣/领域/*.h
海中鱼巣/核心/*.h
海中鱼巣/入口.cpp
海中鱼巣.vcxproj
海中鱼巣.slnx
旧项目源码
SQL / 控制面板 / WebView / 显示写入相关文件
D455 / 体素 / 外设相关文件
```

## 8. 验收方式

计划生成验收：

```text
待确认计划已写入 计划/待确认计划/。
断点清单已写入 实施记录/。
计划索引和项目记忆已登记为待确认计划。
未修改 C++。
git diff --check 通过。
tools/check_specs.py 如存在则通过。
```

本计划确认后，S0 复核至少输出：

```text
哪些服务包已完成 S0，可生成代码实施切片。
哪些服务包仍待用户确认计划。
哪些服务包仍待执行 S0。
哪些旧能力明确排除或暂缓。
哪些问题必须回到待确认队列。
```

## 9. 待确认问题

| 编号 | 问题 | 默认建议 |
| --- | --- | --- |
| MIG-Q1 | 是否确认本计划作为旧能力迁移服务级排期与门禁路线。 | 确认后只登记 S0 服务包状态复核，不直接修改 C++。 |
| MIG-Q2 | S0 后是否优先从已完成 S0 的 FS-04 / FS-05 / FS-06 生成代码实施切片。 | 是；但每个代码实施切片仍需独立确认允许文件、禁止文件和验证方式。 |
| MIG-Q3 | 待确认计划是否先等待用户确认再入队。 | 是；FS-10、FS-03 第二轮、FS-02 后续、基础信息完整增强未确认前不登记 S0。 |

## 10. 完成声明边界

确认本计划后只能声明：

```text
旧能力迁移服务级排期与门禁专项计划已确认。
允许登记旧能力迁移服务包状态复核 S0。
```

不得声明：

```text
旧能力已迁移。
旧函数体已迁移。
旧主信息字段已迁移。
FS-04 / FS-05 / FS-06 后续代码切片已许可。
控制面板 / SQL / D455 / 体素 / 外设已接入。
```
