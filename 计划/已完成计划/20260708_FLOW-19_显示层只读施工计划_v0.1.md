# FLOW-19 显示层只读施工计划 v0.1

日期：2026-07-08
状态：已完成 / 已验证 / 已归档计划 / 代码已实现 / 未迁移旧函数

## 1. 依据

```text
规范/详细设计/显示层只读代码逻辑详细设计.md
流程图/20260708_显示层只读代码逻辑流程图_v0.1.md
当前代码事实：入口输出和领域服务只读材料
```

## 2. 确认范围

```text
显示层只读服务来源。
展示材料不承载机器事实。
显示层禁止写入。
DTO / JSON / WebView 专项门禁。
```

## 3. 允许文件

```text
海中鱼巣/领域/显示服务.h
海中鱼巣/入口.cpp
```

若确认实施时不新增 `显示服务.h`，必须在 S0 记录替代承载文件；不得接控制面板或 WebView。

## 4. 禁止文件

```text
海中鱼巣/核心/**
海中鱼巣/领域/需求服务.h
海中鱼巣/领域/任务服务.h
海中鱼巣/领域/方法服务.h
海中鱼巣.vcxproj
控制面板 / SQL / ADO / WebView / D455 / 体素 / 外设相关文件
旧项目源码
```

## 5. 实施步骤

```text
S0：复核当前入口输出和领域服务只读材料。
S1：建立显示层只读材料壳或入口只读样例，不写业务事实。
S2：展示材料只来源于领域服务只读入口，不直接读写仓库内部可写对象。
S3：DTO / JSON / WebView 只登记后续门禁，不在本轮接入。
S4：补显示层写入禁止和排除项扫描。
```

## 6. 验收方式

```powershell
git diff --check -- 海中鱼巣/领域/显示服务.h 海中鱼巣/入口.cpp
python .\tools\check_specs.py
msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m
.\x64\Debug\海中鱼巣.exe
rg -n "控制面板|SQL|ADO|WebView|D455|体素|外设|std::thread|std::async|工作队列" 海中鱼巣/领域/显示服务.h 海中鱼巣/入口.cpp
```

## 7. 完成声明边界

不得声明控制面板、数据库、WebView、JSON 接口、外设显示或旧显示能力迁移完成。
