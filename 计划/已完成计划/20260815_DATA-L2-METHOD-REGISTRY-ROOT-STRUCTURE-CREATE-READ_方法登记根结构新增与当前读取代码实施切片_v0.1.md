# DATA-L2 方法登记根结构新增与当前读取代码实施切片

日期：2026-08-15
版本：v0.1
身份：`DATA-L2-METHOD-REGISTRY-ROOT-STRUCTURE-CREATE-READ`
状态：设计就绪；以计划索引登记状态为准

## 1. 目标

依据[详细设计](../规范/详细设计/20260815_DATA-L2-METHOD-REGISTRY-ROOT-STRUCTURE-CREATE-READ_方法登记根结构新增与当前读取详细设计_v0.1.md)，在唯一方法 owner 内一次闭合方法登记根公开 DTO、建立、按角色读取和全部当前根读取三入口。

## 2. 激活门禁

必须等待：

1. `DATA-L2-METHOD-IDENTITY-CLOSURE v0.1` 结果发布、索引退出和专属构建输出清理；
2. EVENT 释放 `海中鱼巣.vcxproj/.filters`、构建和唯一代码段；
3. 最新 `main == origin/main`、index 空，L1 owner / 闭包 / 首次材料读取 ABI 与设计匹配。

任一条件不满足时保持待激活，不得消费未提交方法身份 WIP。

## 3. 允许范围

生产修改只允许：

- `海中鱼巣/领域/L2方法结构.数据.h`；
- `海中鱼巣/领域/服务.L2方法结构.ixx`；
- `海中鱼巣.vcxproj`；
- `海中鱼巣.vcxproj.filters`。

允许新增同名专属施工 / 验证记录。禁止修改共同头、L1、概念 / 世界服务、聚合、普通应用、BIZ、线程、规范、流程图、最终需求清单和异主 WIP。

## 4. 施工合同

1. 按设计逐字实现方法根角色、根事实、三请求 / 三结果、两个新纯值 helper 和三类 `成功()`。
2. 形成唯一方法 owner 交付与不可复制 / 移动的 `L2方法结构服务`。
3. 构造期登记方法族来源、根角色类型和首次见证属性类型；坏登记或读回使服务构造失败。
4. 一份 owner 写集原子建立根节点、族归属、根角色和首次请求见证。
5. 全组 / 单根读取使用一次关系类型闭包投影，不拼接多代结果。
6. 建立入口在私有锁内处理同请求重复、异请求占用、漂移、首次材料读回、发布未知和结构化错误；不生成第二幂等身份或重试循环。
7. 只登记新数据头和唯一真实模块，不建立 `.cpp` 实现单元、`.inl` 正文、第二服务或通用 owner 选择。

## 5. 验证与清理

必须执行：

- 根工程 x64 Debug / Release Rebuild；
- 公开 ABI、owner 唯一性、保留幂等值、工程唯一登记与禁止依赖静态检查；
- `git diff --check`、strict、UTF-8、XML 和生产零测试痕迹；
- 本叶专属构建输出文件清理。

不新增独立验收或临时专项。真实根首次 / 重复 / 并发、资源失败、发布未知和阶段 19 消费保持 `NOT_RUN`，由合法消费者实际使用时判断。

## 6. 完成边界

完成只证明方法登记根三入口及唯一 owner / 服务进入生产代码；不证明普通方法登记 CRUD、执行规格、聚合、阶段 19、方法执行或 DATA-EXT-14 整项完成。
