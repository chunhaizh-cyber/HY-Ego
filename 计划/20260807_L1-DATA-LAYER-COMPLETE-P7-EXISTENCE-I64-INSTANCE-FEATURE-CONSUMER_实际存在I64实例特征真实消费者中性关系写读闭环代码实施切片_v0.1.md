# L1-DATA-LAYER-COMPLETE-P7-EXISTENCE-I64-INSTANCE-FEATURE-CONSUMER 实际存在 I64 实例特征真实消费者中性关系写读闭环代码实施切片 v0.1

日期：2026-08-07

状态：待登记；登记后可执行

## 1. 目标、完成条件与依据

原位迁移 `L1实例特征服务` 的结构登记、实际存在 I64 实例建立和正式读取，使合法第二层消费者只经正式生产租约使用 L1 中性 CRUD。完成必须同时具备：四节点登记、1 节点 + 2 关系 + 1 值 + 1 槽实例写集、const 写入口、实际存在提供者正式接线、最小幂等、同宿主同定义唯一性和权威精确读回。

依据：0050、1120、1130、2100、4015 v0.5、4030 v1.3、4110 v0.7、ACCEPTANCE-01 v0.3，以及：

- `规范/详细设计/20260807_L1-DATA-LAYER-COMPLETE-P7-EXISTENCE-I64-INSTANCE-FEATURE-CONSUMER_实际存在I64实例特征真实消费者中性关系写读闭环详细设计_v0.1.md`
- `流程图/20260807_L1-DATA-LAYER-COMPLETE-P7-EXISTENCE-I64-INSTANCE-FEATURE-CONSUMER_建立并复用实际存在I64实例特征函数流程图_v0.1.md`

物理合同以 0050 v0.7、4015 v0.5 的较新上位裁决为准：当前值使用属性槽，来源使用值事实来源字段；不得恢复 4110 / 2100 旧物理文字中的角色 3 当前值关系或独立来源关系。

## 2. S0 与所有权

执行起点不得早于创建侧正式基线 `573e2ad550aba17a3153cbfae7e5cccdaba1e08d`。S0 必须确认：计划索引本版本为可执行；P2—P6 最新消费者计划已退出；P6 代码结果和消费者验收仍分别为 `dc312ef279cc6fb32556898aea8c9998df1140c4`、`e05480125053744d2eefaa2a9e3ba74c5ea04f51`；`main == origin/main`、index 空；实际接口、版本、状态和四参数依赖与详细设计一致；目标四文件没有异主 WIP；唯一活动代码段及构建、运行、模块扫描资源可用。

## 3. 精确允许范围

只允许修改或新增：

1. `海中鱼巣/领域/服务.L1实例特征.ixx`
2. `海中鱼巣/启动.运行期上下文.ixx`
3. `施工记录/20260807_L1-DATA-LAYER-COMPLETE-P7-EXISTENCE-I64-INSTANCE-FEATURE-CONSUMER_施工记录.md`
4. `验证记录/20260807_L1-DATA-LAYER-COMPLETE-P7-EXISTENCE-I64-INSTANCE-FEATURE-CONSUMER_构建与生产链验证.md`

禁止修改数据头、L1 核心 / 仓库 / 中性合同、其它领域服务、生产启动、工程、filters、自检、规范、详细设计、流程图、计划、计划索引或集成验收目录。禁止触碰或夹带 P0-FIX、路线、旧 WORLD-TREE、`.codex-build` 和其它 WIP。

## 4. 实施步骤

1. 把实例服务生产构造收敛为四引用，删除可空实际存在指针；在运行期上下文现有构造顺序中追加既有实际存在组合服务引用。
2. 为 `建立登记` 和 `建立实际存在I64实例特征` 增加 const 重载；非 const 重载只转发同一 const 实现；把锁和本叶定位改为 `mutable`，不复制上下文或提供者。
3. 增加中性写入 / 读取状态显式映射、映射唯一校验和中性节点 / 关系 / 属性 / 值读回辅助；不得使用裸枚举转换。
4. 重写登记建立与读取：提交 4 节点中性写集，只保存四编码定位，每次经首末事实代次和四节点重新形成登记。
5. 建立两份同锁非权威索引：幂等身份到组合 / 读取定位，组合到幂等身份 / 读取定位。索引不得保存领域事实或直接返回成功结果。
6. 重写实际存在实例首次路径：读取登记、P5 宿主、P6 定义和来源，校准共同截止与值域，形成 1 节点 + 2 关系 + 1 值 + 1 槽 + 0 退出中性写集并恰一次提交。
7. 重写重复 / 冲突路径：同身份请求始终重形成并提交完整中性写集，由 L1 裁决精确重复 / 同键异义；不同身份占用同一宿主定义组合时写前拒绝。
8. 重写实际存在实例正式读取：只用提供者公开读和中性事实代次 / 节点 / 关系 / 属性 / 值入口，删除完整快照唯一性检查，逐字段互证全部事实。
9. 删除本叶目标路径的领域意图、预探测、provider 见证、业务标签、legacy 提交、完整快照、首次请求 / 写集 / 结果事实缓存；不得改变初始场景实例和两个当前值换代入口的语义。
10. 写两份专属记录，记录实际变更、验证、未覆盖范围和精确提交；不得写第一层自证或整体完善。

正式 DTO、字段、唯一性所有权、四参数构造或中性状态语义发生会改变实现的漂移时停止并具名退回计划支撑；普通编译和局部代码错误在白名单内修复。

## 5. 验证合同

至少执行并记录：四路径 `git diff --check`、`python .\tools\check_specs.py --strict`、Debug / Release x64 串行 Rebuild、两配置正式生产入口、两配置模块依赖扫描和 staged 范围核对。

静态门禁：

- 登记、实际存在实例建立及其正式读取路径中的 `探测幂等`、legacy `提交写集`、`读取完整快照`、领域意图、执行证据、provider 见证和业务操作标签调用均为 0。
- `提交中性写集` 在本叶目标路径恰 2 个物理调用点：登记 4 节点；实例 1 节点 + 2 关系 + 1 值 + 1 槽；退出均为 0。
- 实例服务构造只剩四引用形态，实际存在依赖不是指针；运行期上下文只追加既有组合服务引用。
- 登记只保存四编码定位；实例索引只保存幂等身份、宿主定义组合和正式读取定位，不保存请求写集、首次领域事实或首次结果。
- 实例正式读回具备首末事实代次校准，并使用中性节点、关系、属性和值入口。
- 数据头、L1 核心 / 仓库 / 中性合同、工程和其它服务差异为 0；初始场景实例与当前值换代语义无改动。

构建、生产入口退出码和静态扫描只证明施工候选。代码发布后另做生产代码外合法消费者验收：从正式生产会话和有效租约取得 const 实例服务，复用初始场景实际存在与 P6 定义，依次执行登记首次 / 重复、越界拒绝、实例首次 / 重复、同键异义冲突、不同键同宿主同定义冲突；每个非成功前后都经正式公开入口比较最终权威状态，驱动不得导入或调用 L1，不解析日志。

## 6. 完成声明边界

施工完成最多证明实例特征领域的中性代码候选和生产接线门禁成立。消费者验收通过后，才可累计本消费者实际使用的节点 / 关系 / 值 / 槽创建与精确读取、两写集原子性、精确重复、同键异义、业务唯一性拒绝和非法请求零变化。

不得宣称当前值修改、退出 / 历史、关系条件查询、恢复、其它消费者、旧栈删除或整个 L1 完善；当前不覆盖崩溃和跨进程恢复。
