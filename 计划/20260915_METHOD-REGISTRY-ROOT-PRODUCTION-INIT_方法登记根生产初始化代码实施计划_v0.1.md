# 方法登记根生产初始化代码实施计划

身份：`METHOD-REGISTRY-ROOT-PRODUCTION-INIT`
版本：v0.2
起点：`8fbc11a1e2fbbdfa61d6b6b7c47afd0f245dae77`
状态：可执行；仅表示设计门禁已闭合，执行者仍须完成 S0。

## 1. 目标、前提与范围

按[详细设计](../规范/详细设计/20260915_METHOD-REGISTRY-ROOT-PRODUCTION-INIT_方法登记根生产初始化详细设计_v0.1.md)实现8120阶段19。目标是：真实自我成功后，普通应用同实例完成唯一、空结构的方法登记根建立或恢复读回；成功才到阶段21短路。

执行 S0 必须复核：8120第5节第7项、0050、3300/5350/5360、上述详细设计、当前 L1 首次材料和 owner API、普通应用装配/启动调用点、`main`/`origin/main`/index/dirty/占用表及知识库条目。任何更改根最小形状、owner、首次材料、公开 DTO、事务/生命周期、工程入口或白名单的漂移，停止并退回计划支撑；不得临场扩展。

允许文件恰为详细设计第6节的11个文件。禁止操作其余生产文件、正式规范、规则、计划/索引及其他人的WIP。

## 2. 实施步骤

1. 新建 L2 方法登记根头/源，实现固定技术键、值式 DTO、状态、参数合法性、单根/全组读取及按首次材料建立或收敛。只用 L1 与技术 owner 端口，物理写集严格只含一个普通节点。
2. 新建生产初始化 provider，完整映射 L2 的缺失、冲突、漂移、资源和发布未知；成功路径做最终单根与全组同截止读回。
3. 在普通应用候选上下文建立技术 owner、聚合和 provider，保持对象/端口析构顺序；公开 `初始化普通应用方法登记根()`，拒绝没有上下文或未成功自我的调用。
4. 接线启动入口：自我成功后调用阶段19；仅在 provider 成功时保留阶段21短路；任何非成功精确映射阶段19。
5. 仅更新仓库根 `海中鱼巣.vcxproj` 与 `海中鱼巣.vcxproj.filters`，登记四个新源/头；不改变其它工程配置。
6. 写入施工和验证记录，含函数级代码知识变更清单，列明新增函数、调用边、L1事实读写/副作用、未覆盖范围和待知识库维护项。

## 3. 验证矩阵

在 `D:\TEMP\海中鱼巣\METHOD-REGISTRY-ROOT-PRODUCTION-INIT\` 建立本计划独占临时验证根：

| 验证 | 期望 |
| --- | --- |
| 空隔离持久仓 | 形成 self 后阶段19建立一个普通无字段节点；首次材料映射和读回身份一致 |
| 第二次进程/重启 | 零新增根事实、`精确重复`、稳定编码相同 |
| 首次材料已有但节点缺失/退出/形状错 | 失败且零重建 |
| 无首次材料但技术 owner 非空 | `所有者非空冲突`且零写 |
| 代次、资源、发布未知可控桩/静态路径 | 原请求、原幂等身份读回；不换键、不猜编码 |
| `运行普通程序` | 阶段19成功后只到阶段21失败 |
| 构建 | 主工程 Debug x64 与 Release x64 隔离 Rebuild |
| 静态 | `git diff --check`、`git diff --cached --check`；工程登记及禁止项检索 |

不得运行自我线程、任务/需求、本能根、业务闭环、集成验收或以构建成功声称其通过。

## 4. 完成与发布

完成要求：白名单内实现完整、所有矩阵命令有真实记录、两配置构建通过、阶段19接线可达、无旧普通方法服务复用、无额外根事实、只暂存白名单。Git 发布遵循单工作区租约：先请求租约，再 fetch/复核、精确暂存、提交、普通推送、读回 `main == origin/main`；其它WIP不得带入。

发布结果只证明本计划范围；不证明方法知识、阶段21、本能根、线程、恢复、运行或业务闭环。
