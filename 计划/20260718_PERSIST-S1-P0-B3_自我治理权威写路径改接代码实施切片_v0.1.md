# PERSIST-S1-P0-B3 自我治理权威写路径改接代码实施切片 v0.6

日期：2026-07-18

执行队列：#301

设计队列：DQ-193

设计裁决：JY-391 / JY-420 / JY-421 / JY-425 / JY-441 / JY-492 / JY-493 / JY-494 / JY-495 / JY-496

计划阶段：940

状态：验证失败待设计修订已裁决 / WT-301-R1 保留三文件 WIP、index 空 / E240 Debug 首轮证明动作键与验收证据所有权漂移 / E241 后授权 S4R 三文件机械修订、两配置 Rebuild 与静态固定点 / 940 由后继 #309 登记

## 0. JY-493 新主线实际接口复核与执行身份

JY-493 已从包含 #310 / #305 及 JY-492 归档的 `main@719943f720e32f02febd3b475d91962a8576de20` 完成设计 S0：#310 的十一项直接业务合同均存在，运行期业务请求 / 结果仍为 14 / 11，`运行期上下文租约::读取()` 与 `读取候选业务操作()` 能提供同身份只读上下文和正式 B0 业务面；现有 B3C 自检已经通过该合同形成需求、任务、方法、动作、因果和读回闭环。当前 `自我治理领域路由` 仍持传统服务并直接写入，真实构造点精确位于本计划允许的两个自检模块；多轮自检中的 B1 桥已经使用同租约只读适配器，无需回改 #299。入口和回执自检只 import，不构造该路由；全仓 940 尚未登记。

已首次创建任务身份：

```text
工作树：C:\Users\zhchh\.codex\worktrees\301-p0b3-r1\海中鱼巢
分支：codex/301-p0b3-r1
冻结基线：719943f720e32f02febd3b475d91962a8576de20
状态：clean / index 空 / 无 upstream / 远端同名分支不存在
```

本次只登记“已创建待派发”。E238 推送后仅允许目标执行任务完成自身 S0 只读接管复核；在设计窗口读回 S0 并另行发布代码固定点授权前，禁止修改、构建、运行、自检、暂存、提交或推送。

### 0.1 JY-494 执行 S0 接管通过与代码固定点

目标执行任务已从冻结工作树只读确认：Git 身份一致；十一项合同逐签名存在；请求 / 结果为 14 / 11；正式业务面沿同身份租约取得；路由五个构造点全部位于两个允许自检模块；四类调用点分别为 13 / 13 / 10 / 8；多轮 B1 桥保持租约只读适配器；允许 / 禁止范围一致；正式 940 登记为零。工作树与 index 保持 clean，未构建、运行或写入。

E239 发布后允许执行 S1—S4，只修改三份既有代码文件，完成 Debug / Release x64 Rebuild、strict、diff、精确三文件、禁止依赖、传统写引用归零、构造点 / 调用点迁移、14 / 11 与 940 零登记静态检查。达到固定点后立即回告并保持 index 空；禁止运行任何 self-test、访问共享事件段目录、创建专属记录、暂存、提交或推送。

### 0.2 JY-495 代码构建静态固定点通过与共享验证

WT-301-R1 已在冻结基线完成三份允许代码的机械改接，差异精确为 462 insertions / 515 deletions；Debug / Release x64 Rebuild 均为 0 warning / 0 error，strict、diff、三文件范围、禁止依赖、传统写引用归零、五处同租约构造、四阶段 B0 调用、14 / 11 和 940 零登记均通过。生产路由采用同一文件内薄 PIMPL 隔离 MSVC IFC 私有依赖；两个自检模块仅在全局模块片段内对旧 `方法服务.h` 的两个历史夹具类型作局部宏别名并立即撤销，不修改服务实现、公开业务合同或运行行为。当前 index 空、产品进程为零，尚未运行任何 self-test，也未创建专属记录、暂存、提交或推送。

E240 发布后，WT-301-R1 才可独占 `D:\海中鱼巣\日志\事件段\自检`，按 Debug 完整自检、Release 完整自检、全新 Debug 严格 20 / 20 的顺序完成 S5；每轮完整 stdout / stderr 必须先落盘，程序自然结束、进程归零且轮间等待 2 秒。Debug 每个有效轮须至少机械确认 SELF-GOV-S4A / S4B / S4C / S4D 各 8 项、SELF-GOV-S5 12 项及既有 SERVICE-DATA-S5 38、TASK-RESULT-S1 18、PERSIST-S1-P0-A 18、PERSIST-S1-P0-B0 20、PERSIST-S1-P0-B3C 12，所有失败数为零；SELF-GOV、SERVICE、TASK、P0-A 按 `: 失败`，B0 / B3C 按 `=失败` 解析。Release 上述 Debug 专项输出均须为零。首个产品、解析、日志、目录、进程或计数异常立即停止并释放共享目录，不补跑、不续算。

只有全部验证通过，才可新建本计划唯一专属实施记录，重跑 strict、diff、精确四文件、禁止依赖、Release 隔离与 staged diff，精确提交三代码加记录到 `codex/301-p0b3-r1` 并非强制推送同名远端。任务完成状态只到“分支完成待独立集成”；禁止推 main、登记 940、修改入口 / 工程 / 运行器 / 中央治理或消费后继。

### 0.3 JY-496 E240 失败根因与 S4R 修订授权

E240 首轮 Debug 产品退出 1；SELF-GOV-S4A / S4B / S4C 各 8 项全部失败，S4D 仅 A06 通过，S5 仅 A01 / A05 / A08 通过；SERVICE-DATA-S5、TASK-RESULT-S1、PERSIST-S1-P0-A / B0 / B3C 仍分别 38 / 18 / 18 / 20 / 12 且失败 0。该结果不是解析器、日志或共享目录竞争，Release 和 20 / 20 未启动，共享目录已释放，进程为零。

只读追根因确认：

1. 路由以历史 `安全维护动作稳定键 = 0x0001'0002'0000'0001` 创建动作入口；同一运行期任务执行组合器只接受 `生产运行期配置.动作键 = 生产运行期稳定动作键 = 0x4859'0002'0000'0001`。冻结入口在稳定动作键比较处写前拒绝，动作、结算和观察后继随之不可达。
2. 两个自检模块启动新生产运行期后，仍用外部传统夹具的仓库、需求 / 任务 / 方法 / 特征 / 状态 / 动态 / 因果服务读取新运行期句柄并统计结构数量。新写入属于运行期上下文拥有的仓库，旧夹具不是权威读者，因而 S4A 以及后继大量验收稳定误报。

E241 发布后只授权 S4R，不授权运行 self-test、创建记录、暂存、提交或推送：

1. 在 `自我治理领域路由.ixx` 令 `安全维护动作稳定键` 直接同源于 `生产运行期稳定动作键`；不得保留第二个数值、修改生产配置或扩展 B0 / 上下文 ABI。
2. 在两个自检模块建立同租约证据面：系统角色来自运行期上下文；需求 / 任务 / 方法 / 因果来自同一 B0 四读；结构数量和关系只读检查来自同一运行期上下文仓库；治理 / 动作 / 结算 / 观察使用路由值式上下文交叉验证。
3. 删除所有“用旧服务读取或写入新运行期句柄”的验收调用。外部传统夹具只保留非权威统计、日志、显示及本模块其它历史切片所需职责，不得成为 S4A—S5 权威成功 / 失败来源。
4. 保留 S4A—S4D 各 8 项、S5 12 项编号和负例 / 重复 / 幂等 / 并发语义。S4B-A2 只把名称和表达式改为“任务实际状态由 1 形成 10”，禁止再宣称旧实例槽位被覆写；不得删项、常量真或用旧夹具零变化冒充通过。
5. 完成后只运行 Debug / Release x64 Rebuild、strict、diff、精确三文件、禁止文件、动作键同源、旧夹具跨所有权读写归零、B1 桥保持、14 / 11 和 940 零登记静态门禁，并回告固定点。self-test 与共享目录必须等待下一独立授权。

## 1. 目标与依据

依据恢复流程图 v0.3 的 JY-441 替换子图、消费者流程图 v0.7、详细设计第 22 节和 #295 事实矩阵。本切片必须从已包含 #310 / #305 的新 `main` 串行执行；只把 `自我治理领域路由` 的权威机器写入按治理、动作、结算、观察四阶段改为同一运行期租约上的 B0 正式业务面，并同步闭环 / 多轮自检中的路由构造调用。非权威统计、日志和显示仍只作人读材料，不成为写入结果或重试依据。

来源性质为 MIG-18 重新实现 / 新增约束，不计旧能力迁移；不接 O1 生产、统计、学习或自动晋级。

## 2. 依赖与假定接口

前置 #310 已在既有 13 类线程路由之外提供 11 个 B3 直接强类型入口；#305 已形成四仓库全状态导出但不被本路由直接消费；#299 已把任务管理上行桥和多轮自检中的桥构造调用迁到 B1 合同。#302 尚未实施且不是本切片前置。当前路由仍直接组合传统领域服务并写机器事实。目标构造只接收同身份 `运行期上下文租约`，每次从租约取得正式业务面，不再持传统仓库或业务服务写引用。

若 #310 任一直接合同缺失或不能无损承载，必须逐签名退回 #310 设计修订；若必须回改 #299 生产查询语义才能完成，退回 B1 责任边界。不得在本路由中绕过组合器、恢复原始事务能力、复制旧实例特征直接写链或把多轮自检中的桥构造回退到传统服务。

## 3. 允许文件

```text
海中鱼巣/线程/自我治理领域路由.ixx
海中鱼巣/线程/自检.自我治理闭环.ixx                     既有自检定向扩展
海中鱼巣/线程/自检.自我治理多轮.ixx                     既有自检定向扩展
实施记录/20260718_PERSIST-S1-P0-B3_自我治理权威写路径改接代码实施_Codex断点清单.md 新建
```

本切片不新建代码文件。既有两个自检真模块已经登记，只允许在原职责内增加同租约 B0 写入、无租约零写、无旧服务兜底和多轮收口验证。

## 4. 禁止范围

禁止 B0 共享路由、B1 / B2 生产文件、入口、工程、统一运行器、旧概念图服务、领域 / 核心仓库、SQL / 面板、O1、快照和恢复。禁止双写、旧服务兜底、从日志 / 统计判断成功、把线程当动作来源或为测试暴露可写内部状态。允许的多轮自检不得撤销 #299 已形成的桥租约值式查询合同。

## 5. 机械阶段

| 阶段 | 前置事实 | 待实施目标 | 实施证据 | 退回条件 |
| --- | --- | --- | --- | --- |
| S0 | #310 / #305 已集成的新 main、11 个 B3 直接合同与路由全部写调用可读 | 逐阶段映射权威写与非权威材料，并复核多轮自检中 B1 构造已经迁移 | 每个当前逻辑阶段有唯一 B0 替代，B1 调用点无需回改 | 缺失合同需修改禁止文件或必须回改 #299 生产语义 |
| S1 | 有效租约和 B0 门面 | 改构造与成员所有权 | 路由零传统仓库 / 服务写引用 | 必须持原始事务能力才能完成 |
| S2 | S1 完成 | 治理阶段改为需求读回、已有需求承接任务、`已承接 / 筹办中`、方法首 / 结果 / 动作和唯一选择 | 每项只调用 #310 或既有 B0 正式入口，无双写 / 兜底 | 前置通过后正式结果与完整读回矛盾 |
| S3 | S2 完成 | 动作 / 结算改为冻结、排队、执行、轻量因果和完成任务并独立结算 | 动态来源动作、实际状态、因果、任务完成和需求结算值式互证 | 需要旧实例特征直接写、手工动态 / 因果或传统结算兜底 |
| S4 | 权威写已改接 | 保留非权威统计 / 日志但切断裁决作用；定向扩展闭环与多轮自检 | 有效、拒绝、幂等、内部不一致均具名；B1 查询合同保持 | 同身份租约内出现第二写所有者或需要恢复旧桥构造 |
| S4R | E240 已形成具名失败证据 | 修正动作键同源和自检证据所有权 | 稳定动作键与生产配置一致；S4A—S5 权威证据全部来自同一租约 | 需要修改 B0 / 上下文 / 装配或无法在允许三文件内消除跨所有权读写 |
| S5 | 自检通过 | 回归和连续验证 | A01—A17 全通过 | 计划外具名失败或零具名退出 1 |
| S6 | 任务验证通过 | 提交同名任务分支 | 只到分支完成待集成；940 不登记 | 需要修改共享文件或其它并行任务所有权 |

## 6. 非成功返回

无租约、请求 / 句柄 / 类型 / 状态前置不满足为逻辑内返回，零写且不回退旧服务。正式业务面已接收有效请求后提交、完整读回或结算互证不一致为追根因。B0 实际签名缺失且需计划外修改为接口漂移。

## 7. 验收项

| 编号 | 验收项 |
| --- | --- |
| P0-B3-A01 | #310 的 11 个直接合同与四阶段逐调用点映射一致 |
| P0-B3-A02 | 路由只持同身份租约 / 正式业务门面，不持仓库、令牌、许可、会话 |
| P0-B3-A03 | 全部权威写只走一个 B0 正式入口，无旧服务兜底或双写 |
| P0-B3-A04 | 任务授权、已有需求承接、生命周期、方法选择和动作来源边界由新运行期正式合同承载 |
| P0-B3-A05 | 日志、显示、统计和消息不裁决机器结果 |
| P0-B3-A06 | 线程仍不是动作来源，动态只来自任务执行组合器，因果只由该正式动态创建 |
| P0-B3-A07 | 无租约与无效请求写前拒绝且零结构变化 |
| P0-B3-A08 | 正式成功后按完整值式材料互证，不用 bool 代替业务事实 |
| P0-B3-A09 | B1 / B2、入口、O1、SQL / 面板、快照恢复零差异 |
| P0-B3-A10 | 生产不依赖自检，入口无场景正文增量 |
| P0-B3-A11 | 本任务不登记 940、不修改入口 / 工程 / 运行器；阶段登记后置 #309 |
| P0-B3-A12 | SELF-GOV 与任务 / 方法 / 动作 / 结算既有回归通过 |
| P0-B3-A13 | Debug / Release Rebuild 与两配置完整自检通过 |
| P0-B3-A14 | 最终 Debug 20 / 20、strict、diff 和范围通过 |
| P0-B3-A15 | 内部矛盾具名归因，零具名退出 1 正式退回 |
| P0-B3-A16 | 实施记录执行完成待设计同步，中央治理文件零修改 |
| P0-B3-A17 | 从包含 #310 / #305 的新 main 串行冻结；多轮自检保留 B1 桥构造，只迁移 B3 路由构造和权威写验证 |
| P0-B3-A18 | 安全维护动作键与生产运行期配置动作键同源，任务执行冻结不得因支持键分叉而拒绝 |
| P0-B3-A19 | S4A—S5 不用外部旧夹具读写新运行期句柄；结构计数、四类读回和路由上下文来自同一租约 |
| P0-B3-A20 | S4B-A2 验证任务实际状态与动态，不宣称直接覆写旧实例特征槽位 |

## 8. 验证

```powershell
msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m
.\x64\Debug\海中鱼巣.exe --self-test-exit
msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Release /p:Platform=x64 /m
.\x64\Release\海中鱼巣.exe --self-test-exit
1..20 | ForEach-Object { .\x64\Debug\海中鱼巣.exe --self-test-exit }
python .\tools\check_specs.py --strict
git diff --check
git diff --name-only
```

## 9. 完成边界

只证明自我治理权威写路径已改接同一运行期业务面；不证明默认入口、#214、O1、快照、恢复、自我循环、自我苏醒或旧能力迁移完成。
