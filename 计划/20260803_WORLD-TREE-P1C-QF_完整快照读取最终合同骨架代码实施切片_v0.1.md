# WORLD-TREE-P1C-QF 完整快照读取最终合同骨架代码实施切片 v0.1

计划身份：WORLD-TREE-P1C-QF

创建侧当前代码复核基线：`1cd635d9ac0eeab702bc1015d43416cd88f73863`

直接代码依据：`规范/详细设计/20260803_WORLD-TREE-P1C-QF_完整快照读取最终合同骨架详细设计_v0.1.md`。

## 1. 目标、开工门禁与完成条件

建立 `世界树快照读取服务` 的最终公开 DTO、三引用构造和三个固定未实现读取函数体。任意输入均返回 `世界树业务状态::未实现(11)`、默认请求回执、空快照和空缺项；参数读取数、provider调用数和机器事实变化均为0。

具名依赖：

- WORLD-TREE-P1A3F v0.3 结果提交 `3242c16640da898b546f294199c00c08032ba49c` 已满足共享事实头与世界结构最终类型。
- WORLD-TREE-P1BF v0.2 最终合同骨架代码尚未进入正式 main。
- WORLD-TREE-P1B-F v0.2 最终合同骨架代码尚未进入正式 main；设计提交 `c56376a690c492ca380bb15b1a8c54ee94ac0986` 只冻结接口和四个类型合同身份，不替代代码结果。

后两项未满足时，S0 必须零代码修改并返回 `DRIFT-WORLD-TREE-P1C-QF-PROVIDER`。进入正式 main 的安全 `未实现` 骨架足以解除类型和构造门禁，不要求提供者先具备真实成功查询行为。本计划不消费 FEATURE-W，不得为了自检顺序连续性增加该依赖。

完成只要求最终 ABI 骨架编译接入、正式自检入口真实调用三根并验证安全拒绝；不要求也不允许真实快照读取。

## 2. 冻结设计与施工图

```text
业务流程图：流程图/20260803_WORLD-TREE-P1C-QF_完整快照读取最终合同骨架业务流程图_v0.1.md
整树根：流程图/20260803_世界树快照读取服务读取完整世界事实快照_函数流程图_v0.2.md
存在根：流程图/20260803_世界树快照读取服务读取存在完整事实_函数流程图_v0.2.md
场景根：流程图/20260803_世界树快照读取服务读取场景完整事实_函数流程图_v0.2.md
函数图谱：规范/详细设计/函数结构知识图谱/20260803_WORLD-TREE-P1C-QF_完整快照读取最终合同骨架函数结构知识图谱_v0.1.md
```

执行者不得从旧 P1C v0.1 图或旧总详细设计补造函数体。字段、签名、构造、返回表达式、状态值和自检身份只按本计划冻结的详细设计施工。

## 3. 允许文件

```text
海中鱼巣/领域/服务.世界树快照.ixx
海中鱼巣/领域/自检.世界树快照合同骨架.ixx
海中鱼巣/启动.应用程序.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
施工记录/20260803_WORLD-TREE-P1C-QF_施工记录.md
验证记录/20260803_WORLD-TREE-P1C-QF_验证记录.md
```

允许修改 `启动.应用程序.ixx`、工程和 filters 的唯一语义是登记上述一个生产模块、一个自检模块和一个顶层自检项。S0 必须确认这些共享文件已由依赖计划释放。

只读依赖：

```text
海中鱼巣/领域/世界树事实.数据.h
海中鱼巣/领域/服务.节点直接世界结构.ixx
海中鱼巣/领域/服务.节点直接特征查询.ixx
海中鱼巣/领域/状态动态合同.数据.h
海中鱼巣/领域/服务.节点直接状态动态查询.ixx
```

## 4. 禁止范围

- 不修改规范、设计、流程图、计划索引或任何 provider 文件。
- 不建立 `世界树业务服务`、写入口、比较入口、SQL/材料入口、运行期装配或路由。
- 不调用世界结构、特征、状态动态 provider，不读取/回显请求，不取得许可、事务、仓库、缓存、时钟或日志。
- 不把未实现映射为已读取、未找到、空成功、资源失败或旧栈回退。
- 不新增重载、helper、兼容壳、默认 provider、第二结果状态或第二顺序240自检。
- 不修改 `世界树事实.数据.h`；P1BF/P1B-F共享事实必须在各自结果中先行形成。

## 5. S0

执行前逐项记录：

1. HEAD/main/origin一致，计划索引精确登记本计划版本/blob且状态可被选择；P1BF与P1B-F结果提交已经满足具名依赖。
2. 五个只读依赖文件均在正式 HEAD，实际类型、构造和完整签名与详细设计 §7.1 一致；不存在仅WIP或名称预留。
3. 允许文件没有其它所有者活动 WIP，Git index和发布租约可用。
4. 当前顶层自检登记数量记为 `N`；顺序240未被其它并列身份占用。本计划只增加一个 S01，目标数量 `N+1`。顺序230缺失不阻断。
5. 工程中不存在 `海中鱼巣.领域.服务.世界树快照`、同名DTO、同名服务或兼容旧入口。

任一失败时零代码修改，记录精确 HEAD、文件、接口、依赖和所有权后收口；不得临场修改提供者或扩大白名单。

## 6. 机械施工步骤

1. 新建 `服务.世界树快照.ixx`，逐字落实详细设计 §2 的 include/import 顺序。
2. 按 §3 顺序声明 `世界树业务状态`、两请求、三快照和三结果；不得把任一实体移入共享事实头。
3. 按 §4 建立服务类和三引用构造；参数、初始化列表、成员顺序固定为世界结构、特征、状态动态。
4. 按 §5 逐字实现三个无名参数函数体和三个固定结果 `noexcept` 静态断言。不得添加判断、调用、日志或请求回显。
5. 新建专项自检模块，唯一导出 `运行世界树完整快照读取合同骨架自检()`。固定构造链为五个权威仓与事务域 -> `节点直接结构查询服务` -> 世界结构/特征/状态动态查询服务 -> 本服务；不得为此建立执行器、提交服务、SQL端口或持久端口。
6. 对三个根分别传入默认、非法、字段完整和边界字段请求；边界字段按详细设计 §7.2 把各整数置于其类型最大值并保持根/目标类型与入口一致。断言所有输出逐字段固定；用 Clang AST 证明三根参数引用、直接调用、成员调用、回调和未解析调用均为0。
7. 自检前后导出并比较节点、关系、索引、类型合同、类型化值五仓权威材料和事务域事实截止代次；全部逐值相等。
8. 在启动聚合中按顺序240登记唯一 `WORLD-TREE-P1C-QF-S01 / 世界树完整快照读取最终合同骨架`，顶层登记/执行数量从S0的N精确变为N+1。
9. 登记两个模块到工程和filters，形成专属施工/验证记录。

## 7. 失败收口与验证矩阵

| 编号 | 场景 | 必须结果 |
| --- | --- | --- |
| V01 | 三根默认/非法/完整/边界字段请求 | 固定未实现11；默认请求回执；空快照/缺项 |
| V02 | 三根 AST 调用边 | 直接、成员、回调和未解析调用均0 |
| V03 | 参数观察器/AST | 三根参数引用计数0；定义中参数无名称 |
| V04 | DTO/枚举ABI | 状态1—11、字段顺序和类型静态断言通过 |
| V05 | 构造 | 三引用正例；默认、缺参、指针、旧签名反例 |
| V06 | 机器事实 | 调用前后五仓权威导出与事务域事实截止代次逐值相等 |
| V07 | 外部边界 | SQL开关、材料、旧栈、日志和时钟不改变结果；调用边0 |
| V08 | 消费者门禁 | 未实现11后已读取成功后继调用数0 |
| V09 | 自检聚合 | 仅S01/240一项；登记与执行均N+1；无FEATURE-W依赖 |
| V10 | 静态唯一性 | 三DTO组、状态、服务类和三根每项恰一正式定义 |

验证命令：

```powershell
git diff --check
python .\tools\check_specs.py --strict
msbuild .\海中鱼巣.vcxproj /m /p:Configuration=Debug /p:Platform=x64
msbuild .\海中鱼巣.vcxproj /m /p:Configuration=Release /p:Platform=x64
.\x64\Debug\海中鱼巣.exe --self-test-exit
rg -n "世界树业务状态|世界树读取请求|世界树对象读取请求|完整世界事实快照|世界树存在完整事实快照|世界树场景完整事实快照|世界树读取结果|世界树存在读取结果|世界树场景读取结果|class 世界树快照读取服务" 海中鱼巣
rg -n "读取完整世界事实快照|读取存在完整事实|读取场景完整事实|WORLD-TREE-P1C-QF-S01" 海中鱼巣
```

第一条唯一性扫描按详细设计实体逐项人工/脚本断言恰一；第二条断言三根定义各一、S01登记一。若 AST 显示任一项目内调用或参数读取，保留现场并返回实现错误；不得放宽骨架合同。

## 8. 完成声明边界

完成只可声明“WORLD-TREE-P1C-QF 最终合同骨架代码候选通过本计划验证”。编译接入或入口巡检不证明世界结构、特征、状态动态或快照读取成功，不证明同代次组合、统一门面、运行期消费者、SQL恢复或服务验收已经完成。
