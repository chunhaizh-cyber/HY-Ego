# LEGACY-DATA-L2-PLUS-RETIREMENT 旧基础数据服务 L2 以上迁移删除代码实施计划 v0.1

日期：2026-09-06

状态：正式代码实施计划；当前执行状态只由 `计划/计划索引.md` 登记的精确 blob 决定

计划身份：`LEGACY-DATA-L2-PLUS-RETIREMENT`

事实基线：`main == origin/main == 2fd774253d3d145ebd745f04852c50e4eb814a74`

## 1. 正式输入与唯一结果

- 详细设计：`规范/详细设计/20260906_LEGACY-DATA-L2-PLUS-RETIREMENT_旧基础数据服务L2以上迁移删除详细设计_v0.1.md`；
- 精确删除清单：`计划/20260906_LEGACY-DATA-L2-PLUS-RETIREMENT_旧基础数据服务L2以上精确删除清单_v0.1.md`；
- 逻辑边界：0050 及当前目录登记的 L1 正式规范；
- 协作边界：AGENTS、单工作区串行执行与发布规则、设计执行双窗口交互规则、层级命名空间规则。

本计划只交付一个完整代码结果：旧基础数据服务 L2 以上实现及引用闭包退出，实际 L1 和新链保留。没有函数 ABI、DTO、owner 或业务事实需要实现。

## 2. 允许范围

### 2.1 整文件删除

精确删除清单中的 119 个物理删除路径和 1 个旧版本 index 删除路径；路径和 source blob 都必须相符。其构成为：

| 类别 | 数量 |
| --- | ---: |
| 旧生产实现与引用代码物理删除 | 113（65 ixx + 45 h + 3 inl） |
| 验证源、工程与运行脚本级联 | 6（2 cpp + 2 vcxproj + 2 ps1） |
| 物理删除小计 | 119 |
| 旧 HEAD 特征概念版本仅从 index 退出 | 1（工作树重写版保留） |
| staged 删除合计 | 120 |

### 2.2 修改与新增

```text
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
施工记录/20260906_LEGACY-DATA-L2-PLUS-RETIREMENT_旧基础数据服务L2以上迁移删除施工记录_v0.1.md
```

工程工作树只允许：

1. 工作树移除 113 个物理删除生产文件的主工程登记和筛选器项；
2. 原样保留交接 WIP 中 `应用服务.特征概念类.ixx`、`应用服务.存在概念树类.ixx`、`应用服务.场景成员概念类.ixx` 的三条 `ClCompile` 及三个筛选器项。

工程工作树 WIP 冻结值：

| 文件 | 工作树 blob | SHA-256 |
| --- | --- | --- |
| `海中鱼巣.vcxproj` | `88880fd1910a148aea830f4e0d32f77c00e336c2` | `B7530C5D705F191928A896ED8C13DA2A3A317C7BC78D0D5F252CDF22C7A7B549` |
| `海中鱼巣.vcxproj.filters` | `45fdbc05263b610362c21d294fa3d492f838f909` | `0682CF19504D6144BB7E2903E7708D04FC9B940315BCFA2FE92AF822F2D8B8F3` |

工程两文件禁止整文件 `git add`。index/提交必须相对 HEAD 删除 114 个登记：113 个物理删除生产文件加旧 HEAD 特征概念登记；不得包含上述六个外部 XML 项。工作树只删除 113 个旧登记并保留六个外部 XML 项。执行者使用可审计的 `git apply --cached`，或以 `hash-object`/`update-index` 精确构造 cached 删除投影，并在提交前分别读回 cached 与工作树两种视图。

## 3. 禁止范围

- 不物理修改或删除清单附录中的 28 个 L1/新链保护路径；唯一版本例外是旧 HEAD 特征概念 blob 从 index 退出，工作树重写版逐字保留；
- 不暂存外部安全暂停的六个源码、两份施工/验证记录或其验证资源；
- 不删除当前不依赖旧闭包的主入口、启动壳、数据库、持久事实或稳定编码；
- 不删除或批量改写 Markdown 规范、知识、流程图、设计、计划、记录、历史证据；
- 不创建替代 DTO、兼容模块、重定向 import、空壳、旧数据 reader/writer、迁移服务或新入口；
- 不使用 reset、clean、stash、merge、rebase、cherry-pick、force 或覆盖外部 WIP；
- 不执行编译、运行、测试、专项、扫描、代码差异检查、验收，不创建验证记录。

## 4. S0

1. 重读计划索引，确认本计划精确 blob 为 `可执行`；
2. 核对分支、HEAD/origin、ahead/behind、index、dirty、全部活动登记和 Git 发布租约；允许 HEAD 只含本设计、计划、清单及其索引登记的文档发布提交，120 个旧 source blob 必须保持；
3. 逐项核对 119 个物理删除 source blob 和 1 个旧版本 index 删除 source blob，不允许“最近同名文件”替代；
4. 核对 28 个保护路径的工作树 blob，以及保护闭包与 119 个物理删除路径零交集；旧 HEAD 特征概念版本退出是已冻结的唯一同路径版本例外；
5. 核对工程/filters 的交接 blob、SHA-256 和六个新链 XML 项；
6. 确认只有本计划接管工程两文件，外部六源码两记录继续安全暂停；
7. 任一 blob、引用闭包、保护交集或所有权变化都停止，具名退回计划支撑更新清单，不自行扩缩。

## 5. 实施顺序

1. 按清单一次性物理删除 113 个旧生产文件和 6 个验证工具文件；仅把旧 HEAD 特征概念 blob 从 index 删除，工作树同路径重写版保持；
2. 在冻结的工作树工程文件上精确删除 113 个旧登记；保留六个交接新链 XML 项；另在 cached 工程投影删除旧 HEAD 特征概念登记，共 114 项；
3. 核对工程 XML 结构和删除项/保留项计数，不做构建、扫描或代码差异检查；
4. 形成施工记录，写清实际清单、工程交接、保护集、Git 边界和未验证范围；
5. 申请唯一 Git 发布租约；租约前不暂存、不提交、不推送；
6. 精确暂存 120 删除、2 修改、1 新记录，共 123 个路径；工程两文件只能暂存相对 HEAD 的 114 项退出投影，禁止整文件 `git add`；
7. 人工审计 staged name-status、cached 内容和未暂存外部 WIP；任何越界先精确移出受影响暂存并修正，不发布部分结果；
8. 普通提交并普通推送；读取 HEAD/origin 和 ahead/behind，确认结果后释放文件登记与 Git 租约；
9. 通知外部新链所有者按新 HEAD 重新扫描并接回工程两文件。

## 6. 非验证发布边界核对

| 编号 | 核对项 | 唯一通过条件 |
| --- | --- | --- |
| B01 | 删除集合 | staged 恰有 120 个 `D`；119 个路径物理删除，旧 HEAD 特征概念版本仅在 index 删除，路径/blob 与清单一致 |
| B02 | 工程双视图 | staged 恰有两个工程 `M`，cached 为 HEAD 减 114 项且无外部新增；工作树为交接 WIP 减 113 项并保留六个交接 XML 项 |
| B03 | 记录 | staged 恰有一个本计划施工记录 `A` |
| B04 | 保护集 | 除旧 HEAD 特征概念 blob 以 `D` 退出外，28 个保护源码没有 `A`/`M` 或其它 `D`；特征概念工作树重写版和外部两记录均未进入 index |
| B05 | 总清单 | staged 恰为 123 路径，无其它所有者 WIP |
| B06 | 内容边界 | 人工读回 cached 删除投影与施工记录，无越界内容；不运行代码差异检查，不把本项宣称为代码验证 |
| B07 | 发布 | 普通提交、普通推送后 HEAD==origin/main，ahead/behind 0/0 |

用户已明确本代码任务不需要验证。B01—B07 是 Git 范围与发布一致性核对，不是编译、运行、测试、静态扫描或验收。严禁用历史验证记录宣称删除后的代码有效。

## 7. 失败与恢复

- 清单目标缺失或 blob 变化：不猜测，停止并退回计划支撑；
- 新消费者出现：不临场扩大清单，重新计算闭包并发布新清单；
- 保护路径进入 119 个物理删除集合：停止，先由设计裁决真实依赖；已冻结的旧 HEAD 特征概念版本 index 退出不触发此条件；
- 工程交接 blob 变化：保留现场，重新协调所有权，禁止覆盖；
- 暂存包含外部 WIP：不提交，精确移出 index 后重新审计；
- 提交时只完成部分删除：不发布，继续补齐同一提交单元；
- 推送异常或超时：先读取远端与本地提交关系，结果未知时不得重复提交；
- 发布后恢复诉求：另立经用户授权的恢复迁移，不在本计划自动补偿。

## 8. 完成声明

只有 123 路径的单一提交已普通推送、远端一致且登记释放，才能声明：

> `LEGACY-DATA-L2-PLUS-RETIREMENT` 候选删除源码及工程登记已发布；旧 L2 以上代码闭包按清单物理退出；代码未编译、未运行、未测试、未扫描、未验收，持久数据未清理。

不得声明新链已接替旧业务、主程序可运行、历史数据已迁移、知识库已更新或业务闭环已完成。
