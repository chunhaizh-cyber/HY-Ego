# FS-06 方法服务角色与条件结果读取 S1-S4 代码实施切片 v0.1

日期：2026-07-07
状态：已确认 / 代码实施门禁已打开 / 未改代码 / 未迁移旧函数

## 1. 依据

```text
计划/20260707_FS06_方法服务角色与条件结果读取增强专项_v0.1.md
实施记录/20260707_FS06_方法服务角色与条件结果读取S0当前代码事实扫描_Codex断点清单.md
规范/方法系统规范.md
规范/详细设计/方法系统详细设计.md
规范/动作入口规范.md
规范/详细设计/动作入口详细设计.md
规范/领域服务授权写入规范.md
```

S0 已确认：`动作入口服务.h` 当前不存在，动作入口入口仍在 `方法服务.h`；方法角色、条件 / 结果节点、条件 / 结果场景和方法虚拟存在已有第一轮入口。

## 2. 确认范围

本切片确认后，只允许执行 FS-06 S1-S4 第一轮代码实施：

```text
S1：方法身份和角色读取复核。
S2：方法条件 / 结果节点和场景读取复核。
S3：方法虚拟存在读取复核。
S4：拒绝路径和越权依赖复核。
```

条件结果对、候选召回、来源 / 父 / 前置 / 后续方法关系和动作动态不进入本轮。

## 3. 允许文件

```text
海中鱼巣/领域/方法服务.h
海中鱼巣/入口.cpp
项目记忆/*
实施记录/20260707_FS06_方法服务角色与条件结果读取S1-S4代码实施_Codex断点清单.md
```

本切片不允许新建 `海中鱼巣/领域/动作入口服务.h`。如确需拆分动作入口服务，必须另建专项。

## 4. 禁止文件和禁止范围

```text
禁止修改旧项目源码。
禁止修改核心仓库、工程文件、SQL / 控制面板 / D455 / 体素 / 外设文件。
禁止新增旧方法主信息字段。
禁止迁移旧函数体。
禁止实现方法候选召回、结果能力索引、来源任务、父方法、前置 / 后续方法绑定、方法调用动作动态、条件结果对归并、运行统计、练习样本缓存和本能方法兼容入口。
```

## 5. 实施要求

```text
方法节点身份必须经方法服务复核节点类型。
方法首、方法条件、方法结果角色必须通过角色状态和关系读取。
方法条件 / 结果节点必须能追溯有效方法首。
条件 / 结果场景读取遇到无效节点、错误角色、多目标或目标类型错误时返回空。
方法虚拟存在必须唯一可读。
拒绝路径必须保证节点、关系、状态和场景结构不变化。
```

## 6. 验证方式

```powershell
msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m
.\x64\Debug\海中鱼巣.exe
git diff --check
rg -n -F "特征值服务" 海中鱼巣/领域/方法服务.h 海中鱼巣/领域/任务服务.h 海中鱼巣/领域/需求服务.h
```

默认入口至少输出或覆盖：

```text
方法身份复核通过。
错误角色读取拒绝。
条件 / 结果节点集合读取通过。
条件 / 结果场景读取通过。
多虚拟存在拒绝。
拒绝后结构数量不变。
高级服务未直接依赖特征值服务。
```

## 7. 完成声明边界

完成后只能声明 FS-06 第一轮方法身份、角色、条件 / 结果节点、场景和虚拟存在读取边界已实现并验证。不得声明完整方法查找、方法候选、条件结果对、方法学习、动作执行系统或旧方法能力迁移完成。

## 8. 待确认问题

```text
FS06-IMP-Q1：已确认。可新增公开只读方法身份复核入口。
FS06-IMP-Q2：已确认。本轮明确禁止新建 `动作入口服务.h`，动作入口拆分推迟到 FS-07 或独立专项。
```
