# INSTANCE-FEATURE-IF-R-CONCEPT-FOUNDATION 代码实施计划 v0.7

状态：可执行。基线：`3ba3f371f3ac054280dd3ea29707886868006d35`。执行前重新 S0。

## 目标与依据

按 4115 v0.7、7150 §2、7160 §5 实现 `IF(E,FT)`、R集合版本和 I64 精确归组；不改当前采用，也不进行 R→FCr 自动或同步抽象。完整 ABI、事务、失败和装配合同见同名详细设计 v0.7，执行者不得临场补设计。

## 允许范围

- `海中鱼巣/领域/数据服务.存在类.h`
- `海中鱼巣/业务/应用服务.特征概念类.h`
- `海中鱼巣/业务/应用服务.特征概念类.cpp`
- `海中鱼巣/装配.普通应用.cpp`
- 本计划专属施工记录

`数据服务.特征类.h/.cpp` 是零改动：只调用已发布的 R 归组/代表读取 ABI。概念树、L1、场景、特征值、原子 I64 出生提供者、工程登记、场景成员概念类、线程、启动、阶段21及正式规范禁止修改。

## 冻结实施顺序

1. 在存在类实现详细设计 §2 的 `读取实例特征IFR` 与 `提交实例特征IFR目标结构`，含完整 DTO、预算、预期快照、目标组、幂等、原子版本交换和完整读回。
2. 在特征概念应用实现详细设计 §3 的 `处理实例特征R观察`、请求/结果、结构异常诊断注入和 I64 调用链；非 I64 返回`比较未启用`且零写。
3. 在普通应用按详细设计 §4 注入存在类和私有诊断回调；不预建 IF/R、不调用观察入口。

读取失败或半结构零业务写；写前快照漂移或冲突零本次 IF/R 写；写后未知按原键读回且不回滚。R 变化只发布/读回 IF/R，绝不调用 R→FCr、创建 FCr、写概念关系或保存汇聚回执。

## 完成与验证边界

用户已取消构建、运行、专项驱动和行为夹具验证；不得建立替代验证。仅运行 `git diff --check`、`git diff --cached --check` 并读回索引/链接。通过不证明代码可编译、运行、恢复、非 I64、弹窗可见性或阶段21。
