# INSTANCE-FEATURE-IFR-L2-LAYOUT-FOUNDATION 代码实施计划 v0.1

状态：可执行。基线：`d215282e`。

## 目标

将存在 owner 的 IF/R 元结构从 v1 六关系布局升级为 v2：保留六条关系类型的方向与职责，新增唯一的 `R项材料属性类型`（`节点种类::属性类型`、`U64组` 表示）。新布局使用独立的元结构幂等身份，避免把已有 v1 首次材料改义或覆盖。普通装配只登记、读回并向存在服务注入 v2 交付；不建立任何 IF/R 业务节点、关系、值或属性槽。

## 范围

允许修改：`海中鱼巣/领域/数据服务.存在类.h`、`海中鱼巣/装配.普通应用.cpp`，以及本计划专属施工、验证记录。

禁止：新增 `读取IFR结构` 或 `提交IFR目标结构`，修改当前采用、特征规则、概念 owner、L1 ABI、定位特征、线程、启动或阶段21；不得退出、复用或重写旧 v1 元结构节点。

## 冻结实现与验证

- `实例特征结构交付` 固定为版本2，字段为六关系类型与 `R项材料属性类型`；构造校验分别验证六普通关系类型和一属性类型。
- 登记请求固定为版本2、幂等身份 `0x4946525354525632`；首次写集按局部键1..6建立普通关系类型节点，键7建立带 `U64组` 表示的属性类型节点。重放必须逐字节匹配该写集；读回逐项验证节点种类、表示、owner、创建代次和未退出。
- 装配只使用 v2 请求和交付。启动期间不得调用新业务入口。
- 验证至少包括 `git diff --check`、`python .\tools\check_specs.py --strict`、目标翻译单元 Debug/Release 编译，以及专属静态探针验证请求键、7节点写集和无业务 IF/R 写入。完整应用启动、旧持久 v1 自动迁移、跨进程恢复均为 `NOT_RUN`。

## 后继

完成后，重新 S0 原 `INSTANCE-FEATURE-IF-R-CONCEPT-FOUNDATION`：该计划才可实现 IF/R 投影、目标提交和 L3 编排。本片不证明这些能力已完成。