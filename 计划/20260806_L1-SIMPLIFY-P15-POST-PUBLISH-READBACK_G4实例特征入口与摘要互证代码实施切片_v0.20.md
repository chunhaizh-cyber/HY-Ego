# L1-SIMPLIFY-P15 G4 实例特征入口与摘要互证代码实施切片 v0.20

计划身份：`L1-SIMPLIFY-P15-POST-PUBLISH-READBACK`

版本：`v0.20`

创建侧基线：`9c09dd4ea186d38f3e2d77ecbb2915e4bf82329d`

替代：P15 v0.19；具名漂移：`DESIGN-DRIFT-P15-V019-G4-INSTANCE-PREPROBE-INTENT-DTO-TAG-MATRIX-MISMATCH`

## 1. 目标与授权

本计划是可执行的 P15 替代计划。正式 S0 通过后，允许在 24 项代码/模块/自检白名单及以下两条专属记录路径内施工：`施工记录/20260806_L1-SIMPLIFY-P15-POST-PUBLISH-READBACK_施工记录.md`、`验证记录/20260806_L1-SIMPLIFY-P15-POST-PUBLISH-READBACK_验证记录.md`。完整范围为 26 文件，除两条记录外不得新增文件。不得触碰 P67、P0-FIX、路线、旧 WORLD-TREE、P15 白名单外文件或其它异主 WIP。

目标是纠正 G4/F01/F02/F03 的真实 DTO、物理标签、共享标签变体、首次结果和恢复矩阵；保留 G1—G3、G5—G8、G0 独立互证、P-L1/P-I/P-S、32/18/50、16/50 及 v0.19 记录范围。

## 2. 唯一施工矩阵

```text
F01 建立登记(const 实例特征登记请求&) -> 0x15000402 / 变体1 -> 实例特征登记结果
F02 建立初始场景I64实例特征(const 初始场景I64实例特征建立请求&) -> 0x15000401 / 变体1 -> I64实例特征结果
F03 建立实际存在I64实例特征(const 实际存在I64实例特征建立请求&) -> 0x15000401 / 变体2 -> I64实例特征结果
F03-T1 换代I64实例特征当前值(const I64实例特征当前值换代请求&) -> 0x15000403 / 变体1 -> I64实例特征结果
F03-T2 换代实际存在I64实例特征当前值(const 实际存在I64实例特征当前值换代请求&) -> 0x15000403 / 变体2 -> I64实例特征结果
```

`提交实例写集` 与 `提交I64当前值换代写集` 的底层结果不是公开首次结果。F02 必须使用初始建立请求字段形成摘要并映射 `I64实例特征结果`，不得映射 `实例特征登记结果`；F02/F03 和两个换代变体即使共享物理标签也不得互用账项。

## 3. 编码与实施步骤

1. 在 `海中鱼巣/核心/L1事实基座.数据.h` 的唯一公共原语和 `海中鱼巣/领域/服务.L1实例特征.ixx` 内冻结 `HZY-L1G4`、格式版本 1、网络大端、L1 标签 U32、公开 DTO 变体 U8 及 DTO 声明顺序递归编码；不得复制 SHA-256 原语。
2. 为 F01、F02、F03、F03-T1、F03-T2 形成独立摘要函数；F02/F03 使用 0x15000401+变体 1/2，两个换代使用 0x15000403+变体 1/2，F01 使用 0x15000402+变体 1。
3. 每个公开入口先形成领域摘要并调用 `探测幂等`，只有未找到后读取 provider/事实；核心只形成独立 G0 摘要并互证，不覆盖调用方摘要。
4. 同一点同 DTO 同变体全字段相等才同义并以 `0/0` 读回首次结果；跨变体、缺字段、错误标签、错误结果 DTO、G0 混淆、账/审计/读回矛盾拒绝或隔离。
5. 恢复按首次真实 DTO、标签、变体和首次结果 DTO 重算；保留 G1—G6 的模式、身份、版本、调用/去重和 32/18/50 合同；施工和验证分别写入 v0.19 两条专属记录路径。

## 4. 验证矩阵

创建侧必须检查五个入口的字段顺序、嵌套 DTO、标签、变体和首次结果；验证 F02 初始请求绝不得到登记结果，F02/F03 同标签不同变体为异义，两个换代变体同理；验证同义 `0/0`、异义/缺字段/错 DTO/错标签拒绝、provider 前探测早于所有 provider 读取、G0 不覆盖领域摘要、恢复重算同点闭合。

同时运行 no-index diff check、`git diff --check`、`python .\tools\check_specs.py --strict`、UTF-8/NUL/尾随空白、24 项代码清单、两条记录路径唯一性、24+2=26、16 文件/50 调用点和 32/18/50 门禁。执行侧另行验证 Debug/Release、完整自检、实际接线和运行闭环。

## 5. 24 项代码白名单

```text
海中鱼巣/核心/L1事实基座.数据.h
海中鱼巣/核心/合同.L1事实基座.ixx
海中鱼巣/核心/仓库.L1事实基座.ixx
海中鱼巣/核心/服务.L1事实基座.ixx
海中鱼巣/核心/自检.L1事实基座.ixx
海中鱼巣/核心/自检.L1可重建索引.ixx
海中鱼巣/领域/服务.世界登记.ixx
海中鱼巣/领域/服务.L1场景结构.ixx
海中鱼巣/领域/服务.L1特征定义.ixx
海中鱼巣/领域/服务.L1实例特征.ixx
海中鱼巣/领域/服务.特征比较.ixx
海中鱼巣/领域/服务.L1实际存在.ixx
海中鱼巣/领域/组合.L1实际存在场景.ixx
海中鱼巣/领域/服务.L1状态.ixx
海中鱼巣/领域/自检.世界登记.ixx
海中鱼巣/领域/自检.L1实际存在.ixx
海中鱼巣/领域/自检.L1实例特征.ixx
海中鱼巣/领域/自检.特征比较.ixx
海中鱼巣/领域/自检.L1状态.ixx
海中鱼巣/自检.运行期上下文.ixx
海中鱼巣/核心/L1可重建索引.数据.h
海中鱼巣/核心/合同.L1可重建索引.ixx
海中鱼巣/核心/索引.L1事实基座.ixx
海中鱼巣/领域/L1场景结构.数据.h
```

发布后将 v0.19 标记为被 v0.20 替代/禁止施工并登记 v0.20 `待激活`；不得自行激活。最多声明设计合同闭合并要求执行侧从新 HEAD 重做 S0，不声明代码实现、构建、自检、provider 前探测、恢复运行或治理闭环完成。
