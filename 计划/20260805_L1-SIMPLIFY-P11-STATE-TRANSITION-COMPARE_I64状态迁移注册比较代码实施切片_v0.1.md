# L1-SIMPLIFY-P11-STATE-TRANSITION-COMPARE I64 状态迁移注册比较代码实施切片 v0.1

计划身份：`L1-SIMPLIFY-P11-STATE-TRANSITION-COMPARE`

版本：`v0.1`

创建侧代码基线：`161b5f2f06d11bc731e2b4b644ecd7f5cab39efc`

状态：必须待激活

## 1. 目标和完成条件

原位扩展 P6 的 `海中鱼巣.领域.服务.特征比较`，建立 `I64+状态迁移` 的真实注册、正式读回和只读比较。

完成必须满足：

- 独立迁移用途、角色、具名关系和 `0x0E` 合同注册形成；
- 比较调用方不传算法、注册身份、P2定义或动态身份；
- P9不可变旧状态与P10/P8新当前在同一当前截止校准；
- 路由只使用P10/P8返回的真实P2定义；
- 固定旧左新右、checked I64差异=`新-旧`，同时返回三态、关系、等价、差异、注册和完整输入；
- 相同材料即使编码/代次改变仍等价成功且差异0；
- 双配置、自检、strict、模块扫描和静态闭包通过。

唯一设计依据为同名v0.1详细设计和函数图，以及4180、4190、4200、4210、P6 v0.2、P9 v0.3、P10 v0.1冻结合同。

## 2. 精确代码白名单

只允许：

```text
海中鱼巣/领域/特征比较.数据.h
海中鱼巣/领域/服务.特征比较.ixx
海中鱼巣/领域/自检.特征比较.ixx
施工记录/20260805_L1-SIMPLIFY-P11-STATE-TRANSITION-COMPARE_施工记录.md
验证记录/20260805_L1-SIMPLIFY-P11-STATE-TRANSITION-COMPARE_验证记录.md
```

禁止修改工程、filters、启动聚合、P6目标判断既有函数、P7/P8/P9/P10、L1、P2、L2、旧比较/状态动态栈、运行期、SQL、材料、恢复、日志、规范、设计、流程图、其它计划或索引。

本叶不新增模块或顶层自检，原位扩展P6顺序340专项；完整自检总数和模块数不变。

## 3. 精确激活门禁

本计划登记为 `待激活`，必须同时满足：

1. P6 v0.2 的正式执行结果经交互智能体接受，实际 `特征比较.数据.h/服务/自检`、P6登记和目标判断合同与冻结公开接口一致；若P6因已具名漂移发布正式修订版，必须由交互确认该修订保持本计划消费的共享登记和公开合同，否则本计划先修订；
2. P7 v0.1、P8 v0.1、P9 v0.3、P10 v0.1 的正式执行结果分别经交互智能体接受；
3. P9实际结果DTO确实不含P2定义，并保留不可变当前/历史读取；P10/P8实际结果确实返回存在宿主、当前场景请求、实例槽、真实P2定义/定义关系和新当前值；
4. 上述实际接口和事实形状与本计划冻结合同逐字段一致；
5. P6释放本计划三个代码文件、顺序340和验证资源所有权；P9/P10及其上游施工切片释放；
6. 从新正式HEAD重做S0，复核计划blob、共享文件、provider签名、自检数、模块数、index和dirty所有权后仍可机械施工。

P5、生产装配、旧比较计划和旧消费者不是机器语义依赖。

## 4. 机械施工步骤

1. 在P6数据头追加状态迁移用途/角色/具名关系、迁移合同DTO、请求、完整输入见证和结果；保持原数值和目标判断DTO兼容。
2. 在同一服务追加三参构造和迁移登记/读取/比较三个公开函数；单参构造的P6能力不变。
3. 实现 `0x0E` 合同单写集：一节点、一配置值、一槽、七关系；配置和P2三值互证按详细设计精确形成。
4. 实现一个特征定义+迁移用途唯一当前路由；不得复用目标判断合同。
5. 比较入口用首末快照包围P9/P8读回，校准统一截止、主体、采样场景、实例槽、真实定义、值属性类型和值换代先后；允许前后状态之间存在合法中间值换代，不要求值事实也恰好相邻。
6. 固定旧值左、新值右；安全返回增加/等价/减少、等价布尔和新减旧差异。不可表示整项拒绝。
7. 构造完整输入见证和左右版本回执；比较前后机器事实零变化。
8. 原位扩展顺序340专项，形成专属施工/验证记录。

## 5. 失败与停工

- 非成功专用输入和比较载荷为空；状态/原因按详细设计§12精确映射。
- 登记失败零新增事实；比较永远零写入。
- 若必须修改P6目标判断语义、让调用方传算法/注册、用P9值属性类型路由、跨场景比较、解析关系19、扩大白名单或发布第二状态/动态，停止并具名退回设计漂移。

## 6. 验证合同

- 目标与cached diff精确五文件，Markdown只允许两份专属记录；
- Debug/Release x64串行Rebuild，零警告零错误；
- 两配置完整自检退出0，总数不增加；
- strict通过，MSVC模块扫描失败0/循环0/外部需求0，模块数不增加；
- `0x0E`、三个新公开函数和迁移合同定义唯一；
- 一节点/一值/一槽/七关系登记形状和配置20项精确；
- 请求不含算法、注册、定义或动态身份；P8定义是唯一迁移路由来源；
- 增加/等价/减少、相同材料新编码、极值可表示/不可表示全部通过；
- 主体/场景/槽/定义/类型/单位/维度/值域/时间先后/截止漂移逐项拒绝；
- P9历史前值和P8当前后值完整互证，比较前后L1快照相等；
- P6目标判断原矩阵保持通过；旧链、关系19解析、关系20、状态/动态/因果写入和生产日志调用为零。

## 7. 完成声明边界

只证明特征比较领域形成I64状态迁移用途的真实注册、独立读回和P9旧状态/P10新当前只读比较源码候选与本计划矩阵。

不得声明上游代码已实现、第二状态、关系20迁移动能、动作动态、因果、生产装配、旧消费者迁移、集成验收、3F、STEP-3或治理闭环完成。
