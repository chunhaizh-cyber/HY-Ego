# RUNTIME-TXN-S3 主装配事务与恢复链所有权端到端验收切片 v0.1

日期：2026-07-12

队列编号：#249 / 设计队列：DQ-141

状态：架构传递门控 / 不可执行 / 等待 #248 与 #270 归并裁决后的唯一主装配合同 / 680 未实施 / 恢复链设计继续门控 / 未改 C++

## 1. 依据与门控

同运行期主装配流程图 / 详细设计、JY-277/JY-290/JY-295 恢复门控和 #246 正式裁决。已验证当前恢复链未实现。假定 #247 已形成不可单独生产发布的结构核心，#248 已按矩阵形成 16 个长期服务、系统角色材料、完整首发和服务接线。接口漂移、#247 存在半上下文成功发布、#248 独立拥有服务、系统角色材料不完整或恢复裁决仍未唯一时不改代码，退回设计。

## 2. 范围

新增 `海中鱼巣/自检.主装配事务.ixx` 真模块，`ClCompile + export module 海中鱼巣.自检.主装配事务 + import`，预期最终回归 680。允许该模块、#247 `启动.运行期上下文`、#248 `装配.领域服务` 的验收所需窄幅修正、入口登记、工程、实施记录和治理。禁止其它生产服务 / 仓库修改，禁止实现 HYSNAP01、恢复解析、线程 / SQL / 面板接线、第二宿主或兼容上下文。

## 3. 步骤

S0 核对域 / 纪元 / 上下文 / 租约身份、16 服务成员、系统角色材料、最终成员顺序和 #226/#229/#237-#239 修订合同；S1 验收全新启动与正常读写，并证明服务生命周期由唯一上下文拥有；S2 验收请求拒绝和内部异常二分；S3 验收快照许可来源、恢复发布许可和空宿主均只能引用 #248 后最终合同，恢复分支注入既有角色材料而不重跑四初始化器，并静态确认生产协调器、发布槽、租约源和服务装配所有者各只有一个；S4 解锁恢复计划执行前复核，不重新生成平行上下文。

## 4. 验收与验证

A01-A12 覆盖上下文唯一、冻结来源、恢复候选不可提前发布、全新 / 恢复互斥分支、租约失效、停止析构、并发 100 轮、680 唯一登记；同时确认入口十二组历史夹具不进入生产上下文，#217 隔离组不成为快照来源。Debug x64、完整自检、严格规范、diff、最终 20 轮；提交推送。

## 5. 完成边界

通过后只解锁恢复计划的执行前实际接口复核；不等于快照或恢复已实现。
