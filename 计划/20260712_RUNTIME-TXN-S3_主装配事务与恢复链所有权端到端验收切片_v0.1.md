# RUNTIME-TXN-S3 主装配事务与恢复链所有权端到端验收切片 v0.1

日期：2026-07-12

队列编号：#249 / 设计队列：DQ-141

状态：依赖门控待执行 / 前置 #248 / 假定最终回归顺序 680 / 恢复链设计解锁门 / 未改 C++

## 1. 依据与门控

同运行期主装配流程图 / 详细设计、JY-277 恢复门控和 #246 正式裁决。已验证当前恢复链未实现。假定 #247/#248 已形成唯一上下文和服务接线。接口漂移或恢复裁决仍未唯一时不改代码，退回设计。

## 2. 范围

新增 `海中鱼巣/自检.主装配事务.ixx` 真模块，`ClCompile + export module 海中鱼巣.自检.主装配事务 + import`，预期最终回归 680。允许该模块、#247 `启动.运行期上下文`、#248 `装配.领域服务` 的验收所需窄幅修正、入口登记、工程、实施记录和治理。禁止其它生产服务 / 仓库修改，禁止实现 HYSNAP01、恢复解析、线程 / SQL / 面板接线、第二宿主或兼容上下文。

## 3. 步骤

S0 核对域 / 纪元 / 上下文 / 租约身份和 #226/#229/#237-#239 假定；S1 验收全新启动与正常读写；S2 验收请求拒绝和内部异常二分；S3 验收快照许可来源、恢复发布许可和空宿主均只能引用同一合同；S4 更新 JY-277 复用 / 修订 / 替代结论并同步恢复计划状态。

## 4. 验收与验证

A01-A12 覆盖上下文唯一、冻结来源、恢复候选不可提前发布、租约失效、停止析构、并发 100 轮、680 唯一登记。Debug x64、完整自检、严格规范、diff、最终 20 轮；提交推送。

## 5. 完成边界

通过后只解锁恢复计划的执行前实际接口复核；不等于快照或恢复已实现。
