# INSTINCT-ROOT-RUNTIME-ANCHOR-PROVIDER 本能根运行锚点组合提供者代码实施计划 v0.1

计划身份：`INSTINCT-ROOT-RUNTIME-ANCHOR-PROVIDER`

所属目标：`GOAL-INSTINCT-ROOT-RUNTIME-ANCHOR-REDESIGN`

设计与代码起点：`89495da35a15eeac5a0467bf5525cac8af44cc70`

精确设计：[本能根运行锚点组合提供者详细设计 v0.1](../规范/详细设计/20260923_INSTINCT-ROOT-RUNTIME-ANCHOR-PROVIDER_本能根运行锚点组合提供者详细设计_v0.1.md)

当前计划状态只以`计划/计划索引.md`登记为准。本文件先作为可执行候选合同发布；只有计划索引随后按本文件精确 blob 登记为`可执行`，代码所有者才能重新 S0并实施。

## 1. 目标与完成条件

新增阶段21组合 provider，本体只组合当前已发布的根实际F provider、根材料 provider和事实代次守卫，在同一 `Gfinal`正式重读后返回不可变双根锚点；同时建立生产外隔离专项，证明首次、重复、恢复、部分前缀、冲突、退出、发布未知和最终漂移分支。

完成必须同时满足：

- 公开请求只含版本和唯一自我；位置、键、预算、根角色和截止不由调用方注入；
- 实际F只来自`特征概念应用服务::形成或读取本能根I64实际F`；根T/D/L只来自`需求类数据服务::建立或读取本能根材料`；
- 最终四组材料在同一非零 `Gfinal`读回，双根身份不交叉；
- `Gfinal`只按详细设计的`提取根F成功截止`从实际F成功回执逐字段互证后取得，不使用任何历史H或首次H替代；
- 非成功结果空锚点，状态严格按设计归一；
- 能按设计字段无损映射为现行`自我线程本能根启动材料_v1`；
- 不修改或接入普通应用、启动、自我线程和根工程，不触碰RC-P1异主 WIP；
- Debug/Release x64隔离 Rebuild与专项运行均通过，并形成专属记录。

本计划完成不证明普通应用生产接线、启动阶段21短路、自我线程创建/停门、治理开门、跨进程故障恢复或业务闭环。

## 2. 正式依据与复用裁决

正式依据：8121 v0.6、8120 v0.16、6130 v0.17、4115 v0.9、7130 v0.19、8100 v1.12、8200 v1.9、CODE-FILE-01 v4.0。

当前基线说明：`89495da35`清理了6072项头源迁移后的历史证据与目录导航；它没有修改上述正式规范，也没有改变本计划直接复用的三个 provider 签名。RC-P1已暂停并释放 index，但其未提交 WIP仍原地保护；本计划只占用四个新生产文件和独立验证资源。

直接复用且零修改：

- `特征概念应用服务::形成或读取本能根I64实际F`；
- `需求类数据服务::建立或读取本能根材料`；
- `需求类数据服务::核验当前事实代次`；
- `特征概念应用服务::使用存在服务`、`需求类数据服务::与存在服务同底座`；
- `真实自我投影`及场景四根读回；
- `自我线程本能根启动材料_v1`的现行消费字段。

`.ixx→.h/.cpp`只为规避C1001，业务代码基本未变时必须优先复用现有 `.h/.cpp` provider及仍符合现行规范的旧编排；文件形态迁移本身不是重写业务逻辑的理由。不得复制上述服务内部算法，也不得恢复旧阶段21、已废止 ABI / 语义、旧 `.ixx` 消费或旧 L2入口。

## 3. 唯一所有权和允许文件

本计划整体为一个代码切片，唯一代码所有者由执行时S0声明。允许新建/修改仅限：

### 生产文件

- `海中鱼巣/业务/本能根运行初始化.数据.h`
- `海中鱼巣/业务/应用服务.本能根运行初始化.h`
- `海中鱼巣/业务/应用服务.本能根运行初始化.内部.h`
- `海中鱼巣/业务/应用服务.本能根运行初始化.cpp`

### 专属验证与记录

- `验证工具/本能根运行锚点组合提供者专项验证.cpp`
- `验证工具/本能根运行锚点组合提供者专项验证.vcxproj`
- `施工记录/20260923_INSTINCT-ROOT-RUNTIME-ANCHOR-PROVIDER_本能根运行锚点组合提供者施工记录_v0.1.md`
- `验证记录/20260923_INSTINCT-ROOT-RUNTIME-ANCHOR-PROVIDER_本能根运行锚点组合提供者验证记录_v0.1.md`

隔离输出固定到`D:\TEMP\海中鱼巣\INSTINCT-ROOT-RUNTIME-ANCHOR-PROVIDER\<本轮唯一标识>`，不得使用共享根工程输出目录。

## 4. 禁止文件和排除项

禁止修改：

- `海中鱼巣.vcxproj`、`海中鱼巣.vcxproj.filters`；
- `海中鱼巣/装配.普通应用.h/.cpp`、`海中鱼巣/启动.应用程序.cpp`及全部启动/宿主文件；
- `海中鱼巣/线程/线程_自我.h/.cpp`；
- `海中鱼巣/业务/应用服务.特征概念类.h/.cpp`、`应用服务.自我形成.h/.cpp`；
- `海中鱼巣/领域/**`、`海中鱼巣/核心/**`；
- 规范、计划索引、目标账、知识索引及全部异主RC-P1文件。

明确排除：普通应用持有provider、启动阶段21调用和失败映射、锚点缓存、self创建/停门、治理开门、任务/方法/子需求/状态/动态/因果、服务账、根D自我绑定、旧格式兼容和跨进程故障恢复验收。

若实现或验证需要修改任何禁止文件，本计划立即以具名DRIFT退回，不得临场扩大范围。

## 5. 目标接口与实现要求

严格按详细设计第3—7节机械实现：

1. 数据头定义`本能根运行单根锚点_v1`、`本能根运行锚点_v1`、`本能根运行初始化请求_v1`、十一值状态枚举和结果；全部`完整/成功`为无I/O值式判断。
2. 应用服务头声明不可复制/移动的`本能根运行初始化提供者`；构造注入特征概念服务、存在服务、需求服务、`真实自我读取请求`和对应`真实自我形成结果`；必须调用现有`结果.成功(请求)`取得投影，不得自行复制投影完整算法；公开初始化入口只有一个。
3. 内部头声明非业务、非持久的三入口调用端口和同一`执行组合`内核；`.cpp`真实适配只原样转发实际F、根材料和代次核验，专项脚本端口只返回现行公开结果类型并选择第N次守卫失败。公开头和生产调用方不得包含内部头。
4. `.cpp`私有固定安全/服务两组八个键、七组预算和角色请求构造；键值和预算不得改变。
5. 构造时以公开成功谓词核对完整自我读回和服务同上下文；初始化时从投影唯一导出`{场景=E, 组织父=唯一特征根}`。
6. 按安全F→安全根→服务F→服务根推进；实际F调用后只以详细设计冻结的`当前采用、IFR读回、准确F、原子读回、单值概念读回`五个嵌套`Gread`相等关系提取新G，初始化交付截止只要求非零且不晚于新G；每一步以后调用`核验当前事实代次`。
7. 冻结 `Gfinal` 后，按原键依次重读安全F、服务F、安全根、服务根；每一步前后守卫同一 `Gfinal`。重读阶段不得出现新发布。
8. 实际F包装失败只读取现行回执仍保留的`IFR/当前采用`；`当前采用读取失败`及缺失所需保留载荷的包装失败固定归`内部不一致`，不得推测已丢失来源。
9. 逐字段互证后组装锚点；所有失败清空锚点；异常按设计收口。
10. 在专项中按设计表逐字段构造`自我线程本能根启动材料_v1`并验证`完整()`，不修改线程生产文件。

锚点`完整()`必须同时证明：自我有效、G非零、每根D/L/F/T内部互异、双根八个身份全部互异，且自我 E 不等于这八个身份中的任何一个。

不得在实现中直接调用L1、保存第二幂等账/状态账、复制根写集、扫描创建顺序、硬编码节点编码或添加兼容分支。

## 6. 实施顺序

1. S0：确认HEAD、`main==origin/main`、计划精确版本、允许文件不存在或未被占用，复核异主 WIP；只停止本切片的真实重叠。
2. 新建数据头并先实现纯值不变式。
3. 新建provider公开头、内部端口头和源文件，落实公开自我读回成功谓词、真实适配、固定请求构造、状态归一和逐步形成。
4. 实现同`Gfinal`四次重读和最终锚点组装；非成功统一空载荷。
5. 新建隔离专项和独立工程；工程显式列出所需现行生产头源及本叶头源，不引用或修改根工程，不包含普通应用和启动源。
6. 真实服务栈覆盖首次、重复、重启、部分前缀、冲突、退出和self映射；同一组合内核通过内部脚本端口确定性覆盖已可能发布及每个`Gfinal`守卫漂移点。脚本不得复制算法，不向公开接口或生产对象添加测试开关。
7. 形成施工/验证记录，记录实际基线、键/预算、用例、命令、结果、未覆盖范围和知识变更清单。
8. 精确检查只包含允许文件；取得唯一发布租约后才可精确暂存、提交并普通推送。本任务起草阶段不执行发布。

## 7. 失败收口与退回条件

- 当前接口与详细设计签名不一致且会改变DTO、状态或算法：具名DRIFT退回计划支撑。
- 隔离专项只有修改根工程/普通应用/异主文件才能链接：所有权/范围阻断，保留现场并退回；不得接线绕过。
- 上游返回未被设计覆盖的新状态：不得默认为内部成功；记录精确枚举和调用点后退回。
- 普通代码错误、include缺失、链接源清单遗漏和专项夹具错误由本切片自行修复，不扩大生产范围。
- `已可能发布`、漂移、冲突和资源失败均为合法非成功分支，不回滚已发布上游事实，不换键。

## 8. 验证命令与成功标准

执行时使用当前 VS2022/MSBuild实际路径，至少运行：

```powershell
msbuild 验证工具/本能根运行锚点组合提供者专项验证.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /p:OutDir=<隔离Debug目录> /p:IntDir=<隔离Debug中间目录>
msbuild 验证工具/本能根运行锚点组合提供者专项验证.vcxproj /t:Rebuild /p:Configuration=Release /p:Platform=x64 /p:OutDir=<隔离Release目录> /p:IntDir=<隔离Release中间目录>
<Debug专项程序>
<Release专项程序>
git diff --check -- <本计划允许文件>
```

另做静态检查：

- 四个生产新文件无`.ixx`、`import/export module`、L1调用、线程创建、装配或启动引用；内部端口头只被provider `.cpp`和专项包含，公开头不暴露它；
- 不出现根D到自我/所属存在/目标宿主的新增字段或关系；
- 现有两个上游provider源码零修改；
- 最终成功路径实际调用`核验当前事实代次(Gfinal)`并完成四次同截止重读；
- 所有非成功结果均无锚点。

成功条件：两配置编译链接零错误，全部专项断言通过，差异检查通过。验证记录须把真实服务栈用例与内部脚本端口用例分账，逐项列出首次/重复/重启/部分恢复/冲突/退出、未知发布映射、每个最终守卫漂移点和self字段映射；脚本结果不得表述为真实L1故障注入。构建通过不能替代业务断言。

## 9. 完成声明和后继

允许声明：阶段21组合provider本体已按8121形成或恢复双根材料，并在生产外真实服务栈同一`Gfinal`完整读回；公开锚点可无损映射self消费字段。

禁止声明：阶段21已接普通应用、程序能够越过阶段21、自我线程已生产创建/运行、治理门已开放、跨进程故障恢复已验收或自我治理闭环完成。

发布后由交互/计划侧重新扫描当前事实。只有RC-P1释放普通应用和根工程所有权后，才建立紧邻的普通应用+启动+停门self消费后继计划；不得把该后继混入本计划提交。
