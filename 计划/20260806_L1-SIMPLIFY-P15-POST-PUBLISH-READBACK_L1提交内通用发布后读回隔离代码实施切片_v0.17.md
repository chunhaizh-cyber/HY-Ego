# L1-SIMPLIFY-P15 G7/G8 真实写入口意图 DTO 修订代码实施切片 v0.17

计划身份：`L1-SIMPLIFY-P15-POST-PUBLISH-READBACK`

版本：`v0.17`

创建侧基线：`aaf7558116d692c336b618d9de607da93d221a5c`

替代：P15 v0.16；具名漂移：`DESIGN-DRIFT-P15-V016-G7-G8-PREPROBE-INTENT-DTO-MATRIX-MISMATCH`

## 1. 目标与禁止范围

只修订 G7/G8 provider 前领域意图摘要所使用的真实写入口 DTO、字段顺序、固定操作标签、首次结果 DTO、同义/异义和恢复重算。保留 G1—G6、公共编码、领域/G0 独立互证、核心不得覆盖调用方摘要、P-L1/P-I/P-S、32/18/50、16/50 和 24 文件白名单。不得实现代码，不得触碰 P63、P0-FIX、路线、旧 WORLD-TREE 或其它 WIP。

## 2. 精确接口与物理位置

不新增文件。只在既有 24 文件白名单中的两个服务模块使用以下无自由标签参数的摘要入口：

```text
领域/服务.L1状态.ixx
  形成G7状态登记意图摘要(const L1状态登记请求&)
  形成G7首个基准状态发布意图摘要(const 实际存在I64首个基准状态发布请求&)

领域/服务.特征比较.ixx
  形成G8特征比较登记意图摘要(const 特征比较登记请求&)
  形成G8目标判断合同登记意图摘要(const I64目标判断比较合同登记请求&)
  形成G8状态迁移合同登记意图摘要(const I64状态迁移比较合同登记请求&)
```

固定标签分别为 T01 `0x15000601`、T02 `0x15000602`、M01 `0x15000801`、M02 `0x15000802`、M03 `0x15000803`。不得用读取 DTO、结果 DTO、调用序号或 G0 写集替代上述请求。

## 3. 实施步骤

1. 从正式 HEAD 重扫五个真实写入口及其 DTO，逐字段对照 v0.17 详细设计；任何接口/字段漂移退回计划支撑，不临场改机器语义。
2. 为 T01/T02/M01/M02/M03 分别形成固定 G7/G8 域摘要；嵌套 DTO 按声明顺序递归，公共原语、端序和 optional/variant/vector/array 沿用 v0.16。
3. 将每一点接入“真实请求 DTO 编码→provider 前探测→未找到后 provider 读取→独立 G0 摘要→核心互证→一次提交”。核心不得覆盖领域摘要。
4. 首次结果严格映射 T01 `L1状态登记结果`、T02 `实际存在I64基准状态结果`、M01 `特征比较登记结果`、M02 `I64目标判断比较合同结果`、M03 `I64状态迁移比较合同结果`；当前 T01/M01 的快照读取在摘要/探测前，施工时必须先形成真实请求摘要并探测，再允许 provider/事实读取。
5. 同一点全字段相等为同义且 provider/提交 `0/0`；任一公开字段差异或不同固定标签为异义；错误 DTO、缺字段、错版本、错标签、摘要/G0 不等均入口拒绝，双账/读回矛盾隔离。
6. 恢复按首次点、固定标签和真实首次 DTO 重算 G7/G8 摘要，与首次账、审计、执行材料、G0 摘要和发布后读回分别互证；G1—G6 恢复规则不变。

## 4. 验证门禁

创建侧必须通过：三文件 no-index diff check、`git diff --check`、`python .\tools\check_specs.py --strict`、UTF-8/NUL/尾随空白；T01/T02/M01/M02/M03 入口签名、标签、字段序列、嵌套 DTO、结果 DTO、同义/异义向量和恢复拒绝矩阵；G1—G6、32/18/50、16/50、24 文件及 G0 独立互证不漂移。

执行侧另行验证 Debug/Release、完整自检、provider 前探测、首次/同义/异义、恢复和发布后读回；本计划不预先宣称代码已实现或运行通过。

## 5. 完成边界与索引门禁

最多声明 v0.17 设计合同已修正并可重新 S0。发布后必须把 v0.16 标为被 v0.17 替代/禁止施工，并新增 v0.17 `待激活` 行；不得自行激活。索引更新只允许 P15 两行切片，必须以正式 plan blob 和设计提交登记，其他计划状态不改。
