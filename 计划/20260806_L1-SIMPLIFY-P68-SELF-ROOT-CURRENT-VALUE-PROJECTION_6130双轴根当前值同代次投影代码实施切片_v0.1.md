# L1-SIMPLIFY-P68-SELF-ROOT-CURRENT-VALUE-PROJECTION
# 6130 双轴根当前值同代次投影代码实施切片 v0.1

计划状态：待激活

创建侧正式基线：`main == origin/main == 748cb16dbaf92935b854b2be2e3af00101d60638`

对应详细设计：
`规范/详细设计/20260806_L1-SIMPLIFY-P68-SELF-ROOT-CURRENT-VALUE-PROJECTION_6130双轴根当前值同代次投影详细设计_v0.1.md`

对应单函数流程图：
`流程图/20260806_L1-SIMPLIFY-P68-SELF-ROOT-CURRENT-VALUE-PROJECTION_组合同代次自我双轴根当前值函数流程图_v0.1.md`

## 1. 目标与完成条件

只实现一个无对象状态纯值投影：从 P56 完整当前自我、P65 双轴根定义发布事实和 P26
同截止当前 I64 特征集合中，形成 A、V、两个完整根特征见证和由 A 派生的控制态。

完成必须同时成立：

- 双轴根当前值投影 DTO、状态和唯一自由函数进入正式模块；
- P56/P26 世界根和非零事实截止严格相等；
- P65 安全、服务定义非零互异且发布代次不晚于共同截止；
- 自我主体、安全根和服务根各唯一，缺失与结构损坏分账；
- A/V 都在 `0..I64_MAX`，控制态只按 A 的 0/1/>1 派生；
- 生产代码 provider、P67/P40、P15、L1、事务、缓存和日志调用为零；
- 顺序 568 专项、构建、自检、strict 和模块扫描通过。

## 2. 允许范围

未来代码和专属记录白名单仅为：

```text
海中鱼巣/领域/L1自我根当前值.数据.h
海中鱼巣/领域/服务.L1自我根当前值投影.ixx
海中鱼巣/领域/自检.L1自我根当前值投影.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
海中鱼巣/启动/完整自检.cpp
施工记录/20260806_L1-SIMPLIFY-P68-SELF-ROOT-CURRENT-VALUE-PROJECTION_施工记录.md
验证记录/20260806_L1-SIMPLIFY-P68-SELF-ROOT-CURRENT-VALUE-PROJECTION_验证记录.md
```

禁止修改 P15、P17—P26、P56、P65—P67 代码和 DTO；禁止触碰 P0-FIX、路线、
旧 WORLD-TREE、生产装配、恢复、旧入口删除或其它 WIP。

## 3. 实施顺序

### W01：S0 与接口门禁

1. 重核 `main == origin/main`、ahead/behind 0/0、Git index 空和目标白名单所有权；
2. 核对本计划精确 plan blob、状态和唯一活动代码计划；
3. 读取 P26、P56、P65、P67 的真实头文件与公开模块，逐字段比对冻结合同；
4. 确认顺序 568、三个新文件、工程、filters、启动和验证资源未占用；
5. 任一真实接口改变事实形状、状态、字段来源或 const 签名时停工并具名退回计划支撑。

### W02：纯值 DTO

1. 新建 `L1自我根当前值.数据.h`；
2. 定义合同版本、投影规则、控制态、投影状态、单根事实和双轴事实；
3. DTO 只保存强类型值和调用方拥有的副本，不保存 provider、锁、句柄或引用；
4. 保证非成功结果事实为空，成功事实含共同非零截止。

### W03：唯一投影函数

1. 新建 `服务.L1自我根当前值投影.ixx`；
2. 先校验请求与三份完整成功事实，再校准 P56/P26 根和截止；
3. 从 P65 取得两个互异定义，在 P26 中查找唯一自我主体组；
4. 精确筛选安全、服务两项，逐字段互证宿主、槽、关系、值、来源和生命周期；
5. 复制 P26 完整特征见证，形成 A/V，并按 A 的 0/1/>1 派生控制态；
6. `bad_alloc` 映射资源失败，其它异常映射内部不一致；不返回部分单根。

### W04：顺序 568 专项

1. 新建 `自检.L1自我根当前值投影.ixx` 并登记唯一完整自检入口；
2. 覆盖版本、坏成功、根/截止、定义、主体、槽、关系、当前值、来源和值域矩阵；
3. 覆盖零项、多项、定义交换、其它主体/定义不夹带及 A 的三控制态；
4. 覆盖输入顺序扰动、并发确定性、分配失败和未知异常；
5. 静态检查所有禁止依赖和 provider 调用为零。

### W05：工程、记录与验证

1. 只登记两个新模块和顺序 568 自检，不新增启动单元或第二服务；
2. 写入本计划专属施工、验证记录，记录实际文件、命令、结果和未验证边界；
3. 串行执行 Debug/Release x64 Rebuild 与两配置退出式完整自检；
4. 执行 strict110/110、MSVC 模块扫描、目标 diff check 和白名单检查；
5. 只提交本计划实际切片并普通推送，不夹带异主 WIP。

## 4. 验证矩阵

| 编号 | 验证 | 成功门禁 |
| --- | --- | --- |
| V01 | 请求版本 | 错版本入口拒绝且不遍历输入 |
| V02 | 上游事实形状 | P56/P65/P26 坏成功统一内部不一致 |
| V03 | 同代次 | P56/P26 同根同截止；漂移零事实 |
| V04 | 双定义 | 安全/服务非零互异，发布代次不晚于截止 |
| V05 | 唯一自我 | 零项缺失，多项或资格矛盾内部不一致 |
| V06 | 唯一双轴 | 两项各唯一；缺失、重复、交换分账 |
| V07 | 完整见证 | 宿主、槽、两关系、值、来源和生命周期闭合 |
| V08 | 数值边界 | A/V 只接受 0..I64_MAX |
| V09 | 控制态 | A=0/1/>1 精确映射，V 不参与选择 |
| V10 | 确定性 | 相同值输入、并发和容器扰动逐字段相同 |
| V11 | 零副作用 | provider、写入、锁、等待、缓存、日志调用为零 |
| V12 | 工程回归 | 两配置构建、自检、strict、模块扫描、diff check 通过 |

## 5. 激活门禁

本计划保持 `待激活`，直至：

1. P17—P26、P56、P65—P67 真实代码结果进入 main 并经交互接受；
2. P26/P56/P65 实际 DTO、字段、状态、排序、事实截止和 const 签名与本包机械匹配；
3. P15 替代真实结果完成，但只通过上游接口和共享资源门禁传递；
4. 唯一活动代码计划、Git 发布租约、工程、启动、顺序 568、构建和自检资源释放；
5. 从新 HEAD 重做 S0，确认无等价双轴根当前值投影且白名单零重叠。

## 6. 完成声明边界

完成后最多声明：源码候选能纯值组合 P56、P65 和 P26 的成功事实，形成同截止 A/V、
完整根特征见证及由 A 唯一派生的控制态。

不得声明生产协调读取、任意后继当前入口、上游代码已实现、根需求、6170 维护、合同进展、
装配、恢复、旧栈删除、正式集成验收、STEP-5 或治理闭环完成。
