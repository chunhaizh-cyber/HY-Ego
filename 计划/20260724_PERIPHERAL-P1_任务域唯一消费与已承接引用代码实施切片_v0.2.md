# PERIPHERAL-P1 任务域唯一消费与已承接引用代码实施切片 v0.2

编号：PERIPHERAL-P1；路由：#366 / DQ-258；合同：PER-C6 / ABI 2；日期：2026-07-24

设计状态：终审 PASS。当前状态、版本和 plan blob 唯一读取 `计划/计划索引.md`。

## 合同与边界

- 依据：3200、5230、6340；`规范/详细设计/D455任务唯一消费冻结工作包与观察方法详细设计.md` v0.2；`流程图/20260724_PERIPHERAL-TASK_任务唯一消费冻结工作包观察方法施工流程图_v0.2.md`；`规范/详细设计/函数结构知识图谱/20260724_PERCEPTION-D0_D455观察体素生产闭环函数结构知识图谱_v0.2.md`。
- 已实现事实：任务 / 工作项现行只读接口存在但仍需 #373 适配；C2 / C5 是待实现合同。
- 待实现：`任务外设材料承接ABI版本 = 2`；等待工作项、消费窗口、承接规格 / 结果、已承接引用；`D455承接结果 尝试承接D455报告(const D455承接规格&)`。
- `D455报告身份` 是全局唯一承接键；任务、工作项和窗口只作准入 / 绑定。

## 文件所有权

允许：`海中鱼巣/线程/协议.任务外设材料承接.ixx`、`海中鱼巣/领域/数据操作.任务外设材料承接.ixx`、`海中鱼巣/领域/服务.任务外设材料承接.ixx`、`海中鱼巣/领域/组合.任务外设报告承接.ixx`、`海中鱼巣/领域/自检.任务外设材料承接.ixx`，以及 `实施记录/20260724_PERIPHERAL-P1_PER-C6任务承接代码实施_Codex断点清单.md`。

禁止：报告提供者实现、C7—C13、现行任务服务、工程、入口、运行器、生产装配和设计文件。

## 施工与 S0

1. S0 核对 plan blob、C2 内部消费提供者 / C5 产品合同、任务当前性只读端口和五文件所有权。
2. S1 建立承接 DTO、数据操作、服务与组合层；不暴露仓库、锁或事务令牌。
3. S2 令报告消费参与者先确认、任务侧承接后确认、完整读回后最后发布；撤销逆序。
4. S3 自检源码覆盖跨窗口 / 任务 / 代次重复、过期、错任务、错工作项、质量不足和恢复分类。
5. S4 核对白名单、稳定唯一键、import 方向和 diff。
6. S5 只提交允许代码与专属记录，推送执行通道分支并恢复 clean。

## 机械验证命令

S0 记录 `$计划段起点 = (git rev-parse HEAD).Trim()`；S4 的 `git diff --check` 与 `git diff --cached --check` 均须退出 0。S5 后运行：

```powershell
$期望 = @('海中鱼巣/线程/协议.任务外设材料承接.ixx','海中鱼巣/领域/数据操作.任务外设材料承接.ixx','海中鱼巣/领域/服务.任务外设材料承接.ixx','海中鱼巣/领域/组合.任务外设报告承接.ixx','海中鱼巣/领域/自检.任务外设材料承接.ixx','实施记录/20260724_PERIPHERAL-P1_PER-C6任务承接代码实施_Codex断点清单.md') | Sort-Object
$实际 = @(git -c core.quotepath=false diff --name-only "$计划段起点..HEAD") | Sort-Object
if (Compare-Object $期望 $实际) { exit 1 }
if ((@(rg -n "任务外设材料承接ABI版本\s*=\s*2" '海中鱼巣/线程/协议.任务外设材料承接.ixx')).Count -ne 1) { exit 1 }
if ((@(rg -n "尝试承接D455报告" '海中鱼巣/领域')).Count -lt 1) { exit 1 }
```

全部命令须退出 0；ABI 精确 1 处，承接入口至少 1 处。

## 验证与收口

- 只做静态 / 专项源码验证，不构建、不运行。
- C2 内部端口 / 唯一键漂移返回 `DRIFT-PER-CONTRACT`；任务只读字段不能适配返回 `DRIFT-PER-CURRENT-INTERFACE`；需要第六代码文件返回 `DRIFT-PER-FILE-OWNERSHIP`；反向依赖返回 `DRIFT-PER-IMPORT-CYCLE`。
- 报告确认后承接记录不能发布 / 读回为 `FAIL-PER-INTERNAL-READBACK`；其它为 `FAIL-PER-VALIDATION`。
- 完成只证明 PER-C6 候选，不证明报告提供者、工作包或任务闭环接通。
