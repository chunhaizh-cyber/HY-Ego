# L1-SIMPLIFY-P16-PRE2-DYNAMIC-SPEC-PROVIDER-EVIDENCE 状态迁移动能规格只读提供者证据代码实施切片 v0.1

计划身份：`L1-SIMPLIFY-P16-PRE2-DYNAMIC-SPEC-PROVIDER-EVIDENCE`

创建侧代码基线：`4e791b37244ccf5333eefd4c87ee4bb4ab24fe4a`

状态：`待激活`

## 1. 目标与完成条件

在 P14 唯一 `L1动态服务` 原位增加状态迁移动能规格只读提供者证据。只有 `已形成` 同时返回固定身份 `0x4C31445000000001ULL`、合同/规则版本1、直接调用次数1、provider数量1、最终共同截止、动态登记当前事实、PRE1传递当前/历史完整事实、排序去重身份组、同索引完整副本组和精确数量；全部非成功规格与证据全空。

完成仅证明 P14 证据合同实现并通过本切片验证，不证明 PRE1/P16/P15、动态发布、生产接线、因果、崩溃恢复或治理闭环。

## 2. 当前事实与依赖门禁

- 当前正式基线为 `4e791b37244ccf5333eefd4c87ee4bb4ab24fe4a`。
- P14 v0.4 代码与局部验收已进入 main；现有结果仍无 provider 身份/凭证。
- PRE1 v0.1 已设计、发布并成为唯一可执行，但本基线没有其真实代码结果；不得把计划状态当实现。
- P16 v0.2 待激活，等待 P13/P14 provider 证据。

激活前必须同时满足：PRE1 真实代码结果进入 main 并经交互接受；实际 P13 provider DTO/字段与详细设计匹配；P14 接口重新 S0 无漂移；P14 三代码路径、既有顺序390及共享构建/运行资源取得唯一所有权。任一不满足保持待激活并具名退回，不由执行者补设计。

## 3. 所有权与精确白名单

唯一所有者：`海中鱼巣.领域.服务.L1动态`。

只允许修改：

```text
海中鱼巣/领域/L1动态.数据.h
海中鱼巣/领域/服务.L1动态.ixx
海中鱼巣/领域/自检.L1动态.ixx
施工记录/20260806_L1-SIMPLIFY-P16-PRE2-DYNAMIC-SPEC-PROVIDER-EVIDENCE_施工记录.md
验证记录/20260806_L1-SIMPLIFY-P16-PRE2-DYNAMIC-SPEC-PROVIDER-EVIDENCE_验证记录.md
```

不得修改 PRE1/P12/P13/P15/P16、工程、filters、启动、计划索引或其它计划材料；不得新增第二服务、第二 exe、自检目标、装配入口、事实缓存或仓库。

## 4. 实施步骤

1. 在 `L1动态.数据.h` 增加合同/规则版本1、固定身份凭证、三类对象种类、三类副本来源、写前身份和 provider 证据 DTO。
2. 扩展 `状态迁移动能规格结果`：成功同时有规格和证据；其它状态两项全空。
3. 在 `服务.L1动态.ixx` 实现无参 `noexcept` 固定身份读取，不取锁、不读 L1、不分配、不写日志。
4. 在唯一根 `形成状态迁移动能规格` 中先验证 P13 规格、迁移证据和 PRE1 provider 凭证完整；复制 P14 登记后读 G0。
5. 要求 P13规格截止、PRE1共同截止、P14规格截止与 G0 同一非零值；从 G0 当前节点组逐项恰一取得 P14 十项登记完整事实，不允许历史回退。
6. 保留既有比较与 `0x44` 规格算法；把 PRE1 当前/历史完整副本机械映射为 P13 传递来源，不重读或重解释 P13 事实。
7. 联合集合必须覆盖 `0x44` 写集全部非本地引用和决定写入内容的事实；排除 `0x44` 及 `0x53000001` 本地键。
8. 按 `(对象种类, 稳定身份)` 升序去重并绑定同索引完整副本；同身份重叠先互证副本相等，再按动态登记当前、P13当前、P13历史的顺序保留来源。
9. 读取 G1，要求 `G1==G0==共同截止`；互证身份/副本/数量等长且非零后，一次返回规格与证据。
10. 任一半结构、错身份/版本/截止/来源/种类/覆盖/排序/数量返回内部不一致全空；G1漂移返回版本漂移全空；分配失败返回资源失败全空。

## 5. 调用、事务与并发边界

- P16 非等价成功分支对 P14 恰调用1次，P14 provider 去重数量1；等价/无前状态调用0次。
- P13结果是 P16 传入的不可变材料，不计作 P14 内部第二次 provider 调用。
- 本函数零 L1 写；只在复制 `登记_` 时短持 P14 锁，G0/G1读取不持该锁；零 sleep、零等待、零阻塞重试、零跨 provider 锁。
- 不形成摘要、幂等域、提交事务、读回计划或恢复材料；它们仍属于 P15/P16 后继职责。

## 6. 专项验证矩阵

静态门禁：固定身份全仓唯一；函数/DTO物理位置与依赖方向正确；PRE1/P12/P13/P15/P16 零修改；结果成功/非成功载荷不变量；既有 P14 自检顺序390仍唯一；无新工程或启动登记。

同一生产程序专项覆盖：

- 增加、减少成功：P14调用1、provider数量1、合同/规则/身份正确、G0/G1/规格/凭证截止相等。
- 等价、禁止动态、无前状态、上游非成功：P14应调用0或返回既有非成功，规格/证据全空。
- 十项登记均为 G0 当前完整事实；任一缺失、多重或历史回退拒绝。
- PRE1当前副本、P12合法历史副本及混合集合成功；P14不得自己重读 P13 历史事实。
- 同身份同副本去重成功；不同副本、当前/历史冲突、错种类、零身份、零截止拒绝。
- 缺来源、缺副本、乱序、重复未去除、组长/数量不等、写集覆盖缺项拒绝。
- G0/G1漂移、坏provider身份或版本、PRE1半结构、分配失败、并发漂移均返回规定状态和全空载荷。

Debug/Release 只使用同一生产程序和既有 P14 自检顺序390；通过正式日志人读核对关键事件、字段顺序与拒绝分支。日志只是观察证据，不是机器事实或完成闭环。

至少运行：

```powershell
git diff --check
python .\tools\check_specs.py --strict
```

代码实施还须执行计划要求的同一生产程序 Debug/Release 构建与运行门禁，并保存两份具名记录。

## 7. P16 消费与完成边界

P16 后继只在幂等探测未找到、P13 非等价成功后调用 P14，并机械检查固定身份、合同/规则、调用/数量、共同截止、登记、身份/副本/数量及规格截止，再把完整 P13/P14 强类型材料交给 P15 统一规范化。

P16 不得补身份、重读 L1、哈希副本、建立第二来源解释、选择通用读回项目，或把 P13 传递事实重复登记为 P14 直接 provider。P14 非成功、证据缺失或共同截止不等均不得提交。

本计划完成后只可声明 P14 provider 证据合同进入 main 并通过本切片门禁；必须明确未覆盖 PRE1/P16/P15 完成、生产接线、因果和跨进程恢复。
