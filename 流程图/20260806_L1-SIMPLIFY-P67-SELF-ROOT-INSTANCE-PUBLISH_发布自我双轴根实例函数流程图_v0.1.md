# L1-SIMPLIFY-P67 发布自我双轴根实例函数流程图 v0.1

创建侧正式基线：`af95c3608aca0130967c1540705fe66df71708f8`

目标函数：`L1自我根实例发布服务::发布自我双轴根实例`

```mermaid
flowchart TD
    A["接收自我双轴根实例发布请求"] -->|开始| B["纯值校验P67与嵌套P66版本及批次范围"]
    B -->|入口不合法| Z["返回入口拒绝 空事实"]
    B -->|入口合法| C["调用P66形成双轴根实例批次规格一次"]
    C -->|P66具名非成功| X["同名映射返回 P40调用零次"]
    C -->|坏成功或未知状态| Y["返回内部不一致 空事实"]
    C -->|已形成| D["校验A为1 V为0 顺序0和1 恰两项共同宿主来源截止"]
    D -->|规格矛盾| Y
    D -->|规格完整| E["把P40批次请求原样委托P40一次"]
    E -->|P40发布前非成功| W["同名映射返回 空事实"]
    E -->|P40发布后内部失败| V["返回内部不一致 保留发布代次"]
    E -->|首次或幂等| F["互证0x0F键 两项目 四编码和4170记录"]
    F -->|缺项 错项或额外项| V
    F -->|逐项成立| G["组合P66规格和P40完整结果"]
    G -->|分配失败| U["返回资源失败并保留P40发布边界"]
    G -->|成功| H["返回已提交或幂等读回完整事实"]
```

固定边界：P66恰一次，P40至多一次且仅P66成功时调用；P15/P36/P37/P38/P39/L1直接调用0。全流程不新增幂等身份、摘要、稳定编码、4170账、事务、独立读回、根需求或生产装配。
