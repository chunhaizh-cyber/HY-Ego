# L1-SIMPLIFY-P15 E01 RP/RW、P-I 与恢复导出服务流程图 v0.28

更新时间：2026-08-06
依据：P15 v0.28 详细设计；正式基线 `23957f3954d4b19826f67fb930cdd578b1ba82f2`。

```mermaid
flowchart TD
 A["E01 请求"] --> B["形成 node-only RP v1"]
 B --> C["G6: HZY-L1G6→格式→标签→变体"]
 C --> D["变体后只插入一次 RP 原始32字节"]
 D --> E["登记合同/规则/幂等/期望代次"]
 E --> F["provider前探测与P-I/P-L1证据"]
 F -->|同义| G["原样返回首次 RW与结果摘要 v2\nprovider/提交 0/0"]
 F -->|未找到| H["唯一提交：锁内映射并形成 RW v1"]
 H --> I["E01结果摘要 v2：一次RS/U32(2)\n字段载荷+RW原始32字节"]
 I --> J["幂等账/审计/结果见证状态组\n同一候选原子交换"]
 J --> K["发布后权威读回"]
 K -->|一致| L["返回首次结果"]
 K -->|不一致| M["内部不一致+隔离，禁止补记"]
 N["生产/恢复调用方"] --> O["L1事实基座服务::导出恢复材料(request)"]
 O --> P["校验合同版本"]
 P -->|错误| Q["入口拒绝\n无材料/不访问仓库"]
 P -->|正确| R["唯一仓库共享锁\n读取已发布状态"]
 R --> S{"状态完整且隔离见证一致?"}
 S -->|否| T["内部不一致\n无材料/不改状态"]
 S -->|是| U["确定性复制排序\n当前快照/历史/账/审计/见证"]
 U --> V["成功导出\n隔离状态原样保留\n候选不导出"]
 V --> W["建立恢复候选"]
 W --> X["确认发布或撤销"]
```

## 恢复导出物理所有权

```mermaid
flowchart LR
 A["请求 DTO\n核心/合同.L1事实基座.ixx"] --> B["服务入口\n核心/服务.L1事实基座.ixx"]
 B --> C["唯一私有仓库\n核心/仓库.L1事实基座.ixx"]
 C --> D["L1恢复材料导出结果\n核心/L1事实基座.数据.h"]
 D --> E["建立/确认/撤销恢复候选\n同一 L1事实基座服务"]
 F["领域调用方"] --> B
 G["自检调用方"] --> B
```

合同版本错误在服务层拒绝；仓库只读共享锁覆盖校验、复制和排序；隔离但完整时成功返回并保留见证；不完整或见证矛盾时内部不一致；不导出未确认候选，不允许外部仓库旁路。
