# DD-02 D455 外设报告队列分类视图与生产者生命周期流程图

更新时间：2026-07-21

## 依据

```text
规范/6320_子规范_外设观察特征与自我场景认知分层_20260720.md
规范/6340_子规范_外设独立控制线程与消息承接边界_20260720.md
规范/6350_子规范_双目相机外设独占观察线程_20260720.md
规范/6360_子规范_相机外设综合工作流程_20260720.md
规范/详细设计/D455材料成熟度与四类产品数据合同详细设计.md
规范/详细设计/D455外设报告队列分类视图与生产者生命周期详细设计.md
```

## 说明

本图覆盖每设备唯一生产者、材料保留、一个物理强类型队列、四类索引 / 只读视图、任务域唯一消费和生产者停止。任务匹配、冻结工作包、方法执行和世界事实提交由 DD-03—DD-05 承担。

## 流程图

```mermaid
flowchart TD
    A["稳定 D455 设备身份和生产控制域登记器"] --> B{"按设备身份取得唯一生产者占用？"}
    B -- "同设备已有占用" --> R1["逻辑内拒绝：不进入启动"]
    B -- "取得成功" --> C["分配生产控制域编号和生产者世代"]
    C --> D["签发一次任务域唯一消费权，绑定材料域与一个报告队列世代"]
    D --> E["生产者：未启动 -> 启动中 -> 运行中"]

    E --> F["接收已通过 DD-01 值式合同的四类产品之一"]
    F --> G{"产品、投递约束和生产者身份有效？"}
    G -- "否" --> R2["逻辑内拒绝：不形成报告候选"]
    G -- "是" --> H["收集并保留全部 D455 受控材料引用"]
    H --> I{"全部引用可解析、当前且元数据一致？"}
    I -- "否" --> R3["逻辑内拒绝：释放已保留材料，不占队列"]
    I -- "是" --> J{"一个物理队列能否预留容量？"}
    J -- "可舍弃产品且队列满" --> R4["逻辑内返回：具名丢弃或摘要收口"]
    J -- "不可舍弃产品且队列满" --> K["生产者进入背压等待投递，保留唯一待投递包"]
    K --> K1{"容量恢复并重试同一报告包？"}
    K1 -- "否" --> K
    K1 -- "是" --> L
    J -- "可以" --> L["建立未发布报告候选；候选对视图和消费者不可见"]
    L --> M["精确读回候选的产品、身份、约束和材料保留"]
    M --> N{"候选读回一致？"}
    N -- "否" --> X1["内部逻辑错误：精确撤销并追根因，生产者故障"]
    N -- "是" --> O["原子确认：写物理活动序列和唯一产品索引，状态=已发布待承接"]
    O --> P{"发布后按身份读回一致？"}
    P -- "否" --> X2["内部逻辑错误：隔离生产者并停止依赖路径"]
    P -- "是" --> Q["四类非消费只读视图可见；视图不改变状态、不取得材料保留权"]

    Q --> S["任务域用唯一消费权按报告身份准备承接候选"]
    S --> T{"报告仍待承接且消费权、时效有效？"}
    T -- "否" --> R5["逻辑内返回：无报告、已认领或已过期"]
    T -- "是" --> U["状态=任务域保留中；其它路径不得认领"]
    U --> V{"DD-03 匹配裁决"}
    V -- "暂不匹配且仍当前" --> W["释放候选 -> 已发布待承接"]
    W --> Q
    V -- "正式承接" --> Y["移出活动序列和索引；材料保留权移入唯一承接交接包"]
    V -- "仅缓存 / 降级 / 摘要 / 丢弃 / 过期" --> Z["具名终结，移出活动序列和索引，释放材料"]

    E --> AA{"发生断流？"}
    AA -- "是" --> AB["断流恢复中：锁存强类型原因和恢复次数"]
    AB --> AC{"同设备来源恢复且身份未变？"}
    AC -- "是" --> E
    AC -- "否或恢复耗尽" --> X3["生产者故障；不得伪造恢复"]

    E --> AD["收到停止请求"]
    K --> AD
    AB --> AD
    AD --> AE["正在停止：拒绝新产品，撤销未发布候选，等待或释放承接候选"]
    AE --> AF{"不可舍弃报告均已承接或有强类型强制终结原因？"}
    AF -- "否" --> R6["逻辑内等待：不得静默清空"]
    AF -- "是" --> AG["停止队列、释放设备占用权 -> 已停止"]
```

## 关键边界

```text
同一设备同一时刻只有一个有效生产者占用；不同设备可以并发。
四类产品共享一个物理队列，只通过四类身份索引和非消费视图区分。
候选确认是报告可见和索引可见的唯一原子发布点。
只读视图不取得消费权；只有任务域持有唯一消费权并能认领、承接或终结。
确认前失败精确撤销；确认后读回矛盾属于内部逻辑错误，不能伪装成普通失败。
不可舍弃报告在队列满或停止时必须背压、承接或具名强制终结，不得静默丢弃。
线程不是生产权来源，报告和队列项不是世界事实。
```
