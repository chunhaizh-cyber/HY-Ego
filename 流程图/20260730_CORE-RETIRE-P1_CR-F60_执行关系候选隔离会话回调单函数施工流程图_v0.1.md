# CR-F60 执行关系候选隔离会话回调单函数施工流程图

版本：v0.1
代码基线：`57866ac5ca63235ed12da60f635d29f1aacd718b`

## 函数合同

```text
根函数：执行关系候选隔离会话回调
物理位置：海中鱼巣/核心/自检.节点直接身份结构写入.ixx，CR-F46 之前内部区
完整签名：void 执行关系候选隔离会话回调(关系候选隔离会话回调上下文& 上下文, 节点直接身份结构写入会话& 会话)
参数：两项均为一次执行期借用；场景固定1—7，字段适用性见函数功能确认§2.13
返回：void；操作结果、审计和可见性证据写入上下文；提交/撤销决定写入会话
```

```mermaid
flowchart TD
    N01{"N01【指令-分类】上下文.场景"} -- "1 创建" --> X01["X01【函数调用】节点直接身份带值结构写入结果<关系句柄> 节点直接身份结构写入会话::创建关系候选(关系类型 类型,节点句柄 源节点,节点句柄 目标节点,std::int64_t 顺序号)：上下文字段逐项绑定；接收首次操作"]
    N01 -- "2/4/5/6/7 失效" --> X02["X02【函数调用】节点直接身份带值结构写入结果<关系句柄> 节点直接身份结构写入会话::失效关系候选(关系句柄 关系)：上下文.输入关系→关系；接收首次操作"]
    N01 -- "3 重挂" --> X03["X03【函数调用】节点直接身份带值结构写入结果<关系句柄> 节点直接身份结构写入会话::重挂关系候选(关系句柄 关系,节点句柄 新源节点,节点句柄 新目标节点)：上下文.输入关系/新源节点/新目标节点逐项绑定；接收首次操作"]
    N01 -- "其它" --> X00["X00【函数调用】void 节点直接身份结构写入会话::记录失败(const 节点直接身份结构写入结果& 失败) noexcept：入口拒绝→失败；返回void"]
    X01 -- "候选结果" --> X04
    X02 -- "候选结果" --> X04["X04【函数调用】std::optional<正式关系审计材料> 节点直接身份结构写入会话::读取关系审计(关系句柄 关系)：首次操作.值→关系；接收事务审计"]
    X03 -- "候选结果" --> X04
    X04 -- "审计已接收" --> N02["N02【指令-赋值】按场景比较当前/历史和端点，并写入六个具名可见性布尔"]
    N02 -- "场景2需双身份重复" --> X05["X05【函数调用】节点直接身份带值结构写入结果<关系句柄> 节点直接身份结构写入会话::失效关系候选(关系句柄 关系)：事务审计当前句柄→关系；接收重复操作"]
    N02 -- "场景4/5/6提交" --> X06["X06【函数调用】void 节点直接身份结构写入会话::请求提交() noexcept：无参数；返回void"]
    N02 -- "其它合法场景撤销" --> X07["X07【函数调用】void 节点直接身份结构写入会话::请求撤销() noexcept：无参数；返回void"]
    X05 -- "重复结果已接收" --> X07
    X00 -- "失败已记录" --> R01["R01【指令-返回】返回void"]
    X06 -- "提交已请求" --> R01
    X07 -- "撤销已请求" --> R01
```

调用者：CR-F46；绑定方式为 `std::bind_front(执行关系候选隔离会话回调,std::ref(上下文))`。公开会话入口内部间接调用 CR-F58，本回调不得直接调用 CR-F58。
