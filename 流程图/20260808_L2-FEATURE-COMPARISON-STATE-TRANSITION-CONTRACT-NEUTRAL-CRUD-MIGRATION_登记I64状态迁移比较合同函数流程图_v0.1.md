# L2-FEATURE-COMPARISON-STATE-TRANSITION-CONTRACT-NEUTRAL-CRUD-MIGRATION 登记 I64 状态迁移比较合同函数流程图 v0.1

日期：2026-08-08

根函数：`特征比较服务::登记I64状态迁移比较合同(const I64状态迁移比较合同登记请求&)`

边界：只画 M03 根函数及其直接调用的登记输入读取、固定写集形成和中性精确读回；不画 M01、M02、公开当前读取、三个运行比较、状态动态联合发布或 L1 内部事务。图是目标施工事实，不是当前实现事实。

```mermaid
flowchart TD
    A["进入M03登记，请求为调用方值"] --> B["进入异常边界并取得状态迁移合同专属互斥锁"]
    B --> C{"完整公开请求是否合法"}
    C -- "否" --> R0["返回入口拒绝/无；零L1调用"]
    C -- "是" --> D["短锁复制迁移合同请求、合同和M01登记缓存状态"]
    D --> E{"缓存组状态"}
    E -- "半结构" --> RI["返回内部不一致/无"]
    E -- "已有完整请求但与本次不同" --> RC["返回入口拒绝/注册不唯一；零L1调用"]
    E -- "已有完整请求且相同" --> F1["以同义合同和M01定位重建首次1/7/1/1/0中性写集；不重读provider，不比较当前代次"]
    E -- "全空，首次路径" --> F0["调用M01读取当前登记"]
    F0 --> G0["读取登记输入束：G0、定义节点、标记/下界/上界值、七目标节点、G1"]
    G0 --> H0{"输入束同代次且业务字段完整"}
    H0 -- "首末竞争，首次" --> F0
    H0 -- "第二次仍漂移" --> RD["返回版本漂移/输入版本失效；零提交"]
    H0 -- "缺失或业务见证失效" --> RD
    H0 -- "完整；不以G0与请求期望不等提前返回" --> I0["形成合同见证和固定1/7/1/1/0中性写集"]
    F1 --> J["恰一次调用提交中性写集"]
    I0 --> J
    J --> K{"L1写入状态"}
    K -- "幂等冲突" --> RC
    K -- "事实代次漂移、端点未找到或已退出" --> RD
    K -- "入口拒绝" --> RR["返回入口拒绝/无"]
    K -- "资源失败" --> RX["返回资源失败/无"]
    K -- "内部不一致" --> RI
    K -- "成功" --> L0{"是否首次路径、输入G0等于请求期望、发布true、重试不适用"}
    K -- "精确重复" --> L1{"是否同义缓存路径、发布false、原键读回收敛"}
    L0 -- "否" --> RI
    L1 -- "否" --> RI
    L0 -- "是" --> M{"合同版本、回显键、非零首次代次、九映射是否完整唯一"}
    L1 -- "是" --> M
    M -- "否" --> RI
    M -- "是" --> N["以键1注册、键2配置、原请求和边界形成完整状态迁移合同见证"]
    N --> O["中性当前精确读回：M01登记、G0、注册节点、配置值、当前属性、七关系源组、七目标节点、定义三值、G1"]
    O --> P{"全部同代次、当前、形状与20项材料精确；九新事实共同创建代次正确"}
    P -- "首末竞争，首次" --> O
    P -- "第二次仍漂移" --> RD
    P -- "资源失败" --> RX
    P -- "缺失、退出、回显或结构矛盾" --> RI
    P -- "完整" --> Q["短锁核对并成组更新迁移合同请求和非权威合同定位缓存"]
    Q --> S["首次成功和精确重复统一返回已比较/无及本次当前合同"]
    B -. "bad_alloc" .-> RX
    B -. "其它异常" .-> RI
```

## 固定写集

```text
节点 1：状态迁移比较注册
值 2：注册节点的 I64组配置，固定20项
关系 3—9：注册 -> 七目标，合同字段关系类型，角色1—7
属性槽：注册.合同配置I64组属性类型 -> 值2
退出：0
```

```text
领域缓存只裁决完整业务请求同义 / 异义
-> L1 只比较完整中性物理写集
-> 成功类必须回到 M03 当前合同精确读回
-> 本 L2 合同闭合不等于第一层自证或完善
```

`0x15000803U`、G8、旧探测、完整快照和 legacy 提交不进入目标登记流程；M01、M02、三个运行比较与状态动态联合发布保持不变。
