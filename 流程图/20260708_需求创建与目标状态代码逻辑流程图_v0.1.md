# 需求创建与目标状态代码逻辑流程图

更新时间：2026-07-08

## 依据

```text
AGENTS.md
规范/000_项目规则总纲.md
规范/001_规则迁移清单.md
规范/详细设计/需求目标状态承载详细设计.md
规范/详细设计/服务操作函数矩阵第一批.md
实施记录/20260708_应用逻辑流程图迁移顺序信息数据.md
实施记录/20260706_FS04_需求树入口只读扫描记录.md
计划/20260707_FS04_需求服务目标状态与任务承接材料增强专项_v0.1.md
计划/已完成计划/20260707_FS04_需求服务目标状态与任务承接材料S1-S4代码实施切片_v0.1.md
实施记录/20260707_FS04_需求服务目标状态与任务承接材料S1-S4代码实施_Codex断点清单.md
流程图/20260708_特征与状态材料代码逻辑流程图_v0.1.md
海中鱼巣/领域/需求服务.h
海中鱼巣/领域/状态服务.h
海中鱼巣/领域/特征服务.h
海中鱼巣/领域/任务服务.h
```

## 说明

本图是第 5 项“需求创建与目标状态流程”的代码逻辑流程图，承接第 4 项“特征与状态材料”输出的当前特征状态材料和抽象目标状态材料。

本图只表达当前已确认和已验证的第一轮需求创建逻辑：需求目标必须由目标状态节点和需求到目标状态的模板关系承载；I64 只作为状态服务或特征服务内部状态值材料；任务承接只能读取需求服务提供的主体、目标宿主、场景和目标状态材料。第一轮简化入口只作为兼容 / 验证入口，不提供完整任务承接材料。父子需求、逻辑组织需求、方向签名、枚举目标、方法候选、SQL 显示和控制面板均不在本图中实施。

## 流程图

```mermaid
flowchart TD
    A["上游请求 / 语素材料 / 特征状态材料"] --> B["降权为需求创建请求"]
    B --> C{"请求类型"}

    C -- "只创建需求身份" --> N1["需求服务::创建需求"]
    N1 --> N2["主信息仓库创建主信息"]
    N2 --> N3["节点仓库创建 需求 节点"]
    N3 --> RVN["读回验证：需求节点"]

    C -- "创建目标状态需求(兼容 / 验证入口)" --> D1["需求服务::创建目标状态需求(目标宿主, 目标特征, 当前特征状态材料, 目标状态值)"]
    D1 --> D2{"目标宿主有效 && 目标特征有效"}
    D2 -- "否" --> RJ["拒绝：不创建需求 / 结构不变化"]
    D2 -- "是" --> D3["特征服务读取当前 I64 特征状态材料"]
    D3 --> D4{"当前状态材料可读"}
    D4 -- "否" --> RJ
    D4 -- "是" --> D5["状态服务::创建抽象状态(目标状态值)"]
    D5 --> D6{"目标状态节点有效"}
    D6 -- "否" --> FS["追根因解决收口：不返回有效需求 / 不允许半结构作为事实"]
    D6 -- "是" --> D7["需求服务::创建需求"]
    D7 --> D8["关系仓库写 需求->目标存在 引用关系"]
    D8 --> D9["关系仓库写 需求->目标特征 引用关系"]
    D9 --> D10["关系仓库写 需求->当前特征状态材料 引用关系"]
    D10 --> D11["关系仓库写 需求->目标状态 模板关系"]
    D11 --> RVD["读回验证：需求节点 / 目标状态 / 目标宿主 / 目标特征 / 当前特征状态材料 / 模板关系"]

    C -- "创建目标状态需求(承接材料完整入口)" --> E1["需求服务::创建目标状态需求(主体, 目标宿主, 场景, 目标特征, 当前特征状态材料, 目标状态值)"]
    E1 --> E2{"主体有效 && 目标宿主有效 && 场景有效 && 目标特征有效 && 主体 / 目标宿主角色已明确"}
    E2 -- "否" --> RJ
    E2 -- "是" --> E3["特征服务读取当前 I64 特征状态材料"]
    E3 --> E4{"当前状态材料可读"}
    E4 -- "否" --> RJ
    E4 -- "是" --> E5["状态服务::创建抽象状态(目标状态值)"]
    E5 --> E6{"目标状态节点有效"}
    E6 -- "否" --> FS
    E6 -- "是" --> E7["需求服务::创建需求"]
    E7 --> E8["关系仓库写 需求->主体 引用关系(主体顺序号)"]
    E8 --> E9["关系仓库写 需求->目标宿主 引用关系(目标宿主顺序号)"]
    E9 --> E10["关系仓库写 需求->场景 引用关系(场景顺序号)"]
    E10 --> E11["关系仓库写 需求->目标特征 引用关系(目标特征顺序号)"]
    E11 --> E12["关系仓库写 需求->当前特征状态材料 引用关系(当前材料顺序号)"]
    E12 --> E13["关系仓库写 需求->目标状态 模板关系"]
    E13 --> RVE["读回验证：需求节点 / 目标状态 / 主体 / 目标宿主 / 场景 / 目标特征 / 当前特征状态材料 / 模板关系"]

    C -- "读取需求目标状态" --> R1["需求服务::读取需求目标状态"]
    R1 --> R2{"需求有效 && 唯一模板目标为 状态 节点"}
    R2 -- "否" --> RJ
    R2 -- "是" --> R3["返回目标状态节点"]
    R3 --> READ["只读方：任务服务 / 需求结算 / 显示层只读"]

    C -- "读取需求承接材料" --> M1["需求服务::读取需求承接材料"]
    M1 --> M2["读取需求主体 / 目标宿主 / 场景 / 目标状态"]
    M2 --> M3{"四项材料均唯一且类型正确"}
    M3 -- "否" --> RJ
    M3 -- "是" --> M4["输出需求承接材料"]
    M4 --> T1["任务服务::按需求创建任务 只读承接材料"]

    C -- "目标状态满足预判" --> J1["需求服务::目标状态满足预判 / 结算预览"]
    J1 --> J2{"需求引用目标特征和当前特征状态材料"}
    J2 -- "否" --> JF["返回 证据不足 / 不写事实"]
    J2 -- "是" --> J3["读取需求目标状态"]
    J3 --> J4["状态服务读取目标状态值"]
    J4 --> J5["特征服务读取当前 I64 特征状态材料"]
    J5 --> J6{"目标状态值和当前材料均可读"}
    J6 -- "否" --> JF
    J6 -- "是" --> J7{"当前材料 >= 目标状态值"}
    J7 -- "是" --> J8["返回 已满足"]
    J7 -- "否" --> J9["返回 未满足"]
    J8 --> NR["局部枚举结果 / 预览材料，不写持久化结算记录"]
    J9 --> NR

    C -- "旧需求树 / SQL / 显示材料" --> X1["排除或后续专项"]
    X1 --> X2["父子需求 / 逻辑组织 / 方向签名 / 枚举目标 转第 6 项及后续"]
    X1 --> X3["SQL / 控制面板 / 显示镜像 不参与机器事实"]
    X1 --> X4["旧需求主信息字段不得直接迁移"]

    RVN --> RV1{"读回符合预期"}
    RVD --> RV1
    RVE --> RV1
    RV1 -- "否" --> LE["逻辑错误：登记断点 / 停止后续施工计划推进"]
    RV1 -- "是" --> OUT["输出需求节点句柄和目标状态结构"]
    OUT --> READ

    N2 -. "主信息创建不及预期" .-> NFS["身份创建追根因解决收口：不返回有效需求 / 需求节点不可读"]
    N3 -. "需求节点创建不及预期" .-> NFS
    NFS --> RJ
    D5 -. "目标状态创建后后续失败" .-> FS
    D7 -. "创建不及预期" .-> FS
    D8 -. "关系写入不及预期" .-> FS
    D9 -. "关系写入不及预期" .-> FS
    D10 -. "关系写入不及预期" .-> FS
    D11 -. "关系写入不及预期" .-> FS
    E7 -. "创建不及预期" .-> FS
    E8 -. "关系写入不及预期" .-> FS
    E9 -. "关系写入不及预期" .-> FS
    E10 -. "关系写入不及预期" .-> FS
    E11 -. "关系写入不及预期" .-> FS
    E12 -. "关系写入不及预期" .-> FS
    E13 -. "关系写入不及预期" .-> FS
    FS -. "覆盖本次创建的目标状态 / 需求节点 / 关系" .-> CLEAN["失败后不得留下可读半结构"]
    CLEAN --> RJ
```

## 关键边界

```text
需求目标是目标状态，不是 I64。
I64 只允许作为状态服务或特征服务内部状态值材料，不得作为需求目标对外语义。
目标状态由状态服务创建抽象状态节点；目标状态本体不携带发生时间戳。
需求到目标状态使用模板关系承载，不能写入旧需求主信息目标字段。
需求主体、目标宿主、场景、目标特征和当前特征状态材料均由需求服务写关系承载，不由调用方隐式补齐事实；当前特征状态材料必须经特征服务读取，需求服务不得直接依赖特征值服务。
完整入口只要求主体 / 目标宿主角色已明确，不在本图硬拒绝“主体 == 目标宿主”；是否禁止同一节点承担两个角色，后续由需求规范确认。
第一轮简化入口只作为兼容 / 验证入口，不提供完整任务承接材料。
任务服务只能通过需求服务读取需求承接材料，缺主体、目标宿主、场景或目标状态时不得创建任务承接壳。
目标状态满足预判 / 目标状态需求结算预览当前只返回局部领域枚举结果，不写持久化结算记录；权威结算记录属于后续需求结算流程。
任一必需节点、状态材料或关系写入不及预期，必须进入追根因解决收口；追根因解决收口必须覆盖本次创建的目标状态、需求节点和关系，不得留下可读半结构。
最小读回验证按路径拆分：需求身份路径读回需求节点；简化入口读回需求节点、目标状态、目标宿主、目标特征、当前特征状态材料和模板关系；完整入口读回需求节点、目标状态、主体、目标宿主、场景、目标特征、当前特征状态材料和模板关系。
旧需求主信息字段、旧任务列表字段、SQL 投影、控制面板显示和文本语义不得作为机器事实迁移。
父子需求、逻辑组织需求、方向签名、枚举目标、方法候选和需求树结构化更新转第 6 项及后续流程。
```

## 当前代码差距

```text
当前需求目标状态、需求主体、目标宿主、场景和承接材料读取已通过 FS-04 S1-S4 第一轮验证，但不代表完整需求树完成。
当前目标状态比较仍是第一轮固定规则：当前状态材料 >= 目标状态值；不支持任意比较表达式、枚举目标生产者分级或方法候选裁决。
当前创建需求过程对多步写入不及预期只返回空句柄，尚未形成完整事务回滚设计；后续详细设计或施工计划需要继续补追根因解决收口和半结构不可读验收。
当前目标状态满足预判 / 目标状态需求结算预览不作为权威事实，不写持久化结算记录；需求结算权威记录转后续需求结算流程。
当前图已有对应详细设计，但不生成待确认计划或代码实施许可。
```

## 后续产物

```text
本图可作为后续“需求创建与目标状态详细设计”或“需求树后续结构流程图”材料。
下一份流程图按迁移顺序应进入第 6 项：需求树后续结构流程。
若进入代码实施，必须另建待确认施工计划，明确允许文件、禁止文件、入口拒绝、追根因解决收口、读回验证和完成声明边界。
```
## 中途非成功返回二分口径

本文件按 2026-07-09 硬规则修订：中途非成功返回只分为 `追根因解决` 和 `逻辑内返回`。

- `追根因解决`：前置条件已经满足，并进入创建、绑定、写关系、写状态、记录动态、结算、读回或结构承载后，结果不符合内部预期；必须停止依赖路径，定位根因，当前未证明完整回滚时登记事务隔离缺口或半结构隔离缺口。
- `逻辑内返回`：领域协议允许的拒绝、候选为空、请求材料返回或人读材料返回；必须保证结构不变化，且返回材料、日志、回执、显示或控制台输出不裁决机器事实。
