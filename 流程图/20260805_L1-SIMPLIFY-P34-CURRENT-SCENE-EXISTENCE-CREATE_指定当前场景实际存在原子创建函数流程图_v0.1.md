# L1-SIMPLIFY-P34-CURRENT-SCENE-EXISTENCE-CREATE 指定当前场景实际存在原子创建函数流程图 v0.1

日期：2026-08-05

状态：设计流程图；代码未实现；绑定 P15 v0.5、P33 v0.1 与 P7 接受结果

## 1. 函数身份

```cpp
实际存在结果 L1当前场景实际存在发布服务::在当前场景创建实际存在(
    const 当前场景实际存在创建请求& 请求);
```

本函数是场景直接成员关系的具名领域发布者，不是组合服务。它只消费 P7/P33 不可变规格并调用一次 L1 v2；未来世界树门面只能透明委托。

## 2. 主流程

```mermaid
flowchart TD
    A["收到当前场景实际存在创建请求"] --> B{"版本、根、场景、幂等身份、期望代次完整?"}
    B -- "否" --> R1["入口拒绝；空载荷；全部provider与提交0次"]
    B -- "是" --> C["按固定标签0x50333401编码公开请求并计算请求意图SHA-256"]
    C --> D{"摘要非全零?"}
    D -- "否" --> R2["内部不一致；零provider；零提交"]
    D -- "是" --> E["用0x0B域幂等键调用L1探测幂等恰1次"]
    E --> F{"探测状态"}
    F -- "同义已发布" --> G["校验首次映射、四项读回、结果摘要与隔离形状"]
    G --> H{"首次副本完整?"}
    H -- "是" --> R3["机械映射幂等读回；provider0；提交0"]
    H -- "否" --> R2
    F -- "异义冲突" --> R4["幂等冲突；provider0；提交0"]
    F -- "入口拒绝" --> R1
    F -- "已隔离" --> R5["许可拒绝；provider0；提交0"]
    F -- "内部不一致/坏状态" --> R2
    F -- "未找到" --> I["调用P7形成实际存在写入规格恰1次"]
    I --> J{"P7成功且规格完整?"}
    J -- "否" --> R6["按P7状态返回；坏成功映射内部不一致；零提交"]
    J -- "是" --> K["调用P33形成当前场景接纳规格恰1次"]
    K --> L{"P33成功且规格完整?"}
    L -- "否" --> R7["按P33同名状态返回；许可拒绝立即结束；零提交"]
    L -- "是" --> M{"两规格截止=请求期望代次且字段互证?"}
    M -- "否：截止" --> R8["版本漂移；不重取；零提交"]
    M -- "否：形状" --> R2
    M -- "是" --> N["形成1节点+1值+1关系+1槽的规范化写集"]
    N --> O["形成节点/关系/值/属性槽四项非空通用读回计划"]
    O --> P["编码provider版本、共同截止、见证、写集和计划并计算执行证据SHA-256"]
    P --> Q{"执行摘要非全零且计划完整?"}
    Q -- "否" --> R2
    Q -- "是" --> S["调用L1 v2提交写集恰1次"]
    S --> T{"L1结果"}
    T -- "成功/精确重复" --> U["校验本地键1-3映射与同锁四项权威读回"]
    U --> V{"节点、值、关系、槽、代次全部吻合?"}
    V -- "是" --> R9["形成实际存在事实；成功=已提交，重复=幂等读回"]
    V -- "否" --> R2
    T -- "幂等冲突/代次漂移/资源失败" --> R10["同名映射；空业务载荷"]
    T -- "内部不一致/发布后失败" --> R11["内部不一致；保留L1发布代次与隔离；不补写"]
```

## 3. 摘要与写集固定形状

```mermaid
flowchart LR
    A["公开请求全部字段"] --> B["请求意图摘要"]
    B --> C["provider前探测"]
    C -->|"仅未找到"| D["P7存在规格"]
    C -->|"仅未找到"| E["P33场景规格"]
    D --> F["共同截止与写前见证"]
    E --> F
    F --> G["规范化写集：节点1、值2、关系3、槽1->2"]
    G --> H["规范化读回：节点、关系、值、属性槽"]
    H --> I["执行证据摘要"]
    I --> J["单次L1 v2提交与同锁读回"]
```

请求意图不编码 provider、写集、本地键或后来分配的稳定编码。执行证据只在首次未找到路径形成，编码请求摘要、provider版本、共同截止、P7/P33见证、规范化写集和读回计划。

## 4. 结果映射与禁止旁路

| 条件 | 公开结果 | 禁止动作 |
| --- | --- | --- |
| 探测同义 | `幂等读回` | 重调P7/P33、重算执行证据、重提交 |
| 探测异义 | `幂等冲突` | 改用第二幂等域或新身份 |
| P33许可拒绝 | `许可拒绝` | wait、sleep、retry、扫描 |
| provider截止不等 | `版本漂移` | 静默换用新代次 |
| L1首次成功 | `已提交` | 事务释放后普通读回 |
| L1竞态精确重复 | `幂等读回` | 使用本次provider回显替代首次结果 |
| 发布后读回矛盾 | `内部不一致` | 回滚swap、补写、第二提交、降级成功 |

固定禁止：组合服务直连 L1、服务私有幂等缓存、SQL/材料/旧仓/句柄、完整快照重取、第二次提交、特征/状态/动态/因果夹带、生产装配和运行期接线。

## 5. 完成声明边界

本图只冻结 P34 未来函数的控制分支、摘要、写集、读回和错误收口。它不证明函数已经实现、P15/P33 已实现、L1 已发布任意事实、世界树写门面存在、STEP-4 或治理闭环完成。
