# NODE-TYPED-MIGRATION NT-P2C 自我内部世界与事实投影迁移施工流程图

更新时间：2026-07-22

## 依据

```text
正式规范：0050、1130—1170、4010—4040、4070、4110、4210、4220、7130。
上级计划：#338 / DQ-230 / NODE-TYPED-MIGRATION，NT-P0、NT-P2。
真实前置：NT-P1 节点直接身份与正式关系 0—23 ABI；NT-P2A 宿主实例槽值式组读取；NT-P2B 主体状态/动态值式组读取。P2C 专属消费关系 19—22。
```

## 身份与边界

本图是 NT-P2C 的施工目标图，不是当前实现事实。只有预冻结待实现接口合同 S0、独立叶子计划、队列登记和真实派发全部完成后，执行窗口才可据此形成候选；提供者实现尚未汇合不构成启动门控，真实接线和完整验证由 #352 完成。全部目标服务、数据操作、初始化和读取组合属于隔离节点直接新域；现行默认系统角色、自我线程和运行期装配保持只读。

## 流程图

```mermaid
flowchart TD
    A["调用语境：全新候选上下文初始化或正式自我结构读取"] --> B{"调用种类"}
    B -->|"创建/幂等初始化"| C["接收世界根、自我所在场景、自我存在和自我内部世界场景的非零稳定主键"]
    B -->|"增加内部世界成员"| C2["接收当前自我、当前内部世界和候选成员的完整句柄"]
    B -->|"聚合读取"| R0["按自我稳定身份或关系 21 角色 1 来源定位当前自我候选"]

    C --> D["存在服务、场景服务分别形成本域不可变规格；组合器不取得仓库能力"]
    C2 --> D
    D --> E{"写前身份、类型、版本、稳定主键、唯一性、成员非自我和无环是否满足"}
    E -->|"否"| LR0["逻辑内返回：入口拒绝、幂等冲突或版本漂移；节点、关系和索引零变化"]
    E -->|"是"| F["事务外权威预读：关系 21 角色 1 的当前来源、目标及角色 2 成员集合"]
    F --> G{"预读分类"}
    G -->|"同键同义且唯一结构完整"| LR1["逻辑内返回：幂等读回完整自我结构，零写入"]
    G -->|"已有另一当前自我或同键异义"| LR2["逻辑内返回：唯一性/幂等冲突，零写入；若已存在多个当前自我则转内部结构错误"]
    G -->|"未找到且前置满足"| H["取得覆盖节点、关系、索引和初始化材料的同一唯一写入权"]
    H --> I["锁内重读所有稳定身份和关系 21；不依赖系统角色缓存或索引命中裁决"]
    I --> J{"锁内仍允许写入"}
    J -->|"否"| LR3["逻辑内收口：同义读回、异义冲突、许可拒绝或版本漂移；不形成候选"]
    J -->|"是"| K["创建或复用带直接稳定主键的自我存在、自我所在场景和独立内部世界场景候选"]
    K --> L["建立自我所在场景的普通成员归属；该场景不是自我内部世界"]
    L --> M["建立关系 21 角色 1：当前自我存在到唯一内部世界场景"]
    M --> N["按请求建立关系 21 角色 2：内部世界场景到子存在；拒绝自我作为目标和任何自包含环"]
    N --> O["事务内读回：当前自我恰一、内部世界恰一、自我非成员、成员无重复、端点/版本/稳定主键一致"]
    O --> P{"候选是否完整"}
    P -->|"否"| IR0["内部逻辑错误：停止新增写入，按成员关系、内部世界关系、场景、存在、索引的依赖逆序撤销"]
    IR0 --> IR1{"撤销后能否证明恢复事务前态"}
    IR1 -->|"否"| IR2["追根因并隔离事务域；不得释放后让普通读取观察半自我结构"]
    IR1 -->|"是"| IR3["返回内部不一致证据；不得降级为普通缺口、等待或幂等成功"]
    P -->|"是"| Q["全部候选确认待发布；进入无失败最后发布区并释放唯一写入权"]
    Q --> R0

    R0 --> R1["节点直接存在服务读回唯一自我和关系 21 角色 1，节点直接场景服务读回角色 2 成员组"]
    R1 --> R2{"唯一自我、唯一内部世界、自我非成员和无环是否成立"}
    R2 -->|"否"| IR4["发布后内部逻辑错误：停止依赖路径，不用缓存、清单或默认值补齐"]
    R2 -->|"是"| R3["调用节点直接特征业务服务::读取宿主实例特征槽位组"]
    R3 --> R4["P2A 逐槽读回关系 22 角色 1 宿主槽、2 定义模板、3 当前值及类型化原始记录"]
    R4 --> R5["调用节点直接状态业务服务::读取主体状态事实组"]
    R5 --> R6["P2B 按关系 19 主体角色反向候选逐项读回完整状态事实"]
    R6 --> R7["调用节点直接动态业务服务::读取主体动态事实组"]
    R7 --> R8["P2B 按关系 20 主体角色反向候选逐项读回完整动态事实"]
    R8 --> R9["组合调用期不可变自我投影：身份、内部世界、成员、实例槽、状态、动态"]
    R9 --> R10["投影不持久化、不反向写入；清空后可从节点、关系和领域记录确定重建"]
    R10 --> Z["完成：唯一自我结构由关系 21 裁决，槽位消费关系 22，状态/动态消费关系 19/20"]
```

## 关键边界

```text
1. 自我所在场景与自我内部世界是两个语义不同的场景；自我可以是前者成员，绝不能是后者成员。
2. 关系 21 只拥有内部世界和成员；实例槽统一由关系 22 承载，状态/动态投影统一消费关系 19/20。
3. P2C 不读取 P2A/P2B 的仓库、索引或记录容器，只消费领域服务返回的不可变值式组。
4. 空成员、空状态或空动态是合法空投影；多个当前自我、多个内部世界、缺必需关系或自包含环是内部结构错误。
5. 系统角色清单可以作为启动回执或缓存，但每次完整性裁决必须回到节点、关系和领域记录。
6. P2C 新建节点直接系统角色回执和候选初始化器，不修改现行系统角色清单、默认运行期上下文或自我线程；P3 只形成新域调用候选，P4 才一次切换默认装配。
6. 本图不实现生产恢复；P4 才负责从完整快照隔离重建并一次发布运行期上下文。
```
