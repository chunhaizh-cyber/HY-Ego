# F-A102 准备特征值类型化记录提交

根函数：`节点直接身份结构写入结果 特征值类型化记录事务参与者::准备提交(const 节点直接身份结构提交准备只读视图& 视图)`

归属：记录参与者模块；待新增；视图仅调用期借用；结果值式。

```mermaid
flowchart TD
 N01{"N01【指令-判断】阶段是否已登记且候选组非空"} -- 否 --> R01["R01【指令-返回】返回 {内部不一致,0,0,0}"]
 N01 -- 是 --> X03["X03【函数调用】F-A202(3)"]
 X03 --> N02["N02【指令-异常边界】try 构造局部预备反向位置向量并 reserve(候选数)"]
 N02 --> N03{"N03【指令-判断】是否捕获 std::bad_alloc 或等价资源失败"}
 N03 -- 是 --> R02["R02【指令-返回】返回 {资源失败,0,0,0}"]
 N03 -- 否 --> N04["N04【指令-赋值】候选位置=0"]
 N04 --> N05{"N05【指令-判断】候选位置是否等于候选数"}
 N05 -- 否 --> N06["N06【函数调用】视图.节点是本会话候选(候选[位置].特征值) -> 是否本会话候选"]
 N06 --> N07{"N07【指令-判断】结果是否为 true"}
 N07 -- 否 --> R03["R03【指令-返回】返回 {入口拒绝,0,0,0}"]
 N07 -- 是 --> N08["N08【函数调用】视图.读取候选节点类型(候选[位置].特征值) -> 可选节点类型"]
 N08 --> N09{"N09【指令-判断】节点类型是否存在且等于特征值"}
 N09 -- 否 --> R03
 N09 -- 是 --> N10["N10【函数调用】视图.读取候选节点稳定主键(候选[位置].特征值) -> 可选稳定主键"]
 N10 --> N11{"N11【指令-判断】主键是否存在、命名域=特征值身份且键值非零"}
 N11 -- 否 --> R03
 N11 -- 是 --> N12["N12【指令-操作】把候选[位置]的稳定 list 迭代器追加到局部预备反向位置"]
 N12 --> N13["N13【指令-赋值】候选位置加一"]
 N13 --> N05
 N05 -- 是 --> X01["X01【函数调用】F-A213(1) -> 是否命中"]
 X01 --> X02{"X02【指令-判断】是否命中"}
 X02 -- 是 --> R01
 X02 -- 否 --> N14["N14【指令-操作】以 try_to_lock 取得记录仓独占锁"]
 N14 --> N15{"N15【指令-判断】是否取得独占锁"}
 N15 -- 否 --> R04["R04【指令-返回】返回 {许可拒绝,0,0,0}"]
 N15 -- 是 --> N16{"N16【指令-判断】记录仓.结构版本_是否小于 uint64 最大值"}
 N16 -- 否 --> R05["R05【指令-返回】释放独占锁并返回 {资源失败,0,0,0}"]
 N16 -- 是 --> N17["N17【指令-赋值】仓扫描位置=首条目"]
 N17 --> N18{"N18【指令-判断】仓扫描是否结束"}
 N18 -- 否 --> N19{"N19【指令-判断】当前仓条目.已发布是否为 true"}
 N19 -- 否 --> R06["R06【指令-返回】释放独占锁并返回 {内部不一致,0,0,0}"]
 N19 -- 是 --> N20["N20【指令-赋值】仓扫描位置移至下一条"]
 N20 --> N18
 N18 -- 是 --> N21["N21【指令-赋值】候选核对位置=0"]
 N21 --> N22{"N22【指令-判断】候选核对位置是否等于候选数"}
 N22 -- 否 --> N23["N23【指令-读取】在已发布仓中按(特征值,原始值版本)查找候选[位置]同键条目"]
 N23 --> N24{"N24【指令-判断】同键分类"}
 N24 -- 同义已存在 --> R07["R07【指令-返回】释放独占锁并返回 {版本漂移,0,0,0}"]
 N24 -- 异义 --> R08["R08【指令-返回】释放独占锁并返回 {身份冲突,0,0,0}"]
 N24 -- 未找到 --> N25["N25【指令-赋值】候选核对位置加一"]
 N25 --> N22
 N22 -- 是 --> N25A["N25A【指令-操作】以 noexcept swap 把局部预备反向位置移入成员反向位置_"]
 N25A --> N26["N26【指令-操作】以 list::splice 无分配地把全部私有条目移入仓尾"]
 N26 --> N27["N27【指令-操作】阶段置已准备并保留独占锁"]
 N27 --> R09["R09【指令-返回】返回 {候选已确认,0,0,0}"]
```

节点合同：N06/N08/N10 对每个候选逐项绑定当前真实准备视图的三个入口，不存在合成读取函数。本版本所有新记录只绑定同一会话新值候选；已发布值的幂等请求在进入执行器或回调第一写前收口。准备阶段不得返回 `已提交/幂等读回`；锁内同义只表示写前事实推进并返回核心 `版本漂移`。唯一分配点 N02 在取得仓锁前完成；N12 只改局部预备向量。所有视图和仓校验通过后，N25A/N26/N27 依次只做 noexcept swap、无分配 splice 和阶段赋值；因此任一较早失败保持成员反向位置、私有 list、仓和阶段的调用前状态。
