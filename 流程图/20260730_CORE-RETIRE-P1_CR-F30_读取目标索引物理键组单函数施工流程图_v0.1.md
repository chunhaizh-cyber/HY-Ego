# CR-F30 读取目标索引物理键组单函数施工流程图

日期：2026-07-30
版本：v0.1
创建侧代码基线：`57866ac5ca63235ed12da60f635d29f1aacd718b`

## 函数合同

```text
根函数：可重建索引仓库::读取目标索引物理键组
归属模块 / 文件 / 层级：海中鱼巣.核心.仓库.可重建索引 / 海中鱼巣/核心/仓库.可重建索引.ixx / 仓库公开只读层
完整签名：std::vector<索引物理键> 可重建索引仓库::读取目标索引物理键组(const 可重建索引目标& 目标) const
参数：目标，const 引用，只在调用期借用；只含目标种类、节点句柄、关系句柄；必须完整且本包仅接受节点目标，不需要伪造索引物理键
返回：调用方独占的已发布有效物理键副本组；空组合法；五字段严格升序
非法值与异常：`可重建索引目标完整(目标)==false` 或非节点目标返回空组；目标节点普通读取无效、反向键缺正向记录、正反目标错配或重复键属于结构不一致，抛固定前缀 CORE-RETIRE-P1: 的 std::logic_error；复制/排序的 std::bad_alloc/std::length_error 原样传播，不返回半结果
```

调用者：CR-F07。被调图：CR-F31 普通分支。

```mermaid
flowchart TD
    X00A["X00A【函数调用】bool 可重建索引目标完整(const 可重建索引目标& 目标) noexcept：输入目标→目标；接收目标完整"] -- "完成" --> N01{"N01【指令-判断】目标完整且目标.目标种类为节点"}
    N01 -- "否" --> R01["R01【指令-返回】返回空键组"]
    N01 -- "是" --> X00["X00【函数调用】std::optional<节点直接身份记录> 节点直接身份仓库::读取节点(节点句柄 节点) const：目标.节点→节点；接收目标节点记录"]
    X00 -- "完成" --> V00{"V00【指令-判断】目标节点记录存在、有效且句柄等于目标.节点"}
    V00 -- "否" --> E01["E01【指令-异常】抛出 std::logic_error；消息=CORE-RETIRE-P1:索引目标节点不可读"]
    V00 -- "是" --> N02["N02【指令-操作】取得索引仓共享锁"]
    N02 -- "完成" --> N02A["N02A【指令-读取】按目标标识定位反向键组；不存在则使用空组"]
    N02A -- "完成" --> N02B["N02B【指令-赋值】反向位置=0；候选键组置空"]
    N02B -- "完成" --> B02{"B02【指令-判断】反向位置到达反向键组末尾"}
    B02 -- "否" --> N02C["N02C【指令-读取】按当前反向键定位正向条目"]
    N02C -- "完成" --> V02{"V02【指令-分类】当前反向键的正向绑定与候选可见性"}
    V02 -- "缺正向/错目标/重复反向键/非法候选组合" --> N02E["N02E【指令-操作】释放索引仓共享锁"]
    N02E -- "完成" --> E02["E02【指令-异常】抛出 std::logic_error；消息=CORE-RETIRE-P1:索引正反绑定不一致"]
    V02 -- "其它事务未发布创建" --> P02["P02【指令-推进】反向位置加一"]
    V02 -- "普通可见或待移除写前" --> N02D["N02D【指令-操作】复制当前反向键到候选键组"]
    N02D -- "完成" --> P02
    P02 -- "完成" --> B02
    B02 -- "是" --> N03["N03【指令-操作】释放索引仓共享锁"]
    N03 -- "完成" --> N03A["N03A【指令-赋值】验证位置=0"]
    N03A -- "完成" --> N04{"N04【指令-判断】验证位置是否到达候选键组末尾"}
    N04 -- "否" --> X01["X01【函数调用】std::optional<可重建索引记录> 可重建索引仓库::读取索引物理键核心_(const 索引物理键& 键,std::optional<std::uint64_t> 事务序号) const：当前键→键，std::nullopt→事务序号；接收发布记录"]
    X01 -- "完成" --> N05{"N05【指令-判断】发布记录存在且目标与输入目标相同"}
    N05 -- "否" --> E02
    N05 -- "是" --> N07["N07【指令-推进】验证位置加1"]
    N07 -- "完成" --> N04
    N04 -- "是" --> N08["N08【指令-操作】按所有者身份、命名域、键格式版本、探测规则版本、键值升序排序"]
    N08 -- "完成" --> V01{"V01【指令-判断】排序后物理键无重复；目标一致性已由每次 X01/N05 证明"}
    V01 -- "否" --> E02
    V01 -- "是" --> R02["R02【指令-返回】移动返回已发布有效键组"]
```

关键边界：无事务重载；普通反向读取只见发布基线和待移除写前，不见其它事务未发布创建。排除未发布创建是合法可见性裁决，不能用过滤或去重掩盖悬空键、错目标、重复反向绑定或非法候选组合。目标节点读取与逐键 CR-F31 调用均发生在索引仓共享锁之外；CR-F31 反向列出本图为调用者。
