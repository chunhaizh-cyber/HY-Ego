# F-W102 换代实例槽位当前值

根函数：`带值结构写入结果<实例特征槽位值式材料> 节点直接特征写入数据操作::换代实例槽位当前值(const 实例槽位当前值换代请求& 请求)`

归属：节点直接特征写入数据操作模块；待新增。

```mermaid
flowchart TD
 N00{"N00【指令-判断】实例槽、写前关系/值、来源、新值主键、写前版本和新三态原始值均有效"} -- 否 --> N17["N17【指令-返回】入口拒绝/精确原因/空材料/版本0"]
 N00 -- 是 --> P01["P01【函数调用】事务域_.取得读取许可() -> 事务外结构读取许可"]
 P01 --> P02{"P02【指令-判断】读取许可是否有效"}
 P02 -- 否 --> P03["P03【指令-返回】许可竞争/并发许可失败/空材料/版本0"]
 P02 -- 是 --> N00A["N00A【函数调用】节点仓_.读取节点审计(请求.来源) -> 来源审计"]
 N00A --> N00B{"N00B【指令-判断】来源节点是否已发布且有效"}
 N00B -- 否 --> N17
 N00B -- 是 --> N01["N01【函数调用】F-W103 读取实例槽位当前材料(事务外结构读取许可, 请求.实例槽) -> 写前材料"]
 N01 --> N01A{"N01A【指令-判断】写前材料结果分类"}
 N01A -- 成功且材料/版本完整 --> N02["N02【函数调用】节点仓.读取稳定主键当前身份(请求.新特征值主键) -> 新值身份"]
 N01A -- 合法未找到或成功但材料/版本不完整 --> N18["N18【指令-返回】返回 内部错误/权威读回不一致/空材料/版本0"]
 N01A -- 许可竞争/读取版本漂移/资源失败/其它内部错误 --> N18A["N18A【指令-返回】原样保留写前读回的状态与精确原因；材料空、版本0"]
 N02 --> N03{"N03【指令-判断】新值主键身份状态与当前槽材料组合"}
 N03 -- 当前有效且当前槽已指向该新值 --> N04["N04【函数调用-异常边界】try 关系仓_.读取全部关系审计() -> 幂等关系审计"]
 N04 --> N04R{"N04R【指令-判断】是否捕获 std::bad_alloc 或等价资源失败"}
 N04R -- 是 --> N20
 N04R -- 否 --> N04A{"N04A【指令-判断】写前关系已按请求失效且写前值/版本可审计，当前材料的新值主键映射、来源和新三态原始值与请求逐字段相等"}
 N04A -- 是 --> N05["N05【指令-返回】返回 幂等读回/无/当前材料/当前材料非零版本"]
 N04A -- 否 --> N06["N06【指令-返回】返回入口拒绝/同键异义或写前漂移；零写入"]
 N03 -- 未占用且当前槽仍等于写前三元组 --> N15["N15【函数调用】节点仓.读取命名域高水位(特征值身份) -> 值高水位"]
 N03 -- 历史占用/当前有效但非请求新值/当前槽已推进到其它值 --> N06
 N03 -- 入口拒绝或内部不一致 --> N18
 N15 --> N16{"N16【指令-判断】新值键值是否等于值高水位+1"} -- 否 --> N17["N17【指令-返回】入口拒绝/错版本/空材料/版本0"]
 N16 -- 是 --> P04["P04【指令-操作】释放事务外结构读取许可"]
 P04 --> N07["N07【指令-异常边界】try 构造记录参与者、回调领域失败、候选返回材料和参与者 span"]
 N07 --> N19{"N19【指令-判断】是否捕获 std::bad_alloc 或等价资源失败"}
 N19 -- 是 --> N20["N20【指令-返回】返回 内部错误/资源失败/空材料/版本0"]
 N19 -- 否 --> N08["N08【函数调用】执行器.执行(回调, span{记录参与者}) -> 核心结果"]
 subgraph CB["N08 回调内指令"]
  C01["C01【函数调用】会话.读取关系(写前当前关系) -> 锁内旧关系"] --> C02["C02【函数调用】会话.读取节点(写前当前值) -> 锁内旧值"]
  C02 --> C03["C03【函数调用】F-A106 读取当前记录(写前当前值) -> 锁内旧记录"]
  C03 --> C03A["C03A【函数调用】会话.读取节点(请求.来源) -> 锁内来源"]
  C03A --> C04{"C04【指令-判断】写前三元组、槽端点、旧记录和来源节点是否仍逐项相等且有效"}
  C04 -- 否 --> CF["CF【指令-操作】保存首个领域失败到回调领域失败"]
  CF --> CF2["CF2【函数调用】会话.请求撤销()"]
  CF2 --> CF3["CF3【指令-返回】结束回调"]
  C04 -- 是 --> C05["C05【函数调用】会话.失效关系候选(写前当前关系) -> 失效结果"]
  C05 -- 失败 --> CF
  C05 -- 成功 --> X07["X07【函数调用】F-W312(7) -> 固定结果"]
  X07 --> X07A{"X07A【指令-判断】固定结果是否有值"}
  X07A -- 是 --> CF
  X07A -- 否 --> C06["C06【函数调用】会话.创建节点候选(特征值,新特征值主键) -> 新值"]
  C06 -- 失败 --> CF
  C06 -- 成功 --> X08["X08【函数调用】F-W312(8) -> 固定结果"]
  X08 --> X08A{"X08A【指令-判断】固定结果是否有值"}
  X08A -- 是 --> CF
  X08A -- 否 --> C07["C07【函数调用】会话.创建关系候选(特征实例角色,实例槽,新值,3) -> 新角色3"]
  C07 -- 失败 --> CF
  C07 -- 成功 --> X09["X09【函数调用】F-W312(9) -> 固定结果"]
  X09 --> X09A{"X09A【指令-判断】固定结果是否有值"}
  X09A -- 是 --> CF
  X09A -- 否 --> C08["C08【函数调用】会话.创建关系候选(因果来源,新值,来源,1) -> 来源关系"]
  C08 -- 失败 --> CF
  C08 -- 成功 --> X10["X10【函数调用】F-W312(10) -> 固定结果"]
  X10 --> X10A{"X10A【指令-判断】固定结果是否有值"}
  X10A -- 是 --> CF
  X10A -- 否 --> C09["C09【函数调用】会话.创建索引候选(值物理键,节点目标新值) -> 值索引"]
  C09 -- 失败 --> CF
  C09 -- 成功 --> C10["C10【函数调用】会话.读取索引物理键(值物理键) -> 索引读回"]
  C10 -- 缺失/不等 --> CF
  C10 -- 相等 --> X11["X11【函数调用】F-W312(11) -> 固定结果"]
  X11 --> X11A{"X11A【指令-判断】固定结果是否有值"}
  X11A -- 是 --> CF
  X11A -- 否 --> C11["C11【函数调用】F-A101 登记记录候选({新值,1,新原始值}) -> 记录结果"]
  C11 -- 非成功 --> CF
  C11 -- 成功 --> C12["C12【函数调用】会话.读取关系审计(写前当前关系) -> 失效审计"]
  C12 -- 缺失/不等 --> CF
  C12 -- 相等 --> C13["C13【函数调用】会话.读取节点(新值) -> 新值读回"]
  C13 -- 缺失/不等 --> CF
  C13 -- 相等 --> C14["C14【函数调用】会话.读取关系(新角色3) -> 角色3读回"]
  C14 -- 缺失/不等 --> CF
  C14 -- 相等 --> C15["C15【函数调用】会话.读取关系(来源关系) -> 来源读回"]
  C15 -- 缺失/不等 --> CF
  C15 -- 相等 --> C15A["C15A【指令-异常边界】try 用写前槽材料、新值、角色3、来源关系和记录构造候选返回材料"]
  C15A --> C15B{"C15B【指令-判断】是否捕获 std::bad_alloc 或等价资源失败"}
  C15B -- 是 --> CFR["CFR【指令-操作】保存首个 内部错误/资源失败"]
  CFR --> CF2
  C15B -- 否 --> C16["C16【函数调用】会话.请求提交()"]
 end
 N08 --> N09{"N09【指令-判断】按详细设计8.2映射核心结果；是否已提交"}
 N09 -- 否 --> N10["N10【指令-返回】返回映射结果或回调领域失败"]
 N09 -- 是 --> N11["N11【函数调用】F-A110 读取本事务发布结构版本() -> 本事务版本"]
 N11 --> N12{"N12【指令-判断】候选返回材料存在且本事务版本成功非零"}
 N12 -- 否 --> N13["N13【指令-返回】内部错误/权威读回不一致；材料空、版本0"]
 N12 -- 是 --> N14["N14【指令-返回】成功/无/候选返回材料/本事务版本"]
```

节点合同：新值已占用的幂等 / 异义分类先于高水位校验；幂等必须同时证明新值主键映射、已失效写前关系、写前值/版本、请求来源和完整新原始值同义。P01 的一次结构读取许可覆盖来源、F-W103 当前槽组合、新值主键、幂等关系审计和高水位；真实写入在 P04 释放许可后才进入独占执行器。现行规则只接受已发布来源，事务外 N00A/N00B 与回调内 C03A/C04 双重准入。只有真实新建值分支要求键值为高水位+1。不重挂旧关系；失效旧关系后的任一失败必须撤销到旧关系仍有效。执行器返回后不读取槽当前状态。
