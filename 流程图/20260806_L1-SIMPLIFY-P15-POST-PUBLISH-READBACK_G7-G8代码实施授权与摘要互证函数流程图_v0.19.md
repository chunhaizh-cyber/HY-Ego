# L1-SIMPLIFY-P15 G7/G8 代码实施授权与摘要互证函数流程图 v0.19

更新时间：2026-08-06

依据：P15 v0.19 详细设计；正式 HEAD `7681de05877dc4aebdac3a1300323c80ecc84c3f`。

本图冻结执行目标、26 文件允许范围和两条专属记录路径；代码是否已实现只由执行侧正式验证证明。

## 权限与范围

```text
计划可执行 + S0 通过
  -> 允许在24项代码/模块/自检白名单内施工
  -> 允许写施工记录/20260806_L1-SIMPLIFY-P15-POST-PUBLISH-READBACK_施工记录.md
  -> 允许写验证记录/20260806_L1-SIMPLIFY-P15-POST-PUBLISH-READBACK_验证记录.md
  -> 完整允许范围=24项代码/模块/自检+2条记录=26文件
  -> 除上述两条记录外禁止新增文件；禁止白名单外、P0-FIX、路线、旧WORLD-TREE、P63/P64及异主WIP
```

## G7/G8 施工主流程

```mermaid
flowchart TD
 A["真实写入口 DTO"] --> B["固定 G7/G8 字段编码"]
 B --> C["领域意图摘要"]
 C --> D["provider 前探测幂等"]
 D -->|同义| E["首次账/结果 DTO 读回，provider=0/0"]
 D -->|异义或格式错误| F["拒绝，无业务载荷"]
 D -->|未找到| G["读取 provider/事实"]
 G --> H["独立 G0 规范化写集摘要"]
 H --> I{"组/版本/标签/领域摘要/G0 互证"}
 I -->|失败| F
 I -->|成功| J["核心保留调用方摘要"]
 J --> K["一次 L1 v2 提交"]
 K --> L["首次结果 DTO/发布后读回"]
 L --> M["双账恢复重算与隔离"]
 M --> N["施工记录/验证记录分别留证"]
```

## 入口映射

```mermaid
flowchart LR
 T1["T01 L1状态登记请求 #15000601"] --> R1["L1状态登记结果"]
 T2["T02 首个基准状态发布请求 #15000602"] --> R2["实际存在I64基准状态结果"]
 M1["M01 特征比较登记请求 #15000801"] --> Q1["特征比较登记结果"]
 M2["M02 目标判断合同登记请求 #15000802"] --> Q2["目标判断比较合同结果"]
 M3["M03 状态迁移合同登记请求 #15000803"] --> Q3["状态迁移比较合同结果"]
 R1 --> V["按同一点恢复重算"]
 R2 --> V
 Q1 --> V
 Q2 --> V
 Q3 --> V
 V --> W["两条专属记录路径留证"]
```

G1—G6、公共编码、G0 互证、P-L1/P-I/P-S、32/18/50、16/50 和 24 项代码白名单保持 v0.18；本图新增的两条记录只承载施工/验证证据，不改变机器合同。
