# NODE-TYPED-MIGRATION NT-P4 九段记录、严格恢复与旧格式拒绝施工流程图

更新时间：2026-07-24

## 依据

```text
规范/4070_子规范_权威结构快照恢复候选与运行期原子发布.md
规范/详细设计/NODE-TYPED-MIGRATION_NT-P4_九段记录ABI严格恢复与旧格式拒绝详细设计.md
NT-C4/v0.4
```

## 身份与边界

本图是正式施工流程图；文件是恢复材料，只有完整隔离候选通过互证并首次发布后才成为运行期事实。

## 流程图

```mermaid
flowchart TD
    A["取得九段同代次冻结材料"] --> B["按 NT-C4/v0.4 逐字段规范编码"]
    B --> C["写段摘要与容器摘要"]
    C --> D["严格解析：头、段、记录帧、枚举、上限"]
    D --> E{"材料完整？"}
    E -- "否" --> R["材料拒绝；宿主不变"]
    E -- "是" --> F["按1→9装入隔离仓"]
    F --> G["重建索引、自我和业务投影"]
    G --> H["跨段互证、旧运行占用失效、再冻结相等"]
    H --> I{"全部闭合？"}
    I -- "否：结构不一致" --> X["内部不一致；销毁整个候选"]
    I -- "否：资源不足" --> Y["资源失败；销毁整个候选"]
    I -- "是" --> J["形成单次移动的已验证候选"]
    J --> K{"#352 空宿主首次发布？"}
    K -- "已有当前上下文" --> Q["逻辑内返回；当前宿主不变"]
    K -- "成功" --> P["发布后完整读回"]
    L["旧格式输入"] --> M{"支持版本集合命中？"}
    M -- "否：当前集合为空" --> N["具名拒绝；输入不变、输出为空"]
    M -- "是" --> O["当前不可达；必须先升版冻结完整旧格式与逐字段映射"]
```

## 关键边界

```text
九段记录 ABI 唯一由 #349 提供，#350/#351 只读消费；
未知或旧格式不得猜测补齐；恢复失败不得回退全新；发布只发生一次。
```
