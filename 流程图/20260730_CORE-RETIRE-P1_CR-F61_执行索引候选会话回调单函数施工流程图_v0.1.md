# CR-F61 执行索引候选会话回调单函数施工流程图

版本：v0.1
代码基线：`57866ac5ca63235ed12da60f635d29f1aacd718b`

## 函数合同

```text
根函数：执行索引候选会话回调
物理位置：海中鱼巣/核心/自检.节点直接身份结构写入.ixx，CR-F47 之前内部区
完整签名：void 执行索引候选会话回调(索引候选会话回调上下文& 上下文, 节点直接身份结构写入会话& 会话)
参数：一次执行期借用；场景1—5；不适用字段不得被读取
返回：void；创建/移除结果、目标键组和单键命中证据写上下文；所有场景显式撤销
```

```mermaid
flowchart TD
    N01{"N01【指令-分类】上下文.场景"} -- "1 创建" --> X01["X01【函数调用】节点直接身份带值结构写入结果<可重建索引记录> 节点直接身份结构写入会话::创建索引候选(可重建索引记录 记录)：上下文.创建记录→记录；接收创建操作"]
    N01 -- "2—5 移除" --> X02["X02【函数调用】节点直接身份索引移除结果 节点直接身份结构写入会话::移除索引候选(const 索引物理键& 键)：上下文.移除键→键；接收移除操作"]
    N01 -- "其它" --> X00["X00【函数调用】void 节点直接身份结构写入会话::记录失败(const 节点直接身份结构写入结果& 失败) noexcept：入口拒绝→失败；返回void"]
    X01 -- "创建结果" --> X03["X03【函数调用】std::vector<索引物理键> 节点直接身份结构写入会话::读取目标索引物理键组(const 可重建索引目标& 目标)：上下文.查询目标→目标；接收本事务目标键组"]
    X02 -- "移除结果" --> X03
    X03 -- "键组已接收" --> X04["X04【函数调用】std::optional<可重建索引记录> 节点直接身份结构写入会话::读取索引物理键(const 索引物理键& 键)：上下文.移除键或创建记录.物理键→键；接收单键记录"]
    X04 -- "单键已接收" --> N02["N02【指令-赋值】上下文.本事务单键命中=单键记录存在"]
    N02 -- "证据已写" --> X05["X05【函数调用】void 节点直接身份结构写入会话::请求撤销() noexcept：无参数；返回void"]
    X00 -- "失败已记录" --> R01["R01【指令-返回】返回void"]
    X05 -- "撤销已请求" --> R01
```

调用者：CR-F47；绑定方式为 `std::bind_front(执行索引候选会话回调,std::ref(上下文))`。直接被调图：CR-F53、F13、F07、F34。
