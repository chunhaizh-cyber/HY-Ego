# 权威状态快照隔离恢复与运行期上下文一次发布流程图 v0.2

更新时间：2026-07-18

## 施工元数据

```text
图类型：施工流程图 / JY-390 当前物理合同修订
当前可执行计划：#295 / DQ-187 / PERSIST-S1-P0 纯只读事实复核
设计门控计划：#226 / DQ-118 / PERSIST-S1-B v0.2
绑定详细设计：规范/详细设计/权威状态快照隔离恢复与运行期上下文一次发布详细设计.md
替代关系：本图替代 20260711 v0.1 作为当前施工路由；v0.1 只保留历史证据
验证方式：MD / HTML Mermaid 同源、严格规范检查、git diff 检查
```

## 依据

```text
AGENTS.md
规范/000_项目规则总纲.md
规范/001_规则迁移清单.md
规范/代码文件建立归属与模块命名规范.md
规范/迁移路线权力分层规范.md
实施记录/20260718_PERSIST-S1-D1_权威冻结与九段物理合同实施前设计审计矩阵.md
实施记录/20260718_PERSIST-S1-D1_权威冻结与九段物理合同实施前设计审计_Codex断点清单.md
计划/20260718_PERSIST-S1-P0_生产运行期所有权与概念活动承载当前事实复核计划_v0.1.md
计划/20260718_PERSIST-S1-B_HYSNAP01规范编码与原子文件发布代码实施切片_v0.2.md
```

## 说明

v0.2 把旧“一个 #226 同时解决所有权、冻结、编码和发布”的路线拆开。当前先由 #295 只读冻结生产旧栈消费者与概念活动字段；设计角色随后生成窄 P0 代码计划。只有生产入口真实使用唯一运行期上下文、段 9 已有新运行期唯一所有者后，才实施 PERSIST-S1-A 同冻结材料；#226 只承担 PERSIST-S1-B 规范编码、严格自读和原子文件发布。

恢复仍在外部快照严格解析后构造隔离候选，通过屏障后发布到同一空宿主。任何阶段都不得用旧入口对象、空段、日志、SQL 或显示补齐机器事实。

## 流程图

```mermaid
flowchart TD
    A["当前事实：#294 已通过；生产入口仍是旧仓库 / 旧服务栈；新上下文段 9 无所有者"] --> B["#295 只读复核：逐调用点冻结旧生产消费者、宿主切换面和概念活动字段"]
    B --> B1{"M0—M6 与 A01—A18 是否形成唯一事实矩阵"}
    B1 -->|"否：当前事实冲突"| R0["退回高推理设计角色：不改代码、不扩范围"]
    B1 -->|"是"| D1["设计角色归档 #295；生成窄 P0-A / P0-B / P0-C 代码计划"]

    D1 --> P0A["P0-A：完整候选初始化后，由生产最小组合点首次发布唯一运行期上下文"]
    P0A --> P0B["P0-B：仍写机器事实的兼容消费者按不重叠批次改接同身份租约 / 正式业务公开面"]
    P0B --> P0C["P0-C：概念活动唯一所有者或确定性重建参与者进入新运行期；上一候选图仍是瞬态缓存"]
    P0C --> G1{"生产旧对象是否已退出机器事实所有者；段 9 是否有唯一当前所有者"}
    G1 -->|"否"| L0["依赖门控返回：#226、恢复链继续不可执行"]
    G1 -->|"是"| F1["PERSIST-S1-A：从已发布正式租约取得同域独占许可"]

    F1 --> F2["逐参与者快速复制：四仓库全状态 / 高水位、特征值侧表、系统角色、索引所有者、领域登记、概念活动"]
    F2 --> F3{"九段材料是否同域、同冻结身份、集合完整且内部一致"}
    F3 -->|"否"| R1["追根因解决：释放许可，不编码当前内部矛盾"]
    F3 -->|"是"| F4["释放独占许可；形成不可变九段冻结材料"]

    F4 --> E1["PERSIST-S1-B / #226：按稳定完整身份规范排序和显式小端编码"]
    E1 --> E2["单 / 多线程编码必须得到相同 HYSNAP01 总头、九段、段校验和跨段摘要"]
    E2 --> E3["CREATE_NEW 临时文件 -> 写穿 -> 当前格式严格自读并逐值比较"]
    E3 --> E4{"提交点前结果"}
    E4 -->|"路径 / 权限 / 空间等允许失败"| L1["逻辑内返回：清理本次临时候选，旧正式快照保持"]
    E4 -->|"写入方无法读回自己的文件"| R2["追根因解决：不得替换旧快照"]
    E4 -->|"完整"| E5["同卷替换正式快照并最终严格读回；仅此时返回已发布"]

    E5 --> S1["启动调用方显式选择：全新初始化或从指定快照恢复"]
    S1 --> S2{"启动策略是否明确且互斥"}
    S2 -->|"否"| L2["逻辑内返回：不猜模式、不删除快照、不启动线程"]
    S2 -->|"全新"| N1["构造完整全新候选；复用同一空宿主首次发布合同"]
    S2 -->|"恢复"| Q1["RECOVERY-S1：严格校验总头 / 段表 / 九段 / 长度 / 校验 / 摘要"]
    Q1 --> Q2{"外部文件是否受支持"}
    Q2 -->|"否"| L3["逻辑内拒绝：丢弃候选，不回退全新初始化"]
    Q2 -->|"是"| Q3["按指定身份构造隔离四仓库；索引只暂存候选"]
    Q3 --> Q4["RECOVERY-S2：恢复侧表与角色；所有者重算索引；重建并比较概念活动"]
    Q4 --> Q5["RECOVERY-S3A：任务中断收口与跨段屏障；形成绑定候选身份的发布许可"]
    Q5 --> Q6{"隔离候选是否完整且无防复活冲突"}
    Q6 -->|"外部材料无效"| L4["逻辑内返回：丢弃整个隔离候选"]
    Q6 -->|"当前实现内部矛盾"| R3["追根因解决：不发布、不扫描修复"]
    Q6 -->|"是"| U1["RECOVERY-S3B：适配为同一运行期上下文类型并消费同一空宿主恢复发布能力"]

    N1 --> U2{"唯一宿主是否为空；候选是否完整"}
    U1 --> U2
    U2 -->|"否"| L5["逻辑内拒绝：首版不在线热替换"]
    U2 -->|"是"| U3["首次发布成功后，所有消费者只取得同身份租约"]
    U3 --> U4["RECOVERY-S3C：线程后启；SQL 审计和控制面板最后只读启动"]
    U4 --> Z["完成：受控 HYSNAP01 发布 / 启动恢复路径；事件重放、断电耐久、跨版本与恰好一次仍后置"]
```

## 关键边界

```text
1. #295 只冻结事实，不修改生产调用，也不直接裁决最终文件许可。
2. P0 必须证明生产入口实际持有唯一上下文；宿主 / 租约自检通过不等于生产已经切换。
3. 旧概念图服务不进入新装配。段 9 必须有新运行期唯一所有者或已实现的确定性重建参与者，禁止伪造空段。
4. O1 继续复用段 2—5且无第十段；本路线不接 O1 统计、学习、晋级、代际或自动动作。
5. PERSIST-S1-A 冻结内只复制和值式复核；排序、编码、校验和文件 I/O 全在冻结外。
6. #226 只消费不可变九段材料，不修改仓库、领域服务、运行期上下文或生产所有权。
7. HYSNAP01 仍固定九类必需段、显式小端、规范顺序、段校验和跨段摘要；索引候选必须带唯一所有者声明。
8. 外部坏快照逻辑拒绝；当前写入方自读失败、内部冻结矛盾或已验证导入失败追根因。
9. 恢复只在隔离候选完成，外部文件、SQL、日志、显示、缓存和事件段不裁决当前事实。
10. 全新与恢复共用同一空宿主首次发布槽；已有上下文时拒绝，首版不在线热替换。
11. 发布后才启动线程；SQL 审计和只读控制面板最后启动且不能修复领域事实。
12. 新生产逻辑与自检必须是真模块并分离；入口只保留最小 import、一次调用和总结果。
13. 完成上述路径仍不证明事件重放、断电耐久、跨版本迁移、任务恰好一次或旧持久化能力迁移。
```
