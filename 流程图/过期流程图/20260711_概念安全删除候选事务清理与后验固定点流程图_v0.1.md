# 概念安全删除候选事务清理与后验固定点流程图

更新时间：2026-07-12

## 施工元数据

```text
图类型：施工流程图
绑定计划：#216 CONCEPT-S8-S1、#217 STRUCT-TXN-S1、#218 CONCEPT-S8-S2、#219 CONCEPT-S8-S3、#220 CONCEPT-S8-S4；#219 已按 JY-286 删除端点关系审计合同补修
绑定详细设计：规范/详细设计/概念安全删除候选事务清理与后验固定点详细设计.md
业务前置：#198、#199、#216-#218、#243 已完成；#219 按 JY-286 保留草案续作，最终回归 530
允许文件：分别以五份绑定计划的允许文件为准，本图不授予跨切片合并实施许可
禁止文件：需求 / 任务 / 方法语义扩展、命名自动解绑、持久化恢复提交、SQL、控制面板写入、D455、体素和外设
验证方式：代码切片执行 Debug x64、完整自检、连续 20 轮、并发端点交错、规范检查与精确范围扫描
不得宣称：#219 已实现、安全物理删除已完成、运行期主装配已接域或崩溃恢复已完成
```

## 依据

```text
AGENTS.md
规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md
规范/规范目录.md
规范/代码文件建立归属与模块命名规范.md
规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md
规范/4040_子规范_不透明结构事务候选确认撤销与最后发布.md
规范/详细设计/写入事务详细设计.md
规范/详细设计/概念图自动生长与抽象关系树形视图详细设计.md
规范/详细设计/概念命名用途与生命周期治理详细设计.md
流程图/20260712_中央自检运行器与第一批领域自检迁移流程图_v0.1.md
规范/详细设计/中央自检运行器与第一批领域自检迁移详细设计.md
实施记录/20260712_中央自检运行器与第一批领域自检逻辑提取引用矩阵.md
实施记录/20260712_概念命名目标与安全删除后继逻辑提取引用矩阵.md
实施记录/20260712_SELFTEST-MIGRATION-B1A_执行时序漂移与阶段化修订_Codex断点清单.md
实施记录/20260711_CONCEPT-S8-S0_安全物理删除当前代码事实复核_Codex断点清单.md
实施记录/20260711_ENTRY-MOD-S0_入口与自检承载当前代码事实复核_Codex断点清单.md
实施记录/20260711_CONCEPT-S7_冷却退役与重新激活代码实施_Codex断点清单.md
海中鱼巣/核心/节点仓库.h
海中鱼巣/核心/主信息仓库.h
海中鱼巣/核心/关系仓库.h
海中鱼巣/核心/索引仓库.h
海中鱼巣/核心/写入事务.h
海中鱼巣/领域/概念图算法.h
海中鱼巣/领域/概念图服务.h
```

## 说明

本图只处理当前活动图中普通概念的第一版安全删除。四根永久拒绝；目标必须已退役、零当前有效实例支持、零全部外部有效关系。删除候选和事务写集都是值式材料，不新增第四生命周期阶段，也不由日志、显示、统计或 SQL 授权。

第一版采用统一读取隔离，不做版本倒退式回滚：普通读取 / 新增 / 关系索引变化持共享许可，节点 / 主信息身份删除与事务提交持独占许可。所有分配、完整输入重建、版本复核和清理包构造都在提交前完成；进入提交段后不再提供普通失败返回，且不声明进程崩溃恢复。

## 流程图

```mermaid
flowchart TD
    A["开始：#198 已形成安全删除十类事实矩阵"] --> B["S8-S1：提交目标概念、预期活动版本和候选预算"]
    B --> C{"目标是当前活动图中的普通概念，非四根，生命周期为退役，且零有效支持 / 零外部引用"}
    C -->|"否"| L1["逻辑内返回：不生成删除候选，结构不变化"]
    C -->|"是"| D["读取完整活动快照、签名、生命周期、相关关系和同类别完整当前概念集合"]
    D --> E["从完整集合移除目标，复用固定点共享直接边与传递约简算法重算剩余结构"]
    E --> F{"候选计算结果"}
    F -->|"版本漂移、预算不足或允许的材料不足"| L2["逻辑内返回：只返回待重算 / 拒绝材料，不写结构"]
    F -->|"完整输入却出现环、根不可达或签名矛盾"| R1["追根因解决：停止依赖路径，不用局部重挂修补"]
    F -->|"领域影响已形成"| G["输出值式删除影响候选：基准版本、目标身份、生命周期关系、相关关系、受影响分量和删除后直接边"]
    G --> H0{"#244-#243 是否依次完成，阶段化总成、模块登记和入口边界是否与 #217 假定合同一致"}
    H0 -->|"否"| L5["逻辑内返回：保留 #217 依赖门控，退回设计计划修订，不改代码"]
    H0 -->|"是"| H["STRUCT-TXN-S1：数据桥固定共享状态、域/纪元/序号/类型令牌、移动许可和四函数表"]
    H --> H1["协调模块维护锁、活动许可表、线程归属、重入拒绝和接线工厂"]
    H1 --> I["普通读取/新增/关系索引变化取共享许可；节点/主信息身份删除取独占许可"]
    I --> I1["46 个公开入口逐项接线；关系/索引锁内复制，释放后同令牌读节点，不同时持两个仓库锁"]
    I1 --> I2["A01-A14 由无参 自检.结构事务 构造隔离已接域仓库组并按最终回归 500 登记"]
    I2 --> J["S8-S2：补齐反向主键、审计读取、结构所有者授权、预构造清理包和仓库私有提交接口"]
    J -. "工程承载" .-> J2["领域/自检.概念清理适配.ixx 承载 A01-A12，最终回归阶段顺序 520"]
    J --> K{"四仓库、概念图及结构所有者是否属于同一事务域且接口完整"}
    K -->|"否"| L3["逻辑内返回：事务域未就绪，不取得删除提交许可，不写结构"]
    K -->|"是"| M["S8-S3：取得独占结构事务许可，冻结参与域内公开读写端点"]
    M -. "工程承载" .-> M2["生产：领域/服务.概念安全删除.ixx；验收：自检.概念安全删除.ixx，最终回归阶段顺序 530"]
    M --> M1["独占结构事务令牌只证明隔离域；唯一编排器尚未取得业务提交能力"]
    M1 --> M3["唯一编排器按顺序非阻塞试取活动图独占锁、图写锁和概念用途缓存独占锁"]
    M3 --> M4{"三把服务冻结锁是否全部取得"}
    M4 -->|"否"| L6["逻辑内返回：逆序释放已取锁和结构独占许可；提交环境繁忙，本次不写"]
    M4 -->|"是"| N["冻结语境内重读活动版本、退役关系版本、零支持、零外部引用、全部关系 / 主键 / 登记、所有者授权和目标缓存组"]
    N --> O{"候选是否仍为当前且准入仍成立"}
    O -->|"否"| L4["逻辑内返回：释放独占许可；候选过期，本次不写删除结构"]
    O -->|"是"| P{"完整写集与所有者材料是否和当前结构精确一致"}
    P -->|"否"| R2["追根因解决：提交前停止；禁止扫描补齐、隐藏残留或缩小写集"]
    P -->|"是"| Q["提交前完成全部分配：四仓库包、登记包、目标级缓存包和根 / 节点 / 关系 / 签名齐全的新活动快照"]
    Q --> R{"精确缓存组、登记包及完整删除后快照是否匹配、无环、四根可达且不含目标"}
    R -->|"否"| R3["追根因解决：尚未进入提交段，丢弃预构造包并停止依赖路径"]
    R -->|"是"| R5["唯一编排器创建不可伪造、不可复制的私有提交会话，绑定运行期状态、目标、写集和单调阶段"]
    R5 --> S["进入唯一提交点：提交段不再提供普通失败返回"]
    S --> T["编排器推进私有会话：四仓库 -> 概念登记 -> 目标级统计缓存 -> 完整活动快照"]
    T --> U["精确清除目标缓存且保留同命名空间其它概念；单次交换完整新快照、推进版本并清除上一候选图"]
    U --> V["释放独占结构事务许可，返回值式删除回执和各结构审计句柄"]
    V --> W["S8-S4：普通关系读取为空，显式关系审计按句柄 / 状态可读删除端点历史；主键、登记和缓存无目标"]
    W -. "工程承载" .-> W2["领域/自检.概念删除后验.ixx 承载 A01-A12，最终回归阶段顺序 540"]
    W --> X{"删除后全量固定点、传递约简、无环、四根可达和树投影是否与候选一致"}
    X -->|"否"| R4["追根因解决：不得用日志、显示、数量或读取过滤冒充清理完成"]
    X -->|"是"| Y["重复提交旧候选幂等读回，不推进版本、不重复删除"]
    Y --> Z["同签名真实实例再次形成概念时创建新节点句柄，不复活旧句柄"]
    Z --> AA["完成：第一版安全删除闭环形成正式结构证据"]
```

## 关键边界

`结构事务接线.数据.h` 是传统头可见的纯数据结构桥；`协调.结构事务`、`服务.概念安全删除` 与全部新增自检文件必须以 `ClCompile + export module + import` 形成真模块。生产模块不得依赖自检模块，任何 Axx 正文不得写回 `入口.cpp`。

```text
1. 四根永久拒绝物理删除；活跃或冷却普通概念也拒绝。
2. 第一版只接受零当前有效支持、零全部外部有效关系的退役普通概念，不自动解绑名称、需求、任务、方法或其它领域引用。
3. 删除候选是调用期值式 DTO，只达到“领域影响已形成”；结构写集由 S8-S2 在事务域内补全后才可能提交。
4. 完整当前输入必须来自同一活动版本的全量活动快照、签名和关系，不从局部受影响分量猜测全图。
5. 删除后直接边必须复用固定点共享的签名比较和传递约简算法；禁止另写局部父子移动算法。
6. 第一版结构事务域采用粗粒度共享 / 独占许可。它先保证正确性，不宣称高并发吞吐或分布式事务。
7. 普通关系创建、关系重挂、索引绑定及全部关系 / 索引读取统计从端点复核前持共享许可；节点 / 主信息身份删除持独占许可，端点在共享许可期不可消失。
8. 同一同步调用链只取得一次结构许可；下层通过值式域 / 纪元 / 序号 / 类型令牌进入令牌感知入口，不得递归申请或跨线程携带。
9. 删除提交持独占许可；所有参与公开读取持共享许可，保证外部只能看到完整旧版本或完整新版本。
10. 锁序固定为：结构事务许可 -> 活动图锁_ -> 图写锁_ -> 单个仓库锁；不得同时持有多个仓库锁，也不得持锁调用上层服务、日志、I/O 或回调。
11. 版本倒退会破坏稳定句柄语义，第一版不做逆向回滚；采用提交前完整准备、提交段无普通失败和统一读取隔离。
12. 候选过期、新增支持 / 引用或事务域未接入属于写前逻辑内返回；完整当前输入内部矛盾、写集缺项和提交后结构不一致属于追根因解决。
13. 一旦进入提交段，各包必须已经完成分配和复核，提交函数不得再返回普通失败或创建新材料。
14. 进程崩溃、断电和跨重启恢复不在第一版证明范围；后续必须由持久化恢复专项承接。
15. 普通读取因端点失效返回空不等于关系或索引已清理；必须通过审计读取和反向索引证明真实清零。
16. 删除回执只汇总仓库与服务结构证据，不成为独立权威事实；重复删除由底层审计状态和版本裁决。
17. 统计缓存、显示、日志、SQL 和控制面板均不得阻止、授权或修复删除。
18. 等价概念重建必须走现有实例、签名、固定点和发布路径，获得新节点句柄；历史材料不得复活旧句柄。
19. #218、#219、#220 自检分别由 `自检.概念清理适配`、`自检.概念安全删除`、`自检.概念删除后验` 真模块承载，均导出无参运行入口并返回正式 `自检单元结果`；入口只登记 `CONCEPT-S8-S2 / 最终回归阶段 520`、`CONCEPT-S8-S3 / 最终回归阶段 530`、`CONCEPT-S8-S4 / 最终回归阶段 540` 无捕获回调。
20. 最终回归阶段顺序 520-540 依赖 #244-#243 形成正式阶段总成、#217 同阶段登记 500、#212 同阶段登记 510 且无占用；执行前必须读取实际阶段接口和注册表，漂移时退回设计，不在执行窗口自行改阶段或改号。
21. `概念安全删除服务.h/.cpp` 旧预案撤销；#219 生产编排固定落入 `海中鱼巣/领域/服务.概念安全删除.ixx`，#220 继续消费该模块。
22. #217 只证明可选接域能力和隔离已接域仓库组；当前入口主装配、既有直接删除与失败清理不在本批接域，不得扩大声明为运行期全局隔离完成。
23. #217 独占令牌只证明结构事务域、纪元、许可类型和线程归属，不承载概念安全删除的业务提交授权；取得接线或独占许可的普通调用方仍不能提交任一仓库清理包。
24. 四仓库单包提交入口必须为私有成员，唯一友元是 #219 的 `海中鱼巣::领域::概念安全删除编排器`；不得公开、不得由自检友元调用，也不得仅靠独占令牌授权。
25. `概念安全删除提交会话` 是外部不可构造、不可复制、仅可移动的调用期值式能力，绑定运行期状态身份、目标完整句柄、写集身份和单调提交阶段；只允许唯一编排器在全部重读与预构造通过后创建和推进。
26. #218 自检只验证准备包可丢弃、私有能力不可构造 / 不可复制和公开提交面不存在；#219 自检只经公开顶层 `提交安全删除` 验证完整固定顺序提交，不获得仓库友元或单包直调通道。
27. `概念缓存清理准备包` 的写集单位是目标概念及其完整用途观察组；整命名空间清空会误删合法共存的其它概念，禁止用于安全删除提交。
28. #219 唯一编排器是统计服务和概念图服务安全删除私有端点的唯一友元；它在结构独占许可后按 `活动图锁 -> 图写锁 -> 概念用途缓存锁` 非阻塞试取并冻结读写，不给自检、UI 或普通业务调用方开放私有端点。
29. 概念图清理包必须预构造完整新活动快照，至少冻结活动版本、四根、剩余概念节点、完整签名和全部剩余活动关系；只带剩余签名 / 直接边不是可提交快照。
30. 提交会话阶段固定扩展为关系、索引、主信息、节点、概念登记、统计缓存、活动快照、已完成；统计提交只删除准备包精确键，活动快照最后交换，任一阶段不得用整空间清空或提交后重建兜底。
31. 当前概念图公开写路径可能先持图锁再进入仓库许可；编排器不得在持结构独占许可时阻塞等待图 / 缓存锁。任一冻结锁忙时必须逆序释放并写前逻辑内返回，不自旋、不创建会话、不留下结构变化。
32. `读取关系审计` 与 `获取关系审计记录组` 是显式历史入口，按完整关系 / 源节点句柄和可审计记录状态读取，不以源 / 目标端点当前有效性过滤；普通关系读取、查询、存在判断、父链和数量统计继续要求当前有效端点，不得泄漏删除后关系。
33. 并发候选发布返回“基准版本过期”表示它线性化在删除后，是 A11 允许的后态结果；自检必须计入合法前 / 后态，不得要求所有并发发布都成功。
```
