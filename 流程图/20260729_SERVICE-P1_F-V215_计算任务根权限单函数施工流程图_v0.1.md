# SERVICE-P1 F-V215 `服务生存任务权限服务::计算任务根权限` 单函数施工流程图 v0.1

图类型：目标施工流程图
状态：现行设计依据；函数待 #379 实现，不表示代码已存在
归属模块：`海中鱼巣.领域.服务.服务生存任务权限`
归属文件：`海中鱼巣/领域/服务.服务生存任务权限.ixx`
唯一根函数：F-V215
完整签名：

```cpp
任务根权限结果 服务生存任务权限服务::计算任务根权限(
    const 任务根权限请求& 请求) const noexcept;
```

本函数只用公开请求显式形成一次生存事实读取和一次任务事实读取；任务提供者在内部按同一请求形成 #377 权限读取。不得枚举安全维护族、填零版本、逐来源重读或隐式读取最新。依据 6140、6150、6170 与服务详细设计 v0.3 第 5.3、6、7.2、7.5、9.4 节。

## 流程图

```mermaid
flowchart TD
    S["S【本函数内具体指令】只读绑定任务根权限请求"]
    B01{"B01【本函数内具体指令】自我、场景、任务、全部值/定义/进展/主动安全/纪元/代次/图/任务集合版本、时间、截止及四个规则版本均有效？"}
    R01["R01【本函数内具体指令】返回入口拒绝且无载荷"]
    B02{"B02【本函数内具体指令】安全层来源选择组按来源需求/维护族/来源因果/最终安全结果稳定升序无重复且同四元身份无异版？"}
    R02["R02【本函数内具体指令】返回结构拒绝且无载荷"]
    N03["N03【本函数内具体指令】逐字段形成任务权限用途的生存事实来源请求：复制自我、期望服务值/安全值/定义/进展/主动安全截止/纪元/代次、单调时间、事实截止和时间/回归规则版本"]
    C04["C04【函数调用】服务生存事实提供者::读取服务生存事实(生存来源请求) const"]
    B05{"B05【本函数内具体指令】生存来源结果为已形成且有载荷？"}
    R05["R05【本函数内具体指令】配对错误返回内部不一致；其它分类原样无载荷返回"]
    N06["N06【本函数内具体指令】逐字段形成服务任务事实来源请求：复制自我、场景、安全选择组、单元素任务组、事实截止、图、任务集合、安全/服务权限规则版本，并写排序规则不适用且optional为空"]
    C07["C07【函数调用】服务任务事实提供者::读取服务任务事实(任务来源请求) const"]
    B08{"B08【本函数内具体指令】任务来源结果为已形成且有载荷？"}
    R08["R08【本函数内具体指令】配对错误返回内部不一致；其它分类原样无载荷返回"]
    B09{"B09【本函数内具体指令】生存载荷逐字段回显全部首次读取条件；任务载荷逐字段回显自我/场景/安全选择组/单任务组/截止/图/任务集合/安全与服务规则及排序不适用？"}
    R09["R09【本函数内具体指令】身份/选择/用途不等返回内部不一致；版本/截止不等返回版本漂移且无载荷"]
    N10["N10【本函数内具体指令】用生存载荷当前值版本和请求事实截止/服务权限规则版本形成安全服务根权限请求"]
    C11["C11【函数调用】F-V204 计算安全服务根权限(生存事实.服务生存定义快照,生存事实.服务生存当前快照,根权限请求)"]
    B12{"B12【本函数内具体指令】根权限结果为已形成或权限为零且有载荷？"}
    R12["R12【本函数内具体指令】配对错误返回内部不一致；其它分类原样无载荷返回"]
    B13{"B13【本函数内具体指令】按控制态与任务来源分类复核#377权限结果适用性、分类/载荷配对及安全来源选择精确映射？"}
    R13["R13【本函数内具体指令】材料缺失返回材料缺失；冲突映射返回结构拒绝；身份不等返回内部不一致；版本不等返回版本漂移"]
    C14["C14【函数调用】F-V205 计算单任务根权限(任务事实载荷,根权限载荷,请求)"]
    R15["R15【本函数内具体指令】原样返回单任务根权限结果"]
    E16["E16【本函数内具体指令】捕获bad_alloc返回资源失败；捕获其它意外异常返回内部不一致；均无载荷"]
    S --> B01
    B01 -- "否" --> R01
    B01 -- "是" --> B02
    B02 -- "否" --> R02
    B02 -- "是" --> N03 --> C04 --> B05
    B05 -- "否" --> R05
    B05 -- "是" --> N06 --> C07 --> B08
    B08 -- "否" --> R08
    B08 -- "是" --> B09
    B09 -- "否" --> R09
    B09 -- "是" --> N10 --> C11 --> B12
    B12 -- "否" --> R12
    B12 -- "是" --> B13
    B13 -- "否" --> R13
    B13 -- "是" --> C14 --> R15
    N03 -. "分配异常" .-> E16
    N06 -. "分配异常" .-> E16
```

## 节点合同

| 节点 | 类型 | 输入 | 输出 | 调用/来源 |
| --- | --- | --- | --- | --- |
| B01—N03 | 本函数内具体指令 | 公开请求 | 生存来源请求 | 全部首次读取条件原值复制 |
| C04—N06 | 函数调用/具体指令 | 生存结果、公开请求 | 单任务来源请求 | 生存提供者恰好一次 |
| C07—B09 | 函数调用/具体指令 | 任务来源请求、双载荷 | 闭合快照 | 任务提供者恰好一次 |
| N10—C11 | 具体指令/函数调用 | 生存快照 | 根权限载荷 | F-V204 恰好一次 |
| B13—C14 | 具体指令/函数调用 | 任务事实、根权限 | 单任务结果 | F-V205 恰好一次 |

## 实现约束

1. 生存提供者与任务提供者各调用一次；不得在 F-V205 前后重读，也不得直接调用 #377 服务。
2. 任务来源请求的排序规则必须显式为“不适用 + `nullopt`”；不得填零排序版本冒充不适用。
3. 安全选择组、任务组、图版本、任务集合版本、事实截止及安全/服务权限规则版本必须逐字段回显。
4. 控制态为终止/休眠时允许 SAFETY 结果不适用；主动运行时按“全部独立服务”或“含安全来源”精确裁决适用性。
5. 不得把材料缺失任务改写为零权限成功，不得把安全兼服务来源重复计入服务权限。
