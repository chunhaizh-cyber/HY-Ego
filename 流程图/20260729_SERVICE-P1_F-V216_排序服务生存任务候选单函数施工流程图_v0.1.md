# SERVICE-P1 F-V216 `服务生存任务权限服务::排序服务生存任务候选` 单函数施工流程图 v0.1

图类型：目标施工流程图
状态：现行设计依据；函数待 #379 实现，不表示代码已存在
归属模块：`海中鱼巣.领域.服务.服务生存任务权限`
归属文件：`海中鱼巣/领域/服务.服务生存任务权限.ixx`
唯一根函数：F-V216
完整签名：

```cpp
服务生存任务排序结果 服务生存任务权限服务::排序服务生存任务候选(
    const 服务生存任务排序请求& 请求) const noexcept;
```

本函数对整批候选只读取一次生存事实和一次任务事实，再调用 F-V204 与 F-V206；禁止逐任务重读生存、任务或 SAFETY 权限。依据 6140、6150、6170 与服务详细设计 v0.3 第 5.3、6、7.2、7.5、9.4 节。

## 流程图

```mermaid
flowchart TD
    S["S【本函数内具体指令】只读绑定服务生存任务排序请求"]
    B01{"B01【本函数内具体指令】自我、场景、非空候选任务组、全部值/定义/进展/主动安全/纪元/代次/图/任务集合版本、时间、截止及五个规则版本均有效？"}
    R01["R01【本函数内具体指令】返回入口拒绝且无载荷"]
    B02{"B02【本函数内具体指令】安全选择组按四元身份稳定升序无重复且无异版，候选任务组按稳定身份升序无重复？"}
    R02["R02【本函数内具体指令】返回结构拒绝且无载荷"]
    N03["N03【本函数内具体指令】逐字段形成任务排序用途的生存事实来源请求：复制自我、期望服务值/安全值/定义/进展/主动安全截止/纪元/代次、单调时间、事实截止和时间/回归规则版本"]
    C04["C04【函数调用】服务生存事实提供者::读取服务生存事实(生存来源请求) const"]
    B05{"B05【本函数内具体指令】生存来源结果为已形成且有载荷？"}
    R05["R05【本函数内具体指令】配对错误返回内部不一致；其它分类原样无载荷返回"]
    N06["N06【本函数内具体指令】逐字段形成服务任务事实来源请求：复制自我、场景、安全选择组、完整候选任务组、事实截止、图、任务集合、安全/服务权限规则，并写排序规则适用且optional为公开排序规则版本"]
    C07["C07【函数调用】服务任务事实提供者::读取服务任务事实(任务来源请求) const"]
    B08{"B08【本函数内具体指令】任务来源结果为已形成且有载荷？"}
    R08["R08【本函数内具体指令】配对错误返回内部不一致；其它分类原样无载荷返回"]
    B09{"B09【本函数内具体指令】生存载荷回显全部首次读取条件；任务载荷回显自我/场景/选择组/完整任务组/截止/图/任务集合/安全服务规则及排序适用性与版本？"}
    R09["R09【本函数内具体指令】身份/选择/用途不等返回内部不一致；版本/截止不等返回版本漂移且无载荷"]
    N10["N10【本函数内具体指令】用生存载荷当前值版本和请求事实截止/服务权限规则版本形成安全服务根权限请求"]
    C11["C11【函数调用】F-V204 计算安全服务根权限(生存事实.服务生存定义快照,生存事实.服务生存当前快照,根权限请求)"]
    B12{"B12【本函数内具体指令】根权限结果为已形成或权限为零且有载荷？"}
    R12["R12【本函数内具体指令】配对错误返回内部不一致；其它分类原样无载荷返回"]
    B13{"B13【本函数内具体指令】按控制态与全任务来源分类复核#377权限结果适用性、分类/载荷配对及每项安全来源选择精确映射？"}
    R13["R13【本函数内具体指令】材料缺失保留给F-V206；冲突映射返回结构拒绝；身份不等返回内部不一致；版本漂移整批返回版本漂移"]
    C14["C14【函数调用】F-V206 排序服务生存任务执行候选(任务事实载荷,根权限载荷,请求)"]
    R15["R15【本函数内具体指令】原样返回整批稳定排序结果"]
    E16["E16【本函数内具体指令】捕获bad_alloc返回资源失败；捕获其它意外异常返回内部不一致；均无载荷"]
    S --> B01
    B01 -- "否" --> R01
    B01 -- "是" --> B02
    B02 -- "否" --> R02
    B02 -- "是" --> N03 --> C04 --> B05
    B05 -- "否" --> R05
    B05 -- "是" --> N06 --> C07 --> B08
    B08 -- "否" --> R08
    B08 -- "是" --> B09
    B09 -- "否" --> R09
    B09 -- "是" --> N10 --> C11 --> B12
    B12 -- "否" --> R12
    B12 -- "是" --> B13
    B13 -- "结构/身份/版本错误" --> R13
    B13 -- "闭合或仅有具名材料缺口" --> C14 --> R15
    N03 -. "分配异常" .-> E16
    N06 -. "分配异常" .-> E16
```

## 节点合同

| 节点 | 类型 | 输入 | 输出 | 调用/来源 |
| --- | --- | --- | --- | --- |
| B01—N03 | 本函数内具体指令 | 公开排序请求 | 生存来源请求 | 全部首次读取条件原值复制 |
| C04—N06 | 函数调用/具体指令 | 生存结果、公开请求 | 整批任务来源请求 | 生存提供者恰好一次 |
| C07—B09 | 函数调用/具体指令 | 任务来源请求、双载荷 | 闭合整批快照 | 任务提供者恰好一次 |
| N10—C11 | 具体指令/函数调用 | 生存快照 | 根权限载荷 | F-V204 恰好一次 |
| B13—C14 | 具体指令/函数调用 | 整批任务事实、根权限 | 稳定排序结果 | F-V206 恰好一次 |

## 实现约束

1. 两个提供者各调用一次；候选数量不得改变读取次数。
2. 排序规则必须显式为“适用 + 请求排序规则版本”；零值、默认值或 `nullopt` 都是入口错误。
3. 任务提供者返回的任务身份组必须与公开候选组顺序和值完全一致；不得补任务、删任务或改用容器当前顺序。
4. 具名材料缺口交 F-V206 分账；版本漂移、冲突映射和身份回显错误必须在排序前整批收口。
5. 最终顺序只由 F-V206 冻结键决定；服务不得按提供者返回顺序、调用到达顺序或缓存顺序追加排序。
