# SERVICE-P1 F-V211 `服务生存数据操作::提交服务合同候选` 单函数施工流程图 v0.1

图类型：目标施工流程图
状态：现行设计依据；函数待 #379 实现，不表示代码已存在
归属模块：`海中鱼巣.领域.数据操作.服务生存维护`
归属文件：`海中鱼巣/领域/数据操作.服务生存维护.ixx`
唯一根函数：F-V211
完整签名：

```cpp
服务合同结果 服务生存数据操作::提交服务合同候选(
    const 服务合同建立请求& 请求,
    服务生存结果分类 候选分类,
    const 服务合同载荷& 候选,
    std::optional<服务生存外部事实参与载荷>&& 外部参与) noexcept;
```

`外部参与` 是 move-only 临时所有权。新合同与 30% 预支必须携带完整外部参与；精确同义重复只携带记录参与者。函数唯一事务入口是当前真实 `节点直接身份结构写入执行器::执行仅参与者事务(std::span<节点直接身份结构写入事务参与者* const>) const`，禁止调用普通回调 `执行`。依据 0050、6160、CORE-C1/v1 与服务详细设计 v0.3 第 7.4、8、10 节。

## 流程图

```mermaid
flowchart TD
    S["S【本函数内具体指令】绑定请求、候选分类、候选并取得外部参与的move-only临时所有权"]
    B01{"B01【本函数内具体指令】候选分类为已结算或精确重复，且请求与候选身份、幂等键、事实截止、合同/服务值版本、发布代次和合同规则版本闭合？"}
    R01["R01【本函数内具体指令】返回入口拒绝、版本漂移或结构拒绝且无载荷"]
    B02{"B02【本函数内具体指令】候选分类明确表示已结算新合同与预支，还是精确重复？"}
    B03{"B03【本函数内具体指令】新候选有外部载荷，重复候选无外部载荷；外部候选代次等于合同建立材料发布代次，来源版本与请求/回显一致，方法绑定组与规格逐项相等？"}
    R03["R03【本函数内具体指令】返回材料缺失或结构拒绝且无载荷"]
    C04["C04【函数调用】F-V207 形成服务合同记录参与者(记录仓库_,请求,候选)"]
    B05{"B05【本函数内具体指令】记录参与结果为已形成且参与者非空？"}
    R05["R05【本函数内具体指令】原样返回参与失败分类且无载荷；分类/指针配对错误返回内部不一致"]
    N06["N06【本函数内具体指令】验证外部参与项按种类数值/提供者稳定候选键严格升序，再按记录参与者在前移动并保存全部参与者所有权"]
    B07{"B07【本函数内具体指令】参与者组非空、种类均为1至4、稳定键非零、无空/重复指针、同类键唯一且来源版本仍闭合？"}
    R07["R07【本函数内具体指令】返回入口拒绝或版本漂移且无载荷"]
    N08["N08【本函数内具体指令】只在move-only载荷和记录参与者存活期形成临时原始指针组与span"]
    C09["C09【函数调用】节点直接身份结构写入执行器::执行仅参与者事务(参与者跨度)"]
    B10{"B10【本函数内具体指令】执行器状态？"}
    C11["C11【函数调用】F-V209 读取已发布服务合同结果(记录仓库_,请求)"]
    B12{"B12【本函数内具体指令】读回有完整成功载荷且身份、合同、预支、服务值、发布代次和版本与候选逐项相等？"}
    R12["R12【本函数内具体指令】读回失败按资源失败返回；缺失或字段不等返回内部不一致且不得撤销已发布事实"]
    R13["R13【本函数内具体指令】执行器已提交时返回已结算和权威读回载荷"]
    R14["R14【本函数内具体指令】执行器幂等读回时返回精确重复和权威读回载荷"]
    R15["R15【本函数内具体指令】入口拒绝/许可拒绝/身份冲突映射入口拒绝；版本漂移/候选已撤销映射版本漂移；资源失败原样返回"]
    R16["R16【本函数内具体指令】候选已确认/内部不一致及其它非法状态返回内部不一致且无载荷"]
    E17["E17【本函数内具体指令】参与者组分配失败返回资源失败且无载荷"]
    E18["E18【本函数内具体指令】执行器调用前或调用中抛bad_alloc时返回资源失败且无载荷"]
    E19["E19【本函数内具体指令】执行器调用前或调用中抛system_error/其它异常时返回内部不一致且无载荷"]
    S --> B01
    B01 -- "否" --> R01
    B01 -- "是" --> B02 --> B03
    B03 -- "否" --> R03
    B03 -- "是" --> C04 --> B05
    B05 -- "否" --> R05
    B05 -- "是" --> N06 --> B07
    B07 -- "否" --> R07
    B07 -- "是" --> N08 --> C09 --> B10
    B10 -- "已提交" --> C11 --> B12
    B10 -- "幂等读回" --> C11
    B12 -- "否" --> R12
    B12 -- "是且执行器已提交" --> R13
    B12 -- "是且执行器幂等读回" --> R14
    B10 -- "可映射拒绝/失败" --> R15
    B10 -- "内部或非法状态" --> R16
    N06 -. "分配异常" .-> E17
    C09 -. "bad_alloc" .-> E18
    C09 -. "system_error/其它异常" .-> E19
```

## 节点合同

| 节点 | 类型 | 输入 | 输出 | 事务/生命周期 |
| --- | --- | --- | --- | --- |
| B01—B03 | 本函数内具体指令 | 请求、候选分类、候选、可选外部载荷 | 准入与参与形态 | 执行器前零写入 |
| C04 | 函数调用 | 仓库、请求、候选 | 记录参与者 | F-V207 唯一调用 |
| N06—N08 | 本函数内具体指令 | 全部 move-only 所有权 | 非空临时 span | span 不保存、不越过载荷生命周期 |
| C09 | 函数调用 | 参与者跨度 | 核心事务结果 | 唯一调用 `执行仅参与者事务` |
| C11—R14 | 函数调用/具体指令 | 已发布事务结果 | 权威合同结果 | 发布后共享读回；不得撤销 |
| R15—R16/E17 | 本函数内具体指令 | 非成功状态 | 无载荷结果 | 候选由执行器按协议撤销/隔离 |

## 实现约束

1. 本函数不得出现普通 `节点直接身份结构写入执行器::执行(回调, span)`、空回调、伪核心写集或任何第二事务入口。
2. F-V207、核心执行器与 F-V209 各至多调用一次；只有核心返回 `已提交` 或 `幂等读回` 才允许调用 F-V209。
3. 参与顺序固定为服务记录、状态、动态、关系/因果、索引；每个外部参与项显式携带稳定种类值和非零提供者稳定候选键，整个组必须已经按二元键严格升序。
4. 执行器返回前，记录参与者与外部载荷持续拥有全部对象；返回后销毁临时 span，不保存任何原始指针。
5. 成功必须来自发布后权威读回；不得直接返回算法候选或参与者准备期读回。
6. 当前真实执行器在取得独占许可或进入参与者序列前可能抛出；`bad_alloc` 映射资源失败，`system_error` 与其它异常映射内部不一致。现行发布阶段由 `noexcept` 的参与者最后发布组成，执行器不存在“真实发布后再抛出”的合法后置抛点；若实际接口新增该行为即设计漂移，禁止尝试撤销已发布事实。
