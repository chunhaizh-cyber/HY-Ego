# 移动世界树成员函数流程图 v0.2

更新时间：2026-08-03

## 依据

```text
4200 v0.8；业务节点到能力与目标函数映射.md“世界事实维护”；WORLD-TREE-P1业务流程图M01/W01/W02；P1A3详细设计v0.2 §3.2.3/§3.3/§4.5/§5
```

## 身份与边界

目标单函数施工图；一图唯一根函数。本函数只在同一世界内失效一条当前父/归属边并创建一条新边，不删除成员或改写资格、特征、状态和动态。

## 函数合同

```text
根函数：节点直接场景业务服务::移动世界树成员
归属模块 / 文件 / 层级：海中鱼巣.领域.服务.节点直接场景 / 海中鱼巣/领域/服务.节点直接场景.ixx / L2
现有或待新增：P1A3F v0.2 骨架原位替换为真实行为
完整签名：节点直接场景写入结果 节点直接场景业务服务::移动世界树成员(const 移动世界树成员请求& 请求) const
参数：请求为const借用，只在同步调用内存续；请求头、成员种类、成员/旧父/旧关系/新父见证和移动幂等身份必须完整
返回结果：调用方拥有的值；只有已提交/幂等读回携带非零代次、持久状态和旧失效+新当前关系读回
被调函数流程图：读取世界结构 -> 流程图/20260803_读取世界结构_函数流程图_v0.2.md
```

## 流程图

```mermaid
flowchart TD
    N01{"N01【指令-判断】请求头、成员种类、四个见证、移动幂等命名域和版本完整"}
    N01 -->|"否"| R01["R01【指令-返回】入口拒绝、代次0、空读回"]
    N01 -->|"是"| T01["T01【指令-异常边界】以单一外层try覆盖N02—N12全部构造、调用和结果映射"]
    T01 -->|"进入受保护体"| N02["N02【指令-构造】按详细设计§3.3计算请求意图SHA-256；已重算=false"]
    N02 -->|"请求.幂等身份+意图摘要"| N03["N03【函数调用】节点直接类型化结构幂等探测结果 节点直接类型化结构提交服务::探测幂等(节点直接事务幂等身份 幂等身份,const std::array<std::uint8_t,32>& 请求意图摘要) const：请求.幂等身份->幂等身份，意图摘要->请求意图摘要；返回->探测结果"]
    N03 -->|"同义已发布+原结果完整"| N04{"N04【指令-判断】原读回恰含请求旧关系的已失效后件和一条新当前关系，类型、端点、顺序与请求同义"}
    N04 -->|"是"| R02["R02【指令-返回】幂等读回、原发布代次/持久状态和移动读回"]
    N04 -->|"否"| R08["R08【指令-返回】内部不一致；已发布则保留非零代次，不伪造读回"]
    N03 -->|"异义冲突"| R03["R03【指令-返回】幂等冲突、代次0"]
    N03 -->|"待发布或隔离/内部不一致"| R08
    N03 -->|"许可拒绝"| R04["R04【指令-返回】许可拒绝、代次0"]
    N03 -->|"未找到"| N05["N05【指令-构造】节点直接世界结构读取请求{合同版本=当前,头=请求.头}->结构请求"]
    N05 -->|"结构请求"| N06["N06【函数调用】世界结构读取结果 节点直接世界结构读取服务::读取世界结构(const 节点直接世界结构读取请求& 请求) const：结构请求->请求；返回->写前世界"]
    N06 -->|"已读取"| N07{"N07【指令-判断】成员、旧父、新父、旧关系均在本世界当前唯一命中且见证逐字段匹配"}
    N06 -->|"入口/版本/许可/资源/内部非成功"| R05["R05【指令-返回】一对一映射世界结构非成功"]
    N07 -->|"图已有多父/多位置/环/悬空端点"| R08
    N07 -->|"合法对象但请求见证已变化"| R06["R06【指令-返回】版本漂移、代次0"]
    N07 -->|"成立"| N08{"N08【指令-判断】成员不是根，新父不同于旧父，存在对应归属/子场景对应普通父子，子场景不移入自身或后代"}
    N08 -->|"否"| R01
    N08 -->|"是"| N09["N09【函数调用】节点直接场景事务形成结果 形成移动世界树成员事务(const 移动世界树成员请求& 请求,const 世界结构读取结果& 写前世界)：本根函数请求->请求，写前世界->写前世界；返回->事务形成结果"]
    N09 -->|"已形成；写项=失效旧关系+创建关系:1，读回=已失效旧关系+当前新关系，字段等于§3.2.3"| N10["N10【函数调用】节点直接类型化结构提交结果 节点直接类型化结构提交服务::提交(const 节点直接类型化结构事务请求& 请求) const：事务形成结果.事务值->请求；返回->提交结果"]
    N09 -->|"其它状态或字段不符"| R07["R07【指令-返回】映射形成非成功；已形成却字段不符为内部不一致"]
    N10 -->|"版本漂移且已重算=false"| N11["N11【指令-赋值】丢弃写前世界/事务局部材料；已重算=true"]
    N11 -->|"从幂等探测重新计算"| N03
    N10 -->|"其它非成功或第二次漂移"| R09["R09【指令-返回】逐项映射提交非成功、发布代次和持久状态"]
    N10 -->|"已提交或幂等读回"| N12{"N12【指令-判断】原许可读回恰含请求旧关系的已失效后件和新当前关系:1，版本/类型/端点/顺序与请求一致"}
    N12 -->|"否"| R08
    N12 -->|"是"| R10["R10【指令-构造并返回】形成世界树移动读回；返回已提交/幂等读回、原代次/持久状态"]
    T01 -. "受保护体抛出std::bad_alloc" .-> E01["E01【指令-返回】资源失败、代次0、空读回"]
    T01 -. "受保护体抛出其它异常" .-> E02["E02【指令-返回】内部不一致；仅已有提交结果且发布代次非零时保留该代次，否则代次0；始终不伪造读回"]
```

## 节点合同

| 节点 | 类型 | 参数 / 输入绑定 | 结果 / 输出绑定 | 事实读写 | 后继条件 | 验证 |
| --- | --- | --- | --- | --- | --- | --- |
| N01/T01/N02 | 本函数内判断/异常边界/构造 | 公开请求全字段 | 入口分类、意图摘要、重算标志 | 零写入 | 完整到N03；异常到E01/E02 | 字段敏感性、命名域/规则版本、两类异常 |
| N03—N04 | 函数调用+本函数内判断 | 幂等身份/意图摘要 | 探测及原读回映射 | 只读幂等账 | 同义直接R02；未找到到N05 | 同义优先于当前图变化 |
| N05—N06 | 本函数内构造+函数调用 | `请求.头->结构请求.头`；`结构请求->读取世界结构.请求` | 写前世界 | 只读世界结构 | 已读取到N07 | 不直接传入请求头 |
| N07—N08 | 本函数内判断 | 写前世界和全见证 | 移动准入 | 零写入 | 唯一当前且规则成立到N09 | 根/自身/后代/跨世界/多父/环/悬空 |
| N09 | 函数调用 | `本根函数请求->请求`；`写前世界->写前世界` | 事务形成结果 | 只形成局部请求 | 已形成到N10 | 只失效旧边+创建新边，读回恰两关系 |
| N10 | 函数调用 | `事务值->提交.请求` | 提交结果 | 单次L1发布和原许可读回 | 漂移重算或结果分流 | 旧关系失效先于新关系当前发布 |
| N11 | 本函数内赋值 | 首次漂移局部材料 | 已重算=true | 只丢弃局部副本 | 回N03 | 至多一次 |
| N12 | 本函数内判断 | 原许可通用读回 | 移动专属读回材料 | 零补写 | 完整到R10 | 旧失效/新当前各恰一 |
| R01—R10/E01—E02 | 本函数内返回 | 具名分支材料 | 合同允许的场景写入结果 | 零补写 | 函数结束 | 非成功不伪造移动读回；异常不越界 |

## 关键边界

```text
同义幂等必须在读取已变化的当前图之前返回。
移动只失效一条旧关系并创建一条新关系；不删除成员、资格、索引、特征、状态或动态。
提交版本漂移最多从幂等探测开始完整重算一次；std::bad_alloc映射资源失败，其它异常映射内部不一致。
```
