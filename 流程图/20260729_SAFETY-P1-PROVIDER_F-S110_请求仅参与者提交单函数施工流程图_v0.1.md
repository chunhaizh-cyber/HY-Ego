# SAFETY-P1-PROVIDER F-S110 请求仅参与者提交单函数施工流程图 v0.1

日期：2026-07-29

函数身份：F-S110

归属模块 / 文件：`海中鱼巣.核心.会话.节点直接身份结构写入` / `海中鱼巣/核心/会话.节点直接身份结构写入.ixx`

完整签名：

```cpp
void 节点直接身份结构写入会话::请求仅参与者提交() noexcept;
```

返回结果：`void`

```mermaid
flowchart TD
    S["S【本函数内具体指令】仅由执行器友元调用"]
    B01{"B01【本函数内具体指令】阶段为写入中、无首次失败、全部核心写集为空且无核心幂等结果？"}
    N02["N02【本函数内具体指令】记录内部不一致并把决定置为撤销"]
    N03["N03【本函数内具体指令】把决定置为提交，阶段置为已请求提交，并标记仅参与者事务"]
    R["R【本函数内具体指令】返回"]
    S --> B01
    B01 -- "否" --> N02 --> R
    B01 -- "是" --> N03 --> R
```

本函数不公开、不接收参与者、不改变任何仓库。普通 `请求提交()` 的空写集拒绝合同保持不变。
