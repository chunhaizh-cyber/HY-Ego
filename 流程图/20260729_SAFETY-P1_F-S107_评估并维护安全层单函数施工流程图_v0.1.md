# SAFETY-P1 F-S107 评估并维护安全层单函数施工流程图 v0.1

日期：2026-07-29

函数身份：F-S107

归属模块 / 文件：`海中鱼巣.领域.服务.分层安全维护` / `海中鱼巣/领域/服务.分层安全维护.ixx`

完整签名：

```cpp
安全维护结果 分层安全维护服务::评估并维护安全层(
    const 安全维护请求& 请求);
```

返回结果：`安全维护结果`

```mermaid
flowchart TD
    S["S【本函数内具体指令】接收安全维护请求"]
    C01["C01【调用具名函数】事实提供者.读取已发布安全维护事实(来源请求) -> 事实结果"]
    B02{"B02【本函数内具体指令】事实结果为已形成且载荷完整？"}
    R02["R02【本函数内具体指令】按来源分类返回无载荷结果"]
    C03["C03【调用具名函数】F-S103 计算分层安全维护候选(定义, 当前, 事实载荷, 请求) -> 候选结果"]
    B04{"B04【本函数内具体指令】候选结果分类？"}
    R04["R04【本函数内具体指令】非成功分类原样返回且零写入"]
    C05["C05【调用具名函数】F-S108 数据操作.提交安全维护候选(请求, 候选载荷, nullopt) -> 提交结果；F-S108 走仅参与者事务"]
    B06{"B06【本函数内具体指令】实际值发生变化？"}
    B07{"B07【本函数内具体指令】外部身份规格完整，三个稳定主键命名域正确？"}
    R07["R07【本函数内具体指令】返回入口拒绝且零写入"]
    N08["N08【本函数内具体指令】以当前快照当前状态、候选前后值、定义提交方法和来源版本形成外部事实规格；主动分支复制所选已发布批次，纯被动分支批次为空"]
    C09["C09【调用具名函数】外部参与提供者.形成安全维护外部事实参与包(规格) -> 外部结果"]
    B10{"B10【本函数内具体指令】外部结果为已形成且完整包唯一存在？"}
    R10["R10【本函数内具体指令】按外部形成分类返回无载荷结果"]
    C11["C11【调用具名函数】F-S108 数据操作.提交安全维护候选(请求, 候选载荷, move(外部载荷)) -> 提交结果"]
    R12["R12【本函数内具体指令】返回提交结果"]

    S --> C01 --> B02
    B02 -- "否" --> R02
    B02 -- "是" --> C03 --> B04
    B04 -- "入口/版本/材料/结构/资源/内部失败" --> R04
    B04 -- "无变化或精确重复" --> C05 --> R12
    B04 -- "已维护" --> B06
    B06 -- "否" --> C05
    B06 -- "是" --> B07
    B07 -- "否" --> R07
    B07 -- "是" --> N08 --> C09 --> B10
    B10 -- "否" --> R10
    B10 -- "是" --> C11 --> R12
```

## 节点合同

| 节点 | 类型 | 输入 / 输出 | 事实读写 | 验证 |
| --- | --- | --- | --- | --- |
| C01 | 被调函数 | 请求映射为来源请求；返回已发布值式事实 | 只读 | SP-A06/A07 |
| C03 | 被调函数 | 完整事实与请求进入 F-S103 | 零写入 | SP-A06—A11 |
| C05/C11 | 被调函数 | 无变化传空包并走仅参与者事务；变化传唯一 move-only 包并走核心会话事务 | 由 F-S108 统一提交 | SP-A12/A19 |
| B07/N08 | 本函数内具体指令 | 稳定主键分别绑定 `特征值身份`、`状态身份`、`动态身份`，并绑定前状态、可选已发布来源批次和版本 | 零写入；不得生成身份 | SP-A07/A11/A12 |
| C09 | 被调函数 | 完整输入规格形成外部事务包 | 只形成不可见候选包 | SP-A12 |
| R02/R04/R07/R10/R12 | 本函数内具体指令 | 强类型结果 | 失败零写入 | 结果载荷矩阵 |

本函数不得生成稳定主键、节点句柄、当前时间或默认来源批次。
