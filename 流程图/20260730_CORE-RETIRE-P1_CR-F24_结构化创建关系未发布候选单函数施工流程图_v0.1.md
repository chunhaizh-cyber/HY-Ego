# CR-F24 结构化创建关系未发布候选单函数施工流程图

版本：v0.1
创建侧代码基线：`57866ac5ca63235ed12da60f635d29f1aacd718b`

## 函数合同

```text
根函数：正式关系仓库::结构化创建关系未发布候选
归属：海中鱼巣.核心.仓库.正式关系 / 海中鱼巣/核心/仓库.正式关系.ixx
现有或待新增：现有公开成员改造
完整签名：正式关系写入结果 正式关系仓库::结构化创建关系未发布候选(关系类型 类型, 节点句柄 源节点, 节点句柄 目标节点, std::int64_t 顺序号, std::uint64_t 事务序号)
参数：全部值传入；事务非零；类型须在 ABI；端点须在同事务节点视图存在且有效
返回：具名状态、当前关系句柄及成功时唯一移动候选；拒绝/失败无可读半候选
所有权：新条目及候选槽归仓库；移动候选归会话；锁不逸出
结果语义：新条目无发布基线，仅设置 optional 候选写后和候选事务序号；普通读取不可见
```

```mermaid
flowchart TD
    N01{"N01【指令-判断】仓库有效、类型有效、事务非零且端点句柄有效"} -- "否" --> R01["R01【指令-返回】返回具名入口拒绝"]
    N01 -- "是" --> X01["X01【函数调用】std::optional<节点直接身份记录> 节点直接身份仓库::读取节点(节点句柄 节点, std::uint64_t 事务序号) const：实参[源节点,事务序号]→形参[节点,事务序号]；返回→源记录"]
    X01 -- "完成" --> X02["X02【函数调用】std::optional<节点直接身份记录> 节点直接身份仓库::读取节点(节点句柄 节点, std::uint64_t 事务序号) const：实参[目标节点,事务序号]→形参[节点,事务序号]；返回→目标记录"]
    X02 -- "完成" --> B01{"B01【指令-判断】两端点存在有效且具名关系准入通过"}
    B01 -- "否" --> R01
    B01 -- "是" --> N02["N02【指令-操作】取得关系仓独占锁"]
    N02 -- "完成" --> B02{"B02【指令-判断】同事务可见关系中是否存在幂等关系或基数冲突"}
    B02 -- "幂等" --> R02["R02【指令-返回】返回幂等读回及当前关系句柄"]
    B02 -- "基数冲突" --> R01
    B02 -- "无冲突且类型不是普通父子" --> N03["N03【指令-判断】下个关系编号可分配且版本一有效"]
    B02 -- "无冲突且类型是普通父子" --> X03["X03【函数调用】bool 正式关系仓库::子链包含节点_已加锁(节点句柄 起点, 节点句柄 目标, std::uint64_t 事务序号) const：实参[目标节点,源节点,事务序号]→形参[起点,目标,事务序号]；返回→是否成环"]
    X03 -- "完成" --> B03{"B03【指令-判断】父链是否成环"}
    B03 -- "是" --> R01
    B03 -- "否" --> N03
    N03 -- "否" --> R03["R03【指令-返回】返回内部不一致"]
    N03 -- "是" --> N04["N04【指令-构造】构造版本一有效写后记录"]
    N04 -- "完成" --> N05["N05【指令-写入】建立无发布基线条目；候选写后=新记录；候选事务序号=事务序号"]
    N05 -- "完成" --> B04{"B04【指令-判断】条目插入和移动候选构造均成功"}
    B04 -- "否" --> N06["N06【指令-写入】删除未发布条目并返回正式关系操作状态::内部不一致(15)"]
    B04 -- "是" --> R04["R04【指令-返回】返回已创建候选、写后句柄和移动候选"]
```

节点边界：端点读取发生在关系锁外；父链检查使用 [CR-F29](./20260730_CORE-RETIRE-P1_CR-F29_子链包含节点已加锁单函数施工流程图_v0.1.md)。确认、撤销、发布分别由 CR-F28、CR-F18、CR-F19 完成。
