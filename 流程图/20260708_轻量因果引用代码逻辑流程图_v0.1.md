# 轻量因果引用代码逻辑流程图

更新时间：2026-07-08

## 依据

```text
AGENTS.md
计划/计划索引.md
规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md
规范/规范目录.md
规范/1190_根规范_因果_20260720.md
规范/4220_子规范_动作动态与因果账本边界_20260720.md
规范/4310_子规范_因果补完与因果信息用法_20260720.md
规范/详细设计/状态动态二次特征因果服务增强详细设计.md
规范/详细设计/因果模板与查询候选详细设计.md
实施记录/20260708_应用逻辑流程图迁移顺序信息数据.md
实施记录/20260707_FS02_状态动态二次特征因果第一轮代码实施_Codex断点清单.md
实施记录/20260707_服务操作函数矩阵第一批代码实施_Codex断点清单.md
实施记录/20260708_BASE-D1_基础信息入账代码实施_Codex断点清单.md
流程图/20260708_任务回执实际结果状态结果回写代码逻辑流程图_v0.1.md
海中鱼巣/领域/因果服务.h
海中鱼巣/领域/动态服务.h
海中鱼巣/领域/需求服务.h
```

## 说明

本图是第 14 项“轻量因果引用流程”的代码逻辑流程图，承接第 13 项任务回执 / 实际结果状态 / 结果回写后的动态证据材料，并为第 15 项需求结算流程提供可选因果引用材料边界。

本图只覆盖当前代码已存在的因果引用基础身份创建、来源动态轻量准入、动态材料准入、因果模板 / 查询候选只读入口和缺失证据候选占位边界。当前代码不持久化“因果引用 -> 来源动态”的关系，不生成稳定因果结论，不自动创建需求、任务、方法或结算记录，也不让单个轻量因果引用裁决需求满足。

本图不确认上一份流程图；已按流程图免确认口径生成对应详细设计，但不生成施工计划，不登记可执行队列，不构成代码实施许可。

## 流程图

```mermaid
flowchart TD
    A["上游：动态记录证据材料 / 任务实际结果状态 / 任务完成状态材料"] --> B["进入轻量因果引用边界"]
    B --> C{"入口类型"}

    C -- "基础身份创建" --> I1["因果服务::记录因果引用()"]
    I1 --> I2["主信息仓库::创建主信息"]
    I2 --> I3["节点仓库::创建节点(因果引用, 主信息句柄)"]
    I3 --> I4["节点仓库::读取节点(因果引用)"]
    I4 --> I5{"节点类型为因果引用且主信息有效"}
    I5 -- "否" --> IF1["追根因解决：不得声明因果引用身份有效"]
    I5 -- "是" --> I6["输出：因果引用基础身份"]
    I6 --> I7["边界：基础身份没有来源动态证据，不能作为稳定因果或结算依据"]

    C -- "仅节点类型准入" --> L1["因果服务::记录因果引用(来源动态)"]
    L1 --> L2["节点仓库::读取节点(来源动态)"]
    L2 --> L3{"来源动态节点存在且类型为动态"}
    L3 -- "否" --> LR1["入口拒绝：不创建因果引用"]
    L3 -- "是" --> L4["调用 因果服务::记录因果引用()"]
    L4 --> L5["输出：轻量因果引用节点"]
    L5 --> L6["边界：该重载只验证动态节点类型，不验证动态材料完整性"]
    L6 --> L7["边界：不写 因果引用 到 来源动态 的持久关系"]

    C -- "动态材料准入" --> M1["因果服务::读取因果准入动态材料(来源动态, 动态服务)"]
    M1 --> M2["动态服务::读取动态材料(来源动态)"]
    M2 --> M3{"动态材料完整"}
    M3 -- "否" --> MR1["入口拒绝：不创建因果引用"]
    M3 -- "是" --> M4["因果服务::因果轻量准入是否有效 = true"]
    M4 --> M5["因果服务::记录因果引用(来源动态, 动态服务)"]
    M5 --> M6["调用 因果服务::记录因果引用() 创建因果引用节点"]
    M6 --> M7["输出：带来源动态准入证据的轻量因果引用"]
    M7 --> M8["边界：来源动态只作入口准入证据，不持久化来源动态关系"]

    C -- "因果模板 / 查询候选只读" --> Q1["因果服务::读取因果模板候选(来源动态, 动态服务)"]
    Q1 --> Q2["转入 因果服务::读取因果查询候选"]
    Q2 --> Q3["节点仓库::读取节点(来源动态)"]
    Q3 --> Q4{"来源动态为动态节点"}
    Q4 -- "否" --> QR1["返回候选状态：缺少动态证据"]
    Q4 -- "是" --> Q5["动态服务::动态证据材料是否完整(来源动态)"]
    Q5 --> Q6{"动态证据材料完整"}
    Q6 -- "否" --> QR2["返回候选状态：动态材料不完整"]
    Q6 -- "是" --> Q7["返回候选状态：可作为候选"]
    Q7 --> Q8["边界：候选只读，不新增节点、关系、任务、方法或结算"]

    C -- "缺失证据候选占位" --> D1["因果服务::读取缺失证据候选(来源动态, 动态服务)"]
    D1 --> D2["先读取因果查询候选"]
    D2 --> D3{"查询候选状态为可作为候选"}
    D3 -- "是" --> D4["当前代码返回：动态材料不完整"]
    D3 -- "否" --> D5["返回原查询候选状态"]
    D4 --> D6["边界：当前为保守占位，不是完整缺失证据清单"]
    D5 --> D6

    C -- "下游结算可选材料" --> N1["需求服务::记录需求结算(..., 可选因果引用)"]
    N1 --> N2{"传入因果引用时节点类型为因果引用"}
    N2 -- "否" --> NR1["结算入口拒绝：不写结算记录"]
    N2 -- "是" --> N3["需求结算仍需目标状态、实际状态、来源任务、动态证据和比较结果成立"]
    N3 --> N4["写结算时可选写：结算状态 引用 因果引用"]
    N4 --> N5["边界：需求结算不由因果引用单独裁决"]

    I7 --> O1["只读输出方：需求结算后续流程 / 因果候选后续专项 / 显示层只读"]
    L7 --> O1
    M8 --> O1
    Q8 --> O1
    D6 --> O1
    N5 --> O1
    IF1 --> O1
    LR1 --> O1
    MR1 --> O1
    QR1 --> O1
    QR2 --> O1
    NR1 --> O1

    O1 --> RV["最小读回验证门禁"]
    RV --> RV1["因果身份路径：读回因果引用节点类型和主信息有效性"]
    RV --> RV2["动态准入路径：读回来源动态、发生场景、主体、目标、前状态、后状态、时间戳和来源动作"]
    RV --> RV3["关系不写路径：记录因果引用前后关系数量不因来源动态准入而增加"]
    RV --> RV4["候选只读路径：查询候选前后节点、主信息、关系、索引数量不变"]
    RV --> RV5["结算可选路径：后续结算必须自行读回结算记录、动态证据和可选因果引用关系"]
    RV1 --> RV6{"各路径读回满足身份、准入、只读和不升级边界"}
    RV2 --> RV6
    RV3 --> RV6
    RV4 --> RV6
    RV5 --> RV6
    RV6 -- "否" --> RVF["不得声明轻量因果引用有效；后续施工计划必须补追根因解决收口、数量快照和半结构不可读验证"]
    RV6 -- "是" --> OK["输出给第 15 项：需求结算流程"]

    B --> G1["当前代码差距"]
    G1 --> G2["无专门因果引用材料读取入口；当前主要通过节点类型和主信息读回复核身份"]
    G2 --> G3["记录因果引用(来源动态) 与 记录因果引用(来源动态, 动态服务) 准入强度不同"]
    G3 --> G4["当前不持久化因果引用到来源动态的关系，也不保存证据集合、评分、版本或可靠性结论"]
    G4 --> G5["读取缺失证据候选仍是保守占位，不是完整缺证据清单"]
    G5 --> G6["当前不自动任务化，不创建需求、任务、方法或结算记录"]
    G6 --> G7["多步创建已有内部读回，但尚未证明完整事务回滚或数量快照级半结构不可读"]

    B --> X1["排除项"]
    X1 --> X2["线程、Tick、控制台、日志、自然语言说明不得成为因果来源"]
    X1 --> X3["单个轻量因果引用不得解释为稳定因果结论"]
    X1 --> X4["因果候选不得自动裁决需求满足、任务完成或方法成功"]
    X1 --> X5["本图不接 SQL、控制面板、D455、体素、外设或真实线程调度"]
```

## 关键边界

```text
当前 `因果服务::记录因果引用()` 只创建因果引用节点，并在函数内部读回节点类型和主信息有效性。
当前 `因果服务::记录因果引用(来源动态)` 只验证来源动态是动态节点，然后创建因果引用；该重载不验证动态材料完整性。
当前 `因果服务::记录因果引用(来源动态, 动态服务)` 通过 `动态服务::读取动态材料` 做动态材料准入后创建因果引用。
当前轻量因果引用不持久化来源动态关系；入口验证中关系数量不应因来源动态准入而增加。
当前因果模板候选、因果查询候选和缺失证据候选均为只读候选材料，不写结构，不创建任务、方法、需求或结算。
当前缺失证据候选入口仍是保守占位，不是完整缺失证据清单。
当前需求结算可以接收可选因果引用，但仍必须由需求服务自行复核目标状态、实际状态、来源任务、动态证据和结算结果；因果引用不能单独裁决需求满足。
本图不接 SQL、控制面板、D455、体素或外设。
```

## 当前代码差距

```text
当前没有专门的因果引用材料读取入口，不能读出证据集合、来源动态集合、评分、版本或可靠性状态。
当前轻量因果引用只创建节点身份，不持久化来源动态关系。
当前 `记录因果引用(来源动态)` 和 `记录因果引用(来源动态, 动态服务)` 的准入强度不同，后续设计必须明确推荐入口和禁止误用边界。
当前缺失证据候选入口不是完整缺证据结构，只能作为保守候选材料。
当前没有稳定因果结论、因果模板持久化、因果任务化或可靠性评分落代码。
当前不自动创建需求、任务、方法或需求结算记录。
当前多步创建路径已有内部读回和追根因解决，但尚未证明完整事务回滚、显式失效隔离或数量快照级半结构不可读。
当前流程图的最小读回验证是后续详细设计 / 施工计划门禁，不宣称每个当前函数内部都已具备统一事务级读回验证。
当前流程图已生成对应详细设计，但不生成待确认计划或代码实施许可。
```

## 后续产物

```text
本图可作为后续“轻量因果引用详细设计重审”或第 15 项“需求结算流程”的输入材料。
下一份流程图按迁移顺序应进入第 15 项：需求结算流程。
若进入代码实施，必须另建待确认施工计划，明确允许文件、禁止文件、入口拒绝、追根因解决收口、读回验证和完成声明边界。
```
## 中途非成功返回二分口径

本文件按 2026-07-09 硬规则修订：中途非成功返回只分为 `追根因解决` 和 `逻辑内返回`。

- `追根因解决`：前置条件已经满足，并进入创建、绑定、写关系、写状态、记录动态、结算、读回或结构承载后，结果不符合内部预期；必须停止依赖路径，定位根因，当前未证明完整回滚时登记事务隔离缺口或半结构隔离缺口。
- `逻辑内返回`：领域协议允许的拒绝、候选为空、请求材料返回或人读材料返回；必须保证结构不变化，且返回材料、日志、回执、显示或控制台输出不裁决机器事实。
