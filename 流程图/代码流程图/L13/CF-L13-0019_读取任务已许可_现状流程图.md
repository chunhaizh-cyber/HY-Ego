# CF-L13-0019 读取任务已许可现状流程图

图类型：现状流程图
代码版本：`3920a74638264f9f539d50f32f3c9fe5fb1982ab`
当前工作区复核基线：`af74e46ef3fd0b1b2c108d695569855909a59ee1`
源码 blob：`16ac9534baeda26ac8a712066e713f3339f6e0c8`
覆盖文件：`海中鱼巣/领域/数据操作.需求任务方法.ixx:2313-2401`
唯一根函数：R0444
完整签名：`海中鱼巣::任务权威材料 海中鱼巣::需求任务方法数据操作::读取任务_已许可(海中鱼巣::节点句柄 任务, const 海中鱼巣::结构事务令牌& 令牌) const`
参数：`任务` 按值传入；`令牌` 为调用期只读借用
返回结果：身份不可读时透传读取状态；类型错误时返回类型不匹配；必需关系、生命周期、授权或选择材料不一致时返回内部不一致；结构齐全时返回已找到的任务权威材料
调用方：已登记 R0444 直接调用方
直接项目函数：R0435（RCE1372）、R0078（RCE1373）、R0436（RCE1374）、R0445（RCE1375）、R0437（RCE1376）、R0446（RCE1377）、R0079（RCE1378）、R0447（RCE1379）、R0428（RCE3014）、F0051（RCE3015）、R0843（RCE3016）
符号外部边界：X04089—X04107
逐行映射表：`流程图/代码流程图/L13/CF-L13-0019_读取任务已许可_逐行代码映射表.md`
依据提交 / 结构化消息：冻结源码提交 `3920a74638264f9f539d50f32f3c9fe5fb1982ab`；当前 `main` 复核目标源码零差异
验证输出：89 行源码完整映射；11 个项目出边、19 个外部边界、0 个未解析项目调用；本切片不运行构建或程序
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`
不得作为施工许可：是
不得宣称：读取成功等于任务已执行或能力完成、内部不一致已经修复，或本函数写入了任务、关系、状态、主信息或索引

## 流程图

```mermaid
flowchart TD
    S["S【本函数内具体指令】进入R0444；构造输出并绑定任务与令牌"]
    C01["C01【函数调用 RCE1372】R0435读取任务身份"]
    C02["C02【函数调用 RCE3014】R0428身份.当前可读()"]
    D03{"D03【本函数内具体指令】身份当前可读"}
    R04["R04【本函数内具体指令】透传身份状态并返回"]
    D05{"D05【本函数内具体指令】身份类型为任务"}
    R06["R06【本函数内具体指令】写类型不匹配并返回"]
    N07["N07【本函数内具体指令】初始化来源需求、虚拟存在、场景、目标状态、实际结果、方法投影六槽"]
    C08["C08【函数调用 RCE1373】R0078读取引用关系记录组"]
    L09["L09【本函数内具体指令】遍历本轮引用关系"]
    C10["C10【函数调用 RCE1372】R0435读取目标身份"]
    C11["C11【函数调用 RCE1374】R0436形成本关系证据"]
    C12["C12【函数调用 RCE3014】R0428检查目标身份当前可读"]
    D13{"D13【本函数内具体指令】目标可读、证据存在并匹配一种受支持类型与顺序号"}
    C14["C14【函数调用 RCE1376】R0437设置对应唯一关系"]
    C15["C15【函数调用 RCE1375】R0445形成任务内部不一致结果"]
    C16["C16【函数调用 RCE1373】R0078读取模板关系记录组"]
    L17["L17【本函数内具体指令】筛选顺序号0的模板关系"]
    C18["C18【函数调用 RCE1372/RCE1374/RCE3014】读取目标身份、形成证据并检查目标为可读状态"]
    C19["C19【函数调用 RCE1376】R0437设置唯一目标状态关系"]
    C20["C20【函数调用 RCE1373】R0078读取任务生命周期关系组"]
    D21{"D21【本函数内具体指令】生命周期恰一条且四项必需关系存在"}
    C22["C22【函数调用 RCE1377】R0446读取生命周期项"]
    C23["C23【函数调用 RCE3016】R0843生命周期.当前()"]
    D24{"D24【本函数内具体指令】生命周期存在且当前"}
    C25["C25【函数调用 RCE1378】R0079读取指向任务的归属关系"]
    L26["L26【本函数内具体指令】遍历并筛选来源需求端点"]
    C27["C27【函数调用 RCE3015】F0051比较归属源节点与来源需求"]
    C28["C28【函数调用 RCE1374/RCE1376】形成并设置唯一授权关系"]
    D29{"D29【本函数内具体指令】授权关系存在"}
    N30["N30【本函数内具体指令】把必需证据、节点和生命周期写入值式输出；可选写入实际结果"]
    C31["C31【函数调用 RCE1373】R0078读取任务方法选择发布组"]
    D32{"D32【本函数内具体指令】发布不多于一条且有发布时方法投影存在"}
    C33["C33【函数调用 RCE1374】R0436形成发布证据"]
    C34["C34【函数调用 RCE1379】R0447读取选择记录"]
    D35{"D35【本函数内具体指令】发布证据与选择材料存在"}
    N36["N36【本函数内具体指令】写入当前选择"]
    R37["R37【本函数内具体指令】写已找到并返回输出"]

    S --> C01 --> C02 --> D03
    D03 -- 否 --> R04
    D03 -- 是 --> D05
    D05 -- 否 --> R06
    D05 -- 是 --> N07 --> C08 --> L09
    L09 -- 本轮记录 --> C10 --> C11 --> C12 --> D13
    D13 -- 不支持 --> L09
    D13 -- 结构无效 --> C15
    D13 -- 匹配 --> C14
    C14 -- 重复 --> C15
    C14 -- 唯一 --> L09
    L09 -- 遍历结束 --> C16 --> L17
    L17 -- 本轮顺序号0 --> C18 --> C19
    C18 -- 无效 --> C15
    C19 -- 重复 --> C15
    C19 -- 唯一 --> L17
    L17 -- 遍历结束 --> C20 --> D21
    D21 -- 否 --> C15
    D21 -- 是 --> C22 --> C23 --> D24
    D24 -- 否 --> C15
    D24 -- 是 --> C25 --> L26
    L26 -- 端点不匹配 --> L26
    L26 -- 本轮匹配 --> C27 --> C28
    C28 -- 无效或重复 --> C15
    C28 -- 唯一 --> L26
    L26 -- 遍历结束 --> D29
    D29 -- 否 --> C15
    D29 -- 是 --> N30 --> C31 --> D32
    D32 -- 否 --> C15
    D32 -- 无发布 --> R37
    D32 -- 一条发布 --> C33 --> C34 --> D35
    D35 -- 否 --> C15
    D35 -- 是 --> N36 --> R37
```

## 当前边界与规范核对

- 本函数只聚合任务当前身份、关系、生命周期、授权和可选选择材料，不创建、更新或发布这些事实。
- 进入身份检查后的结构缺失、重复或端点不一致统一走内部不一致，不得降级为普通未找到。
