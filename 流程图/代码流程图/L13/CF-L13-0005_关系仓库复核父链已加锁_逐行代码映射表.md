# CF-L13-0005 关系仓库复核父链已加锁逐行代码映射表

代码版本：`7ad8cf5113162fe92b9f8d43f2b5b9f00d292d1a`
源码事实提交：`3920a74638264f9f539d50f32f3c9fe5fb1982ab`
源码 blob：`f7a9ea9a09d3065468208c16f9ea34f806b6e183`
覆盖文件：`海中鱼巣/核心/关系仓库.cpp:702-731`
根函数：R0087 `关系仓库::父链复核结果 关系仓库::复核父链_已加锁(节点句柄 起点, 节点句柄 目标, std::vector<节点句柄>* 父链节点组) const`
配对流程图：`流程图/代码流程图/L13/CF-L13-0005_关系仓库复核父链已加锁_现状流程图.md`
依据：入边 RCE0437；出边 RCE0495/RCE0496；外部边界 X01510/X01511/X01512/X02919/X02920/X02921/X02922/X02923/X02924/X02925

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/关系仓库.cpp` | 702-705 | 定义 R0087，绑定起点、目标和可空父链输出指针，借用已加锁 const 仓库 | N01 | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 706 | 经 X01510 以起点构造局部已经过节点向量 | X02 | 外部边界 | 构造 | 仅局部访问集合 | 当前循环材料 | 不适用 | 无 |
| 同上 | 707 | 把当前节点初始化为起点 | N03 | 本函数内具体指令 | 赋值 | 仅局部变量 | 当前循环材料 | 不适用 | 无 |
| 同上 | 708 | 经 X02919 读取关系表大小，加一形成最大检查数量 | X04 / N05 | 外部边界 / 本函数内具体指令 | 读取 / 纯计算 | 无 | 当前循环上限 | 不适用 | 无 |
| 同上 | 709 | 初始化已检查数量并按上限循环，每轮结束递增 | N06 / B07 / N28 | 本函数内具体指令 | 循环控制 | 仅局部计数 | 当前循环 | 不适用 | 无 |
| 同上 | 710 | 经 RCE0495 调用 R0086 复核当前节点的普通父关系 | C08 | 函数调用 | 调用 / 读取 | 无 | 当前父关系复核 | 不适用 | 无 |
| 同上 | 711 | 判断当前父关系是否结构不一致 | B09 | 本函数内具体指令 | 判断 | 无 | 当前复核材料 | 不适用 | 无 |
| 同上 | 712 | 结构不一致时设置结果，经 X02925 清理访问向量并返回 | N10 / X29 / R30 | 本函数内具体指令 / 外部边界 | 返回 / 析构 | 无 | 当前复核结果 | 结构不一致 | 无 |
| 同上 | 713 | 闭合结构不一致分支 | B09 | 本函数内具体指令 | 作用域 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 714 | 经 X02920 判断父节点 optional 是否持值 | X11 / B12 | 外部边界 / 本函数内具体指令 | 读取 / 判断 | 无 | 当前复核材料 | 不适用 | 无 |
| 同上 | 715 | 无父节点时设置未包含目标结果，经 X02925 清理并返回 | N13 / X29 / R30 | 本函数内具体指令 / 外部边界 | 返回 / 析构 | 无 | 当前复核结果 | 合法链终点 | 无 |
| 同上 | 716 | 闭合无父节点分支 | B12 | 本函数内具体指令 | 作用域 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 717 | 经 X02921 读取父节点 optional 值并复制为当前父节点 | X14 | 外部边界 | 读取 / 赋值 | 仅局部变量 | 当前父节点 | 不适用 | 无 |
| 同上 | 718 | 判断调用方父链节点组指针是否非空 | B15 | 本函数内具体指令 | 判断 | 无 | 当前输出契约 | 不适用 | 无 |
| 同上 | 719 | 非空时经 X02922 向调用方父链节点组追加当前父节点 | X16 | 外部边界 | 写入 | 修改调用方提供的输出向量 | 当前输出材料 | 不适用 | 无 |
| 同上 | 720 | 闭合可选输出分支 | B15 | 本函数内具体指令 | 作用域 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 721 | 经 RCE0496/F0051 比较当前父节点与目标 | C17 / B18 | 函数调用 / 本函数内具体指令 | 调用 / 判断 | 无 | 当前目标比较 | 不适用 | 无 |
| 同上 | 722 | 相等时设置包含目标结果，经 X02925 清理并返回 | N19 / X29 / R30 | 本函数内具体指令 / 外部边界 | 返回 / 析构 | 无 | 当前复核结果 | 不适用 | 无 |
| 同上 | 723 | 闭合命中目标分支 | B18 | 本函数内具体指令 | 作用域 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 724 | 经 X02923/X02924 形成范围，X01511 执行查找并由 RCE0496/F0051 比较节点；再经 X02924/X01512 判断是否已访问 | X20 / X21 / X22 / C23 / X24 / B25 | 外部边界 / 函数调用 / 本函数内具体指令 | 查找 / 调用 / 判断 | 无 | 当前环检测 | 不适用 | 无 |
| 同上 | 725 | 已访问时设置结构不一致结果，经 X02925 清理并返回 | N10 / X29 / R30 | 本函数内具体指令 / 外部边界 | 返回 / 析构 | 无 | 当前环检测结果 | 结构不一致 | 无 |
| 同上 | 726 | 闭合已访问分支 | B25 | 本函数内具体指令 | 作用域 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 727 | 经 X02922 把当前父节点追加到局部已经过节点向量 | X26 | 外部边界 | 写入 | 仅局部访问集合 | 当前环检测材料 | 不适用 | 无 |
| 同上 | 728 | 把当前节点更新为当前父节点 | N27 | 本函数内具体指令 | 赋值 | 仅局部变量 | 当前循环材料 | 不适用 | 无 |
| 同上 | 729 | 递增已检查数量并回到循环上限判断 | N28 / B07 | 本函数内具体指令 | 循环控制 | 仅局部计数 | 当前循环 | 不适用 | 无 |
| 同上 | 730 | 达到检查上限时设置结构不一致结果，经 X02925 清理并返回 | N31 / X29 / R30 | 本函数内具体指令 / 外部边界 | 返回 / 析构 | 无 | 当前上限收口 | 结构不一致 | 无 |
| 同上 | 731 | 闭合函数作用域 | X29 / R30 | 外部边界 / 本函数内具体指令 | 作用域 / 异常清理 | 无 | 当前生命周期 | 异常向外传播 | 无 |

覆盖结论：702—731 行全部映射；全部稳定项目边、10 个外部边界和图节点可反查。
