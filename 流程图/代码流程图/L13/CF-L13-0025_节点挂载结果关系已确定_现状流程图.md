# CF-L13-0025 节点挂载结果关系已确定现状流程图

图类型：现状流程图
代码版本：`03a8b7ac099ccea83e57f2696e2290b7bcdfacaf`
当前工作区复核基线：`8632fb891fb3beea255aff1946c07b0fe975ef2b`
源码 blob：`0628ee4ade067238e826b26753d806c05b507764`
覆盖文件：`海中鱼巣/核心/关系仓库.h:155-167`
唯一根函数：R0744
完整签名：`bool 海中鱼巣::节点挂载结果::关系已确定() const`
参数：无显式参数；只读节点、新父节点、旧父节点、父关系与挂载状态
返回结果：端点与父关系有效，且当前状态对应的旧父条件成立时返回 `true`；否则返回 `false`
调用图：`流程图/主程序可达调用关系/00_main可达函数身份表.md`、`01_main可达项目调用边表.md`、`02_main可达外部边界表.md`
已登记调用方：R0743
直接项目函数：F0163（RCE2435，两次）、F0051（RCE2436，两次）
外部边界：X02786—X02790、X04914
逐行映射表：`流程图/代码流程图/L13/CF-L13-0025_节点挂载结果关系已确定_逐行代码映射表.md`
输入契约 / 调用语境表：本图内嵌审查；调用方查询关系仓库已经形成的节点挂载结果
非成功返回二分审查表：本图内嵌审查；`false` 表示结果字段不满足对应状态的一致性条件
偏差清单：无独立文件；本批只记录当前代码事实
依据提交 / 结构化消息：冻结源码提交 `03a8b7ac099ccea83e57f2696e2290b7bcdfacaf`、正式图谱与顶层稳定边裁决
验证输出：Mermaid / 节点集合 / 逐行覆盖 PASS；目标 no-index 与全仓 diff 检查 PASS；strict 108/108 PASS；未构建、未运行
不得作为施工许可：是
不得宣称：关系确定分类已证明仓库终态、能力载荷或业务闭环完成验收

## 流程图

```mermaid
flowchart TD
 N01["N01【本函数内具体指令】进入R0744并只读绑定挂载结果字段"] --> C02["C02【函数调用 RCE2435】F0163 句柄有效(节点) -> 节点有效"]
 C02 --> B03{"B03【本函数内具体指令】节点有效"}
 B03 -- 否 --> R10["R10【本函数内具体指令】返回false"]
 B03 -- 是 --> C04["C04【函数调用 RCE2435】F0163 句柄有效(新父节点) -> 新父有效"]
 C04 --> B05{"B05【本函数内具体指令】新父有效"}
 B05 -- 否 --> R10
 B05 -- 是 --> C06["C06【函数调用 RCE2436】F0051 比较节点与新父节点 -> 端点相同"]
 C06 --> B07{"B07【本函数内具体指令】节点等于新父节点"}
 B07 -- 是 --> R10
 B07 -- 否 --> C08["C08【外部边界 X02786】父关系.has_value() -> 父关系存在"]
 C08 --> B09{"B09【本函数内具体指令】父关系存在"}
 B09 -- 否 --> R10
 B09 -- 是 --> B11{"B11【本函数内具体指令】状态为已创建"}
 B11 -- 是 --> C12["C12【外部边界 X02787】旧父节点.has_value() -> 旧父存在"]
 C12 --> B13{"B13【本函数内具体指令】旧父不存在"}
 B13 -- 是 --> R14["R14【本函数内具体指令】返回true"]
 B13 -- 否 --> R10
 B11 -- 否 --> B15{"B15【本函数内具体指令】状态为已重挂"}
 B15 -- 是 --> C16["C16【外部边界 X02787】旧父节点.has_value() -> 旧父存在"]
 C16 --> B17{"B17【本函数内具体指令】旧父存在"}
 B17 -- 否 --> R10
 B17 -- 是 --> C18["C18【外部边界 X02788】旧父节点.value() -> 旧父句柄"]
 C18 --> C19["C19【函数调用 RCE2436】F0051 比较旧父与新父 -> 父节点相同"]
 C19 --> B20{"B20【本函数内具体指令】父节点不同"}
 B20 -- 是 --> R14
 B20 -- 否 --> R10
 B15 -- 否 --> B21{"B21【本函数内具体指令】状态为已在目标父"}
 B21 -- 否 --> R10
 B21 -- 是 --> C22["C22【外部边界 X02790】由新父节点构造比较用optional"]
 C22 --> C23["C23【外部边界 X02789】比较旧父optional与新父optional -> 相等"]
 C23 --> L24["L24【外部边界 X04914】析构比较用临时optional并保留比较结果"]
 L24 --> B25{"B25【本函数内具体指令】旧父可选值等于新父可选值"}
 B25 -- 是 --> R14
 B25 -- 否 --> R10
```

## 当前边界

- `已创建`、`已重挂` 与 `已在目标父` 三种状态使用不同旧父节点条件；其它状态返回 `false`。
- X04914 只承载第 166 行比较用临时 `optional<节点句柄>` 的生命周期结束，不形成项目函数身份。
