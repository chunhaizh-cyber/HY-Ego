# CF-L13-0018 读取结算实例状态已许可逐行代码映射表

代码版本：`3920a74638264f9f539d50f32f3c9fe5fb1982ab`
当前工作区复核基线：`fe2ca0c3e7b8f734053b87b96de65ea0f2e57b67`
源码 blob：`16ac9534baeda26ac8a712066e713f3339f6e0c8`
覆盖文件：`海中鱼巣/领域/数据操作.需求任务方法.ixx:2001-2072`
根函数：R0440 `需求任务方法读取状态 需求任务方法数据操作::读取结算实例状态_已许可(节点句柄,const 结构事务令牌&,状态值式材料&) const`
配对流程图：`流程图/代码流程图/L13/CF-L13-0018_读取结算实例状态已许可_现状流程图.md`
稳定边全集：RCE1352—RCE1358、RCE3004—RCE3006；X04039—X04053

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/数据操作.需求任务方法.ixx` | 2001—2004 | 进入 R0440，绑定状态、只读令牌和可写输出 | S | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 2005 | 经 RCE1352 读取状态身份 | C01 | 函数调用 | 读取 | 无 | 权威事实读取 | 不适用 | 无 |
| 同上 | 2006 | 经 RCE1353 复核当前可读；失败返回身份状态 | C02、D03、R04 | 函数调用 / 本函数内具体指令 | 判断 / 返回 | 无 | 当前结构化结果 | 入口或当前性结果 | 无 |
| 同上 | 2007 | 判断节点类型；非状态返回类型不匹配 | D05、R06 | 本函数内具体指令 | 判断 / 返回 | 无 | 当前结构化结果 | 入口拒绝式只读结果 | 无 |
| 同上 | 2009—2014 | 四次经 RCE3004 读取状态值、时间戳、幂等低位和高位 | C07、C08、C09、C10 | 函数调用 | 读取 | 无 | 权威主信息读取 | 不适用 | 无 |
| 同上 | 2015—2018 | 经 X04039、X04040 复核四值存在、时间正值且高低位在 uint32 范围 | D11 | 函数调用 / 本函数内具体指令 | 判断 / 纯计算 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 2019 | 经 RCE1354 合并幂等编号并比较身份主键 | C12、D13 | 函数调用 / 本函数内具体指令 | 纯计算 / 判断 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 2020—2022 | 同时复核结论为已满足；任一失败返回内部不一致 | D13、R14 | 本函数内具体指令 | 判断 / 返回 | 无 | 当前结构化结果 | 追根因解决 | 不降级为未找到 |
| 同上 | 2024—2025 | 建立两个空的高级关系证据 optional | N15 | 本函数内具体指令 | 局部构造 | 无 | 临时局部变量 | 不适用 | 无 |
| 同上 | 2026 | 经 RCE1355 获取状态的引用关系组，进入 X04042—X04047 范围循环 | C16、L17 | 函数调用 / 本函数内具体指令 | 读取 / 循环控制 | 无 | 权威关系读取 | 不适用 | 无 |
| 同上 | 2027—2028 | 经 RCE1356 形成证据并经 X04048 复核存在 | C18、D19 | 函数调用 | 读取 / 判断 | 无 | 非持久关系投影 | 不适用 | 无 |
| 同上 | 2028 | 证据为空时返回内部不一致 | R20 | 本函数内具体指令 | 返回 | 无 | 当前结构化结果 | 追根因解决 | 无 |
| 同上 | 2029—2030 | 经 RCE1352 读取目标身份，经 RCE1353 复核当前可读 | C21、C22、D23 | 函数调用 / 本函数内具体指令 | 读取 / 判断 | 无 | 权威事实读取 | 不适用 | 无 |
| 同上 | 2030 | 目标不可读时返回其读取状态 | R24 | 本函数内具体指令 | 返回 | 无 | 当前结构化结果 | 当前性结果 | 无 |
| 同上 | 2031—2034 | 目标为存在时，经 RCE1357 设置唯一主体关系；重复则内部不一致 | D25、C26、D27、R28 | 函数调用 / 本函数内具体指令 | 判断 / 局部赋值 / 返回 | 只改变局部 optional | 当前结构化结果 | 追根因解决 | 无 |
| 同上 | 2035—2038 | 目标为场景时，经 RCE1357 设置唯一场景关系；重复则内部不一致 | D29、C30、D31、R32 | 函数调用 / 本函数内具体指令 | 判断 / 局部赋值 / 返回 | 只改变局部 optional | 当前结构化结果 | 追根因解决 | 无 |
| 同上 | 2039—2040 | 其它目标类型不采纳；推进并闭合本轮循环 | N33、L17 | 本函数内具体指令 | 循环控制 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 2041—2043 | 主体或场景关系缺失时返回内部不一致 | D34、R35 | 函数调用 / 本函数内具体指令 | 判断 / 返回 | 无 | 当前结构化结果 | 追根因解决 | 无 |
| 同上 | 2045 | 建立空的临时关系 optional | N36 | 本函数内具体指令 | 局部构造 | 无 | 临时局部变量 | 不适用 | 无 |
| 同上 | 2046—2047 | 经 RCE1355 获取场景运行期临时关系组并进入范围循环 | C37、L38 | 函数调用 / 本函数内具体指令 | 读取 / 循环控制 | 无 | 权威关系读取 | 不适用 | 无 |
| 同上 | 2048 | 经 RCE3005 比较记录目标与状态；不相等则继续 | C39、D40、N41 | 函数调用 / 本函数内具体指令 | 判断 / 循环控制 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 2049 | 经 RCE1356 形成当前记录证据 | C42 | 函数调用 | 读取 | 无 | 非持久关系投影 | 不适用 | 无 |
| 同上 | 2050—2052 | 证据为空或此前已命中时返回内部不一致 | D43、R44 | 函数调用 / 本函数内具体指令 | 判断 / 返回 | 无 | 当前结构化结果 | 追根因解决 | 无 |
| 同上 | 2053—2054 | 经 X04052 保存唯一临时关系并推进循环 | C45、N46、L38 | 函数调用 / 本函数内具体指令 | 局部赋值 / 循环控制 | 只改变局部 optional | 临时局部变量 | 不适用 | 无 |
| 同上 | 2055 | 临时关系缺失时返回内部不一致 | D47、R48 | 函数调用 / 本函数内具体指令 | 判断 / 返回 | 无 | 当前结构化结果 | 追根因解决 | 无 |
| 同上 | 2057—2065 | 写入输出读取状态、种类、幂等主键、节点、主信息、值、时间、主体与场景 | N49 | 本函数内具体指令 | 局部输出赋值 | 只改变调用方输出材料 | 非持久值式投影 | 不适用 | 无 |
| 同上 | 2066—2068 | 三次经 RCE1358 转换主体、场景与临时关系证据并写入输出 | C50、C51、C52 | 函数调用 | 局部输出赋值 | 只改变调用方输出材料 | 非持久值式投影 | 不适用 | 无 |
| 同上 | 2069 | 经 RCE3006 复核输出完整实例状态 | C53、D54 | 函数调用 | 判断 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 2070—2072 | 按完整性返回已找到或内部不一致，释放全部 optional / vector 并闭合作用域 | R55、R56 | 本函数内具体指令 | 资源释放 / 返回 / 作用域闭合 | 无持久写入 | 当前结构化结果 | 成功或追根因解决 | 无 |

覆盖结论：2001—2072 行全部映射；流程图全部节点均在本表出现，本表没有图外节点。
