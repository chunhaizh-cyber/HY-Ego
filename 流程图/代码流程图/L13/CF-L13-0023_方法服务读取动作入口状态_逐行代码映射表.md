# CF-L13-0023 方法服务读取动作入口状态逐行代码映射表

代码版本：`0a93526882cb33c61c2fbb04ecad1aeaddcfe225`
当前工作区复核基线：`ece29318f80cad2ce7a4abce9b009c60cd4cdfdd`
源码 blob：`839dd462c70e8d54b6f22f1126d751ac9425398a`
覆盖文件：`海中鱼巣/领域/方法服务.h:1818-1832`
根函数：R0487
配对流程图：`流程图/代码流程图/L13/CF-L13-0023_方法服务读取动作入口状态_现状流程图.md`
项目边：RCE1484—RCE1487
稳定外部边界校正：X02172
外部边界：X04504—X04510

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 海中鱼巣/领域/方法服务.h | 1818 | 声明动作入口状态只读入口 | C01 | 本函数内具体指令 | 入口 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 1819-1821 | 调 F0327 读取角色；非动作入口时构造并返回空 optional | C01 / B02 / X18 / X15 / R16 | 函数调用 / 本函数内具体指令 | 读取 / 判断 / 构造 / 返回 | 无 | 当前入口材料 | 逻辑内返回 | 校正 RCE1484；新增空 optional 边界 |
| 同上 | 1822 | 调 F0330 按模板、状态类型和顺序号1读取唯一状态节点 | C03 | 函数调用 | 读取 | 只读关系 | 当前查询结果 | 不适用 | 校正 RCE1485 |
| 同上 | 1823-1825 | 检查状态节点 optional；无值时构造空返回 | X04 / B05 / X18 / X15 / R16 | 函数调用 / 本函数内具体指令 | 判断 / 构造 / 返回 | 无 | 当前查询结果 | 逻辑内返回 | 新增 has_value/nullopt/lifecycle 边界 |
| 同上 | 1826 | 取得状态节点值并调 F0329 读取 I64 状态值 | X06 / C07 | 函数调用 | 读取 | 只读状态 | 当前读回证据 | 不适用 | 校正 RCE1486；新增 value 边界 |
| 同上 | 1827-1829 | 检查状态值 optional；无值时构造空返回 | X08 / B09 / X18 / X15 / R16 | 函数调用 / 本函数内具体指令 | 判断 / 构造 / 返回 | 无 | 当前读回证据 | 逻辑内返回 | 新增 has_value/nullopt/lifecycle 边界 |
| 同上 | 1830 | 取得 I64 值并静态转换为动作入口状态枚举 | X10 / N11 | 函数调用 / 本函数内具体指令 | 读取 / 纯计算 | 无 | 当前读回材料 | 不适用 | 新增 optional value 边界 |
| 同上 | 1831-1832 | 调 R0484；有效时经 X02172 构造有值 optional，否则构造空 optional；释放局部 optional 并返回 | C12 / B13 / X14 / X18 / X15 / R16 | 函数调用 / 本函数内具体指令 | 判断 / 构造 / 生命周期 / 返回 | 无 | 当前返回材料 | 非白名单为逻辑内返回 | 校正 RCE1487、X02172；新增生命周期边界 |

外部边界校正与建议：

| 动作 / 临时 ID | 完整签名 | 位置 | 次数 |
| --- | --- | --- | --- |
| 替换 X02172 | `std::optional<海中鱼巣::动作入口状态>::optional(const 海中鱼巣::动作入口状态&)` | 1831 | 1 |
| X04504 | `std::optional<海中鱼巣::动作入口状态>::optional(std::nullopt_t) noexcept` | 1820、1824、1828、1831 | 4 |
| X04505 | `bool std::optional<海中鱼巣::节点句柄>::has_value() const noexcept` | 1823 | 1 |
| X04506 | `const 海中鱼巣::节点句柄& std::optional<海中鱼巣::节点句柄>::value() const&` | 1826 | 1 |
| X04507 | `std::optional<海中鱼巣::节点句柄>::~optional()` | 1822-1832 | 1 |
| X04508 | `bool std::optional<std::int64_t>::has_value() const noexcept` | 1827 | 1 |
| X04509 | `const std::int64_t& std::optional<std::int64_t>::value() const&` | 1830 | 1 |
| X04510 | `std::optional<std::int64_t>::~optional()` | 1826-1832 | 1 |

覆盖结论：1818—1832 行全部非空有效源码行已映射；十八个图节点全部可从映射双向反查。
