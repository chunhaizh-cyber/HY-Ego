# CF-L13-0011 准备原始材料提交逐行代码映射表

代码版本：`3920a74638264f9f539d50f32f3c9fe5fb1982ab`
源码 blob：`d82fa092a2c1156d2f8de758d5e32415cb64931d`
覆盖文件：`海中鱼巣/领域/参与者.特征值原始材料.ixx:105-173`
根函数：R0260
配对流程图：`流程图/代码流程图/L13/CF-L13-0011_准备原始材料提交_现状流程图.md`
稳定边全集：RCE0547、RCE0866—RCE0873、X02034—X02046、X03226—X03231

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/参与者.特征值原始材料.ixx` | 105 | 定义 R0260 并绑定只读准备视图 | N01 | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 106-108 | 经 X02034 检查材料组；阶段错误或材料为空时返回入口拒绝 | C02 / D03 / R04 | 函数调用 / 本函数内具体指令 | 外部调用 / 判断 / 返回 | 无 | 当前代码事实 | 逻辑内返回 | 无 |
| 同上 | 110 | 经 X03226—X03230 进入第一轮材料范围遍历 | L05 | 函数调用 / 本函数内具体指令 | 外部调用 / 循环控制 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 111-116 | 经 RCE0866—RCE0869、X02035、X02036 复核节点和主信息候选、类型及绑定 | C06 / C07 / C08 / D09 | 函数调用 / 本函数内具体指令 | 调用 / 外部调用 / 读取 / 判断 | 无 | 当前代码事实 | 追根因解决 | 无 |
| 同上 | 117 | 候选不匹配时返回内部不一致和节点编号 | R10 | 本函数内具体指令 | 返回 | 无 | 当前代码事实 | 追根因解决 | 无 |
| 同上 | 118-123 | 经 RCE0870、X02037、X02038 读取并复核 I64 候选；不一致时返回内部不一致 | C11 / C12 / D13 / R10 | 函数调用 / 本函数内具体指令 | 调用 / 外部调用 / 判断 / 返回 | 无 | 当前代码事实 | 追根因解决 | 无 |
| 同上 | 125-126 | 经 X02039 构造 try_to_lock 独占锁并经 X02040 检查许可 | C14 / C15 / D16 | 函数调用 / 本函数内具体指令 | 资源操作 / 判断 | 取得或未取得侧表写锁 | 当前代码事实 | 逻辑内返回 | 无 |
| 同上 | 126 | 未取得锁时形成许可拒绝结果，退出时经 X03231 析构局部锁 | R17 / C35 / R36 | 函数调用 / 本函数内具体指令 | 返回 / 资源释放 | 无 | 当前代码事实 | 逻辑内返回 | 无 |
| 同上 | 128-136 | 经 X03226—X03230 遍历材料，经 RCE0871、RCE0872 读取两表数量并拒绝内部不一致或版本漂移 | L18 / C19 / D20 / R21 / R22 / C35 / R36 | 函数调用 / 本函数内具体指令 | 循环 / 调用 / 判断 / 返回 / 资源释放 | 无 | 权威事实 | 追根因解决 / 逻辑内返回 | 无 |
| 同上 | 139-144 | 初始化两个数量并经 X03226—X03230 统计 I64 与 Vec 材料 | L23 | 函数调用 / 本函数内具体指令 | 赋值 / 循环控制 / 纯计算 | 仅局部计数 | 当前代码事实 | 不适用 | 无 |
| 同上 | 145-149 | 进入 try，经 X02041、X02042 读取服务表数量并为四个向量预留容量 | C24 | 函数调用 | 外部调用 / 资源操作 | 调整容器容量，不增加记录 | 当前代码事实 | 追根因解决 | 无 |
| 同上 | 150-154 | 经 X03226—X03230 遍历；I64 项经 X02043 追加服务记录和撤销跟踪并继续 | L25 / D26 / C27 | 函数调用 / 本函数内具体指令 | 循环 / 判断 / 写入 | 插入 I64 版本记录和跟踪节点 | 权威事实 | 不适用 | 无 |
| 同上 | 156-161 | 构造 Vec 记录，写节点、类型、两个值向量和版本；值向量复制经 X02044 | N28 / C29 | 函数调用 / 本函数内具体指令 | 构造 / 写入 / 外部调用 | 构造局部 Vec 候选记录 | 当前代码事实 | 不适用 | 无 |
| 同上 | 162-164 | 经 X02045、X02043 移动追加 Vec 记录和跟踪节点并闭合 try | C29 / L25 | 函数调用 / 本函数内具体指令 | 外部调用 / 写入 / 循环 | 插入 Vec 记录和跟踪节点 | 权威事实 | 不适用 | 无 |
| 同上 | 165-168 | 捕获异常，经 RCE0873 调用 R0264 回前态并形成内部不一致结果 | C30 / R31 | 函数调用 / 本函数内具体指令 | 异常 / 调用 / 撤销 / 返回 | 尝试删除本轮已插入记录 | 权威事实 | 追根因解决 | 无 |
| 同上 | 170 | 经 X02045、X02046 把局部锁所有权移动给原始值锁_ | C32 | 函数调用 | 资源操作 / 写入 | 参与者持有侧表写锁 | 当前代码事实 | 不适用 | 无 |
| 同上 | 171 | 把阶段写为已准备 | N33 | 本函数内具体指令 | 写入 | 更新参与者阶段 | 当前代码事实 | 不适用 | 无 |
| 同上 | 172-173 | 形成候选已确认；局部锁经 X03231 析构后返回并闭合函数 | R34 / C35 / R36 | 函数调用 / 本函数内具体指令 | 资源释放 / 返回 / 作用域 | 局部 moved-from 锁销毁 | 当前代码事实 | 不适用 | 无 |

覆盖结论：105—173 行全部映射；R0260 与全部稳定边、节点可双向反查。
