# CF-L13-0025 节点挂载结果关系已确定逐行代码映射表

代码版本：`03a8b7ac099ccea83e57f2696e2290b7bcdfacaf`；当前工作区复核基线：`8632fb891fb3beea255aff1946c07b0fe975ef2b`；源码 blob：`0628ee4ade067238e826b26753d806c05b507764`
覆盖文件：`海中鱼巣/核心/关系仓库.h:155-167`；根函数：R0744
配对流程图：`流程图/代码流程图/L13/CF-L13-0025_节点挂载结果关系已确定_现状流程图.md`；稳定边全集：RCE2435、RCE2436、X02786—X02790、X04914

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/关系仓库.h` | 155 | 定义并进入 R0744，只读绑定挂载结果字段 | N01 | 本函数内具体指令 | 入口 / 读取 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 156 | 两次调用 F0163 核对端点，调用 F0051 排除自挂载，再用 X02786 检查父关系存在 | C02 / B03 / C04 / B05 / C06 / B07 / C08 / B09 | 函数调用 / 本函数内具体指令 | 调用 / 判断 | 无 | 当前代码事实 | 任一条件失败返回 `false` | 无 |
| 同上 | 157-158 | 端点或父关系前置失败时返回 `false` 并闭合分支 | R10 | 本函数内具体指令 | 返回 / 作用域 | 无 | 当前代码事实 | 逻辑内返回 | 无 |
| 同上 | 159 | 判断状态是否为已创建 | B11 | 本函数内具体指令 | 判断 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 160-161 | 已创建时调用 X02787 检查旧父不存在，并返回该布尔条件 | C12 / B13 / R14 / R10 | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 | 无 | 当前代码事实 | `false` 为结果不一致分类 | 无 |
| 同上 | 162 | 判断状态是否为已重挂 | B15 | 本函数内具体指令 | 判断 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 163-164 | 已重挂时检查旧父存在、读取旧父并调用 F0051 判断旧父不同于新父，返回合取结果 | C16 / B17 / C18 / C19 / B20 / R14 / R10 | 函数调用 / 本函数内具体指令 | 调用 / 读取 / 判断 / 返回 | 无 | 当前代码事实 | `false` 为结果不一致分类 | 无 |
| 同上 | 165 | 判断剩余状态是否为已在目标父 | B21 | 本函数内具体指令 | 判断 | 无 | 当前代码事实 | 其它状态返回 `false` | 无 |
| 同上 | 166 | 由新父构造临时 optional，比较两个可选值，析构临时 optional，再按比较结果返回 | C22 / C23 / L24 / B25 / R14 / R10 | 函数调用 / 本函数内具体指令 | 构造 / 调用 / 生命周期 / 判断 / 返回 | 无 | 当前代码事实 | `false` 为结果不一致分类 | 无 |
| 同上 | 167 | 闭合 R0744 | R14 / R10 | 本函数内具体指令 | 作用域结束 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：155—167 全部源码行已映射；N01、C02、B03、C04、B05、C06、B07、C08、B09、R10、B11、C12、B13、R14、B15、C16、B17、C18、C19、B20、B21、C22、C23、L24、B25 全部节点可反查。
