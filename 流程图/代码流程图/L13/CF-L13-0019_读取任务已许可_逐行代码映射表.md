# CF-L13-0019 读取任务已许可逐行代码映射表

代码版本：`3920a74638264f9f539d50f32f3c9fe5fb1982ab`
当前工作区复核基线：`af74e46ef3fd0b1b2c108d695569855909a59ee1`
源码 blob：`16ac9534baeda26ac8a712066e713f3339f6e0c8`
覆盖文件：`海中鱼巣/领域/数据操作.需求任务方法.ixx:2313-2401`
根函数：R0444 `任务权威材料 需求任务方法数据操作::读取任务_已许可(节点句柄,const 结构事务令牌&) const`
配对流程图：`流程图/代码流程图/L13/CF-L13-0019_读取任务已许可_现状流程图.md`
稳定边全集：RCE1372—RCE1379、RCE3014—RCE3016；X04089—X04107

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/数据操作.需求任务方法.ixx` | 2313—2316 | 进入 R0444，绑定任务与令牌并默认构造值式输出 | S | 本函数内具体指令 | 入口 / 参数绑定 / 构造 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 2317 | 经 RCE1372 读取任务身份并写入输出 | C01 | 函数调用 | 读取 / 局部赋值 | 无 | 身份材料 | 不适用 | 无 |
| 同上 | 2318—2321 | 经 RCE3014 检查当前可读；失败时透传身份状态并返回 | C02、D03、R04 | 函数调用 / 本函数内具体指令 | 判断 / 返回 | 无 | 当前结构化结果 | 逻辑内返回 | 保持许可与版本状态透传 |
| 同上 | 2322—2326 | 判断身份类型；非任务时写类型不匹配并返回，随后跨过分段空行 | D05、R06 | 本函数内具体指令 | 判断 / 返回 / 排版 | 无 | 当前结构化结果 | 逻辑内返回 | 无 |
| 同上 | 2327—2332 | 默认构造六个关系证据 optional | N07 | 本函数内具体指令 | 构造 | 无 | 临时局部变量 | 不适用 | 无 |
| 同上 | 2333 | 经 RCE1373 读取引用关系记录组并进入范围循环 | C08、L09 | 函数调用 / 本函数内具体指令 | 读取 / 循环控制 | 无 | 权威关系读取 | 不适用 | 无 |
| 同上 | 2334—2336 | 经 RCE1372/RCE1374/RCE3014 读取目标身份、形成证据并检查可读；失败时经 RCE1375 返回内部不一致 | C10、C11、C12、D13、C15 | 函数调用 / 本函数内具体指令 | 读取 / 判断 / 返回 | 无 | 当前结构化结果 | 追根因解决 | 无 |
| 同上 | 2337—2347 | 按目标类型和顺序号选择六槽之一，经 RCE1376 设置唯一关系；重复时经 RCE1375 返回内部不一致，其它关系跳过 | D13、C14、C15、L09 | 函数调用 / 本函数内具体指令 | 判断 / 局部赋值 / 循环控制 / 返回 | 无 | 当前结构化结果 | 追根因解决 | 不接受同槽重复关系 |
| 同上 | 2348—2350 | 结束引用循环，经 RCE1373 读取模板关系组并筛选顺序号 0 | C16、L17 | 函数调用 / 本函数内具体指令 | 读取 / 循环控制 | 无 | 权威关系读取 | 不适用 | 无 |
| 同上 | 2351—2357 | 经 RCE1372/RCE1374/RCE3014 读取目标、形成证据并验证可读状态，经 RCE1376 设置唯一目标状态；失败或重复时经 RCE1375 返回内部不一致，随后跨过分段空行 | C18、C19、C15、L17 | 函数调用 / 本函数内具体指令 | 读取 / 判断 / 局部赋值 / 返回 / 排版 | 无 | 当前结构化结果 | 追根因解决 | 无 |
| 同上 | 2358—2360 | 经 RCE1373 读取生命周期组，要求恰一条且来源需求、虚拟存在、场景、目标状态四项均存在 | C20、D21、C15 | 函数调用 / 本函数内具体指令 | 读取 / 判断 / 返回 | 无 | 当前结构化结果 | 追根因解决 | 无 |
| 同上 | 2361—2363 | 经 RCE1377 读取生命周期项，经 RCE3016 检查当前；缺失或非当前时经 RCE1375 返回内部不一致，随后跨过分段空行 | C22、C23、D24、C15 | 函数调用 / 本函数内具体指令 | 读取 / 判断 / 返回 / 排版 | 无 | 生命周期材料 | 追根因解决 | 无 |
| 同上 | 2364—2366 | 默认构造授权 optional，经 RCE1378 读取指向任务的归属关系并经 RCE3015 比较源节点是否为来源需求 | C25、L26、C27 | 函数调用 / 本函数内具体指令 | 构造 / 读取 / 循环控制 / 判断 | 无 | 权威关系读取 | 不适用 | 无 |
| 同上 | 2367—2371 | 对匹配端点经 RCE1374 形成证据并经 RCE1376 设置唯一授权；证据无效、重复或最终缺失时经 RCE1375 返回内部不一致，随后跨过分段空行 | C28、D29、C15、L26 | 函数调用 / 本函数内具体指令 | 读取 / 判断 / 局部赋值 / 返回 / 排版 | 无 | 当前结构化结果 | 追根因解决 | 无 |
| 同上 | 2372—2381 | 解引用五项必需证据与生命周期材料，写入值式输出的节点、关系和当前生命周期字段 | N30 | 本函数内具体指令 | 局部赋值 | 只改变值式输出 | 当前结构化结果 | 不适用 | 无 |
| 同上 | 2382—2386 | 实际结果存在时把状态节点和关系写入值式输出，随后跨过分段空行 | N30 | 本函数内具体指令 | 判断 / 局部赋值 / 排版 | 只改变值式输出 | 当前结构化结果 | 不适用 | 无 |
| 同上 | 2387—2390 | 经 RCE1373 读取任务方法选择发布组；多于一条或有发布但无方法投影时经 RCE1375 返回内部不一致 | C31、D32、C15 | 函数调用 / 本函数内具体指令 | 读取 / 判断 / 返回 | 无 | 当前结构化结果 | 追根因解决 | 无 |
| 同上 | 2391—2393 | 一条发布时经 RCE1374 形成发布证据；缺失时经 RCE1375 返回内部不一致 | C33、D35、C15 | 函数调用 / 本函数内具体指令 | 读取 / 判断 / 返回 | 无 | 关系证据材料 | 追根因解决 | 无 |
| 同上 | 2394—2397 | 经 RCE1379 读取选择记录；缺失时经 RCE1375 返回内部不一致，存在时写入当前选择 | C34、D35、C15、N36 | 函数调用 / 本函数内具体指令 | 读取 / 判断 / 局部赋值 / 返回 | 无 | 当前结构化结果 | 追根因解决 | 无 |
| 同上 | 2398—2401 | 结束发布分支，写已找到并返回输出；本函数 optional/vector 生命周期归入 X04089—X04107 | R37 | 本函数内具体指令 | 局部赋值 / 返回 / 资源释放 | 无持久写入 | 当前结构化结果 | 不适用 | 不把读取成功写成任务执行完成 |

覆盖结论：2313—2401 行全部映射；流程图全部节点均在本表出现，本表没有图外节点。
