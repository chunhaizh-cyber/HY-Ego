# CF-L5-0197 `统计服务::缓存命名空间有效` 逐行代码映射表

代码版本：`7342125cb3aa42b81a2c01393ca86abd383ec387`
覆盖文件：`海中鱼巣/领域/统计服务.h:958-965`
根函数：F0373 `bool 海中鱼巣::统计服务::缓存命名空间有效(const 海中鱼巣::缓存命名空间& 命名空间) const`
配对流程图：`流程图/代码流程图/L5/CF-L5-0197_统计服务缓存命名空间有效_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4060_子规范_非权威缓存统计失效与确定重建.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 同上 | 958 | 进入 const 成员函数 F0373，借用有效 `this` 与调用方拥有的 const 命名空间引用 | S | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 悬空引用属于生命周期错误，不伪造 `false` 拒绝 |
| 同上 | 959 | 经 RCE0158 调用 F0192，按值传入 `命名空间.类型`；取得类型白名单结果，并在 `false` 时立即短路返回 | C01 / D02 / RF | 函数调用 / 本函数内具体指令 | 字段读取 / 调用 / 判断 / 返回 | 无 | 当前通用准入投影 | 未设置或未知类型为逻辑内返回 | 白名单扩展必须同步 F0192 |
| 同上 | 960 | 仅在类型有效时经 RCE0159 调用 F0193，按值传入 `命名空间.来源`；取得来源白名单结果，并在 `false` 时短路 | C03 / D04 / RF | 函数调用 / 本函数内具体指令 | 字段读取 / 调用 / 判断 / 返回 | 无 | 当前通用准入投影 | 未设置或未知来源为逻辑内返回 | 白名单扩展必须同步 F0193 |
| 同上 | 961 | 仅在前两项有效时读取来源版本并判断不等于零；为零时短路，不再读取规则版本或调用后续白名单函数 | D05 / RF | 本函数内具体指令 | 字段读取 / 判断 / 返回 | 无 | 当前通用准入投影 | 零来源版本为逻辑内返回 | 返回 `true` 仍不证明来源版本当前 |
| 同上 | 962 | 仅在来源版本非零时读取规则版本并判断不等于零；为零时短路，不调用 F0194、F0195 | D06 / RF | 本函数内具体指令 | 字段读取 / 判断 / 返回 | 无 | 当前通用准入投影 | 零规则版本为逻辑内返回 | 返回 `true` 仍不证明规则版本受支持 |
| 同上 | 963 | 仅在前四项有效时经 RCE0160 调用 F0194，按值传入 `命名空间.值类型`；取得值类型白名单结果，并在 `false` 时短路 | C07 / D08 / RF | 函数调用 / 本函数内具体指令 | 字段读取 / 调用 / 判断 / 返回 | 无 | 当前通用准入投影 | 未设置或未知值类型为逻辑内返回 | 白名单扩展必须同步 F0194 |
| 同上 | 964 | 仅在前五项有效时经 RCE0161 调用 F0195，按值传入 `命名空间.失效条件`；其布尔结果成为完整合取式最终结果 | C09 / D10 / RF / RT | 函数调用 / 本函数内具体指令 | 字段读取 / 调用 / 判断 / 返回 | 无 | 当前通用准入投影 | 未设置或未知失效条件为逻辑内返回 | 白名单扩展必须同步 F0195 |
| 同上 | 965 | 函数作用域闭合；不保存借用引用、不释放调用方对象、不写缓存或机器事实 | RT / RF | 本函数内具体指令 | 作用域 / 生命周期 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：F0373 的 958—965 行全部映射；RCE0158—RCE0161 四条出边严格按 `&&` 左到右短路；唯一直接入边为 E0785；项目调用点 4 个、外部边界 0、未解析项目调用 0。
