# CF-L5-0216 `索引仓库::按主键查节点` 逐行代码映射表

代码版本：`14d539ed7ad8af87f35fb0752eed420bbe443316`
源码 blob：`8874312f67f19b220a08b1397c6103396878513d`
覆盖文件：`海中鱼巣/核心/索引仓库.cpp:172-186`
根函数：F0544 `std::optional<海中鱼巣::节点句柄> 海中鱼巣::索引仓库::按主键查节点(std::uint64_t 主键) const`
配对流程图：`流程图/代码流程图/L5/CF-L5-0216_索引仓库按主键查节点_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`、`规范/4040_子规范_不透明结构事务候选确认撤销与最后发布.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/索引仓库.cpp` | 172 | 进入 const 成员函数 F0544；按值接收主键并借用 const 索引仓库 `this` | S | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 不保存仓库引用 |
| 同上 | 173 | 经 RCE2214 调用 F0336 读取事务接线形态；true 进入共享许可路径，false 转入未接域本地读取路径 | C01 / B02 | 函数调用 / 本函数内具体指令 | 接线读取 / 判断 | 无 | 当前事务接线形态 | 完全未接域是正常分支；正式上游保证完整时的部分接线为内部不一致 | 不把裸 bool 当持久机器事实 |
| 同上 | 174 | 仅接域时经 RCE2215 调用当前生产接线唯一目标 F0397，使用运行期状态形成局部共享许可；构造完成前异常直接传播 | C03 / E11 | 函数调用 / 本函数内具体指令 | 许可取得 / 局部构造 / 异常传播 | 无 | 当前运行期许可 | 取得失败不包装为索引未命中 | 无 |
| 同上 | 175 | 经 RCE2216 调用 F0338 判断许可有效；false 物化空 optional，true 经 RCE2217 调用 F0339 读取令牌并按 RCE0253 调用 F0441。返回表达式完成后经 RCE2218 析构许可；F0441 异常时先析构许可再传播 | C04 / B05 / C06 / C07 / N08 / D09 / R10 / D12 / E13 | 函数调用 / 本函数内具体指令 | 许可判断 / 令牌读取 / 索引读取 / RAII / 返回 / 异常传播 | 无 | 当前许可与 F0441 读取结果 | 许可无效或 F0441 空结果折叠为空 optional；强前置成立时异常须追根因 | 六个接域路径项目调用均用稳定边回指 |
| 同上 | 176 | 闭合接域判断；接域路径已经返回或传播异常，仅未接域路径继续 | B02 / R10 / E11 / E13 | 本函数内具体指令 | 分支闭合 / 生命周期 | 无 | 当前控制流 | 不适用 | RCE2218 覆盖已形成许可的正常与异常退出 |
| 同上 | 177 | 仅未接域路径判断 `主键 == 0`；为 true 时物化并返回 `std::nullopt`，不加锁、不查索引 | B14 / RN | 本函数内具体指令 | 判断 / 返回 | 无 | 当前入口材料 | 零主键为逻辑内返回 | 不把空结果解释为历史不存在 |
| 同上 | 178 | 默认构造局部节点句柄 `候选`，等待索引命中后按值赋值 | N15 | 本函数内具体指令 | 局部构造 | 只形成临时变量 | 临时局部变量 | 不适用 | 默认句柄不作为索引结果 |
| 同上 | 179 | 打开只覆盖索引查找与候选复制的内层词法作用域 | X16 前置 | 本函数内具体指令 | 作用域 | 无 | 当前代码事实 | 不适用 | 与 184 行共同冻结锁生命周期 |
| 同上 | 180 | 经 X01103 构造 `std::shared_lock<std::shared_mutex>` 并锁定 `仓库锁_`；锁取得失败时没有已形成锁，异常直接传播 | X16 / EX | 函数调用 / 本函数内具体指令 | 外部边界 / 并发 / RAII 构造 / 异常传播 | 取得索引共享读锁，不改机器事实 | 运行期资源状态 | 锁取得失败不包装为空 optional | 保持 RAII |
| 同上 | 181 | 经 X02526 调用 `主键索引_.find(主键)`，把 const 迭代器结果保存为 `位置` | X17 | 函数调用 | 外部边界 / 索引读取 | 无 | 索引候选位置 | 不适用 | 异常展开时经 X02529 释放已形成锁 |
| 同上 | 182 | 经 X02527 调用 `主键索引_.end()`，再经 X01104 比较位置与尾后位置；相等时从持锁作用域返回空 optional | X18 / X19 / B20 / RN | 函数调用 / 本函数内具体指令 | 外部边界 / 判断 / 返回 | 无 | 索引查找结果 | 索引未命中为逻辑内返回 | 返回前经 X02529 解锁 |
| 同上 | 183 | 索引命中后经 X02528 调用 const_iterator `operator->` 取得主键绑定条目，并复制 `second.节点` 到局部候选 | X21 / N22 | 函数调用 / 本函数内具体指令 | 外部边界 / 索引读取 / 值复制 | 只改变局部候选 | 非权威索引候选 | 不适用 | 不把索引候选直接当权威节点 |
| 同上 | 184 | 内层作用域闭合；经 X02529 析构 `shared_lock` 并释放索引共享锁；181—183 行异常展开也经同一析构边界释放 | X23 / X23E / EX | 函数调用 / 本函数内具体指令 | 外部边界 / RAII 析构 / 异常传播 | 释放运行期共享锁 | 运行期资源状态 | 不适用 | 后续节点读回发生在索引锁外 |
| 同上 | 185 | 经 RCE2219 调用 F0377，用候选句柄回读节点当前有效性；true 时经 X01105 从候选构造有值 optional，false 时物化空 optional，并返回条件表达式结果 | C24 / B25 / X26 / RV / RN | 函数调用 / 本函数内具体指令 | 节点读取 / 判断 / 返回值构造 / 返回 | 无 | 权威节点读回形成的只读结果 | false 一般为空候选；索引悬空语境须追根因 | 不把索引命中直接当节点存在 |
| 同上 | 186 | 函数词法作用域闭合；所有许可、共享锁与返回值已按正常或异常路径完成生命周期 | R10 / RN / RV / E11 / E13 / EX | 本函数内具体指令 | 作用域 / 生命周期 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：F0544 的 172—186 行全部映射；E1208、RCE0253、RCE2214—RCE2219、X01103—X01105、X02526—X02529 均绑定到源码调用点；接域许可、未接域共享锁、候选复制、锁外节点读回、全部空返回和异常资源释放路径均闭合。
