# CF-L5-0079 `索引仓库::有效主键数量` 逐行代码映射表

| 行 | 节点 | 类型 | 行为与绑定 |
| --- | --- | --- | --- |
| 402 | S | 函数合同 | 无参数；返回按值 `uint64_t` |
| 403 | C01-N02 | 项目调用及指令 | F0336 结果裁决接域委托或本地扫描 |
| 404 | U03 | 未解析调用 | U0006 输入运行期状态，按值返回事务许可 |
| 405 | C04-N09/C06-C07 | 项目调用及指令 | F0338 决定条件运算符分支；有效时 F0339 的令牌引用同步传给 F0376，否则形成 0 |
| 404-406 | C10-R11 | 项目调用及指令 | 返回值先形成，随后 F0345 析构许可，再返回 |
| 407 | X12 | 外部边界调用 | 默认构造空 `vector<节点句柄>` |
| 409 | X13 | 外部边界调用 | `shared_lock` 锁定 `仓库锁_` |
| 410 | X14-X15 | 外部边界调用 | 读取索引大小并预留候选容量 |
| 411 | N16-N17/X18 | 指令及外部调用 | 按索引迭代，复制每条绑定记录的节点句柄 |
| 412 | X19 | 外部边界调用 | 局部锁析构并释放共享锁 |
| 413 | N20 | 本函数内具体指令 | 数量置零 |
| 414 | N21-C22-N24 | 指令及项目调用 | 逐候选调用 F0377；仅有效节点递增计数 |
| 415-416 | N25-X26-R27 | 指令及外部调用 | 标量返回值先形成，候选 vector 析构，然后返回 |

覆盖结论：402—416 行完整映射；新发现 F0376、F0377 入队；项目调用边 6 条、外部边界 1 组、未解析调用 U0006 一条。
