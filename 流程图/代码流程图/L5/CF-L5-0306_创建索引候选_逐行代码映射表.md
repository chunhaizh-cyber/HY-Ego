# CF-L5-0306 `节点直接身份结构写入会话::创建索引候选` 逐行代码映射表

代码版本：`4b80f1617dd70ec7a19e1a34efa9da768dce8927`
源码 blob：`ca407389bb111031643ec2d9fa41157166775db6`
覆盖文件：`海中鱼巣/核心/会话.节点直接身份结构写入.ixx:189-213`
根函数：R1248 `节点直接身份带值结构写入结果<可重建索引记录> 海中鱼巣::节点直接身份结构写入会话::创建索引候选(可重建索引记录 记录)`
配对流程图：`流程图/代码流程图/L5/CF-L5-0306_创建索引候选_现状流程图.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/会话.节点直接身份结构写入.ixx` | 189-190 | 定义并进入 R1248，按值接收可重建索引记录 | N01 | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 191 | 默认构造输出，初始操作为入口拒绝且值为空 | N01 | 本函数内具体指令 | 局部结果初始化 | 无 | 当前代码事实 | 默认入口拒绝 | 无 |
| 同上 | 192 | 经 RCE4024 调用 R1261；不可继续写入时返回默认输出 | C02 / B03 / R04 | 函数调用 / 本函数内具体指令 | 准入 / 返回 | 无 | 当前会话状态 | 入口拒绝 | 无 |
| 同上 | 193 | 经 X05568 移动输入记录，并经 RCE4025 调用 R1163 请求结构化创建索引候选 | X05 / C06 | 外部边界 / 函数调用 | 所有权移动 / 仓库候选创建 | 可能在索引仓形成候选 | R1163 当前结果 | 由索引仓状态分类 | 无 |
| 同上 | 194 | 经 RCE4026 调用 R1275，把索引仓状态转换为会话操作结果 | C07 | 函数调用 | 结果转换 | 仅写局部输出 | 当前转换合同 | 由转换结果分类 | 无 |
| 同上 | 195 | 若转换状态为幂等读回，则保存到会话幂等结果槽 | B08 / N09 | 本函数内具体指令 | 状态判断 / 会话记账 | 可能更新 `幂等结果_` | 当前转换结果 | 非幂等时不写该槽 | 无 |
| 同上 | 196 | 经 X06215 把索引仓当前记录 optional 复制赋值到输出值 | X30 | 外部边界 | optional copy assignment / 结果装配 | 仅写局部输出 | 当前索引仓结果 | 可能为空 | 无 |
| 同上 | 197 | 经原地纠正后的 RCE4005 调用 R1159 判断索引写入结果是否成功并分支 | C11 / B12 | 函数调用 / 本函数内具体指令 | 成功判定 / 分支 | 无 | `写入结果` 静态类型 | 非成功进入返回分支 | RCE4005 不另建重复边 |
| 同上 | 198 | 非成功时仅在状态为内部不一致时经 RCE4027 调用 R1267 记录失败 | B13 / C14 | 本函数内具体指令 / 函数调用 | 非成功分类 / 失败记账 | 可能更新会话失败结果 | 当前索引仓状态 | 其它非成功不记录失败 | 无 |
| 同上 | 199 | 返回索引仓非成功对应输出 | R15 | 本函数内具体指令 | 返回 | 无新增变化 | 当前局部输出 | 保留转换结果 | 无 |
| 同上 | 200 | 闭合索引仓非成功分支 | R15 | 本函数内具体指令 | 分支闭合 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 201 | 经 X05569 检查候选 optional；无候选时进入直接返回 | X16 / B17 | 外部边界 / 本函数内具体指令 | 候选存在性判断 | 无 | 当前索引仓结果 | 无候选可为幂等读回 | 无 |
| 同上 | 202 | 无候选时返回输出；有候选时经 X06216 解引用 optional，再经 X05568 移动构造局部候选 | R18 / X31 / X19 | 本函数内具体指令 / 外部边界 | 返回 / optional 解引用 / 所有权移动 | 候选值移入局部变量 | 当前索引仓结果 | 不适用 | 无 |
| 同上 | 203 | 进入仅保护写集追加的 try 区 | T20 | 本函数内具体指令 | 异常边界 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 204 | 经 X05568 把局部候选转为右值，再经精确化后的 X05570 `push_back(索引写入项&&)` 追加 `{候选,false}` | X21 / X22 | 外部边界 | 移动 / 容器追加 | 成功时本会话持有待处理索引候选 | 当前索引写集 | 分配失败抛出 | 无 |
| 同上 | 205 | `push_back` 抛出时捕获任意异常并进入补偿路径 | E23 | 本函数内具体指令 | 异常捕获 | 无 | 当前异常边界 | 转为资源失败 | 无 |
| 同上 | 206 | 经 RCE4028 调用 R1169 撤销可重建索引候选；RCE4240、RCE4268、RCE4315 的其它仓库撤销端点与静态接收者不相容 | C24 | 函数调用 | 补偿 / 撤销 | 可能撤销索引仓候选 | R1169 当前结果 | 撤销结果不改变后续资源失败口径 | 删除三条伪直接出边及其反向调用方投影，保留 RCE4028 |
| 同上 | 207 | 显式忽略撤销返回值 | N25 | 本函数内具体指令 | 结果丢弃 | 无 | 当前代码事实 | 撤销失败未单独暴露 | 不宣称撤销必然成功 |
| 同上 | 208 | 把输出操作改写为资源失败并清零编号、版本 | N26 | 本函数内具体指令 | 结果改写 | 仅写局部输出 | 当前补偿结果 | 资源失败 | 无 |
| 同上 | 209 | 经 X05571 清空输出值 | X27 | 外部边界 | optional 清空 | 仅写局部输出 | 当前补偿结果 | 资源失败不携带值 | 无 |
| 同上 | 210 | 经 RCE4027 调用 R1267 记录资源失败 | C28 | 函数调用 | 失败记账 | 可能更新会话失败结果 | 当前补偿结果 | 资源失败 | 无 |
| 同上 | 211 | 闭合 catch 补偿路径 | C28 | 本函数内具体指令 | 分支闭合 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 212 | 返回输出；可能是已登记候选或资源失败 | R29 | 本函数内具体指令 | 返回 | 无新增变化 | 当前局部输出 | 按前序路径分类 | 不宣称候选已发布 |
| 同上 | 213 | 词法闭合函数，R04、R15、R18 或 R29 已为运行终点 | R04 / R15 / R18 / R29 | 本函数内具体指令 | 作用域结束 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：189—213 行全部映射；全部流程图节点可反查；外部边界 X05568—X05571、X06215、X06216 已覆盖。RCE4005 采用原地纠正后的 R1159；RCE4240、RCE4268、RCE4315 不是 206 行真实直接调用，已删除并清理三名被调函数的反向调用方投影。
