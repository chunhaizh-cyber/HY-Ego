# CF-L5-0044 `句柄有效(节点句柄)` 逐行代码映射表

| 行 | 节点 | 行为 |
| --- | --- | --- |
| 109-113 | 输入 | 冻结句柄三个身份字段 |
| 127 | 身份 | 只读借用节点句柄 |
| 128 | N01-N03、R0-R1 | 从仓库编号到版本号逐项短路判断 |
| 129 | 结束 | 无结构变化 |

旧句柄、伪造非零字段和跨仓库句柄仍可能返回true。项目调用、外部边界、未解析调用均为0。
