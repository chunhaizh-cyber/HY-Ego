# CF-L5-0110 `概念活动初始化参数::读取稳定键组` 逐行代码映射表

代码版本：`a48a70b2d678dea64dd8228adb4f26f0badd9506`
覆盖文件：`海中鱼巣/领域/概念活动状态.数据.h:41-43`
根函数：F0231 `std::array<std::uint64_t, 3> 海中鱼巣::概念活动初始化参数::读取稳定键组() const noexcept`
依据：`规范/0600_代码函数流程图应用与维护规范.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/概念活动状态.数据.h` | 41 | 定义 const、noexcept 的按值返回成员函数；隐式 `this` 为只读借用；首个可执行节点为 N01 | N01 | 本函数内具体指令 | 顺序操作 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 42 | 读取 `活跃状态稳定主键`，以读取值初始化返回数组元素 0 | N01-N02 | 本函数内具体指令 | 读取 / 顺序操作 | 只创建返回值元素，不改变机器结构 | 当前代码事实 | 不适用 | 无 |
| 同上 | 42 | 前一初始化子句完成后读取 `冷却状态稳定主键`，以读取值初始化返回数组元素 1 | N03-N04 | 本函数内具体指令 | 读取 / 顺序操作 | 只创建返回值元素，不改变机器结构 | 当前代码事实 | 不适用 | 无 |
| 同上 | 42 | 前一初始化子句完成后读取 `退役状态稳定主键`，以读取值初始化返回数组元素 2 | N05-N06 | 本函数内具体指令 | 读取 / 顺序操作 | 只创建返回值元素，不改变机器结构 | 当前代码事实 | 不适用 | 无 |
| 同上 | 42 | 三个聚合元素初始化完成后，按值返回完整 `std::array<std::uint64_t, 3>` | N07 | 本函数内具体指令 | 返回 | 返回值离开函数；机器结构不变 | 当前代码事实 | 不适用 | 无 |
| 同上 | 43 | 函数作用域结束；隐式 `this` 借用结束 | N07 | 本函数内具体指令 | 资源操作 | 无动态资源；机器结构不变 | 当前代码事实 | 不适用 | 无 |

覆盖结论：41—43 行全部非空有效代码均已映射；第 42 行的三个聚合初始化子句依次拆分为成员读取、元素初始化和最终返回节点。项目源码调用边零，外部边界调用零，未解析调用零。
