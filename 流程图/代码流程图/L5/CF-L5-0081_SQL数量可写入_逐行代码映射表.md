# CF-L5-0081 `数量可写入` 逐行代码映射表

| 行 | 节点 | 类型 | 行为与绑定 |
| --- | --- | --- | --- |
| 276 | S | 函数合同 | 按值接收 `uint64_t`，返回按值布尔 |
| 277 | X01 | 外部边界调用 | 取得 `int64_t` 最大值 |
| 277 | N02 | 本函数内具体指令 | 把有符号最大值显式转换为 `uint64_t` 可写上界 |
| 277 | N03 | 本函数内具体指令 | 比较输入数量与上界 |
| 277 | RT/RF | 本函数内具体指令 | 直接返回比较结果 |
| 278 | 结束 | 函数边界 | 无状态读写、锁、分配或异常收口 |

覆盖结论：276—278 行完整映射；项目调用和未解析调用为零；标准库外部边界一项。
