# CF-L5-0066 `初始化四个根::<lambda@107>::operator()` 逐行代码映射表

| 行 | 节点 | 类型 | 行为与绑定 |
| --- | --- | --- | --- |
| 106-107 | 入边 | 函数调用 | 存在根确保成功后构造闭包并交给 F0178；仅无动态根登记时调用 |
| 107 | N01 | 本函数内具体指令 | 使用按值捕获的初始化服务裸 `this` 定位非拥有依赖 `动态_` |
| 107 | N02 | 函数调用 | F0366，`this=&动态_`，无参数，返回动态根节点 |
| 107 | N03 | 本函数内具体指令 | 按值返回节点句柄给 F0178 局部 `根节点` |
| 145-161 | 调用合同 | 本函数内具体指令 | F0178 按值接收闭包并在无登记分支同步调用 |

覆盖结论：lambda 定义、捕获和调用语境完整映射；新发现 F0366 入队；项目调用边 1 条，外部边界 0，未解析调用 0。
