# CF-L5-0082 `逻辑内失败` 逐行代码映射表

| 行 | 节点 | 类型 | 行为与绑定 |
| --- | --- | --- | --- |
| 113 | S | 函数合同 | 按值取得阶段与诊断字符串 |
| 114 | N01-N04 | 本函数内具体指令 | 按 DTO 字段声明顺序写入 `false,true,false,0` |
| 114 | X05-X06 | 外部边界调用 | 分别从两个形参移动构造返回 DTO 字符串字段 |
| 114 | N07 | 本函数内具体指令 | 聚合 prvalue 直接形成返回对象 |
| 115 | 结束 | 外部生命周期边界 | 已移动形参析构；不固定两个形参的相对构造/析构顺序 |

覆盖结论：113—115 行完整映射；项目调用和未解析调用均为零。
