# CF-L5-0006 `入口端口存在根支持故障注入 lambda` 逐行代码映射表

| 行 | 节点 | 行为 | 失败收口 |
| --- | --- | --- | --- |
| 235 | 身份 | 冻结 `[&]` 闭包、按值句柄和 optional 返回 | 引用捕获不得逃逸 |
| 236 | N01,N02 | 取得 `调用[5]` 并先自增 | 是端口进入次数 |
| 237 | N03 | 判断失败点是否为5 | 无 |
| 238 | N04,N06 | 注入空 optional 并返回 | 不证明真实错误路径零写入 |
| 239-240 | N05,N06 | 调用 F0050 并原样返回 | 异常向外传播 |

注入空值与真实服务拒绝/找不到的空值无法从类型区分；真实分支可能写概念图，事务和回滚属于 F0050。项目调用 1，外部边界 X0109，未解析调用 0。
