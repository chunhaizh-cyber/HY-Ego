# CF-L5-0111 `结构事务协调器::结构事务协调器` 逐行代码映射表

代码版本：`a48a70b2d678dea64dd8228adb4f26f0badd9506`
覆盖文件：`海中鱼巣/核心/协调.结构事务.ixx:137-145`
相关成员声明顺序证据：`海中鱼巣/核心/协调.结构事务.ixx:164`
根函数：F0232 `海中鱼巣::结构事务协调器::结构事务协调器(std::uint64_t 域编号)`
对应流程图：`流程图/代码流程图/L5/CF-L5-0111_结构事务协调器构造_现状流程图.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/协调.结构事务.ixx` | 164（初始化顺序证据） | `状态_` 是唯一成员；构造函数没有显式初始化列表，因此进入第137行函数体前调用外部 `shared_ptr` 默认构造并形成空成员 | X01 | 函数调用 | 资源初始化 | 形成空资源成员，不建立运行期状态 | 冻结提交代码事实 | 不适用 | 本表现状映射不直接下达纠偏 |
| 同上 | 137 | 定义显式构造函数并按值接收域编号；没有显式成员初始化列表 | 函数合同/X01 | 本函数内具体指令 / 函数调用 | 函数边界 / 隐式成员初始化 | 先形成空 `状态_` | 冻结提交代码事实 | 不适用 | 本表现状映射不直接下达纠偏 |
| 同上 | 138 | 判断域编号是否为零 | N02 | 本函数内具体指令 | 判断 | 无 | 冻结提交代码事实 | 零值口径待后续规范核对 | 本表现状映射不直接下达纠偏 |
| 同上 | 139 | 零值时从构造函数体返回；对象仍成功构造，`状态_` 保持为空 | N03 | 本函数内具体指令 | 返回 / 构造完成 | 不建立运行期状态 | 冻结提交代码事实 | 待后续规范核对；当前代码事实是正常完成构造 | 本表现状映射不直接下达纠偏 |
| 同上 | 140 | 结束零值判断分支；非零值继续执行锁获取 | N02/X04 | 本函数内具体指令 / 函数调用 | 分支汇合 | 无 | 冻结提交代码事实 | 不适用 | 本表现状映射不直接下达纠偏 |
| 同上 | 141 | 以全局纪元锁构造 `std::lock_guard` 并获取锁；获取异常时局部锁对象未构造，已构造成员 `状态_` 随构造失败展开销毁 | X04/X16/N17 | 函数调用 / 本函数内具体指令 | 资源获取 / 异常传播 | 成功时占用同步资源；异常时不发布对象 | 冻结提交代码事实 | 异常边界待后续规范核对 | 本表现状映射不直接下达纠偏 |
| 同上 | 142 | `std::make_shared` 分配并隐式默认构造运行期状态；成功后把临时共享指针移动赋值给 `状态_`，再析构已移空临时指针；分配或状态构造异常时释放纪元锁并展开成员清理 | X05-X07/X14E/X16/N17 | 函数调用 / 本函数内具体指令 | 分配 / 资源所有权 / 异常传播 | 成功时新建并持有运行期状态；异常时不留下对象 | 冻结提交代码事实 | 外部边界异常传播 | 按 0600 核规 |
| 同上 | 143 | 调用共享指针 `operator->`，把参数域编号写入新状态的域编号字段 | X08/N09 | 函数调用 / 本函数内具体指令 | 解引用 / 写入 | 写新运行期状态的域编号 | 冻结提交代码事实 | 不适用 | 本表现状映射不直接下达纠偏 |
| 同上 | 144 | 在纪元锁保护下读取全局下个纪元旧值、递增全局计数，调用共享指针 `operator->`，再把旧值写入新状态纪元 | N10-N13 | 函数调用 / 本函数内具体指令 | 读取 / 写入 / 资源访问 | 写全局纪元计数和新状态纪元 | 冻结提交代码事实 | 不适用 | 本表现状映射不直接下达纠偏 |
| 同上 | 145 | 构造函数体结束；先析构局部 `lock_guard` 并释放纪元锁，再以非空 `状态_` 完成协调器构造 | X14N/N15 | 函数调用 / 本函数内具体指令 | 资源释放 / 构造完成 | 释放同步资源；协调器保留状态共享所有权 | 冻结提交代码事实 | 不适用 | 按 0600 核规 |

## 隐式状态构造证据

`std::make_shared<结构事务运行期状态>()` 使用该结构的编译器隐式默认构造。根据同一冻结文件第 27-35 行的成员声明和默认成员初始化器，成功后的初始状态为：

| 成员 | 初始代码事实 |
| --- | --- |
| `域编号` | `0`，随后在第143行被参数覆盖 |
| `纪元` | `0`，随后在第144行被本次领取的旧纪元覆盖 |
| `下个许可序号` | `1` |
| `结构锁` / `活动锁` | 各自默认构造 |
| `活动表` | 默认构造为空容器 |
| `已隔离` | `false` |

## 覆盖结论

- 根函数第 `137-145` 行全部映射；第164行只作为进入函数体前隐式成员初始化顺序的直接证据。
- 项目源码调用：0。
- 外部边界：7个函数 / 生命周期身份，包括共享指针默认构造、锁守卫构造、`make_shared`、共享指针移动赋值、共享指针析构、共享指针 `operator->`、锁守卫析构。
- 未解析调用：0。
- 异常边界覆盖锁获取失败以及状态分配 / 隐式构造失败；两条路径均不发布未完成协调器对象。
