# CF-L5-0012 `自我线程::~自我线程` 现状流程图

图类型：现状流程图
代码版本：`a48a70b2d678dea64dd8228adb4f26f0badd9506`
覆盖文件：`海中鱼巣/线程/自我线程.ixx:124-133`，隐式成员析构依据 `446-467`
唯一根函数：F0131
完整签名：`海中鱼巣::自我线程::~自我线程() noexcept`
参数：无显式参数；隐式 `this` 为调用方拥有且仍存活的 `自我线程*`
返回结果：析构函数无返回类型；当前根可达对象写停止请求、唤醒状态条件、join 可连接工作线程并逆序析构成员
可达性修正：当前根图谱只存在 F0155 三依赖构造及其 E0241 入边，该构造不写 `治理邮箱_`，成员保持默认 `nullptr`；五依赖构造只被未导入的 `自检.自我治理多轮.ixx` 使用，因此源码 126—128 的非空邮箱分支不属于当前实际可达路径

```mermaid
flowchart TD
    N01["N01【函数调用】X0113 停止请求.store(true)"]
    N02{"N02【本函数内具体指令】治理邮箱指针是否非空；当前根可达构造恒为否"}
    N04["N04【函数调用】X0113 状态条件.notify_all()"]
    N05["N05【函数调用】X0113 线程.joinable()"]
    N06{"N06【本函数内具体指令】线程是否可join"}
    N07["N07【函数调用】X0113 线程.join()"]
    N08["N08【本函数内具体指令】离开显式析构体"]
    N09["N09【函数调用】X0113 逆序析构thread、optional、条件变量、互斥量、原子和处理器"]
    N10["N10【本函数内具体指令】对象生命周期终止"]
    N01 --> N02
    N02 -- "否；当前唯一可达" --> N04
    N04 --> N05 --> N06
    N06 -- "是" --> N07 --> N08
    N06 -- "否" --> N08
    N08 --> N09 --> N10
```

项目调用 0。源码中的 `治理邮箱_->请求停止并唤醒()` 只有五依赖构造令 `治理邮箱_` 非空；该构造及其自检模块当前均不在 `main` 根闭包，因此原 E0612 和 F0320 从当前图谱排除。thread、atomic、condition_variable 与标准成员生命周期登记为 X0113。析构返回前会 join 可连接线程，但正确性仍依赖当前工作线程响应原子停止请求和状态条件唤醒。未解析调用 0。
