# CF-L5-0200 `索引仓库::有效主键数量` 令牌重载逐行代码映射表

代码版本：`4fc271f1d6adafe67b43e262527d2385ea8bcfaa`
覆盖文件：`海中鱼巣/核心/索引仓库.cpp:418-429`
根函数：F0376 `std::uint64_t 海中鱼巣::索引仓库::有效主键数量(const 海中鱼巣::结构事务令牌& 令牌) const`
配对流程图：`流程图/代码流程图/L5/CF-L5-0200_索引仓库有效主键数量令牌重载_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`、`规范/4040_子规范_不透明结构事务候选确认撤销与最后发布.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/索引仓库.cpp` | 418 | 进入 const 成员函数 F0376，借用有效 `this` 和调用方共享许可中的 const 结构事务令牌 | S | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 调用方必须保持许可覆盖完整调用 |
| 同上 | 419 | 经 RCE0162 调用 R0120 验证 `事务接线_` 与令牌；结果为 false 时在任何容器和锁动作前返回 `uint64_t{0}` | C01 / D02 / R03 | 函数调用 / 本函数内具体指令 | 令牌验证 / 判断 / 返回 | 无 | 事务准入结果 | 一般无效令牌为逻辑内返回；E0805 已成立许可语境下拒绝须追根因 | 裸 0 无法区分入口拒绝与合法零数量 |
| 同上 | 420 | 默认构造空 `std::vector<节点句柄>`，局部向量拥有后续节点句柄副本 | X04 | 外部边界 | 容器构造 / 生命周期 | 仅形成进程内临时投影 | 临时局部变量 | 构造异常向外传播 | 不为标准库函数生成项目流程图 |
| 同上 | 421 | 打开索引共享锁的内层词法作用域；不产生独立调用 | X05 前置 | 本函数内具体指令 | 作用域 | 无 | 当前代码事实 | 不适用 | 与 425 行共同冻结锁生命周期 |
| 同上 | 422 | 经正式外部边界 X00998 构造 `shared_lock` 并锁定 `仓库锁_` | X05 | 外部边界 | 资源取得 / 并发 | 取得索引仓库共享读锁，不改变机器事实 | 运行期资源状态 | 锁构造异常向外传播 | 保持 RAII，不手工跨作用域持锁 |
| 同上 | 423 | 调用 `主键索引_.size()` 取得条目数，并调用 `候选组.reserve(...)` 预留等量容量；向量逻辑大小仍为 0 | X06 / X07 | 外部边界 | 容器读取 / 容量操作 | 只改变局部向量容量 | 临时局部变量 | 分配异常触发锁和向量 RAII | `size()` 不是有效主键数量 |
| 同上 | 424 | 范围遍历 `主键索引_`；对每个条目复制 `second.节点`，再以 `push_back` 追加到候选向量 | L08 / N09 / X10 | 本函数内具体指令 / 外部边界 | 循环 / 索引读取 / 值复制 / 容器追加 | 只形成局部节点句柄候选组，不修改索引 | 非权威候选投影 | 追加异常触发锁和向量 RAII | `unordered_map` 遍历顺序不得冒充稳定排序 |
| 同上 | 425 | 内层作用域闭合，`shared_lock` 析构并释放索引共享锁；候选向量继续存活 | X11 | 外部边界 | RAII / 资源释放 | 释放运行期共享锁；机器结构不变化 | 运行期资源状态 | 不适用 | 证明后续节点读取发生在索引锁外 |
| 同上 | 426 | 初始化局部 `std::uint64_t 数量` 为 0 | N12 | 本函数内具体指令 | 纯计算 / 局部状态 | 无项目结构变化 | 临时局部变量 | 不适用 | 不把初值 0 解释为入口拒绝 |
| 同上 | 427 | 范围遍历候选向量；对每个候选经 RCE1736 调用 F0630 两参数重载，透传同一令牌；仅当节点有效时把数量加 1 | L13 / C14 / D15 / N16 | 本函数内具体指令 / 函数调用 | 循环 / 节点读取 / 判断 / 计数 | 只改变局部计数；不修改节点或索引 | 权威节点读取形成的派生计数 | 节点无效时合法不计数；已保证有效却失效须追根因 | 本批已登记 F0376→F0630 并闭合正反引用 |
| 同上 | 428 | 复制数量形成待返回的 `uint64_t` 值；随后局部候选向量按返回语义析构 | N17 / X18 / R19 | 本函数内具体指令 / 外部边界 | 返回值形成 / RAII / 返回 | 释放局部向量存储，不改变仓库 | 派生统计材料 | 不适用 | 返回数量不能替代索引一致性审计 |
| 同上 | 429 | 函数作用域闭合；令牌和仓库均未被本函数拥有或释放 | R19 | 本函数内具体指令 | 作用域 / 生命周期 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：F0376 的 418—429 行全部映射；实际直接入边为 E0805，本批已删除五条重复误绑边并将 RCE0249 改绑到无参数 F0199；源码项目调用点 R0120 和 F0630 分别由 RCE0162、RCE1736 登记；X00998 及容器 `size/reserve/push_back`、范围迭代和 RAII 边界均已记录。
