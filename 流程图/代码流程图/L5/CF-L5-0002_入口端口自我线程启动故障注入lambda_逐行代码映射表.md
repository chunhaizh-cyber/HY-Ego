# CF-L5-0002 `入口端口自我线程启动故障注入 lambda` 逐行代码映射表

| 行 | 节点 | 行为 | 失败收口 |
| --- | --- | --- | --- |
| 211 | 身份 | 冻结 `[&]` 闭包、const 引用参数和按值结果 | 引用捕获不得逃逸 |
| 212 | N01,N02 | 取得 `调用[1]` 并先自增 | 计数是端口进入次数 |
| 213 | N03 | 判断失败点是否为1 | 无 |
| 214 | N04,N06 | 注入默认失败结果并返回 | 失败但拒绝原因为“无” |
| 215-216 | N05,N06 | 调用 F0044 并原样返回 | 异常向外传播 |

非注入分支可能创建线程，本 lambda 不负责停止或 join；清理由隔离上下文生命周期承担。项目调用 1，外部边界 X0105，未解析调用 0。
