# CF-L5-0086 `ODBC连接::句柄` 逐行代码映射表

| 行 | 节点 | 类型 | 行为与绑定 |
| --- | --- | --- | --- |
| 196 | S | 函数合同 | const 借用 `this`，返回无所有权裸句柄 |
| 197 | N01 | 本函数内具体指令 | 对成员 `连接_` 做 lvalue-to-rvalue 读取并复制 |
| 197 | R1 | 本函数内具体指令 | 按值返回句柄副本 |
| 198 | 结束 | 函数边界 | 不改变包装对象或底层连接 |

覆盖结论：196—198 行完整映射；调用和未解析项均为零。
