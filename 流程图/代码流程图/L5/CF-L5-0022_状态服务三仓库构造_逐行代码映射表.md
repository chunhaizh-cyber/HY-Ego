# CF-L5-0022 `状态服务::状态服务` 三仓库构造逐行代码映射表

| 行 | 节点 | 行为 | 失败收口 |
| --- | --- | --- | --- |
| 66 | 身份 | 冻结三个非拥有可变引用参数 | 引用不可为空但可悬空 |
| 67、332-334 | N01-N03 | 按成员声明顺序绑定三个仓库引用 | 不复制、不延寿 |
| 67、335 | N04 | 隐式默认构造域号/纪元为零、共享状态和函数入口为空的结构事务接线 | 事务域能力未接入 |
| 68 | N05 | 空函数体完成 | 不读取或写仓库 |

错误交叉装配仍可成功；误选此三参数重载会静默失去事务域候选能力。项目调用 0，外部边界 X0120，未解析调用 0。
