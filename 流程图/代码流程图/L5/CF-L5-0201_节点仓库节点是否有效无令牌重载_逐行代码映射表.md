# CF-L5-0201 `节点仓库::节点是否有效(节点句柄)` 逐行代码映射表

代码版本：`3686cc2882ba42216609174c8e45e59b684e2958`；源码冻结提交 `2b5e1ea547986ece688988452c6b92e50a6cce23`，两提交间 `海中鱼巣/**` 源码零差异
覆盖文件：`海中鱼巣/核心/节点仓库.cpp:441-447`
根函数：F0377 `bool 海中鱼巣::节点仓库::节点是否有效(海中鱼巣::节点句柄 节点) const`
配对流程图：`流程图/代码流程图/L5/CF-L5-0201_节点仓库节点是否有效无令牌重载_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`、`规范/4040_子规范_不透明结构事务候选确认撤销与最后发布.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/节点仓库.cpp` | 441 | 进入 const 成员函数 F0377；非拥有借用 `this`，并按值接收 `节点句柄 节点` | S | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 442 | 经 RCE1737 调用 F0336 `事务接线_.已接域()`；true 进入共享许可路径，false 转入无令牌读取路径 | C01 / B02 | 函数调用 / 本函数内具体指令 | 接线读取 / 判断 | 无；只读事务接线形态 | 当前事务接线读取结果 | 完全未接域为正常分支；正式上游保证完整时的部分接线为内部不一致 | 需要区分时由结构化接线形态结果承载，不以裸 bool 猜测 |
| 同上 | 443 | 仅已接域时经 RCE1738 调用当前生产接线唯一目标 F0397，以 `事务接线_.运行期状态` 取得局部共享许可；许可成功形成后建立覆盖返回表达式的 RAII 生命周期；若构造未完成即异常则经 E14 直接传播且没有许可可析构 | C03 / E14 | 函数调用 / 本函数内具体指令 | 许可取得 / 局部构造 / 事务 / 异常传播 | 无机器事实写入；成功时建立局部许可资源 | 当前事务协调器运行期材料 | 无效许可由下一行折叠为 false；构造前异常直接传播 | 保持函数指针目标、接线版本与许可身份可审计 |
| 同上 | 444 | 先经 RCE1739 调用 F0338 判断许可有效；false 时按 `&&` 短路形成待返回 false；true 时经 RCE1740 调用 F0339 取得令牌引用，再经 RCE0163 调用二实参重载 F0630，物化其节点有效 bool。正常返回表达式完成后经 RCE1741 调用 F0345 析构许可；F0630 异常也先析构再传播。F0338 与 F0339 的当前定义路径无可抛操作 | C04 / B05 / N06 / C07 / C08 / N09 / D10 / R11 / D12 / E13 | 函数调用 / 本函数内具体指令 | 短路判断 / 令牌读取 / 节点读取 / 返回 / 资源释放 / 异常展开 | 无；只读许可、令牌和节点当前记录 | 当前许可与节点仓读取结果 | 许可无效或节点无效均逻辑内返回 false；正式上游已保证许可有效却再失败时需按调用语境追根因 | 若调用方需要区分许可拒绝与节点无效，应使用结构化读取结果 |
| 同上 | 445 | 闭合接域分支词法作用域；接域路径已由上一行的 return 在退出前完成许可析构，运行时不继续进入 line 446 | D10 / D12 | 函数调用 | 作用域 / RAII 生命周期 | 无 | 当前 C++ 作用域事实 | 不适用 | 保持许可仅在接域分支局部生存 |
| 同上 | 446 | 仅 F0336 返回 false 时，经 RCE0164 调用 F0190 `读取节点(节点)` 得到 optional；随后经外部边界 X02297 调用 `has_value()`，把有无记录折成 bool 并返回。F0190 异常直接传播 | C15 / X16 / R17 / E18 | 函数调用 / 外部边界 / 本函数内具体指令 | 节点读取 / optional 查询 / 返回 / 异常传播 | 无；F0190 内部只读节点仓当前记录 | 当前节点仓读取结果 | 空 optional 为逻辑内 false；部分接线落入该路径且被上游保证不应发生时为内部不一致 | 若需保留未找到、失效、错仓或接线异常原因，应返回结构化读取结果 |
| 同上 | 447 | 词法闭合函数定义；两个 return 节点 R11 / R17 均为各自路径终点，不存在返回后继续执行的运行节点；函数不保存 `this`、节点句柄、令牌引用或许可 | R11 / R17 | 本函数内具体指令 | 作用域 / 生命周期 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：441—447 行全部映射；项目调用边 RCE1737、RCE1738、RCE1739、RCE1740、RCE0163、RCE1741、RCE0164 全部落入具名节点；外部边界 X02297 已映射；接域 / 未接域、`&&` 短路、许可正常析构与异常展开均闭合；未解析项目调用 0。
