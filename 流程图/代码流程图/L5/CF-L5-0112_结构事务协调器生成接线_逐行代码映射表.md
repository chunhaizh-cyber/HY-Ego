# CF-L5-0112 `结构事务协调器::生成接线` 逐行代码映射表

代码版本：`a48a70b2d678dea64dd8228adb4f26f0badd9506`
覆盖文件：`海中鱼巣/核心/协调.结构事务.ixx:147-157`
唯一根函数：F0233
完整签名：`海中鱼巣::结构事务接线 海中鱼巣::结构事务协调器::生成接线()`
配对流程图：`流程图/代码流程图/L5/CF-L5-0112_结构事务协调器生成接线_现状流程图.md`
规范依据：`规范/0600_代码函数流程图应用与维护规范.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/协调.结构事务.ixx` | 147 | 定义无显式参数、返回值式结构事务接线的成员函数 | S | 本函数内具体指令 | 顺序操作 | 无 | 权威代码事实 | 不适用 | 无；偏差由覆盖完成后的规范审计裁决 |
| 同上 | 148 | 只读判断协调器的 `状态_` 是否为空 | N01 | 本函数内具体指令 | 判断 / 读取 | 无 | 权威代码事实 | 空状态进入允许返回分支 | 无 |
| 同上 | 149 | 以空初始化列表构造接线：两个整数为 0，shared_ptr 默认构造为空，五个函数指针为 nullptr；按值返回 | N02-N10 / X05 / R0 | 本函数内具体指令 / 函数调用 | 顺序操作 / 调用 / 返回 | 只构造返回值，不改变运行期状态 | 权威代码事实 | 逻辑内返回 | 无 |
| 同上 | 150 | 结束空状态分支；R0 已完成函数返回 | R0 | 本函数内具体指令 | 返回 | 无 | 权威代码事实 | 逻辑内返回 | 无 |
| 同上 | 151 | 开始按初始化列表构造非空接线；依次读取域编号、纪元，并转换复制 `状态_` 到 `shared_ptr<void>` 返回成员 | N11-N13 / X14 | 本函数内具体指令 / 函数调用 | 读取 / 调用 / 顺序操作 | 增加同一运行期状态控制块的强引用；不改变状态内容 | 权威代码事实 | 不适用 | 无 |
| 同上 | 152 | 构造无捕获 lambda，调用运算符延迟调用 `取得许可(状态, 共享)`；编译器把闭包转换为函数指针并绑定 `取得共享许可` 字段 | N15 / X16 / N17 | 本函数内具体指令 / 函数调用 | 顺序操作 / 调用 | 只写返回接线函数指针字段；当前不取得许可 | 权威代码事实 | 不适用 | 无 |
| 同上 | 153 | 构造无捕获 lambda，调用运算符延迟调用 `取得许可(状态, 独占)`；转换并绑定 `取得独占许可` 字段 | N18 / X19 / N20 | 本函数内具体指令 / 函数调用 | 顺序操作 / 调用 | 只写返回接线函数指针字段；当前不取得许可 | 权威代码事实 | 不适用 | 无 |
| 同上 | 154 | 构造无捕获 lambda，调用运算符延迟调用 `验证令牌(状态, 令牌, false)`；转换并绑定 `验证共享路径令牌` 字段 | N21 / X22 / N23 | 本函数内具体指令 / 函数调用 | 顺序操作 / 调用 | 只写返回接线函数指针字段；当前不验证令牌 | 权威代码事实 | 不适用 | 无 |
| 同上 | 155 | 构造无捕获 lambda，调用运算符延迟调用 `验证令牌(状态, 令牌, true)`；转换并绑定 `验证独占令牌` 字段 | N24 / X25 / N26 | 本函数内具体指令 / 函数调用 | 顺序操作 / 调用 | 只写返回接线函数指针字段；当前不验证令牌 | 权威代码事实 | 不适用 | 无 |
| 同上 | 156 | 构造无捕获 lambda，调用运算符延迟调用 `标记撤销失败隔离(状态, 令牌)`；转换并绑定最终函数指针，完成聚合并按值返回 | N27 / X28 / N29-N30 / R1 | 本函数内具体指令 / 函数调用 | 顺序操作 / 调用 / 返回 | 只写返回接线函数指针字段；当前不标记隔离 | 权威代码事实 | 不适用 | 无 |
| 同上 | 157 | 结束函数定义；返回接线中的 shared_ptr 持续共享运行期状态生命周期 | R1 | 本函数内具体指令 | 返回 | 不写状态内容 | 权威代码事实 | 不适用 | 无 |

## 聚合成员初始化顺序

| 顺序 | 接线成员 | 来源 / 初始化方式 | 节点 | 空状态分支 |
| ---: | --- | --- | --- | --- |
| 1 | `域编号` | `状态_->域编号` | N12 | N03：默认成员初始化为 0 |
| 2 | `运行期纪元` | `状态_->纪元` | N13 | N04：默认成员初始化为 0 |
| 3 | `运行期状态` | `状态_` 转换复制为 `shared_ptr<void>` | X14 | X05：默认构造为空 |
| 4 | `取得共享许可` | 行152无捕获 lambda 转换函数指针 | N15-X16-N17 | N06：nullptr |
| 5 | `取得独占许可` | 行153无捕获 lambda 转换函数指针 | N18-X19-N20 | N07：nullptr |
| 6 | `验证共享路径令牌` | 行154无捕获 lambda 转换函数指针 | N21-X22-N23 | N08：nullptr |
| 7 | `验证独占令牌` | 行155无捕获 lambda 转换函数指针 | N24-X25-N26 | N09：nullptr |
| 8 | `标记撤销失败隔离` | 行156无捕获 lambda 转换函数指针 | N27-X28-N29 | N10：nullptr |

## 延迟调用边

| lambda 定义 | 延迟被调项目函数 | 参数绑定 | 返回绑定 | 静态解析 | 当前清单 |
| --- | --- | --- | --- | --- | --- |
| 行152 | `海中鱼巣::结构事务许可 海中鱼巣::结构事务内部::取得许可(const std::shared_ptr<void>&, 海中鱼巣::结构许可类型)` | `状态 -> 状态`；固定 `类型=共享` | lambda 返回 `结构事务许可` | 唯一 | lambda 与被调函数均无现有身份 |
| 行153 | 同一 `结构事务内部::取得许可` | `状态 -> 状态`；固定 `类型=独占` | lambda 返回 `结构事务许可` | 唯一 | lambda 与被调函数均无现有身份 |
| 行154 | `bool 海中鱼巣::结构事务内部::验证令牌(const std::shared_ptr<void>&, const 海中鱼巣::结构事务令牌&, bool)` | `状态`、`令牌` 原样传递；固定 `需要独占=false` | lambda 返回 bool | 唯一 | lambda 与被调函数均无现有身份 |
| 行155 | 同一 `结构事务内部::验证令牌` | `状态`、`令牌` 原样传递；固定 `需要独占=true` | lambda 返回 bool | 唯一 | lambda 与被调函数均无现有身份 |
| 行156 | `bool 海中鱼巣::结构事务内部::标记撤销失败隔离(const std::shared_ptr<void>&, const 海中鱼巣::结构事务令牌&)` | `状态`、`令牌` 原样传递 | lambda 返回 bool | 唯一 | lambda 与被调函数均无现有身份 |

## 覆盖结论

```text
逐行覆盖：147—157 行全部有效代码已映射
聚合成员覆盖：8 / 8
lambda 构造：5 / 5
lambda 捕获集：均为空
函数指针转换与字段绑定：5 / 5
本函数立即执行的项目调用边：0
延迟回调 lambda 实体：5，均可唯一解析且当前函数清单无身份
lambda 正文中的延迟项目调用边：5 个调用点，3 个唯一被调函数，均可唯一解析且当前函数清单无身份
外部边界：shared_ptr 默认构造 1 类、shared_ptr 转换复制 1 类、编译器 lambda 到函数指针转换 5 个调用点
未解析调用：0
```
