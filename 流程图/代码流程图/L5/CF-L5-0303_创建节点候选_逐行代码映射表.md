# CF-L5-0303 `节点直接身份结构写入会话::创建节点候选` 逐行代码映射表

代码版本：`4b80f1617dd70ec7a19e1a34efa9da768dce8927`
源码 blob：`ca407389bb111031643ec2d9fa41157166775db6`
覆盖文件：`海中鱼巣/核心/会话.节点直接身份结构写入.ixx:98-129`
根函数：R1244 `节点直接身份带值结构写入结果<节点句柄> 海中鱼巣::节点直接身份结构写入会话::创建节点候选(节点类型 类型, 节点稳定主键 稳定主键)`
配对流程图：`流程图/代码流程图/L5/CF-L5-0303_创建节点候选_现状流程图.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/会话.节点直接身份结构写入.ixx` | 98-100 | 定义并进入 R1244，按值绑定节点类型和稳定主键 | N01 | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 101 | 默认构造输出，初始操作为入口拒绝且值为空 | N01 | 本函数内具体指令 | 局部结果初始化 | 无仓库变化 | 当前代码事实 | 默认入口拒绝 | 无 |
| 同上 | 102 | 经 RCE4008 调用 R1261；false 时立即返回默认输出 | C02 / B03 / R04 | 函数调用 / 本函数内具体指令 | 准入 / 返回 | 无 | 当前会话状态 | 入口拒绝 | 无 |
| 同上 | 103 | 经 RCE4009 调用 R1429，以类型、稳定主键和事务序号请求结构化创建 | C05 | 函数调用 | 仓库候选创建 | 可能在节点仓形成未发布候选 | R1429 当前结果 | 由节点仓状态分类 | 无 |
| 同上 | 104 | 经 RCE4010 调用 R1273，把节点仓状态和当前身份转换为会话操作结果 | C06 | 函数调用 | 结果转换 | 仅写局部输出 | 当前转换合同 | 由转换结果分类 | 无 |
| 同上 | 105 | 若转换状态为幂等读回，则保存到会话幂等结果槽 | B07 / N08 | 本函数内具体指令 | 状态判断 / 会话记账 | 可能更新 `幂等结果_` | 当前转换结果 | 非幂等时不写该槽 | 无 |
| 同上 | 106 | 经原地纠正后的 RCE4004 调用 R1502 `节点直接身份创建结果::成功()` 并据其结果分支 | C09 / B10 | 函数调用 / 本函数内具体指令 | 成功判定 / 分支 | 无 | `创建` 的静态类型 | 非成功进入拒绝分支 | RCE4004 不另建重复边 |
| 同上 | 107-110 | 节点仓非成功时，判断状态是否不属于身份冲突、入口拒绝、事务拒绝、版本漂移 | B11 | 本函数内具体指令 | 非成功分类 | 无 | 节点仓状态 | 四类普通拒绝不记录失败 | 无 |
| 同上 | 111 | 非成功且不在四类排除集合时，经 RCE4011 调用 R1267 记录失败 | C12 | 函数调用 | 失败记账 | 可能更新会话失败结果 | 当前操作结果 | 普通拒绝不经过此节点 | 无 |
| 同上 | 112 | 闭合非成功分类分支 | B11 | 本函数内具体指令 | 分支闭合 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 113 | 返回节点仓非成功对应输出 | R13 | 本函数内具体指令 | 返回 | 无新增仓库变化 | 当前局部输出 | 保留转换后的非成功状态 | 无 |
| 同上 | 114 | 闭合节点仓非成功分支 | R13 | 本函数内具体指令 | 分支闭合 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 115 | 节点仓成功时经 X06218 把当前身份赋给输出 optional | X30 | 外部边界 | optional 赋值 / 结果装配 | 仅写局部 optional | 当前节点仓结果 | 不适用 | 无 |
| 同上 | 116 | 经 X05562 检查候选 optional 是否有值 | X15 / B16 | 外部边界 / 本函数内具体指令 | 候选存在性判断 | 无 | 当前节点仓结果 | 无候选时直接返回当前身份 | 无 |
| 同上 | 117 | 候选存在时经 X06212 解引用 optional，再经 X05563 移动构造局部候选 | X29 / X17 | 外部边界 | optional 解引用 / 所有权移动 | 候选值移入局部变量 | 当前节点仓结果 | 不适用 | 无 |
| 同上 | 118 | 进入仅保护写集追加的 try 区 | T18 | 本函数内具体指令 | 异常边界 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 119 | 经 X05563 把局部候选转为右值，再经精确化后的 X05564 `push_back(节点写入项&&)` 追加 `{候选,false}` | X19 / X20 | 外部边界 | 移动 / 容器追加 | 成功时本会话持有待处理节点候选 | 当前写集 | 分配失败抛出 | 无 |
| 同上 | 120 | `push_back` 抛出时捕获任意异常并进入补偿路径 | E21 | 本函数内具体指令 | 异常捕获 | 无 | 当前异常边界 | 转为资源失败 | 无 |
| 同上 | 121 | 经 RCE4012 调用 R1431 尝试撤销节点候选 | C22 | 函数调用 | 补偿 / 撤销 | 可能撤销仓库候选 | R1431 当前结果 | 撤销结果不改变后续资源失败口径 | 无 |
| 同上 | 122 | 显式忽略撤销返回值 | N23 | 本函数内具体指令 | 结果丢弃 | 无 | 当前代码事实 | 撤销失败未单独暴露 | 不宣称撤销必然成功 |
| 同上 | 123 | 把输出操作改写为资源失败并清零编号、版本 | N24 | 本函数内具体指令 | 结果改写 | 仅写局部输出 | 当前补偿结果 | 资源失败 | 无 |
| 同上 | 124 | 经 X05565 清空输出值 | X25 | 外部边界 | optional 清空 | 仅写局部输出 | 当前补偿结果 | 资源失败不携带值 | 无 |
| 同上 | 125 | 经 RCE4011 调用 R1267 记录资源失败 | C26 | 函数调用 | 失败记账 | 可能更新会话失败结果 | 当前补偿结果 | 资源失败 | 无 |
| 同上 | 126 | 闭合 catch 补偿路径 | C26 | 本函数内具体指令 | 分支闭合 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 127 | 闭合候选存在分支 | B16 | 本函数内具体指令 | 分支闭合 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 128 | 返回输出；可能是默认拒绝、节点仓结果、幂等当前身份、已登记候选或资源失败 | R27 | 本函数内具体指令 | 返回 | 无新增变化 | 当前局部输出 | 按前序路径分类 | 不宣称候选已发布 |
| 同上 | 129 | 词法闭合函数，R04、R13 或 R27 已为运行终点 | R04 / R13 / R27 | 本函数内具体指令 | 作用域结束 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：98—129 行全部映射；流程图全部节点可反查；项目调用 R1261、R1429、R1273、R1502、R1267、R1431，以及 X05562—X05565、X06212、X06218 已覆盖；RCE4004 采用原地纠正后的唯一端点 R1502。
