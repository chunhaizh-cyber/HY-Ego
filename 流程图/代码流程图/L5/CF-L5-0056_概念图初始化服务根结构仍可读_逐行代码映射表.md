# CF-L5-0056 `概念图初始化服务::根结构仍可读` 逐行代码映射表

| 行 | 节点 | 类型 | 行为与绑定 |
| --- | --- | --- | --- |
| 219 | 函数身份 | 本函数内具体指令 | F0176；借用 `结果`，返回 `bool` |
| 220-222 | R01 | 本函数内具体指令 | 按存在、动态、关系、因果顺序构造四个非拥有指针 |
| 223 | R02/R03/R17 | 本函数内具体指令 | 范围循环判断、绑定、递增 |
| 224-225 | R04-R08/FAIL | 函数调用及指令 | 空指针短路；依次调用 F0189、F0351，任一失败即返回 `false` |
| 227 | R09 | 函数调用 | F0352，参数 `根->根类别`，返回绑定 `登记` |
| 228-231 | R10-R12/FAIL | 外部调用及指令 | `has_value` 后比较稳定键、根节点；严格首错返回 |
| 233 | R13 | 函数调用 | F0353，参数 `根->根节点`，返回绑定 `生命周期` |
| 234-237 | R14-R16/FAIL | 外部调用及指令 | 检查有值、活跃阶段和 `结果.活跃状态` |
| 240-244 | S01 | 本函数内具体指令 | 按活跃、冷却、退役构造三组阶段/状态节点值 |
| 245 | S02/S03/S12 | 本函数内具体指令 | 状态范围循环判断、结构化绑定、递增 |
| 246 | S04 | 函数调用 | F0354，参数 `阶段`，返回绑定 `已登记` |
| 247 | S05 | 函数调用 | F0329，参数 `状态节点`，返回绑定 `状态值`；无论 S04 结果是否有值均执行 |
| 248-251 | S06-S09/FAIL | 外部调用及指令 | 严格短路核对登记有值/句柄匹配、状态值有值/数值匹配 |
| 252-253 | S10/S11/FAIL | 函数调用及指令 | 前四项通过后调用 F0328；若为实例状态则返回 `false` |
| 256 | F01-F03 | 函数调用及指令 | F0355 返回临时根组，外部 `size()` 后返回数量是否等于 4 |
| 257 | 结束 | 本函数内具体指令 | 临时对象析构；无统一事务快照 |

覆盖结论：219—257 行完整映射；新发现 F0351—F0355 入队；项目调用边 8 条，外部边界 1 组，未解析调用 0。
