# CF-L5-0089 `ODBC语句::句柄` 逐行代码映射表

| 行 | 节点 | 类型 | 行为与绑定 |
| --- | --- | --- | --- |
| 242 | S | 函数合同 | const 借用包装器，返回非拥有裸句柄 |
| 243 | N01 | 本函数内具体指令 | 读取并复制成员 `语句_` |
| 243 | R1 | 本函数内具体指令 | 按值返回句柄副本 |
| 244 | 结束 | 函数边界 | 不推进或释放底层语句 |

覆盖结论：242—244 行完整映射；调用和未解析项均为零。
