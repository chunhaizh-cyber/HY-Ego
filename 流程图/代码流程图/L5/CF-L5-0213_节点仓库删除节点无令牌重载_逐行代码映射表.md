# CF-L5-0213 `节点仓库::删除节点(节点句柄)` 逐行代码映射表

代码版本：`f7bc742f8126fcf66288fc940300de3331a0eebb`
源码 blob：`f38b979a3f7cefd182a50e0b687c5a6fffebc91c`
覆盖文件：`海中鱼巣/核心/节点仓库.cpp:331-348`
根函数：F0523 `bool 海中鱼巣::节点仓库::删除节点(海中鱼巣::节点句柄 节点)`
配对流程图：`流程图/代码流程图/L5/CF-L5-0213_节点仓库删除节点无令牌重载_现状流程图.md`
依据：当前源码、正式函数身份表、正式项目调用边表、正式外部边界表、`规范/0600_代码函数流程图应用与维护规范.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/节点仓库.cpp` | 331 | 进入 F0523；借用可变节点仓库 `this`，按值接收节点句柄并打开函数体 | S | 本函数内具体指令 | 入口 / 参数绑定 / 作用域 | 无 | 当前代码事实 | 不适用 | 不保存仓库或句柄引用 |
| 同上 | 332 | 经 RCE2209 调用 F0336 读取事务接线形态；true 进入接域分支，false 进入本地独占锁分支 | C01 / B02 | 函数调用 / 本函数内具体指令 | 接线读取 / 分支 | 无 | 当前接线事实 | 不适用 | 已登记稳定项目边 |
| 同上 | 333 | 仅已接域时，经 RCE2210 调用函数指针当前唯一目标 F0398，以运行期状态形成局部独占许可 | C03 | 函数调用 | 许可取得 / 局部对象构造 | 无节点写入 | 当前生产接线 | 许可取得异常直接传播 | 已登记唯一目标和稳定项目边 |
| 同上 | 334 | 经 RCE2211 调用 F0338；无效时短路形成 false，有效时经 RCE2212 调用 F0339 读取令牌，再经 RCE0252 调用 R0147；返回表达式求值后经 RCE2213 析构许可并返回 bool | C04 / B05 / N08 / C06 / C07 / D09 / R10 | 函数调用 / 本函数内具体指令 | 有效性判断 / 短路 / 令牌读取 / 删除 / RAII / 返回 | 由 R0147 决定；许可无效时零写入 | 当前许可与令牌删除结果 | 许可无效或 R0147 拒绝均返回 false | 保持短路顺序；许可每个动态分支析构一次 |
| 同上 | 335 | 闭合接域分支；接域运行路径已经在 R10 返回，异常路径在许可形成后先经 D09 清理 | R10 / D09 | 本函数内具体指令 / 函数调用 | 分支闭合 / 生命周期 | 无新增结构变化 | 当前控制流 | 不适用 | 无 |
| 同上 | 336 | 仅未接域时，经 X01099 构造 `unique_lock` 并独占 `仓库锁_`；局部锁生命周期延续至任一返回或异常展开时的 X02525 | X11 | 外部边界 | 锁取得 / 资源生命周期 | 建立独占访问窗口 | 当前仓库锁 | 锁取得异常直接传播 | 不把锁状态写成机器事实 |
| 同上 | 337 | 经 X02522 调用 `节点表_.find(节点.节点编号)`，按值保存迭代位置 | X12 | 外部边界 | 容器查找 / 临时绑定 | 无 | 当前节点表查找候选 | 不适用 | 已登记外部边界 |
| 同上 | 338 | 经 X02523 取得 `节点表_.end()`，经 X01100 比较迭代位置与末尾位置；相等进入未命中返回 | X13 / X14 / B15 | 外部边界 / 本函数内具体指令 | 容器末尾 / 迭代器比较 / 判断 | 无 | 当前节点表查找结果 | 未命中为普通 false | 禁止解引用 end |
| 同上 | 339 | 查表未命中时准备返回 false；返回前经 X02525 析构锁并释放 `仓库锁_` | U21F / R22 | 外部边界 / 本函数内具体指令 | 资源释放 / 返回 | 零节点写入 | 当前只读拒绝 | 普通未命中 | X02525 每条动态路径只执行一次 |
| 同上 | 340 | 闭合查表未命中分支；仅命中路径继续 | B15 | 本函数内具体指令 | 分支闭合 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 341 | 经 X02524 调用迭代器 `operator->()` 取得命中元素地址，并把其中节点记录绑定为可变引用 | X16 / N17 | 外部边界 / 本函数内具体指令 | 迭代器解引用 / 引用绑定 | 尚未写入 | 当前节点表命中记录 | 不适用 | 引用不越过独占锁生命周期 |
| 同上 | 342 | 依次复核句柄仓库编号、句柄版本号和记录状态；三项全部匹配才进入写入路径 | B18 | 本函数内具体指令 | 完整句柄复核 / 短路判断 | 无 | 当前句柄与记录 | 任一不匹配返回 false | 保持错仓、错版本、非有效三项复核 |
| 同上 | 343 | 复核失败时准备返回 false；返回前经 X02525 析构锁并释放 `仓库锁_` | U21F / R22 | 外部边界 / 本函数内具体指令 | 资源释放 / 返回 | 零节点写入 | 当前句柄拒绝 | 普通句柄拒绝 | 不把 false 伪写成节点唯一不存在 |
| 同上 | 344 | 闭合句柄复核失败分支；仅完整匹配路径继续 | B18 | 本函数内具体指令 | 分支闭合 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 345 | 把命中记录状态写为 `记录状态::已删除` | N19 | 本函数内具体指令 | 状态写入 / 逻辑删除 | 节点记录状态变为已删除 | 当前仓库写入 | 不适用 | 不宣称物理删除 |
| 同上 | 346 | 将同一记录版本号加一，使旧句柄版本失配 | N20 | 本函数内具体指令 | 版本推进 | 节点记录版本号增加一 | 当前仓库写入 | 不适用 | 保持状态写入后推进版本的当前顺序 |
| 同上 | 347 | 成功路径准备返回 true；返回前经 X02525 析构锁并释放 `仓库锁_` | U21S / R23 | 外部边界 / 本函数内具体指令 | 资源释放 / 返回 | 已删除状态与新版本保留 | 当前写入结果 | 成功返回 | 不升级为引用关系已清理 |
| 同上 | 348 | 闭合函数词法作用域；R10、R22 或 R23 已是正常终点，未接域异常展开也在离开作用域前经 X02525 释放已形成的锁 | R10 / R22 / R23 / U21F / U21S | 本函数内具体指令 / 外部边界 | 作用域 / 生命周期 / 异常清理 | 已完成写入保留；拒绝路径零写入 | 当前控制流 | 不适用 | 许可或锁在各自动路径只析构一次 |

覆盖结论：F0523 的 331—348 行全部映射；E1138、RCE0252、RCE2209—RCE2213、X01099、X01100、X02522—X02525 及全部流程图节点均闭合；0 个未解析项目调用。接域许可与未接域独占锁在每条动态路径中各自最多形成一次，已形成后退出时恰好析构一次。
