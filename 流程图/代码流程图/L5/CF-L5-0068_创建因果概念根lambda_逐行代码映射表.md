# CF-L5-0068 `初始化四个根::<lambda@111>::operator()` 逐行代码映射表

| 行 | 节点 | 类型 | 行为与绑定 |
| --- | --- | --- | --- |
| 110-111 | 入边 | 函数调用 | 前三根确保成功后构造闭包并交 F0180；仅无因果根登记时调用 |
| 111 | N01 | 本函数内具体指令 | 经裸 `this` 定位非拥有依赖 `因果_` |
| 111 | N02 | 函数调用 | F0368，无参数，返回因果概念根句柄 |
| 111 | N03 | 本函数内具体指令 | 原样返回给 F0180 局部 `根节点` |
| 262 | 外层绑定 | 本函数内具体指令 | 固定键 `0x434E'0001'0000'0004ULL` 由 F0180 消费 |

覆盖结论：lambda 定义、捕获和唯一调用语境完整映射；新发现 F0368 入队；项目调用边 1 条，外部边界 0，未解析调用 0。
