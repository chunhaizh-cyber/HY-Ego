# CF-L5-0092 `追根因失败` 逐行代码映射表

| 行 | 节点 | 类型 | 行为与绑定 |
| --- | --- | --- | --- |
| 117 | S | 函数合同 | 两个拥有字符串和一个按值审计编号 |
| 118 | N01-N04 | 本函数内具体指令 | 依 DTO 字段顺序形成 `false,false,true,审计编号` |
| 118 | X05-X06 | 外部边界调用 | 把阶段和诊断字符串所有权移动到返回 DTO |
| 118 | R07 | 本函数内具体指令 | 聚合 prvalue 直接形成返回对象 |
| 119 | 结束 | 外部生命周期边界 | 已移动形参析构 |

覆盖结论：117—119 行完整映射；项目调用和未解析调用均为零。
