# CF-L5-0001 `入口端口方法登记根故障注入 lambda` 逐行代码映射表

| 行 | 节点 | 行为 | 失败收口 |
| --- | --- | --- | --- |
| 205 | 身份 | 冻结 `[&]` 闭包、按值键和 optional 返回 | 引用捕获不得逃逸 |
| 206 | N01,N02 | 取得 `调用[0]` 并先自增 | callee 异常也不回滚 |
| 207 | N03 | 判断本轮失败点是否为0 | 无 |
| 208 | N04,N06 | 注入空 optional 并返回 | 不调用真实服务 |
| 209-210 | N05,N06 | 调用 F0043 并原样返回 optional | 异常向外传播 |

空 optional 同时可表示注入失败和真实服务失败，原因粒度依赖外部失败点；宽泛引用捕获与非原子计数只适合同步单线程消费。项目调用 1，外部边界 X0104，未解析调用 0。
