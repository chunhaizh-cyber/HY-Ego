# CF-L5-0105 `运行期上下文租约::运行期上下文租约` 逐行代码映射表

| 行 | 节点 | 类型 | 行为与绑定 |
| --- | --- | --- | --- |
| 192-200 | S | 函数合同 | private explicit 构造，只允许宿主 friend 调用 |
| 201 | X01-X02 | 外部边界调用 | 值类别转换后移动 shared_ptr 控制块到唯一成员 |
| 202 | N03-X04/R | 指令及外部调用 | 空正文结束，已移空参数析构，构造完成 |
| 203 | 成员合同 | 本函数内具体指令 | 成员持有 `shared_ptr<const 运行期上下文>` |

覆盖结论：200—203 行完整映射；项目调用和未解析调用均为零。
