# CF-L5-0107 `系统角色初始化参数::有效` 逐行代码映射表

代码版本：`a48a70b2d678dea64dd8228adb4f26f0badd9506`
覆盖文件：`海中鱼巣/领域/系统角色清单.数据.h:92-106`
根函数：F0228 `bool 海中鱼巣::系统角色初始化参数::有效() const noexcept`
规范相关字段：已按 `0600` 第 9 节完成逐节点核规；稳定键非零和唯一性依据 `4010`，版本与数值目标继续由调用方合同互证

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/系统角色清单.数据.h` | 92 | 定义 const、noexcept 的布尔成员函数并借用隐式 `this`；首个可执行节点为 B01 | B01 | 本函数内具体指令 | 顺序操作 | 无 | 当前代码事实 | 不适用 | 符合 |
| 同上 | 93 | 比较清单版本与当前版本；为真时短路后续条件 | B01 | 本函数内具体指令 | 判断 | 无 | 当前代码事实 | 写入前参数无效 | 符合 |
| 同上 | 94 | 仅在前项为假时，依次判断安全当前值、服务当前值是否小于零 | B02-B03 | 本函数内具体指令 | 判断 | 无 | 当前代码事实 | 写入前参数无效 | 符合 |
| 同上 | 95 | 仅在前项均为假时，依次判断两个目标值是否小于或等于各自当前值 | B04-B05 | 本函数内具体指令 | 判断 | 无 | 当前代码事实 | 写入前参数无效 | 符合 |
| 同上 | 96 | 任一短路条件为真时返回 `false` | RF | 本函数内具体指令 | 返回 | 无 | 当前代码事实 | 写入前参数无效 | 符合 |
| 同上 | 98 | 调用 F0230，并按值构造局部 `键组` | C06 | 函数调用 | 调用 | 无机器结构变化；创建局部数组值 | 当前代码事实 | 不适用 | 符合 |
| 同上 | 99 | 初始化 `左 = 0`；调用 `键组.size()` 取得外层上界；比较后决定进入循环；每轮结束递增 `左` | N07、X08、B09、N19 | 本函数内具体指令 / 函数调用 | 循环控制 / 调用 | 无；只改变局部索引 | 当前代码事实 | 不适用 | 符合 |
| 同上 | 100 | 调用 `键组[左]` 读取左键；等于零时立即返回 `false` | X10、B11、RF | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 | 无 | 当前代码事实 | 写入前参数无效 | 符合 |
| 同上 | 101 | 初始化 `右 = 左 + 1`；调用 `键组.size()` 取得内层上界；比较后决定进入循环；每轮结束递增 `右` | N12、X13、B14、N18 | 本函数内具体指令 / 函数调用 | 循环控制 / 调用 | 无；只改变局部索引 | 当前代码事实 | 不适用 | 符合 |
| 同上 | 102 | 对 `键组[左]` 与 `键组[右]` 各调用一次下标访问；`==` 两操作数的相对求值先后未固定；两值相等时立即返回 `false` | X15L-X16R / X15R-X16L、B17、RF | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 | 无 | 当前代码事实 | 写入前参数无效 | 符合 |
| 同上 | 103 | 结束内层循环体；控制回到第 101 行递增 `右` 并重新判定 | N18 | 本函数内具体指令 | 循环控制 | 无 | 当前代码事实 | 不适用 | 符合 |
| 同上 | 104 | 结束外层循环体；控制回到第 99 行递增 `左` 并重新判定 | N19 | 本函数内具体指令 | 循环控制 | 无 | 当前代码事实 | 不适用 | 符合 |
| 同上 | 105 | 外层循环完成后返回 `true` | RT | 本函数内具体指令 | 返回 | 无 | 当前代码事实 | 不适用 | 符合 |
| 同上 | 106 | 函数作用域结束 | RT | 本函数内具体指令 | 顺序操作 | 无 | 当前代码事实 | 不适用 | 符合 |

覆盖结论：92—106 行全部非空有效代码均已映射；纯格式空行与独立闭合括号已并入相邻循环或返回节点。项目源码调用边一条（F0228 -> F0230）；外部边界调用点五个；未解析调用零。逐节点核规见 `00_规范偏差审计.md` 的 F0228 记录。
