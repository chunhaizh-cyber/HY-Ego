# CF-L5-0009 `数据库审计写入失败 lambda` 逐行代码映射表

| 行 | 节点 | 行为 | 失败收口 |
| --- | --- | --- | --- |
| 257 | N01-N08 | 忽略快照和说明，值初始化 `{false,false,false,0,L"",L""}` 并返回 | 唯一结果是无分类的默认失败 |

该替身只验证调用者对 `成功=false` 的路由，不验证参数传递、快照完整性或真实 SQL 错误；失败阶段和诊断均为空。项目调用 0，外部边界 X0110，未解析调用 0。
