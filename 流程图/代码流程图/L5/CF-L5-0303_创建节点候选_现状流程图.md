# CF-L5-0303 `节点直接身份结构写入会话::创建节点候选` 现状流程图

图类型：现状流程图
代码版本：`4b80f1617dd70ec7a19e1a34efa9da768dce8927`
源码 blob：`ca407389bb111031643ec2d9fa41157166775db6`
覆盖文件：`海中鱼巣/核心/会话.节点直接身份结构写入.ixx:98-129`
唯一根函数：R1244
完整签名：`节点直接身份带值结构写入结果<节点句柄> 海中鱼巣::节点直接身份结构写入会话::创建节点候选(节点类型 类型, 节点稳定主键 稳定主键)`
参数：`类型`、`稳定主键` 按值输入；借用当前写入会话
返回结果：入口允许时请求节点仓形成未发布候选并转换操作结果；成功可返回当前身份，候选入本会话写集后等待后续确认或撤销
已登记调用方：R0897（RCE4013）
直接项目函数：R1261（RCE4008）、R1429（RCE4009）、R1273（RCE4010）、R1502（RCE4004）、R1267（RCE4011）、R1431（RCE4012）
外部边界：X05562 `optional::has_value`、X05563 `std::move`、X05564 `vector<节点写入项>::push_back(节点写入项&&)`、X05565 `optional::reset`、X06212 `optional<节点直接身份未发布候选>::operator*`、X06218 `optional<节点句柄>::operator=(const 节点句柄&)`
逐行映射表：`流程图/代码流程图/L5/CF-L5-0303_创建节点候选_逐行代码映射表.md`
非成功口径：入口不可继续时返回默认入口拒绝；节点仓非成功时返回转换结果，非身份冲突、入口拒绝、事务拒绝、版本漂移的状态还会记录失败；写集追加异常时撤销候选并改写为资源失败
异常：节点仓创建和结果转换异常直接传播；仅写集 `push_back` 异常由当前函数捕获。撤销或记录失败若抛出仍向外传播
不得作为施工许可：是
不得宣称：返回成功等于候选已最终发布，或被排除的四类非成功状态已经写入失败账

## 流程图

```mermaid
flowchart TD
    N01["N01【本函数内具体指令】进入R1244并默认构造输出"]
    C02["C02【函数调用 RCE4008】R1261判断会话是否可继续写入"]
    B03{"B03【本函数内具体指令】是否可继续写入"}
    R04["R04【本函数内具体指令】返回默认输出"]
    C05["C05【函数调用 RCE4009】R1429结构化创建节点未发布候选"]
    C06["C06【函数调用 RCE4010】R1273转换节点仓状态和当前身份"]
    B07{"B07【本函数内具体指令】转换状态是否为幂等读回"}
    N08["N08【本函数内具体指令】保存幂等结果"]
    C09["C09【函数调用 RCE4004】R1502判断节点直接身份创建结果是否成功"]
    B10{"B10【本函数内具体指令】节点仓创建是否成功"}
    B11{"B11【本函数内具体指令】非成功状态是否不属于四类普通拒绝"}
    C12["C12【函数调用 RCE4011】R1267记录失败"]
    R13["R13【本函数内具体指令】返回非成功输出"]
    X30["X30【外部边界 X06218】把当前身份赋给输出值optional"]
    X15["X15【外部边界 X05562】检查候选optional是否有值"]
    B16{"B16【本函数内具体指令】是否存在待登记候选"}
    X29["X29【外部边界 X06212】解引用候选optional"]
    X17["X17【外部边界 X05563】移动构造局部候选"]
    T18["T18【本函数内具体指令】进入写集追加try区"]
    X19["X19【外部边界 X05563】把局部候选转为右值"]
    X20["X20【外部边界 X05564】以右值重载向节点写集追加写入项"]
    E21["E21【本函数内具体指令】捕获写集追加的任意异常"]
    C22["C22【函数调用 RCE4012】R1431撤销节点候选"]
    N23["N23【本函数内具体指令】显式忽略撤销结果"]
    N24["N24【本函数内具体指令】把输出操作改写为资源失败"]
    X25["X25【外部边界 X05565】清空输出值"]
    C26["C26【函数调用 RCE4011】R1267记录资源失败"]
    R27["R27【本函数内具体指令】返回输出"]

    N01 --> C02 --> B03
    B03 -- "否" --> R04
    B03 -- "是" --> C05 --> C06 --> B07
    B07 -- "是" --> N08 --> C09
    B07 -- "否" --> C09
    C09 --> B10
    B10 -- "否" --> B11
    B11 -- "是" --> C12 --> R13
    B11 -- "否" --> R13
    B10 -- "是" --> X30 --> X15 --> B16
    B16 -- "否" --> R27
    B16 -- "是" --> X29 --> X17 --> T18 --> X19 --> X20 --> R27
    X20 -. "抛出" .-> E21 --> C22 --> N23 --> N24 --> X25 --> C26 --> R27
```

## 当前边界

- RCE4004 在本批共享表收口时原地纠正为 R1244→R1502；不另建重复边。
- 候选只在 X20 成功后进入本会话写集；异常路径尝试撤销仓库候选，随后清空已返回值并记录资源失败。
- `创建.候选` 无值但创建成功时，函数仍返回当前身份，不追加写集，典型语境是幂等读回。
