# CF-L5-0203 `索引仓库::按主键查节点` 令牌重载逐行代码映射表

代码版本：`5cc7036de0decad163293a0b77c4789f068fd73a`
源码 blob：`8874312f67f19b220a08b1397c6103396878513d`
覆盖文件：`海中鱼巣/核心/索引仓库.cpp:188-198`
根函数：F0441 `std::optional<海中鱼巣::节点句柄> 海中鱼巣::索引仓库::按主键查节点(std::uint64_t 主键, const 海中鱼巣::结构事务令牌& 令牌) const`
配对流程图：`流程图/代码流程图/L5/CF-L5-0203_索引仓库按主键查节点令牌重载_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`、`规范/4040_子规范_不透明结构事务候选确认撤销与最后发布.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/索引仓库.cpp` | 188 | 进入 const 成员函数 F0441；按值接收主键，借用 const 索引仓库 `this` 与 const 令牌引用 | S | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 不保存令牌引用 |
| 同上 | 189 | 经 RCE0210 调用 R0120 验证 `事务接线_` 与令牌，并与 `主键 == 0` 按短路或组合判断；任一条件成立即返回 `std::nullopt` | C01 / B02 / RN | 函数调用 / 本函数内具体指令 | 令牌验证 / 判断 / 返回 | 无 | 事务准入与入口参数事实 | 一般无效输入为逻辑内返回；强前置已保证时矛盾须追根因 | 两种拒绝折叠为同一空 optional |
| 同上 | 190 | 默认构造局部节点句柄 `候选`，等待索引命中后按值赋值 | N03 | 本函数内具体指令 | 局部构造 | 仅形成临时变量 | 临时局部变量 | 不适用 | 不把默认句柄当索引结果 |
| 同上 | 191 | 打开只覆盖索引查找与候选复制的内层词法作用域 | C04 前置 | 本函数内具体指令 | 作用域 | 无 | 当前代码事实 | 不适用 | 与 196 行共同冻结锁生命周期 |
| 同上 | 192 | 经 X01088 构造 `std::shared_lock<std::shared_mutex>` 并锁定 `仓库锁_` | C04 | 函数调用 | 外部边界 / 并发 / RAII 构造 | 取得索引共享读锁，不改机器事实 | 运行期资源状态 | 锁构造异常向外传播 | 保持 RAII |
| 同上 | 193 | 调用 `主键索引_.find(主键)`，把 const 迭代器结果保存为 `位置` | C05 | 函数调用 | 外部边界 / 索引读取 | 无 | 索引候选位置 | 不适用 | 当前正式外部表未独立编号 |
| 同上 | 194 | 调用 `主键索引_.end()`，再经 X01089 比较 `位置` 与尾后迭代器；相等时从持锁作用域直接返回空 optional，随后共享锁按 RAII 析构 | C06 / C07 / B08 / D09 / RN | 函数调用 / 本函数内具体指令 | 外部边界 / 判断 / RAII / 返回 | 释放运行期共享锁；不改索引 | 索引查找结果 | 索引未命中为逻辑内返回 | `end()` 和析构当前未独立编号 |
| 同上 | 195 | 通过位置迭代器读取命中条目的 `second.节点`，复制到局部候选句柄 | C10 / N11 | 函数调用 / 本函数内具体指令 | 外部边界 / 索引读取 / 值复制 | 只改变局部候选 | 非权威索引候选 | 不适用 | 迭代器 `operator->` 当前未独立编号 |
| 同上 | 196 | 内层作用域闭合，局部 `shared_lock` 析构并释放索引共享锁 | C12 | 函数调用 | 外部边界 / RAII 析构 | 释放运行期共享锁 | 运行期资源状态 | 不适用 | 后续节点读回发生在索引锁外 |
| 同上 | 197 | 经 RCE1848 调用 F0630，用候选句柄和同一令牌复核节点当前有效性；true 时经 X01090 从候选构造有值 optional，false 时物化空 optional，并返回条件表达式结果 | C13 / B14 / C15 / RV / RN | 函数调用 / 本函数内具体指令 | 节点读取 / 判断 / 返回值构造 / 返回 | 无 | 权威节点读回形成的只读结果 | false 一般为空候选；索引悬空语境须追根因 | 不把索引候选直接当权威节点 |
| 同上 | 198 | 函数词法作用域闭合；所有局部对象均已按返回或异常路径完成生命周期 | RV / RN | 本函数内具体指令 | 作用域 / 生命周期 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：F0441 的 188—198 行全部映射；RCE0210、RCE1848、X01088—X01090 均绑定到源码调用点；`find/end`、迭代器 `operator->` 与共享锁析构作为当前未编号外部边界记录，未修改共享聚合表。
