# CF-L7-0080 节点类型是允许特征宿主现状流程图

图类型：现状流程图
代码版本：`0a93526882cb33c61c2fbb04ecad1aeaddcfe225`
工作区复核基线：`03a8b7ac099ccea83e57f2696e2290b7bcdfacaf`
源码 blob：`4c7bcd451f2e46e85d707a40c371dcbeaa2c1169`
覆盖文件：`海中鱼巣/领域/特征服务.h:1209-1219`
唯一根函数：R0559
完整签名：`bool 海中鱼巣::特征服务::节点类型是允许特征宿主(节点句柄 节点句柄值) const`
参数：`节点句柄值` 按值输入；`this` 为 const
返回结果：节点存在且类型为存在、场景、需求、任务或方法之一时返回 `true`，读取失败或类型不在集合中时返回 `false`
已登记调用方：F0554（RCE0255）、R0762（RCE2593）、R0781（RCE2669）、R0859（RCE3162）
直接项目函数：F0190（RCE3275）
外部边界：X02248（optional 有值）、X02249（optional 箭头读取）、X04724（optional 析构）
逐行映射表：`流程图/代码流程图/L7/CF-L7-0080_节点类型是允许特征宿主_逐行代码映射表.md`
结构读写：只读节点仓库返回的局部节点记录，不修改节点或关系
不得作为施工许可：是
不得宣称：允许作为特征宿主代表特征已经创建、绑定或写入

## 流程图

```mermaid
flowchart TD
    N01["N01【本函数内具体指令】按值绑定节点句柄"]
    C02["C02【函数调用 RCE3275】F0190 读取节点并接收optional节点记录"]
    X03{"X03【函数调用 X02248】optional节点记录有值"}
    X04["X04【函数调用 X02249】读取节点类型用于存在判断"]
    B05{"B05【本函数内具体指令】节点类型为存在"}
    X06["X06【函数调用 X02249】读取节点类型用于场景判断"]
    B07{"B07【本函数内具体指令】节点类型为场景"}
    X08["X08【函数调用 X02249】读取节点类型用于需求判断"]
    B09{"B09【本函数内具体指令】节点类型为需求"}
    X10["X10【函数调用 X02249】读取节点类型用于任务判断"]
    B11{"B11【本函数内具体指令】节点类型为任务"}
    X12["X12【函数调用 X02249】读取节点类型用于方法判断"]
    B13{"B13【本函数内具体指令】节点类型为方法"}
    X14T["X14T【函数调用 X04724】返回true前析构optional节点记录"]
    X14F["X14F【函数调用 X04724】返回false前析构optional节点记录"]
    R15["R15【本函数内具体指令】返回true"]
    R16["R16【本函数内具体指令】返回false"]
    N01 --> C02 --> X03
    X03 -- 无值 --> X14F --> R16
    X03 -- 有值 --> X04 --> B05
    B05 -- 是 --> X14T --> R15
    B05 -- 否 --> X06 --> B07
    B07 -- 是 --> X14T
    B07 -- 否 --> X08 --> B09
    B09 -- 是 --> X14T
    B09 -- 否 --> X10 --> B11
    B11 -- 是 --> X14T
    B11 -- 否 --> X12 --> B13
    B13 -- 是 --> X14T
    B13 -- 否 --> X14F
```

## 当前边界

- 五种允许类型按 `||` 从左向右短路，命中后不再读取后续枚举比较。
- `节点_.读取节点(...)` 静态绑定 F0190，源码调用点为 1210，对应 RCE3275。
- 局部 `std::optional<节点记录>` 在所有返回路径离开作用域时经 X04724 析构。
