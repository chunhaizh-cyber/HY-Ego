# CF-L10-0132 特征值服务写入 I64 值现状流程图

图类型：现状流程图
代码版本：`03a8b7ac099ccea83e57f2696e2290b7bcdfacaf`
当前工作区复核基线：`00d917a5cf09998d2ab14d9239e1c194b5593ff0`
源码 blob：`be8c82c78253b3bc7d3f4aa15a93660e0025bb06`
覆盖文件：`海中鱼巣/领域/特征值服务.h:212-266`
唯一根函数：R0888
完整签名：`bool 海中鱼巣::特征值服务::写入I64值(节点句柄 特征值节点, std::int64_t 值)`
参数：`特征值节点` 与 `值` 均按值输入；可选事务令牌仅在本次调用和许可生命周期内借用
返回结果：入口、当前原始类型、版本一致性、主信息写入及写后读回均成立时返回 true；任一检查失败返回 false
调用图：正式 main 可达关系表
已登记调用方：R0562（RCE3276）
直接项目函数：F0321（RCE3288）、R0599（RCE3348）、F0336（RCE3289）、F0398（RCE3290）、R0600（RCE3349）、F0338（RCE3291）、F0339（RCE3292）、R0731（RCE3293）、R0732（RCE3294）、R0550（RCE3295）、F0184（RCE3296）、R0010（RCE3297）、F0627（RCE3298）、F0345（RCE3299）
外部边界：X04754、X04755、X04756、X04757、X04758、X04759、X04760、X04761、X04762、X04763
逐行映射表：`流程图/代码流程图/L10/CF-L10-0132_特征值服务写入I64值_逐行代码映射表.md`
不得作为施工许可：是
不得宣称：true 证明上层特征归属、特征状态或跨仓库事务已经发布

## 流程图

```mermaid
flowchart TD
 N01["N01【本函数内具体指令】进入R0888并绑定特征值节点与I64值"] --> C02["C02【函数调用 RCE3288】F0321检查事务接线形态"]
 C02 -- 无效 --> R40["R40【本函数内具体指令】返回false"]
 C02 -- 有效 --> C03["C03【函数调用 RCE3348】R0599默认构造结构事务许可"]
 C03 --> N04["N04【本函数内具体指令】令牌指针初始化为空"]
 N04 --> C05{"C05【函数调用 RCE3289】F0336判断是否已接域"}
 C05 -- 是 --> C06["C06【函数调用 RCE3290】F0398取得独占许可"]
 C06 --> C07["C07【函数调用 RCE3349】R0600把许可移动赋给局部许可"]
 C07 --> C08{"C08【函数调用 RCE3291】F0338检查许可有效"}
 C08 -- 否 --> C38["C38【函数调用 RCE3299】F0345析构结构事务许可"]
 C08 -- 是 --> C09["C09【函数调用 RCE3292】F0339取得令牌并保存地址"]
 C05 -- 否 --> C10["C10【函数调用 RCE3293】R0731读取有效特征值节点"]
 C09 --> C10
 C10 --> X11{"X11【外部边界 X04754】节点记录是否有值"}
 X11 -- 否 --> X37["X37【外部边界 X04756】析构节点记录optional"]
 X11 -- 是 --> X12["X12【外部边界 X04757】取得原始值独占锁"]
 X12 --> X13["X13【外部边界 X04755】读取节点记录中的主信息"]
 X13 --> C14["C14【函数调用 RCE3294】R0732读取写入前原始值状态"]
 C14 --> B15{"B15【本函数内具体指令】状态内部一致且类型不是VecI64/VecU64"}
 B15 -- 否 --> X36["X36【外部边界 X04758】释放原始值独占锁"]
 B15 -- 是 --> X16{"X16【外部边界 X04763】已有I64版本是否达到最大值"}
 X16 -- 是 --> X36
 X16 -- 否 --> N17["N17【本函数内具体指令】按原类型计算新版本"]
 N17 --> C18["C18【函数调用 RCE3295】R0550查找I64版本记录"]
 C18 --> X19["X19【外部边界 X04759/X04760】判断版本记录是否存在"]
 X19 --> C20{"C20【函数调用 RCE3296】F0184核对原始值类型与版本记录存在性一致"}
 C20 -- 否 --> X36
 C20 -- 是 --> B21{"B21【本函数内具体指令】令牌指针非空"}
 B21 -- 是 --> C22["C22【函数调用 RCE3297】R0010带令牌写入主信息I64"]
 B21 -- 否 --> C23["C23【函数调用 RCE3298】F0627无令牌写入主信息I64"]
 C22 --> C24{"C24【函数调用 RCE3296】F0184检查主信息写入成功"}
 C23 --> C24
 C24 -- 否 --> X36
 C24 -- 是 --> B25{"B25【外部边界 X04759】版本记录是否不存在"}
 B25 -- 是 --> X26["X26【外部边界 X04761】追加特征值节点与新版本记录"]
 B25 -- 否 --> X27["X27【外部边界 X04762】把已有版本记录更新为新版本"]
 X26 --> C28["C28【函数调用 RCE3294】R0732读取写入后原始值状态"]
 X27 --> C28
 C28 --> C29["C29【函数调用 RCE3296】F0184核对内部一致、I64类型、新版本和值"]
 C29 --> X36
 X36 --> X37
 X37 --> C38
 C38 --> B39{"B39【本函数内具体指令】最终检查结果"}
 B39 -- true --> R41["R41【本函数内具体指令】返回true"]
 B39 -- false --> R40
```

## 当前边界

- 接线形态无效时尚未构造许可，直接返回；其余路径按实际已构造对象释放锁、optional 和结构事务许可。
- RCE3348、RCE3349 是本次逐行复核发现并由顶层分配的稳定直接项目边；共享关系表由顶层串行收口。
