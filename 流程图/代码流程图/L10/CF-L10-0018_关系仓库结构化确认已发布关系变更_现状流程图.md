# CF-L10-0018 关系仓库结构化确认已发布关系变更现状流程图

图类型：现状流程图
代码版本：`176b674a6c1499ccdd7306f30f910fec6dc5079c`
源码事实提交：`3920a74638264f9f539d50f32f3c9fe5fb1982ab`
源码 blob：`f7a9ea9a09d3065468208c16f9ea34f806b6e183`
覆盖文件：`海中鱼巣/核心/关系仓库.cpp:1365-1392`
唯一根函数：R0071
完整签名：`海中鱼巣::结构写入结果 海中鱼巣::关系仓库::结构化确认已发布关系变更(海中鱼巣::已发布关系变更能力& 能力, const 海中鱼巣::结构事务令牌& 令牌)`
参数：能力为可写借用；令牌为 const 非拥有借用；隐式 `this` 为仓库借用
返回结果：许可或能力不合格时返回拒绝结果；锁内记录一致且能力处于持有阶段时标记能力已确认并返回已提交
已登记调用方：R0060，经 RCE0411
直接项目函数：RCE0449→R0095、RCE0450→R0091、RCE0451→R0070、RCE0452→R0092、RCE0453→R0080、RCE0454→R0064
外部边界：X01499/X01500/X02894—X02897
未解析项目调用：0
逐行映射表：`流程图/代码流程图/L10/CF-L10-0018_关系仓库结构化确认已发布关系变更_逐行代码映射表.md`
结构读取与写入：锁内只读关系表；成功时写能力阶段和局部输出，不修改关系记录
验证输出：1365—1392 行完整映射；1 条项目入边、6 条项目出边、6 个外部边界
不得作为施工许可：是

## 流程图

```mermaid
flowchart TD
    N01["N01【本函数内具体指令】进入R0071并绑定能力与令牌"]
    C02["C02【函数调用 RCE0449】R0095 能力.读取写后记录()→关系记录"]
    N03["N03【本函数内具体指令】以入口拒绝和写后身份初始化输出"]
    C04["C04【函数调用 RCE0450】R0091 验证独占令牌(事务接线_,令牌)→bool"]
    B04A{"B04A【本函数内具体指令】独占令牌是否有效"}
    C05["C05【函数调用 RCE0451】R0070 能力.完整()→bool"]
    B05A{"B05A【本函数内具体指令】能力是否完整"}
    B06{"B06【本函数内具体指令】能力仓库编号是否匹配"}
    C07["C07【函数调用 RCE0452】R0092 令牌一致(能力.令牌_,令牌)→bool"]
    B08{"B08【本函数内具体指令】能力令牌是否与输入令牌一致"}
    N09["N09【本函数内具体指令】把输出状态改为许可拒绝"]
    RR["RR【本函数内具体指令】返回输出"]
    C10["C10【函数调用 RCE0453】R0080 能力.处于持有阶段()→bool"]
    B11{"B11【本函数内具体指令】能力是否仍处于持有阶段"}
    X12["X12【外部边界 X01499】构造unique_lock并取得仓库独占锁"]
    X13["X13【外部边界 X02894】关系表_.find(写后关系编号)→位置"]
    X14["X14【外部边界 X02895】关系表_.end()→尾后位置"]
    X15["X15【外部边界 X01500】比较位置与尾后位置"]
    X16["X16【外部边界 X02896】位置.operator->()→关系表键值对"]
    C17["C17【函数调用 RCE0454】R0064 关系记录一致(位置->second,写后记录)→bool"]
    B18{"B18【本函数内具体指令】命中且记录一致"}
    N19["N19【本函数内具体指令】把输出状态改为内部不一致"]
    N20["N20【本函数内具体指令】能力阶段置已确认且输出状态置已提交"]
    X21["X21【外部边界 X02897】析构unique_lock并释放独占锁"]
    N01 --> C02 --> N03 --> C04 --> B04A
    B04A -- "否" --> N09 --> RR
    B04A -- "是" --> C05 --> B05A
    B05A -- "否" --> N09
    B05A -- "是" --> B06
    B06 -- "否" --> N09
    B06 -- "是" --> C07 --> B08
    B08 -- "否" --> N09
    B08 -- "是" --> C10 --> B11
    B11 -- "否" --> RR
    B11 -- "是" --> X12 --> X13 --> X14 --> X15
    X15 -- "等于end" --> N19 --> X21 --> RR
    X15 -- "命中" --> X16 --> C17 --> B18
    B18 -- "否" --> N19
    B18 -- "是" --> N20 --> X21
```

## 当前边界

- RCE0450、RCE0451、仓库编号比较和 RCE0452 按 `||` 顺序短路。
- X02896 和 RCE0454 仅在 X01500 判定位置不等于 end 时可达。
- X02897 覆盖独占锁构造后的成功、早退与异常展开，不把解锁解释为结构提交。
