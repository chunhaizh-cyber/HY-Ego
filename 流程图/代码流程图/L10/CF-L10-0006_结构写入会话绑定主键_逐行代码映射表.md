# CF-L10-0006 结构写入会话绑定主键逐行代码映射表

代码版本：`3920a74638264f9f539d50f32f3c9fe5fb1982ab`
源码 blob：`df70de9c299c74f7f0766a3e94facaf504f57968`
覆盖文件：`海中鱼巣/核心/会话.结构写入.ixx:451-467`
根函数：R0027 `海中鱼巣::结构写入结果 海中鱼巣::结构写入会话::绑定主键(const 海中鱼巣::索引绑定请求& 请求)`
配对流程图：`流程图/代码流程图/L10/CF-L10-0006_结构写入会话绑定主键_现状流程图.md`
依据：RCE1147、RCE1272、RCE1395、RCE1828、RCE1940、RCE1988、RCE2054、RCE0353—RCE0357、RCE2448—RCE2449、X02804、当前源码与 `规范/0600_代码函数流程图应用与维护规范.md`
稳定身份反查：调用方 R0358、R0405、R0451、R0634、R0656、R0678、R0696；根函数 R0027；直接被调 R0044、R0136、R0119、R0138、R0052、R0742、R0125

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/会话.结构写入.ixx` | 451 | 定义 R0027，借用 const 请求并借用 `this` | N01 | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 452 | 经 RCE0353 判断会话可继续；成立时经 RCE0354 调用 R0136 判断请求显式完整；任一失败返回默认入口拒绝结果 | C02 / B03 / C04 / B05 / R06 | 函数调用 / 本函数内具体指令 | 判断 / 短路 / 返回 | 零索引写入 | 当前入口门禁 | 逻辑内返回 | 无 |
| 同上 | 453 | 经 RCE0355 调用 R0119 结构化绑定主键并保存结果 | C07 | 函数调用 | 索引写入 | 可能绑定主键 | 当前索引写入结果 | 被调协议结果 | 无 |
| 同上 | 454—457 | 经 RCE2448 调用 R0138 判断成功；失败时经 RCE0356 记录失败并返回结果 | C08 / B09 / C10 / R11 | 函数调用 / 本函数内具体指令 | 判断 / 诊断记录 / 返回 | 不新增；保留被调结果 | 当前写入结果 | 协议失败为逻辑内结果；强前置成立时追根因 | 无 |
| 同上 | 458 | 成功时经 RCE2449 调用 R0742 判断是否改变结构；未改变直接返回 | C12 / B13 / R11 | 函数调用 / 本函数内具体指令 | 判断 / 幂等返回 | 无新增 | 当前幂等结果 | 逻辑内返回 | 无 |
| 同上 | 459—460 | 改变结构时进入 try，经 X02804 把请求与未撤销标记加入索引写集 | X14 | 外部边界函数调用 | 会话写集登记 | 会话索引写集新增记录 | 当前会话跟踪事实 | 分配异常进入回滚 | 无 |
| 同上 | 461—462 | 捕获任意登记异常，经 RCE0357 调用 R0125 严格删除主键并丢弃回滚结果 | C15 | 函数调用 | 异常收口 / 回滚写入 | 尝试删除本次主键 | 当前回滚尝试 | 回滚失败未结构化区分 | 验证并传播回滚结果 |
| 同上 | 463—464 | 把结果状态改为内部不一致并经 RCE0356 记录失败 | N16 / C17 | 本函数内具体指令 / 函数调用 | 状态改写 / 失败记录 | 会话失败状态可能更新 | 当前内部错误结果 | 追根因解决 | 无 |
| 同上 | 465 | 闭合 catch 块 | C17 | 本函数内具体指令 | 作用域 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 466 | 返回成功、幂等、协议失败或内部不一致结果 | R11 | 本函数内具体指令 | 返回 | 无 | 当前结果事实 | 依结果状态分账 | 无 |
| 同上 | 467 | 闭合函数作用域 | R06 / R11 | 本函数内具体指令 | 作用域 / 生命周期 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：451—467 行全部映射；R0027、RCE0353—RCE0357、RCE2448—RCE2449、X02804 及全部图节点可反查；无未解析调用。
