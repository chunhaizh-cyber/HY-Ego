# CF-L10-0007 结构写入会话写入候选I64值逐行代码映射表

代码版本：`3920a74638264f9f539d50f32f3c9fe5fb1982ab`
源码 blob：`df70de9c299c74f7f0766a3e94facaf504f57968`
覆盖文件：`海中鱼巣/核心/会话.结构写入.ixx:469-504`
根函数：R0028 `海中鱼巣::结构写入结果 海中鱼巣::结构写入会话::写入候选I64值(海中鱼巣::主信息句柄 主信息, std::uint64_t 槽位, std::int64_t 预期值)`
配对流程图：`流程图/代码流程图/L10/CF-L10-0007_结构写入会话写入候选I64值_现状流程图.md`
依据：RCE1169、RCE1270、RCE1985、RCE0358—RCE0361、RCE2450—RCE2452、X01365—X01372、X02805—X02807、当前源码与 `规范/0600_代码函数流程图应用与维护规范.md`
稳定身份反查：调用方 R0362、R0405、R0678；根函数 R0028；直接被调 R0044、F0565、R0015、R0014、R0615、R0010、R0052

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/会话.结构写入.ixx` | 469—472 | 定义 R0028，按值绑定主信息、槽位、预期值并借用 `this` | N01 | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 473 | 初始化入口拒绝输出，绑定主信息编号和版本 | N01 | 本函数内具体指令 | 局部构造 | 无 | 当前结果材料 | 不适用 | 无 |
| 同上 | 474 | 经 RCE2450 判断会话可继续；成立时经 RCE0358 调用 F0565 检查主信息句柄；任一失败返回输出 | C02 / B03 / C04 / B05 / R06 | 函数调用 / 本函数内具体指令 | 判断 / 短路 / 返回 | 零写入 | 当前入口门禁 | 逻辑内返回 | 无 |
| 同上 | 476 | 初始化本会话候选标志为 false | N07 | 本函数内具体指令 | 局部赋值 | 无 | 临时控制材料 | 不适用 | 无 |
| 同上 | 477 | 经 X01365/X01366 初始化第一范围循环，经 X01368 判断结束；每轮经 X02805 调用 `vector<主信息候选记录>::iterator::operator*() const` 取得 `主信息候选记录&`，随后绑定 `const auto& 记录` | X08 / X09 / B10 / X11 | 外部边界函数调用 / 本函数内具体指令 | 循环 / 读取 | 无 | 当前会话候选组 | 空组进入归属失败 | 无 |
| 同上 | 478 | 经 RCE0359 判断候选完整；完整时经 RCE2451 读取候选句柄并经 RCE2452 调用 R0615 比较目标句柄 | C12 / B13 / C14 / C15 / B16 | 函数调用 / 本函数内具体指令 | 判断 / 读取 / 比较 / 短路 | 无 | 当前候选归属判断 | 不匹配继续循环 | 无 |
| 同上 | 479—481 | 匹配时置归属标志并退出循环；不匹配经 X01367 递增后继续 | N17 / X18 / B10 | 本函数内具体指令 / 外部边界函数调用 | 赋值 / break / 循环 | 无 | 临时控制材料 | 不适用 | 无 |
| 同上 | 482 | 闭合第一循环 | B10 | 本函数内具体指令 | 作用域 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 483 | 不属于本会话候选时返回入口拒绝输出 | B19 / R06 | 本函数内具体指令 | 判断 / 返回 | 零写入 | 当前候选归属门禁 | 逻辑内返回 | 无 |
| 同上 | 485 | 经 X01369/X01370 初始化第二范围循环，经 X01372 判断结束；每轮经 X02806 调用 `vector<候选I64写入记录>::iterator::operator*() const` 取得 `候选I64写入记录&`，随后绑定 `const auto& 记录` | X20 / X21 / B22 / X23 | 外部边界函数调用 / 本函数内具体指令 | 循环 / 读取 | 无 | 当前会话I64写集 | 空集进入首次写入 | 无 |
| 同上 | 486 | 经 RCE2452 调用 R0615 形成主信息不等结果，并与槽位不等作短路判断；任一不同则经 X01371 递增继续 | C24 / B25 / X28 | 函数调用 / 本函数内具体指令 / 外部边界函数调用 | 比较 / continue / 循环 | 无 | 当前写集匹配 | 不匹配继续 | 无 |
| 同上 | 487 | 同主信息同槽时比较预期值；不同立即返回入口拒绝输出 | B26 / R06 | 本函数内具体指令 | 判断 / 返回 | 零新增写入 | 当前单槽一致性门禁 | 逻辑内拒绝 | 无 |
| 同上 | 488—489 | 同值时形成并返回幂等读回结果，随后闭合第二循环 | R27 / B22 | 本函数内具体指令 | 返回 / 作用域 | 无 | 当前幂等结果 | 逻辑内返回 | 无 |
| 同上 | 491 | 写集未命中时经 RCE0360 调用 R0010，以会话令牌写入 I64 并判断结果 | C29 / B30 | 函数调用 / 本函数内具体指令 | 仓库写入 / 判断 | 可能写主信息值槽 | 当前旧值写入事实 | false 为内部不一致 | 无 |
| 同上 | 492—495 | 仓库写入失败时把输出改为内部不一致，经 RCE0361 记录失败并返回 | N31 / C32 / R06 | 本函数内具体指令 / 函数调用 | 状态改写 / 失败记录 / 返回 | 会话失败账可能更新 | 当前内部错误 | 追根因解决 | 无 |
| 同上 | 496—497 | 写入成功后进入 try，经 X02807 把写入记录加入候选 I64 写集 | X33 | 外部边界函数调用 | 会话写集登记 | 写集新增记录 | 当前会话跟踪事实 | 异常进入内部错误 | 无 |
| 同上 | 498—502 | 捕获登记异常，把输出改为内部不一致，经 RCE0361 记录失败并返回；不撤销已写仓库值 | N31 / C32 / R06 | 本函数内具体指令 / 函数调用 | 异常收口 / 失败记录 / 返回 | 仓库值可能已写、写集未登记 | 当前半结构风险事实 | 追根因解决 | 增加撤销或候选发布边界 |
| 同上 | 503 | 登记成功时返回已提交结果 | R34 | 本函数内具体指令 | 返回 | 无新增 | 当前写入结果 | 正常返回 | 无 |
| 同上 | 504 | 闭合函数作用域 | R06 / R27 / R34 | 本函数内具体指令 | 作用域 / 生命周期 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：469—504 行全部映射；R0028、RCE0358—RCE0361、RCE2450—RCE2452、X01365—X01372、X02805—X02807 及全部图节点可反查；无未解析调用。
