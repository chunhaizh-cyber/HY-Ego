# CF-L10-0062 特征体系写入带主键节点会话现状流程图

图类型：现状流程图
代码版本：`3920a74638264f9f539d50f32f3c9fe5fb1982ab`
源码 blob：`a47cd9b07794d30abc6cb9d1df319056105cd596`
覆盖文件：`海中鱼巣/领域/数据操作.特征体系.ixx:1440-1456`
唯一根函数：R0358
完整签名：`static bool 海中鱼巣::特征体系数据操作::写入带主键节点_会话(结构写入会话& 会话, 节点类型 类型, std::uint64_t 主键, 主信息句柄& 新主信息, 节点句柄& 新节点)`
参数：`会话` 为结构写入会话引用；`类型`、`主键` 为创建与绑定输入；`新主信息`、`新节点` 为成功路径输出引用
返回结果：候选创建、主键绑定和三项读回检查均成功时返回 true；任一内部写入或读回异常时返回 false
已登记调用方：R0361（RCE1159）、R0362（RCE1165）、R0708（RCE2093）
直接项目函数：R0021（RCE1145）、R0640（RCE2768）、R0022（RCE1146）、R0641（RCE2769）、R0129（RCE1148）、R0027（RCE1147）、R0138（RCE2770）、R0036（RCE1149）、R0037（RCE1150）、R0638（RCE2164）；RCE1151 误绑 R0040，停用
外部边界：X03715（主信息句柄 optional 解引用）、X03716（主信息句柄 optional 析构）、X03717（节点句柄 optional 解引用）、X03718（节点句柄 optional 析构）
未解析项目调用：0
逐行映射表：`流程图/代码流程图/L10/CF-L10-0062_特征体系写入带主键节点会话_逐行代码映射表.md`
依据：关系发布基线、冻结源码、RCE1151 重载误绑停用裁决与 RCE2768—RCE2770
结构读取与写入：在会话候选结构中创建主信息和节点、绑定主键并读回；失败由外层会话事务收口
逻辑内返回：无
内部错误边界：候选创建、绑定或读回不成立均返回 false，属于前置通过后的内部结构异常
不得作为施工许可：是
不得宣称：返回 true 代表事务已经提交或结构已经发布

## 流程图

```mermaid
flowchart TD
    D00["D00【本函数内具体指令】绑定会话、类型、主键与两个输出引用"]
    C01["C01【函数调用】R0021 创建主信息候选() -> 主结果<br/>RCE1145"]
    C02["C02【函数调用】R0640 主结果.成功() -> bool<br/>RCE2768"]
    B03{"B03【本函数内具体指令】主结果是否成功"}
    X04["X04【函数调用】释放主结果所含optional候选<br/>X03716"]
    R05["R05【本函数内具体指令】返回false"]
    X06["X06【函数调用】optional&lt;主信息句柄&gt;::operator*() -> 句柄<br/>X03715"]
    S07["S07【本函数内具体指令】把句柄写入新主信息"]
    C08["C08【函数调用】R0022 创建节点候选(类型, 新主信息) -> 节点结果<br/>RCE1146"]
    C09["C09【函数调用】R0641 节点结果.成功() -> bool<br/>RCE2769"]
    B10{"B10【本函数内具体指令】节点结果是否成功"}
    X11["X11【函数调用】释放节点结果与主结果所含optional候选<br/>X03718、X03716"]
    X12["X12【函数调用】optional&lt;节点句柄&gt;::operator*() -> 句柄<br/>X03717"]
    S13["S13【本函数内具体指令】把句柄写入新节点"]
    C14["C14【函数调用】R0129 形成索引绑定请求(主键, 新节点, 特征体系) -> 请求<br/>RCE1148"]
    C15["C15【函数调用】R0027 绑定主键(请求) -> 结构写入结果<br/>RCE1147"]
    C16["C16【函数调用】R0138 结构写入结果.成功() -> bool<br/>RCE2770"]
    B17{"B17【本函数内具体指令】主键绑定是否成功"}
    C18["C18【函数调用】R0036 主信息可读(新主信息) -> bool<br/>RCE1149"]
    B19{"B19【本函数内具体指令】主信息是否可读"}
    C20["C20【函数调用】R0037 节点可读(新节点) -> bool<br/>RCE1150"]
    B21{"B21【本函数内具体指令】节点是否可读"}
    C22["C22【函数调用】R0638 主键绑定匹配(主键, 新节点) -> bool<br/>RCE2164"]
    X23["X23【函数调用】释放节点结果与主结果所含optional候选<br/>X03718、X03716"]
    R24["R24【本函数内具体指令】返回主键绑定匹配结果"]
    D00 --> C01 --> C02 --> B03
    B03 -- 否 --> X04 --> R05
    B03 -- 是 --> X06 --> S07 --> C08 --> C09 --> B10
    B10 -- 否 --> X11 --> R05
    B10 -- 是 --> X12 --> S13 --> C14 --> C15 --> C16 --> B17
    B17 -- 否 --> X11
    B17 -- 是 --> C18 --> B19
    B19 -- 否 --> X11
    B19 -- 是 --> C20 --> B21
    B21 -- 否 --> X11
    B21 -- 是 --> C22 --> X23 --> R24
```

## 当前边界

- `主结果.成功()`、`节点结果.成功()` 和绑定结果 `.成功()` 分别是 R0640、R0641、R0138 的直接调用；三条项目边在原聚合中缺失。
- 第 1455 行是双参数 `主键绑定匹配(std::uint64_t, 节点句柄)`，只匹配 R0638；RCE1151 指向单参数 R0040，属于重载误绑并停用。
- 所有 false 分支都发生在结构写入会话内部；本函数不提交事务，也不把局部候选发布为可读结构。
