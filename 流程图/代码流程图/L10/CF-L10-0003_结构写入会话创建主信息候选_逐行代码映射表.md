# CF-L10-0003 结构写入会话创建主信息候选逐行代码映射表

代码版本：`0502bcbc13202f97d0d4b4ddefd8476d6deb6c1a`
源码事实提交：`3920a74638264f9f539d50f32f3c9fe5fb1982ab`
源码 blob：`df70de9c299c74f7f0766a3e94facaf504f57968`
覆盖文件：`海中鱼巣/核心/会话.结构写入.ixx:234-260`
根函数：R0021 `海中鱼巣::带值结构写入结果<海中鱼巣::主信息句柄> 海中鱼巣::结构写入会话::创建主信息候选()`
配对流程图：`流程图/代码流程图/L10/CF-L10-0003_结构写入会话创建主信息候选_现状流程图.md`
依据：入边 RCE1145/RCE1269/RCE1393/RCE1823/RCE1937/RCE1984/RCE2051；出边 RCE0331—RCE0334、RCE2419—RCE2421；外部边界 X01355—X01356、X02767—X02770；当前源码与 `规范/0600_代码函数流程图应用与维护规范.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/会话.结构写入.ixx` | 234-235 | 定义 R0021 并默认构造带值输出 | N01 | 本函数内具体指令 | 入口 / 局部构造 | 仅形成返回材料 | 当前代码事实 | 不适用 | 无 |
| 同上 | 236 | 经 RCE0331 以 `this=this` 调用 R0044；不可写时直接返回默认输出 | C02 / B03 / R00 | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 | 无 | 当前会话状态 | 逻辑内返回 | 无 |
| 同上 | 237 | 经 RCE0332 以 `this=&主信息_`、`令牌=令牌_` 调用 R0011，结果绑定局部 `结果` | C04 | 函数调用 | 调用 / 写入 | 仓库可能创建未发布主信息候选 | 当前仓库结果 | 后继检查 | 无 |
| 同上 | 238 | 把 `结果.操作` 复制到 `输出.操作` | N05 | 本函数内具体指令 | 赋值 | 仅改变返回材料 | 当前代码事实 | 不适用 | 无 |
| 同上 | 239-242 | 经 RCE2419 以 `this=&结果` 调用 R0739 判断结果成功；失败时经 RCE0333 传入 `结果.操作` 记录首次失败并返回 | P06 / B07 / C08 / R00 | 函数调用 / 本函数内具体指令 | 判断 / 诊断 / 返回 | 记录会话失败事实 | 当前代码事实 | 内部非成功返回 | 无 |
| 同上 | 243-247 | 经 X02767 检查 optional 值；缺值时把输出状态改为内部不一致，经 RCE0333 传入 `输出.操作` 后返回 | X09 / B10 / N11 / C12 / R00 | 函数调用 / 本函数内具体指令 | 判断 / 赋值 / 诊断 / 返回 | 记录会话失败事实 | 当前内部一致性检查 | 追根因解决 | 无 |
| 同上 | 248 | 经 X02768 解引用 optional，再经 X01355 移动其候选值并构造局部候选 | X14 / X13 / N15 | 函数调用 / 本函数内具体指令 | 读取 / 移动构造 | 候选所有权移入局部变量 | 当前代码事实 | 不适用 | 无 |
| 同上 | 249 | 经 RCE0334 以 `this=&候选` 调用 R0014，主信息句柄绑定局部 `主信息` | C16 | 函数调用 | 读取 | 无 | 当前候选材料 | 不适用 | 无 |
| 同上 | 250-251 | 进入 try，经 X01356 移动候选，再经 X02769 向会话主信息候选组登记 | B17 / X18 / X19 | 函数调用 / 本函数内具体指令 | 异常边界 / 移动 / 写集登记 | 候选可能进入会话写集 | 当前代码事实 | 抛异常转捕获分支 | 无 |
| 同上 | 252-253 | 捕获任意异常；经 RCE2420 调用 R0015 检查移动后候选，仍完整时经 RCE2421 调用 R0006 撤销，返回值被丢弃 | B17 / P21 / B22 / P23 | 函数调用 / 本函数内具体指令 | 异常 / 判断 / 撤销 | 可能撤销未发布候选 | 当前补偿路径 | 追根因解决 | 无 |
| 同上 | 254-257 | 构造内部不一致操作结果，经 RCE0333 传入 `输出.操作` 记录失败并返回 | N24 / C25 / R00 | 函数调用 / 本函数内具体指令 | 赋值 / 诊断 / 返回 | 记录会话失败事实 | 当前失败结果 | 追根因解决 | 无 |
| 同上 | 258-259 | 正常登记后经 X02770 把主信息置入输出 optional，并返回输出 | X20 / R00 | 函数调用 / 本函数内具体指令 | 构造 / 返回 | 返回材料取得主信息句柄 | 当前成功结果 | 不适用 | 无 |
| 同上 | 260 | 闭合函数作用域 | R00 | 本函数内具体指令 | 作用域 / 生命周期 | 局部材料析构 | 当前代码事实 | 不适用 | 无 |

覆盖结论：234—260 行全部映射；RCE0331—RCE0334、RCE2419—RCE2421、X01355—X01356、X02767—X02770 和全部图节点均可反查。
