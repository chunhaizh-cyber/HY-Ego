# 当前代码函数规范偏差审计

冻结提交：`a48a70b2d678dea64dd8228adb4f26f0badd9506`

本表随可达函数闭包逐函数追加。第一轮只记录代码事实与现行正式规范的差异，不形成代码施工许可。

核规依据：`规范/0600_代码函数流程图应用与维护规范.md` 第 9 节。自 F0227 起使用 `符合 / 证据不足 / 实现缺失 / 接口缺失 / 实现偏差 / 归属偏差 / 冗余候选 / 规范缺口 / 设计缺口` 九类状态，并登记逐节点证据。F0001—F0226 的既有函数级记录仍是历史审计输入，尚未全部转换为 0600 的逐节点格式，不得把“函数级已登记”误报为“节点级已闭合”。

| 编号 | 函数 | 当前代码事实 | 正式规范口径 | 偏差类型 | 唯一纠偏归属 | 是否需要代码计划 |
| --- | --- | --- | --- | --- | --- | --- |
| A0001 | F0001 `main` | 只解析并验证参数、拒绝时映射退出码、接受时调用唯一顶层函数并映射退出码 | `8120` 第 2、4 节要求薄入口只承担这四项职责 | 无偏差 | 不适用 | 否 |
| A0002 | F0002 `解析并验证启动选项` | 逐项拒绝无效数量、空数组、空指针、未知参数、重复/冲突模式和缺失构建能力；接受时返回唯一模式 | `8120` 第 3、4 节规定相同参数、模式和退出口径 | 无偏差 | 不适用 | 否 |
| A0003 | F0003 `启动选项解析结果::成功` | 只在结构化状态为“已接受”时返回 true | `8120` 第 3、4 节要求拒绝和准入由结构化解析结果承载 | 无偏差 | 不适用 | 否 |
| A0004 | F0004 `映射进程退出码` | 把解析拒绝、运行失败和运行成功分别映射为 2、1、0 | `8120` 第 4 节冻结相同进程退出码合同 | 无偏差 | 不适用 | 否 |
| A0005 | F0005 `运行海中鱼巣` | 仅按已验证模式调用一个对应顶层函数；穷举外值返回内部不一致 | `8120` 第 5 节要求顶层路由层按模式分发且不展开装配正文 | 无偏差 | 不适用 | 否 |
| A0006 | F0006 `程序运行结果::成功` | 只在结构化状态为“已完成”时返回 true | `8120` 第 4 节要求程序成功语义由结构化结果承载 | 无偏差 | 不适用 | 否 |
| A0007 | F0007 `运行普通程序` | 装配、生产初始化、最佳努力审计、信号宿主、UI/headless 顺序明确；审计失败不阻断普通启动 | `8120` 第 5—8 节冻结相同普通程序分层 | 无偏差 | 不适用 | 否 |
| A0008 | F0008 `运行生产运行期模式` | 以当前版本请求调用会话启动并映射结构化结果 | `8120` 第 5 节要求生产运行期通过结构化会话合同进入 | 无偏差 | 不适用 | 否 |
| A0009 | F0009 `运行完整自检模式` | 显式模式登记 14 个入口单元及 F27/F29，并核对 16 项批次合同 | `8120` 要求完整自检只按显式模式调用并返回结构化结果 | 无偏差 | 不适用 | 否 |
| A0010 | F0010 `运行数据库专项` | 核心初始化后执行必须往返一致审计；支持覆盖端口；日志不参与裁决 | `8120` 第 7、9 节冻结相同数据库专项和日志边界 | 无偏差 | 不适用 | 否 |
| A0011 | F0011 `运行关系仓库性能专项` | 只在能力宏开启时调用性能自检，并通过具名日志宏记录人读结果 | `8120` 第 9 节要求机器结果独立于日志 | 本函数无偏差；下游偏差见 A0037 | F0037 | 是 |
| A0012 | F0012 `运行D455真实样本专项` | 只在能力宏开启时调用真实样本自检，并通过具名日志宏记录人读结果 | `8120` 第 5、9 节要求显式专项和统一日志 | 本函数无偏差；下游偏差见 A0038 | F0038 | 是 |
| A0013 | F0013 `构造普通应用上下文` | 校验配置后一次构造完整上下文，异常转为结构化装配失败 | `8120` 要求普通应用装配集中拥有仓库和服务生命周期 | 无偏差 | 不适用 | 否 |
| A0014 | F0014 `普通应用装配结果::成功` | 同时要求状态已装配且上下文非空 | `8120` 要求成功结果必须承载可用上下文 | 无偏差 | 不适用 | 否 |
| A0015 | F0015 `初始化普通系统` | 依次校验请求/端口、方法根、自我线程、初始化快照、概念四根和存在根支持 | `8120` 冻结生产初始化顺序及结构化失败 | 无偏差 | 不适用 | 否 |
| A0016 | F0016 `系统初始化结果::成功` | 校验状态、方法根、快照、概念图和存在根支持的完整一致 | `8120` 要求初始化成功不能仅依赖状态枚举 | 无偏差 | 不适用 | 否 |
| A0017 | F0017 `普通程序失败阶段映射 lambda` | 把多类非具名状态及“已初始化但结果字段不完整”统一折叠为普通应用装配阶段 | 正式规范未冻结更细的失败阶段映射 | 语义粒度风险，尚非确认冲突 | 后续入口诊断治理 | 否 |
| A0018 | F0018 `执行启动结构审计` | `要求` 参数未参与分支；快照失败状态当前不可达；命名空间校验未检查可持久化和精确失效条件 | `8120` 区分普通最佳努力与专项必须往返一致，且审计命名空间应可机械确认 | 当前实现差异候选 | 数据库审计职责 | 是 |
| A0019 | F0019 `安装程序停止信号` | 顺序安装三个信号并在失败时依赖租约析构恢复 | `8120` 要求宿主层安装/恢复停止信号 | 无确认偏差；静态原始指针和异常恢复属于风险 | 运行宿主 | 否 |
| A0020 | F0020 `停止信号安装结果::成功` | 同时要求状态已安装和租约非空 | 结构化结果必须承载可用租约 | 无偏差 | 不适用 | 否 |
| A0021 | F0021 `运行无窗口宿主` | 仅轮询停止请求并休眠，收到请求后返回成功 | `8120` 要求无窗口宿主保持常驻并可由停止信号收口 | 无偏差 | 不适用 | 否 |
| A0022 | F0022 `生成控制面板启动投影` | 坐标缺失不会独立拒绝，而是继续生成世界树材料；若下游仍可展示则投影成功 | 现行 ENTRY-ONLY 施工设计曾要求坐标为空时投影失败；正式 `8120` 需复核是否把坐标列为成功前置 | 当前代码与既有设计差异，正式规范裁决待确认 | 控制面板投影 | 是 |
| A0023 | F0023 `控制面板启动投影结果::成功` | 要求状态已形成且两树均可展示、只读 | `8120` 要求控制面板仅消费只读投影 | 无偏差 | 不适用 | 否 |
| A0024 | F0024 `运行控制面板宿主` | 先校验初始化/投影，再装配服务和窗口并把窗口结果映射为程序结果 | `8120` 要求 UI 宿主不承担生产初始化 | 无偏差 | 不适用 | 否 |
| A0025 | F0025 `停止信号租约析构` | 恢复已安装处理器并清空当前租约指针；注入回调抛出会触发隐式 noexcept 终止 | `8120` 要求停止信号生命周期由宿主租约收口 | 无确认偏差；回调异常为风险 | 运行宿主 | 否 |
| A0026 | F0026 `生产运行期会话构造` | 仅默认初始化启动锁和空宿主 | 生产运行期通过会话持有串行启动边界 | 无偏差 | 不适用 | 否 |
| A0027 | F0027 `生产运行期会话::启动` | 完整校验请求、配置、候选、发布和发布后租约一致性 | 生产运行期正式合同要求候选完整后单次发布并读回 | 无偏差 | 不适用 | 否 |
| A0028 | F0028 `生产运行期启动结果::成功` | 仅允许两种成功状态且要求租约有效 | 成功不得脱离有效运行期上下文租约 | 无偏差 | 不适用 | 否 |
| A0029 | F0029 `登记入口初始化自检单元` | 14 项严格短路登记，前项成功后后项失败不回滚 | `8120` 要求这些自检只由显式自检路径登记 | 无偏差 | 不适用 | 否 |
| A0030 | F0030 `自检运行器::登记自检` | 非法或重复登记永久写坏合同；成功只追加，不执行回调 | 统一运行器必须拒绝非法/重复登记并保持结构化合同状态 | 无偏差 | 不适用 | 否 |
| A0031 | F0031 `自检运行器::运行全部` | 稳定排序后逐项执行；回调异常转失败并继续；不核对返回单元编号/名称与登记身份一致 | 结构化自检结果不得由人读材料裁决 | 身份一致性缺少机械核对，属于合同审计候选 | 统一自检运行器 | 是 |
| A0032 | F0032 `运行入口端口合同自检` | 六轮故障注入和顺序析构存在，但没有 `live_count`、构造/析构配对、跨项引用或共享状态泄漏的机械证据；两项数据库验收只用替身端口；宏开启分支把关闭态哨兵固定为 true；关闭态哨兵失败可使失败数10大于总项数9；正文仍位于生产路由模块 | `8120` 与现行 ENTRY-ONLY 合同要求真实自检保留在独立自检模块、上下文隔离由生命周期证据而非地址证明、日志开关不改变机器结果且宏关闭参数不求值 | 自检模块归属、隔离证据、替身证据边界、结果计数与宏验证差异 | 入口初始化自检模块 | 是 |
| A0033 | F0033 `运行入口隔离合同自检` | 已正确取消历史地址互异判断；两批各顺序执行14项，观察当前地址非零和零初始计数，但无运行时构造/析构配对、live/peak计数或跨项引用扫描；登记失败后仍运行已登记前缀；宏开启分支固定通过关闭态哨兵；哨兵失败可使失败数3大于总项数2；正文仍在生产路由模块 | ENTRY-ONLY v0.3 要求不比较已析构对象地址，隔离由逐项独占生命周期与代码结构证明；自检正文归自检模块；结构化结果计数保持一致；日志宏关闭不求值 | 模块归属、隔离证据边界、既有施工图早退差异、结果计数与宏验证差异 | 入口初始化自检模块 | 是 |
| A0039 | F0039 `普通应用配置::有效` | 只校验方法登记根稳定键和根需求参数；数据库审计开关不作为配置有效性前置 | 普通应用配置的核心准入不应把可选最佳努力审计开关当作无效配置 | 无偏差 | 不适用 | 否 |
| A0040 | F0040 `普通应用上下文::普通应用上下文` | 严格按声明顺序装配26个按值成员，服务只引用更早成员；统计、显示在数据库配置读取前默认构造；不执行生产初始化或SQL往返 | `8120` 要求应用装配集中拥有仓库/服务生命周期，并与系统初始化、审计和宿主分离 | 无偏差 | 不适用 | 否 |
| A0041 | F0041 `系统初始化请求::有效` | 在任何初始化调用前校验方法登记根稳定键和根需求参数 | `8120` 要求系统初始化先拒绝无效请求 | 无偏差 | 不适用 | 否 |
| A0042 | F0042 `系统初始化端口::完整` | 只核对六个回调槽非空，不执行回调 | `8120` 要求系统初始化在使用端口前拒绝缺失能力 | 无偏差 | 不适用 | 否 |
| A0043 | F0043 `方法服务::初始化方法登记根` | 稳定键只保存在服务私有材料；单实例锁和缓存只能提供对象生命周期内幂等。首次创建没有事务或补偿，任一后置失败均可遗留主信息、节点、状态或关系半结构；发布后读回失败也只撤回私有缓存。生命周期仍以共享抽象状态和普通模板关系表达 | `0050` 要求前置通过后的内部写入异常追根因且不得留下可读半结构；`3300/4010` 要求方法稳定身份进入正式结构；`5300` 要求方法生命周期使用专用关系和完整实例状态事实 | 仓库级稳定身份、原子发布/回滚、生命周期模型均存在确认偏差 | 方法登记根与生命周期治理 | 是 |
| A0044 | F0044 `自我线程::启动` | 检查快照、状态和 `joinable` 到创建/赋值线程之间没有统一互斥或 CAS，并发调用可同时通过且竞争非原子线程成员；已有成功快照时不核对当前线程状态、参数或治理失败，可能把已停止/已失败对象报告为成功复用；线程构造失败不恢复已清除标志和治理读数；线程入口异常不由本函数捕获 | `0050/8110/8120` 要求唯一线程启动、结构化生命周期/异常边界和一致失败状态；成功复用必须证明当前可用运行事实 | 并发唯一启动、成功快照伪复用、失败状态一致性和线程异常收口均存在确认偏差 | 自我线程启动治理 | 是 |
| A0045 | F0045 `自我线程::等待初始化完成` | 有界等待后严格要求成功快照；失败、停止和治理失败只终止等待，不会被当作初始化成功。状态写入者不持快照互斥，通知可能延迟到超时复核 | `8120` 要求启动后有界等待并读取结构化初始化结果 | 成功口径无偏差；条件变量同锁协议存在延迟风险但不形成无期限阻塞 | 自我线程等待治理 | 否 |
| A0046 | F0046 `自我线程::读取初始化快照` | 在互斥保护内返回快照副本，不暴露内部可写引用 | `8120` 要求初始化结果以结构化快照交给调用方 | 无偏差 | 不适用 | 否 |
| A0047 | F0047 `自我线程初始化快照::成功` | 严格短路检查三个子结果与非零初始化代次 | `8120` 要求成功快照同时承载语素、世界树、根需求和初始化代次 | 无偏差 | 不适用 | 否 |
| A0048 | F0048 `概念图初始化服务::初始化四个根` | 四根、三个生命周期状态、活跃关系和四个名称逐项公开，顶层没有全包事务或补偿；固定数值键和根/状态登记只驻留服务内存，不能从权威仓库恢复；新服务对象可能重复创建根和名称；名称关系缺失会使机器初始化失败 | `0050/7140` 要求固定系统角色稳定主键进入权威结构、影子登记可重建且不得成为第二身份事实、名称不得证明根身份、失败不得留下可读半结构 | 稳定主键、跨服务恢复、全包原子发布、名称与身份解耦均存在确认偏差 | 概念四根初始化治理 | 是 |
| A0049 | F0049 `概念图初始化结果::成功` | 四个根项的成功合同包含名称语素入口和非空预置名称，因此人读名称缺失会直接否决机器初始化结果 | `7140` 要求名称不得证明固定根身份 | 名称投影参与机器成功裁决，确认偏差 | 概念根身份治理 | 是 |
| A0050 | F0050 `概念图服务::确保存在实例根支持` | 只负责存在节点类型准入并委托 F0191；本函数不把下游失败冒充成功，也不把第一次读取误当成跨步骤事务快照 | `7140` 要求实例支持关系经专用入口建立并读回 | 本函数无独立偏差；稳定根身份和关系发布边界随 F0191 审计 | F0191 | 否 |
| A0051 | F0051 `节点句柄 operator==` | 以仓库编号、节点编号、版本号三字段严格等值比较 | 节点直接身份与稳定句柄规则要求跨仓库、跨编号或跨版本句柄不相等 | 无偏差 | 不适用 | 否 |
| A0052 | F0052 `数据库启动审计端口::完整` | 只检查四个回调槽非空，不执行审计能力 | `8120` 要求数据库审计使用前拒绝缺失端口 | 无偏差 | 不适用 | 否 |
| A0053 | F0053 `统计服务::定义缓存命名空间` | 拒绝无效类型、来源、版本、值类型和失效条件；`可持久化` 仅作为策略字段承载 | 统计缓存命名空间必须由完整结构化身份而非人读名称裁决 | 无偏差 | 不适用 | 否 |
| A0054 | F0054 `统计服务::读取权威结构统计快照` | 节点、关系、索引数量通过三个独立仓库入口读取，实参求值先后不确定且没有共享结构事务令牌；结果可组合不同读取时点 | 结构统计材料可以是只读审计投影，但若声明同点一致快照必须具备统一读取版本或事务证据 | “权威”只证明每项来源，不证明跨仓库同一时点；属于合同命名与一致性偏差候选 | 结构统计审计 | 是 |
| A0055 | F0055 `SQL数据库适配::写入结构统计审计` | ODBC 默认自动提交下执行 INSERT 后才取号和另连接读回；后续失败不能回滚已写行。读回按最新编号而非本次编号，并发时可能读到别的记录；只比编号和三计数，不比来源入口及两个版本；列数读取失败会折叠进 Fetch；来源入口内嵌空字符未拒绝而 `SQL_NTS` 会截断 | 数据库仅是审计投影，但结构化结果必须准确表达部分成功；写后读回应绑定本次写入并覆盖完整记录，错误阶段不得丢失 | 原子性、并发读回、完整字段核对、错误分层和字符串准入均存在确认偏差 | SQL结构统计审计 | 是 |
| A0056 | F0056 `SQL数据库适配::读取最近结构统计审计` | 名为读取的 const 函数每次先调用初始化数据库，可能创建数据库、表和索引；查询与逐行转换失败均归追根因并清空前缀。文本读取把 `SQL_SUCCESS_WITH_INFO` 当成功并用固定 512 字符缓冲，schema 漂移时可能静默截断 | 数据库是非权威审计投影；查询不得返回半记录，物理写副作用和失败分类应在合同中明确；文本截断不得冒充完整成功 | 无半记录行为成立；读取副作用、失败分类边界和截断检测存在确认偏差或漂移风险 | SQL结构统计查询 | 是 |
| A0057 | F0057 `停止信号租约::停止信号租约` | 只形成全部成员为初始态的空租约，不安装信号、不发布全局当前租约，也不取得唯一性 | 租约对象存在不能证明停止信号能力已经安装；安装职责属于 F0019 | 本函数实现与空对象合同一致；相邻安装唯一性风险不归本函数修订 | 不适用 | 否 |
| A0058 | F0058 `接收程序停止信号` | 处理器体内只写 sig_atomic_t，但到达字段前两次读取普通静态裸租约指针；安装、析构和异步回调之间无同步，存在丢信号、值变化或悬空窗口 | 异步信号处理器必须只执行信号安全操作；停止请求是宿主控制材料而非领域机器事实 | 普通裸指针异步访问和双重读取构成确认偏差候选；零日志、零分配方向正确 | 程序停止信号治理 | 是 |
| A0059 | F0059 `世界树初始化服务::读取自我相对坐标` | 只校验节点类型为存在，不验证当前自我或世界树归属；节点和三个槽是四次独立读取，无共享版本；I64 裸槽位转 double 可能丢失大整数精度；多类缺失统一折叠为 nullopt | 自我、世界树归属和坐标结构应由具名身份与一致读取证据承载；前置成立后的结构缺失不得静默当普通候选空 | 身份准入、跨仓库一致性、裸槽结构和错误分类均存在确认偏差候选 | 世界树坐标读取治理 | 是 |
| A0060 | F0060 `生成控制面板世界树结构材料` | 递归生成成功后固定来源版本为1，真实节点投影数量仍为0；只读快照标记不具备节点/关系仓库统一版本；所有失败折叠为默认不可展示材料 | 控制面板投影只做人读展示，但结构计数、来源版本和失败状态必须与实际投影一致 | 投影计数、版本证据、一致性和失败分类存在确认偏差 | 控制面板世界树投影 | 是 |
| A0061 | F0061 `生成控制面板语素树结构材料` | 成功树固定含22节点但真实节点投影数量保持0；循环失败执行 `return {}` 把分类重置为默认世界树；根的名称/类型入口重复；来源版本固定1 | 只读投影 DTO 的分类、节点计数和来源版本必须自洽，不能反向证明机器身份 | 成功计数与窗口校验不一致、失败形状和版本语义存在确认偏差 | 控制面板语素树投影 | 是 |
| A0062 | F0062 `控制面板服务::控制面板服务` | 构造只绑定八个非拥有引用并取得两份树材料和根需求副本，不验证材料状态、来源版本或根需求句柄；宏改变原子计数成员布局；生产调用先深复制两份左值材料再移动 | 构造成功只证明对象成形，不证明投影或根需求有效；借用生命周期和构建宏 ABI 必须由装配合同保证 | 延迟验证设计需正式裁决；现状无直接机器事实写入 | 控制面板装配合同 | 否 |
| A0063 | F0063 `控制面板窗口::控制面板窗口` | make_unique 构造 PImpl；启动统计从调用方到外层形参、内层形参和最终快照经历多次复制；构造不验证统计或投影材料 | 窗口构造只建立 UI 宿主所有权，不证明原生窗口、数据库或投影可用 | 重复复制和延迟验证是实现/合同候选，当前无机器事实偏差 | 控制面板窗口装配 | 否 |
| A0064 | F0064 `控制面板窗口::运行` | 只把可空停止请求原样转给 F0223；本函数不拒绝空指针、不防重复运行、不收口异常 | 窗口运行应结构化表达受控启停和失败；人读 UI 不裁决机器事实 | nullable 停止端口、重复运行与异常完整性需在 F0223 合同裁决 | 控制面板窗口宿主 | 是 |
| A0065 | F0065 `控制面板窗口::~控制面板窗口` | 默认析构只经 unique_ptr 调用 F0224 并释放 PImpl；不直接展开 Win32 清理；外层无清理失败结果 | 拥有资源必须由对应析构函数收口；一图一函数禁止把被调析构内联 | 本函数所有权链正确；资源清理失败边界随 F0224 审计 | F0224 | 否 |
| A0066 | F0066 `运行期上下文租约::有效` | shared_ptr 非空后每次调用唯一身份 F0077 深层完整性检查；未声明 noexcept，也不冻结后续读取版本 | 租约存活与上下文业务完整性是不同事实，布尔谓词不得扩大成稳定快照证明 | 名称、成本、异常和时点语义需在租约合同澄清 | 运行期上下文租约 | 是 |
| A0067 | F0067 `第二次失败安装 lambda` | 记录前三个信号编号并模拟 SIGTERM 失败，但忽略处理器参数；第四次以后调用无证据；不执行真实 CRT 安装 | 自检证据必须明确只覆盖控制流与恢复顺序，不得冒充真实信号投递或处理器接线验证 | 处理器绑定和额外调用覆盖缺口确认 | 入口信号端口自检 | 是 |
| A0068 | F0068 `生产运行期启动请求::有效` | 只检查公开请求版本严格等于1；不验证来源、环境或任何其它启动前置 | true 只代表协议版本匹配，false 是逻辑内版本拒绝 | 本函数无实现偏差；上层不得扩大成功语义 | 不适用 | 否 |
| A0069 | F0069 `运行期上下文宿主::读取租约` | 发布锁内复制当前 shared_ptr 并构造共享租约；空宿主返回空租约，无发布代次或结构化未发布原因 | 租约提供生命周期所有权，不自动证明上下文完整或为最新发布 | 当前锁与所有权顺序正确；状态粒度和代次是合同候选 | 运行期上下文宿主 | 否 |
| A0070 | F0070 `读取生产运行期固定配置` | 生成固定域/仓库、24个稳定键及安全0到1等值；精确常量缺现行规范映射，安全当前值0可能与最新生存控制语义漂移 | 固定配置常量必须由现行正式规范授权，生成器一致性不能自证语义正确 | 安全值与稳定键授权存在高优先规范偏差候选 | 生产运行期固定配置 | 是 |
| A0071 | F0071 `生产运行期配置::有效` | 首先要求与 F0070 完全相等，再复核子结构并比较21×3跨组键；失败原因压成bool，动作键不参与跨组唯一性 | “有效”只代表当前唯一固定配置及局部不变量，不代表支持一般可配置输入 | 布尔粒度、重复验证和动作键不变量存在合同候选 | 生产运行期配置 | 是 |
| A0072 | F0072 `运行期上下文::运行期上下文` | 不拒绝零域、零仓库或零动作键；即使接线为空仍继续构造全部仓库和大型业务装配；构造完成时初始化材料为空 | 构造完成只证明对象成形，不证明已接域或上下文完整；成员依赖必须按声明顺序生存 | 延迟验证、构造成本和异常非结构化收口是合同偏差候选 | 运行期上下文装配 | 是 |
| A0073 | F0073 `运行期上下文::初始化系统角色` | 缓存路径持私有锁复核结构；首次路径解锁后下层提交，再加锁发布缓存；并发不同清单只报内部不一致且不回滚已提交结构 | 锁序、提交发布边界和失败后机器事实必须显式；缓存存在不代表真实结构有效 | 锁序反转、准入 TOCTOU、提交后缓存冲突及下层成功信任边界候选 | 系统角色初始化 | 是 |
| A0074 | F0074 `系统角色初始化结果::成功` | 仅已提交/幂等读回状态才检查清单完整；失败状态短路，不验证失败 DTO 是否携带半结构；bool不区分状态失败与清单不完整 | 成功不等于改变结构；成功谓词不得冒充整个结果 DTO 一致性验证 | 失败半结构约束和失败原因粒度需由上层结果合同裁决 | 系统角色初始化结果 | 是 |
| A0075 | F0075 `运行期上下文::初始化概念活动` | 前置与角色失败折叠为入口拒绝；缓存锁内深层复核；首次下层提交后再发布缓存且冲突不回滚；并发失败不读回成功材料 | 结构事务、缓存发布和失败后机器事实必须显式；缓存材料不应掩盖内部不一致 | 并发重复初始化、锁临界区、提交后冲突和失败粒度均为合同偏差候选 | 概念活动初始化 | 是 |
| A0076 | F0076 `概念活动初始化结果::成功` | 仅已提交/幂等读回且材料完整时成功；不重新读取仓库或复核当前发布状态 | 成功谓词只证明返回 DTO 内部一致，不等于本次改变结构或当前仓库持续一致 | 上层不得扩大布尔成功语义；本函数实现本身自洽 | 不适用 | 否 |
| A0077 | F0077 `运行期上下文::完整` | 两份材料在短路前分锁复制，不是原子快照；多层失败压成false；业务装配完整性在复核包装中重复检查 | 完整只证明本次材料与复核通过，不证明已经发布或返回后持续完整 | 非原子双快照、异常传播、失败粒度和重复验证是合同候选 | 运行期上下文完整性 | 是 |
| A0078 | F0078 `尝试首次发布全新上下文` | 发布前两次完整性检查但外部可继续持有可写 shared_ptr；发布提交不冻结候选内容；锁内失败状态分类变粗 | 发布提交必须冻结共享所有权与可变性边界，指针可见性提交不等于内部结构事务提交 | 外部写权限、检查后漂移、深层锁临界区和无发布代次均为合同候选 | 运行期上下文发布 | 是 |
| A0079 | F0079 `运行期上下文租约::读取` | “读取”会再次执行深层有效性检查，再返回非拥有裸指针；同一调用链可能重复检查且无版本冻结 | 裸指针只在共享所有者寿命内有效，非空不代表持续完整或稳定快照 | 名称/成本、重复检查、裸指针寿命和检查使用竞态是合同候选 | 运行期上下文租约 | 是 |
| A0080 | F0080 `入口初始化S01回调lambda` | 日志宏关闭不求值专用材料，开启时日志bool丢弃；但日志函数异常可阻止结构化结果返回 | 日志开关不得改变自检执行、结果、失败计数或退出码，日志成功不得裁决机器事实 | 宏关闭符合合同；日志异常隔离仍需裁决 | 自检日志隔离 | 是 |
| A0081 | F0081 `入口初始化S02回调lambda` | 始终执行S02并原样返回；宏关闭不求值日志材料，宏开启日志异常可阻断已有结果 | 日志开关不得改变自检结构化收口；闭包值捕获不等于观察回调外部状态深隔离 | 日志异常隔离与观察回调共享状态是合同候选 | 自检日志隔离 | 是 |
| A0082 | F0082 `入口初始化S03回调lambda` | 始终执行S03；日志bool不裁决结果，但宏开启异常仍可能越过结果合同 | 人读通过/失败不承载机器事实，宏关闭必须不求值专用参数 | 日志异常隔离仍未闭合 | 自检日志隔离 | 是 |
| A0083 | F0083 `入口初始化S04回调lambda` | 始终执行S04并原样返回；日志宏语义与S01-S03相同 | 日志旁路不得改变结果、失败计数或执行状态 | 日志异常可能改变宏开关可观察行为 | 自检日志隔离 | 是 |
| A0084 | F0084 `入口初始化S05回调lambda` | S05始终执行；宏关闭不求值日志材料；宏开启日志异常可能吞掉已完成结构化结果 | 两次初始化与结构不变证据属于S05正文，日志不裁决机器事实 | 日志异常隔离和观察回调共享状态候选 | 自检日志隔离 | 是 |
| A0085 | F0085 `入口初始化S06回调lambda` | S06始终执行且结果原样返回；按值配置不深隔离std::function目标 | 实例根机器证据只能由结构化结果承载，日志仅人读 | 日志异常与共享观察器寿命需裁决 | 自检日志隔离 | 是 |
| A0086 | F0086 `入口初始化S07回调lambda` | S07始终执行；宏关路径参数零求值，宏开路径异常仍可能阻断返回 | 多实例隔离属于S07正文，不属于适配lambda | 日志异常隔离未闭合 | 自检日志隔离 | 是 |
| A0087 | F0087 `入口初始化S08回调lambda` | S08始终执行；隔离上下文和测试节点删除属于正文，lambda仅条件日志并返回 | 删除/拒绝证据必须由结构化结果承载，日志不作机器裁决 | 日志异常及观察回调共享状态候选 | 自检日志隔离 | 是 |
| A0088 | F0088 `入口初始化S09回调lambda` | S09自建并收口线程环境；lambda得到结果后线程应已析构，不代表生产线程仍运行 | 线程进入、停止和join证据属于S09正文，日志只做人读展示 | 日志异常可能阻断结果，线程语义不得扩大 | 自检日志隔离 | 是 |
| A0089 | F0089 `入口初始化S10回调lambda` | S10始终执行并原样返回；语素和世界树快照不在lambda展开 | 快照机器事实只能由S10结构化结果与读回证据承载 | 日志异常隔离与观察器共享状态候选 | 自检日志隔离 | 是 |
| A0090 | F0090 `入口初始化S11回调lambda` | S11始终执行；稳定键与特征定义只在正文核对，日志仅总通过/失败 | 索引句柄和失败项必须由结构化结果/验证证据承载 | 日志异常与共享观察器仍是合同候选 | 自检日志隔离 | 是 |
| A0091 | F0091 `入口初始化S12回调lambda` | S12始终执行；安全/服务根需求逐字段读回不进入适配lambda | 人读日志不能证明两组根需求机器事实 | 日志异常可能阻断已完成结果 | 自检日志隔离 | 是 |
| A0092 | F0092 `入口初始化S13回调lambda` | S13始终执行；重复启动、结构不变和线程收口属于正文 | 日志宏关闭不求值专用材料，开启不得影响结构化结果 | 日志异常隔离未闭合 | 自检日志隔离 | 是 |
| A0093 | F0093 `入口初始化S14回调lambda` | S14始终执行；无效键拒绝与线程生命周期只在正文验证 | 日志不得代替零写入、状态未启动和结构计数证据 | 日志异常及观察器共享状态候选 | 自检日志隔离 | 是 |
| A0094 | F0094 `自检登记重复判定lambda` | 只按顺序和编号精确判重，不覆盖名称/回调；返回值不区分冲突键；并发登记无同步 | 自检登记身份和唯一性规则必须明确，地址/迭代器不作稳定身份 | 并发竞态、输入规范化和冲突诊断粒度候选 | 自检运行器登记 | 是 |
| A0095 | F0095 `写入日志` | 无线程/文件锁；good在最终关闭刷新前读取；模块入口内容不转义；硬编码异形日志根且失败可能留下目录 | 日志只做人读诊断，不承载机器事实或持久成功证明；统一路径与宏隔离必须一致 | 并发交错、错误成功语义、路径漂移、无限增长及异常影响自检为确认偏差候选 | 日志系统 | 是 |
| A0096 | F0096 `性能基线无效参数写前拒绝lambda` | 把任意 `构建()==false` 解释为无效规模写前拒绝成功，不携带拒绝原因或零写入读回 | 自检结果应由具名结构化证据区分准入拒绝与内部错误 | 裸 bool 误归因、零写入证据缺失、宏关闭无运行证据 | 性能基线自检 | 是 |
| A0097 | F0097 `运行单规模基线` | 构建失败返回部分报告；下标 1 前置未显式门禁；写入测量后不复核非数量结构；人读结果走 `std::cout` 且污染流格式状态 | 自检机器结果须结构化；人读输出仅经具名可关闭日志宏且不得影响执行结果 | 输出旁路、隐藏下标前置、失败原因折叠、后态验证不足和异常传播 | 性能基线自检与日志隔离 | 是 |
| A0098 | F0098 `性能基线报告结果一致lambda` | 空指标组真空通过，只汇总既有 bool；短路不读取后续项且四类失败折叠 | 结构化自检应保存项目存在性、失败项和可复核证据 | 空集通过、失败定位缺失、汇总 bool 非独立验证 | 性能基线自检 | 是 |
| A0099 | F0099 `规模报告::完整` | 硬编码三规模、固定种子和加 2 魔数；信任五个预计算 bool；指标名称与顺序不检查 | 完整性只能证明合同明确的结构字段，不能把汇总 bool 扩大为底层证据 | 失败定位缺失、魔数、布尔转抄及指标身份验证不足 | 性能基线报告合同 | 是 |
| A0100 | F0100 `生成D455默认采集配置` | 超时 0 仍生成配置；设备序列号为空；固定流能力不经有效性或设备能力校验 | 生成结果是请求材料，真实设备能力必须在采集器准入时裁决 | 默认材料与有效配置语义混淆、配置常量漂移风险 | D455采集配置 | 是 |
| A0101 | F0101 `创建并打开D455相机采集器` | 异常前不重置输出参数；仅信任成功 bool；失败析构新增故障无结果通道 | 打开和清理结果须结构化，日志或旧输出值不能承载机器事实 | 异常输出陈旧、成功复核不足、清理失败不可表达 | D455采集器工厂 | 是 |
| A0102 | F0102 `D455相机采集器::读取状态快照` | 锁内一致复制四字段，但不含停止请求、管线存在性、深度比例等内部状态 | 快照合同只能声明其结构实际承载的字段 | 名称可能被扩大为全量状态；复制异常无结构化结果 | D455采集状态快照 | 是 |
| A0103 | F0103 `D455设备身份有效` | 只验证四字符串非空，不验证格式、字段一致性、设备类型或在线状态 | 外设身份首先是材料；真实设备准入须由发现/打开证据裁决 | “非空”被扩大为“有效设备”的语义风险 | D455身份材料 | 是 |
| A0104 | F0104 `D455采集配置有效` | 只验证超时非零、单流形状和左右索引；不检查序列号、超时上限、跨流兼容与SDK支持 | 静态配置形状不得冒充真实设备可打开性 | bool失败折叠、组合兼容与设备能力延迟裁决 | D455配置合同 | 是 |
| A0105 | F0105 `D455帧材料缓存构造` | 四上限只检查非零；无效配置仍形成正在接收对象；noexcept 标准库构造失败会终止 | 配置准入与对象可用状态应结构化区分 | 跨字段约束缺失、无效状态组合、资源失败不可返回 | D455缓存配置 | 是 |
| A0106 | F0106 `D455采样材料上行桥构造` | 只保存两个非拥有引用，不验证会话配对、生命周期或并发使用合同 | 非拥有装配必须冻结所有者与析构顺序 | 空悬、错误配对、复制别名和线程安全风险 | D455上行桥装配 | 是 |
| A0107 | F0107 `D455采样材料上行桥::上行一次` | 候选没有RAII撤销；异常可遗留材料；发布漂移时撤销旧句柄；元数据等价冒充内容一致；消息尚未入队 | 外部材料须保持请求/候选身份，失败不得留下可读半结构，消息返回不等于投递 | 异常撤销缺口、错误句柄、摘要/同一性语义、并发与幂等未闭合 | D455材料上行与缓存事务 | 是 |
| A0108 | F0108 `D455同步帧材料有效` | 非零元数据和逐平面/外参形状被汇总为一个 bool；不验证四帧同步、来源一致或总字节溢出 | 外设帧首先是材料，结构有效不得扩大为同步可信或内容真实 | 失败定位、同步合同、摘要绑定和字节上限缺失 | D455同步帧材料 | 是 |
| A0109 | F0109 `D455相机内参有效` | 宽高仅非零、主点只有限、焦距只正数、畸变系数只有限 | 标定材料有效性应具备模型、范围、设备与版本绑定 | 数值下限过弱和失败原因折叠 | D455标定内参 | 是 |
| A0110 | F0110 `D455相机外参有效` | 任一旋转分量显著即可通过，不检查正交性、行列式或平移物理范围 | 外参需证明合法刚体变换及端点合同 | 几何校验过弱、端点和单位未裁决 | D455标定外参 | 是 |
| A0111 | F0111 `D455帧材料缓存::按编号版本读取` | 锁内一致读取已发布条目并共享不可变材料；无效句柄统一映射版本不匹配，且不复核材料非空、字节数或年龄 | 缓存读取不得把内部损坏或过期材料冒充有效事实，拒绝证据应可定位 | 失败粒度、内部不变量、过期策略与独占读锁候选 | D455缓存读取合同 | 是 |
| A0112 | F0112 `D455相机采集器::请求停止` | 先锁外置原子标志，再锁内仅转换运行中状态；不停止SDK、不等待采样；与打开并发时原子和状态可能分裂 | 停止请求、状态转换和打开/关闭生命周期需冻结同一状态机与并发合同 | 打开覆盖停止请求、双重事实分裂、无确认与非立即停止 | D455采集器生命周期 | 是 |
| A0113 | F0113 `D455相机采集器::采样一次` | 完成批次数在最终材料有效性前递增；脱锁后使用管线且关闭路径可 reset；复制阶段收到停止仍可能成功 | 外设材料首先是请求材料；采集生命周期、批次提交点和失败零半结构必须一致 | 批次提前提交、管线竞态、停止窗口、重复帧静默忽略及错误折叠 | D455采集事务与生命周期 | 是 |
| A0114 | F0114 `D455帧材料缓存::停止接收` | 只锁内关闭新候选接收门，不排空、不撤销、不等待在途操作 | 接收门、排空和停机完成应使用不同结构化语义 | 名称扩大、无完成证据和单向不可逆状态候选 | D455缓存停止合同 | 是 |
| A0115 | F0115 `D455相机采集器::关闭` | 在状态锁外停止并销毁资源，最后才写状态；重复关闭可覆盖故障 | 外设生命周期必须避免资源与状态分裂，并证明在途调用已静止 | 采样UAF竞态、状态资源分裂、隐藏不变量和故障覆盖 | D455采集器关闭状态机 | 是 |
| A0116 | F0116 `D455帧材料缓存::读取缓存快照` | 锁内一致扫描两类状态，但信任维护中的总字节数且未知枚举静默不计 | 快照字段只能声明实际承载事实，内部守恒需独立验证 | 字节守恒不可自证、状态计数静默缺口和字段语义扩大 | D455缓存快照合同 | 是 |
| A0117 | F0117 `D455相机采集器析构` | 调用关闭后逆序析构；不等待在途使用者，关闭失败事实随对象销毁 | 析构前必须证明外部使用静止，资源清理失败不得伪装为已确认完成 | quiescence缺失、故障不可观察、重复关闭语义和noexcept终止 | D455采集器所有权 | 是 |
| A0118 | F0118 `性能报告固定种子与结构签名谓词` | 只要求固定种子相等和签名非零，all_of可短路且该项可真空通过 | 结构签名事实应绑定规模和可复核期望，不得由非零哨兵扩大 | 非零占位通过、错误签名不可识别和覆盖范围扩大 | 性能报告结构证据 | 是 |
| A0119 | F0119 `性能报告指标数量与吞吐谓词` | 只验证10/4数量和正吞吐；正无穷可通过，空报告组的all_of真空通过 | 性能事实应结构化覆盖指标内容、有限数值与样本/并发证据 | 有限性缺失、真空通过和数量冒充内容 | 性能报告验收 | 是 |
| A0120 | F0120 `方法登记根故障注入lambda` | 先计数再按失败点返回空optional或调用真实服务；空结果不含失败原因 | 故障注入只验证路由，不得冒充真实拒绝证据；引用闭包不得逃逸 | 失败原因折叠、引用生命周期、非原子计数和异常未收口 | 入口端口合同自检 | 是 |
| A0121 | F0121 `自我线程启动故障注入lambda` | 注入默认失败结果的拒绝原因为“无”；真实分支可能启动线程而本闭包不清理 | 结构化失败必须具名且隔离上下文完整收口 | 失败语义不完整、引用逃逸、线程清理依赖外部和异常传播 | 入口端口合同自检 | 是 |
| A0122 | F0122 `等待初始化故障注入lambda` | 注入分支立即false而不等待；真实分支不校验时限并原样传播异常 | 注入等待失败不能扩大为真实超时证据，端口消费必须同步 | 模拟语义差异、时限准入、引用生命周期和计数命名 | 入口端口合同自检 | 是 |
| A0123 | F0123 `读取初始化快照故障注入lambda` | 空optional同时模拟注入故障与真实尚无快照；计数先提交且异常不收口 | 故障注入只能证明上层路由，快照事实须由线程结构化结果承载 | 失败语义合并、引用逃逸、非原子计数和异常传播 | 入口端口合同自检 | 是 |
| A0124 | F0124 `概念图四根故障注入lambda` | 默认失败形状无具名失败项；真实初始化仅最后一轮可达且清理由外部上下文承担 | 四根初始化失败与回滚应由结构化结果和隔离读回证明 | 错误粒度、路由覆盖扩大、引用生命周期和异常收口 | 入口端口合同自检 | 是 |
| A0125 | F0125 `存在根支持故障注入lambda` | 注入空值不校验实例也不触图；真实空值与注入空值类型相同 | 实例根支持写入/拒绝必须由真实服务零写入或读回证据裁决 | 空值折叠、入口未校验、隔离外置和异常逸出 | 入口端口合同自检 | 是 |
| A0126 | F0126 `数据库审计固定命名空间lambda` | 两个版本用位置裸字面量1，固定替身不读取生产版本且消费者漏验持久化 | 命名空间版本和字段应具名冻结并由真实定义验证 | 聚合位置漂移、版本常量缺失和验证字段缺口 | 数据库启动审计端口自检 | 是 |
| A0127 | F0127 `数据库审计零统计快照lambda` | 只回显命名空间并返回全零统计，不读取仓库也无来源/成功标志 | 测试替身材料不得冒充真实结构统计快照 | 零值语义歧义、输入不校验和DTO漂移半结构 | 数据库启动审计端口自检 | 是 |
| A0128 | F0128 `数据库审计写入失败lambda` | 忽略快照/说明并返回无逻辑分类、无追根因、无阶段诊断的默认失败 | 故障注入只能验证失败路由，不能承载真实数据库错误证据 | 错误分类全空、参数未验证和异常未结构化 | 数据库启动审计端口自检 | 是 |
| A0129 | F0129 `数据库审计伪写入成功lambda` | 零数据库写入却返回成功和固定审计编号1，两个参数完全未消费 | 测试替身成功只能制造后续不一致，不得冒充持久化证据 | 伪成功、编号不唯一、参数绑定未验证和默认字段依赖 | 数据库启动审计端口自检 | 是 |
| A0130 | F0130 `数据库审计空记录查询lambda` | 忽略上限并返回查询成功加一条全空记录 | 查询成功必须与材料完整性、上限和错误分类一致 | 成功半结构、上限违约、空诊断和异常传播 | 数据库启动审计端口自检 | 是 |
| A0131 | F0131 `自我线程析构` | 请求停止、唤醒并join，但邮箱结果丢弃且无超时；非拥有邮箱可悬空 | 析构前须证明独占生命周期和邮箱有效，收口不得无限等待或self-join | noexcept终止、无限join、共享邮箱副作用和悬空指针 | 自我线程生命周期 | 是 |
| A0132 | F0132 `入口隔离第一观察回调lambda` | 已取消地址两两不同，但仅验证地址非零和初始计数为零，不记录析构事件/live_count | 隔离须证明每项返回后归零、14次构造析构配对、峰值1和无跨项引用 | 生命周期证据不足、短路后不再观察、异常中断和引用逃逸 | 入口初始化自检隔离 | 是 |
| A0133 | F0133 `入口隔离第二观察回调lambda` | 只递增调用数，完全忽略编号、地址和初始计数 | 14次回调不等于14次完整隔离生命周期 | 观察证据过弱、非原子计数和引用生命周期 | 入口初始化自检隔离 | 是 |
| A0134 | F0134 `自我根需求初始化参数::有效` | 七项标量前置仅返回bool，不验证键存在/类型和数值上界 | 参数形状校验不得扩大为领域键真实有效性 | 失败原因折叠、键真实性延迟和上界缺失 | 根需求初始化准入 | 是 |
| A0135 | F0135 `主信息仓库构造` | 编号0静默归一为1，接线只验全空/字段全有且允许未接域 | 仓库身份和事务域可用性不能由形态构造成功扩大推断 | 编号碰撞、接线语义未验、异常拒绝和未接域能力分叉 | 主信息仓库装配 | 是 |
| A0136 | F0136 `节点仓库构造` | 对零仓库编号静默改写为 1；非拥有主信息引用无寿命令牌；接线一致不比较函数指针 | `4010` 要求零身份在准入前拒绝；跨仓库装配须冻结析构顺序并验证完整回调表一致性 | 空悬引用、事务语义分裂、编号归一和构建宏 ABI 差异 | 节点仓库身份准入与装配 | 是 |
| A0137 | F0137 `关系仓库构造` | 非拥有节点引用、编号归一和接线一致性缺少回调指针比较 | 关系仓库生命周期和事务接线须与节点仓库完整一致 | 空悬引用、回调表漂移、异常拒绝和编号身份碰撞 | 关系仓库装配 | 是 |
| A0138 | F0138 `索引仓库构造` | 非拥有节点引用；接线一致不比较回调表且shared_ptr地址相同不等于同控制块 | 索引仓库装配须证明节点寿命与完整事务接线同一性 | 空悬引用、别名shared_ptr误判、ABI宏分叉和异常拒绝 | 索引仓库装配 | 是 |
| A0139 | F0139 `存在服务三仓库构造` | 只绑定三仓库引用并把接线留空，不验证锚定关系或事务域 | 跨仓库存在写入须具备一致接线和明确生命周期所有者 | 错误混配、无事务接线、悬空引用和复合写原子性缺失 | 存在服务装配 | 是 |
| A0140 | F0140 `场景服务构造` | 仅绑定三仓库引用，不验证编号、事务域或内部锚定关系 | 服务构造成功不得冒充跨仓库装配有效 | 错误交叉装配、悬空引用和配置版本未冻结 | 场景服务装配 | 是 |
| A0141 | F0141 `状态服务三仓库构造` | 绑定三仓库并隐式形成完全未接域的事务接线，不验证仓库同源 | 服务能力模式和事务域装配应显式且可验证 | 误选重载会静默失去事务能力；交叉装配与悬空引用风险 | 状态服务装配 | 是 |
| A0142 | F0142 `动态服务构造` | 仅绑定三仓库引用，不验证内部锚定、事务域或并发合同 | 服务构造成功不得冒充跨仓库动态写入可用 | 错误交叉装配、悬空引用和复合写原子性未冻结 | 动态服务装配 | 是 |
| A0143 | F0143 `二次特征服务构造` | 仅绑定三仓库引用，不验证内部锚定、健康或版本 | 服务构造成功不得冒充二次特征结构可用 | 错误交叉装配、悬空引用和配置版本未冻结 | 二次特征服务装配 | 是 |
| A0144 | F0144 `语素服务构造` | 仅绑定四仓库引用，不验证索引仓库内部节点锚点与参数节点相同 | 语素服务装配应保证仓库链一致 | 交叉装配、悬空引用和延迟失败风险 | 语素服务装配 | 是 |
| A0145 | F0145 `特征值服务构造` | 移动接线并建立锁和两张空侧表，但不验证接线形态或仓库域一致 | 事务接线和仓库域必须在事实写入前一致 | 可构造但不可用、默认空接线及侧表未恢复风险 | 特征值服务装配 | 是 |
| A0146 | F0146 `因果服务构造` | 仅绑定主信息与节点仓库，不验证内部锚点、域或版本 | 因果服务构造成功不得冒充因果结构已建立 | 错误交叉装配与悬空引用风险 | 因果服务装配 | 是 |
| A0147 | F0147 `概念图服务构造` | 建立空缓存和锁但不验证两仓库同源 | 构造成功不得冒充概念图已初始化 | 错误装配与空状态误读风险 | 概念图服务装配 | 是 |
| A0148 | F0148 `概念图初始化服务构造` | 绑定七服务并建立实例锁和空结果，不验证同源 | 初始化依赖链应属于同一应用上下文 | 跨实例并发与错误服务图风险 | 概念图初始化装配 | 是 |
| A0149 | F0149 `特征服务完整构造` | 混用引用和观察指针保存五依赖，不验证仓库链同源 | 完整构造应保证依赖服务一致 | 悬空指针与跨上下文服务图风险 | 特征服务装配 | 是 |
| A0150 | F0150 `世界服务构造` | 保存九个非拥有引用且不验证同源，其中三项当前未消费 | 世界服务装配应最小且来自同一上下文 | 过宽依赖、交叉装配与悬空引用风险 | 世界服务装配 | 是 |
| A0151 | F0151 `世界树初始化服务构造` | 只绑定六项依赖，无共同锁、幂等缓存或装配验证 | 世界树初始化入口应具备唯一上下文和重复调用治理 | 重复初始化、交叉装配与并发风险 | 世界树初始化装配 | 是 |
| A0152 | F0152 `语素初始化服务构造` | 只绑定五项依赖，不验证跨服务仓库链同源 | 显示语素初始化依赖应来自同一上下文 | 交叉装配与悬空引用风险 | 语素初始化装配 | 是 |
| A0153 | F0153 `需求服务三仓库构造` | 默认形成未接域事务接线且不显式标记能力模式 | 需求事务能力应与装配合同一致 | 误选重载静默降级和交叉装配风险 | 需求服务装配 | 是 |
| A0154 | F0154 `需求初始化服务构造` | 只绑定四服务，无初始化锁、幂等状态或同源验证 | 根需求初始化入口应具备唯一上下文和重复调用治理 | 重复初始化、交叉装配与并发风险 | 需求初始化装配 | 是 |
| A0155 | F0155 `自我线程三依赖构造` | 建立未启动未接治理的线程宿主，但不验证三依赖同源 | 线程宿主依赖和治理能力应明确装配 | 生命周期、交叉装配和能力误读风险 | 自我线程装配 | 是 |
| A0156 | F0156 `任务服务三仓库构造` | 默认形成未接域接线，使大量事务任务入口逻辑拒绝 | 任务服务事务能力应与装配合同一致 | 静默能力降级和交叉装配风险 | 任务服务装配 | 是 |
| A0157 | F0157 `方法服务构造` | 每实例自建65把锁、空根材料和零编号，不验证仓库同源 | 方法登记锁域与初始化状态应唯一可辨 | 多实例绕锁、隐式未初始化和交叉装配风险 | 方法服务装配 | 是 |
| A0158 | F0158 `从环境创建配置` | 两字段独立读取，非空即覆盖且不在本函数准入；空值/错误均回默认 | 外部配置材料应可区分来源并在使用前准入 | 混合时点配置、错误语义合并和字段验证延后 | SQL配置入口 | 是 |
| A0159 | F0159 `SQL数据库适配构造` | 任意配置均可构造，字段准入延迟到初始化数据库 | 适配对象构造成功不得冒充配置或连接有效 | 能力状态误读与验证延后 | SQL适配装配 | 是 |
| A0160 | F0160 `方法登记根完整_已加锁` | 锁前置靠命名纪律，四次读回不是同一事务快照 | 登记根完整性应在明确锁序和一致快照下裁决 | 锁序、TOCTOU和布尔原因丢失风险 | 方法登记根审计 | 是 |
| A0161 | F0161 `解锁并报告逻辑错误` | unlock异常会漏报，诊断结果被丢弃且不回滚结构 | 诊断不得承载机器事实，但逻辑错误路径必须可追根因 | 异常漏报、空说明和无回滚边界 | 方法服务逻辑错误收口 | 是 |
| A0162 | F0162 `登记方法` | 两仓库分步提交且不校验返回，节点失败会遗留主信息 | 复合创建不得产生可读半结构 | 非原子写入与孤立主信息风险 | 方法登记写入口 | 是 |
| A0163 | F0163 `句柄有效(节点)` | 仅验证三个字段非零，不验证记录、版本或仓库归属 | 形态判断不得冒充机器事实准入 | 名称语义误读风险 | 句柄调用方合同 | 否 |
| A0164 | F0164 `绑定方法角色状态` | 状态和关系分步写入，无事务、回滚或唯一性门禁 | 前置通过后的复合结构写应原子且失败追根因 | 半结构、重复绑定和失败原因丢失 | 方法角色绑定 | 是 |
| A0165 | F0165 `创建抽象状态` | 创建后分两次读回复核，失败返回空句柄但不清理已创建结构 | 前置通过后的内部复核失败应追根因并避免遗留半结构 | 孤立状态、跨时刻读回和原因丢失 | 状态创建入口 | 是 |
| A0167 | F0167 `创建关系` | 端点校验与写入跨仓库非原子，emplace结果未检查，动态许可目标不可静态唯一化 | 关系发布必须验证写入成功并遵守明确事务边界 | TOCTOU、假成功句柄、动态调用和诊断旁路风险 | 关系仓库写入口 | 是 |
| A0168 | F0168 `句柄有效(关系)` | 仅验证三个字段非零，不验证关系、版本或仓库归属 | 形态判断不得冒充机器事实准入 | 名称语义误读风险 | 句柄调用方合同 | 否 |
| A0169 | F0169 `自我线程拒绝` | 允许构造失败但拒绝原因为“无”的矛盾DTO，重复启动原因兼有成功复用语义 | 结构化结果组合应自洽且消费者可唯一解释 | 结果组合约束和原因歧义 | 自我线程结果合同 | 是 |
| A0170 | F0170 `重置治理读数` | 10次独立原子写最先发布状态，读取者可见部分重置组合 | 治理快照应具备可识别的一致代次或发布边界 | 多字段非原子快照和历史清零风险 | 自我线程治理读数 | 是 |
| A0171 | F0171 `启动lambda` | 裸this不延寿，工作线程异常逸出会terminate且无结构化回执 | 线程入口必须具名收口异常并保持宿主生命周期 | UAF与进程终止风险 | 自我线程工作入口 | 是 |
| A0172 | F0172 `等待初始化完成谓词` | 正在停止即满足，未知状态则等到超时；仅看状态不看快照 | 初始化等待条件应区分成功、终止、未启动和内部不一致 | 完成语义混合和未知状态延迟风险 | 自我线程等待合同 | 是 |
| A0173 | F0173 `语素初始化结果::成功` | 只核对18项各自成功，不验证角色唯一、文本语义、代次或覆盖新增字段 | 显示材料完整性应可证明清单覆盖与角色绑定 | 字段复制误通过和未来新增遗漏风险 | 语素初始化结果 | 是 |
| A0174 | F0174 `世界树初始化结果::成功` | 只核对六个句柄非零且忽略坐标，不读回类型、端点或版本 | 世界树初始化成功应由真实结构读回闭合证明 | 形态成功冒充结构成功 | 世界树初始化结果 | 是 |
| A0175 | F0175 `自我根需求初始化结果::成功` | 只组合两个子结果和一个句柄形态，不验证安全/服务角色互异、目标状态语义、跨仓库一致性或当前可读性 | 根需求初始化成功应证明角色、入口、状态与仓库事实一致 | 自洽但错绑或伪造非零句柄可误通过 | 根需求初始化结果 | 是 |
| A0176 | F0176 `概念图初始化服务::根结构仍可读` | 四字段未绑定固定类别/固定稳定键且未显式互异；分次读取不构成统一快照，最终只核对根总数为4 | 概念四根读回应冻结角色、稳定键、唯一性和同一事实视图 | 错位字段、自洽错误键及并发拼接视图可误通过 | 概念图初始化读回合同 | 是 |
| A0177 | F0177 `概念图初始化服务::确保存在根` | 创建成功而登记失败时无回滚；成功只写三字段并保留可能与新根不一致的旧名称材料 | 根创建/登记应具备失败清理或可恢复证据，输出项不得残留跨根材料 | 孤儿根、重试歧义和旧名称材料串扰风险 | 概念根初始化事务边界 | 是 |
| A0178 | F0178 `概念图初始化服务::确保动态根` | 与存在根模板同样无回滚；前一根成功后才到达，失败会留下已完成前缀 | 四根初始化批次应明确部分完成后的恢复或幂等重试合同 | 孤儿动态根、重复创建和批次半完成风险 | 概念根初始化事务边界 | 是 |
| A0179 | F0179 `概念图初始化服务::确保关系根` | 除无回滚外，不直接验证关系根是空组成二次特征；错误但固定键匹配的登记可复用 | 关系根登记应验证节点领域类型、初始组成与四根互异 | 错类型节点冒充关系根及二次特征污染风险 | 关系概念根合同 | 是 |
| A0180 | F0180 `概念图初始化服务::确保因果根` | 四根链末项失败时前三根可能已发布；本函数只确保根身份，不证明因果能力或关系完成 | 初始化失败须有部分发布恢复合同，因果根成功不得扩大为因果能力完成 | 批次半完成、孤儿因果根和能力误报风险 | 因果概念根合同 | 是 |
| A0181 | F0181 `概念图初始化服务::确保生命周期状态` | 非法阶段可在登记拒绝前创建抽象状态；创建/登记/最终读回任一后续失败均不回滚且输出引用可能已改写 | 状态创建与生命周期登记应先验证阶段并冻结失败后的输出/清理合同 | 孤儿状态、已发布但返回失败和重试歧义 | 生命周期状态登记事务 | 是 |
| A0182 | F0182 `概念图服务::初始化活动概念生命周期` | 异常路径不清理已新增关系；清理结果只诊断且丢弃，关系数量粗粒度后验不能证明精确撤销；无快照仍可成功 | 四根生命周期初始化应具备异常安全、结构化清理证据和明确快照合同 | 部分关系残留、清理失败不可见和成功语义扩大 | 活动生命周期初始化事务 | 是 |
| A0183 | F0183 `概念图初始化服务::确保根名称` | 新建失败仍写项目材料；已有材料路径忽略新名称且不验证文本；空名称指针未拒绝 | 名称入口应先拒绝非法指针，复用须核对文本，失败不得留下半状态 | 半状态、错误名称复用、未定义行为及永久卡住 | 概念根名称绑定合同 | 是 |
| A0184 | F0184 `追根因检查` | 宏包装使文件/行号/条件文本退化为头文件与 `条件`；启用时经 F0342 旁路调试输出和弹窗；关闭宏也不能阻止调用实参求值 | 人读诊断只经可关闭统一日志宏，关闭时不得求值仅供诊断参数且不得改变机器控制 | 根因位置失真、日志旁路、阻塞弹窗和宏关闭副作用 | 容错诊断治理 | 是 |
| A0185 | F0185 `创建存在概念根 lambda` | 裸 `this` 捕获且只转发创建结果；不验证身份、无撤销能力，多初始化服务实例可先并发创建再登记 | 创建回调应保持同步生命周期合同并由外层提供精确幂等/失败清理 | 空悬、重复或孤儿存在根风险 | 概念根创建回调 | 是 |
| A0186 | F0186 `创建动态概念根 lambda` | 与存在根回调相同，只转发动态根句柄；第二根失败时第一根可能已发布 | 四根批次和回调应具备部分完成恢复及创建失败清理 | 裸捕获、孤儿动态根和批次半完成 | 概念根创建回调 | 是 |
| A0187 | F0187 `创建关系概念根 lambda` | 只转发二次特征服务结果，不验证二次特征类型/空组成与关系类别一致性，失败无补偿 | 关系根创建与登记之间应冻结类型后验和孤儿清理 | 错类型登记、孤儿二次特征和批次半完成 | 关系根创建回调 | 是 |
| A0188 | F0188 `创建因果概念根 lambda` | 只转发因果服务句柄，不验证因果引用类型或根例外语义，失败无补偿 | 因果根创建与登记应验证节点类型、根语义并可清理失败结构 | 错类型因果根、孤儿引用和裸捕获风险 | 因果根创建回调 | 是 |
| A0189 | F0189 `概念根初始化项::成功` | 类别用数值区间，稳定键仅非零，句柄/名称只做形态检查且预置名称只非空 | 初始化项成功应绑定固定类别/键、当前根事实和名称文本/关系一致性 | 表面完整材料冒充已闭合根事实 | 概念根结果合同 | 是 |
| A0190 | F0190 `节点仓库::读取节点` | 许可取得目标为运行期函数指针且未唯一解析；未接域路径绕过结构事务许可；五类拒绝均折叠为 `nullopt` | 结构读取应冻结接域/未接域授权与可审计拒绝口径 | 调用目标漂移、兼容路径越权或失败原因丢失 | 节点仓库读取合同 | 是 |
| A0191 | F0191 `概念图服务::确保实例支持对应根` | 写后双向读回无共同快照且失败不回滚；只验证至少包含，不验证唯一/无额外关系 | 关系确保应具备原子发布/失败清理并验证精确双向集合 | 返回空值但关系已存在、并发拼接视图与重复关系 | 实例支持根关系合同 | 是 |
| A0192 | F0192 `统计服务::缓存类型有效` | 显式八项白名单，未知值安全拒绝；新增有效枚举需同步更新 | 现行缓存类型准入以具名白名单为准 | 当前无确认偏差，存在维护漂移门禁 | 缓存类型扩展验证 | 否 |
| A0193 | F0193 `统计服务::来源结构类型有效` | 显式九项白名单，未知值安全拒绝；只验证来源枚举身份 | 现行来源结构类型准入以具名白名单为准 | 当前无确认偏差，新增枚举需同步 | 来源类型扩展验证 | 否 |
| A0194 | F0194 `统计服务::缓存值类型有效` | 显式八项白名单，未知值安全拒绝；不裁决类型组合兼容性 | 现行缓存值类型准入以具名白名单为准 | 当前无确认偏差，新增枚举需同步 | 缓存值类型扩展验证 | 否 |
| A0195 | F0195 `统计服务::缓存失效条件有效` | 显式三项白名单，未知值安全拒绝；不裁决与缓存类型/来源组合 | 现行失效条件准入以具名白名单为准 | 当前无确认偏差，另一个调用方待随其展开补入边 | 失效条件扩展验证 | 否 |
| A0196 | F0196 `统计服务::创建统计快照结构` | 计数值不读仓库且允许任意组合；失败原因折叠为空快照，成功只构造值对象 | 结构统计材料应明确计数事实来源、同一快照边界和空结果原因 | 跨时刻计数被组合或值对象被误报为已发布缓存 | 结构统计快照合同 | 是 |
| A0197 | F0197 `节点仓库::有效节点数量` | 动态许可目标未解析；许可失败与真实零数量同值；未接域/畸形接线均走本地扫描 | 权威计数读取应冻结接域准入和结构化失败，跨仓计数需同一事务语境 | 失败冒充零、兼容降级与跨仓快照不一致 | 结构统计读取合同 | 是 |
| A0198 | F0198 `关系仓库::有效关系数量` | 动态许可目标未解析；许可失败返回0；未接域时端点验证不保证跨仓同一快照 | 关系计数应区分失败并冻结跨仓事务快照，宏内许可生命周期应显式 | 失败冒充空仓、端点竞态和隐藏生命周期边 | 结构统计读取合同 | 是 |
| A0199 | F0199 `索引仓库::有效主键数量` | 动态许可目标未解析；许可失败与真实零数量同值；未接域时先复制索引候选、解锁后再逐节点验证 | 主键统计应区分失败，冻结跨仓一致事务视图并明确同节点多主键计数语义 | 失败冒充零、分时观察和前后向索引不一致不可见 | 结构统计读取合同 | 是 |
| A0200 | F0200 `快照可写入` | 只校验类型、来源类别、非零版本、值型和可持久化；未校验失效条件、当前支持版本或来源稳定身份 | SQL 审计写入前应复核完整命名空间、版本兼容与权威来源身份 | 形态合法但来源或版本不可信的材料可被写入审计库 | SQL 审计准入合同 | 是 |
| A0201 | F0201 `数量可写入` | 正确约束单个 `uint64_t` 到 SQL bigint 正值域；调用方对来源版本和规则版本没有等价上界检查 | 所有写入 bigint 的无符号字段应采用一致的无损转换准入 | 计数字段安全，但相邻版本字段存在不对称溢出风险 | SQL 数值写入边界 | 是 |
| A0202 | F0202 `逻辑内失败` | 不验证阶段/诊断非空；三个布尔分类字段的 DTO 本身允许矛盾组合，调用点自行选择失败类别 | 结构化失败应具备互斥分类和可审计的具名证据 | 空诊断及调用点误分类可形成含糊失败材料 | 数据库失败结果合同 | 是 |
| A0203 | F0203 `SQL数据库适配::初始化数据库` | 配置无效及 master 连接失败归逻辑内返回，建库/审计库/建表失败归追根因；建库与建表依赖拼接 SQL 和运行期幂等检查 | 数据库初始化应冻结失败分类、标识符准入、版本迁移和并发初始化合同 | 分类不一致、并发初始化竞态及未版本化表结构漂移 | SQL 初始化合同 | 是 |
| A0204 | F0204 `ODBC连接::ODBC连接` | 仅建立两个空句柄和未连接状态，不执行资源取得 | RAII 连接对象构造须保持中性，资源取得由显式打开负责 | 当前无确认偏差；构造完成不得误报连接成功 | ODBC 资源生命周期 | 否 |
| A0205 | F0205 `ODBC连接::打开` | 丢弃登录超时设置结果；成功不清空旧诊断；后一驱动 DBC 分配失败可覆盖前轮诊断；带信息成功不保留警告 | 外部连接应结构化保留配置应用和逐候选失败证据，不得把警告静默当纯成功 | 超时未生效、陈旧/丢失诊断和安全策略硬编码 | ODBC 连接合同 | 是 |
| A0206 | F0206 `ODBC连接::句柄` | 不检查连接状态，返回不表达借用期和禁止释放的裸句柄 | ODBC 句柄仅在拥有者存活且连接成功时由内部同步调用消费 | 潜在空句柄、悬空保存或外部误释放 | ODBC 句柄所有权 | 是 |
| A0207 | F0207 `ODBC语句::ODBC语句` | 分配失败仅归一为空句柄，不保留精确诊断；带信息成功丢弃警告 | 语句资源失败应保留可审计 ODBC 诊断，同时维持 RAII 空状态 | 调用方只能得到固定失败说明，证据粒度不足 | ODBC 语句资源合同 | 是 |
| A0208 | F0208 `ODBC语句::可用` | 只比较句柄非空，不能证明连接、准备、权限或执行健康 | 可用性名称只能解释为资源句柄存在，不得扩大成数据库健康 | 调用方或人读材料可能误解成功范围 | ODBC 语句状态合同 | 是 |
| A0209 | F0209 `ODBC语句::句柄` | 返回不表达借用期、非所有权或非空保证的裸可变句柄 | 句柄只应在拥有者存活且可用性准入后同步内部消费 | 空句柄、悬空保存或误释放风险 | ODBC 句柄所有权 | 是 |
| A0210 | F0210 `ODBC成功` | 把带信息成功与纯成功合并，并把各类失败/等待状态压缩为 false | caller 应按具体 ODBC 状态决定正常终止、诊断或同步协议 | 警告丢失及调用方误分类风险 | ODBC 返回码分类 | 是 |
| A0211 | F0211 `读取ODBC诊断` | 空串折叠无记录/首条失败；部分读取失败仍返回前缀；忽略消息长度及截断警告，循环无上限 | ODBC 诊断应保留结构化记录、完整性和截断状态 | 不完整人读文本可被误认为完整证据 | ODBC 诊断合同 | 是 |
| A0212 | F0212 `追根因失败` | 状态组合稳定，但阶段是自由文本、诊断可空，审计编号关联不由函数验证 | 追根因结果应具备稳定故障分类和可验证证据关联 | 多类故障只能靠文本区分，非零编号可被误绑定 | 数据库失败结果合同 | 是 |
| A0213 | F0213 `读取整数列` | 正确保证失败不改输出，但带信息状态丢警告，false 折叠 ODBC/NULL/负值且不验证列元数据 | 外部数据库材料应按字段重新准入并保留结构化拒绝原因 | 列级原因不可区分，版本等正数语义依赖 caller | SQL 列读取合同 | 是 |
| A0214 | F0214 `ODBC语句::~ODBC语句` | 非空句柄仅尝试释放且丢弃返回码，无清理失败证据 | RAII 析构不得伪报释放成功，残余外部资源风险应可诊断 | 清理失败静默且无法重试 | ODBC 语句资源合同 | 是 |
| A0215 | F0215 `ODBC连接::~ODBC连接` | 委托 F0382 后无结果通道；关闭成功性完全依赖 callee 且异常不得逸出 | 析构必须保持不抛出并诚实记录无法证明的外部清理结果 | 清理失败或未来异常可能不可见/终止进程 | ODBC 连接生命周期 | 是 |
| A0216 | F0216 `读取文本列` | 固定单次缓冲、不检查指示长度，带信息状态可把截断文本当成功；false 合并错误与 NULL | 外部文本材料应按长度完整读取并保留结构化拒绝原因 | 未来长文本静默截断、嵌入NUL截短和诊断粒度不足 | SQL 文本读取合同 | 是 |
| A0217 | F0217 `主信息仓库::读取I64值` 索引重载 | 动态许可目标未解析；空 optional 折叠多类拒绝；未接域复制整记录且无事务一致视图 | 通用槽位读取应冻结接域准入、结构化拒绝和字段语义归属 | 失败原因丢失、兼容路径分时读取及不必要快照成本 | 主信息读取合同 | 是 |
| A0218 | F0218 `生成控制面板世界树节点材料` | 多次普通读取拼接树；深度判断允许4097层；无全局预算/去重，显式父链pop缺异常安全，排序平局键不足 | 只读投影应基于一致材料、确定顺序、总量门禁和RAII路径守卫 | 并发拼接、爆炸展开、父链污染及非确定显示 | 控制面板世界树投影 | 是 |
| A0219 | F0219 `生成语素树显示节点材料` | 不自行验证两个显示项；根调用保留重复入口/重复文本，角色由 caller 再覆盖 | 投影 helper 的前置、重复来源语义和根角色覆盖应显式冻结 | 复用时可形成半材料，重复入口可能被误解为集合 | 控制面板语素树投影 | 是 |
| A0220 | F0220 `语素初始化显示项::成功` | 仅检查句柄形态、入口结果最低形态和文本非空；主键、当前存在性、类型及关系一致性不参与 | 显示项成功不得扩大为仓库事实闭合，失败原因应可定位 | 表面材料可被误解为完整初始化，三类失败折叠 | 语素初始化结果合同 | 是 |
| A0221 | F0221 `生成概念名称语素树显示节点材料` | 不自行验证输入；名称基础信息保持空，入口组不去重且无失败通道 | 概念名称投影应冻结调用前置和允许空字段语义 | 复用可产生半材料，投影字段信息不对称 | 控制面板语素树投影 | 是 |
| A0222 | F0222 `控制面板窗口::窗口实现` 构造 | 逐项中性初始化，不调用 Win32/数据库；服务引用和启动快照真实性由 caller 保证 | 窗口实现构造只建立会话容器，不得误报窗口或候选已就绪 | 当前无确认偏差；存在引用寿命与构造分配异常边界 | 控制面板窗口生命周期 | 否 |
| A0223 | F0223 `控制面板窗口::窗口实现::运行` | 忽略显示/更新/销毁等 Win32 返回值；GetMessage错误无系统码；重复运行不重置状态；裸停止指针寿命外置 | 窗口宿主应结构化区分实际显示、清理和系统错误，并冻结单次运行与停止信号合同 | 显示伪证据、旧状态污染、清理不可证及停止指针风险 | 控制面板窗口运行合同 | 是 |
| A0224 | F0224 `控制面板窗口::窗口实现` 析构 | 忽略模块取得和窗口类注销结果，不直接销毁仍存活主窗口，清理成功不可证明 | PImpl 析构应在不抛出前提下闭合全部 Win32 资源并保留失败诊断边界 | 窗口/字体引用残留、类注销失败及静默清理失败 | 控制面板窗口生命周期 | 是 |
| A0226 | F0226 `运行期上下文租约` 构造 | 正确移动同一只读 shared_ptr，不自行验证上下文完整性 | 租约构造只建立共享生命周期，合法性由 `有效()` 动态读取 | 当前无确认偏差；全局不可变仍依赖上游不保留可变别名 | 运行期上下文租约 | 否 |
| A0034 | F0034 `数据库专项失败阶段映射 lambda` | default 折叠多类失败原因 | 正式规范未冻结更细映射 | 语义粒度风险，尚非确认冲突 | 后续入口诊断治理 | 否 |
| A0035 | F0035 `数据库启动审计结果::成功` | 同时要求状态和布尔成员均为往返一致 | 数据库专项成功必须由结构化往返证据承载 | 无偏差 | 不适用 | 否 |
| A0036 | F0036 `记录调试日志` | 只把调试分类与既有调用点材料传给统一日志入口，并把日志写入结果返回给调用方；不写业务结构 | 日志系统规范要求日志只做人读诊断，写入成功不得裁决机器事实 | 本函数无偏差；调用方不得消费返回值作机器裁决 | 日志调用方合同 | 否 |
| A0037 | F0037 `运行关系仓库性能基线自检` | `验收[7]` 固定为 true；`验收[11]` 重复前 11 项总门禁并会再次计入失败数量；逐项结果直接经 `std::cout` 输出 | `0050` 禁止无证据 bool 承载事实；AGENTS 与日志系统规范要求自检人读输出只经具名可关闭日志宏，且不得重复改变结构化失败计数 | 伪验收、重复门禁别名、日志旁路 | 关系仓库性能自检 | 是 |
| A0038 | F0038 `运行D455真实样本自检` | 打开失败及八项逐项结果直接经 `std::cout` 输出；名称组只服务该旁路 | `8120` 第 9 节及 AGENTS 要求 `HY_EGO_DEBUG_LOG_SELF_TEST_D455` 统一日志切片，宏关闭时不求值日志专用材料且不改变自检结果 | 日志旁路；日志专用名称材料缺宏隔离 | D455 真实样本自检 | 是 |

| A0227 | F0227 `生产运行期配置::operator==` | const、noexcept，只读并按 `&&` 短路比较全部当前字段；两个 F0230 调用的相对求值顺序未被源码固定；概念活动参数比较已纠正为显式 defaulted 项目函数 F0460 | `0600` 要求签名、字段职责和节点有正式依据；`8120` 只冻结生产运行期启动请求与结果，没有冻结该配置 DTO 的字段全集等值身份 | 证据不足 | 生产运行期配置合同的正式依据补证；F0460 独立展开；不先改代码 | 否 |
| A0228 | F0228 `系统角色初始化参数::有效` | 写入前检查版本、非负当前值、目标严格大于当前值、21 个稳定键非零且两两不同 | `4010` 第 3、6、9 节要求稳定主键零值拒绝及命名域内唯一；本函数只读参数并返回结构化布尔前置 | 符合 | 保留并由调用方图继续互证版本与数值目标合同 | 否 |
| A0229 | F0229 `概念活动初始化参数::有效` | 写入前检查参数版本及活跃、冷却、退役三个稳定键非零且两两不同 | `4010` 第 3 节要求稳定身份非零；`7140` 第 2、4、10 节冻结三个不同生命周期状态身份及其恢复互证 | 符合 | 保留并绑定概念活动初始化验证 | 否 |

## 逐函数逐节点核规

### F0227 `生产运行期配置::operator==`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S | 本函数内具体指令 | const 借用两侧配置，不转移所有权、不写结构；符合值对象比较的实现事实，但正式规范未冻结此完整签名 | 证据不足 | 生产运行期配置合同补证 |
| N01 / N02 / N03 / N04 / N05 | 本函数内具体指令 | 依次短路比较配置版本、域编号、仓库编号、动作键、系统角色清单版本；节点清楚但字段全集是否构成正式配置身份缺正式依据 | 证据不足 | 生产运行期配置合同补证 |
| C07L / C08R / C07R / C08L | 函数调用 | 两个 F0230 调用均绑定明确对象并按值接收键组；图保留 C++ 未规定的两种操作数求值顺序 | 符合 | 保留；待 F0230 图反向闭合 |
| X09 | 函数调用 | 标准库数组等值比较仅作外部边界，不入项目函数队列 | 符合 | 保留外部边界 |
| N10 / N11 / N12 / N13 / N14 / N15 | 本函数内具体指令 | 短路判断键组和四个数值字段、动态根材料；无结构写入，但字段全集正式依据不足 | 证据不足 | 生产运行期配置合同补证 |
| C16 | 函数调用 | 概念活动初始化参数在项目源码 52 行显式 `= default` 定义，已由 X0171 外部子项纠正为 F0460 项目函数；按四个标量成员顺序比较 | 符合 | 展开 F0460 并保持 F0227 反向边 |
| N17 / RF / RT | 本函数内具体指令 | 把首个不等映射为 `false`、全部相等映射为 `true`；不把布尔值扩大为机器事实或运行成功 | 符合 | 保留并绑定值对象比较验证 |

### F0228 `系统角色初始化参数::有效`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| B01 / B02 / B03 / B04 / B05 | 本函数内具体指令 | 在任何结构写入前检查版本、非负当前值和严格递增目标；调用方仍须证明这些字段的业务目标来源 | 符合 | 保留并由初始化调用方图互证 |
| C06 | 函数调用 | 明确调用 F0230，`this` 与返回键组绑定完整，无候选扩散 | 符合 | 保留；待 F0230 图反向闭合 |
| N07 / X08 / B09 / N12 / X13 / B14 / N18 / N19 | 本函数内具体指令 / 函数调用 | 双层循环索引、数组边界调用和循环推进明确；只读局部键组，不写机器结构 | 符合 | 保留并绑定边界覆盖 |
| X10 / B11 | 函数调用 / 本函数内具体指令 | 读取当前键并在零值时写入前拒绝，符合 `4010` 的零稳定身份拒绝 | 符合 | 保留 |
| X15L / X16R / X15R / X16L / B17 | 函数调用 / 本函数内具体指令 | 每对键各读取一次并拒绝重复；图表达两种合法操作数求值顺序，符合 `4010` 的唯一性要求 | 符合 | 保留 |
| RF / RT | 本函数内具体指令 | 首个无效前置返回 `false`，全部验证通过返回 `true`；全过程无结构变化和半结构 | 符合 | 保留并绑定正负例 |

### F0229 `概念活动初始化参数::有效`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| N01 / N02 | 函数调用 / 本函数内具体指令 | F0231 调用对象、返回数组和局部初始化绑定明确；只读三个生命周期稳定键 | 符合 | 保留；待 F0231 图反向闭合 |
| N03 | 本函数内具体指令 | 写入前拒绝未知参数版本，与恢复和 ABI 版本先准入方向一致 | 符合 | 保留 |
| X04 / N05 / X06 / N07 / X08 / N09 | 函数调用 / 本函数内具体指令 | 逐个读取活跃、冷却、退役键并拒绝零值，符合 `4010` 非零稳定身份合同 | 符合 | 保留 |
| X10L / X10R / X10R2 / X10L2 / N11 | 函数调用 / 本函数内具体指令 | 比较活跃与冷却键，覆盖两种操作数求值顺序并拒绝相同身份 | 符合 | 保留 |
| X12L / X12R / X12R2 / X12L2 / N13 | 函数调用 / 本函数内具体指令 | 比较活跃与退役键，符合 `7140` 三个固定生命周期状态身份分离 | 符合 | 保留 |
| X14L / X14R / X14R2 / X14L2 / N15 | 函数调用 / 本函数内具体指令 | 比较冷却与退役键，完成三身份两两互异验证 | 符合 | 保留 |
| N16 / N17 | 本函数内具体指令 | 前置无效返回 `false`，七项均成立返回 `true`；不写结构、不产生半结构 | 符合 | 保留并绑定版本、零值、重复键正负例 |

| A0230 | F0230 `系统角色初始化参数::读取稳定键组` | 按固定顺序复制 21 个系统角色稳定主键并返回独立数组值；不校验版本、零值或重复 | 各稳定身份分散由对象主题规范规定；`0600` 要求函数签名、字段职责和顺序有正式依据 | 证据不足 | 系统角色初始化参数值投影合同补证 | 否 |
| A0231 | F0231 `概念活动初始化参数::读取稳定键组` | 按活跃、冷却、退役顺序复制三个稳定主键并返回独立数组值 | `7140` 第 2、4、10 节冻结三阶段固定身份及其顺序语义；函数不扩大为生命周期事实裁决 | 符合 | 保留并绑定 F0229 验证 | 否 |
| A0232 | F0232 `结构事务协调器::结构事务协调器` | 零域形成空协调器；非零域在全局锁内分配状态并执行 `下个运行期纪元++`，没有零值、上限或回绕拒绝 | `4040` 第 2、4、6 节要求非零运行期域身份、非零代次且推进不能回绕；`0050` 禁止身份回绕和复用 | 实现偏差 | 结构事务协调器纪元签发合同 | 是 |

### F0230 `系统角色初始化参数::读取稳定键组`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / N00 | 本函数内具体指令 | 只读借用 `this` 并建立独立返回数组；所有权边界明确，但正式规范未冻结此辅助函数签名 | 证据不足 | 系统角色初始化参数值投影合同补证 |
| N01 / N02 / N03 / N04 / N05 / N06 / N07 / N08 | 本函数内具体指令 | 依次投影世界、自我、安全相关稳定主键；无结构写入，字段身份分别受对象规范约束 | 证据不足 | 21 字段—对象规范映射补证 |
| N09 / N10 / N11 / N12 / N13 | 本函数内具体指令 | 依次投影服务相关稳定主键；数组顺序只由当前代码与调用方使用证明 | 证据不足 | 21 字段—对象规范映射补证 |
| N14 / N15 / N16 / N17 | 本函数内具体指令 | 四类概念根身份与 `7140` 一致，但其在 21 项数组中的下标不是正式机器合同 | 证据不足 | 值投影顺序合同补证 |
| N18 / N19 / N20 / N21 | 本函数内具体指令 | 方法登记根和状态身份只读投影；不裁决状态有效性 | 证据不足 | 值投影顺序合同补证 |
| N22 / R | 本函数内具体指令 | 完成 21 元素聚合并按值返回，不保留源引用、不产生半结构 | 符合 | 保留并绑定 F0228/F0227 调用验证 |

### F0231 `概念活动初始化参数::读取稳定键组`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| N01 / N02 | 本函数内具体指令 | 读取活跃状态稳定主键并初始化数组元素 0，对应 `7140` 活跃阶段 | 符合 | 保留 |
| N03 / N04 | 本函数内具体指令 | 读取冷却状态稳定主键并初始化数组元素 1，对应 `7140` 冷却阶段 | 符合 | 保留 |
| N05 / N06 | 本函数内具体指令 | 读取退役状态稳定主键并初始化数组元素 2，对应 `7140` 退役阶段 | 符合 | 保留 |
| N07 | 本函数内具体指令 | 按值返回独立数组，不保留源引用、不把数组本身当作生命周期事实 | 符合 | 保留并绑定 F0229 正负例 |

### F0232 `结构事务协调器::结构事务协调器`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| X01 / N02 / N03 | 函数调用 / 本函数内具体指令 | 先形成空 `shared_ptr`；零域不建立事务状态而完成空协调器。该对象后续由 F0233 拒绝接线，当前节点不发布零域事务身份 | 符合 | 保留并由 F0233 图互证 |
| X04 | 函数调用 | 全局纪元锁由 RAII 获取，异常时不留下未完成对象 | 符合 | 保留并绑定异常注入 |
| X05 / X06 / X07 | 函数调用 | 分配、隐式状态构造、所有权移动及临时清理顺序明确；异常时不发布协调器对象 | 符合 | 保留并绑定分配异常验证 |
| X08 / N09 | 函数调用 / 本函数内具体指令 | 仅在非零参数路径把域编号写入新状态，符合 `4040` 非零运行期域身份方向 | 符合 | 保留 |
| N10 / N11 / X12 / N13 | 本函数内具体指令 / 函数调用 | 在锁内领取并写入纪元，但无上限、零值或回绕拒绝；计数回绕后可形成零纪元或复用身份 | 实现偏差 | 结构事务协调器纪元签发合同 |
| X14N / N15 | 函数调用 / 本函数内具体指令 | 正常路径释放锁后完成持有状态的协调器构造 | 符合 | 保留 |
| X14E / X16 / N17 | 函数调用 / 本函数内具体指令 | 异常路径先释放已取得的锁，再清理已构造成员并传播异常；不产生可读半结构 | 符合 | 保留并绑定异常路径验证 |

| A0233 | F0233 `结构事务协调器::生成接线` | 空状态返回全空接线；非空状态按值返回域、纪元、共享状态与五个无捕获回调绑定 | `4040` 第 2、4 节要求协调层只签发、验证、释放同域读写权并隐藏内部可变能力；五个回调均指向该内部职责 | 符合 | 保留并展开五个 lambda 回调函数 | 否 |
| A0238 | F0238 `运行期业务装配::运行期业务装配` | 按声明顺序构造 36 个项目子对象和稳定动作键，异常时逆序清理已完成前缀 | `8120` 第 5 节要求应用装配拥有仓库、领域服务和初始化服务并保持构造依赖顺序；35 个新发现被调构造尚未逐函数制图核规 | 证据不足 | 完成 F0402—F0436 单函数图后回读本装配合同 | 否 |
| A0239 | F0239 `运行期上下文::结构核心完整` | 核对四项入口条件，取得共享许可后只证明空主信息/节点/关系句柄与零主键不命中；把许可失败、无效候选和内部不一致压为裸 `bool` | `0050`、`4010` 要求核心由节点直接身份、关系、领域类型化记录和索引成立；`4050` 要求前置成立后的内部不一致追根因 | 实现偏差 | 运行期上下文结构核心退役与完整性结果合同；本轮只记录，不形成计划 | 是 |
| A0240 | F0240 `运行期业务装配::完整` | 严格短路检查九个数据操作、概念活动服务、系统角色初始化器、稳定动作键和业务操作组合器 | `8120` 要求装配拥有并提供可用服务边界；12 个新发现探针内部合同尚未逐函数制图核规 | 证据不足 | 完成 F0442—F0453 单函数图后回读装配完整性合同 | 否 |
| A0241 | F0241 `系统角色清单::匹配参数` | 先调用 F0245 核对清单完整和 F0228 核对参数有效，再逐项比较版本、21 个稳定主键、4 个安全/服务值和动态根材料；本地比较只读且确定，但 F0245 的下层完整性仍要求旧主信息句柄有效 | 当前系统角色设计要求同参数幂等读回、异义参数拒绝；`0050`、`4010` 已删除通用主信息身份结构；值式比较不独立承担机器事实 | 证据不足 / 实现偏差 | 保留本地比较；展开 F0245 并把旧主信息完整性条件归入系统角色清单合同；本轮不形成计划 | 是 |
| A0242 | F0242 `运行期业务装配::复核系统角色` | 先重复检查 F0240，true 时调用 F0454 复核，false 时把所有装配失败压成默认入口拒绝；三个 main 可达调用方均已先证明同一装配和清单完整 | `4050` 要求前置成立后的内部异常追根因并保留具名原因；`7130` 要求正式服务读回全部身份、关系和投影 | 实现偏差 / 证据不足 | 运行期装配重复门禁结果合同与 F0454 权威读回范围；本轮只记录，不形成计划 | 是 |
| A0243 | F0243 `运行期业务装配::初始化系统角色` | 先重复检查 F0240，true 时调用 F0455 初始化，false 时返回无具名原因的默认入口拒绝；下层仍用 21 个裸键、旧主信息结构和多阶段写入 | `0050`、`4010` 要求节点直接身份并退出通用主信息；`4050` 要求失败分类；`7130` 要求五个完整稳定主键、统一提交和发布后正式读回 | 实现偏差 / 证据不足 | 薄包装结果分类归装配入口合同；结构和原子性归 F0455 初始化迁移合同；本轮不形成计划 | 是 |
| A0244 | F0244 `系统角色清单::operator==` | 显式 defaulted 比较按版本、主体、两条关系逐项短路，只读且确定；递归主体比较仍把旧主信息句柄纳入身份等值 | 值式相等不替代完整性和权威读回；`0050`、`4010` 已删除通用主信息身份结构 | 实现偏差 | 保留值式比较顺序；旧主信息字段归系统角色清单节点直接切换与退役合同；本轮不形成计划 | 是 |
| A0245 | F0245 `系统角色清单::完整` | 验证旧 v1 值式清单的版本、主体、两条关系、端点与顺序号；主体完整性继续要求旧主信息句柄有效 | `0050`、`4010` 删除通用主信息身份结构；`7130` 要求正式自我身份、关系和投影由正式服务读回 | 实现偏差 / 证据不足 | 系统角色清单节点直接迁移及完整自我聚合合同；本轮只记录，不形成计划 | 是 |
| A0246 | F0246 `运行期上下文::读取系统角色材料` | 在互斥锁内复制 optional 清单缓存并返回独立副本；不验证完整、不形成跨材料原子快照，DTO 仍传播旧主信息句柄 | `0050` 要求缓存只作非权威材料；`7130` 禁止缓存清单替代正式身份和关系读回 | 证据不足 / 实现偏差 | 运行期上下文缓存读取与系统角色清单节点直接切换合同；本轮只记录，不形成计划 | 是 |
| A0247 | F0247 `概念活动材料::匹配参数` | 左到右压缩材料完整、参数有效和参数相等为裸 bool；两个生产调用方把全部 false 无差别映射为幂等冲突 | `4050` 要求前置成立后的内部异常追根因；`7140` 把活动材料定义为可重建非权威投影 | 实现偏差 / 证据不足 | 概念活动缓存幂等结果分类和旧主信息投影退役合同；本轮只记录，不形成计划 | 是 |
| A0248 | F0248 `运行期业务装配::复核概念活动` | 重复调用 F0240 选择下层复核或默认入口拒绝；两个当前可达调用方已经先证明同一装配完整，仍把重复门禁漂移折叠为普通拒绝 | `4050` 要求前置成立后的漂移按内部错误处理；`7140` 要求活动投影可清空并从权威结构确定重建 | 实现偏差 / 证据不足 | 概念活动复核重复门禁与下层缓存复核结果合同；本轮只记录，不形成计划 | 是 |
| A0249 | F0249 `运行期业务装配::初始化概念活动` | 重复调用 F0240 后转发 F0462 或默认入口拒绝；唯一当前可达调用方已经先证明同一装配完整，根函数不持有下层事务或补偿边界 | `4050` 要求入口拒绝与内部错误分型；`0050` / `4010` 要求节点直接身份和正式事务发布 | 实现偏差 / 证据不足 | 概念活动初始化门禁结果原因与 F0462 节点直接事务迁移；本轮只记录，不形成计划 | 是 |
| A0250 | F0250 `概念活动材料::等于` | 确定顺序比较两个非权威活动投影但不验证完整；显式 defaulted 状态角色比较继续递归旧主信息句柄；F0075 将成功结果与缓存不等映射为内部不一致 | `7140` 规定活动材料可清空、可重建且不得替代权威事实；`0050` / `4010` 已删除通用主信息身份结构；`4050` 要求前置成立后的不一致不得降级 | 符合 / 实现偏差 / 证据不足 | 保留值式比较和 F0075 内部不一致分型；旧字段归概念活动投影节点直接迁移；本轮不形成计划 | 是 |
| A0251 | F0251 `概念活动材料::完整` | 验证旧值式 DTO 的版本、重复投影、稳定键、根材料和同仓库编号；状态角色仍要求旧主信息句柄，且 `true` 未读回权威节点、关系、领域记录或生命周期 | `0050` / `4010` 要求节点直接身份和正式关系；`4060` / `7140` 规定活动材料是可清空、可重建且不得裁决权威事实的投影 | 实现偏差 / 证据不足 | 概念活动投影节点直接迁移、来源版本与调用方完整性合同；本轮只记录，不形成计划 | 是 |
| A0252 | F0252 `运行期上下文::读取概念活动材料` | 在互斥锁内深复制 optional 活动缓存；空值和复制异常不改缓存，但副本没有事务代次且 DTO 仍传播旧主信息句柄 | `0050` / `4060` 要求缓存只作非权威材料；`7140` 要求活动投影可清空、可确定重建且不成为上下文权威前置 | 实现偏差 / 证据不足 | 运行期上下文活动缓存读取、空值分型和活动投影退役合同；本轮只记录，不形成计划 | 是 |
| A0253 | F0253 `运行期上下文::服务装配完整` | 无条件转发 F0240 装配探针；候选发布前 false 被 F0078 具名拒绝，true 只证明当前下层探针成立 | `4050` 允许写前候选拒绝；`8120` 装配完整性仍需由 F0442—F0453 下层探针逐函数证明 | 符合 / 证据不足 | 保留薄转发；完成 F0442—F0453 后回读证明范围 | 否 |
| A0254 | F0254 `运行概念图四根创建自检` | 隔离上下文内初始化四根后只核对 F0016 旧成功谓词和四个节点类型；未核对固定系统角色稳定主键、完整句柄、本类结构、生命周期关系或权威读回 | `7140` 要求四根由固定系统角色稳定主键、完整句柄和本类结构共同成立且永久活跃；`8120` 要求 S01 隔离自检 | 实现偏差 / 证据不足 | 概念四根正式身份验收与隔离环境后验；本轮只记录，不形成计划 | 是 |
| A0255 | F0255 `运行概念图四根唯一自检` | 只证明旧根登记投影数量为 4 且四个裸稳定非名称键两两不同；登记、裸键和名称均不能单独证明正式根唯一身份 | `4010` 要求命名域和值组成完整稳定主键；`7140` 明确根登记是非权威定位投影 | 实现偏差 / 证据不足 | 四根完整稳定主键、完整句柄、生命周期和权威结构唯一性验收；本轮只记录，不形成计划 | 是 |
| A0256 | F0256 `运行概念图非名称身份自检` | 新增语素名称入口后核对旧根登记中的完整句柄与裸键未变；方向符合名称不承担根身份，但未核对全部正式身份结构，且 `别名.主键==0` 只是旧 DTO 断言 | `7140` 禁止名称或登记单独证明根身份；`4010` 禁止裸键替代完整稳定主键；`8120` 要求 S03 隔离自检 | 符合 / 实现偏差 / 证据不足 | 非名称身份的正式节点/关系/领域记录读回与隔离后验；本轮只记录，不形成计划 | 是 |

## 函数身份纠偏记录

| 误登记身份 | 合并到 | 直接证据 | 结果 |
| --- | --- | --- | --- |
| F0234 | F0135 | 均定义于 `主信息仓库.cpp:31`；差异仅为声明处默认实参，默认实参不属于 C++ 函数签名 | 删除重复身份；E0435 改指 F0135；不生成第二张图 |
| F0235 | F0136 | 均定义于 `节点仓库.cpp:43`；差异仅为声明处默认实参，默认实参不属于 C++ 函数签名 | 删除重复身份；E0436 改指 F0136；把新格式节点分析归并到 F0136 唯一图 |
| F0236 | F0137 | 均定义于 `关系仓库.cpp:169`；差异仅为声明处默认实参 | 删除重复身份；E0437 改指 F0137；不生成第二张图 |
| F0237 | F0138 | 均定义于 `索引仓库.cpp:92`；差异仅为声明处默认实参 | 删除重复身份；E0438 改指 F0138；不生成第二张图 |
| X0171 中的概念活动参数等值子项 | F0460 | `概念活动状态.数据.h:52` 在项目源码显式写出 `operator==(...) const = default` | X0171 仅保留数组标准库比较；F0227/F0247 改建项目调用边；F0460 入队独立制图 |

### F0233 `结构事务协调器::生成接线`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / N01 | 本函数内具体指令 | 只读绑定协调器状态并在写返回对象前分流空/非空路径 | 符合 | 保留 |
| N02 / N03 / N04 / X05 / N06 / N07 / N08 / N09 / N10 / R0 | 本函数内具体指令 / 函数调用 | 空状态依次形成零域、零纪元、空状态及五个空回调，返回完整全空接线；不产生半接域 | 符合 | 保留并绑定全空形态验证 |
| N11 / N12 / N13 / X14 | 本函数内具体指令 / 函数调用 | 非空路径按值复制域、纪元和共享状态生命周期，不深拷贝内部锁或活动表 | 符合 | 保留；纪元来源偏差归 F0232 |
| N15 / X16 / N17 | 本函数内具体指令 / 函数调用 | 构造 F0397、转换函数指针并绑定共享许可回调；当前不执行许可正文 | 符合 | 展开 F0397 |
| N18 / X19 / N20 | 本函数内具体指令 / 函数调用 | 构造 F0398 并绑定独占许可回调 | 符合 | 展开 F0398 |
| N21 / X22 / N23 | 本函数内具体指令 / 函数调用 | 构造 F0399 并绑定共享路径令牌验证回调 | 符合 | 展开 F0399 |
| N24 / X25 / N26 | 本函数内具体指令 / 函数调用 | 构造 F0400 并绑定独占令牌验证回调 | 符合 | 展开 F0400 |
| N27 / X28 / N29 | 本函数内具体指令 / 函数调用 | 构造 F0401 并绑定撤销失败隔离回调 | 符合 | 展开 F0401 |
| N30 / R1 | 本函数内具体指令 | 八成员全部完成后按值返回接线；无捕获函数指针不依赖闭包临时寿命 | 符合 | 保留并绑定回调接线验证 |

### F0136 `节点仓库::节点仓库`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| N01 | 本函数内具体指令 | 引用成员借用主信息仓库且不取得所有权；调用方必须覆盖其生命周期 | 符合 | 保留并绑定装配析构顺序 |
| N02 / N03 / N04 | 本函数内具体指令 | 零仓库编号被改写为 1，而 `4010` 要求零身份拒绝；可能把无效请求误配到有效仓库域 | 实现偏差 | 节点仓库身份准入 |
| X05 / X06 / N07 / N08 / X09 / X10 / N12 / N11A / X11B | 函数调用 / 本函数内具体指令 | 接线所有权移动、编号初值、锁、节点表及条件自检成员按声明顺序构造 | 符合 | 保留并绑定宏开关构造验证 |
| N13 / N14 | 函数调用 | 两次 F0321 受 `||` 短路顺序约束，分别验证本仓库与主信息仓库接线形态 | 符合 | 保留；待 F0321 图闭合 |
| X15 / N16 | 函数调用 / 本函数内具体指令 | 形态无效时在对象发布前抛出并进入逆序清理 | 符合 | 保留并绑定拒绝验证 |
| N17 / X18 / N19 | 函数调用 / 本函数内具体指令 | F0322 证明同域一致，不一致时在对象发布前抛出 | 符合 | 保留；待 F0322 图闭合 |
| N20 / X26 | 本函数内具体指令 / 函数调用 | 全部前置通过后完成对象；移动后按值参数正常析构 | 符合 | 保留 |
| X21 / X22 / X23 / X24 / N25 | 函数调用 / 本函数内具体指令 | 构造失败按已完成成员逆序清理并传播原异常，不留下节点仓库半对象 | 符合 | 保留并绑定异常注入 |

### F0238 `运行期业务装配::运行期业务装配`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| X01 / X03 / X05 / X07 / X09 / X11 / X13 / X15 / X17 / X19 | 函数调用 | 十次复制结构事务接线供按值构造参数消费；每个副本绑定到唯一后继数据操作构造 | 符合 | 保留外部边界 |
| P01 | 函数调用 | F0145 原位构造特征值服务，参数和结果成员明确 | 符合 | 保留并反向引用 F0145 |
| P02 / P03 / P04 / P05 / P06 / P07 / P08 / P09 / P10 | 函数调用 | 九个数据操作构造均已唯一解析并登记 F0402—F0410，但其内部节点尚未制图核规 | 证据不足 | 展开 F0402—F0410 |
| X02 / X04 / X06 / X08 / X10 / X12 / X14 / X16 / X18 / X20 | 函数调用 | 每个按值接线副本在对应构造调用后结束生命周期；不改变被装配状态 | 符合 | 保留外部边界 |
| P11 / P12 / P13 / P14 / P15 / P16 / P17 / P18 / P19 / P20 / P21 / P22 / P23 / P24 / P25 | 函数调用 | 十五个业务服务构造按依赖前缀顺序唯一绑定为 F0411—F0425，内部合同尚待逐函数核规 | 证据不足 | 展开 F0411—F0425 |
| P26 / P27 / P28 / P29 / P30 / P31 / P32 / P33 / P34 | 函数调用 | 九个组合器构造按依赖顺序唯一绑定为 F0426—F0434 | 证据不足 | 展开 F0426—F0434 |
| P35 | 函数调用 | 系统角色初始化器唯一绑定 F0435，消费十四个已构造引用 | 证据不足 | 展开 F0435 |
| P36 | 函数调用 | 运行期业务操作组合器唯一绑定 F0436，消费八个组合器和四个业务服务引用 | 证据不足 | 展开 F0436 |
| N37 / N38 / N39 | 本函数内具体指令 | 复制稳定动作键、执行空正文并在全部成员完成后发布装配对象；不直接写业务仓库 | 符合 | 保留并绑定 `完整()` 验证 |
| E01 / E02 | 本函数内具体指令 | 任一项目构造异常时逆序销毁已完成成员前缀并传播原异常，不发布半装配对象 | 符合 | 保留并绑定逐前缀异常验证 |

### F0239 `运行期上下文::结构核心完整`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / N01 / P01 / R0 | 本函数内具体指令 / 函数调用 | 零仓库编号和未接域对无效候选属于写前逻辑内拒绝；同一条件发生在已正式发布上下文时需按调用语境追根因 | 符合 / 证据不足 | 调用方输入契约 |
| P02 / N02 / P03 / N03 | 函数调用 / 本函数内具体指令 | 协调器、接线与节点仓库编号应由构造顺序建立恒等关系；成立后漂移不是普通候选失败 | 符合 | 保留并绑定构造身份互证 |
| P04 / P05 / P06 / P07 | 函数调用 | 当前上下文的接线唯一来自自身协调器，故共享许可和验证回调可分别解析为 F0397/F0399；许可无效或验证失败在正式上下文语境为内部异常候选 | 证据不足 | 展开 F0397/F0399 及调用方失败语义 |
| P08 / P09 / N04 | 函数调用 / 本函数内具体指令 | 仍把旧主信息仓库列为“结构核心”，与 `0050` / `4010` 的节点直接身份、关系、领域类型化记录和索引合同不一致 | 实现偏差 | 运行期上下文结构核心退役合同 |
| P10 / P11 / N05 / P12 / P13 / N06 / P14 / P15 / N07 | 函数调用 / 本函数内具体指令 | 只验证三类空句柄和零主键不命中，没有复核非空记录、关系端点、索引正反向或领域记录完整性 | 证据不足 | 结构核心完整性证明范围 |
| N10 / N11 / P16 / R1 / R2 / E0 / E1 / E2 | 本函数内具体指令 / 函数调用 | 所有取得许可后的返回或异常路径均经 F0345 释放；裸 `bool` 混合允许拒绝和内部不一致，异常只原样传播 | 实现偏差 | 结构化完整性结果与内部错误合同 |

### F0240 `运行期业务装配::完整`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S | 本函数内具体指令 | 只读绑定生命周期有效的装配对象，不取得所有权，不写机器结构 | 符合 | 保留 |
| N01—N09 | 函数调用 | 九个数据操作按声明依赖顺序逐项检查，目标已唯一登记为 F0442—F0450；内部接线和执行器探针尚待展开 | 证据不足 | 展开 F0442—F0450 |
| N10 / N11 | 函数调用 | 概念活动服务和系统角色初始化器探针唯一登记为 F0451/F0452；当前只证明下层返回 `true` | 证据不足 | 展开 F0451/F0452 |
| N12 | 本函数内具体指令 | 稳定动作键非零是当前装配内值式准入，不独立成为方法或动作事实 | 符合 | 保留 |
| N13 | 函数调用 | 业务操作组合器完整性唯一登记为 F0453，但当前实现内部恒真，需在 F0453 图中单独核规 | 证据不足 | 展开 F0453 |
| R14 / R15 | 本函数内具体指令 | `&&` 左到右短路、零写入；候选语境 false 为逻辑内返回，已发布完整装配语境则由调用方追根因 | 符合 | 调用方输入契约 |

### F0241 `系统角色清单::匹配参数`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S | 本函数内具体指令 | 只读绑定生命周期有效的当前清单和候选参数，不保存引用、不取得所有权 | 符合 | 保留 |
| C01 | 函数调用 | 先由 F0245 验证缓存清单自身完整；其下层 `系统角色身份材料::完整()` 仍要求旧主信息句柄有效，与 `0050` / `4010` 的节点直接身份合同冲突；通用比较可返回 false，F0073 的已发布缓存语境应把异常清单追根因 | 证据不足 / 实现偏差 | 展开 F0245，并把旧主信息完整性条件归入系统角色清单合同 |
| C02 | 函数调用 | 再由 F0228 验证候选参数；F0073 已在调用前做过同一验证，第二次失败属于调用方契约漂移候选 | 符合 | 保留并由 F0073 语境分账 |
| B03—B24 | 本函数内具体指令 | 清单版本和 21 个稳定主键逐项、确定顺序精确比较；不通过名称或对象类别猜测身份 | 符合 | 保留 |
| B25—B28 | 本函数内具体指令 | 安全/服务当前值与目标值只作值式一致性比较，不在本函数重新结算或改变值 | 符合 | 保留 |
| B29 / RF / RT | 本函数内具体指令 | 动态根材料精确比较；首个失败返回 false，全部成立返回 true；零结构写入且 bool 不独立成为权威事实 | 符合 | 保留；动态根材料值域由 F0228 / 初始化合同承载 |

### F0242 `运行期业务装配::复核系统角色`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S | 本函数内具体指令 | 只读绑定生命周期有效的装配和清单，不取得所有权、不直接写机器结构 | 符合 | 保留 |
| C01 | 函数调用 | 一般候选装配中 F0240=false 可写前拒绝；F0073/F0075/F0077 已先证明同一装配完整，重复门禁再失败时不能降级为普通入口拒绝 | 实现偏差 | 运行期装配重复门禁结果合同 |
| C02 | 函数调用 | F0454 的正常结构化结果原样透传，但其全部权威身份、关系和投影读回尚未展开 | 证据不足 | 展开 F0454 |
| N03 / R04 | 本函数内具体指令 | 默认 DTO 零写入，但只用 `入口拒绝` 同时承载装配首次无效和已成立前置漂移，缺少具名原因 | 实现偏差 | 系统角色复核结果原因合同 |
| E05 | 本函数内具体指令 | F0454 异常原样传播且不形成返回值；公开复核入口的结构化异常合同尚未闭合 | 证据不足 | F0454 资源与错误合同 |

### F0243 `运行期业务装配::初始化系统角色`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S | 本函数内具体指令 | 借用生命周期有效的装配和参数；根函数不暴露仓库、许可、锁或令牌 | 符合 | 保留 |
| C01 | 函数调用 | F0073 已先证明 F0240 为 true；重复门禁发生漂移时当前仍返回默认入口拒绝 | 实现偏差 | 运行期装配初始化入口结果合同 |
| C02 | 函数调用 | F0455 可编排多领域写入，但当前仍消费 21 个裸稳定键、旧主信息结构并在多阶段写后继续返回；与节点直接身份、完整主键和统一发布方向不一致 | 实现偏差 / 证据不足 | 展开 F0455 并归系统角色初始化迁移合同 |
| N03 / R04 | 本函数内具体指令 | 默认 DTO 零写入但缺少装配失败具名原因；真分支结果按值原样传播 | 实现偏差 | 系统角色初始化结果原因合同 |
| E05 | 本函数内具体指令 | F0455 异常不捕获；已经产生的下层变化是否隔离、撤销或拒绝发布尚需逐节点核规 | 证据不足 | F0455 事务、撤销和发布后读回 |

### F0244 `系统角色清单::operator==`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / N01 | 本函数内具体指令 | 显式 defaulted 比较按声明顺序先比较版本；允许任意可构造值比较，不把相等冒充完整 | 符合 | 保留 |
| C02 | 函数调用 | F0456 主体比较是项目显式 defaulted 函数；其递归字段包含旧 `主信息句柄` | 实现偏差 | 展开 F0456；旧字段归系统角色清单节点直接切换 |
| C03 / C04 | 函数调用 | F0457 依次比较两条关系材料，直接调用两次且按前项成功短路 | 证据不足 | 展开 F0457 |
| RF / RT | 本函数内具体指令 | false / true 只表示值对象不等 / 相等；无结构写入且不独立裁决权威事实 | 符合 | 保留 |
| E05 | 本函数内具体指令 | 签名未声明 `noexcept`，成员比较异常原样传播；当前不得把实际无显式 throw 改写成静态无异常合同 | 符合 | 保留 potentially-throwing 边界 |

### F0245 `系统角色清单::完整`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / B01 | 本函数内具体指令 | 只读绑定清单并先验证规则版本；一般候选版本不符可逻辑内返回 | 符合 | 保留 |
| C02 | 函数调用 | F0458 会递归验证主体及旧主信息句柄；与节点直接身份合同存在偏差 | 实现偏差 / 证据不足 | 展开 F0458；归系统角色清单节点直接迁移 |
| C03 / C04 | 函数调用 | F0459 两次验证值式关系句柄、端点、状态和同仓库；不证明关系仍是当前权威事实 | 证据不足 | 展开 F0459 并绑定正式关系读回 |
| B05 / B08 / B09 / B12 | 本函数内具体指令 | 两条关系的类型和顺序号按确定顺序核对；只读且零结构变化 | 符合 | 保留 |
| C06 / C07 / C10 / C11 | 函数调用 | 四次 F0051 精确比较端点句柄，不按名称猜测身份 | 符合 | 保留并反向引用 F0051 |
| RF / RT | 本函数内具体指令 | `false/true` 只表示旧局部谓词失败/成立，不能冒充权威完整自我 | 符合 / 证据不足 | 调用方语境分账和正式服务读回 |

### F0246 `运行期上下文::读取系统角色材料`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S | 本函数内具体指令 | 只读借用生命周期有效的上下文，不取得所有权 | 符合 | 保留 |
| X01 / X03 | 函数调用-外部边界 | X0179 以 RAII 获取并释放缓存互斥锁；同步状态不承担机器事实 | 符合 | 保留并绑定锁异常验证 |
| X02 | 函数调用-外部边界 | 锁内复制 optional 缓存，返回值独立且非权威；DTO 仍传播旧主信息字段 | 符合 / 实现偏差 | 保留值式副本；旧字段归清单迁移 |
| R04 | 本函数内具体指令 | 空或有值均按值返回；不验证完整、不形成跨材料或跨仓库原子快照 | 符合 / 证据不足 | 调用方输入契约与正式服务读回 |
| E05 | 本函数内具体指令 | 锁获取失败原样抛出，不返回半成品且不改变缓存 | 符合 | 保留 |

### F0247 `概念活动材料::匹配参数`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S | 本函数内具体指令 | 只读借用材料和候选参数，不保存引用、不写机器结构 | 符合 | 保留 |
| C01 | 函数调用 | F0251 验证可重建活动投影完整；成功缓存语境再次失败属于内部一致性问题，下层仍含旧主信息字段 | 实现偏差 / 证据不足 | 展开 F0251；归活动投影退役合同 |
| C02 | 函数调用 | F0229 验证候选参数；两个生产调用方均已在进入前验证，重复失败属于前置漂移 | 符合 / 证据不足 | 调用方失败分类 |
| C03 | 函数调用 | F0460 是源码显式 defaulted 项目函数；两个有效参数不等才可映射为幂等冲突 | 符合 | 展开 F0460 |
| RF / RT | 本函数内具体指令 | 当前裸 bool 混合三种失败原因；调用方无差别映射为幂等冲突违反失败分账方向 | 实现偏差 | 概念活动幂等结果分类 |

### F0248 `运行期业务装配::复核概念活动`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S | 本函数内具体指令 | 只读绑定装配、系统角色与活动材料；不取得所有权、不直接写机器结构 | 符合 | 保留 |
| C01 | 函数调用 | F0075、F0077 已先证明同一 F0240 为 true；重复门禁再次为 false 时当前仍走普通入口拒绝 | 实现偏差 | 概念活动复核重复门禁结果合同 |
| C02 | 函数调用 | F0461 的结果原样透传；下层以私有活动缓存参与复核，而 7140 要求投影缺失不改变权威业务裁决 | 实现偏差 / 证据不足 | 展开 F0461 并核对缓存、权威重建和嵌套锁 |
| N03 / R04 | 本函数内具体指令 | 默认 DTO 零写入，但状态只写 `入口拒绝`，不能区分首次装配无效与已成立前置漂移 | 实现偏差 | 复核结果具名原因合同 |
| E05 | 本函数内具体指令 | 下层异常原样传播且根函数不形成半结果；下层是否只读、锁释放及错误分型仍待 F0461 展开 | 证据不足 | F0461 生命周期与错误合同 |

### F0249 `运行期业务装配::初始化概念活动`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S | 本函数内具体指令 | 借用装配、系统角色和参数；不暴露仓库、许可、锁或事务令牌 | 符合 | 保留 |
| C01 | 函数调用 | F0075 已先证明同一 F0240 为 true；重复门禁漂移仍返回默认入口拒绝 | 实现偏差 | 概念活动初始化门禁结果合同 |
| C02 | 函数调用 | F0462 可以进入多步结构写入；根函数只透传结果，不拥有下层事务、撤销和发布后读回 | 证据不足 | 展开 F0462 的事务、发布、缓存和错误合同 |
| N03 / R04 | 本函数内具体指令 | false 分支零写入，但默认 DTO 不具名区分初始无效与内部漂移；true 分支结果原样返回 | 实现偏差 | 初始化结果原因合同 |
| E05 | 本函数内具体指令 | F0462 异常不捕获、不补偿；下层是否完整撤销或隔离必须由 F0462 独立证明 | 证据不足 | F0462 事务异常合同 |

### F0250 `概念活动材料::等于`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / B01 / B02 | 本函数内具体指令 | 只读绑定两个活动投影并先比较材料版本、活动版本；值式比较不写结构 | 符合 | 保留 |
| C03 | 函数调用 | F0460 是项目源码显式 defaulted 参数比较；不把编译器生成实现当作外部黑箱 | 符合 | 展开 F0460 |
| X04 / C05 | 函数调用-外部边界 / 函数调用 | X0180 编排固定数组比较，元素回调是项目显式 defaulted F0463；F0463 仍比较旧主信息句柄 | 实现偏差 / 证据不足 | 展开 F0463；旧字段归概念活动投影迁移 |
| C06 | 函数调用 | F0464 比较重建视图；活动投影允许值式比较，但不能取得权威身份 | 符合 / 证据不足 | 展开 F0464 并绑定 7140 投影边界 |
| N07 / X08 / B09 / X10 / C11 / N12 | 本函数内具体指令 / 函数调用 | 以 X0181 确定遍历四个根并调用 F0465；F0465 还要求两侧签名分别规范化，非普通逐字段相等 | 符合 / 证据不足 | 展开 F0465 |
| RF / RT | 本函数内具体指令 | `false/true` 只表示本地比较结果；本函数未调用 `完整()`，相同无效材料也可能相等 | 符合 / 证据不足 | 调用方不得扩大为完整或权威证明 |
| E13 | 本函数内具体指令 | 根函数 `noexcept`；潜在下层异常会终止而非形成 bool，当前接口没有结构化异常结果 | 证据不足 | 下层比较函数异常合同 |

### F0251 `概念活动材料::完整`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / B01 / B02 | 本函数内具体指令 | 只读绑定一个活动投影，先验证旧材料版本和非零活动代次；活动代次只属于投影，不是概念身份或生命周期版本 | 符合 / 证据不足 | 保留局部判断；调用方不得扩大权威性 |
| C03 | 函数调用 | F0229 验证三个旧裸稳定键非零互异；这些键不是 4010 的完整节点稳定主键 | 实现偏差 | 概念活动参数节点直接身份迁移 |
| C04 | 函数调用 | F0466 验证重建视图内部结构，但视图是 7140 明确的非权威投影 | 证据不足 | 展开 F0466 并绑定投影边界 |
| X05 / C06 | 函数调用-外部边界 / 函数调用 | X0182 比较重复状态角色数组并回调显式 defaulted F0463；F0463 仍比较旧主信息句柄 | 实现偏差 / 证据不足 | 展开 F0463；旧字段归活动投影迁移 |
| C07 / N08 / X09 / B10 / X11 / B12 / N13 | 函数调用 / 本函数内具体指令 | F0231 与 X0182/X0183 确定比较三项裸稳定键；过程只证明 DTO 内部重复字段一致 | 符合 / 实现偏差 | 保留确定遍历；稳定键身份迁移归参数合同 |
| N14 / X15 / B16 / C17 / N18 | 本函数内具体指令 / 函数调用 | X0184 遍历四个根并调用 F0465；F0465 还执行根签名规范化，未读权威当前关系 | 证据不足 | 展开 F0465；绑定正式根身份与关系读回 |
| X19 / N20 / X21 / C22 / B23 | 函数调用-外部边界 / 函数调用 / 本函数内具体指令 | 取状态角色0仓库为基准并调用 F0467；F0467 继续要求旧主信息句柄有效，同仓库只证明数值一致 | 实现偏差 / 证据不足 | 展开 F0467；归状态角色节点直接迁移 |
| X24 / B25 / B26 | 函数调用-外部边界 / 本函数内具体指令 | 四个根的节点句柄和活跃关系句柄只比较仓库编号，不验证完整句柄、版本、端点或关系当前状态 | 证据不足 | 概念活动权威读回范围 |
| RF / RT | 本函数内具体指令 | 一般候选 false 可作局部拒绝；候选组装后、提交后或成功结果语境的 false 属内部不一致；true 只证明旧 DTO 局部谓词 | 符合 / 实现偏差 | 调用方输入契约与失败分型 |
| E27 | 本函数内具体指令 | 根函数 `noexcept` 调用未显式 `noexcept` 的 F0463；异常会终止而不形成结构化结果 | 证据不足 | F0463 静态异常合同 |

### F0252 `运行期上下文::读取概念活动材料`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S | 本函数内具体指令 | 只读借用生命周期有效的上下文，不取得所有权 | 符合 | 保留 |
| X01 / X03 | 函数调用-外部边界 | X0185 以 RAII 获取并释放活动缓存互斥锁；同步状态不承担机器事实 | 符合 | 保留并绑定锁异常验证 |
| X02 | 函数调用-外部边界 | 锁内深复制 optional 活动缓存；DTO 递归含 vector，复制可抛出且仍传播旧主信息字段 | 符合 / 实现偏差 | 保留值式副本；旧字段归活动投影迁移 |
| R04 | 本函数内具体指令 | 空或有值均按值返回；不验证完整、不形成跨服务、跨缓存或跨仓库同代次快照 | 符合 / 证据不足 | 调用方输入契约与正式权威读回 |
| E05 / E06 | 本函数内具体指令 | 锁失败或深复制失败原样传播；异常展开释放锁，不返回半副本且不改变缓存 | 符合 | 保留 |
| F0077 调用语境 | 调用方合同 | 候选发布前空值可拒绝；已发布上下文空值应追根因，但本函数无强类型分型 | 证据不足 | 上下文完整性结果合同 |
| F0077 后续裁决 | 调用方合同 | 把可清空活动投影存在列为上下文完整和租约有效前置，与 7140 的非权威投影边界冲突 | 实现偏差 | 运行期上下文完整性与活动投影分账 |

### F0253 `运行期上下文::服务装配完整`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S | 本函数内具体指令 | 只读借用上下文及值成员装配，不取得所有权、不写机器结构 | 符合 | 保留 |
| C01 | 函数调用 | 唯一转发 F0240；下层十二项装配探针 F0442—F0453 尚待展开，因此 true 的证明范围仍不完整 | 证据不足 | 展开 F0442—F0453 |
| RF / RT | 本函数内具体指令 | 当前唯一可达调用方 F0078 在发布前把 false 映射为 `缺少领域服务装配`；true 继续候选完整性复核 | 符合 | 保留写前拒绝 |
| E02 | 本函数内具体指令 | 根函数和 F0240 均为 `noexcept`；违反保证时终止，不形成 bool | 符合 | 保留无异常合同 |

### F0254 `运行概念图四根创建自检`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / C01 / C02 / B03 | 本函数内具体指令 / 函数调用 | S01 只从显式完整自检路由进入；F0468/F0469 形成局部独占环境并决定是否继续 | 符合 / 证据不足 | 展开 F0468/F0469；核对隔离环境初始与析构后验 |
| X04 / N05—N10 | 本函数内具体指令 | 解引用独占上下文并构造六个具名端口闭包；闭包引用不越过同步 F0015 调用和局部端口生命周期 | 符合 | 展开 F0470—F0475 |
| C11 | 函数调用 | F0015 按 8120 固定系统初始化顺序同步调用六端口；其旧初始化合同仍含名称材料和旧结构 | 符合 / 实现偏差 | 保留调用顺序；旧结构归既有迁移治理 |
| N12 / C13—C17 / N18 | 本函数内具体指令 / 函数调用 | F0476 只读四根节点并核对类型 3/10/13/11；类型映射正确，但 F0016 成功和节点类型不能证明 7140 完整根身份 | 符合 / 证据不足 | 展开 F0476；补固定角色主键、完整句柄、本类结构和生命周期读回 |
| B19 / RT / RF | 本函数内具体指令 | 普通失败和强制失败均压缩为一项自检失败；故障注入不承担机器事实 | 符合 / 证据不足 | 保留自检结果计数；具体失败原因另行增强 |
| D20 / C21 / D22 / E21 | 本函数内具体指令 / 函数调用 | 正常及异常路径均依赖 RAII 销毁隔离环境；显式 F0131 可达，但本函数未验证 live-count 与跨项引用清零 | 证据不足 | 隔离环境析构后验 |

### F0255 `运行概念图四根唯一自检`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / C01 / C02 / B03 | 本函数内具体指令 / 函数调用 | S02 仅显式完整自检可达；共享隔离环境合同与 F0254 相同 | 符合 / 证据不足 | 展开 F0468/F0469 |
| X04 / N05—N10 / C11 | 本函数内具体指令 / 函数调用 | 六个独立端口闭包经 F0015 初始化隔离上下文；回调身份不得与其它自检函数合并 | 符合 | 展开 F0477—F0482 |
| N12 / C13 / C14 / B15 | 本函数内具体指令 / 函数调用 | F0016 成功后读取 F0355 旧根登记投影并要求数量 4；7140 明确登记数量不能证明根身份 | 实现偏差 / 证据不足 | 根身份权威读回 |
| B16—B21 / N22 | 本函数内具体指令 | 六对裸 `uint64_t` 键比较只能证明局部投影字段不同；未核对命名域、完整稳定主键和完整句柄 | 实现偏差 | 四根正式唯一性验收 |
| B23 / RT / RF | 本函数内具体指令 | 自检失败与故障注入均形成 1/1；异常不形成结构化结果 | 符合 | 保留；具名失败原因可后续增强 |
| D24 / C25 / D26 / E25 | 本函数内具体指令 / 函数调用 | 隔离上下文最终触发 F0131 析构，但未读回析构后资源计数 | 证据不足 | 隔离后验 |

### F0256 `运行概念图非名称身份自检`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / C01 / C02 / B03 | 本函数内具体指令 / 函数调用 | S03 仅显式完整自检可达；局部环境不发布全局结构 | 符合 / 证据不足 | 展开 F0468/F0469 |
| X04 / N05—N10 / C11 | 本函数内具体指令 / 函数调用 | 六个 F0483—F0488 闭包经 F0015 同步初始化；绑定和生命周期闭合 | 符合 | 展开六个闭包 |
| C12—N14 | 函数调用 / 本函数内具体指令 | 第一次 F0016 成功才调用 F0364 创建名称入口；失败分支默认构造空结果且不伪造调用 | 符合 / 证据不足 | 展开 F0364 的语素与关系发布边界 |
| C15 | 函数调用 | 无论初始化/别名结果均读取 F0352 旧存在根登记；初始化失败时仍读取是当前代码事实 | 符合 / 实现偏差 | 登记投影与权威根读回分账 |
| C16—B21 / N22 | 函数调用 / 本函数内具体指令 | 比较别名成功、旧主键 0、登记有值、完整节点句柄相等和裸键相等；只证明当前旧接口局部行为 | 符合 / 实现偏差 / 证据不足 | 非名称正式身份全结构验收 |
| B23 / RT / RF | 本函数内具体指令 | 强制失败只作测试控制；所有普通失败压成一项，异常向上层传播 | 符合 / 证据不足 | 保留计数并补具名原因 |
| D24 / C25 / D26 / E25 | 本函数内具体指令 / 函数调用 | 环境销毁显式可达 F0131；未验证别名关系、语素材料和全部资源在项间清零 | 证据不足 | 隔离后验和别名全结构读回 |

### F0257 `运行概念图根名绑定自检`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / C01 / C02 / B03 | 本函数内具体指令 / 函数调用 | S04 仅从显式完整自检路由进入；F0468/F0469 形成局部环境并决定是否继续 | 符合 / 证据不足 | 展开 F0468/F0469；核对隔离环境后验 |
| X04 / N05—N10 / C11 | 本函数内具体指令 / 函数调用 | 六个 F0489—F0494 端口闭包只借用局部上下文，并由 F0015 同步消费；不得把闭包构造登记为 F0257 直接调用底层业务函数 | 符合 | 展开六个端口闭包 |
| N12 / C13 / B14—B17 | 本函数内具体指令 / 函数调用 | F0016 成功后比较四个旧预置名称；名称只作当前投影材料，不取得根身份裁决权 | 符合 / 实现偏差 | 根身份权威结构读回 |
| C18—C21 / N22 | 函数调用 / 本函数内具体指令 | F0495 经 F0496/F0497 读取两类关系投影并用两次 `std::find` 验证成员存在；没有核对所属词、词性、入口类型、路由版本、来源、当前性、索引和集合纯度 | 证据不足 | 完整语素入口合同验收 |
| B23 / RT / RF | 本函数内具体指令 | 普通失败与故障注入均压缩为一项失败；F0016 成功后的名称或关系缺失未保留具名内部不一致类别 | 符合 / 证据不足 | 自检失败分型 |
| D24 / C25 / D26 / E25 | 本函数内具体指令 / 函数调用 | 正常及异常路径依赖 RAII 销毁隔离环境并可达 F0131，但本函数未读回析构后资源清零 | 证据不足 | 隔离环境析构后验 |

### F0258 `运行概念图重复初始化结构不变自检`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / C01 / C02 / B03 | 本函数内具体指令 / 函数调用 | S05 仅显式完整自检可达；共享隔离环境合同与前项相同 | 符合 / 证据不足 | 展开 F0468/F0469 |
| X04 / N05—N10 / C11 | 本函数内具体指令 / 函数调用 | 六个 F0498—F0503 闭包经 F0015 完成首轮初始化，回调身份和借用生命周期闭合 | 符合 | 展开六个端口闭包 |
| C12—C15 | 函数调用 | 首轮返回后无条件读取三项计数并直接调用第二轮 F0048；第二轮在检查首次成功前执行，不能并入 F0015 回调或宣称首轮失败即停止 | 符合 / 实现偏差 | 重复初始化前置与失败路径设计 |
| C16—C21 | 函数调用 | 两轮成功后比较四个完整节点句柄；句柄不变是必要证据，但不能证明正式主键、关系、版本、历史和生命周期事实均未替换 | 证据不足 | 四根完整身份读回 |
| C22—C26 / B23 / B25 / B27 / N28 | 函数调用 / 本函数内具体指令 | 节点、关系、索引总量不变不证明具体关系身份、索引映射或历史记录不变 | 证据不足 | 幂等结构明细审计 |
| B29 / RT / RF / D30—D32 | 本函数内具体指令 / 函数调用 | 所有结构不一致压成普通自检失败；RAII 可达 F0131，但无析构后资源读回 | 证据不足 | 具名失败分型与隔离后验 |

### F0259 `运行自我存在实例根支持自检`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / C01 / C02 / B03 | 本函数内具体指令 / 函数调用 | S06 仅显式完整自检可达；局部环境不发布生产运行期结构 | 符合 / 证据不足 | 展开 F0468/F0469 |
| X04 / N05—N10 / C11 | 本函数内具体指令 / 函数调用 | 六个 F0504—F0509 端口闭包经 F0015 同步初始化，闭包借用不逃逸 | 符合 | 展开六个端口闭包 |
| C12 / C13 / N16 | 函数调用 / 本函数内具体指令 | F0016 成功后，`optional<节点句柄>` 相等在两侧有值时调用 F0051；该比较重复使用初始化结果材料，不是独立权威读回 | 证据不足 | 自我与存在根完整身份读回 |
| C14 / B15 / N16 | 函数调用 / 本函数内具体指令 | F0371 只读支持概念组并只检查数量等于 1；未验证唯一元素就是存在根，也未读回关系 9 的源、目标、类型、顺序、当前性、版本和历史 | 实现偏差 / 证据不足 | 完整关系 9 验收 |
| B18 / RT / RF | 本函数内具体指令 | 普通失败与故障注入均压成一项失败；成功前置后的结构不一致未具名 | 符合 / 证据不足 | 自检失败分型 |
| D17 / D19 / C20 / D21 / E22 | 本函数内具体指令 / 函数调用 | 正常和异常路径依赖 RAII 并可达 F0131；未验证关系候选回滚和析构后资源清零 | 证据不足 | 事务回滚与隔离后验 |

### F0260 `运行多存在实例根支持自检`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / C01 / C02 / B03 | 本函数内具体指令 / 函数调用 | S07 仅显式完整自检可达；局部隔离环境不发布生产运行期结构 | 符合 / 证据不足 | 展开 F0468/F0469；核对隔离环境后验 |
| X04 / N05—N10 / C11 | 本函数内具体指令 / 函数调用 | 六个 F0510—F0515 端口闭包经 F0015 同步初始化，闭包借用不逃逸 | 符合 | 展开六个端口闭包 |
| C12 / C13 | 函数调用 | F0516 仍创建通用主信息再创建存在节点，随后 F0050 尝试形成支持关系；且两调用不以 F0015 成功为前置 | 实现偏差 / 证据不足 | 节点直接身份迁移；初始化失败后的写入与撤销合同 |
| C14 / X15 / C16 | 函数调用 / 本函数内具体指令 | F0016 成功后用 optional 元素比较回调 F0051 验证支持候选等于存在根；该结果仍来自同一初始化 DTO 和调用返回，不是独立权威读回 | 证据不足 | 关系 9 完整写后审计 |
| C17 / B18 / N19 | 函数调用 / 本函数内具体指令 | F0372 的反向实例组只检查数量为 2，未验证集合恰含自我实例和第二实例，也未核对成员纯度、关系版本或历史 | 实现偏差 / 证据不足 | 多实例具名成员与关系明细验收 |
| B20 / RT / RF / D21—D23 / E22 | 本函数内具体指令 / 函数调用 | 普通失败与故障注入压成一项；RAII 可达 F0131，但未验证创建/支持候选回滚和析构后资源清零 | 证据不足 | 失败分型、事务回滚与隔离后验 |

### F0261 `运行存在根支持拒绝自检`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / C01 / C02 / B03 | 本函数内具体指令 / 函数调用 | S08 仅显式完整自检可达；共享隔离环境合同与前项相同 | 符合 / 证据不足 | 展开 F0468/F0469 |
| X04 / N05—N10 / C11 | 本函数内具体指令 / 函数调用 | 六个 F0517—F0522 端口闭包经 F0015 同步初始化；回调身份和借用生命周期闭合 | 符合 | 展开六个端口闭包 |
| C12 / C13 | 函数调用 | F0516 旧式创建后直接调用 F0523 节点仓删除，绕过存在服务领域删除和关系/索引/审计编排 | 实现偏差 | 存在删除领域入口与节点直接结构迁移 |
| C14—C19 | 函数调用 | 三项“前计数”在创建和删除之后才取得；三类 F0050 拒绝在检查 F0016 成功前全部执行 | 符合 / 实现偏差 / 证据不足 | 拒绝调用前置；创建—删除全程审计 |
| C20 / B21—B24 | 函数调用 / 本函数内具体指令 | 初始化成功、删除成功和三个空 optional 只证明返回面；未逐项证明错误请求零关系、零索引、零历史写入 | 证据不足 | 三类拒绝零写入读回 |
| C25—C29 / B26—B30 / N31 | 函数调用 / 本函数内具体指令 | 节点、关系、索引总量相等不能证明具体身份、版本、失效记录、映射和历史不变 | 证据不足 | 拒绝后结构明细与历史审计 |
| B32 / RT / RF / D33—D35 / E34 | 本函数内具体指令 / 函数调用 | 所有拒绝与结构差异压成一项失败；RAII 可达 F0131 但无析构后验 | 证据不足 | 具名失败分型与隔离后验 |

### F0262 `运行自我线程进入与初始化自检`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / C01 / C02 / B03 | 本函数内具体指令 / 函数调用 | S09 仅显式完整自检可达；局部环境不发布生产运行期结构 | 符合 / 证据不足 | 展开 F0468/F0469 |
| X04 / N05—N10 / C11 | 本函数内具体指令 / 函数调用 | 六个 F0524—F0529 端口闭包经 F0015 同步初始化，闭包借用不逃逸 | 符合 | 展开六个端口闭包 |
| C12 / C13 / C14 / B15 | 函数调用 / 本函数内具体指令 | F0016 成功后分别读取 F0530 进入位和 F0531 状态；两个独立原子读取不形成同一版本快照 | 符合 / 证据不足 | 线程状态同代次观察合同 |
| C16—C20 / B17—B21 / N22 | 函数调用 / 本函数内具体指令 | 三类总量只需大于环境初始值，未核对新增节点、关系、索引的具名身份、互相对应、版本或发布代次 | 证据不足 | 自我初始化完整结构读回 |
| B23 / RT / RF | 本函数内具体指令 | 线程或结构差异与故障注入均压成一项失败；F0016 成功后的不一致未具名 | 符合 / 证据不足 | 自检失败分型 |
| D24 / C25 / D26 / E25 | 本函数内具体指令 / 函数调用 | RAII 可达 F0131，但未验证线程停止、成员释放和隔离结构清零的独立后验 | 证据不足 | 线程与隔离环境析构后验 |
| 完成声明边界 | 规范核对 | “已进入 + 运行中 + 三类总量增加”不能证明完整自我结构、觉醒、连续治理循环、需求—任务—方法闭环或恢复后续跑 | 符合 / 禁止扩大 | 保持窄声明 |

### F0263 `运行语素与世界树快照读回自检`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / C01 / C02 / B03 | 本函数内具体指令 / 函数调用 | S10 只从完整自检路由进入；F0468/F0469 形成局部隔离环境并决定是否继续 | 符合 / 证据不足 | 展开 F0468/F0469；核对隔离环境初始与析构后验 |
| X04 / N05—N10 / C11 | 本函数内具体指令 / 函数调用 | 六个 F0532—F0537 端口闭包只借用局部上下文，由 F0015 同步消费且不逃逸 | 符合 | 展开六个端口闭包 |
| X12 | 本函数内具体指令 | 第 345 行在 F0016 成功检查前调用 `自我初始化.value()`；空值抛 `bad_optional_access`，不能形成 S10 的结构化失败 | 实现偏差 | 初始化结果 optional 前置与自检失败收口 |
| C13—C18 / B17 | 函数调用 / 本函数内具体指令 | F0016 成功后才读取 F0059，但快照引用已提前取得；F0059 仍消费旧主信息坐标槽并转为 double | 实现偏差 / 证据不足 | optional 前置；世界树坐标类型化记录迁移 |
| B19—B27 / C23 / N28 | 函数调用 / 本函数内具体指令 | 仅精确比较四个语素文本、六句柄成功谓词和三个零坐标；未覆盖 18 个语素文本、完整世界节点类型和正式关系读回 | 证据不足 | 语素与世界树完整权威读回自检 |
| B30 / RT / RF / D31—D33 / E34 | 本函数内具体指令 / 函数调用 | 普通失败与故障注入压成一项；正常或异常退出均依赖 RAII 可达 F0131，但未读回资源清零 | 符合 / 证据不足 | 具名失败分型与隔离后验 |

### F0264 `运行根需求稳定语义与特征定义自检`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / C01 / C02 / B03 | 本函数内具体指令 / 函数调用 | S11 只从完整自检路由进入；局部环境不发布生产运行期结构 | 符合 / 证据不足 | 展开 F0468/F0469 |
| X04 / N05—N11 / C12 | 本函数内具体指令 / 函数调用 | 固定配置和六个 F0538—F0543 端口闭包由 F0015 同步消费；生命周期闭合 | 符合 | 展开六个端口闭包 |
| X13 | 本函数内具体指令 | 第 377 行在 F0016 前通过 `operator->` 解引用 `自我初始化`；空值违反 optional 前置并可能产生未定义行为 | 实现偏差 | 初始化结果 optional 前置与稳定拒绝 |
| C14 / C15 | 函数调用 | 两次 F0544 只按裸 `uint64_t` 键读取可重建索引，且先于初始化成功判断；索引不能独立承担机器事实 | 实现偏差 / 证据不足 | 命名域稳定主键读取与权威结构复核 |
| C16—C19 / N20 | 函数调用 / 本函数内具体指令 | 只比较两个索引节点与初始化 DTO 中的语素入口句柄；未验证命名域、语素绑定/追溯、特征定义类型、值域、版本和来源 | 证据不足 | 根需求稳定语义与特征定义完整验收 |
| B21 / RT / RF / D22—D24 / E25 | 本函数内具体指令 / 函数调用 | 普通失败与故障注入压成一项；RAII 可达 F0131，但缺少资源清零后验 | 符合 / 证据不足 | 具名失败分型与隔离后验 |

### F0265 `运行安全与服务根需求完整读回自检`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / C01 / C02 / B03 | 本函数内具体指令 / 函数调用 | S12 只从完整自检路由进入；局部环境不发布生产运行期结构 | 符合 / 证据不足 | 展开 F0468/F0469 |
| X04 / N05—N10 / C11 | 本函数内具体指令 / 函数调用 | 六个 F0545—F0550 端口闭包由 F0015 同步消费，闭包借用不逃逸 | 符合 | 展开六个端口闭包 |
| X12 / N13 | 本函数内具体指令 | 第 403 行在 F0016 前无保护解引用 `自我初始化`；随后 F0551 捕获结果和上下文引用，仅在根函数内同步调用 | 实现偏差 / 符合 | optional 前置；保留局部闭包身份 |
| C14—C17 / N18 | 函数调用 / 本函数内具体指令 | F0551 两次只核对承接存在、目标特征、宿主槽位、当前 I64 和目标状态值；两根另只以句柄不等区分 | 证据不足 | 完整需求承接、角色、槽位、状态和身份读回 |
| F0551 下层 | 调用方合同 | 未核对承接主体/目标宿主/场景、正式安全阈值、服务合同、规则版本、来源及合法生产者；固定 `1/10` 只是当前局部期望 | 证据不足 | 安全与服务领域完整事实验收 |
| B19 / RT / RF / D20—D22 / E23 | 本函数内具体指令 / 函数调用 | 普通失败与故障注入压成一项；RAII 可达 F0131，但缺少线程和结构清零后验 | 符合 / 证据不足 | 具名失败分型与隔离后验 |

### F0266 `运行自我线程重复启动结构不变自检`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / C01 / C02 / B03 | 本函数内具体指令 / 函数调用 | S13 只从完整自检路由进入；F0468/F0469 形成局部隔离环境并决定是否继续 | 符合 / 证据不足 | 展开 F0468/F0469；核对隔离环境后验 |
| X04 / N05—N11 / C12 | 本函数内具体指令 / 函数调用 | 六个 F0556—F0561 端口闭包只借用局部上下文，由 F0015 同步消费且不逃逸 | 符合 | 展开六个端口闭包 |
| C13—C16 | 函数调用 | F0015 返回后无条件读取三项总量并调用第二次 F0044；首次成功尚未检查，失败路径仍可能继续写入 | 设计缺口 / 实现偏差 | 首轮失败停止依赖路径和重复启动前置 |
| C17—B20 | 函数调用 / 本函数内具体指令 | `成功 + 复用初始化结果 + 重复启动 + 运行中` 是当前结果面证据，不证明复用同一快照、代次和完整身份 | 证据不足 | 重复启动同代次读回合同 |
| C21—N27 | 函数调用 / 本函数内具体指令 | 三仓库总量相等不排除同量替换，且六次读取不属于同一事务快照 | 证据不足 | 节点、关系、索引明细与历史审计 |
| B28 / RT / RF / D29—D31 / E32 | 本函数内具体指令 / 函数调用 | 内部不一致和故障注入都压成一项失败；RAII 可达 F0131，但无析构后资源清零读回 | 符合 / 证据不足 | 具名失败分型与隔离后验 |

### F0267 `运行自我线程无效参数拒绝与生命周期自检`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / C01 / C02 / B03 | 本函数内具体指令 / 函数调用 | S14 只从完整自检路由进入；F0468 建立未初始化隔离上下文和初始总量 | 符合 / 证据不足 | 展开 F0468/F0469；核对隔离环境后验 |
| X04 / N05 / C06 / C07 | 本函数内具体指令 / 函数调用 | 两项无效参数经 F0044 的 F0134 校验后走 F0169，发生在任何线程构造和结构写入前 | 符合 | 保持入口写前拒绝 |
| B08—B15 | 本函数内具体指令 / 函数调用 | 两个拒绝结果及 F0530/F0531/F0562 三项线程观察符合当前负例；不一致仍只压成普通失败 | 符合 / 证据不足 | 具名失败分型与同代次线程观察 |
| C16—N22 | 函数调用 / 本函数内具体指令 | 三仓库总量相等只能形成粗粒度零变化证据，不能排除同量替换或领域记录变化 | 证据不足 | 每项拒绝后完整零变化审计 |
| B23 / RT / RF | 本函数内具体指令 | 故障注入与固定一项结果不改变机器事实 | 符合 | 保持测试边界 |
| D24 / C25 / D26 / E27 | 本函数内具体指令 / 函数调用 | RAII 可达 F0131；当前负例无 join，但缺少析构后 live-count、完整结构清零和生命周期消息读回 | 证据不足 | 线程与隔离环境析构后验 |

### F0268 `日志文件路径`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / B01 / C02—N07 | 本函数内具体指令 / 函数调用 | 三个非调试类别分别映射到固定独立文件；函数只形成路径材料，不访问文件系统或机器结构 | 符合 | 展开 F0563；保持日志非权威边界 |
| C08 / B09 / R10 | 函数调用 / 本函数内具体指令 | 调试切片先经 F0564 校验，不可用时在文件操作前返回空路径；正式规范未冻结精确字符集 | 符合 / 规范证据不足 | 展开 F0564；冻结切片名 API 合同 |
| B11—N22 | 本函数内具体指令 / 函数调用 | 四个具名调试切片进入 `日志/调试/` 独立文件，符合当前入口宏固定名称 | 符合 | 保持四切片映射 |
| C23 / N24 | 函数调用 / 本函数内具体指令 | 合法未知切片形成根目录 `调试_<切片>.log`；规范只要求独立文件或子目录，未裁决该 fallback 布局 | 证据不足 | 日志路径详细设计 |
| R25 | 本函数内具体指令 | 非法枚举返回空路径；当前已知调用方均传具名枚举，正式调用语境出现非法值应追根因 | 符合 / 证据不足 | 非法类别结果合同 |
| E26 | 本函数内具体指令 | 路径或字符串分配异常向上传播；启用调试宏时可能改变调用方控制流，与日志只增加人读输出边界冲突 | 实现偏差 | 统一日志入口异常隔离设计与代码计划 |
| 调用覆盖 | 调用图证据 | 已登记 F0095 入边；控制面板两个源码调用点等待 F0396 子图稳定身份，无调用证据的运行线程自检保持排除 | 证据不足 | 调用图增量展开 |

### F0269 `当前日志时间文本`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / N01 / N02 | 本函数内具体指令 | 无参数读取 `system_clock` 墙钟并转为 `time_t`；只形成日志时间材料，不是单调时钟、事件序号或机器事实 | 符合 / 证据不足 | 人读时间字段与排序边界 |
| N03 / N04 | 本函数内具体指令 | `localtime_s` 使用当前本地时区写局部 `tm`，但非零返回被丢弃，失败后仍进入格式化 | 实现偏差 | 本地时间转换失败合同 |
| N05 / N06 | 本函数内具体指令 | 32 宽字符缓冲区在正常当前日期格式下充足；`wcsftime` 返回 0 时缓冲区不保证仍是有效零终止串，当前无条件继续 | 实现偏差 | 格式化返回值、locale 与失败缓冲区合同 |
| N07 / E08 | 本函数内具体指令 | 把缓冲区直接当零终止串构造 `wstring`；失败材料可能空、部分或越界读取，分配异常也向 F0095 传播 | 实现偏差 | 统一日志时间形成和异常隔离设计 |
| 时间语义 | 外部边界 | 输出只有秒级本地墙钟文本，可能因校时、时区或夏令时变化重复、回拨或跳变 | 符合 / 禁止扩大 | 保持人读字段，不承担审计全序 |

### F0270 `日志类别文本`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / B01 | 本函数内具体指令 | 按值接收枚举并纯分派，无项目结构读写或所有权转移 | 符合 | 保持纯人读投影 |
| R02—R05 | 本函数内具体指令 | 四个声明枚举稳定映射为逻辑错误、运行、调试和事件文本，符合日志四分类 | 符合 | 保持四分类 |
| R06 | 本函数内具体指令 | 非法枚举返回“未知”静态文本；只作表现层 fallback，不能校验或合法化枚举，规范未冻结该精确 API 文本 | 符合 / 规范证据不足 | 日志文本 API 与上游值域检查 |
| 生命周期 | 语言边界 | 五个返回对象均为静态存储期宽字符串字面量；调用方只借用、不释放、不修改 | 符合 | 保持只读静态生命周期 |
| 调用覆盖 | 调用图证据 | F0095 已登记且非法类别会先被 F0268 空路径拒绝；控制面板调用等待 F0396 子图，运行线程自检调用点无根可达证据 | 证据不足 | 调用图增量展开 |

### F0271 `转为UTF8`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / N01—B04 / R05 | 本函数内具体指令 | 空文本、超过 `INT_MAX` 和首轮失败共用空字符串；当前 F0095 输入必定非空，可把空结果当失败，但通用 API 无法区分原因 | 符合 / 证据不足 | 日志编码结果与大小预算合同 |
| N06—B08 | 本函数内具体指令 | `size_t` 在已验证上限后安全窄化为 `int`，首轮查询非正时在文件打开前返回空结果 | 符合 / 证据不足 | 保留错误原因和 Win32 ABI 边界 |
| N09—N11 / R12 | 本函数内具体指令 | 第二次 `WideCharToMultiByte` 返回值被丢弃；失败或长度不一致时仍可能返回非空填零或部分材料，F0095 只检查 `empty()` | 实现偏差 | UTF-8 第二阶段完整写入验证 |
| 编码严格性 | 外部边界 | flags 0 对非法 UTF-16 可形成替代字符；日志规范未裁决普通人读日志应严格拒绝还是允许有损替换，也未冻结嵌入 NUL 合同 | 规范证据不足 | UTF-8 严格性和转义详细设计 |
| E13 | 本函数内具体指令 | 定长结果字符串分配异常向 F0095 传播；启用调试日志可能改变调用方控制流 | 实现偏差 | 统一日志异常隔离与受控内容预算 |
| 调用覆盖 | 调用图证据 | 仅登记 F0095 入边；事件日志段 `严格转为UTF8` 的源码调用点当前无根图调用证据，不伪造身份或边 | 证据不足 | 事件日志段接线后增量展开 |

### F0272 `隔离关系夹具::隔离关系夹具`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / N01—N09 | 本函数内具体指令 | 按声明顺序复制规模 / 种子并计算三个仓库编号；当前两类调用都使用局部夹具，不接生产运行期结构 | 符合 / 证据不足 | 保持隔离边界；冻结泛化编号预算 |
| C04 / C06 / C08 | 函数调用 | 依次构造主信息、节点、关系三仓库，借用链和逆序销毁安全；当前正式 4010 已删除通用主信息生产结构 | 当前旧实现偏差 / 隔离自检事实 | 节点直接身份测试夹具迁移设计 |
| X10 / E13 | 外部边界 / 本函数内具体指令 | 四个容器和默认成员只属于局部自检材料；异常时对象不发布并逆序展开 | 符合 | 保持构造失败无对象发布 |
| 编号算术 | 本函数内具体指令 | `790000/791000/792000 + size_t规模` 无回绕拒绝；当前零值和三档规模安全，泛化调用不完整 | 证据不足 | 仓库编号分配与输入上限合同 |
| 设计对照 | 详细设计证据 | 旧详细设计称“四仓库夹具”，当前只有三仓库和容器参考模型 | 设计证据差异 | 性能治理详细设计现状注记 |

### F0273 `隔离关系夹具::构建`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / B01 / R02 | 本函数内具体指令 | F0096 的规模0负例在第一笔写入前拒绝；三档固定规模若被拒绝则属于实现漂移 | 符合 | 保持写前参数负例 |
| C03—C16 | 函数调用 / 外部边界 | 主信息、1024节点和固定关系即时进入各仓库；失败可留下夹具内可读前缀，函数无共同候选或撤销 | 隔离自检允许 / 禁止扩大 | 不得外推为 4040 生产事务 |
| N19—N31 | 本函数内具体指令 / 函数调用 | 固定 LCG 可复现；自环和重复引用是合法候选跳过，但 F0568 也用空句柄承载仓库创建 / 读回失败 | 实现偏差 | F0568 结构化区分碰撞与内部错误 |
| X32—R37 | 外部边界 / 函数调用 | 填充上限或固定变更失败时保留可读半结构 / 部分变更；当前仅由局部夹具整体析构限制影响 | 实现偏差 / 证据不足 | 失败夹具封存、精确撤销或具名失败结果 |
| N39 / C40—B45 | 本函数内具体指令 / 函数调用 | 摘要成员在最终三项读回前写入；最终 false 不撤销，也不区分失败阶段 | 实现偏差 | 最终验证后冻结摘要与结构化结果 |
| 单次使用 | 调用方合同 | 当前 F0096/F0097 都只调用一次，但函数不检查空夹具或已构建状态 | 证据不足 | 单次使用状态或幂等 / 拒绝合同 |

### F0274 `隔离关系夹具::当前签名`

| 节点 | 类型 | 规范依据与核对结果 | 状态 | 唯一后续归属 |
| --- | --- | --- | --- | --- |
| S / X01—X08 | 本函数内具体指令 / 外部边界 | 复制参考表 value 并按关系编号排序，消除 unordered_map 枚举顺序；不验证 key 与内嵌编号相等 | 符合 / 证据不足 | 参考模型键值不变量 |
| N09 / F0570 | 本函数内具体指令 / 函数调用 | 固定初值不是标准 FNV-1a offset basis，且按64位整词混合；算法是确定的自定义 FNV-like 摘要 | 符合 / 禁止误称 | 保持冻结摘要算法命名 |
| C13—C19 | 函数调用 | 七项顶层记录字段进入摘要，但端点仅混节点编号，遗漏仓库编号 / 版本及其它域材料 | 摘要用途符合 / 完整证明不足 | 禁止用摘要裁决完整句柄或结构相等 |
| 并发与生命周期 | 调用方合同 | F0097 只在构建成功后、并发和写测量前调用；F0274 自身无锁，未来并发调用会数据竞争 | 当前语境符合 / API证据不足 | 冻结只读稳定期前置 |
| 三档常量 | 详细设计 / 调用图证据 | 详细设计冻结三档期望签名；当前调用方只检查非零，未按规模比较常量 | 既有验收缺口 | 保持既有 A0118 唯一归属 |

当前函数级已登记：268。按 0600 完成逐节点核规：45。待转换为 0600 逐节点核规：223。待制图且待函数级审计：296。未解析动态调用：7。
