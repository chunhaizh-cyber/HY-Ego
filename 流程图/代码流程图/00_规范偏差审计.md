# 当前代码图谱规范偏差审计

代码基线：`720f4afd78e63d95d244867aa13d72970a419654`。

| 编号 | 当前代码事实 | 偏差类型 | 唯一治理归属 | 是否形成代码许可 |
| --- | --- | --- | --- | --- |
| G-D001 | 普通控制面板模式完成L1上下文装配后直接返回控制面板宿主失败 | 证据化实现缺口 | 交互智能体后续交给设计治理 | 否 |
| G-D002 | 当前普通应用上下文只接线L1事实基座服务；新增L1领域服务与组合模块没有main可达调用边 | 证据不足/未接线 | 交互智能体后续按目标方案裁决 | 否 |
| G-D003 | 相对旧基线删除205个源码文件，旧1993函数闭包收缩为19个；1489张旧图仅作历史证据 | 当前事实基线变化 | 流程图维护已完成现状分账 | 否 |

本表不修改规范、详细设计、计划或代码；覆盖完成不等于应用或业务能力完成。

# 2026-08-23 当前性增量

- 当前复核基线：`d534058e154177d4576f50a92ec448dc3f38a507`；`dc9aa543..d534058e` 只新增需求当前满足设计、施工图和计划，目标源码 blob 未变。
- 旧全量聚合基线：`720f4afd78e63d95d244867aa13d72970a419654`。
- 当前性差异：自动维护入口产生 122 个源码 / 工程差异、109 个新增待绘制候选；本批只闭合需求任务子目标承接的 9 个核心函数图。
- 唯一治理归属：流程图与知识图谱维护智能体继续按候选批次逐函数展开；不得由计划或执行智能体把旧 `19/19` 解释为当前代码覆盖完成。
- NOT_RUN：普通应用装配整函数、治理专项整函数以及 R1510—R1513 的内部 helper 尚未逐函数展开；本批未运行构建或程序。
- 已发布验收：`dc9aa543` 登记 `INTEGRATION-ACCEPTANCE-FAIL / 未正式接线`；自检场景 PASS 不能替代生产消费者接线或 Release 自检隔离。
- 后继语义差异：`d534058e` 的有效设计已经区分“可共用结果”和“争用结果”。前者允许多个合格需求共同引用，但仍逐需求独立判定 / 结算并禁止价值、预算、归因重复领取；后者才按正式成员分配优先级逐一分配并逐次扣减剩余。当前承接代码没有该分类、结算或分配调用边，且没有正式成员分配优先级机器合同；唯一治理归属为后继设计治理，不形成当前代码许可。

# 2026-08-23 需求初次筹办工作包增量

- 当前复核基线：`7d3e7c9e32623aaa9aa4b4c90b53ef634eb6f094`；实现提交 `17edb9d974be6d961d404247c88a064b8f73df58`，归档和独立验收提交只改变计划 / 验收材料，目标源码 blob 未变。
- 当前已实现：首次准备强类型记录、task owner 融合任务原子建立、准备键前置首次读回、记录身份当前 / 历史读回及首次结果组包。
- 当前未接线：业务 provider 仅由治理专项直接消费；普通应用没有 provider 所有权或线程调用边。治理专项、构建记录和提交均不能替代生产接线。
- 当前未实现：父回流、已有任务重筹办、成员结算、结果共用 / 争用裁决和成员分配优先级；本批只登记事实差异，不形成代码许可。
- 当前性边界：本批闭合 R1515—R1522 八个核心函数；初始化登记、编解码 helper、专项整函数及其它 event 候选保持 `候选待复核`。旧 `720f4afd` 全量统计不更新、不与局部基线混写。

# 2026-08-23 任务统一筹办轮次权威增量

- 当前复核基线：`dd8a7810693b7892b3265333f762b7c1c3bcd4d8`；实现提交 `e385585aef3c2d0d676b282ee16a62e9983749b8`，其后 `dd8a7810` 只归档计划，目标源码 blob 未变。
- 当前已实现：新任务在首次融合事务内建立版本 1 轮次权威和轮次 1 当前关系；统一任务实际结果未达成时以当前 N 建立 N+1 后继记录并换代当前关系。
- 当前生产接线：普通应用构造任务结果生产运行包，运行包持有重筹办 provider，任务管理线程在未达成路径调用 provider。治理专项并非这条生产边的唯一调用者。
- legacy 边界：遗留任务继续按实例轮次形成 N+1 工作包，不产生统一写；显式迁移与退役尚未实现。
- 未实现：任务子目标回流 consumer、成员逐项结算、结果共用 / 争用裁决和成员分配优先级。需求初次筹办 provider 也仍未接普通生产线程。
- 当前性边界：本批闭合 R1523—R1531 九个核心函数；任务筹办轮次初始化登记、编解码和后继记录历史投影等 helper 保持 `候选待复核`。旧 `720f4afd` 全量统计不更新。
- 验证边界：本批只做静态源码 / 调用点 / blob 复核；构建、程序和治理专项均 `NOT_RUN`，既有施工与验证记录不升级为本批运行事实。

| 编号 | 现状代码事实 | 正式设计 / 计划完成边界 | 偏差类型 | 唯一纠偏归属 / 纠偏方向 | 是否需要代码计划 |
| --- | --- | --- | --- | --- | --- |
| RA-G001 | legacy 没有显式迁移到统一轮次权威，也没有退役门禁 | 计划禁止把 legacy 延续冒充迁移完成 | 已知迁移缺口 | 后继设计 / 计划 owner | 是；本批不形成 |
| RA-G002 | `子目标回流` 只存在触发类型枚举，当前无 consumer 边 | 本计划不包含子目标回流 consumer | 预留合同未实现 | 子目标回流后继设计与执行 | 是；本批不形成 |
| RA-G003 | 成员结算、结果共用 / 争用和成员分配优先级不在当前链 | 计划完成边界明确不证明这些闭环 | 后继业务缺口 | 各正式设计 / 计划 owner | 是；本批不形成 |
| RA-G004 | 需求初次筹办 provider 仍只有治理专项直接消费 | 本次生产接线只针对目标未达成重筹办 provider | 既有未接线边界 | 直接后继生产消费设计 | 是；本批不形成 |
| RA-G005 | event 仍从旧全量基线报告大量候选，本批只闭合 R1523—R1531 | 0600 要求不同基线分账 | 图谱覆盖缺口 | 图谱维护继续逐函数展开 | 否；图谱维护任务 |
