# 当前代码函数规范偏差审计

冻结提交：`a48a70b2d678dea64dd8228adb4f26f0badd9506`

本表随可达函数闭包逐函数追加。第一轮只记录代码事实与现行正式规范的差异，不形成代码施工许可。

| 编号 | 函数 | 当前代码事实 | 正式规范口径 | 偏差类型 | 唯一纠偏归属 | 是否需要代码计划 |
| --- | --- | --- | --- | --- | --- | --- |
| A0001 | F0001 `main` | 只解析并验证参数、拒绝时映射退出码、接受时调用唯一顶层函数并映射退出码 | `8120` 第 2、4 节要求薄入口只承担这四项职责 | 无偏差 | 不适用 | 否 |
| A0002 | F0002 `解析并验证启动选项` | 逐项拒绝无效数量、空数组、空指针、未知参数、重复/冲突模式和缺失构建能力；接受时返回唯一模式 | `8120` 第 3、4 节规定相同参数、模式和退出口径 | 无偏差 | 不适用 | 否 |
| A0003 | F0003 `启动选项解析结果::成功` | 只在结构化状态为“已接受”时返回 true | `8120` 第 3、4 节要求拒绝和准入由结构化解析结果承载 | 无偏差 | 不适用 | 否 |
| A0004 | F0004 `映射进程退出码` | 把解析拒绝、运行失败和运行成功分别映射为 2、1、0 | `8120` 第 4 节冻结相同进程退出码合同 | 无偏差 | 不适用 | 否 |
| A0005 | F0005 `运行海中鱼巣` | 仅按已验证模式调用一个对应顶层函数；穷举外值返回内部不一致 | `8120` 第 5 节要求顶层路由层按模式分发且不展开装配正文 | 无偏差 | 不适用 | 否 |
| A0006 | F0006 `程序运行结果::成功` | 只在结构化状态为“已完成”时返回 true | `8120` 第 4 节要求程序成功语义由结构化结果承载 | 无偏差 | 不适用 | 否 |
| A0007 | F0007 `运行普通程序` | 装配、生产初始化、最佳努力审计、信号宿主、UI/headless 顺序明确；审计失败不阻断普通启动 | `8120` 第 5—8 节冻结相同普通程序分层 | 无偏差 | 不适用 | 否 |
| A0008 | F0008 `运行生产运行期模式` | 以当前版本请求调用会话启动并映射结构化结果 | `8120` 第 5 节要求生产运行期通过结构化会话合同进入 | 无偏差 | 不适用 | 否 |
| A0009 | F0009 `运行完整自检模式` | 显式模式登记 14 个入口单元及 F27/F29，并核对 16 项批次合同 | `8120` 要求完整自检只按显式模式调用并返回结构化结果 | 无偏差 | 不适用 | 否 |
| A0010 | F0010 `运行数据库专项` | 核心初始化后执行必须往返一致审计；支持覆盖端口；日志不参与裁决 | `8120` 第 7、9 节冻结相同数据库专项和日志边界 | 无偏差 | 不适用 | 否 |
| A0011 | F0011 `运行关系仓库性能专项` | 只在能力宏开启时调用性能自检，并通过具名日志宏记录人读结果 | `8120` 第 9 节要求机器结果独立于日志 | 本函数无偏差；下游 F0037 存在待审计 stdout 旁路 | F0037 | 待 F0037 审计 |
| A0012 | F0012 `运行D455真实样本专项` | 只在能力宏开启时调用真实样本自检，并通过具名日志宏记录人读结果 | `8120` 第 5、9 节要求显式专项和统一日志 | 本函数无偏差；下游 F0038 存在待审计 stdout 旁路 | F0038 | 待 F0038 审计 |
| A0013 | F0013 `构造普通应用上下文` | 校验配置后一次构造完整上下文，异常转为结构化装配失败 | `8120` 要求普通应用装配集中拥有仓库和服务生命周期 | 无偏差 | 不适用 | 否 |
| A0014 | F0014 `普通应用装配结果::成功` | 同时要求状态已装配且上下文非空 | `8120` 要求成功结果必须承载可用上下文 | 无偏差 | 不适用 | 否 |
| A0015 | F0015 `初始化普通系统` | 依次校验请求/端口、方法根、自我线程、初始化快照、概念四根和存在根支持 | `8120` 冻结生产初始化顺序及结构化失败 | 无偏差 | 不适用 | 否 |
| A0016 | F0016 `系统初始化结果::成功` | 校验状态、方法根、快照、概念图和存在根支持的完整一致 | `8120` 要求初始化成功不能仅依赖状态枚举 | 无偏差 | 不适用 | 否 |
| A0017 | F0017 `普通程序失败阶段映射 lambda` | 把多类非具名状态及“已初始化但结果字段不完整”统一折叠为普通应用装配阶段 | 正式规范未冻结更细的失败阶段映射 | 语义粒度风险，尚非确认冲突 | 后续入口诊断治理 | 否 |
| A0018 | F0018 `执行启动结构审计` | `要求` 参数未参与分支；快照失败状态当前不可达；命名空间校验未检查可持久化和精确失效条件 | `8120` 区分普通最佳努力与专项必须往返一致，且审计命名空间应可机械确认 | 当前实现差异候选 | 数据库审计职责 | 是 |
| A0019 | F0019 `安装程序停止信号` | 顺序安装三个信号并在失败时依赖租约析构恢复 | `8120` 要求宿主层安装/恢复停止信号 | 无确认偏差；静态原始指针和异常恢复属于风险 | 运行宿主 | 否 |
| A0020 | F0020 `停止信号安装结果::成功` | 同时要求状态已安装和租约非空 | 结构化结果必须承载可用租约 | 无偏差 | 不适用 | 否 |
| A0021 | F0021 `运行无窗口宿主` | 仅轮询停止请求并休眠，收到请求后返回成功 | `8120` 要求无窗口宿主保持常驻并可由停止信号收口 | 无偏差 | 不适用 | 否 |
| A0022 | F0022 `生成控制面板启动投影` | 坐标缺失不会独立拒绝，而是继续生成世界树材料；若下游仍可展示则投影成功 | 现行 ENTRY-ONLY 施工设计曾要求坐标为空时投影失败；正式 `8120` 需复核是否把坐标列为成功前置 | 当前代码与既有设计差异，正式规范裁决待确认 | 控制面板投影 | 是 |
| A0023 | F0023 `控制面板启动投影结果::成功` | 要求状态已形成且两树均可展示、只读 | `8120` 要求控制面板仅消费只读投影 | 无偏差 | 不适用 | 否 |
| A0024 | F0024 `运行控制面板宿主` | 先校验初始化/投影，再装配服务和窗口并把窗口结果映射为程序结果 | `8120` 要求 UI 宿主不承担生产初始化 | 无偏差 | 不适用 | 否 |
| A0025 | F0025 `停止信号租约析构` | 恢复已安装处理器并清空当前租约指针；注入回调抛出会触发隐式 noexcept 终止 | `8120` 要求停止信号生命周期由宿主租约收口 | 无确认偏差；回调异常为风险 | 运行宿主 | 否 |
| A0026 | F0026 `生产运行期会话构造` | 仅默认初始化启动锁和空宿主 | 生产运行期通过会话持有串行启动边界 | 无偏差 | 不适用 | 否 |
| A0027 | F0027 `生产运行期会话::启动` | 完整校验请求、配置、候选、发布和发布后租约一致性 | 生产运行期正式合同要求候选完整后单次发布并读回 | 无偏差 | 不适用 | 否 |
| A0028 | F0028 `生产运行期启动结果::成功` | 仅允许两种成功状态且要求租约有效 | 成功不得脱离有效运行期上下文租约 | 无偏差 | 不适用 | 否 |
| A0029 | F0029 `登记入口初始化自检单元` | 14 项严格短路登记，前项成功后后项失败不回滚 | `8120` 要求这些自检只由显式自检路径登记 | 无偏差 | 不适用 | 否 |
| A0030 | F0030 `自检运行器::登记自检` | 非法或重复登记永久写坏合同；成功只追加，不执行回调 | 统一运行器必须拒绝非法/重复登记并保持结构化合同状态 | 无偏差 | 不适用 | 否 |
| A0031 | F0031 `自检运行器::运行全部` | 稳定排序后逐项执行；回调异常转失败并继续；不核对返回单元编号/名称与登记身份一致 | 结构化自检结果不得由人读材料裁决 | 身份一致性缺少机械核对，属于合同审计候选 | 统一自检运行器 | 是 |
| A0034 | F0034 `数据库专项失败阶段映射 lambda` | default 折叠多类失败原因 | 正式规范未冻结更细映射 | 语义粒度风险，尚非确认冲突 | 后续入口诊断治理 | 否 |
| A0035 | F0035 `数据库启动审计结果::成功` | 同时要求状态和布尔成员均为往返一致 | 数据库专项成功必须由结构化往返证据承载 | 无偏差 | 不适用 | 否 |

当前已审计函数：33。已定位确认或待裁决偏差：5。待审计函数：61。
