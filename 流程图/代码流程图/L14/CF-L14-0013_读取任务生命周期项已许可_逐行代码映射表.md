# CF-L14-0013 读取任务生命周期项已许可逐行代码映射表

代码版本：`3920a74638264f9f539d50f32f3c9fe5fb1982ab`
当前工作区复核基线：`57866ac5ca63235ed12da60f635d29f1aacd718b`
源码 blob：`16ac9534baeda26ac8a712066e713f3339f6e0c8`
覆盖文件：`海中鱼巣/领域/数据操作.需求任务方法.ixx:2408-2430`
根函数：R0446 `std::optional<任务生命周期材料> 需求任务方法数据操作::读取任务生命周期项_已许可(const 关系记录&,const 结构事务令牌&,bool) const`
配对流程图：`流程图/代码流程图/L14/CF-L14-0013_读取任务生命周期项已许可_现状流程图.md`
稳定边全集：RCE1380—RCE1381、RCE3017—RCE3018；X04108—X04116

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/数据操作.需求任务方法.ixx` | 2408—2411 | 进入 R0446 并绑定记录、令牌和默认审计标志 | S | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 2412 | 经 RCE1380 形成关系证据；其 optional 生命周期归入 X04108 | C01 | 函数调用 | 读取 | 无 | 关系证据材料 | 不适用 | 无 |
| 同上 | 2413 | 经 RCE1381 读取记录目标的身份 | C02 | 函数调用 | 读取 | 无 | 身份材料 | 不适用 | 无 |
| 同上 | 2414—2418 | 经 RCE3017 检查当前可读，并判断证据、节点类型、正版本与 X04110 的 uint32 上限；失败时经 X04109 返回空 optional，随后跨过分段空行 | C03、D04、R05 | 函数调用 / 本函数内具体指令 | 判断 / 返回 / 排版 | 无 | 当前结构化结果 | 追根因解决 | 由调用语境区分许可、版本与内部材料问题 |
| 同上 | 2419—2420 | 经 RCE3018 两次读取状态值槽位和时间戳槽位 | C06 | 函数调用 | 读取 | 无 | 主信息值材料 | 不适用 | 无 |
| 同上 | 2421—2425 | 经 X04112 检查两个 optional，经 X04114 解引用并判断时间戳及生命周期枚举范围；失败时返回空 optional，随后跨过分段空行 | D07、R05 | 本函数内具体指令 | 判断 / 返回 / 排版 | 无 | 当前结构化结果 | 追根因解决 | 不把非法生命周期值降级为正常未找到 |
| 同上 | 2426—2430 | 经 X04113/X04114 解引用证据和值，构造生命周期材料并经 X04115 返回 optional；相关 optional 析构归入 X04108/X04116 | N08、R09 | 本函数内具体指令 | 构造 / 返回 / 资源释放 | 无持久写入 | 当前结构化结果 | 不适用 | 不把材料构造写成事实发布 |

覆盖结论：2408—2430 行全部映射；流程图全部节点均在本表出现，本表没有图外节点。
