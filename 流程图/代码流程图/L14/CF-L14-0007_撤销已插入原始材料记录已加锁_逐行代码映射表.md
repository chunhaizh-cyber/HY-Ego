# CF-L14-0007 撤销已插入原始材料记录已加锁逐行代码映射表

代码版本：`3920a74638264f9f539d50f32f3c9fe5fb1982ab`
源码 blob：`d82fa092a2c1156d2f8de758d5e32415cb64931d`
覆盖文件：`海中鱼巣/领域/参与者.特征值原始材料.ixx:204-225`
根函数：R0264
配对流程图：`流程图/代码流程图/L14/CF-L14-0007_撤销已插入原始材料记录已加锁_现状流程图.md`
稳定边全集：RCE0873、RCE0874、RCE0875、RCE0876、X02053—X02057、X03232—X03238

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/参与者.特征值原始材料.ixx` | 204-205 | 定义 R0264 并初始化完整标记 | N01 | 本函数内具体指令 | 入口 / 赋值 | 仅局部标记 | 当前代码事实 | 不适用 | 无 |
| 同上 | 206 | 经 X02053、X02054、X03232、X03234 建立并推进 Vec 逆序循环 | L02 | 函数调用 / 本函数内具体指令 | 外部调用 / 循环控制 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 207 | 经 X03233 解引用位置并经 RCE0875 调用 R0546 查找 Vec 记录 | C03 | 函数调用 | 外部调用 / 调用 / 读取 | 无 | 权威事实 | 不适用 | 无 |
| 同上 | 208-211 | 经 X02055、X03235、X03236 复核记录存在和版本；不符则完整=false并继续 | C04 / D05 / N06 / L02 | 函数调用 / 本函数内具体指令 | 外部调用 / 判断 / 赋值 / 循环 | 仅局部完整标记 | 当前代码事实 | 追根因解决 | 无 |
| 同上 | 212-213 | 经 X02056 删除命中的 Vec 记录并闭合循环 | C07 / L02 | 函数调用 / 本函数内具体指令 | 外部调用 / 写入 / 循环 | 删除 Vec 侧表记录 | 权威事实 | 不适用 | 无 |
| 同上 | 214 | 经 X02053、X02054、X03232、X03234 建立并推进 I64 逆序循环 | L08 | 函数调用 / 本函数内具体指令 | 外部调用 / 循环控制 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 215 | 经 X03233 解引用位置并经 RCE0876 调用 R0550 查找 I64 版本记录 | C09 | 函数调用 | 外部调用 / 调用 / 读取 | 无 | 权威事实 | 不适用 | 无 |
| 同上 | 216-219 | 经 X02055、X03237、X03238 复核记录存在和版本；不符则完整=false并继续 | C10 / D11 / N12 / L08 | 函数调用 / 本函数内具体指令 | 外部调用 / 判断 / 赋值 / 循环 | 仅局部完整标记 | 当前代码事实 | 追根因解决 | 无 |
| 同上 | 220-221 | 经 X02056 删除命中的 I64 版本记录并闭合循环 | C13 / L08 | 函数调用 / 本函数内具体指令 | 外部调用 / 写入 / 循环 | 删除 I64 版本记录 | 权威事实 | 不适用 | 无 |
| 同上 | 222-223 | 经 X02057 清空两组撤销跟踪节点 | C14 | 函数调用 | 外部调用 / 写入 | 清除参与者跟踪组 | 当前代码事实 | 不适用 | 无 |
| 同上 | 224-225 | 返回完整标记并闭合函数 | R15 | 本函数内具体指令 | 返回 / 作用域 | 无 | 当前代码事实 | 追根因解决 | 无 |

覆盖结论：204—225 行全部映射；R0264 与全部稳定边、节点可双向反查。
