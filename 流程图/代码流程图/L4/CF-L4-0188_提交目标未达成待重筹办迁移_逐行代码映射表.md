# CF-L4-0188 提交目标未达成待重筹办迁移逐行代码映射表

代码版本：`dd8a7810`；源码 blob：`6e28721b63e27f96abc2abb6e9335c40484b9afc`；根函数：R1528。

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/服务.L2任务结构.ixx` | 7264-7276 | 定义加锁提交入口、失败结果，验证请求并读取当前代次 | N01 / R14 | 本函数内具体指令 | 入口 / 判断 / 返回 | 无 | task owner 提交入口 | 入口拒绝逻辑内返回；零代次追根因 | 无 |
| 同上 | 7277-7315 | G 漂移时按迁移键读取首次写入；统一分区额外按旧结果查已消费关系并可回显后继 | N02 / C03 / N04 / R14 | 本函数内具体指令 / 函数调用 | 读取 / 收敛 / 返回 | 无 | L1 首次写入与后继触发关系 | 漂移 / 已消费逻辑内返回；关系有而记录无追根因 | 无 |
| 同上 | 7316-7343 | 对存在首次写入的重放互证目标 / 前态投影并提交精确重复 | P05 / R14 | 函数调用 / 本函数内具体指令 | 读回 / 重放 / 判断 | 不新增事实 | 首次写入见证 | 同键异义为幂等冲突；重放不守恒追根因 | 无 |
| 同上 | 7344-7392 | 组装精确重复结果；统一分区读回后继并逐字段互证，legacy 禁止出现后继 | P05 / R15 / R14 | 本函数内具体指令 / 函数调用 | 读回 / 判断 / 返回 | 无 | 首次写入与历史事实 | 形状错为幂等冲突或内部错误 | 无 |
| 同上 | 7393-7414 | 当前 G 路径读取前态和实际结果，互证目标未达成、来源及生命周期 | C06 / N07 / R14 | 函数调用 / 本函数内具体指令 | 读取 / 判断 / 返回 | 无 | task owner 治理与实际结果事实 | 未找到 / 引用冲突为逻辑内返回 | 无 |
| 同上 | 7415-7450 | 读取并互证唯一当前治理、路径和实例关系 | C06 / N07 / R14 | 函数调用 / 本函数内具体指令 | 读取 / 判断 / 返回 | 无 | task owner 当前关系 | 多值追根因；引用不符逻辑内冲突 | 无 |
| 同上 | 7452-7463 | 读回旧路径和实例并互证归属 | C06 / N07 / R14 | 函数调用 / 本函数内具体指令 | 读取 / 判断 / 返回 | 无 | task owner 当前投影 | 引用冲突为逻辑内返回 | 无 |
| 同上 | 7464-7473 | 调 R1530 读取实际轮次分区，并要求非内部不一致且等于请求分区 | C08 / N09 / R14 | 函数调用 / 本函数内具体指令 | 读取 / 判断 / 返回 | 无 | 统一 / legacy 分区权威 | 分区漂移逻辑内冲突；内部形状错追根因 | 无 |
| 同上 | 7474-7505 | 统一分区互证权威、首次 / 前一后继、N、实例轮次及唯一当前准备关系 | N10 / R14 | 本函数内具体指令 / 函数调用 | 判断 / 读取 / 返回 | 无 | 统一当前准备 | 前驱冲突逻辑内返回；当前关系错追根因 | 无 |
| 同上 | 7506-7529 | 查同一旧实际结果已消费关系；存在时读回并返回已消费后继 | N10 / N04 / R14 | 函数调用 / 本函数内具体指令 | 读取 / 收敛 / 返回 | 无 | 后继触发唯一关系 | 已消费逻辑内返回；关系有记录无追根因 | 无 |
| 同上 | 7530-7544 | 统一调 R1531，legacy 调既有写集 helper，并提交 L1 中性写集 | C11 / C12 / R14 | 函数调用 / 本函数内具体指令 | 分流 / 提交 / 返回 | 成功时迁移；统一还推进轮次 | L1 owner-scoped 写入 | 具名写入非成功为逻辑内返回 | 无 |
| 同上 | 7545-7557 | 历史读回新治理状态和前态 | N13 / R14 | 函数调用 / 本函数内具体指令 | 读回 / 判断 / 返回 | 无 | 已发布历史事实 | 写成功但读回失败追根因 | 无 |
| 同上 | 7558-7579 | 组装已提交结果；统一分区按新编码读回后继并要求存在 | N13 / R15 / R14 | 本函数内具体指令 / 函数调用 | 读回 / 输出 / 返回 | 无 | L1 发布见证与后继历史事实 | 后继缺失追根因 | 无 |
| 同上 | 7580-7587 | 映射分配 / 长度异常为资源失败，其它异常为内部不一致 | R14 | 本函数内具体指令 | 异常 / 返回 | 无 | 运行时边界 | 资源失败逻辑内返回；未知异常追根因 | 无 |

覆盖结论：7264—7587 的全部非空有效源码行已映射。
