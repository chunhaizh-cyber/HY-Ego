# CF-L4-0071 `D455采样材料上行桥::上行一次` 逐行代码映射表

| 行 | 节点 | 行为 | 失败收口 |
| --- | --- | --- | --- |
| 54-62 | N01-N02 | 六字段 OR 短路请求门禁 | 请求无效，缓存零触碰 |
| 64-71 | N03-N05 | 按调用语境虚调用真实或模拟帧来源；复制采集拒绝 | 采集失败直接返回 |
| 72-75 | N06-N09 | 先查指针，再调用 F0108 验证同步材料 | 无效材料追根因返回 |
| 76-78 | N10-N11 | 比较采集批次与请求批次 | 不匹配逻辑拒绝 |
| 80-88 | N12-N14 | 准备候选并检查成功与 optional | 缓存拒绝直接返回 |
| 90-101 | N15-N24 | 读候选，检查句柄、材料、字节数和材料一致性 | 任一不符调用 F0303 撤销 |
| 103-114 | N25-N28 | 默认构造消息并写身份、角色、句柄、时间、幂等和批次字段 | 异常可绕过撤销 |
| 115-118 | N29-N30 | 计算摘要并标记只读材料、非机器事实 | 摘要不含帧内容 |
| 119-126 | N31-N33 | 确认发布并比较返回句柄 | 失败撤销候选读回句柄 |
| 128-139 | N34-N38 | 普通读回，复核材料和消息三字段 | 不一致撤销已发布句柄 |
| 141-147 | N39-N42 | 形成成功结果，复制消息与共享材料并返回 | 消息尚未入队 |

项目调用：F0108、F0111、F0113、F0297-F0305，共 15 个调用点；外部边界 X0093。异常绕过撤销、发布漂移撤错句柄、元数据等价不等于内容一致、幂等未在桥内裁决以及并发采样未约束是偏差候选。未解析调用 0。
