# CF-L4-0042 `运行期上下文宿主::尝试首次发布全新上下文` 逐行代码映射表

| 行 | 节点 | 行为 | 结果 |
| --- | --- | --- | --- |
| 208-212 | N01-N03 | 检查按值候选是否为空 | 空则无效候选 |
| 213-215 | N04-N07 | 复核结构核心 | 失败返回结构核心不完整 |
| 216-218 | N08-N11 | 复核服务装配 | 失败返回缺少领域服务装配 |
| 219-221 | N12-N15 | 锁外复核综合完整性 | 失败返回缺少初始化完成材料 |
| 222-224 | N16-N19 | 取得发布锁并检查当前值 | 已有则拒绝首次发布 |
| 226-228 | N20-N23 | 锁内再次复核候选完整性 | 漂移失败仍映射为缺少材料 |
| 229 | N24-N25 | 移动候选共享份额到宿主 | 唯一发布提交点 |
| 230-231 | N26-N27 | 返回已发布并按序解锁/析构 | 宿主持有共享所有权 |

项目调用 4 个静态点、3 个唯一身份：F0239、F0253、F0077（两次）。调用方若仍持有非 const `shared_ptr`，发布后仍可修改候选；本函数不验证独占所有权，也不冻结候选内部版本。外部共享指针与锁边界统一登记为 X0066。未解析调用 0。
