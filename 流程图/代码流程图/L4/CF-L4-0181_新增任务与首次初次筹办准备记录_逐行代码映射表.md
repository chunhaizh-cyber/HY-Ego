# CF-L4-0181 新增任务与首次初次筹办准备记录逐行代码映射表

代码版本：`7d3e7c9e`；源码 blob：`d93c97b38a034e868d7d3c0796275d23bf48b652`；根函数：R1517。

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/服务.L2任务结构.ixx` | 4762-4771 | 定义公开入口、取得 task owner 锁并定义零载荷失败结果 | N01 / R10 | 本函数内具体指令 | 入口 / 同步 / 纯计算 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 4772-4778 | 验证强类型请求和 G0 当前性 / 精确重复提交边界 | N02 / R10 | 函数调用 / 本函数内具体指令 | 判断 / 读取 / 返回 | 无 | 权威事实截止 | 入口 / 漂移 / 许可为逻辑内返回 | 无 |
| 同上 | 4779-4800 | 正常首次路径读取列表项当前任务和列表项身份，守卫 0/1 当前任务 | C03 / N04 / R10 | 函数调用 / 本函数内具体指令 | 读取 / 判断 / 返回 | 无 | 权威事实读取 | 已有当前任务 / 引用冲突为逻辑内返回；形状错追根因 | 无 |
| 同上 | 4801-4804 | 由 R1520 形成一个融合写集并提交 L1 owner-scoped 写 | C05 / C06 | 函数调用 | 事务 / 写入 | 提交时可原子建立 3 节点、7 关系、3 值和 3 属性槽 | 权威写入 | 不适用 | 无 |
| 同上 | 4805-4815 | 核对写入合同、owner、幂等键及非成功零发布守恒 | N07 / R10 | 本函数内具体指令 | 判断 / 返回 | 无新增；只审查写入结果 | 权威写入结果 | 具名非成功逻辑内返回；假发布追根因 | 无 |
| 同上 | 4816-4827 | 区分首次发布和精确重复收敛，恢复 13 项融合编码映射 | N07 / C08 / R10 | 函数调用 / 本函数内具体指令 | 判断 / 读回 / 返回 | 无 | 权威发布见证 | 映射或发布形状错为追根因 | 无 |
| 同上 | 4828-4830 | 调 R1521 按 G1 历史读回首次结果 | C09 / R10 | 函数调用 | 读回 / 返回 | 无 | 权威历史事实 | 读回形状错为追根因 | 无 |
| 同上 | 4831-4838 | 映射资源与未知异常并闭合函数 | R10 | 本函数内具体指令 | 异常 / 返回 / 生命周期 | 无 | 当前代码事实 | 资源失败逻辑内返回；未知异常追根因 | 无 |

覆盖结论：4762—4838 的全部非空有效源码行已映射。
