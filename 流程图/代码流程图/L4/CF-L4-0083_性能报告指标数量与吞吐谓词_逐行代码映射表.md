# CF-L4-0083 `性能报告指标数量与吞吐谓词` 逐行代码映射表

| 行 | 节点 | 行为 | 失败收口 |
| --- | --- | --- | --- |
| 912 | 身份 | 冻结无捕获泛型 lambda 的规模报告实例和 const 引用参数 | 外层 all_of 可真空通过 |
| 913 | N01,N02,R01 | 读取查询指标数量并要求精确等于10 | false 后不读取写入指标 |
| 914 | N03,N04,R01 | 读取写入指标数量并要求精确等于4 | false 后不读取吞吐 |
| 915-916 | N05,N06,R01,R02 | 读取吞吐并判断严格大于0.0后返回 | 0、负数和NaN失败，正无穷可通过 |

函数不验证 14 个指标内容、样本数、分位值、结果一致或并发完成操作数；这些不能由数量和正吞吐扩大推断。项目调用 0，外部边界 X0103，未解析调用 0。
