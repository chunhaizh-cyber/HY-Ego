# CF-L4-0031 `第二次失败安装 lambda` 逐行代码映射表

代码版本：`4b80f1617dd70ec7a19e1a34efa9da768dce8927`
源码 blob：`d2581161faef19cb494366ee32886ae2b12ee190`

| 文件 | 行号 | 当前代码行为 | 流程节点 | 节点类型 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- |
| `启动.应用程序.ixx` | 290 | 建立两项引用捕获、接收信号和未命名原处理参数 | N01 | 本函数内具体指令 | 无 | 验证处理器参数 |
| 同上 | 291-293 | 调用 `array.size()` 判断容量；容量内以后置递增旧索引调用 `operator[]` 并写入信号 | N02-N06 | 函数调用 / 本函数内具体指令 | 额外调用无证据 | 记录溢出事实 |
| 同上 | 294-295 | 按信号是否为 SIGTERM 返回 SIG_ERR 或 SIG_DFL，并结束 lambda | N07-N09 | 本函数内具体指令 | 逻辑内模拟返回 | 限定允许信号集 |

覆盖结论：290—295 行全部映射；项目源码调用 0；外部静态调用点为 array size/operator[]；未解析调用 0。
