# CF-L4-0076 `D455相机采集器::请求停止` 逐行代码映射表

| 行 | 节点 | 行为 | 失败收口 |
| --- | --- | --- | --- |
| 690 | 身份 | 冻结无参数、void、noexcept 覆盖函数 | 无 |
| 691 | N01 | 以默认 `memory_order_seq_cst` 存储 true | 无结构化结果 |
| 692 | N02 | 构造 scoped_lock 并取得 `状态互斥_` | mutex 异常在 noexcept 中终止 |
| 693 | N03 | 锁内读取状态并判断是否运行中 | 无 |
| 694 | N04 | 仅 true 分支把状态改为停止请求 | 其它状态保持不变 |
| 695-696 | N05,N06 | 作用域退出解锁并 void 返回 | 无 |

函数不调用 SDK stop、不等待活动采样结束。`打开()` 若与本函数并发，锁外原子 store 可能与打开路径的清零发生覆盖；原子标志和锁内状态可能分裂，需由后续规范治理裁决。项目调用 0，外部边界 X0097，未解析调用 0。
