# CF-L4-0007 `方法服务::初始化方法登记根` 逐行代码映射表

| 文件 | 行号 | 当前代码行为 | 流程节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `领域/方法服务.h` | 254-257 | 进入F0043；拒绝零稳定键 | N01-N02 | 本函数内具体指令 | 入口准入 | 无 | 当前代码事实 | 逻辑内返回 | 无 |
| 同上 | 258-268 | 取得锁；既有键冲突拒绝；同键完整则幂等返回，不完整则诊断 | N03-N10 | 函数调用 / 本函数内具体指令 | 并发与幂等分支 | 本次无新增 | 权威仓库读回 | 冲突为逻辑内返回；不完整追根因 | 无 |
| 同上 | 270-275 | 登记方法节点、校验并绑定登记根角色 | N11-N15 | 函数调用 | 首次结构写入 | 创建主信息、节点、角色状态/关系 | 运行期仓库事实 | 失败追根因且可能留半结构 | 需要事务化撤销设计 |
| 同上 | 276-282 | 无论首状态是否有效都创建第二状态，再短路校验有效且不同 | N16-N22 | 函数调用 / 本函数内具体指令 | 生命周期状态写入 | 创建两个状态节点 | 运行期仓库事实 | 失败追根因且不回滚 | 无 |
| 同上 | 283-291 | 顺序创建两条模板关系，再校验有效及同仓库 | N23-N29 | 函数调用 / 本函数内具体指令 | 生命周期关系写入 | 创建两条关系 | 运行期仓库事实 | 失败追根因且不回滚 | 无 |
| 同上 | 293-294 | 发布服务私有登记根材料和关系仓库编号 | N30-N31 | 本函数内具体指令 | 私有发布 | 服务私有状态可读 | 派生材料 | 不适用 | 保持最后发布 |
| 同上 | 295-300 | 发布后完整读回；失败只撤回私有发布并诊断 | N32-N36 | 函数调用 / 本函数内具体指令 | 读回与局部撤回 | 仓库写入仍保留 | 权威仓库事实 | 失败追根因 | 不得宣称完整回滚 |
| 同上 | 301-302 | 复制并返回新材料；结束函数 | N37 | 本函数内具体指令 | 成功返回 | 无新增 | 结构化结果 | 不适用 | 无 |

覆盖结论：254—302行全部映射；发现9个唯一项目源码被调函数身份；外部边界为锁、optional和转换；未解析调用0。
