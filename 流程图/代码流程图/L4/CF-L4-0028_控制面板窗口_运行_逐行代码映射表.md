# CF-L4-0028 `控制面板窗口::运行` 逐行代码映射表

| 文件 | 行号 | 当前代码行为 | 流程节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `界面/控制面板窗口.cpp` | 1707 | 接收可空停止请求借用 | 函数合同 | 本函数内具体指令 | 参数绑定 | 无 | 进程宿主材料 | 无 | 冻结是否允许空指针 |
| 同上 | 1708 | 解引用构造期不变量中的实现指针并同步调用运行 | N01-N02 | 函数调用 | PImpl 转发 | 被调函数承担全部 UI 状态变化 | 人读 UI 宿主 | 异常向上传播 | 无 |
| 同上 | 1708-1709 | 原样返回被调结构化结果 | N03 | 本函数内具体指令 | 返回 | 无新增 | 结构化窗口结果 | 不改写失败 | 无 |

覆盖结论：1707—1709 行全部映射；项目源码调用只新增 F0223；Win32 调用 0；未解析调用 0。
