# CF-L4-0008 `自我线程::启动` 逐行代码映射表

| 文件 | 行号 | 当前代码行为 | 流程节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `线程/自我线程.ixx` | 182-186 | 读取快照；按 `has_value && 成功()` 短路；完整快照幂等返回 | N01-N04 | 函数调用 / 本函数内具体指令 | 快照复用 | 无 | 结构化快照副本 | 逻辑内成功复用 | 无 |
| 同上 | 187-189 | 按状态先判、`joinable` 后判的短路顺序拒绝重复启动 | N05-N09 | 函数调用 / 本函数内具体指令 | 重复准入 | 无 | 原子状态与线程对象 | 逻辑内拒绝 | 并发检查需统一串行门禁 |
| 同上 | 190-192 | 校验根需求参数；无效时结构化拒绝 | N10-N12 | 函数调用 / 本函数内具体指令 | 参数准入 | 无 | 参数合同 | 逻辑内拒绝 | 无 |
| 同上 | 193-197 | 分别确认邮箱指针和处理器槽；仅接入一方时拒绝 | N13-N17 | 函数调用 / 本函数内具体指令 | 治理依赖准入 | 无 | 运行装配事实 | 逻辑内拒绝 | 无 |
| 同上 | 199-202 | 清除停止/进入标志，重置治理读数，发布初始化中 | N18-N21 | 函数调用 | 启动前状态变更 | 多项原子状态变化 | 线程运行状态 | 不适用 | 构造失败时应裁决是否恢复 |
| 同上 | 203-206 | 构造捕获 `this` 和参数副本的闭包；创建并调度新线程；移动赋值成员线程 | N22-N25、N32-N33 | 函数调用 / 本函数内具体指令 | 并发启动 | 新线程开始执行 | 线程调度事实 | `system_error` 进入捕获；异步异常不回传 | 明确单次启动串行化和线程入口异常收口 |
| 同上 | 207-211 | 捕获 `system_error`，标记初始化失败、通知并返回拒绝 | N27-N31 | 函数调用 / 本函数内具体指令 | 构造失败收口 | 状态变为初始化失败 | 结构化结果与原子状态 | 逻辑内拒绝 | 已重置读数未恢复 |
| 同上 | 212-213 | 线程建立后返回受理成功；不等待初始化 | N26 | 本函数内具体指令 | 异步受理返回 | 无新增 | 结构化启动结果 | 不适用 | 调用方须另等初始化并读快照 |

覆盖结论：182—213 行全部映射；发现 6 个唯一项目源码被调实体，其中 F0171 为异步线程入口；外部边界为 optional、atomic、thread、function、condition_variable 和 system_error；未解析调用 0。
