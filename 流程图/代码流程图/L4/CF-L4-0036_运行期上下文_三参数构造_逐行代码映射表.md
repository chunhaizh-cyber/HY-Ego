# CF-L4-0036 `运行期上下文::运行期上下文` 三参数构造逐行代码映射表

| 行 | 节点 | 类型 | 参数 / 结果 / 行为 |
| --- | --- | --- | --- |
| 45 / 178 | N01 | 函数调用 | `域编号`构造`协调器_` |
| 46 / 179 | N02-N03 | 函数调用、指令 | 生成接线并直接初始化`接线_` |
| 47 / 180 | N04-N05 | 函数调用 | 复制接线；以`仓库编号`和副本构造`主信息_` |
| 48 / 181 | N06-N07 | 函数调用 | 复制接线；借用`主信息_`构造`节点_` |
| 49 / 182 | N08-N09 | 函数调用 | 复制接线；借用`节点_`构造`关系_` |
| 50 / 183 | N10-N11 | 函数调用 | 复制接线；借用`节点_`构造`索引_` |
| 51-52 / 184 | N12 | 函数调用 | 借用接线和四仓库，绑定仓库编号、动作键，构造`业务装配_` |
| 53 / 185 | N13 | 指令 | 保存仓库编号 |
| 186-189 | N14-N17 | 函数调用 | 默认构造两把锁和两个空 optional |
| 53-54 | N18 | 指令 | 空函数体，构造完成 |

项目调用为 F0232—F0238；编译器/标准库边界统一登记为 X0062。所有成员值式归上下文所有，业务装配和仓库借用的依赖对象均先构造、后析构。未解析调用 0。
