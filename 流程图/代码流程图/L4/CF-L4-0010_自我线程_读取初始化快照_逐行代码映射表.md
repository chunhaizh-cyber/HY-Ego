# CF-L4-0010 `自我线程::读取初始化快照` 逐行代码映射表

| 文件 | 行号 | 当前代码行为 | 流程节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `线程/自我线程.ixx` | 230-231 | 进入函数并以 RAII 取得快照互斥 | N01 | 函数调用 | 读取同步 | 无 | 进程内同步 | 锁异常向上传播 | 无 |
| 同上 | 232 | 在锁内复制可选快照并形成返回值 | N02-N03 | 函数调用 / 本函数内具体指令 | 快照读取 | 无 | 结构化快照副本 | 复制异常向上传播 | 无 |
| 同上 | 233 | 返回边界释放锁 | N04 | 函数调用 | 生命周期收口 | 无 | 进程内同步 | 不适用 | 无 |

覆盖结论：230—233 行全部映射；项目源码被调函数 0；外部边界为 `lock_guard` 与 `optional` 复制；未解析调用 0。
