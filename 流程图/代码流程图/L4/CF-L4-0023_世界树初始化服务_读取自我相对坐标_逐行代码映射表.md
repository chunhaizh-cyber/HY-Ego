# CF-L4-0023 `世界树初始化服务::读取自我相对坐标` 逐行代码映射表

| 文件 | 行号 | 当前代码行为 | 流程节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `领域/初始化.世界树.ixx` | 88-89 | 按输入句柄读取节点记录 | N01 | 函数调用 | 节点读取 | 无 | 运行期节点仓库 | 下游 optional | 无 |
| 同上 | 90-91 | 短路检查记录存在且节点类型为存在，否则返回空 | N02-N05 | 函数调用 / 本函数内具体指令 | 输入准入 | 无 | 节点记录 | 逻辑内空返回 | 验证自我身份和世界树归属 |
| 同上 | 93-95 | 严格顺序从同一主信息句柄读取槽 0、1、2 | N06-N11 | 函数调用 | 坐标材料读取 | 无 | 运行期主信息仓库 | 各调用返回 optional | 使用具名结构与共享版本 |
| 同上 | 96-97 | 左到右短路检查三槽都有值，否则返回空 | N12-N14,N05 | 函数调用 / 本函数内具体指令 | 完整性检查 | 无 | 读取结果 | 逻辑内空返回 | 区分允许缺失与内部结构错误 |
| 同上 | 99-103 | 左到右取值、转 double、聚合坐标并构造 optional 返回 | N15-N22 | 函数调用 / 本函数内具体指令 | 返回组装 | 构造值对象 | 非权威只读材料 | `optional::value` 前置已证明 | 防止大整数精度丢失 |
| 同上 | 104 | 函数结束 | N22 | 本函数内具体指令 | 生命周期收口 | 局部 optional 析构 | 不适用 | 标准库析构边界 | 无 |

覆盖结论：88—104 行全部映射；项目源码调用为既有 F0190 和新增 F0217，共 4 个静态调用点；外部边界为 optional 操作；未解析调用 0。
