# CF-L4-0009 `自我线程::等待初始化完成` 逐行代码映射表

| 文件 | 行号 | 当前代码行为 | 流程节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `线程/自我线程.ixx` | 215-216 | 进入函数并取得快照互斥独占锁 | N01 | 函数调用 | 等待同步 | 无 | 进程内同步 | 锁异常向上传播 | 无 |
| 同上 | 217-224 | 构造状态谓词并以给定时限等待；谓词可被重复调用 | N02-N04 | 函数调用 / 本函数内具体指令 | 有界等待 | 无 | 原子线程状态 | 超时为逻辑内返回候选 | 无 |
| 同上 | 225-227 | 按 `等待结束 && 快照有值 && 快照成功` 的短路顺序形成返回值 | N05-N09 | 函数调用 / 本函数内具体指令 | 结果裁决 | 无 | 结构化初始化快照 | 超时、失败、停止或快照不完整均返回 false | 无 |
| 同上 | 228 | 函数结束 | N08-N09 | 本函数内具体指令 | 返回 | 无 | 结构化布尔结果 | 不适用 | 无 |

覆盖结论：215—228 行全部映射；发现 2 个唯一项目源码被调实体，其中 F0172 为条件变量谓词；外部边界为 unique_lock、condition_variable 和 optional；未解析调用 0。
