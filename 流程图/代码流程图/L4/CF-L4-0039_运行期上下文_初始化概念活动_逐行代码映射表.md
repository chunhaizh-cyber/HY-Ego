# CF-L4-0039 `运行期上下文::初始化概念活动` 逐行代码映射表

| 行 | 节点 | 行为 | 条件 / 结果 |
| --- | --- | --- | --- |
| 130 | N01-N07 | 结构核心、业务装配、参数有效性严格短路 | 任一失败返回默认入口拒绝 |
| 131-133 | N08-N17 | 读取清单，检查存在/完整并复核系统角色 | 任一失败返回默认入口拒绝 |
| 135-136 | N18-N20 | 取得概念活动锁并检查缓存 | 值式 optional |
| 137-139 | N21-N25 | 已缓存材料匹配参数 | 不匹配返回幂等冲突 |
| 140-142 | N26-N30 | 锁内复核缓存材料 | 成功改写幂等读回后返回 |
| 144-146 | N31-N36 | 解锁后执行首次初始化并检查结果 | 失败原样返回 |
| 147-149 | N37-N44 | 再加锁检查并发发布的一致性 | 不同材料返回内部不一致 |
| 151-153 | N45-N46 | 复制发布结果材料并返回 | 已有相同材料也再次覆盖 |

项目源码唯一被调身份 12 个，其中 F0076 有两个静态调用点。真实仓库事务由 F0249 内部承担；本函数不持有结构事务令牌，也不撤销已提交结构。外部 optional、锁和 DTO 生命周期统一登记为 X0064。未解析调用 0。
