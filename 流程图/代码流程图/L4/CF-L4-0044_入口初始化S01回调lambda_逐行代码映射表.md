# CF-L4-0044 入口初始化 S01 回调 lambda 逐行代码映射表

| 行 | 节点 | 行为 | 编译变体 / 结果 |
| --- | --- | --- | --- |
| 486 | N01 | 进入回调，配置来自按值捕获成员 | 每次只读同一闭包副本 |
| 486 | N02-N03 | 调用 F0254 并形成局部结构化结果 | 无条件 |
| 486 / 日志宏定义 | N04-N06 | 读取总通过、选择文本、调用 F0036 | 日志 bool 丢弃 |
| 486 / 日志宏关闭 | N04,N07 | `do{}while(false)`空展开 | 日志专用实参完全不求值 |
| 486 | N08 | 原样返回自检结果 | 日志不得改变字段或失败计数 |

项目调用 2，其中 F0036 仅存在于宏开启构建。日志函数若抛异常会阻止结构化结果返回，属于日志隔离合同偏差候选；宏关闭路径本身符合“不求值日志专用材料”。外部宽字符串视图与可选移动/NRVO统一登记为 X0068。未解析调用 0。
