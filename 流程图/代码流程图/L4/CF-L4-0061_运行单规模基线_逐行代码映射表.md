# CF-L4-0061 `运行单规模基线` 逐行代码映射表

| 行 | 节点 | 行为 | 失败收口 |
| --- | --- | --- | --- |
| 756-760 | N01-N02 | 默认构造报告并写规模、固定种子 | 构造异常传播 |
| 761-764 | N03-N08 | 构造夹具、记录开始时间、构建；失败立即返回部分报告；成功记录结束时间 | `构建()==false` 没有原因字段 |
| 766-769 | N09-N16 | 分别调用四个夹具读取/验证函数，再逐字段写报告 | 异常传播 |
| 771-774 | N17-N19 | 形成查询定义与两个同尺寸容器，按规模选 64/8/1 次批内重复 | 空或不足两个查询定义未显式拒绝 |
| 776-798 | N20-N41 | 三轮遍历每项查询；预热累计正确性；正式测量累计耗时和正确性；形成每轮 P50 | `vector<bool>` 使用代理读写；异常传播 |
| 800-806 | N42-N46 | 为每个查询形成并追加分位指标 | 异常传播 |
| 807-810 | N47-N48 | 求三轮最小/最大中位数，计算波动比例与 15% 稳定门禁 | 固定数组非空 |
| 812-814 | N49-N52 | 运行并发基线并验证参考表，分别写报告 | 下标 1 依赖查询组和指标组至少有两项 |
| 815-818 | N53-N60 | 依次测量并追加创建、失效、重挂、删除指标 | 任何异常中断后续测量 |
| 819 | N61-N63 | 借用关系仓库，读取有效关系数量并与规模比较 | 只比较总量 |
| 821 | N64 | 把构建时长转换为毫秒 | 仅观察材料 |
| 822-831 | N65 | 经 `std::cout` 输出规模、签名、构建时长和轮次波动 | 人读旁路；输出异常传播 |
| 832-839 | N66-N67 | 遍历并输出所有查询指标 | 人读旁路 |
| 840-847 | N68-N69 | 遍历并输出所有写入指标 | 人读旁路 |
| 848-853 | N70 | 输出并发指标和等待代理说明 | 人读旁路 |
| 854-855 | N71 | 按值返回报告并析构局部资源 | 返回可为部分报告 |

项目调用 F0272-F0288 及既有 F0198；F0279 在预热和正式测量循环被重复调用。外部边界 X0085。直接 `std::cout`、部分报告无状态、下标前置不显式以及异常无结构化收口是偏差候选。未解析调用 0。
