# CF-L4-0064 `生成D455默认采集配置` 逐行代码映射表

| 行 | 节点 | 行为 | 失败收口 |
| --- | --- | --- | --- |
| 169 | N01 | 绑定按值超时参数；默认值 2000 只属于调用合同 | 无运行期校验 |
| 170 | N01 | 默认初始化配置，设备序列号为空 | 标准库异常传播 |
| 171 | N02 | 赋彩色流固定字段 | 无 |
| 172 | N03 | 赋深度流固定字段 | 无 |
| 173 | N04 | 赋左红外固定字段 | 无 |
| 174 | N05 | 赋右红外固定字段 | 无 |
| 175 | N06 | 原样写入超时 | 0 或异常策略值不拒绝 |
| 176-177 | N07 | 按值返回配置并收口局部生命周期 | 不证明真实设备可打开 |

项目调用 0；外部边界 X0088。空设备身份、固定能力参数、未调用配置有效性校验以及无结构化错误是偏差候选。未解析调用 0。
