# CF-L4-0016 `数据库启动审计端口::完整` 逐行代码映射表

| 文件 | 行号 | 当前代码行为 | 流程节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `适配/审计.数据库启动.ixx` | 25-29 | 依次按短路顺序检查四个回调槽是否非空 | N01-N04 | 函数调用 | 端口完整性 | 无 | 运行装配事实 | 任一空槽返回 false | 无 |
| 同上 | 30 | 返回四项合取结果 | N05-N06 | 本函数内具体指令 | 返回 | 无 | 结构化布尔结果 | 不适用 | 无 |

覆盖结论：25—30 行全部映射；项目源码被调函数 0；外部边界为四次 `std::function::operator bool`；未解析调用 0。
