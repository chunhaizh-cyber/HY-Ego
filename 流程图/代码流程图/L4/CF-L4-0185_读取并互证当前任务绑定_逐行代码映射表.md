# CF-L4-0185 读取并互证当前任务绑定逐行代码映射表

代码版本：`dd8a7810`；源码 blob：`597af149798c2925b38406f95432c074dba28034`；根函数：R1525。

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/内部治理/服务.任务目标未达成重筹办触发.ixx` | 240-248 | 定义函数并按原共同事实截止读取任务 | C01 / N02 / R09 | 函数调用 / 本函数内具体指令 | 读取 / 判断 / 返回 | 无 | task owner 任务投影 | 具名读取非成功为逻辑内返回 | 无 |
| 同上 | 250-259 | 取当前代次，调用 R1529 读取分区并读取旧实例 | C03 / C04 / R09 | 函数调用 / 本函数内具体指令 | 读取 / 判断 / 返回 | 无 | task owner 当前投影 | 具名读取非成功为逻辑内返回 | 无 |
| 同上 | 261-271 | 按实例路径读取路径并读取旧实际结果 | C04 / R09 | 函数调用 / 本函数内具体指令 | 读取 / 判断 / 返回 | 无 | task owner 当前投影 | 具名读取非成功为逻辑内返回 | 无 |
| 同上 | 273-292 | 互证任务、路径、实例、结果、裁决材料和原 G 可见性 | N05 / R09 | 本函数内具体指令 | 判断 / 返回 | 无 | 当前事实与意图引用 | 引用冲突为逻辑内返回 | 无 |
| 同上 | 293-300 | 统一分区要求实例轮次等于统一当前轮次；legacy 直接保留；其它为内部错误 | N06 / N07 / R09 | 本函数内具体指令 | 分流 / 判断 / 返回 | 无 | 统一权威或 legacy 分区 | 冲突逻辑内返回；内部形状错追根因 | 无 |
| 同上 | 301-307 | 输出四类事实、分区及可选统一当前准备并返回已形成 | P08 | 本函数内具体指令 | 输出 / 返回 | 无 | 互证后的当前事实 | 不适用 | 无 |

覆盖结论：240—307 的全部非空有效源码行已映射。
