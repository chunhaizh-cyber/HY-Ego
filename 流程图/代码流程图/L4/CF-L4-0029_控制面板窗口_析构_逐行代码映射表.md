# CF-L4-0029 `控制面板窗口::~控制面板窗口` 逐行代码映射表

| 文件 | 行号 | 当前代码行为 | 流程节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `界面/控制面板窗口.cpp` | 1705 | 进入显式默认化析构 | N01 | 本函数内具体指令 | 析构入口 | 无 | 不适用 | noexcept 边界 | 无 |
| `界面/控制面板窗口.h` | 54-55 | 逆声明顺序析构唯一 unique_ptr 成员 | N02-N03 | 函数调用 | 所有权释放 | 准备删除 PImpl | UI 实现状态 | 空指针时标准库跳过 delete | 无 |
| `界面/控制面板窗口.cpp` | 309-315 | delete 表达式进入具名窗口实现析构 | N04 | 函数调用 | 被拥有对象析构 | Win32 资源由被调函数收口 | 人读 UI 资源 | 被调析构无结构化返回 | 在 F0224 独立展开 |
| 外部边界 | 隐式 | 释放 PImpl 堆存储并完成外层析构 | N05-N06 | 函数调用 / 本函数内具体指令 | 存储释放 | 实现对象生命周期结束 | 不适用 | operator delete 边界 | 无 |

覆盖结论：第 1705 行默认析构及唯一拥有成员的生成语义全部映射；项目源码调用只新增 F0224；未解析调用 0。
