# CF-L4-0037 `运行期上下文::初始化系统角色` 逐行代码映射表

| 行 | 节点 | 行为 | 条件 / 结果 |
| --- | --- | --- | --- |
| 95 | N01 | 进入函数 | 只读借用参数 |
| 96 | N02-N05 | 三项严格短路准入 | 任一失败返回默认入口拒绝 |
| 97-99 | N06-N07 | 取得缓存锁并检查 optional | 锁保护缓存 |
| 100-101 | N08-N09 | 已缓存时匹配参数 | 异参返回幂等冲突，退出前解锁 |
| 103-105 | N10-N12 | 锁内复核真实结构 | 成功状态改写为幂等读回后返回 |
| 107 | N13 | 释放第一把锁 | 仅空缓存路径继续 |
| 108-109 | N14-N15 | 锁外执行下层初始化并检查成功 | 失败结果原样返回 |
| 110-112 | N16-N18 | 第二次加锁并检查并发缓存 | 不同清单返回内部不一致 |
| 114 | N19 | 复制结果清单到 optional | 相同已有值也覆盖 |
| 115-116 | N20 | 返回下层结果 | 退出前解锁 |

项目调用静态点 9 个、唯一身份 8 个：F0239、F0240、F0228、F0241、F0242、F0074、F0243、F0244；F0074 有两处调用。锁序反转、准入与下层写之间的 TOCTOU、提交后缓存冲突不回滚均保留为偏差候选。未解析调用 0。
