# CF-L4-0060 性能基线无效参数写前拒绝 lambda 逐行代码映射表

| 行 | 节点 | 行为 | 失败收口 |
| --- | --- | --- | --- |
| 877 | N01 | 定义无捕获立即调用 lambda，并准备把返回值绑定到 `无效参数写前拒绝` | 宏关闭时实体不存在 |
| 878 | N01-N02 | 形成规模 0 和固定种子两个无副作用实参，构造栈上隔离夹具 | 构造异常向外传播 |
| 879 | N03-N05 | 调用 `构建()`、对结果取反并形成返回值 | `false` 的原因未结构化区分；异常向外传播 |
| 879-880 | N06-N07 | 返回值形成后析构夹具，再把 bool 交给调用方 | 析构属于编译器生命周期边界 |

项目调用 F0272、F0273；外部/编译器边界 X0084。该函数不能单独证明零仓库写入或拒绝原因。未解析调用 0。
