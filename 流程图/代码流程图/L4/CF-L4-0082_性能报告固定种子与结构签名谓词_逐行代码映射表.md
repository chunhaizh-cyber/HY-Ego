# CF-L4-0082 `性能报告固定种子与结构签名谓词` 逐行代码映射表

| 行 | 节点 | 行为 | 失败收口 |
| --- | --- | --- | --- |
| 893 | 身份 | 冻结无捕获泛型 lambda 的规模报告实例和 const 引用参数 | 外层 all_of 可真空通过 |
| 894 | N01,N02,N03,N04 | 先比较随机种子；成功后才读取并比较结构签名 | 任一 false 短路返回 |
| 895 | 结束 | 把 bool 交还外层 all_of | 无 |

非零签名无法发现错误但非零的值、跨规模互换或实现漂移；其余规模、分布、指标和并发事实由其它验收项承担。项目/外部调用 0，未解析调用 0。
