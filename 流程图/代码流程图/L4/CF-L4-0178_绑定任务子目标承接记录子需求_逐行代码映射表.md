# CF-L4-0178 绑定任务子目标承接记录子需求逐行代码映射表

代码版本：`d534058e`；源码 blob：`6e13c91095c9bbfa4361358bc8bf26b0d9bb9a9e`；根函数：R1513。

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/服务.L2任务子目标承接记录结构.ixx` | 134-137 | 定义函数并验证绑定请求 | N01 / R09 | 函数调用 / 本函数内具体指令 | 入口 / 判断 / 返回 | 无 | 当前代码事实 | 逻辑内返回 | 无 |
| 同上 | 138-140 | 读取当前记录 | C02 | 函数调用 | 读取 | 无 | 权威事实读取 | 不适用 | 无 |
| 同上 | 141-142 | 旧截止满足收敛条件时，以原请求直接调用记录 owner 已验证绑定写入口 | N03 / C04 / R09 | 函数调用 / 本函数内具体指令 | 判断 / 写入 / 返回 | 可能按原幂等键收敛记录绑定 | 权威事实写入 | 被调函数结构化返回 | 无 |
| 同上 | 143-145 | 非收敛路径要求记录读取成功且载荷存在，否则映射外部非成功 | N05 / R09 | 本函数内具体指令 | 判断 / 返回 | 无 | 权威事实读取 | 逻辑内返回；形状错追根因 | 无 |
| 同上 | 146-149 | 验证子需求成员链 | C06 / R09 | 函数调用 / 本函数内具体指令 | 读取 / 判断 / 返回 | 无 | 权威事实读取 | 引用冲突逻辑内返回；形状错追根因 | 无 |
| 同上 | 150 | 调用任务记录 owner 已验证绑定写入口 | C07 / R09 | 函数调用 | 写入 / 返回 | 绑定子需求并迁移记录状态 | 权威事实写入 | 被调函数结构化返回 | 无 |
| 同上 | 151-155 | 映射资源和内部异常 | N08 / R09 | 本函数内具体指令 | 异常 / 返回 | 无 | 当前代码事实 | 资源逻辑内返回；内部不一致追根因 | 无 |
| 同上 | 156 | 闭合函数 | R09 | 本函数内具体指令 | 生命周期 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：134—156 全部非空有效源码行已映射。
