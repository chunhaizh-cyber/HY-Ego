# CF-L4-0022 `接收程序停止信号` 逐行代码映射表

| 文件 | 行号 | 当前代码行为 | 流程节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `启动.程序运行宿主.ixx` | 100 | 进入异步处理器；信号编号未命名且未消费 | N01 | 本函数内具体指令 | 回调入口 | 无 | 进程宿主材料 | 无 | 明确信号来源合同 |
| 同上 | 101 | 读取普通静态当前租约指针并判断非空 | N02 | 本函数内具体指令 | 宿主状态读取 | 无 | 进程宿主材料 | 空指针时逻辑内返回 | 消除普通裸指针异步数据竞争 |
| 同上 | 102 | 再次读取指针并把 volatile sig_atomic_t 停止位设为 1 | N03 | 本函数内具体指令 | 停止请求发布 | 宿主控制位变化 | 非领域机器事实 | 无 | 一次获取稳定信号安全目标 |
| 同上 | 103-104 | 离开条件并返回 void | N04 | 本函数内具体指令 | 返回 | 无 | 不适用 | 无 | 无 |

覆盖结论：100—104 行全部映射；体内项目调用 0、外部调用 0、未解析调用 0；三类信号注册和 CRT/OS 异步调用属于入边，不伪装为体内出边。
