# CF-L4-0075 `D455帧材料缓存::按编号版本读取` 逐行代码映射表

| 行 | 节点 | 行为 | 失败收口 |
| --- | --- | --- | --- |
| 228 | 身份 | 冻结按值句柄、const 成员函数和结构化返回合同 | 无 |
| 229-230 | N01-N03 | 调用 F0299 并判断句柄有效性 | 无效时不加锁，返回版本不匹配 |
| 232 | N04 | 构造 scoped_lock 并独占取得 `互斥_` | 加锁异常向外传播 |
| 233 | N05 | 按材料编号查找条目 | 无 |
| 234-235 | N06-N08,N14 | 取得 end 并判断查找结果 | 未找到返回目标不存在，随后解锁 |
| 237-238 | N09,N10,N14 | 比较存储版本与请求版本 | 不同则返回版本不匹配，随后解锁 |
| 240-241 | N11,N12,N14 | 检查条目是否处于已发布状态 | 否则返回状态不匹配，随后解锁 |
| 243-245 | N13,N14 | 复制句柄、共享只读材料和字节数，构造成功结果并解锁返回 | 构造异常按语言规则展开并解锁 |

成功路径没有复核材料非空、字节数与内容一致性或材料年龄；失败结果不携带实际版本和状态。读取使用独占 mutex，多个读者仍串行。项目调用 1，外部边界 X0096，未解析调用 0。
