# CF-L4-0070 `D455采样材料上行桥::D455采样材料上行桥` 逐行代码映射表

| 行 | 节点 | 行为 | 失败收口 |
| --- | --- | --- | --- |
| 50 | N01 | 绑定两个非拥有引用参数 | 调用方负责生命周期 |
| 51 | N01-N02 | 按成员声明顺序绑定帧来源与缓存别名 | 不读取或验证依赖 |
| 51-52 | N03 | 完成 noexcept 空函数体 | 不建立关闭责任 |
| 189-190 | N01-N02 | 引用成员声明提供实际初始化顺序与所有权证据 | 依赖先析构将产生空悬 |

项目调用、外部边界和未解析调用均为 0。生命周期仅靠调用方纪律、复制桥会继续共享别名、错误配对与并发安全未验证是偏差候选。
