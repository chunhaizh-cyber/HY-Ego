# CF-L4-0047 入口初始化 S04 回调 lambda 逐行代码映射表

| 行 | 节点 | 行为 | 结果 |
| --- | --- | --- | --- |
| 492 | N01-N02 | 调用 F0257 并保存结构化结果 | 无条件 |
| 492 / 宏启用 | N03-N05 | 选择文本并调用 F0036 | 日志 bool 丢弃 |
| 492 / 宏关闭 | N03,N06 | 空展开 | 日志专用实参不求值 |
| 492 | N07 | 原样返回结果 | 日志不裁决机器事实 |

项目调用 2，其中日志边为编译期条件边；外部边界为 X0071。日志调用异常会使宏开关改变结构化返回行为，是隔离合同候选。未解析调用 0。
