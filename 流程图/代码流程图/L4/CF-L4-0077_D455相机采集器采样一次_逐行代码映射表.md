# CF-L4-0077 `D455相机采集器::采样一次` 逐行代码映射表

| 行 | 节点 | 行为 | 失败收口 |
| --- | --- | --- | --- |
| 590 | 身份 | 冻结虚覆盖函数、按值超时和结构化采集结果 | 无 |
| 591-593 | N01,R01 | 拒绝零超时 | 配置无效 |
| 594-604 | N02-N05,R02,R03 | 锁内读取停止标志、状态、启动标志和管线 | 已停止或当前未运行 |
| 607-611 | N06-N10 | 调用 F0316 构造错误槽、接收 SDK 错误指针、等待同步帧并立即接管裸帧 | try 内异常进入 E01 |
| 612-616 | N11,N12,R04,R05 | 读取错误槽并按停止标志区分停止与设备断开 | 已停止或设备断开 |
| 617-619 | N13,R06 | 按左到右短路检查取得标志和同步帧所有权 | 等待超时 |
| 620-622 | N14,R07 | SDK 等待后再次检查停止请求 | 已停止 |
| 624-628 | N15-N19,R08 | 调用 F0316 构造数量错误槽，取得嵌入帧数量并检查错误及正数 | 同步帧不完整 |
| 629-656 | N20-N34,R09,R10 | 每轮调用 F0316 构造提取错误槽，提取并立即接管帧，读取流身份后把首次匹配帧移入四路槽 | 提取失败为不完整；身份失败为内部不一致 |
| 657-659 | N35,R11 | 按彩色、深度、左红外、右红外顺序检查四路槽 | 任一为空即不完整 |
| 661-666 | N36-N39 | 构造共享材料；锁内复制设备身份并先递增完成批次数 | 异常进入 E01 |
| 667 | N40,N41 | 调用当前单调时间并写入宿主接收时间戳 | 异常进入 E01 |
| 668-675 | N42-N48,R12 | 依次复制四平面和读取三组外参；每次调用后立即短路判断 | 任一 false 即材料无效并追根因 |
| 677 | N49,N50 | 计算并写标定摘要 | 异常进入 E01 |
| 678-680 | N51,R13 | 调用 F0108 复核完整材料 | false 返回材料无效并追根因 |
| 681 | R14 | move 共享材料并转换为只读共享所有权后返回成功 | 无 |
| 683-687 | E01-E03,R15 | 捕获 try 内任意异常，锁内置故障并返回内部不一致 | catch 内加锁异常仍向外传播 |
| 608-687 | L01 | 所有作用域退出按逆序调用 F0309/F0310 释放错误与非空 SDK 帧 | 不暴露 SDK 缓冲区 |

偏差候选：完成批次数在材料最终校验前递增且失败不回滚；初始锁释放后继续使用管线，和 `关闭()` 的无锁 reset 存在生命周期/数据竞争窗口；复制阶段收到停止请求仍可能成功返回；额外、未知或重复帧被静默忽略；错误分类粒度不足。项目被调函数 11 个，外部边界 X0098，未解析调用 0。
