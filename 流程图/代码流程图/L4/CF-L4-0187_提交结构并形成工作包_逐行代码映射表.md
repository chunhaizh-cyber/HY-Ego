# CF-L4-0187 提交结构并形成工作包逐行代码映射表

代码版本：`dd8a7810`；源码 blob：`597af149798c2925b38406f95432c074dba28034`；根函数：R1527。

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/内部治理/服务.任务目标未达成重筹办触发.ixx` | 413-429 | 定义函数，组装并提交执行结果待验证首态，要求状态和裁决证据完整 | C01 / N02 / R10 | 函数调用 / 本函数内具体指令 | 组装 / 提交 / 返回 | 可能建立首态 | task owner 治理状态 | 具名提交非成功为逻辑内返回 | 无 |
| 同上 | 431-441 | 以首态 G 组装待重筹办迁移公共字段并回显分区 | P03 | 本函数内具体指令 | 组装 | 无 | 已发布首态与绑定材料 | 不适用 | 无 |
| 同上 | 442-458 | 统一分区补齐权威、首次记录、前一后继、N、触发和准备幂等材料；legacy 不补 | N04 / P05 / R10 | 本函数内具体指令 | 分流 / 组装 / 返回 | 无 | 统一当前准备 | 缺统一准备为追根因 | 无 |
| 同上 | 459-465 | 调 R1528；若返回统一引用冲突且带后继记录，则返回触发已消费及记录身份 | C06 / N07 / R10 | 函数调用 / 本函数内具体指令 | 提交 / 判断 / 返回 | 由下游决定 | task owner 迁移结果 | 已消费为逻辑内幂等收敛 | 无 |
| 同上 | 466-468 | 验证迁移、目标状态、证据、旧路径和旧实例完整 | N08 / R10 | 本函数内具体指令 | 判断 / 返回 | 无 | 迁移发布见证 | 具名非成功逻辑内返回；假成功追根因 | 无 |
| 同上 | 470-472 | 调 R1524 在迁移后 G 读回并形成工作包 | C09 | 函数调用 | 读回 / 返回 | 无 | task owner 当前投影 | 由 R1524 分类 | 无 |

覆盖结论：413—472 的全部非空有效源码行已映射。
