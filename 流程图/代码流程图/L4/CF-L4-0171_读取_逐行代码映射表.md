# CF-L4-0171 节点直接事务幂等记录读取逐行代码映射表

代码版本：`4b80f1617dd70ec7a19e1a34efa9da768dce8927`；源码 blob：`51f626fd79eb63dba5b57bf329ef3aae0a59abbc`
覆盖文件：`海中鱼巣/核心/仓库.节点直接事务幂等.ixx:83-90`；根函数：R1315
配对流程图：`流程图/代码流程图/L4/CF-L4-0171_读取_现状流程图.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/仓库.节点直接事务幂等.ixx` | 83 | 定义 R1315，接收身份并声明按值可选记录结果 | D02 | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 84 | 调用 R1286 判断身份完整；不完整时经 X06236 构造并返回空结果 | C01 / D02 / R03 | 函数调用 / 外部边界 / 本函数内具体指令 | 调用 / 判断 / 构造 / 返回 | 无 | 当前代码事实 | 逻辑内返回 | 把 RCE4291 调用点改为第 84 行 |
| 同上 | 85 | 经 X06234 构造共享锁并取得记录读锁 | N04 | 外部边界 | 资源 / 读取 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 86 | 调用 R1339 查找只读记录位置 | C05 | 函数调用 | 调用 / 读取 | 无 | 当前代码事实 | 不适用 | 把 RCE4292 调用点改为第 86 行 |
| 同上 | 87-89 | 经 X05815-X05817 判断和访问位置；经 X06236 构造记录 optional 或空 optional 后返回 | D06 / R07 / R03 | 外部边界 / 本函数内具体指令 | 判断 / 读取 / 构造 / 返回 | 无 | 当前代码事实 | 逻辑内返回 | 无 |
| 同上 | 90 | 结束函数并经 X06235 析构共享锁释放读锁 | R07 / R03 | 外部边界 / 本函数内具体指令 | 生命周期 / 函数边界 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：83-90 全部非空有效源码行已映射；C01、D02、R03、N04、C05、D06、R07 全部节点可反查。
