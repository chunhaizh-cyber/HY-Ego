# CF-L4-0020 `SQL数据库适配::读取最近结构统计审计` 逐行代码映射表

| 文件 | 行号 | 当前代码行为 | 流程节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `适配/SQL数据库适配.cpp` | 471-476 | 值初始化返回结构并拒绝 0 或大于 100 的数量 | N01-N05 | 函数调用 / 本函数内具体指令 | 查询准入 | 无 | 输入合同 | 逻辑内拒绝 | 无 |
| 同上 | 477-481 | 初始化数据库并透传失败 | N06-N09 | 函数调用 / 本函数内具体指令 | 查询前置 | 可能幂等建库建表 | SQL审计投影 | 初始化结果原样透传 | 区分纯查询与建表职责 |
| 同上 | 483-493 | 构造连接和语句，拒绝连接失败并追根因处理语句分配失败 | N10-N21 | 函数调用 / 本函数内具体指令 | ODBC装配 | 打开并持有临时资源 | 外部数据库连接 | 逻辑内 / 追根因 | 无 |
| 同上 | 494-502 | 构造固定 TOP 查询、直接执行并收口诊断 | N22-N32 | 函数调用 / 本函数内具体指令 | 查询执行 | 建立结果集 | 数据库审计投影 | 追根因错误 | 无 |
| 同上 | 504-513 | 循环 Fetch；无数据结束；失败时清空已读前缀 | N33-N41 | 函数调用 / 本函数内具体指令 | 结果集遍历 | 临时记录组可能被清空 | 非权威查询材料 | 追根因且不返回半记录组 | 无 |
| 同上 | 514-526 | 按短路顺序读取八列；任一空值、负值或类型失败即清空 | N42-N61 | 函数调用 / 本函数内具体指令 | 行转换 | 只构造局部记录 | 非权威查询材料 | 追根因且不返回半记录组 | 检测文本截断 |
| 同上 | 527-528 | 把完整记录移动追加并回到 Fetch | N62-N33 | 函数调用 | 记录累计 | 返回记录组增长 | 非权威查询材料 | 分配异常由标准库边界裁决 | 无 |
| 同上 | 529-531 | 空集或全部记录转换完成后返回成功；逆序析构语句与连接 | N63-N66 | 函数调用 / 本函数内具体指令 | 成功返回与资源收口 | 释放 ODBC 资源 | 结构化查询结果 | 无 | 无 |

覆盖结论：471—531 行全部映射；项目源码调用只新增 F0216，其余复用 F0202—F0215；外部边界为 ODBC API、字符串流和标准容器操作；未解析调用 0。
