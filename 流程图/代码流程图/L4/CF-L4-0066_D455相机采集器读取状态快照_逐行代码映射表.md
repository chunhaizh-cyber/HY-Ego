# CF-L4-0066 `D455相机采集器::读取状态快照` 逐行代码映射表

| 行 | 节点 | 行为 | 失败收口 |
| --- | --- | --- | --- |
| 713 | N01 | 进入 const 成员函数 | 只在启用 RealSense 的构建变体中存在 |
| 714 | N01 | 取得状态互斥锁 | 锁异常传播 |
| 715 | N02-N05 | 在锁内复制状态、身份、配置和批次数，随后解锁并按值返回 | 字符串复制异常传播 |
| 716 | N04-N05 | 收口锁和返回对象生命周期 | 无结构化失败 |

项目调用 0；外部边界 X0090。快照只覆盖这四个字段，不包含停止请求、管线存在性或深度比例，不能扩大为全部采集器状态证明。未解析调用 0。
