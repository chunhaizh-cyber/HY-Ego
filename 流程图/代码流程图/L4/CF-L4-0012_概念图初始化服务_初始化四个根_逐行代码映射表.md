# CF-L4-0012 `概念图初始化服务::初始化四个根` 逐行代码映射表

| 文件 | 行号 | 当前代码行为 | 流程节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `领域/初始化.概念图.ixx` | 98-102 | 取得初始化锁；已发布结果成功时完整读回并返回旧结果或空结果 | N01-N05 | 函数调用 / 本函数内具体指令 | 幂等读回 | 无新增 | 服务缓存加仓库读回 | 损坏时返回空且不清缓存 | 增加权威恢复/损坏收口 |
| 同上 | 104-113 | 按 `||` 短路顺序以四个独立模板实例和创建 lambda 确保四根 | N06-N10 | 函数调用 | 根创建与登记 | 可逐项新增四根结构 | 运行期仓库与服务登记 | 任一失败返回空，前序写入保留 | 需要全包候选/撤销边界 |
| 同上 | 115-126 | 依次确保三个生命周期状态并初始化四根活跃生命周期 | N11-N15 | 函数调用 | 生命周期初始化 | 状态与生命周期关系可见 | 运行期仓库事实 | 任一失败返回空；仅 F0182 有局部补偿 | 统一事务边界 |
| 同上 | 128-133 | 依次确保四根名称语素入口和关系 | N16-N20 | 函数调用 | 人读名称投影 | 语素入口及关系逐项可见 | 人读投影与关系事实 | 任一失败返回空；前序名称保留 | 名称不应证明固定根身份 |
| 同上 | 135-138 | 按候选成功、根结构读回、追根因检查顺序作最终门禁 | N21-N24 | 函数调用 / 本函数内具体指令 | 发布前验证 | 无新增 | 候选与仓库读回 | 内部不一致追根因并返回空 | 不得宣称已回滚 |
| 同上 | 139-141 | 复制候选到已发布缓存并返回 | N25-N26 | 本函数内具体指令 | 私有发布 | 服务缓存可读 | 派生缓存，不是权威根身份 | 不适用 | 权威结构应先具备稳定主键 |

覆盖结论：98—141 行全部映射；根函数直接发现 9 类项目源码函数以及 4 个独立模板实例、4 个独立 lambda；外部边界为锁和隐式复制；未解析调用 0。
