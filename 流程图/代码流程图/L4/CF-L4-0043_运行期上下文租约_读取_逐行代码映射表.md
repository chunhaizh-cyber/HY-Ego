# CF-L4-0043 `运行期上下文租约::读取` 逐行代码映射表

| 行 | 节点 | 行为 | 条件 / 结果 |
| --- | --- | --- | --- |
| 196 | N01 | 调用 F0066 深层有效性检查 | 无条件 |
| 196 | N02 | 三元条件裁决 | false 不调用 `get()` |
| 196 | N03 | 返回 `nullptr` | 租约当前无效 |
| 196 | N04 | 调用 X0067 `shared_ptr::get()` | 仅有效路径 |
| 196 | N05 | 原样返回裸指针 | 非拥有只读借用 |

项目调用 1，外部调用 1，未解析调用 0。调用者不得把裸指针保存到所有共享所有者寿命之外，也不得把 `nullptr`解释为宿主从未发布对象。
