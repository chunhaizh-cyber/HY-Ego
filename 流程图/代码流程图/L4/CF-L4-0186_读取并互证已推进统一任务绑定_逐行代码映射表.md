# CF-L4-0186 读取并互证已推进统一任务绑定逐行代码映射表

代码版本：`dd8a7810`；源码 blob：`597af149798c2925b38406f95432c074dba28034`；根函数：R1526。

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/内部治理/服务.任务目标未达成重筹办触发.ixx` | 309-318 | 定义恢复函数，探测当前代次并要求已越过原 G | C01 / N02 / R10 | 函数调用 / 本函数内具体指令 | 读取 / 判断 / 返回 | 无 | 当前事实代次 | 未越过原 G 为逻辑内读取结果 | 无 |
| 同上 | 320-344 | 读取当前任务、统一分区、旧实例、实际结果和路径 | C01 / N02 / R10 | 函数调用 / 本函数内具体指令 | 读取 / 判断 / 返回 | 无 | task owner 当前投影 | 漂移 / 读取状态逻辑内返回 | 无 |
| 同上 | 346-361 | 互证任务、路径、实例、实际结果和裁决材料 | N03 / R10 | 本函数内具体指令 | 判断 / 返回 | 无 | 当前事实与原意图 | 引用冲突为逻辑内返回 | 无 |
| 同上 | 363-375 | 按迁移准备键读取后继；空时仅接受当前后继触发同一旧结果 | C04 / N05 | 函数调用 / 本函数内具体指令 | 读取 / 收敛 | 无 | L1 首次写入见证与当前后继 | 空读可继续当前关系收敛 | 无 |
| 同上 | 376-388 | 互证后继任务、触发、旧路径 / 实例、权威和首次记录 | N05 / N06 / R10 | 本函数内具体指令 | 判断 / 返回 | 无 | 权威后继记录 | 漂移可逻辑返回；权威错追根因 | 无 |
| 同上 | 389-402 | 将当前准备回退到后继前一轮；必要时读前一后继并验证连续性 | C07 / N08 / R10 | 函数调用 / 本函数内具体指令 | 读取 / 纯投影 / 返回 | 无 | 后继历史链 | 链错追根因 | 无 |
| 同上 | 404-411 | 输出推进前任务绑定与统一准备并返回已形成 | P09 | 本函数内具体指令 | 输出 / 返回 | 无 | 已互证恢复投影 | 不适用 | 无 |

覆盖结论：309—411 的全部非空有效源码行已映射。
