# CF-L4-0078 `D455帧材料缓存::停止接收` 逐行代码映射表

| 行 | 节点 | 行为 | 失败收口 |
| --- | --- | --- | --- |
| 290 | 身份 | 冻结无参数、void、noexcept 成员函数 | 无 |
| 291 | N01 | 阻塞取得 `互斥_` 独占锁 | 加锁异常在 noexcept 中终止 |
| 292 | N02 | 锁内把 `正在接收_` 写为 false | 无 |
| 293 | N03 | 解锁并 void 返回 | 无 |

既有候选和已发布材料保持不变，字节数和编号也不变；返回不证明缓存已排空或在途操作已静止。项目调用 0，外部边界 X0099，未解析调用 0。
