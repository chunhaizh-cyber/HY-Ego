# CF-L4-0081 `D455相机采集器::~D455相机采集器` 逐行代码映射表

| 行 | 节点 | 行为 | 失败收口 |
| --- | --- | --- | --- |
| 431 | 身份 | 进入隐式 noexcept 虚析构函数 | 异常导致终止 |
| 432 | N01 | 调用 F0115 关闭同一对象 | void，无关闭结果通道 |
| 433 | N02 | 离开显式析构体 | 无 |
| 733-743 | N03-N06 | 按声明逆序析构标量、字符串、智能指针和同步成员 | 关闭已清空 SDK 独占指针 |
| 协议:162 | N07,N08 | 调用 F0319 默认基类析构并终止对象生命周期 | 无 |

关闭失败状态随对象销毁而不可读取；重复关闭可在析构时覆盖先前故障。若仍有采样、快照或停止调用并发访问，reset 资源和销毁 mutex 均不安全。项目调用 2，外部边界 X0102，未解析调用 0。
