# CF-L4-0065 `创建并打开D455相机采集器` 逐行代码映射表

| 行 | 节点 | 行为 | 失败收口 |
| --- | --- | --- | --- |
| 746-748 | N01 | 绑定按值配置与非拥有可写结果引用 | 当前构建变体外实体不存在 |
| 749 | N01-N02 | 移动配置，经 `make_unique` 分配并调用具体采集器构造 | 分配/构造异常传播；输出结果保持旧值 |
| 750 | N03-N05 | 取具体指针、调用打开并把结构化结果赋给输出引用 | 未收口异常传播 |
| 751 | N06 | 读取成功字段作唯一返回门禁 | 不复核状态快照或实际配置 |
| 752 | N07-N08,N10 | 形成空返回值并析构失败采集器 | 析构清理失败没有第二结果通道 |
| 754-755 | N09,N11 | 转换移动到基类 unique_ptr 并转移唯一所有权 | 后续关闭责任转给调用者 |

项目调用 F0294、F0295、既有 F0117；外部边界 X0089。异常时输出参数可能陈旧、成功仅信任单 bool、失败析构附加故障不可表达是偏差候选。未解析调用 0。
