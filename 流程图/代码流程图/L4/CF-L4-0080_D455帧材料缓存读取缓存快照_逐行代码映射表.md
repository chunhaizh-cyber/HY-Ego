# CF-L4-0080 `D455帧材料缓存::读取缓存快照` 逐行代码映射表

| 行 | 节点 | 行为 | 失败收口 |
| --- | --- | --- | --- |
| 271 | 身份 | 冻结 const 成员函数和按值快照返回 | 无 |
| 272 | N01 | 取得 mutable `互斥_` 独占锁 | 加锁异常传播 |
| 273 | N02 | 默认初始化快照六字段为 false/0 | 无 |
| 274-277 | N03-N06 | 在同一锁内复制四个标量字段 | 无 |
| 278-279 | N07-N09 | 取得范围并按 const 引用绑定当前条目，编号不参与统计 | 空表直接返回零计数 |
| 280-282 | N10,N11 | 未发布候选计数自增 | 无 |
| 283-285 | N12,N13 | 已发布计数自增 | 其它枚举值静默不计 |
| 286 | N14 | 推进迭代器并继续循环 | 无 |
| 287-288 | N15,N16 | 锁内初始化返回对象，随后解锁并返回 | 无 |

快照信任维护中的 `当前总字节数_`，不按条目重算，无法自证字节守恒；下一材料编号也不等于发布数量。项目调用 0，外部边界 X0101，未解析调用 0。
