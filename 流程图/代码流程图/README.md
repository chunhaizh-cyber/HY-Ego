# 当前代码函数流程图

本目录记录提交 `a48a70b2d678dea64dd8228adb4f26f0badd9506` 的项目源码函数可达图谱。

收口状态：`IN-PROGRESS`
`Plan-Package-Ready: no`
当前续点：已完成 265 个实际入队函数并闭合 F0269、F0270、F0271；下一批同层函数为 F0272—F0274。当前唯一函数 558，待处理 293，另有 7 个动态函数指针调用待逐项裁决。F0269 只形成秒级本地墙钟人读文本，`localtime_s` 与 `wcsftime` 返回值均被丢弃；格式化失败后的缓冲区不保证仍是有效零终止串，不能把返回文本用作单调时间、审计全序或机器事实。F0270 只返回五个静态宽字符串字面量指针；非法枚举映射为“未知”仅是显示 fallback，当前 F0095 路径已由 F0268 非空路径前置排除非法类别，控制面板调用仍等待 F0396 子图取得稳定调用者身份。F0271 不新增项目函数或调用边；其第二次 `WideCharToMultiByte` 返回值未核对，非空预分配字符串可能掩盖转换失败，且空输入、超长与首轮失败共用空字符串哨兵。事件日志段中的 `严格转为UTF8` 当前无根图调用证据，只保留源码待核点。X0266—X0272 分账墙钟、时区、格式化、静态文本、宽文本视图、Win32 两阶段转换和结果字符串生命周期。F0556—F0564 及既有 L6/L7 函数继续在队列等待展开；F0234/F0235/F0236/F0237 仍分别并入同定义身份 F0135/F0136/F0137/F0138；F0239 的两个函数指针调用只在当前运行期上下文自有接线语境中唯一解析，不据此改写其它未解析调用。`0600` 新格式逐节点核规已完成 F0227—F0233、F0136、F0238—F0271，既有 223 个函数仍待逐步转换。不得宣称代码函数图谱覆盖、规范审计、日志异常隔离或设计包已经完成。

固定规则：

- 根函数为 `int main(int 参数数量, char* 参数组[])`。
- 按调用深度分层，层内按首次发现顺序逐函数串行展开。
- 一函数一份 Markdown/Mermaid 现状流程图和一份逐行映射。
- 用户在已完成 F0001—F0036 后裁决停止生成 HTML；本轮尚未发布的 HTML 配对文件已在暂停收口时全部删除，当前及后续均只以 Markdown/Mermaid 和逐行映射作为覆盖验收对象。
- 项目源码函数按“提交 + 文件 + 完整限定签名 + 定义位置”形成唯一身份。
- 标准库、操作系统和第三方函数只登记为外部边界，不生成函数图。
- 递归和调用环只登记回边，不重复生成函数图。
- 本目录只表达冻结提交的代码事实，不构成代码施工许可。
- 代码现状覆盖与规范应用闭环分别计数；函数图完成不等于逐节点核规完成。

总入口：

- [函数清单](00_函数清单.md)
- [调用知识图谱](00_调用知识图谱.md)
- [机器可读调用知识图谱](00_调用知识图谱.json)
- [待展开队列](00_待展开队列.md)
- [规范偏差审计](00_规范偏差审计.md)
