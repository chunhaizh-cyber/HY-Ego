# 当前代码函数流程图

本目录记录提交 `a48a70b2d678dea64dd8228adb4f26f0badd9506` 的项目源码函数可达图谱。

收口状态：`IN-PROGRESS`
`Plan-Package-Ready: no`
当前续点：当前正式 `main` 可达全集已经独立收口在 `流程图/主程序可达调用关系/`，共 1162 个项目函数、2757 条项目调用边、1627 个外部边界、0 个未解析调用和 74 个具名排除函数。本目录只承担单函数制图展开投影，现有 653 个投影身份、355 个已制图函数、298 个待处理函数、1924 条项目调用边、384 个外部边界身份和 10 个旧图谱待解析项；不得把 653 解释为当前可达全集。F0356—F0364 已闭合，下一续点为 F0365；F0357 在 E1707 节点记录读取完成后、任何 `if` 短路判断前经 E1667 无条件调用 F0329，只有全部阶段 / 节点 / 类型 / 值前置通过后才经 E1664 调用 F0328。F0358 的 `adjacent_find` 默认谓词已唯一解析为 F0051；F0359 的 `count_if`、`find_if` 和 `any_of` 均按 0..N 次回调表达；F0360 在三个源码行上执行四次 F0051 比较，4050 的公开结果责任归属公开调用方 F0353；F0361 的关系句柄比较已独立登记为 E1908 / R0598。标准库、系统 API、第三方文件和 `rs_context.h` 等外部包含只登记边界，不生成项目函数图。F0342 因当前正式工程全部配置均未定义 `HY_EGO_ENABLE_FAULT_TOLERANCE_CHECK` 而退出制图投影，身份不复用；方法服务三参数 `读取唯一目标` 保持独立身份 F0629。F0331 / F0332 / F0335 的旧结构、函数指针、状态值槽和事务偏差仍按既有审计保留；上述内容不证明状态事实、方法角色、旧主信息 / 节点写入或节点直接身份符合现行规范，也不证明迁移偏差已经修复。F0365 及其它 L6/L7 函数继续在队列等待展开；F0234/F0235/F0236/F0237 仍分别并入同定义身份 F0135/F0136/F0137/F0138；F0239 的两个函数指针调用只在当前运行期上下文自有接线语境中唯一解析，不据此改写其它未解析调用。`0600` 新格式逐节点核规已完成 F0227—F0233、F0136、F0238—F0296、F0298—F0319、F0321—F0341、F0343—F0364；既有早期函数仍待逐步转换。不得宣称单函数流程图全覆盖、规范审计、关系 ABI 偏差修复、方法生命周期迁移、状态类型化切换、主信息退役或设计包已经完成。

固定规则：

- 根函数为 `int main(int 参数数量, char* 参数组[])`。
- 按调用深度分层，层内按首次发现顺序逐函数串行展开。
- 一函数一份 Markdown/Mermaid 现状流程图和一份逐行映射。
- 用户在已完成 F0001—F0036 后裁决停止生成 HTML；本轮尚未发布的 HTML 配对文件已在暂停收口时全部删除，当前及后续均只以 Markdown/Mermaid 和逐行映射作为覆盖验收对象。
- 项目源码函数按“提交 + 文件 + 完整限定签名 + 定义位置”形成唯一身份。
- 标准库、操作系统和第三方函数只登记为外部边界，不生成函数图。
- 递归和调用环只登记回边，不重复生成函数图。
- 本目录只表达冻结提交的代码事实，不构成代码施工许可。
- 代码现状覆盖与规范应用闭环分别计数；函数图完成不等于逐节点核规完成。

总入口：

- [函数清单](00_函数清单.md)
- [调用知识图谱](00_调用知识图谱.md)
- [机器可读调用知识图谱](00_调用知识图谱.json)
- [待展开队列](00_待展开队列.md)
- [规范偏差审计](00_规范偏差审计.md)
