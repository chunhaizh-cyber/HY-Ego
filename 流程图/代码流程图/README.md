# 当前代码函数流程图

本目录记录提交 `a48a70b2d678dea64dd8228adb4f26f0badd9506` 的项目源码函数可达图谱。

收口状态：`IN-PROGRESS`
`Plan-Package-Ready: no`
当前续点：已完成 329 个实际入队函数并闭合 F0335—F0337；下一批从 L6 的 F0338—F0340 继续。当前唯一函数 621，待处理 292，项目调用边 1702，外部边界身份 366，另有 9 个动态函数指针调用待逐项裁决。F0357 在 E1707 节点记录读取完成后、任何 if 短路判断前经 E1667 无条件调用 F0329，只有全部阶段 / 节点 / 类型 / 值前置通过后才经 E1664 调用 F0328；该局部顺序与前置已经校正。方法服务三参数 `读取唯一目标` 已独立登记为待处理 F0629，并以 E1708 显式传入 `std::nullopt` 调用四参数 F0330，不与其合并，也不计入已完成。F0335 仍先创建旧主信息并写通用 I64 槽，再创建带旧主信息锚点的状态节点；写值、写时间或建节点失败均可能遗留可读半结构，新登记 F0626—F0628。F0336 只检查事务接线八项形状非零 / 非空，不证明同源、当前许可或原子快照；新登记外部边界 X0367。F0337 只在线程局部关系令牌链中按仓库地址返回最内层匹配令牌借用或 `nullptr`，不验证域、纪元、令牌类型或当前性；TLS 和裸指针读取均保留为本函数内具体指令，不为外部文件生成项目函数图。标准库、系统 API、第三方文件和 `rs_context.h` 等外部包含只登记边界，不生成项目函数图。F0331 / F0332 的旧结构、函数指针和事务偏差仍按既有审计保留；上述内容不证明状态事实、方法角色、旧主信息 / 节点写入或节点直接身份符合现行规范，也不证明迁移偏差已经修复。F0556—F0629 及其它 L6/L7 函数继续在队列等待展开；F0234/F0235/F0236/F0237 仍分别并入同定义身份 F0135/F0136/F0137/F0138；F0239 的两个函数指针调用只在当前运行期上下文自有接线语境中唯一解析，不据此改写其它未解析调用。`0600` 新格式逐节点核规已完成 F0227—F0233、F0136、F0238—F0296、F0298—F0319、F0321—F0337，既有 223 个函数仍待逐步转换。不得宣称代码函数图谱覆盖、规范审计、方法生命周期迁移、状态类型化切换、主信息退役或设计包已经完成。

固定规则：

- 根函数为 `int main(int 参数数量, char* 参数组[])`。
- 按调用深度分层，层内按首次发现顺序逐函数串行展开。
- 一函数一份 Markdown/Mermaid 现状流程图和一份逐行映射。
- 用户在已完成 F0001—F0036 后裁决停止生成 HTML；本轮尚未发布的 HTML 配对文件已在暂停收口时全部删除，当前及后续均只以 Markdown/Mermaid 和逐行映射作为覆盖验收对象。
- 项目源码函数按“提交 + 文件 + 完整限定签名 + 定义位置”形成唯一身份。
- 标准库、操作系统和第三方函数只登记为外部边界，不生成函数图。
- 递归和调用环只登记回边，不重复生成函数图。
- 本目录只表达冻结提交的代码事实，不构成代码施工许可。
- 代码现状覆盖与规范应用闭环分别计数；函数图完成不等于逐节点核规完成。

总入口：

- [函数清单](00_函数清单.md)
- [调用知识图谱](00_调用知识图谱.md)
- [机器可读调用知识图谱](00_调用知识图谱.json)
- [待展开队列](00_待展开队列.md)
- [规范偏差审计](00_规范偏差审计.md)
