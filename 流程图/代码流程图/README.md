# 当前代码函数流程图

本目录记录提交 `a48a70b2d678dea64dd8228adb4f26f0badd9506` 的项目源码函数可达图谱。

收口状态：`IN-PROGRESS`
`Plan-Package-Ready: no`
当前续点：已完成 317 个实际入队函数并闭合 F0323—F0325；下一批为 L6 的 F0326—F0328。当前唯一函数 610，待处理 293，项目调用边 1645，外部边界身份 357，另有 7 个动态函数指针调用待逐项裁决。原 F0320 / E0612 已按严格根可达性退出；F0321 通过 F0618 `完全未接域()` 与 F0336 `已接域()` 分类全空、八项完整或部分接线。F0322—F0324 分属三个 `.cpp` 匿名命名空间，必须保留三个独立函数身份；三者均四次调用 F0336，再只比较域编号、运行期纪元和 `shared_ptr` 存储指针，不比较五个回调，也不证明共享控制块相同。F0325 通过 Microsoft UCRT 读取两个 SQL 环境字段，外部包含和 API 只登记边界、不生成项目函数图；缺失、读取错误和空值折叠为空字符串，非空结果仍须后继准入，`std::wstring` 构造异常发生在 `free` 前会遗留裸缓冲，已登记为资源生命周期偏差。正式 Clang CPP 候选分别辅助确认 F0323、F0324、F0325 的调用与语句候选，最终图均由冻结源码逐行复核。上述内容不证明事务接线当前可用、SQL 配置合法、数据库已连接或相关偏差已经修复。F0556—F0618 及其它 L6/L7 函数继续在队列等待展开；F0234/F0235/F0236/F0237 仍分别并入同定义身份 F0135/F0136/F0137/F0138；F0239 的两个函数指针调用只在当前运行期上下文自有接线语境中唯一解析，不据此改写其它未解析调用。`0600` 新格式逐节点核规已完成 F0227—F0233、F0136、F0238—F0296、F0298—F0319、F0321—F0325，既有 223 个函数仍待逐步转换。不得宣称代码函数图谱覆盖、规范审计、结构事务或数据库生产闭环、设计包已经完成。

固定规则：

- 根函数为 `int main(int 参数数量, char* 参数组[])`。
- 按调用深度分层，层内按首次发现顺序逐函数串行展开。
- 一函数一份 Markdown/Mermaid 现状流程图和一份逐行映射。
- 用户在已完成 F0001—F0036 后裁决停止生成 HTML；本轮尚未发布的 HTML 配对文件已在暂停收口时全部删除，当前及后续均只以 Markdown/Mermaid 和逐行映射作为覆盖验收对象。
- 项目源码函数按“提交 + 文件 + 完整限定签名 + 定义位置”形成唯一身份。
- 标准库、操作系统和第三方函数只登记为外部边界，不生成函数图。
- 递归和调用环只登记回边，不重复生成函数图。
- 本目录只表达冻结提交的代码事实，不构成代码施工许可。
- 代码现状覆盖与规范应用闭环分别计数；函数图完成不等于逐节点核规完成。

总入口：

- [函数清单](00_函数清单.md)
- [调用知识图谱](00_调用知识图谱.md)
- [机器可读调用知识图谱](00_调用知识图谱.json)
- [待展开队列](00_待展开队列.md)
- [规范偏差审计](00_规范偏差审计.md)
