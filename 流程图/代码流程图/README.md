# 当前代码函数流程图

本目录记录提交 `a48a70b2d678dea64dd8228adb4f26f0badd9506` 的项目源码函数可达图谱。

收口状态：`IN-PROGRESS`
`Plan-Package-Ready: no`
当前续点：已完成 346 个实际入队函数并闭合 F0350—F0355；下一批从 L6 的 F0356—F0361 继续。当前唯一函数 633，待处理 287，项目调用边 1850，外部边界身份 377，另有 10 个动态函数指针调用待逐项裁决。F0342 因当前正式工程全部配置均未定义 `HY_EGO_ENABLE_FAULT_TOLERANCE_CHECK` 而退出可达图谱，身份不复用。F0357 在 E1707 节点记录读取完成后、任何 if 短路判断前经 E1667 无条件调用 F0329，只有全部阶段 / 节点 / 类型 / 值前置通过后才经 E1664 调用 F0328；该局部顺序与前置已经校正。方法服务三参数 `读取唯一目标` 已独立登记为待处理 F0629，并以 E1708 显式传入 `std::nullopt` 调用四参数 F0330，不与其合并，也不计入已完成。F0341 当前只接受关系类型 0—18，错误拒绝正式 ABI 19—23；该实现偏差仅登记，未修改代码。F0343 先经 F0337 查询 TLS 令牌，非空时调用待处理 F0630 令牌重载，空时调用 F0377 普通重载；F0344 只恢复当前线程 TLS 前序；F0345 委托 F0631 释放许可；F0346 委托 F0632 验证共享令牌并值式返回旧节点记录。F0347 的角色集合与 4330 一致，F0348 对角色 11 / 13 仅排除未分类而错误接受其它已知类型和未知枚举；F0349 按语素、世界树、两个根需求顺序初始化并锁内发布快照，但无邮箱路径的条件变量等待可丢通知，异常会越过线程顶层，跨阶段失败没有统一补偿。F0350 仅检查六项局部结果形态；F0351 对关系 7 / 8 完整集合分别读取后做成员判断，两次读取不是统一快照；F0352 / F0354 / F0355 只消费可重建登记投影并经节点仓复核，不能证明概念根或生命周期状态权威身份；F0353 在活动图共享锁内先读粗类别，再调用 `_已加锁` 生命周期读取。F0633—F0642 已作为真实项目被调函数登记待展开。标准库、系统 API、第三方文件和 `rs_context.h` 等外部包含只登记边界，不生成项目函数图。F0331 / F0332 / F0335 的旧结构、函数指针、状态值槽和事务偏差仍按既有审计保留；上述内容不证明状态事实、方法角色、旧主信息 / 节点写入或节点直接身份符合现行规范，也不证明迁移偏差已经修复。F0556—F0642 及其它 L6/L7 函数继续在队列等待展开；F0234/F0235/F0236/F0237 仍分别并入同定义身份 F0135/F0136/F0137/F0138；F0239 的两个函数指针调用只在当前运行期上下文自有接线语境中唯一解析，不据此改写其它未解析调用。`0600` 新格式逐节点核规已完成 F0227—F0233、F0136、F0238—F0296、F0298—F0319、F0321—F0341、F0343—F0355；既有 223 个函数仍待逐步转换。不得宣称代码函数图谱覆盖、规范审计、关系 ABI 偏差修复、方法生命周期迁移、状态类型化切换、主信息退役或设计包已经完成。

固定规则：

- 根函数为 `int main(int 参数数量, char* 参数组[])`。
- 按调用深度分层，层内按首次发现顺序逐函数串行展开。
- 一函数一份 Markdown/Mermaid 现状流程图和一份逐行映射。
- 用户在已完成 F0001—F0036 后裁决停止生成 HTML；本轮尚未发布的 HTML 配对文件已在暂停收口时全部删除，当前及后续均只以 Markdown/Mermaid 和逐行映射作为覆盖验收对象。
- 项目源码函数按“提交 + 文件 + 完整限定签名 + 定义位置”形成唯一身份。
- 标准库、操作系统和第三方函数只登记为外部边界，不生成函数图。
- 递归和调用环只登记回边，不重复生成函数图。
- 本目录只表达冻结提交的代码事实，不构成代码施工许可。
- 代码现状覆盖与规范应用闭环分别计数；函数图完成不等于逐节点核规完成。

总入口：

- [函数清单](00_函数清单.md)
- [调用知识图谱](00_调用知识图谱.md)
- [机器可读调用知识图谱](00_调用知识图谱.json)
- [待展开队列](00_待展开队列.md)
- [规范偏差审计](00_规范偏差审计.md)
