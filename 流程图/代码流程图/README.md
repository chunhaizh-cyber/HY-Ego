# 当前代码函数流程图

本目录记录提交 `a48a70b2d678dea64dd8228adb4f26f0badd9506` 的项目源码函数可达图谱。

收口状态：`IN-PROGRESS`
`Plan-Package-Ready: no`
当前续点：已完成 332 个实际入队函数并闭合 F0338—F0340；下一批从 L6 的 F0341—F0343 继续。当前唯一函数 621，待处理 289，项目调用边 1745，外部边界身份 367，另有 9 个动态函数指针调用待逐项裁决。F0357 在 E1707 节点记录读取完成后、任何 if 短路判断前经 E1667 无条件调用 F0329，只有全部阶段 / 节点 / 类型 / 值前置通过后才经 E1664 调用 F0328；该局部顺序与前置已经校正。方法服务三参数 `读取唯一目标` 已独立登记为待处理 F0629，并以 E1708 显式传入 `std::nullopt` 调用四参数 F0330，不与其合并，也不计入已完成。F0338 只检查运行期状态存储指针、令牌域编号和释放回调三项形状，不验证纪元、许可序号、类型、线程、当前性或同源性；新增外部边界 X0368。F0339 无条件返回许可内部令牌成员的非拥有 `const&`，不验证许可或令牌，返回结果不是冻结快照。F0340 只把仓库 / 令牌借用压入当前线程 TLS 链，依赖 F0344 同线程严格 LIFO 恢复；自动许可宏路径未执行共享令牌验证，且类未显式禁复制的风险仍具名保留。标准库、系统 API、第三方文件和 `rs_context.h` 等外部包含只登记边界，不生成项目函数图。F0331 / F0332 / F0335 的旧结构、函数指针、状态值槽和事务偏差仍按既有审计保留；上述内容不证明状态事实、方法角色、旧主信息 / 节点写入或节点直接身份符合现行规范，也不证明迁移偏差已经修复。F0556—F0629 及其它 L6/L7 函数继续在队列等待展开；F0234/F0235/F0236/F0237 仍分别并入同定义身份 F0135/F0136/F0137/F0138；F0239 的两个函数指针调用只在当前运行期上下文自有接线语境中唯一解析，不据此改写其它未解析调用。`0600` 新格式逐节点核规已完成 F0227—F0233、F0136、F0238—F0296、F0298—F0319、F0321—F0340，既有 223 个函数仍待逐步转换。不得宣称代码函数图谱覆盖、规范审计、方法生命周期迁移、状态类型化切换、主信息退役或设计包已经完成。

固定规则：

- 根函数为 `int main(int 参数数量, char* 参数组[])`。
- 按调用深度分层，层内按首次发现顺序逐函数串行展开。
- 一函数一份 Markdown/Mermaid 现状流程图和一份逐行映射。
- 用户在已完成 F0001—F0036 后裁决停止生成 HTML；本轮尚未发布的 HTML 配对文件已在暂停收口时全部删除，当前及后续均只以 Markdown/Mermaid 和逐行映射作为覆盖验收对象。
- 项目源码函数按“提交 + 文件 + 完整限定签名 + 定义位置”形成唯一身份。
- 标准库、操作系统和第三方函数只登记为外部边界，不生成函数图。
- 递归和调用环只登记回边，不重复生成函数图。
- 本目录只表达冻结提交的代码事实，不构成代码施工许可。
- 代码现状覆盖与规范应用闭环分别计数；函数图完成不等于逐节点核规完成。

总入口：

- [函数清单](00_函数清单.md)
- [调用知识图谱](00_调用知识图谱.md)
- [机器可读调用知识图谱](00_调用知识图谱.json)
- [待展开队列](00_待展开队列.md)
- [规范偏差审计](00_规范偏差审计.md)
