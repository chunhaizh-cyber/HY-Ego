# 当前代码函数流程图

本目录记录提交 `a48a70b2d678dea64dd8228adb4f26f0badd9506` 的项目源码函数可达图谱。

收口状态：`IN-PROGRESS`
`Plan-Package-Ready: no`
当前续点：已完成 323 个实际入队函数并闭合 F0329—F0331；下一批为 L6 的 F0332—F0334。当前唯一函数 614，待处理 291，项目调用边 1670，外部边界身份 362，另有 8 个动态函数指针调用待逐项裁决。F0329 仍经旧 `节点记录::主信息` 和通用零号 I64 槽读取状态值，未读取状态类型化记录、关系 19 五角色、时间、来源与版本；F0357 对 F0329 的 E1667 已按源码顺序纠正为在阶段、节点存在和节点类型判断前调用。F0330 已纠正为无默认实参的四参数重载，当前只按节点类型筛选通用关系目标，并把零目标与多目标折叠为空；1707—1709 三参数包装器是独立函数且经控制面板根模式真实可达，但其直接调用方位于 F0388 后尚待展开的函数链，本批不预占编号、不计入聚合统计，待稳定展开到调用方时登记其身份和到 F0330 的边。F0331 在接域路径经共享许可、未接域路径经本仓锁即时写入旧主信息表，仍存在编号回绕、忽略重复插入、无候选确认 / 撤销 / 最后发布 / 读回和后继节点创建失败不补偿等偏差。Clang 候选辅助确认 F0329 / F0330 的头文件范围及 F0331 的 C++ 调用；新登记 F0619 一参数 I64 读取、F0620 / F0621 两个关系目标组重载和 F0622 令牌创建重载。标准库、编译器隐式值函数、外部包含和 API 仍只登记外部边界，不生成项目函数图；F0331 的运行期许可函数指针登记为 U0008，不伪连静态唯一目标。上述内容不证明状态事实、方法角色、旧主信息写入或节点直接身份符合现行规范，也不证明迁移偏差已经修复。F0556—F0622 及其它 L6/L7 函数继续在队列等待展开；F0234/F0235/F0236/F0237 仍分别并入同定义身份 F0135/F0136/F0137/F0138；F0239 的两个函数指针调用只在当前运行期上下文自有接线语境中唯一解析，不据此改写其它未解析调用。`0600` 新格式逐节点核规已完成 F0227—F0233、F0136、F0238—F0296、F0298—F0319、F0321—F0331，既有 223 个函数仍待逐步转换。不得宣称代码函数图谱覆盖、规范审计、方法生命周期迁移、状态类型化切换、主信息退役或设计包已经完成。

固定规则：

- 根函数为 `int main(int 参数数量, char* 参数组[])`。
- 按调用深度分层，层内按首次发现顺序逐函数串行展开。
- 一函数一份 Markdown/Mermaid 现状流程图和一份逐行映射。
- 用户在已完成 F0001—F0036 后裁决停止生成 HTML；本轮尚未发布的 HTML 配对文件已在暂停收口时全部删除，当前及后续均只以 Markdown/Mermaid 和逐行映射作为覆盖验收对象。
- 项目源码函数按“提交 + 文件 + 完整限定签名 + 定义位置”形成唯一身份。
- 标准库、操作系统和第三方函数只登记为外部边界，不生成函数图。
- 递归和调用环只登记回边，不重复生成函数图。
- 本目录只表达冻结提交的代码事实，不构成代码施工许可。
- 代码现状覆盖与规范应用闭环分别计数；函数图完成不等于逐节点核规完成。

总入口：

- [函数清单](00_函数清单.md)
- [调用知识图谱](00_调用知识图谱.md)
- [机器可读调用知识图谱](00_调用知识图谱.json)
- [待展开队列](00_待展开队列.md)
- [规范偏差审计](00_规范偏差审计.md)
