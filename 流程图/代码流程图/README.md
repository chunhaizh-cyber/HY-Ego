# 当前代码函数流程图

本目录记录源码冻结提交 `0a93526882cb33c61c2fbb04ecad1aeaddcfe225` 的项目源码函数可达图谱。

收口状态：`IN-PROGRESS`
`Plan-Package-Ready: no`
当前续点：正式 `main` 可达全集读取 `流程图/主程序可达调用关系/`，共 1385 个项目函数、4096 条项目调用边、3666 个外部边界、59 个具名回调、0 个未解析调用和 94 个具名排除函数。当前唯一活动制图队列为 `00_制图队列.json`：1120 个当前可达函数已制图，265 个待绘制；另有 52 张当前不可达历史图原地保留但不计入活动完成数。旧 `00_调用知识图谱.json/.md` 仍是 653 身份的历史增量投影，只作已登记旧边和旧未解析项追溯，不再承担活动取队。本批 R0552—R0575 中 23 个仍属当前闭包的函数已经闭合；R0568 经重载复核退出，新增 R0888—R0891 四项可达身份进入待绘队列，连续下一续点为 R0576。标准库、系统 API、第三方文件和 `rs_context.h` 等外部包含只登记边界，不生成项目函数图。现有图只证明对应代码事实和逐行映射，不证明规范偏差已修复、代码已修改或设计包已经完成。

固定规则：

- 根函数为 `int main(int 参数数量, char* 参数组[])`。
- 按调用深度分层，层内按首次发现顺序逐函数串行展开。
- 一函数一份 Markdown/Mermaid 现状流程图和一份逐行映射。
- 用户在已完成 F0001—F0036 后裁决停止生成 HTML；本轮尚未发布的 HTML 配对文件已在暂停收口时全部删除，当前及后续均只以 Markdown/Mermaid 和逐行映射作为覆盖验收对象。
- 项目源码函数按“提交 + 文件 + 完整限定签名 + 定义位置”形成唯一身份。
- 标准库、操作系统和第三方函数只登记为外部边界，不生成函数图。
- 递归和调用环只登记回边，不重复生成函数图。
- 本目录只表达冻结提交的代码事实，不构成代码施工许可。
- 代码现状覆盖与规范应用闭环分别计数；函数图完成不等于逐节点核规完成。

总入口：

- [当前可达函数制图清单](00_函数清单.md)
- [机器可读活动制图队列](00_制图队列.json)
- [当前待绘制队列](00_待展开队列.md)
- [历史不可达流程图保留清单](00_历史不可达流程图保留清单.md)
- [历史增量调用知识图谱](00_调用知识图谱.md)
- [机器可读历史增量调用知识图谱](00_调用知识图谱.json)
- [规范偏差审计](00_规范偏差审计.md)
