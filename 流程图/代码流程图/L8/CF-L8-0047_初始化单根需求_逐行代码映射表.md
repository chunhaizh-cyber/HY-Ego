# CF-L8-0047 初始化单根需求逐行代码映射表

代码版本：`3920a74638264f9f539d50f32f3c9fe5fb1982ab`
源码 blob：`8b2bd4e4d44b747d661d82401db6aea5901c67da`
覆盖文件：`海中鱼巣/领域/初始化.需求.ixx:115-182`
根函数：R0249
配对流程图：`流程图/代码流程图/L8/CF-L8-0047_初始化单根需求_现状流程图.md`
稳定身份反查：RCE0300、RCE0838—RCE0849、RCE2569—RCE2573、X01921—X01930、X01932、X03143、X03144；X01931、X01933—X01936 已停用

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/初始化.需求.ixx` | 115-122 | 定义 R0249 六个参数并默认构造结果 | N01 | 本函数内具体指令 | 入口 / 构造 | 无 | 临时局部变量 | 不适用 | 无 |
| 同上 | 123-126 | 经 RCE0838 创建特征定义，并由 RCE0839/RCE0840 检查；失败返回空结果 | C02 / C03 / C04 / R06 | 函数调用 / 本函数内具体指令 | 调用 / 写入 / 判断 / 返回 | 创建特征定义 | 权威事实 | 追根因解决 | RCE0839 目标为 F0163 |
| 同上 | 128-135 | 经 RCE0841 创建概念入口，经 RCE2571 和 RCE0840 检查成功、主键及本次绑定；失败返回空结果 | C07 / C08 / N09 / C10 / R06 | 函数调用 / 本函数内具体指令 | 调用 / 写入 / 判断 / 返回 | 创建语素入口与关系 | 权威事实 | 追根因解决 | 无 |
| 同上 | 137-140 | 经 RCE2569 创建实例槽位，并由 RCE0839/RCE0840 检查；失败返回空结果 | C11 / C12 / C13 / R06 | 函数调用 / 本函数内具体指令 | 调用 / 写入 / 判断 / 返回 | 创建实例槽位 | 权威事实 | 追根因解决 | 无 |
| 同上 | 141-146 | 经 RCE2570 创建特征值，经 RCE0839/RCE0842/RCE0840 检查并写入 I64 值；失败返回空结果 | C14 / C15 / C16 / C17 / R06 | 函数调用 / 本函数内具体指令 | 调用 / 写入 / 判断 / 返回 | 创建并写入特征值 | 权威事实 | 追根因解决 | 无 |
| 同上 | 148-157 | 经 RCE0843 创建目标状态需求，并由 RCE0839/RCE0840 检查；失败返回空结果 | C18 / C19 / C20 / R06 | 函数调用 / 本函数内具体指令 | 调用 / 写入 / 判断 / 返回 | 创建需求及目标状态 | 权威事实 | 追根因解决 | 无 |
| 同上 | 159 | 经 RCE0844 读取目标状态并以 X03143 `value_or` 回退空句柄 | C21 | 函数调用 | 调用 / 读取 | 无 | 临时局部变量 | 不适用 | 无 |
| 同上 | 161-166 | 经 RCE0845/RCE0846/RCE2572/RCE0847/RCE0848/RCE0849 读取槽位、值、状态、承接和语素关系投影 | C22 / C23 / C24 / C25 / C26 / C27 | 函数调用 | 调用 / 读回 | 无 | 权威事实 | 不适用 | 无 |
| 同上 | 167-175 | 经 RCE2573 检查结果成功，经 X01921—X01923 比较三个 optional，经 X03144 检查承接材料存在并比较字段 | C28 / C29 / N30 | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 读回 | 无 | 权威事实 | 追根因解决 | 无 |
| 同上 | 176-177 | 经 X01924—X01930、X01932 两次查找定义节点并比较迭代器 | C31 / N32 | 函数调用 / 本函数内具体指令 | 调用 / 判断 | 无 | 权威事实 | 追根因解决 | X01931、X01933—X01936 已停用 |
| 同上 | 178-180 | 经 RCE0840 检查完整读回，失败返回空结果 | C33 / R06 | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 | 无 | 当前代码事实 | 追根因解决 | 无 |
| 同上 | 181-182 | 返回完整结果并闭合函数 | R34 | 本函数内具体指令 | 返回 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：115—182 全部有效源码行已映射；N01—R34 全部节点可反查；已停用污染边界未被伪写成调用。
