# CF-L8-0059 生成当前分类真实树只读材料逐行代码映射表

代码版本：`3920a74638264f9f539d50f32f3c9fe5fb1982ab`
源码 blob：`b0b4cdc2cd7e7fd6801f36c7a43bc27595ca89d9`
覆盖文件：`海中鱼巣/领域/控制面板服务.h:311-436`
根函数：R0307
配对流程图：`流程图/代码流程图/L8/CF-L8-0059_生成当前分类真实树只读材料_现状流程图.md`
稳定身份反查：RCE0683、RCE0991—RCE1004、X02115、X03497—X03530、X03535；X02116 建议停用

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/控制面板服务.h` | 311-316 | 定义 R0307，经 RCE0991 判定请求；非可展示经 RCE0992 返回拒绝候选 | C01 / D02 / C03 / R29 | 函数调用 / 本函数内具体指令 | 入口 / 调用 / 判断 / 返回 | 仅返回候选 | 人读候选 | 逻辑内返回 | 无 |
| 同上 | 317-326 | 检查分类索引、节点/深度预算和根页大小；失败均经 RCE0992 返回 | D02 / C03 / R29 | 函数调用 / 本函数内具体指令 | 判断 / 调用 / 返回 | 仅返回候选 | 人读候选 | 逻辑内返回 | 无 |
| 同上 | 327-332 | 概念树经 RCE0993 单独生成；非概念分类经 X03497 拒绝携带当前概念根 | D04 / C05 / C06 / C03 / R29 | 函数调用 / 本函数内具体指令 | 判断 / 调用 / 外部调用 / 返回 | 仅返回候选 | 人读候选 | 逻辑内返回 | 无 |
| 同上 | 334-343 | 初始化构造状态，经 X03522/X03523 构造局部组；世界或语素分类经 RCE0994 读取预绑定根组 | N07 / D08 / C09 | 函数调用 / 本函数内具体指令 | 构造 / 分派 / 调用 | 构造局部根组 | 非权威材料 | 不适用 | 无 |
| 同上 | 344-350 | 需求分类经 X03498—X03502 从固定根数组赋值、经 RCE0995 排序去重并检查数量 | D08 / C10 / C11 | 函数调用 / 本函数内具体指令 | 外部调用 / 调用 / 判断 | 构造局部根组 | 非权威材料 | 追根因解决 | 无 |
| 同上 | 351-357 | 任务分类经 RCE0996 发现根组，并经 X03503/X03504 读取 optional 值、X03535 复制到根组 | D08 / C12 | 函数调用 / 本函数内具体指令 | 调用 / 外部调用 / 判断 | 构造局部根组 | 非权威材料 | 追根因解决 | 无 |
| 同上 | 358-368 | 方法分类先经 RCE0996 发现任务根，再经 RCE0997 发现方法根，并经 X03503/X03504 读取、X03535 复制方法根组 | D08 / C12 / C13 | 函数调用 / 本函数内具体指令 | 调用 / 外部调用 / 判断 | 构造根组和来源任务组 | 非权威材料 | 追根因解决 | 无 |
| 同上 | 369-379 | 非法分类、构造状态异常或经 X03505 检出根组为空时，经 RCE0992 返回拒绝候选 | D08 / D14 / C15 / C03 / R29 | 函数调用 / 本函数内具体指令 | 判断 / 外部调用 / 返回 | 仅返回候选 | 人读候选 | 逻辑内返回 / 追根因解决 | 无 |
| 同上 | 380-381 | 经 RCE0995 排序去重根组；经 RCE0998 和 X03521/X03529 形成无概念根的根集签名 | C11 / C16 | 函数调用 | 调用 / 外部调用 / 纯计算 | 更新局部根组与签名 | 非权威材料 | 不适用 | 无 |
| 同上 | 382-394 | 初始化起始偏移；经 X03506—X03508、X03501 读取并复核继续游标；无效时拒绝，有效时采用下一根偏移 | D17 / N18 / C03 / R29 | 函数调用 / 本函数内具体指令 | 外部调用 / 判断 / 赋值 / 返回 | 更新局部偏移 | 非权威材料 | 逻辑内返回 | 无 |
| 同上 | 395-398 | 经 X02115/X03501 计算结束偏移；空页或逆序页经 RCE0992 拒绝 | C19 / C03 / R29 | 函数调用 / 本函数内具体指令 | 外部调用 / 判断 / 返回 | 形成页区间 | 非权威材料 | 逻辑内返回 | 无 |
| 同上 | 399-401 | 经 X03509—X03511 从根组迭代器范围构造当前页根组 | C20 | 函数调用 | 外部调用 / 构造 | 构造局部页根组 | 非权威材料 | 不适用 | 无 |
| 同上 | 403-425 | 经 X03512 构造当前树 optional；按分类经 RCE0999—RCE1003 调用五种构树函数，并经 X03513 移动赋值 | C21 / D22 / C23 / C24 | 函数调用 / 本函数内具体指令 | 构造 / 分派 / 调用 / 外部调用 | 构造局部当前树 | 非权威材料 | 追根因解决 | 无 |
| 同上 | 426-428 | 经 X03514 检查当前树；无值时经 RCE0992 返回构造状态拒绝候选 | D25 / C03 / R29 | 函数调用 / 本函数内具体指令 | 外部调用 / 判断 / 返回 | 仅返回候选 | 人读候选 | 逻辑内返回 / 追根因解决 | 无 |
| 同上 | 429-433 | 经 X03515 默认构造下一页游标；存在后页时经 X03516/X03521 写入游标 | C26 | 函数调用 | 构造 / 外部调用 / 赋值 | 构造局部游标 | 非权威材料 | 不适用 | 无 |
| 同上 | 434-435 | 经 X03517 读取当前树，经 X03518/X03519 移动树和游标，并构造空概念选项组、空当前概念根，调用 RCE1004 完成候选 | C27 | 函数调用 | 外部调用 / 调用 / 返回 | 移交局部材料 | 人读候选 | 不适用 | 停用模糊且漏计第二次移动的 X02116 |
| 同上 | 436 | 经 X03524—X03530 闭合局部容器与 optional 生命周期并闭合函数 | C28 / R29 | 函数调用 / 本函数内具体指令 | 析构 / 返回 / 作用域 | 释放局部材料 | 当前代码事实 | 不适用 | 无 |

覆盖结论：311—436 全部有效源码行已映射；C01—R29 与映射节点双向一致；14 条项目出边全部反查；X02116 未作为当前边界引用。
