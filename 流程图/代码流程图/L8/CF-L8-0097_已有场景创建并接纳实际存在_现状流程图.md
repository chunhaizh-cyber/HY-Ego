# CF-L8-0097 已有场景创建并接纳实际存在现状流程图

图类型：现状流程图
代码版本：`03a8b7ac099ccea83e57f2696e2290b7bcdfacaf`
当前工作区复核基线：`f19e4b0e001554d25b1cf0847dbcfb0b52d4e22c`
源码 blob：`524922ab2f3d0d76cfed4bdd517f0d7d57e1ae6a`
覆盖文件：`海中鱼巣/领域/数据操作.存在场景.ixx:345-410`
唯一根函数：R0652
完整签名：`海中鱼巣::存在场景业务结果 海中鱼巣::存在场景数据操作::在已有场景中创建并接纳实际存在(海中鱼巣::节点句柄 场景, std::uint64_t 存在主键) const`
参数：`场景`、`存在主键` 按值输入；要求有效场景句柄和非0主键
返回结果：入口、类型、读取失败、幂等冲突、事务提交、版本漂移、幂等读回、补建归属或结构失败的具名业务结果
调用图：`流程图/主程序可达调用关系/01_main可达项目调用边表.md`
输入契约 / 调用语境表：本图“当前边界”节；未另建独立表
非成功返回二分审查表：本图返回节点与“当前边界”节；未另建独立表
偏差清单：无 / 不适用；本图只登记当前实现事实
依据提交 / 结构化消息：源码冻结 `03a8b7ac099ccea83e57f2696e2290b7bcdfacaf`；无结构化执行消息
验证输出：配对逐行映射、节点集合、源码 blob 与本批机械审计
已登记调用方：R0643（RCE1884）
直接项目函数：F0442、F0163、R0648、R0629、R0626、R0635、R0651、R0116、R0649、R0644、R0639（RCE1927—RCE1934、RCE1946—RCE1948）
回调边界：RCB0029 注册 R0656；R0116 经 RCE1935 同步调度 R0656，本图不展开 R0656 正文
外部边界：X02340—X02342
逐行映射表：`流程图/代码流程图/L8/CF-L8-0097_已有场景创建并接纳实际存在_逐行代码映射表.md`
结构读写：写前读取场景和主键身份；R0116 同步执行 R0656 原子创建主信息、存在节点、主键绑定及归属关系；未提交时执行写后身份和归属读回
不得作为施工许可：是
不得宣称：请求提交必然成功，或端到端创建接纳能力已经验收

## 流程图

```mermaid
flowchart TD
    N01["N01【本函数内具体指令】绑定场景和存在主键"]
    C02["C02【函数调用】F0442 有效() -> bool<br/>RCE1927"]
    C03["C03【函数调用】F0163 句柄有效(场景) -> bool<br/>RCE1928"]
    B04{"B04【本函数内具体指令】接线有效、场景句柄有效且主键非0"}
    R05["R05【本函数内具体指令】返回默认入口拒绝结果"]
    C06["C06【函数调用】R0648 读取节点身份(场景) -> 场景材料<br/>RCE1929"]
    B07{"B07【本函数内具体指令】场景身份状态为已找到"}
    C08["C08【函数调用】R0629 映射场景读取失败<br/>RCE1930"]
    R09["R09【本函数内具体指令】返回场景读取失败结果"]
    B10{"B10【本函数内具体指令】场景材料类型为场景"}
    R11["R11【本函数内具体指令】返回默认类型拒绝结果"]
    C12["C12【函数调用】R0626 读取主键身份(存在主键) -> 写前身份<br/>RCE1931"]
    B13{"B13【本函数内具体指令】写前身份状态为已找到"}
    C14["C14【函数调用】R0635 是实际存在(写前身份) -> bool<br/>RCE1932"]
    B15{"B15【本函数内具体指令】写前身份为实际存在"}
    R16["R16【本函数内具体指令】返回幂等冲突"]
    C17["C17【函数调用】R0651 创建场景归属(场景, 当前节点) -> 业务结果<br/>RCE1933"]
    R18["R18【本函数内具体指令】返回归属创建结果"]
    B19{"B19【本函数内具体指令】写前身份状态不是未找到"}
    C20["C20【函数调用】R0629 映射写前身份读取失败<br/>RCE1930"]
    R21["R21【本函数内具体指令】返回写前读取失败结果"]
    N22["N22【本函数内具体指令】初始化场景版本漂移标记为 false"]
    N23["N23【本函数内具体指令】默认构造新主信息、新存在和新关系句柄"]
    N24["N24【本函数内具体指令】定义独立回调 R0656，并经 X02340 包装为 std::function"]
    C25["C25【函数调用】R0116 同步执行回调并返回结构结果<br/>RCE1934；RCB0029；RCE1935 调度 R0656"]
    X26["X26【外部边界 X02341】销毁临时 std::function"]
    N27["N27【本函数内具体指令】绑定结构写入结果"]
    B28{"B28【本函数内具体指令】结构状态为已提交"}
    R29["R29【本函数内具体指令】返回已提交及新存在、新关系"]
    B30{"B30【本函数内具体指令】场景版本已经漂移"}
    R31["R31【本函数内具体指令】返回版本漂移"]
    C32["C32【函数调用】R0626 再次读取主键身份 -> 当前身份<br/>RCE1931"]
    B33{"B33【本函数内具体指令】当前身份状态为已找到"}
    C34["C34【函数调用】R0635 是实际存在(当前身份) -> bool<br/>RCE1932"]
    B35{"B35【本函数内具体指令】当前身份为实际存在"}
    R36["R36【本函数内具体指令】返回幂等冲突"]
    C37["C37【函数调用】R0649 读取场景归属(场景, 当前节点) -> 当前归属<br/>RCE1946"]
    C38["C38【函数调用】R0644 当前归属.已归属() -> bool<br/>RCE1947"]
    B39{"B39【本函数内具体指令】写后已经归属"}
    X40["X40【外部边界 X02342】解引用当前归属关系"]
    R41["R41【本函数内具体指令】返回写后幂等读回"]
    B42{"B42【本函数内具体指令】当前归属状态为未找到"}
    C43["C43【函数调用】R0651 补建场景归属<br/>RCE1933"]
    R44["R44【本函数内具体指令】返回补建归属结果"]
    C45["C45【函数调用】R0629 映射归属读取失败<br/>RCE1930"]
    R46["R46【本函数内具体指令】返回归属读取失败结果"]
    B47{"B47【本函数内具体指令】当前身份状态不是未找到"}
    C48["C48【函数调用】R0629 映射当前身份读取失败<br/>RCE1930"]
    R49["R49【本函数内具体指令】返回身份读取失败结果"]
    C50["C50【函数调用】R0639 映射结构失败(结构结果) -> 业务结果<br/>RCE1948"]
    R51["R51【本函数内具体指令】返回结构失败映射结果"]
    N01 --> C02 --> C03 --> B04
    B04 -- 否 --> R05
    B04 -- 是 --> C06 --> B07
    B07 -- 否 --> C08 --> R09
    B07 -- 是 --> B10
    B10 -- 否 --> R11
    B10 -- 是 --> C12 --> B13
    B13 -- 是 --> C14 --> B15
    B15 -- 否 --> R16
    B15 -- 是 --> C17 --> R18
    B13 -- 否 --> B19
    B19 -- 是 --> C20 --> R21
    B19 -- 否 --> N22 --> N23 --> N24 --> C25 --> X26 --> N27 --> B28
    B28 -- 是 --> R29
    B28 -- 否 --> B30
    B30 -- 是 --> R31
    B30 -- 否 --> C32 --> B33
    B33 -- 是 --> C34 --> B35
    B35 -- 否 --> R36
    B35 -- 是 --> C37 --> C38 --> B39
    B39 -- 是 --> X40 --> R41
    B39 -- 否 --> B42
    B42 -- 是 --> C43 --> R44
    B42 -- 否 --> C45 --> R46
    B33 -- 否 --> B47
    B47 -- 是 --> C48 --> R49
    B47 -- 否 --> C50 --> R51
```

## 当前边界

- 366—387 行 lambda 是独立身份 R0656；根图只记录注册、同步调度、结构结果和生命周期。
- 写前已有实际存在时直接委托 R0651 建立归属；事务未提交后的同身份读回也可能再次委托 R0651 补建归属。
