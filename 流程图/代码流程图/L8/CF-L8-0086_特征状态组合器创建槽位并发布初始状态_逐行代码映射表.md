# CF-L8-0086 特征状态组合器创建槽位并发布初始状态逐行代码映射表

代码版本：`0a93526882cb33c61c2fbb04ecad1aeaddcfe225`
工作区复核基线：`03a8b7ac099ccea83e57f2696e2290b7bcdfacaf`
源码 blob：`e1e70ac49fbfa683a9fbb238f57e123b18d22c01`
覆盖文件：`海中鱼巣/领域/组合.特征状态.ixx:29-41`
根函数：R0566
配对流程图：`流程图/代码流程图/L8/CF-L8-0086_特征状态组合器创建槽位并发布初始状态_现状流程图.md`
稳定边全集：RCE0791、RCE1569—RCE1575、X04728、X04729、X04730

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/组合.特征状态.ixx` | 29-30 | 定义并进入 R0566，借用只读请求 | N01 | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 31-32 | 依次检查两个幂等主键非零且互不相等 | B02 / B03 / B04 | 本函数内具体指令 | 判断 / 短路 | 无 | 当前代码事实 | 任一拒绝返回默认结果 | 无 |
| 同上 | 33 | 经 RCE1569 对宿主、特征定义各调一次 F0163；入口五项均通过才继续，否则返回默认结果 | C05 / C06 / R07 | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 短路 / 返回 | 无 | 当前代码事实 | 入口拒绝 | 无 |
| 同上 | 34-35 | 经 RCE1570 调 R0502，以主键、宿主和特征定义形成槽位规格结果 | C08 | 函数调用 | 调用 / 形成规格 | 新建局部槽位规格结果 | 当前代码事实 | 结果状态由 R0502 决定 | 无 |
| 同上 | 36 | 经 RCE1571 调 R0508；失败时经 RCE1573 调 R0567 并返回映射结果 | C09 / C10 / R11 | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 | 无 | 当前代码事实 | 槽位规格失败映射 | 无 |
| 同上 | 37-38 | 槽位规格成功后经 RCE1574 调 R0503 形成初始值规格结果 | C12 | 函数调用 | 调用 / 形成规格 | 新建局部初始值规格结果 | 当前代码事实 | 结果状态由 R0503 决定 | 无 |
| 同上 | 39 | 经 RCE1572 调 R0509；失败时经 RCE1573 调 R0567，经 X04730 析构局部初始值规格结果后返回 | C13 / C14 / D15 / R16 | 函数调用 / 编译器生命周期 / 本函数内具体指令 | 调用 / 判断 / 析构 / 返回 | 释放初始值规格持有的可选值与向量 | 当前代码事实 | 初始值规格失败映射 | 无 |
| 同上 | 40 | 两份规格成功后经 X04728/X04729 解引用两个 optional，形成求值顺序未指定的两个 const 引用实参，经 RCE1575 调 R0389；随后经 X04730 析构局部初始值规格结果并返回业务结果 | X17 / C18 / D19 / R20 | 外部边界 / 函数调用 / 编译器生命周期 / 本函数内具体指令 | 解引用 / 调用 / 析构 / 返回 | 委托一次结构事务并释放局部值式规格 | 当前代码事实 | R0389 结果直接传递 | 无 |
| 同上 | 41 | 闭合 R0566 | R07 / R11 / R16 / R20 | 本函数内具体指令 | 返回边界 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：29—41 全部非空源码行已映射；N01—R20 全部节点可反查；第 40 行未虚构两个实参的固定求值顺序。
