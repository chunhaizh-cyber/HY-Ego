# CF-L6-0295 `概念活动业务服务::复核` 逐行代码映射表

代码版本：`3920a74638264f9f539d50f32f3c9fe5fb1982ab`
源码 blob：`c96b7a88a6486ebbe8c23948fb9cca6e8effd23e`
覆盖文件：`海中鱼巣/领域/服务.概念活动.ixx:67-82`
根函数：F0461 `海中鱼巣::概念活动初始化结果 海中鱼巣::概念活动业务服务::复核(const 海中鱼巣::系统角色清单& 系统角色, const 海中鱼巣::概念活动材料& 预期材料) const`
配对流程图：`流程图/代码流程图/L6/CF-L6-0295_概念活动业务服务复核_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4040_子规范_不透明结构事务候选确认撤销与最后发布.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/服务.概念活动.ixx` | 67 | 进入 const 成员函数 F0461 | S | 本函数内具体指令 | 入口 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 68 | 绑定 const `系统角色清单&` 参数，只在本次同步调用借用 | S | 本函数内具体指令 | 参数绑定 | 无 | 调用方材料 | 不适用 | 不保存引用 |
| 同上 | 69 | 绑定 const `概念活动材料&` 参数并打开函数体 | S | 本函数内具体指令 | 参数绑定 / 作用域 | 无 | 调用方材料 | 不适用 | 不保存引用 |
| 同上 | 70 | 经 X02493 构造 `std::lock_guard<std::mutex>` 并取得 `活动锁_`；锁覆盖后续全部复核及返回值形成 | X01 | 函数调用 | 外部边界 / 并发 / RAII 构造 | 取得运行期互斥锁，不改机器事实 | 运行期资源状态 | 锁构造异常向外传播 | 已登记 |
| 同上 | 71 | 经 RCE2179、RCE2180、RCE2181 依次调用 F0451、F0245、F0251，检查服务、系统角色和预期材料完整性；任一 false 即短路 | C02 / C03 / C04 / B08 / N09 | 函数调用 / 本函数内具体指令 | 只读调用 / 短路判断 | 无 | 当前装配及值式材料 | 一般不完整为逻辑内返回 | 已登记 |
| 同上 | 72 | 经 X02495 调用 optional `has_value`；有值时经 X02496 调用 `operator->`，再经 RCE2182 调用 F0250 比较当前与预期材料；任一失败形成默认结果，随后经 X02494 解锁返回 | X05 / X06 / C07 / B08 / N09 / X22 / R23 | 函数调用 / 本函数内具体指令 | 外部边界 / 只读调用 / 判断 / 返回 | 无 | 锁保护下当前服务材料 | 一般不匹配为逻辑内返回 | 已登记 |
| 同上 | 73 | 准备调用 R0331，把返回结果按值绑定到局部 `重建` | C11 | 函数调用 | 只读重建 / 局部构造 | 只形成局部结果 | 派生材料 | 异常向外传播 | 与 74 行共同绑定完整调用 |
| 同上 | 74 | 经 RCE0236 调用 R0500 形成签名组，并经 RCE0235 调用 R0331，传入系统角色、预期初始化参数和签名组 | C10 / C11 | 函数调用 | 参数形成 / 只读重建 | 只形成局部重建结果 | 派生材料 | R0331 状态后续分账 | 已登记 |
| 同上 | 75 | 经 RCE0237 调用 R0539；false 时形成携带 `重建.状态` 的结果，解锁后返回 | C12 / B13 / N14 / X22 / R23 | 函数调用 / 本函数内具体指令 | 结果判断 / 返回形成 / RAII | 无 | 重建状态 | 非成功状态按协议传播；内部异常须追根因 | 已登记 |
| 同上 | 76 | 经 RCE2183 调用 F0464 比较预期重建视图与重建视图 | C15 / B16 | 函数调用 / 本函数内具体指令 | 值式比较 / 判断 | 无 | 两份重建视图 | 不等转内部错误 | 已登记 |
| 同上 | 77 | 视图不等时形成内部不一致结果，随后解锁返回 | N17 / X22 / R23 | 本函数内具体指令 / 函数调用 | 内部错误 / RAII / 返回 | 无 | 内部错误结果 | 追根因解决 | 无 |
| 同上 | 79 | 经 X02497 从 `预期材料` 复制构造局部拥有型 `输出` | N18 | 函数调用 | 外部边界 / 值复制 / 局部构造 | 只形成局部副本 | 临时局部变量 | 复制异常向外传播 | 已登记 |
| 同上 | 80 | 经 X02498 以 `重建.视图` 复制赋值覆盖局部输出副本的重建视图字段 | N19 | 函数调用 | 外部边界 / 值赋值 | 只改变局部副本 | 临时局部变量 | 赋值异常向外传播 | 已登记 |
| 同上 | 81 | 经 X01091 对输出执行 `std::move`，再经 X02499 移动构造结果材料并形成幂等读回结果；随后经 X02494 析构 `lock_guard`、解锁并返回 | X20 / X21 / N21 / X22 / R23 | 函数调用 / 本函数内具体指令 | 外部边界 / 返回构造 / RAII / 返回 | 释放运行期锁；零机器事实写入 | 只读复核结果 | 成功幂等读回 | 已登记 |
| 同上 | 82 | 函数作用域闭合；正常或异常路径均经 X02494 结束锁对象生命周期 | X22 / R23 | 函数调用 / 本函数内具体指令 | 作用域 / 生命周期 | 无 | 当前代码事实 | 不适用 | 已登记 |

覆盖结论：F0461 的 67—82 行全部映射；RCE2179—RCE2183、RCE0235—RCE0237 与 X02493—X02499、X01091 均已绑定到对应源码调用点。
