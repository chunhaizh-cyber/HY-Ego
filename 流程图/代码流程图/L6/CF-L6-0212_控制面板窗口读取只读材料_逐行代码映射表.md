# CF-L6-0212 `控制面板窗口::窗口实现::读取只读材料` 逐行代码映射表

代码版本：`2c38ec4017e38b2e541b93c8c9ae038856f849c7`
源码 blob：`fe9dad1eb3728c823beccf305c783be96a3df33d`
覆盖文件：`海中鱼巣/界面/控制面板窗口.cpp:386-416`
根函数：F0388 `bool 海中鱼巣::控制面板窗口::窗口实现::读取只读材料(bool 允许保留旧快照 = false)`
配对流程图：`流程图/代码流程图/L6/CF-L6-0212_控制面板窗口读取只读材料_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`、`规范/8120_子规范_程序入口启动模式与运行宿主边界_20260726.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/界面/控制面板窗口.cpp` | 386 | 进入 F0388；独占可写借用 `窗口实现 this`，按值接收 `允许保留旧快照`，省略实参时为 false | S | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 不保存参数引用 |
| 同上 | 387 | 先经 RCE0176 调用 R0202 形成只读请求，再经 RCE0177 调用 R0304 生成总览值式材料 | C01 / C02 | 函数调用 | 请求构造 / 只读材料调用 / 局部绑定 | 只形成局部人读候选 | 非权威只读材料 | 调用异常直接传播 | X01020 不再重复登记 R0306 |
| 同上 | 388 | 经 RCE0178 调用 R0306 生成六类树视图候选数组并按值保存；原 X01020 是同一项目调用的误分类外部边界 | C03 | 函数调用 | 只读材料调用 / 局部绑定 | 只形成局部树候选 | 非权威只读材料 | 调用异常直接传播 | 删除重复外部边界 X01020 |
| 同上 | 389 | 开始计算局部 `总览有效` bool | B04 | 本函数内具体指令 | 局部绑定 / 判断准备 | 无 | 临时局部变量 | 不适用 | 无 |
| 同上 | 390 | 判断总览状态是否为 `可展示`；false 时后续 `&&` 条件短路 | B04 | 本函数内具体指令 | 判断 / 短路 | 无 | 当前候选状态 | 失败进入追根因门禁 | 无 |
| 同上 | 391 | 仅前项为 true 时判断 `人读材料` | B05 | 本函数内具体指令 | 判断 / 短路 | 无 | 非权威材料标记 | 失败进入追根因门禁 | 不把人读标记当机器事实 |
| 同上 | 392 | 仅前项均为 true 时判断 `来源显示服务` | B06 | 本函数内具体指令 | 判断 / 短路 | 无 | 非权威来源标记 | 失败进入追根因门禁 | 无 |
| 同上 | 393 | 仅前项均为 true 时要求 `允许写业务事实 == false`，并保存 `总览有效` | B07 / N08 | 本函数内具体指令 | 判断 / 局部赋值 | 无 | 只读投影边界 | 失败进入追根因门禁 | 显示层不得写业务事实 |
| 同上 | 394 | 经 X01021 调用 `std::all_of`，准备遍历树材料组并使用 397—401 行 R0612 判定 lambda | X09 | 外部边界 | 标准算法 / 遍历 | 无 | 当前候选遍历 | 空数组按算法为 true；当前固定数组长度为 6 | 保持标准算法与项目回调分账 |
| 同上 | 395 | X01021 取得树材料组 begin 迭代器 | X09 | 外部边界 | 迭代边界绑定 | 无 | 临时迭代器 | 不适用 | 无 |
| 同上 | 396 | X01021 取得树材料组 end 迭代器 | X09 | 外部边界 | 迭代边界绑定 | 无 | 临时迭代器 | 不适用 | 无 |
| 同上 | 397 | 定义 R0612 判定 lambda，X01021 遍历时经 RCE1749 以当前材料 const 引用调用 | C10 | 函数调用 | 回调定义 / 参数绑定 | 无 | 当前项目函数事实 | 不适用 | R0612 独立进入待绘制队列 |
| 同上 | 398 | R0612 判断当前材料状态是否为 `可展示` | C10 | 函数调用 | 回调内判断 / 短路 | 无 | 当前候选状态 | false 使 all_of 返回 false | 无 |
| 同上 | 399 | R0612 仅前项为 true 时判断 `可作为树视图分类` | C10 | 函数调用 | 回调内判断 / 短路 | 无 | 非权威分类材料 | false 使 all_of 返回 false | 无 |
| 同上 | 400 | R0612 仅前项为 true 时要求 `只作为详情材料 == false` | C10 | 函数调用 | 回调内判断 / 返回 | 无 | 非权威用途材料 | false 使 all_of 返回 false | 无 |
| 同上 | 401 | 闭合 R0612 lambda 与 X01021 调用，保存返回值为 `树材料有效` | C10 / N11 | 函数调用 / 本函数内具体指令 | 回调闭合 / 局部赋值 | 无 | 临时局部变量 | false 进入追根因门禁 | 无 |
| 同上 | 402 | 开始计算局部 `统计快照有效` bool | B12 | 本函数内具体指令 | 局部绑定 / 判断准备 | 无 | 临时局部变量 | 不适用 | 无 |
| 同上 | 403 | 判断启动结构统计命名空间类型是否为 `结构统计` | B12 | 本函数内具体指令 | 判断 / 短路 | 无 | 当前启动只读统计材料 | 失败进入追根因门禁 | 统计材料不裁决机器事实 |
| 同上 | 404 | 仅类型匹配时判断来源是否为 `结构仓库集合`，并保存 `统计快照有效` | B13 / N14 | 本函数内具体指令 | 判断 / 局部赋值 | 无 | 当前启动只读统计材料 | 失败进入追根因门禁 | 无 |
| 同上 | 405 | 判断总览、树材料和统计快照三个有效 bool；任一 false 进入错误分支 | B15 | 本函数内具体指令 | 判断 / 短路 | 无 | 当前控制流 | 前置成立后材料边界异常按追根因 | 不在门禁前写当前快照 |
| 同上 | 406 | 失败时经 X01022 构造阶段字符串，并按 RCE0179 调用 F0393 标记窗口人读追根因错误 | X16 / C17 | 外部边界 / 函数调用 | 字符串构造 / 人读诊断写入 | 改变窗口错误状态，不写业务机器事实 | 人读诊断 | 追根因解决 | 不把错误文本作为机器裁决 |
| 同上 | 407 | 材料门禁失败时返回 false | RF | 本函数内具体指令 | 返回 | 总览 / 树材料尚未写入当前快照 | 当前调用结果 | 追根因解决 | 无 |
| 同上 | 408 | 闭合失败分支；仅三类材料都有效时继续 | B15 | 本函数内具体指令 | 分支闭合 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 409 | 复制局部总览材料到 `当前快照.总览材料` | N18 | 本函数内具体指令 | 值式写入 / 投影更新 | 更新窗口只读快照 | 非权威界面投影 | 赋值异常直接传播 | 不写业务事实 |
| 同上 | 410 | 复制六类树材料组到 `当前快照.树视图材料组` | N19 | 本函数内具体指令 | 值式写入 / 投影更新 | 更新窗口只读快照 | 非权威界面投影 | 赋值异常时不撤销 409 行 | 如需原子投影须另有正式设计 |
| 同上 | 411 | 开始形成 RCE0180 对 R0176 的调用；其 bool 返回值将直接成为 F0388 结果 | C22 / RT | 函数调用 / 本函数内具体指令 | 调用 / 返回绑定 | R0176 可能更新当前树投影 | 非权威界面投影 | R0176 false 按其合同返回 | 无 |
| 同上 | 412 | 把 `当前快照.当前分类` 按值绑定为 R0176 第一实参 | C22 | 函数调用 | 实参绑定 | 无 | 当前投影分类 | 不适用 | 无 |
| 同上 | 413 | 经 X01023 复制 `当前快照.当前概念根` optional，作为 R0176 第二实参 | X20 / C22 | 外部边界 / 函数调用 | optional 复制 / 实参绑定 | 形成临时值 | 当前投影材料 | 不适用 | 无 |
| 同上 | 414 | 经 X01024 复制 `当前快照.当前页游标` optional，作为 R0176 第三实参 | X21 / C22 | 外部边界 / 函数调用 | optional 复制 / 实参绑定 | 形成临时值 | 当前投影材料 | 不适用 | 无 |
| 同上 | 415 | 把 `允许保留旧快照` 绑定为 R0176 第四实参，调用并直接返回其 bool | C22 / RT | 函数调用 / 本函数内具体指令 | 实参绑定 / 返回 | 由 R0176 决定当前树投影变化 | 当前调用结果 | 由 R0176 分类 | 保持参数不被本函数改写 |
| 同上 | 416 | 闭合函数作用域；RF 或 RT 已是运行终点，局部候选结束生命周期 | RF / RT | 本函数内具体指令 | 作用域 / 生命周期 | 无新增业务结构 | 当前代码事实 | 不适用 | 无 |

覆盖结论：386—416 行全部映射；入边 E0855 / RCE0651、项目出边 RCE0176—RCE0180 / RCE1749、外部边界 X01021—X01024、材料门禁、失败诊断、快照替换和最终返回均闭合；误分类 X01020 已退出，未解析项目调用 0。
