# CF-L6-0202 `配置字段可用` 逐行代码映射表

代码版本：`3686cc2882ba42216609174c8e45e59b684e2958`
覆盖文件：`海中鱼巣/适配/SQL数据库适配.cpp:66-76`
根函数：F0378 `bool 海中鱼巣::{匿名命名空间@SQL数据库适配.cpp}::配置字段可用(std::wstring_view 字段)`
配对流程图：`流程图/代码流程图/L6/CF-L6-0202_SQL配置字段可用_现状流程图.md`
已登记调用方：正式唯一入边 E0808（F0203，`SQL数据库适配.cpp:330`）
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`、`规范/详细设计/SQLServer结构审计投影与控制面板查看详细设计.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/适配/SQL数据库适配.cpp` | 66 | 进入匿名命名空间局部函数，按值复制非拥有 `wstring_view 字段`；底层宽字符串不转移、不延长生命周期 | S | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 调用方保持底层存储存活且不并发修改 |
| 同上 | 67 | 先经 X02298 调用 `字段.empty()` 并判断结果；仅非空时经 X02299 调用 `字段.size()`，再判断是否大于固定上限 128，保持 `||` 左到右短路 | X01 / B01 / X02 / B02 | 外部边界 / 本函数内具体指令 | 读取 / 判断 / 短路 | 无 | 当前局部配置材料 | 空或超长为写前配置准入拒绝 | 不把通过长度门扩大为完整 SQL 安全 |
| 同上 | 68 | 空字段或长度大于 128 时按值返回 `false`；不进入字符循环 | RF | 本函数内具体指令 | 返回 | 零配置写入、零数据库动作 | 当前函数结果 | 入口拒绝材料 | 由 F0203 形成局部配置失败结果 |
| 同上 | 69 | 长度门通过后初始化范围迭代，并在每轮判断是否还有下一 `wchar_t`；循环结束时进入成功返回 | N03 / B04 / N11 | 本函数内具体指令 | 循环初始化 / 判断 / 迭代 | 无 | 当前代码事实 | 空循环已由前置排除；正常结束表示全部字符通过 | 无 |
| 同上 | 70 | 经 X02300 解引用范围迭代器，按值读取当前宽字符到局部 `字符` | X05 | 外部边界 | 读取 / 局部绑定 | 无 | 借用字符投影 | 借用失效属调用前置违反 / 未定义行为 | 无 |
| 同上 | 71 | 按 `L';'`、`L'{'`、`L'}'`、`L'\r'`、`L'\n'` 的固定顺序执行相等比较和 `||` 短路；任一命中则进入拒绝分支 | B06 / B07 / B08 / B09 / B10 | 本函数内具体指令 | 判断 / 短路 | 无 | 当前局部配置材料 | 命中禁字符为写前配置准入拒绝 | 明确当前未拒绝其它字符 |
| 同上 | 72 | 当前字符命中任一禁字符时按值返回 `false`；不再扫描后续字符 | RF | 本函数内具体指令 | 返回 | 零配置写入、零数据库动作 | 当前函数结果 | 入口拒绝材料 | 无 |
| 同上 | 73 | 闭合禁字符拒绝分支；未命中时继续本轮 | B10 | 本函数内具体指令 | 分支闭合 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 74 | 闭合循环体；范围迭代位置前进并回到 69 行的下一字符判断 | N11 / B04 | 本函数内具体指令 | 循环回边 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 75 | 全部字符扫描完成且均未命中禁字符时按值返回 `true` | RT | 本函数内具体指令 | 返回 | 无写入 | 当前函数结果 | 不适用 | 不得把 true 扩大为连接或 SQL 语义验证 |
| 同上 | 76 | 闭合函数作用域；视图副本、当前字符和迭代状态结束，不释放底层字符串 | RF / RT | 本函数内具体指令 | 作用域 / 生命周期 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：66—76 行全部映射；正式唯一项目入边为 E0808 / F0203；项目出边 0，正式外部边界为 X02298—X02300，未解析项目调用 0。源码中服务器和数据库字段位于同一 `||` 调用表达式：服务器总是先检查，数据库仅在服务器通过后检查；本文按正式聚合身份 E0808 记录，不新增第二稳定边。
