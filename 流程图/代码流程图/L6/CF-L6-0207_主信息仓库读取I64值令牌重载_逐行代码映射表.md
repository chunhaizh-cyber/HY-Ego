# CF-L6-0207 `主信息仓库::读取I64值(主信息句柄,值索引,令牌)` 逐行代码映射表

代码版本：`9b4aa4a151e32d09d30cd9f28c9f85a45fd2692b`
源码 blob：`11f8fa53ff961a8cbaac34cf75b69a22533ca907`
覆盖文件：`海中鱼巣/核心/主信息仓库.cpp:446-452`
根函数：F0383 `std::optional<std::int64_t> 海中鱼巣::主信息仓库::读取I64值(海中鱼巣::主信息句柄 主信息, std::uint64_t 值索引, const 海中鱼巣::结构事务令牌& 令牌) const`
配对流程图：`流程图/代码流程图/L6/CF-L6-0207_主信息仓库读取I64值令牌重载_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`、`规范/4040_子规范_不透明结构事务候选确认撤销与最后发布.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/主信息仓库.cpp` | 446 | 进入 const 成员函数 F0383；非拥有借用 `this` 和 `令牌`，按值接收 `主信息句柄 主信息` 与 `std::uint64_t 值索引` | S | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 不保存仓库、句柄或令牌引用 |
| 同上 | 447 | 经 RCE0170 调用 F0439 `读取主信息(主信息,令牌)`；F0439 在令牌与共享锁窗口内复核句柄并复制记录，返回 optional 值式快照；锁取得或记录复制异常经 E13 直接传播 | C01 / E13 | 函数调用 / 本函数内具体指令 | 令牌读取 / 共享快照 / 异常传播 | 无；只取得局部记录快照 | 当前仓库读取结果 | 空记录为允许的不命中；强前置保证后仍为空时沿 F0439 追根因 | 不把空 optional 猜成单一原因 |
| 同上 | 448 | 先判断记录是否有值；仅有值时读取快照 `值容器.max_size()` 并比较值索引。空记录或索引大于 `max_size()-1` 时按 `||` 左到右短路返回 `std::nullopt` | B02 / N03 / B04 / RN | 本函数内具体指令 | optional 判断 / 容量边界 / 短路 / 返回 | 无 | 当前值式快照 | 记录不命中或索引不可表示均为只读空结果 | 保持转换前上限检查 |
| 同上 | 449 | 值索引通过容器可表示上限后，转换为本地 `std::size_t 索引` | N05 | 本函数内具体指令 | 纯计算 / 类型转换 | 无 | 临时局部变量 | 不适用 | 不在上限门禁前窄化 |
| 同上 | 450 | 先读取快照值容器当前 `size`；越界时返回空。仅索引在范围内时经 X01009 取得 `值容器[索引]` 的 const optional 引用并判断槽位是否有值；空槽位返回 `std::nullopt` | N06 / B07 / X08 / B09 / RN | 本函数内具体指令 / 外部边界 | 容量读取 / 边界判断 / optional 读取 / 返回 | 无 | 当前值式快照 | 槽位越界或为空均为当前只读空结果 | 不把越界和空槽位扩大为记录不存在 |
| 同上 | 451 | 槽位有值时经 X01010 再次取得同一快照槽位，读取其中 I64；经 X01011 从 const I64 值构造独立 optional，并按值返回 | X10 / N11 / X12 / RV | 外部边界 / 本函数内具体指令 | 槽位读取 / 值复制 / 返回 | 只形成调用方拥有的返回 optional | 当前快照中的 I64 值 | 不适用 | 不暴露容器或槽位引用 |
| 同上 | 452 | 词法闭合函数定义；RN 或 RV 已是返回路径终点，局部记录快照、索引和参数借用结束生命周期 | RN / RV | 本函数内具体指令 | 作用域 / 生命周期 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：446—452 行全部映射；项目调用边 RCE0170、入边 E0842 / RCE0273、外部边界 X01009—X01011 均闭合；记录不命中、索引不可表示、槽位越界、空槽位、值式成功返回和 F0439 异常传播均已覆盖；未解析项目调用 0。
