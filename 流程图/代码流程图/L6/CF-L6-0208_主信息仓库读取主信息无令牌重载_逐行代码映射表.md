# CF-L6-0208 `主信息仓库::读取主信息(主信息句柄)` 逐行代码映射表

代码版本：`9b4aa4a151e32d09d30cd9f28c9f85a45fd2692b`
源码 blob：`11f8fa53ff961a8cbaac34cf75b69a22533ca907`
覆盖文件：`海中鱼巣/核心/主信息仓库.cpp:198-210`
根函数：F0384 `std::optional<海中鱼巣::主信息记录> 海中鱼巣::主信息仓库::读取主信息(海中鱼巣::主信息句柄 主信息) const`
配对流程图：`流程图/代码流程图/L6/CF-L6-0208_主信息仓库读取主信息无令牌重载_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`、`规范/4040_子规范_不透明结构事务候选确认撤销与最后发布.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/主信息仓库.cpp` | 198 | 进入 const 成员函数 F0384；非拥有借用 `this`，按值接收 `主信息句柄 主信息` | S | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 不保存仓库或句柄引用 |
| 同上 | 199 | 经 RCE1743 调用 F0336 读取接线形态。接域时经 RCE1744 调用当前生产接线唯一目标 F0397 形成局部许可；经 RCE1745 调用 F0338 判断许可有效，false 物化空 optional，true 经 RCE1746 调用 F0339 读取令牌并按 RCE0171 调用 F0439。正常返回表达式完成后经 RCE1747 析构许可；F0397 构造前异常直接传播，F0439 异常先析构许可再传播 | C01 / B02 / C03 / C04 / B05 / C06 / C07 / N08 / D09 / R10 / E11 / D12 / E13 | 函数调用 / 本函数内具体指令 | 接线读取 / 许可取得 / 条件选择 / 令牌读取 / 仓库读取 / RAII / 返回 / 异常传播 | 无；接域路径只取得值式快照 | 当前接线、许可和 F0439 读取结果 | 许可无效或 F0439 不命中为空 optional；强前置保证后异常需沿接线 / F0439 追根因 | 六条项目出边均已取得稳定身份 |
| 同上 | 200 | 仅 F0336 返回 false 时，经 X01012 构造 `std::shared_lock<std::shared_mutex>` 并锁定 `仓库锁_`；锁取得异常经 E22 直接传播 | X14 / E22 | 外部边界 / 本函数内具体指令 | 锁取得 / 资源生命周期 / 异常传播 | 无；建立未接域只读窗口 | 当前仓库锁 | 锁取得失败不包装为空 optional | 不把线程或锁状态写成机器事实 |
| 同上 | 201 | 在共享锁窗口内按 `主信息.主信息编号` 调用当前 `unordered_map` 查找并保存迭代位置 | N15 | 本函数内具体指令 | 仓库查找 / 临时绑定 | 无 | 当前仓库读取候选 | 不适用 | 正式外部边界表尚未给 find 独立身份 |
| 同上 | 202 | 经 X01013 比较查找位置与 `主信息表_.end()`；相等时进入空返回 | X16 / B17 | 外部边界 / 本函数内具体指令 | 迭代位置比较 / 判断 | 无 | 当前仓库读取候选 | 查表不命中为普通空结果 | 不继续解引用 end |
| 同上 | 203 | 查表不命中时物化并返回 `std::nullopt`；返回值形成后释放共享锁 | RN / U21 | 本函数内具体指令 | 返回 / 资源释放 | 无 | 当前只读结果 | 普通不命中 | 不区分编号从未存在或已删除 |
| 同上 | 204 | 闭合查表不命中分支；运行时仅未返回路径继续 | RN | 本函数内具体指令 | 分支闭合 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 205 | 查表命中后借用迭代位置中的 const `主信息记录`，借用不越过共享锁生命周期 | N18 | 本函数内具体指令 | 记录读取 / 非拥有借用 | 无 | 当前仓库记录 | 不适用 | 禁止把引用返回调用方 |
| 同上 | 206 | 依次比较句柄仓库编号、句柄版本和记录状态；任一不匹配进入空返回 | B19 | 本函数内具体指令 | 句柄复核 / 状态判断 / 短路 | 无 | 当前仓库记录与句柄 | 错仓、错版本或记录非有效为普通读取空结果 | 保持完整句柄和状态复核 |
| 同上 | 207 | 句柄或记录状态复核失败时物化并返回 `std::nullopt`；返回值形成后释放共享锁 | RN / U21 | 本函数内具体指令 | 返回 / 资源释放 | 无 | 当前只读结果 | 普通句柄不命中 | 不把多种原因伪成唯一不存在 |
| 同上 | 208 | 闭合句柄 / 状态拒绝分支；运行时仅未返回路径继续 | B19 | 本函数内具体指令 | 分支闭合 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 209 | 复核通过后经 X01014 复制 const `主信息记录` 并构造 optional 快照；复制成功后经 U21 释放共享锁并按值返回。复制异常时经 U23 释放共享锁，再经 E22 继续传播 | X20 / U21 / RV / U23 / E22 | 外部边界 / 本函数内具体指令 | 记录复制 / 返回 / 资源释放 / 异常传播 | 只形成调用方拥有的值式快照 | 当前有效主信息记录 | 不适用 | 不暴露仓库内部记录引用 |
| 同上 | 210 | 词法闭合函数定义；R10、RN、RV、E11、E13 或 E22 已是各自终点，不存在 return 后继续执行的运行节点 | R10 / RN / RV / E11 / E13 / E22 | 本函数内具体指令 | 作用域 / 生命周期 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：198—210 行全部映射；项目出边 RCE0171、RCE1743—RCE1747、入边 E0844 / RCE0279、外部边界 X01012—X01014 均闭合；接域许可、未接域共享锁、完整句柄复核、值式复制、全部空返回和异常资源释放路径均已覆盖。
