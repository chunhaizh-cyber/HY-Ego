# CF-L6-0372 创建实例特征槽位现状流程图

图类型：现状流程图
代码版本：`03a8b7ac099ccea83e57f2696e2290b7bcdfacaf`
当前工作区复核基线：`ef4f7bda76c92c0eb11456cfa267d76f35810616`
源码 blob：`4c7bcd451f2e46e85d707a40c371dcbeaa2c1169`
覆盖文件：`海中鱼巣/领域/特征服务.h:242-260`
唯一根函数：R0762
完整签名：`节点句柄 海中鱼巣::特征服务::创建实例特征槽位(节点句柄 宿主节点, 节点句柄 特征类型)`
参数：`宿主节点`、`特征类型` 均按值输入；关系服务须已接入，宿主类型须允许，特征类型须为特征，且宿主不得已有同定义槽位
返回结果：入口不满足或任一步创建/检查失败返回空句柄；全部成功时返回新建槽位节点
已登记调用方：R0249、R0847（RCE2569、RCE3135）
直接项目函数：R0559、R0561、F0554、R0560、F0184、F0163、F0167、F0168（RCE2593—RCE2600）
外部边界：X03162 `std::optional<节点句柄>::has_value()`；X04925 临时 `std::optional<节点句柄>` 析构
逐行映射表：`流程图/代码流程图/L6/CF-L6-0372_创建实例特征槽位_逐行代码映射表.md`
结构读写：入口检查只读宿主与特征结构；通过后创建槽位节点、宿主归属关系和特征模板关系
不得作为施工许可：是
不得宣称：返回空句柄时本函数必然没有留下已创建节点或关系

## 流程图

```mermaid
flowchart TD
    N01["N01【本函数内具体指令】绑定宿主节点与特征类型"]
    B02{"B02【本函数内具体指令】关系服务是否为空"}
    C03{"C03【函数调用 RCE2593】R0559 宿主类型是否允许"}
    C04{"C04【函数调用 RCE2594】R0561 特征类型是否为特征"}
    C05["C05【函数调用 RCE2595】F0554 读取宿主特征槽位→临时optional"]
    X06["X06【外部边界 X03162】临时optional.has_value()→是否已有槽位"]
    X07["X07【外部边界 X04925】析构临时optional"]
    B08{"B08【本函数内具体指令】宿主是否已有同定义槽位"}
    R09["R09【本函数内具体指令】返回空句柄"]
    C10["C10【函数调用 RCE2596】R0560 创建特征类型→槽位节点"]
    C11["C11【函数调用 RCE2598】F0163 句柄有效(槽位节点)"]
    C12{"C12【函数调用 RCE2597】F0184 追根因检查槽位节点创建结果"}
    C13["C13【函数调用 RCE2599】F0167 创建宿主归属关系→宿主关系"]
    C14["C14【函数调用 RCE2600】F0168 句柄有效(宿主关系)"]
    C15{"C15【函数调用 RCE2597】F0184 追根因检查宿主关系写入结果"}
    C16["C16【函数调用 RCE2599】F0167 创建特征模板关系→类型关系"]
    C17["C17【函数调用 RCE2600】F0168 句柄有效(类型关系)"]
    C18{"C18【函数调用 RCE2597】F0184 追根因检查模板关系写入结果"}
    R19["R19【本函数内具体指令】返回槽位节点"]
    N01 --> B02
    B02 -- 是 --> R09
    B02 -- 否 --> C03
    C03 -- 否 --> R09
    C03 -- 是 --> C04
    C04 -- 否 --> R09
    C04 -- 是 --> C05 --> X06 --> X07 --> B08
    B08 -- 是 --> R09
    B08 -- 否 --> C10 --> C11 --> C12
    C12 -- 未通过 --> R09
    C12 -- 通过 --> C13 --> C14 --> C15
    C15 -- 未通过 --> R09
    C15 -- 通过 --> C16 --> C17 --> C18
    C18 -- 未通过 --> R09
    C18 -- 通过 --> R19
```

## 当前边界

- 第 243—244 行按 `||` 从左到右短路；只有前三项均不拒绝时才读取已有槽位，X04925 在该完整表达式末释放临时 optional。
- 节点创建、宿主关系创建或模板关系创建未达预期时 F0184 记录追根因结果；本函数没有撤销此前成功创建的节点或关系。
