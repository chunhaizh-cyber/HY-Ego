# CF-L6-0047 `因果服务::创建因果概念根` 逐行代码映射表

代码版本：`52e7496b0dab5a31ba0d4be9b727903541ee9373`
覆盖文件：`海中鱼巣/领域/因果服务.h:56-66`
根函数：F0368 `海中鱼巣::节点句柄 海中鱼巣::因果服务::创建因果概念根()`
配对流程图：`流程图/代码流程图/L6/CF-L6-0047_因果服务创建因果概念根_现状流程图.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/因果服务.h` | 56 | 进入成员函数，绑定隐式 `this` 及两个非拥有仓库引用；无显式参数 | S | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 最终设计需冻结结构化创建结果 |
| 同上 | 57 | 经 RCE1726 精确调用无参数 R0611 创建因果引用，并按值绑定返回的根节点句柄 | C01 / N02 | 函数调用 / 本函数内具体指令 | 结构写入 / 局部绑定 | R0611 可能创建旧主信息和因果引用节点 | 当前仓库结构 | 后续不成立属于写后内部错误 | 使用统一候选事务和精确撤销 |
| 同上 | 58 | 经 RCE1727 调用 F0190 读取根节点；即使根节点句柄无效也调用，并绑定 optional 记录 | C03 / N04 | 函数调用 / 本函数内具体指令 | 读取 / 局部绑定 | 无新增 | 节点仓当前读取 | 无记录进入后验失败 | 将创建结果与读回错误机械区分 |
| 同上 | 59 | 经 X02290 调用 `optional.has_value()`；开始按左到右短路形成传给 F0184 的条件，记录不存在时跳过后续两项 | X05 / B06 / N12 | 外部边界 / 本函数内具体指令 | 读取 / 判断 / 短路 | 无 | optional 局部材料 | 创建调用后的记录缺失为内部错误 | 保留写后追根因语义 |
| 同上 | 60 | 仅记录存在时经 X02291 `operator->` 读取类型，并判断是否为 `节点类型::因果引用` | X07 / B08 / N12 | 外部边界 / 本函数内具体指令 | 读取 / 判断 / 短路 | 无 | 节点记录投影 | 类型不符为内部错误 | 读回稳定身份、版本及因果域记录 |
| 同上 | 61 | 仅前两项成立时经 X02291 `operator->` 取得旧主信息句柄，再经 RCE1728 调用 F0624 检查其形态有效性 | X09 / C10 / B11 / N12 | 外部边界 / 函数调用 / 本函数内具体指令 | 读取 / 判断 / 短路 | 无 | 旧主信息结构 | 无效为内部错误 | 退出旧主信息并改用节点直接身份 |
| 同上 | 62 | 绑定固定说明字符串；在完整合取求值后经 RCE0151 调用 F0184，并对返回 bool 取反作为 `if` 条件 | C13 / B14 | 函数调用 / 本函数内具体指令 | 诊断 / 判断 | 无 | 人读诊断与局部控制结果 | 检查为 false 进入内部错误返回 | 强类型返回错误类别与原因 |
| 同上 | 63 | 检查不通过时返回默认无效节点句柄；不撤销 R0611 可能形成的结构 | RF | 本函数内具体指令 | 返回 | 已形成结构可能残留 | 当前代码结果材料 | 追根因解决 | 失败前精确撤销或隔离 |
| 同上 | 64 | 闭合后验失败分支 | B14 | 本函数内具体指令 | 分支闭合 | 无新增 | 当前控制流 | 不适用 | 无 |
| 同上 | 65 | 检查通过时按值返回根节点句柄 | RT | 本函数内具体指令 | 返回 | 无新增；既有结构继续可读 | 当前代码结果材料 | 不适用 | 返回完整发布回执而非裸句柄 |
| 同上 | 66 | 闭合函数作用域，销毁局部句柄和 optional；仓库结构不随局部析构撤销 | Z | 本函数内具体指令 | 作用域 / 生命周期 | 无新增 | 当前代码事实 | 不适用 | 明确仓库结构生命周期 |

覆盖结论：56—66 行全部映射；项目调用点 4 个，唯一被调身份 4 个；外部边界 2 类、3 个调用点，未解析项目调用 0。RCE1726—RCE1728、RCE0151、X02290、X02291 的重载身份、调用顺序与 `&&` 短路条件已逐项闭合。
