# CF-L6-0360 创建语素入口并保留收口材料现状流程图

图类型：现状流程图
代码版本：`0a93526882cb33c61c2fbb04ecad1aeaddcfe225`
工作区复核基线：`03a8b7ac099ccea83e57f2696e2290b7bcdfacaf`
源码 blob：`1355a1afd544cd44c14ee904246e9cb0778fb39b`
覆盖文件：`海中鱼巣/领域/语素服务.h:328-370`
唯一根函数：R0573
完整签名：`语素入口创建结果 海中鱼巣::语素服务::创建语素入口并保留收口材料(节点句柄 对应信息节点, std::uint64_t 主键)`
参数：`对应信息节点`、`主键` 均按值输入；主键为零时跳过索引占用检查与绑定
返回结果：成功时返回含语素入口、主信息、对应关系、主键及本次绑定标记的结果；各失败分支返回当前结果或值初始化空结果，并按已创建材料执行清理
已登记调用方：F0364（RCE0138）
直接项目函数：F0544（RCE1578）、F0331（RCE1579）、F0565（RCE3281）、F0332（RCE1581）、F0163（RCE3280）、F0167（RCE3282）、F0168（RCE1580）、R0574（RCE1582）、R0889（RCE3283）、R0728（RCE1583）、F0496（RCE1584）、R0570（RCE1585）、F0051（RCE3284）
外部边界：X04742 `std::optional<节点句柄>::has_value`、X04743 `value`、X04744 `~optional`
逐行映射表：`流程图/代码流程图/L6/CF-L6-0360_创建语素入口并保留收口材料_逐行代码映射表.md`
结构读写：依次创建主信息、语素节点、对应关系，可选绑定主键；失败时调用 R0574 撤销本次材料
不得作为施工许可：是
不得宣称：返回非空入口代表外层概念追溯关系或其它业务结构已建立

## 流程图

```mermaid
flowchart TD
    N01["N01【本函数内具体指令】初始化结果并记录主键"]
    B02{"B02【本函数内具体指令】主键是否非零"}
    C03["C03【函数调用 RCE1578】F0544 按主键查节点→已有节点"]
    X04{"X04【外部边界 X04742/X04744】已有节点.has_value()并在完整表达式末析构临时optional"}
    R05["R05【本函数内具体指令】返回仅含主键的当前结果"]
    C06["C06【函数调用 RCE1579】F0331 创建主信息→结果.主信息"]
    C07{"C07【函数调用 RCE3281】F0565 句柄有效(结果.主信息)"}
    C08["C08【函数调用 RCE1581】F0332 创建语素节点→结果.语素入口"]
    C09{"C09【函数调用 RCE3280】F0163 句柄有效(结果.语素入口)"}
    C10["C10【函数调用 RCE1582】R0574 清理本次创建材料"]
    C11["C11【函数调用 RCE3282】F0167 创建语素对应信息关系→结果.对应关系"]
    C12{"C12【函数调用 RCE1580】F0168 句柄有效(结果.对应关系)"}
    B13{"B13【本函数内具体指令】主键是否非零"}
    C14{"C14【函数调用 RCE3283】R0889 索引仓库::绑定主键(主键,语素入口)→本次绑定标记"}
    C15{"C15【函数调用 RCE1583】R0728 节点是语素入口(结果.语素入口)"}
    C16["C16【函数调用 RCE1584】F0496 读取入口绑定目标组→目标组"]
    C17{"C17【函数调用 RCE1585】R0570 节点组包含(目标组,对应信息节点)"}
    B18{"B18【本函数内具体指令】主键是否为零"}
    C19["C19【函数调用 RCE1578】F0544 两次按主键查节点→主键节点"]
    X20["X20【外部边界 X04742/X04743/X04744】主键节点.has_value()/value()并析构各已物化临时optional→节点句柄"]
    C21{"C21【函数调用 RCE3284】F0051 主键节点等于语素入口"}
    B22{"B22【本函数内具体指令】入口、关系与主键均可读"}
    N23["N23【本函数内具体指令】触发无法读回完整结构诊断"]
    R24["R24【本函数内具体指令】返回值初始化空结果"]
    R25["R25【本函数内具体指令】返回完整创建结果"]
    N01 --> B02
    B02 -- 否 --> C06
    B02 -- 是 --> C03 --> X04
    X04 -- 是 --> R05
    X04 -- 否 --> C06
    C06 --> C07
    C07 -- 否 --> R24
    C07 -- 是 --> C08 --> C09
    C09 -- 否 --> C10 --> R24
    C09 -- 是 --> C11 --> C12
    C12 -- 否 --> C10
    C12 -- 是 --> B13
    B13 -- 否 --> C15
    B13 -- 是 --> C14
    C14 -- 失败 --> C10
    C14 -- 成功 --> C15
    C15 --> C16 --> C17 --> B18
    B18 -- 是 --> B22
    B18 -- 否 --> C19 --> X20 --> C21 --> B22
    B22 -- 否 --> N23 --> C10
    B22 -- 是 --> R25
```

## 当前边界

- 项目调用边按实参数量和静态接收者绑定具体重载；X04742—X04744 覆盖临时 optional 的访问与完整表达式末析构。
- 失败收口只清理由当前结果记录的主键、关系、节点和主信息；主键预占用分支在任何创建前返回当前结果。
