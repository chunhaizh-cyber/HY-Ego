# CF-L6-0036 `概念图服务::登记生命周期状态` 逐行代码映射表

代码版本：`a48a70b2d678dea64dd8228adb4f26f0badd9506`
覆盖文件：`海中鱼巣/领域/概念图服务.h:934-967`
根函数：F0357 `std::optional<海中鱼巣::节点句柄> 海中鱼巣::概念图服务::登记生命周期状态(海中鱼巣::概念生命周期阶段 阶段, 海中鱼巣::节点句柄 状态节点, const 海中鱼巣::状态服务& 状态)`
配对流程图：`流程图/代码流程图/L6/CF-L6-0036_概念图服务登记生命周期状态_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/1160_根规范_状态节点_20260720.md`、`规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`、`规范/4020_子规范_领域类型化数据记录与组合读取投影边界.md`、`规范/4030_子规范_基础信息服务分层与领域写授权.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`、`规范/4210_子规范_动态信息分层获取与聚合_20260720.md`、`规范/7140_子规范_概念身份生命周期退役删除与活动投影分账.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/概念图服务.h` | 934—937 | 进入成员函数，按值绑定 `阶段`、`状态节点`，并绑定调用方持有的 const `状态` 服务和概念图服务 `this` 借用 | S | 本函数内具体指令 | 入口 / 参数与借用绑定 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 938 | 经 E1707 调用 F0190，以状态节点完整句柄读取值式节点记录并绑定局部 optional | C01 | 函数调用 | 节点读取 / 局部构造 | 无 | 当前节点读取结果 | 异常向调用方传播 | 无 |
| 同上 | 939 | 在任何 `if` 判断前无条件经 E1667 调用 F0329 读取旧状态 I64 值并绑定局部 optional | C02 | 函数调用 | 状态值读取 / 局部构造 | 无 | 已退出旧值槽读取结果 | 读不到值将在后续写前拒绝；异常传播 | 保持无条件调用顺序，不伪造短路 |
| 同上 | 940—944 | 两项读取已经完成后，经 E1868 调用 F0641；按 `||` 从左到右短路检查阶段有效、记录存在、节点类型为状态、状态值存在且等于阶段转成的 I64 | C03 / B04 | 函数调用 / 本函数内具体指令 | 纯计算 / 判断 / 短路 / 枚举转换 | 无 | 当前代码事实与旧状态值投影 | 任一不满足为写前入口拒绝 | 状态值应回到类型化状态记录 |
| 同上 | 945 | 仅在全部前序条件通过后，经 E1664 调用 F0328 并判断状态节点是否被旧关系分类为实例状态 | C05 / B06 | 函数调用 / 本函数内具体指令 | 状态分类读取 / 判断 | 无 | 已退出旧关系 5 分类结果 | `true` 为抽象状态登记拒绝 | 改由正式状态结构裁决抽象性 |
| 同上 | 946—947 | 任一准入条件不满足时形成并返回空 optional，函数闭合 | RN0 / Z | 本函数内具体指令 | 返回 / 作用域闭合 | 无 | 当前代码事实 | 入口拒绝 | 返回应具名区分拒绝原因 |
| 同上 | 949 | 构造 `unique_lock` 并取得 `生命周期状态锁_` 独占所有权 | X07 | 函数调用-外部边界 X0380 | 加锁 / 资源取得 | 仅同步资源变化 | 非权威同步材料 | 锁异常向调用方传播 | 无 |
| 同上 | 950 | 经 E1869 调用 F0642，把合法阶段转换为三槽数组索引并按值保存 | C08 | 函数调用 | 纯计算 / 局部构造 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 951 | 以索引读取三槽数组并绑定目标阶段 optional 槽的 const 引用 | X09 | 函数调用-外部边界 X0380 | 投影读取 / 引用绑定 | 无 | 非权威生命周期状态登记投影 | 不适用 | 无 |
| 同上 | 952 | 观察目标阶段槽是否已有登记并进入对应分支 | X10 / B11 | 外部边界 / 本函数内具体指令 | 判断 | 无 | 非权威登记投影 | 不适用 | 无 |
| 同上 | 953 | 目标槽有值时经 E1904 调用 F0051 比较完整状态节点；相同则复制已登记 optional 幂等返回，不同则形成空 optional 返回，随后释放独占锁 | C12A / B12 / RI / RN1 / D21 | 函数调用 / 本函数内具体指令 / 外部边界 | 强类型句柄比较 / 幂等读回或拒绝 / 解锁 / 返回 | 无新增结构 | 非权威登记投影 | 同义为幂等结果；异义为入口拒绝 | 强类型区分幂等与冲突 |
| 同上 | 954 | 闭合已有登记分支；只有目标槽为空才继续全数组扫描 | L13 | 本函数内具体指令 | 分支闭合 / 循环前置 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 955 | 建立范围迭代，按三槽数组物理顺序借用下一 optional 状态槽 | L13 | 本函数内具体指令 | 循环控制 / 借用 | 无 | 非权威登记投影 | 不适用 | 数组顺序不承担生命周期历史顺序 |
| 同上 | 956 | 按 `&&` 短路先检查当前槽有值；仅有值时经 E1904 调用 F0051 比较其完整状态节点与本次状态节点 | X14 / C15A / B15 | 外部边界 / 函数调用 / 本函数内具体指令 | 判断 / 短路 / 强类型句柄比较 | 无 | 非权威登记投影 | 跨阶段复用为写前拒绝 | 无 |
| 同上 | 957—958 | 发现状态节点已登记到任一阶段时返回空 optional，并释放独占锁 | RN2 / D21 / Z | 本函数内具体指令 / 外部边界 | 唯一性拒绝 / 解锁 / 返回 | 无 | 当前代码事实 | 入口拒绝 | 具名区分跨阶段复用 |
| 同上 | 959 | 当前槽无冲突时闭合分支并推进循环；数组耗尽后进入发布 | L16 / L13 | 本函数内具体指令 | 循环控制 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 960 | 把状态节点完整句柄复制到目标阶段 optional 槽 | X17 | 函数调用-外部边界 X0380 | 投影写入 | 改变一个生命周期状态定位投影槽；不写权威状态或关系 12 | 非权威可重建投影 | 不适用 | 不把登记数组提升为生命周期权威 |
| 同上 | 961 | 绑定刚写入的目标阶段 optional 槽 const 引用 | X18 | 函数调用-外部边界 X0380 | 投影读回 / 引用绑定 | 无新增结构 | 非权威可重建投影 | 不适用 | 无 |
| 同上 | 962—963 | 先检查已发布槽有值；仅有值时经 E1904 调用 F0051 比较状态节点；无论比较是否执行，完整布尔实参形成后都经 E1870 调用 F0184 | X18 / C18A / C19 / B20 | 外部边界 / 函数调用 / 本函数内具体指令 | 读回 / 短路 / 强类型句柄比较 / 诊断检查 | 无新增结构 | 当前代码事实；诊断不裁决机器事实 | 失败为发布后内部逻辑错误 | 必须保留已写结构证据并追根因 |
| 同上 | 964—965 | F0184 返回假时形成空 optional，释放生命周期状态登记独占锁后返回；不撤销 960 行写入 | RN3 / D21 / Z | 本函数内具体指令 / 外部边界 | 内部错误返回 / 解锁 | 已写登记投影保留 | 当前代码事实 | 追根因解决 | 不得包装为零变化普通失败 |
| 同上 | 966 | 读回检查通过时复制已发布 optional 形成返回对象，随后释放独占锁并按值返回 | RS / D21 | 本函数内具体指令 / 外部边界 | 返回物化 / 解锁 / 返回 | 调用方取得句柄副本；登记投影保持 | 非权威登记投影结果 | 不适用 | 返回有值不证明固定状态身份 |
| 同上 | 967 | 闭合函数作用域并析构局部节点记录、状态值、索引及锁对象；锁前异常向调用方传播 | Z / E | 本函数内具体指令 / 外部边界 | 资源释放 / 异常传播 | 释放局部资源；无新增机器结构 | 当前代码事实 | 异常发生在本次登记写入前 | 无 |

覆盖结论：934—967 行全部映射；直接项目被调函数 7 个唯一身份、9 个调用点，对应 E1707—E1870、E1904；F0051 的三次调用分别受目标槽、遍历槽和已发布槽 `has_value()` 短路门控。X0380 覆盖 optional、独占锁、枚举转换、数组遍历 / 下标、句柄复制、三元表达式与返回生命周期，未解析项目调用 0。已登记入边 E0742 已反向列明；`自检.概念命名需求.ixx:102-104` 只保留为调用方源码候选待展开，不分配 E、不计入聚合边。
