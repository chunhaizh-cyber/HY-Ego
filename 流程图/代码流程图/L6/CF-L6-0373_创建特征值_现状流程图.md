# CF-L6-0373 创建特征值现状流程图

图类型：现状流程图
代码版本：`03a8b7ac099ccea83e57f2696e2290b7bcdfacaf`
当前工作区复核基线：`ef4f7bda76c92c0eb11456cfa267d76f35810616`
源码 blob：`4c7bcd451f2e46e85d707a40c371dcbeaa2c1169`
覆盖文件：`海中鱼巣/领域/特征服务.h:333-354`
唯一根函数：R0763
完整签名：`节点句柄 海中鱼巣::特征服务::创建特征值(节点句柄 特征节点)`
参数：`特征节点` 按值输入；须匹配特征类型，且特征值服务与关系服务均已接入
返回结果：入口不满足、实例槽位已有当前值或任一步创建/检查失败时返回空句柄；成功时返回新建特征值节点
已登记调用方：R0249、R0847（RCE2570、RCE3136）
直接项目函数：R0776、R0778、R0777、R0782、F0184、F0163、F0167、F0168（RCE2601—RCE2608）
外部边界：X03163 `std::unique_lock<std::mutex>` 延迟加锁构造；X03164 `lock()`；X03165 `~unique_lock()`
逐行映射表：`流程图/代码流程图/L6/CF-L6-0373_创建特征值_逐行代码映射表.md`
结构读写：可读取实例槽位当前值；成功路径创建特征值节点及其到特征节点的归属关系
不得作为施工许可：是
不得宣称：返回空句柄时本函数必然没有留下新建特征值节点

## 流程图

```mermaid
flowchart TD
    N01["N01【本函数内具体指令】绑定特征节点"]
    C02{"C02【函数调用 RCE2601】R0776 节点类型匹配(特征节点,特征)"}
    B03{"B03【本函数内具体指令】特征值服务是否为空"}
    B04{"B04【本函数内具体指令】关系服务是否为空"}
    R05["R05【本函数内具体指令】返回空句柄"]
    X06["X06【外部边界 X03163】延迟构造实例槽位当前值写锁"]
    C07{"C07【函数调用 RCE2602】R0778 节点是否为实例特征槽位"}
    X08["X08【外部边界 X03164】写锁.lock()"]
    C09["C09【函数调用 RCE2603】R0777 读取实例槽位当前值材料"]
    B10{"B10【本函数内具体指令】当前值状态是否为无当前值"}
    C11["C11【函数调用 RCE2604】R0782 特征值_->创建特征值()→特征值节点"]
    C12["C12【函数调用 RCE2606】F0163 句柄有效(特征值节点)"]
    C13{"C13【函数调用 RCE2605】F0184 追根因检查特征值节点创建结果"}
    C14["C14【函数调用 RCE2607】F0167 创建归属关系→归属关系"]
    C15["C15【函数调用 RCE2608】F0168 句柄有效(归属关系)"]
    C16{"C16【函数调用 RCE2605】F0184 追根因检查归属关系写入结果"}
    X17["X17【外部边界 X03165】析构写锁并在已加锁路径释放互斥量"]
    R18["R18【本函数内具体指令】返回特征值节点"]
    N01 --> C02
    C02 -- 不匹配 --> R05
    C02 -- 匹配 --> B03
    B03 -- 是 --> R05
    B03 -- 否 --> B04
    B04 -- 是 --> R05
    B04 -- 否 --> X06 --> C07
    C07 -- 否 --> C11
    C07 -- 是 --> X08 --> C09 --> B10
    B10 -- 否 --> X17 --> R05
    B10 -- 是 --> C11
    C11 --> C12 --> C13
    C13 -- 未通过 --> X17 --> R05
    C13 -- 通过 --> C14 --> C15 --> C16
    C16 -- 未通过 --> X17 --> R05
    C16 -- 通过 --> X17 --> R18
```

## 当前边界

- 写锁以 `std::defer_lock` 构造，仅当特征节点是实例特征槽位时加锁并读取当前值；非实例槽位路径保持未加锁。
- X03165 覆盖构造后的所有正常返回路径；已加锁时释放互斥量，未加锁时只结束锁对象生命周期。
- 归属关系创建未达预期时，本函数返回空句柄但不撤销已成功创建的特征值节点。
