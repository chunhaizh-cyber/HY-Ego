# CF-L6-0292 `系统角色主体材料::完整` 逐行代码映射表

代码版本：`3920a74638264f9f539d50f32f3c9fe5fb1982ab`
源码 blob：`c26b12e291f7192b887ef01efce9e6dc157e9cef`
覆盖文件：`海中鱼巣/领域/系统角色清单.数据.h:188-210`
根函数：F0458 `bool 海中鱼巣::系统角色主体材料::完整() const noexcept`
配对流程图：`流程图/代码流程图/L6/CF-L6-0292_系统角色主体材料完整_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/系统角色清单.数据.h` | 188 | 进入 const、`noexcept` 成员函数 F0458，借用当前主体材料 | S | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 189 | 经 RCE0232 依次调用 R0563 检查世界根、自我场景、自我存在身份材料完整性，任一 false 即短路 | C01 / C02 / C03 / B15 / RF | 函数调用 / 本函数内具体指令 | 只读调用 / 短路判断 / 返回 | 无 | 当前值式材料 | 不完整为逻辑内返回 | 已登记 |
| 同上 | 190 | 经 RCE2174 调用 F0459 检查场景接纳自我关系，并经 RCE2175 调用 R0727 检查安全根需求 | C04 / C05 / B15 / RF | 函数调用 / 本函数内具体指令 | 只读调用 / 短路判断 | 无 | 当前值式材料 | 不完整为逻辑内返回 | 已登记 |
| 同上 | 191 | 经 RCE2175 调用 R0727 检查服务根需求，再经 RCE0232 调用 R0563 检查概念存在根 | C06 / C07 / B15 / RF | 函数调用 / 本函数内具体指令 | 只读调用 / 短路判断 | 无 | 当前值式材料 | 不完整为逻辑内返回 | 已登记 |
| 同上 | 192 | 经 RCE0232 调用 R0563 检查概念动态根、概念关系根和概念因果根 | C08 / C09 / C10 / B15 / RF | 函数调用 / 本函数内具体指令 | 只读调用 / 短路判断 | 无 | 当前值式材料 | 不完整为逻辑内返回 | 已登记 |
| 同上 | 193 | 经 RCE0232 调用 R0563 检查方法登记根和方法登记角色状态 | C11 / C12 / B15 / RF | 函数调用 / 本函数内具体指令 | 只读调用 / 短路判断 | 无 | 当前值式材料 | 不完整为逻辑内返回 | 已登记 |
| 同上 | 194 | 经 RCE0232 调用 R0563 检查方法活跃状态和方法失效状态；上述任一调用为 false 时立即返回 false | C13 / C14 / B15 / RF | 函数调用 / 本函数内具体指令 | 只读调用 / 判断 / 返回 | 无 | 当前值式材料 | 不完整为逻辑内返回 | 已登记 |
| 同上 | 196 | 经 RCE0233 调用 R0564，按值形成包含 21 个 const 身份材料指针的数组 | C16 | 函数调用 | 只读投影 / 局部构造 | 仅形成局部指针数组 | 非拥有局部投影 | 不适用 | 无 |
| 同上 | 197 | 经 X02490 调用 `身份组.front()` 取得首身份指针并读取其节点仓库编号作为基准 | C17 / N18 | 函数调用 / 本函数内具体指令 | 外部边界 / 读取 | 无 | 当前值式材料 | 不适用 | 已登记 |
| 同上 | 198 | 初始化左索引为 0；经 X02491 调用 `身份组.size()`，左索引未到尾时进入外层循环 | X19 / L19 | 函数调用 / 本函数内具体指令 | 循环控制 / 外部容器读取 | 仅改变局部索引 | 临时局部变量 | 不适用 | 已登记 |
| 同上 | 199 | 经 X02492 两次调用 `operator[]` 取得左身份，经 RCE0232 调用 R0563 复核完整性，并比较节点仓库编号与基准；失败返回 false | X20A / C20 / B20 / X20B / B21 / RF | 函数调用 / 本函数内具体指令 | 外部边界 / 只读调用 / 判断 / 返回 | 无 | 当前值式材料 | 不一致为逻辑内返回 | 已登记 |
| 同上 | 200 | 初始化右索引为 `左 + 1`；经 X02491 调用 `身份组.size()` 控制内层循环 | X22 / L22 / N28 | 函数调用 / 本函数内具体指令 | 循环控制 / 纯计算 | 仅改变局部索引 | 临时局部变量 | 不适用 | 已登记 |
| 同上 | 201 | 经 X02492 两次调用 `operator[]` 读取左右稳定主键并比较；相等则返回 false | X23A / X23B / B23 / RF | 函数调用 / 本函数内具体指令 | 外部边界 / 值比较 / 返回 | 无 | 当前值式材料 | 重复键为逻辑内返回 | 已登记 |
| 同上 | 202 | 经 X02492 两次调用 `operator[]` 取得左右节点句柄，再经 RCE2176 调用 F0051 比较；相等是重复条件之一 | X24A / X24B / C24 / B26 / RF | 函数调用 / 本函数内具体指令 | 外部边界 / 句柄比较 / 判断 | 无 | 当前值式材料 | 重复节点为逻辑内返回 | 已登记 |
| 同上 | 203 | 经 X02492 两次调用 `operator[]` 取得左右主信息句柄，再经 RCE2177 调用 R0615 比较；键、节点或主信息任一相等即返回 false | X25A / X25B / C25 / B26 / RF | 函数调用 / 本函数内具体指令 | 外部边界 / 句柄比较 / 返回 | 无 | 当前值式材料 | 重复主信息为逻辑内返回 | 已登记 |
| 同上 | 204 | 关闭内层循环体；未发现重复时右索引加 1 并继续 | N27 / L22 | 本函数内具体指令 | 循环控制 | 仅改变局部索引 | 临时局部变量 | 不适用 | 无 |
| 同上 | 205 | 关闭外层循环体；当前左项完成比较后左索引加 1 | N28 / L19 | 本函数内具体指令 | 循环控制 | 仅改变局部索引 | 临时局部变量 | 不适用 | 无 |
| 同上 | 206 | 判断场景接纳自我关系类型是否为 `归属`；false 将最终结果短路为 false | B29 / RF | 本函数内具体指令 | 判断 | 无 | 当前值式材料 | 关系类型不符为逻辑内返回 | 无 |
| 同上 | 207 | 经 RCE2176 调用 F0051 比较接纳关系源节点与自我场景节点 | C30 / B32 / RF | 函数调用 / 本函数内具体指令 | 句柄比较 / 短路判断 | 无 | 当前值式材料 | 端点不符为逻辑内返回 | 已登记 |
| 同上 | 208 | 经 RCE2176 调用 F0051 比较接纳关系目标节点与自我存在节点 | C31 / B32 / RF | 函数调用 / 本函数内具体指令 | 句柄比较 / 短路判断 | 无 | 当前值式材料 | 端点不符为逻辑内返回 | 已登记 |
| 同上 | 209 | 比较接纳关系顺序号是否为 0，并返回整个关系一致性合取结果 | B32 / RF / RT | 本函数内具体指令 | 判断 / 返回 | 无 | 当前值式材料 | 顺序不符为逻辑内返回 | 无 |
| 同上 | 210 | 函数词法作用域闭合，局部身份数组结束生命周期 | RF / RT | 本函数内具体指令 | 作用域 / 生命周期 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：F0458 的 188—210 行全部映射；图中全部调用、判断、循环与返回节点均有映射。RCE0232、RCE2174—RCE2177、RCE0233 与 X02490—X02492 均已绑定到对应源码调用点。
