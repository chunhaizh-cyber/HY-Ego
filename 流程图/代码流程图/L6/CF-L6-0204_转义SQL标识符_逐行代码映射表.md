# CF-L6-0204 `转义SQL标识符` 逐行代码映射表

代码版本：`3686cc2882ba42216609174c8e45e59b684e2958`
覆盖文件：`海中鱼巣/适配/SQL数据库适配.cpp:101-111`
根函数：F0380 `std::wstring 海中鱼巣::{匿名命名空间@SQL数据库适配.cpp}::转义SQL标识符(std::wstring_view 文本)`
配对流程图：`流程图/代码流程图/L6/CF-L6-0204_转义SQL标识符_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/8120_子规范_程序入口启动模式与运行宿主边界_20260726.md`、`规范/详细设计/SQLServer结构审计投影与控制面板查看详细设计.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/适配/SQL数据库适配.cpp` | 101 | 进入匿名命名空间函数 F0380，按值复制非拥有 `wstring_view` 视图 | S | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 调用方保持底层字符存储有效 |
| 同上 | 102 | 经 X01001 默认构造空 `std::wstring 结果` | X01 | 外部边界 | 容器构造 / 生命周期 | 只形成局部字符串 | 临时局部变量 | 构造异常按库合同传播 | 不为标准库函数生成项目图 |
| 同上 | 103 | 经 X02306 调用 `文本.size()` 取得输入长度，再经 X02305 调用 `结果.reserve(...)` 预留等量容量 | X02 / X03 | 外部边界 | 读取 / 容量操作 | 只改变局部字符串容量 | 临时局部变量 | 长度或分配异常向外传播 | 预留量不含新增右方括号数量 |
| 同上 | 104 | 范围循环判断仍有下一代码单元后，经 X02307 解引用迭代器并按值绑定当前 `wchar_t` | L04 / X05 | 本函数内具体指令 / 外部边界 | 循环 / 读取 | 无 | 当前代码事实 | 空输入是合法结果 | 不把代码单元迭代冒充 Unicode 字符规范化 |
| 同上 | 105 | 经 X02308 对每个输入代码单元调用 `push_back` 追加原字符 | X06 | 外部边界 | 容器追加 | 改变局部结果字符串 | 临时局部变量 | 分配异常向外传播 | 保持输入顺序 |
| 同上 | 106 | 判断当前字符是否为右方括号 `L']'` | D07 | 本函数内具体指令 | 判断 | 无 | 当前代码事实 | 不适用 | 只裁决一个 SQL 方括号标识符层 |
| 同上 | 107 | 右方括号分支再次经 X02308 调用 `push_back(L']')`，把一个右方括号扩为连续两个 | X08 | 外部边界 | 容器追加 / 转义 | 改变局部结果字符串 | 非权威 SQL 文本材料 | 分配异常向外传播 | 重复应用会继续加倍 |
| 同上 | 108—109 | 闭合右方括号分支和逐字符循环；继续下一输入代码单元 | D07 / L04 | 本函数内具体指令 | 分支 / 循环控制 | 无额外变化 | 当前代码事实 | 不适用 | 无 |
| 同上 | 110 | 按值返回局部 `结果`；发生 NRVO 时直接在调用方返回存储构造，不调用 X01002；NRVO 未发生时经 X01002 移动构造调用方拥有的 `wstring` | R09 / X10 | 本函数内具体指令 / 条件外部边界 | 返回 / 条件返回值形成 | 调用方取得独立结果所有权 | 非权威 SQL 文本材料 | 不适用 | 不宣称 SQL 已执行 |
| 同上 | 111 | 词法闭合函数定义；R09 或条件 X10 已是返回路径终点，输入视图不拥有也不释放源字符存储 | R09 / X10 | 本函数内具体指令 / 条件外部边界 | 作用域 / 生命周期 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：F0380 的 101—111 行全部映射；项目出边 0；E0817 入边以及 X01001、X01002、X02305—X02308 六个正式外部边界均已记录。
