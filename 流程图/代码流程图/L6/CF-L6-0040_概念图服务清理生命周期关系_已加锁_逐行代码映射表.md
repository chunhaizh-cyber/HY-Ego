# CF-L6-0040 `概念图服务::清理生命周期关系_已加锁` 逐行代码映射表

代码版本：`a48a70b2d678dea64dd8228adb4f26f0badd9506`
覆盖文件：`海中鱼巣/领域/概念图服务.h:3220-3240`
根函数：F0361 `bool 海中鱼巣::概念图服务::清理生命周期关系_已加锁(const std::vector<海中鱼巣::关系句柄>& 关系组)`
配对流程图：`流程图/代码流程图/L6/CF-L6-0040_概念图服务清理生命周期关系_已加锁_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`、`规范/4040_子规范_不透明结构事务候选确认撤销与最后发布.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`、`规范/4060_子规范_非权威缓存统计失效与确定重建.md`、`规范/7140_子规范_概念身份生命周期退役删除与活动投影分账.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/概念图服务.h` | 3220 | 进入可变成员函数，绑定 `this` 与调用方拥有的 const `关系组` 借用；函数名约定调用方已持活动图独占锁和图写锁，本函数不取得或验证两把锁 | S | 本函数内具体指令 | 入口 / 参数与锁前置绑定 | 无 | 当前代码事实 | 锁前置不成立属于调用协议内部错误 | 冻结并验证所有已加锁调用方 |
| 同上 | 3221 | 初始化局部累计结果 `全部完成=true` | N01 | 本函数内具体指令 | 局部赋值 | 只改变局部 bool | 临时局部变量 | 不适用 | 无 |
| 同上 | 3222 | 以 `rbegin()` 建立输入 vector 逆向迭代器；每轮与 `rend()` 比较并在轮末递增 | X02 / L03 / X20 | 外部边界 X0384 / 本函数内具体指令 | 逆序循环控制 | 无 | 当前代码控制流 | 空输入逻辑内返回 `true` | 保留精确逆序清理 |
| 同上 | 3223 | 解引用当前输入逆向迭代器，把关系句柄平凡复制为局部 `待清理关系` | X04 | 函数调用-外部边界 X0384 | 读取 / 值式复制 | 只形成局部值 | 临时局部变量 | 不适用 | 无 |
| 同上 | 3224-3226 | 调用 `std::find_if` 遍历生命周期关系登记投影；简单捕获 lambda 经 E1908 按需调用 R0598，逐项比较 `登记.关系 == 待清理关系`，返回首个匹配位置或 `end()` | X05 / C06 | 函数调用-外部边界 X0384 / 函数调用 E1908 | 投影读取 / 回调 / 强类型关系句柄比较 / 迭代器形成 | 无 | 非权威生命周期关系登记投影 | 当前平凡迭代与 R0598 不抛出 | 登记命中不得替代关系仓权威读回 |
| 同上 | 3227 | 判断查找位置是否等于登记 vector 的 `end()` | B07 | 本函数内具体指令 | 判断 | 无 | 非权威登记投影 | 登记缺失在本轮补偿语境属于追根因解决 | 无 |
| 同上 | 3228 | 登记缺失时把累计结果置为 `false`；先前失败状态不会被后续成功重置 | N08 | 本函数内具体指令 | 局部赋值 / 失败累计 | 只改变局部 bool | 临时局部变量 | 追根因解决 | 返回结果应具名携带失败句柄与原因 |
| 同上 | 3229 | 执行 `continue`，跳过关系仓删除 / 读取和登记擦除，推进到下一逆序输入句柄 | L09 / X20 | 本函数内具体指令 / 外部边界 X0384 | 循环跳转 / 失败继续 | 当前登记和关系仓均不因本项改变 | 当前代码事实 | 追根因解决；继续其它精确项清理 | 保留失败继续但形成残留清单 |
| 同上 | 3230 | 闭合登记缺失分支；登记命中路径继续关系仓操作 | B07 / C10 | 本函数内具体指令 | 分支闭合 | 无 | 当前代码控制流 | 不适用 | 无 |
| 同上 | 3231 | 经 E1901 调用 F0590 删除当前关系；返回 `true` 时把当前项视为已清理并按 `||` 短路，不调用 F0587 | C10 / B11 / N15 | 函数调用 / 本函数内具体指令 | 关系写入 / 判断 / 短路 | F0590 成功时关系记录状态改为已删除、版本加一并即时可见；登记尚未擦除 | 关系仓当前权威结构写入 | F0590 `false` 需继续读回；异常向上传播并可能保留先前部分清理 | 使用结构化删除结果和完整事务补偿证据 |
| 同上 | 3232 | 仅在 F0590 返回 `false` 时经 E1902 调用 F0587；读取旧完整句柄的普通当前关系并调用 `has_value()`，空值时把当前项视为已清理 | C12 / X13 / B14 / N15 / N16 | 函数调用 / 外部边界 X0384 / 本函数内具体指令 | 关系读回 / 短路判断 / 局部结果物化 | 只读取关系仓；不改变关系结构 | 关系仓普通当前读取 | 空值收敛为逻辑内幂等结果；仍有值为追根因解决 | 区分不存在、已删除、旧版本、错域、端点失效及其它空值原因 |
| 同上 | 3233 | 判断局部 `已清理` 是否为 `false` | B17 | 本函数内具体指令 | 判断 | 无 | 当前代码控制流 | 不适用 | 无 |
| 同上 | 3234 | 删除失败且旧句柄仍可读时，把累计结果置为 `false` | N18 | 本函数内具体指令 | 局部赋值 / 失败累计 | 只改变局部 bool | 临时局部变量 | 前置成立后的内部清理错误 | 返回具名失败材料 |
| 同上 | 3235 | 执行 `continue`，保留当前生命周期关系登记并继续逆序处理其它输入句柄 | L09 / X20 | 本函数内具体指令 / 外部边界 X0384 | 循环跳转 / 失败继续 | 当前关系和登记保持本项调用后的实际状态 | 当前代码事实 | 追根因解决；其它项继续尝试 | 保留失败关系与读回证据 |
| 同上 | 3236 | 闭合未清理分支；已清理路径继续擦除登记 | B17 / X19 | 本函数内具体指令 | 分支闭合 | 无 | 当前代码控制流 | 不适用 | 无 |
| 同上 | 3237 | 对命中的生命周期关系登记位置调用 vector `erase`；登记材料为三个平凡句柄，当前擦除搬移不抛出 | X19 | 函数调用-外部边界 X0384 | 非权威投影写入 / 局部容器搬移 | 从 `生命周期关系登记组_` 擦除当前关系的反向索引 / 调试投影 | 非权威生命周期关系登记投影 | 不适用 | 擦除投影不得被解释为权威关系历史删除 |
| 同上 | 3238 | 闭合循环体，推进输入逆向迭代器并重复；失败项不阻止后续项继续 | X20 / L03 | 外部边界 X0384 / 本函数内具体指令 | 循环控制 / 失败继续 | 已完成项的逐条关系删除和登记擦除保持可见 | 当前代码控制流 | 累计失败仍继续 | 异常路径需形成可恢复的剩余写集 |
| 同上 | 3239 | 输入耗尽后按值返回累计 `全部完成`；不对已删除关系、保留登记或关系数量执行额外统一读回 | R21 | 本函数内具体指令 | 返回 | 无新增变化；此前逐项变化保持 | 当前代码结果材料 | `false` 为内部清理异常汇总；空输入和全部收敛返回 `true` | 强类型返回已删除组、已不存在组、失败组和剩余登记组 |
| 同上 | 3240 | 闭合函数作用域；局部值和迭代器失效，本函数不释放调用方持有的活动图锁 / 图写锁；异常会保留此前已发生的部分清理并向上传播 | E / Z | 本函数内具体指令 | 作用域闭合 / 异常传播 | 可能保留此前逐项权威删除和非权威登记擦除 | 当前代码事实 | 异常为内部逻辑错误 | 补偿异常必须按 4040 形成精确剩余写集、隔离或根因收口 |

覆盖结论：3220—3240 行全部映射；直接项目被调函数为 R0598、F0590、F0587，共 3 个唯一函数、3 条冻结调用边。E1908 由 `find_if` 对登记项按需回调；E1901 在登记命中后调用，E1902 只在 E1901 返回 `false` 时调用，删除成功路径严格短路。X0384 覆盖逆向迭代、`find_if` / 简单 lambda 调度、optional、登记擦除和迭代器生命周期，未解析项目调用 0。当前已登记入边 E0753；`:4163` 所在 `清理未发布候选_已加锁` 当前不在 `main` 可达身份表中，本批不新增入边或计入可达聚合。
