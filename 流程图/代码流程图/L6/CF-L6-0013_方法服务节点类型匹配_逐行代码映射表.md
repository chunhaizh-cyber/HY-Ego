# CF-L6-0013 `方法服务::节点类型匹配` 逐行代码映射表

代码版本：`a48a70b2d678dea64dd8228adb4f26f0badd9506`
覆盖文件：`海中鱼巣/领域/方法服务.h:1834-1837`
根函数：F0333 `bool 海中鱼巣::方法服务::节点类型匹配(海中鱼巣::节点句柄 节点句柄值, 海中鱼巣::节点类型 类型) const`
配对流程图：`流程图/代码流程图/L6/CF-L6-0013_方法服务节点类型匹配_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/3300_根规范_方法_20260720.md`、`规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`、`规范/5300_子规范_方法登记选择与执行规则_20260720.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/方法服务.h` | 1834 | 进入 const 方法服务成员函数，按值绑定节点句柄和目标类型并借用隐式 `this` | S | 本函数内具体指令 | 入口 / 借用 | 无 | 当前实现事实 | 不适用 | 不把类型比较扩大为完整方法事实 |
| 同上 | 1835 | 调用 F0190 读取节点记录，按值形成局部 `optional<节点记录>` | C01 | 函数调用 | 节点读取 / 局部构造 | 无 | 当前节点事实读取候选 | 不适用 | 保留完整句柄、版本和状态复核 |
| 同上 | 1836 | 调用 X0365 `has_value()`；为空时由 `&&` 短路并返回 `false` | X02 / B03 / RF0 | 函数调用 / 本函数内具体指令 | 判断 / 返回 | 无 | 当前实现读取 | 普通查询为逻辑内返回；强前置语境为内部错误 | 按调用语境分账 |
| 同上 | 1836 | 记录存在时调用 X0365 `operator->()`，比较记录类型与实参类型；相等返回 `true`，不等返回 `false` | X04 / B05 / RT / RF1 | 函数调用 / 本函数内具体指令 | 借用 / 读取 / 判断 / 返回 | 无 | 当前节点类型读取 | 同上 | 另行验证业务角色、稳定主键和完整结构 |
| 同上 | 1837 | 闭合函数体并析构局部 `optional<节点记录>` | Z | 本函数内具体指令 | 作用域闭合 / 生命周期 | 无 | 当前实现事实 | 不适用 | 无 |

覆盖结论：1834—1837 行全部映射；项目被调函数 1 个、调用点 1 个，外部边界 X0365 一个（聚合节点句柄隐式按值复制、`optional` 存在检查 / 借用、枚举比较和 bool 短路返回），未解析调用 0。E0636、E1658、E1668、E1671 四条入边与 E1684 出边双向闭合。
