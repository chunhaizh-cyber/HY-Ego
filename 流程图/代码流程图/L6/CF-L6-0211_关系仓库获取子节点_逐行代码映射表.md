# CF-L6-0211 `关系仓库::获取子节点` 逐行代码映射表

代码版本：`2c38ec4017e38b2e541b93c8c9ae038856f849c7`
源码 blob：`f7a9ea9a09d3065468208c16f9ea34f806b6e183`
覆盖文件：`海中鱼巣/核心/关系仓库.cpp:733-758`；宏展开依据同文件 `116-123`
根函数：F0387 `std::vector<海中鱼巣::节点句柄> 海中鱼巣::关系仓库::获取子节点(海中鱼巣::节点句柄 父节点) const`
配对流程图：`流程图/代码流程图/L6/CF-L6-0211_关系仓库获取子节点_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`、`规范/4040_子规范_不透明结构事务候选确认撤销与最后发布.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/关系仓库.cpp` | 733 | 进入 const 成员函数 F0387；非拥有借用 `this`，按值接收 `节点句柄 父节点` | S | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 不保存仓库或句柄引用 |
| 同上 | 734 | 展开 `关系共享许可范围({})`：依次声明空 `自动许可` 与空 `自动令牌范围`；经 RCE1750 读取是否接域，经 RCE1751 读取既有关系令牌；必要时经 RCE1752 取得共享许可、RCE1756 移动构造承载值、E1818 在完整表达式末析构已移空临时许可，再经 RCE1753 复核许可有效、RCE1754 读取令牌并经 RCE1755 构造令牌范围。许可无效时先形成空向量，随后经 E1818 析构已承载许可再返回；许可取得或构造异常按已构造状态展开 | M01 / C02 / B03 / C04 / B05 / C06 / C07 / D08 / C09 / B10 / C11 / C12 / N32 / K33 / B34 / D35 / B36 / D37 / B38 / R39 / E40 | 函数调用 / 外部边界 / 本函数内具体指令 | 宏展开 / 接线读取 / 许可取得 / 令牌语境 / RAII / 返回 / 异常传播 | 无；只建立局部共享读取语境 | 当前事务接线、令牌语境和许可 | 许可无效折叠为空向量；异常不包装为空结果 | RCE1750—RCE1756 与 E1818 已闭合宏内项目调用和两次许可析构语境 |
| 同上 | 735 | 默认构造空 `std::vector<节点句柄> 子节点候选` | N13 | 外部边界-聚合缺口 | 局部容器构造 | 无 | 临时局部变量 | 不适用 | 当前外部边界表未给 vector 构造独立 ID |
| 同上 | 736 | 经 E1765 调用 F0343 复核 `父节点` 当前有效性，并对返回 bool 取反决定是否提前返回 | C14 / B15 | 函数调用 / 本函数内具体指令 | 读取 / 判断 | 无 | 当前节点仓库读取结果 | 父节点无效时返回空候选；强上游有效前置下需沿句柄当前性追根因 | E1765 同时承载本行和 753 行两个调用点 |
| 同上 | 737 | 父节点无效时以当前空 `子节点候选` 形成返回值，并进入局部资源、E1790、E1818 的条件清理序列 | N32 / K33 / B34 / D35 / B36 / D37 / B38 / R39 | 本函数内具体指令 / 函数调用 | 返回 / RAII | 无 | 当前只读结果 | 裸空向量合并父节点拒绝与空候选 | 不把空向量解释为关系不存在 |
| 同上 | 738 | 闭合父节点无效分支；运行时只有未返回路径继续 | B15 | 本函数内具体指令 | 分支闭合 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 739 | 打开关系仓库共享锁内层词法作用域；本行本身不取得锁 | X17 / U23 | 本函数内具体指令 | 作用域 / 生命周期 | 无 | 当前控制流 | 不适用 | 明确锁只覆盖 740-749 |
| 同上 | 740 | 经 X01019 以 `仓库锁_` 构造 `std::shared_lock<std::shared_mutex> 锁`；取得失败时异常直接进入逆序清理 | X17 / K33 / E40 | 外部边界 / 本函数内具体指令 | 锁取得 / 异常传播 | 无；建立关系表只读窗口 | 当前仓库锁 | 锁取得异常不包装为空向量 | X01019 是本函数唯一已登记稳定外部边界 |
| 同上 | 741 | 对 `关系表_` 开始范围迭代并判断是否还有条目；迭代结束时离开循环 | L18 / N22 | 外部边界-聚合缺口 | 循环控制 / 迭代 | 无 | 当前关系表读取窗口 | 不适用 | begin/end/递增未取得独立稳定外部边界 ID |
| 同上 | 742 | 从当前条目 `second` 非拥有借用 const `关系记录`；引用不越过共享锁与循环体 | N19 | 本函数内具体指令 | 记录读取 / 临时绑定 | 无 | 当前关系记录 | 不适用 | 不把记录引用返回调用方 |
| 同上 | 743 | 判断当前关系记录状态是否等于 `有效`；false 时短路后续类型与源节点比较 | B20 | 本函数内具体指令 | 判断 / 短路 | 无 | 当前关系记录 | 记录非有效不进入普通父子候选 | 不把过滤写成关系删除 |
| 同上 | 744 | 仅状态有效时判断关系类型是否等于 `普通父子`；false 时短路源节点比较 | B20 | 本函数内具体指令 | 判断 / 短路 | 无 | 当前关系记录 | 非普通父子不属于本查询候选 | 类型过滤服从 4010 关系 ABI |
| 同上 | 745 | 仅前两项成立时比较记录源节点与输入父节点；三项全为 true 才进入候选追加 | B20 | 本函数内具体指令 | 句柄比较 / 判断 | 无 | 当前关系记录与输入句柄 | 源节点不匹配是普通过滤 | 不把本查询结果扩展为其它关系类型 |
| 同上 | 746 | 把匹配记录的目标节点按值追加到 `子节点候选`；分配或复制异常时先释放共享锁，再按逆序释放其余局部资源并传播 | X21 / K33 / B34 / D35 / B36 / D37 / B38 / E40 | 外部边界-聚合缺口 / 函数调用 / 本函数内具体指令 | 容器追加 / 异常传播 / RAII | 只改变临时局部候选 vector | 临时局部变量 | 异常不包装为空结果 | `push_back` 未取得稳定外部边界 ID |
| 同上 | 747 | 闭合三项关系条件分支；未追加路径直接推进关系表迭代 | B20 / N22 | 本函数内具体指令 / 外部边界-聚合缺口 | 分支闭合 / 循环推进 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 748 | 闭合关系表循环体；仍有条目时回到 L18，否则退出循环 | N22 / L18 | 外部边界-聚合缺口 | 循环控制 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 749 | 离开内层作用域并析构 `锁`；从此不再持有关系仓库共享锁 | U23 | 外部边界-聚合缺口 | 锁释放 / 资源生命周期 | 无 | 当前仓库锁 | 不适用 | 共享锁析构尚无稳定外部边界 ID；后继节点复核发生在锁外 |
| 同上 | 750 | 默认构造空 `std::vector<节点句柄> 子节点`，作为过滤后的返回材料 | N24 | 外部边界-聚合缺口 | 局部容器构造 | 无 | 临时局部变量 | 不适用 | 当前外部边界表未给 vector 构造独立 ID |
| 同上 | 751 | 读取 `子节点候选.size()` 并为 `子节点` 预留等量容量；分配异常时按逆序释放局部资源和许可语境后传播 | X25 / K33 / B34 / D35 / B36 / D37 / B38 / E40 | 外部边界-聚合缺口 / 函数调用 / 本函数内具体指令 | 容量读取 / 预留 / 异常传播 | 只改变临时返回 vector 容量 | 临时局部变量 | 异常不包装为空结果 | `size/reserve` 未取得稳定外部边界 ID |
| 同上 | 752 | 对候选 vector 开始范围迭代；判断是否还有候选并按 const 引用绑定当前候选 | L26 / N27 / N31 | 外部边界-聚合缺口 / 本函数内具体指令 | 循环控制 / 临时绑定 | 无 | 临时局部变量 | 不适用 | 候选引用不越过 vector 生命周期 |
| 同上 | 753 | 经同一稳定边 E1765 再次调用 F0343，复核当前候选节点有效性并决定是否追加 | C28 / B29 | 函数调用 / 本函数内具体指令 | 读取 / 判断 | 无 | 当前节点仓库读取结果 | 候选失效时从结果中过滤；强事务前置下需核对跨仓一致性 | 本次复核发生在关系共享锁释放后 |
| 同上 | 754 | 仅候选仍有效时把候选句柄按值追加到返回 vector；分配或复制异常时按逆序释放局部资源和许可语境后传播 | X30 / K33 / B34 / D35 / B36 / D37 / B38 / E40 | 外部边界-聚合缺口 / 函数调用 / 本函数内具体指令 | 容器追加 / 异常传播 / RAII | 只改变临时返回 vector | 临时局部变量 | 异常不包装为空结果 | `push_back` 未取得稳定外部边界 ID |
| 同上 | 755 | 闭合候选有效分支；无论是否追加都继续推进候选迭代器 | B29 / N31 | 本函数内具体指令 / 外部边界-聚合缺口 | 分支闭合 / 循环推进 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 756 | 闭合候选循环体；仍有候选时回到 L26，否则形成返回值 | N31 / L26 | 外部边界-聚合缺口 | 循环控制 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 757 | 以过滤后的 `子节点` 形成返回值；随后释放仍存局部 vector。若宏建立了令牌范围与许可，严格先经 E1790 析构 F0344，再经 E1818 析构 F0345，最后按值返回 | N32 / K33 / B34 / D35 / B36 / D37 / B38 / R39 | 本函数内具体指令 / 函数调用 | 返回 / RAII | 无；返回调用方拥有的句柄 vector | 当前只读结果 | 空结果可来自合法空候选或前述拒绝，不可唯一归因 | 保持 F0344 在 F0345 之前的逆序析构 |
| 同上 | 758 | 词法闭合函数定义；R39 或 E40 已是运行终点，不存在返回 / 异常传播后的继续执行节点 | R39 / E40 | 本函数内具体指令 | 作用域 / 生命周期 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：733—758 共 26 行全部映射；一个 Mermaid 图的 40 个节点均在本表回指。正式稳定项目出边为 E1765、E1790、E1818、RCE1750—RCE1756，入边为 E0851，外部边界为 X01019；宏展开项目调用和两次许可析构语境已经闭合，容器 / 迭代 / 共享锁释放等未获稳定 X 身份的标准库操作仍按当前聚合口径精确标注。关系共享锁在 749 行先释放；函数离开时已承载关系令牌范围经 E1790 先于已承载许可经 E1818 析构。
