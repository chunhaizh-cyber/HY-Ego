# CF-L6-0001 `结构事务接线::接线形态有效` 逐行代码映射表

代码版本：`a48a70b2d678dea64dd8228adb4f26f0badd9506`
覆盖文件：`海中鱼巣/核心/结构事务接线.数据.h:82-84`
根函数：F0321 `bool 海中鱼巣::结构事务接线::接线形态有效() const`
配对流程图：`流程图/代码流程图/L6/CF-L6-0001_结构事务接线接线形态有效_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4040_子规范_不透明结构事务候选确认撤销与最后发布.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`、`规范/详细设计/不透明结构写入会话许可内探测计量与全参与者原子收口详细设计.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/结构事务接线.数据.h` | 82 | 声明并进入 const 成员函数，绑定调用方拥有且调用期有效的隐式 `this` | S | 本函数内具体指令 | 入口 / 借用 | 无 | 当前代码事实 | 不适用 | 不把 `const` 误写成并发快照保证 |
| 同上 | 83 | 调用 F0618 `完全未接域()`，同一隐式 `this`，按值取得 `bool` | C01 | 函数调用 | 读取 / 调用 | 无 | 当前代码事实 | 不适用 | F0618 另建图，不复制八项判空正文 |
| 同上 | 83 | 判断 F0618 结果；为真时按 `||` 规则短路并返回 `true` | B02 / RT0 | 本函数内具体指令 | 判断 / 返回 | 无 | 当前代码事实 | 合法未接域结果 | 保留短路顺序 |
| 同上 | 83 | F0618 为假时调用 F0336 `已接域()`，同一隐式 `this`，按值取得 `bool` | C03 | 函数调用 | 读取 / 调用 | 无 | 当前代码事实 | 不适用 | F0336 另图，不在本函数重复八项判定 |
| 同上 | 83 | 判断并原样返回 F0336 结果；真为完整接域形状，假为部分 / 混合形状 | B04 / RT1 / RF | 本函数内具体指令 | 判断 / 返回 | 无 | 当前代码事实 | `false` 供调用方写前拒绝 | 不把 `true` 扩大为同域、当前或回调正确 |
| 同上 | 84 | 闭合函数体；无额外运行指令或隐式资源释放 | RT0 / RT1 / RF 后置 | 本函数内具体指令 | 作用域闭合 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：82—84 行全部映射；项目被调函数 2 个、调用点 2 个、外部边界 0、未解析调用 0。七条当前根可达入边与 E1638 / E1639 出边双向闭合；当前不可达特征值服务和自检调用不建立项目边。
