# CF-L6-0027 `用途观察目标类型有效` 逐行代码映射表

代码版本：`a48a70b2d678dea64dd8228adb4f26f0badd9506`
覆盖文件：`海中鱼巣/核心/关系仓库.cpp:21-43`
根函数：F0348 `bool 海中鱼巣::{匿名命名空间@关系仓库.cpp}::用途观察目标类型有效(std::int64_t 角色, 海中鱼巣::节点类型 类型)`
配对流程图：`流程图/代码流程图/L6/CF-L6-0027_用途观察目标类型有效_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`、`规范/4210_子规范_动态信息分层获取与聚合_20260720.md`、`规范/4330_子规范_因果用途观察证据账与阶段推进.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/关系仓库.cpp` | 21 | 进入匿名命名空间函数并按值绑定 `角色`、`类型` | S | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 22 | 按 I64 `角色` 进入 `switch` 分派 | B01 | 本函数内具体指令 | 判断 / 分派 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 23 | 角色 1 比较目标类型是否为任务并返回结果 | C02 / RT / RF | 本函数内具体指令 | 纯计算 / 返回 | 无 | 当前代码事实 | 类型不匹配为写前逻辑内拒绝 | 无 |
| 同上 | 24 | 角色 2 比较目标类型是否为需求并返回结果 | C03 / RT / RF | 本函数内具体指令 | 纯计算 / 返回 | 无 | 当前代码事实 | 类型不匹配为写前逻辑内拒绝 | 无 |
| 同上 | 25 | 角色 3 比较目标类型是否为任务方法选择记录并返回结果 | C04 / RT / RF | 本函数内具体指令 | 纯计算 / 返回 | 无 | 当前代码事实 | 类型不匹配为写前逻辑内拒绝 | 无 |
| 同上 | 26-29 | 角色 4 / 5 / 12 / 14 共用方法类型比较并返回结果 | C05 / RT / RF | 本函数内具体指令 | 分派 / 纯计算 / 返回 | 无 | 当前代码事实 | 粗类型不匹配为拒绝；动作入口身份未验证 | 后继治理补齐动作入口身份 |
| 同上 | 30 | 角色 6 比较目标类型是否为场景并返回结果 | C06 / RT / RF | 本函数内具体指令 | 纯计算 / 返回 | 无 | 当前代码事实 | 类型不匹配为写前逻辑内拒绝 | 无 |
| 同上 | 31 | 角色 7 比较目标类型是否为存在并返回结果 | C07 / RT / RF | 本函数内具体指令 | 纯计算 / 返回 | 无 | 当前代码事实 | 类型不匹配为写前逻辑内拒绝 | 无 |
| 同上 | 32-37 | 角色 8 / 9 / 15 / 30 / 31 / 32 共用状态类型比较并返回结果 | C08 / RT / RF | 本函数内具体指令 | 分派 / 纯计算 / 返回 | 无 | 当前代码事实 | 类型不匹配为写前逻辑内拒绝 | 无 |
| 同上 | 38 | 角色 10 比较目标类型是否为动态并返回结果 | C09 / RT / RF | 本函数内具体指令 | 纯计算 / 返回 | 无 | 当前代码事实 | 类型不匹配为写前逻辑内拒绝 | 无 |
| 同上 | 39-40 | 角色 11 / 13 仅比较目标类型不等于未分类并返回结果 | C10 / RT / RF | 本函数内具体指令 | 分派 / 纯计算 / 返回 | 无 | 当前代码事实 | `未分类` 被拒绝；其它错误 / 未知类型被误收 | 收紧为动态合同允许的完整当前目标 |
| 同上 | 41 | 其它角色按 `default` 返回 `false` | RF0 | 本函数内具体指令 | 防御性判断 / 返回 | 无 | 当前代码事实 | 未知角色逻辑内拒绝 | 无 |
| 同上 | 42-43 | 闭合 `switch` 与函数作用域，无资源释放或结构变化 | Z | 本函数内具体指令 | 作用域闭合 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：21—43 行全部映射；项目被调函数、外部边界和未解析调用均为 0。唯一入边 E0677 与调用方 F0167 双向闭合。
