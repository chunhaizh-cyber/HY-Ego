# CF-L6-0213 `控制面板窗口::窗口实现::注册窗口类` 逐行代码映射表

代码版本：`bab41cae4223289e1297a5b7850f79b5b967fb06`
源码 blob：`fe9dad1eb3728c823beccf305c783be96a3df33d`
覆盖文件：`海中鱼巣/界面/控制面板窗口.cpp:422-452`
根函数：F0389 `bool 海中鱼巣::控制面板窗口::窗口实现::注册窗口类()`
配对流程图：`流程图/代码流程图/L6/CF-L6-0213_控制面板窗口注册窗口类_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`、`规范/8120_子规范_程序入口启动模式与运行宿主边界_20260726.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/界面/控制面板窗口.cpp` | 422 | 进入 F0389；无显式参数，独占可写借用 `窗口实现 this` | S | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 不把 bool 返回升级为机器事实 |
| 同上 | 423 | 开始聚合构造栈内 `INITCOMMONCONTROLSEX 公共控件初始化` | N01 | 本函数内具体指令 | 局部构造 | 只形成 Win32 调用描述 | 临时宿主材料 | 不适用 | 无 |
| 同上 | 424 | 把 `sizeof(INITCOMMONCONTROLSEX)` 写为描述结构尺寸 | N01 | 本函数内具体指令 | 纯计算 / 局部赋值 | 无业务结构变化 | 临时宿主材料 | 不适用 | 无 |
| 同上 | 425 | 以位或组合 `ICC_TREEVIEW_CLASSES` 与 `ICC_BAR_CLASSES`，写为要初始化的公共控件类 | N01 | 本函数内具体指令 | 纯计算 / 局部赋值 | 无业务结构变化 | 临时宿主材料 | 不适用 | 无 |
| 同上 | 426 | 完成公共控件初始化描述构造 | N01 | 本函数内具体指令 | 局部构造完成 | 无业务结构变化 | 临时宿主材料 | 不适用 | 无 |
| 同上 | 427 | 经 X01025 调用 `InitCommonControlsEx(&公共控件初始化)` 并对 BOOL 取反；0 进入失败分支，非零继续 | X02 / B03 | 外部边界 / 本函数内具体指令 | Win32 调用 / 判断 | 可能建立进程公共控件运行资源；不写业务事实 | 当前宿主调用结果 | 0 为窗口启动追根因错误 | 保持外部边界与项目诊断分账 |
| 同上 | 428 | 失败时经 X01026 从宽字符串构造阶段值，再按 RCE0181 调用 F0393 标记窗口追根因错误 | X04 / C05 | 外部边界 / 函数调用 | 字符串构造 / 人读诊断写入 | 写 `追根因错误`，且失败阶段为空时写首个阶段；不写业务结构 | 人读诊断 | 追根因解决 | 字符串构造异常直接传播 |
| 同上 | 429 | 公共控件初始化失败后按 bool 返回 false | RF | 本函数内具体指令 | 返回 | 保留 F0393 已写的人读错误状态 | 当前宿主调用结果 | 追根因解决 | F0223 停止后续窗口创建 |
| 同上 | 430 | 闭合公共控件初始化失败分支；仅未返回路径继续 | B03 | 本函数内具体指令 | 分支闭合 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 431 | 空行分隔两段局部宿主初始化，不影响运行路径 | B03 / N06 | 本函数内具体指令 | 排版 / 顺序边界 | 无 | 当前代码版式 | 不适用 | 无 |
| 同上 | 432 | 零初始化栈内 `WNDCLASSEXW 窗口类` | N06 | 本函数内具体指令 | 局部构造 | 只形成窗口类候选描述 | 临时宿主材料 | 不适用 | 无 |
| 同上 | 433 | 写窗口类结构尺寸 | N07 | 本函数内具体指令 | 局部赋值 | 无业务结构变化 | 临时宿主材料 | 不适用 | 无 |
| 同上 | 434 | 写水平和垂直重绘样式标志 | N08 | 本函数内具体指令 | 局部赋值 / 位或 | 无业务结构变化 | 临时宿主材料 | 不适用 | 无 |
| 同上 | 435 | 把唯一项目函数 F0396 `窗口过程` 地址写入 `lpfnWndProc`；形成 RCB0018 回调注册材料，但此处不调用 F0396 | N09 | 本函数内具体指令 | 函数地址绑定 / 回调登记准备 | 只改变局部候选描述 | 当前项目回调事实 | 不适用 | 类已存在分支不证明本候选过程被采用 |
| 同上 | 436 | 经 X01027 调用 `GetModuleHandleW(nullptr)`，把返回模块句柄写入 `hInstance` | X10 / N11 | 外部边界 / 本函数内具体指令 | Win32 调用 / 局部赋值 | 无业务结构变化 | 临时宿主材料 | 空返回未被本函数单独拒绝 | 由 X01031 结果间接收口 |
| 同上 | 437 | 经 X01028 调用 `LoadIconW(nullptr, IDI_APPLICATION)`，把返回大图标句柄写入 `hIcon` | X12 / N13 | 外部边界 / 本函数内具体指令 | Win32 调用 / 局部赋值 | 无业务结构变化 | 临时宿主材料 | 空返回未被本函数单独拒绝 | 由 X01031 结果间接收口 |
| 同上 | 438 | 经 X01029 调用 `LoadCursorW(nullptr, IDC_ARROW)`，把返回光标句柄写入 `hCursor` | X14 / N15 | 外部边界 / 本函数内具体指令 | Win32 调用 / 局部赋值 | 无业务结构变化 | 临时宿主材料 | 空返回未被本函数单独拒绝 | 由 X01031 结果间接收口 |
| 同上 | 439 | 计算 `COLOR_WINDOW + 1` 并 `reinterpret_cast` 为 `HBRUSH`，写入背景刷句柄字段 | N16 | 本函数内具体指令 | 纯计算 / 转换 / 局部赋值 | 无业务结构变化 | 临时宿主材料 | 不适用 | 无 |
| 同上 | 440 | 把静态窗口类名称指针写入 `lpszClassName` | N17 | 本函数内具体指令 | 局部赋值 | 无业务结构变化 | 临时宿主材料 | 不适用 | 类名只是宿主资源身份，不是业务身份 |
| 同上 | 441 | 经 X01030 再次调用 `LoadIconW(nullptr, IDI_APPLICATION)`，把返回句柄写入 `hIconSm` | X18 / N19 | 外部边界 / 本函数内具体指令 | Win32 调用 / 局部赋值 | 无业务结构变化 | 临时宿主材料 | 空返回未被本函数单独拒绝 | 由 X01031 结果间接收口 |
| 同上 | 442 | 空行分隔候选描述形成与注册调用，不影响运行路径 | N19 / X20 | 本函数内具体指令 | 排版 / 顺序边界 | 无 | 当前代码版式 | 不适用 | 无 |
| 同上 | 443 | 经 X01031 调用 `RegisterClassExW(&窗口类)`；把返回 ATOM 与 0 比较，0 进入错误码分支，非零进入本次注册成功分支 | X20 / B21 | 外部边界 / 本函数内具体指令 | Win32 注册 / 判断 | 非零时在进程中注册窗口类及候选窗口过程；0 时本次未注册 | 当前宿主资源结果 | 0 需按末错误继续分类 | 回调 RCB0018 仅在候选类实际采用时成立 |
| 同上 | 444 | 仅 X01031 返回 0 时，经 X01032 立即读取 Win32 末错误并判断是否不等于 `ERROR_CLASS_ALREADY_EXISTS` | X22 / B23 | 外部边界 / 本函数内具体指令 | Win32 错误读取 / 判断 | 无新增注册资源 | 当前宿主调用结果 | 类已存在为可继续；其它错误追根因 | 不用后续调用覆盖末错误 |
| 同上 | 445 | 末错误不是类已存在时，经 X01033 构造失败阶段字符串，并按 RCE0181 调用 F0393 标记追根因错误 | X24 / C25 | 外部边界 / 函数调用 | 字符串构造 / 人读诊断写入 | 写窗口追根因状态；不写业务结构 | 人读诊断 | 追根因解决 | 字符串构造异常直接传播 |
| 同上 | 446 | 窗口类注册其它错误路径按 bool 返回 false | RF | 本函数内具体指令 | 返回 | 保留 F0393 已写的人读错误状态 | 当前宿主调用结果 | 追根因解决 | F0223 停止后续窗口创建 |
| 同上 | 447 | 闭合“不是类已存在”失败分支；仅类已存在时继续外层分支闭合 | B23 / R26 | 本函数内具体指令 | 分支闭合 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 448 | X01031 返回 0 的外层分支结束；类已存在路径保持 `窗口类由本次注册` 原值，随后落到最终 true 返回；非零路径进入 else | B21 / R26 | 本函数内具体指令 | 分支闭合 / 条件选择 | 类已存在时无本次注册所有权变化 | 当前窗口宿主状态 | 类已存在为当前允许继续结果 | 不把原值默认为 false；重复调用时可能已为 true |
| 同上 | 449 | X01031 非零时把成员 `窗口类由本次注册` 写为 true | N27 | 本函数内具体指令 | 宿主状态写入 | 记录本对象承担后续反注册责任；不写业务事实 | 当前窗口宿主状态 | 不适用 | F0224 按该标志反注册 |
| 同上 | 450 | 闭合本次注册成功分支；非零路径继续最终返回 | N27 | 本函数内具体指令 | 分支闭合 | 无新增变化 | 当前控制流 | 不适用 | 无 |
| 同上 | 451 | 公共控件初始化成功且窗口类新注册或已存在时，按 bool 返回 true | R26 / R28 | 本函数内具体指令 | 返回 | 保留 Win32 注册结果和所有权标志当前值 | 当前宿主调用结果 | 类已存在为当前允许继续结果 | true 只允许 F0223 继续创建主窗口 |
| 同上 | 452 | 词法闭合函数定义；局部 Win32 描述结构结束生命周期 | RF / R26 / R28 | 本函数内具体指令 | 作用域 / 生命周期 | 不改变已注册 Win32 类或窗口成员状态 | 当前代码事实 | 不适用 | 无 |

覆盖结论：422—452 行全部映射；入边 E0856、RCE0181 两个调用点、外部边界 X01025—X01033、回调注册 RCB0018、公共控件失败、窗口类首次注册、类已存在和其它注册错误路径均闭合。没有新增或更新 HTML；项目动态未解析调用为 0。
