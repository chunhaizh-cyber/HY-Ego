# CF-L6-0035 `概念图服务::登记概念根` 逐行代码映射表

代码版本：`a48a70b2d678dea64dd8228adb4f26f0badd9506`
覆盖文件：`海中鱼巣/领域/概念图服务.h:849-890`
根函数：F0356 `std::optional<海中鱼巣::节点句柄> 海中鱼巣::概念图服务::登记概念根(const 海中鱼巣::概念根登记材料& 材料)`
配对流程图：`流程图/代码流程图/L6/CF-L6-0035_概念图服务登记概念根_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`、`规范/4030_子规范_基础信息服务分层与领域写授权.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`、`规范/7140_子规范_概念身份生命周期退役删除与活动投影分账.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/概念图服务.h` | 849 | 进入成员函数，绑定调用方拥有的 const `材料` 借用和概念图服务 `this` 借用 | S | 本函数内具体指令 | 入口 / 借用绑定 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 850 | 经 E1861 调用 F0190，以根节点完整句柄读取值式节点记录并绑定局部 optional | C01 | 函数调用 | 节点读取 / 局部构造 | 无 | 当前节点读取结果 | 异常向调用方传播 | 无 |
| 同上 | 851 | 先观察根记录是否有值；有值才读取节点类型并经 E1862 调用 R0521，空值分支直接形成 `nullopt` 节点类别 | X02 / B03 / X04 / C05 / N06 | 外部边界 / 本函数内具体指令 / 函数调用 | 条件表达式 / 短路 / 类型映射 / 局部构造 | 无 | 当前代码事实 | 节点不可读进入写前拒绝候选 | 不把节点类型映射当作完整根身份 |
| 同上 | 852—855 | 经 E1863 调用 F0639 后，按 `||` 从左到右短路检查类别有效、稳定键非零、节点类别存在且等于材料类别 | C07 / B08 | 函数调用 / 本函数内具体指令 | 纯计算 / 判断 / 短路 | 无 | 当前代码事实 | 任一不满足为写前入口拒绝 | 返回应具名区分拒绝原因 |
| 同上 | 856—857 | 任一准入条件不满足时形成并返回空 optional，函数闭合 | RN0 / Z | 本函数内具体指令 | 返回 / 作用域闭合 | 无 | 当前代码事实 | 入口拒绝 | 无 |
| 同上 | 859 | 构造 `lock_guard` 并取得 `图写锁_`；锁所有权持续至函数退出 | X09 | 函数调用-外部边界 X0379 | 加锁 / 资源取得 | 仅同步资源变化 | 非权威同步材料 | 锁异常向调用方传播 | 保持锁序可追溯 |
| 同上 | 860 | 在图写锁内经 E1864 调用 R0088 读取以根节点为目标的有效概念上下位来源组，观察临时 vector 是否为空并在完整表达式末析构 | C10 / X11 / B12 | 函数调用 / 外部边界 / 本函数内具体指令 | 关系读取 / 临时值 / 判断 | 无 | 当前关系读取投影 | 非空为写前拒绝 | 不把空组扩大为唯一根证明 |
| 同上 | 861—862 | 来源组非空时形成空 optional；先形成返回对象，再释放当前唯一持有的图写锁并返回 | RN1 / DG / Z | 本函数内具体指令 / 外部边界 | 返回 / 解锁 | 无 | 当前代码事实 | 入口拒绝 | 无 |
| 同上 | 864 | 在仍持有图写锁时构造 `unique_lock` 并取得 `根登记锁_` 独占所有权 | X13 | 函数调用-外部边界 X0379 | 嵌套加锁 / 资源取得 | 仅同步资源变化 | 非权威同步材料 | 取得失败时释放图写锁后传播 | 冻结锁顺序为图写锁后根登记锁 |
| 同上 | 865 | 建立范围迭代，按四槽数组物理顺序借用下一 optional 登记槽 | L14 | 本函数内具体指令 | 循环控制 / 借用 | 无 | 非权威根登记投影 | 不适用 | 数组顺序不承担业务顺序 |
| 同上 | 866—868 | 观察登记槽是否有值；空槽执行 `continue` 并推进下一槽 | X15 / B16 / L17 | 外部边界 / 本函数内具体指令 | 判断 / 循环控制 | 无 | 非权威根登记投影 | 不适用 | 无 |
| 同上 | 869 | 对有值槽比较其根类别与本次材料根类别 | B18 | 本函数内具体指令 | 判断 | 无 | 非权威根登记投影 | 不适用 | 无 |
| 同上 | 870 | 同类别时先比较稳定非名称键；仅稳定键相同才经 E1903 调用 F0051 比较完整根节点 | B19 / C19A / B19B | 本函数内具体指令 / 函数调用 | 判断 / 短路 / 强类型句柄比较 | 无 | 当前代码事实 | 同义可幂等；异义拒绝 | 同义仍须由权威根结构另行复核 |
| 同上 | 871—872 | 同类别且两项身份材料均相同时复制根节点形成返回对象，逆序释放两把锁后返回 | RI / D28 / Z | 本函数内具体指令 / 外部边界 | 幂等读回 / 解锁 / 返回 | 无新增结构 | 非权威登记投影幂等读回 | 逻辑内幂等结果 | 不把幂等命中当权威身份重建 |
| 同上 | 873—874 | 同类别但稳定键或根节点任一不同时返回空 optional，并释放两把锁 | RN2 / D28 / Z | 本函数内具体指令 / 外部边界 | 唯一性拒绝 / 解锁 / 返回 | 无 | 当前代码事实 | 入口拒绝 | 具名区分同类别异义 |
| 同上 | 875 | 不同类别时先判断是否复用稳定非名称键；仅稳定键不同时经 E1903 调用 F0051 比较完整根节点 | B20 / C20A / B20B | 本函数内具体指令 / 函数调用 | 判断 / 短路 / 强类型句柄比较 | 无 | 当前代码事实 | 身份复用为写前拒绝 | 无 |
| 同上 | 876—877 | 跨类别复用任一身份维度时返回空 optional，并释放两把锁 | RN3 / D28 / Z | 本函数内具体指令 / 外部边界 | 唯一性拒绝 / 解锁 / 返回 | 无 | 当前代码事实 | 入口拒绝 | 具名区分跨类别冲突 |
| 同上 | 878 | 当前有值槽无冲突时闭合分支并推进循环；循环耗尽后进入发布 | L21 / L14 | 本函数内具体指令 | 循环控制 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 880 | 经 E1865 调用 F0640 计算写入索引，把平凡登记材料复制到对应 optional 槽 | C22 / X23 | 函数调用 / 外部边界 X0379 | 纯计算 / 投影写入 | 改变一个根登记定位投影槽；不写权威根结构 | 非权威可重建投影 | 不适用 | 不把登记数组提升为根身份权威 |
| 同上 | 881 | 再经 E1866 调用 F0640 计算读回索引，并绑定对应已发布 optional 槽的 const 引用 | C24 / X25 | 函数调用 / 外部边界 X0379 | 纯计算 / 投影读回 / 引用绑定 | 无新增结构 | 非权威可重建投影 | 不适用 | 无 |
| 同上 | 882—886 | 从左到右短路求值槽有值、类别和稳定键；前三项成立时经 E1903 调用 F0051 比较根节点；无论是否执行 F0051，完整布尔实参形成后都经 E1867 调用 F0184 | X25 / B25A / C25B / C26 / B27 | 外部边界 / 函数调用 / 本函数内具体指令 | 读回 / 短路 / 强类型句柄比较 / 诊断检查 | 无新增结构 | 当前代码事实；诊断不裁决机器事实 | 失败为发布后内部逻辑错误 | 必须保留已写结构证据并追根因 |
| 同上 | 887—888 | F0184 返回假时形成空 optional，逆序释放根登记锁和图写锁后返回；不撤销 880 行写入 | RN4 / D28 / Z | 本函数内具体指令 / 外部边界 | 内部错误返回 / 解锁 | 已写登记投影保留 | 当前代码事实 | 追根因解决 | 不得包装为零变化普通失败 |
| 同上 | 889 | 读回检查通过时复制已发布根节点形成返回对象，随后释放两把锁并按值返回 | RS / D28 | 本函数内具体指令 / 外部边界 | 返回物化 / 解锁 / 返回 | 调用方取得根句柄副本；登记投影保持 | 非权威登记投影结果 | 不适用 | 返回有值不证明权威根成立 |
| 同上 | 890 | 闭合函数作用域并析构局部节点类别、根记录及锁对象；异常路径按已取得资源栈展开 | Z / DX2 / E | 本函数内具体指令 / 外部边界 | 资源释放 / 异常传播 | 释放局部资源；无新增机器结构 | 当前代码事实 | 异常发生在本次登记写入前 | 无 |

覆盖结论：849—890 行全部映射；直接项目被调函数 7 个唯一身份、10 个调用点，对应 E1861—E1867、E1903；F0051 的三次调用分别受 870、875、885 的左侧短路条件门控。X0379 覆盖 optional、两类锁、来源组临时 vector、数组遍历 / 下标、复制与返回生命周期，未解析项目调用 0。四条已登记入边 E0717 / E0723 / E0729 / E0735 已反向列明；`自检.概念命名需求.ixx:92` 只保留为调用方源码候选待展开，不分配 E、不计入聚合边。
