# CF-L6-0004 `索引仓库.cpp::{匿名命名空间}::接线一致` 逐行代码映射表

代码版本：`a48a70b2d678dea64dd8228adb4f26f0badd9506`
覆盖文件：`海中鱼巣/核心/索引仓库.cpp:12-16`
根函数：F0324 `bool 海中鱼巣::{匿名命名空间@索引仓库.cpp}::接线一致(const 海中鱼巣::结构事务接线& 左, const 海中鱼巣::结构事务接线& 右)`
配对流程图：`流程图/代码流程图/L6/CF-L6-0004_索引仓库接线一致_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`、`规范/4040_子规范_不透明结构事务候选确认撤销与最后发布.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`、`规范/详细设计/不透明结构写入会话许可内探测计量与全参与者原子收口详细设计.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/索引仓库.cpp` | 12 | 声明并进入匿名命名空间自由函数，绑定两个调用方拥有的 const 引用 | S | 本函数内具体指令 | 入口 / 借用 | 无 | 当前代码事实 | 不适用 | 保持唯一内部链接函数身份 |
| 同上 | 13 | 调用 F0336 `左.已接域()`；结果为假时才调用 F0336 `右.已接域()` | C01 / B02 / C03 | 函数调用 / 本函数内具体指令 | 调用 / 短路判断 | 无 | 当前代码事实 | 不适用 | 两个静态调用点分别登记，不合并 |
| 同上 | 13 | 若左右首次判断均为未接域，则立即按值返回 `true` | B04 / RT0 | 本函数内具体指令 | 判断 / 返回 | 无 | 当前代码事实 | 合法双方未接域 | 当前唯一调用方必须先保证两项形态有效 |
| 同上 | 14 | 非双方未接路径再次调用 F0336 `左.已接域()`；为真时才调用 F0336 `右.已接域()`；之后比较域编号 | C05 / B06 / C07 / B08 / B09 | 函数调用 / 本函数内具体指令 | 调用 / 读取 / 短路判断 | 无 | 当前代码事实 | 一接一未接或域编号不等返回 `false` | 四个 F0336 调用点不得按同函数身份去掉调用点差异 |
| 同上 | 15 | 仅前项均成立时比较运行期纪元，再调用 X0357 比较两个 `shared_ptr<void>` 的存储指针 | B10 / X11 / B12 | 函数调用 / 本函数内具体指令 | 读取 / 标准库调用 / 判断 | 无 | 当前代码事实 | 不相等返回 `false` | 显式记录比较 `get()` 而非控制块 |
| 同上 | 14—15 | 长短路链任一项为假时按值返回 `false`；全部为真时按值返回 `true` | RF / RT1 | 本函数内具体指令 | 返回 | 无 | 当前代码事实 | 写前拒绝或正式装配追根因 | 调用语境决定二分，不把裸 bool 单独事实化 |
| 同上 | 16 | 闭合函数体；不析构项目资源、不写结构 | RT0 / RT1 / RF 后置 | 本函数内具体指令 | 作用域闭合 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：12—16 行全部映射；项目被调函数唯一身份 1 个、调用点 4 个，外部边界 X0357 一个，未解析调用 0。E0622 入边、E1648—E1651 出边与配对图双向闭合。
