# CF-L6-0033 `概念图服务::读取生命周期状态` 逐行代码映射表

代码版本：`a48a70b2d678dea64dd8228adb4f26f0badd9506`
覆盖文件：`海中鱼巣/领域/概念图服务.h:969-979`
根函数：F0354 `std::optional<海中鱼巣::节点句柄> 海中鱼巣::概念图服务::读取生命周期状态(海中鱼巣::概念生命周期阶段 阶段) const`
配对流程图：`流程图/代码流程图/L6/CF-L6-0033_概念图服务读取生命周期状态_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`、`规范/7140_子规范_概念身份生命周期退役删除与活动投影分账.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/概念图服务.h` | 969 | 进入 const 成员函数，按值绑定生命周期阶段并非拥有借用 `this` | S | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 970 | 调用 F0641 `生命周期阶段有效(阶段)`，判断阶段是否位于活跃至退役 | C01 / B02 | 函数调用 / 指令 | 纯值判断 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 971-972 | 阶段无效时在取得任何锁和计算下标前物化并返回空 optional | N03 / R04 | 本函数内具体指令 | 入口拒绝 / 返回 | 无 | 当前代码事实 | 写前入口拒绝，但原因折叠为空 | 使用具名强类型拒绝 |
| 同上 | 973 | 构造 `std::shared_lock` 并取得 `生命周期状态锁_` 共享所有权 | X05 | 函数调用-标准库边界 | 共享锁获取 | 仅同步资源变化 | 非权威同步材料 | 锁异常为内部异常 | 形成结构化异常收口 |
| 同上 | 974 | 调用 F0642 `生命周期阶段索引(阶段)`；以返回索引调用数组 `operator[]`，绑定内部 optional 的 const 引用 | C06 / X07 | 函数调用 / 标准库边界 | 纯计算 / 投影读取 / 引用绑定 | 无 | 非权威生命周期状态登记投影 | 不适用 | 无 |
| 同上 | 975 | 先调用 `has_value`；为空时按 `||` 短路；有值时调用 `value` 并把句柄传给 F0377 复核节点当前有效性 | X08 / X09 / C10 / B11 | 标准库边界 / 函数调用 / 指令 | 短路判断 / 节点读回 | 无 | 非权威登记投影与节点仓库当前读回 | 未登记可为初始化逻辑内结果；失效条目为内部漂移 | 强类型区分缺失与漂移 |
| 同上 | 976-977 | 登记为空或节点失效时物化空 optional；返回值物化后共享锁按 RAII 释放并返回 | N12 / X13 / R14 | 指令 / 标准库边界 | 返回物化 / 解锁 / 返回 | 无 | 当前代码事实 | 空值折叠合法未登记与内部漂移 | 返回具名类别与原因 |
| 同上 | 978 | 登记有值且节点有效时复制 optional 形成返回值，随后释放共享锁并按值返回 | N15 / X16 / R17 | 指令 / 标准库边界 | 值式复制 / 解锁 / 返回 | 无 | 非权威定位投影 | 不适用 | 调用方不得把命中解释为权威生命周期成立 |
| 同上 | 979 | 闭合函数作用域；正常或异常路径均不保留数组引用和共享锁 | X13 / X16 / XE | 函数调用-标准库边界 | 作用域闭合 / RAII | 无 | 非权威同步材料 | 异常继续传播 | 无 |

覆盖结论：969—979 行全部映射；直接项目被调函数为 F0641、F0642 和 F0377，共 3 个唯一函数、3 个调用点；X0377 覆盖共享锁、数组、optional 与返回生命周期，未解析项目调用 0。当前已登记入边 E0710、E0738，出边 E1857—E1859 与 F0176 / F0181 调用语境闭合。
