# CF-L6-0011 `主信息仓库::创建主信息` 逐行代码映射表

代码版本：`a48a70b2d678dea64dd8228adb4f26f0badd9506`
覆盖文件：`海中鱼巣/核心/主信息仓库.cpp:38-51`
根函数：F0331 `海中鱼巣::主信息句柄 海中鱼巣::主信息仓库::创建主信息()`
配对流程图：`流程图/代码流程图/L6/CF-L6-0011_主信息仓库创建主信息_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`、`规范/4020_子规范_领域类型化数据记录与组合读取投影边界.md`、`规范/4040_子规范_不透明结构事务候选确认撤销与最后发布.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/主信息仓库.cpp` | 38 | 进入旧主信息仓库可写成员函数，绑定隐式 `this`，无显式参数 | S | 本函数内具体指令 | 入口 / 借用 | 无 | 当前实现事实 | 不适用 | 不把旧结构创建扩大为节点直接身份 |
| 同上 | 39 | 调用 F0336 判断事务接线是否完整接域并选择路径 | C01 / B02 | 函数调用 / 本函数内具体指令 | 判断 / 分支 | 无 | 当前接线事实读取 | 不适用 | 保持接域与未接域分账 |
| 同上 | 40 | 接域时经 U0008 运行期函数指针取得共享许可并形成局部许可对象 | U03 | 函数调用 | 许可取得 / 局部构造 | 无 | 事务协调材料 | 函数指针异常向上传播 | 不伪连静态唯一目标 |
| 同上 | 41 | 调用 F0338 判断许可有效；有效时调用 F0339 取得令牌并调用 F0622，失效时形成零句柄 | C04 / B05 / C06 / C07 / R09 | 函数调用 / 本函数内具体指令 | 判断 / 委托写入 / 返回对象形成 | F0622 可能写旧主信息仓库 | 旧结构写入 | 许可无效为旧兼容返回；有效许可下零句柄为内部错误 | 后继使用结构化写结果和具名失败 |
| 同上 | 41—42 | 返回表达式完成或异常展开后调用 F0345 析构许可；正常路径再返回已形成结果，异常路径传播原异常 | C10N / C10E / R08 / XE | 函数调用 / 本函数内具体指令 | 资源释放 / 返回 / 异常展开 | 释放共享许可 | 事务资源事实 | 不适用 | 保留返回对象先形成、许可后释放和异常传播顺序 |
| 同上 | 43 | 未接域时调用 X0363 构造 `unique_lock` 并独占本仓锁；构造失败零写入 | X11 | 函数调用 | 锁 / 资源取得 | 无 | 物理同步 | 异常传播 | 不把本仓锁解释为跨仓事务 |
| 同上 | 44 | 读取下一主信息编号旧值并执行无上限检查的后增 | N12 | 本函数内具体指令 | 编号分配 / 写入 | 推进旧主信息编号高水位 | 旧仓库身份事实 | 回绕未拒绝 | 退役旧编号；正式身份使用稳定主键 |
| 同上 | 45 | 默认构造局部主信息记录，初始为零编号、零版本、无效状态和空值容器 | N13 | 本函数内具体指令 | 局部构造 | 无可读仓库变化 | 临时局部值 | 不适用 | 不把临时默认值发布 |
| 同上 | 46 | 把分配编号写入局部记录 | N14 | 本函数内具体指令 | 局部写入 | 无可读仓库变化 | 临时局部值 | 不适用 | 无 |
| 同上 | 47 | 把局部记录版本写为 1 | N15 | 本函数内具体指令 | 局部写入 | 无可读仓库变化 | 临时局部值 | 不适用 | 无 |
| 同上 | 48 | 把局部记录状态写为有效 | N16 | 本函数内具体指令 | 局部写入 | 无可读仓库变化 | 临时局部值 | 不适用 | 发布前应仍保持不可读候选 |
| 同上 | 49 | 调用 X0363 `主信息表_.emplace(编号,记录)`复制插入旧记录并忽略是否实际插入 | X17 | 函数调用 | 容器写入 / 发布 | 旧主信息记录可能立即可读 | 已退出旧结构事实 | 重复键或异常为内部错误 | 采用候选确认、结果复核和最后发布 |
| 同上 | 50 | 由仓库编号、编号和局部版本形成句柄返回对象 | R18 | 本函数内具体指令 | 结果构造 | 无新增写入 | 旧句柄材料 | 不适用 | 返回前读回身份、版本和状态 |
| 同上 | 50—51 | 正常返回或异常展开时调用 X0363 析构局部记录和锁；正常路径释放锁后按值返回句柄，异常路径传播原异常 | X19N / X19E / R20 / XE / Z | 函数调用 / 本函数内具体指令 | 生命周期 / 解锁 / 返回 / 异常展开 | 无新增写入 | 当前实现事实 | 不适用 | 不把返回句柄解释为跨结构完成 |

覆盖结论：38—51 行全部映射；项目被调函数 5 个、调用点 5 个，外部边界 X0363 一个（聚合 5 个锁 / 容器 / 值生命周期操作），未解析调用 U0008 一个。E0634 / E1303 / E1686 入边与 E1672—E1676 出边双向闭合。
