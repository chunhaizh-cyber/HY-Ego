# CF-L6-0206 `ODBC连接::关闭` 逐行代码映射表

代码版本：`9b4aa4a151e32d09d30cd9f28c9f85a45fd2692b`
覆盖文件：`海中鱼巣/适配/SQL数据库适配.cpp:201-214`
根函数：F0382 `void 海中鱼巣::{匿名命名空间@SQL数据库适配.cpp}::ODBC连接::关闭()`
配对流程图：`流程图/代码流程图/L6/CF-L6-0206_ODBC连接关闭_现状流程图.md`
已登记调用方：E0827 / F0205（`SQL数据库适配.cpp:133`）；E0837 / F0215（126 行）
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`、`规范/详细设计/SQLServer结构审计投影与控制面板查看详细设计.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/适配/SQL数据库适配.cpp` | 201 | 进入私有成员函数 F0382；独占可写借用当前 `ODBC连接` 对象，无显式参数 | S | 本函数内具体指令 | 入口 / 所有权绑定 | 无 | 当前代码事实 | 不适用 | 调用方不得并发使用同一对象 |
| 同上 | 202 | 判断 `连接_ != SQL_NULL_HDBC`；false 时跳过整个 DBC 分支且不修改 `已连接_` | B01 | 本函数内具体指令 | 句柄读取 / 判断 | 无 | 当前适配对象状态 | 空句柄为正常跳过；空句柄配 true 标志为内部不一致 | 需要自愈时应显式归一化连接标志 |
| 同上 | 203 | 仅 DBC 非空时判断 `已连接_`；true 才尝试断连，false 直接进入 DBC 释放 | B02 | 本函数内具体指令 | 状态读取 / 判断 | 无 | 当前适配对象状态 | 未连接 DBC 允许直接释放 | 无 |
| 同上 | 204 | 经 X01006 调用 `SQLDisconnect(连接_)`；返回 `SQLRETURN` 被丢弃，失败不改变后继 | X03 | 外部边界 | 外部断连 / 返回丢弃 | 可能结束外部数据库会话 | ODBC 外部结果 | 失败被当前 void 接口静默丢弃 | 若需可审计关闭，保留返回码和诊断 |
| 同上 | 205 | 闭合 `已连接_` 条件；无论是否调用 X01006，DBC 非空路径继续释放句柄 | B02 / X04 | 本函数内具体指令 | 分支闭合 / 顺序约束 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 206 | 经 X01007 调用 `SQLFreeHandle(SQL_HANDLE_DBC, 连接_)`；返回码被丢弃 | X04 | 外部边界 | 外部资源释放 / 返回丢弃 | 尝试释放外部 DBC 句柄 | ODBC 外部结果 | 失败仍继续清本地句柄 | 保留失败句柄或形成结构化释放结果 |
| 同上 | 207 | 无论 X01007 结果如何，把本地 `连接_` 赋值为 `SQL_NULL_HDBC` | N05 | 本函数内具体指令 | 本地状态写入 | 清除适配对象保存的 DBC 值 | 当前本地资源状态 | 置空不证明外部释放成功 | 避免丢失释放失败句柄的审计线索 |
| 同上 | 208 | 在 DBC 非空分支把 `已连接_` 赋值为 `false` | N06 | 本函数内具体指令 | 本地状态写入 | 清除本地连接标志 | 当前本地资源状态 | 不适用 | DBC 为空分支也应明确成员一致性合同 |
| 同上 | 209 | 闭合 DBC 非空分支；DBC 原为空时没有执行 N05 / N06 | B01 / B07 | 本函数内具体指令 | 分支闭合 | 无 | 当前控制流 | 空 DBC 配 true 标志仍保留 | 无 |
| 同上 | 210 | 独立判断 `环境_ != SQL_NULL_HENV`；false 时跳过 ENV 释放 | B07 | 本函数内具体指令 | 句柄读取 / 判断 | 无 | 当前适配对象状态 | 空环境句柄为正常跳过 | 无 |
| 同上 | 211 | 经 X01008 调用 `SQLFreeHandle(SQL_HANDLE_ENV, 环境_)`；返回码被丢弃 | X08 | 外部边界 | 外部资源释放 / 返回丢弃 | 尝试释放外部 ENV 句柄 | ODBC 外部结果 | 失败仍继续清本地句柄 | 保留失败结果和诊断 |
| 同上 | 212 | 无论 X01008 结果如何，把本地 `环境_` 赋值为 `SQL_NULL_HENV` | N09 | 本函数内具体指令 | 本地状态写入 | 清除适配对象保存的 ENV 值 | 当前本地资源状态 | 置空不证明外部释放成功 | 避免丢失释放失败句柄的审计线索 |
| 同上 | 213 | 闭合 ENV 非空分支；空环境句柄不调用 ODBC | B07 / R10 | 本函数内具体指令 | 分支闭合 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 214 | 闭合函数作用域并按 `void` 返回；不提供断连 / 释放结果或诊断 | R10 | 本函数内具体指令 | 返回 / 作用域 | 本地成员保留上述分支形成的最终状态 | 当前代码事实 | 失败返回码已不可见 | 需要调用方裁决时改为结构化关闭结果 |

覆盖结论：201—214 行全部映射；正式项目入边 E0827、E0837 已回指；项目出边 0；X01006—X01008 三个条件外部边界及其返回码丢弃、成员置空顺序全部闭合；0 未解析项目调用。
