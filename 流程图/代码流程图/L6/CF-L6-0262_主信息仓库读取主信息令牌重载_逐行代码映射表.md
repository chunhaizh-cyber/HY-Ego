# CF-L6-0262 `主信息仓库::读取主信息(主信息句柄,const 结构事务令牌&)` 逐行代码映射表

代码版本：`747223647b2cc5796db66a048009d78fa4d96ed7`
源码 blob：`11f8fa53ff961a8cbaac34cf75b69a22533ca907`
覆盖文件：`海中鱼巣/核心/主信息仓库.cpp:212-218`
根函数：F0439 `std::optional<海中鱼巣::主信息记录> 海中鱼巣::主信息仓库::读取主信息(海中鱼巣::主信息句柄 主信息, const 海中鱼巣::结构事务令牌& 令牌) const`
配对流程图：`流程图/代码流程图/L6/CF-L6-0262_主信息仓库读取主信息令牌重载_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`、`规范/4040_子规范_不透明结构事务候选确认撤销与最后发布.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/主信息仓库.cpp` | 212 | 进入 const 成员函数 F0439；借用仓库 `this` 与 const 令牌引用，按值接收主信息句柄 | S | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 不保存令牌或句柄引用 |
| 同上 | 213 | 经 RCE0207 调用 R0002 验证共享令牌；false 时立即物化并返回 `std::nullopt` | C01 / B02 / RN1 | 函数调用 / 本函数内具体指令 | 令牌验证 / 条件返回 | 无 | 当前令牌与接线 | 令牌拒绝为逻辑内返回 | 不把空结果伪成记录不存在 |
| 同上 | 214 | 仅令牌有效时经 X01085 以 `仓库锁_` 构造局部共享锁；构造异常直接传播 | X03 / E18 | 外部边界 / 本函数内具体指令 | 共享锁取得 / 异常传播 | 建立只读窗口，不写仓库 | 当前仓库锁 | 未收口异常 | 不把读锁解释为写许可 |
| 同上 | 215 | 经 X02311 对 const `主信息表_` 调用 `find(主信息.主信息编号)`，并把返回 const_iterator 绑定到局部 `位置` | X04 / N05 | 外部边界 / 本函数内具体指令 | 关联容器查找 / 临时绑定 | 无 | 共享锁内当前主信息表 | 不适用 | 保持完整主信息编号查找 |
| 同上 | 216 | 经 X02312 取得 `end()`，经 X01086 比较位置；按源码短路依次复核未命中、错仓、错版本和记录非有效。仅位置命中后经 X02313 解引用读取版本与状态；任一失败物化空 optional，结束共享锁生命周期并返回 | X06 / X07 / B08 / B09 / X10 / B11 / X12 / B13 / RN2 / UN / RNULL | 外部边界 / 本函数内具体指令 | 尾后位置 / 迭代器比较 / 句柄与状态复核 / 条件返回 / RAII | 只读命中记录 | 当前共享锁内记录与句柄 | 多种普通拒绝合并为空 optional | 不解引用 end，不省略仓库 / 版本 / 状态复核 |
| 同上 | 217 | 全部复核通过后经 X02313 取得命中键值对指针，再经 X01087 复制 `second` 记录并构造 optional 快照；成功时释放共享锁并按值返回，复制异常时先释放锁再传播 | X14 / X15 / U16 / RV / U17 / E18 | 外部边界 / 本函数内具体指令 | 记录读取 / 值式复制 / RAII / 返回 / 异常传播 | 只形成调用方拥有的快照 | 当前有效记录 | 正常值式返回或未收口异常 | 不暴露仓库内部记录引用 |
| 同上 | 218 | 词法闭合函数定义；正常空 / 值返回或异常展开均已结束局部共享锁生命周期 | UN / U16 / U17 / RN1 / RNULL / RV / E18 | 本函数内具体指令 | 作用域 / 生命周期 | 无 | 当前代码事实 | 沿既定终点结束 | 不补造写入或发布行为 |

覆盖结论：212—218 行全部映射；项目入边 E0923、RCE0170、RCE0171、RCE0317，项目出边 RCE0207，外部边界 X01085、X01086、X01087、X02311、X02312、X02313 全部闭合；逐行短路、值式复制、空返回和资源释放路径均已覆盖。
