# CF-L6-0007 `方法服务::读取方法角色状态值` 逐行代码映射表

代码版本：`a48a70b2d678dea64dd8228adb4f26f0badd9506`
覆盖文件：`海中鱼巣/领域/方法服务.h:1747-1761`
根函数：F0327 `海中鱼巣::方法角色状态 海中鱼巣::方法服务::读取方法角色状态值(海中鱼巣::节点句柄 方法节点, const 海中鱼巣::状态服务& 状态) const`
配对流程图：`流程图/代码流程图/L6/CF-L6-0007_读取方法角色状态值_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/1160_根规范_状态节点_20260720.md`、`规范/3300_根规范_方法_20260720.md`、`规范/5300_子规范_方法登记选择与执行规则_20260720.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/方法服务.h` | 1747 | 进入 const 方法服务成员函数，按值接收方法句柄并借用 const 状态服务 | S | 本函数内具体指令 | 入口 / 借用 | 无 | 当前实现事实 | 不适用 | 保持服务所有权边界 |
| 同上 | 1748 | 调用 F0333 检查方法节点类型；为假时进入拒绝分支 | C01 / B02 | 函数调用 / 本函数内具体指令 | 读取 / 判断 | 无 | 当前实现事实 | 普通查询逻辑内返回；成功初始化语境为内部错误 | 不把节点类型扩大为角色证明 |
| 同上 | 1749—1750 | 返回 `方法角色状态::未定义` 并闭合类型拒绝分支 | RU0 | 本函数内具体指令 | 返回 / 分支闭合 | 无 | 非权威查询结果 | 同上 | 保留具名调用语境分账 |
| 同上 | 1751 | 经 X0359 构造顺序号 optional，调用 F0330 读取唯一模板状态目标 | X03 / C04 | 函数调用 | 值构造 / 关系读取 | 无 | 当前实现读取 | 不适用 | 旧模板角色槽与新结构分账 |
| 同上 | 1752 | 调用 X0359 `has_value()` 判断角色状态目标是否存在 | X05 / B06 | 函数调用 / 本函数内具体指令 | 判断 | 无 | 当前实现读取 | 同上 | 唯一目标缺失或冲突不静默成功 |
| 同上 | 1753—1754 | 目标为空时返回 `未定义` 并闭合拒绝分支 | RU1 | 本函数内具体指令 | 返回 / 分支闭合 | 无 | 非权威查询结果 | 同上 | 无 |
| 同上 | 1755 | 调用 X0359 `value()` 取得目标句柄，再调用 F0329 读取旧 I64 状态值 | X07 / C08 | 函数调用 | 借用 / 状态读取 | 无 | 当前实现读取 | 不适用 | 类型化状态迁移另行治理 |
| 同上 | 1756 | 调用 X0359 `has_value()` 判断状态值是否存在 | X09 / B10 | 函数调用 / 本函数内具体指令 | 判断 | 无 | 当前实现读取 | 同上 | 不伪造默认角色 |
| 同上 | 1757—1758 | 状态值为空时返回 `未定义` 并闭合拒绝分支 | RU2 | 本函数内具体指令 | 返回 / 分支闭合 | 无 | 非权威查询结果 | 同上 | 无 |
| 同上 | 1759 | 调用 X0359 `value()` 取得 I64，并按相同 I64 底层类型转换为方法角色枚举 | X11 | 函数调用 | 读取 / 类型转换 | 无 | 当前实现读取 | 不适用 | 非白名单值留给 F0334 |
| 同上 | 1760 | 调用 F0334 验证角色白名单；有效返回具名角色，否则返回 `未定义` | C12 / B13 / RT / RU3 | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 | 无 | 非权威查询结果 | 普通查询逻辑内返回；成功初始化语境为内部错误 | 不扩大为生命周期或可执行性 |
| 同上 | 1761 | 闭合函数体并析构两个 optional 局部值 | Z | 本函数内具体指令 | 作用域闭合 / 生命周期 | 无 | 当前实现事实 | 不适用 | 无 |

覆盖结论：1747—1761 行全部映射；项目被调函数 4 个、调用点 4 个，外部边界 X0359 一个（聚合 6 个 optional / 枚举操作），未解析调用 0。E0626 入边与 E1658—E1661 出边双向闭合。
