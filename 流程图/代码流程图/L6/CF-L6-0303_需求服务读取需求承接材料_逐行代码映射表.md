# CF-L6-0303 需求服务读取需求承接材料逐行代码映射表

代码版本：`14d539ed7ad8af87f35fb0752eed420bbe443316`
源码 blob：`e41b0c665b360232e55ede728f2a4d561c5c9d32`
覆盖文件：`海中鱼巣/领域/需求服务.h:560-575`
根函数：F0552 `std::optional<海中鱼巣::需求承接材料> 海中鱼巣::需求服务::读取需求承接材料(海中鱼巣::节点句柄 需求节点) const`
配对流程图：`流程图/代码流程图/L6/CF-L6-0303_需求服务读取需求承接材料_现状流程图.md`
依据：当前源码、E1247、RCE0916、R0729、R0581—R0584、RCE2220—RCE2224、X01106—X01107、X02536、`规范/0600_代码函数流程图应用与维护规范.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/需求服务.h` | 560 | 定义 F0552，按值绑定需求节点并借用 const `this` | N01 | 本函数内具体指令 | 入口 / 参数绑定 / 作用域 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 561 | 经 RCE2220 调用 R0729 无令牌 `需求服务::节点类型匹配(需求节点,需求)`，并对结果取反形成入口判断 | C02 / B03 | 函数调用 / 本函数内具体指令 | 调用 / 判断 | 无 | 当前节点类型读取事实 | 类型不符为逻辑内返回 | 无 |
| 同上 | 562-563 | 类型不符时返回 `std::nullopt` 并闭合分支 | R12 | 本函数内具体指令 | 返回 / 作用域 | 无 | 当前拒绝事实 | 逻辑内返回 | 无 |
| 同上 | 564 | 经 RCE2221 调用 R0582 `读取需求主体`，保存 optional `主体` | C04 | 函数调用 | 只读查询 / 局部构造 | 无 | 当前需求读取事实 | optional 可为空 | 无 |
| 同上 | 565 | 经 RCE2222 调用 R0583 `读取需求目标宿主`，保存 optional `目标宿主` | C05 | 函数调用 | 只读查询 / 局部构造 | 无 | 当前需求读取事实 | optional 可为空 | 无 |
| 同上 | 566 | 经 RCE2223 调用 R0584 `读取需求场景`，保存 optional `场景` | C06 | 函数调用 | 只读查询 / 局部构造 | 无 | 当前需求读取事实 | optional 可为空 | 无 |
| 同上 | 567 | 经 RCE2224 调用 R0581 `读取需求目标状态`，保存 optional `目标状态` | C07 | 函数调用 | 只读查询 / 局部构造 | 无 | 当前需求读取事实 | optional 可为空 | 无 |
| 同上 | 568-571 | 经 X01106 依次调用四个 optional 的 `has_value()`，短路判断是否存在任一缺失 | X08 / B09 | 函数调用 / 本函数内具体指令 | 外部边界 / 判断 / 短路 | 无 | 当前组合读取判断 | 任一无值为逻辑内返回 | 无 |
| 同上 | 572-573 | 任一材料缺失时返回 `std::nullopt` 并闭合分支 | R12 | 本函数内具体指令 | 返回 / 作用域 | 无 | 当前组合读取结果 | 逻辑内返回 | 无 |
| 同上 | 574 | 经 X01107 四次读取 optional 值，按固定字段顺序构造 `需求承接材料`，再经 X02536 转换构造 optional 返回值 | X10 / N11 / X12 / R13 | 函数调用 / 本函数内具体指令 | 外部边界 / 聚合构造 / 转换构造 / 返回 | 无持久结构变化 | 当前非持久组合投影 | 不适用 | 无 |
| 同上 | 575 | 闭合函数词法作用域并释放四个局部 optional | R12 / R13 | 本函数内具体指令 | 作用域 / 生命周期 | 无 | 当前代码事实 | 沿既定返回终点结束 | 无 |

覆盖结论：560—575 行全部映射；RCE2220—RCE2224、X01106、X01107、X02536 与全部图节点可反查；无未解析调用。
