# CF-L6-0210 `生成世界树节点显示名称` 逐行代码映射表

代码版本：`2c38ec4017e38b2e541b93c8c9ae038856f849c7`
源码 blob：`fa157b5822e3d67b336ffbee06f763b1f22e59a6`
覆盖文件：`海中鱼巣/界面/投影.控制面板启动.ixx:55-68`
根函数：F0386 `std::wstring 海中鱼巣::生成世界树节点显示名称(海中鱼巣::节点句柄 节点值, const 海中鱼巣::语素节点显示语义材料& 显示语义, const std::optional<海中鱼巣::世界树相对坐标>& 自我相对坐标)`
配对流程图：`流程图/代码流程图/L6/CF-L6-0210_生成世界树节点显示名称_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/8110_子规范_线程生命周期状态上报与控制面板线程信息_20260720.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/界面/投影.控制面板启动.ixx` | 55 | 进入 F0386，声明返回调用方拥有的 `std::wstring` | S | 本函数内具体指令 | 入口 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 56 | 按值接收 `节点句柄 节点值`；本函数只读取其中的节点编号 | S | 本函数内具体指令 | 参数绑定 / 读取准备 | 无 | 临时局部值 | 不适用 | 不把显示编号文本当长期身份 |
| 同上 | 57 | 以 const 非拥有引用接收 `显示语义`；借用只持续到返回或异常传播 | S | 本函数内具体指令 | 参数绑定 | 无 | 当前调用材料 | 不适用 | 不保存引用 |
| 同上 | 58 | 以 const 非拥有引用接收坐标 optional 并打开函数体 | S | 本函数内具体指令 | 参数绑定 / 作用域 | 无 | 当前调用材料 | 不适用 | 不保存引用 |
| 同上 | 59 | 读取名称文本和类型文本，并与固定人读分隔符组成局部 `名称` 初始化表达式 | N02 | 本函数内具体指令 | 读取 / 字符串构造 | 只形成局部人读字符串 | 非权威人读材料 | 构造异常传播，不形成普通返回 | 文本不得参与机器路由或业务裁决 |
| 同上 | 60 | 经 X01015 把 `节点值.节点编号` 转为宽字符串，追加 `#` 和编号文本，完成局部 `名称` 构造；转换或拼接异常经 E12 传播 | X01 / N02 / E12 | 外部边界 / 本函数内具体指令 | 调用 / 字符串构造 / 异常传播 | 只形成局部人读字符串 | 非权威人读材料 | 异常传播 | X01015 已有正式边界身份 |
| 同上 | 61 | 读取坐标 optional 的有值状态；无值直接进入返回，有值进入坐标文本追加路径 | B03 | 本函数内具体指令 | 判断 / 读取 | 无 | 当前调用材料 | 无失败结果；只分显示格式 | 正式聚合未给 optional 操作独立边界 ID，不补造 |
| 同上 | 62 | 坐标有值时开始形成待追加的“相对坐标”人读片段；完整 `+=` 表达式在 63—65 行取得三个分量文本后由 N10 完成 | N10 | 本函数内具体指令 | 字符串追加 | 只修改局部人读字符串 | 非权威人读材料 | 追加异常传播 | 不把坐标文本写成机器事实 |
| 同上 | 63 | 读取横向 double，经 N04 `static_cast<int>` 向零截断，再经 X01016 转为宽字符串并作为 N10 的横向文本；转换 / 追加异常经 E12 传播 | N04 / X05 / N10 / E12 | 本函数内具体指令 / 外部边界 | 读取 / 数值转换 / 调用 / 字符串追加 | 只修改局部人读字符串 | 非权威人读材料 | 不可表示 double 没有结构化返回；字符串异常传播 | 待核上游是否保证有限且处于 `int` 范围 |
| 同上 | 64 | 读取纵向 double，经 N06 `static_cast<int>` 向零截断，再经 X01017 转为宽字符串并作为 N10 的纵向文本；转换 / 追加异常经 E12 传播 | N06 / X07 / N10 / E12 | 本函数内具体指令 / 外部边界 | 读取 / 数值转换 / 调用 / 字符串追加 | 只修改局部人读字符串 | 非权威人读材料 | 不可表示 double 没有结构化返回；字符串异常传播 | 待核上游是否保证有限且处于 `int` 范围 |
| 同上 | 65 | 读取垂向 double，经 N08 `static_cast<int>` 向零截断，再经 X01018 转为宽字符串；N10 拼接三个整数文本、固定 `.0` 与右括号并追加到名称，异常经 E12 传播 | N08 / X09 / N10 / E12 | 本函数内具体指令 / 外部边界 | 读取 / 数值转换 / 调用 / 字符串追加 / 异常传播 | 只修改局部人读字符串 | 非权威人读材料 | 不可表示 double 没有结构化返回；字符串异常传播 | 待核上游是否保证有限且处于 `int` 范围 |
| 同上 | 66 | 闭合坐标有值分支；无值路径保持基础名称，有值路径已经追加坐标文本 | B03 / N10 | 本函数内具体指令 | 分支闭合 | 无新增机器结构 | 当前控制流 | 不适用 | 无 |
| 同上 | 67 | 按值返回局部 `名称`；调用方经 E0850 把结果写入局部控制面板树节点材料的显示字段 | R11 | 本函数内具体指令 | 返回 | 只把人读字符串移交调用方 | 非权威人读材料 | 正常返回 | 不宣称返回文本是节点身份或业务事实 |
| 同上 | 68 | 闭合函数作用域；R11 或 E12 已是终点，所有输入借用结束，异常路径释放已构造局部字符串 | R11 / E12 | 本函数内具体指令 | 作用域 / 生命周期 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：55—68 行逐行完整映射；13 个流程节点全部被映射；唯一项目入边 E0850、四个外部边界 X01015—X01018、0 个项目出边和 0 个未解析项目调用均闭合。正式聚合没有为 optional 访问或宽字符串运算符分配独立边界 ID，本图未补造身份；坐标 double 到 `int` 的有限性 / 范围前置仍是需向顶层维护智能体报告的当前契约风险。
