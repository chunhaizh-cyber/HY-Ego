# CF-L6-0016 `结构事务接线::已接域` 逐行代码映射表

代码版本：`a48a70b2d678dea64dd8228adb4f26f0badd9506`
覆盖文件：`海中鱼巣/核心/结构事务接线.数据.h:77-80`
根函数：F0336 `bool 海中鱼巣::结构事务接线::已接域() const`
配对流程图：`流程图/代码流程图/L6/CF-L6-0016_结构事务接线已接域_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4040_子规范_不透明结构事务候选确认撤销与最后发布.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`、`规范/详细设计/不透明结构写入会话许可内探测计量与全参与者原子收口详细设计.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/结构事务接线.数据.h` | 77 | 进入 const 成员函数，绑定调用方拥有且调用期有效的隐式 `this` | S | 本函数内具体指令 | 入口 / 借用 | 无 | 当前代码事实 | 不适用 | 不把 `const` 误写成同步或快照保证 |
| 同上 | 78 | 判断域编号是否非零；失败时 `&&` 短路返回 `false` | B01 / RF | 本函数内具体指令 | 读取 / 判断 / 返回 | 无 | 接线形状读取 | 调用语境决定逻辑内返回或内部错误 | 不把非零解释为身份新鲜 |
| 同上 | 78 | 仅域编号非零时判断运行期纪元是否非零；失败短路返回 `false` | B02 / RF | 本函数内具体指令 | 读取 / 判断 / 返回 | 无 | 接线形状读取 | 同上 | 不把非零解释为已发布代次一致 |
| 同上 | 78 | 仅前两项通过时调用 X0367 读取 `shared_ptr<void>` 存储指针布尔形状；为空时短路返回 `false` | X03 / B04 / RF | 函数调用 / 本函数内具体指令 | 读取 / 判断 / 返回 | 无 | 接线形状读取 | 同上 | 动态类型、控制块和域归属另行验证 |
| 同上 | 78 | 仅共享状态非空时依次判断取得共享许可、取得独占许可两个函数指针非空；首个失败短路 | B05 / B06 / RF | 本函数内具体指令 | 读取 / 判断 / 返回 | 无 | 回调形状读取 | 同上 | 非空不证明回调同源或行为正确 |
| 同上 | 79 | 依次判断验证共享路径令牌、验证独占令牌、标记撤销失败隔离三个函数指针非空；首个失败返回 `false`，全部通过返回 `true` | B07 / B08 / B09 / RF / RT | 本函数内具体指令 | 读取 / 判断 / 返回 | 无 | 回调形状读取 | 同上 | 不把 `true` 扩大为许可或令牌有效 |
| 同上 | 80 | 闭合函数体；没有局部拥有资源或额外运行指令 | Z | 本函数内具体指令 | 作用域闭合 | 无 | 当前代码事实 | 不适用 | 无 |

覆盖结论：77—80 行全部映射；项目被调函数 0，外部边界 X0367 一个（调用 1 次），未解析调用 0。13 个已登记调用函数的 22 条入边与本文反向引用闭合。
