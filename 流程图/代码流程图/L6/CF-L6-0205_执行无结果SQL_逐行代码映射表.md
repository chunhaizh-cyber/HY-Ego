# CF-L6-0205 `执行无结果SQL` 逐行代码映射表

代码版本：`9b4aa4a151e32d09d30cd9f28c9f85a45fd2692b`
覆盖文件：`海中鱼巣/适配/SQL数据库适配.cpp:250-265`
根函数：F0381 `bool 海中鱼巣::{匿名命名空间@SQL数据库适配.cpp}::执行无结果SQL(SQLHDBC 连接, std::wstring SQL文本, std::wstring& 诊断)`
配对流程图：`流程图/代码流程图/L6/CF-L6-0205_执行无结果SQL_现状流程图.md`
已登记调用方：E0819 / F0203，调用点 `SQL数据库适配.cpp:345,375`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`、`规范/详细设计/SQLServer结构审计投影与控制面板查看详细设计.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/适配/SQL数据库适配.cpp` | 250 | 进入匿名命名空间函数；按值复制连接句柄、取得本地 `SQL文本` 所有权，并借用可写 `诊断` 引用 | S | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 调用方保证连接与诊断引用在调用期间有效 |
| 同上 | 251 | 经 RCE0165 调用 F0207，以 `连接` 构造局部 `ODBC语句 语句`；构造完成后该对象拥有非空 STMT 句柄的释放责任；若构造完成前异常则经 E20 直接传播 | C01 / E20 | 函数调用 / 本函数内具体指令 | 构造 / 资源取得 / 异常传播 | 可能在 ODBC 驱动内分配 STMT 句柄；不写项目机器结构 | 当前适配资源状态 | 构造异常直接传播；句柄分配失败由对象保持空句柄 | 后续退出经 RCE1742 隐式析构 F0214 |
| 同上 | 252 | 经 RCE0166 调用 F0208 `语句.可用()` 并判断结果；false 进入分配失败分支 | C02 / B03 | 函数调用 / 本函数内具体指令 | 句柄状态读取 / 判断 | 无 | 当前局部语句状态 | 不可用为适配层失败；E0819 语境按追根因 | 无 |
| 同上 | 253 | 仅语句不可用时，经 X01003 把固定文本覆盖写入调用方 `诊断`；字符串赋值异常时经 D18 调用 RCE1742 析构已构造语句，再经 E19 继续传播 | X04 / D18 / E19 | 外部边界 / 函数调用 / 本函数内具体指令 | 字符串赋值 / 输出 / RAII / 异常传播 | 改变调用方诊断字符串；不写机器事实 | 人读诊断材料 | 字符串赋值异常在语句构造后发生，先析构再传播 | 诊断不得参与机器事实裁决 |
| 同上 | 254 | 语句不可用分支返回 `false`；返回前经 RCE1742 隐式调用 F0214 析构局部语句 | D05 / RF1 | 函数调用 / 本函数内具体指令 | RAII 析构 / 返回 | 尝试释放非空 STMT；本路径通常为空；零项目结构写入 | 当前适配结果 | E0819 当前语境按追根因 | 保留释放失败不可见边界 |
| 同上 | 255 | 闭合语句不可用分支；只有 F0208=true 时继续 SQL 执行 | B03 | 本函数内具体指令 | 分支闭合 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 256 | 开始形成 X01004 `SQLExecDirectW` 外部调用，并准备保存其 `SQLRETURN` | X09 / N10 | 外部边界 / 本函数内具体指令 | 外部数据库调用 / 局部绑定 | 可能使外部 SQL Server 执行 SQL；不直接写项目内存事实 | 外部数据库操作结果 | ODBC 失败以 SQLRETURN 返回 | 不把调用返回成功扩大为结构读回成功 |
| 同上 | 257 | 经 RCE0167 第一次调用 F0209，读取局部语句的 `SQLHSTMT` 作为 X01004 第一实参 | C06 | 函数调用 | 句柄读取 / 实参绑定 | 无 | 当前局部语句句柄 | 不适用 | 无 |
| 同上 | 258 | 经 X02309 调用 `SQL文本.data()` 取得可写首地址，并 `reinterpret_cast` 为 `SQLWCHAR*` 作为 X01004 第二实参 | X07 / N08 | 外部边界 / 本函数内具体指令 | 连续存储读取 / 指针转换 | 无；借用本地字符串存储 | 当前本地 SQL 文本 | 内嵌 NUL 不被拒绝，ODBC 将按 SQL_NTS 提前终止 | 如需完整长度语义应使用显式长度并冻结合同 |
| 同上 | 259 | 把 `SQL_NTS` 作为 X01004 第三实参，声明 SQL 文本以 NUL 终止 | N08 / X09 | 本函数内具体指令 / 外部边界 | 实参绑定 / 外部调用完成 | 可能改变外部数据库 | ODBC 返回材料 | ODBC 非成功由后继处理 | 无 |
| 同上 | 260 | 经 RCE0168 调用 F0210 对 X01004 的 `结果` 分类；false 进入诊断分支 | C11 / B12 | 函数调用 / 本函数内具体指令 | 结果分类 / 判断 | 无 | 当前 SQLRETURN 分类 | E0819 语境中的非成功为追根因 | 无 |
| 同上 | 261 | 仅执行失败时，经 RCE0167 第二次调用 F0209 取得 STMT 句柄，再经 RCE0169 调用 F0211 形成 ODBC 诊断，最后经 X01005 移动赋值覆盖调用方诊断 | C13 / C14 / X15 | 函数调用 / 外部边界 | 句柄读取 / 诊断读取 / 输出赋值 | 改变调用方人读诊断；不写项目机器事实 | ODBC 人读诊断材料 | 诊断形成或赋值异常先析构局部语句再传播 | 诊断只作失败证据，不作机器裁决 |
| 同上 | 262 | SQL 执行失败分支返回 `false`；返回前经 RCE1742 隐式调用 F0214 释放局部 STMT | D16 / RF2 | 函数调用 / 本函数内具体指令 | RAII 析构 / 返回 | 尝试释放 STMT；释放结果被 F0214 丢弃 | 当前适配结果 | E0819 当前语境按追根因 | 保留释放失败不可见边界 |
| 同上 | 263 | 闭合 SQL 执行失败分支；只有 F0210=true 时进入成功返回 | B12 | 本函数内具体指令 | 分支闭合 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 264 | SQL 执行成功时返回 `true`，不清空 `诊断`；返回前源码隐式调用 F0214 释放局部 STMT | D17 / RT | 函数调用 / 本函数内具体指令 | RAII 析构 / 返回 | 可能已改变外部数据库；零项目机器结构写入 | 当前适配结果 | 成功不是结构读回证据 | 调用方在 true 时不得消费旧诊断为本次结果 |
| 同上 | 265 | 闭合函数作用域；局部 SQL 文本析构，连接句柄仍归调用方，诊断引用不保存 | RF1 / RF2 / RT | 本函数内具体指令 | 作用域 / 生命周期 | 无新增机器结构 | 当前代码事实 | 不适用 | 无 |

覆盖结论：250—265 行全部映射；E0819 入边、RCE0165—RCE0169、RCE1742 与 X01003—X01005、X02309 均已落入具名节点；0 未解析项目调用。
