# CF-L6-0009 `状态服务::读取状态值` 逐行代码映射表

代码版本：`a48a70b2d678dea64dd8228adb4f26f0badd9506`
覆盖文件：`海中鱼巣/领域/状态服务.h:185-191`
根函数：F0329 `std::optional<std::int64_t> 海中鱼巣::状态服务::读取状态值(海中鱼巣::节点句柄 状态节点) const`
配对流程图：`流程图/代码流程图/L6/CF-L6-0009_状态服务读取状态值_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/1160_根规范_状态节点_20260720.md`、`规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`、`规范/4020_子规范_领域类型化数据记录与组合读取投影边界.md`、`规范/4210_子规范_动态信息分层获取与聚合_20260720.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/状态服务.h` | 185 | 进入 const 状态服务成员函数，按值接收状态句柄并绑定服务借用 | S | 本函数内具体指令 | 入口 / 借用 | 无 | 当前实现事实 | 不适用 | 不把旧值读取扩大为完整状态事实 |
| 同上 | 186 | 调用 F0190 从节点仓按完整句柄读取节点记录并形成局部 optional | C01 | 函数调用 | 节点读取 | 无 | 权威节点读取候选 | 不适用 | 后继仍须状态领域完整读取 |
| 同上 | 187 | 调用 X0361 `has_value()`；为空时短路进入拒绝 | X02 / B03 | 函数调用 / 本函数内具体指令 | 判断 | 无 | 当前实现读取 | 依调用语境分账 | 保留普通查询与强前置区别 |
| 同上 | 187 | 记录存在时调用 X0361 `operator->()`，读取并比较节点类型；类型不是状态时短路拒绝 | X04 / B05 | 函数调用 / 本函数内具体指令 | 借用 / 读取 / 判断 | 无 | 权威节点类型读取 | 同上 | 节点类型不等于状态事实完整 |
| 同上 | 188 | 前述任一条件不成立时构造并返回 `nullopt` | RF0 / RF1 | 本函数内具体指令 | 返回 | 无 | 当前实现结果 | 普通查询逻辑内返回；强前置语境为内部错误 | 不吞没成功发布后的读回失败 |
| 同上 | 190 | 再次调用 X0361 `operator->()`读取旧 `主信息` 句柄，调用 F0619 读取零号 I64 槽，并原样按值返回 optional | X06 / C07 / R08 | 函数调用 / 本函数内具体指令 | 旧结构读取 / 返回 | 无 | 已退出旧结构读取 | F0619 的空值依语境分账 | 迁移到状态领域类型化记录及关系 19 组合投影 |
| 同上 | 191 | 闭合函数体并析构局部节点记录 optional | Z | 本函数内具体指令 | 作用域闭合 / 生命周期 | 无 | 当前实现事实 | 不适用 | 无 |

覆盖结论：185—191 行全部映射；项目被调函数 2 个、调用点 2 个，外部边界 X0361 一个（聚合 4 个 optional / 枚举 / 返回操作），未解析调用 0。既有 E0629、E0630、E0643、E0711、E0745、E1254、E1660 与 F0357 在 E1707 节点记录读取完成后、if 短路判断前无条件执行的 E1667 入边，以及 E1665 / E1666 出边双向闭合。
