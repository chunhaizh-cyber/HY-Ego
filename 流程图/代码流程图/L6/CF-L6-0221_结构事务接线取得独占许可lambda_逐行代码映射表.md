# CF-L6-0221 `结构事务协调器::生成接线` 取得独占许可 lambda 逐行代码映射表

代码版本：`9ce1a8acb973155ef7a14c307e22a2b2358c173d`
源码 blob：`946512e4b88d29bbf6d43eb07b02823471489dae`
覆盖文件：`海中鱼巣/核心/协调.结构事务.ixx:153`
根函数：F0398 `海中鱼巣::结构事务许可 海中鱼巣::结构事务协调器::生成接线()::<lambda@协调.结构事务.ixx:153>::operator()(const std::shared_ptr<void>& 状态) const`
配对流程图：`流程图/代码流程图/L6/CF-L6-0221_结构事务接线取得独占许可lambda_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4040_子规范_不透明结构事务候选确认撤销与最后发布.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/协调.结构事务.ixx` | 153 | 定义并进入无捕获 const lambda F0398；借用 `const std::shared_ptr<void>& 状态`，固定 `结构许可类型::独占`，经 RCE0195 调用 R0101 `取得许可(状态, 独占)`，再按值原样返回许可。R0101 未在内部收口的异常经 E03 传播。E0872 在 F0233 中把该 lambda 绑定为 `结构事务接线::取得独占许可` 函数指针；RCE0556 是 R0116 对该函数指针的当前唯一生产运行调用 | S / C01 / R02 / E03 | 本函数内具体指令 / 函数调用 | lambda 定义 / 参数绑定 / 固定枚举 / 项目调用 / 返回 / 异常传播 | F0398 自身不直接改写事务状态；R0101 按其合同登记活动、取得独占锁并形成许可或收口为无效许可 | 当前代码事实 / 事务运行期材料 | 无效许可由 R0101 返回并由 R0116 映射为许可拒绝；未收口异常继续传播 | X01069 与 RCE0195 指向同一调用点和项目函数，顶层已裁决为重复误分类外部边界并将在共享聚合中删除；本图不补造第二次调用 |

覆盖结论：153 单行完整映射；4 个流程节点全部被映射；项目入边 E0872 / RCE0556、项目出边 RCE0195、0 个外部边界和 0 个未解析项目调用闭合。X01069 是 RCE0195→R0101 的重复误分类记录，按顶层维护裁决仅报告并由顶层修改共享聚合。
