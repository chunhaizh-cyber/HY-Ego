# CF-L6-0045 `动态服务::创建动态概念根` 逐行代码映射表

代码版本：`a9fd8ba33963f8d177fae753e5b01cea0073d407`
覆盖文件：`海中鱼巣/领域/动态服务.h:285-299`
根函数：F0366 `海中鱼巣::节点句柄 海中鱼巣::动态服务::创建动态概念根()`
配对流程图：`流程图/代码流程图/L6/CF-L6-0045_动态服务创建动态概念根_现状流程图.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/动态服务.h` | 285 | 进入非 const 成员函数；绑定隐式 `this`，无显式参数 | S | 本函数内具体指令 | 入口 / 所有权绑定 | 无 | 当前代码事实 | 不适用 | 最终合同应显式绑定稳定动态根身份 |
| 同上 | 286 | 经 RCE1713 调用 F0331 无令牌重载，立即创建旧主信息并把句柄绑定到局部常量 | C01 | 函数调用 | 主信息写入 / 返回绑定 | 主信息仓可能新增一条有效记录 | 当前旧主信息结构 | 后继失败属于内部错误 | 退出旧主信息，纳入统一候选事务 |
| 同上 | 287 | 经 RCE1714 调用 F0332 二参数无令牌重载，以动态类型和旧主信息句柄即时创建节点 | C02 | 函数调用 | 节点写入 / 返回绑定 | 节点仓可能新增一个普通动态节点 | 当前旧节点结构 | 空句柄仍继续进入读回，属于内部错误链 | 使用节点直接稳定身份候选 |
| 同上 | 288 | 经 RCE1715 调用 F0190 读取刚返回的根节点并绑定值式可选记录 | C03 | 函数调用 | 读回 | 无新增 | 当前节点仓读取 | 无记录属于写后内部错误 | 发布前完成候选内读回 |
| 同上 | 289 | 第一次经 X02278 检查记录；有值时经 X02279 访问主信息并经 RCE1716 调用 F0619 读取零号 I64，没值时物化空 `optional<int64_t>` | X04 / B05 / X06 / C07 / N08 | 外部边界函数调用 / 函数调用 / 本函数内具体指令 | 条件读取 / 值式构造 | 无新增 | 当前旧主信息值读取 | 本行不返回；失败进入后验内部错误 | 使用类型化动态记录而非旧值槽 |
| 同上 | 290 | 第二次经 X02278 检查同一记录并开始构造传给 F0184 的严格合取条件 | X09 / B10 / N20F / N20T / C21 | 外部边界函数调用 / 本函数内具体指令 / 函数调用 | 判断 / 短路 / 调用参数求值 | 无新增 | 当前控制流 | `false` 使后续条件短路，最终属内部错误 | 结构化列明失败阶段 |
| 同上 | 291 | 仅记录有值时经 X02279 访问记录类型并比较是否为动态 | X11 / B12 | 外部边界函数调用 / 本函数内具体指令 | 读取 / 类型判断 | 无 | 当前节点记录 | 类型错误为写后内部错误 | 固定节点类型与身份共同读回 |
| 同上 | 292 | 仅前两项成立时经 X02279 取主信息并经 RCE1717 调用 F0624 无令牌重载检查旧主信息有效性 | X13 / C14 / B15 | 外部边界函数调用 / 函数调用 / 本函数内具体指令 | 读取 / 判断 | 无 | 当前旧主信息结构 | 无效为写后内部错误 | 退出旧主信息门禁 |
| 同上 | 293 | 仅前项成立时经 X02280 检查普通动态材料是否有值，并要求结果为 false | X16 / B17 | 外部边界函数调用 / 本函数内具体指令 | 读取 / 否定判断 | 无 | 当前旧值槽投影 | 材料存在为写后内部错误 | 用正式动态根签名正面确认身份 |
| 同上 | 294 | 仅前项成立时经 RCE0146 调用 R0610 一参数重载判断实例动态，并要求结果为 false | C18 / B19 / N20F / N20T | 函数调用 / 本函数内具体指令 | 关系读取 / 否定判断 / 合取物化 | 无 | 当前节点与关系投影 | 实例动态成立为写后内部错误 | 固定动态根身份和完整关系角色 |
| 同上 | 295 | 传入固定说明并完成 RCE0145 / F0184 调用；对返回 bool 取反作为失败分支条件 | C21 / B22 | 函数调用 / 本函数内具体指令 | 诊断调用 / 判断 | 当前配置零机器结构变化；诊断启用时只产生非权威日志或界面 | 人读诊断 / 当前控制流 | `false` 为追根因解决 | 诊断不得替代撤销或错误结果 |
| 同上 | 296 | 内部预期不成立时返回默认空节点句柄；不撤销第 286—287 行可能完成的写入 | RF | 本函数内具体指令 | 返回 | 既有主信息或节点可能保留 | 当前返回材料 | 追根因解决 | 返回具名阶段、写集和撤销结果 |
| 同上 | 297 | 闭合失败分支 | B22 | 本函数内具体指令 | 分支闭合 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 298 | 全部后验成立时按值返回根节点句柄 | RT | 本函数内具体指令 | 返回 | 无新增；既有节点保持已发布 | 当前旧结构句柄 | 不适用 | 不得把普通动态节点等同稳定动态根身份 |
| 同上 | 299 | 闭合函数作用域；局部句柄和可选记录销毁，异常路径未捕获 | E / Z | 本函数内具体指令 | 作用域闭合 / 异常 | 局部销毁不撤销仓库结构 | 当前代码事实 | 异常为内部错误 | 统一异常安全和事务撤销 |

覆盖结论：285—299 行全部映射；项目调用边 7 条，外部边界身份 3 个、调用点 6 个，未解析调用 0。第一笔写入后的所有非成功结果均按追根因解决记录。
