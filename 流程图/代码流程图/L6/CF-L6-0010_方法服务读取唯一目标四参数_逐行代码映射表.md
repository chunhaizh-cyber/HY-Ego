# CF-L6-0010 `方法服务::读取唯一目标` 四参数重载逐行代码映射表

代码版本：`a48a70b2d678dea64dd8228adb4f26f0badd9506`
覆盖文件：`海中鱼巣/领域/方法服务.h:1711-1730`
根函数：F0330 `std::optional<海中鱼巣::节点句柄> 海中鱼巣::方法服务::读取唯一目标(海中鱼巣::节点句柄 源节点, 海中鱼巣::关系类型 类型, 海中鱼巣::节点类型 目标类型, std::optional<std::int64_t> 顺序号) const`
配对流程图：`流程图/代码流程图/L6/CF-L6-0010_方法服务读取唯一目标四参数_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/3300_根规范_方法_20260720.md`、`规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`、`规范/5300_子规范_方法登记选择与执行规则_20260720.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/方法服务.h` | 1711—1712 | 进入四参数 const 成员函数，按值绑定源节点、两类枚举、顺序号 optional 与隐式服务借用 | S | 本函数内具体指令 | 入口 / 借用 | 无 | 当前实现事实 | 不适用 | 与三参数重载保持独立身份 |
| 同上 | 1713 | 调用 F0333 检查源节点是否可读且节点类型为方法 | C01 / B02 | 函数调用 / 本函数内具体指令 | 节点读取 / 判断 | 无 | 当前实现读取 | 依语境分账 | 后继还须方法角色复核 |
| 同上 | 1714 | 源节点不是方法时返回 `nullopt` | RU0 | 本函数内具体指令 | 返回 | 无 | 当前实现结果 | 普通查询逻辑内返回；强前置语境为内部错误 | 不吞没已发布方法结构漂移 |
| 同上 | 1716 | 调用 X0362 `顺序号.has_value()`并选择关系读取重载 | X03 / B04 | 函数调用 / 本函数内具体指令 | 判断 / 分支 | 无 | 当前实现输入 | 不适用 | 业务顺序号值域由具名规范裁决 |
| 同上 | 1717 | 有顺序号时调用 X0362 `value()`，再调用 F0620 按源、类型和顺序号读取目标组 | X05 / C06 | 函数调用 | 关系读取 | 无 | 权威关系读取候选 | 普通空组允许；强前置为空是内部错误 | 保留精确角色与基数复核 |
| 同上 | 1718 | 无顺序号时调用 F0621 按源和类型读取全部目标组 | C07 | 函数调用 | 关系读取 | 无 | 权威关系读取候选 | 同上 | 不把多目标静默解释为唯一 |
| 同上 | 1719 | 调用 X0362 默认构造空的 `optional<节点句柄>` | X08 | 函数调用 | 局部构造 | 无 | 临时局部值 | 不适用 | 无 |
| 同上 | 1720 | 按关系仓返回顺序遍历目标组；耗尽时离开循环 | L09 | 本函数内具体指令 | 循环控制 / 借用 | 无 | 当前实现控制流 | 不适用 | 依赖关系仓稳定排序 |
| 同上 | 1721 | 对当前目标调用 F0333 检查目标节点类型 | C10 / B11 | 函数调用 / 本函数内具体指令 | 节点读取 / 判断 | 无 | 当前实现读取 | 依语境分账 | 错类型关系应在业务边界具名处理 |
| 同上 | 1722 | 目标类型不匹配时跳过该目标并继续循环 | L12 / L16 | 本函数内具体指令 | 循环控制 | 无 | 当前实现控制流 | 普通筛选继续 | 当前实现会静默忽略关系污染 |
| 同上 | 1724 | 调用 X0362 `唯一目标.has_value()`判断是否已有匹配目标 | X13 / B14 | 函数调用 / 本函数内具体指令 | 判断 | 无 | 临时局部值 | 不适用 | 无 |
| 同上 | 1725 | 第二个匹配目标出现时立即返回 `nullopt` | RU1 | 本函数内具体指令 | 返回 | 无 | 当前实现结果 | 普通查询逻辑内返回；强前置为基数错误 | 后继具名区分零目标和多目标 |
| 同上 | 1727 | 调用 X0362 把当前唯一匹配目标赋给 optional | X15 | 函数调用 | 局部赋值 | 无 | 临时局部值 | 不适用 | 无 |
| 同上 | 1728 | 回到循环读取下一目标 | L16 | 本函数内具体指令 | 循环控制 | 无 | 当前实现控制流 | 不适用 | 无 |
| 同上 | 1729 | 目标组耗尽后按值返回唯一目标 optional；可能为空或持有一个句柄 | RR | 本函数内具体指令 | 返回 | 无 | 当前实现结果 | 空值依语境分账 | 不把有值扩大为业务角色完整 |
| 同上 | 1730 | 闭合函数体并析构目标组与局部 optional | Z | 本函数内具体指令 | 作用域闭合 / 生命周期 | 无 | 当前实现事实 | 不适用 | 无 |

覆盖结论：1711—1730 行全部映射；项目被调函数 3 个唯一身份、4 个调用点，外部边界 X0362 一个（聚合 9 个 optional / vector / 循环生命周期操作），未解析调用 0。E0631、E0632、E1659 与三参数包装器 F0629 的 E1708 入边，以及 E1668—E1671 出边双向闭合。1707—1709 三参数包装器保持独立 F0629 身份和 `待处理` 状态，不并入 F0330，也不计入已完成。
