# CF-L6-0031 `概念图服务::读取概念根` 逐行代码映射表

代码版本：`a48a70b2d678dea64dd8228adb4f26f0badd9506`
覆盖文件：`海中鱼巣/领域/概念图服务.h:892-902`
根函数：F0352 `std::optional<海中鱼巣::概念根登记材料> 海中鱼巣::概念图服务::读取概念根(海中鱼巣::概念根类别 类别) const`
配对流程图：`流程图/代码流程图/L6/CF-L6-0031_概念图服务读取概念根_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`、`规范/7140_子规范_概念身份生命周期退役删除与活动投影分账.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/概念图服务.h` | 892 | 进入 const 成员函数，按值绑定根类别并借用概念图服务 | S | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 893 | 调用 F0639 项目静态辅助函数 `根类别有效(类别)` 并判断结果 | C01 / B02 | 函数调用 / 本函数内具体指令 | 纯计算 / 判断 | 无 | 当前实现枚举门禁 | 类别非法为逻辑内空结果 | 无 |
| 同上 | 894 | 类别无效时在加锁前返回 `nullopt` | RN0 | 本函数内具体指令 | 返回 | 无 | 当前代码结果 | 入口拒绝折叠为空结果 | 后继强类型入口具名区分类别非法 |
| 同上 | 896 | 构造 `shared_lock` 并取得根登记锁共享所有权 | X03 | 函数调用-外部边界 X0375 | 加锁 / 资源取得 | 持有同步资源；无机器事实变化 | 非权威同步材料 | 锁异常向上传播 | 保持 RAII 释放 |
| 同上 | 897 | 调用 F0640 项目静态辅助函数 `根类别索引`，以结果索引固定数组并绑定对应 optional 槽的 const 引用 | C04 / X05 | 函数调用 / 外部边界 X0375 | 纯计算 / 读取 / 借用 | 无 | 非权威根登记投影读取 | 前置由 C01 保证索引在 0—3 | 无 |
| 同上 | 898 | 调用 `has_value()`；有值时取得材料借用并调用 F0377 复核根节点有效性；`||` 在材料为空时短路节点读取 | X06 / B07 / X10 / C11 / B12 | 外部边界 X0375 / 函数调用 / 本函数内具体指令 | 判断 / 短路 / 节点读取 | 无 | 登记投影与权威节点读取候选 | 依调用语境分账 | 不把双重命中扩大为正式根身份 |
| 同上 | 899 | 登记缺失或节点无效时形成空 optional，释放共享锁并返回 `nullopt` | N08 / D09 / RN1 或 N13 / D14 / RN2 | 本函数内具体指令 / 外部边界 X0375 | 返回物化 / 解锁 / 返回 | 释放同步资源；无机器事实变化 | 当前代码结果 | 普通查询逻辑内返回；强前置为内部错误 | 具名区分投影缺失与节点失效 |
| 同上 | 901 | 在共享锁内复制平凡 `材料`，形成值式 optional 返回对象；随后释放共享锁并返回副本 | N15 / D16 / RR | 本函数内具体指令 / 外部边界 X0375 | 值式复制 / 解锁 / 返回 | 只形成调用方拥有的副本 | 非权威读取投影 | 平凡复制不抛出 | 不暴露内部引用 |
| 同上 | 902 | 闭合函数作用域；异常路径析构已取得的共享锁 | DX / E / Z | 外部边界 X0375 / 本函数内具体指令 | 作用域闭合 / 异常传播 | 释放局部资源；无机器事实变化 | 当前代码事实 | 异常为调用方可见内部错误候选 | 无 |

覆盖结论：892—902 行全部映射；项目被调函数 F0639、F0640、F0377 共 3 个唯一源码目标、3 个调用点；X0375 覆盖共享锁、数组和 optional 访问，平凡返回复制按本函数指令记录，未解析调用 0。七条既有入边 E0708 / E0714 / E0720 / E0726 / E0732 / E0777 / E1023 已反向列明，出边 E1852—E1854 已登记。
