# CF-L6-0008 `状态服务::状态是否实例状态` 逐行代码映射表

代码版本：`a48a70b2d678dea64dd8228adb4f26f0badd9506`
覆盖文件：`海中鱼巣/领域/状态服务.h:205-209`
根函数：F0328 `bool 海中鱼巣::状态服务::状态是否实例状态(海中鱼巣::节点句柄 状态节点) const`
配对流程图：`流程图/代码流程图/L6/CF-L6-0008_状态服务状态是否实例状态_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/1160_根规范_状态节点_20260720.md`、`规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`、`规范/4210_子规范_动态信息分层获取与聚合_20260720.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/状态服务.h` | 205 | 进入 const 状态服务成员函数，按值接收状态句柄并绑定服务借用 | S | 本函数内具体指令 | 入口 / 借用 | 无 | 当前实现事实 | 不适用 | 不把 bool 扩大为状态事实 |
| 同上 | 206 | 调用 F0190 读取节点记录并按值形成局部 optional | C01 | 函数调用 | 节点读取 | 无 | 权威节点事实读取候选 | 不适用 | 后继仍须完整事实复核 |
| 同上 | 207 | 调用 X0360 `has_value()`；为空时短路返回 `false` | X02 / B03 / RF0 | 函数调用 / 本函数内具体指令 | 判断 / 返回 | 无 | 当前实现读取 | 普通查询逻辑内返回；强前置语境为内部错误 | 保留调用语境分账 |
| 同上 | 207 | 记录存在时调用 X0360 `operator->()`，读取并比较节点类型；不等于状态时短路返回 `false` | X04 / B05 / RF1 | 函数调用 / 本函数内具体指令 | 借用 / 读取 / 判断 / 返回 | 无 | 权威节点类型读取 | 同上 | 节点类型不等于实例完整性 |
| 同上 | 208 | 仅前两项成立时调用 F0580 查询旧 `运行期临时` 目标关系，并按结果返回 `true` 或 `false` | C06 / B07 / RT / RF2 | 函数调用 / 本函数内具体指令 | 关系读取 / 判断 / 返回 | 无 | 旧默认域关系事实读取 | 同上 | 迁移到关系 19 和类型化记录完整读回 |
| 同上 | 209 | 闭合函数体并析构局部 optional 记录 | Z | 本函数内具体指令 | 作用域闭合 / 生命周期 | 无 | 当前实现事实 | 不适用 | 无 |

覆盖结论：205—209 行全部映射；项目被调函数 2 个、调用点 2 个，外部边界 X0360 一个（聚合 3 个 optional / 枚举 / 短路操作），未解析调用 0。既有 E0627、E0628、E0644、E0712、E0746 与补登记 E1664 入边，以及 E1662 / E1663 出边双向闭合。
