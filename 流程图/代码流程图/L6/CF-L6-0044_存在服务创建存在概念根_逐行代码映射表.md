# CF-L6-0044 `存在服务::创建存在概念根` 逐行代码映射表

代码版本：`a9fd8ba33963f8d177fae753e5b01cea0073d407`
覆盖文件：`海中鱼巣/领域/存在服务.h:103-112`
根函数：F0365 `海中鱼巣::节点句柄 海中鱼巣::存在服务::创建存在概念根()`
配对流程图：`流程图/代码流程图/L6/CF-L6-0044_存在服务创建存在概念根_现状流程图.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/存在服务.h` | 103 | 进入非 const 成员函数；绑定隐式 `this`，无显式参数 | S | 本函数内具体指令 | 入口 / 所有权绑定 | 无 | 当前代码事实 | 不适用 | 最终合同应显式绑定稳定存在根身份 |
| 同上 | 104 | 经 RCE1709 调用 F0331 无令牌重载，立即创建旧主信息并把句柄绑定到局部常量 | C01 | 函数调用 | 主信息写入 / 返回绑定 | 主信息仓可能新增一条有效记录 | 当前旧主信息结构 | 后继失败属于内部错误 | 退出旧主信息，纳入统一候选事务 |
| 同上 | 105 | 经 RCE1710 调用 F0332 二参数无令牌重载，以存在类型和旧主信息句柄即时创建节点 | C02 | 函数调用 | 节点写入 / 返回绑定 | 节点仓可能新增一个普通存在节点 | 当前旧节点结构 | 空句柄仍继续进入读回，属于内部错误链 | 使用节点直接稳定身份候选 |
| 同上 | 106 | 经 RCE1711 调用 R0609 读取刚返回的根节点并绑定值式可选记录 | C03 | 函数调用 | 读回 | 无新增 | 当前节点仓读取 | 无记录属于写后内部错误 | 发布前完成候选内读回 |
| 同上 | 107 | 经 X02276 检查记录有值；有值时才经 X02277 访问记录并经 RCE1712 调用 F0624 检查旧主信息有效性；按 `&&` 短路形成内部预期条件 | X04 / B05 / X06 / C07 / B08 / N09 / N10 | 外部边界函数调用 / 函数调用 / 本函数内具体指令 | 读取 / 判断 / 短路 | 无新增 | 当前旧结构读回 | 条件为 false 属于写后内部错误 | 用强类型读回结果列明失败阶段 |
| 同上 | 108 | 传入固定说明并完成 RCE0144 / F0184 调用；对返回 bool 取反作为失败分支条件 | C11 / B12 | 函数调用 / 本函数内具体指令 | 诊断调用 / 判断 | 当前配置零机器结构变化；诊断启用时只产生非权威日志或界面 | 人读诊断 / 当前控制流 | `false` 为追根因解决 | 诊断不得替代撤销或错误结果 |
| 同上 | 109 | 内部预期不成立时返回默认空节点句柄；不撤销第 104—105 行可能完成的写入 | RF | 本函数内具体指令 | 返回 | 既有主信息或节点可能保留 | 当前返回材料 | 追根因解决 | 返回具名阶段、写集和撤销结果 |
| 同上 | 110 | 闭合失败分支 | B12 | 本函数内具体指令 | 分支闭合 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 111 | 内部预期成立时按值返回根节点句柄 | RT | 本函数内具体指令 | 返回 | 无新增；既有节点保持已发布 | 当前旧结构句柄 | 不适用 | 不得把普通节点句柄等同稳定存在根身份 |
| 同上 | 112 | 闭合函数作用域；局部句柄和可选记录销毁，异常路径未捕获 | E / Z | 本函数内具体指令 | 作用域闭合 / 异常 | 局部销毁不撤销仓库结构 | 当前代码事实 | 异常为内部错误 | 统一异常安全和事务撤销 |

覆盖结论：103—112 行全部映射；项目调用边 5 条，外部边界身份 2 个、调用点 2 个，未解析调用 0。第一笔写入后的所有非成功结果均按追根因解决记录。
