# CF-L6-0214 `控制面板窗口::窗口实现::创建主窗口` 逐行代码映射表

代码版本：`bab41cae4223289e1297a5b7850f79b5b967fb06`
源码 blob：`fe9dad1eb3728c823beccf305c783be96a3df33d`
覆盖文件：`海中鱼巣/界面/控制面板窗口.cpp:454-475`
根函数：F0390 `bool 海中鱼巣::控制面板窗口::窗口实现::创建主窗口()`
配对流程图：`流程图/代码流程图/L6/CF-L6-0214_控制面板窗口创建主窗口_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`、`规范/8110_子规范_线程生命周期状态上报与控制面板线程信息_20260720.md`、`规范/8120_子规范_程序入口启动模式与运行宿主边界_20260726.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/界面/控制面板窗口.cpp` | 454 | 进入 F0390；无显式参数，借用隐式 `this` 并准备写成员 `主窗口` | S | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 455 | 开始调用 X01034，并把其 `HWND` 返回值赋给成员 `主窗口` | X02 | 外部边界 | Win32 调用 / 成员写入 | 请求建立 Win32 窗口资源并保存句柄 | 当前运行资源 | 空句柄进入追根因路径 | 非空句柄不等于后继窗口运行已完成 |
| 同上 | 456 | 绑定 X01034 扩展样式实参为 0 | X02 | 外部边界 | 实参绑定 | 无 | 当前调用配置 | 不适用 | 无 |
| 同上 | 457 | 绑定固定 `窗口类名称` 作为已注册窗口类实参 | X02 | 外部边界 | 实参绑定 | 无 | 当前调用配置 | 不适用 | 调用方已经先执行窗口类注册 |
| 同上 | 458 | 绑定固定 `窗口标题` | X02 | 外部边界 | 实参绑定 | 无 | 非权威人读材料 | 不适用 | 标题不承担机器身份 |
| 同上 | 459 | 绑定 `WS_OVERLAPPEDWINDOW | WS_CLIPCHILDREN` 窗口样式 | X02 | 外部边界 | 实参绑定 | 无 | 当前调用配置 | 不适用 | 无 |
| 同上 | 460 | 绑定水平位置为 `CW_USEDEFAULT` | X02 | 外部边界 | 实参绑定 | 无 | 当前调用配置 | 不适用 | 无 |
| 同上 | 461 | 绑定垂直位置为 `CW_USEDEFAULT` | X02 | 外部边界 | 实参绑定 | 无 | 当前调用配置 | 不适用 | 无 |
| 同上 | 462 | 绑定初始宽度为 1120 | X02 | 外部边界 | 实参绑定 | 无 | 当前调用配置 | 不适用 | 无 |
| 同上 | 463 | 绑定初始高度为 720 | X02 | 外部边界 | 实参绑定 | 无 | 当前调用配置 | 不适用 | 无 |
| 同上 | 464 | 绑定父窗口为 `nullptr`，请求顶层窗口 | X02 | 外部边界 | 实参绑定 | 无 | 当前调用配置 | 不适用 | 无 |
| 同上 | 465 | 绑定菜单句柄为 `nullptr`；菜单栏由后继函数单独创建 | X02 | 外部边界 | 实参绑定 | 无 | 当前调用配置 | 不适用 | 不宣称主窗口成功已创建菜单 |
| 同上 | 466 | 经 X01035 调用 `GetModuleHandleW(nullptr)` 取得当前模块句柄，并把未单独复核的结果绑定为 X01034 实例句柄 | X01 / X02 | 外部边界 | Win32 调用 / 实参绑定 | 无 | 当前进程材料 | X01035 空结果由 X01034 最终失败路径间接收口 | 当前未读取具名 Win32 错误码 |
| 同上 | 467 | 把当前 `窗口实现* this` 绑定为 X01034 创建参数；调用完成后返回句柄写入成员 | X02 | 外部边界 | 实参绑定 / 调用完成 / 成员写入 | 成功时保存 Win32 窗口句柄，失败时成员为空 | 当前运行资源 | 空句柄进入 B03 | Win32 可在调用期间同步进入已登记窗口过程 |
| 同上 | 468 | 判断成员 `主窗口` 是否为 `nullptr` | B03 | 本函数内具体指令 | 判断 | 无 | 当前运行资源 | 空句柄进入追根因路径 | 无 |
| 同上 | 469 | 主窗口为空时判断 `追根因错误` 是否尚未设置；已设置则保留原失败阶段 | B04 | 本函数内具体指令 | 判断 / 诊断读取 | 无 | 本地诊断状态 | 两种分支都最终返回 false | 不用通用错误覆盖同步创建过程的首个错误 |
| 同上 | 470 | 尚未标错时经 X01036 从字面量构造阶段字符串，再经 RCE0182 调用 F0393 设置追根因错误和首个失败阶段；字符串构造异常经 E09 传播 | X05 / C06 / E09 | 外部边界 / 函数调用 / 本函数内具体指令 | 字符串构造 / 诊断调用 / 异常传播 | 只写本地诊断成员，不写业务机器结构 | 人读诊断 / 当前运行结果材料 | 主窗口创建失败属于追根因解决；异常没有结构化 false 收口 | RCE0182、X01036 均已有稳定身份 |
| 同上 | 471 | 闭合“尚未标错”分支；已标错路径不调用 F0393 | B04 / C06 | 本函数内具体指令 / 函数调用 | 分支闭合 | 无新增结构 | 当前控制流 | 不适用 | 无 |
| 同上 | 472 | 主窗口为空时返回 false；F0223 据此停止菜单栏和快捷键表创建 | R07 | 本函数内具体指令 | 返回 | 无新增机器结构 | 当前局部控制结果 | 追根因解决 | 裸 bool 只在封闭局部调用链内使用 |
| 同上 | 473 | 闭合主窗口为空分支；运行时仅非空句柄路径继续 | B03 | 本函数内具体指令 | 分支闭合 | 无 | 当前控制流 | 不适用 | 无 |
| 同上 | 474 | 主窗口非空时返回 true | R08 | 本函数内具体指令 | 返回 | 保留成员中的 Win32 窗口句柄 | 当前局部控制结果 | 正常结果 | 只证明主窗口句柄形成 |
| 同上 | 475 | 闭合函数作用域；R07、R08 或 E09 已是终点，隐式 `this` 借用结束 | R07 / R08 / E09 | 本函数内具体指令 | 作用域 / 生命周期 | 无新增结构 | 当前代码事实 | 不适用 | 无 |

覆盖结论：454—475 行逐行完整映射；10 个流程节点全部被映射；唯一项目入边 E0857、唯一项目出边 RCE0182、三个外部边界 X01034—X01036和 0 个未解析项目调用均闭合。Win32 同步窗口回调继续由正式回调关系分账，不补造为 F0390 的直接项目边；未发现本批必须修改共享聚合的缺口。
