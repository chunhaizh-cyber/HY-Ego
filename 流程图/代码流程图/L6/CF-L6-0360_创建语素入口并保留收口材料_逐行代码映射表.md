# CF-L6-0360 创建语素入口并保留收口材料逐行代码映射表

代码版本：`0a93526882cb33c61c2fbb04ecad1aeaddcfe225`
工作区复核基线：`03a8b7ac099ccea83e57f2696e2290b7bcdfacaf`
源码 blob：`1355a1afd544cd44c14ee904246e9cb0778fb39b`
覆盖文件：`海中鱼巣/领域/语素服务.h:328-370`
根函数：R0573
配对流程图：`流程图/代码流程图/L6/CF-L6-0360_创建语素入口并保留收口材料_现状流程图.md`
稳定边全集：RCE0138、RCE1578、RCE1579、RCE1580、RCE1581、RCE1582、RCE1583、RCE1584、RCE1585、RCE3280、RCE3281、RCE3282、RCE3283、RCE3284、X04742、X04743、X04744

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/语素服务.h` | 328 | 定义并进入 R0573，按值绑定对应信息节点和主键 | N01 | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 329-330 | 值初始化创建结果，并把输入主键写入局部结果 | N01 | 本函数内具体指令 | 构造 / 赋值 | 仅改变局部结果 | 当前代码事实 | 不适用 | 无 |
| 同上 | 331 | 主键非零时经 RCE1578 调 F0544 查询已有节点，再经 X04742 判断有值，并经 X04744 在完整表达式末析构临时 optional | B02 / C03 / X04 | 本函数内具体指令 / 函数调用 | 判断 / 调用 / 读取 / 生命周期 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 332-333 | 已有主键占用时返回当前结果并闭合分支 | R05 | 本函数内具体指令 | 返回 / 作用域 | 无新增结构 | 当前代码事实 | 逻辑内返回 | 无 |
| 同上 | 335 | 经 RCE1579 调 F0331 创建主信息并写入局部结果 | C06 | 函数调用 | 调用 / 写入 | 创建主信息 | 当前代码事实 | 不适用 | 无 |
| 同上 | 336-338 | 经 RCE3281 调 F0565 检查主信息句柄；无效时返回空结果并闭合分支 | C07 / R24 | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 | 主信息创建失败时无可读主信息 | 当前代码事实 | 逻辑内返回 | 无 |
| 同上 | 340 | 经 RCE1581 调 F0332 创建语素节点并写入局部结果 | C08 | 函数调用 | 调用 / 写入 | 创建语素节点 | 当前代码事实 | 不适用 | 无 |
| 同上 | 341-344 | 经 RCE3280 调 F0163 检查节点句柄；无效时经 RCE1582 调 R0574 清理并返回空结果 | C09 / C10 / R24 | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 撤销 / 返回 | 撤销已记录材料 | 当前代码事实 | 逻辑内返回 | 无 |
| 同上 | 346 | 经 RCE3282 调 F0167 创建语素对应信息关系并写入局部结果 | C11 | 函数调用 | 调用 / 写入 | 创建关系 | 当前代码事实 | 不适用 | 无 |
| 同上 | 347-350 | 经 RCE1580 调 F0168 检查关系句柄；无效时清理并返回空结果 | C12 / C10 / R24 | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 撤销 / 返回 | 撤销已记录材料 | 当前代码事实 | 逻辑内返回 | 无 |
| 同上 | 352-353 | 主键非零时经 RCE3283 调 R0889 `索引仓库::绑定主键`，记录是否由本次绑定 | B13 / C14 | 本函数内具体指令 / 函数调用 | 判断 / 调用 / 写入 | 可新增主键绑定 | 当前代码事实 | 不适用 | 无 |
| 同上 | 354-357 | 主键绑定失败时清理并返回空结果 | C14 / C10 / R24 | 函数调用 / 本函数内具体指令 | 判断 / 撤销 / 返回 | 撤销已记录材料 | 当前代码事实 | 逻辑内返回 | 无 |
| 同上 | 358 | 闭合可选主键绑定分支 | B13 | 本函数内具体指令 | 分支边界 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 360 | 经 RCE1583 调一参数重载 R0728 读回语素入口类型 | C15 | 函数调用 | 调用 / 读回 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 361 | 经 RCE1584 调 F0496 读取绑定目标组，再经 RCE1585 调 R0570 判断包含对应信息节点 | C16 / C17 | 函数调用 | 调用 / 读回 / 判断 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 362 | 主键为零直接视为可读；否则经 RCE1578 两次调 F0544，经 X04742/X04743 访问 optional、经 RCE3284 调 F0051，并经 X04744 析构各已物化临时 optional | B18 / C19 / X20 / C21 | 本函数内具体指令 / 函数调用 | 判断 / 调用 / 读回 / 生命周期 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 363 | 合取入口、关系与主键读回结果 | B22 | 本函数内具体指令 | 纯计算 / 判断 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 364-367 | 读回不完整时触发诊断、清理本次材料并返回空结果 | B22 / N23 / C10 / R24 | 本函数内具体指令 / 函数调用 | 判断 / 诊断 / 撤销 / 返回 | 撤销已记录材料 | 当前代码事实 / 人读诊断 | 追根因解决 | 无 |
| 同上 | 368 | 闭合读回失败分支 | B22 | 本函数内具体指令 | 分支边界 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 369-370 | 读回完整时返回结果并闭合函数 | R25 | 本函数内具体指令 | 返回 / 作用域 | 保留已创建结构 | 当前代码事实 | 正常返回 | 无 |

覆盖结论：328—370 全部非空源码行已映射；N01—R25 全部节点可反查。
