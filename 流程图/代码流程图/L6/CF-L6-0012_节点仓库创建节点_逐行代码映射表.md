# CF-L6-0012 `节点仓库::创建节点` 逐行代码映射表

代码版本：`a48a70b2d678dea64dd8228adb4f26f0badd9506`
覆盖文件：`海中鱼巣/核心/节点仓库.cpp:53-72`
根函数：F0332 `海中鱼巣::节点句柄 海中鱼巣::节点仓库::创建节点(海中鱼巣::节点类型 类型, 海中鱼巣::主信息句柄 主信息)`
配对流程图：`流程图/代码流程图/L6/CF-L6-0012_节点仓库创建节点_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`、`规范/4020_子规范_领域类型化数据记录与组合读取投影边界.md`、`规范/4040_子规范_不透明结构事务候选确认撤销与最后发布.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/节点仓库.cpp` | 53 | 进入旧节点仓库可写成员函数，绑定按值类型、按值主信息句柄和隐式 `this` | S | 本函数内具体指令 | 入口 / 借用 | 无 | 当前实现事实 | 不适用 | 不把旧主信息锚点扩大为节点直接身份 |
| 同上 | 54 | 调用 F0336 判断事务接线是否完整接域并选择路径 | C01 / B02 | 函数调用 / 本函数内具体指令 | 判断 / 分支 | 无 | 当前接线事实读取 | 不适用 | 保持接域与未接域分账 |
| 同上 | 55 | 接域时经 U0009 运行期函数指针取得共享许可并形成局部许可对象 | U03 | 函数调用 | 许可取得 / 局部构造 | 无 | 事务协调材料 | 函数指针异常向上传播 | 不伪连静态唯一目标 |
| 同上 | 56 | 调用 F0338 判断许可有效；有效时调用 F0339 取得令牌并调用 F0623，失效时形成零节点句柄 | C04 / B05 / C06 / C07 / RF0 | 函数调用 / 本函数内具体指令 | 判断 / 委托写入 / 返回对象形成 | F0623 可能写旧节点仓库 | 旧结构写入 | 许可无效或 F0162 传入零主信息为旧兼容拒绝；仅已取得非零主信息且同代次前置成立后仍返回零时为内部错误 | 后继使用结构化写结果和具名失败 |
| 同上 | 56—57 | 返回表达式完成或异常展开后调用 F0345 析构许可；正常路径再返回已形成结果，异常路径传播原异常 | C08N / C08E / R09 / XE | 函数调用 / 本函数内具体指令 | 资源释放 / 返回 / 异常展开 | 释放共享许可 | 事务资源事实 | 不适用 | 保留返回对象先形成、许可后释放和异常传播顺序 |
| 同上 | 58 | 未接域时先调用 F0624 读取主信息有效性；有效时再调用 F0625 判断类型白名单，任一失败形成零句柄 | C10 / B11 / RF1 / C12 / B13 / RF2 | 函数调用 / 本函数内具体指令 | 读取 / 短路判断 / 返回 | 无节点表写入；读取旧主信息仓 | 当前实现事实 | 通用无效输入或 F0162 传入零主信息为旧兼容拒绝；F0273 已验证非零共用主信息与基础信息类型后失败，或 F0162 已取得非零主信息且同代次前置成立后失败，才为内部错误 | 以现行稳定身份和类型 ABI 做结构化准入 |
| 同上 | 59—60 | 无效主信息或当前类型白名单拒绝时返回零节点句柄并闭合条件块 | RF1 / RF2 / Z | 本函数内具体指令 | 返回 / 分支闭合 | 无 | 当前实现事实 | 按调用语境分账 | 不把所有零句柄统一解释为普通拒绝 |
| 同上 | 61 | 前置通过后调用 X0364 构造 `unique_lock` 并独占本仓锁；构造失败零节点表写入 | X14 | 函数调用 | 锁 / 资源取得 | 无 | 物理同步 | 异常传播 | 不把本仓锁解释为跨仓事务 |
| 同上 | 62 | 读取下一节点编号旧值并执行无上限检查的后增 | N15 | 本函数内具体指令 | 编号分配 / 写入 | 推进旧节点编号高水位 | 旧仓库身份事实 | 回绕未拒绝 | 使用稳定主键签发并拒绝回绕 |
| 同上 | 63 | 默认构造局部节点记录 | N16 | 本函数内具体指令 | 局部构造 | 无可读仓库变化 | 临时局部值 | 不适用 | 不把临时默认值发布 |
| 同上 | 64 | 把分配编号写入局部记录 | N17 | 本函数内具体指令 | 局部写入 | 无可读仓库变化 | 临时局部值 | 不适用 | 无 |
| 同上 | 65 | 把入口类型写入局部记录 | N18 | 本函数内具体指令 | 局部写入 | 无可读仓库变化 | 临时局部值 | 不适用 | 类型 0 不得正常发布，16—19 不得误拒绝 |
| 同上 | 66 | 把旧主信息句柄写入局部节点记录作为通用锚点 | N19 | 本函数内具体指令 | 局部写入 | 无可读仓库变化 | 已退出结构候选 | 不适用 | 删除通用主信息锚点，写稳定主键与领域类型化记录 |
| 同上 | 67 | 把局部记录版本写为 1 | N20 | 本函数内具体指令 | 局部写入 | 无可读仓库变化 | 临时局部值 | 不适用 | 无 |
| 同上 | 68 | 把局部记录状态写为有效 | N21 | 本函数内具体指令 | 局部写入 | 无可读仓库变化 | 临时局部值 | 不适用 | 发布前应保持不可读候选 |
| 同上 | 69 | 读取下一创建序号旧值、无上限检查后增并写入局部记录 | N22 | 本函数内具体指令 | 序号分配 / 局部写入 | 推进旧创建序号高水位 | 旧仓库排序事实 | 回绕未拒绝 | 入口拒绝回绕并保持稳定排序 |
| 同上 | 70 | 调用 X0364 `节点表_.emplace(编号,记录)` 复制插入旧记录并忽略是否实际插入 | X23 | 函数调用 | 容器写入 / 发布 | 旧节点记录可能立即可读 | 已退出旧结构事实 | 重复键或异常为内部错误 | 采用候选确认、结果复核和最后发布 |
| 同上 | 71 | 由仓库编号、编号和局部记录版本形成节点句柄返回对象 | R24 | 本函数内具体指令 | 结果构造 | 无新增写入 | 旧句柄材料 | 不适用 | 返回前读回身份、版本、类型和状态 |
| 同上 | 71—72 | 正常返回或异常展开时调用 X0364 析构局部记录和锁；正常路径释放锁后按值返回句柄，异常路径传播原异常 | X25N / X25E / R26 / XE / Z | 函数调用 / 本函数内具体指令 | 生命周期 / 解锁 / 返回 / 异常展开 | 无新增写入 | 当前实现事实 | 不适用 | 不把返回句柄解释为跨结构完成 |

覆盖结论：53—72 行全部映射；项目被调函数 7 个、调用点 7 个，外部边界 X0364 一个（聚合 5 个锁 / 容器 / 值生命周期操作），未解析调用 U0009 一个。E0635 / E1305 入边与 E1677—E1683 出边双向闭合。
