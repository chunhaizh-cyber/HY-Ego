# CF-L6-0046 `二次特征服务::创建关系概念根` 逐行代码映射表

代码版本：`a9fd8ba33963f8d177fae753e5b01cea0073d407`
覆盖文件：`海中鱼巣/领域/二次特征服务.h:49-60`
根函数：F0367 `海中鱼巣::节点句柄 海中鱼巣::二次特征服务::创建关系概念根()`
配对流程图：`流程图/代码流程图/L6/CF-L6-0046_二次特征服务创建关系概念根_现状流程图.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/二次特征服务.h` | 49 | 进入成员函数，绑定隐式 `this` 及三个非拥有仓库引用；无显式参数 | S | 本函数内具体指令 | 入口 / 参数绑定 | 无 | 当前代码事实 | 不适用 | 最终设计需冻结结构化创建结果 |
| 同上 | 50 | 经 RCE0147 调用 R0206 创建二次特征，并按值绑定返回的根节点句柄 | C01 / N02 | 函数调用 / 本函数内具体指令 | 结构写入 / 局部绑定 | R0206 可能创建旧主信息和二次特征节点 | 当前仓库结构 | 后续不成立属于写后内部错误 | 使用统一候选事务和精确撤销 |
| 同上 | 51 | 经 RCE1718 调用 F0190 读取根节点；即使根节点句柄无效也调用，并绑定 optional 记录 | C03 / N04 | 函数调用 / 本函数内具体指令 | 读取 / 局部绑定 | 无新增 | 节点仓当前读取 | 无记录进入后验失败 | 将创建结果与读回错误机械区分 |
| 同上 | 52 | 经 X02281 调用 `optional.has_value()`；开始按左到右短路形成传给 F0184 的条件，记录不存在时跳过后续三项 | X05 / B06 / N15 | 外部边界 / 本函数内具体指令 | 读取 / 判断 / 短路 | 无 | optional 局部材料 | 创建调用后的记录缺失为内部错误 | 保留写后追根因语义 |
| 同上 | 53 | 仅记录存在时经 X02282 `operator->` 读取类型，并判断是否为 `节点类型::二次特征` | X07 / B08 / N15 | 外部边界 / 本函数内具体指令 | 读取 / 判断 / 短路 | 无 | 节点记录投影 | 类型不符为内部错误 | 读回稳定身份、版本及域记录 |
| 同上 | 54 | 仅前两项成立时经 X02282 `operator->` 取得旧主信息句柄，再经 RCE1719 调用 F0624 检查其形态有效性 | X09 / C10 / B11 / N15 | 外部边界 / 函数调用 / 本函数内具体指令 | 读取 / 判断 / 短路 | 无 | 旧主信息结构 | 无效为内部错误 | 退出旧主信息并改用节点直接身份 |
| 同上 | 55 | 仅前三项成立时经 RCE0149 调用 R0204 形成过滤后的组成项临时向量，再经 X02283 调用 `.empty()`；语句结束析构临时向量 | C12 / X13 / B14 / N15 | 函数调用 / 外部边界 / 本函数内具体指令 | 读取 / 判断 / 临时对象生命周期 | 无 | 非持久值式投影 | 非空进入内部检查失败 | 直接审计正式关系角色与无效引用 |
| 同上 | 56 | 绑定固定说明字符串；在完整合取求值后经 RCE0148 调用 F0184，并对返回 bool 取反作为 `if` 条件 | C16 / B17 | 函数调用 / 本函数内具体指令 | 诊断 / 判断 | 无 | 人读诊断与局部控制结果 | 检查为 false 进入内部错误返回 | 强类型返回错误类别与原因 |
| 同上 | 57 | 检查不通过时返回默认无效节点句柄；不撤销 R0206 可能形成的结构 | RF | 本函数内具体指令 | 返回 | 已形成结构可能残留 | 当前代码结果材料 | 追根因解决 | 失败前精确撤销或隔离 |
| 同上 | 58 | 闭合后验失败分支 | B17 | 本函数内具体指令 | 分支闭合 | 无新增 | 当前控制流 | 不适用 | 无 |
| 同上 | 59 | 检查通过时按值返回根节点句柄 | RT | 本函数内具体指令 | 返回 | 无新增；既有结构继续可读 | 当前代码结果材料 | 不适用 | 返回完整发布回执而非裸句柄 |
| 同上 | 60 | 闭合函数作用域，销毁局部句柄和 optional；仓库结构不随局部析构撤销 | Z | 本函数内具体指令 | 作用域 / 生命周期 | 无新增 | 当前代码事实 | 不适用 | 明确仓库结构生命周期 |

覆盖结论：49—60 行全部映射；项目调用点 5 个，唯一被调身份 5 个；外部边界 X02281—X02283 共 3 类，未解析项目调用 0。RCE0147、RCE1718、RCE1719、RCE0149、RCE0148 的调用顺序与 `&&` 短路条件已逐项闭合。
