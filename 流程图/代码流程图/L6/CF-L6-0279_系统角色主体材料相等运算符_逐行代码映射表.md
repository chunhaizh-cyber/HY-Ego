# CF-L6-0279 `系统角色主体材料::operator==` 逐行代码映射表

代码版本：`747223647b2cc5796db66a048009d78fa4d96ed7`
源码 blob：`c26b12e291f7192b887ef01efce9e6dc157e9cef`
覆盖文件：`海中鱼巣/领域/系统角色清单.数据.h:160-175,212`
根函数：F0456 `bool 海中鱼巣::系统角色主体材料::operator==(const 海中鱼巣::系统角色主体材料&) const`
配对流程图：`流程图/代码流程图/L6/CF-L6-0279_系统角色主体材料相等运算符_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`、`规范/7130_子规范_自我内部世界成员特征与事实读取投影.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/系统角色清单.数据.h` | 160、212 | 定义无基类聚合的显式 defaulted const 等值函数；只读绑定 `this` 与未命名右实参 | S | 本函数内具体指令 | 函数入口 / 生命周期 | 无 | 当前代码事实 | 不适用 | 无 |
| 同上 | 161 | 调用 R0613 比较 `世界根`；false 短路 | C01、RF、E | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 / 异常传播 | 无 | 值式身份材料 | 值对象不等 | 无 |
| 同上 | 162 | 前项相等后调用 R0613 比较 `自我场景` | C02、RF、E | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 / 异常传播 | 无 | 值式身份材料 | 值对象不等 | 无 |
| 同上 | 163 | 前项相等后调用 R0613 比较 `自我存在` | C03、RF、E | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 / 异常传播 | 无 | 值式身份材料 | 值对象不等 | 无 |
| 同上 | 164 | 前项相等后调用 F0457 比较 `场景接纳自我关系` | C04、RF、E | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 / 异常传播 | 无 | 值式关系材料 | 值对象不等 | 无 |
| 同上 | 165 | 前项相等后调用 R0614 比较 `安全根需求` | C05、RF、E | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 / 异常传播 | 无 | 值式根需求材料 | 值对象不等 | 无 |
| 同上 | 166 | 前项相等后调用 R0614 比较 `服务根需求` | C06、RF、E | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 / 异常传播 | 无 | 值式根需求材料 | 值对象不等 | 无 |
| 同上 | 167 | 前项相等后调用 R0613 比较 `概念存在根` | C07、RF、E | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 / 异常传播 | 无 | 值式身份材料 | 值对象不等 | 无 |
| 同上 | 168 | 前项相等后调用 R0613 比较 `概念动态根` | C08、RF、E | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 / 异常传播 | 无 | 值式身份材料 | 值对象不等 | 无 |
| 同上 | 169 | 前项相等后调用 R0613 比较 `概念关系根` | C09、RF、E | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 / 异常传播 | 无 | 值式身份材料 | 值对象不等 | 无 |
| 同上 | 170 | 前项相等后调用 R0613 比较 `概念因果根` | C10、RF、E | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 / 异常传播 | 无 | 值式身份材料 | 值对象不等 | 无 |
| 同上 | 171 | 前项相等后以内建 `std::int64_t ==` 比较 `动态根材料` | N11、RF | 本函数内具体指令 | 读取 / 判断 / 返回 | 无 | 值式数值材料 | 值对象不等 | 无 |
| 同上 | 172 | 前项相等后调用 R0613 比较 `方法登记根` | C12、RF、E | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 / 异常传播 | 无 | 值式身份材料 | 值对象不等 | 无 |
| 同上 | 173 | 前项相等后调用 R0613 比较 `方法登记角色状态` | C13、RF、E | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 / 异常传播 | 无 | 值式身份材料 | 值对象不等 | 无 |
| 同上 | 174 | 前项相等后调用 R0613 比较 `方法活跃状态` | C14、RF、E | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 / 异常传播 | 无 | 值式身份材料 | 值对象不等 | 无 |
| 同上 | 175、212 | 前项相等后调用 R0613 比较 `方法失效状态`；false 短路，true 使 defaulted 函数返回 true | C15、RF、RT、E | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 / 异常传播 | 无 | 值式身份材料 | 值对象不等或全部相等 | 无 |

## 项目调用点汇总

| 源码位点 | 稳定边 | 被调目标 | 静态次数 | 参数绑定 | 返回绑定 | 调用条件 |
| --- | --- | --- | --- | --- | --- | --- |
| `系统角色清单.数据.h:161-163,167-170,172-175,212` | RCE1762 | R0613 `系统角色身份材料::operator==(...) const` | 11 | 两侧同名身份成员分别绑定为被调 `this` 与右实参 | 对应身份成员相等布尔值 | 首成员必达；其余仅前序成员全部相等 |
| `系统角色清单.数据.h:164,212` | RCE1763 | F0457 `系统角色关系材料::operator==(...) const` | 1 | 两侧 `场景接纳自我关系` | 关系材料相等布尔值 | 前三个身份成员相等 |
| `系统角色清单.数据.h:165-166,212` | RCE1764 | R0614 `系统根需求角色材料::operator==(...) const` | 2 | 两侧安全 / 服务根需求同名成员 | 根需求材料相等布尔值 | 各自前序成员全部相等 |

覆盖结论：160—175 行直接成员声明与 212 行显式 defaulted 定义全部映射；S、C01—C15、N11、RF、RT、E 共 19 个流程节点全部回指。项目入边 E0949、出边 RCE1762—RCE1764 闭合；无外部边界或未解析调用。
