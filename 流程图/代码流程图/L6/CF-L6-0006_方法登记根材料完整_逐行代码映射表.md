# CF-L6-0006 `方法登记根材料::完整` 逐行代码映射表

代码版本：`a48a70b2d678dea64dd8228adb4f26f0badd9506`
覆盖文件：`海中鱼巣/领域/方法服务.h:137-145`
根函数：F0326 `bool 海中鱼巣::方法登记根材料::完整() const`
配对流程图：`流程图/代码流程图/L6/CF-L6-0006_方法登记根材料完整_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/3300_根规范_方法_20260720.md`、`规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`、`规范/5300_子规范_方法登记选择与执行规则_20260720.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/方法服务.h` | 137 | 进入 const 值式谓词并绑定隐式 `this` 只读借用 | S | 本函数内具体指令 | 入口 / 借用 | 无 | 当前实现事实 | 不适用 | 不把局部谓词扩大为正式完整性 |
| 同上 | 138 | 判断 `稳定非名称键 != 0`；为假时短路返回 `false` | B01 / RF | 本函数内具体指令 | 判断 / 返回 | 无 | 当前实现事实 | 候选拒绝或内部错误依调用语境裁决 | 迁移时改用正式稳定主键合同 |
| 同上 | 139 | 调用 F0163 检查登记根句柄三分量非零并判断结果 | C02 / B03 / RF | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 | 无 | 当前实现事实 | 同上 | 后继另行验证节点存在和类型 |
| 同上 | 140 | 仅前项成立时调用 F0163 检查活跃状态句柄并判断 | C04 / B05 / RF | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 | 无 | 当前实现事实 | 同上 | 不把句柄有效解释为状态事实完整 |
| 同上 | 141 | 仅前项成立时调用 F0163 检查失效状态句柄并判断 | C06 / B07 / RF | 函数调用 / 本函数内具体指令 | 调用 / 判断 / 返回 | 无 | 当前实现事实 | 同上 | 同上 |
| 同上 | 142 | 调用 F0051 比较登记根与活跃状态完整句柄并要求不相等 | C08 / B09 / RF | 函数调用 / 本函数内具体指令 | 调用 / 比较 / 返回 | 无 | 当前实现事实 | 同上 | 不把不等解释为角色关系存在 |
| 同上 | 143 | 调用 F0051 比较登记根与失效状态完整句柄并要求不相等 | C10 / B11 / RF | 函数调用 / 本函数内具体指令 | 调用 / 比较 / 返回 | 无 | 当前实现事实 | 同上 | 同上 |
| 同上 | 144 | 调用 F0051 比较活跃与失效状态完整句柄；不等时返回 `true`，相等时返回 `false` | C12 / B13 / RT / RF | 函数调用 / 本函数内具体指令 | 调用 / 比较 / 返回 | 无 | 当前实现事实 | 同上 | 正式生命周期完整性由关系 23 和实例状态事实裁决 |
| 同上 | 145 | 闭合函数体；不保存借用，不产生结构写入 | Z | 本函数内具体指令 | 作用域闭合 | 无 | 当前实现事实 | 不适用 | 无 |

覆盖结论：137—145 行全部映射；项目被调函数 2 个唯一身份、6 个调用点，外部边界 0，未解析调用 0。E0625 入边与 E1652—E1657 出边双向闭合。
