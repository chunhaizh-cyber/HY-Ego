# CF-L6-0224 `结构事务协调器::生成接线` 标记撤销失败隔离 lambda 逐行代码映射表

代码版本：`ff1d9bc893867acf8a9b3902e860fee85a524e14`
源码 blob：`946512e4b88d29bbf6d43eb07b02823471489dae`
覆盖文件：`海中鱼巣/核心/协调.结构事务.ixx:156`
根函数：F0401 `bool 海中鱼巣::结构事务协调器::生成接线()::<lambda@协调.结构事务.ixx:156>::operator()(const std::shared_ptr<void>& 状态, const 海中鱼巣::结构事务令牌& 令牌) const`
配对流程图：`流程图/代码流程图/L6/CF-L6-0224_结构事务接线标记撤销失败隔离lambda_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/4040_子规范_不透明结构事务候选确认撤销与最后发布.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/核心/协调.结构事务.ixx` | 156 | 进入无捕获 F0401，const 引用借用 `状态` 与 `令牌`；经 RCE0198 调用项目函数 R0099 `标记撤销失败隔离(状态, 令牌)`，原样返回 bool 并结束两个借用。R0099 仅在状态可转换且独占令牌验证通过时，在活动锁内把运行期状态 `已隔离` 写为 true；否则返回 false 且不执行该写入。第 156 行没有标准库调用，X01072 与该项目调用重复 | S / C01 / R02 | 本函数内具体指令 / 函数调用 | 参数绑定 / 项目函数调用 / 返回 | F0401 自身只转发；R0099 成功时写运行期事务域隔离标志，失败时不写；不直接改动业务节点、关系、索引或领域记录 | 当前结构事务接线实现事实 | true 表示隔离标志已写；R0106 已通过许可与回调前置后仍得到 false，属于撤销失败收口中的隔离失败，沿状态、令牌和接线身份追根因 | X01072 是 RCE0198 的重复误分类外部项，不画第二节点；本批已从共享聚合删除 |

覆盖结论：源码第 156 行完整映射为入口借用、RCE0198 项目调用和 bool 原样返回三个节点；E0875 绑定入边、RCE0541 消费入边和 RCE0198 项目出边闭合，未发现缺失直接项目边。X01072 是同一 R0099 项目调用的重复误分类外部边界，本表不把它计为真实外部调用或独立执行节点。
