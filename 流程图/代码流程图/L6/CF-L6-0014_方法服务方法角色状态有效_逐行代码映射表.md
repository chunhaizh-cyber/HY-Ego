# CF-L6-0014 `方法服务::方法角色状态有效` 逐行代码映射表

代码版本：`a48a70b2d678dea64dd8228adb4f26f0badd9506`
覆盖文件：`海中鱼巣/领域/方法服务.h:1763-1770`
根函数：F0334 `bool 海中鱼巣::方法服务::方法角色状态有效(海中鱼巣::方法角色状态 角色) const`
配对流程图：`流程图/代码流程图/L6/CF-L6-0014_方法服务方法角色状态有效_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/3300_根规范_方法_20260720.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`、`规范/5300_子规范_方法登记选择与执行规则_20260720.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/方法服务.h` | 1763 | 进入 const 方法服务成员函数，按值绑定角色；隐式 `this` 不被函数体读取 | S | 本函数内具体指令 | 入口 / 借用 | 无 | 当前实现事实 | 不适用 | 保持纯值谓词，不把 bool 持久化为角色事实 |
| 同上 | 1764 | 比较角色是否为 `方法首(1)`；相等时由 `||` 短路返回 `true` | B01 / RT | 本函数内具体指令 | 判断 / 返回 | 无 | 当前枚举值判断 | 不适用 | 无 |
| 同上 | 1765 | 仅前项不等时比较 `方法条件(2)`；相等返回 `true` | B02 / RT | 本函数内具体指令 | 判断 / 返回 | 无 | 当前枚举值判断 | 不适用 | 无 |
| 同上 | 1766 | 仅前两项不等时比较 `方法结果(3)`；相等返回 `true` | B03 / RT | 本函数内具体指令 | 判断 / 返回 | 无 | 当前枚举值判断 | 不适用 | 无 |
| 同上 | 1767 | 仅前三项不等时比较 `条件结果对(4)`；相等返回 `true` | B04 / RT | 本函数内具体指令 | 判断 / 返回 | 无 | 当前枚举值判断 | 不适用 | 无 |
| 同上 | 1768 | 仅前四项不等时比较 `动作入口(5)`；相等返回 `true` | B05 / RT | 本函数内具体指令 | 判断 / 返回 | 无 | 当前枚举值判断 | 不适用 | 无 |
| 同上 | 1769 | 仅前五项不等时比较 `方法登记根(6)`；相等返回 `true`，不等返回 `false` | B06 / RT / RF | 本函数内具体指令 | 判断 / 返回 | 无 | 当前枚举值判断 | 普通未知值为写前拒绝或只读未定义；强前置语境为内部错误 | 保持调用语境分账 |
| 同上 | 1770 | 闭合函数体；没有局部资源需要释放 | RT / RF | 本函数内具体指令 | 作用域闭合 | 无 | 当前实现事实 | 不适用 | 无 |

覆盖结论：1763—1770 行全部映射；六个具名枚举比较按源码左到右短路。项目被调函数、外部边界和未解析调用均为 0；E0637、E1661 两条入边闭合。
