# CF-L6-0015 `状态服务::创建状态节点` 逐行代码映射表

代码版本：`a48a70b2d678dea64dd8228adb4f26f0badd9506`
覆盖文件：`海中鱼巣/领域/状态服务.h:296-310`
根函数：F0335 `海中鱼巣::节点句柄 海中鱼巣::状态服务::创建状态节点(std::int64_t 状态值, std::optional<std::uint64_t> 发生时间戳 = std::nullopt)`
配对流程图：`流程图/代码流程图/L6/CF-L6-0015_状态服务创建状态节点_现状流程图.md`
依据：`规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`、`规范/0600_代码函数流程图应用与维护规范.md`、`规范/1160_根规范_状态节点_20260720.md`、`规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md`、`规范/4020_子规范_领域类型化数据记录与组合读取投影边界.md`、`规范/4040_子规范_不透明结构事务候选确认撤销与最后发布.md`、`规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md`、`规范/4210_子规范_动态信息分层获取与聚合_20260720.md`

| 文件 | 行号 | 代码行为 | 流程图节点 | 节点类型 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `海中鱼巣/领域/状态服务.h` | 296 | 进入私有成员函数，按值绑定 I64 状态值、可选时间戳和可写状态服务隐式 `this`；默认实参在调用点形成 | S | 本函数内具体指令 | 入口 / 借用 / 局部值 | 无 | 当前实现事实 | 不适用 | 不把默认 `nullopt` 伪写成本体调用 |
| 同上 | 297 | 调用 X0366 判断时间戳是否有值；有值才取值并调用 F0626，随后判断校验结果 | X01 / B02 / X03 / C04 / B05 | 函数调用 / 本函数内具体指令 | 可选值访问 / 调用 / 判断 | 无 | 输入材料读取 | 不适用 | 保持 `has_value` 对 `value` 的短路保护 |
| 同上 | 298—299 | 有时间戳但 F0626 返回假时形成零节点句柄并返回；局部值析构 | RU0 / X18 / Z | 本函数内具体指令 / 函数调用 | 写前返回 / 生命周期 | 零结构变化 | 当前实现结果 | 防御性写前返回；强前置调用语境命中时追根因 | 后继使用具名强类型拒绝或内部错误 |
| 同上 | 300 | 调用 F0331 创建旧主信息并按值保存句柄；不检查零句柄 | C06 | 函数调用 | 旧结构创建 / 局部保存 | 可能立即发布旧主信息 | 已退出旧结构事实 | 零句柄会流入后继写入 | 状态节点、领域记录和关系应在同一事务创建 |
| 同上 | 301 | 先调用 F0627 写零号状态值槽，再无条件调用 F0184 诊断检查写入结果 | C07 / C08 / B09 | 函数调用 / 本函数内具体指令 | 旧值写入 / 诊断 / 判断 | 可能写旧主信息零号槽 | 已退出旧结构事实 | 前置通过后失败为内部逻辑错误 | 失败必须撤销本次全部候选 |
| 同上 | 302—303 | 状态值写入检查不通过时返回零句柄；已创建主信息不撤销 | RU1 / X18 / Z | 本函数内具体指令 / 函数调用 | 返回 / 生命周期 | 旧主信息可能保持可读 | 可读半结构 | 内部逻辑错误 | 不得用空句柄掩盖半结构 |
| 同上 | 304 | 调用 X0366 再次判断是否提供时间戳；无值时跳过时间槽写入 | X10 / B11 | 函数调用 / 本函数内具体指令 | 可选值访问 / 分支 | 无 | 输入材料读取 | 不适用 | 无 |
| 同上 | 305—306 | 有值时调用 X0366 取值并转 I64，调用 F0628 写槽 1，再调用 F0184 检查结果 | X12 / C13 / C14 / B15 | 函数调用 / 本函数内具体指令 | 转换 / 旧值写入 / 诊断 / 判断 | 可能写旧主信息一号槽 | 已退出旧结构事实 | 前置通过后失败为内部逻辑错误 | 时间进入状态域类型化记录，不进入通用槽 |
| 同上 | 307—308 | 时间槽检查不通过时返回零句柄；主信息及零号槽不撤销 | RU2 / X18 / Z | 本函数内具体指令 / 函数调用 | 返回 / 生命周期 | 可读旧主信息和值槽残留 | 可读半结构 | 内部逻辑错误 | 按 4040 精确撤销写集 |
| 同上 | 309 | 调用 F0332 以类型 `状态(12)` 和旧主信息创建节点，原样形成返回结果；零句柄不触发补偿 | C16 / R17 | 函数调用 / 本函数内具体指令 | 节点创建 / 返回对象形成 | 可能立即发布旧节点 | 已退出旧结构事实 | 前置通过后零句柄为内部逻辑错误 | 使用稳定主键、类型化记录和关系 19 完整发布 |
| 同上 | 309—310 | 正常返回或异常展开时调用 X0366 销毁局部可选值与句柄；正常返回已形成结果，异常传播原异常 | X18 / XE / Z | 函数调用 / 本函数内具体指令 | 生命周期 / 返回 / 异常传播 | 不回滚已写仓库事实 | 当前实现事实 | 写入后异常为内部逻辑错误 | 事务参与者共同撤销或最后发布 |

覆盖结论：296—310 行全部映射；项目被调函数 6 个、调用点 7 个，外部边界 X0366 一个（聚合 7 个 `optional` / 句柄 / 转换 / 生命周期操作），未解析调用 0。E0642 入边与 E1685—E1691 出边双向闭合；137 行不可达调用方只作具名排除证据。
