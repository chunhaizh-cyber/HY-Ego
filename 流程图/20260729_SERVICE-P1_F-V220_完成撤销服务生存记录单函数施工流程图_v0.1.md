# SERVICE-P1 F-V220 `服务生存维护记录参与者::完成撤销` 单函数施工流程图 v0.1

图类型：目标施工流程图
状态：现行设计依据；私有 override 待 #379 实现，不表示代码已存在
归属模块：`海中鱼巣.领域.参与者.服务生存维护记录`
归属文件：`海中鱼巣/领域/参与者.服务生存维护记录.ixx`
唯一根函数：F-V220
完整签名：

```cpp
节点直接身份结构写入结果
服务生存维护记录参与者::完成撤销() noexcept override;
```

本函数由核心执行器在准备或确认失败后逆序调用，只清理本参与者的未发布候选和私有阶段，不回删任何已发布事实。依据 0050、CORE-C1/v1 与服务详细设计 v0.3 第 7.3—7.4、10 节。

## 流程图

```mermaid
flowchart TD
    S["S【本函数内具体指令】读取参与者私有阶段及F-V217记录仓独占锁所有权；已持有则沿用，准备前/准备失败未持有则不重新取得"]
    B01{"B01【本函数内具体指令】阶段已经是新候选已发布或幂等完成？"}
    R01["R01【本函数内具体指令】保持已发布容器不变，释放仍持有的独占锁并返回内部不一致"]
    B02{"B02【本函数内具体指令】阶段为新候选已准备或新候选已确认待发布？"}
    N03["N03【本函数内具体指令】清空待发布合同/治理node、幂等读回optional及对应候选暂态"]
    N04["N04【本函数内具体指令】把阶段记为候选已撤销"]
    R04["R04【本函数内具体指令】释放参与者持有的独占锁并返回候选已撤销"]
    B05{"B05【本函数内具体指令】阶段为幂等已准备、幂等已确认或尚未准备？"}
    N06["N06【本函数内具体指令】清理幂等读回optional、空node和对应候选暂态并把阶段记为候选已撤销"]
    R06["R06【本函数内具体指令】释放仍持有的独占锁并返回候选已撤销"]
    B07{"B07【本函数内具体指令】阶段已经为候选已撤销且私有槽为空？"}
    R07["R07【本函数内具体指令】确认无锁泄漏并幂等返回候选已撤销"]
    R08["R08【本函数内具体指令】清理可安全识别的私有半候选，保持公开容器不变，释放仍持有的独占锁并返回内部不一致"]
    S --> B01
    B01 -- "是" --> R01
    B01 -- "否" --> B02
    B02 -- "是" --> N03 --> N04 --> R04
    B02 -- "否" --> B05
    B05 -- "是" --> N06 --> R06
    B05 -- "否" --> B07
    B07 -- "是" --> R07
    B07 -- "否" --> R08
```

## 节点合同

| 节点 | 类型 | 输入 | 输出 | 事实/锁边界 |
| --- | --- | --- | --- | --- |
| S—B02 | 本函数内具体指令 | 私有阶段、可选已持有锁 | 撤销准入 | 复用 F-V217 的锁；不重新获取 |
| N03—R04 | 本函数内具体指令 | 新候选私有槽 | 已撤销 | 零公开写入 |
| B05—R07 | 本函数内具体指令 | 幂等/空/已撤销阶段 | 幂等撤销结果 | 零公开写入 |
| R01/R08 | 本函数内具体指令 | 已发布或非法阶段 | 内部不一致 | 绝不回删已发布事实 |

## 实现约束

1. 撤销必须可重复收口；已经撤销且私有槽为空时继续返回 `候选已撤销`。
2. 已发布后调用撤销固定为 `内部不一致`，不得删除、降版或恢复公开记录。
3. 本函数是准备/确认失败后的唯一正常锁释放点；从未准备的参与者没有锁时不得为撤销重新加锁。
4. 本函数不调用外部参与者、不取得核心写会话、不写日志，不生成任何身份、版本或发布代次。
