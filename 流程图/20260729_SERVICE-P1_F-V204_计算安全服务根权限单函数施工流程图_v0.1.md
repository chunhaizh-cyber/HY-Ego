# SERVICE-P1 F-V204 `计算安全服务根权限` 单函数施工流程图 v0.1

图类型：目标施工流程图
状态：现行设计依据；函数待 #379 实现，不表示代码已存在
归属模块：`海中鱼巣.领域.算法.服务生存任务权限`
归属文件：`海中鱼巣/领域/算法.服务生存任务权限.ixx`
唯一根函数：F-V204
完整签名：

```cpp
安全服务根权限结果 计算安全服务根权限(
    const 服务生存定义快照& 定义,
    const 服务生存当前快照& 当前,
    const 安全服务根权限请求& 请求) noexcept;
```

返回 `已形成`、`权限为零` 完整载荷或无载荷拒绝。依据 6170 第 8—10 节与服务详细设计第 5、9.4 节。验证映射 SS-A15、A17。

## 流程图

```mermaid
flowchart TD
    S["S【本函数内具体指令】只读绑定定义、当前和请求"]
    C01["C01【函数调用】F-V201 复核服务生存定义(定义)"]
    B02{"B02【本函数内具体指令】身份、事实截止、当前值版本和权限规则版本兼容且当前？"}
    R02["R02【本函数内具体指令】返回入口拒绝或版本漂移且无载荷"]
    B03{"B03【本函数内具体指令】当前安全根值等于0或1？"}
    N04["N04【本函数内具体指令】写终止或休眠控制态、普通竞争不适用、两项根权限均为0"]
    R04["R04【本函数内具体指令】返回权限为零和完整载荷"]
    B05{"B05【本函数内具体指令】安全根值位于哪个阈值区间？"}
    N06["N06【本函数内具体指令】写安全根权限1000000和服务根权限0"]
    N07["N07【本函数内具体指令】用checked wide integer线性形成服务根权限1至990000并以差值形成安全根权限"]
    N08["N08【本函数内具体指令】写安全根权限10000和服务根权限990000"]
    B09{"B09【本函数内具体指令】两项权限非负且总和等于1000000？"}
    R09["R09【本函数内具体指令】返回结构拒绝且无载荷"]
    N10["N10【本函数内具体指令】形成主动运行、普通竞争适用和输入版本载荷"]
    R10["R10【本函数内具体指令】返回已形成和完整载荷"]
    S --> C01 --> B02
    B02 -- "否" --> R02
    B02 -- "是" --> B03
    B03 -- "是" --> N04 --> R04
    B03 -- "否" --> B05
    B05 -- "A不高于L" --> N06 --> B09
    B05 -- "L小于A且A小于H" --> N07 --> B09
    B05 -- "A不低于H" --> N08 --> B09
    B09 -- "否" --> R09
    B09 -- "是" --> N10 --> R10
```

版本只做门禁；A=0/1 不进入普通任务竞争；A>1 时两项根权限严格守恒。
