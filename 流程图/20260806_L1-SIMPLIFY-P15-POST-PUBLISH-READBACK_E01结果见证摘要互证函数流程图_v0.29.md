# L1-SIMPLIFY-P15 E01、G2 自检与恢复导出流程图 v0.29

更新时间：2026-08-06

依据：P15 v0.29 详细设计；正式基线 `57c2c08407f9ca0f18d679dcb4777d634846afe8`。

```mermaid
flowchart TD
 A["E01 请求"] --> B["形成 node-only RP v1"]
 B --> C["G6意图编码与provider前探测"]
 C -->|同义| D["原样返回首次结果；provider/提交 0/0"]
 C -->|未找到| E["唯一提交并形成RW/v2结果"]
 E --> F["幂等账/审计/结果见证同候选原子交换"]
 F --> G["发布后权威读回"]
 G -->|一致| H["返回首次结果"]
 G -->|不一致| I["内部不一致并隔离"]

 J["L1场景结构自检：首次初始场景请求"] --> K{"同一幂等身份与首次字段相同?"}
 K -->|是| L["幂等读回"]
 K -->|否：世界根不同或其它首次字段不同| M["G2幂等冲突"]
 M --> N["不交换候选；不提交；provider/提交 0/0"]
 N --> O["P1-S01 第7项通过"]

 P["恢复/生产调用方"] --> Q["L1事实基座服务::导出恢复材料"]
 Q --> R{"状态完整且隔离见证一致?"}
 R -->|否| S["内部不一致；无材料"]
 R -->|是| T["共享锁内复制已发布状态并确定性排序"]
 T --> U["导出材料；候选不导出"]
```

G2 异义分支只验证既有正式合同，不新增生产写入或第二解释者；旧自检第 7 项的
期望必须是 `幂等冲突`。恢复导出路径、RP/G6/RW/v1/v2 和其它 v0.28 分支保持不变。
