# SERVICE-P1 F-V202 `计算服务合同建立候选` 单函数施工流程图 v0.1

图类型：目标施工流程图
状态：现行设计依据；函数待 #379 实现，不表示代码已存在
归属模块：`海中鱼巣.领域.算法.服务合同结算`
归属文件：`海中鱼巣/领域/算法.服务合同结算.ixx`
唯一根函数：F-V202
完整签名：

```cpp
服务合同结果 计算服务合同建立候选(
    const 服务合同事实来源载荷& 合同事实,
    const 服务生存事实来源载荷& 生存事实,
    const 服务合同建立请求& 请求) noexcept;
```

返回新合同 `已结算`、同义重放 `精确重复`，或无载荷拒绝。函数只形成值式候选，不生成稳定身份、不提交事务。依据 6160 全部合同阶段与服务详细设计第 4—7、9 节。验证映射 SS-A01—A07。

## 流程图

```mermaid
flowchart TD
    S["S【本函数内具体指令】只读绑定合同事实、生存事实和请求"]
    C01["C01【函数调用】F-V201 复核服务生存定义(生存事实.服务生存定义快照)"]
    B02{"B02【本函数内具体指令】定义复核为已形成？"}
    R02["R02【本函数内具体指令】原样返回定义失败分类且无载荷"]
    B03{"B03【本函数内具体指令】请求、自我、提出者、需求、目标、范围、法规和用途组合完整？"}
    R03["R03【本函数内具体指令】返回入口拒绝且无载荷"]
    B04{"B04【本函数内具体指令】需求、合同集合、服务值、安全值、事实截止和合同规则版本兼容且当前？"}
    R04["R04【本函数内具体指令】返回版本漂移且无载荷"]
    B05{"B05【本函数内具体指令】当前生存安全根值等于0？"}
    R05["R05【本函数内具体指令】返回入口拒绝且无载荷"]
    B06{"B06【本函数内具体指令】同幂等键或同一有效来源需求已有合同？"}
    B07{"B07【本函数内具体指令】既有合同与本次请求、有效期和规则版本同义且原子预支结果完整？"}
    R07["R07【本函数内具体指令】返回入口拒绝或内部不一致且无载荷"]
    N08["N08【本函数内具体指令】形成精确重复载荷并读回既有合同、预支和当前服务值版本"]
    R08["R08【本函数内具体指令】返回精确重复和载荷"]
    N09["N09【本函数内具体指令】冻结显式有效秒或默认2592000秒并复核正值与可表示性"]
    N10["N10【本函数内具体指令】用checked wide integer计算预算B、30%预支P0和70%余款P1"]
    N11["N11【本函数内具体指令】用服务值剩余空间计算实际预支和饱和舍弃值"]
    N12["N12【本函数内具体指令】形成合同、预支结算、当前/新服务值版本和发布代次候选载荷"]
    R12["R12【本函数内具体指令】返回已结算和完整载荷"]
    R13["R13【本函数内具体指令】坏有效期、坏集合或溢出返回结构拒绝且无载荷"]
    E14["E14【本函数内具体指令】捕获分配异常并返回资源失败且无载荷"]
    S --> C01 --> B02
    B02 -- "否" --> R02
    B02 -- "是" --> B03
    B03 -- "否" --> R03
    B03 -- "是" --> B04
    B04 -- "否" --> R04
    B04 -- "是" --> B05
    B05 -- "是" --> R05
    B05 -- "否" --> B06
    B06 -- "是" --> B07
    B07 -- "否" --> R07
    B07 -- "是" --> N08 --> R08
    B06 -- "否" --> N09 --> N10 --> N11 --> N12 --> R12
    N09 -. "坏范围" .-> R13
    N10 -. "溢出" .-> R13
    N12 -. "分配异常" .-> E14
```

## 节点合同

| 节点 | 类型 | 输出 | 验证 |
| --- | --- | --- | --- |
| C01 | 函数调用 | 定义分类 | F-V201 唯一调用 |
| B03—R08 | 本函数内具体指令 | 入口/版本/终止/幂等裁决 | SS-A01—A03 |
| N09—N12 | 本函数内具体指令 | 合同和30%预支候选 | SS-A03—A07 |
| R13/E14 | 本函数内具体指令 | 无载荷失败 | 溢出与资源失败 |

合同与预支必须作为同一候选；不得出现可读的“合同已建立但预支未发布”状态。
