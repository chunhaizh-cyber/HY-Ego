# L1-SIMPLIFY-P0 三类事实读取与恢复业务流程图 v0.2

绑定计划：`L1-SIMPLIFY-P0 v0.2`

绑定规范：`4015 v0.2`、`4040`、`4070`

本图冻结当前、历史、审计、完整快照和恢复候选五条显式路径；任何路径不得把另一条路径的材料塞入结果。

```mermaid
flowchart TD
    A["L1服务接收值式请求"] --> B{"请求类型"}
    B -->|"当前节点/关系/值"| C["一把共享锁读取当前不可变状态"]
    B -->|"当前属性"| D["一把共享锁读取节点属性槽与当前值"]
    B -->|"历史事实"| E["只查稳定编码对应完整生命周期记录"]
    B -->|"审计"| F["只查幂等编码对应不可变事件组"]
    B -->|"完整快照"| G["复制当前三类事实、当前槽和永久编码占用"]
    B -->|"建立恢复候选"| H["隔离副本完整性校验"]
    B -->|"确认恢复候选"| I["独占锁复核基线代次"]
    B -->|"撤销恢复候选"| J["丢弃隔离候选"]

    C --> K{"首末事实代次相同"}
    D --> K
    K -->|"否且首次"| L["完整重试一次"]
    L --> K
    K -->|"否且已重试"| M["返回事实代次漂移，空业务载荷"]
    K -->|"是"| N["返回当前值式副本"]

    E --> O{"编码存在"}
    O -->|"否"| P["未找到，空历史载荷"]
    O -->|"是"| Q["返回当前或已退出完整事实，不返回槽/审计"]
    F --> R{"审计记录存在"}
    R -->|"否"| S["未找到，空事件组"]
    R -->|"是"| T["按事件序号返回审计组，不返回事实"]
    G --> U["返回单一事实代次的完整当前快照"]

    H --> V{"三类事实、槽、端点、占用、幂等和审计均有效"}
    V -->|"否"| W["材料不完整/内部不一致，零候选"]
    V -->|"是"| X["建立隔离恢复候选，不改当前状态"]
    I --> Y{"当前代次等于候选基线"}
    Y -->|"否"| Z["丢弃候选，事实代次漂移"]
    Y -->|"是"| AA["一次替换当前状态，发布当前代次+1"]
    AA --> AB{"发布后读回一致"}
    AB -->|"否"| AC["内部不一致并隔离，不逐项回填"]
    AB -->|"是"| AD["恢复已发布"]
    J --> AE["候选已撤销；当前状态和代次不变"]
```

## 材料边界

```mermaid
flowchart LR
    C["当前完整快照\n当前三类事实 + 当前槽 + 永久编码"] --> R["恢复材料"]
    H["历史事实组\n仅已退出记录"] --> R
    A["审计记录组\n幂等键/事件/代次"] --> R
    I["幂等账\n规范化写集 + 首次结果"] --> R
    R --> V["隔离完整性校验"]
    V --> P["确认 / 撤销恢复候选"]
```

禁止边：当前读取不得返回历史或审计；历史读取不得返回当前槽；审计不得裁决事实成功；快照不得隐藏历史 / 审计；恢复不得从索引、SQL 或材料旁路补造事实。

## 事务与完成边界

- 普通写集遵循候选、确认、逆序撤销、最后一次代次发布和发布后读回。
- 读取只复制值式材料，不把锁、仓指针、候选令牌传给调用方。
- P0 完成只证明独立 L1 DTO、服务入口、隔离恢复候选和专项矩阵进入工程；不证明任何领域迁移或旧基座删除。
