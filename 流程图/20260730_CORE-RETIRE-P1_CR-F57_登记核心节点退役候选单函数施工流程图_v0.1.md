# CR-F57 登记核心节点退役候选单函数施工流程图

日期：2026-07-30
版本：v0.1
创建侧代码基线：`57866ac5ca63235ed12da60f635d29f1aacd718b`

## 身份与边界

本图只对应底层核心候选编排函数，不承担领域类型化记录退役，也不直接取得许可、确认、撤销或发布。具名领域服务在现有写执行器回调内先让领域参与者持有自己的退役候选，再调用本函数登记关系、索引和节点核心候选，最后由调用者请求提交。

## 函数合同

```text
根函数：节点直接身份结构写入会话::登记核心节点退役候选
归属模块 / 文件 / 层级：海中鱼巣.核心.会话.节点直接身份结构写入 / 海中鱼巣/核心/会话.节点直接身份结构写入.ixx / 核心结构会话编排层
现有或待新增：待新增公开成员
完整签名：节点直接身份核心退役候选结果 节点直接身份结构写入会话::登记核心节点退役候选(const 节点直接身份核心退役规格& 规格)
参数：规格为调用期只读借用；字段为节点句柄、待失效关系句柄组、待移除索引物理键组；关系组按 `(仓库编号,关系编号,版本号)` 严格升序且相邻稳定身份 `(仓库编号,关系编号)` 不得相同；索引组按 `(所有者身份,命名域,键格式版本,探测规则版本,键值)` 字典序严格升序且无重复
返回结果：节点直接身份核心退役候选结果；含结构化操作结果和成功时的节点写后句柄
所有权：输入组归调用方；关系/索引/节点候选唯一移入会话六写集；返回只含值式结果
入口拒绝：会话不可写、节点无效、组未排序/重复、关系不属于目标节点、索引不是目标节点索引
内部错误：任一候选前置通过后读回、身份或版本不一致；函数记录首次失败，使执行器统一撤销
```

```mermaid
flowchart TD
    N01{"N01【指令-判断】会话可继续写入、节点句柄完整、关系完整句柄升序且稳定身份唯一、索引完整键升序无重复"} -- "否" --> R01["R01【指令-返回】返回入口拒绝、空写后节点；零新增写"]
    N01 -- "是" --> X00["X00【函数调用】std::optional<节点直接身份记录> 节点直接身份仓库::读取节点审计(节点句柄 节点) const：规格.节点→节点；接收目标节点审计"]
    X00 -- "完成" --> B00{"B00【指令-判断】目标节点审计存在、同仓同编号，且输入版本等于审计版本或审计为已删除并严格相邻"}
    B00 -- "否" --> E01
    B00 -- "有效" --> N02["N02【指令-赋值】关系位置=0"]
    B00 -- "已删除" --> I01["I01【指令-赋值】幂等关系位置=0"]
    I01 -- "完成" --> IB01{"IB01【指令-判断】幂等关系位置到达关系组末尾"}
    IB01 -- "否" --> IX01["IX01【函数调用】std::optional<正式关系审计材料> 正式关系仓库::读取关系审计(关系句柄 关系) const：关系组[幂等关系位置]→关系；接收普通发布审计"]
    IX01 -- "完成" --> IB02{"IB02【指令-判断】审计当前同稳定身份、状态已失效，且输入版本等于当前版本或严格相邻历史末项版本"}
    IB02 -- "否" --> E01
    IB02 -- "是" --> I02["I02【指令-推进】幂等关系位置加一"]
    I02 -- "完成" --> IB01
    IB01 -- "是" --> I03["I03【指令-赋值】幂等索引位置=0"]
    I03 -- "完成" --> IB03{"IB03【指令-判断】幂等索引位置到达索引组末尾"}
    IB03 -- "否" --> IX02["IX02【函数调用】std::optional<可重建索引记录> 节点直接身份结构写入会话::读取索引物理键(const 索引物理键& 键)：索引组[幂等索引位置]→键；接收本事务索引记录"]
    IX02 -- "完成" --> IB04{"IB04【指令-判断】本事务索引记录为空且会话索引创建/移除写集中没有该键"}
    IB04 -- "否" --> E01
    IB04 -- "是" --> I04["I04【指令-推进】幂等索引位置加一"]
    I04 -- "完成" --> IB03
    IB03 -- "是" --> IR01["IR01【指令-返回】返回幂等读回和由目标节点审计形成的当前已删除句柄；零新增写"]
    N02 -- "完成" --> B01{"B01【指令-判断】关系位置到达待失效关系组末尾"}
    B01 -- "否" --> X01["X01【函数调用】std::optional<正式关系审计材料> 正式关系仓库::读取关系审计(关系句柄 关系) const：关系组[关系位置]→关系；接收普通发布审计"]
    X01 -- "完成" --> B02{"B02【指令-判断】普通发布审计当前为有效且关联目标节点"}
    B02 -- "是" --> N02A["N02A【指令-赋值】期望写前=普通发布审计当前；期望写后版本=当前+1且状态已失效"]
    B02 -- "否" --> X01A["X01A【函数调用】std::optional<正式关系审计材料> 节点直接身份结构写入会话::读取关系审计(关系句柄 关系)：关系组[关系位置]→关系；接收本事务审计"]
    X01A -- "完成" --> B02A{"B02A【指令-判断】本事务审计当前为已失效候选写后，历史末项为关联目标节点的有效写前，且两者版本严格相邻"}
    B02A -- "否" --> E01["E01【函数调用】void 节点直接身份结构写入会话::记录失败(const 节点直接身份结构写入结果& 失败) noexcept：首个具名失败→失败；返回void"]
    B02A -- "是" --> N02B["N02B【指令-赋值】期望写前=本事务审计历史末项；期望写后=本事务审计当前"]
    N02A -- "完成" --> X02["X02【函数调用】节点直接身份带值结构写入结果<关系句柄> 节点直接身份结构写入会话::失效关系候选(关系句柄 关系)：关系组[关系位置]→关系；接收失效结果"]
    N02B -- "完成" --> X02
    X02 -- "完成" --> B03{"B03【指令-判断】失效结果成功且写后关系句柄存在"}
    B03 -- "否" --> E01
    B03 -- "是" --> X03["X03【函数调用】std::optional<正式关系审计材料> 节点直接身份结构写入会话::读取关系审计(关系句柄 关系)：失效结果.值→关系；接收写后审计"]
    X03 -- "完成" --> B04{"B04【指令-判断】写后审计当前记录状态已失效且等于期望写后；其历史末项等于期望写前"}
    B04 -- "否" --> E01
    B04 -- "是" --> N03["N03【指令-推进】关系位置加一"]
    N03 -- "完成" --> B01
    B01 -- "是" --> N04["N04【指令-赋值】索引位置=0"]
    N04 -- "完成" --> B05{"B05【指令-判断】索引位置到达待移除索引组末尾"}
    B05 -- "否" --> X04["X04【函数调用】std::optional<可重建索引记录> 节点直接身份结构写入会话::读取索引物理键(const 索引物理键& 键)：索引组[索引位置]→键；接收写前索引记录"]
    X04 -- "完成" --> B06{"B06【指令-判断】写前记录为空，或写前为完整节点目标且目标节点等于规格.节点"}
    B06 -- "否" --> E01
    B06 -- "是" --> X05["X05【函数调用】节点直接身份索引移除结果 节点直接身份结构写入会话::移除索引候选(const 索引物理键& 键)：索引组[索引位置]→键；接收移除结果"]
    X05 -- "完成" --> B07{"B07【指令-判断】移除结果已提交或同事务幂等读回、移除前记录存在且指向规格.节点；写前存在时两记录相等，写前为空时必须为同事务幂等读回"}
    B07 -- "否" --> E01
    B07 -- "是" --> N05["N05【指令-推进】索引位置加一"]
    N05 -- "完成" --> B05
    B05 -- "是" --> X06["X06【函数调用】节点直接身份带值结构写入结果<节点句柄> 节点直接身份结构写入会话::删除节点候选(节点句柄 节点)：规格.节点→节点；接收删除结果"]
    X06 -- "完成" --> B08{"B08【指令-判断】删除结果操作成功且写后节点存在；同事务重复回显既有写后节点，已发布重复回显已删除身份"}
    B08 -- "否" --> E01
    B08 -- "是" --> R02["R02【指令-返回】返回删除结果.操作和写后节点；候选继续由会话拥有"]
    E01 -- "完成" --> E02["E02【指令-返回】返回会话首次失败、空写后节点"]
```

## 节点合同

| 节点 | 类型 | 输入 / 输出 | 事实读写 | 后继条件 | 验证 |
| --- | --- | --- | --- | --- | --- |
| N01—N02 | 本函数内指令 | 值式规格 | 零写 | 输入完整 | 排序、稳定身份唯一、句柄 |
| X00/B00 | 函数调用 / 判断 | `规格.节点→节点`；`目标节点审计←optional<节点直接身份记录>` | 只读节点审计 | 有效进入候选路径；已删除进入幂等短路 | 新候选/已发布幂等分流 |
| IX01—IR01 | 函数调用 / 指令 | 关系/索引规格项及节点审计当前记录 | 只读正式发布审计和本事务索引视图 | 每项均已是目标后态 | 无变化退役不调用 CR-F12、不推进版本 |
| X01—X03/B02—B04 | 函数调用 / 判断 | 关系句柄、普通发布审计、本事务审计及期望写前/写后 | 关系失效候选写集 | 输入为写前句柄时由普通审计证明关联目标；输入为本事务写后句柄时由事务审计历史末项证明关联目标；两者均回显同一确定后态 | T10、T35、T42、T45 |
| X04—X05/B06—B07 | 函数调用 / 判断 | 索引键与写前记录 | 索引移除写集 | 本事务不命中且写前回显一致 | T20、T35、T42、T45 |
| X06/B08 | 函数调用 / 判断 | 目标节点 | 节点删除写集 | F12 最终复核不存在遗漏关系/索引 | T28—T34 |
| E01 | 函数调用 / 返回 | 首个失败 | 记录会话失败 | 执行器必须撤销 | 零半提交 |
| R02 | 本函数内指令 | 删除结果 | 值式输出 | 无条件 | 不请求提交 |

## 双向引用

- 业务节点：B06—B14。
- 直接被调图：[CR-F12](./20260730_CORE-RETIRE-P1_CR-F12_会话删除节点候选单函数施工流程图_v0.1.md)、[CR-F13](./20260730_CORE-RETIRE-P1_CR-F13_会话移除索引候选单函数施工流程图_v0.1.md)、[CR-F25](./20260730_CORE-RETIRE-P1_CR-F25_普通读取关系审计单函数施工流程图_v0.1.md)、[CR-F34](./20260730_CORE-RETIRE-P1_CR-F34_会话读取索引物理键单函数施工流程图_v0.1.md)、[CR-F58](./20260730_CORE-RETIRE-P1_CR-F58_登记关系候选单函数施工流程图_v0.1.md)（经现有 `失效关系候选`）。
- 复用现有入口：节点仓 `读取节点审计(节点句柄) const`、会话 `读取关系审计(关系句柄)`（其内部调用 CR-F26）、`失效关系候选(关系句柄)`、`记录失败(...)`。

## 关键边界

```text
本函数只编排核心节点、关系和可丢弃索引候选，不验证或退役领域类型化记录。
领域服务必须用现有写执行器的参与者组在同一事务中承载领域记录退役；没有具名领域参与者的业务节点不得把本函数当成完整业务删除许可。
本函数不调用 请求提交()，调用者只有在全部领域参与者材料也已准备后才能请求提交。
```
