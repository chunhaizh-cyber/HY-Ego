# CR-F63 执行六写集故障会话回调单函数施工流程图

版本：v0.1
代码基线：`57866ac5ca63235ed12da60f635d29f1aacd718b`

## 函数合同

```text
根函数：执行六写集故障会话回调
物理位置：海中鱼巣/核心/自检.节点直接身份结构写入.ixx，CR-F49 之前内部区
完整签名：void 执行六写集故障会话回调(六写集故障会话回调上下文& 上下文, 节点直接身份结构写入会话& 会话)
参数：一次执行期借用；场景1—6；六类具名输入字段
返回：void；六类操作和六项精确读回布尔写上下文；参与者由F49外层传执行器，回调不持有参与者
```

```mermaid
flowchart TD
    N01{"N01【指令-判断】场景值在1—6"} -- "否" --> X00["X00【函数调用】void 节点直接身份结构写入会话::记录失败(const 节点直接身份结构写入结果& 失败) noexcept：入口拒绝→失败；返回void"]
    N01 -- "场景5空写集" --> X12
    N01 -- "其它" --> X01["X01【函数调用】节点直接身份带值结构写入结果<节点句柄> 节点直接身份结构写入会话::创建节点候选(节点类型 类型,节点稳定主键 稳定主键)：上下文创建字段逐项绑定；接收节点创建操作"]
    X01 -- "完成" --> X02["X02【函数调用】节点直接身份带值结构写入结果<关系句柄> 节点直接身份结构写入会话::创建关系候选(关系类型 类型,节点句柄 源节点,节点句柄 目标节点,std::int64_t 顺序号)：上下文关系创建字段逐项绑定；接收关系创建操作"]
    X02 -- "完成" --> X03["X03【函数调用】节点直接身份带值结构写入结果<关系句柄> 节点直接身份结构写入会话::失效关系候选(关系句柄 关系)：上下文.待失效关系→关系；接收关系失效操作"]
    X03 -- "完成" --> X04["X04【函数调用】节点直接身份带值结构写入结果<可重建索引记录> 节点直接身份结构写入会话::创建索引候选(可重建索引记录 记录)：上下文.待创建索引→记录；接收索引创建操作"]
    X04 -- "完成" --> X05["X05【函数调用】节点直接身份索引移除结果 节点直接身份结构写入会话::移除索引候选(const 索引物理键& 键)：上下文.待移除索引→键；接收索引移除操作"]
    X05 -- "完成" --> X06["X06【函数调用】节点直接身份带值结构写入结果<节点句柄> 节点直接身份结构写入会话::删除节点候选(节点句柄 节点)：上下文.待删除节点→节点；接收节点删除操作"]
    X06 -- "六写集已形成" --> X07["X07【函数调用】std::optional<节点直接身份记录> 节点直接身份结构写入会话::读取节点(节点句柄 节点)：节点创建操作.值→节点；接收节点创建读回"]
    X07 -- "完成" --> X08["X08【函数调用】std::optional<正式关系审计材料> 节点直接身份结构写入会话::读取关系审计(关系句柄 关系)：关系操作.值→关系；接收关系读回"]
    X08 -- "完成" --> X09["X09【函数调用】std::optional<可重建索引记录> 节点直接身份结构写入会话::读取索引物理键(const 索引物理键& 键)：上下文索引键→键；接收索引读回"]
    X09 -- "完成" --> N02["N02【指令-赋值】按各返回材料写六项已读回布尔；场景2故意保持索引移除已读回=false"]
    N02 -- "场景3显式撤销" --> X11["X11【函数调用】void 节点直接身份结构写入会话::请求撤销() noexcept：无参数；返回void"]
    N02 -- "其它场景" --> X12["X12【函数调用】void 节点直接身份结构写入会话::请求提交() noexcept：无参数；返回void"]
    X00 -- "失败已记录" --> R01["R01【指令-返回】返回void"]
    X11 -- "撤销已请求" --> R01
    X12 -- "提交已请求" --> R01
```

调用者：CR-F49；绑定方式为 `std::bind_front(执行六写集故障会话回调,std::ref(上下文))`。场景4的撤销失败由 F49 外层参与者实现，回调本身不拥有参与者。
