# F-A101 登记特征值类型化记录候选

根函数：`特征值类型化记录候选结果 特征值类型化记录事务参与者::登记记录候选(const 特征值类型化记录规格& 规格)`

归属：`海中鱼巣.领域.参与者.特征值类型化记录` / `海中鱼巣/领域/参与者.特征值类型化记录.ixx`；待新增；输入借用至返回，参与者复制自有候选；返回强类型值。

```mermaid
flowchart TD
 N01{"N01【指令-判断】阶段是否为空或已登记"} -- 否 --> N02["N02【指令-返回】返回 入口拒绝/事务阶段不一致/空材料"]
 N01 -- 是 --> N03{"N03【指令-判断】规格.特征值是否为有效完整句柄"} -- 否 --> N04["N04【指令-返回】返回 入口拒绝/无效身份/空材料"]
 N03 -- 是 --> N05{"N05【指令-判断】规格.原始值版本是否非零"} -- 否 --> N06["N06【指令-返回】返回 入口拒绝/错版本/空材料"]
 N05 -- 是 --> N07{"N07【指令-判断】规格.原始值.类型是否为 I64、VecI64 或 VecU64"} -- 否 --> N08["N08【指令-返回】返回 入口拒绝/错类型/空材料"]
 N07 -- 是 --> N09{"N09【指令-判断】仅与类型对应的值分支非空，向量长度在 1..最大值内"} -- 否 --> N08
 N09 -- 是 --> N10["N10【指令-赋值】扫描位置=私有待插入条目组.begin()、同键候选位置=空"]
 N10 --> N10A{"N10A【指令-判断】扫描位置是否等于私有待插入条目组.end()"}
 N10A -- 否 --> N10B["N10B【指令-借用】当前候选=*扫描位置"]
 N10B --> N10C{"N10C【指令-判断】当前候选的特征值完整句柄和原始值版本是否等于规格键"}
 N10C -- 是 --> N10D["N10D【指令-赋值】同键候选位置=扫描位置"]
 N10C -- 否 --> N10E["N10E【指令-推进】扫描位置加一"]
 N10E --> N10A
 N10D --> N11
 N10A -- 是 --> N11{"N11【指令-判断】同键候选位置是否有值"}
 N11 -- 是 --> N12{"N12【指令-判断】同键候选与规格是否逐字段同义"}
 N12 -- 是 --> X02["X02【函数调用】F-A202(2)"]
 X02 --> N13["N13【指令-构造】复制既有记录到幂等返回材料"]
 N13 --> N22{"N22【指令-判断】幂等返回材料复制是否成功"}
 N22 -- 否 --> N17["N17【指令-返回】返回 内部错误/资源失败/空材料"]
 N22 -- 是 --> N23["N23【指令-返回】返回 幂等读回/无/既有记录副本；不追加"]
 N12 -- 否 --> N14["N14【指令-返回】返回 入口拒绝/同键异义/空材料"]
 N11 -- 否 --> X01["X01【函数调用】F-A202(1)"]
 X01 --> N15["N15【指令-操作】在临时对象中按规格构造有效、格式1、未发布仓条目"]
 N15 --> N16{"N16【指令-判断】临时条目构造是否成功"}
 N16 -- 否 --> N17["N17【指令-返回】返回 内部错误/资源失败/空材料"]
 N16 -- 是 --> N18["N18【指令-构造】从临时条目预构造成功返回材料"]
 N18 --> N19{"N19【指令-判断】成功返回材料构造是否成功"}
 N19 -- 否 --> N17
 N19 -- 是 --> N20["N20【指令-操作】以 list::splice 无分配移入私有待插入条目组并把阶段置已登记"]
 N20 --> N21["N21【指令-返回】返回 成功/无/新记录副本"]
```

节点合同：N10 只读参与者私有列表；N13/N18 在任何状态修改前构造返回材料；N20 是本函数唯一候选状态写入且只使用无分配 `list::splice`。所有非成功结果材料为空；同键同义不追加，不能返回未定义的“已登记”状态。
