# 承接任务子目标记录形成子需求 函数流程图 v0.1

日期：2026-08-23
状态：施工目标图；不描述当前已实现代码

## 1. 唯一函数身份

```cpp
需求任务子目标承接结果 需求任务子目标承接服务::承接任务子目标记录(
    const 需求任务子目标承接请求& 请求) noexcept;
```

- 目标文件：`海中鱼巣/领域/服务.需求任务子目标承接.ixx`
- 目标模块：`海中鱼巣.领域.服务.需求任务子目标承接`
- 参数：只读值式请求；调用方拥有，两组幂等身份和父子顺序均为显式输入
- 返回：值式结构化结果；只有最终双 owner 同 G 读回一致时成功
- 事实写入：先需求 owner，后任务 owner；本函数不取得 L1、owner、事务或 raw 写端口
- 正式设计：`规范/详细设计/20260823_RUNTIME-DEMAND-TASK-SUBGOAL-RECORD-CONSUMPTION_需求消费任务子目标承接记录详细设计_v0.1.md`

## 2. Mermaid

```mermaid
flowchart TD
    A["N01【判断】合同、G0、记录、父子顺序、两幂等身份完整"]
    A -->|否| R1["R01【返回】入口拒绝；零 DATA 调用"]
    A -->|是| B["N02【调用】按 G0 读取任务子目标承接记录"]
    B --> C{"N03【判断】是否事实代次漂移且回显最新 G"}
    C -->|是| D["N04【调用】只用最新 G 对同一记录重读一次"]
    C -->|否| E["N05【判断】记录读取结果和载荷完整"]
    D --> E
    E -->|否| R2["R02【返回】映射记录非成功；再次漂移不循环"]
    E -->|是| F["N06【调用】同 G 按记录读取需求 owner 来源引用"]
    F --> F1{"N06A【判断】来源读取是否回显新的 G"}
    F1 -->|是| F2["N06B【调用】以最新 G 对记录+来源完整预读组重做一次"]
    F1 -->|否| G{"N07【判断】记录与来源同截止且组合合法"}
    F2 --> F3{"N06C【判断】整组成功且截止相同"}
    F3 -->|否| R2
    F3 -->|是| G
    G -->|双向已有且一致| Z["N25【调用】以预读共同 G 进入最终双读回"]
    G -->|任务已绑定但来源缺失/复义/异子| R3["R03【返回】内部不一致"]
    G -->|来源已有、记录待绑定| T0["N20A【调用】同预读 G 完整读取父子需求、父关系、成员和列表项"]
    T0 --> T1{"N20B【判断】六份需求侧材料与记录逐项一致"}
    T1 -->|否| R3
    T1 -->|是| T["N20【调用】按需求侧事实已存在分类，用原任务绑定键绑定记录"]
    G -->|记录待绑定且来源空| H["N08【调用】同 G 读取父需求、父成员、父列表项"]
    H --> I{"N09【判断】显式父域完整且自洽"}
    I -->|否| R4["R04【返回】父域冲突或内部不一致"]
    I -->|是| J["N10【调用】按父所属存在+记录目标精确查询列表项"]
    J --> K["N11【调用】读取父需求当前直接子需求组"]
    K --> L{"N12【判断】精确列表项/同父同目标子是否复义"}
    L -->|是| R5["R05【返回】内部不一致；零新写"]
    L -->|否| M{"N13【判断】是否已有唯一同父同目标子"}
    M -->|是| N{"N14【判断】成员列表一致且既有父子顺序等于请求"}
    N -->|否| R6["R06【返回】目标冲突或父子顺序冲突"]
    N -->|是| O["N15【调用】需求 owner 原子建立既有子需求来源引用"]
    M -->|否| P{"N16【判断】是否已有唯一精确列表项"}
    P -->|是| Q["N17【调用】需求 owner 原子发布新子需求+成员+父子+来源引用"]
    P -->|否| R["N18【调用】需求 owner 原子发布新列表+新子需求+成员+父子+来源引用"]
    O --> S["N19【判断】需求结果成功/重复且权威读回完整"]
    Q --> S
    R --> S
    S -->|否| R7["R07【返回】精确映射漂移/冲突/资源/内部不一致"]
    S -->|是| T
    T --> U{"N21【判断】任务绑定成功/精确重复且载荷完整"}
    U -->|漂移或资源失败| R8["R08【返回】需求已发布待任务绑定；保留需求侧读回"]
    U -->|其它非成功| R9["R09【返回】精确映射；不撤销需求事实"]
    U -->|是| Z
    Z --> AA["N26【调用】同一最终 G 重读记录、来源、父子需求、父关系、成员和列表项"]
    AA --> AB{"N27【判断】是否报告漂移"}
    AB -->|是| AC["N28【调用】以回显最新 G 对完整读回组重做一次"]
    AB -->|否| AD{"N29【判断】双 owner、父域、成员、列表项、目标逐字段一致"}
    AC --> AD
    AD -->|否| R10["R10【返回】再次漂移或内部不一致"]
    AD -->|是| R11["R11【返回】已承接或精确重复"]
```

## 3. 节点合同

| 节点 | 输入 | 调用/动作 | 输出与副作用 |
| --- | --- | --- | --- |
| N01—N05 | 公开请求、记录身份、G0 | 纯值校验；记录最多一次最新 G 重读 | 零写；不无限重试 |
| N06—N07 | 当前记录 | 按记录读取需求 owner 来源关系；来源漂移时最多一次重做完整预读组 | 形成唯一预读共同 G；裁决新承接、需求侧事实已存在、已有双向或内部不一致 |
| N08—N12 | 显式父需求、记录目标 | 读取父域、精确列表项和当前直接子组 | 零写；禁止跨父复用或任选复义项 |
| N13—N18 | 唯一选择结果 | 三个需求 owner 原子入口三选一 | 每次最多一个 owner 写；来源引用与需求结构同代 |
| N19—N21 | 需求读回、任务绑定键 | 新写路径验证含父需求的六份需求结果；恢复路径先完整读回六份需求侧材料；再调用任务记录绑定 | 任务非成功返回需求已发布待任务绑定，不撤销需求 owner 已发布事实 |
| N25—N29 | 最终 G、两个 owner 身份 | 完整双向读回父需求、子需求及两 owner 事实；最多一次整组重读 | 只有父子所属存在等全部一致才返回成功 |

## 4. 禁止边

本函数不得调用或取得：

```text
L1事实基座写入口
L1 owner / 写端口 / 幂等账
任务 owner 私有写端口
需求结构内部 helper
父需求重判
旧任务唤醒或重新筹办
任务创建
需求正式结算
结果共用/争用或优先级分配
```

该图只冻结目标函数。存在本图不证明函数已实现、已编译、已运行或已完成集成验收。
