# 任务结果消费关系与争用分配业务流程图 v0.1

日期：2026-08-23
身份：RUNTIME-GOVERNANCE-TASK-RESULT-CONSUMPTION-ALLOCATION
性质：第四层目标业务流程；不是当前代码流程图

配套详细设计：`规范/详细设计/20260823_RUNTIME-GOVERNANCE-TASK-RESULT-CONSUMPTION-ALLOCATION_任务结果消费关系与争用分配详细设计_v0.1.md`

配套代码计划：`计划/20260823_RUNTIME-GOVERNANCE-TASK-RESULT-CONSUMPTION-ALLOCATION_任务结果消费关系与争用分配代码实施切片_v0.1.md`

## 1. 业务边界

输入必须完整绑定正式任务实际结果、G0 冻结成员集合、每成员目标与消费资格、结果容量 / 范围 / 所有权 / 有效性合同及规则版本。输出只可能是一个不可变可共用关系记录或一份包含正式优先级证据、逐项分配和最终剩余的争用记录。

本图不裁决任务完成，不进入逐需求目标满足、5160 正式结算、价值、预算或归因，不激活旧 `BIZ-L2-004-06`，不表示代码已经实现或生产接线。

## 2. 主流程

```mermaid
flowchart TD
    A["N01【入口】裁决关系并形成分配\n完整请求 + 幂等身份 + G0"] --> B{"N02【判断】纯值合同完整?"}
    B -- "否" --> Z1["N03【返回】入口拒绝\n零写"]
    B -- "是" --> C["N04【函数调用】按幂等身份读既有记录"]
    C --> D{"N05【判断】已有首次记录?"}
    D -- "是且完整请求相同" --> E["N06【函数调用】按原身份与原发布截止独立读回"]
    E --> Z2["N07【返回】精确重复\n不重新裁决或分配"]
    D -- "是但请求不同" --> Z3["N08【返回】幂等冲突\n零写"]
    D -- "否" --> F["N09【函数调用】同 G0 读正式结果、任务、列表项和当前成员组"]
    F --> G{"N10【判断】G0、成员集合、目标与资格逐项互证?"}
    G -- "否或漂移" --> Z4["N11【返回】材料缺口 / 成员集合冲突 / 当前性漂移\n零写"]
    G -- "是" --> H{"N12【判断】容量、范围、所有权、有效性合同受 v1 支持?"}
    H -- "否" --> Z5["N13【返回】消费合同不支持\n零写"]
    H -- "是" --> I{"N14【函数调用】按正式合同裁决消费关系"}
    I -- "可重复引用且四维零扣减" --> J{"N15【判断】正式优先级组为空?"}
    J -- "否" --> Z6["N16【返回】引用冲突\n可共用禁止优先级账"]
    J -- "是" --> K["N17【指令】逐成员形成共同结果引用\n不排序业务优先级"]
    K --> P["N18【函数调用】task owner 原子发布完整关系记录"]
    I -- "任一维度会扣减" --> L{"N19【判断】每成员正式优先级证据、来源与版本完整唯一?"}
    L -- "否" --> Z7["N20【返回】优先级合同缺失 / 冲突\n关系与分配全零写"]
    L -- "是" --> M["N21【指令】仅按优先级序号、同级顺序序号排序"]
    M --> N["N22【指令】剩余 := 初始可用单位"]
    N --> O{"N23【循环】还有成员?"}
    O -- "否" --> P
    O -- "是" --> Q["N24【函数调用】复核本成员 G0 资格与消费合同"]
    Q --> R{"N25【判断】当前剩余"}
    R -- "等于 0" --> S["N26【指令】结果已耗尽\n实际分配 0"]
    R -- "低于最低合法单位" --> T["N27【指令】未获分配\n实际分配 0，剩余不变"]
    R -- "达到最低" --> U["N28【指令】实际分配 := min 期望, 剩余"]
    U --> V["N29【指令】立即扣减\n分配后剩余 := 分配前剩余 - 实际分配"]
    S --> W["N30【指令】保存本成员步骤"]
    T --> W
    V --> W
    W --> O
    P --> X{"N31【判断】提交为已提交 / 精确重复?"}
    X -- "否" --> Z8["N32【返回】漂移 / 冲突 / 资源失败\n不得补成功"]
    X -- "是" --> Y["N33【函数调用】以 G1 独立读回关系、证据、步骤链和最终剩余"]
    Y --> AA{"N34【判断】读回与完整请求、步骤链逐项一致?"}
    AA -- "否" --> Z9["N35【返回】内部错误\n不撤销已发布事实，原键恢复"]
    AA -- "是" --> Z10["N36【返回】已形成可共用关系 / 已形成争用分配 / 精确重复"]
```

## 3. 事务不变量

```text
可共用：优先级证据组为空，剩余账为零

争用：
初始剩余 = 容量合同初始可用单位
第 i 项.分配前剩余 = 第 i-1 项.分配后剩余
第 i 项.分配后剩余 = 第 i 项.分配前剩余 - 第 i 项.实际分配
最终剩余 = 最后一项.分配后剩余
```

关系、正式优先级证据、成员结果和最终剩余只能一次原子发布。稳定编码升序只用于成员集合互证和输出规范化，不能进入 N21 的业务排序键。

## 4. 非成功边界

- 关系不可裁决、成员集合冲突、合同不支持、优先级 provider 输入缺失、同级顺序冲突或当前性漂移：关系与分配全零写；
- `未获分配`、`结果已耗尽`是争用记录内的成员分配结果，不是任务失败、需求未满足或结算成功；
- 发布未知只能以原完整请求和原键恢复；
- 可共用成员之间相互独立，某成员的后继非成功不能污染其它成员；
- 本流程结束后仍未执行逐需求目标满足与 5160 正式结算。
