# L1-SIMPLIFY-P17 L1 非阻塞当前快照与事实代次读取函数流程图 v0.2

对应详细设计：`20260807_L1-SIMPLIFY-P17-NONBLOCKING-READ_L1非阻塞当前快照与事实代次读取详细设计_v0.2.md`

本次只替换过期的 P15 v0.2 门禁：P15 v0.29 已提供真实 L1 合同版本2、唯一仓库/服务接口与组件资源；其正式接线失败边界仍保留，不构成 P17 的服务集成 PASS。

```mermaid
flowchart TD
    A["P17 非阻塞读取入口"] --> B{"入口类型"}
    B -- "尝试读取完整快照" --> C["校验 L1 合同版本2"]
    B -- "尝试读取当前事实代次" --> D["校验 L1 合同版本2"]
    C --> E["仓库只尝试一次 try_to_lock 共享许可"]
    D --> F["仓库只尝试一次 try_to_lock 共享许可"]
    E --> G{"取得许可且状态完整？"}
    F --> H{"取得许可且状态完整？"}
    G -- "否/竞争" --> I["返回许可拒绝；空载荷；零等待零重试"]
    H -- "否/竞争" --> I
    G -- "隔离或损坏" --> J["返回内部不一致；空快照"]
    H -- "隔离或损坏" --> K["返回内部不一致；代次0"]
    G -- "是" --> L["复用唯一私有快照投影并按稳定编码排序"]
    H -- "是" --> M["复制权威事实代次"]
    L --> N["返回完整纯值快照；返回前释放许可"]
    M --> O{"代次是否非零？"}
    O -- "否" --> P["返回未找到；代次0"]
    O -- "是" --> Q["返回成功；非零事实截止代次"]
```

P15 v0.29 的正式生产消费者、正式独立读回、自检隔离和跨进程恢复不在本图范围；日志和验收文本不是机器事实。
