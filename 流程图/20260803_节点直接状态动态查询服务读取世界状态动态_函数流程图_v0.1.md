# 节点直接状态动态查询服务读取世界状态动态函数流程图 v0.1

更新时间：2026-08-03

## 依据

```text
规范/4200_子规范_世界树业务服务与同代次完整事实快照.md v0.9
规范/4201_子规范_世界树合同骨架与未实现结果.md v0.4
规范/详细设计/20260803_WORLD-TREE-P1B-F_状态动态最终合同骨架详细设计_v0.1.md §4—§7
```

## 身份与边界

正式目标单函数施工图；只冻结 P1B-F 查询 ABI 骨架，不证明状态或动态真实读取。

## 函数合同

```text
根函数：节点直接状态动态查询服务::读取世界状态动态
归属模块 / 文件 / 层级：海中鱼巣.领域.服务.节点直接状态动态查询 / 海中鱼巣/领域/服务.节点直接状态动态查询.ixx / L2
现有或待新增：待新增最终合同骨架；后继 P1B-Q 原位替换函数体
完整签名：节点直接世界状态动态读取结果 节点直接状态动态查询服务::读取世界状态动态(const 节点直接世界状态动态读取请求& 请求) const noexcept
参数：请求为调用方拥有的同步 const 借用；骨架不读取、不保存；任意合同版本、空组或大容器均同路
返回结果：调用方拥有值；固定 状态=未实现(8)、事实截止代次=0、状态组/动态组/缺项组为空
被调函数流程图：无；项目内调用边为0
```

## 流程图

```mermaid
flowchart TD
    N01["N01【指令-构造】不读取请求；构造 节点直接世界状态动态读取结果{未实现8,代次0,状态组空,动态组空,缺项组空}"]
    N01 -->|固定prvalue| R01["R01【指令-返回】把固定未实现结果返回调用方"]
```

## 节点合同

| 节点 | 类型 | 参数 / 输入绑定 | 结果 / 输出绑定 | 事实读写 | 后继条件 | 验证 |
| --- | --- | --- | --- | --- | --- | --- |
| N01 | 本函数内具体构造 | `请求` 不解引用、无字段绑定 | 固定 prvalue | 零读取、零写入 | 构造完成 | `static_assert(noexcept(...))`；任意输入逐字段相同 |
| R01 | 本函数内返回 | 固定 prvalue | 调用方拥有完整值 | 无 | 函数结束 | 世界结构与 L1 结构两个保存引用调用计数均为0 |

## 关键边界

```text
不得先校验请求再返回入口拒绝；不得调用世界结构_或结构_；不得把未实现解释为合法零状态、未找到或资源失败。真实查询只能由 P1B-Q 在原模块、原类和原签名内替换函数体。
```
