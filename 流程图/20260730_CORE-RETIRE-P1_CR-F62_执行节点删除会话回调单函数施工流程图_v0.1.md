# CR-F62 执行节点删除会话回调单函数施工流程图

版本：v0.1
代码基线：`57866ac5ca63235ed12da60f635d29f1aacd718b`

## 函数合同

```text
根函数：执行节点删除会话回调
物理位置：海中鱼巣/核心/自检.节点直接身份结构写入.ixx，CR-F48 之前内部区
完整签名：void 执行节点删除会话回调(节点删除会话回调上下文& 上下文, 节点直接身份结构写入会话& 会话)
参数：一次执行期借用；场景1—6、目标节点
返回：void；首次/重复删除及写前/写后事务命中写上下文；场景5提交，其它撤销
```

```mermaid
flowchart TD
    N01{"N01【指令-判断】场景值在1—6"} -- "否" --> X00["X00【函数调用】void 节点直接身份结构写入会话::记录失败(const 节点直接身份结构写入结果& 失败) noexcept：入口拒绝→失败；返回void"]
    N01 -- "是" --> X01["X01【函数调用】节点直接身份带值结构写入结果<节点句柄> 节点直接身份结构写入会话::删除节点候选(节点句柄 节点)：上下文.节点→节点；接收首次删除"]
    X01 -- "场景3精确重复" --> X02["X02【函数调用】节点直接身份带值结构写入结果<节点句柄> 节点直接身份结构写入会话::删除节点候选(节点句柄 节点)：上下文.节点→节点；接收重复删除"]
    X01 -- "其它场景" --> X03
    X02 -- "重复结果" --> X03["X03【函数调用】std::optional<节点直接身份记录> 节点直接身份结构写入会话::读取节点(节点句柄 节点)：上下文.节点→节点；接收写前事务记录"]
    X03 -- "写前已接收" --> X04["X04【函数调用】std::optional<节点直接身份记录> 节点直接身份结构写入会话::读取节点(节点句柄 节点)：首次删除.值→节点；接收写后事务记录"]
    X04 -- "读回完成" --> N02["N02【指令-赋值】写前句柄事务内命中=写前记录存在；写后句柄事务内命中=写后记录存在"]
    N02 -- "场景5" --> X05["X05【函数调用】void 节点直接身份结构写入会话::请求提交() noexcept：无参数；返回void"]
    N02 -- "其它合法场景" --> X06["X06【函数调用】void 节点直接身份结构写入会话::请求撤销() noexcept：无参数；返回void"]
    X00 -- "失败已记录" --> R01["R01【指令-返回】返回void"]
    X05 -- "提交已请求" --> R01
    X06 -- "撤销已请求" --> R01
```

调用者：CR-F48；绑定方式为 `std::bind_front(执行节点删除会话回调,std::ref(上下文))`。关系/索引残留夹具由 F48 外层在执行前建立，普通发布后审计也由外层完成。
