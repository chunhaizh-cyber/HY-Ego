# TASK-ROUND-INITIAL-STATE-ARCH-L2-MIGRATION 任务轮次首态分阶段发布业务流程图

版本：v0.1
日期：2026-08-24
性质：业务流程图；冻结一个“具体需求形成首个任务轮次与筹办中状态”的业务闭环，不表示当前代码已经实现

```mermaid
flowchart TD
    A[输入具体需求 D 与原请求全部幂等键] --> B{按准备键已有完整最终记录?}
    B -- 是 --> R[按各 owner 历史截止独立读回]
    R --> Z[返回精确重复完整工作包]
    B -- 否 --> C[在 G0 裁决 D 当前未满足并取得所属列表项 L]
    C --> C1{D/L/目标与当前性完整?}
    C1 -- 否 --> F0[具名失败 零后继写]
    C1 -- 是 --> E1[存在 owner 建立或恢复任务专属 E]
    E1 --> T1[task owner 单 G_T 建立 T R1 T到L T到D T到E]
    T1 --> X1[特征定义 owner 以全局固定键建立或恢复任务阶段基础定义]
    X1 --> X2[特征实例 owner 为 E 建立或恢复唯一阶段实例]
    X2 --> S0[状态 owner 单 G_S0 建立筹办/待初次筹办首态]
    S0 --> N0[确认首态无动态]
    N0 --> P1[task owner 单 G_P1 建立首次记录 P1 与治理动作调用事实 A1]
    P1 --> A2[独立读回 A1 的函数定义 T R1 P1 前态 截止 幂等与版本]
    A2 --> M1[以 A1 为来源 状态/动态两个 owner 单 G_M1 原子迁移到筹办中]
    M1 --> M2[形成第一动态并退出旧状态当前关系]
    M2 --> R2[分别读回 E T R1 关系 特征 首后态 P1 A1 第一动态]
    R2 --> C2{全部端点 生命周期 值和代次互证?}
    C2 -- 是 --> Z2[返回已形成完整工作包]
    C2 -- 否 --> F2[内部错误 零成功载荷]

    E1 -. 非成功且截止非零 .-> U1[只按存在原键读回收敛]
    T1 -. 非成功且截止非零 .-> U2[只按 task 原键读回收敛]
    X1 -. 非成功且截止非零 .-> U3[只按全局定义原键读回收敛]
    X2 -. 非成功且截止非零 .-> U4[只按实例原键读回收敛]
    S0 -. 非成功且截止非零 .-> U5[只按首态原键读回收敛]
    P1 -. 非成功且截止非零 .-> U6[只按 P1 原键读回收敛]
    M1 -. 非成功且截止非零 .-> U7[只按首迁移原键读回收敛]
    U1 -->|确认同义| T1
    U2 -->|确认同义| X1
    U3 -->|确认同义| X2
    U4 -->|确认同义| S0
    U5 -->|确认同义| N0
    U6 -->|确认同义| A2
    U7 -->|确认同义| R2
    U1 -->|未确认或异义| FU[发布未知或幂等冲突 停止]
    U2 -->|未确认或异义| FU
    U3 -->|未确认或异义| FU
    U4 -->|未确认或异义| FU
    U5 -->|未确认或异义| FU
    U6 -->|未确认或异义| FU
    U7 -->|未确认或异义| FU
```

## 节点合同

| 节点 | 必须成立 | 明确禁止 |
| --- | --- | --- |
| E1 | E 来自唯一 `L2存在结构服务`，每个 T 专属 | task owner 新建私有存在节点 |
| T1 | 同一 task-owner 代次建立 T、R1 与三条业务关系 | 把 G_T 写成 E / 状态 / 动态共同 G1 |
| X1 / X2 | 全局定义一次；每个 E 一个实例 | 每任务重复定义、用字符串或枚举缓存代替特征事实 |
| S0 | `{筹办, 待初次筹办}`，绑定 E 与同一特征实例 | 为首态伪造动态 |
| P1 | 引用既有 R1、E、实例、首态；同次形成 A1 | 在首态前建立 P1；用 P1 / 文本 / 魔数冒充动作调用 |
| M1 | `{待初次筹办}->{筹办中}`，形成第一动态并引用 A1 | 使用空旧态、另一个 E / 实例或坏函数 / 前态 / 版本 |
| U1—U7 | 原键、原请求、公开读回 | 换键、刷新请求、跨越未确认阶段 |

## 完成声明边界

本图只冻结业务四检查点与七个 owner 提交段的关系。它不表示跨 owner 原子创建整份工作包，也不证明普通启动线程、方法筹办、执行、结果、结算或跨进程恢复已完成。
