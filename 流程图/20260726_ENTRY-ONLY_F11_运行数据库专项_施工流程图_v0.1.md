# ENTRY-ONLY F11 运行数据库专项

图类型：施工流程图
完整签名：`程序运行结果 运行数据库专项()`
归属：`海中鱼巣/启动.应用程序.ixx`；返回 T02。绑定规范 8120、详细设计 12.2、计划。
允许：F07/F08/F10 严格审计；禁止完整自检、投影和宿主。结构变化：生产必要初始化 + SQL 审计。执行前复核数据库可用性不属于入口前置；验证 V15-V16、V23。不得宣称普通启动依赖 SQL。

```mermaid
flowchart TD
    N01["N01【函数调用】构造普通应用上下文(固定配置) -> 装配"] --> N02{"N02【指令】装配成功"}
    N02 -- "否" --> R1["N03【指令】返回装配失败"]
    N02 -- "是" --> N04["N04【函数调用】初始化普通系统(上下文, 请求) -> 初始化"]
    N04 --> N05{"N05【指令】初始化成功"}
    N05 -- "否" --> R2["N06【指令】返回初始化失败"]
    N05 -- "是" --> N07["N07【函数调用】执行启动结构审计(上下文, 必须往返一致) -> 审计"]
    N07 --> N08{"N08【指令】审计往返一致"}
    N08 -- "否" --> R3["N09【指令】返回审计失败与阶段"]
    N08 -- "是" --> N10["N10【指令】返回已完成"]
```

| 节点 | 类型 | 分析 |
| --- | --- | --- |
| N01/N04/N07 | 被调函数 | 分别 F07/F08/F10。 |
| 其余 | 指令 | 严格专项映射；不进入宿主。 |

调用方 F04；被调图 F07/F08/F10。
