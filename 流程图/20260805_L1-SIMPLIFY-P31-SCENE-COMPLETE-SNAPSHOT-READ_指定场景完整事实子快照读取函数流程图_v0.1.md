# L1-SIMPLIFY-P31-SCENE-COMPLETE-SNAPSHOT-READ 指定场景完整事实子快照读取函数流程图 v0.1

日期：2026-08-05

状态：设计图；代码未实现；计划待激活

对应函数：

```cpp
完整世界场景快照读取结果 世界树快照读取服务::读取场景完整事实(
    const 完整世界对象读取请求& 请求) const;
```

## 1. 调用边界

```mermaid
flowchart TD
    C["后继统一世界树只读门面/治理消费者"] --> P31["读取场景完整事实"]
    P31 -->|"恰1次"| P29["读取完整世界事实快照"]
    P29 --> W["P28完整世界事实快照"]
    W --> S["P31纯值场景投影"]
    S -->|"直接成员0..N次"| H["P30私有存在投影helper"]

    P31 -. "公开调用0" .-> E["P30读取存在完整事实"]
    P31 -. "直接调用0" .-> N1["P19/P21/P17/P22/P24—P28"]
    P31 -. "禁止" .-> N2["L1解析/仓库/SQL/缓存/历史"]
    P31 -. "禁止" .-> N3["第二整树读取/额外重试/写事实"]
```

P31 不形成第二世界读取链。P29 内部最多两轮是唯一漂移重试；全部成员都在同一 P29 返回的不可变快照上复用 P30 私有纯值 helper。

## 2. 公开函数流程

```mermaid
flowchart TD
    A["进入读取场景完整事实"] --> V{"世界请求/投影规则/目标见证有效?"}
    V -->|"否"| RI["入口拒绝\nP29调用0/空载荷"]
    V -->|"是"| R["调用P29整树读取恰1次"]
    R -->|"普通非成功"| M["同名映射\n空子快照/空缺项"]
    R -->|"内部不一致"| MI["内部不一致\n稳定缺项原样转交"]
    R -->|"坏成功"| BI["内部不一致\n空子快照"]
    R -->|"已读取完整快照"| E{"目标在场景组恰一命中?"}
    E -->|"零命中"| NF["未找到\n空子快照"]
    E -->|"重复/异内容"| II["内部不一致\n稳定缺项"]
    E -->|"是"| P{"根无父/非根单父且双向闭合?"}
    P -->|"否"| II
    P -->|"是"| C{"直接子关系逐项双向闭合?"}
    C -->|"否"| II
    C -->|"是"| D{"直接成员关系与存在归属双向闭合?"}
    D -->|"否"| II
    D -->|"是"| Z{"直接成员为空?"}
    Z -->|"是"| O["形成空成员完整事实组"]
    Z -->|"否"| S["按存在编码升序逐成员调用P30私有helper"]
    S --> Q{"全部成员已读取且同根同次同归属?"}
    Q -->|"否"| II
    Q -->|"是"| O2["形成完整成员事实组"]
    O --> OK["已读取\n同根同次/完整=true/缺项空"]
    O2 --> OK

    A -. "bad_alloc" .-> RM["资源失败\n不二次调用P29"]
    A -. "其它异常" .-> RX["内部不一致\n不二次调用P29"]
```

## 3. 唯一字段来源

| 输出字段 | 唯一来源 | 规则 |
| --- | --- | --- |
| 合同/世界/组合/完整性/排序版本 | P28完整快照 | 必须等于正式常量 |
| 投影规则版本 | P30共享常量1 | 请求必须精确等于1 |
| 世界根/截止 | P29成功快照 | 原样复制，截止非零 |
| 场景 | P28场景组唯一目标 | 含目标、直接父、直接子和直接成员关系 |
| 直接父/子场景 | P28场景事实及关系端点 | 根无父；非根单父；只含直接关系 |
| 直接成员身份与归属 | P28场景关系+存在组 | 双向一一闭合，不含子场景成员 |
| 直接成员完整事实 | 同一世界快照上的P30私有helper | 每成员恰1；公开存在读调用0 |
| 完整 | 全部检查后的true | 任一失败不形成部分子快照 |
| 缺项 | P28稳定诊断类型 | 仅内部不一致可非空 |

## 4. 直接范围与合法空组

```mermaid
flowchart LR
    A["目标当前场景"] --> B["父/直接子关系闭合"]
    B --> C["直接成员关系与存在归属闭合"]
    C --> D["每个直接成员完整事实"]
    D --> OK["合法完整成功"]

    Z1["零直接子场景"] --> E1["成功空关系组"]
    Z2["零直接成员"] --> E2["成功空成员组/helper调用0"]
    X["子场景成员/其它场景成员"] --> NO["不得夹带"]
```

场景子快照不是递归子树快照。直接子场景只由关系见证携带身份；其详情和成员不进入本结果。若同一动态同时与两个直接成员相关，可以分别出现在两份成员完整事实中，场景层不去重或重解释。
