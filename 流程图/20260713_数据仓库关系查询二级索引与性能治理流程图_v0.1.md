# 数据仓库关系查询二级索引与性能治理流程图

更新时间：2026-07-13

状态：JY-356 / #257、790 已完成 / JY-355 首版草案性能门禁失败 / 原 #258 提交算法修订后重新待执行 / 800 未实施

## 依据

```text
AGENTS.md
规范/000_项目规则总纲.md
规范/仓库逻辑空间与领域树结构规范.md
规范/多线程防锁机制规范.md
规范/详细设计/关系仓库详细设计.md
规范/详细设计/索引仓库详细设计.md
海中鱼巣/核心/关系仓库.h/.cpp
海中鱼巣/核心/索引仓库.h/.cpp
海中鱼巣/核心/协调.结构事务.ixx
海中鱼巣/领域/控制面板服务.h
实施记录/20260713_WAREHOUSE-OPT-S0_关系查询与并发性能基线代码实施_Codex断点清单.md
实施记录/20260715_WAREHOUSE-OPT-S1_正式性能门禁复测失败_Codex断点清单.md
```

## 说明

本图表达统一物理仓库上的关系查询优化顺序。当前六项是显示分类，不是六棵物理树；概念结构允许多根、多父共享节点和图关系。关系表继续承载权威关系，关系仓库内部二级索引只召回候选，候选必须按关系句柄、版本、状态和有效端点复核。

## 流程图

```mermaid
flowchart TD
    A["前置：#262-#271 完成仓库与服务分层纠偏，生产调用不再穿透原始令牌"] --> A1["S0：建立分层后隔离关系仓库夹具与可复现规模"]
    A1 --> B["记录全表扫描路径、查询耗时、并发等待代理值、吞吐和正确性摘要"]
    B --> B1{"基线是否完整且可重复"}
    B1 -- "否" --> X1["追根因解决：停止 S1，不以单次微基准裁决结构"]
    B1 -- "是" --> C0["JY-355 首版草案：查询候选缩小且专项正确性成立，但写入与稳定门禁失败"]
    C0 --> C01{"查询正确性、稳定性、写入回归是否独立判定"}
    C01 -- "否" --> C02["JY-356：拆分报告材料完整、查询正确、稳定性和写入门禁；总验收仍要求全部通过"]
    C01 -- "是" --> C03{"失效 / 删除写入 P95 是否满足 S0 的 150% 硬上限"}
    C02 --> C03
    C03 -- "否" --> C04["原 #258 重做提交算法：受影响桶一次定位、预备、定向切换和逆序回退"]
    C03 -- "是" --> C["S1：关系仓库同锁维护正向、反向、节点相关和审计候选索引"]
    C04 --> C05["移除热路径重复全索引复核与稀疏桶无必要分配 / 回收；Debug 全量诊断退出正式计时段"]
    C05 --> C

    C --> C1["写前完成句柄、类型、唯一性和领域前置拒绝"]
    C1 --> C2{"创建 / 失效 / 重挂 / 逻辑删除 / 撤销 / 批量失效"}
    C2 -- "创建" --> W1["预备索引项；关系表与全部索引在同一独占锁内提交"]
    C2 -- "失效" --> W2["有效索引移除；审计索引保留当前失效记录候选"]
    C2 -- "重挂" --> W3["预备新端点索引项；推进同一关系版本；移除旧端点索引项"]
    C2 -- "逻辑删除" --> W4["关系表记录推进为已删除并保留；有效 / 审计索引候选全部移除"]
    C2 -- "撤销" --> W6["恢复写前关系记录与旧索引；移除写后索引"]
    C2 -- "批量失效" --> W7["整批写前复核；同锁移除有效索引，审计索引保留当前失效候选"]
    W1 --> W5{"提交是否完整"}
    W2 --> W5
    W3 --> W5
    W4 --> W5
    W6 --> W5
    W7 --> W5
    W5 -- "否" --> X2["追根因解决：回退本次索引与主表改动；不得发布半索引关系"]
    W5 -- "是" --> R1["按查询键从二级索引复制关系编号候选"]

    R1 --> R2["逐项复核关系表当前记录：仓库 / 编号 / 版本 / 类型 / 顺序 / 状态 / 源目标端点"]
    R2 --> R3["复核有效端点；按顺序号、关系编号确定性排序"]
    R3 --> R4{"候选与权威记录是否一致"}
    R4 -- "否" --> X3["追根因解决：停止后续产物；禁止事后全表扫描修复"]
    R4 -- "是" --> O1["返回值式关系记录或节点句柄组；索引不裁决业务事实"]

    O1 --> G1{"A01-A14 与两次正式性能运行是否全部通过"}
    G1 -- "否" --> X4["不登记 800、不归档 #258；本轮算法重做后仍失败则退回并默认不发布 S1"]
    G1 -- "是" --> D1{"S1 后指标是否仍达到令牌 / 批量读取优化门槛"}

    D1 -- "否" --> D2["S2 记录不实施裁决；保持事务 ABI 和现有令牌语义"]
    D1 -- "是" --> D3["S2：只优化已证明热点的计数、批量读取或许可活动表路径"]
    D2 --> E1{"S1 / S2 后锁竞争是否仍达到分片门槛"}
    D3 --> E1
    E1 -- "否" --> E2["S3 记录不实施裁决；保持单一统一仓库"]
    E1 -- "是" --> E3["S3：设计统一句柄下的仓库分片或类型分区与跨分区事务边界"]

    E2 --> P1["独立后继：显示分类 / 当前概念根按需请求只读投影"]
    E3 --> P1
    P1 --> P2["按根、关系白名单、深度和页限读取候选并复核权威关系"]
    P2 --> P3["多根森林、多父共享节点和图边保留；重复节点显示去重但边不丢失"]
    P3 --> P4["控制面板只消费值式投影，不写仓库、不缓存为机器事实"]

    Q1["索引命中但关系版本 / 端点 / 状态不符"] --> Q2["拒绝候选；若同一许可内出现则按内部不一致追根因"]
    Q3["微基准变快但入口拒绝、既有共享兼容删除 / 严格独占删除许可或事务安全回归"] --> Q4["验收失败；不得接受优化"]
```

## 非成功返回二分

```text
逻辑内返回：无候选、请求键不合法、证据未达到 S2 / S3 门槛、显示请求越权或超限；均不改变结构。
追根因解决：写入后主表与索引不一致、同一许可内候选无法复核、失效 / 重挂 / 逻辑删除 / 撤销 / 批量失效留下旧索引项、结果与全表参考模型不一致。
```

## 关键边界

```text
关系仓库是树和图的权威结构承载；二级索引只是关系仓库内部派生候选结构。
通用索引仓库继续只负责领域入口候选，不接管关系事实。
世界、概念、需求、任务、方法不得建立互不相通的身份体系。
普通父子当前单父约束不外推到概念上下位等允许多父的关系类型。
六类控制面板视图是显示分类，不是六棵物理树。
#257 正式基线只证明指定机器与夹具的全表候选增长，不自动证明生产运行饱和、锁竞争瓶颈或分片必要性。
当前删除只推进关系表记录为已删除，不物理移除权威记录；二级索引不得改写这一生命周期事实。
旧带令牌删除保持共享兼容，严格结构化删除继续要求独占；S1 不改变许可强度或事务 ABI。
JY-355 的查询收益与专项正确性是草案证据，不是已发布实现；失效 / 删除写入回归不得由吞吐收益抵消。
JY-356 只拆分验收证据归属，不放宽 150% 写入上限、15% 三轮稳定门槛或 800 登记条件。
```
