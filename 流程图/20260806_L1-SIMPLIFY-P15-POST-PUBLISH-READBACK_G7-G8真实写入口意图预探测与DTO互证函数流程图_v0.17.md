# L1-SIMPLIFY-P15 G7/G8 真实写入口意图预探测与 DTO 互证函数流程图 v0.17

更新时间：2026-08-06

依据：P15 v0.17 详细设计；正式 HEAD `aaf7558116d692c336b618d9de607da93d221a5c`。本图是待施工合同，不是现状实现图。

## 真实入口绑定

```text
T01 领域/服务.L1状态.ixx::建立登记(L1状态登记请求)                         #0x15000601
T02 领域/服务.L1状态.ixx::发布实际存在I64首个基准状态(实际存在I64首个基准状态发布请求) #0x15000602
M01 领域/服务.特征比较.ixx::建立登记(特征比较登记请求)                       #0x15000801
M02 领域/服务.特征比较.ixx::登记I64目标判断比较合同(I64目标判断比较合同登记请求)     #0x15000802
M03 领域/服务.特征比较.ixx::登记I64状态迁移比较合同(I64状态迁移比较合同登记请求)     #0x15000803
```

## 主流程

```mermaid
flowchart TD
 A["真实写入口 DTO"] --> B{"G7 或 G8?"}
 B -->|T01| T1["编码 L1状态登记请求"]
 B -->|T02| T2["编码 首个基准状态发布请求"]
 B -->|M01| M1["编码 特征比较登记请求"]
 B -->|M02| M2["编码 目标判断合同登记请求"]
 B -->|M03| M3["编码 状态迁移合同登记请求"]
 T1 --> H["固定域前缀/格式1/标签/公共大端原语"]
 T2 --> H
 M1 --> H
 M2 --> H
 M3 --> H
 H --> S["SHA-256 领域意图摘要"]
 S --> P["provider 前探测幂等"]
 P -->|同义| R["账/首次结果 DTO 读回，provider=0/0"]
 P -->|异义/错误| X["冲突或入口拒绝，无业务载荷"]
 P -->|未找到| Q["读取 provider/事实"]
 Q --> G["独立 G0 写集摘要"]
 G --> V{"组/版本/标签/摘要互证"}
 V -->|否| X
 V -->|是| C["核心保留调用方摘要，不得覆盖"]
 C --> W["一次 L1 v2 提交与发布后读回"]
 W --> A2["首次结果 DTO 映射/恢复双摘要重算"]
 A2 -->|矛盾| I["内部不一致并隔离"]
 A2 -->|一致| O["成功结果"]
```

## G7/G8 字段和恢复分支

```mermaid
flowchart LR
 T01["T01: 合同/规则/状态幂等/期望代次"] --> T01R["L1状态登记结果"]
 T02["T02: 合同/规则/幂等/代次/当前特征/时间戳"] --> T02R["实际存在I64基准状态结果"]
 M01["M01: 比较合同/规则/幂等/期望代次"] --> M01R["特征比较登记结果"]
 M02["M02: 比较合同/规则/幂等/代次/定义登记/定义读取/实例登记"] --> M02R["目标判断比较合同结果"]
 M03["M03: 比较合同/规则/幂等/代次/定义登记/定义读取/实例登记"] --> M03R["状态迁移比较合同结果"]
 T01R --> Z["首次账/审计/发布后读回按同一点重算"]
 T02R --> Z
 M01R --> Z
 M02R --> Z
 M03R --> Z
 Z -->|DTO/标签/摘要不等| E["拒绝或隔离"]
 Z -->|完全一致| Y["允许恢复候选"]
```

M02/M03 虽然嵌套字段形状相近，固定标签不同且摘要不同；读取合同 DTO 和业务比较 DTO 不进入首次写意图编码。

## 验证门禁

机械验证 T01/T02/M01/M02/M03 的入口签名、固定标签、字段序列、结果 DTO、同义/异义向量和恢复拒绝；G1—G6、32/18/50、16/50、24 文件及 G0 独立互证保持 v0.16。
