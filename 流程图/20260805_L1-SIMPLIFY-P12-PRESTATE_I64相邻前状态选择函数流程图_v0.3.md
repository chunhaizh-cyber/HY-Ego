# L1-SIMPLIFY-P12-PRESTATE I64 相邻前状态选择函数流程图 v0.3

对应详细设计：`规范/详细设计/20260805_L1-SIMPLIFY-P12-PRESTATE_I64相邻前状态选择详细设计_v0.3.md`

正式基线：`06012409536b52b0d79ce66d34640cde507f22da`

```mermaid
flowchart TD
    A[校验请求] --> B[读取起始快照G0]
    B --> C{解析主体端点}
    C -- 当前存在 --> D{解析实例槽端点}
    C -- 后来退出 --> D
    C -- 从未存在 --> X1[未找到 空载荷]
    C -- 矛盾/损坏 --> X2[内部不一致 空载荷]
    D -- 当前存在 --> E[扫描关系19候选]
    D -- 后来退出 --> E
    D -- 从未存在 --> X1
    D -- 矛盾/损坏 --> X2
    E --> F{严格更早候选}
    F -- 空 --> G[读取结束快照G1]
    F -- 同时间多状态 --> X3[冲突 空载荷]
    F -- 唯一最大 --> H[构造P9读取请求]
    H --> I[P9当前/历史互证读取]
    I --> J{身份/生命周期/截止一致?}
    J -- 否 --> X4[映射非成功或内部不一致]
    J -- 是 --> G
    G --> K{G0/P9/G1同代次?}
    K -- 否且未重试 --> B
    K -- 否且已重试 --> X5[版本漂移 空载荷]
    K -- 是且无候选 --> X6[无前状态 完整零前态证据]
    K -- 是且有候选 --> X7[已选择 完整前状态和三态见证]
```

端点解析只允许 L1 当前节点读取、历史事实读取及生命周期字段互证；禁止审计、SQL、日志、缓存和无条件历史扫描。顺序 360 既有状态自检追加三态矩阵，不新增顺序 370。
