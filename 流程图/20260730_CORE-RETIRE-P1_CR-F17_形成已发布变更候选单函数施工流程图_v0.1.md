# CR-F17 形成已发布变更候选单函数施工流程图

版本：v0.1
创建侧代码基线：`57866ac5ca63235ed12da60f635d29f1aacd718b`

## 函数合同

```text
根函数：正式关系仓库::形成已发布变更候选_
归属：海中鱼巣.核心.仓库.正式关系 / 海中鱼巣/核心/仓库.正式关系.ixx
现有或待新增：现有类私有成员改造
完整签名：正式关系写入结果 正式关系仓库::形成已发布变更候选_(关系句柄 关系, 节点句柄 新源节点, 节点句柄 新目标节点, std::uint64_t 事务序号, 正式关系候选种类 种类)
参数：均值传入；事务非零；种类只允许失效或重挂；重挂端点须在同事务节点视图有效
返回：具名写入状态、当前关系和唯一移动候选；拒绝/内部不一致零候选、零发布事实变化；本函数不新增正式关系“资源失败”状态
所有权：已发布记录和独立候选槽归仓库；移动候选归会话；锁不逸出
结果语义：发布基线保持原位；写后只进入 optional 候选槽，并绑定该条目的候选事务序号；失效入口按4010同时接受本次失效写前句柄和其写后审计句柄的精确幂等重放
```

```mermaid
flowchart TD
    N01{"N01【指令-判断】仓库、关系句柄、事务和候选种类有效"} -- "否" --> R01["R01【指令-返回】返回具名入口拒绝"]
    N01 -- "重挂" --> X01["X01【函数调用】std::optional<节点直接身份记录> 节点直接身份仓库::读取节点(节点句柄 节点, std::uint64_t 事务序号) const：实参[新源节点,事务序号]→形参[节点,事务序号]；返回→源记录"]
    X01 -- "完成" --> X02["X02【函数调用】std::optional<节点直接身份记录> 节点直接身份仓库::读取节点(节点句柄 节点, std::uint64_t 事务序号) const：实参[新目标节点,事务序号]→形参[节点,事务序号]；返回→目标记录"]
    X02 -- "完成" --> B01{"B01【指令-判断】两端点均存在且有效"}
    B01 -- "否" --> R01
    B01 -- "是" --> N02["N02【指令-操作】取得关系仓独占锁并定位已发布条目"]
    N01 -- "失效" --> N02
    N02 -- "完成" --> B02{"B02【指令-判断】条目存在、仓库身份匹配且已有发布基线"}
    B02 -- "否" --> R01
    B02 -- "是" --> B02A{"B02A【指令-判断】候选写后槽状态"}
    B02A -- "同事务、同种类；输入等于写前或写后句柄；失效无其它语义，重挂端点等于既有写后" --> R00["R00【指令-返回】返回幂等读回、既有写后关系和空候选"]
    B02A -- "其它事务、不同种类或不同写后语义" --> R01
    B02A -- "空槽且候选事务序号为零" --> B02B{"B02B【指令-判断】失效请求且发布基线已失效"}
    B02B -- "是；输入等于当前审计句柄或历史末项的本次失效写前句柄" --> R04["R04【指令-返回】返回已发布失效事实的幂等读回、当前审计句柄和空候选"]
    B02B -- "是；其它旧版本" --> R01
    B02B -- "否" --> B02C{"B02C【指令-判断】输入句柄等于发布基线且发布基线有效"}
    B02C -- "否" --> R01
    B02C -- "是；重挂端点与发布基线相同" --> R05["R05【指令-返回】返回已发布重挂后态的幂等读回、发布基线句柄和空候选"]
    B02C -- "是；需要形成新后态" --> B03{"B03【指令-判断】具名准入、基数、重挂许可和父链无环均成立"}
    B03 -- "否" --> R01
    B03 -- "是" --> N03["N03【指令-构造】由发布基线复制写后，版本加一并置失效或新端点"]
    N03 -- "完成" --> N04["N04【指令-写入】候选写后槽=写后；保持已有发布基线=true且发布基线原样；候选事务序号=事务序号"]
    N04 -- "完成" --> B04{"B04【指令-判断】移动候选构造成功"}
    B04 -- "否" --> N05["N05【指令-写入】清候选写后和候选事务序号；保持已有发布基线=true及发布基线/历史原样"]
    N05 -- "完成" --> R02["R02【指令-返回】返回正式关系操作状态::内部不一致(15)，清候选槽/事务号且发布基线与历史不变"]
    B04 -- "是" --> R03["R03【指令-返回】返回已变更候选、当前写后句柄与移动候选"]
```

节点边界：候选形成期不得把写前推入历史或覆盖发布基线；候选槽检查先于发布基线版本拒绝，使同事务重复可以用本次写前或写后句柄回显同一写后。失效已经正式发布后，只有当前审计句柄或正式历史末项中与当前版本严格相邻、端点/类型/顺序号相同且状态有效的本次写前句柄可幂等读回；其它旧版本继续拒绝。父链检查调用 [CR-F29](./20260730_CORE-RETIRE-P1_CR-F29_子链包含节点已加锁单函数施工流程图_v0.1.md)。公开失效/重挂入口经 [CR-F58](./20260730_CORE-RETIRE-P1_CR-F58_登记关系候选单函数施工流程图_v0.1.md) 登记仓库结果。
