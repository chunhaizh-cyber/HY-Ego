# NODE-TYPED-MIGRATION NT-P2 反向读取、五角色身份与退役参与包施工流程图

更新时间：2026-07-24

## 施工图元数据

```text
图类型：施工流程图
绑定正式规范：4010、4040、4210、4220、7130
绑定计划：NT-P2 v0.4、#341 v0.6、#342 v0.6
绑定详细设计：P2B v0.5、P2C v0.5
允许文件：分别只取 #341 / #342 叶子白名单；本图不扩大任一白名单
禁止文件：核心、相邻领域、工程、入口、运行器、默认装配及非当前计划文件
预期结构变化：P2B 形成状态 / 动态完整事实和记录参与包；P2C 形成五身份 / 五关系、恢复验证和无私仓退役包
执行前复核：NT-C1Q/v0.1、累计父链、目标叶子 blob、提供者真实接口与文件所有权
验证方式：叶子合同静态检查；完整构建 / 运行 / 接线后置
不得宣称：接口已联调、已接线、已恢复、已切换或能力完成
```

## 流程图

```mermaid
flowchart TD
    A["取得状态 / 动态、自我初始化 / 读取 / 恢复验证或退役请求"] --> B{"NT-C1Q 与目标叶子真实接口吻合？"}
    B -->|"否"| R["DRIFT：停止受影响叶，退回精确合同所有者"]
    B -->|"是"| C["按目标 / 相关节点 / 类型角色读取当前关系值式副本"]
    C --> D{"P2B 还是 P2C？"}

    D -->|"P2B"| E["形成关系 19 / 20 具名角色证据和状态 / 动态类型化记录"]
    E --> E2{"普通读取、同代次冻结或记录退役？"}
    E2 -->|"读取 / 冻结"| P["返回完整值式材料"]
    E2 -->|"记录退役"| G["形成带真实记录参与者的 move-only 单次退役包"]
    G --> X["核心执行器 + 真实领域参与者"]

    D -->|"P2C"| F{"初始化、普通读取、恢复验证或历史自我退役？"}
    F -->|"初始化"| F1["一次形成五项互异身份"]
    F1 --> F2["登记五条固定关系：普通父子 3 + 归属 1 + 关系 21 角色 1"]
    F2 --> F3["关系 21 角色 2 另登记零到多成员，不计入五条固定关系"]
    F3 --> X
    F -->|"普通读取"| F4["连续同序采集两遍五身份 / 五关系、成员、槽位、状态、动态"]
    F4 --> F5{"逐字段相等？"}
    F5 -->|"否"| V["读取版本漂移；材料为空"]
    F5 -->|"是"| P
    F -->|"恢复验证"| F6["值式验证关系 0 / 1 / 21、成员 21 / 2、P2A 22、P2B 19 / 20"]
    F6 --> P
    F -->|"历史自我退役"| F7["形成无私仓参与者的 move-only 单次退役包"]
    F7 --> F8["包登记关系 21；#346 回调登记其它关系、索引和单一自我节点删除"]
    F8 --> X

    X --> J{"准备、确认与发布闭合？"}
    J -->|"提交前失败"| K["逆序撤销；零发布"]
    J -->|"已提交"| L["最后发布事实成立"]
    L --> M{"权威读回闭合？"}
    M -->|"否"| N["内部逻辑错误；停止依赖路径，不否认已发布事实"]
    M -->|"是"| P
```

## 关键边界

```text
匿名关系组退出；五项身份不复用；五条固定关系不含角色 2 成员。
P2B 退役包包含真实记录参与者；P2C 没有私有记录仓，禁止伪造空参与者。
P4 保存完整记录、历史和失效关系，不保存列表投影。
```
