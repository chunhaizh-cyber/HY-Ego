# 发布主动安全结算来源规则函数流程图 v0.1

更新时间：2026-08-30

## 依据

```text
规范/6100_子规范_安全服务值结算_20260720.md v0.6
规范/6120_子规范_自我存在服务与生存基本因果闭环_20260720.md v0.10
规范/详细设计/20260830_INSTINCT-STAGE3-ACTIVE-SAFETY-SOURCE-CATALOG_主动安全结算来源方法完整集合详细设计_v0.1.md
当前代码：海中鱼巣/领域/服务.安全根定义与当前值.ixx
```

## 身份与边界

本图是待新增公开函数的目标单函数流程图，只冻结来源规则发布；读取入口和生产初始化组合由详细设计定义。本函数不发布安全根定义、不建立方法、不解释变化账，也不改变 L/H 或 A。

## 函数合同

```text
根函数：安全根定义与当前值服务::发布主动安全结算来源规则_v1
归属模块 / 文件 / 层级：海中鱼巣.领域.服务.安全根定义与当前值 / 海中鱼巣/领域/服务.安全根定义与当前值.ixx / ARCH-L4
现有或待新增：待新增
完整签名：主动安全结算来源规则发布结果_v1 发布主动安全结算来源规则_v1(const 发布主动安全结算来源规则请求_v1& 请求) noexcept
参数：请求；const 输入借用；函数返回前有效；非法合同、G0、身份、版本、顺序或 v1 非空组拒绝
返回结果：强类型发布结果；只有已发布 / 精确重复携带完整事实和正式读回截止；已可能发布只携带提交见证
被调函数流程图：无；调用的 L1 owner 公开入口沿用其现行合同
```

## 流程图

```mermaid
flowchart TD
    N01["N01【指令-判断】请求合同、G0、幂等身份、锚点、版本、规范方法组是否有效；规则1是否为空组"]
    N01 -- "否" --> N02["N02【指令-返回】返回 入口拒绝，空事实与截止0"]
    N01 -- "是" --> N03["N03【函数调用】读取首次写入材料(幂等身份) -> 重放材料"]
    N03 -- "首次材料存在" --> N04{"N04【指令-判断】首次规范化请求是否与本请求逐字段相同"}
    N04 -- "否" --> N05["N05【指令-返回】返回 幂等冲突"]
    N04 -- "是" --> N06["N06【函数调用】按首次G1读回来源规则事实 -> 历史事实"]
    N06 --> N07{"N07【指令-判断】历史事实、首次结果和当前正式截止是否完整"}
    N07 -- "否" --> N08["N08【指令-返回】返回 已可能发布与首次G1"]
    N07 -- "是" --> N09["N09【指令-返回】返回 精确重复、完整事实、首次G1和Gread"]
    N03 -- "首次材料未找到" --> N10["N10【函数调用】读取中性当前事实代次() -> Gnow"]
    N10 --> N11{"N11【指令-判断】Gnow是否等于请求G0且锚点绑定完整"}
    N11 -- "否" --> N12["N12【指令-返回】返回 当前性漂移或引用冲突"]
    N11 -- "是" --> N13["N13【函数调用】确保来源规则登记(G0) -> 登记定位"]
    N13 -- "非成功" --> N14["N14【指令-返回】映射登记失败状态"]
    N13 -- "成功" --> N15["N15【函数调用】读取当前来源规则(G0) -> 当前规则"]
    N15 -- "已有当前" --> N16{"N16【指令-判断】当前规则是否与请求同义"}
    N16 -- "否" --> N17["N17【指令-返回】返回 版本漂移或引用冲突"]
    N16 -- "是" --> N18["N18【指令-返回】返回 精确重复与当前规则"]
    N15 -- "未发布" --> N19["N19【函数调用】所有者范围写入(新规则值 + 当前槽切换) -> 写入结果"]
    N19 -- "提交非成功" --> N20["N20【指令-返回】映射写入失败；可能提交时保留G1"]
    N19 -- "提交成功" --> N21["N21【函数调用】按G1正式读回来源规则 -> 读回事实"]
    N21 --> N22{"N22【指令-判断】读回事实逐字段一致且读后当前代次等于G1"}
    N22 -- "否" --> N23["N23【指令-返回】返回 已可能发布与G1"]
    N22 -- "是" --> N24["N24【指令-返回】返回 已发布、完整事实、G1与正式截止"]
```

## 节点合同

| 节点 | 类型 | 参数 / 输入绑定 | 结果 / 输出绑定 | 事实读写 | 后继条件 | 验证 |
| --- | --- | --- | --- | --- | --- | --- |
| N01—N02 | 指令 | `请求` | 入口拒绝 | 无 | 形状非法 | 坏版本、非空 v1、乱序重复 |
| N03—N09 | 调用 + 判断 | 幂等身份 | 首次材料、历史事实 | 只读 | 首次材料命中 | 精确重复、同键异义、坏首次材料 |
| N10—N14 | 调用 + 判断 | 请求 G0、锚点 | 当前代次、登记定位 | 登记缺失时写登记 | 同截止且登记完整 | 漂移、登记恢复、资源失败 |
| N15—N18 | 调用 + 判断 | G0、请求定义 | 当前规则 | 只读 | 已有当前 | 同义异键零第二版本、版本冲突 |
| N19—N20 | 函数调用 | 规范 U64 组与槽切换 | 写入结果 / G1 | 原子写来源规则值和当前槽 | 提交状态 | 写失败、已可能发布 |
| N21—N24 | 调用 + 判断 | G1、规则身份 | 正式读回事实 | 只读 | 完整且读后守卫成立 | 写后读回、读后漂移 |

## 关键边界

```text
规则版本1只允许显式空方法组。
规则未发布不等于空集。
未知方法不因“不在空集”而自动取得非主动来源结论。
本函数不改变安全根定义版本1及L/H。
```
