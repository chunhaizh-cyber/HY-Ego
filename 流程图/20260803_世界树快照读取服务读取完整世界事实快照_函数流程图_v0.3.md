# 世界树快照读取服务::读取完整世界事实快照 函数流程图 v0.3

日期：2026-08-03

设计身份：WORLD-TREE-P1C-Q / F-P1CQ-01

## 1. 身份与边界

```text
根函数：世界树快照读取服务::读取完整世界事实快照
归属模块：海中鱼巣.领域.服务.世界树快照
物理文件：海中鱼巣/领域/服务.世界树快照.ixx
层级：L2 世界组合读取服务
现有或待扩展：P1C-QF 代码结果中的最终骨架原位替换
完整签名：世界树读取结果 读取完整世界事实快照(const 世界树读取请求& 请求) const noexcept
参数：请求；const 借用；调用方拥有；函数不保存
返回：调用方按值拥有的世界树读取结果
```

本图只冻结根函数直接过程。三个 provider 的内部复杂过程分别由 P1A3、FEATURE-Q、P1B-Q 自有流程图承载。

## 2. 流程图

```mermaid
flowchart TD
    N01["N01【指令-判断】请求头合同版本、世界规则版本、世界根完整性与场景类型"] --> N02{"N02【指令-判断】入口是否合法"}
    N02 -- "否" --> R01["R01【指令-返回】入口拒绝；默认请求头；空快照与空缺项"]
    N02 -- "是" --> N03["N03【指令-赋值】已验证请求头=请求.头；轮次=1"]
    N03 --> N04["N04【函数调用】世界结构_.读取世界结构({合同1,请求.头}) -> 结构结果"]
    N04 --> N05{"N05【指令-判断】结构状态是否已读取且代次非零"}
    N05 -- "状态非成功" --> R02["R02【指令-返回】同名映射；回显已验证请求头；空快照；仅内部不一致可带原缺项"]
    N05 -- "已读取但代次0/成功材料矛盾" --> D01["D01【函数调用】安全记录快照读取逻辑异常_(说明)"]
    D01 --> R03["R03【指令-返回】内部不一致；回显已验证请求头；空快照"]
    N05 -- "已读取且非零" --> N06["N06【指令-形成】宿主组=结构结果全部存在身份；按稳定身份精确去重"]
    N06 --> N07["N07【函数调用】特征_.读取宿主组完整当前特征({合同1,请求.头,宿主组}) -> 特征结果"]
    N07 --> N08{"N08【指令-判断】特征状态是否已读取且代次非零"}
    N08 -- "状态非成功" --> R02
    N08 -- "已读取但代次0/成功材料矛盾" --> D01
    N08 -- "已读取且非零" --> N09["N09【指令-形成】场景组=全部场景身份；主体组=全部存在身份"]
    N09 --> N10["N10【函数调用】状态动态_.读取世界状态动态({合同1,请求.头,场景组,主体组}) -> 状态动态结果"]
    N10 --> N11{"N11【指令-判断】状态动态状态是否已读取且代次非零"}
    N11 -- "状态非成功" --> R02
    N11 -- "已读取但代次0/成功材料矛盾" --> D01
    N11 -- "已读取且非零" --> N12["N12【函数调用】世界结构_.读取当前事实代次() -> 末次结果"]
    N12 --> N13{"N13【指令-判断】末次状态是否已读取且代次非零"}
    N13 -- "状态非成功" --> R02
    N13 -- "已读取但代次0" --> D01
    N13 -- "已读取且非零" --> N14{"N14【指令-判断】结构、特征、状态动态、末次四代次是否完全相等"}
    N14 -- "否且轮次=1" --> N15["N15【指令-清除】丢弃本轮全部材料；轮次=2"]
    N15 --> N04
    N14 -- "否且轮次=2" --> R04["R04【指令-返回】版本漂移；回显已验证请求头；空快照与空缺项"]
    N14 -- "是" --> N16["N16【指令-复核】根、单父、无环、单位置、特征完整性、状态动态范围与端点"]
    N16 --> N17{"N17【指令-判断】是否存在重复、悬空、多解、同身份异内容或稳定缺项"}
    N17 -- "是" --> D02["D02【函数调用】安全记录快照读取逻辑异常_(说明)"]
    D02 --> R05["R05【指令-返回】内部不一致；共同非零代次；空快照；稳定缺项组"]
    N17 -- "否" --> N18["N18【指令-排序】按世界树稳定排序规则版本1排序五类事实"]
    N18 --> N19["N19【指令-构造】完整=true、共同代次、根和五类完整事实的快照"]
    N19 --> R06["R06【指令-返回】已读取；回显已验证请求头；完整快照；缺项为空"]
    E01["E01【指令-捕获】bad_alloc或length_error"] --> D03["D03【函数调用】安全记录快照读取逻辑异常_(说明)"]
    D03 --> R07["R07【指令-返回】资源失败；按入口是否已验证选择请求回执；空快照与缺项"]
    E02["E02【指令-捕获】其它异常"] --> D04["D04【函数调用】安全记录快照读取逻辑异常_(说明)"]
    D04 --> R08["R08【指令-返回】内部不一致；按入口是否已验证选择请求回执；空快照与缺项"]
```

## 3. 节点合同

| 节点 | 类型 | 输入绑定 | 输出绑定 | 事实读写 | 后继 / 验证 |
| --- | --- | --- | --- | --- | --- |
| N01—N03 | 本函数内指令 | `请求` | 已验证请求头、轮次 | 只读参数 | 非法入口零 provider 调用 |
| N04 | 函数调用 | `{节点直接世界结构读取合同版本, 请求.头}` | `结构结果` | 只读内存世界结构 | 每轮恰一次 |
| N06 | 本函数内指令 | `结构结果.存在组` | 唯一宿主组 | 只读局部值 | 不加入场景宿主 |
| N07 | 函数调用 | 请求头、宿主组 | `特征结果` | 只读内存特征 | 每轮恰一次；空宿主组仍合法调用并取得代次 |
| N09 | 本函数内指令 | 结构场景/存在 | 场景组、主体组 | 只读局部值 | 均稳定唯一 |
| N10 | 函数调用 | 请求头、场景组、主体组 | `状态动态结果` | 只读内存状态动态 | 每轮恰一次；合法零事实仍为已读取非零代次 |
| N12 | 函数调用 | 无参数 | `末次结果` | 只读内存事实代次 | 每轮恰一次 |
| N14—N15 | 本函数内指令 | 四代次、轮次 | 重试或版本漂移 | 丢弃局部材料 | 仅全成功代次不等可重试；最多两轮 |
| N16—N19 | 本函数内指令 | 四份同代次材料 | 完整快照或缺项 | 只读和局部构造 | 无 SQL/缓存/旧栈 |
| D01—D04 | 函数调用 | 本层首次发现说明 | 无机器返回材料 | 诊断旁路 | provider 已诊断非成功不重复记录 |
| R01—R08 | 本函数内返回 | 冻结分支材料 | `世界树读取结果` | 无机器事实变化 | 非成功绝不携带部分快照 |

## 4. 状态映射和关键边界

```text
provider 已读取 -> 继续
provider 未找到 -> 世界树业务状态::未找到
provider 入口拒绝 -> 世界树业务状态::入口拒绝
provider 版本漂移 -> 世界树业务状态::版本漂移
provider 许可拒绝 -> 世界树业务状态::许可拒绝
provider 资源失败 -> 世界树业务状态::资源失败
provider 内部不一致 -> 世界树业务状态::内部不一致；只可转交稳定缺项组
provider 未实现 -> 世界树业务状态::未实现
```

成功状态携带零代次属于 provider 合同矛盾，立即返回内部不一致，不作为代次漂移重试。只有四个状态全部为已读取、四代次均非零但不完全相等时，才执行一次完整重试。

```text
直接 provider 最大调用数（两轮）：结构读取2、特征读取2、状态动态读取2、末次代次读取2
SQL、材料、缓存、显示、旧世界服务、L1仓库、事务、许可传递、写入调用：0
```

## 5. 双向引用

上游业务图：`流程图/20260803_WORLD-TREE-P1C-Q_同代次完整快照真实读取业务流程图_v0.1.md`。

本函数被以下目标函数恰调用一次：

```text
世界树快照读取服务::读取存在完整事实
世界树快照读取服务::读取场景完整事实
```
