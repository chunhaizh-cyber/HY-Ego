# SAFETY-P1 F-S105 `计算单任务安全权限` 单函数施工流程图 v0.1

图类型：目标施工流程图
状态：现行设计依据；函数待 #377 实现，不表示代码已存在
归属模块：`海中鱼巣.领域.算法.安全任务权限`
归属文件：`海中鱼巣/领域/算法.安全任务权限.ixx`
唯一根函数：F-S105
完整签名：

```cpp
任务权限结果 计算单任务安全权限(
    const 安全任务事实来源载荷& 任务事实,
    const 权限读取载荷& 权限,
    const 任务权限请求& 请求) noexcept;
```

参数：三项只读借用；不保存引用。
返回：至少一个完整当前来源时返回 `已形成` 或 `权限为零` 完整载荷；身份非法为 `入口拒绝`；版本漂移为 `版本漂移`；来源材料缺失为 `材料缺失`；重复稳定键、目标不等价、负值或 checked 溢出为 `结构拒绝`；分配失败为 `资源失败`。
依据：6150 第 8、9、11、13、14 节；安全详细设计第 8、10、12、14 节。
验证：SP-A15、SP-A17。

## 流程图

```mermaid
flowchart TD
    S["S【本函数内具体指令】只读绑定任务事实、权限和请求"]
    B01{"B01【本函数内具体指令】请求自我、场景、任务句柄和安全层来源选择完整有序，图/事实/任务集合版本非零且规则版本等于1？"}
    R01["R01【本函数内具体指令】返回入口拒绝且无载荷"]
    B02{"B02【本函数内具体指令】任务集合、事实截止、图、值、阈值和权限规则版本兼容且当前？"}
    R02["R02【本函数内具体指令】返回版本漂移且无载荷"]
    N03["N03【本函数内具体指令】选择请求任务的当前关联和执行候选并复核稳定键唯一"]
    B04{"B04【本函数内具体指令】执行候选与至少一条当前目标等价来源关联完整？"}
    R04["R04【本函数内具体指令】返回材料缺失且无载荷"]
    N05["N05【本函数内具体指令】逐来源按精确组读取对应安全权限并形成安全任务来源权限"]
    B06{"B06【本函数内具体指令】任一来源处于材料缺口或缺少当前权限？"}
    R06["R06【本函数内具体指令】返回材料缺失且无载荷"]
    N07["N07【本函数内具体指令】取全部来源权限最大值为任务最高权限并保留全部并列最高来源"]
    N08["N08【本函数内具体指令】按需求句柄、具体安全结果、精确深度和规范化路径选择主来源"]
    N09["N09【本函数内具体指令】用checked wide integer计算floor基础优先级乘最高权限除1000000"]
    N10["N10【本函数内具体指令】形成任务权限载荷并稳定排序全部来源与并列来源"]
    B11{"B11【本函数内具体指令】最高权限等于0？"}
    R11["R11【本函数内具体指令】返回权限为零和完整载荷"]
    R12["R12【本函数内具体指令】返回已形成和完整载荷"]
    E13["E13【本函数内具体指令】捕获分配异常并返回资源失败且无载荷"]
    R13["R13【本函数内具体指令】重复键、负值、目标不等价或checked溢出时返回结构拒绝且无载荷"]

    S --> B01
    B01 -- "否" --> R01
    B01 -- "是" --> B02
    B02 -- "否" --> R02
    B02 -- "是" --> N03 --> B04
    B04 -- "否" --> R04
    B04 -- "是" --> N05 --> B06
    B06 -- "是" --> R06
    B06 -- "否" --> N07 --> N08 --> N09 --> N10 --> B11
    B11 -- "是" --> R11
    B11 -- "否" --> R12
    N03 -. "坏结构" .-> R13
    N05 -. "坏结构" .-> R13
    N09 -. "溢出" .-> R13
    N07 -. "分配异常" .-> E13
    N10 -. "分配异常" .-> E13
```

## 节点合同

| 节点 | 类型 | 输入 | 输出 | 事实读写 | 后继与验证 |
| --- | --- | --- | --- | --- | --- |
| S—B02 | 本函数内具体指令 | 请求自我、场景、任务、选择组与全部版本 | 入口/版本准入 | 零写入 | 版本只做门禁；不得从任务事实反推首次请求 |
| N03—R06 | 本函数内具体指令 | 任务候选、关联、组权限 | 完整来源组或材料缺失 | 零写入 | 目标等价、来源当前；SP-A15 |
| N07—N10 | 本函数内具体指令 | 全部合法来源权限、基础优先级 | 最高权限、主来源、有效优先级 | 零写入 | 取最高不求和；SP-A15 |
| B11—R12 | 本函数内具体指令 | 完整载荷 | 成功分类 | 零写入 | 零权限不删除任务；SP-A17 |
| E13/R13 | 本函数内具体指令 | 异常或坏结构 | 无载荷失败 | 零写入 | `noexcept` 边界 |

## 实现约束

1. 任务可以保留多个来源，但目标语义必须等价；不得合并为无来源汇总。
2. `权限为零` 只退出普通竞争，不改变任务、需求、筹办、材料或观察事实。
3. 任务有效执行优先级使用非负 I64 与 checked wide integer，不强行补 1。
