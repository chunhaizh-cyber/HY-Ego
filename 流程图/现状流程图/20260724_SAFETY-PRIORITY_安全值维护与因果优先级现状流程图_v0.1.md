# SAFETY-PRIORITY：安全值维护与因果优先级现状流程图 v0.1

更新时间：2026-07-24

## 依据

- `规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md`
- `规范/6100_子规范_安全服务值结算_20260720.md`
- `规范/6120_子规范_自我存在服务与生存基本因果闭环_20260720.md`
- `规范/8100_子规范_自我线程与任务管理线程权责边界_20260720.md`
- `规范/8200_子规范_自我内部循环实现_20260720.md`
- `海中鱼巣/领域/初始化.需求.ixx`
- `海中鱼巣/线程/自我线程.ixx`
- `海中鱼巣/线程/任务管理线程.ixx`
- `海中鱼巣/线程/任务工作线程.ixx`
- `海中鱼巣/线程/自我治理领域路由.ixx`
- `海中鱼巣/线程/自检.自我治理回执.ixx`
- `海中鱼巣/线程/自检.自我治理多轮.ixx`
- `海中鱼巣/入口.cpp`

## 现状元数据

| 项 | 当前事实 |
| --- | --- |
| 图类型 | 当前代码事实图；不是目标施工流程 |
| 扫描基线 | `main@a2acebb01ddc10f1b72f79ad4cfc756a65aa3ccf`；本图涉及的代码文件相对前序设计提交无变化 |
| 已有结构 | 自我、内部世界、根安全需求、根服务需求、强类型治理消息和强类型任务执行请求队列 |
| 当前调度 | 任务管理 / 工作线程模块支持强类型请求按 `deque` 的 `front/pop_front` 顺序消费，但生产 `入口.cpp` 未装配该运行链 |
| 当前安全维护 | 没有按自我线程循环调用的分层安全值维护入口 |
| 固定安全路由 | `自我治理领域路由` 的固定安全根链由专项自检装配调用，不能视为生产分层安全维护 |
| 当前缺口 | 无分层阈值、有效未复发时间、因素排除聚合、因果深度组、递归权限和任务权限投影 |
| 不得宣称 | 根安全初始化、自检链或 FIFO 队列不证明安全值被动变化及安全任务优先级门控已接通 |

## 身份与边界

本图只描述当前代码。线程、消息、队列顺序、自检报告、日志和显示都不是安全事实或任务优先级权限；固定安全根自检链也不是生产调度链。

```mermaid
flowchart TD
    A["入口装配存在、场景、特征、需求和线程服务"] --> B["需求初始化建立安全值与服务值语素、两个根需求及初始关系"]
    B --> C["当前只有根安全需求；没有逐层安全值、低位阈值和高位阈值结构"]
    C --> D["入口使用三参数构造自我线程：未注入治理邮箱和治理处理器"]
    D --> E["启动自我线程、完成语素/世界树/两个根需求初始化并发布初始化快照"]
    E --> F["线程状态进入运行中，随后只等待停止请求"]
    F -. "当前生产没有此调用" .-> GAP1["缺口：每轮安全维护评估、单调时间和有效未复发证据"]

    C -. "类能力但生产未装配" .-> V0["五参数构造可选注入治理邮箱和处理器"]
    V0 --> V1["仅该可选构造进入消息批次治理循环"]
    V1 --> V2{"本轮是否取得可处理消息？"}
    V2 -- "否" --> V3["继续等待；不形成安全值变化"]
    V2 -- "是" --> V4["锁外调用既有治理处理器"]
    V4 --> V5["该能力未被当前入口实例采用，不能视为生产路径"]
    V5 -. "不得升级为实现事实" .-> GAP1

    J["任务管理模块可确认强类型执行请求"] --> K["请求编号 push_back 到可消费 deque"]
    K --> L["工作线程按 front/pop_front 取出请求"]
    L --> M["推进绑定任务工作包并返回强类型回执"]
    M --> N["模块内顺序是 FIFO；当前入口未装配该链，也未读取安全层级权限投影"]
    N -. "当前没有此结构" .-> GAP2["缺口：因果最短深度组、分层递归权限、基础优先级合成和确定性并列"]

    O["固定安全根领域路由"] --> P["建立固定根需求、方法、动作、结算和观察上下文"]
    P --> Q["由自我治理闭环与多轮专项自检显式调用"]
    Q --> R["当前生产自我线程未装配该路由为周期安全维护入口"]
    R -. "不得升级为实现事实" .-> GAP3["固定安全自检链不是生产分层安全维护或任务权限门控"]

    GAP1 --> Z["当前结论：安全事实可以由既有领域入口承载，但被动维护与调度消费尚未接通"]
    GAP2 --> Z
    GAP3 --> Z
```

## 关键边界

1. 根安全需求已初始化，不等于存在多层安全需求及各层阈值。
2. 当前生产入口使用三参数自我线程构造，初始化后只等待停止；五参数消息批次治理能力存在但未被生产入口装配，更没有“每轮必调、但不保证数值变化”的安全维护服务调用。
3. 强类型任务执行请求队列模块按确认顺序消费，但当前生产入口未装配该链；模块本身也不读取安全值、因果深度或任务执行优先级权限。
4. 固定安全根领域路由的调用证据位于专项自检装配；导入模块或自检通过不能证明生产接线。
5. 当前代码没有把时间、循环次数或队列状态写成安全事实，这一禁止边界必须保留。
6. 现状中的材料不足、无消息和无请求是逻辑内返回；前置成立后的治理处理内部不一致必须停止并追根因。
