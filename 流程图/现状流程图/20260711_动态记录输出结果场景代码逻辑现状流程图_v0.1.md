# 动态记录输出结果场景代码逻辑现状流程图

更新时间：2026-07-11

## 元数据

```text
图类型：现状流程图
对应施工流程图：流程图/20260708_动态记录输出结果场景代码逻辑流程图_v0.1.md
代码版本：3f08239；基于当前脏工作区
实现状态：已实现或已形成第一轮入口
覆盖文件：海中鱼巣/核心/节点仓库.cpp、海中鱼巣/领域/动态服务.h、海中鱼巣/领域/方法服务.h、海中鱼巣/领域/状态服务.h
覆盖函数：动态服务::记录实例动态、状态服务::创建实例状态、节点仓库::创建节点、动态服务::读取动态材料、动态服务::记录状态变化动态、状态服务::读取状态材料、方法服务::读取方法结果场景、方法服务::读取动作输出规格、动态服务::动态证据材料是否完整
逐行映射表：实施记录/现状流程图核查/20260711_动态记录输出结果场景代码逻辑现状流程图_v0.1配套核查表.md 第 2 节
输入契约 / 调用语境表：实施记录/现状流程图核查/20260711_动态记录输出结果场景代码逻辑现状流程图_v0.1配套核查表.md 第 3 节
非成功返回二分审查表：实施记录/现状流程图核查/20260711_动态记录输出结果场景代码逻辑现状流程图_v0.1配套核查表.md 第 4 节
偏差清单：实施记录/现状流程图核查/20260711_动态记录输出结果场景代码逻辑现状流程图_v0.1配套核查表.md 第 5 节
不得作为施工许可：是
不得宣称：未匹配节点、未来候选或施工目标已经实现
```

## 现状说明

当前代码存在对应入口；完成范围仍以实施记录和现状边界为准。
本图与根目录施工图并存；施工图回答准备怎样实现，本图只回答当前代码实际存在什么入口和边界。

## 流程图

```mermaid
flowchart TD
    CURRENT_SCOPE["现状基线：已实现或已形成第一轮入口；未来/候选节点不视为当前已实现"] --> A
    A["上游：方法执行动作入口流程 / 特征状态材料 / 动作动态证据请求"] --> B["进入动态记录与输出结果场景边界"]
    B --> C{"入口类型"}

    C -- "直接记录实例动态" --> D1["动态服务::记录实例动态(状态服务, 场景, 存在, 时间戳, 被改变目标, 前值, 后值, 来源动作)"]
    D1 --> D2{"场景为场景节点，存在为存在节点，时间戳有效"}
    D2 -- "否" --> DR1["入口拒绝：不创建动态 / 不创建前后状态"]
    D2 -- "是" --> D3{"被改变目标有效，前值和后值均存在"}
    D3 -- "否" --> DR2["入口拒绝：缺目标或前后值"]
    D3 -- "是" --> D4{"来源动作为空或动作入口角色节点"}
    D4 -- "否" --> DR3["入口拒绝：来源动作非法"]
    D4 -- "是" --> D5["状态服务::创建实例状态 生成改变前状态和改变后状态"]
    D5 --> D6{"前后状态材料读回：同场景、同主体、值和时间戳匹配"}
    D6 -- "否" --> DR4["追根因解决：不声明动态有效"]
    D6 -- "是" --> D7["创建动态主信息并写发生时间戳"]
    D7 --> D8["节点仓库::创建节点(动态)"]
    D8 --> D9["写关系：场景 运行期临时 动态"]
    D9 --> D10["写关系：动态 引用 主体存在；动态 引用 发生场景"]
    D10 --> D11["写关系：动态 因果来源 被改变目标 / 前状态 / 后状态"]
    D11 --> D12{"来源动作有效"}
    D12 -- "是" --> D13["写关系：动态 因果来源 来源动作入口"]
    D12 -- "否" --> D14["保持来源动作为空"]
    D13 --> D15["动态服务::读取动态材料 读回验证"]
    D14 --> D15
    D15 --> D16{"动态材料完整且与输入一致"}
    D16 -- "否" --> DR5["追根因解决：后续施工计划必须补追根因解决收口和数量快照"]
    D16 -- "是" --> D17["输出实例动态证据材料"]

    C -- "记录状态变化动态" --> S1["动态服务::记录状态变化动态(状态服务, 前状态, 后状态, 被改变目标, 来源动作)"]
    S1 --> S2["状态服务::读取状态材料(前状态 / 后状态)"]
    S2 --> S3{"前后状态材料均完整"}
    S3 -- "否" --> SR1["入口拒绝：不创建动态"]
    S3 -- "是" --> S4{"主体一致且场景一致"}
    S4 -- "否" --> SR2["入口拒绝：跨主体或跨场景"]
    S4 -- "是" --> S5{"被改变目标有效，来源动作为空或动作入口角色"}
    S5 -- "否" --> SR3["入口拒绝：目标或来源动作非法"]
    S5 -- "是" --> S6["使用后状态时间戳创建动态节点"]
    S6 --> S7["写同组动态关系：场景临时、主体引用、场景引用、目标、前状态、后状态、可选来源动作"]
    S7 --> S8{"动态材料读回成立"}
    S8 -- "否" --> SR4["追根因解决：不声明状态变化动态有效"]
    S8 -- "是" --> S9["输出状态变化动态材料"]

    C -- "无状态服务重载" --> V1["动态服务::记录实例动态(场景, 存在, 时间戳, 目标, 前值, 后值, 来源动作)"]
    V1 --> V2{"基础入参和来源动作均有效"}
    V2 -- "否" --> VR1["入口拒绝：不创建动态"]
    V2 -- "是" --> V3["创建值材料状态：前值状态 / 后值状态"]
    V3 --> V4["写实例动态和同组关系"]
    V4 --> V5{"读取动态材料可读出前后 I64 值"}
    V5 -- "否" --> VR2["追根因解决：不声明动态有效"]
    V5 -- "是" --> V6["输出：值材料型动态证据"]
    V6 --> VG["边界：值材料状态不等于完整实例状态材料，若需完整主体 / 场景 / 时间戳状态材料，应走状态服务路径"]

    C -- "输出结果场景材料边界" --> O1["读取相邻场景材料"]
    O1 --> O2["动态材料中的场景 = 动态发生场景"]
    O1 --> O3["方法服务::读取方法结果场景 = 方法结构规格场景"]
    O1 --> O4["方法服务::读取动作输出规格 = 动作入口输出规格场景"]
    O2 --> O5{"当前是否存在专门的动态输出结果场景关系"}
    O3 --> O5
    O4 --> O5
    O5 -- "否，当前代码事实" --> OG1["只可作为相邻材料读取；不得声明已写输出结果场景事实"]
    O5 -- "后续专项确认后" --> OG2["必须明确写入方、关系类型、唯一性、生命周期和读回验证"]
    OG1 --> OG3["输出给第 13 项前，不能直接写任务实际结果状态"]
    OG2 --> OG4["后续可进入任务回执 / 实际结果状态 / 结果回写设计"]

    C -- "动态材料读取与候选" --> Q1["动态服务::读取动态材料(动态)"]
    Q1 --> Q2{"动态是实例动态"}
    Q2 -- "否" --> QR1["返回空：非实例动态"]
    Q2 -- "是" --> Q3["读取唯一发生场景、主体、被改变目标、前值、后值、可选来源动作和发生时间戳"]
    Q3 --> Q4{"必填材料完整，来源动作为空或动作入口角色"}
    Q4 -- "否" --> QR2["返回空：动态材料不完整或来源动作非法"]
    Q4 -- "是" --> Q5["输出动态材料只读视图"]
    Q5 --> Q6["动态服务::动态证据材料是否完整"]
    Q5 --> Q7["动态服务::读取动态来源动作"]
    Q5 --> Q8["动态服务::读取动态聚合候选 / 读取运动基元候选"]
    Q8 --> Q9["候选只表达变化量和材料可用性，不裁决任务完成、方法成功或稳定因果"]

    D17 --> R0["只读输出方：任务服务 / 因果服务 / 显示层只读 / 后续结果回写流程"]
    S9 --> R0
    V6 --> R0
    OG3 --> R0
    OG4 --> R0
    Q6 --> R0
    Q7 --> R0
    Q9 --> R0
    DR1 --> R0
    DR2 --> R0
    DR3 --> R0
    DR4 --> R0
    DR5 --> R0
    SR1 --> R0
    SR2 --> R0
    SR3 --> R0
    SR4 --> R0
    VR1 --> R0
    VR2 --> R0
    QR1 --> R0
    QR2 --> R0

    R0 --> RV["最小读回验证门禁"]
    RV --> RV1["动态路径：读回动态节点、发生场景、主体、发生时间戳、被改变目标、前状态、后状态和来源动作"]
    RV --> RV2["状态路径：读回前后状态的值、主体、场景和发生时间戳；值材料状态须单独标注边界"]
    RV --> RV3["场景路径：读回场景到动态运行期临时关系、动态到发生场景引用关系"]
    RV --> RV4["输出结果场景路径：当前只校验相邻材料；若后续新增专门关系，必须读回该关系和唯一性"]
    RV1 --> RV5{"各路径读回满足归属、唯一性和材料完整性"}
    RV2 --> RV5
    RV3 --> RV5
    RV4 --> RV5
    RV5 -- "否" --> RVF["不得声明动态记录有效；后续详细设计 / 施工计划必须补追根因解决收口、数量快照和半结构不可读验证"]
    RV5 -- "是" --> OK["输出给第 13 项：任务回执 / 实际结果状态 / 结果回写流程"]

    B --> G1["当前代码差距"]
    G1 --> G2["没有专门的动态输出结果场景关系写入口"]
    G2 --> G3["动态发生场景、方法结果场景和动作输出规格场景当前不能自动合并为权威结果场景"]
    G3 --> G4["动态聚合候选和运动基元候选只读材料，不是稳定抽象动态、任务结果或方法成功"]
    G4 --> G5["实例动态删除、失效、升格、衰减和淘汰仍是后续治理"]
    G5 --> G6["多步写入已有入口拒绝和读回判定，但尚未证明完整事务回滚或数量快照级半结构不可读"]

    B --> X1["排除项"]
    X1 --> X2["线程 / 函数名 / 动作文本 / 日志标签 / 控制台命令不得成为来源动作"]
    X1 --> X3["日志、显示、SQL、控制面板不得写动态、状态或结果场景事实"]
    X1 --> X4["本图不写任务实际结果状态、不写需求结算、不创建稳定因果结论"]
    X1 --> X5["D455、体素、外设、真实本能动作执行器不从本图进入实施"]
```

## 完成边界

本图是当前代码证据基线，不是代码实施许可。当前存在未提交代码时，只能解释为当前工作区事实；未实现和部分实现节点不得扩大为系统完成。
