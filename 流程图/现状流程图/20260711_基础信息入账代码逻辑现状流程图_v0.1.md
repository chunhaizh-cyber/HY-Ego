# 基础信息入账代码逻辑现状流程图

更新时间：2026-07-11

## 元数据

```text
图类型：现状流程图
对应施工流程图：流程图/20260708_基础信息入账代码逻辑流程图_v0.1.md
代码版本：3f08239；基于当前脏工作区
实现状态：已实现或已形成第一轮入口
覆盖文件：海中鱼巣/领域/世界服务.h、海中鱼巣/领域/动态服务.h、海中鱼巣/领域/因果服务.h、海中鱼巣/领域/场景服务.h、海中鱼巣/领域/存在服务.h、海中鱼巣/领域/状态服务.h
覆盖函数：世界服务::创建基础信息、存在服务::创建实际存在、场景服务::创建场景、状态服务::创建实例状态、状态服务::创建抽象状态、动态服务::记录实例动态、动态服务::创建抽象动态、因果服务::记录因果引用
逐行映射表：实施记录/现状流程图核查/20260711_基础信息入账代码逻辑现状流程图_v0.1配套核查表.md 第 2 节
输入契约 / 调用语境表：实施记录/现状流程图核查/20260711_基础信息入账代码逻辑现状流程图_v0.1配套核查表.md 第 3 节
非成功返回二分审查表：实施记录/现状流程图核查/20260711_基础信息入账代码逻辑现状流程图_v0.1配套核查表.md 第 4 节
偏差清单：实施记录/现状流程图核查/20260711_基础信息入账代码逻辑现状流程图_v0.1配套核查表.md 第 5 节
不得作为施工许可：是
不得宣称：未匹配节点、未来候选或施工目标已经实现
```

## 现状说明

当前代码存在对应入口；完成范围仍以实施记录和现状边界为准。
本图与根目录施工图并存；施工图回答准备怎样实现，本图只回答当前代码实际存在什么入口和边界。

## 流程图

```mermaid
flowchart TD
    CURRENT_SCOPE["现状基线：已实现或已形成第一轮入口；未来/候选节点不视为当前已实现"] --> A
    A["外部材料 / 上游服务请求"] --> B["降权为基础信息入账请求"]
    B --> C{"入账类型"}
    C -- "基础信息通用节点" --> W["世界服务::创建基础信息 兼容壳"]
    C -- "实际存在" --> E["存在服务::创建实际存在"]
    C -- "场景" --> S["场景服务::创建场景"]
    C -- "场景存在 / 接纳存在" --> SE["世界服务或场景服务接纳存在"]
    C -- "实例状态" --> IS["状态服务::创建实例状态"]
    C -- "抽象状态" --> AS["状态服务::创建抽象状态"]
    C -- "实例动态" --> ID["动态服务::记录实例动态"]
    C -- "抽象动态" --> AD["动态服务::创建抽象动态"]
    C -- "二次特征" --> BF["二次特征服务::创建二次特征或组合二次特征"]
    C -- "因果引用基础身份" --> CB["因果服务::记录因果引用()"]
    C -- "轻量因果引用(来源动态)" --> CL["因果服务::记录因果引用(来源动态, 动态服务)"]
    C -- "特征 / 特征值 / 特征状态材料" --> NEXT["转第 4 项：特征与状态材料流程"]
    C -- "需求 / 任务 / 方法业务节点" --> ADV["转高级服务流程，不在基础信息入账中创建"]

    W --> W1["主信息仓库创建主信息"]
    W1 --> W2["节点仓库创建 基础信息 节点"]
    W2 --> RV["最小读回验证：节点 / 主信息 / 必需关系或索引可读"]

    E --> E1["主信息仓库创建主信息"]
    E1 --> E2["节点仓库创建 存在 节点"]
    E2 --> RV

    S --> S1["主信息仓库创建主信息"]
    S1 --> S2["节点仓库创建 场景 节点"]
    S2 --> RV

    SE --> SE1{"场景有效 && 存在有效"}
    SE1 -- "否" --> R["拒绝：来源追溯 / 结构不变化"]
    SE1 -- "是" --> SE2["关系仓库创建 场景-存在 归属关系"]
    SE2 --> RV

    IS --> IS1{"场景有效 && 存在有效 && 时间戳可用"}
    IS1 -- "否" --> R
    IS1 -- "是" --> IS2["主信息写状态值和发生时间戳"]
    IS2 --> IS3["节点仓库创建 状态 节点"]
    IS3 --> IS4["关系仓库写运行期临时 / 主体引用 / 场景引用"]
    IS4 --> RV

    AS --> AS1["主信息写状态值"]
    AS1 --> AS2["节点仓库创建 状态 节点"]
    AS2 --> RV

    ID --> ID1{"场景有效 && 存在有效 && 时间戳可用 && 被改变目标有效 && 前后值齐全 && 来源动作有效或为空"}
    ID1 -- "否" --> R
    ID1 -- "是" --> ID2["主信息写动态发生时间戳"]
    ID2 --> ID3["节点仓库创建 动态 节点"]
    ID3 --> ID4["状态服务创建前值状态材料"]
    ID4 --> ID5["状态服务创建后值状态材料"]
    ID5 --> ID6["关系仓库写运行期临时 / 主体 / 场景 / 目标 / 前值 / 后值 / 可选来源动作关系"]
    ID6 --> RV

    AD --> AD1["主信息写抽象动态材料"]
    AD1 --> AD2["节点仓库创建 动态 节点"]
    AD2 --> RV

    BF --> BF1{"无组成项 或 组成项类型不允许"}
    BF1 -- "是" --> R
    BF1 -- "否" --> BF2["主信息仓库创建主信息"]
    BF2 --> BF3["节点仓库创建 二次特征 节点"]
    BF3 --> BF4["可选写组成项引用关系"]
    BF4 --> RV

    CB --> CB1["主信息仓库创建主信息"]
    CB1 --> CB2["节点仓库创建 因果引用 节点"]
    CB2 --> RV

    CL --> CL1{"来源动态有效 && 动态材料准入通过"}
    CL1 -- "否" --> R
    CL1 -- "是" --> CL2["主信息仓库创建主信息"]
    CL2 --> CL3["节点仓库创建 因果引用 节点"]
    CL3 --> RV

    W1 -. "失败" .-> F["统一追根因解决收口：不返回有效句柄 / 不留下可读半结构 / 登记断点"]
    W2 -. "失败" .-> F
    E1 -. "失败" .-> F
    E2 -. "失败" .-> F
    S1 -. "失败" .-> F
    S2 -. "失败" .-> F
    SE2 -. "失败" .-> F
    IS2 -. "失败" .-> F
    IS3 -. "失败" .-> F
    IS4 -. "失败" .-> F
    AS1 -. "失败" .-> F
    AS2 -. "失败" .-> F
    ID2 -. "失败" .-> F
    ID3 -. "失败" .-> F
    ID4 -. "失败" .-> F
    ID5 -. "失败" .-> F
    ID6 -. "失败" .-> F
    AD1 -. "失败" .-> F
    AD2 -. "失败" .-> F
    BF2 -. "失败" .-> F
    BF3 -. "失败" .-> F
    BF4 -. "失败" .-> F
    CB1 -. "失败" .-> F
    CB2 -. "失败" .-> F
    CL2 -. "失败" .-> F
    CL3 -. "失败" .-> F

    RV --> RV1{"读回是否符合预期"}
    RV1 -- "否" --> LE["逻辑错误：停止后续产物生成 / 登记断点问题"]
    RV1 -- "是" --> OUT["输出基础信息类节点句柄或关系句柄"]
    OUT --> READ["只读方：语素服务绑定 / 高级服务读取材料 / 显示层只读"]
    R --> R1["不创建有效基础信息事实 / 不写可读半结构"]
    F --> R1
```

## 完成边界

本图是当前代码证据基线，不是代码实施许可。当前存在未提交代码时，只能解释为当前工作区事实；未实现和部分实现节点不得扩大为系统完成。
