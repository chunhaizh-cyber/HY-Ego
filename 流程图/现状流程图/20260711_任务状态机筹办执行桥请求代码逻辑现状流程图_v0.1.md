# 任务状态机筹办执行桥请求代码逻辑现状流程图

更新时间：2026-07-11

## 元数据

```text
图类型：现状流程图
对应施工流程图：流程图/20260708_任务状态机筹办执行桥请求代码逻辑流程图_v0.1.md
代码版本：3f08239；基于当前脏工作区
实现状态：已实现或已形成第一轮入口
覆盖文件：海中鱼巣/线程/任务工作线程.ixx、海中鱼巣/线程/任务管理线程.ixx、海中鱼巣/领域/任务服务.h、海中鱼巣/领域/状态服务.h
覆盖函数：任务服务::读取生命周期迁移请求材料、任务服务::记录任务生命周期状态、状态服务::创建实例状态、任务服务::读取筹办回执请求材料、任务服务::读取任务承接材料、任务服务::读取执行桥请求材料、任务服务::读取任务选择方法、任务服务::记录任务完成状态、任务服务::读取运行统计材料
逐行映射表：实施记录/现状流程图核查/20260711_任务状态机筹办执行桥请求代码逻辑现状流程图_v0.1配套核查表.md 第 2 节
输入契约 / 调用语境表：实施记录/现状流程图核查/20260711_任务状态机筹办执行桥请求代码逻辑现状流程图_v0.1配套核查表.md 第 3 节
非成功返回二分审查表：实施记录/现状流程图核查/20260711_任务状态机筹办执行桥请求代码逻辑现状流程图_v0.1配套核查表.md 第 4 节
偏差清单：实施记录/现状流程图核查/20260711_任务状态机筹办执行桥请求代码逻辑现状流程图_v0.1配套核查表.md 第 5 节
不得作为施工许可：是
不得宣称：未匹配节点、未来候选或施工目标已经实现
```

## 现状说明

当前代码存在对应入口；完成范围仍以实施记录和现状边界为准。
本图与根目录施工图并存；施工图回答准备怎样实现，本图只回答当前代码实际存在什么入口和边界。

## 流程图

```mermaid
flowchart TD
    CURRENT_SCOPE["现状基线：已实现或已形成第一轮入口；未来/候选节点不视为当前已实现"] --> A
    A["上游任务承接壳 / 任务管理请求 / 人工推进请求"] --> B["降权为任务状态机 / 筹办 / 执行桥请求材料"]
    B --> C{"请求类型"}

    C -- "生命周期迁移请求材料" --> L1["任务服务::读取生命周期迁移请求材料(任务, 目标状态, 状态服务)"]
    L1 --> L2{"任务节点类型是 任务"}
    L2 -- "否" --> LR1["返回：无效任务 / 不写状态"]
    L2 -- "是" --> L3{"任务承接壳完整 && 目标状态 != 未定义"}
    L3 -- "否" --> LR2["返回：承接壳不完整 / 不写状态"]
    L3 -- "是" --> L4["读取任务创建状态作为当前状态材料"]
    L4 --> L5["返回：可作为请求材料"]
    L5 --> LG["后续门禁：状态迁移合法性矩阵"]

    C -- "记录生命周期状态辅助" --> W1["任务服务::记录任务生命周期状态(任务, 虚拟存在, 场景, 生命周期状态, 时间戳, 状态服务)"]
    W1 --> W2["转入 记录任务状态"]
    W2 --> W3{"任务/场景/虚拟存在类型正确 && 状态值 != 未定义"}
    W3 -- "否" --> WR1["入口拒绝：返回空状态 / 不写关系"]
    W3 -- "是" --> W4["状态服务::创建实例状态(场景, 虚拟存在, 时间戳, 状态值)"]
    W4 --> W5{"状态节点创建成功"}
    W5 -- "否" --> WR2["追根因解决：不声明迁移成功"]
    W5 -- "是" --> W6["写关系：任务 引用 生命周期状态节点"]
    W6 --> W7{"关系写入成功"}
    W7 -- "否" --> WR3["追根因解决：不声明迁移成功"]
    W7 -- "是" --> W8["返回生命周期状态节点"]
    W8 --> WG["当前只是状态记录辅助，不证明完整状态机合法迁移"]

    C -- "筹办回执请求材料" --> P1["任务服务::读取筹办回执请求材料(任务, 状态服务)"]
    P1 --> P2["任务服务::读取任务承接材料"]
    P2 --> P3{"承接材料完整"}
    P3 -- "否" --> PR1["返回：承接壳不完整 / 不写筹办回执"]
    P3 -- "是" --> P4["返回：任务 / 来源需求 / 运行场景 / 可作为请求材料"]
    P4 --> PG["后续门禁：候选方法集合 / 缺口 / 派生需求候选"]
    PG --> PG2["筹办回执只是过程材料，不直接写任务状态 / 方法关系 / 需求树"]

    C -- "执行桥请求材料" --> E1["任务服务::读取执行桥请求材料(任务, 状态服务)"]
    E1 --> E2{"任务承接壳完整"}
    E2 -- "否" --> ER1["返回：承接壳不完整 / 不执行"]
    E2 -- "是" --> E3["任务服务::读取任务选择方法"]
    E3 --> E4{"存在唯一任务选择方法关系"}
    E4 -- "否" --> ER2["返回：缺少任务方法关系 / 不执行"]
    E4 -- "是" --> E5["返回：任务 / 方法 / 可作为请求材料"]
    E5 --> EG["后续门禁：方法结构、动作入口、条件结果、输入输出场景、时间戳"]
    EG --> EG2["当前不启动线程、不 std::async、不派发动作、不记录动作动态"]

    C -- "完成状态请求" --> F1["任务服务::记录任务完成状态(任务, 时间戳, 状态服务)"]
    F1 --> F2["读取任务承接材料"]
    F2 --> F3{"承接材料完整 && 已有实际结果状态 && 时间戳 != 0"}
    F3 -- "否" --> FR1["入口拒绝：不写完成状态"]
    F3 -- "是" --> F4["记录任务生命周期状态(已完成)"]
    F4 --> FG["当前完成状态写入不等于完整目标达成裁决"]
    FG --> FG2["目标状态与实际结果状态比较仍需后续任务结果/需求结算流程复核"]

    C -- "非权威运行统计材料" --> S1["任务服务::读取运行统计材料(任务, 状态服务)"]
    S1 --> S2{"任务承接壳完整"}
    S2 -- "否" --> SR1["返回：承接壳不完整 / 统计数为 0"]
    S2 -- "是" --> S3["遍历任务引用目标中的状态节点"]
    S3 --> S4["统计可读状态节点数"]
    S4 --> S5["返回：任务 / 状态节点数 / 可作为请求材料"]
    S5 --> SG["统计是非权威材料，不裁决任务完成、方法成功或需求满足"]

    LG --> D1["后续状态机设计门禁"]
    WG --> D1
    D1 --> D2{"迁移是否合法"}
    D2 -- "否" --> STOP["拒绝：不新增任务状态、不写方法关系、不写需求树"]
    D2 -- "是" --> D3["后续确认后才允许提交状态迁移写入口"]

    PG2 --> N1["输出给第 9/10 项：方法结构、方法候选召回与选择"]
    EG2 --> N2["输出给第 11 项：方法执行 / 动作入口流程"]
    FG2 --> N3["输出给第 13/15 项：任务结果回写 / 需求结算流程"]
    SG --> N4["输出给第 16 项：非权威缓存 / 统计流程"]

    LR1 --> READ["只读方：任务服务 / 方法服务 / 显示层只读"]
    LR2 --> READ
    WR1 --> READ
    WR2 --> READ
    WR3 --> READ
    PR1 --> READ
    ER1 --> READ
    ER2 --> READ
    FR1 --> READ
    SR1 --> READ
    STOP --> READ
    D3 --> READ
    N1 --> READ
    N2 --> READ
    N3 --> READ
    N4 --> READ

    C -- "排除项" --> X1["旧任务筹办 / 执行函数体 / 工作线程 / 运行包 / 治理协议"]
    X1 --> X2["线程、日志、控制台、显示、SQL 不得写任务事实"]
    X1 --> X3["控制面板 / D455 / 体素 / 外设不从本图进入实施"]
```

## 完成边界

本图是当前代码证据基线，不是代码实施许可。当前存在未提交代码时，只能解释为当前工作区事实；未实现和部分实现节点不得扩大为系统完成。
