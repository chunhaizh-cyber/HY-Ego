# 海中鱼巣当前总入口生产装配自检 SQL 与运行分支现状流程图

更新时间：2026-07-12

图类型：现状流程图

逐行映射表：`实施记录/20260712_海中鱼巣当前总入口生产装配自检SQL与运行分支逐行代码映射表.md`

## 依据

```text
代码版本：70c73a7
海中鱼巣/入口.cpp
实施记录/20260711_ENTRY-MOD-S0_入口与自检承载当前代码事实复核_Codex断点清单.md
```

## 说明

本图按 #236 的正式八段矩阵记录当前入口，不把自检输出、SQL 审计或控制面板投影解释为机器事实。

## 流程图

```mermaid
flowchart TD
    A["main 解析 --self-test-exit 与 --database-self-test-exit"] --> B["初始化运行日志"]
    B --> C["构造四仓库、领域服务、初始化服务、线程、快照、显示与 SQL 适配"]
    C --> D["启动自我线程并完成语素、世界树、两个根需求初始化"]
    D --> E["初始化概念四根、存在支持与方法登记根"]
    E --> F["执行存量领域 Axx、自我治理、线程、概念、方法召回、事件、控制面板自检"]
    F --> G{"总自检是否通过"}
    G -- "否" --> R1["自检模式返回失败；常驻模式停止进入后续启动"]
    G -- "是且自检模式" --> H["输出总用时并返回 0"]
    G -- "是且常驻 / 数据库模式" --> I["读取结构统计并写入 SQL 审计投影"]
    I --> J{"数据库自检模式"}
    J -- "是" --> K["按 SQL 往返结果退出"]
    J -- "否" --> L["安装停止信号"]
    L --> M{"是否启用控制面板"}
    M -- "是" --> N["运行原生只读控制面板并等待关闭"]
    M -- "否" --> O["进入 headless 常驻循环等待停止信号"]
    N --> P["记录正常关闭并退出"]
    O --> P
    I --> L1{"SQL 外部通道不可用"}
    L1 -- "是" --> L2["逻辑内返回：记录人读失败，不改机器事实"]
    L1 -- "否" --> J
    C --> R2{"正式装配后内部服务关系矛盾"}
    R2 -- "是" --> R3["追根因：停止启动，不由输出修复"]
    R2 -- "否" --> D
```

## 关键边界

```text
入口当前仍有 15903 行，#211 只证明新切片不恶化，不证明存量自检已迁出。
SQL Server 只做审计投影；控制面板只读，不裁决业务或恢复。
当前没有统一自检运行器、权威数据库恢复或真实外设接线。
```
