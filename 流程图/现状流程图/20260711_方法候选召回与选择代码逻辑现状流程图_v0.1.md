# 方法候选召回与选择代码逻辑现状流程图

更新时间：2026-07-11

## 元数据

```text
图类型：现状流程图
对应施工流程图：流程图/20260708_方法候选召回与选择代码逻辑流程图_v0.1.md
代码版本：3f08239；基于当前脏工作区
实现状态：已实现或已形成第一轮入口
覆盖文件：海中鱼巣/领域/任务服务.h、海中鱼巣/领域/方法服务.h、海中鱼巣/领域/需求服务.h
覆盖函数：需求服务::方法候选准入材料是否可读、任务服务::读取任务承接材料、任务服务::读取筹办回执请求材料、方法服务::方法首是否有效、方法服务::按稳定动作键读取方法动作入口、任务服务::选择任务方法、任务服务::读取执行桥请求材料
逐行映射表：实施记录/现状流程图核查/20260711_方法候选召回与选择代码逻辑现状流程图_v0.1配套核查表.md 第 2 节
输入契约 / 调用语境表：实施记录/现状流程图核查/20260711_方法候选召回与选择代码逻辑现状流程图_v0.1配套核查表.md 第 3 节
非成功返回二分审查表：实施记录/现状流程图核查/20260711_方法候选召回与选择代码逻辑现状流程图_v0.1配套核查表.md 第 4 节
偏差清单：实施记录/现状流程图核查/20260711_方法候选召回与选择代码逻辑现状流程图_v0.1配套核查表.md 第 5 节
不得作为施工许可：是
不得宣称：未匹配节点、未来候选或施工目标已经实现
```

## 现状说明

当前代码存在对应入口；完成范围仍以实施记录和现状边界为准。
本图与根目录施工图并存；施工图回答准备怎样实现，本图只回答当前代码实际存在什么入口和边界。

## 流程图

```mermaid
flowchart TD
    CURRENT_SCOPE["现状基线：已实现或已形成第一轮入口；未来/候选节点不视为当前已实现"] --> A
    A["上游需求树后续结构材料 / 任务筹办回执请求 / 方法结构材料"] --> B["进入方法候选召回与选择边界"]
    B --> C{"入口类型"}

    C -- "需求侧候选准入材料" --> D1["需求服务::方法候选准入材料是否可读(需求)"]
    D1 --> D2["读取需求承接材料"]
    D2 --> D3{"需求主体、目标宿主、场景、目标状态均可读"}
    D3 -- "否" --> DR1["返回 false / 不召回方法 / 不写任务方法关系"]
    D3 -- "是" --> D4["形成需求候选准入材料"]
    D4 --> D5["材料只说明可进入候选流程，不裁决候选方法"]

    C -- "任务侧选择上下文" --> T1["任务服务::读取任务承接材料(任务, 状态服务)"]
    T1 --> T2{"任务承接壳完整"}
    T2 -- "否" --> TR1["返回空 / 不选择方法"]
    T2 -- "是" --> T3["读取任务目标状态、运行场景、来源需求、任务虚拟存在"]
    T3 --> T4["任务服务::读取筹办回执请求材料"]
    T4 --> T5{"材料状态为可作为请求材料"}
    T5 -- "否" --> TR2["返回承接壳不完整 / 不写关系"]
    T5 -- "是" --> T6["形成任务候选选择上下文"]

    C -- "方法结构候选读取" --> M1["方法服务读取候选方法结构"]
    M1 --> M2["方法服务::方法首是否有效"]
    M2 --> M3{"方法首有效并有唯一方法虚拟存在"}
    M3 -- "否" --> MR1["剔除候选：无效方法首"]
    M3 -- "是" --> M4["读取方法条件节点集合"]
    M4 --> M5["读取方法结果节点集合"]
    M5 --> M6["读取方法动作入口"]
    M6 --> M7{"条件 / 结果 / 动作入口满足候选准入"}
    M7 -- "否" --> MR2["保留为缺口材料或剔除；不得自动选择"]
    M7 -- "是" --> M8["形成候选方法结构材料"]

    C -- "索引召回候选" --> I1["索引仓库候选入口"]
    I1 --> I2["当前已落代码仅支持稳定动作键读取动作入口"]
    I2 --> I3["方法服务::按稳定动作键读取方法动作入口"]
    I3 --> I4{"稳定动作键非 0，方法首有效，动作入口归属成立"}
    I4 -- "否" --> IR1["返回空 / 不产生候选"]
    I4 -- "是" --> I5["返回动作入口结构材料"]
    I5 --> I6["索引只做入口召回，不裁决方法可执行事实"]

    D5 --> F1["候选材料汇合"]
    T6 --> F1
    M8 --> F1
    I6 --> F1
    MR2 --> F1

    F1 --> F2{"后续设计已确认候选规则"}
    F2 -- "否" --> F3["只输出候选材料 / 不排序 / 不自动选择 / 不写任务方法关系"]
    F2 -- "是" --> F4["按后续规则复核：目标状态、条件场景、结果场景、动作入口、来源任务和策略状态"]
    F4 --> F5{"候选唯一或选择策略已授权"}
    F5 -- "否" --> FR1["拒绝选择：多候选或无授权策略"]
    F5 -- "是" --> S1["任务服务::选择任务方法(任务, 方法, 状态服务)"]

    S1 --> S2{"任务承接壳完整"}
    S2 -- "否" --> SR1["入口拒绝：不写任务选择方法关系"]
    S2 -- "是" --> S3{"方法节点类型为 方法"}
    S3 -- "否" --> SR2["入口拒绝：目标非方法"]
    S3 -- "是" --> S4{"当前任务尚无选择方法"}
    S4 -- "否" --> SR3["入口拒绝：重复当前方法"]
    S4 -- "是" --> S5["写关系：任务 引用 方法，顺序号=任务选择方法顺序号"]
    S5 --> S6{"读取任务选择方法读回唯一成立"}
    S6 -- "否" --> SR4["不得声明选择成功；后续施工计划需补追根因解决收口和数量快照"]
    S6 -- "是" --> S7["输出：任务选择方法关系"]

    S7 --> E1["任务服务::读取执行桥请求材料"]
    E1 --> E2{"任务承接壳完整且唯一任务选择方法可读"}
    E2 -- "否" --> ER1["返回缺少任务方法关系 / 不执行"]
    E2 -- "是" --> E3["输出给第 11 项：方法执行 / 动作入口流程"]

    F3 --> R0["只读输出方：任务服务 / 方法服务候选读取方 / 显示层只读"]
    FR1 --> R0
    SR1 --> R0
    SR2 --> R0
    SR3 --> R0
    SR4 --> R0
    ER1 --> R0
    E3 --> R0
    DR1 --> R0
    TR1 --> R0
    TR2 --> R0
    MR1 --> R0
    IR1 --> R0

    F1 --> G1["当前代码差距"]
    G1 --> G2["无专门结果能力索引和方法候选召回入口"]
    G2 --> G3["无条件判定算法、条件结果对归并、候选排序或自动选择策略"]
    G3 --> G4["来源任务、父方法、前置 / 后续方法关系仍是后续"]

    C -- "排除项" --> X1["旧方法查找函数体 / 旧结果能力裸字段 / 可查找 bool / 成熟度 I64"]
    X1 --> X2["不得作为权威候选裁决或方法成功事实"]
    X1 --> X3["日志、显示、SQL、控制面板不得裁决候选命中"]
    X1 --> X4["D455、体素、外设不从本图进入实施"]
    X1 --> X5["方法服务不得直接依赖特征值服务；需要特征状态材料必须经特征服务"]
```

## 完成边界

本图是当前代码证据基线，不是代码实施许可。当前存在未提交代码时，只能解释为当前工作区事实；未实现和部分实现节点不得扩大为系统完成。
