# 需求树后续结构代码逻辑现状流程图

更新时间：2026-07-11

## 元数据

```text
图类型：现状流程图
对应施工流程图：流程图/20260708_需求树后续结构代码逻辑流程图_v0.1.md
代码版本：3f08239；基于当前脏工作区
实现状态：已实现或已形成第一轮入口
覆盖文件：海中鱼巣/核心/关系仓库.h、海中鱼巣/领域/需求服务.h
覆盖函数：需求服务::读取需求树后续结构材料、需求服务::读取需求目标状态、需求服务::读取需求承接材料、需求服务::需求树结构化更新请求已拒绝、需求服务::枚举目标准入材料是否可读、需求服务::方法候选准入材料是否可读
逐行映射表：实施记录/现状流程图核查/20260711_需求树后续结构代码逻辑现状流程图_v0.1配套核查表.md 第 2 节
输入契约 / 调用语境表：实施记录/现状流程图核查/20260711_需求树后续结构代码逻辑现状流程图_v0.1配套核查表.md 第 3 节
非成功返回二分审查表：实施记录/现状流程图核查/20260711_需求树后续结构代码逻辑现状流程图_v0.1配套核查表.md 第 4 节
偏差清单：实施记录/现状流程图核查/20260711_需求树后续结构代码逻辑现状流程图_v0.1配套核查表.md 第 5 节
不得作为施工许可：是
不得宣称：未匹配节点、未来候选或施工目标已经实现
```

## 现状说明

当前代码存在对应入口；完成范围仍以实施记录和现状边界为准。
本图与根目录施工图并存；施工图回答准备怎样实现，本图只回答当前代码实际存在什么入口和边界。

## 流程图

```mermaid
flowchart TD
    CURRENT_SCOPE["现状基线：已实现或已形成第一轮入口；未来/候选节点不视为当前已实现"] --> A
    A["上游请求 / 需求创建结果 / 任务筹办候选"] --> B["降权为需求树后续结构材料请求"]
    B --> C{"请求类型"}

    C -- "读取需求树后续结构材料" --> R1["需求服务::读取需求树后续结构材料(需求节点, 父需求)"]
    R1 --> R2{"需求节点是 需求"}
    R2 -- "否" --> RS1["返回材料：无效需求 / 不写结构"]
    R2 -- "是" --> R3{"父需求为空或有效需求且不是自身"}
    R3 -- "否" --> RS2["返回材料：自环已拒绝 / 不写结构"]
    R3 -- "是" --> R4["需求服务::读取需求目标状态"]
    R4 --> R5{"目标状态唯一且可读"}
    R5 -- "否" --> RS3["返回材料：缺少目标状态 / 不写结构"]
    R5 -- "是" --> R6["需求服务::读取需求承接材料"]
    R6 --> R7{"主体 / 目标宿主 / 场景 / 目标状态齐全"}
    R7 -- "是" --> R8["阻塞权重材料 = 1"]
    R7 -- "否" --> R9["阻塞权重材料 = 0"]
    R8 --> R10["返回材料：可作为请求材料"]
    R9 --> R10
    R10 --> RV["读回验证：需求 / 父需求 / 目标状态 / 阻塞权重 / 请求状态"]

    C -- "结构化更新请求判定" --> U1["需求服务::需求树结构化更新请求已拒绝"]
    U1 --> U2["复用 读取需求树后续结构材料"]
    U2 --> U3{"状态 == 可作为请求材料"}
    U3 -- "否" --> U4["返回 已拒绝 / 不提交更新"]
    U3 -- "是" --> U5["返回 未拒绝候选 / 当前仍不写父子关系"]
    U4 --> RV
    U5 --> RV

    C -- "枚举目标准入材料" --> E1["需求服务::枚举目标准入材料是否可读"]
    E1 --> E2["复用 读取需求树后续结构材料"]
    E2 --> E3{"请求材料可用 && 目标状态节点类型为 状态"}
    E3 -- "否" --> E4["返回 false / 不写枚举目标事实"]
    E3 -- "是" --> E5["返回 true / 仅表示准入材料可读"]
    E4 --> RV
    E5 --> RV

    C -- "方法候选准入材料" --> M1["需求服务::方法候选准入材料是否可读"]
    M1 --> M2["需求服务::读取需求承接材料"]
    M2 --> M3{"主体 / 目标宿主 / 场景 / 目标状态齐全"}
    M3 -- "否" --> M4["返回 false / 不召回方法事实"]
    M3 -- "是" --> M5["返回 true / 仅供方法候选读取"]
    M4 --> RV
    M5 --> RV

    C -- "父子需求角色 / 阻塞 / 权重" --> P1["后续候选门禁"]
    P1 --> P2{"父需求和子需求均有效 && 非自身 && 不形成环"}
    P2 -- "否" --> RJ["拒绝：需求树关系数量 / 索引数量 / 需求节点数量不变"]
    P2 -- "是" --> P3["候选：父子角色只读视图 / 阻塞材料 / 非权威权重"]
    P3 --> P4["不得直接裁决任务执行或需求满足"]

    C -- "逻辑组织需求 / 获取途径" --> L1["后续候选门禁"]
    L1 --> L2{"有父子关系 && 有目标状态 && 角色材料已确认"}
    L2 -- "否" --> RJ
    L2 -- "是" --> L3["候选：AND / OR / 方法路径组 / 因果子链支撑"]
    L3 --> L4["文本和旧枚举只做人读或候选，不承载机器事实"]

    C -- "方向签名 / 枚举目标 / 方法候选" --> G1["后续候选门禁"]
    G1 --> G2{"目标状态可读 && 二次特征或值域材料已确认"}
    G2 -- "否" --> RJ
    G2 -- "是" --> G3["候选：方向签名 / 枚举目标合法性 / 方法候选准入"]
    G3 --> G4["只输出候选或拒绝原因，不选择方法、不裁决满足"]

    C -- "结构化更新提交" --> S1["后续施工门禁"]
    S1 --> S2["先构造请求壳：不写节点 / 不写关系 / 不写任务"]
    S2 --> S3{"预读父需求 / 目标需求 / 主体 / 目标宿主 / 场景 / 目标状态 / 角色材料全部通过"}
    S3 -- "否" --> RJ
    S3 -- "是" --> S4["后续确认后才允许统一提交入口写需求树关系"]
    S4 --> S5["提交失败必须无可读半结构"]

    C -- "SQL / 控制面板 / 旧字段" --> X1["排除项"]
    X1 --> X2["SQL 投影 / 控制面板 / 旧主信息字段不得进入机器事实"]
    X1 --> X3["D455 / 体素 / 外设不从本图进入实施"]

    RV --> RV1{"读回符合预期"}
    RV1 -- "否" --> LE["逻辑错误：登记断点 / 停止后续施工计划推进"]
    RV1 -- "是" --> OUT["输出只读材料 / 拒绝结果 / 候选准入布尔"]
    OUT --> READ["只读方：需求服务 / 任务服务 / 方法候选 / 显示层只读"]

    RS1 --> RJ
    RS2 --> RJ
    RS3 --> RJ
    RJ --> READ
```

## 完成边界

本图是当前代码证据基线，不是代码实施许可。当前存在未提交代码时，只能解释为当前工作区事实；未实现和部分实现节点不得扩大为系统完成。
