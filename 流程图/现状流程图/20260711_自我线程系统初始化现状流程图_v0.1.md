# 自我线程系统初始化现状流程图

更新时间：2026-07-11

## 元数据

```text
图类型：现状流程图
对应施工流程图：流程图/20260710_自我线程系统初始化流程图_v0.1.md
代码版本：70c73a7；工作区干净
实现状态：已实现或已形成第一轮入口
覆盖文件：海中鱼巣/线程/自我线程.ixx、海中鱼巣/领域/初始化.需求.ixx
覆盖函数：当前无可匹配正式入口
逐行映射表：实施记录/现状流程图核查/20260711_自我线程系统初始化现状流程图_v0.1配套核查表.md 第 2 节
输入契约 / 调用语境表：实施记录/现状流程图核查/20260711_自我线程系统初始化现状流程图_v0.1配套核查表.md 第 3 节
非成功返回二分审查表：实施记录/现状流程图核查/20260711_自我线程系统初始化现状流程图_v0.1配套核查表.md 第 4 节
偏差清单：实施记录/现状流程图核查/20260711_自我线程系统初始化现状流程图_v0.1配套核查表.md 第 5 节
不得作为施工许可：是
不得宣称：未匹配节点、未来候选或施工目标已经实现
```

## 现状说明

当前代码存在对应入口；完成范围仍以实施记录和现状边界为准。
本图与根目录施工图并存；施工图回答准备怎样实现，本图只回答当前代码实际存在什么入口和边界。

## 流程图

```mermaid
flowchart TD
    CURRENT_SCOPE["现状基线：已实现或已形成第一轮入口；未来/候选节点不视为当前已实现"] --> A
    A["入口构造仓库、领域服务和初始化服务"] --> B["构造自我线程与显式初始化参数"]
    B --> C{"依赖、稳定键、当前值和目标值参数是否有效"}
    C -- "否" --> L1["逻辑内返回：拒绝启动，不创建线程，不写结构"]
    C -- "是" --> D["启动自我线程"]
    D --> E["自我线程入口标记为初始化中"]
    E --> F{"首笔写入前是否已收到停止请求"}
    F -- "是" --> L2["逻辑内返回：线程正常停止，结构不变化"]
    F -- "否" --> G["调用语素初始化服务，建立基础语素信息"]
    G --> H{"语素初始化结果和读回是否符合预期"}
    H -- "否" --> R1["追根因解决：标记初始化失败，停止依赖路径，程序不得进入常驻态"]
    H -- "是" --> I["调用世界树初始化服务，建立世界根、场景和自我存在"]
    I --> J{"世界树句柄、父子关系、归属关系和坐标读回是否符合预期"}
    J -- "否" --> R2["追根因解决：标记初始化失败，停止依赖路径，程序不得进入常驻态"]
    J -- "是" --> K["调用 初始化.需求.ixx 初始化两个根需求"]
    K --> K1["经特征服务建立安全值、服务值定义和自我当前特征材料"]
    K1 --> K2["经语素服务绑定安全值、服务值概念语素和显式稳定键"]
    K2 --> K3["经需求服务和状态服务创建安全根需求、服务根需求及抽象目标状态"]
    K3 --> M{"两个根需求、目标状态和完整承接材料是否可读"}
    M -- "否" --> R3["追根因解决：标记初始化失败，停止依赖路径，程序不得进入常驻态"]
    M -- "是" --> N["一次性发布自我线程初始化结果快照"]
    N --> O{"初始化期间是否锁存停止请求"}
    O -- "是" --> P["线程正常停止并由入口收口等待"]
    O -- "否" --> Q["进入驻留等待态，等待停止或后续治理接线"]
```

## 完成边界

本图是当前代码证据基线，不是代码实施许可。当前存在未提交代码时，只能解释为当前工作区事实；未实现和部分实现节点不得扩大为系统完成。
