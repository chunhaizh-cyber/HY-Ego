# 概念活动实例支持权威失效与投影推进现状流程图

更新时间：2026-07-11

## 元数据

```text
图类型：现状流程图
对应施工流程图：流程图/20260711_概念活动实例支持权威失效与投影推进流程图_v0.1.md
代码版本：70c73a7；工作区干净
实现状态：已实现或已形成第一轮入口
覆盖文件：海中鱼巣/领域/概念图服务.h
覆盖函数：当前无可匹配正式入口
逐行映射表：实施记录/现状流程图核查/20260711_概念活动实例支持权威失效与投影推进现状流程图_v0.1配套核查表.md 第 2 节
输入契约 / 调用语境表：实施记录/现状流程图核查/20260711_概念活动实例支持权威失效与投影推进现状流程图_v0.1配套核查表.md 第 3 节
非成功返回二分审查表：实施记录/现状流程图核查/20260711_概念活动实例支持权威失效与投影推进现状流程图_v0.1配套核查表.md 第 4 节
偏差清单：实施记录/现状流程图核查/20260711_概念活动实例支持权威失效与投影推进现状流程图_v0.1配套核查表.md 第 5 节
不得作为施工许可：是
不得宣称：未匹配节点、未来候选或施工目标已经实现
```

## 现状说明

当前代码存在对应入口；完成范围仍以实施记录和现状边界为准。
本图与根目录施工图并存；施工图回答准备怎样实现，本图只回答当前代码实际存在什么入口和边界。

## 流程图

```mermaid
flowchart TD
    CURRENT_SCOPE["现状基线：已实现或已形成第一轮入口；未来/候选节点不视为当前已实现"] --> A
    A["调用方提交一个明确的活动支持失效请求"] --> B["请求携带实例、概念、当前支持关系和预期活动版本"]
    B --> C{"句柄、类别、关系类型、活动版本有效，且目标是当前活动普通概念"}
    C -- "否" --> R1["逻辑内返回：结构不变化"]
    C -- "是" --> D["概念图服务先取得活动图独占锁，再取得图写锁"]
    D --> E["重读当前活动快照、专用登记和关系仓库有效记录"]
    E --> F{"当前是否存在完全相同的有效支持"}
    F -- "否且有同端点已失效历史" --> R2["逻辑内返回：幂等返回已失效材料"]
    F -- "否且无历史" --> R3["逻辑内返回：目标支持不存在"]
    F -- "当前结构互相矛盾" --> X1["追根因解决：停止依赖路径"]
    F -- "是" --> G["在内存中复制活动快照和专用登记"]
    G --> H["移除精确支持边、活动版本加一、清除上一候选幂等材料"]
    H --> I{"新快照根、概念、签名、上下位和剩余关系仍完整"}
    I -- "否" --> X2["追根因解决：尚未写仓库，保持旧事实"]
    I -- "是" --> J["关系仓库把原支持关系状态从有效改为已失效"]
    J --> K["保持关系编号、版本加一，普通有效查询不再返回"]
    K --> L{"失效审计记录、旧有效读取消失和新版本一致"}
    L -- "否" --> X3["追根因解决：停止活动图与生命周期依赖"]
    L -- "是" --> M["无分配交换发布新活动快照和有效专用登记"]
    M --> N{"活动版本、有效支持投影和审计历史完整读回"}
    N -- "否" --> X4["追根因解决：停止后续候选和转换"]
    N -- "是" --> O["返回支持已失效材料"]

    O --> P["#195 生命周期候选按新活动版本重算有效支持数量"]
    P --> Q{"目标普通概念是否仍有其它有效支持、外部引用或有效用途"}
    Q -- "是" --> R4["逻辑内返回：#197 仍不得冷却"]
    Q -- "否" --> S["#197 获得真实可达的活跃到冷却样本"]
    S --> T["后续新候选可创建新的有效支持关系，不复活旧关系记录"]

    Z1["测试裸删支持关系或从快照直接抹边"] --> Z9["禁止"]
    Z2["把已失效关系继续计入活动支持或外部活性"] --> Z9
    Z3["不推进活动版本却改变有效支持投影"] --> Z9
    Z4["修改候选完整性以允许无来源实例概念"] --> Z9
    Z5["用统计缓存、日志或 bool 标记支持失效"] --> Z9
    Z6["任何双锁路径先取得图写锁再请求活动图锁"] --> Z9
    Z7["把四根支持纳入本切片失效范围"] --> Z9
```

## 完成边界

本图是当前代码证据基线，不是代码实施许可。当前存在未提交代码时，只能解释为当前工作区事实；未实现和部分实现节点不得扩大为系统完成。
