# 任务回执实际结果状态结果回写代码逻辑现状流程图

更新时间：2026-07-11

## 元数据

```text
图类型：现状流程图
对应施工流程图：流程图/20260708_任务回执实际结果状态结果回写代码逻辑流程图_v0.1.md
代码版本：70c73a7；工作区干净
实现状态：已实现或已形成第一轮入口
覆盖文件：海中鱼巣/线程/任务管理上行桥.ixx、海中鱼巣/领域/任务服务.h、海中鱼巣/领域/动态服务.h
覆盖函数：任务服务::读取任务承接材料、任务服务::记录任务实际结果状态、任务服务::读取任务实际结果状态、任务服务::记录任务完成状态、任务服务::记录任务生命周期状态、任务服务::记录任务状态、动态服务::读取动态材料、任务服务::读取运行统计材料
逐行映射表：实施记录/现状流程图核查/20260711_任务回执实际结果状态结果回写代码逻辑现状流程图_v0.1配套核查表.md 第 2 节
输入契约 / 调用语境表：实施记录/现状流程图核查/20260711_任务回执实际结果状态结果回写代码逻辑现状流程图_v0.1配套核查表.md 第 3 节
非成功返回二分审查表：实施记录/现状流程图核查/20260711_任务回执实际结果状态结果回写代码逻辑现状流程图_v0.1配套核查表.md 第 4 节
偏差清单：实施记录/现状流程图核查/20260711_任务回执实际结果状态结果回写代码逻辑现状流程图_v0.1配套核查表.md 第 5 节
不得作为施工许可：是
不得宣称：未匹配节点、未来候选或施工目标已经实现
```

## 现状说明

当前代码存在对应入口；完成范围仍以实施记录和现状边界为准。
本图与根目录施工图并存；施工图回答准备怎样实现，本图只回答当前代码实际存在什么入口和边界。

## 流程图

```mermaid
flowchart TD
    CURRENT_SCOPE["现状基线：已实现或已形成第一轮入口；未来/候选节点不视为当前已实现"] --> A
    A["上游：任务执行桥请求 / 方法执行动作入口 / 动态记录证据材料"] --> B["进入任务回执实际结果状态结果回写边界"]
    B --> C["任务服务::读取任务承接材料(任务节点, 状态服务)"]
    C --> C1{"任务承接壳完整"}
    C1 -- "否" --> R1["入口拒绝：回执无有效来源任务 / 不写实际结果状态 / 不写完成状态"]
    C1 -- "是" --> C2["读回来源需求、任务虚拟存在、运行场景、目标状态、创建状态"]
    C2 --> C3{"目标状态唯一且为状态节点"}
    C3 -- "否" --> R2["入口拒绝：任务目标状态不可作为结果回写依据"]
    C3 -- "是" --> D0{"回写路径"}

    D0 -- "记录实际结果状态" --> S1["任务服务::记录任务实际结果状态(任务节点, 实际结果状态, 状态服务)"]
    S1 --> S2{"承接壳完整，实际结果状态为状态节点"}
    S2 -- "否" --> SR1["入口拒绝：实际结果状态无效或任务不可读"]
    S2 -- "是" --> S3{"当前任务尚无实际结果状态"}
    S3 -- "否" --> SR2["入口拒绝：重复实际结果状态关系"]
    S3 -- "是" --> S4["写关系：任务节点 引用 实际结果状态"]
    S4 --> S5["关系顺序号：任务实际结果状态顺序号 = 20"]
    S5 --> S6["任务服务::读取任务实际结果状态(任务节点, 状态服务)"]
    S6 --> S7{"读回唯一 引用 + 状态节点 + 顺序号 20"}
    S7 -- "否" --> SF1["追根因解决：不得声明实际结果状态已回写"]
    S7 -- "是" --> S8["输出：任务实际结果状态材料"]
    S8 --> S9["边界：当前不比较目标状态与实际结果状态是否满足"]

    D0 -- "记录完成状态" --> F1["任务服务::记录任务完成状态(任务节点, 发生时间戳, 状态服务)"]
    F1 --> F2["再次读取任务承接材料"]
    F2 --> F3{"已有任务实际结果状态"}
    F3 -- "否" --> FR1["入口拒绝：缺实际结果状态时不能写已完成"]
    F3 -- "是" --> F4{"发生时间戳非 0"}
    F4 -- "否" --> FR2["入口拒绝：缺完成发生时间戳"]
    F4 -- "是" --> F5["任务服务::记录任务生命周期状态(... 已完成 ...)"]
    F5 --> F6["任务服务::记录任务状态 经状态服务创建实例状态"]
    F6 --> F7["写关系：任务节点 引用 已完成生命周期状态"]
    F7 --> F8["任务服务::读取任务完成状态(任务节点, 状态服务)"]
    F8 --> F9{"读回唯一 已完成 生命周期状态"}
    F9 -- "否" --> FF1["追根因解决：不得声明完成状态已记录"]
    F9 -- "是" --> F10["输出：任务完成状态材料"]
    F10 --> F11["边界：已完成状态记录不等于需求满足或结算完成"]

    D0 -- "动态证据相邻材料" --> E1["动态服务::读取动态材料 / 动态证据材料是否完整 / 读取动态来源动作"]
    E1 --> E2{"动态材料完整且来源动作有效"}
    E2 -- "否" --> ER1["拒绝升级：动态证据缺失不得作为完成依据"]
    E2 -- "是" --> E3["输出：相邻动态证据材料"]
    E3 --> E4["边界：当前任务服务记录实际结果状态不强制接收动态证据"]
    E4 --> E5["后续详细设计可把动态证据设为结果回写门禁"]

    D0 -- "运行统计只读" --> T1["任务服务::读取运行统计材料(任务节点, 状态服务)"]
    T1 --> T2{"任务承接壳完整"}
    T2 -- "否" --> TR1["返回：承接壳不完整"]
    T2 -- "是" --> T3["枚举任务 引用 目标中的状态节点"]
    T3 --> T4["统计状态节点数"]
    T4 --> T5["输出：任务非权威统计材料"]
    T5 --> T6["边界：统计材料不裁决任务完成、方法成功或需求满足"]

    S9 --> O1["只读输出方：任务服务 / 需求结算后续流程 / 显示层只读"]
    F11 --> O1
    E5 --> O1
    T6 --> O1
    R1 --> O1
    R2 --> O1
    SR1 --> O1
    SR2 --> O1
    SF1 --> O1
    FR1 --> O1
    FR2 --> O1
    FF1 --> O1
    ER1 --> O1
    TR1 --> O1

    O1 --> RV["最小读回验证门禁"]
    RV --> RV1["承接路径：读回任务节点、来源需求、虚拟存在、运行场景、目标状态、创建状态"]
    RV --> RV2["实际结果路径：读回任务到实际结果状态的 引用 关系、状态节点类型、顺序号 20、唯一性"]
    RV --> RV3["完成状态路径：读回已完成生命周期状态、任务虚拟存在、运行场景、发生时间戳"]
    RV --> RV4["动态证据路径：读回动态节点、发生场景、主体、时间戳、前后状态、来源动作；当前只作相邻证据"]
    RV --> RV5["统计路径：读回统计状态为非权威材料，不参与完成裁决"]
    RV1 --> RV6{"各路径读回满足归属、唯一性和边界标注"}
    RV2 --> RV6
    RV3 --> RV6
    RV4 --> RV6
    RV5 --> RV6
    RV6 -- "否" --> RVF["不得声明结果回写有效；后续施工计划必须补追根因解决收口、数量快照和半结构不可读验证"]
    RV6 -- "是" --> OK["输出给第 14 项：轻量因果引用流程"]

    B --> G1["当前代码差距"]
    G1 --> G2["记录任务实际结果状态只写任务到状态的顺序号 20 引用关系"]
    G2 --> G3["记录任务完成状态只要求已有实际结果状态和非零时间戳"]
    G3 --> G4["当前不比较任务目标状态与实际结果状态是否满足"]
    G4 --> G5["当前不写需求结算记录、不写持久化回执记录、不生成稳定因果结论"]
    G5 --> G6["当前运行统计只是非权威状态节点计数"]
    G6 --> G7["多步写入已有入口拒绝和追根因解决，但尚未证明完整事务回滚或数量快照级半结构不可读"]

    B --> X1["排除项"]
    X1 --> X2["线程、Tick、控制台、日志、显示不得成为任务结果来源"]
    X1 --> X3["统计、缓存、运行包 bool、错误码不得裁决任务完成"]
    X1 --> X4["本图不写需求结算、不接 SQL、控制面板、D455、体素或外设"]
    X1 --> X5["本图不声明完整任务状态机、任务筹办、真实执行桥或旧任务能力迁移完成"]
```

## 完成边界

本图是当前代码证据基线，不是代码实施许可。当前存在未提交代码时，只能解释为当前工作区事实；未实现和部分实现节点不得扩大为系统完成。
