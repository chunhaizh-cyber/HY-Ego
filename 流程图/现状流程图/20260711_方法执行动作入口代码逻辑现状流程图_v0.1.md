# 方法执行动作入口代码逻辑现状流程图

更新时间：2026-07-11

## 元数据

```text
图类型：现状流程图
对应施工流程图：流程图/20260708_方法执行动作入口代码逻辑流程图_v0.1.md
代码版本：3f08239；基于当前脏工作区
实现状态：已实现或已形成第一轮入口
覆盖文件：海中鱼巣/领域/任务服务.h、海中鱼巣/领域/动态服务.h、海中鱼巣/领域/方法服务.h、海中鱼巣/领域/特征服务.h
覆盖函数：方法服务::登记动作入口、方法服务::绑定动作输入规格、方法服务::读取动作输入规格、方法服务::本能动作注册材料是否有效、任务服务::读取执行桥请求材料、任务服务::读取任务选择方法、方法服务::执行特征状态写入方法、调用特征服务::写入I64特征值、方法服务::记录动作动态证据、动态服务::记录实例动态
逐行映射表：实施记录/现状流程图核查/20260711_方法执行动作入口代码逻辑现状流程图_v0.1配套核查表.md 第 2 节
输入契约 / 调用语境表：实施记录/现状流程图核查/20260711_方法执行动作入口代码逻辑现状流程图_v0.1配套核查表.md 第 3 节
非成功返回二分审查表：实施记录/现状流程图核查/20260711_方法执行动作入口代码逻辑现状流程图_v0.1配套核查表.md 第 4 节
偏差清单：实施记录/现状流程图核查/20260711_方法执行动作入口代码逻辑现状流程图_v0.1配套核查表.md 第 5 节
不得作为施工许可：是
不得宣称：未匹配节点、未来候选或施工目标已经实现
```

## 现状说明

当前代码存在对应入口；完成范围仍以实施记录和现状边界为准。
本图与根目录施工图并存；施工图回答准备怎样实现，本图只回答当前代码实际存在什么入口和边界。

## 流程图

```mermaid
flowchart TD
    CURRENT_SCOPE["现状基线：已实现或已形成第一轮入口；未来/候选节点不视为当前已实现"] --> A
    A["上游：任务选择方法关系 / 执行桥请求材料 / 方法结构材料"] --> B["进入方法执行与动作入口边界"]
    B --> C{"入口类型"}

    C -- "动作入口登记与读取" --> D1["方法服务::登记动作入口(方法首, 类型, 稳定动作键, 状态服务, 索引仓库)"]
    D1 --> D2{"有效方法首、动作入口类型有效、稳定动作键非 0"}
    D2 -- "否" --> DR1["入口拒绝：不创建动作入口"]
    D2 -- "是" --> D3{"稳定动作键未占用且方法首尚无当前动作入口"}
    D3 -- "否" --> DR2["入口拒绝：不创建重复入口 / 不覆盖旧入口"]
    D3 -- "是" --> D4["登记方法节点作为动作入口角色节点"]
    D4 --> D5{"动作入口节点有效且不等于方法首"}
    D5 -- "否" --> DR3["追根因解决：不返回有效动作入口"]
    D5 -- "是" --> D6["绑定方法角色状态=动作入口，并绑定动作入口状态"]
    D6 --> D7["写关系：方法首 引用 动作入口节点"]
    D7 --> D8["索引仓库绑定稳定动作键到动作入口节点"]
    D8 --> D9{"有效动作入口读回成立"}
    D9 -- "否" --> DR4["追根因解决：后续施工计划必须补追根因解决收口和数量快照"]
    D9 -- "是" --> D10["输出动作入口结构身份"]

    C -- "动作规格与注册材料复核" --> S1["方法服务::绑定动作输入规格 / 绑定动作输出规格"]
    S1 --> S2{"动作入口角色有效、规格场景有效、同类规格未重复"}
    S2 -- "否" --> SR1["入口拒绝：不写规格关系"]
    S2 -- "是" --> S3["写关系：动作入口 引用 输入规格场景 / 输出规格场景"]
    S3 --> S4["方法服务::读取动作输入规格 / 读取动作输出规格"]
    S4 --> S5["方法服务::本能动作注册材料是否有效"]
    S5 --> S6{"稳定动作键命中同一入口、入口主动可调用、输入输出规格均可读"}
    S6 -- "否" --> SR2["材料无效：不得进入本能动作注册或真实分派"]
    S6 -- "是" --> S7["输出本能动作注册材料候选"]

    C -- "任务执行桥请求" --> T1["任务服务::读取执行桥请求材料(任务, 状态服务)"]
    T1 --> T2{"任务承接壳完整"}
    T2 -- "否" --> TR1["返回承接壳不完整 / 不执行"]
    T2 -- "是" --> T3["任务服务::读取任务选择方法"]
    T3 --> T4{"唯一任务选择方法可读"}
    T4 -- "否" --> TR2["返回缺少任务方法关系 / 不执行"]
    T4 -- "是" --> T5["输出执行桥请求材料：任务节点 + 方法节点"]

    T5 --> E1["方法服务::执行特征状态写入方法"]
    E1 --> E2{"任务节点有效且类型为任务"}
    E2 -- "否" --> ER1["返回 false：不写特征状态材料"]
    E2 -- "是" --> E3{"方法节点有效且类型为方法"}
    E3 -- "否" --> ER2["返回 false：不写特征状态材料"]
    E3 -- "是" --> E4{"存在任务 引用 方法关系"}
    E4 -- "否" --> ER3["返回 false：缺任务方法关系"]
    E4 -- "是" --> E5{"方法具备主动动作入口"}
    E5 -- "否" --> ER4["返回 false：方法无主动入口"]
    E5 -- "是" --> E6["调用特征服务::写入I64特征值(目标特征, 特征值节点, 新状态值)"]
    E6 --> E7["输出：第一轮 I64 特征状态材料写入结果"]
    E7 --> E8{"后续门禁：经特征服务读回当前特征状态材料"}
    E8 -- "否" --> ER5["不得声明执行写入成功；需补读回验证和失败停止"]
    E8 -- "是" --> E9["特征状态材料可供后续任务回执 / 动态 / 结算读取"]

    C -- "动作动态证据记录" --> Y1["方法服务::记录动作动态证据"]
    Y1 --> Y2{"动作入口归属方法首有效 && 方法具备主动动作入口"}
    Y2 -- "否" --> YR1["返回空：不创建动态"]
    Y2 -- "是" --> Y3["动态服务::记录实例动态(场景, 存在, 时间戳, 被改变目标, 前值, 后值, 来源动作=动作入口)"]
    Y3 --> Y4{"场景、存在、发生时间戳、被改变目标、前值、后值、来源动作均有效"}
    Y4 -- "否" --> YR2["返回空：不创建有效动态"]
    Y4 -- "是" --> Y5["写实例动态节点、场景临时关系、主体引用、目标/前值/后值因果来源关系"]
    Y5 --> Y6["若来源动作有效，写动态到动作入口的因果来源关系"]
    Y6 --> Y7{"动态服务读回动态材料完整"}
    Y7 -- "否" --> YR3["返回空：不得声明动作动态证据有效"]
    Y7 -- "是" --> Y8["方法服务写动态 引用 来源方法首"]
    Y8 --> Y9{"来源方法关系写入成功"}
    Y9 -- "否" --> YR4["返回空：后续施工计划必须补半结构不可读验证"]
    Y9 -- "是" --> Y10["输出动作动态证据材料"]

    D10 --> R0["只读输出方：任务服务 / 方法服务 / 动态服务 / 显示层只读"]
    S7 --> R0
    E9 --> R0
    Y10 --> R0
    DR1 --> R0
    DR2 --> R0
    DR3 --> R0
    DR4 --> R0
    SR1 --> R0
    SR2 --> R0
    TR1 --> R0
    TR2 --> R0
    ER1 --> R0
    ER2 --> R0
    ER3 --> R0
    ER4 --> R0
    ER5 --> R0
    YR1 --> R0
    YR2 --> R0
    YR3 --> R0
    YR4 --> R0

    R0 --> RV["最小读回验证门禁"]
    RV --> RV1["动作入口路径：读回方法首、动作入口角色、动作入口状态、稳定动作键索引、输入规格、输出规格和方法动作入口关系"]
    RV --> RV2["执行写入路径：读回任务节点、方法节点、任务方法关系、主动动作入口、目标特征、特征值归属和当前特征状态材料"]
    RV --> RV3["动态证据路径：读回动态节点、场景、主体、发生时间戳、被改变目标、前后状态、来源动作和来源方法关系"]
    RV1 --> RV4{"各路径读回结果满足唯一性和归属复核"}
    RV2 --> RV4
    RV3 --> RV4
    RV4 -- "否" --> RVF["不得声明路径有效；后续详细设计 / 施工计划必须补追根因解决收口、数量快照和半结构不可读验证"]
    RV4 -- "是" --> OK["输出给第 12 项：动态记录 / 输出结果场景流程"]

    B --> G1["当前代码差距"]
    G1 --> G2["执行特征状态写入方法不实现真实调用、线程分派、工作队列、取消、超时或重试"]
    G2 --> G3["动作动态证据入口不等于任务结果回写、需求结算或稳定因果结论"]
    G3 --> G4["多步写入已有入口拒绝和追根因解决，但未证明完整事务回滚、失效隔离或数量快照级半结构不可读"]
    G4 --> G5["最小读回验证是本图门禁，不证明所有当前函数已内置统一事务级读回"]

    B --> X1["排除项"]
    X1 --> X2["线程 / 函数名 / 动作名文本 / 日志标签 / 控制台命令不得作为动作来源"]
    X1 --> X3["方法服务不得直接依赖特征值服务；特征状态材料必须经特征服务读写"]
    X1 --> X4["I64 只是一轮特征状态材料或状态值材料，不是需求目标语义"]
    X1 --> X5["不接真实本能动作函数指针注册表、std::async、外部执行器、SQL、控制面板、D455、体素或外设"]
```

## 完成边界

本图是当前代码证据基线，不是代码实施许可。当前存在未提交代码时，只能解释为当前工作区事实；未实现和部分实现节点不得扩大为系统完成。
