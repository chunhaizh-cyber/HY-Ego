# 控制面板功能现状流程图

更新时间：2026-07-11

## 元数据

```text
图类型：现状流程图
对应施工流程图：流程图/20260709_控制面板功能流程图_v0.1.md
代码版本：70c73a7；工作区干净
实现状态：已实现或已形成第一轮入口
覆盖文件：海中鱼巣/界面/控制面板窗口.cpp、海中鱼巣/领域/控制面板服务.h
覆盖函数：当前无可匹配正式入口
逐行映射表：实施记录/现状流程图核查/20260711_控制面板功能现状流程图_v0.1配套核查表.md 第 2 节
输入契约 / 调用语境表：实施记录/现状流程图核查/20260711_控制面板功能现状流程图_v0.1配套核查表.md 第 3 节
非成功返回二分审查表：实施记录/现状流程图核查/20260711_控制面板功能现状流程图_v0.1配套核查表.md 第 4 节
偏差清单：实施记录/现状流程图核查/20260711_控制面板功能现状流程图_v0.1配套核查表.md 第 5 节
不得作为施工许可：是
不得宣称：未匹配节点、未来候选或施工目标已经实现
```

## 现状说明

当前代码存在对应入口；完成范围仍以实施记录和现状边界为准。
本图与根目录施工图并存；施工图回答准备怎样实现，本图只回答当前代码实际存在什么入口和边界。

## 流程图

```mermaid
flowchart TD
    CURRENT_SCOPE["现状基线：已实现或已形成第一轮入口；未来/候选节点不视为当前已实现"] --> A
    A["用户明确请求：控制面板功能重建材料"] --> B["进入控制面板功能边界识别"]
    B --> C{"功能类型"}
    C -->|只读总览| D["读取显示服务：必须来自服务只读入口"]
    C -->|树视图 / 详情| E["读取树视图只读材料：世界树 / 概念树 / 语素树 / 需求树 / 任务树 / 方法树"]
    C -->|日志 / 统计观察| F["读取人读日志材料和非权威统计快照"]
    C -->|操作按钮或命令| G["降级为操作请求壳：只能生成请求材料"]
    C -->|数据库 / SQL| H["转数据库审计适配候选"]
    C -->|WebView / HTML / 截图| I["转人读渲染通道候选"]
    C -->|D455 / 体素 / 外设| J["转独立外设或体素专项"]
    D --> K{"显示请求是否试图越权读取、写事实或用文本裁决"}
    E --> K
    F --> K
    K -->|是| R0["拒绝：返回人读错误材料；权威结构不变"]
    K -->|否| L["组装控制面板只读视图材料"]
    L --> M["功能区：世界树 / 概念树 / 语素树 / 需求树 / 任务树 / 方法树 / 因果引用 / 统计缓存 / 日志审计"]
    M --> N{"是否请求外部显示通道"}
    N -->|否| O["输出控制台或后续只读视图材料；不写机器事实"]
    N -->|是| P["当前显示服务返回后续专项门禁"]
    G --> Q{"请求是否试图直接写业务事实"}
    Q -->|是| R1["拒绝：控制面板按钮不得写节点、关系、任务、方法或状态"]
    Q -->|否| S["后续只能交给已确认领域服务入口；本图不执行"]
    H --> T{"SQL 是否试图裁决运行期事实"}
    T -->|是| R2["拒绝：运行期核心以内存仓库为准"]
    T -->|否| U["仅可作为快照、事件日志、恢复、审计或只读查询候选"]
    I --> V["只允许渲染已生成的人读材料；HTML / JSON / 截图不得承载机器事实"]
    J --> W["等待外设材料规范、体素服务或真实样本专项；不得借控制面板接入"]
    P --> X["后续专项门禁：控制面板只读专项 / 数据库审计专项 / WebView 渲染专项"]
    S --> X
    U --> X
    V --> X
    W --> X
    O --> Y["完成：文档级功能图生成；不入队、不改 C++、不运行构建"]
    X --> Y
```

## 完成边界

本图是当前代码证据基线，不是代码实施许可。当前存在未提交代码时，只能解释为当前工作区事实；未实现和部分实现节点不得扩大为系统完成。
