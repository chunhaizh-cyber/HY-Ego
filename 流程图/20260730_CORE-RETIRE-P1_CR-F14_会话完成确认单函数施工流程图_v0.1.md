# CR-F14 会话完成确认单函数施工流程图

日期：2026-07-30
版本：v0.1
代码基线：`57866ac5ca63235ed12da60f635d29f1aacd718b`

## 依据与身份

依据：业务图 B11—B15；函数功能确认 CR-F14；逐节点分析 CR-F14；详细设计 §5、§7—§9。
本图是待实现目标函数的正式施工图。

## 函数合同

```text
根函数：完成确认
归属：海中鱼巣.核心.会话.节点直接身份结构写入 / 核心/会话.节点直接身份结构写入.ixx / 会话编排层私有成员
完整签名：节点直接身份结构写入结果 节点直接身份结构写入会话::完成确认()
参数：无。
返回：节点直接身份结构写入结果，值归执行器。
- 候选已确认：全部非空写集按固定顺序确认并把会话阶段推进为已确认待发布。
- 幂等读回：全部写集为空且存在合法幂等结果，阶段推进为已确认待发布。
- 内部不一致或底层映射结果：任一前置/候选确认失败时立即返回；不发布。
确认顺序：节点创建→新关系→关系变更→索引创建→索引移除→节点删除。
```

被调函数：
- CR-F35 `节点直接身份结构写入会话::全部写集已读回() const noexcept`。
- CR-F36 `节点直接身份结构写入会话::全部写集为空() const noexcept`。
- `节点直接身份仓库::确认节点候选`（节点创建）。
- `正式关系仓库::确认候选`（新关系、失效/重挂）。
- CR-F09 `可重建索引仓库::确认候选`，见 `流程图/20260730_CORE-RETIRE-P1_CR-F09_确认索引候选单函数施工流程图_v0.1.md`。
- `节点直接身份仓库::确认节点删除候选`（节点删除）。
调用者：`节点直接身份结构写入执行器::执行` 的确认阶段。失败后由 CR-F15 逆序撤销；成功后由 CR-F37 `完成发布()` 进入无失败区。

## 流程图

```mermaid
flowchart TD
    X00["X00【函数调用】bool 节点直接身份结构写入会话::全部写集已读回() const noexcept：无参数；接收全部已读回"] -- "完成" --> N01{"N01【指令-判断】已请求提交、无首次失败且全部已读回"}
    N01 -- "否" --> N02["N02【指令-返回】返回内部不一致，不推进阶段"]
    N01 -- "是" --> X00A["X00A【函数调用】bool 节点直接身份结构写入会话::全部写集为空() const noexcept：无参数；接收全部为空"]
    X00A -- "完成" --> N03{"N03【指令-判断】全部为空"}
    N03 -- "是且有合法幂等结果" --> N04["N04【指令】把会话阶段推进为已确认待发布"]
    N04 -- "完成" --> N05["N05【指令-返回】返回幂等读回"]
    N03 -- "是且无幂等结果" --> N02
    N03 -- "否" --> N06["N06【指令-赋值】节点创建位置=0"]
    N06 -- "完成" --> B06{"B06【指令-判断】节点创建位置到达节点创建写集末尾"}
    B06 -- "否" --> X06["X06【函数调用】节点直接身份操作结果 节点直接身份仓库::确认节点候选(节点直接身份未发布候选& 候选,std::uint64_t 事务序号)：写入项.候选→候选，事务序号_→事务序号；接收节点结果"]
    X06 -- "完成" --> V06{"V06【指令-判断】节点结果.状态是否为已确认待发布"}
    V06 -- "否" --> N08["N08【指令-返回】映射当前底层失败，不确认后续项或组"]
    V06 -- "是" --> P06["P06【指令-推进】节点创建位置加一"]
    P06 -- "完成" --> B06
    B06 -- "是" --> N09["N09【指令-赋值】新关系位置=0"]
    N09 -- "完成" --> B09{"B09【指令-判断】新关系位置到达新关系写集末尾"}
    B09 -- "否" --> X09["X09【函数调用】正式关系操作状态 正式关系仓库::确认候选(正式关系候选& 候选,std::uint64_t 事务序号)：写入项.候选→候选，事务序号_→事务序号；接收关系状态"]
    X09 -- "完成" --> V09{"V09【指令-判断】关系状态是否为已确认待发布"}
    V09 -- "否" --> N08
    V09 -- "是" --> P09["P09【指令-推进】新关系位置加一"]
    P09 -- "完成" --> B09
    B09 -- "是" --> N11["N11【指令-赋值】关系变更位置=0"]
    N11 -- "完成" --> B11{"B11【指令-判断】关系变更位置到达关系变更写集末尾"}
    B11 -- "否" --> X11["X11【函数调用】正式关系操作状态 正式关系仓库::确认候选(正式关系候选& 候选,std::uint64_t 事务序号)：写入项.候选→候选，事务序号_→事务序号；接收关系状态"]
    X11 -- "完成" --> V11{"V11【指令-判断】关系状态是否为已确认待发布"}
    V11 -- "否" --> N08
    V11 -- "是" --> P11["P11【指令-推进】关系变更位置加一"]
    P11 -- "完成" --> B11
    B11 -- "是" --> N13["N13【指令-赋值】索引创建位置=0"]
    N13 -- "完成" --> B13{"B13【指令-判断】索引创建位置到达索引创建写集末尾"}
    B13 -- "否" --> X13["X13【函数调用】可重建索引操作状态 可重建索引仓库::确认候选(可重建索引候选& 候选,std::uint64_t 事务序号)：写入项.候选→候选，事务序号_→事务序号；接收索引状态"]
    X13 -- "完成" --> V13{"V13【指令-判断】索引状态是否为已确认待发布"}
    V13 -- "否" --> N08
    V13 -- "是" --> P13["P13【指令-推进】索引创建位置加一"]
    P13 -- "完成" --> B13
    B13 -- "是" --> N15["N15【指令-赋值】索引移除位置=0"]
    N15 -- "完成" --> B15{"B15【指令-判断】索引移除位置到达索引移除写集末尾"}
    B15 -- "否" --> X15["X15【函数调用】可重建索引操作状态 可重建索引仓库::确认候选(可重建索引候选& 候选,std::uint64_t 事务序号)：写入项.候选→候选，事务序号_→事务序号；接收索引状态"]
    X15 -- "完成" --> V15{"V15【指令-判断】索引状态是否为已确认待发布"}
    V15 -- "否" --> N08
    V15 -- "是" --> P15["P15【指令-推进】索引移除位置加一"]
    P15 -- "完成" --> B15
    B15 -- "是" --> N17["N17【指令-赋值】节点删除位置=0"]
    N17 -- "完成" --> B17{"B17【指令-判断】节点删除位置到达节点删除写集末尾"}
    B17 -- "否" --> X17["X17【函数调用】节点直接身份操作结果 节点直接身份仓库::确认节点删除候选(节点直接身份删除未发布候选& 候选,std::uint64_t 事务序号)：写入项.候选→候选，事务序号_→事务序号；接收节点结果"]
    X17 -- "完成" --> V17{"V17【指令-判断】节点结果.状态是否为已确认待发布"}
    V17 -- "否" --> N08
    V17 -- "是" --> P17["P17【指令-推进】节点删除位置加一"]
    P17 -- "完成" --> B17
    B17 -- "是" --> N19["N19【指令-赋值】把会话阶段推进为已确认待发布"]
    N19 -- "完成" --> N20["N20【指令-返回】返回候选已确认"]
```

## 节点合同与边界

| 节点 | 事实读写 | 顺序/失败边界 | 验证 |
| --- | --- | --- | --- |
| X00—N05 | 具名调用 CR-F35/CR-F36 后只读会话决定、失败和读回标记 | 空写集只有合法幂等可确认 | T35、T45 |
| N06—N16 | 依次确认创建/关系/索引候选 | 任一失败立即返回，后组不确认 | T35—T42 |
| N17—N20 | 节点删除最后确认并推进会话阶段 | 删除确认不得先于引用退役确认 | T39—T45 |

确认不改变普通读取可见性，不清除撤销材料，也不发布任何候选。部分组已确认而后组失败时，执行器必须调用 CR-F15，从节点删除到节点创建逆序尝试全部撤销；不得就地继续发布。
