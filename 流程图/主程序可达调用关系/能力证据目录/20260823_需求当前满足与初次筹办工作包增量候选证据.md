# 需求当前满足与初次筹办工作包增量候选证据

证据批次：`CAP-INC-20260823-DEMAND-CURRENT-SATISFACTION-AND-INITIAL-PLANNING`

当前证据基线：`172b6969174d699e38094cd887d1004d3375172f`

需求当前满足实现：`a597f24bbf3917e83fb1cab3d83a962ed10946c3`

初次筹办实现：`17edb9d974be6d961d404247c88a064b8f73df58`

独立验收：`7d3e7c9e32623aaa9aa4b4c90b53ef634eb6f094`；结论：`INTEGRATION-ACCEPTANCE-FAIL / 未正式接线`

局部图谱：`172b6969174d699e38094cd887d1004d3375172f`

本文件只登记两个已发布代码增量，不改写仍绑定旧全量冻结基线的`当前能力证据目录.json`、`当前能力证据目录.md`和`能力候选队列.json`，也不改变1287项候选、45项稳定条目和342个提供函数的旧统计。

## 1. 候选能力

### CAP-INC-20260823-02 单个需求同 G0 当前满足裁决

- 候选状态：`候选待复核`；`172b6969`局部子图只把该入口列为R1515的既有依赖，没有为它分配新的R稳定身份。
- 提供入口：`需求当前满足裁决结果 海中鱼巣::运行期只读查询组合器::裁决需求当前满足(const 需求当前满足裁决请求& 请求) const noexcept`。
- 作用：在一份非零G0下读取需求、当前成员、完整列表项、目标宿主的当前特征、完整目标状态合同和当前比较注册，调用正式特征比较后返回`已满足/未满足`及完整裁决快照。
- 事实边界：全程只读，不刷新G0、不重试、不读取任务结果，不写需求、任务或结算事实。
- 当前调用语境：由R1515前置调用并由治理专项直接验证；R1515自身尚无普通应用正式消费者，因此该边不构成生产接线。

### CAP-INC-20260823-03 唯一任务、首次准备记录与轮次1工作包形成

- 候选状态：`候选待复核 / 未正式接线`。
- 候选提供入口：R1515 `需求初次筹办准备提供者::形成唯一任务与初次筹办工作包(...)`。
- task owner公开依赖：R1517融合写、R1518按准备键首次读回、R1519按记录身份当前/历史读回。
- 作用：先跨生命周期读取首次准备记录；只有未找到且G0仍当前时才裁决需求。未满足时由task owner在同一G1原子发布任务、任务虚拟存在、首次记录、7条关系、3个值和3个属性槽，再以首次G0/G1事实形成轮次1工作包。
- 幂等边界：原键原完整请求返回首次结果；同键异义返回`幂等冲突`；发布未知只按原键收敛，不换键、不刷新请求、不重新裁决。
- 生命周期边界：任务退出会闭合记录到任务和记录到任务虚拟存在的两条强关系；首次记录节点和值保留历史可读。

## 2. 图谱身份

| 身份 | 函数 | 分类 | 当前证据 |
| --- | --- | --- | --- |
| R1515 | `需求初次筹办准备提供者::形成唯一任务与初次筹办工作包(...)` | 候选业务入口 | 唯一仓库内动态调用者仍是治理专项 |
| R1516 | `需求初次筹办准备内部::形成首次结果(...)` | 内部组包 | 只从权威首次记录、任务和身份来源形成结果 |
| R1517 | `L2任务结构服务::新增任务与首次初次筹办准备记录(...)` | task owner公开写入口 | 单一owner-scoped融合事务与G1历史读回 |
| R1518 | `L2任务结构服务::按初次筹办准备幂等身份读取首次记录(...) const` | task owner公开读入口 | 跨生命周期按首次写入材料恢复首次记录 |
| R1519 | `L2任务结构服务::读取首次初次筹办准备记录(...) const` | task owner公开读入口 | 按记录身份提供当前/历史读取 |
| R1520 | `L2任务结构内部::形成初次筹办融合写集(...)` | 内部事务helper | 形成3节点、7关系、3值和3属性槽的待提交写集 |
| R1521 | `L2任务结构内部::读回初次筹办融合首次结果(...)` | 内部读回helper | 逐项互证已发布G1历史事实 |
| R1522 | `L2任务结构内部::按初次筹办准备幂等身份读取首次记录投影(...)` | 内部投影helper | 从L1首次写入见证恢复融合记录；普通任务旧写返回空记录 |

初始化登记、编码/解码helper和治理专项整函数尚未进入本局部图的R身份表，保持`候选待复核`，不得补造稳定身份。

## 3. 验证与验收分账

- 需求当前满足计划内验证：V01—V05、V08—V09、V12—V13、V15—V17、V19—V20为PASS；V06、V07、V10、V11、V12A、V14、V18为`NOT_RUN`。
- 初次筹办计划内验证：V01—V03、V05—V06、V08、V10—V11、V13—V14为PASS；V04和V07各含明确`NOT_RUN`子项，V09、V12、V15为`NOT_RUN`。
- 独立验收为FAIL：普通应用正式入口巡检`0/5`；provider、融合写和两类读回的动态调用只在治理专项；Release目标仍无条件包含测试模块和`--test-治理`。
- 已发布施工构建和治理专项结果只证明具名候选范围，不能替代普通应用合法消费者、正式独立读回或服务导向集成验收。

## 4. 明确未实现

- 普通筹办线程消费轮次1工作包；
- 父需求结果回流和已有任务的新筹办轮次；
- 任务完成后的逐需求成员核算；
- 先裁决结果可共用/争用，再按关系处理；
- 争用时按正式成员分配优先级顺序满足。

可共用关系不进入优先级排队；只有争用关系才使用正式成员分配优先级。当前没有该优先级的机器合同/provider，需求优先级、任务基础优先级、列表顺序和遍历顺序均不得替代。

## 5. 证据

- 当前调用子图：`流程图/主程序可达调用关系/06_需求初次筹办工作包当前调用子图.md`。
- 需求当前满足施工/验证：`施工记录/20260823_RUNTIME-DEMAND-CURRENT-SATISFACTION-ADJUDICATION_施工记录_v0.1.md`、`验证记录/20260823_RUNTIME-DEMAND-CURRENT-SATISFACTION-ADJUDICATION_验证记录_v0.1.md`。
- 初次筹办施工/验证：`施工记录/20260823_RUNTIME-GOVERNANCE-DEMAND-INITIAL-PLANNING-WORK-PACKAGE_施工记录_v0.3.md`、`验证记录/20260823_RUNTIME-GOVERNANCE-DEMAND-INITIAL-PLANNING-WORK-PACKAGE_验证记录_v0.3.md`。
- 独立验收：`验证记录/集成验收/RUNTIME-GOVERNANCE-DEMAND-INITIAL-PLANNING-WORK-PACKAGE-v0.3/20260823_需求初次筹办准备服务独立集成验收记录_v0.3.md`。
