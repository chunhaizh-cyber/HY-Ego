# `main` 当前可达调用关系

本目录的 `00`—`04` 聚合表记录提交 `720f4afd78e63d95d244867aa13d72970a419654`、`Debug|x64` 下从 `int main(int 参数数量, char* 参数组[])` 出发的历史闭合快照。当前 `dd8a7810693b7892b3265333f762b7c1c3bcd4d8` 增量只在具名子图中登记；两种基线不得合并解读为全量当前闭包。

## 旧聚合快照统计

- 可达项目函数：19。
- 项目调用边：20。
- 外部边界：24。
- 具名回调表项：0；信号回调F0058按项目边E0075登记。
- 未解析调用：0。
- 具名排除函数：2074。
- 工程配置：`Debug|x64`；D455能力宏关闭。

## `720f4afd` 基线重建说明

相对旧图谱代码基线 `4b80f1617dd70ec7a19e1a34efa9da768dce8927`，当前提交删除205个旧架构源码文件并引入L1事实基座与L1领域模块。维护过程没有接受自动删除结论，而是从F0001重新读取入口、启动解析、普通应用装配、程序宿主和L1事实基座构造链，重建BFS与正反投影。

当前普通控制面板和无窗口路径只到达19个项目函数。新增L1领域服务虽已登记工程，但普通应用上下文当前只默认构造L1事实基座服务和仓库；没有真实调用边的模块不进入可达身份表。旧函数和调用边进入排除账；已有旧图原地保留为历史证据。

## 文件

- `00_main可达函数身份表.json/.md`
- `01_main可达项目调用边表.json/.md`
- `02_main可达外部边界表.json/.md`
- `03_main未解析调用表.json/.md`
- `04_main不可达与配置排除表.json/.md`
- `05_需求任务子目标承接当前调用子图.md`：`d534058e` 复核的需求—任务子目标承接增量；包含生产装配边、治理专项实际消费边及 `NOT_RUN / 待展开` 边界。
- `06_需求初次筹办工作包当前调用子图.md`：`7d3e7c9e` 复核的首次准备记录、融合任务原子建立和首次历史读回增量；明确治理专项不是普通生产接线，后继回流 / 重筹办 / 结算 / 共用争用仍未实现。
- `07_任务统一筹办轮次权威当前调用子图.md`：`dd8a7810` 复核的新任务轮次 1 权威、统一 / legacy 分区和实际结果未达成 N→N+1 推进增量；生产任务管理线程已接该重筹办 provider，但 legacy 迁移、子目标回流和结算链仍未实现。

本目录只证明当前实现关系，不证明业务能力、构建、运行、集成验收或规范符合。
