# `main` 当前可达调用关系

本目录独立记录从正式入口 `int main(int 参数数量, char* 参数组[])` 出发、在当前正式工程配置和真实接线下可达的项目自定义函数与调用关系。

## 统计口径

- 唯一根：`海中鱼巣/入口.cpp:8` 的 `main`。
- 项目函数：只收录当前配置下能沿项目调用边从根递归到达的项目自定义函数。
- 外部边界：标准库、Windows、SQL/ODBC、RealSense、编译器隐式函数等单独登记，不生成项目函数身份。
- 未解析调用：函数指针、虚调用、回调或其它无法唯一确定目标的调用单独登记，不猜测唯一被调方。
- 排除：条件编译关闭、装配为空、仅定义未调用、旧不可达路径和不可满足分支必须具名记录。

本目录不改写 `流程图/代码流程图/` 中以单函数制图进度为目的的既有统计。两套材料的职责不同：

- `流程图/代码流程图/`：单函数流程图、逐行映射及其制图进度。
- 本目录：`main` 当前可达函数身份和调用关系的独立核对表。

## 冻结基线与完成统计

- 当前观测提交：`03a8b7ac099ccea83e57f2696e2290b7bcdfacaf`。
- 源码冻结提交：`03a8b7ac099ccea83e57f2696e2290b7bcdfacaf`；关系表已按该提交的当前 Debug|x64 装配和调用闭包重新冻结。
- 工程配置：`Debug|x64`；D455 能力宏按当前工程配置关闭。
- 可达项目函数：1383 个。
- 项目调用边记录：4103 条；同一 caller/callee 的多个调用点保留在同一记录时不重复计边。
- 外部边界记录：3743 条。
- 具名回调注册：59 条；能够按当前真实注册接线唯一解析的项目回调目标同时登记为具名项目调用边。
- 无法唯一解析的动态调用：0 条。
- 从旧图谱候选中排除的函数：96 个。
- 经源码、静态接收者或重载复核拒绝的 MCP 候选边：546 条。

上述数量只裁决本基线和配置下从 `main` 出发的可达关系，不是项目源码全部定义的全量函数清单，也不证明任何能力、规范符合性、构建或运行结果。

## 形成方法

本批以已发布调用图为可复用基座，并使用 fresh codebase-memory MCP、MSVC `Debug|x64` 模块依赖扫描、Clang AST/libclang 候选和逐调用点源码复核交叉收口。模块候选观测提交为 `4c89f990`，该提交至本表代码基线的 `海中鱼巣/**` 源码差异为零；普通 `.cpp` 候选已在当前基线重新扫描。工具输出只用于发现候选；以下情况均由源码和当前装配最终裁决：

- 同名与重载目标；
- 条件编译和当前参数模式；
- 函数指针、回调、局部 lambda 和递归回边；
- 结构事务接线的唯一目标或未解析状态；
- 项目函数与标准库、Windows、SQL/ODBC、RealSense 等外部边界。

当前完整自检入口登记入口初始化 15 项及 `F27/F29`；新增 S15 自我场景多存在特征状态自检及其直接递归闭包进入当前可达图谱。默认化特殊成员是否建立独立身份由当前实际调用语义裁决：实际参与当前调用链的默认构造、移动或比较函数单独登记；没有当前调用证据的平凡 / 默认化定义不因存在声明而自动纳入。

本次增量复核还完成了以下局部事实校正：

- 修正候选定义范围截断、相邻函数串入和跨文件传递头误归属；
- 把 `概念图初始化服务::确保根` 的四个实际模板实例按完整签名拆成独立身份；
- 按窗口类注册的函数指针接线恢复控制面板窗口过程回调；
- 将 `概念图服务.h:4232` 的静态接收者裁决为 `概念活动关系材料::完整()`，不再误连动态模式关系材料；
- 将 `语素服务.h:35—38` 的三类 `句柄有效` 调用按节点、主信息和关系句柄重载拆账；
- 将 `语素服务.h:106` 裁决为一参数概念追溯准入重载，并闭合该重载与语素对应信息准入函数的节点读取和普通关系读取后继边；
- 对 F0365—F0367 局部复核：删除 F0048 对三个 lambda 正文的重复归属边，补齐主信息、节点、读回与有效性直接边；把 `动态服务.h:294` 从两参数事务重载 R0250 修正为一参数重载 R0610，并新增 R0609/R0610 独立身份及其其它当前可达调用方；
- 对 F0368 局部复核：确认其唯一当前可达调用方为 F0188，删除 F0048 对 F0368 正文的伪归属边；将 `因果服务.h:57` 精确绑定无参数重载 R0611，删除误归属到双参数 R0274 的边；R0274、R0273 与 R0251—R0258 因失去唯一可达前驱退出当前闭包，并补齐 F0368/R0611 的项目调用与 `std::optional` 外部边界；
- 对 F0369—F0370 局部复核：补齐 F0369→F0190 的节点读取边与 F0370→F0051 的强类型句柄相等边，并把节点记录 optional、概念类别 optional 与图写锁 RAII 归入外部边界账；
- 对 F0385—F0388 局部复核：把 F0385 第 45 行静态接收者校正为 F0173 并独立登记 RCE1748；补齐 F0387 共享许可宏展开中的 F0336 / F0337 / F0397 / F0375 / F0338 / F0339 / F0340 七条直接项目边，并在 E1818 合并两次许可析构语境；把 F0388 的 397—401 行标准算法谓词登记为 R0612 / RCE1749，删除把 R0306 重复误分类为外部边界的 X01020；
- 对 F0393 局部复核：补齐第 1528 行 `失败阶段.empty()` 的标准库字符串边界 X02310；项目调用边与当前 20 个调用方、39 个调用点保持闭合；
- 对 F0397—F0400 局部复核：确认第 152—155 行唯一显式调用分别是已登记项目函数 R0101 / R0098，删除与 RCE0194—RCE0197 重复的误分类外部边界 X01068—X01071；
- 对 F0401—F0402 局部复核：删除把 F0401→R0099 项目调用 RCE0198 重复误分类为外部边界的 X01072；补齐 F0402 第 150 行构造成员 `执行器_` 时调用 R0114 的直接项目边 RCE1757；
- 对 F0405—F0408 局部复核：补齐 F0406 第 102 行构造成员 `执行器_` 时调用 R0114 的直接项目边 RCE1758；其余三个构造函数的直接项目边与外部 `std::move` 边界保持既有稳定身份；
- 对 F0409—F0412 局部复核：补齐 F0409 第 35 行构造成员 `执行器_` 时调用 R0114 的直接项目边 RCE1759；F0411/F0412 只保存数据操作引用，不产生额外项目或外部调用；
- 对 F0439、F0440 与 F0456 局部复核：补齐主信息表 `find/end/operator->` 三个标准库边界；补齐令牌宏展开的 R0089/F0340 两条边；把当前实际参与的默认比较链登记为 R0613—R0615 与 RCE1762—RCE1769；
- 对 F0458—F0469 局部复核：把 RCE0232 从错误的 R0456 校正到 R0563，把 RCE0248 从带令牌 R0083 校正到无参 F0198；补齐 28 条直接项目边、两项直接可达函数身份 R0726—R0727，以及数组、互斥锁、optional、`unique_ptr`、`std::function` 和值生命周期边界 X02490—X02517；同步校正 X01091、X01093、X01095 的具体模板签名；
- 对 F0478—F0497 与直接闭包局部复核：将 F0496/F0497 的单实参入口类型判断从带令牌 R0568 校正到无令牌 R0728，补齐两条 F0621 关系读取边、R0728→F0190 无令牌节点读取边和 R0568→F0346 带令牌节点读取边；新增 R0728 身份及 X02518—X02521，具体化 F0495 的两个 `std::find` 模板实例、迭代范围与结果比较边界；
- 对 F0498—F0517 局部复核：闭合四组入口初始化自检 lambda 的既有入出边；补齐 F0516→F0331/F0332/F0624 三条无令牌直接项目边 RCE2206—RCE2208，并据此把 F0624、R0009 的最短深度分别校正为 L6、L7；
- 对 F0518—F0537 局部复核：闭合三组入口初始化自检 lambda 及自我线程状态只读函数；补齐 F0523 接域删除路径的已接域、独占许可、许可有效、令牌读取与许可析构五条项目边 RCE2209—RCE2213，以及节点表 find/end/iterator 和 unique_lock 析构外部边界 X02522—X02525；
- 对 F0538—F0557 与直接闭包局部复核：闭合入口初始化自检 lambda、索引仓库读取及需求/特征值服务路径；新增 R0729—R0732 四项当前可达函数身份、RCE2214—RCE2258 四十五条项目调用边和 X02526—X02560 三十五项外部边界，并将 X01103—X01105 精确化为实际 unordered_map 迭代器边界；
- 对 F0558—F0577 与直接闭包局部复核：闭合入口初始化自检 lambda、自我线程只读状态、日志工具、强类型句柄校验及关系仓库性能基线夹具路径；新增 RCE2259—RCE2266 八条项目调用边和 X02561—X02599 三十九项外部边界，并精确化 RCE0258—RCE0268 与 X01108—X01143 的调用点、静态类型、参数和结果绑定；
- 对 F0578—F0593、F0618—F0621 与直接闭包局部复核：按已发布同宏先例闭合关系共享许可范围的真实展开路径，新增 RCE2267—RCE2373 一百零七条项目调用边和 X02600—X02711 一百一十二项外部边界；删除与 F0588 当前源码类型不相容的 X01171，并精确化本批既有 E/RCE 项目边及 X01144—X01200 中实际参与的锁、容器、算法、可选值、原子和时间边界；
- 对 F0622—F0636、F0638—F0642 与直接闭包局部复核：新增 R0733—R0738 六项当前可达函数身份、RCE2374—RCE2416 四十三条项目调用边和 X02712—X02737 二十六项外部边界；把 RCE0297 校正为节点句柄有效重载，删除 RCE0299 的错误重复调用点，把 X01205 精确化为主信息表迭代器比较，并同步闭合本批函数的调用参数、返回绑定与条件；
- 对 R0002—R0024 与直接闭包局部复核：确认 RCE0011 把 F0027 的 `读取租约()` 错归为 R0001，真实调用已由 F0069/E0095 覆盖；R0001、RCE0011、RCE0301 随之退出当前可达闭包，并删除 RCE0315；新增 R0739—R0746 八项当前可达函数身份、RCE2417—RCE2446 三十条项目调用边和 X02738—X02800 六十三项外部边界，把 RCE0325 校正为 F0565，并把 X01339—X01360 精确化为真实容器、锁、可选值、向量和移动边界；删除伪前驱后 R0004、R0007 的最短深度同步校正为 L11；
- 对 R0025—R0051 与直接闭包局部复核：新增 R0747 一项当前可达函数身份、RCE2447—RCE2481 三十五条项目调用边和 X02801—X02852 中四十六项外部边界；把 RCE0363 校正为 F0383、RCE0367 与 RCE0392 校正为 F0565、RCE0394 校正为 F0163；按非平凡生命周期和真实迭代器静态类型停用 X02820、X02823、X02827、X02829、X02846、X02851，且不移动其它稳定 ID；R0029 比较表达式的两个操作数保持无固定先后求值，不把文档顺序伪写成 C++ 执行顺序；
- 对 R0052—R0078 与直接闭包局部复核：新增 R0748 一项当前可达函数身份、RCE2482—RCE2501 二十条项目调用边和 X02853—X02911 五十九项外部边界；把 RCE0448 从同名关系记录比较函数校正为 F0051 节点句柄相等运算，把 RCE0472 的调用点收窄到第 1458 行，并具体化 RCE0441 的新建关系插入后比较语境；校正 X01495、X01496 的非 const 迭代器签名，停用与 RCE0442 重复记账的 X01497，且不移动其它稳定 ID；
- 对 R0079—R0108 与直接闭包局部复核：新增 R0749 析构身份、RCE2502—RCE2522 二十一条项目调用边和 X02912—X02991 八十项外部边界；把 RCE1525 校正为 F0579、RCE0063/RCE0075/RCE0080/RCE0101/RCE0103 校正为 F0198、RCE0521 校正为 F0398，并由反向闭包移出 R0081、R0083、R0118、R0117 及其不再可达的 RCE0486、RCE0492、RCE0572；校正 X01520、X01521、X01528—X01530，停用八项重复或项目函数误分类外部边界，且不移动其它稳定 ID；
- 对 R0109—R0140 与直接闭包局部复核：新增 R0750 无令牌删除主键身份、RCE2523—RCE2534 十二条项目调用边和 X02992—X03048 五十七项外部边界；把 RCE1587 从带令牌重载 R0123 校正到无令牌重载 R0750，删除不存在调用的 RCE0582 和误绑 const 自检重载的 RCE0036，R0139 随之退出当前闭包；停用与 F0398/RCE0556 重复记账的 X02319，精确化 X01571—X01601 的容器、迭代器、移动与智能指针签名，并闭合执行器、索引仓库和结构事务许可的非平凡生命周期；
- 对 R0141—R0189 局部复核：新增 RCE2535 一条结构事务接线函数指针调用边，删除把节点删除入口误连到撤销候选的 RCE0617，并把 RCE0605、RCE0611 收窄到真实调用点；新增 X03049—X03113 六十五项容器、数组、可选值、字符串、流、文件系统和 RAII 外部边界，停用把项目日志路径函数重复记为外部边界的 X01765、X01774，精确化 X01603、X01605、X01606、X01609 与 X01775；R0143、R0146 的真实最短深度同步校正为 L11；
- 对 R0190—R0249 与直接闭包局部复核：新增 R0751—R0789 三十九项当前可达函数身份、RCE2536—RCE2706 一百七十一条项目调用边、X03114—X03225 一百一十二项外部边界和 RCB0035—RCB0038 四项具名回调；把 RCE0820、RCE0826、RCE0829、RCE0830、RCE0832—RCE0834、RCE0839 校正到真实重载目标，并把 RCE0831 按节点、主信息与关系句柄重载拆账；停用 X01869、X01892、X01931、X01933—X01936；递归闭合基础信息读取与创建写侧、特征槽位当前值读取、标准算法回调和许可生命周期；
- 对 R0259—R0292、R0329、R0331—R0341 与直接闭包局部复核：新增 R0790—R0795 六项当前可达函数身份、RCE2707—RCE2748 四十二条项目调用边、X03226—X03454 二百二十九项外部边界和 RCB0039—RCB0043 五项具名回调；停用把节点句柄有效性误绑到关系句柄重载的 RCE0899、RCE0925、RCE0947、RCE0957，并停用 X02074、X02083、X02090 三项重复或误归属标准算法边界；补齐控制面板标准算法局部 lambda、概念活动重建与初始化的直接调用及 RAII 边界，R0399、R0400 的最短深度同步校正为 L8；
- 对 R0293—R0328、R0342—R0357、R0369—R0377 与直接闭包局部复核：新增 R0796—R0797 两项当前可达 lambda 身份、RCE2749—RCE2767 十九条项目调用边、X03455—X03714 二百六十项外部边界和 RCB0044—RCB0048 五项具名回调；停用 RCE0987、RCE1010、RCE1038、RCE1049 四条节点句柄误绑关系句柄重载边及 X02107、X02109、X02112、X02116、X02138、X02139 六项外层算法误归属或泛化边界；校正 RCE1187 到 F0163、X02137 调用次数为二，并闭合控制面板查找/排序回调、递归扩展及特征体系读侧直接边和 RAII 边界；
- 对 R0358—R0368 与直接闭包局部复核：新增 RCE2768—RCE2776 九条项目调用边和 X03715—X03748 三十四项外部边界；停用把双参数 `主键绑定匹配` 误绑到单参数重载的 RCE1151、RCE1155，精确化 X02149 的 `std::move` 模板实例，并区分 R0360 外层非 const iterator 与内层 const_iterator 的完整范围遍历边界；
- 对 R0378—R0405 与直接闭包局部复核：新增 R0798—R0799 两项当前可达函数身份、RCE2777—RCE2804 与 RCE2811—RCE2826 四十四条项目调用边，以及 X03749—X03758、X03771—X03809 四十九项外部边界；校正 RCE1188—RCE1191、RCE1195、RCE1201、RCE1206、RCE1242、RCE1244、RCE1246、RCE1254、RCE1259、RCE1685 与 X02155—X02157，停用 RCE1200、RCE1205、RCE1215、RCE1258、RCE1277 五条重载误绑或重复项目边；R0371—R0373、R0375 的最短深度同步校正为 L10；
- 对 R0406—R0441 与直接闭包局部复核：新增 R0830—R0832、R0840—R0841 五项当前可达函数身份、RCE2840—RCE2872、RCE2920—RCE2925、RCE2950—RCE2967、RCE3000—RCE3011 六十九条项目调用边和 X03810、X03826—X03923、X03931—X03942、X03981—X03998、X04021—X04068 一百七十七项外部边界；停用 RCE1295、RCE1348，校正十一条既有项目边与 X02163—X02164，并闭合系统角色、方法登记根、需求正式结算材料及关系证据读取路径；
- 对 R0442—R0477 与直接闭包局部复核：新增 R0842—R0846 五项当前可达函数身份，吸收 RCE3012—RCE3069、RCE3112 等稳定项目边与 X04069—X04478 分段外部边界；删除伪边 RCE1413、校正 RCE1433 后，R0040、R0329、R0747 退出当前闭包，R0040 与 R0329 的既有图转入历史保留清单；
- 对 R0502—R0527 与直接闭包局部复核：校正 RCE1505 / RCE1506 / RCE1516 三条重载或静态接收者边，并在 RCE1506 改绑后确认 R0504 失去唯一入边，删除已不可达的 RCE1513 并将 R0504 转为历史保留图；新增 R0879—R0881 三项当前可达身份、RCE3246—RCE3260 十五条项目边和 RCB0050—RCB0055 六项具名算法回调；具体化 X02189 与 X02202—X02209 / X02212，并补入规格 optional、锁、容器、迭代器和标准算法生命周期边界；
- 对 R0528—R0551 与直接闭包局部复核：新增 R0882—R0887 六项当前可达函数身份、RCE3261—RCE3273 十三条项目调用边、RCB0056—RCB0059 四项具名算法回调和 X04675—X04723 四十九项外部边界；把 RCE1551、RCE1553 的节点句柄有效性重载从 F0168 校正到 F0163，把 RCE1691 的 lambda caller 校正为 R0882，并退出误归属或重复的 X02218、X02229、X02233、X02237；新增 R0530→R0528 后，R0528、R0529 最短深度同步校正为 L11、L12；
- 对 R0552—R0575 与直接闭包局部复核：完成 23 个仍属当前闭包的正式排队函数现状图和逐行映射；将 RCE1583 从误判的双实参 R0568 校正到一实参 R0728，R0568 及其 RCE2205、X02252、X02253 随之退出当前闭包；新增 R0888—R0891 四项当前可达身份、RCE3274—RCE3313 四十条项目调用边和 X04724—X04767 四十四项外部边界，删除父函数算法重复错投 X02241，并校正 RCE1566、RCE1567、RCE1569、RCE1571、RCE1572、RCE1578—RCE1581、RCE1586、RCE1588—RCE1591 的真实重载或调用点；
- 对 03a8b7ac WORLD-TREE-P1A1 代码增量复核：三个新仓库模块与共享合同头虽已进入工程，但唯一实际消费者是未接线的节点直接身份结构写入专项自检；`运行节点直接身份结构写入自检()` 全项目无调用点，F0001 无入向前驱。故本次仅推进冻结基线并登记模块级配置排除，可达函数、项目调用边、外部边界和制图队列数量均不变；
- 对 R0576—R0599 与直接闭包局部复核：完成 22 个仍属当前闭包的正式排队函数现状图和逐行映射；五条单实参 `读取需求承接材料` 调用改绑 F0552 后，带令牌 R0586 及其唯一后继 R0595 退出闭包；新增 RCE3314—RCE3323 十条项目边和 X04768—X04821 中四十七项有效外部边界，停用退出函数的 X04805—X04811；同步重算全部正反投影、F0001 BFS 和受影响最短深度，其中 R0580 改为 L10、R0594 改为 L11，R0581—R0584 改为 L7；
- 对 R0478—R0501 局部复核：完成 24 个正式排队函数的现状图和逐行映射，新增 RCE3113—RCE3116 四条项目边与 X04479—X04533 五十五项外部边界，并删除错误边 RCE1460；
- 对入口初始化 S15、世界服务构造收缩与 R0501 直接闭包增量复核：新增 R0847—R0878 三十二项当前可达身份、RCE3117—RCE3245 一百二十九条去重后项目边、X04534—X04597 六十四项外部边界和 RCB0049；删除 RCE0012—RCE0025 十四条旧词法伪边，并同步更新受影响定义位置、调用点和最短深度；
- 补入 `语素服务.h:115` 使用默认顺序号 `0` 调用四参数 `关系仓库::创建关系` 的项目边；
- 按普通应用真实构造和空邮箱装配排除当前不可达的自我治理循环；
- 保持 `rs_context.h`、标准库、Windows、ODBC、RealSense 等外部定义只进入外部边界账。

`rs_context.h`、标准库、Windows、ODBC、RealSense 等外部定义不进入项目函数表，只在外部边界表中登记显式调用。已发布的 F0356—F0361 单函数流程图及其聚合材料保留在原目录，不属于本目录完成数，也不作为本目录的正式可达证据。

## 验证边界

发布前机械检查必须证明：

- `F0001` 的 BFS 闭包与函数身份表完全相等；
- 函数 ID、完整限定签名和重载身份均唯一；同一模板体的不同实例允许共享定义行，但“定义位置 + 完整重载身份”必须唯一；
- 每条项目边端点存在，调用方/被调方正反投影闭合；
- 回调、递归、未解析调用、外部边界和配置排除分账；
- JSON 可解析，Markdown 与 JSON 数量一致；
- `git diff --check`、未跟踪文件差异检查和 `tools/check_specs.py --strict` 通过。

本目录不生成新的单函数流程图，不恢复旧制图完成数，也不形成代码修订计划。

## 文件

- `00_main可达函数身份表.json/.md`：稳定身份、完整限定签名、定义范围、最短深度、全部直接调用者/被调者和可达依据。
- `01_main可达项目调用边表.json/.md`：项目调用方、被调方、调用点、类别、实参状态、可达条件和解析来源。
- `02_main可达外部边界表.json/.md`：外部调用边界与具名回调注册；外部定义不取得项目函数身份。
- `03_main未解析调用表.json/.md`：无法唯一解析的动态项目调用。
- `04_main不可达与配置排除表.json/.md`：旧候选排除、配置/装配排除、拒绝候选边和工具诊断。
