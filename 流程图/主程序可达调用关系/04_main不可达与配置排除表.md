# main 不可达与配置排除表

- 代码基线：<code>4b80f1617dd70ec7a19e1a34efa9da768dce8927</code>
- 旧候选中不在最终闭包的函数：96
- 被源码/静态类型审计拒绝的候选边：546

## 配置与装配排除

- <code>--d455-real-sample-exit / E0013</code>：Debug\|x64 默认 EnableD455RealSense=false，参数解析阶段拒绝
- <code>E0205</code>：当前唯一调用语境动态迭代次数为0
- <code>入口/数据库/性能/D455调试日志边</code>：对应调试日志宏在当前配置关闭
- <code>F0637治理邮箱分支</code>：普通应用使用三参自我线程构造，治理邮箱为空；五参构造仅存在于无入口自检
- <code>特征批次写入、批次参与包与特征体系分层自检</code>：当前 main 的完整自检只登记入口初始化14项及 F27/F29；生产与其它入口均未调用提交特征批次，故不得由通用参与者模板虚调用反向伪造批次参与者可达性
- <code>无当前调用证据的平凡 / 默认化特殊成员</code>：不因存在声明而自动建立项目函数身份；实际参与当前调用链的默认构造、移动或比较函数单独登记
- <code>语素服务::节点是可作为概念追溯目标(节点句柄, const 结构事务令牌&) const @ 海中鱼巣/领域/语素服务.h:310-326</code>：当前 main 可达调用点只绑定一参数重载；带结构事务令牌重载没有当前可达调用方，不得由同名候选误连
- <code>R0330 概念图结构业务结果::成功()</code>：旧 RCE0220 把 F0454 的系统角色初始化结果误绑到本函数；校正为 F0074 后无其它当前可达调用方，退出 F0001 闭包
- <code>SAFETY-P1 分层安全维护与任务权限模块组</code>：10 个新增模块未列入 vcxproj，且无当前装配、import、调用或自检注册，不进入 Debug\|x64 的 F0001 闭包
- <code>节点直接身份仅参与者事务链</code>：新核心入口只由未纳入工程的 SAFETY-P1 模块和无入口核心自检调用，不进入 F0001 闭包
- <code>WORLD-TREE-P1A1 节点直接记录仓与共享合同模块组</code>：代码提交 03a8b7ac 已登记三个仓库模块和共享合同头，但唯一实际消费者是未接线的节点直接身份结构写入专项自检；运行节点直接身份结构写入自检全项目无调用点，F0001 不存在入向前驱，因此只记模块级配置排除，不分配函数身份且不计入排除函数数
- <code>R0081 / R0083 关系仓库令牌重载</code>：RCE1525 实际绑定三实参无令牌重载 F0579；RCE0063、RCE0075、RCE0080、RCE0101、RCE0103 实际绑定零实参无令牌重载 F0198，两个令牌重载均无当前 main 可达调用方
- <code>R0118 / R0117 节点直接身份独占许可链</code>：RCE0521 的生产接线唯一绑定已校正为 F0398；R0118 随之失去当前可达调用方，R0117 仅由 R0118 调用并随反向闭包退出
- <code>R0139 关系仓库性能基线隔离关系夹具 const 读取重载</code>：自检.关系仓库性能基线.ixx:819 的夹具为非 const，实际绑定非 const 重载 F0288；const 重载没有其它当前 main 可达调用方

- <code>R0040 / R0329 / R0747 反向闭包退出</code>：删除 RCE1413 并把 RCE1433 校正到 R0466 后，R0040、R0329 失去全部当前可达前驱；R0747 仅由 R0040 调用并随之退出 F0001 当前闭包
- <code>R0586 / R0595 带令牌需求读取岛</code>：RCE0094、RCE0847、RCE0917、RCE1602、RCE1625 的当前调用点均为单实参无令牌重载 F0552；R0586 只剩未接线令牌任务服务岛调用，R0595 只由 R0586 及同一未接线岛调用，因此二者退出 F0001 当前闭包

- <code>WORLD-TREE 3242c166 新增候选排除</code>：P1A2 未接线探测/重试包装、三份载荷具名无前驱访问器和无实例化特殊成员保持候选排除；不分配当前可达函数身份，不把名称存在升级为调用事实。

## 工具诊断

- Clang模块候选：46成功、8 AST导出失败、102依赖PCM不可用、1存在诊断；只作候选，不据此删减源码事实
- Clang CPP候选：9文件、8成功、1存在诊断；只作候选
- fresh codebase-memory MCP：3384 Function/Method，9798 CALLS；低置信和 field_type_hint 必须经源码裁决

逐函数和逐候选边明细见同名 JSON；排除项不计入函数、项目边或制图完成数。
