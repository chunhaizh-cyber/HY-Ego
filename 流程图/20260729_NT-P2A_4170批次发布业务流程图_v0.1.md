# NT-P2A 4170 批次发布业务流程图

更新时间：2026-07-29

## 依据与身份

正式目标业务流程图。依据：2100 v0.6、4040、4050、4110 v0.5、4170 现行版。

业务闭环：以一个非零批次身份和规则版本 1，把连续有序的初始 / 换代混合项目、所有核心结构候选、全部类型化记录候选和一条独立 4170 记录在同一事务原子发布。

## 流程图

```mermaid
flowchart TD
    C01["C01【指令】复核批次身份、规则版本1、宿主和0..N-1项目"] --> C02["C02【函数调用】读取特征批次记录(批次身份) -> 事务外账"]
    C02 --> C03{"C03【判断】预读分类"}
    C03 -- "找到" --> C22["C22【函数调用】读取批次发布结果(批次身份) -> 历史结构材料"]
    C22 --> C23{"C23【判断】账和历史结构是否逐项目完整等于请求"}
    C23 -- "完整同义" --> C12["C12【返回】幂等读回；只返回本批次项目"]
    C23 -- "异义/不完整" --> C13
    C03 -- "异义/异常" --> C13["C13【返回】冲突或具名异常；零写入"]
    C03 -- "未找到" --> C04["C04【函数调用】执行(会话回调, [记录参与者, 批次参与者])"]
    C04 --> C05["C05【指令】独占许可内重读批次账并再次分类"]
    C05 --> C06{"C06【判断】锁内分类是否仍为未找到"}
    C06 -- "完整同义" --> C18["C18【指令】保存幂等领域结果并调用会话.请求撤销()"]
    C06 -- "异义/异常" --> C19["C19【指令】保存精确失败领域结果并调用会话.请求撤销()"]
    C18 --> C20["C20【返回】执行器候选已撤销后恢复幂等结果；否则内部错误"]
    C19 --> C21["C21【返回】执行器候选已撤销后恢复失败结果；否则内部错误"]
    C06 -- "是" --> C07["C07【循环】按顺序号形成每项新槽或换代核心候选和记录候选"]
    C07 --> C08["C08【指令】形成一条完整有序4170候选"]
    C08 --> C09["C09【指令】两参与者与核心写集按登记顺序准备/确认"]
    C09 --> C10{"C10【判断】全部确认"}
    C10 -- "否" --> C14["C14【指令】批次参与者→记录参与者→核心写集逆序撤销"]
    C10 -- "是" --> C11["C11【指令】同一最后发布区共同发布"]
    C11 --> C15["C15【函数调用】记录参与者.读取本事务发布结构版本() -> 确切发布版本"]
    C15 --> C16["C16【返回】回调内捕获的请求有序候选材料和确切版本；无宿主额外槽位"]
    C14 --> C17["C17【返回】失败结果；普通读取保持前态"]
```

## 业务节点—能力—函数映射

| 节点 | 所需能力 | 复用 / 新建 | 目标函数组 |
| --- | --- | --- | --- |
| C01—C03、C22—C23 | 4170 账预读、历史重建与完整同义 | 新建本域侧账；复用规范语义 | F-W104、F-W110、F-W113 |
| C04—C11 | 多参与者事务 | 复用核心执行器；新建记录 / 批次参与者 | F-W104—F-W108 |
| C07 | 初始 / 换代会话内候选 | 组合 F-W101/F-W102 的会话内算法，不启动子事务 | F-W104 |
| C15—C16 | 真实提交确切候选材料与记录仓版本 | 新建参与者回显 | F-A110、F-W104 |
| C18—C21 | 锁内竞争分类和显式撤销 | 复用核心请求撤销；新建领域结果恢复 | F-W104、F-W113 |

## 关键边界

- 一批次不能逐项提交，也不能先发布核心结构后补写 4170。
- 4170 不复制原始值；通过新值句柄回读类型化记录。
- 同键同义需同时证明账和全部结构；同键异义、顺序变化、来源变化或写前证据变化零写冲突。
- 锁内同键判断使用当前会话、关系审计、类型化记录访问器和批次账完成，不调用会再次取得结构读取许可的 F-W110；执行器撤销返回后才允许用 F-W110 形成幂等历史材料。
- 真实提交禁止事后调用 F-W110；返回回调内确切候选材料和 F-A110 本事务版本。
