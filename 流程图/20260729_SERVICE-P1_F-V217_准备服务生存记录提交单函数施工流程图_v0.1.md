# SERVICE-P1 F-V217 `服务生存维护记录参与者::准备提交` 单函数施工流程图 v0.1

图类型：目标施工流程图
状态：现行设计依据；私有 override 待 #379 实现，不表示代码已存在
归属模块：`海中鱼巣.领域.参与者.服务生存维护记录`
归属文件：`海中鱼巣/领域/参与者.服务生存维护记录.ixx`
唯一根函数：F-V217
完整签名：

```cpp
节点直接身份结构写入结果
服务生存维护记录参与者::准备提交(
    const 节点直接身份结构提交准备只读视图& 视图) noexcept override;
```

本函数由当前真实核心执行器在取得事务域独占许可后调用。它在服务记录仓独占锁内完成幂等/冲突/版本裁决、全部可能分配、私有待发布写入和首次读回；不写公开容器，不取得核心写会话。依据 0050、6160、6170、CORE-C1/v1 与服务详细设计 v0.3 第 7.3—7.4、10 节。

## 流程图

```mermaid
flowchart TD
    S["S【本函数内具体指令】只读绑定核心提交准备视图并检查参与者持有仓库借用、请求副本和合同或治理候选副本"]
    B01{"B01【本函数内具体指令】阶段为未准备，仓库指针非空，形态与合同/治理请求候选optional唯一配对，幂等主键、身份版本材料和发布代次完整？"}
    R01["R01【本函数内具体指令】返回内部不一致"]
    B02{"B02【本函数内具体指令】候选全部稳定身份来自提供者材料且不依赖视图中的本会话新节点，不生成或改写身份/版本？"}
    R02["R02【本函数内具体指令】返回身份冲突或内部不一致"]
    N03["N03【本函数内具体指令】以unique_lock取得仓库shared_mutex独占锁并移动保存到参与者独占锁_"]
    N04["N04【本函数内具体指令】按幂等主键读取唯一已发布结果和当前合同/维护账/值/集合版本"]
    B05{"B05【本函数内具体指令】同键已存在已发布结果？"}
    B06{"B06【本函数内具体指令】已发布结果与请求及候选身份、语义、事实截止、全部记录和值版本逐字段同义完整？"}
    N07["N07【本函数内具体指令】把权威结果复制到对应合同/治理幂等读回optional并把阶段记为幂等已准备"]
    R07["R07【本函数内具体指令】保持独占锁跨确认阶段并返回幂等读回"]
    R08["R08【本函数内具体指令】释放独占锁并返回身份冲突"]
    B09{"B09【本函数内具体指令】仓库当前版本与请求期望版本及候选输入版本逐项相等？"}
    R09["R09【本函数内具体指令】释放独占锁并返回版本漂移"]
    N10["N10【本函数内具体指令】在局部单项map中完成全部分配并extract唯一node到对应待发布合同/治理node_type成员"]
    B11{"B11【本函数内具体指令】待发布node非空且其键/值的幂等键、身份、字段、版本、发布代次与候选逐项相等，另一node为空且公开容器仍不可见？"}
    N12["N12【本函数内具体指令】记录参与者阶段为新候选已准备"]
    R12["R12【本函数内具体指令】保持独占锁跨确认阶段并返回已提交"]
    R13["R13【本函数内具体指令】清除本次私有半候选，释放独占锁并返回内部不一致"]
    E14["E14【本函数内具体指令】复制或分配失败时清除本次私有候选，释放独占锁并返回资源失败"]
    E15["E15【本函数内具体指令】取得unique_lock发生system_error或其它异常，或锁内出现非分配异常时清除暂态、释放已持锁并返回内部不一致"]
    S --> B01
    B01 -- "否" --> R01
    B01 -- "是" --> B02
    B02 -- "否" --> R02
    B02 -- "是" --> N03 --> N04 --> B05
    B05 -- "是" --> B06
    B06 -- "是" --> N07 --> R07
    B06 -- "否" --> R08
    B05 -- "否" --> B09
    B09 -- "否" --> R09
    B09 -- "是" --> N10 --> B11
    B11 -- "否" --> R13
    B11 -- "是" --> N12 --> R12
    N03 -. "system_error/其它异常" .-> E15
    N07 -. "分配异常" .-> E14
    N10 -. "分配异常" .-> E14
    N04 -. "其它异常" .-> E15
    N10 -. "其它异常" .-> E15
```

## 节点合同

| 节点 | 类型 | 输入 | 输出 | 事实/锁边界 |
| --- | --- | --- | --- | --- |
| S—B02 | 本函数内具体指令 | 参与者私有副本、只读视图 | 阶段与身份准入 | 零写入 |
| N03—B09 | 本函数内具体指令 | 已发布仓库、请求版本 | 幂等/冲突/版本裁决 | 服务记录仓独占锁 |
| N10—B11 | 本函数内具体指令 | 完整候选 | 单一预分配 map node 及首次读回 | 公开读取不可见 |
| N12—R14 | 本函数内具体指令 | 准备裁决 | 核心写入结果 | 成功/幂等保留锁；失败释放锁 |

## 实现约束

1. 只读视图不得保存、复制或送出调用期；本函数不得请求核心会话写入。
2. 同键同义完整结果唯一返回 `幂等读回`；同键异义返回 `身份冲突`，版本不等返回 `版本漂移`。
3. F-V219 发布需要的 map node 和完整值式结果在本函数局部单项 map 中预分配并以 `extract` 存入对应 `node_type` 成员，发布阶段只能 node insertion，不得再分配。
4. `已提交` 与 `幂等读回` 返回时参与者继续持有记录仓独占锁，F-V218 不得释放；只由 F-V219 或 F-V220 正常释放。
5. 准备失败必须清除本函数产生的全部私有候选并当场释放锁；公开容器和其它参与者事实保持不变。
6. `unique_lock` 构造、已发布结果复制和局部 map 分配都位于本函数 `try` 边界内：`bad_alloc` 唯一映射资源失败，`system_error` 与其它异常映射内部不一致；任何异常都不得越过 `noexcept`。
