# CR-F59 执行关系枚举会话回调单函数施工流程图

版本：v0.1
代码基线：`57866ac5ca63235ed12da60f635d29f1aacd718b`

## 函数合同

```text
根函数：执行关系枚举会话回调
物理位置：海中鱼巣/核心/自检.节点直接身份结构写入.ixx，CR-F45 之前内部区
完整签名：void 执行关系枚举会话回调(关系枚举会话回调上下文& 上下文, 节点直接身份结构写入会话& 会话)
参数：上下文和会话均为一次执行期可写借用，不保存；场景只允许合法空组读取并撤销=1
返回：void；四组值式结果写入上下文；失败由会话结构结果承载
```

```mermaid
flowchart TD
    N01{"N01【指令-判断】上下文.场景==合法空组读取并撤销"} -- "否" --> X00["X00【函数调用】void 节点直接身份结构写入会话::记录失败(const 节点直接身份结构写入结果& 失败) noexcept：入口拒绝结果→失败；返回void"]
    N01 -- "是" --> X01["X01【函数调用】std::vector<正式关系记录> 节点直接身份结构写入会话::读取有效关系组(节点句柄 源节点,关系类型 类型)：上下文.源节点→源节点，上下文.类型→类型；接收上下文.按源组"]
    X01 -- "完成" --> X02["X02【函数调用】std::vector<正式关系记录> 节点直接身份结构写入会话::读取指向有效关系组(节点句柄 目标节点,关系类型 类型)：上下文.目标节点→目标节点，上下文.类型→类型；接收上下文.按目标组"]
    X02 -- "完成" --> X03["X03【函数调用】std::vector<正式关系记录> 节点直接身份结构写入会话::读取相关有效关系组(节点句柄 节点,关系类型 类型)：上下文.相关节点→节点，上下文.类型→类型；接收上下文.相关组"]
    X03 -- "完成" --> X04["X04【函数调用】std::vector<正式关系记录> 节点直接身份结构写入会话::读取有效类型角色关系组(关系类型 类型,std::int64_t 顺序号)：上下文.类型→类型，上下文.顺序号→顺序号；接收上下文.类型角色组"]
    X04 -- "四组已接收" --> X05["X05【函数调用】void 节点直接身份结构写入会话::请求撤销() noexcept：无参数；返回void"]
    X00 -- "失败已记录" --> R01["R01【指令-返回】返回void"]
    X05 -- "撤销已请求" --> R01
```

调用者：CR-F45 通过 `std::bind_front(执行关系枚举会话回调,std::ref(上下文))` 传入现有执行器。被调方图：CR-F22、F04、F05、F06。
