# 节点直接状态动态查询服务读取世界状态动态函数流程图 v0.2

更新时间：2026-08-03

设计身份：WORLD-TREE-P1B-Q v0.1

## 1. 身份与边界

```text
唯一根函数：节点直接世界状态动态读取结果 节点直接状态动态查询服务::读取世界状态动态(const 节点直接世界状态动态读取请求& 请求) const noexcept
物理位置：海中鱼巣/领域/服务.节点直接状态动态查询.ixx
层级：L2 状态动态只读服务
施工形态：原位替换 P1B-F 的固定未实现函数体；公开 DTO、类构造、成员和签名不变
输入所有权：调用方拥有，请求仅在调用期 const 借用，函数不保存
输出所有权：调用方拥有完整值，不含许可、锁、仓库、句柄、引用或 SQL 对象
机器事实：只读内存权威事实；零写入、零比较、零发布、零 SQL；本层首次资源/内部异常仅写诊断旁路
```

被调业务提供者函数仅有：

```text
世界结构读取结果 节点直接世界结构读取服务::读取世界结构(const 节点直接世界结构读取请求& 请求) const noexcept
节点直接目标角色源投影结果 节点直接结构查询服务::读取目标角色命中源的当前关系与类型化值(const 节点直接目标角色源投影请求& 请求) const noexcept
```

诊断边仅有：

```text
void 安全记录状态动态查询逻辑异常_(const wchar_t* 说明) noexcept
bool 追根因检查(bool 条件, const wchar_t* 消息)  // 物理提供者：海中鱼巣/核心/容错检查.h
```

## 2. 单根函数流程图

```mermaid
flowchart TD
    N00["N00【指令-异常边界】try 包围 N01—N20 的全部查询、容器和解析指令"]
    N00 --> N01
    N00 -. "bad_alloc/length_error" .-> X01["X01【函数调用】安全记录状态动态查询逻辑异常_(资源异常说明)"]
    X01 --> X02["X02【函数调用】追根因检查(false,说明)；助手内部吞掉诊断异常"]
    X02 --> XR01["XR01【指令-返回】资源失败；代次0；三组空"]
    N00 -. "其它异常" .-> X03["X03【函数调用】安全记录状态动态查询逻辑异常_(内部异常说明)"]
    X03 --> X04["X04【函数调用】追根因检查(false,说明)；助手内部吞掉诊断异常"]
    X04 --> XR02["XR02【指令-返回】内部不一致；代次0；三组空"]
    N01["N01【指令-准入】校验查询合同版本、请求头合同/规则版本、世界根见证、场景/主体见证类型和组内稳定身份唯一"]
    N01 -->|非法| R01["R01【指令-返回】入口拒绝；代次0；状态组/动态组/缺项组空"]
    N01 -->|合法| N02["N02【指令-构造】节点直接世界结构读取请求{1,请求.头}"]
    N02 --> N03["N03【函数调用】世界结构_.读取世界结构(结构请求) -> 世界结构结果"]
    N03 -->|非成功| N04["N04【指令-映射】同名映射未找到/入口拒绝/版本漂移/许可拒绝/资源失败/内部不一致/未实现；下层结果不重复诊断"]
    N04 --> R02["R02【指令-返回】非成功结果；不返回部分状态或动态"]
    N03 -->|已读取且非零代次| N05["N05【指令-复核】世界根、请求场景组和主体组逐项在世界结构中精确存在；同主键异类型/版本为版本漂移，缺失为未找到"]
    N05 -->|非成功| R03["R03【指令-返回】未找到或版本漂移；状态组/动态组/缺项组空"]
    N05 -->|任一筛选组为空| R04["R04【指令-返回】已读取；携带世界结构非零代次；三组空"]
    N05 -->|两组均非空| N06["N06【指令-构造】必须节点=根+场景+主体精确去重；每主体追加关系19/20角色1筛选，每场景追加关系19/20角色2筛选"]
    N06 --> N07["N07【函数调用】结构_.读取目标角色命中源的当前关系与类型化值(投影请求) -> 投影结果"]
    N07 -->|非成功| N08["N08【指令-映射】映射L1状态；清空状态/动态/缺项；不保留部分投影"]
    N08 --> R05["R05【指令-返回】映射后的非成功结果"]
    N07 -->|成功| N09["N09【指令-同代次】要求世界结构代次=投影代次且均非零"]
    N09 -->|不相等或为0| R06["R06【指令-返回】版本漂移；代次0；三组空；本函数不重试"]
    N09 -->|相等| N10["N10【指令-解析状态】逐关系19源校验源类型、角色1至5恰一、端点类型、范围归属和唯一状态发生时间U64类型化值"]
    N10 -->|材料问题| N11["N11【指令-诊断】追加状态角色或端点缺项；按源和类别稳定去重"]
    N10 -->|完整| N12["N12【指令-形成】构造世界树状态事实并按状态稳定身份登记到本轮状态表"]
    N11 --> N13["N13【指令-解析动态】逐关系20源校验公共角色、动态种类适用角色、三项类型化值、来源和发布代次"]
    N12 --> N13
    N13 -->|材料问题| N14["N14【指令-诊断】追加动态角色或端点缺项；按源和类别稳定去重"]
    N13 -->|动作致变且角色6/8恰一、角色7非空并按目标唯一、版本非零；或聚合且角色6/8空、角色7非空并按目标唯一、版本非零| R09["R09【指令-返回】未实现；代次0；状态/动态/缺项组空；不诊断"]
    N13 -->|可证明非聚合角色和值完整| N15["N15【指令-端点复核】前后状态必须在本轮状态表且主体/场景/特征一致；时间严格递增；动态时间等于后状态时间；被改变目标合法"]
    N15 -->|端点问题| N16["N16【指令-诊断】追加端点缺项；不形成该动态"]
    N15 -->|完整| N17["N17【指令-形成】构造世界树动态事实"]
    N14 --> N18["N18【指令-裁决】检查是否存在任何缺项"]
    N16 --> N18
    N17 --> N18
    N18 -->|有缺项| N19["N19【指令-排序去重】缺项按种类、对象、关系稳定顺序排序并精确去重；清空状态组和动态组"]
    N19 --> D01["D01【函数调用】安全记录状态动态查询逻辑异常_(材料内部不一致说明)"]
    D01 --> D02["D02【函数调用】追根因检查(false,说明)；助手内部吞掉诊断异常"]
    D02 --> R07["R07【指令-返回】内部不一致；携带共同非零代次和稳定缺项组"]
    N18 -->|无缺项| N20["N20【指令-排序】状态按场景/主体/特征/发生时间/状态排序；动态按场景/后状态时间/动态排序"]
    N20 --> R08["R08【指令-返回】已读取；携带共同非零代次；完整状态组/动态组；缺项组空；零命中合法"]
```

## 3. 节点实现合同

| 节点组 | 输入 | 输出 | 分支和不变量 | 验证 |
| --- | --- | --- | --- | --- |
| N01—N05 | 请求、世界结构结果 | 已验证范围 | 请求组可空；非空项类型固定；只接受根、场景、主体的当前精确见证 | 非法、缺失、同主键异版本/类型矩阵 |
| N06—N09 | 已验证范围 | 单一共同事实截止的 P1B-M 材料 | 两组均非空才调用 P1B-M；两个提供者各恰调用一次；不重试 | AST 调用边和代次漂移测试 |
| N10—N12 | 关系19源投影 | 状态表或状态缺项 | 五角色恰一；未知/额外角色拒绝；发生时间值恰一且证据完整 | 角色、类型、值材料、来源、代次矩阵 |
| N13—N17 | 关系20源投影和状态表 | 非聚合动态事实、未实现或动态/端点缺项 | 公共角色恰一；四类非聚合形状精确；聚合动作要求角色6/8恰一、聚合动态要求角色6/8为空，两者角色7非空并按目标唯一且版本非零后才整轮未实现；前后状态来自本轮状态表 | 非聚合四类、两类聚合基础形状、聚合未实现、端点、时间和来源矩阵 |
| D01—D02、X01—X04 | 本层首次材料内部不一致、资源异常或其它异常 | 诊断旁路完成 | 公开根只调用私有无抛助手；助手唯一调用 `容错检查.h::追根因检查` 并吞掉诊断异常 | AST 两级调用边、诊断异常不改变机器返回 |
| N18—N20 | 临时完整事实及诊断 | 唯一结果 | 有缺项不返回部分事实；无缺项允许状态/动态同时为空 | 稳定排序、去重和全空合法成功 |

## 4. 异常边

整个函数体位于 `try` 内。`std::bad_alloc` 或 `std::length_error` 经模块私有无抛助手诊断一次后返回 `资源失败`、代次0、三组空；其它异常同样诊断一次后返回 `内部不一致`、代次0、三组空。本根发现并最终返回的材料内部不一致只诊断一次；下层非成功和合法未实现不重复诊断。异常不得越过 `noexcept`，不得重试、休眠、读时钟或改由 SQL 回源。

## 5. 完成边界

本图只冻结 P1B-Q 根函数的真实非聚合只读查询和聚合安全未实现过程。它不证明 P1B-F、P1B-M 或 P1A3 已实现，不证明聚合 DAG、状态/动态写入、比较、发布、完整世界快照、生产接线或服务验收完成。
