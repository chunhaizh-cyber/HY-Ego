# 读取世界结构函数流程图 v0.2

更新时间：2026-08-03

## 依据

```text
4200 v0.8；业务节点到能力与目标函数映射.md“世界事实维护”；WORLD-TREE-P1业务流程图Q01/Q02；P1A3F详细设计v0.1未替换段+v0.2；P1A3详细设计v0.2 §4.2/§5；P1A2节点/关系/类型合同/类型化值/代次公开查询合同
```

## 身份与边界

目标单函数施工图；一图唯一根函数。本函数从一个具名世界根向下遍历当前直接世界结构，只消费 L1 值式查询；不取得仓库、许可或事务，不写事实。

## 函数合同

```text
根函数：节点直接世界结构读取服务::读取世界结构
归属模块 / 文件 / 层级：海中鱼巣.领域.服务.节点直接世界结构 / 海中鱼巣/领域/服务.节点直接世界结构.ixx / L2
现有或待新增：P1A3F v0.2 骨架原位替换为真实行为
完整签名：世界结构读取结果 节点直接世界结构读取服务::读取世界结构(const 节点直接世界结构读取请求& 请求) const
参数：请求为const借用，只在同步调用内存续；合同/规则版本必须当前，世界根见证必须完整且类型=场景；不保存引用
返回结果：调用方拥有的值；只有已读取携带非零代次和完整稳定排序世界结构；非成功不携带部分场景/存在事实
被调函数流程图：无新增目标图；六个被调L1查询是已冻结提供者
```

## 流程图

```mermaid
flowchart TD
    N01{"N01【指令-判断】请求合同/世界规则为当前版本，且世界根见证完整、类型=场景"}
    N01 -->|"否"| R01["R01【指令-返回】入口拒绝、代次0、无部分结构"]
    N01 -->|"是"| T01["T01【指令-异常边界】以单一外层try覆盖N02—N42、D01—D02全部局部构造、调用和结果映射"]
    T01 -->|"进入受保护体"| N02["N02【指令-赋值】轮次=1；清空局部场景/存在/关系/资格表、小根堆、已访问/已排队集合和初始场景候选组"]
    N02 -->|"已清空"| N03["N03【函数调用】节点直接代次读取结果 节点直接结构查询服务::读取当前事实代次() const：无实参；返回->首代次结果"]
    N03 -->|"成功+非零"| N04["N04【函数调用】节点直接类型合同读取结果 节点直接结构查询服务::读取精确类型合同(类型合同稳定身份 合同身份,std::uint32_t 合同版本) const：世界场景资格类型合同->合同身份，1->合同版本；返回->场景合同结果"]
    N03 -->|"入口拒绝且代次0"| R01
    N03 -->|"许可拒绝且代次0"| R03["R03【指令-返回】许可拒绝、代次0、无部分结构"]
    N03 -->|"资源失败且代次0"| R04["R04【指令-返回】资源失败、代次0、无部分结构"]
    N03 -->|"未找到且代次0或版本漂移且代次0"| R02["R02【指令-返回】版本漂移、代次0、无部分结构"]
    N03 -->|"内部不一致、成功零代次或任一非成功携带非零代次"| R05["R05【指令-返回】内部不一致、代次0、稳定排序缺项、无部分成功"]
    N04 -->|"成功、非零、同首代次且场景合同全字段匹配"| N05["N05【函数调用】节点直接类型合同读取结果 节点直接结构查询服务::读取精确类型合同(类型合同稳定身份 合同身份,std::uint32_t 合同版本) const：世界存在资格类型合同->合同身份，1->合同版本；返回->存在合同结果"]
    N04 -->|"成功、非零且异于首代次"| D01{"D01【指令-判断】本轮是否只因成功或合法空关系的非零代次与首代次不等而到达，且轮次=1"}
    N04 -->|"入口拒绝且代次0"| R01
    N04 -->|"许可拒绝且代次0"| R03
    N04 -->|"资源失败且代次0"| R04
    N04 -->|"版本漂移且代次0"| R02
    N04 -->|"未找到（代次0或当前非零）或内部不一致且代次0"| R05
    N04 -->|"入口/版本/许可/资源/内部状态携带非零代次，成功零代次，或成功同首代次但载荷缺失/字段不符"| R05
    N05 -->|"成功、非零、同首代次且存在合同全字段匹配"| N06["N06【指令-初始化】小根堆加入{请求世界根,发现来源=根,预期入边=nullopt}；已排队集合加入根稳定主键"]
    N05 -->|"成功、非零且异于首代次"| D01
    N05 -->|"入口拒绝且代次0"| R01
    N05 -->|"许可拒绝且代次0"| R03
    N05 -->|"资源失败且代次0"| R04
    N05 -->|"版本漂移且代次0"| R02
    N05 -->|"未找到（代次0或当前非零）或内部不一致且代次0"| R05
    N05 -->|"入口/版本/许可/资源/内部状态携带非零代次，成功零代次，或成功同首代次但载荷缺失/字段不符"| R05
    N06 -->|"队列已建立"| N07{"N07【指令-判断】待访问小根堆是否为空"}
    N07 -->|"否"| N08["N08【指令-弹出】按节点稳定主键最小值弹出当前访问项"]
    N08 -->|"已访问"| R05
    N08 -->|"未访问"| N09["N09【指令-登记】当前主键加入已访问集合，从已排队集合移除"]
    N09 -->|"当前主键"| N10["N10【函数调用】节点直接节点读取结果 节点直接结构查询服务::读取当前节点(节点稳定主键 主键) const：当前主键->主键；返回->当前节点结果"]
    N10 -->|"成功、同代次且见证逐字段等于待访问见证"| N11["N11【函数调用】节点直接类型化值读取结果 节点直接结构查询服务::读取所属身份当前类型化值组(节点稳定主键 所属身份) const：当前主键->所属身份；返回->当前资格值结果"]
    N10 -->|"成功但代次不等"| D01
    N10 -->|"未找到/版本漂移"| R02
    N10 -->|"入口拒绝"| R01
    N10 -->|"许可拒绝"| R03
    N10 -->|"资源失败"| R04
    N10 -->|"内部不一致或见证不符"| R05
    N10 -->|"非法状态、载荷或代次组合"| R05
    N11 -->|"成功、同代次且非空值组"| N12["N12【函数调用】节点直接关系组读取结果 节点直接结构查询服务::读取按目标当前关系组(节点稳定主键 目标,关系类型 类型) const：当前主键->目标，普通父子->类型；返回->入父子结果"]
    N11 -->|"成功但代次不等"| D01
    N11 -->|"未找到/版本漂移"| R02
    N11 -->|"入口拒绝"| R01
    N11 -->|"许可拒绝"| R03
    N11 -->|"资源失败"| R04
    N11 -->|"内部不一致"| R05
    N11 -->|"非法状态、载荷或代次组合"| R05
    N12 -->|"成功非空或未找到空组，同首代次，关系类型/目标端/顺序完整"| N13["N13【函数调用】节点直接关系组读取结果 节点直接结构查询服务::读取按源当前关系组(节点稳定主键 源,关系类型 类型) const：当前主键->源，普通父子->类型；返回->出父子结果"]
    N12 -->|"合法结果但代次不等"| D01
    N12 -->|"入口拒绝"| R01
    N12 -->|"许可拒绝"| R03
    N12 -->|"资源失败"| R04
    N12 -->|"版本漂移"| R02
    N12 -->|"内部不一致或材料不符"| R05
    N12 -->|"未找到但非空、未找到代次0或其它非法状态载荷代次组合"| R05
    N13 -->|"成功非空或未找到空组，同首代次，关系类型/源端/顺序完整"| N14["N14【函数调用】节点直接关系组读取结果 节点直接结构查询服务::读取按目标当前关系组(节点稳定主键 目标,关系类型 类型) const：当前主键->目标，归属->类型；返回->入归属结果"]
    N13 -->|"合法结果但代次不等"| D01
    N13 -->|"入口拒绝"| R01
    N13 -->|"许可拒绝"| R03
    N13 -->|"资源失败"| R04
    N13 -->|"版本漂移"| R02
    N13 -->|"内部不一致或材料不符"| R05
    N13 -->|"未找到但非空、未找到代次0或其它非法状态载荷代次组合"| R05
    N14 -->|"成功非空或未找到空组，同首代次，关系类型/目标端/顺序完整"| N15["N15【函数调用】节点直接关系组读取结果 节点直接结构查询服务::读取按源当前关系组(节点稳定主键 源,关系类型 类型) const：当前主键->源，归属->类型；返回->出归属结果"]
    N14 -->|"合法结果但代次不等"| D01
    N14 -->|"入口拒绝"| R01
    N14 -->|"许可拒绝"| R03
    N14 -->|"资源失败"| R04
    N14 -->|"版本漂移"| R02
    N14 -->|"内部不一致或材料不符"| R05
    N14 -->|"未找到但非空、未找到代次0或其它非法状态载荷代次组合"| R05
    N15 -->|"成功非空或未找到空组，同首代次，关系类型/源端/顺序完整"| N16{"N16【指令-判断】当前节点类型是场景还是存在"}
    N15 -->|"合法结果但代次不等"| D01
    N15 -->|"入口拒绝"| R01
    N15 -->|"许可拒绝"| R03
    N15 -->|"资源失败"| R04
    N15 -->|"版本漂移"| R02
    N15 -->|"内部不一致或材料不符"| R05
    N15 -->|"未找到但非空、未找到代次0或其它非法状态载荷代次组合"| R05
    N16 -->|"场景"| N20["N20【指令-筛选】从当前资格值组筛选当前世界场景资格合同/1候选"]
    N16 -->|"存在"| N30["N30【指令-筛选】从当前资格值组筛选当前世界存在资格合同/1候选"]
    N16 -->|"其它类型"| R05
    N20 -->|"候选恰一、来源=世界结构服务、材料是I64 1..3"| N21{"N21【指令-判断】当前场景是否为请求世界根"}
    N20 -->|"否"| R05
    N21 -->|"是"| N22{"N22【指令-判断】发现来源=根、入父子/入归属均空、资格=现实世界根"}
    N21 -->|"否"| N23{"N23【指令-判断】发现来源=普通父子、预期入边存在且入父子恰一同义、入归属空、资格=初始场景或普通场景"}
    N22 -->|"否"| R05
    N23 -->|"否"| R05
    N22 -->|"是"| N24["N24【指令-登记】登记场景资格与场景事实；初始场景资格则加入候选组"]
    N23 -->|"是；初始场景的父为世界根"| N24
    N23 -->|"初始场景的父不是世界根"| R05
    N24 -->|"当前场景出边"| N25["N25【指令-循环登记】逐项校验出父子为场景目标、出归属为存在目标；关系稳定主键再见时必须逐字段同义"]
    N25 -->|"关系冲突"| R05
    N25 -->|"登记完成"| N26["N26【指令-循环入队】出父子目标若已访问则环，若已排队则多父；否则以预期入边加入小根堆和已排队集合"]
    N26 -->|"环或多父"| R05
    N26 -->|"完成"| N27["N27【指令-循环入队】出归属目标若已访问或已排队则多位置；否则以预期入边加入小根堆和已排队集合"]
    N27 -->|"多位置"| R05
    N27 -->|"完成"| N07
    N30 -->|"候选恰一、来源=存在服务、材料=I64(1)"| N31{"N31【指令-判断】发现来源=归属，预期入边存在，入归属恰一同义，入父子/出父子/出归属均空"}
    N30 -->|"否"| R05
    N31 -->|"否"| R05
    N31 -->|"是"| N32["N32【指令-登记】按关系稳定主键登记预期归属边并登记存在事实/资格；同键必须逐字段同义"]
    N32 -->|"同键冲突"| R05
    N32 -->|"登记完成"| N07
    N07 -->|"是"| N40["N40【函数调用】节点直接代次读取结果 节点直接结构查询服务::读取当前事实代次() const：无实参；返回->末代次结果"]
    N40 -->|"成功、非零且等于首代次"| N41{"N41【指令-判断】初始场景候选恰一且为根直接子场景；根恰一、场景单父、存在单位置、关系正反闭合；堆/已排队集合均空"}
    N40 -->|"成功但代次不等"| D01
    N40 -->|"未找到且代次0或版本漂移且代次0"| R02
    N40 -->|"入口拒绝且代次0"| R01
    N40 -->|"许可拒绝且代次0"| R03
    N40 -->|"资源失败且代次0"| R04
    N40 -->|"内部不一致、成功零代次或任一非成功携带非零代次"| R05
    N41 -->|"否"| R05
    N41 -->|"是"| N42["N42【指令-排序】按4200稳定键分别排序场景组、存在组和缺项材料"]
    N42 -->|"完整结构"| R06["R06【指令-返回】已读取、事实截止代次=首代次、完整稳定排序结构"]
    D01 -->|"是"| D02["D02【指令-赋值】丢弃本轮全部局部材料；轮次=2；重新清空表/堆/集合/候选组"]
    D01 -->|"否"| R02
    D02 -->|"新一轮"| N03
    T01 -. "受保护体抛出std::bad_alloc" .-> E01["E01【指令-返回】资源失败、代次0、无部分结构"]
    T01 -. "受保护体抛出其它异常" .-> E02["E02【指令-返回】内部不一致、代次0、稳定排序缺项、无部分结构"]
```

## 节点合同

| 节点 | 类型 | 参数 / 输入绑定 | 结果 / 输出绑定 | 事实读写 | 后继条件 | 验证 |
| --- | --- | --- | --- | --- | --- | --- |
| N01/T01/N02 | 本函数内判断/异常边界/赋值 | 请求及其版本/根见证 | 轮次与空局部材料 | 零写入 | 入口成立到N03；受保护体异常到E01/E02 | 非当前版本、无效根、非场景根、两类异常 |
| N03/N40 | 函数调用 | 无实参→无形参 | 首/末代次结果 | 只读当前已发布代次 | 状态分流或漂移 | 成功必须非零；未找到必须零；其它非成功必须零；非法组合到R05 |
| N04 | 函数调用 | `世界场景资格类型合同->合同身份`；`1->合同版本` | 场景合同结果 | 只读场景合同 | 字段全等到N05 | 成功携带非零当前代次和完整合同；未找到可携带当前非零代次但因合同必需而到R05；其它非成功必须代次0；命名空间、表示、值域1..3、所有者、生命周期/兼容组 |
| N05 | 函数调用 | `世界存在资格类型合同->合同身份`；`1->合同版本` | 存在合同结果 | 只读存在合同 | 字段全等到N06 | 成功携带非零当前代次和完整合同；未找到可携带当前非零代次但因合同必需而到R05；其它非成功必须代次0；值域1..2、所有者=存在服务 |
| N06—N09 | 本函数内队列/集合指令 | 根见证或小根堆顶 | 当前访问项和已访问登记 | 只改局部容器 | 重复展开拒绝 | 稳定主键小根堆、已访问/已排队互斥 |
| N10 | 函数调用 | `当前主键->主键` | 当前节点结果 | 只读节点 | 见证同义到N11 | 成功携带同轮非零代次和完整节点；未找到可携带当前非零代次并映射漂移；其它非成功必须代次0；类型/主键/版本逐字段同义 |
| N11 | 函数调用 | `当前主键->所属身份` | 当前资格值组 | 只读类型化值 | 必需非空到N12 | 成功携带同轮非零代次；未找到可携带当前非零代次但不是合法空资格；其它非成功必须代次0 |
| N12—N15 | 四次独立函数调用 | `当前主键->源/目标`；`普通父子/归属->类型` | 入/出父子及入/出归属结果 | 只读关系 | 成功非空或未找到空组到N16 | 成功非空或未找到空组都必须携带当前非零代次；未找到非空/代次0及其它非法组合到R05；每个位置再独立裁决空关系是否合法 |
| N16/N20—N27 | 本函数内判断/筛选/登记/入队 | 场景节点、资格和四组关系 | 场景事实、关系表和新待访问项 | 只改局部材料 | 单父/无环/端点成立回N07 | 根空入边、非根恰一父、初始场景直属根、出边端点类型 |
| N30—N32 | 本函数内筛选/判断/登记 | 存在节点、资格和四组关系 | 存在事实和归属关系登记 | 只改局部材料 | 单归属成立回N07 | 资格恰一/正确来源，入归属恰一，其它三关系组为空 |
| N41—N42 | 本函数内判断/排序 | 全部局部材料 | 完整稳定排序结构 | 零权威写入 | 全不变量成立到R06 | 唯一初始场景、正反关系闭合、堆/排队集合空 |
| D01—D02 | 本函数内判断/赋值 | 仅成功/合法空结果间的代次不等 | 第二轮空局部材料 | 丢弃局部副本 | 首轮才重试 | 未找到必需事实/许可/资源/内部错误不重试 |
| R01—R06/E01—E02 | 本函数内返回 | 具名分支材料 | 合同允许的值式结果 | 零补写 | 函数结束 | 只有R06携带非零代次和完整结构；异常不越界 |

## 关键边界

```text
合法空关系必须是 L1 返回“未找到+空组+同一非零代次”，并且当前节点结构位置允许该空组；不把必需节点、资格或非根入边未找到当作合法空。
同一关系从源向和目标向第二次出现是正反闭合，不是重复错误；只有同稳定主键但字段不同才是内部不一致。
只有合法成功/空组读取之间的代次不等进入恰一次完整重试；许可拒绝、资源失败、必需未找到和内部不一致直接返回。
std::bad_alloc在本L2边界映射资源失败；其它异常映射内部不一致；不使用SQL、句柄、显示名或容器迭代顺序补判。
```
