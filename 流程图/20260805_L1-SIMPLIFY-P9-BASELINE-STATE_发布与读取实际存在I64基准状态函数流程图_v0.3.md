# 发布与读取实际存在 I64 基准状态函数流程图 v0.3

## 发布函数

```cpp
实际存在I64基准状态结果 L1状态服务::发布实际存在I64首个基准状态(
    const 实际存在I64首个基准状态发布请求& 请求);
```

```mermaid
flowchart TD
    A[接收请求并取得状态服务互斥量] --> B{入口 登记 幂等身份\n代次和时间有效?}
    B -- 否 --> X1[入口拒绝 空载荷]
    B -- 是 --> C{本进程幂等意图账}
    C -- 异义 --> X2[幂等冲突 空载荷]
    C -- 同义 --> R[取原完整状态读取请求]
    C -- 首次 --> D[由P8和P7取得采样时\n实例槽 主体 场景 当前值]
    D --> E{P8组合含真实P2定义关系\n且全部同一截止代次?}
    E -- 否 --> X3[映射结构化非成功]
    E -- 是 --> F[仅用组合证明发布资格\n不把P2定义写入状态]
    F --> G[0x0D一次提交\n二节点 五值 五关系 五属性槽]
    G --> H{L1成功或精确重复?}
    H -- 否 --> X4[映射结构化非成功]
    H -- 是 --> I[校验本地键1至12\n形成状态自身读取请求]
    I --> R
    R --> J[调用同一不可变状态读取入口]
    J --> K{读取成功?}
    K -- 否 --> X5[结构化非成功\n不补写 不回滚]
    K -- 是 --> Z[返回完整首态\n不输出P2特征定义]
```

## 读取函数

```cpp
实际存在I64基准状态结果 L1状态服务::读取实际存在I64基准状态(
    const 实际存在I64基准状态读取请求& 请求) const;
```

```mermaid
flowchart TD
    A[接收状态身份及状态自身见证编码] --> B{合同 规则和编码有效?}
    B -- 否 --> X1[入口拒绝 空载荷]
    B -- 是 --> C[读取起始L1完整快照G0]
    C --> D[状态自有事实逐项先读当前\n已退出则按同编码读历史]
    D --> E{事实存在且种类正确?}
    E -- 否 --> X2[未找到或内部不一致]
    E -- 是 --> F[由关系19冻结角色1至5\n角色3只解释为P8实例槽]
    F --> G{角色 属性槽 来源和材料完整?}
    G -- 否 --> X3[内部不一致 空载荷]
    G -- 是 --> H[从四项引用取得被采样P8值编码]
    H --> I[被采样值先读当前\n已退出则读历史]
    I --> J{I64 所属实例槽 属性类型\n来源 材料 生命周期互证?}
    J -- 否 --> X3
    J -- 是 --> K[原样返回值属性类型\n禁止解释为P2特征定义]
    K --> L[读取结束L1完整快照G1]
    L --> M{G0等于G1且当前读代次一致?}
    M -- 否且未重试 --> C
    M -- 否且已重试 --> X4[版本漂移 空载荷]
    M -- 是 --> Z[返回不可变状态快照\n不调用P7 P8 P2或4180]
```

后继边界：3E以P9角色3实例槽与P10/P8新当前实例槽相等证明同一具体特征，再使用P10/P8合法返回的P2定义路由4180；不得使用P8值属性类型代替定义。
