# 世界树快照读取服务::读取场景完整事实 函数流程图 v0.3

日期：2026-08-03

设计身份：WORLD-TREE-P1C-Q / F-P1CQ-03

## 1. 函数合同

```text
根函数：世界树快照读取服务::读取场景完整事实
归属模块 / 文件：海中鱼巣.领域.服务.世界树快照 / 海中鱼巣/领域/服务.世界树快照.ixx
层级：L2 世界组合读取服务
现有或待扩展：P1C-QF 最终骨架原位替换
完整签名：世界树场景读取结果 读取场景完整事实(const 世界树对象读取请求& 请求) const noexcept
参数：请求；const 借用；调用方拥有；函数不保存
返回：调用方按值拥有的世界树场景读取结果
直接复杂被调函数：读取完整世界事实快照；恰调用一次
```

## 2. 流程图

```mermaid
flowchart TD
    N01["N01【指令-判断】请求头合法、世界根和目标均为完整场景见证"] --> N02{"N02【指令-判断】入口是否合法"}
    N02 -- "否" --> R01["R01【指令-返回】入口拒绝；默认对象请求；空快照与缺项"]
    N02 -- "是" --> N03["N03【函数调用】读取完整世界事实快照({请求.头}) -> 整树结果"]
    N03 --> N04{"N04【指令-判断】整树结果是否已读取且快照完整"}
    N04 -- "非成功" --> R02["R02【指令-返回】映射整树状态；回显已验证对象请求；空快照；内部不一致可转交缺项"]
    N04 -- "已读取但快照空/不完整/代次0" --> D01["D01【函数调用】安全记录快照读取逻辑异常_(说明)"]
    D01 --> R03["R03【指令-返回】内部不一致；回显请求；空快照"]
    N04 -- "完整" --> N05["N05【指令-筛选】按目标完整身份查找场景；同时检查同稳定主键异版本"]
    N05 --> N06{"N06【指令-判断】命中分类"}
    N06 -- "零命中且无同键异版本" --> R04["R04【指令-返回】未找到；回显请求；空快照与缺项"]
    N06 -- "同键异版本" --> R05["R05【指令-返回】版本漂移；回显请求；空快照与缺项"]
    N06 -- "多命中或同身份异内容" --> D02["D02【函数调用】安全记录快照读取逻辑异常_(说明)"]
    D02 --> R06["R06【指令-返回】内部不一致；空快照；稳定场景缺项"]
    N06 -- "唯一精确命中" --> N07["N07【指令-筛选】沿目标场景事实的直接子关系和直接成员关系解析唯一当前对象"]
    N07 --> N08{"N08【指令-判断】直接子场景和成员是否完整唯一"}
    N08 -- "否" --> D03["D03【函数调用】安全记录快照读取逻辑异常_(说明)"]
    D03 --> R07["R07【指令-返回】内部不一致；空快照；父关系或成员关系缺项"]
    N08 -- "是" --> N09["N09【指令-筛选】特征宿主属于直接成员；状态场景=目标且主体属于直接成员"]
    N09 --> N10["N10【指令-筛选】动态场景=目标，且主体/被改变目标/来源存在至少一项属于直接成员"]
    N10 --> N11["N11【指令-复核】筛选事实身份唯一、端点完整且不越过直接范围"]
    N11 --> N12{"N12【指令-判断】是否存在矛盾"}
    N12 -- "是" --> D04["D04【函数调用】安全记录快照读取逻辑异常_(说明)"]
    D04 --> R08["R08【指令-返回】内部不一致；空快照；稳定缺项组"]
    N12 -- "否" --> N13["N13【指令-排序】按规则版本1排序直接子场景、成员、特征、状态、动态"]
    N13 --> N14["N14【指令-构造】完整场景快照；沿用整树合同/规则/根/代次"]
    N14 --> R09["R09【指令-返回】已读取；回显请求；完整快照；缺项为空"]
    E01["E01【指令-捕获】bad_alloc或length_error"] --> D05["D05【函数调用】安全记录快照读取逻辑异常_(说明)"]
    D05 --> R10["R10【指令-返回】资源失败；按入口是否已验证选择请求回执；空快照"]
    E02["E02【指令-捕获】其它异常"] --> D06["D06【函数调用】安全记录快照读取逻辑异常_(说明)"]
    D06 --> R11["R11【指令-返回】内部不一致；按入口是否已验证选择请求回执；空快照"]
```

## 3. 节点合同

| 节点 | 类型 | 输入 | 输出 | 事实读写 | 验证 |
| --- | --- | --- | --- | --- | --- |
| N01—N02 | 本函数内指令 | 对象请求 | 准入分类 | 只读参数 | 非法入口整树调用0 |
| N03 | 函数调用 | `世界树读取请求{请求.头}` | 整树结果 | 只读内存事实 | 每次对象调用恰一次 |
| N05—N08 | 本函数内指令 | 完整整树快照、目标 | 唯一目标、直接子场景和成员 | 纯值筛选 | 只接受直接关系，不递归子树 |
| N09—N13 | 本函数内指令 | 直接成员集、五类事实 | 场景相关事实 | 纯值筛选和排序 | provider二次调用0 |
| N14 | 本函数内指令 | 筛选材料 | 场景完整事实快照 | 局部构造 | 事实截止代次原样沿用 |
| D01—D06 | 函数调用 | 本层首次异常说明 | 无机器事实 | 诊断旁路 | 不重复整树/provider诊断 |
| R01—R11 | 本函数内返回 | 分支材料 | 世界树场景读取结果 | 写入0 | 非成功无部分快照 |

## 4. 关键边界

```text
直接 provider 调用：0
整树根调用：恰1
场景快照范围：目标场景本身 + 直接子场景 + 直接存在成员；不递归后代
特征：只取直接成员宿主
状态：场景等于目标且主体是直接成员
动态：场景等于目标，且主体/被改变目标/来源存在至少一项是直接成员
合法零子场景、零成员、零特征、零状态、零动态：成功空组
SQL、材料、缓存、旧栈：0调用
```

上游业务图：`流程图/20260803_WORLD-TREE-P1C-Q_同代次完整快照真实读取业务流程图_v0.1.md`。

被调整树图：`流程图/20260803_世界树快照读取服务读取完整世界事实快照_函数流程图_v0.3.md`。
