# NODE-TYPED-MIGRATION NT-P4 统一快照恢复退役施工流程图

更新时间：2026-07-22

## 施工元数据

```text
图类型：施工流程图
绑定正式规范：0050、4010、4020、4040、4070、4110、4210、4220、5210、5230、5300、7130、7140
绑定计划：计划/20260722_NODE-TYPED-MIGRATION_NT-P4_恢复退役与全仓验收子计划_v0.2.md
绑定详细设计：规范/详细设计/NODE-TYPED-MIGRATION_NT-P4_统一快照恢复退役验收详细设计.md
允许文件：由 P4 叶子计划按冻结、解析装载、旧材料、最终切换四类精确登记；共享装配只归最终切换叶子
禁止文件：P1—P3 未固定接口、未登记业务语义、外设/显示/SQL/日志；禁止在旧默认域继续新增生产事实
预期结构变化：新域统一冻结与恢复接通，默认装配一次切换，旧主信息及旧默认域物理退役
执行前复核：FREEZE / RESTORE / LEGACY 候选开发读取预冻结合同；CUTOVER 最终执行读取 P1—P3 和 P4 固定候选、旧默认/新候选零互通、文件所有权和模块图
验证方式：Debug/Release、跨进程导出恢复再导出、完整自我读回、故障矩阵、静态零命中、独立覆盖审计
不得宣称：本施工图自身不证明代码已实施、恢复已接通或旧结构已退役
```

## 流程图

```mermaid
flowchart TD
    A["前置：P1—P3 固定提交只形成具名节点直接隔离新域"] --> B{"切换前隔离审计是否通过"}
    B -->|"否"| STOP0["停止：旧默认与新候选存在句柄、关系、索引、投影、账本、恢复候选或装配互通"]
    B -->|"是"| C["新域冻结协调器取得覆盖命名域高水位、节点、关系 0—23、全部领域记录和 4170 的单一冻结权"]
    C --> D["同一快照代次复制节点直接身份、关系 0—23、任务选择记录、概念签名、具名领域历史和不可变业务账"]
    D --> E{"冻结材料是否全量自洽"}
    E -->|"否"| IR0["内部逻辑错误：放弃本次冻结，不生成当前格式快照，追根因"]
    E -->|"是"| F["形成显式段清单、格式版本、来源域、快照代次、数量/长度上限和完整性校验"]
    F --> G["规范化序列化到临时文件；完成后原子替换目标快照材料"]

    H["启动请求"] --> I{"全新模式与恢复模式是否精确互斥"}
    I -->|"否"| LR0["逻辑内返回：参数拒绝，宿主保持空，不尝试其它模式"]
    I -->|"全新"| J["按 P3 固定新生产候选调用图构造完整节点直接新域候选"]
    I -->|"恢复"| K["读取快照字节并在分配前检查文件大小、头、段数、长度、数量和校验"]
    K --> L{"是否为当前格式"}
    L -->|"旧格式且有具名离线映射"| M["离线迁移器只读转换为独立当前格式输出；不触碰运行期宿主"]
    L -->|"旧格式且无无歧义映射"| LR1["逻辑内返回：明确拒绝并保留只读审计"]
    L -->|"当前格式"| N["严格解析为不可变段材料；拒绝未知、重复、缺失、截断、尾随和越界"]
    M --> N
    N --> O["构造全新隔离恢复候选域；禁止读取当前运行期结构补项"]
    O --> P["按固定顺序装载节点直接身份、命名域 ABI 和各域高水位"]
    P --> Q["装载关系 0—23 全部有效、失效和历史并复核端点/角色/基数"]
    Q --> R["装载任务选择记录、概念签名、每个具名领域记录、历史事实和 4170；未知或缺段拒绝"]
    R --> S["全量互证稳定身份、关系 16 / 23、概念签名 / 关系 9—12 和全部领域不变量"]
    S --> T["只从候选权威结构重建索引、自我投影、概念登记、活动图、抽象树和召回投影"]
    T --> U["通过 P2 / P3 正式入口读回自我结构、任务选择、方法生命周期、概念签名和概念投影"]
    U --> V["对隔离候选再次统一冻结，并与输入规范化权威材料值式相等"]
    V --> W{"候选是否完整且全部互证通过"}
    W -->|"否"| FAIL["销毁本批次候选；宿主和当前上下文不变；恢复失败不得回退全新启动"]
    W -->|"是"| X["封闭为已验证运行期上下文候选"]
    J --> J1["全量互证全新候选的稳定身份、关系 0—23、领域记录、历史事实和业务不变量"]
    J1 --> J2["只从全新候选权威结构确定重建索引、自我投影和概念投影"]
    J2 --> J3["形成规范化初始冻结并复核候选自洽"]
    J3 --> J4{"全新候选是否完整且验证通过"}
    J4 -->|"否"| FAILNEW["销毁全新候选；宿主保持空；不得绕过验证发布"]
    J4 -->|"是"| X

    X --> Y{"运行期上下文宿主是否为空"}
    Y -->|"否"| LR2["逻辑内返回：已有当前上下文；拒绝二次发布，不清空、不替换"]
    Y -->|"是"| Z["尝试首次发布已验证上下文；全新与恢复共用同一空宿主原子入口"]
    Z --> AA["发布后尚不启动消费者，先经正式服务按稳定身份完成全量关键读回"]
    AA --> AB{"发布后读回是否一致"}
    AB -->|"否"| IR1["内部逻辑错误：停止启动并退出进程；不回滚半旧半新、不重开候选撤销"]
    AB -->|"是"| AC["允许启动消费者并持有同身份租约"]

    G --> AD["跨进程验证：进程甲冻结退出，进程乙恢复发布并再冻结"]
    AC --> AD
    AD --> AE{"全量验证、旧快照策略和独立审计是否通过"}
    AE -->|"否"| STOP1["保持旧默认装配；新域继续隔离，不提前删除旧域"]
    AE -->|"是"| AF["准备唯一最终切换提交：默认装配改指新域 + 旧入口/影子权威读取不可达 + 旧文件/字段/工程项删除"]
    AF --> AG{"切换提交的暂存白名单、模块图和静态零命中是否完整"}
    AG -->|"否"| STOP2["停止提交；不得形成先切换后删除或先删除后补接线的中间主线"]
    AG -->|"是"| AH["一次提交并经独立集成发布；切换后只存在节点直接新域生产路径"]
```

## 关键边界

```text
1. P1—P3 只构造具名“节点直接”隔离新域；P4 最终切换前，旧默认域和新候选域在身份、关系、索引、读取、账本、恢复候选与装配上精确零互通。
2. 一次统一冻结必须覆盖命名域 ABI / 高水位、节点、关系 0—23、任务选择、概念签名、全部具名领域记录、历史事实、失效关系和 4170；不得按仓库不同时间分别导出后拼接。
3. 索引、成员 / 槽位 / 状态 / 动态列表、概念登记数组、活动快照、抽象树、召回组、统计缓存和组合投影不作为必需权威段；恢复后只从权威候选确定重建，无事件账统计恢复为空。
4. 序列化只是快照材料编码，不取得机器事实身份；严格解析在任何大规模分配前检查长度、数量、溢出、重复、缺段、校验和和尾随字节。
5. 旧快照只能离线无歧义迁移后作为全新当前格式候选重新准入，或明确拒绝；生产恢复入口不猜测旧主信息槽语义。
6. 全新与恢复只在候选形成方式上不同；全新候选须通过全量互证、投影确定重建和规范化初始冻结，恢复候选还须再冻结与输入相等，二者验证后才进入同一个“空宿主首次发布已验证上下文”入口；不存在恢复替换、清空或在线热切换入口。
7. 恢复失败保持宿主和当前上下文不变，并返回失败；不得自动构造全新候选继续启动。
8. 发布后读回发生在启动消费者之前；不一致属于内部逻辑错误，只能停止进程，不把已发布结构回滚成半旧半新。
9. 完整跨重启恢复必须读回唯一自我、内部世界、关系 19—23、关系 16 选择角色、概念签名、关系 9—12及全部类型化记录和历史。
10. 默认装配切换、旧生产入口 / 影子权威读取不可达、旧仓库 / 句柄 / 字段 / 文件和工程登记删除属于同一最终切换提交；不得让 main 出现可构建但双域生产并列的中间状态。
```
