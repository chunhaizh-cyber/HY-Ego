# 历史初始化真模块迁移与唯一上下文装配流程图

更新时间：2026-07-12

替代状态：本图已由 `JY-337` 和 `流程图/20260714_系统角色初始化与历史兼容初始化调用迁移代码逻辑流程图_v0.1.md` 替代，只保留为历史设计证据，不再作为 `#250 / DQ-142` 执行依据。

## 依据

```text
规范/代码文件建立归属与模块命名规范.md
实施记录/20260711_ENTRY-MOD-S0_入口与自检承载当前代码事实复核_Codex断点清单.md
流程图/20260712_运行期主装配结构事务接域与恢复链单一所有权流程图_v0.1.md
```

## 说明

本图只迁移四个历史兼容初始化 `.ixx` 的编译身份和调用边界，不改变初始化业务顺序、根身份或领域语义。

## 流程图

```mermaid
flowchart TD
    A["开始：#249 已形成唯一正式运行期上下文和主装配合同"] --> B["执行前扫描四个历史初始化文件、include 链、公开类型和调用点"]
    B --> C{"实际接口是否可按概念图 -> 世界树 -> 语素 -> 需求的有向依赖迁移"}
    C -->|"否"| G0["设计漂移返回：不改代码，修订初始化计划"]
    C -->|"是"| D["建立 初始化.概念图 / 初始化.世界树 / 初始化.语素 / 初始化.需求 真模块"]
    D --> E["使用唯一上下文提供的仓库与领域服务；禁止模块创建第二套仓库"]
    E --> F{"前置请求无效还是初始化后内部读回异常"}
    F -->|"前置无效"| L1["逻辑内返回：不发布半初始化上下文"]
    F -->|"内部异常"| R1["追根因解决：停止后继初始化"]
    F -->|"成功"| H["入口只 import 初始化模块并执行一次顶层初始化总成"]
    H --> I["独立自检模块复核顺序、幂等、失败不变式和旧 include 清零"]
    I --> Z["完成：四个历史兼容初始化文件不再按 ClInclude/include 使用"]
```

## 关键边界

```text
1. 只改变工程承载和调用方向，不改初始化领域事实。
2. 新行为文件全部是真模块；生产与自检分离。
3. 入口不展开初始化正文或验收场景。
4. 不删除既有文件前先完成调用迁移、工程登记替换和回归；接口漂移时退回设计。
```
