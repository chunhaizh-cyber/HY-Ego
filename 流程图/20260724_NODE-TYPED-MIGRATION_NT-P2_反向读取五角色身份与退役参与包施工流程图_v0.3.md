# NODE-TYPED-MIGRATION NT-P2 反向读取、五角色身份与退役参与包施工流程图

更新时间：2026-07-25

## 施工图元数据

```text
图类型：施工流程图
绑定正式规范：4010、4040、4210、4220、7130
绑定计划：NT-P2 v0.4、#341 v0.6、#342 v0.7
绑定详细设计：P2B v0.5、P2C v0.6
允许文件：分别只取 #341 / #342 叶子白名单；本图不扩大任一白名单
禁止文件：核心、相邻领域、工程、入口、运行器、默认装配及非当前计划文件
预期结构变化：P2B 形成状态 / 动态完整事实和记录参与包；P2C 形成五身份 / 五关系、恢复验证和基于历史审计的无私仓单节点退役包
执行前复核：NT-C1Q/v0.1、关系全量审计、累计父链、目标叶子 blob、提供者真实接口与文件所有权
验证方式：叶子合同静态检查；完整构建 / 运行 / 接线后置
不得宣称：接口已联调、已接线、已恢复、已切换或能力完成
```

## 流程图

```mermaid
flowchart TD
    A["取得状态 / 动态、自我初始化 / 读取 / 恢复验证或退役请求"] --> B{"目标叶子与累计真实接口吻合？"}
    B -->|"否"| R["DRIFT：停止受影响叶，退回精确合同所有者"]
    B -->|"是"| D{"P2B 还是 P2C？"}

    D -->|"P2B"| E["按 NT-C1Q 读取当前关系并形成关系 19 / 20 证据和类型化记录"]
    E --> E2{"普通读取、同代次冻结或记录退役？"}
    E2 -->|"读取 / 冻结"| P["返回完整值式材料"]
    E2 -->|"记录退役"| G["形成带真实记录参与者的 move-only 单次退役包"]
    G --> X["核心执行器 + 真实领域参与者"]

    D -->|"P2C"| F{"初始化、普通读取、恢复验证或历史自我退役？"}
    F -->|"初始化"| F1["一次形成五项互异身份和五条固定关系；角色 2 成员另成零到多集合"]
    F1 --> X
    F -->|"普通读取"| F2["连续同序采集两遍五身份 / 五关系、成员、槽位、状态、动态"]
    F2 --> F3{"逐字段相等？"}
    F3 -->|"否"| V["读取版本漂移；材料为空"]
    F3 -->|"是"| P
    F -->|"恢复验证"| F4["值式验证关系 0 / 1 / 21、成员 21 / 2、P2A 22、P2B 19 / 20"]
    F4 --> P
    F -->|"历史自我退役"| H0["全量审计关系 21；当前有效视图不参与历史发现"]
    H0 --> H1{"无有效角色 1，且全部角色 1 谱系最高头已失效？"}
    H1 -->|"否"| H2["合法未找到、入口拒绝或内部不一致；零变化"]
    H1 -->|"已删除且审计精确"| H3["已退役幂等读回；不形成包、不推进版本"]
    H1 -->|"是"| H4["冻结全部历史谱系和内部世界静止态，形成 move-only 单次包"]
    H4 --> H5["独占许可内再次全量审计；P2C 关系写集恒为空"]
    H5 --> H6["#346 回调只登记其它当前关系、索引和单一历史自我节点删除"]
    H6 --> X

    X --> J{"准备、确认与发布闭合？"}
    J -->|"提交前失败"| K["逆序撤销；零发布"]
    J -->|"已提交"| L["最后发布事实成立"]
    L --> M{"权威读回闭合？"}
    M -->|"否"| N["内部逻辑错误；停止依赖路径，不否认已发布事实"]
    M -->|"是"| P
```

## 关键边界

```text
匿名关系组退出；五项身份不复用；五条固定关系不含角色 2 成员。
P2B 退役包包含真实记录参与者；P2C 没有私仓，历史关系只读且 P2C 写集为空。
历史自我由全部已失效角色 1 谱系证明，多代内部世界和角色 2 只作完整审计集合。
P4 保存完整记录、历史和失效关系，不保存列表投影。
```
