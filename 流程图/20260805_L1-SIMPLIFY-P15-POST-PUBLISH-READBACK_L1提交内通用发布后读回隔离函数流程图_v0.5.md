# L1-SIMPLIFY-P15 L1提交内通用发布后读回隔离函数流程图 v0.5

对应详细设计：`规范/详细设计/20260805_L1-SIMPLIFY-P15-POST-PUBLISH-READBACK_L1提交内通用发布后读回隔离详细设计_v0.5.md`

正式基线：`e254f5a67d98340d8f32f56343edd05e12a67600`

v0.5 完整替代 v0.4 的调用点清单、迁移矩阵和激活门禁；v0.4 双摘要机器合同保持有效。

## 调用点边界

```text
实际代码调用点：16 个文件、46 处 `.提交写集(...)`
v0.5 新增：领域/服务.特征比较.ixx 在P11结果后的第3处调用
流程图/Markdown 命中：仅证据文本，不是代码调用
代码白名单：20 个代码文件；另有2份专属记录，共22路径
```

## 函数合同

```text
根函数：L1事实基座服务::提交
归属模块/文件：海中鱼巣.核心.服务.L1事实基座 / 海中鱼巣/核心/服务.L1事实基座.ixx
现有或待新增：现有函数，v0.4 -> v2合同迁移
完整签名：L1写入结果 L1事实基座服务::提交(const L1写集请求& 请求)
参数：const 引用借用，仅调用期间有效，不保留
返回：首次成功、精确重复、幂等冲突、隔离拒绝、资源失败或内部不一致
```

## 流程图

```mermaid
flowchart TD
    N01["校验L1 v2合同、摘要、幂等键和发布后计划"]
    N02["入口拒绝，零候选零代次变化"]
    N03["取得唯一独占锁"]
    N04{"幂等键是否已有记录"}
    N05{"请求意图摘要是否相同"}
    N06["返回首次完整结果；不访问provider、不重算、不追加审计"]
    N07["幂等冲突，零业务变化"]
    N08{"当前是否隔离"}
    N09["许可拒绝/已隔离，返回允许的定位见证"]
    N10{"执行证据、写集和读回计划完整"}
    N11["入口拒绝或资源失败，零发布"]
    N12["形成候选、双摘要、审计、幂等账和结果摘要"]
    N13["swap推进业务事实代次一次"]
    N14["同一独占锁内按通用计划权威读回"]
    N15{"发布后读回是否完整一致"}
    N16["返回首次映射、完整读回和结果摘要"]
    N17["安装失败见证/失败审计并设置隔离"]
    N18["内部不一致，保留发布代次与首次幂等记录"]
    N01 -- 非法 --> N02
    N01 -- 合法 --> N03 --> N04
    N04 -- 有记录 --> N05
    N05 -- 相同且未隔离 --> N06
    N05 -- 不同 --> N07
    N05 -- 相同且隔离 --> N09
    N04 -- 无记录 --> N08
    N08 -- 是 --> N09
    N08 -- 否 --> N10
    N10 -- 否 --> N11
    N10 -- 是 --> N12 --> N13 --> N14 --> N15
    N15 -- 是 --> N16
    N15 -- 否/异常 --> N17 --> N18
```

## 16文件/46调用点清单

```text
核心/自检.L1可重建索引.ixx             2
核心/自检.L1事实基座.ixx               13
领域/服务.世界登记.ixx                  1
领域/服务.L1场景结构.ixx                1
领域/服务.L1特征定义.ixx                2
领域/服务.L1实例特征.ixx                3
领域/服务.特征比较.ixx                  3 [v0.5新增1点]
领域/服务.L1实际存在.ixx                1
领域/组合.L1实际存在场景.ixx            2
领域/服务.L1状态.ixx                    2
领域/自检.世界登记.ixx                  1
领域/自检.L1实际存在.ixx                2
领域/自检.L1实例特征.ixx                7
领域/自检.特征比较.ixx                  1
领域/自检.L1状态.ixx                    4
自检/运行期上下文.ixx                   1
```

每处调用均须在 provider 前完成公开请求意图摘要和 `探测幂等`；只有未找到才读取 provider 并形成执行证据，随后进入唯一 L1 v2 提交合同。清单不是额外根函数，流程图文本命中不计数。

## 验证边界

执行前检查 Mermaid、唯一根函数合同、16文件/46调用点和22路径白名单；执行时复核双摘要同义/异义、首次结果、同锁读回、失败见证、隔离、审计/恢复互证。本文不证明代码已实现、编译或运行通过。
