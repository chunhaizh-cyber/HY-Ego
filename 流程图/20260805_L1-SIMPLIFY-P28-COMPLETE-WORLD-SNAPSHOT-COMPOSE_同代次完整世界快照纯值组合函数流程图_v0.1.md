# L1-SIMPLIFY-P28-COMPLETE-WORLD-SNAPSHOT-COMPOSE 同代次完整世界快照纯值组合函数流程图 v0.1

日期：2026-08-05

状态：设计图；代码未实现

对应函数：

```cpp
完整世界快照组合结果 组合同代次完整世界事实快照(
    const 完整世界快照组合请求& 请求,
    const L1完整快照& 快照,
    const 当前世界结构事实& 世界结构,
    const 当前实际存在资格集合事实& 实际存在,
    const 当前实际存在实例特征槽集合事实& 实例槽,
    const 实例槽引用I64特征定义集合事实& 特征定义,
    const 当前实际存在I64实例特征值集合事实& 当前特征值,
    const 当前世界状态动态集合事实& 状态动态,
    const 当前世界事实代次结果& 末次世界事实代次);
```

## 1. 调用边界

```mermaid
flowchart TD
    C["后继世界快照协调者"] --> S0["P19读取结构，取得G0"]
    S0 --> E["P21读取实际存在集合"]
    E --> L1["P17取得本轮唯一L1完整快照"]
    L1 --> F1["P24读取全部实例槽"]
    F1 --> F2["P25读取槽引用定义详情"]
    F2 --> F3["P26读取全部槽当前值"]
    F3 --> SD["P27读取状态动态集合"]
    SD --> G1["P19读取当前事实代次，取得G1"]
    G1 --> P28["P28纯值组合函数"]

    C --> X{"任一上游非成功?"}
    X -- "许可拒绝" --> XR["立即返回许可拒绝\nP28调用0/重试0"]
    X -- "其它非成功" --> XO["结构化短路\nP28调用0"]

    P28 -. "禁止" .-> N1["provider/L1服务/锁"]
    P28 -. "禁止" .-> N2["SQL/缓存/历史/审计"]
    P28 -. "禁止" .-> N3["重取快照/sleep/retry/写事实"]
```

P28 只组合已经成功形成的纯值材料。许可拒绝短路和完整漂移重试属于后继协调者；本图冻结调用责任，但不把协调能力伪装成本叶代码结果。

## 2. 单函数流程

```mermaid
flowchart TD
    A["进入组合同代次完整世界事实快照"] --> B{"请求合同/规则/根有效?"}
    B -- "否" --> R1["入口拒绝\n空快照/空缺项"]
    B -- "是" --> C{"G1结果成功且非零?"}
    C -- "许可拒绝" --> R2["许可拒绝\n空快照/空缺项"]
    C -- "其它非成功" --> R3["固定状态映射\n空快照/空缺项"]
    C -- "是" --> D{"全部上游合同、根、完整性和排序有效?"}
    D -- "否" --> RI["内部不一致\n稳定缺项/空快照"]
    D -- "是" --> G{"G0=P17=P21=P24=P25=P26=P27=G1且非零?"}
    G -- "否" --> RV["版本漂移\n空快照/空缺项"]
    G -- "是" --> W{"P19场景/父子/成员闭合?"}
    W -- "否" --> RI
    W -- "是" --> E{"P21与全部成员一一对应?"}
    E -- "否" --> RI
    E -- "是" --> F{"P24/P25/P26主体、槽、定义和值闭合?"}
    F -- "否" --> RI
    F -- "是" --> H{"P27状态动态范围和端点闭合?"}
    H -- "否" --> RI
    H -- "是" --> O["按稳定编码形成场景/存在/宿主特征/状态/动态组"]
    O --> Q{"输出排序、唯一性、完整性复核通过?"}
    Q -- "否" --> RI
    Q -- "是" --> OK["已读取\n共同非零截止\n完整=true\n缺项空"]

    A -. "bad_alloc" .-> RM["资源失败\n空快照/空缺项"]
    A -. "其它异常" .-> RX["内部不一致\n空快照"]
```

## 3. 稳定映射

| 输出 | 唯一来源 | 组合规则 |
| --- | --- | --- |
| 合同/世界/组合/完整性/排序版本 | P28正式常量 | 不接受上游或调用方覆盖 |
| 世界根 | 已验证请求根 | 与全部上游根逐字段相等 |
| 事实截止代次 | G0/P17/全部领域/G1共同值 | 任一不等返回版本漂移 |
| 场景组 | P19 | 按场景编码；父/子/成员关系按关系编码 |
| 存在组 | P19成员关系 + P21资格项 | 一成员关系对应一存在和一个当前场景 |
| 宿主特征组 | P24槽 + P25定义 + P26当前值 | 按宿主、定义、实例槽编码 |
| 状态组 | P27 | 保持场景、主体、特征、发生时间、状态顺序 |
| 动态组 | P27 | 保持场景、后状态时间、动态顺序 |
| 完整 | 全部检查完成后的常量true | 任一失败零部分快照 |
| 缺项组 | P28跨域检查 | 仅内部不一致非空；稳定排序去重 |

## 4. 合法空世界与失败边界

```mermaid
flowchart LR
    A["正式根场景存在"] --> B["零子场景/零成员"]
    B --> C["P21/P24/P25/P26范围空且完整"]
    C --> D["P27零状态/零动态且完整"]
    D --> E["同一非零截止"]
    E --> F["合法完整空世界成功"]

    Z["根为零或场景组为空"] --> X["未找到或内部不一致\n不得空成功"]
```

本函数不设计存在/场景对象子快照，不设计统一业务门面，不实现生产重试。后继只能在本函数第一轮因纯代次不等返回版本漂移时完整重读一次；不得重用旧轮材料。
