# L1-SIMPLIFY-P15 G6 E01 身份循环与证据互证流程图 v0.22

更新时间：2026-08-06
依据：P15 v0.22 详细设计；正式基线 `11c0ee5e4f9e7154e4ea6bf43458c9e44c66c2ea`。

## E01/X02 证据模式

```mermaid
flowchart LR
 E01["E01 实际存在登记请求\n0x15000301/变体1"] --> L1S["写前 P-L1-S 1/1\nP-E 0/0 合法创建模式"]
 L1S --> W["一次 L1 v2 提交"]
 W --> ER["实际存在登记结果\n产生 P-E 结果身份 1/1"]
 X02["X02 实际存在创建请求\n0x15000901/变体1"] --> XP["写前 P-E+P-S 2/2"]
 XP --> XW["一次 L1 v2 提交"]
 XW --> XR["实际存在结果"]
```

## E01 顺序与恢复拒绝

```mermaid
flowchart TD
 A["E01 公开请求 DTO"] --> B["G6 领域意图摘要"]
 B --> C["探测幂等"]
 C -->|同义| D["首次账/实际存在登记结果，0/0"]
 C -->|未找到| E["读取 P-L1-S，P-E 不读取"]
 E --> F["独立 G0 摘要与领域摘要互证"]
 F --> G["一次 0x15000301 提交"]
 G --> H["首次结果产生 P-E 身份"]
 H --> I["发布后读回与恢复重算"]
 C -->|异义/错误 P-E 写前材料| X["拒绝/隔离，零业务载荷"]
```

E01 的 P-E 是创建结果身份，不是提交前 provider。X02 继续在 provider 前读取 P-E/P-S 并保持 2/2；两种模式不可互换。完整范围仍为 24 项代码/模块/自检加两条专属记录路径，共 26 文件。
