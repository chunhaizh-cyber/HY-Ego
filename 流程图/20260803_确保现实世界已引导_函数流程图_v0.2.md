# 确保现实世界已引导函数流程图 v0.2

更新时间：2026-08-03

## 依据

```text
4200 v0.8；4201 v0.2；业务节点到能力与目标函数映射.md“世界事实维护”；WORLD-TREE-P1业务流程图B01/B02；P1A3F详细设计v0.1未替换段+v0.2；P1A2Q详细设计v0.1；P1A3详细设计v0.2 §4.1/§5
```

## 身份与边界

目标单函数施工图；只冻结 P1A3F 骨架的真实行为原位替换。它提供世界根与初始场景子能力，不创建唯一自我，也不单独闭合完整世界事实维护返回材料。

## 函数合同

```text
根函数：节点直接世界引导服务::确保现实世界已引导
归属模块 / 文件 / 层级：海中鱼巣.领域.服务.节点直接世界引导 / 领域/服务.节点直接世界引导.ixx / L2
现有或待新增：P1A3F 骨架原位替换为真实行为
完整签名：节点直接世界引导结果 节点直接世界引导服务::确保现实世界已引导(const 节点直接世界引导请求& 请求) const
参数：请求 -> const借用，仅本次同步调用；合同版本、规则版本、安装实例身份不完整为非法；不保存引用
返回结果：调用方拥有值；已恢复/已创建/幂等读回才携带非零代次和完整世界根/初始场景/直接关系读回；其它状态不携带伪事实
被调函数流程图：读取世界结构 -> 流程图/20260803_读取世界结构_函数流程图_v0.2.md；读取当前索引 -> 流程图/20260803_节点直接结构查询服务读取当前索引_函数流程图_v0.1.md
```

## 流程图

```mermaid
flowchart TD
    N01{"N01【指令-判断】请求合同、规则版本和安装实例身份完整"}
    N01 -->|否| R01["R01【指令-返回】入口拒绝、代次0、无读回"]
    N01 -->|是| N02["N02【指令-构造】从请求形成安装实例恢复请求"]
    N02 -->|恢复请求| N03["N03【函数调用】节点直接结构恢复结果 节点直接结构恢复服务::恢复安装实例(const 节点直接安装实例恢复请求& 请求) const：恢复请求->请求；返回->恢复结果"]
    N03 -->|恢复结果| N04{"N04【指令-判断】恢复结果状态"}
    N04 -->|已恢复或精确同义| N05{"N05【指令-判断】恢复事实截止代次非零"}
    N04 -->|已初始化确认空域或已恢复治理空域| N16{"N16【指令-判断】事实截止代次恰为1"}
    N04 -->|材料不完整| R02["R02【指令-返回】无可验证历史、代次0、无读回"]
    N04 -->|版本漂移| R03["R03【指令-返回】版本漂移、代次0、无读回"]
    N04 -->|资源失败| R04["R04【指令-返回】资源失败、代次0、无读回"]
    N04 -->|其它状态| R05["R05【指令-返回】内部不一致、代次0、无读回"]
    N05 -->|否| R05
    N05 -->|是| N06["N06【指令-构造】按安装实例和固定合同形成世界根/初始场景索引物理键"]
    N06 -->|世界根索引键| N07["N07【函数调用】节点直接索引读取结果 节点直接结构查询服务::读取当前索引(const 索引物理键& 键) const：世界根索引键->键；返回->根索引结果"]
    N07 -->|根索引结果| N08{"N08【指令-判断】根索引结果状态"}
    N08 -->|许可拒绝| R06["R06【指令-返回】许可拒绝、代次0、无读回"]
    N08 -->|资源失败| R04
    N08 -->|未找到、入口拒绝、版本漂移或内部不一致| R05
    N08 -->|成功但根索引载荷缺失、错形或物理键不符| R05
    N08 -->|成功且根索引完整| N09["N09【函数调用】节点直接索引读取结果 节点直接结构查询服务::读取当前索引(const 索引物理键& 键) const：初始场景索引键->键；返回->初始场景索引结果"]
    N09 -->|初始场景索引结果| N10{"N10【指令-判断】初始场景索引结果状态"}
    N10 -->|许可拒绝| R06
    N10 -->|资源失败| R04
    N10 -->|未找到、入口拒绝、版本漂移或内部不一致| R05
    N10 -->|成功| N11{"N11【指令-判断】两结果同恢复代次、各恰一当前场景节点且物理键匹配"}
    N11 -->|否| R05
    N11 -->|是| N12["N12【指令-构造】用根节点见证形成世界结构读取请求"]
    N12 -->|结构请求| N13["N13【函数调用】世界结构读取结果 节点直接世界结构读取服务::读取世界结构(const 节点直接世界结构读取请求& 请求) const：结构请求->请求；返回->结构结果"]
    N13 -->|结构结果| N14{"N14【指令-判断】已读取、同恢复代次、根无父且初始场景资格/直接关系唯一匹配"}
    N14 -->|未找到或版本漂移| R03
    N14 -->|入口拒绝| R01
    N14 -->|许可拒绝| R06
    N14 -->|资源失败| R04
    N14 -->|内部不一致、未实现、非法状态载荷或结构不符| R05
    N14 -->|成立| R07["R07【指令-返回】已恢复、原代次和三项完整读回"]
    N16 -->|否| R05
    N16 -->|是| N17["N17【函数调用】节点直接场景事务形成结果 形成首次现实世界事务(const 节点直接世界引导请求& 请求,std::uint64_t 预期事实截止代次)：本根函数请求->请求，1->预期事实截止代次；返回->事务形成结果"]
    N17 -->|形成结果| N18{"N18【指令-判断】事务形成状态"}
    N18 -->|资源失败| R04
    N18 -->|其它非成功| R08["R08【指令-返回】映射入口拒绝/版本漂移/内部不一致"]
    N18 -->|已形成| N19["N19【函数调用】节点直接类型化结构提交结果 节点直接类型化结构提交服务::提交(const 节点直接类型化结构事务请求& 请求) const：事务形成结果.事务值->请求；返回->提交结果"]
    N19 -->|提交结果| N20{"N20【指令-判断】已提交/幂等读回、代次=2且通用读回完整"}
    N20 -->|许可拒绝| R06
    N20 -->|资源失败| R04
    N20 -->|其它业务非成功| R09["R09【指令-返回】映射提交非成功"]
    N20 -->|状态成功但读回不符| R10["R10【指令-返回】内部不一致、保留非零发布代次"]
    N20 -->|成立且提交为已提交| R11["R11【指令-构造并返回】只用原许可通用读回形成引导读回；返回已创建、代次2和完整读回"]
    N20 -->|成立且提交为幂等读回| R12["R12【指令-构造并返回】只用原许可通用读回形成引导读回；返回幂等读回、原代次和原读回"]
    N02 -. "std::bad_alloc" .-> R04
    N03 -. "std::bad_alloc" .-> R04
    N06 -. "std::bad_alloc" .-> R04
    N07 -. "std::bad_alloc" .-> R04
    N09 -. "std::bad_alloc" .-> R04
    N12 -. "std::bad_alloc" .-> R04
    N13 -. "std::bad_alloc" .-> R04
    N17 -. "std::bad_alloc" .-> R04
    N19 -. "std::bad_alloc" .-> R04
    N02 -. "其它异常" .-> R05
    N03 -. "其它异常" .-> R05
    N06 -. "其它异常" .-> R05
    N07 -. "其它异常" .-> R05
    N09 -. "其它异常" .-> R05
    N12 -. "其它异常" .-> R05
    N13 -. "其它异常" .-> R05
    N17 -. "其它异常" .-> R05
    N19 -. "其它异常" .-> R05
```

## 节点合同

| 节点 | 类型 | 参数 / 输入绑定 | 结果 / 输出绑定 | 事实读写 | 后继条件 | 验证 |
| --- | --- | --- | --- | --- | --- | --- |
| N01—N04 | 判断、构造、函数调用 | `请求 -> 恢复请求`；`恢复请求 -> 恢复安装实例.请求` | 恢复结果 | 读取持久恢复输入并可能原子发布隔离恢复事实 | 按全部恢复状态分流 | 身份/版本非法、全部恢复状态 |
| N05—N14 | 判断、构造、函数调用 | 安装实例 -> 两个固定索引键；键 -> `读取当前索引.键`；根见证 -> 结构请求 | 两个精确索引结果、世界结构结果 | 同一恢复后内存代次只读 | 许可/资源单独映射；其它矛盾内部不一致；完整到R07 | 未找到、错键、错类型、代次不等、多项不变量、结构不符 |
| N16—N20 | 判断与函数调用 | `请求,1 -> 形成首次现实世界事务`；事务 -> `提交.请求` | 事务形成结果、提交结果 | 治理代次1后单次 L1 最后发布及原独占许可通用读回 | 只有已形成进入提交；成功结果经逐项映射直接到 R11/R12，不发起事务后普通快照 | 固定写项顺序、双摘要、代次2、节点/值/关系/索引通用读回 |
| R01—R12 | 本函数内返回 | 对应分支材料 | 合同允许的具名结果 | 除已发生的恢复/提交外零补写 | 函数结束 | 状态、代次、载荷和日志边界 |

## 关键边界

```text
不得从 SQL、显示名或扫描猜根；恢复成功只给出事实截止，根与初始场景必须经 P1A2Q 两次精确键读取取得。
已恢复治理空域不是最终成功，只允许继续首次引导；本函数不创建唯一自我。
首次提交成功后只使用 P1A2 在原独占许可内形成的通用读回；禁止事务释放后调用普通快照证明原提交。
```
