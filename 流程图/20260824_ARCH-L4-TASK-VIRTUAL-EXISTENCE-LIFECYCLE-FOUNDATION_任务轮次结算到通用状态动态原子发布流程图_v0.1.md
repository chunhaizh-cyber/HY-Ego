# ARCH-L4-TASK-VIRTUAL-EXISTENCE-LIFECYCLE-FOUNDATION 任务轮次结算到通用状态动态原子发布流程图

版本：v0.1
对应详细设计：`规范/详细设计/20260824_ARCH-L4-TASK-VIRTUAL-EXISTENCE-LIFECYCLE-FOUNDATION_任务通用虚拟存在与生命周期状态基础闭环详细设计_v0.1.md`
图类型：单一业务函数流程图；不表示代码现状，不授予施工许可

```mermaid
flowchart TD
    A["L4任务轮次结算状态消费者::提交轮次结算并发布当前状态"]
    B["同一G0读取任务、实例方法、实际结果、目标裁决、轮次结算"]
    C{"目标裁决与轮次结算引用同一任务/轮次且当前?"}
    D["失败返回；零L2发布"]
    E["读取或幂等建立任务通用L2存在"]
    F{"通用存在完整读回且任务引用一致?"}
    G["失败返回；不得再建第二存在"]
    H["读取任务生命周期特征实例与唯一当前值"]
    I{"唯一当前槽、值材料、前态和G0一致?"}
    J["失败返回；零L2发布"]
    K["形成本轮已收束/等待自我后继决议值与动态材料"]
    L["调用L2状态动态原子发布一次"]
    M{"结果为已提交/精确重复?"}
    N["非成功返回；零载荷；不重试"]
    O["以同一G1读回新状态、新状态关系、新动态、新动态关系、旧状态历史退出"]
    P{"五组读回与任务通用存在逐字段一致?"}
    Q["内部不一致回报；不再次发布"]
    R["返回L4值式轮次结算回执；保留旧路径/实例/结果/历史"]

    A --> B --> C
    C -- 否 --> D
    C -- 是 --> E --> F
    F -- 否 --> G
    F -- 是 --> H --> I
    I -- 否 --> J
    I -- 是 --> K --> L --> M
    M -- 否 --> N
    M -- 是 --> O --> P
    P -- 否 --> Q
    P -- 是 --> R
```

图中不含路径验证算法、目标比较、任务完成、重新筹办、需求结算、授权或学习分支；“目标裁决”只作为已形成的上游值式材料读取。任何新增写入口必须先修订详细设计并取得新的计划身份。
