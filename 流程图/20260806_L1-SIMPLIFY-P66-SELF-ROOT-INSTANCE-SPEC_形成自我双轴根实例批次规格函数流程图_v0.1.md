# L1-SIMPLIFY-P66 形成自我双轴根实例批次规格函数流程图 v0.1

创建侧正式基线：`bcb7d32ec0ca2b8d322589b114600a76161855f8`

目标函数：`L1自我根实例规格服务::形成自我双轴根实例批次规格`

```mermaid
flowchart TD
    A["接收批次身份 完整自我结果和双轴根定义结果"] -->|开始| B["纯值校验顶层版本和批次范围"]
    B -->|入口不合法| Z["返回入口拒绝 空规格"]
    B -->|入口合法| C["校验完整自我成功状态 完整事实和实际存在资格"]
    C -->|明确未发布| X["返回完整自我未发布"]
    C -->|坏成功或结构矛盾| Y["返回内部不一致"]
    C -->|完整| D["校验P65成功状态 登记和安全服务两个P2定义"]
    D -->|明确未发布| W["返回根定义未发布"]
    D -->|坏成功 错角色或错值域| Y
    D -->|完整| E["比较发布代次并取P65代次为共同截止"]
    E -->|定义早于自我| V["返回事实代次漂移"]
    E -->|顺序成立| F["从完整自我形成宿主读取请求并复制为来源请求"]
    F -->|见证矛盾| Y
    F -->|完整| G["从P65形成安全和服务两个P2读取请求"]
    G -->|定义重复或字段矛盾| Y
    G -->|完整| H["形成项目0 安全初值1 自我来源"]
    H -->|固定顺序| I["形成项目1 服务初值0 自我来源"]
    I -->|两项完成| J["形成P40批次请求并逐字段自检"]
    J -->|分配失败| U["返回资源失败 空规格"]
    J -->|数量 顺序 截止或字段矛盾| Y
    J -->|恰两项完整| K["返回已形成的不可变批次规格"]
```

固定边界：全流程只组合值式事实，P2/P7/P15/P36/P40/L1调用均为0；不接受自由初始值或外部来源，不发布关系22、实例槽、当前值或根需求。
