# DATA-L2 写入概念名称关系函数流程图

日期：2026-08-17

版本：v0.1

身份：`DATA-L2-CONCEPT-NAME-STRUCTURE-SERVICE-CLOSURE`

对应目标函数：`L2概念名称结构服务::执行概念名称关系写入(...)`（私有单函数；新增、替换、退出三个公开入口的共同实现根）

```mermaid
flowchart TD
    A["接收操作种类与强类型请求"] --> B{"合同、G0、幂等身份、角色和字段有效?"}
    B -- "否" --> X1["入口拒绝；空载荷；零变化"]
    B -- "是" --> C["取得名称服务唯一写锁"]
    C --> D["按 owner 幂等身份读取首次完整材料"]
    D --> E{"已有首次材料?"}
    E -- "同写集" --> F["按首次截止历史重建新旧关系"]
    F --> X2["精确重复；返回首次形状"]
    E -- "异写集" --> X3["幂等冲突；空载荷"]
    E -- "读取失败" --> X4["映射失败；空载荷"]
    E -- "未找到" --> G{"操作种类"}
    G -- "新增" --> H1["形成候选新关系"]
    G -- "替换" --> H2["G0读取并逐字段命中旧关系"]
    G -- "退出" --> H3["G0读取并逐字段命中旧关系"]
    H2 --> I2["形成候选新关系并排除旧身份"]
    H1 --> J
    I2 --> J
    H3 --> K
    J["同G0经概念、语言/词条和L1读取概念、词条、适用语言、来源"] --> L{"端点完整且词条语言一致?"}
    L -- "否" --> X5["精确映射未找到/已退出/引用冲突/内部不一致"]
    L -- "是" --> M["正向读取概念当前完整名称组"]
    M --> N{"无重复四元组且同语言首选唯一?"}
    N -- "否" --> X6["引用冲突；空载荷；零变化"]
    N -- "是" --> K["形成确定 owner-scoped 写集"]
    K --> O{"写集形状"}
    O -- "新增" --> O1["建立记录节点+概念/词条/语言/来源四关系"]
    O -- "替换" --> O2["退出旧五事实+建立新五事实"]
    O -- "退出" --> O3["退出旧五事实"]
    O1 --> P["以G0提交唯一名称 owner"]
    O2 --> P
    O3 --> P
    P --> Q{"L1提交状态"}
    Q -- "守卫漂移" --> X7["事实代次漂移；不重选截止"]
    Q -- "资源/许可/引用/内部失败" --> X8["原状态；保留原请求供调用方重放"]
    Q -- "成功或精确重复" --> R["按首次提交截止历史读回新旧五事实"]
    R --> S{"owner、类型、端点、角色、生命周期和载荷形状完整?"}
    S -- "否" --> X9["内部不一致；不得伪造成功"]
    S -- "是" --> T["返回已提交/精确重复与完整新旧关系"]
```

## 不变量

1. 本函数始终只使用名称 owner 写端口；概念、词条、语言和来源只读、不可级联修改。
2. 新增建立五个当前事实；替换同事务退出旧五项并建立新五项；退出只关闭旧五项。
3. 同一概念 / 词条 / 角色 / 语言当前关系唯一；同一概念 / 语言最多一个当前首选名称。
4. 任何非成功都返回空载荷、空变更代次和零权威变化。
5. 反向候选读取不在本写函数中做词性、类型、上下文或首项过滤。
6. 发布未知保留原请求、原 `G0` 和原幂等身份；函数不内部重试、不重选截止。
