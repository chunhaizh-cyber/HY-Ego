# F-A201 执行类型化记录专项子检查单函数施工流程图

更新时间：2026-07-29

## 函数合同

```text
根函数：bool 执行类型化记录专项子检查(
    类型化记录专项子检查编号 编号,
    const 完整正式关系准入表& 准入表) noexcept
归属：海中鱼巣.领域.自检.节点直接特征值类型化记录 /
海中鱼巣/领域/自检.节点直接特征值类型化记录.ixx / module-private
参数：编号按值；准入表调用期只读借用且必须完整。
返回：该固定子检查结果、预期字段和全部前后快照一致返回 true；否则 false。
调用方：仅 F-A108。
```

## 流程图

```mermaid
flowchart TD
 N00["N00【指令-异常边界】try 执行一个固定子检查"] --> N01{"N01【指令-判断】编号在8.4的23项有序表内且准入表完整"}
 N00 -. "任一异常" .-> E01["E01【函数调用】清除类型化记录资源失败注入()"]
 E01 --> E02["E02【函数调用】清除类型化记录阶段失败注入()"]
 E02 --> E03["E03【指令-返回】返回false"]
 N01 -- "否" --> E01
 N01 -- "是" --> N02["N02【指令-绑定】按编号从23项固定描述数组取得唯一 const 描述引用"]
 N02 --> C01["C01【函数调用】清除类型化记录资源失败注入()"]
 C01 --> C02["C02【函数调用】清除类型化记录阶段失败注入()"]
 C02 --> F01["F01【指令-构造】节点直接身份结构事务域 事务域"]
 F01 --> F02["F02【指令-构造】节点直接身份仓库 节点仓{0x5032A001}"]
 F02 --> F03["F03【指令-构造】正式关系仓库 关系仓{节点仓,准入表,0x5032A002}"]
 F03 --> F04["F04【指令-构造】可重建索引仓库 索引仓{节点仓,关系仓}"]
 F04 --> F05["F05【指令-构造】节点直接身份结构写入执行器 执行器{&事务域,&节点仓,&关系仓,&索引仓}"]
 F05 --> F06["F06【指令-构造】特征值类型化记录仓 记录仓"]
 F06 --> F07["F07【指令-构造】特征值类型化记录只读访问器 访问器{记录仓}"]
 F07 --> F08["F08【指令-构造】记录参与者甲{记录仓}"]
 F08 --> F09["F09【指令-构造】记录参与者乙{记录仓}"]
 F09 --> F10["F10【指令-构造】固定失败探针参与者"]
 F10 --> P01{"P01【指令-判断】描述是否要求六基础节点"}
 P01 -- "是" --> P02["P02【函数调用】执行器.执行(基础节点回调) -> 基础结果"]
 P02 --> P03{"P03【指令-判断】基础结果是否已提交且句柄等于8.4固定完整句柄"}
 P03 -- "否" --> E01
 P03 -- "是" --> P04["P04【指令-赋值】前态动作序号=0"]
 P01 -- "否" --> P04
 P04 --> P05{"P05【指令-判断】前态动作序号是否等于描述.前态动作组.size()"}
 P05 -- "否" --> P06{"P06【指令-判断】当前动作种类"}
 P06 -- "甲登记" --> P07["P07【函数调用】记录参与者甲.登记记录候选(动作.记录规格) -> 前态结果"]
 P06 -- "乙登记" --> P08["P08【函数调用】记录参与者乙.登记记录候选(动作.记录规格) -> 前态结果"]
 P06 -- "执行事务" --> P09A["P09A【指令-构造】按动作.参与者组配方形成局部数组/span；局部lambda唯一指令为F-A212(动作.回调配方,上下文,会话)"]
 P09A --> P09["P09【函数调用】执行器.执行(局部lambda,局部参与者span) -> 前态结果"]
 P06 -- "追加历史" --> P10["P10【函数调用】专项追加已发布类型化记录并推进版本(记录仓,动作.记录) -> 前态结果"]
 P07 --> P11
 P08 --> P11
 P09 --> P11
 P10 --> P11["P11【指令-判断】前态结果是否逐字段等于动作.预期"]
 P11 -- "否" --> E01
 P11 -- "是" --> P12["P12【指令-赋值】前态动作序号加一"]
 P12 --> P05
 P05 -- "是" --> S01["S01【函数调用】节点仓.导出权威状态() -> 前节点权威材料"]
 S01 --> S02["S02【函数调用】关系仓.读取全部关系审计() -> 前关系快照"]
 S02 --> S03["S03【函数调用】索引仓.导出可丢弃加速材料() -> 前索引快照"]
 S03 --> S04["S04【函数调用】访问器.读取结构版本() -> 前记录版本"]
 S04 --> I01{"I01【指令-判断】描述是否含资源注入"}
 I01 -- "是" --> I02["I02【函数调用】设置类型化记录资源失败注入(描述.点,1)"]
 I01 -- "否" --> I03
 I02 --> I03{"I03【指令-判断】描述是否含阶段注入"}
 I03 -- "是" --> I04["I04【函数调用】设置类型化记录阶段失败注入(描述.点)"]
 I03 -- "否" --> D00
 I04 --> D00["D00【指令-构造】按描述.参与者组配方形成局部数组/span；局部lambda唯一指令为F-A212(描述.回调配方,上下文,会话)"]
 D00 --> D01{"D01【指令-判断】描述.目标根"}
 D01 -- "F-A101" --> D02{"D02【指令-判断】描述.目标调用位于回调内"}
 D02 -- "否" --> A01["A01【函数调用】记录参与者甲.登记记录候选(描述.记录规格) -> 目标结果"]
 D02 -- "是" --> A01A["A01A【函数调用】执行器.执行(局部lambda,局部参与者span) -> 执行结果并从上下文取得F-A101目标结果"]
 D01 -- "执行器触发F-A102—F-A105" --> A02["A02【函数调用】执行器.执行(局部lambda,局部参与者span) -> 目标结果"]
 D01 -- "F-A106" --> A03["A03【函数调用】访问器.读取当前记录(描述.特征值) -> 目标结果"]
 D01 -- "F-A107" --> A04["A04【函数调用】访问器.读取审计记录(描述.特征值) -> 目标结果"]
 D01 -- "F-A109" --> A05["A05【函数调用】访问器.读取结构版本() -> 目标结果"]
 D01 -- "F-A110" --> A06["A06【函数调用】记录参与者甲.读取本事务发布结构版本() -> 目标结果"]
 A01 --> C03
 A01A --> C03
 A02 --> C03
 A03 --> C03
 A04 --> C03
 A05 --> C03
 A06 --> C03["C03【函数调用】清除类型化记录资源失败注入()"]
 C03 --> C04["C04【函数调用】清除类型化记录阶段失败注入()"]
 C04 --> S05["S05【函数调用】节点仓.导出权威状态() -> 后节点权威材料"]
 S05 --> S06["S06【函数调用】关系仓.读取全部关系审计() -> 后关系快照"]
 S06 --> S07["S07【函数调用】索引仓.导出可丢弃加速材料() -> 后索引快照"]
 S07 --> S08["S08【函数调用】访问器.读取结构版本() -> 后记录版本"]
 S08 --> V01{"V01【指令-判断】目标结果和四项探针计数逐字段等于预期"}
 V01 -- "否" --> R02["R02【指令-返回】返回false"]
 V01 -- "是" --> V02["V02【指令-赋值】材料种类=节点、向量位置=0"]
 V02 --> V03{"V03【指令-判断】当前前后材料标量字段与对应预期相等且向量大小相等"}
 V03 -- "否" --> R02
 V03 -- "是" --> V04{"V04【指令-判断】向量位置是否等于当前向量大小"}
 V04 -- "否" --> V05["V05【指令-借用】当前实际元素和预期元素"]
 V05 --> V06{"V06【指令-判断】元素全部字段递归相等"}
 V06 -- "否" --> R02
 V06 -- "是" --> V07["V07【指令-推进】向量位置加一"]
 V07 --> V04
 V04 -- "是" --> V08{"V08【指令-判断】是否已比较节点、关系、索引三类向量"}
 V08 -- "否" --> V09["V09【指令-推进】切换下一材料种类并把向量位置置0"]
 V09 --> V03
 V08 -- "是" --> V10{"V10【指令-判断】记录版本前后完整结果逐字段等于预期"}
 V10 -- "否" --> R02
 V10 -- "是" --> R01["R01【指令-返回】返回true"]

 subgraph CB["基础节点回调（属于本函数）"]
  B01["B01【函数调用】会话.创建节点候选(场景,场景身份1) -> 宿主"] --> B02["B02【函数调用】会话.创建节点候选(特征,抽象定义身份1) -> 定义甲"]
  B02 --> B03["B03【函数调用】会话.创建节点候选(特征,抽象定义身份2) -> 定义乙"]
  B03 --> B04["B04【函数调用】会话.创建节点候选(状态,状态身份1) -> 来源甲"]
  B04 --> B05["B05【函数调用】会话.创建节点候选(状态,状态身份2) -> 来源乙"]
  B05 --> B06["B06【函数调用】会话.创建节点候选(特征,实例特征槽身份1) -> 错命名域特征"]
  B06 --> B07{"B07【指令-判断】六结果都为已形成候选且完整句柄等于8.4"}
  B07 -- "是" --> B08["B08【函数调用】会话.请求提交()"]
  B07 -- "否" --> B09["B09【函数调用】会话.请求撤销()"]
 end
```

## 关键边界

编号先在 8.4 固定表中查到唯一描述；描述冻结目标根、完整 aggregate 输入、可选注入、参与者 span 配方、前态动作和逐字段预期。记录参与者甲、乙和探针均由本函数构造：A08 固定 span 为 `{&记录参与者甲,&固定失败探针参与者}`，A09 固定为 `{&记录参与者甲,&记录参与者乙}`，其它执行器子项固定为 `{&记录参与者甲}`。一个 F-A201 调用只执行一个子检查。需要本会话候选的用例在对应执行器回调内逐条调用 F-A101，不把候选跨事务保存。节点快照唯一调用当前真实 `导出权威状态()` 并逐字段比较 `节点直接身份仓库权威材料`；异常和正常路径均清除注入状态。
