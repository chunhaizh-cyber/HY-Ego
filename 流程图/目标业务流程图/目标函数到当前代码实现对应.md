# 目标函数到当前代码实现对应

状态：待按现行头源代码重建

冻结核对基线：`e716ad91fe47e27e1a59c98ba6ca753176a76632`

源码子树：`海中鱼巣/` tree `7e2af41ae35bb3855a218045b57ff87020e2ff7e`

## 1. 当前裁决

旧模块时期形成的逐函数实现映射、源码行号、源码 blob、知识图谱和代码流程图已经退出当前证据面。把旧路径机械改写为 `.h/.cpp` 不能证明同名文件存在，也不能证明旧函数、调用边或能力仍由现行代码承载。

本文件因此不再保留旧逐项对应表。`流程图/目标业务流程图/**` 继续定义目标业务合同、目标函数身份和未来物理形态；它们不自动证明当前实现。当前函数是否存在、位于头文件还是源文件、由谁调用、是否符合规范，必须从上述冻结基线中的真实 `.h/.cpp`、工程登记和生产调用点重新核对。

## 2. 已核对的迁移边界

- 当前跟踪源码中不存在已退役模块格式文件。
- 现行头源文件必须分别引用真实 `.h` 与 `.cpp` 路径；不得用一个不存在的 `某文件.h/.cpp` 字符串冒充物理文件。
- 只有头文件存在的头内实现，必须只登记 `.h`；头源均存在时分别登记两份文件及各自 blob。
- 目标文件尚未建立时，只能写“目标物理位置”或“待设计裁决”，不得写成“当前发布源码”。
- 旧同名模块已经物理退出、当前无同名头源文件时，旧实现对照一并退出；不得继续保留旧 blob 并挂到新扩展名下。

## 3. 当前可直接定位的最小源码集合

| 范围 | 现行物理文件 | 当前用途边界 |
| --- | --- | --- |
| 普通应用启动与运行宿主 | `海中鱼巣/启动.应用程序.h`、`海中鱼巣/启动.应用程序.cpp`、`海中鱼巣/启动.程序运行宿主.h`、`海中鱼巣/启动.程序运行宿主.cpp` | 只证明当前启动声明、定义和宿主调用事实，不证明全部目标启动阶段闭合 |
| DATA-L1 服务与仓库 | `海中鱼巣/核心/服务.L1事实基座.h`、`海中鱼巣/核心/仓库.L1事实基座.h` | 当前为头内承载；是否满足目标操作仍须按4015、4080和调用点核对 |
| 支持线程候选 | `海中鱼巣/线程/支持线程候选.数据.h`、`海中鱼巣/线程/候选.支持线程.h`、`海中鱼巣/线程/候选.支持线程.cpp` | 只证明候选生命周期底层能力存在，不证明支持线程组或生产接线 |
| 自我线程当前骨架 | `海中鱼巣/线程/线程_自我.h`、`海中鱼巣/线程/线程_自我.cpp` | 只作为现行自我线程候选；不得继承旧创建模块的实现结论 |
| D455材料链 | `海中鱼巣/适配/协议.D455采样材料.h、海中鱼巣/适配/协议.D455采样材料.cpp`、`海中鱼巣/适配/采集器.D455相机.h、海中鱼巣/适配/采集器.D455相机.cpp`、`海中鱼巣/线程/缓存.D455帧材料.h`、`海中鱼巣/线程/上行桥.D455采样材料.h` | 只证明当前D455材料、采集、缓存和上行桥底层实现，不证明外设线程业务闭环 |
| 线程生命周期投影 | `海中鱼巣/线程/线程生命周期.数据.h`、`海中鱼巣/线程/投影.线程生命周期.h` | 当前为头内承载；不证明任一具体线程已接线 |

表中的 `.h/.cpp` 是“同一基本名的头文件和源文件均已核对存在”的简写，不是单个物理路径。

## 4. 明确失效的旧对应

旧任务管理线程、旧任务工作线程、旧自我线程创建模块、旧六类 L2 结构服务，以及其它当前无同名 `.h/.cpp` 的路径，不再具有当前实现对应资格。目标图可以继续保留相应业务函数，但必须标为待实现、待适配或待重新裁决，不能从旧名称、旧行号、旧 blob 或历史施工记录恢复“已实现”结论。

## 5. 后续重建要求

后续如需恢复逐函数对应，必须逐项完成：

```text
冻结最新正式 HEAD 和源码子树
-> 核对真实文件存在性与工程登记
-> 读取完整声明、定义、DTO和全部生产调用点
-> 对照正式规范与目标业务函数
-> 给出直接复用 / 组合 / 适配 / 扩展 / 新建裁决
-> 分别登记头文件、源文件、blob和当前行号
```

在该重建完成并发布前，本文件只证明旧对应已经退出并给出最小重建边界，不证明任何目标函数已实现、构建通过、运行成功或业务闭环完成。
