# 目标函数到当前代码实现对应

更新时间：2026-08-28

## 1. 定位

本表把目标函数设计与当前已发布代码事实连接起来。目标业务流程图规定“应当怎样”，代码流程图和源码冻结事实记录“当前怎样”；本表只裁决对应状态，不把目标设计说成已实现，也不以当前代码反向覆盖规范。

对应状态固定为：

```text
一致：当前函数合同和行为满足目标合同
部分实现：存在合法可复用片段，但未闭合全部目标合同
未实现：当前没有满足目标身份和语义的函数
实现漂移：当前存在相邻入口，但行为顺序、事实来源、职责或错误语义违反目标合同
证据待刷新：源码已进入正式main，但代码图谱或能力目录尚未刷新到该代码基线
```

## 1.1 2026-08-27 当前接口重基线增量

本节使用本批已复核的已发布主线 `73098d1159f6b4ac5f1daba6e2a4f75bc34052c5` 裁决当前增量；后续发布前仍须按届时最新 `main == origin/main` 重核，不得把该哈希当永久当前事实。下文 2026-08-04 的旧实现对应及其 L1 直连、旧初始化、旧世界入口或旧阶段判断只作历史证据，不得用于新施工。

| 目标函数 / 能力 | 当前代码候选 | 对应状态 | 裁决与证据 |
| --- | --- | --- | --- |
| I1 `初始化并发布系统世界树根` | `业务/系统世界树根初始化.数据.h`、`业务/初始化.系统世界树根.inl`、`装配.普通应用.ixx`、`启动.应用程序.ixx` | **部分实现并接正式启动** | 首次/待收敛及启动14→I2接线存在；已发布重复在无关事实推进后因旧截止必须等于本轮新截止而错误失败，且8120调用方非零选择与DTO固定身份冲突。物理实现同置普通应用模块接口，不存在独立I1 `.ixx` |
| R1 场景树登记、根建立、树归属与整树读取 | 结果提交 `64aad7172e85836d523d302a05b89c04b08837d3`；数据头 / 服务 blob 为 `fc105e159269dcdd7b98e0be3f8bdf82fac7d23c` / `8247c14d888d1f99e0ac8c1e1714a21ff0c80c2c` | **已发布、接受并退出** | `读取当前场景树结构登记()`、`建立场景树根(const L2场景树根建立请求&)`、`读取场景树(const L2场景树读取请求&) const` 及强类型 DTO 已成为正式代码事实；这不证明现实世界资格、A1、I1 或消费者闭环 |
| A1 `L2结构聚合服务` | 实现 `b6c0279f`、退出 `847aa2e7`、清理 `e38ca0de` 已进入 `main / origin/main` | **已发布、接受并退出** | 六个非 owning 服务引用、十二个透明 getter 和普通应用唯一装配已经落盘；三份临时验证已清理。A1 完成不证明 I1 已实现 |
| 普通应用六组直接 getter | `普通应用上下文::取得L2存在结构服务`、`取得L2特征结构服务`、`取得L2状态结构服务`、`取得L2动态结构服务`、`取得L2因果结构服务`、`取得L2场景结构服务`，位于 `海中鱼巣/装配.普通应用.ixx:57—93` | **当前复用** | 六个服务实例和 const / 非 const 直接 getter 是已发布 HEAD 事实；它们允许当前调用者逐服务取得实例，但不能替代或证明 A1 |
| 六类结构公开服务 | `服务.L2场景结构.ixx`、`服务.L2存在结构.ixx`、`服务.L2特征结构.ixx`、`服务.L2状态结构.ixx`、`服务.L2动态结构.ixx`、`服务.L2因果结构.ixx` | **当前复用，按函数逐项复核** | 当前服务、强类型 DTO、当前 / 历史读取和各自 CRUD 已进入 HEAD。旧裸创建与父关系分拆场景入口已经退出，新消费者不得绑定；当前 R1 v0.2 的具名根建立、当前登记读取和整树读取 ABI 已发布，可按目标函数逐项绑定 |
| I2 `消费并发布自我世界树根` | `业务/自我世界树根消费.数据.h`、`业务/消费.自我世界树根.inl`、`装配.普通应用.ixx`、`启动.应用程序.ixx` | **现行固定身份详细设计已实现并接正式启动** | 每次经A1同实例R1重新读取登记与整树，零根写、同请求按本轮截止刷新；失败映射16，成功进入SELF-FORM。请求校验硬绑定I1固定身份，项目树选择ABI须随I1具名DRIFT统一裁决 |
| E-ROLE 存在单例角色与角色化存在 | `领域/L2存在结构.数据.h`、`领域/服务.L2存在结构.ixx` | **已发布、接受、退出并清理专项** | 结果`f7c3a684`、记录更正`1819cfce`、退出`df228f91`、清理`66cd780b`；提供角色预读、角色化存在新增和当前/历史正式读取，不解释自我 |
| S-HOST 场景宿主存在关系 | `领域/L2场景结构.数据.h`、`领域/服务.L2场景结构.ixx` | **已发布、接受、退出并清理专项** | 结果`5e3a8573`、退出`63d6e93a`、清理`c72be0f7`；提供新增/替换/退出、按场景读和按存在反读，不把成员当宿主、不解释自我 |
| SELF-FORM `真实自我形成提供者::形成` | `业务/真实自我形成.数据.h`、`业务/提供者.真实自我形成.ixx`；`启动.应用程序.ixx` 在 I2 后构造并调用 | **部分实现并接正式启动** | provider、工程登记、五步调用边和阶段17映射存在；但完整请求只比较部分字段且锁未覆盖正文，成功重复直接返回缓存；根检查使用I2旧截止，最终使用宿主创建代次而非当前登记截止，缺两完整场景/反向宿主/唯一性互证；状态与结果完整性也被压缩。成功负载仍未传阶段20 |
| 阶段18 `四本体根生产初始化提供者::初始化` | DTO、`.inl`、provider、`L2概念结构服务`三能力和聚合 getter 均在 HEAD；`运行普通程序`真实调用 | **部分实现；唯一协调者所有权漂移** | 请求锁、待重放、受限漂移重组、四次逐根读回、最终全组读回、完整结果谓词和阶段18映射静态对应目标；但provider仍为栈上公开可构造对象，`DRIFT-BIZ-CONCEPT-ROOTS-COORDINATOR-OWNERSHIP-NOT-ENFORCED`未闭合。本批未重跑动态矩阵 |
| 方法登记根生产初始化（非成功映射阶段19）`方法登记根生产初始化提供者::初始化` | DTO、provider、`L2方法结构服务`三能力和聚合 getter 均在 HEAD；`运行普通程序`真实调用 | **部分实现；唯一协调者所有权漂移** | 请求锁、待重放、受限漂移重组、单根与最终恰一根读回、完整结果谓词和阶段19映射静态对应目标；但provider仍为栈上公开可构造对象，`DRIFT-BIZ-METHOD-REGISTRY-ROOT-COORDINATOR-OWNERSHIP-NOT-ENFORCED`未闭合。旧03-01不得复活 |
| 自我线程生产创建并停门（非成功映射阶段20）`自我线程::创建并使自我线程停在治理运行门` | `线程/自我线程生产创建.数据.h`、`线程/创建.自我线程.ixx` 已登记；`运行普通程序`栈上构造并调用 | **部分实现 + 三组具名实现漂移** | 停门见证、重复、失败回收与阶段20映射存在；但缺SELF-FORM材料，I2仅树/根级校验和重放比较；对象不由普通应用上下文唯一持有且在创建入口临时装配运行期provider；生命周期端口为`nullptr`，异常wrapper也不发布故障/异常退出投影。三组DRIFT为`CREATION-INPUT-AND-REPLAY-INCOMPLETE`、`CONTEXT-OWNERSHIP-AND-DEPENDENCY-ASSEMBLY-NOT-CLOSED`、`LIFECYCLE-PROJECTION-INCOMPLETE`。BIZ-L4-001-03-11-01另有循环进入见证和退出结果保存缺口 |
| 支持线程候选生命周期 `创建支持线程候选 / 等待支持线程进入见证 / 停止并连接支持线程启动候选` | `线程/支持线程候选.数据.h` blob `e01456813c90379991180fcfd2c417f9d43c8410`；`线程/候选.支持线程.ixx` blob `f3580ff4ce414c9ba93b4e9b2abfb78ce6304f93` | **已实现；EVENT已消费，CACHE/持久未消费** | `fd1c88525d493e3ad33b977622388e9e01bde685` 已实现三组配对、一个具名入口端口、仅移动 `std::jthread` 租约、内部进入/完成见证、8110 最佳努力投影和返回前安全 join。EVENT provider 已真实复用；该结果仍不证明父支持线程组、CACHE、持久证据或生产接线完成 |
| BIZ-L2-001-04 `启动支持线程组` | `启动.应用程序.ixx` 中无参 `bool 启动支持线程组()`；目标 `启动.线程组协调.ixx`、请求/结果 DTO 与租约组不存在 | **实现漂移** | 当前函数固定返回 `true`，不调用任何叶、不持有租约、不形成逐叶降级；父调用用布尔 `&&` 把潜在支持失败并入整体启动成功，违反“支持旁路失败只降级”目标。不能因无条件成功让后继可达就记为支持组已启动 |
| 事件日志线程生产启动 `启动事件日志线程 / 事件日志线程入口::运行线程入口` | `线程/事件日志线程.数据.h` 与 `线程/运行.事件日志线程.ixx` 已存在并唯一登记工程；HEAD 除本模块声明/定义外零调用 | **provider与入口已实现；生产接线未实现** | `f2726649` 实现固定 EVENT 类别—模块、地址稳定拥有体、SUPPORT 创建/等待/失败回收、MPSC/单消费者 FIFO、stop callback、单次人读写入、唯一移动租约及提交/停收/位置/等待/快照/右值停止连接；`a867a528` 退出计划、`3cd75bad` 清理专项。历史记录为20 PASS/0 FAIL/3 NOT_RUN，本批未重跑；父组、普通应用依赖和阶段15仍未实现 |
| BIZ-L2-001-15 v0.2 `刷新并连接支持线程组`及四个 L4 适配叶 | `启动.应用程序.ixx` 中 `void 刷新并连接支持线程组()` 为空；EVENT 租约已有停收、等待、位置、快照和右值停止连接；`启动.分阶段停止.ixx`、`启动.支持线程收口.ixx`、支持租约组和阶段15 DTO均不存在 | **父组合与四个适配叶未实现；EVENT专属底层能力可复用** | 目标已校正为逐线程停收取截止、等待/同截止快照、材料资格和只移动有资格租约；EVENT 的 `前置未闭合但已安全连接` 只能映射异常资源回收。CACHE/持久专属能力、逐类owner读回和普通应用租约持有仍缺失，不能以空壳、析构join或EVENT单叶声明阶段15完成 |
| 缓存统计线程生产启动 `启动缓存统计线程 / 缓存统计线程入口::运行线程入口` | 目标文件 `线程/缓存统计线程.数据.h` 与 `线程/运行.缓存统计线程.ixx` 当前均不存在；旧缓存统计线程壳已删除 | **未实现 + 启动非成功矩阵设计漂移** | BIZ-L3-001-04-02 / BIZ-L4-001-04-02-01 v0.2 冻结 SUPPORT 创建/进入等待/失败安全回收、唯一移动 CACHE 租约和 8110 const 当前投影组读取；但私有分配资源失败及SUPPORT资源失败/入口故障/超过诊断预算安全连接未逐项映射，`DRIFT-BIZ-CACHE-STATISTICS-START-NON-SUCCESS-MATRIX-INCOMPLETE`未闭合。DATA-L1—L3 零需求，不依赖EVENT；未编译、未装配、未接BIZ-L2-001-04/阶段15 |
| BIZ-L2-001-05 `启动任务工作线程池` | 目标`启动.线程组协调.ixx`、线程池请求/结果和池租约不存在；`任务结果生产运行包`只内嵌一个`任务工作线程` | **未实现 + 启动顺序漂移** | HEAD先调用`管理线程_.启动()`，成功后再调用单个`工作线程_.启动()`；目标要求T04消费者池全部进入关闭接收门后的等待态，再把完整池租约交给T03启动。当前没有池内身份组、逐位置进入、半失败逆序回收或完整池结果 |
| BIZ-L3-001-05-01 v0.2 / BIZ-L4-001-05-01-01—03 单工作线程启动 | `任务工作线程::启动()`为无参成员，只在对象锁内创建调用`运行结果尾部()`的`std::thread`；没有目标DTO和三叶入口 | **同名语义漂移；目标三叶未实现且合同未闭合** | 构造函数只有旧结果队列容量和T03引用，对象不可移动；无运行代次、逻辑身份、池内序号、关闭接收门、候选/正式租约、8110发布或内部进入见证。目标owner/依赖寿命/非成功矩阵未冻结，旧当前租约/同义复用与唯一租约冲突；平台线程构造只能作机械候选 |
| BIZ-L4-001-05-01-02 平台入口及 BIZ-L3-001-05-02 v0.2 启动候选回收 | 私有`运行结果尾部()`消费已形成`任务工作结果材料`并形成旧回执；`停止接收并排空()`与`收口等待()`处理同一旧队列和对象内线程 | **入口职责漂移；候选回收未实现且合同未闭合** | 目标平台入口只发布内部见证并调用唯一BIZ-L1-T04根循环；当前旧循环自行处理业务尾部。停止与无时限join没有候选身份、完成见证、全量先停、总预算或租约返还；目标回收ABI及有限预算下剩余租约后继owner未冻结，不能冒充启动失败回收 |
| BIZ-L2-001-06 `启动任务管理线程` | 目标父根、物理文件、请求/结果、T03候选/正式租约、启动联锁门和T04接收门开放事务不存在；运行包直接调用无参T03启动 | **设计合同未闭合 + 实现与T04联锁缺失** | 正确顺序是先消费已进入且门关闭的T04池租约，等待T03进入启动联锁并证明零治理消费，再原子开放T04门。父根签名/DTO、owner、四种输入物理拓扑、门事务和非成功矩阵未冻结；HEAD先T03后旧T04且没有门 |
| BIZ-L3-001-06-01 v0.2 / BIZ-L4-001-06-01-01—03旧草图 T03候选启动 | `任务管理线程::启动()`无参；先置运行中，再创建lambda，lambda置无身份`已进入`后立即调用`运行结果上行循环()`；调用方直接收到成功 | **同名语义漂移；目标ABI未冻结且旧三叶不可施工** | 没有运行代次、逻辑身份、T04池/关闭门绑定、启动幂等、候选租约、启动联锁见证或进入时限。旧“多源输入等待态”不成立；目标入口必须先停在T04开门启动联锁态，父级开门前零治理消费，开门后才运行唯一治理循环 |
| BIZ-L3-001-06-02 v0.2 / BIZ-L4-001-06-02-01—02旧草图 T03启动候选回收 | `任务管理线程`由运行包内嵌对象直接持有线程；相邻`请求停止并发送停止消息()`面向旧正式线程，`收口等待()`与析构直接无时限join | **目标ABI/owner未冻结；旧草图不可施工** | 当前没有候选阶段、移动租约、停止/完成/回收见证、总预算、墓碑或未连接租约返还；旧根const借用与连接叶移动消费冲突。停止原子量、notify和join只可作机械候选，不能证明有限安全回收 |
| BIZ-L2-001-12 v0.2 / BIZ-L3-001-12-00—02 v0.2 治理停产 | T03停止锁存只阻止新强类型执行请求预留；已确认请求仍可取出。T03队列快照只有状态计数，T04快照只有累计计数与最后结果；T02真实D承接下行尚未接线 | **局部锁存/统计候选存在；目标生产链、工作包宇宙与完整在途账未冻结** | HEAD没有T02全链边界、封闭工作包种类、统一派发owner、正式停止阶段或可枚举全部成员的在途账。两个局部统计快照不能拼成0..N完整集合；各线性化owner、闭合事件、共同截止、四叶ABI和父级部分提交/重试矩阵闭合前不可施工 |
| BIZ-L3-004-01-01 v0.3 `需求目标事实提供者::读取需求目标当前事实` | 两个 L4 消费合同均已冻结；`海中鱼巣/领域/提供者.需求目标事实.ixx` 与 `需求目标事实.数据.h` 当前仍不存在，两个 L4 函数和父组合函数均未实现 | **父组合合同已冻结；L3 与两个 L4 的 BIZ 代码均未实现** | 父函数在同一 provider 内先读目标状态合同 / 定义 / 目标判断注册，再以同一非零 G0 读取指定树 / 场景内目标宿主唯一同定义当前实例和值；代码待两个 L4 前驱真实结果、退出、清理及同两文件释放后串行扩展。旧 `需求业务服务` 路由、4200 业务门面、状态 / 动态快照拼装及世界快照身份退出；差距比较、普通应用持有 / 专用 getter / 装配和父业务闭环均 `NOT_RUN` |
| BIZ-L4-004-01-01-01 v0.2 `需求目标事实提供者::读取目标状态合同与值域定义` | DATA 已正式发布 `L2状态结构服务::读取完整目标状态合同`、`L2特征结构服务::读取完整统一特征定义`和`读取当前特征比较注册`；普通应用已有 const 聚合 getter；当前无 `需求目标事实.数据.h`或`提供者.需求目标事实.ixx` | **DATA ABI和取得路径已实现；BIZ消费合同已冻结；BIZ provider未实现** | 新 BIZ provider 只持消费者经现有 getter取得的同一 `L2结构聚合服务`，在一个非零G0下显式三读并互证身份链；普通应用持有 / 专用getter / 装配状态 NOT_RUN；只闭合I64标量材料，不读取宿主当前值、不执行差距比较、不直达L1 |
| BIZ-L4-004-01-01-02 v0.2 `需求目标事实提供者::读取目标宿主同截止当前特征事实` | `L2场景结构服务::读取场景树归属/读取完整场景`、`L2存在结构服务::读取完整存在`、`L2特征结构服务::按存在读取全部当前特征`均已发布；当前 BIZ provider 文件尚不存在 | **DATA ABI 已实现；BIZ 完整消费合同已冻结；BIZ 代码未实现** | 串行扩展上一叶同一 provider，四个公开当前读取使用同一非零G0，证明目标宿主为指定树 / 场景直接成员并按定义取得唯一当前实例和值；首读缺失、合法空筛选与已证明引用后的矛盾分账，所有非成功空业务载荷；旧4200业务门面、状态 / 动态拼装退出，不新增 DATA 接口 |

### 初始化六叶当前静态复核边界

当前启动链后续纠偏至少必须保留：

```text
I1/I2 正式函数、值式材料与系统世界树根读回字段
-> SELF-FORM、四本体根、方法登记根实际公开 DTO / provider / DATA 调用
-> 14 / 缺号15 / 16 / 17 / 18 / 19 / 20 阶段矩阵
-> SELF-FORM 成功负载向阶段20逐字段交付
-> 普通应用上下文对三个协调者和自我线程的唯一所有权
-> 线程生命周期端口、停门见证与后继运行门开放边界
```

任一签名、构造、装配、状态、生命周期或资源项漂移都必须结构化记录；不得增加别名、直接 getter 旁路、兼容 DTO、第二 owner，亦不得用前一步成功码替代后继所需的完整值式材料。

## 2. 证据基线

```text
目标业务图基线：a6b6a701（2026-08-04发布）
当前代码核对起点：697397a4及其后续main；每批以具体提交重新冻结
代码流程图声明源码冻结：4b80f161
能力证据目录声明冻结：64a2d63c；内嵌源码事实版本3920a746
```

代码流程图和能力目录当前均早于目标图发布时的正式代码HEAD，因此可以提供候选稳定编号，但不能单独证明新源码入口已制图或可复用。每批必须回读正式源码并记录是否受图谱基线滞后影响。

## 3. 全量机械候选扫描

按当前文件名稳定身份去重，目标图 BIZ-L1—BIZ-L4 共432个根函数合同；本次新增的三个合同是001-02三类停止来源安装叶，旧415项口径已经滞后。旧“24项命中 / 377项无同名”统计不覆盖 I1/I2、SELF-FORM、四本体根、METHOD、03-11和停止来源新叶，也不能从旧数机械外推。全量正式受跟踪源码名称候选统计等待代码图谱维护者按最新发布基线刷新；这里的“出现”仍不证明签名或行为一致，“无同名”也不自动等于未实现。

正式代码图谱旧基线能以完整函数身份唯一命中的只有6项；其余正式候选必须按业务链人工比较签名、事实来源、线程、事务和非成功语义。人工复核优先级固定为：正式执行主链上的精确命中与实现漂移，随后是相邻可复用能力，最后才是无同名目标；不得沿旧377项统计做盲搜，也不把两项预 ABI 命中纳入正式图谱。

## 4. 启动装配与初始化链

本链在代码图谱冻结提交 `4b80f161` 后已连续变化；当前代码图谱仍由独立维护者刷新，所以下表以本批开始时已发布 HEAD `dad75c430f54ccffe8601880a6c3cea0b7d55f19` 的生产源码为事实，不把旧 F 身份或未提交图谱 WIP 当作现行实现证明。

| 目标函数 | 当前代码候选 / 稳定编号 | 当前代码图 | 对应状态 | 裁决与证据 |
| --- | --- | --- | --- | --- |
| BIZ-L2-001-01 v0.2 `构造普通应用上下文` | F0013，同名同签名，`海中鱼巣/装配.普通应用.ixx`；当前装配L1运行包、owner能力交付、L2结构服务、聚合及任务筹办提供者 | `CF-L3-0001_构造普通应用上下文_现状流程图.md` | 基础装配已实现；后继所有权接线未闭合 | 当前函数没有创建物理线程，符合初始装配边界；阶段20仍在`运行普通程序`栈上持有自我线程，普通应用上下文没有接纳唯一租约的边界。该偏差归001-03/03-11接线，不恢复已退出的第二装配根 |
| BIZ-L2-001-02 v0.2 `安装程序停止来源` | HEAD只有子叶`安装程序停止信号`，没有正式多来源组合入口 | 子叶已有`CF-L3-0007_安装程序停止信号_现状流程图.md`；父根无当前代码图 | 部分实现；来源宇宙设计漂移 | 信号子叶的平台边界可复用但安装期仍可能丢信号；程序控制和致命故障来源只属旧目标候选，正式规范尚未冻结其owner/生产者/生命周期或必须纳入同一来源组 |
| BIZ-L3-001-02-01 `安装程序停止信号` | F0019，同名同签名，`海中鱼巣/启动.程序运行宿主.ixx` | `CF-L3-0007_安装程序停止信号_现状流程图.md` | 部分实现 | 当前先逐项安装新处理器，全部成功后才发布普通静态租约指针；安装窗口内信号可被新处理器接收却因指针为空而丢失。析构忽略恢复调用结果并无条件清空指针。目标要求进程级`sig_atomic_t`、安装期零丢失和恢复失败租约守恒 |
| BIZ-L3-001-02-02 `构造程序控制停止来源` | HEAD无同名函数或程序控制停止来源模块 | 无当前代码图 | 未实现 | 需新增进程内唯一来源拥有体、提交/读取分面、一次锁存和首次记录；T05只是一个可选生产者，无窗口模式来源保持未锁存。禁止实现成有界邮箱或通用命令队列 |
| BIZ-L3-001-02-03 `构造致命线程故障停止来源` | HEAD无同名函数或致命线程故障停止来源 | 无当前代码图 | 未实现且受具名DRIFT限制 | 目标仅需一次锁存/首次记录及受约束提交/读取分面，不建观察缓存或历史队列；分类生产者、来源ABI和后到历史未冻结前不能接8110、日志或普通故障替代 |
| BIZ-L2-001-10 / BIZ-L3-001-10-01 v0.2 `等待程序停止请求 / 等待任一停止来源` | HEAD只有`运行无窗口宿主(停止信号租约&)`轮询单一停止位；没有程序控制/致命停止来源、组合结果、仲裁或T01停止阶段owner | 无对应组合根函数图 | 部分信号能力可适配；目标合同未冻结 | 先冻结封闭来源宇宙、各owner/生产者/生命周期、多个候选仲裁、组合读取当前性/等待矩阵和停止阶段提交/读回；不得按旧图直接新增三来源或首项锁存 |
| BIZ-L2-001-03 `初始化普通系统` | 当前无同名生产函数；`海中鱼巣/启动.应用程序.ixx` 已内联调用 I1、I2、SELF-FORM、四本体根、方法登记根和自我线程创建 | 旧 `CF-L3-0003` 不再作为现行证明 | 实现漂移 | 六叶顺序和14/16/17/18/19/20映射已存在，但父聚合根、请求/结果 DTO 和目标文件不存在；SELF-FORM本身仍有请求/重放、共同截止完整读回和状态/结果形状偏差；三个 provider及自我线程未由普通应用上下文唯一持有，阶段20请求缺 SELF-FORM 正式材料。运行门开放仍属后继001-08，不能用内联顺序相同声明分层闭合 |

## 5. 动作完成、权威回读与动态校准链

| 目标函数 | 当前代码候选 / 稳定编号 | 当前代码图 | 对应状态 | 裁决与证据 |
| --- | --- | --- | --- | --- |
| BIZ-L2-004-07 `任务工作线程::消费并推进一个不可变任务工作包` | HEAD `任务工作线程.ixx`没有该函数；私有`运行结果尾部()`只出队已经形成的任务工作结果材料、形成旧回执并上交T03 | 无对应根函数图 | 未实现；旧结果尾部不可复用为父根，目标v0.1自身仍有旧回执/接收门偏差 | 目标须新增接收门开放后才取包的筹办/执行互斥受控消费和动作后恢复，并只形成/重交结构化工作结果定位；旧结果材料队列、回执计数与直接上交不能替代最终复核、实例执行、完成消息、权威实际回读或同定位重交 |
| BIZ-L3-004-07-04 `动作结果事实组合器::校准并发布实际状态与动作动态` | `任务执行组合器::执行任务目标状态值变化`，`海中鱼巣/领域/组合.任务执行.ixx:117`；正式图谱无F/R身份 | 只有构造函数图 `CF-L6-0255`，不是执行函数图 | 实现漂移 + 证据待刷新 | 当前从调度请求读取`实际状态值`并直接调用状态动态组合器创建状态和动态（同文件151—184），发生在完成消息和权威回读之前，不能复用为目标校准入口 |
| BIZ-L3-004-07-05 `运行期只读查询组合器::读取已发布动作结果定位材料` | R0662/R0663仅提供按主键读取单项动态材料 | `CF-L9-0097`、`CF-L9-0098` | 未实现 | 当前没有按工作包、方法运行、固定截止联合读取状态、迁移动能和可选动作动态并互证的入口 |
| BIZ-L3-004-07-08 `动作完成消息桥::发布并验真动作完成消息` | 无合法对应；HEAD任务工作线程只在旧结果尾部形成并上交任务结果回执 | 无对应图 | 未实现 | 任务结果回执不能替代动作完成消息，且没有独立动作运行登记读取和绑定验真 |
| BIZ-L4-004-07-04-02 `动作实际回读路由::按实在域读取动作后实际状态` | 无合法对应 | 无对应图 | 未实现 | 当前“调用方传入实际值”违反8210权威实际回读原则 |
| BIZ-L4-004-07-04-03 `动作结果校准器::比较动作前状态预计动态与实际状态` | 可候选复用4180比较算法族，尚无动作校准入口 | 无对应图 | 未实现 | 必须绑定具名特征定义、用途、算法和误差版本，禁止默认减法 |
| BIZ-L4-004-07-08-01 `动作完成消息队列::发布或重试动作完成消息` | 无合法对应 | 无对应图 | 未实现 | 需要独立消息边界和同消息重试；不得复用任务结果回执队列伪装 |
| BIZ-L4-004-07-08-02 `动作运行业务服务::读取动作完成消息与运行绑定` | R0486/R0487/R0494只能读取方法动作入口结构，不是动作运行实例 | `CF-L13-0022`、`CF-L13-0023`、`CF-L12-0047` | 未实现 | 可复用动作入口结构读取，但动作运行实例、消息消费见证和逐项绑定读取仍须新增 |

## 6. 分层安全维护链

| 目标函数 | 当前代码候选 / 稳定编号 | 当前代码图 | 对应状态 | 裁决与证据 |
| --- | --- | --- | --- | --- |
| BIZ-L2-003-01 v0.2 `分层安全维护服务::评估并维护安全层` | `海中鱼巣/领域/服务.L2治理函数骨架.ixx:416` 只有恒定材料拒绝空壳；生产目标模块 `服务.分层安全维护.ixx` 不存在 | 无对应现状图 | 空壳 + 目标未实现 | T02 当前调用空壳；协议和纯值算法不能代替根编排、事实读取、外部参与、数据操作、正式读回与装配 |
| BIZ-L3-003-01-01 v0.2 `安全维护事实提供者::读取已发布安全维护事实` | `协议.分层安全维护.ixx:565` 只有来源请求/载荷/结果DTO；无公开提供者函数或生产实现 | 无对应现状图 | 未实现生产端口与提供者 | 必须按三组L4读取合同建立具名生产提供者并由生产装配持有；不得由线程、缓存或测试替身提供当前事实 |
| BIZ-L3-003-01-02 v0.2 `计算分层安全维护候选` | 同名纯值函数，`海中鱼巣/领域/算法.分层安全维护.ixx:225` | 无对应现状图 | 实现偏差 + 证据待刷新 | HEAD在事实截止等于当前游标时由纯值函数返回`精确重复`；v0.2目标要求正式重复只由提交入口按幂等账读回裁决，算法只形成候选 |
| BIZ-L3-003-01-03 v0.2 `安全维护外部事实参与提供者::形成安全维护外部事实参与包` | 仅有旧 `安全维护外部事实身份规格` DTO，`协议.分层安全维护.ixx:317`；无提供者函数或实现 | 无对应现状图 | 目标已裁决、代码未实现 | 确认稿51已撤销第四稳定主键停点；代码需删除调用方三主键形状，改为四互异写集本地键和发布后新编码映射 |
| BIZ-L3-003-01-04 v0.2 `分层安全数据操作::提交安全维护候选` | 目标文件 `海中鱼巣/领域/数据操作.分层安全.ixx` 不存在 | 无对应现状图 | 未实现 | 需实现零变化仅维护账、非零变化四事实同写集、来源G0/首次G1、新编码映射、已可能发布和联合读回；当前不能宣称生产闭环 |
| BIZ-L2-003-02 v0.2 `服务生存治理服务::维护服务与生存安全` | `海中鱼巣/领域/服务.L2治理函数骨架.ixx:434` 只有合同版本和`完整秒区间==0`判断；随后固定返回`无完整秒`。T02只接受`已结算完整秒` | 无对应现状图 | **空壳 + 消费端实现漂移** | 目标要求N=0合法继续、可恢复时间缺口发布新纪元、一次处理全部到期段、最后统一发布及G0/G1矩阵；当前空壳既不维护事实，T02还把合法N=0导向领域终止 |
| BIZ-L3-003-02-01/02 v0.2 合同终态与准备补回候选 | `服务.服务合同.ixx`、`服务.服务准备结算.ixx` 及同名函数均不存在 | 无对应现状图 | **未实现** | 必须按同一G0调用四张v0.2只读/纯值叶并只形成候选；不得从任务回执支付P1、从结果码自证准备完成或由候选函数宣称权威精确重复 |
| BIZ-L3-003-02-03/04 v0.2 两个纯值算法 | `算法.服务时间维护.ixx`、`算法.服务生存回归.ixx` 及同名函数均不存在 | 无对应现状图 | **未实现** | 时间衰减须批量等价逐秒并分账入口/版本/材料/算术；安全回归须主动安全优先、A=0先行和低位上升门禁，二者均不拥有持久幂等或发布语义 |
| BIZ-L3-003-02-05 v0.2 `服务生存数据操作::发布服务生存维护事务` | `海中鱼巣/领域/数据操作.服务生存治理.ixx` 不存在 | 无对应现状图 | **未实现** | 新纪元、全部段合同/补回/衰减/安全根/状态动态/游标只在一个写集发布；来源G0、各owner CAS、首次G1、首次载荷、已可能发布和提交后读回失败必须分账 |
| BIZ-L4-003-02-01-01/02 v0.2 合同终态读取与裁决 | 目标组合读取和纯值算法均不存在；现有需求/任务/状态等相邻服务不能替代同G0合同闭包 | 无对应现状图 | **未实现** | 01必须完成0/N合同G0双守卫读取；02只按发生秒/正式发布序/稳定身份裁决，既有终态保持不是纯值函数发布的精确重复 |
| BIZ-L4-003-02-02-01/02 v0.2 准备成果与衰减段读取 | 目标两个只读入口均不存在 | 无对应现状图 | **未实现** | 准备成果须互证任务/方法/状态动态/验证；衰减段须证明0/N完整覆盖、唯一归属、未补回和本秒新段排除，索引空不能直接冒充合法空集 |
| BIZ-L4-003-02-05-01—03 v0.2 事务参与、执行与联合读回 | 目标数据操作文件和三个函数均不存在；现有L1事务底座只能作为候选复用证据 | 无对应现状图 | **未实现** | 01形成唯一移动事务包，02全准备/确认后最后交换并分账首次G1/已可能发布，03按原键和首次G1读取首次历史载荷；不得拼接各owner当前最新值 |
| BIZ-L2-003-04 v0.2 `服务合同服务::建立或读取服务合同` | `海中鱼巣/领域/服务.服务合同.ixx`、对应DTO、provider、算法和数据操作均不存在；T02九类路由也没有交付完整建立请求的来源 | 无对应现状图 | **目标根有效；生产来源与实现均缺失** | 先冻结人类服务需求正式准入完成后的强类型调用点，再实现同G0来源/法规/安全根和0/N当前合同读取；不得新增消息类别或从派生需求载荷补造合同键 |
| BIZ-L3-003-04-01/02 v0.2 准入与当前合同读取 | HEAD无准入provider、法规服务、合同关系/记录/结算/幂等读回组合；相邻需求/关系/安全根目标能力不能替代完整组合 | 无对应现状图 | **未实现** | 01完成来源/法规/根值G0读取及身份版本材料交付；02用正式关系而非索引证明0/N完整性，并读回当前合同与首次G1完整载荷 |
| BIZ-L3-003-04-03 v0.2 `计算服务合同预算与预支` | `海中鱼巣/领域/算法.服务合同预算.ixx`及同名函数不存在 | 无对应现状图 | **未实现** | 只消费E、V和正式规则定义；D0/M/默认有效期不是调用方输入，算法不生成身份/版本或宣称权威精确重复 |
| BIZ-L3-003-04-04、BIZ-L4-003-04-04-01—03 v0.2 合同/P0事务 | 目标数据操作与三个事务/读回函数均不存在；4040/L1事务底座只提供候选复用边界 | 无对应现状图 | **未实现** | 正式身份版本材料、各owner CAS、来源G0与首次G1分账；合同/P0/服务值/关系/幂等最后发布，未知只按原键收敛，首次历史载荷不得由当前最新值重建 |

本链的合理后继不是再建第二个根服务。目标合同已经按确认稿51闭合；后继应校正协议/纯值算法，实现唯一根组合、事实提供者、外部参与提供者、数据操作和装配，再经合法T02消费者、正式状态×载荷矩阵与独立读回做集成验收。

## 6.1 世界与自我冻结截止读取

| 目标函数 | 当前代码能力 | 当前状态 | 精确缺口 / 裁决 |
| --- | --- | --- | --- |
| BIZ-L1-T01 v0.4 `运行普通程序(启动模式)` | HEAD在T01内联001-03初始化叶，由运行包合并启动T03/T04；已有单一进程信号等待、T03停发和部分空壳收口 | **普通模式部分实现且分层有偏差；停止来源宇宙/停止阶段、生产运行期及致命分类均未冻结** | 先把初始化内联收回001-03聚合入口；001-02/10不得按旧图新增三来源或首项锁存，须先冻结来源owner/生产者、仲裁、组合读取和停止阶段提交/读回。后继排空、窗口宿主和线程租约按各编号继续校正 |
| BIZ-L1-T02 v0.6 `自我线程::运行治理循环` | HEAD已进入根循环并持有邮箱、批次路由、完整秒和事实版本，部分调用一致事实/安全/服务/调度骨架和任务结果处理槽；公开协议与路由固定九类，未生产002-06/003-04/004-01/004-06所需完整请求 | **部分实现且存在分层、路由、停止、后继及四根来源偏差** | 只按11个来源已闭合根继续接线；四个缺来源目标根先冻结调用点和材料生产者。002-07与002-09分别调用002-08；禁止扩展消息类别、回退旧无载荷消息组或临场派生键 |
| BIZ-L2-002-09 v0.1 `自我任务后继决议提供者::发布或读取自我任务后继决议` | HEAD无self后继决议数据/module、owner、发布/读取入口或T02生产调用；未跟踪同名文件不是发布能力 | **目标owner合同已冻结；实现缺失** | 按显式T/R/轮次结算/G和完整治理依据形成唯一四分支决议并独立读回；消息只携带定位，不复制指令或依据 |
| BIZ-L1-T03 v0.5 `任务管理线程::运行结果上行循环` | 目标严格分解为12个直接BIZ-L2根；HEAD已在两个内部队列间优先处理主阶段治理、再处理工作定位，并接部分同截止后继排队、强类型执行请求队列和动作后结果读回，但没有统一处理槽恢复判断、003-03 v0.4候选fresh选择、004-09三源公平取出、004-02 v0.2统一承接、004-16 v0.2正式后继消费、004-17零动作收束、正式上级调用返回/槽结算或004-12 v0.2单项回流治理槽 | **部分实现且存在处理槽、调度、承接、回流、结果收束/停止偏差** | 根循环先恢复已接管处理槽，无槽才调用004-09；主阶段治理槽不得冒充第四来源。执行冻结后须先由003-03 v0.4选择0..1项；唯一上级调用槽结算后只形成显式上级T/R回流槽，再经004-11/004-12同G0重读。不得从需求父链猜上级或批量结算其它任务；资源失败丢槽不能证明收口 |
| BIZ-L1-T04 v0.3 `任务工作线程::运行强类型执行循环` | HEAD没有该目标根；`启动()`只创建物理线程运行`运行结果尾部()`，其输入是已形成结果材料，回执形成/上交失败只计数后丢弃 | **目标根未实现；只有旧结果尾部物理线程可作资源壳参考** | 新根只直接调用004-07/004-15 v0.2；004-07 v0.2已冻结唯一在途槽优先恢复、接收门开放后取包和结构化工作结果定位交付；复用condition_variable与停止排空资源模式，但不得把旧结果材料、回执计数或直接上交反写为目标业务链 |
| BIZ-L2-004-15 v0.2 `任务工作线程::报告一次生命周期故障` | HEAD任务工作线程没有生命周期发布端口或故障发布调用；通用`线程生命周期投影服务`已发布 | **底座可直接复用；T04适配入口未实现** | T04真实故障先锁存隔离并停止取新包，再形成单调事件序号的一次发布请求；未接受只留序号缺口，不建立队列/重试槽，不改变004-07业务恢复 |
| BIZ-L3-004-15-01 v0.2 `线程生命周期投影服务::发布` | HEAD已发布同名公开override及发布端口；实现有双版本/错代校验、try_lock、幂等、迟到链、终态、双账更新和结构化结果，provider专项双配置19/19记录已归档 | **当前复用；provider能力已实现，T04消费未接线** | 直接复用唯一provider，不新增`协议.线程生命周期.ixx`形成器或`线程生命周期消息队列.ixx`第二owner；具体线程负责真实事件和物理前态 |
| 旧 BIZ-L3-004-15-02 v0.1 `线程生命周期消息队列::提交线程生命周期消息` | HEAD不存在该模块或函数；现有唯一投影服务已经拥有消息账和当前投影 | **冗余目标，已退出004-15、005-02与006-02当前子树** | 006-02 v0.2只保留纯值分类/收束职责；真实外设线程事件只经006-05复用唯一8110发布端口。006-02实例级错误报告作用范围/产品承载仍待6340/6350冻结，不新增第二队列填补缺口 |
| BIZ-L2-004-16 v0.2 `任务主阶段治理提供者::消费自我任务后继决议` | HEAD无`任务阶段治理.数据.h`、`服务.任务主阶段治理.ixx`、self后继owner或task机械继续/生命周期收口入口；未跟踪同名文件不属于发布能力 | **业务语义已冻结；详细设计ABI漂移；整条生产链未实现** | 只按显式T/R/结算/决议/G读取self正式决议；继续以task/阶段两个owner分段收敛并切换当前R/P引用；等待状态/指令且零写；结束/普通取消复核已收束阶段，不重复接收停止证据 |
| BIZ-L3-004-16-01—04 v0.2 | HEAD无四个module-private组合；现有旧任务后继筹办准备链不能替代self正式决议读回或四分支消费 | **目标组合语义已冻结；详细设计ABI待同步；实现缺失** | 不按任务猜决议、不从目标未达成自动继续；后段失败保留已发布前段task事实和提交见证；执行中紧急取消另走3200第19.2节合同 |
| BIZ-L4-004-16-01-01 v0.2 `自我任务后继决议提供者::读取自我任务后继决议` | HEAD无self后继决议module、DTO、owner索引或公开读取入口 | **业务语义已冻结；详细设计状态ABI待同步；实现缺失** | 读取成功沿已形成/精确重复与完整事实/非零截止谓词；新合同不产生旧许可拒绝；消息或最近决议不能替代指定身份回源 |
| BIZ-L4-004-16-02-01/04-01 v0.2 `L2任务结构服务`机械后继入口 | HEAD任务数据/服务无后继继续四事实、当前R/P切换、决议引用、生命周期收口DTO和公开写读入口 | **业务语义已冻结；机械ABI待详细设计同步；实现缺失** | task owner不选择指令、不调阶段端口；继续同写集切换当前R/P；生命周期收口不复制停止证据；同键异义冲突，提交后读回失败保留四字段见证 |
| BIZ-L4-004-16-03-01 v0.2 `任务阶段结构消费端口::发布阶段迁移` | HEAD无任务阶段结构消费端口module；已发布ARCH-L2状态/动态v2底座存在但未形成该目标适配器 | **DATA底座可复用；详细设计状态ABI待同步；目标端口未实现** | 只映射ARCH-L2迁移及当前/历史读回，不在task owner复制阶段权威；新合同不产生旧许可拒绝；task/阶段提交代次分账 |
| BIZ-L2-001-09 / BIZ-L3-001-09-01—03 / BIZ-L1-T05 v0.4 控制面板宿主与只读窗口循环 | HEAD只有按模式返回bool的空壳，普通模式仍调用无窗口宿主；无窗口/UI/程序控制桥；8110只读provider可复用 | **未实现；物理执行拓扑、窗口owner、运行成功谓词及候选回收合同未冻结** | 8120只要求程序宿主运行普通窗口或无窗口等待。主宿主/独立T05必须先选一；独立线程方案才需要候选租约、进入和回收，09-02/03仍须冻结成功/终止点、生产/通知、回收owner、预算、连接和未回收所有权矩阵，不能由BIZ编号、窗口句柄、其它线程join形状或8110类别推导 |
| BIZ-L2-005-02 v0.2 `控制面板线程::报告一次生命周期投影` | HEAD 92e8b492已发布 `线程生命周期发布端口::发布` 及唯一消息账/当前投影事务，T05目标类与适配调用不存在 | **provider可复用；T05适配未实现** | 具体T05只按真实物理事件形成一次发布请求并原样保留公开载荷矩阵：容量已满/冲突可携带既有当前投影；一次调用不得合并退出前与已退出，消息循环根返回后才由外层wrapper报告物理已退出并发布线程完成见证；不新建消息形成器、第二队列、桥内重试槽或业务失败事实 |
| BIZ-L2/L3/L4-005-03-01 v0.2 业务投影读取链 | HEAD 02e89164不存在控制面板投影服务、业务投影provider集合、逐项读取适配或注册表；8120只冻结世界树、语言材料和四类概念本体锥体投影职责 | **业务职责已定位；provider集合与ABI设计缺口；代码未实现** | 不能从任意领域只读函数自动发现provider，也不能把候选注册表当第二路由权威；后继详细设计须逐项冻结范围、强类型键、版本/完整性、状态载荷及必要分页后才可施工 |
| BIZ-L3/L4-005-03-02 v0.2 `读取线程生命周期投影 -> 读取当前线程投影组` | HEAD 02e89164已发布 `线程生命周期投影读取端口::读取当前线程投影组`；控制面板组合器不存在 | **读取provider已实现；控制面板筛选/组合未实现** | 只按运行代次读当前组并保留逐项当前消息身份；不读取原始消息账、不接受DATA-L1事实截止、不二次归并生命周期 |
| BIZ-L2/L3/L4-005-04 v0.2 控制面板人读显示链 | HEAD 302fb76c不存在控制面板窗口、内容适配端口、人读显示材料形成或显示刷新生产实现；8120未选择WebView、HTML、原生控件、同步/异步或平台消息技术 | **技术中性职责已校正；直接依赖未冻结；代码未实现** | 只允许消费005-03完整值式结果，经005-01选择的同一内容适配端口形成人读材料并在同窗口本地刷新身份内判重；合法空态/具名不可用态可显示，分账来源原样保留。005-03业务provider输出ABI和单一内容适配合同未冻结前不可施工，不得恢复页面片段、HTML转义或`PostMessage`候选为正式目标 |
| BIZ-L2/L3-005-05 v0.2 程序控制停止请求提交链 | HEAD 67e08bf5只有`停止信号租约`和单信号位轮询；程序控制来源拥有体、提交/读取端、不可变首次记录和停止锁存均不存在 | **职责已校正；共享停止来源ABI未冻结；代码未实现** | T05只向001-02创建的同一来源提交强类型停止请求；首次记录与锁存同线性化，精确重复先于既有锁存，读取端非破坏性重读，通知只唤醒且失败不撤销。与001-02/001-10共享的DTO、租约分面、版本、关闭和同步合同未冻结前不可施工，不得实现通用控制类型、第二邮箱、规范化消息或隔离见证 |
| BIZ-L2/L3-005-06 v0.2 控制面板窗口关闭与资源收口链 | HEAD 1c13ace8不存在控制面板窗口、内容适配、关闭完成谓词、资源完成位或唯一资源租约生产实现；8120未选择平台窗口/内容技术 | **技术中性职责已校正；直接适配ABI未冻结；代码未实现** | 只在窗口所有线程结束当前实例呈现；请求投递/不可见/句柄失效不等于完成。成功还须全部创建完成位清空和零剩余租约；任何未完成返回或保留唯一剩余租约。005-01单一窗口/内容适配、完成谓词和资源联合ABI未冻结前不可施工，不得恢复`DestroyWindow`、WebView、页面或控件清单为正式目标 |
| BIZ-L1-T06 v0.4 `外设线程::运行采集循环` | 目标五根逐项对应006-01—05；目标模块/类不存在，启动/收口为空壳。HEAD `85aa5903` 的D455有对象内控制的完整四流采集、设备原时间、宿主接收单调时间、同基准TTL缓存和标量上行桥；每帧采样不接收命令/等待/订阅，无失败分类器、四产品形成、恢复持久边界、强类型报告物理队列、006-05接线、外层wrapper完成见证或6340承接 | **未实现；006-02实例级错误报告作用范围/产品承载另有正式合同缺口** | 创建每实例独占控制域时绑定适配器；006-03直接接管006-01同一不可变引用，用宿主单调基准计算运行期年龄并保持设备原时间，不补造跨时钟owner。后继仍须先由6340/6350闭合实例级错误报告合同，再实现唯一在途槽、四产品/报告、恢复持久边界、物理队列及停止接管；真实生命周期只走006-05 |
| BIZ-L2/L3/L4-006-03 v0.2 时间与报告形成链 | HEAD保存每流设备时间戳/时间域和宿主接收单调时间，`D455帧材料缓存`已用后者计算TTL；没有通用报告组合、四产品形成、报告协议或跨设备/进程/重启强时间合同 | **运行期时间基础可复用；目标职责已校正；物理ABI和产品实现缺失** | 同一进程运行期年龄直接复用宿主单调时间；设备原时间原样保留。只有确需跨基准比较时才等待正式强时间合同；缺失返回材料不足。持续采样报告无需每份具名触发，稳定身份生成规则仍待详细设计 |
| BIZ-L2/L3-006-04 v0.2 报告发布链 | HEAD只有D455原始帧进程内缓存、候选确认/读回和标量运行消息；没有强类型报告协议、四产品物理队列/分类索引、不可舍弃恢复持久边界或任务域正式承接接线 | **局部事务形状可参考；目标职责已校正；恢复owner/ABI与整体实现缺失** | 先复核强类型报告，再按6340分类闭合可恢复强类型报告/承接事实，最后原子发布唯一队列与所属分类索引。D455缓存不是报告队列，标量消息不是恢复见证；持久边界唯一owner和公开读写矩阵须详细设计冻结 |
| BIZ-L2-006-05 v0.1 外设线程生命周期薄适配 | HEAD已发布8110请求/结果和唯一`线程生命周期发布端口`，支持线程与自我线程已有真实调用；外设线程对象和生产接线不存在 | **provider可直接复用；外设薄适配/接线未实现** | 不新增外设专用生命周期DTO、第二队列或重发槽；每次真实物理事件形成一个现行公开请求并调用端口一次。暂不可用不阻塞采集；显式重放只能保留原消息身份和完整请求 |
| BIZ-L2-001-07 / BIZ-L3-001-07-01—02 v0.2 外设组启动与半失败回收 | HEAD组启动恒true、收口为空；模拟线程禁止D455且停止消息不被线程消费；D455工厂/停止和标量桥零生产生命周期接线 | **目标职责方向存在；启动/回收ABI与材料owner未冻结** | 0/N、全部进入后发布、先停全部、正式材料复核、有限连接和租约守恒方向保留；但控制域owner、6340报告交付/恢复owner、T03就绪、001-13接管、预算和矩阵未冻结。缓存空、无时限join或8110均不能证明闭合 |
| BIZ-L2-001-13 `收口并连接外设线程组与任务工作线程池` | HEAD `启动.应用程序.ixx` 中收口函数仍为空壳；`任务工作线程.ixx` 只有旧 `运行结果尾部()`，`停止接收并排空()`只关闭旧结果队列，`收口等待()`直接无时限join；`运行包.任务结果生产.ixx` 先停旧派发、同步连接一个旧工作线程再排空T03 | **目标收口顺序已校正；代码未实现** | T06先冻结新采集并收束门前轮次，T04停止领取并把边界内结果定位交T03；逐线程只认无剩余或专属owner见证，合格项内部等待完成后join，未合格项返还原租约。禁止通用剩余材料owner和用join伪造业务闭合 |
| BIZ-L2-001-08 / BIZ-L3-001-08-01—03 v0.2 `开放/读取/确认/等待自我治理运行门` | HEAD同名无参bool恒真；父函数旁路调用公开void布尔开门。对象只有停门见证读取、布尔门/条件变量和门后T02调用，T02入口可立即因依赖缺失返回且退出结果被丢弃 | **业务顺序有正式依据；目标ABI、资格交付、运行成功谓词与矩阵未冻结** | 只可确认必要模块就绪后独立开门、门前零邮箱消费、同对象短锁协调及有限等待方向。调用前预告不等于函数已进入；越过门/函数体入口/前置通过/首个可消费点须选一，见证生产点、总预算和终态矩阵均为具名DRIFT |
| BIZ-L2-002-01 v0.2 `自我治理批次路由::冻结当前治理批次` | 当前只有 `有界自我治理邮箱`生产实现，以及邮箱冻结、完整秒边界、共享事实截止三个子 provider 的设计/计划；没有父组合路由、父 DTO 或 T02 调用点 | **父组合未实现** | 新建 `海中鱼巣/线程/路由.自我治理批次.ixx`，绑定三个 provider 最终 ABI；只建立邮箱取出后待完成槽、补证和值式交付，不修改 DATA/provider，不接线 T02。T02 v0.1 的停止阶段参数已失效，父线程真实接线属后继 |
| BIZ-L3-002-02-01 v0.3 `运行期只读查询组合器::读取共享截止世界与自我快照` | `L2结构聚合服务`同实例场景/存在 getter；场景整树、成员、宿主正反向；存在角色与完整存在读取均已发布 | **DATA读取能力可复用；BIZ组合模块未实现** | 新建 `海中鱼巣/领域/组合.运行期只读查询.ixx`，全部读取统一使用一个 L1 共享截止。旧节点直接入口、关系21角色1和两个 owner 可异截止语义均退出 |
| BIZ-L3-002-02-02 v0.2 `运行期只读查询组合器::读取生存安全与服务维护当前快照` | 当前无同名 BIZ 组合入口；DATA-EXT-15/16 只有最终需求语义，精确公开 ABI 尚未发布 | **目标合同已冻结；BIZ与DATA均未实现** | 在 WORLD-SELF 结果模块上扩展组合器，并新建 `服务.服务生存治理.ixx`；全部事实读取统一使用父级 G0，DATA只返回事实，BIZ派生控制态、活动和摘要。计划必须待 DATA-EXT-15/16 最终 ABI 与 WORLD-SELF 文件释放 |
| BIZ-L4-002-02-02-01 v0.2 `服务生存治理服务::读取当前服务生存维护快照` | 当前没有 `服务.服务生存治理.ixx`；旧 SERVICE-P1 文档/自检材料不构成生产公开读取实现 | **未实现** | 新服务只注入 DATA-EXT-15/16 const 公开端口，按同一 G0 读取安全根、服务值/游标与四类有界组并形成值式投影；不直达L1、不复用旧私有仓/脚本 |
| BIZ-L4-002-05-01-01 v0.2 `服务生存治理服务::读取生存安全根当前值` | DATA-EXT-15 精确入口尚未发布；现有分层安全模块只处理6140安全层，不是6120/6170生存安全根 | **未实现且禁止错复用** | 待 DATA-EXT-15 发布按自我、安全根、G0读取完整根事实；BIZ验证A/L/H并派生控制态。不得枚举6140安全层或从线程/服务值推断A |
| BIZ-L2-002-05 v0.2 `自我治理裁决组合器::裁决生存控制与现实执行授权前置` | HEAD `服务.L2治理函数骨架.ixx` 仍只有旧名 `裁决生存控制输入`，仅校验合同/批次后固定返回材料缺失；没有同G0控制态与当前冻结组合；HEAD无 `组合.自我治理裁决.ixx` | **实现漂移；旧骨架不是目标能力** | 新建组合器并接入两个v0.2 L3组合；只返回无需授权/需要授权/控制态禁止及结构化非成功，不建立工作项占用、旧许可账或通用执行令牌 |
| BIZ-L3-002-05-01 v0.2 `生存控制读取组合器::读取同G0生存控制态` | HEAD无 `组合.生存控制读取.ixx`；只有目标L4依赖，6120/6170完整根DATA provider仍未发布 | **未实现** | 请求不得携带声明控制态；待DATA-EXT-15后按G0读回A/L/H并唯一派生控制态 |
| BIZ-L3-002-05-02 v0.2 `现实执行授权前置组合器::复核现实执行授权请求与当前冻结` | HEAD无 `组合.现实执行授权前置.ixx`；L2任务公开端口已能按任务读取当前方法选择和当前实例方法 | **底层可复用，BIZ组合未实现** | 组合BIZ-L4-002-05-02-02，同G0逐字段互证T/R/P/E、冻结、实例和动作范围；不读旧许可材料 |
| BIZ-L4-002-05-02-02 v0.2 `任务执行事实提供者::按任务读取当前执行冻结与实例方法` | HEAD `L2任务结构服务::按任务读取当前方法选择` 已返回正式选择、冻结和实例；`按任务读取当前实例方法`已发布；无目标BIZ组合器 | **两项DATA能力可复用；组合未实现** | 新建只读组合器并互证两次截止/实例；不得建立第二当前索引或从来源需求组补造冻结 |
| BIZ-L2-002-10 v0.2 `自我现实执行授权提供者::发布或读取自我现实执行授权` | HEAD无自我现实执行授权owner/provider；工作区未提交候选不计入当前能力 | **未实现** | 只消费002-05需要授权前置，经唯一L2自我治理owner发布/重放/读回；安全硬否决、技术许可和调度资格保持独立 |
| BIZ-L4-004-02-01-01—06 及其 L5 叶 | 物理图和旧映射仍在 | **已退出 BIZ-L3-004-02 当前父图** | 历史需求形成候选，待后续重归属；不得由 T03 当前承接流程调用或作为当前代码施工合同 |
| BIZ-L4-004-02-01-01 v0.3 `需求业务服务::复核需求候选准入` | 当前没有 `服务.需求.ixx`、需求候选准入DTO或统一来源准入服务；完整正差距父合同已冻结但相关BIZ代码仍待实现 | **历史候选；已退出当前父图** | 待重归属，当前不得施工或由 T03 调用 |
| BIZ-L5-004-02-01-01-01 v0.2 `需求来源准入服务::复核需求来源与主体授权` | 正式源码中无 `服务.需求来源准入.ixx`、统一来源provider或当前必需的完整来源专用provider；现有自我治理消息、任务回执和D455材料均明确不是统一权威来源事实 | **v2目标/公共协议已冻结；整体未实现** | 新BIZ真实模块统一导出DTO、provider协议、factory和服务，按版本化强类型来源种类恰一分派；请求、来源对象、授权、拒绝和待补定位必须绑定同一非零父需求。无传统DTO头、无第二DATA服务；具体来源provider、父需求结构读取最终ABI、普通应用装配和01消费全部待后继 |
| BIZ-L4-004-02-01-02 v0.3 `需求业务服务::读取同父同目标当前需求` | 当前只有 `L2需求身份`、已实现但非内容规范化唯一的目标合同 / 比较注册身份和 DATA-EXT-12 服务族边界；最终需求清单尚未登记本叶所需精确组读、完整当前路径关系组及三类预算，也没有需求结构 owner / 服务、`服务.需求.ixx`或本函数 | **v0.2消费设计已修订为多路径组；DATA操作未登记；代码未实现** | DATA-EXT-12 必须按主体、可空父和宿主+特征+完整目标状态语义键在同一G0有界组读；BIZ裁决零/一/多。路径不入键，唯一需求完整返回全部当前路径关系；同父同目标不同路径不得产生第二需求，DATA不得选择活动 / 结算路径。v0.2单路径形状不得施工 |
| BIZ-L4-004-02-01-03 v0.2 `需求业务服务::合并来源与当前路径材料` | 目标 DTO 头 `需求来源路径合并.数据.h`、`服务.需求.ixx`和本函数均不存在；01来源授权见证与02完整结果尚未形成最终代码 ABI | **消费与纯值候选合同已冻结；代码未实现** | 后继只复用02唯一完整需求和全部当前来源 / 授权 / 路径组，结合01准入见证与路径意图形成无需补充、仅来源补充或必须经04的新增路径候选；零DATA调用、零写入、不新增基础数据接口。旧重复读取、DATA内部写集和路径不同即冲突方向退出；01/02最终结果、04防环及父业务闭环均 `NOT_RUN` |
| BIZ-L4-004-02-01-04 v0.2 `需求业务服务::复核父需求路径角色与防环` | 当前没有 `需求父路径准入快照.数据.h`、`服务.需求.ixx`、L2需求结构服务或本函数；0050仍把需求权重完整维度列为未决 | **部分重基线；04根不可施工** | 已拆为04-01中性快照读取、04-02权重分配、04-03角色 / 重叠 / 候选防环。只冻结04-01；04-02/04-03和根DTO必须另行裁决，旧v0.1根见证 / 虚构路径治理读取不得施工 |
| BIZ-L5-004-02-01-04-01 v0.1 `需求业务服务::读取父需求路径准入快照` | 当前只有公共 `L2需求身份`；DATA-EXT-12没有需求服务 / 准入快照DTO / 普通应用getter，目标BIZ合同头、服务和函数均不存在 | **消费合同已冻结；DATA与BIZ代码均未实现** | 待DATA-EXT-12按父需求、非零G0、全部版本和父链 / 直接子 / 路径关系 / 路径组 / 预算来源五预算，一次返回根到父完整链、逐父全部直接子 / 路径，以及请求父需求唯一权重 / 全部预算来源；BIZ完整互证和稳定排序，请求父与祖先缺失分账。零L1 / owner / 拼装 / retry，不输出角色、重叠、候选成环、OR/AND或权重算法结论 |
| BIZ-L4-004-02-01-05 v0.2 `需求业务服务::发布单目标需求结构` | 当前没有 `需求结构发布消费.数据.h`、`服务.需求.ixx`、L2需求结构服务或公开发布ABI；旧 `数据操作.需求.ixx`方向未实现且已冻结 | **公开发布消费合同已冻结；DATA与BIZ代码均未实现** | 05只把上游完整候选转换为DATA中性强类型结构并恰一次调用唯一DATA-EXT-12，回显首次G0、原G1和完整首次请求见证；父L3 v0.4验证首次`G1=G0+1`及精确重复沿用原G0/G1后进入06。当前状态材料、满足关系、路径权重 / 分配 / 预算尚须随04和DATA最终合同补齐；BIZ零L1/owner/事务/撤销，5150回执由父入口统一形成 |
| BIZ-L4-004-02-01-06 v0.2 `需求业务服务::完整读回需求目标来源与路径` | 当前共同头只有 `L2需求身份`；既有运行期事实版本服务仍待BIZ实现，DATA-EXT-12没有需求历史完整读DTO / 服务 / 普通应用getter，`需求结构正式读回.数据.h`、`服务.需求.ixx`和本函数均不存在 | **历史读回消费合同已冻结；DATA与BIZ代码均未实现** | 06先经既有运行期事实版本服务取G读，再以G读守卫和原G1历史截止恰一次调用DATA-EXT-12；成功完整返回目标、当前状态材料/满足关系、可空父、全部来源授权、全部路径权重/分配/预算、生命周期和版本。05见证仅由父级互证，不进入DATA请求；零L1/owner/拼装/重试。计划等待DATA最终ABI和跨叶字段后激活 |
| BIZ-L4-004-11-03-01 v0.3 `需求业务服务::读取需求当前父关系与生命周期` | HEAD已发布 `L2按子需求读取当前父需求请求/结果` 和 `L2需求结构服务::按子需求读取当前父需求`，可在指定事实代次返回可空当前父关系与父需求；BIZ `需求业务服务`、当前子需求与生命周期的完整同G0组合结果尚未实现 | **DATA当前父关系能力可复用；BIZ完整消费组合未实现** | 在同一 `需求业务服务` 中持同实例L2需求结构服务，按需求和非零G0读取完整当前需求、生命周期、可空唯一父关系和同现父端点；父关系叶直接复用已发布L2 ABI。无父合法成功；不得直达L1、分次异截止拼装、历史补缺或从权重/顺序推断父关系 |
| BIZ-L4-004-11-03-02 v0.3 `需求业务服务::读取父链各节点当前活动子关系` | 当前代码仅有共同 `L2需求身份`；DATA-EXT-12、`需求当前活动子关系.数据.h`、`服务.需求.ixx`和本函数均不存在；前两份需求 BIZ 只在待激活设计中冻结 | **消费合同已冻结；DATA 与 BIZ 代码均未实现** | 串行扩展同一 `需求业务服务`并复用同一 DATA-EXT-12 const 公开端口；按根到叶父链、非零 G0、版本和单一总预算一次读取逐父全部当前直接子关系及活动材料，由 BIZ 只按已发布强类型结论选择活动项。逐父筛选后空组合法成功；不得逐父调用 DATA、重判活动、读取历史摘要或从权重 / 任务 / 顺序补活动 |
| BIZ-L3-004-11-03 v0.3 `需求业务服务::读取需求父链与活动路径事实` | 当前没有 `需求父链活动路径.数据.h`或父函数；父关系DATA叶已发布，两个BIZ-L4消费叶仍只在目标图中冻结 | **父组合合同已迁移冻结；代码未实现** | 在同一 `需求业务服务` 内逐项调用父关系叶形成非空无环根到叶父链，再恰一次调用活动子关系叶形成等长同序逐父活动组；复用同一 G0，深度 / 数量预算分账，初始缺失与后继父缺失分账，任何失败零前缀。不得直达 DATA / L1、历史拼装或重判活动 |
| BIZ-L3-002-06-01 v0.2 `需求父关系回流读取组合器::读取核算需求当前直接父关系` | HEAD已发布L2按子需求读取当前父需求ABI；当前没有 `组合.需求父关系回流读取.ixx`、回流专属请求/结果或BIZ包装函数 | **DATA父关系叶可复用；回流组合器未实现** | 只验证一项显式需求核算触发并以紧邻Gstep调用BIZ-L4-004-11-03-01；无父合法成功，失败核算、已退出、漂移、多父/错端点和半结构分账。不得从任务结果、需求列表、缓存或历史关系形成受影响需求集合 |
| BIZ-L2-002-07 v0.2 `自我治理裁决组合器::裁决生存控制与当前具体需求承接后继` | HEAD `L2治理函数骨架.数据.h`仍含旧`执行许可后继`；`服务.L2治理函数骨架.ixx`只校验合同/批次后固定返回材料缺失；`创建.自我线程.ixx`调用该旧骨架 | **实现漂移；目标比较规则另有设计缺口** | 新组合器只消费正式控制态和当前需求业务视图，A=0/1返回控制态，A>1选择一个具体D并形成承接意图；不读取当前任务、调度资格或许可。多候选完整比较规则未冻结前不得实施完整根 |
| BIZ-L3-002-07-01 v0.2 `自我治理裁决组合器::裁决生存控制根后继` | HEAD没有同名纯裁决；当前只能从旧一致快照和6170相邻材料取得候选，旧002-07还混入其它后继状态 | **目标合同已冻结；实现缺失** | 只验证正式A与控制态读回的0/1/>1唯一映射；合同、30%预支和安全门禁不进入请求，也不由本函数产生唤醒 |
| BIZ-L3-002-07-02 v0.2 `选择当前主需求`目标身份 | HEAD已有需求根/父子/列表和运行期结构快照，但没有完整当前活动资格/路径/权重业务视图或主需求比较器 | **正式机器语义缺口；不可施工** | 0050仍将需求权重完整维度列为未裁决；多候选比较维度、优先序、并列规则和稳定同值键必须先冻结。不得用6150/6170调度资格、任务数量、队列或稳定身份补位 |
| BIZ-L3-002-07-03 v0.2 `自我治理裁决组合器::形成当前具体需求任务承接意图` | HEAD旧后继DTO只保存数值身份并含许可状态；没有只绑定具体D及完整幂等信封的强类型承接意图合同 | **目标边界已冻结；实现缺失** | 冻结D、自我、批次、代次、G0、选择见证、输入键、来源关系承接键、准备键和六阶段键组；任务管理后续按D所属L查询0..1任务。自我侧不得查任务、选择创建/复用、形成许可或派生阶段键 |
| BIZ-L2-002-03 v0.4 `自我治理批次路由::复核并路由治理批次` | HEAD路由仍把协议v4九类中的第一类固定为`任务结果复核请求`，并把该分支送入旧任务结果绑定路径 | **目标已改为轮次结算定位；当前实现漂移** | 保持邮箱冻结顺序和其它八类边界，只把第一类迁移为显式T/R/轮次结算身份/G定位；不得从旧结果消息或线程序号补造结算 |
| BIZ-L2-002-04 v0.4 `复核任务轮次结算定位绑定事实` | HEAD `服务.L2治理函数骨架.ixx`及T02仍消费旧任务结果治理输入；没有同名结算定位绑定入口 | **目标合同已冻结；实现缺失且旧路径漂移** | 只处理002-03已分类的结算定位，调用01/02按显式身份复核并独立读回；不读取当前实例方法或动作结果正文 |
| BIZ-L3-002-04-01 v0.3 `任务轮次结算定位复核器::复核任务轮次结算定位` | HEAD没有`协议.任务轮次结算定位.ixx`或同名纯值复核器；旧叶仍绑定已发布的按任务当前实例方法读取 | **目标合同已冻结；实现缺失** | 新增只复核T/R/轮次结算身份/G的强类型协议叶；当前实例读取能力可留给其它消费者，但退出self结算路径 |
| BIZ-L3-002-04-02 v0.4 `运行期只读查询组合器::按显式身份独立读回任务轮次结算` | HEAD没有同名组合入口；现有self接收侧仍按当前实例、执行轮次和任务实际结果组成旧读回路径 | **目标合同已冻结；实现缺失且旧组合漂移** | 按显式结算身份、T/R和同G0读前后守卫读取首次完整轮次结算与轮次已收束阶段；未找到和失败不得退回旧实际结果路径 |
| BIZ-L2-004-14 v0.2 `任务管理上行桥::提交一个任务管理到self具名输入` | HEAD `上行桥.任务实际结果.ixx`从正式任务实际结果直接构造`任务结果复核请求`；未形成四种具名强类型输入的唯一提交入口 | **目标合同已冻结；当前实现漂移** | 迁移为每次只提交轮次结算定位、现实执行授权请求、派生需求三件套或结构化治理缺口定位恰一；合法等待留在T03原槽，桥不建立第二待重发状态 |
| BIZ-L3-004-14-01 v0.2 `任务管理上行输入复核器::复核一个任务管理到self具名输入` | HEAD `自我治理消息协议.ixx`协议v4仍是九类通用variant，任务结果复核载荷和调用方优先级仍在公开消息中 | **目标合同已冻结；协议实现漂移** | 为T03生产边形成四类具名强类型纯值复核；派生需求与结构化治理缺口分账，并拒绝旧任务结果正文、等待消息、线程地址和调用方自定义优先级 |
| BIZ-L3-004-14-02 v0.2 `有界自我治理邮箱::原子提交一个已复核治理输入` | HEAD `有界自我治理队列.ixx`仍按消息优先级插入，未按`{运行代次, 输入幂等身份}`收束T03具名输入 | **目标合同已冻结；队列实现漂移** | 短锁内依次完成幂等、停止、容量、稳定到达顺序和原子入队；幂等查询先于停发，不调用领域服务，不保存桥内重试材料 |
| BIZ-L2-004-02 v0.2 `任务承接组合器::按具体需求承接或复用任务` | HEAD没有`组合.任务承接.ixx`或统一入口；旧初次provider只携带一枚任务键并创建task-owner私有虚拟存在，task结构仍按列表项读取且没有已发布承接具体来源需求/T→D组/正式E/R1公开面 | **目标粗合同已校正；实现缺失且旧实现漂移** | 只消费一个D及原完整幂等信封。0项T按六阶段键分owner建立正式E、T/R1和首态链；1项T只按来源关系承接键新增/重复T→D；每段独立读回。不得恢复旧需求创建、目标等价任务树或私有存在 |
| BIZ-L3-004-02-02 v0.2 `任务承接组合器::读取具体需求及同类任务当前承接` | HEAD能按旧`需求列表项`读取0..1任务，但没有正式T→D组、按锚点新名、逐来源目标互证或同一G0完整组合；运行期组合仍可从列表成员聚合来源 | **目标合同已校正；实现缺失/候选漂移** | 同G0读取D及唯一L、fresh目标、按L的0..1当前T；唯一T必须至少一条正式当前T→D且全部来源目标与新D同义。旧整树、零父根、治理存在拓扑不属于本函数 |
| BIZ-L3-004-02-03 v0.2 `任务承接组合器::提交具体需求任务承接并完整读回` | HEAD新任务融合入口仍是单owner私有存在旧写集；没有来源关系承接公开入口、正式E/R1读回和六阶段分owner恢复 | **两个目标分支均未实现** | 新任务调用准备键+六阶段链；既有任务只写T→D；发布未知保留原信封，成功独立读回T/L/D/E/R1及新任务首态链。旧跨owner统一事务不得施工 |
| BIZ-L4-004-02-02-01—07 | 物理图和旧映射仍在 | **已退出 BIZ-L3-004-02 当前父图** | 旧任务树读取候选；当前必要叶已由02-08—11重建，不得按旧路线施工 |
| BIZ-L4-004-02-02-01 v0.2 `任务业务服务::按来源需求读取当前承接任务组` | 当前只有共同 `L2需求身份`、`L2任务身份`和节点族身份来源合同；无任务结构服务、`当前承接任务组.数据.h`、`服务.任务.ixx`或普通应用装配 | **历史候选；已退出当前父图** | 不得按旧任务树读取路线施工 |
| BIZ-L4-004-02-02-02 v0.2 `任务业务服务::读取各任务完整承接与生命周期事实` | 当前只有共同任务/需求身份和既有目标状态合同、场景身份；无任务结构服务、任务完整承接生命周期组合同头、`服务.任务.ixx`或批量完整读取ABI | **消费合同已冻结；DATA 与 BIZ 代码均未实现** | 扩展02-01同一任务业务服务和同一DATA-EXT-13 const借用，按任务组、G0及任务/来源两总预算恰一次读取全部当前来源、唯一目标、唯一场景和唯一当前生命周期；BIZ验证入口来源绑定和结构完整，零历史、目标等价、当前根、子任务、治理存在、完成或调度判断 |
| BIZ-L4-004-02-02-03 v0.2 `任务业务服务::读取当前子任务拓扑和运行态治理存在` | 当前只有共同`L2任务身份`、`L2存在身份`及相应节点族；无任务结构服务、任务—治理存在关系/资格DTO、当前子任务拓扑治理存在合同头、`服务.任务.ixx`或整树组读ABI；普通存在事实不能证明任务治理资格 | **消费合同已冻结；DATA 与 BIZ 代码均未实现** | 扩展02-01/02-02同一任务业务服务和同一DATA-EXT-13 const借用，按父级已裁决根、G0和节点/关系两总预算恰一次读取完整有根子任务拓扑及逐任务独占治理存在；治理存在复用L2存在身份载体，但必须与任务关系、生命周期和世界成员排除资格共同回显。BIZ防御互证根/单父/全可达/无环/关系数守恒和治理存在唯一不共享，不重复来源读取、逐节点调用或判断当前根资格/阶段/完成/调度 |
| BIZ-L4-004-02-02-04 v0.1 `任务业务服务::读取全部后继任务完整承接与生命周期事实` | 当前无任务结构服务、后继任务完整材料合同头、`服务.任务.ixx`或批量完整读取ABI；02/03也只在待激活设计中冻结 | **消费合同已冻结；DATA 与 BIZ 代码均未实现** | 先互证03完整成功读取结果的状态、回显、固定版本、节点/关系预算、G0和optional快照再排除根，空后继组零DATA调用返回合法空；非空时扩展同一任务业务服务并复用02要求的同一DATA-EXT-13 const批量完整读，一次返回全部非根任务来源、唯一目标/场景/当前生命周期。不得伪造入口来源、逐任务读取、重复根/拓扑/治理存在或作业务裁决 |
| BIZ-L4-004-02-02-05 v0.1 `任务业务服务::读取候选任务当前父关系组` | 当前无任务结构服务、候选任务当前父关系合同头、`服务.任务.ixx`或批量当前父关系ABI；02也只在待激活设计中冻结 | **消费合同已冻结；DATA 与 BIZ 代码均未实现** | 互证02完整成功结果后，扩展同一任务业务服务并只经DATA-EXT-13按候选任务组、同一G0和两预算恰一次读取逐任务可空唯一当前父关系与完整父端点；解除根预选循环，但不选择根、不读取整树/治理存在或裁决目标等价 |
| BIZ-L4-004-02-02-06 v0.1 `任务来源目标事实提供者::读取候选任务全部来源需求目标材料组` | 当前无需求结构服务、需求业务服务、任务来源目标合同头或provider；02与BIZ-L4-004-11-03-01均仅在待激活设计中冻结 | **消费合同已冻结；DATA 与 BIZ 代码均未实现** | 新建只持同一需求业务服务const借用的provider，互证02后对全部来源需求稳定去重，逐唯一需求以同一G0恰一次复用单需求当前完整读取并投影目标宿主/特征/合同引用；不新增DATA批量入口，不读取目标正文、裁决等价或选择根 |
| BIZ-L4-004-02-02-07 v0.1 `任务目标等价裁决服务::复核候选任务与全部来源需求目标等价` | 当前无候选任务目标等价合同头、任务目标等价服务或目标定义可调用provider；02、06、TARGET-STATE-CONTRACT-DEFINITION-CONSUME和共享目标状态语义键均只在待激活设计中冻结 | **消费合同已冻结；BIZ代码及所需上游ABI均未实现** | 新建只持需求目标事实提供者const借用的任务目标等价服务，以同一G0对D个来源与T个任务目标各正式读取一次定义，按`L2存在身份目标宿主 + L2特征定义身份 + 唯一共享目标状态语义键`逐任务全量互证；不调用比较特征、不读当前世界值、不新增DATA接口、选择根或复制03-01写侧闸门 |
| BIZ-L4-004-02-02-08 v0.1 `任务承接组合器::读取具体需求及唯一当前列表项` | HEAD已有需求身份/成员/列表项DTO及旧`读取需求成员列表`候选，但没有`组合.任务承接.ixx`、本强类型结果或同G0的需求核心+身份来源+唯一归属完整组合 | **目标合同已冻结；组合未实现** | 只从D回源恰一当前成员关系和L；零/多归属内部不一致，不枚举L其它成员 |
| BIZ-L4-004-02-02-09 v0.1 `L2任务结构服务::按同类任务查询锚点读取当前任务` | HEAD只有旧名`按需求列表项读取当前任务`，其当前锁内实现未形成正式新名/G0后置合同 | **相邻旧能力存在；目标ABI未实现** | 新入口按L严格0/1/>1并回显G0；不得把旧名编译通过当作正式目标完成 |
| BIZ-L4-004-02-02-10 v0.1 `L2任务结构服务::按任务读取来源需求关系组` | HEAD任务数据/服务没有该公开请求、结果或实现；现有正式选择中的来源需求组不是任务当前T→D权威 | **正式规范已冻结；代码未实现** | 由task owner同G0枚举0..N正式T→D并规范排序；禁止从列表成员或执行选择需求组补造 |
| BIZ-L4-004-02-02-11 v0.1 `任务来源目标事实提供者::逐项读取任务来源需求当前目标材料` | HEAD无同名provider、DTO或普通应用装配；旧候选图按多任务/候选关系读取且已退出当前父图 | **目标合同已冻结；代码未实现** | 每条正式T→D都须在同G0回源D/目标/宿主材料，缺一项零部分成功；父级再逐项裁决同义 |
| BIZ-L4-004-02-03-01—05 | 物理图和旧映射仍在 | **已退出 BIZ-L3-004-02 当前父图** | 旧目标等价任务事务候选；不得恢复治理私有存在或跨owner统一事务 |
| BIZ-L4-004-02-03-06 v0.1 `需求初次筹办准备提供者::形成唯一任务与初次筹办工作包` | HEAD同名函数仍是合同v1、单任务建立键、task-owner私有虚拟存在与旧融合写集；工作树候选不属于已发布基线 | **函数存在但语义严重漂移；目标v2未实现** | 按原六阶段键分owner形成正式E、T/R1、阶段定义/实例、首态、P1/A1和首迁移；每段未知只原键收敛，最终独立读回完整工作包 |
| BIZ-L4-004-02-03-07 v0.1 `L2任务结构服务::承接具体来源需求` | HEAD无请求/结果/服务入口和T→D持久关系公开面 | **正式规范已冻结；代码未实现** | 既有任务只以独立关系键新增或精确重复一条T→D，不重建其它任务结构 |
| BIZ-L4-004-02-03-08 v0.1 `任务承接组合器::完整读回既有任务新增来源承接` | HEAD无`组合.任务承接.ixx`和正式T→D/T→E/R1联合读回；旧虚拟存在读取不能证明E | **目标合同已冻结；代码未实现** | 首次提交按G1、精确重复按当前守卫Gread完整读取T/L、当前T→D、T→E/E来源和R1；禁止旧G1/当前值混拼 |
| BIZ-L2-004-11 v0.2 `运行期只读查询组合器::完整读回任务承接与需求活动路径` | HEAD有`组合.运行期只读查询.ixx`及分散任务/选择读取，但无本根函数；正式T→D/T→E/当前任务轮次、ARCH-L2阶段与需求活动路径尚未形成完整生产组合 | **目标粗合同已校正；实现缺失** | 只以T和G0同截止读取正式任务核心/阶段、非空T→D、逐需求目标/活动路径及阶段可空材料；旧目标副本、治理存在/状态、列表成员和线程缓存不得参与 |
| BIZ-L3-004-11-01 v0.2 `运行期只读查询组合器::读取任务完整当前治理材料` | HEAD可复用`读取任务`、`按任务读取当前方法选择/实例方法`；无正式T→D/T→E/当前轮次生产ABI，也无阶段完整空见证与具名等待组合 | **目标组合合同已冻结；DATA与BIZ实现缺失** | 复用004-02-02-10/11并组合T/L/T→E/E/当前R/ARCH-L2阶段及阶段材料；活动任务空T→D为内部不一致，筹办前正式空选择为完整形状 |
| BIZ-L3-004-11-02 v0.2 `运行期只读查询组合器::联合复核任务需求互证` | HEAD没有同名module-private纯值函数；旧目标图仍比较任务目标副本并混入通用授权/等待后继 | **目标纯值合同已校正；实现缺失** | 双向精确互证T→D与逐需求路径组，任务目标只由具体需求目标同义投影形成；不访问仓库、不重判父等待、不合并来源预算/权重 |
| BIZ-L4-004-11-01-01 v0.2 `运行期只读查询组合器::读取任务核心正式存在当前轮次与主阶段` | HEAD有`读取任务`和通用存在/状态读取；没有已发布`读取任务正式存在关系`、`读取任务轮次`生产实现或本同G0组合 | **目标BIZ读取叶已冻结；DATA前置与组合缺失** | 经公开task/存在/特征/状态服务读取T/L、唯一T→E/E来源、当前R及E上ARCH-L2当前阶段；旧虚拟存在和任务治理状态不得补齐 |
| BIZ-L4-004-11-01-02 v0.1 历史`枚举任务全部当前来源需求` | 旧图把来源、目标和需求回源再建一套读取 | **已退出当前父图** | 完整当前T→D唯一复用BIZ-L4-004-02-02-10 v0.1；不得恢复第二来源读取或从L成员补造 |
| BIZ-L4-004-11-01-03 v0.2 `运行期只读查询组合器::按主阶段读取可空选择冻结与具名等待事实` | HEAD已发布当前选择/实例读取，但没有覆盖全部主阶段的0项完整空见证、具名等待事实统一读回和本组合 | **目标阶段材料叶已冻结；实现缺失** | 阶段由ARCH-L2输入；待筹办空选择、等待具名事实、冻结/执行非空材料和收束退出当前分账。读取失败不得冒充正式空或合法等待 |
| BIZ-L2-004-12 v0.2 `任务等待回流重判组合器::按单项具名回流重判受影响任务` | HEAD已有`任务子目标回流重判提供者::重判父需求并准备筹办`，但它从旧子目标承接记录的父需求/列表项读取或创建任务，并直接提交旧后继筹办准备记录；T03无目标组合调用 | **目标粗合同已校正；实现语义严重漂移** | 只消费正式单项回流与004-11同G0快照，返回不适用/保持等待/目标已满足/同轮后继/阶段不允许。保持等待零写；不得恢复任务父子、私有虚拟存在、旧治理状态或自建联合事务 |
| BIZ-L3-004-12-01 v0.2 `复核单项具名回流与受影响任务绑定` | HEAD旧provider直接从父需求和需求列表项推导当前任务，没有正式调用槽/等待槽与T/R同G0绑定纯值函数 | **目标纯值合同已校正；实现缺失** | 正式上级调用只取发起调用/已结算槽显式T/R；需求活动与其它等待必须绑定当前具名槽。消息、子结束枚举和父需求关系均不能补位 |
| BIZ-L3-004-12-02 v0.2 `计算受影响任务当前活动阻塞项` | HEAD旧provider不读取004-11完整活动路径/具名等待全集，也不重算0..N当前阻塞项 | **目标纯值合同已校正；实现缺失** | 只对同G0正式材料重算，历史/退出项不再阻塞；0项不等于目标满足，不产生写入 |
| BIZ-L3-004-12-03 v0.2 `形成同任务轮次后继筹办来源` | HEAD直接提交旧`L2任务后继筹办准备记录事实`，仍携带任务虚拟存在、需求列表项和旧触发枚举；没有5230判别联合 | **目标纯值合同已校正；旧实现漂移；早期等待来源有规范DRIFT** | 只形成5230强类型来源，实际选择/冻结复用004-03/task owner。5210/5220早期等待无旧路径/实例与5230必填冲突未裁决时返回材料不足，禁止空身份补造 |
| BIZ-L3-004-12-04 v0.2 `比较受影响任务当前目标事实` | HEAD旧provider能调用`裁决需求当前满足`，但只针对父需求，并仍通过列表项/当前任务旧结构串联；没有004-11完整T→D同义目标投影适配 | **相邻比较能力可复用；目标组合未实现** | 从004-11逐需求同义目标投影读取当前事实并复用004-01注册比较；已满足只形成任务治理输入，需求结算仍走004-06 |
| BIZ-L4-004-12-03-01—05 v0.1 | 只有历史目标图，没有对应HEAD生产函数；其职责是父任务等待联合候选准备/确认/撤销和发布见证 | **已退出004-12 v0.2当前调用树** | 保持等待零写；目标、选择/冻结、阶段和需求结算分别由既有owner处理，不新建第二事务族 |
| BIZ-L2-004-13 v0.2 `任务主阶段治理组合器::裁决任务下一工作阶段` | HEAD没有本组合器；manager仍按旧重筹办槽推进，治理骨架把控制态/权限与阶段复核混在一个未激活入口 | **目标阶段路由已校正；实现缺失且旧路径漂移** | 只消费004-11/12同G0值式材料，以正式E上的ARCH-L2阶段形成初次/同轮筹办、待普通派发、授权、等待、零动作或保持当前阶段路由；A00纯值且不产生许可结论，控制态/调度归003-03 |
| BIZ-L3-004-13-01—03 v0.2 | HEAD无三个同名module-private纯值函数；旧目标004-13-02还要求另建冻结读取，004-13-03在阶段内选择最高权限来源 | **目标纯值子树已重建；实现缺失** | 生命周期与主阶段分账；阶段材料只复核004-11快照；工作路由不读取仓库、队列、控制态、调度资格、技术许可或硬否决 |
| BIZ-L4-004-13-02-01 v0.1（文件已删除） | HEAD没有对应生产函数；其旧目标职责与004-11阶段材料读取、003-03普通派发及004-07动作前fresh复核重叠 | **已退出004-13 v0.2当前调用树** | 不新增第二冻结组合；选择/实例/冻结读取复用004-11，派发和动作前材料分别归003-03/004-07 |
| BIZ-L2-004-17 v0.2 `任务零动作收束提供者::形成合法无执行短路结果并收束当前任务轮次` | HEAD无合法无执行短路结果DTO、同一任务结果owner互斥分支入口或T03生产调用；现有004-08只消费T04筹办/执行定位；正式3200/5090仍残留旧全部T→D/分配口径 | **用户业务口径已裁决；正式规范与物理ABI待同步；实现缺失** | 只消费004-13筹办期零动作路由；先发布强类型短路分支，再经004-08-03 v0.3分账收束、004-08-04 v0.2合法零动作返回/槽结算和004-08-05 v0.2最小self定位 |
| BIZ-L3-004-17-01 v0.2 `任务零动作结果提供者::发布或读取合法无执行短路结果` | HEAD没有零动作结果事实、幂等账或独立读回；缺完成消息不能证明零动作 | **业务步骤已裁决；同一任务结果owner互斥分支ABI待设计** | 必须正式证明尚未执行；实际执行/合法短路同一T/R恰一；来源G0与owner提交/读回截止分账，提交后读回失败保留见证；不写需求满足、不建第二owner |
| BIZ-L3-002-01-03 v0.2 `读取当前已发布共享事实截止` | `L2存在结构服务::读取当前事实代次()`已公开并直接读取同一 L1 当前事实代次；聚合服务已有存在 const getter | **DATA能力可直接复用；BIZ包装未实现** | 新建 `海中鱼巣/领域/组合.运行期事实版本.ixx`，只绑定运行代次/范围并复核非零截止；不新增 DATA-EXT-17、owner 向量、provider 组或第二见证 |

## 7. 任务执行普通派发选择已展开首组L4、DATA ABI与实现待闭合

| 目标函数 | 当前代码候选 / 稳定编号 | 当前代码图 | 对应状态 | 裁决与证据 |
| --- | --- | --- | --- | --- |
| BIZ-L2-003-03 v0.4 `任务普通派发选择组合器::选择一个冻结后当前可普通派发任务` | HEAD只有 `服务.L2治理函数骨架.ixx` 中字段不闭合且固定返回材料缺口的旧 `计算任务执行权限`；没有新组合器、中性当前任务全集/集合版本provider、逐任务BIZ候选读取或T03生产调用 | 正式图谱无对应身份或图 | **目标L2/L3及必要L4已校正；实现缺失且旧骨架漂移** | 只在执行冻结后、真实派发前按05—10读取完整候选、根调度和逐来源fresh材料并选择0..1项；旧T02全局权限入口不得改名冒充 |
| BIZ-L3/L4-003-03 v0.1旧子树 | HEAD存在安全权限纯值算法和旧协议DTO，但不存在旧图声明的服务文件 / provider闭环，也未绑定新父图完整候选、共同场景和Gnow | 正式图谱无对应身份或图 | **历史候选；已退出当前父图** | 只作05—10实现复用证据；不得自动改挂旧四叶或把固定材料缺口骨架视为实现 |
| BIZ-L3-003-03-05 v0.2 `读取当前待普通派发冻结候选组` | HEAD无中性当前任务全集与任务集合版本公开入口，也无逐任务ARCH-L2阶段/冻结/排队组合读取；任务管理本地槽不构成全集 | 正式图谱无对应身份或图 | **目标合同和两项L4已冻结；DATA ABI与实现缺失** | 先由05-01返回中性0..N当前任务全集与集合版本，再由05-02逐任务形成BIZ候选；不得让task owner判断业务阶段或遍历内部容器绕过 |
| BIZ-L4-003-03-05-01 `L2任务结构服务::读取当前任务身份全集` | HEAD无当前未退出任务0..N完整枚举、完整覆盖见证或任务集合版本的公开读取ABI | 正式图谱无对应身份或图 | **目标边界已冻结；专属DATA ABI仍漂移** | `DRIFT-DATA-L2-TASK-CURRENT-SET-VERSION-AND-COMPLETE-ENUMERATION-ABI-UNFROZEN`；不得用Gnow、数量、哈希、线程计数或内部索引冒充集合版本与全集 |
| BIZ-L4-003-03-05-02 `任务普通派发候选事实提供者::读取一个任务的当前普通派发候选事实` | HEAD有部分任务选择/冻结与排队读回能力，缺任务核心/正式存在/当前轮次、ARCH-L2当前阶段及排队空见证的同Gnow组合 | 正式图谱无对应身份或图 | **目标BIZ读取叶已冻结；实现缺失** | 复用既有选择冻结实例和排队事实读取，业务阶段只由正式任务存在上的ARCH-L2当前选择裁决；兼容旧治理状态和线程槽不得参与 |
| BIZ-L3-003-03-06 v0.2 `读取同Gnow生存控制与根调度快照` | HEAD已有 `读取生存安全根当前值` 的目标叶所映射底层读回片段和6170规则，无本module-private组合器 | 正式图谱无对应身份或图 | **目标合同已冻结；可组合复用** | 复用正式A/L/H、控制态和维护来源读取，并在本函数内按6170纯值派生根配额；不接受调用方声明控制态或根配额 |
| BIZ-L3-003-03-07 v0.2 `读取一个冻结候选的当前来源调度材料` | HEAD有部分任务、需求和安全算法；缺任务当前基础优先级公开读取及目标/方法/路径/冻结共同作用场景的同Gnow读回接线 | 正式图谱无对应身份或图 | **目标子图已按6150 v0.12/5230 v1.6校正；实现缺失** | 只消费冻结本轮实际T→D，逐来源保留关系身份和版本；共同作用场景只能由目标/方法约束与路径/冻结正式字段互证，不得用需求列表、动作目标、自我位置或公共祖先补造 |
| BIZ-L4-003-03-07-01 v0.1 `L2任务结构服务::读取任务当前基础优先级` | HEAD无任务基础优先级当前正式读取ABI、特征映射或来源版本联合读回 | 正式图谱无对应身份或图 | **目标DATA读取边界已冻结；专属ABI仍漂移** | `DRIFT-DATA-L2-TASK-BASE-PRIORITY-CURRENT-READ-ABI-UNFROZEN`；B为非负I64基础排序值，不含安全倍率、根配额、等待时长或队列位置 |
| BIZ-L4-003-03-07-02 v0.1 `任务普通派发选择组合器::读取候选当前安全来源调度材料` | HEAD有安全因果分层和五级权限计算片段，但无共同作用场景字段贯穿、目标/方法/路径/冻结同Gnow互证及场景成员读回实现 | 正式图谱无对应身份或图 | **目标图已校正；实现缺失** | 现行权限键只可原样使用正式共同作用场景；禁止从动作主体、间接结果、观察范围、自我位置、首项或公共祖先推导 |
| BIZ-L3-003-03-08 `形成一个冻结候选的fresh调度投影` | HEAD有安全类局部纯值算法，缺安全/服务根配额合并和完整候选形状 | 正式图谱无对应身份或图 | **目标纯值合同已冻结；实现缺失** | 复用前逐字段核对；安全/服务来源、多来源取最大、零资格和材料缺口分账 |
| BIZ-L3-003-03-09 `从完整候选投影选择零或一个普通派发任务` | HEAD旧算法未覆盖当前候选全集、服务来源、组5完整同值和0..1结构化结果 | 正式图谱无对应身份或图 | **目标纯值合同已冻结；实现缺失** | E/Q/B及适用的6150链确定全序；队列顺序、版本、等待时长不参与 |
| BIZ-L3-003-03-10 v0.3 `复核普通派发选择当前性` | HEAD无派发前中性任务全集、逐任务候选、根快照、基础优先级、共同作用场景和全部安全/服务版本的整体读后守卫 | 正式图谱无对应身份或图 | **目标图已按现行机器语义校正；实现缺失** | 整体重放05—07；任务加入/退出、阶段/冻结/排队、根事实、B、共同作用场景互证见证、T→D或任一来源版本变化都使整份选择失效；只重读首项不足 |

本链的T03、L2、L3-07/10和L4-07-02已经按6150 v0.12/5230 v1.6重基线：共同作用场景由目标/方法约束与路径/冻结字段同Gnow互证，不再等待独立producer。代码仍未实现，且当前仍缺任务全集/集合版本DATA ABI、任务基础优先级DATA ABI，以及共同作用场景字段贯穿和同Gnow读回。L3-06继续复用现有生存安全根读取叶，08/09纯值算法不恢复旧T02全局权限链。

## 8. 选择性继续分解审计

以下函数是当前 L3 中“没有 BIZ-L4 引用且含两个以上函数调用”的全部候选。调用数量只用于发现隐藏过程，不自动要求下钻；裁决必须结合复用关系、平台边界、正式依赖和未决机器语义。

| L3 目标函数 | 调用形态 | 继续分解裁决 | 理由 / 后继 |
| --- | --- | --- | --- |
| BIZ-L3-001-08-02 v0.2 `自我线程::确认治理运行门` | HEAD只有公开无参void布尔开门；旧图设想两次读取和版本化幂等事务 | 设计门禁、不可裁决 | 父协调者的必要模块就绪结论必须经最小不可伪造资格交给同一对象owner；确认入口、重复/提交/读回/唤醒矩阵须与08-01共同冻结，不能复制全部上游DTO或由复用表反向授权 |
| BIZ-L3-001-08-03 v0.2 `自我线程::等待进入治理循环` | HEAD门后直接调用T02，无运行成功见证；旧图把调用前预告命名为已进入 | 设计门禁、不可裁决 | 必须先冻结越过门、函数体入口、运行前置通过或首个可消费点中的唯一成功谓词，再由真实代码点生产见证并按父级总预算等待；8110、消息出队或调用前预告均不能替代 |
| BIZ-L3-003-01-03 v0.2 `形成安全维护外部事实参与包` | 形成后值、后状态、状态迁移动能、被动动作致变动态和关系候选 | 目标已闭合，代码未实现 | 调用方正式稳定主键退出；同一写集四本地键互异，动作动态角色8只引用同源迁移动能，正式编码只由首次G1发布映射取得 |
| BIZ-L3-004-01-02 v0.2 `特征判断收束服务::按注册比较合同裁决目标差距` | `L2特征结构服务::比较特征`已实现并退出；当前没有 `目标差距比较.数据.h`、`服务.特征判断收束.ixx`或 BIZ wrapper | **DATA统一比较可复用；BIZ消费合同已冻结、代码未实现** | 新 provider-only 服务只持同实例特征服务，使用完整父组合结果构造目标判断、左当前事实 / 右目标状态和结果位6请求，恰一次调用 DATA 后按低1 / 等2 / 高4与允许关系位裁决。旧注册预读、算法直调、排序三态和简化比较 DTO 退出；父组合、普通应用持有 / getter / 装配及父正差距闭环均 `NOT_RUN` |
| BIZ-L2-004-01 v0.2 `需求目标裁决服务::确认当前正差距` | 两个 BIZ-L3 子合同已冻结但均未实现；当前没有 `需求正差距.数据.h`、`服务.需求目标裁决.ixx`或同名生产函数 | **父组合合同已冻结、代码未实现** | 新组合服务从同一 L2聚合按值形成目标事实 provider和判断收束服务，同一 G0 恰两次 BIZ调用并发布完整当前事实 / 比较证据。函数无需求身份且允许需求尚未建立，旧 `需求业务服务`、世界快照身份和骨架路径退出；普通应用持有 / getter / 装配、T02接线和需求建立均 `NOT_RUN` |
| BIZ-L3-004-07-03 `方法动作执行路由::调用冻结具体方法动作入口` | 读取登记入口 + 调用具体动作 | 按登记动作按需下钻 | 通用路由已经闭合；只有正式登记且内部非平凡的具体动作才各自形成L4，不预造默认动作函数 |
| BIZ-L2-005-01 v0.2 `控制面板窗口端口::创建并显示控制面板窗口` | HEAD 5bcc121d只有`按模式启动控制面板线程(启动模式)`的bool空壳；普通控制面板模式仍调用`运行无窗口宿主`；无控制面板线程、窗口或界面目录 | **目标函数未实现；相邻启动空壳不构成部分实现** | 目标只建立并显示UI载体，不读写业务事实；8120未选择WebView等具体内容技术，后继详细设计必须选择单一适配实现 |
| BIZ-L3-005-01-01 v0.2 `控制面板窗口端口::创建窗口运行载体` | HEAD无窗口类注册、宿主窗口、内容呈现或交互资源生产代码 | **目标函数及三个BIZ-L4叶均未实现** | 已冻结线程亲和、稳定实例、重复、完整候选确认和部分失败逆序回收；内容资源叶保持技术中性，不能按旧图直接施工WebView环境/控制器 |
| BIZ-L3-005-01-02 v0.2 `控制面板窗口端口::显示窗口` | HEAD无该项目函数；`ShowWindow` + `UpdateWindow`只属未来Windows平台叶子 | **目标函数待新增** | 实例、线程亲和、幂等和返回语义已在目标图闭合；不为平台API生成项目目标函数图，也不冒充当前实现 |

本次审计后，当前没有可在不新增机器语义、不重复现有函数身份且不等待正式提供者设计的其它 L3→L4 必画项。后续继续分解由新规范、正式登记动作、COMPARE-R设计进入或当前函数新增非平凡调用触发，而不是按层级编号触发。

## 9. 使用方法

每完成一条目标业务链，按以下顺序追加：

```text
目标函数完整签名
-> 查询能力目录取得候选
-> 回读当前正式源码和代码流程图
-> 比较输入、输出、事实来源、线程、事务、非成功和版本
-> 裁决一致 / 部分实现 / 未实现 / 实现漂移 / 证据待刷新
-> 只对仍隐藏非平凡过程的目标函数继续分解
```

本表不是代码实施许可。代码修订后由代码流程图维护智能体刷新当前事实图谱，再由设计或验收侧复核对应状态；不得直接把“未实现”改成“一致”。
