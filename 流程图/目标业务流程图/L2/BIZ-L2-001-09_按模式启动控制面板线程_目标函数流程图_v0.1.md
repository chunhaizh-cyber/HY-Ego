# BIZ-L2-001-09 按模式启动控制面板线程目标函数流程图 v0.1

更新时间：2026-08-28

## 依据

```text
0050、8100、8110 v0.3、8120 v0.10；目标线程归属与跨线程材料；BIZ-L1-T01 v0.4、BIZ-L1-T05 v0.4；BIZ-L2-001-02、001-08
```

## 身份与边界

正式目标治理图。无窗口常驻模式不创建窗口；普通模式必须运行控制面板窗口。8120只把窗口运行和无窗口等待归于程序运行宿主，没有冻结普通模式必须创建独立T05线程。主宿主/独立线程、窗口owner、线程亲和和消息循环生命周期未裁决前，本根不能固定为“启动控制面板线程”。

## 函数合同

```text
根函数候选：按模式进入控制面板运行宿主
归属模块 / 文件 / 目标分解深度：程序运行宿主 / 物理文件待拓扑裁决 / BIZ-L2
现有或待新增：目标根、请求/结果 DTO、唯一 T05 租约和候选回收结果均未实现；HEAD `180ea484` 的同名函数位于启动.应用程序.ixx，只按模式返回 bool，不创建线程或窗口
完整签名：未冻结；旧控制面板线程启动请求/结果只作独立线程方案草图
参数：共同字段至少含已接受模式、非零运行代次和BIZ-L2-001-08同代次完整成功结果；普通控制面板分支还必须含唯一逻辑线程/窗口身份、具名业务只读投影端口组、001-02同代次安装结果中借用的程序控制消息提交端、唯一8110发布/读取端口、线程亲和窗口端口和非零单调启动时限，全部借用覆盖返回租约；逐字段ABI仍由详细设计冻结
返回结果：移动值式控制面板线程启动结果；无窗口为无需启动且空租约，普通模式唯一成功含 T05 唯一租约和窗口就绪/消息循环进入见证；非成功必须区分零候选、已完整回收和仍需 T01 正式收口，并返还全部未连接唯一租约
被调函数流程图：[BIZ-L3-001-09-01 v0.2](../L3/BIZ-L3-001-09-01_启动控制面板线程入口_目标函数流程图_v0.2.md) 已校正为拓扑设计门禁；09-02/03仍为仅独立线程方案适用的旧草图
```

## 流程图

```mermaid
flowchart TD
    N00{"N00【指令-判断】控制面板执行拓扑、窗口owner、宿主接口和失败矩阵是否正式闭合"} -->|否| N15["N15【指令-返回】登记设计漂移并停止本根施工"]
    N00 -->|是| N01
    N01{"N01【指令-判断】模式、运行代次和001-08成功结果是否完整同代次"} -->|否| N10["N10【指令-返回】返回请求拒绝或前置未闭合；空租约"]
    N01 -->|是| N02{"N02【指令-判断】模式是否为无窗口常驻"}
    N02 -->|是| N08["N08【指令-返回】返回无需启动；不创建隐藏窗口或T05线程"]
    N02 -->|否| N03{"N03【指令-判断】是否为普通控制面板且全部T05身份/端口/启动时限完整"}
    N03 -->|否| N10
    N03 -->|是且选择独立线程| N04["N04【指令-构造】按冻结合同形成唯一T05候选和统一单调截止"]
    N03 -->|是且选择主宿主窗口循环| N16["N16【函数调用】在程序主宿主进入冻结的窗口运行入口"]
    N04 --> N05["N05【函数调用】启动控制面板线程入口(候选请求) -> 候选启动结果"]
    N05 -->|无候选非成功| N11["N11【指令-返回】返回原启动非成功；空租约"]
    N05 -->|已创建候选并返还租约| N06["N06【函数调用】读取控制面板线程进入见证(候选租约, 剩余时限) -> 进入结果"]
    N05 -->|非成功但返还候选租约| N12["N12【函数调用】停止并连接控制面板线程启动候选(移动租约, 剩余时限) -> 回收结果"]
    N06 -->|已进入| N07{"N07【指令-判断】见证是否同线程/窗口/代次且确证窗口就绪与消息循环资格"}
    N07 -->|是| N09["N09【指令-构造并返回】返回控制面板已启动、唯一租约和原值进入见证"]
    N07 -->|否| N12
    N06 -->|超时/窗口失败/线程故障或退出/内部非成功| N12
    N12 -->|已完整回收| N13["N13【指令-返回】返回原启动非成功和回收见证；空租约"]
    N12 -->|未完整回收并返还租约| N14["N14【指令-返回】返回启动失败、精确回收状态和待T01正式收口租约"]
```

## 节点合同

| 节点 | 类型 | 输入 | 输出 | 事实读写 | 验证 |
| --- | --- | --- | --- | --- | --- |
| N01—N03 | 指令-判断 | 共同请求及按模式分支材料 | 准入或无需启动 | 无 | 错代、坏模式、无窗口零UI依赖、普通模式缺端口 |
| N04—N07 | 构造 / 函数调用 / 判断 | T05候选请求和统一截止 | 唯一候选、内部进入结果 | 只取得本地线程/UI资源；8110只作旁路 | 候选创建失败、窗口失败、进入超时、生命周期发布失败不冒充启动失败 |
| N08—N14 | 返回 / 函数调用 | 启动或回收结论 | 无需启动、完整成功、零候选失败、已回收失败或待收口租约 | 不写业务事实，不丢失joinable线程 | 两模式、每个提交边界、候选租约守恒 |

## 关键边界

- 无窗口常驻仍由001-02建立未锁存的程序控制停止来源和故障观察端口，但001-09不借用UI专属提交端、不创建隐藏窗口、不形成伪T05见证。
- 普通控制面板启动成功只由T05对象内部发布的“窗口已创建显示且消息循环已取得消费资格”见证证明。窗口句柄、页面文本、父函数bool、8110当前投影或生命周期消息都不能替代该见证。
- 001-08成功结果是本函数的直接启动前置；它已经闭合更早同代次线程准备。本函数不重新解释支持、T03、T04、T06或自我门的原始材料，也不从线程投影补造它们。
- 一旦物理线程候选创建成功，任一后继非成功都必须先尝试停止和连接；未连接时把唯一租约返还T01并进入共同收口，禁止析构joinable线程、detach或用空失败结果遗失资源。

## 当前代码对应（HEAD `655a2bf3`）

- `海中鱼巣/启动.应用程序.ixx` 的 `bool 按模式启动控制面板线程(启动模式)` 对无窗口返回true，对普通模式也只比较枚举并返回true；没有请求、运行代次、001-08结果、结构化状态、租约或进入见证。
- 同文件普通控制面板与无窗口路径都调用 `运行无窗口宿主`；HEAD没有 `海中鱼巣/线程/控制面板线程.ixx`、界面目录、窗口实现或程序控制消息桥。
- 已发布 `线程生命周期投影服务` 支持控制面板线程类别及当前单项/组读取，但它只可复用为8110诊断旁路，不能成为候选所有者、窗口进入见证或启动成功依据。
- `DRIFT-BIZ-CONTROL-PANEL-EXECUTION-TOPOLOGY-WINDOW-OWNER-AND-THREAD-AFFINITY-UNFROZEN`闭合前，本根及09-02/03不可交代码施工。
