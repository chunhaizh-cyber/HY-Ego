# BIZ-L2-003-02 维护服务与生存安全目标函数流程图 v0.1

更新时间：2026-08-03

## 依据

```text
6120、6160、6170
父函数：BIZ-L1-003 自我治理服务::执行一轮自我治理
```

## 身份与边界

正式目标函数设计图。根函数按完整单调秒原子维护服务合同、服务值和生存安全根值；不消费 6140 分层安全算法。

## 函数合同

```text
根函数：维护服务与生存安全
归属模块 / 文件 / 层级：服务生存治理 / 海中鱼巣/领域/服务.服务生存治理.ixx / L2
现有或待新增：待新增；当前代码中目标完整签名零命中，底层相邻能力仍待核
完整签名：服务生存维护结果 服务生存治理服务::维护服务与生存安全(const 服务生存维护请求& 请求)
参数：
  请求：const 服务生存维护请求&，只读借用，调用期间有效；必须携带自我、运行代次、时间纪元、完整秒区间、服务值、安全根、合同/进展集合和规则版本
返回结果：
  服务生存维护结果；包含已结算完整秒、合同结算、服务值、安全根、控制态、衰减段和发布身份，或无完整秒、时间缺口、拒绝、精确重复和内部错误
被调函数流程图：下一层待按合同结算、衰减计算、安全根回归和全参与者事务发布分别展开
```

## 流程图

```mermaid
flowchart TD
    N01["N01【指令-判断】时间纪元、当前值、合同和规则版本是否完整"] -->|"否"| N17["N17【指令-返回】返回维护请求拒绝或时间证据缺口"]
    N01 -->|"是"| N02["N02【指令-计算】计算待结算完整秒区间"]
    N02 --> N03{"N03【指令-判断】N是否为0"}
    N03 -->|"是"| N18["N18【指令-返回】返回无完整秒变化"]
    N03 -->|"否"| N04["N04【指令-分段】按事实秒、合同终态、30天和比例档位切分区间"]
    N04 --> N05["N05【函数调用】处理服务合同结算(当前段) -> 合同结果"]
    N05 --> N06["N06【函数调用】处理服务准备补回(已归属衰减段) -> 补回结果"]
    N06 --> N07["N07【指令-读取】重算有效未满足需求和有效服务活动"]
    N07 --> N08{"N08【指令-判断】最长等待是否达到30天"}
    N08 -->|"是"| N09["N09【指令-计算】按活动保护把服务值目标设为0或1"]
    N08 -->|"否且无需求"| N10["N10【指令-计算】按每秒步长1衰减"]
    N08 -->|"否且有需求"| N11["N11【指令-计算】按最长等待地板和衰减"]
    N09 --> N12["N12【指令-判断】本秒是否已有主动安全根结算"]
    N10 --> N12
    N11 --> N12
    N12 -->|"是"| N13["N13【指令-构造】保留主动安全根结果"]
    N12 -->|"否"| N14["N14【函数调用】计算服务调节安全根回归(服务值候选) -> 安全根候选"]
    N13 --> N15["N15【函数调用】发布服务生存维护事务(全部候选) -> 发布结果"]
    N14 --> N15
    N15 --> N16["N16【指令-返回】返回服务生存维护结果"]
```

## 节点合同

| 节点 | 类型 | 参数 / 输入绑定 | 结果 / 输出绑定 | 事实读写 | 后继条件 | 验证 |
| --- | --- | --- | --- | --- | --- | --- |
| N04 | 指令-分段 | 完整秒和事实集合 | 确定性段序列 | 无 | 有界段数 | 批量等价逐秒 |
| N05 | 函数调用 | 同秒合同事实 | 预支/余款/终态 | 合同候选 | 当前段完成 | 30/70和同秒顺序 |
| N06 | 函数调用 | 已归属衰减段 | 补回候选 | 服务值候选 | 不含本秒新衰减 | 不重复补回 |
| N09-N11 | 指令-计算 | V、t、D0、活动状态 | 服务值候选 | 无 | 饱和算术成功 | 0/1保护与30天 |
| N14 | 函数调用 | A、L/H、V和主动事实 | 安全根候选 | 无直接发布 | A=0先行 | 比例档位边界 |
| N15 | 函数调用 | 合同、值、状态、动态、衰减段 | 发布结果 | 跨领域事务 | 全确认或撤销 | 同秒零中间态 |

## 关键边界

- 循环次数不参与数值；批量 N 秒必须与逐秒参考顺序相同。
- 有效服务活动只保护正服务值不降到0，不能凭活动从0生成服务值。
- 服务值为0时把 `A>=1` 调到1；`A=0` 始终保持终止，不能被动唤醒。
