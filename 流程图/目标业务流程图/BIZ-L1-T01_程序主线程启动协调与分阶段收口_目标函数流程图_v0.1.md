# BIZ-L1-T01 程序主线程启动协调与分阶段收口目标函数流程图 v0.1

更新时间：2026-08-03

## 依据

```text
8110、8120；目标线程归属与跨线程材料
旧鱼巢参考：流程图/鱼巢流程/20260712_旧鱼巢启动与九阶段初始化现状流程图_v0.1.md
吸收边界：只吸收初始化阶段和完整读回顺序；旧主链镜像、运行控制同步、自检/UI同入口不迁移
```

## 身份与边界

正式目标函数设计图。唯一根函数运行在程序主线程，只拥有装配、线程生命周期和停止阶段协调权，不拥有任何领域事实写入权。

## 函数合同

```text
根函数：运行普通多线程宿主
归属模块 / 文件 / 层级：程序运行宿主 / 海中鱼巣/启动.程序运行宿主.ixx / L1
现有或待新增：待新增；可复用现有停止信号和普通宿主能力
完整签名：程序运行结果 运行普通多线程宿主(const 启动选项& 选项)
参数：选项为只读借用；模式只能是普通控制面板或无窗口常驻
返回结果：程序运行结果；包含启动失败阶段、停止阶段、线程收口失败和正常停止
被调函数流程图：BIZ-L2-001-01—15；BIZ-L1-T02—T06 是并发线程入口，不是本函数的同步子流程
```

## 流程图

```mermaid
flowchart TD
    N01{"N01【指令-判断】选项是否属于普通模式"} -->|否| N20["N20【指令-返回】返回启动模式拒绝"]
    N01 -->|是| N02["N02【指令-构造】由选项形成普通应用配置"]
    N02 -->|配置| N03["N03【函数调用】构造普通应用上下文(配置) -> 装配结果"]
    N03 -->|装配结果| N04{"N04【指令-判断】装配结果是否完整"}
    N04 -->|否| N21["N21【指令-返回】返回装配失败"]
    N04 -->|是| N05["N05【函数调用】安装程序停止信号() -> 停止信号结果"]
    N05 -->|停止信号结果| N06{"N06【指令-判断】停止信号租约是否有效"}
    N06 -->|否| N22["N22【指令-返回】返回停止信号失败"]
    N06 -->|是| N07["N07【函数调用】初始化普通系统(初始化端口, 初始化请求) -> 初始化结果"]
    N07 -->|初始化结果| N08{"N08【指令-判断】初始化是否完整且自我线程停在治理运行门"}
    N08 -->|否| N24["N24【指令-赋值】锁存初始化失败及已启动资源"]
    N08 -->|是| N09["N09【函数调用】启动支持线程组(支持请求) -> 支持启动结果"]
    N09 -->|支持启动结果| N10["N10【函数调用】启动任务工作线程池(工作请求) -> 工作启动结果"]
    N10 -->|工作启动结果| N11["N11【函数调用】启动任务管理线程(管理请求) -> 管理启动结果"]
    N11 -->|管理启动结果| N12["N12【函数调用】启动外设线程组(外设请求) -> 外设启动结果"]
    N12 -->|外设启动结果| N13["N13【函数调用】开放自我线程治理循环(开放请求) -> 开放结果"]
    N13 -->|开放结果| N14["N14【函数调用】按模式启动控制面板线程(面板请求) -> 面板启动结果"]
    N14 -->|面板启动结果| N15{"N15【指令-判断】全部必需线程和运行门是否已进入"}
    N15 -->|否| N24
    N15 -->|是| N16["N16【函数调用】等待程序停止请求(停止等待请求) -> 停止原因"]
    N16 -->|停止原因| N17["N17【指令-赋值】锁存停止阶段并禁止新的控制面板与外设输入"]
    N24 -->|初始化或启动失败| N17
    N17 -->|停止阶段已锁存| N18["N18【函数调用】请求自我线程停止产生新任务意图(自我停发请求) -> 自我停发结果"]
    N18 -->|自我停发结果| N19["N19【函数调用】请求任务管理停止新派发(管理停发请求) -> 管理停发结果"]
    N19 -->|管理停发结果| N25["N25【函数调用】收口并连接外设线程组与任务工作线程池(在途请求) -> 在途收口结果"]
    N25 -->|在途收口结果| N26["N26【函数调用】排空结果上行并连接任务管理、自我和控制面板线程(治理请求) -> 治理收口结果"]
    N26 -->|治理收口结果| N27["N27【函数调用】刷新并连接支持线程组(移动支持线程租约组, 支持收口请求) -> 支持收口结果"]
    N27 -->|支持收口结果| N28["N28【指令-构造】按初始化、停止原因和各阶段结果构造程序运行结果"]
    N28 -->|程序运行结果| N29["N29【指令-返回】返回程序运行结果"]
```

## 节点合同

| 节点 | 类型 | 输入绑定 | 输出绑定 | 事实读写 | 验证 |
| --- | --- | --- | --- | --- | --- |
| N02—N06 | 指令 / 函数调用 / 判断 | 普通模式选项 | 配置、上下文和停止信号租约 | 只装配并安装进程停止信号 | 配置无效、构造失败和信号部分安装恢复 |
| N07—N08 | 函数调用 / 判断 | 8120初始化端口与请求 | 一份完整初始化结果 | 领域写入只由初始化服务完成；宿主不拆写六步 | 逐阶段失败、同代次读回、治理运行门未开放 |
| N09—N15 | 函数调用 / 判断 | 同一上下文的具名启动端口 | 消费者、生产者和运行门启动结果 | 只写线程生命周期投影 | 消费者先于生产者、逐线程失败注入 |
| N16 | 函数调用 | 停止信号和程序控制邮箱 | 具名停止原因 | 无业务写入 | 信号/UI/内部故障停止 |
| N17—N27 | 指令或函数调用 | 已锁存停止阶段 | 分阶段收口结果 | 禁止新输入；允许在途结果完成领域提交 | 在途工作包、回执与邮箱排空 |
| N28 | 指令-构造 | 全部结构化结果 | 程序运行结果 | 不用日志或线程状态伪造业务结果 | 失败阶段守恒 |

## 关键边界

- 启动先由 `初始化普通系统` 严格完成 8120 六步；自我线程完成初始化后停在治理运行门，不得在任务消费者就绪前处理治理批次。随后启动支持线程、任务工作线程池、任务管理线程和外设线程组，开放自我治理，最后按模式启动控制面板线程。停止顺序遵循“先停新输入和新派发、再收口在途工作、再排空上行、最后刷新支持旁路”。
- 旧鱼巢的“数据层与世界根、语素与概念、自我锚点、核心特征、默认方法、根需求、完整读回”只按现行 8120 服务合同吸收；旧主链镜像、运行控制同步、自检线程和 UI 同入口明确排除。
- 程序主线程不得进入自我治理、任务筹办、方法执行、世界事实提交或外设解释函数。
- `join` 只证明物理线程已回收，不证明任务完成、需求满足或业务结算成功。
- 任一启动阶段失败都进入同一逆序收口路径，禁止遗留已启动线程。
