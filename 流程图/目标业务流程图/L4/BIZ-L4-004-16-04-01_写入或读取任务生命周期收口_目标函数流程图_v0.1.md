# BIZ-L4-004-16-04-01 写入或读取任务生命周期收口目标函数流程图 v0.1

更新时间：2026-08-26

## 依据

```text
0050、4260 v0.11、5090 v0.8
BIZ-L3-004-16-04
```

## 身份与边界

正式目标函数设计图。task owner按结束或取消指令发布决议引用和对应生命周期收口，并独立读回；不删除任务、轮次或结果历史，不建立新轮次。

## 函数合同

```text
根函数：L2任务结构服务::写入或读取任务生命周期收口
归属模块 / 文件 / 目标分解深度：任务结构 owner / 海中鱼巣/领域/服务.L2任务结构.ixx / BIZ-L4
现有或待新增：当前工作区已有同名WIP
完整签名：L2任务生命周期收口写入结果 L2任务结构服务::写入或读取任务生命周期收口(const L2任务生命周期收口写入请求& 请求) noexcept
参数：L2请求头、幂等、结束/取消指令、任务、已收束轮次、轮次结算、收口种类、可选停止证据、时间和规则
返回结果：已形成、精确重复、入口/许可拒绝、当前性漂移、引用/幂等冲突、资源失败、已可能发布或内部不一致
被调函数流程图：无；owner内部事务和读回
父图：BIZ-L3-004-16-04
```

## 流程图

```mermaid
flowchart TD
    N01{"N01【指令-判断】请求头、幂等、指令、任务、轮次、结算、收口、停止、时间和规则是否完整"}
    N02{"N02【指令-判断】结束对应正常结束、取消对应取消收口"}
    N03["N03【函数调用】读取轮次结算与既有决议/收口 -> 前置"]
    N04["N04【函数调用】task owner原子发布决议引用和生命周期收口 -> 写入结果"]
    N05["N05【函数调用】按决议引用和收口身份独立读回 -> 收口读回"]
    N06{"N06【指令-判断】读回与请求是否逐字段同义"}
    N07["N07【指令-返回】返回已形成或精确重复"]
    N08["N08【指令-返回】返回拒绝、漂移或资源失败"]
    N09["N09【指令-返回】返回冲突、已可能发布或内部不一致"]

    N01 -->|否| N08
    N01 -->|是| N02
    N02 -->|否| N09
    N02 -->|是| N03
    N03 -->|非成功| N08
    N03 -->|完整| N04
    N04 -->|已形成或精确重复| N05
    N04 -->|普通非成功| N08
    N04 -->|冲突或未知| N09
    N05 -->|非成功| N08
    N05 -->|完整| N06
    N06 -->|否| N09
    N06 -->|是| N07
```

## 关键边界

等待指令不得调用本入口；结束/取消收口不发布需求满足、价值结算或新轮次。
