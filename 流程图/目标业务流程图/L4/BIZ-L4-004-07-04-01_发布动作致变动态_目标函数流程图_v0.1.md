# BIZ-L4-004-07-04-01 发布动作致变动态目标函数流程图 v0.1

更新时间：2026-08-03

## 依据

```text
0050、4190、4220、5300、8210；BIZ-L3-004-07-04
```

## 身份与边界

正式目标函数设计图。引用已发布状态迁移动能和方法运行，独立发布动作致变动态及来源关系；不撤销或重写前级状态。

## 函数合同

```text
根函数：动态业务服务::发布动作致变动态
归属模块 / 文件 / 层级：动态领域 / 海中鱼巣/领域/服务.动态.ixx / L4
现有或待新增：适配扩展；按4220补齐方法运行、动作入口和被改变目标同源合同
完整签名：动作致变动态发布结果 动态业务服务::发布动作致变动态(const 动作致变动态发布请求& 请求)
参数：请求为只读借用；含迁移动能、前后状态、方法运行、动作入口、场景、主体、发生时间、规则版本和幂等身份
返回结果：不可变值式结果；含已发布、精确重复、材料不足、漂移、异义冲突、结果未知或内部不一致，以及动作动态、来源关系和发布见证
被调函数流程图：动态与来源关系事务参与者在L5展开
父图：BIZ-L3-004-07-04
```

## 流程图

```mermaid
flowchart TD
 N01{"N01【指令-判断】迁移、状态、运行、动作、场景、主体、时间、规则和幂等是否完整"} -->|否| N11["N11【指令-返回】返回材料不足"]
 N01 -->|是| N02{"N02【指令-判断】前后状态、被改变目标、方法运行和动作入口是否满足4220"}
 N02 -->|否| N12["N12【指令-返回】返回漂移或内部不一致"]
 N02 -->|是| N03["N03【函数调用】取得动作动态事务许可并复核幂等 -> 许可"]
 N03 -->|重复| N13["N13【指令-返回】返回精确重复"]
 N03 -->|异义| N14["N14【指令-返回】返回异义冲突"]
 N03 -->|通过| N04["N04【函数调用】准备动作动态、关系20角色和发布见证 -> 候选"]
 N04 -->|已准备| N05["N05【函数调用】确认候选并最后发布动作动态代次 -> 发布结果"]
 N04 -->|失败| N15["N15【指令-返回】返回具名非成功"]
 N05 -->|未知| N16["N16【指令-返回】返回结果未知"]
 N05 -->|已发布| N06["N06【函数调用】联合读回动作动态、来源关系和见证 -> 读回"]
 N06 -->|不完整| N17["N17【指令-返回】返回内部不一致"]
 N06 -->|完整| N07["N07【指令-返回】返回已发布"]
```

## 节点合同

| 节点 | 类型 | 输入 | 输出 | 事实读写 | 验证 |
| --- | --- | --- | --- | --- | --- |
| N02/N03 | 指令/调用 | 已发布迁移与运行 | 发布资格 | 无状态写入 | 4220同源 |
| N04—N06 | 函数调用 | 动态写集 | 发布与读回 | 动态领域 | 前级状态不可回滚 |

## 关键边界

结果未知时只按原幂等身份收敛；不得重做外部动作，也不得删除已经发布的真实状态。
