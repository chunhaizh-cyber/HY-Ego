# BIZ-L0-001 项目总体业务生命周期目标函数流程图 v0.3

更新时间：2026-08-03

## 依据

```text
0050、6340、6360、8100、8110、8120、8200、8300、8310、8320
旧鱼巢参考：流程图/鱼巢流程/20260712_旧鱼巢运行主链与两条旁路总览现状流程图_v0.1.md
业务主链承接：BIZ-MAIN-001_自我治理到需求任务方法结果结算_目标业务流程图_v0.1.md
```

## 身份与边界

正式目标函数设计图。本版按多线程运行模式重新校准：L0 只负责启动模式路由，不在程序主线程同步执行世界、自我、任务、方法、外设和审计业务循环。

## 函数合同

```text
根函数：运行海中鱼巣
归属模块 / 文件 / 层级：启动应用 / 海中鱼巣/启动.应用程序.ixx / L0
现有或待新增：现有函数适配扩展
完整签名：程序运行结果 运行海中鱼巣(const 启动选项& 选项)
参数：
  选项：const 启动选项&，只读借用，调用期间有效；已经由薄入口完成解析
返回结果：
  程序运行结果；覆盖专项完成、普通多线程宿主正常停止、启动拒绝、装配失败、运行失败和内部错误
被调函数流程图：BIZ-L1-T01；各专项模式沿 8120 的独立函数合同
```

## 流程图

```mermaid
flowchart TD
    N01{"N01【指令-判断】选项中的启动模式"}
    N01 -->|普通控制面板或无窗口常驻| N02["N02【函数调用】运行普通多线程宿主(选项) -> 普通运行结果"]
    N01 -->|生产运行期专项| N03["N03【函数调用】运行生产运行期专项(选项) -> 专项结果"]
    N01 -->|完整自检或其它专项| N04["N04【函数调用】运行具名专项(选项) -> 专项结果"]
    N01 -->|未登记模式| N05["N05【指令-返回】返回启动模式拒绝"]
    N02 -->|普通运行结果| N06["N06【指令-返回】返回普通运行结果"]
    N03 -->|生产运行期专项结果| N07["N07【指令-返回】返回生产运行期专项结果"]
    N04 -->|具名专项结果| N08["N08【指令-返回】返回具名专项结果"]
```

## 节点合同

| 节点 | 类型 | 参数 / 输入绑定 | 结果 / 输出绑定 | 事实读写 | 后继条件 | 验证 |
| --- | --- | --- | --- | --- | --- | --- |
| N01 | 指令-判断 | `选项.模式` | 唯一模式分支 | 无 | 只进入一个分支 | 启动模式矩阵 |
| N02 | 函数调用 | 选项 -> 选项 | 普通运行结果 | 由 L1-T01 建立并收口线程宿主；L0 不写业务事实 | 调用返回 | 多线程启动/停止专项 |
| N03/N04 | 函数调用 | 选项 -> 专项请求 | 专项结构化结果 | 仅对应专项隔离上下文 | 调用返回 | 专项互斥矩阵 |
| N05—N08 | 指令-返回 | 当前结构化结果 | 程序运行结果 | 无新增事实 | 函数结束 | 退出码映射 |

## 关键边界

- `运行海中鱼巣` 不包含普通业务驻留循环，不轮询业务队列，也不调用一轮自我治理、任务闭环、外设观察或审计发布。
- 普通模式只调用一次 `运行普通多线程宿主`；六类核心线程随后独立运行，通过不可变值式材料协作。
- 旧鱼巢主链没有被删去，而是从 L0 同步调用链迁入 BIZ-MAIN-001 的跨线程业务闭环；L0 只保留模式路由，避免程序主线程重新拥有业务写权。
- 专项模式不得启动普通生产线程组；普通模式不得隐式运行完整自检。
- 线程启动、消息入队、日志或 UI 显示均不证明业务成功；程序结果只证明宿主是否按合同收口。
