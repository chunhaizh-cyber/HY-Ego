# 第 18 项：正差距比较状态分账

状态：已确认设计输入；非正式规范。

安全与服务分别构造 G05 请求，不合并为布尔值。结果必须区分已满足、可处理正差距、约束冲突、不可比较、材料缺失、比较未注册、漂移、拒绝、资源失败、未实现和内部错误。

只有 `已满足` 表示本次比较无正差距；空载荷、默认结果、未实现、缺材料、不可比较、未注册、资源失败、漂移和拒绝都不得解释为零差距。一轴成功不能替代另一轴失败；两轴均满足才可进入当前无正差距后继。
