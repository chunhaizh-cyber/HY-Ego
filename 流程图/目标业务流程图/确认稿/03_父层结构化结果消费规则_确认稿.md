# 第 03 项：父层结构化结果消费规则

状态：已确认设计输入；非正式规范。

每次函数调用后，父层必须先逐项解释该函数的正式结果枚举，不建立模糊的通用 `是否成功()`。成功须校验完整载荷；精确重复须返回既有发布身份和权威读回；空、等待和超时不得构造默认载荷；资源暂不可用只重试同一请求；漂移重新冻结；拒绝交具名消费者；停止按排空合同；未实现停止依赖路径；内部错误、半发布、异义冲突和未知枚举一律隔离。

结果载荷只在合法成功分支解包，非成功分支必须验证零默认成功、零错误事实写入。
