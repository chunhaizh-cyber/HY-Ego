# 第 06 项：T05 控制面板状态消费

状态：已确认设计输入；非正式规范。

只读投影、显示、程序停止消息和窗口故障上报均须逐状态消费。部分投影可显示缺口；无材料显示暂无材料；漂移重新读取；拒绝、内部错误和未知状态转诊断。窗口暂不可用保留值式投影，窗口关闭进入收口。

停止消息只有已提交、精确重复或停止已锁存才算正式接收；队列暂不可用重试同一消息。UI 不直接停止线程，提交成功也不等于程序已停止。故障只有发布或精确重复后才可称为已上报。
