# 第 16 项：安全、服务与权限联合版本门

状态：已确认设计输入；非正式规范。

分层安全维护与服务/生存维护保持各自事务所有者。两轴分别闭合并返回正式版本后，重新读取安全层、安全根、服务值和控制态的联合快照；版本兼容后才计算权限。

一轴成功而另一轴暂不可用时不撤销成功事实、不重复成功轴，也不计算权限或进入主需求裁决。后续若新合同、预支、安全值、服务值、控制态或需求来源变化，本批权限立即失效，必须在下一批重新维护、读回和计算。
