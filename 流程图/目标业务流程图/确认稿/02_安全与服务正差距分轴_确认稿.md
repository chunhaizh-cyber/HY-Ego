# 第 02 项：安全与服务正差距分轴

状态：已确认设计输入；非正式规范。

G02 只确认生存安全根的正差距，G03 只确认服务根的正差距；两者分别携带治理坐标、根值身份与版本、目标合同、维护见证、事实截止和比较合同版本。通用 `确认当前正差距` 下沉为 G05 比较能力，由两轴分别调用。

每轴必须区分无正差距、严格正差距、反向或越界、材料缺失、漂移、比较未登记、拒绝、资源失败、内部错误和未实现。只有严格正差距可进入 G06；一轴未实现时不得用另一轴成功替代完整治理结论。
