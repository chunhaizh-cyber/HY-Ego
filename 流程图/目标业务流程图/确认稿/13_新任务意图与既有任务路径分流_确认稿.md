# 第 13 项：新任务意图与既有任务路径分流

状态：已确认设计输入；非正式规范。

只有经正式正差距裁决的新任务治理意图可以调用建立或复用需求任务树。重筹办、权限刷新、父等待变化、工作结果以及已绑定外设材料必须沿既有任务、轮次和工作项身份继续。

无匹配外设报告只能缓存、舍弃或形成结构化缺口上报，任务管理线程不得自行生成需求或任务。只有权威读取明确证明不存在、且任务化闸门和唯一性检查完成时才可创建；读取故障、权限缺失或漂移不能伪装成不存在。
