# 第 17 项：服务合同准入与唯一性状态分账

状态：已确认设计输入；非正式规范。

法规结果分为已准入、明确拒绝、待确认、材料缺失、漂移和内部错误；待确认是合法等待，不等于拒绝。合同读取分为同义当前合同、异义或多个合同、明确不存在以及读取失败；只有权威证明不存在才允许新建。

生存控制态为零拒绝普通合同，休眠或运行态可继续合同规则，但合同与 30% 预支不得直接唤醒。建立或重复后须独立读回合同、预支、服务值和版本；半发布、撤销失败和未知状态隔离写路径。
