# 第 21 项：先找方法、再找条件的固定筹办顺序

状态：已确认设计输入；非正式规范。

每轮筹办固定顺序为：读取任务目标，按结果能力召回候选方法，回读登记、生命周期和结果规格，再逐候选读取条件与条件结果对，绑定当前事实、输入和参数，复核禁止项、风险与动作入口，形成就绪候选，最后排序并冻结唯一当前选择。

第一步得到的是候选方法，不是可用方法。零候选收束为合法等待或已定位能力缺口；条件缺失、条件不满足、材料缺失和漂移分别处理。固定返回 `未实现` 的骨架不能成为就绪候选。下一轮必须重新从结果能力召回开始，不能继承上轮就绪结论。
