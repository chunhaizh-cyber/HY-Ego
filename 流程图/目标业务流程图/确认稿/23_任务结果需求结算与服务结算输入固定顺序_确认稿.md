# 第 23 项：任务结果、需求结算与服务结算输入固定顺序

状态：已确认设计输入；非正式规范。

固定顺序为：T03 独立回读实际状态，任务领域按目标、验证、禁止结果、归因和当前性裁决并正式发布任务结果，独立读回后向 T02 发布只含结果定位和版本的不可变治理消息；T02 在新治理批次重新读取正式任务结果、来源需求、目标合同和当前状态，由需求领域提交并独立读回正式需求结算；只有需求正式满足后，才可形成服务合同终态或服务准备完成的值式结算输入，并交后续服务生存维护事务复核和发布。

工作回执和上行消息只负责定位，不承担任务结果权威。T02 不得直接使用消息携带的实际状态提交需求结算。任务完成不自动等于需求满足；任务未达目标时，需求保持活动并进入等待、重筹办、缺口或其它后继治理。

筹办发现目标原本已满足但缺少 5160 要求的合法动态证据时，可以按任务合同收束任务，但需求正式结算必须返回证据缺口，不得临时制造动作动态。服务结算输入只是值式候选，不是服务值、完工余款或准备补回事实。

后继步骤失败不得撤销已经正式发布的任务结果或需求结论；保持同一幂等身份继续收束。任一版本漂移都废弃当前派生材料并重新读取，不得混用旧任务结果、新需求状态或旧服务合同。
