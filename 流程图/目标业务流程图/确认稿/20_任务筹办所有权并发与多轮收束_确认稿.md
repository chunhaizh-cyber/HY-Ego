# 第 20 项：任务筹办所有权、并发与多轮收束

状态：已确认设计输入；非正式规范。

任务管理线程决定哪个任务进入哪一筹办轮次并负责派发；任务工作线程调用并执行筹办函数。不同任务可由多个工作线程并行筹办，同一任务同一时刻只能有一个有效工作项、筹办占用和轮次执行者。

同一任务允许顺序经历多轮筹办。每轮绑定任务、轮次、运行代次、工作项、事实截止和幂等身份，并收束为可执行冻结、下一轮、子目标、能力缺口、合法等待、目标完成、取消或错误恢复。工作线程不得在一次调用中自行进入下一轮；任务管理必须独立读回本轮结果后再裁决。精确重复只复用同轮结果。
