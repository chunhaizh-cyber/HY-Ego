# INSTINCT-STAGE3-ACTIVE-SAFETY-WINDOW-ADJUDICATION 读取 UTC 窗口主动安全变化裁决函数流程图 v0.1

日期：2026-08-30

对应设计：`规范/详细设计/20260830_INSTINCT-STAGE3-ACTIVE-SAFETY-WINDOW-ADJUDICATION_主动安全UTC窗口裁决详细设计_v0.1.md`

## 1. 函数

```cpp
本能主动安全UTC窗口裁决读取结果_v1
本能主动安全UTC窗口裁决组合器::读取UTC窗口主动安全变化裁决_v1(
    const 本能主动安全UTC窗口裁决读取请求_v1&) const noexcept;
```

## 2. 流程

```mermaid
flowchart TD
    N01{"N01【指令-判断】合同、基础请求、UTC半开窗口和预算有效"}
    N01 -->|否| N20["N20【指令-返回】入口拒绝；空载荷、截止0"]
    N01 -->|是| N02["N02【函数调用】第一次读取本能被动维护基础快照"]
    N02 -->|非成功| N21["N21【指令-返回】映射定义、当前值、游标、时间或资源状态"]
    N02 -->|成功| N03["N03【指令-冻结】取得G0、安全根特征实例和定义规则版本"]
    N03 --> N04["N04【函数调用】同G0读取当前主动安全结算来源规则"]
    N04 -->|非成功| N22["N22【指令-返回】规则未发布/漂移/引用/资源状态"]
    N04 -->|成功| N05{"N05【指令-判断】规则版本等于定义冻结版本且为版本1显式空组"}
    N05 -->|否| N23["N23【指令-返回】规则版本漂移或材料不闭合"]
    N05 -->|是| N06["N06【函数调用】同G0按安全根特征实例和原UTC窗口读取永久变化账完整组"]
    N06 -->|非成功| N24["N24【指令-返回】账未登记/预算/集合/漂移/引用/资源状态"]
    N06 -->|成功且非空| N25["N25【指令-返回】来源未裁定；空载荷、截止0"]
    N06 -->|成功且显式空| N07["N07【指令-形成】未改变A候选"]
    N07 --> N08["N08【函数调用】第二次读取本能被动维护基础快照"]
    N08 -->|非成功或稳定字段变化| N26["N26【指令-返回】当前性漂移或映射状态"]
    N08 -->|与首次稳定字段相同| N09{"N09【指令-判断】规则、见证、窗口、特征、G0和裁决完整"}
    N09 -->|否| N27["N27【指令-返回】内部错误；空载荷、截止0"]
    N09 -->|是| N10["N10【指令-返回】已读取；完整快照；截止=G0"]
```

## 3. 边界

本函数不生产 UTC 窗口，不把单调完整秒转换为 UTC。版本 1 非空变化组必须 fail-closed；只有规则 1 显式空集合与永久账合法空组同时成立时，才能返回 `未改变A`。
