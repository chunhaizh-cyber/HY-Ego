# L1-SIMPLIFY-P27 指定世界根当前状态动态集合读取函数流程图 v0.1

日期：2026-08-05

对应函数：

```cpp
当前世界状态动态集合结果 从快照读取当前世界状态动态集合(
    const 当前世界状态动态集合读取请求& 请求,
    const L1完整快照& 快照);
```

## 主流程

```mermaid
flowchart TD
    A["进入纯值函数"] --> B{"合同、读取规则和根有效？"}
    B -- "否" --> R1["入口拒绝；空事实"]
    B -- "是" --> C{"P19/P21/P24均完整、同根且排序唯一？"}
    C -- "否：调用形状错" --> R1
    C -- "否：成功载荷矛盾" --> R2["内部不一致；稳定诊断；空事实"]
    C -- "是" --> D{"三个上游截止与P17快照代次相同？"}
    D -- "否" --> R3["版本漂移；空事实"]
    D -- "是" --> E["验证P17当前节点/关系/值/属性槽唯一、当前且稳定"]
    E --> F{"P9/P14登记定位能在同一快照精确互证？"}
    F -- "否" --> R2
    F -- "是" --> G["冻结场景集、主体集和按主体槽集"]
    G --> H["按登记关系19类型分组当前关系"]
    H --> I["选取触达本树范围的状态候选"]
    I --> J{"每个候选角色1至5、六节点和五类型化值完整？"}
    J -- "否" --> R2
    J -- "是" --> K["形成状态事实并按场景/主体/特征/时间/状态排序"]
    K --> L["按登记关系20类型分组当前关系"]
    L --> M["选取触达范围或已形成状态的动态候选"]
    M --> N{"角色1至5/9恰一且角色6至8为零？"}
    N -- "否" --> R2
    N -- "是" --> O{"八槽、前后状态、时间和比较证据完整互证？"}
    O -- "否" --> R2
    O -- "是" --> P["形成迁移动能并按场景/后状态时间/动态排序"]
    P --> Q{"排序键唯一且缺项为空？"}
    Q -- "否" --> R2
    Q -- "是" --> R4["已读取；同截止；完整=true；双组可为空"]

    E -. "bad_alloc" .-> R5["资源失败；空事实"]
    E -. "其它异常" .-> R2
```

## 状态候选校验

```mermaid
flowchart TD
    S0["一个关系19源触达场景/主体/槽范围"] --> S1{"源是当前普通状态且资格槽存在？"}
    S1 -- "否" --> SF["内部不一致"]
    S1 -- "是" --> S2{"角色1至5各恰一、同源同关系类型？"}
    S2 -- "否" --> SF
    S2 -- "是" --> S3{"主体和来源在P21、场景在P19、槽属于主体P24？"}
    S3 -- "否" --> SF
    S3 -- "是" --> S4{"状态资格/时间/见证资格/四项引用/采样I64恰一？"}
    S4 -- "否" --> SF
    S4 -- "是" --> S5{"所属、表示、来源、材料、创建与当前生命周期一致？"}
    S5 -- "否" --> SF
    S5 -- "是" --> S6{"引用P8值仍在当前快照？"}
    S6 -- "是" --> S7{"编码/槽/来源/创建/I64逐项相同？"}
    S7 -- "否" --> SF
    S7 -- "是" --> SS["形成当前世界状态事实"]
    S6 -- "否：允许已退出历史引用" --> S8{"四项引用自洽且采样截止早于状态创建？"}
    S8 -- "否" --> SF
    S8 -- "是" --> SS
```

## 动态候选校验

```mermaid
flowchart TD
    D0["一个关系20源触达范围或状态组"] --> D1{"源是当前普通动态？"}
    D1 -- "否" --> DF["内部不一致"]
    D1 -- "是" --> D2{"角色1至5/9各恰一，角色6至8和未知角色为零？"}
    D2 -- "否" --> DF
    D2 -- "是" --> D3{"主体/场景/实例槽/来源属于输入范围？"}
    D3 -- "否" --> DF
    D3 -- "是" --> D4{"前后状态都在本轮状态组且端点同源？"}
    D4 -- "否" --> DF
    D4 -- "是" --> D5{"八个属性槽及类型化值恰一？"}
    D5 -- "否" --> DF
    D5 -- "是" --> D6{"种类=状态迁移、聚合=0、0<T0<T1？"}
    D6 -- "否" --> DF
    D6 -- "是" --> D7{"20项合同、13项引用、2项材料与差异逐项互证？"}
    D7 -- "否" --> DF
    D7 -- "是" --> DS["形成当前世界迁移动能事实"]
```

## 调用与锁边界

```mermaid
sequenceDiagram
    participant C as "后继协调者"
    participant P17 as "P17 L1事实基座"
    participant P27 as "P27纯值读取"

    C->>P17: "尝试读取完整快照（一次try-lock）"
    alt "许可拒绝或其它非成功"
        P17-->>C: "结构化非成功"
        Note over C,P27: "P27调用0；立即返回；不重试"
    else "成功"
        P17-->>C: "调用方拥有的纯值快照G"
        C->>P27: "请求 + 同一快照G"
        Note over P27: "无锁、无服务调用、无缓存、无SQL、无第二快照"
        P27-->>C: "同截止完整状态/动态集合或结构化非成功"
    end
```

本图只描述 P27 单函数。4200 的 G0—全部领域读取—G1 与一次完整漂移重试属于后继总世界快照协调者，不在本函数内实现。
