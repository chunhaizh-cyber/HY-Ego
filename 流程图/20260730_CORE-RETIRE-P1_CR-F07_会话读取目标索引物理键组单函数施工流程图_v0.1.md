# CR-F07 会话读取目标索引物理键组单函数施工流程图

版本：v0.1
创建侧代码基线：`57866ac5ca63235ed12da60f635d29f1aacd718b`
根函数：`节点直接身份结构写入会话::读取目标索引物理键组`
完整签名：`std::vector<索引物理键> 节点直接身份结构写入会话::读取目标索引物理键组(const 可重建索引目标& 目标)`
归属模块 / 文件：`海中鱼巣.核心.会话.节点直接身份结构写入` / `海中鱼巣/核心/会话.节点直接身份结构写入.ixx`；待新增公开成员。
参数与所有权：`目标` 仅在调用期借用且不保存；会话写集由当前事务独占。
返回与所有权：返回调用方独占的 `索引物理键` 副本组；空组合法。
非法值与错误：会话不可查询、`可重建索引目标完整(目标)==false` 或 `目标.目标种类 != 索引目标种类::节点` 时返回空组；本包不放行关系索引。正反绑定错配、重复物理键、移除写集缺写前记录或同一键创建与移除并存是内部不一致，记录首次失败且不返回半结果。
结果语义：从本事务视图返回仍指向同一节点目标的键；本事务移除键不命中、创建键命中；按 `(所有者身份, 命名域, 键格式版本, 探测规则版本, 键值)` 严格升序。

```mermaid
flowchart TD
    N01{"N01【指令-判断】会话可查询、目标记录完整且目标种类为节点"} -- "否" --> R01["R01【指令-返回】返回调用方自有空物理键组"]
    N01 -- "是" --> X01["X01【函数调用】std::vector<索引物理键> 可重建索引仓库::读取目标索引物理键组(const 可重建索引目标& 目标) const：实参[目标]→形参[目标]；返回→已发布基线键组"]
    X01 -- "完成" --> N02["N02【指令-赋值】候选键组=已发布基线键组；索引移除写集位置置零"]
    N02 -- "完成" --> B01{"B01【指令-判断】索引移除写集位置是否到达末尾"}
    B01 -- "否" --> B02{"B02【指令-判断】当前移除写入项.写前记录目标是否与输入目标相同"}
    B02 -- "是" --> X03["X03【函数调用】std::optional<可重建索引记录> 节点直接身份结构写入会话::读取索引物理键(const 索引物理键& 键)：实参[写前记录.物理键]→形参[键]；返回→本事务记录"]
    X03 -- "完成" --> B05{"B05【指令-判断】本事务记录为空且移除项已由CR-F34标记读回"}
    B05 -- "否" --> E01
    B05 -- "是" --> N03["N03【指令-操作】从候选键组移除所有等于写前记录.物理键的项并接收移除计数"]
    N03 -- "完成" --> B05A{"B05A【指令-判断】移除计数是否严格等于1"}
    B05A -- "否" --> E01
    B05A -- "是" --> N05
    B02 -- "否" --> N04["N04【指令-操作】保持候选键组不变"]
    N04 -- "完成" --> N05
    N05 -- "完成" --> B01
    B01 -- "是" --> N06["N06【指令-赋值】索引创建写集位置置零"]
    N06 -- "完成" --> B03{"B03【指令-判断】索引创建写集位置是否到达末尾"}
    B03 -- "否" --> B04{"B04【指令-判断】当前创建候选写后记录目标是否与输入目标相同"}
    B04 -- "是" --> N07["N07【指令-操作】把写后记录.物理键追加到候选键组并标记该创建项已读回"]
    B04 -- "否" --> N08["N08【指令-操作】保持候选键组不变"]
    N07 -- "完成" --> N09["N09【指令-推进】索引创建写集位置加一"]
    N08 -- "完成" --> N09
    N09 -- "完成" --> B03
    B03 -- "是" --> N10["N10【指令-操作】按物理键五字段字典序排序候选键组"]
    N10 -- "完成" --> V01["V01【指令-赋值】验证位置置零"]
    V01 -- "完成" --> V02{"V02【指令-判断】验证位置是否到达候选键组末尾"}
    V02 -- "否" --> X02["X02【函数调用】std::optional<可重建索引记录> 节点直接身份结构写入会话::读取索引物理键(const 索引物理键& 键)：实参[当前键]→形参[键]；返回→本事务记录"]
    X02 -- "完成" --> V03{"V03【指令-判断】本事务记录有值、目标等于输入目标且当前键不重复"}
    V03 -- "是" --> V04["V04【指令-推进】验证位置加一"]
    V04 -- "完成" --> V02
    V03 -- "否" --> E01["E01【函数调用】void 节点直接身份结构写入会话::记录失败(const 节点直接身份结构写入结果& 失败) noexcept：实参[{内部不一致,0,0,0}]→形参[失败]；返回void"]
    E01 -- "完成" --> R01
    V02 -- "是" --> R02["R02【指令-返回】移动返回调用方自有事务物理键组"]
```

节点合同：本包目标身份只允许 `索引目标种类::节点`、一个有效节点句柄和一个无效关系句柄共同形成；声明中的 `关系=2` 不取得新增、移除或恢复权限。X01 只取得普通已发布基线，不存在事务反向读取重载。N03 先移除、N07 后纳入；X02 逐键调用 CR-F34 读取本事务视图并同时标记对应创建/移除写入项已读回，V03 禁止同键在创建和移除写集中并存。

双向引用：

- 上游业务节点：`B10—B11/B14`，见 [关系反向读取与节点退役事务业务流程图](./20260730_CORE-RETIRE-P1_关系反向读取与节点退役事务业务流程图_v0.1.md)。
- 直接调用方：`CR-F12 节点直接身份结构写入会话::删除节点候选(节点句柄)`；其删除前必须证明目标索引键均已进入本事务移除写集。
- 直接被调方：CR-F30 `可重建索引仓库::读取目标索引物理键组(const 可重建索引目标&) const`、CR-F34 `节点直接身份结构写入会话::读取索引物理键(const 索引物理键&)`、`可重建索引目标完整`、会话私有 `记录失败`。
