# CR-F13 会话移除索引候选单函数施工流程图

日期：2026-07-30
版本：v0.1
代码基线：`57866ac5ca63235ed12da60f635d29f1aacd718b`

## 依据与身份

依据：业务图 B09、B11；函数功能确认 CR-F13；逐节点分析 CR-F13；详细设计 §4.2—§4.3、§5、§7。
本图是待实现目标函数的正式施工图。

## 函数合同

```text
根函数：移除索引候选
归属：海中鱼巣.核心.会话.节点直接身份结构写入 / 核心/会话.节点直接身份结构写入.ixx / 会话编排层公开成员
完整签名：节点直接身份索引移除结果 节点直接身份结构写入会话::移除索引候选(const 索引物理键& 键)
参数：键，const 索引物理键&，只借用到返回；五字段完整；调用方保有所有权。
返回：节点直接身份索引移除结果，值归调用方。
- 已提交：移除前记录必须有值，候选唯一移入索引移除写集且事务读取已证明不命中。
- 幂等读回：同事务已经登记该键时返回原写集的同一 `移除前记录`；物理键原本不存在时才允许 `移除前记录` 为空。
- 入口拒绝/资源失败/内部不一致：移除前记录为空；已形成局部候选必须先撤销。
```

被调函数：
- CR-F08，见 `流程图/20260730_CORE-RETIRE-P1_CR-F08_结构化移除索引候选单函数施工流程图_v0.1.md`。
- CR-F10，见 `流程图/20260730_CORE-RETIRE-P1_CR-F10_撤销索引候选单函数施工流程图_v0.1.md`。
- CR-F34 `节点直接身份结构写入会话::读取索引物理键(const 索引物理键&)`：事务视图读回并标记写集。
调用者：[CR-F57](./20260730_CORE-RETIRE-P1_CR-F57_登记核心节点退役候选单函数施工流程图_v0.1.md)；CR-F12 只验证本写集，不替代本函数。

## 流程图

```mermaid
flowchart TD
    N01{"N01【指令-判断】会话可继续写入且键五字段完整"}
    N01 -- "否" --> N02["N02【指令-返回】返回入口拒绝、空移除前记录"]
    N01 -- "是" --> B01{"B01【指令-判断】索引移除写集是否已有同键写入项"}
    B01 -- "是" --> N03A["N03A【函数调用】std::optional<可重建索引记录> 节点直接身份结构写入会话::读取索引物理键(const 索引物理键& 键)：实参[键]→形参[键]；返回→本事务记录"]
    N03A -- "完成" --> B02{"B02【指令-判断】事务记录为空且既有写入项写前记录完整"}
    B02 -- "否" --> N09
    B02 -- "是" --> R00["R00【指令-返回】返回幂等读回和既有写入项的同一移除前记录；零新增写"]
    B01 -- "否" --> N02A["N02A【指令-容量预留】执行 索引移除写集_.reserve(索引移除写集_.size()+1)"]
    N02A -. "抛出 std::bad_alloc/std::length_error" .-> R00A["R00A【指令-返回】记录并返回资源失败、空移除前记录；仓库尚无新候选"]
    N02A -- "预留成功" --> N03["N03【函数调用】可重建索引写入结果 可重建索引仓库::结构化移除索引候选(const 索引物理键& 键,std::uint64_t 事务序号)：键→键，事务序号_→事务序号；接收仓库结果"]
    N03 -- "完成" --> N04{"N04【指令-判断】仓库结果状态"}
    N04 -- "幂等不存在且当前记录为空" --> N05["N05【指令】保存幂等结果_为幂等读回"]
    N05 -- "完成" --> N06["N06【指令-返回】返回物理不存在幂等读回、空移除前记录"]
    N04 -- "幂等不存在且当前记录有值" --> N09["N09【指令-返回】记录内部不一致：仓库存在本事务待移除标记但会话没有对应写集"]
    N04 -- "拒绝或内部不一致" --> N07["N07【指令-返回】映射结果；内部不一致记录首次失败"]
    N04 -- "已移除候选" --> N08{"N08【指令-判断】当前记录和独占候选是否同时存在且种类为移除"}
    N08 -- "否" --> N09["N09【指令-返回】记录内部不一致"]
    N08 -- "是" --> N10["N10【指令-移动】复制移除前记录；在已预留容量内无分配移动候选进入索引移除写集"]
    N10 -- "完成" --> N14["N14【函数调用】std::optional<可重建索引记录> 节点直接身份结构写入会话::读取索引物理键(const 索引物理键& 键)：键→键；接收本事务记录"]
    N14 -- "完成" --> N15{"N15【指令-判断】事务记录为空且对应写入项可标记已读回"}
    N15 -- "否" --> N16["N16【指令-返回】记录内部不一致，等待统一撤销"]
    N15 -- "是" --> N17["N17【指令-返回】返回已提交和移除前记录"]
```

## 节点合同与边界

| 节点 | 事实读写 | 后继条件 | 验证 |
| --- | --- | --- | --- |
| N01—N07 | 先读回同事务既有写集，再调用 CR-F08 或处理物理不存在/拒绝 | 同事务重复返回原写前记录；只有物理不存在允许空材料 | T18—T22 |
| N02A/N08—N10 | 候选形成前预留容量；形成后无分配移动 | 预留失败时仓库尚无候选；移动后唯一归会话 | T23、T35—T39 |
| N14—N17 | 调用 CR-F34 读取事务视图并置移除写入项已读回 | 普通读取仍命中、本事务不命中 | T24、T27 |

不得把 `幂等不存在` 伪造成带默认记录的成功；不得把可能分配的 `push_back` 与候选移动合并；不得在候选已经移入写集后继续使用移动前对象；任一未闭合读回都阻止 CR-F14 确认。
