# SERVICE-P1 F-V207 `形成服务合同记录参与者` 单函数施工流程图 v0.1

图类型：目标施工流程图
状态：现行设计依据；函数待 #379 实现，不表示代码已存在
归属模块：`海中鱼巣.领域.参与者.服务生存维护记录`
归属文件：`海中鱼巣/领域/参与者.服务生存维护记录.ixx`
唯一根函数：F-V207
完整签名：

```cpp
服务生存记录参与结果 形成服务合同记录参与者(
    服务生存维护记录仓库& 仓库,
    const 服务合同建立请求& 请求,
    const 服务合同载荷& 候选) noexcept;
```

`仓库` 为覆盖参与者全生命周期的借用；`请求`、`候选` 在本函数内复核并复制进私有参与者候选，不保存其引用。仅 `已形成` 返回非空 move-only 参与者，其它分类必须返回空指针。依据 0050、6160 与服务详细设计 v0.3 第 6、7.4、8、10 节。

## 流程图

```mermaid
flowchart TD
    S["S【本函数内具体指令】只读绑定请求与候选并借用服务生存维护记录仓库"]
    B01{"B01【本函数内具体指令】请求的幂等主键、自我、提出者、需求、全部期望需求/值/定义/集合/主动安全/纪元/代次版本、时间、截止和三个规则版本均有效？"}
    R01["R01【本函数内具体指令】返回入口拒绝和空参与者"]
    B02{"B02【本函数内具体指令】显式有效期适用性与可选秒成对一致，候选的合同、预支、服务值、发布代次和版本字段完整？"}
    R02["R02【本函数内具体指令】返回结构拒绝和空参与者"]
    B03{"B03【本函数内具体指令】候选自我、提出者、需求、幂等主键、完整秒/事实截止、全部输入版本、合同/时间/回归规则版本及提供者写入材料与请求逐项一致？"}
    R03["R03【本函数内具体指令】返回版本漂移或结构拒绝和空参与者"]
    N04["N04【本函数内具体指令】深复制请求与候选，构造借用仓库的私有合同记录事务参与者候选"]
    N05["N05【本函数内具体指令】冻结参与者状态为未准备并绑定F-V217准备、F-V218确认、F-V219发布、F-V220撤销四个私有override合同"]
    R06["R06【本函数内具体指令】返回已形成和唯一非空参与者所有权"]
    E07["E07【本函数内具体指令】捕获分配异常并返回资源失败和空参与者"]
    S --> B01
    B01 -- "否" --> R01
    B01 -- "是" --> B02
    B02 -- "否" --> R02
    B02 -- "是" --> B03
    B03 -- "否" --> R03
    B03 -- "是" --> N04 --> N05 --> R06
    N04 -. "分配异常" .-> E07
```

## 节点合同

| 节点 | 类型 | 输入 | 输出 | 事实读写 |
| --- | --- | --- | --- | --- |
| S—B03 | 本函数内具体指令 | 公开请求、合同候选 | 入口、结构、版本裁决 | 零写入 |
| N04—N05 | 本函数内具体指令 | 已复核值式副本、借用仓库 | move-only 私有参与者 | 仅形成未发布内存候选 |
| R01—R06/E07 | 本函数内具体指令 | 已裁决分类 | 参与结果 | 失败零可读结构 |

## 实现约束

1. 参与者保存仓库借用与请求/候选值式副本，不得保存调用方引用、原始指针视图、锁或执行许可。
2. 精确同键同义由参与者准备阶段返回幂等读回；同键异义、异版或已发布半结构不得改写为新候选。
3. 参与者内部锁只由执行器回调期取得；不得在本工厂中提前持锁，不得调用上层服务、日志或外部提供者。
4. 未确认候选始终不可由 F-V209 普通读取看见；确认前失败必须可逆序撤销，发布后不得伪撤销。
