# 执行节点直接类型化结构事务目标函数流程图 v0.1

更新时间：2026-08-02

## 依据

```text
正式规范：4020—4070、4200
函数合同：WORLD-TREE-P1 函数功能确认 2.2
当前事实：节点直接身份结构事务域已有 try-lock 独占许可和通用参与者执行框架；无发布代次、持久准备和原许可内发布后读回
```

## 身份与边界

本图只描述执行器固定事务根函数；它只接受数据操作模块形成的中性规格，不接受服务请求、外部参与者或回调。

## 函数合同

```text
根函数：节点直接身份结构写入执行器::执行节点直接类型化结构事务
归属：海中鱼巣.核心.执行器.节点直接身份结构写入 / 海中鱼巣/核心/执行器.节点直接身份结构写入.ixx / L1；唯一合法调用者为 `节点直接类型化结构数据操作`
待新增：是，模块公开窄入口；调用白名单由 import/调用边静态门禁限制为数据操作模块，不依赖跨模块 friend
完整签名：节点直接类型化结构数据操作结果 执行节点直接类型化结构事务(const 节点直接类型化结构数据操作规格& 规格) const
参数：`规格: const 节点直接类型化结构数据操作规格&`，输入、只读借用；唯一调用者数据操作拥有规格至同步返回，执行器不保存引用
非法值：数据操作规格中的双摘要/编码见证不一致、执行器未用八依赖完整构造、合同/身份/代次/写集/读回规格不完整、计划身份溢出或历史占用
返回：`节点直接类型化结构数据操作结果` 调用方拥有的中性纯值；正常为已提交/幂等读回，逻辑内返回为入口拒绝/冲突/漂移/许可或资源失败，撤销或读回矛盾为内部不一致；异常全部在函数内分类且不得越过 L1
调用者流程图：`20260802_节点直接类型化结构提交服务提交_函数流程图_v0.1.md` 经 `节点直接类型化结构数据操作::提交` 间接到达；本函数无其它合法调用者
```

## 流程图

```mermaid
flowchart TD
    N01["N01【函数调用】节点直接身份结构事务域::取得独占许可() -> 许可"] --> N02{"N02【指令-判断】许可有效"}
    N02 -- "否" --> R01["R01【指令-返回】返回 许可拒绝；代次=0"]
    N02 -- "是" --> N03["N03【函数调用】节点直接事务幂等仓库::读取(幂等身份) -> 可选记录"]
    N03 --> N04{"N04【指令-判断】已有记录分类"}
    N04 -- "同请求意图已发布" --> R02["R02【指令-返回】返回 幂等读回、原代次、原通用读回和内存持久状态"]
    N04 -- "异请求意图" --> R03["R03【指令-返回】返回 幂等冲突"]
    N04 -- "已隔离/待发布" --> R04["R04【指令-返回】返回 内部不一致"]
    N04 -- "未找到" --> N05{"N05【指令-判断】请求预期代次非零且当前已发布代次 == 请求预期代次"}
    N05 -- "否" --> R05["R05【指令-返回】返回 版本漂移；零写入"]
    N05 -- "是" --> N06["N06【指令-计算】按命名域和局部身份升序计算计划稳定身份、预计代次、计划读回和确定性写集；不推进高水位"]
    N06 --> N07{"N07【指令-判断】无溢出、占用冲突、前向引用或合同矛盾"}
    N07 -- "否" --> R04
    N07 -- "是" --> N08["N08【函数调用】节点直接持久证据写入端口::准备(准备请求) -> 持久结果"]
    N08 --> N09{"N09【指令-判断】已准备或精确同义"}
    N09 -- "否" --> R06["R06【指令-返回】映射资源失败/冲突；尚无候选或高水位变化"]
    N09 -- "是" --> N10["N10【指令-顺序】按计划身份建立类型合同→节点→类型化值→关系→索引→删除→幂等候选"]
    N10 --> N11["N11【指令-顺序】按固定顺序准备并确认全部候选"]
    N11 --> N12{"N12【指令-判断】全部确认成功"}
    N12 -- "否" --> N13["N13【指令-逆序】撤销已建立/确认候选"]
    N13 --> N14{"N14【指令-判断】撤销完整恢复前态"}
    N14 -- "是" --> R07["R07【指令-返回】返回首次失败；代次不变"]
    N14 -- "否" --> R08["R08【指令-写入】标记事务域隔离并返回内部不一致"]
    N12 -- "是" --> N15["N15【指令-顺序】全部参与者含幂等记录最后发布；已发布代次推进一次"]
    N15 --> N15A{"N15A【指令-判断】全部参与者发布与代次推进均完整"}
    N15A -- "否" --> N19
    N15A -- "是" --> N16["N16【指令-读取】原独占许可内按读回规格逐字段复核通用读回"]
    N16 --> N17{"N17【指令-判断】读回完全匹配计划结果"}
    N17 -- "是" --> N18["N18【指令-构造】采用已发布幂等记录中的计划结果摘要和真实通用读回"]
    N17 -- "否" --> N19["N19【指令-写入】隔离事务域；形成内部不一致失败见证摘要，不改写已发布幂等记录"]
    N18 --> N20["N20【函数调用】节点直接持久证据写入端口::标记已发布(见证请求) -> 见证结果"]
    N19 --> N20
    N20 --> N21{"N21【指令-判断】业务读回成功"}
    N21 -- "是" --> R09["R09【指令-返回】返回 已提交；见证失败则持久化结果未知"]
    N21 -- "否" --> R10["R10【指令-返回】返回 内部不一致；保留发布代次；见证失败则持久化结果未知"]
```

## 节点合同

| 节点组 | 类型 | 事实读写 | 核心验证 |
| --- | --- | --- | --- |
| N01—N07 | 函数调用 / 本函数内指令 | 只读内存代次、幂等、仓库高水位和当前事实 | 幂等先行、预期代次、确定性映射、溢出 |
| N08—N09 | 函数调用 / 判断 | 只写持久准备证据，不写内存候选 | 资源失败时零内存变化 |
| N10—N15A | 本函数内事务指令 | 候选、确认、撤销、全部参与者最后发布、代次 | 固定顺序和逆序撤销；代次切换是唯一线性化点；部分发布立即隔离并形成失败见证 |
| N16—N19 | 本函数内读取 / 指令 | 原许可内通用读回、结果摘要和隔离标记 | 发布后失败不回滚或二次改写幂等事实；立即隔离事务域 |
| N20—R10 | 函数调用 / 返回 | 追写持久见证；返回纯值结果 | 正常与失败读回均尝试见证 |

## 关键边界

```text
不得持线程队列锁调用本函数。
持久准备在独占结构许可内执行；调用期间不建立候选但会阻塞同域读写。
空域初始化由恢复服务的非公开安装实例初始化事务先建立 L1 治理代次 1；普通提交始终拒绝预期代次 0。
恢复使用独立非公开恢复入口，不从本函数普通 DTO 指定历史稳定身份。
```
