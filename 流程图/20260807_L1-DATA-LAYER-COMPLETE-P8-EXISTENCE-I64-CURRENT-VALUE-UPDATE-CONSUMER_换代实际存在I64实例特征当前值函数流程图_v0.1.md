# 换代实际存在 I64 实例特征当前值函数流程图 v0.1

日期：2026-08-07

计划身份：`L1-DATA-LAYER-COMPLETE-P8-EXISTENCE-I64-CURRENT-VALUE-UPDATE-CONSUMER`

目标函数：`L1实例特征服务::换代实际存在I64实例特征当前值(const 实际存在I64实例特征当前值换代请求&) const`

```mermaid
flowchart TD
    A["接收当前值换代请求"] -->|进入唯一 const 实现并取得同一服务锁| B{"请求 DTO 是否完整"}
    B -->|否，版本、身份或读取定位不完整| R1["返回入口拒绝，权威状态零变化"]
    B -->|是，字段完整| C{"当前登记能否权威读回"}
    C -->|否，登记缺失、漂移或损坏| R2["返回对应结构化非成功"]
    C -->|是，取得当前值属性类型| D{"幂等身份是否已有路由定位"}
    D -->|是，重复或同键异义候选| J["由本次请求与登记机械形成完整中性写集"]
    D -->|否，首次候选| E["插入仅含幂等身份的暂定路由定位"]
    E -->|定位建立| F["读取起始中性事实代次"]
    F -->|起始代次不等于请求期望| X1["删除暂定定位并返回事实代次漂移"]
    F -->|起始代次等于请求期望| G["经正式入口读取写前实际存在实例"]
    G -->|非成功或字段不一致| X2["删除暂定定位并返回对应非成功"]
    G -->|实例完整且字段匹配| H["经正式入口读取定义并校验新值闭区间"]
    H -->|定义非成功或新值越界| X3["删除暂定定位并返回对应非成功"]
    H -->|定义同截止且新值合法| I["经中性当前节点入口读取新来源并复读结束代次"]
    I -->|来源无效或共同截止不一致| X4["删除暂定定位并返回对应非成功"]
    I -->|来源为普通节点且全部同截止| J
    J -->|固定零节点零关系一值一槽一退出| K["恰一次提交中性写集"]
    K -->|入口拒绝、漂移、冲突或失败| L{"是否首次暂定定位"}
    L -->|是，未形成成功类结果| X5["删除暂定定位并返回中性映射状态"]
    L -->|否，既有定位| R3["保留定位并返回中性映射状态"]
    K -->|成功或精确重复且映射完整| M["保留幂等路由定位并取得首次新值编码与发布代次"]
    M -->|形成新读取请求| N["读取读回起始中性事实代次"]
    N -->|取得稳定起点| O["经正式实例入口读取新当前值"]
    O -->|新当前非成功或字段不一致| R4["返回对应非成功，不用缓存补成功"]
    O -->|新当前完整| P["中性当前读取旧值"]
    P -->|不是已退出或仍带当前事实| R5["返回内部不一致"]
    P -->|已退出且无当前事实| Q["中性历史读取旧值"]
    Q -->|旧材料、来源、所属、类型或退出代次不一致| R6["返回内部不一致"]
    Q -->|旧历史完整| S["中性读取实例槽当前属性"]
    S -->|未指向首次新值编码| R7["返回内部不一致"]
    S -->|当前槽正确| T["复读结束中性事实代次"]
    T -->|首末截止或新当前截止不一致| R8["返回事实代次漂移或内部不一致"]
    T -->|同一截止且不早于首次发布| U{"中性提交状态"}
    U -->|首次成功| V["返回已提交及新当前领域事实"]
    U -->|精确重复| W["返回幂等读回及同一新当前领域事实"]
```

边界：流程不调用幂等预探测、完整快照、legacy 提交、审计或恢复；不缓存首次领域结果。旧值的“删除”只表示当前事实退出，历史仍由正式历史入口回查。
