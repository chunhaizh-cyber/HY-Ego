# 发布与读取实际存在 I64 基准状态函数流程图 v0.2

## 发布函数

```cpp
实际存在I64基准状态结果 L1状态服务::发布实际存在I64首个基准状态(
    const 实际存在I64首个基准状态发布请求& 请求);
```

```mermaid
flowchart TD
    A[接收请求并取得状态服务互斥量] --> B{入口 登记 幂等身份\n代次和时间有效?}
    B -- 否 --> X1[入口拒绝 空载荷]
    B -- 是 --> C{本进程幂等意图账}
    C -- 异义 --> X2[幂等冲突 空载荷]
    C -- 同义 --> R[取原完整状态读取请求]
    C -- 首次 --> D[由P8和P7读取采样时\n当前实例特征 主体与场景]
    D --> E{同一截止代次且材料一致?}
    E -- 否 --> X3[映射结构化非成功]
    E -- 是 --> F[从L1当前值互证P8值\n扫描关系19拒绝已有首态]
    F --> G{结构有效且无首态?}
    G -- 否 --> X4[入口拒绝或内部不一致]
    G -- 是 --> H[0x0D一次提交\n二节点 五值 五关系 五属性槽]
    H --> I{L1成功或精确重复?}
    I -- 否 --> X5[映射冲突 漂移 资源或内部错误]
    I -- 是 --> J[校验本地键1至12\n形成状态自身完整读取请求]
    J --> R
    R --> K[调用同一公开不可变状态读取入口]
    K --> L{读取成功?}
    L -- 否 --> X6[结构化非成功\n不补写 不回滚]
    L -- 是 --> Z[返回已提交或幂等读回完整首态]
```

## 读取函数

```cpp
实际存在I64基准状态结果 L1状态服务::读取实际存在I64基准状态(
    const 实际存在I64基准状态读取请求& 请求) const;
```

```mermaid
flowchart TD
    A[接收状态身份及完整状态见证编码] --> B{合同 规则和编码有效?}
    B -- 否 --> X1[入口拒绝 空载荷]
    B -- 是 --> C[读取起始L1完整快照 G0]
    C --> D[逐项读取状态节点 见证节点\n五关系 五状态域值]
    D --> E{当前入口结果}
    E -- 成功 --> F[使用当前不可变副本]
    E -- 已退出 --> G[按同一编码读取历史事实]
    E -- 未找到 --> X2[未找到 空载荷]
    E -- 其它非成功 --> X3[映射结构化非成功]
    G --> H{历史事实存在且种类正确?}
    H -- 否 --> X2
    H -- 是 --> F
    F --> I[由关系19冻结角色1至5\n不调用P7或P8当前入口]
    I --> J{角色 属性槽 来源和材料完整?}
    J -- 否 --> X4[内部不一致 空载荷]
    J -- 是 --> K[从四项引用取得被采样P8值编码]
    K --> L{被采样值当前?}
    L -- 是 --> M[使用当前P8值副本]
    L -- 已退出 --> N[从L1历史入口取得被采样值]
    L -- 未找到 --> X2
    N --> M
    M --> O{所属实例槽 类型 来源 创建代次\nI64材料和采样生命周期互证?}
    O -- 否 --> X4
    O -- 是 --> P[读取结束L1完整快照 G1]
    P --> Q{G0等于G1且当前读代次一致?}
    Q -- 否且未重试 --> C2[完整重试一次]
    C2 --> C
    Q -- 否且已重试 --> X5[版本漂移 空载荷]
    Q -- 是 --> Z[返回不可变状态快照\n读取截止与采样截止分列]
```

边界：P10 换代后旧 P8 值从历史入口互证；场景移动后仍返回状态角色2冻结的采样场景。本入口不选择当前或相邻状态，不生成关系20或动态。
