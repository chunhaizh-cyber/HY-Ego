# 存在场景首组垂直样例代码逻辑流程图

更新时间：2026-07-13

## 依据

```text
规范/仓库与服务分层事务边界规范.md
规范/详细设计/仓库底层与服务数据操作分层纠偏详细设计.md
计划/已完成计划/20260713_SERVICE-DATA-S1_不透明结构写入会话与执行器代码实施切片_v0.1.md
实施记录/20260713_SERVICE-DATA-S1_不透明结构写入会话与执行器代码实施_Codex断点清单.md
海中鱼巣/核心/会话.结构写入.ixx
海中鱼巣/核心/执行器.结构写入.ixx
海中鱼巣/领域/存在服务.h
海中鱼巣/领域/场景服务.h
海中鱼巣/领域/世界服务.h
```

## 说明

本图按 JY-319 固定第一组五条路径：创建实际存在、创建虚拟存在、创建场景、场景接纳已有存在、在已有场景中创建并接纳实际存在。业务服务和组合器不直接导入结构写入执行器；只有 `数据操作.存在场景` 在模块内部调用执行器并提交同步会话回调。

旧无令牌、带令牌和手写候选入口继续作为兼容路径保留。本轮只在隔离自检验证新分层，不迁移既有生产调用，不创建“场景候选 + 存在候选”的双新身份组合。

## 流程图

```mermaid
flowchart TD
    A["调用存在业务服务 / 场景业务服务 / 存在场景组合器；输入业务请求"] --> B{"请求类型"}
    B -- "创建实际存在" --> C1["存在业务服务：检查非零全局幂等主键"]
    B -- "创建虚拟存在" --> C2["存在业务服务：检查幂等主键和需求 / 任务 / 方法来源"]
    B -- "创建场景" --> C3["场景业务服务：检查非零全局幂等主键"]
    B -- "场景接纳已有存在" --> C4["场景业务服务：检查已有场景和已有存在句柄"]
    B -- "在已有场景中创建并接纳实际存在" --> C5["组合器：检查已有场景和实际存在幂等主键"]

    C1 --> D["业务层只调用数据操作层的值式只读材料入口"]
    C2 --> D
    C3 --> D
    C4 --> D
    C5 --> D
    D --> E{"写前材料裁决"}
    E -- "请求字段无效 / 句柄过期 / 类型错误" --> L1["逻辑内返回：入口拒绝；结构不变化"]
    E -- "主键已绑定到冲突身份或虚拟来源不一致" --> L2["逻辑内返回：幂等冲突；结构不变化"]
    E -- "同键同语义身份或同一归属关系已存在" --> L3["逻辑内返回：幂等读回；不重复写入"]
    E -- "权威结构多条、矛盾或不可唯一解释" --> X1["追根因解决：停止后继实施"]
    E -- "需要创建缺失结构" --> F{"结构操作类型"}

    F --> G["数据操作.存在场景在模块内部调用 #264 结构写入执行器"]
    G --> H["执行器取得一次独占许可并在当前线程栈构造不透明会话"]
    H --> I["会话先复核已有完整句柄当前可读；节点类型沿同一当前版本不可变"]
    I --> J{"许可内当前性复核通过"}
    J -- "否" --> R["执行器在许可释放前逆序撤销本次精确写集"]
    J -- "是" --> K{"按请求调用会话窄写入入口"}
    K -- "实际存在" --> W1["主信息候选 -> 存在节点候选 -> 主键绑定"]
    K -- "虚拟存在" --> W2["主信息候选 -> 存在节点候选 -> 主键绑定 -> 存在到来源引用"]
    K -- "场景" --> W3["主信息候选 -> 场景节点候选 -> 主键绑定"]
    K -- "接纳已有存在" --> W4["场景到已有存在归属关系"]
    K -- "已有场景中创建并接纳" --> W5["按幂等材料创建缺失的实际存在身份和 / 或场景归属"]
    W1 --> V["同会话读回本次主信息、节点、关系和主键绑定"]
    W2 --> V
    W3 --> V
    W4 --> V
    W5 --> V
    V --> Q{"请求提交并完成候选确认"}
    Q -- "已提交" --> U["释放许可；返回值式领域结果和完整稳定句柄"]
    Q -- "提交决定前非成功" --> R
    Q -- "确认后内部不一致" --> X2["追根因解决：不得静默删除已确认结构"]
    R --> R1{"数量快照和可读结构回到前态"}
    R1 -- "否" --> X3["追根因解决：停止后继实施并登记断点"]
    R1 -- "是" --> T{"权威重读是否证明并发形成了同一幂等事实"}
    T -- "是" --> L3
    T -- "否" --> L4["返回结构化失败；无可读半结构"]

    P1["既有存在服务 / 场景服务 / 世界服务兼容入口"] --> P2["原地保留；本轮不接生产调用迁移、不删除或改义"]
```

## 五条固定路径

```text
1. 创建实际存在：幂等主键 -> 主信息候选 -> 存在节点候选 -> 主键绑定 -> 完整读回 -> 提交。
2. 创建虚拟存在：幂等主键 + 来源需求 / 任务 / 方法 -> 存在身份 -> 主键绑定 -> 来源引用最后形成 -> 完整读回 -> 提交。
3. 创建场景：幂等主键 -> 主信息候选 -> 场景节点候选 -> 主键绑定 -> 完整读回 -> 提交。
4. 场景接纳已有存在：已有场景 + 已有存在 -> 场景到存在归属；同一关系已存在时幂等读回。
5. 在已有场景中创建并接纳实际存在：已有场景 + 存在幂等主键 -> 创建缺失的实际存在身份和 / 或归属 -> 完整读回 -> 提交。
```

## 关键边界

```text
业务服务、组合器、线程和消息不得直接导入结构写入执行器，不接触原始令牌、许可、锁或仓库引用。
数据操作层可以私有持有只读仓库引用用于返回值式身份、主键和关系材料，但不得向上层暴露仓库或会话。
会话只复核完整句柄当前可读，不返回节点类型；业务层使用写前值式类型材料，数据操作层在独占会话中以同一完整版本句柄复核当前性。
关系仓库不提供重复关系幂等创建。重复接纳只有在权威读回确认同一当前归属关系后，才可映射为业务幂等成功。
第一轮不同时新建场景和存在，不实现虚拟存在创建并接纳，不收纳状态 / 动态，不迁移既有生产调用。
```
