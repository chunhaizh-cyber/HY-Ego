# NODE-TYPED-MIGRATION NT-P3 服务兼容删除审计迁移施工流程图

更新时间：2026-07-22

## 依据

```text
AGENTS.md
计划/20260722_NODE-TYPED-MIGRATION_节点直接身份与领域类型化持久结构代码修订总计划_v0.2.md
计划/20260722_NODE-TYPED-MIGRATION_NT-P3_服务兼容删除审计收口子计划_v0.2.md
规范/0050_项目通用机器逻辑与禁止性规则总纲_20260721.md
规范/4010_子规范_统一仓库稳定句柄与通用关系索引边界.md
规范/4020_子规范_领域类型化数据记录与组合读取投影边界.md
规范/4030_子规范_基础信息服务分层与领域写授权.md
规范/4040_子规范_不透明结构事务候选确认撤销与最后发布.md
规范/4050_子规范_入口拒绝逻辑内结果与内部逻辑错误.md
规范/4060_子规范_非权威缓存统计失效与确定重建.md
规范/7100_子规范_存在概念与实例创建最小闭环_20260720.md
规范/7130_子规范_自我内部世界成员特征与事实读取投影.md
规范/7140_子规范_概念身份生命周期退役删除与活动投影分账.md
规范/4330_子规范_因果用途观察证据账与阶段推进.md
NT-P1、NT-P2A、NT-P2B、NT-P2C 详细设计形成的隔离新域候选合同
详细设计：规范/详细设计/NODE-TYPED-MIGRATION_NT-P3_服务兼容删除审计迁移详细设计.md
```

## 身份与边界

本图是 NT-P3 的施工目标图。4010、5210、5230、5300、7140 已闭合命名域、关系 16 / 23 和概念分账规则；只有 P1—P2C 实际接口已提交、验证并由设计窗口接受，叶子计划登记并正式派发后，本图才能成为代码施工依据。P3 只能建立具名“节点直接”隔离新域服务、专用数据操作、值式请求适配和新生产候选调用图；默认入口、旧装配、旧服务头、旧生产调用方的切换及物理删除统一由 P4 完成。

## 流程图

```mermaid
flowchart TD
    A["隔离新域调用方或隔离自检形成值式业务请求"] --> B["节点直接值式请求适配：只规范化请求，不持有仓库、令牌或会话"]
    B --> C{"节点稳定主键命名域、请求对象类型、业务语义和全部句柄域身份是否明确"}
    C -->|"否"| LR0["逻辑内返回：拒绝裸主键歧义、旧域句柄、跨域句柄、未知命名域或缺失业务语义；零写入"]
    C -->|"是"| D{"目标对象族"}

    D -->|"存在、场景、系统角色"| A1["NT-P3A 节点直接存在场景服务与专用数据操作"]
    D -->|"语素、基础信息、轻量因果、概念结构"| B1["NT-P3B 节点直接语义概念服务、概念签名参与者与专用数据操作"]
    D -->|"需求、任务、方法、用途观察"| C1["NT-P3C 强类型业务记录与服务；消费关系 16 角色和关系 23"]
    D -->|"删除、失效、审计、索引复判"| D1["NT-P3D 节点直接退役协调器"]

    A1 --> E["领域服务解释业务规则并形成强类型规格"]
    B1 --> E
    C1 --> E
    E --> F["节点直接专用数据操作读取 P1 节点、关系、索引候选及 P2 类型化记录值式合同"]
    F --> G{"预读分类"}
    G -->|"同键同义且全部结构互证"| LR1["逻辑内返回：幂等读回，零写入"]
    G -->|"同键异义、业务前置不足、版本漂移"| LR2["逻辑内返回：冲突或拒绝，零写入"]
    G -->|"许可竞争"| LR3["逻辑内返回：未进入候选，可按协议重试"]
    G -->|"前置满足"| H["取得隔离新域唯一写入权，第一笔写入前锁内重读"]
    H --> I["调用 P1 节点 / 关系 / 索引候选和对应 P2 强类型记录参与者形成共同事务"]
    I --> J["完整读回节点直接身份、正式关系、领域记录、索引复判和业务账"]
    J --> K{"候选、参与者和业务语义是否完整一致"}
    K -->|"否"| IR0["内部逻辑错误：停止新增写入并按依赖逆序精确撤销"]
    IR0 --> IR1{"是否证明恢复事务前态"}
    IR1 -->|"否"| IR2["隔离事务域并追根因；不得释放半结构"]
    IR1 -->|"是"| IR3["返回内部不一致证据；不得降级为普通拒绝"]
    K -->|"是"| L["全部参与者确认待发布"]
    L --> M{"任一确认失败"}
    M -->|"是"| IR0
    M -->|"否"| N["进入无失败最后发布区并释放唯一写入权"]
    N --> O["通过节点直接服务值式读回；索引命中后必须复判权威结构"]

    D1 --> P["对象领域服务形成强类型退役规格，冻结节点、全部当前关系、所属领域记录和索引候选"]
    P --> Q{"冻结材料与权威结构是否完全一致"}
    Q -->|"否"| LR4["逻辑内返回：候选过期、版本漂移或许可拒绝；零变化"]
    Q -->|"是"| R["关系失效、各具名领域记录退役、概念替代关系 10、索引移除和节点删除进入同一共同事务"]
    R --> S["读回节点删除审计、关系历史、领域记录历史和索引不存在"]
    S --> T{"权威退役审计是否完整"}
    T -->|"否"| IR4["内部逻辑错误：停止依赖路径并追根因"]
    T -->|"是"| U["输出强类型权威退役 / 删除回执；不混入缓存命中"]
    U --> V["发布后使活动图、统计和其它非权威投影失效、确定重建并形成独立后验报告"]

    O --> W["NT-P3E 登记逐调用点新生产候选调用图：请求适配到节点直接服务"]
    V --> W
    W --> X["NT-P3-SHARED 只形成隔离装配和聚合自检候选，不写工程或统一运行器"]
    X --> Y{"接口合同、静态隔离、白名单和旧域零互查候选检查是否全部通过"}
    Y -->|"否"| IR5["停止受影响叶子并正式退回，不扩大到默认装配"]
    Y -->|"是"| Z["形成 P3 实现候选：等待真实提供者汇合，不声明编译或能力完成"]
    Z --> P4["交给 #352：唯一工程登记、完整构建自检、一次切换默认装配并物理退役旧结构"]
```

## 关键边界

```text
1. P3 的所有生产候选模块必须具名“节点直接”新域；同形节点句柄必须通过独立仓库类型、互异仓库身份或具名运行域校验机械拒绝跨域，不能只靠调用约定。
2. 请求适配只做值式输入校验、命名域消歧和结果映射；不得暴露或保存仓库、事务令牌、结构会话、参与者和写锁，也不得回读旧域补项。
3. P3 不复制 P1 的节点 / 关系 / 索引算法，不复制 P2 的领域记录；它只在领域服务解释业务规则后，通过专用数据操作编排已有强类型接口。
4. 每个领域记录参与者必须强类型；删除路径不得重新建立“通用记录退役包”或用 variant / 无类型槽容纳不同领域事实。
5. 权威退役在同一事务中覆盖关系失效、具名领域记录退役、索引移除和节点删除；概念删除还必须同事务创建或复用删除后新增的直接关系 10。缓存、活动快照和统计只在发布后失效与重建，不参与权威完成裁决。
6. 4010、5210、5230、5300、7140 已闭合正式规则；对应叶子只有在 P1 / P2 实际接口可机械实现命名域 ABI、关系 16 / 23、概念签名和关系 9—12 时才可派发，不能回用旧槽或影子登记。
7. NT-P3A—P3E 与 P3-SHARED 在文件所有权和预冻结接口合同隔离后并行形成候选；工程 XML 和统一自检运行器只归 #352 最终接线 / 切换计划。
8. P3 只形成可由隔离自检到达的新生产候选调用图。入口.cpp、旧默认装配、旧服务头、旧生产调用方、旧仓库和旧句柄的切换及物理删除只由 P4 完成。
9. 本图不证明 P1—P3 已实施，不证明默认生产已切换，也不证明完整快照恢复或旧材料退役完成。
```
