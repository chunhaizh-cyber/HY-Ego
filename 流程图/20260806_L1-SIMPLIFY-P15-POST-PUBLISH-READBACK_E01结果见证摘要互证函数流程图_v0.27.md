# L1-SIMPLIFY-P15 E01 RP/RW 与 P-I 模块归属流程图 v0.27

更新时间：2026-08-06
依据：P15 v0.27 详细设计；正式基线 `45304d10c0c3d49728b03bacb7d327185e25c2c6`。

```mermaid
flowchart TD
 A["E01 请求"] --> B["形成 node-only RP v1"]
 B --> C["G6: HZY-L1G6→格式→标签→变体"]
 C --> D["变体后只插入一次 RP 原始32字节"]
 D --> E["登记合同/规则/幂等/期望代次"]
 E --> F["provider前探测与P-I/P-L1证据"]
 F -->|同义| G["原样返回首次 RW与结果摘要 v2\nprovider/提交 0/0"]
 F -->|未找到| H["唯一提交：锁内映射并形成 RW v1"]
 H --> I["E01结果摘要 v2：一次RS/U32(2)\n字段载荷+RW原始32字节"]
 I --> J["幂等账/审计/结果见证状态组\n同一候选原子交换"]
 J --> K["发布后权威读回"]
 K -->|一致| L["返回首次结果"]
 K -->|不一致| M["内部不一致+隔离，禁止补记"]
```

## P-I 物理所有权

```mermaid
flowchart LR
 A["P-I DTO\n核心/L1可重建索引.数据.h"] --> B["合同模块只导出 DTO\n核心/合同.L1可重建索引.ixx"]
 B --> C["索引模块导出类声明与成员实现\n核心/索引.L1事实基座.ixx"]
 C --> D["读取提供者身份() const noexcept\n身份0x4C31495800000001\n合同/规则1/1"]
 E["行为消费者\n导入索引模块"] --> C
 F["DTO消费者\n导入合同模块"] --> B
```

`合同.L1可重建索引.ixx` 不声明 `L1事实可重建索引` 的成员；类声明和实现同归 `索引.L1事实基座.ixx`，不使用 PIMPL、自由函数或新旁路接口。24 项代码路径 + 2 条记录仍为完整范围；RP/RW/v1/v2 字节顺序、同义原样返回和恢复按版本重算均继承 v0.26。
