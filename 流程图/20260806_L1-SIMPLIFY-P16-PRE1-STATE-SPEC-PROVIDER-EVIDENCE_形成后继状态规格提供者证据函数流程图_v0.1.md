# L1-SIMPLIFY-P16-PRE1 形成后继状态规格提供者证据函数流程图 v0.1

## 1. 唯一根函数

```cpp
实际存在I64后继状态规格结果 形成实际存在I64后继状态规格(
    L1事实基座服务& L1,
    L1实例特征服务& 实例特征,
    L1状态服务& 状态,
    特征比较服务& 比较,
    const 实际存在I64后继状态规格请求& 请求);
```

## 2. 流程

```mermaid
flowchart TD
    A["校验P13公开请求"] --> B{"请求有效?"}
    B -- "否" --> R0["入口拒绝; 规格/迁移/提供者证据全空"]
    B -- "是" --> C["读取G0完整快照"]
    C --> D["读取后当前实例特征"]
    D --> E["P12选择相邻前状态"]
    E --> F{"P12结果"}
    F -- "无前状态" --> R1["无前状态; 全空载荷"]
    F -- "其它非成功" --> R2["机械映射非成功; 全空载荷"]
    F -- "已选择" --> G["P11比较I64状态迁移"]
    G --> H{"比较完整成功?"}
    H -- "否" --> R3["不可比较/漂移/失败; 全空载荷"]
    H -- "是" --> I["读取G1完整快照"]
    I --> J{"G0、各provider截止、G1同一非零?"}
    J -- "否且首轮" --> C
    J -- "否且第二轮" --> R4["版本漂移; 全空载荷"]
    J -- "是" --> K["状态所有者同锁形成0x53规格+状态登记证据"]
    K --> L{"两项完整且截止一致?"}
    L -- "否" --> R5["内部不一致; 全空载荷"]
    L -- "是" --> M["读取固定P13身份0x4C31535000000001"]
    M --> N["汇集登记/后当前/前状态/选择/比较五类来源"]
    N --> O["抽取写前事实身份; 按种类+身份排序去重"]
    O --> O1["逐项绑定G1当前副本; 仅P12已退出身份允许恰一次历史读取"]
    O1 --> O2["读取末次G2; 互证G1/G2共同截止"]
    O2 --> P{"身份/副本/种类/截止/覆盖/数量完整?"}
    P -- "否" --> R5
    P -- "是" --> Q["返回已形成: 状态规格+迁移证据+P13提供者证据"]
```

## 3. 证据边界

- P16 的直接提供者见证固定一项：P13 身份、合同1、规则1、调用次数1。
- P13 内部 L1/实例特征/P12/P11/状态规格形成调用属于传递边界，不由 P16 猜测身份。
- 写前事实身份只来自最终成功轮次五类强类型材料；G0/G1首项、局部键、日志、默认L1身份均不能补造。
- 每个身份必须绑定同索引完整事实副本；优先G1当前事实，只有P12明确已退出的前状态身份允许历史回退，禁止重建事实。
- 成功身份组/副本组非空且等长，数量字段等于排序去重后的实际长度；非成功提供者证据为空。
- 本函数零写、零P15/P16/P14、无跨provider锁；日志仅为同一生产程序的人读观察。
- 专项复用既有唯一L1状态自检顺序360，不新增顺序582或其它启动登记。
