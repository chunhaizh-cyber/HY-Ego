# L1-SIMPLIFY-P69-SELF-ROOT-CURRENT-VALUE-READ
# 读取当前双轴根值函数流程图 v0.1

更新时间：2026-08-06

## 依据

```text
0050、4015、4190、4200、6100、6120、6130、6170、7130
P17—P26、P29、P56、P65、P68 v0.1 正式详细设计
```

## 身份与边界

正式单函数设计图。只冻结生产读取协调、状态短路和一次完整组合漂移重读；不写机器事实。

## 函数合同

```text
根函数：L1自我根当前值读取服务::读取当前双轴根值
归属模块 / 文件 / 层级：海中鱼巣.领域.服务.L1自我根当前值读取 / 同名ixx / L2
现有或待新增：待新增
完整签名：自我双轴根当前值读取结果 读取当前双轴根值(
    const 自我双轴根当前值读取请求& 请求) const
参数：请求为只读借用；含合同、协调规则、世界规则和指定世界根
返回结果：调用方拥有的值；成功含P68双轴事实，非成功含状态与最早失败阶段
被调函数流程图：复用P56、P19、P21、P17、P24—P26、P68冻结合同；不新增其内部图
```

## 流程图

```mermaid
flowchart TD
    N01{"N01【指令-判断】请求和三个保存定位材料是否完整"} -->|否| N20["N20【指令-返回】返回入口拒绝或内部不一致"]
    N01 -->|是| N02["N02【指令-赋值】轮次设为1"]
    N02 -->|轮次1| N03["N03【函数调用】P56读取完整自我结构(登记身份, 根) -> 自我结果"]
    N03 -->|非成功| N21["N21【指令-返回】映射完整自我阶段结果"]
    N03 -->|已读取| N04["N04【函数调用】P19读取当前世界结构(根) -> 世界结果"]
    N04 -->|非成功| N22["N22【指令-返回】映射世界结构阶段结果"]
    N04 -->|已读取| N05["N05【函数调用】P21读取当前实际存在资格集合(世界事实) -> 存在结果"]
    N05 -->|非成功| N23["N23【指令-返回】映射实际存在资格阶段结果"]
    N05 -->|已读取| N06["N06【函数调用】P17尝试读取完整快照(L1请求) -> 快照结果"]
    N06 -->|非成功或截止不同| N24["N24【指令-返回】映射L1快照阶段结果"]
    N06 -->|成功同截止| N07["N07【函数调用】P24从快照读取当前实例槽(P21事实, 定位, 快照) -> 槽结果"]
    N07 -->|非成功| N25["N25【指令-返回】映射实例特征槽阶段结果"]
    N07 -->|已读取| N08{"N08【指令-判断】实例槽总数是否为0"}
    N08 -->|是| N10["N10【指令-构造】形成空特征定义登记定位"]
    N08 -->|否| N09["N09【函数调用】P22尝试读取当前登记(登记请求) -> 登记结果"]
    N09 -->|非成功| N26["N26【指令-返回】映射特征定义登记阶段结果"]
    N09 -->|已读取| N10
    N10 -->|登记定位| N11["N11【函数调用】P25从快照读取定义详情(槽事实, 登记, 快照) -> 定义结果"]
    N11 -->|非成功| N27["N27【指令-返回】映射特征定义集合阶段结果"]
    N11 -->|已读取| N12["N12【函数调用】P26从快照读取当前I64值(槽, 定义, 快照) -> 当前值结果"]
    N12 -->|非成功| N28["N28【指令-返回】映射实例特征当前值阶段结果"]
    N12 -->|已读取| N13["N13【函数调用】P68组合同代次双轴值(自我, P65锚, 当前值) -> 投影结果"]
    N13 -->|已形成| N19["N19【指令-返回】返回已读取和P68完整事实"]
    N13 -->|非漂移非成功| N29["N29【指令-返回】映射双轴投影阶段结果"]
    N13 -->|事实代次漂移| N14{"N14【指令-判断】当前是否第一轮"}
    N14 -->|否| N30["N30【指令-返回】返回版本漂移"]
    N14 -->|是| N15["N15【指令-销毁】丢弃第一轮全部局部材料"]
    N15 -->|材料已销毁| N16["N16【指令-赋值】轮次设为2"]
    N16 -->|重新完整读取| N03
```

## 节点合同

| 节点 | 类型 | 参数 / 输入绑定 | 结果 / 输出绑定 | 事实读写 | 后继条件 | 验证 |
| --- | --- | --- | --- | --- | --- | --- |
| N03—N06 | 函数调用 | 根、登记身份、上一成功事实 | 自我、世界、存在、L1快照 | 只读 | 成功才继续 | 固定顺序、同截止、短路 |
| N07—N12 | 函数调用 | 同一P17快照及前序事实 | 槽、登记、定义、当前值 | 只读 | 成功才继续 | 空槽跳P22，其余各恰一次 |
| N13 | 函数调用 | P56事实、P65锚、P26事实 | P68投影结果 | 无写入 | 成功、漂移或具名失败 | 不重算P68事实 |
| N14—N16 | 指令 | 第一轮组合漂移 | 第二轮完整重读 | 无写入 | 最多一次 | 零跨轮材料复用 |
| N19—N30 | 返回 | 具名状态和阶段 | 不可变读取结果 | 无写入 | 函数结束 | 非成功零事实 |

## 关键边界

```text
不伪造P21单主体范围；P24—P26只消费同一P17快照。
P22只在非空槽分支调用；P65只作定位锚，不调用P65发布者。
只有P68组合漂移触发一次完整重读；provider非成功立即短路。
不调用P29状态动态链，不形成根需求、6170维护、装配、恢复或正式验收。
```
