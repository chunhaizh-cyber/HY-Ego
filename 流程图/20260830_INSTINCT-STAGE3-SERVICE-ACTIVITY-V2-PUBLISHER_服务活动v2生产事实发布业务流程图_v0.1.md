# INSTINCT-STAGE3-SERVICE-ACTIVITY-V2-PUBLISHER 服务活动 v2 生产事实发布业务流程图 v0.1

日期：2026-08-30

```mermaid
flowchart TD
    A["调用方冻结 G0 与生产材料"] --> B{"请求/材料完整?"}
    B -- "否" --> R1["入口拒绝；零写"]
    B -- "是" --> C{"同幂等身份已有首次写入?"}
    C -- "有且异义" --> R2["幂等冲突；零写"]
    C -- "有且同义" --> JH["按首次映射与首次 G1\n历史闭包正式读回"]
    C -- "无" --> D{"当前代次仍为 G0?"}
    D -- "否" --> R3["当前性漂移；零写"]
    D -- "是" --> E["读取 v2 当前完整集合"]
    E --> F{"同一活动流 0/1/N?"}
    F -- "N>1" --> R4["集合不闭合；零写"]
    F -- "0 但携带预期\n或 1 但预期不符" --> R5["当前事实冲突；零写"]
    F -- "0且无预期\n或1且预期相同" --> G["验证服务合同与全部外部当前引用"]
    G --> H{"引用完整?"}
    H -- "否" --> R6["引用冲突；零写"]
    H -- "是" --> I["一个 owner 写集：退出旧当前闭包 + 建立新事实/载荷/成员/引用关系"]
    I --> K{"提交成功或精确重复?"}
    K -- "否" --> R7["映射许可/漂移/冲突/资源/内部"]
    K -- "是" --> J["冻结 Gread；锁内调用 v2 完整读取主体"]
    J --> L{"新事实逐字段完整\n旧事实不再当前?"}
    JH --> LH{"首次事实在 G1 完整\n历史闭包与首次写集一致?"}
    L -- "否且提交可能已发生" --> R8["已可能发布，仅返回提交见证"]
    L -- "是" --> S["已发布/精确重复 + 完整事实 + G1/Gread"]
    LH -- "否" --> R9["内部错误；零成功载荷"]
    LH -- "是" --> S
```

边界：公开 v2 读取和发布后读取共用同一个“已锁定读取主体”，发布函数不得持锁递归调用公开入口。本图只形成 v2 生产事实；它不证明真实生产调用已接线，不把空集合解释为无活动，不执行安全门禁或单秒维护，不修改 L/H。
