# 探测方法登记根首次发布函数流程图 v0.1

对应计划：`L1-SIMPLIFY-METHOD-ROOT-R10-FIRST-PROBE v0.1`

唯一根函数：
`L1方法登记根首次发布探测服务::探测方法登记根首次发布`

```mermaid
flowchart TD
    A["接收R10请求：R4公开意图与R5最终键"] --> B{"R10/R4/R5版本和成功载荷完整？"}
    B -- "否：入口问题" --> R1["入口拒绝；P15调用0；材料空"]
    B -- "否：成功载荷矛盾" --> R2["内部不一致；P15调用0；材料空"]
    B -- "是" --> C{"G10/格式1/标签/空结果计划/公开身份一致？"}
    C -- "否" --> R2
    C -- "是" --> D{"R5键高8位0x19且低56位回到公开身份？"}
    D -- "否" --> R2
    D -- "是" --> E["形成唯一L1幂等探测请求：合同2+当前摘要合同+R5键+R4摘要/凭证"]
    E --> F["调用P15 探测幂等恰一次；此前R8/provider/当前事实读取0"]
    F --> G{"P15结构化状态"}
    G -- "未找到" --> H{"同键且首次结果空？"}
    H -- "是" --> S1["返回未找到+原请求/结果；只此分支允许后继R8再R9"]
    H -- "否" --> R2
    G -- "同义已发布" --> I{"同键、首次结果存在、非隔离、代次和摘要非零？"}
    I -- "是" --> S2["返回同义已发布+完整原始材料；交独立领域解释叶；R8/R9调用0"]
    I -- "否" --> R2
    G -- "异义冲突" --> J{"同键且首次结果空？"}
    J -- "是" --> S3["返回异义冲突；材料空；零写收口"]
    J -- "否" --> R2
    G -- "已隔离" --> K{"回显键一致？"}
    K -- "是" --> S4["返回已隔离；材料空；零写收口"]
    K -- "否" --> R2
    G -- "入口拒绝" --> R1
    G -- "内部不一致或未知" --> R2
    F -. "bad_alloc" .-> R3["资源失败；材料空"]
    F -. "其它异常" .-> R2
```

## 边界

- 本图只有一个 Mermaid 块和一个 `flowchart TD` 根。
- 未找到不保证后继提交仍无竞争；最终同键竞争仍由 P15 提交事务裁决。
- 同义材料不等于方法登记根领域读回；R2/R1 角色与五项读回解释不在本函数。
- 全部分支均不形成 R8/provider 证据、R9 执行请求、稳定编码、事务、提交、发布或读回。
- 日志只供同一生产程序人读观察，不参与任何判断。
