# F-W106 准备特征批次记录提交单函数施工流程图

更新时间：2026-07-29

## 依据

```text
规范/4170_子规范_特征批次发布记录与幂等账.md
海中鱼巣/核心/会话.节点直接身份结构写入.ixx
规范/详细设计/NODE-TYPED-MIGRATION_NT-P2A_特征值类型化记录与当前关系迁移详细设计.md
```

## 身份与边界

本图是待新增事务参与者准备函数的正式单函数施工流程图。它逐节点消费核心准备只读视图，随后在批次仓独占锁内完成最终同键竞争检查和无分配 splice。

## 函数合同

```text
根函数：
节点直接身份结构写入结果
节点直接特征批次记录参与者::准备提交(
    const 节点直接身份结构提交准备只读视图& 视图)

归属模块 / 文件 / 层级：
海中鱼巣.领域.参与者.节点直接特征批次记录
海中鱼巣/领域/参与者.节点直接特征批次记录.ixx
private override

参数：
视图，执行器调用期只读借用；不得保存。

返回：
准备成功：{候选已确认,0,0,0}；
已发布同键同义：{版本漂移,0,0,0}；
已发布同键异义：{身份冲突,0,0,0}；
锁竞争：{许可拒绝,0,0,0}；
候选节点、阶段或结构异常：入口拒绝或内部不一致；
版本溢出：{资源失败,0,0,0}。
```

## 流程图

```mermaid
flowchart TD
    N01{"N01【指令-判断】阶段是否已登记且私有待插入条目组恰一条未发布记录"} -- 否 --> N02["N02【指令-返回】返回 {内部不一致,0,0,0}"]
    N01 -- 是 --> N03["N03【指令-赋值】把私有唯一 list 节点稳定迭代器保存到局部预备反向位置"]
    N03 --> N04["N04【指令-赋值】项目位置置为0"]
    N04 --> N05{"N05【指令-判断】项目位置是否等于项目数量"}
    N05 -- 是 --> X03["X03【函数调用】F-W302(3)"]
    X03 --> N18A["N18A【指令-赋值】以 noexcept 赋值把局部预备反向位置写入成员反向位置_"]
    N18A --> X01["X01【函数调用】F-W311(1) -> 是否命中"]
    X01 --> X02{"X02【指令-判断】是否命中"}
    X02 -- 是 --> N02
    X02 -- 否 --> N20["N20【指令-操作】以 try_to_lock 尝试取得批次仓独占锁"]
    N05 -- 否 --> N06["N06【指令-借用】const auto& 当前项目=记录.项目组[项目位置]"]
    N06 --> N07["N07【函数调用】视图.节点是本会话候选(当前项目.新特征值) -> 新值是候选"]
    N07 --> N08["N08【函数调用】视图.读取候选节点类型(当前项目.新特征值) -> 新值类型"]
    N08 --> N09["N09【函数调用】视图.读取候选节点稳定主键(当前项目.新特征值) -> 新值主键"]
    N09 --> N10{"N10【指令-判断】新值是候选且类型=特征值且主键命名域=特征值身份"}
    N10 -- 否 --> N11["N11【指令-返回】返回 {入口拒绝,0,0,0}"]
    N10 -- 是 --> N12{"N12【指令-判断】当前项目是否为初始项目"}
    N12 -- 否 --> N19["N19【指令-赋值】项目位置加1"]
    N12 -- 是 --> N13["N13【函数调用】视图.节点是本会话候选(当前项目.实例槽) -> 槽是候选"]
    N13 --> N14["N14【函数调用】视图.读取候选节点类型(当前项目.实例槽) -> 槽类型"]
    N14 --> N15["N15【函数调用】视图.读取候选节点稳定主键(当前项目.实例槽) -> 槽主键"]
    N15 --> N16{"N16【指令-判断】槽是候选且类型=特征且主键命名域=实例特征槽身份"}
    N16 -- 否 --> N11
    N16 -- 是 --> N19
    N19 --> N05
    N20 --> N21{"N21【指令-判断】是否取得独占锁"}
    N21 -- 否 --> N22["N22【指令-操作】清空反向位置"]
    N22 --> N22A["N22A【指令-返回】返回 {许可拒绝,0,0,0}"]
    N21 -- 是 --> N23{"N23【指令-判断】批次仓结构版本是否小于 uint64 最大值"}
    N23 -- 否 --> N24["N24【指令-操作】释放独占锁"]
    N24 --> N24A["N24A【指令-操作】清空反向位置"]
    N24A --> N24B["N24B【指令-返回】返回 {资源失败,0,0,0}"]
    N23 -- 是 --> N34{"N34【指令-判断】批次仓内是否存在任何未发布条目"}
    N34 -- 是 --> N27["N27【指令-操作】释放独占锁"]
    N27 --> N27A["N27A【指令-操作】清空反向位置"]
    N27A --> N27B["N27B【指令-返回】返回 {内部不一致,0,0,0}"]
    N34 -- 否 --> N25["N25【指令-赋值】仓扫描位置=条目组_.begin()、已发布同键数量=0、唯一位置=空"]
    N25 --> N25A{"N25A【指令-判断】仓扫描位置是否等于条目组_.end()"}
    N25A -- 否 --> N25B["N25B【指令-借用】当前仓条目=*仓扫描位置"]
    N25B --> N25C{"N25C【指令-判断】当前条目已发布且批次身份等于候选批次身份"}
    N25C -- 是 --> N25D["N25D【指令-操作】已发布同键数量加一并令唯一位置=仓扫描位置"]
    N25C -- 否 --> N25E["N25E【指令-推进】仓扫描位置加一"]
    N25D --> N25E
    N25E --> N25A
    N25A -- 是 --> N26{"N26【指令-判断】已发布同键数量是否为0、1或大于1"}
    N26 -- 大于1 --> N27
    N26 -- 1 --> N28{"N28【指令-判断】已发布同键记录是否逐字段同义"}
    N28 -- 是 --> N29["N29【指令-操作】释放独占锁"]
    N29 --> N29A["N29A【指令-操作】清空反向位置"]
    N29A --> N29B["N29B【指令-返回】返回 {版本漂移,0,0,0}"]
    N28 -- 否 --> N30["N30【指令-操作】释放独占锁"]
    N30 --> N30A["N30A【指令-操作】清空反向位置"]
    N30A --> N30B["N30B【指令-返回】返回 {身份冲突,0,0,0}"]
    N26 -- 0 --> N31["N31【指令-操作】把私有唯一 list 节点 splice 到批次仓条目组"]
    N31 --> N32["N32【指令-赋值】阶段置已准备"]
    N32 --> N33["N33【指令-返回】保持独占锁并返回 {候选已确认,0,0,0}"]
```

## 节点合同

| 节点 | 类型 | 输入绑定 | 输出 | 事实读写 | 后继 | 验证 |
| --- | --- | --- | --- | --- | --- | --- |
| N07/N13 | 函数调用 | 候选完整句柄 -> `节点` | 候选布尔 | 只读核心候选 | 完成 | W30/W31 |
| N08/N14 | 函数调用 | 同一候选句柄 -> `节点` | `optional<节点类型>` | 只读核心候选 | 完成 | W30/W31 |
| N09/N15 | 函数调用 | 同一候选句柄 -> `节点` | `optional<节点稳定主键>` | 只读核心候选 | 完成 | W30/W31 |
| N20 | 指令-操作 | 批次仓互斥 | 独占锁 | 取得私有仓许可 | 成功/失败 | W32 |
| N25/N28 | 指令-读取/判断 | 仓条目、输入记录 | 数量和同义布尔 | 只读已发布仓条目 | 分支 | W33/W34 |
| N31 | 指令-操作 | 私有 list 节点、仓 list | 稳定迭器仍有效 | 私有候选移入仓，未发布 | 完成 | W35 |
| 返回节点 | 指令-返回 | 具名核心分类 | 四字段核心结果 | 失败不留下仓候选 | 终止 | W30—W35 |

## 关键边界

```text
三个准备视图入口必须逐个调用，不得由句柄外观或调用方请求代替核心候选身份。
换代项目的实例槽是已发布节点，不得要求它是本会话候选；初始项目必须要求实例槽和新值都为本会话候选。
N06 只建立 `const&` 借用，不复制项目。进入仓锁前已完成全部可能分配；全部视图校验通过后才以 noexcept 赋值写成员反向位置，后续每条失败边都清空该成员；持锁分支只有线性扫描、逐字段比较和 list::splice。
同义竞争不返回幂等成功，因为事务外或回调第一写前应已收口；此处精确返回核心版本漂移以触发整体撤销。
```
