# CONCEPT-S5 活动图发布与抽象树投影现状流程图

更新时间：2026-07-11

## 元数据

```text
图类型：现状流程图
代码版本：当前 HEAD aed6db5 之后的当前工作区；目标文件 海中鱼巣/领域/概念图服务.h 无未提交改动
覆盖文件：
  海中鱼巣/领域/概念图服务.h
覆盖入口：
  初始化活动图基础版本
  活动快照公开读取组
  发布候选图版本
  候选图可发布_已加锁
  构造候选活动快照_已加锁
  清理未发布候选_已加锁
  验证活动快照_已加锁
  失效旧活动关系_已加锁
  读取抽象树视图
  读取全部抽象树视图
  生成抽象树视图
逐行映射表：实施记录/20260711_CONCEPT-S5活动图发布与抽象树投影逐行代码映射表.md
输入契约表：实施记录/20260711_CONCEPT-S5活动图发布与抽象树投影输入契约与调用语境表.md
非成功审查表：实施记录/20260711_CONCEPT-S5活动图发布与抽象树投影非成功返回二分审查表.md
不得作为施工许可：是；代码修订只允许按登记计划执行
```

## 现状边界

本图依据当前未显示为脏改动的 `海中鱼巣/领域/概念图服务.h` 绘制。`入口.cpp` 和 `控制面板服务.h` 正在发生未提交的 PANEL-TREES-S1 改动，本图只把它们作为调用语境证据，不映射其施工中代码行，也不把控制面板接线写成 CONCEPT-S5 已完成事实。

原 `流程图/20260711_概念图自动生长与抽象关系树形视图流程图_v0.1.md` 是施工流程图；本图不覆盖它，只记录 S5 当前代码怎样实际分支和返回。

## 流程图

```mermaid
flowchart TD
    A["S5-01 发布请求：候选图 + 四类原子服务"] --> B["S5-02 初始化活动图基础版本"]
    B --> C{"S5-03 四类根是否恰为 4 个？"}
    C -- "否" --> R1["现状：空返回；正式初始化语境应追根因"]
    C -- "是" --> D{"S5-04 活动快照已存在？"}
    D -- "是" --> OK1["成功返回当前活动版本"]
    D -- "否" --> E["S5-05 组装版本 1：四根、根签名、既有根支持关系"]
    E --> F["S5-06 规范化并完整验证基础快照"]
    F -- "失败" --> RC1["追根因解决：基础快照不符合内部预期"]
    F -- "成功" --> G["S5-07 写入活动快照并返回版本 1"]
    G --> H["S5-08 取得活动写锁"]
    OK1 --> H
    H --> I{"S5-09 快照是否仍存在？"}
    I -- "否" --> R2["现状：空返回；初始化成功后的正式语境应追根因"]
    I -- "是" --> J{"S5-10 与上一候选内容等价？"}
    J -- "是" --> OK2["正常成功：幂等返回当前版本"]
    J -- "否" --> K{"S5-11 基准版本是否过期？"}
    K -- "是" --> L1["逻辑内返回：过期候选写前拒绝，结构不变"]
    K -- "否" --> L{"S5-12 候选是否可发布？"}
    L -- "未收敛或空候选" --> L2["逻辑内返回：候选材料尚不可发布"]
    L -- "已收敛但编号、签名、根、支持、定义或直接边不合法" --> R3["现状：空返回；S4 正式契约语境应追根因"]
    L -- "是" --> M["S5-13 保存旧快照和登记数量，开始构造新快照"]
    M --> N["S5-14 复用根或旧签名概念；否则调用四类原子服务创建概念"]
    N --> O{"S5-15 创建后句柄和类别读回符合预期？"}
    O -- "否" --> RC2["现状经空返回进入清理；本质为追根因解决"]
    O -- "是" --> P["S5-16 建立候选编号到概念句柄映射"]
    P --> Q["S5-17 复用根支持关系；依次确保实例支持、定义材料、直接上下位关系"]
    Q --> R{"S5-18 映射、关系写入或关系读回失败？"}
    R -- "是" --> RC3["追根因解决：逆序清理本轮关系和节点；清理失败继续追根因"]
    R -- "否" --> S["S5-19 规范化新快照并完整读回验证"]
    S -- "失败" --> RC4["追根因解决：清理未发布结构并停止依赖路径"]
    S -- "成功" --> T["S5-20 原子替换服务内活动快照并保存上一候选"]
    T --> U["S5-21 后置失效旧活动关系"]
    U -- "失败" --> RC5["追根因解决：现状已切新快照但返回空，存在失败收口风险"]
    U -- "成功" --> OK3["正常成功：返回新活动版本"]
    V["TREE-01 读取单树或全部四树"] --> W{"TREE-02 活动快照和四根是否存在？"}
    W -- "否；任意请求语境" --> L3["逻辑内返回：尚无可读活动图"]
    W -- "否；正式 PANEL 契约语境" --> R4["现状空返回；上游已保证初始化时应追根因"]
    W -- "是" --> X{"TREE-03 根句柄有效且为活动根，预算大于 0？"}
    X -- "否" --> L4["逻辑内返回：拒绝根或预算请求材料"]
    X -- "是" --> Y["TREE-04 从根开始深度遍历，只沿活动上下位关系"]
    Y --> Z["TREE-05 记录同一概念句柄、完整路径、深度和重复投影标记"]
    Z --> AA{"TREE-06 路径为空、活动图成环或最终视图不完整？"}
    AA -- "是" --> R5["现状空返回；已验证活动快照语境应追根因"]
    AA -- "否" --> AB{"TREE-07 超过调用方节点或深度预算？"}
    AB -- "是" --> L5["逻辑内返回：预算耗尽，不改活动结构"]
    AB -- "否" --> AC["正常成功：返回单树或四树只读值材料"]
```

## 审查结论

当前代码已经对基础快照验证失败、候选写后读回失败、清理失败和旧关系失效失败调用 `追根因检查`。但仍有三组“同一个空返回混合两种语境”的路径：

1. 初始化前探测与正式初始化契约混合。
2. 未收敛/过期候选与已收敛但内部结构错误的 S4 候选混合。
3. 任意树读取请求与 PANEL 正式读取契约混合。

因此，现状不能只按“结构是否改变”分类。正式上游已经保证有效时，即使尚未写结构，返回非预期结果也必须追根因。

## 完成边界

本图只证明已按当前代码建立 S5 现状流程、逐行映射、调用语境和非成功返回审查。它不证明代码已经纠偏，不证明 PANEL-TREES-S1 已完成，也不证明命名、生命周期、安全删除、跨重启恢复、自我循环或自我苏醒完成。
