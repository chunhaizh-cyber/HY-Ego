# SAFETY-P1 F-S108 提交安全维护候选单函数施工流程图 v0.1

日期：2026-07-29

函数身份：F-S108

归属模块 / 文件：`海中鱼巣.领域.数据操作.分层安全` / `海中鱼巣/领域/数据操作.分层安全.ixx`

完整签名：

```cpp
安全维护结果 分层安全数据操作::提交安全维护候选(
    const 安全维护请求& 请求,
    const 安全维护载荷& 候选,
    std::optional<安全维护外部事实参与载荷>&& 外部参与) noexcept;
```

返回结果：`安全维护结果`

```mermaid
flowchart TD
    S["S【本函数内具体指令】接收请求、候选和可选外部参与载荷"]
    B01{"B01【本函数内具体指令】请求/候选版本一致，值变化与外部包存在性一致？"}
    R01["R01【本函数内具体指令】返回入口拒绝或版本漂移且零写入"]
    B02{"B02【本函数内具体指令】外部包、候选代次、来源版本和参与者视图完整且无空/重复？"}
    R02["R02【本函数内具体指令】返回入口拒绝或版本漂移且零写入"]
    C03["C03【调用具名函数】形成分层安全维护记录参与者(仓库, 请求, 候选) -> 记录参与结果"]
    B04{"B04【本函数内具体指令】记录参与者已形成？"}
    R04["R04【本函数内具体指令】返回记录形成分类且零可读变化"]
    B05{"B05【本函数内具体指令】本次有实际值变化和外部事务包？"}
    N06["N06【本函数内具体指令】按安全记录参与者在前、外部包参与者视图在后形成原始指针 span；所有权仍在两个 move-only 包内"]
    C07["C07【调用具名函数】核心执行器.执行(会话回调, 参与者span) -> 核心结果；回调先调用外部包.写入核心候选(会话)，完整后再请求提交"]
    C08["C08【调用具名函数】核心执行器.执行仅参与者事务(只含安全记录参与者的span) -> 核心结果"]
    B09{"B09【本函数内具体指令】核心结果分类？"}
    R09["R09【本函数内具体指令】按冻结映射返回无载荷失败"]
    C10["C10【调用具名函数】读取已发布分层安全维护结果(仓库, 请求) -> 安全读回"]
    B11{"B11【本函数内具体指令】安全读回与候选逐字段相同？"}
    R11["R11【本函数内具体指令】返回内部不一致且不得撤销已发布事实"]
    B12{"B12【本函数内具体指令】本次有实际值变化？"}
    C13["C13【调用具名函数】外部包.读取发布后事实() -> 外部读回"]
    B14{"B14【本函数内具体指令】后值/后状态/动态/稳定主键/可选来源批次/代次/版本全部精确匹配？"}
    R14["R14【本函数内具体指令】返回内部不一致且不得伪回滚"]
    N15["N15【本函数内具体指令】核心幂等读回映射精确重复，否则保留候选成功分类"]
    R16["R16【本函数内具体指令】返回权威安全维护读回"]
    E17["E17【本函数内具体指令】捕获资源异常并返回资源失败；已发布后异常按内部不一致收口"]

    S --> B01
    B01 -- "否" --> R01
    B01 -- "是" --> B02
    B02 -- "否" --> R02
    B02 -- "是/无外部包" --> C03 --> B04
    B04 -- "否" --> R04
    B04 -- "是" --> B05
    B05 -- "是" --> N06 --> C07 --> B09
    B05 -- "否" --> C08 --> B09
    B09 -- "非提交/非幂等" --> R09
    B09 -- "已提交或幂等读回" --> C10 --> B11
    B11 -- "否" --> R11
    B11 -- "是" --> B12
    B12 -- "否" --> N15
    B12 -- "是" --> C13 --> B14
    B14 -- "否" --> R14
    B14 -- "是" --> N15 --> R16
    S -. "分配异常" .-> E17
```

## 节点合同

| 节点 | 类型 | 输入 / 输出 | 事务与生命周期 | 验证 |
| --- | --- | --- | --- | --- |
| B01/B02 | 本函数内具体指令 | 候选、包、代次、版本、span | 第一笔写前拒绝 | SP-A12/A19 |
| C03 | 被调函数 | 安全领域记录候选 | 返回独占参与者 | SP-A12 |
| B05 | 本函数内具体指令 | 实际变化必须有包；无变化/精确重复必须无包 | 第一笔写前选定唯一事务入口 | SP-A12/A19 |
| N06 | 本函数内具体指令 | 两个所有权包 -> 临时 span | span 不逃逸；包活到读回结束 | move-only 静态断言 |
| C07 | 被调函数 | 核心候选回调和参与者 span | 回调先形成节点/关系/索引候选，再统一准备/确认/发布；失败逆序撤销 | SP-A12 |
| C08 | 被调函数 | 只含安全记录参与者的非空 span | 消费 CORE-C1/v1，仅参与者事务零核心写集但仍统一确认、发布或撤销 | SP-A12/A19 |
| C10/C13 | 被调函数 | 安全记录与外部事实权威读回 | 仅在提交/幂等后读取 | SP-A12/A19 |
| B14 | 本函数内具体指令 | 三个请求稳定主键与三个已发布句柄、值、来源批次、代次和版本 | 不接受半结构 | SP-A12 |
| R11/R14 | 本函数内具体指令 | 发布后不一致 | 内部错误，不重新撤销 | SP-A12 |

外部包内部的参与者可以借用包内候选身份材料；包和记录参与者必须覆盖核心执行、最后发布及两路发布后读回的完整生命周期。
