# ARCH-L1-OWNER-PORT-REISSUANCE 所有者范围写端口重签发流程图

版本：v0.1
对应详细设计：`规范/详细设计/20260824_ARCH-L1-OWNER-PORT-REISSUANCE_所有者范围写端口重签发中性恢复提供者详细设计_v0.1.md`
图类型：单一 L1 管理函数；不表示现有代码已实现

```mermaid
flowchart TD
    A["L1所有者范围签发器::重新签发所有者范围写端口"]
    B{"合同版本、owner、建立幂等身份有效?"}
    C["入口拒绝；无端口；代次0"]
    D{"运行包状态有效?"}
    E["许可拒绝；无端口"]
    F["取得端口锁"]
    G{"owner当前且独占、建立账与owner逐字段互证?"}
    H["未找到/已退出/引用冲突/内部不一致；零事实变化"]
    I{"同owner已有活动端口?"}
    J["许可拒绝；不交付第二端口"]
    K["构造唯一move-only端口并登记活动owner"]
    L["成功；不推进事实代次；返回当前安全代次与owner事实"]
    A --> B
    B -- 否 --> C
    B -- 是 --> D
    D -- 否 --> E
    D -- 是 --> F --> G
    G -- 否 --> H
    G -- 是 --> I
    I -- 是 --> J
    I -- 否 --> K --> L
```

重签发不建立 owner、不修改事实、不调用 L2，不包含任务或业务分支。退出与重签发共享“端口锁→仓库管理验证/变更”的顺序。
