# SERVICE-P1 F-V213 `服务合同服务::建立或读取服务合同` 单函数施工流程图 v0.1

图类型：目标施工流程图
状态：现行设计依据；函数待 #379 实现，不表示代码已存在
归属模块：`海中鱼巣.领域.服务.服务合同`
归属文件：`海中鱼巣/领域/服务.服务合同.ixx`
唯一根函数：F-V213
完整签名：

```cpp
服务合同结果 服务合同服务::建立或读取服务合同(
    const 服务合同建立请求& 请求) noexcept;
```

本函数只借用构造注入的合同事实提供者、生存事实提供者、外部参与提供者和数据操作。全部首次读取条件来自公开请求；合同、结算、服务值等新身份和新版本只允许来自事实提供者返回的稳定写入身份版本材料，不得由服务、算法、仓库或执行器生成。依据 0050、6160、6170 与服务详细设计 v0.3 第 6—8、10 节。

## 流程图

```mermaid
flowchart TD
    S["S【本函数内具体指令】只读绑定服务合同建立请求"]
    B01{"B01【本函数内具体指令】幂等主键、自我、提出者、需求、全部期望需求/值/定义/合同集合/准备集合/进展/主动安全/纪元/代次版本、时间、截止及三个规则版本全部有效？"}
    R01["R01【本函数内具体指令】返回入口拒绝且无载荷"]
    B02{"B02【本函数内具体指令】显式有效期适用性与可选秒成对一致且秒数合法可表示？"}
    R02["R02【本函数内具体指令】返回结构拒绝且无载荷"]
    N03["N03【本函数内具体指令】逐字段形成合同建立用途的服务合同事实来源请求：自我/提出者/需求和期望需求版本适用，其它单合同/准备身份版本不适用，期望合同/准备集合版本、完整秒截止、事实截止和合同规则版本原值复制"]
    C04["C04【函数调用】服务合同事实提供者::读取服务合同事实(合同来源请求) const"]
    B05{"B05【本函数内具体指令】合同来源结果为已形成且有载荷？"}
    R05["R05【本函数内具体指令】结果/载荷配对错误返回内部不一致；其它分类原样无载荷返回"]
    N06["N06【本函数内具体指令】逐字段形成合同建立用途的服务生存事实来源请求：自我、期望服务值/安全值/定义/进展集合/主动安全截止/时间纪元/运行代次、单调时间、事实截止和时间/回归规则版本原值复制"]
    C07["C07【函数调用】服务生存事实提供者::读取服务生存事实(生存来源请求) const"]
    B08{"B08【本函数内具体指令】生存来源结果为已形成且有载荷？"}
    R08["R08【本函数内具体指令】结果/载荷配对错误返回内部不一致；其它分类原样无载荷返回"]
    B09{"B09【本函数内具体指令】两载荷的读取用途、自我、提出者/需求、时间、全部值/定义/集合/主动安全/纪元/代次/截止/规则版本与请求逐字段回显相等，合同建立写入身份版本材料适用且完整？"}
    R09["R09【本函数内具体指令】身份不等返回内部不一致；版本/截止不等返回版本漂移；材料缺失返回材料缺失，均无载荷"]
    C10["C10【函数调用】F-V202 计算服务合同建立候选(合同事实载荷,生存事实载荷,请求)"]
    B11{"B11【本函数内具体指令】算法结果分类与载荷配对？"}
    R11["R11【本函数内具体指令】非已结算/精确重复分类原样无载荷返回；配对错误返回内部不一致"]
    B12{"B12【本函数内具体指令】算法结果为新合同已结算还是精确重复？"}
    N13["N13【本函数内具体指令】只用两事实载荷材料和新候选形成合同+预支外部规格：候选发布代次=合同建立材料.发布代次，输入版本逐字段取请求/回显，方法绑定组取定义中合同建立方法；禁止生成身份或版本"]
    C14["C14【函数调用】服务生存外部事实参与提供者::形成服务生存外部事实参与包(外部事实规格)"]
    B15{"B15【本函数内具体指令】外部参与结果为已形成且有完整move-only载荷，候选代次、来源版本、方法绑定组与规格逐字段相等？"}
    R15["R15【本函数内具体指令】配对或回显错误返回内部不一致/版本漂移；其它分类原样无载荷返回"]
    N16["N16【本函数内具体指令】新合同移动外部参与载荷；精确重复形成空外部参与"]
    C17["C17【函数调用】F-V211 服务生存数据操作::提交服务合同候选(请求,算法结果.分类,*算法结果.载荷,move(外部参与))"]
    R18["R18【本函数内具体指令】原样返回数据操作的权威发布后结果"]
    E19["E19【本函数内具体指令】捕获bad_alloc返回资源失败；捕获其它意外异常返回内部不一致；均无载荷"]
    S --> B01
    B01 -- "否" --> R01
    B01 -- "是" --> B02
    B02 -- "否" --> R02
    B02 -- "是" --> N03 --> C04 --> B05
    B05 -- "否" --> R05
    B05 -- "是" --> N06 --> C07 --> B08
    B08 -- "否" --> R08
    B08 -- "是" --> B09
    B09 -- "否" --> R09
    B09 -- "是" --> C10 --> B11
    B11 -- "失败或坏配对" --> R11
    B11 -- "成功且有载荷" --> B12
    B12 -- "已结算" --> N13 --> C14 --> B15
    B15 -- "否" --> R15
    B15 -- "是" --> N16
    B12 -- "精确重复" --> N16
    N16 --> C17 --> R18
    N03 -. "分配异常" .-> E19
    N13 -. "分配异常" .-> E19
```

## 节点合同

| 节点 | 类型 | 输入 | 输出 | 调用/事实边界 |
| --- | --- | --- | --- | --- |
| B01—N03 | 本函数内具体指令 | 公开请求 | 合同来源请求 | 全字段显式复制；零写入 |
| C04—N06 | 函数调用/具体指令 | 合同来源请求与结果 | 生存来源请求 | 合同提供者恰好一次 |
| C07—B09 | 函数调用/具体指令 | 生存来源请求与双载荷 | 闭合事实快照 | 生存提供者恰好一次 |
| C10—B12 | 函数调用/具体指令 | 双事实载荷、请求 | 值式候选 | F-V202 恰好一次 |
| N13—C15 | 具体指令/函数调用 | 新候选、提供者身份版本材料 | move-only 外部参与 | 新候选时外部提供者恰好一次 |
| N16—R18 | 具体指令/函数调用 | 候选、可选外部参与 | 发布后权威结果 | F-V211 恰好一次 |

## 实现约束

1. 两个事实提供者各调用一次；不得为补字段、重试或“刷新最新”二次读取。
2. 公开请求的每个首次读取字段必须原值进入对应来源请求，并由载荷逐字段回显；身份回显不等与版本回显不等必须分账。
3. F-V202 不得生成稳定身份/版本；新合同、结算、服务值与发布所需身份版本只取合同/生存事实载荷的写入材料。
4. 精确重复不调用外部参与提供者，但仍调用 F-V211，以记录参与者和发布后读回形成权威 `精确重复`。
5. 本函数不直接读取仓库、不持锁、不调用执行器、不返回算法临时候选。
