// 文件规则：只组织 NT-C4 固定夹具、故障点和静态合同检查，不进入生产依赖、不登记工程或运行器。
module;

#include "统一权威快照.数据.h"

#include <array>
#include <cstdint>

export module 海中鱼巣.核心.自检.统一权威冻结;

import 海中鱼巣.核心.适配.统一权威冻结;
import 海中鱼巣.核心.冻结.统一权威结构;
import 海中鱼巣.核心.序列化.统一权威快照;

export namespace 海中鱼巣 {

enum class 统一权威冻结故障点 : std::uint8_t {
    无 = 0,
    来源运行期域不一致 = 1,
    来源仓库不一致 = 2,
    快照代次不一致 = 3,
    缺少必需段 = 4,
    跨段互证失败 = 5,
    规范化复读不一致 = 6,
    临时文件刷新失败 = 7,
    原子替换失败 = 8
};

struct 统一权威冻结固定夹具 {
    统一冻结规格 规格;
    统一冻结提供者组 提供者;
};

struct 统一权威冻结静态检查结果 {
    std::uint32_t 格式版本 = 统一权威容器格式版本;
    std::uint32_t 段数量 = 9;
    std::uint32_t 段头字节数 = 统一权威段头字节数;
    std::uint32_t 容器头字节数 = 统一权威容器头字节数;
    std::array<统一权威冻结故障点, 8> 故障点组 = {
        统一权威冻结故障点::来源运行期域不一致,
        统一权威冻结故障点::来源仓库不一致,
        统一权威冻结故障点::快照代次不一致,
        统一权威冻结故障点::缺少必需段,
        统一权威冻结故障点::跨段互证失败,
        统一权威冻结故障点::规范化复读不一致,
        统一权威冻结故障点::临时文件刷新失败,
        统一权威冻结故障点::原子替换失败
    };
};

enum class 统一权威冻结固定夹具结果状态 : std::uint8_t {
    已形成并编码 = 1,
    冻结入口拒绝 = 2,
    冻结内部不一致 = 3,
    编码内部不一致 = 4
};

struct 统一权威冻结固定夹具结果 {
    统一权威冻结固定夹具结果状态 状态 = 统一权威冻结固定夹具结果状态::冻结入口拒绝;
    std::uint32_t 必需段数量 = 0;
    std::uint64_t 编码字节数 = 0;
};

统一权威冻结固定夹具 构造统一权威冻结固定夹具() {
    统一权威冻结固定夹具 夹具;
    夹具.规格.来源运行期域身份 = 0x1001;
    夹具.规格.来源节点直接仓库身份 = 0x2001;
    夹具.规格.快照代次 = 1;

    夹具.提供者.冻结权仍有效 = [] { return true; };
    夹具.提供者.读取节点直接记录段 = [规格 = 夹具.规格] {
        节点直接记录段 段; 段.来源 = { 规格.来源运行期域身份, 规格.来源节点直接仓库身份, 规格.快照代次 }; return 段;
    };
    夹具.提供者.读取正式关系记录段 = [规格 = 夹具.规格] {
        正式关系记录段 段; 段.来源 = { 规格.来源运行期域身份, 规格.来源节点直接仓库身份, 规格.快照代次 }; return 段;
    };
    夹具.提供者.读取特征值类型化记录段 = [规格 = 夹具.规格] {
        特征值类型化记录段 段; 段.来源 = { 规格.来源运行期域身份, 规格.来源节点直接仓库身份, 规格.快照代次 }; return 段;
    };
    夹具.提供者.读取状态域记录段 = [规格 = 夹具.规格] {
        状态域记录段 段; 段.来源 = { 规格.来源运行期域身份, 规格.来源节点直接仓库身份, 规格.快照代次 }; return 段;
    };
    夹具.提供者.读取动态域记录段 = [规格 = 夹具.规格] {
        动态域记录段 段; 段.来源 = { 规格.来源运行期域身份, 规格.来源节点直接仓库身份, 规格.快照代次 }; return 段;
    };
    夹具.提供者.读取任务方法选择记录段 = [规格 = 夹具.规格] {
        任务方法选择记录段 段; 段.来源 = { 规格.来源运行期域身份, 规格.来源节点直接仓库身份, 规格.快照代次 }; return 段;
    };
    夹具.提供者.读取概念签名记录段 = [规格 = 夹具.规格] {
        概念签名记录段 段; 段.来源 = { 规格.来源运行期域身份, 规格.来源节点直接仓库身份, 规格.快照代次 }; return 段;
    };
    夹具.提供者.读取用途观察记录段 = [规格 = 夹具.规格] {
        用途观察记录段 段; 段.来源 = { 规格.来源运行期域身份, 规格.来源节点直接仓库身份, 规格.快照代次 }; return 段;
    };
    夹具.提供者.读取特征批次发布记录段 = [规格 = 夹具.规格] {
        特征批次发布记录段 段; 段.来源 = { 规格.来源运行期域身份, 规格.来源节点直接仓库身份, 规格.快照代次 }; return 段;
    };
    return 夹具;
}

constexpr bool 统一权威静态合同成立() noexcept {
    return 统一权威容器头字节数 == 104
        && 统一权威段头字节数 == 64
        && 统一权威段清单版本 == 1
        && 统一权威摘要算法版本 == 1
        && 统一权威单段最大记录数 == 4194304ULL;
}

static_assert(统一权威静态合同成立());

统一权威冻结固定夹具结果 形成统一权威冻结固定夹具结果() {
    const auto 夹具 = 构造统一权威冻结固定夹具();
    auto 提供者 = 夹具.提供者;
    const auto 冻结结果 = 形成统一权威冻结材料(夹具.规格, 提供者);
    if (冻结结果.状态 != 统一冻结结果状态::已形成 || !冻结结果.材料.has_value()) {
        return { 统一权威冻结固定夹具结果状态::冻结内部不一致, 0, 0 };
    }
    const auto 编码结果 = 序列化统一权威快照(*冻结结果.材料);
    if (编码结果.状态 != 规范化编码结果状态::已形成 || !编码结果.材料.has_value()) {
        return { 统一权威冻结固定夹具结果状态::编码内部不一致, 9, 0 };
    }
    return { 统一权威冻结固定夹具结果状态::已形成并编码, 9, 编码结果.材料->字节.size() };
}

} // namespace 海中鱼巣
