// 文件规则：只把 P1—P3 已冻结的有界只读提供面适配为 NT-C4 输入，不取得仓库、锁或令牌所有权。
module;

#include "统一权威快照.数据.h"

#include <array>
#include <cstddef>

export module 海中鱼巣.核心.适配.统一权威冻结;

export namespace 海中鱼巣 {

必需段声明组读取结果 读取当前必需段声明组() {
    必需段声明组读取结果 结果;
    结果.状态 = 必需段声明组状态::已形成;
    const std::array<统一权威快照段类型, 9> 类型组 = {
        统一权威快照段类型::节点直接记录,
        统一权威快照段类型::正式关系记录,
        统一权威快照段类型::特征值类型化记录,
        统一权威快照段类型::状态域记录,
        统一权威快照段类型::动态域记录,
        统一权威快照段类型::任务方法选择记录,
        统一权威快照段类型::概念签名记录,
        统一权威快照段类型::用途观察记录,
        统一权威快照段类型::特征批次发布记录
    };
    for (std::size_t 索引 = 0; 索引 < 类型组.size(); ++索引) {
        结果.声明组[索引].类型 = 类型组[索引];
        结果.声明组[索引].段格式版本 = 1;
        结果.声明组[索引].段顺序 = static_cast<std::uint32_t>(索引);
        结果.声明组[索引].最大记录数 = 统一权威单段最大记录数;
        结果.声明组[索引].最大载荷字节数 = 统一权威单段最大字节数;
    }
    return 结果;
}

} // namespace 海中鱼巣
