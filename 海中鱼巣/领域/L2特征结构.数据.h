#pragma once

#ifndef L2_FEATURE_STRUCTURE_NO_INCLUDES
#include <optional>
#include <vector>
#include "L2结构公共.数据.h"
#endif

namespace 海中鱼巣 {

inline constexpr std::uint32_t L2特征关系类型登记规则版本 = 1;
inline constexpr std::uint32_t L2特征值类型登记规则版本 = 1;
inline constexpr std::uint32_t L2特征定义属性类型登记规则版本 = 1;

struct L2特征宿主关系类型身份 final {
    稳定编码 值;
    friend bool operator==(const L2特征宿主关系类型身份&,
        const L2特征宿主关系类型身份&) = default;
};

struct L2特征定义关系类型身份 final {
    稳定编码 值;
    friend bool operator==(const L2特征定义关系类型身份&,
        const L2特征定义关系类型身份&) = default;
};

enum class L2特征关系类型登记状态 : std::uint8_t {
    已提交 = 1,
    精确重复 = 2,
    已读取 = 3,
    登记未加载 = 4,
    入口拒绝 = 5,
    许可拒绝 = 6,
    事实代次漂移 = 7,
    幂等冲突 = 8,
    资源失败 = 9,
    内部不一致 = 10
};

struct L2特征关系类型登记请求 final {
    L2结构请求头 请求头;
    std::uint32_t 规则版本 = L2特征关系类型登记规则版本;
    L2结构幂等身份 幂等身份{1};
    friend bool operator==(const L2特征关系类型登记请求&,
        const L2特征关系类型登记请求&) = default;
};

struct L2特征关系类型登记 final {
    std::uint32_t 合同版本 = L2结构合同版本;
    std::uint32_t 规则版本 = L2特征关系类型登记规则版本;
    L2结构幂等身份 首次幂等身份{1};
    std::uint64_t 首次期望事实代次 = 0;
    std::uint64_t 建立事实代次 = 0;
    std::uint64_t 事实截止代次 = 0;
    L2特征宿主关系类型身份 宿主关系类型;
    L2特征定义关系类型身份 定义关系类型;
    friend bool operator==(const L2特征关系类型登记&,
        const L2特征关系类型登记&) = default;
};

struct L2特征关系类型登记结果 final {
    L2特征关系类型登记状态 状态 = L2特征关系类型登记状态::入口拒绝;
    std::uint64_t 事实截止代次 = 0;
    std::optional<L2特征关系类型登记> 登记;
    bool 成功() const noexcept;
    friend bool operator==(const L2特征关系类型登记结果&,
        const L2特征关系类型登记结果&) = default;
};

struct L2特征I64值类型身份 final {
    稳定编码 值;
    friend bool operator==(const L2特征I64值类型身份&,
        const L2特征I64值类型身份&) = default;
};

struct L2特征I64组值类型身份 final {
    稳定编码 值;
    friend bool operator==(const L2特征I64组值类型身份&,
        const L2特征I64组值类型身份&) = default;
};

struct L2特征U64组值类型身份 final {
    稳定编码 值;
    friend bool operator==(const L2特征U64组值类型身份&,
        const L2特征U64组值类型身份&) = default;
};

struct L2特征独立材料值类型身份 final {
    稳定编码 值;
    friend bool operator==(const L2特征独立材料值类型身份&,
        const L2特征独立材料值类型身份&) = default;
};

enum class L2特征值类型登记状态 : std::uint8_t {
    已提交 = 1,
    精确重复 = 2,
    已读取 = 3,
    登记未加载 = 4,
    入口拒绝 = 5,
    许可拒绝 = 6,
    事实代次漂移 = 7,
    幂等冲突 = 8,
    资源失败 = 9,
    内部不一致 = 10
};

struct L2特征值类型登记请求 final {
    L2结构请求头 请求头;
    std::uint32_t 规则版本 = L2特征值类型登记规则版本;
    L2结构幂等身份 幂等身份{1};
    friend bool operator==(const L2特征值类型登记请求&,
        const L2特征值类型登记请求&) = default;
};

struct L2特征值类型登记 final {
    std::uint32_t 合同版本 = L2结构合同版本;
    std::uint32_t 规则版本 = L2特征值类型登记规则版本;
    L2结构幂等身份 首次幂等身份{1};
    std::uint64_t 首次期望事实代次 = 0;
    std::uint64_t 建立事实代次 = 0;
    std::uint64_t 事实截止代次 = 0;
    L2特征I64值类型身份 I64值类型;
    L2特征I64组值类型身份 I64组值类型;
    L2特征U64组值类型身份 U64组值类型;
    L2特征独立材料值类型身份 独立材料值类型;
    friend bool operator==(const L2特征值类型登记&,
        const L2特征值类型登记&) = default;
};

struct L2特征值类型登记结果 final {
    L2特征值类型登记状态 状态 = L2特征值类型登记状态::入口拒绝;
    std::uint64_t 事实截止代次 = 0;
    std::optional<L2特征值类型登记> 登记;
    bool 成功() const noexcept;
    friend bool operator==(const L2特征值类型登记结果&,
        const L2特征值类型登记结果&) = default;
};

#define 定义L2特征定义属性类型身份(类型名) \
    struct 类型名 final { \
        稳定编码 值; \
        friend bool operator==(const 类型名&, const 类型名&) = default; \
    }

定义L2特征定义属性类型身份(L2特征定义I64属性类型身份);
定义L2特征定义属性类型身份(L2特征定义I64组属性类型身份);
定义L2特征定义属性类型身份(L2特征定义U64组属性类型身份);
定义L2特征定义属性类型身份(L2特征定义独立材料属性类型身份);

#undef 定义L2特征定义属性类型身份

enum class L2特征定义属性类型登记状态 : std::uint8_t {
    已提交 = 1,
    精确重复 = 2,
    已读取 = 3,
    登记未加载 = 4,
    入口拒绝 = 5,
    许可拒绝 = 6,
    事实代次漂移 = 7,
    幂等冲突 = 8,
    资源失败 = 9,
    内部不一致 = 10
};

struct L2特征定义属性类型登记请求 final {
    L2结构请求头 请求头;
    std::uint32_t 规则版本 = L2特征定义属性类型登记规则版本;
    L2结构幂等身份 幂等身份{1};
    friend bool operator==(const L2特征定义属性类型登记请求&,
        const L2特征定义属性类型登记请求&) = default;
};

struct L2特征定义属性类型登记 final {
    std::uint32_t 合同版本 = L2结构合同版本;
    std::uint32_t 规则版本 = L2特征定义属性类型登记规则版本;
    L2结构幂等身份 首次幂等身份{1};
    std::uint64_t 首次期望事实代次 = 0;
    std::uint64_t 建立事实代次 = 0;
    std::uint64_t 事实截止代次 = 0;
    L2特征定义I64属性类型身份 I64属性类型;
    L2特征定义I64组属性类型身份 I64组属性类型;
    L2特征定义U64组属性类型身份 U64组属性类型;
    L2特征定义独立材料属性类型身份 独立材料属性类型;
    friend bool operator==(const L2特征定义属性类型登记&,
        const L2特征定义属性类型登记&) = default;
};

struct L2特征定义属性类型登记结果 final {
    L2特征定义属性类型登记状态 状态 = L2特征定义属性类型登记状态::入口拒绝;
    std::uint64_t 事实截止代次 = 0;
    std::optional<L2特征定义属性类型登记> 登记;
    bool 成功() const noexcept;
    friend bool operator==(const L2特征定义属性类型登记结果&,
        const L2特征定义属性类型登记结果&) = default;
};

struct L2特征定义事实 final {
    L2特征定义身份 身份;
    L2生命周期 生命周期;
    std::vector<L2属性事实> 固定属性;
    friend bool operator==(const L2特征定义事实&,
        const L2特征定义事实&) = default;
};

struct L2特征定义新增请求 final {
    L2结构请求头 请求头;
    L2结构幂等身份 幂等身份;
    friend bool operator==(const L2特征定义新增请求&,
        const L2特征定义新增请求&) = default;
};

struct L2特征定义写入结果 final {
    L2结构结果头 结果头;
    std::optional<L2特征定义事实> 特征定义;
    friend bool operator==(const L2特征定义写入结果&,
        const L2特征定义写入结果&) = default;
};

struct L2特征定义固定属性新增请求 final {
    L2结构请求头 请求头;
    L2结构幂等身份 幂等身份;
    L2特征定义身份 特征定义;
    L2属性提交项 新属性;
    friend bool operator==(const L2特征定义固定属性新增请求&,
        const L2特征定义固定属性新增请求&) = default;
};

struct L2特征定义固定属性换代请求 final {
    L2结构请求头 请求头;
    L2结构幂等身份 幂等身份;
    L2特征定义身份 特征定义;
    稳定编码 旧值稳定编码;
    L2属性提交项 新属性;
    friend bool operator==(const L2特征定义固定属性换代请求&,
        const L2特征定义固定属性换代请求&) = default;
};

struct L2特征定义固定属性写入结果 final {
    L2结构结果头 结果头;
    std::optional<L2属性事实> 当前属性;
    std::optional<L2属性事实> 已退出属性;
    bool 成功() const noexcept;
    friend bool operator==(const L2特征定义固定属性写入结果&,
        const L2特征定义固定属性写入结果&) = default;
};

struct L2特征定义身份来源读取请求 final {
    L2结构请求头 请求头;
    L2读取类别 读取类别 = L2读取类别::当前;
    L2特征定义身份 特征定义;
    std::uint64_t 历史截止事实代次 = 0;
    friend bool operator==(const L2特征定义身份来源读取请求&,
        const L2特征定义身份来源读取请求&) = default;
};

struct L2特征定义身份来源读取结果 final {
    L2结构结果头 结果头;
    L2读取类别 读取类别 = L2读取类别::当前;
    std::uint64_t 历史截止事实代次 = 0;
    std::optional<L2节点族身份来源事实> 身份来源;
    bool 成功() const noexcept;
    friend bool operator==(const L2特征定义身份来源读取结果&,
        const L2特征定义身份来源读取结果&) = default;
};

struct L2特征定义退出请求 final {
    L2结构请求头 请求头;
    L2结构幂等身份 幂等身份;
    L2特征定义身份 特征定义;
    稳定编码 族归属关系稳定编码;
    std::vector<稳定编码> 当前固定属性值;
    friend bool operator==(const L2特征定义退出请求&,
        const L2特征定义退出请求&) = default;
};

struct L2特征定义退出结果 final {
    L2结构结果头 结果头;
    std::optional<L2特征定义事实> 已退出特征定义;
    bool 成功() const noexcept;
    friend bool operator==(const L2特征定义退出结果&,
        const L2特征定义退出结果&) = default;
};

struct L2特征定义完整读取请求 final {
    L2结构请求头 请求头;
    L2读取类别 读取类别 = L2读取类别::当前;
    L2特征定义身份 特征定义;
    std::uint64_t 历史截止事实代次 = 0;
    friend bool operator==(const L2特征定义完整读取请求&,
        const L2特征定义完整读取请求&) = default;
};

struct L2特征定义完整读取结果 final {
    L2结构结果头 结果头;
    L2读取类别 读取类别 = L2读取类别::当前;
    std::uint64_t 历史截止事实代次 = 0;
    std::optional<L2特征定义事实> 特征定义;
    friend bool operator==(const L2特征定义完整读取结果&,
        const L2特征定义完整读取结果&) = default;
};

struct L2特征值事实 final {
    稳定编码 值稳定编码;
    L2特征实例身份 特征实例;
    L2原始值材料 类型化不可变材料;
    稳定编码 来源稳定编码;
    L2生命周期 生命周期;
    friend bool operator==(const L2特征值事实&,
        const L2特征值事实&) = default;
};

struct L2特征当前值新增请求 final {
    L2结构请求头 请求头;
    L2结构幂等身份 幂等身份;
    L2特征实例身份 特征实例;
    L2原始值材料 新值材料;
    稳定编码 来源稳定编码;
    friend bool operator==(const L2特征当前值新增请求&,
        const L2特征当前值新增请求&) = default;
};

struct L2特征当前值换代请求 final {
    L2结构请求头 请求头;
    L2结构幂等身份 幂等身份;
    L2特征实例身份 特征实例;
    稳定编码 旧值稳定编码;
    L2原始值材料 新值材料;
    稳定编码 来源稳定编码;
    friend bool operator==(const L2特征当前值换代请求&,
        const L2特征当前值换代请求&) = default;
};

struct L2特征当前值写入结果 final {
    L2结构结果头 结果头;
    std::optional<L2特征值事实> 当前值;
    std::optional<L2特征值事实> 已退出值;
    bool 成功() const noexcept;
    friend bool operator==(const L2特征当前值写入结果&,
        const L2特征当前值写入结果&) = default;
};

struct L2特征值历史读取请求 final {
    L2结构请求头 请求头;
    L2特征实例身份 特征实例;
    稳定编码 值稳定编码;
    friend bool operator==(const L2特征值历史读取请求&,
        const L2特征值历史读取请求&) = default;
};

struct L2特征值历史读取结果 final {
    L2结构结果头 结果头;
    std::optional<L2特征值事实> 特征值;
    friend bool operator==(const L2特征值历史读取结果&,
        const L2特征值历史读取结果&) = default;
};

struct L2特征实例自有关系引用 final {
    稳定编码 宿主关系稳定编码;
    稳定编码 定义关系稳定编码;
    friend bool operator==(const L2特征实例自有关系引用&,
        const L2特征实例自有关系引用&) = default;
};

struct L2特征实例事实 final {
    L2特征实例身份 身份;
    L2存在身份 宿主存在;
    L2特征定义身份 特征定义;
    L2生命周期 生命周期;
    std::optional<L2特征值事实> 当前值;
    friend bool operator==(const L2特征实例事实&,
        const L2特征实例事实&) = default;
};

struct L2特征实例身份来源读取请求 final {
    L2结构请求头 请求头;
    L2读取类别 读取类别 = L2读取类别::当前;
    L2特征实例身份 特征实例;
    std::uint64_t 历史截止事实代次 = 0;
    friend bool operator==(const L2特征实例身份来源读取请求&,
        const L2特征实例身份来源读取请求&) = default;
};

struct L2特征实例身份来源读取结果 final {
    L2结构结果头 结果头;
    L2读取类别 读取类别 = L2读取类别::当前;
    std::uint64_t 历史截止事实代次 = 0;
    std::optional<L2节点族身份来源事实> 身份来源;
    bool 成功() const noexcept;
    friend bool operator==(const L2特征实例身份来源读取结果&,
        const L2特征实例身份来源读取结果&) = default;
};

struct L2特征实例退出请求 final {
    L2结构请求头 请求头;
    L2结构幂等身份 幂等身份;
    L2特征实例身份 特征实例;
    稳定编码 族归属关系稳定编码;
    L2特征实例自有关系引用 自有关系;
    std::optional<稳定编码> 当前值稳定编码;
    friend bool operator==(const L2特征实例退出请求&,
        const L2特征实例退出请求&) = default;
};

struct L2特征实例退出结果 final {
    L2结构结果头 结果头;
    std::optional<L2特征实例事实> 已退出特征实例;
    std::optional<L2特征实例自有关系引用> 已退出自有关系;
    bool 成功() const noexcept;
    friend bool operator==(const L2特征实例退出结果&,
        const L2特征实例退出结果&) = default;
};

struct L2特征实例新增请求 final {
    L2结构请求头 请求头;
    L2结构幂等身份 幂等身份;
    L2存在身份 宿主存在;
    L2特征定义身份 特征定义;
    friend bool operator==(const L2特征实例新增请求&,
        const L2特征实例新增请求&) = default;
};

struct L2特征实例写入结果 final {
    L2结构结果头 结果头;
    std::optional<L2特征实例事实> 特征实例;
    friend bool operator==(const L2特征实例写入结果&,
        const L2特征实例写入结果&) = default;
};

struct L2特征实例完整读取请求 final {
    L2结构请求头 请求头;
    L2读取类别 读取类别 = L2读取类别::当前;
    L2特征实例身份 特征实例;
    std::uint64_t 历史截止事实代次 = 0;
    friend bool operator==(const L2特征实例完整读取请求&,
        const L2特征实例完整读取请求&) = default;
};

struct L2特征实例完整读取结果 final {
    L2结构结果头 结果头;
    L2读取类别 读取类别 = L2读取类别::当前;
    std::uint64_t 历史截止事实代次 = 0;
    std::optional<L2特征实例事实> 特征实例;
    std::optional<L2特征实例自有关系引用> 自有关系;
    friend bool operator==(const L2特征实例完整读取结果&,
        const L2特征实例完整读取结果&) = default;
};

struct L2按存在当前特征读取请求 final {
    L2结构请求头 请求头;
    L2存在身份 存在;
    friend bool operator==(const L2按存在当前特征读取请求&,
        const L2按存在当前特征读取请求&) = default;
};

struct L2按存在当前特征读取结果 final {
    L2结构结果头 结果头;
    std::vector<L2特征实例事实> 特征实例组;
    bool 成功() const noexcept;
    friend bool operator==(const L2按存在当前特征读取结果&,
        const L2按存在当前特征读取结果&) = default;
};

// 诊断责任：无适用错误分支；固定登记合同只接受唯一规则与幂等身份。
inline bool L2特征关系类型登记请求有效(
    const L2特征关系类型登记请求& 请求) noexcept {
    return L2结构请求头合同有效(请求.请求头)
        && 请求.规则版本 == L2特征关系类型登记规则版本
        && 请求.幂等身份 == L2结构幂等身份{1};
}

// 诊断责任：无适用错误分支；固定值类型登记只接受唯一规则与幂等身份。
inline bool L2特征值类型登记请求有效(
    const L2特征值类型登记请求& 请求) noexcept {
    return L2结构请求头合同有效(请求.请求头)
        && 请求.规则版本 == L2特征值类型登记规则版本
        && 请求.幂等身份 == L2结构幂等身份{1};
}

// 诊断责任：无适用错误分支；定义属性类型登记只接受固定规则与身份。
inline bool L2特征定义属性类型登记请求有效(
    const L2特征定义属性类型登记请求& 请求) noexcept {
    return L2结构请求头合同有效(请求.请求头)
        && 请求.规则版本 == L2特征定义属性类型登记规则版本
        && 请求.幂等身份 == L2结构幂等身份{1};
}

// 诊断责任：无适用错误分支；四种定义属性类型必须唯一且来自同次登记。
inline bool L2特征定义属性类型登记完整(
    const L2特征定义属性类型登记& 登记) noexcept {
    const auto 有符号64位 = 登记.I64属性类型.值;
    const auto 有符号64位组 = 登记.I64组属性类型.值;
    const auto 无符号64位组 = 登记.U64组属性类型.值;
    const auto 独立材料 = 登记.独立材料属性类型.值;
    return 登记.合同版本 == L2结构合同版本
        && 登记.规则版本 == L2特征定义属性类型登记规则版本
        && 登记.首次幂等身份 == L2结构幂等身份{1}
        && 登记.建立事实代次 != 0
        && 登记.首次期望事实代次 < 登记.建立事实代次
        && 登记.事实截止代次 >= 登记.建立事实代次
        && 有效(有符号64位) && 有效(有符号64位组)
        && 有效(无符号64位组) && 有效(独立材料)
        && 有符号64位 != 有符号64位组 && 有符号64位 != 无符号64位组
        && 有符号64位 != 独立材料 && 有符号64位组 != 无符号64位组
        && 有符号64位组 != 独立材料 && 无符号64位组 != 独立材料;
}

// 诊断责任：无适用错误分支；四类值类型必须保持首次事实与唯一编码。
inline bool L2特征值类型登记完整(
    const L2特征值类型登记& 登记) noexcept {
    const auto 有符号64位 = 登记.I64值类型.值;
    const auto 有符号64位组 = 登记.I64组值类型.值;
    const auto 无符号64位组 = 登记.U64组值类型.值;
    const auto 独立材料 = 登记.独立材料值类型.值;
    return 登记.合同版本 == L2结构合同版本
        && 登记.规则版本 == L2特征值类型登记规则版本
        && 登记.首次幂等身份 == L2结构幂等身份{1}
        && 登记.建立事实代次 != 0
        && 登记.首次期望事实代次 < 登记.建立事实代次
        && 登记.事实截止代次 >= 登记.建立事实代次
        && 有效(有符号64位) && 有效(有符号64位组)
        && 有效(无符号64位组) && 有效(独立材料)
        && 有符号64位 != 有符号64位组 && 有符号64位 != 无符号64位组
        && 有符号64位 != 独立材料 && 有符号64位组 != 无符号64位组
        && 有符号64位组 != 独立材料 && 无符号64位组 != 独立材料;
}

// 诊断责任：无适用错误分支；定位必须保持首次事实与当前截止的单调关系。
inline bool L2特征关系类型登记完整(
    const L2特征关系类型登记& 登记) noexcept {
    return 登记.合同版本 == L2结构合同版本
        && 登记.规则版本 == L2特征关系类型登记规则版本
        && 登记.首次幂等身份 == L2结构幂等身份{1}
        && 登记.建立事实代次 != 0
        && 登记.首次期望事实代次 < 登记.建立事实代次
        && 登记.事实截止代次 >= 登记.建立事实代次
        && 有效(登记.宿主关系类型.值) && 有效(登记.定义关系类型.值)
        && 登记.宿主关系类型.值 != 登记.定义关系类型.值;
}

// 诊断责任：无适用错误分支；纯值实例新增请求判断不读取事实。
inline bool L2特征实例新增请求有效(
    const L2特征实例新增请求& 请求) noexcept {
    return L2结构请求头合同有效(请求.请求头)
        && 请求.请求头.期望事实代次 != 0
        && L2结构幂等身份有效(请求.幂等身份)
        && 有效(请求.宿主存在.值) && 有效(请求.特征定义.值);
}

// 诊断责任：无适用错误分支；四类材料沿共同规则判断且不读取事实。
inline bool L2特征当前值材料有效(const L2原始值材料& 材料) noexcept {
    return std::visit([](const auto& 值) noexcept {
        using 类型 = std::decay_t<decltype(值)>;
        if constexpr (std::is_same_v<类型, std::int64_t>) return true;
        else if constexpr (std::is_same_v<类型, L2独立材料引用>)
            return 有效(值.编码);
        else return !值.empty();
    }, 材料);
}

// 诊断责任：无适用错误分支；纯值初始当前值请求判断不读取事实。
inline bool L2特征当前值新增请求有效(
    const L2特征当前值新增请求& 请求) noexcept {
    return L2结构请求头合同有效(请求.请求头)
        && 请求.请求头.期望事实代次 != 0
        && L2结构幂等身份有效(请求.幂等身份)
        && 有效(请求.特征实例.值) && 有效(请求.来源稳定编码)
        && L2特征当前值材料有效(请求.新值材料);
}

// 诊断责任：无适用错误分支；换代另要求具名旧值非零。
inline bool L2特征当前值换代请求有效(
    const L2特征当前值换代请求& 请求) noexcept {
    return 有效(请求.旧值稳定编码)
        && L2结构请求头合同有效(请求.请求头)
        && 请求.请求头.期望事实代次 != 0
        && L2结构幂等身份有效(请求.幂等身份)
        && 有效(请求.特征实例.值) && 有效(请求.来源稳定编码)
        && L2特征当前值材料有效(请求.新值材料);
}

// 诊断责任：无适用错误分支；具名值读取要求同期非零实例与值编码。
inline bool L2特征值历史读取请求有效(
    const L2特征值历史读取请求& 请求) noexcept {
    return L2结构请求头合同有效(请求.请求头)
        && 请求.请求头.期望事实代次 != 0
        && 有效(请求.特征实例.值) && 有效(请求.值稳定编码);
}

// 诊断责任：无适用错误分支；实例退出闭包编码必须非零、两两不混淆。
inline bool L2特征实例退出请求有效(
    const L2特征实例退出请求& 请求) noexcept {
    const auto 宿主关系 = 请求.自有关系.宿主关系稳定编码;
    const auto 定义关系 = 请求.自有关系.定义关系稳定编码;
    if (!L2结构请求头合同有效(请求.请求头)
        || 请求.请求头.期望事实代次 == 0
        || !L2结构幂等身份有效(请求.幂等身份)
        || !有效(请求.特征实例.值)
        || !有效(请求.族归属关系稳定编码) || !有效(宿主关系)
        || !有效(定义关系) || 宿主关系 == 定义关系) return false;
    if (请求.族归属关系稳定编码 == 宿主关系
        || 请求.族归属关系稳定编码 == 定义关系) return false;
    return !请求.当前值稳定编码
        || (有效(*请求.当前值稳定编码)
            && *请求.当前值稳定编码 != 请求.族归属关系稳定编码
            && *请求.当前值稳定编码 != 宿主关系
            && *请求.当前值稳定编码 != 定义关系);
}

// 诊断责任：无适用错误分支；纯值完整读取请求判断不读取事实。
inline bool L2特征实例完整读取请求有效(
    const L2特征实例完整读取请求& 请求) noexcept {
    if (!L2结构请求头合同有效(请求.请求头)
        || 请求.请求头.期望事实代次 == 0
        || !有效(请求.特征实例.值)) return false;
    if (请求.读取类别 == L2读取类别::当前)
        return 请求.历史截止事实代次 == 0;
    return 请求.读取类别 == L2读取类别::历史
        && 请求.历史截止事实代次 != 0
        && 请求.历史截止事实代次 <= 请求.请求头.期望事实代次;
}

// 诊断责任：无适用错误分支；纯值按存在读取请求判断不读取事实。
inline bool L2按存在当前特征读取请求有效(
    const L2按存在当前特征读取请求& 请求) noexcept {
    return L2结构请求头合同有效(请求.请求头)
        && 请求.请求头.期望事实代次 != 0 && 有效(请求.存在.值);
}

// 诊断责任：无适用错误分支；只判断特征值在具名截止的值式投影。
inline bool L2特征值事实截止投影完整(
    const L2特征值事实& 特征值, std::uint64_t 截止) noexcept {
    const bool 材料完整 = std::visit([](const auto& 材料) noexcept {
        using 材料类型 = std::decay_t<decltype(材料)>;
        if constexpr (std::is_same_v<材料类型, std::int64_t>) return true;
        else if constexpr (std::is_same_v<材料类型, L2独立材料引用>)
            return 有效(材料.编码);
        else return !材料.empty();
    }, 特征值.类型化不可变材料);
    return 截止 != 0 && 有效(特征值.值稳定编码)
        && 有效(特征值.特征实例.值) && 有效(特征值.来源稳定编码)
        && 材料完整 && L2生命周期完整(特征值.生命周期)
        && 特征值.生命周期.创建事实代次 <= 截止
        && (!特征值.生命周期.退出事实代次
            || *特征值.生命周期.退出事实代次 <= 截止);
}

// 诊断责任：无适用错误分支；最终完整性允许一个同实例的可选当前值。
inline bool L2特征实例事实完整(
    const L2特征实例事实& 特征实例, std::uint64_t 截止) noexcept {
    if (截止 == 0 || !有效(特征实例.身份.值)
        || !有效(特征实例.宿主存在.值) || !有效(特征实例.特征定义.值)
        || L2生命周期完整(特征实例.生命周期) == false
        || 特征实例.生命周期.创建事实代次 > 截止
        || (特征实例.生命周期.退出事实代次
            && *特征实例.生命周期.退出事实代次 > 截止)) return false;
    return !特征实例.当前值
        || (L2特征值事实截止投影完整(*特征实例.当前值, 截止)
            && 特征实例.当前值->特征实例 == 特征实例.身份
            && !特征实例.当前值->生命周期.退出事实代次);
}

// 诊断责任：无适用错误分支；F2 阶段实例完整且当前值固定为空。
inline bool L2特征实例F2事实完整(
    const L2特征实例事实& 特征实例, std::uint64_t 截止) noexcept {
    return L2特征实例事实完整(特征实例, 截止) && !特征实例.当前值;
}

// 诊断责任：无适用错误分支；纯值请求判断不读取事实。
inline bool L2特征定义新增请求有效(
    const L2特征定义新增请求& 请求) noexcept {
    return L2结构请求头合同有效(请求.请求头)
        && L2结构幂等身份有效(请求.幂等身份);
}

// 诊断责任：无适用错误分支；纯值请求判断不读取事实。
inline bool L2特征定义固定属性新增请求有效(
    const L2特征定义固定属性新增请求& 请求) noexcept {
    return L2结构请求头合同有效(请求.请求头)
        && 请求.请求头.期望事实代次 != 0
        && L2结构幂等身份有效(请求.幂等身份)
        && 有效(请求.特征定义.值) && L2属性提交项有效(请求.新属性);
}

// 诊断责任：无适用错误分支；纯值请求判断不读取事实。
inline bool L2特征定义固定属性换代请求有效(
    const L2特征定义固定属性换代请求& 请求) noexcept {
    return 有效(请求.旧值稳定编码)
        && L2特征定义固定属性新增请求有效({请求.请求头, 请求.幂等身份,
            请求.特征定义, 请求.新属性});
}

// 诊断责任：无适用错误分支；显式属性闭包须严格升序且无重复。
inline bool L2特征定义退出请求有效(
    const L2特征定义退出请求& 请求) noexcept {
    if (!L2结构请求头合同有效(请求.请求头)
        || 请求.请求头.期望事实代次 == 0
        || !L2结构幂等身份有效(请求.幂等身份)
        || !有效(请求.特征定义.值)
        || !有效(请求.族归属关系稳定编码)) return false;
    for (std::size_t 索引 = 0; 索引 < 请求.当前固定属性值.size(); ++索引) {
        if (!有效(请求.当前固定属性值[索引])
            || (索引 != 0
                && !(请求.当前固定属性值[索引 - 1] < 请求.当前固定属性值[索引])))
            return false;
    }
    return true;
}

// 诊断责任：无适用错误分支；纯值完整读取请求判断不读取事实。
inline bool L2特征定义完整读取请求有效(
    const L2特征定义完整读取请求& 请求) noexcept {
    if (!L2结构请求头合同有效(请求.请求头)
        || 请求.请求头.期望事实代次 == 0
        || !有效(请求.特征定义.值)) return false;
    if (请求.读取类别 == L2读取类别::当前)
        return 请求.历史截止事实代次 == 0;
    return 请求.读取类别 == L2读取类别::历史
        && 请求.历史截止事实代次 != 0
        && 请求.历史截止事实代次 <= 请求.请求头.期望事实代次;
}

// 诊断责任：无适用错误分支；只判断特征定义完整值式投影及稳定属性顺序。
inline bool L2特征定义事实完整(
    const L2特征定义事实& 特征定义, std::uint64_t 截止) noexcept {
    if (截止 == 0 || !有效(特征定义.身份.值)
        || !L2生命周期完整(特征定义.生命周期)
        || 特征定义.生命周期.创建事实代次 > 截止
        || (特征定义.生命周期.退出事实代次
            && *特征定义.生命周期.退出事实代次 > 截止)) return false;
    for (std::size_t 索引 = 0; 索引 < 特征定义.固定属性.size(); ++索引) {
        const auto& 属性 = 特征定义.固定属性[索引];
        if (!L2属性事实截止投影完整(属性, 截止) || 属性.退出事实代次)
            return false;
        if (索引 != 0) {
            const auto& 前项 = 特征定义.固定属性[索引 - 1];
            if (!(前项.属性类型身份 < 属性.属性类型身份)
                || 前项.属性类型身份 == 属性.属性类型身份) return false;
        }
    }
    return true;
}

// 诊断责任：无适用错误分支；只判断登记成功状态与完整载荷。
inline bool L2特征关系类型登记结果::成功() const noexcept {
    return (状态 == L2特征关系类型登记状态::已提交
            || 状态 == L2特征关系类型登记状态::精确重复
            || 状态 == L2特征关系类型登记状态::已读取)
        && 登记 && 事实截止代次 == 登记->事实截止代次
        && L2特征关系类型登记完整(*登记);
}

// 诊断责任：无适用错误分支；只判断值类型登记成功状态与完整载荷。
inline bool L2特征值类型登记结果::成功() const noexcept {
    return (状态 == L2特征值类型登记状态::已提交
            || 状态 == L2特征值类型登记状态::精确重复
            || 状态 == L2特征值类型登记状态::已读取)
        && 登记 && 事实截止代次 == 登记->事实截止代次
        && L2特征值类型登记完整(*登记);
}

// 诊断责任：无适用错误分支；只判断定义属性类型登记成功与完整载荷。
inline bool L2特征定义属性类型登记结果::成功() const noexcept {
    return (状态 == L2特征定义属性类型登记状态::已提交
            || 状态 == L2特征定义属性类型登记状态::精确重复
            || 状态 == L2特征定义属性类型登记状态::已读取)
        && 登记 && 事实截止代次 == 登记->事实截止代次
        && L2特征定义属性类型登记完整(*登记);
}

// 诊断责任：无适用错误分支；来源请求的类别与截止必须闭合。
inline bool L2特征定义身份来源读取请求有效(
    const L2特征定义身份来源读取请求& 请求) noexcept {
    if (!L2结构请求头合同有效(请求.请求头)
        || 请求.请求头.期望事实代次 == 0 || !有效(请求.特征定义.值)) return false;
    if (请求.读取类别 == L2读取类别::当前)
        return 请求.历史截止事实代次 == 0;
    return 请求.读取类别 == L2读取类别::历史
        && 请求.历史截止事实代次 != 0
        && 请求.历史截止事实代次 <= 请求.请求头.期望事实代次;
}

// 诊断责任：无适用错误分支；实例来源请求复用同一类别与截止规则。
inline bool L2特征实例身份来源读取请求有效(
    const L2特征实例身份来源读取请求& 请求) noexcept {
    if (!L2结构请求头合同有效(请求.请求头)
        || 请求.请求头.期望事实代次 == 0 || !有效(请求.特征实例.值)) return false;
    if (请求.读取类别 == L2读取类别::当前)
        return 请求.历史截止事实代次 == 0;
    return 请求.读取类别 == L2读取类别::历史
        && 请求.历史截止事实代次 != 0
        && 请求.历史截止事实代次 <= 请求.请求头.期望事实代次;
}

// 诊断责任：无适用错误分支；定义来源成功必须闭合定义族与截止。
inline bool L2特征定义身份来源读取结果::成功() const noexcept {
    return 结果头.状态 == L2结构状态::已读取
        && 结果头.事实截止代次 != 0 && !结果头.变更事实代次
        && 身份来源 && 身份来源->节点族 == L2节点族::特征定义
        && L2节点族身份来源事实完整(*身份来源, 结果头.事实截止代次)
        && (读取类别 == L2读取类别::当前
            ? 历史截止事实代次 == 0
            : 读取类别 == L2读取类别::历史
                && 历史截止事实代次 == 结果头.事实截止代次);
}

// 诊断责任：无适用错误分支；实例来源成功必须闭合实例族与截止。
inline bool L2特征实例身份来源读取结果::成功() const noexcept {
    return 结果头.状态 == L2结构状态::已读取
        && 结果头.事实截止代次 != 0 && !结果头.变更事实代次
        && 身份来源 && 身份来源->节点族 == L2节点族::特征实例
        && L2节点族身份来源事实完整(*身份来源, 结果头.事实截止代次)
        && (读取类别 == L2读取类别::当前
            ? 历史截止事实代次 == 0
            : 读取类别 == L2读取类别::历史
                && 历史截止事实代次 == 结果头.事实截止代次);
}

// 诊断责任：无适用错误分支；只判断当前枚举成功头与严格升序完整组。
inline bool L2按存在当前特征读取结果::成功() const noexcept {
    if (结果头.状态 != L2结构状态::已读取
        || 结果头.事实截止代次 == 0 || 结果头.变更事实代次) return false;
    for (std::size_t 索引 = 0; 索引 < 特征实例组.size(); ++索引) {
        if (!L2特征实例事实完整(
                特征实例组[索引], 结果头.事实截止代次)
            || 特征实例组[索引].生命周期.退出事实代次
            || (索引 != 0 && !(特征实例组[索引 - 1].身份.值
                < 特征实例组[索引].身份.值))) return false;
    }
    return true;
}

// 诊断责任：无适用错误分支；值写结果必须保持首次新值与可选同代退出旧值。
inline bool L2特征当前值写入结果::成功() const noexcept {
    const bool 成功状态 = 结果头.状态 == L2结构状态::已提交
        || 结果头.状态 == L2结构状态::精确重复;
    if (!成功状态 || 结果头.事实截止代次 == 0 || !结果头.变更事实代次
        || *结果头.变更事实代次 != 结果头.事实截止代次 || !当前值
        || !L2特征值事实截止投影完整(*当前值, 结果头.事实截止代次)
        || 当前值->生命周期.退出事实代次) return false;
    if (!已退出值) return true;
    return L2特征值事实截止投影完整(*已退出值, 结果头.事实截止代次)
        && 已退出值->生命周期.退出事实代次 == 结果头.事实截止代次
        && 已退出值->特征实例 == 当前值->特征实例
        && 已退出值->值稳定编码 != 当前值->值稳定编码;
}

// 诊断责任：无适用错误分支；实例、两关系及可选值必须保持同代退出。
inline bool L2特征实例退出结果::成功() const noexcept {
    const bool 成功状态 = 结果头.状态 == L2结构状态::已提交
        || 结果头.状态 == L2结构状态::精确重复;
    if (!成功状态 || 结果头.事实截止代次 == 0 || !结果头.变更事实代次
        || *结果头.变更事实代次 != 结果头.事实截止代次
        || !已退出特征实例 || !已退出自有关系
        || !L2生命周期完整(已退出特征实例->生命周期)
        || 已退出特征实例->生命周期.退出事实代次
            != 结果头.事实截止代次
        || !有效(已退出自有关系->宿主关系稳定编码)
        || !有效(已退出自有关系->定义关系稳定编码)
        || 已退出自有关系->宿主关系稳定编码
            == 已退出自有关系->定义关系稳定编码) return false;
    return !已退出特征实例->当前值
        || (L2特征值事实截止投影完整(
                *已退出特征实例->当前值, 结果头.事实截止代次)
            && 已退出特征实例->当前值->特征实例
                == 已退出特征实例->身份
            && 已退出特征实例->当前值->生命周期.退出事实代次
                == 结果头.事实截止代次);
}

// 诊断责任：无适用错误分支；只判断属性写成功载荷与首次截止一致。
inline bool L2特征定义固定属性写入结果::成功() const noexcept {
    const bool 成功状态 = 结果头.状态 == L2结构状态::已提交
        || 结果头.状态 == L2结构状态::精确重复;
    if (!成功状态 || 结果头.事实截止代次 == 0 || !结果头.变更事实代次
        || *结果头.变更事实代次 != 结果头.事实截止代次 || !当前属性
        || !L2属性事实截止投影完整(*当前属性, 结果头.事实截止代次)
        || 当前属性->退出事实代次) return false;
    if (!已退出属性) return true;
    return L2属性事实截止投影完整(*已退出属性, 结果头.事实截止代次)
        && 已退出属性->退出事实代次 == 结果头.事实截止代次
        && 已退出属性->属性类型身份 == 当前属性->属性类型身份
        && 已退出属性->值稳定编码 != 当前属性->值稳定编码;
}

// 诊断责任：无适用错误分支；只判断定义和全部属性在同一代次退出。
inline bool L2特征定义退出结果::成功() const noexcept {
    const bool 成功状态 = 结果头.状态 == L2结构状态::已提交
        || 结果头.状态 == L2结构状态::精确重复;
    if (!成功状态 || 结果头.事实截止代次 == 0 || !结果头.变更事实代次
        || *结果头.变更事实代次 != 结果头.事实截止代次
        || !已退出特征定义
        || 已退出特征定义->生命周期.退出事实代次
            != 结果头.事实截止代次) return false;
    for (std::size_t 索引 = 0;
        索引 < 已退出特征定义->固定属性.size(); ++索引) {
        const auto& 属性 = 已退出特征定义->固定属性[索引];
        if (!L2属性事实截止投影完整(属性, 结果头.事实截止代次)
            || 属性.退出事实代次 != 结果头.事实截止代次
            || (索引 != 0
                && !(已退出特征定义->固定属性[索引 - 1].属性类型身份
                    < 属性.属性类型身份))) return false;
    }
    return 有效(已退出特征定义->身份.值)
        && L2生命周期完整(已退出特征定义->生命周期);
}

} // namespace 海中鱼巣
