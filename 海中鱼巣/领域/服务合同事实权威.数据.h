#pragma once

#ifndef INSTINCT_SERVICE_CONTRACT_FACT_AUTHORITY_NO_INCLUDES
#include <cstdint>
#include <optional>
#include <variant>
#include <vector>
#include "L2结构公共.数据.h"
#include "L2状态结构.数据.h"
#include "../核心/L1所有者范围CRUD.数据.h"
#endif

namespace 海中鱼巣 {

inline constexpr std::uint32_t 服务合同事实权威合同版本_v1 = 1;
inline constexpr std::uint32_t 服务合同事实权威结构登记版本_v1 = 1;
inline constexpr std::uint32_t 服务进展事实扩展合同版本_v1 = 1;
inline constexpr std::uint32_t 服务进展事实结构登记版本_v1 = 1;
inline constexpr std::uint32_t 服务准备事实扩展合同版本_v1 = 1;
inline constexpr std::uint32_t 服务准备事实结构登记版本_v1 = 1;
inline constexpr L1所有者范围建立幂等身份
    服务合同事实权威所有者建立身份_v1{0x494E'5354'5343'4F57ULL};

struct 服务合同身份_v1 final {
    稳定编码 值{};
    friend bool operator==(const 服务合同身份_v1&,
        const 服务合同身份_v1&) = default;
};

struct 服务到期未满足事件身份_v1 final {
    稳定编码 值{};
    friend bool operator==(const 服务到期未满足事件身份_v1&,
        const 服务到期未满足事件身份_v1&) = default;
};

#define 定义服务进展强类型身份(类型名) \
    struct 类型名 final { \
        稳定编码 值{}; \
        friend bool operator==(const 类型名&, const 类型名&) = default; \
    }

定义服务进展强类型身份(服务进展事实身份_v1);
定义服务进展强类型身份(服务进展执行冻结见证身份_v1);
定义服务进展强类型身份(服务进展方法执行见证身份_v1);
定义服务进展强类型身份(服务进展安全门禁见证身份_v1);
定义服务进展强类型身份(服务准备事实身份_v1);
定义服务进展强类型身份(服务能力缺口身份_v1);
定义服务进展强类型身份(服务准备目标身份_v1);
定义服务进展强类型身份(服务准备结果身份_v1);
定义服务进展强类型身份(服务准备验证事实身份_v1);

#undef 定义服务进展强类型身份

inline bool 有效(服务合同身份_v1 身份) noexcept { return 有效(身份.值); }
inline bool 有效(服务到期未满足事件身份_v1 身份) noexcept {
    return 有效(身份.值);
}
inline bool 有效(服务进展事实身份_v1 身份) noexcept { return 有效(身份.值); }
inline bool 有效(服务进展执行冻结见证身份_v1 身份) noexcept {
    return 有效(身份.值);
}
inline bool 有效(服务进展方法执行见证身份_v1 身份) noexcept {
    return 有效(身份.值);
}
inline bool 有效(服务进展安全门禁见证身份_v1 身份) noexcept {
    return 有效(身份.值);
}
inline bool 有效(服务准备事实身份_v1 身份) noexcept { return 有效(身份.值); }
inline bool 有效(服务能力缺口身份_v1 身份) noexcept { return 有效(身份.值); }
inline bool 有效(服务准备目标身份_v1 身份) noexcept { return 有效(身份.值); }
inline bool 有效(服务准备结果身份_v1 身份) noexcept { return 有效(身份.值); }
inline bool 有效(服务准备验证事实身份_v1 身份) noexcept { return 有效(身份.值); }

enum class 服务合同当前状态_v1 : std::uint8_t {
    有效未满足 = 1,
    已完整完成 = 2,
    提出者取消 = 3,
    已到期 = 4,
    正式终止 = 5
};

struct 服务合同事实_v1 final {
    服务合同身份_v1 身份{};
    L2存在身份 自我{};
    L2存在身份 提出者{};
    L2需求身份 需求{};
    稳定编码 来源提出事件{};
    L2存在身份 目标宿主{};
    L2特征定义身份 目标特征{};
    L2目标状态合同身份 目标状态合同{};
    稳定编码 服务范围{};
    std::uint64_t 提出运行代次 = 0;
    稳定编码 时间纪元身份{};
    std::int64_t 提出完整秒边界 = 0;
    std::int64_t 有效开始完整秒边界 = 0;
    std::int64_t 冻结有效总秒 = 0;
    std::int64_t 冻结预算 = 0;
    std::int64_t 预支值 = 0;
    std::int64_t 完工余款 = 0;
    std::uint64_t 需求版本 = 0;
    稳定编码 法规准入见证{};
    std::uint32_t 法规规则版本 = 0;
    std::uint64_t 合同代次 = 0;
    std::uint32_t 合同规则版本 = 0;
    std::uint64_t 形成事实代次 = 0;
    friend bool operator==(const 服务合同事实_v1&,
        const 服务合同事实_v1&) = default;
};

struct 服务合同状态事实_v1 final {
    服务合同身份_v1 合同{};
    服务合同当前状态_v1 状态 = 服务合同当前状态_v1::有效未满足;
    std::int64_t 已消费有效秒 = 0;
    std::int64_t 最后已结算完整秒边界 = 0;
    std::uint64_t 状态版本 = 0;
    稳定编码 来源事实{};
    std::uint64_t 形成事实代次 = 0;
    friend bool operator==(const 服务合同状态事实_v1&,
        const 服务合同状态事实_v1&) = default;
};

struct 服务到期未满足事件事实_v1 final {
    服务到期未满足事件身份_v1 身份{};
    服务合同身份_v1 合同{};
    L2需求身份 需求{};
    L2存在身份 提出者{};
    std::int64_t 冻结有效总秒 = 0;
    std::int64_t 实际已消费有效秒 = 0;
    std::int64_t 到期完整秒边界 = 0;
    稳定编码 目标未满足事实{};
    std::uint64_t 合同终态版本 = 0;
    std::uint32_t 规则版本 = 0;
    std::uint64_t 形成事实代次 = 0;
    friend bool operator==(const 服务到期未满足事件事实_v1&,
        const 服务到期未满足事件事实_v1&) = default;
};

enum class 服务进展运行状态_v1 : std::uint8_t {
    进行中 = 1,
    暂停 = 2,
    等待条件 = 3,
    阻塞 = 4,
    失败 = 5,
    取消 = 6,
    到期 = 7,
    终止 = 8
};

struct 服务合同关联进展事实_v1 final {
    服务进展事实身份_v1 身份{};
    L2存在身份 自我{};
    服务合同身份_v1 服务合同{};
    L2需求身份 需求{};
    L2任务身份 任务{};
    L2方法身份 方法{};
    稳定编码 T到D关系稳定编码{};
    服务进展执行冻结见证身份_v1 执行冻结见证{};
    服务进展方法执行见证身份_v1 方法执行见证{};
    服务进展安全门禁见证身份_v1 安全门禁见证{};
    std::optional<L2状态身份> 进展状态{};
    std::optional<L2动态身份> 进展动态{};
    服务进展运行状态_v1 运行状态 = 服务进展运行状态_v1::进行中;
    std::uint64_t 运行代次 = 0;
    std::int64_t 进展发生完整秒边界 = 0;
    std::int64_t 计量窗口开始完整秒边界 = 0;
    std::int64_t 计量窗口结束完整秒边界 = 0;
    std::uint64_t 方法内容版本 = 0;
    std::uint64_t 方法规格版本 = 0;
    std::uint64_t 方法生命周期版本 = 0;
    std::uint32_t 进展规则版本 = 0;
    std::uint64_t 形成事实代次 = 0;
    L2生命周期 生命周期{};
    friend bool operator==(const 服务合同关联进展事实_v1&,
        const 服务合同关联进展事实_v1&) = default;
};

using 服务准备来源身份_v1 =
    std::variant<L2需求身份, 服务能力缺口身份_v1>;

enum class 服务准备运行状态_v1 : std::uint8_t {
    进行中 = 1, 暂停 = 2, 等待条件 = 3, 阻塞 = 4, 失败 = 5,
    取消 = 6, 到期 = 7, 终止 = 8, 已完整完成 = 9
};

struct 服务准备当前事实_v1 final {
    服务准备事实身份_v1 身份{};
    L2存在身份 自我{};
    服务准备来源身份_v1 来源{L2需求身份{}};
    服务准备目标身份_v1 准备目标{};
    稳定编码 适用服务范围{};
    std::int64_t 有效开始完整秒边界 = 0;
    std::int64_t 有效结束完整秒边界 = 0;
    L2任务身份 任务{};
    L2方法身份 方法{};
    std::optional<稳定编码> T到D关系稳定编码{};
    服务进展执行冻结见证身份_v1 执行冻结见证{};
    服务进展方法执行见证身份_v1 方法执行见证{};
    服务进展安全门禁见证身份_v1 安全门禁见证{};
    std::optional<L2状态身份> 进展状态{};
    std::optional<L2动态身份> 进展动态{};
    std::optional<服务准备结果身份_v1> 准备结果{};
    std::optional<服务准备验证事实身份_v1> 完成验证{};
    服务准备运行状态_v1 运行状态 = 服务准备运行状态_v1::进行中;
    std::uint64_t 运行代次 = 0;
    std::int64_t 进展发生完整秒边界 = 0;
    std::int64_t 计量窗口开始完整秒边界 = 0;
    std::int64_t 计量窗口结束完整秒边界 = 0;
    std::uint64_t 方法内容版本 = 0;
    std::uint64_t 方法规格版本 = 0;
    std::uint64_t 方法生命周期版本 = 0;
    std::uint32_t 准备规则版本 = 0;
    std::uint64_t 形成事实代次 = 0;
    L2生命周期 生命周期{};
    friend bool operator==(const 服务准备当前事实_v1&,
        const 服务准备当前事实_v1&) = default;
};

struct 服务合同完整集合见证_v1 final {
    std::uint32_t 结构登记版本 = 0;
    std::uint64_t 声明成员数 = 0;
    std::vector<服务合同身份_v1> 规范成员身份组;
    std::uint64_t G0 = 0;
    friend bool operator==(const 服务合同完整集合见证_v1&,
        const 服务合同完整集合见证_v1&) = default;
};

struct 服务到期事件完整集合见证_v1 final {
    std::uint32_t 结构登记版本 = 0;
    std::uint64_t 声明成员数 = 0;
    std::vector<服务到期未满足事件身份_v1> 规范成员身份组;
    std::uint64_t G0 = 0;
    friend bool operator==(const 服务到期事件完整集合见证_v1&,
        const 服务到期事件完整集合见证_v1&) = default;
};

struct 服务进展完整集合见证_v1 final {
    std::uint32_t 结构登记版本 = 0;
    std::uint64_t 声明成员数 = 0;
    std::vector<服务进展事实身份_v1> 规范成员身份组;
    std::uint64_t G0 = 0;
    friend bool operator==(const 服务进展完整集合见证_v1&,
        const 服务进展完整集合见证_v1&) = default;
};

struct 服务准备完整集合见证_v1 final {
    std::uint32_t 结构登记版本 = 0;
    std::uint64_t 声明成员数 = 0;
    std::vector<服务准备事实身份_v1> 规范成员身份组;
    std::uint64_t G0 = 0;
    friend bool operator==(const 服务准备完整集合见证_v1&,
        const 服务准备完整集合见证_v1&) = default;
};

struct 服务合同完整集合读取请求_v1 final {
    std::uint32_t 合同版本 = 服务合同事实权威合同版本_v1;
    L2结构请求头 请求头{};
    L2存在身份 自我{};
    friend bool operator==(const 服务合同完整集合读取请求_v1&,
        const 服务合同完整集合读取请求_v1&) = default;
};

struct 服务到期事件完整集合读取请求_v1 final {
    std::uint32_t 合同版本 = 服务合同事实权威合同版本_v1;
    L2结构请求头 请求头{};
    L2存在身份 自我{};
    friend bool operator==(const 服务到期事件完整集合读取请求_v1&,
        const 服务到期事件完整集合读取请求_v1&) = default;
};

struct 服务进展完整集合读取请求_v1 final {
    std::uint32_t 合同版本 = 服务进展事实扩展合同版本_v1;
    L2结构请求头 请求头{};
    L2存在身份 自我{};
    friend bool operator==(const 服务进展完整集合读取请求_v1&,
        const 服务进展完整集合读取请求_v1&) = default;
};

struct 服务准备完整集合读取请求_v1 final {
    std::uint32_t 合同版本 = 服务准备事实扩展合同版本_v1;
    L2结构请求头 请求头{};
    L2存在身份 自我{};
    friend bool operator==(const 服务准备完整集合读取请求_v1&,
        const 服务准备完整集合读取请求_v1&) = default;
};

enum class 服务合同事实权威读取状态_v1 : std::uint8_t {
    已读取 = 1,
    入口拒绝 = 2,
    许可拒绝 = 3,
    当前性漂移 = 4,
    结构未登记 = 5,
    集合不闭合 = 6,
    引用冲突 = 7,
    资源失败 = 8,
    内部错误 = 9
};

struct 服务合同完整集合读取结果_v1 final {
    std::uint32_t 合同版本 = 服务合同事实权威合同版本_v1;
    服务合同事实权威读取状态_v1 状态 =
        服务合同事实权威读取状态_v1::入口拒绝;
    std::vector<服务合同事实_v1> 完整合同事实组;
    std::vector<服务合同状态事实_v1> 当前状态事实组;
    std::optional<服务合同完整集合见证_v1> 完整集合见证;
    std::uint64_t 本次正式读回截止 = 0;

    bool 成功() const noexcept;
    friend bool operator==(const 服务合同完整集合读取结果_v1&,
        const 服务合同完整集合读取结果_v1&) = default;
};

struct 服务到期事件完整集合读取结果_v1 final {
    std::uint32_t 合同版本 = 服务合同事实权威合同版本_v1;
    服务合同事实权威读取状态_v1 状态 =
        服务合同事实权威读取状态_v1::入口拒绝;
    std::vector<服务到期未满足事件事实_v1> 完整事件事实组;
    std::optional<服务到期事件完整集合见证_v1> 完整集合见证;
    std::uint64_t 本次正式读回截止 = 0;

    bool 成功() const noexcept;
    friend bool operator==(const 服务到期事件完整集合读取结果_v1&,
        const 服务到期事件完整集合读取结果_v1&) = default;
};

struct 服务进展完整集合读取结果_v1 final {
    std::uint32_t 合同版本 = 服务进展事实扩展合同版本_v1;
    服务合同事实权威读取状态_v1 状态 =
        服务合同事实权威读取状态_v1::入口拒绝;
    std::vector<服务合同关联进展事实_v1> 完整进展事实组;
    std::optional<服务进展完整集合见证_v1> 完整集合见证;
    std::uint64_t 本次正式读回截止 = 0;

    bool 成功() const noexcept;
    friend bool operator==(const 服务进展完整集合读取结果_v1&,
        const 服务进展完整集合读取结果_v1&) = default;
};

struct 服务准备完整集合读取结果_v1 final {
    std::uint32_t 合同版本 = 服务准备事实扩展合同版本_v1;
    服务合同事实权威读取状态_v1 状态 =
        服务合同事实权威读取状态_v1::入口拒绝;
    std::vector<服务准备当前事实_v1> 完整准备事实组;
    std::optional<服务准备完整集合见证_v1> 完整集合见证;
    std::uint64_t 本次正式读回截止 = 0;
    bool 成功() const noexcept;
    friend bool operator==(const 服务准备完整集合读取结果_v1&,
        const 服务准备完整集合读取结果_v1&) = default;
};

inline bool 服务合同当前状态有效_v1(服务合同当前状态_v1 状态) noexcept {
    return 状态 == 服务合同当前状态_v1::有效未满足
        || 状态 == 服务合同当前状态_v1::已完整完成
        || 状态 == 服务合同当前状态_v1::提出者取消
        || 状态 == 服务合同当前状态_v1::已到期
        || 状态 == 服务合同当前状态_v1::正式终止;
}

inline bool 服务合同事实完整_v1(
    const 服务合同事实_v1& 事实, std::uint64_t 截止) noexcept {
    if (截止 == 0 || !有效(事实.身份) || !有效(事实.自我.值)
        || !有效(事实.提出者.值) || !有效(事实.需求.值)
        || !有效(事实.来源提出事件) || !有效(事实.目标宿主.值)
        || !有效(事实.目标特征.值) || !有效(事实.目标状态合同.值)
        || !有效(事实.服务范围) || 事实.提出运行代次 == 0
        || !有效(事实.时间纪元身份) || 事实.提出完整秒边界 < 0
        || 事实.有效开始完整秒边界 < 事实.提出完整秒边界
        || 事实.冻结有效总秒 <= 0 || 事实.冻结预算 <= 0
        || 事实.预支值 <= 0 || 事实.预支值 > 事实.冻结预算
        || 事实.完工余款 != 事实.冻结预算 - 事实.预支值
        || 事实.需求版本 == 0 || !有效(事实.法规准入见证)
        || 事实.法规规则版本 == 0 || 事实.合同代次 == 0
        || 事实.合同规则版本 == 0 || 事实.形成事实代次 == 0
        || 事实.形成事实代次 > 截止)
        return false;
    const auto 预支 = (事实.冻结预算 / 10) * 3
        + ((事实.冻结预算 % 10) * 3) / 10;
    return 事实.预支值 == (预支 == 0 ? 1 : 预支);
}

inline bool 服务合同状态事实完整_v1(
    const 服务合同状态事实_v1& 事实, std::uint64_t 截止) noexcept {
    return 截止 != 0 && 有效(事实.合同)
        && 服务合同当前状态有效_v1(事实.状态)
        && 事实.已消费有效秒 >= 0
        && 事实.最后已结算完整秒边界 >= 0
        && 事实.状态版本 != 0 && 有效(事实.来源事实)
        && 事实.形成事实代次 != 0 && 事实.形成事实代次 <= 截止;
}

inline bool 服务到期未满足事件事实完整_v1(
    const 服务到期未满足事件事实_v1& 事实, std::uint64_t 截止) noexcept {
    return 截止 != 0 && 有效(事实.身份) && 有效(事实.合同)
        && 有效(事实.需求.值) && 有效(事实.提出者.值)
        && 事实.冻结有效总秒 > 0 && 事实.实际已消费有效秒 >= 0
        && 事实.实际已消费有效秒 <= 事实.冻结有效总秒
        && 事实.到期完整秒边界 >= 0 && 有效(事实.目标未满足事实)
        && 事实.合同终态版本 != 0 && 事实.规则版本 != 0
        && 事实.形成事实代次 != 0 && 事实.形成事实代次 <= 截止;
}

inline bool 服务进展运行状态有效_v1(服务进展运行状态_v1 状态) noexcept {
    return 状态 >= 服务进展运行状态_v1::进行中
        && 状态 <= 服务进展运行状态_v1::终止;
}

inline bool 服务合同关联进展事实完整_v1(
    const 服务合同关联进展事实_v1& 事实, std::uint64_t 截止) noexcept {
    return 截止 != 0 && 有效(事实.身份) && 有效(事实.自我.值)
        && 有效(事实.服务合同) && 有效(事实.需求.值)
        && 有效(事实.任务.值) && 有效(事实.方法.值)
        && 有效(事实.T到D关系稳定编码) && 有效(事实.执行冻结见证)
        && 有效(事实.方法执行见证) && 有效(事实.安全门禁见证)
        && (事实.进展状态.has_value() || 事实.进展动态.has_value())
        && (!事实.进展状态 || 有效(事实.进展状态->值))
        && (!事实.进展动态 || 有效(事实.进展动态->值))
        && 服务进展运行状态有效_v1(事实.运行状态)
        && 事实.运行代次 != 0 && 事实.计量窗口开始完整秒边界 >= 0
        && 事实.进展发生完整秒边界 >= 事实.计量窗口开始完整秒边界
        && 事实.计量窗口结束完整秒边界 >= 事实.进展发生完整秒边界
        && 事实.方法内容版本 != 0 && 事实.方法规格版本 != 0
        && 事实.方法生命周期版本 != 0 && 事实.进展规则版本 != 0
        && 事实.形成事实代次 != 0 && 事实.形成事实代次 <= 截止
        && L2生命周期完整(事实.生命周期)
        && 事实.生命周期.创建事实代次 <= 截止
        && (!事实.生命周期.退出事实代次
            || *事实.生命周期.退出事实代次 > 截止);
}

inline bool 服务准备运行状态有效_v1(服务准备运行状态_v1 状态) noexcept {
    return 状态 >= 服务准备运行状态_v1::进行中
        && 状态 <= 服务准备运行状态_v1::已完整完成;
}

inline bool 服务准备当前事实完整_v1(
    const 服务准备当前事实_v1& 事实, std::uint64_t 截止) noexcept {
    const bool 需求来源 = std::holds_alternative<L2需求身份>(事实.来源);
    const bool 来源有效 = 需求来源
        ? 有效(std::get<L2需求身份>(事实.来源).值)
            && 事实.T到D关系稳定编码 && 有效(*事实.T到D关系稳定编码)
        : 有效(std::get<服务能力缺口身份_v1>(事实.来源))
            && !事实.T到D关系稳定编码;
    const bool 进展形状 = 事实.运行状态 != 服务准备运行状态_v1::进行中
        || 事实.进展状态 || 事实.进展动态;
    const bool 完成形状 = 事实.运行状态 == 服务准备运行状态_v1::已完整完成
        ? 事实.准备结果 && 有效(*事实.准备结果)
            && 事实.完成验证 && 有效(*事实.完成验证)
        : true;
    return 截止 != 0 && 有效(事实.身份) && 有效(事实.自我.值)
        && 来源有效 && 有效(事实.准备目标) && 有效(事实.适用服务范围)
        && 事实.有效开始完整秒边界 >= 0
        && 事实.有效结束完整秒边界 >= 事实.有效开始完整秒边界
        && 有效(事实.任务.值) && 有效(事实.方法.值)
        && 有效(事实.执行冻结见证) && 有效(事实.方法执行见证)
        && 有效(事实.安全门禁见证) && 进展形状
        && (!事实.进展状态 || 有效(事实.进展状态->值))
        && (!事实.进展动态 || 有效(事实.进展动态->值)) && 完成形状
        && 服务准备运行状态有效_v1(事实.运行状态)
        && 事实.运行代次 != 0
        && 事实.计量窗口开始完整秒边界 >= 事实.有效开始完整秒边界
        && 事实.进展发生完整秒边界 >= 事实.计量窗口开始完整秒边界
        && 事实.计量窗口结束完整秒边界 >= 事实.进展发生完整秒边界
        && 事实.计量窗口结束完整秒边界 <= 事实.有效结束完整秒边界
        && 事实.方法内容版本 != 0 && 事实.方法规格版本 != 0
        && 事实.方法生命周期版本 != 0 && 事实.准备规则版本 != 0
        && 事实.形成事实代次 != 0 && 事实.形成事实代次 <= 截止
        && L2生命周期完整(事实.生命周期)
        && 事实.生命周期.创建事实代次 <= 截止
        && (!事实.生命周期.退出事实代次
            || *事实.生命周期.退出事实代次 > 截止);
}

template<class 身份类型>
inline bool 服务合同事实权威身份组严格升序_v1(
    const std::vector<身份类型>& 身份组) noexcept {
    std::uint64_t 前 = 0;
    for (const auto& 身份 : 身份组) {
        if (!有效(身份) || 身份.值.值 <= 前) return false;
        前 = 身份.值.值;
    }
    return true;
}

inline bool 服务合同完整集合见证完整_v1(
    const 服务合同完整集合见证_v1& 见证) noexcept {
    return 见证.结构登记版本 == 服务合同事实权威结构登记版本_v1
        && 见证.G0 != 0 && 见证.声明成员数 == 见证.规范成员身份组.size()
        && 服务合同事实权威身份组严格升序_v1(见证.规范成员身份组);
}

inline bool 服务到期事件完整集合见证完整_v1(
    const 服务到期事件完整集合见证_v1& 见证) noexcept {
    return 见证.结构登记版本 == 服务合同事实权威结构登记版本_v1
        && 见证.G0 != 0 && 见证.声明成员数 == 见证.规范成员身份组.size()
        && 服务合同事实权威身份组严格升序_v1(见证.规范成员身份组);
}

inline bool 服务进展完整集合见证完整_v1(
    const 服务进展完整集合见证_v1& 见证) noexcept {
    return 见证.结构登记版本 == 服务进展事实结构登记版本_v1
        && 见证.G0 != 0 && 见证.声明成员数 == 见证.规范成员身份组.size()
        && 服务合同事实权威身份组严格升序_v1(见证.规范成员身份组);
}

inline bool 服务准备完整集合见证完整_v1(
    const 服务准备完整集合见证_v1& 见证) noexcept {
    return 见证.结构登记版本 == 服务准备事实结构登记版本_v1
        && 见证.G0 != 0 && 见证.声明成员数 == 见证.规范成员身份组.size()
        && 服务合同事实权威身份组严格升序_v1(见证.规范成员身份组);
}

inline bool 服务合同完整集合读取结果_v1::成功() const noexcept {
    if (合同版本 != 服务合同事实权威合同版本_v1
        || 状态 != 服务合同事实权威读取状态_v1::已读取
        || !完整集合见证 || !服务合同完整集合见证完整_v1(*完整集合见证)
        || 本次正式读回截止 == 0
        || 完整集合见证->G0 != 本次正式读回截止
        || 完整合同事实组.size() != 当前状态事实组.size()
        || 完整合同事实组.size() != 完整集合见证->规范成员身份组.size())
        return false;
    for (std::size_t i = 0; i < 完整合同事实组.size(); ++i) {
        if (!服务合同事实完整_v1(完整合同事实组[i], 本次正式读回截止)
            || !服务合同状态事实完整_v1(
                当前状态事实组[i], 本次正式读回截止)
            || 当前状态事实组[i].状态 != 服务合同当前状态_v1::有效未满足
            || 当前状态事实组[i].合同 != 完整合同事实组[i].身份
            || 完整合同事实组[i].身份 != 完整集合见证->规范成员身份组[i])
            return false;
    }
    return true;
}

inline bool 服务到期事件完整集合读取结果_v1::成功() const noexcept {
    if (合同版本 != 服务合同事实权威合同版本_v1
        || 状态 != 服务合同事实权威读取状态_v1::已读取
        || !完整集合见证
        || !服务到期事件完整集合见证完整_v1(*完整集合见证)
        || 本次正式读回截止 == 0
        || 完整集合见证->G0 != 本次正式读回截止
        || 完整事件事实组.size() != 完整集合见证->规范成员身份组.size())
        return false;
    for (std::size_t i = 0; i < 完整事件事实组.size(); ++i) {
        if (!服务到期未满足事件事实完整_v1(
                完整事件事实组[i], 本次正式读回截止)
            || 完整事件事实组[i].身份 != 完整集合见证->规范成员身份组[i])
            return false;
    }
    return true;
}

inline bool 服务进展完整集合读取结果_v1::成功() const noexcept {
    if (合同版本 != 服务进展事实扩展合同版本_v1
        || 状态 != 服务合同事实权威读取状态_v1::已读取
        || !完整集合见证 || !服务进展完整集合见证完整_v1(*完整集合见证)
        || 本次正式读回截止 == 0
        || 完整集合见证->G0 != 本次正式读回截止
        || 完整进展事实组.size() != 完整集合见证->规范成员身份组.size())
        return false;
    for (std::size_t i = 0; i < 完整进展事实组.size(); ++i) {
        if (!服务合同关联进展事实完整_v1(
                完整进展事实组[i], 本次正式读回截止)
            || 完整进展事实组[i].身份 != 完整集合见证->规范成员身份组[i])
            return false;
    }
    return true;
}

inline bool 服务准备完整集合读取结果_v1::成功() const noexcept {
    if (合同版本 != 服务准备事实扩展合同版本_v1
        || 状态 != 服务合同事实权威读取状态_v1::已读取
        || !完整集合见证 || !服务准备完整集合见证完整_v1(*完整集合见证)
        || 本次正式读回截止 == 0
        || 完整集合见证->G0 != 本次正式读回截止
        || 完整准备事实组.size() != 完整集合见证->规范成员身份组.size())
        return false;
    for (std::size_t i = 0; i < 完整准备事实组.size(); ++i) {
        if (!服务准备当前事实完整_v1(
                完整准备事实组[i], 本次正式读回截止)
            || 完整准备事实组[i].身份 != 完整集合见证->规范成员身份组[i])
            return false;
    }
    return true;
}

} // namespace 海中鱼巣
