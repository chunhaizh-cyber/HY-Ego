module;

#include <array>
#include <cstddef>
#include <cstdint>
#include <mutex>
#include <new>
#include <optional>
#include <variant>

export module 海中鱼巣.领域.服务.世界登记;

export import 海中鱼巣.核心.合同.L1中性CRUD;
import 海中鱼巣.核心.服务.L1事实基座;

#define 世界登记数据已导入L1
export {
#include "世界登记.数据.h"
}
#undef 世界登记数据已导入L1

export namespace 海中鱼巣 {

namespace 世界登记内部 {

// 诊断责任：无适用错误分支；纯值幂等身份映射。
L1中性写集幂等键 映射L1幂等键(世界操作幂等身份 身份) noexcept {
    return {(世界登记中性幂等域 << 56) | 身份.值};
}

} // namespace 世界登记内部

class 世界登记服务 final {
public:
    explicit 世界登记服务(L1事实基座服务& L1) noexcept : L1_(L1) {}
    世界登记服务(const 世界登记服务&) = delete;
    世界登记服务& operator=(const 世界登记服务&) = delete;

    // 诊断责任：向上送出；全部非成功保留为结构化世界登记状态。
    世界登记结果 建立空世界(const 世界登记建立请求& 请求) {
        std::lock_guard<std::mutex> 锁(锁_);
        try {
            if (!世界登记建立请求有效(请求)) return {};

            const auto 幂等键 = 世界登记内部::映射L1幂等键(请求.幂等身份);
            L1中性写集请求 写集;
            写集.合同版本 = L1中性CRUD合同版本;
            写集.期望事实代次 = 请求.期望事实代次;
            写集.幂等键 = 幂等键;
            写集.节点 = {
                {{1}, 节点种类::普通, std::nullopt},
                {{2}, 节点种类::属性类型, L1中性值表示种类::I64},
                {{3}, 节点种类::普通, std::nullopt},
                {{4}, 节点种类::普通, std::nullopt},
                {{5}, 节点种类::普通, std::nullopt}
            };
            写集.值 = {{
                {6}, L1中性写集本地键{5}, L1中性写集本地键{2},
                std::int64_t{1}, L1中性写集本地键{1}
            }};
            写集.属性槽变更 = {{
                L1中性写集本地键{5}, L1中性写集本地键{2},
                L1中性写集本地键{6}
            }};

            const auto 写入 = L1_.提交中性写集(写集);
            const auto 写入状态 = 映射写入状态(写入.状态);
            if (写入状态 != 世界登记状态::已提交
                && 写入状态 != 世界登记状态::幂等读回) {
                return {写入状态, std::nullopt};
            }

            const bool 首次成功完整 = 写入.状态 == L1中性写入状态::成功
                && 写入.是否形成内存权威发布
                && 写入.重试边界 == L1中性重试边界::不适用;
            const bool 重复成功完整 = 写入.状态 == L1中性写入状态::精确重复
                && !写入.是否形成内存权威发布
                && 写入.重试边界 == L1中性重试边界::原幂等键读回收敛;
            if (写入.合同版本 != L1中性CRUD合同版本
                || !(写入.幂等键 == 幂等键) || 写入.事实代次 == 0
                || 写入.新编码映射.size() != 6
                || (!首次成功完整 && !重复成功完整)
                || !映射唯一(写入)) {
                return {世界登记状态::内部不一致, std::nullopt};
            }

            const auto 编码1 = 查找映射(写入, 1);
            const auto 编码2 = 查找映射(写入, 2);
            const auto 编码3 = 查找映射(写入, 3);
            const auto 编码4 = 查找映射(写入, 4);
            const auto 编码5 = 查找映射(写入, 5);
            const auto 编码6 = 查找映射(写入, 6);
            if (!编码1 || !编码2 || !编码3 || !编码4 || !编码5 || !编码6) {
                return {世界登记状态::内部不一致, std::nullopt};
            }

            世界结构登记 新登记;
            新登记.首次幂等身份 = 请求.幂等身份;
            新登记.首次中性写集幂等键 = 幂等键.值;
            新登记.已验证事实代次 = 写入.事实代次;
            新登记.服务身份 = *编码1;
            新登记.场景标记属性类型 = *编码2;
            新登记.直接父场景关系类型 = *编码3;
            新登记.直接成员关系类型 = *编码4;
            新登记.世界根场景 = *编码5;
            if (!世界结构登记完整(新登记)) {
                return {世界登记状态::内部不一致, std::nullopt};
            }

            const auto 读回 = 按登记读取权威事实(新登记);
            if (!读回.成功()) return {读回.状态, std::nullopt};
            登记_ = 新登记;
            return {写入状态, 登记_};
        } catch (const std::bad_alloc&) {
            return {世界登记状态::资源失败, std::nullopt};
        } catch (...) {
            return {世界登记状态::内部不一致, std::nullopt};
        }
    }

    // 诊断责任：向上送出；缓存只作定位，成功必须来自L1权威精确读回。
    世界登记结果 读取当前登记() const {
        std::lock_guard<std::mutex> 锁(锁_);
        if (!登记_.has_value()) return {世界登记状态::登记未加载, std::nullopt};
        try {
            return 按登记读取权威事实(*登记_);
        } catch (const std::bad_alloc&) {
            return {世界登记状态::资源失败, std::nullopt};
        } catch (...) {
            return {世界登记状态::内部不一致, std::nullopt};
        }
    }

private:
    // 诊断责任：无适用错误分支；一致当前读取顶层状态机械映射。
    static 世界登记状态 映射一致读取失败(
        L1中性一致当前读取状态 状态) noexcept {
        switch (状态) {
        case L1中性一致当前读取状态::许可拒绝:
            return 世界登记状态::许可拒绝;
        case L1中性一致当前读取状态::事实代次漂移:
            return 世界登记状态::事实代次漂移;
        case L1中性一致当前读取状态::资源失败:
            return 世界登记状态::资源失败;
        case L1中性一致当前读取状态::成功:
        case L1中性一致当前读取状态::入口拒绝:
        case L1中性一致当前读取状态::内部不一致:
            return 世界登记状态::内部不一致;
        }
        return 世界登记状态::内部不一致;
    }

    // 诊断责任：无适用错误分支；纯值核对顶层非成功的空材料与代次边界。
    static bool 一致读取失败形状完整(
        const L1中性一致当前读取结果& 结果,
        std::uint64_t 期望代次) noexcept {
        if (结果.状态 == L1中性一致当前读取状态::成功
            || 结果.合同版本 != L1中性一致当前读取合同版本
            || 结果.期望事实代次 != 期望代次
            || !结果.节点.empty() || !结果.关系.empty()
            || !结果.值.empty() || !结果.属性值.empty()
            || !结果.源关系组.empty() || !结果.目标关系组.empty()) {
            return false;
        }
        return 结果.状态 == L1中性一致当前读取状态::事实代次漂移
            ? 结果.读取事实代次 != 0 && 结果.读取事实代次 != 期望代次
            : 结果.读取事实代次 == 0;
    }

    // 诊断责任：无适用错误分支；中性写入状态机械映射。
    static 世界登记状态 映射写入状态(L1中性写入状态 状态) noexcept {
        switch (状态) {
        case L1中性写入状态::成功: return 世界登记状态::已提交;
        case L1中性写入状态::精确重复: return 世界登记状态::幂等读回;
        case L1中性写入状态::入口拒绝: return 世界登记状态::入口拒绝;
        case L1中性写入状态::事实代次漂移: return 世界登记状态::事实代次漂移;
        case L1中性写入状态::幂等冲突: return 世界登记状态::幂等冲突;
        case L1中性写入状态::资源失败: return 世界登记状态::资源失败;
        case L1中性写入状态::未找到:
        case L1中性写入状态::已退出:
        case L1中性写入状态::内部不一致:
            return 世界登记状态::内部不一致;
        }
        return 世界登记状态::内部不一致;
    }

    // 诊断责任：无适用错误分支；纯值映射唯一查找。
    static std::optional<稳定编码> 查找映射(
        const L1中性写入结果& 结果, std::uint32_t 本地键) noexcept {
        std::optional<稳定编码> 找到;
        for (const auto& [键, 编码] : 结果.新编码映射) {
            if (键.值 != 本地键) continue;
            if (找到.has_value()) return std::nullopt;
            找到 = 编码;
        }
        return 找到;
    }

    // 诊断责任：无适用错误分支；纯值映射完整性判断。
    static bool 映射唯一(const L1中性写入结果& 结果) noexcept {
        for (std::size_t i = 0; i < 结果.新编码映射.size(); ++i) {
            if (!有效(结果.新编码映射[i].first)
                || !有效(结果.新编码映射[i].second)) return false;
            for (std::size_t j = i + 1; j < 结果.新编码映射.size(); ++j) {
                if (结果.新编码映射[i].first == 结果.新编码映射[j].first
                    || 结果.新编码映射[i].second == 结果.新编码映射[j].second) {
                    return false;
                }
            }
        }
        return true;
    }

    // 诊断责任：向上送出；一致读取失败和字段矛盾保留为结构化状态。
    世界登记结果 按登记读取权威事实(const 世界结构登记& 登记) const {
        if (!世界结构登记完整(登记)) {
            return {世界登记状态::内部不一致, std::nullopt};
        }

        const std::array<稳定编码, 5> 编码组{
            登记.服务身份, 登记.场景标记属性类型,
            登记.直接父场景关系类型, 登记.直接成员关系类型,
            登记.世界根场景};
        std::uint64_t 期望代次 = 登记.已验证事实代次;
        constexpr std::size_t 最大尝试次数 = 2;
        for (std::size_t 尝试序号 = 0; 尝试序号 < 最大尝试次数; ++尝试序号) {
            L1中性一致当前读取请求 请求;
            请求.合同版本 = L1中性一致当前读取合同版本;
            请求.期望事实代次 = 期望代次;
            请求.节点.assign(编码组.begin(), 编码组.end());
            请求.属性值 = {{登记.世界根场景, 登记.场景标记属性类型}};

            const auto 读取 = L1_.尝试读取中性一致当前投影(请求);
            if (读取.状态 == L1中性一致当前读取状态::事实代次漂移) {
                if (!一致读取失败形状完整(读取, 期望代次)) {
                    return {世界登记状态::内部不一致, std::nullopt};
                }
                if (尝试序号 == 0) {
                    期望代次 = 读取.读取事实代次;
                    continue;
                }
                return {世界登记状态::事实代次漂移, std::nullopt};
            }
            if (读取.状态 != L1中性一致当前读取状态::成功) {
                if (!一致读取失败形状完整(读取, 期望代次)) {
                    return {世界登记状态::内部不一致, std::nullopt};
                }
                return {映射一致读取失败(读取.状态), std::nullopt};
            }
            if (读取.合同版本 != L1中性一致当前读取合同版本
                || 读取.期望事实代次 != 期望代次
                || 读取.读取事实代次 == 0
                || 读取.读取事实代次 != 期望代次
                || 读取.节点.size() != 编码组.size()
                || !读取.关系.empty() || !读取.值.empty()
                || 读取.属性值.size() != 1
                || !读取.源关系组.empty() || !读取.目标关系组.empty()) {
                return {世界登记状态::内部不一致, std::nullopt};
            }

            std::uint64_t 登记创建代次 = 0;
            for (std::size_t i = 0; i < 读取.节点.size(); ++i) {
                const auto& 结果 = 读取.节点[i];
                if (结果.查询编码 != 编码组[i]
                    || 结果.状态 != L1中性一致当前读取项目状态::成功
                    || !结果.事实.has_value()) {
                    return {世界登记状态::内部不一致, std::nullopt};
                }
                const auto& 事实 = *结果.事实;
                const auto 期望种类 = i == 1
                    ? 节点种类::属性类型 : 节点种类::普通;
                if (i == 0) 登记创建代次 = 事实.创建事实代次;
                if (事实.编码 != 编码组[i] || 事实.种类 != 期望种类
                    || 登记创建代次 == 0
                    || 事实.创建事实代次 != 登记创建代次
                    || 事实.创建事实代次 > 读取.读取事实代次
                    || 事实.退出事实代次.has_value()
                    || (i == 1 && 事实.属性类型表示
                        != std::optional<L1中性值表示种类>{
                            L1中性值表示种类::I64})
                    || (i != 1 && 事实.属性类型表示.has_value())
                    || (i != 4 && !事实.当前属性.empty())) {
                    return {世界登记状态::内部不一致, std::nullopt};
                }
            }

            const auto& 根 = *读取.节点[4].事实;
            const auto& 属性 = 读取.属性值[0];
            if (属性.节点 != 登记.世界根场景
                || 属性.属性类型 != 登记.场景标记属性类型
                || 属性.状态 != L1中性一致当前读取项目状态::成功
                || !属性.投影.has_value() || 根.当前属性.size() != 1
                || 属性.投影->属性槽 != 根.当前属性[0]
                || 属性.投影->属性槽.属性类型节点
                    != 登记.场景标记属性类型
                || 属性.投影->属性槽.当前值
                    != 属性.投影->当前值事实.编码) {
                return {世界登记状态::内部不一致, std::nullopt};
            }

            const auto& 值事实 = 属性.投影->当前值事实;
            if (值事实.所属节点 != 登记.世界根场景
                || 值事实.属性类型节点 != 登记.场景标记属性类型
                || 值事实.来源节点 != 登记.服务身份
                || 值事实.创建事实代次 != 登记创建代次
                || 值事实.创建事实代次 > 读取.读取事实代次
                || 值事实.退出事实代次.has_value()
                || !std::holds_alternative<std::int64_t>(值事实.材料)
                || std::get<std::int64_t>(值事实.材料) != 1) {
                return {世界登记状态::内部不一致, std::nullopt};
            }
            auto 当前登记 = 登记;
            当前登记.已验证事实代次 = 读取.读取事实代次;
            return {世界登记状态::已读取, 当前登记};
        }
        return {世界登记状态::内部不一致, std::nullopt};
    }

    L1事实基座服务& L1_;
    mutable std::mutex 锁_;
    std::optional<世界结构登记> 登记_;
};

} // namespace 海中鱼巣
