// 文件规则：节点直接退役自检只构造隔离候选并报告合同边界，不进入生产依赖、不接统一运行器、不修改机器事实。
module;

#include "节点直接P3合同.数据.h"

#include <cstdint>
#include <vector>

export module 海中鱼巣.领域.自检.节点直接退役;

import 海中鱼巣.领域.退役.节点直接对象;
import 海中鱼巣.领域.审计.节点直接退役;

export namespace 海中鱼巣::领域 {

enum class 节点直接退役自检项目 : std::uint16_t {
    八类强类型重载 = 1,
    关系先于记录与节点 = 2,
    概念替代关系十同事务 = 3,
    权威回执与后验分型 = 4,
    退役候选过期零写 = 5,
    发布后投影失败不否认权威退役 = 6,
    旧域与通用载荷零命中 = 7,
    上游与共享文件零修改 = 8
};

struct 节点直接退役自检项目结果 {
    节点直接退役自检项目 项目 = 节点直接退役自检项目::八类强类型重载;
    节点直接P3结果状态 状态 = 节点直接P3结果状态::请求拒绝;
    std::uint32_t 证据数量 = 0;
};

struct 节点直接退役自检报告 {
    std::uint32_t 合同版本 = 0x00020000;
    std::uint32_t 用例总数 = 0;
    std::uint32_t 通过数 = 0;
    std::vector<节点直接退役自检项目结果> 项目组;
};

节点直接退役自检报告 运行节点直接退役自检();

}
