#pragma once

#ifndef INSTINCT_PASSIVE_MAINTENANCE_CURSOR_NO_INCLUDES
#include <cstdint>
#include <optional>
#include "L2结构公共.数据.h"
#include "L2状态动态原子发布.数据.h"
#include "服务合同事实权威.数据.h"
#include "../业务/本能根运行初始化.数据.h"
#include "../核心/L1所有者范围CRUD.数据.h"
#endif

namespace 海中鱼巣 {

inline constexpr std::uint32_t 本能被动维护游标合同版本_v1 = 1;
inline constexpr std::uint32_t 本能被动维护游标合同版本_v2 = 2;
inline constexpr std::uint32_t 本能被动维护游标合同版本_v3 = 3;
inline constexpr std::uint32_t 本能被动维护游标合同版本_v4 = 4;
inline constexpr std::uint32_t 本能被动维护纪元激活合同版本_v1 = 1;
inline constexpr std::uint32_t 服务时间维护规则版本_v1 = 1;
inline constexpr L1所有者范围建立幂等身份
    本能被动维护游标所有者建立身份_v1{0x494E'5354'4355'5253ULL};

struct 本能被动维护游标事实_v1 final {
    L2存在身份 自我{};
    std::uint64_t 运行代次 = 0;
    std::uint64_t 时间纪元身份 = 0;
    std::int64_t 上一已维护完整秒边界 = 0;
    std::uint32_t 时间源版本 = 0;
    std::uint32_t 服务时间维护规则版本 = 0;
    std::uint64_t 形成事实代次 = 0;
    friend bool operator==(const 本能被动维护游标事实_v1&,
        const 本能被动维护游标事实_v1&) = default;
};

inline bool 本能被动维护游标事实完整_v1(
    const 本能被动维护游标事实_v1& 事实) noexcept {
    return 有效(事实.自我.值) && 事实.运行代次 != 0
        && 事实.时间纪元身份 != 0
        && 事实.上一已维护完整秒边界 >= 0
        && 事实.时间源版本 != 0
        && 事实.服务时间维护规则版本 != 0
        && 事实.形成事实代次 != 0;
}

struct 建立或恢复本能被动维护游标请求_v1 final {
    std::uint32_t 合同版本 = 本能被动维护游标合同版本_v1;
    L2结构请求头 请求头{};
    L2结构幂等身份 幂等身份{};
    本能根运行锚点_v1 本能根锚点;
    std::uint64_t 运行代次 = 0;
    std::uint64_t 时间纪元身份 = 0;
    std::int64_t 当前完整秒边界 = 0;
    std::uint32_t 时间源版本 = 0;
    std::uint32_t 服务时间维护规则版本 = 服务时间维护规则版本_v1;
    friend bool operator==(const 建立或恢复本能被动维护游标请求_v1&,
        const 建立或恢复本能被动维护游标请求_v1&) = default;
};

enum class 本能被动维护游标建立状态_v1 : std::uint8_t {
    已建立 = 1,
    已恢复 = 2,
    已建立新纪元 = 3,
    入口拒绝 = 4,
    当前性漂移 = 5,
    幂等冲突 = 6,
    引用冲突 = 7,
    资源失败 = 8,
    内部错误 = 9,
    已可能发布 = 10
};

struct 本能被动维护游标建立结果_v1 final {
    std::uint32_t 合同版本 = 本能被动维护游标合同版本_v1;
    本能被动维护游标建立状态_v1 状态 =
        本能被动维护游标建立状态_v1::入口拒绝;
    L2结构幂等身份 幂等身份{};
    std::optional<本能被动维护游标事实_v1> 游标{};
    std::uint64_t 首次提交事实代次 = 0;
    std::uint64_t 本次正式读回截止 = 0;

    bool 成功() const noexcept {
        return 合同版本 == 本能被动维护游标合同版本_v1
            && (状态 == 本能被动维护游标建立状态_v1::已建立
                || 状态 == 本能被动维护游标建立状态_v1::已恢复
                || 状态 == 本能被动维护游标建立状态_v1::已建立新纪元)
            && L2结构幂等身份有效(幂等身份)
            && 游标 && 本能被动维护游标事实完整_v1(*游标)
            && 首次提交事实代次 != 0
            && 本次正式读回截止 != 0
            && 首次提交事实代次 <= 本次正式读回截止;
    }

    friend bool operator==(const 本能被动维护游标建立结果_v1&,
        const 本能被动维护游标建立结果_v1&) = default;
};

struct 本能被动维护游标读取请求_v1 final {
    std::uint32_t 合同版本 = 本能被动维护游标合同版本_v1;
    L2结构请求头 请求头{};
    L2存在身份 自我{};
    friend bool operator==(const 本能被动维护游标读取请求_v1&,
        const 本能被动维护游标读取请求_v1&) = default;
};

enum class 本能被动维护游标读取状态_v1 : std::uint8_t {
    已读取 = 1,
    未建立 = 2,
    入口拒绝 = 3,
    当前性漂移 = 4,
    引用冲突 = 5,
    资源失败 = 6,
    内部错误 = 7
};

struct 本能被动维护游标读取结果_v1 final {
    std::uint32_t 合同版本 = 本能被动维护游标合同版本_v1;
    本能被动维护游标读取状态_v1 状态 =
        本能被动维护游标读取状态_v1::入口拒绝;
    std::optional<本能被动维护游标事实_v1> 游标{};
    std::uint64_t 本次正式读回截止 = 0;

    bool 成功() const noexcept {
        return 合同版本 == 本能被动维护游标合同版本_v1
            && 状态 == 本能被动维护游标读取状态_v1::已读取
            && 游标 && 本能被动维护游标事实完整_v1(*游标)
            && 本次正式读回截止 != 0
            && 游标->形成事实代次 <= 本次正式读回截止;
    }

    friend bool operator==(const 本能被动维护游标读取结果_v1&,
        const 本能被动维护游标读取结果_v1&) = default;
};

struct 本能被动维护游标事实_v2 final {
    L2存在身份 自我{};
    std::uint64_t 运行代次 = 0;
    std::uint64_t 时间纪元身份 = 0;
    std::int64_t 上一已维护完整秒边界 = 0;
    std::uint64_t 已消费主动安全变化事实代次 = 0;
    std::uint32_t 时间源版本 = 0;
    std::uint32_t 服务时间维护规则版本 = 0;
    std::uint64_t 形成事实代次 = 0;
    friend bool operator==(const 本能被动维护游标事实_v2&,
        const 本能被动维护游标事实_v2&) = default;
};

inline bool 本能被动维护游标事实完整_v2(
    const 本能被动维护游标事实_v2& 事实) noexcept {
    return 有效(事实.自我.值) && 事实.运行代次 != 0
        && 事实.时间纪元身份 != 0
        && 事实.上一已维护完整秒边界 >= 0
        && 事实.已消费主动安全变化事实代次 != 0
        && 事实.时间源版本 != 0
        && 事实.服务时间维护规则版本 != 0
        && 事实.形成事实代次 != 0;
}

struct 建立或恢复本能被动维护游标请求_v2 final {
    std::uint32_t 合同版本 = 本能被动维护游标合同版本_v2;
    L2结构请求头 请求头{};
    L2结构幂等身份 幂等身份{};
    本能根运行锚点_v1 本能根锚点;
    std::uint64_t 运行代次 = 0;
    std::uint64_t 时间纪元身份 = 0;
    std::int64_t 当前完整秒边界 = 0;
    std::uint64_t 目标已消费主动安全变化事实代次 = 0;
    L2特征当前值变化账登记结果_v2 变化账登记见证{};
    std::uint32_t 时间源版本 = 0;
    std::uint32_t 服务时间维护规则版本 = 服务时间维护规则版本_v1;
    friend bool operator==(const 建立或恢复本能被动维护游标请求_v2&,
        const 建立或恢复本能被动维护游标请求_v2&) = default;
};

enum class 本能被动维护游标建立状态_v2 : std::uint8_t {
    已建立 = 1,
    已恢复 = 2,
    已迁移 = 3,
    已建立新纪元 = 4,
    精确重复 = 5,
    入口拒绝 = 6,
    当前性漂移 = 7,
    幂等冲突 = 8,
    引用冲突 = 9,
    资源失败 = 10,
    内部错误 = 11,
    已可能发布 = 12
};

struct 本能被动维护游标建立结果_v2 final {
    std::uint32_t 合同版本 = 本能被动维护游标合同版本_v2;
    本能被动维护游标建立状态_v2 状态 =
        本能被动维护游标建立状态_v2::入口拒绝;
    L2结构幂等身份 幂等身份{};
    std::optional<本能被动维护游标事实_v2> 提交游标{};
    std::uint64_t 首次提交事实代次 = 0;
    std::uint64_t 本次正式读回截止 = 0;

    bool 成功() const noexcept {
        return 合同版本 == 本能被动维护游标合同版本_v2
            && (状态 == 本能被动维护游标建立状态_v2::已建立
                || 状态 == 本能被动维护游标建立状态_v2::已恢复
                || 状态 == 本能被动维护游标建立状态_v2::已迁移
                || 状态 == 本能被动维护游标建立状态_v2::已建立新纪元
                || 状态 == 本能被动维护游标建立状态_v2::精确重复)
            && L2结构幂等身份有效(幂等身份)
            && 提交游标 && 本能被动维护游标事实完整_v2(*提交游标)
            && 首次提交事实代次 != 0 && 本次正式读回截止 != 0
            && 首次提交事实代次 <= 本次正式读回截止;
    }

    friend bool operator==(const 本能被动维护游标建立结果_v2&,
        const 本能被动维护游标建立结果_v2&) = default;
};

struct 本能被动维护游标读取请求_v2 final {
    std::uint32_t 合同版本 = 本能被动维护游标合同版本_v2;
    L2结构请求头 请求头{};
    L2存在身份 自我{};
    friend bool operator==(const 本能被动维护游标读取请求_v2&,
        const 本能被动维护游标读取请求_v2&) = default;
};

enum class 本能被动维护游标读取状态_v2 : std::uint8_t {
    已读取 = 1,
    未建立 = 2,
    待迁移 = 3,
    入口拒绝 = 4,
    当前性漂移 = 5,
    引用冲突 = 6,
    资源失败 = 7,
    内部错误 = 8
};

struct 本能被动维护游标读取结果_v2 final {
    std::uint32_t 合同版本 = 本能被动维护游标合同版本_v2;
    本能被动维护游标读取状态_v2 状态 =
        本能被动维护游标读取状态_v2::入口拒绝;
    std::optional<本能被动维护游标事实_v2> 游标{};
    std::uint64_t 本次正式读回截止 = 0;

    bool 成功() const noexcept {
        return 合同版本 == 本能被动维护游标合同版本_v2
            && 状态 == 本能被动维护游标读取状态_v2::已读取
            && 游标 && 本能被动维护游标事实完整_v2(*游标)
            && 本次正式读回截止 != 0
            && 游标->形成事实代次 <= 本次正式读回截止;
    }

    friend bool operator==(const 本能被动维护游标读取结果_v2&,
        const 本能被动维护游标读取结果_v2&) = default;
};

struct 本能被动维护游标事实_v3 final {
    L2存在身份 自我{};
    std::uint64_t 运行代次 = 0;
    std::uint64_t 时间纪元身份 = 0;
    std::int64_t 上一已维护完整秒边界 = 0;
    std::uint64_t 已消费主动安全变化事实代次 = 0;
    std::uint64_t 已消费服务维护历史事实代次 = 0;
    std::uint32_t 时间源版本 = 0;
    std::uint32_t 服务时间维护规则版本 = 0;
    std::uint64_t 形成事实代次 = 0;
    friend bool operator==(const 本能被动维护游标事实_v3&,
        const 本能被动维护游标事实_v3&) = default;
};

inline bool 本能被动维护游标事实完整_v3(
    const 本能被动维护游标事实_v3& 事实) noexcept {
    return 有效(事实.自我.值) && 事实.运行代次 != 0
        && 事实.时间纪元身份 != 0
        && 事实.上一已维护完整秒边界 >= 0
        && 事实.已消费主动安全变化事实代次 != 0
        && 事实.已消费服务维护历史事实代次 != 0
        && 事实.时间源版本 != 0
        && 事实.服务时间维护规则版本 != 0
        && 事实.形成事实代次 != 0;
}

struct 建立或恢复本能被动维护游标请求_v3 final {
    std::uint32_t 合同版本 = 本能被动维护游标合同版本_v3;
    L2结构请求头 请求头{};
    L2结构幂等身份 幂等身份{};
    本能根运行锚点_v1 本能根锚点;
    std::uint64_t 运行代次 = 0;
    std::uint64_t 时间纪元身份 = 0;
    std::int64_t 当前完整秒边界 = 0;
    std::uint64_t 目标已消费主动安全变化事实代次 = 0;
    L2特征当前值变化账登记结果_v2 变化账登记见证{};
    std::uint64_t 目标已消费服务维护历史事实代次 = 0;
    服务维护历史覆盖起点读取结果_v1 服务历史覆盖起点见证{};
    std::uint32_t 时间源版本 = 0;
    std::uint32_t 服务时间维护规则版本 = 服务时间维护规则版本_v1;
    friend bool operator==(const 建立或恢复本能被动维护游标请求_v3&,
        const 建立或恢复本能被动维护游标请求_v3&) = default;
};

enum class 本能被动维护游标建立状态_v3 : std::uint8_t {
    已建立 = 1,
    已恢复 = 2,
    已迁移 = 3,
    已建立新纪元 = 4,
    精确重复 = 5,
    入口拒绝 = 6,
    当前性漂移 = 7,
    幂等冲突 = 8,
    引用冲突 = 9,
    资源失败 = 10,
    内部错误 = 11,
    已可能发布 = 12
};

struct 本能被动维护游标建立结果_v3 final {
    std::uint32_t 合同版本 = 本能被动维护游标合同版本_v3;
    本能被动维护游标建立状态_v3 状态 =
        本能被动维护游标建立状态_v3::入口拒绝;
    L2结构幂等身份 幂等身份{};
    std::optional<本能被动维护游标事实_v3> 提交游标{};
    std::uint64_t 首次提交事实代次 = 0;
    std::uint64_t 本次正式读回截止 = 0;
    bool 成功() const noexcept {
        return 合同版本 == 本能被动维护游标合同版本_v3
            && (状态 == 本能被动维护游标建立状态_v3::已建立
                || 状态 == 本能被动维护游标建立状态_v3::已恢复
                || 状态 == 本能被动维护游标建立状态_v3::已迁移
                || 状态 == 本能被动维护游标建立状态_v3::已建立新纪元
                || 状态 == 本能被动维护游标建立状态_v3::精确重复)
            && L2结构幂等身份有效(幂等身份)
            && 提交游标 && 本能被动维护游标事实完整_v3(*提交游标)
            && 首次提交事实代次 != 0 && 本次正式读回截止 != 0
            && 首次提交事实代次 <= 本次正式读回截止;
    }
    friend bool operator==(const 本能被动维护游标建立结果_v3&,
        const 本能被动维护游标建立结果_v3&) = default;
};

struct 本能被动维护游标读取请求_v3 final {
    std::uint32_t 合同版本 = 本能被动维护游标合同版本_v3;
    L2结构请求头 请求头{};
    L2存在身份 自我{};
    friend bool operator==(const 本能被动维护游标读取请求_v3&,
        const 本能被动维护游标读取请求_v3&) = default;
};

enum class 本能被动维护游标读取状态_v3 : std::uint8_t {
    已读取 = 1,
    未建立 = 2,
    待迁移 = 3,
    入口拒绝 = 4,
    当前性漂移 = 5,
    引用冲突 = 6,
    资源失败 = 7,
    内部错误 = 8
};

struct 本能被动维护游标读取结果_v3 final {
    std::uint32_t 合同版本 = 本能被动维护游标合同版本_v3;
    本能被动维护游标读取状态_v3 状态 =
        本能被动维护游标读取状态_v3::入口拒绝;
    std::optional<本能被动维护游标事实_v3> 游标{};
    std::uint64_t 本次正式读回截止 = 0;
    bool 成功() const noexcept {
        return 合同版本 == 本能被动维护游标合同版本_v3
            && 状态 == 本能被动维护游标读取状态_v3::已读取
            && 游标 && 本能被动维护游标事实完整_v3(*游标)
            && 本次正式读回截止 != 0
            && 游标->形成事实代次 <= 本次正式读回截止;
    }
    friend bool operator==(const 本能被动维护游标读取结果_v3&,
        const 本能被动维护游标读取结果_v3&) = default;
};

struct 本能被动维护游标事实_v4 final {
    L2存在身份 自我{};
    std::uint64_t 运行代次 = 0;
    std::uint64_t 时间纪元身份 = 0;
    std::uint64_t 维护纪元身份 = 0;
    std::int64_t 上一已维护完整秒边界 = 0;
    std::uint64_t 已消费主动安全变化事实代次 = 0;
    std::uint64_t 已消费服务维护历史事实代次 = 0;
    std::uint64_t 服务历史v2覆盖种子事实代次 = 0;
    std::uint64_t 成功维护提交序号 = 0;
    std::uint32_t 时间源版本 = 0;
    std::uint32_t 服务时间维护规则版本 = 0;
    std::uint64_t 形成事实代次 = 0;
    friend bool operator==(const 本能被动维护游标事实_v4&,
        const 本能被动维护游标事实_v4&) = default;
};

inline bool 本能被动维护游标事实完整_v4(
    const 本能被动维护游标事实_v4& 事实) noexcept {
    return 有效(事实.自我.值) && 事实.运行代次 != 0
        && 事实.时间纪元身份 != 0 && 事实.维护纪元身份 != 0
        && 事实.上一已维护完整秒边界 >= 0
        && 事实.已消费主动安全变化事实代次 != 0
        && 事实.已消费服务维护历史事实代次 != 0
        && 事实.服务历史v2覆盖种子事实代次 != 0
        && 事实.已消费服务维护历史事实代次
            >= 事实.服务历史v2覆盖种子事实代次
        && 事实.时间源版本 != 0
        && 事实.服务时间维护规则版本 != 0
        && 事实.形成事实代次 != 0;
}

struct 显式激活或恢复本能被动维护纪元请求_v1 final {
    std::uint32_t 合同版本 = 本能被动维护纪元激活合同版本_v1;
    L2结构请求头 请求头{};
    L2结构幂等身份 幂等身份{};
    本能根运行锚点_v1 本能根锚点;
    std::uint64_t 运行代次 = 0;
    std::uint64_t 时间纪元身份 = 0;
    std::int64_t 当前完整秒边界 = 0;
    L2特征当前值变化账登记结果_v2 主动安全变化账登记见证{};
    服务维护历史边界读取结果_v2 服务历史v2边界见证{};
    std::uint32_t 时间源版本 = 0;
    std::uint32_t 服务时间维护规则版本 = 0;
    friend bool operator==(const 显式激活或恢复本能被动维护纪元请求_v1&,
        const 显式激活或恢复本能被动维护纪元请求_v1&) = default;
};

enum class 本能被动维护纪元激活状态_v1 : std::uint8_t {
    已激活 = 1,
    已恢复 = 2,
    精确重复 = 3,
    入口拒绝 = 4,
    当前性漂移 = 5,
    幂等冲突 = 6,
    引用冲突 = 7,
    旧格式成功维护历史不可证明 = 8,
    已有成功维护需完整迁移 = 9,
    覆盖种子漂移 = 10,
    材料不足 = 11,
    资源失败 = 12,
    内部错误 = 13,
    已可能发布 = 14
};

struct 本能被动维护纪元激活见证_v1 final {
    std::uint64_t 维护纪元身份 = 0;
    std::uint64_t Gseed = 0;
    std::uint64_t Gseed前服务历史不补算结束边界 = 0;
    std::int64_t 初始化完整秒边界 = 0;
    friend bool operator==(const 本能被动维护纪元激活见证_v1&,
        const 本能被动维护纪元激活见证_v1&) = default;
};

struct 本能被动维护纪元激活结果_v1 final {
    std::uint32_t 合同版本 = 本能被动维护纪元激活合同版本_v1;
    本能被动维护纪元激活状态_v1 状态 =
        本能被动维护纪元激活状态_v1::入口拒绝;
    L2结构幂等身份 幂等身份{};
    std::optional<本能被动维护游标事实_v4> 提交游标{};
    std::optional<本能被动维护纪元激活见证_v1> 激活见证{};
    std::uint64_t 首次提交事实代次 = 0;
    std::uint64_t 本次正式读回截止 = 0;

    bool 成功() const noexcept {
        const bool 成功状态 = 状态 == 本能被动维护纪元激活状态_v1::已激活
            || 状态 == 本能被动维护纪元激活状态_v1::已恢复
            || 状态 == 本能被动维护纪元激活状态_v1::精确重复;
        return 合同版本 == 本能被动维护纪元激活合同版本_v1
            && 成功状态 && L2结构幂等身份有效(幂等身份)
            && 提交游标 && 本能被动维护游标事实完整_v4(*提交游标)
            && 激活见证
            && 激活见证->维护纪元身份 == 提交游标->维护纪元身份
            && 激活见证->Gseed
                == 提交游标->服务历史v2覆盖种子事实代次
            && 激活见证->Gseed前服务历史不补算结束边界
                == 激活见证->Gseed
            && 激活见证->初始化完整秒边界
                == 提交游标->上一已维护完整秒边界
            && 首次提交事实代次 != 0 && 本次正式读回截止 != 0
            && 首次提交事实代次 <= 本次正式读回截止;
    }

    friend bool operator==(const 本能被动维护纪元激活结果_v1&,
        const 本能被动维护纪元激活结果_v1&) = default;
};

struct 本能被动维护游标读取请求_v4 final {
    std::uint32_t 合同版本 = 本能被动维护游标合同版本_v4;
    L2结构请求头 请求头{};
    L2存在身份 自我{};
    friend bool operator==(const 本能被动维护游标读取请求_v4&,
        const 本能被动维护游标读取请求_v4&) = default;
};

enum class 本能被动维护游标读取状态_v4 : std::uint8_t {
    已读取 = 1,
    未建立 = 2,
    旧格式待裁决 = 3,
    入口拒绝 = 4,
    当前性漂移 = 5,
    引用冲突 = 6,
    版本漂移 = 7,
    资源失败 = 8,
    内部错误 = 9
};

struct 本能被动维护游标读取结果_v4 final {
    std::uint32_t 合同版本 = 本能被动维护游标合同版本_v4;
    本能被动维护游标读取状态_v4 状态 =
        本能被动维护游标读取状态_v4::入口拒绝;
    std::optional<本能被动维护游标事实_v4> 游标{};
    std::uint64_t 本次正式读回截止 = 0;

    bool 成功() const noexcept {
        return 合同版本 == 本能被动维护游标合同版本_v4
            && 状态 == 本能被动维护游标读取状态_v4::已读取
            && 游标 && 本能被动维护游标事实完整_v4(*游标)
            && 本次正式读回截止 != 0
            && 游标->形成事实代次 <= 本次正式读回截止;
    }

    friend bool operator==(const 本能被动维护游标读取结果_v4&,
        const 本能被动维护游标读取结果_v4&) = default;
};

} // namespace 海中鱼巣
