# INSTINCT-ROOT-RC-PRODUCTION-INIT 安全服务双根RC生产初始化验证记录

日期：2026-09-24

计划：`INSTINCT-ROOT-RC-PRODUCTION-INIT` v0.7

验证源码基线：施工起点 `7ed7b884949d3035d827fc478c296358e0793e05` 加本计划白名单内最终本地差异

代码结果提交：本记录所在提交

隔离验证根：`D:\TEMP\HY-Ego\INSTINCT-ROOT-RC-PRODUCTION-INIT`

## 1. 范围与静态检查

- `main == origin/main == 7ed7b884949d3035d827fc478c296358e0793e05`，index 为空；三份异主流程图不属于本验证输入。
- 新 provider 只依赖同一上下文中的特征服务与概念服务；没有新增 L1 直读、第二 owner、恢复账、兼容入口或固定节点编码。
- pure 与 RC 固定键、K 安全/服务固定键均保持不变；恢复和重放只使用 owner 返回的首次材料或完整原请求。
- 发布未知静态防御复核通过：pure、K、RC 都只在正式成功收敛或完整同义读回后解除未知；任一未决路径返回`已可能发布`，清空安全根、服务根及 `Gread`，普通应用不进入后续装配。
- 核心静态证据位置：provider pure 收敛约第236—261行、K收敛约第294—330行、RC收敛约第358—398行、共同读回约第517—581行、失败清空约第582—601行；普通应用失败停装配约第509—519行。
- 发布未知运行项：**未执行：现行公开 ABI 不可确定触达**。现行 final L1/owner/特征/概念服务没有提交后回执未知故障注入或可替换窄端口；本计划未新增测试开关，也未把该项写成 PASS。

## 2. RC 初始化核心专项

工程：`验证工具/本能双根二次关系概念初始化专项验证.vcxproj`

Debug、Release 均使用配置隔离 `OutDir` / `IntDir` 执行 `Rebuild`，随后无参数运行。

| 配置 | 构建 | 无参数运行 |
| --- | --- | --- |
| Debug x64 | 成功，0警告、0错误 | `37 PASS / 0 FAIL`，退出码0 |
| Release x64 | 成功，0警告、0错误 | `37 PASS / 0 FAIL`，退出码0 |

覆盖结果：

1. 首次形成通用 EC、双 K、双 `D_POS` RC，最终共同截止完整；
2. 同进程重复零写恢复，EC/K/RC 身份稳定；
3. RC 发布后，`精确查询纯概念`与`读取当前I64特征概念`均跳过合法 RC；缺见证、错角色、错目标三类损坏成员在两个入口共六项均返回拒绝；
4. K 无预算建立/原请求重放保持零读取用量；当前路由与身份读取使用具名预算；
5. 预建 EC、单根 K、单根 RC 均可收敛；固定键错误 RC 返回幂等冲突；
6. RC 活跃、冷却、退役读取状态分别准确；冷却继续交付，退役结构化失败并清空双根；
7. pure、K、RC 三组不足预算结构化失败；代次漂移与异义当前 K 均结构化失败且无伪成功。

## 3. 跨进程恢复

Debug 与 Release 分别使用全新且互不共享的持久根；每个配置先以新进程执行 `ordinary-seed`，再以另一个新进程执行 `ordinary-recover`。

| 配置 | seed | recover |
| --- | --- | --- |
| Debug x64 | `4 PASS / 0 FAIL`，退出码0 | `4 PASS / 0 FAIL`，退出码0 |
| Release x64 | `4 PASS / 0 FAIL`，退出码0 | `4 PASS / 0 FAIL`，退出码0 |

恢复证明普通应用交付状态为`已恢复`，通用 EC、双 K、双 RC 身份与首次进程一致，SELF-FORM 继续复用同一通用 EC。

## 4. 生产消费专项

工程：`验证工具/本能根运行生产消费专项验证.vcxproj`

在最终 provider 源码上，Debug/Release 均 `Rebuild` 成功、0警告、0错误。每配置使用全新绝对根，严格按 `config -> stage-first -> stage-recover -> ordinary -> headless` 串行运行；只有同配置的两个 stage 模式共享阶段根。

| 配置 | config | stage-first | stage-recover | ordinary | headless | 合计 |
| --- | --- | --- | --- | --- | --- | --- |
| Debug x64 | 8 PASS | 10 PASS | 11 PASS | 2 PASS | 2 PASS | `33/33 PASS` |
| Release x64 | 8 PASS | 10 PASS | 11 PASS | 2 PASS | 2 PASS | `33/33 PASS` |

全部运行退出码为0、FAIL为0。阶段21首次与持久恢复锚点、阶段20唯一 self 停在关闭治理门、活动租约拒绝、安全停止回收、控制面板阶段12和无窗口停止信号后的成功回收断言均保持。

## 5. 世界树根启动专项

工程：`验证工具/世界树根启动专项验证.vcxproj`

| 配置 | 构建 | 无参数运行 |
| --- | --- | --- |
| Debug x64 | 成功，0警告、0错误 | `205/205 PASS`，退出码0 |
| Release x64 | 成功，0警告、0错误 | `205/205 PASS`，退出码0 |

原验证入口和断言均继续执行；`existence-instance-feature-layout` 使用现行版本2与键 `0x4946525354525632` 并通过。生产端没有恢复 v1 接受路径。

## 6. 根工程与 Git 检查

工程：`海中鱼巣.vcxproj`

| 配置 | 结果 |
| --- | --- |
| Debug x64 | Rebuild 成功，0警告、0错误 |
| Release x64 | Rebuild 成功，0警告、0错误 |

根工程构建证明新头源、工程登记和生产调用链接闭合，不证明完整普通程序运行或业务闭环。

发布前执行 `git diff --check`、精确暂存后执行 `git diff --cached --check`；结果随本提交发布检查确认。三份异主流程图保持未暂存。

## 7. 结论边界

本验证支持“双根 RC 生产初始化、稳定定位、共同截止读回及普通应用交付完成”的代码层结论。它不支持 RC 求值、目标常量来源、G4-A、sink、治理门、消息循环、任务线程、完整自我治理闭环或具名集成验收完成声明。
