# 实际存在登记当前读回一致投影构建与生产消费验证

日期：2026-08-08

计划：`L2-EXISTENCE-REGISTRY-CURRENT-READ-CONSISTENT-PROJECTION-MIGRATION v0.1`

plan blob：`fa1c0f2abe918d0b9c6ba464072f05f1cfcca13a`

设计提交：`02b1b6b504f2490de3c5f6359e15981cc21248dc`

施工起点：`0dfd72d012d9a2cca4d753b4dd8bd76781fb97b5`

## 1. 构建、模块与生产入口

| 门禁 | 结果 |
| --- | --- |
| Debug x64 串行完整 Rebuild | 通过，退出 0；0 编译错误、1 条 `MSB8029` 临时输出目录提示 |
| Release x64 串行完整 Rebuild | 通过，退出 0；0 编译错误、1 条 `MSB8029` 临时输出目录提示 |
| Debug MSVC 模块扫描 | 通过，sources / modules=`136 / 136`、failed=`0`、拓扑 `136`、循环 `0`、外部需求 `0` |
| Release MSVC 模块扫描 | 通过，sources / modules=`136 / 136`、failed=`0`、拓扑 `136`、循环 `0`、外部需求 `0` |
| Debug 正式 `--runtime-context` | 30 秒时间盒内自行退出 0；stdout / stderr 均为 0 字节 |
| Release 正式 `--runtime-context` | 30 秒时间盒内自行退出 0；stdout / stderr 均为 0 字节 |
| `python .\tools\check_specs.py --strict` | 通过，110 / 110 |
| 三个白名单路径 `git diff --check` | 通过，无 whitespace 错误；仅 Git 行尾策略提示 |

构建使用 Visual Studio 18 Professional，Debug / Release、x64、完整 `Rebuild`、`/m:1`；系统临时验证根为 `C:\Users\zhchh\AppData\Local\Temp\hzy-existence-registry-consistent-2a84b9170f4e4a13a63bd97b9f307512`。模块扫描只写该临时目录，没有写正式图谱输出。

## 2. 精确静态门禁

| 检查 | 结果 |
| --- | --- |
| 目标根一致投影调用 | `尝试读取中性一致当前投影` 恰 1 个物理调用点；逻辑调用最多 2 次 |
| 有界控制结构 | `最大尝试次数 = 2` 恰一；目标根只有一个 `for`、零 `while`、零 sleep；唯一 `continue` 只位于首轮合法漂移分支 |
| 旧读取与快照 | `读取中性当前事实代次`、`读取中性当前节点`、`读取中性当前属性`、`读取中性当前值`、`读取中性当前关系`、`读取完整快照`、`尝试读取完整快照` 在目标根内均为 0 |
| 请求形状 | 节点 / 关系 / 值 / 属性值 / 源关系组 / 目标关系组精确为 `2/0/0/0/0/0`；节点顺序为服务身份、资格属性类型；首轮期望取定位截止 |
| 漂移收敛 | 首轮只有合法失败形状才按回显非零不同代次完整重试一次；第二轮漂移返回版本漂移；不存在第三次调用 |
| 许可与其它顶层失败 | 许可拒绝零等待零重试；资源失败保持分账；入口拒绝、内部不一致和坏失败形状返回内部不一致 |
| 项目非成功 | 未找到返回未找到；已退出、种类不匹配、属性未设置、成功缺事实 / 非成功带事实返回内部不一致；内部类错误优先 |
| 成功顶层形状 | 合同、期望回显、共同读取代次和六组长度全部精确核对 |
| 两节点业务形状 | 查询 / 事实编码、普通服务节点、I64 属性类型节点、空槽、未退出、共同创建代次和可选期望创建代次全部核对 |
| 返回与缓存 | 成功只更新返回登记副本的事实截止；全部非成功返回空登记；目标根不修改定位缓存 |
| 公开合同 | 实际存在数据头零差异；合同版本、规则版本、十个状态数值、公开 DTO 和函数签名零变化 |
| 同文件其它函数 | 差异只包含目标专用 helper 与 `按登记读取权威事实`；登记写面、资格读取、规格和其它函数相对起始 blob 行为零变化 |
| 白名单外生产文件 | 差异为 0；核心 L1、世界登记、场景、原子发布、组合、启动、运行期上下文、工程、filters 和自检均未修改 |

## 3. 正式生产消费与未独立读回

Debug / Release `--runtime-context` 均使用本轮新构建程序，并在 30 秒时间盒内自行退出 0。若发生超时，验证命令只会终止本段刚启动的精确 PID；本次没有触发超时终止。运行前存在的异主世界登记验收生产进程未被终止、修改或计入结论。

两次运行经现有正式链路触达实际存在登记建立 / 读回及运行期上下文完整性门禁。进程退出 0 只说明正式生产入口没有拒绝该次启动；本记录未读取日志来补写机器事实，也未把空 stdout / stderr 当成业务成功。

公开租约连续两次结构化登记 / 场景 / 世界读回未在本段独立执行。

因此本记录不独立证明两次实际存在登记、初始场景和世界登记均取得结构化已读取、逐字段相等或共同截止，也不形成实际存在登记消费者集成验收结论。计划冻结的首次成功、一次漂移后成功、第二轮漂移、许可竞争、未找到和坏业务形状分支由源码结构化分账；本段未用私有探针、专项驱动或人为竞争逐项触发。

## 4. 范围与声明边界

实际结果范围只含实际存在服务文件和本施工 / 验证记录。P0-FIX、路线、异主 `.codex-build`、两份 WORLD-TREE 及其它 WIP 未纳入修改、暂存或结论。

允许声明：实际存在登记当前权威读回候选已按冻结合同迁移到中性非阻塞一致当前投影，并通过双配置构建、模块扫描、strict、静态门禁和正式生产入口运行门禁。

禁止声明：公开租约独立结构化读回已通过；真实许可竞争或二次漂移、消费者集成验收、其它消费者迁移、值 / 属性 / 关系 / 关系组 / 修改 / 退出 / 历史、资格读取、L1 自证或第一层完善已经得到证明。
