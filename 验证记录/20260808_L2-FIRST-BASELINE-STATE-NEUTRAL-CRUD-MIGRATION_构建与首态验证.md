# L2-FIRST-BASELINE-STATE-NEUTRAL-CRUD-MIGRATION 构建与首态验证

日期：2026-08-08

## 1. 验证结论

计划要求的 Debug / Release 全量重建、双配置模块依赖扫描、正式规范目录 strict 和静态门禁均通过。验证输出使用本轮唯一系统临时目录：

`C:\Users\zhchh\AppData\Local\Temp\海中鱼巣-L2-FIRST-f915542e2ecb40e68dff511033c20a33`

没有使用、删除、覆盖或枚举异主 `.codex-build`，没有写正式图谱输出。

## 2. 构建与模块扫描

| 项目 | 结果 |
| --- | --- |
| Debug x64 `/t:Rebuild /m:1` | 通过，exit 0，生成临时目录 `Debug/bin/海中鱼巣.exe` |
| Release x64 `/t:Rebuild /m:1` | 通过，exit 0，生成临时目录 `Release/bin/海中鱼巣.exe` |
| Debug 模块依赖扫描 | `sources=136, modules=136, failed=0`；拓扑 136，循环 0，外部需求 0 |
| Release 模块依赖扫描 | `sources=136, modules=136, failed=0`；拓扑 136，循环 0，外部需求 0 |
| `python .\tools\check_specs.py --strict` | 110 份正式规范目录项全部通过 |

两次 Rebuild 均出现 MSBuild `MSB8029`：中间目录或输出目录位于系统临时目录可能影响增量生成。本计划明确要求使用系统临时目录并执行完整 Rebuild；两配置均 exit 0，该环境警告不影响本次完整构建结论。

## 3. 静态门禁

- `git diff --check`：目标代码通过；形成两份记录后再次通过四路径检查。
- 生产代码中 `L1首个基准状态幂等域` 只有一处定义和一处消费；`L1状态登记幂等域` 仍只有原定义和原消费。
- 全生产代码 `形成G7首个基准状态发布意图凭证`、`形成G7凭证`、`0x15000602U` 均为 0。
- `发布实际存在I64首个基准状态` 内：`探测幂等=0`、legacy `L1写集请求=0`、`提交写集(=0`、`读取完整快照=0`、`L1领域意图凭证=0`、`提交中性写集=1`。
- 固定中性写集为 `2/5/5/5/0`，本地键 1—12、关系角色 1—5、五槽、材料和来源均按详细设计落位。
- 重复路径从 `首态账_` 复制完整中性写集和读取请求，跳过首次路径的 P8、P7、当前值及目标 / 源关系查询；同键异请求在提交前返回幂等冲突。
- 私有具名读回中 legacy 完整快照及 legacy 当前节点 / 关系 / 值 / 历史读取均为 0；G0/G1 两次读取当前事实代次，并通过中性 helper 精确读取当前或历史事实。
- `建立登记`、`读取当前登记`、相邻前状态选择、后继状态规格形成相对 `ae747ddd3d6b0cccb43f2df3e6ece414dc8dc015` 逐字一致；Git diff 没有其它排除函数 hunk。

## 4. 分支证据与未运行范围

源码可审计覆盖：首次成功、新键旧代次漂移、既有完整首态拒绝、相关关系形状异常、同键异请求冲突、当前代次推进后的原写集精确重复、发布后当前 / 历史精确读回及 G0/G1 漂移重试。

仓库没有现成合法消费者可确定触发上述全部分支；本次没有运行真实消费者，没有新增 L1 专项驱动、测试 seam、伪 provider 或坏结果工厂。因此本记录不把构建与静态证据升级为运行期业务闭环、生产接线或集成验收通过。
