# L1-SIMPLIFY-P13-STATE-SPEC v0.2 验证记录

## 1. 验证对象

- plan blob：`25c36025a7b0da323345a8f7a29b2cc8e6084c89`
- 验证基线：`d3356f78e1c4fb81771e0c047b134866c5fc2734` 加 P13 八文件工作区差异
- 目标：SELF-GOV-CLOSURE STEP-3F 的关系19后继状态纯值规格源码提供者

## 2. 构建、运行与规范门禁

| 门禁 | 实际结果 |
| --- | --- |
| Debug x64 串行 Rebuild | PASS |
| Debug `--self-test-exit` | PASS，退出码0 |
| Release x64 串行 Rebuild | PASS |
| Release `--self-test-exit` | PASS，退出码0 |
| Debug MSVC 模块扫描 | PASS，sources/modules=198/198，failed=0，拓扑198，循环0，外部需求0 |
| Release MSVC 模块扫描 | PASS，sources/modules=198/198，failed=0，拓扑198，循环0，外部需求0 |
| `python .\tools\check_specs.py --strict` | PASS，110/110 |
| P13 目标 tracked/untracked `git diff --check` | PASS |

双配置模块扫描使用两个唯一系统临时输出路径串行运行，没有形成仓库产物或共享输出竞争。

## 3. 专项矩阵

既有顺序360保持唯一顶层专项。P12 的14项矩阵保持原位，P13 新增17项，L1状态报告共63项并随 Debug/Release 完整自检通过。P13 新增矩阵实际覆盖：

1. 经正式公开入口建立 P7 实际存在、P8 实例特征、P9 首态、P10 当前值换代、P11 迁移比较合同及 P12 相邻前状态；
2. 尚无首态时返回无前状态，规格与迁移证据为空；
3. 首态 `-1`、当前值 `18` 形成增加、排序 `-1`、差异 `19` 和允许动态的规格；
4. 当前值再换代为 `-1` 形成等价、差异0并禁止动态；再换代为 `-5` 形成减少和允许动态；
5. 成功规格精确为二节点、五值、五关系19、五属性槽、零退出，12个本地键连续且唯一；
6. 重复同义请求得到相同确定性摘要和相同写集，形成前后 L1 事实代次不变；
7. 非法业务请求、旧当前值、未知当前值、未登记比较服务均结构化非成功且规格与迁移证据为空；
8. 成功证据中的 P12 前状态、P11 左右输入、主体、场景、实例槽和当前值均与实际读回一致；状态服务空材料直接拒绝。

P12 既有矩阵继续覆盖当前候选、合法无前态、从未存在、后来退出无当前关系、退出关系拒绝、历史互证、冲突、版本漂移及非成功零载荷；P13 只消费其正式结果，不另造 P9 读取。

## 4. 静态合同与范围

- 组合根函数中的 `提交写集` 调用为0；关系20、动态发布、SQL、恢复、旧句柄和缓存命中为0。
- L1 状态服务导入比较服务为0；任何 provider 反向导入 P13 组合模块为0。
- 新规格形状固定为节点2、值5、关系19五条、属性槽5、退出0；本地键固定 `0x53000001..0x5300000C`。
- 启动文件差异为0；P13 临时诊断命中为0；工程只增加一个组合模块登记。
- P13 精确八文件与 P39、P15、P0-FIX、路线输入及旧 WORLD-TREE WIP 零路径重叠，Git index 保持为空。

## 5. 最大证明边界

本验证只证明 P13 源码候选、顺序360具名矩阵、双配置本地构建/自检、规范检查、模块依赖和静态范围门禁通过。不证明第二状态或关系20已经发布，不证明迁移动能、动作动态、最终原子事务、撤销、生产装配、持久恢复、旧消费者迁移、具名集成验收、STEP-3F 整体或治理闭环。
