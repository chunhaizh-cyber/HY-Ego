# L2 存在固定属性、退出、完整读取与场景成员反向查询验证记录

日期：2026-08-11

计划身份：`L2-STRUCTURE-CRUD-REBUILD-E2` v0.1

验证基线：施工起点 `3a26541a88ddca066a122d3c2cdd8970e0575797` 加本计划十文件未提交 WIP

## 1. 主工程双配置重建

执行：

```powershell
MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /verbosity:minimal
MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Release /p:Platform=x64 /verbosity:minimal
```

结果：清理中文命名并补强反向结果全组唯一性后，Debug 与 Release x64 均完整扫描模块、编译并链接成功。该结果只证明生产工程在两项具名配置下可编译链接，不证明 E2 已生产装配、被业务消费者调用或通过独立集成验收。

## 2. 生产外 18 项参数矩阵

执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\验证工具\运行L2存在固定属性退出与场景引用参数验证.ps1 `
  -配置 Debug -幂等基值 13000 -初始I64 11 -换代I64 22 -成员顺序一 10 -成员顺序二 20
powershell -ExecutionPolicy Bypass -File .\验证工具\运行L2存在固定属性退出与场景引用参数验证.ps1 `
  -配置 Release -幂等基值 17000 -初始I64 33 -换代I64 44 -成员顺序一 30 -成员顺序二 40
```

Debug 与 Release 的 1—18 项均输出 PASS，覆盖结果如下：

| 范围 | 已验证事实 |
| --- | --- |
| 固定属性新增 / 换代 | I64、U64 组、独立材料三类当前完整投影；同代次建值设槽；旧值退出；首次载荷精确重复；同键异义、错旧值和未设置状态结构化拒绝 |
| 完整当前 / 历史读取 | 当前逐字段与稳定排序；兼容入口逐字段收敛；换代前后历史截止独立重建；退出前完整、退出截止和当前已退出空载荷 |
| 场景成员反向查询 | 合法空成功；不存在 / 已退出目标不误报空成功；两场景引用逐字段完整并按顺序、场景、关系排序；带属性存在仍可作为成员端点 |
| 退出与引用闭包 | 场景成员、值来源和独立材料外部引用分别由 L1 保护；外部引用闭合后，存在及全部当前属性恰在一个新代次退出并历史互证 |
| 入口、幂等与漂移 | 漏项 / 多项引用冲突、未升序 / 重复闭包、合同与零字段、空材料均零提交拒绝；新幂等旧期望漂移；原成功请求精确重复；同键异义冲突 |
| 有界并发 | `std::barrier` 同起步、固定最大轮数且无 sleep；只接受同代次完整成功、许可拒绝或事实代次漂移空载荷，不接受混代或部分结构 |

最终并发计数：

- Debug：`已读取=0`、`事实代次漂移=0`、`许可拒绝=32`。
- Release：`已读取=3`、`事实代次漂移=0`、`许可拒绝=29`。

计数属于允许集合内的实际调度结果，不是固定期望值。两项配置均输出：`[BOUNDARY] 当前两次投影或历史前后夹取内部窗口未证明。` 因而本记录不宣称覆盖该内部窗口。

两次专项构建均出现 MSB8029：MSBuild 不建议把中间 / 输出目录置于系统临时目录。这是计划要求的工作区外隔离路径提示；实际 Rebuild、链接和程序运行成功，且仓库内未生成专项构建物。

## 3. 静态、规范与工程门禁

- `python .\tools\check_specs.py --strict`：`111 份目录项全部通过`。
- 五个生产文件与三个专项文件通过严格 UTF-8 解码；主工程和专项工程 XML 均可解析。
- 主工程登记为 26 个生产 `.ixx`、12 个 `ClInclude`、四配置 4 项 `/utf-8`；专项项目为八个模块、一个验证 `.cpp`、两配置 2 项 `/utf-8`。
- 专项项目在主工程和 filters 中均为零登记。
- 五个公开函数均存在，十个新 DTO 各唯一；两个共同属性辅助各唯一，本地键字面量唯一，L1 `引用冲突` 显式映射存在。
- 存在服务对场景服务零 import / 调用；新增生产代码未命中第二仓、幂等账、缓存、SQL、日志、sleep、yield 或测试钩子。
- 本轮新增的纯 `L1` 参数 / 变量已改为 `第一层服务`；没有机械修改既有非本轮代码。

## 4. 未验证范围与结论边界

未运行 Win32、真实分配失败、私有索引破坏、长时公平竞争、故障注入、崩溃 / 恢复、跨进程、生产入口、生产装配、业务资格或独立集成验收。没有证明实际命中当前两次投影之间或历史前后夹取之间的内部窗口。

结论：E2 的存在固定属性新增 / 换代、显式闭包退出、完整当前 / 历史读取、场景成员反向查询和场景存在端点适配，在 Debug / Release x64 主工程重建及具名 18 项生产外参数矩阵下通过。Git 提交和推送事实仍待顶层唯一发布者完成。
