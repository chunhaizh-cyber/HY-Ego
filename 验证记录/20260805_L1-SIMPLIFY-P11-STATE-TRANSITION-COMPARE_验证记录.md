# L1-SIMPLIFY-P11-STATE-TRANSITION-COMPARE v0.1 验证记录

## 1. 验证对象

- plan blob：`8fdba8c3ae490128abb458e80f987878db404b73`
- 验证基线：`36e95b33c36cea4af524bcc23b561a98a62589d1`加P11五文件工作区差异
- 目标：SELF-GOV-CLOSURE v0.2 STEP-3E源码提供者

## 2. 动态与规范门禁

| 门禁 | 实际结果 |
| --- | --- |
| Debug x64 串行Rebuild | PASS |
| Debug `--self-test-exit` | PASS，退出码0 |
| Release x64 串行Rebuild | PASS |
| Release `--self-test-exit` | PASS，退出码0 |
| Debug MSVC模块扫描 | PASS，sources/modules=197/197，failed=0，循环0，外部需求0 |
| Release MSVC模块扫描 | PASS，sources/modules=197/197，failed=0，循环0，外部需求0 |
| `python .\tools\check_specs.py --strict` | PASS，110/110 |
| P11目标`git diff --check` | PASS |

模块扫描候选输出仅写入系统临时目录，没有形成仓库产物。P32发布后重新运行双配置自检和双配置模块扫描；P32只含文档及索引，不改变此前双配置构建产物对应的P11代码差异。

## 3. 专项矩阵

顺序340保持唯一顶层专项，验收项由26扩展为39，全部随双配置完整自检通过。新增13项验证：

1. P7实际存在、P8实例特征、P9首态、P10当前换代及P6共享登记均经正式公开入口建立；
2. P6目标判断合同和P11迁移合同同时登记、正式读回且注册身份不同；
3. `0x0E`精确重复读回、已有不同请求拒绝，一节点/一配置值/一槽/七关系和20项配置精确；
4. P10把初值`-1`换代为`18`后，P9旧状态为左、P8新当前为右，得到增加、排序`-1`和差异`19`；
5. 同材料新编码换代得到等价、排序`0`和差异`0`；后续换代得到减少、排序`1`和负差异；
6. `INT64_MAX - (-1)`不可表示时整项入口拒绝，排序、具名关系、差异、等价和完整输入见证均为空；
7. provider未绑定、请求身份为零、错误实例槽和错误场景均结构化非成功且零专用载荷；
8. 四线程各20次并发只读只落成功且保持同一排序；比较前后L1事实代次及节点、关系和值数量不变；
9. 成功结果携带P9旧状态、P8新当前、左右输入版本回执、前状态/后当前事实角色和右减左方向；迁移比较完成后P6目标合同仍可正式读回。

## 4. 静态合同与范围

- `0x0E`定义唯一；三个新增公开函数在服务内各定义唯一，迁移请求不含用途、算法、注册身份、P2裸定义或动态身份。
- 迁移合同写集静态形状为节点1、值1、槽1、关系7；配置材料20项和七目标的解析读回均有机械校验。
- 生产比较只调用P9状态读取、P8实例特征读取和L1完整快照；未引入旧比较链、关系20、动作动态、因果、SQL、日志或旧WORLD-TREE依赖。
- P6单参构造、共享登记、目标判断合同和五个既有公开函数未删除或改名；P11差异精确限定三个代码文件和两份专属记录。
- P32增量只含三份设计材料和计划索引，与P11五文件零重叠；其它所有者dirty和未跟踪WIP均未暂存。

## 5. 最大证明边界

本验证只证明P11源码提供者、顺序340具名矩阵、双配置本地构建/自检、规范检查和模块依赖闭合。不证明第二状态发布、关系20迁移动能、动作动态、因果、生产装配、SQL/恢复、旧消费者迁移、具名集成验收、STEP-3整体或治理闭环完成。
