# RUNTIME-GOVERNANCE-TASK-SUBGOAL-RETURN-REJUDGMENT 验证记录 v0.1

日期：2026-08-23

## 1. 基线与验证根

- 计划：`RUNTIME-GOVERNANCE-TASK-SUBGOAL-RETURN-REJUDGMENT v0.1`。
- plan blob：`2f5bea911cf5c8ea7a46f410735673f357111964`；设计提交：`3b49b83c32b16a18490a35d78b6b1407903049eb`。
- 验证时正式基线：`HEAD == origin/main == 72b21bdf80f211daad374231656296296eaa2866`，分支为`main`，index 为空。
- 隔离验证根：`D:\TEMP\海中鱼巣\RUNTIME-GOVERNANCE-TASK-SUBGOAL-RETURN-REJUDGMENT\20260823-112726036`。

## 2. 构建与运行证据

| 检查 | 实际命令/证据 | 结果 |
| --- | --- | --- |
| Debug x64 全新重建 | `MSBuild.exe .\海中鱼巣.slnx /t:Rebuild /m:1 /p:Configuration=Debug /p:Platform=x64 /p:OutDir=<验证根>\Debug\out\ /p:IntDir=<验证根>\Debug\obj\ /v:minimal` | PASS；退出 0；0 warning、0 error |
| Release x64 全新重建 | 同上，`Configuration=Release`并使用`Release`隔离目录 | PASS；退出 0；0 warning、0 error |
| Debug 治理专项 | `<验证根>\Debug\out\海中鱼巣.exe --test-治理` | PASS；最终候选精确复跑退出 0、233.569 秒；`Debug/final-exact.治理专项.stdout.log`为 5338 字节且末行为`[治理测试] 全部通过`，stderr 为 0 字节；另一次未持久化退出码的正常复跑不作为精确退出码证据 |
| Release 治理专项 | `<验证根>\Release\out\海中鱼巣.exe --test-治理` | PASS；退出 0；约 10 秒 |

## 3. V01—V27 逐项对账

| 编号 | 结论 | 动态/静态证据与边界 |
| --- | --- | --- |
| V01 | PASS | 动态覆盖坏合同版本、零请求身份、零运行代次、零 G 和零记录；入口返回`入口拒绝`，公开请求不存在调用方自选处理键。 |
| V02 | PASS | 动态读取非待重判/不可消费具名记录并返回`记录不可消费`；未形成初次或后继载荷。 |
| V03 | PASS | 动态由具名记录取得显式父需求，并以同一 G 互证当前成员、完整列表项、当前任务和满足裁决；入口没有扫描任选记录。 |
| V04 | PASS | 动态当前已满足返回`目标已满足不筹办`且仅携带满足裁决；初次/后继工作包均空，事实代次不增加。 |
| V05 | NOT_RUN | 无法从合法公开 API 构造“已回流记录仍存活但列表项无当前任务”：来源任务被该记录强引用，退出任务返回`L2结构状态::引用冲突`。未修改退出语义或加入破坏 seam。无当前任务时复用初次 provider 的控制流与参数形状保留源码守恒。 |
| V06 | NOT_RUN | 依赖 V05 的合法初次结果，因同一`引用冲突`构造门禁未运行。源码固定使用`L2结构幂等身份{回流记录.值.值}`，先按后继键恢复、后继空才按初次键恢复。 |
| V07 | NOT_RUN | 依赖 V05 后退出所形成任务；任务仍受回流记录强引用，合法退出被`引用冲突`拒绝。未伪造退出或更改 task-owner 生命周期语义。 |
| V08 | PASS | 动态建立来源任务为统一当前任务的场景，公开 provider 返回`已形成后继筹办`并从 N 推进到 N+1。 |
| V09 | PASS | 动态读回后继准备记录、旧当前关系退出和新当前关系建立共享同一 G1；新当前仅指向该后继。 |
| V10 | PASS | 动态断言触发类型为`子目标回流`、触发事实为具名记录，前驱轮次/权威/初次记录一致；旧路径、旧实例、目标裁决和正式待重筹办状态引用均空。 |
| V11 | PASS | 动态同记录同请求重试返回`后继筹办精确重复`和同一后继/工作包；当前轮次不形成 N+2。 |
| V12 | PASS | 动态直接调用 DATA 请求，以同记录伪造其它键返回`入口拒绝`；既有后继身份及事实数量不变。 |
| V13 | PASS | 动态更改请求身份/运行代次以及构造 DATA 同键异义均返回`幂等冲突`；首次后继保留且零新增。 |
| V14 | NOT_RUN | 合法构造“当前任务不同于记录来源任务”需要先退出来源任务再建立新当前任务；来源任务退出因记录强引用返回`引用冲突`。未扩大本计划去改变退出合同。源码分支明确返回`当前任务冲突`且不调用后继提交。 |
| V15 | PASS | 动态当前任务为 legacy 时返回`轮次权威待迁移`；前后事实代次不变，未写 legacy 或统一后继。 |
| V16 | NOT_RUN | 公开 API 无法合法制造统一版本未知或权威/当前关系断裂；源码在分区非统一、权威/当前不完整时收口`内部错误`，没有 legacy fallback。 |
| V17 | PASS（静态/source invariant） | task-owner 以回流记录作触发事实反向查询并要求 0..1；同记录既有后继与请求不一致返回`引用冲突`并携带可选既有身份，写集触发事实固定为该记录。V12 同时动态证明伪造换键零新增。 |
| V18 | NOT_RUN | 未增加并发时序 seam，未动态注入两个不同回流在同 N 同时提交。现有 task-owner 互斥体内复核当前 N/当前关系并以期望 G 写入，失败不自动刷新为 N+2。 |
| V19 | NOT_RUN | 合法公开 API 无法形成`UINT64_MAX`当前统一轮次；提交入口在加一前显式检查上限并返回`内部错误`，未增加破坏 seam。 |
| V20 | PASS | 动态用旧 G 请求返回`当前性漂移`；不追赶当前事实、不刷新 N、不拼装或改走其它分支。 |
| V21 | NOT_RUN | 无合法 seam 注入“提交已经发布但调用方未知”。V11 动态证明原稳定键可恢复；源码恢复顺序只使用记录身份派生键，不生成新键或刷新 N 后重放。 |
| V22 | PASS | 动态按 G0/G1 历史读回后继，重构出原工作包与前驱链；身份、轮次、触发和原截止保持一致。 |
| V23 | PASS（静态/source invariant） | 静态扫描确认新 provider 零记录组扫描、零旧路径/实例复活，且不调用结算、分配、迁移、执行或旧实际结果重筹办入口。 |
| V24 | PASS | Debug/Release x64 隔离 Rebuild 均退出 0，0 warning、0 error。 |
| V25 | PASS | Debug/Release 最终候选均运行`--test-治理`并退出 0；本表保留逐项 PASS/NOT_RUN，不以总退出码替代。 |
| V26 | PASS | `git diff --check`、strict spec、integration self-test、UTF-8 严格解码、XML 解析及工程唯一登记均通过；无 HTML 变化。 |
| V27 | NOT_RUN | 新 provider 只由治理专项 import/调用；普通应用上下文、生产线程和正式入口零接线。迁移、筹办执行、结算和跨进程恢复均未运行。 |

V05—V07/V14 的`NOT_RUN`来自现行公开生命周期合同的真实构造限制，不是动态 PASS；V16/V18/V19/V21 也只保留源码守恒，不能升级为故障/并发/发布未知运行证明。

## 4. 结构、编码与静态门禁

| 检查 | 结果 |
| --- | --- |
| `git diff --check`（精确九文件候选） | PASS；只有 Git 行尾转换 warning，无空白错误 |
| `python .\tools\check_specs.py --strict` | PASS；116 个目录项全部通过 |
| `python .\tools\check_integration_acceptance.py --self-test` | PASS |
| UTF-8 严格解码 | PASS；七个代码/工程文件及两份专属记录均通过`.NET UTF8Encoding(false,true)`严格解码 |
| 工程 XML 解析 | PASS；`.vcxproj`和`.filters`均可解析 |
| 唯一工程登记 | PASS；新`.ixx`在项目/filters 各唯一`ClCompile`，新`.数据.h`各唯一`ClInclude`且 filter 匹配 |
| 生产零接线 | PASS；新 provider 仅在工程登记、provider 自身和治理专项出现，无普通应用/线程/正式入口 import 或调用 |
| 静态禁项 | PASS；新 provider 不调用旧实际结果重筹办、迁移、结算、分配、执行；`轮次权威待迁移`只出现在合法状态映射/返回 |
| HTML 守恒 | PASS；白名单和工作区 diff 无 HTML 变化 |

## 5. 事实数量、身份与恢复守恒

- 处理键由`回流记录.值.值`唯一派生；DATA 请求要求该键同时等于准备幂等身份和触发业务幂等身份。
- V08 首次提交只形成一份后继 N+1；V11 重放、V12 伪造换键、V13 换请求/运行代次和 V20 旧 G 后，既有后继身份保持相同且无 N+2。
- 后继写集在同一 G1 退出旧当前关系并建立指向新后继的唯一当前关系；G0/G1 历史读回保持原后继和前驱链。
- V15 legacy 前后事实代次不变，统一/legacy 写入数量均为 0。

## 6. 未证明边界

本记录只验证具名回流记录候选 provider 的实时重判、稳定键恢复、统一 N+1 task-owner 写集和明确非成功边界。它不证明 provider 或初次筹办已生产接线，不证明 legacy 已迁移/退役，不证明筹办执行、路径/实例/冻结/实际结果、成员结算、结果共用/争用或跨进程恢复已经完成，也不构成具名发布后集成验收。记录形成时结果提交仍为`待发布`。
