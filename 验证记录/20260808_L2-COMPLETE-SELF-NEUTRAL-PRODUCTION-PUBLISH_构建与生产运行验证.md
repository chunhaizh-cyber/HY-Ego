# 完整自我中性单事务生产发布构建与生产运行验证

日期：2026-08-08

计划身份：`L2-COMPLETE-SELF-NEUTRAL-PRODUCTION-PUBLISH`

计划版本：v0.2

plan blob：`c7838e365572604d832b70d4a3138c82125a633c`

施工起点：`31bc5d684c4837a2df11d0cf955873a2d6e4637a`

## 1. 已执行验证

| 项目 | 实际结果 |
| --- | --- |
| 目标路径 whitespace 检查 | 通过；无 whitespace 错误 |
| 正式规范严格检查 | 通过；110 / 110 |
| Debug x64 完整重建 | 通过；模块扫描、编译和链接完成，生成正式生产程序 |
| Release x64 完整重建 | 通过；模块扫描、编译和链接完成，生成正式生产程序 |
| Debug 独立模块依赖扫描 | 通过；sources / modules = 141 / 141，失败 0，循环 0，外部需求 0 |
| Release 独立模块依赖扫描 | 通过；sources / modules = 141 / 141，失败 0，循环 0，外部需求 0 |
| Debug 正式生产运行期模式 | 已运行；退出码 0，标准输出 0 字节，标准错误 0 字节 |
| Release 正式生产运行期模式 | 已运行；退出码 0，标准输出 0 字节，标准错误 0 字节 |
| 第一层调用位置 | 两个准入服务和组合服务直接第一层调用为 0；数据操作内中性提交物理调用点 1 |
| 写前 / 写后读取 | 写前一致当前投影一次；发布后按已分配稳定编码正式读取，生产启动再经组合公开入口全等读回 |
| 禁止路径 | 四个新模块对旧全量读取、审计、恢复、导入、重放和全局遍历调用为 0；本地日志、自检和通过判定为 0 |
| 生产顺序 | 自我结构登记 -> 完整自我发布 -> 完整自我公开读回 -> 旧系统角色初始化 |
| 写集静态形状 | 一般为四节点、四值、五加成员数条关系、四属性槽、零退出；生产成员数一时为四节点、四值、六关系、四属性槽、零退出 |
| 四项首跑门禁 | 默认空构造第一层、无恢复导入、无其它合法关系 21 写者、唯一完整自我数据操作发布者均有当前代码证据 |

实际构建命令使用当前 `海中鱼巣.slnx`、Debug / Release、x64、`Rebuild` 和并行编译。计划正文所列旧 Python 模块扫描入口在当前仓库不存在，实际使用仓库现行 `tools/clang_ast/扫描.MSVC模块依赖.ps1`。最终扫描候选输出写入系统临时目录，没有写入异主 `.codex-build`：

```text
C:\Users\zhchh\AppData\Local\Temp\L2完整自我-Debug-模块依赖-releasecandidate.json
C:\Users\zhchh\AppData\Local\Temp\L2完整自我-Release-模块依赖-releasecandidate.json
```

最终双配置生产运行的输出捕获位于：

```text
C:\Users\zhchh\AppData\Local\Temp\L2完整自我-Debug-releasecandidate.stdout.log
C:\Users\zhchh\AppData\Local\Temp\L2完整自我-Debug-releasecandidate.stderr.log
C:\Users\zhchh\AppData\Local\Temp\L2完整自我-Release-releasecandidate.stdout.log
C:\Users\zhchh\AppData\Local\Temp\L2完整自我-Release-releasecandidate.stderr.log
```

## 2. 真实运行链路

正式生产运行期只有在以下链路成立后才能继续并最终退出为零：世界、场景、实际存在、状态、动态、特征和自我结构等既有初始化先完成；生产上下文调用完整自我组合服务；组合服务从真实第二层 provider 取得同截止材料；两个领域准入服务分别批准存在域和场景域规格；唯一数据操作经第一层写前一致读取、中性原子提交和发布后正式读回形成完整自我当前事实；生产启动再经组合公开入口读回全等事实；之后才允许进入旧系统角色、概念活动和上下文发布。

因此，本次双配置运行能说明：完整自我这一真实第二层生产初始化路径实际使用了第一层的中性组合新增和正式查询能力，并且该业务路径没有观察到阻止启动的错误。日志和退出码只提供人读运行边界；业务控制来自结构化结果和正式读回，第一层没有生成自检或验收结论。

## 3. 未证明范围

- 没有运行第一层专项自检、外部第一层验收驱动、双目标或生产内通过判定。
- 没有故意制造同键精确重复、异义冲突、许可拒绝、资源失败、代次竞争、坏投影或提交后读回失败；这些分支由结构化控制流和静态合同复核覆盖，未被本次正常生产运行单独证明。
- 没有验证崩溃、重启、恢复、导入、跨进程收敛或持久化重建。
- 本次只覆盖完整自我这一实际消费者所需的第一层新增与查询路径；不证明第一层修改、删除能力，也不证明第一层对全部第二至第五层消费者已经完善。
