# 当前关系源端条件查询中性提供者构建验证

日期：2026-08-08

## 1. 验证对象与结论层级

- 起点：`f03d901d96290ab920a22ff1cada3a4d47f0702d`
- 计划 blob：`b89e709d484d2b347ebb9f5c2a3fc6264cdcc658`
- 对象：第一层按源节点与关系类型读取当前关系有序组，以及源端、目标端两个非权威索引在中性与兼容提交候选中的同步维护。
- 结论层级：源码候选的完整构建、模块依赖和静态合同门禁通过；没有执行第一层独立验收，也没有真实第二层消费者运行结论。

## 2. 已执行验证

| 验证 | 结果 |
| --- | --- |
| 三代码路径 `git diff --check` | 通过，无 whitespace 错误 |
| Debug x64 串行完整重建 | 通过；模块扫描、编译和链接完成，生成正式生产程序 |
| Release x64 串行完整重建 | 通过；模块扫描、编译和链接完成，生成正式生产程序 |
| Debug MSVC 模块依赖扫描 | 136 / 136，失败 0，循环 0，外部缺失 0 |
| Release MSVC 模块依赖扫描 | 136 / 136，失败 0，循环 0，外部缺失 0 |
| 正式规范严格检查 | 110 / 110 通过 |

两份模块扫描候选输出写入系统临时目录，没有写入或覆盖工作区现有 `.codex-build`。两配置构建和模块扫描只证明代码候选能够编译、链接并保持模块闭合；本次没有通过生产程序单独驱动第一层查询，也没有生成第一层验收结论。

## 3. 静态合同复核

| 门禁 | 实测 |
| --- | --- |
| 新中性数据结构 | 请求定义 1 处、结果定义 1 处 |
| 唯一公开服务入口 | 第一层事实基座服务定义 1 处；仓库条件查询定义 1 处 |
| 合同与禁止路径 | 合同模块、第二至第五层、运行期上下文、启动、工程、filters 和生产入口差异均为 0 |
| 派生索引形状 | 两级键为源节点和关系类型；桶值只含关系稳定编码有序组 |
| 两条提交路径维护 | 中性和兼容路径的关系创建各插入源端、目标端索引一次，关系退出各删除源端、目标端索引一次 |
| 原子发布边界 | 两个索引只修改候选状态；失败不交换，成功沿用既有一次状态交换 |
| 独立完整覆盖 | 源端和目标端索引分别使用独立集合验证对全部当前关系的恰好覆盖 |
| 普通查询访问 | 共享锁 1 处，只查一个源端类型桶，并在同一锁内回读权威当前关系 |
| 普通查询禁用调用 | 状态完整、全量派生、目标端查询、完整快照、历史、审计、恢复、SQL、旧管理索引和日志均为 0 |
| 结果形状 | 合法零命中成功空组；非空组按关系稳定编码严格升序、无重复，并逐项验证编码、源端、类型和未退出 |
| 验收机制 | 新增第一层自检、独立验收、双目标、第二可执行程序和生产内通过判定均为 0 |

## 4. 尚未证明

本记录不是第一层自检或自证。代码精确发布后，仍须由真实第二层场景完整事实生产服务经正式第一层公开入口实际读取直接子场景和直接存在成员，并由第二层自行验证资格、方向、角色、单父、无环、世界归属、共同截止和稳定业务排序。

只有第二至第五层合法消费者在自身真实服务运行中，经第一层正式入口、结构化结果和最终权威读回累计覆盖增、查、改、退、历史读取、幂等、原子性和非法请求零变化，且各自业务合同成立，才可认定第一层足以支撑当前消费者集合。本次不证明关系替换、性能、并发压力、崩溃、重启、跨进程恢复、旧栈删除或整个第一层完善。
