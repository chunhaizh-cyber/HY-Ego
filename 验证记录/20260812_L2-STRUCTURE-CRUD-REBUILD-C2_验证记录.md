# L2-STRUCTURE-CRUD-REBUILD-C2 验证记录

日期：2026-08-12

## 已运行

- C2 专项 `Debug|x64 Rebuild` 加运行，默认参数基值 101000、顺序 10 / 20 / 30 / 40：24/24 PASS，失败数 0。
- C2 专项 `Release|x64 Rebuild` 加运行，参数基值 105000、顺序 11 / 21 / 31 / 41：24/24 PASS，失败数 0。
- 两配置并发路径均以 `std::barrier` 同起步，一个动作替换写者与两个完整读取者各固定 64 轮；实际计数均为允许状态 129、非法状态 0，无 sleep、内部不一致、部分关系组或混代结果。
- 根工程 `Debug|x64 Rebuild`：PASS。
- 根工程 `Release|x64 Rebuild`：PASS。
- `python .\tools\check_specs.py --strict`：113/113 PASS。

## C2 专项直接覆盖

- 条件和结果引用组整组原子替换；替换前后历史均为完整同截止投影，未修改的其它角色保持不变。
- 动作引用有到异值、有到空、空到有；同值替换、错误旧闭包和退出闭包缺项均返回 `引用冲突`、空载荷且事实代次零推进。
- 来源不同材料值换代、同来源显式换代、错误旧来源值拒绝；旧来源值与新值的代次和稳定编码符合合同。
- 因果节点及全部自有事实同代退出；退出后当前读取为 `已退出`，退出截止和退出前历史均返回完整同截止投影；原退出请求为精确重复且零推进。
- 独立外部 owner 经其公开写端口建立“外部自有普通源节点 -> 因果节点”的关系。该当前关系使 C2 `退出因果` 透传 `引用冲突`、空载荷且零推进；外部 owner 关闭关系后，C2 以新当前代次和新幂等身份退出该因果成功。

## L1 通用七角色保护绑定

- 已发布专项提交：`962154927fc37ed85f6b72807cc6bbc1644264b4`。
- `验证工具/L1中性当前引用闭包保护参数验证.cpp`：提交内及当前 blob 均为 `57e0fd7e7ad6164c9494c51da09126470bbac690`。
- `验证记录/20260811_L1-NEUTRAL-CURRENT-REFERENCE-CLOSURE-PREWRITE-GUARD_中性当前引用闭包保护验证记录.md`：提交内及当前 blob 均为 `fdc7e2e64b8f1184cece1dbf7b3d3ae223a187cd`。
- 该绑定是 L1 公共写端口对关系、所属值、值来源、独立材料等七角色的通用闭包保护证据。所属值、值来源和独立材料引用因果三类没有在 C2 因果专项中直接运行，不能据此写成 C2 端到端通过。

## 最终静态门禁

- 五份实现 / 专项文件严格 UTF-8：PASS；专项 `.vcxproj` XML 解析：PASS。
- C2 专项三文件未登记主工程或 filters；主工程四个编译配置的 `/utf-8` 保持不变。
- 生产差异静态确认只有一个角色枚举、八个 C2 DTO 和四个公开函数；四类替换 / 换代本地键复用原 C1 唯一定义。
- 两份生产文件未命中测试钩子、sleep、L2 动态、任务、方法、第二服务、仓库、缓存、SQL 或日志依赖。
- 七路径 `git diff --check`：PASS，仅有工作区 LF/CRLF 转换提示，不是空白错误。

## 尚未覆盖

专项未覆盖因果真实性 / 必然性、动作是否成立、A1 聚合、生产消费者、真实分配失败、长时公平、崩溃恢复、跨进程或独立集成验收。最终 staged 范围、`git diff --cached --check` 和 Git 发布事实须在发布阶段复核；本文件不预先宣称未发生的提交或推送。

## v0.4 重新验证

旧 v0.3 的 24 / 24、构建和记录不计入本节。以下均从 v0.4 起点 `7c796268446f2edb359778a9778f8db5f26c808c` 重新运行：

- C2 专项 `Debug|x64 Rebuild` 加运行，基值 101000、顺序 10 / 20 / 30 / 40：93 / 93 具名检查 PASS，失败数 0；正式随机临时目录产物发布前复跑仍为 93 / 93 PASS、进程退出 0。
- C2 专项 `Release|x64 Rebuild` 加运行，基值 105000、顺序 11 / 21 / 31 / 41：93 / 93 具名检查 PASS，失败数 0；正式随机临时目录产物发布前复跑仍为 93 / 93 PASS、进程退出 0。
- 根工程 `Debug|x64 Rebuild`：PASS；根工程 `Release|x64 Rebuild`：PASS。
- 九组直接运行账：状态组替换与三查询、状态组拒绝、状态组幂等、动作替换与查询 / 历史、来源换代、退出请求与三查询、四入口共同参数 / 漂移、跨 owner 外部关系、四个独立并发窗均 PASS。
- 状态组逐叶覆盖当前组原样，旧闭包缺 / 错 / 多，新组空、零顺序、零目标、重复、未升序、超 `0x0FFF`，wrong-family、legacy、裸 L1 和已退出状态端点，以及立即 / 后继重复和同键改角色 / 新组 / 旧闭包。
- 动作逐叶覆盖空到有、有到异值、有到空的查询与前后完整历史，空到空、旧关系形状错误、未找到、已退出、非普通、立即 / 后继重复和同键改旧关系 / 新动作。
- 来源逐叶覆盖异来源、同来源显式新版本、错旧值、未找到、已退出、立即 / 后继重复和同键异来源；新值、旧值退出、材料和值来源直接核对。
- 退出逐叶覆盖族归属、条件、动作、结果和来源值五族的漏 / 错 / 多，以及零、重复、未升序、含因果节点；成功后当前单项已退出、三查询排除，退出前 / 退出截止 / 更晚历史完整，重复 / 异义精确分账。
- 四公开入口分别直接运行错合同、零期望、零幂等、零因果身份、新键旧期望和已退出因果，全部非成功空载荷且提交前零推进；同键异义由各自入口另行直接运行。
- 四个并发窗分别只有状态组、动作、来源或退出一个写者，并配当前完整、条件、动作、结果四个读者；每窗 `std::barrier` 五方同起步，每读者固定 32 轮，无 sleep / 重试。每窗精确计数为允许状态 `1 + 4 * 32 = 129`、资源失败 0、非法状态 0；每配置四窗合计允许状态 516、资源失败 0、非法状态 0。竞争后静止终态唯一完整；退出窗唯一终态为已退出。

## v0.4 NOT_RUN 与证据分账

- `NOT_RUN`：发布后中性代次探测真实资源失败、owner-aware 许可 / 资源失败以及成功但私有事实损坏，没有在 C2 端到端专项注入。C2 公开 ABI 没有合法故障 / 私损注入入口；本轮没有为补证增加友元、测试钩子、计数器或生产故障入口。
- 这些状态由生产 `switch` / 共同辅助的穷尽静态闭包和既有 L1 公开 / Debug 故障证据保守绑定；不能写成 C2 运行通过。
- `NOT_RUN`：所属值、值来源、独立材料引用因果三类未在 C2 因果专项直接触发。底层闭包证据仍只绑定 L1 通用七角色提交 `962154927fc37ed85f6b72807cc6bbc1644264b4`、源码 blob `57e0fd7e7ad6164c9494c51da09126470bbac690`、记录 blob `fdc7e2e64b8f1184cece1dbf7b3d3ae223a187cd`。
- C2 直接证明的外部引用只有合法跨 owner 关系：关系当前时 `退出因果` 透传 `引用冲突`；外部 owner 关闭关系后，以新代次 / 新幂等身份退出因果成功。

v0.4 仍未覆盖因果真实性 / 必然性、主动 / 被动、动作成立 / 执行、动态证据、概率、模板、业务用途、A1、生产装配、真实消费者、长期公平、真实分配失败、私有损坏、恢复、跨进程或独立集成验收。最终 staged 与 Git 发布事实仍只在发布阶段形成。

## v0.4 第二次发布前复核与最终重跑

- 第二次发布前逐入口扫描发现四写后续完整历史读回和四类逐项旧历史互证仍有非成功折叠；修复后，四个完整历史调用点均先透传 L2 非成功状态，再检查成功载荷形状；四类 L1 历史互证均先经 `映射历史失败` 分账，再检查成功事实种类、字段和退出代次。
- 编译期静态门禁：`映射历史失败` 全部九个 L1 枚举叶均由 `static_assert` 锁定；成功映射已读取，入口、许可、未找到、已退出、事实代次漂移和资源失败分别映射同名 L2 状态，属性未设置与内部不一致映射内部不一致。该门禁证明生产映射闭包，不冒充不可注入状态的 C2 端到端运行。
- C2 专项 `Debug|x64 Rebuild` 加运行：93 / 93 PASS、失败数 0；加入完整九叶静态断言后的最终随机临时目录 `C:\Users\zhchh\AppData\Local\Temp\海中鱼巣_C2_be63b584c24749f4a4b0a490807063dc`。
- C2 专项 `Release|x64 Rebuild` 加运行，基值 105000、顺序 11 / 21 / 31 / 41：93 / 93 PASS、失败数 0；加入完整九叶静态断言后的最终随机临时目录 `C:\Users\zhchh\AppData\Local\Temp\海中鱼巣_C2_ccda32c8d4f2438aabe1dd1b10b3a68c`。
- 两配置各四个并发窗均 PASS；每窗允许状态 129、资源失败 0、非法状态 0，每配置四窗合计 516 / 0 / 0。
- 根工程 `Debug|x64 Rebuild`：PASS；根工程 `Release|x64 Rebuild`：PASS；`python .\tools\check_specs.py --strict`：113 / 113 PASS；七路径 `git diff --check`：PASS，仅 LF / CRLF 转换提示。
- 本节结果取代修复前的发布前复跑结论；`NOT_RUN` 和完成声明边界保持上一节口径，不因静态映射与构建通过而升级。

## v0.4 第二次独立核验退回与异常边界重跑

- 第二次独立核验对结果提交 `929c50ad7da9ed0f54c94e9d319325d5a3fd20a0` 判定 FAIL，具名 BLOCKER 为 `DRIFT-C2-POSTPUBLISH-EXCEPTION-LOSES-FIRST-CUTOFF`：四公开写入口的外层 catch 未区分写前异常与已确认发布后的异常，后者错误返回截止 0、变更空。
- 生产静态门禁：四个 `已发布首次代次` 声明、四个成功边界后赋值、十二个 catch 统一调用 `形成发布后异常代次边界`；两条 `static_assert` 直接锁定空值与非空值的截止 / 变更形状。赋值只发生在 `写入成功边界完整` 返回真之后，不以“调用过提交”代替已发布证明。
- C2 专项 `Debug|x64 Rebuild` 加运行：93 / 93 PASS、失败数 0；随机临时目录 `C:\Users\zhchh\AppData\Local\Temp\海中鱼巣_C2_91416da9076c4b4c8429029ee0aae79b`。
- C2 专项 `Release|x64 Rebuild` 加运行，基值 105000、顺序 11 / 21 / 31 / 41：93 / 93 PASS、失败数 0；随机临时目录 `C:\Users\zhchh\AppData\Local\Temp\海中鱼巣_C2_3a35dffd178e416aa966a93906cc3b2e`。
- 两配置各四个并发窗均 PASS；每窗允许状态 129、资源失败 0、非法状态 0，每配置四窗合计 516 / 0 / 0。
- 根工程 `Debug|x64 Rebuild`：PASS；根工程 `Release|x64 Rebuild`：PASS。
- `NOT_RUN`：C2 公开 ABI 没有合法异常 / 分配失败注入入口，故写前真实分配失败、发布后读回 / 互证 / 结果构造真实抛出 `bad_alloc`、`length_error` 或其它异常未端到端触发。本轮不为补证增加友元、故障入口、计数器或测试钩子；运行矩阵证明正常与并发路径未回归，异常代次边界只由生产静态闭包与编译期断言证明，不写成 C2 运行通过。
- 既有 `NOT_RUN`、L1 七角色保守绑定和业务完成声明边界继续有效；本修复不证明因果真实性 / 必然性、A1、生产装配、真实消费者、恢复、跨进程或独立集成验收。
