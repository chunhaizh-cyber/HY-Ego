# RUNTIME-GOVERNANCE-BATCH-CLASSIFICATION 验证记录 v0.2

日期：2026-08-20

计划：`RUNTIME-GOVERNANCE-BATCH-CLASSIFICATION v0.2`

重新 S0 起点：`e54bb85607eae30601ad962b6f80b779ad33ca05`

## 1. 构建与运行

| 验证 | 结果 |
| --- | --- |
| Debug x64 fresh Rebuild | PASS，`0 warning / 0 error`。v0.2 最终实现用时约 27.53 秒。 |
| Debug `海中鱼巣.exe --test-治理` | PASS，exit 0。 |
| Release x64 fresh Rebuild | PASS，`0 warning / 0 error`，用时约 30.64 秒。 |
| Release `海中鱼巣.exe --test-治理` | PASS，exit 0。 |

治理专项新增动态覆盖：

- 停止前普通消息和停止消息均经邮箱合法提交；冻结批次保留两条完整消息，分类只交付停止项并截断后继；
- 概念待命名和观察材料两种合法普通消息保持邮箱稳定顺序并映射唯一类别；
- 截止时间在提交时有效、分类时已过期的消息形成唯一 `材料已过期`时效诊断，最终状态为`存在时效拒绝`；
- 坏合同、空指针、坏运行代次和坏协议版本均返回`入口拒绝`；
- 分类前后从同一事实版本服务读取的共享事实截止代次一致，未观察到分类写入领域事实。

## 2. 静态门禁

| 验证 | 结果 |
| --- | --- |
| `python .\tools\check_specs.py --strict` | PASS，116/116。 |
| `git diff --check` | PASS；仅报告现有 LF/CRLF 工作树提示，无空白错误。 |
| 白名单核对 | PASS：五个代码文件和两份计划专属记录；其它 WIP 未修改、未纳入。 |
| 不可达 seam 核对 | PASS：未修改邮箱实现、冻结批次私有字段或 L3 协议，未增加第二消息 / 批次入口。 |

## 3. 未验证和声明边界

`002-04`及其后业务均为 `NOT_RUN`。真实内存耗尽、异常注入、跨进程恢复、长时间并发、需求活动快照、治理判断、任务筹办、任务执行、结果学习和价值回流未由本矩阵证明。v0.2 明确不构造合法路径不可达的重复 / 同键异义 / 空冻结批次成功测试。
