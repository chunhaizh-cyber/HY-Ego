# INSTINCT-STAGE3-SERVICE-MAINTENANCE-HISTORY-LEDGER 验证记录 v0.1

日期：2026-08-31

计划：`INSTINCT-STAGE3-SERVICE-MAINTENANCE-HISTORY-LEDGER v0.1`

验证基线：工作区起点 `0a34e3ba26099e6e74d8289f08cddad61d77afc6` 加本计划五文件候选；发布提交待填。

隔离根：

```text
D:\TEMP\海中鱼巣\INSTINCT-STAGE3-SERVICE-MAINTENANCE-HISTORY-LEDGER\20260831-022752
```

## 1. 结果表

| 验证项 | 结果 | 证据边界 |
| --- | --- | --- |
| Debug x64 `ClCompile`，专项宏启用 | PASS | 隔离基线覆盖三份候选后编译通过 |
| Release x64 `ClCompile`，专项宏启用 | PASS | 隔离基线覆盖三份候选后编译通过 |
| Debug 专项目标运行 | PASS | H00—H06、W00—W05及既有 P/V/Q 全通过 |
| Release `/GL + /LTCG` 专项目标运行 | PASS | 同一专项全通过 |
| Debug 外部 ABI consumer | PASS | 导入正式模块并静态核对目标入口完整 member-function 签名及两版本常量 |
| Release `/GL` 外部 ABI consumer | PASS | 同上；对象编译通过 |
| 五类持久恢复 | PASS | W05 对合同、状态、到期事件、进展、准备的历史组、排序、见证和截止逐字段相等 |
| `git diff --check` | PASS | 本计划候选五文件无空白错误；行尾转换警告不改变结论 |
| `python .\tools\check_specs.py --strict` | PASS | 116 / 116 |
| 第二 owner / 候选键冲突 / 生产调用扫描 | PASS | 固定登记仅在目标 owner；候选常量的其它命中仅为已发布设计 / 计划；目标入口当前无生产消费者 |
| 阈值重算残留扫描 | PASS | 本计划三份源码没有 L/H 常量或运行期重算增量；既有 `INT64_MAX` 只用于载荷解码值域守卫 |
| 共享工作区完整 Debug / Release 链接 | BLOCKED（异主） | 当前 SELF-GOVERNANCE WIP 使普通应用接口和多个 L2 模块符号不一致；不归入本切片 |

## 2. 专项覆盖

- H00：登记覆盖边界、合同 / 状态范围、少 1 数量预算、合法空范围。
- H01：合同、初态、到期终态、到期事件永久历史和唯一退出当前边界。
- H02：合同 / 状态 1 项、进展 3 项、准备 2 项，规范见证、精确预算与左开边界。
- H03：合同、状态、到期事件坏载荷整体 `引用冲突`、零载荷。
- H04：读中事实代次漂移和资源失败均结构化零载荷。
- H05：退出登记后当前进展的永久历史成员，反向完整性检查返回 `集合不闭合`。
- H06：重复历史成员和同写集无载荷成员分别整体失败。
- W00 / W01：进展 / 准备首次、更新、旧当前退出、旧历史保留和精确重放。
- W04：提交后读回失败以原请求重试，收敛到首次事实且无第二历史副作用。
- W05：持久恢复后两发布者精确重放、当前集合和五类历史逐字段一致。

## 3. 链接证据说明

共享基线的完整链接被本计划外的未提交接口漂移阻断。为运行本计划专项，隔离根只链接目标测试、目标服务、L1 基座和 DTO 所需模块对象；基线中与目标不可达但仍产生未解析符号的 L2 函数使用 `/FORCE:UNRESOLVED` 保留警告。该映像实际执行了目标专项并返回 0，但此证据只证明目标可达路径运行，不替代完整根工程链接。外部 ABI consumer 另以 Debug / Release IFC 正式导入和静态签名核对通过，不使用该运行映像推断全工程 ABI 闭合。

## 4. 未验证 / 不声明

- 未在共享 dirty 工作区形成可发布的完整根工程链接结果。
- 未验证后继单调秒历史段 composer、普通应用或线程消费，因为本计划不实现这些消费者。
- 未声明前三类事实已经有生产发布者；当前仅有测试宏 fixture 验证读取机制。
- 未声明阶段三、服务维护闭环或 `INSTINCT-ROUTE` 完成。
