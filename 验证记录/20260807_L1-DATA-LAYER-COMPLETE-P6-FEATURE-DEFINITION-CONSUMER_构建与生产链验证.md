# I64 特征定义真实消费者构建与生产链验证

日期：2026-08-07

## 1. 验证对象

- 起点：`2f0e3ad5f1a3470d61fa9a29fdc423e87a608f72`
- 计划 blob：`a11a0adbb80a1ee256113176478f6179bfdb8c80`
- 对象：`L1特征定义服务` 的登记建立、I64 定义建立和两条公开权威读取路径。
- 结论层级：施工门禁通过；合法消费者服务验收尚未执行。

## 2. 已执行验证

| 验证 | 结果 |
| --- | --- |
| Debug x64 串行完整重建 | 通过；零警告、零错误；模块依赖扫描实际执行并编译、链接目标服务 |
| Release x64 串行完整重建 | 通过；零警告、零错误；模块依赖扫描实际执行并编译、链接目标服务 |
| Debug 正式 `--runtime-context` | 退出码 0 |
| Release 正式 `--runtime-context` | 退出码 0 |
| 正式规范严格检查 | 110/110 通过 |
| 工作区差异空白检查 | 通过，无 whitespace 错误 |

生产入口退出码只证明现有生产链未因本次迁移失效；日志和退出码不承担消费者服务验收或第一层完成判定。

## 3. 静态合同复核

| 门禁 | 实测 |
| --- | --- |
| `探测幂等`、旧 `提交写集`、`读取完整快照` | 均为 0 |
| 领域意图、提供者见证和业务操作标签 | 均为 0 |
| `提交中性写集` | 恰 2 个物理调用点 |
| 登记写集 | 4 节点 + 0 关系 + 0 值 + 0 属性槽 + 0 退出 |
| 定义写集 | 1 节点 + 3 值 + 3 属性槽 + 0 关系 + 0 退出 |
| 当前读回 | 只使用中性事实代次、节点、属性和值入口，并执行首末代次校准 |
| 服务内权威副本 | 0；只保留登记四编码非权威定位 |
| 数据头、运行期上下文、生产启动、工程和筛选器差异 | 0 |

## 4. 尚未证明

本记录不是第一层自检或自证。代码精确发布后仍须由生产代码之外的合法消费者验收方，经生产会话和运行期上下文租约取得具名特征定义服务，依次覆盖登记首次、定义首次、定义精确重复、同键不同合法区间冲突、非法区间拒绝，并在非成功前后通过正式定义读取逐字段核对权威状态零变化。验收驱动不得导入或调用第一层，也不得解析日志。

该消费者验收通过后也只能累计证明本消费者实际使用的节点 / 值 / 属性槽新增与读取、两个写集原子性、精确重复、同键异义冲突和非法请求零变化；不证明修改、退出 / 历史、关系查询、其它消费者或整个第一层完善。

## 5. v0.2 回修验证

| 验证 | 结果 |
| --- | --- |
| Debug x64 串行完整重建 | 通过；零警告、零错误；模块依赖扫描执行 |
| Release x64 串行完整重建 | 通过；零警告、零错误；模块依赖扫描执行 |
| Debug / Release 正式 `--runtime-context` | 两配置均退出码 0 |
| 正式规范严格检查 | 110/110 通过 |
| 两条非 `const` 兼容入口 | 各恰一次转发对应 `const` 正式入口 |
| 两条 `const` 正式写入口 | 各恰一份完整实现，共享唯一锁与定位 |
| `mutable` 成员 | 唯一服务锁和四编码定位各一处 |
| 中性 / legacy 门禁 | 中性提交恰 2；旧探测、旧提交、完整快照和 `const_cast` 均为 0 |
| 禁止路径差异 | 运行期租约、上下文、数据头、第一层、工程、筛选器和生产入口均为 0 |

本节只证明 v0.2 施工候选可编译、可链接且原生产启动未退化。必须在该候选精确提交并推送后重新执行独立消费者验收；只有合法消费者能经 `const` 具名服务完成首次、精确重复、冲突、非法请求零变化和正式权威读回，P6 才能取得服务验收结论。
