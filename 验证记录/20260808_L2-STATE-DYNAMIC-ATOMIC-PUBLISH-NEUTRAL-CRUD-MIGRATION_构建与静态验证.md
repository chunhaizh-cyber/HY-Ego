# L2-STATE-DYNAMIC-ATOMIC-PUBLISH-NEUTRAL-CRUD-MIGRATION 构建与静态验证

日期：2026-08-08

计划：`L2-STATE-DYNAMIC-ATOMIC-PUBLISH-NEUTRAL-CRUD-MIGRATION v0.1`

plan blob：`2a86422a99d8314c00c0f0593b0fe72ff1565f83`

施工起点：`432f1f320b8d305fb9bb2b02cfd5603231d78bd6`

## 1. 构建与模块扫描

| 门禁 | 结果 |
| --- | --- |
| 第一次 Debug x64 串行完整 Rebuild | FAIL；`C2280`、`C2088`，原因是嵌套请求 DTO 使默认 `状态动态原子发布请求 operator==` 被删除 |
| 修复后 Debug x64 串行完整 Rebuild | PASS，退出 0；0 错误、1 条 `MSB8029` 临时目录提示 |
| Release x64 串行完整 Rebuild | PASS，退出 0；0 错误、1 条 `MSB8029` 临时目录提示 |
| Debug MSVC 模块扫描 | PASS，sources / modules=`136 / 136`、failed=`0`、拓扑 `136`、循环 `0`、未解析需求 `0` |
| Release MSVC 模块扫描 | PASS，sources / modules=`136 / 136`、failed=`0`、拓扑 `136`、循环 `0`、未解析需求 `0` |
| `python .\tools\check_specs.py --strict` | PASS，110 / 110 |
| 四个白名单路径 `git diff --check` | PASS，无 whitespace 错误；仅 Git 行尾策略提示 |

第一次失败属于计划白名单内的代码错误：公开请求的嵌套 DTO 没有完整默认相等运算链。修复只删除不可用的默认请求相等运算，并在服务私有实现中逐字段比较完整公开请求；修复后两配置均完成全量编译链接。

构建使用 Visual Studio 18 Professional `MSBuild.exe`，配置为 Debug / Release、x64、`Rebuild`、`/m:1`。`IntDir`、`OutDir`、构建日志和扫描 JSON 均位于系统临时目录 `C:\Users\zhchh\AppData\Local\Temp\hzy-l2-state-dynamic-neutral-c838e3c535e54a9fb605884c21337977`。模块扫描记录的观察提交为施工起点 `432f1f320b8d305fb9bb2b02cfd5603231d78bd6`；扫描包含当前未提交源码候选。生成的生产程序没有运行。

## 2. 静态合同门禁

| 检查 | 结果 |
| --- | --- |
| 合同 / 规则版本 | PASS，v2 各唯一 1 个 |
| 旧公开幂等域 / 意图组 / 入口变体 / 业务标签 | PASS，数据头和目标服务中均为 0 |
| 旧幂等探测调用 | PASS，0 |
| 旧 `提交写集` 调用 | PASS，0 |
| 领域意图 / 执行证据材料构造 | PASS，均为 0 |
| 旧摘要 / 旧完整读回 / L1 失败见证字段 | PASS，数据头中均为 0 |
| 中性提交调用点 | PASS，`L1_.提交中性写集(...)` 恰 1 |
| 普通互斥锁所有者 | PASS，唯一成员 `std::mutex 锁_`；另一个 `std::mutex` 文本命中是 `std::lock_guard<std::mutex>` 使用点，不是第二个锁对象 |
| 多项发布账所有者 | PASS，唯一成员 `std::map<std::uint64_t, 发布记录> 发布账_` |
| 同键完整请求比较 | PASS，逐字段覆盖请求、后状态、实例、宿主、场景、定义及节点身份字段 |
| 首次 / 重复提交物理入口 | PASS，共用唯一中性提交调用点；重复复制并重提首次完整写集 |
| 首次 L1 精确重复 | PASS，静态路径允许建立本对象首次代次、映射和读回账 |
| 发布后保留 | PASS，L1 已形成内存权威发布或成功形状均先进入 `已发布待读回`；删除只出现在首次明确零发布或尚未进入 L1 的异常路径 |
| 状态权威读回 | PASS，核对读回身份、首次创建代次和读取截止 |
| 动态权威读回 | PASS，核对读回身份、首次创建代次、读取截止和后状态关系目标 |
| 本计划之外新增暂存文件 | PASS，Git index 为空 |

## 3. 业务场景运行矩阵

当前没有合法生产消费者，计划禁止新增专项驱动、L1 专项验收、外部覆盖补格或生产内自检。因此下列场景全部明确为 `未运行`：

| 场景 | 结果 |
| --- | --- |
| 非法版本、零幂等身份或无效后状态请求在上游调用前拒绝 | 未运行 |
| P13 无前状态、不可比较、漂移及其它结构化非成功 | 未运行 |
| P13 等价后状态且不调用 P14 | 未运行 |
| P13 非等价并调用 P14 | 未运行 |
| P14 非成功、坏成功或共同截止不一致 | 未运行 |
| 首次中性提交成功并读回仅后状态 | 未运行 |
| 首次中性提交成功并读回后状态与迁移动能 | 未运行 |
| 新服务对象首次遇到 L1 精确重复并建立本对象账 | 未运行 |
| 同义重复原样重提首次写集并得到幂等读回 | 未运行 |
| 同键异请求在领域层冲突 | 未运行 |
| L1 明确零发布非成功并删除首次预占 | 未运行 |
| L1 已形成内存权威发布但返回非成功并保留隔离记录 | 未运行 |
| 中性映射缺失、重复或形状矛盾 | 未运行 |
| 状态 / 动态读回请求形成失败 | 未运行 |
| 状态身份或创建代次矛盾 | 未运行 |
| 动态身份、创建代次或后状态关系矛盾 | 未运行 |
| 同服务对象并发首次、同义重复和同键异义串行化 | 未运行 |
| `std::bad_alloc` / 其它异常在发布前后分账 | 未运行 |

构建、模块扫描、strict 和静态路径复核只证明源码候选可构建、模块依赖闭合且机械形状符合计划；不能冒充上述运行期业务证据或 L1 第一层证明。

## 4. 声明边界

允许声明：状态动态原子联合发布入口的源码候选已迁移到业务中性提交，旧探测 / 旧提交 / 业务证据构造在目标模块归零，专属普通锁、多项完整请求账、首次写集重提和状态 / 动态权威读回静态路径已经形成；双配置重建、双配置模块扫描、strict 与静态门禁通过。

禁止声明：服务对象已由运行期装配或被真实业务调用；任何业务场景已经运行；世界树第四写、其它旧读取迁移、旧核心写面删除、第二至第五层服务集合、集成验收或 L1 已完善。

验证完成时 Git index 为空；本轮未 stage、commit 或 push。
