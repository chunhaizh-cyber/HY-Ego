# 实际存在 I64 实例特征真实消费者构建与生产链验证

日期：2026-08-07

## 1. 验证对象与结论层级

- 起点：`94b77802cd615486c8e179183ffa8210b0cb2177`
- 计划 blob：`29d305e5e3b5c0138e3764dc5dcacaab8bc50107`
- 对象：实例特征登记、实际存在 I64 实例建立、正式读取和运行期第四引用装配。
- 结论层级：施工候选构建与生产链门禁通过；合法消费者业务验收尚未执行。

## 2. 已执行验证

| 验证 | 结果 |
| --- | --- |
| Debug x64 串行完整重建 | 通过；模块扫描、编译和链接完成，生成正式生产程序 |
| Release x64 串行完整重建 | 通过；模块扫描、编译和链接完成，生成正式生产程序 |
| Debug 正式 `--runtime-context` | 退出码 0 |
| Release 正式 `--runtime-context` | 退出码 0 |
| Debug MSVC 模块依赖扫描 | 136 / 136，失败 0，循环 0，外部缺失 0 |
| Release MSVC 模块依赖扫描 | 136 / 136，失败 0，循环 0，外部缺失 0 |
| 正式规范严格检查 | 110 / 110 通过 |
| 目标差异空白检查 | 通过，无 whitespace 错误 |

构建、模块扫描和生产入口退出码只证明候选代码可编译、可链接并未破坏该生产启动路径；日志和退出码不承担业务验收或第一层完成判定。

## 3. 静态合同复核

| 门禁 | 实测 |
| --- | --- |
| 实例服务生产构造 | 唯一四引用形态；实际存在依赖为必需引用 |
| `提交中性写集` | 恰两个物理调用点：登记一次、实际存在实例一次 |
| 登记写集 | 4 节点 + 0 关系 + 0 值 + 0 属性槽 + 0 退出 |
| 实例写集 | 1 节点 + 2 关系 + 1 I64 值 + 1 当前属性槽 + 0 退出 |
| 登记内部状态 | 只保存四编码定位，不保存登记事实副本 |
| 实例内部状态 | 只保存幂等身份、宿主定义组合和正式读取定位 |
| 实例正式读回 | 使用中性事实代次、节点、关系、属性和值入口并执行首末代次校准 |
| 目标建立和读取路径 | 不调用预探测、旧提交、完整快照、领域意图、执行证据、提供者见证或业务操作标签 |
| 禁止路径 | 数据头、第一层核心与仓库、中性合同、工程、筛选器、自检和其它领域服务差异为 0 |

## 4. 尚未证明

本记录不是第一层自检或自证。代码发布后仍须由生产代码之外的合法消费者验收方，经正式生产会话和有效租约取得 `const` 实例特征服务，覆盖登记首次与重复、越界拒绝、实例首次与精确重复、同键异义冲突、不同键占用同一宿主定义组合冲突，并在每个非成功分支前后通过正式公开入口比较最终权威状态。

该验收不得导入或调用第一层，也不得解析日志。通过后最多累计证明这个第二层消费者实际使用的节点、关系、值和属性槽创建与精确读取、两个写集原子性、精确重复、同键异义、业务唯一性拒绝和非法请求零变化；不证明修改、退出与历史、关系条件查询、其它消费者或整个第一层完善。
