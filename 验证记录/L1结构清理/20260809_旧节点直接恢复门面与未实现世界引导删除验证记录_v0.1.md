# 旧节点直接恢复门面与未实现世界引导删除验证记录 v0.1

日期：2026-08-09

## 1. 验证对象

- 计划：`L1-LEGACY-NODE-DIRECT-RESTORE-FACADE-CLEANUP` v0.1
- 正式起点：`f21435bf9e6bae2d402cbf8cd409bc79d0dba0c0`
- 计划内容身份：`bac7bfeb95f981ce7d27767e0a533d7f06a1e01a`
- 范围：删除零消费者的节点直接结构恢复服务，以及唯一引用它但固定返回“未实现”的节点直接世界引导服务。

## 2. 实际变更

- 删除 `海中鱼巣/核心/服务.节点直接结构恢复.ixx`。
- 删除 `海中鱼巣/领域/服务.节点直接世界引导.ixx`。
- 从工程和 filters 精确删除上述两个文件的登记；双轴根新增登记仍留在工作区，未纳入本计划。
- 生产源码与工程静态扫描确认两个模块、两个服务类、`恢复安装实例` 和 `确保现实世界已引导` 均为零命中。

## 3. 验证结果

| 验证项 | 结果 |
| --- | --- |
| Debug x64 完整重建 | 通过，生成单一生产程序 |
| Release x64 完整重建 | 通过，生成单一生产程序 |
| Debug MSVC 模块依赖扫描 | 通过，135/135，失败 0，循环 0，外部需求 0 |
| Release MSVC 模块依赖扫描 | 通过，135/135，失败 0，循环 0，外部需求 0 |
| 严格规范检查 | 通过，110/110 |
| 差异空白检查 | 通过 |
| 共享工作区 Debug / Release 正式生产入口 | 均返回 1；共享工作区包含异主双轴根未发布启动草案 |
| 正式起点加本计划精确差异的隔离 Debug / Release 完整重建 | 均通过；临时目录构建各有一条 MSB8029 警告，错误 0 |
| 隔离 Debug / Release 正式生产入口 | 均返回 0 |

隔离根为 `C:\Users\zhchh\AppData\Local\Temp\L1-restore-facade-isolated-f21435bf-20260809`。隔离副本来自正式起点，只叠加本计划两文件删除与两项工程登记删除，并排除异主双轴根草案；没有修改、回退或暂存共享工作区中的异主文件。

## 4. 保留能力与完成边界

第一层中性写集提交、一致当前投影读取、节点直接结构查询和节点直接类型化结构提交仍存在并进入两配置构建。下层持久证据端口、恢复数据操作、执行器恢复事务和仓库恢复候选未修改。

本记录只证明两个顶层零消费者恢复 / 引导死门面物理退出，并证明本次精确差异能够构建和启动。它不证明下层恢复实现、旧节点直接体系或第一至第五层已经清理和完善。
