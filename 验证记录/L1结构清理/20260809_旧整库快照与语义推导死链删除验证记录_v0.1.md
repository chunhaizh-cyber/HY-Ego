# 旧整库快照与语义推导死链删除验证记录 v0.1

日期：2026-08-09

## 1. 验证对象

- 计划：`L1-LEGACY-SNAPSHOT-DEADCHAIN-CLEANUP` v0.1
- 正式起点：`3b6b1944292a2e7f3fe276e36826175b92f662e3`
- 计划内容身份：`67ba59ef439b516917773ed86aa4220e6e11bf18`
- 范围：删除旧 L1 整库快照、旧精确读取公开面，以及只为状态推导、迁移动能、业务比较、提供者见证和方法根解释服务的未接线死链。

## 2. 实际变更

- 删除后继状态规格、状态动态原子发布、方法登记根当前读取共五个源码文件及其工程登记。
- 从核心数据、合同、仓库和服务删除完整快照、旧事实读取请求及旧公开读取入口；保留中性读取合同和仓库内部必要的精确读取。
- 从状态、动态、特征比较和场景结构服务删除相邻前状态选择、后继状态形成、迁移动能形成与读取、运行期业务比较、专属数据结构及无调用者辅助函数。
- 生产源码静态扫描确认旧完整快照、五个删除文件身份、相邻前状态、后继状态规格、迁移动能、状态动态原子发布、方法登记根当前读取及被删运行期比较入口均为零命中。

## 3. 验证结果

| 验证项 | 结果 |
| --- | --- |
| Debug x64 完整重建 | 通过，生成单一生产程序 |
| Release x64 完整重建 | 通过，生成单一生产程序 |
| Debug MSVC 模块依赖扫描 | 通过，137/137，失败 0，循环 0，外部需求 0 |
| Release MSVC 模块依赖扫描 | 通过，137/137，失败 0，循环 0，外部需求 0 |
| 严格规范检查 | 通过，110/110 |
| 差异空白检查 | 通过 |
| 共享工作区 Debug / Release 正式生产入口 | 均返回 1；共享工作区包含异主双轴根未发布启动草案 |
| 正式起点加本计划精确差异的隔离 Debug / Release 完整重建 | 均通过；临时目录构建各有一条 MSB8029 警告，错误 0 |
| 隔离 Debug / Release 正式生产入口 | 均返回 0 |

构建中曾发现两处计划内机械遗漏：动态服务内部命名空间结束括号误删，以及特征比较服务残留的无调用者见证辅助函数。两处均在原计划范围内修正；最终两配置完整重建和隔离正式生产入口通过。

隔离根为 `C:\Users\zhchh\AppData\Local\Temp\L1-legacy-cleanup-isolated-3b6b1944-20260809`。隔离副本来自正式起点，只叠加本计划精确代码差异，并从共享工程登记中排除异主双轴根草案；没有修改、回退或暂存共享工作区中的异主文件。

## 4. 保留能力与完成边界

中性提交、当前节点、当前关系、当前值、当前属性、历史事实、关系条件和一致当前投影读取仍存在并进入两配置构建。

本记录只证明旧整库快照、旧精确读取公开面和具名未接线语义推导死链已物理退出，并证明本次精确差异能够构建和启动。它不证明其它旧结构服务、旧业务写集合同、节点直接旧仓或第一至第五层已经完成清理、重建或整体完善；共享工作区双轴根草案的运行失败也不归入本计划结果。
