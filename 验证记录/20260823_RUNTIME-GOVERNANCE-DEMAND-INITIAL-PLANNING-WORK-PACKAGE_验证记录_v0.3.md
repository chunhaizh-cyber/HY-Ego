# RUNTIME-GOVERNANCE-DEMAND-INITIAL-PLANNING-WORK-PACKAGE 验证记录 v0.3

日期：2026-08-23

## 1. 基线与验证根

- 计划：`RUNTIME-GOVERNANCE-DEMAND-INITIAL-PLANNING-WORK-PACKAGE v0.3`。
- plan blob：`56ec429ad18cf18e4e2b3e7ffdaede8c52925998`。
- 验证时正式基线：`HEAD == origin/main == 4901d5b1a06d67f4e5a7f304d4e801d949691560`，index 为空。
- 最终隔离根：`D:\TEMP\海中鱼巣\RUNTIME-GOVERNANCE-DEMAND-INITIAL-PLANNING-WORK-PACKAGE\20260823-v03-final-02`。

## 2. 构建与运行证据

| 检查 | 命令/证据 | 结果 |
| --- | --- | --- |
| Debug x64 全新重建 | `MSBuild.exe .\海中鱼巣.slnx /t:Rebuild /m:1 /p:Configuration=Debug /p:Platform=x64 /p:OutDir=<验证根>\Debug\bin\ /p:IntDir=<验证根>\Debug\obj\ /v:minimal` | PASS；退出 0；`Debug/rebuild.stderr.log`为 0 字节 |
| Release x64 全新重建 | 同上，`Configuration=Release`并使用`Release`隔离目录 | PASS；退出 0；`Release/rebuild.stderr.log`为 0 字节 |
| Debug 治理专项 | `<验证根>\Debug\bin\海中鱼巣.exe --test-治理` | PASS；退出 0；约 209.8 秒；`Debug/治理.stderr.log`为 0 字节；stdout 第 1 行为本计划矩阵摘要，第 68 行为`全部通过` |
| Release 治理专项 | `<验证根>\Release\bin\海中鱼巣.exe --test-治理` | PASS；退出 0；约 8.5 秒；`Release/治理.stderr.log`为 0 字节；stdout 第 1 行为本计划矩阵摘要，第 68 行为`全部通过` |

## 3. V01—V15 对账

| 编号 | 结论 | 动态/静态证据与边界 |
| --- | --- | --- |
| V01 | PASS | 动态注入无效合同版本、双键不等、零请求身份、零运行代次、零需求和零 G0；均`入口拒绝`，前后事实代次不变。 |
| V02 | PASS | 合法未满足且无当前任务/记录时返回`已形成`；任务、虚拟存在、首次记录、七关系和三值由同一融合写发布，记录和工作包回显同一 G1，且 G1>G0、轮次=1。 |
| V03 | PASS | 原键原完整请求立即重放返回`精确重复`，工作包与首次结果逐字段一致。 |
| V04 | PASS + 2 子项 NOT_RUN | 请求身份、运行代次、G0、需求四类仍合法异义逐项返回`幂等冲突`。合同版本单字段变化、任务建立键单字段变化会先违反生产入口守卫，按 v0.3 归 V01；两项不得绕过守卫，记 NOT_RUN。 |
| V05 | PASS | 按准备键、按记录身份当前读、按记录身份 G1 历史读三路同源；任务退出、新当前任务和后继特征值发布后再次三路读回仍为首次记录，无混代。 |
| V06 | PASS | 两个不同准备键从同 G0 并发同列表项，恰一 provider 成功；当前任务唯一，两个准备键合计仅一个首次记录。 |
| V07 | PASS + 许可子项 NOT_RUN | 已满足、证据不足、不可比较动态返回各自具名状态且零成功载荷。当前普通应用没有合法许可拒绝 seam，许可子项记 NOT_RUN；映射控制流保持`许可拒绝`。 |
| V08 | PASS | 新准备键使用旧 G0，前置记录未找到后返回`当前性漂移`，不刷新、不重试、不写。 |
| V09 | NOT_RUN | 正式 API 无法合法制造 G1 后记录/任务/虚拟存在/来源/裁决材料缺失或复义；读回函数对缺失、复义、G0/G1 不一致均收口`内部不一致/内部错误`，未增加破坏 seam。 |
| V10 | PASS | 首次任务退出、同列表项新当前任务、首次特征值退出并发布后继值 55 后，原请求仍返回首次任务和首次 G0 裁决快照；不复活、不替换、不重裁决。 |
| V11 | PASS | 原`新增任务`和融合 provider 从同 G0 并发同列表项，恰一成功；融合成功当且仅当其准备键能读到首次记录，不形成无记录融合成功。 |
| V12 | NOT_RUN | 无合法 seam 注入“提交已发生但调用方未知”；原键重放和按首次写入材料收敛路径由 V03/V05 动态覆盖，源码只允许原键原请求前置读回，未伪造发布未知。 |
| V13 | PASS | 动态工作包/记录完整性通过；静态确认仅含首次请求、裁决、任务来源、记录、轮次和 G0/G1，无路径、实例、执行结果、重筹办或优先级字段；生产外层禁项扫描零命中。 |
| V14 | PASS | final-02 双配置全新重建和双配置`--test-治理`均退出 0，四个 stderr 文件均为 0 字节。 |
| V15 | NOT_RUN | 普通应用无合法资源失败、许可拒绝或内部破坏 seam；`bad_alloc/length_error`映射资源失败，其它异常映射内部错误，未增加测试专用生产入口。 |

动态 PASS 不把 NOT_RUN 子项升级为已运行；NOT_RUN 不证明相应故障注入路径已被运行验证。

## 4. 治理、编码与静态检查

| 检查 | 结果 |
| --- | --- |
| `git diff --check`（本计划七个代码/工程文件） | PASS；只有 Git 行尾转换 warning，无空白错误 |
| `python .\tools\check_specs.py --strict` | PASS；`116`份目录项全部通过 |
| UTF-8 严格解码 | PASS；两个工程文件、治理专项、两个 task-owner 文件和两个内部治理新文件全部通过 `.NET UTF8Encoding(false,true)` 解码 |
| provider 旧入口守恒 | PASS；`任务结构服务_.新增任务(`零命中，只有融合入口调用 |
| 生产外层禁项 | PASS；父需求、重筹办、成员分配优先级、结果共用/争用、实际结果、任务方法路径、实例方法零命中 |
| 工程登记 | PASS；新数据头和 provider 模块在`.vcxproj/.filters`各登记一次，双配置构建已消费 |

## 5. 未证明边界

本记录只验证：合法未满足需求能够原子形成唯一任务、任务虚拟存在和可恢复的首次准备记录，随后形成轮次 1 工作包；原键原请求在后继变化后仍返回首次 G0/G1 载荷。

本记录不证明普通筹办已消费工作包，不证明方法候选、路径、实例、冻结、执行、父回流、重筹办、成员结算、结果共用/争用或成员分配优先级已经实现，也不证明 HY-Ego 最小生命闭环完成。记录形成时尚未提交、推送或执行发布后集成验收。
