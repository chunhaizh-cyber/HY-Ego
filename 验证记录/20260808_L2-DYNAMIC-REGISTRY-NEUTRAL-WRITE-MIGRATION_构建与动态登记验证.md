# L2-DYNAMIC-REGISTRY-NEUTRAL-WRITE-MIGRATION 构建与动态登记验证

日期：2026-08-08

基线：`7ee147e28ce476629de8b9a6389609ad6e42d0e7`

计划：`L2-DYNAMIC-REGISTRY-NEUTRAL-WRITE-MIGRATION v0.2`

plan blob：`bcdc0f1727d67e71da7f96b0b7db0423064947d7`

## 1. 完整构建

使用 Visual Studio MSBuild 18.5.4，显式把 `IntDir` 和 `OutDir` 指向系统临时目录，未使用异主 `.codex-build`。

| 配置 | 目标 | 结果 | 诊断 |
| --- | --- | --- | --- |
| Debug x64 | `Rebuild` | PASS | 0 错误；1 个 MSB8029 临时目录不适合增量构建警告 |
| Release x64 | `Rebuild` | PASS | 0 错误；1 个 MSB8029 临时目录不适合增量构建警告 |

两个配置均生成 `海中鱼巣.exe` 到各自系统临时目录。没有运行二进制，构建结果不得升级为真实业务运行证据。

## 2. MSVC 模块扫描

Windows PowerShell 5.1 首次调用在 `cl.exe` 输出普通 stderr 进度行时，被脚本内 `ErrorActionPreference=Stop` 提升为 `NativeCommandError`，未形成候选结果。未修改扫描脚本，改由 PowerShell 7 执行同一脚本与同一项目 / 配置 / 平台参数后通过：

| 配置 | sources | modules | failed | 拓扑 | 循环 | 外部需求 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| Debug x64 | 136 | 136 | 0 | 136 | 0 | 0 |
| Release x64 | 136 | 136 | 0 | 136 | 0 | 0 |

候选 JSON 位于系统临时目录，不写正式图谱输出。

## 3. strict 与差异检查

- `python .\tools\check_specs.py --strict`：PASS，110 份目录项全部通过。
- 四个白名单路径 `git diff --check`：PASS。
- 被排除的 `形成状态迁移动能规格` 至 `private:` 前完整状态动态区与起点 HEAD 逐字相同：PASS。
- 数据头相对起点只删除三项登记业务常量，公开请求 / 结果 / 登记 DTO 不变：PASS。

## 4. 静态门禁计数

| 门禁 | 新计数 / 结果 |
| --- | ---: |
| 数据头 `L1动态登记操作标签` | 0 |
| 数据头 `L1动态登记意图组` | 0 |
| 数据头 `L1动态登记入口变体` | 0 |
| 数据头 `L1动态登记幂等域 = 0x0F` | 1 |
| 全文件 `首次登记请求_` | 0 |
| 建立登记内 `探测幂等` | 0 |
| 建立登记内 `读取完整快照` | 0 |
| 建立登记内 legacy `提交写集(` | 0 |
| 建立登记内 `L1写集请求` | 0 |
| 建立登记内 `L1领域意图凭证` | 0 |
| 建立登记内 `提交中性写集` | 1 |
| 建立登记固定节点新建项 | 10 |
| 建立登记关系 / 值 / 槽变更 / 退出赋值 | `0 / 0 / 0 / 0` |
| 首次成功分支 P 一致性核对 | 1 |
| 共享读回 `读取中性当前节点` 物理调用点 | 1 |
| 共享读回 `读取中性当前事实代次` 物理调用点 | 2 |
| `读取当前登记` legacy 当前节点读取 | 0 |
| `读取当前登记` 直接返回 `登记_` 成功 | 0 |
| 动态服务全文件 `探测幂等` | 0 |
| 动态服务全文件 legacy `提交写集(` | 0 |
| 动态服务全文件 `读取完整快照` | 4，全部位于明确排除的状态动态路径 |

控制流复核：中性提交调用点位于预读成功之后；提交前不存在 `P != 请求.期望事实代次` 返回。首次成功才核对 P 一致性；精确重复分支不执行该阻断。写集原样保留公开请求的期望代次，同键只更换代次由 L1 的完整请求幂等裁决为冲突。

## 5. 未运行与未证明

本计划禁止把构建或专项自检升级为真实消费者运行，因此未运行生产程序、真实 L2 消费者或集成验收。首次成功、原请求在代次推进后的精确重复、同键更换代次冲突、新键旧期望漂移均由实现控制流、L1 既有顺序和静态证据闭合，没有伪造运行结果。P 相等后提交前竞争推进也未确定性运行，只保留 L1 独占许可内最终代次裁决的代码证据。
