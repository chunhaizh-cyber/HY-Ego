# L1-OWNER-P1 L1所有者范围提供者与存在结构代表迁移验证记录

## 1. 验证对象

- 计划：`L1-OWNER-P1 v0.1`
- 起点：`2890fbc0a8dc5b01a580f4f93fdb093b1d96c53c`
- 验证范围：owner v2 公共合同、单仓所有权不变量、运行包/签发器/端口边界、v1 legacy 隔离、owner-aware 读取、真实普通应用存在 provider、场景构造闭包和工程登记。
- 专项驱动：`验证工具/L1所有者范围与存在服务公开工厂参数验证.cpp`，只 import 公开服务、存在服务与普通应用装配；专项工程虽为链接而编译仓库模块，但验证源码不 import 仓库、不访问锁、内部状态或日志。

## 2. 已通过验证

### 2.1 工程与格式

- `git diff --check`：通过；输出仅含其他所有者 WIP 的换行符提示，无 whitespace error。
- `python .\tools\check_specs.py --strict`：通过，111 份目录项一致。
- 根工程、filters 与专项 vcxproj XML 解析：通过。
- 本切片 14 个代码/工程/临时验证文件严格 UTF-8 解码：通过；加入两份记录后同样纳入提交前 UTF-8 复核。

### 2.2 根工程构建

- Debug x64 `Rebuild`：通过，生成 `x64/Debug/海中鱼巣.exe`。
- Release x64 `Rebuild`：通过，生成 `x64/Release/海中鱼巣.exe`。

### 2.3 专项公开黑盒矩阵

- Debug，起始幂等身份 `12001`：最终复跑通过，18 项检查全部 PASS。
- Release，起始幂等身份 `22001`：通过，18 项检查全部 PASS。
- 已覆盖：同实例两个 owner、跨实例建立、单 owner 单活动端口、跨实例绑定拒绝、端口移动源失效、包析构失效、owner-scoped 节点/值/关系写、跨 owner 关系端点和值来源、同数值幂等身份 owner 隔离、owner-aware 当前/关系组/属性/一致投影、v1 legacy owner 与独占隔离、事实清零后 owner 退出、失效端口许可拒绝。
- 已覆盖真实普通应用链：公开工厂、重复取得同一存在服务、当前代次、新增、精确重复、同键异义冲突、当前完整读、退出、当前已退出和带退出代次的历史完整读。
- 编译期断言覆盖：L1 服务不可默认构造、运行包/端口不可复制且可移动、存在服务和普通应用上下文不可默认构造、存在服务不可复制、owner v2 写请求没有可写 owner 字段。

### 2.4 静态闭包

- `L2存在结构服务` 中 v1 `提交中性写集(` 调用：0。
- owner 端口 `提交所有者范围中性写集(` 调用：3。
- 存在服务旧单参数构造：0。
- 场景临时 `L2存在结构服务{L1}` 构造：0。
- L1 核心 `import L2`：0；`friend L2`：0。
- 新模块与新头在根工程和 filters 各登记一次；三个临时验证文件未登记到根工程或 filters。

## 3. 首轮失败与归因

专项 Debug 首轮成功编译链接后有 1 项运行失败：“值所属和类型同 owner 且来源允许跨 owner”。根因为 owner v2 属性槽候选没有进入 `状态完整` 的历史属性索引期望集合。修复 owner-scoped 幂等账候选互证后，Debug 与 Release 最终矩阵均为 PASS。没有通过放宽 owner 合同、建立测试旁路或伪造数据消除失败。

## 4. 未执行与未覆盖

- 固定属性新增/换代的代码路径已迁移并由编译和静态闭包覆盖，但本叶没有正式同 owner 属性类型来源，因此未运行其成功分支。
- 未执行未知发布后的恢复、恢复候选或 owner 重入；本叶没有导出伪重入入口。
- 未验证七族身份来源、场景/特征 owner 写、T1、DYN2、G2、A1、世界树、生产业务闭环或独立集成验收。
- 根工程和专项验证通过不证明上述未覆盖能力已经实现。

## 5. 发布后清理门禁

实现提交必须先包含本次实际运行过的三个临时验证文件。实现提交普通推送并确认后，只删除这三个文件形成独立清理提交；清理后复核 `git diff --check`、strict、XML/filters 登记和三个路径不存在。实现与清理提交的精确哈希、推送结果及剩余 dirty 由最终执行回执给出。
