# L2-FEATURE-COMPARISON-TARGET-CONTRACT-NEUTRAL-CRUD-MIGRATION v0.1 构建与目标判断比较合同验证

日期：2026-08-08

计划身份：`L2-FEATURE-COMPARISON-TARGET-CONTRACT-NEUTRAL-CRUD-MIGRATION`

plan blob：`b706d19f3200fee3639f61598f085a8e80d8d0b9`

施工起点：`dcfebdfd61d70ff62c3b251c90d0165dc851c631`

## 1. 正式执行结果

| 门禁 | 结果 |
| --- | --- |
| Debug x64 串行完整 Rebuild | PASS，退出 0；语义修正后复验编译链接成功；1 条 `MSB8029` 临时输出目录提示 |
| Release x64 串行完整 Rebuild | PASS，退出 0；编译链接成功；1 条 `MSB8029` 临时输出目录提示 |
| Debug MSVC 模块扫描 | PASS，sources/modules=`136/136`、failed=`0`、拓扑 `136`、循环 `0`、外部需求 `0` |
| Release MSVC 模块扫描 | PASS，sources/modules=`136/136`、failed=`0`、拓扑 `136`、循环 `0`、外部需求 `0` |
| `python .\tools\check_specs.py --strict` | PASS，110/110 |
| 三个白名单路径 `git diff --check` | PASS，无 whitespace 错误；仅 Git 行尾策略提示 |
| 验证时 Git index | 空；尚未 stage / commit / push |

构建使用 Visual Studio 18 Community `MSBuild.exe`，配置为 Debug / Release、x64、`Rebuild`、`/m:1`。临时输出根为 `C:\Users\zhchh\AppData\Local\Temp\hzy-l2-feature-comparison-target-neutral-3b7efe31169f4e4cb2ba7ae8a043103d`；扫描 JSON 也只保存在该目录，不是正式图谱产物。没有运行生成的 `海中鱼巣.exe`。

## 2. 精确静态门禁

| 检查 | 结果 |
| --- | --- |
| M02 中性幂等键 | PASS，保留唯一 `0x09` 域并由专用映射形成；M03 `0x0E` 不变 |
| M02 登记根 legacy 退出 | PASS，`0x15000802U`、G8 helper、`探测幂等`、`读取完整快照`、`L1写集请求`、`提交写集` 均为 0 |
| M02 中性提交 | PASS，`提交中性写集` 恰 1 个物理调用点 |
| 固定中性写集 | PASS，`1/8/1/1/0`；配置材料精确 20 项，关系角色精确 1—8，本地键 1—10 |
| 提交前代次边界 | PASS，G0 只闭合输入束；`输入代次 != 请求.期望事实代次` 仅在中性提交后的首次成功核对中出现 |
| 成功类结果 | PASS，合同版本、回显键、非零事实代次、十映射、首次 / 重复路径、发布标志和重试边界均显式核对 |
| 当前精确读回 | PASS，M01 当前登记 + G0 + 注册节点 / 配置值 / 当前属性 / 八关系源组 / 八目标节点 / 定义三值 + G1 闭合 |
| 缓存边界 | PASS，`合同请求_` / `合同_` 成组；登记和读取均不直接以缓存形成成功 |
| M02 串行与锁序 | PASS，两条公开根均先持有 `目标判断合同锁_`，只短暂取得现有 `锁_`；未发现反向取锁 |
| 排除同一性 | PASS，M01、M03 与运行比较公开段、`解析合同`、`解析迁移合同` 及后续排除私有段相对起点逐字相同 |
| 目标外差异 | PASS，本轮新增差异只在三个白名单路径；异主 dirty 未纳入判断 |

静态检查还确认：中性关系源组恰八项、角色 1—8 唯一且目标精确；注册节点、配置值和八关系共同创建代次一致；首次成功与精确重复都执行权威读回后才更新缓存。全服务文件保留的 legacy G8、探测、完整快照和 `提交写集` 只位于明确排除且逐字未改的 M03 或运行比较路径，未被误报为全文件归零。

## 3. 分支证据与逐项未运行声明

| 场景 | 本轮真实业务运行覆盖 |
| --- | --- |
| 首次合法登记 | 未运行 |
| 原请求在后继代次精确重复 | 未运行 |
| 同键更换完整业务请求字段的异义冲突 | 未运行 |
| 新键旧期望 / 提交竞争 | 未运行 |
| 同义并发竞争 | 未运行 |
| 节点、值、属性、关系组或定义三值损坏 | 未运行 |

源码和冻结 L1 顺序可审计上述分支的候选控制流，但本计划禁止新增专项 L1 测试、外部补格驱动、双目标、生产验收运行器或日志 PASS 判定，现有合法业务入口也未由本轮运行。因此构建、静态顺序、模块扫描和日志都不得升级为运行期业务覆盖或第一层证明。

## 4. 范围与声明边界

计划结果只含一个生产代码文件和本施工 / 验证记录。P0-FIX、路线输入、异主 `.codex-build/`、两份 WORLD-TREE 计划 WIP 及其它 dirty 均未纳入目标 diff 或验证结论。

允许声明：M02 目标判断比较合同登记与合法当前读取的中性 CRUD 代码候选已按冻结形状和顺序实现，并通过双配置完整重建、双配置模块扫描及静态合同核验。

禁止声明：真实消费者或生产接线已经运行通过；M03、运行比较、状态动态联合发布、其它 legacy 或核心 L1 完成迁移；集成验收通过；第一层已完善；整个特征比较或项目闭环完成。
