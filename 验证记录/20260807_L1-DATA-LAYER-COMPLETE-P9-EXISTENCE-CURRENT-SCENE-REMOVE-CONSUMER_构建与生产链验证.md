# 实际存在当前场景移除真实消费者构建与生产链验证

日期：2026-08-07

## 1. 验证对象与结论层级

- 起点：`7ca719b64b3b4258e7dcc53891c5abcb9b54a204`
- 计划 blob：`dfafa3c37f7c92252e4787cda41ded733c440072`
- 对象：场景结构服务的一条直接存在成员关系退出，以及实际存在场景组合服务的合法第二层编排候选。
- 结论层级：施工候选构建、生产启动和静态门禁通过；合法消费者实际运行验收尚未执行。

## 2. 已执行验证

| 验证 | 结果 |
| --- | --- |
| 第一次 Debug x64 串行完整重建 | 未通过；组合服务使用空可选值常量但缺少标准可选值头，共 4 条同根编译诊断 |
| 修复后 Debug x64 串行完整重建 | 通过；模块扫描、编译和链接完成，生成正式生产程序 |
| Release x64 串行完整重建 | 通过；模块扫描、编译和链接完成，生成正式生产程序 |
| Debug 正式 `--runtime-context` | 退出码 0 |
| Release 正式 `--runtime-context` | 退出码 0 |
| Debug MSVC 模块依赖扫描 | 136 / 136，失败 0，循环 0，外部缺失 0 |
| Release MSVC 模块依赖扫描 | 136 / 136，失败 0，循环 0，外部缺失 0 |
| 正式规范严格检查 | 110 / 110 通过 |
| 目标差异空白检查 | 通过，无 whitespace 错误 |

两份模块扫描候选输出写入系统临时目录，没有写入或覆盖工作区现有 `.codex-build`。构建、模块扫描和生产入口退出码只证明代码候选能够编译、链接并保持现有生产启动路径；生产入口本次没有执行新的关系退出业务，日志和退出码不承担业务验收或第一层完成判定。

## 3. 静态合同复核

| 门禁 | 实测 |
| --- | --- |
| 组合服务第一层直接依赖 | 第一层事实基座类型、中性写集、中性读取和中性幂等类型命中均为 0 |
| 场景退出根中性提交 | `提交中性写集` 恰一个物理调用点 |
| 中性写集形状 | 0 节点 + 0 新关系 + 0 值 + 0 属性槽 + 1 旧关系退出 |
| 组合移除根 | 场景退出入口恰一次调用；只使用三个具名领域服务完成写前、委托和写后互证 |
| 领域或组合旁路 | 幂等路由、请求缓存、写集缓存和结果缓存均为 0 |
| 旧复杂提交路径 | 旧 `提交写集`、`探测幂等` 和完整快照读取均为 0 |
| 场景服务合同位置 | 新场景请求和结果只声明在既有场景服务模块；场景数据头差异为 0 |
| 关系退出幂等域 | 只由本场景退出入口使用，目标设计、计划和索引之外无其它占用 |
| 运行期公开边界 | 只新增只读组合服务访问器；没有新增可写访问器 |

## 4. 尚未证明

本记录不是第一层自检或自证。代码精确发布后，仍须由生产代码之外的合法后续层消费者验收方，经正式生产会话和有效租约取得只读实际存在场景组合服务，实际完成创建、移除和正式领域未找到读回，并覆盖精确重复、同键异义、错误关系、错误代次和无效请求。

消费者应只根据正式公开入口的结构化结果和权威状态读回判定；不得导入或调用第一层，不得解析日志，也不得使用双目标验收。该消费者通过后，最多累计证明其实际使用的一条关系退出、退出后当前不可读、历史可回查、精确重复、同键异义和适用非法请求零变化；不证明关系替换、新关系发布、条件关系集合查询、存在删除、崩溃恢复、跨进程恢复、其它消费者或整个第一层完善。
