# 实际存在资格当前读取一致投影构建与生产消费验证

日期：2026-08-08

计划：`L2-EXISTENCE-QUALIFICATION-CURRENT-READ-CONSISTENT-PROJECTION-MIGRATION v0.2`

plan blob：`fb6ed73133876fe5f1c63940ee50851fbe87c7d4`

设计提交：`82a074a8733aaeaee3894a66a470274a78f2afb1`

施工起点：`399bb34ee269715c71a8d25e50f06dde4cef8d56`

## 1. 构建和模块结果

| 门禁 | 结果 |
| --- | --- |
| Debug x64 完整 Rebuild | 通过，退出 0；零编译错误，一条临时目录提示 |
| Release x64 完整 Rebuild | 通过，退出 0；零编译错误，一条临时目录提示 |
| Debug 模块扫描 | sources / modules=`136 / 136`、failed=`0`、循环=`0`、外部需求=`0` |
| Release 模块扫描 | sources / modules=`136 / 136`、failed=`0`、循环=`0`、外部需求=`0` |
| Debug 正式 `--runtime-context` | 自行退出 0；stdout / stderr=`0 / 0` 字节 |
| Release 正式 `--runtime-context` | 自行退出 0；stdout / stderr=`0 / 0` 字节 |
| 正式规范 strict | 110 / 110 |
| 三个允许路径差异检查 | 通过，无 whitespace 错误 |

构建使用 Visual Studio 18 Community、x64、完整 Rebuild、串行构建和系统临时输出目录。模块扫描只写系统临时材料，不更新正式代码图谱或仓库 `.codex-build`。

## 2. 静态合同检查

| 检查 | 结果 |
| --- | --- |
| 资格根一致投影调用 | 物理调用点恰 1；逻辑最多完整尝试 2 次 |
| 旧分散读取 | 当前事实代次、当前节点、当前属性、当前值、当前关系和完整快照调用均为 0 |
| 请求形状 | 无显式值为 `3/0/0/1/0/0`；有显式值为 `3/0/1/1/0/0` |
| 请求顺序 | 实际存在、资格属性类型、登记服务来源；属性查询为实际存在 × 资格属性类型 |
| 属性值处理 | 无显式值直接采用投影当前值；有显式值时同轮独立值事实与投影当前值逐字段全等 |
| 漂移 | 首轮合法漂移完整重做一次；第二轮漂移返回版本漂移；无第三次调用 |
| 许可与资源 | 许可拒绝不等待不重试；资源失败独立分账 |
| 项目失败 | 内部错误优先；节点 / 显式值仅未找到映射未找到，属性未找到 / 未设置映射未找到；已退出 / 种类不匹配 / 非法项目状态映射内部不一致 |
| 显式值语义 | 编码不存在返回未找到；已退出、非当前值、查询编码或完整事实不等返回内部不一致 |
| 成功形状 | 顶层合同、共同代次、载荷形状、节点、资格槽、当前值、来源、材料和生命周期逐项核对 |
| 返回副作用 | 成功只形成值式资格事实；非成功为空事实；目标根零写入、零缓存修改 |
| 公开合同 | 实际存在数据头、两个公开资格入口和生产调用者零差异 |
| 登记读取 | 只机械更新共用 helper 名称，固定两节点一致读取逻辑不变 |

## 3. 正式生产入口与未完成验收

Debug 与 Release 新构建程序均通过现有正式运行期上下文入口并自行退出零。该生产链会初始化世界登记、初始场景、实际存在登记和初始场景实际存在；实际存在资格读取是后续原子发布读回与完整性检查的内部依赖。

本段没有从生产代码外取得有效租约并保存 `读取初始场景实际存在` 的结构化结果，也没有连续两次读回资格、登记、场景和世界进行逐字段比较。生产退出码和空 stdout / stderr 不能替代这些结果。

因此：

- 允许声明资格读取候选已经迁移到业务中性一致当前投影，并通过构建、模块、静态和正式入口运行门禁；
- 不允许声明初始场景实际存在消费者服务验收通过；
- 不允许声明真实许可竞争、第二轮漂移、其它失败分支、其它消费者或第一层整体完善已得到运行证明。

后续独立验收只能绑定最终已提交候选，通过合法第二层消费者的正式公开入口运行；不得直调第一层或为补覆盖增加请求。
