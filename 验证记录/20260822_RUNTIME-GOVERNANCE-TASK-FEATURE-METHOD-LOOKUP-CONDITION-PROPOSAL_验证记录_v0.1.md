# 任务特征方法查找与条件提议验证记录

计划身份：`RUNTIME-GOVERNANCE-TASK-FEATURE-METHOD-LOOKUP-CONDITION-PROPOSAL v0.5`

日期：2026-08-22

验证基线：`main@6b5f6b4dcdc6f3e923c4d01283b45e41522ae7d1`加本计划允许切片；support=`c3f015e6be9ba59710459ef0b85d4e6498add0f6`；plan blob=`f55e095eda2cd049892d48a79359fa3cf97ac1bf`；代码与本记录同提交，精确哈希由 Git 历史记录。

## 1. 证据口径

本记录区分：

- 动态 PASS：Debug / Release 产物经公开 DATA / 普通应用 getter / 真实 provider 执行并完成断言；
- 源码静态证明：生产入口、控制分支、状态映射、catch 与结果守恒可直接穷尽，但没有把它写成动态 PASS；
- NOT_RUN：生产合同不允许自然构造，或没有不扩张 ABI / 不增加生产 seam 的合法注入路径；
- 测试输出：只标识断言组完成，不单独升级为机器事实。

没有增加计数器、故障钩子、第二事实入口、私有容器注入或手工伪造工作包来把不可达分支升级为 PASS。

## 2. 构建与静态门禁

隔离根：`D:\TEMP\海中鱼巣\RUNTIME-GOVERNANCE-TASK-FEATURE-METHOD-LOOKUP-CONDITION-PROPOSAL\20260822-v05-final`

| 验证项 | 命令 / 材料 | 结论 |
| --- | --- | --- |
| Debug x64 全量重建 | `msbuild .\海中鱼巣.slnx /t:Rebuild /m:1 /p:Configuration=Debug /p:Platform=x64`，OutDir / IntDir 指向隔离 Debug 目录 | PASS；退出码 0 |
| Debug 需求专项 | Debug `海中鱼巣.exe --test-需求` | PASS；`DEBUG_DEMAND_EXIT_CODE=0` |
| Debug 治理专项 | Debug `海中鱼巣.exe --test-治理` | PASS；`DEBUG_GOVERNANCE_EXIT_CODE=0` |
| Release x64 全量重建 | 同命令切换 Release 并写隔离 Release 目录 | PASS；`RELEASE_REBUILD_EXIT_CODE=0`；0 错误；清理阶段有 1 条`vc145.pdb`占用警告，随后完成全量编译和链接 |
| Release 需求专项 | Release `海中鱼巣.exe --test-需求` | PASS；`RELEASE_DEMAND_EXIT_CODE=0` |
| Release 治理专项 | Release `海中鱼巣.exe --test-治理` | PASS；`RELEASE_GOVERNANCE_EXIT_CODE=0` |
| 正式规范目录 | `python .\tools\check_specs.py --strict` | PASS；116/116，退出码 0 |
| 旧 ABI 扫描 | 精确搜索七项旧类型 / 函数 / provider | PASS；`OLD_ABI_SCAN=ZERO_HIT` |
| 新 ABI / provider / getter | 精确搜索六项新类型、函数、provider、根函数与 getter | PASS；均命中定义 / 登记 / 调用 |
| 已跟踪白名单差异 | `git diff --check -- <精确白名单>` | PASS；仅 Git 换行提示 |
| 暂存差异 | 发布租约取得后精确暂存 13 路径并执行`git diff --cached --check` | PASS；退出码 0；7 修改、2 删除、4 新增 |

两次短时限终端调用被工具超时中断，没有据此形成构建或测试结论；表中结果均来自随后完整重跑。

## 3. DATA-L2 v3 验证矩阵

| 验证项 | 证据 | 结论 |
| --- | --- | --- |
| 合同 v3 与旧 v2 拒绝 | 需求专项写入 / 读取 v3；错版本请求结构化拒绝 | PASS |
| 零 / 一 / 多方法 | 按同目标特征读取空组、单方法和多方法 | PASS |
| 同方法多用途 | 全部命中用途返回，同方法只消耗一次候选预算 | PASS |
| 稳定顺序 | 结果按方法身份、用途身份严格升序，结果守恒通过 | PASS |
| DATA 不提前做适用判断 | 同特征而对象 / 方向 / 结果不同的用途均进入粗召回 | PASS |
| 当前 / 历史 | 替换、退出后当前不可见，旧截止历史仍可读 | PASS |
| 用途扫描预算 | 预算不足返回`数量预算不足`且候选为空 | PASS |
| 方法候选预算 | 预算不足返回`数量预算不足`且候选为空 | PASS |
| 旧 ABI 退出 | 七项精确扫描零命中，旧两文件和工程登记删除 | PASS |

## 4. BIZ-L3 逻辑与载荷矩阵

| 验证项 | 公开结果 / 事实证据 | 结论 |
| --- | --- | --- |
| 请求合同、工作包、八预算、G 零值 | 入口拒绝且零业务载荷 | PASS |
| 工作包身份与共同截止 | 已评估结果工作包等于请求，共同截止等于请求期望代次和迁移后代次 | PASS |
| 任务真实需求来源 | 经任务→需求列表项读取目标，不消费任务预造条件 | PASS |
| 旧实际状态链 | 旧实际结果→完整状态→特征实例读回互证 | PASS |
| 治理状态与正式裁决 | `目标未达成待重筹办`与未达成裁决互证 | PASS |
| 目标低于 / 高于 | 所需方向分别形成且与允许位一致 | PASS |
| 达到、仅低 / 仅高 / 双向 | 三种方向分流覆盖 | PASS |
| 九种合法结论 | required / forbidden 载荷与`成功()`守恒逐项动态验证 | PASS |
| 粗召回空 | `方法能力缺失`，保留空完整粗召回 | PASS |
| 零适用且有结构缺口 | `方法结构待补齐`，保留缺口 | PASS |
| 零适用且无结构缺口 | `当前无适用方法`，保留完整粗召回与不兼容证据 | PASS |
| 单适用 | 唯一采用方案进入条件 / 输入 / 限制处理 | PASS |
| 多适用 | `多候选待选择证据`，不采用、零条件草案 | PASS |
| 对象类型不兼容 | 四类任务不兼容证据之一 | PASS |
| 所需方向不兼容 | 四类任务不兼容证据之一 | PASS |
| 动态概念结果 | 不冒充目标状态合同 | PASS |
| 结果种类 / 目标合同不兼容 | 两类结果不兼容证据分别覆盖 | PASS |
| 用途 / 结果角色冲突 | 投影结构缺口被保留，不生成已删除原因 | PASS |
| provider 零写 | 调用前后事实代次一致；不写任务、需求、路径、实例或学习事实 | PASS |
| getter 唯一同实例 | 同一普通应用两次 getter 地址一致，测试只经 getter 消费 | PASS |
| G 后变化 | 目标事实变化后旧请求返回`当前性漂移`且零载荷 | PASS |

## 5. 概念、条件、绑定与预算矩阵

| 验证项 | 证据 | 结论 |
| --- | --- | --- |
| 对象零概念 | 不猜默认，形成`对象概念待形成` | PASS |
| 存在根直达 | 不读取普通概念，根直接进入闭包 | PASS |
| 普通概念 | 根祖先闭包完整 | PASS |
| 多概念 | 保留全部合法对象概念与适用候选 | PASS |
| 两级祖先 | 祖先链闭包完整 | PASS |
| 错根 | 返回结构化引用冲突 / 非成功且零载荷 | PASS |
| 概念支持扫描 / 数量 / 祖先预算 | 分别触发`数量预算不足` | PASS |
| 条件特征零匹配 | 合法进入未满足 / 缺失条件草案分支 | PASS |
| 条件恰一满足 / 不满足 | 两分支动态覆盖 | PASS |
| 同特征多当前状态 | 冲突按结构化状态收口 | PASS |
| 场景不满足 | 条件未满足并形成草案 | PASS |
| 角色未绑定 | 形成具名未绑定证据；已绑定组为空，动态结果不读取状态材料 | PASS（结果语义，不作为调用次数证据） |
| 多已绑定条件 | 共同消费一组同 G 状态材料，结果语义复用 | PASS（结果语义，不作为调用次数证据） |
| 输入规格 / 限制绑定 | 角色相等时绑定目标宿主，身份 / 语义保持 | PASS |
| 输入规格 / 限制未绑定 | 角色不等时形成对应类别具名证据 | PASS |
| 场景成员结果上限 | 超限整项`数量预算不足`；不宣称底层扫描有预算 | PASS |
| 方法关联材料结果上限 | 超限整项`数量预算不足`；不扩张方法读取 ABI | PASS |
| 八预算零值 | 八个入口字段逐项拒绝 | PASS |
| 方法用途扫描、候选、概念支持扫描 / 数量、祖先、主体状态四类动态预算 | 对应预算不足均零业务载荷 | PASS |

## 6. v0.5 A1—A6 静态证明与 NOT_RUN

| 编号 | 验证对象 | 源码静态证明 | 动态结论 |
| --- | --- | --- | --- |
| A1 | 当前支持关系指向已退出普通概念 | 普通概念退出入口在`服务.L2概念结构.ixx`先读取当前支持组并要求为空（约 4431—4438）；provider 对概念读取非成功 / 退出状态继续防御性收口（新 provider 约 493—501） | NOT_RUN；合法公开生产链不能构造 |
| A2 | 当前概念图成环 | `当前概念图完整`使用三色访问拒绝环（约 3431—3503），关系写 / 替换提交前再次调用完整图校验（约 5874、5998）；provider 保留祖先遍历防御 | NOT_RUN；合法写入口提交前拒绝 |
| A3 | 同 G 成功空主体状态组 | 已绑定条件组非空才调用状态组，成功后复用返回组（新 provider 约 671—694）；合法已绑定条件的主体来自正式目标宿主当前实际状态链 | NOT_RUN；未建立伪造空组夹具 |
| A4 | 固定 G 目标引用缺失 / 退出 / 属性未设置或重算矛盾 | 目标状态与特征实例读取分支把固定 G 的缺失 / 退出 / 属性未设置收口为`引用冲突`（约 409—427）；比较合同与正式裁决 / 允许位互证，重算矛盾同样非成功零载荷（约 219—287、437—460） | NOT_RUN；无合法公开注入路径 |
| A5 | 资源异常与完全初始化七服务下内部状态注入 | 15 状态 switch 显式映射且各有`static_assert`（约 110—166）；`bad_alloc/length_error`catch 映射资源失败，其它 catch 映射内部错误（约 753—758）；非成功由`失败`统一形成零载荷 | NOT_RUN；未增加生产故障 seam |
| A6 | 主体状态组零 / 一次调用 | 全模块只有一个`按主体读取全部当前状态`调用表达式；它位于`!已绑定条件.empty()`分支内（约 685—692）且在随后条件循环（约 694）之前 | 静态证明成立；动态只 PASS 多条件结果复用语义，不宣称调用计数 PASS |

三项 v0.5 增量断言已存在：`已提交 -> 内部错误`、`精确重复 -> 内部错误`、`已读取 -> 内部错误`（新 provider 约 161—166）。

## 7. 状态、异常与非成功分账

| 验证项 | 结论 |
| --- | --- |
| `数量预算不足`、`事实代次漂移`、`引用冲突`、`幂等冲突`、`入口拒绝`等具有公开路径的状态 | PASS；专项动态覆盖对应入口，非成功零载荷 |
| 15 个`L2结构状态`显式映射 | PASS（源码静态证明）；每项有`static_assert` |
| `bad_alloc/length_error`真实分配失败 | NOT_RUN；catch 静态证明，未增加故障钩子 |
| 完全初始化七服务下`未实现/内部不一致/许可拒绝`等强制注入 | NOT_RUN；无合法生产 seam |
| 成功空主体状态组 | NOT_RUN；合法工作包链不能自然构造 |
| 固定 G 引用 / 重算防御矛盾 | NOT_RUN；合法公开写入链原子阻止 |

本次最终矩阵没有 `FAIL` 项。`NOT_RUN` 项没有被静态证明或测试输出冒充为动态 PASS。

## 8. 完成声明边界

本记录证明：按目标特征粗召回 v3、任务特征方法查找九结论、唯一采用后条件提议、四类不兼容、结构缺口、八预算、共同截止、零写和普通应用唯一 getter 在 Debug / Release 专项下成立；计划要求的不可合法注入分支具有相应源码防御和明确`NOT_RUN`分账。

本记录不证明条件草案已写入需求树，不证明输入规格 / 限制已经成为事实，不证明方法当前可执行，也未执行任务线程接线、路径选择、实例方法、执行冻结、动作执行、任务完成、学习需求生产、价值回流、跨进程恢复或系统业务闭环。

发布状态：本记录随精确 13 路径结果提交；提交 / 推送只证明本切片进入 Git，不升级业务完成声明。
