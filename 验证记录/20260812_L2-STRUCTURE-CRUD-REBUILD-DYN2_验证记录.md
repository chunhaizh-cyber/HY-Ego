# L2-STRUCTURE-CRUD-REBUILD-DYN2 验证记录

日期：2026-08-12

## 验证基线

- 计划：`L2-STRUCTURE-CRUD-REBUILD-DYN2` v0.5
- 起点：`71c985eb6b784078e461a91917dd3bfd2b523ea2`
- plan blob：`993ea2a012a88dfc75a7ed53ff38c4d6c2cf70f7`

## 运行结果

| 项目 | 结果 |
| --- | --- |
| DYN2 临时专项 Debug x64 | PASS，20/20 |
| DYN2 临时专项 Release x64 | PASS，20/20 |
| 根工程 Debug x64 Rebuild | PASS |
| 根工程 Release x64 Rebuild | PASS |
| `python .\tools\check_specs.py --strict` | PASS，113/113 |
| 七路径 `git diff --check` | PASS |
| 七路径严格 UTF-8 | PASS |
| 临时 vcxproj XML 解析 | PASS |

专项实际覆盖同一普通应用上下文四服务、E-ID 存在、F-ID 定义/实例及来源、T1/T2 状态及来源、DYN1 动态新增/来源、三个单角色引用替换、替换前后历史与条件查询、角色错配/无变化/重复/幂等冲突、起止值换代、`2 角色 x 4 表示`、相同材料新编码、逆序合法、错旧值、六编码闭包漏/错/多/未排序/重复/零/含动态、F-ID 值来源与独立材料引用保护及合法闭合、七事实退出、当前已退出空载荷、退出截止及更晚历史完整读回、退出重复及闭包冲突。

## 并发窗口

固定 `std::barrier`、128 轮、无 sleep。交互核验发现结果提交 `4f1cb1f23954108952d0baccd289d0f202b83462` 的旧专项会在单次资源失败后继续重试，旧“资源失败为 0”结论无效且不接受。后继修复对写者和读者均不重试资源失败：每次单次资源失败立即累计，三组矩阵最终分别强制断言资源失败总数为 0；本次实际资源失败、重试耗尽、混合投影、零截止跳过均为 0。

| 配置 | 引用替换 | 时间值换代 | 退出 |
| --- | --- | --- | --- |
| Debug | 完整窗口 129，许可拒绝 127，资源失败 0，重试耗尽 0，混合 0 | 写成功；完整读取 2，许可拒绝 254，漂移 0，资源失败 0，重试耗尽 0，混合 0 | 写成功；完整读取 2，许可拒绝 254，漂移 0，资源失败 0，重试耗尽 0，混合 0 |
| Release | 完整窗口 130，许可拒绝 126，资源失败 0，重试耗尽 0，混合 0 | 写成功；完整读取 129，许可拒绝 127，漂移 0，资源失败 0，重试耗尽 0，混合 0 | 写成功；完整读取 129，许可拒绝 127，漂移 0，资源失败 0，重试耗尽 0，混合 0 |

三组竞争结束后的最新实际截止静止读回分别收敛为完整新引用、完整新值、`已退出` 空载荷。

## 静态与范围结果

- 生产仍为 30 个 `.ixx`、16 个头；主工程、filters、普通应用无本切片差异，临时专项未登记到主工程。
- `读取当前动态结构类型登记` 及 `替换动态引用`、`换代动态时间值`、`退出动态` 中登记读取后的受影响链路零旧 `读取中性当前事实代次` 探测；三写的首次结果以首次写入代次为历史截止，读回请求头取登记 owner-aware 截止与首次写入代次较大者，后继推进后的精确重复仍重建首次事实。
- 两个生产文件零 F-ID import/调用/服务引用，零验证钩子、第二仓、缓存、索引、SQL、日志、sleep、隐藏重试或阻塞回退。
- 只允许七路径进入结果提交；异主 WIP 不属于本验证结论。

## 未验证和不得升级内容

未运行指向动态的外部关系和以动态为所属节点的闭包外值；二者不登记为本叶 PASS 或 NOT_RUN，等待未来具名合法公共消费者独立集成验收。本记录也不证明生产业务消费者、状态真实性、主体一致性、时间排序、因果、A1、长期公平、真实分配失败、恢复、跨进程或完整 L2。
