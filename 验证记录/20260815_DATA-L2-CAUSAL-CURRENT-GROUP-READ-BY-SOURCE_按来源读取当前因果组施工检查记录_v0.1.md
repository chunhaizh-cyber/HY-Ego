# 按来源读取当前因果组施工检查记录 v0.1

日期：2026-08-15

## 1. 检查对象

- 计划：`DATA-L2-CAUSAL-CURRENT-GROUP-READ-BY-SOURCE` v0.1
- 生产文件：
  - `海中鱼巣/领域/L2因果结构.数据.h`
  - `海中鱼巣/领域/服务.L2因果结构.ixx`
- 检查性质：施工期间风险相称检查，不是独立集成验收，也不替代合法消费者实际使用判断。

## 2. 已运行检查

### 根工程

使用 MSBuild 对 `海中鱼巣.vcxproj` 执行 x64 Debug 与 x64 Release `Rebuild`：

- 临时专项运行前：Debug PASS，Release PASS；
- 删除全部临时验证源码、工程和专属输出后：Debug PASS，Release PASS；
- 两次清理后构建均编译 `服务.L2因果结构.ixx` 并成功生成正式根工程可执行文件。

### 生产外临时公开入口检查

临时工程未登记到生产工程。Debug 与 Release 各运行 13 项，结果均为 `13/13 PASS`：

1. 错合同、零守卫、零来源、零预算分别入口拒绝且零载荷；
2. 当前来源存在但无因果时成功空组；
3. 同来源多因果完整读回、严格升序；
4. 不同来源隔离；
5. 预算等于因果数量成功；
6. 少一预算返回数量预算不足且零载荷；
7. 来源未找到；
8. 来源已退出；
9. 陈旧守卫返回事实代次漂移；
10. 因果退出后不再命中；
11. 已退出因果的既有历史完整读取仍保留；
12. 既有条件状态、动作、结果状态查询保持；
13. 读取前后事实代次不推进。

该专项证明公开入口可构造的正常、拒绝、退出和守卫路径；总数本身不升级为完整业务闭环。

### 静态与清理门禁

- `python .\tools\check_specs.py --strict`：113/113 PASS；
- 全工作区 `git diff --check`：无空白错误，只有异主 WIP 的行尾转换提示；
- 两生产文件定向 `git diff --check`：PASS；
- 两生产文件：UTF-8 可解码且无 BOM；
- 生产差异范围：只包含两个计划白名单生产文件；
- 临时 `.cpp`、`.vcxproj`、`.vcxproj.filters` 与专属输出目录均不存在；
- 专属 exe、pdb、obj、ifc、module.json 等构建产物随输出目录一并删除；
- 本切片未向生产文件增加验证入口、故障 seam 或验证工具依赖。生产服务原有编译期 `static_assert` 不属于本切片新增验证代码。

## 3. NOT_RUN

以下项目没有合法稳定注入条件，且未为其增加生产 seam：

- 真实内存或长度资源耗尽；
- 私有因果 owner、来源索引或因果事实损坏；
- 共享锁长期公平性；
- 跨进程恢复；
- 正式 BIZ 消费和集成验收；
- DATA-EXT-06 其它范围查询及数据服务最终需求清单整体闭环。

## 4. 结论边界

施工检查支持“按来源读取当前因果组 provider 的公开合同已在两个生产文件中实现，根工程双配置可构建，临时检查已清理”。它不支持“全部因果查询完成”“DATA-EXT-06 完成”或“数据服务最终需求清单完成”的结论。
