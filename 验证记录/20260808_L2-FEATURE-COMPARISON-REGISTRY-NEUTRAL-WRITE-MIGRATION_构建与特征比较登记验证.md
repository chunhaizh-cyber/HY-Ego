# L2-FEATURE-COMPARISON-REGISTRY-NEUTRAL-WRITE-MIGRATION v0.1 构建与特征比较登记验证

日期：2026-08-08

计划身份：`L2-FEATURE-COMPARISON-REGISTRY-NEUTRAL-WRITE-MIGRATION`

plan blob：`310f9eacb1fef683acda16a2e868fd2e024d89eb`

施工起点：`0a3464f72ce1263864108871a5759e701840974e`

## 1. 正式执行结果

| 门禁 | 结果 |
| --- | --- |
| Debug x64 串行完整 Rebuild | PASS，退出 0；编译链接成功；1 条 `MSB8029` 临时输出目录提示 |
| Release x64 串行完整 Rebuild | PASS，退出 0；编译链接成功；1 条 `MSB8029` 临时输出目录提示 |
| Debug MSVC 模块扫描 | PASS，sources/modules=`136/136`、failed=`0`、拓扑 `136`、循环 `0`、外部需求 `0` |
| Release MSVC 模块扫描 | PASS，sources/modules=`136/136`、failed=`0`、拓扑 `136`、循环 `0`、外部需求 `0` |
| `python .\tools\check_specs.py --strict` | PASS，110/110 |
| 目标路径 `git diff --check` | PASS，无 whitespace 错误；仅 Git 行尾策略提示 |
| 验证时 Git index | 空；尚未 stage / commit / push |

构建使用 Visual Studio 18 Community `MSBuild.exe`，配置为 Debug / Release、x64、`Rebuild`、`/m:1`。临时输出根为 `C:\Users\zhchh\AppData\Local\Temp\hzy-l2-feature-comparison-registry-neutral-19f49a6820d84970ad4e7d519b26ce8a`；扫描 JSON 也只保存在该目录，不是正式图谱产物。没有运行生成的 `海中鱼巣.exe`。

## 2. 精确静态门禁

| 检查 | 结果 |
| --- | --- |
| M01 `0x08` 幂等域 | PASS，数据头唯一一份定义；服务内同义旧常量删除 |
| M01 专用旧件 | PASS，`形成G8特征比较登记意图凭证`、`0x15000801U`、`登记请求_` 全文件均为 0 |
| `建立登记` 旧路径 | PASS，旧探测、完整快照、legacy 写请求 / 提交、领域意图均为 0 |
| `建立登记` 中性提交 | PASS，`提交中性写集` 恰 1 个物理调用点 |
| 固定中性写集 | PASS，六节点；关系、值、属性槽变更、退出事实均保持空，即 `6/0/0/0/0` |
| P 代次裁决位置 | PASS，提交前无 `P != 请求.期望事实代次` 返回；P 一致性只在首次成功分支核对 |
| 中性成功合同 | PASS，合同、回显键、非零首次发布代次、六映射及首次 / 重复的发布标志和重试边界均显式核对 |
| 共享精确读回 helper | PASS，`读取中性当前节点` 1 个物理循环调用点并循环六编码；`读取中性当前事实代次` 2 个物理调用点 |
| `读取当前登记` | PASS，只提取六编码并调用共享 helper；不直接返回缓存成功，不调用完整快照或写入口 |
| M02 / M03 与目标外公开路径 | PASS，从 M02 起至 `private:` 前相对施工起点逐字一致 |
| 目标外私有解析与运行比较 helper | PASS，从 `解析合同` 起至成员区前相对施工起点逐字一致 |

全服务文件保留的 legacy `探测幂等`、`读取完整快照`、`L1写集请求` 和 `提交写集` 均位于明确排除且未改的 M02/M03 或运行比较路径；`0x15000802U` 与 `0x15000803U` 保持原调用合同。本轮没有把这些保留项误报为 M01 未退出。

## 3. 分支证据与未运行项

源码和冻结 L1 顺序可审计以下分支：首次成功；当前代次推进后的原请求精确重复；同键只改变期望代次的幂等冲突；新键旧期望代次的版本漂移；G0 / 六节点 / G1 混代拒绝。静态证据确认领域层没有改键、改请求或以 P / 缓存提前吸收这些分支，最终幂等和代次裁决仍由现行 L1 完整请求与独占许可内顺序负责。

本轮计划禁止新增专项 L1 测试、测试驱动或覆盖补格，也没有合法现成入口可确定触发提交前竞争。因此上述真实业务分支均未被本轮运行覆盖，只完成编译、模块、规范和静态证据核验。构建日志、扫描 JSON 和代码顺序不得升级为运行期业务事实。

## 4. 范围与声明边界

计划结果只含两份生产代码和本施工 / 验证记录。P0-FIX、路线输入、异主 `.codex-build`、两份 WORLD-TREE 计划 WIP 及其它 dirty 均未纳入目标 diff 或验证结论。

允许声明：特征比较第一结构登记根 M01 的代码候选已按冻结顺序迁移到正式中性提交和中性当前精确读回，并通过双配置完整重建、双配置模块扫描、strict 和静态门禁。

禁止声明：真实消费者或生产接线已经运行通过；M02/M03、运行期比较或状态动态联合发布完成迁移；集成验收通过；核心 L1 已完善；整个特征比较或项目闭环完成。
