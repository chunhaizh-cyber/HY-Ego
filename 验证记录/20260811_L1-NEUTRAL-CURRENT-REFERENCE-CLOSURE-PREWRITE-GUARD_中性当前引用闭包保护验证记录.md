# L1 中性当前引用闭包保护验证记录

日期：2026-08-11

计划：`L1-NEUTRAL-CURRENT-REFERENCE-CLOSURE-PREWRITE-GUARD v0.1`

## 验证基线

- 执行起点：`500869e1678e5b3c4ff1f3b43a48b2f282cd2fbb`
- 配置：Visual Studio / MSBuild 18.5、PlatformToolset `v145`、x64、C++20、Unicode、`/utf-8`
- 外验输出 / 中间目录：系统临时目录，不在工作区生成专项构建物。

## 构建与运行

### 主工程

```powershell
MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /verbosity:minimal
MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Release /p:Platform=x64 /verbosity:minimal
```

结果：最终生产代码下两项均退出码 `0`，分别生成 Debug / Release x64 可执行文件。未运行主程序，因此不把构建结果升级为生产业务闭环证据。

### 生产外公开接口矩阵

```powershell
powershell -ExecutionPolicy Bypass -File .\验证工具\运行L1中性当前引用闭包保护参数验证.ps1 -配置 Debug -起始幂等键 6101
powershell -ExecutionPolicy Bypass -File .\验证工具\运行L1中性当前引用闭包保护参数验证.ps1 -配置 Release -起始幂等键 8101
```

结果：最终代码下两项均退出码 `0`；两种配置各 12 项检查全部 `PASS`，失败数 `0`，最终事实代次均为 `23`。覆盖：

1. 关系源、目标、类型和值所属、属性类型、来源、独立材料引用七类角色逐项返回 `引用冲突`；
2. 七类冲突逐字段回显当前代次、零发布、空映射、修正请求后可重试，权威当前事实和历史读回零变化；
3. 同一目标被多关系、多值和同一事实多角色引用时，部分闭包冲突，完整闭包恰一推进；
4. 完整闭包后目标节点和全部引用关系 / 值同代次退出，其它端点仍当前可读；
5. 无引用节点直接退出、单独关系 / 值退出不要求退出其节点引用；
6. 当前属性值显式退出移除属性槽，历史值不再阻断所属 / 类型 / 来源 / 材料节点；
7. 属性换代后旧值及旧材料引用不再阻断，新值继续阻断直至与目标一起闭合；
8. 新关系、值或属性槽变更指向同请求退出节点均入口拒绝且零变化；
9. 旧期望代次优先返回事实代次漂移，不存在 / 已退出目标优先返回未找到 / 已退出；
10. 完整退出原请求精确重复收敛到首次退出代次，同键改变退出集返回幂等冲突；
11. 自环关系和值四角色同一节点均只形成一个候选，目标与唯一关系 / 值即可完整闭合；
12. 非退出创建、精确重复、同键异义和非法合同保持现行结构化语义，且 `引用冲突` 数值为 10。

专项入口首次编译、链接成功后，在矩阵前置节点建立阶段退出码为 `1`。根因是验证辅助在空仓读取当前事实代次时未把空仓代次明确折叠为 `0`；该失败发生在断言前，未修改生产状态。只在专项验证文件中修正空仓期望代次后，Debug 12/12 通过；最终机械中文变量收口后又完整重跑 Debug / Release 主工程和两配置专项矩阵，以上结果均通过。

两次最终外验构建均出现 MSBuild `MSB8029` 警告：输出 / 中间目录位于系统临时目录，可能影响增量生成。本包装每轮使用新的 GUID 目录并执行 `Rebuild`，本次完整构建、链接和运行均成功；该警告未被当作长期增量构建或性能证据。

## 静态与治理检查

```powershell
python .\tools\check_specs.py --strict
```

结果：退出码 `0`，111 份正式规范目录项全部通过。

严格 UTF-8 解码并解析主工程和专项工程 XML，结果：

- 主工程生产 `.ixx`：26；
- 主工程 `ClInclude`：12；
- 主工程包含 `/utf-8` 的四配置项：4；
- 专项入口登记：1，主工程 / filters 内专项登记：0；
- 专项入口 import 总数：1，唯一 import 为公开 `海中鱼巣.核心.服务.L1事实基座`；
- `引用冲突 = 10`：1；私有三值枚举：1；索引字段：1；闭包函数：1；
- 索引插入关系 / 值重载：2；索引删除关系 / 值重载：2；
- 两个生产文件以外零生产代码差异；服务、合同导出、主工程 / filters、入口、L2 和装配零差异；
- 新增生产代码未命中公开引用列表、新仓库 / 锁 / 缓存 / SQL / 日志 / sleep 等禁止扩展；新增函数和变量的英文标识符扫描通过。

两份记录写入后执行最终七文件 `git diff --check`（五个未跟踪文件分别以 `git diff --no-index --check` 补充）、`git diff --cached --check`、严格 UTF-8 解码和 Git 事实复核，均通过。`main` / HEAD / `origin/main` 仍为起点 `500869e1678e5b3c4ff1f3b43a48b2f282cd2fbb`，分歧 `0/0`，Git index 为空；本计划范围精确呈现两个已修改生产文件和五个未跟踪生产外文件，构建进程为零。Git 同时提示七文件下次由 Git 接触时会按仓库设置从 LF 转为 CRLF，该提示未造成空白错误或文件解码失败。

## 未验证范围

- 未注入私有反向引用索引缺项、多项、错类型或失序；内部不一致分支只由源码闭包、完整性互证和两配置编译覆盖。
- 未注入真实 `bad_alloc`、临时退出集合分配失败或其它资源耗尽。
- 未做锁公平性、长时间并发、性能、崩溃 / 重启 / 恢复或跨进程测试。
- 未执行生产入口、真实消费者、L2 S3、完整场景退出、其它五类 L2 结构、A1 聚合面或具名独立集成验收。
