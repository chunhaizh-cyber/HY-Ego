# L2-STRUCTURE-CRUD-REBUILD-T1 验证记录

日期：2026-08-12

## 1. 构建与专项结果

| 验证 | 结果 |
| --- | --- |
| 主工程 Debug x64 `Rebuild` | PASS |
| 主工程 Release x64 `Rebuild` | PASS |
| Debug 临时专项，参数 `51000 / 11 / 101 / 202 / 303` | PASS，16 / 16，失败数 0 |
| Release 临时专项，参数 `55000 / 44 / 404 / 505 / 606` | PASS，16 / 16，失败数 0 |

专项构建使用系统临时 GUID 目录作为输出和中间目录；唯一警告为该用法对应的预期 `MSB8029`，没有编译警告升级或运行失败。扩充专项过程中的失败均为临时验证 cpp 编译错误（缺标准头、强类型比较 / 构造和无辅助函数调用），修正验证文件后双配置完整重跑通过；未据此修改生产合同。

## 2. 运行矩阵

双配置实际通过：

1. 普通应用状态 provider 唯一装配，状态交付不可默认构造 / 复制且可移动，装配状态 11—13 有静态断言；
2. 状态来源与十类型登记已加载；
3. 状态节点、三关系、两值和两槽原子新增；
4. 精确重复返回首次完整结果，同键异义返回幂等冲突；
5. 状态当前 / 历史身份来源闭合；
6. 当前与创建截止历史完整读回；
7. 同主体 / 实例多个当前状态稳定返回，不选择最新；
8. 精确 / 时间两角色的 I64、I64 组、U64 组、独立材料引用八种表示真实写入读回；
9. 合法无状态主体 / 实例返回成功空组；
10. 主体与实例宿主不符返回引用冲突且零写入；
11. wrong-family 状态身份返回入口拒绝；
12. 零主体、零实例、零来源、空数组、零独立材料和错合同返回入口拒绝且事实代次不推进；
13. 不存在主体 / 实例不得误报合法空组；
14. 已退出主体 / 实例分别返回已退出和空载荷；
15. 十二轮无 sleep `std::barrier` 同起步，一个状态写者与主体 / 实例两个条件读者只出现完整升序已读取、许可拒绝或事实代次漂移，没有部分组、重复状态或内部不一致，且实际命中成功写入；
16. 人为推进后，旧期望当前、历史和两条件查询全部返回漂移与空载荷。

## 3. 静态门禁

- `python .\tools\check_specs.py --strict`：PASS，111 份正式规范目录项一致。
- 十路径 `git diff --check`：PASS；既有三个受版本控制文件只报告未来 Git 触碰时 LF 转 CRLF 提示，无空白错误。
- 十路径严格 UTF-8 解码、主工程 / filters / 临时项目 XML 解析：PASS。
- 主工程计数：29 个生产 `.ixx`、15 个头、四配置 `/utf-8` 四项；临时项目未登记到主工程 / filters。
- `海中鱼巣/领域/服务.L2状态结构.ixx` 对旧通用 `提交中性写集`：零命中。
- 精确源码复核：生产代码没有测试函数、测试分支、sleep、故障注入、验证计数器、第二状态仓 / 缓存 / 索引 / SQL / 日志入口；宽泛的 `friend`、函数名“验证并移动交付”和局部变量“索引”不属于验证专用实体。

## 4. 未验证与声明边界

没有通过生产后门构造 malformed 状态来源、缺失 / 重复归属、错 owner / 锚点 / 类型 / 角色或生命周期损坏；现有公开 L2 创建入口不提供这些构造能力。本记录也不覆盖真实资源分配失败、长期公平、故障注入、崩溃恢复、跨进程、生产消费者或独立集成验收。

本结果只证明 T1 具名生产外矩阵、双配置构建和静态门禁通过；不证明 T2、动态、因果、世界树聚合、状态真实性或生产业务闭环完成。
