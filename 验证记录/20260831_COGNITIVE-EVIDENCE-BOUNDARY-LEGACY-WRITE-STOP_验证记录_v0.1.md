# COGNITIVE-EVIDENCE-BOUNDARY-LEGACY-WRITE-STOP 验证记录 v0.1

日期：20260831

计划：`COGNITIVE-EVIDENCE-BOUNDARY-LEGACY-WRITE-STOP v0.3`

候选基线：`4e631436c9e1f0ed4b29354ec2a841beb35eb9de`

## 1. 通过项

| 验证 | 结果 | 证据边界 |
| --- | --- | --- |
| `python tools/check_specs.py --strict` | PASS，116/116 | 只证明正式规范目录一致性 |
| 隔离候选 whitespace 检查 | PASS，无 whitespace 诊断 | 服务差异 `+71/-347`；测试差异 `+86/-0` |
| 公开 ABI / 数据头 / 工程零差异 | PASS | 52 个 tracked `*.数据.h`、`vcxproj`、`filters` 均与 HEAD 相同；两目标文件 import 集不变 |
| 静态首次写扫描 | PASS | 两入口各读取首次账 1 次、重放原写集 1 次、首次写 helper 调用 0 次；文件中 `#if 0` 为 0 |
| Debug x64 候选构建 | PASS | VS2022 MSBuild，`PlatformToolset=v143`，完整 `Rebuild` 后最终候选 `Build` 通过 |
| Release x64 候选构建 | PASS | VS2022 MSBuild，`PlatformToolset=v143`，完整 `Rebuild` 后最终候选 `Build` 通过 |
| Debug 专项运行 | PASS | 两入口无账均入口拒绝、截止 0、空载荷；每次调用后事实代次不变 |
| Release 专项运行 | PASS | 同 Debug |
| 数据头与工程文件候选差异 | PASS，0 | 未通过候选树夹带未提交 SELF/FRESH ABI |

专项运行使用隔离候选临时入口参数，运行后已恢复`海中鱼巣/入口.cpp`，其最终内容与 HEAD 字节相同；临时入口不进入候选 blob 或提交。

## 2. 已执行但未通过的既有全根 / 工具链项

### 2.1 正式自我治理一致事实全根

Debug、Release 均实际执行。两配置都先输出：

```text
[治理测试] 认知证据边界两个旧入口无账拒绝矩阵通过
```

随后在本计划以外的旧矩阵失败：

```text
[治理测试] 失败：方法查找真实链系统根初始化失败
[治理测试] 失败：需求初次筹办工作包步骤=建立需求目标公开事实基础
```

两配置退出码均为 1。该失败早于本计划之外的需求初次筹办工作包闭合，不得记为本计划专项通过，也未越界修改其实现。

### 2.2 正式项目 `v145` 全量重建

- VS 18.8、MSVC 14.51/14.52：候选与未修改基线都在未改文件
  `海中鱼巣/领域/内部治理/服务.需求初次筹办准备.ixx:251`
  触发同一 MSVC `C1001`（IFC 导入，`symbols.cpp:5758`）。
- VS 18.5、MSVC 14.50：候选源编译完成，但链接阶段出现 32 个跨 module 未解析外部符号。
- 两个目标源码及专项源码均已在 `v145` 扫描/编译阶段通过；完整可执行闭包改由 `v143` 双配置重建证明。

因此不能声明当前仓库默认 `v145` 全根门禁已经通过。`v145` 基线工具链问题不是本计划代码回归，也未在本计划白名单外修复。

## 3. NOT_RUN

没有可授权的正式 legacy 持久样本，以下矩阵为 `NOT_RUN`：

- 已有账同义精确重放；
- 已有账同键异义；
- 首次账封套、首次写集或首次结果损坏；
- 必要本地键缺失；
- 历史投影或闭包损坏；
- L1 重放返回异常状态、G1 或新编码映射不一致；
- 重启后的已有账恢复与精确重复。

冻结后的公共 API 不得为测试建立新的旧 proof 账；没有新增测试写后门。

## 4. 生产调用残留与声明边界

静态扫描确认 HEAD 中`服务.任务目标未达成重筹办触发.ixx`仍调用两个 legacy 入口。发布后这些无首次账调用会结构化 fail-closed；调用移除不属于本计划。

本记录不能用于声明历史恢复已证明、默认 `v145` 构建通过、正式全根通过、v2 provider 可用、执行后结算接通、current-value 比较闭合，或 SELF/FRESH/学习/完整认知闭环完成。
