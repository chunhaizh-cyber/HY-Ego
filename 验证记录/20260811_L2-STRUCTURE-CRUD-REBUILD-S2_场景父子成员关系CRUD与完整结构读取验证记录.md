# L2 场景父子成员关系 CRUD 与完整结构读取验证记录

日期：2026-08-11

计划身份：`L2-STRUCTURE-CRUD-REBUILD-S2` v0.1

验证基线：`eaa68a920fdf11f112157ce66f0af4c84dac486c` 加本计划七文件未提交 WIP

## 1. 主工程双配置构建

执行：

```powershell
MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /verbosity:minimal
MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Release /p:Platform=x64 /verbosity:minimal
```

最终生产修正后，Debug 与 Release x64 均完整扫描模块、编译并链接成功，分别生成 `x64/Debug/海中鱼巣.exe` 与 `x64/Release/海中鱼巣.exe`。这只证明两项具名配置可编译链接，不证明生产入口已装配或调用 S2。

## 2. 生产外参数矩阵

执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\验证工具\运行L2场景关系CRUD与完整读取参数验证.ps1 `
  -配置 Debug -幂等基值 2000 -父子顺序 10 -成员顺序 20
powershell -ExecutionPolicy Bypass -File .\验证工具\运行L2场景关系CRUD与完整读取参数验证.ps1 `
  -配置 Release -幂等基值 4000 -父子顺序 30 -成员顺序 40
```

最终生产修正后，Debug 与 Release 均打印 14 项 `[PASS]`、`验证结论：PASS，失败数=0`。共同诊断为：最终事实代次 26，父子首次截止 11、父替换截止 14、父退出截止 15、成员退出截止 20。

| 验证组 | 实际结果 |
| --- | --- |
| 登记与夹具 | 公开登记、七场景和两存在逐次建立，身份非零且代次逐次推进 |
| 父子新增与幂等 | 首次提交；后继推进后原请求精确重复；同键异义幂等冲突，均零额外推进 |
| 单父与无环 | 第二父、自环和后继指回祖先均引用冲突 |
| 父关系替换 | 新关系建立与旧关系退出共享同一代次，当前目标组唯一一致 |
| 父关系退出 | 退出与重复零额外推进；替换首次截止不混入未来退出 |
| 成员新增 | 同场景 / 存在重复拒绝，同一存在跨场景独立关系合法 |
| 成员替换 | 同场景换序与跨场景迁移成功；改变存在不伪装成替换 |
| 成员退出 | 首次退出成功，原请求精确重复且零额外推进 |
| 当前完整读取 | 两类关系按业务顺序稳定排序，重复读取对本切片字段逐项相同 |
| 历史完整读取 | 多个截止按半开区间重建替换前后、退出前后的父子与成员关系 |
| 空组与状态分账 | 合法空组已读取；未找到、当前已退出、退出前历史和已退出父端点分别按合同返回 |
| 非法请求 | 零 / 超大顺序、相同父子、无变化替换、错合同和坏读取请求均零变化 |
| 旧期望与写集裁决 | 旧期望新键漂移，原完整写集精确重复，同键异写集幂等冲突，均零发布 |
| 并发推进 | 无 sleep 条件下读取只出现同截止成功、漂移或许可拒绝；写入恰推进一次 |

专项工程构建均出现 MSB8029，提示中间 / 输出目录不宜位于系统临时目录；实际 Rebuild、链接、程序运行和退出码均成功，工作区没有外验生成物。

## 3. 静态、工程与范围门禁

- `python .\tools\check_specs.py --strict`：`111 份目录项全部通过`。
- 主工程、专项工程和 filters 通过严格 UTF-8 解码及 XML 解析。主工程保持 26 个 `.ixx`、12 个 `ClInclude`、4 个 `/utf-8`；专项项目未进入主工程或 filters。
- 七个公开 S2 函数在服务类中各唯一一处；十个新增请求 / 结果 DTO 各唯一一处；本地键精确为 `0x0201'0201U` 与 `0x0201'0202U`。
- 新增生产 diff 中关系类型编码字段、硬编码稳定编码、L1 私有仓库 import、第二幂等账 / 仓 / 缓存、SQL、mutex、sleep 和轮询均为零；新增自定义函数与变量保持中文命名。
- 入口、启动、装配、L1、存在结构、规范、计划、计划索引和禁止目录没有本切片差异；流程图、路线输入与 `.codex-build/` 的异主 WIP 原样保留。
- 七条允许路径最终执行 `git diff --check` 无空白错误；Git index 保持为空。

## 4. 未验证范围与结论边界

未运行生产入口、真实业务消费者、Win32 配置、故障注入、长时间公平竞争、跨进程恢复、独立集成验收或真实业务数据；计划不要求这些范围。并发外验不使用 sleep、轮询或私有锁放大竞争窗口。

结论：S2 的 DTO、父子 / 成员六写、当前 / 历史完整场景读取和生产外矩阵已通过 Debug / Release x64 主工程构建及双配置专项外验。该结论不证明生产接线、恢复、集成验收、S3 / 其它结构族、A1 或完整 L2；提交与推送仍待顶层唯一发布者完成。
