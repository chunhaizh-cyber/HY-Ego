# L1-SIMPLIFY-P12-PRESTATE v0.4 验证记录

## 1. 验证对象

- plan blob：`377c4f9e5a30fd930856d6c3984c36bef4684028`
- 验证基线：`3eb95fc2b701349178799ae80bb959cad6d9f357`加P12五文件工作区差异
- 目标：SELF-GOV-CLOSURE STEP-3F-PRESTATE当前关系19候选提供者

## 2. 动态与规范门禁

| 门禁 | 实际结果 |
| --- | --- |
| Debug x64串行Rebuild | PASS |
| Debug `--self-test-exit` | PASS，退出码0 |
| Release x64串行Rebuild | PASS |
| Release `--self-test-exit` | PASS，退出码0 |
| Debug MSVC模块扫描 | PASS，sources/modules=197/197，failed=0，循环0，外部需求0 |
| Release MSVC模块扫描 | PASS，sources/modules=197/197，failed=0，循环0，外部需求0 |
| `python .\tools\check_specs.py --strict` | PASS，110/110 |
| P12目标`git diff --check` | PASS |

模块扫描候选只写入系统临时目录，没有形成仓库产物。

## 3. 顺序360追加矩阵

既有L1状态自检报告由32项扩展为45项；新增13项随Debug/Release完整自检通过：

1. 当前主体和实例槽合法、当前关系19范围为空时返回`无前状态`、非零截止和零前态证据；
2. 从未存在端点返回`未找到`且载荷为空；
3. 时间30、10、20乱序写入后，拟发布时间25唯一选择时间20并由P9完整读回；
4. 等于拟发布时间的状态与未来状态均被排除；
5. 跨主体、跨实例槽的当前状态不进入候选；
6. 直接退出仍被当前关系引用的端点被L1以`入口拒绝`拒绝；
7. 同事务退出当前关系19、实例槽和采样值后，P12对后来退出端点返回`未找到`且不枚举历史关系；
8. 同一状态仍可由P9已知编码入口完整历史读回，实例槽和采样值退出见证成立，但不改变P12候选集合；
9. 非法请求返回`入口拒绝`且载荷为空；
10. 同主体、同实例槽、同时间两个状态返回`冲突`；
11. 缺少关系19角色的结构返回`内部不一致`；
12. 请求DTO静态不存在前状态、候选、截止、算法、注册、动态、许可和事务字段；
13. 选择规则版本、查询身份及成功证据一致。

## 4. 静态合同与范围

- 公开选择函数定义唯一；P12生产区域包含2次完整快照、1次当前节点读取、2处具名历史读取和1次P9公开读取调用。
- P12生产区域中`提交写集`、P7/P8服务调用、SQL、日志、缓存和关系20命中均为0。
- 启动`登记自检(370`命中为0；临时诊断标记命中为0；顺序360身份和启动文件未修改。
- 工作区其它所有者dirty未进入P12目标差异；P12正式范围保持三个代码文件和两份v0.4专属记录。

## 5. 最大证明边界

本验证只证明当前关系19范围内的相邻前状态选择源码、顺序360追加矩阵、双配置本地构建/完整自检、规范检查和模块依赖闭合。不证明退出关系历史枚举、P9/P10/P11、第二状态、关系20、迁移动能、动态、运行期装配、恢复、具名集成验收、STEP-3F整体或治理闭环。
