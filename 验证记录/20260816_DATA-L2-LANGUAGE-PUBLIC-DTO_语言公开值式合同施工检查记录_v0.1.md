# DATA-L2 语言公开值式合同施工检查记录

日期：2026-08-16

计划身份：`DATA-L2-LANGUAGE-PUBLIC-DTO v0.1`

计划 blob：`736e97eaf8c317247e87627eb2e9888eeb71bbbf`

验证基线：`5e0831bed1477bf51145c7c7ffad3a91f10e879f`

## 1. 结论

仓库外 MSVC C++20 纯值检查、根工程 x64 Debug / Release fresh Rebuild、工程 XML、UTF-8、唯一登记、诊断责任、第二 owner、生产零测试痕迹、diff 和 strict 门禁均通过。

这些结果只证明本次语言 DTO ABI 候选和施工门禁；没有建立独立验收结论，也不证明语言服务、权威语言事实或后继消费已经完成。

## 2. 仓库外 C++20 检查

使用 Visual Studio 18 Professional x64 工具环境，对仓库外临时源码执行等价命令：

```text
cl.exe /nologo /std:c++20 /EHsc /permissive- /W4 /WX /utf-8 <临时源码> /Fe:<临时程序>
```

最终编译退出 `0`，临时程序退出 `0`。33 个判定点覆盖：

- 合同版本、期望事实代次、幂等身份、语言身份、登记键材料和来源材料的零 / 非零守卫；
- 替换前后完整公开输入相同拒绝，以及登记键或来源任一变化的异义形状；
- 当前 / 历史读取类别、历史截止非零且不晚于期望代次；
- 语言身份、节点族、登记键 / 来源关系编码、四路同步生命周期、owner / 族锚点 / 关系类型活动状态；
- 新增、替换、退出三种写成功形状；按身份、按登记键两种读成功形状；
- 当前读不接受退出事实，按身份历史读可回显已退出事实，按登记键历史读只接受截止上未退出事实；
- 缺载荷、错变更代次、同身份替换、错创建 / 退出截止和非成功状态均不被 `成功()` 误判。

首次临时夹具编译的 `C2512` 来自夹具对显式 `L2语言身份` 使用空聚合初始化；改为完整强类型聚合构造后通过，正式生产代码没有因此修改。

验证完成后，仓库外临时检查源码、对象、程序和构建日志文件计数均为 0；仅保留安全策略不要求继续删除的空目录骨架。

## 3. 根工程双配置

使用两个互不共享、位于系统临时目录的 `OutDir / IntDir`，执行：

```text
MSBuild.exe .\海中鱼巣.slnx /t:Rebuild /m:1 /p:Configuration=Debug /p:Platform=x64 /p:OutDir=<Debug临时目录> /p:IntDir=<Debug临时目录>
MSBuild.exe .\海中鱼巣.slnx /t:Rebuild /m:1 /p:Configuration=Release /p:Platform=x64 /p:OutDir=<Release临时目录> /p:IntDir=<Release临时目录>
```

- Debug：退出 `0`；
- Release：退出 `0`；
- 两配置仅出现系统临时输出目录对应的 `MSB8029` 增量构建提示，无编译或链接错误；
- 未读取、写入或清理异主 `.codex-build`。

## 4. 静态门禁

- `python .\tools\check_specs.py --strict`：`113/113`；
- 新头、vcxproj、filters 均可按严格 UTF-8 解码；两个工程 XML 均可解析；
- Debug / Release、Win32 / x64 四个配置均保留 `/utf-8`；
- 新头在 vcxproj / filters 的登记计数为 `1/1`；
- 12 个新增 helper 的诊断责任计数为 `12/12`；
- 生产树中语言 DTO 唯一所有者文件仅 `海中鱼巣/领域/L2语言结构.数据.h`，第二 owner 计数为 `0`；
- 新头零 `L2语言结构服务`、owner、字符串 / 路径类型、PASS / FAIL、自检、测试或验证痕迹；
- 精确白名单 `git diff --check` 通过，仅有既有工程行尾的 LF→CRLF 提示；
- 未新增词条、语素、词性、概念名称 DTO / 服务或普通应用接线。

## 5. NOT_RUN 与未证明范围

以下按计划保持 `NOT_RUN`：真实不可变材料文件与严格 UTF-8 读回、语言 owner / 服务、同键唯一和引用保护、并发冲突、资源失败、发布与进程恢复、普通应用装配、词条、语言记录、概念名称、BIZ 消费以及最终 `DATA-EXT-10` 闭环。

临时纯值检查不是生产消费者、机器事实或独立黑盒验收；根工程构建和代码发布也不得升级为上述范围通过。
