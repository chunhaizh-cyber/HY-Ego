# P5 初始场景实际存在消费者独立验收

日期：2026-08-07

结构化结论：`INTEGRATION-ACCEPTANCE-PASS`

候选提交：`9e5b3431ac0c7dd5aa2ce7905bee8bce43131d35`

具名目标：`初始场景实际存在创建真实消费者中性写读闭环 v0.1`

## 1. 结论

候选已推送且等于验收开始时的 `origin/main`。验收从候选 `git archive` 建立全新临时根，不使用共享工作区异主WIP。

Debug/Release原生产目标均完整重建成功，两配置正式 `--runtime-context` 均退出0。生产代码外消费者驱动只导入 `海中鱼巣.启动.生产运行期`，调用 `生产运行期会话::启动`，消费结构化启动结果和有效租约，再经运行期上下文公开入口对以下四项各连续读取两次：

- 初始场景实际存在；
- 实际存在登记；
- 初始场景；
- 世界登记。

两配置均取得相同结构化结果：四项各自两次逐字段全等，共同事实截止4；存在编码12、直接成员关系13、资格值14，资格属性类型11、资格来源10、直接成员关系类型4。世界根、当前场景、资格属性类型/来源和直接成员关系方向跨四项权威事实逐字段互证，13个不同实体编码全部非零唯一，完整性函数均成立。

候选生产影响闭包冻结30个新增或直接修改函数。30个全部编译并登记正式诊断责任；合法消费者成功路径实际进入27个，另3个失败状态映射函数只作编译接入和静态检查，不为覆盖率补造失败或旁路入口。未发现未收口逻辑异常。

因此，本具名P5服务切片通过独立消费者验收。

## 2. 正式消费边界

```text
生产代码外消费者
-> 生产运行期会话::启动
-> 结构化启动结果 + 有效租约
-> 运行期上下文::读取初始场景实际存在（两次）
-> 运行期上下文::读取实际存在登记（两次）
-> 运行期上下文::读取初始场景（两次）
-> 运行期上下文::读取世界登记（两次）
```

驱动不导入或直调L1，不读取私有仓、锁或内部事务，不解析日志，不增加验收入口。驱动只在临时归档内替换外部入口翻译单元，生产模块和工程保持候选内容。

## 3. 允许声明

- 初始场景实际存在这一合法L2消费者通过中性入口原子创建并公开读取一个节点、一个值、一个属性槽和一条直接成员关系。
- 固定候选的P5具名消费者服务切片验收通过。
- 四项公开权威事实在同一当前截止互证成立。

## 4. 禁止声明与未覆盖

不得声明精确重复、非法请求零变化、修改、移动、退出或历史读取、特征、状态、动态、并发、其它消费者或整个L1完善。用户已明确当前不考虑崩溃、重启、断电和跨进程恢复。

本轮未修改候选代码、工程、规范、设计、计划、索引或其它记录；未暂存、未提交、未推送。
