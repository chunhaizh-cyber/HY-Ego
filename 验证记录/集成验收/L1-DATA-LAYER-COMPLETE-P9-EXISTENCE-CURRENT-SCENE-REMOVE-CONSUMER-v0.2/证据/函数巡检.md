# P9 候选函数与诊断责任巡检

候选修改四个生产文件。正式 Debug / Release 完整重建证明修改闭包编译接入；外部消费者经 const 运行期上下文实际进入创建、读取和移除公开链。

| 类别 | 函数 | 诊断责任 | 巡检事实 |
| --- | --- | --- | --- |
| 运行期 | `运行期上下文::运行期上下文` | 无适用错误分支 | 生产会话启动实际构造三服务组合 |
| 运行期 | `读取L1实际存在场景组合服务` | 无适用错误分支 | 消费者实际取得 const 引用 |
| 实际存在纯值 | 移除请求有效、退出见证完整、移除事实完整、移除结果成功 | 无适用错误分支 | 成功与各拒绝路径实际调用 |
| 场景纯值 | 退出幂等键映射、退出状态映射、请求有效、退出见证完整、退出事实完整、退出结果成功 | 无适用错误分支 | 首次、重复、冲突及拒绝路径实际调用 |
| 场景读取 | `读取接纳实现` 的精确已退出分账 | 向上送出 | 退出后组合正式读取返回未找到 |
| 场景写入 | `退出初始场景直接存在成员关系` | 向上送出 | 首次、重复、同键异义实际进入 |
| 组合内部 | `映射场景状态`、`写前事实匹配请求` | 无适用错误分支 | 成功、关系错误、重复和冲突路径实际进入 |
| 组合构造 | `L1实际存在场景组合服务` 构造 | 无适用错误分支 | 运行期启动实际构造三引用服务 |
| 组合创建 | 非const `在初始场景创建实际存在` | 向上送出 | 排除；消费者持有 const 服务 |
| 组合创建 | const `在初始场景创建实际存在` | 向上送出 | 非启动锚定主目标创建实际进入 |
| 组合移除 | const `移除实际存在当前场景` | 向上送出 | 全部目标场景实际进入 |

分账：新增或直接修改函数23；非const创建兼容入口排除1；其余22均编译接入并在正式启动、成功、重复或拒绝闭包中进入；逻辑异常0。

CODE-FILE-01 诊断责任成立：公开服务错误以结构化状态向上送出；纯值 `noexcept` 判断、映射、完整性检查和只读访问器无错误分支且无副作用。缺少生产日志不参与本裁决，消费者依据结构化结果和正式读回。

未修改的其它领域服务、第一层、仓库及其它消费者不属于本验收目标。
