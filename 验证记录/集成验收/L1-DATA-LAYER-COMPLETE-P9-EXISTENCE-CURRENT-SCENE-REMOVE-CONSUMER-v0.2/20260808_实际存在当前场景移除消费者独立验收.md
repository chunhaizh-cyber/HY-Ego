# P9 实际存在当前场景移除消费者独立验收

## 1. 结论

`INTEGRATION-ACCEPTANCE-PASS`。

候选 `6e50b0a2cc1843a0b41b79fab87dac4b9176c690` 在生产代码外的合法第二层消费者中取得 `公开黑盒局部通过`。消费者只导入 `海中鱼巣.启动.生产运行期`，经正式会话、有效租约和 const 运行期上下文取得 const `L1实际存在场景组合服务`，创建一个非启动锚定实际存在并退出其当前场景归属关系。

Debug、Release 两配置的原生产目标和外部消费者均完整重建并退出 0。消费者两配置结构化输出一致：主目标退出后经正式组合读取为 `未找到`；首次与精确重复均返回同一完整历史退出见证；无效请求、错误关系、错误代次和同键异义前后，适用公开事实与世界截止保持不变。

本结论只累计证明该第二层消费者实际覆盖的一条直接存在成员关系退出、当前不可读、历史退出见证回查、精确重复、同键异义和适用非法请求零变化。不证明第一层自检或自证，不证明整个第一层完善，也不证明关系替换、新关系发布、条件关系集合查询、存在删除、并发或崩溃恢复。

## 2. 基线与隔离

- 候选提交：`6e50b0a2cc1843a0b41b79fab87dac4b9176c690`
- 正式 plan blob：`dfafa3c37f7c92252e4787cda41ded733c440072`
- 隔离归档：`C:\Users\zhchh\AppData\Local\Temp\hzy-p9-current-scene-remove-accept-20260807-235701`
- 驱动 SHA-256：`9963150C233B3A051457734E61BB6EF36BD0E4B99F25E9579F6A488C2967E3F9`

归档由候选 `git archive` 展开，不是 worktree，不携带共享工作区 WIP。外部驱动只在隔离归档内临时替换入口用于消费者构建，未修改候选、工程或生产目标。

## 3. 正式消费者链

```text
生产运行期会话::启动
-> 有效运行期上下文租约
-> const 运行期上下文::读取L1实际存在场景组合服务
-> 在初始场景创建实际存在 const
-> 读取实际存在
-> 移除实际存在当前场景 const
-> 读取实际存在 / 读取世界登记
```

驱动不导入或调用第一层、仓、锁、事务私有入口，不解析日志，不建立生产验收入口或双目标。启动锚定实际存在只在同键异义场景中作为已有错误输入，且前后公开读回不变；唯一主目标是新建的非启动锚定实际存在。

## 4. 实际结果

两配置消费者输出一致：

```json
{"通过":true,"创建":1,"无效请求":5,"错误关系":10,"错误代次":7,"移除首次":1,"移除重复":2,"同键异义":6,"创建代次":5,"退出代次":6,"当前截止":6,"存在":15,"资格":17,"退出关系":16}
```

首次退出结果和精确重复结果逐字段相同，均携带关系编码16、创建代次5、退出代次6的完整历史退出见证。退出后主目标经正式组合读取返回 `未找到` 且无事实；启动锚定存在在同键异义前后保持同一当前事实。

## 5. 过程事实与边界

- 原生产 Debug / Release 完整重建及 `--runtime-context` 运行均退出0，stdout/stderr 均为空。
- 外部消费者首次构建因驱动辅助函数显式命名未由顶层生产模块直接导出的结果类型而失败；改为模板/`auto` 消费公开返回合同后两配置通过，产品代码和矩阵未变。
- 候选 `git show --check` 通过；隔离归档 strict 为110/110。
- 当前不考虑崩溃、重启、断电或跨进程恢复，均为 `NOT_APPLICABLE`。

详细证据见 `manifest.json` 与 `证据/`。
