# P6 v0.2 I64特征定义消费者独立验收

日期：2026-08-07

结构化结论：`INTEGRATION-ACCEPTANCE-PASS`

人读结论：`公开黑盒局部通过 / 可作为第二层消费者的L1实际覆盖证据`

候选提交：`dc312ef279cc6fb32556898aea8c9998df1140c4`

正式 plan blob：`ad160efd2659cef966a36eb8e590ae1912cdee7d`

## 1. 结论

生产代码外的第二层特征定义消费者只导入 `海中鱼巣.启动.生产运行期`，经正式会话、有效租约、`const 运行期上下文*` 与 `const L1特征定义服务&` 调用了正式增查入口。Debug 与 Release 的完整重建和运行均退出 0，结构化结果一致：

- 登记首次：`已提交`，事实代次 `4 -> 5`；
- I64 定义首次：`已提交`，事实代次 `5 -> 6`；
- 完整相同定义请求：`幂等读回`，未形成第二事实；
- 同幂等键、不同合法上界：`幂等冲突`，无部分定义；
- 下界大于上界：`入口拒绝`，无部分定义；
- 冲突和拒绝前后均经 `读取I64特征定义` 逐字段读回同一事实；
- 登记四编码与定义节点、三值共八编码非零且唯一，定义侧编码为 `19/20/21/22`；
- 世界登记当前截止在重复、冲突和拒绝后均保持定义截止代次 `6`。

因此 v0.1 的 const 可达性漂移已经在 v0.2 候选中解除，本消费者所实际调用的创建、读取、精确重复、幂等冲突和非法请求零变化维度可以累计为 L1 的消费者覆盖证据。

## 2. 结论边界

本记录不是第一层自检、自证或整体完善声明。它只证明 P6 具名第二层消费者经公开生产边界取得了符合合同的结构化结果和权威读回。生产构建、日志和退出码仅作工程证据，未被解析为机器事实。

不证明实例特征、当前值修改、关系、退出/历史、其它消费者、旧栈删除或整个 L1 完善。用户明确当前不考虑程序崩溃，因此崩溃、重启、断电和跨进程恢复标为不适用；并发与外部资源失败也不在本计划合同内。

本轮只新增本 v0.2 验收目录，未修改候选代码、工程、规范、设计、计划、索引或 v0.1 历史验收；未暂存、未提交、未推送。
