# 命令与退出码

专用验证根：

```text
D:\TEMP\海中鱼巣\INTEGRATION-ACCEPTANCE-RUNTIME-DEMAND-TASK-SUBGOAL-CONSUMPTION\20260823-ROUND1
```

| 命令 | 退出码 | 观察 |
| --- | ---: | --- |
| `msbuild .\海中鱼巣.slnx /t:Rebuild /m /p:Configuration=Debug /p:Platform=x64 /p:OutDir=<专用根>\Debug\bin\ /p:IntDir=<专用根>\Debug\obj\ /v:minimal` | 0 | 目标模块编译并链接到专用 Debug exe |
| `<专用根>\Debug\bin\海中鱼巣.exe --test-治理` | 0 | 完整重跑输出目标专项动态通过/NOT_RUN 分账，末行`[治理测试] 全部通过` |
| `msbuild .\海中鱼巣.slnx /t:Rebuild /m /p:Configuration=Release /p:Platform=x64 /p:OutDir=<专用根>\Release\bin\ /p:IntDir=<专用根>\Release\obj\ /v:minimal` | 0 | 目标模块编译并链接到专用 Release exe |
| `<专用根>\Release\bin\海中鱼巣.exe --test-治理` | 0 | 输出与 Debug 相同的目标专项动态通过/NOT_RUN 分账，末行`[治理测试] 全部通过` |
| `python .\tools\check_specs.py --strict` | 0 | `116 份目录项全部通过` |
| `git diff --check` | 0 | 没有 whitespace error；只出现异主流程图工作副本 LF/CRLF 提示 |
| `git diff --cached --check` | 0 | index 为空，无错误 |
| `git rev-list --left-right --count HEAD...origin/main` | 0 | 记录开工时为`0 0` |

目标专项精确摘要：

```text
[治理测试] 需求消费任务子目标记录 V01-V06、V08-V12、V14-V15、V18-V19 动态通过；V07、V13、V16-V17、V21 按源码守恒/不可注入 NOT_RUN；V20 由静态禁项扫描裁决
```

首次 Debug 运行使用了 120 秒短工具超时；工具返回 124 时进程仍正常运行，随后自然退出。该次不计产品失败，也不计通过。清空该进程后以 600 秒上限完整重跑，退出码 0；只有完整重跑进入结论。

构建成功和运行退出码只证明编译接入、自检入口可达及当次自检观察，未用于替代正式接线或机器事实。
