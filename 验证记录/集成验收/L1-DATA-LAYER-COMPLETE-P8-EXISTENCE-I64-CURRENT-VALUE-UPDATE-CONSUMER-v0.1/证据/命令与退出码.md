# P8 验收命令与退出码

候选：`f7dd52fefc27f1f0f9a6a577df01940045caa978`

隔离归档：`C:\Users\zhchh\AppData\Local\Temp\hzy-p8-current-value-consumer-accept-20260807-220915`

```powershell
$验收根 = 'C:\Users\zhchh\AppData\Local\Temp\hzy-p8-current-value-consumer-accept-20260807-220915'
$构建器 = 'C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe'
```

归档由候选 `git archive` 展开，不是 worktree。先保持候选原始入口验证生产目标，保存结果后才在归档内临时替换入口构建外部消费者。

## 原生产目标

```powershell
& $构建器 "$验收根\海中鱼巣.slnx" /t:Rebuild '/p:Configuration=Debug;Platform=x64' /m /nologo *> "$验收根\debug-production-build.log"
& "$验收根\x64\Debug\海中鱼巣.exe" --runtime-context 1> "$验收根\debug-production.stdout.log" 2> "$验收根\debug-production.stderr.log"
& $构建器 "$验收根\海中鱼巣.slnx" /t:Rebuild '/p:Configuration=Release;Platform=x64' /m /nologo *> "$验收根\release-production-build.log"
& "$验收根\x64\Release\海中鱼巣.exe" --runtime-context 1> "$验收根\release-production.stdout.log" 2> "$验收根\release-production.stderr.log"
```

四条命令退出码均为 `0`；两次生产运行 stdout/stderr 均为 0 字节。

## 第二层消费者

驱动 SHA-256：`921BA0446E054CF76E0D0AB66E39A9B58F9AF9BB7C95FB27AAB9FD1D6A1AE6A2`。

```powershell
Copy-Item -LiteralPath 'D:\海中鱼巣\验证记录\集成验收\L1-DATA-LAYER-COMPLETE-P8-EXISTENCE-I64-CURRENT-VALUE-UPDATE-CONSUMER-v0.1\证据\实际存在I64实例特征当前值消费者驱动.cpp' -Destination "$验收根\海中鱼巣\入口.cpp" -Force
& $构建器 "$验收根\海中鱼巣.slnx" /t:Rebuild '/p:Configuration=Debug;Platform=x64' /m /nologo *> "$验收根\debug-consumer-build.log"
& "$验收根\x64\Debug\海中鱼巣.exe" 1> "$验收根\debug-consumer.stdout.log" 2> "$验收根\debug-consumer.stderr.log"
& $构建器 "$验收根\海中鱼巣.slnx" /t:Rebuild '/p:Configuration=Release;Platform=x64' /m /nologo *> "$验收根\release-consumer-build.log"
& "$验收根\x64\Release\海中鱼巣.exe" 1> "$验收根\release-consumer.stdout.log" 2> "$验收根\release-consumer.stderr.log"
```

最终四条命令退出码均为 `0`。首次 Debug 消费者运行曾退出 `110`：驱动辅助比较错误地要求首次 `已提交` 和重复 `幂等读回` 的状态字段相等；只修改驱动比较逻辑，不修改产品，随后 Debug / Release 均通过。

两配置 stdout 相同：

```json
{"通过":true,"换代首次":1,"换代重复":2,"同键异义":7,"越界拒绝":6,"来源拒绝":6,"写前漂移":8,"初始代次":4,"实例代次":8,"换代代次":9,"旧值":30,"新值":31,"旧I64":3,"新I64":5}
```

## 门禁

```powershell
Push-Location $验收根
python tools/check_specs.py --strict
Pop-Location
git show --check --oneline f7dd52fefc27f1f0f9a6a577df01940045caa978
python tools/check_integration_acceptance.py --manifest '验证记录/集成验收/L1-DATA-LAYER-COMPLETE-P8-EXISTENCE-I64-CURRENT-VALUE-UPDATE-CONSUMER-v0.1/manifest.json' --verify-git --strict
```

隔离归档 strict 为 110/110，候选 `git show --check` 退出 0。manifest 最终结果见发布前校验。

消费者二进制不是正式生产目标；临时驱动未进入候选、工程文件或生产二进制。
