# P8 实际存在 I64 实例特征当前值消费者独立验收

## 1. 结论

`INTEGRATION-ACCEPTANCE-PASS`。

候选 `f7dd52fefc27f1f0f9a6a577df01940045caa978` 在生产代码外的合法第二层消费者中取得 `公开黑盒局部通过`：消费者只导入 `海中鱼巣.启动.生产运行期`，经正式会话、有效租约和 const 运行期上下文取得 const `L1实例特征服务`，完成一次实际存在 I64 实例特征当前值换代，并覆盖精确重复、同键异义、值域越界、无效来源和写前代次漂移。

Debug、Release 两配置的原生产目标和外部消费者均完整重建并退出 0。消费者两配置结构化输出一致；所有适用拒绝前后，公开当前实例读回和世界截止均保持不变。

本结论只证明 P8 第二层消费者实际覆盖的中性值创建、当前属性槽切换、旧当前值退出、精确重复、同键异义和非法请求零变化。它不证明第一层自检或自证，不证明整个 L1 完善，也不证明崩溃、重启、断电、跨进程恢复、并发或其它消费者。

## 2. 基线与隔离

- 候选提交：`f7dd52fefc27f1f0f9a6a577df01940045caa978`
- 唯一父提交：`eecca2f91cf5f7f0fc865915c7f403518c95754c`
- 正式 plan blob：`ae783e0540840ee08e60c647778d21b5b53c70ca`
- 隔离归档：`C:\Users\zhchh\AppData\Local\Temp\hzy-p8-current-value-consumer-accept-20260807-220915`
- 驱动 SHA-256：`921BA0446E054CF76E0D0AB66E39A9B58F9AF9BB7C95FB27AAB9FD1D6A1AE6A2`

归档由候选 `git archive` 展开，不是 worktree，不携带共享工作区 WIP。外部驱动只在隔离归档中临时替换入口用于构建消费者二进制；未修改候选、工程或生产目标。

## 3. 正式消费者链

```text
生产运行期会话::启动
-> 有效运行期上下文租约
-> const 运行期上下文::读取L1实例特征服务
-> const L1实例特征服务::换代实际存在I64实例特征当前值
-> 结构化结果
-> L1实例特征服务::读取实际存在I64实例特征
-> 运行期上下文::读取世界登记
```

驱动不导入或直调 L1、仓、锁、事务私有入口，不解析日志，不建立生产自检入口、双目标模式或第二套机器事实。

## 4. 实际结果

两配置消费者输出一致：

```json
{"通过":true,"换代首次":1,"换代重复":2,"同键异义":7,"越界拒绝":6,"来源拒绝":6,"写前漂移":8,"初始代次":4,"实例代次":8,"换代代次":9,"旧值":30,"新值":31,"旧I64":3,"新I64":5}
```

首次换代后，公开新定位读回为 I64 值 5、新当前值编码 31；旧定位经同一公开领域读取返回 `未找到`。第二层没有公开旧值历史 DTO；旧值历史完整、旧当前已退出和当前槽指向新值由目标入口的正式成功合同在返回成功前内部强制读回，外部验收不为补格绕过领域边界直调 L1。

## 5. 验收边界

- 原生产目标保持原入口完成 Debug / Release 完整重建与 `--runtime-context` 运行。
- 外部消费者完成 Debug / Release 完整重建与运行。
- strict 为 110/110；候选 `git show --check` 通过。
- 首次 Debug 消费者运行退出 110，归因为驱动错误地要求首次 `已提交` 与重复 `幂等读回` 状态字段相等；只修正驱动逐字段事实比较后，两配置通过，产品代码和验收矩阵未变。
- 崩溃、重启、断电和跨进程恢复按用户当前边界为 `NOT_APPLICABLE`。

详细证据见 `manifest.json` 和 `证据/`。
