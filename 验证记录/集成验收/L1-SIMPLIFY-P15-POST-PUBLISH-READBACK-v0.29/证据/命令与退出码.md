# P15 v0.29 独立验收命令与退出码

候选提交：`0184a0446d7ddc15cf0f2004e5b91ab70dda5f41`

临时导出：`C:\Users\zhchh\AppData\Local\Temp\P15-v029-acceptance-0184a044-20260806-111322`

临时导出由 `git archive` 从候选提交生成，没有叠加当前工作区未提交内容，也不是 Git worktree。

## 1. 身份与范围

| 检查 | 结果 |
| --- | --- |
| `git rev-parse HEAD` | `0184a0446d7ddc15cf0f2004e5b91ab70dda5f41`，退出 0 |
| `git rev-parse origin/main` | `0184a0446d7ddc15cf0f2004e5b91ab70dda5f41`，退出 0 |
| `git rev-list --left-right --count HEAD...origin/main` | `0 0`，退出 0 |
| `git rev-parse 0184a044^` | `57620c808af8d0642fe9960d91920f98efd2b6ff`，退出 0 |
| `git branch -r --contains 0184a044...` | `origin/main`，退出 0 |
| `git diff-tree --name-status` | 24 个代码/模块/自检文件修改 + 2 条专属记录，未包含当前工作区异主 WIP |
| `git diff --check 0184a044^ 0184a044` | 退出 0，无输出 |
| `python .\tools\check_specs.py --strict` | `110 份目录项全部通过`，退出 0 |

## 2. 独立构建与入口

| 命令 | 退出码 | 观察 |
| --- | --- | --- |
| `MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Release /p:Platform=x64` | 0 | 生成 `x64\Release\海中鱼巣.exe` |
| `x64\Release\海中鱼巣.exe --self-test-exit` | 0 | stdout 7449 字节，stderr 0；31 单元总门禁由 `运行完整自检模式()` 强制；P1-S01 `checks=14 failed=0` |
| `x64\Release\海中鱼巣.exe --runtime-context` | 0 | stdout/stderr 均 0；只证明生产运行期上下文首发入口正常退出 |
| `MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Debug /p:Platform=x64` | 0 | 生成 `x64\Debug\海中鱼巣.exe` |
| `x64\Debug\海中鱼巣.exe --self-test-exit` | 0 | stdout 7449 字节，stderr 0；P1-S01 `checks=14 failed=0` |

Release/Debug 自检 stdout 的 SHA-256 均为
`477BB621EE1E9DB252CB75CE0E96FCE10B6C68F498F901A387204CD160F7E96B`。

构建诊断日志分别为 24508487 / 25555060 字节，SHA-256：

- Release：`31FC9C94EDFE94121537336C34B773958BFAFDEAF492F959F7D68423C5D4982F`
- Debug：`977AC1F51EEFB85D29A48C759BCF6003F0F1E884111FC67628AEA8D125B2C0C2`

临时目录位于 Windows TEMP，哈希只绑定本轮亲自核对的原始输出；仓库内同时保存
两配置自检 stdout 副本作为发布证据。

## 3. 正式接线与隔离静态核对

1. `海中鱼巣/启动.运行期上下文.ixx:59-63` 构造唯一 `L1事实基座服务` 及四个领域服务。
2. `海中鱼巣/启动.生产运行期.ixx:49-129` 的正式启动只初始化并发布上下文；没有调用 `提交写集`、`探测幂等`、`导出恢复材料` 或恢复候选入口。
3. `海中鱼巣/启动.应用程序.ixx:50-58` 的 `--runtime-context` 路由只调用 `生产运行期会话::启动`。
4. `海中鱼巣/启动.应用程序.ixx:477-523` 将 P15 相关调用登记在 `运行完整自检模式()`；Release 二进制实测接受 `--self-test-exit` 并执行成功。
5. 因而候选已编译、已装配且自检可达，但尚无生产合法消费者通过正式入口执行 P15 的写入、恢复和独立读回闭环；Release 目标也没有隔离掉完整自检入口。
