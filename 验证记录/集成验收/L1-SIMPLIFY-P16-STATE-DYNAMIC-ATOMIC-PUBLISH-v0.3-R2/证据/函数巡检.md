# P16 v0.3 R2 函数巡检

## 1. 总账

影响闭包仍为37个函数：23个新服务函数、10个新自检函数、2个新增既有服务成员、1个新增启动包装、1个直接修改启动函数。Debug/Release全部编译并进入顺序400静态或运行闭包。

| 分类 | 数量 | 结论 |
| --- | ---: | --- |
| 显式P16责任备注 | 36 | 文字存在 |
| 继承P14责任 | 1 | `运行完整自检模式`向上送出 |
| 责任与实现一致 | 36 | PASS |
| 责任与实现矛盾 | 1 | FAIL |

## 2. 31项修复复核

新候选在服务文件增加22项备注、在自检文件增加9项备注，均紧邻第一次验收列出的未登记函数；相对父提交只有注释行，没有函数体或机器合同变化。这31项按实现可分别归入“无适用错误分支”或“向上送出”，与源码不矛盾。

## 3. 唯一剩余失败

| 函数 | 正式备注 | 实际实现 | 裁决 |
| --- | --- | --- | --- |
| `运行状态动态原子发布自检()` | `本地记录；最终责任边界为启动层顺序400观察边界` | 捕获异常后只设置 `报告.未分类异常=true` 并返回；函数内没有任何日志调用 | FAIL：实际为向上送出 |

直接上层 `运行状态动态原子发布专项自检()` 才调用 `记录运行日志`，并在 `报告.未分类异常` 时调用 `记录逻辑错误日志`。这条实际链是“自检根向上送出 -> 顺序400包装本地记录”，与P14同类链一致；不能把上层的记录行为倒算成本函数的“本地记录”。

因此机械登记为37/37，但符合 CODE-FILE-01 分类语义的只有36/37。
