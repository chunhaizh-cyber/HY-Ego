# 纯概念消费者独立验收回执

日期：2026-09-13；验收者：`/root/l1_read_execute`。

**结论：FAIL。** 候选 `f5dda0a5ec920e8ebe72f441c813bf8151b2f946` 的合法 v2 原登记请求仅改变 G0 时，返回旧格式不支持（22），应返回幂等冲突（16）。Debug、Release 均复现。原矩阵、并发和私有权限检查通过不能抵消此合同失败。本回执不修改候选、冻结状态或计划；等待代码所有者发布修复候选后，重新验证受影响消费者。

## 1. 范围、独立性和输入

本子项属于 `INT-DATA-L1-TWO-READ-CONSUMER-A`，依据已发布候选内 ACCEPTANCE-01 v0.7、4080 v1.1、4015 第 9 节及 A v0.6 计划和详细设计。一般 DATA 不另建验收阶段；本次只执行 4080 两项新增读取及概念消费的具名例外。验收者未编写概念消费者；曾编写 L1，因此不将本记录作为 L1 自身独立验收，不自行声明整体 FROZEN。

唯一独占资源 `P = D:/TEMP/海中鱼巣/INT-DATA-L1-TWO-READ-CONSUMER-A/01a06f6b-concept-review-b407`。源来自候选 Git archive，并按 Git blob 核对编译闭包；不消费 B 工作区修改。38 份源码、头文件及规范输入的精确 blob、原始 SHA256 和规范化 SHA256 见 `P/input-manifest.json`。正式消费源 `海中鱼巣/领域/数据服务.概念树类.ixx`：blob `b6170b9d5780a1e61f6b52e66719822890e49ce9`，SHA256 `54988DEB62DCCE09B91F069E1346A1667F39B56D43160A7C431E6CDF34F53DEF`。

初始及本轮记录落笔前 `HEAD = origin/main = f5dda0a5ec920e8ebe72f441c813bf8151b2f946`，候选已普通推送；index 为空。随后主实施对概念源的已登记修复 WIP 不进入本快照。未写生产或执行 Git 写操作。当前子项只写本回执及 `concept-evidence.json`，根协调者拥有汇总清单和发布权。

原 A round10 驱动、工程及负编译脚本只读复制，来源 SHA 全列于输入清单。`driver.cpp` 保持原始字节，SHA256 `55A4BD237FD63433629B052376110C8055A5F519F4B9580DF2D5C726F343265A`。新项目仅将源码前缀改为本 Git 快照，输出、IFC、obj、编译 TEMP 均在 P；不复用旧 IFC。原始副本只作来源证据，不得直接执行。

## 2. 正式消费链与权威判据

本节行号均指上述候选概念源；驱动行号指 P/driver.cpp。

- `登记纯概念结构_v2`（6219）：先读 owner 原首次材料（6246），新请求实际调用 `读取所有者范围空域`（6272–6275），按结构化状态拒绝或发布。新布局 15 节点、13 关系及格式值共 29 条事实由 owner 权威读取逐项互证；原键重放核原 G0、首次 H、完整账和当前 G。原 G0 异义映射存在第 4 节失败。
- 公开创建、`读取纯概念`（6584）及 `精确查询纯概念`（6615）实际构造 FT、FC、EC 与合取定义；核 C、D、定义成员、族关系、内容归一化、当前 G、首次账及精确查询终态。读取失败/资源异常保留正式状态映射，不交付成功载荷。构造不是伪造 C/D；中性元节点按正式布局合同先建立。
- `核验纯概念无保护引用`（6938–7003）以现存允许事实构成集合，对 C/D 分别实际调用 `读取节点全部当前引用`（6978–6982），核同 G、预算、完整事实去重与集合相等。`迁移纯概念生命周期`（7005，调用 7051）及 `退出无引用叶概念`（7140，调用 7182）据此保护外部引用。
- 生命周期、退出和幂等分支通过当前读取、历史墓碑和首次写入材料互证；拒绝前后当前 G 和相应原账不变。异常映射、已知首次 H 与发布状态来自真实结构化返回；未观察到的资源 19 / 可能发布 21 分支不冒充已测。

驱动 `with_port`（204）先销毁概念服务，再重签发唯一 owner 端口，完成公开事实操作后重新构造概念服务。没有额外活动端口、私有探针、生产测试钩子或跳过成功互证。带 world-use 名称的用例仅证明中性 E 事实经概念公开使用读取的保护，不证明世界树/绑定提供者完整业务。恢复用例是同运行包内销毁服务后按原键重新读取，不能写为磁盘恢复或真跨进程恢复。

## 3. 实际执行与分账

编译工具为 VS 18 Professional 的 `C:/Program Files/Microsoft Visual Studio/18/Professional/MSBuild/Current/Bin/MSBuild.exe`，v145，MSVC 14.50.35717，C++20、UTF-8、x64、bcrypt；Debug/Release 保留各自优化配置。21 个生产模块均从本次 Git 源生成。完整命令、起止时间、退出码和实际编译/链接选项分别见 result.json 的 commands、execution 文件及 compiler-*-cl/link.txt。程序断言直接检查结构化结果和权威事实，日志只保存人读证据。

| 项目 | Debug | Release | 声明边界 |
| --- | --- | --- | --- |
| 原完整矩阵构建/运行 | exit 0 / 0，35/35 | exit 0 / 0，35/35 | 每配置 27 个概念用例 + 8 个 L1 前置拒绝 |
| 额外同 G0 竞争 | 25/25，exit 0 | 25/25，exit 0 | 同实例双线程；实际状态仅 2、15 |
| 私有权限负编译 | 5/5 仅 C2248 | 5/5 仅 C2248 | 公共控制 exit 0；各私口 exit 2 |
| 原 G0 异义补测构建/运行 | exit 0 / 1，FAIL | exit 0 / 1，FAIL | 保留严格期望状态 16 |

原 35 项中 7 个一般概念用例为布局恢复、创建查询归一化重放、生命周期退出历史、并发、结果谓词、模板/子概念/存在引用、族边出生代次。其余 C/D × 七引用位置 × 同/跨 owner 共 28 项：20 项合法外部引用实际进入概念保护；8 项公开写入先由 L1 拒绝。

七位置依次为关系源、关系目标、关系类型、值宿主、值属性、值来源、材料引用。对 C/D 而言，值属性的同/跨 owner 共 4 项、关系类型跨 owner 的 2 项、值宿主跨 owner 的 2 项属于 L1 前置拒绝，不能计作概念引用保护通过。其余 20 项真实发布引用后验证退役/退出拒绝，移除引用后推进，再挂引用后验证退出拒绝和旧请求重放。逐 case 归属见结构化证据 cases。

并发还核唯一胜者 C/D、族边不重复、败者无孤立定义、原首次材料恢复、胜者原键重放、败者旧 G0 拒绝和 G 不变。正确判据允许规范中的资源 19 / 已知 H 可能发布 21，但 50 轮实际只出现创建 2 与代次漂移 15；19/21 未注入。

五个私口为 `绑定于`、`借用存在概念引用写端口`、`准备存在概念引用片段`、`读取存在概念引用首次材料`、`读取存在概念使用`。公共控制是可见类型指针编译，正向服务构造另由完整矩阵实际执行；没有把预期负编译失败算作产品编译失败。

| 汇总八场景 | 本子项证据与限制 |
| --- | --- |
| 成功 | 实际空域登记、合法纯概念创建/查询及完整终态通过。 |
| 精确重复 | 原布局键、概念创建/生命周期/退出原请求、首次 H 和原账读回通过；原 G0 异义另列 FAIL。 |
| 合法拒绝 | 20 个真实概念引用保护、预算/当前 G 拒绝通过；8 个不可公开构造引用由 L1 先拒绝。补测精确分类失败阻断总体 PASS。 |
| 外部失败 | 本消费者未注入资源耗尽/19/21；持久材料损坏和占用由独立 L1 子项负责，不借用其结果冒充本项运行。 |
| 并发 | 25 + 25 轮同实例、同 G0 双线程竞争；跨实例竞争 NOT_RUN。 |
| 崩溃恢复 | kill、断电、介质故障 NOT_RUN；本项没有磁盘或真跨进程恢复声明。 |
| 正式读回 | 公开当前、历史、墓碑、完整首次写集/结果及 owner 端口销毁后重签发读回；没有私有数据探针。 |
| 正式构建 | 本独立 21 模块消费者两配置实际编译/链接通过；主工程和生产隔离检查由根负责。 |

## 4. 具名失败 CONCEPT-REGISTRATION-G0-01

A v0.6 详细设计 E.1 / E.4（895、952 行）要求：原请求按原 G0 重放；同键改变原 G0 / 写集返回幂等冲突。真正不同布局键或旧布局则保持旧格式不支持（900 行）。候选 `登记纯概念结构_v2` 的 6253–6256 行把任何首次规范化写集差异统一映射为 22，混淆了这两类事实。

补测源码 `P/supplement.cpp`，SHA256 `A2E577A4C0AD8C66BC045C173B342775FA3F3A914AC2E0C787370FBA6530AB6B`。先通过合法 v2 原登记和服务构造；销毁服务后重签发原 owner 端口，复制原请求，只执行 `++changed.G0`，固定同键 `0x7142`（28994）和全部原布局材料。两配置实际值相同：

| 量 | 值 |
| --- | --- |
| 合同版本 / 原键 | 2 / 0x7142 |
| 原 G0 / 改后 G0 / 原发布 H | 15 / 16 / 16 |
| 调用前 / 后当前 G | 17 / 17 |
| 期望 / 实际状态 | 幂等冲突 16 / 旧格式不支持 22 |
| 发布状态 / 交付 / 返回首次 H | 未进入 0 / 无 / 无 |
| 原首次规范化写集及首次结果 | 调用前后完整 optional 内容相等 |

零事实和原账不变、无成功载荷的断言均在精确状态断言之前通过；因此已观察到的是非成功分类错误，没有观察到事实损坏。Debug 和 Release 的补测运行均 exit 1。原日志和失败输入保留，不降判据或修改候选。

不能简单把所有 22 改成 16：真实 v1 旧布局在同固定登记键的 H 合同仍必须拒绝为 22。该旧布局分支属于既有 H 证据，本子项未另行重跑。修复后必须以新精确提交复验合法 v2 原键异义，并保留真正旧布局拒绝边界。

## 5. 路径事故及证据影响

`INCIDENT-001`：复制的 PowerShell 脚本包含使用正斜杠的旧目录字面量，替换代码却使用 Windows Path 产生的反斜杠字符串；替换未命中，原检查也检查了错误拼写。验收者误启动 Debug/Release 两条命令进入旧 `01a06f6b-pure-concept-regression-4c591208` 根。此为资源边界违例，已即时告知根及旧资源所有者；两次运行全部排除于本次独立结果。

实际提交命令为 P/run-regression.ps1 的 `-Configuration Debug -Round 10` 与 `-Configuration Release -Round 10`；脚本实际 MSBuild 工程和 exe 却位于旧根。Debug 发生在 19:27:12.7804267–19:27:51.6220148 +08:00；Release 在 19:27:24.6437996–19:27:30.7283891 +08:00。两次 build/run 均 exit 0，均正常退出，没有大范围 kill。原 helper 未记录操作系统子进程 PID，明确 NOT_CAPTURED；Debug 工具 session 78226 不是 PID，不能补造。实际完整命令见 incident.json。

只读逐项复核旧 evidence-manifest.json 共 1432 项：1426 MATCH、6 CHANGED、0 MISSING。六个改变项为两个 build 日志、两个 execution JSON、两个 output exe。原运行日志虽被覆盖，内容恰好仍与原 SHA 相同。另 16 项未纳原清单的构建产物在事故时间窗修改，只有现 SHA / 时间，没有原 SHA，不能声称为新建。原、新 SHA、分类及完整逐项结果见 incident.json；六项差异也复制进入本记录 JSON。

事故后未回写旧资源或伪恢复。A 20 个生产、工程和冻结工具输入经规范化字节核对全部与候选一致；事故没有改动生产或主工程。A 通过仍是历史观测事实，旧现场证据已部分改变，不再声称旧目录完整保持原冻结指纹。

修正脚本使用唯一明确的新根，并断言根等于脚本目录；重新静态扫描驱动、项目、导入/输出/中间目录和执行参数。`path-preflight-final.json` 中 9 个实际入口均无旧资源可写路径。随后在 P 独立构建、运行并重新生成本次全部证据；归档的错误脚本和 provenance 只读保留，不属于可执行入口。

## 6. 冻结回传与未测范围

| P 内文件 | SHA256 |
| --- | --- |
| result.json | B868BB12238CF9D31A923B95616BE8B0C917B3C25D8F014DC2EF58CAE9E56488 |
| input-manifest.json | 8DE1C936F1B55406E391A772141B2A4FF06ACB555E46C51CB2B473BD03E72DB4 |
| artifact-index.json | 06B3E8FC6BA452729681A77985B0008A919A6CD856A232F70F5BC37CD0C75F31 |
| incident-001/incident.json | 62D385D245C6B33D118E95B2D5536B237573791EE7F6EFC793595F8DA5351FB9 |

本子项停止新增测试并保留 f5 原候选的 FAIL。未运行真实 kill/crash、断电、介质故障、资源耗尽、长时可靠性、跨实例概念竞争、19/21 注入和纯概念持久跨进程恢复；未重复 H07、世界 A4 或主工程。不以部分通过提升整体 L1 状态，不替根生成最终集成验收门禁结论。没有本子项提交或推送，交由唯一发布所有者整合。
