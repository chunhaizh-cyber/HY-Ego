# DATA-L1 两项中性读取及概念消费者独立验收

日期：2026-09-13

状态：**事实失败（FAIL），本候选验收已收口。** L1 子范围通过；概念登记原 G0 异义分类失败，阻断整体通过。

## 1. 对象与依据

唯一代码候选为 `f5dda0a5ec920e8ebe72f441c813bf8151b2f946`，已普通推送至 `origin/main`。本次只消费该提交的 Git 快照，不消费随后 WORLD-TREE-STRUCTURE-B 的工作区变化。

依据为该候选中的 4015 第 9 节、4070、4080 v1.1 和 WORLD-TREE-CONCEPT-FIRST-A v0.6 设计及计划。ACCEPTANCE-01 的一般 DATA 施工规则不要求另建完整验收阶段；本次仅执行 4080 v1.1 对两个新增读口、统一黑盒及独立验收的明确要求，不扩展到世界树全类型 CRUD 或完整自我治理。

目标包括原 43 项冻结操作的兼容回归、`读取所有者范围空域`、`读取节点全部当前引用`，以及真实概念服务对两项读取的登记和引用保护消费。只读结果不得写入事实，失败必须清空不完整载荷；概念操作须由其正式公开服务产生结构化结果，并通过权威当前、历史和首次材料读回核验。

代码作者与本次复核分开：L1 由未编写其实现的 `/root/world_tree_execute` 复核；概念消费者由未编写概念实现的 `/root/l1_read_execute` 复核；`/root` 负责独立的主工程构建、隔离检查和证据整合。两位复核者不得修改产品代码或以自己编写的实现作为独立验收对象。

## 2. 独占资源

- L1：`D:/TEMP/海中鱼巣/INT-DATA-L1-TWO-READ-CONSUMER-A/01a06f6b-l1-review-a913`。
- 概念消费：`D:/TEMP/海中鱼巣/INT-DATA-L1-TWO-READ-CONSUMER-A/01a06f6b-concept-review-b407`。
- 整合及正式构建：`D:/TEMP/海中鱼巣/INT-DATA-L1-TWO-READ-CONSUMER-A/01a06f6b-integration-fb26`。

各目录已在统一文件登记中分配独立所有者。只使用这些目录内的构建、模块、可执行文件和测试数据，不运行用户默认数据根，不修改 LOCALAPPDATA，不共用原 A 验证输出。旧夹具和固定样本只读复制并核对来源；生产源从候选 Git 重新导出。

整合源码归档 SHA256：`DBCC87B138A3F6B017B18B1F9CEC30546EDCA2C9BEC5D42CDCB73670030DAAA9`。实际生产工程包含 57 个编译单元；项目列表和直接导入扫描未发现测试编译项、测试专用入口或自检模式，完整清单位于 `production-isolation.json`。

## 3. 开始前冻结的矩阵

| 场景 | 本次应证明的内容 |
| --- | --- |
| 成功 | 两项完整中性读取、概念首次登记及创建的结构化结果与权威终态。 |
| 精确重复 | 原请求、原身份及首次 H 保留，重复读取或重放不增加事实。 |
| 合法拒绝 | 空域、预算、七类引用、跨 owner、原键异义和引用保护等拒绝，零未授权改变。 |
| 外部失败 | 已授权恢复材料缺失、损坏、版本及占用等精确拒绝，不交付半结构。 |
| 并发 | 同 G 守卫、唯一端口及同义概念竞争后恢复唯一终态。 |
| 崩溃恢复 | 真实 kill、断电和介质故障不在本次执行范围；受控销毁后新进程恢复另列实际证据，不称为崩溃试验。 |
| 正式读回 | 公开当前、历史、墓碑及首次账；受控新运行包或新进程独立读回。 |
| 正式构建 | 精确候选的 Debug/Release 主工程构建及生产测试隔离。 |

## 4. 当前检查事实

候选快照上的 `check_l1_frozen_surface.py --strict` 已通过：45 项操作、5 个冻结文件。`check_specs.py --strict` 已通过：124 份目录项。主工程 Debug/Release 均完成 Rebuild，退出码均为 0；L1 独立统一驱动在两配置均通过原兼容矩阵及各 322 项新增断言。概念复核的原 35 项、并发和私有权限检查通过，但新增登记原 G0 异义用例失败。编译器采用原 MSVC 14.50.35717、v145 和项目原优化配置，只隔离输入及输出目录，没有运行普通程序。

最终结论由本目录 `manifest.json`、两个独立复核记录及结构化证据共同承载。根协调者已读取两份独立回执、两配置原 G0 失败日志及 L1 运行日志，并核对主工程构建与隔离证据。本记录固定原 f5 候选的失败事实；后续修复复验须产生新候选记录，不把本文件改写为通过。

### 4.0 新增拒绝分支失败

在合法 v2 概念布局已经登记后，独立复核保持原登记键和全部布局材料，只把请求原 G0 从 15 改为 16。Debug/Release 补测均实际返回 `纯概念状态::旧格式不支持`（22），而 A v0.6 详细设计 E.1、E.4 明确要求 `幂等冲突`（16）。两次补测构建均成功、运行退出码均为 1；调用前后当前 G 均为 17，原首次写集、首次结果均未改变，结果没有交付布局或首次 H。这是非成功分类错误，不是数据损坏。

源码位置为 `数据服务.概念树类.ixx` 的 `登记纯概念结构_v2`：它把任何首次规范化写集不等统一映射为 22。修复必须区分合法 v2 原请求异义和真正旧布局不支持，不能简单地把所有旧格式拒绝改为幂等冲突。该问题属于 A 已冻结合同内的实现错误，须由代码所有者修复并产生新候选；本次验收者不修改产品源码。原 35 项、并发和私有权限检查通过不能抵消这一新增分支失败。

### 4.1 验证资源误用事故

概念复核者复制旧脚本后，路径分隔符差异导致旧根常量未被替换；误启动的 Debug/Release 命令进入旧 `01a06f6b-pure-concept-regression-4c591208` 目录。对旧清单 1432 项复核后，1426 项保持原 SHA，6 项改变，0 项缺失；改变项是两个构建日志、两个执行 JSON 及两个 exe，原运行日志的内容未变。另有 16 项原清单未记录的构建产物在事故时间窗内修改，不能据此声称它们为新建文件。

这两次运行全部排除于新验收，不覆盖或伪造恢复旧证据。两次命令均正常退出；原脚本没有保存操作系统子进程 PID，该项明确为 `NOT_CAPTURED`，没有猜测或使用大范围终止。详细命令、时段和原、新 SHA 随 `concept-review.md` 和 `concept-evidence.json` 归档；原始 `incident-001/incident.json` 的 SHA256 为 `62D385D245C6B33D118E95B2D5536B237573791EE7F6EFC793595F8DA5351FB9`。

本次事故发生后，根协调者重新核对 A 的 20 个生产与冻结工具输入，20/20 仍与已发布 A 的源码清单相同，生产代码及主工程没有新增差异。A 提交前实际执行和观测到的结果仍为历史事实，但不再声明旧概念验证目录全部保持原冻结状态；本次验收须以修正路径后在新独占目录重新产生的结果为依据。

## 5. 声明边界

本次不声称真实断电、kill、介质故障、资源耗尽、长时压力、任意旧格式、世界树 B—E 或自我线程已经验证。概念 35 组中公开不可构造的引用位置若由 L1 拒绝，必须与实际进入概念服务的保护用例分账。

4080 的整体能力和冻结清单继续保持 `PENDING_ACCEPTANCE`。只有后续修复候选通过具名独立复验，且记录提交推送后，才能由另一次具名治理发布绑定候选与验收记录；验收者不自行修改冻结状态。
