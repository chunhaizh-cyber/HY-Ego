# 实际存在登记一致当前投影消费者独立验收

日期：2026-08-08

## 结论

`服务验收通过`。

精确候选提交 `c5e6c868c61b96dbb7f3e37ccd3bc033953d3acc` 已推送并由 `origin/main` 到达。Debug 与 Release 均从候选提交的隔离归档完整重建，正式生产目标以 `--runtime-context` 正常结束。生产代码之外的消费者驱动只导入生产运行期公开模块，经 `生产运行期会话::启动` 取得结构化成功和有效租约，再通过租约公开入口对实际存在登记、初始场景和世界登记各连续读取两次。

两种配置的六次公开读回均为结构化已读取。两份实际存在登记逐字段一致，两份初始场景逐字段一致，两份世界登记逐字段一致，三类事实的共同截止均为第四代。实际存在登记的两个身份非零、互异，合同、规则和首次请求身份均符合公开合同。

本结论验收的是实际存在登记这一第二层消费者的权威当前读取服务，不是第一层自身。它只为第一层新增一项实际使用证据：该消费者为完成自身登记读回，真实使用一次指定事实代次的一致投影读取两个节点；当后继业务已经推进事实代次时，读取根通过一次完整重做收敛到当前截止。既有两节点创建能力不在本轮重复计数。

## 实际消费者链

```text
生产运行期会话::启动
-> 初始化世界登记
-> 初始化初始场景
-> 建立实际存在登记
-> 发布后继初始场景实际存在并推进事实代次
-> 启动结果返回结构化状态与有效租约
-> 运行期上下文::读取实际存在登记（连续两次）
-> 实际存在服务按登记读取两个权威节点
-> 运行期上下文::读取初始场景（连续两次）
-> 运行期上下文::读取世界登记（连续两次）
-> 比较三对结果和共同事实截止
```

消费者驱动没有导入或直接调用第一层模块，没有访问私有仓、锁、候选、事务或恢复材料，也没有进入生产工程。

## 结构化结果

Debug 与 Release 结果完全一致：

```json
{"通过":true,"启动状态":1,"世界登记启动状态":1,"初始场景启动状态":1,"实际存在登记启动状态":1,"实际存在读回状态":3,"初始场景读回状态":3,"世界登记读回状态":3,"发布状态":1,"合同版本":2,"规则版本":1,"首次幂等身份":1,"事实截止":4,"服务身份":10,"资格属性类型":11}
```

这些数值仅保存结构化枚举和公开事实字段；验收判断来自强类型结果、逐字段比较和公开终态，不来自日志文本或生产退出码。

## 证据

- [命令与退出码](证据/命令与退出码.md)
- [结构化结果与覆盖边界](证据/结构化结果与覆盖边界.md)
- [函数巡检](证据/函数巡检.md)
- [生产外消费者驱动](证据/实际存在登记一致投影消费者驱动.cpp)
- [机器清单](manifest.json)

## 允许与禁止声明

允许声明：实际存在登记消费者已经通过正式生产会话和公开运行期上下文，连续取得结构化一致的登记、场景和世界事实；第一层的一次指定代次一致投影及一次有界完整重做足以支撑该消费者实际需要的两节点当前读取。

禁止声明：第一层自证或已经整体完善；不得把本轮重复计为实际存在登记创建证明；不得声明值、属性、关系、关系组、修改、退出、历史、实际存在资格读取、其它消费者、并发、崩溃或跨进程恢复已由本轮覆盖。
