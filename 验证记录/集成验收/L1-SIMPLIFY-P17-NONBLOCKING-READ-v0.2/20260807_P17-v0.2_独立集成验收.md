# P17 v0.2 非阻塞 L1 当前快照与事实代次读取独立集成验收

日期：2026-08-07

结构化结论：`INTEGRATION-ACCEPTANCE-PASS`

人读结论：`自检通过 / 组件局部可继续 / 未正式接线`

候选提交：`35f5288ba8aeb6558ce48a8deaea4da1a0b52222`

正式 plan blob：`8c830e9016553315f826d359e7f614af3d40d846`

## 1. 结论

固定候选能够在隔离归档中完成 Debug/Release x64 重建；同一生产程序 `海中鱼巣.exe --self-test-exit` 两配置均退出 0，stderr 均为 0 字节。双配置 MSVC 模块扫描均为 202/202、failed 0、循环 0、外部需求 0；严格规范检查为 110/110。

源码闭包证明两个新公开服务入口分别只向唯一 L1 仓库转发；仓库入口各执行一次 `std::try_to_lock`，竞争立即返回 `许可拒绝`，没有 sleep、等待或循环重试。Debug 自检实际覆盖共享并发、独占竞争、损坏、资源失败和未知异常；Release 构建排除 Debug 故障辅助，并以四个占位真值保持自检数量，因此不得把 Debug 故障注入矩阵写成 Release 实际运行证明。

候选没有生产消费者。两个公开入口除服务转发和定义外，调用全部位于 `核心/自检.L1事实基座.ixx`；启动只通过既有顺序 270 的 `L1-SIMPLIFY-P0-S01` 聚合自检进入。P17 没有具名生产日志，运行输出也没有 P17 具名摘要。因此本轮最多支持“自检通过、组件局部可继续”，不能升级为生产接线或服务验收通过。

## 2. 固定输入与隔离

- 候选是 `origin/main` 可达祖先，唯一父为 `5868970df2ff9f4819263a4f1b9c37ed1dce4553`。
- 候选提交精确修改五个 L1 核心文件，并新增 P17 施工、验证记录，共七文件。
- 验收只使用候选 `git archive` 副本；归档本地 Git 提交只供模块扫描读取标识，源码全部来自固定候选。
- 隔离根：`C:\Users\zhchh\AppData\Local\Temp\P17-v02-accept-35f5288b-20260807-a1`。
- 未读取工作区代码 WIP 或仓库 `.codex-build`；P0-FIX、路线、旧 WORLD-TREE 与计划索引 WIP 均未纳入验收输入。

## 3. 八类场景

| 场景 | 结果 | 事实边界 |
| --- | --- | --- |
| 成功 | PASS | 空快照成功/代次0；非零状态快照与代次一致；退出后只返回当前事实。 |
| 精确重复 | PASS | 同状态重复快照逐字段一致，事实代次不变，不产生写入。 |
| 合法拒绝 | PASS | 合同版本错误返回入口拒绝；Debug 独占竞争立即返回许可拒绝。 |
| 外部失败 | NOT_APPLICABLE | 入口只读取进程内 L1，不访问 SQL、文件、网络或外设；Debug 资源/未知异常映射另作组件故障矩阵覆盖。 |
| 并发 | PASS | 两个共享读者均成功且一致；Debug 独占竞争耗时门禁小于 100ms，释放后恢复成功。 |
| 崩溃恢复 | NOT_APPLICABLE | 本切片纯读取、零权威写入，且用户明确当前不考虑崩溃和跨进程恢复。 |
| 正式读回 | NOT_APPLICABLE | 本切片本身就是读取入口且没有生产消费者；自检重复读取不是生产正式独立读回。 |
| 正式构建 | PASS | Release x64 重建和同一程序运行通过；P17 Debug 故障标志与辅助函数不进入 Release。 |

## 4. 函数闭包

候选新增或直接修改 9 个项目函数，另有 2 个 `= default` 比较运算符按编译器生成函数排除。9 个项目函数均完成编译接入和安全入口巡检，逻辑异常数 0；逐项证据见 `证据/函数巡检.md`。

## 5. 允许与禁止声明

允许声明：

- 固定候选的 P17 L1 组件一次 try-lock 当前完整快照与当前事实代次读取通过双配置构建和自检。
- Debug 实际覆盖并发竞争、损坏、资源失败与未知异常；Release 只证明正式代码构建、链接、运行及 Debug 故障辅助隔离。
- P17 组件局部可继续，足以作为后继设计和更高层接线验证的候选输入。

禁止声明：

- 不得声明 P17 已有生产消费者、正式接线或服务验收通过。
- 不得声明 P15 正式接线或正式独立读回通过；P15 仍保持 `INTEGRATION-ACCEPTANCE-FAIL / 未正式接线`。
- 不得把退出式自检、控制台输出或日志当作生产机器事实。
- 不得声明 Release 实际执行了 Debug 故障注入矩阵。
- 不得声明指定根世界结构、存在/特征/状态/动态读回、完整世界快照、崩溃恢复、STEP-4 或治理闭环完成。
