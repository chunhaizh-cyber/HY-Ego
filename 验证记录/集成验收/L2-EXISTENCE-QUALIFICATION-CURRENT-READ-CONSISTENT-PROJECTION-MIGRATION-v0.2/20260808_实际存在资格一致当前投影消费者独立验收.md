# 实际存在资格一致当前投影消费者独立验收

日期：2026-08-08

结论：`INTEGRATION-ACCEPTANCE-PASS / 服务验收通过`

## 1. 验收对象

精确候选提交 `c71e3b6c0d38e23f984351346f205ef8d8a26870` 已推送并可由 `origin/main` 到达。验收只使用该提交的临时归档，不读取当前工作区的异主修复、路线、旧计划或构建现场。

主对象是初始场景实际存在这一第二层服务对“实际存在资格显式定位读取”的真实消费。生产代码之外的驱动只导入生产运行期公开模块，经 `生产运行期会话::启动` 取得结构化成功和有效租约，再通过租约连续读取初始场景实际存在、实际存在登记、初始场景和世界登记。

本验收不是第一层验收。第一层的一致当前投影只作为第二层服务为完成自身合同而真实使用的底层能力，不形成另一份第一层通过结论。

## 2. 实际结果

Debug 与 Release 均完成：

- 候选原生产项目完整重建并以正式运行期参数退出零；
- 生产外消费者驱动完整重建并退出零；
- 启动结果为已启动，初始场景实际存在初始化为已提交，上下文为已发布；
- 初始场景实际存在、登记、场景和世界各连续读取两次，八次均取得结构化成功；
- 每一对结果逐字段一致，四类事实的共同截止均为第四代；
- 资格值编码、资格属性类型和资格来源与登记逐字段互证；
- 消费者没有导入或直调第一层，没有读取日志、私有仓、锁或事务。

两种配置得到相同结构化结果：

```json
{"通过":true,"启动状态":1,"初始场景实际存在启动状态":1,"实际读回状态":3,"登记读回状态":3,"场景读回状态":3,"世界读回状态":3,"发布状态":1,"共同截止":4,"存在":12,"资格值":14,"资格属性类型":11,"资格来源":10}
```

## 3. 判定

本轮允许声明：初始场景实际存在这一合法第二层消费者，能够经正式生产会话取得结构化成功，并通过公开租约连续读回完整资格、登记、场景和世界事实；实际存在资格显式读取的一致投影足以支撑该消费者当前合同。

本轮不得声明：第一层自证或第一层整体完善；无显式资格值的当前定位分支、显式值未找到 / 已退出 / 非当前等失败分支已经运行；其它消费者、修改、退出 / 历史、并发、崩溃或跨进程恢复已经证明。

第一层是否对当前项目已完善，仍由当前全部第二至第五层数据服务在实际运行中完成自身合同的累计结果判定。
