# METHOD-ROOT-R1 v0.1 方法登记根当前读取独立集成验收

日期：2026-08-07

结构化结论：`INTEGRATION-ACCEPTANCE-FAIL`

人读结论：`事实失败 / 组件自检通过但验收合同未闭合 / 未正式接线`

候选提交：`be35ff14fa72adad91297225fa010ffc69ce26f7`

正式 plan blob：`e2c8b9de92ce0a4c3701a17de9a474b407b008be`

## 1. 结论

固定候选在隔离归档中完成 Debug/Release x64 重建；同一 `海中鱼巣.exe --self-test-exit` 两配置均退出 0，R1 专项均显示 `checks=14 failed=0`，stderr 均为 0 字节。双配置 MSVC 模块扫描均为 204/204、failed 0、循环 0、外部需求 0；严格规范检查为 110/110。

这些结果只能证明14项既有组件自检和编译接入通过，不能抵消以下正式验收失败：

1. 15个新增或直接修改的项目函数中，只有既有 `运行完整自检模式()` 保留一条P14来源的“向上送出”责任备注；其余14个新增函数均没有登记 `本地记录 / 向上送出 / 无适用错误分支`。这违反 ACCEPTANCE-01 对新增和直接修改函数的逐函数诊断责任门禁。
2. `映射读取失败(...)` 只被 `static_assert` 编译求值；实际14项运行矩阵没有使P17返回未找到、许可拒绝、资源失败、内部不一致、坏成功或未知状态，因而该函数没有运行期入口巡检证据。
3. 正式计划要求专项覆盖P17全部相关状态、坏成功、未知状态、异常映射、P17调用次数和禁止写入/provider为0。当前专项没有这些运行观察，也没有可从公开入口确定触发它们的正式夹具；不能用分支存在或P17自己的自检替代R1映射结果。
4. 计划要求正式日志人读核对专项、状态、事实截止和调用次数。候选对R1的日志调用为0，只通过 `std::cout` 输出标识、通过/失败、checks和failed；固定日志目录R1命中为0。该控制台摘要既不是正式日志，也不包含状态、截止或P17调用次数。

因此本轮结论必须是 `INTEGRATION-ACCEPTANCE-FAIL / 事实失败`。失败属于验收合同和诊断责任未闭合，不表示已经执行的14项自检产生错误机器事实。

## 2. 固定输入与隔离

- 候选唯一父：`9f01335bac6de210921cd5b5d7f051e006f514ab`。
- 候选精确修改工程、filters、启动三个文件，新增三个代码文件和两份专属记录，共八路径。
- 验收只使用候选 `git archive`，归档本地Git提交只供模块扫描读取标识。
- 隔离根：`C:\Users\zhchh\AppData\Local\Temp\METHOD-R1-accept-be35ff14-20260807-a1`。
- 验收期间正式HEAD前进至`163f10db596e47a792137046088b9d11db49f221`；`be35ff14..163f10db`精确为R9三份设计文档和计划索引一行，R1候选代码与本验收目录零路径重叠。
- 未读取工作区代码WIP或仓库 `.codex-build`；R9、P0-FIX、路线和旧WORLD-TREE均未纳入验收输入。

## 3. 八类场景

| 场景 | 结果 | 事实边界 |
| --- | --- | --- |
| 成功 | PASS | 自检内真实L1发布三节点、一值、一槽后，R1返回已读取和完整角色值见证。 |
| 精确重复 | PASS | 同请求重复读取逐字段相等，R1自身零写。 |
| 合法拒绝 | PASS（不完整） | 入口非法、全无、半结构、无槽、旧角色、错来源和错表示已覆盖；P17非成功映射未运行。 |
| 外部失败 | FAIL | 计划要求的P17资源失败和异常映射没有运行证据。 |
| 并发 | FAIL | P17许可拒绝映射、调用次数和零重试没有在R1正式入口运行观察。 |
| 崩溃恢复 | NOT_APPLICABLE | 本切片纯读取、零权威写入，且用户明确当前不考虑崩溃恢复。 |
| 正式读回 | NOT_APPLICABLE | 当前只有自检消费者，尚无生产合法消费者或正式独立读回。 |
| 正式构建 | PASS | 双配置重建同一生产程序；没有第二exe或独立自检目标。 |

## 4. 消费者与权威边界

`读取当前方法登记根`除定义外只在 `领域/自检.L1方法登记根当前读取.ixx` 调用；启动仅以顺序583注册同一程序的退出式自检。没有生产业务消费者、生产定位凭证来源或正式方法登记根发布结果。

自检成功使用真实进程内L1事实基座，证明隔离夹具中的结构化结果；它不证明生产中已经存在登记根、定位凭证或调用路径。日志和控制台输出均不是机器权威事实。

## 5. 允许与禁止声明

允许声明：

- 固定候选双配置构建、自检14/14、模块扫描204/204和strict 110/110通过。
- 已执行的成功、重复、入口拒绝、全无、半结构、无槽、旧角色、错来源和错表示组件路径未观察到运行错误。
- 候选验收失败点已定位为逐函数诊断责任、P17非成功运行矩阵和正式日志观察未闭合。

禁止声明：

- 不得声明R1组件局部可继续、服务验收通过或生产接线完成。
- 不得把P17自身自检、`static_assert`、控制台摘要或日志缺失解释为R1失败映射已运行。
- 不得声明方法登记根或定位凭证已发布、方法已登记或可执行。
- 不得声明需求任务方法承接、崩溃恢复、STEP-5或治理闭环完成。
