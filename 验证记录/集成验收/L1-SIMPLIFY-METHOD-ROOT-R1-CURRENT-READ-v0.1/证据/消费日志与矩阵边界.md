# METHOD-ROOT-R1 v0.1 消费、日志与矩阵边界

## 消费者

候选中 `读取当前方法登记根` 的全部调用位于：

```text
海中鱼巣/领域/自检.L1方法登记根当前读取.ixx
```

启动闭包为：

```text
海中鱼巣.exe --self-test-exit
-> 运行完整自检模式()
-> 顺序583 L1-SIMPLIFY-METHOD-ROOT-R1-S01
-> 运行L1方法登记根当前读取专项自检()
-> 运行L1方法登记根当前读取自检()
```

没有生产消费者、生产定位凭证形成者或正式方法登记根发布结果。顺序583只构成同一程序自检入口，不是生产服务接线。

## 已执行14项

实际运行覆盖：默认请求拒绝、合同版本拒绝、重复身份拒绝、空L1截止漂移、三定位节点全无、半结构、完整结构成功、精确重复、截止漂移、创建见证入口拒绝、无槽、旧角色6、错来源、错属性表示。

## 未执行计划矩阵

- P17未找到、许可拒绝、资源失败、内部不一致、坏成功和未知状态的R1运行映射；
- 角色槽多于1、槽指向缺失/重复/退出值；
- 值错所属、错属性类型、错创建代次和退出值；
- `bad_alloc`与其它异常映射；
- P17成功路径恰调用1次、入口失败调用0次和内部零重试的运行计数；
- 禁止provider调用0和全部写入0的运行观察。

这些分支部分可静态阅读，但正式计划明确要求专项覆盖，不能用源码存在、`static_assert`或P17自己的自检替代R1公开根运行证据。

## 日志

R1新自检和启动包装内 `记录运行日志`、`记录逻辑错误日志` 调用均为0。固定日志目录对 `L1-SIMPLIFY-METHOD-ROOT-R1` 命中0。唯一输出是控制台：

```text
L1-SIMPLIFY-METHOD-ROOT-R1-S01 通过 checks=14 failed=0
```

它不是正式日志，且没有计划要求的人读状态、事实截止和P17调用次数。日志缺失不能证明机器失败，但构成计划验证合同未完成。
