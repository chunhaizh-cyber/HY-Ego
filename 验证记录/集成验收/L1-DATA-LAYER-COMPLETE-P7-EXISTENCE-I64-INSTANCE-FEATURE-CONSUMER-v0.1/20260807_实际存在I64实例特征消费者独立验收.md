# P7 实际存在 I64 实例特征消费者独立验收

日期：2026-08-07

结构化结论：`INTEGRATION-ACCEPTANCE-PASS`

人读结论：`公开黑盒局部通过 / 可作为第二层消费者的 L1 实际覆盖证据`

候选提交：`07771fd4346d687ad58476cc80484eb86ce6be8e`

正式 plan blob：`29d305e5e3b5c0138e3764dc5dcacaab8bc50107`

## 1. 实际结果

生产代码外的单目标第二层消费者只导入 `海中鱼巣.启动.生产运行期`，经正式生产会话、有效租约、`const 运行期上下文*` 和 `const L1实例特征服务&` 调用公开入口。Debug 与 Release 的完整重建和运行均退出 0，结构化结果一致：

- 实例特征登记首次：`已提交`，事实代次 `6 -> 7`；
- 登记精确重复：`幂等读回`，登记、宿主、定义和世界截止不变；
- 初始值 `18` 超过定义上界 `17`：`入口拒绝`，无部分实例；
- 实际存在 I64 实例首次：`已提交`，事实代次 `7 -> 8`；
- 实例精确重复：`幂等读回`，实例及所有上游公开读回不变；
- 同幂等键把初始值 `3` 改为 `4`：`幂等冲突`，无部分实例；
- 不同幂等键占用同一实际存在宿主与同一特征定义：`幂等冲突`，无部分实例；
- 每个非成功前后都重新读取实例登记、实际存在宿主、I64 定义、适用的实例事实和世界当前截止，逐字段一致；
- 首次实例形成实例槽 `27`、宿主关系 `28`、定义关系 `29`、当前值 `30`，四编码非零唯一，I64 值为 `3`。

因此，本消费者实际调用的登记、节点 / 关系 / 值 / 属性槽创建与精确读取、精确重复、同键异义、同宿主同定义唯一性拒绝及越界零变化可以累计为 L1 的消费者覆盖证据。

## 2. 边界

本记录不是第一层自检、自证或整体完善声明。生产构建、日志和退出码只作工程证据；业务结论来自结构化状态与正式公开读回。验收只使用一个生产上下文、一个实际存在宿主和一个 I64 定义，没有双目标模式。

不证明当前值修改、退出 / 历史、关系条件查询、其它消费者、旧栈删除或整个 L1 完善。按本计划和用户当前口径，并发、崩溃、重启、断电和跨进程恢复均不适用；本消费者也不使用 SQL、文件、网络或外设。

验收驱动只在候选 `git archive` 的隔离副本中临时替换入口生成外部验收二进制，不是正式生产目标；驱动未进入候选、工程或生产二进制。本轮未修改产品、工程、规范、设计、计划、索引或其它 WIP。
