# P7 候选函数与诊断责任巡检

候选修改 `服务.L1实例特征.ixx` 与 `启动.运行期上下文.ixx`。正式双配置完整重建证明修改闭包编译接入；以下具名公开闭包由消费者实际进入或按合同排除。

| # | 函数 | 诊断责任 | 本消费者入口 |
| ---: | --- | --- | --- |
| 1 | `运行期上下文::运行期上下文` | 向上送出 | 生产会话启动进入，四引用装配成立 |
| 2 | 非const `L1实例特征服务::建立登记` | 向上送出 | 排除；兼容重载，本消费者持有const服务 |
| 3 | const `L1实例特征服务::建立登记` | 向上送出 | 首次与重复进入并结构化退出 |
| 4 | `L1实例特征服务::读取当前登记` | 向上送出 | 各非成功前后进入 |
| 5 | 非const `建立实际存在I64实例特征` | 向上送出 | 排除；兼容重载，本消费者持有const服务 |
| 6 | const `建立实际存在I64实例特征` | 向上送出 | 越界、首次、重复、同键异义和组合冲突进入 |
| 7 | `读取实际存在I64实例特征` | 向上送出 | 首次后及各适用非成功前后进入 |

分账：目标公开函数7、合同排除2、可验收5、编译接入5/5、入口巡检5/5、逻辑异常0。

新增中性内部函数逐项登记诊断责任：`映射中性幂等键`、`查找中性映射`、`中性映射唯一`、`映射中性写入状态`、`映射中性读取状态`、`中性写入成功合同完整` 为“无适用错误分支”；`形成中性实例写集`、`读取中性当前代次`、`读取中性节点`、`读取中性关系`、`读取中性值`、`读取中性属性`、`读取当前登记已持锁`、`读取实际存在I64实例特征已持锁` 为“向上送出”。成功、重复与拒绝矩阵实际进入其适用成功路径；读取失败专用映射不为覆盖率补造失败。

未迁移的初始场景实例和两个当前值换代入口不属于本验收目标，未为它们建立验收入口或扩大结论。
