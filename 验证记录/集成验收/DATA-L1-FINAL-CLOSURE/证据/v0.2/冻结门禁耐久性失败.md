# FROZEN 门禁耐久性失败

## 变体

变体只存在于：

```text
D:\TEMP\海中鱼巣\DATA-L1-FINAL-CLOSURE-INTEGRATION-ACCEPTANCE\v0.2-9b1649d9-01\neg-run4\15-durability
```

基线仍由候选 `9b1649d91cacc1a9353bea5bddb161ebda510367` 的归档重新展开。隔离变体只做：

1. 冻结清单改为 `data_l1_status=FROZEN`；
2. `accepted_commit` 故意填写 `0000000000000000000000000000000000000000`；
3. `acceptance_record` 指向 `验证记录/集成验收/DATA-L1-FINAL-CLOSURE/manifest.json`；
4. 该验收清单保留 `结论.类型=事实失败`，只把记录的远端提交刷新为当前 `origin/main`，使身份字段可核对；
5. 4080 的整体状态行由 `PENDING_ACCEPTANCE` 改为 `FROZEN`；规范目录保持候选原文。

## 实际结果

```text
python tools/check_l1_frozen_surface.py --strict
exit 0
DATA-L1 冻结面检查通过：43 项操作、5 个冻结文件与生产边界一致
```

预期必须为非零，因为验收结论不是 `服务验收通过`，且 `accepted_commit` 与验收清单候选提交不一致、甚至不是有效提交。实际 exit 0，构成可稳定复现的事实失败。

对应汇总末尾：

```text
15-durability-fail-record-and-mismatched-commit: exit=0; expected_nonzero=False
ALL_15_STANDARD_NEGATIVE_EXPECTATIONS=True
DURABILITY_GATE_REJECTED_INVALID_FREEZE=False
```

根因位于候选 `tools/check_l1_frozen_surface.py` 的 FROZEN 状态路径：它只运行 `check_integration_acceptance.py --manifest ... --verify-git --strict` 并观察退出码；该检查器允许结构合法的 `事实失败` 清单退出 0，而调用方没有读取并要求 `结论.类型=服务验收通过`，也没有比较冻结清单 `accepted_commit` 与验收清单 `基线.候选提交`。同时规范目录检查的摘要片段固定为 `目标合同 FROZEN，整体 PENDING_ACCEPTANCE`，没有随状态轴变化。
