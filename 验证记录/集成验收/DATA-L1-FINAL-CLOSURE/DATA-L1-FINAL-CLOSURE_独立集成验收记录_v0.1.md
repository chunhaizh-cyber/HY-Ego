# DATA-L1-FINAL-CLOSURE 独立集成验收记录 v0.1

日期：2026-09-01

验收角色：集成验收与回归智能体

结构化返回：`INTEGRATION-ACCEPTANCE-FAIL`

最小结论：`事实失败`

## 1. 结论先行

候选提交 `63849adaa916ef69123d3e9b7c3c2c43069d593f` 已推送且等于当前 `origin/main`。43 项冻结公开操作、五个冻结文件、严格规范检查、三类事实恢复、载荷格式 1 / 2 / 3、幂等、拒绝、fail-closed、正式读回以及 Debug / Release 构建均取得通过证据。

但候选不满足正式构建隔离：

1. `海中鱼巣/入口.cpp` 在生产入口 import `海中鱼巣.端到端测试.L1事实基座持久恢复`，并公开 `--test-l1-persistent-recovery`；
2. `海中鱼巣.vcxproj` 共将 9 个 `端到端测试.*` 模块编入正式目标，其中包括 L1 持久恢复和有限 N 原子事务两个专项；
3. 精确候选的 Release 正式程序以该开关实际运行专项、打印 PASS，并以退出码 0 返回。

这直接违反 `规范/运行期服务导向代码验收与全函数入口巡检规范.md` 第 6 节“生产目标零检查专用代码”。因此 `正式构建`、`自检隔离`、`正式构建隔离` 均为 `FAIL`，整体不能形成 `服务验收通过`，不能触发第二次治理发布，DATA-L1 必须保持 `PENDING_ACCEPTANCE`。

## 2. 冻结基线

- 仓库：`D:\海中鱼巣`
- 候选与远端：`63849adaa916ef69123d3e9b7c3c2c43069d593f == origin/main`
- 验收输入：候选提交的 `git archive` 独立导出树，不消费主工作区未提交内容
- 候选归档 SHA-256：`D8C98D60F5F8C478A4D5BF95C203650837DEE0470E94BC58C0E2235CCD1D3945`
- 独立根：`D:\TEMP\海中鱼巣\DATA-L1-FINAL-CLOSURE-INTEGRATION-ACCEPTANCE\run-20260901-63849ada-01`
- 工具链：Visual Studio 2022 Professional 17.14，`PlatformToolset=v143`，x64 Debug / Release

正式输入和 blob 见 `manifest.json`；该清单由 `tools/check_integration_acceptance.py --verify-git --strict` 复核。

## 3. 八类场景

| 场景 | 状态 | 本次证据边界 |
| --- | --- | --- |
| 成功 | PASS | 生产外统一公开 driver 在 Debug / Release 均以 0 退出；节点、关系、值、当前 / 历史 / 墓碑、组 / 一致投影通过 |
| 精确重复 | PASS | v1 / v2 / v3 精确重放、幂等与同键异义分账通过 |
| 合法拒绝 | PASS | 非法 N、身份、owner、幂等、端口、G0、引用和属性槽边界零写通过 |
| 外部失败 | PASS | 清单 / 槽 / 长度 / 摘要 / 未知版本或标签 / 占用等 fail-closed 矩阵通过 |
| 并发 | PASS | 只证明合同规定的 G0 漂移、参与者失败与原子零写；不证明压力并发 |
| 崩溃恢复 | NOT_RUN | 未执行突然崩溃、断电或进程强杀；受控新运行包恢复记入正式读回，不冒充崩溃恢复 |
| 正式读回 | PASS | 三类事实、当前 / 历史 / 墓碑和格式 1 / 2 / 3 均由新运行包恢复并继续 v3 提交 |
| 正式构建 | FAIL | 两配置 fresh Rebuild 均成功，但正式目标携带并实际暴露专项自检开关，不满足隔离合同 |

## 4. 接线与巡检边界

- 正式接线：`PASS`（仅证明候选正式装配 import L1 服务、建立持久运行包，既有 L2 消费者从公开服务取用；不把静态接线扩大为业务验收）。
- 冻结函数范围：4080 的 43 项公开操作；冻结面检查和两配置链接均通过。
- 逐函数入口巡检：本轮没有形成 43 项逐函数的独立进入 / 退出清单，故清单中入口巡检通过数记 0；场景 PASS 只绑定统一 driver 明确输出的行为矩阵。
- 自检隔离：`FAIL`。
- 正式构建隔离：`FAIL`。

## 5. 允许与禁止声明

允许声明：

- 候选的冻结面静态门禁与严格规范目录检查通过；
- 候选在 VS 2022 v143 的 Debug / Release x64 fresh Rebuild 均通过；
- 生产外统一 driver 的三类事实恢复、旧格式 1 / 2、格式 3、幂等、拒绝、fail-closed 和受控冷启动读回矩阵通过；
- 本次失败点是正式目标的检查专用代码隔离，不撤销上述局部通过事实。

禁止声明：

- 不得声明 `DATA-L1` 已整体 `FROZEN`；
- 不得声明 `DATA-L1-FINAL-CLOSURE` 已通过独立集成验收；
- 不得执行第二次治理发布或把 `data_l1_status` 升级为 `FROZEN`；
- 不得把统一 driver、自检 PASS、编译成功或日志文本扩大为服务验收通过；
- 不得声明突然崩溃、断电、真实介质故障、资源耗尽、压力并发、长时运行或真实负载已验收。

## 6. 退回点

这是候选与正式验收规范之间的已证实事实冲突，不是环境阻断。后继修订必须由有权所有者去除生产入口的专项开关，并从生产工程移出全部 9 个 `端到端测试.*` 工程项；仅移除两个 L1 专项仍会被其余七个专项阻断。测试可由不属于生产目标的独立测试工程或仓库外临时 driver 编译。形成新的已提交、已推送精确候选后，必须重新执行本验收，不能在当前候选上补写 PASS。

## 7. 证据

- `manifest.json`
- `证据/命令与退出码.md`
- `证据/正式构建隔离失败.md`

仓库外原始构建日志及二进制保存在上述独立根；其 SHA-256 已写入命令证据。仓库内记录只保留可审计摘要，不把临时构建产物带入 Git。
