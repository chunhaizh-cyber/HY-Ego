# 实际存在当前场景归属条件查询消费者独立验收

## 1. 结论

`INTEGRATION-ACCEPTANCE-PASS`。

候选 `4921b0903b3443fa7ccf9f0e030d174f59167da6` 在生产代码外的合法第二层消费者中取得 `公开黑盒局部通过`。消费者只导入 `海中鱼巣.启动.生产运行期`，经正式会话、有效租约和 const 运行期上下文取得 const `L1实际存在场景组合服务`，创建一个非启动锚定实际存在、经正式组合服务读取成功、退出其当前场景归属关系，再经同一正式组合服务读取为未找到。

这条生产链实际经过第二层场景接纳服务：创建后的读取要求条件关系组恰一项且逐字段匹配；退出后的读取要求相同目标和类型条件查询返回空组。Debug、Release 两配置的原生产目标和外部消费者均完整重建并退出 0，消费者两配置结构化输出一致。

本结论只累计证明这个第二层消费者实际覆盖的目标加关系类型当前查询：关系创建后为一项且匹配，关系退出后为空；同时保留原消费者对关系退出、历史见证、精确重复、同键异义和适用非法请求零变化的覆盖。不证明第一层自检或自证，不证明整个第一层完善，也不证明多项业务关系、关系替换、源端查询、分页、性能、并发或崩溃恢复。

## 2. 基线与隔离

- 候选提交：`4921b0903b3443fa7ccf9f0e030d174f59167da6`
- 正式 plan blob：`f09f5d48c74b934d74ab874fc4050cb8b73dd711`
- 隔离归档：`C:\Users\zhchh\AppData\Local\Temp\hzy-p10-query-accept-4921b090`
- 驱动 SHA-256：`9963150C233B3A051457734E61BB6EF36BD0E4B99F25E9579F6A488C2967E3F9`

归档由候选 `git archive` 展开，不是 worktree，不携带共享工作区 WIP。外部驱动复用上一真实消费者已经发布的单主目标入口，只在隔离归档内临时替换入口用于消费者构建，未修改候选、工程或生产目标。

## 3. 正式消费者链

```text
生产运行期会话::启动
-> 有效运行期上下文租约
-> const 运行期上下文::读取L1实际存在场景组合服务
-> 在初始场景创建实际存在 const
-> 读取实际存在
   -> 第二层读取初始场景接纳
   -> 第一层按成员目标和直接成员关系类型返回当前关系组
-> 移除实际存在当前场景 const
-> 读取实际存在
   -> 第二层读取初始场景接纳
   -> 第一层同条件返回空组
```

驱动不导入或调用第一层、仓、索引、锁、事务私有入口，不解析日志，不建立生产验收入口或双目标。启动锚定实际存在只作为既有错误输入；唯一主目标是新建的非启动锚定实际存在。

## 4. 实际结果

两配置消费者输出一致：

```json
{"通过":true,"创建":1,"无效请求":5,"错误关系":10,"错误代次":7,"移除首次":1,"移除重复":2,"同键异义":6,"创建代次":5,"退出代次":6,"当前截止":6,"存在":15,"资格":17,"退出关系":16}
```

创建后主目标经正式组合读取返回已读取和完整事实；这一步在候选代码中只能经过新的目标加类型关系组读取并取得唯一匹配。首次退出后和精确重复后，主目标经同一正式组合读取均返回未找到；候选代码中该结果来自相同条件查询的成功空组。首次和重复还返回同一完整历史退出见证。

## 5. 过程事实与边界

- 固定提交归档中的原生产 Debug / Release 完整重建及 `--runtime-context` 运行均退出 0。
- 外部消费者 Debug / Release 完整重建和运行均一次通过，结构化输出一致。
- 临时归档位于系统临时目录，构建器发出“临时目录不适合增量生成”的环境警告；本轮使用完整重建，编译、链接和运行均成功，该警告不改变产品结果。
- 候选 `git show --check` 通过；隔离归档 strict 为 110 / 110。
- 当前不考虑崩溃、重启、断电或跨进程恢复，均为 `NOT_APPLICABLE`。
- 不为多项分支伪造第二条业务归属；该分支只以索引与第二层静态不变量留证。

详细证据见 `manifest.json` 与 `证据/`。
