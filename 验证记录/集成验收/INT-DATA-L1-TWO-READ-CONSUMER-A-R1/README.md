# DATA-L1 v1.1 与概念消费者修复独立复验 R1

日期：2026-09-13

结论：**服务验收通过，范围限于 DATA-L1 v1.1 的 45 项操作及两项新增读取的具名概念消费者。** 新候选为 `8ed1c1335f5cfba714602f12aca1e3c7fdcd87a6`，已普通推送。世界树 B—E 和普通自我治理不在本结论范围。

总体清单为 [manifest_v1.1_R1.json](../DATA-L1-FINAL-CLOSURE/manifest_v1.1_R1.json)。使用既有冻结检查器要求的目录、服务名和精确候选标识，不修改检查器或放宽绑定门禁。本记录提交推送后才可另作冻结状态治理；本次不修改 4080 或冻结清单。

## 1. 原失败与修复

原候选 `f5dda0a5ec920e8ebe72f441c813bf8151b2f946` 在合法 v2 布局同键仅改变原 G0 时，返回旧格式 22，应为幂等冲突 16。Debug/Release 均失败，原账和当前 G 不变。原六份失败验收记录已发布于 `138bd4a51e262aa001e7a605ab1c18dcf993ab9d`，保持不变。

新候选只调整概念登记函数：本次完整 v2 写集替换为首次账原 G0 后，若与首次完整写集相等，才返回 16；真正不同布局仍返回 22。DTO、L1、持久格式、私有权限及其它概念操作均未改变。根协调者已逐行检查完整函数和补丁，不把所有 22 改为 16。

## 2. 独立性与精确输入

概念复核者为未编写该概念实现及修复的 `/root/l1_read_execute`。根协调者独立构建主工程、核对 L1 输入等价并整合；原 L1 独立复核者 `/root/world_tree_execute` 未编写 L1 实现。本轮没有让修复作者验收自己的修复。

- 新概念复验根：`D:/TEMP/海中鱼巣/INT-DATA-L1-TWO-READ-CONSUMER-A-R1/01a06f6b-concept-recheck-c220`。
- 根构建及等价核验：`D:/TEMP/海中鱼巣/INT-DATA-L1-TWO-READ-CONSUMER-A-R1/01a06f6b-integration-793c`。
- 原 L1 证据只读引用：`D:/TEMP/海中鱼巣/INT-DATA-L1-TWO-READ-CONSUMER-A/01a06f6b-l1-review-a913`。

源来自新候选 Git archive；根归档 SHA256 为 `39D3D8DF2DE37B9A1DC3E94BF66AEAB99C40B5D143D26E6D4C4D2BA10AAB583D`。概念 38 份输入中只有概念源 blob 改变。新运行脚本实际校验候选、源码 SHA、工程及全部输出路径；构建 TEMP、obj、IFC、exe、测试数据均在本轮各自新根，保存实际 PID/命令/退出码。未运行用户默认数据根，未消费 B 的工作区修改。

## 3. L1 结果承接的确切边界

R1 重新核对原独立 L1 证据中的 11 个源码、8 个格式 1/2 固定样本、5 条冻结输入记录，24 条逐 Git blob 全等（冻结记录与源码有重复项）；18 项主要日志、退出文件、二进制等证据 SHA 全等。新候选冻结面检查和规范严格检查亦通过。

据此承接原同一未变 L1 实现的 45 项逐调用点、4080 九组统一矩阵和每配置 322 项新增断言独立 PASS。**本轮没有重新运行 L1 统一驱动，也不宣称重新生成原 8691 项构建产物。** 具体原测试及未覆盖边界由原 `l1-review.md`、`l1-evidence.json` 承载；等价证明在本轮 `l1-equivalence.json`。

## 4. 本轮实际执行

| 项目 | Debug | Release |
| --- | --- | --- |
| 原完整概念矩阵构建/运行 | exit 0 / 0，35/35 | exit 0 / 0，35/35 |
| 三组登记补测构建/运行 | exit 0 / 0，3/3 | exit 0 / 0，3/3 |
| 精确候选主工程 Rebuild | exit 0 | exit 0 |

每配置原 35 组仍分为 27 个实际概念用例与 8 个 L1 前置拒绝；20 项合法外部引用确实进入概念保护。原矩阵中的真实双线程竞争本次已运行；旧额外 25+25 轮及五私口负编译经未变代码/权限核对引用，明确 `NOT_RERUN`，不计为本次新执行。

三组补测的两配置结果相同：

| 补测 | 结构化结果与权威读回 |
| --- | --- |
| 合法 v2 原键 G0 异义 | 键 0x7142，原 G0 15 改为 16；实际状态 16；G 17→17，无交付/H，原首次完整写集及结果相等。 |
| 原键预算变化及异键 | 预算增至 8192，精确重复状态 3，原 H16、G17；异键状态 22，无新账，原账不变。 |
| 真实 v1 固定键 | 公开 L1 建立旧布局 34 节点、33 关系、7 值；键 0x1401，原 G0=1/H2；实际状态 22，G2不变、完整原账不变。该项不是手工伪造旧持久载荷。 |

编译工具为 VS18 Professional、v145、MSVC14.50.35717；保留 Debug/Release 原优化。主工程实际 57 个编译单元，项目及直接导入没有测试项；此扫描不等于完整调用图或普通程序运行证明。根已读取两配置原始补测日志、退出码及主工程构建尾部，并核对独立补测源码中的状态、原账和 G 断言。

## 5. 证据指纹与限制

根复验目录内：

| 文件 | SHA256 |
| --- | --- |
| l1-equivalence.json | 51DF5F6872DA8A5756D36D534478FFCEB04B9F1B89F6914B9D5282014733E258 |
| static-result.json | 51211D94B305C3092A4A2842F99F8D0AAB0D1AA83B6DB657A3DFC16D1584D8BD |
| main-build-results.json | D09C017247F997AE016FED78866AFA5FD1F7D1E898375982E0641F64E7E1F839 |
| production-isolation.json | 027C94121F788186BC9A45C8B212EA7839765CF7E62BBDFC29CF0F600D1DB426 |

概念输入、完整命令、原始断言结果、逐组归属和最终指纹见本目录 `concept-review.md`、`concept-evidence.json`。

原 `INCIDENT-001` 的两次错误根运行及 6 项旧概念证据变化仍由原失败记录保留；这两次运行不进入 R1。R1 使用新的固定根与拒绝覆盖的脚本，没有再次写旧概念验证目录。

真实 kill/崩溃、断电、介质故障、资源耗尽、长时可靠性、概念跨实例竞争、19/21 注入、任意未登记旧布局及完整自我治理均未验证。受控析构/重开、同运行包原账恢复与真实崩溃恢复分别声明。验收清单检查只证明字段、结论门禁和 Git 绑定，不代替这些运行事实。
