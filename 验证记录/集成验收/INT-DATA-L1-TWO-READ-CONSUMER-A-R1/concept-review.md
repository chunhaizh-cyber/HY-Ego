# R1 纯概念消费者独立复验

日期：2026-09-13；验收者：`/root/l1_read_execute`。

**结论：INTEGRATION-ACCEPTANCE-PASS，仅本次两项 L1 读取的纯概念消费者范围。** 已发布候选 `8ed1c1335f5cfba714602f12aca1e3c7fdcd87a6` 在 Debug、Release 均通过原 35 项和 3 项补测；4 次构建及 4 次运行退出码均为 0。合法 v2 原键仅变 G0 精确返回 16，异键既有域及真实 v1 固定键仍返回 22；完整原账与 G 互证成立。

本记录不声明 L1 自身独立验收、整体 FROZEN、世界树完整 CRUD 或内部治理完成。验收者曾编写 L1，未编写此次概念消费者；只独立复验后者。总体清单由根协调者写入 `验证记录/集成验收/DATA-L1-FINAL-CLOSURE/manifest_v1.1_R1.json`，本子项不修改冻结表或门禁。

## 1. 基线与不可改写的历史

本轮对象 `INT-DATA-L1-TWO-READ-CONSUMER-A-R1`，依据候选内 ACCEPTANCE-01 v0.7、4080 v1.1、4015 第 9 节及 A v0.6 计划、详细设计 E.1/E.4。一般 DATA 不另起独立验收；本轮仍为 4080 两新读口及其实际概念消费的具名例外。

旧候选 `f5dda0a5ec920e8ebe72f441c813bf8151b2f946` 的 `CONCEPT-REGISTRATION-G0-01` FAIL 及脚本路径事故已随 `138bd4a51e262aa001e7a605ab1c18dcf993ab9d` 发布。本复验只记录新候选的实际结果，不回写旧六份记录或旧 TEMP，不抹除旧失败，不把新 PASS 追溯到旧候选。此前事故使旧 A 现场部分证据改变的事实仍按原记录成立。

根具名通知修复提交后才取源和构建；开始及落笔前核对 `HEAD = origin/main = 8ed1c1335f5cfba714602f12aca1e3c7fdcd87a6`，`ls-remote` 相同，index 为空。仅写本回执和 concept-evidence.json；未修改生产、规范、计划或 Git。

独占根 `P = D:/TEMP/海中鱼巣/INT-DATA-L1-TWO-READ-CONSUMER-A-R1/01a06f6b-concept-recheck-c220`。新 Git archive 经路径边界检查后解包，38 份编译/规范输入以 `git cat-file blob` 字节落盘，记录归档字节及精确 Git 字节差异；不将 CRLF 差异当语义变化。所有 21 个生产模块、IFC、obj、exe 及编译 TEMP 均由本资源新建，不复用旧构建产物或默认数据根。

## 2. 变更闭包和复用证据

38 个输入中仅 `海中鱼巣/领域/数据服务.概念树类.ixx` 的 blob 改变，新 blob `9b9abdfaee514f78d1d0c58fcb360674bfa8a503`，精确 Git 字节 SHA256 `6357F52BCD454F7067B4ABF754E23B1EE23C4656597281355CF2C74A42EA6F2F`。其余编译输入和正式依据逐 blob 相同，两个 L1 冻结工具亦逐 blob 相同。

生产差分仅 `登记纯概念结构_v2` 中 6253 附近的 12 行：首次材料不等时，以本次固定 v2 写集只替换为原账 G0 作完整比较；等则分类为幂等冲突，不等保留旧格式不支持。未改公开请求/结果、原账、事务、所有权、私有端口或其声明，未增节点/索引。精确差分见 P/candidate-concept-change.patch。

该入口仍把合法非成功作为结构化状态向调用者返回；本次分支继续保留原读取 G，不生成交付/H。原异常映射和正式读后互证未删改；新增局部写集复制若发生资源异常仍进入原有资源错误映射，但本轮未注入资源耗尽，不能称已执行该分支。

原驱动 driver.cpp 的字节 SHA256 `55A4BD237FD63433629B052376110C8055A5F519F4B9580DF2D5C726F343265A` 保持不变。原严格 G0 补测保留完整断言和次序：先核 G/完整首次写集与首次结果零变化、再核无成功载荷、最后核精确状态 16。新增断言不替代原失败断言。

L1/私有权限未改，本轮未重跑 L1 底座和五私口负编译。只读复核原独立 b407 资源中两配置私口相关 40 份源、响应文件、日志和结果，均匹配已发布原 artifact 清单；`inherited-private-evidence.json` 保存每项 SHA。公共控制原 exit 0，五私口原各 exit 2 且只有 C2248；这是历史继承证据，不是本轮运行。原结果 JSON 中旧 inputManifest 标签不作为当前路径使用，继承包装明确绑定原 prior-input-manifest.json 及实际原命令。原额外 25+25 并发也未重跑；本轮原矩阵各自实际执行一个双线程竞争用例。

## 3. 实际合法消费与权威终态

正式入口包括 `登记纯概念结构_v2`、公开纯概念创建/读取/精确查询/首次结果恢复、`迁移纯概念生命周期`、`退出无引用叶概念`。登记新域实际调用 `读取所有者范围空域`；退役/退出对 C 与 D 通过 `核验纯概念无保护引用` 实际调用 `读取节点全部当前引用`，按完整事实、同 G、预算、去重及允许引用集合判断。

原矩阵通过真实 v2 15 节点、13 关系、格式值和完整首次账建立概念服务，创建真实 FT/FC/EC 与定义，并用公开当前、历史、墓碑及首次材料读回核 C/D、定义成员、族关系、生命周期、首次 H、当前 G、归一化与重放。各成功谓词保留完整互证，没有假成功、私有探针或生产测试钩子。

驱动需要 owner 端口时先销毁概念服务，再经公开签发器重签发唯一端口，完成事实操作后重新构造服务；未借额外活动端口。此恢复是同运行包内服务销毁后的原账读回，不是磁盘或跨进程恢复。world-use 用例只证明中性 E 与真实概念的公开引用消费，不声称完整世界树/绑定业务。

## 4. 运行配置及完整分账

VS 18 Professional MSBuild，v145，MSVC 14.50.35717，x64、C++20、UTF-8、bcrypt；Debug/Release 各自保留原优化配置。完整命令位于 execution-*.json，实际编译/链接选项导出为 compiler-*.txt。每次运行前核候选通知、远端绑定、38 输入 SHA、工程编译项及所有输出路径；根与脚本目录必须一致，结果已存在则拒绝覆盖，PID 在启动时保存。

| 配置/目标 | Build PID / exit | Run PID / exit | 实际结果 |
| --- | --- | --- | --- |
| Debug / matrix | 23724 / 0 | 9072 / 0 | 35/35 |
| Release / matrix | 27628 / 0 | 10736 / 0 | 35/35 |
| Debug / supplement | 27748 / 0 | 24564 / 0 | 3/3 |
| Release / supplement | 11680 / 0 | 24340 / 0 | 3/3 |

原 35 每配置仍分为 27 个概念消费和 8 个 L1 前置拒绝。27 = 7 个一般概念用例 + 20 个真实引用保护；C/D × 七位置 × 同/跨 owner 的 28 个组合中，值属性同/跨 owner 共 4 个、关系类型跨 owner 2 个、值宿主跨 owner 2 个先被 L1 拒绝，不能称进入了概念保护。其余 20 个真实发布外部引用后，核退役/退出拒绝、移除引用后的推进、重新挂引用后的退出拒绝和旧请求重放。新 3 个补测另外分账，合计 76 次具名 case 执行；不以计数取代结构化后置条件。

本轮双线程同 G0 竞争每配置 1 次，核唯一胜者、C/D 与族关系、败者无孤立定义、首次账、旧 G0 和重放。实际仅观察到创建 2、代次漂移 15；19/21 未注入，不把允许分支写成已执行。

## 5. 修复后的精确 16/22 与零变化

两配置以下实际值相同，均由程序内正式 DTO/权威读回断言；日志只是保存这些值供复核。

| 场景 | 请求/布局 | 精确返回及互证 |
| --- | --- | --- |
| 原 G0 异义 | 合法 v2，原键 0x7142；只将 G0 15→16；原 H16 | 状态16；发布0；无交付/H；G17→17；完整原首次规范化写集、首次结果相等。 |
| 同键预算恢复 | 原 v2、原 G0=15，预算4096→8192 | 精确重复3；成功谓词通过，原锚点/原 H16；G17不变，完整原账不变。预算不是布局身份。 |
| 异键已存在域 | 真实 v2 owner，当前 G17；新键0x7199 | 旧格式22；无交付/H；新键前后未找到且无账；G17不变、原键完整账不变。 |
| 真实 v1 固定键 | 正式旧布局34节点、33关系、7值；原键0x1401（5121），原G0=1/H2 | 旧格式22；无交付/H；G2→2；完整原账不变。 |

真实 v1 夹具从已冻结 H07 的 legacy_concept 单函数原样提取，完整原源和提取后 SHA 列于 provenance.json。它通过正式 L1 中性写入建立旧 28 个登记类型、四类根和签名规则，核完整写集/结果与 74 个映射及公开读回；不是把新 v2 格式值改成 1，也未伪造概念 C/D。测试使用同固定键及原 G0，确认修复没有将所有 22 粗暴改成 16。

## 6. 八场景和未测边界

| 场景 | 结论与边界 |
| --- | --- |
| 成功 | 本次新建 v2、纯概念 CRUD、完整正式终态通过。 |
| 精确重复 | 原身份/首次 H/原账保持；预算变化仍恢复原身份，通过。 |
| 合法拒绝 | 精确16、异键22、真实v1固定键22、引用保护、预算/G守卫及零变化通过；8项L1前置拒绝分账。 |
| 外部失败 | 资源耗尽、显式19/21注入 NOT_RUN；不以合法拒绝冒充外部故障。 |
| 并发 | 同实例双线程1+1实际通过；额外25+25仅历史，不称本轮执行；跨实例 NOT_RUN。 |
| 崩溃恢复 | 真kill/crash、断电、介质故障及纯概念持久跨进程恢复 NOT_RUN。 |
| 正式读回 | 当前/历史/墓碑/完整首次账，经合法唯一 owner 端口重签发读回通过；无私有探针。 |
| 正式构建 | 本消费者21模块两配置构建/链接通过；主工程、生产隔离和L1继承审查归根负责。 |

没有运行 H07、世界 A4、主工程或 L1 回归；本轮旧 v1 只复用 H07 夹具来源，不借其持久运行结果。长时可靠性、任意旧布局、世界树 B—E 和完整自我治理均未声明完成。

## 7. 冻结证据与交接

| P 内文件 | SHA256 |
| --- | --- |
| input-manifest.json | C93F50079F88D73E0AD76B3491C9EC6DF63DC31936480C5A5A0122558FAAA9EC |
| result.json | F6343827179F8ADE18E055E29F3EF75938EFF02C8A9A9770C29F5836701735B6 |
| artifact-index.json | A3C907D50E36C12DE57E365E283F0D5D402CD8157B1D1526655ABFB258DEBDEA |

完整输入 blob、源/驱动 SHA、命令、PID、退出码、结构化实际值、76 case 分账、历史继承和禁止声明见 concept-evidence.json 及其指纹引用。旧 f5 FAIL、事故和所有旧目录保持原状。本资源停止新增测试，保留取证材料；无本子项 Git 提交或推送，由根统一验收清单和唯一发布。此结论只证明本候选已发生的概念消费范围可用，不能自行提升 DATA-L1 整体冻结状态。
