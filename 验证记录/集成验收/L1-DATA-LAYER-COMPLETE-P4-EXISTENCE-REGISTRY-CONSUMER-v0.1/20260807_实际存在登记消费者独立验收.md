# L1-DATA-LAYER-COMPLETE-P4 实际存在登记消费者独立验收

日期：2026-08-07

结构化结论：`INTEGRATION-ACCEPTANCE-FAIL`

人读结论：`事实失败 / 合法L2消费者业务闭环运行通过，但逐函数诊断责任门禁未闭合`

候选提交：`7926b1c4ebc3d92e7a5b34884eefca1273c2f874`

## 1. 结论

候选已推送且可由 `origin/main` 到达。验收只使用该提交的 `git archive`，未纳入工作区异主 WIP。Debug/Release 原生产目标均完整重建成功，两配置 `--runtime-context` 均退出0。

生产代码外消费者驱动只导入 `海中鱼巣.启动.生产运行期`，调用 `生产运行期会话::启动`，取得结构化成功与有效租约；随后仅通过运行期上下文公开入口，对 `读取实际存在登记`、`读取初始场景`、`读取世界登记` 各连续读取两次。两配置结果均为：

```text
已启动
世界登记/初始场景/实际存在登记均已提交
候选已发布
三项公开读回均已读取且各自两次全等
实际存在登记合同2/规则1/首次幂等身份1
共同当前事实截止3
服务身份10，资格属性类型11，非零且互异
```

上述证明本具名L2消费者实际使用的两节点新增、公开查询和同一中性写集原子性运行成立；它不是L1自证。

但是，候选五个生产文件的23个新增或直接修改函数中，15个没有按 ACCEPTANCE-01 登记 `本地记录 / 向上送出 / 无适用错误分支` 之一的诊断责任。该门禁适用于新增或直接修改函数，不能由双配置构建或成功业务路径抵消。因此整体验收必须返回 `FAIL`。

## 2. 正式边界

```text
生产代码外L2消费者
-> 生产运行期会话::启动
-> 结构化启动结果 + 有效租约
-> 运行期上下文::读取实际存在登记（两次）
-> 运行期上下文::读取初始场景（两次）
-> 运行期上下文::读取世界登记（两次）
```

驱动没有直调L1、私有仓、锁或内部事务，也没有解析日志作为事实。原生产目标与消费者驱动分别完整重建；驱动只在临时归档内替换唯一外部入口翻译单元，所有生产模块保持候选内容。

## 3. 允许声明

- 固定候选的合法L2消费者业务路径在Debug/Release中均取得结构化成功和公开权威读回。
- 本路径实际覆盖两节点新增、查询和本写集原子性。
- 候选的逐函数诊断责任门禁未闭合，整体集成验收为FAIL。

## 4. 禁止声明与未覆盖

不得声明L1自证或整体完善；不得声明精确重复写、非法请求零变化、修改、退出/历史、关系、值、属性槽、场景内实际存在创建、特征、状态、动态、并发、崩溃或跨进程恢复已验证。

本轮未修改候选代码，未修改工程、规范、设计、计划、索引或状态；未暂存、未提交、未推送。
