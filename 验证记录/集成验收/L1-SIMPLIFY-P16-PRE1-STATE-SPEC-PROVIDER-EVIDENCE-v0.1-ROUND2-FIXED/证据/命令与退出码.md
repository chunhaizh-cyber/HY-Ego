# PRE1 v0.1 修复后第二轮命令与退出码

固定候选：`24431394d65dc849171905ad66ae2f4eaa810cd2`

| 命令 / 检查 | 退出码 | 结果 |
| --- | ---: | --- |
| `git show -s --format=%H/%P 24431394...` | 0 | 候选及唯一父身份一致 |
| `git diff-tree --no-commit-id --name-status -r 24431394...` | 0 | 精确三代码文件和两份原计划记录 |
| 候选archive Debug x64 `Rebuild` | 0 | 单一 `海中鱼巣.exe` |
| Debug `海中鱼巣.exe --self-test-exit` | 0 | PRE1运行日志新增2行，逻辑错误新增0 |
| 候选archive Release x64 `Rebuild` | 0 | 单一 `海中鱼巣.exe` |
| Release `海中鱼巣.exe --self-test-exit` | 0 | PRE1运行日志新增2行，逻辑错误新增0 |
| Debug MSVC模块扫描 | 0 | sources/modules=200/200，failed0，拓扑200，循环0，外部需求0 |
| Release MSVC模块扫描 | 0 | sources/modules=200/200，failed0，拓扑200，循环0，外部需求0 |
| 候选archive `python tools/check_specs.py --strict` | 0 | 110/110 |
| 三代码文件检索 `PRE1-诊断责任` | 0 | 19项；无适用15、向上送出3、本地记录1 |
| 全部生产源码检索P13公开根调用 | 0 | 除定义外只有 `自检.L1状态.ixx` 九个调用 |

隔离根：

```text
C:\Users\zhchh\AppData\Local\Temp\P16-PRE1-accept-round2-24431394
```

归档strict首次曾误用该工具不支持的 `--root` 参数并退出2；验收者随即在归档根目录按正式签名重新运行 `python tools/check_specs.py --strict`，退出0、110/110。前一次属于命令调用方式错误，不是候选规范失败，未用于通过结论。

模块扫描工具依赖Git提交标识，因此只在archive副本建立本地标记提交；候选源码没有改变，模块扫描JSON位于隔离根。固定日志目录只用于运行前后PRE1具名行数比较，日志不是机器权威事实。
