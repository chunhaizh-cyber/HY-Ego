# PRE1 v0.1 修复后第二轮函数巡检

## 1. 影响闭包

影响闭包保持第一次验收冻结的19个新增或直接修改函数，加上两个既有入口闭包 `运行L1状态自检` 与 `运行L1状态专项自检`，共21个目标函数。候选相对第一次验收只为原19函数补诊断责任并增加最终日志观察，不改变函数集合。

Debug/Release Rebuild证明21个函数进入编译和链接；顺序360静态闭包和两配置自检退出0证明21个入口在受控路径中进入并退出。目标21、排除0、编译21、入口21、逻辑异常0。

## 2. 19项诊断责任

| 分类 | 数量 | 函数 |
| --- | ---: | --- |
| `无适用错误分支` | 15 | 身份读取、四个匹配见证、两个P12见证查找、三个P12副本/值匹配、唯一当前、当前同编码数量、包含节点身份、写集覆盖、提供者证据组有效 |
| `向上送出` | 3 | 状态服务规格形成、形成提供者证据、P13公开形成根 |
| `本地记录` | 1 | `运行P13矩阵`专项最终人读观察边界 |

三代码文件中 `PRE1-诊断责任：` 精确19处，分类数量之和为19。15项纯值或有限只读谓词没有外部访问和需诊断失败分支；三个结构化形成函数将合法非成功或异常映射向上；只有P13专项最终汇总边界记录一次人读日志。

## 3. 结构化专项覆盖

`运行P13矩阵` 返回24项布尔矩阵，覆盖：

- 增加、等价、减少和精确重复；
- provider身份、合同/规则、直接调用1、去重provider 1；
- 34项排序去重写前事实身份及同索引副本，33当前、1历史；
- 无前状态、入口拒绝、未找到、不可比较和状态服务直接拒绝；
- 形成前后L1代次不变，生产根零写。

两配置日志摘要均报告 `验收项数=24；失败项数=0`。根函数除定义外只有自检模块九个调用，当前没有P16生产调用；这属于原计划排除边界，不影响自检通过，也不升级为正式接线。
