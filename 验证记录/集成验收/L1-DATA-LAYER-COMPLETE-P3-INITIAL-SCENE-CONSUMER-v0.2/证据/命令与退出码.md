# 命令与退出码

候选提交：`b1f3787a267252cc0eb2c3bee4f0baaca38132fc`

临时归档：`C:\Users\zhchh\AppData\Local\Temp\hzy-initial-scene-consumer-accept-20260807-145949`

临时归档由 `git archive` 从候选提交直接展开，不包含当前工作区的旧修复、路线、旧世界树计划或 `.codex-build`。归档不是工作树，也没有修改正式仓库源码。

## 身份检查

| 操作 | 结果 |
| --- | --- |
| 候选提交格式与空白检查 | 退出 0 |
| 检查候选为 `origin/main` 祖先 | 退出 0 |
| 候选提交文件范围 | 精确六个生产代码文件和两份施工 / 验证记录 |
| 正式规范严格检查 | 退出 0，110/110 |

## 正式生产目标

| 配置 | 原项目完整重建 | `海中鱼巣.exe --runtime-context` | 可执行文件 SHA-256 |
| --- | --- | --- | --- |
| Debug x64 | 退出 0 | 退出 0 | `4A25E7044D1D21D46D98755910490E562E38D4341444878F8B6699E2020A4DC7` |
| Release x64 | 退出 0 | 退出 0 | `09CC05308F25F61F987B62DC47B9B84D743A3809F8454E642B4860545803B4C4` |

生产程序退出码只作为正式运行门禁，不作为业务结果；业务结论来自生产代码之外的消费者取得的结构化返回和公开读回。

## 生产外消费者驱动

消费者驱动只在临时归档中替换外部入口翻译单元；全部正式模块保持候选提交内容。驱动只导入生产运行期模块，不导入或直接调用第一层模块。最终驱动源码 SHA-256 为 `19984D59D0A9BD599656E3478A284DAD2AA8FD30737422B0825939AD8A478F79`。

| 配置 | 完整重建 | 实际调用 | 可执行文件 SHA-256 |
| --- | --- | --- | --- |
| Debug x64 | 退出 0 | 退出 0 | `491E24FF1455946868B100103B097028DB3A66080C17AD22D57929788B77B609` |
| Release x64 | 退出 0 | 退出 0 | `44DEC1CACD4519A79C7C184EADD7B582DC18ADF877EF774E50A9C233FED4E017` |

首次驱动重建因验收代码直接比较含可选事实的整个结果对象而失败；该结果类型没有可用的整体相等运算。验收目录内的驱动随后改为在确认可选事实存在后逐字段比较状态和事实，生产代码、合同和候选提交均未修改。修正后的两配置均重新执行完整重建并通过。

四个最终完整重建各只有临时输出目录对应的 `MSB8029` 警告；没有最终编译或链接错误。两次消费者调用输出相同结构化结果，见《结构化结果与覆盖边界》。
