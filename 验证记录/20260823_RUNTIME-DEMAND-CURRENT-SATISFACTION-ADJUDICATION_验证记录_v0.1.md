# RUNTIME-DEMAND-CURRENT-SATISFACTION-ADJUDICATION 验证记录

日期：2026-08-23
计划身份：`RUNTIME-DEMAND-CURRENT-SATISFACTION-ADJUDICATION`
执行版本：基础计划 `v0.1` + 执行期漂移修订 `v0.2`
验证根：`D:\TEMP\海中鱼巣\RUNTIME-DEMAND-CURRENT-SATISFACTION-ADJUDICATION\20260823-exec-current-satisfaction-01`

## 1. 构建与运行结果

| 验证 | 结果 | 说明 |
| --- | --- | --- |
| Debug x64 Rebuild（v0.2 修复后第一轮） | PASS | 退出码 0，产物位于专属 Debug 目录 |
| Debug `--test-治理`（第一轮） | FAIL | V02 已通过，随后 V10 夹具退出被引用合同失败；归因为测试企图制造公开 API 禁止的破坏形状 |
| Debug x64 Rebuild（夹具修正后） | PASS | 退出码 0 |
| Debug `--test-治理`（120 秒命令时限） | 环境重试 | 进程未在命令时限内结束且无失败输出；未据此形成业务结论 |
| Debug `--test-治理`（300 秒时限复验） | PASS | 退出码 0，235.5 秒；本专项与全部既有治理回归通过 |
| Release x64 Rebuild | PASS | 退出码 0，产物位于专属 Release 目录 |
| Release `--test-治理` | PASS | 退出码 0，8.3 秒；结论与 Debug 一致 |
| `python .\tools\check_specs.py --strict` | PASS | 116 份目录项全部通过 |
| `git diff --check` | PASS | 无空白错误；仅 Git 提示工作区 LF 未来可能转换为 CRLF |
| `git diff --cached --check` | PASS | 暂存区为空时通过；发布前需在精确暂存后复验 |
| UTF-8 严格解码 | PASS | 三个源码文件均可由抛错式 UTF-8 解码器读取 |

构建命令均使用计划要求的 `msbuild .\海中鱼巣.slnx /t:Rebuild /m /p:Configuration=<Debug或Release> /p:Platform=x64`，并把 `OutDir`、`IntDir`定向到上述专属验证根。

## 2. V01—V20 分账

`NOT_RUN`只表示公开生产 API 无法合法形成该损坏或注入形状；不升级为通过。

| 编号 | 结论 | 证据 |
| --- | --- | --- |
| V01 | PASS | 无效合同版本、零裁决身份、零需求、零 G0 均返回`入口拒绝`且零裁决 |
| V02 | PASS | 六次读取共用同一 G0；当前值等于目标时返回`已满足`，列表项四字段、完整谓词、生命周期、当前值、合同和比较回执进入完整快照 |
| V03 | PASS | 当前值低于目标且允许位不含低于时返回`未满足`并保留比较证据 |
| V04 | PASS | 允许位包含低于时按具名关系返回`已满足`，未硬编码“只有相等满足” |
| V05 | PASS | 不存在需求返回`证据不足`和零裁决 |
| V06 | NOT_RUN | 当前成员关系引用的列表项缺失/不完整无法经合法 API 形成；provider 对成员缺失及完整列表项未找到/已退出/属性未设置静态映射为`内部错误` |
| V07 | NOT_RUN | 需求与完整列表项四字段不一致无法经合法 API 形成；provider 只用完整读取载荷互证，任一不等静态返回`引用冲突` |
| V08 | PASS | 无匹配特征实例场景返回`证据不足`和零裁决 |
| V09 | PASS | 两个匹配当前实例场景返回`内部错误`，未任选 |
| V10 | NOT_RUN | 退出仍被当前需求引用的目标合同被公开入口以`引用冲突`拒绝；provider 对合法读取返回的未找到/已退出保留`证据不足`映射 |
| V11 | NOT_RUN | 退出仍被当前合同引用的比较注册被公开入口以`引用冲突`拒绝；provider 对未注册/未实现静态映射为`不可比较` |
| V12 | PASS | 非 I64 当前值返回`不可比较`和零裁决 |
| V12A | NOT_RUN | 无公开生产 seam 合法注入底层许可拒绝；六类读取的结构状态映射和比较服务许可拒绝分支静态保留`许可拒绝`，未与入口拒绝合并 |
| V13 | PASS | 独立合法写入推进事实代次后，以旧 G0 调用返回`当前性漂移`和零裁决；新增完整列表项读取也有同 G0 回显守卫且函数内无重试 |
| V14 | NOT_RUN | 无公开生产 seam 注入成员坏成功形状、完整列表项坏成功形状或比较回执损坏；载荷、身份回显、G0、注册、算法版本和输入回执均有静态守恒检查 |
| V15 | PASS | 同请求同 G0 重复调用得到完全相同的值式结果 |
| V16 | PASS | 列表项没有当前任务时仍形成当前满足裁决 |
| V17 | PASS | 同一列表项两个当前需求成员分别裁决，保留各自需求与成员关系；未形成结算 |
| V18 | NOT_RUN | 无合法资源/长度异常注入 seam；`bad_alloc`和`length_error` catch 静态映射为`资源失败`，未知异常映射为`内部错误` |
| V19 | PASS | 裁决前后结构事实代次完全不变 |
| V20 | PASS | Debug/Release 完整治理专项中的既有任务目标已达成、未达成、等待、漂移、冲突和不可比较回归矩阵未退化，程序输出`全部通过` |

## 3. 静态守恒

目标函数独立截取后的扫描结果：

```text
读取需求完整调用 = 1
读取需求成员列表调用 = 1
读取需求列表项完整调用 = 1
按存在读取全部当前特征调用 = 1
读取完整目标状态合同调用 = 1
读取当前特征比较注册调用 = 1
比较特征调用 = 1
成员读取.所属列表项消费 = 0
当前特征实例后追加读取当前值 = 0
任务服务、读取任务、任务方法路径、实例方法、任务实际结果调用 = 0
需求/任务/结算写入口调用 = 0
父需求回流扫描或消费 = 0
函数内刷新 G0、无限循环或重试 = 0
L2需求结构数据头/服务实现修改 = 0
```

完整列表项请求身份只来自`当前成员关系.列表项`；成功快照的`所属列表项`只来自`读取需求列表项完整`成功载荷。新增函数、变量和业务成员均使用中文命名；保留的 `G0`、`L2`、`I64`、C++关键字和标准库名称属于冻结合同或语言标识。

## 4. 未验证与声明边界

本记录没有执行具名集成验收智能体的独立验收，也没有验证父需求回流、任务筹办、结果分配或逐成员结算。结果共用/争用及争用时按正式优先级顺序分配属于后继目标；当前仓库尚无可据此宣称完成的正式优先级 provider。
