# GOAL-IFR-INSTANCE-STRUCTURE-RESULT-VERSION-REPAIR 验证记录

- 仓外`root_provider_probe.cpp`使用真实L1运行包、owner端口和现有特征/存在构造路径：实例特征结构首次与以当前截止的精确重复均满足既有`成功(request)`和交付相等；同一驱动继续通过根 provider的安全/服务根首次、恢复、换F冲突、F未找到/退出/错型分支。
- 仓外`ordinary_assembly_probe.cpp`以独立持久化根调用普通应用装配：输出`assembly_status=1 success=1 rootG=26 demand=1`，证明越过实例特征结构登记检查并取得同一上下文的需求服务；不调用根 provider。
- 根工程Debug x64、Release x64隔离Rebuild均通过（各0警告、0错误）。构建不证明启动或集成。
- `git diff --check`和暂存差异检查在发布前执行。

未覆盖：资源失败、发布未知、历史v1迁移、跨进程恢复、阶段21、根材料完整运行矩阵、普通应用启动和集成验收。
