# DATA-L2 概念定义合同读取与普通概念退出闭环验证记录

日期：2026-08-17

计划：`DATA-L2-CONCEPT-DEFINITION-CONTRACT-READ-AND-EXIT-CLOSURE v0.1`

验证根：`D:\TEMP\海中鱼巣\DATA-L2-CONCEPT-DEFINITION-CONTRACT-READ-AND-EXIT-CLOSURE\20260817-001\`

## 1. 结论

两个生产文件完成计划实现。外置专项在 x64 Debug / Release 均输出：

```text
PASS D1 D2 D3 D4 X1 X2 X3 C1 R1
```

两次专项进程退出 `0`。不带专项覆盖的根工程 x64 Debug / Release fresh Rebuild 也均退出 `0`。静态门禁、严格规范目录、严格 UTF-8、固定键 / 分区、公开 ABI、白名单和生产零专项痕迹检查通过。

这些证据只覆盖本计划生产实现与具名专项，不升级为真实业务定义、真实概念退出、合法 BIZ 消费、跨进程恢复或整个数据服务闭环。

## 2. 外置专项构建与运行

专项使用仓库外 `专项.cpp` 与 `专项覆盖.targets`，通过：

```text
/p:ForceImportBeforeCppTargets=<验证根>\专项覆盖.targets
```

把根工程入口替换为公开服务专项入口；没有修改 `.slnx`、`.vcxproj`、`.filters` 或生产入口。Debug / Release 分别使用独立 `IntDir`、`OutDir` 和全新运行根。

| 配置 | Rebuild | 最终运行根 | 结果 |
| --- | --- | --- | --- |
| x64 Debug | 退出 `0` | `runtime24` | `PASS D1 D2 D3 D4 X1 X2 X3 C1 R1`，退出 `0` |
| x64 Release | 退出 `0` | `runtime25` | `PASS D1 D2 D3 D4 X1 X2 X3 C1 R1`，退出 `0` |

Debug 无诊断二进制第一次用候选路径位于配置材料根之外，材料服务按公开合同返回 `入口拒绝=5`。该次不是生产概念断言失败；修正专项调用为“每个全新运行根内的候选文件”后，Debug / Release 最终批次均通过。没有为此修改生产代码或放宽材料服务合同。

## 3. 自定义参数矩阵

| 组 | 实际覆盖 | 最终结果 |
| --- | --- | --- |
| D1 | 空合同、`0或1`、`1到1`、多值有限基数建立与完整读回 | PASS |
| D2 | 零角色、零最大、上下界逆序、合同乱序 / 重复、缺合同、顺序断裂、欠 / 超基数零变化拒绝 | PASS |
| D3 | 按概念和按定义目标当前 / 历史读取、合法空组、稳定排序 | PASS |
| D4 | 扫描预算与数量预算不足返回零载荷，禁止截断成功 | PASS |
| X1 | 退役叶概念首次退出、精确重复、同幂等异请求、当前已退出、历史前后完整读回 | PASS |
| X2 | 中间概念必要替代边、已有绕行零冗余边、多父 DAG 单根可达 | PASS |
| X3 | 根、活跃、冷却、错退役编码、当前支持、反向定义拒绝且零变化 | PASS |
| C1 | 同请求并发；新支持引用与退出竞争恰一提交，另一方结构化失败，零部分载荷 | PASS |
| R1 | 根、普通概念、上下位、支持和生命周期既有公开 ABI 静态 / 动态回归 | PASS |

专项仅通过现行公开服务与正式读回判断；没有生产 seam、测试宏、固定 sleep、日志 PASS 判定或第二写入口。

## 4. 根工程双配置

不传 `ForceImportBeforeCppTargets`，仍把构建中间件与输出隔离在本任务验证根：

| 配置 | 入口 | 输出 | 结果 |
| --- | --- | --- | --- |
| x64 Debug | `msbuild .\海中鱼巣.slnx /t:Rebuild` | `build\RootDebug` | 退出 `0` |
| x64 Release | `msbuild .\海中鱼巣.slnx /t:Rebuild` | `build\RootRelease` | 退出 `0` |

两次均真实编译生产 `入口.cpp`，没有编译 `专项.cpp`；构建输出没有落入或清理仓库内异主 `.codex-build`。

## 5. 静态门禁

执行结果：

- 全工作区 `git diff --check`：退出 `0`；仅出现 Git 行尾转换提示；
- 目标两生产文件限定 `git diff --check`：退出 `0`；
- `git diff --cached --check`：index 为空时退出 `0`；
- `python .\tools\check_specs.py --strict`：`113/113 PASS`；
- 两生产文件严格 UTF-8 无 BOM：`2/2 PASS`；
- `L2普通概念结构合同版本=2`、`L2概念定义合同版本=1`：匹配；
- 固定键 `0x020A'6206`、`0x020A'6219—0x020A'621B`：各唯一声明；
- `0xB—0xF` 五个分区：容量检查与写集使用一一匹配，零重叠；
- `按概念读取定义关系组`、`按定义目标读取概念定义关系组`、`退出普通概念`：三个公开 ABI 匹配；
- 工程、普通应用、聚合 getter 未进入本切片白名单；
- 两生产文件中的 `专项 / 测试 / 自检 / cstdio / fprintf` 及临时诊断标记：零命中；
- 两生产文件 SHA-256：`A7A22C199598BFE3B887A9069B884DD36461B14E301BFB5680F9E108BF0E4BCA`、`D690D4E0E74FA81C82C091BBBC0868F53696BAA863D62F54B9EC652974E94A7B`。
- 临时目录清理：`NOT_CLEANED`。精确解析路径为 `D:\TEMP\海中鱼巣\DATA-L2-CONCEPT-DEFINITION-CONTRACT-READ-AND-EXIT-CLOSURE\20260817-001`，删除前计 `832` 文件、`177` 子目录；两次递归删除调用均被工具安全策略在执行前拒绝，路径未被改动。专项进程已全部退出，残留不占锁或共享资源，按现行规则作为普通残留分账，不升级为清理 PASS，也不单独否定已取得的构建 / 运行证据。

只暂存四个允许文件后的 cached 白名单、最终提交父链、目标 blob 和远端读回在同批发布时继续固定。临时目录保持上一条具名普通残留状态。

## 6. NOT_RUN 与声明边界

以下范围没有执行，不得由本记录升级：

- 任意外部 owner 直接 L1 引用与退出竞争；
- 真实内存 / 文件 / 许可资源耗尽和发布未知；
- 崩溃、重启、跨进程恢复和长期并发公平；
- 普通应用真实生产运行、真实概念建立 / 退出和 BIZ 合法消费者；
- 概念名称、语言表达、`DATA-EXT-11—14` 及 `DATA-EXT-01—14` 整体；
- 具名集成验收。

本记录不修改计划状态；结果发布后由计划索引唯一所有者依据正式结果提交另行登记退出。
