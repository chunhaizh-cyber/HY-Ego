# 方法六项静态能力投影与候选准入验证记录

计划身份：`RUNTIME-GOVERNANCE-METHOD-STATIC-CAPABILITY-ADMISSION`

计划版本：v0.3

日期：2026-08-22

验证基线：工作区起点 `9d90969255a5230ddcb77ebfd98fb4ee88407042` 加本计划允许切片；结果提交待本批形成。

## 1. 证据口径

本记录区分：

- 函数返回：公开 DATA-L2 或 DATA 外入口的结构化状态；
- 值式投影：用途粗召回、六项签名、投影和结构缺口；
- 正式 DATA 事实：经公开新增 / 替换 / 退出和当前 / 历史读取取得的权威事实。

测试输出只标识断言组已执行，不单独作为机器事实。没有自然可控入口的项目保留 `NOT_RUN`，未用生产故障钩子、伪造事实或私有容器把它们升级为 PASS。

## 2. 构建与静态验证

| 验证项 | 命令 / 材料 | 结果 |
| --- | --- | --- |
| Debug x64 全量重建 | MSBuild `Rebuild`；`OutDir/IntDir` 为 `D:\TEMP\海中鱼巣\RUNTIME-GOVERNANCE-METHOD-STATIC-CAPABILITY-ADMISSION\20260822-执行1\Debug` | PASS；退出码 0；输出无警告 / 错误行，约 39.8 秒 |
| Release x64 全量重建 | MSBuild `Rebuild`；`OutDir/IntDir` 为同轮 `Release` 目录 | PASS；退出码 0；输出无警告 / 错误行，约 44.4 秒 |
| Debug 需求专项 | Debug 产物 `海中鱼巣.exe --test-需求` | PASS；退出码 0 |
| Debug 治理专项 | Debug 产物 `海中鱼巣.exe --test-治理` | PASS；退出码 0 |
| Release 需求专项 | Release 产物 `海中鱼巣.exe --test-需求` | PASS；退出码 0 |
| Release 治理专项 | Release 产物 `海中鱼巣.exe --test-治理` | PASS；退出码 0 |
| 正式规范目录 | `python .\tools\check_specs.py --strict` | PASS；116/116 |
| 工作区差异 | `git diff --check` | PASS；仅 Git 换行提示 |
| 暂存差异 | `git diff --cached --check` | PASS；检查时 index 为空 |
| 禁止实现搜索 | `variant::index`、旧 `按结果规格读取当前方法候选` | PASS；均无命中 |
| 旧筹办消费者搜索 | 源码与工程中搜索旧模块身份 / 文件名 | PASS；模块外正式消费者为零 |

## 3. DATA-L2 验证矩阵

| 验证项 | 函数返回 / 值式证据 | 正式 DATA 事实 | 结论 |
| --- | --- | --- | --- |
| 单用途、单结果 | 当前投影恰好一份且无缺口 | 公开新增并当前读回完整 v2 方法 | PASS |
| 空条件、空输入、空限制 | 治理专项建立空组完整方法并形成一份投影 | 经公开方法服务写入和投影读取 | PASS |
| 存在本体根目标对象 | 写入成功，投影目标类型保持原身份 | 当前方法事实读回一致 | PASS |
| 存在根下普通概念目标对象 | 未在专项中建立完整普通概念材料链 | 无 | NOT_RUN |
| 特征 / 动态错根目标对象 | 动态根作为目标对象返回引用冲突 | 拒绝前后事实代次不变 | PASS |
| 已退出 / 混代目标对象 | 没有可控且不破坏其它合同的公开夹具 | 无 | NOT_RUN |
| 状态合同结果类型 | 写入、读回、粗召回和投影均保持状态分支 | 当前与历史事实一致 | PASS |
| 动态概念结果类型 | 多轴方法的动态根概念结果写入并形成独立投影 | 当前及旧截止历史读回一致 | PASS |
| 错根概念结果类型 | 存在根作为动态概念结果返回引用冲突 | 拒绝前后事实代次不变 | PASS |
| 已退出 / 混代概念结果 | 未建立可控普通概念退出 / 混代夹具 | 无 | NOT_RUN |
| 联合分支同底层编码 | 纯值比较保持状态分支序号 1、概念分支序号 2，且不相等 | 不写事实 | PASS |
| 多用途、多结果无笛卡尔组合 | 两个结果只形成两份投影；每份方向和分支与对应用途一致 | 多轴方法完整读回 | PASS |
| 条件结果关系 | 两个条件只进入各自正式关联结果 | 条件结果关系事实读回 | PASS |
| 全方法 / 条件级输入规格 | 每主轴恰有一个全方法和一个对应条件级输入 | 输入规格事实读回 | PASS |
| 全方法 / 条件级限制 | 每主轴恰有一个全方法和一个对应条件级限制 | 限制条件事实读回 | PASS |
| 精确重复 | 返回同一方法；事实代次不变 | 没有第二事实 | PASS |
| 同键异义 | 返回幂等冲突 | 事实代次不变 | PASS |
| 本地顺序重复 / 非连续 | 返回入口拒绝 | 事实代次不变 | PASS |
| 条件引用冲突 | 返回入口拒绝或引用冲突 | 事实代次不变 | PASS |
| v1 新写请求 | 结构化拒绝 | 事实代次不变 | PASS |
| 当前 / 历史投影 | 替换后当前一份、旧截止历史两份 | 新旧方法身份与生命周期隔离 | PASS |
| 替换后历史粗召回 | 当前动态用途为空；旧截止仍召回旧方法 | 历史方法事实保留 | PASS |
| 退出后历史粗召回 | 当前为空；旧截止仍召回旧方法 | 退出事实与旧历史并存 | PASS |
| 扫描用途事实预算不足 | 返回数量预算不足且粗召回为空 | 零写 | PASS |
| 粗召回方法预算不足 | 返回数量预算不足且粗召回为空 | 零写 | PASS |
| v1 旧事实投影缺口 | 公开 v2 新写入口禁止建立 v1 事实，未绕过 owner 注入旧数据 | 无 | NOT_RUN |

## 4. DATA 外候选准入矩阵

| 验证项 | 公开提供者结果 | 事实证据 | 结论 |
| --- | --- | --- | --- |
| 六项完全相同 | 唯一候选命中 | 候选身份来自公开投影 | PASS |
| 输入条件不同 | `匹配为空` | 无部分候选 / 缺口 | PASS |
| 目标对象类型不同 | `匹配为空` | 无部分候选 / 缺口 | PASS |
| 特征类型不同 | `匹配为空` | 无部分候选 / 缺口 | PASS |
| 变化方向不同 | `匹配为空` | 无部分候选 / 缺口 | PASS |
| 结果类型不同 | `匹配为空` | 无部分候选 / 缺口 | PASS |
| 限制条件不同 | `匹配为空` | 无部分候选 / 缺口 | PASS |
| 空组严格相等 | 空组完整方法命中；加入非空输入后不命中 | 方法投影组保持空组 | PASS |
| 有匹配候选 | 候选非空、结构缺口为空 | 公开投影身份一致 | PASS |
| 匹配为空 | 全量扫描后候选和缺口均空 | 无写入 | PASS |
| 仅结构不完整 | 用途命中但结果方向冲突，只有结构缺口 | 不返回伪候选 | PASS |
| 有匹配且有结构不完整 | 同用途完整与不完整方法同时存在 | 候选与缺口分账 | PASS |
| 扫描预算不足 | 数量预算不足，候选 / 缺口均空 | 无截断载荷 | PASS |
| 候选预算不足 | 数量预算不足，候选 / 缺口均空 | 无截断载荷 | PASS |
| 当前性漂移 | 旧当前守卫返回当前性漂移 | 无候选 / 缺口 | PASS |
| 入口拒绝 | 错合同版本返回入口拒绝 | 无候选 / 缺口 | PASS |
| 引用冲突 | 当前公开写入口原子拒绝畸形结构，未找到不增加生产故障钩子的自然触发路径 | 无 | NOT_RUN |
| 资源失败 | 没有自然可控资源分配失败入口 | 无 | NOT_RUN |
| 内部错误 | 未破坏生产状态制造复义或内部不一致 | 无 | NOT_RUN |
| 固定旧截止后替换 | 返回原截止和旧候选 | 替换新事实未升级历史截止 | PASS |
| 退出后旧截止 | 当前候选退出；旧截止仍返回旧候选 | 历史事实保留 | PASS |
| 提供者全链零写 | 多组准入前后中性事实代次相等 | 零任务、需求、路径、实例、学习和能力缺口写入 | PASS |
| 提供者只读依赖 | 以 `const L2方法结构服务&` 构造并完成全部调用 | 不取得写服务或线程 | PASS |

## 5. 运行期消费者与工程矩阵

| 验证项 | 证据 | 结论 |
| --- | --- | --- |
| 状态合同分支命中既有任务目标裁决 | Debug / Release 治理专项的已达成、未达成主链通过 | PASS |
| 动态概念分支不按同底层编码命中 | 生产代码只用 `std::get_if<L2目标状态合同身份>`；纯值联合同编码分支测试通过，但未另建完整任务 / 路径 / 实例链运行该消费者 | NOT_RUN |
| 方法服务只读概念依赖装配 | Debug / Release 全量构建；需求与治理专项均成功构造并运行 | PASS |
| 新模块工程 / filters 登记 | 两配置全量模块扫描、编译和链接通过 | PASS |
| 旧筹办两文件退出 | 工作区删除，工程登记删除，正式消费者搜索为零 | PASS |

## 6. 未验证范围与结论

本次没有 `FAIL` 项。主链、两类结果、六项精确匹配、四态分账、当前 / 历史隔离、双预算、零写、两配置构建和专项为 `PASS`。

以下保留为 `NOT_RUN`：存在根下普通概念、已退出 / 混代目标和结果概念、公开 v1 旧事实种子、提供者引用冲突 / 资源失败 / 内部错误强制运行，以及运行期目标裁决动态概念完整任务链。没有为这些项目增加生产故障钩子、伪造 DATA 事实或第二入口。

未执行下一筹办工作包、当前就绪、方法选择、路径 / 实例生产、真实动作、任务完成、需求结算、价值回流、学习、路径验证或跨进程恢复。本记录不是顶层 HY-Ego 最小生命闭环完成证明。
