# 初始场景实际存在创建真实消费者构建与生产链验证

日期：2026-08-07

## 1. 验证对象

- 起点：`6035c680ffaf7b5da68120331d4300432442fce5`
- 计划 blob：`b305af533b17db930819d6460675d9f723e5fbba`
- 对象：初始场景实际存在原子发布、两个事实所有者精确读回、透明组合门和生产启动接线。
- 结论层级：施工门禁通过；独立消费者验收尚未执行。

## 2. 已执行验证

| 验证 | 结果 |
| --- | --- |
| Debug x64 串行完整重建 | 通过；模块依赖扫描实际执行，新原子发布模块进入扫描和编译，生成 `x64/Debug/海中鱼巣.exe` |
| Release x64 串行完整重建 | 通过；模块依赖扫描实际执行，新原子发布模块进入扫描和编译，生成 `x64/Release/海中鱼巣.exe` |
| Debug 正式 `--runtime-context` | 退出码 0 |
| Release 正式 `--runtime-context` | 退出码 0 |
| 正式规范严格检查 | 110/110 通过 |
| 工作区差异空白检查 | 通过，无 whitespace 错误 |

仓库没有独立的模块全量检查脚本；本次不虚构额外脚本结果。两次完整重建日志均明确执行“扫描源以查找模块依赖项”，且新模块在两种配置中均被扫描、编译和链接。

## 3. 静态合同复核

| 门禁 | 实测 |
| --- | --- |
| 新原子发布模块调用中性写集提交 | 恰 1 个物理调用点 |
| 新原子发布模块旧幂等探测、旧写集提交 | 0 |
| 写集形状 | 1 节点 + 1 值 + 1 关系 + 1 属性槽 + 0 退出 |
| 旧组合服务第一层引用、写集、锁、缓存和状态映射 | 0；创建和读取各透明委托一次 |
| 实际存在资格 legacy 节点、属性、值读取 | 0；改用中性精确读取 |
| 初始场景接纳完整快照 | 0；改用中性精确读取 |
| 生产初始化调用 | 恰 1；位于实际存在登记之后、旧系统角色之前 |
| 新增或直接修改函数诊断责任 | 已逐项登记或复核 |

## 4. 生产运行观察边界

两种配置的正式生产入口均能依次完成世界登记、初始场景、实际存在登记、初始场景实际存在、旧系统角色、概念活动、候选完整性和首次发布，并以退出码零结束。该结果只说明生产链未在本轮结构化门禁上失败；日志和退出码不承担验收判定。

## 5. 尚未证明

本记录不证明独立合法消费者已验收该候选，也不证明第一层整体完善。代码精确提交并推送后，仍须由生产代码外的验收方仅通过正式生产消费者、结构化启动结果和租约公开读回，连续核对初始场景实际存在、实际存在登记、初始场景和世界登记的同截止权威事实。不得直调第一层或用日志替代结果。
