# L2-STRUCTURE-CRUD-REBUILD-C1 验证记录

日期：2026-08-12

## 已运行

- 根工程 `Debug|x64 Rebuild`：PASS。
- 根工程 `Release|x64 Rebuild`：PASS。
- C1 专项 `Debug|x64`，参数基值 691000 / 顺序 10、20、10、20：21/21 PASS。
- C1 专项 `Release|x64`，参数基值 791000 / 顺序 10、20、10、20：21/21 PASS。
- 专项并发路径无 sleep、以 `std::barrier` 同起步，包含一个新增写者和条件 / 动作 / 结果三个读者，各读者固定 128 轮。Debug 实际计数为写成功 1、完整 1、许可 383、漂移 0、资源失败 0、部分 / 混代 / 重复 0；Release 为写成功 1、完整 128、许可 256、漂移 0、资源失败 0、部分 / 混代 / 重复 0。
- `python .\tools\check_specs.py --strict`：113/113 PASS。

## 覆盖

- 普通应用状态 17—19 与 provider const / 非 const getter 同址；专项独立同仓的存在、特征定义 / 实例、状态、因果五个 owner 两两不同，因果锚点 / 归属与四类型登记完整。
- 四个状态经存在 / 特征 / 状态正式入口建立，最终均由状态身份来源证明；动作 / 来源经同包 L1 公共入口建立普通节点。专项 cpp 动态 import / owner / 服务 / 调用为零。
- 首次新增的因果节点、归属、五角色关系与来源值共八事实编码唯一、同 owner、同代次，来源槽精确指向来源值；空动作不补关系，条件等于结果合法。
- 同状态异顺序、同顺序异状态合法；重复项、零顺序 / 目标、空组、未排序、超过上限和两个私有保留键均逐项拒绝且零推进。
- 原请求精确重复回首次载荷且零推进；同键改顺序、目标、动作有无 / 编码、来源、结果顺序 / 目标均幂等冲突。
- 不存在 / 已退出 / wrong-family / legacy / 裸状态包装以及动作非普通 / 不存在 / 已退出、来源不存在 / 已退出均取得精确结构化状态。
- 当前 / 历史身份来源、完整投影、全部关系、来源槽和值与状态端点同截止；wrong-family、同 owner 元数据、裸 L1、malformed 因果来源当前 / 历史精确入口拒绝且空载荷。
- 三类查询多匹配稳定升序，空动作不入动作组；合法无匹配空成功，非法 / 不存在 / 已退出端点不误报空；旧期望当前 / 历史 / 三查询均漂移空载荷。

## 尚未覆盖

专项未做真实分配失败注入、长时公平、崩溃/恢复、跨进程、生产消费者或独立集成验收；也不证明 C2 引用替换/来源换代/退出、A1、因果真实性/必然性、动作成立、动态证据、概率或模板。

## 最终静态门禁

十路径严格 UTF-8、三份 XML、31 个生产模块 / 17 个头、专项三文件未进入主工程、专项 cpp 动态零命中、因果数据头 / 服务存在 / 特征 / 动态依赖零命中、`git diff --check` 均 PASS。最终 staged 范围、`git diff --cached --check` 和 Git 发布事实在发布阶段复核；本文件不预先宣称未发生的提交或推送。
