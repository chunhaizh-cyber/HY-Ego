# L2-STRUCTURE-CRUD-REBUILD-C1 验证记录

日期：2026-08-12

## 已运行

- 根工程 `Debug|x64 Rebuild`：PASS。
- 根工程 `Release|x64 Rebuild`：PASS。
- C1 专项 `Debug|x64`，参数基值 91000 / 顺序 10、20、10、20：11/11 PASS。
- C1 专项 `Release|x64`，参数基值 95000 / 顺序 11、21、12、22：11/11 PASS。
- 专项并发路径无 sleep、以 `std::barrier` 同起步；未观察到或吞掉 `资源失败`。

## 覆盖

- 因果 owner 为 move-only，普通应用状态 17—19 和 provider const getter 同址。
- 四类型登记完整；至少四个正式状态均经状态服务公开身份来源证明。
- 双条件/有动作/双结果与单条件/空动作/单结果新增；精确重复、同键异义冲突。
- 空条件、零顺序、未排序、两个私有保留幂等值入口拒绝。
- 当前与创建截止历史完整读取；条件、动作、结果三类查询，空动作不入动作组。
- 事实代次推进后的旧期望漂移空载荷；三类并发读者只接受已读取、许可拒绝或漂移。

## 尚未覆盖

专项未做真实分配失败注入、长时公平、崩溃/恢复、跨进程、生产消费者或独立集成验收；也不证明 C2 引用替换/来源换代/退出、A1、因果真实性/必然性、动作成立、动态证据、概率或模板。

## 最终静态门禁

UTF-8、XML、31/17 工程计数、strict、十路径 diff/range/禁止项和最终 Git 状态在发布前补记于提交回执；本文件不预先宣称未发生的发布事实。
