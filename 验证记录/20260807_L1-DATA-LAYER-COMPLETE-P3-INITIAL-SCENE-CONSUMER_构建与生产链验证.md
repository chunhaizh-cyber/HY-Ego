# 初始场景结构真实消费者构建与生产链验证

日期：2026-08-07

验证对象：本记录所在的初始场景消费者代码结果提交。

## 构建与运行

| 项目 | 结果 |
| --- | --- |
| Debug x64 串行完整重建 | 通过；零警告、零错误 |
| Release x64 串行完整重建 | 通过；零警告、零错误 |
| Debug 正式生产运行期入口 | 退出码 0 |
| Release 正式生产运行期入口 | 退出码 0 |
| 正式规范严格检查 | 110/110 通过 |
| 白名单差异检查 | 通过 |

仓库当前没有计划草案中举例的独立模块依赖检查脚本；两次完整重建均实际执行编译器模块依赖扫描并成功完成全部模块编译和链接。本记录不虚构不存在的独立脚本结果。

## 源码门禁

场景结构服务实测：

- 预探测调用：零；
- 旧提交入口：零；
- 业务意图、旧业务标签和旧摘要：零；
- 中性写集提交：一处；
- 中性当前事实代次读取：两处；
- 中性当前节点、属性、值和关系读取：各一处；
- 完整快照：一处，精确位于后续实际存在消费者的成员接纳读取，不在初始场景创建或当前读取链。

生产启动实测只有一处初始场景初始化调用，且位于旧系统角色初始化之前。

## 结论边界

上述结果只证明代码能够在两种构建配置下编译、链接并从正式生产入口运行，且静态调用形状符合计划。退出码、构建和源码扫描不等于消费者合同验收；后续必须绑定精确代码提交，从真实生产会话取得结构化启动结果、初始场景公开读回和世界登记同截止读回，才能累计关系创建、关系查询和该混合写集原子性覆盖。
