# 实例特征场景专用旧接口物理退出构建与生产链验证

日期：2026-08-08

## 1. 验证对象与结论层级

- 起点：`ba0c3556dcf19d0bf851b6ebb5ad73d2fc5e1e06`
- 计划 blob：`c24acebfc32ae500a50342e06bbaf5d6450e912e`
- 对象：实例特征服务中初始场景宿主旧公开表面及其旧幂等、业务标签、执行证据、完整快照和 legacy 写入闭包的物理退出。
- 结论层级：源码删除范围、双配置完整构建、双配置模块依赖和静态门禁通过；没有形成第一层独立验收结论。

## 2. 已执行验证

| 验证 | 结果 |
| --- | --- |
| 目标代码与两份记录 `git diff --check` | 通过，无 whitespace 错误 |
| Debug x64 串行完整重建 | 首轮发现必要变体头文件被过度清理；恢复后通过，零警告、零错误 |
| Release x64 串行完整重建 | 通过，零警告、零错误 |
| Debug MSVC 模块依赖扫描 | 136 / 136，失败 0，循环 0，外部缺失 0 |
| Release MSVC 模块依赖扫描 | 136 / 136，失败 0，循环 0，外部缺失 0 |
| 正式规范严格检查 | 110 / 110 通过 |

构建日志和模块扫描候选输出位于系统临时目录，没有写入或覆盖工作区现有 `.codex-build`。本次没有运行第一层专项驱动、生产自检、后续数据服务或恢复流程。

## 3. 静态合同复核

| 门禁 | 实测 |
| --- | --- |
| 旧数据结构 | 三个旧请求定义为 0 |
| 旧校验 | 两个旧请求有效性检查定义为 0 |
| 旧公开入口 | 建立、读取、换代三个旧函数定义为 0 |
| 实例特征旧依赖 | 旧幂等探测、legacy 提交、完整快照、领域意图、执行证据均为 0 |
| 中性写入保持 | 实例特征服务内 `提交中性写集` 恰 3 处 |
| 全领域 legacy 提交 | 由 5 个文件 9 处降为 4 个文件 7 处 |
| 全领域旧幂等探测 | 由 5 个文件 10 处降为 4 个文件 8 处 |
| 正式入口 | 登记、实际存在宿主建立、读取和当前值换代仍存在并完成两配置编译链接 |
| 范围 | 除两个目标生产文件和两份专属记录外，本切片没有其它差异 |

## 4. 尚未证明

本记录只证明旧表面按计划退出且保留生产代码可构建、模块闭合。它不证明保留入口本轮已经实际运行，不证明剩余四个领域服务的 legacy 入口已经迁移，也不证明第二至第五层现行数据服务已经全部得到第一层支持。

第一层不自证。后续数据服务只按自身业务需要调用第一层的增、查、改、退和历史读取；其结构化结果与最终权威状态符合本层业务合同时，实际发生的第一层调用才自然成为第一层能力证据。单次构建、静态计数、日志或本记录均不能单独推出第一层已经完善。
