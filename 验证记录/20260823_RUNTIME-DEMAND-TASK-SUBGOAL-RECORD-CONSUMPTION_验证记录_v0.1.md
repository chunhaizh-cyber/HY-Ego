# RUNTIME-DEMAND-TASK-SUBGOAL-RECORD-CONSUMPTION 验证记录 v0.1

日期：2026-08-23

基线：`678529c09cd23ff16a67d5e0905eae36b54e0d62`；计划 blob：`654dd527dcd180f34acaf0e11c7684d879535d8d`；详细设计提交：`904ad552b4db0222cc94f1618eeb367add7848ef`。

## 1. V01—V21 逐项结论

| 编号 | 结论 | 证据与边界 |
| --- | --- | --- |
| V01 | PASS | Debug / Release 动态逐一传入坏业务版本、零 G、零记录、零顺序、零需求幂等、零任务幂等和两键相同，全部返回`入口拒绝`；调用前后当前事实代次不变。 |
| V02 | PASS | 动态消费新记录，无精确列表项和同目标子需求，得到`新建列表项与子需求`；新列表、子需求、来源关系同代后完成任务绑定。 |
| V03 | PASS | 动态已有精确列表项且显式父下无同目标子需求，得到`复用列表项并新建子需求`。 |
| V04 | PASS | 动态同父同目标子需求已存在，得到`复用当前子需求`；身份与既有子需求相同。 |
| V05 | PASS | 动态另一个父下已有同目标子需求，显式第二父仍建立自己的子需求，不跨父复用。 |
| V06 | PASS | 动态先经公开 API 在同父下建立异目标子需求，再消费记录；服务忽略异目标子并复用正确同目标子需求。 |
| V07 | NOT_RUN | 当前公开需求 API 不能合法形成同父同目标当前子复义或精确列表项复义，未新增 friend / raw / 第二写端口。源码守恒：`承接任务子目标记录`对匹配子组或列表项组大于 1 固定返回`内部不一致`且在任何新写之前退出。 |
| V08 | PASS | 动态以不同父子顺序复用当前子需求，返回`父子顺序冲突`，无新来源或任务绑定。 |
| V09 | PASS | 动态直接以完全相同的需求 owner 请求和原幂等身份重试，得到`精确重复`。 |
| V10 | PASS | 动态以同需求幂等身份改变来源记录及强类型引用，得到`幂等冲突`；随后 V11 仍以首次来源关系恢复并成功，证明首次事实未被替换。 |
| V11 | PASS | 动态预置唯一需求来源、保持任务记录待绑定；业务服务分类为`需求侧事实已存在`，跳过需求写并使用原任务绑定键完成绑定。成功结果含父需求在内六份需求侧材料。 |
| V12 | PASS | 动态用业务服务实际使用的原任务绑定请求和同一幂等身份重试，记录 owner 返回`精确重复`且子需求身份不变。 |
| V13 | NOT_RUN | 公开普通应用没有合法的确定性资源耗尽或绑定期间并发 G 漂移注入点，未伪造失败。源码守恒：绑定状态为`事实代次漂移`或`资源失败`时只调用`形成等待`，返回`需求已发布待任务绑定`、完整需求侧载荷和原绑定请求，不撤销需求、不换键。 |
| V14 | PASS | 动态对已绑定且来源一致的记录重复承接，得到`精确重复 + 已有双向事实`，经预读共同 G 完整读回且零写。 |
| V15 | PASS | 动态通过正式记录服务先绑定子需求但不建立需求来源，再调用业务服务，固定返回`内部不一致`且不补写来源。 |
| V16 | NOT_RUN | 来源关系指向其它子需求或复义不能由当前公开需求 API 合法构造。源码守恒：唯一结果谓词、记录子需求与来源子需求比较、完整材料组端点互证任一失败均返回`内部不一致`。 |
| V17 | NOT_RUN | 记录父需求、目标或子需求在同次业务调用中的并发漂移无确定性公开注入点。源码守恒：预读整组最多重读一次最新 G；父域、目标、顺序和最终材料不一致分别返回具名非成功或`内部不一致`，没有默认值分支。 |
| V18 | PASS | 所有动态成功结果均须通过`需求任务子目标承接结果::成功`；该谓词核对记录、来源、父需求、子需求、父子、成员、列表项、所属存在和目标端点同 G 完整一致；专项另按记录权威反查来源并得到唯一结果。 |
| V19 | PASS | Debug / Release 动态核对 mutable / const getter 多次取得同一服务地址。 |
| V20 | PASS | 静态扫描：新业务模块 L1 / raw / owner 私有依赖 0；BIZ 事实账、幂等账、发布见证 0；父重判、任务唤醒、重筹办、成员结算、结果共用 / 争用调用 0；业务服务仅借用两个结构服务引用。 |
| V21 | NOT_RUN | 普通应用装配没有合法的 DATA 许可拒绝注入 seam，未建立第二服务或测试后门。源码守恒：`映射状态`把`L2结构状态::许可拒绝`独立映射为业务`许可拒绝`；通用失败结果不携带业务载荷，不能并入入口拒绝或伪装成功。 |

动态专项固定输出：`V01-V06、V08-V12、V14-V15、V18-V19 动态通过；V07、V13、V16-V17、V21 按源码守恒/不可注入 NOT_RUN；V20 由静态禁项扫描裁决`。总通过数不替代逐项结论。

## 2. 最终构建与运行

验证根：`D:\TEMP\海中鱼巣\RUNTIME-DEMAND-TASK-SUBGOAL-RECORD-CONSUMPTION\20260823-FINAL3`。

| 命令 | 结果 |
| --- | --- |
| `msbuild .\海中鱼巣.slnx /t:Rebuild /m /p:Configuration=Debug /p:Platform=x64 /p:OutDir=...\20260823-FINAL3\Debug\bin\ /p:IntDir=...\20260823-FINAL3\Debug\obj\ /v:minimal` | PASS，退出码 0；日志`Debug/rebuild.log`。 |
| 同上，`Configuration=Release`，输出到`...\20260823-FINAL3\Release` | PASS，退出码 0；日志`Release/rebuild.log`。 |
| `...\Debug\bin\海中鱼巣.exe --test-治理` | PASS，退出码 0；stderr 0 字节；stdout 末行`[治理测试] 全部通过`。 |
| `...\Release\bin\海中鱼巣.exe --test-治理` | PASS，退出码 0；stderr 0 字节；stdout 末行`[治理测试] 全部通过`。 |

FINAL3 双构建加双运行组合耗时 309.5 秒。此前诊断轮和 FINAL / FINAL2 用于定位测试证据缺口，不替代 FINAL3 结论；曾由短工具超时终止的前台进程已精确停止，未删除仓库文件。

## 3. 静态、规范和文本检查

| 检查 | 结果 |
| --- | --- |
| `python .\tools\check_specs.py --strict` | PASS；116 份正式规范目录项全部通过。 |
| `git diff --check`、`git diff --cached --check` | PASS；仅 Git 提示工作副本未来触碰时 LF 转 CRLF，不是差异错误。 |
| 七个生产 / 专项白名单文件使用严格 UTF-8 解码 | 全部 PASS。 |
| 旧`RUNTIME-DEMAND-CHILD-CANDIDATE-UNIFIED-ENTRY`生产身份 | 0。 |
| 新业务模块直接 import 三个正式结构模块 | 3；其中`L2任务结构`恰 1。 |
| 新业务模块 import L1 / owner / raw / 内部命名空间 | 0。 |
| 三个既有结构模块反向 import 新业务模块 | 0。普通应用和专项作为合法消费者不计反向结构依赖。 |
| BIZ 私有事实账 / 幂等账 / 发布见证 | 0。 |
| 父需求重判 / 任务唤醒 / 重筹办 / 成员结算 / 结果共用 / 争用调用 | 0；仅对记录正式状态枚举`子需求已回流待父需求重判`作只读一致性判断，不调用后继服务。 |
| 新增英文函数名、变量名、业务成员名 | 0；扫描候选仅为 C++ / STL 固定词、既有`L1_`引用、V 编号和测试文字。 |
| 新模块工程登记 | `.vcxproj`恰 1，`.filters`恰 1。 |
| 新模块第二 export / include 面 | 0；仅本模块唯一`export module`命中 1。 |

## 4. 未验证与声明边界

- 动态`NOT_RUN`：V07、V13、V16、V17、V21；各项均绑定源码守恒位置，没有建立生产 seam、friend、raw 端口、伪数据或第二业务入口。
- 未运行独立的具名集成验收智能体；本记录是本计划 Debug / Release 自检与静态验证，不是独立集成验收结论。
- 未验证任务完成后的逐需求单独核算、成员结算、父需求是否满足重判、任务重筹办、结果可共用 / 争用裁决、争用时按优先级依次满足、预算或归因。
- 实现与初始记录提交为`5aada0f2eb5c015617b0c55e22aaccf2bfdb328c`，只含计划九个白名单文件，已普通推送至`origin/main`；首次发布后`HEAD == origin/main`、ahead / behind 为`0/0`、index 为空。随后只补记本条已发生的发布事实；记录补记提交和最终 Git 回读由最终交接给出。构建、日志和提交本身均不升级未验证边界。
