# DATA-L2 L1许可拒绝到六入口显式传播检查记录

日期：2026-08-17

计划：`DATA-L2-L1-PERMISSION-REJECTION-PROPAGATION v0.1`

plan blob：`1f0c3d1846843c1e51c7e5a11150d6342ec455f7`

验证基线：`04c1179ca5cc3b2945dc39fa3aa6f9e2b56f9088`

验证根：`D:\TEMP\海中鱼巣\DATA-L2-L1-PERMISSION-REJECTION-PROPAGATION\20260817-001\`

## 1. 结论

计划内十二个生产文件形成最小兼容补丁。根工程 x64 Debug / Release fresh Rebuild 成功；仓库外六入口 × 双配置最终矩阵 12 / 12 退出 0。每个入口和配置均实际观察到稳定成功、确定的非零事实代次漂移及至少一次许可拒绝；许可拒绝全部为截止 0、变更事实代次空、业务载荷空，资源失败误分类与内部不一致均为 0。

此结论只证明本计划状态传播候选及施工门禁，不升级为 WORLD-SELF、BIZ、普通应用消费者闭环或完整 DATA-L2 验收。

## 2. 双配置构建

根工程 `海中鱼巣.slnx` 与仓库外专项均把 `OutDir / IntDir / PDB / BMI` 定向到本计划验证根。

| 对象 | Debug | Release |
| --- | --- | --- |
| 根工程 fresh Rebuild | 退出 0，0 error | 退出 0，0 error |
| 仓库外专项 fresh Rebuild | 退出 0，0 error | 退出 0，0 error |

根工程编译了当前正式工程登记及异主但已存在的 WORLD-SELF 三文件现场；构建未修改这些文件，也未使用或清理 `.codex-build`。

## 3. 六入口双配置矩阵

最终日志均位于 `logs/Debug` 或 `logs/Release` 的 `*-Final.log`。

| # | 公开入口 | Debug | Release |
| ---: | --- | --- | --- |
| 1 | `读取场景树` | 成功 / 漂移 / 许可拒绝，`permission_denied=1`，资源0，内部0，失败0 | 同左，失败0 |
| 2 | `读取存在单例角色` | 成功 / 漂移 / 许可拒绝，`permission_denied=1`，资源0，内部0，失败0 | 同左，失败0 |
| 3 | `读取完整存在` | 成功 / 漂移 / 许可拒绝，`permission_denied=1`，资源0，内部0，失败0 | 同左，失败0 |
| 4 | `读取存在当前场景成员引用` | 成功 / 漂移 / 许可拒绝，`permission_denied=1`，资源0，内部0，失败0 | 同左，失败0 |
| 5 | `读取场景宿主存在关系` | 成功 / 漂移 / 许可拒绝，`permission_denied=1`，资源0，内部0，失败0 | 同左，失败0 |
| 6 | `读取存在当前宿主场景` | 成功 / 漂移 / 许可拒绝，`permission_denied=1`，资源0，内部0，失败0 | 同左，失败0 |

稳定成功结果保持原完整载荷；P1 先正式发布新代次后以旧守卫读取，结果为非零新截止、空变更代次和空业务载荷；P2 经正式 writer 与公开 reader 同步起跑有界探测，许可拒绝结果严格为零截止和全空载荷。

## 4. 静态门禁

- `python .\tools\check_specs.py --strict`：113 / 113 通过。
- 十二个生产文件 `git diff --check`：通过，仅现有工作树 LF / CRLF 提示。
- 十二个生产文件：有效 UTF-8、无 BOM，12 / 12 通过。
- `L1中性读取状态` 旧 1—7 不变，尾追加许可拒绝恰为 8；合同版本、公开 DTO 和六个函数签名不变。
- 全仓相关生产文件扫描：十二个允许文件加底层仓库文件，无白名单外可观察消费者。
- 十二个生产文件中的计划身份、临时路径、专项、计数、`sleep` / `yield`、故障宏与日志 PASS 增量：0。
- 工程 / filters 差异：0；生产专项登记与第二目标：0。

## 5. 回归与 NOT_RUN

- 成功和已发布非零异截止漂移：双配置六入口动态通过。
- 锁竞争许可拒绝：双配置六入口动态通过。
- 真正资源耗尽无法从公开边界安全确定制造，保持 `NOT_RUN`；既有资源失败分支和枚举映射未改，矩阵中的资源失败误分类为 0。
- 私有坏形状不注入；合同、owner、端点或成功形状矛盾仍由现有代码静态保持内部不一致，动态坏形状为 `NOT_RUN`。
- 长时公平、普通应用真实消费者、WORLD-SELF v0.4、BIZ 提供者拒绝、其它 DATA 服务全部并发路径、崩溃、重启、断电和跨进程恢复均为 `NOT_RUN`。

## 6. 声明边界

最多声明：L1 仓库已有的非阻塞许可拒绝被强类型保留到 L1 中性结果和六个具名 DATA-L2 当前读取入口，公共枚举已知直接消费者未引入兼容回归，并通过本计划双配置构建和并发矩阵。不得据此声明 WORLD-SELF、BIZ 或完整 DATA 服务闭环完成。
