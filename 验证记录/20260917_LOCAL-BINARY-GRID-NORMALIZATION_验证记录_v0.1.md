# LOCAL-BINARY-GRID-NORMALIZATION 验证记录

计划：`LOCAL-BINARY-GRID-NORMALIZATION v0.1`

## 隔离专项

隔离根：`D:\TEMP\海中鱼巣\LOCAL-BINARY-GRID-NORMALIZATION\20260917-2203`

以 VS 2026 x64 开发环境编译专项驱动及 `数据服务.局部二值格解析类.cpp`：

- Debug：编译成功；`probe.exe` 退出码 `0`。
- Release（`/O2`）：编译成功；`probe-release.exe` 退出码 `0`。

驱动覆盖：8×8 非零轮廓原样规范化和 v3 解析、8×8 全零轮廓转换 `[0,1,8]`、4³ 非零体素含 RGB 原样规范化和 v3 解析、4³ 全零体素转换 `[0,2,4]`并解析。

## 静态检查

- `git diff --check`（两个生产文件）通过。
- v2 请求/结果符号在两个生产文件零命中。
- `L1事实基座服务`、`特征值类数据服务`、写端口和保存调用在两个生产文件零命中。

## 主工程

已完成 x64 Debug/Release `Rebuild`；两次均编译到本解析器翻译单元并生成主程序产物。主工程验证仅作为共享工程尝试，不替代隔离专项，也不证明运行期业务闭环。

## 未覆盖范围

未建立 DATA-L3 消费者，未接入 U64 保存，未验证真实设备、特征、概念、恢复或业务运行链。
