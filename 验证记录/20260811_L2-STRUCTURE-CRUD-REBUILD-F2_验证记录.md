# L2-STRUCTURE-CRUD-REBUILD-F2 验证记录

日期：2026-08-11

计划：`L2-STRUCTURE-CRUD-REBUILD-F2 v0.1`

施工起点：`f619db3b75b7afea36aaf8562b04b92361bfd318`

## 1. 构建与参数矩阵

以下命令均以退出码 0 完成：

```powershell
MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /verbosity:minimal
MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Release /p:Platform=x64 /verbosity:minimal

powershell -ExecutionPolicy Bypass -File .\验证工具\运行L2通用特征实例参数验证.ps1 `
  -配置 Debug -幂等基值 31000 -实例数量 3
powershell -ExecutionPolicy Bypass -File .\验证工具\运行L2通用特征实例参数验证.ps1 `
  -配置 Release -幂等基值 35000 -实例数量 5
```

主工程 Debug / Release x64 均重建成功。临时验证项目两配置均构建并运行成功；MSBuild 对系统临时目录报告 `MSB8029` 警告，但未影响独立全量重建和验证程序执行。

Debug 参数矩阵最终事实代次为 24，首实例创建截止为 7；Release 参数矩阵最终事实代次为 26，首实例创建截止为 7。两轮均输出 `验证结论：PASS，失败数=0`。

## 2. 已通过行为矩阵

1. 两类关系类型首次登记、精确重复和当前读回一致，编码非零且不同；未加载服务映射为空载荷失败。
2. 登记错合同、错规则和错幂等身份均入口拒绝。
3. 两个宿主存在、一个空宿主存在和两个特征定义均经 E2 / F1 正式公开入口建立。
4. 实例新增只推进一个事实代次，实例节点和两关系由一份写集建立，返回宿主、定义、生命周期和空当前值。
5. 原请求精确重复零推进并返回首次实例；同键异宿主 / 定义返回幂等冲突。
6. 零身份、错合同、不存在 / 已退出宿主、不存在 / 已退出定义和旧期望新键均精确失败且零发布。
7. 单项当前读取逐字段一致；不存在实例与公开 L1 完整退出夹具形成的已退出实例精确分账。
8. 历史读取覆盖创建前未找到、创建截止成功、后继当前代次仍按原截止重建，以及退出截止已退出。
9. 同一宿主 / 定义多实例、同宿主异定义和异宿主同定义均成功，没有增加组合唯一约束。
10. 按存在枚举只返回目标宿主实例，严格按实例身份升序；其它宿主实例不混入。
11. 合法空宿主返回 `已读取` 空组；不存在和已退出宿主不误报空成功。
12. 通过 L1 公开写集增加重复宿主关系后，单项和枚举均返回内部不一致与空载荷，没有部分结果。
13. 首次成功请求在多个后继代次后仍精确重复并返回首次实例，同键异请求仍冲突。
14. 确定性推进 L1 后，旧期望单项和枚举均返回事实代次漂移与空载荷。
15. 四轮固定上限 `std::barrier` 并发中，单项 / 枚举只接受同代次完整成功、许可拒绝或漂移空载荷；写侧每轮只推进一次。两配置均未命中成功或漂移内部窗口，因此本轮读侧由许可拒绝空载荷收口；未出现部分组、重复实例或混代载荷。
16. F2 全部成功实例的 `当前值` 始终为空；验证没有把 DTO 或 F1 / F2 成功升级为 F3 或生产装配完成。

## 3. 静态与治理检查

以下检查通过：

- `python .\tools\check_specs.py --strict`：111 份目录项全部通过；
- 七文件严格 UTF-8 解码通过；临时 `.vcxproj` XML 解析通过；
- 主工程保持 27 个生产模块、13 个头和四配置 `/utf-8`；
- `海中鱼巣.vcxproj` 与 `海中鱼巣.vcxproj.filters` 均无 F2 差异；
- scoped `git diff --check` 与 `git diff --cached --check` 通过；Git index 为空；
- 五个新公开函数、登记实体、实例 / 查询 DTO 和五类固定本地键均位于计划指定生产文件；
- 两个生产文件未发现验证专用入口、友元、计数器、注入、让行、日志、SQL、缓存、第二仓库、历史状态 / 动态登记复用或 sleep。

## 4. 未覆盖范围与结论边界

未覆盖真实分配失败、长时公平、故障注入、恢复、跨进程、生产消费者、F3 特征值 / 实例退出公开合同、状态 / 动态 / 因果、世界树聚合、长期回归和独立集成验收。

本记录只证明 F2 空值通用特征实例路径在具名临时生产外矩阵中成立，以及主工程两配置可构建；不证明 F3、生产装配或完整业务闭环完成。
