# 实际存在 I64 实例特征当前值修改真实消费者构建与生产链验证

日期：2026-08-07

## 1. 验证对象与结论层级

- 起点：`eecca2f91cf5f7f0fc865915c7f403518c95754c`
- 计划 blob：`ae783e0540840ee08e60c647778d21b5b53c70ca`
- 对象：`L1实例特征服务::换代实际存在I64实例特征当前值` 的中性改、退和历史读回候选。
- 结论层级：施工候选构建与生产链门禁通过；合法消费者业务验收尚未执行。

## 2. 已执行验证

| 验证 | 结果 |
| --- | --- |
| Debug x64 串行完整重建 | 通过；模块扫描、编译和链接完成，生成正式生产程序 |
| Release x64 串行完整重建 | 通过；模块扫描、编译和链接完成，生成正式生产程序 |
| Debug 正式 `--runtime-context` | 退出码 0 |
| Release 正式 `--runtime-context` | 退出码 0 |
| Debug MSVC 模块依赖扫描 | 136 / 136，失败 0，循环 0，外部缺失 0 |
| Release MSVC 模块依赖扫描 | 136 / 136，失败 0，循环 0，外部缺失 0 |
| 正式规范严格检查 | 110 / 110 通过 |
| 目标差异空白检查 | 通过，无 whitespace 错误 |

两份模块扫描候选输出写入系统临时目录，没有写入或覆盖工作区现有 `.codex-build`。构建、模块扫描、生产入口退出码和本记录只证明代码候选能够编译、链接并未破坏该生产启动路径；日志和退出码不承担业务验收或第一层完成判定。

## 3. 静态合同复核

| 门禁 | 实测 |
| --- | --- |
| 正式写入口 | 一个非 `const` 转发入口和一个 `const` 唯一业务实现 |
| 目标路径 `提交中性写集` | 恰一个物理调用点 |
| 目标路径旧 `提交写集`、`探测幂等`、`读取完整快照` | 均为 0 |
| 目标路径领域意图、执行证据、提供者见证、业务操作标签 | 均为 0 |
| 中性写集形状 | 0 节点 + 0 关系 + 1 值 + 1 属性槽变更 + 1 退出 |
| 实际存在换代内部状态 | 只保存幂等身份定位，不保存请求、写集、旧 / 新事实或首次结果 |
| 成功类读回 | 新当前领域读取、新值中性读取、旧值当前退出、旧值历史、当前属性与首末事实代次全部核对 |
| 未迁移初始场景换代 | 旧账和旧路径保留，未改变 |
| 禁止路径 | 数据头、第一层核心 / 仓库 / 合同、运行期上下文、工程、筛选器、自检和其它领域服务差异为 0 |

## 4. 尚未证明

本记录不是第一层自检或自证。代码精确发布后仍须由生产代码之外的合法第二层消费者验收方，经正式生产会话和有效租约取得 `const` 实例特征服务，在同一合法链内取得当前实例，执行合法换代并通过领域正式入口独立核对新当前。

消费者还须覆盖精确重复、同键异义、新值越界、新来源无效和写前漂移，并在每个非成功前后通过正式公开入口比较适用权威状态零变化。验收驱动不得导入或调用第一层，也不得解析日志。

消费者验收通过后，最多累计证明该消费者实际使用的一个值新建、属性修改、旧值退出、当前不可读、历史可回查、原子性、精确重复、同键异义和非法请求零变化；不证明关系替换、节点或关系退出、其它消费者、崩溃恢复、跨进程恢复或整个第一层完善。
