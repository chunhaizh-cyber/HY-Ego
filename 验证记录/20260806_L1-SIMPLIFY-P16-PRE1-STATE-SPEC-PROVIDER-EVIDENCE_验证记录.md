# L1-SIMPLIFY-P16-PRE1-STATE-SPEC-PROVIDER-EVIDENCE v0.1 验证记录

## 1. 验证对象

- plan blob：`3c14385983bbbadcdbe322c7483625c91e3a378c`
- 验证执行基线：`4e791b37244ccf5333eefd4c87ee4bb4ab24fe4a`加本计划6个物理文件工作区差异
- 发布前复核基线：`a9417a013d3599aebb10bc025b7baae919a6e933`；其相对验证执行基线只新增PRE2三份设计文档和一条待激活索引行，与本计划范围、接口及验证资源零重叠
- 同一生产目标：`海中鱼巣.exe`

## 2. 构建、运行与模块门禁

| 门禁 | 结果 |
| --- | --- |
| Debug x64隔离`Rebuild` | PASS，退出0 |
| Debug同一生产程序`--self-test-exit` | PASS，退出0 |
| Release x64隔离`Rebuild` | PASS，退出0 |
| Release同一生产程序`--self-test-exit` | PASS，退出0 |
| Debug MSVC模块扫描 | PASS，sources/modules=200/200，failed=0，拓扑200，循环0，外部需求0 |
| Release MSVC模块扫描 | PASS，sources/modules=200/200，failed=0，拓扑200，循环0，外部需求0 |

构建中间目录、可执行文件、自检输出和模块扫描JSON均位于系统临时目录；仓库`.codex-build`未被读取、清理、覆盖或纳入结果。

## 3. 专项矩阵

同一生产程序既有顺序360入口保持唯一，L1状态报告63项扩展为70项，其中P13专项17项扩展为24项。新增验证覆盖：

1. 固定身份`(0x4C31535000000001,1,1)`、直接调用1、去重provider数量1；
2. 状态登记、后当前、前状态、前态选择和比较五类来源与最终成功轮次截止一致；
3. 34个写前事实身份严格按种类/身份升序、无重复，与34份完整`L1事实副本`同索引一一对应；
4. 33份最终G1当前副本和1份P12明确退出的历史副本，历史项精确对应前状态被采样P8值；
5. 增加、等价、减少和精确重复均返回完整稳定凭证；入口拒绝、无前状态、未找到、不可比较以及状态服务直接拒绝均无提供者成功载荷；
6. 形成前后L1代次不变，P13生产函数不提交写集。

## 4. 静态与治理门禁

- `python .\tools\check_specs.py --strict`：PASS，110/110。
- 4个tracked代码文件`git diff --check`与2个untracked记录`git diff --no-index --check`：PASS（仅预期LF→CRLF提示）。
- 固定身份正式定义唯一，专项测试向量命中1；顺序360登记唯一；临时诊断、`fprintf`和临时`cstdio`零命中。
- P13组合生产模块`.提交写集(`、P14/P15/P16调用零命中；正式plan blob复核一致。
- Git index保持为空；未stage、commit或push。

## 5. 证明边界

本验证只证明P13组件在双配置同一生产程序自检中形成版本化、同轮、同截止、可供后继P15规范化的只读提供者证据。日志和自检输出不是机器权威事实；不证明P14证据、P15后继接口、P16发布、正式生产接线、因果、崩溃恢复、STEP-3F或SELF-GOV闭环。
