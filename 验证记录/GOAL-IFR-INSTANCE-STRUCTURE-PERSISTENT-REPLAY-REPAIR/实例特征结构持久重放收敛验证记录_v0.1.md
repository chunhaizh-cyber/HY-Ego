# 实例特征结构持久重放收敛验证记录

- 仓外两进程真实持久L1探针：第一进程`seed`以正常退出形成实例特征结构和安全根；第二进程`recover-instance`重新打开同一持久仓、重新取得相同 owner 端口后输出`recover-instance pass G=10 version=2 firstH=7`并退出0。该结果证明重放交付使用当前`G=10`、保留首次`H=7`，满足既有成功谓词且没有新写入。
- 探针源码、对象、可执行文件和持久快照均隔离在`D:\TEMP\海中鱼巣\ARCH-L4-INSTINCT-ROOT-DEMAND-PROVIDER\20260920-s0\root-provider-persistence-probe\`，不进入工程或Git。
- 根工程Debug x64隔离重建成功：`D:\TEMP\海中鱼巣\GOAL-IFR-INSTANCE-STRUCTURE-PERSISTENT-REPLAY-REPAIR\20260920-s0\Debug-x64\`已生成EXE且`unsuccessfulbuild`标记已清除。Release x64隔离重建成功（0警告、0错误），输出同任务目录的`Release-x64\`。
- `git diff --check`和暂存差异检查在发布前执行。

未覆盖：资源失败、发布未知、故障注入、普通应用跨进程恢复、需求结构登记持久重放、根材料 provider 持久重放、阶段21、启动和集成验收。两进程探针在实例结构成功后继续进入需求结构登记时返回`事实代次漂移`；该后续缺口属于上位根材料计划，不作为本目标失败或完成证据。
