# 旧业务标签、意图和执行证据写入口退出验证

日期：2026-08-08

计划：`L1-LEGACY-BUSINESS-WRITE-SURFACE-RETIREMENT v0.1`

plan blob：`fe6c2d6aaef2077ea219fa12801f26a544f724e6`

施工起点：`3b4620b1ee7cbf13cc810fe9865c0a6640bf384e`

## 1. 构建与模块依赖

| 门禁 | 结果 |
| --- | --- |
| Debug x64 完整 Rebuild | PASS，退出 0；0 错误、1 条临时输出目录提示 |
| Release x64 完整 Rebuild | PASS，退出 0；0 错误、1 条临时输出目录提示 |
| Debug 编译器模块扫描 | PASS，sources / modules=`136 / 136`、failed=`0`、拓扑 `136`、循环 `0`、未解析需求 `0` |
| Release 编译器模块扫描 | PASS，sources / modules=`136 / 136`、failed=`0`、拓扑 `136`、循环 `0`、未解析需求 `0` |
| 正式规范严格检查 | PASS，110 / 110 |
| 五个白名单路径空白检查 | PASS，无 whitespace 错误；仅 Git 行尾策略提示 |

构建使用 Visual Studio 18 Professional，Debug / Release、x64、完整 `Rebuild`、单进程编译；中间目录、输出目录和日志均位于 `C:\Users\zhchh\AppData\Local\Temp\hzy-l1-legacy-retire-b3716b3c94244ccf94bc338d0b9a66e1`，没有使用仓库内异主 `.codex-build`。生成程序未运行。

## 2. 退出与保留门禁

| 检查 | 结果 |
| --- | --- |
| 核心服务旧 `提交写集` 重载 | PASS，生产 C++ 源中零定义零引用；目录生成工具仍有两条静态展示名称，非运行期调用且不属本计划白名单 |
| 核心服务 / 仓库旧幂等探测 | PASS，零定义零引用 |
| 旧探测请求、状态、首次结果、结果和映射 | PASS，零定义零引用 |
| 仓库旧 `提交(const L1写集请求&)` | PASS，零定义零引用 |
| 旧提交专属读回、代次、摘要和审计 helper | PASS，零定义零引用 |
| 正式普通写入口 | PASS，只保留 `提交中性写集` |
| 中性当前 / 历史精确读取 | PASS，公开入口和仓库实现仍存在 |
| legacy 当前 / 历史 / 审计 / 完整快照读取 | PASS，公开入口和仓库实现仍存在 |
| 恢复材料与候选事务 | PASS，导出、建立、确认、撤销入口仍存在 |
| 核心数据头 | PASS，零差异 |
| P13 / P14 与状态动态迁移结果 | PASS，零差异 |
| 本计划之外暂存文件 | 发布前复核 |

本轮没有删除或改写历史幂等账、审计记录、失败见证、完整快照、恢复材料、意图 / 执行证据历史格式和领域结果见证历史格式。它们继续只服务历史兼容校验和管理 / 恢复读取，不再拥有普通生产写入入口。

## 3. 未运行范围

本计划禁止建立第一层专项自检、外部覆盖补格驱动或生产内验收模式，因此没有运行生成的生产程序，也没有制造第二至第五层业务请求。

构建、模块扫描和静态零引用只能证明旧代码面已经退出且当前源码候选可构建；不能证明恢复运行、下游业务运行、当前消费者集合闭合或第一层已经完善。
