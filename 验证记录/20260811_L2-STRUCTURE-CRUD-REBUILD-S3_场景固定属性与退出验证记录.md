# L2-STRUCTURE-CRUD-REBUILD-S3 场景固定属性与退出验证记录

日期：2026-08-11

计划：`L2-STRUCTURE-CRUD-REBUILD-S3 v0.2`

基线：`57cec91f23d595aa9ff7a74b206fffcf23d6b516`

## 正式命令与结果

```powershell
MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /verbosity:minimal
MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Release /p:Platform=x64 /verbosity:minimal

powershell -ExecutionPolicy Bypass -File .\验证工具\运行L2场景固定属性与退出参数验证.ps1 `
  -配置 Debug -幂等基值 5000 -初始I64 11 -换代I64 22 -父子顺序 10 -成员顺序 20
powershell -ExecutionPolicy Bypass -File .\验证工具\运行L2场景固定属性与退出参数验证.ps1 `
  -配置 Release -幂等基值 9000 -初始I64 33 -换代I64 44 -父子顺序 30 -成员顺序 40

python .\tools\check_specs.py --strict
```

主工程两配置 Rebuild 均成功。专项两配置均重新编译现行 L1 合同/仓库/服务、L2 公共/存在/场景模块和专项入口，并在新的系统临时 GUID 目录运行成功。strict 输出为 `海中鱼巣正式规范目录一致性检查: 111 份目录项全部通过`。

## 专项 16 项矩阵

| 编号 | 验证内容 | Debug | Release |
| --- | --- | --- | --- |
| 1 | 登记、场景、存在、三属性类型、来源和独立材料夹具 | PASS | PASS |
| 2 | 初始属性同代次建值设槽、精确重复、同键异义 | PASS | PASS |
| 3 | 已设置再初始、未设置换代、错旧值零变化 | PASS | PASS |
| 4 | I64、U64 组、独立材料三属性完整投影和排序 | PASS | PASS |
| 5 | I64 换代、旧值退出、当前/历史无空洞无双当前 | PASS | PASS |
| 6 | 有属性场景作为 S2 新增、替换、退出端点 | PASS | PASS |
| 7 | `读取场景节点(当前)` 与完整当前逐字段同形 | PASS | PASS |
| 8 | 可选父、两子、一成员、两属性的严格升序闭包 | PASS | PASS |
| 9 | 闭包漏项或多项均引用冲突、空载荷、零变化 | PASS | PASS |
| 10 | L1 跨结构引用冲突精确映射 | PASS | PASS |
| 11 | 场景和全部附属事实同代次退出，父/子/存在端点仍当前 | PASS | PASS |
| 12 | 全部退出编码与 L1 历史逐项同代次，重复和幂等冲突 | PASS | PASS |
| 13 | 退出前历史完整；退出截止历史和当前均已退出空场景 | PASS | PASS |
| 14 | 合同、零身份/场景/类型/来源/旧值、空材料、零独立引用、未排序/重复闭包入口拒绝 | PASS | PASS |
| 15 | 属性写和场景退出的新键旧期望漂移；既有重复/冲突保持 | PASS | PASS |
| 16 | 确定漂移及无 sleep/yield/忙循环有界并发三类结果形状 | PASS | PASS |

## 第 16 项公开状态计数

| 配置 | 已读取 | 事实代次漂移 | 许可拒绝 | 非法状态/部分载荷/写者失败 |
| --- | ---: | ---: | ---: | ---: |
| Debug | 0 | 0 | 16 | 0 |
| Release | 0 | 0 | 16 | 0 |

每轮 `许可拒绝` 均为事实截止 0、空场景、空变更代次；每轮公开写者均成功建立新场景并精确推进一代。两配置没有观察到并发 `已读取` 或 `事实代次漂移`，这不降低矩阵结论，但属于未覆盖公开调度形状。

公开返回不能证明推进恰好落在当前两次内部投影之间或历史前后夹取之间。本轮没有独立可判别证据，因此结论固定为：内部窗口未证明。

## 静态与范围验证

- 严格 UTF-8 解码和 XML 解析成功。
- 主工程：26 个生产 `.ixx`、12 个 `ClInclude`、四配置 4 项 `/utf-8`。
- 专项工程：8 个模块、1 个专项入口、两配置 2 项 `/utf-8`。
- 专项名称在主工程和 filters 中命中 0 次。
- 三个公开函数、五个 DTO、本地键和三个 L1 写入状态 switch 均在预定生产文件内；未增加第二仓、幂等账、缓存、日志、SQL、sleep、yield 或测试钩子。
- 生产代码差异仅位于 `L2场景结构.数据.h` 和 `服务.L2场景结构.ixx`；其余代码差异仅为本专项三文件。
- 两份已跟踪生产文件的 `git diff --check`、七文件严格逐行尾随空白扫描和全 index `git diff --cached --check` 均通过；Git index 为空。

## 证据分账与未验证范围

- v0.1 第 16 项观察到的 `许可拒绝` 只作为计划漂移诊断；含 4096 次人工让行的旧 Debug/Release 运行全部不计正式通过证据。
- 系统临时专项根目录的递归删除被环境策略拒绝；正式两配置均由包装脚本在全新 GUID 目录重新生成和运行，未复用旧构建产物。
- 未注入资源失败、私有索引/幂等账破坏、崩溃恢复、跨进程和长期公平性；未运行生产入口、真实业务消费者或独立集成验收。
- 本记录只证明 `57cec91f` 上七文件 WIP 在具名矩阵和静态门禁下成立，不证明生产装配、其它 L2 结构或完整 L2 已完成。
