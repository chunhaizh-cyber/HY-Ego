# DATA-L2 特征实例宿主与定义引用替换施工检查记录

日期：2026-08-15
版本：v0.1
计划身份：`DATA-L2-FEATURE-INSTANCE-REFERENCE-REPLACE`

## 1. 构建结果

以仓库外专属输出目录运行当前根工程：

- x64 Debug Rebuild：PASS；
- x64 Release Rebuild：PASS；
- 清理临时驱动和首轮输出后，x64 Debug Rebuild：PASS；
- 清理临时驱动和首轮输出后，x64 Release Rebuild：PASS。
- 补齐 `许可拒绝` 映射后以仓库外最终输出目录再次执行 x64 Debug Rebuild：PASS；
- 补齐 `许可拒绝` 映射后以仓库外最终输出目录再次执行 x64 Release Rebuild：PASS。

当前工作区工程文件含异主 EVENT WIP 登记，因此根工程构建也编译了该候选；本记录只消费“两个特征生产文件在当前工程中可编译链接”的证据，不评价、不接受 EVENT 实现。

## 2. 生产外公开入口检查

临时驱动经普通应用唯一装配取得正式存在 / 特征服务，不访问私有仓库、锁、端口或候选。实际结果：

- Debug：`21/21 PASS`；
- Release：`21/21 PASS`。

实际覆盖：

1. 普通应用装配和特征登记截止取得；
2. 两个存在、两个基础特征定义和一个特征实例的正式建立；
3. 只替换宿主；
4. 只替换定义；
5. 同时替换宿主与定义；
6. 新实例端点、空当前值和旧端点投影；
7. 替换前历史截止读回旧引用；
8. 同完整请求精确重复且不改变首次截止；
9. 完全相同端点对返回引用冲突；
10. 陈旧守卫返回事实代次漂移；
11. 按旧宿主读取为空、按新宿主读取命中同一实例；
12. 坏请求 helper 和坏成功结果形状被拒绝。

## 3. 静态门禁

- `git diff --check`：PASS；
- `python .\tools\check_specs.py --strict`：`113/113 PASS`；
- 两生产文件 UTF-8 可读：PASS；
- 生产范围只包含两个特征文件：PASS；
- 新函数、变量和业务成员为中文：PASS；
- L1 `许可拒绝` 精确映射为 L2 `许可拒绝`：PASS；
- 生产文件未新增“测试、验证、故障注入、seam、sleep、deadline”逻辑：PASS；
- 临时驱动、targets、exe、pdb、obj、ifc、module.json 和三个专属输出目录：已删除。

## 4. NOT_RUN

以下没有合法稳定注入入口，未伪造生产 seam：

- 真实分配 / 长度耗尽；
- 私有 owner、关系组、关系类型或历史事实损坏；
- L1 发布结果未知；
- 跨进程恢复与原幂等请求重放；
- 共享锁长期公平和高频并发压力；
- 正式 BIZ 消费者实际使用判断。

## 5. 声明边界

构建和 `21/21` 只证明本叶公开入口施工检查，不构成独立验收，也不证明 DATA-EXT-03、DATA-L1—L3、世界树 / 概念空间或最终需求清单整体完成。服务是否满足具体业务由合法消费者实际使用时结合结构化结果与权威读回判断。
