# L1 中性历史属性值组提供者验证记录

日期：2026-08-11

计划：`L1-NEUTRAL-HISTORICAL-ATTRIBUTE-VALUE-GROUP-PROVIDER v0.1`

## 验证基线

- 执行起点：`031f9722fc8ca4207c8f2ff310776cffaaad1d42`
- 配置：Visual Studio / MSBuild 18.5、PlatformToolset `v145`、x64、C++20、Unicode、`/utf-8`
- 外验输出 / 中间目录：系统临时目录，不在工作区生成外验构建物。

## 构建与运行

### 主工程

```powershell
MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /verbosity:minimal
MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Release /p:Platform=x64 /verbosity:minimal
```

结果：两项均退出码 `0`，分别生成 Debug / Release x64 可执行文件。未运行主程序，因此不把构建结果升级为生产业务闭环证据。

### 生产外公开接口矩阵

```powershell
powershell -ExecutionPolicy Bypass -File .\验证工具\运行L1历史属性值组参数验证.ps1 -配置 Debug -合同版本 1 -起始幂等键 5101
powershell -ExecutionPolicy Bypass -File .\验证工具\运行L1历史属性值组参数验证.ps1 -配置 Release -合同版本 1 -起始幂等键 6101
```

结果：两项均退出码 `0`；两种配置各 12 项检查全部 `PASS`，失败数 `0`。最终事实代次均为 `6`，初始属性代次 `2`、换代代次 `4`、显式退出代次 `5`、节点退出代次 `6`。覆盖：

1. 建立所属节点、两个属性类型、来源节点和空所属节点；
2. 创建前截止成功空组，创建截止返回两个初始属性值；
3. 稳定排序及所属、类型、材料、来源和生命周期逐字段完整；
4. 未入槽普通值在当前 / 单事实历史入口可读，但永不进入历史属性值组；
5. 属性换代在半开边界无空洞或双值，第二属性保持不变；
6. 显式值退出前最后截止有效，退出截止排除；
7. 含槽变更原请求精确重复不推进事实代次，候选不重复；
8. 同键异请求幂等冲突，当前与历史查询零变化；
9. 错合同、零所属和零截止均入口拒绝且零状态变化；
10. 未来截止准确返回事实代次漂移，未知所属组合成功空组；
11. 当前属性 / 当前值入口只反映当前事实，不受历史候选污染；
12. 所属节点及附属当前值闭合退出后，旧截止仍可读，退出截止为空。

两次外验构建均出现 MSBuild `MSB8029` 警告：输出 / 中间目录位于系统临时目录，可能影响增量生成。本包装每轮使用新的 GUID 目录并执行 `Rebuild`，本次完整构建、链接和运行均成功；该警告未被当作长期增量构建或性能证据。

## 静态与治理检查

```powershell
python .\tools\check_specs.py --strict
```

结果：退出码 `0`，111 份正式规范目录项全部通过。

严格 UTF-8 解码并使用 XML DOM / `Select-Xml` 解析主工程和外验工程，结果：

- 主工程生产 `.ixx`：26；
- 主工程 `ClInclude`：12；
- 主工程包含 `/utf-8` 的四配置项：4；
- 外验入口登记：1，主工程内专项登记：0；
- 外验入口 import 总数：1，唯一 import 为公开 `海中鱼巣.核心.服务.L1事实基座`；
- 状态、请求、结果 DTO：各 1；
- 仓库 / 服务 `读取中性历史属性值组` 定义：各 1；
- `插入历史属性槽值候选索引` 只出现槽变更调用和辅助定义共 2 处，候选删除命名出现 0 处；
- 主工程、filters、合同导出模块、入口、L2 和装配：零差异。

两份记录写入后执行最终八文件 `git diff --check`（五个未跟踪文件分别以 `git diff --no-index --check` 补充）、`git diff --cached --check`、严格 UTF-8 解码和 Git 事实复核，均通过。Git index 为空；本计划范围精确呈现三个已修改生产文件和五个未跟踪生产外文件。Git 同时提示八文件下次由 Git 接触时会按仓库设置从 LF 转为 CRLF，该提示未造成空白错误或文件解码失败。

## 未验证范围

- 未注入真实 `bad_alloc` / `length_error` 或其它资源耗尽；异常映射只由源码闭包和两配置编译覆盖。
- 未故意破坏私有候选索引、幂等账或仓状态；矛盾处理只由状态完整性源码闭包和公开写入矩阵覆盖。
- 未做长时间并发、公平性、性能、崩溃 / 重启 / 跨进程恢复或生产消费者测试。
- 未执行 S3 场景固定属性换代、场景退出、引用保护、完整历史场景、其它 L2 结构族或具名集成验收。
