# L1-SIMPLIFY-P7-EXISTENCE v0.1 验证记录

## 1. 验证对象

- 计划：`L1-SIMPLIFY-P7-EXISTENCE v0.1`
- plan blob：`d08bf8b2f35a4be08524da862397e9f28af3b5bd`
- 最终验证基线：`84183ddf532631547d99ccc1defc0d5a1fc1c676`
- 范围：计划规定的十一文件。

## 2. 构建与运行

| 门禁 | 实际结果 |
| --- | --- |
| Debug x64 Rebuild | PASS，0 warning / 0 error |
| Debug `--self-test-exit` | PASS，退出码 0，30/30 |
| Release x64 Rebuild | PASS，0 warning / 0 error |
| Release `--self-test-exit` | PASS，退出码 0，30/30 |
| `python .\tools\check_specs.py --strict` | PASS，110/110 |

Debug 与 Release 模块扫描均在 PowerShell 7 中先设置 `$PSNativeCommandUseErrorActionPreference = $false`，再运行 `tools/clang_ast/扫描.MSVC模块依赖.ps1`。两配置结果相同：`sources=195, modules=195, failed=0`，拓扑节点 195、循环 0、外部需求 0。

## 3. 静态与范围门禁

- 八个公开函数签名各唯一出现一次。
- 一个新头和三个新模块在 `.vcxproj` 与 `.filters` 中各唯一登记一次；`.ixx` 文本 include 为 0。
- 登记写集静态形状为两个节点；创建写集静态形状为一个节点、一个值、一条关系、一个属性槽变更。
- 资格固定为 I64 值 1，所属节点为新实际存在，来源为存在服务身份，属性类型为登记的资格属性类型。
- 归属关系固定为 `初始场景 -> 实际存在`，关系类型为世界登记直接成员关系类型，顺序为 0；读取要求唯一当前归属。
- 创建后的资格入口与场景接纳入口分别读回，事实截止代次必须相同。
- 生产模块对旧句柄、旧存在服务/仓、SQL、材料、P5、P6、状态/动态和日志调用为 0。
- 临时诊断标记 `iostream`、`cout`、`cerr`、`P7-EVIDENCE` 为 0。
- 正式 L1、世界登记和 P2—P6 上游文件为 0 变更；场景结构两文件只追加计划冻结 DTO、完整性函数和两个公开入口，原有合同与函数语义未改。
- P21、P22 正式提交范围仅文档与计划索引，与本计划十一文件零重叠。
- `git diff --check`：PASS；提交前继续执行 cached diff check。

## 4. 专项矩阵结论

专项 24 项全部通过，覆盖世界/初始场景基线、存在登记、登记重复/冲突/错误输入、两节点登记事实、存在/场景规格、精确创建写集、资格与归属见证、正式独立读回、创建重复/冲突、不同键创建、错误资格与零存在、反向关系、双当前归属、八线程同键并发只发布一次，以及无特征/状态/动态额外事实。

完整自检入口以顺序 350 注册本专项，并机械要求登记数量和执行数量均为 30；两配置进程均以退出码 0 完成，因此 30 个自检单元全部通过。

## 5. 未证明边界与剩余问题

未验证存在宿主特征/当前值、关系 19 状态、迁移动能、动作动态、因果、自我资格、治理虚拟存在、移动/移除、生产运行期装配、SQL/恢复、旧消费者、跨服务集成验收、STEP-3 后续步骤或 SELF-GOV-CLOSURE 全链路完成。计划范围内未留已知失败项；STEP-3A 目标是否接受由交互智能体依据正式结果独立核验。
