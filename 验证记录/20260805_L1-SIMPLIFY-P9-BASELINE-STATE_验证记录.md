# L1-SIMPLIFY-P9-BASELINE-STATE v0.3 验证记录

## 1. 身份与范围

- plan blob：`f49dd0b4f190b85e32f95c74244d130383229e31`
- 设计提交：`af7348deb81803e48aef4daf8bc51f082029bf04`
- 验证基线：`000ff4c758f45ba2340d108813587cb2ec314deb` 加 P9 八文件工作区差异
- 白名单：状态数据头、状态服务、状态专项自检、vcxproj、filters、启动聚合及本施工/验证记录

## 2. 动态验证

| 门禁 | 结果 |
| --- | --- |
| Debug x64 串行 Rebuild | PASS，退出码 0 |
| Debug `--self-test-exit` | PASS，退出码 0，31/31 |
| Release x64 串行 Rebuild | PASS，退出码 0 |
| Release `--self-test-exit` | PASS，退出码 0，31/31 |
| Debug MSVC 模块扫描 | PASS，`sources=197, modules=197, failed=0`，拓扑 197，循环 0，外部需求 0 |
| Release MSVC 模块扫描 | PASS，`sources=197, modules=197, failed=0`，拓扑 197，循环 0，外部需求 0 |
| `python tools/check_specs.py --strict` | PASS，110/110 |
| 精确差异 `git diff --check` | PASS |

两配置完整自检都机械要求登记数量与执行数量等于 31；进程均以退出码 0 完成。P9 顺序 360 专项包含 32 个验收项。

## 3. 专项矩阵

1. 正式世界、初始场景、P2 I64 定义、P7 实际存在与 P8 实际存在实例特征夹具按代次 1—8 建立；P9 七节点登记在代次 9 完成。
2. 首态写前代次 9、写后代次 10；当前节点精确增加 2、当前关系精确增加 5、当前值精确增加 5，本地键1—12均形成非零互异稳定编码。
3. 五条关系使用同一登记关系19类型，源均为状态节点，角色1—5和目标端点精确；角色3是 P8 实例槽，角色4是新状态值见证节点。
4. 五值分别验证状态资格、发生时间、见证资格、四项 P8 值引用和采样 I64；五属性槽精确指向对应值，状态自有事实创建代次均为采样截止加一。
5. 发布后通过同一公开读取入口独立读回；读取截止为 10，采样截止为 9，主体、场景、实例槽、来源、时间和值见证完整。
6. 同键同义为幂等读回且代次不变；同键异义为幂等冲突；当前代次下同主体+实例槽第二键为入口拒绝。
7. 八线程并发读取只返回允许的已读取完整事实，未出现混代载荷。
8. 直接退出被采样 P8 值后，当前入口为已退出，历史入口返回原 I64、真实属性类型、来源和退出代次；P9 读取截止推进而采样截止保持 9。
9. 直接注入同源重复角色1关系后读取返回内部不一致；清理该损坏事实后重新读取成功。
10. 状态场景关系退出后，P9 从历史入口读回原角色2关系并保持采样场景，不重新调用 P7/P8。
11. 编译期断言最终结果 DTO 不含 `特征定义`；运行期证明被采样值属性类型等于 P8 当前 I64 值属性类型且不同于 P2 定义节点。

## 4. 静态门禁

- `L1状态服务` 公开面只有构造、登记、登记读取、首态发布和唯一状态读取五个函数。
- 幂等私域精确为登记 `0x0C`、首态 `0x0D`；启动唯一顺序为 360。
- 读取实现中 P7/P8 调用数为 0；两次调用只存在于发布前资格校验路径。
- P9 数据头与结果 DTO 无 `特征定义` 字段；P2 定义不进入首态写集。
- 临时 `iostream`、`cout`、`cerr`、`P9-EVIDENCE` 符号扫描为 0。
- 未修改 P7/P8/P10、L1 核心、旧状态栈、运行期、恢复、SQL、规范、设计、流程图、计划或计划索引。

## 5. 最大证明边界

本记录证明 P9 v0.3 源码提供者及具名自检矩阵在 Debug/Release 本地构建中闭合；不把本地专项升级为生产装配、持久恢复、L2、STEP-3、集成验收或整体治理闭环。
