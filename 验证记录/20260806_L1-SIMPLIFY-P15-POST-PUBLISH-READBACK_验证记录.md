# L1-SIMPLIFY-P15-POST-PUBLISH-READBACK v0.29 验证记录

日期：2026-08-06

## 1. 验证基线与隔离方法

- 正式 HEAD：`57620c808af8d0642fe9960d91920f98efd2b6ff`
- 计划 blob：`4707ef7719fddca89b46c155e9d9cba73535eabb`
- 使用正式 HEAD 隔离源码叠加精确 25 个 P15 允许代码路径；工程、启动入口和自检运行器保持正式版本。
- P15 短时诊断在正式源码和最终隔离源码中均为零命中。

## 2. 构建与运行

| 门禁 | 结果 |
| --- | --- |
| Release x64 Rebuild | 退出 0 |
| Release `--self-test-exit` | 退出 0 |
| Debug x64 Rebuild | 退出 0 |
| Debug `--self-test-exit` | 退出 0 |
| P1-S01 第 7 项 | `checks=14 failed=0`，原 `failed#7` 消失 |
| P0 核心事实基座、P5 可重建索引、P6 特征比较、P7 实际存在、P9 状态 | 均由 31 单元完整自检批次执行并随进程退出 0 |

Release 和 Debug 均使用 MSBuild `Rebuild`，输出分别位于 `.codex-build/P15-Final-Release` 和 `.codex-build/P15-Final-Debug`。两配置完整自检均正常退出，未复现旧 Debug 超时或 Release `0xC0000005`。

## 3. 静态与模块门禁

| 门禁 | 结果 |
| --- | --- |
| 正式 HEAD 直接 `.提交写集(` | 16 文件/50 表达式 |
| 当前完成目标 | 16 文件/49 表达式 |
| v0.29 代码允许路径 | 25/25 存在 |
| 实际代码 diff | 24 文件；唯一未改动允许路径为 `核心/合同.L1可重建索引.ixx` |
| Debug MSVC 模块扫描 | 22 个 P15 模块、失败 0、循环 0 |
| Release MSVC 模块扫描 | 22 个 P15 模块、失败 0、循环 0 |
| 正式规范检查 | `strict 110/110` |
| P15 范围 `git diff --check` | 通过 |
| P15-DIAG | 正式源码 0、最终隔离源码 0 |
| Git index | 空 |

32 个生产/业务场景和 18 个自检场景保持计划冻结的 50/50 映射；物理完成目标为 16/49，其中 X01 同义分支物理提交为 0，X02 首次保留唯一共享提交。

## 4. 归因与修复复验

1. v0.29 修订前隔离 Release 全量自检稳定复现 P1-S01 `failed#7`；修订后该项 `failed=0`。
2. 随后 P6/P9 失败定位到 G7/G8 唯一登记分支错误和 G0 未规范化排序。修复后 Release、Debug 全量自检均退出 0。
3. 退出写集修复前可复现“候选读回一致但交换后状态完整性失败并隔离”；规范化摘要排序后 P12 退出/历史矩阵随 P9 全量单元通过。
4. 隔离诊断只用于归因，最终隔离源由正式 25 路径重新覆盖并恢复正式自检运行器后重建；最终结果不依赖诊断代码。

## 5. 结论边界

验证证明 P15 v0.29 允许范围的双配置构建、完整自检、模块依赖、静态计数、规范和 diff 门禁闭合。尚未取得 Git 发布租约，尚未提交或推送，也未执行发布后独立集成验收；不得据此宣称恢复治理、P15 顶层目标、SELF-GOV 或项目闭环完成。
