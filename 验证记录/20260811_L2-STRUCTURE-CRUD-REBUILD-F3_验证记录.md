# L2-STRUCTURE-CRUD-REBUILD-F3 验证记录

日期：2026-08-12

计划：`L2-STRUCTURE-CRUD-REBUILD-F3 v0.1`

施工起点：`db49059382f933a203b839a7cd3c53486c9c4dc3`

最终验证基线：`413d30e6a96d6b2074596f7b643a2694b4d93bb1`

## 1. 构建与参数矩阵

以下命令均以退出码 0 完成：

```powershell
MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /verbosity:minimal
MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Release /p:Platform=x64 /verbosity:minimal

powershell -ExecutionPolicy Bypass -File .\验证工具\运行L2特征值换代与实例退出参数验证.ps1 `
  -配置 Debug -幂等基值 41000 -初始I64 11 -换代U64一 22 -换代U64二 33
powershell -ExecutionPolicy Bypass -File .\验证工具\运行L2特征值换代与实例退出参数验证.ps1 `
  -配置 Release -幂等基值 45000 -初始I64 44 -换代U64一 55 -换代U64二 66

python .\tools\check_specs.py --strict
```

主工程 Debug / Release x64 均重建成功。临时验证项目两配置均构建并运行成功；MSBuild 对系统临时目录报告 `MSB8029` 警告，但未影响独立全量重建或程序运行。

Debug 和 Release 参数矩阵最终事实代次均为 28，初始截止为 10，第一次换代截止为 11，第二次换代截止为 12，具值实例退出截止为 19；两轮均输出 `验证结论：PASS，失败数=0`。并发窗口实际命中完整成功，未命中漂移内部窗口；这只记录本次调度结果，不将未命中窗口虚报为已观测。

## 2. 已通过行为矩阵

1. 四种值类型首次登记、精确重复和当前读回一致，四编码唯一非零，节点表示分别为 I64、I64 组、U64 组和独立材料引用；未加载、错规则和同键异请求精确分账。
2. 定义、存在和四个空值实例均经正式公开入口建立；F3 值写前当前、历史和按存在枚举仍返回空当前值，单项读取同时返回两条自有关系编码。
3. 初始 I64 当前值只推进一个代次，材料、来源、生命周期和当前槽正确；原请求重复返回首次载荷，同键异材料幂等冲突。
4. 已有当前值再次初始返回引用冲突；无值实例换代返回属性未设置；错旧值返回引用冲突，全部零提交。
5. I64 到 U64 组跨表示换代只推进一个代次，旧值同代退出且当前仅有新值。
6. U64 组同表示再次换代后，当前和两个历史截止分别返回正确材料，没有空洞或双当前。
7. 其它实例完成 I64 组初始和独立材料换代，覆盖四类登记映射；空数组和零独立材料入口拒绝。
8. 具名历史读取分别返回已退出旧值和仍当前新值的完整生命周期；错实例不产生部分值载荷。
9. 当前单项、按存在枚举和换代前后历史实例均返回同截止可选当前值；枚举保持稳定升序和同宿主范围。
10. 完整单项返回两非零不同自有关系编码；重复关系编码、错关系和漏当前值闭包分别入口拒绝或引用冲突，均零提交。
11. 通过 L1 公开入口建立指向实例的外部关系后，实例退出由 L1 当前引用保护返回引用冲突；没有把该夹具冒充状态服务实现。
12. 外部关系退出后，以新代次 / 新幂等身份退出具值实例；实例节点、两自有关系和当前值同代退出，宿主、定义、来源及独立材料节点仍在退出截止有效。
13. 实例退出原请求重复返回首次载荷，同键异闭包幂等冲突；当前单项返回已退出，按存在枚举剔除实例，退出前历史仍完整。
14. 无当前值实例以实例节点和两自有关系闭包同代退出。
15. 错合同、零来源、重复关系编码、空材料和旧期望新键精确拒绝且零发布。
16. 确定性推进 L1 后，旧期望当前、历史和按存在枚举均返回事实代次漂移与空载荷。
17. 固定三轮 `std::barrier` 换代并发和一轮退出并发中，公开读取 / 枚举只接受同代次完整成功、许可拒绝、事实代次漂移或已退出空载荷；没有部分组、双当前或混代载荷。
18. 主工程双配置、临时项目双配置、strict、UTF-8、XML、PowerShell、工程隔离和临时生命周期检查均通过。

## 3. 静态与治理检查

以下检查通过：

- `python .\tools\check_specs.py --strict`：111 份目录项全部通过；
- 七文件严格 UTF-8 解码通过；临时 `.vcxproj` XML 解析和 PowerShell AST 解析通过；
- 主工程保持 27 个生产模块、13 个头和四配置 `/utf-8`；
- `海中鱼巣.vcxproj` blob 保持 `4a845eb328be550b5f1587326b6a88ebc40e4969`，`海中鱼巣.vcxproj.filters` blob 保持 `8c253c7874940781194d06ed44961ea28baaf2d9`，两文件均无 F3 差异；
- scoped `git diff --check` 与 `git diff --cached --check` 通过；
- 六个新公开函数、四类值类型登记、八类 F3 请求 / 结果 / 闭包 DTO、一个完整读取结果字段和五个本地键均位于计划指定生产文件；
- 两个生产文件未发现验证专用入口、友元、计数器、注入、让行、第二服务 / 仓库 / 索引 / 缓存、SQL、日志、sleep 或历史状态 / 动态登记复用。

## 4. 未覆盖范围与结论边界

未覆盖真实分配失败、长时公平、故障注入、恢复、跨进程、生产消费者、状态 / 动态 / 因果、世界树聚合、生产装配、长期回归和独立集成验收。

本记录只证明 F3 具名公开路径在临时生产外参数矩阵中成立，以及主工程两配置可构建；不证明完整 L2、生产装配、集成验收或业务闭环完成。
