# 初始场景当前读回一致投影构建与生产消费验证

日期：2026-08-08

计划：`L2-INITIAL-SCENE-CURRENT-READ-CONSISTENT-PROJECTION-MIGRATION v0.1`

plan blob：`b1595a32d9d8aa922271a0421fbcdc436279662f`

设计提交：`092c23a3410ba12d158b341a2842fe0698ed1cf0`

施工起点：`c9e2bdd0042c0343972dba9bbca9d7e91dfde4c6`

## 1. 构建、模块与生产入口

| 门禁 | 结果 |
| --- | --- |
| Debug x64 串行完整 Rebuild | 通过，退出 0；0 编译错误、1 条 `MSB8029` 临时输出目录提示 |
| Release x64 串行完整 Rebuild | 通过，退出 0；0 编译错误、1 条 `MSB8029` 临时输出目录提示 |
| Debug MSVC 模块扫描 | 通过，sources / modules=`136 / 136`、failed=`0`、拓扑 `136`、循环 `0`、外部需求 `0` |
| Release MSVC 模块扫描 | 通过，sources / modules=`136 / 136`、failed=`0`、拓扑 `136`、循环 `0`、外部需求 `0` |
| Debug 正式 `--runtime-context` | 30 秒时间盒内自行退出 0；stdout / stderr 均为 0 字节 |
| Release 正式 `--runtime-context` | 30 秒时间盒内自行退出 0；stdout / stderr 均为 0 字节 |
| `python .\tools\check_specs.py --strict` | 通过，110 / 110 |
| 三个白名单路径 `git diff --check` | 通过，无 whitespace 错误；仅 Git 行尾策略提示 |

构建使用 Visual Studio 18 Professional，Debug / Release、x64、完整 `Rebuild`、`/m:1`；系统临时验证根为 `C:\Users\zhchh\AppData\Local\Temp\hzy-initial-scene-consistent-7f2d8c49e63a4f9ba9243164b3c99a21`。模块扫描只写该临时目录，没有写正式图谱输出。

## 2. 精确静态门禁

| 检查 | 结果 |
| --- | --- |
| 目标根一致投影调用 | `尝试读取中性一致当前投影` 恰 1 个物理调用点 |
| 旧读取与快照 | `读取中性当前事实代次`、`读取中性当前节点`、`读取中性当前属性`、`读取中性当前值`、`读取中性当前关系`、`读取完整快照`、`尝试读取完整快照` 在目标根内均为 0 |
| 等待与重试 | `for`、`while`、sleep、`continue` 均为 0；没有按漂移回显代次局部重试 |
| 请求形状 | 节点 / 关系 / 值 / 属性值 / 源关系组 / 目标关系组精确为 `1/1/0/1/0/0`；期望代次取登记已验证截止 |
| 顶层非成功 | 漂移、许可拒绝、资源失败分别保留分账；入口拒绝、内部不一致和坏失败形状返回内部不一致 |
| 项目非成功 | 节点 / 关系未找到和属性未找到 / 未设置返回未找到；已退出、种类不匹配及成功缺事实 / 投影返回内部不一致 |
| 成功顶层形状 | 合同、期望回显、共同读取代次和六组长度全部精确核对 |
| 初始场景节点 | 查询 / 事实编码、普通种类、无类型表示、未退出、唯一当前槽和创建代次全部核对 |
| 标记槽和值 | 投影槽与节点槽相等；值编码、所属、类型、来源、I64(1)、创建代次和未退出全部核对 |
| 直接父关系 | 查询 / 事实编码、世界根源端、初始场景目标端、登记关系类型、顺序 0、共同创建代次和未退出全部核对 |
| 返回与缓存 | 成功按现有字段组装完整事实；全部非成功返回空事实；目标根不修改定位缓存 |
| 白名单外生产文件 | 差异为 0；核心 L1、世界登记、启动、运行期上下文、工程、自检、规范、设计、流程图、计划和索引均未修改 |
| 同文件其它函数 | 差异只包含目标专用 helper 与 `读取实现`；其它函数相对起始 blob 行为零变化 |

## 3. 正式生产消费与未独立读回

Debug / Release `--runtime-context` 均使用本轮新构建程序，并在 30 秒时间盒内自行退出 0。若发生超时，验证命令只会终止本段刚启动的精确 PID；本次没有触发超时终止。运行前存在的异主世界登记验收生产进程未被终止、修改或计入结论。

两次运行经现有正式链路触达初始场景创建 / 读回及运行期上下文完整性门禁。进程退出 0 只说明正式生产入口没有拒绝该次启动；本记录未读取日志来补写机器事实，也未把空 stdout / stderr 当成业务成功。

公开租约连续两次结构化场景 / 登记读回未在本段独立执行。

因此本记录不独立证明两次场景与登记均取得结构化已读取、逐字段相等或共同截止，也不形成初始场景消费者集成验收结论。计划冻结的漂移、许可竞争、未找到和坏业务形状分支由源码结构化分账；本段未用私有探针、专项驱动或人为竞争逐项触发。

## 4. 范围与声明边界

实际结果范围只含场景结构服务文件和本施工 / 验证记录。P0-FIX、路线、异主 `.codex-build`、两份 WORLD-TREE 及其它 WIP 未纳入修改、暂存或结论。

允许声明：初始场景当前权威读回候选已按冻结合同迁移到一次中性非阻塞一致当前投影，并通过双配置构建、模块扫描、strict、静态门禁和正式生产入口运行门禁。

禁止声明：公开租约独立结构化读回已通过；真实许可竞争、消费者集成验收、其它消费者迁移、关系组 / 历史 / 修改 / 退出、历史正文闭合、L1 自证或第一层完善已经得到证明。
