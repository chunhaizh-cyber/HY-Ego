# 初始场景完整事实真实第二层消费者构建与生产链验证

日期：2026-08-08

## 1. 验证对象与结论层级

- 起点：`409b7f800bca8e500e885c5747c42c4a542856b9`
- 计划 blob：`5caf4cc0597e509cd48b309165224bedf7f9a49c`
- 对象：场景服务消费第一层源端与目标端条件查询、实际存在服务定位当前资格，以及组合服务形成初始场景完整事实的生产代码候选。
- 结论层级：双配置构建、模块依赖、既有生产运行期上下文和静态合同门禁通过；尚未执行生产代码外合法第二层消费者的业务运行验收。

## 2. 已执行验证

| 验证 | 结果 |
| --- | --- |
| 五代码路径 `git diff --check` | 通过，无 whitespace 错误 |
| Debug x64 串行完整重建 | 首轮因组合模块不可见世界规则常量失败；白名单内修复后通过 |
| Release x64 串行完整重建 | 通过；模块扫描、编译和链接完成 |
| Debug 正式 `--runtime-context` | 退出码 0 |
| Release 正式 `--runtime-context` | 退出码 0 |
| Debug MSVC 模块依赖扫描 | 136 / 136，失败 0，循环 0，外部缺失 0 |
| Release MSVC 模块依赖扫描 | 136 / 136，失败 0，循环 0，外部缺失 0 |
| 正式规范严格检查 | 110 / 110 通过 |

两份模块扫描候选输出位于系统临时目录，没有写入或覆盖工作区现有 `.codex-build`。运行期上下文只证明既有生产构造链仍能建立和退出，没有调用或证明新完整事实业务。

## 3. 静态合同复核

| 门禁 | 实测 |
| --- | --- |
| 新数据结构与入口 | 新请求、事实、结果及三个公开入口各唯一 |
| 第一层源查询消费 | 场景事实所有者内两处调用，分别枚举直接子场景和直接成员；其它后续层新增消费为零 |
| 目标端互证 | 初始场景、每个直接子场景和每个直接成员都以目标端条件组验证唯一父或唯一归属 |
| 资格定位 | 旧显式资格入口仍存在并编译；新旧入口共用唯一已持锁私有实现 |
| 零成员分支 | 在成员循环前直接形成结果；资格和实际存在完整读回调用为零 |
| 依赖方向 | 场景模块导入实际存在模块为零；组合模块导入或直调第一层为零 |
| 失败原子性 | 任一成员失败返回空事实；临时组不进入非成功结果 |
| 稳定排序 | 子场景、成员结构及成员完整事实均按公开稳定编码排序并检查重复 |
| 禁止路径 | 新增代码对完整快照、全仓扫描、SQL、材料、日志、自检、验收和恢复调用为零 |
| 禁止机制 | 无双目标、验收模式、生产内通过判定、第二可执行程序或第一层专用驱动 |
| 范围 | 第一层、运行期上下文、启动、工程、filters、自检和集成验收目录差异均为零 |

## 4. 尚未证明

本记录不是第一层自检或第一层通过结论。代码发布后仍须由合法第二层消费者只经正式生产模块、正式会话和有效租约，实际覆盖初始场景直接成员从一项到零项，并核对公开结构化结果、成员资格、关系和最终权威读回。

即使该运行验收通过，也只累计证明初始场景的一项到零项成员读取，以及本入口实际使用的源端查询、目标端查询和精确读回。当前不证明非空子场景、多成员排序、任意场景、移动、关系替换、性能、并发压力、崩溃、重启、跨进程恢复、旧栈删除、其它第二至第五层消费者或整个第一层完善。
