# L1 所有者一致关系类型闭包原子读取验证记录

日期：2026-08-14

计划身份：`L1-OWNER-CURRENT-RELATION-TYPE-CLOSURE-READ v0.1`

验证基线：`main == origin/main == 46ccf0eb553c112748b16b1b2fa4ada6b4d293f3` 加本计划八路径未提交候选；异主 WIP 不属于验证输入。

## 1. 构建与专项结果

| 验证 | 命令 | 实际结果 |
| --- | --- | --- |
| 根工程 x64 Debug fresh Rebuild | `msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m:1` | PASS；最终生产源码复验成功 |
| 根工程 x64 Release fresh Rebuild | `msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Release /p:Platform=x64 /m:1` | PASS；最终生产源码复验成功 |
| 专项 Debug fresh Rebuild | `msbuild .\验证工具\L1所有者关系类型闭包参数验证.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m:1` | PASS；0 warning / 0 error |
| 专项 Debug 运行 | `验证工具\x64\Debug\L1所有者关系类型闭包参数验证.exe` | `46 PASS / 0 FAIL / 3 NOT_RUN`；并发成功读取 6、许可拒绝 1018、漂移 0、坏形状 0 |
| 专项 Release fresh Rebuild | `msbuild .\验证工具\L1所有者关系类型闭包参数验证.vcxproj /t:Rebuild /p:Configuration=Release /p:Platform=x64 /m:1` | PASS；0 warning / 0 error |
| 专项 Release 运行 | `验证工具\x64\Release\L1所有者关系类型闭包参数验证.exe` | `46 PASS / 0 FAIL / 3 NOT_RUN`；并发成功读取 1024、许可拒绝 0、漂移 0、坏形状 0 |
| 规范一致性 | `python .\tools\check_specs.py --strict` | `113 / 113 PASS` |
| diff | `git diff --check`（精确白名单复核） | PASS |
| UTF-8 | `.NET UTF8Encoding(false, true)` 解码三生产文件与三专项 | `6 / 6 PASS`，均无 BOM |
| XML | PowerShell `[xml]` 解析专项工程与 filters | `2 / 2 PASS` |

汇总 `46` 是具名断言输出数量，不替代下述十三类计划矩阵逐项对账。

## 2. 十三类计划矩阵逐项证据

| 矩阵 | 输入与预期 | Debug / Release 实际证据 | 结论 |
| --- | --- | --- | --- |
| M01 合同、DTO、非法项与 v2 回归 | 静态核对合同版本 1 和六类 aggregate DTO；错版本 2、零 / 重复入口；六个端点选择组分别构造含零、逆序、重复共 18 个反例，均预期入口拒绝、读取代次 0、八组空载荷；代表性 v2 请求继续成功 | 两配置全部具名子例 PASS；v2 结果合同版本仍为 2 | PASS |
| M02 闭包单独合法与空成员 | 请求只含一个当前普通入口类型，该类型没有当前关系 | 两配置均顶层成功、项目成功、关系类型事实存在、成员空 | PASS |
| M03 三成员顺序与完整关系 | 同一入口类型建立三条当前关系，其中一条跨 owner；预期按稳定编码严格升序，关系和两端节点逐字段互证 | 两配置三成员数量、升序、端点、类型、未退出和创建材料均 PASS | PASS |
| M04 两端属性 | 源 / 目标各选择一个已设置和一个未设置属性；预期数量与请求顺序一致，成功项携带槽与完整值，未设置项无投影 | 两配置均核对节点、类型、I64 材料、写入 owner、生命周期和空投影 PASS | PASS |
| M05 四向端点一跳 | 对源 / 目标各自的源 / 目标方向分别选择当前非空类型、当前空类型、已退出类型、属性类型节点和从未存在编码 | 两配置四个方向分别输出成功非空、成功空组、已退出、种类不匹配、未找到；关系、对端、类型事实和排序逐项 PASS | PASS |
| M06 入口项目状态 | 同一请求分别选择从未存在、已退出、当前属性类型节点作为入口 | 两配置分别映射未找到、已退出、种类不匹配，三项均零类型事实与零成员 | PASS |
| M07 跨 owner 端点 | 入口关系及类型属于 owner 甲，源端属于甲，目标端属于 owner 乙 | 两配置均保留该成员并如实回显关系 / 类型 owner 与两端真实 owner | PASS |
| M08 零 / 非零守卫 | 非空库零守卫；使用返回截止作同代守卫；使用截止减一作陈旧守卫 | 两配置零守卫成功且截止非零；同代成功；陈旧返回漂移、当前代次和八组空载荷 | PASS |
| M09 八组组合与 v2 七组回归 | 同一 v1 请求组合 owner、节点、关系、值、属性、源组、目标组和闭包；另以同样七组调用既有 v2 | v1 八组均在一个顶层截止返回；v2 七组数量与状态保持成功 | PASS |
| M10 建立、退出、再建与索引一致 | 先读三入口成员；同代退出一个入口关系、一个一跳关系及两个无引用类型节点；再建新编码；对比闭包和直接源关系组 | 两配置证明退出前后成员数量、退出编码零残留、再建编码后移、源 / 目标 / 类型索引输出一致且排序稳定 | PASS |
| M11 并发同截止 | 单 writer 连续 16 次在同一写集发布新端点、入口关系、端点属性替换与一跳关系；reader 循环零守卫闭包 | Debug 成功 6 / 许可拒绝 1018，Release 成功 1024 / 许可拒绝 0；两配置漂移 0、坏形状 0，每个成功结果内关系、端点、值和一跳创建代次均不晚于截止 | PASS |
| M12 有界端点索引 | 在已发现端点之外，以同一一跳类型建立 64 条无关端点关系；预期目标成员的一跳组数量不变 | 两配置均确认无关关系未混入；静态审查一跳 helper 不访问关系类型全量索引 | PASS |
| M13 根与专项双配置 | 根工程现行调用者零源码修改；新 v1 函数在独立专项编译链接 | 根工程 Debug / Release 与专项 Debug / Release fresh Rebuild 均 PASS | PASS |

## 3. 精确 NOT_RUN

| 项目 | 未运行原因 | 保留证据 |
| --- | --- | --- |
| 真实分配 / 长度耗尽 | 公开合同没有安全、确定的 `bad_alloc` / `length_error` 注入入口；禁止生产故障注入 | 两类异常在公开边界映射资源失败；没有把源码分支冒充动态 PASS |
| 私有索引损坏 | 正式公开入口不能合法制造缺项、重复或错端点的私有索引；禁止测试友元和私有仓出口 | `状态完整` 与闭包逐字段互证失败映射内部不一致；动态 NOT_RUN |
| 共享锁长期公平 | 有界专项可观察实际 try-lock 许可拒绝，但不能稳定证明调度器无限期公平或构造长期饥饿 | Debug 实际观察 1018 次许可拒绝且 writer 结束后可读；长期公平仍 NOT_RUN |

三项 NOT_RUN 没有覆盖任何可经正式公开入口稳定构造的十三类强制动态分支，也没有增加生产 seam。

## 4. 静态范围、路径与生产零验证痕迹

- 私有 `尝试读取所有者范围一致投影内部` 文本范围内 `shared_lock` 计数为 1，且不调用 v1 / v2 任一公开入口。
- `读取一致关系类型闭包` 只以 `当前关系类型索引.find(入口关系类型节点)` 枚举入口成员。
- `读取一致闭包端点关系组` 调用既有源 / 目标关系组 helper；其文本范围对 `当前关系类型索引` 为零命中，因此一跳不按第二类型全仓扫描。
- 公共数据头相对正式 HEAD 只有新增行，删除行数为 0；既有 v2 合同常量、请求 / 结果字段与枚举没有修改。
- 三生产文件扫描 `测试|验证专用|seam|sleep|yield|friend.*验证|#if.*TEST` 为零命中。
- Git 范围检查只识别本计划六件代码 / 专项文件和两份永久记录；异主 dirty 未进入验证输入。

## 5. 未证明范围与清理门禁

本验证只证明 DATA-L1 owner-aware 一致关系类型闭包原子读取及本矩阵内的 v2 回归，不证明 C1、A1、阶段 18、方法登记根、恢复、跨进程或完整 DATA-L1—L5。

三份专项仍为临时资产。结果提交被交互核验接受且计划索引退出后，必须独立删除并重新运行根工程 Debug / Release fresh Rebuild、strict、diff、工程零引用和生产零验证痕迹检查；永久记录、计划、设计、规范和生产实现保留。
