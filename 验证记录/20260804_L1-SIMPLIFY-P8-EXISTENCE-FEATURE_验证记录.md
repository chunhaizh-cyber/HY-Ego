# L1-SIMPLIFY-P8-EXISTENCE-FEATURE v0.1 验证记录

## 1. 验证对象

- 计划：`L1-SIMPLIFY-P8-EXISTENCE-FEATURE v0.1`
- plan blob：`d1495c5cfc9b0fd8de41240d77eff09707e70e6b`
- 激活基线：`a17d406c932382d09af5f84b77b7cc6e77aa4c80`
- 最终发布准备正式基线：`27d12b813f2d616f2a1eb23ac1b0066e307187fe`
- 范围：计划规定的五文件。

## 2. 构建、运行和规范门禁

| 门禁 | 实际结果 |
| --- | --- |
| Debug x64 串行 Rebuild | PASS，0 warning / 0 error |
| Debug `--self-test-exit` | PASS，退出码 0，顶层登记数保持 30 |
| Release x64 串行 Rebuild | PASS，0 warning / 0 error |
| Release `--self-test-exit` | PASS，退出码 0，顶层登记数保持 30 |
| `python .\tools\check_specs.py --strict` | PASS，110/110 |
| `git diff --check` | PASS |

Debug 与 Release 模块扫描均在 PowerShell 7 中先设置 `$PSNativeCommandUseErrorActionPreference = $false`，再运行 `tools/clang_ast/扫描.MSVC模块依赖.ps1`。两配置结果相同：`sources=195, modules=195, failed=0`，拓扑节点 195、循环 0、外部需求 0。

## 3. 静态合同与范围门禁

- 两个新 DTO、两个有效性函数和两个新公开业务函数均已形成；保留旧三参构造并新增四参构造。
- 原 `建立登记`、`建立初始场景I64实例特征`、`换代I64实例特征当前值` 和 `读取I64实例特征` 公开签名未改变；顺序 320 仅原位扩展，未新增顶层登记。
- 共用建立写集静态形状为一个节点、两条关系、一个值、一个属性槽；幂等域唯一为 `0x06`。
- 宿主关系固定为 `实际存在 -> 新实例槽`、角色 1；定义关系固定为 `新实例槽 -> P2定义`、角色 2。专项机械验证未复用场景槽、场景值或 P7 当前场景归属关系。
- 生产模块只导入 L1 事实服务、场景结构、P2 特征定义和 P7 实际存在场景组合服务；对旧句柄/旧仓、SQL、材料、P5、P6、状态/动态和日志生产模块无新增依赖。
- 临时诊断 `iostream`、`cout`、`cerr`、`printf`、`fprintf`、`P8-EVIDENCE` 为 0。
- P24、P25 两次正式增量均只含各自三文档和计划索引，与本计划五文件零重叠；其它所有者 dirty 未修改、未暂存。

## 4. 专项矩阵结论

顺序 320 的实例特征专项 45 项全部通过。新增 18 项覆盖：P7 实际存在真实建立与读回、P8 成功写集和独立读回、精确重复、同键异义、未绑定 provider、非法身份、不同实际存在同定义、错来源、越界、未找到宿主、已有宿主定义组合、八线程同键，以及多宿主、多定义、多当前值、重复槽四类破坏矩阵。原场景实例建立、读取和 P4 当前值换代矩阵继续通过。

完整自检仍使用原顶层顺序 320 单元，登记数和模块数均保持执行 S0 基线；Debug/Release 进程都以退出码 0 完成。

## 5. 未证明边界与剩余问题

以上验证只覆盖本计划源码、专项夹具、本地双配置构建、自检、规范检查和模块依赖扫描。未验证存在宿主当前值换代、关系 19 状态、状态迁移动能、动作动态、因果、自我资格、治理虚拟存在、生产运行期装配、SQL/恢复、旧消费者、跨服务集成验收、STEP-3 后续步骤或 SELF-GOV-CLOSURE 全链路。计划范围内未留已知失败项；STEP-3B 目标是否接受由交互智能体依据正式结果独立核验。
