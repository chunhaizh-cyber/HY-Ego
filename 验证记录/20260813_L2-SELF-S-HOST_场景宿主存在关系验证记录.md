# L2-SELF-S-HOST 场景宿主存在关系验证记录

计划身份：`L2-SELF-S-HOST v0.1`

验证基线：`cb6f7771275063f4f0f79ff2ce2de89af16b30a5` 加本计划七路径 WIP

执行日期：2026-08-14

## 1. 结论

根工程 Debug / Release fresh Rebuild 均 PASS；独立专项 Debug / Release 各 19 / 19 PASS、失败数 0；strict 113 / 113、UTF-8、XML、diff、白名单范围和生产验证痕迹检查均 PASS。该结论只覆盖本记录列出的业务中性结构矩阵，不升级为生产接线、真实自我或独立集成验收。

## 2. 专项矩阵

专项文件：

- `验证工具/L2场景宿主存在关系参数验证.cpp`
- `验证工具/L2场景宿主存在关系参数验证.vcxproj`
- `验证工具/运行L2场景宿主存在关系参数验证.ps1`

Debug 与 Release 均逐项得到相同结果：

| # | 检查项 | 结果 |
| --- | --- | --- |
| 1 | 构造期固定宿主登记与 owner-aware 正式读回 | PASS |
| 2 | 固定登记精确重复 | PASS |
| 3 | 建立三个正式树内场景 | PASS |
| 4 | 建立三个正式存在 | PASS |
| 5 | 宿主关系新增 | PASS |
| 6 | 宿主新增精确重复 | PASS |
| 7 | 同幂等键异义请求冲突 | PASS |
| 8 | 同场景不同宿主、同宿主不同场景双向唯一冲突 | PASS |
| 9 | 相同端点的成员关系与宿主关系编码和投影分账 | PASS |
| 10 | 宿主关系原子替换及旧关系退出读回 | PASS |
| 11 | 按场景当前 / 历史和按存在反向当前读取互证 | PASS |
| 12 | 旧期望事实代次漂移 | PASS |
| 13 | 非法入口拒绝且事实代次零变化 | PASS |
| 14 | 正式场景族节点不被接受为存在端点 | PASS |
| 15 | 裸 L1 私有登记节点不被接受为正式树内场景 | PASS |
| 16 | 当前宿主引用保护目标存在退出 | PASS；`引用冲突` |
| 17 | 场景普通退出闭包同代退出宿主关系 | PASS |
| 18 | 宿主关系具名退出及当前清空 | PASS |
| 19 | 不同场景并发竞争同一宿主存在 | PASS；恰一成功 |

场景来源与树归属均由每次宿主按场景读取的正式公开路径共同核对；当前公开 ABI 不提供“已取得正式场景来源但没有树归属”的合法构造入口，因此该非法中间态未以测试 seam 伪造。专项以正式树内场景证明成功路径，以裸 L1 私有节点证明来源拒绝；生产实现中的树归属拒绝分支由源码静态核对保留，不宣称单独动态构造了不可公开形成的中间态。

## 3. 构建与静态门禁

| 验证 | 结果 |
| --- | --- |
| 根工程 Debug x64 `/m:1 /t:Rebuild` | PASS；`x64/Debug/海中鱼巣.exe` |
| 根工程 Release x64 `/m:1 /t:Rebuild` | PASS；`x64/Release/海中鱼巣.exe` |
| 专项 Debug `Rebuild` + 运行 | PASS；19 / 19，失败 0 |
| 专项 Release `Rebuild` + 运行 | PASS；19 / 19，失败 0 |
| `python .\tools\check_specs.py --strict` | PASS；113 / 113 |
| 七路径严格 UTF-8、无 BOM / `U+FFFD` | PASS；7 / 7 |
| 根工程、filters、专项工程 XML | PASS；3 / 3 |
| 七路径 `git diff --check` | PASS；只有 LF / CRLF 转换预告 |
| 生产工程 / filters 专项登记扫描 | PASS；零命中 |
| 两个生产文件验证专用痕迹扫描 | PASS；测试实体、测试友元、故障注入、计数器和 sleep 零命中 |

专项双配置各出现一条 MSBuild `MSB8029` 临时目录增量构建警告；专项实际使用独立 GUID 临时目录与 `Rebuild`，编译、链接和程序运行均为退出码 0。

## 4. NOT_RUN

- 真实 `std::bad_alloc` / 分配耗尽：`NOT_RUN`；仅静态复核异常映射。
- 私有事实损坏或存储层故障注入：`NOT_RUN`；生产没有测试 seam。
- 进程内恢复、跨进程恢复与跨进程幂等重放：`NOT_RUN`。
- 普通应用、启动阶段 17、SELF-FORM 或阶段 18 的生产消费者：`NOT_RUN`；本计划禁止修改生产装配和启动。
- 独立集成验收：`NOT_RUN`；应在结果提交、推送和交互接受之后由独立角色执行。

## 5. 清理门禁

当前三份专项仍属于计划允许的临时证据，不是正式生产代码。只有交互接受并把 `L2-SELF-S-HOST` 在计划索引正式登记退出后，才独立删除三份专项，核对三路径不存在和生产工程 / filters 零引用，并重跑根工程双配置、strict 与生产验证痕迹扫描。删除前不能宣称计划已完成最终清理。
