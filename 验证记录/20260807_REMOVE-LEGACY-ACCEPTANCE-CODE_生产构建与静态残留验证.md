# REMOVE-LEGACY-ACCEPTANCE-CODE v0.2 生产构建与静态残留验证

日期：2026-08-07

## 验证身份与边界

```text
施工起点：7b9c6a2dc4ff46e0abae121de953e15bfadecbf1
计划：REMOVE-LEGACY-ACCEPTANCE-CODE v0.2
plan blob：e5d096f3c989aedd3e03ff51c508e811faf55d20
验证目标：72 个删除文件、35 个修改文件及两个生产配置的三个正式启动模式
```

本验证只检查静态退出、生产构建、启动和正常收口。日志只做人读观察，不解析为自动 PASS；本记录不证明新 CRUD 或服务级黑盒验收。

## 静态验证

| 验证项 | 结果 |
| --- | --- |
| 设计删除表与实际删除 | PASS，72/72，缺失 0，额外 0 |
| 设计修改表与实际目标修改 | PASS，35/35，缺失 0 |
| 名称含“自检”的生产源码文件 | PASS，0 |
| 工程和筛选器中的自检源码引用 | PASS，0 |
| 旧故障自检宏和四个自检日志宏 | PASS，0 |
| 四个旧验收启动参数 | PASS，0 |
| 旧运行器、登记、专项 DTO 和具名私有探针 | PASS，0 |
| `git diff --check` | PASS |
| `python .\tools\check_specs.py --strict` | PASS，110/110 |

异主 P0-FIX、路线输入、旧 WORLD-TREE 和 `.codex-build/` 不参加目标范围统计，也未被修改。

## 构建验证

当前仓库正式解决方案文件为 `海中鱼巣.slnx`，因此按计划的同一双配置 Rebuild 合同使用该真实路径。

| 配置 | 结果 |
| --- | --- |
| Debug x64 串行 Rebuild | PASS，退出 0，生成 `x64/Debug/海中鱼巣.exe` |
| Release x64 串行 Rebuild | PASS，退出 0，生成 `x64/Release/海中鱼巣.exe` |

两次完整模块扫描和编译清单均不含已删除的旧自检模块。

## 生产启动与正常收口

| 配置 / 模式 | 启动与收口结果 |
| --- | --- |
| Debug / 普通控制面板 | 取得非零主窗口句柄；`CloseMainWindow()` 返回 true；退出 0 |
| Release / 普通控制面板 | 取得非零主窗口句柄；`CloseMainWindow()` 返回 true；退出 0 |
| Debug / `--headless` | 等待生产初始化 30 秒后发送既有 SIGBREAK 停止信号；退出 0 |
| Release / `--headless` | 等待生产初始化 30 秒后发送既有 SIGBREAK 停止信号；退出 0 |
| Debug / `--runtime-context` | 正常返回，退出 0 |
| Release / `--runtime-context` | 正常返回，退出 0 |

无窗口模式在启动后 3 秒发送 SIGINT/SIGBREAK 的前置尝试得到 Windows 控制台中断终止码，证明当时尚未进入已安装停止租约的运行阶段；这些尝试未计为通过，也未以强制终止冒充正常收口。最终双配置结果均来自等待初始化后由既有停止处理器完成的退出 0。验证结束后目标进程数量为 0。

## 具名日志人工观察

- `D:\海中鱼巣\日志\运行.log` 在本轮六个正式启动组合中没有新增记录，最后修改时间仍为 2026-08-07 07:42:39；其末尾旧专项验收文本早于本轮构建和运行，不作为当前结果。
- `D:\海中鱼巣\日志\逻辑错误.log` 没有新增记录，最后修改时间仍为 2026-08-06 18:59:38。
- 本轮没有从日志生成自动 PASS；构建退出码、正式进程退出码、窗口句柄和既有停止信号的实际收口共同构成本计划工程验证结果。

## 未证明范围

本记录不证明新 L1 中性 CRUD 已实现，不证明合法 / 非法 CRUD 的外部黑盒往返，不证明生产服务已正式接线，也不证明崩溃、跨进程恢复、STEP-5 或治理闭环完成。
