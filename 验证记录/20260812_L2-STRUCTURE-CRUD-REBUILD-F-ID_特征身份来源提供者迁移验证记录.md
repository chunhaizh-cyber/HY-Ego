# F-ID 特征身份来源提供者迁移验证记录

日期：2026-08-12

## 结果

| 验证项 | 结果 |
| --- | --- |
| 根工程 x64 Debug Rebuild | PASS |
| 根工程 x64 Release Rebuild | PASS |
| 专项 Debug 构建与运行 | PASS，8 项通过，失败数 0 |
| 专项 Release 构建与运行 | PASS，8 项通过，失败数 0 |
| 双 owner 交付不可交换 | PASS，编译期断言 |
| 定义节点与归属同写、当前来源 | PASS |
| 实例节点与三关系同写、当前来源 | PASS |
| 实例历史同截止闭合宿主与定义来源 | PASS |
| 普通应用唯一特征服务 getter | PASS |
| 英文局部变量与临时生产诊断静态扫描 | PASS，`I64` 局部变量与 `fid-init` / `fprintf` 零命中 |

## 根因修复

专项首轮运行在特征服务构造期等待。精确诊断表明定义来源初始化写返回 `事实代次漂移`：错误地使用了 owner 创建代次，而此前存在服务已推进当前事实代次。改为提交前读取当前事实代次后，Debug / Release 均完整通过。临时生产诊断已全部删除。

## 未运行

- `NOT_RUN`：生产业务运行、长期公平、分配故障注入、恢复、跨进程、私有损坏注入、独立集成验收。
- 构建和专项参数验证不证明 T1、DYN2、S-ID、A1 或业务闭环。

## 工作区说明

验证期间存在流程图维护及其它所有者 WIP，均未纳入本切片；现有 `.codex-build` 未使用。
