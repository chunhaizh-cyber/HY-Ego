# 任务结果上行桥与自我治理邮箱生产接线验证记录

日期：2026-08-21

计划身份：`RUNTIME-GOVERNANCE-TASK-RESULT-UPSTREAM-PRODUCER-WIRING v0.1`

验证基线：

- 主体源码提交：`df93cd810567ffd146f83d5275e2d35441fc07fc`；
- 拒绝分账补测提交：`0d17ef37bd06bc1670362ceed2cdf47aadd2e045`；
- 最终验证工作树相对结果提交无本计划源码 dirty；
- plan blob：`2606e9e47d794dfce165be7731e4889b3e3fd00f`。

## 1. 验证原则

生产链专项只从 `任务结果生产运行包::提交已形成任务工作结果`进入，经过真实任务工作线程、任务管理线程、唯一上行桥、正式任务实际结果发布、G1 独立读回、唯一自我治理邮箱和真实自我线程。

通过判断来自结构化返回、上行槽快照、工作线程快照、正式任务结果独立读回、自我线程处理槽及逐字段断言。控制台输出只用于定位，不作为机器通过依据。

## 2. 构建与运行

以下命令均在 `D:\海中鱼巣`执行：

```powershell
msbuild .\海中鱼巣.slnx /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m
.\x64\Debug\海中鱼巣.exe --test-治理
msbuild .\海中鱼巣.slnx /t:Rebuild /p:Configuration=Release /p:Platform=x64 /m
.\x64\Release\海中鱼巣.exe --test-治理
python .\tools\check_specs.py --strict
git diff --check
git diff --cached --check
```

结果：

- Debug x64 全量 Rebuild：通过，0 个警告、0 个错误；
- Debug 治理专项：退出码 0；
- Release x64 全量 Rebuild：通过，0 个警告、0 个错误；
- Release 治理专项：退出码 0；
- 规范目录严格检查：116 / 116 通过；
- 工作树和暂存区 diff check：通过。

两配置专项均完成现有运行期事实 provider、任务实际结果 provider、邮箱恢复重试、治理输入绑定、自我线程生产消费和最终一致事实断言。该整体输出没有被单独用作本计划矩阵结论；矩阵结论取自下述结构化断言。

## 3. 生产链验证矩阵

| 场景 | 结构化入口与输入 | 实际状态 / 正式读回 | 断言与结论 |
| --- | --- | --- | --- |
| 首次正式结果 | 从运行包提交一项版本 3、版本 / 幂等 / G0 / 七身份完整的工作结果 | 工作线程接收并形成回执；管理槽进入已提交；G1 独立读回取得唯一任务实际结果；自我线程处理槽为后继已形成 | 正式结果、治理消息、批次和后继各一；任务、实例方法、执行轮次、方法、动作、场景、主体、实际状态、动态、来源版本、运行代次和 G1 逐字段相等，PASS |
| 精确重复 | 再次提交全等工作结果 | 回执槽读回既有完成结果；正式任务结果入口返回精确重复或管理槽直接去重 | 发布计数不增加，不形成第二消息、第二批次或第二后继，PASS |
| 来源任务序号变化 | 只改变回执编号、幂等键和来源任务序号，任务结果材料保持同义 | 上行桥仍从 G0 当前实例方法取得相同正式执行轮次；按正式结果身份复用首次消息槽 | 来源任务序号未进入执行轮次，复用槽指向首次消息，零第二后继，PASS |
| 同轮异义 | 同一任务和正式执行轮次提交异义实际状态材料 | 任务事实层返回引用冲突；上行槽阶段为引用冲突 | 治理消息为空，自我线程零新后继，PASS |
| 邮箱满后恢复 | 先占满唯一邮箱，再提交合法工作结果；之后释放容量 | 正式结果已经发布并锁定 G1；槽保持治理消息待提交，邮箱恢复后同槽重试 | 任务结果发布尝试次数不增加，结果身份、G1、消息编号、幂等键、载荷和摘要保持全等，PASS |
| 停止期间排空 | 工作结果已经进入工作线程队列后请求停止新派发并收口 | 工作线程先排空并 join；管理线程继续处理结果槽；自我线程在上行收口前保持运行 | 已接收工作结果没有丢失，已发布待提交消息没有被重建，最终槽到达结构化终态，PASS |
| 当前性漂移 | 复用旧 G0 的完整工作结果，经真实工作线程进入 | 管理上行槽阶段为当前性漂移；治理消息为空 | 不自行换 G0，不形成伪造消息或后继，PASS |
| 协议拒绝 | 来源材料版本为 0 的工作结果，经真实工作线程入口进入 | 工作线程已接收数量 6、已形成回执 5、已上交 5、已拒绝 1；最后形成结果为已拒绝 / 来源材料版本缺失；管理槽不存在 | 拒绝发生在回执形成阶段，零正式结果、零治理消息、零上行槽，PASS |
| 资源 / 内部边界 | 队列容量、停止状态和阶段一致性由结构化枚举分账 | 邮箱满为资源等待；协议拒绝、引用冲突、当前性漂移和内部终止互不冒充 | 只有资源等待允许同阶段重试；已发布槽不回退，PASS |

工作线程最终计数 `6 / 5 / 5 / 1`分别表示已接收、已形成回执、已上交、已拒绝。该计数与最后拒绝结果共同证明拒绝材料没有绕过回执形成阶段进入管理线程。

## 4. 工程、编码和范围门禁

- `海中鱼巣.vcxproj`和`海中鱼巣.vcxproj.filters`均可按 UTF-8 解析为 XML；
- 九个目标源码 / 工程文件均通过严格 UTF-8 解码，NUL 数量为 0；
- `海中鱼巣.线程.任务工作线程`、`海中鱼巣.线程.上行桥.任务实际结果`、`海中鱼巣.线程.运行包.任务结果生产`各只有一个 `export module`；
- 三个新模块在工程和 filters 中各登记一次；
- 计划、详细设计和流程图实际 blob 分别为 `2606e9e47d794dfce165be7731e4889b3e3fd00f`、`2bee8b330772344d4067d5c3b2a03220834de3a8`和`ca9c0b59bcbcae96a4332c5a2f0f17314f7225d2`；
- 主体源码提交精确九文件；补测提交精确一份专项文件；两提交 `git show --check`通过；
- 发布前后 `main == origin/main`、ahead / behind 为 0 / 0、Git index 为空；
- 工作区其余未跟踪材料和构建残留均不属于本计划，未暂存、未提交、未删除。

## 5. 完成结论与限制

本基线证明以下真实进程内生产链已按计划闭合：

```text
已形成任务工作结果
-> 真实任务工作线程
-> 任务管理线程
-> 唯一任务实际结果上行桥
-> 正式任务实际结果和 G1 独立读回
-> 唯一自我治理邮箱
-> 自我线程分类与绑定复核
-> 唯一任务实际结果后继输入
```

没有验证真实任务方法已经执行，也没有证明任务完成、需求满足、服务成功、安全值或服务值变化、结算、学习、下一治理步骤、程序崩溃或跨进程恢复。普通生产程序的长时间驻留和外部真实工作线程来源仍为 `NOT_RUN`。
