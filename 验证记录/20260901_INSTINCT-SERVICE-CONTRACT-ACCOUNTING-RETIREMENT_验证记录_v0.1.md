# INSTINCT-SERVICE-CONTRACT-ACCOUNTING-RETIREMENT 验证记录 v0.1

日期：2026-09-01

计划身份：`INSTINCT-SERVICE-CONTRACT-ACCOUNTING-RETIREMENT`

代码差异起点：`d27c6ce685c52d1c42ac17c2535478e90bae9bc3`

执行期重新 S0 基线：`368009f86fe379ca60b438056797833e3f7f7d2d`

根工程：`海中鱼巣.vcxproj`

## 1. 结论分账

| 项目 | 结果 | 结论边界 |
| --- | --- | --- |
| 目标差异独立快照 Debug x64 fresh Rebuild | PASS | `368009f + 本计划差异`根工程 0 error |
| 目标差异独立快照 Release x64 fresh Rebuild | PASS | `368009f + 本计划差异`根工程 0 error |
| 目标差异独立快照 Debug 专项 runner | 分项 PASS / 整体退出 2 | 两项上游初始化路径失败；本计划相关分项通过 |
| 目标差异独立快照 Release 专项 runner | 分项 PASS / 整体退出 2 | 与 Debug 分账一致 |
| 纯 `368009f` Debug 对照 runner | 整体退出 2 | 失败文本与目标差异快照一致，排除本计划新增回归；不把失败升级为 PASS |
| 退役符号扫描 | PASS | 生产源码和工程登记零命中 |
| 保留能力扫描 | PASS | 完整秒时钟、UTC、主动 / 分层安全、硬否决和普通认知结构仍存在 |
| `git diff --check` | PASS | 无空白错误；仅行尾转换提示 |
| 严格规范检查 | PASS | `114/114` |
| 旧九项持久载荷运行恢复 | NOT_RUN | 现行业务 API只生成八项；未增加测试缝或旧 writer |

## 2. 构建

从 `368009f` 的 Git archive 建立全新目标差异快照，只覆盖十二个逐 hunk 混合文件、五个目标专属纯文件并删除十二个退役文件。Debug x64 与 Release x64 fresh Rebuild 均完整编译、链接并返回 0 error。

隔离快照路径：

```text
D:\TEMP\海中鱼巣\INSTINCT-SERVICE-CONTRACT-ACCOUNTING-RETIREMENT\20260901-stage-01\snapshot-02
```

独立复核按 HEAD 逐文件比较确认：除后续只用于运行专项的 `海中鱼巣/入口.cpp` runner 覆盖外，快照相对 HEAD 只包含十七个白名单修改和十二个白名单删除；未带入工作树任务 / 自我治理外来 WIP。Debug / Release fresh Rebuild 发生在 runner 覆盖入口之前，runner 构建仅随后重编入口并链接既有目标快照对象。

## 3. 仓库外专项 runner

目标差异隔离目录：

```text
D:\TEMP\海中鱼巣\INSTINCT-SERVICE-CONTRACT-ACCOUNTING-RETIREMENT\20260901-stage-01\snapshot-02
```

runner 调用以下既有专项：

```text
运行安全根定义与当前值端到端测试
运行安全根生产定义初始化端到端测试
运行本能路线阶段二端到端测试
运行需求服务端到端测试
运行自我治理一致事实端到端测试
运行任务现实执行硬否决规则宇宙端到端测试
```

Debug 与 Release 均成功重编 runner 入口、链接、启动；两种配置分项结果一致。

### 3.1 本计划相关 PASS

- 安全根定义 S00-S19 的全部可运行项通过；S08、S10 因验证宏未启用为 NOT_RUN。
- 安全根生产定义初始化 P00-P04B 全部可运行项通过；P05 因验证宏未启用为 NOT_RUN。
- 本能路线阶段二 V01、V01A、V02-V17、V16A 的全部可运行项通过；V13、V05 的专属 fixture 分支和 V16A 的故障 fixture 分支为 NOT_RUN。
- 任务现实执行硬否决规则宇宙 P00-P08 全部通过，包括持久恢复。
- 新八项安全定义的发布、精确重复、冲突、当前读回和重启恢复均取得运行证据。

### 3.2 上游专项既有失败与对照

目标差异 Debug 与 Release runner 都出现：

```text
[测试] 失败：系统世界树根初始化失败
[治理测试] 失败：方法查找真实链系统根初始化失败
[治理测试] 失败：需求初次筹办工作包步骤=建立需求目标公开事实基础
```

因此：

- `运行需求服务端到端测试` 失败；
- `运行自我治理一致事实端到端测试` 失败；
- runner 整体退出码为 2。

另从纯 `368009f` Git archive 建立 `baseline-head-02`，不加入任何本计划代码差异；Debug Rebuild 后只覆盖同一 runner 入口并运行，得到完全相同的三条失败文本和整体退出码 2。因此这些失败不是本计划新增回归；但对照也不能把 V05 / V07 中依赖这些上游初始化路径的运行结论升级为 PASS。

`端到端测试.自我治理一致事实.ixx` 的退役 ABI 最小适配已在目标差异快照中独立编译并实际进入 runner；其可在到达上游阻断前完成“认知证据边界两个旧入口无账拒绝矩阵”。完整一致事实专项仍按上述边界记为未通过，而不是伪报 PASS。

## 4. 旧九项兼容边界

现行 `发布安全根定义请求_v1` 已不包含退役字段，正式 `编码定义` 只会生成八项；现有验证宏也没有九项载荷注入入口。因此普通发布、重复和重启测试不能证明旧九项持久载荷恢复。

本轮没有为了测试新增旧 writer、生产注入钩子或迁移入口。结论为：

- 源码检查：`解码定义` 接受八或九项；九项索引 7 仅校验格式后忽略，来源取末项；
- Debug / Release 构建：兼容代码均编译通过；
- 旧九项持久载荷运行恢复：`NOT_RUN`；
- 不声明“全部旧数据均可恢复”。

## 5. 静态删除与保留扫描

退役扫描命令：

```powershell
rg -n --glob '!施工记录/**' --glob '!验证记录/**' `
  '服务合同事实权威|本能单完整秒被动维护|本能被动维护游标|本能被动维护基础快照|Gseed|服务生存维护|需求服务结算|被动服务维护规则版本|当前合同组|当前准备组|当前进展组|当前到期未满足组' `
  海中鱼巣 海中鱼巣.vcxproj 海中鱼巣.vcxproj.filters
```

结果：退出码 1，零命中。说明生产源和工程登记不再含退役模块或符号。

反向保留扫描确认：

- `海中鱼巣/领域/服务.完整秒时钟.ixx` 及工程登记仍存在；
- UTC 时钟、启动 UTC 证据和运行代次仍存在；
- 主动安全结算来源规则与安全根定义仍存在；
- 分层安全协议、算法、服务和自我线程安全维护阶段仍存在；
- 任务现实执行硬否决规则宇宙与两轮硬否决调用仍存在；
- 普通需求、任务、方法、正式结果，以及状态、动态、因果结构模块仍存在。

保留扫描只证明代码和工程登记没有被误删；不单独证明相关业务闭环全部完成。

## 6. 治理检查

```text
git diff --check
=> PASS

python .\tools\check_specs.py --strict
=> 海中鱼巣正式规范目录一致性检查: 114 份目录项全部通过
```

精确暂存后还必须执行 `git diff --cached --check` 并逐 hunk 核对 index；发布只允许包含本计划白名单代码、十二个删除文件和本轮两份记录。候选工具只在固定 `368009f`、全新且位于计划专属 `D:\TEMP` 的互不包含目录下条件成立；脚本没有自行锁定 HEAD 或保护错误参数，不能把脚本本身记为无条件安全工具。

## 7. 可声明范围

本证据最多支持：服务合同记账、合同驱动完整秒维护、Gseed / 游标和服务值被动调节安全根的生产体系已经退出；旧材料没有被主动擦除；所列保留能力没有被误删。

不支持：真实服务请求专用入口已接线、完整服务业务闭环已验收、全部旧数据可恢复、服务根已有自动生产者、自我治理全部完成或集成验收完成。
