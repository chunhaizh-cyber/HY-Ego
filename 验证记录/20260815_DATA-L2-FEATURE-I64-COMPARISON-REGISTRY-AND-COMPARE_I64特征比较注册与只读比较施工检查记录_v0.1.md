# DATA-L2 I64 特征比较注册与只读比较施工检查记录

日期：2026-08-16
版本：v0.1
计划身份：`DATA-L2-FEATURE-I64-COMPARISON-REGISTRY-AND-COMPARE`

## 1. 检查范围

本记录是施工检查，不是独立验收。检查对象只有两份生产变更和两份专属记录；没有建立专项工程、生产 seam、伪造消费者或永久测试入口。

## 2. 实际命令与结果

最终代码执行：

```powershell
MSBuild.exe .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m:1 /nologo /v:minimal
MSBuild.exe .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Release /p:Platform=x64 /m:1 /nologo /v:minimal
```

结果：

- x64 Debug Rebuild：PASS，编译并链接 `x64\Debug\海中鱼巣.exe`；
- x64 Release Rebuild：PASS，编译并链接 `x64\Release\海中鱼巣.exe`；
- 两次命令退出码均为 0，输出未报告编译或链接错误。

治理与机械检查：

```powershell
git diff --check -- <两份生产文件与两份专属记录>
git diff --cached --check
python .\tools\check_specs.py --strict
```

最终静态检查覆盖：

- 4180 比较状态、拒绝原因和三位结果要求的冻结数值；
- 八个公开入口存在，注册只接受 I64 标量算法族；
- 类型登记固定三个节点，注册新增固定为一节点、一关系、两值、两槽；
- 同特征定义和用途的唯一当前路由、同键复义拒绝、替换/退出闭包和定义退出引用保护；
- 当前与历史读取的同截止完整性，以及历史额外属性拒绝；
- 目标判断和状态迁移左右角色与具名关系族不反转；
- 排序不做减法，差异固定右减左，I64 极值先做可表示性检查；
- 请求结果位与成功载荷逐位一致；
- `比较特征(...)` 函数体内不存在 `提交所有者范围中性写集` 或写集构造；
- 工程文件零差异，旧比较模块、第二服务、测试 seam、sleep 和忙重试为零；
- 四份本计划文件可按 UTF-8 严格解码、无 BOM、无行尾空白；
- `check_specs.py --strict`：113/113 PASS。

## 3. 未运行与声明边界

遵循“由实际使用判断服务是否合格”的现行口径，本叶没有另建动态专项或独立验收。以下保持 `NOT_RUN`：

- 合法消费者真实执行注册首次、精确重复、替换、退出和比较；
- 同/异请求并发、真实资源耗尽和发布结果未知重放；
- 私有半结构、注册复义和坏历史投影的运行态防御；
- 抽象目标状态、需求、任务、状态迁移和需求满足的正式消费；
- 普通应用装配、DATA-L3 聚合和 BIZ 业务闭环。

双配置构建与静态检查只证明本叶进入当前生产目标并符合已检查合同，不证明上述运行分支或《数据服务最终需求清单》整体完成。
