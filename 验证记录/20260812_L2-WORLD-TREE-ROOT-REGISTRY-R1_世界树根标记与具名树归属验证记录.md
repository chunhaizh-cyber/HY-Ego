# L2-WORLD-TREE-ROOT-REGISTRY-R1 世界树根标记与具名树归属验证记录

日期：2026-08-13

验证对象：计划 v0.2，plan blob `c697337a6aaf281a8df91d92646d26b6980bb7b4`，施工起点 `96a5089cf08e1d4bbde9e07a7e59631bbbf13ce3`。

## 1. 总结

根工程 Debug / Release x64 Rebuild 均 PASS；R1 独立专项 Debug / Release 各 29 / 29 PASS、失败数 0；strict 113 / 113、UTF-8、XML、diff、范围、旧 ABI 和生产验证痕迹扫描均 PASS。专项双配置仅有 MSBuild `MSB8029` 临时目录增量构建警告；每轮实际使用独立 GUID 目录和 `Rebuild`，构建与运行退出码均为 0。

## 2. 运行矩阵

| 类别 | 结果 | 实际覆盖 |
| ---: | --- | --- |
| 1 | PASS | 构造期正式树结构登记和原请求精确重复读回。 |
| 2 | PASS | 两棵树分别建立，身份互异，根身份等于树身份。 |
| 3 | PASS | 根的场景 / 族归属 / 根标记同写；根零父。 |
| 4 | PASS | 非根的场景 / 族归属 / 树归属 / 父关系同写。 |
| 5 | PASS | 当前与创建截止历史树归属一致；退出前历史仍可读。 |
| 6 | PASS | 当前 / 历史整树包含完整节点和父关系并稳定读回。 |
| 7 | PASS | 同树父子移动原子换代；原请求精确重复。 |
| 8 | PASS | 跨树父子和根作子端入口拒绝且零变化。 |
| 9 | PASS + NOT_RUN | 根创建原请求精确重复；私有重复根标记 / 树归属、双类并存、错锚点 / 类型 / 角色 / 端点无法经公开 API 注入，保持 NOT_RUN。 |
| 10 | PASS | 成员新增不改变树归属；成员新增 / 替换 / 退出原请求精确重复均收敛。 |
| 11 | PASS | 属性新增 / 换代原请求精确重复均收敛；树归属门禁参与同代前置。旧裸场景分支由生产源码与旧 ABI 零消费静态复核。 |
| 12 | PASS | 非叶退出、根规范退出、缺树归属和错直接父均引用冲突且四载荷空；非根叶单事务退出并返回四项同代历史载荷；退出原请求精确重复；退出截止公开归属为 `已退出` 空载荷。 |
| 13 | PASS | 旧期望新请求事实代次漂移；同一旧期望并发不同请求仅一笔发布、另一笔漂移；公开写原请求重复有界收敛。 |
| 14 | PASS | 生产源码零旧世界登记 / P1B / P1A3、故障注入、sleep、计数器、测试友元、测试分支和诊断标记。 |
| 15 | PASS | 根工程与专项双配置、strict、UTF-8、XML 和 diff 全部通过。 |
| 16 | PASS | 三套旧专项九文件删除；旧四 ABI 非文档运行源码零消费；新 R1 专项覆盖新 ABI。 |

## 3. direct-submit 专项复核

属性新增 / 换代、父子替换、成员新增 / 替换 / 退出均在确定性写集形成后调用 `检查S2当前代次`。同代成功才执行树归属和业务前置；事实代次已前进的原完整请求绕过当前态前置并由 L1 返回 `精确重复`。Debug / Release 均逐项运行验证这六个入口，未把许可、资源或内部错误降格为入口拒绝。

## 4. 命令与结果

| 命令 | 退出码 |
| --- | ---: |
| `MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /verbosity:minimal` | 0 |
| `MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Release /p:Platform=x64 /verbosity:minimal` | 0 |
| `.\验证工具\运行L2世界树根标记与具名树归属参数验证.ps1 -配置 Debug -幂等基值 915000` | 0；29 / 29 |
| `.\验证工具\运行L2世界树根标记与具名树归属参数验证.ps1 -配置 Release -幂等基值 916000` | 0；29 / 29 |
| `python .\tools\check_specs.py --strict` | 0；113 / 113 |
| 十六允许路径 `git diff --check` | 0 |
| 五个保留文件严格 UTF-8 解码与专项 vcxproj XML 解析 | 0 |
| 旧四 ABI 非文档消费扫描 | 0 命中 |
| 生产验证痕迹与专项 `[STEP]` / `[DIAG]` / `unitbuf` 扫描 | 0 命中 |

## 5. NOT_RUN

- 真实分配故障与私有损坏注入：NOT_RUN。
- 持久恢复、崩溃恢复、跨进程恢复和跨进程幂等重入：NOT_RUN。
- 长期并发公平：NOT_RUN。
- 生产业务、真实消费者与独立集成验收：NOT_RUN。
- 现实世界资格、自我初始化、A1、I1 / I2：NOT_RUN。
- 整树退出、递归级联和物理回收：NOT_RUN。

## 6. 声明边界

这些结果只证明当前 R1 代码切片在场景 owner 内形成多树、单根、显式归属、同树父子和所列读写 / 退出矩阵；不证明完整世界树服务聚合、生产装配、自我初始化、持久恢复或数字生命业务闭环。独立集成验收尚未执行。
