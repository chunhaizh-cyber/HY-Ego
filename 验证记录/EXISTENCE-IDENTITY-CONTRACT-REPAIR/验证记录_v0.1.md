# EXISTENCE-IDENTITY-CONTRACT-REPAIR 验证记录 v0.1

## 1. 静态检查

- `git diff --check`：通过。
- `git diff --cached --check`：提交前执行，结果见本次发布复核。
- 静态读回：`存在信息身份`位于`存在类数据合同版本`之后，`实例特征IFR完整投影`、读取请求和提交请求之前；唯一字段、默认相等和`有效`谓词与计划第3节一致。

## 2. 独立头翻译

临时目录：`D:\TEMP\海中鱼巣\EXISTENCE-IDENTITY-CONTRACT-REPAIR\`（不在仓库）。临时源仅为：

```cpp
#include "数据服务.存在类.h"
int main() { return 0; }
```

使用 Visual Studio 18 Professional 的 x64 C++20 工具链执行：

```text
call "C:\Program Files\Microsoft Visual Studio\18\Professional\Common7\Tools\VsDevCmd.bat" -arch=x64 -host_arch=x64
cl /nologo /std:c++20 /EHsc /c /I "D:\海中鱼巣\海中鱼巣\领域" "D:\TEMP\海中鱼巣\EXISTENCE-IDENTITY-CONTRACT-REPAIR\include_exists.cpp" /Fo"D:\TEMP\海中鱼巣\EXISTENCE-IDENTITY-CONTRACT-REPAIR\include_exists.obj"
```

结果：退出码0，仅输出`include_exists.cpp`；对象文件已在隔离目录生成。它证明该头在该最小翻译单元中可解析，并特别排除了之前`存在信息身份`未声明的首错。

## 3. 结论边界

未运行根工程、IF/R行为夹具、启动、恢复或集成验收。最小翻译成功不替代根材料计划的专项及 Debug/Release 构建验证。
