# L1-SIMPLIFY-P10-EXISTENCE-FEATURE-VALUE v0.1 验证记录

## 1. 验证对象

- plan blob：`8c9b4d2fdcc2b5ee97579453d02c7e01eb03d5a4`
- 验证基线：`51a59f763635325f09a1dc55341d24a8526114bf`加P10五文件工作区差异
- 目标：SELF-GOV-CLOSURE v0.2 STEP-3D源码提供者

## 2. 动态与规范门禁

| 门禁 | 实际结果 |
| --- | --- |
| Debug x64 串行Rebuild | PASS，0 warning / 0 error |
| Debug `--self-test-exit` | PASS，退出码0，顶层31/31 |
| Release x64 串行Rebuild | PASS，0 warning / 0 error |
| Release `--self-test-exit` | PASS，退出码0，顶层31/31 |
| Debug MSVC模块扫描 | PASS，sources/modules=197/197，failed=0，循环0，外部需求0 |
| Release MSVC模块扫描 | PASS，sources/modules=197/197，failed=0，循环0，外部需求0 |
| `python .\tools\check_specs.py --strict` | PASS，110/110 |
| P10目标`git diff --check` | PASS |

模块扫描候选输出仅写入系统临时目录，没有形成仓库产物。

## 3. 专项矩阵

顺序320保持唯一顶层专项，验收项由45扩展为62，全部随双配置完整自检通过。新增17项验证：

1. 两个P7实际存在及各自P8 I64实例槽按正式入口建立；
2. 首次换代只推进一次代次，当前节点/关系数量不变，当前值一退一进；
3. 新当前保留宿主、P2定义、实例槽、宿主关系和定义关系；
4. 旧当前返回已退出且空载荷，历史事实材料/来源保留，当前槽唯一指向新值；
5. 完整重复为幂等读回且不推进代次，同键异义和场景/实际存在跨variant同键均冲突；
6. 新旧材料相同时使用新幂等键仍换代；同一存在连续三轮后较早值只在历史入口可读；
7. 非法身份、越界、旧基线、错误当前和未绑定provider均返回结构化非成功且不形成假成功；
8. 第二存在独立换代成立；两个存在基于同一代次真实竞争时精确一个提交、一个事实代次漂移，最终权威读回完整且无混宿主/混槽。

## 4. 静态合同与范围

- 新DTO、有效性函数、公开根函数和私有提交核心均定义唯一；旧四个单值换代字段命中0。
- 两入口共用`0x07`有序variant账和唯一写集核心；静态写集门为节点0、关系0、值1、槽变更1、退出1。
- P10生产入口经P8公开实际存在实例特征读取、P2定义和L1公开读取/提交；对P6、P9状态、旧WORLD-TREE、SQL和日志依赖命中0。
- 最终工作区P10差异精确三个代码文件和两份专属记录；P15/P29发布增量均只含各自文档和计划索引，与P10五文件零重叠。

## 5. 最大证明边界

本验证只证明P10源码提供者、顺序320具名矩阵、双配置本地构建/自检、规范检查和模块依赖闭合。不证明第二关系19状态、关系20迁移动能、动作动态、因果、生产装配、SQL/恢复、旧消费者迁移、具名集成验收、STEP-3整体或治理闭环完成。
