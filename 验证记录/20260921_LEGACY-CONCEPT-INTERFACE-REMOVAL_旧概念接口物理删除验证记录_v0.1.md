# LEGACY-CONCEPT-INTERFACE-REMOVAL 验证记录 v0.1

日期：2026-09-21

计划：`LEGACY-CONCEPT-INTERFACE-REMOVAL` v0.1

计划 blob：`857de5e4bdc2a0e0463d04859aae78a712dba031`

验证基线：`aa465854b210d9e0af01c64b314ea8fdf2fbd655` 加本计划未提交白名单 WIP

结果提交：本记录与代码结果同一提交发布。

仓外隔离根：`D:\TEMP\海中鱼巣\LEGACY-CONCEPT-INTERFACE-REMOVAL\20260921-a`

## 1. 根工程构建

### 1.1 Debug x64

使用当前 Visual Studio 18 Professional MSBuild 对 `海中鱼巣.vcxproj`执行 Debug x64 构建，`OutDir/IntDir`指向仓外 `root-debug*`隔离目录。

结果：退出码 0，0 个警告，0 个错误；生成仓外 `海中鱼巣.exe`。

### 1.2 Release x64

命令：`MSBuild.exe 海中鱼巣.vcxproj /m /t:Rebuild /p:Configuration=Release /p:Platform=x64`，输出目录为 `root-release/out`和`root-release/int`。

结果：退出码 0；生成 `root-release/out/海中鱼巣.exe`。`Previous IPDB not found`为隔离目录首次全量代码生成信息，不是警告或错误。

## 2. 专项验证

### 2.1 绑定存在创建专项

命令：Debug x64 构建 `验证工具/绑定存在创建专项验证.vcxproj`，输出目录为 `binding-debug2`，随后运行生成的可执行文件。

最终结果：构建退出码 0；运行退出码 0；`BOUND_EXISTENCE_OK 118`。

覆盖：现行存在结构、世界树 v4 直接子场景/场景成员/父存在组成、精确重放、非法目标零写、pure v2/FC 出生使用/存在两组结构、I64 特征概念、显式顶层、同类别多父、类别冲突零写和两组定义正式读回。

中途分账：初次运行在越界的纯概念退役断言失败；删除该计划外治理断言后，读回测试先因错误传入基础概念预算而返回“数量预算不足”。最终按读取合同使用创建结果 `H`和完整`存在概念两组预算_v3`，运行通过。生产语义未为测试改动。

### 2.2 相关概念窄参与专项

命令：Debug x64 Rebuild `验证工具/相关概念窄参与专项验证.vcxproj`，输出目录为 `related-debug3`，随后运行生成的可执行文件。

最终结果：构建退出码 0；运行退出码 0；`PASS-TOTAL 152`。

覆盖：现行相关概念构造、特征/存在概念参与、组合事务、精确重放、幂等冲突、缺失挂接、类别/签名/父类冲突、成环、预算、截止、参与者/端口/owner 错误和同 G 竞争单提交。

中途分账：首次构建发现专项夹具仍调用 6 参数存在服务构造，并出现概念树对象 `/bigobj`不足；修正现行存在结构登记和专项编译选项后，链接又暴露已无测试调用边的普通应用等陈旧编译闭包。专项工程删除这些无关编译单元并补入特征值域比较实现后构建通过。首次运行因单例角色登记幂等身份不是合同固定值 `0x1202`失败；修正为正式固定值后全部断言通过。

### 2.3 世界树根启动专项

命令：Debug x64 Rebuild `验证工具/世界树根启动专项验证.vcxproj`，输出目录为 `world-root-debug`。

结果：构建退出码 0；生成 `world-root-debug/out/世界树根启动专项验证.exe`。详细设计只要求该项构建，本轮未运行该可执行文件。

## 3. 旧符号与工程登记扫描

在 `海中鱼巣/**`、`验证工具/**`、根 `.vcxproj/.filters`的 C++ 和工程文件中扫描：

- `概念树根引用组`
- `概念树结构类型`
- `概念树结构交付`
- `概念树共享结构交付`
- `特征概念结构交付`
- `概念树存在扩展交付`
- `共享应用已启用`
- `旧布局(`
- `存在概念树应用服务`

结果：`OLD_SYMBOL_SCAN=ZERO`。历史规范、设计、计划和记录未纳入生产零命中要求，也未修改。

## 4. Git 与范围检查

- `git diff --check`：通过；仅输出 Windows 工作区 LF/CRLF 提示，无空白错误。
- 变更文件均位于计划白名单或为本计划专属施工/验证记录。
- `%TEMP%staged.txt`、既有 `.obj`、`vc140.pdb`和`海中鱼巣/Debug/`仍为未跟踪异主材料，未修改、未清理、未暂存。
- `git diff --cached --check`在精确暂存后执行并记录于发布步骤。

## 5. 未执行与声明边界

- 未运行世界树根专项；详细设计只要求其 Debug 构建。
- 未执行旧持久数据迁移/物理删除、跨进程恢复、RC/DC、全仓全部专项或集成验收。
- 本记录只证明旧接口零命中、根工程双配置编译、三个具名专项编译和两个具名专项局部运行，不升级为完整概念系统或业务闭环完成。
