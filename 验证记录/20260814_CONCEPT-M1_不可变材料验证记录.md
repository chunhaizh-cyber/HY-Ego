# CONCEPT-M1 不可变材料结构验证记录

日期：2026-08-14

计划身份：`CONCEPT-SPACE-STRUCTURE-CRUD-REBUILD-M1 v0.3`

验证基线：工作区 `main == origin/main == ea222a734c4cab239b6d459613a914adf2135e9a` 加 M1 九路径未提交候选；异主 WIP 不属于验证输入。

## 1. 构建与专项结果

| 验证 | 命令 | 实际结果 |
| --- | --- | --- |
| 根工程 x64 Debug fresh Rebuild | `MSBuild.exe .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m` | PASS，0 warning / 0 error |
| 根工程 x64 Release fresh Rebuild | `MSBuild.exe .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Release /p:Platform=x64 /m` | PASS，0 warning / 0 error |
| 专项 Debug | `.\验证工具\运行不可变材料结构参数验证.ps1 -配置 Debug` | `59 PASS / 0 FAIL / 6 NOT_RUN` |
| 专项 Release | `.\验证工具\运行不可变材料结构参数验证.ps1 -配置 Release` | `59 PASS / 0 FAIL / 6 NOT_RUN` |
| 规范一致性 | `python .\tools\check_specs.py --strict` | `113 / 113 PASS` |
| diff | `git diff --check` | PASS |
| XML | PowerShell `[xml]` 解析根工程、filters、专项工程 | 3 / 3 PASS |
| UTF-8 | `.NET UTF8Encoding(false, true)` 解码九路径 | 9 / 9 PASS |

专项输出为每个用例单独打印 `CASE / 输入 / 预期 / 实际 / PASS`，下表按计划第 7 节逐行对账；`59` 只是汇总，不替代矩阵。

## 2. 计划第 7 节逐行证据

| 计划类别 | 输入与预期 | 实际黑盒证据 | 静态 / 物理证据 | 结论 |
| --- | --- | --- | --- | --- |
| 构造登记黑盒 | 显式根；六节点；同 owner 同根再构造；预期首次成功、再构造精确重复、登记截止不前进 | 显式构造、六项当前 / 历史读回、六节点同 owner 同代、三属性类型均 U64 组、二次构造全部 PASS | 固定 owner `0x4C324D4154455201`、登记幂等身份 `0x4C324D4154524701`、六本地键只在生产模块出现；固定登记用 owner 创建代次原样重放 | PASS |
| 合法材料黑盒 | 系统签发严格 UTF-8 `abc`；普通来源通用字节 `00 7f 80 ff 01`；预期首次提交与逐字段读回 | 两种来源 / 格式均首次成功；格式、长度、摘要、来源、创建代次和物理定位逐字段一致 | 写集固定一个节点、三值、三槽；格式 / 长度 / SHA-256 均为 U64 组 | PASS |
| provider 构造 | 空根、相对根、不存在根、普通文件根、根重解析点；预期配置无效；显式绝对普通目录成功 | 五种拒绝和一种成功在 Debug / Release 全部 PASS；根重解析点由 Win32 目录挂载点真实构造 | 服务中无默认路径、`getenv`、`GetEnvironmentVariable` 或环境回退；逐组件句柄验证 | PASS |
| 来源分层 | 零编码、未找到节点、已退出节点、非节点关系、调用前陈旧截止、真实当前普通节点；预期对应拒绝 / 未找到 / 已退出 / 非成功 / 漂移 / 成功，失败零 L1 变化 | 六类全部 PASS；失败前后事实代次相同；普通节点来源写入回显相同来源编码 | 唯一来源入口为 `读取中性当前节点`；读取截止与请求期望代次互证后才进入候选和写集 | PASS；读后真实漂移另见 NOT_RUN |
| 候选边界 | 根外、`候选/../` 穿越、相对、缺失、父目录重解析越界、C 根候选指向 D 卷；预期入口拒绝且零 L1 变化 | 六类全部 PASS；同卷和跨卷目录挂载点均真实构造 | `FILE_FLAG_OPEN_REPARSE_POINT`、最终规范路径包含关系和卷序列三重校验；不是字符串前缀证明 | PASS |
| 内容 | 长度错、摘要错、严格 UTF-8 空 / BOM / 过长序列 / `U+FFFD`；预期入口拒绝 | 七类全部 PASS且失败零 L1 变化 | 流式严格 UTF-8 状态机、BCrypt SHA-256、长度和摘要全量复读 | PASS |
| 幂等与内容收敛 | 同键同请求、同键异义、不同键同内容同来源、同内容异来源、摘要路径预置错误字节；预期重复 / 冲突 / 同身份 / 冲突 / 内部不一致 | 五类全部 PASS；错误字节通过预置真实最终摘要路径黑盒触发，不以“SHA-256 碰撞无法构造”代替 | 请求账比较完整请求；内容键为格式 + 长度 + 摘要；命中后仍复读完整字节和 L1；待决失败时只有原幂等身份可再执行，异键返回资源失败 | PASS；待决失败重放由静态证据证明 |
| 当前 / 历史 | 当前、首次截止历史、创建前截止、退出后当前 / 历史；预期单一 optional、创建前未找到、退出后当前已退出、历史保留生命周期 | 五类全部 PASS；没有 vector 历史 API 或结果 | 读取从历史节点、三个槽和三值重组；DTO 只含单个 `optional<不可变材料事实>` | PASS |
| 引用保护 | 当前结构关系引用；当前独立材料引用值；预期退出引用冲突、发布代次不变、材料仍完整可读 | 结构引用和值引用两组均 PASS | L1 完整退出集合一次提交，服务不跨 owner 拼接退出集合 | PASS |
| 退出闭包 | 退出前取三值；节点 + 三值同代退出；退出后四项当前不可见、历史节点三槽与三历史值完整 | 三值编码、四事实退出代次、当前状态、历史载荷全部逐项 PASS | 退出写集只有四个升序事实编码，无新节点 / 关系 / 值 / 槽；发布后正式历史读回 | PASS |
| 失败守恒 | 公开可达候选、路径、格式、来源、内容腐坏失败；预期零 L1 变化 | 每项均比较前后事实代次并 PASS；对象目录最终无 `.候选-*` 临时残留 | 已知错误路径删除本次临时文件；L1 发布后失败不回滚事实、不以日志补成功 | PASS；不可稳定注入项见 NOT_RUN |
| 并发分层 | 八线程同幂等同请求；八线程不同幂等同内容同来源；预期恰一 `已提交`、其余 `精确重复`、同一身份、只前进一代 | 两组均逐结果检查状态、载荷、身份和事实代次，全部 PASS | 协调锁只保护请求账 / 内容索引；等待前后均释放锁，文件系统和 L1 调用不在锁内 | PASS |
| 物理与静态 | 摘要路径、复读、无覆盖、跨卷 / 重解析、对象腐坏、无临时残留；预期物理合同成立 | 两位分片 + 64 位小写摘要路径、完整字节复读、重复后写入时间不变、腐坏对象内部不一致、无临时残留全部 PASS | BCrypt、同卷临时文件、`MoveFileExW` 不可覆盖、失败后目标复读、卷序列、重解析点、最终规范路径和 `bcrypt.lib` 工程登记均有源码 / 工程证据；生产模块无 `.inl` | PASS |

## 3. 精确 NOT_RUN

以下六项均是计划第 7 节明确允许在“无法无注入稳定触发”时逐项登记的边界；没有用它们替代任何可公开构造的强制动态分支：

| 项目 | 未运行原因 | 已保留证据 |
| --- | --- | --- |
| 来源读取后到 L1 提交前真实漂移 | 生产代码禁止调度让行 / 故障注入，公开 API 无稳定窗口 | 动态覆盖调用前陈旧截止；静态确认来源读取截止进入提交条件且漂移映射为 `事实代次漂移` |
| 真实 I/O 耗尽 | 不可安全、确定地耗尽系统 I/O 资源 | 文件 / 目录 / 读写失败映射与临时清理静态证据 |
| 真实分配耗尽 | 不可安全、确定地耗尽进程分配 | 公开边界捕获 `bad_alloc` / `length_error` 并映射资源失败 |
| rename 结果未知 | Windows API 无公开、稳定的“已执行但结果未知”选择入口 | `MoveFileExW` 失败后重新打开最终摘要对象并按存在 / 一致 / 不一致裁决的静态证据 |
| L1 资源失败 | 正式 L1 公开入口无故障选择或注入 | 所有 L1 状态到 M1 状态的穷尽映射静态证据 |
| 发布后正式读回失败 | 正式 L1 公开入口无发布后损坏或读回故障注入 | 写后 / 退出后必须正式读回，否则内部不一致且不伪造回滚的静态证据 |

真实 SHA-256 碰撞异字节和异卷没有登记为 NOT_RUN：前者通过在真实摘要目标路径预置错误字节验证 `内部不一致`，后者通过 C 卷受控根下指向 D 卷的目录挂载边界验证 `入口拒绝`。

## 4. 生产零验证痕迹与工程检查

- 生产模块扫描 `测试|故障注入|调度让行|friend class|验证工具|NOT_RUN|PASS|海中鱼巣_M1` 为零命中。
- 生产模块没有测试配置、计数器、调度 seam、宏选择、私有状态出口、替代 provider、`.inl` 或第二实现文件。
- 数据头中的默认相等运算符属于冻结 DTO 合同，不是测试访问器；服务模块没有新增公开比较运算符。
- 根工程对数据头、模块和 `bcrypt.lib` 的登记唯一；filters 同步；专项工程独立于生产入口。
- XML、严格 UTF-8、`git diff --check` 和 strict 均通过。

## 5. 未证明范围与清理门禁

本验证最多证明不可变材料结构服务的计划内公开入口、文件物理边界和进程内并发。未证明跨进程恢复、材料物理回收、生产根选择 / 生命周期、A1 / 普通应用 / 启动接线、语言 / 词条 / 词性、四本体根、概念 / 名称 / 因果链或阶段 18。

三份专项源码 / 工程 / 脚本仍为受跟踪临时资产。只有结果提交被交互接受且计划索引退出后，才允许独立删除；删除后必须再跑根工程 x64 Debug / Release fresh Rebuild、strict、diff 和生产验证痕迹扫描。正式代码中不保留为验证增加的额外代码。
