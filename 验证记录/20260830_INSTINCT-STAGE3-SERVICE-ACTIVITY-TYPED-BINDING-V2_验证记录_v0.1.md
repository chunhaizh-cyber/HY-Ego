# INSTINCT-STAGE3-SERVICE-ACTIVITY-TYPED-BINDING-V2 验证记录 v0.1

日期：2026-08-30

## 1. 验证对象与隔离根

- 当前候选白名单：两个服务合同事实权威生产文件、一个现有专项文件及本计划两份记录。
- 隔离候选根：`D:/TEMP/海中鱼巣/INSTINCT-STAGE3-SERVICE-ACTIVITY-TYPED-BINDING-V2/20260830-064529`。
- 隔离树由 `HEAD` 导出后只覆盖本计划三个生产/专项文件；仓库内其它未提交 WIP 未进入构建输入。
- 专项临时入口、IFC 映射、静态库和 ABI consumer 全部位于上述仓库外隔离根。

## 2. 结果分账

| 门禁 | 结果 | 证据边界 |
| --- | --- | --- |
| v143 x64 Debug 根工程 | PASS | 验证宏开启，隔离候选完整构建与链接成功 |
| v143 x64 Release 根工程 | PASS | 验证宏开启，完整代码生成并链接成功 |
| Debug / Release 服务合同专项 | PASS | P00—P24、Q00—Q06、V00—V04（含 V02A/V02B）全部 PASS，进程 exit 0 |
| Debug ABI consumer | PASS | 独立导入模块，消费 v2 DTO、完整性及两个公开成员函数，编译/链接/运行 exit 0 |
| Release ABI consumer | PASS | `/GL` 编译、`/LTCG /OPT:REF /OPT:ICF` 链接，运行 exit 0 |
| `python .\tools\check_specs.py --strict` | PASS | 116/116 |
| `git diff --check` | PASS | 三个候选文件无空白错误；仅出现工作区 CRLF 提示 |
| `git diff --cached --check` | PASS | index 恰含计划五文件，无空白错误 |

共享工作树曾运行 Debug `ClCompile`；本计划两个目标模块均编译成功，随后被异主普通应用配置 WIP 的 `等待合同登记` 缺失阻断。该失败没有通过修改非白名单文件规避；正式构建证据来自不含异主 WIP 的隔离候选根。

## 3. 专项矩阵

| 编号 | 结果 | 覆盖 |
| --- | --- | --- |
| V00 | PASS | v2 非法请求为空失败；进展/准备 0 项均有版本 2 完整见证 |
| V01 | PASS | 8 项进展、9 项准备规范排序；绑定完整；精确重放零第二权威副作用 |
| V02 | PASS | 进展/准备的无效轮次、坏载荷和错关系端点均引用冲突且空载荷 |
| V02A | PASS | 重复当前成员使完整集合失败，不产生部分成功 |
| V02B | PASS | 登记版本错配结构化为结构未登记 |
| V03 | PASS | v1 成员不投影、不回落到 v2 |
| V04 | PASS | G0 读中漂移和资源失败均为空载荷、空见证、截止 0 |
| P00—P24 | PASS | 既有合同、事件、进展和准备 v1 全回归 |
| Q00—Q06 | PASS | 既有到期事件合同终态完整读回全回归 |

## 4. 静态边界

- `0x0307'3030..0x0307'304F` 只在目标服务模块出现，`303F` 保留未使用。
- 登记幂等身份 `0x494E'5354'5350'5632`、`0x494E'5354'5050'5632` 各只出现一次。
- 新增代码没有生产进展发布入口、有效活动组合器或安全门禁持久字段。
- 当前生产定义版本 1、`L=2,767,011,611,056,432,742`、`H=7,378,697,629,483,820,645` 未修改。

## 5. 结论边界

当前候选已经证明 v2 服务进展/准备可保存并完整读回 task owner 强类型选择、冻结、实例和筹办/执行轮次引用。它没有证明生产发布、真实执行、T→D 当前性、安全门禁、有效服务活动、阶段三或 `INSTINCT-ROUTE` 完成。
