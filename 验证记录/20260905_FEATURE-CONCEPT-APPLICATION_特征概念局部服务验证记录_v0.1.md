# FEATURE-CONCEPT-APPLICATION 局部服务验证记录

日期：2026-09-05。计划/设计 v0.6。验证所有者 `/root/feature_concept_execute`；唯一隔离资源 `D:\TEMP\FEATURE-CONCEPT-APPLICATION\海中鱼巣-01a07099-feature_concept_execute`。

当前状态：局部代码验证完成，待顶层统一发布。最终源码构建与运行详见下列分账；本文件不构成已提交基线的独立集成验收报告。

## 1. 实际链路和验证对象

`verify.cpp` 使用真实 `建立L1事实基座运行包`、正式所有者范围签发、真实属性/关系类型登记、材料文件及 SHA-256、真实不可变材料 provider、C1 概念 owner、特征/值/存在 provider 和应用模块。没有 mock、假成功 provider、应用私有权威映射或强制绕引用闭包退出。

宿主、实例、成员和值材料由合法上游正式 provider 写入。应用操作前后的已知世界特征完整投影、存在成员组和原始值比较，并结合生产调用静态零世界写核对。概念事务会推进全局 G，这不等同于世界 owner 写入。

临时工程编译 21 个真实模块及消费者，x64、v145、C++20、UTF-8；Debug `/Od /MDd`，Release `/O2 /MD`，require 断言不依赖 NDEBUG。生产工程已登记新模块。本轮不是整个 GUI 产品启动或正式集成验收。

## 2. 命令与运行结果

构建器：`C:\Program Files\Microsoft Visual Studio\18\Professional\MSBuild\Current\Bin\MSBuild.exe`。

```powershell
& $msbuild "$temp\verify.vcxproj" /m:1 /p:Configuration=Debug /p:Platform=x64 /v:minimal
& "$temp\Debug\out\verify.exe" $temp
& $msbuild "$temp\verify.vcxproj" /m:1 /p:Configuration=Release /p:Platform=x64 /v:minimal
& "$temp\Release\out\verify.exe" $temp
git diff --check
```

`$temp` 即本记录的精确隔离根；四次运行各自用单独日志并保留实际退出码。首次 26 断言 smoke、后续 68 断言及 `run-debug-131-pass.log` 的 131 断言 Debug 完整轮均实际退出 0。131 轮后补充更多既定矩阵及格式/编码，不能将旧轮结果当最终源码结果。

## 3. 19 项矩阵及真实证据边界

行号对应最终固定的 `verify.cpp`；每个 PASS 标签进入运行日志。某行包含多个字段比较时，依源码完整 require 表达式判断，不能仅凭标签或断言数量升级结论。

| 设计项 | fixture 断言行 | 实际覆盖与限制 |
| --- | --- | --- |
| 1 | 170, 176, 218, 276 | 运行；应用对三世界 provider 的只读调用集合另经根独立静态核对。 |
| 2 | 187, 369, 375, 436 | 运行。 |
| 3 | 202, 377, 379, 442, 444 | 运行；整体向量匹配保留元素顺序及长度。 |
| 4 | 364, 423, 452 | 运行；歧义完整载荷与预算清空。 |
| 5 | 244, 251, 263, 267, 270 | 运行；具体选中时间由证据身份反查，完整成熟规则/证据顺序/截止比较。 |
| 6 | 271, 335, 337, 338, 341, 342, 343, 344, 348 | 运行；成熟后身份不变，生命周期恢复同身份。 |
| 7 | 166, 178, 185, 315, 463, 468 | 运行原 G/原键精确恢复及冲突；没有故障注入资源失败，发布未知路径属于独立静态防御审查。 |
| 8 | 143, 146, 226, 369, 371 | 运行部分合法反例；其它非法端点/来源的入口防御按完整源码条件静态核对，未伪造 L1 事实。 |
| 9 | 282, 467 | 运行正常当前引用阻止世界退出与名称退出独立性；未验证普通概念物理清理流程。 |
| 10 | 320, 322 | 运行；应用对象重新构造后正式概念/名称历史/观测读回比对，非跨进程存储恢复。 |
| 11 | 356, 359, 431, 493 | 运行规则身份与历史异义/版本及同 L1 不同实例拒绝；同数字异库的指针绑定防护静态核对，未额外构造两套世界事实。 |
| 12 | 481, 483 | 运行旧概念 tag 与新 11—14 实际编码读回；其余旧 tag 序列/长度校验及无 tag15/规则整数身份由独立静态审查。材料去重使用未改的真实材料 provider，未声称本轮全面材料服务回归。 |
| 13 | 305, 306, 308, 309, 310 | 运行创建前/退出后/H/G/预算边界；零值组、预算内多值组和生命周期损坏不能由本 fixture 正式世界写入口合法形成，单列静态防御。 |
| 14 | 409, 483 | 运行裸实例新标签完整读回及精确错误值无回退；没有应用/概念越 owner 的 L1 枚举，静态调用核对。 |
| 15 | 282, 294, 303, 312, 313 | 运行合法先释放、后上游世界退出；Grecord/Gobs 历史观测及成熟、旧名称不变。 |
| 16 | 288, 290, 299, 301, 315 | 运行完整/空/部分/预算/原键重放与改清单冲突；并发写制造的读间 G 波动未注入，逐读取尾 G 防御静态核对。 |
| 17 | 398, 401, 406, 408, 469, 470 | 运行引用值真实观测成熟、合法退出材料后的 H 读及值错误不回退；不可达损坏组、资源故障、错误来源/类型事实属于静态防御，不计运行覆盖。 |
| 18 | 416, 418, 420, 425, 428 | 运行跨旧规则新分区、空范围验证与预算拒绝；另由独立 supplement 真实验证冷却/退役观测参与和无观测概念普通不可逆退出后规则拒改义，见补充证据。旧生命周期历史缺失映射保持静态分账。 |
| 19 | 135, 141, 143, 146, 149, 161, 166, 322 | 运行首写防篡改、原 G 两分支、原材料保留/重建恢复；发布未知已由独立审查验证保留两份输入，未故障注入。 |

## 4. 防御性状态单列

真实 typed owner 入口不允许构造值/节点生命周期不配对、预算内多值材料、缺失/损坏类型头、错误 source 历史或截断成熟记录。本轮未绕 owner 注入损坏事实来制造业务样本。对应新增代码中逐项头/G/owner/生命周期/类型/来源/证据检查已独立只读复核，但不能计作实际故障运行覆盖。

L1 资源失败、内部错误、首次键查明前故障及读间并发 G 漂移没有公开故障注入工具，本轮未伪造故障运行。未知发布分支由固定源码静态复核：首次键读取不能完成时不认定零发布，统一结果保留原结构请求及创建原生成请求，已知首次发布 H 继续回显；成功结果按首次 H 重建。合法重放测试模拟调用方不消费首次响应后持原材料再次请求，而非修改 L1 内部。

独立审查在格式化前的应用 blob `392f54cce49f5e420e17d0da900e19c2ff0e3603`、概念 provider blob `e5923e2f9241996ad5885a8ed6dfb6e31b3038d8` 确认创建篡改原反例及首次查询未知发布分类闭合；最终格式化和结果谓词加强后以最终构建/运行及 hash 为准。

## 5. 历史失败归因

- `run-debug-before-lifecycle-fix.log`：旧 helper 把首次 H 之后退出误当首次结果无效。修复后真实多次迁移及原键重放通过。
- `run-debug-before-value-fixture-fix.log`、`probe.log`：fixture 把 feature owner 的类型给 value owner；真实引用冲突且 G 不变。`probe-value-fixed.log` 用 value owner 自有类型创建成功，来源仍为外部真实 host。
- 局部 fixture 增补一次变量名重复导致编译错误，改名后编译通过；没有据此放松生产类型或错误判断。

## 6. 完成声明边界

仅证明本应用模块通过真实 provider 的具名局部服务链，以及明确列出的 Debug/Release 构建与运行。未验证生产采样、自然语言词条命名、自动学习区间、轮廓正侧面识别、跨进程持久化、GUI 主程序装配、其它概念根或所有旧服务全面回归。

临时源码、工程、运行日志和材料文件保留在唯一登记根，供顶层复核和后续已提交基线验收复跑，不自动清除证据。Git 提交/推送由顶层统一执行，结果提交待发布后关联。

## 7. §18 独立补充消费者与修复分账

`supplement/supplement.cpp` 复用固定 `verify.cpp` 的真实 Fixture，另外提供独立 main；没有重新运行完整长 Debug 矩阵，也没有修改其已冻结输入。前置冷却/退役观测两场景在修前已经运行通过。修前 `supplement/run-before-fix.log` 实际普通退出状态 14、G35 前后不变，消费者退出 1。

修复只将初始程度/独立规则记录追加提为单一私有 helper，并让普通退出的首次完整写集验证复用它。独立规则节点、锚关系、材料关系和元数据/分区值保留，概念程度仍退出。`supplement/run-debug.log` 修后实际已提交状态 1、G35→36，44 条断言全过，退出 0，含：

- 冷却及退役的两条独立观测均参与新规则分区，零写；
- 无观测/无当前外部引用的专用概念通过正式冷却、退役后由普通概念 owner 原子退出；
- 退出后原创建请求按首次 H 精确恢复，同一概念身份；
- 退出后同材料/版本改分区返回幂等冲突、清空载荷且 G 不变；
- 不可逆退出原键重放精确成功、G 不变。

修前完整 Debug/Release 输入和日志仍由 `final-inputs.json`、`final-results.json` 绑定；修后最终输入由 `postfix-inputs.json` 绑定，最终日志由 `postfix-results.json` 绑定。修后执行 Debug 补充消费者（其中完整消费者源码亦编译）和 Release 完整矩阵+补充消费者，不把修前全 Debug 运行错误标成修后全 Debug 运行。

## 8. 最终验证结果与固定输入

不可逆退出补充修复前的完整 Debug `/Od /MDd` 和 Release `/O2 /MD` 各执行 162 条断言，构建与运行均退出 0，详见 `final-results.json`。该轮概念 provider blob 为 `c61f08e6a77a660667657f270b5e6e29d534f4c8`。原日志与输入清单保留。

修后最终源码的 Debug 补充工程编译了同一完整消费者源码、全部真实模块，执行 44 条补充断言；Release 完整矩阵执行 162 条、补充执行 44 条。以下全部退出码 0，外层串行脚本退出 0。未重跑长 Debug 全矩阵，依据顶层对该局部补充修复明确允许的验证分账处理。

| 最终任务 | 阶段 | 退出码 | 精确日志文件（均位于隔离根） | UTC 起止 |
| --- | --- | --- | --- | --- |
| debug-supplement | build | 0 | `postfix-build-debug-supplement.log` | 2026-09-05T11:58:36.1783153Z → 2026-09-05T11:58:50.2349236Z |
| debug-supplement | run | 0 | `postfix-run-debug-supplement.log` | 2026-09-05T11:58:50.2467390Z → 2026-09-05T11:59:00.5956104Z |
| release-full | build | 0 | `postfix-build-release-full.log` | 2026-09-05T11:59:00.5983337Z → 2026-09-05T11:59:16.8034689Z |
| release-full | run | 0 | `postfix-run-release-full.log` | 2026-09-05T11:59:16.8051198Z → 2026-09-05T11:59:35.8993523Z |
| release-supplement | build | 0 | `postfix-build-release-supplement.log` | 2026-09-05T11:59:35.9009266Z → 2026-09-05T12:00:28.4534725Z |
| release-supplement | run | 0 | `postfix-run-release-supplement.log` | 2026-09-05T12:00:28.4544914Z → 2026-09-05T12:00:28.8087269Z |

实际末行分别为 `CHECKS 44`、`CHECKS 162`、`CHECKS 44`。逐断言源码与日志作为主要证据，数量只用于定位完整运行结束。补充源码对应的关键断言包括冷却/退役旧观测、普通退出发布 G35→36、首次创建 H 重放、原规则异义拒绝及退出重放。

构建后重新计算所有 `postfix-inputs.json` 输入 SHA-256，与冻结前全部相等。既有生产文件 BOM 状态与 HEAD 相同；项目 XML 中模块与 filter 各恰有一项；`git diff --check` 实际退出 0。

| 最终输入 | Git blob |
| --- | --- |
| `海中鱼巣/领域/L2普通概念结构.数据.h` | `d73e0bdd11e7b4b944e17cfc458453a2562613a3` |
| `海中鱼巣/领域/服务.L2概念结构.ixx` | `7c56d689bb4da113f5d0e65b124c7183a8ccd5d5` |
| `海中鱼巣/领域/数据服务.特征类.ixx` | `c13c15b5389674c8ca05f490da83565e5390412c` |
| `海中鱼巣/领域/数据服务.存在类.ixx` | `ab5bcd0fa05c0f177e22508da47d29479f30221f` |
| `海中鱼巣/领域/数据服务.特征值类.ixx` | `634e62f75dcc47c4ab06b526d94c6648cf2c26ee` |
| `海中鱼巣/业务/应用服务.特征概念类.ixx` | `4412ae461029667fb7fb252cb4ac82959e577237` |
| `海中鱼巣.vcxproj` | `d85de25ffa48cf3f95c5fd32aa4b8a4265704654` |
| `海中鱼巣.vcxproj.filters` | `804b674bb4e07a1efad1296ccd893ab7c2343c02` |
| `verify.cpp` | `f42ef69e91b2fc2ba1f0b5d0ba322e667ed52d52` |
| `verify.vcxproj` | `98735e8d0c50d931826ff9fa8216c12220fdb532` |
| `supplement/supplement.cpp` | `d054c48a851f2044b5e5ee60712b6c8c0eea1588` |
| `supplement/supplement.vcxproj` | `cfaaab6f726f926e8d4d11b4a16366f4a6a540a5` |

完整 SHA-256 及绝对路径保存在 `postfix-inputs.json`；最终生产与两记录交付 hash 另外保存在 `delivery-files.json`。该外部清单避免记录文件自引用 hash。待发布提交由顶层处理，本执行所有者未执行 Git 暂存、提交或推送。
