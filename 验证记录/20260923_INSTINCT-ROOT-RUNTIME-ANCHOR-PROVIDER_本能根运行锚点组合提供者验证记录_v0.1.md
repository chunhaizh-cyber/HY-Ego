# 本能根运行锚点组合提供者验证记录 v0.1

日期：2026-09-23

计划身份：`INSTINCT-ROOT-RUNTIME-ANCHOR-PROVIDER`

验证代码起点：`0e7fd9c4ee521968c71f4bd7035a3737129aacda`

隔离根：`D:\TEMP\海中鱼巣\INSTINCT-ROOT-RUNTIME-ANCHOR-PROVIDER\20260923-155230-631`

## 1. 验证输入边界

从正式HEAD导出完整源码到隔离根，只覆盖本计划6个代码/专项WIP文件；未复制共享工作区中RC-P1、根工程、普通应用、领域类或流程图的未提交修改。因此以下结果绑定正式HEAD上游加本计划允许文件，不绑定其它活动WIP。

构建使用：

```text
C:\Program Files\Microsoft Visual Studio\18\Professional\MSBuild\Current\Bin\MSBuild.exe
Configuration=Debug/Release
Platform=x64
/t:Rebuild /m:1 /nologo /v:minimal
OutDir/IntDir均位于本轮隔离根
```

## 2. 构建结果

| 配置 | 结果 |
| --- | --- |
| Debug x64隔离Rebuild | 通过，零编译/链接错误 |
| Release x64隔离Rebuild | 通过，零编译/链接错误 |

初次Debug构建曾因专项工程未编译既有`线程_自我.cpp`而出现`自我线程本能根启动材料_v1::完整()`未解析；恢复既有源文件编译项后重新执行两配置完整Rebuild并通过。最终结果不依赖共享根工程。

## 3. 运行结果

### 3.1 真实服务栈与公开入口

Debug与Release各运行一次无参数专项，均为`PASS total=46`。真实服务栈覆盖：

- 首次双根形成与完整self值式映射；
- 同一provider第二次调用`入口拒绝`且零写；
- 以首次锚点截止重新正式读回自我并新建provider，返回`已恢复`、锚点相同且零写；
- 坏版本、零self、错误self和伪造自我读回均被公开入口拒绝且零写。

### 3.2 内部组合脚本

Debug与Release均覆盖：

- 首次形成、全部恢复、9次代次守卫和self字段映射；
- 安全F、安全根、服务F三种部分前缀失败后，以新鲜投影执行完整恢复序列；
- 实际F/根材料的已可能发布、根幂等冲突、根材料退出、引用冲突、材料未闭合、资源失败和嵌套载荷不一致；
- 冻结`Gfinal`后五个具名守卫点分别返回当前性漂移且无锚点；
- 坏版本与错误self值式拒绝。

脚本只证明同一组合内核对正式公开结果类型的状态映射、停止推进、原请求收敛和空锚点；它不冒充真实L1故障注入或上游provider自身专项。

### 3.3 两进程持久恢复

每个配置分别用两个独立进程依次执行：

| 配置 | seed | recover |
| --- | --- | --- |
| Debug | `PASS total=2` | `PASS total=3` |
| Release | `PASS total=2` | `PASS total=3` |

seed证明首次形成和self值式映射；recover证明重启后返回`已恢复`、self映射完整，且恢复整栈当前事实代次等于入口事实代次，即零新增事实。

## 4. 静态负面检查

- 四个生产文件不存在`.ixx`、`import/export module`、L1调用、线程创建、普通应用或启动引用。
- 专项不存在`读取所有者范围空域`、`建立或读取方法登记根`、`UINT64_MAX`或`uint64_t`最大值预算。
- 阶段19截止仅经`方法登记根生产初始化提供者::初始化({1})`取得，消费唯一生产默认空域预算。
- 内部端口头只被provider `.cpp`和本专项包含；公开头不暴露内部端口。
- 两个上游provider源码零修改；根工程、普通应用、启动和线程生产文件零修改。

## 5. 最终差异检查

发布前对本计划8个允许文件执行`git diff --check`和`git diff --cached --check`，两项均通过；暂存清单精确为本计划8个允许文件。未执行根工程构建、普通应用运行或正式启动链验证。

## 6. 结论边界

验证支持的结论仅为：阶段21组合provider可在正式HEAD上游的隔离真实服务栈中形成/恢复双根锚点，执行同`Gfinal`正式重读，并无损映射现行self消费字段。

不支持声明：普通应用已接线、程序已越过阶段21、自我线程已生产创建或运行、治理门已开放、正式跨进程故障恢复已经集成验收，或自我内部治理闭环已经完成。
