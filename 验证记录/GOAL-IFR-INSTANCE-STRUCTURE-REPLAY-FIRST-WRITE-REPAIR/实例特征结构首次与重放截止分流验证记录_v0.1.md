# 实例特征结构首次与重放截止分流验证记录

- 新持久仓`data-v3`双根seed通过：`seed pass G=10 T=27 D=28 L=29 serviceT=34`，退出0；证明实例结构首次写入后的H读回不再误判发布未知。
- 第二进程只验证实例结构恢复：`recover-instance pass G=11 version=2 firstH=7`，退出0；证明重放仍以当前截止交付、保留首次H。
- 仓外探针以当前头文件和`数据服务.特征类.cpp`重新编译、链接通过；所有材料位于`D:\TEMP\海中鱼巣\ARCH-L4-INSTINCT-ROOT-DEMAND-PROVIDER\20260920-s0\root-provider-persistence-probe\`。

未覆盖：完整根 provider 恢复、资源失败、发布未知、启动与集成；同一探针的安全根随后返回事实代次漂移，属于上位根材料计划范围。
