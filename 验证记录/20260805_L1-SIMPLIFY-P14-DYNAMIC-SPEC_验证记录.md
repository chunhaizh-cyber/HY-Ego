# L1-SIMPLIFY-P14-DYNAMIC-SPEC v0.3 验证记录

## 1. 验证对象

- plan blob：`13d936c815f57becc612f55339bc12065a9e3592`
- 验证基线：`7274580f779a01b28e6b5f393f38008b4d1f1f74` 加 P14 八文件工作区差异
- 目标：关系20状态迁移动能登记、纯值规格及十五键独立读取源码候选

## 2. 构建、运行与规范门禁

| 门禁 | 实际结果 |
| --- | --- |
| Debug x64 隔离 `Rebuild` | PASS，输出仅在系统临时目录 |
| Debug 同一生产程序 `--self-test-exit` | PASS，退出码0 |
| Release x64 隔离 `Rebuild` | PASS，输出仅在系统临时目录 |
| Release 同一生产程序 `--self-test-exit` | PASS，退出码0 |
| Debug MSVC 模块扫描 | PASS，sources/modules=200/200，failed=0，拓扑200，循环0，外部需求0 |
| Release MSVC 模块扫描 | PASS，sources/modules=200/200，failed=0，拓扑200，循环0，外部需求0 |
| `python .\tools\check_specs.py --strict` | PASS，110/110 |
| P14 tracked/untracked 目标 `git diff --check` | PASS |

构建中间目录、可执行文件、日志和模块扫描候选 JSON 均位于系统临时目录；仓库 `.codex-build` 未被本切片读取、清理、覆盖或纳入结果。

## 3. 专项与静态矩阵

同一生产程序的 P14 专项顺序390唯一，完整自检登记/执行总数均为32。P14 的28项矩阵随 Debug/Release 完整自检通过，覆盖：

1. G7 测试向量、入口变体3、`0x15000F01U`、域 `0x0F` 和 L1 v2 provider 前探测；
2. 十节点登记首次成功、当前登记读回、同义首次映射、异义冲突和非法入口；
3. P13 增加/减少形成 `0x44` 规格，等价与无前状态不形成规格，非成功零载荷；
4. 一节点、八值、六关系、八属性槽、零退出，关系20角色1—5/9及角色5单向 P13 本地键引用；
5. 十五个已发布稳定编码的节点/值/关系/属性槽/生命周期读取，比较合同与输入引用中的写前截止独立互证，G0/G1 当前读取代次互证；
6. 坏十五键请求入口拒绝，读回失败零部分事实；规格形成和读取均不调用 P12/P9/P11 provider，不提交、不缓存、不读 SQL 或恢复材料。

静态复核还确认 plan blob 精确一致、顺序390命中1处、P14 临时诊断命中0、启动只新增一个专项且完整自检计数31→32。P78、P79、P0-FIX、路线、旧 WORLD-TREE 和其它所有者 dirty 未进入 P14 八文件范围，Git index 保持为空。

## 4. 最大证明边界

本验证只证明 P14 源码候选、顺序390矩阵、双配置本地构建/同一生产程序自检、规范检查、模块依赖和静态范围门禁通过。日志只用于观察程序退出与摘要，不作为机器权威事实；不证明生产动态发布、最终原子提交/撤销、生产接线、恢复、动作/因果、具名集成验收、STEP-3F 或 SELF-GOV 闭环。
