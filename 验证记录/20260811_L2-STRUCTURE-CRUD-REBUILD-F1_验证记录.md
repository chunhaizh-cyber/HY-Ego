# L2 特征定义增查改退与历史读取验证记录

日期：2026-08-11

计划：`L2-STRUCTURE-CRUD-REBUILD-F1 v0.1`

计划 blob：`ff4fafec590f74536c3f64a6a642fea9fb9613a3`

验证基线：`2543a7a1bd8a91aadb1de1669b43d1a1c6e7c8c9` 加本计划九文件未提交 WIP

## 1. 正式命令与结果

```powershell
MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /verbosity:minimal
MSBuild.exe .\海中鱼巣.vcxproj /m:1 /t:Rebuild /p:Configuration=Release /p:Platform=x64 /verbosity:minimal

powershell -ExecutionPolicy Bypass -File .\验证工具\运行L2特征定义参数验证.ps1 `
  -配置 Debug -幂等基值 21000 -初始I64 11 -换代I64 22
powershell -ExecutionPolicy Bypass -File .\验证工具\运行L2特征定义参数验证.ps1 `
  -配置 Release -幂等基值 25000 -初始I64 33 -换代I64 44

python .\tools\check_specs.py --strict
```

结果：主工程 Debug / Release x64 均完成模块扫描、编译和链接；专项 Debug / Release 的 1—16 项均输出 PASS；strict 输出 `海中鱼巣正式规范目录一致性检查: 111 份目录项全部通过`。

两次专项构建均出现 MSB8029：MSBuild 不建议把中间 / 输出目录放在系统临时目录。该提示来自计划要求的工作区外隔离路径；实际 Rebuild、链接和程序运行成功，仓库内没有生成专项构建物。

## 2. 生产外 16 项矩阵

| 编号 | 验证内容 | Debug | Release |
| --- | --- | --- | --- |
| 1 | 经公开 L1 建立三属性类型、来源、独立材料和外部关系夹具 | PASS | PASS |
| 2 | 新增定义形成唯一普通节点、创建代次和空属性首次载荷 | PASS | PASS |
| 3 | 创建精确重复首次空载荷，同键异义零变化冲突 | PASS | PASS |
| 4 | 初始 I64 同代次建值设槽、首次载荷重复和同键异义 | PASS | PASS |
| 5 | 已设置再初始、未设置换代和错旧值分别结构化拒绝 | PASS | PASS |
| 6 | I64、U64 组、独立材料三属性完整投影和稳定排序 | PASS | PASS |
| 7 | I64 换代、新旧值生命周期及换代前 / 截止历史 | PASS | PASS |
| 8 | 创建前、空属性、逐次初始 / 换代历史独立重建 | PASS | PASS |
| 9 | 显式退出闭包漏项 / 多项冲突，乱序 / 重复入口拒绝 | PASS | PASS |
| 10 | 当前普通关系由 L1 引用闭包保护，F1 不扫描消费者 | PASS | PASS |
| 11 | 闭合外部关系后定义与全部当前属性同代次退出并历史互证 | PASS | PASS |
| 12 | 后继代次退出精确重复首次载荷，同键改闭包幂等冲突 | PASS | PASS |
| 13 | 退出前历史完整，退出截止历史与当前已退出空载荷 | PASS | PASS |
| 14 | 合同、零字段、空材料和非法历史截止入口拒绝且零变化 | PASS | PASS |
| 15 | 创建、属性、退出的新幂等旧期望漂移，原请求仍收敛 | PASS | PASS |
| 16 | 确定漂移及无 sleep 有界并发只产生合法完整或空载荷 | PASS | PASS |

## 3. 第 16 项公开状态计数

| 配置 | 已读取 | 事实代次漂移 | 许可拒绝 | 非法状态 / 部分载荷 / 写者失败 |
| --- | ---: | ---: | ---: | ---: |
| Debug | 0 | 14 | 18 | 0 |
| Release | 0 | 4 | 28 | 0 |

计数属于允许集合内的实际调度结果，不是固定期望值。两配置均未观察到并发 `已读取`，也没有独立证据证明写线程推进恰好落在当前两次投影之间或历史前后夹取之间；因此内部窗口明确记录为未证明。

Release 归因过程先观察到历史合法 `已读取` 被验证侧错误截止条件拒绝，修正后又观察到旧式 L1 代次探测把许可竞争压成 L2 `资源失败`。最终生产实现以目标定义节点的一致投影保留许可拒绝，验证以各自具名截止判断当前 / 历史成功。空一致投影的机械尝试违反 L1 非空查询合同并造成一次访问异常，已经撤销，不计入正式 PASS 证据；最终双配置重跑未复现该异常。

## 4. 静态、工程与范围门禁

- 九文件均通过严格 UTF-8 解码；主工程与专项工程 XML 可解析。
- 主工程为 27 个生产 `.ixx`、13 个 `ClInclude`、四配置 4 项 `/utf-8`；专项工程为七个模块、一个验证 `.cpp`、两配置 2 项 `/utf-8`。
- 新模块名、`export module`、工程 `ClCompile`、filters 和 import 一致；专项三个文件在主工程与 filters 中均为零登记。
- 数据头十个 DTO 各唯一；服务类五个公开函数声明各唯一；两个本地键字面量各唯一。
- 生产模块只 import L1 事实基座并导出 L2 公共合同 / 新数据头，零存在服务 import；未增加 F2、第二仓、幂等账、缓存、SQL、日志、sleep、yield 或测试钩子。
- 生产代码零验证专用 DTO、函数、友元、状态出口、计数器、故障注入、让行、测试分支和 PASS 聚合；本轮函数名、变量名使用中文，正式 `L1`、`L2`、`I64`、`U64` 只作为协议 / 类型标识保留。
- 最终 `main == origin/main == 2543a7a1bd8a91aadb1de1669b43d1a1c6e7c8c9`，F1 plan blob 仍为登记值，Git index 为空；九路径外的异主 dirty 未纳入本切片。

## 5. 未验证范围与清理交接

未运行 Win32、真实分配失败、私有索引破坏、F2 实例语义、长时公平竞争、故障注入、崩溃 / 恢复、跨进程、生产入口装配、真实业务消费者或独立集成验收。没有证明实际命中当前两次投影之间或历史前后夹取之间的内部窗口。

结论：F1 特征定义创建、固定属性初始 / 换代、显式属性闭包退出、L1 外部引用保护、完整当前 / 历史读取和首次结果幂等重建，在 Debug / Release x64 主工程重建及具名 16 项生产外矩阵下通过。Git 提交和推送事实仍待顶层唯一发布者完成。

结果提交发布且计划索引登记 F1 退出后，顶层应另行形成独立清理提交，只删除以下临时文件：

- `验证工具/L2特征定义参数验证.cpp`
- `验证工具/L2特征定义参数验证.vcxproj`
- `验证工具/运行L2特征定义参数验证.ps1`

两份永久记录、计划、结果提交和 Git 历史继续保留；临时清理不改变 F1 结果身份，也不证明长期回归。
