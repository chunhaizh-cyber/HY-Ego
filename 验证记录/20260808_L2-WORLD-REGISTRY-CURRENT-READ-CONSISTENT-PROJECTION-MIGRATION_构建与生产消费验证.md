# 世界登记当前读回一致投影构建与生产消费验证

日期：2026-08-08

计划：`L2-WORLD-REGISTRY-CURRENT-READ-CONSISTENT-PROJECTION-MIGRATION v0.1`

plan blob：`eabd44f3754337c38c1ea6b3b181280b16461ca1`

设计提交：`801734293eaae4b074f7c39f72712c5c1563bba2`

施工起点：`f96f27397cfcfd781bc886081e7e6d9ec00fd484`

## 1. 构建、模块与生产入口

| 门禁 | 结果 |
| --- | --- |
| Debug x64 串行完整 Rebuild | 通过，退出 0；0 编译错误、1 条 `MSB8029` 临时输出目录提示 |
| Release x64 串行完整 Rebuild | 通过，退出 0；0 编译错误、1 条 `MSB8029` 临时输出目录提示 |
| Debug MSVC 模块扫描 | 通过，sources / modules=`136 / 136`、failed=`0`、拓扑 `136`、循环 `0`、外部需求 `0` |
| Release MSVC 模块扫描 | 通过，sources / modules=`136 / 136`、failed=`0`、拓扑 `136`、循环 `0`、外部需求 `0` |
| Debug 正式 `--runtime-context` | 运行门禁通过，退出 0 |
| Release 正式 `--runtime-context` | 运行门禁通过，退出 0 |
| `python .\tools\check_specs.py --strict` | 通过，110 / 110 |
| 三个白名单路径 `git diff --check` | 通过，无 whitespace 错误；仅 Git 行尾策略提示 |

构建使用 Visual Studio 18 Professional，Debug / Release、x64、完整 `Rebuild`、`/m:1`；临时验证根为 `C:\Users\zhchh\AppData\Local\Temp\hzy-world-registry-consistent-269108f6fc624b45996964f8a99f15cb`。模块扫描没有写正式图谱输出。

## 2. 精确静态门禁

| 检查 | 结果 |
| --- | --- |
| 目标根一致投影调用 | `尝试读取中性一致当前投影` 恰 1 个物理调用点 |
| 最大尝试边界 | `最大尝试次数 = 2` 恰 1；只有第一次合法事实代次漂移执行一次 `continue` |
| 旧读取与快照 | `读取中性当前事实代次`、`读取中性当前节点`、`读取中性当前属性`、`读取中性当前值`、`读取完整快照`、`尝试读取完整快照` 在目标根内均为 0 |
| 请求形状 | 五节点按登记顺序形成；关系 / 值 / 源关系组 / 目标关系组保持空；属性值恰为世界根 + 场景标记类型，不把当前值编码加入值组 |
| 漂移合法性 | 合同、期望回显、非零且不同的实际代次和六空组全部成立才允许第一次完整重试；第二次漂移返回事实代次漂移 |
| 许可与资源 | 许可拒绝零重试并独立映射；资源失败保持分账；坏失败形状返回内部不一致 |
| 成功顶层形状 | 合同、期望回显、读取代次及六组长度 `5/0/0/1/0/0` 精确核对 |
| 五节点业务形状 | 同序编码、成功项目、事实编码、种类 / I64 表示、共同创建代次、未退出及属性数量均核对 |
| 根槽和值事实 | 根唯一槽、投影槽、值编码、所属 / 类型 / 来源、创建 / 退出及 I64(1) 均核对 |
| 缓存所有权 | 成功只更新返回登记副本的 `已验证事实代次`；目标根对输入和成员 `登记_` 修改为 0；全部非成功空登记 |
| 公开合同 | 世界登记数据头零差异；公开 DTO、十个枚举值、公开函数签名零变化 |
| 写入路径 | `建立空世界` 的固定中性写集、提交、映射和成员缓存发布未修改 |
| 白名单外生产文件 | 差异为 0；启动、运行期上下文、工程、filters、自检和核心 L1 均未修改 |

## 3. 正式生产消费与未独立读回

Debug / Release `--runtime-context` 运行均经现有正式链路触达 `运行期上下文::完整 -> 世界登记服务::读取当前登记 -> 按登记读取权威事实`，且进程退出 0。该证据只说明正式生产入口没有拒绝该次启动；未读取或解析日志来补写业务结论。

公开运行期上下文租约连续两次结构化 `读取世界登记` 未在本段独立执行。因而本记录不独立证明两次均取得 `已读取 + 完整登记`、逐字段相等或与有效上下文一致，也不宣称世界登记消费者服务验收通过。

计划冻结的缓存截止相等、第一次合法漂移后成功、两次漂移、许可竞争和坏业务形状分支由源码结构化分账；本段未用私有探针、专项驱动或人为竞争逐项触发这些分支。

## 4. 范围与声明边界

实际结果范围只含世界登记服务文件和本施工 / 验证记录。P0-FIX、路线、异主 `.codex-build`、两份 WORLD-TREE 及其它 WIP 未纳入修改、验证输出或结论。

允许声明：世界登记当前权威读回候选已按冻结合同迁移到一次中性非阻塞一致当前投影，并通过双配置构建、模块扫描、strict、静态门禁和正式生产入口运行门禁。

禁止声明：公开租约独立结构化读回已通过；真实许可竞争、消费者集成验收、其它消费者迁移、关系组 / 历史 / 修改 / 退出、历史正文闭合、L1 自证或第一层完善已经得到证明。
