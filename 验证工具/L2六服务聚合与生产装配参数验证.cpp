#include <iostream>
#include <memory>
#include <type_traits>

import 海中鱼巣.领域.服务.L2结构聚合;
import 海中鱼巣.装配.普通应用;

namespace {

template<class 服务类型>
bool 同址(const 服务类型& 左, const 服务类型& 右) noexcept {
    return std::addressof(左) == std::addressof(右);
}

template<class 登记结果类型>
bool 登记结果头逐字段相同(
    const 登记结果类型& 左, const 登记结果类型& 右) noexcept {
    return 左.状态 == 右.状态
        && 左.事实截止代次 == 右.事实截止代次
        && 左.登记.has_value() == 右.登记.has_value()
        && (!左.登记 || *左.登记 == *右.登记);
}

template<class 结果头类型>
bool 结果头逐字段相同(const 结果头类型& 左,
    const 结果头类型& 右) noexcept {
    return 左.合同版本 == 右.合同版本 && 左.状态 == 右.状态
        && 左.事实截止代次 == 右.事实截止代次
        && 左.变更事实代次.has_value() == 右.变更事实代次.has_value()
        && 左.变更事实代次.value_or(0) == 右.变更事实代次.value_or(0);
}

}

int main() {
    static_assert(!std::is_default_constructible_v<海中鱼巣::L2结构聚合服务>);
    static_assert(!std::is_copy_constructible_v<海中鱼巣::L2结构聚合服务>);
    static_assert(!std::is_copy_assignable_v<海中鱼巣::L2结构聚合服务>);
    static_assert(!std::is_move_constructible_v<海中鱼巣::L2结构聚合服务>);
    static_assert(!std::is_move_assignable_v<海中鱼巣::L2结构聚合服务>);

    int 失败数 = 0;
    const auto 检查 = [&](bool 条件, const char* 名称) {
        std::cout << (条件 ? "[PASS] " : "[FAIL] ") << 名称 << '\n';
        if (!条件) ++失败数;
    };

    auto 装配结果 = 海中鱼巣::构造普通应用上下文({});
    检查(装配结果.成功()
            && 装配结果.状态 == 海中鱼巣::普通应用装配状态::已装配,
        "普通应用唯一生产装配成功");
    if (!装配结果.上下文) return 1;

    auto& 上下文 = *装配结果.上下文;
    auto& 聚合 = 上下文.取得L2结构聚合服务();
    检查(同址(聚合.取得L2场景结构服务(), 上下文.取得L2场景结构服务())
            && 同址(聚合.取得L2存在结构服务(), 上下文.取得L2存在结构服务())
            && 同址(聚合.取得L2特征结构服务(), 上下文.取得L2特征结构服务())
            && 同址(聚合.取得L2状态结构服务(), 上下文.取得L2状态结构服务())
            && 同址(聚合.取得L2动态结构服务(), 上下文.取得L2动态结构服务())
            && 同址(聚合.取得L2因果结构服务(), 上下文.取得L2因果结构服务()),
        "六个非 const getter 与上下文直接 getter 逐一同址");

    检查(同址(聚合.取得L2场景结构服务(), 聚合.取得L2场景结构服务())
            && 同址(聚合.取得L2存在结构服务(), 聚合.取得L2存在结构服务())
            && 同址(聚合.取得L2特征结构服务(), 聚合.取得L2特征结构服务())
            && 同址(聚合.取得L2状态结构服务(), 聚合.取得L2状态结构服务())
            && 同址(聚合.取得L2动态结构服务(), 聚合.取得L2动态结构服务())
            && 同址(聚合.取得L2因果结构服务(), 聚合.取得L2因果结构服务()),
        "六个非 const getter 重复取得同址");

    const auto& 常量上下文 = 上下文;
    const auto& 常量聚合 = 常量上下文.取得L2结构聚合服务();
    检查(同址(常量聚合.取得L2场景结构服务(), 常量上下文.取得L2场景结构服务())
            && 同址(常量聚合.取得L2存在结构服务(), 常量上下文.取得L2存在结构服务())
            && 同址(常量聚合.取得L2特征结构服务(), 常量上下文.取得L2特征结构服务())
            && 同址(常量聚合.取得L2状态结构服务(), 常量上下文.取得L2状态结构服务())
            && 同址(常量聚合.取得L2动态结构服务(), 常量上下文.取得L2动态结构服务())
            && 同址(常量聚合.取得L2因果结构服务(), 常量上下文.取得L2因果结构服务()),
        "六个 const getter 与上下文直接 getter 逐一同址");
    检查(同址(常量聚合.取得L2场景结构服务(), 聚合.取得L2场景结构服务())
            && 同址(常量聚合.取得L2存在结构服务(), 聚合.取得L2存在结构服务())
            && 同址(常量聚合.取得L2特征结构服务(), 聚合.取得L2特征结构服务())
            && 同址(常量聚合.取得L2状态结构服务(), 聚合.取得L2状态结构服务())
            && 同址(常量聚合.取得L2动态结构服务(), 聚合.取得L2动态结构服务())
            && 同址(常量聚合.取得L2因果结构服务(), 聚合.取得L2因果结构服务()),
        "const 与非 const 观察指向同一原实例");

    const auto 聚合场景读取 = 常量聚合.取得L2场景结构服务().读取当前场景关系类型登记();
    const auto 直接场景读取 = 常量上下文.取得L2场景结构服务().读取当前场景关系类型登记();
    const auto 聚合存在读取 = 常量聚合.取得L2存在结构服务().读取当前事实代次();
    const auto 直接存在读取 = 常量上下文.取得L2存在结构服务().读取当前事实代次();
    const auto 聚合特征读取 = 常量聚合.取得L2特征结构服务().读取当前特征关系类型登记();
    const auto 直接特征读取 = 常量上下文.取得L2特征结构服务().读取当前特征关系类型登记();
    const auto 聚合状态读取 = 常量聚合.取得L2状态结构服务().读取当前状态结构类型登记();
    const auto 直接状态读取 = 常量上下文.取得L2状态结构服务().读取当前状态结构类型登记();
    const auto 聚合动态读取 = 常量聚合.取得L2动态结构服务().读取当前动态结构类型登记();
    const auto 直接动态读取 = 常量上下文.取得L2动态结构服务().读取当前动态结构类型登记();
    const auto 聚合因果读取 = 常量聚合.取得L2因果结构服务().读取当前因果结构类型登记();
    const auto 直接因果读取 = 常量上下文.取得L2因果结构服务().读取当前因果结构类型登记();
    检查(登记结果头逐字段相同(聚合场景读取, 直接场景读取),
        "场景合法只读结果逐字段相同");
    检查(结果头逐字段相同(聚合存在读取, 直接存在读取),
        "存在合法只读结果逐字段相同");
    检查(登记结果头逐字段相同(聚合特征读取, 直接特征读取),
        "特征合法只读结果逐字段相同");
    检查(登记结果头逐字段相同(聚合状态读取, 直接状态读取),
        "状态合法只读结果逐字段相同");
    检查(登记结果头逐字段相同(聚合动态读取, 直接动态读取),
        "动态合法只读结果逐字段相同");
    检查(登记结果头逐字段相同(聚合因果读取, 直接因果读取),
        "因果合法只读结果逐字段相同");

    std::cout << "[NOT_RUN] 聚合分配失败未做生产故障注入\n";
    std::cout << "验证结论：" << (失败数 == 0 ? "PASS" : "FAIL")
        << "，失败数=" << 失败数 << '\n';
    return 失败数 == 0 ? 0 : 1;
}
