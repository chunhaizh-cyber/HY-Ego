#include <atomic>
#include <cstdint>
#include <iostream>
#include <memory>
#include <optional>
#include <thread>

import 海中鱼巣.核心.服务.L1事实基座;
import 海中鱼巣.领域.服务.L2存在结构;
import 海中鱼巣.领域.服务.L2场景结构;

namespace {

std::optional<std::uint64_t> 读取当前代次(
    const 海中鱼巣::L1事实基座服务& 服务) {
    const auto 结果 = 服务.读取中性当前事实代次(
        {海中鱼巣::L1中性CRUD合同版本});
    return 结果.状态 == 海中鱼巣::L1中性读取状态::成功
        && 结果.事实代次 != 0
        ? std::optional<std::uint64_t>{结果.事实代次} : std::nullopt;
}

} // namespace

int wmain() {
    int 失败数 = 0;
    int 用例数 = 0;
    const auto 检查 = [&](bool 条件, const char* 名称) {
        ++用例数;
        std::cout << (条件 ? "[PASS] " : "[FAIL] ") << 名称 << '\n';
        if (!条件) ++失败数;
    };

    auto 运行包 = 海中鱼巣::建立L1事实基座运行包();
    auto 存在原始交付 = 运行包.所有者范围签发器().建立所有者范围(
        {海中鱼巣::L1所有者范围CRUD合同版本,
            海中鱼巣::存在所有者建立身份,
            海中鱼巣::L1所有者范围种类::独占结构范围});
    auto 存在交付 = 海中鱼巣::尝试形成L2存在所有者交付(
        运行包.读取服务(), std::move(存在原始交付));
    if (!存在交付) return 1;
    auto 存在服务 = std::make_unique<海中鱼巣::L2存在结构服务>(
        运行包.读取服务(), std::move(*存在交付));
    auto 场景原始交付 = 运行包.所有者范围签发器().建立所有者范围(
        {海中鱼巣::L1所有者范围CRUD合同版本,
            海中鱼巣::场景所有者建立身份,
            海中鱼巣::L1所有者范围种类::独占结构范围});
    auto 场景交付 = 海中鱼巣::尝试形成L2场景所有者交付(
        运行包.读取服务(), std::move(场景原始交付));
    if (!场景交付) return 1;
    auto 场景服务 = std::make_unique<海中鱼巣::L2场景结构服务>(
        运行包.读取服务(), *存在服务, std::move(*场景交付));

    auto 代次 = 读取当前代次(运行包.读取服务());
    const auto 登记 = 场景服务->读取当前场景宿主结构登记();
    检查(代次 && 登记.成功() && 登记.登记
            && 海中鱼巣::L2场景宿主结构登记完整(*登记.登记)
            && 登记.登记->事实截止代次 == *代次,
        "构造期宿主登记与owner-aware读回");
    if (!代次 || !登记.登记) return 1;
    const auto 登记重复 = 场景服务->建立场景宿主结构登记({
        {海中鱼巣::L2结构合同版本, 登记.登记->首次期望事实代次},
        登记.登记->规则版本, 登记.登记->首次幂等身份});
    检查(登记重复.成功()
            && 登记重复.结果头.状态 == 海中鱼巣::L2结构状态::精确重复,
        "固定宿主登记精确重复");

    const auto 根 = 场景服务->建立场景树根(
        {{海中鱼巣::L2结构合同版本, *代次}, {2001}});
    if (!根.成功() || !根.树 || !根.场景节点) return 1;
    代次 = 读取当前代次(运行包.读取服务());
    const auto 场景甲 = 场景服务->新增树内场景(
        {{海中鱼巣::L2结构合同版本, *代次}, {2002}, *根.树,
            根.场景节点->场景.身份, 1});
    代次 = 读取当前代次(运行包.读取服务());
    const auto 场景乙 = 场景服务->新增树内场景(
        {{海中鱼巣::L2结构合同版本, *代次}, {2003}, *根.树,
            根.场景节点->场景.身份, 2});
    代次 = 读取当前代次(运行包.读取服务());
    const auto 场景丙 = 场景服务->新增树内场景(
        {{海中鱼巣::L2结构合同版本, *代次}, {2004}, *根.树,
            根.场景节点->场景.身份, 3});
    检查(场景甲.成功() && 场景乙.成功() && 场景丙.成功(),
        "建立三个正式树内场景");
    if (!场景甲.场景节点 || !场景乙.场景节点 || !场景丙.场景节点)
        return 1;

    代次 = 读取当前代次(运行包.读取服务());
    const auto 存在甲 = 存在服务->新增存在节点(
        {{海中鱼巣::L2结构合同版本, *代次}, {2101}});
    代次 = 读取当前代次(运行包.读取服务());
    const auto 存在乙 = 存在服务->新增存在节点(
        {{海中鱼巣::L2结构合同版本, *代次}, {2102}});
    代次 = 读取当前代次(运行包.读取服务());
    const auto 存在丙 = 存在服务->新增存在节点(
        {{海中鱼巣::L2结构合同版本, *代次}, {2103}});
    检查(存在甲.存在 && 存在乙.存在 && 存在丙.存在,
        "建立三个正式存在");
    if (!存在甲.存在 || !存在乙.存在 || !存在丙.存在) return 1;

    代次 = 读取当前代次(运行包.读取服务());
    const auto 成员 = 场景服务->新增场景成员关系(
        {{海中鱼巣::L2结构合同版本, *代次}, {2201},
            场景甲.场景节点->场景.身份, 存在甲.存在->身份, 1});
    代次 = 读取当前代次(运行包.读取服务());
    const 海中鱼巣::L2场景宿主存在新增请求 新增请求{
        {海中鱼巣::L2结构合同版本, *代次}, {2202},
        场景甲.场景节点->场景.身份, 存在甲.存在->身份};
    const auto 新增 = 场景服务->新增场景宿主存在关系(新增请求);
    检查(新增.成功() && 新增.当前关系 && !新增.已退出关系,
        "宿主关系新增");
    if (!新增.当前关系 || !成员.当前关系) return 1;
    const auto 重复 = 场景服务->新增场景宿主存在关系(新增请求);
    检查(重复.成功()
            && 重复.结果头.状态 == 海中鱼巣::L2结构状态::精确重复
            && 重复.当前关系 == 新增.当前关系,
        "宿主新增精确重复");
    auto 同键异义请求 = 新增请求;
    同键异义请求.宿主存在 = 存在乙.存在->身份;
    const auto 同键异义 = 场景服务->新增场景宿主存在关系(同键异义请求);
    检查(同键异义.结果头.状态 == 海中鱼巣::L2结构状态::幂等冲突,
        "宿主新增同键异义拒绝");

    代次 = 读取当前代次(运行包.读取服务());
    const auto 场景唯一冲突 = 场景服务->新增场景宿主存在关系(
        {{海中鱼巣::L2结构合同版本, *代次}, {2203},
            场景甲.场景节点->场景.身份, 存在乙.存在->身份});
    const auto 存在唯一冲突 = 场景服务->新增场景宿主存在关系(
        {{海中鱼巣::L2结构合同版本, *代次}, {2204},
            场景乙.场景节点->场景.身份, 存在甲.存在->身份});
    检查(场景唯一冲突.结果头.状态 == 海中鱼巣::L2结构状态::引用冲突
            && 存在唯一冲突.结果头.状态 == 海中鱼巣::L2结构状态::引用冲突,
        "场景端与存在端双向唯一冲突");

    const auto 完整甲 = 场景服务->读取完整场景(
        {{海中鱼巣::L2结构合同版本, *代次},
            海中鱼巣::L2读取类别::当前,
            场景甲.场景节点->场景.身份, 0});
    检查(完整甲.结果头.状态 == 海中鱼巣::L2结构状态::已读取
            && 完整甲.场景 && 完整甲.场景->直接成员.size() == 1
            && 完整甲.场景->当前宿主关系
            && 完整甲.场景->当前宿主关系->关系稳定编码
                != 成员.当前关系->关系稳定编码,
        "成员关系与宿主关系分账");

    const auto 替换前截止 = 新增.结果头.事实截止代次;
    代次 = 读取当前代次(运行包.读取服务());
    const auto 替换 = 场景服务->替换场景宿主存在关系(
        {{海中鱼巣::L2结构合同版本, *代次}, {2301},
            新增.当前关系->关系稳定编码,
            场景甲.场景节点->场景.身份,
            存在甲.存在->身份, 存在乙.存在->身份});
    检查(替换.成功() && 替换.当前关系 && 替换.已退出关系
            && 替换.当前关系->宿主存在 == 存在乙.存在->身份,
        "宿主关系原子替换");
    if (!替换.当前关系) return 1;
    代次 = 读取当前代次(运行包.读取服务());
    const auto 当前读取 = 场景服务->读取场景宿主存在关系(
        {{海中鱼巣::L2结构合同版本, *代次},
            海中鱼巣::L2读取类别::当前,
            场景甲.场景节点->场景.身份, 0});
    const auto 历史读取 = 场景服务->读取场景宿主存在关系(
        {{海中鱼巣::L2结构合同版本, *代次},
            海中鱼巣::L2读取类别::历史,
            场景甲.场景节点->场景.身份, 替换前截止});
    const auto 反向读取 = 场景服务->读取存在当前宿主场景(
        {{海中鱼巣::L2结构合同版本, *代次}, 存在乙.存在->身份});
    检查(当前读取.成功() && 历史读取.成功() && 反向读取.成功()
            && 当前读取.关系 == 反向读取.关系
            && 历史读取.关系->宿主存在 == 存在甲.存在->身份,
        "按场景当前历史及按存在反向读取");

    const auto 漂移 = 场景服务->新增场景宿主存在关系(
        {{海中鱼巣::L2结构合同版本, *代次 - 1}, {2401},
            场景乙.场景节点->场景.身份, 存在丙.存在->身份});
    检查(漂移.结果头.状态 == 海中鱼巣::L2结构状态::事实代次漂移,
        "旧代次漂移拒绝");
    const auto 非法前 = 读取当前代次(运行包.读取服务());
    const auto 非法 = 场景服务->新增场景宿主存在关系(
        {{海中鱼巣::L2结构合同版本, *非法前}, {},
            场景乙.场景节点->场景.身份, 存在丙.存在->身份});
    const auto 非法后 = 读取当前代次(运行包.读取服务());
    检查(非法.结果头.状态 == 海中鱼巣::L2结构状态::入口拒绝
            && 非法前 == 非法后,
        "非法入口零变化");
    const auto 错族 = 场景服务->新增场景宿主存在关系(
        {{海中鱼巣::L2结构合同版本, *非法后}, {2402},
            场景乙.场景节点->场景.身份,
            海中鱼巣::L2存在身份{根.场景节点->场景.身份.值}});
    检查(!错族.成功() && !错族.当前关系,
        "wrong-family存在端点拒绝");
    const auto 裸场景 = 场景服务->新增场景宿主存在关系(
        {{海中鱼巣::L2结构合同版本, *非法后}, {2403},
            海中鱼巣::L2场景身份{登记.登记->关系类型.值},
            存在丙.存在->身份});
    检查(!裸场景.成功() && !裸场景.当前关系,
        "裸L1节点不被接受为正式树内场景");

    代次 = 读取当前代次(运行包.读取服务());
    const auto 宿主丙 = 场景服务->新增场景宿主存在关系(
        {{海中鱼巣::L2结构合同版本, *代次}, {2501},
            场景丙.场景节点->场景.身份, 存在丙.存在->身份});
    if (!宿主丙.当前关系) return 1;
    代次 = 读取当前代次(运行包.读取服务());
    const auto 存在丙来源 = 存在服务->读取存在身份来源(
        {{海中鱼巣::L2结构合同版本, *代次},
            海中鱼巣::L2读取类别::当前, 存在丙.存在->身份, 0});
    if (!存在丙来源.身份来源) return 1;
    const auto 保护 = 存在服务->退出存在(
        {{海中鱼巣::L2结构合同版本, *代次}, {2502},
            存在丙.存在->身份,
            存在丙来源.身份来源->族归属关系稳定编码, {}});
    检查(保护.结果头.状态 == 海中鱼巣::L2结构状态::引用冲突,
        "当前宿主引用保护存在退出");

    代次 = 读取当前代次(运行包.读取服务());
    const auto 场景丙来源 = 场景服务->读取场景身份来源(
        {{海中鱼巣::L2结构合同版本, *代次},
            海中鱼巣::L2读取类别::当前,
            场景丙.场景节点->场景.身份, 0});
    const auto 场景丙归属 = 场景服务->读取场景树归属(
        {{海中鱼巣::L2结构合同版本, *代次},
            海中鱼巣::L2读取类别::当前,
            场景丙.场景节点->场景.身份, 0});
    if (!场景丙来源.身份来源 || !场景丙归属.树归属
        || !场景丙.直接父关系) return 1;
    const auto 退出场景 = 场景服务->退出场景(
        {{海中鱼巣::L2结构合同版本, *代次}, {2503},
            场景丙.场景节点->场景.身份,
            场景丙来源.身份来源->族归属关系稳定编码,
            场景丙归属.树归属->关系稳定编码,
            场景丙.直接父关系->关系稳定编码,
            {}, {}, {}, 宿主丙.当前关系->关系稳定编码});
    检查(退出场景.成功() && 退出场景.已退出宿主关系
            && 退出场景.已退出场景
            && 退出场景.已退出场景->当前宿主关系
                == 退出场景.已退出宿主关系,
        "场景退出闭包同代退出宿主关系");

    代次 = 读取当前代次(运行包.读取服务());
    const auto 退出 = 场景服务->退出场景宿主存在关系(
        {{海中鱼巣::L2结构合同版本, *代次}, {2601},
            替换.当前关系->关系稳定编码,
            场景甲.场景节点->场景.身份, 存在乙.存在->身份});
    const auto 退出后 = 场景服务->读取场景宿主存在关系(
        {{海中鱼巣::L2结构合同版本, 退出.结果头.事实截止代次},
            海中鱼巣::L2读取类别::当前,
            场景甲.场景节点->场景.身份, 0});
    检查(退出.成功() && 退出.已退出关系 && !退出.当前关系
            && 退出后.结果头.状态 == 海中鱼巣::L2结构状态::未找到,
        "宿主关系退出及当前清空");

    代次 = 读取当前代次(运行包.读取服务());
    std::atomic<bool> 开始{false};
    海中鱼巣::L2场景宿主存在写入结果 并发甲;
    海中鱼巣::L2场景宿主存在写入结果 并发乙;
    std::thread 线程甲([&] {
        while (!开始.load(std::memory_order_acquire)) std::this_thread::yield();
        并发甲 = 场景服务->新增场景宿主存在关系(
            {{海中鱼巣::L2结构合同版本, *代次}, {2701},
                场景甲.场景节点->场景.身份, 存在甲.存在->身份});
    });
    std::thread 线程乙([&] {
        while (!开始.load(std::memory_order_acquire)) std::this_thread::yield();
        并发乙 = 场景服务->新增场景宿主存在关系(
            {{海中鱼巣::L2结构合同版本, *代次}, {2702},
                场景乙.场景节点->场景.身份, 存在甲.存在->身份});
    });
    开始.store(true, std::memory_order_release);
    线程甲.join();
    线程乙.join();
    检查((并发甲.成功() ? 1 : 0) + (并发乙.成功() ? 1 : 0) == 1,
        "并发同存在仅一方提交");

    std::cout << "总用例=" << 用例数 << " 失败=" << 失败数 << '\n';
    return 失败数 == 0 ? 0 : 1;
}
