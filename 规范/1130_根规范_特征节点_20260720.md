# 1130 根规范 特征节点

编号：1130
日期：20260907
版本：v0.10
状态：正式根规范

## 1. 根定位

新 DATA-L2 特征信息是由正式特征类型、准确当前观察值、识别用历史值集合和值域集合描述、以特征节点身份独立引用的数据对象。它不以先存在某个宿主、宿主当前属性槽、观察时间或稳态结论为成立前提。多个同类型特征信息可以通过值域细分形成父子层次；特征信息节点与 2100 的值事实身份不是同一种身份。

本次固定第 1.1 节的新字段和第 1.2 节的细分关系，不新增结构 owner，不把既有特征实例身份或持久编码静默改义。第 2—5、7 节只约束既有具名宿主实例槽、当前值发布及长期观察合同；它们不再是新特征信息对象的字段清单或无宿主数据创建的业务前置。旧合同迁移前继续在原路径有效，不因此授权第二写入入口。新字段不等于完整 CRUD、持久映射、迁移或分裂应用已实现。

### 1.1 新特征信息固定字段

新特征信息对象只包含以下五个逻辑字段，清单封闭；父子和宿主等关联另由关系事实承载：

| 字段 | 固定含义 |
| --- | --- |
| 特征信息身份 | L1 分配的唯一、稳定、非零节点身份；不是值身份、类型身份、名称或内存地址。 |
| 特征类型 | 一个正式特征定义的稳定身份；说明所表达的特征及其值解释，不是硬编码业务枚举或显示词条。 |
| 当前观察值 | 本节点最近一次成功接收的准确观察值，采用 `内联 I64` 或 `2100 值身份引用`；不是域代表值，也不自动表示各宿主自己的当前值。 |
| 历史值集合 | 保存用于后续识别的不同值；允许具体值项与本节点稳定值域项引用混合，以域概括减少具体项，不承诺逐样本无损还原。 |
| 值域集合 | 本特征信息维护的零到多个值域项；各项保存父内稳定标识、代表值、范围描述和命中次数，具体封闭字段由 4110 第 1.2.2 节规定，不能以一个可变代表值解释全部形态。 |

完整特征信息必须具有合法身份、正式类型和一个明确的准确当前观察值；集合成员和初始化按 4110 校验。值域集合为空只表示尚无范围描述，不表示任意值均适用。逐字段维护时的未设置由操作结果明确表达，不能把整数 0、空引用或默认范围伪装成已经设置的内容；整数 0 本身是合法值。

逻辑字段不要求全部内联在一条物理节点记录中：身份来自节点，特征类型关联来自正式关系，值及范围由类型化材料承载，读取时组合；索引只加速定位。字段的承载方式、值域项最小结构和父子关系由 4110 第 1.2 节展开，不另建平行事实仓。

名称和节点类型沿用通用节点 / 名称关联规则；父子、宿主、场景、来源和观察时间由各自关联承载。当前观察值和识别历史是业务数据，不是版本、事实代次、幂等、批次、生命周期或技术恢复历史；不得借此恢复公共技术头或扩充 2100 三字段值对象。

### 1.2 核心处理原则

特征类在接收合法观察值时内部维护当前值、历史集合和值域，按累计三次命中的操作性稳定判据简化历史并被动建立同类型子节点；父节点及全部已有域保留。继承域仍可识别、积累及简化历史，但不因再次满三次重复生成同域后代。准确当前值不被域替代，历史概括也不授权删除独立准确材料；具体条件、初始化、去重和共同保存只在 4110 第 1.2 节展开。

域形成与误差参数来源由特征类型绑定规则确定，读取 4120；正式比较边界读取 4180。类内自动处理不向 L1 增加领域入口，不自动生成概念、状态或动态；各宿主当前采用仍由独立关联表达。本根规范不代替新 ABI、具体规则提供者、存储映射或旧入口迁移设计，也不证明相关代码已经可用。

## 2. 既有实例槽的本项目承载

实例特征由以下结构共同承载：

```text
节点类型为特征的实例槽位及稳定句柄
+ 宿主到槽位的唯一归属关系
+ 槽位到特征定义的唯一模板关系
+ 槽位中按正式值表示类型唯一指向当前特征值事实的属性槽
+ 具有独立稳定编码、来源和生命周期的当前值事实及历史值事实
+ 状态和来源证据关系
+ 主键与定位索引
```

宿主必须是规范允许拥有特征的稳定对象。允许的宿主类型为存在、场景、需求、任务和方法；新增宿主类型必须先修订正式规范。

## 3. 事实与边界

1. 一个实例槽位必须引用一个正式特征定义；词条、枚举或显示名称不得替代该定义身份。
2. 同一宿主与同一特征定义在同一当前版本下只能有一个有效实例槽位。
3. 实例槽位通过属性槽引用唯一当前值事实；历史值由已退出值事实及其生命周期读回，不再为当前值或历史值建立节点间关系，也不得复制原始材料。
4. 特征解释必须来自特征定义、二次特征、状态、动态或因果证据，不得写入自由文本业务字段。
5. 仅凭词条、外部词性标签或类型标签不能成立实例特征；正式写入必须先从名称候选和上下文解析并读回特征定义节点。
6. 4125 的静态一次特征事实集合是本实例结构内的专用复合记录：集合版本没有原始值属性槽，以完整成员关系表达事实组成；它不替代普通实例槽位，也不得由普通“唯一当前值”入口读取或写入。

## 4. 写入、读取与验证

实例槽位、当前值属性槽和值事实只能由特征领域服务写入。特征领域服务负责宿主类型、定义身份、唯一槽位、特征定义允许的值表示和值域、值来源及独立材料准入；数据操作层负责同一写入会话中的节点、关系、类型化值事实、属性槽和索引发布。

需求、任务、方法及其它高级服务只能经特征领域服务读取宿主的当前特征值完整投影，不得绕过特征服务读取或改写底层值事实。有效读回必须在同一事实截止证明宿主关系、模板关系、唯一当前属性槽、值事实身份、值表示、材料、来源及生命周期一致。业务比较只消费该当前值事实；状态、动态及其当前选择不得替代。

## 5. 生命周期与当前性

槽位身份随宿主和特征定义长期可追溯。当前值换代必须经统一变化入口：业务规则先裁决新值，再依次预形成新值事实、新状态记录和引用既有前状态的新动态，最终原子发布新值事实、切换唯一当前属性槽、退出旧值事实并发布新状态 / 新动态；不得改写旧载荷，也不得从新状态或动态后状态反推新值。旧值退出当前后仍保留来源、材料和生命周期。

首次赋值没有前状态，只原子发布首个当前值和对应初始状态，不生成动态。新值与旧值完全相同时不形成换代、状态或动态。历史补录状态 / 动态不得切换当前属性槽；状态记录引用缺失的旧数据必须先走迁移或返回材料不足，禁止临场伪造前状态。宿主、定义、属性槽或值事实发生版本漂移时，旧读回不得继续冒充当前特征值事实或当前特征值投影。

## 6. 子规范路由

| 子规范 | 负责内容 |
| --- | --- |
| 4110 | 新五字段展开、历史简化、值域被动分裂及共同保存；既有实例槽另节限域 |
| 4120 | 特征定义、材料解释、域形成规则绑定及复合组成 |
| 4125 | 静态一次特征事实集合、版本和成员追溯 |
| 4130 | 特征值比较和当前性 |
| 4140 | 枚举型实例特征值来源 |
| 4170 | 特征批次发布记录、完整语义幂等和历史重放 |
| 4180 | 正式比较注册、差异与结构域命中边界 |

## 7. 既有长期观察与独立子特征

同一存在、同一特征类型可以长期积累多个独立形态区间；它们是历史观察的子特征记录，不是多个未经裁决的当前实例槽。区间身份与一次观测的特征实例身份分开，不能因返回旧形态而伪造旧实例仍为当前。

一次观察先在同一事实截止证明存在、直接特征成员、实际值、类型及观察身份，再按固定校正规则与该范围内全部保留区间比较。返回全部适用区间和各自差异；只有完整读取并确认全部超过阈值时才能创建新区间。读取失败、预算不足和不支持比较均不属于全不匹配。多命中不创建新区间，也不按稳定编码或创建先后选一个形态。

区间固定首代表可阻止小步渐变无限吞并新形态；旧形态重现复用原域。观测时间、原事实截止、成员关系及实际值可回查；同一观测同义重放不重复积累，异义拒绝。实际最小最大值只作统计证据，不修改概念匹配域或原实例值。

实例换代、宿主成员变更、区间记录和概念关联由各自唯一 owner 发布。跨 owner 流程保留完整原请求和各阶段正式回执，不能宣称整体单事务；当前完成必须重新核验共同截止下的真实实例及关联。

## 修订记录

| 日期 | 版本 | 修订内容 |
| --- | --- | --- |
| 20260907 | v0.10 | 固定准确当前观察值、可用域简化的历史集合及五个顶层字段；压缩根过程正文，初始化、继承域防同义后代与共同保存转由 4110，域规则绑定由 4120 承接。 |
| 20260907 | v0.9 | 固定特征信息四字段及逐域标识、代表、范围与命中数据；值域累计命中三次由特征类内部被动生成子节点，父节点和全部已有域保留。旧宿主槽与长期观察合同限域保留，不扩展 L1 或声明新代码可用。 |
| 20260906 | v0.8 | 增加同存在同类型长期观察、固定代表匹配全部区间及多形态独立区间；保持实例原值。 |
| 20260905 | v0.7 | 增加静态一次特征事实集合的专用复合记录例外；集合版本无原始值槽，普通实例当前值合同不变。 |
| 20260828 | v0.6 | 明确唯一当前属性槽指向的值事实是业务比较权威；当前值真实变化统一原子发布值切换、状态和动态，首次赋值无动态、无变化无新记录，禁止从状态 / 动态反推当前值。 |
| 20260824 | v0.5 | 按 L1 节点、关系、值事实分账模型将当前特征值改为稳定值事实身份和唯一属性槽；取消“特征值必为节点”和当前值节点关系口径，并补入值表示、值域与独立材料准入。 |
| 20260815 | v0.4 | 实例特征名称入口收敛为词条候选；类型只由正式特征定义节点成立，词条和外部词性标签均不得替代。 |
| 20260722 | v0.3 | 删除实例特征对平行附属身份和通用值容器的依赖，保留节点、关系和特征值类型化原始材料。 |
| 20260722 | v0.2 | 增加 4170 批次发布记录路由；根对象身份、写入权和当前值边界不变。 |
| 20260720 | v0.1 | 确立实例特征必须引用抽象特征，并由宿主归属、模板定义和当前值三类正式关系承载。 |
