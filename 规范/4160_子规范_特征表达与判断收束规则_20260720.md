# 4160 子规范 特征表达与判断收束规则

编号：4160
日期：20260905
版本：v0.6
状态：正式子规范

直接上级规范：1120、1130、1160、1180、2100。

直接依赖规范：无。

相关边界规范：4110—4150、4180、6300—6320。

## 1. 定位

本规范规定材料、测量、状态和判断在特征系统中的基础表达语法。核心原则是：判断结果不得伪装成直接事实。

## 2. 基础分类

1. 一阶特征表达可直接取得、测量、计数、引用或追溯的基础材料值；4125 还允许把多个已经成立的一次特征事实按角色和顺序保存为一个静态事实集合。集合只表达成员结构，不产生由成员推导的新值。由多个事实、变化量、关系、评分规则或比较结果计算出的新值不是一阶特征。
2. 复合特征表达具名子特征的组成关系，本体不承载判定日志、错误码、界面状态、临时缓存或说明文本。
3. 二次特征属于特征，表达需求驱动形成的关系性、比较性、方向性、变化率和聚合性派生；实际二次至 n 次统一称呼，但必须回查输入事实、直接前驱、真实阶次和派生规则。
4. 状态或治理收束结果表达合法流程或方法执行后的阶段性结论，必须保留来源、输入、规则、适用范围和有效期。

凡特征值依赖其它特征、评分、数量、版本、来源、阈值、任务上下文或治理裁决推导，无论值类型是枚举、整数还是二态值，都属于判断型结果。

## 3. 本项目承载

```text
一阶材料 -> 特征定义 + 实例槽位 + 稳定值事实身份、类型化材料及生命周期
复合组成 -> 抽象特征或二次特征的有序关系
静态事实集合 -> 4125 的集合身份、版本和完整成员关系
比较与计算聚合 -> 二次特征及其输入、规则和版本
阶段结论 -> 状态节点或治理对象特征集合
来源证据 -> 动态、因果、规则身份和版本关系
```

普通函数返回值、日志、错误码、控制台和界面只可作为人读或调用结果，不得直接写成上述机器事实。

## 4. 判断收束原则

判断型结果必须同时声明并可回读：输入特征族、收束规则、写入方、读取方、失败分流、来源证据、适用范围、有效期和规则版本。输入不足、冲突、过期、不可回查或阈值不满足必须形成具名结果，不得退化为裸 `false`。

单个评分超过阈值、错误码为零、外设报告“可用”、界面显示成功或日志写入成功均不能单独形成判断结论。

比较或判断业务必须先使用已有特征和关系。值相同、值缺失、类型不可比较、关系不足、事实冲突和确实无可派生差异必须分别返回；只有事实完整而现有表达不足以形成需求所需差异时，才调用 4150 形成最小任务局部二次特征候选。候选是否有用首先由当前需求结果验证，不由名称、词性或候选生成成功自证。

## 5. 值类型边界

直接分类码和由唯一生命周期入口推进的阶段码可以作为一阶材料，但只表达分类或生命周期。质量、可用性、完成度、归属、稳定性、安全、服务结算、需求满足和任务完成属于判断型结果，必须经过正式收束。

句柄有效、版本一致、评分较高或候选唯一都只可作为收束输入，不自动等于材料可用或判断完成。普通 `bool` 不得持久化为业务判断事实。

## 6. 外部材料边界

外设、网络、文件、数据库和用户输入只能直接形成可追溯原始材料、测量、计数、评分、版本、来源和低层候选。它们不得直接裁决材料质量、正式提交、存在归属、需求满足、任务完成或安全与服务结算。

外部材料必须经观察、中间层收束、自我认知或指定治理方法，结合当前任务、场景、阈值和证据回查后，才能形成判断型特征或状态。

## 7. 子规范路由

| 子规范段 | 负责内容 |
| --- | --- |
| 4110—4120 | 一阶、实例和复合特征结构 |
| 4130 | 特征值三态比较和不可比结果 |
| 4140 | 枚举型实例特征值合法来源 |
| 4150 | 二次特征输入、规则和持久化边界 |
| 4180 | 类型化差异算子、算法注册、单位维度和调度边界 |
| 6300—6320 | 外设材料、质量诊断和自我收束分层 |

## 修订记录

| 日期 | 版本 | 修订内容 |
| --- | --- | --- |
| 20260905 | v0.6 | 区分“不计算新值的静态一次事实集合”和“产生新值的比较 / 计算聚合”，前者路由 4125，后者仍为二次特征。 |
| 20260824 | v0.5 | 将一阶材料承载统一为稳定值事实身份、类型化材料和生命周期，不再要求特征值成为节点。 |
| 20260814 | v0.4 | 明确一阶特征不包含关系、评分或变化率派生；二次至 n 次统一归入二次特征，按需求先用已有特征 / 关系、缺事实观察、表达不足时最小派生并由当前需求验证。 |
| 20260728 | v0.3 | 将比较与差异型判断接入 4180 的有限算法族、注册身份和输入输出合同。 |
| 20260721 | v0.2 | 从伪根对象 1175 迁入 4160 特征主题子规范；对象身份和原则继续服从对应特征、状态及特征值根规范，业务逻辑不变。 |
| 20260720 | v0.1 | 确立一阶、复合、二次、状态收束及外设材料边界，并由特征、状态、动态和因果结构承载。 |
