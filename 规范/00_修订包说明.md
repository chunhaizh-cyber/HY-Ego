# 20260821 需求、任务与方法根规范 v0.6 正式收口说明

状态：正式修订包说明
依据代码/规范基线：`HY-Ego/main@62008c747b9f61ee3bb7e26a387c1ee2413586a9`

## 0. 当前正式收口

本说明中的 v0.5 材料保留为形成过程证据；当前正式根规范只由以下稳定路径承载：

```text
规范/3100_根规范_需求.md  v0.6
规范/3200_根规范_任务.md  v0.6
规范/3300_根规范_方法.md  v0.6
```

当前正式口径为：

```text
需求列表项拥有目标三元组
→ 同一列表项至多一个当前任务
→ 候选可以多个，但每轮当前活动方法路径只能一条
→ 无六项能力匹配时形成方法能力缺失判断结果
→ 经需求统一入口形成学习需求
→ 不建立能力缺口节点、关系、owner 或数量事实
```

本次收口只改变规范路径、版本和正式身份，不授权代码实施，不改变 5090、5160、学习或价值回流的后继提供者门禁。

以下第 1—7 节保留 v0.5 形成过程和历史裁决，不再作为当前正文合同；当前机器语义以三份 v0.6 正式根规范和规范目录登记为准。

## 1. 本轮统一裁决

本轮固定以下新口径：

```text
需求节点
    = 某个存在的一个确定目标契约

需求树
    = 目标分解与来源上下文的唯一权威树

需求列表
    = 按精确目标聚合需求树中分散节点的正式聚合结构
    = 权重查询入口
    = 任务查询入口
    = 保存对应需求树节点成员集合

任务
    = 对一个需求列表项所代表的确定目标组织一次有身份活动

任务方法路径
    = 同一目标在当前筹办轮次可选择的 0..N 条达成路径

当前实例方法
    = 一轮执行实际选择的唯一具体执行快照

任务虚拟存在
    = 任务运行状态的唯一承载体
```

## 2. 双态一体

需求信息不是两套事实，而是同一批需求节点的两种正式组织状态：

```text
需求树态：
    保留父子分解、来源、路径、权重和上下文

需求列表态：
    按 所属存在 + 目标宿主 + 目标特征 + 目标状态合同 精确聚合
    保存成员需求节点集合
    作为权重和任务查询入口
```

列表项不删除、覆盖或合并树节点。树节点仍分别保留自己的路径、权重、授权、预算、来源和结算身份。

## 3. 任务承接模型

旧：

```text
多个等价需求 -> 直接共同授权任务
```

新：

```text
D1 ─┐
D2 ─┼─> 需求列表项 L -> 当前任务 T
D3 ─┘
```

任务核心不复制目标。任务目标由列表项的精确聚合目标确定，并可追溯到全部成员需求。

## 4. 权重模型

需求列表项是权重查询入口，但不保存一个不可审计的“总权重”作为第二权威。

```text
列表项
-> 当前成员需求集合
-> 各成员路径 / 当前性 / 权重 / 授权 / 预算
-> 业务层形成当前权重与权限投影
```

聚合结果必须保留成员来源明细。

## 5. 方法路径与目标分解

```text
同一目标多个方法
    -> 任务方法路径集合
    -> 不生成需求

独立前置目标
    -> 子需求
    -> 进入需求树
    -> 子需求经列表聚合后取得自己的任务
```

## 6. 修订文件

### 6.1 v0.5 历史替换稿与新建子规范（形成过程）

修订稿（替换既有正式规范）：

- `3100_根规范_需求_v0.5_修订稿.md`
- `3200_根规范_任务_v0.5_修订稿.md`
- `3300_根规范_方法_v0.5_修订稿.md`
- `4250_子规范_L2需求树需求列表与对外接口统一合同_v0.4_修订稿.md`
- `4260_子规范_L2任务核心方法路径与对外接口统一合同_v0.4_修订稿.md`

新建子规范（3300 方法根规范的下级子规范，首次建立）：

- `4270_子规范_L2方法核心结构与对外接口统一合同_v0.1.md`

### 6.2 v0.5 后续同步清单（历史形成过程）

以下子规范属于后续同步治理范围；它们的现行文件和状态不因本次根规范收口自动改变。具体后继仍须按当前代码事实和提供者合同单独修订，不得把本说明当作代码实施授权。

- `5090_需求任务方法运行闭环`：需按“一个列表项至多一个当前任务、一轮一个实例方法”口径同步
- `5100_需求下级规则`：需按“所属存在是需求核心不可变关系”口径同步
- `5110_需求树生长机制`：需按“需求树 + 需求列表双态一体、替代方法不进入需求树”口径同步
- `5120_需求任务单目标与方法多路径`：需按“任务通过列表项读取目标、不复制目标副本”口径同步
- `5160_需求正式结算`：需按“列表项成员独立结算、聚合不丢失成员来源”口径同步
- `5200_任务根据需求初始化`：需按“任务身份 + 需求列表项 + 任务虚拟存在”结构锚点同步
- `5210_任务状态特征集合`：需按“任务虚拟存在为状态唯一承载”口径同步
- `5220_子任务完成触发父任务重筹办`：需按“父子关系由需求树推导、不建立第二任务树”口径同步
- `5230_任务筹办与执行边界`：需按“0..N 可达路径 + 0..1 当前选中 + 0..1 实例方法”三层结构同步

## 7. 推荐确认顺序

```text
3100
-> 3200
-> 3300
-> 4250
-> 4260
-> 4270
```

6.1 五份完整替换稿确认完成后，即解除旧“多来源需求直接挂任务”“任务父子树”“任务复制目标”口径对代码实施计划的阻塞。

6.2 待编写同步修订稿按以下顺序推进，不阻塞 6.1：

```text
5100
-> 5110
-> 5120
-> 5200
-> 5210
-> 5220
-> 5230
-> 5090
-> 5160
```
