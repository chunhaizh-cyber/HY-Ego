# 1110 管理类原函数与四域方法树接口规则

## 1. 目标

本文用于把“任务管理任务如何使用管理类原函数接入四域方法树”固定成一份可直接落代码的接口规范。

本文要回答的是：

- 管理类原函数在四域方法树里到底是什么
- 为什么它们属于先天执行原语，而不是树外管理员
- 输入场景、输出场景、任务虚拟存在、方法虚拟存在之间怎样分工
- 四域方法树需要的信息，哪些属于前置上下文，哪些应由原函数产出
- 当前最少应补哪些管理类原函数


## 2. 上下位关系

上位规范：

- [0910_任务控制四域职责表.md](D:\鱼巢\规范\0910_任务控制四域职责表.md)
- [0940_四域方法树的出生与成长规则.md](D:\鱼巢\规范\0940_四域方法树的出生与成长规则.md)
- [0960_四域方法树自动生长闭环规范_v0.md](D:\鱼巢\规范\0960_四域方法树自动生长闭环规范_v0.md)
- [0970_任务筹办规则.md](D:\鱼巢\规范\0970_任务筹办规则.md)
- [1100_本能动作体系规范（任务方法本能方法函数）.md](D:\鱼巢\规范\1100_本能动作体系规范（任务方法本能方法函数）.md)

直接实现落点：

- [本能动作管理类.ixx](D:\鱼巢\本能动作管理类.ixx)
- [四域方法树注册模块_v0.ixx](D:\鱼巢\四域方法树注册模块_v0.ixx)
- [四域方法树匹配器_v0.ixx](D:\鱼巢\四域方法树匹配器_v0.ixx)
- [任务控制总控模块_v0.ixx](D:\鱼巢\任务控制总控模块_v0.ixx)
- [任务类.ixx](D:\鱼巢\任务类.ixx)
- [方法类.ixx](D:\鱼巢\方法类.ixx)


## 3. 核心结论

必须固定以下结论：

1. `任务管理任务` 自身也是任务，不是任务系统外的额外管理员。
2. 管理类原函数是任务管理任务在四域方法树中的先天执行原语。
3. 四域方法树是本域优先复用的经验树，本域执行器是未命中时才接管的兜底器。
4. 输入场景、输出场景只是统一容器，容器中既可以放现实世界存在，也可以放任务虚拟存在、方法虚拟存在、需求目标状态和当前步骤指针。
5. `任务虚拟存在` 与 `方法虚拟存在` 是特征承载存在，不是任务或方法本体；其中主要存储运行特征、统计特征、派生特征与二次特征。
6. 四域方法树运行所需信息，必须拆成 `前置上下文特征` 与 `原函数产出特征` 两层，不允许混成“一次返回全包”。
7. 新增管理类原函数的职责，是产出本职责范围内的管理事实、管理建议和管理证据，而不是重复返回全部四域上下文。
8. 管理类原函数的输出，默认先落到输出场景和必要的虚拟存在特征中，不直接等同于调度器消息。
9. 任务状态推进、消息分发、队列流转，仍由任务管理器、总控与调度器负责。
10. 管理类原函数允许输出二次特征、评分、原因码、指针句柄和子场景证据，但不得借名发明第二套任务状态机。

可收敛为一句话：

`管理类原函数是四域方法树中的先天执行原语；四域运行依赖“前置上下文特征 + 原函数产出特征”的分层供给，运行时先匹配本域树，未命中才回落到本域兜底执行器。`


## 4. 分层定义

### 4.1 结构层

结构层负责回答“谁是谁、挂在哪里、彼此怎样关联”。

包括：

- 任务信息节点
- 方法信息节点
- 需求节点
- 步骤节点
- 结果节点

### 4.2 虚拟存在层

虚拟存在层负责承载“可读写、可比较、可被方法消费”的运行特征。

包括：

- 任务虚拟存在
- 方法虚拟存在

### 4.3 场景层

场景层负责一次调用的容器化装载。

包括：

- 输入场景
- 输出场景
- 子场景
- 场景中的状态、关系、指针与证据


## 5. 四域所需信息的两层来源

### 5.1 前置上下文特征

以下信息属于进入四域方法树匹配前就应具备的上下文，不要求由单个原函数重复返回：

- 当前功能域
- 任务虚拟存在
- 方法虚拟存在
- 当前任务头结点 / 当前步骤结点 / 当前方法首节点
- 当前现实场景
- 需求目标状态
- 安全值
- 服务值
- 需求值
- 当前预算
- 最近结果稳定
- 最近执行失败
- 风险变坏
- 检查点通过
- 依赖缺失
- 需要新子任务或新步骤
- 关键方法不再可用
- 已满足完成判据

### 5.2 原函数产出特征

以下信息适合作为管理类原函数的直接产出：

- 条件缺口分类
- 可执行性判断
- 推荐方法指针
- 候选方法评分
- 推荐分解方向
- 是否需要补条件任务
- 是否需要观察任务
- 是否需要恢复任务
- 是否建议等待学习
- 是否建议回到筹办
- 失败原因码
- 评分证据
- 二次特征证据
- 结果签名

### 5.3 分层硬约束

必须固定：

1. 原函数不负责重复生成全部四域上下文。
2. 四域匹配器消费的是完整上下文，而不是某个原函数的单一返回值。
3. 原函数必须完整覆盖其职责范围内的产出特征。
4. 前置上下文缺失时，应由外层先补齐输入场景，而不是要求原函数自造上下文真值。


## 6. 输入输出场景规范

### 6.1 输入场景允许装载的对象

管理类原函数的输入场景允许直接装载：

- 任务虚拟存在指针
- 方法虚拟存在指针
- 需求目标状态
- 当前任务头结点指针
- 当前步骤结点指针
- 当前方法首节点指针
- 最近反馈摘要
- 当前控制预算
- 当前风险与检查点状态

### 6.2 输出场景允许写入的对象

管理类原函数的输出场景允许写入：

- I64 特征值
- 指针特征值
- 状态节点
- 二次特征节点
- 原因码
- 推荐方法 / 推荐步骤 / 推荐子任务的指针
- 子场景形式的证据包

### 6.3 写回边界

必须固定：

1. 即时供调用方消费的信息优先写输出场景。
2. 需要长期沉淀的统计与组织信息，允许写回任务虚拟存在或方法虚拟存在。
3. 原函数不应直接冒充调度器去提交 `结构_任务返回消息`。
4. 原函数若需要推动状态机，必须通过外层适配器解释其输出后再落地。


## 7. 七个管理类原函数的职责表

### 7.1 第一批最小闭环

当前最少应先落成以下 4 个：

- `管理_筹办器(4000)`：负责读取需求目标、任务虚拟存在与当前场景，输出候选方法、条件缺口与可执行筹办结论。
- `管理_门控器(4004)`：负责在维护域判断继续维护、转筹办、转纠偏或转收束。
- `管理_复盘器(4005)`：负责在收束域判断是否满足完成/失败闭环，并输出收束证据与经验摘要。
- `管理_失败策略器(4006)`：负责在纠偏域输出替代方法、回退、收缩、恢复任务与等待学习建议。

### 7.2 第二批补全能力

为了让任务管理任务更完整地转为原函数驱动，还应补齐：

- `管理_选择器(4001)`：在候选方法、候选步骤、候选子任务之间做选择。
- `管理_分解器(4002)`：把父任务分解成步骤、阶段任务、补条件任务和恢复任务建议。
- `管理_求证器(4003)`：验证需求是否满足、预测是否命中、完成判据是否成立。

### 7.3 各函数的最小输出责任

必须固定：

- `管理_筹办器` 至少输出：候选方法、条件缺口、可直接执行标记、推荐分解方向。
- `管理_选择器` 至少输出：被选对象指针、排序分、选择原因码。
- `管理_分解器` 至少输出：新步骤建议、新子任务建议、并发/串行关系建议。
- `管理_求证器` 至少输出：需求满足标记、命中证据、完成判据标记。
- `管理_门控器` 至少输出：下一域建议、继续/回退/等待标记。
- `管理_复盘器` 至少输出：终态结论、经验摘要、收束原因码。
- `管理_失败策略器` 至少输出：替代方法、恢复动作、等待学习或无法继续建议。


## 8. 与四域方法树的优先级关系

必须固定运行顺序：

1. 先判定当前功能域。
2. 再匹配对应的本域方法树。
3. 本域树命中时，优先使用本域树建议方法。
4. 只有本域树未命中时，才允许进入本域兜底执行器。
5. 兜底执行器走通后的成功路径，允许写回本域方法树。

因此：

- 管理类原函数不是树外兜底补丁。
- 管理类原函数首先是四域树根上的先天动作来源。
- 本域执行器应视为“域树未长出足够枝条时”的保底承接层。


## 9. 当前实现对齐结论

按当前代码口径，应视为：

1. `4000~4006` 的管理类原函数 ID 已预留。
2. 四域方法树根已绑定 `管理_筹办器 / 管理_门控器 / 管理_失败策略器 / 管理_复盘器` 作为根动作。
3. 当前主要任务管理能力仍大量落在总控模块、任务执行器、缺方法分流器与各域执行器中。
4. 因此当前系统已经具备“四域树优先、未命中才兜底、成功路径可写回”的框架，但管理类原函数执行体仍未完全落成。


## 10. 筹办兜底方法化边界

当前筹办兜底逻辑允许逐步方法化，但必须固定为“分析与决策方法化，结构与状态落地留在外层”。

### 10.1 适合先方法化的部分

以下能力适合作为第一批筹办管理类原函数沉入方法树：

- 候选方法召回
- 方法结果满足需求的评分
- 条件缺口分析
- 候选方法可执行性判断
- 补条件 / 观察 / 等待学习 / 无法继续的分流判定
- 候选步骤排序与首选步骤推荐

这些部分的共同特点是：

- 输入可以主要来自任务虚拟存在、方法虚拟存在、需求目标状态和当前场景
- 输出可以自然落成评分、原因码、推荐指针、缺口分类和证据二次特征
- 它们本质上属于“算什么、判什么、推荐什么”

### 10.2 不应直接方法化的部分

以下能力不宜直接下沉为管理类原函数主体，而应继续由任务管理器、总控或调度器负责：

- 确保任务头结点存在
- 创建初始步骤结点
- 创建初始结果结点
- 把方法绑定到步骤
- 写任务状态 `就绪 / 待重筹办 / 无法执行 / 等待中`
- 提交任务返回消息
- 进入队列、挂起、等待学习唤醒

这些部分的共同特点是：

- 它们直接改写任务结构或状态机
- 它们承担调度与生命周期责任
- 它们不只是“给建议”，而是在真正修改系统运行结构

### 10.3 固定分工

因此，筹办兜底方法化后必须保持以下分工：

1. 管理类原函数负责输出管理事实、管理建议和管理证据。
2. 任务管理器负责按这些输出真正创建步骤、结果、子任务和绑定关系。
3. 总控与调度器负责把外层解释结果落成状态迁移与消息推进。

可收敛为一句话：

`筹办兜底可以方法化，但应方法化的是“筹办分析与决策”；真正的任务结构创建、状态写回和消息推进仍留在外层控制器。`


## 11. 硬约束

1. 管理类原函数必须被视为四域方法树中的先天执行原语。
2. 任务管理任务使用的输入场景，允许直接携带任务虚拟存在与方法虚拟存在。
3. 虚拟存在存储的是任务或方法的运行特征、统计特征、派生特征与二次特征，而不是结构关系本体。
4. 四域方法树运行信息必须拆成前置上下文特征与原函数产出特征两层。
5. 新增原函数不得被要求单次返回四域全部所需特征。
6. 新增原函数必须完整覆盖其职责范围内需要产出的管理事实、管理建议与管理证据。
7. 运行时必须先优先匹配当前域方法树，未命中后才允许进入本域兜底执行器。
8. 兜底成功路径允许写回本域方法树，作为临时枝继续生长。
9. 管理类原函数不得绕开外层适配器直接冒充调度器消息。
10. 第一批实现最少应补齐 `管理_筹办器 / 管理_门控器 / 管理_复盘器 / 管理_失败策略器`。
