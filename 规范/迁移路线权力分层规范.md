# 迁移路线权力分层规范

日期：2026-07-08
状态：正式规范

## 1. 定位

本规范定义旧项目能力进入 `海中鱼巣` 后的迁移权力分层。

当前迁移路线已经从“逐函数 / 逐小切片迁移”调整为：

```text
服务操作函数矩阵
-> 应用逻辑流程图迁移包
-> 批次级代码实施包
-> 批次级验收
```

旧函数事实、旧逻辑迁移包、旧小切片计划和派生详细设计仍可保留，但默认只能作为证据材料、边界材料或候选材料，不再直接成为代码实施入口。

## 2. 四层权力关系

| 层级 | 文件或产物 | 权力 | 禁止扩大 |
| --- | --- | --- | --- |
| 证据层 | 旧函数事实、只读扫描、逻辑迁移包归并表、被归并小切片 | 发现旧行为、副作用、输入输出、排除项和风险 | 不得直接生成 C++ 实施许可 |
| 服务矩阵层 | `规范/详细设计/服务操作函数矩阵*.md` | 定义每个服务可创建、读取、绑定、状态迁移、候选读取、拒绝和失效阻断哪些结构 | 不得替代领域服务详细设计和入口拒绝验收 |
| 流程图迁移层 | `流程图/*.md` / `流程图/*.html` | 把旧项目功能流程改写为服务调用编排、拒绝路径和验收路径 | 不得把流程图当作代码已实现 |
| 实施包层 | 批次级代码实施包 | 明确允许文件、禁止文件、实施步骤、验证命令和完成声明边界 | 不得绕过确认门禁扩大到旧函数体、旧字段或外设链路 |

## 3. 旧材料保留规则

以下材料必须保留为证据，不得因为路线改变而删除：

```text
逐函数只读扫描清单
FS-00 至 FS-X 只读扫描记录
逻辑迁移包归并表
旧能力排除项汇总
已确认但被新路线降权的小切片计划
已生成但仅作为候选边界的详细设计
```

保留材料必须标清当前权力：

```text
证据保留
候选设计
已被服务矩阵归并
不再单独确认
不单独入队
需经流程图迁移包重审
```

## 4. 计划目录规则

已经被批次级实施包归并、且不再单独确认的小切片，不得继续留在 `计划/待确认计划/`。

这类文件应移动到 `计划/已完成计划/`，并在状态行写明：

```text
已归并关闭 / 证据保留 / 不再单独确认 / 不单独入队
```

`计划/待确认计划/` 只保留真正等待用户确认、且确认后可能进入设计队列或可执行队列的计划。

## 5. 详细设计清理规则

详细设计按以下五类处理：

| 分类 | 含义 | 处理方式 |
| --- | --- | --- |
| 保留权威 | 核心结构、仓库、领域服务、入口拒绝、日志、锁、命名等基础契约 | 保持权威，后续实施必须遵守 |
| 已验证实现依据 | 已有代码切片按该设计实现并验证 | 保留为实现依据和验收证据 |
| 候选设计 | 描述后续候选算法、候选读取、后续生命周期或材料治理 | 保留，但必须经服务矩阵和流程图迁移包重审 |
| 证据保留 | 从旧函数或旧计划归纳出的边界材料 | 保留为证据，不直接生成代码 |
| 可撤销 / 可合并 | 与新路线重复、被批次包吸收或只有目录路由意义 | 先登记待确认问题，再决定删除、合并或归档 |

候选设计中的“后续候选切片草案”“允许文件草案”“入口名草案”只能作为后续服务矩阵或流程图迁移包的输入，不得直接登记为可执行代码切片。

## 6. 当前已降权小切片

以下计划已被 `计划/20260707_服务操作函数矩阵第一批代码实施包_v0.1.md` 归并，移出待确认目录并保留为证据：

```text
计划/已完成计划/20260707_FS02_动态聚合运动基元候选代码实施切片_v0.1.md
计划/已完成计划/20260707_FS02_因果模板查询候选代码实施切片_v0.1.md
计划/已完成计划/20260707_FS02_实例状态生命周期读取阻断代码实施切片_v0.1.md
计划/已完成计划/20260707_FS04_需求树后续结构代码实施切片_v0.1.md
计划/已完成计划/20260707_FS05_任务状态机筹办执行桥代码实施切片_v0.1.md
计划/已完成计划/20260707_基础信息服务分层完整增强后续代码实施切片_v0.1.md
```

这些计划不能再被用户单独确认、不能单独入队、不能作为独立代码实施入口。

## 7. 当前需重审候选详细设计

以下详细设计保留，但后续进入 C++ 前必须经服务矩阵和流程图迁移包重审：

```text
规范/详细设计/动态聚合与运动基元候选详细设计.md
规范/详细设计/因果模板与查询候选详细设计.md
规范/详细设计/实例状态生命周期治理详细设计.md
规范/详细设计/需求树后续结构详细设计.md
规范/详细设计/任务状态机筹办执行桥详细设计.md
规范/详细设计/世界服务兼容壳与依赖注入治理详细设计.md
规范/详细设计/抽象状态动态治理详细设计.md
规范/详细设计/实例状态动态生命周期治理详细设计.md
```

## 8. 完成声明边界

本规范只能证明：

```text
迁移路线权力分层已确认。
旧小切片和候选详细设计的执行权已降级。
```

不能证明：

```text
服务矩阵全部完成。
应用逻辑流程图迁移完成。
C++ 代码已新增。
旧项目能力已迁移。
旧函数体已迁移。
```
