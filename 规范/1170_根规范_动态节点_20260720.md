# 1170 根规范 动态节点

编号：1170
日期：20260909
版本：v0.8
状态：正式根规范

## 1. 根定位

动态节点描述具体某一存在在一段时间内发生的变化，承载原子状态变化及其组合；合法动作致变记录在此基础上由4220收紧。动态信息由场景组织，独立动态概念由2220/7190在已有动态根下组织；“变化”领域锚点不兼任概念身份，不另设并列动态模板根。

动态只记录已经发生的变化、组成、来源及其业务引用，不承载特征当前值。涉及存在的业务比较先重读其当前采用，再按 1130 / 2100 取得对应固定准确 F 的完整值；不能以旧 F 可读、最后动态、动态后状态、时间或目录顺序推定当前采用。

## 2. 本项目承载

```text
动态节点及稳定句柄
+ 所属场景动态实例树组织关系与被描述主体存在关系
+ 原子：真实前后状态及同一被描述属性绑定
+ 组合：直接子动态及必要组成角色
+ 由来源取得的实际时间范围和必要非拓扑材料
+ 可选名称概念引用
+ 可选来源动作关系
+ 来源、聚合依据及可重建定位索引
```

上述逻辑字段、必需关系与生成规则由4210唯一展开；不要求组合另有两个伪造总体状态，不以旧单目标角色承接全部新结构。动作致变动态的来源动作和同源账基数由4220收紧。来源动作必须指向合法方法动作入口或领域服务动作事实。线程、调度器、外部输入、日志和界面不得成为来源动作。

## 3. 事实与边界

1. 原子变化保留真实状态端点；组合保留子动态来源、角色和实际时序。主体、场景、被描述属性、用途及来源由独立强类型关系承载，不进入成员状态本体。技术可靠性和保留合同不新增为本轮业务字段；没有动作证据的被动变化不得伪造方法动作。
2. 外部变化只能记录为观察动态或外部事件动态，不得伪装成自我方法动作。
3. 原子及组合动态通过1150/4202的实际场景动态根组织为实例树；组织父单一，组合来源可复用引用。不得把动态当作普通场景成员、存在组成部分或特征分裂子节点；旧全面禁止动态实例树的条款退役。
4. 简单动态可以聚合成复杂动态，但聚合只能形成动态域内部的来源证据链，不改变原子动态的场景归属。
5. 基础事实池或索引登记不构成业务世界树层级或动态聚合层级。

## 4. 写入、读取与验证

DATA之外的动态应用负责相邻变化判断、时间段归组与递归组合，再向唯一动态结构服务提交规范化请求。数据服务只校验来源、组成、时间、关系和既有可靠性/清理约束并发布、读取结构；不得在DATA内判断概念、业务许可、目标满足或动作成功。新结构的精确物理合同须另行闭合，不把本规范当作已有ABI。

有效实例动态必须完整回读主体、所属场景、实际时间及变化内容：原子核对同一属性的真实前后状态，组合核对直接成员及来源闭包，保留各成员主体和端点。动作证据按4220另核。内部结构缺口属于内部逻辑错误，不得包装成普通动态缺失。

动态可以触发或定位某个特征需要重新检查，但消费者必须先冻结非零事实截止，重新读取目标涉及的当前特征值和目标状态合同后比较。动态前值、后值及其状态记录只能作为历史、来源、变化解释和审计证据，不能直接形成自检、需求满足或目标差距结论。

## 5. 生命周期与当前性

动态事实创建后不可通过覆写改变历史；调整组合内容或动作解释须形成合法新结果。实例保留、有限TTL、数量上限、墓碑与历史已清理按0050/1160/4230；索引裁剪和事实清理分账，不承诺旧载荷永久可读。来源/组合闭包必须受保护，概念抽象不隐式删除来源动态。

## 6. 子规范路由

| 子规范 | 负责内容 |
| --- | --- |
| 4210、4202 | 动态字段、变化组合及场景动态实例树的实际组织边界 |
| 4220 | 状态迁移账、动作动态账和因果账本边界 |
| 4320 | 动态到目标状态的因果投影 |
| 7190 | 独立动态概念字段、关键条件抽象、形成、识别与治理 |

## 修订记录

| 日期 | 版本 | 修订内容 |
| --- | --- | --- |
| 20260909 | v0.8 | 允许场景动态根下实际实例组织树，区别组织父与组合来源；旧全面禁止历史树条款退出。 |
| 20260908 | v0.7 | 原子端点与子动态组合分支承载；实例归场景、概念归动态根，细则转引4210/7190，纠正永久历史保证并保留现行可靠性。 |
| 20260907 | v0.6 | 当前重检改为存在采用到固定准确F的读取链；动态本体与记录边界不变。 |
| 20260907 | v0.5 | 删除实例当前属性槽作为比较前置的描述，改为按独立特征节点取得准确当前观察值；动态仍只提供变化记录。 |
| 20260828 | v0.4 | 冻结动态不承载特征当前值；主体、场景、目标和来源由独立关系解释，DATA 只校验结构；动态触发比较后必须重新读取当前特征值。 |
| 20260722 | v0.3 | 把动态事实关系的专用类型、角色和基础基数唯一交由 4210 展开，并由 4220 收紧动作致变动态。 |
| 20260722 | v0.2 | 动态发生时间和必要非拓扑原始值进入动态域类型化记录，全部对象联系继续由正式关系承载。 |
| 20260720 | v0.1 | 确立动态根名、状态端点、来源动作、零散事实和域内聚合规则，并由动态节点及具名关系承载。 |
