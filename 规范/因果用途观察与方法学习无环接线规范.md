# 因果用途观察与方法学习无环接线规范

编号：CAUSAL-USE-001

日期：2026-07-16

状态：JY-376 三项 S0 后修订 / #287、DQ-179 为当前唯一已登记后继代码切片 / 节点 15、关系 18、权威用途观察、统计、学习、晋级、代际、生产接线和恢复仍未获代码许可

## 1. 定位

本规范固定从非权威因果概率候选、正式任务结果和需求结算到未来方法学习链的单向权力关系。JY-371、JY-373、JY-375 已完成结构承载、复合因果身份和方法学习三项当前事实复核；它们证明若干底层材料可复用，同时证明旧节点 15、关系槽位、概念签名、学习算法和方法代际草案均不足以直接实施。

现行路线为：

```text
非权威因果概率候选
+ 动作前用途来源冻结纯值材料
+ 正式任务结果与需求结算
-> #287 纯值因果完整结构键
-> 后继权威用途观察结构合同
-> 从权威观察重建的非权威分栏统计
-> 完整方法执行快照与确定差异候选
-> 独立晋级授权
-> 方法业务服务发布不可变新代际
-> 后续选择快照重新召回、授权、选择和执行
```

本链不证明稳定因果，不把概率等同于效用，不把统计或候选等同于方法事实，不允许本轮结果反写本轮选择，不建立第二事实账或第二恢复链。

## 2. 三项 S0 后正式事实

| 事实来源 | 已确认事实 | 不得扩大为 |
| --- | --- | --- |
| #282 / #286 | 已有执行前用途来源冻结和结构化结果效用纯值合同；已有三种任务结果和单一正式 `已满足` 结算 | 生产运行期已形成冻结、权威用途已记录、完整动态 / 因果来源恢复 |
| JY-371 / #283 | 完整请求比较、物理索引、关系审计和结构会话原语局部可复用 | 节点 15、双索引、阶段关系、容量、专用数据操作和恢复合同已存在 |
| JY-373 / #284 | 当前因果对象只有原因动态键、结果动态键和因果规则版本三元组；单把动态键已有七字段比较和召回哈希 | 独立复合身份、A/B 比较、无损活动概念签名或通用原子当前视图已存在 |
| JY-375 / #285 | 方法条件 / 结果可读，单动作可完整比较，任务结果和结算可分栏读取 | 完整执行快照、学习算法、晋级授权、父代、代际号、N / N+1 或生产学习链已存在 |

## 3. 概念与权力分层

| 概念 | 性质 | 当前 / 未来承载 | 唯一形成或写入方 | 禁止替代 |
| --- | --- | --- | --- | --- |
| 因果发生概率候选 | 非权威值式候选 | `因果概率候选` 的机会组、正负计数和 Q10000 | 因果模式业务服务 | 任务达成、需求效用、稳定因果 |
| 因果完整结构键 | 纯值复合身份 | 原因动态键、结果动态键、因果规则版本和格式版本 | #287 因果模式算法 | 哈希、概念句柄、名称、冻结键 |
| 结构化结果效用 | 值式业务裁决 | 任务达成、需求结算、观察阶段和历史概率分栏 | 用途事件业务服务 | 单一 I64、普通 bool、Q10000 |
| 权威用途观察 | 未来长期证据账 | 结构待后继设计；当前没有节点、关系、索引或恢复合同 | 未来用途观察业务服务经专用数据操作 | 日志、缓存、线程去重、SQL、纯值事件 |
| 统计投影 | 非权威、可清空重建 | 未来完整复合身份键控分栏投影 | 未来统计投影服务 | 权威观察、稳定因果、方法事实 |
| 学习候选 | 非权威、可重建 | 完整方法版本、确定观察组和确定差异 DTO | 未来学习候选服务 | 已登记方法、授权、新选择 |
| 晋级授权 | 未来权威业务裁决 | 版本化任务授权、来源观察和候选完整身份 | 未来独立晋级服务 | 候选自证、统计线程自动结论 |
| 新代际方法 | 未来权威高级业务事实 | 既有方法节点体系的不可变新身份与正式代际结构 | 方法业务服务经专用数据操作 | 原地覆盖来源方法、任务序号充当代际 |

## 4. 因果完整结构键

### 4.1 唯一字段

第一版 `因果完整结构键` 必须完整保留：

```text
格式版本。
原因动态键：目标完整句柄、前值、后值、来源动作完整句柄、是否有来源动作、动态规则版本、可选二次角色值。
结果动态键：同样七字段。
因果规则版本。
```

原因与结果是有序角色。交换两把动态键形成不同身份；不得排序后抹平角色。两把动态键当前规则版本数值相同，也必须分别保留，不能由因果版本推导或省略。

### 4.2 版本合同

格式版本 1 只接受明确版本兼容表：

```text
身份格式版本 = 1
原因动态键规则版本 = 当前动态模式规则版本
结果动态键规则版本 = 当前动态模式规则版本
因果规则版本 = 当前因果模式规则版本
```

兼容表之外返回“不支持”，不得用三个版本当前数值相等推断未来兼容。后继版本必须新增显式兼容行和迁移计划；不得原地放宽版本判断。

### 4.3 比较与哈希

```text
A/B 相同：格式版本、原因七字段、结果七字段和因果规则版本全部相同。
A/B 不同：上述任一字段不同。
稳定哈希：按格式、原因角色、结果角色、因果版本固定顺序混入，只用于候选桶召回。
哈希命中：仍须执行 A/B 全字段比较。
哈希碰撞：两个不同完整键保持不同事实，不得合并、覆盖或选首项。
```

完整比较和哈希只接受局部完整、原因与结果不同且版本兼容的身份。任一输入无效 / 不支持时，完整比较返回假；哈希返回空值，不得把两个默认无效对象归为同一身份，也不得给无效对象分配可进入候选桶的默认哈希。

名称、类型、概念句柄、向量首项、时间戳、节点编号、冻结键或日志均不得参与最终身份裁决。

### 4.4 #287 当前许可

#287 只允许：

```text
在现有因果材料模块声明纯值因果完整结构键及格式版本。
在现有因果算法模块形成、复核、完整比较和计算召回哈希。
让用途事件的历史概率分量持有该完整身份，不再散列复制三元组字段。
新增独立 Debug 自检模块和阶段 860 最小登记。
```

身份形成结果必须默认初始化为明确入口拒绝态，并只允许四类组合：成功时“已形成 + 无拒绝原因 + 有合法身份”；材料错误时“入口拒绝 + 候选不完整 / 原因结果相同 + 无身份”；版本错误时“不支持 + 版本不兼容 + 无身份”；算法异常时“内部不一致 + 算法不变量破坏 + 无身份”。

#287 禁止节点、主信息、关系、索引、仓库、许可、数据操作、线程、运行期装配、持久化、概念映射、统计、学习、晋级或方法写入。阶段 860 是 JY-376 对 #287 的新授权，不表示旧用途观察阶段草案复活。

## 5. 正式任务结果与用途效用

第一版结果口径继续固定：

| 正式结果 | 任务达成 | 需求结算 | 观察阶段 | 后继边界 |
| --- | --- | --- | --- | --- |
| 未达成已转待重筹办 | 未达成 | 未形成 | 尝试未达成 | 可形成尝试材料；不得伪造最终需求负效用 |
| 任务已完成待需求结算 | 已达成 | 待结算 | 待结算 | 保留同一回合材料；不得进入统计或学习 |
| 已完成并结算 | 已达成 | 已满足 | 最终已结算 | 未来可进入唯一权威观察发布 |

Q10000、历史机会组、任务达成、需求结算和观察阶段始终分栏。任何服务不得把它们压成单一好坏分数、置信度、可靠性或自动晋级阈值。

## 6. 权威用途观察后继门禁

### 6.1 语义身份

未来用途观察的语义唯一身份固定为：

```text
完整任务句柄 + 任务序号
```

`冻结键与观察主键` 的旧字段名只保留为 #282 调用期来源关联材料；它不再被预设为权威观察主键、物理索引键或事实身份。物理索引可以使用版本化命名域和哈希加速，但命中后必须比较完整语义身份与完整观察内容。

### 6.2 节点与关系暂不分配

`节点类型::用途观察记录 = 15` 和 `关系类型::用途观察证据角色 = 18` 当前均未锁定。后继设计只有同时闭合以下项目后，才可通过新计划分配枚举：

```text
用途观察最小权威字段和明确排除字段。
完整任务回合身份与完整内容 A/B 比较。
物理索引命名域、碰撞分类和双重唯一。
每一关系角色的端点类型、顺序号、基数和审计语义。
固定单观察元素 / 字节上限、总预算和写前容量拒绝。
待结算到最终已结算的同账阶段换代与历史读回。
专用数据操作、不透明会话、精确撤销、确认和最后发布。
同一运行期宿主、同一恢复格式和同一恢复发布链。
```

不得把完整嵌套 DTO、回执、结算解释或历史机会证据组直接序列化进通用主信息 I64 槽；不得为了抢占编号先锁定节点或关系，再把字段、容量和恢复留给执行角色发明。

### 6.3 幂等与冲突

未来合同至少保持：

```text
同完整任务句柄 + 同任务序号 + 同完整内容 -> 幂等读回。
同完整任务句柄 + 同任务序号 + 异完整内容 -> 幂等冲突。
同语义身份出现多个已发布记录 -> 内部不一致并追根因。
任务序号倒退或与完整冻结请求不一致 -> 写前逻辑返回。
尝试未达成和最终已结算为终态；待结算只允许在同一账项内换代为最终已结算。
```

## 7. 统计与概念映射

后继统计只能从已发布权威用途观察重建，并保持：

```text
尝试未达成与最终已结算分栏；待结算不统计。
完整因果结构键是主键；Q10000、任务达成和需求结算分别投影。
清空、重建或丢弃统计不改变观察、任务、需求、因果或方法事实。
同一输入和规则版本得到确定排序、确定计数和相同输出。
```

当前活动概念签名对完整因果结构键有损。第一版概念映射只可形成零 / 一 / 多结果的非权威可选标注：零项返回未命中，一项也不得替代完整键，多项返回材料不足；不得按名称、类型、哈希或向量首项猜选。

## 8. 方法执行快照与确定差异

学习前必须在动作执行前冻结来源方法完整内容：

```text
来源方法完整身份。
独立方法内容版本。
确定排序的全部条件规格。
确定排序的全部结果规格。
完整动作入口规格与输入 / 输出场景。
来源生命周期和选择材料只作当时快照，不替代内容版本或父代。
```

当前条件 / 结果规格的 `来源版本` 只描述其来源材料，不是方法内容版本；任务方法选择中的来源生命周期只证明选择时活跃，不是方法代际。

来源观察组必须按完整观察身份确定排序、去重并逐项复核当前性。差异规则必须显式输出：

```text
新增：新规格存在、来源版本不存在。
保留：正式角色和完整规格相同。
移除：来源规格存在、新规格明确不存在。
替换：同一正式角色槽存在唯一旧规格和唯一新规格，且完整内容不同。
未知：材料不足，不能确定新增 / 移除 / 替换。
冲突：同一角色槽多映射、同身份异内容或正式材料矛盾。
```

在正式角色槽、方法内容版本和完整观察组尚未形成前，默认返回材料不足，不自动生成差异。Q10000、单次任务达成、单一分数、哈希或任务序号均不得直接裁决新增、移除、替换或晋级。

## 9. 晋级与方法代际

晋级必须独立复核：来源观察仍有效、统计可重建、来源方法内容版本当前、候选完整、任务明确授权、晋级规则版本受支持、同义未重复消费、异义不冲突。

通过后也只能向方法业务服务提交类型化新代际请求。统计、线程、因果模式、学习候选或晋级服务不得直接操作仓库、原始令牌、结构会话或方法关系。

新代际必须使用不可变新方法身份；来源方法继续保留审计，不得原地覆盖。传统父方法、前置方法、后续方法只保留兼容组织语义，不能充当正式父代。现有任务序号只标识同一任务回合，不能表达方法代际 N / N+1。后继可见性必须通过独立方法登记版本或选择快照边界裁决；合同形成前不得登记代际代码计划。

## 10. 唯一运行期与恢复

未来生产顺序固定为：

```text
实际选择因果候选
-> 动作前冻结完整用途来源与完整方法内容版本
-> 执行动作
-> 正式任务结果与需求结算收口
-> 发布或换代同一权威用途观察
-> 重建非权威统计
-> 形成学习候选
-> 独立晋级授权
-> 方法服务发布新代际
-> 后续选择快照重新召回
```

第 N 回合结果不得改变第 N 回合的冻结、选择、方法或动作。这里的 N 只是叙述顺序，不映射当前任务序号字段。

未来权威用途观察、晋级授权和方法内容版本 / 代际若获正式结构合同，只能原地并入现有四仓库、唯一运行期上下文、唯一宿主和既有恢复链。统计、召回索引、学习候选和概念标注均为可清空重建材料。恢复默认不自动晋级、不自动发布新方法、不自动执行动作。

## 11. 非成功返回二分

| 场景 | 分类 | 收口 |
| --- | --- | --- |
| 请求、完整键、任务回合或方法快照材料缺失 | 逻辑内返回 | 零结构变化，不补默认值 |
| 格式或规则版本不在显式兼容表 | 逻辑内不支持 | 不猜测兼容，不降级读取 |
| 哈希命中但完整比较不同 | 逻辑内未命中 / 冲突 | 不合并，不选首项 |
| 无效 / 不支持身份参与比较或哈希 | 逻辑内无效材料 | 比较为假，哈希为空 |
| 已接受的正式 DTO 内字段互相矛盾 | 追根因解决 | 停止后继，不用日志 / 缓存修补 |
| 用途算法已通过候选深度自洽后，身份形成仍返回任一非成功 | 追根因解决 | 统一内部不一致，停止后继 |
| 已发布同一观察身份出现多个记录 | 追根因解决 | 不扫描选优，不静默删除 |
| 候选写后读回、确认、发布或恢复不符合合同 | 追根因解决 | 精确撤销；撤销失败继续按内部不一致收口 |

## 12. 当前计划路由

```text
#287 / DQ-179 / CAUSAL-IDENTITY-S1：依赖已满足，纯值代码切片，阶段 860。
权威观察结构：等待 #287 正式接口后重新设计；节点 15 / 关系 18 未分配。
统计与概念映射：等待权威观察发布接口；概念标注不阻断完整身份统计。
方法版本、学习、晋级、代际、生产接线和恢复：继续等待各自上游结构合同，不提前登记代码许可。
```

## 13. 完成声明边界

#287 完成后只可声明纯值因果完整结构键、显式版本准入、A/B 全字段比较、召回哈希和用途概率分量接线已实现并验证。不得声明：

```text
节点 15 或关系 18 已建立
权威用途观察已记录
统计或概念映射已实现
方法已学习或晋级
方法内容版本或代际已建立
任务序号已经成为 N / N+1
生产运行期或恢复已接线
稳定因果、自动行动或旧能力迁移完成
```
