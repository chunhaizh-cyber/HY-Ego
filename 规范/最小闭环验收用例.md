# 最小闭环验收用例导航

编号：014
日期：2026-07-06
状态：导航文件 / 非权威旧入口已收束

## 1. 当前权威文件

当前最小闭环验收用例的权威文件为：

```text
规范/验收/最小闭环验收用例.md
```

本文件只保留旧链接导航，不再承载验收标准。

## 2. 旧版口径废止

旧版文件曾包含旧目标值、旧状态材料变化值和来源方法强回查等口径。

这些旧表述已经废止，不得作为当前第一轮验收依据。

当前第一轮 DV-1 口径为：

```text
需求目标是目标状态，不是 I64 对外语义。
目标状态的第一轮状态值材料为 10。
满足规则固定为：当前特征状态材料 >= 目标状态值。
方法执行权限来自任务承接和任务方法关系。
动态必须记录发生时间戳、被改变目标、改变前值和改变后值。
来源方法 / 动作可以为空。
因果引用只以来源动态作为准入证据，不持久化来源动态关系。
```

## 3. 禁止误用

后续执行者不得从本文件读取验收目标、用例编号或通过标准。

若计划、实施记录或项目记忆需要引用最小闭环验收用例，必须引用：

```text
规范/验收/最小闭环验收用例.md
```
