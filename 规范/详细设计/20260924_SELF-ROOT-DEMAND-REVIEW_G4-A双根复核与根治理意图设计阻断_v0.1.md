# SELF-ROOT-DEMAND-REVIEW G4-A双根复核与根治理意图设计阻断

版本：v0.2

日期：2026-09-24

状态：设计阻断；不是代码实施计划，不授权施工

对应目标：`GOAL-SELF-GOVERNANCE-CLOSURE`

代码事实基线：`1e573f6a33aa70a7f11c2b3c9a8c904aed9eb917`

## 1. 结论

G2-A已经形成纯读双根材料、fresh正式上下文provider及普通应用唯一生产所有权，但没有把provider注入self，也没有实现G4-A双根需求复核或根治理意图sink。

现行正式规范足以固定G4-A的业务边界、大体调用链和每轮完整复核双根的触发范围，却不足以唯一确定双根比较的正式前置、可施工公开DTO、比较绑定生产来源、预算映射、失败映射和sink物理合同。因此本轮不得建立或登记G4-A代码计划。计划索引保持空，直到第5节阻断全部解除。

本结论不否定G4-A目标，也不要求重新讨论双根模型；它只说明当前缺少把既定逻辑落入代码的精确物理合同。

## 2. 已冻结的业务语义

依据5170 v0.3、6120 v0.16、8100 v1.13和8200 v2.0，可以直接固定：

1. 每轮以同一非零`G0`分别读取安全根和服务根，双根身份、目标、当前值及裁决互不合并。
2. 根当前值来自唯一self对对应FT的当前采用准确F，不取最后状态、动态、消息载荷或缓存。
3. 每根结果必须区分`当前满足`、`正差距`、`方向相反`、`不可比较`、`材料不足`、`当前性漂移`和`内部不一致`；技术非成功不得伪装成业务未满足。
4. 固定目标为`I64_MAX`，合法当前值域为`0..I64_MAX`；差值仅在正式比较成立后作为本轮临时材料使用，不持久化、不合并成综合分数。
5. 只有`正差距`或新到达的合法父任务材料允许建立、唤醒或复用对应根治理任务；`当前满足`只关闭本轮展开，`方向相反`和技术非成功不得发任务承接意图。
6. 根需求长期存在；G4-A不新建需求、不写根值、不写任务状态、不建立服务账，也不把运行消息当正式事实。
7. self到manager只能发布不可变治理意图，manager必须独立读取正式需求和任务事实；消息投递不证明任务已经建立、复用或推进。
8. 8120 v0.17要求阶段20停门后先使支持线程、任务管理线程、工作线程池、外设线程等生产者/消费者就绪，再由独立业务步骤开放治理门。G4-A provider或进程内接口单独完成不能解除该门禁。

## 3. 当前可复用的代码能力

| 能力 | 当前物理入口 | 可复用边界 |
| --- | --- | --- |
| fresh正式上下文 | `自我线程正式上下文提供者::读取正式上下文(...)` | 返回同截止世界、当前场景、self和双根锚点；零业务写 |
| 双根材料纯读 | `需求类数据服务::读取本能根材料(...) const` | 按角色和锁定实际F读取D/L/目标合同/目标I64及闭包 |
| 当前采用读取 | `存在类数据服务::读取当前采用(...) const` | 可按self、FT和G0取得当前采用F |
| 准确F读取 | `特征类数据服务::读取有界准确特征事实(...) const` | 可在同G0读取F、FT、I64内容及来源闭包 |
| 当前目标判断K读取 | `特征类数据服务::读取当前I64比较绑定(...) const` | 仅在生产已经存在唯一当前K时可用；当前普通应用未建立安全/服务K |
| 纯I64比较 | `有序I64特征比较提供者::比较(...) const noexcept` | 只执行已给定K，不提供K来源，不发布事实 |
| 线程消费端口骨架 | `自我线程根需求复核端口::复核双根当前需求(...)` | 只有接口；现行结果表达和预算不满足完整G4-A合同 |

推荐依赖方向仍为：业务应用层provider实现线程端口，内部组合需求、存在、特征公开服务和纯比较器；`线程_自我.h`只保留值式DTO与抽象端口，不包含领域服务头或向DATA层下钻。

## 4. 不能继续推导的合同

### 4.1 根复核业务结果表达不完整

现行`自我线程根当前满足状态`只有`满足/未满足`。它不能区分规范要求的`正差距`和`方向相反`，会让后继sink无法判断是否允许形成根治理任务意图。

需要冻结：新的强类型业务分支、每根成功投影应携带的当前值/目标值/比较K/排序或差值证据，以及业务分支与技术状态的分账。不得继续用布尔或二态枚举承载。

### 4.2 双根比较的RC前置与当前物理路线冲突

8100和6120要求按“已登记二次特征概念”即时计算双根差值。按7170、7180的正式定义，这里的二次特征概念是具有独立概念身份、完整`D + FC + EC`以及必要K引用、经概念owner完整发布并可读回的RC。保存一份标量派生定义不自动成为RC；仅建立或读取唯一当前K同样不自动成为RC。原v0.1把“已保存二次特征/标量派生定义”和“唯一当前K + 本次局部二次图”列为两条可以直接二选一的路线，因此不完整，应予撤回。

4151允许不以已保存派生定义或RC为前置，按已成立FT、用途和K组织本次临时计算；但该条是通用调用期基础计算许可，不能自行覆盖8100、6120对self双根比较提出的更强专属合同。8121又明确禁止阶段21创建二次概念、二次实例，因此也不能把缺失RC静默塞回现有本能根初始化步骤。

当前只剩一个需要用户裁决的最小机器语义问题：是否正式取消8100、6120对双根比较的RC前置，改为“安全值FT、服务值FT各自具备唯一当前目标判断K；每轮以正式当前准确F、根目标合同中的`I64_MAX`和该K组织本次局部图，临时形成差值并用后丢弃”。

- 若确认取消RC前置：先按正式审查和批准流程修订8100、6120及必要转引，使上述局部计算成为self双根比较的正式专属合同；随后再冻结K的初始化、owner、幂等、恢复、唯一当前读回以及目标合同与K的定位关系。
- 若不取消RC前置：必须另行设计并初始化安全根、服务根各自所需RC，冻结其完整定义、owner、发布/恢复/唯一读回，并使两个根目标合同能够正式定位对应RC；既不能以保存标量派生定义替代RC，也不能只凭K声称满足前置。

在用户作出上述裁决前，provider不能临时创建K或RC，不能用硬编码`M-current`绕过正式来源，也不能把4151的基础计算许可解释为已经修改了self专属合同。

### 4.3 读取预算不能机械映射

现行根复核预算只有`每根最大当前采用候选数量/最大目标合同材料数量/最大关系材料数量`。准确F读取和K读取需要独立的节点、关系、值和总材料预算，K读取还需要最大扫描候选数量。

需要冻结真实消费字段及逐调用映射。禁止把一个总量复制到多个维度、用固定常量补齐或在provider内部静默放大预算。

### 4.4 状态映射不完整

根材料、当前采用、准确F、K读取和比较器分别可能返回未找到、已退出、数量不足、历史不可用、漂移、未注册、注册不唯一、算法未实现、资源失败和内部不一致。现行线程外部状态没有`材料不足`或`预算不足`的独立表达，也没有完整映射矩阵。

需要冻结每个下层状态到业务分支或技术状态的唯一映射，并固定非成功为空成功载荷、零业务写。不得把未注册/材料不足自动解释为`未满足`。

### 4.5 触发范围已由8100裁决

8100已经规定“后续每轮在同一G0分别读取两个根D、目标合同和当前值”，并规定状态、动态及其它材料触发只授权fresh重判与裁决。由此可以唯一裁决：`双根/安全根/服务根`只表示本轮唤醒和定位原因，不裁剪复核范围；每次合法复核仍在同一非零G0完整读取、比较并返回安全根与服务根两份结果。单根定位必须与锁定锚点中的对应根一致，但不会把另一根从预算、成功谓词、消息去重或sink输入中删除。

因此原v0.1第4.5节阻断关闭，不再请求用户选择“单根只返回一根”。后续DTO、预算和状态矩阵一律按每轮完整双根复核设计。

### 4.6 根治理意图sink及任务owner入口缺失

当前没有任务管理线程/任务工作线程正式类，也没有按根列表项查询`0..1`当前同目标任务、建立/唤醒/复用任务、独立读回Vt并开始筹办的公开应用入口。现有`任务类数据服务`只有基础记录、虚拟存在、方法与实例结构操作，不能承担8100/8200规定的manager业务职责；旧通用运行消息协议也不是现行根治理sink。

需要冻结：

- sink的唯一owner和物理位置；
- 不可变意图DTO中的具体D、列表项、根角色、比较证据定位、G0、消息身份、原请求身份及业务幂等身份；
- `当前满足/正差距/方向相反`各自是否调用sink；
- manager对同目标当前T的查询、零条建立、一条复用/唤醒、多条冲突；
- 投递成功、重复、已有当前任务、发布未知、漂移、资源失败和内部错误的结果映射；
- 独立读回、恢复和不得重开已完成/失败/取消任务的合同。

在这些内容冻结前，不能声明一个只返回成功的进程内回调为sink，也不能让self直接调用任务数据服务建立任务。

## 5. 阻断解除顺序

1. 先由用户裁决4.2的唯一问题：是否正式取消双根比较的RC前置，改为每FT唯一当前目标判断K加本轮局部图；若不取消，则转入双根RC的完整定义、初始化和目标合同定位设计。任何正式规范修改仍须另行取得确认并完成审查、批准和发布。
2. 路线正式冻结后，以真实K及必要RC、读取入口反推4.1、4.3、4.4，形成完整根复核provider DTO、签名、预算和状态矩阵；4.5已裁决为每轮完整双根复核，不再作为阻断。
3. 以`正差距`成功载荷反推4.6的根治理意图DTO、manager owner、任务应用入口、幂等和恢复合同。
4. 只在上述合同全部冻结后，建立最早可独立施工的代码计划；若K生产初始化是先决提供者，优先只登记该提供者计划，G4-A消费者留作紧邻后继。
5. G4-A provider及sink合同完成仍不等于治理门可开。G2-B必须另行证明8120要求的全部生产者/消费者就绪，并由独立开门步骤消费真实provider和真实sink。

## 6. 当前完成声明边界

本文件证明：已按HEAD `1e573f6a3`核对G2-A结果、正式规范和当前接口；纠正了原A/B二选一对RC含义的误判，将用户裁决收敛为是否取消双根比较RC前置，并依据8100关闭了单根触发范围阻断；其余不可裁决合同继续阻止伪可执行计划进入索引。

本文件不证明G4-A provider、比较K、sink、任务管理线程、消息循环或治理门已经实现，也不授权修改规范、代码、流程图、工程、共享知识或施工/验证记录。

## 7. 修订记录

| 日期 | 版本 | 修订内容 |
| --- | --- | --- |
| 2026-09-24 | v0.2 | 明确7170/7180中的“已登记二次特征概念”指RC；撤回不完整的标量派生定义/K二选一，将最小用户裁决收敛为是否取消RC前置；依据8100裁决单根触发只作定位、每轮仍完整复核双根。 |
| 2026-09-24 | v0.1 | 核对G2-A结果并登记G4-A结果、比较、预算、状态、触发和sink合同阻断。 |
