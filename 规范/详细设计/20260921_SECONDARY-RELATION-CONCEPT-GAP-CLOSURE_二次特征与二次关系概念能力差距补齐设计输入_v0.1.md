# 二次特征与二次关系概念能力差距补齐设计输入

身份：`SECONDARY-RELATION-CONCEPT-GAP-CLOSURE`

版本：v0.4

日期：2026-09-22

状态：P0已裁决；路线设计输入，不产生代码实施许可

当前代码基线：`b7b104e08deb8046dcd341fe5a68014aa7a75578`；该提交只将旧RC-P1计划资格降为`待激活`，相关生产源码仍未改变

## 1. 目标与结论

本设计输入只比较现行二次特征、二次关系概念规范与当前生产源码，识别可复用能力和真实缺口，并给出最短补齐顺序。它不重新设计已经实现的调用期二次特征计算，也不恢复已经物理删除的`数据服务.二次特征类`。

结论如下：

1. 调用期有序I64二次特征计算已经实现，应直接复用。
2. 当前生产源码没有4242定义的任何`二次关系*`合同、结构CRUD、RC/FC/EC求值或形成应用。
3. 最早可独立闭合的代码叶不是“再建一个二次特征类”，而是“RC结构所有者闭环”：合同、外部结构初始化、唯一概念owner扩展、第5至6节全部结构操作、恢复及完整读回。
4. 用户已裁决删除旧接口且不保留兼容层；提交`18239d93cb15626c7792689f34b22c7b9ae9424d`已完成旧生产DTO、构造、CRUD和无消费者旧应用的物理删除。4241/4242、7170/7180和7191现行修订统一使用`纯概念v2 + 特征概念出生使用 + 存在概念两组`、不可变类别、顶层空父和同类别多父DAG。RC结构P1不再等待旧接口删除，但必须基于当前HEAD重新冻结并登记计划。
5. S0确认4242/7191共同使用的`概念树规则身份`在当前生产源码中零定义。用户已限定裁决：按旧接口删除前提交`aa465854b210d9e0af01c64b314ea8fdf2fbd655`中的纯值形状，将它恢复到`合同.相关概念添加参与.h`，作为RC-P1本次第三种公共纯值增补；只复用该值类型，不恢复其历史邻接服务、CRUD、根、定义联合、构造链或兼容入口。

## 2. 当前事实

### 2.1 已实现并必须复用

| 能力 | 当前物理位置 | 当前结论 | 本目标裁决 |
| --- | --- | --- | --- |
| 调用期二次特征DAG计算 | `海中鱼巣/业务/计算.二次特征.h/.cpp` | 支持准确F、已保存派生定义输出、本次局部输出；具有真实阶次、有限DAG、G/H、预算、固定K/当前K互证和完整结果 | 原样复用，不新增持久二次实例，不恢复旧DATA门面 |
| 有序I64差异算法 | `海中鱼巣/领域/算法.有序I64特征比较.h` | 已实现排序、幅度、差异及溢出拒绝 | RC求值唯一数值算法提供者；不复制算法 |
| 准确F、派生定义和K读取 | `海中鱼巣/领域/数据服务.特征类.h/.cpp` | 已有有界准确F、标量派生定义、固定I64比较绑定和当前合法绑定读取 | 作为RC求值的正式特征来源和K来源 |
| 状态使用绑定窄读 | `海中鱼巣/领域/合同.场景状态使用绑定.h`；实现为`场景类数据服务` | 已有当前/历史B读取，场景类实现窄provider | B来源只能走该provider，不向RC暴露完整场景服务 |
| 状态历史内容 | `海中鱼巣/领域/数据服务.状态类.h` | 已有`读取状态历史内容`及完整S内容 | B→S后读取准确内容；禁止裸S或裸I64替代B |
| 存在身份及当前采用 | `海中鱼巣/领域/数据服务.存在类.h` | 已有存在身份历史见证和当前采用读取 | F来源核验当前采用，F/B两类都核验E身份 |
| 唯一概念owner和概念治理基础 | `海中鱼巣/领域/数据服务.概念树类.h/.cpp` | 已有纯概念、FC、EC两组定义、父组、生命周期、来源/引用和读回基础 | RC结构只能扩展此服务和同一owner；不得建立第二概念仓 |

代码基线blob：

- `数据服务.概念树类.h`：`d4c687310afc130751ca69b39dbca52a19343e07`
- `数据服务.概念树类.cpp`：`6df32cbeab0a40bd1871efb002248a21fd3dece6`
- `装配.普通应用.h`：`0e565ab1f19d24caaa1452030a466b0528f7a295`
- `装配.普通应用.cpp`：`cb1f3e4e99d50b5d9fba15921974f0699fcca80a`
- `计算.二次特征.h`：`add2bb708b8d796ebeabb73652d1a5ef6ed85827`
- `计算.二次特征.cpp`：`34c51100112de94297de6f53bf662b5c047d94d5`

### 2.2 当前生产装配事实

`装配.普通应用.cpp`当前按下列顺序建立概念服务：

```text
纯概念结构_v2登记
-> 特征概念出生使用结构登记
-> 存在概念两组结构登记
-> 构造唯一概念树类数据服务
-> 本能先天特征概念初始化
-> 特征值域比较、原子I64特征出生及特征概念应用
```

构造函数实际接受：

```cpp
概念树类数据服务(const L1事实基座服务&,
    const 特征类数据服务&, const 存在类数据服务&,
    const 特征值类数据服务&, const 场景类数据服务&,
    L1所有者范围写端口&&,
    const 纯概念结构交付_v2&,
    const 特征概念出生使用结构交付&,
    const 存在概念两组结构交付_v1&);
```

该生产形态由2026-09-17的存在概念两组提供者重建切片引入，并已由4242 v0.2在2026-09-21正式吸收为RC集成构造基线。

### 2.3 当前不存在的能力

当前受跟踪`.h/.cpp`源码中，4242定义的`二次关系定义`、`二次关系结构交付`、`二次关系数据状态`、`二次关系概念只读提供者`、`二次关系求值应用服务`和`二次关系概念应用服务`均为零命中。旧`领域/数据服务.二次特征类`没有受跟踪源码，只存在可忽略的历史构建产物；禁止恢复。

## 3. 与现行规范的差距矩阵

| 差距 | 4242要求 | 当前事实 | 补齐责任 |
| --- | --- | --- | --- |
| 概念公共纯值类型 | 4242/7191共同使用`概念树规则身份`、`概念树读取头`和`概念树写入头` | 旧接口删除后当前源码三者均零定义；身份、类别、生命周期和组织类型已在`合同.相关概念添加参与.h` | 只在该现行公共合同恢复三种纯值类型：规则身份=`稳定编码`强类型包装，读取头=`合同版本,Gread,H`，写入头=`合同版本,期望事实代次,L1所有者范围写入幂等身份`；不恢复旧CRUD、旧根、旧定义联合或旧构造链 |
| RC合同 | 原子/AND、规范形、预算、见证、状态、全部请求/结果和窄provider | 完全缺失 | 新建`领域/合同.二次关系概念.h/.cpp` |
| RC结构初始化 | 构造前借同一概念owner端口完成固定53项单写集、读回及交付 | 完全缺失 | 新建`业务/初始化.二次关系概念结构.h/.cpp` |
| RC结构CRUD | 规范化、查重、读取、建立、原键收敛、来源、用途、图、父组、生命周期、释放和退出 | 完全缺失 | 扩展`领域/数据服务.概念树类.h/.cpp`，不得建立独立DATA类 |
| FC/EC专属约束读适配 | 有界完整读取并报告真实用量 | 现有概念服务有底层事实，但没有4242专属适配和计量结果 | 与RC结构闭环同owner实现 |
| RC/FC/EC调用期求值 | F/B来源、B→C/E/S、FC域、EC模板、固定K和当前K互证、全量非短路判断 | 完全缺失；现有二次特征计算不负责B、FC或EC语义 | 新建`业务/计算.二次关系求值.h/.cpp`；复用现有provider，不改写二次特征DAG核心 |
| RC形成编排 | 候选求值→查重→建立/复用→完整读回→来源阶段→原键恢复 | 完全缺失 | 新建`业务/应用服务.二次关系概念类.h/.cpp` |
| 生产装配 | 初始化交付、唯一概念服务扩展、求值和形成服务生命周期 | 未接线 | 在各代码叶完成后按同一概念owner顺序扩展`装配.普通应用.h/.cpp` |
| 工程与验证 | `.h/.cpp`工程登记、Debug/Release、公开入口有界黑盒、恢复与故障分账 | 缺失 | 更新根工程/filters并建立专属生产外验证工程；不得恢复`.ixx` |
| 代码知识 | 发布后用途卡和聚合当前性 | 现有概念知识仍绑定旧L2模块时代基线，不能裁决当前实现 | 代码施工记录形成函数级知识变更清单；由知识维护者消费正式代码提交回写 |

## 4. 复用、扩展和新建裁决

### 4.1 直接复用

- `有序I64特征比较提供者::比较`：RC的唯一实际数值运算入口。
- `特征类数据服务`的有界准确F、正式FT准确值核验、固定K读取和当前K读取。
- `状态使用绑定只读提供者`的历史B读取。
- `状态类数据服务::读取状态历史内容`。
- `存在类数据服务`的身份历史见证和当前采用读取。
- 概念服务已有同owner首次材料、L1写集、生命周期、父组和完整读回实现模式；只复用机制，不把RC塞入旧定义variant。

### 4.2 窄扩展

- `合同.相关概念添加参与.h`：只补入4242/7191共同使用的规则身份、读取头和写入头，不迁移、替换或清理其它当前公共类型。规则身份保持独立强类型，不与概念身份或比较绑定K合并。
- `概念树类数据服务`：只扩展RC专属结构和专属公开结果，不改变既有纯概念、FC或EC结果含义。
- `装配.普通应用`：只按已裁决后的唯一构造合同加入RC结构交付和DATA外服务生命周期。
- `海中鱼巣.vcxproj`及filters：登记新`.h/.cpp`，不新增模块扫描、BMI或IFC。

### 4.3 必须新建

- `领域/合同.二次关系概念.h/.cpp`：RC专属DTO、成功谓词和只读provider。
- `业务/初始化.二次关系概念结构.h/.cpp`：唯一构造前外部初始化所有者。
- `业务/计算.二次关系求值.h/.cpp`：唯一RC/FC/EC及候选定义求值所有者，无写端口。
- `业务/应用服务.二次关系概念类.h/.cpp`：定义与来源阶段编排，不保存第二概念仓。

### 4.4 明确禁止

- 不建立或恢复`数据服务.二次特征类`。
- 不新增RC节点族、第五根、第二概念owner或持久二次准确实例。
- 不实现非I64、跨K自动等价、任意关系码判零、非FC模板EC或无界扫描。
- 不用裸S、裸I64、当前采用值或节点编码替代B/F的完整来源。
- 不把缺证据、规则不支持、预算不足、溢出、漂移或历史不可用降级为“不命中”或“同义缺失”。
- 不把形成见证、发布见证或匹配见证提升为新的认知证据。

## 5. 补齐顺序

以下只是目标路线，不是同时登记的多层执行库存。计划索引在任一时刻只登记当前最早代码叶。

### P0：合同和当前概念拓扑裁决（已完成）

目标：解决第6节所列唯一机器合同漂移，并处理现行存在概念计划对同一owner/文件的先后关系。

完成条件：

1. 4242精确构造合同与当前生产概念交付只保留一组正式答案。
2. 明确`EXISTENCE-CONCEPT-DEFINITION-PROVIDER-REBUILD`先完成、被替代还是退出；不得让两个计划同时修改`合同.相关概念添加参与.h`、`数据服务.概念树类.h/.cpp`和`装配.普通应用.cpp`。
3. 形成可由执行者机械实施的RC结构构造签名、交付顺序和旧布局退出边界。

### P1：RC结构所有者闭环

目标：实现4242第3至6节的完整结构能力，形成最早独立代码结果。

完整范围：

1. 在现行公共概念合同中新增`概念树规则身份`、`概念树读取头`和`概念树写入头`三种纯值类型并完成独立头及直接编译消费者核对；不迁移现有概念身份、类别、生命周期或组织类型。
2. RC全部定义、结构、预算、状态、请求、结果、见证和窄provider合同。
3. 固定53项外部结构初始化、首次材料重放、完整复用和半交付拒绝。
4. 唯一概念owner内的规范化、查重、读取、建立和原键收敛。
5. 来源、用途、图、父组、生命周期、来源释放和退出全部专属操作。
6. FC/EC约束专属有界读取适配。
7. 普通应用的RC结构初始化和唯一概念服务构造接线。
8. Debug/Release、公开结构入口黑盒、并发/幂等/发布未知/历史/退出专项验证。

P1完成只证明RC结构提供者闭合；不证明求值、形成、命名、动态、因果或业务采用。

### P2：RC/FC/EC调用期求值闭环

目标：实现4242第7节，不写任何持久事实。

完整范围：

1. F与B两类参与者材料及完整来源见证。
2. B→C/E/S及S内容互证，同方同C和强时间约束。
3. FC精确I64域、EC模板和存在根求值。
4. 固定K与当前合法K双重核验，物理左=B、右=A，实际差异为A-B。
5. 原子与AND全量非短路求值、D位判断和完整错误分流。
6. 已发布RC、单FC、单EC及候选定义四入口共用一套私有算法。
7. 普通应用生命周期接线和生产外有界黑盒验证。

P2复用`有序I64特征比较提供者`，不调用或复制旧DATA门面；现有`二次特征计算应用服务`继续服务一般二次DAG，不承担RC的B/FC/EC语义。

### P3：RC形成与采用闭环

目标：实现4242第8节的可恢复形成编排。

完整范围：

1. 候选命中后才查重、建立或复用。
2. 定义阶段与来源阶段使用两个非零且不同的幂等键。
3. 定义发布未知先按原键收敛；来源发布未知同样保存并收敛原请求。
4. 定义已发布而来源失败保留`定义已发布`阶段，不谎报回滚。
5. 形成成功后不自动命名、不自动记用途；真实消费方另行记录采用。
6. 普通应用装配、公开消费者和阶段恢复专项验证。

## 6. 已裁决记录

### 6.1 正式源级合同与当前生产拓扑冲突（已由规范裁决）

4242 v0.3继续把RC集成构造冻结为当前`纯概念v2 + 特征概念出生使用 + 存在概念两组`生产拓扑，并明确RC完整定义走专属结果、不进入`纯概念定义`联合。旧`base + FC + EC`生产DTO、构造、CRUD和兼容层已经由`18239d93c`物理删除；P1不得恢复。旧持久材料本次不物理删除，需要转换时另立离线迁移合同。

### 6.2 同一概念owner旧计划冲突（已解除）

裁决前`计划/计划索引.md`曾登记`EXISTENCE-CONCEPT-DEFINITION-PROVIDER-REBUILD`及后继`LEGACY-CONCEPT-INTERFACE-REMOVAL`，其允许范围包含：

- `领域/合同.相关概念添加参与.h`
- `领域/数据服务.概念树类.h/.cpp`
- `装配.普通应用.cpp`

这些与P1的公开ABI、owner、生产文件和装配入口重合。该计划正文还说明`c5ef98486`只是中间WIP，旧pure variant和消费者迁移尚未闭合。因此P1不能作为独立并行计划登记，也不能在该计划状态未裁决时取得同一语义所有权。

正式结果：`LEGACY-CONCEPT-INTERFACE-REMOVAL`已由`18239d93c`发布完成，旧计划不再占用同一owner、ABI或生产文件。当前计划索引仍保留其陈旧`可执行`条目，必须在登记RC-P1的同一治理事务中归档并删除；不得再次派发旧计划。

### 6.3 公共规则身份缺项（已由用户限定裁决）

4242的`二次关系结构交付::规范化规则`、`二次关系概念事实::规则`和7191的`动态概念定义事实::规则`、`动态概念结构交付::规范化规则`共同需要`概念树规则身份`。它属于公共概念结构值类型，不属于RC私有解释器；若放入RC专属合同，会迫使DC依赖RC私有身份并产生重复所有权。

唯一声明恢复到`海中鱼巣/领域/合同.相关概念添加参与.h`，形状只包含`稳定编码 值{}`、默认构造、显式`稳定编码`构造和按值相等。默认值不表示规则已存在，构造和比较零仓库访问；规则节点的存在、归属、版本和生命周期仍由各专属结构服务核验。该类型不得用`using`退化为裸编码，不得与`概念树概念身份`或`特征I64比较绑定身份`合并。直接代码消费者在P1仅为新RC合同；7191是后继DC合同的正式消费者，本阶段只保证公共类型可独立包含，不提前建立或装配DC实现。

## 7. P1形成可执行计划时必须冻结的文件

以下范围只有在P0裁决完成后才能转入正式计划：

允许生产文件候选：

- `海中鱼巣/领域/合同.相关概念添加参与.h`（只新增规则身份、读取头和写入头三种纯值类型；保留现有合法内容）
- `海中鱼巣/领域/合同.二次关系概念.h/.cpp`
- `海中鱼巣/业务/初始化.二次关系概念结构.h/.cpp`
- `海中鱼巣/领域/数据服务.概念树类.h/.cpp`
- 因三种纯值类型新增而实际需要修改的直接include消费者；开工S0必须逐项列出，当前P1只允许RC新合同消费，DC仅作后继公共头编译核对而不实施
- `海中鱼巣/装配.普通应用.h/.cpp`
- `海中鱼巣.vcxproj`
- `海中鱼巣.vcxproj.filters`
- P1专属验证工程、施工记录和验证记录

禁止生产文件：

- `海中鱼巣/业务/计算.二次特征.h/.cpp`
- `海中鱼巣/领域/数据服务.特征类.h/.cpp`
- `海中鱼巣/领域/算法.有序I64特征比较.h`
- L1、状态、存在、场景、动态、因果、需求、任务和方法的机器合同
- 正式规范，除非P0另行得到用户明确批准

若机械迁移实际命中超出上述候选，计划智能体必须回到当前代码重新列出每个消费者；不得让执行者按编译错误临场扩大范围。

## 8. 验证矩阵与完成声明边界

| 阶段 | 最低验证 | 可以声明 | 不能声明 |
| --- | --- | --- | --- |
| P1 | `git diff --check`、公共头独立编译、规则身份默认/显式构造及按值相等、三种公共类型唯一完整定义、RC直接消费者编译、旧符号零恢复、根工程x64 Debug/Release、全部受影响消费者编译链接、53项初始化、规范化/同义、CRUD、来源用途、图治理、幂等恢复、历史和退出专项 | RC结构所有者代码闭合 | RC能对现实材料求值或自动形成 |
| P2 | 根工程双配置、F/B两来源、B→C/E/S、FC/EC、K、边界I64、溢出、全量错误分流和非短路专项 | RC/FC/EC调用期I64求值闭合 | RC已形成、已命名、已实际用于业务 |
| P3 | 根工程双配置、候选不命中、同义复用、定义/来源两阶段未知与原键恢复、重启读回和真实消费者专项 | RC形成编排代码闭合 | 动态概念、因果、语言或业务目标整体闭环 |

独立重启恢复、故障注入、生产装配、正式消费者、端到端业务和概念实际有用性必须分别报告；构建或专项通过不能替代这些证据。

## 9. 当前交付边界

本文件完成当前代码差距核对、能力复用裁决和补齐路线。旧接口删除已经发布；下一合法代码产物是P1“RC结构所有者闭环”。P2和P3仍只保留为后继路线输入。本文件本身不证明RC已实现、求值已接通或代码已经验证。

## 10. 修订记录

| 日期 | 版本 | 修订内容 |
| --- | --- | --- |
| 2026-09-22 | v0.4 | 按用户限定裁决补齐`概念树规则身份`公共所有权、历史纯值形状、直接消费者和验证边界；P1公共增补由两种更新为三种，不恢复旧概念接口或扩大RC业务范围。 |
| 2026-09-21 | v0.3 | 吸收旧概念接口物理删除后的pure v2现状，冻结RC-P1最早代码叶及同owner边界。 |
