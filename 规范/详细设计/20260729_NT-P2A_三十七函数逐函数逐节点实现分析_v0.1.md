# NT-P2A 三十七函数逐函数逐节点实现分析

日期：2026-07-29

范围：F-A101—F-A110、F-W101—F-W115、F-W201—F-W203、F-R101—F-R105、F-R201—F-R204，共三十七个业务 / 公开目标根；另含二十八个专项内部支持根 F-A201—F-A213、F-W301—F-W312、F-R301—F-R303，总计六十五根。节点身份、参数绑定和返回路径以同编号单函数施工图为准。

## 1. 记录底座

| 函数 | 节点实现分析 | 规范核对 | 非成功 / 验证 |
| --- | --- | --- | --- |
| F-A101 | 逐字段值式校验；按 `(特征值, 原始值版本)` 查私有候选；候选 list 节点和成功 / 幂等返回材料都在状态修改前构造，最后无分配 splice | 2100 三态；4020 不含拓扑 | 无效 / 异义 / 资源失败且调用前状态不变；A01-A04 |
| F-A102 | 用准备只读视图逐候选调用三个真实入口，核对值节点为本会话特征值候选且主键命名域正确；锁前 reserve 反向位置；一次独占仓；无分配 splice 后保存稳定迭代器 | 4040 准备阶段只读候选视图 | 已发布同义=核心版本漂移、异义=身份冲突；部分插入内部逆序撤销；A05-A11/A18 |
| F-A103 | 只检查阶段、仓占用和反向位置 | 4040 确认不发布 | 阶段异常内部错误；A09 |
| F-A104 | `noexcept` 标记仓条目发布、真实组一次推进仓版本并保存 `本事务发布结构版本_` | 4040 最后发布无失败 | 无返回；A10-A11/A18 |
| F-A105 | 所有可撤销阶段都清私有 list；已准备/确认先按稳定迭代器逆序 erase，再清反向位置并释放仓锁 | 4040 逆序撤销 | 失败交执行器隔离；A07-A11/A14-A15 |
| F-A106 | shared lock 下筛选唯一有效最高版本，复制后释放 | 2100 当前性 | 0=合法未找到，多=内部错误；A13 |
| F-A107 | shared lock 下复制全部已发布结构完整版本并排序 | 4020 历史保留 | 重复版本 / 未知格式内部错误；A14-A15 |
| F-A108 | 内联按固定七字段形成24项专项准入表；隔离夹具固定运行 A01—A18；private 四阶段只经真实执行器触发；只计结构化失败 | 4050 日志不裁决；专项表不冒充生产准入 | 返回失败数；A01-A18 |
| F-A109 | shared lock 下读取非零仓结构版本，不读取记录 | 4040 发布版本 | 许可竞争 / 零版本内部错误；A17 |
| F-A110 | 执行器返回后读取参与者保存的本事务确切发布版本 | 4040 最后发布 | 非已发布阶段内部错误；A18 |

## 2. 关系 22 与批次写入

| 函数 | 节点实现分析 | 规范核对 | 非成功 / 验证 |
| --- | --- | --- | --- |
| F-W101 | 一次事务外结构许可内读端点、两主键、F-W103、关系和两域高水位；幂等分类先于高水位；真实分支释放许可后由普通执行器在回调重读并形成槽 / 值 / 四关系 / 两索引 / 记录，捕获确切材料，提交后 F-A110 | 2100 4.1/4.2；4110 2.1 | 完整同义幂等、历史/部分占用异义、写前漂移和全故障逆序；W06-W16 |
| F-W102 | 一次事务外结构许可内由 private F-W103 固定写前三元组并核对新值主键/高水位；真实分支释放许可后锁内重读，先失效旧角色3再形成新值/关系/索引/记录，捕获材料，提交后 F-A110 | 4110 换代 | 漂移零写；后续失败恢复旧关系；W17-W22 |
| F-W103 | private 组合读取；只接收同对象 F-W101/F-W102 传入的结构许可；全审计扫描角色1/2/3/来源；有值走 F-A109/A106/A109，无值单次 F-A109 | 4110 逐角色基数 | 外部专项不可直调；不返回部分材料；W07/W15/W16 经 F-W101 可达分支 |
| F-W104 | 一次事务外结构许可内先读批次账：存在则释放后 F-W110 完整历史比较；不存在则在同一许可内规范化、读端点/写前三元组/关系唯一性和两域连续高水位；释放后进入普通执行器，锁内用当前会话、关系审计、F-A107/A109 完整复核，再按稳定顺序形成全部核心候选、记录候选和批次候选 | 4170 同事务；4040 多参与者 | 同义读回 / 异义冲突 / 并发先行分类 / 全故障按批次→记录→核心逆序；W31-W39 |
| F-W105 | 以显式位置循环校验项目非空、顺序号 0..N-1、每项完整字段和 optional 三元组全空 / 全有；临时 list 节点和成功 / 幂等返回材料全部预构造后无分配 splice；记录本体无发布位 | 4170 记录内容 | W23 首次、W24 同义 / 异义、W25/W26 准备前记录完整性、W38 资源点1/2 |
| F-W106 | 以显式项目循环对每个新值和初始槽逐项调用准备视图三个真实入口；换代槽不要求候选；独占后再显式扫描仓条目，账内同义返回核心版本漂移、异义返回身份冲突，未找到无分配 splice | 4170 三次互证；不虚构准备视图能力 | W25/W26 准备、W27 发布 / 撤销、W30 持锁、W37 阶段与执行器故障、W38 资源点3 |
| F-W107 | 核对阶段、占用和候选完整性 | 4040 | 内部错误；W28 |
| F-W108 | `noexcept` 发布并一次推进批次仓版本 | 4040/4170 | 无可失败动作；W29 |
| F-W109 | 删除未发布批次记录并释放占用 | 4040 | 撤销失败隔离；W30 |
| F-W110 | 自行取得一次结构许可；F-W113 后以 F-A109 固定记录仓版本；全关系审计按账内四个完整句柄逐项目精确匹配，F-A107 取指定原始值版本，末次 F-A109 复核；不枚举宿主其它槽且不调用 F-W103 | 4170 历史读回 | 任一项坏或版本漂移则零材料；W32-W34 |
| F-W111 | 内联形成24项专项准入表并仅用 F-W115 替换下标22；W01—W39 覆盖两单槽、混合批次、关系准入、主键高水位、参与者、历史读回和确定资源失败矩阵 | 4050；夹具表不冒充生产装配 | 返回失败数；W01-W39 |
| F-W112 | 批次仓共享锁内拒绝未发布条目，逐格式1项目按 `新特征值==输入` 或 `写前当前值==输入` 两角色反查；同项目恰一角色只返回一次，两角色同时命中为内部不一致；排序自有副本 | 4170 退役引用；不持节点仓 | 合法空组带非零版本；双角色、坏格式 / 顺序 / optional 内部错误；W28-W29 |
| F-W113 | 仅取得批次记录仓共享锁，按批次身份键读取一条已发布格式 1 记录并复制自有副本；不取得结构许可 | 4170 批次主键与幂等账 | 未找到 / 同键重复 / 坏格式；W17-W18/W31 |
| F-W114 | 按顺序号裁决关系22核心端点类型组合 | 4110 2.2；不替代业务基数 | 非三角色组合返回 false；W01—W04 |
| F-W115 | 逐字段构造唯一关系22具名准入项 | 4110 2.2；#352 组合 | 纯值无失败；W05 |

## 3. 查询与退役

F-R101—F-R104 的物理根均为 `节点直接特征查询退役数据操作`；公开 `节点直接特征服务` 同名函数只作单调用转发，不复制下表过程。

| 函数 | 节点实现分析 | 规范核对 | 非成功 / 验证 |
| --- | --- | --- | --- |
| F-R101 | 一次结构许可；关系按编号归并最高版本后扫描角色3反向链；当前 / 历史分流；角色2、节点和记录互证；F-A109 前后夹取版本 | NT-C2/v0.5 | 合法未找到 / 基数异常 / 读取版本漂移 / 资源失败；R01-R05 |
| F-R102 | 一次结构许可；F-A109 固定版本；有效角色1枚举，逐槽复核反向角色1、角色2/3、来源和 F-A106 同版本；返回前复核版本 | 4110 | 合法空组带宿主非零版本；禁止部分组；R06-R10 |
| F-R103 | 复用历史归属，比较节点版本和原始值版本 | 2100 生命周期 | 漂移 / 未找到沿原分类；R11-R13 |
| F-R104 | 一次结构许可；节点 / F-A107 / 全关系审计 + F-W112 按新值或写前值双角色反查；逐引用按四关系句柄和 F-W113 批次记录精确互证；新值角色核对发布后版本，写前角色额外核对写前关系/值/版本；F-A109 前后夹取 | 4020/4170 | 双角色 / 悬空 / 重复 / 未知格式 / 版本漂移零材料；R14-R20 |
| F-R105 | 内联形成24项专项准入表并仅用 F-W115 替换下标22；固定 R01—R24 隔离夹具 | 4050；夹具表不冒充生产装配 | 返回失败数；R01-R24 |

## 4. 服务根

| 函数 | 节点实现分析 | 规范核对 | 非成功 / 验证 |
| --- | --- | --- | --- |
| F-W201 | 单次调用 F-W101 并原样返回 | 2100 公开新槽入口 | 不转换任何分类；W39-1 |
| F-W202 | 单次调用 F-W102 并原样返回 | 2100 公开换代入口 | 不启动第二事务；W39-2 |
| F-W203 | 单次调用 F-W104 并原样返回 | 2100 公开批次入口 | 不拆批；W39-3 |
| F-R201 | 四参数逐项转发到 F-R101 | NT-C2 归属读取 | 当前 / 历史口径和可选目标原始值版本原样；R22-1 |
| F-R202 | 单次调用 F-R102 | NT-C2 宿主槽组 | 合法空组和版本原样；R22-2 |
| F-R203 | 三参数逐项转发到 F-R103 | NT-C2 退役规格 | 版本原样；R22-3 |
| F-R204 | 单次调用 F-R104 | NT-C2 退役材料 | 空引用组不改类；R22-4 |
| F-A201 | 按编号绑定23项固定描述；构造隔离核心三仓、记录仓/访问器、两个记录参与者和探针；执行具名前态动作、设置单项注入、调用唯一 A 根、清除并逐字段比较节点权威材料/关系/索引/记录版本与目标结果 | 只在 A 专项模块；描述结构和 span 配方见详细设计8.4 | 任一描述不完整、夹具不等、目标/探针/快照不等或异常均 false；A01—A18 |
| F-A202/F-A213 | 资源状态递减至零后先清除再抛异常；阶段状态精确单次命中先清除再 true | 只在宏开记录参与者；每个冻结边界显式调用 | A05/A06/A14/A15/A18 |
| F-A203 | 先校验并预构造已发布条目，再独占仓、拒绝重复键、无分配 splice 并推进版本 | 宏开 export，只给 A/R 专项 | A17、R阶段漂移 |
| F-A204—F-A211 | 两类探针四阶段完整 override；准备/确认/发布/撤销计数，读探针准备内调用 F-A106/A107/A109 | A 自检私有；执行器可达 | A08/A16 固定 1/0/0/1 |
| F-A212 | 穷尽记录回调配方，形成固定核心候选、调用 F-A101、核对后请求提交；失败请求撤销 | A 自检私有；局部 lambda 只单调用本根 | A02—A16 |
| F-W301 | 按编号绑定101项固定描述；构造五仓、记录/批次访问器、两参与者、写数据操作/服务；通过本函数六节点回调和公开写根形成前态，单次调用唯一 W 根并比较五仓 | 只在 W 专项模块；不得读取 private 容器或跨用例复用 | 任一固定子项失败返回 false；W01—W39 |
| F-W302/F-W303/F-W311/F-W312 | 批次/写资源递减抛错；批次阶段单次命中；核心步骤复制固定结果后清除 | 对应宏开生产模块；业务根每个冻结点显式调用 | W31—W38 |
| F-W304 | 预构造夹具条目，独占后按参数裁决重复身份，无分配插入并推进版本 | 宏开批次参与者 export | W29、R损坏账 |
| F-W305—F-W308 | 批次读探针四阶段；准备内调用 F-W112/W113 后固定失败，撤销成功 | W 自检私有 | W30 固定 1/0/0/1 |
| F-W309/F-W310 | 事务配方分派与阶段回调分派；后者单次调用 F-W104 并捕获全部异常 | W 自检私有 | W23—W27、W33/W34/W36 |
| F-R301 | 按编号绑定50项固定描述；构造五仓、两个访问器、写/查询数据操作和查询服务；合法前态只经公开 W 根，受控损坏只经宏开夹具；单次调用唯一 R 根并比较五仓 | 只在 R 专项模块；节点快照调用真实 `导出权威状态()` | 任一固定子项失败返回 false；R01—R24 |
| F-R302/F-R303 | 查询资源递减抛错；查询阶段回调单次调用 F-A203，false/异常写准备失败 | #383 查询数据操作 / 自检私有 | R08—R24 |

## 5. 跨函数调用和节点闭合

```text
F-W101/F-W102 -> F-W103 + 当前核心执行器 + F-A101—F-A105 + F-A110
F-W103 -> F-A109 + F-A106 + F-A109
F-W104 -> F-W113/F-W110 + 当前核心执行器 + F-A101—F-A105 + F-W105—F-W109 + F-A107/F-A109/F-A110
F-W110 -> F-W113 + F-A109 + F-A107 + 正式关系仓库::读取全部关系审计
F-R101 -> F-A109 + F-A106 + F-A107
F-R102 -> F-A109 + F-A106
F-R103 -> F-R101
F-R104 -> F-A109 + F-A107 + F-W112 + F-W113
F-W201/F-W202/F-W203 -> F-W101/F-W102/F-W104
F-R201/F-R202/F-R203/F-R204 -> F-R101/F-R102/F-R103/F-R104
F-A108 -> F-A201
F-A201 -> F-A204..F-A212
F-A101/F-A102/F-A106/F-A107 -> F-A202
F-A102/F-A103 -> F-A213
F-R303 -> F-A203
F-W111 -> F-W301
F-W301 -> F-W304..F-W310
F-W105/F-W106/F-W112/F-W113 -> F-W302
F-W101/F-W102/F-W103/F-W104/F-W110 -> F-W303
F-W106/F-W107 -> F-W311
F-W101/F-W102/F-W104 -> F-W312
F-R105 -> F-R301
F-R301 -> F-R303
F-R101/F-R102/F-R103/F-R104 -> F-R302
```

没有公开写入口调用另一个公开写入口；批次内部不会启动子事务；F-W110 不调用 F-W103。F-W103 是 private 成员，外部专项不能传入其它事务域许可。所有单函数图节点必须为一个具名调用或本函数内一条具体指令，参数 / 结果绑定没有待执行侧选择项。
