# STATE-STRUCTURE-OPERATION-CONTRACT 状态类结构与操作合同详细设计

日期：2026-09-09
版本：v0.1
状态：可施工详细设计
正式依据：1160 v0.17、4212 v0.1、4230 v0.42

## 1. 目标、完成条件与排除项

目标是把当前 `数据服务.状态类.ixx` 的旧“时间 + 多特征快照”模型一次性替换为状态身份、正式 FT、固定准确值、强时间四字段模型，并使现有三个仅做状态身份核验的消费者改用窄身份入口。

完成条件：合同版本2 DTO和十一项公开函数全部实现；两种值表示、两种时间语义和0成立；创建、当前/历史读取、三类字段查询、退出、同步实例清理、共同身份读回和旧格式拒绝闭合；旧 v1 声明/定义/调用零命中；工程可编译。

明确排除：4202 C/E/P/S 状态使用绑定、动态数据、场景动态组织、动作来源、动态概念、因果业务、当前选择关系、普通应用周期装配、存量转换程序、测试专用生产入口。它们不得进入状态 DTO 或状态写集。

## 2. S0 当前事实

- 起点：`0c1c0da91b59d9e65a2fe1f88cf93c4ebf41519b`，`main == origin/main`，index 空。
- 当前状态模块已由工程 `ClCompile` 登记，约1192行；公开合同版本1，只承载实例状态，要求时间大于0，以 I64 时间和 U64 交替“F编码/值事实编码”快照保存多个特征。
- 当前模块依赖 L1 与特征类，构造持有时间/快照两个属性类型；没有生产构造点。
- 直接消费者只有存在类、场景类、因果类，均调用 `查询状态` 并只核对节点身份，没有读取旧快照字段。
- 当前特征类已有 FT、准确值、特征值服务和同截止私有核验能力，但缺少“无需 F 身份、按 FT + 准确值核验”的公开窄入口。
- L1 已有 owner 原子写集、当前/历史事实与关系/属性读取、按属性类型当前值组、按目标关系组、首次写入材料和物理清理入口；无需修改 L1。
- 状态、特征、存在、场景、因果模块均已在根工程和 filters 登记；本计划不改工程文件。
- 旧 L2 状态结构文件已由 `6a2cfbca` 删除，不能复用或恢复。

知识库的旧 L2 状态用途卡绑定退役模块，低于当前源码；本设计只用其确认“旧材料不可作为新 ABI”，不复用旧实现。

## 3. 字段与 L1 映射

| 类别 | 字段 | 物理表达 | 基数/可变性 |
| --- | --- | --- | --- |
| 身份 | 状态身份 | 状态 owner 普通节点 + 指向状态族锚点的归属关系 | 恰一；不复用 |
| 关系 | 正式 FT | 状态节点 → FT 节点具名关系 | 恰一；不可修改 |
| 值 | 固定准确值 | I64属性，或长度1 U64组保存特征值身份编码 | 两分支恰一；不可修改 |
| 值 | 强时间 | 实例绝对UTC或抽象相对属性类型之一，材料I64 | 两分支恰一；非负；不可修改 |
| 技术材料 | 首次形成UTC | I64属性 | 恰一；不可修改；非本体字段 |

结构交付八项身份、默认TTL和数量上限及构造签名逐字采用4212第3、11节。创建写集固定为一个节点、两条关系、三个值和三个槽；所有自有事实同次创建。

### 3.1 字段操作裁决

CODE-FILE-01要求逐字段操作映射；1160又禁止类型、值、时间独立改写。两者按权威顺序收口如下，不建立拒绝壳或同义setter：

| 对象/字段 | 新增 | 查询 | 修改 | 删除/退出 | L1映射 |
| --- | --- | --- | --- | --- | --- |
| 完整状态 | `创建状态` | 当前/历史读取 | 不允许；新内容另建状态 | `退出状态` | owner原子写集/历史读回 |
| 状态身份 | 随完整创建 | `确认当前状态结构身份`、历史见证 | 不允许 | 随完整状态退出 | 节点+族归属 |
| 正式FT | 随完整创建 | 完整读取、`按正式特征类型查询当前状态组` | 不允许 | 随完整状态退出 | FT关系/目标关系组 |
| 固定准确值 | 随完整创建 | 完整读取、`按固定准确值查询当前状态组` | 不允许 | 随完整状态退出 | 互斥属性/属性类型值组 |
| 强时间 | 随完整创建 | 完整读取、`按强时间范围查询当前状态组` | 不允许 | 随完整状态退出 | 互斥属性/属性类型值组 |

“不允许”来自上级机器语义，不生成永远失败的公开函数；所有可成立操作均有独立强类型入口。技术首次形成UTC只供同步清理内部读取，不上升为对象字段公开CRUD。

## 4. DTO、函数和物理位置

4212第2、5、6、11节的声明逐字进入现有模块 `海中鱼巣.领域.数据服务.状态类`、文件 `海中鱼巣/领域/数据服务.状态类.ixx`；公开类型置于 `export namespace 海中鱼巣`，实现 helper 留在模块内部命名空间或类 private。不得新建第二状态文件或 DTO owner。

特征窄核验 DTO 与 `特征类数据服务::核验正式特征类型准确值` 置于现有 `海中鱼巣.领域.数据服务.特征类`，声明紧邻现有准确特征读 DTO/公开读函数，定义复用 `读类型`、`解析输入`、值域检查和G/H守卫。诊断责任为“向上送出”，零日志、零写。

状态十一项函数均为状态结构 owner 的对外服务函数；构造/绑定属于装配与特殊成员。读取、查询、身份确认和历史见证为 const；创建、退出、同步清理为写函数。公开函数诊断责任均为“向上送出”，以结构化结果承载；私有转换/校验 helper同样向上送出到公开边界，不本地重复记录。

## 5. 核心流程

### 5.1 创建

校验请求形状与G0 → 先查首次幂等材料 → 未绑定时调用特征窄核验 → 取得服务内部当前UTC → 形成固定写集 → 状态owner一次提交 → 从映射取得节点/关系/值身份 → 按发布截止完整历史读回 → 返回已创建。已绑定同请求不重新按当前来源否定首次事实，按首次材料和首次截止读回；异请求冲突。

### 5.2 读取与查询

完整读取使用同一Gread/H依次读取节点、族归属、FT关系和三值，逐项核对owner、角色、创建/退出与互斥分支；首尾重读当前G防拼接。字段查询先用L1具名组入口召回候选，再调用同G完整当前读取，排序去重。空组成功，预算不足零载荷。

身份确认只读取节点和族归属；历史见证只返回身份来源。二者不读取四字段，也不能供4202冒充状态内容。

### 5.3 退出与清理

退出先完整当前读回，再以一个状态owner写集退出节点、两关系、三值，最后在新截止历史读回。任何外部引用由L1引用保护拒绝，状态类不级联。

同步清理按首次形成UTC属性有界取得全量当前候选，完整读回后只保留实例分支，按首次形成UTC/身份排序，计算TTL与数量并集。无候选零写成功；有候选先批量逻辑退出，再以新截止调用L1物理清理六项自有事实。任一引用冲突整次不形成部分成功。普通应用先清动态/绑定/组织/当前选择的跨owner编排不在本计划。

## 6. 旧格式和迁移

删除状态 v1 DTO、函数及内部快照编码/解析/查询。版本2只识别新族归属、FT关系、互斥准确值、互斥时间和首次UTC；旧“一个时间+一个快照”形状返回 `旧格式不支持`。存量材料不删除、不回写、不双读、不转换。当前不存在可无歧义选择多快照中唯一FT的规则，因此任何转换都属于未来独立合同。

存在、场景、因果三处旧 `查询状态` 改为 `确认当前状态结构身份`；只检查头的已读取、合同版本、截止和零变更。它们不得读取完整状态或新增反向依赖。动态类当前为异主WIP，不触碰。

## 7. 文件与调用边

| 文件 | 变更 | 允许 | 禁止 |
| --- | --- | --- | --- |
| `海中鱼巣/领域/数据服务.特征类.ixx` | 增加窄只读核验DTO/函数 | 复用现有私有FT/值核验 | 改F字段、写入、概念或比较合同 |
| `海中鱼巣/领域/数据服务.状态类.ixx` | 原文件整体重构为v2 | 四字段、共同身份、查询、退出、同步清理 | C/E/P/S、当前选择、动态/动作/概念/因果 |
| `海中鱼巣/领域/数据服务.存在类.ixx` | 身份调用迁移 | 只换窄调用和结果核对 | 改存在数据语义 |
| `海中鱼巣/领域/数据服务.场景类.ixx` | 身份调用迁移 | 同上 | 建状态使用绑定或改场景结构 |
| `海中鱼巣/领域/数据服务.因果类.ixx` | 身份调用迁移 | 同上 | 改因果结构 |
| `施工记录/20260909_STATE-STRUCTURE-OPERATION-CONTRACT_状态类结构与操作施工记录_v0.1.md` | 新建结果与知识变更清单 | 记录实际事实 | 冒充验证或业务闭环 |

工程文件不变；状态模块已登记。依赖新增仅为状态→特征窄核验；三个消费者原有状态引用保持，调用面变窄。

## 8. 验证矩阵与声明边界

创建侧只要求设计文档 `git diff --check` 与 strict。代码执行至少完成：

1. `rg` 确认旧状态DTO、`特征值快照`、`快照属性类型`、旧 `查询状态` 消费和状态模块中的存在/场景/动态/因果依赖零命中；
2. 核对工程仍只登记一个状态模块且UTF-8配置不变；
3. 在 `D:\TEMP\海中鱼巣\STATE-STRUCTURE-OPERATION-CONTRACT\<唯一轮次>\Debug|Release` 隔离重建 x64 根工程；
4. 静态逐项核对4212最低矩阵和结果成功谓词；不为覆盖率向生产代码加入自检入口。

本计划不要求运行程序、专项业务测试、数据库/重启恢复、故障注入、状态使用绑定、动态或业务验收。编译成功只证明代码接入，不证明运行、迁移、周期装配或业务闭环。
