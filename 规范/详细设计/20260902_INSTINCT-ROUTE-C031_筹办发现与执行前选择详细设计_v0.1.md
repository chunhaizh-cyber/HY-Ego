# INSTINCT-ROUTE-C031 筹办发现与执行前选择详细设计 v0.2

日期：2026-09-02

身份：`INSTINCT-ROUTE-C031-PREPARATION-DISCOVERY-EXECUTION-PRESELECTION`

状态：**正式详细设计 v0.2；已随 C031 正式规范收口。它不是代码实施计划，未取得生产文件所有权、精确白名单、S0 和验证合同前不得施工。**

依据：用户确认的筹办—执行—需求治理口径、`0050` v2.6、`3100` v1.6、`3200` v1.6、`3300` v1.2、`4250` v0.9、`4260` v0.17、`5090` v1.1、`5100` v0.7、`5110` v1.0、`5210` v1.3、`5230` v2.1、`5240` v0.2、`5250` v0.12、`5300` v1.0、`8100` v1.6 及 C031 路由。代码事实扫描基线为 `main=origin/main=1653e6f47d000cd0fb8085c3a5d3a9977f3780ea`；工作区同名协议、线程和领域文件存在异主未提交 WIP，本文不把它们当作已发布能力。

---

## 1. 目标、范围和非目标

目标是把一条既有任务的内部治理改成下列职责顺序：

```text
筹办：找候选方法，找每个方法自己的条件组
-> manager：按消息独立读回并路由
-> self：确认是否真有需要，管理其后继
-> demand owner：建立或复用正式需求
-> 执行前：fresh 判断可用性，选择一个可执行选项并冻结
-> 既有授权 / 硬否决 / 调度 / 动作 / 结果链
```

范围只包括筹办发现、线程交接、需求确认、执行前选择与既有选择 / 冻结的时机调整。

本输入不：

- 新建服务合同、服务预算、服务余额、预期收益账、筹办历史账、持久 outbox、第二结果账、专用筹办线程或第二消息通道；
- 把服务根当前值、服务合同余额或服务进展作为自动方法代价；
- 改变 6100 / 6140 的实际安全值结算，或把执行前预测写成安全 / 服务实际值；
- 声称当前代码、P01 消息链、P02 子目标准入、运行测试或整条本能路线已完成。

## 2. 已确认的业务合同

### 2.1 筹办发现

筹办只回答“当前有哪些可能方法、它们各自需要哪些条件、哪些材料当前缺失”。它不回答“该任务应当采用哪一个方法”，也不把任一方法写成已采用。

每个发现候选必须保留：

```text
方法身份
+ 方法内容版本 / 规格版本 / 生命周期读回身份
+ 目标结果或用途的正式引用
+ 0..N 个该方法自己的条件组
```

每个条件组是一次执行可选项的值式部分：

```text
规范有序的条件 / 条件结果关系集合
+ 输入、参数和限制绑定材料
+ 各项在来源截止的可读状态（满足 / 缺失 / 结构缺失 / 漂移）
```

条件组内部按方法合同共同成立；组间可替代、可组合或其它关系只能由同一方法内容版本的正式组合条件关系显式声明。没有强类型组身份或组合语义时，返回`条件组结构缺口`，不猜测“多个组天然可替代”。条件组是由既有条件关系和绑定材料组成的值式投影，不建立新的持久条件组节点、历史账或任务级总条件清单。

因此发现结果可表达：

```text
M1@V1 -> C11 / C12
M2@V7 -> C21
```

`方法缺失`是完整粗召回后候选为空的发现；`条件缺失`精确属于某个“方法 + 版本 + 条件组 + 条件”组合。两者都不是需求，也不能自动演变为子任务。

### 2.2 每个工作包的一条消息

保留“一个已消费筹办工作包最终只投递一条不可变消息；队满只重投同一消息；manager 已接收或精确重复后工作包才结束”的协议。

未来消息承载的是一次`筹办发现`定位，而不是现行五类互斥业务结果。正常发现以候选、条件组、缺口和来源事实的强类型定位为载荷；当前性漂移、引用冲突、资源失败和内部错误仍走结构化非成功，不能静默结束。

消息不是正式候选、条件、需求、路径、选择或冻结事实。manager 必须使用任务、任务轮次、筹办轮次、工作包、来源截止和结果截止重新读取各 owner 的正式事实。

目标已满足、合法等待、正式子目标承接和执行冻结不再是筹办消息的业务结果：它们分别由目标比较、self 后继治理、确认后的正式需求承接和执行前选择 / 冻结链处理。

### 2.3 初次与后继来源

筹办传输请求必须以一个强类型判别联合同时承载：

```text
初次筹办来源：任务初次筹办工作包
后继筹办来源：下一筹办工作包
```

两个分支都绑定任务、任务身份来源、来源需求、任务轮次、筹办轮次、G0 和幂等身份；它们不能由裸序号、消息外层字段或空 optional 互相伪装。复用一条工作包运输协议和一条结果消息，不创建初次筹办的平行线程或平行队列。

## 3. manager、自我和需求领域的边界

### 3.1 manager 独立读回后只路由

manager 不重跑候选发现、不选择方法、不形成条件结论、不直接写需求或子任务。它收到筹办发现后：

```text
验证不可变消息与工作包身份
-> 按同一事实截止独立读回任务、轮次、候选方法、条件组和缺口
-> 可进入执行前判断的候选：编排执行前选择工作项
-> 需要需求治理的发现：向 self 发缺口定位
-> 当前性 / 引用 / 资源问题：转交既有结构化非成功或具名后继 owner；manager 不生成等待合同
```

正式 HEAD 没有方法 / 条件缺口的 manager -> self 消息。因此新增的是**既有自我治理消息协议中的一个强类型“筹办发现缺口定位”分支**，不是新的消息通道。最小载荷只含任务、父需求、任务 / 筹办轮次、工作包、来源 / 结果截止、发现类别、方法与版本、条件组 / 条件或粗召回证据、候选独立目标定位、合法生产者与重触发材料。消息不携带 manager 的需求结论。

### 3.2 self 确认，需求 owner 写入或复用

self 收到定位必须 fresh 读取父需求、目标合同、任务 / 轮次、候选材料和当前事实。它只能形成基于正式材料的后继治理意图：

| fresh 结论 | 后继 |
| --- | --- |
| 外部事实未产生、时间未到、材料可由已知 owner 产生 | 正式等待及重触发；不建需求 |
| 已有方法但输入、入口、验证或归因合同缺失 | 当前任务结构缺口或等待；不自动建需求 |
| 独立目标确实必要、父域合法、目标合同可最终判定 | 形成 self 确认的承接意图 |
| 粗召回完整为空且学习目标满足独立目标门禁 | 可形成同一类承接意图；不是自动学习需求 |
| 粗召回非空但候选当前均不可用 / 不适用 | 不得伪装为方法缺失或学习需求 |

self 确认后的意图通过既有 self 治理交接进入 task owner。task owner 以公开入口发布或精确重放既有`任务子目标承接记录`；需求领域服务独立读回该记录，建立或复用正式子需求并绑定回记录。manager 只是转运和编排者，不能代替 self 确认或需求 owner 写入。

现有子目标记录当前主要携带方法版本和条件来源。正式收口必须扩展 / 重构其来源联合，使它能区分“已确认条件目标”“合法学习目标”“不应承接的结构缺口”，但不得用布尔门禁、文本、裸编码或第二证明账填充缺失语义。

## 4. 执行前可用性、选择和冻结

### 4.1 执行选项与硬过滤

执行前的值式选项是：

```text
方法身份 + 方法内容版本 + 条件组 + 输入 / 参数绑定 + 动作范围
```

在一个 fresh 事实截止下，只有同时通过以下硬门的选项才可进入比较：

```text
目标 / 结果合同允许
方法生命周期、内容版本和动作入口当前
本选项自己的条件组、输入和参数闭合
作用范围、验证和归因合同完整
禁止项、结构拒绝和安全硬否决未命中
生存安全根控制态允许普通执行竞争
```

任何不通过的选项不进入比较。安全硬否决不能由时间、资源、服务目标或任何收益抵销；`A=0/1` 不能被普通服务任务绕过。6150 调度倍率仍只在冻结后、真实派发前用于安全任务调度排序，不是方法选择、成本或授权。

### 4.2 选择

硬过滤后才进行实际适用性判断和选项选择。结果一致、条件 / 输入闭合、禁止 / 风险、运行质量的现有分层原则必须在新的执行前入口中重新冻结，而不留给 worker 临场判断。

目前不引入把安全、服务和时间相加的“最小代价”公式：

- 安全实际值只通过 6100 / 6140 的事后正式链变化；跨安全层原始值不可相加。
- 服务根当前值、服务合同、余额、预支、进展、准备和衰减账已退出；它们不能成为选择维度。
- 只有来源、单位、范围和规则版本均已正式定义的运行质量材料（例如同一单位的预计单调执行时长、资源或既有成本）才能比较。

未知、不可比或并列最优时不得按遍历顺序、编码、哈希或线程顺序选择。只保留发现与原因并向 self 发具名待裁决定位；只有具名后继 owner 才能形成正式等待，self 也不能用主观偏好补造比较算法。

### 4.3 复用既有选择 / 冻结记录

一旦选择完成，复用当前 L2 选择、路径、实例方法和执行冻结结构；其 producer 从筹办阶段迁移到执行前入口。原子写入必须仍同时绑定选中方法版本、条件组、输入 / 参数、动作入口 / 范围、验证、归因、来源事实截止和规则版本。任一绑定事实变化使选择和冻结失效，下一次尝试重新 fresh 读回和判断。

不增加选择历史账或代价账；既有选择记录就是已选一个执行选项的唯一业务记录。

## 5. 正式 HEAD 的结构映射与变更面

| 当前正式事实 | 设计裁决 |
| --- | --- |
| `任务初次筹办工作包` 已定义，但 `任务筹办工作与结果` transport 只接受后继 `下一筹办工作包` | 在同一 transport 内建立初次 / 后继来源判别联合；不能创建平行消息。 |
| `任务特征方法查找与条件提议` 只能返回平铺候选、单个已采用方案和该方案条件 | 将其改为候选 -> 条件组投影；多候选不再提前返回“待选择”，单方案也不提前写“采用方案”。 |
| `任务筹办当前就绪与执行冻结` 当前在筹办中选择、发布路径、实例和冻结 | 拆为“筹办发现”与“执行前选择 / 冻结”两个职责入口；后者才可调用既有 L2 选择发布。 |
| `任务筹办工作与结果` 当前固定五结果分支 | 迁移为发现定位 + 结构化非成功；必须保留同一消息幂等重投与 manager 接收完成边界。 |
| `任务管理线程` 只对可执行冻结读回并进入执行 | 增加发现读回、执行前工作项编排和 self 缺口定位；不获取选择权或需求写权。 |
| `自我治理消息协议` 没有方法 / 条件缺口类型 | 在既有协议中增加强类型发现定位与 self 确认后继交接；不建立第二通道。 |
| `L2任务子目标承接记录` 与需求消费服务已存在，但无业务生产者 | 只在 self 确认后由 task owner 公共入口生产 / 重放，需求服务消费；筹办不能直接使用它建立需求。 |
| L2 选择 / 冻结结构已能承载方法版本、条件、输入、参数、范围、验证和归因 | 保留为执行前选择成功的唯一持久产物，不另造记录。 |

涉及的正式 HEAD 模块至少包括：

```text
海中鱼巣/领域/内部治理/任务初次筹办.数据.h
海中鱼巣/领域/内部治理/任务特征方法查找与条件提议.数据.h
海中鱼巣/领域/内部治理/服务.任务特征方法查找与条件提议.ixx
海中鱼巣/领域/内部治理/任务筹办当前就绪与执行冻结.数据.h
海中鱼巣/领域/内部治理/服务.任务筹办当前就绪与执行冻结.ixx
海中鱼巣/线程/协议.任务筹办工作与结果.ixx
海中鱼巣/线程/任务工作线程.ixx
海中鱼巣/线程/任务管理线程.ixx
海中鱼巣/线程/自我治理消息协议.ixx
海中鱼巣/线程/自我线程生产创建.数据.h
海中鱼巣/领域/L2任务子目标承接记录.数据.h
海中鱼巣/领域/服务.需求任务子目标承接.ixx
海中鱼巣/领域/L2任务结构.数据.h
海中鱼巣/领域/服务.L2任务结构.ixx
```

具体文件白名单、函数签名、事务顺序、协议版本和迁移策略必须在正式规范收口后再冻结；此表不是施工白名单。

## 6. 正式收口、最早代码叶和禁止宣称

### 6.1 必须先同步的正式材料

正式机器语义须先同步 0050、3100、3200、3300、4250、4260、5090—5110、5210、5230—5250、5300、8100 和规范目录；6100、6140、6150 保持其现有安全 / 服务账分离边界。C031 只登记漂移和冻结范围，不替代这些正文。

### 6.2 待实现能力注记与最早代码叶

以下是实现前必须显式标注为`待实现`的能力，不是本设计授予的代码白名单：

| 待实现能力 | 目标承载面 | 当前边界 |
| --- | --- | --- |
| 初次 / 后继统一筹办来源联合 | 任务筹办 transport | 复用同一工作线程和消息，不建平行队列 |
| 多方法—逐条件组发现 DTO / 结果消息 | 方法查找 provider 与筹办协议 | 值式投影，不建候选或条件组账 |
| manager 独立读回与发现路由 | 任务管理线程 | 只路由，不能选择 / 写需求 |
| `筹办发现缺口定位`消息与 self fresh 确认 | 既有自我治理消息协议 | 不建第二通道，确认不直写需求 |
| 自我确认来源联合 | 既有任务子目标承接记录 | 需求领域仍是 D 的唯一写入 / 复用者 |
| 执行前 hard filter、比较、选择和冻结 producer | task owner 组合入口 | 复用选择记录 / 路径 / 实例 / 冻结，不建代价账 |

最早能独立施工的叶应是“初次 / 后继统一筹办来源 + 多方法—条件组发现 DTO / 消息 + manager 独立读回”这一组同 owner 合同。它必须先完成正式语义、公开 DTO、provider、manager 读回和消息幂等的闭环；不能把下游 self 消息、需求消费或执行冻结消费者预先登记为多层待激活计划。

在该叶已实现并重新 S0 后，才依次形成：

```text
manager -> self 发现定位与 self 确认的承接生产者
-> 执行前可用性过滤、选择与既有冻结 producer
-> 合法消费者的端到端验证
```

### 6.3 不得宣称

```text
当前筹办 provider 已经满足新口径
已有任务缺口消息已经可把方法 / 条件交给 self
已有子目标记录已由 self 确认链生产
服务根值可参与自动代价比较
本设计输入已经成为正式规范、可执行计划或代码能力
```

## 修订记录

| 日期 | 版本 | 修订内容 |
| --- | --- | --- |
| 2026-09-02 | v0.2 | C031 正式规范收口后转为正式详细设计：补条件组语义缺口 fail-closed、待实现能力注记、异主 WIP 保护及最早代码叶门禁。 |
| 2026-09-02 | v0.1 | 按 C031 和正式 HEAD 只读扫描，形成筹办发现、self 确认需求和执行前选择的最小设计输入。 |
