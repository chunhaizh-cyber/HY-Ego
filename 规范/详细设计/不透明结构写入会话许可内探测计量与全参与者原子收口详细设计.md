# 不透明结构写入会话许可内探测计量与全参与者原子收口详细设计

编号：CORE-SESSION-S5-D1

日期：2026-07-17

设计裁决：JY-382

执行计划：#291 / DQ-183

最终回归阶段：759

状态：JY-382 双窗口修订完成 / #291、DQ-183 已派发待执行回执 / E116 设计派发 / 当前代码尚未具备本设计接口 / 不构成 O1-S2 代码许可

## 1. 目标

本设计补齐 E115 / #290 已确认的两类通用底座缺口：

```text
第一笔候选写入前，同一不透明会话不能按任意物理主键点查，
也不能形成指定节点类型与索引绑定的一致值式投影。

会话按批确认后若后批失败，已确认写集不能反向撤销；
外部参与者确认失败也不能撤销已确认会话，故障注入只覆盖确认前。
```

完成后只允许声明：通用许可内查询、可逆确认、全参与者原子收口和撤销失败隔离已按 #291 验证。不得声明节点 15、关系 18、O1 身份 / 容量、用途观察发布、HYSNAP01、统计、学习、生产接线或旧能力迁移已实现。

## 2. 依据

```text
AGENTS.md
规范/000_项目规则总纲.md
规范/仓库与服务分层事务边界规范.md
规范/代码文件建立归属与模块命名规范.md
规范/详细设计/不透明结构写入会话材料能力扩展详细设计.md
规范/详细设计/权威用途观察结构承载与事务发布恢复详细设计.md
流程图/20260717_不透明结构写入会话许可内探测计量与全参与者原子收口代码逻辑流程图_v0.1.md
实施记录/20260717_CAUSAL-USE-O1-S2-D1_权威用途观察结构承载与事务发布恢复实施前设计审计矩阵.md
海中鱼巣/核心/会话.结构写入.ixx
海中鱼巣/核心/执行器.结构写入.ixx
海中鱼巣/核心/节点仓库.h/.cpp
海中鱼巣/核心/主信息仓库.h/.cpp
海中鱼巣/核心/关系仓库.h/.cpp
海中鱼巣/核心/索引仓库.h/.cpp
海中鱼巣/核心/结构事务接线.数据.h
海中鱼巣/核心/协调.结构事务.ixx
海中鱼巣/领域/特征值服务.h
海中鱼巣/领域/参与者.特征值原始材料.ixx
海中鱼巣/装配.运行期业务.ixx
```

## 3. 当前事实与预期接口分离

### 3.1 已验证当前事实

1. `会话.结构写入.ixx` 已有同许可完整关系正反查、节点 / 主信息 / I64 读取和已知预期节点的写后主键绑定匹配。
2. 会话没有“任意物理主键 -> 空闲或完整节点”的查询结果；仓库 `optional<节点句柄>` 不能作为会话业务结果直接暴露。
3. 节点仓库只有全类型有效总数；索引仓库只有全局有效主键总数和单节点反向主键组，没有指定节点类型与全部索引绑定的同许可投影。
4. 节点 / 主信息候选记录在独占许可内已经存在于仓库表；候选确认当前只切换能力阶段，不改变仓库记录本体。
5. 当前候选撤销只接受未确认阶段；当前已发布关系变更撤销只接受持有阶段。
6. `结构写入会话::完成提交` 按节点、主信息、关系变更逐批确认，后批失败直接结束；`完成撤销` 只接受写入中。
7. 执行器当前先完成会话提交，再完成参与者提交；参与者失败时不反向撤销会话。
8. 普通四仓库读取在接域后先取得结构共享许可，独占会话结束前不能进入；同线程无令牌重入也被协调器拒绝。
9. `特征值服务.h` 的七个公开读取当前先读节点、释放结构许可，再取得 `原始值锁_`；侧表锁内又调用无令牌主信息读取，形成“侧表共享锁 -> 结构共享许可”的反向锁序。唯一运行期装配构造特征值服务时也没有传入结构接线。
10. 协调器当前在阻塞取得 `结构锁` 前先登记活动记录；撤销失败若只拒绝隔离后新登记请求，隔离前已登记但尚未授予的等待请求仍可能在原独占许可释放后取得锁。

### 3.2 本设计的假定后继接口

下列名称是 #291 的实施合同，不是当前代码事实。执行前必须逐签名复核；若实现需要改变语义或扩大允许文件，退回设计，不得临时改义。

```cpp
结构物理主键读取结果 读取物理主键当前绑定(std::uint64_t 主键);
结构类型索引投影结果 读取有效节点与索引投影(节点类型 类型);

std::vector<节点记录> 读取指定类型有效节点记录组(
    节点类型 类型,
    const 结构事务令牌& 令牌) const;

std::vector<主键绑定记录> 读取全部主键绑定组(
    const 结构事务令牌& 令牌) const;

特征值服务(主信息仓库& 主信息, 节点仓库& 节点, 结构事务接线 接线 = {});

bool 标记撤销失败隔离(
    const std::shared_ptr<void>& 状态,
    const 结构事务令牌& 当前独占令牌);
```

结果与记录实体优先归属既有唯一所有者：主键绑定记录归索引仓库头，查询结果归 `海中鱼巣.核心.会话.结构写入` 模块。#291 默认不新建代码文件；若实际 C++ 可见性迫使新建数据结构文件，必须保持代码零修改并退回设计补文件分类、工程登记和允许范围。

## 4. 权力边界

```text
协调层：许可签发、线程 / 纪元验证和撤销失败域隔离。
执行器：阶段编排、参与者顺序、失败收口和唯一许可释放。
会话：通用查询结果、精确写集、确认进度、发布 / 撤销阶段。
仓库：本仓锁、值式读取、精确确认和精确逆操作。
特征值服务：接域生产侧表入口的结构许可外门禁；既有业务签名保持不变。
参与者：服务内部侧表写集、内部锁、可逆确认和无失败发布。
数据操作：把通用值式查询解释为本领域写前快照。
领域服务：业务准入和成功 / 拒绝语义。
```

核心会话不得出现 `O1`、`用途观察`、`FNV`、`角色 1`、`1024`、`2048`、`65536` 或 `128 MiB` 的业务解释。领域服务、数据操作公开入口不得接收或返回仓库、原始令牌、许可、会话或查询投影。

## 5. 许可内物理主键点查

### 5.1 结果状态

`结构物理主键读取结果` 至少使用强类型状态：

```text
空闲：当前物理键无绑定；正常探测结果。
已绑定：返回一个当前有效完整节点句柄。
入口拒绝：主键为零、线程 / 阶段 / 决定不允许或会话已有失败。
内部不一致：索引命中悬空节点、句柄版本 / 仓库不一致或正反记录矛盾。
```

只有 `已绑定` 可以携带节点；`空闲` 不携带节点且不得登记首次失败。`内部不一致` 必须登记会话首次失败并禁止后继写入。不得继续用一个空 `optional` 同时表示空闲、许可异常和悬空目标。

### 5.2 查询门禁

点查只允许同时满足：

```text
调用线程等于会话创建线程。
会话阶段为写入中。
决定仍为未决定。
不存在首次失败。
主信息、节点、关系、索引、关系变更和候选 I64 写集均为空。
主键非零。
```

任一门禁不满足时零仓库写入。查询不登记写集、不取得第二许可、不缓存仓库引用，也不允许请求提交后继续读取。

### 5.3 完整性复核

索引命中后，会话必须用同一令牌：

```text
读取目标完整节点记录。
确认句柄仓库编号、节点编号和版本精确匹配。
读取该节点反向主键组。
确认当前物理键恰在反向组中且无重复。
```

索引空闲为正常结果；命中但目标不可读、版本漂移或反向缺键均为内部不一致。

## 6. 类型—索引值式投影

### 6.1 仓库原语

节点仓库增加指定类型当前有效记录的值式副本；索引仓库增加全部正向主键绑定的值式副本。两者均只接受会话内部原始令牌，返回后不保留锁或引用。

仓库锁规则：

```text
每次只持有一个仓库锁。
先复制节点记录组并释放节点锁。
再复制索引绑定组并释放索引锁。
最后由仍持结构独占许可的会话做跨仓库比较。
```

不得同时持有节点仓库锁和索引仓库锁，不得把跨仓库一致性判断下沉到任一仓库。

### 6.2 会话投影

`结构类型索引投影结果` 至少承载：

```text
强类型状态：已形成 / 入口拒绝 / 内部不一致。
指定类型当前有效完整节点组。
指向这些节点的物理主键—完整节点句柄绑定组。
```

会话还必须用每个节点现有反向主键组逐一复核全部正向绑定：

```text
每个正向绑定目标都是当前有效完整节点。
每个正向绑定恰在目标节点反向主键组中。
每个反向主键恰有一条正向绑定且指回同一完整节点。
指定类型投影不混入其它类型节点。
主键、节点或完整绑定不重复。
```

任一悬空、重复、错类型或正反不对称都返回内部不一致并登记首次失败；不得把不一致记录过滤后继续形成“干净投影”。

### 6.3 后继 O1 使用边界

#291 只形成通用投影。后继 O1 专用数据操作在同一会话中才能：

```text
按关系 18 角色 1 完整反查同任务记录。
对节点 15 读回 15 槽和角色结构。
重建 36 字节身份、探测序号和最终物理键。
证明每个节点 15 恰有一个合法 O1 所有者声明。
计算已保留规范字节 = 合法 O1 所有者数量 × 2048，并做溢出检查。
```

固定预留计算是 O1 业务设计，不进入通用会话，也不新增字节账或第二索引。

## 7. 会话状态机

会话状态固定为：

```text
写入中
-> 确认中
-> 已确认待发布
-> 已发布

写入中 / 确认中 / 已确认待发布
-> 撤销中
-> 已撤销

撤销中
-> 撤销失败隔离
```

当前“确认失败直接进入已结束”必须删除。确认失败只记录首次失败、确认进度和“需要撤销”；写集、候选能力和原令牌必须保留到撤销完成或域隔离。

请求提交只表达业务回调已经完成写集和读回，不是发布。`完成确认` 只把全部写集推进到“已确认待发布”，也不是发布。`完成发布` 只在执行器已确认所有参与者后调用，必须是无分配、无回调、无日志、无故障点、无可失败返回的阶段封口。

## 8. 仓库可逆确认合同

### 8.1 节点与主信息候选

同一候选能力绑定：创建仓库、原独占令牌和完整句柄。候选撤销必须接受：

```text
未发布：擦除候选记录，进入已撤销。
已确认待发布：按同一完整句柄精确擦除，进入已撤销。
已撤销：幂等返回候选已撤销。
已发布、已移动、令牌失效或版本不一致：拒绝或内部不一致。
```

确认只改变能力阶段，不清除反向材料。撤销不得调用普通删除，不得扫描编号，不得回拨 `下个编号` 或创建序号高水位。失败候选烧掉的编号允许形成空洞；完成标准是记录不可读且无关系、索引、侧表或容量占用。

### 8.2 新关系与索引

新关系继续由完整关系句柄逆序严格删除；索引继续由“主键 + 预期完整节点句柄”严格删除。每一逆操作都必须执行，不能因前一个失败短路。

### 8.3 已发布关系变更

关系变更能力的撤销必须同时接受“持有”和“已确认待发布”，按写后完整记录精确匹配后恢复写前完整记录。只有 `已发布` 才永久关闭本次撤销能力。

## 9. 事务参与者四段协议

`结构写入事务参与者` 统一为：

```text
准备提交(结构提交准备只读视图)
确认待发布()
完成发布() noexcept
完成撤销()
```

参与者内部状态至少为：

```text
未准备 -> 已准备 -> 已确认待发布 -> 已发布
未准备 / 已准备 / 已确认待发布 -> 已撤销
撤销失败 -> 撤销失败隔离
```

约束：

1. 准备阶段可分配、写入未发布侧表材料并取得内部独占锁。
2. 确认待发布可以失败，但失败前后仍持有锁和精确反向材料。
3. 完成发布只清理撤销材料、切换阶段并释放内部锁，必须 `noexcept`，不得包含分配、业务回调、日志、故障注入或任何可能失败的结构复核。
4. 完成撤销从已准备或已确认待发布精确恢复侧表前态并释放内部锁；已撤销幂等。
5. 参与者不得取得仓库、原始令牌、结构许可或可写会话，也不得调用仓库入口。

准备阶段的非成功必须使用强类型来源：只有资源不足或许可 / 侧表竞争等设计允许拒绝，且尚未产生任何可读半结构时，才保留为逻辑内返回；抛出、参与者内部状态不一致、准备返回“成功”但锁 / 写集 / 反向材料不完整，或准备后读回异常，均归为追根因解决。两类非成功都必须先撤销参与者，再撤销会话；任一撤销失败进入域隔离。

真实适配至少覆盖当前特征值原始材料参与者；只用假参与者不能形成 #291 完成声明。

## 10. 锁序与唯一可见点

写锁顺序固定为：

```text
结构独占许可
-> 参与者侧表独占锁
```

接域生产侧表读写固定为：

```text
结构共享许可
-> 参与者侧表共享锁
-> 值式读取
```

`特征值服务` 必须保存可选结构事务接线；唯一 `运行期业务装配` 必须传入与四仓库相同的正式接线。接域模式下，创建、写入和七个公开读取都先取得对应结构许可，再使用带令牌仓库入口并取得 `原始值锁_`；不得在持有侧表锁后调用无令牌仓库入口。既有两参数构造和无接线行为只保留给当前隔离自检 / 未接域兼容上下文，不计入 #291 原子可见性完成声明，也不得出现在唯一运行期装配。

不得保留“直接取侧表锁而不经过结构许可”的接域生产旁路。参与者可以在结构许可释放前先完成发布并解锁侧表，因为普通读者仍无法取得结构共享许可；结构独占许可最后释放才是四仓库和侧表统一可见点。

## 11. 执行器顺序

生产入口固定为：

```text
取得结构独占许可
-> 构造会话
-> 同步业务回调、查询、写入和读回
-> 请求提交
-> 参与者准备提交
-> 会话完成可逆确认
-> 参与者确认待发布
-> 进入无失败最终发布区
-> 会话完成发布
-> 参与者完成发布 noexcept
-> 结构独占许可最后释放
-> 返回成功
```

任一最终发布前失败固定为：

```text
保存原失败结果
-> 参与者完成撤销
-> 会话逆序撤销全部写集
-> 读回前态
-> 全部成功才释放许可并返回原失败
```

执行器必须继续尝试所有撤销项并汇总结果；不得使用会掩盖剩余逆操作的提前返回或短路表达式。

## 12. 撤销失败隔离

任一参与者或会话逆操作失败时：

```text
继续尝试其它逆操作并记录全部失败位。
在结构许可析构前通知协调层把当前事务域置为撤销失败隔离。
协调层从此拒绝该域所有共享和独占许可。
隔离前已经登记但尚未取得结构锁的请求，在获锁后、返回许可对象前必须在活动锁内二次复核隔离标志；若已隔离，则移除自身活动记录、释放刚取得的结构锁并返回无效许可。
触发公共逻辑错误诊断：写独立逻辑错误日志并弹窗，仅供人读。
停止依赖该域的运行期路径；只能走重启和正式恢复，不允许在线扫描修补。
```

物理互斥量可以在隔离状态登记后由当前许可对象正常析构，但协调层不得再签发有效许可；新请求在登记前拒绝，已登记等待者在获锁后二次复核并自清理。因此普通读者不能在半结构上继续运行。隔离标志、活动表登记和二次复核必须受同一活动锁保护；不得以 `std::terminate`、死锁或永久持有线程栈对象冒充隔离合同。

## 13. Debug 故障矩阵

Debug 专用故障点：

| 编号 | 位置 |
| --- | --- |
| F01 | 每一个节点候选确认后 |
| F02 | 节点批确认完成后 |
| F03 | 每一个主信息候选确认后 |
| F04 | 主信息批确认完成后 |
| F05 | 每一个已发布关系变更确认后 |
| F06 | 会话全部写集确认完成后 |
| F07 | 参与者准备返回失败或抛出 |
| F08 | 参与者确认待发布返回失败或抛出 |
| F09 | 参与者已确认待发布后、最终发布前 |

撤销失败点：

| 编号 | 位置 |
| --- | --- |
| R01 | 参与者反向撤销 |
| R02 | 已发布关系变更恢复写前记录 |
| R03 | 索引严格删除 |
| R04 | 新关系严格删除 |
| R05 | 节点候选撤销，包括已确认待发布 |
| R06 | 主信息候选撤销，包括已确认待发布 |

故障控制只存在于 `HY_EGO_ENABLE_STRUCTURE_COMMIT_FAULT_SELF_TEST` 的 Debug 自检路径。生产 `执行` 签名不得增加故障参数；Release 不编译故障入口和符号。最终发布区内禁止任何故障点。

## 14. 非成功返回二分

| 场景 | 分类 | 收口 |
| --- | --- | --- |
| 接线无效、仓库缺失、许可竞争 | 逻辑内返回 | 写前拒绝，零变化 |
| 查询主键为零、类型未定义、查询阶段不允许 | 逻辑内返回 / 入口拒绝 | 零变化 |
| 物理键空闲 | 正常查询结果 | 不记失败 |
| 悬空索引、正反不对称、句柄版本矛盾 | 追根因解决 | 登记首次失败，禁止写入 |
| 业务回调合法拒绝或未请求提交 | 逻辑内返回 | 撤销未确认写集 |
| 参与者准备因资源 / 竞争拒绝且零可读半结构 | 逻辑内返回 | 保留原拒绝；参与者和会话全撤销 |
| 参与者准备抛出、内部不一致或准备后材料异常 | 追根因解决 | 保留原错误；参与者和会话全撤销 |
| 任一确认点或参与者确认失败 | 追根因解决 | 全参与者和会话逆序撤销 |
| 撤销后前态读回不一致 | 追根因解决 | 域隔离，停止运行期 |
| 任一撤销操作失败 | 追根因解决 | 域隔离，拒绝后续许可 |
| 最终发布区出现可失败操作 | 设计 / 实现内部不一致 | 不得发布 #291 |

## 15. #290 映射

| #290 项 | #291 正式产物应提供的证据 |
| --- | --- |
| A07 | 任意物理主键许可内点查 + 完整节点复核；完整关系反查继续复用既有会话入口 |
| A08 | 同一会话可连续完成最多 1024 次点查，不取得第二许可；1024 是 O1 数据操作的上层循环，不写入核心 |
| A09 | 指定类型节点与全部索引绑定值式投影；O1 数据操作据此重建所有者并按固定 2048 字节计算预留 |
| A10 | 部分确认失败后会话保留写集并完成全量逆序撤销 |
| A11 | 全部查询经不透明会话完成；领域与数据操作公开签名无仓库 / 令牌穿透 |
| A12 | 参与者和会话全部确认后才进入无失败发布区；许可释放是唯一可见点 |
| A13 | F01—F09、R01—R06 和并发读者可见性形成正式证据 |

#291 完成不自动让 #290 通过。设计角色必须从 #291 正式提交逐签名复核；范围一致后再发布同一 #290 为重新待执行，由执行角色独立形成第二次审计矩阵。

## 16. 验收

### 16.1 查询

1. 空键、有效绑定、悬空目标和正反不对称严格分流。
2. 同一会话连续 1024 次点查只取得一次独占许可，写集始终为空。
3. 指定类型投影覆盖零、一、多节点；零键、一键、多键、错类型和悬空绑定有唯一结果。
4. 第一笔候选后、请求提交后、错误线程和已有首次失败时查询均拒绝。
5. 静态扫描证明核心查询无 O1 业务常量，领域公开面无仓库、令牌或许可。

### 16.2 原子收口

1. F01—F09 每一项失败后，可读权威记录、有效计数、关系、索引、侧表和容量占用等于前态；失败候选完整句柄不可读。
2. 允许编号 / 创建序号高水位前进，不要求回拨或复用句柄。
3. 成功路径中接域 `特征值服务` 的普通读者在许可释放前不得完成，释放后一次看到完整四仓库与侧表；唯一运行期装配不得保留两参数无接线构造。
4. R01—R06 每一项使事务域进入隔离；隔离后新请求在登记前拒绝，隔离前已登记等待者获锁后二次复核后返回无效，逻辑错误诊断触发。
5. 最终发布区静态和运行审计证明无分配、回调、日志、故障点和可失败返回。
6. 当前特征值原始材料参与者完成真实适配，`特征值服务` 接域生产读写按结构许可 -> 侧表锁收口；既有结构写入、仓库、关系换代、特征体系和运行期事务自检全部回归。

### 16.3 构建与治理

```powershell
msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m
msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Release /p:Platform=x64 /m
.\x64\Debug\海中鱼巣.exe --self-test-exit
.\x64\Release\海中鱼巣.exe --self-test-exit
python .\tools\check_specs.py --strict
git diff --check
```

最终 Debug 完整自检连续 20 / 20 通过后才登记阶段 759。Release 必须零登记 `CORE-SESSION-S5`、759 和故障入口符号。

## 17. 完成声明六项证据

| 维度 | 证据 |
| --- | --- |
| 输入入口 | 已登记数据操作同步回调；查询发生在第一笔写入前，写入经同一会话请求提交 |
| 核心处理 | 许可内点查、类型—索引投影、可逆确认、参与者四段协议、完整撤销和域隔离 |
| 结构承载 | 现有四仓库、精确候选 / 关系 / 索引写集、参与者侧表与协调器域状态；不新增业务事实账 |
| 输出变化 | 通用查询结果、会话 / 参与者状态机、Debug 故障矩阵和阶段 759 |
| 调用路径 | 数据操作 -> 执行器 -> 独占会话 / 参与者 -> 仓库 -> 无失败发布 -> 许可释放 |
| 验证方式 | A01—A24、F01—F09、R01—R06、Debug / Release、Release 隔离、20 / 20、strict 与范围扫描 |

## 18. 禁止宣称

本设计和 #291 完成后仍不得宣称：

```text
O1-S2 已实现或 #290 已通过。
节点 15、关系 18、15 槽、O1 索引 / 容量或 HYSNAP01 已实现。
所有历史兼容写路径已经迁移、私有化或删除。
崩溃断电恢复、持久化事务或跨进程原子性已经实现。
统计、学习、晋级、方法代际、生产接线或自动行动已经完成。
旧鱼巢能力迁移完成。
```
