# 真实闭环串联优先目标详细设计

编号：REAL-LOOP-D1
日期：2026-07-09
状态：已生成 / 后置详细设计 / 可生成待确认计划 / 未入队 / 未改代码 / 未构建 / 未迁移旧函数

## 1. 定位

本详细设计承接用户口径：当运行宿主与多线程消息队列专项计划完成后，下一批优先目标应把已完成的分散能力串成更真实的闭环。

目标链路为：

```text
需求
-> 任务承接
-> 方法候选 / 选择
-> 动作请求
-> 动态记录
-> 任务回执
-> 需求结算
-> 非权威缓存 / 事件日志 / 只读显示材料
```

本设计不是代码实施许可，不声明真实闭环、运行宿主线程、自我循环、自我苏醒、旧能力迁移、完整任务状态机、完整方法执行系统或完整缓存 / 事件日志 / 显示系统完成。

## 2. 前置门禁

本设计生成后可以生成待确认计划，但该计划不得确认入队，除非满足：

```text
运行宿主与多线程消息队列专项计划完成。
THREAD-S0 到 THREAD-S6 已形成正式实施记录或真实阻塞记录。
运行宿主线程、任务管理线程、任务工作线程、缓存统计线程和事件日志线程第一轮边界已按确认切片验证。
消息和队列仍被证明不承载机器事实。
线程仍被证明不作为动作来源。
```

外设采样材料线程可以保持模拟或空材料壳；真实 D455、体素和外设接入不作为本目标前置。

## 3. 依据

```text
AGENTS.md
规范/000_项目规则总纲.md
规范/001_规则迁移清单.md
规范/验收/最小闭环验收用例.md
计划/20260709_运行宿主与多线程消息队列专项计划_v0.1.md
规范/详细设计/运行宿主与多线程消息队列详细设计.md
流程图/20260709_真实闭环串联优先目标流程图_v0.1.md
规范/详细设计/方法候选召回与选择代码逻辑详细设计.md
规范/详细设计/方法执行动作入口代码逻辑详细设计.md
规范/详细设计/动态记录输出结果场景代码逻辑详细设计.md
规范/详细设计/任务回执实际结果状态结果回写代码逻辑详细设计.md
规范/详细设计/需求结算代码逻辑详细设计.md
规范/详细设计/非权威缓存统计代码逻辑详细设计.md
规范/详细设计/事件日志审计材料代码逻辑详细设计.md
规范/详细设计/显示层只读代码逻辑详细设计.md
项目记忆/当前状态.md
项目记忆/Codex任务队列.md
```

## 4. 串联原则

1. 需求目标仍由目标状态承载。
2. 任务承接必须复核需求、主体、目标宿主、场景和目标状态。
3. 方法候选 / 选择只在已确认边界内写任务选择方法关系，不把候选材料当成功事实。
4. 动作请求来自任务选择方法和方法动作入口，不来自线程、Tick、日志标签或控制台命令。
5. 任务工作线程只能调用领域服务入口，不能裸写仓库。
6. 动态记录必须有存在主体、场景归属和发生时间戳。
7. 任务回执和需求结算必须回到任务服务、需求服务、状态服务、动态服务和因果服务复核。
8. 非权威缓存、事件日志和只读显示材料只能观察，不裁决。

## 5. 目标结构

| 环节 | 权威结构 | 非权威 / 人读材料 | 第一轮目标 |
| --- | --- | --- | --- |
| 需求 | 需求节点、目标状态关系 | 显示只读材料 | 读取或创建一个目标状态需求 |
| 任务承接 | 任务节点、任务到需求关系、任务状态 | 任务候选材料 | 生成可调度任务材料 |
| 方法候选 / 选择 | 任务选择方法关系 | 候选只读材料、缓存候选 | 选出一个保守方法入口 |
| 动作请求 | 方法动作入口、动作规格、执行桥请求材料 | 请求消息 | 生成动作请求但不让消息成事实 |
| 动态记录 | 实例动态节点、状态变化材料 | 动态候选材料 | 记录实际动作结果动态 |
| 任务回执 | 实际结果状态关系、完成状态关系 | 回执消息 | 回写任务实际结果状态 |
| 需求结算 | 结算状态、来源任务、动态证据、可选因果引用 | 结算观察材料 | 复核目标状态与实际状态 |
| 缓存 / 日志 / 显示 | 无权威裁决 | 非权威缓存、事件日志、只读显示材料 | 观察闭环结果，不反向改事实 |

## 6. 后续切片建议

| 切片 | 目标 | 状态 |
| --- | --- | --- |
| REAL-LOOP-S0 | 运行宿主专项完成后事实复核 | 待前置完成后确认 |
| REAL-LOOP-S1 | 闭环样例输入和结构证据矩阵 | 待确认 |
| REAL-LOOP-S2 | 需求到任务承接到方法选择串联 | 待独立代码切片 |
| REAL-LOOP-S3 | 方法执行动作请求到动态记录串联 | 待独立代码切片 |
| REAL-LOOP-S4 | 动态到任务回执到需求结算串联 | 待独立代码切片 |
| REAL-LOOP-S5 | 非权威缓存、事件日志、只读显示材料观察串联 | 待独立代码切片 |
| REAL-LOOP-S6 | 闭环验收、拒绝路径和禁止宣称扫描 | 待独立验证切片 |

## 7. 验收矩阵

| 编号 | 验收项 | 通过标准 |
| --- | --- | --- |
| REAL-LOOP-A1 | 前置门禁 | 运行宿主与多线程消息队列专项已完成且记录齐全 |
| REAL-LOOP-A2 | 需求目标 | 需求目标状态可读，I64 只作为状态值材料 |
| REAL-LOOP-A3 | 任务承接 | 任务承接材料完整，缺材料拒绝且不写半结构 |
| REAL-LOOP-A4 | 方法选择 | 任务选择方法关系唯一可读，不证明方法成功 |
| REAL-LOOP-A5 | 动作请求 | 动作请求由任务方法关系和方法动作入口生成 |
| REAL-LOOP-A6 | 动态记录 | 动态有主体、场景、时间戳、前值和后值 |
| REAL-LOOP-A7 | 任务回执 | 实际结果状态关系和完成状态按入口复核写入 |
| REAL-LOOP-A8 | 需求结算 | 结算状态能追溯目标状态、实际状态、来源任务和动态证据 |
| REAL-LOOP-A9 | 非权威观察 | 缓存清空、事件日志缺失或显示关闭不改变权威事实 |
| REAL-LOOP-A10 | 并发边界 | 消息清空、重复、过期、队列满不生成业务事实 |
| REAL-LOOP-A11 | 禁止项扫描 | 不接 SQL、ADO、WebView、D455、体素、真实外设或旧函数体 |

## 8. 完成声明边界

本详细设计生成后只能声明：

```text
真实闭环串联优先目标详细设计已生成。
它是运行宿主与多线程消息队列专项完成后的后置设计依据。
未改 C++，未运行构建，未入队，未迁移旧函数。
```

不得声明：

```text
真实闭环已实现。
自我循环或自我苏醒已完成。
运行宿主线程或任务工作线程已完成。
需求满足、任务完成、方法成功、动态因果、缓存统计、事件日志或显示系统已完整完成。
旧项目能力已迁移。
```
