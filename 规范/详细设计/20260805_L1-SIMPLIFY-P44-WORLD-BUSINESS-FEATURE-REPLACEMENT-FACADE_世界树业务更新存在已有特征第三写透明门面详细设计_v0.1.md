# L1-SIMPLIFY-P44 世界树业务更新存在已有特征第三写透明门面详细设计 v0.1

日期：2026-08-05

计划身份：`L1-SIMPLIFY-P44-WORLD-BUSINESS-FEATURE-REPLACEMENT-FACADE`

创建侧基线：`8a834fe30ba21a7fd3525ee313db3fd598e5f00d`

## 1. S0 与最早缺口裁决

正式基线 `main == origin/main == 8a834fe30ba21a7fd3525ee313db3fd598e5f00d`，0/0，Git index 空。`06597075..b9d8fab9` 精确只发布 P15 v0.7 三文档并把 P15 v0.6 登记为已退出、v0.7登记为待激活；`b9d8fab9..8a834fe3` 精确只把 P15 v0.7 单行由待激活改为可执行。两段均未修改 P32/P35/P41/P43、4200、世界树门面或实例特征批次公开合同。P15现有代码 WIP 仍由执行所有者保留，本叶不触碰。P0-FIX、路线输入和旧 WORLD-TREE 仍是其它所有者 WIP。

正式依据：0050、4030、4050、4170、4200；SELF-GOV-CLOSURE v0.2 第12节顺序4；`WORLD-TREE-P1` 业务图 F03；目标函数—数据需求矩阵的 L1 有限访问模式；P32、P35、P41、P42、P43 v0.1 冻结合同。

能力矩阵：

| 目标能力 | 唯一提供者 / 冻结合同 | P44 裁决 |
| --- | --- | --- |
| 三种完整事实读取门面 | P32 `世界树业务服务` | 原样保留 |
| 第一写创建存在并接纳 | P35 同一类 | 原样保留 |
| 第二写发布存在初始特征 | P41 同一类 | 原样保留 |
| 单项已有 I64 特征换代规格 | P42 | 由 P43 内部消费，门面不得直调 |
| 完整已有 I64 特征换代批次发布 | P43 `实际存在I64已有特征换代批次发布服务` | 第三写唯一 provider |
| 第三写统一世界业务门面 | 无 | **P44 是最早真实缺口** |
| 发布状态并整理动态、移动和移除 | provider 链未按本轮边界闭合 | 后继独立叶 |
| 治理世界事实白名单路由 | 尚未形成 | 等正式门面能力闭合后另叶设计 |

P43 已冻结完整请求/结果 DTO、同宿主同世界资格、连续全换代集合、0x0F 双账幂等、4170记录、一个 L1 v2 事务、发布后读回和全部非成功，因此第三写之前不存在批次读取、资格、定位、稳定编码、领域读回或调用方 DTO 缺口。门面若再读 P38、调用 P42、检查资格或映射状态，会形成第二事实解释者并违反4200依赖方向。

P15 v0.7 已修订 P43 的间接 L1 提交合同，但不改变 P44 的透明委托语义；P44 激活必须等待 P43 真实结果经交互接受并机械匹配。故不等待 P15/P42/P43代码实现即可冻结本叶，但状态必须保持待激活。

本叶不重复 P42/P43，不实现第四写及之后的状态/动作动态、移动、移除、治理白名单、因果、任务/结算、恢复装配、运行期接线、旧入口删除、集成验收或 STEP-5。

## 2. 目标与完成条件

原位扩展 P32/P35/P41 唯一 L2 `世界树业务服务`，新增4200六写中的第三项：

```cpp
实际存在I64已有特征换代批次发布结果 更新存在已有特征(
    const 实际存在I64已有特征换代批次发布请求& 请求) const;
```

函数只把调用方同一请求同步 const 借给 P43 `发布实际存在I64已有特征换代批次` 恰一次，并按值逐字段原样返回 P43 结果。门面不解释项目种类、合同/规则、批次、截止、宿主、定义、槽、写前值、来源、0x0F键、双摘要、4170记录、事务、读回、`L1发布已存在` 或状态。

完成必须同时证明：

- P32三读、P35第一写和P41第二写的签名、const、异常及一对一委托不变；
- P41三引用构造完整退出，四引用构造成为唯一合法构造；
- 第三写直接复用 P43 请求/结果，不新增 DTO、状态或转换层；
- 第三写只调用 P43恰一次，其它三个 provider及P42/P39/P15调用0；
- 成功、幂等、发布前非成功、发布后内部失败和异常均保留 P43 原语义；
- P32/P35/P41回归、P44顺序530专项和完整验证通过。

## 3. 所有权、物理位置与依赖方向

未来代码修改固定为：

| 文件 | 唯一职责 |
| --- | --- |
| `海中鱼巣/领域/服务.世界树.ixx` | 原位扩展 P41 唯一类：构造追加 P43 必需引用，新增第三写透明委托。 |
| `海中鱼巣/领域/自检.世界树业务只读.ixx` | 只迁移 P32 三读专项构造夹具，证明三读不访问三个写 provider。 |
| `海中鱼巣/领域/自检.世界树业务创建存在.ixx` | 只迁移 P35 第一写专项构造夹具，证明第一写不访问两个特征 provider。 |
| `海中鱼巣/领域/自检.世界树业务发布初始特征.ixx` | 只迁移 P41 第二写专项构造夹具，证明第二写不访问 P43。 |
| `海中鱼巣/领域/自检.世界树业务更新已有特征.ixx` | 新建顺序530专项，验证第三写签名、一次委托、结果全等和零旁路。 |

依赖方向固定：

```text
后继治理 / 业务消费者
-> 世界树业务服务
   -> const 世界树快照读取服务&
   -> L1当前场景实际存在发布服务&
   -> 实际存在I64初始特征批次发布服务&
   -> 实际存在I64已有特征换代批次发布服务&
      -> P43内部的P15/P42/P38/P39链
```

P40 与 P43 位于同一正式实例特征服务模块；世界树模块复用 P41 的模块 import，只消费两个公开服务类和 DTO，不 import P15、P37—P39 内部数据操作/参与者，也不访问 L1、4170账、P42规格函数、仓库、SQL、锁或缓存。实例特征领域不得反向依赖世界树门面。

## 4. 最终类、构造迁移与公开函数

在 P41 目标类上形成：

```cpp
export inline constexpr std::uint32_t 世界树业务服务合同版本 = 1;

export class 世界树业务服务 final {
public:
    世界树业务服务(
        const 世界树快照读取服务& 快照读取,
        L1当前场景实际存在发布服务& 当前场景实际存在发布,
        实际存在I64初始特征批次发布服务& 初始特征批次发布,
        实际存在I64已有特征换代批次发布服务& 已有特征换代批次发布) noexcept
        : 快照读取_(快照读取),
          当前场景实际存在发布_(当前场景实际存在发布),
          初始特征批次发布_(初始特征批次发布),
          已有特征换代批次发布_(已有特征换代批次发布) {}

    世界树业务服务() = delete;
    世界树业务服务(const 世界树业务服务&) = delete;
    世界树业务服务& operator=(const 世界树业务服务&) = delete;
    世界树业务服务(世界树业务服务&&) = delete;
    世界树业务服务& operator=(世界树业务服务&&) = delete;

    完整世界存在快照读取结果 查询存在完整事实(
        const 完整世界对象读取请求& 请求) const;
    完整世界场景快照读取结果 查询场景完整事实(
        const 完整世界对象读取请求& 请求) const;
    完整世界快照组合结果 读取完整世界事实快照(
        const 完整世界快照组合请求& 请求) const;
    实际存在结果 创建存在并接纳到场景(
        const 当前场景实际存在创建请求& 请求) const;
    实际存在I64初始特征批次发布结果 发布存在初始特征(
        const 实际存在I64初始特征批次发布请求& 请求) const;
    实际存在I64已有特征换代批次发布结果 更新存在已有特征(
        const 实际存在I64已有特征换代批次发布请求& 请求) const;

private:
    const 世界树快照读取服务& 快照读取_;
    L1当前场景实际存在发布服务& 当前场景实际存在发布_;
    实际存在I64初始特征批次发布服务& 初始特征批次发布_;
    实际存在I64已有特征换代批次发布服务& 已有特征换代批次发布_;
};
```

P41三引用构造被四引用构造完整替代，不保留 overload。四个 provider 都是已公开门面能力的必需依赖；nullable P43、setter、函数对象、服务定位器或固定未实现第三写都会制造半装配服务。

`世界树业务服务合同版本` 保持1，并静态断言等于完整世界快照、P34当前场景创建、P40初始批次和P43换代批次合同版本。各事实所有者解释各自版本，门面不重复校验。

第三写为 `const`、非 `noexcept`。门面无可变状态；const成员函数可经保存的非const P43引用调用其非const发布函数。请求由调用方拥有，门面只在同步调用期间借用且不保存；返回值由调用方拥有。依赖对象由装配宿主拥有且寿命覆盖门面；本叶不修改装配。

## 5. 唯一函数算法

函数体固定为：

```cpp
实际存在I64已有特征换代批次发布结果 世界树业务服务::更新存在已有特征(
    const 实际存在I64已有特征换代批次发布请求& 请求) const {
    return 已有特征换代批次发布_.发布实际存在I64已有特征换代批次(请求);
}
```

调用前后不得增加：

- 对版本、批次、代次、项目、宿主、定义、槽、写前值、新值或来源的校验；
- P38预读、P42规格调用、排序、去重、摘要、本地键、写集、读回计划或草案形成；
- 状态映射、optional补造、项目重组、记录重建或`L1发布已存在`改写；
- try/catch、日志、计时、retry、sleep、fallback、旧入口回退或第二provider调用；
- 世界快照读取、P34/P40调用、缓存或跨调用状态。

P43 返回什么，门面逐字段返回什么，包括状态、批次、L1幂等键、事实代次、发布存在标志、持久状态、项目组和4170记录。异常若越过 P43 合同，门面不捕获改判，因此不声明 `noexcept`。

## 6. 既有五根、并发与生命周期

P32三读、P35第一写和P41第二写全部保持原函数体与一对一provider：三读只调用快照读取；第一写只调用P34；第二写只调用P40。新增P43引用不能被既有五根访问，第三写对快照/P34/P40调用为0。

门面不加锁、不排队、不重试。P43独占换代批次准入、幂等和事务，P40独占初始批次，P34独占存在创建，P29—P31独占读取。并发结果完全由对应provider决定；门面不建立跨请求顺序、跨P40/P43批次账或第二幂等身份。

构造只绑定引用，不读取、不恢复、不登记、不发布事实。未来装配必须先构造四个provider再构造门面；这是依赖合同，不授权本叶写运行期文件。

## 7. 验证矩阵

P44专项至少覆盖：

1. 世界树类、模块、合同版本和六个公开函数唯一；旧P1C-F/FW DTO及入口引用为0；
2. 四引用构造完整替代P41三引用构造；默认/复制/移动、指针、optional、setter和兼容overload均不存在；
3. 世界树、完整快照、P34、P40、P43五合同版本均为1并静态一致；
4. 第三写完整签名为const、非noexcept，请求/结果与P43精确相同；
5. 入口拒绝请求经门面与直接P43结果逐字段全等，P43调用恰1次；
6. P43的已提交、幂等读回、登记未加载、未找到、已退出、漂移、冲突、许可拒绝、未知格式、资源失败、内部不一致和发布后失败代表结果全等；无真实测试seam的成功分支以P43专项与源码闭包分账，不伪造provider；
7. 第三写不读取快照，不调用P34/P40/P42/P38/P39/P15、L1、仓库、SQL、材料、缓存、锁、日志或时钟；
8. 三读不调用三个写provider；第一写不调用快照/P40/P43；第二写不调用快照/P34/P43；第三写不调用快照/P34/P40；
9. P32顺序460、P35顺序480、P41顺序500全部既有矩阵不退化，构造夹具只增加真实P43引用；
10. 顺序530专项唯一，工程/filters/启动只登记新专项和薄包装，不接生产消费者；
11. 并发调用没有门面共享可变状态，结果只由选中的provider决定；
12. Debug/Release Rebuild、两配置完整自检、strict、MSVC模块扫描和精确白名单diff check通过。

## 8. 激活门禁与最大声明

保持 `待激活`，直至：

1. P32、P34、P35、P40、P41真实结果进入正式main并经交互接受，三读、第一/第二写、三引用构造和顺序460/470/480/500实际匹配；
2. P42、P43及其正式依赖链真实结果进入main，P43经交互接受，其类、请求、结果、函数、const/noexcept、状态和完整字段机械匹配；
3. P15替代版对P43间接依赖的接口已由P43实际结果门禁吸收，不要求门面直接读取或解释P15；
4. P32/P35/P41/P43释放世界树服务、三个既有专项、工程/filters/启动、顺序530、构建、自检和模块扫描资源；
5. 从新正式HEAD重做完整S0，确认全部`世界树业务服务`构造调用点均在白名单内，530未占用，旧P1C-F/FW未施工且无等价第三写门面。

若实际构造调用点、模块可见性、P41或P43签名/字段、异常边界或顺序漂移，保持待激活并退回计划支撑；不得用旧 DTO、P43旁路、nullable provider、兼容构造、状态映射或第二门面绕过。

完成后最多声明：P44白名单内源码候选使唯一`世界树业务服务`具备第三个真实写门面，能把完整实际存在I64已有特征换代批次请求一对一委托给P43并逐字段透明返回，同时保持P32三读、P35第一写和P41第二写不变。

不得声明：P32/P35/P41/P43本身由本叶实现、第四写及后续状态/动作动态、移动/移除、治理白名单、生产装配、运行期接线、旧入口退出、集成验收、STEP-4、STEP-5或治理闭环完成。
