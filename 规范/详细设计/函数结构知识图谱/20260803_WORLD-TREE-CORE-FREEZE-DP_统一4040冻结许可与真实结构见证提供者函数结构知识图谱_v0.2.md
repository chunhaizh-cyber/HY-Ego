# WORLD-TREE-CORE-FREEZE-DP 统一4040冻结许可与真实结构见证提供者函数结构知识图谱 v0.2

创建侧正式基线：`30dc5df188ecc0b2f1cb7e0c719fbe21dbdb1803`

```mermaid
flowchart LR
    PM["核心/许可.节点直接统一冻结.ixx\n最终ABI唯一所有者"] --> W["节点直接统一冻结见证"]
    PM --> S["节点直接统一冻结状态"]
    PM --> P["节点直接统一冻结许可\nmove-only RAII"]
    P --> OPAQUE["私有状态不透明指针\nF0=nullptr / R1原位实现"]
    PM --> PR["节点直接统一冻结许可取得结果"]

    TX["节点直接身份结构事务域"] --> ACQ["取得统一冻结许可"]
    TX --> OWN["冻结许可属于本域"]
    P --> ACQ
    P --> OWN

    STORE["节点直接类型化值仓库"] --> VIEW["节点直接类型化值冻结只读访问器"]
    VIEW --> PTR["私有非拥有仓指针\nF0不解引用"]
    VIEW --> COPY["复制全部已发布记录"]
    VIEW --> OBSERVE["观察已发布结构\n只返回见证/版本/数量"]
    P --> COPY
    COPY --> OUT["节点直接类型化值冻结读取结果"]

    DP["FEATURE-W-DP"] --> ACQ
    DP --> OWN
    DP --> COPY
```

## F0调用图

```text
取得统一冻结许可(any)
└─ 返回 {未实现, nullopt}

冻结许可属于本域(any)
└─ 返回 false

节点直接统一冻结许可::有效()
└─ 返回 false

节点直接统一冻结许可::读取见证()
└─ 返回 {0,0,0}

节点直接类型化值冻结只读访问器::复制全部已发布记录(any)
└─ 返回 {未实现, nullopt}

节点直接类型化值冻结只读访问器::观察已发布结构(any)
└─ 返回 {未实现, nullopt}
```

F0没有事务域成员读取、shared_mutex调用、仓导出、容器复制、SQL、日志或机器事实写入边。F0保留固定大小的不透明私有状态指针但不分配；R1不得通过新增许可成员改变对象布局。

## R1目标图与未决门禁

```mermaid
flowchart TD
    A["事务域真实状态"] --> B["运行期域身份\nFREEZE-R1-DOMAIN-ID待裁决"]
    A --> C["已发布代次"]
    A --> D["冻结规则版本1"]
    B --> E["统一冻结见证"]
    C --> E
    D --> E
    E --> F["RAII共享冻结许可"]
    F --> G["所属域不可伪造校验"]
    F --> H["类型化值仓共享副本"]
    F --> H2["复制后轻量结构观察"]
    I["真实结构版本\nFREEZE-R1-TYPE-VERSION待裁决"] --> H
    H --> J["见证+版本+数量+全部已发布记录"]
    H2 --> J2["见证+版本+数量\n不复制记录"]
```

| 未决身份 | 必须裁决 | 禁止补位 |
| --- | --- | --- |
| `FREEZE-R1-DOMAIN-ID` | 签发来源、唯一范围、失败、恢复 | 地址、随机猜测、调用方值、安装日志 |
| `FREEZE-R1-TYPE-VERSION` | 初值、推进粒度、溢出、恢复 | 4040代次、最大值版本、记录数量 |
| `FREEZE-R1-ISOLATION` | 损坏见证的隔离收口 | 静默返回普通竞争 |

## 生命周期

```mermaid
sequenceDiagram
    participant C as 统一冻结协调方
    participant T as 4040事务域
    participant P as 统一冻结许可
    participant V as 类型化值冻结访问器

    C->>T: 取得统一冻结许可(规则1)
    T-->>C: R1已形成许可 / F0未实现空
    C->>T: 冻结许可属于本域(P)
    C->>V: 复制全部已发布记录(P)
    V-->>C: 见证+结构版本+数量+值式副本
    C->>V: 观察已发布结构(P)
    V-->>C: 同一见证+结构版本+已发布记录数量
    Note over C,P: P只在调用栈持有，不保存到服务或线程
    C-->>P: 析构并释放共享冻结边界
```

## 禁止边

```text
许可模块 -X-> 类型化值仓/领域/SQL
类型化值冻结访问器 -X-> 自行取得第二许可
上层 -X-> 锁守卫/互斥/原子/仓容器/可写事务许可
结构版本 -X-> 最大记录版本/记录数量/SQL水位
统一冻结 -X-> 全仓万能冻结/外设冻结/全进程暂停
F0 -X-> FEATURE-W-DP门禁解除
```
