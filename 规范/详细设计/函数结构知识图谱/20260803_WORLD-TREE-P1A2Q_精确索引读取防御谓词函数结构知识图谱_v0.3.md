# WORLD-TREE-P1A2Q 精确索引读取防御谓词函数结构知识图谱 v0.3

日期：2026-08-03

设计身份：WORLD-TREE-P1A2Q

来源问题：`DRIFT-P1A2Q-LOWLEVEL-FIXTURE-PUBLISHED-GENERATION-UNFORMABLE`

## 1. 所有权与模块关系

```mermaid
flowchart LR
    T["节点直接类型化结构事务.数据.h\n现有恢复纯值 DTO"] --> F["自检.节点直接结构查询\n恢复式双目标夹具+专项根"]
    X["执行器.节点直接身份结构写入\n六仓恢复+代次开放"] --> F
    R["仓库.可重建索引\n结构化精确读取"] --> F
    Q["服务.节点直接结构\n稳定见证投影"] --> F
    F --> S["启动.应用程序\n顺序190专项登记"]
```

P1A2Q 自检只拥有隔离对象、恢复请求编排和断言。执行器继续拥有六仓恢复事务和代次开放；
各仓继续拥有权威记录。共享恢复 DTO、执行器和仓库接口均为只读依赖。

## 2. 生产读取调用图

```mermaid
flowchart TD
    Q["节点直接结构查询服务::读取当前索引"] --> P["事务域::取得读取许可"]
    Q --> G["索引仓::读取索引物理键结构化"]
    G --> A["索引仓::目标权威可读_"]
    Q --> N["节点仓::读取节点"]
    Q --> E["关系仓::读取关系"]
    Q --> V["查询服务::形成关系见证_"]
```

该图不因夹具修订而改变。查询服务共享许可覆盖代次、索引和目标复核。

## 3. 恢复式夹具调用图

```mermaid
flowchart TD
    F["形成已发布双目标索引夹具"] --> B["构造节点/关系稳定见证与两索引恢复读回"]
    B --> X["执行器::执行节点直接恢复事务"]
    X --> N["节点仓恢复候选"]
    X --> C["类型合同仓恢复候选"]
    X --> V["类型化值仓恢复候选"]
    X --> R["关系仓恢复候选"]
    X --> I["索引仓恢复候选\n节点目标+关系目标"]
    X --> D["幂等仓恢复候选"]
    D --> P["六仓完成发布"]
    P --> G["事务域::从零设置恢复事实截止代次并开放(1)"]
    G --> L["事务域::取得读取许可\n读取代次1"]
    L --> I1["索引仓结构化读取两键\n取得内部句柄"]
    I1 --> A1["节点/关系仓核对稳定见证"]
    A1 --> Q1["查询服务读取两键\n核对代次与公开见证"]
```

低层 `执行(callback)` 不在本图中，因为它不推进运行期事实代次，不能作为本夹具发布提供者。

## 4. 故障谓词调用图

```mermaid
flowchart TD
    S["运行节点直接结构查询专项自检"] --> F["形成已发布双目标索引夹具"]
    S --> SI["索引仓::测试_令下次结构化精确读取模拟内部损坏"]
    S --> QI["查询服务::测试_令下次当前索引读取模拟内部损坏"]
    SI --> RF["索引仓实例私有 atomic<uint8_t>"]
    QI --> QF["查询服务实例私有 atomic<uint8_t>"]
    RF --> G["读取索引物理键结构化\n合法键后 exchange(0)"]
    QF --> Q["读取当前索引\n合法键后 exchange(0)"]
```

资源/其它异常设置函数继续共用各自字段。非法键不消费；合法键后一次消费；第二次恢复正常。

## 5. 字段来源与结果追踪

| 材料 | 唯一来源 | 最终消费者 |
| --- | --- | --- |
| 两节点/关系稳定见证 | 专项固定值 | 恢复请求、权威与服务读回核对 |
| 节点/关系索引键 | 专项固定互异值 | 恢复索引历史、仓/服务读取 |
| 内部节点/关系句柄 | 恢复后的索引仓结构化读回 | 权威记录读取、夹具返回 |
| 非零代次 | 恢复执行器最后发布边界 | 读取许可与两服务结果 |
| 防御测试模式 | 被测实例的宏内原子字段 | 对应生产检查节点 |

| 不变量 | 机械证明 |
| --- | --- |
| 双目标夹具可形成 | 单次恢复事务已恢复，仓/服务两类读回均成功 |
| 非零代次真实归属本夹具 | 六仓发布后同一恢复许可从0开放到1 |
| 内部句柄不伪造 | 只从两条结构化索引记录提取 |
| 防御注入零事实变化 | 每项调用前后代次和六仓导出逐值相等 |
| 一次性恢复 | 首次具名失败后第二次同输入返回基线 |
| 宏关闭零实体 | 测试枚举、设置函数、字段和分支均不存在 |

## 6. 禁止边

```text
专项自检 -X-> 执行器::执行(callback) 形成本夹具
专项自检 -X-> 无关普通事务补推代次
专项自检 -X-> 仓库恢复候选/确认/发布入口
专项自检 -X-> 推导或伪造内部句柄
专项自检 -X-> 扩展共享 DTO 或生产提交服务
生产模块 -X-> 测试设置函数
```

## 7. 完成边界

本图谱完整替代 v0.2 的夹具关系解释；生产读取与故障谓词关系保持。
它不证明 P1A2Q 已实现、构建、运行或进入后继世界树能力。
