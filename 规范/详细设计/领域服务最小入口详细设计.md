# 领域服务最小入口详细设计

编号：020-D1
日期：2026-07-05
状态：已确认详细设计 / HR-001 第一轮领域服务入口

## 1. 定位

本详细设计对应：

```text
规范/000_项目规则总纲.md
规范/领域服务授权写入规范.md
规范/验收/最小闭环验收用例.md
```

领域服务负责把业务请求转为允许写入的结构变化。仓库只负责最小结构，领域服务负责业务准入、节点类型检查、生命周期检查和必要前置结构检查。

本文件只描述第一轮最小入口，不证明完整领域系统完成，不进入旧项目能力迁移。

## 2. 总体规则

第一轮领域服务入口必须满足：

```text
函数入口先排除无效句柄、错误节点类型、已删除节点和缺少前置结构。
拒绝路径只返回无效句柄、false、std::nullopt 或领域枚举结果。
拒绝路径不得写业务事实。
日志、弹窗和控制台输出只做人读诊断。
业务事实必须落到节点、关系、主信息值容器、状态节点、动态节点或因果引用节点。
```

目标领域服务分层：

```text
特征值服务
特征服务
存在服务
场景服务
状态服务
动态服务
二次特征服务
因果服务
世界服务
需求服务
任务服务
方法服务
语素服务
```

其中 `世界服务` 是基础信息聚合服务；特征值、特征、存在、场景、状态、动态、二次特征、因果是基础信息原子服务；需求、任务、方法是高级信息服务；语素服务只负责文本入口绑定。

当前第一轮代码仍保留以下最小集合：

```text
世界服务
特征服务
特征值服务
需求服务
任务服务
方法服务
因果服务
语素服务
```

当前最小集合不代表目标服务分层已经完成代码重构。

## 2.1 基础信息服务目标边界

基础信息服务边界以 `规范/基础信息服务分层规范.md` 为准。

| 服务 | 目标职责 | 当前实现状态 |
| --- | --- | --- |
| 特征值服务 | 只服务于特征系统的值承载，不作为通用值容器 | 第一轮已有最小 I64 写入 / 读取，权限边界待收紧 |
| 特征服务 | 管理特征入口 | 第一轮已有 `创建特征类型` |
| 存在服务 | 管理实际存在和虚拟存在 | 尚未独立拆出，当前由 `世界服务::创建存在` 承担壳入口 |
| 场景服务 | 管理场景和场景内临时实例状态 / 实例动态 | 尚未独立拆出，当前由 `世界服务::创建场景` 承担壳入口 |
| 状态服务 | 区分实例状态和抽象状态 | 尚未独立拆出，当前任务 / 需求服务直接创建状态节点 |
| 动态服务 | 区分实例动态和抽象动态 | 尚未独立拆出，当前因果服务直接创建动态节点 |
| 二次特征服务 | 管理关系性、程度性或复合判断前置特征 | 尚未独立拆出，当前由 `特征服务::创建二次特征` 承担壳入口 |
| 因果服务 | 管理因果引用、证据和抽象候选 | 第一轮已有最小因果引用入口 |
| 世界服务 | 编排基础信息原子服务 | 当前仍有直接创建基础信息、存在、场景的壳入口，后续需改为聚合编排 |

高级信息服务必须通过基础信息服务取得基础能力：

```text
需求服务通过存在服务、场景服务、特征服务、特征值服务和状态服务工作。
任务服务通过存在服务、场景服务、特征服务、状态服务和动态服务工作。
方法服务通过存在服务、场景服务、特征服务、特征值服务、动态服务和因果服务工作。
```

语素服务只绑定已存在信息节点，不创建基础信息、高级信息或临时实例状态 / 动态。

## 3. 函数总表

| 服务 | 函数 | 对应规范 | 功能 | 主要用途 | 使用范围 | 输入 | 输出 | 读写结构 | 验证方式 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 世界服务 | `创建基础信息` | 信息节点口径 | 创建基础信息通用节点 | 为暂未拆入具体类型的信息提供可绑定节点 | 第一轮世界入口 | 无 | 节点句柄 | 主信息记录、基础信息通用节点 | 返回有效基础信息通用节点 | 代码已实现 / 已复验 |
| 世界服务 | `创建存在` | 最小闭环种子 | 创建存在节点 | 表达自我或对象身份 | 第一轮闭环种子 | 无 | 节点句柄 | 主信息记录、存在节点 | 返回有效存在节点 | 代码已实现 / 已复验 |
| 世界服务 | `创建场景` | 最小闭环种子 | 创建场景节点 | 表达现实场景入口 | 第一轮闭环种子 | 无 | 节点句柄 | 主信息记录、场景节点 | 返回有效场景节点 | 代码已实现 / 已复验 |
| 特征服务 | `创建特征类型` | 特征结构入口 | 创建特征节点 | 表达目标特征类型 | 第一轮闭环种子 | 无 | 节点句柄 | 主信息记录、特征节点 | 返回有效特征节点 | 代码已实现 / 已复验 |
| 特征服务 | `创建二次特征` | 基础信息口径 | 创建二次特征节点 | 表达关系性、程度性或复合判断前置特征 | 第一轮语义修正 | 无 | 节点句柄 | 主信息记录、二次特征节点 | 返回有效二次特征节点，可作为语素对应信息节点 | 代码已实现 / 已复验 |
| 特征值服务 | `创建特征值` | 值承载 | 创建特征值节点 | 承载 I64 当前值 | 第一轮闭环种子 | 无 | 节点句柄 | 主信息记录、特征值节点 | 返回有效特征值节点 | 代码已实现 / 已复验 |
| 特征值服务 | `写入I64值` | 值容器 | 写入 I64 当前值 | 记录目标特征当前值或方法执行后的值 | 第一轮闭环 | 特征值节点、I64 值 | 成功 / 失败 | 特征值节点绑定主信息值容器 | 错误节点类型拒绝，写入后可读 | 代码已实现 / 已复验 |
| 特征值服务 | `读取I64值` | 值读取 | 读取 I64 当前值 | 需求结算和验收读回 | 第一轮闭环 | 特征值节点 | I64 或空 | 主信息值容器 | 未写值返回空 | 代码已实现 / 已复验 |
| 需求服务 | `创建需求` | 需求入口 | 创建空需求节点 | 最小需求身份入口 | 第一轮壳 | 无 | 节点句柄 | 主信息记录、需求节点 | 返回有效需求节点 | 代码已实现 / 已复验 |
| 需求服务 | `创建I64目标需求(目标值)` | 需求目标 | 创建带目标值的需求节点 | 第一轮目标值承载 | 第一轮壳 | I64 目标值 | 节点句柄 | 需求节点、主信息值容器 | 目标值可读 | 代码已实现 / 已复验 |
| 需求服务 | `创建I64目标需求(目标存在, 目标特征, 特征值服务, 当前特征值, 目标值)` | MC-02 | 创建带前置目标的 I64 需求 | 防止创建需求时偷偷补建目标结构 | 第一轮闭环 | 存在节点、特征节点、当前特征值、目标值 | 节点句柄或无效句柄 | 需求节点、主信息值容器 | 无效存在、无效特征、当前值缺失均拒绝 | 代码已实现 / 已复验 |
| 需求服务 | `结算I64目标` | MC-08 | 比较当前值和目标值 | 生成需求结算结果 | 第一轮闭环 | 需求节点、特征值服务、特征值节点 | 需求结算枚举 | 读取需求目标值、读取当前特征值 | 当前值 >= 目标值返回已满足 | 代码已实现 / 已复验 |
| 需求服务 | `记录结算状态` | MC-08 | 创建需求结算状态节点 | 将结算结果落为结构 | 第一轮闭环 | 需求节点、结算结果 | 状态节点或无效句柄 | 主信息记录、状态节点、I64 状态值 | 无效需求或未结算结果拒绝 | 代码已实现 / 已复验 |
| 任务服务 | `创建任务` | 任务入口 | 创建任务节点 | 最小任务身份入口 | 第一轮壳 | 无 | 节点句柄 | 主信息记录、任务节点 | 返回有效任务节点 | 代码已实现 / 已复验 |
| 任务服务 | `按需求创建任务` | MC-03 | 根据需求创建任务 | 需求承接任务 | 第一轮闭环 | 需求节点 | 任务节点或无效句柄 | 任务节点 | 无效需求拒绝 | 代码已实现 / 已复验 |
| 任务服务 | `记录任务状态` | MC-03 / MC-05 | 创建任务状态节点 | 记录任务状态变化 | 第一轮闭环 | 任务节点、I64 状态值 | 状态节点或无效句柄 | 主信息记录、状态节点、I64 状态值 | 无效任务拒绝，前后状态节点不同 | 代码已实现 / 已复验 |
| 方法服务 | `登记方法` | 方法入口 | 创建方法节点 | 登记手写方法入口 | 第一轮闭环 | 无 | 方法节点 | 主信息记录、方法节点 | 返回有效方法节点 | 代码已实现 / 已复验 |
| 方法服务 | `执行I64写入方法` | MC-05 | 执行前授权后直接改写特征值 | 执行手写改值方法 | 第二轮闭环增强 | 执行前授权、特征值服务、特征值节点、新值 | 成功 / 失败 | 特征值主信息值容器 | 授权存在，改变前值和改变后值可读；未授权拒绝 | 第一轮已复验 / 执行前授权待实现 |
| 因果服务 | `记录动态` | 动态入口 | 创建动态节点 | 最小动态身份入口 | 第一轮壳 | 无 | 动态节点 | 主信息记录、动态节点 | 返回有效动态节点 | 代码已实现 / 已复验 |
| 因果服务 | `记录动态(被改变目标, 改变前值, 改变后值, 可选来源动作)` | MC-06 | 带被改变目标和前后值材料创建动态 | 防止缺少目标或前后值时写动态；来源方法 / 动作可以为空 | 第二轮闭环增强 | 被改变目标节点、改变前值、改变后值、可选来源动作节点 | 动态节点或无效句柄 | 动态节点、前后值材料、目标引用 | 缺目标、缺前值或缺后值拒绝 | 第一轮已复验 / 前后值材料待实现 |
| 因果服务 | `记录因果引用` | 因果入口 | 创建因果引用节点 | 最小因果引用身份入口 | 第一轮壳 | 无 | 因果引用节点 | 主信息记录、因果引用节点 | 返回有效因果引用节点 | 代码已实现 / 已复验 |
| 因果服务 | `记录因果引用(来源动态)` | MC-07 | 带来源动态创建因果引用 | 防止缺少来源动态时写因果引用 | 第二轮闭环增强 | 来源动态节点 | 因果引用节点或无效句柄 | 因果引用节点、来源动态关系 | 缺来源动态拒绝 | 第一轮已复验 / 来源动态口径待实现 |
| 语素服务 | `创建语素入口` | 语素系统规范 | 创建语素入口并绑定信息节点 | 文本入口到项目身份 | 第一轮语素 | 信息节点、主键 | 语素节点或无效句柄 | 语素节点、语素对应信息关系、索引 | 基础信息类节点、需求、任务和方法可绑定；特征值等未确认值承载节点拒绝 | 代码已实现 / 已复验 |
| 语素服务 | `创建概念入口` | 语素系统规范 | 创建最小词单元概念入口 | 概念入口和追溯 | 第一轮语素 | 文本、信息节点、概念节点、主键 | 语素节点或无效句柄 | 语素节点、对应信息关系、概念追溯关系 | 超长或含分隔符文本拒绝 | 代码已实现 / 已复验 |

## 4. 当前实现边界

当前可以声明：

```text
第一轮领域服务最小入口已存在。
入口前置拒绝第一轮样例已通过。
需求、任务、方法、动态、因果引用可组成控制台入口的最小结构样例。
```

当前不得声明：

```text
完整领域服务已完成。
候选方法选择已完成。
任务状态机已完成。
需求树已完成。
因果解释系统已完成。
旧项目能力已迁移。
```

## 5. 后续待补

后续进入领域服务扩展前，应继续补齐：

```text
存在服务详细设计。
场景服务详细设计。
状态服务详细设计。
动态服务详细设计。
二次特征服务详细设计。
世界服务聚合编排详细设计。
特征值服务只服务特征的入口拒绝规则。
高级信息服务依赖基础信息服务的调用契约。
需求状态枚举规范。
任务状态枚举规范。
方法可执行条件规范。
动态记录最小字段详细设计。
因果引用前值、后值、验证方式的结构承载详细设计。
领域服务返回错误模型统一化。
```

本文件已按 2026-07-05 当前执行口径确认，可作为第一轮领域服务入口实现和验收依据；仍不作为旧能力迁移依据。
