# 本能根运行锚点详细设计 v0.1

状态：待激活，等待 `INSTANCE-FEATURE-IF-R-CONCEPT-FOUNDATION` 和ARCH-L4物理合同。基线：`b48e3826e8288cf31041c037611d895b1481658f`。本设计不使用旧候选中的`.ixx`路径。

## 执行 DRIFT（2026-09-15）

`INSTINCT-ROOT-RUNTIME-ANCHOR` 的执行 S0 已证实下列问题，故本设计的“施工对象”与后续步骤不能作为当前实施依据：

1. 原“区间F节点内值可变”DRIFT已由4115裁决：准确F固定准确值，R集合另行版本化；F建立前须有单值FC，IF/R由存在 owner 写入，F到单值FC使用引用由概念 owner 写入。最早上游缺口因此收束为 `INSTANCE-FEATURE-IF-R-CONCEPT-FOUNDATION` 的已发布存在类组合提供者 `按存在持有特征变更并返回实际F`。阶段21不得模拟该提供者或改写当前采用语义。
2. 首值、初态、当前选择及状态选择不再是阶段21合同。若特征类变更合同需要状态或其它内部参与者，其 owner、关系、写集、本地映射和发布未知原请求回放均由该上游合同冻结；现有状态类的单owner创建入口不能被阶段21顺序拼接。
3. 根目标合同、独立根需求 D、根列表项 L 和成员关系只有逻辑语义，尚无可实施的 ARCH-L4 DTO、关系形状、原子写集和读回合同；当前需求头仍是已退役的“方向二次特征”记录。已裁决的边界是：双根 D 不写入所属存在、目标宿主、自我字段、关系或 owner；其逻辑归属只由阶段21入口和最终运行锚点表达，且阶段21不处理任务实体或任务物理关系。
4. 阶段21不决定安全 / 服务值是标量、区间或其它特征表示，也不创建`M-X`、二次实例、状态或动态；这些均不可以由需求服务替代。根值后续比较与任何即时投影必须读取各自正式合同。

在剩余合同发布前，禁止按下文的旧“施工对象”“步骤”新增 DTO、跨 owner 入口、需求服务、普通应用装配或启动调用。8121已经按独立双根 D 口径修订；不得再把该已裁决边界回填为自我物理绑定，也不得把上游特征 / 存在组合机制搬回阶段21。

## 已裁决边界与待冻结合同

已裁决：安全 / 服务双根 D 是物理独立根节点，各自仅有独立身份、根特征、根目标合同、父空、顺序0和根列表闭包；不得写入任何自我物理绑定。阶段21入口与值式双根锚点表达其逻辑归属。任务逻辑属于普通需求层级`自我 → 根需求 → 任务`，不在阶段21处理。

仍待冻结、不能由本设计推测的是：

1. 最早上游 `INSTANCE-FEATURE-IF-R-CONCEPT-FOUNDATION`：发布存在类的`按存在持有特征变更并返回实际F`请求 / 结果、根语义选择、实际F成功谓词、位置 / 概念 / 当前采用闭合、幂等 / 发布未知和同截止读回。4115已经裁决F不可变与R版本化；该提供者不得在8121中重写或模拟。
2. 在不改变已裁决的独立根 D 边界、父空/顺序0、独立 L、当前成员恰一的前提下，目标合同、D、L、成员的 ARCH-L4 DTO、关系、写集、恢复和读回合同是什么？目标值及比较只可引用特征类及目标合同的正式定义，不在本设计预设标量或区间语义。

## 原施工设想（已暂停，不可作为施工依据）

新增`业务/本能根运行初始化.数据.h/.cpp`与`业务/应用服务.本能根运行初始化.h/.cpp`；扩展`领域/数据服务.特征类.h/.cpp`、`状态类.h`、`需求类.h`，以及`装配.普通应用.h/.cpp`、`启动.应用程序.cpp`、项目文件。禁止线程文件与旧`L2*`模块。

ARCH-L2由 `INSTANCE-FEATURE-IF-R-CONCEPT-FOUNDATION` 在存在类新增“按存在持有特征变更并返回实际F”提供者；阶段21只调用它并消费实际F，不新增特征 / 状态 DTO、跨owner入口或比较注册。ARCH-L4需求服务新增：目标合同CRUD和按幂等身份读回、独立根D（父空、顺序0且无自我物理绑定）建立/读回、根列表项及当前成员建立/读回；所有请求携带G0和固定键，所有成功回显完整实体与截止。

业务provider按8121顺序逐步调用已发布的根特征变更提供者并补齐ARCH-L4材料；每一步读当前G0，提交后重新读取。最终只在同一Gfinal形成双根锚点；调用方不得从中取得可写端口。普通应用增加`初始化普通应用本能根()`，启动在阶段19成功后调用，成功仍停在阶段20以前。

非成功逐层保留来源状态并归一为8121结果；资源失败、异常、代次漂移、幂等冲突、引用冲突、材料未闭合、已可能发布均零锚点。根值变更、时间及重启行为全部服从已发布特征类提供者合同，阶段21不另行锁定或重置。

验证：待上游特征类提供者和ARCH-L4合同发布后，专属隔离驱动覆盖8121 §5矩阵及“阶段21仅调用提供者、未直接改F或模拟其内部机制”；Debug/Release x64 Rebuild；静态检查无`.ixx`、无自我线程调用、无硬编码根节点编码。
