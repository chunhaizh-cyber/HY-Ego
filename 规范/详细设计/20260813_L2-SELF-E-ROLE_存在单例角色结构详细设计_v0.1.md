# L2-SELF-E-ROLE 存在单例角色结构详细设计 v0.1

日期：2026-08-13

## 1. 目标与边界

在唯一 `L2存在结构服务` 和既有存在 owner 内增加业务中性的单例角色结构，使上层能够以一个不透明角色键唯一定位一个正式存在。L2 不定义“自我”角色、不建立项目资格、不读取 I2、不接触场景，也不增加 owner 或世界服务。

本叶是 7130 真实自我三叶的第一叶。它只证明通用结构能力，不删除启动阶段 17。

## 2. 当前事实与复用裁决

设计基线 `1f0019c9d827f21438b7f7305b43aef8187e92db` 的存在服务已拥有唯一存在 owner、族锚点 / 族归属、节点新增、当前 / 历史来源与完整读取、属性和退出。当前没有单例角色登记或角色查询；`句柄.h` 的旧 `自我结构角色 = 21` 与 `服务.L1自我结构登记` 属于旧中性登记闭包，不可复用。

复用存在 owner、端口、来源读回、全局幂等域、状态分账和 L1 owner-aware 读取；原位扩展两个存在生产文件。禁止恢复关系 21、旧完整自我、专用自我 DTO、资格属性或第二幂等账。

## 3. 结构所有权和事实

全部角色结构由存在 owner 写入：

```text
角色登记锚点（私有普通节点）
角色关系类型（私有普通节点）
角色事实：角色登记锚点 --角色关系类型 / 角色键--> 正式存在
```

角色键为非零、正值 `int64_t`；L2 不登记固定键含义。一个角色键至多一条当前关系；一个存在可承载多个不同角色键。登记锚点和类型不是 `L2存在身份`，无族归属，不退出。

新增角色化存在的一笔 owner-scoped 写集固定为：存在节点、存在族归属、角色关系三项；把既有存在绑定角色只新增一条角色关系。替换角色目标同事务建立新关系并退出旧关系；退出角色只退出具名关系。存在退出由 L1 当前引用保护阻止仍被角色关系引用的存在退出。

## 4. DTO 与公开 ABI

物理位置固定为 `海中鱼巣/领域/L2存在结构.数据.h`：

```cpp
struct L2存在单例角色键 final { std::int64_t 值 = 0; };
struct L2存在单例角色关系类型身份 final { 稳定编码 值; };

inline constexpr std::uint32_t L2存在单例角色登记规则版本 = 1;
inline constexpr L2结构幂等身份 L2存在单例角色登记固定幂等身份{
    0x4C32'4552'4F4C'4501ULL};

struct L2存在单例角色结构登记请求 final {
    L2结构请求头 请求头;
    std::uint32_t 规则版本 = L2存在单例角色登记规则版本;
    L2结构幂等身份 幂等身份 = L2存在单例角色登记固定幂等身份;
};

struct L2存在单例角色结构登记 final {
    std::uint32_t 合同版本;
    std::uint32_t 规则版本;
    L2结构幂等身份 首次幂等身份;
    std::uint64_t 首次期望事实代次;
    std::uint64_t 建立事实代次;
    稳定编码 角色登记锚点稳定编码;
    L2存在单例角色关系类型身份 角色关系类型;
    L2生命周期 锚点生命周期;
    L2生命周期 类型生命周期;
    std::uint64_t 事实截止代次;
};

struct L2存在单例角色事实 final {
    L2存在单例角色键 角色;
    L2存在身份 存在;
    稳定编码 关系稳定编码;
    L2生命周期 生命周期;
};

struct L2存在单例角色结构登记结果 final {
    L2结构结果头 结果头;
    std::optional<L2存在单例角色结构登记> 登记;
    bool 成功() const noexcept;
};

struct L2角色化存在新增请求 final {
    L2结构请求头 请求头;
    L2结构幂等身份 幂等身份;
    L2存在单例角色键 角色;
};

struct L2角色化存在写入结果 final {
    L2结构结果头 结果头;
    std::optional<L2存在事实> 存在;
    std::optional<L2节点族身份来源事实> 存在身份来源;
    std::optional<L2存在单例角色事实> 当前角色关系;
    bool 成功() const noexcept;
};

struct L2存在单例角色绑定请求 final {
    L2结构请求头 请求头;
    L2结构幂等身份 幂等身份;
    L2存在单例角色键 角色;
    L2存在身份 存在;
};

struct L2存在单例角色替换请求 final {
    L2结构请求头 请求头;
    L2结构幂等身份 幂等身份;
    L2存在单例角色键 角色;
    稳定编码 旧关系稳定编码;
    L2存在身份 旧存在;
    L2存在身份 新存在;
};

struct L2存在单例角色退出请求 final {
    L2结构请求头 请求头;
    L2结构幂等身份 幂等身份;
    L2存在单例角色键 角色;
    稳定编码 关系稳定编码;
    L2存在身份 存在;
};

struct L2存在单例角色写入结果 final {
    L2结构结果头 结果头;
    std::optional<L2存在单例角色事实> 当前角色关系;
    std::optional<L2存在单例角色事实> 已退出角色关系;
    bool 成功() const noexcept;
};

struct L2存在单例角色读取请求 final {
    L2结构请求头 请求头;
    L2读取类别 读取类别 = L2读取类别::当前;
    L2存在单例角色键 角色;
    std::uint64_t 历史截止事实代次 = 0;
};

struct L2存在单例角色读取结果 final {
    L2结构结果头 结果头;
    L2读取类别 读取类别 = L2读取类别::当前;
    std::uint64_t 历史截止事实代次 = 0;
    std::optional<L2存在单例角色事实> 角色关系;
    std::optional<L2节点族身份来源事实> 存在身份来源;
    bool 成功() const noexcept;
};
```

全部结构定义 `operator==`。请求有效性逐字段要求合同、非零期望代次、非保留业务幂等身份、正角色键及全部身份 / 关系编码有效；替换还要求新旧存在不同。当前读取要求历史截止为零，历史读取要求历史截止非零且不大于请求期望代次。非成功结果全部可选载荷为空。写成功只允许 `已提交 / 精确重复`、非零且相等的事实截止 / 变更代次；角色化存在结果的存在、来源和关系必须同截止完整且三项同创建代次。绑定成功只含当前关系，替换成功同时含新当前和本代退出旧关系，退出成功只含本代退出关系。读取成功状态为 `已读取`，角色关系与目标存在来源必须同截止完整；未找到 / 已退出为空载荷。

唯一存在服务原位新增：

```cpp
L2存在单例角色结构登记结果 建立存在单例角色结构登记(
    const L2存在单例角色结构登记请求& 请求);
L2存在单例角色结构登记结果 读取当前存在单例角色结构登记() const;
L2角色化存在写入结果 新增角色化存在(
    const L2角色化存在新增请求& 请求);
L2存在单例角色写入结果 绑定存在单例角色(
    const L2存在单例角色绑定请求& 请求);
L2存在单例角色写入结果 替换存在单例角色目标(
    const L2存在单例角色替换请求& 请求);
L2存在单例角色写入结果 退出存在单例角色(
    const L2存在单例角色退出请求& 请求);
L2存在单例角色读取结果 读取存在单例角色(
    const L2存在单例角色读取请求& 请求) const;
```

登记构造期在存在来源登记之后建立，固定写集只有两个普通节点：角色登记锚点本地键 `0x0202'1301U`、角色关系类型本地键 `0x0202'1302U`；二者无族归属且正式读回。实现 S0 只核对这两个冻结值和固定登记幂等身份在最终存在域仍无冲突，任何冲突退回计划支撑，不得自行改值。业务请求使用调用者给定 `L2结构幂等身份`，命中登记固定身份或既有存在来源登记身份固定拒绝。

## 5. 校验、读取与并发

所有写先核对共同合同、非零期望代次、角色键、幂等身份和适用存在来源。绑定 / 替换目标必须是当前正式存在；读取按精确关系类型、锚点、owner、角色、端点和生命周期返回。零关系为 `未找到`，重复同角色关系、错 owner / 类型 / 方向 / 角色或退出混代为 `内部不一致`。

当前读取共享请求指定的当前截止；历史读取隐藏截止后的未来退出。首次 / 精确重复均从首次变更截止正式读回。并发同角色不同目标至多一条成功，其余返回漂移、幂等冲突或引用冲突；不得按关系遍历顺序取首项。

## 6. 文件、验证与完成边界

生产只允许修改两个存在文件。临时专项固定在生产工程外：

- `验证工具/L2存在单例角色结构参数验证.cpp`
- `验证工具/L2存在单例角色结构参数验证.vcxproj`
- `验证工具/运行L2存在单例角色结构参数验证.ps1`

永久记录精确为 `施工记录/20260813_L2-SELF-E-ROLE_存在单例角色结构施工记录.md` 与 `验证记录/20260813_L2-SELF-E-ROLE_存在单例角色结构验证记录.md`。验证覆盖登记、原子三项创建、既有绑定、替换、退出、当前 / 历史、wrong-family、唯一性、幂等、旧代次和并发；专项只使用测试角色键，不出现“自我”业务断言。生产代码零测试实体、故障钩子、计数器和 sleep。实施接受且索引退出后，以独立提交只删除三份临时源码；清理后核对三路径不存在、生产工程 / filters 零引用，并重跑根工程双配置 Rebuild、strict 与生产验证痕迹扫描。

完成最多证明中性单例角色结构成立；不证明角色代表自我、S-HOST、SELF-FORM、生产启动或集成验收。
