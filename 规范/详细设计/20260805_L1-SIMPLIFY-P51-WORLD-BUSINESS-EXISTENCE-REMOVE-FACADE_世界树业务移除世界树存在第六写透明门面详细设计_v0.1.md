# L1-SIMPLIFY-P51 世界树业务移除世界树存在第六写透明门面详细设计 v0.1

日期：2026-08-05

计划身份：`L1-SIMPLIFY-P51-WORLD-BUSINESS-EXISTENCE-REMOVE-FACADE`

创建侧基线：`06019d32e656d075cd9cd0677d5396a0135dbd08`

状态：设计就绪；代码未实现；计划必须待激活

## 1. S0、能力矩阵与最早缺口裁决

正式基线 `main == origin/main == 06019d32e656d075cd9cd0677d5396a0135dbd08`，Git index空。P15代码、P0-FIX、路线输入和旧WORLD-TREE均为其它所有者WIP；本叶只读取正式HEAD，不把未提交代码当接口事实，不触碰、不暂存、不夹带。

4200 §4公开能力顺序在第五写`移动世界树成员`之后固定为第六写`移除世界树存在`。P32/P35/P41/P44/P45/P48已经冻结唯一`世界树业务服务`的三读与前五写；P49/P50已经闭合第六写的完整领域链。能力矩阵如下：

| 第六写所需能力 | 冻结所有者 | P51裁决 |
| --- | --- | --- |
| 同根、同截止、当前场景、实际存在资格和唯一当前成员关系 | P49 | 只由P50消费；门面零旁路 |
| 双摘要幂等、单关系退出、一次L1 v2提交、同锁读回和隔离 | P50经P15最终结果 | 完整复用；门面不解释 |
| 操作专属强类型请求、九状态和完整移除事实 | P50 | 直接复用DTO；不建世界树包装DTO |
| 移除专用账、参与者、编码分配器或普通读取provider | P50已裁决不需要 | 不新增第二账、第二提交或额外资格 |
| 第六写统一世界树业务身份 | 尚无 | **P51是P50后的最早独立缺口** |
| 场景移除、下一公开能力或STEP-5 | 未进入 | 后继重新计算；本叶不混入 |

P49/P50已经使第六写可机械组合。P51不需要新的资格、读取、稳定编码、事务、账、读回或结果映射provider；新增任何一种都会复制P49/P50所有权。当前正式代码仍无P32—P50目标实现，旧`节点直接场景业务服务::移除世界树存在`及旧P1C-SW同名门面使用不同DTO、provider和结果映射，属于未施工旧入口，不得复用、恢复或兼容转发。预冻结合同只支持设计，不证明上游代码已实现。

P15当前具名DRIFT由计划支撑并行修订。P51不依赖P15 DTO、摘要、恢复载体、写集、读回或失败见证；该漂移仅经P50真实结果成为间接激活门禁。若P15最终结果改变P50公开DTO、服务对象、函数const/noexcept或结果字段，P51保持待激活并由计划支撑同步修订，不在门面复制未发布语义。

精确扫描确认顺序551在创建基线未占用；550由既有CONCEPT-NAMING-S3正式占用。P48/P49/P50依次冻结547/548/549，故P51固定551，不临场改用550或换号。

## 2. 目标与完成条件

原位扩展P48目标`世界树业务服务`，新增第六写：

```cpp
世界树存在移除发布结果 移除世界树存在(
    const 世界树存在移除发布请求& 请求) const;
```

唯一算法是把同一请求同步借给`L1世界树存在移除发布服务::移除世界树存在`恰一次，并把返回值逐字段原样返回。

完成必须满足：

- P48十引用构造完整退出，尾部追加一个P50必需const引用形成唯一十一引用构造；
- P32三读与P35/P41/P44/P45/P48前五写名称、签名、函数体和provider调用均不变；
- 第六写直接复用P50请求和结果，为`const`、非`noexcept`；
- 门面校验、状态映射、DTO重组、摘要、幂等、事务、读回、异常改判和额外资格均为0；
- P50方法恰调用1次，其它provider直接调用0；
- P51顺序551专项、既有八根回归和完整验证通过。

## 3. 所有权、物理位置与依赖方向

未来代码白名单：

| 文件 | 唯一修改 |
| --- | --- |
| `海中鱼巣/领域/服务.世界树.ixx` | 原位扩展P48唯一类：构造追加P50服务引用，新增第六写透明委托。 |
| `海中鱼巣/领域/自检.世界树业务只读.ixx` | 只迁移三读构造夹具，证明P50调用0。 |
| `海中鱼巣/领域/自检.世界树业务创建存在.ixx` | 只迁移第一写构造夹具，证明P50调用0。 |
| `海中鱼巣/领域/自检.世界树业务发布初始特征.ixx` | 只迁移第二写构造夹具，证明P50调用0。 |
| `海中鱼巣/领域/自检.世界树业务更新已有特征.ixx` | 只迁移第三写构造夹具，证明P50调用0。 |
| `海中鱼巣/领域/自检.世界树业务发布状态动态.ixx` | 只迁移第四写构造夹具，证明P50调用0。 |
| `海中鱼巣/领域/自检.世界树业务移动成员.ixx` | 只迁移第五写构造夹具，证明P50调用0。 |
| `海中鱼巣/领域/自检.世界树业务移除存在.ixx` | 新建顺序551专项。 |
| `海中鱼巣.vcxproj`、`海中鱼巣.vcxproj.filters`、`海中鱼巣/启动.应用程序.ixx` | 只登记新专项与薄包装，不接生产消费者。 |
| 两份P51专属施工/验证记录 | 只记录本叶证据。 |

依赖方向固定：

```text
世界树业务服务::移除世界树存在
-> L1世界树存在移除发布服务::移除世界树存在
   -> P50内部P49/P15/L1链
```

门面只import P50公开服务模块和公开DTO所需模块。它不得import P49、P15、L1数据、摘要、写集、读回、参与者、仓库、SQL、恢复、缓存、旧节点直接场景或旧WORLD-TREE模块。P50及其下层不得反向依赖世界树门面。

## 4. 最终类、构造迁移与公开函数

在P48目标类上形成：

```cpp
export class 世界树业务服务 final {
public:
    世界树业务服务(
        const 世界树快照读取服务& 快照读取,
        L1当前场景实际存在发布服务& 当前场景实际存在发布,
        实际存在I64初始特征批次发布服务& 初始特征批次发布,
        实际存在I64已有特征换代批次发布服务& 已有特征换代批次发布,
        L1事实基座服务& L1,
        L1实例特征服务& 实例特征,
        L1状态服务& 状态,
        特征比较服务& 比较,
        L1动态服务& 动态,
        const L1世界树成员移动发布服务& 世界树成员移动发布,
        const L1世界树存在移除发布服务& 世界树存在移除发布) noexcept;

    世界树存在移除发布结果 移除世界树存在(
        const 世界树存在移除发布请求& 请求) const;

private:
    const 世界树快照读取服务& 快照读取_;
    L1当前场景实际存在发布服务& 当前场景实际存在发布_;
    实际存在I64初始特征批次发布服务& 初始特征批次发布_;
    实际存在I64已有特征换代批次发布服务& 已有特征换代批次发布_;
    L1事实基座服务& L1_;
    L1实例特征服务& 实例特征_;
    L1状态服务& 状态_;
    特征比较服务& 比较_;
    L1动态服务& 动态_;
    const L1世界树成员移动发布服务& 世界树成员移动发布_;
    const L1世界树存在移除发布服务& 世界树存在移除发布_;
};
```

完整类继续保留P32三读、P35第一写、P41第二写、P44第三写、P45第四写、P48第五写及已删除的默认/复制/移动特殊成员；此处只列十一引用构造和第六写。P48十引用构造不保留overload。P50服务公开方法是`const`，故门面只保存const引用；不得改成指针、optional、setter、函数对象、服务定位器或固定未实现入口。

`世界树业务服务合同版本`保持1，并静态断言等于P50 `世界树存在移除发布合同版本`及既有门面依赖合同版本。门面不重复解释各DTO版本。

请求由调用方拥有，门面只同步const借用且不保存；返回值由调用方拥有。依赖宿主寿命覆盖门面和同步调用。本叶不修改装配。

## 5. 唯一函数算法

函数体固定为：

```cpp
世界树存在移除发布结果 世界树业务服务::移除世界树存在(
    const 世界树存在移除发布请求& 请求) const {
    return 世界树存在移除发布_.移除世界树存在(请求);
}
```

调用前后不得增加：

- 合同、规则、幂等身份、根、场景、存在、当前成员关系或截止校验；
- P49规格形成、P15探测、摘要、退出写集、L1提交、读回或失败见证访问；
- 状态映射、optional补造、结果包装、字段重排、部分成功或第二DTO；
- try/catch、日志、计时、retry、sleep、fallback、快照预读或旧入口回退；
- 前五写、场景移除provider、缓存或跨调用可变状态。

P50返回什么，门面逐字段返回什么，包括状态、完整移除事实及全部非成功空事实。异常若越过P50合同，门面不捕获改判。

## 6. 既有八根、并发与生命周期

P32三读和前五写全部保持原函数体与一对一provider。新增P50引用不能被既有八根访问；第六写对快照、P34、P40、P43、P16和P47直接调用为0。

门面不加锁、不排队、不重试。P50独占移除准入、幂等、事务、读回、隔离和并发结果；其它provider继续独占自身领域。门面不建立跨入口顺序、第二幂等身份、第二账或第二失败见证。

构造只绑定十一个引用，不读取、不恢复、不登记、不发布事实。未来装配必须先构造全部依赖再构造门面；这是激活依赖，不授权本叶修改运行期装配。

## 7. 验证矩阵

P51顺序551专项至少覆盖：

1. 世界树类、模块、合同版本和九个公开函数唯一；旧P1C-SW及节点直接移除DTO/入口引用为0；
2. 十一引用构造完整替代P48十引用构造，默认/复制/移动、指针、optional、setter和兼容overload均不存在；
3. 第六写签名为const、非noexcept，请求/结果与P50精确相同；
4. 代表性入口拒绝、未找到、版本漂移、许可拒绝、幂等冲突、资源失败、内部不一致与P50逐字段全等；
5. P50已提交/幂等读回路径按P50正式夹具验证完整事实全等，不伪造下层机器语义；
6. P50调用恰1次，请求对象不变，门面校验和状态映射0；
7. P49/P15/L1、快照、P34/P40/P43/P16/P47、仓库、SQL、恢复、缓存、锁、日志、时钟直接调用0；
8. 既有八根只调用原provider，新增P50引用调用0；
9. P32/P35/P41/P44/P45/P48既有专项不退化，构造夹具只增加P50必需引用；
10. 顺序551唯一，550保持原占用，工程/filters/启动只登记新专项与薄包装，不接生产消费者；
11. 并发调用无门面共享可变状态，结果只由P50决定；
12. Debug/Release Rebuild、两配置完整自检、strict、MSVC模块扫描和精确白名单diff check通过。

## 8. 激活门禁与最大声明

保持`待激活`，直至：

1. P32/P35/P41/P44/P45/P48真实结果进入正式main并经交互接受，八根、十引用构造和既有专项机械匹配；
2. P50真实结果进入正式main并经交互接受，实际DTO、类、const函数、九状态和字段机械匹配；P50接受结果须已吸收P15最终结果，不以当前并行修订中的恢复载体作为P51接口；
3. P48/P50及届时活动计划释放世界树服务、六个既有专项、工程/filters/启动、顺序551、构建、自检和模块扫描资源；
4. 新HEAD确认全部世界树服务构造调用点在十三文件白名单内，551未占用，550仍为原占用，旧P1C-F/SW未施工且无等价第六写门面；
5. 执行侧从新HEAD重做完整S0并确认本计划为唯一可执行代码段。

若P15最终结果导致P50公开DTO、服务对象、函数const/noexcept或结果字段改变，保持待激活并退回计划支撑同步修订；门面不得临场读取P15、映射状态或保留兼容构造。

完成后最多声明：P51白名单内源码候选使唯一`世界树业务服务`具备第六个真实写门面，能把完整实际存在移除请求一对一委托给P50并逐字段透明返回，同时保持P32三读和前五写不变。

不得声明：P15/P49/P50或P32—P48由本叶实现、场景移除、存在删除/退役/回收、其它事实级联、下一公开能力、动作动态、因果、任务/结算、恢复装配、运行期接线、旧入口退出、集成验收、STEP-4、STEP-5或治理闭环完成。
