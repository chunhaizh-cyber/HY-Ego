# DATA-L2-SCENE-FEATURE-INSTANCE-ORGANIZATION 场景特征实例组织详细设计 v0.5

日期：2026-09-11

身份：`DATA-L2-SCENE-FEATURE-INSTANCE-ORGANIZATION`

状态：设计冻结，等待代码实施

## 1. 目标与完成条件

在现有4204真实场景角色/四根/B/状态动态组织结果之上，交付特征根的首个真实可写可读叶：调用方显式给出`C/P/F`，DATA在同G0分别证明C当前角色、F准确内容、C已知F和P同树资格，仅由场景owner追加一条特征组织边；成功后可按当前或历史截止读取C特征根到F的完整唯一路径。

完成要求同时包括：

- 4204协议v2、六编码映射、固定键1及既有状态值逐字段不变；
- 独立登记一个场景owner特征组织关系类型并可从首次材料恢复；
- 既有存在身份小合同追加组合窄读，存在类内部用自身历史读取与既有特征服务共同证明准确F及C已知F；场景类不import或持有具体存在类、特征类及独立provider；
- 根父和父F两种写入闭合，全局单父、无环、跨C拒绝；
- 幂等、并发、发布未知、C退出保护、恢复后当前/历史读回闭合；
- 专项Debug/Release编译链接运行通过，记录实际证据和未覆盖面。

本设计不交付拆边、重挂、成员/子场景、F内容CRUD、已知/采用写入、关系实例、生产装配或完整七字段场景。

## 2. 当前事实与复用裁决

首次设计基线为`f0d081004145d3a26764fa438ca95ec1228dea12`。v0.2正式起点为`e1f441ff5a947bc2b9e75354bbc73341b50f3076`，v0.3正式起点为`6b84d212c3342dbb6d744286b74592c821901415`，v0.4正式起点为`49d5472fdc78b39054c2c3aa1d19428790ed378e`；本修订正式起点为`8e4fe76f44994b9ca3133ea578899a151f6b9a48`，`origin/main`相同。v0.4后首次Debug仍分别在窄桥实现单元第69行、场景类实现单元第254行及专项驱动第151行稳定触发MSVC `symbols.c:5692` C1001，并明确伴随“检测到IFC导入”。三个失败点的共同条件是同一编译单元导入存在/特征/场景这一组大型模块；继续拆函数体或新增实现单元不能解除。裁决只改变组合核验的物理承载：扩展现有存在小合同、由存在类内部复用其已有特征服务、场景类仅调用小合同；C/P/F、同G0、owner、写集和事务语义不变。范围外大量删除/修改为异主WIP，执行不得清理、恢复、暂存或带入。

| 所需能力 | 当前事实 | 裁决 |
| --- | --- | --- |
| C角色、四根、状态/动态组织 | `合同.场景角色组织.ixx`协议v2和`数据服务.场景类.ixx`已实现 | 直接复用；禁止改义 |
| F当前/历史完整读 | 特征服务已有`读取有界准确特征事实`，存在类已经持有同L1`特征服务_`并有多处合法调用 | 存在类内部直接复用；场景类不再import或持有特征服务 |
| C存在身份及C已知F | `存在结构身份只读提供者`唯一生产实现者为存在类，生产消费者仅场景类和动态类；不存在其它派生实现或测试桩 | 在既有小合同追加两个组合窄读virtual；存在类保持单一基类，场景类复用既有`existence_`，不建第二provider或桥 |
| owner-aware当前/历史、首次材料、严格G0写入 | L1 4015公开合同已存在 | 组合复用，不新增L1入口 |
| 特征组织关系类型 | 4204六编码没有该类型，且不得改变键1写集 | 新建独立扩展登记，一次一节点 |
| 全局组织路径 | 场景类已有状态/动态路径实现和轻量写回执 | 适配为独立特征v1路径，既有v2请求仍不接受特征 |

现有知识库用途卡绑定较早全量基线，仅作候选导航；本次以当前源码和调用点复核为准。代码实施须在施工记录列出存在小合同、存在类和场景类的函数用途知识变更清单，并明确特征类最终零修改及三个未发布候选的物理删除。

## 3. 文件与依赖

### 3.1 生产文件

1. `海中鱼巣/领域/合同.存在结构身份只读.ixx`
   - 保持既有存在身份合同版本2、DTO、成功谓词及三个virtual原样；追加4203 v0.9的组合窄读版本1、状态、当前/历史请求、见证、结果、成功谓词及两个virtual。
   - C/F均使用`稳定编码`；见证只含唯一已知关系编码及F生命周期，不import存在类、特征类或场景合同。
2. `海中鱼巣/领域/合同.场景角色组织.ixx`
   - 追加4205 v1特征组织纯值合同、固定扩展登记键和写/路径结果便利判断。
   - 删除未发布独立已知F provider及其DTO；保持既有协议v2原样，禁止重编号或给既有结构加字段。
3. `海中鱼巣/领域/数据服务.存在类.ixx`
   - 继续只继承既有`存在结构身份只读提供者`；实现新增两个override，内部复用身份历史、存在历史、特征组及已有`特征服务_`。
   - 不import场景合同，不增加场景基类、场景DTO、写入、owner、端口、缓存或第二provider。
4. `海中鱼巣/领域/数据服务.场景类.ixx`
   - 删除未发布候选中的存在类、特征类、窄桥import及适配器；不保存`features_`或独立已知provider，复用既有`existence_`。
   - 追加扩展登记、组织特征、读取特征组织历史及私有准入/路径helper；函数体继续留在场景主模块接口单元，不新增实现单元。
   - C退出保护加入特征组织关系，不改变退出八事实写集。
5. `海中鱼巣/领域/数据服务.场景已知特征读取桥.ixx`、`数据服务.场景已知特征读取桥.impl.cpp`、`数据服务.场景类.impl.cpp`
   - 三份均为未发布执行候选，本版物理删除且不得留空文件、转发、别名或回退。
6. `海中鱼巣.vcxproj`、`海中鱼巣.vcxproj.filters`
   - 删除上述三个候选登记；其它项禁止重排。

不新增第二生产合同文件、第二provider或第二场景服务。依赖固定为`存在类 -> {存在小合同,特征类}`、`场景类 -> {场景角色组织合同,场景状态使用绑定合同 -> 存在小合同}`。禁止恢复`场景类/桥 -> 存在类或特征类`的大型模块组合，禁止让特征类反向继承场景合同。若编译器可见性与当前扫描不同，停止退回，不得通过新增反向import、桥或实现单元绕过。

### 3.2 验证与记录

- `验证工具/场景特征实例组织专项验证.cpp`
- `验证工具/场景特征实例组织专项验证.vcxproj`
- `验证工具/运行场景特征实例组织专项验证.ps1`
- `施工记录/20260911_DATA-L2-SCENE-FEATURE-INSTANCE-ORGANIZATION_场景特征实例组织施工记录_v0.1.md`
- `验证记录/20260911_DATA-L2-SCENE-FEATURE-INSTANCE-ORGANIZATION_场景特征实例组织验证记录_v0.1.md`

专项使用独立输出目录和内存L1实例，不与其它活动验证共享数据库、端口或构建目录。同一专项源文件使用两个互斥编译定义分别形成“存在组合provider探针”和“场景组织探针”：前者只显式import存在小合同与存在类，后者只显式import存在小合同、场景合同与场景类并用严格合同桩驱动；任何单一编译单元不得同时import具体存在类和场景类。脚本分别编译、链接、运行两个探针，工程默认只登记不会形成大型组合的场景组织模式。

## 4. DTO与函数冻结

所有场景组织DTO、固定键和便利判断逐字段以4205第4节为准；存在组合窄读DTO及两个virtual逐字段以4203 v0.9第3节为准。执行者不得改名、合并到v2、复用`场景组织历史请求`接受特征或另造公共结果头。

生产函数冻结为：

```cpp
// 既有存在小合同中只追加两个virtual；存在类实现，不新增基类
virtual 存在已知准确特征读取结果 确认当前已知准确特征(
    const 存在已知准确特征当前请求&) const=0;
virtual 存在已知准确特征读取结果 读取已知准确特征历史(
    const 存在已知准确特征历史请求&) const=0;

// 场景类
static 场景特征组织扩展登记结果 登记特征组织扩展(
    const L1事实基座服务&, L1所有者范围写端口&,
    const 场景特征组织扩展登记请求&);
场景特征组织写结果_v1 组织特征实例(const 场景特征组织请求&);
场景特征组织历史结果 读取特征组织历史(const 场景特征组织历史请求&) const;
```

场景接口单元包含以下本切片新增函数并按现有场景IFC方式定义：`登记特征组织扩展`、`组织特征实例`、`读取特征组织历史`、`扩展登记材料匹配`、`读特征路径`、`核验当前已知准确特征`、`核验特征组织父`、`核验特征未组织`、`核验特征组织前置`、`读回特征组织`、`特征到场景`、`形成特征组织写集`、`普通幂等身份有效`。删除原本分开的`核验当前准确特征`/`核验当前已知特征`双helper，合并helper只调用`existence_`新增组合窄读；不得把函数体移入新实现单元。

场景类唯一构造签名在现有参数后追加：

```cpp
const 场景特征组织扩展结构交付&
```

已有场景调用点扫描为零，故本切片无需修改生产消费者。专项分别验证真实存在provider和以同一小合同桩驱动的场景消费者，不在一个编译单元组合两个大型具体服务。执行S0如发现新调用点或其它`存在结构身份只读提供者`派生实现，视为代码漂移并退回，不得扩大白名单。

## 5. 算法与事务顺序

### 5.1 扩展登记

用固定键`0x5343454E45464F52`和一个本地节点键1形成固定写集。先读首次材料；同义则原样重放，异义幂等冲突。成功/精确重复后从确定映射取得唯一关系类型，并以实际Gread读取owner、普通无属性节点和首次生命周期。原4204登记函数、六本地键和固定写键1均不调用、不改写。

### 5.2 特征与已知窄读

存在组合provider的当前读只将请求规范化为`Gread=H=G0`的历史读。历史读固定复用`读取存在身份来源历史见证`、`读取存在历史事实({1,Gread,H,C,4096})`及`特征服务_.读取有界准确特征事实`，特征预算固定为每类4096、总材料65536；先核验各自成功谓词与Gread/H/C/F回显，再从`特征组`取目标为F的唯一7150成员关系，并从准确F事实形成F生命周期见证。0项为未找到，重复目标、坏形状或回显矛盾为内部不一致，预算耗尽为数量预算不足；结束守卫仍为Gread。当前采用不参与证明。

场景类当前准入只调用`existence_.确认当前已知准确特征({1,G0,C,F})`；历史读回调用对应历史入口。它只消费成功谓词和窄见证，不读取或复制准确F内容。P为父F时也用同一组合入口验证P，不再直接依赖特征类。

### 5.3 组织写入

固定处理顺序为：形状→首次幂等材料→C当前角色→扩展类型→F当前准确→C当前已知F→P资格→F全局入/出边→路径预算→严格G0提交→实际Gread读回→结束守卫。

P等于特征根时深度0；否则调用本类历史路径`Gread=H=G0`核验P，且P再经既有特征服务有界准确读取确认。反向查询F在扩展类型的全部当前入边，正向查询F全部当前出边；任何预算溢出不截断。不同C或不同根的既有父返回引用冲突；已接受结构的多父、环、错类型为内部不一致。

写集只有一条场景owner关系，本地键1，源=P、目标=F、类型=扩展交付、角色1。普通操作幂等身份不得等于4204固定键1或扩展固定键。严格G0竞争由L1裁决，不用场景锁证明无其它owner并发。

### 5.4 读取与退出

历史路径从F逆向逐边读取到唯一特征根；每步核验H有效、唯一父、同一类型、角色1、节点/边无重复。到其它C根、状态/动态/关系根或无父断链均不成功。当前路径即H=Gread。

C退出在提交八项退出前，使用扩展关系类型读取特征根正向边；并对发现路径保持完整引用保护。存在任何活动特征组织内容返回引用冲突。无活动边时4204原B、状态、动态保护和八项退出语义完全不变。

## 6. 错误、并发与恢复

- 输入坏形状/保留键：入口拒绝。
- C/F/P不存在或退出：保持来源的未找到/目标已退出；P断链为实例未组织。
- C不知道F、跨C、多父、孤立子树、当前引用冲突：引用冲突或实例已组织，零写。
- Gread变化：事实代次漂移，丢弃载荷，不自动循环。
- 资源异常：提交前资源失败；提交开始后不能证明未发布则已可能发布。
- 成功或精确重复后的任一独立读回失败：已可能发布，原键原C/P/F重放。

服务写操作串行；外部provider调用发生在短写临界区之外，取得材料后进入临界区并重验G0。存在组合provider零写、零修复，不自建服务、owner、端口或缓存；场景类只保留既有`existence_`引用。三个未发布候选物理删除，场景特征函数体留在场景IFC；不存在适配器寿命或第二实现单元。恢复新L1实例后重新显式登记/读回扩展交付、从新存在服务与既有小合同引用构造场景服务、清空非权威索引，再按当前或历史请求从L1事实重建；不扫描名称、不补边。

## 7. 验证矩阵

| 编号 | 场景 | 成功条件 |
| --- | --- | --- |
| F01 | v2回归 | 六编码、固定键1、状态值和既有成功谓词静态/运行零变化 |
| F02 | 扩展登记 | 首次/精确重复/异义/恢复编码稳定，且仅一个普通节点 |
| F03 | provider | 既有存在身份版本2及三virtual零变化；存在类仍只有原基类并实现新增两virtual，内部组合特征服务与存在历史；场景类不持具体特征服务或第二provider；三个未发布候选不存在；准确F、C已知F、缺失、退出、漂移、预算和坏形状分账 |
| F04 | 根父组织 | C/P/F成功，轻量回执边正确，F内容/已知/采用前后相同 |
| F05 | 父F组织 | 两层及历史截止路径完整，排序稳定 |
| F06 | 结构拒绝 | 跨C、多父、环、孤立子树、错根、预算不足零写 |
| F07 | 幂等并发 | 同键同义、异义、同G0竞争和发布未知重放收敛 |
| F08 | 退出恢复 | 活动特征组织保护C退出；恢复后当前/历史一致 |
| F09 | 禁止项 | 无F/已知/采用写，无第二服务/owner/L1入口，无拆边重挂 |

验证命令以计划正文为权威。根工程若受范围外既有错误失败，必须保存前后归一错误集并证明本切片没有新增前沿；不得将其记为根工程通过。

## 8. 完成声明边界

设计与计划可执行只表示合同已冻结。代码实施后的专项通过最多证明本切片进程内目标路径；数据库跨进程恢复、普通应用生产装配、完整场景、正式集成验收及业务闭环仍为未验证。
