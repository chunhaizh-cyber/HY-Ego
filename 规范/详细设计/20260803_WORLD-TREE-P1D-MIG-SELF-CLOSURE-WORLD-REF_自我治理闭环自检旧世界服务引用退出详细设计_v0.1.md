# WORLD-TREE-P1D-MIG-SELF-CLOSURE-WORLD-REF 自我治理闭环自检旧世界服务引用退出详细设计 v0.1

日期：2026-08-03

设计身份：WORLD-TREE-P1D-MIG-SELF-CLOSURE-WORLD-REF

创建侧代码基线：`b8dd29cdcda86fb88c0bee4a91467f5d637f817c`

## 1. 目标与完成边界

删除 `自我治理闭环自检上下文` 保存但从未调用的 `世界服务& 世界`，同步删除目标模块对 `世界服务.h` 的直接 include 和根函数中未使用的局部别名。该模块仍消费 `世界树初始化结果` 的自我存在、场景和世界根裸句柄；这是真实运行时材料，必须由后继稳定身份迁移叶处理，不能在本叶删除、默认化或转换。

本叶不新增函数、DTO、状态或模块，不改变运行时语义，不删除 `世界服务.h` 文件，不修改旧世界服务提供者、生产装配、统一门面或其它消费者。

其它旧消费者迁移叶不提供本叶所需接口，也不与唯一代码目标重叠；迁移主题、文档顺序和创建顺序均不构成计划依赖。本叶满足自身 S0 即可独立执行。

## 2. 当前事实和完整传递链

当前目标文件中直接链为：

```text
#include ../领域/世界服务.h
-> 自我治理闭环自检上下文::世界服务& 世界
-> 运行自我治理闭环自检 内部 auto& 世界 = 上下文.世界
-> 无读取、无成员调用、无取址、无比较、无参数传递、无返回
```

全仓扫描 `自我治理闭环自检上下文` 和 `运行自我治理闭环自检` 只有定义位置，没有构造、聚合初始化、函数调用或保存位置。因此删除字段不存在需要同步修改的调用方实参槽位。

与本链相邻但明确保留：

```text
import 海中鱼巣.领域.初始化.世界树
世界树初始化服务& 世界树初始化
const 世界树初始化结果& 世界树初始化读数
auto& 世界树初始化 = 上下文.世界树初始化
const auto& 世界树初始化读数 = 上下文.世界树初始化读数
```

其中 `世界树初始化读数` 被多处真实读取，绝不可在本叶移除。`世界树初始化服务` 虽当前未调用，也不属于本具名世界服务引用切片。

## 3. 复用与删除裁决

| 能力 | 当前证据 | 裁决 |
| --- | --- | --- |
| 自我治理闭环四组专项矩阵 | 根函数既有实现 | 完整保留 |
| 旧世界服务引用 | 字段和局部别名；运行时用途0 | 删除 |
| 世界稳定身份输入 | 当前仅有旧初始化读数；新迁移尚未设计 | 保留旧读数并明确待迁，不猜测替换 |
| 统一门面 | 本叶没有世界业务调用 | 不依赖、不建空函数 |
| 兼容路径 | 正式规范禁止旧门面兼容壳 | 禁止 |

## 4. 物理位置和精确修改

唯一代码文件及模块：

```text
海中鱼巣/线程/自检.自我治理闭环.ixx
export module 海中鱼巣.线程.自检.自我治理闭环
```

精确删除：

```cpp
#include "../领域/世界服务.h"

世界服务& 世界;

auto& 世界 = 上下文.世界;
```

根函数身份保持：

```cpp
自我治理闭环自检报告 运行自我治理闭环自检(
    const 自我治理闭环自检上下文& 上下文);
```

不得重排上下文其余字段，不得删除相邻世界树初始化服务/读数，不得新增占位字段、构造函数、默认引用、指针或兼容重载。

## 5. 数据、所有权、生命周期和结果

| 项 | 合同 |
| --- | --- |
| 删除对象 | 非拥有 `世界服务&` 引用；不负责世界服务生命周期 |
| 运行时读取 | 删除前为0，删除后为0 |
| 事实写入 | 删除前后均为0 |
| 上下文布局 | 少一个引用字段；当前正式仓库无构造消费者 |
| 根函数输入 | const借用上下文；其余字段保持 |
| 返回 | 原 `自我治理闭环自检报告`，字段和汇总不变 |
| 错误 | 不新增状态；已有报告位和异常边界保持 |

## 6. S0漂移与结构化停止

若届时出现任一事实，零代码修改返回 `DRIFT-P1D-MIG-SELF-CLOSURE-WORLD-REF-USAGE-OR-CALLER`：

- `世界` 字段或局部别名新增运行时用途；
- 全仓出现上下文构造、聚合初始化或根函数调用，且传入世界服务；
- 目标文件被其它活动计划持有；
- 删除 include 后暴露第二个未声明旧类型，导致本计划需扩大到旧结果迁移。

禁止用字段保留、空服务、类型别名或兼容构造消解漂移。

## 7. 验证矩阵

| ID | 验证 | 成功条件 |
| --- | --- | --- |
| V01 | 目标旧服务扫描 | `世界服务.h`、`世界服务&`、`上下文.世界`、局部世界别名均0 |
| V02 | 真实旧读数边界 | `世界树初始化结果`及全部既有读取保持 |
| V03 | 构造/调用扫描 | 全仓仍只有上下文和根函数定义，无调用点 |
| V04 | 精确diff | 仅三处删除，无新增行 |
| V05 | 模块依赖 | 目标模块不再文本包含世界服务头；初始化世界树模块import仍存在 |
| V06 | 函数结构 | 四组验收、报告字段、汇总与返回不变 |
| V07 | 构建自检 | Debug/Release及两配置完整自检通过 |
| V08 | 旧头边界 | `世界服务.h`文件和其它消费者未修改 |

## 8. 完成声明

完成只声明“自我治理闭环专项自检退出旧世界服务对象的直接引用与头文件依赖”。不得声明旧初始化结果、其它消费者、生产装配、统一门面或最终删除已经完成。
