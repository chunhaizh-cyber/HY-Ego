# FEATURE-SHARED-CONCEPT 共享特征概念与应用迁移详细设计

日期：20260906
版本：v0.6
设计完成边界：合同冻结；A紧邻后继，代码等待A正式发布，执行状态以计划索引为准

正式依据：1120 v0.5、1130 v0.8、2100 v0.11、2220 v1.10、4180 v0.5、4240 v0.21、7110 v1.4、7140 v1.5、6320 v0.5；施工遵守0050及现行计划规则。

## 1. 当前事实、范围与依次施工

准备基线262f6a4541e804bd821aff3a54f6f2de7e934cd0。直接依赖A `FEATURE-LONG-TERM-CONCEPT v0.2` 的真实代码结果；A已实现合同仍在最终验证，发布SHA在激活前补入，不以聊天进度代替代码依赖。A拥有类型域/校正/跟踪/观察，不在本片另建观察仓。

本片一次冻结同ABI闭环，内部顺序如下：

| 顺序 | 精确源码 | 修改函数 / 结构 |
| --- | --- | --- |
| B1 | 海中鱼巣/领域/数据服务.特征类.ixx | 原子域对真实F判定、拟建值合法预检；复用A整数算法/类型注册/历史材料，原A12入口不改义 |
| B2 | 海中鱼巣/领域/数据服务.概念树类.ixx | 新共享FC域/读写/包含/完整域/并集/命中/名称/图与治理，删除当前成熟公开API；旧28/动态8结构分支保留 |
| C | 海中鱼巣/业务/应用服务.特征概念类.ixx | 新共享查找/生成/名称/原请求继续；删除评估成熟及请求 / 结果，保留根静态绑定 |
| D | 海中鱼巣/业务/应用服务.存在概念树类.ixx、应用服务.动态概念树类.ixx、应用服务.场景成员概念类.ixx | 逐项强类型新域消费和原H / 当前G见证、同根构造、跨阶段恢复；没有有效新kind落入旧裸std::get |

工程海中鱼巣.vcxproj及filters接管原WIP中真实应用登记，不新增模块。新施工/验证记录另列计划；原大计划两记录不带入。精确允许6源码+2工程+2记录。禁止L1、旧L2、根初始化、相机生产接线、完整存在组成 / 关系穷举及无关旧链删除。

成熟直接生产消费者只有FC：评估成熟→记录成熟评估；EC/DC/scene没有直接成熟字段引用。新域则四应用均受影响，必须本片全部适配并整体编译运行，不能以“编译未报错”代替新对象分支可用。旧未发布32成熟WIP不是永久历史兼容义务；其成熟新写/API/DTO/逻辑从当前新链删除，现已发布28/动态8的真实格式仍按旧含义读取。

## 2. F公开原子判定与拟建值预检

A的比较长期特征要求两真实观察；匹配长期区间要求观察E==跟踪E，不能用来给任意E的共享FC判定。本片新增两个具名const成员，不修改A请求解释、不取原E伪装新实例、不在concept/app复制二值距离。

`特征原子域判定请求{uint32_t 版本=1;特征长期读头 读头;特征类型域身份 类型域;特征判定域选择 域选择;特征原子实际值见证 实际;特征长期预算 预算;}`。`判定特征原子域(const 特征原子域判定请求&) const -> 特征长期读取结果<特征原子域判定事实>`。事实{类型域完整事实,特征判定域选择 规范域,特征类结点 实际F,可选引用材料,特征原子实际值见证 实际,bool 适用,optional<特征原子差异标量> 差异}；不回写F，不产生持久派生值。

判定先同G/H读取真实类型域及输出FT。域选择必须有效；单原子分支要求原子.类型完整等于真实类型合同；区间非空且在完整I64域内，二值原子格式完全一致、半径>=0且代表图完整有效。实际F先按实际.H通过`按实例读取特征历史事实`证明该H完整属性组及唯一实际值，再以`读取特征历史事实`精确互证请求value；累计属性/历史预算，不能只读碰巧存在的指定值而跳过唯一性。禁止错valueID回退；实际FT必须等于类型FT，独立材料仅一次历史解引用。F只核验自己事实，存在上下文 / 成员定位不证明E归属，由应用另查E。前后G一致；原子代表直接来自完整请求，不要求其旧实例仍活动或原来源payload仍可回查。数据不宣称此请求已经是某个正式FC定义，concept需先从自己的正式定义读出相同原子。

I64区间用精确集合成员谓词，端点不改、无舍入；结果适用表示域成员判定，不另导出无类型布尔特征值。二值原子复用A `长期差异` 的同一私有实现、逐像素解析与预算，差异<=半径才适用。`特征原子差异标量{输出FT,类型域身份,单位,缩放,真实阶次=2,特征类型化原子域 左域输入,特征原子实际值见证 右实际,int64 值}` 是新具名值式合同，来源如实标记本次类型化域输入，不伪造左F或FC身份；其来源并不自行证明FC记录存在。二值比较失败或超预算整项失败无值；I64区间判定差异空，不能用0冒充二值比较回执。

`特征拟建值预检请求{uint32_t 版本=1;特征长期读头 读头;特征类型域身份 类型域;稳定编码 FT;特征类值 原始值;特征长期预算 预算;}`；`预检长期特征拟建值(const 特征拟建值预检请求&) const -> 特征长期读取结果<特征拟建值预检事实>`。事实{FT,类型域完整事实,原始值,variant<int64_t,特征二值图> 已解析值,可选引用材料}。复用普通F新建的FT活动 / 表示 / 禁用输出FT自槽准入及A格式解析，只读、零写；不伪造F/value身份、观察或二次比较结果，不提前执行共享域匹配。它仅防止先发布F后才发现不支持格式；F后续新增仍必须按新G自身重新验证。

两个方法预算复用A现七字段，所有实际类型/节点/值/像素/边界对都累计，读取前后守卫共同G。错误沿特征长期状态1—17：未知类型/坏格式/原子越界入口拒绝或类型不支持；真实历史缺失与预算、漂移、资源、差异不可表示分开，失败无读取成功载荷。原F退出后早H合法投影仍可判定；当前业务成功由应用另行fresh读。原子判定的bool只是域成员控制回执，不是新建无类型二次特征；真正暴露的二值差异量始终具名I64。

新增`特征原子实际值见证{稳定编码 F,值事实,FT;uint64_t H;}`，身份均非零；H与读头投影截止一致。它是本次只读判定的真实值来源，不要求观察标识、时间、E或成员关系，不产生A观察记录。FC把已验证观察映射成该四字段，EC/DC从其同H真实特征见证映射；不得用G冒充时间或填常数观察ID。F读取该真实F/value/FT的历史事实后复用A私有解析及差异内核，不能调用要求完整观察资格的A入口并伪填字段；原A12入口合同不变。

### 2.1 两新入口的实际用量与组合预算

本v0.2仅补本片新增供给，不改变A原12入口/请求/结果。新增纯值`特征长期只读用量{uint64_t 记录数=0,关系数=0,属性数=0,样本数=0,像素数=0,点对数=0,历史数=0;}`；`特征原子域判定事实`与`特征拟建值预检事实`各追加该具名`用量`字段。顺序分别对应`特征长期预算`七字段：最大记录数、最大关系数、最大属性数、最大样本数、最大像素数、最大边界点对数、最大历史事实数。用量不是预算上限、估算、二次特征值或持久业务事实，不参与定义/请求幂等等义。

用量必须准确复制这一次`长期上下文`最终七计数，含类型/输出FT、whole-instance→exact唯一值、材料解析、代表图解析与差异的所有实际扣量。不得在类型读取后、实际F/材料读取前或差异计算前取快照。现`长期读包装`在action之后还有尾G守卫：仅对这两种新结果，action全部完成且尾G守卫通过后、发布成功读取数据前赋最终计数；赋值/移动失败仍按原读包装清空数据。可以用内部具名收尾重载实现，不增加第三getter或修改A其它结果。成功但不适用仍返回完整实际用量；任何失败（包括预算不足/漂移）不返回部分成功事实或可供继续扣减的成功用量。

两个新入口接受七预算分量为0：该维本次完全不用可成功，任何正耗用必须在执行前以剩余额度拒绝。数值判定不使用样本/像素/点对时，这三项0不是入口错误；0不代表无限。现私有扣量对amount=0也要求limit非零，必须仅两新入口启用内部零量策略：上下文标识默认旧策略，仅新两入口设置；上下文扣量在该策略下允许amount=0且used<=limit，正量仍要求amount<=limit-used。所有新入口实际共享helper中的扣量都通过该上下文策略，不得只改某个外层检查；原A12仍原策略、不改公开合同。不能把0提升为1来取得正耗用后才拒绝，也不能绕开任何实际计数。

concept/app的同次复合域匹配持有一份七维剩余；每次串行调用两新入口时传当前剩余，先互证成功头、共同G/H、实际目标，再逐字段核验返回用量<=剩余并减去。超额回执是内部不一致，不能截断或钳位。相同F被不同原子重复读取时按实际重复消耗累计，不假定共享跨调用缓存；允许私有缓存仅在其真实作用范围计数。若还要判定原子，某维剩余0可以传入，下一个入口依本次实际需求裁决；不能仅因某维为0提前拒绝所有标量路径。原子列表为空的合法TOP路径调用同一判定入口的完整域分支，返回的一份历史实际值判定用量照常扣减，不调用拟建预检。任何一项失败，整个只读查找/模板判定清空成功结果，不以先前适用项或部分用量继续后续原子；写流程保留此前独立发布的阶段和完整原请求，但本次失败匹配不形成名称或命中。概念/应用自己的读取开销按其既有本地预算另计，不能把该回执冒充所有provider调用的统一计数。

同一新应用模板判定内部对所有原子累计，外层FC/EC/DC遍历不同模板时同样消费其原子回执用量总和（TOP完整域历史判定回执也须透出）。`概念树应用模板判定事实`追加`特征长期只读用量 特征用量`，仅汇总其成功子调用实际用量；FC自调F及concept转接不得重复扣同一回执。完整请求中的A观察积累仍是独立写阶段预算，不用新只读回执替换A原预算语义。

### 2.2 v0.3完整域历史判定

`SHARED-TOP-HISTORICAL-DOMAIN`修复本片尚未发布接口的TOP供给。新增空的具名标记`struct 特征完整域标记 final {}`，以及`using 特征判定域选择 = variant<特征完整域标记,特征类型化原子域>`。既有本片`特征原子域判定请求`的单原子字段替换为该`域选择`；`特征原子域判定事实`的`规范化原子`替换为同型`规范域`。函数名、请求版本1和另一个拟建预检入口不变，原A12结果与入口不变；有效variant二选一，valueless明确入口拒绝。消费者不能再对新域选择裸get原子。

完整域标记的实际范围唯一取自请求`类型域`身份在Gread/H读回的正式`特征类型域事实.内容.完整域`，不是调用方另传上下界、图、半径或默认原子。F通过已有本片`长期读原子实际`等同职责helper进行whole-instance→exact唯一F/value/FT历史互证，在实际.H（等于有效读头H）核验原FT非派生输出及真实材料；引用材料沿新值历史入口一次解引用。不得调用按当前G检查拟建资格的`验证特征类型`/`验证特征值结点引用`或`预检长期特征拟建值`替代历史解析。当前已退出而在H有效的F、值及材料，只要正式历史可读均按原H处理；不放宽H下活动、所属、类型、唯一性及材料完整性。实际资料物理缺失仍明确历史不可用，不吞错误。

完整域分支使用与单原子完全相同的实际值解析：I64必须在正式类型完整区间内；二值必须为相同完整格式和合法非空图。成功时`规范域`为完整域标记、`适用=true`、`差异=nullopt`，真实类型域/实际F/可选引用材料/实际见证和七项最终用量齐全；不存在代表，不运行边界点对比较，不能以差异0伪装计算。错误类型、完整域之外或无效图仍按原解析的类型不支持/入口拒绝等状态失败无数据；单原子分支保持原有成功但不适用的语义及具名I64差异。二值TOP解析仍耗实际像素，点对用量0且点对预算0可合法成功。

concept名称、用途准入、FC查找/生成/继续、EC和DC模板判定的TOP全部采用该显式分支，复合模板结果的`原子回执`名称可保留但现在是一组域判定回执：TOP恰一项完整域标记，非TOP按规范原子顺序逐项。concept/app先互证返回规范域与真实定义所选分支/类型完全一致，再按v0.2逐项扣七用量，并纳入`特征用量`汇总；不得空TOP回执或再走预检漏计。跨模板剩余预算和失败整体清空规则不变。普通生成F之前的拟建预检仍验证当前G准入，不因历史匹配需要改为只验H；最终名称/支持新写及应用完成仍独立要求本次当前E/F/member真实成立，不能用早H成功把已退出F当当前采用。

## 3. 共享FC定义、相等与包含

新增独立`概念树共享特征定义{概念树特征类型引用 FT;特征类型域内容 类型;bool 完整域=false;vector<特征类型化原子域> 原子组;}`。种类4=共享特征，根角色仍特征，旧kind1宿主定义不原地删字段；kind2/3不重编号。所有原子类型必须完整等于类型，FT相等。完整域=true时原子组必须空；false时必须非空。类型域记录ID和区间来源ID不进入等价，类型内容包括实际FT/单位/格式/算法/缩放，不能仅比较编码或哈希。

规范化：I64原子依下界/上界排序，合并重叠及无间隙相邻整数区间（邻接运算防溢出），若覆盖完整域则规范成完整域；不得填补间隙。二值原子按完整图格式、像素及半径全结构排序去重，此序只为稳定表示，不是向量距离。二值两个不同原子不借三角不等式推包含。相等=规范形式全等；包含=同类型TOP、I64集合包含、二值原子集合精确包含。新形成概念由FT+规范域共享，宿主/校正记录/来源观察/名称/计数不进入签名。

FT完整域为该FT顶层普通概念，直挂已有特征根；不新建本体根。单原子、多原子精确并集属于同一表示闭集；union操作是类型化集合析取，不能用包围半径代替。定义不可原地改义，当前（含退役）同义唯一；真实退出后新形成可获新身份，旧key始终恢复原身份。

## 4. 新概念扩展布局及旧材料边界

沿现唯一conceptowner和外部28根/规则交付，增加具名`概念树共享结构交付{稳定编码 锚点;uint32_t 格式=2;array<稳定编码,21> 类型;}`。用基础28的类型登记从新锚点登记角色1..21，锚点有基础格式槽2；每项同owner、种类和表示完整读回，缺失 / 重复 / 换用途拒绝。数据不创建根或交付全局变量。新构造为原base6参数后追加共享交付，组合dynamic版本在原动态交付后追加共享交付；旧base / dynamic构造保留只读原格式和已支持操作。

| 角色 | 用途与格式 |
| --- | --- |
| 1 | 定义记录→F类型域记录，真实关系 |
| 2 | 定义记录域形式I64，1=完整、2=I64并集、3=二值并集 |
| 3 | 定义记录→原子节点，顺序1..N |
| 4 | 原子二值代表U64组，A固定二值图v1载荷 |
| 5 | 原子半径I64 |
| 6 | 原子I64区间组，恰[L,U] |
| 7 | 来源记录→F区间，当前可显式释放 |
| 8 | F→共享FC名称字段关系，当前至多一项 |
| 9 | 锚点→名称历史定位节点，永久 |
| 10 | 名称历史定位原key U64组，恰一非零值，禁止0x4E43高16位 |
| 11 | 锚点→用途记录，永久 |
| 12 | 用途记录→共享FC，当前可显式释放 |
| 13 | 用途记录→F长期观察，当前可显式释放 |
| 14 | 用途定位U64组：[业务标识,原H,原概念编码,原跟踪编码,原观察编码,用途角色] |
| 15 | 用途时间I64，正绝对纳秒 |
| 16 | EC来源记录→较早同根EC，原WIP功能移入此明确新布局 |
| 17 | 共享FC来源记录→旧kind1概念，显式转换依据，可释放 |
| 18 | 来源记录→组成并集的共享FC，显式集合依据，可释放 |
| 19 | 来源依据种类I64，1区间、2旧概念、3并集、4类型完整域 |
| 20 | 来源冻结域成员关系，来源记录→定义原子节点；只证明此次域来源映射，不能代替定义3 |
| 21 | 区间来源记录→F跟踪，真实关系；每条7恰配一条21，完整域/旧概念/并集来源禁止 |

kind4沿基础概念本体/种类/生命周期/根/规则/定义/父边结构；定义到FT用已有基础定义类型关系，不建定义宿主边；每项含1和2，非TOP含3及原子4/5或6。新定义规则使用基础正式完整签名规则，新增域形式和原子规范化由本合同固定版本解释。F类型域自身不可变，对其真实读取提供完整含义；当前定义引用保护FT、类型记录与原子，不可当形成依据释放。

来源记录使用基础来源成员和证据H，并有19及上述一个分支：区间7+21成对；旧概念17；并集18按规范完整输入排序；TOP通过1的真实类型合同，不伪造世界源。20把具体原子与本次依据对应，在首次写入后不可改；所有来源与定义含义必须逐项匹配。旧kind1转换不默删宿主，只有本次新定义与旧真实FT / 值域严格等价时才能形成新共享身份。无A类型合同或不支持旧vector域明确拒绝，不编码替代。

本v0.3共享扩展格式2恰21角色；旧未发布格式1/20角色交付明确拒绝，测试必须重新建立完整新交付，不从缺21推测默认跟踪，不自动迁移旧WIP库。1..20角色用途原样保持，基础28及动态8已发布格式完全不变。格式2是共享交付/锚点协议；原名称及用途记录的基础格式1、载荷和本地键不变，区间来源关系顺序固定为7、21、规范20组。每个区间二元组独立来源节点、一个7区间边、一个21跟踪边及来源H；不能一个来源混多个跟踪而丢对应。首次G/H核验`读取长期区间({读头,跟踪身份,预算})`完整组中恰一匹配该区间，互证区间所属跟踪、FT/类型、固定原子和实际来源定义的规范并集，不读取A私有role13。统计min/max变化不参与源固定域等义。

结构读、来源审计、首写回读和原key重建均从真实来源记录7/21边取原身份，在来源记录的形成H、当次Gread公开读取该跟踪的区间组并精确定位；两边原创建/退出及owner/type/角色/H完整互证。当前边已释放时按来源创建H读取真实原边，不能从墓碑/裸U64恢复端点；必要历史材料确实不可用则明确失败，不因定义有原子就吞掉缺依据。预算包括实际扫描返回的完整区间组及来源/两边历史展开；同G私有缓存可去重相同跟踪/H，但不能省略实际计入的访问。此读取仍不展开原world观测payload，A自身固定域可读即沿其正式接口验证。

释放区间来源必须完整预期同时列7与21，同一owner写集同时退出两关系；只给一边、错配跟踪或来源种类不符为入口/引用拒绝、零写，不允许先释放半对再补。来源记录/H/20和两条历史native边保留，继续保护其端点物理清理。原key写集等义和本地键规范顺序包含新增21，读取不能只查7即宣称完整。其它来源及用途没有21，不能把用途14的跟踪字段复用于区间来源。

未发布静态32构造与成熟 / 旧形成规则 / 旧观测的新增DTO、写分支和读取API从当前新链退出，不保留一套可执行成熟业务。旧名称 / EC应用必要功能用上表具名布局重新实现；旧32测试数据库不保证兼容，不在生产迁移清单。已发布基础kind1及动态8真实数据仍原样可读 / 治理，不把新20字段填进旧记录，不把源宿主从旧身份比较中悄悄删除。

成熟删除必须同时清理concept导出规则/评估variant、状态/预算未发布尾字段、评估读写/退出/原key分支、32类型完整性，以及FC评估请求/结果/评估成熟函数。旧已发布概念状态1—25保留；删除未发布静态尾项后显式新增：26已创建共享定义、27已绑定共享名称、28已退出共享名称、29已记录共享用途、30不支持、31差异不可表示。F的17差异不可表示映射31，预算映射21，不能改内部不一致或成功0值。新状态不复活被删除成熟请求类型。

## 5. 公开ABI与新旧结构读取

已发布`概念树定义`及其`概念树概念事实`不原地扩大variant或删除宿主。新增`概念树共享定义事实{概念身份,本体根,规则,定义记录,共享定义,治理状态,生命周期,直接上位组,类型域身份,原子事实组,来源事实组}`；各原子事实有真实节点、属性值、成员边和完整域；来源事实含原H、种类、真实边及创建 / 退出。根在新应用读取中仍用旧基础根事实，无共享定义。所有公开事实通过结果头返回版本、Gread/H及成功谓词，失败清读取数据。共享定义事实在结构回读时不是旧kind1，不能写入旧概念树定义variant。

新增`概念树应用定义事实=variant<概念树概念事实,概念树共享定义事实,概念树应用存在事实>`；`概念树应用读取结果<T>{uint32_t 版本=1;概念树数据状态 状态;uint64_t Gread,H;optional<T> 数据;bool 成功() const noexcept;}`。旧完整读取 / old定义查找仍只承载kind1/2/3；新kind4传入无法表示的旧入口明确不支持，不能坏variant或静默漏项。新应用统一使用下表完整覆盖新旧对象的结构读；旧完整读仍展开它原有的来源证据，不因启用共享构造全局改成窄读。

旧公开图的请求若要求范围含kind4且旧结果无法表达，返回不支持，不能成功缺项。新增应用图结果包含原图头 / 根 / 世界底部引用 / 直接边以及`vector<概念树应用定义事实> 节点组`，完整保留多父。EC/DC图原根内节点仍kind2/3，但内部模板验证必须分别解析kind1旧/4新；不在对外旧模板事实中塞入共享域。下表新应用模板读承接这一新ABI。

新预算`概念树共享预算{概念树预算 基础;特征长期预算 特征;uint64_t 最大原子数,最大用途数,最大名称数,最大首次材料项数;}`；删除未发布成熟预算字段，所有实际访问字段正数且累计。已有动态预算内基础字段不改义，增加共享预算需要新具名应用请求版本，不能把0当无限。根构造固定六字段1仍只读根，不扫描共享普通概念。

| public方法 | 完整请求 / 返回 |
| --- | --- |
| 读取应用概念定义 const | `{旧概念树读取头,概念,共享预算}` → 应用读取结果<应用定义事实>；结构用途不展开world |
| 读取应用概念图 const | `{旧图请求,共享预算}` → 应用读取结果<应用图事实>；有界全部请求范围 |
| 读取应用特征模板 const | `{读取头,模板,共享预算}` → 应用读取结果<variant<旧动态模板事实,共享定义事实>>；仅kind1/4 |
| 查找共享特征定义 const | `{读取头,共享定义,共享预算}` → 应用读取结果<vector<共享定义事实>>；精确当前唯一 |
| 读取共享特征域组 const | `{读取头,FT,共享预算}` → 应用读取结果<vector<共享定义事实>>；该FT全部治理状态，完整读取 |
| 创建共享特征概念 | `{写入头,共享定义,共享来源选择,vector<概念身份>直接上位,共享预算}` → 共享写入结果 |
| 操作共享名称字段 | `{写入头,F,optional<共享名称事实>预期,optional<概念身份>新概念,optional<共享实例见证>见证,共享预算}` → 共享写入结果 |
| 读取共享名称字段 const | `{读取头,variant<F引用,概念身份>选择,bool包含已退出,共享预算}` → 应用读取结果<vector<共享名称事实>> |
| 记录共享概念使用 | `{写入头,业务标识,概念,长期跟踪身份,长期观察身份,原H,时间,用途角色,共享预算}` → 共享写入结果 |
| 读取共享概念使用 const | `{读取头,概念,共享预算}` → 应用读取结果<vector<共享用途事实>> |
| 操作共享概念上位 | `{旧上位操作请求,共享预算}` → 共享写入结果 |
| 迁移共享概念生命周期 | `{旧生命周期迁移请求,optional<共享实例见证>重新采用见证,共享预算}` → 共享写入结果 |
| 退出共享特征概念 | `{旧退出请求,vector<子节点及预期父/新父组>完整替代,共享预算}` → 共享写入结果 |
| 释放共享概念引用 | `{写入头,vector<共享引用事实>完整预期,共享预算}` → 共享写入结果；仅7/21配对及12/13/17/18 |
| 创建存在概念 | 新具名请求`{写入头,旧存在定义,直接上位,variant<E来源,较早EC来源>依据,共享预算}` → 概念树存在写入结果_v2；旧未发布专用请求由本DTO替换 |
| 释放存在抽象依据 | 新请求`{写入头,所属EC,vector<概念树存在依据事实_v2>预期,共享预算}` → 概念树存在写入结果_v2 |
| 治理存在概念 | 新`概念树存在治理请求_v2{版本=2,variant<旧上位操作请求,旧生命周期迁移请求,旧概念退出请求>操作,共享预算}` → 概念树存在写入结果_v2；仅存在根所属普通EC，复用既有治理语义 |

每行请求名为`概念树`+方法名去动词后的具名对象+`请求`，最终源码明确采用：`概念树应用定义读取请求、概念树应用图读取请求、概念树应用模板读取请求、概念树共享定义查找请求、概念树共享域组请求、概念树共享建立请求、概念树共享名称请求、概念树共享名称读取请求、概念树共享使用请求、概念树共享使用读取请求、概念树共享上位请求、概念树共享生命周期请求、概念树共享退出请求、概念树共享释放请求、概念树存在建立请求_v2、概念树存在依据释放请求_v2`；均版本1（名字v2者合同版本2），不与已发布旧DTO同名重释。

`共享来源选择`为闭合variant：`类型完整域依据{类型域身份}`、`区间依据{vector<区间身份>,vector<跟踪身份>}`（两组等长、按下标一一对应，每项作为完整二元组排序去重，不能把两组独立排序）、`旧概念依据{kind1概念}`、`并集依据{vector<kind4概念>}`。不是任意混合来源列表。类型内容真实读回，区间从指定跟踪完整读回后按身份精确定位；缺项不能重建为原子输入。并集必须严格等于输入全部规范域之并，旧依据只能严格等价转换。ID排序只决定写入顺序。

`共享实例见证{E,F,值事实,成员关系,H,观察标识,时间}`。新名称绑定 / 替换必须有见证，H等于原写G，data依现E/F公开同H历史核验实际关系和值，再调用F原子域判定；TOP统一调用F判定入口的显式完整域分支，按同H真实F/value/FT及引用材料解析；不转拟建预检，不用自代表、默认图或常量造原子，不计算虚构代表距离。退出名称只验证原关系及其建立时端点结构，不要求旧F当前值可用。名称单值由当前8全组核验，不把支持或用途当名称。共享名称事实字段为关系/F/概念/真实生命周期，保持具名新类型。

`共享写入结果`保留版本、状态、Gread、optional首次H、发布状态、完整原请求variant、已退出编码、optional共享概念/名称/用途事实及直接边；共享写成功不塞进无法表示kind4的旧写结果。支持事实本身对E/F/D保留已发布形状；新应用支持操作须使用5.2的显式共享预算包装及新写结果，不能把仅八项预算的旧公共请求当足够供给，不伪造旧FC概念事实。所有新写依同owner唯一执行器原key裁决，方法多不等于新端口。

数据共享只读 / 图 / 上位 / 治理 / 退出 / 名称解除 / 来源释放及原键结构结果读回不递归要求F原样本payload。首次创建来源与新支持 / 名称 / 使用准入按职责实际读取。新旧模板均验证本owner自有完整定义和不可释放引用；坏结构不因未展开world被吞掉。所有受影响public→private用途必须显式传递，不能仅依据共享构造bool让旧完整读自动变窄。

### 5.1 v0.4存在依据与独立结果载体

具名退回`FEATURE-SHARED-CONCEPT-EC-EVIDENCE-SUPPORT`：已发布`概念树概念事实`没有角色16依据组，`概念树写入结果.原请求`也不能表达EC v2请求；不得向这两个已发布DTO尾加字段、扩大其variant或把不完整旧事实包装成成功。仅本片新应用variant增加第三分支，新增下列具名值式合同，生产位置仍conceptprovider公开DTO区。

`概念树存在依据事实_v2{概念树概念身份 所属EC,来源EC;稳定编码 记录,成员关系,引用关系,截止值事实;uint64_t 证据H;概念树生命周期 记录生命周期,成员生命周期,引用生命周期;bool 当前引用已释放=false;}`。所有编码是正式实际事实，引用关系恰角色16、源为记录、目标为来源EC；成员边为所属EC的原基础来源成员，截止值为该记录原H。既有三种生命周期均真实读取，不能只带目标ID或bool推定边已退出。公开相等比较完整结构字段；创建/退出代次与读截止按历史公式互证。

`概念树应用存在事实{概念树概念身份 概念,本体根;概念树规则身份 规则;概念树存在定义 定义;稳定编码 定义记录,生命周期值事实;概念树生命周期状态 治理状态;概念树生命周期 生命周期;vector<概念树直接上位事实>直接上位组;vector<概念树来源事实>世界来源组;vector<概念树存在依据事实_v2>抽象依据组;}`。这是完整具名EC投影，不继承或内嵌半完整旧概念事实。所有普通kind2由新应用读取返回此分支，包括只有旧世界来源的EC；世界来源与抽象依据二选一且非空，按现创建合同实际数量核验，不造空来源普通EC。本体存在根继续返回旧根事实分支，kind1/3旧事实与kind4共享事实不改。定义仍已发布EC模板身份集合，角色16没有变成概念签名字段。

既有`读取应用概念定义`及`读取应用概念图`完整包含新EC分支，不加第三个只读getter；读取范围包括真实已释放16历史事实，定位从仍保留的基础来源成员/记录/截止值获得，在概念形成H读原16，再查其真实历史退出。没有16的旧普通EC走世界来源分支；有16不准丢弃依据仅返回旧事实。新应用结果成功谓词验证新EC独立合同；旧完整读取/旧图遇到角色16无法表达的EC明确不支持，且必须在写派发前识别，不能在修改后才以返回类型不支持收口。

来源新写在原请求G核验来源是较早已发布同根普通EC、来源创建G<=原G、非自身，且新定义是来源定义的严格子集；不改变既有上位抽象语义。新读回逐项验证本owner完整来源记录/边/截止和来源身份原形成H事实，保留此前独立定义与来源审计分账：当前16释放后仍能读取真实历史依据，不递归要求来源world实例payload常驻；坏自有边/被物理清理材料不能当空依据成功。释放只退出16当前关系，不退出来源记录/成员/截止/EC定义；完整预期组从新应用存在事实取得，原H派发前分类与归属核验后由owner对完整原结构/原key等义裁决。读回的已释放依据仍保留原关系及真实退出代次。

`概念树存在写请求_v2=variant<概念树存在建立请求_v2,概念树存在依据释放请求_v2,概念树存在治理请求_v2>`；`概念树存在写入结果_v2{uint32_t 版本=2;概念树数据状态 状态;uint64_t Gread;概念树发布状态 发布状态;optional<uint64_t> 首次H;optional<概念树存在写请求_v2> 原请求;optional<概念树应用存在事实> 概念;vector<概念树直接上位事实>关系组;vector<稳定编码>已退出事实;bool 成功() const noexcept;}`。创建、精确重复、无须变更、释放及治理均从同一新EC投影读回；依据通过概念.抽象依据组提供，不另建意图或回执仓。纯结构失败清空成功事实，但保留已派发原请求/首次H/可能发布；无须变更没有key绑定。成功只证明所列原H/首次H事实，最终当前完成仍fresh共同G独立读。

旧泛型上位/迁移/退出的成功结果会返回旧概念事实，不能用其公开入口先写角色16对象再转换。新增唯一具名`治理存在概念(const 概念树存在治理请求_v2&)`，返回上述v2结果：请求内三种旧操作仅借用完整原字段，内部共用唯一owner的构造写集、生命周期/图闭包守卫和原key机制，直接选择新EC结果读取用途。内嵌操作预算必须与外层共享预算.基础相等，矛盾入口拒绝；上位操作全部端点须是存在根或同根普通EC且符合原准入，本体根不允许迁移/退出。它不新增治理动作、替代边语义或持久角色；请求与退出原事实集合按原规范原子写入、零退出节点槽变更。凡通用私有图/结果分派原本会漏16的路径都同步识别，不能仅替换导出函数返回类型。

EC应用的创建/抽象、直接上位、迁移、退出、释放和继续材料分别保存新v2完整请求及结果；确认事实variant新增应用存在事实，禁止把角色16塞入旧世界来源/动态依据。支持建立/退出的业务字段和支持事实形状保留，public调用采用5.2新预算包装/结果；目标结构校验使用完整新EC投影。场景组合与EC读树同步第三variant分支；FC/DC通用图访问器也须按真实类型取共同身份/边，不能把第三分支送进旧裸get，动态模板读取自身仍只kind1/4。

原key首查命中时以首次H重建完整原请求写集、来源16及映射，新v2结果逐字段读回；已释放关系不能阻止恢复原创建，但当前完成要fresh读到已释放状态。释放重放恢复真实退出编码/首次H后，新应用读取在freshG给出完整旧依据及退出代次。治理退出结果中概念结构取原写G的完整EC投影，退出生命周期/边变更须由实际首次回执和历史事实互证，不能靠调用方原请求填充；没有完整证据就保留发布状态失败。新入口合计18个concept新增/具名替换方法（前17加这一治理入口），不改变十文件范围。

### 5.2 v0.5旧操作的新共享预算供给

具名退回`FEATURE-SHARED-CONCEPT-LEGACY-BUDGET-SUPPORT`：已发布旧支持/动态等请求仅有`概念树预算`，不能提供kind4/角色16完整结构所需的共享原子、来源、首次材料和F七维预算。不能从旧八字段换算、设大常量、0当无限、丢校验或先调用旧public后补验。保留已发布请求/结果/定义ABI；本片新增两个封闭入口而不增加owner、持久角色或业务动作，concept新增/具名替换总数20。

`概念树应用业务操作 = variant<概念树动态建立请求,概念树支持建立请求,概念树支持退出请求,概念树上位操作请求,概念树生命周期迁移请求,概念树概念退出请求,概念树形成引用释放请求,概念树动态依据释放请求>`；`概念树应用写请求{uint32_t 版本=1;概念树应用业务操作 操作;概念树共享预算 预算;}`。方法`执行应用概念写入(const 概念树应用写请求&) -> 概念树应用写入结果`。写头来自内嵌操作的真实旧写头（动态两项的`头`，其余`写入头`），不另造外层G/key。内嵌预算必须全字段等于外层预算.基础，不一致入口拒绝、零写；外层全部共享字段由调用者显式给定，保存原请求时不能剥掉。

`概念树应用写入结果{uint32_t 版本=1;概念树数据状态 状态;uint64_t Gread;概念树发布状态 发布状态;optional<uint64_t>首次H;optional<概念树应用写请求>原请求;optional<概念树应用定义事实>概念;optional<概念树支持事实>支持;vector<概念树直接上位事实>关系组;vector<稳定编码>已退出事实;vector<概念树应用定义事实>释放所属概念组;bool 成功() const noexcept;}`。完整原请求包括外层共享预算及实际内嵌操作。成功状态/必要载荷按原动作：动态创建/治理含概念、支持动作含支持、边操作含真实边/退出、两类引用释放含真实退出编码及5.3规定的完整释放所属概念组，单项概念字段为空；不能把kind4或角色16强转到旧写结果。失败清成功载荷而保留完整请求、实际已知首次H及可能发布；只读预算不足不证明旧尝试未发布。

支持建立/退出可针对原合法world→concept角色，含共享FC和EC v2，目标结构使用新应用投影。新包装的上位/生命周期/概念退出分支负责普通动态kind3或旧kind1：新kind4治理仍唯一走现共享专用入口，普通kind2治理仍唯一走v0.4 `治理存在概念`，避免两套新业务资格。请求若误选种类，在原H分类/归属守卫后、任何首次写前拒绝；本体根生命周期/退出继续禁止，动态接根的合法边准入不变。形成引用释放保留原完整预期组和结构规则，kind4新来源/角色16分别走各自专用释放，不用此包装冒充；动态依据释放只为kind3。所有主体/根/成员/时间及真实支持准入沿原合同，不因预算补齐变宽。

新查询封闭为`概念树应用关联操作 = variant<概念树按概念支持读取请求,概念树按世界支持读取请求,概念树形成引用读取请求,概念树动态依据审计请求,概念树动态支持核验请求>`；`概念树应用关联请求{uint32_t 版本=1;概念树应用关联操作 操作;概念树共享预算 预算;}`，方法`读取应用概念关联(const 概念树应用关联请求&) const -> 概念树应用读取结果<概念树应用关联事实>`。内层预算等于外层基础；读取头来自实际分支，动态依据审计的`头`与其它`读取头`按明确类型提取，不猜字段布局。保留原形成引用读取只支持其已冻结当前截止的限制，不新增任意历史枚举。

`概念树应用关联事实 = variant<vector<概念树支持事实>,vector<概念树形成引用事实>,vector<概念树动态依据审计项>,概念树支持事实>`；前两种支持查询唯一返回第一分支，形成引用读返回第二，动态依据审计第三，动态支持核验第四。外层结果头承担同G/H和状态，不嵌套旧结果头。每分支保持原完整性/非空条件、同G首尾守卫及动态证据可用性规则；成功只表示该请求所要求的核验完整，预算不足/必要材料不可用清整组数据，不能返回部分支持或未审完依据。动态定义结构读取独立于原world证据的既有规则保留，专用审计仍按其明确要求展开。

| 旧公共路径 / 原消费 | 本片新路径与结果落点 |
| --- | --- |
| 创建动态概念、建立世界支持、退出世界支持 | 显式应用写包装；动态概念返回应用定义variant旧kind3分支，支持为真实支持事实 |
| 操作直接上位、迁移概念生命周期、退出概念 | DC用应用写包装；FC/EC各自已具备共享预算的专用入口继续，不创建重复资格路径 |
| 释放动态形成依据、释放形成来源当前引用 | 应用写包装，保留完整原预期/原key及退出事实；实际分类由调用方和owner共同核验 |
| 按概念读取支持、按世界读取支持 | 应用关联查询第一分支，双向分别真实读取且共同G，两个读取均用显式共享预算 |
| 读取形成来源当前引用、读取动态形成依据、核验动态支持依据 | 应用关联查询对应后三种分支，不用只改签名但内部仍旧预算 |
| 读取概念、读取概念图、读取动态特征模板 | 本片已有应用定义/图/模板入口；DC的精确定义查找通过有界完整动态根应用图按全定义相等过滤，预算不够失败而非“未找到”，不新增查找getter |

内部从public入口即建立显式共享读取/写准备上下文，将同一上下文或其受控剩余贯通到：目标/模板完整读取、全库唯一查找与图闭包、直接边/支持组、动态world准入、当前引用反查、原G候选重建、首次H原key比对、无须变更及发布后结果读回。共享定义读取不得重新以默认预算建立上下文；同次graph/模板多次实际访问累计本owner计数，重复F判定按七用量扣除。A原12入口未导出内部用量的独立只读投影仍依原合同每次限额与本owner来源/调用数量约束，不能宣称已把其未公开计数纳入七用量汇总。v0.2累计七用量适用于本片两F新增入口，不能制造A回执。

新写的首次查询必须原key优先；命中时以真实首次H重建内层业务完整规范写集、映射及新结果，外层预算只限制本次读取工作量，不进入业务写集等义。预算改变不得使已绑定相同业务键冲突，也不得缺预算时跳过首次证据直接宣称成功；不足返回失败并保留请求/已知H。新旧请求若表示同一原业务和完全相同规范写集，仍由原唯一owner幂等机制裁决，不因新包装额外加key盐或建第二幂等账。形成/写入后读回任何失败或异常都保留首次H及可能发布，不换键重建。

旧公开入口仍可消费其原已支持对象和完整证据；遇当前目标、模板或必要全图含新kind4/角色16而无法提供完整预算/结果时，在实际写前返回不支持，不能先写再以读回无法表示拒绝，也不能悄悄跳过新节点证明唯一/无环。合法旧原key恢复若其首次完整操作全为旧格式，可按原首次H恢复；需要读取新格式当前结构时仍明确不支持。没有共享交付的旧构造不伪造预算启用新能力。

### 5.3 v0.6两类释放的完整结果基数

具名退回`FEATURE-SHARED-CONCEPT-RELEASE-RESULT-CARDINALITY`：形成引用释放的预期组可以跨多个所属概念，动态依据释放只有一个所属。仅本片尚未发布的`概念树应用写入结果`尾增`vector<概念树应用定义事实> 释放所属概念组`；请求、方法数20、持久角色21、写集、业务动作、owner及十文件范围不变。已发布旧写结果/概念事实不加字段；5.1独立EC结果也不变。新结果合同版本仍1，不能把两类释放截取首项填入单项概念字段。

两类释放的成功回执均要求`概念`和`支持`为空、`关系组`为空，完整所属投影只放`释放所属概念组`。其它六种应用写分支此组必须为空，原单项载荷资格保持。形成引用释放先从规范请求的全部`预期当前引用.所属概念`生成去重身份集合；动态依据释放的集合恰为请求`所属概念`一项。按概念稳定编码严格升序读取并输出，排序仅决定序列，不改变业务选择；同一概念有多条待退引用也只输出一次。每项取同一共享上下文下的正式`应用定义`完整投影，按实际kind分派既有三种应用事实，不能由预期字段拼造或把新EC转成旧事实。

`成功()`除原动作状态/发布头校验外，必须核：组内每项身份非零、variant有效、结构形状符合对应应用事实；身份严格升序无重复，并与上述规范请求推导的完整集合逐项相等；两释放的已退出事实集合与规范请求全部待退关系编码精确一致，不能只比较数量。服务读回另外核真实owner、归属、关系历史创建/退出与各所属定义在读截止成立。形成释放空预期且当前真实引用组确为空时，沿既有合同返回无须变更：释放所属概念组与退出编码均空；不能凭世界引用猜一个所属。动态释放空预期且该所属的当前依据组确为空时，沿既有合同返回无须变更，但仍完整读取该所属并返回恰一项。两种无须变更均无首次H、未派发且不绑定key；不能仅因为空预期跳过原当前组/目标资格校验。非空预期的首次成功及精确重复按原释放状态和真实发布回执判定，不改已有准入。

首写准备、原key查询/原G写集等义重建保持现合同；新组只是值式投影，不写入事实或另建索引，不进入幂等写集。首次发布及原key重放均在实际首次H逐项读回所属完整结构，验证所有应退引用的真实退出事实；无须变更在本次共同G读取。所属概念后来退出或改变不应阻断早H合法重放，必须沿真实历史入口取该H的结构，不能用当前存在性替代；必要历史事实不可用则失败，不能省略这一项。最终当前完成仍须由应用fresh共同G重读，原首次H回执不冒充当前关联。新组构建与原操作所有准备/读回共用同一共享上下文、累计预算；先去重再逐项完整展开，每次实际读取按既有计数计入，不能重置预算，也不能以组长度替代内部实际用量。任一项预算不足、材料不可用、代次漂移或异常，清空整组及所有其它成功载荷，保留完整原请求、已知首次H及可能发布；禁止返回已读前缀当部分成功。尾G守卫覆盖整个组。

所有应用保存的步骤回执/继续材料保留这个新组，不把多项压入既有optional概念字段；确认事实按组顺序逐项加入已有闭合variant，并依实际类型核身份，不能盲取第一项。继续时完整原请求仍须正式原key核验，缓存组或回执提示不构成跳过依据；失败后的早退不得丢失原请求及已知首次H。DC具名释放重载的原H动态归属前置核验继续覆盖所有预期所属，返回同一新应用写结果，不因组可多项放宽其它根的准入。共享FC、EC16各自专用释放入口不转移到此包装。

## 6. 固定写集、用途与生命周期

新共享写的节点本地键1起、关系0x10000起、值0x20000起，序列号上限0xFFFF，乘加和累计预算先检查。节点顺序：共享概念1、定义2、规范原子3..、最后每个来源记录；关系按基础本体根/规则/定义/规范父/FT，再扩展1/3/来源成员与19分支，成员顺序固定；值按节点序和属性类型用途顺序（基础种类4、生命周期1，定义形式，原子4/5或6，来源H/19）。属性来源均所属节点，指向FT及F记录必须真实强引用，不用DTO blob。来源20只映射本次对应原子；TOP无20，旧单概念依据映射全部新原子；区间与并集的每个来源记录统一映射其自身输入域对输出有贡献的原子：I64按该来源输入集合与输出原子的交集非空建立20，二值按精确原子相等建立20。每个kind1区间来源读取时核验其实际贡献目标集合，不再强制targets等于全部atomIds；再对全部来源合并核验规范并集严格等于完整输出。合并后的[0,3]可以同时接受来源[0,1]及[2,3]的贡献，不声称任一来源覆盖整个输出原子；再核验全部来源规范并集与完整输出严格相等、所有贡献映射恰好齐全。TOP没有原子或20，仍从真实来源全集验证完整域相等。

创建当前同义概念返回原事实无须变更、零写、不绑定新key；现有退役也不能同义新建。旧已退出概念不参与当前唯一，原key恢复仍返回旧身份。首写共享完整域 / 原子 / 来源 / 直接父在同一事务；应用多次创建多个节点不宣称全图原子。

名称绑定/替换节点1为永久定位，8业务关系本地键0x10000、9登记0x10001；值基础格式1与10原key依次0x20000/1，各有自槽。替换同笔退旧8，建立新8和定位；仅退出不建定位。历史读取从9永久组扫描，每项通过原key首次正式写集 / 映射找到真实8边，在首次H读取原关系、互证owner/源/目标/类型/创建退出；预算包含全部扫描和首次材料项，不能先按F过滤漏预算。不按G暴力扫描，不用原key或墓碑伪造被清理关系。key拒绝0x4E43高16位，应用任何前序派发前也检查最终名称key。

用途节点1永久11登记，12/13当前引用，基础格式/H、14定位、15时间属性；只接受用途角色1=实际适用、2=名称采用。按请求中真实跟踪身份调用现`读取长期观察({读头,跟踪身份,预算})`，在返回完整组内按观察身份精确定位且核验观察所属跟踪；角色14保存该跟踪身份用于跨进程展开。扫描全部返回项累计预算，缺项不改为零观察或另猜跟踪。按本次Gread读取长期观察的自有正式记录，核验其创建G不晚于本次请求G、见证.H等于所提交原H，不能在原观察H读取后来才发布的观察记录；再按该原H证明实际F内容，FC完整域真实判定，采用角色必须有该观察F的当前名称指向此FC。第一去重为业务标识/概念/角色，第二为观察身份/概念/角色，内容同义复用、不绑定新key，异义拒绝；次数从真实记录重建。后台域扫描、并集生成、边精简和原key重放不调用计数。释放12/13不删记录，按创建H原边展开历史，资料已清理返回明确不可用，不能计为零次。

共享FC上位必须同FT、严格域包含，顶层完整域直挂特征根；非TOP不得直接跨FT接边。完整多父组替换沿owner原子写集，不把同义连成环。`退出共享特征概念`前核验所有名称8、EC/动态定义模板、用途12、形成依据17/18和支持当前引用；可先显式解除本业务用途 / 来源 / 支持，但不能批量解除其它业务事实。被当前模板引用不得退出；退役不自动拆定义。

精简冗余直接边时明确替代路径已存在；删除中间节点只允许其已退役且无受保护引用，提交请求包含全部直接子节点及其新完整父集合，与退旧边 / 节点 / 自有定义同owner一次事务。旧概念退出请求若不能承载替代集合，`概念树共享退出请求`在旧请求之外明确追加`vector<子节点及预期父/新父组>`；缺少任何当前子边拒绝，不能执行者临时多事务拼退出。FT顶层完整域不自动精简退出，本体根仍不可退出。

共享生命周期允许活跃↔冷却、活跃/冷却→退役、退役→活跃；最后一种请求必须有当前G真实E/F成员及值满足域的重新采用见证，不允许退役→冷却绕过。活跃与冷却可匹配复用，退役返回需显式治理；现合法治理状态迁移沿7140，重用先明确恢复并读回，不重新分配同义当前身份。无使用治理只消费真实用途和显式时间规则，不新增成熟度或固定次数晋级。原生历史关系继续保护端点物理清理；释放当前边不是保证其目标可物理清理。

新key首写仅在正式首次查询未找到后按原G验证；命中先按首次H重建完整规范写集和映射，不因为当前父边 / 名称 / F已变化拒绝合法原key。已发布后所有读回失败或异常保留首次H与可能发布。无须变更与查询成功没有key绑定，不得用于声称某个新key已发布。预算不参与等义，业务定义 / 来源 / 原H / 预期集合全部参与。

## 7. FC新请求、实例阶段与继续

保留特征概念应用服务类名及(c,F&,E&)依赖、静态根变量 / getter / 末实例清理；构造改核验共享交付已启用及相同F/E对象。根仍从同C正式读取，六字段预算1、同G首尾守卫，不建立根。删除当前`评估成熟`及专用请求/结果；不提供永远失败的成熟包装入口。

新具名公共值：`共享特征应用预算{概念树共享预算 数据;uint64_t 最大派发步骤,最大保留尝试,最大遍历节点;}`；`共享场景限制{S,E,原场景H}`。场景限制只由场景应用传入并同实例绑定，普通FC不依赖S模块构造。需要S核验的重载显式借用同库场景服务，沿现`使用同一场景基座`，不以相同G数字证明同库。

`共享特征生成请求{版本=1,Gread,原H,E,FT,类型域身份,跟踪身份,特征类值 实际拟建值,观察标识,时间,共享操作键组,预算}`；`共享特征关联请求{版本=1,Gread,H,E,F,值事实,成员关系,类型域身份,跟踪身份,观察标识,时间,键组,预算}`。生成用于不存在同FT当前成员；关联用于明确已有F。不得把生成解释为覆盖同FT成员。两者的跟踪须经A完整读取证明E/FT/类型域相同，校正已正式存在；本片不自动新建校正、复制作假观察或从窗口猜阈值。

键组不是预先无上限vector：固定F新建key、E成员key、A观察key、TOPkey、名称key，加`vector<共享步骤键{阶段角色,规范定义或概念身份目标,非零key}>`用于有限本次域创建、父边替换、使用记录。开始纯值校验全部已提供key互异、完整业务语义目标相符且拒绝名称/用途禁用前缀；动态发现新步骤时在派发前由调用方继续材料补足明确key，不能由应用猜散列、重用别步骤key或因没key跳过工作。结果返回准确待准备步骤（角色及完整目标），调用方提供key后原继续，不改原输入。允许一次请求提供足够的有限键分配表完成；键不足是待继续，不是错误创建。

应用public新方法（均明确请求名）：`生成并关联共享特征(const 共享特征生成请求&)`、`关联已有共享特征(const 共享特征关联请求&)`、`继续共享特征操作(const 共享特征继续请求&)`、`查找适用共享特征(const 共享特征查找请求&) const`、`读取共享特征实例(const 共享特征实例读取请求&) const`、`读取共享特征概念树(const 共享特征图请求&) const`、`治理共享特征概念(const 共享特征治理请求&)`、`转换旧特征概念(const 共享特征转换请求&)`。三个写/继续另保留场景限制+同库S服务重载；旧生成 / 关联 / 继续的未发布DTO及方法由新具名接口替换，不维持第二套旧宿主生成算法。

查找请求{版本,Gread,H,E,F,值,成员,类型域,预算}，只查已发布真实F；实例读请求同形但名称可空；图请求{旧图请求,共享预算}；治理请求{版本,Gread,H,目标,完整当前生命周期及父子预期,目标治理状态,optional<共享实例见证>重新采用见证,optional替代子父组,显式释放引用组,阶段键,预算}；转换请求{版本,Gread,H,旧kind1概念,真实类型域身份,新建key,预算}。转换只建立新定义及来源，不自动替换所有旧EC/DC定义或扫描全库名称；需要实例新名称时另用关联入口重新真见证。治理不自动设置固定次数 / 成熟资格，不凭没有用途记录擅自删除；调用方提出合法目标状态和完整引用处置，应用核验后按data正式事务执行。

### 7.1 固定业务阶段

0. 同G/原H完整验证E、FT类型合同和跟踪；生成先遍历E全部直接F，逐F读取类型，已有同FT任何成员则拒绝并返回必须显式关联，不能用全局FT查询代替E范围。随后F拟建值预检零写，确认支持且无输出FT冒充普通值。关联先精确核验F/value/member及实际类型。场景限制同时核验原H和当前G的S直接E关系。
1. 生成：保存完整F新建请求，调用原F新增；恢复首次F后保存完整E新增成员请求，调用原E。从真实E回执提取精确成员ID，把E首次发布H作为本次已生成观察H写入继续材料。原业务原H仍是起点，不把后来成员回填成原H已存在。关联：原观察H就是请求H，零F/E写。
2. 保存并派发A积累观察，见证使用真实F/value/member及正确观察H；读取全部匹配与本次新区间。A记录只证明结构上下文，应用事先已实际读取E。F当前有变化时先返回被后继覆盖，不把原H记录当当前成功。
3. 精确形成 / 复用该FT完整域概念和本次命中的全部区间原子概念（A全不匹配时为真实新域）。同域跨E只一份身份；旧FC转换另走显式旧依据，不能偷偷抹掉旧宿主字段复用旧ID。
4. 同截止读取全FT共享域图，从TOP按严格包含向下遍历，所有实际判定通过F新原子入口；TOP也验证完整类型允许值。每节点的并集任一原子适用即适用，但返回全部实际比较回执，不用首成功掩盖后续预算 / 材料错误。选择全部适用且不存在更具体适用下位的保留域；多个不可包含项形成 / 复用精确并集共同上位，绝不按ID/时间/最小距离择一。
5. 根据完整FT保留定义集计算严格包含偏序的直接边，每个节点接所有最小严格超集；TOP接特征根。按规范域排序仅决定有限事务顺序，每节点完整父组替换原子；保留所有合法多父，旧冗余根边一同退出。新父形成后不让它不断增加本轮遍历原子，有限输入与游标冻结。多节点整理可能分阶段，未完成不能回报整树完成。
6. 对最终单域或并集重新fresh核验当前E/F/value/member，按新名称请求绑定 / 替换；记录各实际适用域命中和最终采用（各独立业务键）。最终同一freshG重读实例、E成员、名称、新概念 / 域、修改过的直接边及用途，尾G一致才完成。原值保持，不能用代表 / 区间端点覆盖F。

公开只读`查找适用共享特征`执行完整匹配与最具体选择，返回全部适用最具体事实、比较回执及规范并集建议；零写，不声称共同上位已经形成。步骤4的形成/复用并集和步骤5的父边整理仅由生成、关联或其继续写流程执行，必须保存各原请求与实际发布结果。

当只需名称继续而域 / 概念已发布，仍逐原请求恢复并fresh核验当前归属；当前F已移出或换代返回被后继覆盖，保存合法已发布阶段，不创建替代F、不换E、不回滚别owner。多适用并集可能不是其中最细节点，但它是全部不可比较最细适用结果的精确共同上位，重复查询得到同一规范定义。

### 7.2 继续材料、结果及失败

`共享特征继续材料{版本=1,variant<生成请求,关联请求,治理请求,转换请求>原业务,原H,optional生成观察H,vector<共享步骤>步骤,vector<旧尝试>归档,完整冻结域集及遍历位置,optional已定位F/成员/观察/概念/名称,待补键目标组}`。每步骤含阶段角色、所属owner、完整原请求variant、optional首次H / 回执提示，提示不能跳过provider原key查证。派发前保存原请求，不增加持久意图账或依赖应用私有map。读预算与单次派发上限分开，全部保存容量不足不能截断材料。

确认事实闭合variant为F结点、E成员事实、A长期观察事实、概念树应用存在事实、共享定义事实、共享名称事实、共享用途事实、直接上位事实和旧概念正式读取事实。结果含业务状态、阶段状态、原H / Gread、已确认事实组、继续材料；业务状态完成 / 待继续 / 被后继覆盖 / 需显式治理 / 不支持 / 入口拒绝 / 预算不足 / 历史不可用 / 资源失败 / 内部不一致；阶段状态未派发 / 部分确认 / 先前阶段待核验 / 全部确认。已准备但未正式查证的请求必须保留可能已发布标志，不能把已证实迁出覆盖成模糊错误，也不能因迁出丢原请求。

每owner恢复原key优先；F/E裸G漂移只有该调用正式未找到key且确认未发布时才能归档旧尝试、同key freshG重准备，下一次仍owner查key优先，不能说该key从未或永远未绑定。资源、内部或历史失败不能走同义重准备。原key恢复只证明首次发布，当前完成需fresh共同G，不能恢复一项就信任整段cursor。

治理 / 释放请求先在原H读目标真实角色与完整所属，避免未绑定key被用来先写错根。支持 / 名称 / 依据关系ID及完整集合等义交其owner正式原G核验；调用方不能用自填回执证明边退出。最终边退出由原key真实写回执+fresh不存在及其它父保留证明，不宣称没有公开入口的物理退出H读回。

## 8. EC、DC和场景全分支适配

EC保留类名、全局根及E/F依赖。旧未发布静态定义 / 预算 / 继续类型调整为新具名应用结果，`形成见证`不再裸get旧FC：先判返回variant，kind1只可按真实旧定义保留读取或显式转换；当前新形成选择kind4逐原子调用F公开判定。每项见证同H实际E成员、F/value、FT成立，多约束为合取，多同FT允许逐项真实见证。新定义仍当前显式模板组合，不谎称已实现7110完整组成 / 关系穷举。最终支持前及fresh读回重新用当前H=G验证原F/member/value；已移出则被后继覆盖，原支持历史发布仍可保留。

EC生成用新`创建存在概念_v2`请求（公开方法可保留名，参数是具名v2），来源E或较早同根EC二选一，角色16归属明确。共享模板引用真实kind4，通用concept私有验证识别两种FC，不要求新FC宿主。EC读树使用新应用图结果；治理通过新v2治理请求/结果，支持的业务字段保留，但需5.2显式预算包装/结果及支持载荷；输出与继续材料不得通过旧静态事实强包新kind4或角色16。所有新kind分支显式覆盖后才算适配。

DC保留精确S/E上下文、有序相对时间及显式三元状态见证；旧已发布动态定义模板身份不原地修改。新请求仍可引用真实旧kind1（旧同宿主限制继续）或共享kind4（无宿主字段），通过新应用模板读取分支，不能使用旧`读取动态特征模板`结果强装新域。每槽旧F在原状态创建H核验主体成员和值，当前变化目标按其H/FT真实对应；kind4调用F新原子判定，F.H允许旧实例已在当前G退出。data动态定义准入 / 历史读的两处宿主比较同样按模板种类分支，不能仅改app。

DC应用没有F构造依赖，使用concept新增窄只读`判定应用特征模板(const 概念树应用模板判定请求&) const -> 概念树应用读取结果<概念树应用模板判定事实>`，请求`{版本=1,读取头,模板,特征原子实际值见证,共享预算}`转到所绑定F原子入口，返回新`概念树应用模板判定事实{真实模板variant,vector<特征原子域判定事实>原子回执,适用}`；kind1使用旧精确域匹配，旧范围仍被明确验证，kind4逐项同规则。所有原子按同G/H实际判定且预算合计，完整域也需真实值类型准入；适用bool只作控制回执。它不建立新owner、getter或第二算法；数据不验证E成员，DC分别通过E/Q/D已有公开历史闭合。此为本片第17个concept公共新增 / 具名替换入口。

场景类保持S直接E上下文，不造S概念或根。其公开子请求variant、继续材料、子结果、组合投影必须同步FC新类型与EC新读取事实，生成/关联/继续/读取四路径不能遗漏。每次owner派发前检查原H及当前G的S→E；最终fresh共同G重读S/E/F、名称、EC支持。S迁出时业务=被后继覆盖、阶段可同时先前阶段待核验，完整cursor保留，不能先重放一个未验证请求再发现已换场景。

所有根静态变量、mutex / provider计数、只读optional getter、末实例清空、禁止copy/move保持原WIP含义；构造与读取新启用标志同步，不要求外部root参数。没有第二套兼容wrapper维持成熟或旧宿主生成业务。

### 8.1 DC与其它应用的显式预算请求

DC新增具名`共享动态概念应用预算{概念树共享预算 数据;uint64_t 最大场景关系数,最大存在关系数,最大状态数,最大变化特征数,最大快照特征总数,最大继续尝试数;}`，全部来自调用方；`数据.基础`仅供旧操作内部字段，完整`数据`直接给新应用读/写/关联入口，F七维来自`数据.特征`。世界S/E/D/Q的六项预算按原含义路由各真实历史入口，不能用共享原子数或G替换。

新增`共享动态概念形成请求`和`共享动态概念上位请求`：分别显式保留旧形成请求的Gread/H、S/E/D、槽见证、创建/支持键，及旧上位请求的Gread、下位、保留槽模板、创建/接边键，预算改为上述新具名类型；合同版本1。新`共享动态概念继续材料`保留原冻结定义、首次读取G、已定位概念、原业务variant；建立/支持/接边原请求均为完整`概念树应用写请求`，未绑定归档同型，回执提示为`概念树应用写入结果`。`共享动态概念继续请求{版本=1,Gread,材料,共享动态概念应用预算 本次预算}`；本次预算可调整工作量，已保存业务和原key不得改义。新`共享动态概念应用结果`保留旧动态业务状态/阶段/Gread/概念/支持/直接边字段语义，继续材料与步骤回执采用上述新类型；概念仅接受应用定义返回的真实kind3，不能盲getEC/FC分支。

DC类保留全局根与已有已发布旧请求重载；为`形成或复用动态概念`、`形成上位动态概念`、`继续动态概念操作`各增加上述新具名请求重载，返回新结果。新树读取重载接`概念树应用图读取请求`并检查动态根，返回新应用图结果；迁移/退出/支持退出/两类依据释放的新重载均接`概念树应用写请求`并检查操作variant与方法含义相同、原H目标归属动态，然后派发一次新应用写入口。旧重载不得以缺失共享预算构造任意默认值；旧支持范围与原读取/拒绝规则不变，不产生第二套共享业务算法。

FC已冻结共享预算直接供给名称、用途及新关联读取；EC v2预算同样完整传入创建/治理，支持步骤改保存新应用写请求/结果，双向支持读改新关联入口。EC继续材料中的步骤请求variant含EC v2请求与应用写请求（支持），步骤回执按相应新EC结果/应用结果分支，不能在继续时丢外层预算。scene所有子请求及投影继续沿FC/EC具名类型；没有从场景关系数推概念预算。任何调用方只提供旧8项预算时不得进入共享路径。所有新读失败清结果，跨owner失败仍保留已确认阶段和完整cursor；最终fresh共同G必须经过同样完整预算路径，不另设免预算验收入口。

## 9. 完整读取、构造及跨进程恢复约束

新私有结构分派必须覆盖旧kind1/2/3和新kind4。受影响路径逐项落到同一分派：应用定义 / 图 / 模板读取、共享唯一查找、创建共享候选 / 全库已发布对象核验、EC v2创建模板及完整图、动态新模板准入 / 读回、支持目标校验、父边新增 / 替换 / 退出、共享治理 / 不可逆退出、来源 / 名称 / 用途解除，以及各自原key首写重建 / 无须变更 / 发布后结果读回。公共旧完整读取仍使用旧完整用途，新结构读取显式选择结构用途，不能更改旧审计含义。

没有共享交付的旧实例读取其旧合法对象继续成立；如果操作遇到无法表示的新kind4，明确不支持而不跳过它，不能报告错误的全库唯一或完整图。新组合实例的所有新应用读可以读取完整新旧图，旧显式完整读遇到缺原证据按原合同失败。未知其它owner引用不靠新代码枚举全库猜测，仍由L1同G原子守卫阻止退出；派发后失败保守可能发布，不假称预检查遍历了未公开的关系。

共享定义、来源和用途读取的独立性取自本owner完整结构与F不可变类型 / 域载荷；原F/材料退出或依法不能审计不改变定义。原子直接复制到概念定义是抽象规则载荷，不是第二份world当前值；必须和来源读回完全互证。类型规则改变不能原地更新已有共享定义或A类型合同；另行正式FT / 域身份，旧来源按其版本读回。旧域[单值]归一化只按数学等价，不能把I64组重新解释成图、把旧宿主身份改成新全球身份。

跨进程只用当前L1持久运行包。新名称永久定位首次key、21角色登记、共享原子内容、用途去重与完整原请求恢复全部由真实持久事实重建；不得把调用方cursor当证明，也不得从日志复制已成功状态。当前引用释放不会使原native历史端点可随意物理清理。原物理来源确实不可回查时，结构读与明确来源审计分账；不要求构造不可能的“来源边完整而其端点已清理”TTL正例。

## 10. 验证矩阵与完成边界

资源根`D:/TEMP/海中鱼巣/FEATURE-SHARED-CONCEPT/01a07099-20260906-shared`；Debug/Release隔离src/build/data。等A发布后从其真实代码快照出发，冻结A确切F blob、六源码和工程两WIP输入SHA；原大计划两记录只保护不修改 / 不发布。复用A数值内核的实际断言证据，并对新增公开原子入口真实运行，不以A同E匹配代替跨E验证。

| 组 | 必须实际通过 |
| --- | --- |
| F供给 | 两E不同tracking / 无同E区间仍可对共享冻结原子判定；源代表F退出后实际新F匹配；EC无观察ID/time仍以真实四字段判定，缺F/value不填常量；I64区间边界及完整域；二值全部原子、空图 / 错规则 / 错value / 预算 / 溢出无值；预检零G不造F，输出FT普通值拒绝 |
| 累计预算 | 真实两原子分别读取获得用量u1/u2，同G组合总预算恰u1+u2成功；选择第二次必耗维减1，单项各自可过但组合必须预算不足且无部分成功；不适用第一原子仍扣量。标量样本/像素/点对预算0成功且用量0；二值单原子点对0失败；预检引用材料的后续解析计数在最终回执内。共享模板含TOP时完整域历史判定用量不得漏计，尾G失败不得返回成功用量；A原12零量行为回归不变 |
| TOP历史对照 | 建立真实引用材料V及引用它的F，在H有效；依法退出F及V而保留历史、不中断L1历史引用保护。G>H时完整域和合适单原子均成功、真实身份同H且完整域差异空；拟建预检同原始引用在当前G拒绝且零写。分别覆盖I64引用及二值引用；二值TOP点对预算0成功但像素不足失败。当前名称/支持采用仍拒已退出F，不把历史判定当当前成功；历史材料实际不可用则两种判定均不得成功。两种成功回执七用量完整计入跨模板预算 |
| 共享定义 | 同FT同域跨E仅一个新FC、TOP唯一；标量相邻合并 / 有间隙并集；二值原子去重和非度量反例不错误推包含；同规范域换输入顺序同身份；不同FT不误合并 |
| 区间来源定位 | 同跟踪多区间与不同跟踪来源均从21+7真实二元组定位；非相邻[0,1]与[4,5]形成两个输出原子时各来源只映射其对应项，相邻[0,1]与[2,3]合并后两来源可映射同一[0,3]；两者均验证全部来源规范并集等于输出。源跟踪错配、数组独立错序、缺21/多21及旧20角色交付拒绝；只释放7或21拒绝零G，成对释放与原key重复后历史结构读仍定位原域。第三进程从真实持久21恢复，不能依赖首请求或A私有关系 |
| 旧结构 | 真实旧28 kind1及动态8继续按旧含义读；旧宿主保留；显式旧精确I64转换新共享域；无合法类型合同拒绝；当前新概念链成熟公开方法及DTO已删除（不修改旧L2自身历史代码），未发布32不伪装生产兼容 |
| 图与治理 | 最小严格超集多父、冗余根边原子替换、并集上位不产生环；命名 / 模板保护退出；显式退役恢复有实际见证；中间退出与完整替代边单事务，缺边拒绝；旧无法表达入口不支持而不漏项 |
| FC既有F | A观察→所有命中域→TOP / 原子精确复用→最具体多适用并集→名称与用途→fresh共同G；源E不同无需改FC；原值不变 |
| FC新F | 拟建合法预检→真实F→E成员→正确新观察H→概念 / 名称；同FT已存在拒绝静默覆盖；失败保留阶段，不能把概念失败说成F未创建 |
| 原键续作 | 每个owner实际单步中断，调用方补明确待准备key后继续；已保存请求真实恢复；同义不重计、异义冲突；已发布源F移出后被后继覆盖+原请求保留；预算不足不截断材料 |
| EC | 每个新kind4模板同H及最终当前G的F/value/member实际见证；多同FT合取；已有EC根读 / 支持双向 / 上位 / 来源解除覆盖；不声称存在穷举 |
| 统一预算 | 分别真实消费8种应用写分支及5种关联查询：kind4支持/EC16支持、动态共享模板创建/上位/治理/退出/依据读退、双向支持与动态核验。显式共享预算恰够成功、原子/首次材料/F预算不足零部分结果且无新事实；旧八预算入口遇新kind在写前拒绝、G不变。原key相同业务改变仅预算可重放，预算不足保留首次H/原包装请求；DC新请求明确传完整预算，三进程cursor读回不丢字段 |
| 释放结果基数 | 真实一个world引用被两个以上合法所属概念引用，并使其中一个有多条待退边：一次形成释放返回全量去重升序所属组及全部真实退出编码；请求乱序规范化、跨所有者伪预期/遗漏预期拒绝、同key业务异义零新G。动态单所属释放恰一项。形成空预期无须变更返回空组，动态空预期无须变更仍一项，均无首次H/key绑定；其它动作组为空。两释放首写/原key、所属后续退出后的早H重放及第三进程完整组恢复；组读预算恰够/不足无前缀成功，已绑定key不足保留原请求和首次H。应用cursor往返保留全组，不任取首项，不把旧DTO尾加字段 |
| EC依据ABI | 新应用定义/图返回普通EC第三分支及真实16全字段；创建世界来源EC及抽象来源EC、无须变更、上位/迁移/退出v2、完整依据释放和每阶段原key均真实读取。释放前取公开完整预期，错关系/来源/所属拒绝；释放后旧依据保留、重复原key无重写。第三进程恢复16事实及已退出边，旧完整DTO/variant保持原样，不得隐藏依据或先修改后拒绝返回类型 |
| DC | 两状态旧F快照与当前变化目标三元见证；新共享FC无宿主且域真实适用；旧kind1宿主规则仍生效；相对时间 / 场景上下文不改；新模板variant不会进入旧裸std::get |
| 场景 | 直接S→E限制、生成 / 关联 / 继续 / 组合读取全新DTO；迁出同时返回被后继覆盖与先前阶段待核验，不能静默换场景 |
| 根与成熟 | 三静态全局根绑定 / 多实例 / 最后清空 / 不同provider拒绝回归；无外部root实参回退；无新成熟记录、无旧成熟调用 |
| 持久 | seed进程建A真实数据 / 新共享域及名称用途；continue独立进程跨E命中并原key继续；audit第三进程完整域图、名称含退出历史、用途去重和支持共同G独立读回；不依赖进程map |

最低完成要求为全部六源码 / 工程候选在Debug和Release实际编译、链接、上述合法消费者运行成功；全依赖闭包预计至少原21模块，按实际生成模块清单报告，不能只编局部新文件。每个新公开方法要有至少真实成功或其专用合法拒绝消费；未编译的包装不是能力。

各片顺序可以边编译边修纯代码错误，但最终共享ABI及全部消费者必须同一冻结代码结果验证。资源失败、尾G竞态等无法稳定注入的分支明确NOT_RUN；静态审阅不算动态通过。正式集成验收、主程序 / 相机生产接线、全局初始化、自动完整存在穷举和存量全库迁移未覆盖。A与B/C/D本片全部发布后才可声明本次长期特征到共享概念应用闭环；未通过阶段不得借计划状态升级为代码完成。
