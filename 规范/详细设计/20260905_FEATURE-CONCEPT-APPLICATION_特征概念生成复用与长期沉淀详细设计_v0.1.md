# 特征概念生成复用与长期沉淀详细设计

身份：`FEATURE-CONCEPT-APPLICATION`；版本：v0.1；日期：2026-09-05。

状态：冻结设计。施工资格只由对应计划在计划索引中的精确版本及执行 S0 决定。

## 1. 目标及正式边界

交付 `海中鱼巣/业务/应用服务.特征概念类.ixx`，真实实现特征概念的生成、按值域匹配复用、已发布实例的名称字段节点关联、长期观测记录、成熟评估及冷却/退役。依据用户本轮明确目标及 2220、4240、7140；特征值及宿主结构分别服从 2100、1120、1130、4120、4230。

概念可以早期建立并立即复用；成熟要求在同一个存在、同一个特征类型范围内长期重复得到支持。成熟程度与活跃/冷却/退役生命周期分账。概念节点本身是实例特征的名称字段节点，字段以实例到独立概念的具名关系表达；自然语言名称仍是 2210 的概念到词条关系。

采用单一概念 owner：扩展现有 `L2概念结构服务`，不新建概念 owner，不迁移整个旧概念族，不建立只转发的 `数据服务.概念类.ixx`。本轮不接普通应用初始化或生产采样线程，不替换旧服务，不产生词条，不实现其它根概念、轮廓识别、自动聚类、自动拆合或物理删除。

## 2. 当前事实及复用裁决

初扫基线为 `3f91896d4e300a4b62e57f3f4d11092505d384e9`；正式规范基线为 `cb89b5c60b172becb662650608d4aa198a587399`，设计冻结基线为世界树切片发布后的 `f61aee960aad54b6cef1c5f05693c1ccd6b79c34`，main。源码以执行 S0 的已发布 HEAD 再核对。

| 能力 | 当前事实 | 本轮裁决 |
| --- | --- | --- |
| 普通概念、四根、签名唯一、定义、上下位 | `服务.L2概念结构.ixx` 已有真实 owner CRUD，`建立普通概念` 同签名异幂等返回引用冲突 | 复用身份及写锁；补公开精确查询，业务先查再建，竞争冲突回查 |
| 生命周期 | 7140 活跃→冷却、冷却→活跃/退役、退役→活跃已有 provider | 复用，不增加“成熟”生命周期枚举 |
| 新特征 | `数据服务.特征类.ixx` 支持 I64、VecI64、VecU64 及不可变值节点引用，普通新增/查询独立于旧 L2 特征实例族 | 复用原值 CRUD；新增值域业务不放入数据类 |
| 新存在 | `数据服务.存在类.ixx` 已有查询存在、增加/删除特征成员关系 | 复用，观测必须反查宿主特征成员关系 |
| 旧概念对世界引用 | 只识别旧 L2 强类型存在/特征定义/特征实例，不能接受新类稳定编码冒充旧身份 | 新增具名引用变体及真实新 provider 读回；不强转 |
| 名称字段节点、观测、成熟 | 当前新特征类和概念 provider 均没有此专用结构 | 由同一概念 owner 新增三类结构，应用形成完整实例投影 |

知识入口与概念/特征用途卡仅作索引；其中多数条目早于新数据类，以上裁决直接回读当前源码。历史 README 的抽象树位置、实例缓存和旧生成函数不能直接成为实现依据。

## 3. 文件与依赖

核心生产文件：

- `海中鱼巣/领域/L2普通概念结构.数据.h`：新增纯值 DTO、引用变体、成功谓词及数据合同。
- `海中鱼巣/领域/服务.L2概念结构.ixx`：唯一概念 owner 的专用结构读写与新引用校验。
- `海中鱼巣/业务/应用服务.特征概念类.ixx`：导出模块 `海中鱼巣.业务.应用服务.特征概念类`，公开 `特征概念应用服务`。
- `海中鱼巣/领域/数据服务.特征类.ixx`：仅在当前缺失时新增只读特征类型确认及历史引用读回合同；不得改原始新增/修改/删除或重放语义。
- `海中鱼巣/领域/数据服务.存在类.ixx`、`海中鱼巣/领域/数据服务.特征值类.ixx`：补齐具名历史成员见证与历史不可变值材料读取；原始写接口不变。
- `海中鱼巣.vcxproj`、`海中鱼巣.vcxproj.filters`：登记新应用模块。

应用依赖概念服务、特征类、特征值类、存在类的公开合同；概念服务为校验专用端点只借入新特征/存在 provider 的只读引用。新特征及存在类不 import 概念服务。所有服务实例均由调用方拥有且生命周期长于应用；应用无 L1 写端口、无 owner、无权威内存容器。

旧概念构造函数保留原 ABI；增加一次性 `配置特征概念依赖(const 不可变材料服务&, const 特征类数据服务&, const 存在类数据服务&, const 特征值类数据服务&)`，仅借入只读引用并初始化同一 owner 的专用结构定位。重复配置同一对象精确重复；不同对象拒绝；配置前专用入口返回结构未配置。初始化不得改变既有根/概念事实。

三类新数据服务各增加 `bool 绑定于(const L1事实基座服务&) const noexcept`，仅内部地址比较且不导出 owner。概念配置用自己的 L1 引用逐一验证同一基座；稳定编码相同或数字 G 相等不能证明同库。概念服务另提供 `bool 特征概念依赖匹配(const 特征类数据服务&, const 存在类数据服务&, const 特征值类数据服务&) const noexcept`，应用构造验证全部为已经配置的同一实例，避免注入同型异库 provider。

不可变材料服务已提供 `使用同一L1事实基座实例(const L1事实基座服务&) const noexcept`，配置直接复用该地址检查，不修改材料provider或增加第二同库检测接口。

仅本体根C1加不可变材料服务及三新类必须足以使用本功能，不要求装配旧七服务或第二套旧特征 owner。替换普通概念创建、读取及治理涉及的无条件 `普通概念依赖_.完整()` 为逐实际引用分支检查：材料引用只需材料provider，新存在/新FT/新实例引用只需对应新provider，旧引用只在出现时要求对应旧provider，缺失返回结构未配置。旧完整构造及旧引用行为保留，禁止用“先配置全部旧服务”掩盖新专用能力无法运行。

## 4. 值域、规则与确定匹配

`特征概念值域` 为显式二选一：

- 精确值：完整 `variant<int64_t, vector<int64_t>, vector<uint64_t>>`，类型及逐分量完全一致；空向量按特征类现行材料合法性处理。
- I64 闭区间：`下界 <= 上界`，包含两端；零宽区间规范化为精确 I64 单点。其它表示不得转换为 I64 区间。

引用值必须经特征值类正式读取出完整材料，再进行同类型比较；不得比较地址、摘要或截断向量。FT、宿主及句柄型引用进入具名定义关系，不得编码为签名中的整数冒充值。

`特征概念形成规则` 只含非零匹配/分区规则身份、非零版本、可正式读回的不可变规则材料身份及显式有序允许区间组（可空）。`特征概念成熟规则` 独立含非零规则身份、非零版本、不可变规则材料身份、`最少独立观测数`、`最短观察跨度`、`最小观测间隔`。绝对时间及间隔采用 0050 的强时间类型与纳秒语义，禁止自造毫秒时标；三个阈值均非零，最少观测数至少 2，数值来自显式请求，不设置业务默认阈值。同一类规则同身份/版本异内容拒绝。

概念签名只包含匹配/分区规则及实际值域、宿主和 FT；成熟规则及阈值不进入抽象签名。调整成熟阈值不建立新概念、不改变含义，成熟评估记录保存实际采用的规则版本。既有成熟记录只证明其具名规则下的结论，不自动证明后来更严格规则也满足；新规则评估不足返回证据不足并保留旧记录历史，不将旧结论冒充本次结果。

形成时先按宿主、FT、规则身份/版本召回当前活跃的特征概念，逐项正式读回完整签名和值域：零命中则按允许区间组选择包含当前值的唯一范围，仍零命中则新建精确值域；多范围或多既有概念命中均返回完整确定排序候选及匹配歧义，零后继实例写入。不得按排序首项、最近中心或最窄区间私自消歧。未成熟活跃概念参加匹配；冷却和退役不参加普通自动召回。

同根、同规则、同完整定义和值域必须唯一；同宿主/FT 下不同范围可形成多个概念。边界或抽象含义变化建立新身份；本轮不自动调整、拆分或合并区间。

`生成值域细分候选` 真正执行显式分区规则：输入上述规则及同宿主/FT 的正式独立观测记录组，按规则原序遍历区间，逐条用闭区间包含关系收集观测；无观测的区间不生成候选，有观测者形成该区间、全部匹配观测身份、首次/末次观测时间和独立观测数的值式候选。精确值规则对全部材料逐值确定分组，形成单值候选。同一观测命中多个区间时保留各候选及重叠标记；候选不是自动发布许可。应用采用某候选生成概念时仍走完整唯一性与歧义分支。规则没有相应表示的细分方式时返回明确不支持，不猜测向量距离或自行产生区间边界。

## 5. 数据合同与事实布局

新增专属版本 `L2特征概念合同版本=1`。公开身份沿用 `L2概念身份`；新世界引用使用独立 wrapper，至少含新存在引用、新特征类型引用、新特征实例引用及新特征精确值引用。禁止用这些 wrapper 接受旧族身份；provider 按新类正式结果校验。

公共请求头使用 `L2结构请求头`。写请求使用完整语义绑定的 `L2结构幂等身份`，读取使用非零共同事实截止及显式预算。专属状态固定为：已创建、精确重复、已读取、已绑定、已记录、已成熟、已迁移、入口拒绝、未找到、目标已退出、结构未配置、事实代次漂移、幂等冲突、引用冲突、匹配歧义、证据不足、数量预算不足、资源失败、内部不一致、已可能发布。

对无法支持的材料表示另返回 `规则不支持`，不得按空候选处理。数据层不存在匹配与证据不足的自行裁决；应用使用这些状态表达业务结果。

上述前20项按列出顺序固定编码1—20；追加 `规则不支持=21`、`历史材料不可用=22`、`已退出=23`、`材料不足=24`。状态10“目标已退出”是拒绝，状态23只表示本次关系退出完成，两者不可合并。成功谓词只接受适合本结果类型的1—7或23，并同时检查版本、非零截止和完整对应载荷；匹配零候选使用已读取+空完整集合，不能冒充概念已生成。

### 5.1 特征概念定义

普通概念使用特征本体根，初始直接上位至少为特征根；定义含宿主存在、FT、完整形成规则和值域。规范签名中的句柄角色与定义关系一一互证，规则及值域值式内容完整编码。初建治理生命周期为活跃、成熟程度为未成熟。首次概念建立必须把普通身份、签名、定义、根边和未成熟程度事实放入同一概念 owner 写集；不得先发布缺程度事实的专用概念。

成熟程度以概念 owner 的独立当前属性槽表达，`0=未成熟、1=成熟`；未成熟与成熟均不改变普通概念身份/签名。成熟后不得因一次新值不匹配自动回退或扩宽定义；新值参与其它概念形成。

### 5.2 名称字段节点引用

概念 owner 保存 `实例特征 -> 概念` 专用有向关系，记录关系身份、宿主存在、特征类型、创建/退出代次。每个当前实例特征最多一条当前名称字段节点关系。绑定前同截止核验：实例存在且未退出、FT 一致、宿主正式特征成员关系存在、目标为本合同特征概念、规则/值域与应用裁决一致。数据层只验证结构和所提交引用，不自行运行匹配算法。

同一实例同概念绑定精确重复；已有不同概念绑定返回引用冲突，禁止静默替换。退出本关系不退出实例或概念。冷却/退役保留关系及历史；普通概念不可逆退出必须检查该关系，存在当前引用则拒绝。原特征类值、名称文本 ABI、2210 词条名称关系均不变化。关系事实保存绑定时的 Gbind 及当时宿主成员关系身份，历史清理按此核验。

需要显式替换时，`替换特征名称字段节点` 请求必须携带原当前关系及新概念，概念 owner 在单一写集退出原关系并建立新关系；不存在原关系、原关系已非当前或新目标不完整均零写拒绝。应用自动生成流程不调用替换入口。

### 5.3 观测与成熟证据

观测记录属于概念 owner：独立观测稳定身份、宿主存在、FT、实例特征及精确值事实引用、目标概念、观测时间、来源材料引用、观测事实截止 Gobs、当时宿主成员关系身份、发布代次。记录只追加、不可变、不覆盖。独立观测稳定身份全局唯一，同身份完全相同返回精确重复；同身份异材料冲突。不同观测身份也不能把同一来源材料和同一观测时间重复计数。

观测登记先正式读回实例与宿主成员、名称字段关系以及来源材料。请求必须提供非负绝对观测时间和可正式读回的来源材料，epoch零点合法，时间/来源只证明具名输入证据，不声称传感器真实性。重复读取或重放同一观测不能充当新观测。记录时概念必须活跃，实例值必须由应用匹配目标完整值域。后续评估以记录绑定的 Gobs 及成员关系见证回读当时实例、精确值与归属，不要求原实例仍当前，也不能用后来加入的关系冒充过去归属。观测记录的创建截止与所观察世界事实截止分开保存。

成熟评估仅消费正式观测记录：按观测时间、观测身份确定排序；去重后以首个记录为起点，后续记录距离上一个计入记录至少达到 `最小观测间隔` 才计入，得到最大数量的贪心序列。若至少计入两项，把最后计入项替换为全部合格观测中的最后一项（本来已为末项则保持）；替换不会减少数量或破坏与倒数第二项的间隔，且保证跨度覆盖最后观测。数量达到阈值且末项减首项达到跨度阈值，才形成成熟候选；无持续不变要求。时间差经强时间类型的合法比较/差值形成，不对原始有符号值做可能溢出的相减。相同时间最多计入一次。边界测试须覆盖相对时间偏移 0/10/19、间隔10、数量2、跨度19：采用0/19成熟，不能因贪心0/10误判。

成熟发布请求必须携带完整成熟规则、全部被计入观测记录身份、其对应正式事实截止及期望当前成熟事实。概念 owner 不重做业务阈值判断，但必须验证记录均属于同一宿主/FT/概念、引用完整、成熟规则材料可读、确定排序且无重复，并以单写集换代程度事实与保存具名成熟规则和证据关系。应用在写前计算并在成功后独立读回成熟程度、证据及概念身份，不得仅返回进程内 bool。

### 5.4 DTO 字段冻结

`特征概念材料` 精确为 `variant<int64_t, vector<int64_t>, vector<uint64_t>>`；不得在公共数据头 import 新类模块。观测时间与两个间隔均复用 `L2中性时间_v1`：观测只允许绝对UTC纳秒且数值>=0，间隔只允许非负相对纳秒且数值>0。在相应数据头/模块的全局片段按现行包含约定补齐时间类型声明来源，不复制同名类型。新存在、新FT、新实例 wrapper 各只有 `稳定编码 值`；新精确值引用由新实例 wrapper 和 `稳定编码 值事实` 组成。向 `L2概念定义目标`、`L2世界事实引用` 追加适用的新 wrapper，不更改既有 variant 编码顺序；新FT/新存在进入定义目标，新实例/新值进入定义及支持目标。序列化标签采用旧标签之后的新值，历史旧标签读回保持不变。

精确引用标签冻结为：11=`L2新存在引用`，编码 `[11,存在编码]`；12=`L2新特征类型引用`，`[12,FT编码]`；13=`L2新特征实例引用`，`[13,实例编码]`；14=`L2新特征值引用`，`[14,实例编码,值事实编码]`。长度必须完全相等且所有编码非零。旧标签1—10及其长度/含义不变；新世界引用解码允许11—14，但仍拒绝标签1概念身份。新FT历史定义核验不得依赖某个当前样本实例，新存在历史核验也不得要求它仍有当前特征成员。

| DTO | 除版本/请求头之外的完整字段 |
| --- | --- |
| 特征概念形成规则 | `稳定编码 规则身份`、`uint64_t 规则版本`、`不可变材料身份 规则材料`、`vector<特征概念I64区间> 分区` |
| 特征概念成熟规则 | `稳定编码 规则身份`、`uint64_t 规则版本`、`不可变材料身份 规则材料`、`uint64_t 最少独立观测数`、强时间间隔 `最短观察跨度/最小观测间隔` |
| 特征概念值域 | 显式类别、`optional<特征概念材料> 精确值`、`optional<特征概念I64区间> 区间`；恰一有效 |
| L2特征概念建立请求 | 幂等身份、本体根、新存在宿主、新FT、完整规则、值域、来源材料组、直接上位组 |
| L2特征概念读取请求 | 概念、`L2读取类别`、历史截止（当前时为0） |
| L2特征概念组请求 | 新存在宿主、新FT、规则身份/版本、最大候选数；只读当前 |
| L2特征名称字段绑定请求 | 幂等身份、新实例、宿主、新FT、概念、Gbind、当时宿主成员关系身份 |
| L2特征名称字段替换请求 | 绑定请求全部字段、原关系稳定编码 |
| L2特征名称字段读取请求 | 新实例、读取类别、历史截止 |
| L2特征名称字段退出请求 | 幂等身份、新实例、原关系稳定编码 |
| L2特征观测登记请求 | 幂等身份、观测稳定编码、宿主、新FT、新精确值引用、概念、强类型绝对观测时间、不可变来源材料身份、Gobs、当时宿主特征成员关系身份 |
| L2特征观测组请求 | 概念、读取类别、历史截止、最大观测数 |
| L2特征概念成熟请求 | 幂等身份、概念、原程度值事实编码、完整成熟规则、观测证据截止、完整有序观测记录身份组 |
| L2特征概念事实 | 完整普通概念事实、新宿主/FT、完整形成规则、值域、当前程度bool与其值事实身份、`optional<特征概念成熟规则> 成熟规则`、成熟证据身份组与评估截止 |
| L2特征名称字段事实 | 关系身份、新实例、宿主、新FT、概念、Gbind、宿主成员关系身份、创建/退出代次 |
| L2特征观测事实 | 观测记录节点身份、观测稳定编码、宿主、新FT、新精确值引用、概念、绝对时间、来源材料、Gobs、宿主特征成员关系身份、创建代次 |

数据结果使用专属状态、合同版本、实际截止及 `optional<对应完整事实>`；组结果使用完整向量及回显筛选键。应用匹配请求为宿主、新FT、完整形成规则、原始材料、候选预算、期望截止；生成请求再加概念幂等身份与来源材料组。实例关联请求含已发布实例/精确值身份、宿主、FT、概念、Gbind、宿主成员关系身份、本次幂等身份及 `optional<稳定编码> 原关系稳定编码`；空值只准备绑定请求，非空只准备显式替换请求，不自动从当前查询猜分支。成熟评估请求为概念、完整成熟规则、成熟写幂等身份、期望截止及最大观测数；成熟规则是显式本次业务输入，其材料及全部字段必须正式保存到结果证据，不偷偷更换既有记录规则。

未成熟投影的成熟规则为空、成熟证据为空、评估截止为0；成熟投影必须有完整成熟规则、非空且确定有序证据和非零评估截止，规则身份/版本/材料及三个阈值与发布记录逐字段互证。缺项不能返回成功。

`特征概念细分请求` 含宿主、FT、完整形成规则、期望截止、最大概念数及最大观测数；应用先读该宿主/FT 下全部专用概念，再逐概念读正式观测，核验宿主/FT 并按观测键去重后执行第4节分区。专用概念组数据读取不过滤生命周期，普通匹配才由应用过滤活跃；历史退役概念的观测仍可形成分区候选。

## 6. 公开函数与完整签名形态

全部 DTO 位于 L2普通概念数据头；应用专属请求/结果位于新应用模块 export namespace。以下函数均为实例成员，数据读取为 const，应用无写权限副本。

| 所属类 | 完整签名 | 责任 |
| --- | --- | --- |
| L2概念结构服务 | `L2特征概念结果 建立特征概念(const L2特征概念建立请求&)` | 普通概念与专用定义/初始成熟程度同 owner 原子建立 |
| L2概念结构服务 | `L2特征概念结果 读取特征概念(const L2特征概念读取请求&) const` | 当前/历史完整特征概念定义、程度与治理生命周期 |
| L2概念结构服务 | `L2特征概念组结果 按宿主和类型读取特征概念(const L2特征概念组请求&) const` | 完整有界候选，不做匹配、不猜选 |
| L2概念结构服务 | `L2特征名称字段结果 绑定特征名称字段节点(const L2特征名称字段绑定请求&)` | 单一实例名称字段关系 |
| L2概念结构服务 | `L2特征名称字段结果 读取特征名称字段节点(const L2特征名称字段读取请求&) const` | 当前/历史具名关系读回 |
| L2概念结构服务 | `L2特征名称字段结果 替换特征名称字段节点(const L2特征名称字段替换请求&)` | 具名原关系到新目标的同 owner 原子换代 |
| L2概念结构服务 | `L2特征名称字段结果 退出特征名称字段节点(const L2特征名称字段退出请求&)` | 只退出关系，引用历史保留 |
| L2概念结构服务 | `L2特征观测结果 登记特征概念观测(const L2特征观测登记请求&)` | 观测身份唯一与真实引用检查 |
| L2概念结构服务 | `L2特征观测组结果 读取特征概念观测(const L2特征观测组请求&) const` | 同截止完整有界观测集 |
| L2概念结构服务 | `L2特征概念结果 发布特征概念成熟(const L2特征概念成熟请求&)` | 程度与具名证据同 owner 原子发布 |
| 特征概念应用服务 | `特征概念匹配结果 查找匹配概念(const 特征概念匹配请求&) const` | 值域匹配、零/一/多候选 |
| 特征概念应用服务 | `特征概念细分结果 生成值域细分候选(const 特征概念细分请求&) const` | 规则驱动对正式观测分区或按精确值分组，返回多稳态候选 |
| 特征概念应用服务 | `特征概念生成结果 取得或生成概念(const 特征概念生成请求&)` | 先查再建；完整同义冲突回查 |
| 特征概念应用服务 | `特征概念实例结果 关联已发布特征实例(const 特征概念实例关联请求&)` | 校验已发布实例/值/宿主并绑定或显式替换名称字段节点；不创建世界实例 |
| 特征概念应用服务 | `L2特征观测结果 登记独立观测(const 特征概念观测请求&)` | 匹配既有概念与已发布精确值，形成正式独立观测 |
| 特征概念应用服务 | `特征概念实例结果 读取特征实例(const 特征概念实例读取请求&) const` | 同截止完整实例+名称字段节点+概念定义投影 |
| 特征概念应用服务 | `特征概念成熟结果 评估并发布成熟(const 特征概念成熟评估请求&)` | 从正式观测计算，证据不足零写，满足后正式发布读回 |
| 特征概念应用服务 | `L2概念治理生命周期写入结果 迁移概念生命周期(const L2概念治理生命周期迁移请求&)` | 验证为本合同特征概念后复用既有合法迁移 |

新 DTO 必须显式包含上文所列字段，不用字符串 JSON 装业务合同。写结果除状态、版本、实际截止外，成功携带完整对应事实；非成功仅在已可能发布时携带首次发布截止及阶段，不能泄漏未经完成自证的部分成功载荷。读取预算不足不截断成功。

### 6.1 新世界提供者只读入口

以下最小历史补齐属于本闭环同一计划。四个独立请求均含版本、非零 Gread、非零 `H <= Gread`（当前类型确认入口没有 H）、具名端点；成功同时回显 Gread/H，载荷是 H 时刻有效的完整事实，隐藏 H 后发生的退出。任何无法核验的清理历史返回具名历史材料不可用，禁止降格为未成熟。

| 所属类 | 签名 | 必需输入与返回 |
| --- | --- | --- |
| 特征类数据服务 | `特征类型确认结果 确认特征类型(const 特征类型确认请求&) const` | Gread、FT；当前真实类型节点与材料表示；不运行区间算法 |
| 特征类数据服务 | `特征类型历史结果 读取特征类型历史事实(const 特征类型历史请求&) const` | Gread、H、FT；H时刻真实类型与表示，不依赖样本 |
| 特征类数据服务 | `特征历史读取结果 读取特征历史事实(const 特征历史读取请求&) const` | Gread、H、实例、精确值事实；完整实例+值+FT及源事实生命周期 |
| 存在类数据服务 | `存在历史读取结果 读取存在历史事实(const 存在历史读取请求&) const` | Gread、H、宿主、关系预算；H时完整存在及特征成员组，用于独立定义端点核验 |
| 存在类数据服务 | `存在特征成员历史结果 读取特征成员历史事实(const 存在特征成员历史请求&) const` | Gread、H、宿主、成员关系、特征实例；宿主/关系在H有效、关系类型及两端精确 |
| 特征值类数据服务 | `特征值历史读取结果 读取不可变材料历史事实(const 特征值历史读取请求&) const` | Gread、H、不可变值节点、其精确值事实；完整材料及身份/owner/生命周期 |

实现复用现有 L1 `读取所有者范围历史事实`，逐项确认返回读取代次均为 Gread，前后再核对 Gread；owner、类型、端点、`创建 <= H`、`退出为空或退出 > H` 必须全部成立。不能仅用“尚未退出”判断历史。当前属性槽可能已被退出清空，历史值由调用方具名精确值事实取得并检查归属，不从当前槽反推。

现特征类 `读取历史新特征` 拒绝任何后来退出代次，不能直接把该helper包装成历史读取；新增只读H投影核心并保留原始写/重放路径不变。存在类 `读取历史存在快照` 已按H处理生命周期，抽取复用而不重写。特征值类历史读入口同样使用H投影；本轮不调用三类世界provider任何写入口，也不迁移不可变材料服务。

名称关系记录增加绑定时世界事实截止及宿主成员关系见证。退出/替换旧名称关系时，旧实例端点按关系绑定截止核验；已退出实例不阻止退出其仍当前的名称关系。新目标绑定仍要求当前完整；不得因旧端点已退出而把清理永久锁死。

## 7. 单 owner 操作与重试

本应用不创建、修改或退出世界特征/值/状态/动态，不写宿主成员。合法上层先调用 `取得或生成概念`，再通过正式世界特征提供者形成实例及初态，最后调用本应用关联；本轮不实现1130首次赋值或值变化的原子提供者。概念在首实例之前可真实形成；缺已发布实例时关联返回材料不足，不能创建薄节点冒充完整当前槽。

每个公开应用写函数一次最多调用一个概念 owner 写操作。概念生成执行先查再建；关联只绑定或在明确给出原关系时单次替换；观测只登记一条；成熟只发布一项程度与证据；生命周期只进行一条7140合法迁移。各操作独立接收原G和完整幂等身份，无跨owner阶段状态、应用私有操作仓或五键链。

所有应用写请求必须完整包含将被送交概念provider的材料。为保证发布未知后的重试不被重新计算影响，公开 `特征概念写入准备结果 准备特征概念写入(const 特征概念写入准备请求&) const` 纯读完成匹配、定义、关联或成熟证据计算，返回闭合 `variant<L2特征概念建立请求,L2特征名称字段绑定请求,L2特征名称字段替换请求,L2特征观测登记请求,L2特征概念成熟请求,L2概念治理生命周期迁移请求>` 和原G/原键。调用方在执行写入前保存完整准备结果；执行入口逐字采用其中完整请求，不重新抽样或用freshG覆盖原G。便利入口可在同次调用准备并执行一个owner写，但任何未知发布结果必须返回原始完整写入材料供原样重放。

新增 `特征概念写入结果 重放特征概念写入(const 特征概念写入准备结果&)` 直接重放对应结构请求，provider必须在当前G与当前端点校验之前先核对原键首次材料。准备结果不是机器事实，重放provider仍验证完整首次写集相等；伪造材料不取得权限或成功。成功结果按首次发布H读回，不以当前端点后来退出阻断原请求重放。

资源失败、内部错误或无法证明零发布的异常一律按已可能发布处理，返回原完整请求；无发布G也不能推断零写。只有provider明确返回零发布的事实代次漂移时，才能重新准备并保存新G请求。相同签名并发创建的引用冲突只在确认该请求零发布后fresh回查，完整唯一同义命中才复用；不能把任意引用冲突吞成成功。预算不足不截断成功，查询失败不解释成未找到或未成熟。

应用对象销毁后，调用方可用保存的完整请求重建应用并重放；本轮不承诺仅凭业务键恢复被丢弃的准备材料。引用端点历史已物理清理时返回历史材料不可用，不伪造历史。内部不得持有概念写锁再调用同样加锁的公开写入口；复用私有无锁核心，或在公开阶段间释放锁并依靠G与唯一性收敛。

## 8. 结构登记与存储约束

新增结构定位仅在已有概念 owner 内登记：名称字段关系类型、观测族锚点及来源/宿主/FT/实例/概念关系类型、观测时间与稳定键属性类型、成熟程度属性类型、成熟证据关系类型。各新登记幂等身份及本地键必须避开现概念服务已用区间，并列入施工记录；普通概念图只枚举原概念族锚点，不把观测记录当普通概念。

新结构 current/history 的读取只由同 owner 的 L1 一致公开投影和相应跨 owner 的具名公开读回形成；业务应用不得扫描 L1。新值引用不得写进值式签名作为整数；世界事实支持关系和名称字段关系分开，不能把支持关系冒充当前命名。

既有 `退出普通概念` 必须在首次写前确认没有当前名称字段、当前成熟证据或当前观测结构外引用。应用只公开合法冷却/退役，不自动调用不可逆退出。退役保留名称字段与观测可审计性，不增加新的自动删除策略。

## 9. 验证矩阵及完成界限

构建/测试使用 `D:\TEMP\FEATURE-CONCEPT-APPLICATION\<执行任务标识>` 的隔离工程、对象、日志和运行数据；不得覆盖其它任务或项目内构建目录。必须等活动 world-tree 复杂计划完成并释放工程与代码切片后执行。真实 provider 测试必须创建实际 L1 运行包和唯一概念 owner，不得以 mock 容器替代节点事实。

至少覆盖：

1. 首值新概念且未成熟；第二同值复用同概念；关联测试使用已发布实例，名称字段指向概念节点且原值不变；应用零世界写入。
2. 同宿主同 FT 的两个精确值或两个不相交区间形成两概念；不同宿主/FT 不混组。
3. I64 边界、零宽、上下界反序、极值无溢出；VecI64/VecU64 整体精确匹配；不支持的区间表示明确拒绝。
4. 多命中完整返回歧义并零实例写；候选预算不足不返回截断成功。
5. 一次观测、短时间重复、重复观测键、相同来源/时间均不能虚增成熟；满足显式次数、跨度、间隔后成熟发布和读回。
6. 成熟不改概念身份/签名，活跃未成熟可复用；冷却/退役停止普通自动召回，恢复后身份不变。
7. 同键同材料精确重复、同键异材料冲突、G0 漂移、多个同义创建竞争、每种单owner写入响应丢失后原G/原键/完整请求重放。
8. 宿主不含实例、FT 不符、伪造概念端点、名称字段重复异目标、缺来源材料均拒绝。
9. 名称字段退出不删实例/概念；存在当前引用的不可逆退出被挡住；历史观测不混入当前字段。
10. 清空应用对象后重建应用，仅凭正式 provider 读回获得等价概念、名称字段、观测和成熟程度，证明无应用侧第二权威。

成功只证明本模块经真实 provider 的局部服务闭环及相应构建/测试，不证明生产采样、自然语言命名、自动区间学习、轮廓识别、持久跨进程恢复或其它概念根。
