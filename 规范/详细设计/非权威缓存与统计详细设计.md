# 非权威缓存与统计详细设计

编号：非权威缓存-001-D1
日期：2026-07-06
状态：待确认详细设计 / 第一轮非权威缓存和统计方案 / 未改代码

## 1. 设计目标

本详细设计对应 `规范/非权威缓存与统计规范.md`，用于把 MI-V2、MI-BI2 和 MI-D3 中的非权威摘要部分改写成可编码的缓存和统计入口。

第一轮目标：

```text
缓存不作为业务事实。
缓存不进入主信息权威字段。
缓存缺失不改变节点、关系、特征、状态、动态、需求、任务或方法事实。
清空缓存后可由权威结构重建。
缓存写入方和读取方必须明确。
```

## 2. 当前代码事实

当前新项目已有：

```text
索引仓库
主信息仓库 I64 最小值容器
特征值服务 I64 最小值入口
特征服务对特征值服务的唯一直接访问封口
需求目标状态结算最小入口
```

当前新项目未实现：

```text
VecIU64 特征值原始值容器。
特征值内容哈希。
命中次数。
最后命中。
结构统计缓存。
需求结算非权威摘要缓存。
缓存失效和重建入口。
```

## 3. 结构设计

### 3.1 特征值缓存

| 结构 | 用途 | 写入方 | 读取方 | 生命周期 | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| 内容哈希缓存 | 特征值去重和索引查找 | 特征服务经特征值系统或索引仓库 | 特征服务、索引仓库 | 随特征值内容版本 | 清空后可由原始值重算 |
| 命中次数缓存 | 记录索引命中观察次数 | 索引仓库或特征服务 | 诊断层、调度策略 | 可清空 | 清空不影响业务事实 |
| 最后命中缓存 | 记录最近索引命中观察时间 | 索引仓库或特征服务 | 诊断层 | 可清空 | 不是状态发生时间 |

### 3.2 结构统计缓存

| 结构 | 用途 | 写入方 | 读取方 | 生命周期 | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| 节点统计缓存 | 节点数量、类型分布等诊断统计 | 后续统计服务或诊断工具 | 诊断层、显示层 | 可重建 | 扫描权威节点仓库可复算 |
| 关系统计缓存 | 关系数量、关系类型分布、孤立关系诊断 | 后续统计服务或诊断工具 | 诊断层、显示层 | 可重建 | 扫描权威关系仓库可复算 |
| 索引统计缓存 | 索引桶数量、命中观察、重建观察 | 索引仓库或统计服务 | 诊断层 | 可重建 | 清空不影响索引权威项 |

### 3.3 需求结算摘要缓存

| 结构 | 用途 | 写入方 | 读取方 | 生命周期 | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| 累计安全结算摘要 | 人读或兼容统计 | 需求服务或后续结算服务 | 显示层、诊断层 | 可清空 / 可重建 | 权威结算记录仍可追溯 |
| 累计服务结算摘要 | 人读或兼容统计 | 需求服务或后续结算服务 | 显示层、诊断层 | 可清空 / 可重建 | 权威结算记录仍可追溯 |
| 最近结算任务摘要 | 人读最近记录 | 需求服务或后续结算服务 | 显示层、诊断层 | 可清空 / 可重建 | 不作为最近结算事实权威 |
| 最近结算时间摘要 | 人读最近时间 | 需求服务或后续结算服务 | 显示层、诊断层 | 可清空 / 可重建 | 不作为状态发生时间权威 |

## 4. 函数设计

| 函数 | 功能 | 输入 | 输出 | 写入结构 | 入口拒绝 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| `计算特征值内容哈希` | 根据原始 Vec 值计算哈希 | 特征值原始值 | 哈希值 | 无或缓存 | 原始值缺失 | 目标设计 / 待确认 |
| `刷新特征值内容哈希缓存` | 写入或更新内容哈希缓存 | 特征值节点、特征服务、索引仓库 | 局部成功 / 失败 | 内容哈希缓存、索引项 | 无效特征值、无原始值、哈希命名空间缺失 | 目标设计 / 待确认 |
| `记录特征值命中` | 增加命中观察并记录最后命中时间 | 特征值节点、发生时间戳 | 局部成功 / 失败 | 命中次数缓存、最后命中缓存 | 无效特征值、缺发生时间戳 | 目标设计 / 待确认 |
| `清空特征值缓存` | 清空哈希和命中缓存 | 特征值节点 | 局部成功 / 失败 | 缓存项 | 无效特征值 | 目标设计 / 待确认 |
| `重建结构统计缓存` | 从权威仓库扫描生成统计 | 节点仓库、关系仓库、索引仓库 | 统计快照 | 非权威统计缓存 | 仓库不可读 | 目标设计 / 待确认 |
| `清空结构统计缓存` | 清空诊断统计 | 无或统计命名空间 | 局部成功 / 失败 | 非权威统计缓存 | 缓存命名空间无效 | 目标设计 / 待确认 |
| `刷新需求结算摘要缓存` | 从权威结算记录更新摘要 | 需求节点、结算记录集合 | 局部成功 / 失败 | 非权威摘要缓存 | 无效需求、无权威结算记录 | 目标设计 / 待确认 |
| `清空需求结算摘要缓存` | 清空需求结算摘要 | 需求节点 | 局部成功 / 失败 | 非权威摘要缓存 | 无效需求 | 目标设计 / 待确认 |

## 5. 入口前置拒绝

缓存写入口必须在入口拒绝：

```text
无效节点。
错误节点类型。
缺少权威来源。
缺发生时间戳。
缓存命名空间无效。
试图把缓存结果写成业务事实。
试图让需求、任务或方法以缓存值作为完成依据。
```

## 6. 失效和重建规则

缓存失效条件：

```text
特征值原始值变化。
节点、关系或索引重建。
需求结算记录新增、失效或回滚。
快照恢复后缓存版本不匹配。
统计服务版本变化。
```

重建规则：

```text
内容哈希只能由原始特征值重算。
结构统计只能由节点、关系、索引等权威仓库扫描重建。
需求结算摘要只能由需求结算记录、状态、动态和因果证据重建。
缓存之间不能互相推导出业务事实。
```

## 7. 当前计划门禁

本详细设计建立后，下一步应先由用户确认非权威缓存与统计规范和详细设计。

确认前不得：

```text
修改特征值服务、索引仓库或需求服务。
新增主信息缓存字段。
把内容哈希、命中次数、结构统计或结算摘要作为业务判断依据。
实现缓存重建或统计服务。
```

## 8. 禁止扩大声明

本详细设计只能证明：

```text
非权威缓存和统计第一轮代码实施方案已建立。
MI-V2、MI-BI2 和 MI-D3 缓存摘要部分已映射到新项目结构。
```

不能证明：

```text
缓存统计详细设计已确认。
缓存统计代码已实现。
需求结算代码已实现。
旧缓存字段已迁移。
```
