# 旧特征批次同层授权桥清理详细设计 v0.1

日期：2026-08-10

设计身份：`L1-LEGACY-FEATURE-BATCH-PEER-BRIDGE-CLEANUP`

## 1. 目标

物理删除零生产消费者的旧 `特征批次同层授权桥` 模块及工程登记。

完成条件：

1. `海中鱼巣/领域/桥接.特征批次同层授权.ixx` 物理退出；
2. 模块名、类名、三个桥接函数名及工程登记在现行生产范围归零；
3. 新 L1、L2 数据服务、线程、D455、安全算法、日志和容错保持不变；
4. Debug / Release x64 完整编译链接通过。

## 2. 当前事实与裁决

施工事实基线为 `016a9646836ac4b51b3b156cb2ca92c1d9e93e5c`。

- 该模块没有任何现行生产 `import`、类型引用或函数调用；当前唯一命中是模块自身。
- 仓库内其它命中只位于过期设计、过期计划和历史已完成计划，不构成生产消费者。
- 桥内模板只转发 `形成特征批次同层参与包_`、`执行特征批次同层参与包_` 和候选结果；对应旧特征/需求数据操作模块已经随旧四仓反向闭包退出。
- 本模块没有独立数据事实、算法或线程职责，不能由新 L1、L2 或线程基础模块复用。

因此直接删除，不迁移、不改名、不建立兼容壳。其它当前未接启动链的模块不因本结论删除。

## 3. 允许范围

删除：

- `海中鱼巣/领域/桥接.特征批次同层授权.ixx`

修改：

- `海中鱼巣.vcxproj`
- `海中鱼巣.vcxproj.filters`

新增两份专属施工/验证记录。若编译证明存在集合外消费者，停止并返回漂移，不扩大范围。

## 4. 验证

执行具名符号与工程登记归零、Debug / Release x64 Rebuild、`git diff --check`、strict 和 cached diff check。不运行程序、CRUD、自检或集成验收。

完成只证明旧桥退出，不证明相关旧业务迁移、其它未接线模块应删除或 L1—L5 完成。
