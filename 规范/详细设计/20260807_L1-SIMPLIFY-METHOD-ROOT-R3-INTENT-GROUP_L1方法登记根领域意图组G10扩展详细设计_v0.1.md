# L1-SIMPLIFY-METHOD-ROOT-R3-INTENT-GROUP
# L1 方法登记根领域意图组 G10 扩展详细设计 v0.1

更新时间：2026-08-07

## 1. S0 与最早缺口裁决

创建侧正式基线为 `2c7a233e0af24b7a6b9ebdd6f20f00da3833d014`，
`main == origin/main`、ahead/behind 为 `0/0`、Git index 为空。工作区剩余 dirty 只属于
P0-FIX、路线、旧 WORLD-TREE 和 `.codex-build`，与本叶文档和未来五路径零所有权重叠。
P16 正在重新独立验收，但其未发布验收 WIP 不是方法登记根机器依赖，本设计不读取或引用。

R2 已冻结方法登记根首次草案的完整 `3节点/0关系/1值/1槽/0退出/5读回` 形状，并把
领域意图组、标签、幂等和原子事务明确留给后继。当前 P15 核心唯一
`L1领域意图凭证完整` 仍硬编码只接受 G0—G8：

```cpp
return 凭证.请求意图组 <= 8
    && 凭证.请求意图格式版本 == L1请求意图格式版本
    && 凭证.请求意图操作标签 != 0
    && 完整(凭证.请求意图摘要);
```

现有组所有权已经闭合：G1世界、G2初始场景、G3特征定义、G4实例特征、G5实例写集透明
适配、G6实际存在、G7状态/状态动态、G8比较。正式 P78 又已唯一冻结 G9 为动态因果候选
组，并要求 G10+ 拒绝；P78 仍待激活，不代表 G9 已实现。

方法登记根是方法领域独立事实所有者，不能冒充 G5 实例特征透明适配、G7、G8 或 G9，也
不能降级为 G0 后再用操作标签替代领域意图。因此直接设计原子发布者仍会缺少合法请求意图
身份。按已冻结组顺序，方法领域唯一下一可用组是 **G10**；该编号由 G1—G9 正式占用机械
推出，不需要新增用户机器语义。

故 R2 后最早独立叶不是发布者，而是 P78 的公共凭证边界后继：在同一唯一验证器中新增具名
G10 并把公共上限推进到10。本叶只维护公共意图基础设施，不进入 P76—P81 因果业务，不发布
候选、不确认因果，也不修改 P78 设计或计划。

## 2. 正式依据、目标和完成边界

依据：4015、4030—4050，3300、5300，SELF-GOV-CLOSURE v0.2，P15 v0.29 当前代码合同，
R1/R2 v0.1，以及 P78 v0.1 对 G9 的正式唯一占用。

唯一目标：在 P78 真实结果提供 G9 常量和唯一上限后，原位扩展公共 L1 数据边界，使完整
G10 凭证合法、G0—G9 行为逐字兼容、G11+ 继续拒绝。摘要器已经按 U8 编码组号，本叶不
新增第二验证器、摘要器、账本、提交入口、锁、缓存或领域服务。

完成只证明公共凭证可区分方法登记根 G10 与既有 G0—G9；不证明 R2 已实现、方法根生产
请求意图摘要/标签/幂等域已冻结、原子发布或正式读取完成。

## 3. 唯一所有者、常量与根函数

唯一机器合同所有者保持：

```text
模块：海中鱼巣.核心.合同.L1事实基座 导出的公共数据头
文件：海中鱼巣/核心/L1事实基座.数据.h
```

以 P78 真实结果为前置，常量最终形状固定为：

```cpp
inline constexpr std::uint8_t L1动态因果候选领域意图组 = 9;
inline constexpr std::uint8_t L1方法登记根领域意图组 = 10;
inline constexpr std::uint8_t L1领域意图组上限 = L1方法登记根领域意图组;
```

只原位修改现有唯一根函数，不创建同义入口：

```cpp
inline bool L1领域意图凭证完整(
    const L1领域意图凭证& 凭证) noexcept {
    return 凭证.请求意图组 <= L1领域意图组上限
        && 凭证.请求意图格式版本 == L1请求意图格式版本
        && 凭证.请求意图操作标签 != 0
        && 完整(凭证.请求意图摘要);
}
```

依赖方向固定：

```text
P78 G9公共边界真实结果
-> R3 G10公共边界
-> 后继方法登记根原子发布者
-> R1方法登记根当前读取
-> 后继本能方法登记
```

P78 的因果 DTO、生产标签、幂等域、候选事实和业务调用均不是本叶输入；R3 只消费其公共
常量/验证器真实代码形状。R3 不让方法领域拥有核心验证器，也不让核心反向依赖方法 DTO。

## 4. G0—G11 机械边界

| 输入组 | 正式所有者/语义 | R3 结果 |
| --- | --- | --- |
| G0 | 核心规范化写集 fallback | 保持现状 |
| G1—G6 | 世界、场景、特征、实际存在及既有透明适配 | 逐字兼容 |
| G7 | 状态/状态动态 | 逐字兼容 |
| G8 | 比较 | 逐字兼容 |
| G9 | P78 动态因果候选 | 保持合法，不读取或形成因果材料 |
| G10 | 方法登记根领域 | 新增合法公共组号；不含生产业务合同 |
| G11—G255 | 未分配 | 一律拒绝 |

G10 的合法性仍同时要求格式版本精确、操作标签非零、意图摘要非零。公共验证器不判断生产
标签是否合法，也不解释方法 DTO。后继发布者必须另行冻结 G10 入口变体、生产标签、幂等域、
公开字段确定编码、provider 前探测和结果 DTO；不得把本叶自检夹具身份用于生产。

`形成L1写集摘要`、`形成L1执行证据材料摘要` 和幂等账已有 U8 组号编码，本叶逐字不改。
G9 与 G10 在其它字段相同的夹具中必须产生不同执行证据摘要，G0 规范化写集摘要保持相同，
证明领域意图与中性写集两账没有合并。

## 5. 自检根、夹具和单生产程序边界

在既有核心自检模块新增唯一专项根：

```cpp
L1事实基座自检报告 运行L1方法登记根领域意图组自检();
```

专项身份固定：

```text
专项：L1-SIMPLIFY-METHOD-ROOT-R3-S01
顺序：585
自检专用操作标签：0x1501C00FU
标题：L1方法登记根领域意图组G10
```

`0x1501C00F` 仅是隔离自检夹具，不是后继生产标签，不得写入方法领域公开 DTO 或计划。专项
使用隔离 L1 服务验证公共凭证、探测和摘要边界，不调用 R1、R2、P16、P76—P81、旧方法
服务、需求、任务、安全或生产装配。

验证只经同一个生产程序的既有自检薄入口运行；禁止第二 exe、独立自检目标、第二链接/装配、
测试专用 DTO、替身生产入口或调试状态出口。正式日志只供人读核对专项身份、顺序、计数和
拒绝分支，不是机器事实、业务状态、发布证明或恢复材料。

## 6. 物理白名单与实施步骤

未来代码白名单精确五路径：

```text
海中鱼巣/核心/L1事实基座.数据.h
海中鱼巣/核心/自检.L1事实基座.ixx
海中鱼巣/启动.应用程序.ixx
施工记录/20260807_L1-SIMPLIFY-METHOD-ROOT-R3-INTENT-GROUP_施工记录.md
验证记录/20260807_L1-SIMPLIFY-METHOD-ROOT-R3-INTENT-GROUP_验证记录.md
```

机械实施顺序：

1. 从激活时新 HEAD 重做 S0，核对 P78 真实代码结果、常量、验证器、五路径所有权和顺序585；
2. 在 P78 G9 常量之后新增 G10，原位把唯一上限改为 G10；
3. 保持唯一验证器其它三项门禁和全部摘要/幂等编码逐字不变；
4. 在既有核心自检模块新增专项根，使用固定夹具标签和隔离服务；
5. 在启动层新增薄包装并按顺序585登记，不新建模块或工程项；
6. 按激活 S0 的真实自检总数机械加1，形成两份专属记录并完成验证。

## 7. 验证矩阵

专项至少覆盖：

1. G9常量为9、G10常量/上限为10；
2. G0—G9完整凭证保持合法，G10完整凭证合法，G11和255拒绝；
3. G10错格式、零标签、零摘要拒绝，且零事实变化；
4. 仅组号不同的G9/G10执行证据摘要不同，G0摘要相同且不被覆盖；
5. 合法G10预探测未找到，探测摘要与凭证摘要不等时入口拒绝；
6. 隔离非空L1基线下G10最小夹具首次成功、同义重复不增代次、异义冲突快照不变；
7. G11和缺陷凭证不能通过探测或提交；
8. P78既有专项、P15核心及G1—G9相关专项不退化；
9. R1/R2/P16/因果/安全/需求/任务/旧方法服务及生产装配调用0；
10. 同一生产程序Debug/Release构建运行，日志仅人读；
11. 零第二exe/目标/链接/装配、测试DTO、替身入口和调试出口；
12. 五路径范围、顺序585、`0x1501C00F`、strict110/110和diff check通过。

本叶当前不覆盖程序崩溃、异常终止、断电、重启、恢复介质或跨进程幂等重放；不得宣称恢复
闭环。正常进程内幂等/异义只作为公共凭证兼容验证，不形成方法根生产事务。

## 8. 激活门禁与失败收口

计划登记保持待激活。必须等待 P78 或其正式替代真实代码结果进入 main、经交互接受并提供
唯一 G9 常量、上限常量和验证器；实际接口与本设计机械匹配；活动代码计划释放核心数据头、
核心自检、启动、构建、完整自检、固定输出和顺序585资源；再从新 HEAD 完整 S0。

P78 真实结果未进入 main 时不得直接从硬编码8跳到10、不得顺带实现 G9，也不得修改 P78
计划或因果合同。若 P78 改变常量名、验证器、组号连续性或摘要编码，具名退回计划支撑；不得
由执行者临场合并两计划。

## 9. 完成声明

完成后最多声明：核心公共凭证可合法区分 G10 方法登记根与既有 G0—G9，并继续拒绝 G11+；
同一生产程序专项验证正常进程内确定编码和同义/异义边界。

不得声明：R2已实现、方法登记根已发布/可读、生产G10摘要/标签/幂等域已冻结、方法首/规格/
动作入口已登记、方法可召回或执行、需求任务方法承接、服务候选、安全、因果、生产接线、
崩溃恢复或治理闭环完成。
