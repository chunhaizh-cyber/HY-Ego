# 服务操作函数矩阵第一批

日期：2026-07-07
状态：已生成 / 第一批矩阵 / 文档治理 / 未改代码 / 未迁移旧函数

## 1. 依据

```text
计划/已完成计划/20260707_服务操作函数矩阵与流程图迁移策略调整计划_v0.1.md
规范/000_项目规则总纲.md
规范/001_规则迁移清单.md
规范/领域服务授权写入规范.md
规范/详细设计/状态动态二次特征因果服务增强详细设计.md
规范/详细设计/特征服务封口增强详细设计.md
海中鱼巣/领域/*.h 当前代码事实
计划/待确认计划/20260707_FS02_动态聚合运动基元候选代码实施切片_v0.1.md
计划/待确认计划/20260707_FS02_因果模板查询候选代码实施切片_v0.1.md
计划/待确认计划/20260707_FS02_实例状态生命周期读取阻断代码实施切片_v0.1.md
计划/待确认计划/20260707_FS04_需求树后续结构代码实施切片_v0.1.md
计划/待确认计划/20260707_FS05_任务状态机筹办执行桥代码实施切片_v0.1.md
计划/待确认计划/20260707_基础信息服务分层完整增强后续代码实施切片_v0.1.md
```

本矩阵把旧函数事实降为证据材料，把服务操作函数作为代码实施前的第一层承载单位。

## 2. 总规则

```text
服务操作函数必须先入口拒绝，再写结构。
服务操作函数不得裸写旧主信息字段。
高级服务不得直接访问特征值服务。
世界服务只做聚合编排，不绕过原子服务写基础信息结构。
流程图节点必须映射到领域服务入口、只读材料或拒绝路径。
```

## 3. 第一批矩阵

| 服务 | 负责结构 | 权威写入结构 | 只读材料 | 可创建入口 | 可读取入口 | 可绑定关系入口 | 可状态迁移入口 | 可候选读取入口 | 可失效 / 删除阻断入口 | 禁止入口 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 特征服务 | 特征节点、实例特征槽位、特征值归属、I64 特征状态材料 | 特征节点、特征到特征值归属关系、宿主到槽位归属关系、槽位到特征类型模板关系 | 特征身份、特征语义类型、宿主槽位、宿主当前 I64 材料 | `创建特征类型`、`创建实例特征槽位`、`创建特征值` | `读取特征身份`、`读取特征语义类型`、`读取宿主特征槽位`、`读取宿主I64特征状态材料` | 宿主归属槽位、槽位模板绑定特征、特征归属特征值 | 第一轮只允许 I64 特征状态材料写入，不治理当前值唯一性 | 后续特征值 Vec、哈希、稳态和值域候选需独立矩阵 | 读取不存在槽位、特征值不属特征时拒绝；不新增删除写入口 | 高级服务直接访问特征值服务、Vec / 哈希 / 体素、旧特征主信息字段 |
| 状态服务 | 抽象状态、实例状态、状态材料、状态发生时间戳 | 状态节点、状态值、发生时间戳、场景运行期临时关系、状态引用主体和场景 | 状态值、发生时间戳、状态主体、状态场景、状态材料、场景状态集合 | `创建实例状态`、`创建抽象状态`、`提升实例状态` | `读取状态值`、`读取发生时间戳`、`读取状态主体`、`读取状态场景`、`读取状态材料` | 实例状态归入场景，实例状态引用主体和场景 | 后续只读阻断可判断状态能否失效；删除写入口暂缓 | 抽象状态候选、实例状态生命周期阻断候选 | 缺主体、场景、值或时间戳时读取拒绝；被动态或二次特征引用时阻断 | `删除实例状态` 写入口、级联清理、旧状态删除函数体 |
| 动态服务 | 实例动态、抽象动态、动态材料、来源动作准入 | 动态节点、发生时间戳、被改变目标、前值、后值、来源动作关系 | 动态材料、来源动作、动态证据完整性、状态变化材料 | `记录实例动态`、`记录状态变化动态`、`创建抽象动态`、`提升实例动态` | `读取动态材料`、`读取动态来源动作`、`动态证据材料是否完整` | 动态归入场景，动态引用主体，因果来源关系记录目标 / 前值 / 后值 / 来源动作 | 状态变化动态要求前后状态同主体、同场景 | 动态聚合候选、运动基元候选、抽象动态候选 | 缺时间戳、跨场景、来源动作非法或材料不完整时拒绝；后续实例动态引用阻断只读 | 线程、函数名、日志标签、控制台文本作为动作来源；真实运动算法、外设 / 体素 |
| 二次特征服务 | 二次特征节点、组成项关系、状态迁移二次特征 | 二次特征节点、组成项引用关系 | 组成项集合、状态迁移组成材料 | `创建二次特征`、`创建组合二次特征`、`创建状态迁移二次特征` | `读取组成项` | 二次特征引用组成项 | 第一轮只建立组成关系，不写差值材料 | 后续二次特征候选需由动态 / 状态候选提供材料 | 组成项无效、来源动态与前后状态不一致时拒绝 | 写差值材料、候选裁决事实、直接访问特征值服务 |
| 因果服务 | 因果引用节点、轻量因果准入材料 | 因果引用节点 | 来源动态材料、轻量准入结果 | `记录因果引用` | `读取因果准入动态材料`、`因果轻量准入是否有效` | 第一轮不持久化来源动态关系 | 不做状态迁移 | 因果模板候选、因果查询候选、缺失证据候选 | 来源动态无效、动态材料不完整时拒绝 | 稳定因果结论、任务化自动创建、因果模板长期节点 |
| 需求服务 | 需求节点、目标状态、主体、目标宿主、场景、结算状态 | 需求节点、需求引用材料、目标状态模板关系、结算状态关系 | 需求身份、目标状态、承接材料、最近结算记录、结算来源材料 | `创建需求`、`创建目标状态需求`、`记录需求结算` | `需求是否有效`、`读取需求目标状态`、`读取需求承接材料`、`读取最近结算记录` | 需求引用主体、目标宿主、场景、目标特征、当前特征值，模板绑定目标状态 | 结算状态由状态服务创建，需求服务记录结算材料 | 需求树父子、阻塞、权重、方向签名、枚举目标准入、方法候选准入 | 目标状态缺失或不唯一、主体 / 宿主 / 场景缺失时拒绝；结构化更新第一轮只请求壳 | 方向 bool、目标语义文本、旧需求主信息字段、完整需求树算法 |
| 任务服务 | 任务节点、承接壳、生命周期状态、任务方法关系、实际结果状态 | 任务节点、任务来源需求、虚拟存在、运行场景、目标状态、生命周期状态 | 任务承接材料、来源需求、虚拟存在、运行场景、目标状态、创建 / 完成状态、选择方法 | `创建任务`、`按需求创建任务`、`记录任务状态`、`记录任务生命周期状态` | `读取任务承接材料`、`读取任务来源需求`、`读取任务目标状态`、`读取任务选择方法` | 需求归属任务，任务引用需求 / 虚拟存在 / 场景 / 实际结果状态 / 选择方法，模板绑定目标状态 | 后续生命周期状态迁移请求入口，只允许经任务服务拒绝矩阵 | 筹办回执、执行桥请求材料、运行统计和错误码非权威材料 | 承接壳不完整、缺目标状态、缺任务方法关系时拒绝 | 线程派发、真实执行桥、外设等待、旧任务主信息字段 |
| 方法服务 | 方法节点、方法首、条件、结果、动作入口角色、动作规格 | 方法节点、方法角色状态、条件 / 结果场景、动作入口角色、稳定动作键索引 | 方法角色、方法虚拟存在、条件 / 结果节点、动作入口、动作入口状态、输入 / 输出规格 | `登记方法`、`创建方法首节点`、`创建方法条件节点`、`创建方法结果节点`、`登记动作入口` | `读取方法角色状态`、`读取方法条件节点集合`、`读取方法结果节点集合`、`读取方法动作入口` | 方法首模板绑定条件 / 结果，方法首引用动作入口，动作入口引用规格场景 | `执行特征状态写入方法` 必须有任务方法关系和主动动作入口 | 方法候选召回、条件结果对、来源任务和前后方法关系后续候选 | 方法首无虚拟存在、动作入口状态无效、稳定动作键冲突时拒绝 | 新建独立动作入口服务、真实动作分派、函数指针注册表、线程 |
| 世界服务 | 基础信息聚合入口、存在、场景、场景存在 | 兼容基础信息通用节点、存在、场景、场景接纳关系 | 场景有效性、存在接纳结果 | `创建基础信息`、`创建存在`、`创建场景`、`创建现实场景`、`创建场景存在` | 第一轮主要通过原子服务读取 | `接纳存在` | 不直接承载状态迁移 | 兼容壳用途复核、抽象状态 / 动态候选由原子服务提供 | 已明确类型应优先走原子服务；兼容壳收窄不等于移除 | 绕过原子服务直接写特征、状态、动态、二次特征或因果 |
| 语素服务 | 语素入口、语素对应信息、多路概念追溯 | 语素节点、语素对应信息关系、语素概念追溯关系、可选主键索引 | 入口绑定目标组、概念追溯组、准入状态 | `创建语素入口`、`创建概念入口` | `读取入口绑定目标组`、`读取入口概念追溯组` | 追加对应信息、追加概念追溯 | 不承载状态迁移 | 后续应用流程图入口可把词面请求转成服务调用材料 | 外部文本不准入、绑定临时实例状态 / 动态时拒绝 | 文本直接成为世界事实、绑定特征值、绑定临时实例状态 / 动态 |

## 4. 跨服务依赖矩阵

| 调用方 | 允许直接依赖 | 禁止直接依赖 |
| --- | --- | --- |
| 特征服务 | 特征值服务、二次特征服务、节点 / 主信息 / 关系仓库 | 需求、任务、方法服务 |
| 状态服务 | 节点 / 主信息 / 关系仓库 | 特征值服务、控制面板、SQL |
| 动态服务 | 状态服务材料、节点 / 主信息 / 关系仓库 | 线程、外设、控制台文本 |
| 二次特征服务 | 状态服务、动态服务 | 特征值服务、任务 / 方法执行 |
| 因果服务 | 动态服务材料 | 任务、方法、稳定因果结论写入 |
| 需求服务 | 特征服务、状态服务、动态服务、因果服务 | 特征值服务、控制面板、SQL |
| 任务服务 | 需求服务、存在服务、场景服务、状态服务、方法服务材料 | 线程、外设、特征值服务 |
| 方法服务 | 存在服务、场景服务、状态服务、动态服务、特征服务、索引仓库 | 特征值服务、SQL、控制面板 |
| 世界服务 | 存在、场景、特征、状态、动态、二次特征、因果原子服务 | 绕过原子服务写基础信息结构 |
| 语素服务 | 节点 / 主信息 / 关系 / 索引仓库的语素入口封装 | 直接裁决业务事实 |

## 5. 第一批入口拒绝共性

```text
无效句柄拒绝。
错误节点类型拒绝。
缺少必需前置关系拒绝。
唯一材料不唯一时拒绝。
来源服务越权时拒绝。
外部文本、日志、显示、SQL、线程或外设材料不得直接成为机器事实。
拒绝后节点、主信息、关系和索引数量不得变化。
```

## 6. 第一批批次归并建议

现有 6 份小切片不再逐个确认，建议归并为一个批次级候选：

```text
计划/待确认计划/20260707_服务操作函数矩阵第一批代码实施包_v0.1.md
```

该批次只在用户确认后才可进入 C++ 实施。

## 7. 完成声明边界

本文件只能证明：

```text
第一批服务操作函数矩阵已生成。
现有服务和待确认切片已经按服务操作函数维度归并。
```

不能证明：

```text
服务操作函数已实现。
应用逻辑已迁移。
旧函数体已迁移。
旧项目能力迁移完成。
```
