# L1-SIMPLIFY-P14-DYNAMIC-SPEC 关系20状态迁移动能登记、规格与读取详细设计 v0.2

计划身份：`L1-SIMPLIFY-P14-DYNAMIC-SPEC`

版本：`v0.2`

修订基线：`12bc9678d49971356da19b8e897926054c909bce`

具名漂移：`DESIGN-DRIFT-P14-UPSTREAM-VERSION-AND-SEQUENCE-STALE-AFTER-P13-V0.2`

## 1. 修订结论与边界

本版本完整替代并禁止施工 v0.1，只修订 P13/P12 上游版本、实际字段和顺序依赖；关系20状态迁移动能登记、纯值规格形成和独立读取机器目标不变。P13 正式结果为 v0.2，结果提交 `b79544472e268bac87c24be8ec12494d7bf47abf`，已由交互侧接受；P12 正式依赖为 v0.4，结果链为 `df0e0b1b7aeabd700c3289a266db7ebe24207fe8` 与 `de0009c624391ff1c76fd0f55fbfff78d9fd7f61`。P12/P13 专项均由既有顺序360承载；P14 自有动态专项仍新增并登记顺序390，不再引用不存在的 P13 顺序380。

不扩展第二状态之外的语义，不修改 P13/P12 代码或设计、不改 L1事实基座、P15、P0-FIX、路线、旧 WORLD-TREE、SQL、恢复或其它 WIP。

## 2. P13 v0.2 实际合同

P14 只消费 P13 组合器的值式结果：

```cpp
实际存在I64后继状态规格结果 形成实际存在I64后继状态规格(
    L1事实基座服务&, L1实例特征服务&, L1状态服务&, 特征比较服务&,
    const 实际存在I64后继状态规格请求& 请求);
```

P13 请求含合同/规则/规格版本、业务请求身份、整理幂等身份、新状态发生时间和当前事实请求；调用方不得提供动态身份、关系角色、算法、注册、截止、许可、事务或稳定编码。P13 结果状态为：

```text
已形成 / 无前状态 / 入口拒绝 / 未找到 / 冲突 /
不可比较 / 版本漂移 / 许可拒绝 / 资源失败 / 内部不一致
```

`已形成` 时 `状态规格` 必须包含 `事实截止代次`、`动态准入`、请求意图、部分 `L1写集`、确定性摘要；`迁移证据` 必须包含 P12 选择证据、P11 比较结果和完整输入见证。状态规格的后状态节点局部键固定为 `0x53000001`，P14 关系20角色5单向引用该键。`无前状态`、等价成功的 `禁止生成迁移动能` 和所有非成功均形成零动态规格；只有非等价且 `允许形成迁移动能规格`、差异非零且迁移证据完整时才准入 P14。

P13 的 P12 来源已在 P13 内完成：P12 v0.4 `已选择` 返回完整前状态和选择证据，P13 不要求 P14 重新调用 P12/P9/P11。P14 不重解释 P13 的前状态选择或比较，只校验字段一致性和共同截止。

## 3. 动态领域所有权、登记与规格

动态领域唯一拥有关系20、动态资格/种类/聚合规则、迁移比较与差异证据的登记、规格解释和读取。模块与白名单沿用 v0.1：

```text
海中鱼巣/领域/L1动态.数据.h
海中鱼巣/领域/服务.L1动态.ixx
海中鱼巣/领域/自检.L1动态.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
海中鱼巣/启动.应用程序.ixx
施工记录/20260805_L1-SIMPLIFY-P14-DYNAMIC-SPEC_施工记录.md
验证记录/20260805_L1-SIMPLIFY-P14-DYNAMIC-SPEC_验证记录.md
```

登记写集仍零关系、零值、零槽、零退出；成功登记后同代次读回十个非零互异映射。规格形成零 L1 提交、零最终幂等、零稳定编码分配；未来最终发布者只拼接不可变 P13/P14 规格并填唯一最终幂等键。

## 4. 关系20规格形成和读取

1. 校验 P13 结果、动态登记、业务身份和共同截止。
2. P13 非成功或 `动态准入::禁止生成迁移动能` 返回具名禁止/对应非成功，零规格；不得从 P12/P9/P11 重新发现材料。
3. 校验 P13 已形成规格的主体、场景、实例槽、来源存在、前状态、后状态键 `0x53000001`、P12选择证据、P11注册/算法/三态/差异、P10/P8输入和 `0 < T0 < T1`。
4. 仅非等价增加/减少成功形成一动态节点、八值、关系20角色1—5/9六关系、八槽；命名空间固定 `0x44`，角色6—8和占位事实严格为空，角色5目标为 `0x53000001`，状态规格不反向引用动态键。
5. `比较输入I64组`、迁移差异和持久比较证据逐字段来自 P13 迁移证据，不重新调用 P11，不用 P2 上界、请求回显、缓存、日志或时间推断。
6. 读取按十五个已发布稳定编码首快照 G0、当前/历史入口、末快照 G1 互证；端点退出不删除动态，G0/G1 漂移完整重试一次，仍漂移返回 `版本漂移`。读取不调用 P11、不依赖 SQL/缓存/日志。

## 5. 验证与顺序

P14 自有顺序390覆盖登记建立/重复/冲突/读回、P13增加/减少成功、P13无前态、等价/禁止、全部非成功、坏成功、`0x53000001`单向引用、关系20角色1—5/9、角色6—8为空、十五键读回和首末代次重试。P12/P13专项保持顺序360事实，不得在 P14 文档中声称顺序370或380。

只可声明动态领域登记、关系20纯值规格形成和通用 L1 夹具独立读取候选闭合；不声明 P13/P12 上游重新实现、第二状态/动态生产发布、最终跨域幂等/原子事务/撤销、动作动态、因果、结算、生产装配、STEP-3F 或治理闭环完成。
