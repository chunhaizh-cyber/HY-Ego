# INSTINCT-STAGE2-CURRENT-VALUE-ATOMIC-RECORD 详细设计

日期：2026-08-28
版本：v0.5
状态：正式实施设计；以计划索引登记的精确 blob 为施工依据

## 1. 目标与完成边界

本设计闭合本能路线阶段二的最早代码缺口：让一个已经有正式当前值的特征实例，通过唯一公开组合入口完成真实值变化，并在同一 L1 三分区事务中形成新当前值、中性状态当前选择、状态迁移动能及完整上下文关系。

本叶完成只证明：

```text
普通应用取得唯一组合服务
-> 真实变化 / 无变化 / legacy 基线兼容
-> 特征、状态、动态三个 owner 同 G1 提交
-> 公开结果
-> 独立正式读回
-> 本能根重启优先消费中性记录选择
```

不实现安全值/服务值被动维护规则、周期游标、自检、需求业务、任务、方法执行和结算；这些只能在本叶结果发布后形成后继计划。唯一例外是第 2 节执行期证实的现有需求请求守卫机械纠错：它只恢复既有正式合同已经允许的 `所属存在 == 目标宿主`，不新增需求语义、入口或持久事实。

## 2. 正式依据与当前事实

正式依据：0050 v2.0、1130 v0.6、1160 v0.8、1170 v0.4、4210 v0.4、4220、4230 v0.18。

当前代码事实：

- 阶段一真实运行首次建立安全根需求时，`L2新增需求并建立列表项请求有效` 因“所属存在、目标宿主、目标特征、目标合同四项必须互异”而拒绝 `所属存在=目标宿主=唯一自我`；HEAD 中 3100 已明确允许二者相等，阶段一 provider 也按该正式形状提交，因此这是既有请求守卫的代码缺陷，不是待裁决机器语义。
- 修复固定为删除四字段跨角色互异检查，继续逐字段要求四个强类型身份各自有效；不放宽同一强类型内部的有效性、父关系、幂等、目标引用或服务后置条件。

- `L1所有者范围写端口::提交三分区原子事务_v2` 已发布，恰好支持三个互异 owner、参与者 1/2/3、后向跨参与者节点引用、单一 G1、精确重复、幂等冲突、漂移和已可能发布。
- `L2状态动态原子发布服务` 是现有唯一组合服务，但两个旧迁移入口均固定零写拒绝。
- 特征、状态、动态服务已有正式当前/历史读取、中性状态/动态与中性当前选择；现有组合 helper 仍是旧双参与者语义，不能复活。
- 第一阶段双根通过 legacy `新增特征首值与初始状态_v1` 建立首值和旧首态选择。本叶必须允许第一次真实变化把旧首态转换为中性基线，同时不改写旧历史。
- 本能根初始化当前每次要求 legacy 当前状态值等于特征当前值；真实变化后该条件会过期，因此恢复逻辑必须优先读取中性当前选择。
- 执行期 V16 已证明 L1 持久恢复后的语言 owner 首次账、首次规范化写集、首次结果和四项编码映射完整；失败来自普通应用构造期用恢复后的当前 G 重建同一固定登记幂等请求。L1 按完整规范化请求判同，因此该请求不是精确重复而是同键异义。语言只是构造顺序中最早暴露者；存在、特征、状态、需求、任务、动态、因果、场景、概念、概念名称和方法服务的构造期固定登记必须按同一恢复合同审计并闭合，不能逐个等待重启失败后再临场扩义。

## 3. 复用与新建裁决

| 能力 | 裁决 |
| --- | --- |
| L1 三分区原子事务 v2 | 直接复用，不修改 L1 |
| 特征当前值换代的值类型选择和正式读回 | 从 `L2特征结构服务` 抽取 module-private 三分区参与者 helper，公开旧 CRUD 不复义 |
| 中性状态、引用和读取 | 状态本体继续复用现有物理编码；普通历史引用继续兼容既有节点载荷式编码；原子当前选择以状态 owner 持有的具名关系作为物理身份，使目标端点可引用同事务新状态。公开 `L2中性状态引用事实_v1` 不变，读取统一两种历史物理形状并拒绝多义 |
| 中性动态和读取 | 复用现有有序状态本体；增加上下文关系登记、参与者 helper 和完整读回 helper |
| 组合服务 | 扩展现有 `L2状态动态原子发布服务`，不新建第二 provider |
| legacy 首态 | 只读兼容；第一次变化新建中性基线，不修改或退出 legacy 事实 |
| 普通应用 | 移动现有组合服务构造点到场景服务可用之后，仍只持有一份服务和一组 getter |
| 构造期固定登记恢复 | 复用 L1 写端口现有 `读取首次写入材料`；有首次账时重放首次规范化写集，无首次账时才按真实当前 G 建立，禁止修改 L1 幂等判同 |

## 4. 公开数据合同

数据继续物理落在 `领域/L2状态动态原子发布.数据.h`；状态/动态上下文关系的强类型事实分别落在各自数据头。专属合同版本固定为 1。

```cpp
inline constexpr std::uint32_t L2特征当前值变化记录合同版本_v1 = 1;
inline constexpr 稳定编码 L2特征当前值记录状态角色_v1{
    0x4E53'4355'5252'454EULL};

struct L2特征当前值变化记录请求_v1 final {
    std::uint32_t 合同版本;
    L2结构请求头 请求头;
    L2结构幂等身份 组合幂等身份;
    L2结构幂等身份 特征参与者幂等身份;
    L2结构幂等身份 状态参与者幂等身份;
    L2结构幂等身份 动态参与者幂等身份;
    L2特征实例身份 特征实例;
    L2特征值事实 预期旧当前值;
    L2原始值材料 新值材料;
    std::int64_t 变化UTC纳秒;
    L2存在身份 主体存在;
    L2场景身份 共同场景;
    L2存在身份 来源存在;
    std::optional<L2方法身份> 来源方法;
};
```

四个幂等身份有效且两两不同；G0、全部身份和 UTC 纳秒非零；旧当前值必须是 G0 可见且未退出的完整值事实，属于请求特征实例；新值材料必须满足当前特征定义的值类型和值域。主体必须等于特征实例宿主。共同场景、主体、来源存在及可选来源方法必须在 G0 可由正式结构引用闭合。

状态上下文事实固定为：状态身份、主体、共同场景、特征实例、来源存在、四条关系稳定编码和共同生命周期。动态上下文事实固定为：动态身份、类别、主体、共同场景、被改变特征实例、前/后状态、可选来源方法、可选同源状态迁移动能、来源存在、对应关系编码组和共同生命周期。

动态类别固定：`状态迁移=1、动作致变=2`。状态迁移没有来源方法、来源低层动态和同源迁移动能；动作致变必须同时有来源方法和同源状态迁移动能。来源方法为空时不建立动作致变动态。

写结果物理顺序：合同版本、状态、四幂等身份提交见证、optional 当前值、optional 前状态、optional 后状态、optional 当前选择、optional 已退出旧选择、optional 状态迁移动能、optional 动作致变动态、状态上下文组、动态上下文组、legacy 基线兼容标志、正式截止。

写状态数值：

```text
1 已提交
2 精确重复
3 无变化
4 入口拒绝
5 当前性漂移
6 幂等冲突
7 材料不足
8 引用冲突
9 资源失败
10 内部错误
11 已可能发布
```

`成功()` 对已提交/精确重复要求完整变化载荷和 G1；对无变化只要求旧当前值和 G0，且其它变化载荷全空；其它状态必须空业务载荷。已可能发布只允许返回四幂等身份和 optional G1 见证。

独立读取请求字段顺序：合同版本、共同请求头(Gread)、特征实例、新当前值稳定编码、新当前选择身份、前状态、后状态、状态迁移动能、optional 动作致变动态、首次 G1。读取结果返回与写成功形状相同的业务载荷，但状态固定为 `已读取、入口拒绝、当前性漂移、材料不足、引用冲突、资源失败、内部错误`，非成功载荷空且截止 0。

## 5. 固定物理登记

新增登记不扩展旧不可变登记请求，避免既有数据同键异义：

```text
状态上下文关系类型登记幂等身份 = 0x4C32'5354'4354'5801
状态上下文关系类型本地键       = 0x0204'0501

动态上下文关系类型登记幂等身份 = 0x4C32'4459'4354'5801
动态上下文关系类型本地键       = 0x0205'0401
```

两项由各自服务构造期建立或精确恢复，进入服务构造完整性；不得复用或改写旧状态/动态结构登记。关系 19 和 20 的写入仍落在状态、动态 owner，不新增 owner。

## 6. 三分区写集

### 6.1 特征参与者 1

module-private helper 固定形成：

- 一条新值事实，材料为请求新值；
- 特征实例当前属性槽切换到新值；
- 退出请求完整指定的旧值事实；
- 值来源：有来源方法取方法节点，否则取来源存在节点。

helper 必须先正式读取值类型登记、特征实例、当前槽和值事实；请求旧值任一字段不等于正式读回即返回当前性漂移或引用冲突。

### 6.2 状态参与者 2

正常路径建立：新中性状态、关系 19 四角色、新中性当前选择，并退出旧中性当前选择。新状态的特征类型取特征实例定义，值取请求新值，时间为实例 UTC。

legacy 兼容路径在同一参与者内额外建立：旧值中性基线状态及其四条关系 19。基线时间同本次变化时间，明确表示“迁移时刻读回的旧值基线”，不伪装成第一阶段历史发生时间。新选择直接指向新状态；旧 legacy 状态与选择保持历史只读。

中性选择固定：引用方=`特征实例.值`，引用角色=`L2特征当前值记录状态角色_v1`。后继变化只允许恰一当前选择。

原子路径不得把本事务新状态的未知稳定编码塞入 opaque 值载荷。其新选择物理形状固定为一条由状态 owner 持有的关系事实：源为特征实例、目标为参与者 2 本地新状态、关系类型为现有状态当前选择关系类型、`角色或顺序` 为 `L2特征当前值记录状态角色_v1` 的稳定值；该关系稳定编码就是 `L2中性状态引用身份_v1`。既有节点载荷式中性选择只读兼容，正常迁移必须读回其完整物理闭包并在同一写集中退出；关系式选择只退出关系本身。当前选择读取和按身份历史读取必须统一投影两种形状为同一公开 DTO，合并后 0/1/>1 分别映射未找到、唯一成功、多义阻断。不得预测稳定编码、拆分提交或增加选择 owner。

### 6.3 动态参与者 3

始终建立一条状态迁移动能：有序状态组 `[前状态, 后状态]`，关系 20 角色 1/2/3/4/5/9 完整。

请求有来源方法时再建立动作致变动态：同样引用 `[前状态, 后状态]`，关系 20 另含角色 6 来源方法和角色 8 同源状态迁移动能。参与者 3 可以后向引用参与者 2 的状态节点及本参与者先建迁移动能；写集本地键严格固定并由 L1 规范排序。

## 7. 服务算法与错误映射

```text
验证请求
-> 读取 L1 当前 G0
-> 正式读取特征实例、旧当前值、主体、共同场景、来源存在
-> 新旧材料相同：返回无变化，零写
-> 读取中性当前选择
   -> 1 条完整：正常路径
   -> 0 条：读取 legacy 首态；完整且匹配才走兼容路径
   -> 多义/坏闭包：内部错误
-> 三个服务分别形成参与者写集
-> L1 三分区事务一次提交
-> 已提交/精确重复取得首次映射与 G1
-> 以当前 Gread 做读前守卫，以 G1 逐项历史读回
-> 再读当前 Gread 守卫
-> 返回完整结果
```

L1 状态映射：入口拒绝→入口拒绝；事实代次漂移→当前性漂移；幂等冲突→幂等冲突；引用冲突→引用冲突；资源失败→资源失败；内部不一致→内部错误；已可能发布→已可能发布。提交映射或独立读回不完整时，如果 L1 已确认交换则已可能发布，否则内部错误。

精确重复优先于 G0；原请求重放以首次映射/G1读回。新键+旧 G0 为当前性漂移。禁止用新幂等身份重试已可能发布。

## 8. 本能根恢复适配

`提供者.本能根运行初始化` 的根值恢复顺序改为：

1. 读取特征当前值；
2. 按固定引用方/角色读取中性当前选择；
3. 若存在，读取目标中性状态及状态上下文，逐字段验证类型、值、主体和特征实例；
4. 若中性选择不存在且当前值仍等于第一阶段初值，才读取 legacy 首态兼容；
5. 当前值已变化却没有中性记录，或中性记录与值不一致，返回材料不足/内部不一致并停止启动。

本适配不创建状态或动态，不把 legacy 选择重新解释为当前值。

## 9. 装配与依赖

普通应用继续持有唯一 `L2状态动态原子发布服务` 和现有 getter。构造函数增加同实例的存在、场景和特征服务引用以及三分区凭证；构造点移到场景服务已建立之后。三分区凭证验证三个端口有效、绑定同一 L1、owner 两两不同，提交时再固定 participant owner。

不修改 L1，不新建工程生产模块。新增专项测试模块时才修改工程登记。

### 9.1 普通应用构造期固定登记恢复

普通应用构造链中，所有使用固定 owner、固定登记幂等身份和固定结构键的初始化路径统一执行：

```text
按固定登记幂等身份读取首次写入材料
-> 成功：要求首次规范化写集和首次写入结果同时完整
   -> 以首次规范化写集自身的 G0 重建当前版本的固定结构期望
   -> 逐字段相等后原样重放首次规范化写集
   -> 要求精确重复结果与首次结果的 owner、幂等身份、G1、编码映射一致
-> 未找到：读取真实当前 G0
   -> 形成当前版本固定登记写集并首次提交
   -> 提交后再次读取首次材料，要求与本次请求和结果逐字段相等
-> 许可 / 资源 / 内部失败或首次账缺项：构造失败，零补造
-> 读取恢复后的当前观察代次 Gread，要求 Gread >= 首次 G1
-> 在 Gread 独立读回全部当前登记事实，并逐项要求创建代次仍为首次 G1
```

固定结构期望校验必须覆盖合同版本、owner、幂等身份、G0、节点 / 关系 / 属性形状、本地键、类型和值表示；不能只检查编码映射数量。旧首次材料与当前固定结构异义时 fail-closed，后继结构演进必须使用新登记身份，不能覆盖旧账。

`G1` 与 `Gread` 职责不可混用：`G1` 来自首次写入结果，只证明固定登记事实的创建代次和编码映射；`Gread` 是本次服务构造恢复时的当前观察代次，用于当前一致投影的期望代次和读前 / 读后守卫。恢复后已经存在其它合法事实时通常 `Gread > G1`，不得要求当前读取结果的观察代次等于 G1，也不得因而重建登记。当前读回的每个登记事实仍必须满足 `创建事实代次 == G1`、未退出、owner 和结构完整。

本修复只适用于普通应用构造期的固定登记，不改变业务新增、替换、退出、迁移等公开写入口，也不把“读取首次材料”变成第二权威。L1 首次账仍是重放请求与首次结果的唯一权威，L2 独立读回仍证明登记事实当前存在且结构完整。

## 10. 精确生产白名单

```text
海中鱼巣/领域/L2状态动态原子发布.数据.h
海中鱼巣/领域/服务.L2状态动态原子发布.ixx
海中鱼巣/领域/L2需求结构.数据.h（仅修正新增需求请求守卫，不修改 DTO 布局或服务）
海中鱼巣/领域/L2特征结构.数据.h（只在需要公开辅助投影时）
海中鱼巣/领域/服务.L2特征结构.ixx
海中鱼巣/领域/L2状态结构.数据.h
海中鱼巣/领域/服务.L2状态结构.ixx
海中鱼巣/领域/L2动态结构.数据.h
海中鱼巣/领域/服务.L2动态结构.ixx
海中鱼巣/领域/服务.L2语言结构.ixx（仅构造期固定登记恢复）
海中鱼巣/领域/服务.L2存在结构.ixx（仅构造期固定登记恢复）
海中鱼巣/领域/服务.L2需求结构.ixx（仅构造期固定登记恢复）
海中鱼巣/领域/服务.L2任务结构.ixx（仅构造期固定登记恢复）
海中鱼巣/领域/服务.L2因果结构.ixx（仅构造期固定登记恢复）
海中鱼巣/领域/服务.L2场景结构.ixx（仅构造期固定登记恢复）
海中鱼巣/领域/服务.L2概念结构.ixx（仅构造期固定登记恢复）
海中鱼巣/领域/服务.L2概念名称结构.ixx（仅构造期固定登记恢复）
海中鱼巣/领域/服务.L2方法结构.ixx（仅构造期固定登记恢复）
海中鱼巣/业务/提供者.本能根运行初始化.ixx
海中鱼巣/装配.普通应用.ixx
```

专项测试与工程登记：

```text
海中鱼巣/端到端测试.本能路线阶段二.ixx（新增）
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
```

明确禁止修改全部 L1 源码、安全/服务维护、需求 / 任务 / 方法业务写入语义及其它生产消费者、SELF-GOVERNANCE L4 WIP 和现有自我治理端到端测试。`L2需求结构.数据.h` 只允许上述单一守卫纠错；本节新增的九个服务文件只允许修复构造期固定登记恢复。

## 11. 验证矩阵

至少覆盖：

```text
V01 普通应用唯一构造和 getter
V01A 同一正式存在可同时作为需求所属存在与目标宿主；请求守卫接受，首次安全/服务根装配不再入口拒绝
V02 legacy 首态第一次真实变化，形成基线+新状态+迁移动能
V03 正常第二次变化，只形成一个新状态并退出旧中性选择
V04 无变化零值/零状态/零动态/零代次
V05 有来源方法时形成迁移动能+动作致变动态+同源关系
V06 无来源方法时没有动作致变动态
V07 精确重复返回首次 G1 且零新代次
V08 同组合键异请求幂等冲突
V09 新键旧 G0 当前性漂移
V10 旧值事实任一字段错配零写
V11 中性选择多义、坏上下文、前状态值错配阻断
V12 三 owner 任一引用缺失/退出阻断
V13 提交后首次读回失败返回已可能发布，原请求重放收束
V14 写结果与独立历史读回逐字段相等
V15 三状态连续迁移，当前值只取最后值，全部状态/动态历史可读
V16 持久恢复后第二次普通应用完整构造成功；每项构造期固定登记均复用首次 G0 / G1 和编码映射，以恢复时 Gread 读取并验证创建代次 G1，本能根恢复消费中性选择且不要求 legacy 旧值等于当前值
V16A 首次账未找到时按当前 G0 首次建立；许可 / 资源 / 内部失败、首次账缺项或同键异固定结构均阻断构造且零补造
V17 动态后状态不能替代特征当前值：制造历史后继后仍按当前槽读回
V18 v143 Debug/Release 根工程 Rebuild
V19 ABI consumer 只 import 公开模块可编译
```

其中 V03、V07、V14—V16 必须至少覆盖一次“旧节点载荷式选择作为前态”和一次“新关系式选择作为前态”，证明兼容读取、退出闭包和统一投影没有形成两个当前权威。

没有合法生产方法动作 fixture 的来源方法分支允许在提交前记 `NOT_RUN`，但源码静态矩阵、公开 ABI consumer 和无来源被动变化必须通过；若专项测试可建立正式方法节点则不得降级。

## 12. 完成声明

只有代码提交、普通应用接线、两配置构建、专项矩阵、独立读回和重启恢复全部通过后，才可声明“本能路线阶段二基础能力已完成”。这不等于阶段三被动维护或整个本能路线完成。

## 13. 修订记录

| 日期 | 版本 | 修订内容 |
| --- | --- | --- |
| 2026-08-29 | v0.5 | V16 进一步证实固定登记恢复不能以首次 G1 充当当前观察代次；冻结 G1 只证明创建，Gread 负责恢复时当前一致投影，读回事实仍逐项绑定创建 G1。 |
| 2026-08-29 | v0.4 | V16 证实普通应用构造期固定登记在持久恢复后用当前 G 重建同键请求；统一冻结“先读首次材料、原样重放、独立读回”的恢复合同，扩展具名构造登记白名单并禁止修改 L1 幂等语义。 |
| 2026-08-29 | v0.3 | 执行期运行发现阶段一根需求被旧“四字段互异”请求守卫拒绝；依据 3100 既有正式合同允许所属存在等于目标宿主，授权只删除该跨角色互异检查并增加 V01A，不改变需求 DTO、服务或业务语义。 |
| 2026-08-28 | v0.2 | 修正 L1 三分区不能向 opaque 载荷回填本地新状态编码的物理不可达：原子当前选择改用状态 owner 的具名关系身份，公开中性选择 DTO 不变；既有节点载荷式选择只读兼容并由统一读取拒绝多义。 |
| 2026-08-28 | v0.1 | 冻结当前值、状态、动态三 owner 原子变化、legacy 基线兼容、独立读回和本能根恢复。 |
