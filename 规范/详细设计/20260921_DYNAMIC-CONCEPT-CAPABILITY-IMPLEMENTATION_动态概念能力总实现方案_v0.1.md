# 动态概念能力总实现方案

身份：`DYNAMIC-CONCEPT-CAPABILITY-IMPLEMENTATION`

版本：v0.1

日期：2026-09-21

状态：当前总方案；只授权形成最近代码叶计划，不直接授权生产代码施工

当前事实基线：`1f72dd489ad438f99877b7d9e9d086f88a18a54a`

## 1. 目标与路线裁决

本方案以7190—7196规定的动态概念闭环为主线，覆盖实例动态来源、二次比较、RC/FC/EC求值、方向结构、递归匹配、规范化、同义查重、分类、发布恢复和实际消费者。它不把“二次特征系统整体完善”设为先决项目，而是从动态概念各阶段反推最小充分依赖，再逐叶闭合。

固定路线为：

```text
动态概念完整业务链
    ↓ 反推
当前阶段必需的动态实例、二次特征、RC和概念owner能力
    ↓
为最早缺口冻结一份可机械施工计划
    ↓
代码结果与证据发布
    ↓
重新S0并形成紧邻后继
```

“逐步完善二次特征支持”只表示代码和验证分期，不表示边写边决定机器语义。每个代码叶开工前仍须冻结DTO、函数、物理位置、所有权、来源、失败、恢复和验证合同。禁止在动态概念实现中临时增加任意I64入口、持久二次准确实例、第二概念owner或第二事实仓。

## 2. 正式依据和不变量

直接依据：4213、4242、7190、7191、7192、7193、7194、7195、7196；其直接依赖中的4120、4151、4203、4204、4212、4240、7140、7170、7180按各阶段读取。

全程保持：

1. 动态实例与动态概念分账；`动态类数据服务`不冒充动态概念服务。
2. 二次准确结果、方向项、匹配绑定和规范化临时序列只在调用期存在，不取得世界身份或持久事实权力。
3. RC、FC、EC和DC由同一个概念owner承接；专属完整定义不塞入`纯概念定义`联合。
4. 未知不转零，规则不支持、预算不足、历史清理、事实漂移和内部错误不转“不匹配”或“同义缺失”。
5. 匹配采用满足、不满足、证据不足三态；技术状态独立分流。
6. 完整范围与条件模式分账；不压缩连续同向段，不插值，不默认时间规整或相位等价。
7. 只沿4120正式组成槽位递归；概念上下位、派生来源、向量元素和任意对象关系不作为组成边。
8. 世界来源需要发生期证明时，缺少正式提供者必须返回`发生期依据不足`；机械计算成功不能升级为现实支持成立。

## 3. 当前事实矩阵

| 能力 | 当前实现事实 | 与目标差距 | 裁决 |
| --- | --- | --- | --- |
| 调用期二次特征DAG | `业务/计算.二次特征.h/.cpp`已实现准确F、已保存派生输出、本次局部输出、有限DAG、G/H、预算、阶次和固定K/当前K互证 | 不读取B，不解释FC/EC/RC，不形成方向模式 | 原样复用其一般计算能力；不让它承担RC语义 |
| 有序I64比较 | `领域/算法.有序I64特征比较.h`已提供正式比较和溢出分流 | 缺RC及动态概念调用编排 | 作为4242唯一实际数值运算提供者 |
| 实例动态合同与服务 | `合同.动态结构.h`、`数据服务.动态类.h`存在4213 DTO、12个公开操作、E/B/S/FT/K核验、组合/展开/退出/清理候选实现 | 未进入生产装配或任何编译单元；无创建/清理生产调用；交付共同代次与生命周期、未知owner关系拒绝、共享预算和显式栈存在漂移 | D0已完成静态事实审计，代码符合性修复尚未实施；另立`DYNAMIC-4213-CONFORMANCE-CLOSURE`修复叶，不与RC语义混写 |
| 唯一概念owner | `数据服务.概念树类.h/.cpp`承接pure v2、FC出生使用、存在概念两组和相关概念现行面 | 无RC/DC专属结构、读取、写入及初始化交付 | 只在同一类、同一owner中窄扩展 |
| RC结构 | 受跟踪生产源码零命中4242 RC合同和结构入口 | 4242第3—6节整体缺失 | 首个代码叶RC-P1 |
| RC/FC/EC求值 | 生产源码零命中 | 4242第7节整体缺失 | RC-P2；复用既有I64提供者 |
| RC形成 | 生产源码零命中 | 4242第8节整体缺失 | RC-P3 |
| 动态概念结构与算法 | 生产源码零命中7191专属合同、结构服务和应用 | 7191—7195整体缺失 | DC-P1至DC-P4逐叶闭合 |
| 动态概念验收 | T001—T096尚未在当前实现执行 | 缺算法、结构、来源、并发、恢复和消费者证据 | 各阶段分账，最终DC-P5汇总 |

当前关键源码blob：

- `数据服务.概念树类.h`：`d4c687310afc130751ca69b39dbca52a19343e07`
- `数据服务.概念树类.cpp`：`6df32cbeab0a40bd1871efb002248a21fd3dece6`
- `装配.普通应用.h`：`0e565ab1f19d24caaa1452030a466b0528f7a295`
- `装配.普通应用.cpp`：`cb1f3e4e99d50b5d9fba15921974f0699fcca80a`
- `计算.二次特征.h/.cpp`：`add2bb708b8d796ebeabb73652d1a5ef6ed85827` / `34c51100112de94297de6f53bf662b5c047d94d5`
- `合同.动态结构.h`：`5095d28247e131f883bbdd72ee39301d199a2ee3`
- `数据服务.动态类.h`：`cb0544b5a4a255b03f88cc5fcc98c4cd9b468387`
- `算法.有序I64特征比较.h`：`66894d57ff810b9d3b9fd26258dc9b69b44ec3d5`

上述blob只证明基线提交中相应文件的精确内容，不证明本工作区新增设计已经发布，也不证明能力闭合。代码相对目标账发布提交未变化；知识库概念/动态用途卡绑定旧L2模块时代基线，只能作历史导航，当前设计以现行规范和源码为准。

## 4. 依赖有向图与代码叶

```text
D0 实例动态4213符合性审计/必要修复
                         ┐
RC-P1 RC结构owner闭环 ──┼→ RC-P2 RC/FC/EC求值 → RC-P3 RC形成
                         │                         │
                         └→ DC-P1 DC结构owner与纯算法 ─┐
D0 + RC-P2 ───────────────→ DC-P2 来源与方向结构 ─────┼→ DC-P3递归匹配
RC-P3 + DC-P1 + DC-P3 ────────────────────────────────┼→ DC-P4形成发布恢复
                                                       └→ DC-P5生产接线与7196验收
```

RC-P1与D0只读审计可以并行；任何D0代码修复若触及普通应用、概念owner、共享工程或相同验证资源，必须与RC-P1串行。当前最早可以形成独立代码结果的是RC-P1，因为它不依赖动态实例写入，却是RC求值、DC定义引用和后续发布的共同结构前置。

## 5. 阶段设计

### D0：实例动态符合性闭合

目标：修复已确认的4213实现漂移并取得当前源码证据。固定范围包括：

1. 先按正式治理消除4213仍写`.ixx`而项目已迁移`.h/.cpp`的物理定位漂移；未获规范修订批准前只保留设计输入，不直接施工该项。
2. 让`数据服务.动态类.h`进入真实编译单元，避免仅`ClInclude`登记造成根工程从未解析实现正文。
3. 补齐动态结构交付的共同G、建立代次、owner和生命周期核验。
4. 枚举并拒绝动态owner未知、混合或额外自有关系，不能只查询四种已知类型。
5. 按4213统一累计候选、动态、来源边、深度和公开材料预算。
6. 将组合核验和来源展开从受调用者`uint64_t`控制的C++递归改为显式有界栈。
7. 在后继独立叶完成动态owner、结构登记、普通应用服务生命周期、S→B→D→组织D生产路径以及启动/每60秒清理接线。

现有4203真实B提供者及两个源码级读取消费者可复用，但它们不证明动态服务已经生产装配。D0修复与RC-P1互不消费；只要不同时占用普通应用、工程和验证资源，设计可并行，代码发布仍串行。

完成边界：只证明4213具名范围；不证明场景组织、RC、DC或现实发生期成立。

### RC-P1：RC结构所有者闭环

目标：实现4242第3—6节。新增RC合同、53项外部结构初始化、同一概念owner专属结构读写、规范化/查重、来源、用途、图、父组、生命周期、来源释放、退出和FC/EC专属有界读取。

当前公共合同缺口：4242/7191使用的`概念树读取头`、`概念树写入头`已随旧接口退出。它们按既有正式设计恢复为纯值公共头：读取头为`合同版本、Gread、H`，写入头为`合同版本、期望事实代次、L1所有者范围写入幂等身份`；放入现行`合同.相关概念添加参与.h`，不恢复旧CRUD、旧根、旧定义variant或旧服务入口。

完成边界：RC结构和恢复闭合，不证明现实求值或自动形成。

### RC-P2：RC/FC/EC调用期求值

目标：实现4242第7节的四类入口，共用一套私有算法：已发布RC、FC、EC和候选RC定义。来源只接受正式F或B分支；B必须展开C/E/S，FC域、EC定义、固定K和当前K逐项互证，实际数值只调用有序I64比较提供者。

完成边界：只产生调用期结果和见证，零持久写；不证明RC已经形成或被业务采用。

### RC-P3：RC形成与采用阶段

目标：实现4242第8节候选命中、查重、定义发布/复用、来源阶段及原键收敛。定义键和来源键分账；定义已发布而来源失败保留真实阶段。形成不自动命名或记用途。

### DC-P1：DC结构owner与纯算法

目标：实现7191第3—8节中不依赖世界样本求值的部分：动态类别公共值扩展、DC合同、73项外部初始化、唯一owner专属定义/图/关联/名称/治理、纯规范化和包含算法、旧格式显式分流。

与RC-P1共用概念owner但不并行写。DC类别加入现行`相关概念类别`时必须同步核对所有穷尽switch和现有存在/特征消费者，不能只追加枚举后依赖默认分支。

### DC-P2：来源、槽位递归与方向结构

目标：实现7192/7193以及7191第14节具名后继格式。两类来源分支统一为调用期视图；只沿4120正式槽位递归；原生、补充和保持比较分账；保留完整离散方向及相对端点关系。

该阶段只消费RC-P2和4213/4203/4212/4204正式读取，不复制二次比较算法。缺发生期证明时只开放规范允许的机械出口。

### DC-P3：递归匹配与整体判定

目标：实现7194三态匹配、有界候选域、整体角色/槽位/位置/时间统一绑定、完整范围/条件模式分账以及调用期匹配见证。叶局部命中不能拼成父级成功。

### DC-P4：原型、查重、分类与发布恢复

目标：实现7195和7191第9—11/14节：完整原型、同义查询、保守包含、多父分类、形成或复用、实例支持、上位形成、P0—P4阶段和原键收敛。未完成查询不得触发新建。

世界形成依据和支持若需要但缺正式发生期提供者，保持规范规定的`发生期依据不足`零写；不得以该拒绝分支声称现实支持已实现。

### DC-P5：生产接线、恢复与验收

目标：完成普通应用生命周期、根工程/受影响消费者双配置、独立重启、发布未知、并发、取消、真实消费者和7196分层验收。完成声明必须列实际执行的T编号和未运行项。

## 6. 7196分阶段验收映射

| 验收范围 | 主要承接阶段 | 证据层 |
| --- | --- | --- |
| T001—T016数值与方向 | RC-P2、DC-P2 | 纯算法+正式provider黑盒 |
| T017—T036序列与时间 | DC-P2、DC-P3 | 来源视图、方向结构、时间闭包 |
| T037—T056组成与递归 | D0、DC-P2、DC-P3 | 4213来源、4120槽位、整体绑定 |
| T057—T072定义查重分类 | DC-P1、DC-P4 | 纯规范化、owner唯一、图治理 |
| T073—T081并发发布与阶段 | RC-P1/P3、DC-P1/P4 | 首次材料、发布读回、并发唯一 |
| T082—T086现实来源与兼容 | D0、DC-P2/P4 | 发生期拒绝、两类来源、旧格式分流 |
| T087—T093预算调度缓存历史 | 全阶段按责任分账 | 边界/性质/历史清理 |
| T094—T096退出取消未知 | RC-P1/P3、DC-P1/P4/P5 | 引用保护、阶段恢复、取消/未知 |

任何阶段只运行其已实现范围；不得将部分T项通过升级为7196全部完成。

## 7. 文件和所有权边界

公共概念值类型只在`领域/合同.相关概念添加参与.h`保留一份。RC新增文件归RC合同、初始化、求值、应用各自唯一职责；DC新增文件归DC合同、初始化和应用。概念结构写入统一扩展`数据服务.概念树类.h/.cpp`，普通应用只做构造前初始化与生命周期装配。

`计算.二次特征.h/.cpp`和`算法.有序I64特征比较.h`在RC/DC阶段默认只读复用；只有当前事实证明其公开合同与正式规范不一致，才另立修复叶，不能借消费者施工扩大修改。

## 8. 当前下一动作

1. 按本方案刷新`SECONDARY-RELATION-CONCEPT-GAP-CLOSURE`到当前HEAD，删除旧删除计划尚未完成、51项初始化和概念基础类型迁移等过期结论。
2. 将已完成的`LEGACY-CONCEPT-INTERFACE-REMOVAL`移入已完成计划并从计划索引删除。
3. 形成并登记唯一当前可执行计划`SECONDARY-RELATION-CONCEPT-STRUCTURE-P1`。
4. P1代码发布后重新S0，再形成RC-P2；其余阶段只保留在本方案，不提前堆入计划索引。

## 9. 完成声明边界

本方案发布只证明动态概念主线、依赖顺序、当前差距和分层验收路径已经设计闭合到可以形成首个代码叶。它不证明RC、动态概念、构建、运行、恢复、现实支持、实际消费者或T001—T096已经实现或通过。
