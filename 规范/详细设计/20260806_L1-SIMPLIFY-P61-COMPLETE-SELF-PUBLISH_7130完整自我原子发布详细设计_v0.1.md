# L1-SIMPLIFY-P61 7130 完整自我原子发布详细设计 v0.1

日期：2026-08-06

状态：设计就绪；代码未实现；计划必须待激活

创建侧正式基线：`main == origin/main == 11c2c30a9d0e3bfde9aadedafb52803c982b7e24`

正式依据：0050、4015、4040、4070、4190、7130；SELF-GOV-CLOSURE v0.2；P15现行正式合同及替代结果门禁；P17—P21、P53—P60冻结合同。

## 1. S0 与最早缺口裁决

P59已冻结存在域/场景域跨域总规格，P60已冻结总规格到连续L1本地键、完整创建写集和通用发布后读回计划的纯值映射。两者仍不形成公开幂等身份、请求意图、P15执行证据、一次提交、稳定编码结果或业务发布事实。

| 所需能力 | 唯一现有或冻结提供者 | P61裁决 |
| --- | --- | --- |
| 两域准入、共同登记/根/截止、四角色与成员顺序 | P59 | 首次路径恰调用一次，不旁路P57/P58 |
| `4节点+4值+(5+N)关系+4槽`写集与`17+N`读回 | P60 | 首次路径恰调用一次，不重排键或重建第二草案 |
| provider前幂等、结构化执行证据、一次L1 v2提交、同锁读回与隔离 | P15 v0.14冻结合同及其待实现结果 | 消费已冻结L1身份与分账合同；不把可执行计划伪装成代码能力 |
| 五身份五固定关系领域读取 | P56 | L1成功/精确重复后恰调用一次，验证当前完整结构 |
| 出生期角色2发布读回 | P60计划 + P15首次同锁读回 | 按键14以后逐项映射，不新增第二成员账 |
| 自我结构登记附属投影 | P53已由P55独立发布 | 本次不改投影，P54/P37调用为0 |
| 公开完整自我原子发布 | 无 | **本叶最早真实缺口** |

不另建“发布前幂等/证据规格provider”：公开幂等键、意图摘要、P59值式规格、P60规范化草案和写前见证属于同一发布操作；拆分会制造第二解释者，并不能解除P15真实接口门禁。不另建事务参与者：完整自我的全部新事实只写L1，P53登记已经独立存在，P56是只读provider，没有第二候选或第二账需要与L1同步确认。

因此P61直接成为7130具名完整自我原子发布者。P15 v0.14已正式冻结唯一L1 provider身份`(0x4C31464200000001,2,0)`、调用次数/去重见证分账和恢复拒绝合同，当前可执行但代码结果尚未进入main并经交互接受。P61冻结本操作拥有的输入、来源集合、身份读取时点、调用计数和结果约束；P15内部账审计材料、恢复编码及实际DTO仍等待真实代码结果机械适配。

## 2. 目标与最大边界

新增唯一公开根：

```cpp
完整自我发布结果 L1完整自我发布服务::发布完整自我(
    const 完整自我发布请求& 请求) const;
```

首次业务顺序固定为：

```text
公开请求纯值准入
-> 0x14 L1幂等键与请求意图摘要
-> P15 provider前探测恰一次
-> 仅未找到时读取L1 provider身份凭证恰一次
-> 仅未找到时调用P59形成跨域总规格恰一次
-> 调用P60形成L1写集/读回草案恰一次
-> 绑定本操作拥有的执行证据并提交一次L1 v2事务
-> 同锁读回全部13+N新事实与4属性槽
-> P56正式读取五身份五固定关系
-> 映射首次或幂等完整发布事实
```

本叶只发布完整自我五身份中的四个新节点、五条固定关系、三个场景标记、自我实际存在资格和请求给出的出生期成员关系；世界根、出生期成员和P53登记均为已发布前置。它不建立初始化装配入口、实例槽、安全/服务根定义、根值、根需求、恢复、旧栈删除或正式集成验收。

## 3. 物理位置、所有权与依赖

原位扩展共享数据头，并新增发布与专项模块：

```text
海中鱼巣/领域/L1完整自我结构.数据.h
海中鱼巣/领域/发布.L1完整自我.ixx
海中鱼巣/领域/自检.L1完整自我发布.ixx
```

服务固定为：

```cpp
class L1完整自我发布服务 final {
public:
    L1完整自我发布服务(
        L1事实基座服务& L1,
        const L1完整自我创建服务& 完整自我创建,
        const L1完整自我写集数据操作& 写集数据,
        const L1完整自我结构服务& 完整自我读取) noexcept;

    完整自我发布结果 发布完整自我(
        const 完整自我发布请求& 请求) const;
};
```

四个依赖均为非拥有引用，宿主寿命覆盖服务和同步调用。服务不可默认构造、复制或移动，不保存锁、缓存、规格、草案、首次结果、稳定编码或调用方引用。

```text
后继全新运行期初始化入口
-> L1完整自我发布服务::发布完整自我
   -> P15 探测幂等 / 提交写集
   -> P59 L1完整自我创建服务::形成完整自我跨域总规格
   -> P60 L1完整自我写集数据操作::形成完整自我L1写集草案
   -> P56 L1完整自我结构服务::读取完整自我结构
```

P56/P59/P60/L1不得反向依赖发布服务。发布模块不import P37/P54、仓库、SQL、材料、旧WORLD-TREE或运行期上下文。

## 4. 公开DTO与结果完整性

在共享数据头追加：

```cpp
inline constexpr std::uint32_t 完整自我发布合同版本 = 1;
inline constexpr std::uint32_t 完整自我发布规则版本 = 1;

struct 完整自我发布幂等身份 final {
    std::uint64_t 值 = 0;
    friend bool operator==(const 完整自我发布幂等身份&,
        const 完整自我发布幂等身份&) = default;
};

struct 完整自我发布请求 final {
    std::uint32_t 合同版本 = 完整自我发布合同版本;
    std::uint32_t 发布规则版本 = 完整自我发布规则版本;
    完整自我发布幂等身份 幂等身份;
    完整自我跨域总规格请求 规格请求;
    friend bool operator==(const 完整自我发布请求&,
        const 完整自我发布请求&) = default;
};

enum class 完整自我发布状态 : std::uint8_t {
    已提交 = 1,
    幂等读回 = 2,
    入口拒绝 = 3,
    自我结构登记未加载 = 4,
    世界结构未加载 = 5,
    实际存在登记未加载 = 6,
    出生期成员未找到 = 7,
    已有完整自我 = 8,
    版本漂移 = 9,
    许可拒绝 = 10,
    幂等冲突 = 11,
    资源失败 = 12,
    内部不一致 = 13
};

struct 完整自我出生期成员发布事实 final {
    世界结构节点身份见证 成员;
    世界结构关系身份见证 发布成员关系;
    std::uint64_t 创建事实代次 = 0;
    friend bool operator==(const 完整自我出生期成员发布事实&,
        const 完整自我出生期成员发布事实&) = default;
};

struct 完整自我发布事实 final {
    std::uint32_t 合同版本 = 完整自我发布合同版本;
    std::uint32_t 发布规则版本 = 完整自我发布规则版本;
    std::uint64_t 发布事实代次 = 0;
    完整自我结构事实 完整结构;
    std::vector<完整自我出生期成员发布事实> 出生期成员;
    friend bool operator==(const 完整自我发布事实&,
        const 完整自我发布事实&) = default;
};

struct 完整自我发布结果 final {
    完整自我发布状态 状态 = 完整自我发布状态::入口拒绝;
    完整自我发布幂等身份 幂等身份;
    写集幂等键 L1幂等键;
    std::uint64_t 事实代次 = 0;
    bool L1发布已存在 = false;
    std::optional<完整自我发布事实> 事实;
};
```

幂等身份合法范围为`1..0x00FF'FFFF'FFFF'FFFF`。仅`已提交/幂等读回`携带完整事实；发布前非成功`L1发布已存在=false`且事实代次0。P15已确认发布但P56读回失败时保留P15非零代次和`L1发布已存在=true`，事实为空，不否认已发布L1事实。

`完整结构`必须是P56 `完整=true`值式事实，五身份和五固定关系逐项等于P15首次映射/读回，结构读取截止不得早于发布事实代次。出生期成员按公开请求顺序返回，成员和关系均非零；关系源为自我内部世界场景、目标为成员、类型为P53自我结构关系类型、角色2、创建代次等于发布代次且当前有效。空成员组合法。

## 5. 入口、幂等键与请求意图

在P15/P59/P60/P56前纯值验证：顶层两版本精确；幂等身份合法；嵌套P59五版本、登记身份、普通根、非零期望代次有效；成员组严格按存在编码升序、无重复且每项普通非零。失败返回入口拒绝，全部依赖调用0。

身份固定且创建基线扫描未占用：

```text
L1幂等域高8位 = 0x14
低56位 = 完整自我发布幂等身份.值
领域操作标签 = U32 0x5033'6101
专项顺序 = 561
```

请求意图摘要按P15正式确定性编码入口形成，语义字段顺序固定为：魔数、编码格式版本、操作标签、顶层合同/发布规则、幂等身份、嵌套P59请求全部字段声明顺序。成员组按请求顺序全量编码；不包含P59/P60结果、L1本地键、稳定编码、provider材料、锁、地址或时间。摘要全零为内部不一致且不探测。

调用P15 `探测幂等`恰一次：

| 探测状态 | P61结果 | P59/P60/提交 |
| --- | --- | --- |
| 未找到 | 进入首次路径 | 最多1/1/1 |
| 同义已发布且首次映射/读回完整 | 进入首次结果恢复和P56验证，最终幂等读回 | 0/0/0 |
| 异义冲突 | 幂等冲突 | 0/0/0 |
| 入口拒绝 | 入口拒绝 | 0/0/0 |
| 许可拒绝或已隔离 | 许可拒绝 | 0/0/0 |
| 资源失败 | 资源失败 | 0/0/0 |
| 内部不一致、坏成功或未知 | 内部不一致 | 0/0/0 |

同义路径只消费本次请求、首次`1..13+N`映射、`17+N`同锁读回和首次确定性结果材料，不重调P59/P60，不重写事实。随后P56只验证当前完整结构；不能用P56当前值重新解释原始发布写集。

## 6. 首次P59/P60与执行证据边界

仅探测未找到时：

1. 把`请求.规格请求`原样传给P59恰一次；P59全部具名非成功同名映射，`已形成`坏规格或错请求/截止为内部不一致。
2. 把P59总规格传给P60恰一次；`入口拒绝`在内部合法调用中映射内部不一致，资源失败同名，其它坏成功/未知为内部不一致。
3. 草案必须满足P60规则版本、登记、根和截止等于P59；本地键连续`1..13+N`；数量`4/4/(5+N)/4/0/(13+N)/(17+N)`精确。

P61只向P15正式执行证据接口提供本操作拥有的确定材料：请求意图摘要；P59/P57/P58/P60合同与规则版本；P59完整总规格；P60完整草案；共同事实截止；草案中全部既有稳定引用的写前事实见证来源；规范化写集和读回计划。仅在幂等探测未找到后，通过注入的L1服务读取身份凭证恰一次，必须精确为`(0x4C31464200000001,2,0)`；该身份读取不计入事实provider调用次数。既有引用至少覆盖指定世界根、场景标记服务/属性类型、实际存在资格服务/属性类型、三种关系类型和每个出生期成员资格见证；按P15正式对象种类、稳定身份和完整字段规则排序去重，不能只取首项或用调用次数冒充去重后数量。

P59直接调用次数为1，P60纯数据操作调用次数为1；P57/P58及其内部provider调用由P59合同拥有，不在P61重复计数。L1身份读取为合同见证读取且事实provider调用次数为0；P60规则版本属于确定算法材料，不伪装成机器provider稳定身份。P15拥有provider身份DTO、合同版本、调用次数/去重见证分账、材料摘要、账/审计双副本和恢复互证；P61不得复制或另存第二载体。

若P15真实代码结果要求P61提供P59当前未携带、且P15 v0.14未冻结的新稳定provider身份，或要求改变P59/P60公开DTO、事实来源或写集语义，则计划保持待激活并具名退回，不发明身份、不从函数地址/调用序号/摘要反推。

## 7. 一次提交、映射与同锁读回

从P60草案构造唯一L1 v2请求：合同版本取P15正式版本；期望代次=P59/P60共同截止；幂等键=0x14映射；五类写集与读回计划逐字段复制草案；执行证据按第6节绑定。调用P15正式`提交写集`恰一次。

`成功/精确重复`分别进入`已提交/幂等读回`候选；入口拒绝、事实代次漂移、幂等冲突、许可拒绝、资源失败和内部不一致逐类映射，未知状态内部不一致。成功/重复必须满足：

1. 发布代次非零且晚于P59截止；新编码映射恰`13+N`项，键1至`13+N`各一次、编码非零互异；
2. 同锁读回恰`17+N`项，顺序、种类和逐字段事实等于P60计划；所有新节点/关系/值创建代次等于发布代次且当前；
3. 键1—4映射四新节点，键5—8映射三场景标记值和自我资格值，键9—13映射五固定关系，键14以后逐项映射角色2成员关系；
4. 四属性槽当前值分别为键5—8映射，零退出；世界根和出生期成员无新编码映射；
5. 首次执行证据、请求/写集摘要、确定性结果材料、失败见证和隔离形状符合P15真实合同。

任何缺项、额外项、重复键、错类型/端点/角色/材料/来源/槽/代次或坏成功均为发布后内部不一致，保留非零代次和`L1发布已存在=true`，不返回部分事实，不补写或回滚。

## 8. P56正式读回与业务结果

只在P15成功/精确重复且首次材料完整后，以请求登记身份和指定根调用P56恰一次。P56 `已读取`且`完整=true`才继续；其五身份、五固定关系、自我资格和登记关系类型必须逐项等于P15映射/读回，读取截止不得早于发布代次。

P56版本漂移映射P61版本漂移；许可拒绝、资源失败同名映射；登记/世界/存在未加载、尚无已发布自我、入口拒绝、内部不一致、坏成功或未知值在P15已经证明发布后均映射内部不一致。以上发布后非成功都保留P15代次和`L1发布已存在=true`，事实为空；同幂等请求可零写重试P56验证。

P56不返回角色2成员组。P61只从P15首次同锁读回中按键14以后形成请求同序出生期成员发布事实，并用P56登记中的关系21类型和内部世界场景逐项互证。不得扫描当前其它角色2成员，不把后续新增成员加入原始发布结果。

## 9. 并发、异常与生命周期

服务不自建锁、缓存或幂等账。P59/P60发生在L1独占提交前，不持任何provider许可跨调用；探测后竞争由L1再次裁决。同键同义只发布一次，其余读取首次结果；同键异义冲突；不同键同截止竞争最多一笔成功，其余版本漂移；已有其它完整自我由P59/P56拒绝。

P56发布后读取可能遇到新事实推进；只要其当前完整结构仍与首次映射一致且截止不早于发布代次即可完成，不要求无关事实停止。P56具名版本漂移返回可重试版本漂移，不重新提交。

请求、摘要、规格、草案、写集、P15结果、P56结果和公开结果均为值式对象。函数不`noexcept`；分配失败在发布前映射资源失败，未知异常发布前映射内部不一致；P15成功后任何异常保留已发布代次并返回内部不一致。零sleep、零内部重试、零普通快照旁路。

## 10. 验证矩阵

顺序561专项至少覆盖：

1. 顶层/嵌套版本、零登记/根/代次、乱序/重复成员、零/越界幂等身份在探测前拒绝；
2. 0x14键、0x50336101标签、网络序、字段顺序、成员全量编码和全零摘要拒绝；
3. P15同义时P59/P60/提交均0，首次映射/读回恢复后P56一次；异义/隔离/坏账全短路；
4. 首次顺序严格P59一次、P60一次、P15提交一次、P56一次；P61直接P57/P58/P37/P54调用0；
5. P59全部状态、P60四状态、坏成功、错截止/登记/根/成员逐类映射；
6. 执行证据来源覆盖全部既有稳定引用，调用次数与去重见证数量分账，不伪造P59/P60 provider身份；
7. 写集、映射和读回数量公式对N=0、1、多项成立，本地键1至`13+N`连续唯一；
8. 键1—13固定角色/值/关系、键14以后成员关系、四槽和零退出逐字段精确；
9. 映射/读回缺失、额外、重复、错对象/类型/端点/角色/材料/来源/生命周期/代次均发布后内部不一致；
10. P56五身份五关系完整互证，截止等于或晚于发布代次；P56漂移可同键零写重试；
11. 出生期成员只从首次读回按请求顺序形成，空组合法，不扫描或夹带其它当前成员；
12. 同键同义/异义、不同键同截止竞争、P59形成后竞争和P56读回期间推进矩阵；
13. 发布前失败L1零变化；发布后失败保留代次/幂等账/失败见证和`L1发布已存在=true`，零回滚/补写；
14. 静态证明P37/P54、第二账、仓库、SQL、材料、缓存、自建锁、sleep/retry、普通快照、第二提交、初始化装配和旧栈调用0；
15. P56/P59/P60/P15专项不退化；顺序561唯一且560—550保持；
16. Debug/Release Rebuild、两配置完整自检、strict110/110、模块扫描和八文件白名单diff check通过。

## 11. 激活门禁与最大声明

计划保持`待激活`，直至P15替代结果与P53—P60真实结果进入main并经交互接受；实际探测、执行证据、提交、映射、读回、隔离接口及P56/P59/P60 DTO机械匹配。P15设计/索引发布或可执行状态本身不解除代码结果门禁。

活动计划须释放共享数据头、两个新模块、工程/filters/启动、顺序561、构建、自检和模块扫描资源；P56—P60须释放`L1完整自我结构.数据.h`所有权。新HEAD确认0x14、0x50336101、561和发布者唯一，无等价完整自我发布者，目标八文件与异主WIP零重叠。

完成后最多声明：源码候选能以公开幂等身份消费P59总规格和P60草案，经一次L1 v2事务发布完整自我四个新节点、五固定关系、资格/场景标记及出生期成员关系，并由同锁读回和P56正式读取形成首次/幂等完整结果。

不得声明：P15/P53—P60由本叶实现、全新运行期初始化入口、实例槽、安全/服务根、根值、根需求、装配、恢复、旧栈删除、正式集成验收、STEP-5或治理闭环完成。
