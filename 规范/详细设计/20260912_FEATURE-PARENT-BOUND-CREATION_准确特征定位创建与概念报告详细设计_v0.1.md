# 准确特征定位创建与概念报告详细设计

身份：FEATURE-PARENT-BOUND-CREATION；版本：v0.1；日期：2026-09-12。

## 1. 目标、基线与不变边界

用户已裁决：没有上级节点信息不能创建准确特征信息。上级定位固定为场景C和组织父P，P是C特征根或同树已组织父F。缺失、无效、错场景或无法核验时，不分配身份、不写F、不登记观察、不生成概念。F内容、C已知F和P到F组织必须同次正式发布，不允许先造无父F后补挂。

代码基线为 `45ff0105a15b4f65e0160b71f6ac203f5f48e705`。1130/4110/4202/4205/7150/7160及0050、4030新口径是实施依据；0300 C043只记裁决路由。概念仍按FT加规范化域独立形成，位置不参与概念签名，名称/概念失败不撤销已定位F。C038具体二次派生实例不在范围。

本计划交付数据组合发布与现有应用消费者迁移，不要求主程序真实采集或跨进程恢复证明。两个20260906 FEATURE-EXISTENCE-SCENE未跟踪记录属于他人，禁止修改、移动、删除或提交。本任务不复活已退出的20260905/06旧概念计划。

## 2. 当前事实与复用裁决

| 当前事实 | 裁决 |
| --- | --- |
| 特征类 `创建准确特征(FT,准确值)` 只建立内容；唯一生产调用在特征概念应用，专项另有一个调用文件 | 删除该无父公开函数和定义；保留其字段构造算法为内部原子片段 |
| 场景 `组织特征实例` 已按4205验证C/P/F、已知、全局单父及路径，要求F已经存在 | 保留存量迁移接口；新生F不顺序调用它，复用位置核验与组织边形状 |
| 存在类已知F使用自己唯一特征关系类型和角色1 | 新生F同时写C到新F的已知边，不写当前采用 |
| L1写端口已有 `提交三分区原子事务_v2(request,第二端口,第三端口)`；仓库共同G及首次组合账、owner首次账和全局闭包已存在 | 直接复用，L1零改；不用旧状态/动态v1，不需要v3 |
| 4030通常禁止借出写端口，C++跨命名模块的前声明不能随意作为friend占位 | 本目标用同模块定义的具名窄参与者接口与组合服务；仅private虚调用可接触端口，不公开getter、不循环import |
| 特征概念应用只持feature/concept，观察请求无C/P；场景成员应用场景引用为const | 只给应用增加定位服务依赖和位置材料，不让应用持L1或原始写集 |

知识库相关卡仍为旧接口快照，仅用于导航；施工必须回读上述当前源、4205已发布记录、L1 v2真实实现和所有调用点。不存在历史概念组或通用参与者工厂的复用假设。

## 3. 唯一物理位置及可见性

新增模块 `海中鱼巣/领域/数据服务.定位特征.ixx`，模块名 `海中鱼巣.领域.数据服务.定位特征`。本模块定义本节全部DTO、窄参与者接口及 `定位特征数据服务`，只import现L1公开合同/服务和特征值模块，不import特征/存在/场景具体类。新类型使用具名稳定编码字段，不迁移既有FT/F身份类型的模块归属。

三个既有类import新模块并各自公开继承 `定位特征内容参与者`、`定位特征已知参与者`、`定位特征组织参与者` 中对应的一个；三接口的虚方法全部private，只有在同一新模块定义的 `定位特征数据服务` 为friend，析构public virtual。既有类的override也为private。外部仅能传递借用引用以装配，不能调用准备、读取端口或提交自造写集。禁止将接口用于其它事务/owner选择或新增公开操作令牌。

每接口private固定两个基本方法：`const L1事实基座服务& 定位底座() const noexcept`、`const L1所有者范围写端口& 定位端口() const noexcept`；仅用于一次组合的同实例核验和同步提交，组合服务不保存/复制/移动端口。其余private方法见第5节；方法体留各自原模块。组合服务持三接口引用及自己的串行互斥锁，不持第二owner、签发器或可变事实缓存。组合核心不得持有某参与者的本地锁再进入另一参与者；各owner既有内部只读依赖沿原锁顺序，L1 G守卫负责外部并发收敛。

构造 `定位特征数据服务(定位特征内容参与者&,定位特征已知参与者&,定位特征组织参与者&)` 只核验三者底座实例地址相同、端口真实绑定、owner两两不同及既有结构登记就绪；失败抛invalid_argument，零写。接口不得从编号相同推断同底座。

## 4. 新公开合同

全部结果值式，操作字段不进入F本体。当前准确特征实现仅支持I64及可解析为I64的特征值身份，本计划保持该表示面；其它表示明确不支持，不增加轮廓算法。

| 类型 | 精确字段 |
| --- | --- |
| 定位特征位置 | `稳定编码 场景,组织父`，两者有效且不等 |
| 定位特征预算 | `uint64_t 最大候选数,最大路径长度`，前者非零，后者1..4096；内容/已知组合窄读沿现有限预算 |
| 定位特征创建请求 | `uint32_t 版本=1; uint64_t G0=0; 定位特征位置 位置; 稳定编码 正式特征类型; variant<int64_t,特征值身份> 准确值; optional<稳定编码> 指定F; L1所有者范围写入幂等身份 组合键,内容键,已知键,组织键; 定位特征预算 预算` |
| 定位特征事实 | `稳定编码 F,正式特征类型; int64_t 准确I64; 定位特征位置 位置; 稳定编码 已知关系,组织关系; uint64_t 出生H`；出生H取F实际创建代次，迁移后位置关系可较晚，不伪造同生 |
| 定位特征结果 | `uint32_t 版本=1; 定位特征状态 状态; uint64_t Gread=0; optional<uint64_t> 首次发布H; optional<定位特征事实> 事实; vector<稳定编码> 候选F; 定位特征创建请求 原请求` |
| 定位特征读取请求 | `uint32_t 版本=1; uint64_t Gread,H; 稳定编码 F; 定位特征预算 预算` |
| 定位特征读取结果 | 版本1、专属状态、Gread/H、`optional<定位特征事实> 事实` |

`定位特征状态:uint8_t` 固定：已创建=1、已复用=2、精确重复=3、已读取=4、入口拒绝=5、未找到=6、目标已退出=7、位置冲突=8、需选择F=9、存量未定位=10、事实代次漂移=11、幂等冲突=12、预算不足=13、历史材料不可用=14、表示不支持=15、资源失败=16、内部不一致=17、已可能发布=18。映射现provider精确同义状态；无对应的引用冲突按其位置/内容来源映射位置冲突或入口拒绝，损坏来源/回显矛盾内部不一致，不吞掉资源/历史/漂移。

公开函数：`定位特征结果 创建或复用(const 定位特征创建请求&)`、`定位特征读取结果 读取定位特征(const 定位特征读取请求&) const`。同一创建入口承担原键重放，不再设通用提交函数。仅供装配的三个公开只读谓词为 `bool 使用内容参与者(const 定位特征内容参与者&) const noexcept`、`bool 使用已知参与者(const 定位特征已知参与者&) const noexcept`、`bool 使用组织参与者(const 定位特征组织参与者&) const noexcept`，分别只比较借入对象地址，不返回provider或端口。

请求四键必须非零且两两不同，不得等于任一参与owner的保留登记键；保留键判断复用各owner的现有守卫。请求不能缺位置后在函数内部默认填根。候选/路径预算不参与内容同一性，调用方须保存原完整请求以便有界重试。L1精确幂等比较的是原G、四键、C/P/FT、准确表示种类和解析I64所形成的完整规范化三写集；不声称能从首次账恢复未写入的预算或同值输入引用身份字节。新建必须指定F为空；非空只能进入复用裁决。预算变更仍须重新满足完整核验，不造成新的语义身份；不同原始引用解析为相同I64且表示种类相同，按原复制值模型是同义写入，不另存外部身份整数副本。

创建成功谓词仅接受1/2/3、版本1、非零Gread、完整F/FT/位置/两关系及出生H不晚于读回截止，准确值和位置与原请求互证；1/3必须有非零首次发布H及共同三参与者读回，2零本次发布且无首次发布H。需选择F返回全部确定身份升序唯一候选且无事实；超预算不截断。其它失败事实及候选均空；已可能发布保留原请求及已知首次H，不把部分F作为成功载荷。读取成功要求4且Gread/H回显严格一致，H时三种事实均有效。

## 5. 参与者私有准备及共同写集

三个接口使用同模块具名值式见证，类型不暴露写端口，不依赖三个具体owner的模块：

| 私有协作见证 | 精确字段 |
| --- | --- |
| 定位内容见证 | `稳定编码 F,正式特征类型; int64_t 准确I64; uint64_t 创建H; optional<uint64_t> 退出H` |
| 定位关系见证 | `稳定编码 关系,源,目标,关系类型; int64_t 角色; uint64_t 创建H; optional<uint64_t> 退出H` |
| 定位场景已知见证 | `稳定编码 场景; optional<稳定编码> 父F; optional<定位关系见证> 父已知关系`；父为根时后二者均空，父为F时均完整 |
| 定位父路径见证 | `稳定编码 场景,特征根,组织父; vector<稳定编码> 路径节点; vector<定位关系见证> 路径关系`；从根到P的完整有序单链，根父时一节点、零边 |
| 定位路径见证 | `稳定编码 场景,特征根,组织父,F; vector<稳定编码> 路径节点; vector<定位关系见证> 路径关系`；从根到F的完整单链，末边为P到F，不接受零边 |

统一返回模板 `定位参与者结果<T>{定位特征状态 状态;uint64_t Gread,H;optional<T> 数据;}`，成功为已读取+完整指定T、精确Gread/H；每个见证创建H不晚于H，退出H为空或大于H；预算不足不截断。异常按既有owner映射，不允许自由成功枚举。所有private虚方法为const，不在片段准备中发布，完整签名如下（`准确值`形参为 `const variant<int64_t,特征值身份>&`）：

| 接口 | private 方法签名 |
| --- | --- |
| 内容 | `定位参与者结果<L1三分区原子参与者写集_v2> 准备定位内容(const 定位特征创建请求&,uint64_t Gread) const` |
| 内容 | `定位参与者结果<定位内容见证> 读取定位内容(uint64_t Gread,uint64_t H,稳定编码 F) const` |
| 内容 | `定位参与者结果<vector<稳定编码>> 查询定位准确候选(uint64_t Gread,uint64_t H,稳定编码 FT,const variant<int64_t,特征值身份>& 准确值,uint64_t 最大候选数) const` |
| 已知 | `定位参与者结果<L1三分区原子参与者写集_v2> 准备定位已知(const 定位特征创建请求&,uint64_t Gread) const` |
| 已知 | `定位参与者结果<定位场景已知见证> 核验定位场景与父已知(uint64_t Gread,uint64_t H,稳定编码 C,optional<稳定编码> 父F,const 定位特征预算&) const` |
| 已知 | `定位参与者结果<定位关系见证> 读取定位已知(uint64_t Gread,uint64_t H,稳定编码 C,稳定编码 F) const` |
| 组织 | `定位参与者结果<L1三分区原子参与者写集_v2> 准备定位组织(const 定位特征创建请求&,uint64_t Gread) const` |
| 组织 | `定位参与者结果<定位父路径见证> 核验定位父(uint64_t Gread,uint64_t H,稳定编码 C,稳定编码 P,uint64_t 最大路径长度) const` |
| 组织 | `定位参与者结果<定位路径见证> 读取定位路径(uint64_t Gread,uint64_t H,稳定编码 F,uint64_t 最大路径长度) const` |

内容调用当前特征类型、值解析和历史准确读取，查询本身不计观察。已知复用存在当前/历史身份及已知准确窄读，只接受唯一真实关系。组织复用既有场景角色、根、扩展登记恢复及全局类型索引；无路径返回存量未定位，不能另造关系类型。三个准备结果H固定为请求G0；其它读取按形参H。底座/端口只受保护同步访问，各owner私有mutex在每次返回前释放。

内容片段固定参与者1，owner=特征信息分区，期望G0、内容键。本地键：1新普通F；2为F到原信息锚点的原归属关系；3为F到FT的原准确类型关系；4为准确值事实；必要当前槽按原 `加值` 实际形状原样形成。属性类型和表示选择复用旧创建路径，值事实所属及来源均为新F，节点/关系/值角色不改义，输入引用仍复制完整I64，不把值身份写成整数内容。单个本地键在不同事实类别间也唯一。

已知片段固定参与者2，owner=存在，期望G0、已知键；本地键1为 `C -> {参与者1,本地节点1}`、原特征成员关系类型、角色1。不写当前采用，不要求尚未出生F在G0已存在；验证的是C真实有效和提交后正确新F引用。

组织片段固定参与者3，owner=场景，期望G0、组织键；本地键1为 `P -> {参与者1,本地节点1}`、已登记特征组织关系类型、角色1。C/P必须在G0真实合法、P路径深度+1不超预算，父F须C已知。新F为本次新局部节点，原来没有入出边，天然不是孤立子树；不把已有任意节点作为新F局部引用。

三份原始片段组成 `L1三分区原子事务请求_v2{2,G0,组合键,三份片段}`；以内容端口调用现v2方法，传已知/组织端口。参与者1/2/3正好三份且owner互异，每份有实际变化，后两份只引用前序新F，满足L1既有引用顺序和全局闭包。单owner方法不得另行提交同一片段。

## 6. 原键重放、首次创建和复用

入口先形状检查，缺C/P必须在任何provider写之前结束。取实际Gread；G0>Gread漂移。所有首次已有端点以G0为H读取，读时Gread保持一致；不将G0覆盖为freshG。各owner准备必须支持历史G0，包括后来已退出的C/P/FT，不能在重放前强制它们仍当前。历史材料被清理明确失败，不猜恢复。

先查三个owner内部原键首次材料（由组合核心经private端口调用既有读取首次写入材料，不增公开首次查询）：全无时才是首次；一项/两项存在或三个首次发布H不同为内部不一致，禁止补齐。三个存在时先拒绝非空指定F，再由三个原请求片段与L1首次规范化材料及映射完整互证，直接重送原组合请求，由L1组合账决定精确重复或幂等冲突；不要依据当前同值查询或当前父路径重决策。首次记录存在但组合键不同由L1拒绝，不单独重放owner片段。

若全无且Gread不等于G0，返回漂移零写。Gread=G0时核验位置、正式FT/值和预算，再按准确内容查询全部候选。指定F只允许属于此完整候选组；按实际路径分类：同C/P且C已知F可以复用，异C/P位置冲突，无路径存量未定位，损坏路径内部不一致。未指定且多于一项精确候选返回需选择F，不自动按位置过滤到首项；一项按上述规则，零项才建立新F。准确相同但位置不同不隐式复制。复用不写出生事务、不新增组织或已知边；缺已知关系明确失败，由外部原有存在操作修复后新请求重试。

首次新建重新同G核验三片段并提交。前置拒绝、L1交换前失败均零F/零两边/零首次账。提交后确认三份映射和共同G1=G0+1，再以实际新Gread及H=G1分别读取内容、已知、组织历史及原C/P路径；一致才返回已创建。结果不确定返回已可能发布和原请求/已知G1；即使异常也不回滚、不换键、不独立补边。重放成功按首次G1读回而非把后来的治理变化映射进首次结果。

读取定位特征先读准确内容，再通过场景路径得到自身C/P及正式组织边，再核验C已知F。原生F出生必须三事实创建代次同G1；已明确迁移的存量允许旧出生H与后来组织H不同，并按实际事实报告，不重释出生代次。无路径返回存量未定位，不能包装成已定位。

## 7. 概念报告与消费者迁移

`特征概念观察请求` 必加 `定位特征创建请求 定位`，原观察中的FT/准确值/指定F须与定位请求逐字段一致。构造签名改为 `特征概念应用服务(特征类数据服务&,概念树类数据服务&,定位特征数据服务&)`，在原 `concepts.使用特征服务(features)` 核验之外必须用定位服务的 `使用内容参与者(features)` 比较实际同一实例；三参与者同底座由定位构造保障。不在应用新增L1。增加只读装配谓词 `bool 使用定位参与者(const 定位特征已知参与者&,const 定位特征组织参与者&) const noexcept`，仅转调定位服务的两项对象地址检查。处理首先调用定位创建/复用，在完整成功之前不调用类型观察登记或概念形成。原无父 `创建准确特征` 声明/定义从特征类物理删除，没有默认父重载或fallback。

`特征概念处理结果` 添加 `optional<定位特征结果> 定位结果`，沿原完整概念/名称字段报告最终概念身份及定义。新增状态 `定位拒绝=11`、`定位待原请求确认=12`、`准确F已定位概念未完成=13`（尾部追加，不复义原值）；原成功“完成”必须定位事实完整且概念、名称指向同一实际F/最终概念，概念C与场景C严格分字段。已有概念处理的技术原因枚举可用，定位原因为 `optional<定位特征状态>`，不压成未找到。

定位失败返回原定位请求及真实原因，概念/观察载荷空。定位已可能发布必须先原请求收敛，不能进入观察。定位成功但概念失败保留定位事实、准确F、已发布观察和原概念待确认请求，报告准确F已定位概念未完成；重建应用后以调用方保存的完整原观察+定位请求及既有观察键恢复。定位原键固定，概念后续使用自己的合法G，不用出生G强迫全部阶段原子。完成结果不能只有F或FT而遗漏概念事实。

场景成员应用输入中的实际场景必须与定位C一致，组织父显式传递，不从成员E/概念父推断P；其特征请求及结果组合沿现服务，场景成员应用构造签名保持现有五参数，并追加 `features.使用定位参与者(existences,scenes)` 检查，证明定位中的存在/场景正是当前消费者借入实例。存在概念应用只消费已定位准确F与概念，不新增另一创建链。扫描所有旧创建调用，当前唯一生产点和 `验证工具/场景特征实例组织专项验证.cpp` 同次迁移。已有场景组织专项的独立新叶用三分区定位创建得到，既有组织行为测试使用起点旧模型的确定存量fixture：测试装配期以唯一L1端口按起点创建路径的实际字段/关系形状写入无父F，再将同一端口move交真实特征provider，当前新模块不得提供该写入口；fixture无第二端口、无整数伪造身份，记录其仅代表旧存量。不能通过公开无父创建恢复旧生产入口。

场景组合的恢复与返回同步闭合：`场景成员概念继续材料_v2` 尾部增加 `optional<定位特征结果> 已确认定位`，特征阶段取得完整定位成功后保存；重建对象继续时将其传给下述双参特征收敛入口，未知发布材料不伪装成功回执。`场景组合投影_v2` 增加必需 `定位特征事实 定位`，完整谓词必须互证定位F与投影F、定位场景与S、根路径/已知关系及H读取。`场景特征概念结果_v2::成功` 还须其处理结果定位完整并与已确认F一致。场景状态尾部追加 `定位拒绝=22`、`定位待原请求确认=23`、`准确F已定位概念未完成=24`，映射子结果三状态且保留原完整子载荷；需选择F仍映射原需选择准确F并保留全部候选。不能把这些状态降成内部不一致或已完成。现版本2保持，旧调用缺新增必需定位输入时入口拒绝；无隐式默认语义。

`读取已发布类型观察` 等返回完成的只读路径同样以调用方Gread和该路径的正式H读取定位事实并互证观察F；预算沿请求定位预算。未归树只返回存量未定位的具名原因，不补组织、观察或概念。只读名称查询可继续作为准确内容审计，但不据其返回宣称完成定位创建。

存量无父F不批量推断位置、不删除；正常创建/复用返回存量未定位，调用方显式C/P并先按7150建立已知后走原4205组织入口，位置核验通过才可使用新应用。已有其它位置的F只能按原使用引用共享，调用创建接口要求错误自身位置时失败。当前不增加换父/解组织/级联删除。

零出生写的已复用结果不绑定四键，因此跨调用恢复不能把原G0请求当作已发布创建重放。追加公开重载 `特征概念处理结果 收敛类型观察(const 特征概念观察请求&,const 定位特征结果& 已确认定位)`：先逐字段核验保存结果原请求与本次定位输入相同，以实际Gread和保存成功结果的Gread为H调用定位历史读取，核验F/FT/准确值/C/P/两边/出生H全部一致，才恢复已定位阶段并按原观察键继续概念。该重载不产生出生写、不修改原键、不接受调用方未经provider验证的成功标记。保存的创建成功回执同样可走此路；只有已可能发布回执时必须原创建键收敛，不能跳过出生确认。无保存回执且零写复用无法在新G恢复时返回具名材料缺失，不猜新F。原单参收敛入口保留用于已绑定创建键和现有对象已确认状态，不谎称可恢复已丢弃回执。

## 8. 验证矩阵与完成界限

真实L1运行包、三个真实owner、真实概念服务和新组合服务，全部绑定同一底座；不以map/mock证明共同发布。新增仓库验证工具、工程、运行脚本，输出仅 `D:\TEMP\海中鱼巣\FEATURE-PARENT-BOUND-CREATION\<执行任务>`。

1. 缺C/P、零身份、非法键、预算及类型无效：调用前后G/节点/两关系/观察/概念/首次账相同，缺父必须零身份分配。
2. 根父及已有父F成功；F/已知/组织三映射同G1，F内容不变且当前采用零变化；完整位置与概念读回。
3. 错C/P、父未知/退出/未归树/跨根/超深度拒绝；候选同值异位置冲突、多候选完整需选择、同位置复用零出生写。
4. 三底座或owner不一致构造拒绝；private接口/raw端口普通应用不可调用的编译负例，不存在普通通用提交入口。
5. 原键同义重放、异值/父/场景/FT/键冲突，G漂移、并发同G仅一次发布；每个交换前失败无半F、半已知、半组织。注入结果丢失后原请求收敛，其它事实推进G后仍按首次H重放；当前组织边存在时C/P退出应被引用保护拒绝，不用强制退出构造不可达夹具。
6. 概念失败保留已定位F、具名未完成；后续原观察键续办不新F、不重复观察；完成必须报告最终概念而非FT/F。缺定位时概念总写数为0。
7. 存量无父明确拒绝新完整结果，显式迁移可读实际出生/组织两截止；不自动猜父/复制/删除。原组织/存在当前采用/共享引用回归。
8. 旧无父创建声明/定义/调用全仓归零；三个owner不新签发、不泄漏公开端口，L1生产文件零差异；工程唯一模块登记、循环import检查。

隔离Debug/Release完整构建与运行，各关键分支实际断言。根工程分别构建并与起点的已知范围外失败对比；不以历史成功代替本版结果。strict及diff检查运行；外部固定manifest故障精确记录，不将失败记通过。故障注入若没有合法触发器，明确NOT_RUN并不得声明其运行验证已完成。

完成至少：本计划全部代码/消费者迁移、真实共同发布/读回、缺父零写和概念报告路径、双配置专项及记录发布。生产采样、所有存量自动迁移、概念识别质量、跨进程恢复、正式集成验收均不在本轮声明。
