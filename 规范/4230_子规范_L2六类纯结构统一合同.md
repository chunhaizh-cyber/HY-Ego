# 4230 子规范 DATA-L2六类世界强类型结构统一合同

编号：4230
日期：20260909
版本：v0.41
状态：正式子规范

直接上级规范：0050。

直接依赖规范：4010、4015、4030、4190、4200。

相关边界规范：0500、1120—1190、2100、4201、4210、4220、7150、ACCEPTANCE-01。

## 1. 定位

本规范冻结 `DATA-L2` 对场景、存在、特征、状态、动态和因果六类世界强类型结构的唯一所有权、共同 DTO、生命周期、依赖方向、事务、读取和退出边界。现行 C++ `L2*` 类型与服务名称保持真实 ABI，不因架构层显示名机械改名。六个公开服务内部共有场景、存在、特征定义、特征实例、状态、动态和因果七个节点族；每个节点族具有独立的 `DATA-L1` 技术 owner 和不可由普通节点形状替代的正式身份来源。七族的当前结构身份与适用历史身份来源见证按第 2.4 节分为不同公开合同；这一分流不收缩非世界节点族或 DATA-L3 聚合使用的完整见证。

在全局命名空间中，六类结构属于 `ARCH-L2`，与 `DATA-L2` 一一映射；场景树和概念空间聚合属于 `ARCH-L3/DATA-L3`。本规范只治理世界结构，不把所有名称含“存在、状态、动态”的上层业务记录并入世界结构 owner。后继任务、方法、概念和自我需要世界材料时均经公开强类型接口引用本层。任务正式虚拟存在 `Vt` 及其任务状态、状态迁移记录属于 `ARCH-L4` 任务域：`Vt` 是任务特征、任务状态和状态迁移记录的唯一宿主，但不是本规范的世界存在，不进入世界树，也不形成世界存在、状态或动态的第二权威。

六类世界结构不等于全部 `DATA-L2`。0050 与 4030 冻结的 DATA-L2 穷尽面共含十一节点 owner 和一个概念名称关系 owner；本规范只承接其中七个世界节点 owner。语言、词条、概念、不可变材料及概念名称关系由 4240 另行承接；语素和词性当前不形成持久结构族。需求、任务和方法属于 ARCH-L4，现有 `L2*` 名称只作兼容 ABI；这些结构既不改写本规范六服务职责，也不形成第七个“世界”数据服务。

六类名称具有结构类型语义：`DATA-L2` 知道节点和关系属于哪个正式世界结构族，并校验相应字段、端点、基数和生命周期；这不授予其判断现实世界、存在资格、特征意义、状态真实性、变化重要性、动作归因或因果真伪的权力。业务调用方位于 `DATA` 之外，先完成业务裁决，再提交本规范允许的完整强类型结构请求。

具体操作语义由各根 / 子规范承接：存在按1140/7150，特征按1130/4110，状态逻辑字段按1160，动态逻辑结构与操作语义按1170/4211。状态类与动态类的精确C++ DTO、内容操作、专用结果和成功谓词均尚未由专属物理合同冻结，因此本规范不授权实施者自行补造。第2.4节既有共同身份操作不等于内容CRUD已经冻结。后继实现不得建立第七个“世界”数据服务、第二写入入口或兼容业务解释层。

### 1.1 存在、特征值与特征信息的对象边界

1140/7150的存在信息为稳定节点、特征引用及可选同节点场景展开；1150/4202固定场景七字段和四个实际实例根。2100值对象为三字段，1130/4110准确F为身份、类型、固定值及可选概念名称四字段。名称权威归4240；共同技术头、代次、幂等和生命周期不成为这些对象字段，可靠性继续由L1及结构组合提供。

特征以独立准确节点定位，存在引用并维护当前采用。旧宿主实例槽、旧五字段、F当前值改写、历史简化及三次建子合同均不作为现行生产入口；禁止双轨写入或把旧DTO重释为新四字段。4110和本规范第12—13节保护旧材料；实际代码及数据迁移须独立完成。

### 1.2 技术分区与实现边界

现有名为“特征实例”的节点族身份、技术 owner 或 L1 端口名称只说明既有内部结构分区，不证明旧宿主槽模型继续有效。本次不新增、合并或重编号这些分区，不原地改义旧记录；独立节点的正式结构读回按 1130 / 4110，不能仅凭旧身份确认成功就认定新字段完整。

准确F操作按4110，FT来源与域规则按1120/4120，比较按4180；7160应用负责类型共享积累和概念形成，不在F数据CRUD内部执行。底层映射及替代函数须有完整合同，不从旧DTO回填已退出字段或恢复旧入口；其它对象及比较合同不因此整体退役。

### 1.3 新场景目标与旧物理ABI的适用分界

场景目标以1150/4202为准：同节点存在/场景视角、四个实际实例根、直接成员列表及子场景递归；场景类维护角色、根和组织/场景关系，各实例类保留内容及来源写入，存在类保留已知/采用和组成关系，不新增独立owner或第二内容入口。

第2节既有场景/存在强类型及族来源、第4.1节旧五项场景DTO、R1根标记/树归属/裸场景规则和场景宿主关系，以及第5、8节相关依赖/验证条款，只在原有物理格式与兼容读回范围保留，不证明新目标已经实现。旧两身份必须不同、两族独立来源或非根独立场景父边，不再是新同节点角色的业务定义；不可强转、重编号、双登记同码或按名称合并旧对象。

新完整场景不使用旧固定属性组补出七字段，不将技术封套搬回场景对象，不以旧目录冒充实际根。后继须具名冻结节点身份/角色到内部结构分区的唯一映射、公开DTO、四根原子创建、组织边、共享引用、跨角色归属守卫、退出及恢复迁移。旧代码和存量事实不因规范发布被自动修改或清除，未迁移旧调用不得被报告为新模型能力；本节不重开C038。

## 2. 共同身份、枚举与请求结果头

### 2.1 强类型身份

合同版本固定为 `1`。以下身份均为彼此不同的中文强类型，内部只承载一个非零 `稳定编码`，不得隐式转换、按名称推断或跨类型替换：

```text
L2场景身份
L2存在身份
L2特征定义身份
L2特征实例身份
L2状态身份
L2动态身份
L2因果身份
L2场景树身份
```

`L2场景树身份` 只在场景结构内使用，值等于该树根场景的稳定编码；它不增加第八节点族、独立树节点、独立 owner 或第七世界服务，也不得隐式转换为普通场景身份。建立根成功后由服务返回该身份，调用方缓存或函数参数本身不构成身份事实。

写请求使用独立强类型 `L2结构幂等身份`，其值为非零 `uint64`。它只标识一份完整 L2 结构请求，不是任一结构事实、业务对象或 L1 事实编码。

### 2.2 读取类别与结构状态

```text
L2读取类别：
  当前=1
  历史=2

L2结构状态：
  已提交=1
  精确重复=2
  已读取=3
  未实现=4
  入口拒绝=5
  许可拒绝=6
  未找到=7
  已退出=8
  属性未设置=9
  事实代次漂移=10
  幂等冲突=11
  引用冲突=12
  资源失败=13
  内部不一致=14
```

`已提交`、`精确重复` 和 `已读取` 通常是携带完整成功载荷的状态；登记读取 ABI 是明确例外：公共 `已读取=3` 与专用 `待迁移=4` 时载荷为空且成功否。其它状态不得携带部分结构、默认补齐结构或混代材料。任何单一公共状态都不得独立推出成功，成功性必须由公共结果头状态 × 专用状态 × 载荷组合共同判定。`未实现` 只供 4201 的最终合同骨架使用；真实入口完成后不得用它掩盖未覆盖分支。

合同版本不是兼容猜测字段。入口只接受版本 `1`；不兼容变化必须建立新的具名合同与 DTO 身份，并明确迁移或拒绝关系。

`L2读取类别`仍适用于本规范其它明确具有当前 / 历史双分支的完整读取合同；它不得出现在第 2.4 节七族当前确认的输入或返回、也不得出现在历史身份来源见证合同中，更不得借该枚举保留旧共同身份来源读取的当前分支。

### 2.3 共同请求头与结果头

`L2结构请求头` 固定包含：

```text
合同版本
期望事实代次
```

写请求还必须包含绑定完整请求语义的 `L2结构幂等身份`。期望事实代次用于写前基线校验和同截止读取；不匹配时返回 `事实代次漂移`，不得返回混代载荷或产生数据变化。`事实代次漂移` 明确证明该完整请求没有取得首次结果、没有形成任何权威发布且没有绑定幂等身份；调用方重新正式读回后可以保留同一幂等身份并形成一份新完整请求。除此之外，资源失败、异常或发布结果未知均不能据此改写请求。

`L2结构结果头` 固定包含：

```text
合同版本
L2结构状态
事实截止代次
可选变更事实代次
```

成功读取的事实截止代次必须非零。成功写入的变更事实代次是本次原子发布的新代次；精确重复返回首次请求的原结果，不再次推进代次。交换前候选验证或提交失败才承诺整笔零写、零幂等账和零代次变化。L1 已确认提交或精确重复后的独立读回失败不得回滚；已形成事实、幂等账和变更事实代次必须保留，结果固定映射为专用“已可能发布”与公共 `内部不一致`，调用方以原幂等身份重放并经正式读回收束；登记 ABI 是例外：登记写/读回失败固定为公共 `内部不一致=14` + 专用 `内部不一致=9`，仅四字段提交见证、完整登记事实为空且成功否，不改回“已可能发布”。任何单一公共状态都不得独立推出成功，成功性必须由公共结果头状态 × 专用状态 × 载荷组合共同判定。没有发生变更的交换前结构化非成功不伪造变更事实代次。

### 2.4 七族 owner、当前确认与历史见证

七个节点族各有一个独占内部结构分区：场景、存在、特征定义、特征信息、状态、动态、因果；既有owner两两不同且不重编号。特征结构服务分别持有定义和信息端口，类型定义写入前者，准确F身份/类型/值写入后者。F到C名称逻辑字段由4240概念owner的唯一关系提供，不由特征端口跨写，也不新增owner。名称含“实例”的兼容身份不恢复宿主槽；普通调用者不得选择、交换或转签内部端口。

普通应用的唯一 provider 使用同一个 `L1事实基座运行包` 和唯一签发器，按“存在、特征定义、特征实例、状态、动态、因果、场景”的顺序建立 owner 并移动注入唯一服务实例。尚未进入有效实施范围的后继服务可以暂不构造，但已构造前缀不得改序、复用建立幂等身份或先交付服务再补 owner。该顺序只约束上层装配，不给 L1 核心增加 L2 枚举、分支、friend 或反向 import。

L1 owner 是服务内部身份来源校验的第一层证明，但不能把同 owner 的关系类型节点、属性类型节点或内部登记节点冒充公开 L2 身份。每个节点族还必须在本族 owner 内建立一个不可变族锚点节点和一个不可变族归属关系类型节点。两者由本族服务的 provider 私有初始化路径在首个公开节点创建前建立并从 owner-aware 入口读回；定位副本只作非权威查找材料，丢失时不得扫描、猜测或补造。

每族初始化使用只属于该 owner 的固定非零建立幂等身份和完整写集幂等身份；七族数值域不得复用。精确重复必须回读首次锚点和关系类型编码，同键异写集返回幂等冲突。一个 owner 当前只能有一组来源登记；再次形成不同锚点、不同关系类型或第二活动登记属于内部不一致。锚点和关系类型的创建代次必须不晚于首个公开节点创建代次，生命周期覆盖全部本族公开节点及归属关系；它们不得在仍有当前公开节点、归属关系、未收敛幂等结果或历史 / 恢复引用时退出。

每个公开节点创建必须在同一 owner-scoped 写集内同时建立唯一族归属关系：

```text
源节点 = 本次公开节点
目标节点 = 本族族锚点节点
关系类型 = 本族族归属关系类型节点
角色或顺序 = 1
关系 owner = 节点 owner = 关系类型 owner = 族锚点 owner = 本族 owner
```

该关系不是节点到自身的自环，也不是业务拓扑、标签、索引、缓存或第二身份仓。服务必须先在内部确证精确本族 owner 事实、公开节点、指向精确本族锚点的唯一归属关系和精确本族关系类型。既有完整创建、完整读取、场景树和其它结果继续按各自合同决定是否及如何携带完整见证；本项只分离七族“当前确认”与“历史身份来源见证”，不收缩任何既有完整结果。普通稳定编码、强类型包装、节点种类、名称、本地键、属性 / 关系数量、节点到自身关系、缓存定位或 v1 无 owner 投影都不能单独证明任一 L2 节点族。

族归属关系固定只从公开节点指向锚点；锚点不反向指向成员，成员之间不因族归属增加边。场景、存在、特征、状态、动态和因果的业务 / 结构引用继续服从第 5 节单向依赖，因而身份来源不增加服务调用边、不形成同层循环依赖，也不要求低层服务反查全部消费者。

`L2结构公共.数据.h`拥有闭合枚举 `L2节点族`和完整值式 `L2节点族身份来源事实`。后者固定包含：节点族、节点稳定编码、族锚点稳定编码、族归属关系类型稳定编码、族归属关系稳定编码、角色或顺序，以及节点、族锚点、关系类型和族归属关系四份生命周期；`L1结构所有者身份`及其生命周期仍只用于服务内部校验，不进入该 DTO。该 DTO 在既有完整创建、完整读取、DATA-L3 聚合或其它已冻结结果中的使用，继续由各自合同规定；作为七族历史见证的载荷时也必须保持完整，不得收缩、改名或伪装成当前确认结果。共同见证不公开端口、owner 选择、L1 本地键或写集，也不提供通用跨族读取服务。

每个七族公开服务都必须提供一个 `确认当前{族}结构身份` 的 const 当前确认入口。其请求输入严格只有非零 `G0` 的 `L2结构请求头`和对应本族强类型身份；它直接返回既有 `L2结构结果头`，不建立当前 request/result wrapper、当前 DTO、optional payload 或身份回显。成功只在结果头合同版本正确、状态为 `已读取`、变更事实代次为空且事实截止代次精确等于请求 `G0` 时成立。成功的含义仅为：“该请求身份在该 G0 已由本族 provider 通过结构当前性确认”；它不向调用方返回节点、锚点、关系类型、归属关系、角色 / 顺序、生命周期或内部 owner。

当前 provider 必须在一次 owner-aware 一致当前投影中确认目标节点、族锚点、归属关系类型、唯一归属关系和内部 owner 均在同一 `G0` 当前。不得从不同代次拼接、按最大代次或空退出集合扫描、由结果头反向猜测当前性、读取时补偿写入或把状态当前选择、动态内容、特征当前值当作结构身份当前证明。状态或动态的结构身份当前不等于其内容或使用关系当前。当前确认不得接受 `L2读取类别`、历史截止或完整见证；当前非成功只返回该既有结果头，零公开变化。

适用的历史、退出、恢复和审计路径必须使用与当前确认不同的、按族具名的历史身份来源见证合同。它的输入由非零 `Gread` 的 `L2结构请求头`、本族身份和非零 `H <= Gread` 组成；该合同不接受 `L2读取类别`，不得提供当前分支。结果固定回显 `历史截止事实代次=H`，并只在结果截止精确等于 `H` 时返回完整 `L2节点族身份来源事实`；`成功()`必须同时检查回显 H、结果头截止和 `L2节点族身份来源事实完整(payload, H)`三者相等 / 完整，隐藏晚于 H 的未来退出。H 回显是历史结果与其事实截止的值式自证，不是第二个可独立解释的代次或领域字段；不得删除、改作可选字段或仅要求调用方把请求 H 与结果头事后比较。普通退出结果不因此被强制携带共同见证，状态 / 动态墓碑、TTL、清理顺序、当前选择、legacy 和双 owner 边界仍完全留给第 10 节及 P08。

七个唯一当前公开 const 函数固定为：

```text
L2场景结构服务::确认当前场景结构身份
L2存在结构服务::确认当前存在结构身份
L2特征结构服务::确认当前特征定义结构身份
L2特征结构服务::确认当前特征实例结构身份
L2状态结构服务::确认当前状态结构身份
L2动态结构服务::确认当前动态结构身份
L2因果结构服务::确认当前因果结构身份
```

七个当前确认函数固定采用下列 C++ 形状，不增 request / result wrapper、DTO、payload 或 overload：

```cpp
L2结构结果头 确认当前{族}结构身份(
    const L2结构请求头& 请求头,
    L2{族}身份 身份) const;
```

七个历史见证的 C++ 类型、字段、函数名固定如下；`{族}`只代表表中的一个本族强类型，不建立通用跨族请求或服务：

```cpp
struct L2{族}身份来源历史见证读取请求 final {
    L2结构请求头 请求头;                 // 非零 Gread
    L2{族}身份 身份;
    std::uint64_t 历史截止事实代次 = 0; // 非零 H，且 H <= Gread
};

struct L2{族}身份来源历史见证读取结果 final {
    L2结构结果头 结果头;
    std::uint64_t 历史截止事实代次 = 0; // 成功时与结果头截止相等
    std::optional<L2节点族身份来源事实> 身份来源见证;
    bool 成功() const noexcept;
};
```

| 服务 | 历史请求 | 历史结果 | 唯一公开 const 函数 |
| --- | --- | --- | --- |
| `L2场景结构服务` | `L2场景身份来源历史见证读取请求` | `L2场景身份来源历史见证读取结果` | `读取场景身份来源历史见证` |
| `L2存在结构服务` | `L2存在身份来源历史见证读取请求` | `L2存在身份来源历史见证读取结果` | `读取存在身份来源历史见证` |
| `L2特征结构服务` | `L2特征定义身份来源历史见证读取请求` | `L2特征定义身份来源历史见证读取结果` | `读取特征定义身份来源历史见证` |
| `L2特征结构服务` | `L2特征实例身份来源历史见证读取请求` | `L2特征实例身份来源历史见证读取结果` | `读取特征实例身份来源历史见证` |
| `L2状态结构服务` | `L2状态身份来源历史见证读取请求` | `L2状态身份来源历史见证读取结果` | `读取状态身份来源历史见证` |
| `L2动态结构服务` | `L2动态身份来源历史见证读取请求` | `L2动态身份来源历史见证读取结果` | `读取动态身份来源历史见证` |
| `L2因果结构服务` | `L2因果身份来源历史见证读取请求` | `L2因果身份来源历史见证读取结果` | `读取因果身份来源历史见证` |

历史请求有效当且仅当公共请求头合同正确、`Gread != 0`、本族身份非零、`H != 0`且 `H <= Gread`。历史结果成功当且仅当结果头合同正确、状态为 `已读取`、结果头截止和回显 H 同为非零且相等、变更事实代次为空、见证存在、见证节点族恰为本族且 `L2节点族身份来源事实完整(*身份来源见证, H)`。任一非成功见证为空且变更事实代次为空；已通过请求形状检查的历史结果仍回显请求 H，形状检查失败则 H 为零。当前确认成功当且仅当结果头合同正确、状态为 `已读取`、事实截止代次精确等于输入 G0 且变更事实代次为空；任何当前非成功只返回该既有结果头。

七族当前确认和历史见证按下列阶段裁决；前一阶段已经得出结果时不得为寻找后续分类再读取、补偿写入、历史回退或猜测。资源或异常在其发生的阶段立即映射，不能伪造后续语义结果。

| 阶段 | 当前确认 | 历史见证 | 固定结果 |
| --- | --- | --- | --- |
| 1. 请求形状 | 合同版本错、G0 为零或本族身份为零 | 合同版本错、Gread / H 为零、H > Gread 或本族身份为零 | `入口拒绝`；无载荷、无变更，结果截止和历史 H 均为零 |
| 2. 一致性守卫 | 不能取得精确 G0 | 读前或读后不能保持精确 Gread | `事实代次漂移`；无载荷、无变更，结果截止只可回显实际已观测代次 |
| 3. 当前目标存在性 | 当前目标编码不存在 | 不适用 | `未找到` |
| 4. 当前退出 | 当前目标已退出 | 不适用 | `已退出`；本阶段在当前入口先于错族、裸节点或保留元节点分类，且不得为重新分类回读历史 |
| 5. 历史目标存在性 | 不适用 | H 早于目标创建，或目标编码不存在 | `未找到` |
| 6. 身份可接受性 | 当前存在且未退出，但属于另一 owner、无本族唯一归属、为 legacy / 裸 L1 节点，或为本族保留锚点、关系类型、关系 / 属性元节点 | 在 H 可见但不属于本族的同类对象 | `入口拒绝` |
| 7. 已接受身份的来源闭包 | 已接受本族目标后，owner、锚点、关系类型、唯一归属关系、方向、角色、生命周期或同截止闭包缺失、重复、错配或混代 | 同左，按 H 的历史闭包判断 | `内部不一致` |
| 8. 完整见证 | 不适用 | 目标在 H 前已退出，但第 6—7 阶段完整 | 仍为 `已读取`，返回完整见证并保留 H 可见的退出生命周期；不得改报 `已退出` |

对已通过第 2 阶段的语义结果，当前结果头截止固定为 G0，历史结果头截止固定为 H；在第 2 阶段之前发生的资源失败或内部异常截止为零，之后发生的资源失败或内部异常保留已确认的 G0 / H，但始终空载荷、空变更。`std::bad_alloc`、`std::length_error`及等价资源不可用映射 `资源失败`；未预期异常、L1 返回形状 / 合同矛盾、provider 路由或装配矛盾映射 `内部不一致`。这些规则是本七族合同对 4050 通用分类的具体化；尤其当前 `已退出`优先于当前错族分类，历史从不因 H 前退出返回 `已退出`。

任何公开源状态不得同时保留新当前确认与旧 `读取{族}身份来源`的请求类型、结果类型、公开函数声明或定义；不得保留结构化拒绝壳、转发、别名、兼容开关、隐藏 overload、第二当前 DTO 或第二当前投影。七 owner 两两不同及同一稳定编码不能被两个族来源同时接受，仍是 provider 装配与生产外集成不变量，不要求任一单族服务扫描其它族。

## 3. 共同值式 DTO

### 3.1 有序引用

`L2有序身份引用` 固定包含非零顺序和目标稳定编码。同一角色组按“顺序、目标稳定编码”升序形成唯一稳定投影；顺序相同且目标相同的重复项、零顺序和零目标在写前拒绝。

有序引用只表达结构角色和顺序，不证明目标的业务资格、动作成立或因果真实性。

### 3.2 属性提交项与属性事实

`L2属性提交项` 固定包含：

```text
属性类型身份
原始值材料
来源稳定编码
```

原始值材料只能是下列四类之一：

```text
I64
I64组
U64组
独立材料引用
```

不得接收通用 JSON、裸字节、字符串拼接、无类型联合容器或调用方指定的 L1 当前值编码。材料必须与属性类型允许的表示种类匹配；L2 只校验类型和结构，不解释单位、轮廓、颜色、位置、业务时间或其它业务意义。

`L2属性事实` 固定包含：

```text
属性类型身份
值稳定编码
类型化不可变材料
来源稳定编码
创建事实代次
可选退出事实代次
```

属性事实是调用方拥有的完整值式投影，不泄漏 L1 属性槽对象、内部材料路径或可变值对象。无显式顺序的属性组按“属性类型身份、值稳定编码”升序返回。

### 3.3 生命周期

`L2生命周期` 固定包含非零创建事实代次和可选退出事实代次。退出代次存在时必须大于等于创建代次；已退出事实不得复活、原地覆写或重新成为当前事实。当前入口不返回已退出事实，历史入口可以返回其完整生命周期。

### 3.4 完整投影边界

结果只返回适用根 / 子规范规定的完整值式事实。存在和特征对象使用第 1.1 节的字段边界；第 2—3 节技术封套不是其第二字段定义。可靠读取在内部保证节点、关系和值属于同一一致性边界；不得用当前事实补历史缺项，也不得用默认值、缓存、SQL、材料目录或其它代次记录补齐结构。

L2 DTO 不得携带 L1 本地键、中性写集、仓库、锁、事务、候选、撤销权、SQL、缓存写权或材料内部路径。

## 4. 六服务、七族唯一所有权与事实形状

### 4.1 场景结构（旧ABI限域保留，新字段见1.3）

唯一所有者为 `L2场景结构服务`，DTO 物理所有者为 `L2场景结构.数据.h`。场景节点族只使用场景 owner 和场景端口；全部完整读取必须在同一截止闭合场景身份来源。

`L2场景父子关系事实` 固定包含关系稳定编码、父场景身份、子场景身份、非零顺序和生命周期。一个当前子场景至多有一个当前直接父场景；父子关系不得形成结构环。没有当前父关系的场景只是结构根候选，不因此取得现实世界或业务根资格。

`L2场景成员关系事实` 固定包含关系稳定编码、场景身份、存在身份、非零顺序和生命周期。对同一存在，全部场景成员关系与存在组成父关系合计至多一条当前直接归属，不按场景、树或关系类型分别允许；该关系只表示结构成员，不判断业务真实性。写入和替换必须联合校验，完整包含路径无环；准确语义按 7150。

旧格式`L2场景事实`包含场景身份、生命周期、完整固定属性组、完整直接子场景关系组和完整直接成员关系组，关系组按共同有序引用规则排序。这五项仅为旧DTO布局，不再是新场景字段定义；1150七字段与4202实际根不能由该布局推定完整。

场景服务唯一负责场景节点、固定属性、父子关系和直接成员关系的创建、替换与退出，并公开按存在查询场景成员关系的反向值式投影。内部场景中的组成展开不是新的成员关系，读取须区分来源；场景服务不得把组成子存在再次接纳为直接成员。它不得判断现实世界、所在场景、成员资格或建立独立世界登记。

场景 owner 内另有一个不可变树登记锚点、一个根标记关系类型和一个树归属关系类型。三者都是场景服务私有结构元数据，不是公开节点族身份、树身份、业务节点或新 owner；其生命周期覆盖全部具名树、场景节点、根标记、树归属和历史 / 恢复引用。根标记固定为“根场景 -> 树登记锚点、角色 1”，树归属固定为“非根场景 -> 根场景、角色 1”。两类关系不得互换、反向、换端、单独退出或被普通父子 / 成员关系替代。

每个当前已登记场景恰有一份树归属证明：根场景恰有一条当前根标记且没有树归属关系，非根场景恰有一条当前树归属且没有根标记。`L2场景树身份` 等于根场景编码，因此一个底座可以有多个互异树身份；同一场景不得同时成为多树根、同时属于多树或既是根又是非根。根没有当前父关系，每个当前非根场景恰有一条直接父关系，且父、子、树归属目标必须落在同一树；整树从根沿父子关系可达且无环。

建立根必须在一笔场景 owner-scoped 写集中原子建立场景节点、场景族归属和根标记。新增非根场景必须在一笔写集中原子建立场景节点、场景族归属、树归属和指向同树当前场景的直接父关系。成功发布 R1 后，普通公开创建不得再产生缺少根标记 / 树归属的裸场景；旧裸场景在 R1 全部正式场景入口固定拒绝，读取、清退或保码迁移只能由未来独立 legacy 退役合同承接，不得由普通读取、恢复、创建或退出入口自动认领或接管。

场景服务必须公开根、单场景树归属和整树结构的当前 / 历史值式读取。每次成功读取都要在同一截止互证场景 owner、场景族来源、树登记锚点、精确关系类型、关系端点、角色、生命周期和父子同树约束；当前整树按场景身份和关系稳定编码排序，历史读取不得用当前事实补项。场景成员关系与存在端点只表达成员结构，不参与树归属裁决。

场景 owner 另行拥有业务中性的“场景宿主存在关系”结构。关系方向固定为“场景 -> 存在”，只表达该场景由哪个存在持有或作为其内部结构承载；它与“场景 -> 成员存在”不同，不表示该存在位于该场景中，也不改变场景树归属或父子拓扑。每个当前场景至多一个宿主存在，每个当前存在至多被一个场景作为宿主目标。新增、替换、退出、按场景当前 / 历史读取和按存在反向当前读取均由场景服务负责；写前必须以同一期望事实代次分别经场景来源 / 树归属和存在来源证明端点，关系由场景 owner 写入并在发布后同截止读回。L2 不解释哪个场景是“内部世界”、哪个存在是“自我”，也不以宿主关系证明场景成员或业务资格。

### 4.2 存在结构

唯一所有者为 `L2存在结构服务`，DTO 物理所有者为 `L2存在结构.数据.h`。存在节点族只使用存在 owner 和存在端口；全部完整读取必须在同一截止闭合存在身份来源。

存在字段唯一按 1140 / 7150，不保留旧 `L2存在事实` 的生命周期 / 固定属性组作为并列本体。存在服务负责存在节点、自身已知特征引用、当前采用、组成关系及本类具名存在关系的操作；各字段独立读取、关系增删改查及直接 / 递归查询以节点定位。它不改写特征字段，不写场景成员或宿主关系，也不判断存在真实性或业务资格。组成关系方向为父存在 → 子存在，与场景成员关系跨类联合唯一，反向父查询和正向子查询读取同一事实；特征变化、归属替换和内部展开均不更换存在身份。已知引用与当前采用的关系角色、同E同FT基数、目标属于已知、读写及原子组合完整性唯一按7150第2.1节；不得借同类型多项已知F允许多个当前采用，也不得以该约束恢复旧宿主槽。

存在 owner 另行拥有业务中性的“存在单例角色”结构。服务私有登记一个不可变角色登记锚点和一个角色指向关系类型；当前角色事实方向固定为“角色登记锚点 -> 存在”，关系角色字段承载非零且不大于 `INT64_MAX` 的不透明角色键。一个角色键至多指向一个当前存在，同一存在可以承载多个互异角色键。服务公开“原子建立新存在并绑定单例角色”“把既有存在绑定单例角色”、替换当前角色目标、退出当前角色和按角色键当前 / 历史读取；前者必须在同一存在 owner-scoped 写集中建立存在节点、存在族归属和角色关系，绑定只建立角色关系，替换必须同事务建立新关系并退出旧关系，退出只闭合具名当前角色关系。各写入口共享存在 owner 的全局幂等域，同键同完整请求精确重复、同键异义冲突；仍被当前角色关系引用的存在必须由 L1 当前引用保护拒绝退出。L2 只校验角色键、存在来源、唯一性、owner、生命周期和事务，不定义固定角色键，不把任何角色解释为自我、系统身份或项目资格，也不允许名称、配置、位置、顺序或缓存建立角色事实。

“被哪些场景成员关系引用”使用存在 DTO 定义的值式引用组，但只能由场景所有者的公开反向结构查询提供。存在服务不得反向 import 或调用场景服务；调用方需要该组合时分别消费存在事实和场景反向关系投影。

### 4.3 特征结构

特征类是定义与特征信息的唯一结构服务。现有定义与信息分区保持技术隔离，字段和关系在各自范围内组合 L1 写入；普通调用方不选择 owner、端口或 L1 本地键。

定义节点及其正式类型信息按 1120 / 4120，先天 / 后天按来源解释。二次至 n 次的阶次、来源和派生规则继续按 1180；不建立随阶次扩张的新节点族，也不因来源分类改写值持久化边界。

准确F按1130四逻辑字段及4110操作成立，类型和值固定，名称可选；不含宿主字段、历史集合、域、计数或共同技术封套。提供按节点创建、读取、内容查询与受保护删除，不再提供改写类型/准确值的普通setter。存在可以引用同类型多个F，解除或替换采用不删除或改写旧节点。

4125静态一次特征事实集合保留其专属成员和版本合同，沿用原分区；它不是1130四逻辑字段的普通准确F，也不恢复旧槽或旧五字段。

特征内容复用L1通用属性槽和值事实，但不因此变成可变当前值。准确值采用内联I64或2100强类型值引用，完整准确内容成立后固定；缺值不能伪造0或独立值身份。完整带名称的应用视图组合准确F与概念名称公开读取，不产生另一份名称事实；名称缺失与读取失败分开。

### 4.4 状态结构

状态节点族的唯一所有者、固定四项逻辑字段、强时间、引用和实例清理由1160规定。具体世界状态使用由4202以发生场景、被描述存在、正式属性上下文和状态身份组成完整绑定；状态内容与场景组织分属不同owner。

本统一合同只补充以下共同约束：

1. 状态创建只接受调用方提交的正式特征类型、固定准确值和强时间，不接受主体、场景、当前/历史标志、业务owner或用途作为状态字段。
2. 状态内容先由状态类独立发布并完成正式读回，随后才能作为4202完整使用绑定或4211原子动态端点。
3. 当前选择是引用上下文的独立关系，不是状态字段，也不是特征当前值权威。
4. 状态内容不可原地修改；退出和实例物理清理须遵守引用保护、墓碑和紧凑幂等合同。
5. 精确DTO、公开操作和成功谓词须由后继状态类专属结构与操作合同冻结；该合同形成前不得实施或从其它对象合同类推。

### 4.5 动态结构

动态节点族具有唯一动态内容owner。动态信息固定为动态身份、主体存在和变化内容；可选名称概念引用由概念结构owner独立管理，不进入动态数据服务载荷。4211只冻结逻辑结构、操作语义和实施门禁，不宣称现行动态类物理ABI已经成立。

变化内容是以下互斥联合：

1. 原子内容：前、后两个4202完整状态使用端点；二者须同场景、同主体、同正式属性上下文、同正式特征类型，且前时间严格小于后时间。
2. 组合内容：一个或多个有规范化顺序的直接子动态；成员无重复、父子无环，组合主体须由同主体或正式组成关系闭包证明。

动态的实际起止时间从原子端点或组合成员派生，不保存独立发生时间字段。来源状态或直接子动态必须先独立形成并读回；动态随后发布并读回；场景类最后按4202把动态组织进发生场景的动态实例树。三个阶段不得合并成跨owner隐式写入，也不得以组织失败撤销已经正式发布的动态内容。

九项动态内容操作的语义、来源展开、预算、新写与幂等重放分流、引用保护以及普通退出/物理清理分账由4211冻结；它们与本规范第2.4节两项共同身份操作分账。公开DTO、枚举数值、关系编码、精确函数形状和无环装配仍待后继物理合同。本规范不另设动态CRUD、迁移入口、兼容结构或历史格式解析合同。

### 4.6 因果结构

唯一所有者为 `L2因果结构服务`，DTO 物理所有者为 `L2因果结构.数据.h`。因果节点族只使用因果 owner 和因果端口；因果创建前经状态公开身份来源核对条件 / 结果引用，完整读取在同一截止闭合因果身份来源。

`L2因果事实` 固定包含：

```text
L2因果身份
非空有序条件状态引用组
可空动作引用
非空有序结果状态引用组
来源稳定编码
L2生命周期
```

条件和结果项均使用 `L2有序身份引用` 指向状态身份。动作为空是合法结构；非空动作只是不透明稳定编码，L2 只经 L1 验证其当前节点形状，不依赖任务、方法或其它业务服务。

因果服务只保存调用方完整提交的因果记录及其引用替换 / 退出，不判断因果真实、必然、主动或被动，不推断动作，也不自动补条件或结果。

## 5. 关系归属与单向依赖

场景父子/成员、存在组成/特征引用及其它各域具名关系由L1关系事实承载；精确值、时间和材料由类型化值承载。4202四根及组织边是实际事实，完整读取组才是投影，不得另存第二权威列表。新模型的组成/成员/子场景归属联合唯一并无环；旧宿主边和旧场景族依赖只按第1.3节兼容，不解释为新同节点自身关系。

依赖方向固定为：

```text
L2场景结构 -> L1事实基座
L2存在结构 -> L1事实基座
L2场景成员关系 -> L2存在结构公开读取 + L1事实基座
L2特征结构 -> L1事实基座
L2存在特征引用 -> L2特征结构公开读取 + L1事实基座
L2状态结构 -> L2特征定义公开读取 + 合法值提供者公开读取 + L1事实基座
L2动态结构 -> 状态使用绑定窄只读提供者 + L2存在结构公开读取 + L2状态结构公开读取 + L1事实基座
L2因果结构 -> L2状态结构公开读取 + L1事实基座
普通应用唯一 provider -> 同一 L1 运行包签发器 + 六个 L2 服务唯一实例
DATA-L3 世界树结构聚合 -> 六类 DATA-L2 服务公开面；零写入所有权
```

场景树根标记、树归属和父子同树约束仍属于 `L2场景结构 -> L1事实基座` 这一条边；不得增加独立树服务、树 owner 或场景对 A1 / 自我初始化的反向依赖。A1 只能透明返回同一个场景服务实例，不创建、选择或缓存根。后继自我初始化只能消费调用方已经明确并经正式读取证明的 `L2场景树身份` / 根场景，不得创建、扫描或猜测根。

不得增加反向 import、同层循环依赖、跨所有者仓库访问或不存在结构依赖的转发层。跨族引用必须调用目标族公开身份来源或完整读取，不能直达 `DATA-L1` 重演目标族证明。存在服务不依赖场景服务；场景反向关系查询由场景所有者提供。`DATA-L3` 世界树聚合只能持有并组合六类公开服务，不能形成第二写入入口、第二 DTO 解释、中央身份服务或业务门面。

## 6. 共同事务、幂等与读取

1. 每次创建、替换或退出由目标结构所属数据类形成合法 L1 中性写集。节点创建与本族归属关系必须同写；关系替换、旧事实退出和新引用必须在一个可靠原子边界发布。存在跨场景 / 组成类别移动按 7150 组织各所属分区的联合守卫与共同提交，复用 4015 已有中性原子能力，不把两个分区冒充一个 owner，也不退化为两次普通 CRUD。该结构组合不授予 L2 跨族业务解释权；具体替代函数及内部组合提供者须经正式设计建立，不宣称现有 ABI 已具备能力。
2. 一份请求取得首次结果后，其 `L2结构幂等身份` 才与逐字段完整请求绑定；后继逐字段完全相同的请求返回 `精确重复` 和首次原结果，同一已绑定身份用于不同请求语义返回 `幂等冲突`，零变化。只有返回结构化 `事实代次漂移` 的未发布请求不绑定身份，调用方可在重新正式读回后保留该身份并重组期望代次及其派生请求字段；不得以记录已存在、摘要相同或业务同义冒充幂等。
3. 期望事实代次不符返回 `事实代次漂移`，仅交换前候选阶段承诺零变化、无首次结果且幂等身份未绑定。资源失败或异常若发生在交换前同样零写；L1 已确认提交或精确重复后的独立读回失败不得回滚，必须保留已形成事实、幂等账和代次，并使用原幂等身份与原完整请求读回收敛。当前源码没有无法取得 L1 提交结果的生产分支，本叶不新增另一种发布结果。
4. 当前组合读取只能使用 L1 一致当前投影；节点、关系、值、属性和引用组必须共享一个非零事实截止代次。一次允许的重试后仍漂移时返回 `事实代次漂移`，不得返回部分结构。
5. 历史读取必须显式使用具名历史入口和指定截止，只返回 0/1 个有效快照，不返回退出投影。不得声明完整演进版本集合、排序或退出时间线；完整历史能力登记为后续 L1 提供者缺口。不得使用当前事实补历史缺项，也不得把已退出事实重新标为当前。
6. 当前属性不存在返回 `属性未设置`；目标不存在返回 `未找到`；目标已退出且从当前入口读取时返回 `已退出`。三者不得互相折叠。
7. 任一成功结果缺项、身份复义、同一结果混代、关系角色矛盾、属性槽复义或发布后读回矛盾均返回 `内部不一致`，停止依赖路径；不得降级为未找到、资源失败或业务拒绝。
8. 原完整读取与条件查询必须复用同一身份来源判定，并把来源事实和本结构其它节点 / 关系 / 值放入同一当前一致投影或同一历史截止闭环；禁止先在一个代次证明身份、再从另一代次拼装结构。跨族写前读取均使用同一期望事实代次；其后本族提交仍以该代次为期望，期间任何发布由 L1 返回事实代次漂移。
9. 场景根创建的三项写集与非根创建的四项写集不可拆分；同键同完整请求返回首次树 / 场景 / 关系编码，同键异义冲突。父子新增或替换前必须从同一期望代次读回两端树身份相等，根作为子端或跨树端点固定入口拒绝，零变化。
10. 跨族组合、写前校验和普通当前消费者需要确认七族身份时，只能调用对应 `确认当前{族}结构身份` 并把成功结果截止精确绑定到其请求 G0；历史重放、退出、恢复或审计需要来源见证时，只能调用该族独立的历史身份来源见证合同，并使 H 等于结果截止。动态创建核验当前来源时使用当前确认，按历史截止重放或读取来源时使用历史见证；DATA-L3 场景树、语言 / 概念、ARCH-L4 和其它非七族 ABI 不因该规则收缩共同见证。

## 7. 引用保护与退出

1. 退出请求必须显式处理本结构所有的当前附属关系和值，并包含本节点唯一族归属关系；不得按业务名称自动推断要退出的引用。
2. 目标仍被当前关系、属性、值来源或独立材料引用时，且这些引用未在同一合法写集中闭合，返回 `引用冲突`，整笔零变化。
3. 跨结构引用统一由 L1 当前关系和引用闭包保护；较低层服务不反向依赖全部消费者，也不得扫描上层业务服务补造退出项。
4. 退出成功只改变当前可见性并保留完整历史。物理回收、崩溃恢复、跨进程恢复和持久证据清理不属于六类普通 CRUD。
5. 节点与族归属关系必须同代退出；族锚点和族归属关系类型不随单个节点退出。归属关系不得单独退出、换锚点、复活或迁到另一 owner。旧未标记节点默认拒绝，不得在普通读取中补写；保码迁移必须另立具名合同并携带可审计原 owner 证据，否则由新 owner 重建新稳定编码并按独立 legacy 退出计划闭合旧事实。
6. 本族退役必须先用仍有效的本族端口退出全部公开节点、归属关系、锚点和关系类型并使当前事实归零，再释放端口；随后只有在幂等提交收敛且历史 / 恢复 / 审计引用闭合时，签发器才可按 4015 退出 owner。owner、锚点和关系类型历史永久保留，不得复活、转交另一族或用新的 owner 继续接受旧族身份。
7. 普通场景退出不得退出根，也不得让仍有当前子场景的非根退出后留下无父树成员。根退出只能由具名整树退出合同在闭合全部节点、场景族归属、根标记、树归属、父子、成员、属性和值以及外部当前引用后执行；没有该合同的实现阶段，根和树固定不可退出。树登记锚点及两种树关系类型不随单树退出。

## 8. 实际消费与施工检查边界

六类真实实现由合法消费者在实际使用时经正式公开强类型入口、结构化结果和权威读回判断是否满足当前需要。施工期间的临时专项可以按风险逐叶覆盖：

1. 合法创建后完整当前读回；
2. 零身份、错类型、坏引用、坏顺序、旧期望代次等非法请求返回结构化非成功且权威状态零变化；
3. 同键同完整请求精确重复、同键异请求幂等冲突；
4. 当前或条件查询结果完整、稳定排序且共享一个事实截止代次；
5. 替换后新事实为当前、旧事实可从历史入口读取；
6. 引用未闭合时退出冲突且零变化，引用闭合后退出成功、当前不可见且历史仍可查；
7. 适用的组合写原子性、并发漂移和发布后读回一致性。
8. 七个 owner 两两不同，特征服务两端口路由唯一；wrong-family、同 owner 元数据节点、v1 legacy 与裸 L1 普通节点均被拒绝且权威状态零变化。
9. 每族节点与非自环族归属关系同写、同创建代次；来源的当前 / 历史读回完整，退出时同代闭合，错锚点、错类型、错角色、重复关系和混代均精确失败。
10. 同一场景 owner 底座建立至少两棵树并分别证明恰一当前根；根创建三项原子，非根创建四项原子，场景恰一树归属、根无父、非根恰一父、同树父子成功、跨树父子和根作子端拒绝。
11. 无父旧场景不等于根，旧裸场景不能扫描 / 猜测 / 自动认领；根标记、树归属和整树当前 / 历史读取逐字段、同截止且稳定排序。普通成员关系、存在端点和 A1 getter 均不能建立或改变树归属。
12. 存在单例角色的登记、原子新存在绑定、既有存在绑定、当前 / 历史读取、角色唯一、同键重复 / 异义和并发竞争成立；专项只使用测试角色键，不把任何键解释为自我。
13. 场景宿主存在关系的新增、替换、退出、当前 / 历史与反向读取、场景与存在两端来源、双向唯一和并发漂移成立；宿主关系与成员关系、父子关系和树归属互不替代。
14. 七个当前身份入口均在请求 G0 内核对节点、锚点、关系类型、唯一归属关系和内部 owner 当前；任一项退出 / 缺失 / 不唯一均按第 2.4 节错误优先级空载荷失败，成功 DTO 不含生命周期或内部 owner，结果截止精确等于请求 G0。七个历史见证入口均拒绝零 H，保留非零 Gread 守卫，成功 H 等于结果截止且完整见证满足本族生命周期条件。当前 / 历史消费者清单必须逐项覆盖动态来源的当前确认与历史见证；场景树、语言 / 概念、ARCH-L4、P08 与 4070 不得被越权改写。

临时检查驱动不得编译进生产目标，不得取得 L1 / L2 私有仓、锁、候选或事务权限，不得构造业务资格、真实世界、真实状态、动作成立或因果真伪剧本；所属切片完成后必须清理。构建、日志、自检、骨架、退出码和未观察到错误只证明相应工程事实，不替代合法消费者的实际使用判断，也不要求建立独立验收计划补齐未发生分支。

## 9. 完成边界与禁止事项

本规范生效只证明六类世界强类型结构的正式所有权、DTO、依赖和使用边界已经冻结，不证明任一模块、函数、骨架、CRUD、聚合面、生产接线或业务闭环已经实现。

禁止：

- 在 `DATA-L2` 判断世界、存在、特征、状态、动态或因果的业务真实性；
- 建立第二世界登记、第二事实仓、第二当前值或第二关系解释；
- 为轮廓、颜色、位置、动作或业务场景建立专用结构 CRUD；
- 从状态 / 动态反推当前值，或由普通状态 / 动态 CRUD 擅自推导业务新值；历史统计仍不得自动形成因果；
- 直达 L1 仓库、锁、事务、SQL、材料目录、缓存或内部写集；
- 用 4210、4220 的上层业务解释覆盖本规范的 L2 纯结构合同；
- 用 `未实现`、编译、日志或单条成功路径宣称结构服务完成。
- 用节点到自身关系、普通节点形状、名称、本地键、强类型包装、缓存或 v1 投影证明节点族身份。
- 让特征定义与特征实例共享 owner / 端口，或让任一服务端口写出另一节点族公开读取接受的事实。
- 以“现实世界”“自我”资格解释场景树根，或绕过场景结构owner建立第二组织入口。
- 把树身份做成独立节点族 / 新 owner，或只保存在参数、缓存、provider、A1、自我初始化结果中而没有场景 owner 下的根标记与树归属事实。
- 在七族当前确认 endpoint 中，用完整 `L2节点族身份来源事实`、其生命周期、退出时间或历史见证反向证明当前，或新建 / 回显当前 DTO、身份、锚点、关系类型、归属关系或角色字段；也不得为旧 `读取{族}身份来源`保留请求 / 结果 / 函数、转发、别名、兼容开关、隐藏 overload 或第二当前投影。
- 借七族身份分流修改状态 / 动态墓碑、TTL、清理、当前选择、特征当前值权威或 DATA-L3 场景树合同；这些分别属于专属合同与 P08 / P12。

## 10. 状态、动态与实例材料统一合同

### 10.1 状态事实

状态只保存1160冻结的状态身份、正式特征类型、固定准确值和强时间。主体、场景与属性上下文由4202完整使用绑定表达，不进入状态本体。状态内容、使用绑定和场景组织必须分别由各自owner发布并读回。

### 10.2 动态事实

动态按1170/4211固定动态身份、主体存在和变化内容；原子内容使用两个完整状态使用端点，组合内容使用直接子动态。两种内容互斥，来源不可原地修改。名称概念引用由4240规定的概念结构owner维护，不进入动态数据服务载荷。

动态实际时间只从端点或成员派生。场景组织由4202场景类维护，不是动态字段。4211冻结九项内容操作语义，并与本规范两项共同身份操作分账；精确DTO、结果状态、关系编码、函数形状和完整读回ABI仍待后继物理合同。

### 10.3 实例材料保留与清理

1. 实例状态与动态分别设置非零有限TTL和非零数量上限；抽象状态不进入该策略，可复用变化模式由7190形成动态概念。
2. TTL 从材料首次正式形成UTC纳秒计算，不使用状态事件时间或动态派生时间。
3. 同类超量按首次形成UTC纳秒、稳定身份升序选择最旧材料。
4. 清理顺序固定为先动态、后不再被活动动态引用的状态；父子动态依赖、当前选择与L1引用保护必须纳入清理闭包。
5. 普通引用允许过期；当前选择须在目标状态清理前同代退出且不得回退。
6. 清理推进新事实代次，删除完整实例载荷并保留最小墓碑与紧凑幂等记录；历史读取命中墓碑返回“历史材料已清理”，不得伪造原内容。
7. 状态与动态服务只提供同步清理入口，不创建线程、定时器或后台任务。运行宿主的维护周期、失败重试和维护幂等身份继续按0050/1160执行。

## 11. 当前观察值与业务记录

1. 当前比较先在一致读取范围内取得存在的当前采用，再读取固定准确F及目标合同；F仍可读、同名、同概念、最后动态或被选状态均不证明当前采用或无变化。
2. 状态和动态只用于记录、触发、定位、历史追踪、解释和审计，不能反推或改写当前特征值。
3. 任务状态属于ARCH-L4任务域并由Vt承载，不进入世界状态或动态结构。动作来源按4220独立关联，不改写动态本体；变化成立不等于因果成立。

## 12. 已有变化记录的历史保护

现行动态语义只产生1170/4211规定的动态结构。L1仍须按照其可靠性合同保护已经发布的节点、关系、值、墓碑、幂等和历史材料；保护这些事实不表示动态类接受其它结构，也不授权从历史材料补造当前动态。

## 修订记录

| 日期 | 版本 | 修订内容 |
| --- | --- | --- |
| 20260909 | v0.41 | 同步4211 v1.1：删除动态精确ABI已冻结的过度声明，分账九项内容操作与两项共同身份操作，并以状态使用绑定窄只读提供者消除场景/动态模块循环。 |
| 20260909 | v0.40 | 状态统一转引1160/4202，动态结构与操作统一转引1170/4211，并保留L1可靠性保护。 |
