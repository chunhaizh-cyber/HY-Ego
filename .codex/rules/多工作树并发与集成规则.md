# 多工作树并发与集成规则（已退出）

本文件只为旧路径兼容和规则检查器保留，不再提供 worktree、任务分支、长期执行分支、并行代码施工或独立集成工作区授权。

自 2026-07-25 起，全部当前规则统一读取：

```text
.codex/rules/单工作区串行执行与发布规则.md
```

若历史计划、设计、施工记录或聊天材料与新规则冲突，以新规则的单工作区、`main`、唯一写入者、唯一代码执行智能体和串行验证发布口径为准。历史材料中的 worktree / 分支字段只保留历史意义，不得据此创建、复用或继续施工。
