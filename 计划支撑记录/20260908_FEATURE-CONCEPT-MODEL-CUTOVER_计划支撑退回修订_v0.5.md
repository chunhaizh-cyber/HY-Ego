# FEATURE-CONCEPT-MODEL-CUTOVER 计划支撑退回修订记录 v0.5

日期：2026-09-08
角色：计划支撑智能体 `/root/support_feature_scene_v04`
正式起点：`9708a7beb5fb2dd60d07fa4ae3771f5f8ea0d664`，`main == origin/main`，ahead/behind `0/0`，index 空。
Git 发布租约：已由交互智能体授予，仅限本记录第 3 节四项产物；精确暂存、复核后普通推送。

## 1. 来源 DRIFT

执行者具名退回三项：

1. EC 仍消费特征概念模块中的 `共享特征应用预算`、`共享场景限制`、`共享特征业务状态`、`共享特征阶段状态`，与旧共享 DTO 清零目标冲突；
2. DC 将 provider 的 `不支持`、`旧格式不支持`、`规则缺失`、`类型不相容`、`前次写入待收敛` 默认映射为内部不一致；
3. 场景 v0.4 直接收敛五分支 `特征概念写请求`，但完整场景请求不能证明任意分支已派发，存在未绑定陌生请求首次执行风险。

三项都属于原计划接口合同缺口，可由正式规范和当前接口在原目标内裁决，不需要修改规范或扩大白名单。

## 2. v0.5 裁决

1. 四个旧共享类型由 EC 模块自有的预算、场景限制、业务状态和阶段状态取代；字段及行为保持最小等价，名称和 owner 明确，不留别名。
2. DC 保留原 1..14，追加 15..19 五个公开状态，并按 provider 状态逐项映射；外层待继续不吞并 provider 前次待收敛。
3. 场景不保存 `概念原请求`，不设概念写入继续阶段，不直接调用 `收敛特征概念写入`。特征应用的五分支请求只由 `特征概念应用服务` 内部拥有和验证。
4. 场景初次调用 `处理类型观察`，续行调用 `收敛类型观察`；同实例 pending 由应用自行核对，换实例只有正式观察已经可读时才能继续，否则不支持。
5. 存在采用仍保存并逐字段互证精确 provider 原请求；最终读回和 v0.4 其它合同不变。

## 3. 候选产物与门禁

- `规范/详细设计/20260907_FEATURE-CONCEPT-MODEL-CUTOVER_准确特征与特征概念模型切换详细设计_v0.5.md`
- `计划/20260907_FEATURE-CONCEPT-MODEL-CUTOVER_准确特征与特征概念模型切换代码实施计划_v0.5.md`
- `计划/计划索引.md` 中唯一 `FEATURE-CONCEPT-MODEL-CUTOVER` 项
- 本支撑记录

候选检查结果：索引 `git diff --check` 为 0；三个新文件 `git diff --no-index --check -- NUL <file>` 均为 1，仅表示存在正常差异且没有空白错误；`python .\tools\check_specs.py --strict` 为 0，正式规范目录 `101/101` 通过。精确扫描确认四个 EC 专用类型在详细设计中各有一个定义、DC 五项各有一个显式映射，场景继续 DTO 没有概念 provider 请求字段。

本轮不构建、不运行程序、不改代码、工程、正式规范或执行记录。四项候选已取得 RELEASE 并按白名单发布；发布后执行者须从正式 v0.5 精确 blob 和新 HEAD 重新 S0。
