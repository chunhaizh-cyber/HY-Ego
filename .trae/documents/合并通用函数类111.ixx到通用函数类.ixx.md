# 合并通用函数类111.ixx到通用函数类.ixx的计划

## 分析差异
通过对比两个文件，发现通用函数类111.ixx包含了许多通用函数类.ixx中缺少的函数，主要包括：

1. **特征值处理函数**：
   - 余弦距离计算（cosine_distance_vec_i64_norm）
   - 颜色距离计算（color_distance_bgr_norm）

2. **二值图处理函数**：
   - 二值图压缩为VecIU64（二值图_压缩为VecIU64）
   - VecIU64还原为二值图（VecIU64_还原为二值图）

3. **像素操作函数**：
   - 打包像素UV（打包像素UV）
   - 解包U和V（解包U、解包V）

4. **AABB和空间编码函数**：
   - 计算AABB尺寸（计算AABB尺寸_点集）
   - Morton3D_13编码（Morton3D_13）
   - 占据金字塔编码（编码_占据金字塔、解码_占据金字塔_level）

5. **二值掩码处理函数**：
   - 二值掩码膨胀（二值掩码_膨胀一次_8邻域）
   - 二值掩码填洞（二值掩码_填洞_8邻域）

6. **区间操作函数**：
   - 区间由点创建（区间由点创建）
   - 维度一致检查（维度一致）
   - 点到区间_Linf距离（点到区间_Linf）
   - 区间并入点（区间并入点）

7. **其他工具函数**：
   - 取得不小于x的最小2的幂（next_pow2）
   - AABB相交和交叠体积计算（AABB相交_1D、AABB交叠体积）
   - 量化工具（取整_mm、取整_Q10000）
   - 轮廓相似度计算（轮廓相似度_VecIU64）

## 合并策略
1. **保持现有结构**：不改变通用函数类.ixx的基本结构和现有函数
2. **按类别添加**：将通用函数类111.ixx中的新函数按类别添加到通用函数类.ixx的对应位置
3. **去重处理**：对于两个文件中都存在的函数，保留通用函数类.ixx中的版本
4. **保持兼容性**：确保合并后的代码能够正常编译和运行
5. **注释一致性**：确保新添加的函数注释风格与现有函数一致，包括注释格式、参数说明和返回值说明

## 合并步骤
1. **添加特征值处理函数**：在特征值类部分添加余弦距离和颜色距离计算函数，确保注释风格一致
2. **添加二值图处理函数**：在二值轮廓图编解码部分添加压缩和解压函数，确保注释风格一致
3. **添加像素操作函数**：在通用工具部分添加像素打包和解包函数，确保注释风格一致
4. **添加AABB和空间编码函数**：在通用工具部分添加AABB统计和Morton编码函数，确保注释风格一致
5. **添加二值掩码处理函数**：在通用工具部分添加掩码膨胀和填洞函数，确保注释风格一致
6. **添加区间操作函数**：在特征稳态区间工具部分添加新的区间操作函数，确保注释风格一致
7. **添加其他工具函数**：在适当位置添加其他工具函数，确保注释风格一致

## 预期结果
合并后，通用函数类.ixx将包含所有通用函数类111.ixx中的函数，同时保持现有函数的完整性和注释风格的一致性，为系统提供更全面的通用功能支持。