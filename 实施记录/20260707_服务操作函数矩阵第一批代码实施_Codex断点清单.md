# 服务操作函数矩阵第一批代码实施 Codex 断点清单

日期：2026-07-08
状态：已完成 / 已验证 / 未迁移旧函数

## 1. 输入入口

```text
计划/已完成计划/20260707_服务操作函数矩阵第一批代码实施包_v0.1.md
规范/详细设计/服务操作函数矩阵第一批.md
流程图/20260707_应用逻辑流程图迁移模板_v0.1.md
项目记忆/Codex任务队列.md #129
```

本切片只消费已确认并登记到可执行队列的 #129。#123-#127 仍按当前项目记忆记录暂停后置。

## 2. 核心处理

```text
S1：动态服务补动态聚合候选、运动基元候选、实例动态引用阻断只读入口。
S1：因果服务补因果模板候选、因果查询候选、缺失证据候选只读入口。
S1：状态服务补实例状态引用阻断只读入口。
S2：需求服务补需求树后续结构材料、结构化更新拒绝、枚举目标和方法候选准入材料入口。
S3：任务服务补生命周期迁移请求、筹办回执请求、执行桥请求和非权威统计材料入口。
S4：世界服务补兼容壳用途复核、抽象状态候选和抽象动态候选只读入口。
S5：入口默认验收补 MATRIX-01 到 MATRIX-06 和总项输出。
```

## 3. 结构承载

```text
状态服务：实例状态引用阻断结果。
动态服务：动态候选材料、实例动态引用阻断结果。
因果服务：因果查询候选材料。
需求服务：需求树后续结构材料。
任务服务：生命周期迁移请求材料、筹办回执请求材料、执行桥请求材料、非权威统计材料。
世界服务：兼容壳复核结果、抽象状态 / 动态候选读取。
入口：服务操作函数矩阵第一批验收布尔和控制台人读输出。
```

本切片未新增节点类型、关系类型、工程文件、旧主信息字段、真实线程调度、工作队列、外设等待、控制面板、数据库、SQL、D455、体素或外设入口。

## 4. 输出变化

默认入口新增并通过以下批次级验收输出：

```text
MATRIX-01 动态候选和运动基元只读入口: 通过
MATRIX-02 因果查询候选和缺证据入口: 通过
MATRIX-03 实例状态动态引用阻断: 通过
MATRIX-04 需求树请求材料和自环拒绝: 通过
MATRIX-05 任务请求材料和执行桥拒绝: 通过
MATRIX-06 基础信息后续保守入口: 通过
服务操作函数矩阵第一批: 通过
```

## 5. 调用路径

```text
main
-> 状态服务::读取实例状态引用阻断
-> 动态服务::读取动态聚合候选 / 读取运动基元候选 / 读取实例动态引用阻断
-> 因果服务::读取因果模板候选 / 读取因果查询候选 / 读取缺失证据候选
-> 需求服务::读取需求树后续结构材料 / 需求树结构化更新请求已拒绝 / 枚举目标准入材料是否可读 / 方法候选准入材料是否可读
-> 任务服务::读取生命周期迁移请求材料 / 读取筹办回执请求材料 / 读取执行桥请求材料 / 读取运行统计材料
-> 世界服务::复核兼容壳用途 / 读取抽象状态候选 / 读取抽象动态候选
```

## 6. 验证方式

```powershell
git diff --check
msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m
.\x64\Debug\海中鱼巣.exe
python .\tools\check_specs.py
rg -n "D455|体素|控制面板|SQL|WebView|ADO|std::thread|std::async|工作队列|删除实例状态|删除实例动态" 海中鱼巣/核心 海中鱼巣/领域 海中鱼巣/入口.cpp
```

已完成验证：

```text
Debug x64 构建通过，0 警告，0 错误。
默认入口运行通过，进程返回 0。
git diff --check 通过。
禁止能力 / 排除项扫描无命中。
```

## 7. 完成声明边界

可以声明：

```text
服务操作函数矩阵第一批保守入口已按批次实施并通过默认入口验收。
拒绝路径和只读候选路径已按批次验收。
```

不得声明：

```text
旧能力迁移完成。
完整需求树、完整任务状态机、稳定因果系统或动态聚合算法完成。
控制面板、数据库、SQL、D455、体素或外设已接入。
真实线程调度、工作队列或并发压力测试已通过。
```
