# RUNTIME-TXN-S3 运行期上下文首次发布租约与恢复链单一所有权代码实施记录

日期：2026-07-15

状态：#249 / DQ-141 已完成 / JY-338 已落实 / 最终回归 680 已登记 / A01-A16 通过 / 已构建 / 已运行 / 已归档

## 1. 输入与实际接口复核

```text
正式实施基线：a07e323
流程图/20260714_运行期上下文首次发布租约与恢复链单一所有权代码逻辑流程图_v0.1.md/.html
规范/详细设计/运行期上下文首次发布租约与恢复链单一所有权详细设计.md
计划/已完成计划/20260712_RUNTIME-TXN-S3_主装配事务与恢复链所有权端到端验收切片_v0.1.md
实施记录/20260714_RUNTIME-TXN-S3_运行期上下文首次发布租约与恢复链单一所有权设计重做_Codex断点清单.md
JY-338 / WIN-0001-E49
```

实施前逐签名确认：`运行期上下文::完整()` 仍复核结构核心、业务装配、系统角色清单和权威材料；宿主仍只有占位 `尝试发布`，租约持有宿主 `shared_ptr` 的值式副本；三个既有调用点、工程登记和阶段 680 占用与计划一致。允许文件足以闭合，无需修改核心、领域、线程、界面、持久化、恢复计划或恢复模块。

## 2. 输入入口与公开结果

宿主公开入口收窄并改名为 `尝试首次发布全新上下文`。入口继续拒绝空候选、结构核心不完整、缺少业务装配和缺少初始化材料；完整全新候选在唯一发布锁内执行最终完整性复核后写入唯一 `当前上下文_`，返回 `已发布`。

宿主已经持有当前上下文时，重复同候选、第二候选和并发落败候选均返回 `已有当前上下文`，不得替换、清空或销毁原上下文。原占位枚举 `首次发布尚未启用` 和旧通用入口已从生产与自检调用面移除。

## 3. 核心处理与结构承载

唯一提交点是 `运行期上下文宿主::当前上下文_`。发布锁保护最终完整性复核、已有上下文判断和同一 `shared_ptr` 提交；租约复制宿主持有的同一共享所有权，因此租约读取身份、结构核心、业务装配和系统角色材料均与获胜候选一致。

调用路径固定为：

```text
完整全新运行期上下文候选
-> 运行期上下文宿主::尝试首次发布全新上下文
-> 发布锁内最终完整性复核
-> 唯一 当前上下文_ 槽
-> 运行期上下文租约值式复制
-> 同一运行期上下文身份只读访问。
```

本批发布入口不写四仓库、领域侧表或系统角色清单。全部拒绝、重复发布和并发竞争前后，候选节点数量、关系数量、主键数量和系统角色材料保持不变。

## 4. 自检与并发证据

新增 `海中鱼巣/自检.主装配事务.ixx` 真模块，独立承载 A01-A16。自检覆盖空候选、结构核心不完整、缺初始化材料、完整候选首发、重复发布、第二候选拒绝、同身份租约、无恢复 / 清空 / 替换入口，以及 100 轮双候选并发竞争。

每轮并发均得到一个 `已发布` 和一个 `已有当前上下文`；租约身份等于唯一获胜候选，两个候选结构快照均保持不变。并发线程、循环和证据输出只存在于 Debug 自检模块，生产模块未新增线程、`std::async`、工作队列或等待事件。

三个既有自检调用点只按新入口和真实首发语义适配。`入口.cpp` 只在 Debug 自检宏内增加模块导入、一个值式结果、一次 `RUNTIME-TXN-S3 / 680` 登记和总结果汇总；Release 不登记 680 或本切片显示材料。

## 5. 恢复所有权与失败收口

普通公开面只有全新候选首次发布入口，没有恢复发布、清空或替换兼容入口。本批没有新增 HYSNAP01、恢复候选、恢复屏障、恢复许可、恢复适配或恢复启动分支，也没有修改恢复计划文件。

JY-338 的恢复所有权继续冻结为：#226 只从正式已发布租约取得冻结来源；#229 拥有不可伪造恢复许可；#237 复用同一上下文、宿主槽和租约并消费恢复专用能力。恢复路径不得调用 `尝试首次发布全新上下文`。

## 6. 输出变化

新增并登记一个自检真模块：

```text
海中鱼巣/自检.主装配事务.ixx
```

修改 `启动.运行期上下文.ixx` 完成唯一首次发布和同身份租约；修改运行期上下文、运行期组合分层、系统角色初始化三个既有自检调用点；修改入口和工程清单完成 Debug 隔离登记。核心、领域、线程、界面、持久化、SQL、控制面板和恢复文件差异为 0。

## 7. 验证证据

```text
Debug x64：通过，0 警告、0 错误。
Debug --self-test-exit：退出码 0，RUNTIME-TXN-S3 A01-A16 全部通过。
并发证据：rounds=100 single-winner=100 same-identity=100 unchanged=100。
Release x64：通过，0 警告、0 错误。
Release --self-test-exit：退出码 0。
Release 输出及二进制 RUNTIME-TXN-S3 / RUNTIME-TXN-S3-EVIDENCE / 运行期上下文首次发布与租约 / 680 命中：0。
最终 Debug 完整自检：连续 20/20；每轮内部并发证据均为 100/100。
tools/check_specs.py --strict：通过。
git diff --check：通过。
允许范围扫描：CHANGED=8，OUT_OF_SCOPE=0。
旧发布入口和占位状态命中：0；680 登记：唯一 1 处。
新模块文件、export module、ClCompile、filters、import 五项：全部成立。
生产新增线程 / async / 恢复材料：0；恢复文件差异和恢复路径调用全新发布入口：0。
```

## 8. 状态与下一步

#249 / DQ-141 已完成并归档，最终回归 680 已登记。#226 只解除正式租约来源前置，仍受其余快照依赖门控；#229 仍等待 #228、#225；#237 仍等待 #229 和恢复适配实际接口。#271 的 #249 前置已满足，但仍须形成精确兼容矩阵并重新实际接口复核，当前不可执行。#214、#257 继续暂停。

本完成声明只证明全新完整运行期上下文候选的一次首次发布、唯一宿主持有和同身份租约第一版已实现并验证，以及恢复链对同一上下文 / 宿主 / 租约的所有权门禁已冻结。不得扩大为快照、恢复、恢复发布、生产入口切换、线程启动、数据库恢复、在线热替换、自我苏醒或旧能力迁移完成。
