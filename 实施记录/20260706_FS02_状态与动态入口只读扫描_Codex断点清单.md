# FS-02 状态与动态入口只读扫描 Codex 断点清单

日期：2026-07-06
状态：已记录断点 / 文档切片 / 未改代码 / 未迁移旧函数

## 1. 本切片输入

```text
FS-02 第三批存在与场景入口扫描已提交并推送。
计划索引 HR-001-FM-SCAN 要求继续 FS-02 旧项目状态类、动态类、二次特征类和因果类第一批函数只读扫描。
用户明确“不需要确定的步骤应该持续执行下去”。
```

## 2. 已完成动作

```text
1. 回读 `计划/计划索引.md`、`规范/000_项目规则总纲.md`、`规范/001_规则迁移清单.md`。
2. 回读函数迁移前置专项、逐函数扫描清单、函数功能说明、基础信息服务分层规范、状态服务详细设计、动态服务详细设计、二次特征服务详细设计和领域服务最小入口详细设计。
3. 确认当前无 `计划/.计划索引.lock`，并在编辑计划索引前创建本切片锁。
4. 只读扫描 `D:\鱼巢\状态类.h`、`D:\鱼巢\状态类.cpp`、`D:\鱼巢\动态类.h`、`D:\鱼巢\动态类.cpp`。
5. 新增 `实施记录/20260706_FS02_状态与动态入口只读扫描记录.md`。
6. 登记 OF-080 到 OF-093。
7. 同步逐函数只读扫描清单、计划索引和项目记忆。
```

## 3. 本切片结论

```text
旧状态类将状态主体、特征、值、发生时间、收到时间、场景状态索引、状态清理、动态刷新和显示变化缓存混在旧状态主信息和场景索引中。
新项目必须拆为状态服务、场景服务、动态服务、特征服务、非权威缓存 / 统计和显示层只读边界；实例状态必须有场景、存在主体和发生时间戳。
旧动态类将实例动态、动作动态、方法动作动态、存在 / 场景动态、动态聚合、运动基元候选和 SQL 显示镜像混在一起。
新项目必须拆为动态服务、动作入口角色、方法系统、二次特征 / 因果增强、非权威统计和显示层只读视图；动作语义下来源动作不得用函数名、动作文本、日志标签或方法首节点冒充。
```

## 4. 下一步

```text
下一旧函数编号从 OF-094 开始。
继续 FS-02：旧二次特征类和因果类第一批函数只读扫描。
旧状态 / 动态中的 Vec / 指针特征值材料转 FS-03。
旧动态运动基元候选、动态聚合、影响统计转 FS-07 或后续方法系统增强专项。
旧 SQL 控制面板动态显示镜像转 FS-X / FS-10 排除边界。
```

## 5. 禁止宣称

```text
不得声明旧函数已扫描完成。
不得声明旧状态类或旧动态类已迁移。
不得声明旧状态 / 动态主信息字段、旧场景状态 / 动态索引、旧显示缓存或 SQL 控制面板镜像可直接迁移。
不得声明完整状态系统、动态系统、动作系统、因果系统、二次特征系统或方法学习系统完成。
不得接控制面板 / SQL / D455 / 体素 / 外设。
```

## 6. 验证记录

```text
本切片只做文档和只读事实记录验证。
未改代码。
未运行构建。
未运行当前项目入口。
未运行旧项目程序。
```
