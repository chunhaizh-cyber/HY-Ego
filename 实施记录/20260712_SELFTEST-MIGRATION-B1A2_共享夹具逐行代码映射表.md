# SELFTEST-MIGRATION-B1A2 共享夹具逐行代码映射表

基线：`10e24cb`。行号用于本次计划修订定位，执行前必须重新用 `rg -n` 复核。

| 文件 | 代码位置 | 代码行为 | 流程图节点 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `入口.cpp` | 387-439 | 构造主信息、四仓库、领域服务、稳定键与根需求参数 | A | 生产装配 | 建立运行期主结构 | 权威生产语境 | 装配失败追根因 | 入口继续拥有，只借出调用期引用和值式参数 |
| `入口.cpp` | 697-1826 | S4A-D 复用主装配形成真实任务、方法、动作、动态、结算与观察 | B-B2 | 自检经正式入口写入 | 形成后继可读测试结构 | 领域结构权威，报告非权威 | 前置材料缺失逻辑内返回；写入后异常追根因 | 迁入 `自检.自我治理闭环`，显式传主信息与服务语境 |
| `入口.cpp` | 1828-1919 | 建立 S5 完整隔离仓库、服务、初始化器、路由、桥、邮箱和统计 | C-C2 | 隔离自检夹具 | 仅改变隔离世界 | 不影响主装配事实 | 参数无效逻辑内返回；内部不完整追根因 | 整体迁入 `自检.自我治理多轮` 并唯一拥有 |
| `入口.cpp` | 1921-2274 | 承载 S5 同步、处理器、自我线程、多轮消息与停止收口 | C2 | 并发自检夹具 | 仅改变隔离世界 | 非生产事实 | 设计允许停止为逻辑内返回；收口失配追根因 | 与隔离装配整体迁移，不拆散、不复制 |
| `入口.cpp` | 2459 | 用主信息观察锚点生成只读签名 | E2 | 只读观察 | 无 | 非权威测试证据 | 锚点缺失逻辑内返回 | 在 S3B 模块内部实现，入口只借出锚点组 |
| `入口.cpp` | 2737-2775、4045-4068 | 形成动作键冲突方法、被动动作入口和 FS02 跨场景负例 | E1 | 前序跨域夹具 | 已由前序逻辑形成 | 句柄对应真实测试结构 | 缺失时 S3B 不登记 | 不迁移、不复制，只借用现成句柄 |
| `入口.cpp` | 5989-6434 | S3B 复用后段材料并内部建立临时线程、桥和邮箱 | E-E2 | 后段自检 | 新建 S3B 测试材料，桥接不得改既有结构 | 领域写入权威，回执报告非权威 | 材料缺失逻辑内返回；结构变化或收口失败追根因 | 迁入 `自检.自我治理回执`，显式后段语境和值式材料 |
| `入口.cpp` | 430、2344-2354、12538-12543 | 阶段总成登记、运行和报告交接 | K | 测试控制 | 无业务结构变化 | 非权威测试控制 | 接口漂移逻辑内返回 | 入口只保留三段语境组装、登记和运行 |
