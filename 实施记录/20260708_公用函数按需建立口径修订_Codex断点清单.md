# 20260708 公用函数按需建立口径修订 Codex 断点清单

## 状态

已完成 / 口径修订 / 撤回待确认计划 / 未入队 / 未改 C++ / 未迁移旧函数。

## 输入入口

用户确认新口径：

```text
公用函数不用迁移,只在需要建立符合公用规则的函数时,按照规则建立
```

## 核心处理

将公用函数规则从“后续迁移 / 当前入口复核”修正为“按需准入规则”：

```text
不迁移公用函数。
不主动扫描当前入口来推进公用函数专项。
不主动建立公共工具层。
只有具体切片需要新增或调整公用函数时，才按规则查重、归属、入口拒绝和验收。
```

## 结构承载

- `规范/公用函数规则规范.md`：补充公用函数不作为迁移对象、只按需建立。
- `实施记录/20260708_公用函数规则后续实施门禁信息数据.md`：将 S0/S1/S2 后续门禁改为 A0/A1/A2 按需门禁。
- `计划/待确认计划/20260708_公用函数规则当前入口只读复核计划_v0.1.md`：已删除。
- `计划/计划索引.md`、`项目记忆/Codex任务队列.md`、`项目记忆/当前状态.md`、`项目记忆/待确认问题.md`、`项目记忆/决策记录.md`：同步撤回和口径。

## 输出变化

撤回待确认项：

```text
QR-099
DQ-46
公用函数规则当前入口只读复核计划
```

后续规则：

```text
具体切片提出新增 / 调整公用函数需求
-> 查当前代码和函数说明
-> 判断纯通用 / 仓库内部 / 领域服务 / 流程编排 / 禁止候选
-> 明确允许文件、禁止文件、调用方、入口拒绝和结构不变化验收
-> 再生成待确认代码实施切片
```

## 调用路径

```text
用户新口径
-> 公用函数规则规范
-> 公用函数后续按需门禁信息
-> 未来具体切片按需引用
```

## 验证方式

文档治理验证：

```powershell
git diff --check
if (Test-Path .\tools\check_specs.py) { python .\tools\check_specs.py }
```

未运行 C++ 构建，未运行程序。

## 禁止宣称

- 不宣称公用函数工具层已实现。
- 不宣称当前入口只读复核已完成。
- 不宣称 C++ 已修改或已构建。
- 不宣称旧函数体已迁移。
- 不宣称旧项目能力已迁移。
