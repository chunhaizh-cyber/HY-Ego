# S0 缺口处理顺序确认与语素多对应实现 Codex 断点清单

日期：2026-07-05

## 1. 当前入口

计划索引当前下一步：

```text
确认 S0 缺口处理顺序：优先处理语素多对应 / 多路追溯实现缺口，或先建立需求目标值承载专项，或先补函数功能说明。
确认前不得进入 S1 或改代码。
```

用户已确认“按你的意见执行”，本切片按建议顺序执行：

```text
1. 先处理语素多对应 / 多路概念追溯实现缺口。
2. 再建立需求目标值承载专项。
3. 最后补函数功能说明清单或生成机制。
```

## 2. 本切片范围

允许：

```text
确认 S0 缺口处理顺序。
更新计划索引和项目记忆。
按已确认语素口径更新关系仓库 / 语素系统详细设计中的单目标旧描述。
实现语素对应信息多目标关系读取和追加入口。
实现语素概念追溯多目标关系读取和追加入口。
更新控制台入口样例，验证同一语素入口可读取多个对应信息节点和多个概念追溯节点。
```

禁止：

```text
迁移旧项目主信息字段或旧函数体。
修复需求目标值主信息承载缺口。
补完整函数功能说明文档。
声明完整语素系统、候选查询、自然语言理解或旧能力迁移完成。
```

## 3. 初始证据

S0 只读扫描已记录：

```text
关系仓库当前把 语素对应信息 和 语素概念追溯 作为源节点唯一目标关系。
语素服务当前只提供单目标读取接口。
已确认口径要求语素入口可对应多个信息节点，概念追溯支持多级、多路。
```

## 4. 待验证

```text
关系仓库允许同一语素入口写入多个语素对应信息目标。
关系仓库允许同一语素入口写入多个语素概念追溯目标。
语素服务提供目标集合读取入口。
控制台入口输出语素多对应 / 多路追溯通过。
Debug x64 构建通过。
默认 exe 运行通过。
```

## 5. 实施结果

已完成：

```text
确认 S0 缺口处理顺序：先处理语素多对应 / 多路追溯，再进入需求目标值承载专项，最后补函数功能说明。
关系仓库新增 获取目标节点组，用于按源节点和关系类型读取目标集合。
关系仓库不再把 语素对应信息 和 语素概念追溯 当作源节点唯一目标关系。
语素服务新增 追加语素对应信息、追加语素概念追溯、读取入口绑定目标组、读取入口概念追溯组。
旧的 读取入口绑定目标 和 读取入口概念追溯 保留为兼容单目标读取，不能作为多目标事实裁决入口。
入口样例新增 语素多对应追溯 输出。
```

## 6. 验证结果

```text
git diff --check：通过；仅有 LF/CRLF 工作区提示。
msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m：通过，0 警告，0 错误。
.\x64\Debug\海中鱼巣.exe：退出码 0。
入口输出：语素多对应追溯: 是 | 对应信息数=2 概念追溯数=2。
入口输出：MC-01 到 MC-11 均为通过。
```

## 7. 当前剩余缺口

```text
缺口 2：需求服务当前直接把 I64 目标值写入需求节点主信息值容器，下一步应建立需求目标值承载专项。
缺口 3：说明书/函数功能说明文档.md 不存在，函数迁移前仍需补函数功能说明清单或生成机制。
```

## 8. 禁止扩大声明

```text
本切片只证明语素多对应信息和多路概念追溯当前样例已通过。
不得声明完整语素系统完成。
不得声明候选查询、冲突包、自然语言理解或显示标题完成。
不得声明主信息迁移完成或旧项目能力已迁移。
```
