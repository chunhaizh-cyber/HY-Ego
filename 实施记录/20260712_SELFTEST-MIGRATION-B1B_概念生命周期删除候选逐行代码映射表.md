# SELFTEST-MIGRATION-B1B 概念生命周期删除候选逐行代码映射表

基线：`cdc96aa`。执行前重新复核行号和标识符。

| 文件 | 代码位置 | 代码行为 | 流程图节点 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `入口.cpp` | 8841-8851；调用 9001、9068、9086、9178、9189、9369、9659、9701、10081、10118、10193 | 闭包按目标概念实时过滤并排序实例支持关系；S6D 六处、S7 四处、S8 一处调用 | A、D、F、M1-M2 | 只读测试辅助 | 无 | 实时结构读回 | 材料缺失逻辑内返回 | 迁为 S7 模块唯一导出 `读取概念目标支持关系组`；十一处先改接再删除闭包 |
| 预期两个新 `.ixx` 文件头 | 模块首部 | `module;`、全局模块片段 include、`export module`、逐行 `import`、`export namespace` | M | 模块公开接口 | 无 | 编译期工程边界 | 不适用 | 先建立并编译骨架；禁止机械压缩声明或在导出前声明公开类型 |
| `入口.cpp` | 9651-10165 | S7 失效支持、生命周期转换、候选图发布、并发退役 / 支持 / 恢复并计算 A01-A08 | B-E | 自检经正式入口写入 | 改变当前测试概念图、生命周期和支持关系 | 领域结构权威，报告非权威 | 入口材料缺失逻辑内返回；写入后异常追根因 | 显式 S7 非拥有语境和不可变前序材料 |
| `入口.cpp` | 10166 | 记录“概念冷却退役与重新激活”启动耗时 | N | 人读计时 | 无 | 非权威 | 不参与验收 | 留在入口顺序 300 登记包装 lambda |
| `入口.cpp` | 10168-10499 | S8 消费 S7 身份，准备退役，读取删除影响候选，重算固定点并发只读，再绑定支持 | F-I | 自检经正式入口写入与只读候选 | 准备阶段改变测试生命周期 / 支持；候选本身不提交 | 领域结构权威，候选 / 报告非权威 | 输入缺失逻辑内返回；候选提交或结构异常追根因 | 显式 S8 非拥有语境、前序值式证据和 S7 封存报告 |
| `入口.cpp` | 10500 | 记录“概念删除影响候选与完整输入重建”启动耗时 | N | 人读计时 | 无 | 非权威 | 不参与验收 | 留在入口顺序 310 登记包装 lambda |
| `入口.cpp` | 11032-11093 | 输出 S7 八项和 S8 十项 | E、I、N | 人读自检输出 | 无 | 非权威 | 单项失败由阶段汇总 | 随各自模块迁移，入口不保留逐项输出 |
| `入口.cpp` | 最终回归登记区 | 阶段 300 / 310 / 400 登记和一次运行 | O | 测试控制 | 无业务结构写入 | 非权威 | 登记漂移逻辑内返回 | 入口只保留上下文、报告槽、计时包装和阶段调用 |
