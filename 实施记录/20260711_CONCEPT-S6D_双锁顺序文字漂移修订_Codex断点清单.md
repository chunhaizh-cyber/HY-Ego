# CONCEPT-S6D 双锁顺序文字漂移修订 Codex 断点清单

日期：2026-07-11

状态：当前代码事实已复核 / 流程图、详细设计与 #210 计划已修订 / JY-229 / 未改 C++ / 未构建 / 未运行程序

## 1. 输入基线

```text
HEAD：d484fb5
分支：main
main...origin/main：0 / 0（修订开始时）
#210 执行前实际接口复核报告：原详细设计把既有双锁顺序写反。
```

执行窗口已把六份 #197 草案保存为：

```text
stash@{0}
14bb976cd17b80913636e41e9a7a929b35d71a6a
On main: CONCEPT-S7-#197-草案-执行#210前隔离-20260711
```

本窗口只读确认该 stash 含六份预期文件；不应用、不删除、不改写该 stash。

## 2. 当前代码事实

`海中鱼巣/领域/概念图服务.h` 中三处当前双锁路径为：

```text
初始化活动概念生命周期：活动图锁_ 独占锁 -> 图写锁_。
初始化活动图基础版本：活动图锁_ 独占锁 -> 图写锁_。
发布候选图版本：活动图锁_ 独占锁 -> 图写锁_。
```

当前不存在先持有 `图写锁_` 再请求 `活动图锁_` 的既有双锁路径。候选发布在持有活动图独占锁后取得图写锁；若 #210 支持失效反向实现，会与候选发布形成 ABBA 死锁。

## 3. 漂移与裁决

原文字：

```text
详细设计 6.1：图写锁_ -> 活动图锁_ 的既有顺序。
流程图：取得图写锁和活动图独占锁。
计划 S2：仅写在两锁内，未固定顺序。
```

正式裁决：

```text
需要同时持有两把概念图内部锁时，固定按 活动图锁_ -> 图写锁_ 获取。
禁止持有图写锁_ 后请求活动图锁_。
支持失效与候选发布必须同序。
反向嵌套属于结构实现缺陷，不得进入构建完成声明或 #197 解锁声明。
```

该裁决来自当前代码和既有防锁规范，不新增用户待确认问题，不改变支持失效业务模型、关系状态、活动版本或 #197 依赖。

## 4. 修订材料

```text
流程图/20260711_概念活动实例支持权威失效与投影推进流程图_v0.1.md
流程图/20260711_概念活动实例支持权威失效与投影推进流程图_v0.1.html
规范/详细设计/概念活动实例支持权威失效与投影推进详细设计.md
计划/20260711_CONCEPT-S6D_活动实例支持权威失效与投影推进代码实施切片_v0.1.md
```

范围一致性：流程图把取锁节点改为先活动后图写，并把反向嵌套列为禁止路径；详细设计固定同一顺序并解释 ABBA 风险；#210 计划把该顺序写入当前事实、实施步骤、禁止项、漂移退回、失败收口和并发验收。

## 5. 治理映射

```text
队列：#210，编号不变。
设计队列：DQ-102，编号不变。
决策记录：JY-229。
待确认问题：不新增；QR-167 补记代码事实锁序。
```

计划索引、任务队列、当前状态、决策记录、实施记录和 QR-167 同步更新。本修订不把 #210 标为已执行，也不证明支持失效或 #197 已实现。

## 6. 验证状态

已执行：

```text
三处当前双锁路径定向代码复核通过，顺序均为活动图锁_后图写锁_。
全材料反向锁序残留扫描通过，剩余反向文字仅为历史错误说明和明确禁止项。
MD / HTML Mermaid 图文本完全一致。
git diff --check 与 staged diff check 通过。
tools/check_specs.py 通过，未发现规范治理问题。
精确 staged scope 为 11 份文档，无 C++ / ixx / 工程文件；stash 仍为 14bb976cd17b80913636e41e9a7a929b35d71a6a。
```

按设计窗口规则不运行 C++ 构建或程序。

## 7. 完成声明边界

本修订完成后只能声明 #210 的双锁设计已与当前代码统一为 `活动图锁_ -> 图写锁_`，执行窗口可以继续其余实际接口复核。

不得声明 #210 已实施、并发压力已通过、#197 已解锁、支持失效已实现、生命周期转换已实现或 stash 已恢复。
