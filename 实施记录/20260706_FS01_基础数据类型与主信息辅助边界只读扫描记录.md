# FS-01 基础数据类型与主信息辅助边界只读扫描记录

日期：2026-07-06
状态：FS-01 第三批已登记 / 只读扫描 / 未改代码 / 未迁移旧函数
依据清单：`实施记录/20260706_逐函数只读扫描清单.md`
前序记录：

```text
实施记录/20260706_FS00_FS01_逐函数只读扫描记录.md
实施记录/20260706_FS01_旧世界树核心边界只读扫描记录.md
```

## 1. 本轮定位

本轮继续 FS-01，只读登记旧核心结构剩余边界中与基础数据类型、句柄 / 值容器、主信息比较协议和旧主信息字段辅助逻辑有关的函数族。

本轮不展开语素、特征、状态、动态、二次特征、因果、需求、任务、方法等领域完整业务迁移判定；涉及这些领域时只登记目标分流和禁止边界。

## 2. 只读扫描范围

```text
D:\鱼巢\基础数据类型.h
D:\鱼巢\基础信息主信息类.h
D:\鱼巢\基础信息主信息类.cpp
```

扫描证据来自：

```text
基础数据类型.h:22-120：I64 区间、三向关系、比较量和方向掩码工具。
基础数据类型.h:126-174：可解析引用、指针 + 主键双材料引用。
基础数据类型.h:197-238：统计、VecU 句柄、指针句柄、特征值 variant 和有效性判断。
基础数据类型.h:605-691：VecI64 / VecIU64、I64 ZigZag 编码和解码。
基础数据类型.h:1194-1279：特征使用统计和特征生命周期辅助判断。
基础信息主信息类.h:89-126：主信息基类、基础信息基类、比较协议。
基础信息主信息类.h:171-198：环形缓冲。
基础信息主信息类.h:250-319：抽象特征主信息值域和结构校验。
基础信息主信息类.h:321-378：特征节点主信息当前值和缓存字段。
基础信息主信息类.h:380-430：存在节点主信息旧需求 / 任务 / 方法根字段和比较辅助。
基础信息主信息类.h:432-522：场景、状态、动态主信息字段。
基础信息主信息类.h:524-925：二次特征主信息比较、模板匹配和度量签名辅助。
基础信息主信息类.h:927-956：因果主信息计数和稳定度辅助。
基础信息主信息类.cpp:8-16：存在主信息析构释放旧需求 / 任务 / 方法根树。
```

## 3. 逐函数登记

### OF-032

```text
旧函数编号：OF-032
扫描批次：FS-01
旧项目路径：D:\鱼巢\基础数据类型.h:22
旧函数名：I64区间::有效 / 点值 / 包含 / 点
旧函数签名：constexpr bool 有效() const noexcept; constexpr std::int64_t 点值() const noexcept; constexpr bool 包含(std::int64_t) const noexcept; static constexpr I64区间 点(std::int64_t) noexcept
当前调用方：旧特征值域、二次特征区间、比较模板和状态 / 需求阈值判断路径。
触发路径：构造或检查 I64 区间时调用。
输入：I64 区间低值、高值或点值。
输出：区间有效性、点值、包含判断或点区间。
读结构：本地 I64 区间字段。
写结构：无；`点` 返回临时值对象。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：I64 区间作为第一轮状态值材料、特征状态材料或值域材料的局部工具语义。
删除行为：不得把旧区间字段直接作为需求目标、结算事实或主信息权威字段迁移。
新增约束：需求目标仍是目标状态；I64 只作为第一轮状态值材料或特征状态材料。
目标新结构：状态值材料、特征状态材料、后续特征值域结构。
目标领域服务入口：状态服务、特征服务；特征值域增强待 FS-03。
对应规范 / 详细设计：需求目标状态承载详细设计、状态服务详细设计、特征 / 特征值系统增强专项待设计。
入口拒绝条件：区间低值大于高值、试图用区间裸字段替代目标状态或结算记录。
验证方式：由使用方服务读回结构；拒绝路径结构不变化。
初步判定：保留
待用户确认项：I64 区间工具是否纳入特征 / 特征值系统增强专项统一值域库。
扫描证据：`基础数据类型.h:22` 至 `基础数据类型.h:36`。
```

### OF-033

```text
旧函数编号：OF-033
扫描批次：FS-01
旧项目路径：D:\鱼巢\基础数据类型.h:72
旧函数名：三向关系被接受 / 比较I64_三态 / 比较量转三向关系 / 比较量方向掩码 / 比较量命中方向
旧函数签名：bool 三向关系被接受(...); constexpr 比较量 比较I64_三态(...); constexpr 枚举_三向关系 比较量转三向关系(...); constexpr 三向关系掩码 比较量方向掩码(...); constexpr bool 比较量命中方向(...)
当前调用方：旧二次特征比较、模板匹配、方向区间和条件命中路径。
触发路径：把 I64 比较、比较量或方向掩码转换为模板匹配条件。
输入：比较量、三向关系、方向掩码或 I64 左右值。
输出：三态比较量、方向掩码或命中 bool。
读结构：本地数值。
写结构：无。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：方向比较工具语义可作为二次特征和后续条件判定的局部计算工具。
删除行为：不得把返回 bool 当作长期机器事实、任务完成或需求满足。
新增约束：条件命中必须由状态、二次特征、动态或结算记录承载；局部 bool 只作临时控制流。
目标新结构：二次特征节点、状态比较材料、后续方法条件判定索引。
目标领域服务入口：二次特征服务；方法条件判定索引待后续方法系统增强。
对应规范 / 详细设计：二次特征服务详细设计、错误结果与入口拒绝规范、方法系统详细设计。
入口拒绝条件：未确认比较语义、把局部 bool 持久化为业务事实。
验证方式：比较工具仅由领域服务内部调用；业务事实必须能从结构读回。
初步判定：保留
待用户确认项：是否在后续二次特征增强中统一命名为方向比较工具。
扫描证据：`基础数据类型.h:72` 至 `基础数据类型.h:120`。
```

### OF-034

```text
旧函数编号：OF-034
扫描批次：FS-01
旧项目路径：D:\鱼巢\基础数据类型.h:126
旧函数名：可解析引用::有效 / 获取 / operator-> / operator T节点* / operator= / 绑定 / 清空 / 私有_提取主键
旧函数签名：template<class T节点> struct 可解析引用 {...}
当前调用方：旧主信息字段中大量 `可解析引用<T>`，包括概念模板、内部世界、状态索引、动态索引、来源动态、因果模板等。
触发路径：旧主信息字段保存裸指针和字符串主键双材料时调用。
输入：旧节点裸指针、可选主键字符串。
输出：旧节点裸指针、主键字符串或有效性 bool。
读结构：旧节点指针、旧节点 `获取主键()`。
写结构：本地引用结构字段。
是否写机器事实：待确认；作为主信息字段时可能承载旧事实引用。
是否依赖日志：否。
是否依赖显示：可能被显示 / SQL 投影读取主键。
是否依赖 SQL：否；但主键可能用于 SQL 投影。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：可追溯引用需要同时支持稳定身份和运行期解析。
删除行为：删除裸指针作为长期身份；不得把字符串主键单独当作事实关系。
新增约束：新项目长期身份必须使用稳定句柄；关系必须进入关系仓库或领域服务关系入口，主键只作为索引材料。
目标新结构：节点句柄、关系仓库关系、索引仓库主键命名空间。
目标领域服务入口：节点仓库、关系仓库、索引仓库、各领域服务读取入口。
对应规范 / 详细设计：节点仓库详细设计、关系仓库详细设计、索引仓库详细设计、领域服务授权写入规范。
入口拒绝条件：空主键、无效句柄、主键命名空间缺失、多目标语义未确认、裸指针长期持久化。
验证方式：通过句柄读回节点，通过关系仓库读回引用；清空索引不改变权威关系事实。
初步判定：改写为领域服务入口
待用户确认项：旧 `可解析引用` 是否只作为旧字段扫描依据，不进入新项目公共类型。
扫描证据：`基础数据类型.h:126` 至 `基础数据类型.h:174`。
```

### OF-035

```text
旧函数编号：OF-035
扫描批次：FS-01 / FS-08
旧项目路径：D:\鱼巢\基础数据类型.h:197
旧函数名：结构_统计::记录观测 / 特征稳态 / 结构_特征使用统计::有效 / 特征生命周期仍在试用暴露期 / 特征统计应保留 / 特征统计应归档 / 基于使用统计更新特征生命周期
旧函数签名：void 记录观测(时间戳); bool 有效() const noexcept; inline bool 特征统计应保留(...); inline 枚举_特征生命周期 基于使用统计更新特征生命周期(...)
当前调用方：旧主信息统计、特征使用治理、特征生命周期治理和显示诊断。
触发路径：观测、命中、使用统计或生命周期评估时调用。
输入：观察时间、特征使用统计、生命周期、试用期状态。
输出：命中次数 / 最近时间更新、有效性 bool、是否保留 / 归档、生命周期枚举。
读结构：本地统计字段、时间戳、计数字段。
写结构：本地统计字段或返回生命周期枚举。
是否写机器事实：待确认；旧项目中可能被用于治理判断，新项目第一轮只能作为非权威缓存 / 统计候选。
是否依赖日志：否。
是否依赖显示：可能用于显示和诊断。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：统计观察和特征使用治理可作为后续非权威缓存 / 统计或特征增强参考。
删除行为：不得把命中次数、最近命中、生命周期推导结果作为需求满足、任务完成或方法成功事实。
新增约束：缓存统计只服务性能、索引、去重、诊断和人读观察，清空不影响业务事实。
目标新结构：非权威缓存记录、结构统计运行期快照、后续特征治理结构。
目标领域服务入口：非权威缓存 / 统计入口；特征 / 特征值系统增强专项待设计。
对应规范 / 详细设计：非权威缓存与统计规范、非权威缓存与统计详细设计。
入口拒绝条件：缓存命名空间缺失、缺权威来源、用统计结果裁决业务事实。
验证方式：清空缓存后权威节点 / 关系 / 状态 / 动态不变化；业务流程不依赖缓存存在。
初步判定：拆分
待用户确认项：特征生命周期治理是否纳入“特征 / 特征值系统增强专项”。
扫描证据：`基础数据类型.h:197` 至 `基础数据类型.h:207`、`基础数据类型.h:1194` 至 `基础数据类型.h:1279`。
```

### OF-036

```text
旧函数编号：OF-036
扫描批次：FS-01 / FS-03
旧项目路径：D:\鱼巢\基础数据类型.h:217
旧函数名：VecU句柄::有效 / 指针句柄::有效 / operator== / 特征值有效
旧函数签名：bool 有效() const noexcept; bool operator==(...); inline bool 特征值有效(const 特征值&) noexcept
当前调用方：旧特征值、特征快照、值池桥接、指针句柄和 VecU 证据池路径。
触发路径：判断特征值 variant 是否有值，或判断运行期句柄是否有效。
输入：VecU 句柄、指针句柄、特征值 variant。
输出：有效性 bool 或相等 bool。
读结构：本地句柄整数、variant 类型。
写结构：无。
是否写机器事实：否；但旧特征值字段本身可能承载机器事实。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：VecU 可能承载轮廓、体素或外设材料。
保留行为：特征值有效性判断和 Vec 数据间接引用语义。
删除行为：不得让指针句柄成为长期身份；不得让高级服务直接访问特征值服务或值池。
新增约束：长期身份使用节点句柄或主键；特征值服务只服务特征系统，只有特征服务可直接访问。
目标新结构：特征值节点、特征状态材料、VecI64 / VecIU64 原始值容器后续设计。
目标领域服务入口：特征服务；特征值服务仅作为特征系统内部服务。
对应规范 / 详细设计：基础信息服务分层规范、领域服务授权写入规范；特征 / 特征值系统增强专项待设计。
入口拒绝条件：无效特征值、错误特征值节点类型、特征值不归属目标特征、越权高级服务直接访问特征值服务。
验证方式：经特征服务读回状态材料；依赖检索确认高级服务不直接包含特征值服务。
初步判定：拆分
待用户确认项：VecU 句柄是否统一改写为 VecIU64 / VecI64 原始值容器句柄。
扫描证据：`基础数据类型.h:217` 至 `基础数据类型.h:238`。
```

### OF-037

```text
旧函数编号：OF-037
扫描批次：FS-01 / FS-03
旧项目路径：D:\鱼巢\基础数据类型.h:623
旧函数名：编码I64为U64_ZigZag / 解码U64为I64_ZigZag / 编码I64为U64 / 解码U64为I64 / 编码VecI64为VecIU64_ZigZag / 解码VecIU64为VecI64_ZigZag / 编码VecI64为VecIU64 / 解码VecIU64为VecI64
旧函数签名：inline constexpr std::uint64_t 编码I64为U64_ZigZag(I64) noexcept; inline VecIU64 编码VecI64为VecIU64_ZigZag(const VecI64&)
当前调用方：旧特征值 Vec 编码、值池、快照和后续持久化 / 显示兼容路径。
触发路径：I64 与 U64 存储编码或 VecI64 与 VecIU64 转换时调用。
输入：I64、U64、VecI64 或 VecIU64。
输出：编码或解码后的数值 / 向量。
读结构：本地值容器。
写结构：返回值容器。
是否写机器事实：否；属于值编码工具。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：VecIU64 可能用于轮廓、体素或外设材料编码。
保留行为：有符号值进入无符号容器时的可逆编码语义。
删除行为：不得把 Vec 编码工具提前解释为体素 / 外设接入完成。
新增约束：特征值原始 Vec 容器和内容哈希需等特征 / 特征值系统增强专项确认后实现。
目标新结构：VecI64 / VecIU64 原始值容器、特征值节点、后续内容哈希缓存。
目标领域服务入口：特征服务、特征值服务内部入口；非权威缓存统计入口后续承接哈希。
对应规范 / 详细设计：非权威缓存与统计规范、领域服务授权写入规范；特征 / 特征值系统增强专项待设计。
入口拒绝条件：Vec 原始值容器未确认、试图把编码结果作为业务事实、越权写特征值服务。
验证方式：编码解码往返测试；经特征服务读写；缓存清空不影响权威特征状态。
初步判定：保留
待用户确认项：VecI64 / VecIU64 原始值容器是否作为特征 / 特征值系统增强专项第一批。
扫描证据：`基础数据类型.h:623` 至 `基础数据类型.h:691`。
```

### OF-038

```text
旧函数编号：OF-038
扫描批次：FS-01
旧项目路径：D:\鱼巢\基础信息主信息类.h:114
旧函数名：基础信息基类::比较 / 生成比较量 / 命中比较条件
旧函数签名：virtual std::int64_t 比较(...)=0; virtual std::int64_t 生成比较量(...); virtual bool 命中比较条件(...)
当前调用方：旧查找、关系生成、主信息多态比较和候选命中路径。
触发路径：按比较字段比较两个旧主信息对象。
输入：旧基础信息主信息对象、比较字段。
输出：比较量或命中 bool。
读结构：旧主信息字段。
写结构：无。
是否写机器事实：否；但可能被调用方用作候选裁决。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：多态比较协议中的比较量语义。
删除行为：不得让 `命中比较条件` 的 bool 成为长期事实；不得把比较字段枚举直接迁移为主信息权威字段族。
新增约束：候选命中必须经领域服务复核节点类型、关系和状态；事实裁决必须落结构。
目标新结构：索引仓库候选、关系仓库关系、状态 / 二次特征 / 结算记录。
目标领域服务入口：索引仓库查询、领域服务复判入口、二次特征服务。
对应规范 / 详细设计：索引仓库详细设计、错误结果与入口拒绝规范、领域服务授权写入规范。
入口拒绝条件：比较字段未确认、候选多目标语义未确认、用 bool 作为机器事实。
验证方式：索引召回后由领域服务复核；拒绝路径结构不变。
初步判定：改写为领域服务入口
待用户确认项：旧比较字段枚举是否仅作为旧函数扫描线索，不进入新项目通用字段枚举。
扫描证据：`基础信息主信息类.h:114` 至 `基础信息主信息类.h:126`。
```

### OF-039

```text
旧函数编号：OF-039
扫描批次：FS-01
旧项目路径：D:\鱼巢\基础信息主信息类.h:171
旧函数名：环形缓冲::clear / push / get_from_latest
旧函数签名：void clear(); void push(const T&); const T* get_from_latest(std::size_t) const
当前调用方：旧特征节点近帧候选、近帧时间和稳态识别缓存。
触发路径：写入或读取最近 N 帧候选材料。
输入：候选值、索引位置。
输出：最近值指针或空。
读结构：本地数组缓存。
写结构：本地数组缓存。
是否写机器事实：否；作为旧近帧缓存时只应视为非权威缓存。
是否依赖日志：否。
是否依赖显示：可能用于调试显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：可能间接承载外设观测帧候选。
保留行为：固定窗口运行期缓存语义。
删除行为：不得把环形缓存内容作为权威状态、动态或结算事实。
新增约束：缓存缺失不得影响业务事实；权威事实必须进入状态、动态、特征或二次特征结构。
目标新结构：非权威缓存运行期快照；权威材料进入状态 / 动态 / 特征。
目标领域服务入口：非权威缓存统计入口；特征服务、状态服务、动态服务。
对应规范 / 详细设计：非权威缓存与统计规范、特征 / 特征值系统增强专项待设计。
入口拒绝条件：用缓存值裁决任务完成、需求满足或方法成功。
验证方式：清空缓存后权威结构不变；业务读取缺缓存不失败。
初步判定：拆分
待用户确认项：近帧候选是否进入特征增强专项而非本轮迁移。
扫描证据：`基础信息主信息类.h:171` 至 `基础信息主信息类.h:198`。
```

### OF-040

```text
旧函数编号：OF-040
扫描批次：FS-03
旧项目路径：D:\鱼巢\基础信息主信息类.h:277
旧函数名：抽象特征主信息类::有区间 / 是叶子特征 / 是复合特征 / 取值域 / 设为叶子特征 / 设为复合特征 / 校验结构 / 比较
旧函数签名：bool 有区间() const noexcept; const 特征值域* 取值域() const noexcept; void 设为叶子特征(特征值域); bool 校验结构() const noexcept
当前调用方：旧抽象特征创建、特征值域、特征模板和特征查找路径。
触发路径：构造或校验抽象特征值域和叶子 / 复合角色。
输入：特征类型、I64 区间或特征值域。
输出：值域指针、结构有效性或比较结果。
读结构：旧抽象特征主信息字段。
写结构：旧抽象特征主信息字段。
是否写机器事实：是，旧实现中值域和形态承载抽象特征事实。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：VecU 值域可能间接涉及体素 / 外设材料。
保留行为：抽象特征叶子 / 复合角色和值域约束语义。
删除行为：不迁移旧抽象特征主信息字段族为裸主信息字段。
新增约束：抽象特征和值域需进入特征 / 特征值系统增强专项；高级服务不得直接访问特征值服务。
目标新结构：特征节点、抽象特征角色、特征值域关系或二次特征结构。
目标领域服务入口：特征服务、二次特征服务。
对应规范 / 详细设计：基础信息服务分层规范；特征 / 特征值系统增强专项待设计。
入口拒绝条件：名称或类型缺失、叶子特征缺值域、复合特征残留值域、越权写特征值服务。
验证方式：经特征服务读回抽象特征结构；拒绝路径不产生半结构。
初步判定：排除 / 后续专项
待用户确认项：转入“特征 / 特征值系统增强专项”。
扫描证据：`基础信息主信息类.h:250` 至 `基础信息主信息类.h:319`。
```

### OF-041

```text
旧函数编号：OF-041
扫描批次：FS-03
旧项目路径：D:\鱼巢\基础信息主信息类.h:366
旧函数名：特征节点主信息类::有当前值 / 有区间 / 设置当前值 / 比较
旧函数签名：bool 有当前值() const noexcept; bool 有区间() const noexcept; void 设置当前值(const 特征值&); std::int64_t 比较(...)
当前调用方：旧特征写入、读取快照、稳态识别和特征比较路径。
触发路径：读写旧特征节点主信息中的当前值、区间和缓存字段。
输入：特征值。
输出：有效性 bool 或比较结果。
读结构：旧特征节点主信息当前值、区间、缓存字段。
写结构：旧特征节点主信息当前值。
是否写机器事实：是，旧当前值承载一次特征本体值。
是否依赖日志：否。
是否依赖显示：可能被显示读取。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：VecU / VecI64 值可能承载外设或体素材料。
保留行为：特征当前状态材料写入和读取语义。
删除行为：不让高级服务直接写特征值或主信息当前值；不迁移命中缓存、近帧缓存、稳态缓存为权威事实字段。
新增约束：特征状态材料必须经特征服务；缓存进入非权威缓存或后续特征增强。
目标新结构：特征节点、特征状态材料、状态 / 动态证据、非权威缓存。
目标领域服务入口：特征服务；动态 / 状态服务用于证据记录。
对应规范 / 详细设计：基础信息服务分层规范、领域服务授权写入规范；特征 / 特征值系统增强专项待设计。
入口拒绝条件：无效特征、错误特征值类型、特征值不归属目标特征、越权高级服务直接调用特征值服务。
验证方式：经特征服务读回状态材料；缓存清空不影响权威特征状态。
初步判定：拆分
待用户确认项：当前值写入和稳态缓存是否拆为 FS-03 两个后续条目。
扫描证据：`基础信息主信息类.h:321` 至 `基础信息主信息类.h:378`。
```

### OF-042

```text
旧函数编号：OF-042
扫描批次：FS-04 / FS-05 / FS-06
旧项目路径：D:\鱼巢\基础信息主信息类.h:380; D:\鱼巢\基础信息主信息类.cpp:8
旧函数名：存在节点主信息类::~存在节点主信息类 / 存在节点主信息类::比较
旧函数签名：~存在节点主信息类() override; std::int64_t 比较(...)
当前调用方：旧存在生命周期释放、需求树 / 任务树 / 方法树随存在释放路径。
触发路径：存在主信息析构时释放旧需求根、任务根、方法根；比较时按名称 / 类型判断存在相似。
输入：存在主信息对象。
输出：无返回或比较结果。
读结构：旧存在主信息中的需求根节点、任务根节点、方法根节点、名称、类型。
写结构：旧需求 / 任务 / 方法树释放并将根指针置空。
是否写机器事实：是，析构改变旧高级树生命周期。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：存在与需求 / 任务 / 方法归属和生命周期追溯语义。
删除行为：不迁移存在主信息里的需求根 / 任务根 / 方法根裸指针字段；不由存在析构直接释放高级树。
新增约束：需求、任务、方法由各自节点类型和关系承载；生命周期由领域服务或仓库句柄版本管理。
目标新结构：需求节点、任务节点、方法节点、存在-需求 / 任务 / 方法关系、生命周期状态。
目标领域服务入口：需求服务、任务服务、方法服务、关系仓库。
对应规范 / 详细设计：需求结算记录规范、任务系统规范、方法系统规范、主信息系统规范。
入口拒绝条件：存在节点无效、旧根指针无效、试图新增旧主信息字段、越权释放高级结构。
验证方式：通过需求 / 任务 / 方法服务读回关系；删除或失效必须由仓库版本和领域服务证明。
初步判定：改写为领域服务入口
待用户确认项：FS-04、FS-05、FS-06 是否分别细化旧根树释放语义。
扫描证据：`基础信息主信息类.h:380` 至 `基础信息主信息类.h:430`、`基础信息主信息类.cpp:8` 至 `基础信息主信息类.cpp:16`。
```

### OF-043

```text
旧函数编号：OF-043
扫描批次：FS-02 / FS-07
旧项目路径：D:\鱼巢\基础信息主信息类.h:432
旧函数名：场景节点主信息类::比较 / 状态节点主信息类::比较 / 动态节点主信息类::比较
旧函数签名：std::int64_t 比较(...) const override
当前调用方：旧场景、状态、动态查找和主信息多态比较路径。
触发路径：按名称 / 类型或同类判定查找场景、状态、动态。
输入：旧主信息对象、比较字段。
输出：比较量。
读结构：旧场景宿主和索引字段、旧状态时间 / 主体 / 特征 / 状态值字段、旧动态初始 / 结果状态和来源字段。
写结构：无。
是否写机器事实：否；字段本身在旧项目中承载事实。
是否依赖日志：否。
是否依赖显示：可能被显示 / SQL 投影读取。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：场景、状态、动态作为基础信息结构的身份和同类比较语义。
删除行为：不得把状态 / 动态事实继续塞入主信息字段；不得把 `是否变化`、`来源执行成功`、`来源错误码` 等 bool / 错误码作为权威业务事实。
新增约束：实例状态 / 实例动态必须有存在主体、场景归属和发生时间戳；动作来源由动作入口角色或方法入口确认。
目标新结构：场景节点、实例状态节点、实例动态节点、动作入口角色节点、状态 / 动态关系。
目标领域服务入口：场景服务、状态服务、动态服务、动作入口服务。
对应规范 / 详细设计：场景服务详细设计、状态服务详细设计、动态服务详细设计、动作入口详细设计。
入口拒绝条件：缺主体、缺场景、缺发生时间戳、来源动作未确认、把 bool 作为业务事实。
验证方式：状态 / 动态时间戳可读；动态前后值、目标和来源证据可读；拒绝前后结构数量不变。
初步判定：拆分
待用户确认项：状态 / 动态旧主信息字段是否在 FS-07 中继续细化。
扫描证据：`基础信息主信息类.h:432` 至 `基础信息主信息类.h:522`。
```

### OF-044

```text
旧函数编号：OF-044
扫描批次：FS-02 / FS-07
旧项目路径：D:\鱼巢\基础信息主信息类.h:524
旧函数名：二次特征主信息类::刷新度量签名_链键 / 由差值判断方向区间 / 方向区间编码 / 方向区间文本 / 由比较量判断方向区间 / 安全计算右减左 / 设置差值结果 / 语义相同 / 模板匹配 / 比较 / 私有_引用键 / 私有_引用匹配 / 私有_比较可选I64 / 私有_生成度量签名_链键
旧函数签名：二次特征主信息类中的比较、模板匹配、签名和差值辅助函数族。
当前调用方：旧二次特征模板、状态迁移、关系性特征、条件 / 结果判断和因果模板路径。
触发路径：生成二次特征签名、比较二次特征语义、按模板匹配实例。
输入：二次特征主信息字段、左值、右值、来源状态 / 动态引用。
输出：签名字符串、方向区间、差值、语义相同 bool、模板匹配 bool 或比较量。
读结构：旧二次特征主信息字段和可解析引用。
写结构：度量签名、比较左值 / 右值、差值、方向区间等旧主信息字段。
是否写机器事实：是，旧项目中二次特征主信息承载关系性特征事实和模板命中。
是否依赖日志：否。
是否依赖显示：`方向区间文本` 和离散语义键可能服务显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：二次特征的关系性比较、模板匹配、方向区间和签名语义。
删除行为：不把度量签名字符串、模板匹配 bool 或 `是否满足` 作为一阶业务事实；不得把显示文本进入机器裁决。
新增约束：二次特征事实必须由二次特征服务创建并绑定来源状态 / 动态 / 场景；模板匹配结果需结构承载或作为局部计算。
目标新结构：二次特征节点、二次特征模板关系、来源状态 / 动态 / 场景关系、非权威签名缓存。
目标领域服务入口：二次特征服务、状态服务、动态服务、因果服务。
对应规范 / 详细设计：二次特征服务详细设计、因果服务详细设计、非权威缓存与统计规范。
入口拒绝条件：来源状态无效、来源动态无效、缺场景、模板关系缺失、用 bool / 文本签名裁决业务事实。
验证方式：二次特征节点可读回来源关系；缓存签名清空不影响权威结构。
初步判定：拆分
待用户确认项：二次特征增强是否单列专项，还是并入特征 / 特征值系统增强专项。
扫描证据：`基础信息主信息类.h:524` 至 `基础信息主信息类.h:925`。
```

### OF-045

```text
旧函数编号：OF-045
扫描批次：FS-07
旧项目路径：D:\鱼巢\基础信息主信息类.h:927
旧函数名：因果主信息类::计算稳定度 / 比较
旧函数签名：std::int64_t 计算稳定度() const noexcept; std::int64_t 比较(...)
当前调用方：旧因果模板、因果解释、因果统计和显示诊断路径。
触发路径：计算成立次数与失败次数差值，或比较因果主信息对象。
输入：因果主信息计数字段或另一个因果主信息对象。
输出：稳定度差值或比较量。
读结构：条件模板、因方法模板、果比较模板、证据动态样本、观察 / 命中 / 因果出现计数、最近时间和验证标记。
写结构：无。
是否写机器事实：否；但旧因果主信息字段中保存因果统计和验证标记。
是否依赖日志：否。
是否依赖显示：可能被显示和诊断读取。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：因果统计需要通过动态证据样本累计形成可靠性判断。
删除行为：不把单个因果引用或简单稳定度差值解释为稳定因果结论；不把 `已验证` bool 直接迁移为因果权威字段。
新增约束：第一轮轻量因果引用只用来源动态做准入证据，不持久化来源动态关系；可靠因果结论后续专项设计。
目标新结构：轻量因果引用节点、动态证据集合、后续可靠因果结论结构。
目标领域服务入口：因果服务、动态服务；后续因果聚合专项。
对应规范 / 详细设计：因果服务详细设计、错误结果与入口拒绝规范。
入口拒绝条件：来源动态无效、把单条因果引用解释为稳定结论、用 bool 验证标记裁决业务事实。
验证方式：轻量因果引用只检查来源动态有效；不得写来源动态关系；可靠性后续由证据集合和评分验证。
初步判定：拆分
待用户确认项：可靠因果结论是否作为 FS-07 后续专项单独设计。
扫描证据：`基础信息主信息类.h:927` 至 `基础信息主信息类.h:956`。
```

## 4. 本轮初步结论

```text
1. 旧基础数据类型中的 I64 区间、比较量、方向掩码和 ZigZag 编码可以保留为局部工具语义，但不得直接升级为需求目标、结算事实或业务完成事实。
2. 旧 `可解析引用` 的“指针 + 主键”双材料语义应改写为稳定句柄、关系仓库和索引仓库；裸指针不得作为长期身份。
3. 旧主信息类混合了承载事实、关系锚点、缓存、显示文本和生命周期副作用，新项目不得整体迁移旧主信息字段。
4. 特征值域、特征当前值、二次特征模板、因果统计、状态 / 动态来源等内容需要分流到 FS-03、FS-07 或后续专项。
5. FS-01 已覆盖当前第一轮旧核心结构边界的主要候选文件；后续若发现遗漏，只能追加登记，不得反向宣称已形成迁移许可。
```

## 5. 后续待执行

```text
1. 下一旧函数编号从 OF-046 开始。
2. 可进入 FS-02 旧项目语素 / 基础信息函数只读扫描。
3. FS-03 需要细化特征 / 特征值函数，特别是 VecU / VecI64 / 值域 / 缓存 / 体素材料排除边界。
4. FS-07 需要细化状态 / 动态 / 二次特征 / 因果函数。
5. OF-032 到 OF-045 均为初步判定，需用户确认后才可进入任何代码实施。
```

## 6. 禁止宣称

```text
不得声明旧函数已扫描完成。
不得声明旧函数已迁移。
不得声明旧基础数据类型或旧主信息类已迁移。
不得声明旧主信息字段可以进入新项目主信息字段。
不得声明特征 / 特征值系统增强、二次特征系统或因果可靠性系统完成。
不得接控制面板 / SQL / D455 / 体素 / 外设。
```
