# 所有者范围首次写入材料读取施工记录 v0.1

日期：2026-08-15

计划身份：`DATA-L1-OWNER-SCOPED-FIRST-WRITE-RESULT-READ v0.1`

计划 blob：`deae88e37385294abb5cc669d27f7f88b6a8b5d2`

详细设计 blob：`4975bdbb68b126c1adb991a6c8cbd8b5a9a2ca6c`

## 1. S0 与所有权

- 设计提交 `060c99e7cbf3918293dc075e4a1979327937c403` 和索引提交 `4abd3b9d9f71fd6dd7f9d7df5782dd6edeea6c3e` 已正式发布；计划索引把本计划登记为 `可执行`，plan / design blob 与正式文件一致。
- 执行续点复核为 `main == origin/main == 4abd3b9d9f71fd6dd7f9d7df5782dd6edeea6c3e`、ahead / behind `0 / 0`、index 空；三份目标生产文件只包含本计划改动。
- SUPPORT 线程切片已经正式释放唯一代码执行段、构建资源和 Git 发布租约；复核时没有 `msbuild`、`cl` 或 `link` 构建进程。
- 流程图、SELF、线程、路线、历史文件删除、C1 候选和 `.codex-build` 等异主 WIP 原地保留，未纳入本计划范围。

## 2. 实际施工范围

永久生产文件：

1. `海中鱼巣/核心/L1所有者范围CRUD.数据.h`
2. `海中鱼巣/核心/仓库.L1事实基座.ixx`
3. `海中鱼巣/核心/服务.L1事实基座.ixx`

永久记录：

4. `实施记录/20260815_DATA-L1-OWNER-SCOPED-FIRST-WRITE-RESULT-READ_所有者范围首次写入材料读取施工记录_v0.1.md`
5. `验证记录/20260815_DATA-L1-OWNER-SCOPED-FIRST-WRITE-RESULT-READ_所有者范围首次写入材料读取施工检查记录_v0.1.md`

施工期间曾建立下列生产外临时检查三件套，完成后已删除，未登记生产工程，也不进入结果提交：

- `验证工具/L1所有者范围首次写入材料读取参数检查.cpp`
- `验证工具/L1所有者范围首次写入材料读取参数检查.vcxproj`
- `验证工具/L1所有者范围首次写入材料读取参数检查.vcxproj.filters`

## 3. 已实现内容

- 公共数据头新增独立读取合同版本、请求 DTO 和双 `optional` 结果 DTO；复用既有 `L1所有者范围读取状态`，没有修改原枚举数值或增加第二套状态。
- `L1所有者范围写端口` 新增唯一 `const` 入口 `读取首次写入材料`。请求纯值校验先于 capability 校验；移动源、失效端口、实例销毁或租约不存在映射为许可拒绝。
- 普通 `L1事实基座服务` 和签发器没有新增跨 owner 查询、列举或同义读取入口。
- 仓库按 `(owner, 写入幂等身份)` 在一次 `shared_lock` 内精确读取私有首次记录，不扫描其它 owner、其它幂等账、legacy 账或 owner 建立账。
- 成功结果逐字段返回首次规范化完整写集及原始首次成功结果，并互证 owner、幂等身份、首次发布代次、确定编码映射和当前 / 历史事实归属。
- 读取路径不写事实、不推进事实代次；未找到保留锁内当前代次，非成功双载荷为空。
- 分配或长度异常映射资源失败，其它不变量矛盾映射内部不一致；未增加生产故障注入、测试 friend、检查专用公开 ABI 或第二执行入口。

## 4. 施工中发现并修复的问题

临时检查第一次编译失败来自检查代码而不是生产实现：一处使用了不存在的 `节点种类::关系类型`，另一处跨模块直接依赖现有大 DTO 默认比较，触发 MSVC 模块可见性问题。临时检查改用实际节点种类和检查单元私有逐字段 helper 后，Debug / Release 均通过；没有为检查增加公开 `operator==` 或修改生产 ABI。

## 5. 完成边界

本记录只证明活动 owner 写端口具备按非零幂等身份读取本 owner 首次规范化完整写集和原始首次成功结果的 DATA-L1 代码切片，并完成本次实际施工检查。

它不证明任何 DATA-L2 / DATA-L3 服务、C1 四本体根、方法登记根、普通应用、阶段 18 / 19、跨进程恢复或业务闭环可用。合法消费者仍须在实际使用时判断该能力是否满足其合同。
