# 下一批待确认计划确认 Codex 断点清单

日期：2026-07-07
状态：已完成 / 计划确认 / 已登记可执行队列 / 未改代码 / 未迁移旧函数

## 1. 输入入口

用户确认 7 份待确认计划：

```text
FS-04 需求服务目标状态与任务承接材料 S1-S4 代码实施切片：小修后确认。
FS-05 任务服务承接材料与生命周期 S1-S4 代码实施切片：确认。
FS-06 方法服务角色与条件结果读取 S1-S4 代码实施切片：确认。
FS-07 动作动态因果入口增强 S1-S4 代码实施切片：确认。
FS-08 缓存统计结构快照增强 S1-S4 代码实施切片：确认。
FS-09 事件日志持久化恢复详细设计专项：确认。
FS-09 仓库快照格式与恢复拒绝矩阵专项：确认。
```

## 2. 核心处理

```text
1. 将 7 份计划从 `计划/待确认计划/` 移入 `计划/`。
2. 将 7 份计划状态改为已确认。
3. 按用户意见小修 FS-04 的 FS04-IMP-Q1 和 FS04-IMP-Q2。
4. 同步确认 FS-05 到 FS-09 各待确认问题的保守口径。
5. 修正待确认问题重复编号，将本批计划确认项登记为 QR-063 到 QR-069。
6. 将本批确认计划登记到 `项目记忆/Codex任务队列.md` 可执行队列 #107 到 #113。
7. 同步计划索引、当前状态、待确认问题、实施记录、决策记录和验收记录。
```

## 3. 可执行队列登记

```text
#107：执行 FS-04 需求服务目标状态与任务承接材料 S1-S4 代码实施切片。
#108：执行 FS-05 任务服务承接材料与生命周期 S1-S4 代码实施切片。
#109：执行 FS-06 方法服务角色与条件结果读取 S1-S4 代码实施切片。
#110：执行 FS-07 动作动态因果入口增强 S1-S4 代码实施切片。
#111：执行 FS-08 缓存统计结构快照增强 S1-S4 代码实施切片。
#112：执行 FS-09 事件日志持久化恢复详细设计专项。
#113：执行 FS-09 仓库快照格式与恢复拒绝矩阵专项。
```

## 4. 确认口径

```text
FS04-IMP-Q1：第一轮可复用现有 `引用` 关系，但必须由需求服务按“主体 / 目标宿主”角色过滤并做唯一性复核。
FS04-IMP-Q2：第一轮允许新增需求到场景的 `引用` 关系作为需求场景材料，不允许由调用方隐式补齐场景事实。
FS05-IMP-Q1：第一轮只复用任务虚拟存在和生命周期状态读取，不新增过程账锚点。
FS05-IMP-Q2：第一轮任务方法关系继续使用 `引用` 关系，并由任务服务封装选择 / 读取入口。
FS06-IMP-Q1：可新增公开只读方法身份复核入口。
FS06-IMP-Q2：本轮明确禁止新建 `动作入口服务.h`，动作入口拆分推迟到 FS-07 或独立专项。
FS07：不新建 `动作入口服务.h`，继续在 `方法服务.h` 内包裹动作入口能力；本能动作注册材料第一轮只验证结构身份、状态和动作键候选，不实现真实执行分派。
FS08：不新建 `非权威缓存统计服务.h`，继续使用 `统计服务.h` 承载第一轮缓存统计能力；本轮只复核已有结构统计快照，不新增孤立关系诊断报告结构。
FS09-DES：结构化事件材料第一轮只记录节点 / 主信息 / 关系 / 索引句柄和入口名，不记录旧 SQL 字段；事件日志只作为后续恢复材料候选，不作为当前运行期事实源。
FS09-SNAP：第一轮快照只覆盖节点仓库、主信息仓库、关系仓库和索引仓库，不覆盖特征值 Vec / 哈希 / 序列化；恢复入口必须在完整拒绝矩阵确认后另建 C++ 实施切片。
```

## 5. 禁止扩大声明

```text
本轮只确认计划并登记可执行队列。
本轮未修改 C++。
本轮未运行构建或默认入口。
本轮不证明 FS-04 至 FS-08 代码已实现。
本轮不证明事件日志持久化恢复、仓库快照格式或恢复入口已实现。
不得迁移旧函数体。
不得新增旧主信息字段。
不得接 SQL / 控制面板 / D455 / 体素 / 外设。
```

## 6. 验证方式

```powershell
git diff --check
if (Test-Path .\tools\check_specs.py) { python .\tools\check_specs.py }
```
