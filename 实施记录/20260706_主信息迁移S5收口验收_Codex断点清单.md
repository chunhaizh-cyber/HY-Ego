# 主信息迁移 S5 收口验收 Codex 断点清单

日期：2026-07-06
状态：已完成 / 文档收口验收通过 / 未迁移旧字段

## 1. 输入入口

计划索引 HR-001 当前下一步为：

```text
进入 S5 主信息迁移收口验收；收口前不得迁移旧函数体或旧主信息字段。
```

## 2. 本次范围

允许：

```text
汇总 S0 到 S4 证据。
检查旧项目主信息类和旧字段未回流。
检查 S4 后续专项已登记且未误判为完成。
更新主信息迁移实施映射计划、计划索引、项目记忆和验收记录。
```

禁止：

```text
迁移旧项目主信息类。
新增旧主信息字段。
新增任务 / 方法 / 动作 / 缓存字段。
确认或实现 S4 后续专项。
把本轮收口验收说成旧项目能力迁移完成。
```

## 3. 预计验收项

| 验收项 | 预期结果 |
| --- | --- |
| S0 只读扫描证据 | 已记录当前主信息仓库、特征值、语素、需求和方法服务事实 |
| S1 主信息仓库最小函数 | 已通过当前入口验证，未迁移旧字段 |
| S2 需求目标状态 | 已通过 DV-1，需求目标不再作为 I64 对外语义 |
| S3 语素对应信息关系 | 已通过多对应 / 多路追溯当前入口样例 |
| S4 后续专项登记 | 已登记任务 / 方法 / 动作入口 / 非权威缓存专项 |
| 旧主信息类回流检查 | 不应在 `海中鱼巣` 代码中命中旧主信息类 |
| 文档状态一致性 | 计划索引、映射计划、项目记忆和验收记录一致 |

## 4. 验收命令

```powershell
git diff --check
rg -n "词主信息|语素入口主信息|基础信息主信息|需求主信息|任务主信息|方法主信息" 海中鱼巣
rg -n "S0|S1|S2|S3|S4|S5|未迁移|旧项目能力" 计划 项目记忆 实施记录 规范
```

如代码未改，本切片默认不重新运行构建；若执行构建或入口运行，必须单独记录命令和结果。

## 5. 实际验收结果

```text
git diff --check 通过。
旧主信息类回流检查：`rg -n "词主信息|语素入口主信息|基础信息主信息|需求主信息|任务主信息|方法主信息" 海中鱼巣` 无输出，退出码 1，表示未命中。
S0-S4 状态一致性检索通过，可在计划、项目记忆、实施记录和规范中追溯。
本切片未修改代码，未重新运行构建和默认入口；沿用 S1、S2、S3 已记录的 Debug x64 构建和默认入口验证证据。
收口结论：主信息迁移第一轮门禁、字段分流和 S0-S4 证据已收口；未迁移旧字段、旧主信息类或旧函数体。
下一步计划：S5 当时要求先确认 `规范/仓库逻辑空间与领域树结构规范.md`；后续已在 `20260706_仓库逻辑空间与领域树图结构规范确认` 切片中按审查意见小修后确认。任务系统规范和详细设计已在后续切片小修后确认；方法系统规范和详细设计已在后续切片小修后确认；动作入口规范和详细设计也已在后续切片小修后确认；非权威缓存与统计、需求结算记录规范和详细设计仍待用户确认。
```

## 6. 收口结论

本次 S5 可以声明：

```text
主信息迁移第一轮门禁收口验收完成。
S0 到 S4 的文档证据链已闭合。
旧主信息类未回流到当前代码目录。
任务 / 方法 / 动作入口 / 非权威缓存字段已分流到后续专项。
```

本次 S5 不能声明：

```text
主信息字段已迁移。
主信息迁移完成。
旧项目能力已迁移。
任务系统、方法系统、动作入口或缓存统计系统已完成。
```

## 7. 禁止扩大声明

```text
S5 只能声明主信息迁移第一轮门禁和证据收口完成。
不得声明主信息字段已迁移。
不得声明旧项目主信息类已移植。
不得声明旧项目能力已迁移。
不得声明任务系统、方法系统、动作入口或缓存统计专项已完成。
```
