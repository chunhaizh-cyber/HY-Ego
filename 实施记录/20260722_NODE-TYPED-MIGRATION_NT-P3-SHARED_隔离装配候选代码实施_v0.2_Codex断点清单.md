# #348 / DQ-240 / NT-P3-SHARED v0.2 执行断点清单

## 1. 身份与范围

- 派发状态：JY-591/E332；S0 接受：JY-592/E333。
- 任务 worktree：`C:\Users\zhchh\.codex\worktrees\348-nt-p3-shared-isolated-r1\海中鱼巢`。
- 分支：`codex/348-nt-p3-shared-isolated-r1`。
- 冻结基线：`93cc9c8ab8e7753312a5ad5a05107ae9a62fc82d`（JY-589/E330）。
- 计划 blob：`1401435e4a7817084bbe656c6e3153126602a504`。
- 允许文件：公共合同头、节点直接运行期业务装配候选、节点直接运行期业务自检候选及本记录。

## 2. 合同核对

- `NT-C3/v0.2` 合同版本：`0x00020000`；结果 ABI 版本：`1`；对象规则版本：`1`。
- 本任务提供 `C3-A0`、`C3-F1`、`C3-F2`，消费 `C3-A1—C3-E3`。
- `C3-F1` 只持有 15 个唯一 export 服务实体的 `std::reference_wrapper<const T>`；其中 `C3-D2` 提供退役审计与投影后验两个实体；不持有仓库、会话、令牌或锁。
- 提供者模块仍是预冻结待实现合同；本记录不把候选表述为已编译、已登记或已接通。

## 3. 已形成候选

- `海中鱼巣/领域/节点直接P3合同.数据.h`：公共 ABI、结果状态、内部错误阶段、请求头、结果头。
- `海中鱼巣/装配.节点直接运行期业务.ixx`：隔离装配请求、不可默认构造 / 不可复制 / 可移动装配类型及合同版本拒绝 / 成功候选。
- `海中鱼巣/自检.节点直接运行期业务.ixx`：隔离全图结构自检报告和装配生命周期静态检查候选。

## 4. 禁止范围核对

- 未修改 `海中鱼巣.vcxproj`、`海中鱼巣.vcxproj.filters`、`海中鱼巣/自检.运行器.ixx`。
- 未修改入口、启动、运行期宿主、默认装配、旧服务 / 调用方、提供者业务文件或最终删除集。
- 未构建、未运行、未暂存、未提交、未推送。

## 5. 候选级验证续点

- 待在不补造提供者实现的前提下完成 import 图、生产不 import 自检、旧域零 import、白名单、UTF-8 和 `git diff --check` 复核。
- 本任务不运行完整构建、自检运行器、真实联调或最终切换；这些由 #352 统一接线和验收。
