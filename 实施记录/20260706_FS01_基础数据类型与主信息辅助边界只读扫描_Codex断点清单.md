# FS-01 基础数据类型与主信息辅助边界只读扫描 Codex 断点清单

日期：2026-07-06
状态：已记录断点 / 文档切片 / 未改代码 / 未迁移旧函数

## 1. 本切片输入

```text
用户要求无需确认的步骤持续执行。
计划索引 HR-001-FM-SCAN 要求继续 FS-01 剩余旧核心结构只读扫描。
```

## 2. 已完成动作

```text
1. 只读扫描 `D:\鱼巢\基础数据类型.h`。
2. 只读扫描 `D:\鱼巢\基础信息主信息类.h`。
3. 只读扫描 `D:\鱼巢\基础信息主信息类.cpp`。
4. 新增 `实施记录/20260706_FS01_基础数据类型与主信息辅助边界只读扫描记录.md`。
5. 登记 OF-032 到 OF-045。
6. 同步逐函数只读扫描清单、计划索引和项目记忆。
```

## 3. 本切片结论

```text
旧基础数据类型中的 I64 区间、比较量、方向掩码和 ZigZag 编码可保留为局部工具语义。
旧 `可解析引用` 必须改写为稳定句柄、关系仓库和索引仓库语义，裸指针不得长期持久化。
旧主信息类混合事实、关系锚点、缓存、显示文本和生命周期副作用，不得整体迁移旧字段。
特征值域、二次特征模板、因果统计、状态 / 动态来源等内容转入 FS-03、FS-07 或后续专项。
```

## 4. 下一步

```text
下一旧函数编号从 OF-046 开始。
进入 FS-02：旧项目语素 / 基础信息函数只读扫描。
若后续发现 FS-01 遗漏，只能追加登记，不得反向宣称已形成迁移许可。
```

## 5. 禁止宣称

```text
不得声明旧函数已扫描完成。
不得声明旧函数已迁移。
不得声明旧基础数据类型或旧主信息类已迁移。
不得声明旧主信息字段可以进入新项目主信息字段。
不得声明特征 / 特征值系统增强、二次特征系统或因果可靠性系统完成。
不得接控制面板 / SQL / D455 / 体素 / 外设。
```

## 6. 验证记录

```text
本切片只做文档和只读事实记录验证。
未改代码。
未运行构建。
未运行当前项目入口。
未运行旧项目程序。
```
