# 运行宿主与多线程消息队列专项计划确认入队 Codex 断点清单

日期：2026-07-09
状态：已确认 / 已登记可执行队列 #154 / 未改 C++ / 未构建 / 未迁移旧函数

## 1. 输入入口

用户确认：

```text
确认《20260709_运行宿主与多线程消息队列专项计划_v0.1.md》。
```

确认对象：

```text
计划/20260709_运行宿主与多线程消息队列专项计划_v0.1.md
```

联动确认依据：

```text
流程图/20260709_运行宿主与多线程消息队列流程图_v0.1.md
流程图/20260709_运行宿主与多线程消息队列流程图_v0.1.html
规范/详细设计/运行宿主与多线程消息队列详细设计.md
```

## 2. 核心处理

按计划确认联动门禁复核上游材料：

```text
流程图存在，且只定义运行宿主与多线程消息队列的消息流和边界。
详细设计存在，且只定义线程角色、消息语义、有界队列、写入复核和后续切片。
专项计划存在，且明确第一步只允许 THREAD-S0 当前代码事实扫描。
```

用户确认后执行：

```text
将计划从 计划/待确认计划/ 移入 计划/。
同步确认流程图和详细设计。
登记可执行队列 #154 THREAD-S0 当前代码事实扫描。
更新计划索引、项目记忆当前状态、待确认问题、决策记录和设计计划队列。
```

## 3. 结构承载

确认状态承载在：

```text
计划/20260709_运行宿主与多线程消息队列专项计划_v0.1.md
计划/计划索引.md
项目记忆/Codex任务队列.md
项目记忆/待确认问题.md
项目记忆/当前状态.md
项目记忆/决策记录.md
```

## 4. 输出变化

输出变化：

```text
QR-129 已确认。
DQ-46 已更新为已确认并已入队。
可执行队列新增 #154 THREAD-S0 当前代码事实扫描。
流程图和详细设计已随本计划确认联动确认。
计划路径改为 计划/20260709_运行宿主与多线程消息队列专项计划_v0.1.md。
```

未发生变化：

```text
未改 C++。
未运行构建。
未实现运行宿主线程、任务管理线程、任务工作线程、缓存统计线程、事件日志线程或外设采样线程。
未接 D455、体素、真实外设、SQL、ADO、WebView 或真实控制面板 UI。
未声明自我循环、自我苏醒或旧能力迁移完成。
```

## 5. 调用路径

```text
用户确认计划
-> 计划确认联动门禁复核
-> 移动计划到 计划/
-> 更新计划索引和项目记忆
-> 登记可执行队列 #154 THREAD-S0
-> 生成本断点清单
```

## 6. 验证方式

本确认切片验证方式：

```powershell
git diff --check
if (Test-Path .\tools\check_specs.py) { python .\tools\check_specs.py }
```

说明：

```text
本次为计划确认和入队治理，不运行 C++ 构建。
当前可执行队列仍有 #152 和 #153 待执行，#154 已登记为后续只读扫描项。
```

## 7. 下一步

执行窗口后续按队列顺序承接：

```text
#152 FS-08 非权威缓存二级材料代码实施切片
#153 FS-09 事件日志第一轮代码实施切片
#154 THREAD-S0 运行宿主与多线程消息队列当前代码事实扫描
```

若用户明确要求优先执行 #154，必须在执行窗口语义下重新确认队列顺序或明确插队依据。
