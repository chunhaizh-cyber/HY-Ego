# RECOVERY-GATE-S0 主装配接域与恢复链所有权主动预审矩阵

日期：2026-07-12

治理编号：JY-277

状态：文档治理 / 正式基线 `0e39b57` / #217 未提交代码不作为实现事实 / 未改 C++ / 未构建 / 未运行程序

## 1. 已验证事实

| 编号 | 事实 | 风险 / 裁决 |
| --- | --- | --- |
| F01 | #217 明确只验证隔离已接域四仓库组，不接入口主装配 | #226 不能仅凭 #217 完成冻结“当前运行期” |
| F02 | #246 已登记为 #217 后主装配、构造、直接删除和失败清理 S0 | 应成为恢复链运行期所有权的事实裁决点 |
| F03 | #226 假定全部权威写入口已在同一事务域 | 必须等待 #246 后继主装配接线与端到端计划完成 |
| F04 | #237 计划新建运行期上下文、空宿主和租约 | 与 #246 后继主装配上下文可能重叠，不能提前实施 |
| F05 | #229 发布许可绑定隔离候选 / 快照 / 事务身份 | 其类型必须适配 #246 裁决的唯一生产宿主合同 |
| F06 | #238 启动线程、SQL 和控制面板，#239/#230 做端到端验收 | 必须消费同一正式租约，不能以隔离候选代替生产接线 |
| F07 | #227/#228 只做严格解析和隔离重建 | 设计可以保留，但代码执行仍经 #226 传递门控 |

## 2. 新门控

```text
#217 正式完成
-> #246 执行当前事实扫描
-> 生成并完成主装配接线 / 失败清理 / 端到端接域计划
-> #246 裁决恢复链复用、修订或替代
-> 同步恢复流程图 / 详细设计 / #226-#230 / #237-#239
-> 才恢复 #226 的执行资格
```

依赖链后续仍为 `#226 -> #227 -> #228 -> #229 -> #237 -> #238 -> #239 -> #230`，但门控未满足时所有项保持设计复核门控，不记真实阻塞。

## 3. 唯一所有者

| 对象 | 唯一生产所有者要求 |
| --- | --- |
| 运行期上下文 | #246 后继正式合同唯一拥有仓库、服务和协调状态 |
| 宿主 | 全新 / 恢复两路进入同一空宿主首次发布入口 |
| 租约 | 快照、线程、SQL、控制面板只消费同一种上下文租约 |
| 快照许可 | 从已发布运行期上下文取得，不临时新建协调器 |
| 恢复候选 | 发布前隔离拥有；发布后移动进入正式上下文，不保留平行生产所有者 |

## 4. 执行前复核

#246 必须输出并登记恢复链接口归属裁决，列明 #226 / #229 / #237-#239 每项复用、修订或替代结果。若当前主装配与 #237 均声称拥有生产仓库 / 服务，或快照服务只能冻结隔离组，必须先修订上游三件套，不得修改 C++。

## 5. 三件套一致性

本矩阵、恢复流程图 MD / HTML、详细设计第 16 节、扩展后的 #246 与 #226-#230 / #237-#239 状态一致。当前只新增门控，不预判 #246 实际扫描结论。

## 6. 完成边界

只证明恢复链所有权冲突已提前识别并纳入 #246。未证明 #217、#246、快照、恢复、运行期发布、主装配接域或旧能力迁移完成。
