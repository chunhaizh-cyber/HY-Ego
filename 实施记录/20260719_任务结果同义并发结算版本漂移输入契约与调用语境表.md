# 任务结果同义并发结算版本漂移输入契约与调用语境表

日期：2026-07-19

标准检查字段：调用方、输入字段、输入来源、上游是否保证有效、允许逻辑内返回、追根因触发条件、结构变化、验证方式。下表“输入来源”同时列出输入字段，“上游保证”对应上游是否保证有效，“验证”对应验证方式；结构变化在允许返回与追根因两列中明确。

| 入口 | 调用方 | 输入来源 | 上游保证 | 允许逻辑内返回 | 追根因触发条件 | 验证 |
| --- | --- | --- | --- | --- | --- | --- |
| `需求业务服务::提交独立结算` | 任务结果结算组合器、需求任务方法组合器、自检 | 已完成任务、实际状态、动态证据、可选因果和结算幂等材料 | 句柄形状、任务完成、目标达成和证据可读 | 外部输入无效、许可暂不可得、未写入的真实版本漂移 | 同版本权威结构矛盾或写后不完整 | 入口拒绝与结构计数不变 |
| `需求任务方法数据操作::提交需求结算` | 需求业务服务 | 已形成的完整值式写入规格 | 规格完整，来源任务和证据已由服务复核 | 竞争后当前仍未结算；当前异义结算冲突 | 当前多归属、缺字段、提交后无法完整同义读回 | SERVICE-DATA-S5 同义 / 异义并发 |
| `任务结果结算组合器::处理任务执行结果` | 任务结果治理路由 | 强类型非权威回执和幂等参数 | 协议可准入后仍重读权威结构 | 正常当前性漂移、任务完成后结算暂不可用 | 权威结构内部矛盾、写后不一致 | TASK-RESULT-S1 A14/A18 |

同义请求定义为需求、来源任务、实际状态、动态证据、因果引用空值语义、结算结论、结算状态幂等主键与发生时间戳全部相同。只有这组完整材料匹配时，竞争退出才可归并为幂等读回。
