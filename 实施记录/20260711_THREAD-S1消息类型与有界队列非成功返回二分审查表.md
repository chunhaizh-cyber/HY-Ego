# THREAD-S1 消息类型与有界队列非成功返回二分审查表

日期：2026-07-12

| 节点 | 代码位置 | 返回条件 | 输入语境 | 是否上游保证有效 | 结构是否变化 | 设计是否允许 | 二分口径 | 追根因对象或逻辑返回理由 | 计划动作 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Q2A-Q13A | `有界运行消息队列.ixx:31-97` | 容量零、编号无效、承载事实、版本过期、冲突、倒退、停止后普通消息或满载 | 外部 / 测试请求 | 否 | 否 | 是 | 逻辑内返回 | 写前拒绝并返回结构化原因 | 保留 |
| Q2B-Q8B | 同上 | 上游正式流程已保证非零编号、非事实材料和合法版本后仍命中同一拒绝 | 正式线程契约 | 是 | 否 | 否 | 追根因解决 | 追查上游消息构造、版本冻结或绕过协议 | 补契约 / 修调用方 |
| Q10B-Q13B | 同上 | 停止竞争、稳定版本漂移或设计允许的满载策略 | 正式线程并发 | 是 | 否 | 是 | 逻辑内返回 | 正常并发门禁，不把候选伪失败 | 保留并验证 |
| Q18A | `有界运行消息队列.ixx:113-116` | 空队列出队 | 消费线程 | 否 | 否 | 是 | 逻辑内返回 | 当前没有消息，携带停止态 | 保留 |
| Q19B | `有界运行消息队列.ixx:118-121` | 队列非空但队首读取、弹出或停止态结果矛盾 | 内部当前结构 | 是 | 可能 | 否 | 追根因解决 | 队列内部不变式 | 后续新增诊断时进入代码计划 |
