# 任务结果同义并发结算版本漂移逐行代码映射表

日期：2026-07-19

状态：S0 当前代码事实 / 纯文档 / 未修代码

标准检查字段：文件、行号、代码行为、流程图节点、行为类型、权威性、非成功口径、纠偏建议。下表“文件与行”合并表达文件和行号，“当前有效逻辑”表达代码行为，“结论”结合流程图节点、行为类型、权威性、非成功口径与纠偏建议给出裁决。

| 文件与行 | 当前有效逻辑 | 结构变化 | 结论 |
| --- | --- | --- | --- |
| `海中鱼巣/领域/服务.需求.ixx:142-180` | 准入结算请求，复核证据、已完成任务和目标达成，形成结算状态规格并调用数据操作 | 调用数据操作前无结构变化 | 服务公开 ABI 无需修改 |
| `海中鱼巣/领域/数据操作.需求任务方法.ixx:1463-1499` | 读取需求、来源任务、动态与可选因果；写前已有正式结算时直接按完整语义判定幂等或冲突 | 无结构变化 | 写前幂等分支正确 |
| `海中鱼巣/领域/数据操作.需求任务方法.ixx:1500-1511` | 进入结构会话；会话材料与外层写前快照不匹配时把收口记为 `版本漂移` | 会话未请求提交 | 可能是竞争者已发布同义结算 |
| `海中鱼巣/领域/数据操作.需求任务方法.ixx:1512-1521` | 写前仍匹配时查状态型归属；已有归属记为 `并发占用` | 会话未请求提交 | 后续已有权威读回归并 |
| `海中鱼巣/领域/数据操作.需求任务方法.ixx:1522-1576` | 创建结算状态与关系组，完整读回后请求提交 | 成功时形成唯一正式结算 | 唯一提交路径正确 |
| `海中鱼巣/领域/数据操作.需求任务方法.ixx:1578-1583` | `内部不一致` 返回内部错误；`版本漂移` 在读取当前需求前直接返回 | 无新增结构 | 当前调度依赖偏差所在 |
| `海中鱼巣/领域/数据操作.需求任务方法.ixx:1589-1607` | `并发占用` 读取当前需求、正式结算和任务，完整同义返回幂等，异义返回冲突 | 无新增结构 | 应与无写入版本漂移共享归并规则 |
| `海中鱼巣/领域/组合.任务结果结算.ixx:346-384` | 完成任务后调用需求独立结算；版本漂移映射为待结算提示，成功映射为已完成并结算 | 组合器不直接写结构 | 数据操作返回差异传播到 A14 |
| `海中鱼巣/线程/自检.任务结果结算.ixx:464-530` | 完成组与独立结算组各 100 个同义请求；要求一个提交且全部调用收口为已完成并结算 | 隔离自检结构 | 已直接暴露调度依赖，但失败间歇出现 |
| `海中鱼巣/领域/自检.需求任务方法分层.ixx:913-946` | 8 个同义需求结算并发，要求一个提交、其余幂等 | 隔离自检结构 | 并发规模不足以稳定覆盖偏差窗口 |

映射覆盖本次纠偏的完整调用链；其它函数、纯括号和类型声明不属于 #314 修改范围。
