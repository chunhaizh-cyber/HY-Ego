# 20260706 领域服务授权写入规范确认 Codex 断点清单

## 1. 输入入口

用户确认 `规范/领域服务授权写入规范.md` 方向正确，但当前过薄，需小修后确认：

```text
补仓库正式入口和领域服务入口边界。
补跨服务依赖白名单。
补每个服务的可写结构清单。
补越权写入判定方式。
补最小验收矩阵。
```

## 2. 本切片范围

| 文件 | 处理 |
| --- | --- |
| `规范/领域服务授权写入规范.md` | 状态改为已确认规范；补仓库入口 / 领域入口边界、跨服务依赖白名单、服务写入矩阵、越权写入判定、新增服务注册规则、验收矩阵和完成声明边界。 |
| `计划/20260705_海中鱼巣全量重构总计划_v0.1.md` | 将领域服务授权写入规范状态从已建立 / 待确认推进为已确认 / 未改代码。 |
| `计划/计划索引.md` | 将领域服务授权写入规范加入当前允许推进说明和确认文档清单。 |
| `项目记忆/*` | 同步当前状态、决策、待确认问题、任务队列、验收记录和实施记录。 |

## 3. 验证

```text
文档一致性检索。
git diff --check。
git diff --cached --check。
```

本切片未改代码，未运行构建和默认入口。

## 4. 禁止扩大声明

```text
不得声明完整运行时权限系统已实现。
不得声明所有越权路径已测试。
不得声明所有领域服务依赖已代码化治理。
不得声明任务 / 方法 / 动作入口 / 需求结算 / 缓存统计完整完成。
不得声明旧项目能力已迁移。
```
