# FS-08 缓存统计结构快照增强专项生成 Codex 断点清单

日期：2026-07-07
状态：已生成逻辑迁移包归并表和专项计划 / 已同步计划索引和项目记忆 / 未改代码 / 未迁移旧函数

## 1. 输入入口

用户要求：

```text
生成下一个计划
```

根据 `计划/计划索引.md` 和当前设计队列，FS-07 动作动态因果入口增强专项之后，下一项为：

```text
FS-08 缓存 / 统计入口专项计划
```

## 2. 本次处理

```text
读取项目规则、计划索引和 FS-08 只读扫描记录。
读取非权威缓存与统计规范、非权威缓存与统计详细设计、需求结算记录规范和相关详细设计。
生成 FS-08 缓存统计入口逻辑迁移包归并表。
生成 FS-08 缓存统计结构快照增强专项 v0.1。
```

## 3. 生成文件

```text
实施记录/20260707_FS08_缓存统计入口逻辑迁移包归并表.md
计划/20260707_FS08_缓存统计结构快照增强专项_v0.1.md
实施记录/20260707_FS08_缓存统计结构快照增强专项生成_Codex断点清单.md
```

## 4. 同步状态

生成时 `计划/.计划索引.lock` 曾被执行窗口占用，因此最初没有改写计划索引和项目记忆。

本轮锁已释放，已同步：

```text
计划/计划索引.md
项目记忆/Codex任务队列.md
项目记忆/当前状态.md
项目记忆/待确认问题.md
项目记忆/实施记录.md
```

FS-08 专项计划生成时仍是待确认计划；后续已由用户确认，并已登记 FS-08 S0 到可执行队列。

## 5. 仍需确认后才能推进

```text
FS-08 专项计划本身仍待用户确认。
确认后只能登记 FS-08 S0 当前代码事实扫描。
FS-08 S1 到 S4 独立代码实施切片仍需另行生成并确认。
任何 C++ 修改、旧函数体迁移、旧字段落地或机器结构改变都仍需单独确认。
```

## 6. 禁止宣称

```text
不得声明 FS-08 代码已实现。
不得声明完整缓存系统已完成。
不得声明旧特征值哈希 / 命中字段、旧需求结算摘要、旧存在观测缓存或旧二次特征签名缓存已迁移。
不得把本生成清单单独解释为代码实施许可；确认后同步状态以 `实施记录/20260707_FS08_FS09_专项计划确认_Codex断点清单.md` 为准。
不得把缓存、统计、SQL 快照、日志摘要或控制面板显示作为业务事实。
不得接控制面板 / SQL / D455 / 体素 / 外设。
```
