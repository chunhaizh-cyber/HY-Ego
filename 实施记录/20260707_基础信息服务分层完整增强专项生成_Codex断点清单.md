# 基础信息服务分层完整增强专项生成 Codex 断点清单

日期：2026-07-07
状态：已生成 / 已确认计划 / 未改代码 / 未迁移旧函数

## 1. 输入入口

用户要求按照新规则持续生成计划，直到遇到问题。

## 2. 核心处理

基于 `规范/基础信息服务分层规范.md` 和存在、场景、状态、动态、二次特征、世界聚合服务详细设计中列出的第一轮后续缺口，生成：

```text
计划/已确认计划/20260707_基础信息服务分层完整增强专项_v0.1.md
```

同步登记该计划为已确认计划。未进入 C++ 实施，未登记可执行 S0。

## 3. 结构承载

```text
计划/已确认计划/20260707_基础信息服务分层完整增强专项_v0.1.md
实施记录/20260707_基础信息服务分层完整增强专项生成_Codex断点清单.md
计划/计划索引.md
项目记忆/Codex任务队列.md
项目记忆/当前状态.md
项目记忆/待确认问题.md
项目记忆/实施记录.md
```

## 4. 输出变化

基础信息服务分层完整增强专项已确认，确认后已登记 S0；执行 S0 仍不得修改 C++。

纳入后续路线：

```text
世界服务::创建基础信息() 兼容壳治理。
世界服务依赖注入治理。
抽象状态 / 抽象动态治理。
场景内实例状态 / 实例动态生命周期治理。
高级服务依赖基础信息原子服务复核。
```

## 5. 调用路径

```text
设计计划队列 DQ-10
-> 基础信息服务分层规范和详细设计
-> 基础信息服务分层完整增强已确认计划
-> 登记 QR-054 待确认问题
-> 同步计划索引和项目记忆
```

## 6. 验证方式

本断点清单生成阶段应执行：

```powershell
git diff --check
if (Test-Path .\tools\check_specs.py) { python .\tools\check_specs.py }
```

本次未修改 C++，不运行 MSBuild，不运行默认入口。

## 7. 禁止扩大声明

```text
不声明基础信息服务分层完整完成。
不声明世界服务、抽象状态、抽象动态或实例生命周期治理已实现。
不迁移旧函数体。
不新增旧主信息字段。
不接控制面板 / SQL / D455 / 体素 / 外设。
```
