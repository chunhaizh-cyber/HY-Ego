# 运行宿主与多线程消息队列详细设计小修确认 Codex 断点清单

日期：2026-07-10

## 1. 输入入口

用户确认 `规范/详细设计/运行宿主与多线程消息队列详细设计.md` 为“小修后确认”。

确认口径：

```text
实时性要求高，能并行则默认并行。
可并行且不破坏机器事实顺序、入口拒绝、领域服务边界和写入复核的路径，默认设计为多线程或异步队列。
线程只负责调度、搬运、等待和耗时计算，不成为动作来源。
消息和队列只承载请求、材料、回执和调度信号，不承载机器事实。
权威写入必须回到领域服务或仓库正式入口复核。
```

## 2. 修改范围

已修改：

```text
规范/详细设计/运行宿主与多线程消息队列详细设计.md
计划/计划索引.md
项目记忆/当前状态.md
项目记忆/决策记录.md
项目记忆/待确认问题.md
项目记忆/验收记录.md
实施记录/20260710_运行宿主与多线程消息队列详细设计小修确认_Codex断点清单.md
```

未修改：

```text
海中鱼巣/*
海中鱼巣.vcxproj
海中鱼巣.sln
海中鱼巣.slnx
流程图/*
tools/*
```

## 3. 小修收口

已补齐：

```text
可并行判定条件。
句柄版本过期处理。
幂等键冲突处理。
同一任务顺序裁决。
任务调度、任务回执、缓存统计、事件日志、外部材料五类队列满载策略。
五类队列优先级策略和停止消息最高优先级。
异步事件日志持久化声明边界。
停止收口顺序。
禁止持锁等待队列、join、外设、flush 和回调的防死锁规则。
外设采样材料线程第一轮只允许模拟或空采样材料。
```

## 4. 状态同步

已同步：

```text
计划索引：运行宿主与多线程消息队列专项计划行补充详细设计小修后确认状态。
当前状态：记录详细设计小修后确认和禁止扩大声明。
决策记录：新增 JY-159。
待确认问题：新增并关闭 QR-133。
验收记录：新增“运行宿主与多线程消息队列详细设计小修确认”验收项。
```

## 5. 验证

已运行：

```powershell
git diff --check
python .\tools\check_specs.py
```

结果：

```text
git diff --check：通过；仅提示 Git 工作区 LF/CRLF 换行转换 warning，未发现空白错误。
python .\tools\check_specs.py：通过，输出“海中鱼巣规范治理检查: 未发现问题”。
```

## 6. 完成声明边界

本切片只能声明：

```text
运行宿主与多线程消息队列详细设计已按小修要求确认。
已完成文档修订、计划索引和项目记忆同步。
```

不得声明：

```text
C++ 已修改。
构建已运行。
线程已启动。
消息队列已实现。
真实外设已接入。
自我线程、自我循环或自我苏醒已完成。
完整任务状态机、真实执行桥、完整缓存系统或事件日志持久化恢复已完成。
旧函数或旧能力已迁移。
```
