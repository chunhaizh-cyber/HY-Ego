# 20260706 最小闭环验收用例小修确认 Codex 断点清单

## 1. 输入入口

用户确认 `规范/验收/最小闭环验收用例.md` 方向基本可确认，但要求先完成小修：

```text
处理 `规范/最小闭环验收用例.md` 旧版同名文件冲突。
修正 MC-02 的比较方式 / 验证方式旧表述。
修正 MC-08 的旧结算记录、旧当前材料、旧目标材料和旧比较规则表述。
同步修订 `规范/最小闭环设计.md` 中旧的需求输入口径。
补 MC-03 任务状态发生时间戳。
补 MC-05 当前方法执行入口拒绝项。
```

## 2. 本次处理

| 文件 | 处理 |
| --- | --- |
| `规范/验收/最小闭环验收用例.md` | 状态改为已确认验收用例；MC-02、MC-03、MC-05、MC-08 同步到目标状态、发生时间戳、方法执行拒绝项和轻量因果口径。 |
| `规范/最小闭环验收用例.md` | 改为导航文件，明确当前权威文件是 `规范/验收/最小闭环验收用例.md`，旧版目标值和来源方法回查口径废止。 |
| `规范/最小闭环设计.md` | 把创建需求输入从目标值 / 比较方式 / 验证方式收束为目标状态 / 满足方式 / 验证读取路径；第一轮 DV-1 固定为当前特征状态材料 >= 目标状态值。 |
| `规范/详细设计/写入事务详细设计.md` | 同步最小闭环事务表，把需求触发事务和结算事务改为目标状态、当前特征状态材料和结算状态发生时间戳口径。 |
| `计划/计划索引.md` | 确认文档清单中将最小闭环验收用例标为已确认 / 未改代码。 |
| `项目记忆/*` | 同步当前状态、决策、待确认问题、任务队列、实施记录和验收记录。 |

## 3. 当前结论

```text
最小闭环验收用例已确认。
本次未改代码。
本次未重新构建。
本次不声明完整验收体系完成。
本次不改变任务系统第一轮代码实施门禁。
```

## 4. 验证计划

```text
检索旧口径残留。
执行 git diff --check。
仅提交本切片相关文档。
提交并推送当前分支。
```

## 5. 下一步

本窗口若继续以设计和确认为主，应优先检查并处理仍处于待确认状态且已经可确认的规范：

```text
规范/节点类型与关系类型枚举规范.md
规范/错误结果与入口拒绝规范.md
规范/领域服务授权写入规范.md
规范/UTF8与中文标识符工程规范.md
```

代码实施队列仍为：

```text
任务系统第一轮代码实施
```
