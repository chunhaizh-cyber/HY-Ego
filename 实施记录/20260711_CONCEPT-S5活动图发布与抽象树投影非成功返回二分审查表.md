# CONCEPT-S5 活动图发布与抽象树投影非成功返回二分审查表

日期：2026-07-11

状态：#199 纠偏后代码事实

| 节点 | 代码位置 | 返回条件 | 输入语境 | 是否上游保证有效 | 结构是否变化 | 设计是否允许 | 二分口径 | 追根因对象或逻辑返回理由 | 计划动作 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| S5-03A | `初始化活动图基础版本` | 探测时四根不足 | 任意探测 / 测试 | 否 | 否 | 是 | 逻辑内返回 | 初始化条件尚未满足 | 保留 |
| S5-03B | `发布候选图版本并返回结果` | 正式发布无法建立基础版本 | 正式发布 | 是 | 否 | 否 | 追根因解决 | 状态为基础活动图异常 | 已纠偏 |
| S5-09 | 同上 | 初始化成功后快照不存在 | 正式发布 | 是 | 否 | 否 | 追根因解决 | 活动快照生命周期异常 | 已纠偏 |
| S5-10 | 同上 | 同内容候选重复提交且快照完整 | 正式或测试 | 是 / 否 | 否 | 是 | 正常成功 | 状态为已是当前版本 | 保留 |
| S5-11 | 同上 | 基准活动版本过期 | 候选材料 | 否 | 否 | 是 | 逻辑内返回 | 乐观并发候选过期 | 已独立分类 |
| S5-12A | `候选图可发布_已加锁` | 候选未收敛 | S4 候选材料 | 否 | 否 | 是 | 逻辑内返回 | 状态为候选未收敛 | 已独立分类 |
| S5-12B | 同上 | 候选项为空 | 候选材料 | 否 | 否 | 是 | 逻辑内返回 | 状态为候选为空 | 已独立分类 |
| S5-12C | 同上 | 已收敛候选编号、签名、根或边不合法 | S4 正式候选 | 是 | 否 | 否 | 追根因解决 | 状态为候选内部错误 | 已纠偏 |
| S5-15A | `创建候选概念节点` | 已准入类别未知或原子服务返回无效句柄 | 正式发布写入 | 是 | 可能已开始写 | 否 | 追根因解决 | 首次异常点诊断，状态为概念创建异常 | 已纠偏 |
| S5-15B | `构造候选活动快照_已加锁` | 生命周期确保失败 | 正式发布写入 | 是 | 是 | 否 | 追根因解决 | 状态为生命周期异常 | 已纠偏 |
| S5-18 | 同上 | 映射缺失、关系确保或追加失败 | 正式发布写入 | 是 | 可能是 | 否 | 追根因解决 | 状态为映射或关系异常 | 已纠偏 |
| S5-19 | `发布候选图版本并返回结果` | 新快照完整读回失败 | 正式发布写入 | 是 | 是后清理 | 否 | 追根因解决 | 状态为发布前读回异常 | 已纠偏 |
| S5-清理 | `清理未发布候选_已加锁` | 本轮关系或节点未清理 | 正式失败收口 | 是 | 是且可能残留 | 否 | 追根因解决 | 状态为未发布结构清理异常，并返回残留句柄 | 已纠偏 |
| S5-21 | `失效旧活动关系_已加锁` | 新快照提交后旧关系失效失败 | 正式发布 | 是 | 新版本已提交 | 否 | 追根因解决 | 状态为已提交但旧关系失效异常；返回新版本和异常旧关系 | 已纠偏 |
| TREE-02A | `读取抽象树视图并返回结果` | 任意单树请求时无快照 | 请求 / 测试 | 否 | 否 | 是 | 逻辑内返回 | 状态为活动图未就绪 | 已纠偏 |
| TREE-02B | `读取全部抽象树视图并返回结果` | 正式读取无快照或四根不全 | 正式 PANEL 契约 | 是 | 否 | 否 | 追根因解决 | 状态为正式契约异常 | 已纠偏 |
| TREE-03 | `生成抽象树视图` | 非活动根或预算为零 | 任意请求 | 否 | 否 | 是 | 逻辑内返回 | 状态为非活动根或零预算 | 已独立分类 |
| TREE-06A | 同上 | 节点或深度预算耗尽 | 任意或正式请求 | 否 | 否 | 是 | 逻辑内返回 | 状态为预算耗尽，不输出半树 | 已独立分类 |
| TREE-06B | 同上 | 路径为空、活动图成环或最终视图不完整 | 已验证活动快照 | 是 | 否 | 否 | 追根因解决 | 状态为活动图内部错误 | 已纠偏 |

## 统计

```text
逻辑内返回：7 类（初始化探测不足、过期基准、未收敛、空候选、任意读取未就绪、非活动根 / 零预算、预算耗尽）。
追根因解决：12 类（正式基础图异常、快照丢失、收敛候选内部错误、创建、生命周期、映射 / 关系、发布前读回、清理、提交后旧边、正式四树契约、路径 / 环、最终视图不完整）。
正常成功但需独立标明：重复候选幂等返回当前版本。
```
