# 流程图中途异常口径修订计划确认入队 Codex 断点清单

日期：2026-07-09

## 1. 输入入口

用户确认：

```text
确认 20260709_流程图中途异常口径修订计划_v0.1
```

确认对象：

```text
计划/20260709_流程图中途异常口径修订计划_v0.1.md
```

## 2. 核心处理

本次只做计划确认和入队，不执行修订：

```text
从待确认计划移入正式计划。
将计划状态改为已确认 / 已登记可执行队列 #155 / 未执行。
登记 `项目记忆/Codex任务队列.md` #155。
同步计划索引、当前状态、待确认问题和决策记录。
```

## 3. 上游联动说明

本计划不对应单一上游流程图或单一详细设计；它是基于规则入口和统计表生成的同步修订计划。

替代依据为：

```text
AGENTS.md
规范/000_项目规则总纲.md
规范/001_规则迁移清单.md
规范/错误结果与入口拒绝规范.md
实施记录/20260709_流程图中途异常口径统计表.md
实施记录/20260709_流程图二分口径硬规则同步_Codex断点清单.md
```

执行目标为计划列出的既有流程图、HTML、详细设计和代码允许文件；执行窗口必须在改代码前读取对应代码文件头部规则注释。

## 4. 结构承载

确认后计划路径：

```text
计划/20260709_流程图中途异常口径修订计划_v0.1.md
```

队列登记：

```text
项目记忆/Codex任务队列.md #155
```

状态同步：

```text
计划/计划索引.md
项目记忆/当前状态.md
项目记忆/待确认问题.md
项目记忆/决策记录.md
```

## 5. 输出变化

1. `FLOW-EX-Q1` 已按用户确认结论执行：本计划从“只修流程图”提升为“同步修流程图、详细设计和代码”。
2. `FLOW-EX-Q2` 已按用户确认结论执行：统计表只保留“追根因解决 / 逻辑内返回”两类。
3. `FLOW-EX-Q3` 保持计划默认建议：若追根因类需要新增事务层或结构化错误结果公共类型，不在本计划内直接实现，先登记待确认问题并另建专项。
4. 本次未执行 #155，未修改流程图正文、详细设计或 C++，未运行构建，未迁移旧函数。

## 6. 调用路径

```text
用户确认
-> 待确认计划状态更新
-> 移入计划/
-> Codex任务队列 #155
-> 计划索引 / 项目记忆 / 决策记录同步
```

## 7. 验证方式

已执行：

```powershell
git diff --check
& 'C:\Users\zhchh\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' .\tools\check_specs.py
```

不运行 C++ 构建；本次未改 C++。

结论：

```text
git diff --check 通过，仅有 Windows 换行转换提示。
tools/check_specs.py 通过，未发现问题。
状态一致性扫描未发现 `QR-131` 待确认或旧待确认计划路径残留；#155 已成为当前最新已确认待执行项。
```
