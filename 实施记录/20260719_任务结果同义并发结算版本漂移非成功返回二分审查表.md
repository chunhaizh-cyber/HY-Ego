# 任务结果同义并发结算版本漂移非成功返回二分审查表

日期：2026-07-19

标准检查字段：节点、代码位置、返回条件、输入语境、是否上游保证有效、结构是否变化、设计是否允许、二分口径。下表“节点”对应代码位置与返回条件，“当前条件”补充输入语境和上游有效性，“结构变化”与“二分口径”明确设计是否允许。

| 节点 | 当前条件 | 结构变化 | 二分口径 | 纠偏动作 |
| --- | --- | --- | --- | --- |
| 写前已有异义正式结算 | 当前正式结算与请求不匹配 | 无 | 逻辑内返回 | 保持幂等冲突 |
| 会话写前材料不匹配，当前仍无正式结算 | 真实版本漂移或许可路径变化 | 无 | 逻辑内返回 | 保留版本漂移，不自动重试 |
| 会话写前材料不匹配，当前已有完整同义正式结算 | 竞争者已完成同一请求 | 仅竞争者已写，当前调用未写 | 逻辑内完成 | 读回后归并为幂等读回 |
| 会话写前材料不匹配，当前已有异义正式结算 | 竞争者发布不同语义 | 仅竞争者已写 | 逻辑内拒绝 | 返回幂等冲突，不覆盖 |
| 并发占用且当前完整同义 | 已有状态型归属 | 当前调用未写 | 逻辑内完成 | 保持幂等读回 |
| 已请求提交但结构结果非已提交或读回不匹配 | 写后承载不完整 | 已进入结构写入 | 追根因解决 | 返回内部不一致，停止后继 |
| 当前需求多归属、正式结算缺字段或来源任务矛盾 | 权威结构内部矛盾 | 已存在权威结构 | 追根因解决 | 不降级为版本漂移或待结算提示 |
