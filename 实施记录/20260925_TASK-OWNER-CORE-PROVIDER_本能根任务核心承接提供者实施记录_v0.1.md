# TASK-OWNER-CORE-PROVIDER 本能根任务核心承接提供者实施记录

日期：2026-09-25
计划：`TASK-OWNER-CORE-PROVIDER` v0.2
施工起点：`1bff7892ccf9f1f13e4ee1166d0ffc8f47fcfda6`
执行所有者：当前交互智能体代码切片

## 1. S0 与边界

- `main == origin/main == 1bff7892ccf9f1f13e4ee1166d0ffc8f47fcfda6`，index 在施工前为空。
- 计划索引登记状态为`可执行`，计划精确 blob 为`56e8668088bc1b2888c02ceb939d3834290f94c6`。
- 三份目标业务流程图属于异主 WIP，本切片未修改、未暂存。
- 本切片未修改规范、计划、流程图、知识库或 L1，未增加第二 owner、第二账或测试钩子。

## 2. 实际变更

1. 新增本能根 task owner v1 头源实现：结构登记、不可变初始化包、高水位预留、核心任务、P1、Vt 首迁移、当前/历史目标投影、退出和原键首次材料重放。
2. 核心、P1、首迁移和退出均以 L1 原幂等键的首次规范化写集、首次写入结果和冻结本地键映射为唯一重放权威；task owner 回执只保存提交前已知索引。
3. 核心回执只生成和接受分支1、2；值3没有生成或接受路径。
4. 根材料当前与历史读取只核验需求 owner 自有的 `D→F`、`D→目标合同`、`L→D` 三组关系恰一，允许 task owner 的合法任务引用并存。
5. 核心首次结果逐项绑定 Vt、R1、三个初始属性值、六条任务关系和回执事实；P1、首迁移、退出分别核验完整首次写集、正式回执和发布事实闭包。
6. 退出闭包同时证明旧 `L→当前T`、旧生命周期值已经退出，新生命周期值进入当前槽，退出回执正式存在。
7. 提交竞争返回 L1 精确重复时保留精确重复阶段；提交调用开始后无法证明结果时优先返回`已可能发布`；非成功承接或退出结果清空可选投影。
8. 普通应用唯一持有并公开非 const `本能根任务核心端口_v1&`；工程登记新头源和专项验证，退出旧任务 v3 文件、唯一旧消费者及存在类旧任务专用符号。

## 3. 验证结果

- 专项 Debug x64 隔离 Rebuild：通过，0 错误。
- 专项 Release x64 隔离 Rebuild：通过，0 错误。
- 根工程 Debug x64 隔离 Rebuild：通过，0 错误。
- 根工程 Release x64 隔离 Rebuild：通过，0 错误。
- Debug 专项新库 seed/recover：通过。
- Release 专项新库 seed/recover：通过。
- 旧持久库 `runtime-20260925-1520` recover：通过；已证明 task owner 合法任务引用不再破坏本能根锚点恢复。
- 运行覆盖：安全根、服务根、三阶段形成与精确重放、同根第二意图承接既有当前任务及重放、P1/首迁移之前的历史目标投影、退出及退出重放、跨进程包和任务恢复。
- 静态检查：旧任务专用抽样符号零命中；核心回执分支3生成/接受零命中；临时 TRACE 零命中。
- `git diff --check`：通过；仅报告异主流程图及本切片既有文件的换行提示，无空白错误。

未执行具名集成验收；上述结果不证明普通需求任务、多D、完整5260、self 调用、manager、筹办、治理门或完整自我治理闭环。

## 4. 代码知识变更清单

### 新增公开稳定函数

- `本能根任务核心服务_v1::登记结构(...)`：登记 task owner 私有结构；不负责普通需求任务或治理推进。
- `本能根任务核心服务_v1::签发或恢复不可变初始化包(...)`：按完整语义请求预留稳定阶段键并恢复原包。
- `本能根任务核心服务_v1::承接或建立任务(...)`：按三阶段建立或承接单根当前任务。
- `本能根任务核心服务_v1::恢复任务初始化(...)`：复用承接入口并优先按原阶段键重放。
- `本能根任务核心服务_v1::按任务读取核心(...)`：按当前截止读取任务核心。
- `本能根任务核心服务_v1::按查询锚点读取当前任务(...)`：按 L 查询唯一当前任务。
- `本能根任务核心服务_v1::按任务与显式截止读取目标投影(...)`：从需求、self、准确特征及采用事实重建当前/历史根目标。
- `本能根任务核心服务_v1::退出任务当前资格(...)`：原子退出当前关系和旧生命周期值，写入新生命周期及退出回执。
- `本能根任务核心()`：普通应用非 const 端口 getter，不转移 provider 所有权。
- `需求类数据服务::读取本能根历史材料(...)`及对应成功谓词：在显式 `Gread/H` 下读取根需求历史材料。

### 修改的稳定函数

- `需求类数据服务`既有当前根材料读取：闭包从“全部当前引用集合”收窄为三组需求 owner 自有关系恰一；不枚举或拒绝其它 owner 引用。
- 普通应用构造/释放路径：增加 task owner 结构登记、服务唯一构造和销毁。

### 退出函数与文件

- 删除旧`数据服务.任务类.h`公开任务 v3 能力。
- 删除旧`应用服务.任务目标方向.h`唯一旧消费者。
- 删除`数据服务.存在类.h`中计划逐符号冻结的旧任务专用入口和 helper；方法 Vm 能力保留。

共享`知识库/函数用途/**`和知识状态未由本执行切片修改。发布后应由代码知识库维护者基于结果提交新建/更新上述用途卡，并把旧任务 v3 用途标记退出。

## 5. 发布

结果提交：本实施记录所在提交。普通推送结果由完成回传记录。
