# DYNAMIC-CAUSAL-S0 无令牌正式接口复核与重新发布 Codex 断点清单

日期：2026-07-16

交互：WIN-0001-E80

计划 / 队列：#231 / DQ-123 / DYNAMIC-CAUSAL-S0

决策：JY-362

基线：`f7e56405c05449e91edc051a674720164ed529c0`

状态：设计修订完成 / 纯只读 S0 重新待执行 / 未修改 C++ / 未构建 / 未运行程序

## 1. 接收事实

1. #261 / DQ-153 / 830 已完成归档；本轮不重复执行或修改其代码。
2. #224、#225、#271 已完成，正式运行期业务公开面不暴露原始结构事务令牌。
3. `任务执行冻结请求` 已包含调度、任务、方法和动作完整值式材料；新版 `任务结果回执材料` 保存完整 `来源执行请求`，并携带回执、任务序号、批次、时间戳及任务、方法、动作、状态、动态、场景和主体句柄。
4. 上述材料可形成非权威观察回合值式身份候选，但当前没有权威“任务尝试节点”。
5. 动态、状态、特征和二次特征业务服务通过各自数据操作内部取得共享许可并返回值式材料；公开业务入口不接受原始令牌。
6. 二次特征有序组成项只证明结构位置；当前代码未证明顺序号是跨实例稳定语义角色。
7. 阶段 630 / 640 / 650 与 `服务.动态模式`、`算法.动态模式`、三份对应自检真模块当前均未登记。

## 2. 漂移裁决

JY-278 与旧 #231 把 #217 的“一次共享许可、同一原始令牌贯穿多个生产服务”当作正式接口。该合同已被 JY-316 / JY-317 和 #271 正式无令牌边界撤回，不能继续作为 #231 验收或 #232 实施依据。

JY-362 固定：

```text
公开组合器只持有冻结请求和完整值式材料
-> 分别调用动态 / 状态 / 特征 / 二次特征业务服务
-> 复核完整句柄、版本、关系状态、有效端点和时间范围
-> 能证明同一观察材料才形成非权威候选
```

如果分别读取不能排除混合快照，#231 必须记录服务专用只读会话或批量读取边界需求；不得让调用方携带原始令牌、直接访问四仓库或特征值服务。

## 3. 计划修订

原 #231 保留编号和纯只读性质，主要修订如下：

1. 前置改为已完成的 #224、#225、#271，删除 #217 当前前置。
2. 扫描范围加入现行任务协议、两条路由、两条组合器、正式业务服务、数据操作模块和唯一运行期业务装配；旧 `.h` 只作为兼容调用点证据。
3. 输出改为非权威回合值式身份、逐服务值式读取、完整句柄 / 版本 / 端点复核和跨服务一致性缺口矩阵。
4. 明确不得发明权威任务尝试节点；二次特征角色未证明时，#232 版本 1 角色部分固定为空。
5. A13 改为无令牌公开面和跨服务一致性边界验收，不再要求同一令牌贯穿服务。
6. #231 完成后先重做 #232 流程图、详细设计和计划；原 630 合同不得直接实施。

本计划不改变机器流程，因此新增流程图和详细设计不适用。第三批旧流程图与详细设计只作为扫描问题来源；JY-362 和当前代码优先裁决原始令牌漂移。

## 4. 执行边界

允许：只读代码、计划、规范、工程登记和实施记录；生成正式事实复核断点并同步计划治理文件。

禁止：修改 `海中鱼巣/**`、工程、规范、流程图或持久化文件；运行构建、程序、自检或测试；实现 #232；恢复或删除任何 stash；恢复 #214。

## 5. 后继顺序

```text
#231 纯只读事实复核
-> 基于正式事实重做 #232 三件套
-> #232
-> #233
-> #234
-> #235 纯只读用途 / 学习所有权复核
```

恢复链 #226-#230 / #237-#239、第二批自检 #251-#254 和状态生命周期 #255 继续按原前置门控，不因 #231 重新发布自动解锁。

## 6. 设计验证

```powershell
python .\tools\check_specs.py --strict
git diff --check
```

本轮只验证文档与治理一致性，不运行 C++ 构建或程序。
