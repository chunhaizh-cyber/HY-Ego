# DD-05F0 v0.3 特征批次自检超时诊断与续作记录

## 1. 执行身份

- 计划 / 队列：`#329 / DQ-221 / DD-05F0 v0.3`
- 设计接受提交：`9b7e4023cb78ca99e335ada9fbd2b806c4964891`（JY-577 / E318）
- 续作起点：`f3dc981c20903f17f39f60934c4be49242e6e7e5`
- worktree：`C:\Users\zhchh\.codex\worktrees\329-dd05f0-feature-current-txn-r2\海中鱼巢`
- 分支：`codex/329-dd05f0-feature-current-txn-r2`
- 全新 S0 已由设计正式接受；S1 开始时 HEAD、upstream 和远端同名分支均为续作起点，0/0、worktree / index clean、残留进程 0。

## 2. S1 粗分段合同

临时检查点仅写入 `自检.特征体系分层.ixx`，位于 Debug 自检宏内，使用 `std::cerr` 与 `std::endl` 立即刷新。检查点只观察位置和单元内耗时，不改变请求、返回值、分支、异常、锁序、许可、线程数量、等待方式、结构或业务结果。

| 检查点 | 范围 |
| --- | --- |
| P0 | 夹具仓库、执行器、服务构造和基础节点 |
| P1 | 旧特征定义、槽位、初始值、组合和读回 |
| P2 | 旧拒绝、故障撤销和普通结构写点 |
| P3 | 旧并发竞争、工作线程和 join |
| P4 | 新批次正常、历史、同义与异义 |
| P5 | 新批次漂移、双参与者故障、分类与许可竞争 |
| P6 | A17 事务外预读、抢先发布和写权内重读 |
| P7 | 具名输出和函数返回前 |

根因当前仍未知。只有两轮独立 180 秒完整自检取得相同最后 `BEGIN` 且缺少对应 `END`，才进入 S2；在稳定细分叶子形成前不修改业务逻辑、不延长时限、不宣称死锁根因。

## 3. S1 两轮粗分段证据

- Debug x64 当轮 Rebuild：通过，`0 warning / 0 error`。
- 第 1 轮：PID `53208`，`180073 ms` 超时，精确终止后残留进程 0；stdout `30565` 字节，stderr `624` 字节。
  - stdout：`C:\Users\zhchh\AppData\Local\Temp\DD05F0-v03-S1-R1-ea737bce01c44aceb6bc3a9bc3a12c66.stdout.log`
  - stderr：`C:\Users\zhchh\AppData\Local\Temp\DD05F0-v03-S1-R1-ea737bce01c44aceb6bc3a9bc3a12c66.stderr.log`
  - 最后完整检查点：`P4 END batch-normal-idempotency elapsed-ms=111`
  - 首个缺失 END：`P5 BEGIN batch-drift-fault-classification elapsed-ms=112`
- 第 2 轮：PID `34140`，`180062 ms` 超时，精确终止后残留进程 0；stdout `30565` 字节，stderr `625` 字节。
  - stdout：`C:\Users\zhchh\AppData\Local\Temp\DD05F0-v03-S1-R2-d0b68119a6c24bb8ba6ac1aea3432191.stdout.log`
  - stderr：`C:\Users\zhchh\AppData\Local\Temp\DD05F0-v03-S1-R2-d0b68119a6c24bb8ba6ac1aea3432191.stderr.log`
  - 最后完整检查点：`P4 END batch-normal-idempotency elapsed-ms=112`
  - 首个缺失 END：`P5 BEGIN batch-drift-fault-classification elapsed-ms=113`
- 两轮均有 P0—P4 成对 BEGIN / END，且均以 P5 BEGIN 为最后检查点；满足进入 S2 的稳定粗分段门禁。
- S2 只细分 P5 的四类漂移提交、故障夹具 / 规格、五个故障配置及其读回、记录分类和批次账许可竞争调用；其它阶段保持原粗检查点。

## 4. S2 两轮细分证据

初次 P5 细分在漂移提交前仍只有 P5 BEGIN，继续按原段细分规则把漂移前复合数量读取以及四个漂移请求的本地形成 / 版本改写纳入成对检查点。最终固定检查点树当轮 Debug x64 Rebuild 通过，`0 warning / 0 error`。

- 叶子第 1 轮：PID `40536`，`180078 ms` 超时，精确终止后残留进程 0；stdout `30565` 字节，stderr `683` 字节。
  - stdout：`C:\Users\zhchh\AppData\Local\Temp\DD05F0-v03-S2L-R1-d0aef66bfc314d0a8932ce704ab4d1c9.stdout.log`
  - stderr：`C:\Users\zhchh\AppData\Local\Temp\DD05F0-v03-S2L-R1-d0aef66bfc314d0a8932ce704ab4d1c9.stderr.log`
  - 最后完整 END：`P4 END batch-normal-idempotency elapsed-ms=95`
  - 首个 BEGIN 无 END：`P5 BEGIN form-source-drift-request elapsed-ms=98`
- 叶子第 2 轮：PID `55064`，`180076 ms` 超时，精确终止后残留进程 0；stdout `30568` 字节，stderr `686` 字节。
  - stdout：`C:\Users\zhchh\AppData\Local\Temp\DD05F0-v03-S2L-R2-3cabc3c1bfa542579a1658b34865b525.stdout.log`
  - stderr：`C:\Users\zhchh\AppData\Local\Temp\DD05F0-v03-S2L-R2-3cabc3c1bfa542579a1658b34865b525.stderr.log`
  - 最后完整 END：`P4 END batch-normal-idempotency elapsed-ms=101`
  - 首个 BEGIN 无 END：`P5 BEGIN form-source-drift-request elapsed-ms=104`
- 对应符号：自检局部 lambda `形成单项换代请求(...)`；首条语句为 `多换代结果.项目组[0]`。两轮已稳定证明该调用未返回，但尚未仅凭此证据把项目组为空或具体业务失败分类写成事实。
- S3 分类前增加一次不改变分支的立即刷新形态检查点，只记录多换代结果的提交状态类别和项目数量类别；仍使用固定 180 秒边界。

## 5. S3 根因分类、第一违反点和修复前置

分类探针使用固定 180 秒边界运行：PID `17272`，`180080 ms` 超时，精确终止后残留进程 0；stdout `30565` 字节，stderr `817` 字节。

- stdout：`C:\Users\zhchh\AppData\Local\Temp\DD05F0-v03-S3C-R1-675734cd2b7e4109be17c7aa1bc41a87.stdout.log`
- stderr：`C:\Users\zhchh\AppData\Local\Temp\DD05F0-v03-S3C-R1-675734cd2b7e4109be17c7aa1bc41a87.stderr.log`
- 立即刷新状态证据：`P5 END multi-replace-not-committed-size-0 elapsed-ms=98`。
- 随后的稳定未返回点：`P5 BEGIN form-source-drift-request elapsed-ms=99`。

已证实根因：`多换代结果` 未提交且 `项目组` 为空；自检局部 lambda `形成单项换代请求(...)` 无条件读取 `多换代结果.项目组[0]`。该无保护索引是本次完整自检不返回的第一违反点。它属于自检调用组织错误；现有证据不证明生产锁、许可或参与者生命周期死锁，也不证明多换代业务失败本身的后续根因。

预期行为：当多换代没有形成两个完整结果项时，自检必须保留对应验收失败并跳过依赖该结果形成的四类漂移派生请求，继续运行后续故障、分类、A17 和具名输出；不得对空项目组索引，也不得把多换代失败改写为成功。

最小改动：只在 `自检.特征体系分层.ixx` 内以“状态为已提交且项目数为 2”作为派生漂移自检前置。前置不成立时 `批次验收[8]` 保持失败，不形成或提交漂移请求；不修改生产代码、4040 / 4170、公开接口、模块依赖或文件所有权。

复验条件：删除无保护索引后，Debug 完整自检必须在 180 秒内返回或形成新的具名证据；若出现新的未返回点或计划内失败，再按当前合同定向处理。最终仍须清除全部 `DD05F0-DIAG` 并完成 Debug / Release、A01—A17 和全部静态 / Git 门禁。

## 6. S3 完整返回后的第二层根因分类

删除无保护索引后，Debug x64 当轮 Rebuild 通过，`0 warning / 0 error`。完整自检 PID `3756` 在 `13543 ms` 内正常退出，退出码 `1`，残留进程 0；P0—P7 均成对完成，证明本次 180 秒超时第一违反点已解除。

- stdout：`C:\Users\zhchh\AppData\Local\Temp\DD05F0-v03-S3V-R1-f2b4e0f35f524e16ba1596f10b4b6be6.stdout.log`
- stderr：`C:\Users\zhchh\AppData\Local\Temp\DD05F0-v03-S3V-R1-f2b4e0f35f524e16ba1596f10b4b6be6.stderr.log`

### 6.1 多换代及其级联失败

具名失败为 `DD05F0-A02—A05、A07、A09`。只读调用链复核证明：

1. `数据操作.特征体系.ixx` 的换代候选在 `失效已发布关系(...)` 成功后，只检查操作成功，没有使用写后句柄调用 `会话.读取关系审计(...)`。
2. 结构写入会话要求全部关系变更写集完成权威读回后才允许 `请求提交()`；缺失写后审计读回使请求提交返回 false。
3. 执行器完整撤销后返回入口拒绝，特征批次结果最终映射为 `内部不一致` 且项目组为空，精确对应诊断事实。
4. 多初始不包含已发布关系变更所以通过；混合批次、历史保留、发布前后读回、私有桥和四类漂移失败均由多换代未提交级联形成。

已证实根因：换代生产路径遗漏失效关系写后审计读回，违反既有写集完整读回合同。预期行为是在同一会话内读取写后句柄，严格确认已失效记录的关系编号、版本、源节点和目标节点仍与换代规格一致，再继续形成新值；不改变 4040 / 4170、公开接口或模块依赖。

最小改动：只修改 `海中鱼巣/领域/数据操作.特征体系.ixx`。复验条件：`DD05F0-A02—A05、A07、A09` 全部通过，且 A01、A06、A08、A10—A17 保持通过。

### 6.2 核心多参与者矩阵失败

具名失败为 `DD05F0-CORE-A02、A04—A08、A10`。只读调用链复核证明：

1. 生产执行器的登记序准备、确认、发布、逆序撤销和撤销失败隔离路径不存在当前失败证据；A01、A03、A09 已通过。
2. 核心自检夹具 `运行多参与者收口场景(...)` 创建主信息候选、节点候选并绑定主键后，直接忽略 `请求提交()` 返回值。
3. 该夹具没有先调用 `主信息可读(...)`、`节点可读(...)` 和 `主键绑定匹配(...)` 完成写集权威读回，因此请求提交返回 false；执行器进入“未请求提交且未显式撤销”入口拒绝，准备、确认、故障定位和隔离路径均未进入。

已证实根因：自检夹具未满足既有会话完整读回前置，不是生产执行器顺序语义错误。预期行为是三项权威读回全部成立后才请求提交；不改变执行器、4040 / 4170 或生产接口。

最小改动：只修改 `海中鱼巣/核心/自检.结构写入会话.ixx`。复验条件：`DD05F0-CORE-A01—A10` 全部通过，再以完整 Debug 自检确认没有第二层失败。

## 7. S3 定向修复结果

- `数据操作.特征体系.ixx`：换代失效旧当前关系后，读取写后审计记录并核对已失效状态、写后关系编号 / 版本、源槽位和预期当前值，随后才继续形成新值。
- `核心/自检.结构写入会话.ixx`：多参与者夹具在请求提交前完成候选主信息、候选节点和主键绑定三项权威读回。
- `领域/自检.特征体系分层.ixx`：保留“多换代已提交且项目数为 2”保护，前置不成立时跳过依赖结果的漂移派生请求并保持验收失败，防止空项目组索引。

修复后 Debug x64 当轮 Rebuild 通过。完整自检 PID `50768` 在 `14063 ms` 内退出码 0，残留进程 0；`DD05F0-A01—A17` 与 `DD05F0-CORE-A01—A10` 全部通过。

- stdout：`C:\Users\zhchh\AppData\Local\Temp\DD05F0-v03-S3V-R2-35ac6671746c4115929aa5c93a858d7c.stdout.log`
- stderr：`C:\Users\zhchh\AppData\Local\Temp\DD05F0-v03-S3V-R2-35ac6671746c4115929aa5c93a858d7c.stderr.log`

## 8. S4 临时诊断清理

- 已删除全部 P0—P7、细分检查点、状态标签和只为定位形成的辅助代码。
- `rg -n "DD05F0-DIAG" .\海中鱼巣`：零命中，退出码 1。
- 正式 A01—A17、自检空项目组保护及历史诊断日志均保留。

## 9. S5 最终验证

### 9.1 双配置构建和完整自检

- Debug x64 当轮 Rebuild：成功，MSBuild 退出码 0。
- Debug 完整自检：PID `46336`，`13508 ms`，退出码 0，stderr 0 字节，残留进程 0；`DD05F0-A01—A17` 共 17 项、`DD05F0-CORE-A01—A10` 共 10 项全部通过，完整输出具名失败 0。
  - stdout：`C:\Users\zhchh\AppData\Local\Temp\DD05F0-v03-S5-Debug-0cda35b1ccb944d2b0e7aa6baba67977.stdout.log`
  - stderr：`C:\Users\zhchh\AppData\Local\Temp\DD05F0-v03-S5-Debug-0cda35b1ccb944d2b0e7aa6baba67977.stderr.log`
- Release x64 当轮 Rebuild：成功，MSBuild 退出码 0。
- Release 完整自检：PID `45436`，`3947 ms`，退出码 0，stderr 0 字节，残留进程 0，完整输出具名失败 0。
  - stdout：`C:\Users\zhchh\AppData\Local\Temp\DD05F0-v03-S5-Release-f6aaea1dc16e41109bcabf1d89c9e219.stdout.log`
  - stderr：`C:\Users\zhchh\AppData\Local\Temp\DD05F0-v03-S5-Release-f6aaea1dc16e41109bcabf1d89c9e219.stderr.log`

### 9.2 静态和治理门禁

- `python .\tools\check_specs.py --strict`：91 份目录项全部通过。
- `git diff --check`：通过。
- R1 `f01a370f...` 不是当前 HEAD 祖先：通过。
- 授权桥 import 精确为 `数据操作.特征体系.ixx` 与 `数据操作.需求任务方法.ixx` 两个生产模块：通过。
- 批次记录参与者只由 `数据操作.特征体系.ixx` import：通过。
- 桥项目模块依赖、跨模块需求类声明 / friend、主信息批次槽、`DD05F0-DIAG`：均零命中。
- 相对 JY-571 的禁止范围差异：零。

## 10. 完成边界

本记录证明 #329 / DD-05F0 v0.3 在 R2 任务分支上定位并修复自检超时及其暴露的两个第一违反点，完成原 A01—A17 和双配置任务级验证。它不证明任务提交已集成 main，也不授权消费 #330 / #333、实现恢复、任务状态、D455、自我回流或生产闭环。

Git 收口限定为当前同名任务分支 `codex/329-dd05f0-feature-current-txn-r2`，只暂存 v0.3 实际修改的三份代码 / 自检文件与本专属记录；从 JY-571 到最终任务分支的 14 个文件精确等于 v0.2 / v0.3 白名单。提交和非强制推送结果由本文件所在任务提交及窗口回执共同标识；不得推送 `origin/main` 或自行集成。
