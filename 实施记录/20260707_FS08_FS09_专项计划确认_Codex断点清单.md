# FS-08 / FS-09 专项计划确认 Codex 断点清单

日期：2026-07-07
状态：已确认 / 已登记可执行 S0 / 文档治理 / 未改代码 / 未迁移旧函数

## 1. 输入入口

用户要求：

```text
确认 两个待确认计划
```

确认对象：

```text
计划/20260707_FS08_缓存统计结构快照增强专项_v0.1.md
计划/20260707_FS09_事件日志持久化恢复增强专项_v0.1.md
```

## 2. 确认范围

FS-08 确认范围：

```text
FS-08 缓存统计结构快照增强专项 v0.1。
只确认 FS-08-E 结构统计运行期快照第一轮增强路线。
确认后只登记 FS-08 S0 当前代码事实扫描。
```

FS-09 确认范围：

```text
FS-09 事件日志持久化恢复增强专项 v0.1。
只确认 FS-09-A 日志输出与人读诊断入口、FS-09-D 仓库级快照与字段恢复验证路线。
确认后只登记 FS-09 S0 当前代码事实扫描。
```

FS09-Q1 到 FS09-Q4 按默认保守口径执行：

```text
第一轮优先文件事件日志和仓库快照格式，不使用 SQL Server ADO。
旧 SQL 字段恢复比对只作为后续持久化适配诊断输入，不作为运行期事实源。
旧双向链表模板二进制保存恢复格式不作为新项目仓库快照格式。
恢复入口第一轮必须先做版本、枚举、句柄、节点类型、关系目标和越权恢复拒绝设计；未确认前不写恢复代码。
```

## 3. 本次处理

```text
将 FS-08 / FS-09 专项计划从 计划/待确认计划/ 移回 计划/。
更新两份计划状态为已确认。
同步计划索引、当前状态、任务队列、待确认问题、实施记录和决策记录。
将 QR-049、QR-050 改为已确认。
登记 FS-08 S0 和 FS-09 S0 到可执行队列。
```

## 4. 可执行队列新增项

```text
执行 FS-08 S0 当前代码事实扫描。
执行 FS-09 S0 当前代码事实扫描。
```

S0 只允许只读扫描和生成断点清单；扫描发现的新问题写入 `项目记忆/待确认问题.md`。

## 5. 禁止宣称

```text
不得声明 FS-08 或 FS-09 代码已实现。
不得声明缓存统计系统或事件日志持久化恢复系统完成。
不得迁移旧函数体。
不得新增旧主信息字段。
不得直接进入 FS-08 S1-S4 或 FS-09 S1-S4。
不得接控制面板 / SQL / D455 / 体素 / 外设。
```
