# SERVICE-COMPOSER-S1 运行期组合器专项设计与旧计划归并 Codex 断点清单

日期：2026-07-14

交互：WIN-0001-E45

决策：JY-336

计划 / 队列：#270 / DQ-162 / SERVICE-COMPOSER-S1

阶段：770 待实施

## 1. 输入入口

```text
WIN-0001-E44 执行窗口回传
提交 a8c2cb0
#269 / DQ-161 / 760 正式归档计划和实施记录
#247、#265-#269、#275 当前正式模块
#248-#250、#270/#271 现有计划骨架
计划索引、Codex 任务队列、当前状态和项目规则
```

## 2. 当前事实复核

1. `main` 与 `origin/main` 位于 `a8c2cb0`，权威执行 worktree 干净。
2. `启动.运行期上下文` 只拥有结构核心，`完整()` 固定为 `false`，宿主不能发布。
3. 入口仍手工构造传统服务、四个兼容初始化器和旧自我线程。
4. 四个初始化 `.ixx` 仍由入口、线程和多个自检以 `#include` 消费。
5. 旧概念命名治理路由仍直接取得独占许可并向传统服务传递原始令牌。
6. 新分层已经形成七个数据操作、十四个无令牌业务服务和六个组合器，但生产调用尚未迁移。
7. 概念生命周期、统计缓存和安全删除没有新分层替代路径，不得被运行期组合器顺带接入或删除。

## 3. 核心裁决

```text
#270：建立唯一运行期业务装配、六组合器八类操作的顶层组合器、线程侧强类型无令牌同步路由，并接入运行期上下文候选；系统角色初始化完成前上下文继续不可发布。
#248：撤销执行路由，旧十六传统服务矩阵只保留为历史证据；670 不实施。
#250：保留原编号，后置到 #270 / 770 后基于实际装配接口重做三件套；不并入 #270，690 当前不实施。
#249：保留原编号，后置到 #250 完成后重做首次发布 / 租约 / 恢复链验收三件套；680 当前不实施。
#271：后置到 #249 完成后按精确矩阵收口；概念生命周期、统计和删除先标记保留兼容所有者。
```

后继顺序固定为：

```text
#270 -> #250 -> #249 -> #271 -> #257
```

## 4. 结构承载

本轮只生成和修订文档治理材料：

```text
流程图/20260714_运行期组合器与线程路由去令牌代码逻辑流程图_v0.1.md
流程图/20260714_运行期组合器与线程路由去令牌代码逻辑流程图_v0.1.html
规范/详细设计/运行期组合器与线程路由去令牌详细设计.md
计划/20260713_SERVICE-COMPOSER-S1_运行期组合器与线程路由去令牌代码实施切片_v0.1.md
#248/#249/#250/#271 计划状态修订
计划索引和项目记忆同步
```

未修改 C++、工程、filters 或机器结构，未构建、未运行程序，770 未登记。

## 5. 输出变化

1. `#270 / DQ-162` 从“设计资料与归并门控 / 不可执行”转为“专项三件套完整 / 当前待执行”。
2. `#248 / DQ-140` 执行路由正式撤销，不再阻塞新链。
3. `#250 / DQ-142` 和 `#249 / DQ-141` 保留原编号但必须分别重做三件套，当前不可执行。
4. `#271 / DQ-163` 依赖改为 `#270 -> #250 -> #249` 正式完成及精确兼容矩阵。
5. `#214` 和 `#257` 继续暂停；本轮不消费 `#273` 或任何外设范围。

## 6. 允许与禁止

执行窗口下一步只允许按 `#270` 当前计划先做实际接口复核；一致后实施 770。禁止：

```text
执行 #248、#249、#250 或 #271。
修改旧自我治理路由、概念命名路由或历史初始化文件。
把服务装配完成解释为系统角色初始化或生产上下文可发布。
把概念生命周期、统计缓存或安全删除纳入 #270。
恢复 #214 或 #257。
```

## 7. 范围一致性

流程图、详细设计和 #270 计划均固定四个新真模块、八类顶层操作、运行期上下文候选装配和 A01-A18；均排除旧线程正文、初始化、首次发布、概念生命周期 / 统计 / 删除和兼容删除，范围一致，无未裁决问题。

## 8. 验证

本轮应执行：

```text
Markdown / HTML Mermaid 同源检查
python .\tools\check_specs.py --strict
git diff --check
C++ / vcxproj / filters 差异为零
770 唯一性和计划 / 队列状态一致性扫描
```

验证边界：只验证文档治理和范围一致性，不证明任何 `#270` 代码、构建、自检、生产线程迁移、系统角色初始化或上下文发布已经完成。
