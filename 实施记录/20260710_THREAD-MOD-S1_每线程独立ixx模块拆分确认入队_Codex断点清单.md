# THREAD-MOD-S1 每线程独立 ixx 模块拆分确认入队 Codex 断点清单

日期：2026-07-10
状态：确认入队记录 / 后续 #167 已完成并验证

## 1. 确认依据

```text
用户明确要求：每个线程一个文件，不要混在一起，使用 .ixx 文件。
```

该指令同时确认线程文件结构方向和进入代码实施的目标。代码实施范围由已生成的 THREAD-MOD-S1 计划进一步收窄，不允许超出计划允许文件。

## 2. 联动确认材料

```text
流程图/20260710_每线程独立ixx模块拆分流程图_v0.1.md
流程图/20260710_每线程独立ixx模块拆分流程图_v0.1.html
规范/详细设计/每线程独立ixx模块拆分详细设计.md
计划/已完成计划/20260710_THREAD-MOD-S1_每线程独立ixx模块拆分代码实施切片_v0.1.md
```

上述流程图、详细设计和计划范围一致，均固定 6 个线程角色分别落入 6 个独立 `.ixx`，共享消息协议和有界队列使用不含线程类的独立 `.ixx`。

## 3. 可执行队列

```text
队列编号：#167
执行入口：先完成 S0 最小 C++20 模块编译探针。
后续顺序：S0 通过后执行 S1-S6；S0 失败必须追根因，不得退回混合头文件。
```

## 4. 允许与禁止边界

允许文件、禁止文件和验证命令以 `计划/已完成计划/20260710_THREAD-MOD-S1_每线程独立ixx模块拆分代码实施切片_v0.1.md` 为准。

```text
不得修改核心仓库、事务、领域服务、控制面板或 SQL Server 行为。
不得新增第七个业务线程。
不得把多个线程角色放入同一 .ixx。
不得复制 D:/鱼巢 的旧 .ixx 或旧线程函数体。
```

## 5. 确认时断点与后续结果

```text
计划已确认并登记 #167。
尚未修改 C++ 或工程文件。
尚未执行模块探针、构建或运行。
后续结果：#167 已完成并验证，见 `实施记录/20260710_THREAD-MOD-S1_每线程独立ixx模块拆分代码实施_Codex断点清单.md`。
```
