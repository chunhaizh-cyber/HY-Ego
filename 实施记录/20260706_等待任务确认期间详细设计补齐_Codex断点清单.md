# 等待任务确认期间详细设计补齐 Codex 断点清单

日期：2026-07-06
状态：已完成 / 文档已生成 / 待确认 / 未改代码

## 1. 输入入口

用户要求：

```text
在我确认期间请生成其它可以生成但还未生成的详细设计
```

执行门禁口径：

```text
建立规范和详细设计属于可直接执行文档治理动作。
设计完成后进入代码实施前才需要用户确认。
```

## 2. 本次范围

允许：

```text
根据 S4 后续专项登记补齐尚未生成的方案文档。
建立方法系统规范和详细设计。
建立动作入口规范和详细设计。
建立非权威缓存与统计规范和详细设计。
建立需求结算记录规范和详细设计。
更新计划索引、项目记忆和相关结构规范状态。
同步修正旧计划和断点中的“后续建立 / 未建立详细设计”措辞，统一为“已建立 / 待确认 / 未改代码”。
```

禁止：

```text
修改代码。
迁移旧主信息字段。
迁移旧函数体。
声明任务、方法、动作入口、缓存统计或需求结算系统完成。
```

## 3. 修订内容

新增：

```text
规范/方法系统规范.md
规范/详细设计/方法系统详细设计.md
规范/动作入口规范.md
规范/详细设计/动作入口详细设计.md
规范/非权威缓存与统计规范.md
规范/详细设计/非权威缓存与统计详细设计.md
规范/需求结算记录规范.md
规范/详细设计/需求结算记录详细设计.md
```

核心结论：

```text
MI-M1、MI-M3、MI-M4、MI-M5 进入方法系统方案，不回流为方法主信息裸字段。
MI-M2 进入动作入口方案，动作句柄不得用函数名、文本或主信息裸字段裁决。
MI-V2、MI-BI2 进入非权威缓存和统计方案，清空缓存不得改变结构事实。
MI-D3 拆分为需求结算权威记录和非权威摘要，摘要不得作为需求满足、任务完成或方法成功依据。
```

## 4. 验证方式

```powershell
rg -n "方法系统规范|方法系统详细设计|动作入口规范|动作入口详细设计|非权威缓存|需求结算记录|MI-M1|MI-M2|MI-M3|MI-M4|MI-M5|MI-V2|MI-BI2|MI-D3" 规范 计划 项目记忆 实施记录
git diff --check
rg -n "优先建立任务系统规范和详细设计|后续建立方法系统|未建立详细设计前|缺对应专项详细设计|方法系统专项待详细设计|下一步建立任务系统规范和详细设计" 计划 规范 项目记忆 实施记录 --glob "!实施记录/20260706_等待任务确认期间详细设计补齐_Codex断点清单.md"
```

验证结果：

```text
新文档和 MI 编号一致性检索通过，能追溯到规范、计划、项目记忆和实施记录。
旧状态措辞检索无命中。
git diff --check 通过，仅有 CRLF 换行提示。
本切片不改代码，不运行构建和默认入口。
```

## 5. 输出变化

```text
除任务系统外，S4 登记中可生成的后续专项规范和详细设计已建立。
这些方案均进入待确认状态。
确认前不得修改方法服务、动态服务、需求服务、特征值服务或索引仓库代码。
```

## 6. 禁止扩大声明

```text
本次只能声明文档已生成。
不能声明相关规范已确认。
不能声明代码已实现。
不能声明方法系统、动作入口、缓存统计或需求结算系统完成。
不能声明旧项目字段或旧函数已迁移。
```
