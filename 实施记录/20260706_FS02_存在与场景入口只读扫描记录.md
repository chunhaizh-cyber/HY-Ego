# FS-02 存在与场景入口只读扫描记录

日期：2026-07-06
状态：FS-02 第三批已登记 / 只读扫描 / 未改代码 / 未迁移旧函数
依据清单：`实施记录/20260706_逐函数只读扫描清单.md`
前序记录：

```text
实施记录/20260706_FS02_语素入口与基础信息桥接只读扫描记录.md
实施记录/20260706_FS02_基础信息模板入口只读扫描记录.md
```

## 1. 本轮定位

本轮继续 FS-02，登记旧项目 `存在类` 和 `场景类` 中与存在身份、存在概念 / 实例、存在观测坐标、场景身份、场景子节点查询、场景宿主、场景状态 / 动态 / 二次特征索引有关的函数族。

本轮只做只读事实登记和初步分流，不复制旧 `存在节点主信息类`、`场景节点主信息类` 字段，不迁移旧函数体。涉及深度 / 彩图轮廓、三维体素、D455 或特征值原始值的函数只登记为 FS-X / 后续专项边界。

## 2. 只读扫描范围

```text
D:\鱼巢\存在类.h
D:\鱼巢\存在类.cpp
D:\鱼巢\场景类.h
D:\鱼巢\场景类.cpp
D:\鱼巢\场景索引同步.h
```

扫描证据来自：

```text
存在类.h:35-63：存在服务绑定、存在节点判断、按主键解析、名称 / 类型 / 内部世界 / 概念 / 旧三类根读取声明。
存在类.h:65-132：存在创建、查找、概念绑定、内部世界绑定、观测位置和场景坐标写入声明。
存在类.cpp:297-327：绑定基础信息、取主信息、存在节点判断和按主键解析实现。
存在类.cpp:331-380：存在概念 / 实例判断、名称 / 类型 / 内部世界 / 概念快照读取。
存在类.cpp:384-490：存在场景绝对坐标读取、相对坐标换算、观测位置缓存读取。
存在类.cpp:493-537：旧需求 / 任务 / 方法三类根节点读取。
存在类.cpp:541-649：存在创建、内部治理存在、子存在取或创建、概念和实例创建、删除。
存在类.cpp:658-769：存在枚举、子存在查询、概念集读取、实例按概念查找。
存在类.cpp:772-824：名称 / 类型 / 概念模板 / 实例概念 / 内部世界绑定。
存在类.cpp:833-968：观测位置、场景绝对坐标、相对坐标来源和未命中缓存写入。
场景类.h:27-86：场景服务绑定、场景节点判断、创建、查询、体素候选、宿主、来源文章和索引声明。
场景类.cpp:234-267：绑定基础信息、取主信息、场景节点判断和按主键解析。
场景类.cpp:269-353：场景创建、子场景取或创建、内部治理场景和删除。
场景类.cpp:365-430：场景枚举、子场景 / 子存在 / 子特征读取、按名称查找。
场景类.cpp:433-493：按深度 / 彩图轮廓图查找存在候选。
场景类.cpp:510-562：场景宿主和来源文章绑定 / 读取。
场景类.cpp:529-707：场景状态、动态和二次特征索引快照、追加和移除。
```

## 3. 逐函数登记

### OF-067

```text
旧函数编号：OF-067
扫描批次：FS-02
旧项目路径：D:\鱼巢\存在类.cpp:297
旧函数名：存在类::绑定基础信息 / 取存在主信息 / 是存在节点 / 按主键解析存在节点
旧函数签名：void 绑定基础信息(...); 存在节点主信息类* 取存在主信息(...); bool 是存在节点(...); 存在节点类* 按主键解析存在节点(...)
当前调用方：旧存在类所有读写入口、语素 / 基础信息模板、场景查询和世界树聚合路径。
触发路径：旧服务定位基础信息树后，按主信息类型或主键识别存在节点。
输入：基础信息类指针、基础信息节点、主键字符串。
输出：存在主信息指针、存在节点指针或局部 bool。
读结构：旧基础信息树、旧主信息类型、旧主键索引。
写结构：`绑定基础信息` 只写运行期服务指针；其它函数只读。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：存在节点类型复核、按稳定键定位候选、服务入口先确认基础信息依赖。
删除行为：不得用运行期裸指针作为长期身份；不得把旧主信息类型判断直接等同于新项目存在事实完整。
新增约束：新项目必须通过节点句柄、节点类型和存在服务读取入口复核；索引命中后仍需节点类型复判。
目标新结构：节点类型::存在、节点句柄、索引主键、存在服务只读入口。
目标领域服务入口：存在服务 `读取存在`、`存在是否有效`；索引仓库主键查询只作候选定位。
对应规范 / 详细设计：存在服务详细设计、节点仓库详细设计、索引仓库详细设计、领域服务授权写入规范。
入口拒绝条件：基础服务缺失、主键为空、句柄无效、节点类型不是存在、已删除节点。
验证方式：索引命中后读回节点类型；无效句柄 / 错误类型返回空且结构数量不变。
初步判定：改写为领域服务入口
待用户确认项：无。
扫描证据：`存在类.cpp:297` 至 `存在类.cpp:327`。
```

### OF-068

```text
旧函数编号：OF-068
扫描批次：FS-02
旧项目路径：D:\鱼巢\存在类.cpp:331
旧函数名：是存在概念 / 是实例存在 / 读取存在名称 / 读取存在类型 / 读取内部世界 / 读取概念集快照 / 读取概念模板引用 / 确保存在三类根节点 / 获取需求根节点 / 获取任务根节点 / 获取方法根节点
旧函数签名：读判断和旧根节点读取函数族。
当前调用方：旧概念 / 实例区分、语素候选、存在创建、需求树 / 任务树 / 方法树挂载兼容路径。
触发路径：判断存在节点是否在概念根子树内，或读取旧存在主信息中的名称、类型、内部世界、概念引用和三类高级根。
输入：存在节点、存在概念根。
输出：局部 bool、语素入口、场景节点、存在引用快照或旧需求 / 任务 / 方法根节点。
读结构：旧存在主信息名称、类型、内部世界、概念集、概念模板、需求根、任务根、方法根。
写结构：`确保存在三类根节点` 可能补旧三类根；其它读取函数不写结构。
是否写机器事实：多数否；`确保存在三类根节点` 在旧实现中可能补写旧高级根。
是否依赖日志：否。
是否依赖显示：名称 / 类型可服务显示和语素候选。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：存在概念 / 实例区分、名称 / 类型 / 内部世界 / 概念引用只读追溯。
删除行为：不得迁移旧需求根、任务根、方法根主信息字段；不得把名称 / 类型语素入口当作权威存在属性字段。
新增约束：概念、内部世界和高级信息归属必须由关系承载；需求 / 任务 / 方法必须由对应服务创建并读取专用关系。
目标新结构：存在节点、语素对应信息关系、存在概念 / 模板关系、存在到内部场景引用关系、需求 / 任务 / 方法专用关系。
目标领域服务入口：存在服务、场景服务、语素服务；需求服务 / 任务服务 / 方法服务专用读取入口。
对应规范 / 详细设计：存在服务详细设计、场景服务详细设计、语素系统详细设计、任务系统详细设计、方法系统详细设计。
入口拒绝条件：存在节点无效、概念根无效、关系多目标语义未确认、旧三类根字段试图迁移为主信息。
验证方式：关系集合读回概念和内部世界；旧根字段不进入主信息迁移；高级根由对应服务专用关系读回。
初步判定：拆分
待用户确认项：旧存在三类根节点是否仅作为 FS-04 / FS-05 / FS-06 后续扫描线索。
扫描证据：`存在类.cpp:331` 至 `存在类.cpp:380`、`存在类.cpp:493` 至 `存在类.cpp:537`。
```

### OF-069

```text
旧函数编号：OF-069
扫描批次：FS-02 / FS-03 / FS-08
旧项目路径：D:\鱼巢\存在类.cpp:384
旧函数名：读取存在场景绝对坐标 / 计算场景绝对坐标_由参考存在相对坐标 / 计算场景绝对坐标_由相对坐标表 / 读取最近观测位置缓存 / 读取上次观测位置缓存
旧函数签名：坐标读取、换算和观测缓存读取函数族。
当前调用方：旧存在观测、场景查找、体素候选和位置显示 / 特征化路径。
触发路径：从旧特征结构读取存在坐标，或用参考存在相对坐标换算绝对坐标。
输入：存在节点、参考存在、相对坐标表。
输出：坐标、换算结果或局部 bool。
读结构：旧存在子特征、旧场景归属、旧主信息观测缓存。
写结构：无。
是否写机器事实：否；读取缓存本身不写事实。
是否依赖日志：否。
是否依赖显示：观测缓存可服务显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：间接服务外设 / 体素候选路径。
保留行为：坐标读回、同场景相对坐标换算、缓存只读观察。
删除行为：不得把观测缓存、显示坐标或一次换算结果直接裁决为需求满足、任务完成或世界事实。
新增约束：坐标事实应由特征服务 / 状态 / 动态证据承载；观测缓存属于非权威缓存或显示材料。
目标新结构：特征状态材料、实例动态、非权威观测缓存、场景归属关系。
目标领域服务入口：特征服务、动态服务、场景服务、非权威缓存 / 统计入口。
对应规范 / 详细设计：基础信息服务分层规范、场景服务详细设计、动态服务详细设计、非权威缓存与统计详细设计。
入口拒绝条件：存在无效、场景归属缺失、参考存在与目标不在同一场景、坐标材料缺失、把缓存当权威事实。
验证方式：通过特征服务读回坐标材料；缓存清空不改变权威结构事实。
初步判定：拆分
待用户确认项：存在观测缓存是否归入非权威缓存专项，坐标特征是否转入 FS-03 特征增强专项。
扫描证据：`存在类.cpp:384` 至 `存在类.cpp:490`。
```

### OF-070

```text
旧函数编号：OF-070
扫描批次：FS-02
旧项目路径：D:\鱼巢\存在类.cpp:541
旧函数名：创建存在 / 创建存在_按名称类型 / 创建内部治理存在_按名称类型 / 取或创建子存在_按名称类型 / 取或创建子存在_按类型 / 确保存在概念 / 创建存在实例 / 删除存在
旧函数签名：存在创建、取或创建、概念 / 实例创建和删除函数族。
当前调用方：旧世界树、基础信息模板、语素入口、场景成员、任务 / 方法内部治理容器。
触发路径：按父节点、名称、类型、概念集创建存在节点，或按名称 / 类型复用已有子存在。
输入：父节点、名称入口、类型入口、概念父节点、概念集合。
输出：存在节点或删除 bool。
读结构：旧基础信息树子节点、旧语素入口名称 / 类型。
写结构：旧存在节点、旧存在主信息、旧父子关系、旧内部治理持有表。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：名称 / 类型可服务显示。
是否依赖 SQL：否。
是否依赖线程：内部治理持有表使用互斥，但不应迁移为动作来源。
是否依赖外设 / D455 / 体素：否。
保留行为：存在创建、实际存在与内部治理 / 虚拟存在区分、按名称 / 类型避免重复创建、实例关联概念。
删除行为：不得复制旧父子树和旧三类根补写；不得让语素或模板路径越权自动创建存在事实。
新增约束：实际存在和虚拟存在必须通过存在服务；虚拟存在来源需求 / 任务 / 方法关系必须可追溯；删除走节点仓库生命周期。
目标新结构：节点类型::存在、存在来源关系、场景归属关系、概念关系、节点生命周期版本。
目标领域服务入口：存在服务 `创建实际存在`、`创建虚拟存在`、后续存在增强入口；世界服务只做聚合编排。
对应规范 / 详细设计：存在服务详细设计、世界聚合服务详细设计、节点仓库详细设计、领域服务授权写入规范。
入口拒绝条件：父节点无效、名称 / 类型目标无效、概念节点无效、重复语义未确认、内部治理存在缺来源、高级服务绕过存在服务。
验证方式：创建后读回存在节点类型和来源关系；拒绝路径节点 / 关系数量不变；删除后旧句柄失效。
初步判定：改写为领域服务入口
待用户确认项：旧“内部治理存在”是否完全映射为虚拟存在，还是需要后续内部容器专项。
扫描证据：`存在类.cpp:541` 至 `存在类.cpp:649`。
```

### OF-071

```text
旧函数编号：OF-071
扫描批次：FS-02
旧项目路径：D:\鱼巢\存在类.cpp:658
旧函数名：枚举全部存在 / 获取子存在 / 查找子存在_按名称 / 查找子存在_按类型 / 获取概念集 / 存在包含概念 / 查找实例_按概念
旧函数签名：存在枚举、子存在查询和概念集合查询函数族。
当前调用方：旧场景候选、语素候选、存在创建前复用、概念 / 实例反查。
触发路径：按父节点、名称、类型或概念关系查询存在集合。
输入：父节点、名称入口、类型入口、概念节点、范围根。
输出：存在节点集合、存在节点或局部 bool。
读结构：旧基础信息树、旧内部世界、旧存在主信息名称 / 类型 / 概念集 / 概念模板。
写结构：无。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：查询结果可能服务显示候选。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：存在候选召回、按概念查实例、概念引用复判。
删除行为：不得静默选第一个候选作为事实；不得以旧父子树范围裁决新项目领域归属。
新增约束：候选查询只读；事实归属通过关系仓库和存在 / 场景服务专用入口复核。
目标新结构：存在节点集合、普通父子 / 归属 / 引用关系、索引候选。
目标领域服务入口：存在服务只读增强、场景服务成员读取、语素候选查询增强。
对应规范 / 详细设计：存在服务详细设计、场景服务详细设计、关系仓库详细设计、错误结果与入口拒绝规范。
入口拒绝条件：父节点无效、名称 / 类型入口无效、概念无效、多候选冲突未处理、范围根语义未确认。
验证方式：候选集合可读但不写事实；多候选返回集合或冲突，不静默裁决。
初步判定：拆分
待用户确认项：按概念查实例是否进入存在服务增强，还是后续语素 / 世界候选专项。
扫描证据：`存在类.cpp:658` 至 `存在类.cpp:769`。
```

### OF-072

```text
旧函数编号：OF-072
扫描批次：FS-02
旧项目路径：D:\鱼巢\存在类.cpp:772
旧函数名：写入名称 / 写入类型 / 绑定概念模板 / 添加实例概念 / 绑定内部世界
旧函数签名：存在名称 / 类型写入、概念关系写入和内部世界绑定函数族。
当前调用方：旧存在创建、概念兼容、基础信息模板、世界树内部世界挂接。
触发路径：修改旧存在主信息中的名称、类型、概念模板、概念集或内部世界引用。
输入：存在节点、语素入口、概念节点、场景节点。
输出：局部 bool。
读结构：旧存在主信息。
写结构：旧存在主信息名称、类型、概念模板、概念集、内部世界引用。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：名称 / 类型会影响显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：存在名称 / 类型可绑定语素入口，实例可追加概念，存在可绑定内部世界。
删除行为：不得把这些关系继续作为旧主信息裸字段迁移；不得由显示名改写事实身份。
新增约束：名称 / 类型 / 概念 / 内部世界应通过语素关系、模板关系或引用关系承载；写入口必须在存在服务或相关服务内。
目标新结构：语素对应信息关系、存在概念关系、存在内部场景引用关系、模板关系。
目标领域服务入口：存在服务增强入口、语素服务、场景服务。
对应规范 / 详细设计：存在服务详细设计、语素系统详细设计、关系仓库详细设计、领域服务授权写入规范。
入口拒绝条件：存在无效、语素入口无效、概念无效、自指概念、内部世界不是场景、重复关系语义未确认。
验证方式：关系读回名称 / 类型 / 概念 / 内部世界；拒绝路径结构不变。
初步判定：改写为领域服务入口
待用户确认项：名称 / 类型是否作为语素入口反向关系，还是需要独立存在属性关系角色。
扫描证据：`存在类.cpp:772` 至 `存在类.cpp:824`。
```

### OF-073

```text
旧函数编号：OF-073
扫描批次：FS-02 / FS-03 / FS-08
旧项目路径：D:\鱼巢\存在类.cpp:833
旧函数名：写入观测位置 / 写入存在场景绝对坐标 / 写入存在场景绝对坐标_由参考存在相对坐标 / 写入存在场景绝对坐标_由相对坐标表 / 标记观测未命中
旧函数签名：观测位置、坐标特征和未命中缓存写入函数族。
当前调用方：旧观察、场景定位、体素候选和特征化路径。
触发路径：存在被观测到、根据参考存在计算坐标，或本帧未命中时调用。
输入：存在节点、坐标、参考存在、相对坐标表、时间戳、置信度。
输出：局部 bool。
读结构：旧存在主信息、旧场景归属、旧子特征。
写结构：旧存在主信息观测缓存、连续未命中 / 静止计数、旧特征 I64 材料、坐标来源指针。
是否写机器事实：是；其中观测位置和未命中计数应分流为非权威缓存，坐标材料应经特征 / 动态服务确认。
是否依赖日志：否。
是否依赖显示：观测位置可服务显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：间接依赖观察 / 体素链路，本轮不接入。
保留行为：观测坐标写入、坐标时间戳和置信度、同场景参考存在约束、未命中统计。
删除行为：不得直接迁移旧观测缓存字段；不得让外设输入或线程直接写世界事实；不得绕过特征服务写特征值。
新增约束：权威坐标材料经特征服务写入；观测变化应形成实例动态证据；缓存统计只作非权威材料。
目标新结构：特征状态材料、实例动态、场景关系、非权威缓存记录。
目标领域服务入口：特征服务、动态服务、场景服务、非权威缓存 / 统计入口；外设观察另建后续专项。
对应规范 / 详细设计：基础信息服务分层规范、特征服务边界、动态服务详细设计、非权威缓存与统计详细设计。
入口拒绝条件：存在无效、缺场景、缺时间戳、参考存在不在同一场景、坐标不可编码、特征服务不可用、外设材料未经领域入口。
验证方式：特征状态材料和动态证据可读；缓存清空不改变权威事实；拒绝路径结构不变。
初步判定：拆分
待用户确认项：观测坐标第一轮是否归入特征 / 特征值系统增强专项，外设来源仍保持暂缓。
扫描证据：`存在类.cpp:833` 至 `存在类.cpp:968`。
```

### OF-074

```text
旧函数编号：OF-074
扫描批次：FS-02
旧项目路径：D:\鱼巢\场景类.cpp:234
旧函数名：场景类::绑定基础信息 / 取场景主信息 / 是场景节点 / 按主键解析场景节点
旧函数签名：void 绑定基础信息(...); 场景节点主信息类* 取场景主信息(...); bool 是场景节点(...); 场景节点类* 按主键解析场景节点(...)
当前调用方：旧场景类所有读写入口、世界树聚合、状态 / 动态索引和语素场景桥接。
触发路径：旧服务定位基础信息树后，按主信息类型或主键识别场景节点。
输入：基础信息类指针、基础信息节点、主键字符串。
输出：场景主信息指针、场景节点指针或局部 bool。
读结构：旧基础信息树、旧主信息类型、旧主键索引。
写结构：`绑定基础信息` 只写运行期服务指针；其它函数只读。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：场景节点类型复核、按稳定键定位候选。
删除行为：不得用裸指针或旧主信息类型直接裁决完整场景事实。
新增约束：新项目场景身份由节点类型::场景和场景服务读取入口复核；索引命中后复判节点类型。
目标新结构：节点类型::场景、节点句柄、索引主键、场景服务只读入口。
目标领域服务入口：场景服务 `场景是否有效`、场景服务读取增强；索引仓库主键查询只作候选定位。
对应规范 / 详细设计：场景服务详细设计、节点仓库详细设计、索引仓库详细设计、领域服务授权写入规范。
入口拒绝条件：基础服务缺失、主键为空、句柄无效、节点类型不是场景、已删除节点。
验证方式：索引命中后读回节点类型；错误类型返回空且结构数量不变。
初步判定：改写为领域服务入口
待用户确认项：无。
扫描证据：`场景类.cpp:234` 至 `场景类.cpp:267`。
```

### OF-075

```text
旧函数编号：OF-075
扫描批次：FS-02
旧项目路径：D:\鱼巢\场景类.cpp:269
旧函数名：创建场景 / 创建场景_按名称 / 取或创建子场景_按名称 / 取或创建内部治理场景_按名称 / 删除场景
旧函数签名：场景创建、取或创建、内部治理场景和删除函数族。
当前调用方：旧世界树、内部治理容器、任务 / 方法运行场景和场景子树创建路径。
触发路径：按父节点和名称创建场景，复用已有子场景，或创建不进世界树根链的内部治理场景。
输入：父节点、父场景、名称入口、世界类型。
输出：场景节点或删除 bool。
读结构：旧基础信息树子场景、旧内部治理持有集。
写结构：旧场景节点、旧场景主信息、旧父子关系、旧内部治理场景持有表。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：名称可服务显示。
是否依赖 SQL：否。
是否依赖线程：内部治理持有表使用互斥，但不应迁移为动作来源。
是否依赖外设 / D455 / 体素：否。
保留行为：场景创建、按名称复用、内部治理场景作为任务 / 方法运行容器的需求。
删除行为：不得复制旧父子树和内部治理持有表；不得让任务 / 方法绕过场景服务创建场景。
新增约束：场景必须由场景服务创建；内部治理场景需要来源任务 / 方法或业务场景关系追溯。
目标新结构：节点类型::场景、场景归属关系、任务 / 方法运行场景关系、节点生命周期。
目标领域服务入口：场景服务 `创建场景`、世界服务聚合入口、任务 / 方法服务经场景服务创建运行场景。
对应规范 / 详细设计：场景服务详细设计、世界聚合服务详细设计、任务系统详细设计、方法系统详细设计。
入口拒绝条件：父节点无效、名称入口无效、内部治理父场景无效、来源任务 / 方法缺失、删除绕过节点生命周期。
验证方式：创建后可由场景服务读回；运行场景关系可追溯来源；删除后旧句柄失效。
初步判定：改写为领域服务入口
待用户确认项：旧内部治理场景是否统一映射为任务 / 方法运行场景关系。
扫描证据：`场景类.cpp:269` 至 `场景类.cpp:353`。
```

### OF-076

```text
旧函数编号：OF-076
扫描批次：FS-02 / FS-03
旧项目路径：D:\鱼巢\场景类.cpp:365
旧函数名：枚举全部场景 / 获取子场景 / 获取子存在 / 获取子特征 / 查找子场景_按名称
旧函数签名：场景枚举、场景内子节点读取和按名称查找函数族。
当前调用方：旧场景候选、世界树遍历、观察候选、特征和存在查询路径。
触发路径：按父节点或场景节点枚举直接子场景、子存在、子特征。
输入：父节点、名称入口。
输出：场景 / 存在 / 特征集合或场景节点。
读结构：旧基础信息树直接子节点、旧场景 / 存在 / 特征主信息。
写结构：无。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：可能服务显示候选。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：场景成员和子结构候选读取。
删除行为：不得把普通父子树枚举直接等同于场景归属事实；不得静默选第一个候选。
新增约束：场景成员事实应通过场景到存在 / 状态 / 动态 / 特征关系或领域服务读取入口复核。
目标新结构：场景到存在归属关系、场景成员读取入口、特征归属关系、索引候选。
目标领域服务入口：场景服务、存在服务、特征服务。
对应规范 / 详细设计：场景服务详细设计、存在服务详细设计、关系仓库详细设计、特征服务边界。
入口拒绝条件：场景无效、父节点无效、名称入口无效、多候选冲突未处理、特征服务边界未确认。
验证方式：场景服务可读回接纳存在关系；查询只读不写事实。
初步判定：拆分
待用户确认项：`获取子特征` 具体迁入 FS-03 特征 / 特征值系统增强。
扫描证据：`场景类.cpp:365` 至 `场景类.cpp:430`。
```

### OF-077

```text
旧函数编号：OF-077
扫描批次：FS-X / FS-03
旧项目路径：D:\鱼巢\场景类.cpp:433
旧函数名：场景类::查找存在_按深度或彩图轮廓图
旧函数签名：std::vector<结构_场景三维体素轮廓存在比较结果> 查找存在_按深度或彩图轮廓图(...)
当前调用方：旧场景观察、D455 / 彩图 / 深度轮廓和三维体素存在候选路径。
触发路径：从局部深度 / 彩图轮廓生成体素，与场景内存在的三维体素链比较并排序候选。
输入：场景节点、局部视角集合、融合参数、查找参数、特征值池、存在概念根。
输出：三维体素轮廓存在候选比较结果集合。
读结构：旧场景子存在、旧特征节点、旧特征值池、旧体素链、存在坐标和存在概念。
写结构：无。
是否写机器事实：否；但旧调用方可能据候选形成观察事实。
是否依赖日志：否。
是否依赖显示：可能服务观察候选显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：是，依赖深度 / 彩图轮廓和三维体素。
保留行为：候选召回、相似度排序和存在坐标参与比较的需求可以作为后续观察专项线索。
删除行为：本轮不得接 D455、体素、外设或三维体素特征值链；不得把候选排序直接写成世界事实。
新增约束：外设材料只能作为材料；观察事实必须经后续观察 / 特征 / 动态 / 因果专项确认。
目标新结构：后续外设材料、特征 / 特征值增强、实例动态、候选集合；本轮无落地结构。
目标领域服务入口：特征 / 特征值系统增强专项、外设 / 体素观察专项；本轮不迁移。
对应规范 / 详细设计：基础信息服务分层规范、特征 / 特征值系统增强专项待设计、外设 / 体素专项待设计。
入口拒绝条件：试图接入 D455 / 体素、特征值原始容器未确认、候选直接裁决事实、外设输入绕过领域服务。
验证方式：本轮仅登记为排除 / 后续专项；后续必须证明候选不写业务事实。
初步判定：排除 / 后续专项
待用户确认项：是否纳入后续“特征 / 特征值系统增强专项”和外设 / 体素专项交界清单。
扫描证据：`场景类.cpp:433` 至 `场景类.cpp:493`。
```

### OF-078

```text
旧函数编号：OF-078
扫描批次：FS-02 / FS-10
旧项目路径：D:\鱼巢\场景类.cpp:510
旧函数名：绑定宿主 / 读取场景宿主存在 / 绑定来源文章
旧函数签名：bool 绑定宿主(...); 存在节点类* 读取场景宿主存在(...); bool 绑定来源文章(...)
当前调用方：旧场景宿主、文章来源、显示和文本场景桥接路径。
触发路径：为场景写入宿主存在引用、读取宿主存在，或绑定来源文章。
输入：场景节点、宿主存在节点、文章节点。
输出：局部 bool 或存在节点。
读结构：旧场景主信息宿主存在引用、来源文章引用。
写结构：旧场景主信息宿主存在和来源文章引用。
是否写机器事实：是，宿主和来源文章是场景关系事实；来源文章同时涉及显示 / 文本材料。
是否依赖日志：否。
是否依赖显示：来源文章可服务显示或文本来源追溯。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：场景可追溯宿主存在；文本 / 文章来源可作为材料来源。
删除行为：不得用旧主信息字段迁移宿主或来源；不得让文章文本直接成为世界事实。
新增约束：宿主关系经场景服务写入；来源文章若进入新项目需先有文本材料 / 显示层只读或持久化专项设计。
目标新结构：场景到存在引用关系、材料来源关系、显示层只读材料。
目标领域服务入口：场景服务增强入口、显示层只读视图专项、事件日志 / 持久化恢复专项。
对应规范 / 详细设计：场景服务详细设计、领域服务授权写入规范、显示层只读视图专项待设计。
入口拒绝条件：场景无效、宿主存在无效、来源文章节点类型未确认、材料来源被当作世界事实。
验证方式：宿主关系读回；来源材料只读，不改变业务事实。
初步判定：拆分
待用户确认项：来源文章是否进入显示层只读视图专项或事件日志持久化恢复专项。
扫描证据：`场景类.cpp:510` 至 `场景类.cpp:562`。
```

### OF-079

```text
旧函数编号：OF-079
扫描批次：FS-02 / FS-07 / FS-08
旧项目路径：D:\鱼巢\场景类.cpp:529
旧函数名：读取场景状态索引快照 / 读取场景动态索引快照 / 追加状态 / 移除场景状态索引 / 移除场景状态索引引用 / 追加动态 / 移除场景动态索引 / 追加关系 / 追加二次特征 / 移除场景二次特征索引 / 读取场景二次特征索引快照
旧函数签名：场景状态 / 动态 / 二次特征索引读取、追加和移除函数族。
当前调用方：旧状态、动态、二次特征和场景事实目录维护路径。
触发路径：场景内新增或移除状态、动态、二次特征时，旧场景主信息维护引用目录和排序。
输入：场景节点、状态节点、动态节点、二次特征节点或可解析引用。
输出：引用快照或局部 bool。
读结构：旧场景主信息状态索引、动态索引、二次特征索引、状态 / 动态排序时间。
写结构：旧场景主信息中的状态 / 动态 / 二次特征索引缓存。
是否写机器事实：待确认；旧实现把它称为事实目录，但新项目应区分场景收纳关系和非权威索引缓存。
是否依赖日志：否。
是否依赖显示：索引快照可服务显示和诊断。
是否依赖 SQL：否。
是否依赖线程：使用场景索引全局互斥；线程不作为动作来源。
是否依赖外设 / D455 / 体素：否。
保留行为：场景收纳实例状态、实例动态和二次特征引用；按发生时间排序的读取价值。
删除行为：不得把场景主信息索引字段作为权威事实字段迁移；不得由场景服务解释状态值、动态语义或二次特征业务结论。
新增约束：权威收纳关系由场景服务关系承载；排序 / 快照可作为非权威缓存或读取视图；实例状态 / 动态必须有主体、场景和发生时间戳。
目标新结构：场景到实例状态关系、场景到实例动态关系、二次特征关系、非权威缓存 / 统计快照。
目标领域服务入口：场景服务 `收纳实例状态`、`收纳实例动态`、二次特征服务、非权威缓存 / 统计入口。
对应规范 / 详细设计：场景服务详细设计、状态服务详细设计、动态服务详细设计、二次特征服务详细设计、非权威缓存与统计详细设计。
入口拒绝条件：场景无效、状态 / 动态 / 二次特征无效、实例状态 / 动态缺主体或时间戳、用缓存索引裁决业务事实。
验证方式：关系读回场景收纳；缓存清空不影响权威结构；拒绝路径节点 / 关系 / 状态 / 动态数量不变。
初步判定：拆分
待用户确认项：旧场景状态 / 动态 / 二次特征索引是否整体归入非权威缓存与统计后续增强。
扫描证据：`场景类.cpp:529` 至 `场景类.cpp:707`。
```

## 4. 本轮初步结论

```text
1. 旧 `存在类` 中存在身份、名称 / 类型、概念 / 实例、内部世界和观测坐标混在旧存在主信息字段中；新项目必须拆成存在节点、语素关系、场景关系、特征状态材料、动态证据和非权威缓存。
2. 旧存在创建路径有可迁移语义，但不能复制旧父子树、三类根节点或内部治理持有表；实际存在 / 虚拟存在必须由存在服务创建。
3. 旧观测位置和未命中计数只适合作为非权威缓存或外设观察后续专项线索，不能作为需求满足、任务完成或世界事实。
4. 旧 `场景类` 中场景创建、宿主绑定和状态 / 动态 / 二次特征索引有可迁移语义，但场景主信息索引不得直接迁移为权威字段；权威收纳应由场景服务关系承载。
5. `查找存在_按深度或彩图轮廓图` 依赖深度 / 彩图 / 三维体素 / 特征值原始材料，本轮只登记为 FS-X / 后续专项排除边界。
```

## 5. 后续待执行

```text
1. 下一旧函数编号从 OF-080 开始。
2. 继续 FS-02，扫描旧状态类、动态类、二次特征类和因果类第一批函数。
3. 观测坐标、三维体素、特征值池和特征值原始材料转 FS-03 或 FS-X 后续专项。
4. OF-067 到 OF-079 均为初步判定，需用户确认后才可进入任何代码实施。
```

## 6. 禁止宣称

```text
不得声明旧函数已扫描完成。
不得声明旧存在类或旧场景类已迁移。
不得声明旧存在主信息字段、旧场景主信息字段、旧观测缓存或旧事实索引可直接进入新项目。
不得声明基础信息服务分层、存在服务、场景服务、状态 / 动态系统或二次特征系统完整完成。
不得接控制面板 / SQL / D455 / 体素 / 外设。
```
