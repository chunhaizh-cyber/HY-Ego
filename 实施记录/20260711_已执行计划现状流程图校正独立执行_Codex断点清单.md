# 已执行计划现状流程图校正独立执行 Codex 断点清单

日期：2026-07-11
状态：本窗口独立执行 / 不加入计划索引 / 不登记 Codex 任务队列 / 未改代码

## 1. 输入入口

用户连续给出口径：

```text
根据代码现状校正已经执行过计划的流程图。
现状流程图要和施工流程图区分开。
现状流程图应该足够详细，每行代码都要体现，以便后续校正流程图，然后对代码纠偏。
制定一个独立计划，并在这个窗口独立执行。
不要加到计划索引。
```

## 2. 当前工作区事实

执行前只读确认：

```text
当前提交：4cb0988
工作区已有 FS03 / CONCEPT 相关未提交改动。
计划/计划索引.md、项目记忆/Codex任务队列.md、项目记忆/当前状态.md 等共享文件已有未提交改动。
```

因此本窗口按用户要求采用独立计划方式，不写计划索引，不登记 Codex 任务队列，避免与既有未提交切片混合。

## 3. 核心处理

本窗口执行了以下文档治理动作：

```text
建立现状流程图与施工流程图区分规范。
建立已执行计划现状流程图校正独立计划。
建立后续校正目标清单。
写入本断点记录。
```

## 4. 结构承载

本次只新增文档结构：

```text
规范/现状流程图与施工流程图区分规范.md
计划/20260711_已执行计划现状流程图校正独立计划_v0.1.md
实施记录/20260711_已执行计划现状流程图校正目标清单.md
实施记录/20260711_已执行计划现状流程图校正独立执行_Codex断点清单.md
```

未修改机器结构，未修改 C++，未修改既有流程图。

## 5. 输出变化

新增口径：

```text
现状流程图：按当前代码事实、实施记录和验证输出展开。
施工流程图：按计划和详细设计给出拟施工路径。
逐行代码映射表：现状流程图的必要产物，必须覆盖每一个非空、非纯格式的有效代码行。
```

新增目标清单：

```text
P0：真实闭环串联优先目标。
P0：SELF-GOV 自我治理闭环。
P0：运行宿主与多线程消息队列。
P1：THREAD-S1 至 THREAD-S6。
P1：20260708 领域服务代码逻辑流程图。
```

## 6. 调用路径

```text
用户口径
-> AGENTS 开工入口读取
-> 只读检查当前工作区和已有口径
-> 新建独立规范 / 独立计划 / 目标清单 / 断点记录
-> 文档检查
```

## 7. 验证结果

已执行验证：

```text
rg -n "现状流程图|施工流程图|逐行代码映射|不要加到计划索引|不加入计划索引|不登记 Codex" ...
结果：命中新增四份独立产物，确认口径已落文档。

git diff --check -- 新增四份独立产物
结果：通过。

python .\tools\check_specs.py
结果：当前 shell 的 python 指向 WindowsApps 占位程序，返回非零且无输出。

py -3 .\tools\check_specs.py
结果：海中鱼巣规范治理检查: 未发现问题。

git -c core.quotePath=false status --short
结果：新增四份本窗口产物；同时存在本窗口开始前已有的 FS03 / CONCEPT 相关未提交改动。
```

## 8. 未完成事项

本窗口未做：

```text
未校正任何既有流程图。
未生成逐行代码映射表实例。
未生成 HTML 现状流程图。
未修改计划索引。
未登记 Codex 任务队列。
未提交和推送，因为当前工作区存在其它未提交切片，需避免混合提交。
```

后续若要实际校正某张流程图，应先选择单一对象，例如：

```text
真实闭环串联优先目标现状流程图
SELF-GOV 自我治理闭环现状流程图
运行宿主与多线程消息队列现状流程图
```

然后按本规范建立逐行代码映射表、现状流程图、偏差清单和后续代码纠偏计划。

## 9. 完成声明边界

本窗口只能声明：

```text
已形成现状流程图与施工流程图区分规范。
已形成已执行计划现状流程图校正独立计划。
已形成后续校正目标清单。
```

不得声明：

```text
既有流程图已经校正。
代码已经纠偏。
流程图和代码已经全部一致。
真实闭环、自我循环、自我苏醒或旧能力迁移已经完成。
```
