# FS-00 / FS-01 逐函数只读扫描记录

日期：2026-07-06
状态：FS-00 已复核 / FS-01 第一批已登记 / 只读扫描 / 未改代码 / 未迁移旧函数
依据清单：`实施记录/20260706_逐函数只读扫描清单.md`

## 1. 本轮定位

本记录承接逐函数只读扫描清单，只登记旧项目当前本地代码事实和初步判定。

本轮不复制旧函数体，不新增旧主信息字段，不接控制面板、SQL、D455、体素或外设，不形成迁移许可。

## 2. FS-00 扫描环境复核

| 项目 | 复核结果 | 证据 |
| --- | --- | --- |
| 当前项目路径 | 存在 | `D:\海中鱼巣` |
| 旧项目候选路径 | 存在 | `D:\鱼巢` |
| 当前项目 git 状态 | 干净，开始扫描前为 `## main...origin/main` | `git -c core.quotePath=false status --short --branch` |
| 旧项目 git 状态 | 本地脏工作树，且 `main...birthplace/main [ahead 22]` | `D:\鱼巢` 的 `git status` |
| 旧项目处理口径 | 只读扫描当前本地事实，不修改旧项目文件 | 本轮只执行 `rg`、`Get-Content` 和 `git status` |

旧项目当前存在未提交修改，因此本轮扫描结论只能绑定到 `D:\鱼巢` 当前本地工作树事实，不得直接作为稳定迁移基线。

## 3. FS-01 第一批扫描范围

本轮先收束到旧核心结构候选文件：

```text
D:\鱼巢\双向链表模板核心.h
D:\鱼巢\双向链表模板_深拷贝.inl
D:\鱼巢\双向链表模板_序列化.inl
D:\鱼巢\基础数据类型.h
D:\鱼巢\基础信息类.h
D:\鱼巢\基础信息类.cpp
D:\鱼巢\基础信息主信息类.h
D:\鱼巢\基础信息主信息类.cpp
```

本轮未扫描完整需求、任务、方法、动作、动态、因果、缓存统计、控制面板、SQL、D455、体素或外设函数。

## 4. 逐函数登记

### OF-001

```text
旧函数编号：OF-001
扫描批次：FS-01
旧项目路径：D:\鱼巢\双向链表模板核心.h:255
旧函数名：链表模板::添加节点_已加锁
旧函数签名：节点类* 添加节点_已加锁(节点类* 位置节点, 节点类* 新节点)
当前调用方：链表模板::添加节点；语素类、语言类等旧树结构入口间接使用。
触发路径：旧双向链表树同层插入。
输入：位置节点、新节点。
输出：新节点指针或 nullptr。
读结构：旧链表根指针、位置节点同层环、根主键。
写结构：旧节点父指针、同层环、全链环、主键、父节点子数量。
是否写机器事实：是，写旧树节点结构事实。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：函数内部加锁版本外由调用方控制；旧模板含锁。
是否依赖外设 / D455 / 体素：否。
保留行为：创建节点身份并建立可遍历结构。
删除行为：删除裸指针同层环、全链环和根主键自增作为新项目核心身份机制。
新增约束：必须改为节点仓库稳定句柄、主信息仓库和关系仓库写入；入口拒绝无效主信息、错误节点类型和越权写入。
目标新结构：节点、主信息、普通父子 / 归属 / 引用关系。
目标领域服务入口：核心仓库正式入口；业务场景必须经领域服务封装。
对应规范 / 详细设计：节点仓库详细设计、主信息系统详细设计、关系仓库详细设计、领域服务授权写入规范。
入口拒绝条件：无效位置节点、无效新节点、错误节点类型、越权写入方、缺主信息。
验证方式：拒绝前后节点 / 主信息 / 关系数量不变；有效路径可按句柄读回节点和关系。
初步判定：改写为领域服务入口
待用户确认项：是否只保留语义，不保留旧同层环 / 全链环实现。
扫描证据：`双向链表模板核心.h:255`。
```

### OF-002

```text
旧函数编号：OF-002
扫描批次：FS-01
旧项目路径：D:\鱼巢\双向链表模板核心.h:303
旧函数名：链表模板::添加子节点_已加锁
旧函数签名：节点类* 添加子节点_已加锁(节点类* 父节点, 节点类* 子节点)
当前调用方：链表模板::添加子节点；基础信息类、语素类、语言类、任务类、方法类等旧树入口间接使用。
触发路径：旧树父子挂载。
输入：父节点、子节点。
输出：子节点指针或 nullptr。
读结构：父节点子链、根主键。
写结构：父指针、父节点子指针、同层环、全链环、主键、子节点数量。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：旧模板外层加锁。
是否依赖外设 / D455 / 体素：否。
保留行为：表达父子结构挂载。
删除行为：删除旧指针树父子关系作为权威结构。
新增约束：新项目必须用关系仓库 `普通父子` 或领域确认关系承载；不得绕过节点 / 关系仓库。
目标新结构：节点仓库节点 + 关系仓库普通父子关系。
目标领域服务入口：关系仓库正式入口；领域服务按业务封装。
对应规范 / 详细设计：关系仓库详细设计、仓库逻辑空间与领域树结构规范。
入口拒绝条件：父节点无效、子节点无效、移动到自身或子孙、关系类型错误、重复关系冲突。
验证方式：关系读回、父节点子集读取、拒绝后关系数量不变。
初步判定：改写为领域服务入口
待用户确认项：旧父子树只保留结构语义，不迁移旧环形链表实现。
扫描证据：`双向链表模板核心.h:303`。
```

### OF-003

```text
旧函数编号：OF-003
扫描批次：FS-01
旧项目路径：D:\鱼巢\双向链表模板核心.h:354
旧函数名：链表模板::查找主键_已加锁 / 查找主键
旧函数签名：节点类* 查找主键_已加锁(const std::string& 主键) const；节点类* 查找主键(const std::string& 主键) const
当前调用方：世界树类、存在类、场景类、特征类、状态类、动态类、因果类、方法类等旧服务。
触发路径：旧主键到裸节点指针解析。
输入：字符串主键。
输出：节点裸指针或 nullptr。
读结构：旧全链环、节点主键。
写结构：无。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：读锁。
是否依赖外设 / D455 / 体素：否。
保留行为：按稳定身份查找节点。
删除行为：删除裸指针返回作为长期身份。
新增约束：新项目必须返回稳定句柄或 `std::optional` 结果；读取后复核节点有效性和类型。
目标新结构：节点句柄、索引仓库主键绑定。
目标领域服务入口：索引仓库 `按主键查节点` 或领域专用读取入口。
对应规范 / 详细设计：索引仓库详细设计、节点仓库详细设计。
入口拒绝条件：空主键、主键命名空间无效、索引命中节点无效或类型不匹配。
验证方式：按主键读取句柄并经节点仓库复核。
初步判定：改写为领域服务入口
待用户确认项：旧字符串主键是否仅作为迁移期索引材料。
扫描证据：`双向链表模板核心.h:354`、`双向链表模板核心.h:365`。
```

### OF-004

```text
旧函数编号：OF-004
扫描批次：FS-01
旧项目路径：D:\鱼巢\双向链表模板核心.h:405
旧函数名：链表模板::查找节点_当前链_已加锁 / 查找节点_当前链
旧函数签名：节点类* 查找节点_当前链(节点类* 当前节点, 主信息类型 查找内容, 枚举_比较字段 比较字段) const
当前调用方：旧树按主信息比较查找的通用入口。
触发路径：当前同层链扫描 + 主信息比较。
输入：当前节点、查找内容、比较字段。
输出：命中节点指针或 nullptr。
读结构：同层环、旧主信息字段、比较协议。
写结构：无。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：读锁。
是否依赖外设 / D455 / 体素：否。
保留行为：同一范围内按结构属性查找候选。
删除行为：删除旧主信息字段比较作为业务事实裁决。
新增约束：必须改为索引 / 关系 / 领域专用读取；命中候选后需复核权威结构。
目标新结构：索引、关系集合、领域专用读取结果。
目标领域服务入口：领域服务只读入口或索引仓库查询入口。
对应规范 / 详细设计：索引仓库详细设计、领域服务授权写入规范、错误结果与入口拒绝规范。
入口拒绝条件：当前节点无效、比较字段未确认、目标类型不匹配、多目标语义未确认。
验证方式：候选召回后复核节点类型和关系；不得以缓存或比较返回值直接裁决事实。
初步判定：拆分
待用户确认项：哪些旧比较字段进入后续索引命名空间，哪些删除。
扫描证据：`双向链表模板核心.h:405`。
```

### OF-005

```text
旧函数编号：OF-005
扫描批次：FS-01
旧项目路径：D:\鱼巢\双向链表模板核心.h:472
旧函数名：链表模板::修改节点信息_已加锁 / 修改节点信息
旧函数签名：void 修改节点信息_已加锁(节点类* 节点, 主信息类型 新信息)
当前调用方：旧树节点主信息替换入口。
触发路径：裸节点主信息改写。
输入：节点、新主信息。
输出：无。
读结构：节点指针、根指针。
写结构：节点主信息字段。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：写锁。
是否依赖外设 / D455 / 体素：否。
保留行为：更新节点材料。
删除行为：删除裸改节点主信息字段。
新增约束：必须按主信息仓库槽位、字段功能确认和领域服务授权写入。
目标新结构：主信息仓库值容器或已确认领域结构。
目标领域服务入口：主信息仓库正式入口；业务写入经领域服务。
对应规范 / 详细设计：主信息系统规范、主信息系统详细设计、领域服务授权写入规范。
入口拒绝条件：无效节点、根节点写入、未确认字段功能、越权写入方、类型不匹配。
验证方式：写入后按句柄读回；拒绝路径主信息数量和值不变。
初步判定：改写为领域服务入口
待用户确认项：旧主信息整体替换行为是否全部禁止，只保留分槽或结构化写入。
扫描证据：`双向链表模板核心.h:472`。
```

### OF-006

```text
旧函数编号：OF-006
扫描批次：FS-01
旧项目路径：D:\鱼巢\双向链表模板核心.h:500
旧函数名：链表模板::删除节点_已加锁 / 删除节点
旧函数签名：void 删除节点_已加锁(节点类* 被删除节点)
当前调用方：链表模板::删除节点；基础信息类删除节点间接调用；多领域旧服务通过基础信息类调用。
触发路径：旧节点及子链递归删除。
输入：被删除节点。
输出：无。
读结构：父子指针、同层环、全链环、子链。
写结构：父子指针、同层环、全链环、子数量；释放节点内存。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：写锁外层。
是否依赖外设 / D455 / 体素：否。
保留行为：节点生命周期失效和关系清理。
删除行为：删除递归裸释放和指针环修复作为正常路径。
新增约束：新项目删除必须通过节点仓库 / 关系仓库生命周期入口，句柄版本失效，拒绝后结构不变。
目标新结构：节点删除标记、关系删除标记、版本递增。
目标领域服务入口：节点仓库删除、关系仓库删除；业务删除经领域服务。
对应规范 / 详细设计：节点仓库详细设计、关系仓库详细设计、错误结果与入口拒绝规范。
入口拒绝条件：无效节点、根节点、已删除节点、越权写入方、缺少删除语义确认。
验证方式：旧句柄失效、新版本不复用；关系读回不再有效。
初步判定：改写为领域服务入口
待用户确认项：是否禁止旧递归删除语义直接迁入业务服务。
扫描证据：`双向链表模板核心.h:500`。
```

### OF-007

```text
旧函数编号：OF-007
扫描批次：FS-01
旧项目路径：D:\鱼巢\双向链表模板核心.h:542
旧函数名：链表模板::删除子链_已加锁 / 删除子链
旧函数签名：void 删除子链_已加锁(节点类* 父节点)
当前调用方：删除节点_已加锁、旧树清理。
触发路径：父节点子链递归删除。
输入：父节点。
输出：无。
读结构：父节点子链。
写结构：子链节点、父节点子指针、节点内存。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：写锁外层。
是否依赖外设 / D455 / 体素：否。
保留行为：批量失效一组子结构。
删除行为：删除裸递归释放。
新增约束：批量删除必须有领域语义和事务 / 半结构失败边界。
目标新结构：批量关系删除、节点生命周期失效。
目标领域服务入口：后续结构移动 / 删除专项或领域服务封装。
对应规范 / 详细设计：关系仓库详细设计、写入事务详细设计、错误结果与入口拒绝规范。
入口拒绝条件：父节点无效、删除范围未确认、越权写入方、事务边界缺失。
验证方式：删除前后节点和关系数量可解释；失败不得留下可读半结构。
初步判定：待确认
待用户确认项：是否需要新项目第一轮提供批量删除入口，或全部暂缓。
扫描证据：`双向链表模板核心.h:542`。
```

### OF-008

```text
旧函数编号：OF-008
扫描批次：FS-01
旧项目路径：D:\鱼巢\双向链表模板核心.h:569
旧函数名：链表模板::删除链表_已加锁 / 删除链表
旧函数签名：void 删除链表_已加锁()
当前调用方：链表模板析构。
触发路径：旧容器生命周期结束。
输入：无。
输出：无。
读结构：根指针全链环。
写结构：删除全部节点和根节点。
是否写机器事实：是，但属于旧容器生命周期清理。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：写锁外层。
是否依赖外设 / D455 / 体素：否。
保留行为：释放运行期容器资源。
删除行为：不迁移为新项目业务事实删除入口。
新增约束：新项目仓库生命周期由仓库对象管理，不得作为业务批量清空事实入口。
目标新结构：无目标业务结构。
目标领域服务入口：不迁移；仅可作为仓库析构资源释放语义参考。
对应规范 / 详细设计：节点仓库详细设计、错误结果与入口拒绝规范。
入口拒绝条件：不适合作为业务入口。
验证方式：无业务验收；如后续仓库析构验证，只检查资源释放不改变完成声明。
初步判定：删除
待用户确认项：无。
扫描证据：`双向链表模板核心.h:569`。
```

### OF-009

```text
旧函数编号：OF-009
扫描批次：FS-01
旧项目路径：D:\鱼巢\双向链表模板核心.h:593
旧函数名：链表模板::校验结构 / 校验结构_已加锁
旧函数签名：bool 校验结构() const；bool 校验结构_已加锁() const
当前调用方：旧容器结构诊断入口。
触发路径：扫描旧指针环和父子计数一致性。
输入：无。
输出：bool。
读结构：旧节点指针环、父子关系、子数量。
写结构：无。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：读锁。
是否依赖外设 / D455 / 体素：否。
保留行为：结构诊断思想。
删除行为：不得用事后校验作为正常结构正确性兜底。
新增约束：入口必须先拒绝错误输入；诊断只能做人读或验收辅助。
目标新结构：无权威业务结构。
目标领域服务入口：不迁移为正常运行入口；可后续登记为诊断专项。
对应规范 / 详细设计：错误结果与入口拒绝规范、领域服务授权写入规范。
入口拒绝条件：不适合作为业务写入前置替代。
验证方式：只可辅助验收，不作为机器事实来源。
初步判定：删除
待用户确认项：是否后续另建只读诊断工具专项。
扫描证据：`双向链表模板核心.h:593`。
```

### OF-010

```text
旧函数编号：OF-010
扫描批次：FS-01
旧项目路径：D:\鱼巢\双向链表模板_深拷贝.inl:5
旧函数名：链表模板::深拷贝树_独立 / 深拷贝树 / 深拷贝树_已加锁
旧函数签名：节点类* 深拷贝树_独立(const 节点类* 源根)
当前调用方：任务类、方法类、本能方法类、自我类等旧独立树复制路径。
触发路径：旧树或子树复制。
输入：源根。
输出：独立树根裸指针。
读结构：旧树节点、主键、父子链。
写结构：新裸指针树。
是否写机器事实：待确认；可能写运行期候选树。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：部分入口读锁。
是否依赖外设 / D455 / 体素：否。
保留行为：复制候选结构用于任务 / 方法规划的语义可能保留。
删除行为：不复制旧裸指针树和浅拷贝指针主信息策略。
新增约束：若需要复制，必须定义快照、候选、事务或事件恢复语义；不得共享主信息裸指针作为长期事实。
目标新结构：后续快照、事件日志恢复材料或候选结构。
目标领域服务入口：后续事件日志持久化恢复专项或任务 / 方法候选专项。
对应规范 / 详细设计：写入事务详细设计、事件日志持久化恢复专项待设计。
入口拒绝条件：源节点无效、复制范围未确认、主信息所有权不明。
验证方式：快照可读但不裁决当前事实；拒绝后权威结构不变。
初步判定：排除 / 后续专项
待用户确认项：是否纳入事件日志持久化恢复专项。
扫描证据：`双向链表模板_深拷贝.inl:5`、`任务类.cpp:287`、`方法类.cpp:287`。
```

### OF-011

```text
旧函数编号：OF-011
扫描批次：FS-01
旧项目路径：D:\鱼巢\双向链表模板_序列化.inl:73
旧函数名：链表模板::保存数据_已加锁 / 保存数据
旧函数签名：bool 保存数据_已加锁(const std::string& 文件名) const
当前调用方：旧持久化候选入口。
触发路径：旧树二进制序列化。
输入：文件名。
输出：bool。
读结构：旧全链节点、主键、链关系、主信息序列化。
写结构：文件。
是否写机器事实：否，写持久化材料；但旧项目可能用作恢复事实。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：读锁。
是否依赖外设 / D455 / 体素：否。
保留行为：持久化快照材料语义。
删除行为：不迁移旧二进制树格式作为当前运行期事实裁决。
新增约束：数据库 / 文件只做快照、事件、恢复和审计材料，不直接裁决机器事实。
目标新结构：事件日志、快照、恢复材料。
目标领域服务入口：事件日志持久化恢复专项。
对应规范 / 详细设计：日志系统规范、日志系统详细设计；事件日志持久化恢复专项待设计。
入口拒绝条件：文件路径未授权、结构版本未确认、恢复裁决边界缺失。
验证方式：恢复材料可重放但必须经正式恢复入口和领域服务裁决。
初步判定：排除 / 后续专项
待用户确认项：事件日志持久化恢复专项中是否重新设计快照格式。
扫描证据：`双向链表模板_序列化.inl:73`。
```

### OF-012

```text
旧函数编号：OF-012
扫描批次：FS-01
旧项目路径：D:\鱼巢\双向链表模板_序列化.inl:153
旧函数名：链表模板::读取数据_已加锁 / 读取数据
旧函数签名：bool 读取数据_已加锁(const std::string& 文件名)
当前调用方：旧持久化恢复候选入口。
触发路径：旧二进制树恢复。
输入：文件名。
输出：bool。
读结构：文件。
写结构：旧链表全部节点、主信息、链关系；中途失败会释放临时节点。
是否写机器事实：是，恢复后替换根指针。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：写锁。
是否依赖外设 / D455 / 体素：否。
保留行为：恢复材料重放语义。
删除行为：不允许绕过领域服务直接恢复当前事实。
新增约束：恢复必须走正式恢复入口，明确快照版本、事件顺序、领域裁决和失败回滚。
目标新结构：事件日志 / 快照恢复材料。
目标领域服务入口：事件日志持久化恢复专项。
对应规范 / 详细设计：领域服务授权写入规范、错误结果与入口拒绝规范；事件日志持久化恢复专项待设计。
入口拒绝条件：文件无效、版本不匹配、节点类型不合法、领域裁决缺失、半结构失败边界缺失。
验证方式：拒绝后当前仓库结构不变；恢复后可追溯来源快照和事件。
初步判定：排除 / 后续专项
待用户确认项：恢复入口是否完全独立于旧链表格式。
扫描证据：`双向链表模板_序列化.inl:153`。
```

### OF-013

```text
旧函数编号：OF-013
扫描批次：FS-01
旧项目路径：D:\鱼巢\基础信息类.cpp:81
旧函数名：基础信息类::添加节点
旧函数签名：节点类* 添加节点(节点类* 位置节点, 基础信息基类* 主信息)
当前调用方：动态类、语言类等旧基础信息写入路径。
触发路径：旧基础信息节点同层创建。
输入：位置节点、基础信息主信息裸指针。
输出：基础信息节点指针或 nullptr。
读结构：基础信息树世界根、旧链表。
写结构：基础信息节点和主信息裸指针。
是否写机器事实：是。
是否依赖日志：间接依赖 `鱼巢_不允许空指` 诊断。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：底层链表锁。
是否依赖外设 / D455 / 体素：否。
保留行为：创建基础信息节点。
删除行为：删除裸主信息指针和通用基础信息写入口。
新增约束：基础信息必须由对应原子服务创建；世界服务只做聚合编排。
目标新结构：存在、场景、特征、抽象状态、抽象动态、二次特征、因果引用等节点。
目标领域服务入口：存在服务、场景服务、特征服务、状态服务、动态服务、二次特征服务、因果服务。
对应规范 / 详细设计：基础信息服务分层规范、领域服务授权写入规范。
入口拒绝条件：主信息缺失、节点类型未确认、越权写入方、试图创建实例状态 / 动态缺主体场景时间戳。
验证方式：经对应服务创建后可读回节点类型和必要关系；拒绝前后结构数量不变。
初步判定：拆分
待用户确认项：旧通用基础信息创建入口是否只作为历史来源，不保留公共入口。
扫描证据：`基础信息类.cpp:81`。
```

### OF-014

```text
旧函数编号：OF-014
扫描批次：FS-01
旧项目路径：D:\鱼巢\基础信息类.cpp:89
旧函数名：基础信息类::添加子节点
旧函数签名：节点类* 添加子节点(节点类* 父节点, 基础信息基类* 主信息)
当前调用方：存在类、场景类、特征类、二次特征类、因果类等旧服务。
触发路径：旧基础信息父子树挂载。
输入：父节点、基础信息主信息。
输出：基础信息节点指针或 nullptr。
读结构：世界根、父节点。
写结构：基础信息节点、父子指针关系。
是否写机器事实：是。
是否依赖日志：间接依赖 `鱼巢_不允许空指` 诊断。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：底层链表锁。
是否依赖外设 / D455 / 体素：否。
保留行为：表达基础信息节点归属或树形上下文。
删除行为：删除通用父子挂载入口绕过领域服务。
新增约束：父子或归属关系必须有领域语义；实例状态 / 动态不得长期挂为语素对应信息。
目标新结构：关系仓库普通父子、归属、引用或模板关系。
目标领域服务入口：各基础信息原子服务或关系仓库正式入口。
对应规范 / 详细设计：仓库逻辑空间与领域树结构规范、关系仓库详细设计、基础信息服务分层规范。
入口拒绝条件：父节点无效、主信息缺失、关系语义未确认、越权写入方。
验证方式：关系读回和领域专用读取入口复核。
初步判定：拆分
待用户确认项：旧基础信息树层级如何分流到逻辑空间关系。
扫描证据：`基础信息类.cpp:89`。
```

### OF-015

```text
旧函数编号：OF-015
扫描批次：FS-01
旧项目路径：D:\鱼巢\基础信息类.cpp:97
旧函数名：基础信息类::重挂节点
旧函数签名：bool 重挂节点(节点类* 节点, 节点类* 新父节点)
当前调用方：世界树类内部世界重挂等旧结构整理路径。
触发路径：旧基础信息节点移动到新父节点。
输入：节点、新父节点。
输出：bool。
读结构：旧父链、新父链、同层环。
写结构：父子指针、同层环头、父节点子数量。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：写锁。
是否依赖外设 / D455 / 体素：否。
保留行为：节点普通父子关系重挂。
删除行为：删除裸改指针环。
新增约束：新项目已确认节点移动统一视为普通父子关系重挂，通用移动逻辑只能落在关系仓库。
目标新结构：关系仓库普通父子关系重挂。
目标领域服务入口：关系仓库::重挂节点；领域服务不得重复实现移动算法。
对应规范 / 详细设计：关系仓库详细设计、仓库逻辑空间与领域树结构规范。
入口拒绝条件：无效节点、移动到自身、移动到自身子孙、移动到当前父节点、没有当前父关系却请求重挂。
验证方式：默认入口已验证当前项目 `重挂拒绝: 自身=是 子孙=是 当前父=是`；后续旧迁移需独立验收。
初步判定：改写为领域服务入口
待用户确认项：旧 `基础信息类::重挂节点` 是否仅映射到当前关系仓库重挂入口。
扫描证据：`基础信息类.cpp:97`、`世界树类.cpp:4860`。
```

### OF-016

```text
旧函数编号：OF-016
扫描批次：FS-01
旧项目路径：D:\鱼巢\基础信息类.cpp:152
旧函数名：基础信息类::删除节点
旧函数签名：bool 删除节点(节点类* 节点)
当前调用方：存在类、场景类、特征类、状态类、动态类、二次特征类等旧服务。
触发路径：删除基础信息节点。
输入：节点。
输出：bool。
读结构：世界根、节点。
写结构：旧链表节点和关系指针。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：底层链表锁。
是否依赖外设 / D455 / 体素：否。
保留行为：基础信息节点生命周期失效。
删除行为：删除裸删除基础信息树节点。
新增约束：必须由对应领域服务或仓库正式入口删除，且清楚说明关系和状态后果。
目标新结构：节点删除标记、关系删除标记。
目标领域服务入口：节点仓库 / 关系仓库正式入口，或领域服务封装删除入口。
对应规范 / 详细设计：节点仓库详细设计、关系仓库详细设计、领域服务授权写入规范。
入口拒绝条件：节点无效、根节点、越权写入方、删除语义未确认。
验证方式：删除后旧句柄失效，拒绝后节点 / 关系数量不变。
初步判定：改写为领域服务入口
待用户确认项：各基础信息原子服务是否需要第一轮删除入口。
扫描证据：`基础信息类.cpp:152`。
```

### OF-017

```text
旧函数编号：OF-017
扫描批次：FS-01
旧项目路径：D:\鱼巢\基础信息类.cpp:160
旧函数名：基础信息类::更新主信息
旧函数签名：bool 更新主信息(节点类* 节点, 基础信息基类* 主信息)
当前调用方：旧基础信息主信息替换候选入口。
触发路径：删除旧主信息并替换为新主信息。
输入：节点、主信息。
输出：bool。
读结构：节点、世界根。
写结构：节点主信息裸指针。
是否写机器事实：是。
是否依赖日志：间接依赖 `鱼巢_不允许空指` 诊断。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：写锁。
是否依赖外设 / D455 / 体素：否。
保留行为：更新基础信息材料。
删除行为：删除整体替换旧主信息对象。
新增约束：必须分流到状态、动态、特征、关系或主信息仓库授权槽位，不得新增旧主信息字段。
目标新结构：主信息仓库已确认值槽、状态、动态、关系或特征状态材料。
目标领域服务入口：对应基础信息原子服务或主信息仓库正式入口。
对应规范 / 详细设计：主信息系统规范、基础信息服务分层规范、领域服务授权写入规范。
入口拒绝条件：节点无效、根节点、主信息缺失、字段功能未确认、越权写入方。
验证方式：结构读回和拒绝前后数量 / 值不变。
初步判定：改写为领域服务入口
待用户确认项：旧基础信息主信息替换行为是否全部禁止直接迁移。
扫描证据：`基础信息类.cpp:160`。
```

### OF-018

```text
旧函数编号：OF-018
扫描批次：FS-01
旧项目路径：D:\鱼巢\基础信息类.cpp:175
旧函数名：基础信息类::枚举全部节点
旧函数签名：std::vector<节点类*> 枚举全部节点() const
当前调用方：世界树类统计 / SQL 投影、特征类扫描等旧读取路径。
触发路径：遍历旧全链环。
输入：无。
输出：节点裸指针数组。
读结构：旧全链环。
写结构：无。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：部分调用方服务显示、SQL 或统计。
是否依赖 SQL：调用方可能依赖，函数本身不依赖。
是否依赖线程：读锁。
是否依赖外设 / D455 / 体素：否。
保留行为：只读枚举节点用于诊断、统计或结构验收。
删除行为：不得让枚举结果直接裁决业务事实。
新增约束：显示层 / 统计层只能只读；业务读取必须走领域专用入口或仓库正式读取。
目标新结构：节点仓库只读枚举或结构统计运行期快照。
目标领域服务入口：节点仓库只读统计、非权威缓存 / 统计入口。
对应规范 / 详细设计：非权威缓存与统计规范、节点仓库详细设计、领域服务授权写入规范。
入口拒绝条件：用于业务裁决时拒绝；用于统计时必须标非权威。
验证方式：清空统计缓存不改变节点 / 关系权威事实。
初步判定：拆分
待用户确认项：显示 / SQL 调用方进入 FS-10 或 FS-X，不进入本轮迁移。
扫描证据：`基础信息类.cpp:175`、`世界树类.cpp:2348`、`特征类.cpp:2477`。
```

## 5. 本轮初步结论

```text
1. 旧项目核心结构第一批主要围绕环形双向链表、裸指针主信息、字符串主键、父子指针树和文件序列化。
2. 当前新项目已具备更合适的节点仓库、主信息仓库、关系仓库、索引仓库、领域服务授权和错误结果规范，不应复制旧容器实现。
3. 旧核心结构函数多数只能保留语义，初步改写为仓库正式入口或领域服务入口；旧链表实现、裸主信息替换和事后校验不迁移。
4. 保存 / 读取数据属于事件日志持久化恢复专项候选，不进入本轮 FS-01 迁移判定。
5. 枚举全部节点可作为只读统计 / 诊断候选，但不得用于业务事实裁决。
```

## 6. 后续待执行

```text
1. FS-01 还需继续扫描旧核心结构中的世界树聚合入口、旧主信息字段访问入口和旧主键 / SQL 投影边界。
2. FS-02 才开始登记语素 / 基础信息长期节点、抽象 / 实例状态动态相关函数。
3. FS-03 到 FS-10 仍未执行。
4. 所有初步判定均需用户确认后，才能进入任何代码实施切片。
```

## 7. 禁止宣称

```text
不得声明 FS-01 已完整扫描。
不得声明旧函数已迁移。
不得声明旧链表模板可复制。
不得声明旧主信息字段已迁移。
不得声明事件日志持久化恢复已设计或实现。
不得声明控制面板、SQL、D455、体素或外设已接入。
不得声明旧项目能力迁移完成。
```
