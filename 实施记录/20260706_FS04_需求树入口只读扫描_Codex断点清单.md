# FS-04 需求树入口只读扫描 Codex 断点清单

日期：2026-07-06
状态：已记录断点 / 文档切片 / 未改代码 / 未迁移旧函数

## 1. 本切片输入

```text
FS-03 第一批特征与特征值入口扫描已提交并推送。
计划索引 HR-001-FM-SCAN 要求继续 FS-04 旧项目需求树函数第一批只读扫描。
用户明确“不需要确定的步骤应该持续执行下去”。
```

## 2. 已完成动作

```text
1. 回读计划索引、逐函数只读扫描清单、函数功能说明、需求目标状态承载详细设计、需求结算记录详细设计和任务系统详细设计。
2. 只读扫描 `D:\鱼巢\需求类.h`、`D:\鱼巢\需求类.cpp`、`D:\鱼巢\需求主信息类.h`、`D:\鱼巢\需求节点类型.h`、`D:\鱼巢\需求信息类型.h`。
3. 在编辑计划索引前创建 `计划/.计划索引.lock`。
4. 新增 `实施记录/20260706_FS04_需求树入口只读扫描记录.md`。
5. 登记 OF-118 到 OF-127。
6. 同步逐函数只读扫描清单、计划索引和项目记忆。
```

## 3. 本切片结论

```text
旧需求类把需求目标状态、当前状态、任务绑定、父子角色、结算摘要、方向比较、逻辑组织和 SQL 投影集中在旧需求主信息和需求树指针中。
新项目必须拆为需求目标状态关系、任务承接关系、需求结算记录关系、非权威摘要和显示层只读视图；旧主信息字段不能直接迁移。
```

## 4. 下一步

```text
下一旧函数编号从 OF-128 开始。
继续 FS-05 旧项目任务管理函数第一批只读扫描。
若后续发现 FS-04 第一批遗漏，只能追加登记，不得把当前记录解释为迁移许可。
```

## 5. 禁止宣称

```text
不得声明旧需求类或旧需求树已迁移。
不得声明旧需求主信息字段可直接迁移。
不得声明需求树、任务管理、方法候选、需求结算或 SQL 显示系统完成。
不得接控制面板 / SQL / D455 / 体素 / 外设。
```

## 6. 验证记录

```text
本切片只做文档和只读事实记录验证。
未改代码。
未运行构建。
未运行当前项目入口。
未运行旧项目程序。
```
