# 需求树后续结构流程图生成 Codex 断点清单

日期：2026-07-08

## 1. 输入入口

```text
用户指令：生成下一份流程图。
当前窗口：设计确认窗口。
承接顺序：第 5 项“需求创建与目标状态”之后，进入第 6 项“需求树后续结构流程”。
```

## 2. 核心处理

```text
1. 回读 AGENTS.md、计划索引、项目规则总纲、规则迁移清单。
2. 回读应用逻辑流程图迁移顺序信息数据，确认第 6 项为“需求树后续结构流程”。
3. 回读需求树后续结构详细设计、FS-04 需求树逻辑迁移包和服务操作函数矩阵第一批实施记录。
4. 回读当前 `需求服务.h`，确认当前已落代码入口为：
   - 读取需求树后续结构材料
   - 需求树结构化更新请求已拒绝
   - 枚举目标准入材料是否可读
   - 方法候选准入材料是否可读
5. 生成需求树后续结构代码逻辑流程图 MD / HTML。
```

## 3. 结构承载

```text
流程图承载：
- 流程图/20260708_需求树后续结构代码逻辑流程图_v0.1.md
- 流程图/20260708_需求树后续结构代码逻辑流程图_v0.1.html

实施记录承载：
- 实施记录/20260708_需求树后续结构流程图生成_Codex断点清单.md
```

## 4. 输出变化

```text
新增第 6 项“需求树后续结构”代码逻辑流程图。
图中区分：
- 当前已落代码：材料读取、拒绝判断、枚举目标准入材料可读、方法候选准入材料可读。
- 后续候选门禁：父子需求角色、逻辑组织需求、方向签名、枚举目标、结构化更新提交。
- 排除项：旧主信息字段、旧方向 bool、SQL、控制面板、D455、体素和外设。
```

## 5. 调用路径

```text
用户生成下一份流程图
-> 流程图迁移顺序信息数据第 6 项
-> 需求树后续结构详细设计 / FS-04 逻辑迁移包 / 服务矩阵第一批代码事实
-> 当前需求服务入口复核
-> 需求树后续结构代码逻辑流程图
```

## 6. 验证方式

```text
本切片为流程图文档治理切片。
计划验证：
- 确认 Markdown 和 HTML 两份流程图文件存在。
- 确认 Markdown 存在 mermaid fenced block。
- 确认 HTML 包含 Mermaid CDN import。
- 确认 MD / HTML Mermaid 图文本一致。
- 运行 git diff --check。
- 运行 tools/check_specs.py。

实际结果：
- Markdown、HTML 和本断点清单文件均存在。
- Markdown 存在 mermaid fenced block。
- HTML 包含 Mermaid CDN import。
- MD / HTML Mermaid 图文本一致。
- `git diff --check -- 流程图/20260708_需求树后续结构代码逻辑流程图_v0.1.md 流程图/20260708_需求树后续结构代码逻辑流程图_v0.1.html 实施记录/20260708_需求树后续结构流程图生成_Codex断点清单.md` 通过。
- `python tools/check_specs.py` 输出：海中鱼巣规范治理检查: 未发现问题。

未执行：
- 未修改 C++。
- 未运行 msbuild。
- 未运行默认入口。
- 未生成详细设计。
- 未生成施工计划。
- 未登记可执行队列。
```

## 7. 禁止宣称

```text
不得宣称完整需求树已实现。
不得宣称父子需求运行时写入已实现。
不得宣称逻辑组织需求、方向签名、枚举目标值域或方法候选算法已实现。
不得宣称结构化更新提交入口已实现。
不得宣称旧需求类、旧需求主信息字段、SQL、控制面板、D455、体素或外设已迁移。
不得宣称本流程图提供代码实施许可。
```
