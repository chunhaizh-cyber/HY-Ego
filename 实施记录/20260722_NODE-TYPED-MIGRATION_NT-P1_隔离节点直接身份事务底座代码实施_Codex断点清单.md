# NT-P1 隔离节点直接身份事务底座代码实施记录

## 1. 任务与 Git 身份

- 计划 / 队列：`#339 / DQ-231 / NT-P1 / v0.1`。
- 执行任务：`019f6a26-ef76-73a3-be4f-60e95498dff9`，独立顶层执行角色。
- 设计接受：`47ff41e440a90030d7e1eda98bc3c26f4f0cd3dc`（JY-588 / E329）。
- 派发状态：`2541e910a0ca9d84663e9c521c04d8885d55c7d6`（JY-587 / E328）。
- 冻结基线：`bcf129ec742ecf86763fa651704f547cf326fe0a`（JY-586 / E327）。
- 计划 blob：`69ad59eec5b3cfa7e9b3643063c735543565cbb3`。
- worktree：`C:\Users\zhchh\.codex\worktrees\339-nt-p1-node-typed-r2\海中鱼巢`。
- 分支：`codex/339-nt-p1-node-typed-r2`。
- S0：设计窗口已正式接受 `PASS`；任务分支始终保持 JY-586 为冻结祖先，未 merge、rebase、cherry-pick 或复制 JY-587 / JY-588。

## 2. 实施范围

实际修改精确为计划十二项白名单：

1. `海中鱼巣/核心/句柄.h`
2. `海中鱼巣/核心/仓库.节点直接身份.ixx`
3. `海中鱼巣/核心/仓库.正式关系.ixx`
4. `海中鱼巣/核心/仓库.可重建索引.ixx`
5. `海中鱼巣/核心/会话.节点直接身份结构写入.ixx`
6. `海中鱼巣/核心/执行器.节点直接身份结构写入.ixx`
7. `海中鱼巣/核心/冻结.节点直接身份结构.ixx`
8. `海中鱼巣/核心/自检.节点直接身份结构写入.ixx`
9. `海中鱼巣.vcxproj`
10. `海中鱼巣.vcxproj.filters`
11. `海中鱼巣/入口.cpp`
12. 本记录。

白名单外代码、旧核心仓库 / 会话 / 执行器 / 冻结、领域、线程、适配、数据库、界面、默认生产装配和中央治理均为零差异。

## 3. ABI、模块和结构事实

- 节点类型只追加 ABI `16—19`：文章、段落、自然句、子句；旧值未改。
- 关系类型只追加 ABI `19—23`：状态事实角色、动态事实角色、自我结构角色、特征实例角色、方法生命周期；旧值未改。
- `节点稳定主键` 精确为 `命名域 + 键值` 两个非零 `uint64`；命名域 ABI 版本为 `1`，固定表精确覆盖 4010 的 `0x4E534B5900000001—0x4E534B5900000015`。
- 节点记录精确承载：节点编号、稳定主键、节点类型、版本号、记录状态、创建序号。
- 节点仓形成复合主键唯一、同义幂等、异义 / 历史复用冲突、每域高水位、撤销烧号、普通 / 事务内 / 审计读回和值式权威导出。
- 正式关系仓具名准入表精确覆盖关系 ABI `0—23`；未知关系类型在编号和容器写入前拒绝且零写；关系端点每次回读节点权威记录。
- 可重建索引以具名所有者、物理命名域、格式版本、探测版本和键值定位节点 / 关系；命中后回读权威仓；清空、丢失和重建不改变节点或关系事实。

七模块及固定项目内 import DAG：

```text
仓库.节点直接身份
-> 仓库.正式关系
-> 仓库.可重建索引
-> 会话.节点直接身份结构写入
-> 执行器.节点直接身份结构写入

仓库.节点直接身份 + 仓库.正式关系 + 仓库.可重建索引
-> 冻结.节点直接身份结构

上述六模块
-> 自检.节点直接身份结构写入
```

工程以七项 `ClCompile` 和七项 `核心` Filter 登记；入口只增加一个自检模块 import、一次最终回归登记 / 调用和一个顶层门禁。

## 4. 原子收口和失败合同

- 新域只使用隔离事务域非零序号，不复用旧结构事务接线、旧仓库或旧结果类型。
- 提交准备只读视图只暴露候选节点判断、候选节点类型和候选稳定主键。
- 两个编译期不同的自检强类型参与者按登记顺序准备、确认、发布。
- 成功顺序：独占许可 -> 同步形成节点 / 关系 / 索引 / 参与者候选 -> 同事务完整读回 -> 参与者准备 -> 结构确认 -> 参与者确认 -> 无失败发布 -> 许可析构形成唯一普通可见点。
- 失败顺序：参与者逆序 -> 索引逆序 -> 关系变更逆序 -> 新关系逆序 -> 节点逆序；撤销失败在许可释放前隔离事务域，后继写入和读取许可均拒绝，首次失败不被覆盖。
- 冻结输出节点权威材料、有效 / 失效 / 历史关系和显式可丢弃索引材料；不建立跨域完整快照、恢复候选或通用领域载荷容器。

## 5. 自检与验证

### 5.1 Debug x64

- 命令：`msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m:1`。
- 结果：退出 `0`，当轮 Rebuild 成功。
- 命令：`x64\Debug\海中鱼巣.exe --self-test-exit`，固定 `180` 秒上限。
- 结果：退出 `0`；总用时约 `13121 ms`；运行前后目标进程均为 `0`。

### 5.2 Release x64

- 命令：`msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Release /p:Platform=x64 /m:1`。
- 结果：退出 `0`，当轮 Rebuild 成功。
- 命令：`x64\Release\海中鱼巣.exe --self-test-exit`，固定 `180` 秒上限。
- 结果：退出 `0`；总用时约 `1828 ms`；运行前后目标进程均为 `0`。

### 5.3 P1-A01—A15

两配置完整入口均具名输出并通过：

```text
P1-A01 稳定主键与高水位
P1-A02 节点记录精确字段
P1-A03 幂等与冲突零写
P1-A04 不读取旧主信息
P1-A05 关系 ABI 与可重建索引
P1-A06 提交准备只读视图
P1-A07 双强类型参与者
P1-A08 固定逆序撤销
P1-A09 撤销失败事务域隔离
P1-A10 独占许可可见点
P1-A11 值式冻结边界
P1-A12 无通用载荷容器
P1-A13 完整自检故障矩阵
P1-A14 ABI 与模块合同
P1-A15 新旧域零互通
```

### 5.4 静态、治理和范围

- `python .\tools\check_specs.py --strict`：`94` 份目录项全部通过。
- `git diff --check`：通过。
- 七模块声明、工程登记、Filter 登记：`7 / 7 / 7`。
- 四配置 `/utf-8`：`4 / 4`。
- 新域生产模块 import 方精确为新域模块内部；旧域和默认生产装配未提前接入。
- 通用附属结构、歧义主键索引术语、`.ixx` 文本包含、旧仓库 / 旧会话 / 旧执行器反向依赖：零命中。
- 相对冻结基线禁止范围差异：零。
- 工作树差异：只含十二项白名单。
- 验证结束目标进程：零残留。

## 6. Git 收口与剩余边界

- 本记录与代码 / 工程 / 自检作为同一精确任务提交暂存；提交哈希无法在同一提交内自引用，以提交后 Git 读回和跨窗口回执为准。
- 只向 `origin/codex/339-nt-p1-node-typed-r2` 做非强制推送；推送结果和远端读回以提交后回执为准。
- 本分支只证明 NT-P1 任务分支完成，不等于进入 `main`，不解除 `#340—#352` 的依赖门控。
- 新域仍保持隔离，不接默认生产装配，不形成与旧域的互查、关系、索引、投影、幂等账、恢复候选或稳定身份双写；后继接线只由后续正式计划处理。
