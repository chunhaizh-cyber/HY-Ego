# 方法结构流程图生成 Codex 断点清单

日期：2026-07-08

## 1. 输入入口

```text
用户指令：任务状态机筹办执行桥请求代码逻辑流程图_v0.1 确认，生成下一份流程图。
当前窗口：设计确认窗口。
承接顺序：第 8 项“任务状态机 / 筹办 / 执行桥请求流程”之后，进入第 9 项“方法结构流程”。
```

## 2. 核心处理

```text
1. 回读流程图技能、AGENTS.md、计划索引、项目规则总纲、规则迁移清单。
2. 回读应用逻辑流程图迁移顺序信息数据，确认第 9 项为“方法结构流程”。
3. 回读 FS-06 方法系统入口只读扫描记录、方法系统详细设计、FS-06 S1-S4 代码实施记录和当前 `方法服务.h`。
4. 确认当前已落代码入口为：
   - 登记方法
   - 创建方法首节点
   - 创建方法条件节点
   - 创建方法结果节点
   - 读取方法角色状态
   - 方法节点是否有效
   - 方法首是否有效
   - 读取方法虚拟存在
   - 读取方法条件节点集合
   - 读取方法结果节点集合
   - 读取方法条件归属方法首
   - 读取方法结果归属方法首
   - 读取方法条件场景
   - 读取方法结果场景
   - 登记动作入口
   - 读取方法动作入口
5. 生成方法结构代码逻辑流程图 MD / HTML。
```

## 3. 结构承载

```text
流程图承载：
- 流程图/20260708_方法结构代码逻辑流程图_v0.1.md
- 流程图/20260708_方法结构代码逻辑流程图_v0.1.html

确认断点承载：
- 实施记录/20260708_任务状态机筹办执行桥请求流程图确认_Codex断点清单.md

实施记录承载：
- 实施记录/20260708_方法结构流程图生成_Codex断点清单.md
```

## 4. 输出变化

```text
新增第 9 项“方法结构”代码逻辑流程图。
图中区分：
- 当前已落代码：方法身份壳、方法首、条件节点、结果节点、方法角色状态、方法虚拟存在、条件 / 结果场景、动作入口结构锚点。
- 当前读取边界：角色状态读取、虚拟存在唯一读取、条件 / 结果集合读取、归属方法首读取、条件 / 结果场景读取、动作入口读取。
- 当前拒绝边界：无效方法首、无效角色、无效场景、重复唯一目标、无效动作入口类型、稳定动作键无效或重复时拒绝。
- 当前差距：完整事务回滚、显式失效隔离、数量快照级半结构不可读、来源任务、父 / 前置 / 后续方法、结果能力索引、条件结果对、方法候选和方法学习均未由本图证明。
- 排除项：旧方法主信息字段、旧动作句柄字段、动作名文本、本能函数 ID、SQL、控制面板、D455、体素和外设。
```

## 5. 调用路径

```text
用户确认第 8 项并要求生成下一份流程图
-> 流程图迁移顺序信息数据第 9 项
-> FS-06 方法系统入口只读扫描记录 / 方法系统详细设计 / FS-06 S1-S4 代码实施记录 / 当前方法服务入口复核
-> 方法结构代码逻辑流程图
```

## 6. 验证方式

```text
本切片为流程图文档治理切片。
计划验证：
- 确认第 8 项确认断点清单存在。
- 确认第 9 项 Markdown 和 HTML 两份流程图文件存在。
- 确认 Markdown 存在 mermaid fenced block。
- 确认 HTML 包含 Mermaid CDN import。
- 确认 MD / HTML Mermaid 图文本一致。
- 运行 git diff --check。
- 运行 tools/check_specs.py。

实际结果：
- 第 8 项确认断点清单存在。
- 第 9 项 Markdown、HTML 和本断点清单文件均存在。
- Markdown 存在 mermaid fenced block。
- HTML 包含 Mermaid CDN import。
- MD / HTML Mermaid 图文本一致。
- `git diff --check -- 实施记录/20260708_任务状态机筹办执行桥请求流程图确认_Codex断点清单.md 流程图/20260708_方法结构代码逻辑流程图_v0.1.md 流程图/20260708_方法结构代码逻辑流程图_v0.1.html 实施记录/20260708_方法结构流程图生成_Codex断点清单.md` 通过。
- `python tools/check_specs.py` 输出：海中鱼巣规范治理检查: 未发现问题。

未执行：
- 未修改 C++。
- 未运行 msbuild。
- 未运行默认入口。
- 未生成详细设计。
- 未生成施工计划。
- 未登记可执行队列。
```

## 7. 禁止宣称

```text
不得宣称完整方法系统已完成。
不得宣称方法候选召回、候选排序、条件结果对、方法学习或动作执行系统已完成。
不得宣称来源任务、父方法、前置 / 后续方法关系已完整落地。
不得宣称当前多步写入已证明完整事务回滚或数量快照级半结构不可读。
不得宣称旧方法主信息字段、旧方法函数体、旧动作句柄、SQL、控制面板、D455、体素或外设已迁移。
不得宣称本流程图提供代码实施许可。
```
