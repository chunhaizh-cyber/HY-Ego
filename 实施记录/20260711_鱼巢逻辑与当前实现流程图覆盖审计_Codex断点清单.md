# 鱼巢逻辑与当前实现流程图覆盖审计 Codex 断点清单

日期：2026-07-11

复核：2026-07-12，按 HEAD `15032ed` 修正递归盘点

状态：审计产物已生成 / #240、DQ-132 已登记 / 未改 C++ / 未构建 / 未运行程序 / 纳入 FLOW-COVERAGE-S1 审计批次

## 1. 输入入口

```text
D:\鱼巢 当前 Git、121 个受跟踪源码文件和旧运行入口。
D:\海中鱼巢 HEAD 15032ed、59 个源码文件、102 份递归 Markdown 流程图和 45 对专门目录现状图。
OF-001 至 OF-206 旧函数证据和九份逻辑迁移包。
现状 / 施工流程图区分规范与流程图纠偏检查工具。
FLOW-STATE-BATCH-001 的 3f08239 批量基线、43 份合并核查表和 63cec55 批次提交。
```

## 2. 核心处理

1. 复核旧仓库 HEAD、ahead 和 dirty 代码事实。
2. 提取旧启动、初始化、自我循环、需求、任务、筹办、方法、执行、动态、因果、结算、外设和审计支线。
3. 复核当前总入口、初始化、领域服务、线程、自我治理、概念、事件、SQL 和控制面板流程。
4. 递归读取专门目录，按图类型、代码版本、覆盖文件和纠偏产物完整性检查现有覆盖。
5. 生成覆盖审计现状图、覆盖矩阵、映射、契约、二分审查、偏差清单和补齐计划。

## 3. 结构承载

本轮只新增流程图和文档治理产物，不修改节点、主信息、关系、索引、特征值、状态、动态、需求、任务、方法、因果、线程或数据库结构。

## 4. 输出变化

当前正式结论为：

```text
旧鱼巢逻辑尚未全部梳理成可回到代码行的流程图。
海中鱼巢当前实现尚未全部形成完整现状流程图包。
现有 45 对现状图建立了历史证据基线，但版本漂移、源码绑定和实现后回绘尚未收口。
```

#240 已按 S0-S5 分批，S2 先做 45 对基线漂移分类和 21 个源码绑定缺口；入口总图依赖 #236，其它旧证据冻结和现状回绘可先执行。

## 5. 调用路径

```text
旧 / 当前 Git 与代码
-> 运行主链提取
-> 现有流程图递归清单、代码版本与文件引用审计
-> 完整纠偏包机械检查
-> 覆盖矩阵与偏差清单
-> #240 文档修订计划
```

## 6. 验证方式

```text
Git 状态和提交号已读取。
旧 HEAD 121 个源码文件、202635 行；当前 HEAD 59 个源码文件、42907 行已统计。
HEAD 102 份流程图 Markdown、45 对专门目录现状图和 43 份合并核查表已递归统计。
现状图元数据绑定 38/59 个 HEAD 源码，21 个未绑定；3f08239..15032ed 有 17 个源码变化。
`generate_current_flowcharts.py --check` 通过 45 对文件配对检查，但不作为代码当前性证明。
CONCEPT-S5 独立包结构可通过严格检查，但目标代码和兼容副本漂移已另列缺口。
THREAD-S1 缺失产物已由文件事实复现，两个目标代码文件未发生版本漂移。
本轮总审计包已在计划登记后通过 `check_flowchart_correction.py --strict --require-plan-registration`。
`tools/check_specs.py --strict` 已通过；`git diff --check` 未发现空白错误，仅报告既有 LF / CRLF 提示。
```

## 7. 已知风险

1. 旧仓库仍有两个 dirty C++ 和一份计划变化；旧证据必须在 S0 冻结。海中鱼巢本轮只统计 HEAD `15032ed`，明确排除并行工作树变化。
2. 旧自我线程图存在失效证据路径，执行 #240 S1B 前必须修订。
3. `入口.cpp` 与两个 #211 新模块正在由 JY-254 并行收口；它们不在本批次暂存范围，总入口图不得抢跑 #236。
4. 本轮未构建、未运行程序，不评价运行回归。
