# METHOD-ACTION-REL-S1 方法动作场景角色关系代码实施记录

日期：2026-07-15

状态：#279 / DQ-171 已完成 / JY-347、JY-349 已落实 / QR-183、QR-184 已关闭 / A01-A16 通过 / 785 已登记 / 已构建 / 已运行 / 已归档

## 1. 输入与实际接口复核

```text
正式实施基线：06e02b0
流程图/20260715_方法动作场景角色关系代码逻辑流程图_v0.1.md/.html
规范/详细设计/方法动作场景角色关系详细设计.md
流程图/20260715_服务模块单一声明所有权与规格构造代码逻辑流程图_v0.1.md/.html
规范/详细设计/服务模块单一声明所有权与规格构造详细设计.md
计划/已完成计划/20260715_METHOD-ACTION-REL-S1_方法动作场景角色关系代码实施切片_v0.1.md
实施记录/20260715_METHOD-ACTION-REL-S1_服务模块单一声明所有权设计修订_Codex断点清单.md
JY-349 / WIN-0001-E61
```

实施前重新确认：7 个数据操作、9 个业务服务、22 个私有写入规格和 14 个正式服务公开面与修订计划一致；关系类型上限仍为 16，动作入口仍写普通引用槽 1 / 2，阶段 785 与新自检模块名未占用。允许文件足以闭合，未恢复或续写非权威 stash `5a0c9a3`。

## 2. 服务模块单一所有权

9 个业务服务类、服务请求和服务自有包装结果继续只由对应 `服务.*.ixx` 声明 / 定义。7 个数据操作模块删除跨模块服务前置声明，22 个私有写入规格删除服务友元，构造权只授予同模块数据操作类。

7 个数据操作新增 22 个一一对应的窄规格形成入口。服务完成业务准入后按值调用对应入口；入口只复核值式结构不变量并形成规格，不取得许可、不读写仓库、不请求提交、不接收服务 DTO。运行期 14 个服务、7 个组合器和 1 个路由公开面没有扩大。

## 3. 关系 17 与动作入口

新增 `关系类型::方法动作场景 = 17`，关系仓库固定：

```text
顺序号 1 = 输入场景
顺序号 2 = 输出场景
源节点类型 = 方法
目标节点类型 = 场景
有效唯一键 = 关系类型 + 完整源动作入口句柄 + 顺序号
同一场景可同时承担输入 / 输出角色
同一角色第二目标或重复目标均写前拒绝
普通与结构化通用重挂均拒绝关系 17
```

新方法动作入口只写关系 17。读取先复核关系 17 的两个角色；纯旧普通引用槽 1 / 2 继续严格只读兼容，不双写、不自动迁移。部分结构、重复角色、未知槽或新旧混存返回内部不一致。普通引用继续按类型 + 源 + 目标拒绝重复，关系 15 / 16 原义保持。

## 4. 结构承载与调用路径

```text
业务服务入口准入
-> 对应数据操作窄规格形成入口
-> 数据操作执行正式结构写入
-> 结构写入执行器 / 不透明会话
-> 关系仓库写入关系 17
-> 方法业务服务按输入 / 输出角色权威读回
```

`海中鱼巣/领域/自检.方法动作场景关系.ixx` 是独立 Debug 真模块，只通过正式业务服务公开面承载 A01-A16。`入口.cpp` 只增加 Debug import、一个值式结果、一次阶段 785 登记和总结果汇总；工程和 filters 各登记一次，Release 不导入、不调用、不登记。

## 5. 实施期根因复核

A12 初次自检错误地把已删除记录视为可审计，并使用失效前旧版本句柄读取失效审计。当前仓库合同是删除后普通读取与审计均不可读；失效后必须使用失效结果返回的当前关系句柄读取审计。自检已按实际合同修正，生产代码不变。

A13 初次自检额外要求固定故障入口结果必须为 `内部不一致`，而计划只要求每个新增写点独立命中并证明完整回前态。已删除超范围的状态限制，仍逐点复核节点、关系、主键数量和可读结构恢复。该修正没有放宽回滚或结构不变量。

## 6. 验收与静态门禁

METHOD-ACTION-REL-S1-A01 至 A16 全部通过，覆盖数值与未知关系 18、角色槽、端点类型、同场景双角色、同角色冲突、普通关系回归、新动作入口、混存拒绝、旧引用兼容、幂等 / 异义、重挂 / 失效 / 删除 / 审计、两个写点故障回前态、50 组并发单提交、模块所有权和独立链接 / Release 隔离。

```text
data-ops=7
services=9
spec-factories=22
forward-decls=0
service-friends=0
non-service-data-op-calls=0
formal-services=14
composers=7
routes=1
forbidden-capability-hits=0
```

C++ 与工程共 24 个变更文件，全部位于 #279 允许范围；线程、组合器、运行期装配、传统服务头、控制面板、SQL、持久化、核心事务 ABI、#224、#225、#257 和 #214 均未修改。

## 7. 验证证据

```text
Debug x64 完整 Rebuild：通过，0 警告、0 错误。
Debug --self-test-exit：退出码 0，METHOD-ACTION-REL-S1 A01-A16 全部通过。
Release x64 完整 Rebuild：通过，0 警告、0 错误。
Release --self-test-exit：退出码 0。
Release 输出命中：METHOD-ACTION-REL-S1、自检显示名和阶段 785 均为 0。
Release 二进制命中：切片名、自检显示名、两个故障入口和固定故障控制均为 0。
最终 Debug 完整自检：连续 20/20，全部退出码 0。
tools/check_specs.py --strict：通过。
git diff --check、git diff --cached --check：通过。
阶段 785：唯一登记。
未知关系 18：三个预期自检负例命中。
模块所有权、规格调用、正式公开面、禁止能力和允许文件扫描：通过。
```

## 8. 状态与下一步

#279 / DQ-171 已完成并归档，A01-A16 通过，阶段 785 已登记。#224 / DQ-116 的 #279、785 前置已满足，但按计划只转待设计窗口从本次正式提交逐签名复核并重新发布；本轮不实施 600。#257 继续等待 #224 正式完成，790 未实施；#225 继续等待 #224、600；#214 继续用户暂停，590 未实施。

本完成声明只证明服务模块单一所有权、关系 17、动作入口新读写、旧引用严格兼容和对应失败 / 并发边界已实现验证。不得扩大为传统服务兼容 ABI 已删除、任务执行 / 回执 / 结果结算已完成、仓库优化已实施、旧动作关系已自动迁移或项目迁移完成。
