# 方法系统详细设计小修确认 Codex 断点清单

日期：2026-07-06
状态：已完成 / 文档小修后确认 / 未改代码

## 1. 输入入口

用户确认结论：

```text
方法系统详细设计方向通过。
正式确认前需补齐方法角色状态绑定、条件 / 结果节点绑定口径、MI-M3 来源任务 / 父方法 / 前置后续方法函数占位、MI-M4 条件判定索引 / 结果初始状态函数占位。
动作入口仍由动作入口规范和详细设计另行确认。
```

## 2. 本次范围

允许：

```text
修订方法系统规范和方法系统详细设计。
同步计划索引和项目记忆状态。
补充断点和验证记录。
```

禁止：

```text
修改代码。
迁移旧方法主信息字段。
迁移旧方法函数体。
实现动作入口。
声明方法系统代码已实现或方法查找 / 执行系统已完成。
```

## 3. 待修订要点

```text
P0-1：方法角色状态绑定到方法节点，第一轮采用抽象状态节点，不携带发生时间戳。
P0-2：方法条件 / 结果节点创建时自动绑定方法首，避免未绑定半结构被读取为有效条件 / 结果。
P0-3：MI-M3 来源任务、父方法、前置方法、后续方法补函数占位，第一轮暂缓实现。
P1-1：MI-M4 条件判定索引和结果初始状态补函数占位，第一轮暂缓实现。
P1-2：明确方法系统确认不覆盖 MI-M2 动作入口。
P1-3：补有效方法首判定。
```

## 4. 验证方式

```powershell
rg -n "方法角色状态|抽象状态|自动绑定方法首|方法来源任务|前置方法|结果初始状态|动作入口|有效方法首" 规范 计划 项目记忆 实施记录
rg -n "方法系统.*待确认|不能声明方法系统规范已确认|不能证明方法系统详细设计已确认|确认前不得修改 `方法服务.h`" 规范 计划 项目记忆 实施记录 --glob "!实施记录/20260706_方法系统详细设计小修确认_Codex断点清单.md" --glob "!实施记录/20260705_主信息相关规范候选提取待确认清单.md"
git diff --check
```

本切片不改代码，不运行构建和默认入口。

## 5. 实际修订

```text
已补方法角色状态承载口径：第一轮使用抽象状态节点，不携带发生时间戳。
已补方法节点到唯一角色状态的绑定和方法服务专用读取边界。
已补方法条件 / 结果节点创建时自动绑定方法首的口径，未绑定节点不得读为有效条件 / 结果。
已补 MI-M3 来源任务、父方法、前置方法、后续方法函数占位，第一轮暂缓。
已补 MI-M4 条件判定索引和结果初始状态函数占位，第一轮暂缓。
已补有效方法首、有效方法条件、有效方法结果判定。
已明确方法系统确认不覆盖 MI-M2 动作入口。
```

## 6. 验证结果

```text
文档一致性检索已执行。
git diff --check 已执行。
本切片未修改代码，未运行构建，未运行默认入口。
```

## 7. 下一步

```text
按计划索引继续进入任务系统第一轮代码实施。
动作入口规范和详细设计已在后续切片小修后确认；非权威缓存与统计规范和详细设计也已在后续切片小修后确认；需求结算记录仍待用户确认。
```
