# 迁移路线权力分层修订 Codex 断点清单

日期：2026-07-08

## 1. 输入入口

用户修订迁移口径：

```text
1. 先生成服务操作函数矩阵；该算法为通用算法，且与原项目差距较大，不再逐项迁移。
2. 项目功能按原项目功能流程图抽取、修改优化后，在本项目重构。
```

本次执行为文档治理和项目记忆同步，不修改 C++。

## 2. 核心处理

1. 新增 `规范/迁移路线权力分层规范.md`。
2. 将迁移权力分为：

```text
证据层
-> 服务矩阵层
-> 流程图迁移层
-> 批次级实施包层
```

3. 将已被 `计划/20260707_服务操作函数矩阵第一批代码实施包_v0.1.md` 归并的 6 份小切片移出 `计划/待确认计划/`，保留到 `计划/已完成计划/` 作为证据。
4. 将动态聚合、因果模板、实例状态生命周期、需求树后续结构、任务状态机筹办执行桥、世界服务兼容壳、抽象状态动态和实例状态动态生命周期等详细设计标记为候选设计。
5. 同步计划索引、项目记忆、规则迁移清单和规范治理检查工具。

## 3. 结构承载

```text
规范/迁移路线权力分层规范.md
规范/000_项目规则总纲.md
规范/001_规则迁移清单.md
规范/详细设计/服务操作函数矩阵第一批.md
tools/check_specs.py
计划/计划索引.md
计划/已完成计划/20260707_FS02_动态聚合运动基元候选代码实施切片_v0.1.md
计划/已完成计划/20260707_FS02_因果模板查询候选代码实施切片_v0.1.md
计划/已完成计划/20260707_FS02_实例状态生命周期读取阻断代码实施切片_v0.1.md
计划/已完成计划/20260707_FS04_需求树后续结构代码实施切片_v0.1.md
计划/已完成计划/20260707_FS05_任务状态机筹办执行桥代码实施切片_v0.1.md
计划/已完成计划/20260707_基础信息服务分层完整增强后续代码实施切片_v0.1.md
项目记忆/当前状态.md
项目记忆/决策记录.md
项目记忆/Codex任务队列.md
项目记忆/待确认问题.md
项目记忆/验收记录.md
项目记忆/实施记录.md
```

## 4. 输出变化

| 项 | 当前结果 |
| --- | --- |
| 旧计划和工作结果 | 保留为证据、边界材料或候选材料，不删除。 |
| 6 份小切片 | 已归并关闭，移入 `计划/已完成计划/`，不再单独确认，不单独入队。 |
| 候选详细设计 | 保留，但进入 C++ 前必须经服务矩阵和应用逻辑流程图迁移包重审。 |
| 后续默认路线 | 服务操作函数矩阵 -> 应用逻辑流程图迁移包 -> 批次级代码实施包 -> 批次级验收。 |

## 5. 验证方式

计划执行以下文档验证：

```powershell
git diff --check
python .\tools\check_specs.py
python -m py_compile .\tools\check_specs.py
```

本次不运行 C++ 构建，不运行默认入口。

## 6. 禁止扩大声明

本次只证明：

```text
迁移路线权力分层规范已生成。
计划和项目记忆已按新口径同步。
已归并小切片已从待确认目录移出并证据保留。
候选详细设计已标记为需重审。
```

不能证明：

```text
服务矩阵全部完成。
应用逻辑流程图迁移完成。
C++ 代码新增完成。
旧函数体已迁移。
旧项目能力已迁移完成。
```
