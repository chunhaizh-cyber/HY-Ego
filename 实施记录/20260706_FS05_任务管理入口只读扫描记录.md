# FS-05 任务管理入口只读扫描记录

日期：2026-07-06
状态：FS-05 第一批已登记 / 只读扫描 / 未改代码 / 未迁移旧函数
依据清单：`实施记录/20260706_逐函数只读扫描清单.md`

## 1. 本轮定位

本轮进入 FS-05，登记旧项目 `任务类` 和 `任务主信息类` 中与任务承接、任务主信息、任务虚拟存在、任务目标 / 实际结果状态、任务生命周期状态、任务方法关系、任务角色阶段、权重统计、任务创建和 SQL 投影有关的函数族。

本轮只做只读事实登记，不迁移旧 `任务主信息类` 字段，不把旧任务树、任务步骤节点、线程派发字段、SQL 投影或控制面板显示作为新项目任务事实。当前项目口径仍是：任务只承接一个需求，任务目标来自需求目标状态，任务虚拟存在由任务服务经存在服务创建，生命周期时间由任务生命周期实例状态发生时间戳承载，任务服务不得直接访问特征值服务。

## 2. 只读扫描范围

```text
D:\鱼巢\任务主信息类.h
D:\鱼巢\任务类.h
D:\鱼巢\任务类.cpp
D:\鱼巢\任务节点类型.h
D:\鱼巢\任务信息类型.h
```

本轮暂不展开扫描：

```text
D:\鱼巢\任务模块.筹办.ixx
D:\鱼巢\任务模块.执行.ixx
D:\鱼巢\任务模块.管理工作线程.ixx
D:\鱼巢\任务模块.管理界面线程.impl.cpp
```

上述文件进入后续 FS-05 批次或 FS-X 排除边界。本轮不运行旧项目程序、不接线程、不接 SQL、不接控制面板。

扫描证据来自：

```text
任务类.cpp:3-7：旧任务类文件头部规则，任务树正式变化经任务类和任务管理提交入口，SQL 只投影显示，任务虚拟存在不挂入世界树。
任务主信息类.h:16-54：旧任务状态、阶段、节点种类和任务角色枚举。
任务主信息类.h:57-78：旧任务主信息字段，含名称、类型、节点种类、创建 / 启动 / 完成时间、对应需求、任务虚拟存在、场景、目标状态和实际结果状态。
任务类.h:13-95：任务虚拟存在、可执行输入参数场景、运行镜像和 SQL 投影入口。
任务类.h:97-135：任务根实体、控制实体和局部运行实体特征写入入口。
任务类.h:137-229：对应需求、虚拟存在、场景、目标状态、实际结果状态、生命周期状态、任务管理对象、执行方法和候选方法虚拟存在入口。
任务类.h:229-303：选中能力、任务角色、阶段、等待缺口、优先级、权重、运行统计、错误码和特征账本入口。
任务类.cpp:23-724：任务场景、任务虚拟存在、默认任务名称 / 类型、任务运行特征和权重推导内部辅助函数。
任务类.cpp:767-1458：任务树 SQL 投影、字段恢复比对和显示镜像函数族。
任务类.cpp:1468-1799：任务虚拟存在、任务特征、可执行输入参数场景、初始化和运行镜像同步函数族。
任务类.cpp:1803-1887：任务根实体、任务控制实体和局部运行实体特征写入函数族。
任务类.cpp:1895-2178：任务状态终结、对应需求 / 虚拟存在 / 场景 / 目标状态 / 实际结果状态读写、生命周期时间读取、任务状态和节点种类读写、派发准入和初始状态函数族。
任务类.cpp:2182-2390：任务管理对象、任务执行方法、当前方法虚拟存在和方法候选虚拟存在函数族。
任务类.cpp:2397-2507：选中自我能力、任务角色、当前阶段和等待缺口唤醒函数族。
任务类.cpp:2510-2686：优先级、权重、运行次数、成功 / 失败次数、缺口转入次数和最近错误码函数族。
任务类.cpp:2690-2724：任务特征使用统计和特征账本生命周期读取函数族。
任务类.cpp:2728-2783：创建任务头结点函数。
```

## 3. 逐函数登记

### OF-128

```text
旧函数编号：OF-128
扫描批次：FS-05
旧项目路径：D:\鱼巢\任务主信息类.h:16
旧函数名：枚举_任务状态 / 枚举_任务阶段 / 枚举_任务节点种类 / 枚举_任务角色 / 任务主信息类::比较 / 生成比较量 / 命中比较条件
旧函数签名：任务状态、阶段、节点种类、任务角色和主信息比较函数族。
当前调用方：旧任务树、任务类读写入口、任务管理线程、SQL 投影和显示诊断。
触发路径：任务节点创建、状态写入、阶段推导、任务角色判断、主信息查找和显示投影。
输入：任务主信息对象、比较字段、状态 / 阶段 / 角色枚举值。
输出：比较量、命中 bool、枚举值或文本投影材料。
读结构：旧任务主信息字段。
写结构：无；字段由其它入口写入。
是否写机器事实：枚举字段本身是旧机器事实；比较函数只读。
是否依赖日志：否。
是否依赖显示：枚举文本可服务显示。
是否依赖 SQL：间接被 SQL 投影读取。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：任务状态、阶段、角色和节点种类需要进入新任务系统角色 / 生命周期设计。
删除行为：不得把旧节点种类、状态、阶段、角色枚举作为主信息裸字段迁移。
新增约束：任务生命周期由实例状态和发生时间戳承载；任务角色由任务角色状态或关系承载。
目标新结构：任务节点、任务角色状态、任务生命周期实例状态、任务关系角色。
目标领域服务入口：任务服务 `记录任务生命周期状态`、后续任务角色读取入口。
对应规范 / 详细设计：任务系统规范、任务系统详细设计、状态服务详细设计。
入口拒绝条件：任务节点无效、状态值未定义、角色值未确认、节点种类被当作节点类型。
验证方式：任务状态 / 角色读取只通过任务服务入口，不写旧主信息字段。
初步判定：拆分
待用户确认项：任务角色和任务阶段是否进入任务系统后续增强专项。
扫描证据：`任务主信息类.h:16` 至 `任务主信息类.h:116`。
```

### OF-129

```text
旧函数编号：OF-129
扫描批次：FS-05 / FS-06 / FS-08
旧项目路径：D:\鱼巢\任务类.cpp:23
旧函数名：私有_推导任务场景 / 私有_取需求目标状态 / 私有_取需求当前状态 / 私有_默认任务名称词 / 私有_默认任务类型词 / 私有_推导基准优先级 / 私有_构建任务虚拟存在概念集
旧函数签名：任务承接前置材料、默认语义、场景和权重推导内部辅助函数族。
当前调用方：创建任务头结点、任务虚拟存在初始化、任务运行特征初始化。
触发路径：从来源需求和上级任务推导任务场景、目标状态、实际结果初值、任务名称 / 类型、概念集和基准优先级。
输入：来源需求、上级任务、目标特征类型、任务头结点。
输出：场景、状态、语素入口、存在概念、权重值。
读结构：旧需求主信息、旧任务主信息、语素入口、世界默认场景、需求权重字段。
写结构：部分默认词函数会创建语素入口词；不直接写任务树。
是否写机器事实：语素入口创建是结构写入；推导结果本身不是权威任务事实。
是否依赖日志：否。
是否依赖显示：名称 / 类型词可服务显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：任务场景和目标状态来自来源需求；任务虚拟存在可补概念集。
删除行为：不得用显示名称、类型词或权重字段裁决任务事实。
新增约束：任务服务创建任务时必须预读需求唯一目标状态，场景必须有效或由确认规则取得。
目标新结构：任务来源需求关系、任务运行场景关系、任务目标状态关系、任务虚拟存在关系。
目标领域服务入口：任务服务 `按需求创建任务`、需求服务 `读取需求目标状态`。
对应规范 / 详细设计：任务系统详细设计、需求目标状态承载详细设计、领域服务授权写入规范。
入口拒绝条件：来源需求无效、需求目标状态缺失、场景无效、权重被当业务事实。
验证方式：任务承接壳可回读来源需求、场景、目标状态和虚拟存在。
初步判定：拆分
待用户确认项：任务名称 / 类型词是否进入显示层只读视图，任务优先级是否进入任务管理后续增强。
扫描证据：`任务类.cpp:23` 至 `任务类.cpp:724`。
```

### OF-130

```text
旧函数编号：OF-130
扫描批次：FS-05 / FS-10 / FS-X
旧项目路径：D:\鱼巢\任务类.cpp:767
旧函数名：私有_同步SQL控制面板任务显示项 / 私有_收集任务树SQL行 / 私有_任务树SQL建库脚本 / 私有_任务树SQL建表脚本 / 私有_任务树SQL视图脚本 / 私有_构造任务树SQL重写脚本 / 私有_执行任务SQL字段恢复比对 / 重写任务树SQL投影
旧函数签名：任务 SQL 控制面板显示镜像和任务树 SQL 投影函数族。
当前调用方：旧任务状态写入、任务树投影刷新、控制面板读窗口和字段恢复比对。
触发路径：把任务树和任务主信息投影到 SQL Server 查询表和视图。
输入：任务根节点、来源原因、字段恢复比对标记。
输出：局部 bool、SQL 表、视图和显示镜像。
读结构：旧任务树、任务主信息、任务状态、任务目标 / 结果状态、需求引用、场景引用。
写结构：SQL 控制面板显示项、投影库任务树快照、任务树节点和任务主信息表。
是否写机器事实：否，按旧文件规则只应是显示镜像。
是否依赖日志：是，写运行日志和错误日志。
是否依赖显示：是。
是否依赖 SQL：是。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：可作为后续显示层只读视图字段参考。
删除行为：本轮不得接 SQL、控制面板或投影库；SQL 失败不得影响任务事实。
新增约束：显示层只读结构，不写业务事实；数据库只做快照、事件日志、恢复和审计适配。
目标新结构：显示层只读视图后续专项，不进入当前机器事实结构。
目标领域服务入口：无，显示专项后续确认。
对应规范 / 详细设计：领域服务授权写入规范、函数迁移前置专项、显示层只读视图后续专项。
入口拒绝条件：当前本轮直接拒绝 SQL / 控制面板接入。
验证方式：只登记排除边界，不运行 SQL，不连接控制面板。
初步判定：排除 / 后续专项
待用户确认项：任务视图字段是否进入显示层只读视图专项。
扫描证据：`任务类.cpp:767` 至 `任务类.cpp:1458`。
```

### OF-131

```text
旧函数编号：OF-131
扫描批次：FS-05 / FS-03 / FS-08
旧项目路径：D:\鱼巢\任务类.cpp:1468
旧函数名：取或创建_任务虚拟存在 / 记录任务虚拟存在特征 / 读取任务虚拟存在特征_I64 / 读取任务虚拟存在特征_指针 / 记录任务虚拟存在指针特征 / 查找任务虚拟存在特征节点 / 取或创建任务虚拟存在特征节点 / 初始化任务虚拟存在信息 / 补齐任务虚拟存在概念集 / 同步任务节点到虚拟存在 / 同步任务运行镜像特征
旧函数签名：任务虚拟存在创建、特征读写、概念补齐和运行镜像同步函数族。
当前调用方：任务创建、任务状态 / 阶段 / 角色 / 方法关系写入、任务统计和控制面板镜像。
触发路径：任务主信息持有虚拟存在引用，任务运行特征和统计通过旧特征服务挂到任务虚拟存在。
输入：任务节点、特征类型、I64 / 指针 / Vec 值、场景、时间戳。
输出：任务虚拟存在、特征节点、特征值、局部 bool。
读结构：旧任务主信息任务虚拟存在引用、旧特征树、旧语素入口、旧存在概念。
写结构：旧任务主信息任务虚拟存在字段、存在节点、存在概念关系、特征节点和特征值。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：任务名称 / 类型可服务显示。
是否依赖 SQL：间接被 SQL 投影读取。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：任务虚拟存在承载任务运行特征和过程账。
删除行为：不得把任务虚拟存在句柄写回新主信息裸字段；高级服务不得直接访问特征值服务。
新增约束：任务虚拟存在必须由存在服务创建，任务特征材料必须通过特征服务处理。
目标新结构：任务虚拟存在关系、存在节点、任务运行场景关系、特征服务管理的特征状态材料。
目标领域服务入口：任务服务 `读取任务虚拟存在`，存在服务，特征服务。
对应规范 / 详细设计：任务系统详细设计、存在服务详细设计、特征服务边界、领域服务授权写入规范。
入口拒绝条件：无效任务、无效场景、缺发生时间戳、虚拟存在创建失败、特征类型无效、直接调用特征值服务。
验证方式：有效任务可回读唯一虚拟存在；缺虚拟存在不得被读作完整任务承接壳。
初步判定：改写为领域服务入口
待用户确认项：任务运行统计哪些保留为非权威缓存或任务过程账。
扫描证据：`任务类.cpp:1468` 至 `任务类.cpp:1799`。
```

### OF-132

```text
旧函数编号：OF-132
扫描批次：FS-05 / FS-08
旧项目路径：D:\鱼巢\任务类.cpp:1803
旧函数名：写入任务根实体特征 / 写入任务控制实体特征 / 写入任务局部运行实体特征
旧函数签名：任务根实体、控制实体和局部运行实体特征写入函数族。
当前调用方：旧任务管理线程、控制意图承接、任务队列、局部运行状态和控制面板镜像。
触发路径：把任务根 ID、来源需求指针、父任务指针、授权等级、队列状态、优先级、时间 / 资源预算、工作线程 ID、派发标记、等待标记、步骤前沿和局部完成度写到任务虚拟存在特征。
输入：任务节点、任务根 ID、指针、I64 状态、bool 标志、场景、时间戳。
输出：局部 bool。
读结构：旧任务主信息、旧任务虚拟存在。
写结构：任务虚拟存在 I64 特征和指针特征。
是否写机器事实：是，但大量字段属于旧运行态 / 控制态。
是否依赖日志：否。
是否依赖显示：可服务显示。
是否依赖 SQL：间接服务控制面板和投影。
是否依赖线程：是，绑定工作线程和派发状态来自旧线程治理。
是否依赖外设 / D455 / 体素：否。
保留行为：任务控制态和局部运行态可作为后续任务管理专项参考。
删除行为：不得迁移裸指针、工作线程 ID、派发 bool、控制请求 bool 为新项目权威任务字段。
新增约束：线程不是动作来源；任务状态必须由任务服务写入结构状态，控制态不得绕过任务服务。
目标新结构：任务生命周期状态、任务选择方法关系、任务实际结果状态关系、非权威运行统计或任务管理后续结构。
目标领域服务入口：任务服务后续任务管理 / 调度入口。
对应规范 / 详细设计：任务系统详细设计、非权威缓存与统计详细设计、领域服务授权写入规范。
入口拒绝条件：任务无效、线程字段被当动作来源、bool 被事实化、缺发生时间戳。
验证方式：只登记运行态字段，不写代码，不把线程 / 控制标记迁移为任务事实。
初步判定：排除 / 后续专项
待用户确认项：任务控制实体是否进入任务管理后续增强专项。
扫描证据：`任务类.cpp:1803` 至 `任务类.cpp:1887`。
```

### OF-133

```text
旧函数编号：OF-133
扫描批次：FS-05
旧项目路径：D:\鱼巢\任务类.cpp:1895
旧函数名：任务状态已终结 / 读取任务状态 / 写入任务状态 / 读取任务节点种类 / 写入任务节点种类 / 任务允许派发 / 确保任务初始状态
旧函数签名：任务生命周期状态、节点种类、派发准入和初始状态函数族。
当前调用方：旧任务创建、任务管理线程、任务执行、SQL 投影和控制面板显示。
触发路径：读写任务虚拟存在状态特征，完成时回写完成时间，状态写入后同步 SQL 显示。
输入：任务节点、任务状态、节点种类、场景、时间戳。
输出：任务状态、节点种类、派发 bool、局部 bool。
读结构：旧任务主信息、任务虚拟存在特征。
写结构：旧任务主信息完成时间、节点种类、任务虚拟存在 I64 特征、SQL 显示镜像。
是否写机器事实：是。
是否依赖日志：SQL 失败时会写日志。
是否依赖显示：是，状态写入同步控制面板显示。
是否依赖 SQL：是，间接同步。
是否依赖线程：派发准入服务旧线程调度。
是否依赖外设 / D455 / 体素：否。
保留行为：任务生命周期状态和发生时间戳必须结构化。
删除行为：不得把创建 / 启动 / 完成时间作为主信息字段迁移；不得用 SQL 同步成功证明任务状态。
新增约束：生命周期时间由实例状态发生时间戳承载；任务完成不能只看状态值，必须有目标状态和实际结果状态比较。
目标新结构：任务生命周期实例状态、任务目标状态关系、任务实际结果状态关系。
目标领域服务入口：任务服务 `记录任务生命周期状态`、`标记任务完成` 后续入口。
对应规范 / 详细设计：任务系统详细设计、状态服务详细设计、错误结果与入口拒绝规范。
入口拒绝条件：任务无效、状态未定义、缺发生时间戳、无目标状态、实际结果未达目标。
验证方式：任务创建 / 启动 / 完成状态可读发生时间戳；SQL 不参与事实裁决。
初步判定：拆分
待用户确认项：旧任务状态枚举与新任务生命周期状态草案如何对齐。
扫描证据：`任务类.cpp:1895` 至 `任务类.cpp:2178`。
```

### OF-134

```text
旧函数编号：OF-134
扫描批次：FS-05
旧项目路径：D:\鱼巢\任务类.cpp:1905
旧函数名：读取任务对应需求引用 / 解析任务对应需求 / 读取任务虚拟存在引用 / 解析任务虚拟存在 / 读取任务场景引用 / 解析任务场景 / 读取任务目标状态引用 / 解析任务目标状态 / 写入任务目标状态 / 读取任务实际结果状态 / 解析任务实际结果状态 / 写入任务实际结果状态 / 绑定对应需求 / 读取任务创建时间 / 读取任务启动时间 / 读取任务完成时间
旧函数签名：任务来源需求、虚拟存在、场景、目标状态、实际结果状态和生命周期时间读写函数族。
当前调用方：旧任务创建、任务初始化、任务完成判断、需求回写、SQL 投影和方法执行。
触发路径：从旧任务主信息读写对应需求、目标状态、实际结果状态和时间字段。
输入：任务节点、需求节点、状态节点或可解析引用。
输出：引用、解析节点、时间戳或局部 bool。
读结构：旧任务主信息对应需求、任务虚拟存在、场景、目标状态、实际结果状态、创建 / 启动 / 完成时间。
写结构：旧任务主信息对应需求、目标状态、实际结果状态。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：间接被 SQL 投影读取。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：任务必须可追溯来源需求、虚拟存在、场景、目标状态和实际结果状态。
删除行为：不得把这些旧主信息字段直接迁移。
新增约束：来源需求、虚拟存在、场景、目标状态和实际结果状态由任务关系角色承载；时间由生命周期状态发生时间戳承载。
目标新结构：任务来源需求关系、需求拥有任务关系、任务虚拟存在关系、任务场景关系、任务目标状态关系、任务实际结果状态关系。
目标领域服务入口：任务服务 `读取任务来源需求`、`读取任务虚拟存在`、`读取任务目标状态`、后续 `读取任务实际结果状态`。
对应规范 / 详细设计：任务系统详细设计、需求结算记录详细设计、状态服务详细设计。
入口拒绝条件：任务无效、需求无效、目标状态无效、实际状态无效、唯一关系多目标。
验证方式：有效完整任务承接壳可通过任务服务专用读取入口回读。
初步判定：改写为领域服务入口
待用户确认项：任务实际结果状态最新规则是否进入任务管理后续增强。
扫描证据：`任务类.cpp:1905` 至 `任务类.cpp:2055`。
```

### OF-135

```text
旧函数编号：OF-135
扫描批次：FS-05 / FS-06
旧项目路径：D:\鱼巢\任务类.cpp:2182
旧函数名：读取任务管理对象任务 / 读取任务管理对象或自身 / 任务管理对象指向 / 写入任务管理对象任务 / 读取任务执行方法 / 任务执行方法指向 / 写入任务执行方法 / 写入任务当前方法虚拟存在 / 追加任务方法候选虚拟存在 / 读取任务方法候选虚拟存在集
旧函数签名：任务管理对象、当前执行方法、当前方法虚拟存在和候选方法虚拟存在函数族。
当前调用方：旧任务筹办、任务执行、方法候选、任务管理线程和控制面板。
触发路径：用任务虚拟存在指针特征保存管理对象任务、执行方法、当前方法虚拟存在和方法候选集合。
输入：任务节点、目标任务、方法节点、方法虚拟存在、场景、时间戳。
输出：任务节点、方法节点、存在节点集合、局部 bool。
读结构：任务虚拟存在指针特征、方法虚拟存在、任务主信息。
写结构：任务虚拟存在指针特征、候选方法特征节点。
是否写机器事实：是，旧项目用于任务方法选择和执行授权。
是否依赖日志：否。
是否依赖显示：候选可服务显示。
是否依赖 SQL：间接服务控制面板。
是否依赖线程：任务管理对象服务旧任务管理线程。
是否依赖外设 / D455 / 体素：否。
保留行为：任务选择方法是方法执行权限依据。
删除行为：不得用指针特征作为新项目任务方法关系；不得把候选集合当选中事实。
新增约束：当前任务选择方法由任务服务写任务选择方法关系；候选方法集合后续设计，方法服务不得绕过任务授权。
目标新结构：任务选择方法关系、方法虚拟存在关系、后续候选方法集合结构。
目标领域服务入口：任务服务 `选择任务方法`、方法服务只读入口。
对应规范 / 详细设计：任务系统详细设计、方法系统详细设计、动作入口详细设计。
入口拒绝条件：任务无效、方法无效、方法虚拟存在无效、候选被当选中事实、缺任务方法关系却执行方法。
验证方式：方法执行必须复核任务方法关系；候选读取不形成执行许可。
初步判定：拆分
待用户确认项：候选方法集合和任务管理对象是否进入任务管理 / 方法系统后续增强专项。
扫描证据：`任务类.cpp:2182` 至 `任务类.cpp:2390`。
```

### OF-136

```text
旧函数编号：OF-136
扫描批次：FS-05 / FS-06
旧项目路径：D:\鱼巢\任务类.cpp:2397
旧函数名：读取任务选中自我能力值 / 写入任务选中自我能力值 / 读取任务角色 / 写入任务角色 / 读取任务当前阶段 / 写入任务当前阶段 / 读取任务等待缺口唤醒 / 写入任务等待缺口唤醒
旧函数签名：任务选中能力、角色、阶段和等待缺口唤醒读写函数族。
当前调用方：旧任务筹办、任务执行、缺口转需求、方法候选和任务调度。
触发路径：把选中能力值、任务角色、当前阶段和等待缺口标志写入任务虚拟存在 I64 特征。
输入：任务节点、I64 能力值、任务角色、任务阶段、bool 标记、场景、时间戳。
输出：I64、角色、阶段、bool 或局部 bool。
读结构：任务虚拟存在特征、任务状态。
写结构：任务虚拟存在 I64 特征。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：可服务显示。
是否依赖 SQL：间接服务控制面板。
是否依赖线程：任务阶段和等待缺口服务旧任务调度。
是否依赖外设 / D455 / 体素：否。
保留行为：任务角色和阶段可作为后续任务管理状态。
删除行为：不得把选中自我能力 I64、等待缺口 bool 或阶段特征直接迁移为权威字段。
新增约束：任务角色和阶段必须通过任务服务写状态或关系，方法能力选择需任务方法关系承载。
目标新结构：任务角色状态、任务生命周期状态、任务选择方法关系、后续缺口需求关系。
目标领域服务入口：任务服务后续角色 / 阶段入口、任务服务 `选择任务方法`。
对应规范 / 详细设计：任务系统详细设计、方法系统详细设计、错误结果与入口拒绝规范。
入口拒绝条件：任务无效、角色未确认、阶段未确认、bool 被事实化、选中能力缺方法关系。
验证方式：角色 / 阶段只作为任务服务读取结果；方法执行仍需任务方法关系。
初步判定：排除 / 后续专项
待用户确认项：选中自我能力值是否改写为动作入口 / 方法选择结构。
扫描证据：`任务类.cpp:2397` 至 `任务类.cpp:2507`。
```

### OF-137

```text
旧函数编号：OF-137
扫描批次：FS-05 / FS-08
旧项目路径：D:\鱼巢\任务类.cpp:2510
旧函数名：读取 / 写入任务基准优先级、局部优先级偏移、调度优先级、承接安全权重上限、承接服务权重上限、任务权重评分、任务权重版本号、已折算安全值、已折算服务值、运行次数、成功次数、失败次数、连续失败次数、转入缺口需求次数、最近错误码
旧函数签名：任务优先级、权重、运行统计和错误摘要读写函数族。
当前调用方：旧任务管理、任务筹办、任务执行、结算摘要、调度和显示诊断。
触发路径：把任务优先级、权重、成功失败计数、缺口转入次数和错误码写入任务虚拟存在 I64 特征。
输入：任务节点、I64 值、场景、时间戳。
输出：I64 或局部 bool。
读结构：任务虚拟存在特征。
写结构：任务虚拟存在 I64 特征。
是否写机器事实：旧项目中部分为治理事实，部分为非权威统计摘要。
是否依赖日志：否。
是否依赖显示：可服务显示。
是否依赖 SQL：间接服务控制面板。
是否依赖线程：调度优先级和运行次数服务旧调度线程。
是否依赖外设 / D455 / 体素：否。
保留行为：优先级和权重可作为后续任务管理候选材料。
删除行为：不得把统计计数、错误码或权重摘要当作任务完成、需求满足或方法成功事实。
新增约束：统计类材料进入非权威缓存或任务管理专项；权威完成仍由目标状态和实际结果状态比较。
目标新结构：非权威缓存 / 统计记录、任务生命周期状态、需求结算记录。
目标领域服务入口：任务服务后续统计读取入口、非权威缓存统计服务。
对应规范 / 详细设计：非权威缓存与统计详细设计、任务系统详细设计、需求结算记录详细设计。
入口拒绝条件：任务无效、统计值被当业务事实、缺权威来源、缺发生时间戳。
验证方式：清空统计不改变任务来源、目标状态、实际结果和结算事实。
初步判定：排除 / 后续专项
待用户确认项：哪些任务统计保留为非权威缓存。
扫描证据：`任务类.cpp:2510` 至 `任务类.cpp:2686`。
```

### OF-138

```text
旧函数编号：OF-138
扫描批次：FS-05 / FS-08
旧项目路径：D:\鱼巢\任务类.cpp:2690
旧函数名：同步任务特征使用统计 / 读取任务特征账本生命周期标准特征
旧函数签名：任务虚拟存在特征使用统计和特征账本生命周期函数族。
当前调用方：旧任务筹办、候选方法、特征使用统计和任务治理。
触发路径：初始化任务虚拟存在后，将特征使用统计写入任务虚拟存在下的特征节点。
输入：任务节点、特征使用统计、生命周期枚举、场景、时间戳。
输出：局部 bool 或 I64 生命周期值。
读结构：任务虚拟存在、特征统计特征。
写结构：任务虚拟存在统计特征和生命周期特征。
是否写机器事实：统计材料不是业务事实。
是否依赖日志：否。
是否依赖显示：可服务诊断显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：任务虚拟存在可以承载过程账和观察统计。
删除行为：不得把特征使用统计当作任务成功、方法好用或需求满足依据。
新增约束：统计只能非权威，清空后不影响权威结构。
目标新结构：非权威缓存记录、任务虚拟存在过程账、特征服务统计后续入口。
目标领域服务入口：非权威缓存统计服务、特征服务、任务服务只读统计入口。
对应规范 / 详细设计：非权威缓存与统计详细设计、任务系统详细设计、特征服务边界。
入口拒绝条件：任务无效、任务虚拟存在缺失、统计缺权威来源、统计值被事实化。
验证方式：统计缺失不影响任务节点、任务关系、目标状态或生命周期状态读取。
初步判定：排除 / 后续专项
待用户确认项：任务特征账本生命周期是否进入非权威缓存与统计后续增强。
扫描证据：`任务类.cpp:2690` 至 `任务类.cpp:2724`。
```

### OF-139

```text
旧函数编号：OF-139
扫描批次：FS-05
旧项目路径：D:\鱼巢\任务类.cpp:2728
旧函数名：创建任务头结点
旧函数签名：任务类::节点类* 创建任务头结点(需求节点类* 来源需求, 节点类* 上级任务节点, 时间戳 now)
当前调用方：旧需求绑定任务、任务管理、子任务生成和自我线程任务提交。
触发路径：根据来源需求和上级任务创建任务头节点，写入名称、类型、节点种类、创建时间、目标状态、实际结果状态、场景、对应需求、节点种类、状态、阶段、优先级、需求对应任务和任务虚拟存在。
输入：来源需求、上级任务节点、时间戳。
输出：任务头节点或空。
读结构：来源需求目标状态、当前状态、场景、权重，上级任务。
写结构：任务树节点、任务主信息字段、需求对应任务字段、任务虚拟存在、任务状态 / 阶段 / 优先级特征。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：名称 / 类型可服务显示。
是否依赖 SQL：间接由状态写入或需求绑定触发显示同步。
是否依赖线程：旧调用方可能来自自我线程或任务管理线程，但线程不是动作来源。
是否依赖外设 / D455 / 体素：否。
保留行为：按需求创建任务承接壳是新项目第一轮任务系统核心入口。
删除行为：不得迁移旧任务树父子指针、旧主信息字段、需求主信息对应任务字段或任务虚拟存在字段。
新增约束：按需求创建任务必须写需求拥有任务、任务来源需求、任务虚拟存在、任务场景、任务目标状态和任务创建生命周期状态；任一失败不得返回有效承接任务。
目标新结构：任务节点、需求拥有任务关系、任务来源需求关系、任务虚拟存在关系、任务场景关系、任务目标状态关系、任务创建生命周期状态。
目标领域服务入口：任务服务 `按需求创建任务`。
对应规范 / 详细设计：任务系统详细设计、需求目标状态承载详细设计、写入事务详细设计。
入口拒绝条件：无效需求、无效场景、缺发生时间戳、需求无目标状态、需求目标状态不唯一、虚拟存在创建失败、关系写入失败。
验证方式：完整任务承接壳可读来源需求、虚拟存在、目标状态和创建时间戳；半结构不得被读取为有效承接任务。
初步判定：改写为领域服务入口
待用户确认项：旧上级任务 / 子任务树是否进入需求树 / 任务管理后续增强专项。
扫描证据：`任务类.cpp:2728` 至 `任务类.cpp:2783`。
```

## 4. 本轮初步分流

```text
可保留方向：
- 任务必须来源于有效需求。
- 任务目标状态来自需求目标状态。
- 任务虚拟存在承载任务运行特征和过程账。
- 任务生命周期状态必须有发生时间戳。
- 任务选择方法是方法执行权限依据。

必须删除或改写：
- 旧任务主信息中的节点种类、创建 / 启动 / 完成时间、对应需求、任务虚拟存在、场景、目标状态、实际结果状态字段直接迁移。
- 旧任务树步骤节点 / 结果节点作为第一轮任务主线。
- 旧线程 ID、派发 bool、控制请求 bool、裸指针特征作为权威任务事实。
- 旧 SQL 控制面板和投影库作为任务事实来源。

后续专项：
- 需求树 / 任务管理 / 方法系统后续增强专项：任务管理对象、子任务、阶段、候选方法、选中能力和等待缺口。
- 非权威缓存与统计专项：任务运行次数、成功失败次数、权重评分、错误码、特征使用统计。
- 显示层只读视图专项：任务树 SQL 投影和控制面板任务显示字段。
- 事件日志持久化恢复专项：任务生命周期事件审计和恢复材料。
```

## 5. 禁止宣称

```text
不得声明旧任务类、旧任务树或旧任务管理系统已迁移。
不得声明旧任务主信息字段可直接迁移。
不得声明完整任务状态机、任务筹办、任务执行、调度线程或控制面板任务页完成。
不得把旧运行统计、线程字段、SQL 投影或显示文本当作权威任务事实。
不得接控制面板 / SQL / D455 / 体素 / 外设。
```
