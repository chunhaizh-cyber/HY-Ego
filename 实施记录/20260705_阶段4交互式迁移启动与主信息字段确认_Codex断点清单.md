# 阶段 4 交互式迁移启动与主信息字段确认 Codex 断点清单

日期：2026-07-05

## 1. 输入入口

用户确认：

```text
确认,继续
```

承接计划：

```text
计划/计划索引.md
计划/20260705_海中鱼巣全量重构总计划_v0.1.md
```

## 2. 当前判断

用户本次确认只表示：

```text
允许进入阶段 4 旧项目能力交互式迁移流程。
```

本确认不表示：

```text
主信息字段功能已确认
主信息字段允许迁移
旧项目函数允许直接复制
旧项目能力已迁移
```

## 3. 已对齐事实

```text
语素系统规范和详细设计已确认。
语素系统第一轮小能力已实现并验证。
主信息相关规范候选已只读提取。
主信息相关规范讨论结论已确认。
主信息系统规范和主信息系统详细设计已确认。
```

仍未闭合：

```text
主信息字段功能确认。
字段功能确认后的主信息规范更新。
主信息结构或代码迁移。
```

## 4. 本切片处理

本切片只做阶段 4 路由校准和下一确认门禁建立：

```text
更新计划索引中的 HR-001 下一步。
更新项目记忆中的当前状态、任务队列、待确认问题、决策记录、实施记录和验收记录。
校准语素候选提取清单的历史状态，避免和后续确认 / 实现记录冲突。
新增主信息字段功能确认待确认清单。
```

## 5. 下一门禁

下一步必须确认：

```text
实施记录/20260705_主信息字段功能确认待确认清单.md
```

确认方式必须逐项或按字段族明确：

```text
保留
改写
废弃
暂缓
拆到对应领域系统
```

未确认前不得：

```text
更新主信息规范以容纳旧字段
新增主信息字段
迁移旧项目主信息代码
迁移旧项目主信息函数体
```

## 6. 验证方式

本切片为文档和计划路由切片，验证范围为：

```text
git diff --check
计划索引回读
git status
提交并推送到 origin/main
```

不执行构建；不声明代码状态变化。

## 7. 禁止扩大声明

本切片不得声明：

```text
旧项目主信息字段已迁移
主信息迁移完成
旧项目能力已迁移
鱼巢重构完成
自我循环完成
```

## 8. 后续状态

2026-07-05 后续切片已按用户“全部确认”释放主信息字段功能确认门禁。

当前边界：

```text
主信息字段功能第一轮口径已确认。
主信息规范和详细设计已按确认口径更新。
仍未迁移旧项目主信息字段或旧代码。
下一步必须建立主信息迁移实施计划或字段迁移映射计划。
```
