# FS-10 显示层只读候选只读扫描记录

日期：2026-07-06
状态：FS-10 第一批已登记 / 只读扫描 / 未改代码 / 未迁移旧函数
依据清单：`实施记录/20260706_逐函数只读扫描清单.md`

## 1. 本轮定位

本轮进入 FS-10，登记旧项目中可作为后续显示层只读视图设计输入的函数族，并同步登记必须排除或后续专项处理的控制面板、SQL、WebView2、线程和外设 / 体素显示路径。

本轮只做只读事实登记，不运行旧项目，不迁移旧函数体，不新增旧主信息字段，不接控制面板、SQL 实时投影、D455、体素或外设。显示层只允许读取结构并生成人读视图，不得写业务事实，不得作为需求满足、任务完成、方法成功、动作动态或世界事实的裁决来源。

## 2. 只读扫描范围

```text
D:\鱼巢\语素类.cpp
D:\鱼巢\世界树类.h
D:\鱼巢\世界树类.cpp
D:\鱼巢\控制面板类.cpp
D:\鱼巢\控制面板摘要线程模块.impl.cpp
D:\鱼巢\任务模块.管理界面线程.ixx
D:\鱼巢\任务模块.管理界面线程.impl.cpp
D:\鱼巢\鱼巢.cpp
```

本轮同时识别但不进入当前迁移的边界：

```text
旧控制面板 WebView2 窗口、相机窗口、自我场景窗口和截图入口。
旧 SQL 控制面板读模型、HTML 保存和运行态显示镜像。
旧控制台控制面板命令入口和控制台摘要输出。
旧控制面板摘要线程、任务管理界面线程和线程快照。
旧自我场景、体素、相机、D455 相关显示 JSON、HTML 和窗口逻辑。
```

扫描证据来自：

```text
语素类.cpp:261-270、1538-1615、1662-1665：语素和基础信息显示标题函数明确只读、不写回项目事实，冲突时只追加临时显示标志。
世界树类.h:750-767、世界树类.cpp:2225-2228、2364-2368、3169-3190、3535-3562、3940-3949、4137-4177、4289-4292、4544-4564、5048-5053：世界树提供只读仓库访问、名称读取、场景状态 / 动态 / 二次特征快照、因果自然语言和特征快照读取；部分缓存只作为特征化或显示输入。
世界树类.cpp:2605-2656：世界树同步 SQL 控制面板显示项失败只写日志且明确世界树写入不回滚。
控制面板类.cpp:3-7：旧控制面板文件头声明本模块只供人读，不参与机器判断。
控制面板类.cpp:11305-11318、11427-11469、12154-12167、12171-12338、12562-12810：控制面板读取当前结构、SQL 区段和节点详情生成 JSON / 树节点视图。
控制面板类.cpp:13065-13295、13432-13444、17999-18019：控制面板摘要、任务管理摘要、缺口摘要、需求树生长摘要、HTML 和自我场景复现 JSON / 签名均由只读快照生成。
控制面板类.cpp:18022-18175：SQL 控制面板 HTML、保存 HTML、WebView2 窗口、相机窗口、自我场景窗口、截图和命令行解析属于显示 / 控制面板入口，本轮不接入。
控制面板摘要线程模块.impl.cpp:55-81、100-126、248-392、457-481、593-681、693-708：控制面板摘要线程读取旧需求 / 任务 / 方法 / 自我线程材料生成摘要快照，并写入线程内最新快照。
任务模块.管理界面线程.ixx:20-110、任务模块.管理界面线程.impl.cpp:7493-7507、9209-9238：任务管理界面线程可启动、提取上行消息并读取线程快照；线程路径只作为显示 / 调度观察边界。
鱼巢.cpp:1752-1865、1937-2035、2313-2460、2639-2665：旧控制台控制面板命令读取快照并输出摘要、HTML 或启动窗口；控制台输出和命令入口不得成为新项目机器事实。
```

## 3. 逐函数 / 函数组登记

### OF-191

```text
旧函数编号：OF-191
扫描批次：FS-10
旧项目路径：D:\鱼巢\语素类.cpp
旧函数名：私有_组合显示标题、读取语素入口显示词、读取基础信息默认显示名称、读取基础信息显示标题、生成基础信息显示标题集合、生成显示集合内临时标志
旧函数签名：语素和基础信息只读显示标题函数族
归属逻辑包：LP-FS10-VIEW-01 / 语素和基础信息显示命名候选
当前调用方：旧语素查询、基础信息候选展示、控制面板和诊断显示路径
触发路径：基础信息集合展示、语素入口详情展示、同名冲突显示消歧
输入：语素入口节点、基础信息节点、基础信息节点集合、是否使用类型前缀、临时显示标志
输出：显示词、默认显示名称、显示标题、显示标题集合、临时序号文本
读结构：语素词面、基础信息主信息名称、类型
写结构：只读；不写回名称、类型、关系或索引
是否写机器事实：否
是否依赖日志：否
是否依赖显示：是
是否依赖 SQL：否
是否依赖线程：否
是否依赖外设 / D455 / 体素：否
保留行为：保留从已确认结构生成显示标题、同名冲突时只做临时显示消歧的方向
删除行为：删除把显示标题、临时序号或组合文本写回主信息字段、索引或业务事实的口径
新增约束：显示名称只能由只读视图层生成；不得作为语素绑定、候选裁决或节点身份依据
目标新结构：无权威目标结构；后续显示层只读视图材料
目标领域服务入口：后续显示层只读视图专项
对应规范 / 详细设计：`规范/000_项目规则总纲.md`；缺显示层只读视图详细设计
入口拒绝条件：无效节点、错误节点类型、缺主信息时只能返回空显示，不得补写结构
验证方式：读取显示标题前后节点、主信息、关系、索引数量不变；显示文本缺失不影响业务结构
初步判定：排除 / 后续专项
初步迁移口径：只作为显示层只读命名规则输入，不进入当前代码实施
待用户确认项：后续显示层只读视图是否采用统一显示标题生成服务
扫描证据：语素类.cpp:261-270、1538-1615、1662-1665
```

### OF-192

```text
旧函数编号：OF-192
扫描批次：FS-10 / FS-02 / FS-03 / FS-07 / FS-08
旧项目路径：D:\鱼巢\世界树类.h、D:\鱼巢\世界树类.cpp
旧函数名：只读访问基础信息仓库_内部服务、获取名称、读取存在名称、读取存在类型、读取最近观测位置缓存、读取场景状态索引快照、读取场景动态索引快照、读取场景二次特征索引快照、读取最近清理问题快照、获取场景动态、读取位置变化原子动态候选、读取位置变化主体动态片段组、读取位置变化运动基元候选、读取观察关系动态片段组、生成抽象因果自然语言、读取特征快照、读取特征_I64、取VecU只读指针
旧函数签名：世界树只读仓库访问、快照和显示材料函数族
归属逻辑包：LP-FS10-VIEW-02 / 世界树只读结构视图材料
当前调用方：旧控制面板、诊断显示、二次特征 / 动态分析、自然语言显示和特征读取路径
触发路径：世界树详情展示、场景事实目录展示、状态清理诊断、动态片段展示、因果模板命名、特征快照读取
输入：基础信息节点、存在节点、场景节点、特征节点、特征类型、VecU 句柄、时间缺口和显示参数
输出：名称、入口引用、缓存位置、状态 / 动态 / 二次特征引用快照、动态片段、运动基元候选、自然语言文本、特征值或只读 VecU 指针
读结构：世界树基础信息仓库、存在服务、场景服务、状态服务、动态服务、因果服务、特征服务、值池缓存
写结构：只读；但 `清除最近清理问题快照` 会清理诊断快照，应另行排除
是否写机器事实：否；清理诊断快照也不得改变业务事实
是否依赖日志：否
是否依赖显示：是
是否依赖 SQL：否
是否依赖线程：否
是否依赖外设 / D455 / 体素：部分 Vec / 观测材料可服务后续外设或体素显示，本轮不接入
保留行为：保留只读视图从权威结构读取快照，不解释状态值业务含义、不创建或修改节点的方向
删除行为：删除通过世界树只读访问暴露可写仓库对象、从显示视图反向修改结构或把缓存当权威事实的口径
新增约束：新项目显示层只读入口不得暴露可写引用；高级服务仍不得绕过特征服务读取特征值服务
目标新结构：无权威目标结构；后续显示层只读视图材料
目标领域服务入口：后续显示层只读视图专项 / 世界服务只读视图入口
对应规范 / 详细设计：`规范/000_项目规则总纲.md`、`规范/领域服务授权写入规范.md`；缺显示层只读视图详细设计
入口拒绝条件：无效节点、错误节点类型、缺场景、缺特征、无效 Vec 句柄、试图通过只读视图写业务事实
验证方式：只读访问前后节点、主信息、关系、索引、状态、动态、因果数量不变；返回空或空集合不触发补写
初步判定：排除 / 后续专项
初步迁移口径：作为显示层只读视图和世界服务只读视图设计输入，不直接迁移旧世界树函数体
待用户确认项：后续只读视图是否允许聚合状态 / 动态 / 二次特征快照，还是按领域分视图
扫描证据：世界树类.h:750-767；世界树类.cpp:2225-2228、2364-2368、3169-3190、3535-3562、3940-3949、4137-4177、4289-4292、4544-4564、5048-5053
```

### OF-193

```text
旧函数编号：OF-193
扫描批次：FS-10 / FS-09 / FS-X
旧项目路径：D:\鱼巢\世界树类.cpp
旧函数名：同步SQL控制面板世界显示项
旧函数签名：世界树到 SQL 控制面板显示镜像同步函数
归属逻辑包：LP-FS10-X-01 / SQL 控制面板世界显示镜像排除边界
当前调用方：旧世界树存在、场景、状态创建和写入路径
触发路径：创建存在、创建场景、创建状态、写入状态值、标记状态变化等旧世界树写入后
输入：基础信息节点、阶段文本
输出：SQL 控制面板显示项、同步状态、运行警告日志
读结构：旧基础信息节点主信息、父节点、节点主键和显示短标题
写结构：SQL 控制面板显示项、SQL 控制面板同步状态、日志
是否写机器事实：否；旧代码明确失败不回滚世界树写入
是否依赖日志：是
是否依赖显示：是
是否依赖 SQL：是
是否依赖线程：否
是否依赖外设 / D455 / 体素：否
保留行为：保留“显示镜像失败不得影响权威结构”的边界
删除行为：删除本轮接入 SQL 控制面板显示镜像、运行态表和同步状态的实现
新增约束：后续显示层只读视图只能从当前结构读，不得要求业务写入路径同步写显示镜像
目标新结构：无当前目标结构
目标领域服务入口：后续显示层只读视图专项 / FS-X 排除边界
对应规范 / 详细设计：`规范/000_项目规则总纲.md`；缺显示层只读视图详细设计
入口拒绝条件：节点无效、主信息无效、显示域非法、SQL 不可用、试图把显示镜像写入当业务成功条件
验证方式：本轮不接入；后续若设计显示镜像，必须证明镜像失败不改变节点 / 主信息 / 关系 / 状态 / 动态
初步判定：排除 / 后续专项
初步迁移口径：仅作为显示镜像反例和后续只读视图边界证据
待用户确认项：无
扫描证据：世界树类.cpp:2605-2656
```

### OF-194

```text
旧函数编号：OF-194
扫描批次：FS-10 / FS-08 / FS-09 / FS-X
旧项目路径：D:\鱼巢\控制面板类.cpp
旧函数名：私有_读取世界树节点详情视图JSON、私有_读取需求列表分页、私有_读取控制面板快照、读取控制面板快照、读取控制面板轻量摘要快照、读取控制面板页面刷新JSON、读取SQL控制面板子链JSON、读取控制面板节点子项JSON、读取控制面板节点详情JSON、私有_填充控制面板实时世界树投影
旧函数签名：控制面板只读快照和页面 JSON 读取函数族
归属逻辑包：LP-FS10-VIEW-03 / 控制面板只读快照和树视图材料
当前调用方：旧控制面板 HTML / WebView2、控制台摘要命令、页面局部刷新接口
触发路径：控制面板启动、页面刷新、节点详情读取、摘要输出、自我场景窗口数据刷新
输入：树深度上限、树广度上限、页面 ID、SQL 区段、节点 ID、分页参数、是否读取自我场景快照
输出：控制面板快照、轻量摘要快照、页面刷新 JSON、子链 JSON、节点详情 JSON、树节点集合
读结构：旧世界树、自我对象、需求 / 任务 / 方法树、线程快照、控制面板摘要快照、SQL 读模型和实时世界树投影
写结构：局部快照和 JSON 文本；旧实现会启动 / 请求刷新摘要线程
是否写机器事实：否；但会触发显示摘要线程刷新，不能迁入当前第一轮
是否依赖日志：是
是否依赖显示：是
是否依赖 SQL：部分页面读取 SQL 区段
是否依赖线程：是，读取或请求控制面板摘要线程、任务管理界面线程和自我线程快照
是否依赖外设 / D455 / 体素：自我场景快照可能包含相机 / 体素显示材料，本轮不接入
保留行为：保留从已有结构生成只读快照和 JSON 树视图的方向
删除行为：删除启动线程、读取 SQL 读模型、连接 WebView2、输出控制面板 HTML 的本轮实现
新增约束：后续只读视图必须显式区分权威结构读取、非权威缓存、线程快照、SQL 投影和外设材料
目标新结构：无权威目标结构；后续只读视图 DTO / JSON 材料
目标领域服务入口：后续显示层只读视图专项
对应规范 / 详细设计：`规范/000_项目规则总纲.md`、`规范/领域服务授权写入规范.md`；缺显示层只读视图详细设计
入口拒绝条件：页面 ID 非法、节点句柄无效、请求 SQL 区段但 SQL 不可用、试图以 JSON 结果写业务事实
验证方式：读取快照 / JSON 前后权威结构数量不变；SQL 区段失败只返回显示错误，不回写结构
初步判定：排除 / 后续专项
初步迁移口径：只作为显示层只读视图输入；需要先补显示层只读视图规范和详细设计
待用户确认项：后续显示层是否允许依赖运行期线程快照，还是第一轮只读仓库结构
扫描证据：控制面板类.cpp:3-7、11305-11318、11427-11469、12154-12167、12171-12338、12562-12810
```

### OF-195

```text
旧函数编号：OF-195
扫描批次：FS-10
旧项目路径：D:\鱼巢\控制面板类.cpp
旧函数名：渲染控制面板摘要、渲染任务管理摘要、渲染缺口摘要、渲染需求树生长摘要、私有_生成控制面板HTML、生成自我场景独立窗口HTML、生成自我场景复现JSON、生成自我场景复现签名
旧函数签名：控制面板文本、HTML 和复现帧渲染函数族
归属逻辑包：LP-FS10-VIEW-04 / 控制面板只读渲染输出
当前调用方：旧控制面板页面、控制台摘要命令、自我场景独立窗口和诊断显示路径
触发路径：控制面板快照已生成后，转成人读摘要、HTML、JSON 复现帧或稳定签名
输入：结构_控制面板快照、树节点 JSON、HTML 用途、显示上限
输出：摘要文本、HTML 文本、自我场景复现 JSON、自我场景复现签名
读结构：只读控制面板快照内的结构摘要和显示材料
写结构：只生成文本 / JSON / HTML；不写项目结构
是否写机器事实：否
是否依赖日志：否
是否依赖显示：是
是否依赖 SQL：间接依赖快照来源，函数本体只渲染
是否依赖线程：间接依赖快照来源
是否依赖外设 / D455 / 体素：自我场景复现可能包含相机 / 体素材料，本轮不接入
保留行为：保留显示渲染只能消费只读快照、不裁决业务事实的边界
删除行为：删除把 HTML、JSON、签名或摘要文本当作机器事实、恢复来源或验收通过依据的口径
新增约束：新项目显示输出必须和结构事实分离；签名只能服务显示去重或刷新判断，不得当作状态 / 动态版本
目标新结构：无权威目标结构；后续显示层只读视图输出
目标领域服务入口：后续显示层只读视图专项
对应规范 / 详细设计：`规范/000_项目规则总纲.md`；缺显示层只读视图详细设计
入口拒绝条件：快照缺失、用途非法、请求外设 / 体素渲染但材料未确认、试图写业务事实
验证方式：渲染前后权威结构数量不变；输出为空或失败不影响任务 / 需求 / 方法状态
初步判定：排除 / 后续专项
初步迁移口径：只作为后续只读视图渲染层参考，不迁入当前内核
待用户确认项：无
扫描证据：控制面板类.cpp:13065-13295、13432-13444、17999-18019
```

### OF-196

```text
旧函数编号：OF-196
扫描批次：FS-10 / FS-09 / FS-X
旧项目路径：D:\鱼巢\控制面板类.cpp、D:\鱼巢\鱼巢.cpp
旧函数名：生成SQL控制面板HTML、生成SQL控制面板启动HTML、保存控制面板HTML、启动控制面板窗口、启动控制面板相机窗口、启动控制面板自我场景窗口、保存控制面板自我场景窗口截图、请求关闭控制面板窗口、等待控制面板窗口关闭、获取控制面板启动诊断码、默认控制面板HTML路径、解析控制面板命令行、私有_执行控制面板命令
旧函数签名：SQL / WebView2 / 控制台控制面板入口函数族
归属逻辑包：LP-FS10-X-02 / 控制面板接入排除边界
当前调用方：旧 `鱼巢.cpp` 命令行入口、控制面板窗口入口和 HTML 生成入口
触发路径：`--panel-text`、`--summary`、`--panel-html`、`--panel`、`--panel-camera`、`--panel-self-scene` 等命令
输入：命令行参数、HTML 输出路径、控制面板快照、WebView2 窗口参数、等待关闭参数
输出：HTML 文件、WebView2 窗口、相机窗口、自我场景窗口、截图文件、控制台文本、诊断码
读结构：SQL 控制面板读模型、当前控制面板快照、自我环境初始化状态、线程快照
写结构：HTML 文件、截图文件、窗口线程、日志、控制台输出；不应写新项目机器结构
是否写机器事实：否；但属于控制面板 / SQL / WebView2 接入，本轮禁止
是否依赖日志：是
是否依赖显示：是
是否依赖 SQL：是
是否依赖线程：是
是否依赖外设 / D455 / 体素：相机、自我场景和体素窗口路径相关，本轮禁止
保留行为：保留控制面板只做人读显示、SQL 读取失败只显示错误的边界
删除行为：删除本轮控制面板、SQL HTML、WebView2、相机窗口、自我场景窗口和截图功能接入
新增约束：后续显示层只读视图专项确认前，不允许新增控制面板命令、HTML 文件输出或窗口入口
目标新结构：无当前目标结构
目标领域服务入口：FS-X 排除边界 / 后续显示层只读视图专项
对应规范 / 详细设计：`规范/000_项目规则总纲.md`、`规范/001_规则迁移清单.md`；缺显示层只读视图详细设计
入口拒绝条件：任何当前新项目入口试图接控制面板、SQL、WebView2、相机、D455、体素或截图输出
验证方式：本轮不接入；后续若接入必须证明显示层只读且不写业务事实
初步判定：排除 / 后续专项
初步迁移口径：明确排除，不作为当前显示层只读候选代码
待用户确认项：无
扫描证据：控制面板类.cpp:18022-18175；鱼巢.cpp:2313-2460、2639-2665
```

### OF-197

```text
旧函数编号：OF-197
扫描批次：FS-10 / FS-05 / FS-08 / FS-X
旧项目路径：D:\鱼巢\控制面板摘要线程模块.impl.cpp
旧函数名：私有_发送摘要线程生命周期消息、私有_读取方法运行账时间_摘要、私有_同步摘要快照数值槽、私有_采集需求树摘要、私有_采集任务树摘要、私有_采集方法树摘要、私有_采集运行事实状态槽、私有_采集自我线程状态槽、私有_采集自检状态槽、控制面板摘要线程类::读取最新快照、控制面板摘要线程类::主循环_、控制面板摘要线程类::采集快照_、控制面板摘要线程类::写入快照_、启动控制面板摘要线程、停止控制面板摘要线程、请求刷新控制面板摘要
旧函数签名：控制面板摘要线程和摘要快照采集函数族
归属逻辑包：LP-FS10-X-03 / 控制面板摘要线程排除边界
当前调用方：旧控制面板快照读取、控制台摘要命令、后台刷新路径
触发路径：控制面板快照读取时启动 / 请求刷新摘要线程，摘要线程定期采集需求 / 任务 / 方法 / 自我线程状态
输入：摘要线程生命周期、刷新请求、需求 / 任务 / 方法根、方法虚拟存在、特征类型、自我线程快照
输出：结构_控制面板摘要快照、线程生命周期消息、线程内最新快照
读结构：旧需求树、任务树、方法树、方法运行账特征、自我线程、自检线程
写结构：控制面板摘要线程内部快照、线程生命周期消息；不应写新项目业务事实
是否写机器事实：否；线程快照和摘要不是业务事实
是否依赖日志：可能间接通过线程消息和错误路径
是否依赖显示：是
是否依赖 SQL：否
是否依赖线程：是
是否依赖外设 / D455 / 体素：间接读取自我线程材料，本轮不接入外设
保留行为：保留“摘要快照是非权威观察材料”的边界
删除行为：删除控制面板摘要线程、本轮线程启动和摘要快照写入
新增约束：线程不是动作来源；摘要快照不得作为需求结算、任务完成、方法成功或动作动态证据
目标新结构：无当前目标结构；后续可作为非权威诊断快照或只读视图材料
目标领域服务入口：后续显示层只读视图专项 / 非权威缓存统计专项
对应规范 / 详细设计：`规范/非权威缓存与统计规范.md`、`规范/000_项目规则总纲.md`；缺显示层只读视图详细设计
入口拒绝条件：试图启动控制面板摘要线程、把线程快照写成业务事实、从摘要反推需求结算或任务完成
验证方式：本轮不接入；后续若设计摘要快照，清空摘要不得影响权威结构
初步判定：排除 / 后续专项
初步迁移口径：只作为显示层和非权威统计后续设计输入
待用户确认项：后续显示层只读视图是否允许后台摘要线程
扫描证据：控制面板摘要线程模块.impl.cpp:55-81、100-126、248-392、457-481、593-681、693-708
```

### OF-198

```text
旧函数编号：OF-198
扫描批次：FS-10 / FS-05 / FS-X
旧项目路径：D:\鱼巢\任务模块.管理界面线程.ixx、D:\鱼巢\任务模块.管理界面线程.impl.cpp、D:\鱼巢\鱼巢.cpp
旧函数名：启动任务管理界面线程、请求停止任务管理界面循环线程、停止任务管理界面线程、提取任务管理上行消息、读取任务管理界面线程快照、私有_记录任务管理摘要即时状态、私有_读取控制面板快照_记录耗时、私有_摘要快照状态行、私有_控制台刷新摘要文本、私有_输出控制台刷新摘要、私有_需求树生长离线摘要文本、私有_语素入口显示文本_控制命令、私有_执行需求树生长摘要Live
旧函数签名：任务管理界面线程快照和控制台显示摘要函数族
归属逻辑包：LP-FS10-X-04 / 任务管理界面线程和控制台摘要排除边界
当前调用方：旧控制台摘要、任务管理界面、控制面板快照和任务管理线程路径
触发路径：任务管理界面线程启动、控制台刷新、需求树生长摘要 live / offline、任务管理摘要命令
输入：任务管理线程状态、上行消息队列、控制面板快照、场景标记、语素入口
输出：线程快照、上行消息、控制台摘要文本、运行日志
读结构：任务管理界面线程状态、任务管理工作线程快照、控制面板快照、语素入口
写结构：线程状态、上行队列消费、运行日志、控制台输出；不应写新项目业务事实
是否写机器事实：否；但线程状态会变化，本轮不迁入
是否依赖日志：是
是否依赖显示：是
是否依赖 SQL：否
是否依赖线程：是
是否依赖外设 / D455 / 体素：任务管理界面线程会启动外设承接后台线程，本轮禁止
保留行为：保留线程快照和控制台摘要只能做人读观察的边界
删除行为：删除本轮任务管理界面线程、上行消息队列、控制台自动刷新和外设承接后台线程接入
新增约束：新项目第一版不接多线程显示调度；线程消息不得成为动作来源或业务事实
目标新结构：无当前目标结构
目标领域服务入口：FS-X 排除边界 / 后续显示层只读视图专项
对应规范 / 详细设计：`规范/000_项目规则总纲.md`、`规范/001_规则迁移清单.md`；缺显示层只读视图详细设计
入口拒绝条件：试图启动任务管理界面线程、消费上行消息写业务事实、把控制台摘要当验收或机器事实
验证方式：本轮不接入；后续若接显示线程，必须证明线程快照缺失不影响权威结构
初步判定：排除 / 后续专项
初步迁移口径：只作为线程 / 显示排除边界和后续只读视图设计输入
待用户确认项：无
扫描证据：任务模块.管理界面线程.ixx:20-110；任务模块.管理界面线程.impl.cpp:7493-7507、9209-9238；鱼巢.cpp:1752-1865、1937-2035、2313-2327
```

## 4. 逻辑迁移包初步归并

| 逻辑包编号 | 逻辑能力 | 包含旧函数 | 新项目结构承载 | 目标领域服务 | 当前结论 |
| --- | --- | --- | --- | --- | --- |
| LP-FS10-VIEW-01 | 语素和基础信息显示命名候选 | OF-191 | 无权威结构；只读显示材料 | 显示层只读视图专项 | 后续专项，缺详细设计 |
| LP-FS10-VIEW-02 | 世界树只读结构视图材料 | OF-192 | 无权威结构；仓库 / 领域只读视图材料 | 显示层只读视图专项 / 世界服务只读视图入口 | 后续专项，需确认只读聚合边界 |
| LP-FS10-X-01 | SQL 控制面板世界显示镜像排除边界 | OF-193 | 无当前目标结构 | FS-X / 显示层只读视图专项 | 排除 / 后续专项 |
| LP-FS10-VIEW-03 | 控制面板只读快照和树视图材料 | OF-194 | 无权威结构；后续只读视图 DTO / JSON | 显示层只读视图专项 | 后续专项，必须先补设计 |
| LP-FS10-VIEW-04 | 控制面板只读渲染输出 | OF-195 | 无权威结构；显示输出 | 显示层只读视图专项 | 后续专项 |
| LP-FS10-X-02 | 控制面板 SQL / WebView2 / 窗口接入排除边界 | OF-196 | 无当前目标结构 | FS-X | 当前排除 |
| LP-FS10-X-03 | 控制面板摘要线程排除边界 | OF-197 | 非权威摘要或只读视图材料 | 显示层只读视图专项 / 非权威缓存统计专项 | 后续专项，线程不得入当前第一轮 |
| LP-FS10-X-04 | 任务管理界面线程和控制台摘要排除边界 | OF-198 | 无当前目标结构 | FS-X / 显示层只读视图专项 | 当前排除 |

## 5. 本轮结论

1. 旧语素和基础信息显示标题函数具备后续只读视图参考价值，但显示标题、临时序号和组合文本不得写回业务结构。
2. 旧世界树只读访问、名称读取、场景事实目录快照和特征快照可作为后续只读视图输入，但不得暴露可写仓库对象，不得绕过特征服务或领域服务授权。
3. 旧控制面板快照、JSON 和渲染函数只证明旧项目有显示层输出路径，不证明新项目显示层已设计或可接入。
4. 旧 SQL 控制面板显示镜像、HTML 保存、WebView2 窗口、相机窗口、自我场景窗口、截图、控制台命令和线程摘要路径本轮全部排除。
5. 旧控制面板摘要线程和任务管理界面线程只可作为非权威观察 / 显示材料，不得作为动作来源、事件日志、任务完成、需求满足或方法成功证据。

## 6. 下一步

```text
下一旧函数编号：OF-199
下一步：继续 FS-X，汇总控制面板 / SQL / D455 / 体素 / 外设函数排除项。
```

若后续发现 FS-10 遗漏，只能追加登记，不得重写既有编号或把当前记录解释为迁移许可。

## 7. 禁止扩大声明

```text
不得声明显示层只读视图已设计或已实现。
不得声明旧控制面板、SQL 读模型、WebView2、相机窗口、自我场景窗口、截图或控制台命令已迁移。
不得把显示标题、HTML、JSON、控制台摘要、线程快照、SQL 显示镜像或控制面板事件作为机器事实。
不得接控制面板 / SQL / D455 / 体素 / 外设。
```
