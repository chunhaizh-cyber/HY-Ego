# CONCEPT-S5 活动图发布与抽象树投影现状施工偏差清单

日期：2026-07-11

| 编号 | 现状代码事实 | 流程图 / 详细设计 / 计划描述 | 偏差类型 | 纠偏方向 | 是否需要代码计划 |
| --- | --- | --- | --- | --- | --- |
| DEV-CONCEPT-S5-001 | `发布候选图版本` 用同一个空返回合并基准过期和全部候选结构校验失败 | 原施工计划把未收敛/过期列为逻辑内返回，把内部候选环/孤儿列为追根因，但代码无法区分 | 修图/修详细设计/修代码/补契约 | 让校验返回结构化分类；未收敛/空/过期为逻辑返回，正式收敛候选内部错误触发追根因 | 是 |
| DEV-CONCEPT-S5-002 | 正式发布刚完成基础初始化后，活动快照不存在仍静默空返回 | 正式流程已保证基础版本可用 | 修代码 | 将该不可能分支改为追根因检查并停止依赖路径 | 是 |
| DEV-CONCEPT-S5-003 | 候选类别未知、原子服务返回无效句柄、映射缺失或追加关系失败可只经上层空返回和清理，不一定记录失败根因 | 总纲要求前置通过并进入创建/写关系后不及预期必须追根因 | 修代码 | 在首次发现异常的位置触发公共追根因诊断，保留清理为失败收口而不是根因替代 | 是 |
| DEV-CONCEPT-S5-004 | 新快照先替换，随后旧关系失效；若失效失败，公开入口返回空但新快照已经活动 | 施工设计允许后置失效，但没有明确“返回失败时新版本已生效”的调用契约 | 修详细设计/修代码 | 明确发布提交点和返回语义；避免对外返回与活动版本事实矛盾，并验收旧边残留不可混入活动读取 | 是 |
| DEV-CONCEPT-S5-005 | 单树/四树读取把任意请求缺快照、正式 PANEL 前置缺失、预算耗尽和活动图内部异常都压成空 optional | 详细设计只给出粗粒度拒绝矩阵，未按调用语境拆分 | 修图/修详细设计/修代码/补契约 | 返回可区分原因或提供正式读取入口；正式快照异常、环和不完整必须追根因 | 是 |
| DEV-CONCEPT-S5-006 | 活动关系/签名公开读取对未初始化和未命中统一返回空 | 任意查询允许空，但内部正式读回已有 `追根因检查` 口径 | 补契约/修代码 | 保留宽松查询入口，同时为正式调用增加契约化读取或调用点追根因检查 | 是 |
| DEV-CONCEPT-S5-007 | 原施工流程图覆盖 S1-S8，总体粒度无法逐行表达 S5 当前分支 | 用户硬规则要求现状图和施工图区分，现状图足够详细以纠偏代码 | 修图 | 保留原施工图，新建本现状图和逐行映射，不回写历史计划发生事实 | 否 |
| DEV-CONCEPT-S5-008 | 当前 `入口.cpp` 和 `控制面板服务.h` 存在 PANEL-TREES-S1 未提交施工改动 | 本次目标是稳定的 S5 服务代码现状 | 补契约 | 本次不映射脏文件；代码纠偏计划依赖 #187 收口并执行前复核接口 | 是 |

## 结论

当前需要修订的是“返回分类和失败收口”，不是重写概念图算法。修订必须同时改现状/施工流程描述、详细设计、`概念图服务.h` 和相应入口验收；不得借机进入命名、用途、生命周期、物理删除或跨重启恢复。
