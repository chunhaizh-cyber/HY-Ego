# DEMAND-SETTLEMENT-S1 上游合同一致性设计修订 Codex 断点清单

日期：2026-07-16

决策：JY-370

裁决问题：QR-186 已关闭

计划 / 队列 / 阶段：#286 / DQ-178 / 复用 610、760

交互：WIN-0001-E96

基线提交：`9dd440c25e64bb6b1c3dbee4d51f5697f8bd38ae`

状态：纯文档设计修订完成 / 原 #286 重新待执行 / C++、工程零修改 / A01-A16、610 / 760 新验收未实施

## 1. 输入入口

```text
实施记录/20260716_DEMAND-SETTLEMENT-S1_上游合同范围不一致实施前复核_Codex断点清单.md
规范/需求结算记录规范.md
规范/详细设计/需求结算记录详细设计.md
规范/详细设计/需求结算单一正式合同语义收口详细设计.md
流程图/20260716_需求结算单一正式合同语义收口流程图_v0.1.md
流程图/20260716_需求结算单一正式合同语义收口流程图_v0.1.html
计划/20260716_DEMAND-SETTLEMENT-S1_需求结算单一正式合同语义收口代码实施切片_v0.1.md
海中鱼巣/领域/服务.需求.ixx
海中鱼巣/领域/数据操作.需求任务方法.ixx
海中鱼巣/领域/组合.需求任务方法.ixx
```

## 2. 当前事实复核

1. 当前正式结算请求只承载一个动态句柄和一个可选因果句柄，允许文件不能无损实现多证据集合。
2. `#225` 新任务结果治理路径已经拥有未达成转 `待重筹办` 的正式写入口和幂等材料。
3. `#225` 之外的既有通用组合入口没有待重筹办专用幂等材料，且 #286 禁止修改 #225 组合器。
4. 已完成任务可直接调用独立结算；值不等时只能拒绝本次结算，不得回退或改写既有任务状态。
5. 动态和可选因果节点均由上游正式入口创建；需求服务当前只需复核，需求专用数据操作层只需绑定。
6. 三个正式模块、两个自检及阶段 610 / 760 的所有权未漂移；无需扩大代码允许范围。

## 3. JY-370 裁决

```text
第一轮正式结算固定绑定上游已创建的一条动态和零或一条因果引用。
需求服务只复核，需求专用数据操作层只绑定，不新增动态服务或因果服务创建依赖。
多动态或多因果证据集合后置独立专项。
#225 新任务结果治理路径：值不等时继续转待重筹办，不写需求最终结算。
#225 之外既有通用组合入口：值不等时不调用任务完成或需求结算，两类结构零变化。
已完成任务直接独立结算：值不等时只拒绝本次结算，保持既有任务状态。
```

三条入口共享“值不等不写需求最终结算”合同，但不得互相借用写权限。

## 4. 三件套一致性

已同步修订：

```text
规范/需求结算记录规范.md
规范/详细设计/需求结算记录详细设计.md
规范/详细设计/需求结算单一正式合同语义收口详细设计.md
流程图/20260716_需求结算单一正式合同语义收口流程图_v0.1.md
流程图/20260716_需求结算单一正式合同语义收口流程图_v0.1.html
计划/20260716_DEMAND-SETTLEMENT-S1_需求结算单一正式合同语义收口代码实施切片_v0.1.md
```

流程图已经把值不等拆成三条入口路径；Markdown 与 HTML Mermaid 必须逐字符同源。读取合同固定为关系 1—4 各恰一且完整，关系 5 零或一且若有完整；计划继续复用 610 / 760，A01-A16、允许文件和禁止文件不扩大。

## 5. 输出变化

```text
QR-186：待设计裁决 -> JY-370 已关闭。
#286 / DQ-178：退回设计 -> JY-370 重新待执行，代码仍零修改，A01-A16 未实施。
#283 / DQ-175：#282 前置已满足，继续等待 #286 正式完成。
#284 / #285：继续按原依赖门控。
#214：继续用户暂停。
#258 / #261 救援 stash：禁止边界不变，未读取、未恢复、未修改、未暂存、未删除。
```

## 6. 执行前复核

E96 自动本地提交并自动非强制推送成功后，当前物理窗口才能切换执行角色。切换后必须重新读取 Git、计划索引、任务队列、#286、本断点、E95 复核断点和五个允许代码 / 自检文件，并逐项核对：

```text
提交独立结算与目标状态达成裁决签名。
需求结算写入规格、需求权威材料和正式结算读取接口。
需求到任务与需求到结算状态两类归属的类型过滤。
固定关系 1—4 与可选关系 5 的当前读写角色。
独占会话内状态型归属二次复核能力。
610 / 760 的唯一登记和自检所有权。
```

实际接口不一致且必须修改禁止文件或新增结构时，代码零修改再次退回设计。

## 7. 验证方式

```text
基础规范、两份详细设计、流程图和计划逐项范围一致。
Markdown / HTML Mermaid 解码后逐字符一致。
JY-370、QR-186、#286、DQ-178 和 E96 唯一性扫描。
python .\tools\check_specs.py --strict。
git diff --check。
C++、工程和 filters 差异为 0。
```

本设计轮不运行 C++ 构建、程序或自检；JY-370 是施工合同，不是代码完成证据。

## 8. 完成声明边界

本记录只证明 QR-186 已形成正式裁决，需求结算上游合同和原 #286 三件套已范围一致并重新登记。不得声明 #286、A01-A16、610 / 760 新验收、正式结算语义收口、`需求服务.h` 历史兼容路径、恢复、需求系统、旧能力迁移或项目迁移已经完成。
