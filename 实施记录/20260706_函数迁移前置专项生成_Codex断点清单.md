# 函数迁移前置专项生成 Codex 断点清单

日期：2026-07-06
状态：已生成 / 待确认 / 未改代码 / 未迁移旧函数

## 1. 输入入口

用户要求生成“函数迁移前置专项”，并指定必须包含：

```text
任务目标
依据文件
只读扫描范围
允许修改文件
禁止修改文件
旧函数功能说明模板
保留 / 改名 / 拆分 / 合并 / 删除 / 改写为领域服务入口的判定表
验收标准
禁止宣称
```

用户同时补充硬边界：

```text
不直接迁移旧函数体
不新增旧主信息字段
不声明旧项目能力迁移完成
不接控制面板 / SQL / D455 / 体素
```

## 2. 核心处理

本切片只建立函数迁移前置门禁文档，并同步计划索引和项目记忆。

处理内容：

```text
1. 新建 `计划/20260706_函数迁移前置专项_v0.1.md`。
2. 将专项登记为 HR-001-FM-PRE。
3. 明确本专项状态为已建立 / 待确认 / 未改代码 / 未迁移旧函数。
4. 补旧函数功能说明模板。
5. 补保留 / 改名 / 拆分 / 合并 / 删除 / 改写为领域服务入口判定表。
6. 将用户补充的四条禁止边界写入专项、计划索引、项目记忆和本断点清单。
```

## 3. 结构承载

```text
计划/20260706_函数迁移前置专项_v0.1.md
计划/计划索引.md
项目记忆/当前状态.md
项目记忆/决策记录.md
项目记忆/实施记录.md
项目记忆/待确认问题.md
项目记忆/Codex任务队列.md
项目记忆/验收记录.md
实施记录/20260706_函数迁移前置专项生成_Codex断点清单.md
```

## 4. 输出变化

```text
函数迁移前置专项已生成。
旧函数迁移前必须先完成逐函数功能说明、保留行为、删除行为、新增约束和判定确认。
旧函数不得直接进入代码迁移。
当前专项不改变 C++ 代码、不运行构建、不接外部显示或外设。
```

## 5. 调用路径

```text
用户请求
-> 读取计划索引、项目规则、函数功能说明基线和项目记忆
-> 建立函数迁移前置专项
-> 同步计划索引和项目记忆
-> 文档一致性验证
-> 提交并推送
```

## 6. 验证方式

本切片应验证：

```text
rg 检索专项编号、禁止边界和模板关键项。
git diff --check。
git status 确认只包含本切片相关文件和既有无关未跟踪文件。
```

本切片不运行：

```text
msbuild
默认入口程序
旧项目函数扫描
控制面板 / SQL / D455 / 体素样例
```

## 7. 禁止扩大声明

本切片不得声明：

```text
旧函数已迁移。
旧函数体已可直接复制。
旧项目能力迁移完成。
旧主信息字段已迁移。
控制面板、SQL、D455 或体素已接入。
代码已编译。
入口前置拒绝已重新验证。
最小闭环已重新验证。
真实外设样本通过。
自我循环、自我苏醒或初步成熟完成。
```

## 8. 后续断点

```text
等待用户确认 `计划/20260706_函数迁移前置专项_v0.1.md`。
确认后才能建立逐函数只读扫描清单。
逐函数扫描仍只能只读，不得迁移旧函数体。
涉及代码落地时，必须另建独立代码实施切片。
```
