# STRUCT-TXN-S1 实施记录

日期：2026-07-12
治理编号：JY-273
计划：`计划/已完成计划/20260711_STRUCT-TXN-S1_端点冻结写集与统一读取隔离代码实施切片_v0.1.md`

| 项 | 实际结果 |
| --- | --- |
| 输入入口 | 隔离已接域四仓库组与最终回归 500；入口主装配保持默认空接线。 |
| 核心处理 | 建立数据桥、协调模块、移动 RAII 许可与活动表；四仓库覆盖 46 个公开入口，其中 41 条令牌路径；节点和主信息身份删除限定独占许可。 |
| 结构承载 | `结构事务接线.数据.h` 持有值式令牌和 `shared_ptr<void>` 运行期状态；协调模块持有结构锁与线程归属活动表；仓库只保存值式接线。 |
| 输出变化 | 新增隔离接域自检 A01-A14 并登记最终回归 500；主信息、节点、关系和索引仓库均可在同域令牌下调用。 |
| 调用路径 | `协调器 -> 值式接线 -> 仓库无令牌包装或显式令牌入口 -> 隔离自检 500`。 |
| 验证方式 | Debug x64 0 警告 / 0 错误；完整自检退出 0；`tools/check_specs.py --strict` 与 `git diff --check` 通过；完整自检连续 20 / 20 轮退出 0。 |
| 完成边界 | 只证明单进程隔离仓库组可选接域、共享 / 独占许可和端点冻结基础；不证明入口主装配已接域、全局读写统一隔离、安全删除提交、回滚、恢复或旧能力迁移完成。 |
