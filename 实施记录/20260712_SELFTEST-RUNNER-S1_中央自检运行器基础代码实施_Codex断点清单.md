# SELFTEST-RUNNER-S1 中央自检运行器基础代码实施 Codex 断点清单

日期：2026-07-12

队列编号：#241

决策编号：JY-259

状态：已完成 / 已验证 / 云端同步随本切片提交

## 1. 输入入口

`#236`、`#240` 正式记录，中央自检运行器流程图与详细设计，#241 正式计划，当前 `线程/自检.概念命名治理.ixx`、`入口.cpp` 和工程登记。

## 2. 实际接口复核

1. 当前不存在同功能中央运行器。
2. `海中鱼巣.线程.自检.概念命名治理` 仍公开 `运行概念命名治理自检(const 概念命名治理自检上下文&)`，返回含 A01-A12 与 `总通过` 的值式报告。
3. 入口仍构造该上下文并直接调用，未迁移到其它运行器。
4. 工程继续使用 `ClCompile + export module + import` 真模块登记方式。
5. 未发现计划假定接口漂移。

## 3. 实施范围

| 文件 | 变化 |
| --- | --- |
| `海中鱼巣/自检.运行器.ixx` | 新增中央运行器真模块、值式单元 / 批次结果、登记拒绝、冻结、稳定排序、全量执行、一次执行和退出码转换；内置运行器合同自检。 |
| `海中鱼巣/入口.cpp` | 导入中央运行器，以顺序 1 登记运行器合同、顺序 400 登记既有概念命名自检，执行一次并合并总结果。 |
| `海中鱼巣.vcxproj` / `.filters` | 按 `ClCompile` 登记新模块。 |

禁止文件中的领域服务、仓库、生产线程、SQL、控制面板、日志、快照、恢复和外设实现均未修改；既有 `线程/自检.概念命名治理.ixx` 未修改。

## 4. 验收结果

| 编号 | 结果 | 证据 |
| --- | --- | --- |
| A01 | 通过 | 文件名、模块名、工程 `ClCompile` 和入口 `import` 一致。 |
| A02 | 通过 | 非零顺序、唯一编号 / 顺序和非空回调登记成功。 |
| A03 | 通过 | 零顺序、空编号 / 名称、重复编号 / 顺序和空回调在执行前拒绝，批次执行数为零。 |
| A04 | 通过 | 反序登记后按顺序字段稳定执行。 |
| A05 | 通过 | 中间失败时后续仍执行，三个回调各执行一次。 |
| A06 | 通过 | 首次执行后冻结登记，冻结后登记和二次执行均拒绝。 |
| A07 | 通过 | 登记 / 执行 / 通过 / 失败数、失败编号组、总通过和退出码一致。 |
| A08 | 通过 | CONCEPT-NAMING-S1-A01 至 A12 全部保持通过。 |
| A09 | 通过 | 入口只新增模块导入、两个登记回调、一次执行和值式总结果合并，没有新增领域场景或并发压力正文。 |
| A10 | 通过 | 运行器只持有测试控制值与回调，不访问业务仓库，不启动线程、SQL、窗口或外设。 |

## 5. 验证

```text
Debug x64：成功，0 警告，0 错误。
完整自检：海中鱼巣.exe --self-test-exit，退出码 0。
连续验证：20 / 20 轮退出码 0，总用时 285.1 秒；单轮 12,955-15,924 ms。
规范检查：git diff --check 与 tools/check_specs.py --strict 通过。
```

## 6. 完成声明

| 项 | 内容 |
| --- | --- |
| 输入入口 | #241 计划、现有概念命名值式上下文 / 报告和入口直接调用。 |
| 核心处理 | 登记前拒绝非法材料；首次运行冻结并稳定排序；全部回调恰好执行一次，普通失败不短路；汇总值式报告和退出码。 |
| 结构承载 | `自检.运行器` 模块内的调用期登记组、单元结果和批次结果；不持久化、不写业务事实。 |
| 输出变化 | 中央运行器正式接口形成；概念命名自检改为顺序 400 登记样本；#242 获得正式接口复核前置。 |
| 调用路径 | `入口最小上下文 -> 登记运行器合同 / 概念命名 -> 运行全部一次 -> 值式批次结果 -> 程序总结果`。 |
| 验证方式 | A01-A10、Debug x64、完整自检、连续 20 轮、规范和差异检查。 |

## 7. 未完成边界

入口存量自检尚未全部迁出；#242、#243 和 #217 尚未实施。本切片不证明完整自检体系、结构事务、安全删除、持续自主循环、自我苏醒、真实外设或旧能力迁移完成。
