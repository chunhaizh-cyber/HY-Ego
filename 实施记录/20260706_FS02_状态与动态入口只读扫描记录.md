# FS-02 状态与动态入口只读扫描记录

日期：2026-07-06
状态：FS-02 第四批已登记 / 只读扫描 / 未改代码 / 未迁移旧函数
依据清单：`实施记录/20260706_逐函数只读扫描清单.md`
前序记录：

```text
实施记录/20260706_FS02_语素入口与基础信息桥接只读扫描记录.md
实施记录/20260706_FS02_基础信息模板入口只读扫描记录.md
实施记录/20260706_FS02_存在与场景入口只读扫描记录.md
```

## 1. 本轮定位

本轮继续 FS-02，登记旧项目 `状态类` 和 `动态类` 中与状态身份、状态主体 / 特征 / 值材料、实例状态创建、场景状态索引、状态清理、动态身份、动态因果输入、动作动态、场景动态索引、运动基元候选和来源绑定有关的函数族。

本轮只做只读事实登记和初步分流，不复制旧 `状态节点主信息类`、`动态节点主信息类` 字段，不迁移旧函数体。旧 SQL 控制面板同步只登记为 FS-X / FS-10 排除边界；旧运动基元候选只登记为后续二次特征 / 方法候选专项线索，不创建稳定规则或方法候选。

## 2. 只读扫描范围

```text
D:\鱼巢\状态类.h
D:\鱼巢\状态类.cpp
D:\鱼巢\动态类.h
D:\鱼巢\动态类.cpp
```

扫描证据来自：

```text
状态类.h:29-72：状态服务绑定、主信息读取、按主键解析、状态主体 / 特征 / 值 / 排序时间读取声明。
状态类.h:74-128：状态创建、内部状态、世界状态、内部特征状态和删除声明。
状态类.h:130-161：场景状态读取、活动状态判断、状态索引清理、主体特征绑定、状态值写入和变化标记声明。
状态类.cpp:1-5：旧状态文件头部规则，声明状态节点为零散事实记录，SQL / 显示不得替代结构关系。
状态类.cpp:149-197：绑定基础信息、取状态主信息和按主键解析状态节点实现。
状态类.cpp:166-305：状态主体 / 特征 / 值读取、生成状态、状态有效检查和状态差值。
状态类.cpp:309-510：创建状态、追加场景状态索引、状态记录后刷新动态、内部 / 世界状态包装。
状态类.cpp:514-601：删除状态、枚举状态、获取场景状态、活动状态判断和状态引用统计。
状态类.cpp:605-803：状态索引清理、活动状态限量清理、状态写入后整理和清理问题快照。
状态类.cpp:806-841：绑定主体特征、写入状态值和标记变化显示缓存。
动态类.h:3-6：旧动态文件头部规则，线程 / 显示 / SQL 不得成为动作来源。
动态类.h:105-160：动态身份读取、名称 / 类型推导、因果输入、来源场景、排序时间、双状态时间段、聚合和影响评估声明。
动态类.h:162-221：动态创建、动作动态、方法动作动态、存在 / 场景动态、允许生成检查和删除声明。
动态类.h:223-258：场景动态读取、位置 / 关系运动基元候选、相邻前状态、状态后刷新动态和来源绑定声明。
动态类.cpp:976-1210：动态主信息读取、因果输入、来源场景、时间段、聚合信息、来源低层动态和影响评估读取。
动态类.cpp:1301-1369：SQL 控制面板动态显示镜像同步。
动态类.cpp:1372-1885：创建动态、状态动态、动作动态、方法动作动态、存在 / 场景动态和允许生成检查。
动态类.cpp:1885-1945：删除动态、枚举动态和获取场景动态。
动态类.cpp:1924-2260：位置变化和观察关系运动基元候选只读提取。
动态类.cpp:2262-2399：查找相邻前状态和状态记录后刷新动态。
动态类.cpp:2402-2467：设置主体特征、聚合信息、追加来源低层动态、绑定来源方法和来源场景。
```

## 3. 逐函数登记

### OF-080

```text
旧函数编号：OF-080
扫描批次：FS-02
旧项目路径：D:\鱼巢\状态类.cpp:149
旧函数名：状态类::绑定基础信息 / 取状态主信息 / 按主键解析状态节点
旧函数签名：void 绑定基础信息(...); 状态节点主信息类* 取状态主信息(...); 状态节点类* 按主键解析状态节点(...)
当前调用方：旧状态类所有读写入口、场景状态索引、动态刷新和二次特征引用统计。
触发路径：旧服务定位基础信息树后，按主信息类型或主键识别状态节点。
输入：基础信息类指针、状态节点、主键字符串。
输出：状态主信息指针、状态节点指针或空。
读结构：旧基础信息树、旧状态主信息、旧主键索引。
写结构：`绑定基础信息` 只写运行期服务指针；其它函数只读。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：状态节点类型复核、按稳定键定位候选、索引命中后复判状态主信息。
删除行为：不得用运行期裸指针或旧主信息类型直接裁决新项目状态事实完整。
新增约束：新项目必须通过状态服务复核节点类型、实例 / 抽象状态边界和句柄有效性。
目标新结构：节点类型::状态、节点句柄、索引主键、状态服务只读入口。
目标领域服务入口：状态服务 `读取状态值`、后续 `读取实例状态材料` / `状态是否有效`。
对应规范 / 详细设计：状态服务详细设计、节点仓库详细设计、索引仓库详细设计、领域服务授权写入规范。
入口拒绝条件：基础服务缺失、主键为空、句柄无效、节点类型不是状态、已删除节点。
验证方式：索引命中后读回节点类型和状态材料；错误类型返回空且结构数量不变。
初步判定：改写为领域服务入口
待用户确认项：新项目是否补专用 `状态是否有效` 只读入口。
扫描证据：`状态类.cpp:149` 至 `状态类.cpp:197`。
```

### OF-081

```text
旧函数编号：OF-081
扫描批次：FS-02 / FS-03
旧项目路径：D:\鱼巢\状态类.cpp:166
旧函数名：读取状态主体特征和值引用 / 读取状态主体与特征 / 读取状态特征 / 读取状态特征和值 / 读取状态主体 / 读取状态排序时间
旧函数签名：状态主体、状态特征、状态值和排序时间读取函数族。
当前调用方：旧动态生成、场景状态排序、状态清理、位置变化候选和二次特征比较。
触发路径：读取旧状态主信息中的主体、特征、值、发生时间和收到时间。
输入：状态节点。
输出：主体引用、特征引用、特征值、排序时间或局部 bool。
读结构：旧状态主信息状态主体、状态特征、状态值、发生时间、收到时间。
写结构：无。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：读取实例状态的主体、特征、值材料和排序时间。
删除行为：不得把旧特征值变体、指针句柄或 Vec 句柄直接作为新项目通用状态值容器。
新增约束：实例状态必须有存在主体、场景归属和发生时间戳；高级服务读取特征状态材料必须经特征服务。
目标新结构：实例状态节点、状态值 I64 材料、发生时间戳、状态到存在 / 场景关系、特征状态材料。
目标领域服务入口：状态服务读取入口、特征服务读取状态材料入口。
对应规范 / 详细设计：状态服务详细设计、特征服务边界、基础信息服务分层规范。
入口拒绝条件：状态无效、错误节点类型、缺主体、缺特征、缺值、实例状态缺发生时间戳。
验证方式：有效实例状态可读主体、场景、发生时间戳和值；缺材料返回空。
初步判定：拆分
待用户确认项：旧 `特征值` 变体中的 Vec / 指针材料是否全部转入 FS-03 特征 / 特征值增强专项。
扫描证据：`状态类.cpp:166` 至 `状态类.cpp:258`。
```

### OF-082

```text
旧函数编号：OF-082
扫描批次：FS-02 / FS-07
旧项目路径：D:\鱼巢\状态类.cpp:262
旧函数名：生成状态 / 状态有效检查 / 获取状态二次特征
旧函数签名：状态节点主信息类 生成状态(...); bool 状态有效检查(...); I64 获取状态二次特征(...)
当前调用方：旧状态创建、状态差值、动态创建和二次特征生成路径。
触发路径：把主体、特征、状态值、发生时间和收到时间装入旧状态主信息，并用两个 I64 状态值计算差值。
输入：主体节点、特征节点、状态值、发生时间、收到时间、两个状态主信息。
输出：状态主信息、局部 bool 或 I64 差值。
读结构：旧状态主信息、旧特征值变体。
写结构：只在内存中构造待写主信息；不直接写仓库。
是否写机器事实：否；但输出会被后续创建状态写入事实。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：状态创建前材料归集、缺主体 / 特征 / 值拒绝、I64 状态差值可作为后续二次特征线索。
删除行为：不得把二态 bool 或差值函数直接当作二次特征节点或判断结论。
新增约束：二次特征节点只表达复合前置结构，差值结论应由状态、特征值、动态或因果证据承载。
目标新结构：状态节点、状态值、发生时间戳、二次特征组成关系或后续判断结果状态。
目标领域服务入口：状态服务 `创建实例状态` / `创建抽象状态`，二次特征服务后续差值特征入口。
对应规范 / 详细设计：状态服务详细设计、二次特征服务详细设计、错误结果与入口拒绝规范。
入口拒绝条件：主体无效、特征无效、状态值缺失、实例状态缺发生时间戳、二次特征组成项无效。
验证方式：拒绝路径不创建状态节点；有效状态值和发生时间戳可读；差值不被写成业务事实。
初步判定：拆分
待用户确认项：旧 `获取状态二次特征` 是否进入后续二次特征差值专项。
扫描证据：`状态类.cpp:262` 至 `状态类.cpp:305`。
```

### OF-083

```text
旧函数编号：OF-083
扫描批次：FS-02 / FS-07 / FS-08
旧项目路径：D:\鱼巢\状态类.cpp:309
旧函数名：创建状态 / 创建内部状态 / 记录内部状态到场景 / 创建世界状态 / 记录世界状态到场景 / 记录内部特征状态
旧函数签名：状态创建和内部 / 世界状态包装函数族。
当前调用方：旧世界树、观察入账、任务 / 方法内部状态和动态刷新路径。
触发路径：创建状态节点，追加场景状态索引，必要时触发状态记录后刷新动态。
输入：场景、主体、特征、状态值、是否变化、发生时间 / 收到时间、后处理回调。
输出：状态节点或局部 bool。
读结构：旧状态主信息、旧场景状态索引。
写结构：旧状态节点、旧状态主信息、旧基础信息全链登记、旧场景状态索引；间接触发动态创建。
是否写机器事实：是。
是否依赖日志：是，慢创建阶段写运行日志。
是否依赖显示：`是否变化` 可服务显示缓存。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：在场景中记录实例状态，保留主体、特征、值和发生时间戳，状态写入后可触发动态证据。
删除行为：不得迁移旧基础信息全链登记、旧场景主信息状态索引字段、运行日志阶段文本或显示变化缓存作为事实。
新增约束：实例状态必须经状态服务创建，再由场景服务收纳；动态刷新必须经动态服务，不能由状态服务隐式越权。
目标新结构：实例状态节点、状态值、发生时间戳、场景运行期临时关系、存在引用关系、动态证据。
目标领域服务入口：状态服务 `创建实例状态`，场景服务 `收纳实例状态`，动态服务 `记录实例动态` 或后续状态变更入口。
对应规范 / 详细设计：状态服务详细设计、场景服务详细设计、动态服务详细设计、领域服务授权写入规范。
入口拒绝条件：场景无效、主体无效、特征无效、值缺失、发生时间戳缺失、状态服务直接写动态、场景收纳失败被读成有效状态。
验证方式：创建后状态节点、场景收纳关系、存在关系和值 / 时间戳可读；拒绝路径状态 / 动态数量不变。
初步判定：改写为领域服务入口
待用户确认项：状态写入后自动刷新动态是否作为后续动态增强入口，而不是第一轮状态服务职责。
扫描证据：`状态类.cpp:309` 至 `状态类.cpp:510`。
```

### OF-084

```text
旧函数编号：OF-084
扫描批次：FS-02
旧项目路径：D:\鱼巢\状态类.cpp:514
旧函数名：删除状态 / 枚举全部状态 / 获取场景状态
旧函数签名：bool 删除状态(...); std::vector<状态节点类*> 枚举全部状态(); std::vector<状态节点类*> 获取场景状态(...)
当前调用方：旧状态清理、场景状态读回、动态相邻状态查找和诊断路径。
触发路径：删除状态前从所有场景状态索引移除；或按场景状态索引读取并排序状态。
输入：状态节点、场景节点。
输出：局部 bool 或状态节点集合。
读结构：旧基础信息树、旧场景状态索引、旧状态排序时间。
写结构：删除状态会写旧场景状态索引和节点生命周期。
是否写机器事实：删除状态是；枚举和读取为否。
是否依赖日志：否。
是否依赖显示：读取集合可服务显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：状态生命周期删除、按场景读取实例状态集合、按发生 / 收到时间排序。
删除行为：不得把旧场景状态索引字段直接迁移为权威主信息字段；不得用全局枚举替代领域读取准入。
新增约束：删除走节点仓库生命周期和场景服务关系删除；场景状态读取必须复核实例状态有效。
目标新结构：状态节点生命周期、场景到实例状态运行期临时关系、状态发生时间戳。
目标领域服务入口：状态服务删除 / 读取增强、场景服务读取场景状态组。
对应规范 / 详细设计：状态服务详细设计、场景服务详细设计、节点仓库详细设计。
入口拒绝条件：状态无效、场景无效、状态不在场景中、删除后仍可被读成有效场景状态。
验证方式：删除后旧句柄失效且场景状态关系不可读；读取只返回有效实例状态。
初步判定：拆分
待用户确认项：是否第一轮需要状态服务删除入口，还是暂缓到生命周期治理专项。
扫描证据：`状态类.cpp:514` 至 `状态类.cpp:558`。
```

### OF-085

```text
旧函数编号：OF-085
扫描批次：FS-02 / FS-08
旧项目路径：D:\鱼巢\状态类.cpp:562
旧函数名：是否当前活动状态 / 统计状态引用数量 / 可从活动状态链清理 / 清理已处理状态索引 / 清理超过时间窗口的状态索引 / 按活动状态限量清理 / 状态写入后整理 / 读取最近清理问题快照 / 清除最近清理问题快照
旧函数签名：状态活动判断、引用统计、清理和诊断快照函数族。
当前调用方：旧场景状态索引治理、状态写入后整理、运行诊断和动态 / 二次特征引用保护。
触发路径：基于同主体同特征更新状态数量、状态时间窗口和动态 / 二次特征引用数量清理场景状态索引。
输入：场景节点、状态节点、主体、特征、时间窗口、活动状态上限。
输出：局部 bool、清理数量、状态引用数量或清理问题快照。
读结构：旧场景状态索引、旧状态主信息、旧动态初始 / 结果状态引用、旧二次特征状态引用。
写结构：旧场景状态索引移除项、运行期清理问题快照、运行警告日志。
是否写机器事实：待确认；索引移除影响读取目录，但新项目应分流为运行期临时关系治理 / 非权威诊断。
是否依赖日志：是，清理失败写运行警告日志。
是否依赖显示：清理问题快照可服务诊断显示。
是否依赖 SQL：否。
是否依赖线程：使用互斥保护清理问题快照；线程不作为动作来源。
是否依赖外设 / D455 / 体素：否。
保留行为：场景内实例状态保留策略、被动态或二次特征引用的状态不得清理、清理问题可做人读诊断。
删除行为：不得由清理器自动修复权威结构；不得把清理问题快照、日志或统计数量当作业务事实。
新增约束：权威关系清理必须经状态 / 场景 / 动态 / 二次特征服务准入；缓存统计清空不得改变业务事实。
目标新结构：场景运行期临时关系、非权威缓存 / 统计快照、诊断材料。
目标领域服务入口：场景服务关系清理增强、非权威缓存与统计服务、状态服务生命周期治理。
对应规范 / 详细设计：非权威缓存与统计详细设计、状态服务详细设计、动态服务详细设计、二次特征服务详细设计。
入口拒绝条件：场景无效、状态无效、引用仍存在却强删关系、把诊断快照当机器事实、日志驱动结构修复。
验证方式：清理只影响可清理索引 / 临时关系；被引用状态不清理；清理快照缺失不影响权威状态读取。
初步判定：拆分
待用户确认项：状态活动索引治理是否归入非权威缓存与统计增强，还是状态服务生命周期专项。
扫描证据：`状态类.cpp:562` 至 `状态类.cpp:803`。
```

### OF-086

```text
旧函数编号：OF-086
扫描批次：FS-02 / FS-03 / FS-10
旧项目路径：D:\鱼巢\状态类.cpp:806
旧函数名：绑定主体特征 / 写入状态值 / 标记变化
旧函数签名：bool 绑定主体特征(...); bool 写入状态值(...); bool 标记变化(...)
当前调用方：旧状态补写、特征状态更新、显示变化缓存和动态 / 二次特征比较路径。
触发路径：直接改写旧状态主信息主体、特征、状态值、发生时间、收到时间或变化标记。
输入：状态节点、主体、特征、特征值、发生时间、收到时间、是否变化。
输出：局部 bool。
读结构：旧状态主信息。
写结构：旧状态主信息主体、特征、状态值、时间、统计观测和显示变化缓存。
是否写机器事实：是；`标记变化` 仅显示缓存。
是否依赖日志：否。
是否依赖显示：是，`标记变化` 注释明确只保留兼容显示缓存。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：授权状态值写入、主体 / 特征绑定和时间材料更新。
删除行为：不得迁移显示变化缓存为业务事实；不得绕过特征服务写特征状态材料；不得后改实例状态主体造成追溯漂移。
新增约束：实例状态创建后主体、场景和发生时间戳应稳定；特征当前材料写入通过特征服务，状态记录通过状态服务创建新节点。
目标新结构：实例状态节点、特征状态材料、发生时间戳、非权威显示缓存。
目标领域服务入口：状态服务创建新实例状态、特征服务写入状态材料；显示层只读视图专项。
对应规范 / 详细设计：状态服务详细设计、特征服务边界、显示层只读视图专项待设计。
入口拒绝条件：状态无效、主体无效、特征无效、值缺失、发生时间戳缺失、显示缓存被当作事实。
验证方式：新状态写入后值和时间可读；显示缓存不参与需求结算、任务完成或动态因果。
初步判定：拆分
待用户确认项：旧状态节点是否允许原地改值，还是新项目统一要求创建新实例状态记录。
扫描证据：`状态类.cpp:806` 至 `状态类.cpp:841`。
```

### OF-087

```text
旧函数编号：OF-087
扫描批次：FS-02 / FS-07
旧项目路径：D:\鱼巢\动态类.cpp:976
旧函数名：动态类::绑定基础信息 / 取动态主信息 / 推导动态名称 / 推导动态类型 / 按主键解析动态节点
旧函数签名：动态身份读取、名称 / 类型推导和按主键解析函数族。
当前调用方：旧动态类所有读写入口、因果生成、场景动态读取、运动基元候选和 SQL 显示镜像。
触发路径：旧服务定位基础信息树后，按主信息类型或主键识别动态节点，并按结构字段推导名称和类型。
输入：基础信息类指针、动态节点、动态主信息、主键字符串。
输出：动态主信息指针、动态节点指针、语素入口或空。
读结构：旧基础信息树、旧动态主信息、旧主键索引、旧语素入口。
写结构：`绑定基础信息` 只写运行期服务指针；其它函数只读。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：名称 / 类型可服务显示。
是否依赖 SQL：间接服务 SQL 显示镜像。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：动态节点类型复核、按稳定键定位候选、按结构字段推导人读名称 / 类型。
删除行为：不得把函数名、动作文本、日志标签或显示类型当作动作入口身份。
新增约束：新项目动态身份由动态服务和节点类型复核；动作语义的来源动作第一轮指向动作入口角色节点。
目标新结构：节点类型::动态、动态节点句柄、动作入口角色节点、只读显示名称。
目标领域服务入口：动态服务读取入口、动作入口服务 / 方法服务读取入口、语素服务只读绑定。
对应规范 / 详细设计：动态服务详细设计、动作入口详细设计、语素系统详细设计。
入口拒绝条件：主键为空、动态无效、错误节点类型、动作来源试图使用函数名 / 文本 / 日志标签。
验证方式：动态节点有效且材料可读；显示名称缺失不影响动态事实。
初步判定：拆分
待用户确认项：动态名称 / 类型推导是否仅归显示层只读视图，不进入动作入口。
扫描证据：`动态类.cpp:976` 至 `动态类.cpp:1013`。
```

### OF-088

```text
旧函数编号：OF-088
扫描批次：FS-02 / FS-07 / FS-08
旧项目路径：D:\鱼巢\动态类.cpp:1016
旧函数名：读取动态因果生成输入 / 读取动态来源场景 / 读取动态排序时间 / 读取动态双状态时间段 / 动态双状态同源可复算 / 读取动态聚合信息 / 是原子相邻动态 / 读取动态来源低层动态 / 计算动态列表路径签名 / 动态聚合来源链相同 / 评估动态特征影响
旧函数签名：动态材料、来源链、聚合信息和影响评估读取函数族。
当前调用方：旧因果生成、动态聚合、动态特征影响统计、动作致变双账复算和候选治理。
触发路径：从旧动态主信息读取初始 / 结果状态、来源动作、来源场景、层级、聚合方式、来源低层动态和路径签名。
输入：动态节点、动态列表、影响样本集合、阈值。
输出：状态端点、来源动作、来源场景、时间段、聚合材料、来源动态集合、影响统计。
读结构：旧动态主信息、旧状态主信息、旧来源低层动态引用、旧语素入口。
写结构：无。
是否写机器事实：否；影响评估输出是候选统计，不直接写事实。
是否依赖日志：否。
是否依赖显示：统计可服务诊断。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：因果生成所需动态材料读取、来源动态链读取、聚合路径复判和影响统计候选。
删除行为：不得把单个动态、路径签名或统计返回值解释为稳定因果结论或方法成功。
新增约束：因果引用只以来源动态做准入证据；影响统计和来源链属于后续因果 / 非权威缓存增强。
目标新结构：实例动态材料、轻量因果引用、动态来源链关系或后续缓存、非权威统计。
目标领域服务入口：动态服务读取动态材料、因果服务记录因果引用、非权威缓存 / 统计入口。
对应规范 / 详细设计：动态服务详细设计、因果轻量化口径、非权威缓存与统计详细设计。
入口拒绝条件：动态无效、缺端点 / 目标 / 前后值、来源动态无效、把统计候选当事实、把因果引用当稳定结论。
验证方式：动态材料可读；来源动态缺失时因果引用拒绝；统计清空不影响权威动态。
初步判定：拆分
待用户确认项：来源低层动态是否进入动态聚合后续专项，影响统计是否进入非权威缓存 / 因果增强。
扫描证据：`动态类.cpp:1016` 至 `动态类.cpp:1210`。
```

### OF-089

```text
旧函数编号：OF-089
扫描批次：FS-X / FS-10
旧项目路径：D:\鱼巢\动态类.cpp:1301
旧函数名：动态类::同步SQL控制面板动态显示项
旧函数签名：void 同步SQL控制面板动态显示项(const 动态节点类* 动态, const char* 阶段) noexcept
当前调用方：旧动态创建后 SQL 控制面板显示镜像同步。
触发路径：动态节点创建后写入旧 SQL 控制面板显示项和同步状态。
输入：动态节点、阶段文本。
输出：SQL 显示镜像写入和运行警告日志。
读结构：旧动态主信息、旧语素入口、旧父节点。
写结构：旧 SQL 控制面板显示项、旧 SQL 控制面板同步状态、运行警告日志。
是否写机器事实：否。
是否依赖日志：是。
是否依赖显示：是。
是否依赖 SQL：是。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：后续显示层可只读动态摘要、父来源和类型文本。
删除行为：本轮不得接 SQL、控制面板或显示镜像；不得用 SQL 同步失败回滚或裁决动态事实。
新增约束：显示层只读，SQL / 控制面板不得写业务事实或参与恢复。
目标新结构：无目标机器结构；后续显示层只读视图材料。
目标领域服务入口：显示层只读视图专项；本轮不迁移。
对应规范 / 详细设计：领域服务授权写入规范、显示层只读视图专项待设计。
入口拒绝条件：试图接入 SQL / 控制面板、显示文本参与机器裁决、日志或 SQL 状态作为动态事实来源。
验证方式：本轮仅登记为排除边界；后续显示专项必须证明只读。
初步判定：排除 / 后续专项
待用户确认项：是否进入后续“显示层只读视图专项”的动态摘要候选。
扫描证据：`动态类.cpp:1301` 至 `动态类.cpp:1369`。
```

### OF-090

```text
旧函数编号：OF-090
扫描批次：FS-02 / FS-07
旧项目路径：D:\鱼巢\动态类.cpp:1372
旧函数名：创建动态 / 创建叶子动态 / 创建动作动态 / 创建方法动作动态 / 创建存在动态 / 创建场景动态 / 允许生成状态动态 / 允许生成存在动态 / 允许生成场景动态 / 允许生成方法动作动态 / 删除动态
旧函数签名：动态创建、动作动态、方法动作动态、存在 / 场景动态和生成准入函数族。
当前调用方：旧状态记录后刷新动态、方法动作入账、观察存在 / 场景变化、因果生成和动态聚合路径。
触发路径：基于场景、状态端点、主体、特征、来源方法 / 动作、输入 / 输出场景创建动态节点并追加场景动态索引。
输入：场景、初始状态、结果状态、主体、特征、来源动作、来源动作名、时间段、输入 / 输出场景、来源低层动态。
输出：动态节点或局部 bool。
读结构：旧状态主信息、旧方法动作入口、旧场景动态索引。
写结构：旧动态节点、旧动态主信息、旧场景动态索引、旧动态二次特征 / 聚合后处理、SQL 显示镜像。
是否写机器事实：是。
是否依赖日志：是，慢创建阶段写运行日志。
是否依赖显示：间接触发 SQL 显示镜像。
是否依赖 SQL：是，创建后调用 SQL 显示同步。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：记录实例动态、状态端点变化、方法动作来源、存在 / 场景变化和生成准入检查。
删除行为：不得迁移 SQL 显示镜像、旧动作名文本、旧方法裸指针或动态创建后的自动聚合为第一轮事实。
新增约束：实例动态必须有场景、存在主体、发生时间戳、被改变目标、前值和后值；动作语义来源动作指向动作入口角色节点。
目标新结构：实例动态节点、发生时间戳、目标引用、前后值材料、可选动作入口角色节点、场景运行期临时关系。
目标领域服务入口：动态服务 `记录实例动态`、动作入口服务、方法服务执行后动态证据入口、场景服务 `收纳实例动态`。
对应规范 / 详细设计：动态服务详细设计、动作入口详细设计、方法系统详细设计、场景服务详细设计。
入口拒绝条件：场景无效、存在主体无效、发生时间戳缺失、目标无效、缺前值 / 后值、来源动作不是有效动作入口角色、SQL / 显示路径参与裁决。
验证方式：动态节点有效，时间戳、目标和前后值可回查；拒绝路径动态 / 关系数量不变。
初步判定：改写为领域服务入口
待用户确认项：旧 `创建方法动作动态` 中 `执行成功 / 错误码` 是否转入动作动态证据专项或非权威诊断。
扫描证据：`动态类.cpp:1372` 至 `动态类.cpp:1885`。
```

### OF-091

```text
旧函数编号：OF-091
扫描批次：FS-02 / FS-07 / FS-X
旧项目路径：D:\鱼巢\动态类.cpp:1898
旧函数名：枚举全部动态 / 获取场景动态 / 读取位置变化原子动态候选 / 读取位置变化主体动态片段组 / 读取位置变化运动基元候选 / 读取观察关系动态片段组 / 读取观察关系运动基元候选
旧函数签名：动态枚举、场景动态读取和运动基元候选只读函数族。
当前调用方：旧观察运动理解、关系动态候选、方法候选学习和显示诊断路径。
触发路径：从场景动态索引读取动态，筛选位置变化或观察关系片段并输出运行期运动基元候选。
输入：场景、最大时间缺口、停止阈值、关系片段阈值。
输出：动态集合、动态片段组、位置 / 关系运动基元候选。
读结构：旧场景动态索引、旧动态主信息、旧状态主信息、旧特征语义类型、旧二次特征证据。
写结构：无。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：候选可服务显示和诊断。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：间接服务观察链路，本轮不接入外设。
保留行为：场景动态只读集合、位置 / 关系变化候选、运动基元候选可作为后续方法学习线索。
删除行为：不得把候选静默升级为稳定方法、稳定因果或动作入口；不得接外设 / 体素观察链。
新增约束：候选只读，不写业务事实；稳定规则必须经二次特征、方法系统、因果评分和确认专项。
目标新结构：动态集合、二次特征候选、非权威统计、后续方法候选材料。
目标领域服务入口：动态服务只读增强、二次特征服务增强、方法候选 / 学习专项待设计。
对应规范 / 详细设计：动态服务详细设计、二次特征服务详细设计、方法系统详细设计、外设 / 体素专项待设计。
入口拒绝条件：场景无效、动态无效、特征语义类型未确认、候选被写成事实、外设材料绕过领域入口。
验证方式：候选读取不新增节点 / 关系；多候选返回集合，不静默裁决。
初步判定：拆分
待用户确认项：位置 / 关系运动基元候选是否归入后续“需求树 / 任务管理 / 方法系统增强专项”。
扫描证据：`动态类.cpp:1898` 至 `动态类.cpp:2260`。
```

### OF-092

```text
旧函数编号：OF-092
扫描批次：FS-02 / FS-07
旧项目路径：D:\鱼巢\动态类.cpp:2262
旧函数名：查找相邻前状态 / 状态记录后刷新动态
旧函数签名：状态节点类* 查找相邻前状态(...); 动态节点类* 状态记录后刷新动态(...)
当前调用方：旧状态创建后自动动态刷新路径。
触发路径：状态入账后按同主体同特征倒序查找前状态，形成叶子动态。
输入：场景、新状态、时间戳。
输出：前状态或动态节点。
读结构：旧场景状态索引、旧状态主信息、旧状态值和时间。
写结构：`状态记录后刷新动态` 会创建旧动态节点和场景动态索引。
是否写机器事实：`查找相邻前状态` 否；`状态记录后刷新动态` 是。
是否依赖日志：是，慢查找阶段写运行日志。
是否依赖显示：间接触发动态显示同步。
是否依赖 SQL：间接触发动态创建后的 SQL 显示同步。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：同主体同特征状态变化可生成动态证据，前状态查找需要按场景和时间复核。
删除行为：不得由状态服务隐式越权创建动态；不得把运行日志或 SQL 显示同步当动态事实。
新增约束：动态记录应由动态服务显式入口执行；状态服务可提供材料或调用授权动态入口，但不能绕过动态入口拒绝。
目标新结构：实例状态节点、实例动态节点、前后值材料、发生时间戳、场景关系。
目标领域服务入口：动态服务 `记录实例动态`、后续 `状态变化生成动态` 显式入口。
对应规范 / 详细设计：动态服务详细设计、状态服务详细设计、领域服务授权写入规范。
入口拒绝条件：场景无效、新状态无效、前状态缺失、主体 / 特征不一致、缺前值 / 后值、缺发生时间戳。
验证方式：有效状态变化形成动态；无前状态不创建动态；拒绝路径结构数量不变。
初步判定：改写为领域服务入口
待用户确认项：是否建立显式 `状态变化生成动态` 入口作为动态服务增强。
扫描证据：`动态类.cpp:2262` 至 `动态类.cpp:2399`。
```

### OF-093

```text
旧函数编号：OF-093
扫描批次：FS-02 / FS-07
旧项目路径：D:\鱼巢\动态类.cpp:2402
旧函数名：设置主体特征 / 设置聚合信息 / 追加来源低层动态 / 绑定来源方法 / 绑定来源场景
旧函数签名：动态主信息字段改写和来源绑定函数族。
当前调用方：旧动态创建补齐、聚合动态、存在 / 场景动态、方法动作动态和因果输入读取路径。
触发路径：创建后补写动态主体、特征、层级、聚合方式、来源低层动态、来源方法和输入 / 输出场景。
输入：动态节点、主体、特征、动态层级、聚合方式、来源低层动态、来源方法、来源动作名、输入场景、输出场景。
输出：局部 bool。
读结构：旧动态主信息、旧方法动作入口。
写结构：旧动态主信息主体、特征、名称、类型、聚合字段、来源低层动态、来源动作和来源场景。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：名称 / 类型补齐可服务显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：动态来源、聚合信息和输入 / 输出场景追溯。
删除行为：不得用方法节点、动作名文本或函数名冒充动作入口；不得原地改写已确认动态的主体 / 特征导致证据漂移。
新增约束：动作来源绑定必须指向动作入口角色节点；聚合动态和来源链需后续专项确认唯一性和多目标规则。
目标新结构：动态到动作入口角色关系、动态到输入 / 输出场景关系、来源低层动态关系、聚合材料。
目标领域服务入口：动态服务来源绑定增强、动作入口服务读取入口、方法服务只读入口。
对应规范 / 详细设计：动态服务详细设计、动作入口详细设计、方法系统详细设计、关系仓库详细设计。
入口拒绝条件：动态无效、主体 / 特征无效、来源动态无效、来源动作不是动作入口角色、输入 / 输出场景无效、重复来源关系语义未确认。
验证方式：来源关系可读；错误来源拒绝；显示名称变化不改变动态事实。
初步判定：拆分
待用户确认项：来源低层动态和输入 / 输出场景是否进入动态聚合专项，还是动作动态证据专项。
扫描证据：`动态类.cpp:2402` 至 `动态类.cpp:2467`。
```

## 4. 本轮初步结论

```text
1. 旧 `状态类` 把状态主体、特征、值、发生时间、收到时间、场景状态索引、动态刷新、清理和显示变化缓存混在旧状态主信息与场景索引中；新项目必须拆成状态服务、场景服务、动态服务、特征服务和非权威缓存 / 显示只读边界。
2. 旧状态创建语义可保留为实例状态入账，但新项目实例状态必须有场景、存在主体和发生时间戳；状态写入后动态刷新不能隐式绕过动态服务。
3. 旧状态清理和活动状态索引治理只能作为运行期临时关系治理或非权威缓存 / 统计增强线索，不能作为业务事实或结构自动修复器。
4. 旧 `动态类` 中实例动态、动作动态、方法动作动态、存在 / 场景动态、聚合动态、运动基元候选和 SQL 显示同步混在一起；新项目必须分流为动态服务、动作入口、方法系统、二次特征 / 因果增强、非权威统计和显示层只读视图。
5. 旧动态来源方法 / 动作名不能直接迁移为动作身份；动作语义下第一轮来源动作应指向动作入口角色节点，普通场景变化可以无来源动作。
```

## 5. 后续待执行

```text
1. 下一旧函数编号从 OF-094 开始。
2. 继续 FS-02，扫描旧二次特征类和因果类第一批函数。
3. 旧状态 / 动态中的特征值变体、Vec / 指针值材料转 FS-03 特征 / 特征值增强专项。
4. 旧动态运动基元候选、动态聚合和影响统计转 FS-07 / 后续方法系统增强专项。
5. 旧 SQL 控制面板动态显示同步转 FS-X / FS-10 排除边界。
6. OF-080 到 OF-093 均为初步判定，需用户确认后才可进入任何代码实施。
```

## 6. 禁止宣称

```text
不得声明旧函数已扫描完成。
不得声明旧状态类或旧动态类已迁移。
不得声明旧状态主信息字段、旧动态主信息字段、旧场景状态 / 动态索引、旧显示缓存或旧 SQL 控制面板镜像可直接进入新项目。
不得声明完整状态系统、动态系统、动作系统、因果系统、二次特征系统或方法学习系统完成。
不得接控制面板 / SQL / D455 / 体素 / 外设。
```
