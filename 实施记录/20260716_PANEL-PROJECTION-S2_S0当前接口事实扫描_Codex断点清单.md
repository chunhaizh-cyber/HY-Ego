# PANEL-PROJECTION-S2 S0 当前接口事实扫描 Codex 断点清单

日期：2026-07-16

交互：WIN-0001-E74

决策：JY-358

计划 / 队列 / 阶段：#261 / DQ-153 / PANEL-PROJECTION-S2 / 830

状态：当前正式接口 S0 已完成 / 三件套已重做 / 830 未实施 / 未改 C++ / 未构建 / 未运行程序

## 1. 输入入口

```text
正式基线提交 35f4b3b
JY-357 与 WIN-0001-E73 正式治理产物
海中鱼巣/领域/控制面板服务.h
海中鱼巣/领域/概念图服务.h
海中鱼巣/界面/控制面板窗口.h
海中鱼巣/界面/控制面板窗口.cpp
规范/详细设计/控制面板六类真实树只读显示详细设计.md
规范/详细设计/数据仓库关系查询二级索引与性能治理详细设计.md
计划/20260713_PANEL-PROJECTION-S2_显示分类与当前概念根按需只读投影计划_v0.1.md
```

执行工作树 `D:/海中鱼巣-执行-265` 的四文件失败 WIP 已由执行窗口按 E73 创建本地可逆 stash；本扫描未读取、恢复、修改、暂存或消费该 stash。

## 2. 当前代码事实

1. `控制面板服务::生成六类真实树只读材料` 固定依次生成世界、概念、语素、需求、任务和方法六类；任一分类失败，整组六类候选不发布。
2. 概念分类先读活动版本和全部四个概念根，再逐根调用 `概念图服务::读取抽象树视图`。概念图服务已经正式提供单根 `读取抽象树视图并返回结果(根概念, 节点预算, 深度预算)`，并区分零预算、非活动根、预算耗尽和活动图内部错误。
3. 世界与语素来自控制面板服务构造时保存的值式 DTO；需求根固定来自两个根需求；任务根从需求承接关系发现；方法根从任务选择关系发现。
4. 任务和方法根发现当前嵌在上一分类的完整生成路径中，但正式领域服务已经提供所需值式读取。控制面板服务可在自身内部抽取根身份发现辅助，不需要修改需求、任务或方法服务。
5. 当前控制面板请求没有当前分类、当前概念根、节点预算、深度预算、根页大小或继续游标字段。递归节点 DTO 保留稳定节点句柄、关系角色、子项和重复投影标志，不承载独立边表。
6. 控制面板窗口每次 `读取只读材料` 都生成完整六类候选；刷新时删除整个 TreeView 并重建六个分类及全部节点。分类导航只定位已经存在的分类根，不触发单分类读取。
7. 窗口快照固定保存六项树结构数组和六树候选序号。当前没有当前分类页快照、当前概念根选择或根页游标状态。
8. #258 已按 JY-357 正式不实施、不发布，关系仓库仍使用权威关系表和当前全表查询入口。#261 不得假定二级索引、候选桶、阶段 800 或失败 WIP 存在。

## 3. 当前差距与可复用面

```text
可复用：六类分类枚举、现有节点树 DTO、六类私有构造逻辑、概念图正式单根读取、现有领域服务值式读取、窗口导航与节点详情。
需新增：按需请求 / 结果 / 根页游标、当前概念根选项、单分类构造入口、任务 / 方法根身份发现、当前页窗口快照和当前分类 TreeView 重建。
必须保留：生成六类真实树只读材料 的公开原义和回归入口。
不得新增：关系仓库读取旁路、显示索引事实、跨页权威快照声明、业务写入口或原始事务能力。
```

任务、方法分类可以读取上游服务来发现根身份，但不得因此构造或发布需求树、任务树。非概念分类当前没有统一活动版本；根页继续只绑定确定排序后的根身份签名，每次结果都是独立当前只读页并整体替换，不得把多页拼成一个同版本机器快照。

## 4. JY-358 裁决

1. #261 从 JY-357 退回设计修订为 JY-358 待执行，取消 #258、800 前置；正式前置只保留 #257 已完成、JY-357 已收口和本次 S0 / 三件套。
2. 生产窗口改用当前分类按需入口；旧六类完整入口只保留兼容和回归，不再作为生产刷新路径。
3. 概念分类一次只生成一个当前活动根；窗口通过控制面板值式结果获得四根选项，不直接持有概念图服务。
4. 根分页只在真实根组边界切页，不截断单棵递归树。下一页重新读取当前材料并整体替换当前页，不累计为机器事实。
5. 实施只允许修改控制面板服务、控制面板窗口、新独立 Debug 自检真模块、入口和工程最小登记；四仓库、概念图服务、其它领域服务、事务 ABI、线程、SQL、持久化、D455 和 #214 禁止修改。

## 5. 输出变化与调用路径

```text
分类导航或刷新
-> 形成当前分类请求
-> 控制面板服务读取该分类所需值式材料
-> 概念类只读当前根；任务 / 方法只做上游根身份发现
-> 根页边界与单根预算复核
-> 形成当前页只读候选
-> TreeView 只替换当前分类页
```

本轮只形成正式设计治理文件。C++、工程、filters、入口、自检阶段和运行期结构均不变化，830 未登记。

## 6. 验证边界

本轮验证 Markdown / HTML Mermaid 同文、详细设计和计划一致性、路径与阶段占用、严格规范、Git 差异及 C++ / 工程零修改；不运行构建、程序、性能基准或连续自检。

可以声明：#261 已基于当前正式接口完成 S0 与三件套重做并重新具备执行前复核条件。

不得声明：按需投影、分页、性能提升、二级索引、完整业务控制面板、旧控制面板迁移或项目迁移已经完成。
