# SELFTEST-MIGRATION-B1B 实际调用语境漂移修订 Codex 断点清单

日期：2026-07-12
治理编号：JY-268
队列：#243 / DQ-135

## 1. 输入入口

```text
HEAD/main/origin：05c0004。
#245 / JY-267 已完成并验证。
执行窗口 #243 实际接口复核草案已完整回退，worktree / index 干净，无提交。
当前入口 S7 9651-10165、S8 10168-10499、输出 11032-11093 和前序支持读取闭包 8841-8851。
```

## 2. 核心裁决

1. S7 使用显式调用期非拥有语境：概念图、语素、统计、节点 / 关系 / 索引、存在 / 动态 / 二次特征 / 因果。
2. S7 前序不可变材料：概念图初始化读数、第五切片活动根组、第五切片发布输入、S6D 目标概念。
3. S7 单写报告向 S8 交接 A01-A08、总结果、目标概念、可选支持实例和完整标志；不把实时活动图封存成权威快照。
4. S8 使用同一概念图、统计和三仓库的调用期语境；消费 S7 报告、存在根、自我存在、第四 / 第五切片回归证据，并实时读取后 S7 状态。
5. 支持关系读取闭包迁为 S7 模块导出的只读测试辅助，S8 单向导入；不复制前序夹具，不保留入口闭包。
6. 两个启动计时调用留在入口 300 / 310 登记包装层；逐项输出、报告和临时并发对象归各自模块。

## 3. 输出变化

修订现状图完整包、施工流程图、详细设计、逻辑提取矩阵和 #243。#243 重新登记为依赖已满足待执行，执行前逐字段、逐编号、逐实时读取点复核。#217 继续门控。下一完成治理编号顺延为 JY-269。

## 4. 验证方式

```text
流程图纠偏严格检查。
规范严格检查。
两套 Mermaid MD / HTML 图体一致。
计划、索引、队列登记一致。
git diff --check。
```

本批不修改 C++、工程或测试，不运行构建和程序，不声明 #243 已实现。
