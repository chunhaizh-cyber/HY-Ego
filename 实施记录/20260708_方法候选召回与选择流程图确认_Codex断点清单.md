# 方法候选召回与选择流程图确认 Codex 断点清单

日期：2026-07-08

## 1. 输入入口

```text
用户确认：《方法候选召回与选择代码逻辑流程图》，结论：可以确认。
确认范围：仅限第 10 项“方法候选召回与选择流程”的代码逻辑流程图材料。
禁止扩大：不生成详细设计，不生成施工计划，不登记可执行队列，不修改 C++。
```

## 2. 核心处理

```text
1. 将 `流程图/20260708_方法候选召回与选择代码逻辑流程图_v0.1.md` 和同名 HTML 标记为已确认流程图材料。
2. 保留当前需求侧边界：`需求服务::方法候选准入材料是否可读` 只说明需求承接材料可读，不等于已召回方法。
3. 保留当前任务侧边界：`任务服务::选择任务方法` 是保守写入当前任务方法关系的入口，不证明候选方法满足需求目标。
4. 保留当前方法侧边界：方法服务可读取方法首、条件节点、结果节点、条件 / 结果场景、动作入口和稳定动作键动作入口，但没有结果能力索引和候选排序算法。
5. 保留后续门禁边界：专门候选召回算法、结果能力索引、条件判定、条件结果对归并、候选排序、自动选择策略、方法学习、来源任务、父 / 前置 / 后续方法关系和完整方法查找系统仍需后续详细设计或施工计划确认。
6. 保留排除边界：旧方法查找函数体、旧结果能力裸字段、可查找 bool、成熟度 I64、SQL、控制面板、D455、体素和外设不得从本图进入实施。
```

## 3. 结构承载

```text
流程图承载：
- 流程图/20260708_方法候选召回与选择代码逻辑流程图_v0.1.md
- 流程图/20260708_方法候选召回与选择代码逻辑流程图_v0.1.html

项目记忆承载：
- 项目记忆/当前状态.md
- 项目记忆/决策记录.md
- 项目记忆/待确认问题.md
- 计划/计划索引.md

实施记录承载：
- 实施记录/20260708_方法候选召回与选择流程图生成_Codex断点清单.md
- 实施记录/20260708_方法候选召回与选择流程图确认_Codex断点清单.md
```

## 4. 输出变化

```text
第 10 项“方法候选召回与选择”代码逻辑流程图确认完成。
项目记忆同步为：
- 当前状态新增第 10 项流程图确认态。
- 决策记录新增本流程图确认决策。
- 待确认问题新增并关闭本流程图确认项。
- 计划索引新增本流程图确认状态行。
```

## 5. 调用路径

```text
用户确认
-> AGENTS.md / 计划索引 / 项目规则总纲 / 规则迁移清单
-> 方法候选召回与选择流程图生成记录和当前项目记忆
-> 本确认断点清单与项目记忆状态同步
```

## 6. 验证方式

```text
本切片为流程图确认和项目记忆同步切片。
计划验证：
- 确认 Markdown 和 HTML 两份流程图文件存在。
- 确认新增确认断点清单存在。
- 运行 git diff --check。
- 运行 tools/check_specs.py。

实际结果：
- Markdown、HTML 和本确认断点清单文件均存在。
- `git diff --check -- 计划/计划索引.md 项目记忆/当前状态.md 项目记忆/决策记录.md 项目记忆/待确认问题.md 实施记录/20260708_方法候选召回与选择流程图确认_Codex断点清单.md` 通过；仅出现工作区 LF 将被 Git 触碰时替换为 CRLF 的提示。
- `python .\tools\check_specs.py` 输出：海中鱼巣规范治理检查: 未发现问题。

未执行：
- 未修改 C++。
- 未运行 msbuild。
- 未运行默认入口。
- 未生成详细设计。
- 未生成施工计划。
- 未登记可执行队列。

收尾同步：
- 未暂存、未提交、未推送本确认切片。
- 阻塞原因：当前 Git index 已存在非本切片的 BASE-D1 代码实施暂存集合，包括 C++ 文件、BASE-D1 计划归档、BASE-D1 实施记录和 `项目记忆/Codex任务队列.md`；若直接提交会混入执行窗口内容。
- 本确认切片改动仍保留在工作区，等待 BASE-D1 暂存集合完成提交或释放 index 后再单独暂存提交。
```

## 7. 禁止宣称

```text
不得宣称方法候选召回已实现。
不得宣称候选排序、自动选择、条件结果对归并、方法学习或完整方法查找系统已完成。
不得宣称结果能力索引、来源任务、父方法、前置 / 后续方法关系已完整落地。
不得宣称任务选择方法关系证明方法满足需求目标。
不得宣称旧方法主信息字段、旧方法函数体、旧动作句柄、SQL、控制面板、D455、体素或外设已迁移。
不得宣称本流程图确认提供代码实施许可。
不得宣称已生成方法候选召回与条件结果对详细设计、施工计划或可执行队列项。
```
