# 20260708 FLOW-14 轻量因果引用代码实施 Codex 断点清单

日期：2026-07-08

状态：已完成 / 已验证 / 已归档计划 / 代码已实现 / 未迁移旧函数

## 1. 输入入口

- 可执行队列：`项目记忆/Codex任务队列.md` #143。
- 施工计划：`计划/已完成计划/20260708_FLOW-14_轻量因果引用施工计划_v0.1.md`。
- 上游流程图：`流程图/20260708_轻量因果引用代码逻辑流程图_v0.1.md`。
- 上游详细设计：`规范/详细设计/轻量因果引用代码逻辑详细设计.md`。

## 2. 核心处理

- 在 `海中鱼巣/入口.cpp` 增加 FLOW-14 独立验收：
  - BASE-D12-A1 因果引用基础身份。
  - BASE-D12-A2 来源动态准入拒绝。
  - BASE-D12-A3 动态材料准入。
  - BASE-D12-A4 来源动态关系不持久化。
  - BASE-D12-A5 因果候选只读。
  - BASE-D12-A6 缺证据候选占位。
  - BASE-D12-A7 结算可选材料边界。
  - BASE-D12-A8 排除项扫描。

## 3. 结构承载

- 因果引用基础身份仍由因果引用节点和主信息承载。
- 来源动态只作为入口准入证据，不持久化因果引用到来源动态的关系。
- 动态材料准入仍由 `动态服务::读取动态材料` 和 `动态服务::动态证据材料是否完整` 支撑。
- 因果模板候选、查询候选和缺证据候选只作为只读候选材料，不新增任务、方法、需求或结算。
- 需求结算中的因果引用只作为可选材料边界，不单独裁决需求满足。

## 4. 输出变化

- `海中鱼巣/入口.cpp` 新增 FLOW-14 和 BASE-D12-A1 到 BASE-D12-A8 验收输出，并纳入服务操作函数矩阵第一批总验收。
- 未修改 `海中鱼巣/领域/因果服务.h`、`海中鱼巣/领域/动态服务.h`、核心仓库或工程文件。
- 本计划已归档到 `计划/已完成计划/`。
- `项目记忆/Codex任务队列.md` #143 已标记完成，当前下一项为 #144 FLOW-15。

## 5. 验证结果

```powershell
git diff --check -- 海中鱼巣/领域/因果服务.h 海中鱼巣/领域/动态服务.h 海中鱼巣/入口.cpp
python .\tools\check_specs.py
msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m
.\x64\Debug\海中鱼巣.exe
rg -n "控制面板|SQL|ADO|WebView|D455|体素|外设|std::thread|std::async|工作队列" 海中鱼巣/领域/因果服务.h 海中鱼巣/领域/动态服务.h 海中鱼巣/入口.cpp
```

- `git diff --check`：通过；仅提示 LF/CRLF，未发现 whitespace error。
- `python .\tools\check_specs.py`：通过，输出 `海中鱼巣规范治理检查: 未发现问题`。
- Debug x64 构建：通过，0 警告，0 错误。
- 默认入口：返回 0；`FLOW-14 轻量因果引用第一轮`、BASE-D12-A1 到 BASE-D12-A8 均输出 `通过`；既有 FLOW-13、FLOW-12、FLOW-11、服务操作函数矩阵第一批和最小闭环验收也输出通过。
- 排除项扫描：仅命中既有 `XS-A9` 人读输出标签，不是控制面板、SQL、ADO、WebView、D455、体素、外设、线程或工作队列接入代码。

## 6. 完成声明边界

本次可以声明：

```text
FLOW-14 轻量因果引用第一轮因果引用身份、来源动态准入、动态材料准入、候选只读和结算可选材料边界验收已通过。
```

不得声明：

```text
稳定因果、因果可靠性评分、需求满足裁决或旧因果能力已完成。
控制面板、SQL、ADO、WebView、D455、体素或外设已接入。
```
