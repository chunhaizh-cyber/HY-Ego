# TASK-PLANNING-S1 筹办召回与任务服务选择提交代码实施 Codex 断点清单

日期：2026-07-13

队列编号：#223

治理编号：JY-305

状态：已完成 / 已验证 / 已治理归档

## 1. 输入与前置

```text
#203 78ab192：方法首权威登记与生命周期基线
#204 18f7bca：条件结果结构化规格与版本读回
#205 50f6c10：结果能力派生多值索引
#206 1fbf7c8：任务目标只读候选召回
#207 72d5be9：运行上下文条件复判
#208 26e63ef：确定性排序与只读选择建议
#209 7541c23：任务授权选择接线当前代码事实复核
#221 f6eac60：任务生命周期初态
#222 0ffb8da：任务生命周期合法迁移与终态封口
JY-302 / JY-303 / JY-304：选择记录、批次所有权和节点 14 准入合同
```

## 2. 实际实施

1. 追加节点 14 `任务方法选择记录`、关系 16 `任务方法选择`；节点仓库只扩私有类型上限，关系 16 固定任务源、记录目标、顺序号 30 和同任务单有效。
2. 任务服务新增双向承接授权、扁平选择请求、不可变记录快照、当前选择、审计和独占提交入口；完整请求以可逆 I64 规范元组保存，关系 16 最后发布。
3. 方法召回服务在现有 `复核只读选择建议来源` 之后展开召回请求、完整有序候选、规格、条件和动作来源；任务服务不反向依赖方法或索引。
4. 方法服务按任务服务重读的组合身份复核关系 16 记录、顺序号 30 投影和生命周期，再形成动作桥材料。
5. 正式 `待重筹办 -> 筹办中` 在同一独占许可内失效旧投影与关系 16，记录节点、主信息和完整关系审计保留。
6. 新增真模块 `自检.任务筹办选择.ixx`，入口只登记最终回归 580 并汇总结果；概念命名未知关系负例顺延到 17。

## 3. 验证

```text
TASK-PLANNING-S1-A01 至 A15：全部通过
Debug x64：0 警告，0 错误
完整自检：海中鱼巣.exe --self-test-exit，退出码 0
并发选择：每轮 50 线程同键提交，单一当前且其余幂等读回
严格规范：py -3.14 tools/check_specs.py --strict，通过
差异检查：git diff --check，通过
静态门禁：任务服务反向包含方法侧 0；580 / 模块声明 / 工程登记各 1
连续复验：最终版本 20 / 20 轮退出 0，总用时 271.3 秒
```

## 4. 完成边界

只证明连接态隔离语境中的方法召回建议适配、任务服务唯一当前选择、不可变审计、重筹办换代和精确动作桥接口。未执行方法，未形成通用回执、任务结果、需求结算、完整生产服务装配、恢复或旧能力迁移完成。
