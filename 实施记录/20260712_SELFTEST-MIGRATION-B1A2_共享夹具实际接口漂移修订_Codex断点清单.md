# SELFTEST-MIGRATION-B1A2 共享夹具实际接口漂移修订 Codex 断点清单

日期：2026-07-12
治理编号：JY-265
队列：#245 / DQ-137

## 1. 输入入口

```text
HEAD / main / origin：10e24cb。
执行窗口 #245 实际编译复核回传：原 6.2 / 6.3 最小上下文假定不成立，草案已完整回退，无提交、无代码或工程差异。
海中鱼巣/入口.cpp 当前 S4A-D、S5、S3B 真实代码锚点。
#242 正式阶段与报告接口。
CODE-FILE-01、现有施工流程图、详细设计和 #245 计划。
```

## 2. 核心处理

1. 按当前代码逐项冻结 S4A-D、S5、S3B 的真实外部依赖和内部所有物。
2. 裁决 S4A-D 使用主装配非拥有调用语境，不隐藏 `主信息` 与服务引用。
3. 裁决 S5 独立自检模块唯一拥有完整隔离夹具，只接收方法登记根稳定键和自我根需求参数值。
4. 裁决 S3B 借用后段现成材料、跨域负例句柄和主信息观察锚点，模块内部拥有临时线程、桥和邮箱。
5. 新增聚焦现状图、逐行映射、输入契约、非成功二分和偏差清单；同步修订施工图、详细设计、#245、索引、队列和项目记忆。

## 3. 结构承载

本批只新增或修改流程图、详细设计、计划和治理记录，不新增机器结构，不修改 C++ 或工程文件。预期代码产物仍由后续执行窗口按修订 #245 建立。

## 4. 输出变化

```text
#245 从“两个模块 + 最小上下文”改为“三个模块 + 显式所有权”。
初始化闭环顺序 100：S4A-D，借用主装配语境。
初始化闭环顺序 200：S5，模块内部独占隔离夹具。
领域回执顺序 100：S3B，借用后段现成材料并内部拥有临时线程桥邮箱。
#243 继续门控，不在本批消费。
```

## 5. 调用路径

```text
执行前接口漂移
-> 完整回退
-> 当前代码事实扫描
-> 三种所有权拆分
-> 现状图与四表
-> 施工流程图
-> 详细设计
-> 修订 #245
-> 执行前逐字段复核
```

## 6. 验证方式

```text
流程图 MD / HTML Mermaid 图体逐字一致。
check_flowchart_correction.py --strict --require-plan-registration。
check_specs.py --strict。
git diff --check。
```

本批是纯文档治理，不运行 C++ 构建或程序，不声明 #245 已实现。

## 7. 释放与后继

提交推送后释放 `计划/计划索引.md`、`项目记忆/*`、逐能力台账和 git index；执行窗口只从修订 #245 重新开始实际接口复核。再次不一致时继续退回，不得自行扩大上下文、复制夹具或修改测试。
