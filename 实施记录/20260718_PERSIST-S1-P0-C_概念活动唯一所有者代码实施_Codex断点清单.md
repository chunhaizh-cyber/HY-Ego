# PERSIST-S1-P0-C 概念活动唯一所有者代码实施 Codex 断点清单

日期：2026-07-18

执行队列：#296

设计队列：DQ-188

计划版本：v0.5

批次：WB-296-05 / WT-296-R5

冻结基线：`292efeeab734312444d91ef9e265cd736cbf8c73`

状态：分支完成待集成 / S0-S7 完成

## 1. 接管事实

1. 任务 worktree 为 `C:\Users\zhchh\.codex\worktrees\3d0a\海中鱼巢`，分支为 `codex/296-p0c`，接管时 HEAD 等于冻结基线且工作树 clean。
2. 已按 JY-395 使用默认 Windows 沙箱完成接管复测；未使用人工审批或提升权限。
3. 已只读复核 `origin/main@e6d8cb2` 的登记、索引、队列、窗口记录和 JY-395 平台口径，并读取 #296 计划、v0.3 流程图、详细设计第 17 节、#295 正式矩阵、项目规则和实际接口。

## 2. S0 逐签名复核

| 计划假定接口 | 当前事实 | 结论 |
| --- | --- | --- |
| `运行期上下文::初始化概念活动(...)` | 当前不存在；允许修改 `启动.运行期上下文.ixx` 可新增 | 待实施目标，不是漂移 |
| `运行期业务装配::初始化概念活动(...)` | 当前不存在；装配已同域拥有状态业务服务、概念图结构数据操作和四仓库接线 | 待实施目标，不是漂移 |
| `运行期上下文::读取概念活动材料()` | 当前不存在；允许修改上下文可新增值式读回 | 待实施目标，不是漂移 |
| 三生命周期角色抽象状态 | `状态业务服务::创建抽象状态`、`读取状态材料`、`读取主键状态材料` 正式存在 | 一致 |
| 四根重建 | `系统角色清单` 正式提供四根稳定主键、节点和主信息身份 | 一致 |
| 三类专用关系重建 | 关系类型 9 / 10 / 11 和同域关系仓库读取入口正式存在 | 一致 |
| 生命周期关系 | `关系类型::概念生命周期 = 12` 与结构写入会话正式存在 | 一致 |
| 阶段 890 仅由 `自检.运行器.ixx` 登记 | 当前专项 import、阶段回调登记、结果变量和最终汇总实际位于禁止文件 `海中鱼巣/入口.cpp`；`自检.运行器.ixx` 只提供通用运行器与阶段总成类型，没有默认专项登记表、外部登记钩子或可由新模块接入的聚合入口 | 不一致，命中退回条件 |

## 3. 具名接口漂移

当前完整自检的真实调用路径为：

```text
入口.cpp import 专项自检模块
-> 入口.cpp 创建 阶段化自检总成
-> 入口.cpp 调用 登记自检(最终回归, 阶段号, 回调)
-> 入口.cpp 运行最终回归阶段
-> 入口.cpp 把登记结果和专项结果并入最终具名布尔汇总
```

当前证据：

1. `海中鱼巣/入口.cpp:9806` 登记阶段 660 的运行期上下文专项。
2. `海中鱼巣/入口.cpp:9809` 登记阶段 690 的系统角色初始化专项。
3. `海中鱼巣/入口.cpp:9877` 与 `海中鱼巣/入口.cpp:9885` 把上述登记和专项结果并入最终汇总。
4. `海中鱼巣/自检.运行器.ixx` 没有现成的专项 import、默认登记函数或全局登记宿主。

#296 第 4 节只允许修改 `海中鱼巣/自检.运行器.ixx` 并明确禁止 `海中鱼巣/入口.cpp`。因此，即使四个新文件、装配和上下文全部实现，新 `自检.概念活动所有权.ixx` 也无法在现有调用图中被 import、登记为 890、执行并进入完整自检最终汇总。

执行角色不能通过以下方式自行收口：

1. 不能修改禁止文件 `入口.cpp` 增加 import、结果槽、890 回调和最终汇总。
2. 不能让 `阶段化自检总成` 构造函数隐式登记专项；该类型被多处合同自检创建，隐式全局副作用会改变既有运行器语义。
3. 不能用静态初始化、链接副作用或未调用模块伪造阶段登记与通过结果。
4. 不能把专项正文移入 `自检.运行器.ixx`；这违反生产 / 自检模块分离和计划的新文件所有权。

## 4. 退回请求

请设计角色修订 #296 三件套并明确以下二选一正式路径：

1. 定向允许 `海中鱼巣/入口.cpp` 只增加新自检模块 import、结果槽、阶段 890 一次登记和最终具名汇总；或
2. 先形成独立前置计划，把专项登记所有权从入口迁入正式自检聚合模块，再让 #296 消费该真实接口。

修订必须继续保持入口不承载专项正文、Release 零专项文本、生产不反向依赖自检，以及 #296 其余九个文件和结构边界不扩大。

## 5. 本轮变化与验证

本轮只新增本执行专属断点。未修改 C++、工程、计划、计划索引、任务队列、流程图、详细设计、中央项目记忆或主工作树；未运行构建、自检或阶段 890，因为代码实施在 S0 门禁前停止。

完成本断点后执行：

```text
python tools/check_specs.py --strict
git diff --check
git diff --name-only
```

任务分支提交推送后状态只能回报“接口漂移待设计修订”，不得回报“分支完成待集成”。

## 6. WB-296-02 / v0.2 第二次执行前复核

JY-401 已在 `origin/main@f72bf4e075f7db13e9fca6a38ec27aa3b7ad2b51` 发布正式派发。本窗口在登记的 `WT-296-R2` 接管，实际路径、分支、HEAD 与 clean 状态分别为：

```text
C:\Users\zhchh\.codex\worktrees\296-p0c-r2\海中鱼巢
codex/296-p0c-r2
3fa286bc4b65183f297f571f82434d0c6f3a62b7
worktree / index clean
```

旧 WB-296-01 的阶段 890 接线漂移已经由 JY-399 修订：v0.2 允许 `入口.cpp` 增加 Debug import、一个结果槽、阶段 890 一次登记和最终具名门禁，并禁止修改 `自检.运行器.ixx`。该旧漂移已关闭。

## 7. 第二次具名接口漂移

#296 v0.2 的 A10 和详细设计 17.2 固定：

```text
系统角色已初始化但概念活动材料未初始化时，运行期上下文::完整() 必须为假。
只有显式调用 初始化概念活动(概念活动初始化参数) 并完整读回后，完整() 才能为真。
三生命周期角色稳定键必须来自概念活动初始化参数。
```

当前至少三个已登记前序自检仍固定相反合同，且都不在 #296 允许文件中：

| 阶段 | 当前文件与调用点 | 当前正式断言 | 与 v0.2 的冲突 |
| --- | --- | --- | --- |
| 660 | `海中鱼巣/自检.运行期上下文.ixx:109-120` | 只调用 `初始化系统角色` 后要求 `上下文->完整()` 为真，并立即由宿主发布 | 新合同要求此时为假，阶段 660 将失败且宿主必须拒绝发布 |
| 680 | `海中鱼巣/自检.主装配事务.ixx:98-104` | `形成完整候选` 只初始化系统角色，再以 `候选->完整()` 决定是否返回候选 | 新合同下两个候选都为空，主装配事务专项无法进入后续验收 |
| 690 | `海中鱼巣/自检.系统角色初始化.ixx:441-447` | 只初始化系统角色后要求上下文完整，并要求宿主发布成功、租约有效 | 新合同要求发布前仍缺概念活动材料，阶段 690 将失败 |

阶段 890 的新自检模块不能修改 660 / 680 / 690 回调内部创建的局部上下文，也不能在这些前序阶段失败后补写通过。Release 虽不登记 890，但阶段 660 仍会运行，因此同一冲突也会破坏 Release 完整自检。

## 8. 禁止的伪收口

执行角色不能用以下方式绕过：

1. 修改三个禁止的既有自检文件，自行补 `初始化概念活动` 与三稳定键参数。
2. 让 `初始化系统角色` 隐式推导 `+22 / +23 / +24` 或其它生命周期键并自动初始化活动；这违反显式 `概念活动初始化参数` 和稳定键来源合同。
3. 让 `运行期上下文::完整()` 在“尚未调用概念活动初始化”时继续返回真；这直接违反 A10 和 P0-C 先于 P0-A 的发布门禁。
4. 按域编号、阶段号或自检对象形状增加测试专用生产分支。
5. 让阶段 890、静态初始化或链接副作用反向修改前序自检局部夹具。

## 9. 第二次退回请求

请设计角色同步修订流程图、详细设计、#296 和 WB-296 后继批次，定向裁决现有 660 / 680 / 690 自检迁移。建议把以下三个文件加入允许范围，仅做新完整性前置的机械适配：

```text
海中鱼巣/自检.运行期上下文.ixx
海中鱼巣/自检.主装配事务.ixx
海中鱼巣/自检.系统角色初始化.ixx
```

修订必须为每个既有夹具提供显式、互不冲突、非零的三生命周期稳定键，固定调用顺序为：

```text
初始化系统角色
-> 验证此时完整() 为假且宿主拒绝发布
-> 显式初始化概念活动
-> 验证完整() 为真
-> 才执行首次发布 / 租约回归
```

不得改自检原有系统角色业务语义、并发规模或其它阶段结果；不得把自检参数提升为生产配置。若设计不允许修改这些回归文件，则必须给出不违反 A10、显式初始化和稳定键来源合同的正式替代接口。

## 10. 第二次执行变化与验证边界

本次只修改本执行专属断点。未修改 C++、工程、入口、自检运行器、流程图、详细设计、计划、索引、队列或中央治理；未构建、未运行，因为冲突在 S0 静态逐调用点复核中已经成立。

本次只执行：

```text
python tools/check_specs.py --strict
git diff --check
git diff --name-only
```

任务提交推送后状态仍只能回报“接口漂移待设计修订”，不得回报“分支完成待集成”或“阶段 890 已登记”。

## 11. WB-296-03 / v0.3 接管与阶段证据

JY-405 已在 `origin/main@1a8093ba107c24badba50af6dba5eba5c01c9077` 发布正式派发。本窗口在登记的 `WT-296-R3` 接管，实际状态为：

```text
路径：C:\Users\zhchh\.codex\worktrees\296-p0c-r3\海中鱼巢
分支：codex/296-p0c-r3
HEAD / 冻结基线：d18a1047df5fa3ab5202451af3b9cc8aef90b1c3
upstream：none
接管 dirty state：worktree / index clean
```

本轮严格按 v0.3 状态机推进，形成以下可重复证据：

1. S0 已逐项复核状态规格、单会话写入、系统角色四根、660 / 680 / 690 和入口四个接线点；未发现此前两项漂移复发。
2. S1 曾建立稳定数据头和两个生产真模块固定骨架，并登记工程；Debug x64 Rebuild 与 Release x64 Rebuild 均为 0 警告、0 错误。
3. S2 曾在新数据操作内接入 `状态动态数据操作::写入抽象状态_会话`、四条 `概念生命周期` 关系、会话内读回和一次提交；新服务只持私有活动状态。S2 后 Debug 增量构建为 0 警告、0 错误。
4. S3 把新服务接入 `装配.运行期业务` 时才触发下述 IFC 冲突；因此目标模块缺失或状态规格并非漂移来源。
5. 失败草案已完整撤回，当前代码、工程和三个新文件相对冻结基线均为零变化；只保留本执行专属断点。

## 12. 第三次具名接口漂移：同名根类别无法在装配 IFC 共存

v0.3 同时固定：

```text
概念活动状态.数据.h 必须复用 概念图算法.h 唯一拥有的 概念根类别 / 概念签名材料。
数据操作.概念活动 不得 import 数据操作.概念图结构。
运行期业务装配必须同时保留既有概念图结构对象并接入新概念活动服务。
```

当前真实接口却存在两个不同实体、同一未限定名称：

| 所有者 | 实体 | 底层类型 | 到达装配的路径 |
| --- | --- | --- | --- |
| `领域/概念图算法.h` | `概念根类别` | `std::uint32_t` | 新 `概念活动状态.数据.h` -> 新数据操作 / 服务 |
| `领域/数据操作.概念图结构.ixx:28` | `概念根类别` | `std::uint8_t` | 既有 `运行期业务装配` 直接 import 旧概念图结构数据操作 / 服务 |

新数据操作本身没有 import 旧概念图结构模块，S1 / S2 也能独立编译；但 `运行期业务装配` 是两条生产依赖第一次汇合的位置。直接让装配看见新活动数据类型时，MSVC 报：

```text
error C1117: 导入模块“海中鱼巣.领域.数据操作.概念图结构”时出现无法恢复的错误：
已定义符号“概念根类别”。
```

尝试仅用模板 / 推导返回类型避免装配重复 `#include` 后，两个不同宽度的同名实体仍经 IFC 同时到达，MSVC 在 `概念活动材料::完整()` 的规范根类别数组处报：

```text
error C4789: 缓冲区“预期类别”(大小为 8 字节)将溢出；将写入 12 字节。
```

该错误说明问题不是缺一个 include，也不能通过隐藏公开签名文本解决：只要装配同时拥有旧概念结构服务和包含规范签名的活动服务，两个同名不同宽度实体就会进入同一模块依赖图。继续实现会迫使执行角色修改 #296 当前禁止的旧概念结构模块，或放弃 `概念图算法.h` 的唯一签名所有权；两者都命中退回条件。

## 13. 最小设计修订请求

建议设计角色保留 #296 编号和原子初始化合同，先定向消除旧模块内部同名：

```text
海中鱼巣/领域/数据操作.概念图结构.ixx
海中鱼巣/领域/服务.概念图结构.ixx
海中鱼巣/领域/自检.概念图结构分层.ixx
```

最小裁决建议为把旧模块私有语义枚举机械改名为 `概念结构根类别`，底层值、字段语义、写入结构、公开业务行为和专项验收均保持不变；不得把它转换、比较或合并为 `概念图算法.h::概念根类别`，也不得借机修改旧概念图 ABI 以外的行为。当前 `rg` 证明旧名称在模块化生产 / 自检面的引用只落于上述三文件；装配本身不直接引用该枚举。

修订后的执行前门禁应新增：

1. 全仓不再存在第二个 `enum class 概念根类别` 定义。
2. 旧概念结构专项全量保持通过，关系 9 / 10 / 11 的写入和读回语义不变。
3. 新活动数据头仍直接复用 `概念图算法.h`，新数据操作仍零 import `数据操作.概念图结构`。
4. 装配同时 import 两条生产依赖时 Debug / Release 均能正式生成 IFC。

若不允许机械重命名上述三文件，则必须新增正式隔离模块并调整装配所有权，使旧概念结构 IFC 与规范签名实体不在同一导入图汇合；该路径范围更大，不建议由执行角色临时发明。

## 14. 第三次退回验证与声明边界

本轮不保留 C++、工程或新文件草案，不运行完整自检，也不登记阶段 890。断点提交前只执行：

```text
python tools/check_specs.py --strict
git diff --check
git diff --name-only
```

任务分支提交推送后状态只能为“第三次接口漂移待设计修订”；不得声明 #296 分支完成、概念活动已实现或 660 / 680 / 690 已迁移。

## 15. WB-296-04 / v0.4 接管与 S1 实证

JY-409 已在 `origin/main@a0e6c85cccb106f32a3a3745f2b36d81a4ff06b9` 发布正式派发。本窗口在登记的 `WT-296-R4` 接管，实际状态为：

```text
路径：C:\Users\zhchh\.codex\worktrees\296-p0c-r4\海中鱼巢
分支：codex/296-p0c-r4
HEAD / 冻结基线：038ecad93fecef358263846cd68c1b536224734e
upstream：none
接管 dirty state：worktree / index clean
```

S0 复核确认状态规格、单会话写入、系统角色四根、660 / 680 / 690 和入口四点仍与 v0.4 一致。按 S1 只在三个允许文件机械执行 `概念根类别 -> 概念结构根类别` 后，精确改动范围为三文件，全仓只剩 `概念图算法.h` 声明规范 `enum class 概念根类别`；但首次 Debug x64 Rebuild 在旧专项之外的第四个显式调用点稳定失败。

## 16. 第四次具名接口漂移：运行期组合分层仍消费旧类型

实际调用点为：

```text
海中鱼巣/自检.运行期组合分层.ixx:31
import 海中鱼巣.领域.数据操作.概念图结构;

海中鱼巣/自检.运行期组合分层.ixx:199
关系类型::概念上下位, 概念根类别::存在,
```

三文件机械改名后，MSVC 在该调用点返回：

```text
error C2653: “概念根类别”: 不是类或命名空间名称
error C2065: “存在”: 未声明的标识符
```

这证明 JY-407 的“外围导入者不直接命名旧类型”和 #296 v0.4 的“三文件显式引用闭合”事实不完整。该文件通过旧数据操作模块构造 `发布概念结构请求`，其 `概念根类别::存在` 明确属于旧创建型概念结构枚举，而不是 `概念图算法.h` 的规范签名枚举；改名必须同步此调用点。当前文件不在 v0.4 的 16 项允许范围内，执行角色不得越权修改。

## 17. 第四次退回请求

请设计角色保留 #296 的原子活动合同并定向修订三件套：

1. 把 `海中鱼巣/自检.运行期组合分层.ixx` 加入允许范围，文件总数由 16 调整为 17。
2. 该文件只允许把第 199 行旧创建型请求中的 `概念根类别::存在` 机械同步为 `概念结构根类别::存在`；模块 import、阶段 770、场景、并发和其它断言全部保持不变。
3. S1 静态门禁改为：旧类型新名称只允许显式出现在原三文件和此第四消费文件；规范 `概念根类别` 仍由 `概念图算法.h` 唯一声明。
4. S1 验证必须覆盖 Debug / Release x64 Rebuild、完整自检、阶段 755 和阶段 770，再进入新活动模块骨架。

不得把第四调用点改用规范 `概念根类别`、增加别名 / 宏、在装配层做类型转换，或扩大到运行期组合分层的其它业务变化。

## 18. 第四次退回验证与声明边界

失败后已把三个旧概念结构文件完整恢复到冻结基线；其 worktree blob hash 均与 HEAD 相同。当前 C++ / 工程零差异，只保留本执行专属断点。本轮未运行 Release Rebuild、完整自检或阶段 890，因为 S1 Debug Rebuild 已按计划命中第四显式调用点退回条件。

断点提交前只执行：

```text
python tools/check_specs.py --strict
git diff --check
git diff --name-only
```

任务分支提交推送后状态只能为“第四次接口漂移待设计修订”；不得声明 #296 分支完成、阶段 755 / 770 已验证或概念活动已实现。

## 19. WB-296-05 / v0.5 接管与范围

JY-413 已在 `origin/main@5148e7f9398811a88f7f3293b777fd1ee7f61ac9` 发布正式派发。本窗口在登记的 `WT-296-R5` 接管，实际状态为：

```text
路径：C:\Users\zhchh\.codex\worktrees\296-p0c-r5\海中鱼巢
分支：codex/296-p0c-r5
HEAD / 冻结基线：292efeeab734312444d91ef9e265cd736cbf8c73
接管 dirty state：worktree / index clean
```

S0 逐项复核确认 JY-411 的第四调用点裁决已纳入 v0.5：旧创建型枚举只在三个所有者文件和 `自检.运行期组合分层.ixx` 一处调用点机械改名；状态会话写入、系统角色四根、660 / 680 / 690 和入口四点合同均与计划一致，未发现第五次接口漂移。

本轮最终差异精确为计划允许的 17 份文件：16 份代码 / 工程 / 自检文件，加本执行专属断点记录；未修改计划、计划索引、任务队列、流程图、详细设计、中央项目记忆、四仓库、传统状态服务或自检运行器。

## 20. S1-S5 生产实现

1. 把旧概念结构模块内部枚举机械改名为 `概念结构根类别`，同步冻结的四个显式文件；全仓规范 `概念根类别` 仍由 `概念图算法.h` 唯一声明，旧关系 9 / 10 / 11 语义不变。
2. 新建 `概念活动状态.数据.h`，固定参数版本、三个生命周期稳定键、活跃 / 冷却 / 退役角色、四根规范签名、生命周期关系和确定性重建视图的值式合同。
3. 新建 `数据操作.概念活动.ixx`，在一次不透明结构写入会话内完成三抽象状态、四条 `概念生命周期` 关系、会话内完整读回与最后一次提交；预存活动结构归为内部不一致，不接管既有所有权。
4. 新建 `服务.概念活动.ixx`，由服务私有持有唯一活动材料；同参数只做确定性幂等读回，异参数拒绝且不改变结构。
5. `运行期业务装配` 和 `运行期上下文` 接入同域数据操作与唯一服务。系统角色已完成但活动未完成时，上下文仍不完整且宿主拒绝发布；只有显式活动初始化与重建复核完成后才可首次发布。
6. 660 / 680 / 690 三个既有夹具按 v0.5 显式增加活动参数与初始化，不缩减既有主装配 100 轮并发规模，不改变系统角色业务断言。

## 21. S6 独立验收与阶段 890

新增 `自检.概念活动所有权.ixx`，以 18 项验收覆盖：

1. 版本、枚举值、零键 / 重复键 / 错版本入口拒绝与零写入。
2. 仅系统角色完成时上下文不完整、宿主发布拒绝。
3. 首次活动初始化精确新增 3 节点、4 关系、3 主键，活动版本固定为 1。
4. 三状态角色、四根规范签名、生命周期关系审计、同仓库归属与确定性重建完整。
5. 同参数幂等无增长、异参数冲突无增长、首次发布与租约读回。
6. 预存活动身份拒绝接管、材料保持空、上下文不可发布。
7. 两线程同参数并发只有一次结构改变，双方读回同一材料且结构增量仍为 3 / 4 / 3。

入口只增加 Debug import、结果槽、阶段 890 一次登记和最终具名汇总。Release 二进制对 `PERSIST-S1-P0-C` 和专项描述的匹配数均为 0。

## 22. S7 验证证据与交接状态

最终验证均在任务 worktree 的冻结实现上重新执行：

```text
Debug x64 Rebuild：0 警告 / 0 错误
Debug 完整自检：退出 0
Release x64 Rebuild：0 警告 / 0 错误
Release 完整自检：退出 0
Debug 连续完整自检：20 / 20 退出 0，总耗时 340.9 秒
连续 20 轮阶段 890 日志命中：20 / 20
RUNTIME-TXN-S3：A1-A16 通过，100 轮竞争证据保持
INIT-MOD-S1：A1-A22 通过
python tools/check_specs.py --strict：通过
git diff --check：通过
Release 专项文本匹配：0
```

本任务已达到“分支完成待集成”。下一步只允许精确暂存上述 17 份文件、提交并非强制推送到同名远端 `codex/296-p0c-r5`；不得由任务分支直接推送 `origin/main`，不得在设计角色归档前宣称 #296 正式完成。
