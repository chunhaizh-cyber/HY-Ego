# THREAD-S5 外设采样材料线程壳代码实施 Codex 断点清单

日期：2026-07-10

状态：已完成 / 已验证 / 已归档计划 / 代码已实现 / 未迁移旧函数

## 1. 输入入口

```text
计划/20260710_THREAD-S5_外设采样材料线程壳代码实施切片_v0.1.md
流程图/20260709_运行宿主与多线程消息队列流程图_v0.1.md
规范/详细设计/运行宿主与多线程消息队列详细设计.md
实施记录/20260706_FSX_控制面板SQLD455体素外设排除项汇总记录.md
```

计划已确认并登记可执行队列 #160。第一轮只允许模拟或空采样材料线程壳，不接 D455、体素、真实驱动、真实外设 API、外设动作、SQL、ADO、WebView 或真实控制面板。

## 2. 修改文件

```text
海中鱼巣/核心/运行宿主.h
海中鱼巣/入口.cpp
计划/已完成计划/20260710_THREAD-S5_外设采样材料线程壳代码实施切片_v0.1.md
计划/计划索引.md
项目记忆/Codex任务队列.md
项目记忆/当前状态.md
项目记忆/待确认问题.md
项目记忆/决策记录.md
项目记忆/验收记录.md
```

未修改禁止文件；未新增独立外设服务文件；未接旧鱼巢源码。

## 3. 核心处理

```text
新增 外设采样材料线程壳状态。
新增 外设采样材料线程壳拒绝原因。
新增 外设采样材料线程壳操作结果。
新增 外设采样材料线程壳消费结果。
新增 外设采样材料线程壳。
默认入口新增 THREAD-S5-A1 到 THREAD-S5-A5 自检。
```

外设采样材料线程壳只承载外部材料消息：

```text
启动和停止线程壳。
发送外部材料消息。
消费外部材料一次。
丢弃过期外部材料一次。
请求停止并发送停止消息。
收口等待。
```

所有结果材料显式保持：

```text
不写业务事实。
不写世界事实。
不裸写仓库。
不裁决机器事实。
不接入真实外设。
不接入 D455。
不接入体素。
不触发外设动作。
```

## 4. 结构承载

```text
运行消息类型::外部材料
运行线程角色::外设采样材料
有界运行消息队列
外设采样材料线程壳
外设采样材料线程壳操作结果
外设采样材料线程壳消费结果
```

外部材料消息携带来源角色、发生时间戳、批次号、材料摘要和只读材料说明。空采样材料以材料摘要为 0 的外部材料消息表达；过期模拟材料只在外部材料队列内丢弃，不进入存在、场景、状态、动态、需求、任务、方法或因果结构。

## 5. 输出变化

默认入口新增输出：

```text
THREAD-S5 外设采样材料线程壳第一轮: 通过
THREAD-S5-A1 外设采样材料线程壳可启动和停止: 通过
THREAD-S5-A2 空采样材料可入队: 通过
THREAD-S5-A3 过期模拟材料可丢弃: 通过
THREAD-S5-A4 材料不直接写世界事实: 通过
THREAD-S5-A5 禁止 D455/体素/真实外设接入: 通过
```

## 6. 验证结果

```powershell
msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m
```

结果：通过，0 警告，0 错误。

```powershell
.\x64\Debug\海中鱼巣.exe
```

结果：返回 0，THREAD-S5-A1 到 THREAD-S5-A5 均输出通过。

```powershell
git diff --check
```

结果：通过，仅有 CRLF 提示。

```powershell
python .\tools\check_specs.py
```

结果：`海中鱼巣规范治理检查: 未发现问题`。

```powershell
rg -n "D455|双目|相机|体素|RealSense|外设控制|真实采样|真实外设|SQL|ADO|WebView" "海中鱼巣\核心\运行宿主.h" "海中鱼巣\核心\运行消息队列.h" "海中鱼巣\入口.cpp"
```

结果：只命中 `运行宿主.h` 中 THREAD-S5 禁止能力布尔标志、`入口.cpp` 中 THREAD-S5-A5 禁止能力断言和既有 XS-A9 / THREAD-S4 人读排除标签；未发现真实外设、D455、体素、SQL、ADO 或 WebView 接入路径。

```powershell
rg -n "std::async|std::future|std::promise|condition_variable|写世界事实 = true|接入真实外设 = true|接入D455 = true|接入体素 = true|触发外设动作 = true|自我循环|自我苏醒" "海中鱼巣\核心\运行宿主.h" "海中鱼巣\核心\运行消息队列.h" "海中鱼巣\入口.cpp"
```

结果：无命中。

## 7. 完成边界

本次只证明：

```text
外设采样材料线程壳第一轮已实现。
外部材料消息可入队和消费。
空采样材料只作为消息材料。
过期模拟材料可在队列材料层丢弃。
满载拒绝不写业务事实或世界事实。
停止消息和 join 收口通过。
```

不得声明：

```text
D455 已接入。
双目相机已接入。
体素已接入。
真实驱动或真实外设 API 已接入。
外设动作已实现。
真实外设样本通过。
自我循环或自我苏醒已完成。
旧项目能力已迁移完成。
```
