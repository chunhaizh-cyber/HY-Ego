# SAFETY-P1-PROVIDER 节点直接身份结构纯参与者事务代码实施记录 v0.1

## 计划与 Git 身份

- 计划：`#381 / DQ-273 / SAFETY-P1-PROVIDER v0.1`
- 提供合同：`CORE-C1 / v1`
- plan blob：`ce03681267c6051819a98e0e70354057b647f590`
- 计划索引 blob：`f57f19664ec05fc32b4c72ec52054d5ed6b05c84`
- 计划段起点：`ff1d9bc893867acf8a9b3902e860fee85a524e14`
- 分支：`main`
- 结果边界：三代码文件源码候选与候选级静态检查形成；不代表 #377、安全维护、工程接线、构建、运行或生产能力完成。

## S0

- `main == origin/main == ff1d9bc893867acf8a9b3902e860fee85a524e14`，ahead/behind 为 `0/0`，Git index clean。
- #381 在计划索引中登记为 `可执行`，plan blob 精确匹配，无未合同化实现依赖。
- #377 设计漂移 WIP `bab41cae4223289e1297a5b7850f79b5b967fb06` 位于当前祖先链中；本计划未修改其七个文件。
- 普通执行器入口、公开 `请求提交()`、普通 `完成确认()`、参与者基类、事务域许可、准备/确认/逆序撤销/最后发布接口与 `CORE-C1/v1` 一致。
- F-S109—F-S111、逐函数逐节点分析、功能确认、详细设计 v0.1 和知识图谱均存在，未决项为空。
- 工作区另有流程图与知识图谱维护智能体拥有的 `流程图/**` 并行现场；与本计划文件、语义及验证资源无重叠，本计划未修改、暂存或提交这些文件。

## 变更文件

1. `海中鱼巣/核心/会话.节点直接身份结构写入.ixx`
   - 新增私有 F-S110 `请求仅参与者提交()`。
   - 新增私有 F-S111 `完成仅参与者确认()`。
   - 新增独立的 `仅参与者事务_` 阶段标记；普通入口的空核心写集拒绝保持不变。
2. `海中鱼巣/核心/执行器.节点直接身份结构写入.ixx`
   - 新增 F-S109 `执行仅参与者事务(span)`。
   - 非空唯一参与者组在同一独占许可内顺序准备、确认、最后发布；失败复用现行逆序撤销和隔离收口。
   - 全幂等返回 `幂等读回`，混合或新候选返回 `已提交`；诊断数值全零。
3. `海中鱼巣/核心/自检.节点直接身份结构写入.ixx`
   - 追加 CORE-A01—A12 隔离源码覆盖。

## 验证

- F-S109—F-S111 完整签名、参数和返回命中。
- 新入口不接 `std::function`、会话回调或业务 DTO。
- 空组、空指针、重复地址与普通入口空核心写集拒绝均有源码覆盖。
- CORE-A01—A12 编号 12/12 命中；源码覆盖单/双参与者、全幂等、混合幂等、准备/确认故障逆序撤销、发布可见性、许可/资源/内部错误和零核心候选。
- 差异白名单为三代码文件与本记录；未构建、未运行程序或二进制自检。
- 提交前执行 `git diff --check` 和 `git diff --cached --check`。

## 残余风险

- 本计划按合同只做候选级静态检查，三代码文件尚未通过工程编译和运行期自检。
- #377 v0.6 仍须由计划支撑复核本结果提交后另行恢复为 `可执行`；本提交不自行解除索引门禁。
