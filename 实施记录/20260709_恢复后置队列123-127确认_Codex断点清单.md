# 恢复后置队列 #123-#127 确认 Codex 断点清单

日期：2026-07-09

## 1. 输入入口

用户确认：

```text
确认恢复 #123-#127，先从 #123 开始
```

本窗口仍按计划设计和确认窗口处理。本次只恢复已经确认并已登记可执行队列的后置项状态，不在本窗口消费队列，不执行 #123，不修改 C++。

## 2. 恢复范围

恢复以下已确认计划：

| 队列号 | 计划 | 恢复后状态 |
| --- | --- | --- |
| #123 | `计划/20260707_FS06_方法候选召回与条件结果对拆分设计专项_v0.1.md` | 当前优先执行入口 |
| #124 | `计划/20260707_FS07_动作执行桥与本能动作治理拆分设计专项_v0.1.md` | 等待 #123 完成后排队 |
| #125 | `计划/20260707_FS08_非权威缓存二级材料拆分设计专项_v0.1.md` | 等待 #124 完成后排队 |
| #126 | `计划/20260707_FS09_事件日志详细设计确认后实施门禁计划_v0.1.md` | 等待 #125 完成后排队 |
| #127 | `计划/20260707_FS03_特征值序列化恢复边界专项_v0.1.md` | 等待 #126 完成后排队 |

## 3. 核心处理

本次同步以下治理文件：

```text
项目记忆/Codex任务队列.md
项目记忆/当前状态.md
项目记忆/决策记录.md
计划/计划索引.md
```

状态从“暂停 / 后置 / 用户要求其他计划先暂停”收束为：

```text
#123：已恢复 / 当前优先执行入口。
#124-#127：已恢复 / 顺序排队。
```

## 4. 明确排除项

```text
不执行 #123。
不消费 Codex 任务队列。
不生成新的代码实施许可。
不修改 C++。
不运行构建。
不迁移旧函数体。
不接控制面板 / SQL / ADO / WebView / D455 / 体素 / 外设。
```

## 5. 下一步

执行窗口下一步应从 #123 开始：

```text
执行 FS-06 方法候选召回与条件结果对拆分设计专项。
```

#123 的完成边界仍以计划文件为准：只生成方法候选召回、条件 / 结果对、来源任务和方法关系后续详细设计或子计划，不进入 C++ 实施。

## 6. 完成声明边界

本次只证明：

```text
#123-#127 已解除暂停并恢复排队。
#123 已被设为下一执行入口。
```

本次不能证明：

```text
#123 已执行。
方法候选召回详细设计已生成。
条件判定或方法学习已实现。
C++ 已修改或验证。
旧项目方法能力已迁移。
```

## 7. 验证方式

文档治理验证：

```powershell
git diff --check
python .\tools\check_specs.py
```

执行结果：

```text
git diff --check：通过；仅出现 LF / CRLF 工作区提示，无空白错误。
python .\tools\check_specs.py：通过，输出“海中鱼巣规范治理检查: 未发现问题”。
```
