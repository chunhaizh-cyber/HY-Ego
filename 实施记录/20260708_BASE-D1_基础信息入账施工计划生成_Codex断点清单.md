# BASE-D1 基础信息入账施工计划生成 Codex 断点清单

日期：2026-07-08
状态：已生成待确认施工计划 / 未入队 / 未改 C++ / 未迁移旧函数

## 1. 输入入口

用户确认《基础信息入账详细设计》（BASE-D1），并限定：

```text
可作为后续施工计划依据。
不自动生成施工计划之外的执行许可。
不登记可执行队列。
不允许直接修改 C++。
后续施工计划必须逐项覆盖 BASE-GAP-01 到 BASE-GAP-06。
后续施工计划必须明确允许文件、禁止文件、失败收口策略、状态 / 动态服务边界、核心仓库事务边界待确认问题和 BASE-A1 到 BASE-A12 验收矩阵。
不迁移旧函数体，不新增旧主信息字段，不接控制面板 / SQL / ADO / WebView / D455 / 体素 / 外设。
```

## 2. 核心处理

```text
读取 AGENTS.md、计划索引、规则总纲、规则迁移清单、Codex 任务队列、待确认问题和 BASE-D1 详细设计。
生成 BASE-D1 基础信息入账施工计划候选。
同步 BASE-D1 已确认但不入队、不改 C++ 的状态。
补充计划索引和项目记忆中的待确认计划登记。
```

## 3. 结构承载

```text
规范/详细设计/基础信息入账详细设计.md
计划/待确认计划/20260708_BASE-D1_基础信息入账施工计划_v0.1.md
计划/计划索引.md
项目记忆/当前状态.md
项目记忆/决策记录.md
项目记忆/待确认问题.md
实施记录/20260708_BASE-D1_基础信息入账施工计划生成_Codex断点清单.md
```

## 4. 输出变化

```text
BASE-D1 详细设计状态更新为已确认。
新增 BASE-D1 施工计划候选。
新增 QR-101 待确认问题。
新增 JY-086 决策记录。
计划索引新增 BASE-D1 施工计划候选登记。
未修改 Codex 任务队列。
未修改 C++。
```

## 5. 调用路径

```text
用户确认 BASE-D1
-> 设计确认窗口生成施工计划候选
-> 计划/待确认计划
-> 计划索引待确认登记
-> 项目记忆状态 / 决策 / 待确认问题同步
```

## 6. 验证方式

```powershell
git diff --check
python .\tools\check_specs.py
```

本断点清单生成时未运行 C++ 构建，未运行程序入口，未验证入口前置拒绝。

## 7. 完成声明边界

可以声明：

```text
BASE-D1 基础信息入账施工计划候选已生成。
BASE-D1 详细设计已确认，可作为施工计划依据。
本次未登记可执行队列，未修改 C++。
```

不能声明：

```text
BASE-D1 施工计划已确认。
BASE-D1 已登记可执行队列。
基础信息入账代码已实现。
Debug x64 构建已运行。
入口前置拒绝已通过。
基础信息服务分层完整完成。
旧基础信息能力已迁移。
控制面板、SQL、ADO、WebView、D455、体素或外设已接入。
```
