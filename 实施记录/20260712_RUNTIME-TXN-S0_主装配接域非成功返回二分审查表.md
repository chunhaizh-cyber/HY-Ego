# RUNTIME-TXN-S0 主装配接域非成功返回二分审查表

标准检查字段：节点、代码位置、返回条件、输入语境、是否上游保证有效、结构是否变化、设计是否允许、二分口径。下表按调用路径聚合代码位置，以当前类别、正式二分和后继要求共同表达其余字段。

| 路径 | 当前类别 | 正式二分 | 后继要求 |
| --- | --- | --- | --- |
| 无效参数尚未写结构 | 写前拒绝 | 逻辑内返回 | 不创建补偿事务 |
| 候选为空、预算不足、非权威缓存丢弃 | 允许分支 | 逻辑内返回 | 权威结构不变 |
| 世界树 / 语素本轮创建中途失败 | 已写未发布结构 | 追根因后精确补偿 | 不把补偿成功改写为业务成功 |
| 语素服务最终发布前失败 | 已写未发布结构 | 追根因后精确逆序补偿 | 保持服务所有权和同一许可 |
| 概念关系写后读回异常 | 内部不一致 | 追根因解决 | 记录异常残留，不静默丢弃 |
| 固定点提交后旧关系删除异常 | 已发布结构异常 | 追根因解决 | 不允许部分提交后普通返回 |
| 结构域、纪元、令牌或线程不匹配 | 权限前置不满足 | 逻辑内拒绝 | 写前结构不变 |
| 恢复屏障未完成或候选身份漂移 | 发布前置不满足 | 逻辑内拒绝 | 不发布运行期上下文 |
| 唯一上下文已发布后再次发布 | 所有权冲突 | 逻辑内拒绝并诊断 | 不覆盖、不创建第二宿主 |

## 结论

当前代码的多处补偿位于“前置满足并已写入”之后，不能继续被笼统描述为普通失败清理。#248 必须保留追根因语义，并仅统一许可与所有权，不得用空读、日志或扫描把异常降级。
