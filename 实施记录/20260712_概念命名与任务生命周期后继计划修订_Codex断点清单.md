# 概念命名与任务生命周期后继计划修订 Codex 断点清单

日期：2026-07-12

状态：已按 JY-264 完成三件套修订与正式登记 / #242、JY-263 已完成 / #213-#215、#221-#225 保持依赖门控 / 等待 #245、#243、#217 与前批 #212-#220 / 未改 C++ / 未构建 / 未运行程序

## 1. 输入入口

```text
AGENTS.md
规范/000_项目规则总纲.md
规范/001_规则迁移清单.md
规范/代码文件建立归属与模块命名规范.md
流程图/20260711_概念命名需求协议目标创建绑定结算流程图_v0.1.md/.html
规范/详细设计/概念命名需求协议目标创建绑定结算详细设计.md
流程图/20260711_任务生命周期筹办通用执行与结果结算流程图_v0.1.md/.html
规范/详细设计/任务生命周期筹办通用执行与结果结算详细设计.md
实施记录/20260712_概念命名与任务生命周期后继逻辑提取引用矩阵.md
实施记录/20260712_概念命名目标与安全删除后继计划修订_Codex断点清单.md
实施记录/20260712_SELFTEST-MIGRATION-B1A_线程协议阶段自检迁移代码实施_Codex断点清单.md
业务代码事实 b33c602
阶段化总成事实 c924bf2
#242 正式提交 387f2bd
初始文档登记基线 68ddae1
```

## 2. 修订对象

| 队列 | 原承载问题 | 修订结果 |
| --- | --- | --- |
| #213 | 没有固定统一生产路由、自检真模块与阶段顺序 | 新建 `路由.概念命名治理` 和 `自检.概念命名需求`，登记预期顺序 550 |
| #221 | 完整生命周期初态自检可能继续进入入口 | 新建 `自检.任务生命周期初态`，登记预期顺序 560 |
| #222 | 合法迁移、终态封口和并发自检没有独立承载 | 新建 `自检.任务生命周期迁移`，登记预期顺序 570 |
| #223 | 筹办召回、精确选择和动作桥自检没有独立承载 | 新建 `自检.任务筹办选择`，登记预期顺序 580 |
| #214 | 旧名平行路由会分裂命名治理所有权 | 只扩展 #213 统一路由，新建 `自检.概念名称绑定`，登记预期顺序 590 |
| #224 | 旧计划使用无分类协议名和行为 `.h/.cpp` | 改为 `协议.任务执行请求`、`服务.任务执行` 生产真模块与 `自检.任务执行调度`，登记预期顺序 600 |
| #225 | 旧计划使用行为 `.h/.cpp` 和无分类路由名 | 改为 `服务.任务结果`、`路由.任务结果治理` 生产真模块与 `自检.任务结果结算`，登记预期顺序 610 |
| #215 | 结算可能另建平行路由并继续在入口验收 | 只扩展 #213 统一路由，新建 `自检.概念命名结算`，登记预期顺序 620 |

## 3. 逻辑提取结论

```text
概念命名：协议目标 -> 唯一需求 -> 任务筹办与选择 -> 名称绑定及动作动态 -> 通用任务结果 -> 需求正式结算。
任务生命周期：唯一当前态 -> 合法迁移 -> 双向授权与精确方法选择 -> 冻结请求及受控执行 -> 权威结果与独立结算。
```

两个流程通过 #214 的绑定回执和 #225 的通用结果契约衔接，但继续保留名称关系、动作动态、任务结果和需求结算四个权威边界。旧函数和旧文件名不是迁移单位；迁移单位是流程图中完整的输入、准入、结构承载、读回和失败收口。

## 4. 三件套一致性

| 路线 | 流程图范围 | 详细设计范围 | 计划范围 | 结论 |
| --- | --- | --- | --- | --- |
| #213 | 概念命名 S3 | CN-D17 与 S3 承载 | 唯一命名需求创建 / 复用 | 一致 |
| #221-#223 | 任务 S1-S3 | TL-D27-TL-D29 | 初态、迁移、筹办选择 | 一致 |
| #214 | 概念命名 S4 | CN-D18 与 S4 承载 | 名称绑定和动作动态回执 | 一致 |
| #224 | 任务 S4 | TL-D30 与 S4 承载 | 冻结请求、调度、执行、非权威回执 | 一致 |
| #225 | 任务 S5 | TL-D31 与 S5 承载 | 结果、完成、独立需求结算 | 一致 |
| #215 | 概念命名 S5 | CN-D19 与 S5 承载 | 命名结果和需求正式结算 | 一致 |

两套流程图的 MD 与 HTML 同步修订。八份计划均列明已验证事实、假定契约、前置计划、预期产物、允许文件、禁止文件、入口拒绝、失败收口、实际接口复核、漂移退回、验证方式与完成边界。

## 5. 文件归属与排队

```text
#213 / 550
-> #221 / 560
-> #222 / 570
-> #223 / 580
-> #214 / 590
-> #224 / 600
-> #225 / 610
-> #215 / 620
```

#221 不以 #213 为业务依赖，#224 不以 #214 为业务依赖；两条边只负责本批阶段注册和共享文件交付顺序。其余业务依赖按各计划和逻辑提取矩阵执行。所有新代码文件均按真模块分类，生产与自检依赖单向，入口只作最小登记。

## 6. 执行前门控

```text
读取每个前置计划的正式提交和实施记录。
读取实际 export / import、函数签名、关系 / 版本材料和失败收口。
读取中央运行器阶段注册表，确认 550-620 依次可用。
逐项比对计划的假定接口契约与实际正式产物。
一致才修改代码；不一致则停止并退回设计窗口。
```

禁止执行窗口自行换阶段号、改模块名、扩允许文件、复制固定安全根夹具、把入口变成第二实现或让线程直接写机器事实。

## 7. 六项完成声明

| 项 | 内容 |
| --- | --- |
| 输入入口 | 两套正式流程图 / 详细设计、CODE-FILE-01、业务代码 `b33c602`、阶段化总成 `c924bf2`、#242 正式提交 `387f2bd` 与前批 JY-262 文档基线。 |
| 核心处理 | 按逻辑流程提取概念命名和任务生命周期闭环，裁决真实业务依赖、批次顺序、生产 / 自检承载和失败二分。 |
| 结构承载 | 两套流程图 MD+HTML、两份详细设计、八份修订计划、逻辑提取矩阵和本断点清单；不新增机器结构。 |
| 输出变化 | #213-#215、#221-#225 已按最终回归阶段 550-620 和固定批次顺序重新登记为依赖门控待执行；共享计划索引、任务队列、项目记忆与逐能力台账已同步。 |
| 调用路径 | `正式逻辑 -> 当前事实 -> 文件归属 -> 依赖门控计划 -> 执行前实际接口复核 -> 执行窗口`。 |
| 验证方式 | MD / HTML Mermaid 一致性、计划字段与旧路径扫描、`git diff --check`、`tools/check_specs.py --strict`；本批不运行构建或程序。 |

## 8. 未完成边界

#242 / JY-263 已由独立实施记录证明完成。本记录不证明 #245、#243、#212-#215、#217-#225 已实施，不证明计划所述领域接口、阶段 550-620、自我循环、恢复或旧能力迁移完成；本批只完成三件套修订、正式登记、文档验证、提交和推送。
