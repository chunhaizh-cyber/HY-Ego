# 计划执行确认门禁修正 Codex 断点清单

日期：2026-07-06
状态：已完成 / 执行规则已修正 / 未改代码

## 1. 输入入口

用户要求：

```text
请修改计划执行逻辑，如“下一步已改为：建立任务系统规范和详细设计”这种本身是不需要我来确认的动作，需要我确认的是设计好后的方案在执行前的确认。
```

## 2. 本次范围

允许：

```text
修正 AGENTS.md、计划索引和项目记忆中的执行确认门禁。
明确文档治理动作默认直接执行。
明确设计方案进入代码实现、迁移落地或改变机器结构前才需要用户确认。
```

禁止：

```text
修改代码。
建立任务系统规范和详细设计正文。
迁移旧任务主信息字段。
宣称任务系统已完成。
```

## 3. 修订内容

```text
AGENTS.md 增加计划执行确认门禁硬规则。
计划索引新增“执行确认门禁”章节。
项目记忆新增 JY-031 决策和任务队列记录。
验收记录增加本次规则修正确认。
```

## 4. 验证方式

```powershell
rg -n "执行确认门禁|文档治理动作|建立任务系统规范和详细设计|设计方案完成后|进入代码实施" AGENTS.md 计划 项目记忆 实施记录
git diff --check
```

本切片不改代码，不运行构建和默认入口。

## 5. 输出变化

```text
后续计划索引中的“建立规范 / 详细设计 / 计划 / 验收项 / 只读扫描 / 断点清单 / 项目记忆同步”默认直接执行。
用户确认门禁后移到设计方案完成后、代码实施或迁移落地前。
任务系统规范和详细设计可作为下一步直接建立。
```

## 6. 禁止扩大声明

```text
本次只能声明计划执行确认门禁已修正。
不能声明任务系统规范已建立。
不能声明任务系统代码已实现。
不能声明旧任务字段已迁移。
```
