# FS-X 控制面板 SQL D455 体素外设排除项汇总 Codex 断点清单

日期：2026-07-06
状态：已完成 / 只读扫描 / 排除项汇总 / 未改代码 / 未迁移旧函数

## 1. 输入入口

计划索引当前执行项为 `HR-001-FM-SCAN`，上一闭合点为 FS-10 第一批显示层只读候选只读扫描，下一旧函数编号从 `OF-199` 开始。

## 2. 本次读取依据

```text
AGENTS.md
计划/计划索引.md
规范/000_项目规则总纲.md
规范/001_规则迁移清单.md
计划/20260706_函数迁移前置专项_v0.1.md
实施记录/20260706_逐函数只读扫描清单.md
```

## 3. 当前边界

```text
本轮只读汇总控制面板 / SQL / D455 / 体素 / 外设排除项。
不运行旧项目。
不修改旧项目文件。
不修改当前项目代码。
不接控制面板。
不接 SQL 实时投影。
不接 D455 / 体素 / 外设。
```

## 4. 本次只读扫描输出

```text
已生成：
实施记录/20260706_FSX_控制面板SQLD455体素外设排除项汇总记录.md

已登记旧函数编号：
OF-199 至 OF-206

覆盖边界：
旧控制面板快照 / HTML / JSON / 摘要渲染
旧 WebView2 窗口、相机窗口、自我场景窗口和截图
旧 SQL Server ADO 查询、命令、运行态投影和事件流水
旧 D455 相机采集和双目相机适配器
旧外设观察报告队列和 D455 外设线程
旧场景体素、视觉观察融合和局部体素候选
旧双目相机外设本能动作实现
```

## 5. 当前判断

1. FS-X 是排除项汇总，不是迁移候选清单。
2. OF-199 到 OF-206 全部标记为 `排除 / 后续专项`。
3. 控制面板、SQL、WebView2、D455、体素、外设线程、外设材料和外设本能动作均不得进入当前第一轮迁移。
4. 后续若恢复，必须先补规范、详细设计、计划、允许文件、禁止文件和验收方式。

## 6. 下一编号和下一步

下一旧函数编号：

```text
OF-207
```

下一步：

```text
FS-00 至 FS-X 第一轮只读扫描已覆盖。
后续应按逻辑迁移优先规则，继续生成已扫描函数族的逻辑迁移包归并表，优先从 FS-03 特征 / 特征值逻辑迁移包归并表开始。
```

## 7. 验证方式

本切片为文档和只读扫描切片，未运行构建、未运行当前项目入口、未运行旧项目程序。

收尾验证应覆盖：

```text
rg -n "FS-X|OF-199|OF-206|OF-207|控制面板 / SQL / D455 / 体素 / 外设" 实施记录 计划 项目记忆
git diff --check
```

## 8. 禁止扩大声明

```text
不得声明控制面板、SQL、WebView2、D455、体素或外设已接入。
不得声明旧外设本能动作已迁移。
不得声明显示、SQL、窗口、线程、截图、日志或外设材料可承载机器事实。
不得声明旧函数扫描全部完成后即可进入代码实施。
```
