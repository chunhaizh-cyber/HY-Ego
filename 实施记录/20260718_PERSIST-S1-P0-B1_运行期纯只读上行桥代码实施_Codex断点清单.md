# PERSIST-S1-P0-B1 运行期纯只读上行桥代码实施 Codex 断点清单

日期：2026-07-19

执行队列：#299

设计队列：DQ-191

计划版本：v0.4

批次：WB-P0B-01 / WT-299-R1

冻结代码基线：`33ae6bc3772dafa93a259de452bd509c739f10e8`

正式派发治理：`origin/main@a7a06ed6ed7eb9d10753ad7c319b99e8c2594aea`（JY-437 / E182，只读消费）

状态：分支完成待集成 / S0-S4 完成

## 1. 接管与范围

1. 任务 worktree 为 `C:\Users\zhchh\.codex\worktrees\299-p0b1-r1\海中鱼巢`，分支为 `codex/299-p0b1-r1`；接管时 HEAD 等于冻结代码基线，worktree / index clean，upstream 为空，远端同名分支不存在。
2. 已只读 fetch 并从 `origin/main@a7a06ed` 复核 JY-437 / E182 的 `已派发待执行回执`、计划索引、任务队列和并行工作树登记；未 merge、rebase 或 cherry-pick 中央治理，任务代码父提交保持 `33ae6bc`。
3. 本任务只修改计划第 3 节五个既有代码文件，并新增本实施记录；未修改入口、工程、运行器、计划、索引、任务队列、登记表、流程图、详细设计、B0、B3、#304 或其它后继文件。
4. 阶段 920 未登记；完成状态仅为“分支完成待集成”。

## 2. 生产桥改接

1. `上行桥.概念命名治理.ixx` 改为值式持有 `运行期只读查询适配器`，每条请求只调用一次 `查询概念命名`；桥仍只形成非权威治理消息，不创建需求、不绑定名称、不管理线程。
2. 概念命名桥先收口空租约产生的明确入口 / 许可拒绝，再对可读业务结果做目标句柄一致性复核；空租约因此逻辑拒绝为“只读查询不可用”，不伪造目标材料，也不误报内部不一致。
3. `任务管理上行桥.ixx` 改为值式持有同一查询适配器，每条回执只调用一次 `复核任务结果`；既有取出、顺序、停止、许可候选和非权威消息转换语义保持不变。
4. 两个生产桥不再 include / import 或持有概念图、语素、需求、任务、方法、状态、动态服务，也不包含仓库、接线、原始结构事务令牌、许可或结构写入会话。
5. 不存在旧服务兜底、双读、结果择优或写入路径；生产模块不依赖自检模块。

## 3. 既有自检迁移

1. `自检.概念命名治理.ixx` 使用隔离生产运行期租约构造查询适配器；正例消费同租约活动根，已有名称由同运行期业务入口形成，消失样本使用同运行期世界根表达“合法节点但不在当前活动四根”。
2. 空租约、活动版本漂移、已有名称、当前活动中未找到、过期和无效请求均保持零邮箱提交；桥接前后原自检仓库结构数量和活动版本守恒。
3. `自检.自我治理回执.ixx` 在模块内拥有隔离运行期夹具，形成真实执行中任务、方法选择、动作、结果状态与动态证据；有效回执可准入，错误方法 / 动作 / 场景 / 主体及版本漂移均按新值式合同拒绝，桥接前后两套权威结构守恒。
4. 篡改动态句柄节点号在当前查询合同中被精确识别为版本漂移，自检按 `只读查询版本漂移` 收口；未改变生产查询语义。
5. `自检.自我治理多轮.ixx` 只同步 B1 任务管理上行桥构造与隔离只读运行期启动证据；既有自我治理领域路由的构造、调用和生产语义零修改。

## 4. A01-A15

| 验收 | 结果 | 证据 |
| --- | --- | --- |
| A01 | 通过 | #298 查询请求、结果、适配器和租约签名逐项一致 |
| A02 | 通过 | 查询适配器保持值式租约门面；本任务未引入仓库、接线、令牌、许可、会话或裸上下文指针 |
| A03 | 通过 | 两桥业务成员仅为值式查询适配器与邮箱引用 |
| A04 | 通过 | 两桥传统服务头 include / import 扫描零命中 |
| A05 | 通过 | 旧服务兜底、双读和择优路径为零 |
| A06 | 通过 | 概念命名桥仍只读查询并提交非权威消息 |
| A07 | 通过 | 任务管理桥仍只复核回执、形成许可候选并转换非权威消息 |
| A08 | 通过 | 概念、任务回执与多轮自检均验证桥接前后权威结构 / 状态守恒 |
| A09 | 通过 | 空租约逻辑拒绝为只读查询不可用，邮箱和结构零变化 |
| A10 | 通过 | B2 / B3、入口、SQL、面板和 O1 文件零差异；多轮自检只改 B1 构造调用 |
| A11 | 通过 | 生产模块不依赖自检，三个自检模块保持原验收职责 |
| A12 | 通过 | 入口、工程、运行器零差异，920 精确扫描零命中 |
| A13 | 通过 | Debug / Release Rebuild、两配置完整自检及 Debug 连续 20 / 20 通过 |
| A14 | 通过 | strict、diff check、五文件范围和禁止依赖检查通过 |
| A15 | 通过 | 多轮自检的自我治理领域路由 diff 为零，只增加 B1 只读运行期和桥构造改接 |

## 5. 最终验证

```text
Debug x64 Rebuild：退出 0，构建诊断匹配 0 警告 / 0 错误
Debug 完整自检：退出 0
Release x64 Rebuild：退出 0，构建诊断匹配 0 警告 / 0 错误
Release 完整自检：退出 0
SELF-GOV-S3B A01-A09：全部通过
CONCEPT-NAMING-S1 A01-A12：全部通过
Debug 最终连续完整自检：20 / 20，每轮退出 0，失败 0，411 秒
py -3.14 tools/check_specs.py --strict：通过
git diff --check：通过
生产桥传统服务 / 原始令牌 / 写入会话扫描：零命中
入口 / 工程 / 运行器 / B0 / B3 / #304 文件差异：零
阶段 920：零命中
```

连续验证前曾与 WT-304-R1 共享固定 `D:\海中鱼巣\日志\事件段\自检` 目录。按设计窗口协调，本任务暂停全部程序运行，待 #304 明确释放后才重新从单轮和连续验证取证；最终 20 / 20 在独占目录条件下完成。

## 6. 交接边界

本任务只证明两个既有上行桥已经改为租约值式只读，并完成三个既有自检消费者的 B1 合同迁移。它不证明命名写、自我治理写、默认入口、SQL / 面板、快照、恢复、B3 或旧能力迁移完成。

下一步只允许精确暂存五个代码文件与本记录，提交并非强制推送同名远端 `codex/299-p0b1-r1`。在独立集成、main 发布和设计同步前，不得宣称 #299 正式完成。
