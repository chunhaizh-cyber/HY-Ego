# 计划生成直接登记计划索引规则修订 Codex 断点清单

更新时间：2026-07-10

状态：历史过渡记录 / 已被现行计划生成即入队规则取代 / 不作为当前执行许可

## 记录定位

```text
本文件保存 2026-07-10 从“待确认计划”转向“计划直接登记”的过渡事实。
当前有效规则以 AGENTS.md、规范总纲、规则迁移清单、计划索引和任务队列为准。
恢复本文件不恢复旧流转，也不新增任何待执行计划。
```

## 输入入口

```text
用户当时明确口径：计划不再需要确认，生成后直接加入计划索引。
```

## 历史核心处理

```text
1. 将默认流转从“详细设计 -> 待确认计划 -> 用户确认 -> 计划索引”修订为“详细设计 -> 计划 -> 计划索引登记”。
2. 当时继续区分计划索引登记、可执行队列登记和代码实施许可。
3. 涉及代码、旧能力、字段、函数或机器结构时，仍要求计划明确允许文件、禁止文件和验证方式。
4. 计划/待确认计划/ 从默认输出目录降级为历史或遗留证据目录。
```

## 当前有效规则

```text
1. 流程图生成后默认直接生成详细设计。
2. 详细设计生成后默认直接生成完整计划。
3. 完整计划同步登记计划索引和项目记忆/Codex任务队列.md，状态为待执行或依赖门控待执行。
4. 执行窗口只消费依赖满足、允许与禁止范围完整且实际接口复核通过的计划。
5. 计划登记不允许扩大到计划外代码、旧能力迁移、字段落地、函数迁移或机器结构变更。
```

## 结构承载

```text
AGENTS.md
规范/000_项目规则总纲.md
规范/001_规则迁移清单.md
计划/计划索引.md
项目记忆/Codex任务队列.md
项目记忆/实施记录.md
```

## 输出变化

```text
1. 恢复缺失的历史规则演进证据。
2. 三份相关详细设计按当前规则修正计划生成与入队表述。
3. 不恢复旧待确认门禁，不登记新计划，不修改 C++。
```

## 调用路径

```text
误提交救援包
-> 只读复核最新 main
-> 手工移植历史段落
-> 按现行规则校正有效口径
-> 文档验证
```

## 验证方式

```text
git diff --cached --check
python .\tools\check_specs.py --strict
rg -n "待确认施工计划|待计划确认后执行|计划生成后直接登记" 规范/详细设计 项目记忆/实施记录.md 本文件
```

## 禁止扩大声明

```text
本次只恢复和校正规则历史材料。
未修改 C++，未运行构建，未新增可执行队列项。
不证明任何计划已执行，不证明旧能力迁移或外设接入完成。
```
