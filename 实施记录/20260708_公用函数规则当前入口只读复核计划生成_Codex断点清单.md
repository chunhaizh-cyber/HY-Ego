# 20260708 公用函数规则当前入口只读复核计划生成 Codex 断点清单

## 状态

已生成 / 待确认计划 / 设计确认窗口 / 未入队 / 未改 C++ / 未迁移旧函数。

## 输入入口

用户在计划设计和确认窗口输入：

```text
继续
```

按已修订的窗口规则，本窗口中的 `继续` 只继续设计、计划、信息数据和确认材料治理，不消费可执行队列。

## 核心处理

基于 `实施记录/20260708_公用函数规则后续实施门禁信息数据.md` 中的三类门禁，生成第一项正式待确认计划：

```text
计划/待确认计划/20260708_公用函数规则当前入口只读复核计划_v0.1.md
```

该计划只授权后续执行窗口做 S0 当前入口只读复核。

## 结构承载

- 待确认计划：`计划/待确认计划/20260708_公用函数规则当前入口只读复核计划_v0.1.md`
- 计划索引：新增待确认计划登记。
- 项目记忆：同步当前状态、设计计划队列和待确认问题。

## 输出变化

新增 S0 只读复核计划，明确：

```text
只读范围
禁止文件
分类归属
风险判断
验收方式
完成声明边界
待确认问题
```

## 调用路径

```text
公用函数规则规范
-> 公用函数规则后续实施门禁信息数据
-> S0 当前入口只读复核待确认计划
-> 用户确认后登记可执行队列
-> 执行窗口只读复核
```

## 验证方式

本切片为文档治理：

```powershell
git diff --check
if (Test-Path .\tools\check_specs.py) { python .\tools\check_specs.py }
```

未运行 C++ 构建，未运行程序。

## 禁止宣称

- 不宣称 S0 只读复核已完成。
- 不宣称公用函数工具层详细设计已生成。
- 不宣称公用函数工具层已实现。
- 不宣称 C++ 已修改或已构建。
- 不宣称旧函数体已迁移。
