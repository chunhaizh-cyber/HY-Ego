# FS-01 旧世界树核心边界只读扫描记录

日期：2026-07-06
状态：FS-01 第二批已登记 / 只读扫描 / 未改代码 / 未迁移旧函数
依据清单：`实施记录/20260706_逐函数只读扫描清单.md`
前序记录：`实施记录/20260706_FS00_FS01_逐函数只读扫描记录.md`

## 1. 本轮定位

本轮继续 FS-01，只读登记旧 `世界树类` 中与核心结构边界相关的函数族：

```text
世界树聚合入口
旧基础信息仓库访问桥
旧主键解析入口
旧节点父子读取入口
SQL / 控制面板投影边界
特征值池和体素接口排除边界
```

本轮不登记完整特征、存在、场景、状态、动态、因果业务函数细节。那些函数后续按 FS-02 到 FS-07 分域扫描。

## 2. 只读扫描范围

```text
D:\鱼巢\世界树类.h
D:\鱼巢\世界树类.cpp
```

扫描证据来自：

```text
世界树类.h：世界树门面函数声明和文件头部规则注释。
世界树类.cpp:2186-2428：初始化、服务访问、主键解析、节点读取、SQL 投影入口。
世界树类.cpp:2606-2765：SQL 控制面板同步和存在概念初始化入口。
世界树类.cpp:2925-3068：抽象特征根和抽象特征创建门面。
世界树类.cpp:3200-3309：存在创建、查找和旧需求 / 任务 / 方法根读取。
世界树类.cpp:4462-4545：特征写入和值池桥接。
世界树类.cpp:4778-4916：默认世界、自我存在和内部世界创建 / 重挂入口。
```

## 3. 逐函数登记

### OF-019

```text
旧函数编号：OF-019
扫描批次：FS-01
旧项目路径：D:\鱼巢\世界树类.cpp:2186
旧函数名：世界树类::初始化 / 初始化默认世界
旧函数签名：void 初始化(); void 初始化默认世界()
当前调用方：旧世界树启动流程。
触发路径：世界树启动时初始化基础信息链、值池、语素集、度量模板注册表、抽象特征根和默认世界。
输入：无显式输入，依赖全局语素集和旧服务成员。
输出：无返回值。
读结构：旧世界树成员、语素集、默认世界指针。
写结构：基础信息链初始化、值池初始化、语素集初始化、抽象特征根、默认世界场景、存在概念系统。
是否写机器事实：是，创建默认世界和概念结构。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：系统启动时建立最小世界种子。
删除行为：删除旧世界树一次性大初始化和全局语素 / 值池混合初始化方式。
新增约束：新项目世界种子必须通过世界服务聚合存在服务和场景服务；特征值服务不得被世界服务当作通用值池直接暴露给高级服务。
目标新结构：存在节点、场景节点、抽象状态 / 抽象动态或后续世界种子关系。
目标领域服务入口：世界服务、存在服务、场景服务；涉及特征必须经特征服务。
对应规范 / 详细设计：基础信息服务分层规范、世界聚合服务详细设计、领域服务授权写入规范。
入口拒绝条件：缺少语义入口、场景创建失败、存在创建失败、越权直接写特征值服务。
验证方式：世界种子可由世界服务读取；拒绝路径节点 / 关系数量不变化。
初步判定：拆分
待用户确认项：默认世界和存在概念系统是否作为后续世界种子专项独立设计。
扫描证据：`世界树类.cpp:2186`、`世界树类.cpp:2196`。
```

### OF-020

```text
旧函数编号：OF-020
扫描批次：FS-01
旧项目路径：D:\鱼巢\世界树类.cpp:2208
旧函数名：世界树类::基础信息 / 借用基础信息仓库_内部服务
旧函数签名：基础信息类& 基础信息(); 基础信息类& 借用基础信息仓库_内部服务()
当前调用方：旧内部服务、显示 / 诊断或兼容路径。
触发路径：直接返回旧基础信息链引用。
输入：无。
输出：基础信息类引用。
读结构：世界树内部基础信息链。
写结构：函数本身不写，但返回可写引用可导致外部绕过服务边界写结构。
是否写机器事实：待确认；取决于调用方。
是否依赖日志：否。
是否依赖显示：可能被显示 / 诊断调用。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：内部服务需要访问基础仓库的依赖注入语义。
删除行为：删除外部可写借用仓库作为事实写入捷径。
新增约束：新项目外部调用方不得绕过领域服务直接写仓库；如需内部访问，必须是构造注入或服务私有成员。
目标新结构：无新增业务结构；属于服务依赖边界。
目标领域服务入口：世界服务内部依赖注入；显示层只能只读。
对应规范 / 详细设计：领域服务授权写入规范、基础信息服务分层规范。
入口拒绝条件：外部业务调用方请求可写仓库引用、显示层写入、日志层写入。
验证方式：编译期依赖检查和服务构造参数检查；越权路径不存在或被拒绝。
初步判定：拆分
待用户确认项：是否禁止新项目提供任何公开可写基础仓库借用入口。
扫描证据：`世界树类.cpp:2208`、`世界树类.cpp:2214`。
```

### OF-021

```text
旧函数编号：OF-021
扫描批次：FS-01
旧项目路径：D:\鱼巢\世界树类.cpp:2220
旧函数名：世界树类::值池 / 特征 / 存在 / 场景 / 二次特征 / 状态 / 动态 / 因果
旧函数签名：各服务成员引用访问器。
当前调用方：旧世界树门面调用方。
触发路径：直接取得旧领域服务对象引用。
输入：无。
输出：旧服务引用。
读结构：世界树内部服务成员。
写结构：函数本身不写，但可暴露写服务。
是否写机器事实：待确认；取决于调用方。
是否依赖日志：否。
是否依赖显示：可能间接被显示和控制面板读取。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：值池和特征体素接口可能间接涉及体素材料。
保留行为：服务组合和依赖注入语义。
删除行为：删除世界树作为万能服务定位器的公共写入口。
新增约束：高级服务只允许依赖白名单基础服务；特征值服务只允许特征服务直接访问。
目标新结构：无新增业务结构。
目标领域服务入口：按领域服务授权写入规范逐项登记服务依赖。
对应规范 / 详细设计：领域服务授权写入规范、基础信息服务分层规范。
入口拒绝条件：需求 / 任务 / 方法等高级服务直接访问特征值服务；显示层取得可写服务引用。
验证方式：依赖白名单检索；构造参数不暴露特征值服务给高级服务。
初步判定：拆分
待用户确认项：新项目是否保留只读服务定位入口，还是全部改为显式依赖注入。
扫描证据：`世界树类.cpp:2220` 至 `世界树类.cpp:2283`。
```

### OF-022

```text
旧函数编号：OF-022
扫描批次：FS-01
旧项目路径：D:\鱼巢\世界树类.cpp:2292
旧函数名：世界树类::按主键解析节点 / 按主键解析存在节点 / 场景节点 / 特征节点 / 状态节点 / 动态节点 / 二次特征节点 / 因果节点
旧函数签名：节点指针* 按主键解析...(const std::string& 主键) const noexcept
当前调用方：旧恢复、显示、关系解析和各领域读取路径。
触发路径：字符串主键到裸节点指针解析。
输入：字符串主键。
输出：节点裸指针或 nullptr。
读结构：旧基础信息链主键索引或各服务按主键解析入口。
写结构：无。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：可能被显示 / SQL 诊断使用。
是否依赖 SQL：否。
是否依赖线程：底层服务决定。
是否依赖外设 / D455 / 体素：否。
保留行为：按稳定身份解析节点。
删除行为：删除裸指针作为长期身份返回。
新增约束：新项目必须返回稳定句柄，并经节点仓库和领域服务复核节点类型；字符串主键只能是索引材料。
目标新结构：节点句柄、索引仓库主键绑定、领域专用读取入口。
目标领域服务入口：索引仓库 `按主键查节点`、节点仓库 `读取节点`、领域服务专用读取入口。
对应规范 / 详细设计：索引仓库详细设计、节点仓库详细设计、领域服务授权写入规范。
入口拒绝条件：空主键、主键命名空间无效、节点无效、节点类型不匹配、多目标语义未确认。
验证方式：按主键查句柄后复核节点类型；拒绝路径结构不变。
初步判定：改写为领域服务入口
待用户确认项：旧主键字符串是否只作为迁移期索引命名空间输入。
扫描证据：`世界树类.cpp:2292`、`世界树类.cpp:4272`。
```

### OF-023

```text
旧函数编号：OF-023
扫描批次：FS-01
旧项目路径：D:\鱼巢\世界树类.cpp:2334
旧函数名：世界树类::获取父节点 / 获取子节点 / 统计全部节点 / 基础节点属于当前世界树 / 获取名称
旧函数签名：基础信息节点类* 获取父节点(...); std::vector<基础信息节点类*> 获取子节点(...); std::size_t 统计全部节点() const
当前调用方：旧结构浏览、统计、SQL 投影和诊断。
触发路径：读取旧基础信息树父子关系和节点名称。
输入：节点或无。
输出：父节点、子节点集合、节点数量或名称文本。
读结构：旧基础信息父子指针、全链枚举、主信息名称。
写结构：无。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：是，名称和枚举常用于显示 / SQL 投影。
是否依赖 SQL：函数本身否，调用方可能是 SQL 投影。
是否依赖线程：底层基础信息链读锁。
是否依赖外设 / D455 / 体素：否。
保留行为：只读结构浏览和统计诊断。
删除行为：不得用枚举结果或名称文本裁决业务事实。
新增约束：业务读取必须走节点仓库 / 关系仓库 / 领域服务专用读取；统计只能是非权威缓存或诊断。
目标新结构：关系仓库普通父子读取、节点仓库统计、非权威统计快照。
目标领域服务入口：关系仓库 `获取父节点` / `获取子节点`，非权威缓存统计入口。
对应规范 / 详细设计：关系仓库详细设计、非权威缓存与统计规范、领域服务授权写入规范。
入口拒绝条件：无效节点、显示层写入、统计结果参与业务裁决。
验证方式：清空统计不改变权威结构；父子关系由关系仓库读回。
初步判定：拆分
待用户确认项：哪些只读枚举函数进入后续显示层只读视图专项。
扫描证据：`世界树类.cpp:2334` 至 `世界树类.cpp:2365`。
```

### OF-024

```text
旧函数编号：OF-024
扫描批次：FS-X
旧项目路径：D:\鱼巢\世界树类.cpp:2371
旧函数名：世界树类::重写世界树SQL投影 / 校验世界树SQL投影往返
旧函数签名：bool 重写世界树SQL投影(const char* 来源原因, bool 执行字段恢复比对) const noexcept；结构_世界树SQL往返校验结果 校验世界树SQL投影往返(...)
当前调用方：旧 SQL 投影、控制面板或诊断。
触发路径：把内存世界树快照写入 SQL Server 投影库，再做读回诊断。
输入：来源原因、是否执行字段恢复比对、差异上限。
输出：bool 或 SQL 往返校验结果。
读结构：旧世界树节点、主信息、关系。
写结构：SQL Server 投影库、运行日志 / 错误日志。
是否写机器事实：否；但旧实现可能被误用为恢复或显示依据。
是否依赖日志：是，写项目运行日志和错误日志。
是否依赖显示：是，SQL 投影服务控制面板显示。
是否依赖 SQL：是。
是否依赖线程：使用 SQL 投影互斥。
是否依赖外设 / D455 / 体素：否。
保留行为：后续只读视图和持久化诊断可参考快照 / 往返校验语义。
删除行为：本轮不接 SQL，不把 SQL 读回结果作为运行期事实裁决。
新增约束：数据库只做快照、事件日志、恢复和审计适配；显示层只读。
目标新结构：事件日志持久化恢复专项、显示层只读视图专项。
目标领域服务入口：后续专项；本轮不迁移。
对应规范 / 详细设计：日志系统规范、领域服务授权写入规范；事件日志持久化恢复专项和显示层只读视图专项待设计。
入口拒绝条件：本轮禁止接 SQL；缺少专项设计；试图用 SQL 投影裁决机器事实。
验证方式：后续专项必须证明清空 / 缺失 SQL 投影不影响运行期权威结构。
初步判定：排除 / 后续专项
待用户确认项：是否进入事件日志持久化恢复专项或显示层只读视图专项。
扫描证据：`世界树类.cpp:2371`、`世界树类.cpp:2419`。
```

### OF-025

```text
旧函数编号：OF-025
扫描批次：FS-X
旧项目路径：D:\鱼巢\世界树类.cpp:2606
旧函数名：世界树类::同步SQL控制面板世界显示项
旧函数签名：void 同步SQL控制面板世界显示项(const 基础信息节点类* 节点, const char* 阶段) const noexcept
当前调用方：旧世界树创建 / 写入后显示镜像刷新。
触发路径：存在、场景、状态节点变化后写 SQL 控制面板显示项。
输入：节点、阶段文本。
输出：无。
读结构：节点主信息、父节点、主键、显示标题。
写结构：SQL 控制面板显示项、SQL 控制面板同步状态、运行警告日志。
是否写机器事实：否，写显示镜像和诊断。
是否依赖日志：是。
是否依赖显示：是。
是否依赖 SQL：是。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：后续显示层只读刷新语义可参考。
删除行为：本轮不接控制面板，不让显示同步失败影响世界树写入裁决。
新增约束：显示层只读结构，不写业务事实；显示同步失败不得回滚或改变机器事实。
目标新结构：显示层只读视图专项。
目标领域服务入口：后续显示层只读视图专项；本轮不迁移。
对应规范 / 详细设计：领域服务授权写入规范；显示层只读视图专项待设计。
入口拒绝条件：本轮禁止接控制面板 / SQL；显示同步请求写业务事实。
验证方式：后续专项验证显示缺失不影响节点、关系、状态、动态事实。
初步判定：排除 / 后续专项
待用户确认项：显示层只读视图专项是否以此作为旧显示镜像参考。
扫描证据：`世界树类.cpp:2606`。
```

### OF-026

```text
旧函数编号：OF-026
扫描批次：FS-01
旧项目路径：D:\鱼巢\世界树类.cpp:2660
旧函数名：世界树类::取或创建存在概念根 / 确保根概念_* / 初始化核心存在概念树
旧函数签名：存在节点类* 取或创建存在概念根(); bool 初始化核心存在概念树()
当前调用方：旧默认世界和概念系统初始化。
触发路径：创建存在概念根、实物存在、虚拟存在、信息存在、数字生命、自我、需求、任务、方法、状态、事件、规则等概念节点。
输入：无显式输入，内部使用语素入口词。
输出：存在节点指针或 bool。
读结构：旧语素集、基础信息树、存在服务。
写结构：存在概念节点、概念父子结构、语素入口。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：核心概念种子初始化。
删除行为：不把概念树裸父子结构直接迁入；不新增高级信息节点类型。
新增约束：`高级信息` 不是节点类型；需求、任务、方法只能作为既有节点类型或方法 / 任务 / 需求角色结构。
目标新结构：存在概念节点、语素对应关系、需求 / 任务 / 方法节点类型。
目标领域服务入口：存在服务、语素服务、世界服务。
对应规范 / 详细设计：节点类型与关系类型枚举规范、基础信息服务分层规范、语素系统详细设计。
入口拒绝条件：语素入口缺失、试图新增 `高级信息` 节点类型、概念父子关系未确认。
验证方式：核心概念可由存在服务读取；语素入口对应信息只绑定长期基础信息或高级节点。
初步判定：拆分
待用户确认项：核心概念种子是否作为世界种子专项还是 FS-02 基础信息专项登记。
扫描证据：`世界树类.cpp:2660` 至 `世界树类.cpp:2751`。
```

### OF-027

```text
旧函数编号：OF-027
扫描批次：FS-03
旧项目路径：D:\鱼巢\世界树类.cpp:2925
旧函数名：世界树类::取或创建抽象特征根 / 创建抽象特征 / 创建叶子抽象特征 / 创建复合抽象特征
旧函数签名：基础信息节点类* 取或创建抽象特征根(); 抽象特征节点类* 创建抽象特征(...)
当前调用方：旧特征系统初始化和特征模板创建。
触发路径：抽象特征根和子抽象特征创建。
输入：父抽象特征、名称、类型、值域或主信息。
输出：抽象特征节点指针。
读结构：基础信息世界根、语素入口、旧抽象特征主信息。
写结构：抽象特征节点、主信息、特征服务结构。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：抽象特征根和抽象特征创建语义。
删除行为：不迁移旧抽象特征主信息字段族为新主信息裸字段。
新增约束：特征 / 特征值系统增强专项确认前，只能登记为 FS-03 后续候选。
目标新结构：特征节点、抽象特征角色、特征值域结构或二次特征结构。
目标领域服务入口：特征服务、二次特征服务。
对应规范 / 详细设计：特征 / 特征值系统增强专项待设计；基础信息服务分层规范。
入口拒绝条件：父抽象特征无效、名称 / 类型语素缺失、值域未确认、越权直接写特征值服务。
验证方式：特征服务读取抽象特征结构；高级服务不直接依赖特征值服务。
初步判定：排除 / 后续专项
待用户确认项：转入“特征 / 特征值系统增强专项”。
扫描证据：`世界树类.cpp:2925` 至 `世界树类.cpp:2981`。
```

### OF-028

```text
旧函数编号：OF-028
扫描批次：FS-02
旧项目路径：D:\鱼巢\世界树类.cpp:3200
旧函数名：世界树类::创建存在 / 取或创建子存在_按名称类型 / 取或创建子存在_按类型 / 查找子存在_按名称 / 查找子存在_按类型
旧函数签名：存在节点类* 创建存在(...); 存在节点类* 取或创建子存在_按名称类型(...)
当前调用方：旧世界树存在创建和查找路径。
触发路径：按父节点、名称和类型创建或查找存在。
输入：父节点、名称语素、类型语素、存在主信息。
输出：存在节点指针或 nullptr。
读结构：旧基础信息树、存在服务、语素入口。
写结构：存在节点、存在主信息、概念集补齐、SQL 控制面板显示镜像。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：是，创建后可能同步 SQL 控制面板。
是否依赖 SQL：是，创建后可能调用显示镜像同步。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：创建和查找存在身份。
删除行为：删除创建后自动写 SQL 控制面板镜像作为迁移主路径。
新增约束：存在创建必须由存在服务负责；显示镜像后续只读视图专项处理。
目标新结构：存在节点、存在概念关系、场景归属关系。
目标领域服务入口：存在服务、场景服务、世界服务。
对应规范 / 详细设计：存在服务详细设计、场景服务详细设计、领域服务授权写入规范。
入口拒绝条件：父节点无效、名称 / 类型缺失策略未确认、越权写入方、显示层写业务事实。
验证方式：存在节点可由存在服务读取；显示同步缺失不影响存在事实。
初步判定：拆分
待用户确认项：存在创建细节转入 FS-02，SQL 显示同步转入 FS-X。
扫描证据：`世界树类.cpp:3200` 至 `世界树类.cpp:3261`。
```

### OF-029

```text
旧函数编号：OF-029
扫描批次：FS-04 / FS-05 / FS-06
旧项目路径：D:\鱼巢\世界树类.cpp:3269
旧函数名：世界树类::获取需求根节点 / 获取任务根节点 / 获取方法根节点
旧函数签名：需求类::节点类* 获取需求根节点(存在节点类* 节点) const noexcept；任务类::节点类* 获取任务根节点(...); 方法类::节点类* 获取方法根节点(...)
当前调用方：旧需求树、任务树和方法树挂在存在主信息下的读取路径。
触发路径：从存在节点主信息读取旧需求 / 任务 / 方法根裸指针。
输入：存在节点。
输出：旧需求 / 任务 / 方法树节点裸指针。
读结构：存在主信息中的需求根节点、任务根节点、方法根节点字段。
写结构：无。
是否写机器事实：否；但读取旧主信息字段承载的高级树事实。
是否依赖日志：否。
是否依赖显示：可能被显示 / SQL 投影使用。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：存在拥有需求 / 任务 / 方法树的追溯语义。
删除行为：不迁移存在主信息里的需求根 / 任务根 / 方法根裸指针字段。
新增约束：需求、任务、方法必须由各自节点类型和关系承载；不得回流旧主信息字段。
目标新结构：需求节点、任务节点、方法节点、存在 / 需求 / 任务 / 方法关系。
目标领域服务入口：需求服务、任务服务、方法服务。
对应规范 / 详细设计：需求结算记录规范、任务系统规范、方法系统规范、主信息系统规范。
入口拒绝条件：存在节点无效、旧根指针无效、试图新增旧主信息字段。
验证方式：通过需求 / 任务 / 方法服务专用读取入口读回关系。
初步判定：改写为领域服务入口
待用户确认项：在 FS-04、FS-05、FS-06 分别细化旧需求树、任务树、方法树根语义。
扫描证据：`世界树类.cpp:3269` 至 `世界树类.cpp:3299`。
```

### OF-030

```text
旧函数编号：OF-030
扫描批次：FS-03
旧项目路径：D:\鱼巢\世界树类.cpp:4462
旧函数名：世界树类::写入特征_I64 / 写入特征值 / 写入特征_指针 / 写入特征_VecU / 写入特征_VecI64 / 读取特征快照
旧函数签名：bool 写入特征_I64(...); bool 写入特征值(...); 特征值 读取特征快照(...)
当前调用方：旧世界树特征写入门面和观察 / 方法结果写入路径。
触发路径：经世界树包装调用特征服务和值池，写 I64、指针句柄、VecU、VecI64 或读取特征快照。
输入：特征节点或宿主 + 特征类型、值、时间戳。
输出：bool 或特征值。
读结构：特征节点、宿主、特征类型、值池。
写结构：特征当前值、值池 VecU 句柄、特征状态材料。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：VecU / VecI64 可能承载轮廓或体素材料。
保留行为：经特征服务写入特征状态材料。
删除行为：删除世界树作为通用特征值写入口；高级服务不得直接访问特征值服务或值池。
新增约束：新项目特征状态材料必须通过特征服务；特征值服务只服务特征系统内部。
目标新结构：特征节点、特征值节点、特征状态材料、状态 / 动态证据。
目标领域服务入口：特征服务；体素相关转后续专项。
对应规范 / 详细设计：基础信息服务分层规范、需求目标状态承载详细设计；特征 / 特征值系统增强专项待设计。
入口拒绝条件：无效特征、特征值不归属特征、时间戳缺失、越权高级服务直接调用特征值服务。
验证方式：经特征服务读回状态材料；高级服务依赖白名单检索。
初步判定：拆分
待用户确认项：I64 / 指针 / VecU / VecI64 写入分别在 FS-03 中细化。
扫描证据：`世界树类.cpp:4462` 至 `世界树类.cpp:4545`。
```

### OF-031

```text
旧函数编号：OF-031
扫描批次：FS-01 / FS-X
旧项目路径：D:\鱼巢\世界树类.cpp:4778
旧函数名：世界树类::取或创建默认世界 / 取或创建自我现实场景 / 取或创建自我存在 / 取或创建存在内部世界
旧函数签名：场景节点类* 取或创建默认世界(...); 存在节点类* 取或创建自我存在(); 场景节点类* 取或创建存在内部世界(...)
当前调用方：旧世界启动、自我初始化和内部世界建立路径。
触发路径：创建默认场景、自我初始场景、自我存在、内部世界根和宿主内部世界；可能重挂旧内部世界节点。
输入：世界名称、世界类型、宿主存在、场景名称。
输出：场景节点或存在节点指针。
读结构：旧语素集、场景服务、存在服务、基础信息父子树、坐标特征。
写结构：场景节点、存在节点、内部世界绑定、普通父子重挂、存在坐标特征、SQL 控制面板显示镜像；自我最小边界体素模型候选。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：是，调用 SQL 控制面板同步。
是否依赖 SQL：是，显示镜像同步。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：是，自我存在初始化会尝试确保最小边界体素模型。
保留行为：默认世界、自我存在和内部世界初始化语义。
删除行为：本轮不接 SQL 控制面板和体素模型；不迁移旧内部世界裸重挂算法。
新增约束：默认世界 / 自我 / 内部世界必须拆成世界种子、存在服务、场景服务和关系仓库重挂；体素进入后续专项。
目标新结构：场景节点、存在节点、存在-内部世界关系、普通父子关系、特征状态材料。
目标领域服务入口：世界服务、存在服务、场景服务、关系仓库重挂入口、特征服务。
对应规范 / 详细设计：世界聚合服务详细设计、存在服务详细设计、场景服务详细设计、关系仓库详细设计；体素 / 显示专项待设计。
入口拒绝条件：宿主存在无效、场景创建失败、内部世界关系缺失、移动到非法父节点、试图接体素或 SQL 控制面板。
验证方式：世界服务可读回默认场景和自我存在；关系仓库读回内部世界归属；显示 / 体素缺失不影响世界事实。
初步判定：拆分
待用户确认项：默认世界 / 自我初始化是否单独建立世界种子专项；体素和显示部分排入 FS-X。
扫描证据：`世界树类.cpp:4778` 至 `世界树类.cpp:4916`。
```

## 4. 本轮初步结论

```text
1. 旧 `世界树类` 是聚合门面和服务定位器，不适合整体迁移。
2. 核心结构语义应拆分到世界服务、存在服务、场景服务、特征服务、关系仓库、索引仓库和后续显示 / 持久化专项。
3. SQL 投影和控制面板同步只登记为 FS-X / 后续显示层只读视图专项，不得进入本轮迁移。
4. 体素相关接口只登记为排除边界，等待体素 / 外设专项，不得提前接入。
5. 旧需求根 / 任务根 / 方法根裸指针读取是后续 FS-04 / FS-05 / FS-06 的重点，不得迁移为存在主信息字段。
```

## 5. 后续待执行

```text
1. FS-01 还需补旧核心结构中的主信息字段访问辅助函数和基础数据类型中句柄 / 值容器边界。
2. FS-02 可开始扫描语素 / 基础信息函数，但必须先完成或明确关闭 FS-01 剩余核心边界。
3. FS-03 需要细化特征 / 特征值函数，特别是 VecU / VecI64 / 体素材料的排除边界。
4. OF-019 到 OF-031 均为初步判定，需用户确认后才可进入任何代码实施。
```

## 6. 禁止宣称

```text
不得声明 FS-01 已完整扫描。
不得声明旧世界树已迁移。
不得声明旧函数已迁移。
不得声明 SQL 投影、控制面板、体素或外设已接入。
不得声明世界服务、特征服务、存在服务或场景服务完整完成。
不得声明旧需求树、任务树或方法树根字段已迁移。
```
