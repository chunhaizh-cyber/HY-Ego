# 计划索引锁规则取消 Codex 断点清单

日期：2026-07-08
状态：已完成 / 协作规则修订 / 未改 C++ / 未构建 / 未迁移旧函数

## 1. 输入入口

```text
用户口径：取消“编辑 计划/计划索引.md 必须创建 计划/.计划索引.lock”的规则，改用默认 Git 保护。
```

## 2. 核心处理

```text
取消本地计划技能中的强制计划索引锁规则。
取消本地执行技能中的强制计划索引锁规则。
改为要求编辑共享治理文件前检查 git status，编辑后检查定向 diff，只暂存本切片文件。
同一目标行发生不明确并发变更时停止并记录阻塞，不靠锁文件裁决。
```

## 3. 结构承载

```text
.codex/skills/hai-zhong-yu-chao-plan/SKILL.md
.codex/skills/hai-zhong-yu-chao-execute/SKILL.md
项目记忆/决策记录.md
项目记忆/当前状态.md
```

## 4. 输出变化

```text
后续编辑 计划/计划索引.md 不再需要创建 计划/.计划索引.lock。
Git 工作区、定向 diff、限定暂存和提交边界成为并行窗口保护方式。
历史计划或实施记录中提到的旧锁占用只保留为历史事实，不再作为当前规则。
```

## 5. 调用路径

```text
设计确认窗口
-> 用户确认取消锁规则
-> 修订本地技能规则
-> 同步项目记忆
-> 留存本断点清单
```

## 6. 验证方式

```text
rg "计划索引\\.lock|\\.计划索引\\.lock|Before editing `计划/计划索引.md`, create|requires `计划/.计划索引.lock`" .codex/skills AGENTS.md 规范
git diff --check
python .\tools\check_specs.py
```

本断点清单只证明协作规则已修订，不证明任何 C++ 代码、构建、执行队列项或旧能力迁移发生。
