# FS-09 事件日志持久化恢复只读扫描记录

日期：2026-07-06
状态：FS-09 第一批已登记 / 只读扫描 / 未改代码 / 未迁移旧函数
依据清单：`实施记录/20260706_逐函数只读扫描清单.md`

## 1. 本轮定位

本轮进入 FS-09，登记旧项目日志输出、SQL / ADO 存储验证、SQL 快照投影、二进制保存恢复、字段序列化和线程事件消息边界。

本轮只做只读事实登记，不运行旧项目，不迁移旧函数体，不新增旧主信息字段，不接控制面板、SQL 实时投影、D455、体素或外设。当前新项目日志口径仍是：日志只做人读诊断、运行观察、审计辅助和调试定位，不承载机器逻辑；事件日志第一轮只是独立文件入口，后续持久化和审计适配不得反向裁决运行期事实。

## 2. 只读扫描范围

```text
D:\鱼巢\日志模块.ixx
D:\鱼巢\日志接入.h
D:\鱼巢\数据库ADO模块.ixx
D:\鱼巢\数据库ADO模块.cpp
D:\鱼巢\双向链表模板_序列化.inl
D:\鱼巢\说明书\双向链表模板序列化说明.md
D:\鱼巢\特征值主信息类.h
D:\鱼巢\世界树类.h
D:\鱼巢\世界树类.cpp
D:\鱼巢\语素类.cpp
D:\鱼巢\需求类.cpp
D:\鱼巢\任务类.cpp
D:\鱼巢\方法类.cpp
D:\鱼巢\任务模块.工作线程协议.ixx
D:\鱼巢\自我动作实现.外设模块.ixx
```

本轮同时识别但不进入当前迁移的边界：

```text
旧项目运行日志、异常日志、弹窗诊断和调试器输出。
旧 ADO 查询、命令、字段恢复比对和 SQL Server 未启动弹窗。
旧 SQL 控制面板运行态显示项、事件流水和同步状态。
旧世界树、语素树、需求树、任务树和方法树 SQL 快照投影。
旧双向链表模板二进制保存 / 读取。
旧特征值主信息序列化 / 反序列化。
旧任务推进事件线程消息。
旧自我动作 / 外设 / 体素路径中的日志和快照输出。
```

扫描证据来自：

```text
日志模块.ixx:1-9、111-162、191-213、513-557、563-735：旧日志模块只负责日志、弹窗、调试器输出，运行日志和异常日志分文件，日志失败多处吞掉或只作人读诊断；低级错误会终止。
日志接入.h:1-48：旧日志接入声明项目运行日志、错误日志、弹窗和异常日志，并用预处理开关关闭输出面。
数据库ADO模块.ixx:1-7、48-101：旧 ADO 模块明确 SQL 写入只作为控制面板显示镜像，不作为业务权威源或恢复源。
数据库ADO模块.cpp:1-6、722-887：ADO 查询、字段恢复比对和命令执行只服务显示 / 诊断 / 存储验证；写命令受 SQL 控制面板同步开关控制。
数据库ADO模块.cpp:560-702、888-1087：旧控制面板运行态 SQL 显示镜像会清空运行态表、写运行批次、事件流水和同步状态。
世界树类.cpp:5、1902-1998、2371-2535：世界树 SQL 投影和字段恢复比对只用于显示 / 诊断 / 存储验证，重写 SQL 投影和往返校验不得反向作为世界树业务判断来源。
语素类.cpp:5、895-1058：语素 SQL 投影和字段恢复比对只用于显示 / 诊断 / 存储验证，不反向作为语素业务判断来源。
需求类.cpp:5、840-937、2721-2771：需求 SQL 投影失败不得回滚需求树内存结构提交，字段恢复比对只作存储验证。
任务类.cpp:5、1316-1455：任务 SQL 投影失败不得回滚任务树内存结构提交，字段恢复比对只作诊断。
方法类.cpp:5-6、1827-1915、2403-2448：方法 SQL 投影失败不得回滚方法树或方法虚拟存在结构提交。
双向链表模板_序列化.inl:4-6、73-148、153-246：旧链表模板保存 / 读取整棵双向链表和主信息，指针型主信息仍建议保存主键 / 索引而非对象本体。
说明书/双向链表模板序列化说明.md:1-11、76-106、108-133：旧说明明确保存前应校验结构、读取后应校验结构，二进制格式跨版本不稳定。
特征值主信息类.h:91-132：旧特征值主信息序列化 / 反序列化会保存值类型、维度、哈希和命中缓存字段。
任务模块.工作线程协议.ixx:10-33：旧任务推进事件是线程消息语义槽，不是持久化事件日志。
自我动作实现.外设模块.ixx:大量 `项目运行日志` 和观察快照输出依赖外设 / 体素路径，当前只作为排除边界。
```

## 3. 逐函数 / 函数组登记

### OF-183

```text
旧函数编号：OF-183
扫描批次：FS-09
旧项目路径：D:\鱼巢\日志模块.ixx、D:\鱼巢\日志接入.h
旧函数名：初始化项目日志、关闭项目日志、项目运行日志、项目运行错误日志、项目弹窗错误提示、项目记录异常日志、项目致命日志、项目自检无上级需求日志、日志::写、日志::记录异常、报告低级错误并终止_
旧函数签名：日志输出入口函数族
归属逻辑包：LP-FS09-LOG-01 / 日志输出与人读诊断入口
当前调用方：旧世界树、语素、需求、任务、方法、二次特征、自我动作和控制面板相关路径
触发路径：系统启动、运行阶段、异常捕获、逻辑错误提示、调试切片和低级错误诊断
输入：日志类别、日志级别、模块 / 入口 / 文本、异常对象、弹窗标题和文本
输出：运行日志、异常日志、弹窗、调试器输出、局部返回或终止
读结构：预处理开关、日志参数、线程 ID、系统时间
写结构：旧日志文件和弹窗；不写项目机器结构
是否写机器事实：否
是否依赖日志：是
是否依赖显示：弹窗属于人读显示
是否依赖 SQL：否
是否依赖线程：记录线程 ID，但线程不作为事实来源
是否依赖外设 / D455 / 体素：否
保留行为：保留日志只做人读诊断、运行观察和调试定位的边界；逻辑错误诊断不得沉默成业务成功
删除行为：删除日志文本参与需求满足、任务完成、方法成功、结构修复或旧能力完成声明的口径
新增约束：新项目日志入口必须按逻辑错误日志、运行日志、调试日志和事件日志分离；事件日志后续只能记录已由结构确认的事件材料
目标新结构：日志文件；后续事件日志持久化恢复专项材料
目标领域服务入口：日志系统 / 后续事件日志持久化恢复专项
对应规范 / 详细设计：`规范/日志系统规范.md`、`规范/详细设计/日志系统详细设计.md`
入口拒绝条件：未知日志类别、调试切片为空、内容为空、目录或文件无法打开；不得把日志写入失败转成业务结构变化
验证方式：默认运行只写运行日志；逻辑错误日志和调试日志按宏隔离；日志缺失不影响机器事实结构
初步判定：改写为领域服务入口
初步迁移口径：旧日志函数只提供边界证据；当前新项目日志系统已有第一轮入口，事件日志持久化恢复另建专项
待用户确认项：后续事件日志是否需要结构化事件记录格式，而不是沿用旧自由文本日志
扫描证据：日志模块.ixx:1-9、111-162、191-213、513-557、563-735；日志接入.h:1-48
```

### OF-184

```text
旧函数编号：OF-184
扫描批次：FS-09
旧项目路径：D:\鱼巢\数据库ADO模块.ixx、D:\鱼巢\数据库ADO模块.cpp
旧函数名：生成SQLServerWindows认证ADO连接串、执行ADO查询、执行ADO字段恢复比对、执行ADO命令
旧函数签名：ADO 基础访问和字段恢复比对函数族
归属逻辑包：LP-FS09-SQL-01 / ADO 存储验证与 SQL 访问适配
当前调用方：旧世界树、语素树、需求树、任务树、方法树 SQL 投影和控制面板运行态镜像
触发路径：SQL 投影重写、字段恢复比对、控制面板运行态同步
输入：连接串、SQL 文本、预期行集、命令超时
输出：查询行集、字段恢复比对结果、错误文本、局部 bool
读结构：SQL Server 投影库、ADO 结果集、COM 错误
写结构：SQL 命令可能写投影库；新项目当前不得接 SQL 作为运行期事实源
是否写机器事实：否；旧注释明确查询和字段恢复比对只服务显示 / 诊断 / 存储验证
是否依赖日志：错误路径可写日志或弹窗
是否依赖显示：间接服务控制面板
是否依赖 SQL：是
是否依赖线程：COM 初始化与当前线程相关，但线程不成为事实来源
是否依赖外设 / D455 / 体素：否
保留行为：保留“存储验证只能验证投影材料能否读回”的诊断方向
删除行为：删除 SQL 查询、字段恢复比对或 ADO 命令结果反向裁决运行期节点 / 关系 / 状态 / 任务 / 方法事实的口径
新增约束：新项目持久化恢复必须先有快照 / 事件规范和恢复详细设计；恢复后仍需结构入口复核
目标新结构：后续持久化适配器、快照材料、事件日志审计材料
目标领域服务入口：事件日志持久化恢复专项 / 持久化适配服务
对应规范 / 详细设计：`规范/日志系统规范.md`；缺事件日志持久化恢复详细设计
入口拒绝条件：连接串为空、SQL 为空、COM 初始化失败、ADO 连接失败、SQL 写入开关关闭、试图用 SQL 结果裁决业务事实
验证方式：SQL 不可用时业务结构不回滚；字段恢复比对失败只能作为诊断；关闭 SQL 写入开关不得写库
初步判定：后续专项
初步迁移口径：当前只作为后续持久化适配专项输入，不进入代码实施
待用户确认项：事件持久化恢复专项是否继续使用 SQL Server ADO，还是先采用文件快照 / 事件日志文件
扫描证据：数据库ADO模块.ixx:1-7、48-101；数据库ADO模块.cpp:1-6、722-887
```

### OF-185

```text
旧函数编号：OF-185
扫描批次：FS-09 / FS-10 / FS-X
旧项目路径：D:\鱼巢\数据库ADO模块.cpp
旧函数名：初始化SQL控制面板运行态投影、同步写入SQL控制面板显示项、同步删除SQL控制面板显示项、追加SQL控制面板事件流水、写入SQL控制面板同步状态
旧函数签名：SQL 控制面板运行态镜像和事件流水函数族
归属逻辑包：LP-FS09-SQL-02 / SQL 控制面板运行态镜像排除边界
当前调用方：旧控制面板、旧需求 / 任务 / 方法 / 世界 / 动态显示同步入口
触发路径：控制面板启动、显示项新增 / 更新 / 删除、同步状态写入和事件流水追加
输入：显示域、对象主键、父主键、状态值、摘要、同步域、最近阶段和错误文本
输出：SQL 控制面板运行批次、当前显示项、控制面板事件流水、同步状态
读结构：控制面板运行 ID、SQL 投影表
写结构：SQL 投影库运行态表、控制面板事件流水、同步状态
是否写机器事实：否；旧文件头已声明只作为控制面板显示镜像
是否依赖日志：错误路径可写日志或弹窗
是否依赖显示：是
是否依赖 SQL：是
是否依赖线程：否
是否依赖外设 / D455 / 体素：否
保留行为：保留后续显示层只读视图可用的显示项和同步状态字段语义
删除行为：删除本轮接入控制面板、SQL 实时投影和控制面板事件流水的实现
新增约束：显示层只读结构，不写业务事实；事件流水不得冒充动作动态或事件日志权威记录
目标新结构：无当前目标结构；后续显示层只读视图专项或持久化审计专项
目标领域服务入口：后续显示层只读视图专项
对应规范 / 详细设计：`计划/20260706_函数迁移前置专项_v0.1.md`；缺显示层只读视图详细设计
入口拒绝条件：主键为空、显示域非法、操作为空、运行态 SQL 不可用、试图把控制面板事件流水作为机器事实
验证方式：本轮不接入；后续只读视图必须证明不写节点、主信息、关系、索引、状态、动态或因果事实
初步判定：排除 / 后续专项
初步迁移口径：转 FS-10 显示层只读视图专项或后续持久化审计专项
待用户确认项：无
扫描证据：数据库ADO模块.cpp:560-702、888-1087
```

### OF-186

```text
旧函数编号：OF-186
扫描批次：FS-09 / FS-08 / FS-10
旧项目路径：D:\鱼巢\世界树类.h、D:\鱼巢\世界树类.cpp
旧函数名：重写世界树SQL投影、校验世界树SQL投影往返、私有_执行世界树SQL字段恢复比对、私有_验证世界树SQL存储字段
旧函数签名：世界树 SQL 快照投影和往返校验函数族
归属逻辑包：LP-FS09-SNAPSHOT-01 / 世界树 SQL 快照和字段恢复验证
当前调用方：旧世界树 SQL 投影、控制面板显示、诊断校验入口
触发路径：世界树快照重写、SQL 往返校验、字段恢复比对
输入：来源原因、是否执行字段恢复比对、连接串、预期节点 / 主信息 / 关系行集
输出：SQL 快照表、字段恢复比对结果、往返校验结果、运行日志
读结构：旧世界树节点链、主信息分表、关系投影、SQL 快照表
写结构：SQL 世界树快照、节点、主信息和关系投影表
是否写机器事实：否；旧文件头明确 SQL 投影和字段恢复比对不得反向作为世界树业务判断来源
是否依赖日志：是
是否依赖显示：是
是否依赖 SQL：是
是否依赖线程：否
是否依赖外设 / D455 / 体素：否
保留行为：保留“从权威内存结构生成只读快照并校验可读回”的诊断方向
删除行为：删除 SQL 快照裁决运行期世界事实、从 SQL 恢复直接覆盖内存事实的口径
新增约束：新项目持久化快照必须有仓库快照规范、版本、枚举数值锁定和恢复入口拒绝；恢复材料必须经仓库 / 领域服务复核
目标新结构：后续仓库快照、事件日志、审计材料
目标领域服务入口：事件日志持久化恢复专项 / 持久化适配服务
对应规范 / 详细设计：`规范/日志系统规范.md`；缺事件日志持久化恢复详细设计
入口拒绝条件：来源原因缺失、SQL 不可用、恢复行集与预期不一致、快照版本不匹配、试图反向裁决运行期事实
验证方式：快照清空不影响内存节点 / 关系；字段恢复比对失败不回滚业务结构；恢复专项另行验证
初步判定：后续专项
初步迁移口径：保留为后续快照 / 审计设计输入，不接旧 SQL 投影
待用户确认项：是否先设计通用仓库快照格式，再决定是否映射 SQL
扫描证据：世界树类.h:134-137；世界树类.cpp:5、1902-1998、2371-2535
```

### OF-187

```text
旧函数编号：OF-187
扫描批次：FS-09 / FS-04 / FS-05 / FS-06 / FS-10
旧项目路径：D:\鱼巢\语素类.cpp、D:\鱼巢\需求类.cpp、D:\鱼巢\任务类.cpp、D:\鱼巢\方法类.cpp
旧函数名：重写语素SQL投影、重写需求树SQL投影、重写任务树SQL投影、重写方法树SQL投影、各自私有_SQL字段恢复比对函数
旧函数签名：领域树 SQL 快照投影和字段恢复比对函数族
归属逻辑包：LP-FS09-SNAPSHOT-02 / 领域树 SQL 快照和字段恢复验证
当前调用方：旧语素 / 需求 / 任务 / 方法树投影、控制面板显示、诊断校验入口
触发路径：领域树变化后重写 SQL 投影，或人工触发字段恢复比对
输入：来源原因、根节点、是否执行字段恢复比对、领域树行集
输出：SQL 快照、字段恢复比对结果、运行日志
读结构：旧语素树、需求树、任务树、方法树、旧主信息字段和 SQL 快照表
写结构：SQL 领域树快照、节点表、主信息表
是否写机器事实：否；旧注释和错误处理均指向显示 / 诊断 / 存储验证
是否依赖日志：是
是否依赖显示：是
是否依赖 SQL：是
是否依赖线程：否
是否依赖外设 / D455 / 体素：否
保留行为：保留领域树可生成只读诊断快照的方向
删除行为：删除把 SQL 快照字段当作语素、需求、任务、方法权威字段或恢复源的口径
新增约束：新项目领域事实必须由节点、主信息、关系、索引、状态、动态、因果和领域服务承载；SQL 只能做后续快照 / 审计适配
目标新结构：后续领域树只读视图、仓库快照、事件日志审计材料
目标领域服务入口：显示层只读视图专项 / 事件日志持久化恢复专项
对应规范 / 详细设计：`规范/日志系统规范.md`；缺显示层只读视图详细设计、缺事件日志持久化恢复详细设计
入口拒绝条件：来源原因缺失、根节点无效、SQL 不可用、字段恢复不一致、试图用 SQL 投影字段回写业务事实
验证方式：SQL 投影失败不回滚领域结构；显示层读取不写业务事实；快照恢复专项另行验证
初步判定：后续专项
初步迁移口径：转后续显示层只读视图和事件日志持久化恢复专项，不在 FS-09 直接编码
待用户确认项：领域树快照是否优先统一为仓库级快照，还是保留领域视图快照
扫描证据：语素类.cpp:5、895-1058；需求类.cpp:5、840-937、2721-2771；任务类.cpp:5、1316-1455；方法类.cpp:5-6、1827-1915、2403-2448
```

### OF-188

```text
旧函数编号：OF-188
扫描批次：FS-09 / FS-01
旧项目路径：D:\鱼巢\双向链表模板_序列化.inl、D:\鱼巢\说明书\双向链表模板序列化说明.md
旧函数名：保存数据_已加锁、保存数据、读取数据_已加锁、读取数据、_写POD、_读POD、_写字符串、_读字符串、_索引或空、_由索引取指针
旧函数签名：旧双向链表模板二进制保存和读取函数族
归属逻辑包：LP-FS09-SERIAL-01 / 旧链表模板二进制保存恢复
当前调用方：旧双向链表模板使用方；旧说明书作为用法依据
触发路径：旧链表持久化保存、读取恢复、工具或人工调用
输入：文件名、链表节点、主信息、节点指针关系、二进制流
输出：二进制存档文件、恢复后的旧链表节点和指针关系、局部 bool
读结构：旧链表根指针、全链环、父子树、同层环、主信息
写结构：二进制文件；读取成功时替换旧链表内存结构
是否写机器事实：旧项目读取会替换内存树；新项目不得直接迁移该结构
是否依赖日志：否
是否依赖显示：否
是否依赖 SQL：否
是否依赖线程：使用锁，不代表线程是事实来源
是否依赖外设 / D455 / 体素：否
保留行为：保留保存前版本 / 魔数 / 数量检查、读取失败不替换旧树、读取后需结构复核的方向
删除行为：删除旧双向链表指针关系作为新项目核心持久化格式，删除直接反序列化旧主信息对象覆盖新仓库事实
新增约束：新项目长期身份必须使用稳定句柄、仓库编号、本地编号和版本号；快照恢复必须校验枚举数值、关系版本和入口拒绝
目标新结构：后续仓库快照格式、事件日志恢复材料
目标领域服务入口：事件日志持久化恢复专项 / 仓库快照服务
对应规范 / 详细设计：`规范/000_项目规则总纲.md`、`规范/节点类型与关系类型枚举规范.md`；缺事件日志持久化恢复详细设计
入口拒绝条件：文件打不开、魔数不匹配、版本不支持、节点数量非法、字符串超限、主信息反序列化失败、关系索引越界、试图恢复旧链表覆盖新仓库
验证方式：读取失败不改变当前结构；恢复后必须经仓库结构校验和领域入口复核；跨版本格式必须拒绝
初步判定：后续专项
初步迁移口径：只作为持久化恢复反例和设计输入，不迁移旧双向链表模板
待用户确认项：后续快照格式是否采用仓库级快照 + 事件日志，而不是旧链表二进制格式
扫描证据：双向链表模板_序列化.inl:4-6、73-148、153-246；说明书/双向链表模板序列化说明.md:1-11、76-133
```

### OF-189

```text
旧函数编号：OF-189
扫描批次：FS-09 / FS-03 / FS-08
旧项目路径：D:\鱼巢\特征值主信息类.h
旧函数名：特征值主信息类::序列化、特征值主信息类::反序列化
旧函数签名：特征值主信息保存恢复函数族
归属逻辑包：LP-FS09-SERIAL-02 / 特征值主信息序列化字段边界
当前调用方：旧链表模板序列化、旧特征值保存恢复路径
触发路径：旧特征值主信息保存、读取恢复、比较或去重缓存重建
输入：特征值类型、轮廓维度、VecU 原始值、内容哈希、命中次数、最后命中
输出：序列化字节流、反序列化后的旧特征值主信息
读结构：旧特征值主信息字段
写结构：旧特征值主信息对象和二进制流
是否写机器事实：旧项目恢复会重建旧主信息；新项目不得新增旧特征值主信息字段
是否依赖日志：否
是否依赖显示：否
是否依赖 SQL：否
是否依赖线程：否
是否依赖外设 / D455 / 体素：VecU 可被体素使用，但本函数不接体素模块
保留行为：保留 Vec 原始值可作为权威值材料、哈希可由原始值重建的方向
删除行为：删除把内容哈希、命中次数和最后命中作为权威特征值字段恢复到新主信息的口径
新增约束：特征值服务只服务特征系统，只有特征服务可直接访问；高级服务不得直接恢复或调用特征值服务
目标新结构：后续特征值原始值容器、非权威内容哈希缓存、事件恢复材料
目标领域服务入口：特征服务 / 后续特征值系统增强专项 / 事件日志持久化恢复专项
对应规范 / 详细设计：`规范/非权威缓存与统计规范.md`、`规范/详细设计/非权威缓存与统计详细设计.md`；缺特征值原始 Vec 增强详细设计
入口拒绝条件：缺原始值、类型非法、缓存命名空间缺失、高级服务越权恢复特征值、试图把哈希命中字段恢复为权威事实
验证方式：清空哈希和命中缓存后仍可由特征服务读取权威特征状态材料；恢复路径必须重建缓存而非信任旧字段
初步判定：后续专项
初步迁移口径：转特征 / 特征值系统增强专项和非权威缓存专项，不直接迁移旧字段
待用户确认项：旧序列化材料中的哈希 / 命中字段在后续恢复时应忽略、校验后重建，还是作为非权威缓存导入
扫描证据：特征值主信息类.h:91-132
```

### OF-190

```text
旧函数编号：OF-190
扫描批次：FS-09 / FS-05 / FS-07 / FS-X
旧项目路径：D:\鱼巢\任务模块.工作线程协议.ixx、D:\鱼巢\自我动作实现.外设模块.ixx
旧函数名：结构_任务推进事件、同步工作线程事件语义槽、自我动作实现外设模块日志和快照输出函数族
旧函数签名：线程推进事件消息和外设日志排除边界
归属逻辑包：LP-FS09-X-01 / 线程事件与外设日志排除边界
当前调用方：旧任务管理工作线程、自我动作实现外设路径、观察变化和场景快照导出路径
触发路径：线程推进消息、外设动作执行、观察变化事件生成、场景识别快照导出、运行日志输出
输入：任务推进事件类型、事件语义槽、外设观察材料、动作上下文、场景快照
输出：线程消息、运行日志、观察变化事件材料、场景识别快照材料
读结构：旧任务线程上下文、自我动作上下文、外设材料、场景快照
写结构：线程消息、日志、外设 / 体素相关结构；当前不进入新项目
是否写机器事实：待确认；线程消息不是事实来源，外设材料必须后续专项设计
是否依赖日志：是
是否依赖显示：可间接显示
是否依赖 SQL：否
是否依赖线程：是
是否依赖外设 / D455 / 体素：是
保留行为：保留“线程消息只能作为调度 / 通知材料，外设材料必须经领域服务入账”的边界
删除行为：删除把线程事件、日志或外设快照直接当作动作来源、事件日志或世界事实的口径
新增约束：线程不是动作来源；外设输入只作为材料或请求；动作动态来源必须来自方法执行或领域服务写入
目标新结构：无当前目标结构；后续外设材料、显示层只读视图或动作动态专项
目标领域服务入口：后续外设 / 显示 / 动作动态专项
对应规范 / 详细设计：`计划/20260706_函数迁移前置专项_v0.1.md`；缺外设和显示层详细设计
入口拒绝条件：试图把线程事件或日志写成机器事实、试图把外设材料直接写成世界事实、试图把快照导出当持久化恢复
验证方式：本轮不接入；后续专项必须证明线程 / 外设 / 显示只作为材料或只读视图，不写业务事实
初步判定：排除 / 后续专项
初步迁移口径：归入 FS-X 外设 / 体素排除边界和后续显示层只读视图专项
待用户确认项：无
扫描证据：任务模块.工作线程协议.ixx:10-33；自我动作实现.外设模块.ixx:大量 `项目运行日志`、观察变化事件和场景识别快照导出路径
```

## 4. 逻辑迁移包初步归并

| 逻辑包编号 | 逻辑能力 | 包含旧函数 | 新项目结构承载 | 目标领域服务 | 当前结论 |
| --- | --- | --- | --- | --- | --- |
| LP-FS09-LOG-01 | 日志输出与人读诊断入口 | OF-183 | 日志文件、后续事件日志材料 | 日志系统 / 事件日志持久化恢复专项 | 当前已有日志入口，事件持久化待后续 |
| LP-FS09-SQL-01 | ADO 存储验证与 SQL 访问适配 | OF-184 | 后续持久化适配器、快照材料、审计材料 | 持久化适配服务 | 后续专项，缺详细设计 |
| LP-FS09-SQL-02 | SQL 控制面板运行态镜像排除边界 | OF-185 | 无当前目标结构 | 显示层只读视图专项 | 排除 / 后续专项 |
| LP-FS09-SNAPSHOT-01 | 世界树 SQL 快照和字段恢复验证 | OF-186 | 后续仓库快照、事件日志、审计材料 | 持久化适配服务 | 后续专项，不能反向裁决事实 |
| LP-FS09-SNAPSHOT-02 | 领域树 SQL 快照和字段恢复验证 | OF-187 | 后续领域树只读视图、仓库快照、审计材料 | 显示层只读视图 / 持久化适配服务 | 后续专项 |
| LP-FS09-SERIAL-01 | 旧链表模板二进制保存恢复 | OF-188 | 后续仓库快照格式、事件日志恢复材料 | 仓库快照服务 | 后续专项，不迁移旧链表格式 |
| LP-FS09-SERIAL-02 | 特征值主信息序列化字段边界 | OF-189 | 特征值原始值容器、非权威缓存、恢复材料 | 特征服务 / 特征值增强专项 | 后续专项，不迁移旧字段 |
| LP-FS09-X-01 | 线程事件与外设日志排除边界 | OF-190 | 无当前目标结构 | 后续外设 / 显示 / 动作动态专项 | 排除 / 后续专项 |

## 5. 本轮结论

1. 旧日志模块只能作为新项目日志边界和事件日志后续设计输入，不能把日志文本当机器事实。
2. 旧 ADO 字段恢复比对只证明 SQL 投影材料可读回，不证明 SQL 是运行期事实源。
3. 旧 SQL 控制面板运行态事件流水不是新项目动作动态，也不是事件日志持久化恢复的直接实现。
4. 旧世界树 / 领域树 SQL 快照可作为后续快照和显示只读视图设计输入，但不得反向恢复覆盖当前仓库事实。
5. 旧双向链表模板二进制保存恢复不迁入新项目核心；新项目快照恢复必须围绕稳定句柄、枚举数值锁定、关系版本和仓库入口拒绝另行设计。
6. 旧特征值主信息序列化中的哈希、命中次数和最后命中只能作为非权威缓存或恢复重建参考，不能恢复为权威字段。
7. 旧线程推进事件、外设日志和场景快照导出不进入本轮迁移。

## 6. 下一步

```text
下一旧函数编号：OF-191
下一步：继续 FS-10，扫描旧项目显示层只读候选函数第一批。
```

若后续发现 FS-09 遗漏，只能追加登记，不得重写既有编号或把当前记录解释为迁移许可。

## 7. 禁止扩大声明

```text
不得声明事件日志持久化恢复已实现。
不得声明旧 SQL / ADO / 控制面板投影已迁移。
不得声明旧链表二进制保存恢复格式可作为新项目快照格式。
不得把日志、字段恢复比对、SQL 快照、控制面板事件流水、线程事件或外设快照作为运行期机器事实。
不得接控制面板 / SQL / D455 / 体素 / 外设。
```
