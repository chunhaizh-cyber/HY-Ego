# 逐函数只读扫描清单生成 Codex 断点清单

日期：2026-07-06
状态：已生成 / 待执行扫描 / 未改代码 / 未扫描旧函数 / 未迁移旧函数

## 1. 输入入口

用户要求：

```text
确认,生成 逐函数只读扫描清单
```

## 2. 核心处理

```text
1. 基于已确认的函数迁移前置专项生成逐函数只读扫描清单。
2. 将扫描拆分为 FS-00 到 FS-10，以及 FS-X 排除项。
3. 建立逐函数登记模板和空槽位。
4. 同步计划索引和项目记忆。
5. 不执行旧项目扫描，不改代码，不迁移旧函数。
```

## 3. 结构承载

```text
实施记录/20260706_逐函数只读扫描清单.md
实施记录/20260706_逐函数只读扫描清单生成_Codex断点清单.md
计划/计划索引.md
项目记忆/当前状态.md
项目记忆/决策记录.md
项目记忆/实施记录.md
项目记忆/待确认问题.md
项目记忆/Codex任务队列.md
项目记忆/验收记录.md
```

## 4. 输出变化

```text
逐函数只读扫描清单已生成。
后续可以从 FS-00 开始复核扫描环境，再逐批登记旧函数。
登记完成后仍需用户确认判定。
```

## 5. 验证方式

本切片验证：

```text
rg 检索清单、扫描批次、逐函数登记模板和禁止边界。
git diff --check。
git status 检查暂存范围。
```

本切片未运行：

```text
旧项目函数扫描
msbuild
默认入口程序
控制面板 / SQL / D455 / 体素样例
```

## 6. 禁止扩大声明

```text
不得声明旧函数已扫描完成。
不得声明旧函数已迁移。
不得声明旧函数体可以复制。
不得声明旧主信息字段已迁移。
不得声明旧项目能力迁移完成。
不得声明控制面板、SQL、D455、体素或外设已接入。
不得声明代码已编译或入口已重新验证。
```
