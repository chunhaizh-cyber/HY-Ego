# RUNTIME-TXN-S2 服务装配完整集合与真实前置合同漂移修订 Codex 断点清单

日期：2026-07-12

治理编号：JY-295

状态：#248 执行前实际接口漂移已完成设计修订 / 未改 C++ / 未构建 / 未运行程序

## 1. 输入入口

```text
#248 执行窗口实际接口复核回传
HEAD 7157f9e1d92778b552438d86807d93b2e1dd332a
#247 / JY-293 正式运行期结构核心
入口.cpp 当前生产构造顺序
#246 主装配与清理四表
#221-#225、#214 后继计划
```

## 2. 真实漂移

原 #248 只笼统写“全部服务同域”，没有最终成员、构造依赖、接线能力、初始化读回或清理所有权矩阵。允许清单遗漏世界、特征值、概念图 / 需求初始化、统计和显示等真实入口对象，也没有裁决快照、控制面板、SQL 与线程是否属于完整性。

同时，#221-#223、#214 尚会改变任务、需求、存在、状态、方法、语素、动态和概念图正式接口；#248 若只依赖 #247 先行，会形成临时装配并再次返工。

## 3. 裁决

1. 长期服务成员精确冻结为存在至显示 16 项，全部引用唯一上下文四仓库。
2. 四个初始化服务只在全新候选工厂局部存在，生成系统角色材料后销毁。
3. `完整()` 同时验证结构核心、16 服务同仓库身份和系统角色材料读回。
4. 快照、控制面板、SQL、线程和算法 / 路由模块是发布后消费者，不计入 `完整()`。
5. #248 硬前置改为 #214，传递包含 #221-#223；#224/#225 调用期模块不属于长期成员。
6. “同域”分为同仓库实例、必要服务值式接线、正式业务同令牌能力，不给纯只读 / 无状态服务虚构接线。

## 4. 允许范围

新建 `装配.领域服务`、`自检.领域接线`；回改 #247 两模块、入口与工程。清理修改只限 `初始化.世界树.ixx`、`初始化.语素.ixx`、`语素服务.h`、`概念图服务.h` 的 #246 命中点。其它 16 服务头、`初始化.概念图.ixx`、`初始化.需求.ixx` 和核心 ABI 均为只读依赖。

## 5. 执行前复核

执行窗口必须从 #214 后 clean HEAD 逐项比对 16 个构造函数、四初始化器、系统角色字段、值式接线 / 令牌能力、清理点和 670 占用。出现第 17 个长期成员、构造参数、初始化角色或允许文件漂移时不得自行扩表。

## 6. 验证边界

本治理只运行 Mermaid 同源、严格规范和文档差异检查，不运行 C++ 构建或程序。未证明 #221/#214/#248 已完成，未证明入口切换、快照 / 恢复、控制面板接线或旧能力迁移完成。
