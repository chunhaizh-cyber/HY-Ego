# WAREHOUSE-OPT-S1 定向提交算法正式性能门禁失败 Codex 断点清单

日期：2026-07-16

决策：JY-356

计划 / 队列 / 阶段：#258 / DQ-150 / 800

交互：WIN-0001-E72

状态：定向提交算法 WIP 已形成 / Debug x64 完整 Rebuild 0 警告 0 错误 / 首次正式冷却运行退出 1 / A01-A12、A14 通过而 A13 失败 / 按 JY-356 停止后续运行 / 默认不实施、不发布 S1 / WIP 未暂存未提交 / 退回设计 / 800 未登记

## 1. 输入入口

```text
正式设计提交 0d2d2413e18ec190d48f42fb8c834969457a8f77
执行前本地主线 b633aaa96cfc0aa82da667ee9b9dd195a753e78
流程图/20260713_数据仓库关系查询二级索引与性能治理流程图_v0.1.md
规范/详细设计/数据仓库关系查询二级索引与性能治理详细设计.md
计划/20260713_WAREHOUSE-OPT-S1_关系仓库二级索引代码实施切片_v0.1.md
实施记录/20260715_WAREHOUSE-OPT-S1_性能门禁失败设计裁决与提交算法修订_Codex断点清单.md
海中鱼巣/核心/关系仓库.h
海中鱼巣/核心/关系仓库.cpp
海中鱼巣/核心/自检.关系仓库性能基线.ixx
海中鱼巣/入口.cpp
```

执行工作树在保留四文件 WIP 的前提下由 `b633aaa` 纯快进到 `0d2d241`，没有冲突，没有第五个 dirty 文件，暂存区保持为空。

## 2. 已形成但未发布的算法重做

本轮按 JY-356 逐段重做四文件 WIP：

```text
四类关系编号候选桶由嵌套 unordered_set 收敛为紧凑 vector<uint64_t>。
关系编号保存对应桶指针与位置附着元数据。
转换路径先一次定位受影响桶并预备目标桶，再定向发布、swap-pop 移除和更新交换项位置。
中途失败按已发布步骤逆序回退；空桶保留复用，避免热路径即时分配和回收。
有效关系失效和逻辑删除增加无分配快速路径。
关系记录索引完整_已加锁退出普通切换热路径，只保留确认与 Debug 全量诊断用途。
自检新增材料完整独立判定，A02-A06 不再被三轮稳定总布尔联动压成失败；A13 仍独立要求全部稳定与性能硬门禁。
```

关系表仍是唯一权威；查询仍须按关系编号回表复核完整句柄、版本、状态、类型、端点和顺序号。旧共享兼容删除、严格独占删除、逻辑删除保留权威记录和全部事务 ABI 原义不变。

这些改动只存在于执行工作树的四个未暂存文件中，不是正式实现事实。按 JY-356，本轮正式门禁失败后不得继续修改、暂存、提交或发布这些文件。

## 3. 构建、正确性与结构证据

构建结果：

```text
Debug x64 增量构建：通过，0 warning / 0 error。
Debug x64 完整 Rebuild：通过，0 warning / 0 error。
```

首次正式 `--self-test-exit` 结果：

```text
退出码：1。
WAREHOUSE-OPT-S1-A01 至 A12：通过。
WAREHOUSE-OPT-S1-A13：失败。
WAREHOUSE-OPT-S1-A14：通过。
```

三档结构签名保持：

```text
1,000   c4704f13e23e41b9
10,000  f44904a873488f65
100,000 2e595d5cba78a2c1
```

A01-A12 证明入口拒绝、查询正确性、创建故障回滚、失效 / 撤销、重挂、逻辑删除、许可原义和并发原子可见专项在本次 WIP 中成立；A14 证明专项候选检查数来自实际索引桶。它们不能覆盖 A13 的稳定性和性能硬门禁失败。

## 4. 首次正式性能验收

环境固定为当前机器、Debug x64、平衡电源方案、固定种子、相同夹具、完整 Rebuild 后等待 75 秒、4 线程 90% 读 / 10% 重挂写。

正式结果：

```text
三档查询轮波动：116.3% / 361.9% / 238.6%。
稳定门槛：每档 <= 15%。

1,000 关系逻辑删除 P95：1.1 us；冻结上限：1.05 us；超线。
10,000 关系失效 P95：2.5 us；冻结上限：4.35 us；通过。
100,000 关系失效 P95：1.6 us；冻结上限：11.85 us；通过。
100,000 关系逻辑删除 P95：1.1 us；冻结上限：4.95 us；通过。
```

低绝对耗时不构成放宽门槛的依据。1,000 关系逻辑删除 P95 已超过 #257 冻结基线的 150% 上限，且三档稳定性全部超过 15%；因此 A13 真实失败。

## 5. 停止原因与默认裁决

JY-356 和目标计划明确规定：算法重做后至少两次独立正式 Debug 运行均须通过 D-H；任一次写入超线即再次退回；本轮仍失败时默认不实施、不发布 S1，不再修改阈值追逐实现。

首次正式运行已经满足停止条件，执行窗口因此不得继续：

```text
运行第二次正式性能验收。
运行 Release x64 或 Release 零登记检查。
运行最终 Debug 连续 20/20。
继续修改或调参四文件 WIP。
放宽 150% 写入或 15% 稳定门槛。
覆盖 #257 S0 基线，伪造 A13，或用 A01-A12 / A14 替代 A13。
登记 800、归档 #258、释放 #259-#261 或恢复 #214。
暂存、提交或推送四文件 WIP。
```

## 6. 输出变化

```text
#258 / DQ-150：JY-356 原编号重新待执行 -> 定向提交算法正式性能硬门禁再次失败 / 退回设计 / 默认不实施、不发布 S1。
A01-A14：A01-A12、A14 通过，A13 失败；未全部通过。
阶段 800：未实施、未登记。
#259 / DQ-151：继续等待 #258、800，不得执行 810。
#260 / DQ-152：继续等待 #259 裁决，不得执行 820。
#261 / DQ-153：继续等待 #258、800，不得执行 830。
#214 / DQ-106：继续用户暂停，590 未实施。
WIN-0001：#258 治理所有权转回设计计划窗口；四文件 WIP 保持未暂存未提交。
```

## 7. 验证边界

```text
Debug x64 增量构建：通过，0 warning / 0 error。
Debug x64 完整 Rebuild：通过，0 warning / 0 error。
首次正式完整 --self-test-exit：退出 1，A13 失败。
第二次正式性能验收：按 JY-356 停止，未执行。
Release x64：未执行。
最终 Debug 连续 20/20：未执行。
阶段 800：未登记。
治理文件严格规范：`python .\\tools\\check_specs.py --strict` 通过；`git diff --check` 通过；暂存范围检查在本轮提交前继续执行。
```

本记录只证明定向提交算法 WIP 在固定夹具中通过 A01-A12、A14，但没有通过 A13 和 #258 完整性能验收。不得声明二级索引、A01-A14、阶段 800、性能改善、仓库优化、令牌优化、分片、显示投影、旧能力迁移或项目迁移完成。
