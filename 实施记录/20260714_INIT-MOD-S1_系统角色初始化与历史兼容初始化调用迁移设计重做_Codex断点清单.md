# INIT-MOD-S1 系统角色初始化与历史兼容初始化调用迁移设计重做 Codex 断点清单

日期：2026-07-14

状态：JY-337 / WIN-0001-E47 / #250、DQ-142 重新待执行 / 690 未实施 / 纯文档治理

## 1. 输入入口

```text
WIN-0001-E46 执行窗口正式回传。
正式基线 f2cc5bb。
#270 / DQ-162 / SERVICE-COMPOSER-S1 / 770 已完成产物。
原 #250 流程图、详细设计和实施计划。
当前 装配.运行期业务、启动.运行期上下文、四个历史初始化文件、工程登记和全部 include 调用点。
```

## 2. 当前事实

1. 唯一运行期业务装配已经拥有七个数据操作、十四个业务服务、六个组合器和八类顶层业务操作，但没有系统角色初始化入口。
2. 运行期上下文已经接入候选业务装配，`完整()` 仍固定为假；系统角色材料缺失时不发布上下文、不生成租约。
3. 四个历史初始化 `.ixx` 仍是 include 兼容文件，当前有 11 个文件直接 include；旧世界树、旧语素和旧线程路径不能进入新系统角色初始化。
4. #267 的二次特征长期组成项白名单拒绝动态节点；关系根不能按 `[存在根, 动态根]` 创建，必须在因果根形成后以 `[存在根, 因果根]` 创建。
5. #275 已验证四根角色类型为存在、抽象动态、二次特征和因果引用；根身份由对应原子业务服务创建，再由概念图结构服务按稳定键和完整句柄复核。
6. 当前虚拟存在来源只接受需求、任务、方法节点；概念存在根必须经存在业务服务创建实际存在，不得把世界根扩成虚拟存在来源。

## 3. 核心裁决

JY-337 固定双轨收口：

```text
新系统角色初始化路径：
候选运行期上下文 -> 唯一运行期业务装配 -> 系统角色初始化器
-> 新业务服务 / 既有组合器 -> 系统角色数据操作 -> 不透明结构写入会话
-> 权威读回 -> 版本 1 系统角色清单 -> 候选上下文完整。

历史兼容初始化路径：
四个历史初始化文件原地转换为 ClCompile 真模块
-> 当前 include 调用点只迁为 import
-> 兼容正文和结果不改义
-> 禁止进入新系统角色初始化路径。
```

版本 1 系统角色清单包含世界根、自我场景、自我存在及世界拓扑，两组特征当前状态 / 目标状态 / 根需求，概念存在 / 动态 / 关系 / 因果四根和方法登记根。旧 18 个显示语素、名称裁决和旧坐标 I64 槽不进入清单。

## 4. 结构承载与失败边界

1. 系统角色清单只保存稳定键、完整句柄和权威关系证据，不建立平行事实仓库。
2. 无效版本、零键、重复键、值域错误和已占用键异义在第一笔写入前逻辑内返回。
3. 独立服务步骤已提交后，后继未完成只表示系统角色清单未形成；已提交对象必须各自是稳定键可读的完整事实，不得宣称物理数量完全不变。
4. 前置通过后的类型、主信息、关系、目标状态或主键读回矛盾归追根因解决，不得扫描修复或切回旧初始化路径。
5. #250 完成后候选上下文可达到完整，但宿主仍返回“首次发布尚未启用”；真实首次发布、租约和恢复链继续由后继 #249 承接。

## 5. 输出变化

```text
新增流程图 MD / HTML：系统角色初始化与历史兼容初始化调用迁移。
新增详细设计：系统角色初始化与历史兼容初始化调用迁移。
原 #250 计划升级为 v0.2，保留 #250 / DQ-142 / 690。
旧流程图和旧详细设计标记为已由 JY-337 替代。
#250 转当前待执行；#249 等 #250 正式完成后重做三件套；#271 继续等待 #250 -> #249 和精确兼容矩阵。
```

本批不修改 C++、工程、filters，不运行构建或程序，不登记 690。

## 6. 允许与禁止边界

执行窗口必须以修订计划列出的允许文件为唯一范围。允许新增系统角色清单数据头、系统角色数据操作、系统角色初始化器和独立自检模块；允许修改唯一业务装配、候选上下文、四个历史初始化模块、11 个 include 调用点及最小工程 / 入口登记。

禁止修改四仓库、事务 ABI、既有七个数据操作、十四个业务服务、六个组合器业务语义、线程正文、持久化、控制面板、SQL、D455，以及概念生命周期、统计缓存和安全删除。禁止发布上下文、生成租约、新增线程或把旧初始化模块注入新装配。

## 7. 验证与执行前复核

设计治理验证：

```text
新流程图 Markdown / HTML Mermaid 同源。
python .\tools\check_specs.py --strict 通过。
git diff --check 通过。
C++、工程和 filters 差异为 0。
```

执行窗口必须从本次正式提交重新读取实际公开签名、工程登记、11 个 include 调用点和阶段 690 占用。任一接口不足、出现新增调用点需修改业务正文、形成模块循环或允许文件不足时，保持代码零修改，以原 #250 / DQ-142 退回设计。

## 8. 完成声明边界

本记录只证明 #250 三件套和执行合同已经按 #270 正式接口重做并重新入队，不证明系统角色初始化代码、历史模块迁移、构建、自检、上下文发布、租约、恢复链、生产入口切换或旧能力迁移完成。
