# TASK-EXECUTION-S1 正式接口复核与重新发布 Codex 断点清单

日期：2026-07-15

决策：JY-344

计划 / 队列 / 阶段：#224 / DQ-116 / 600

交互：WIN-0001-E57

状态：正式接口逐签名复核通过 / #224 当前待执行 / A01-A18 与 600 未实施 / 未改 C++ / 未构建 / 未运行

## 1. 输入入口

```text
正式基线：2cfb31b
#271 / DQ-163 / COMPAT-CLOSURE-S1 / 780 正式归档产物
流程图/20260715_任务执行调度与强类型回执代码逻辑流程图_v0.1.md/.html
规范/详细设计/任务执行调度与强类型回执详细设计.md
计划/20260711_TASK-EXECUTION-S1_通用执行调度与强类型回执代码实施切片_v0.1.md
海中鱼巣/领域/服务.任务.ixx
海中鱼巣/领域/服务.方法.ixx
海中鱼巣/领域/服务.状态.ixx
海中鱼巣/领域/数据操作.状态动态.ixx
海中鱼巣/领域/组合.状态动态.ixx
海中鱼巣/线程/任务管理线程.ixx
海中鱼巣/线程/任务工作线程.ixx
海中鱼巣/线程/任务结果回执协议.ixx
```

## 2. 前置状态

```text
#271 / DQ-163：已完成并归档，A01-A18 与 780 已登记。
#224 / DQ-116：JY-341 三件套已形成，直接前置已满足，等待实际接口复核。
#225 / DQ-117：继续等待 #224 正式完成和 600。
#214 / DQ-106：继续用户暂停，590 未实施。
阶段 600：未登记。
四个 #224 预期新模块：未占用。
```

## 3. 逐签名复核

| #224 需要的能力 | `2cfb31b` 正式接口 | 结论 |
| --- | --- | --- |
| 任务权威读取 | `任务业务服务::读取任务` | 一致；返回任务、归属需求、生命周期和当前选择值式材料 |
| 任务生命周期迁移 | `任务业务服务::迁移生命周期` | 一致；业务服务形成规格并进入统一数据操作层 |
| 动作桥材料 | `任务业务服务::读取动作桥请求` | 一致；保持任务、方法首、动作入口、稳定动作键、输入场景、输出场景六字段 |
| 方法登记材料 | `方法业务服务::读取登记项` | 一致；可复核当前登记身份和生命周期 |
| 方法动作规格 | `方法业务服务::读取规格组` | 一致；可按角色读取固定动作规格 |
| 状态材料 | `状态业务服务::读取状态材料` | 一致；返回值式状态材料，不暴露事务能力 |
| 实例状态规格 | `状态业务服务::形成实例状态写入规格` | 一致；构造权保持业务服务封闭 |
| 状态动态同会话提交 | `数据操作.状态动态` 既有会话参与路径 | 一致；允许在原计划范围内定向扩展已有前状态、新后状态和动态同会话路径 |

生命周期合法迁移已经覆盖：

```text
筹办中 -> 排队中
排队中 -> 执行中
排队中 / 执行中 / 等待中 -> 待重筹办
```

不需要恢复传统服务原始令牌下传，也不需要修改核心、四仓库、结构事务 ABI或运行期业务装配。

## 4. 队列与协议边界

```text
请求采用预留候选 -> 任务排队发布 -> 完整读回 -> 确认可消费。
工作线程只消费任务执行调度路由窄入口，不解释任务、方法、状态或动态结构。
强类型回执只承载非权威执行证据，不裁决任务完成或需求结算。
一个稳定动作键仍由组合器构造时显式配置，不扩为任意执行器或外设动作。
```

## 5. 允许与禁止范围

允许文件、四个新真模块分类、入口 / 工程最小登记和治理收口文件保持原 #224 第 5、6 节。阶段 600 和下列新模块名均未占用：

```text
协议.任务执行请求
组合.任务执行
路由.任务执行调度
自检.任务执行调度
```

继续禁止：

```text
海中鱼巣/核心/**
四仓库与结构事务 ABI
传统任务服务.h / 方法服务.h / 状态服务.h / 动态服务.h
运行期业务装配与运行期上下文
持久化、界面、SQL、外设与 D455
任务结果完成、需求结算和 #225 实施
```

## 6. 输出变化与调用路径

```text
#224 / DQ-116：前置满足待复核 -> JY-344 当前待执行。
阶段 600：仍未实施、未登记。
#225：继续等待 #224 与 600。
#257：接口复核通过，但执行顺序后于 #224。
#214：继续用户暂停。

#271 正式产物
-> 设计窗口逐签名复核 #224
-> 流程图 / 详细设计 / 原计划同步
-> JY-344 与 WIN-0001-E57
-> 计划索引、任务队列和项目记忆同步
-> 正式提交并快进推送
-> 执行窗口重读实际接口
-> 一致后实施 600；漂移则原编号退回设计
```

## 7. 验证方式与边界

```text
流程图 Markdown / HTML Mermaid 同源。
python .\tools\check_specs.py --strict。
git diff --check。
C++、工程和 filters 相对 2cfb31b 差异为 0。
阶段 600 与四个新模块名未占用。
计划、索引、队列、当前状态、决策、验收和交互记录一致。
```

本轮只执行只读代码复核和文档治理验证，不运行 C++ 构建、程序或自检。不得据此声明 #224、A01-A18、600、任务执行、真实动作、任务完成、需求结算、常驻生产线程、旧能力迁移或项目迁移已经完成。
