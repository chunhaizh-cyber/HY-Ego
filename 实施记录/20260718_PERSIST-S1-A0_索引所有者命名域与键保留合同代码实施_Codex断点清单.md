# PERSIST-S1-A0 索引所有者命名域与键保留合同代码实施 Codex 断点清单

日期：2026-07-19

计划：#304 / DQ-196 / PERSIST-S1-A0 v0.3

任务批次：WB-P0B-01 / WT-304-R1

冻结父提交：`33ae6bc3772dafa93a259de452bd509c739f10e8`

任务分支：`codex/304-a0-r1`

状态：执行完成待设计同步 / 分支完成待集成

## 1. 执行前复核

1. E182 / JY-437 已在 `origin/main@a7a06ed6ed7eb9d10753ad7c319b99e8c2594aea` 正式登记本 worktree、分支、冻结父提交、文件所有权和已派发状态；任务代码分支未合并中央治理。
2. 当前 `主键绑定记录` 原先只含主键和节点句柄；索引仓库与结构写入会话原先只提供二参数绑定。
3. 显式新运行期共有十二个生产绑定点：存在场景 3、状态动态 4、特征体系 1、语素基础 1、轻量因果 1、需求任务方法 1、用途观察 1。
4. 十二个调用点全部位于七个允许数据操作模块；系统角色初始化只组合调用，不直接形成第十三个绑定点。
5. `用途观察` 既有物理合同为命名域 `0x4F315332`、格式 / 探测规则版本 1、36 字节规范身份和最多 1024 次确定探测。

## 2. 实施结果

1. 新增 `核心/索引所有权.数据.h`，唯一声明七个显式所有者、固定命名域、所有者格式版本、探测规则 / 上限、规范身份字节数、键保留策略和 `索引绑定请求`。
2. 旧二参数入口形成精确的 `未知兼容` 声明；新值式入口只接受与固定所有者规范逐字段相等的显式声明。
3. `主键绑定记录` 完整保存物理键、目标完整句柄、所有者声明和探测序号；索引仓库正向表保存完整记录，全部字段相等才幂等，字段冲突零覆盖。
4. 显式永久键在首次绑定时同步建立保留记录；已发布删除只移除当前正反向绑定并保留历史占用，之后同键重绑写前拒绝。会话候选撤销使用严格删除，同时移除本次未发布保留，允许合法重试。
5. `结构写入会话` 新增 `绑定主键(索引绑定请求)` 和完整记录读回；旧二参数入口继续作为历史兼容。领域上层仍不接收原始事务令牌。
6. 十二个生产绑定点全部改为显式所有者请求；O1 在绑定前从既有 36 字节身份 / 物理键合同解析并携带真实探测序号，没有新增生产装配或 O1 调用点。
7. 新增 `核心/自检.索引所有者声明.ixx` 真模块；入口只在 Debug 宏内增加 import、一个结果槽、970 一次登记、一个通过值和最终具名汇总。通用自检运行器零修改。

## 3. 验收结果

| 验收 | 结果 | 证据 |
| --- | --- | --- |
| A01—A04 | 通过 | 稳定值类型、完整请求、全字段幂等和冲突零覆盖专项通过 |
| A05—A08 | 通过 | 七所有者 / 十二生产点显式归属；新数据操作二参数绑定调用为 0；领域不接令牌 |
| A09—A12 | 通过 | O1 `0x4F315332` / v1 / 36 / 1024；生产装配零改；永久键不重绑；记录规范排序 |
| A13—A15 | 通过 | 内部不一致强类型收口；新文件分类和工程登记通过；970 仅 Debug |
| A16—A19 | 通过 | 两配置、完整自检、20 / 20、strict / diff / 范围；中央治理与运行器零修改 |

专项 Debug 输出：`PERSIST-S1-A0-A01`—`PERSIST-S1-A0-A19` 全部为 1，失败数量 0。

## 4. 验证证据

```text
Debug x64 Rebuild: exit 0, 0 warning, 0 error
Debug complete self-test: exit 0
Release x64 Rebuild: exit 0, 0 warning, 0 error
Release complete self-test: exit 0
Release PERSIST-S1-A0 / 970 专项文本匹配: 0
Debug strict continuous complete self-test: 20 / 20, each exit 0, elapsed 406.7 s
python tools/check_specs.py --strict: pass
git diff --check: pass
```

连续验证首次遇到固定 `D:\海中鱼巣\日志\事件段\自检` 的跨 worktree 文件竞争，具名失败仅为既有 EVENT-SEGMENT-S1 文件项，#304 A01—A19 在该轮仍为 19 / 19。协调暂停其它完整自检，并在进程轮次间保留 2 秒文件句柄释放间隔后，从第一轮重新计数，最终形成严格连续 20 / 20；未修改事件段代码、入口既有事件段正文或测试语义。

## 5. 精确范围

本轮只修改计划允许的索引数据 / 仓库 / 会话、七个生产数据操作、专项自检、入口 Debug 最小接线、工程 / filters 和本记录。`初始化.系统角色.ixx` 经 S0 证明无直接绑定点，因此保持零差异。

以下范围保持零修改：中央计划与项目记忆、`自检.运行器.ixx`、生产入口分支、运行期上下文 / 装配、传统服务、SQL、控制面板、快照、恢复、O1 生产接线和 #214。

## 6. 完成边界

本轮只证明索引所有者、命名域、版本、探测和键保留合同已实现并通过当前回归；不证明四仓库全状态导出、九段冻结、HYSNAP01 编码 / 恢复、O1 生产接线或旧能力迁移完成。
