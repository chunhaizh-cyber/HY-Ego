# PERSIST-S1-P0-B0 运行期关键路径值式公开合同代码实施 Codex 断点清单

日期：2026-07-18

执行角色：WT-298-R1 / `codex/298-p0b0-r1`

计划：#298 / DQ-190 / PERSIST-S1-P0-B0 v0.4

目标状态：接口漂移待设计修订

## 1. 身份与冻结基线

```text
任务 worktree：C:\Users\zhchh\.codex\worktrees\298-p0b0-r1\海中鱼巢
任务分支：codex/298-p0b0-r1
冻结代码父提交：b93f0711a081af1cb226e93b13b6102381581acc
正式派发：origin/main@e5487b9ed8a935aea2494d2364039344c6bea475 / JY-426 / WIN-0001-E168
执行前 worktree / index：clean
执行前 upstream：无
唯一候选：#298
阶段 910：未登记
```

## 2. S0 已复核当前接口

```text
运行期业务操作组合器：8 个既有强类型业务入口。
运行期业务请求：9 个备选项，含 monostate。
运行期业务结果：9 个备选项，含 monostate。
任务执行组合器构造：任务业务服务、方法业务服务、状态动态组合器、非零支持稳定动作键。
任务结果结算组合器构造：需求、任务、方法、状态、动态五个业务服务。
运行期业务装配：当前拥有六个业务组合器，尚未拥有任务执行、任务结果结算和运行期只读查询组合器。
运行期上下文：当前只有二参数构造，生产启动仍以二参数构造候选。
语素基础数据操作：可在共享许可内读取语素入口完整值式材料，具备语素概念追溯关系正向读取基础。
任务结果回执：现有协议和任务管理上行桥已提供逐字段协议、任务、方法、状态、动态复核语义。
```

上述接口除下节具名字段外，可承接 #298 的目标实现。

## 3. 真实接口漂移

#298 v0.4 第 3.1 节固定合同要求：

```text
生产运行期会话必须把 生产运行期配置.稳定动作键 显式传入三参数构造。
```

第 9 节又把以下文本冻结为机械验收：

```text
make_shared<运行期上下文>(配置.域编号, 配置.仓库编号, 配置.稳定动作键)
```

但冻结基线中的正式权威接口是：

```cpp
struct 生产运行期配置 {
    std::uint64_t 动作键 = 生产运行期稳定动作键;
};
```

`海中鱼巣/生产运行期配置.数据.h` 不在 #298 第 4 节允许文件内，并被第 5 节禁止范围覆盖。执行角色因此不能新增 `稳定动作键` 别名、重命名正式字段或修改配置 ABI，也不能把不可能命中的机械验收伪报为通过。

## 4. 退回条件命中

命中计划 S0 退回条件：实际正式签名与冻结合同不一致，且满足原文字面合同需要修改禁止文件。

建议设计窗口只作最小修订：

```text
把 #298 第 3.1 节的 生产运行期配置.稳定动作键 改为现有正式字段 生产运行期配置.动作键；
把第 9 节对应 rg 验收同步改为 配置.动作键；
保持三参数运行期上下文构造的参数名和任务执行组合器语义仍为 稳定动作键；
不修改 生产运行期配置.数据.h，不新增兼容别名，不扩大 #298 允许范围。
```

## 5. 本轮边界

本轮未修改 C++、工程、入口、计划、计划索引、任务队列、流程图、详细设计或中央治理，未登记阶段 910，未运行构建或程序。当前只形成具名执行断点并退回设计窗口；不得据此声明 #298 已实施或任何后继已解锁。
