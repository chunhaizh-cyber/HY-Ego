# PERSIST-S1-P0-B0 运行期关键路径值式公开合同代码实施 Codex 断点清单

日期：2026-07-19

执行队列：#298

设计队列：DQ-190

计划版本：v0.6

批次：WB-298-03 / WT-298-R3

冻结基线：`c896f7dcc70ef7a76b8808d9e6a9c86c3b082b7d`

正式派发：`origin/main@4c19de773d755738f82a5c243dd5f0202ab101b5`

状态：分支完成待集成 / S0-S7 完成

## 1. 接管与范围

1. 任务 worktree 为 `C:\Users\zhchh\.codex\worktrees\298-p0b0-r3\海中鱼巢`，分支为 `codex/298-p0b0-r3`；接管时 HEAD 等于冻结基线，worktree / index clean，upstream 为空，远端同名分支不存在。
2. 已从正式 `origin/main@4c19de7` 只读复核 JY-430 至 JY-432、E174 至 E176、计划索引、任务队列、并行工作树登记表第 10 节和 #298 v0.6；唯一候选、冻结父提交及第 4 / 5 节十五文件范围一致。
3. 本窗口只修改 #298 第 4 节十五文件中的实际实施文件及本记录；未修改计划、计划索引、任务队列、并行工作树登记、流程图、详细设计、中央项目记忆、#299、#304 或其它后继计划。
4. WT-298-R1 / R2 及其断点保持只读，未 cherry-pick、复制或复用旧草案。

## 2. S0 实际接口与消费者复核

1. 冻结基线中 `运行期业务请求` / `运行期业务结果` 的既有消费者精确为三处：运行期业务请求路由、运行期组合分层自检、服务写入兼容边界自检；未发现第四个冻结尺寸、类型或索引消费者。
2. 实施后全仓命中四处，新增的第四处仅为本计划唯一自检 `自检.运行期兼容请求.ixx`，不是计划外冻结消费者。
3. 运行期上下文已有二参数构造、租约、业务装配、系统角色和概念活动初始化接口；生产固定配置已有 `动作键` 字段，三参数第三参数按稳定动作键语义使用。
4. 语素、任务、方法、状态、动态和概念活动服务的公开值式读取签名与计划假定一致；反向概念名称读取可在语素数据操作的一次共享许可内实现，无需扩大仓库 ABI 或穿透结构事务能力。
5. S0 未发现新的实际接口漂移，阶段 910 在实施前未登记。

## 3. S1-S4 值式查询与唯一装配

1. 新建 `组合.运行期只读查询.ixx`，公开概念命名查询和任务结果复核的请求、状态、拒绝原因及结果 DTO。概念查询只认当前活动材料中的四根，复核预期活动版本，并通过语素服务返回完整名称入口值式材料。
2. `数据操作.语素基础.ixx` 在一次共享许可内按 `语素概念追溯` 反向关系读取完整语素材料，逐关系互证、拒绝重复或矛盾，并按完整句柄确定性排序；空名称组是合法值。`服务.语素.ixx` 只做句柄入口拒绝和委托。
3. 任务结果复核顺序固定为协议、任务承接 / 生命周期 / 选择、方法与唯一动作规格、实际状态、动态证据以及场景 / 主体 / 来源动作 / 值 / 时间互证；全程零写，不调用结果结算，不生成许可结论。
4. 新建 `查询.运行期只读.ixx` 适配门面，只持 `运行期上下文租约` 值；每次调用重新读取上下文和候选查询组合器，不缓存裸上下文、组合器指针或内部引用。
5. `装配.运行期业务.ixx` 唯一拥有任务执行、任务结果结算和运行期只读查询三个新增组合器；`运行期上下文` 三参数构造接收稳定动作键，二参数构造只以键 1 保持既有隔离自检兼容；生产启动显式传入 `配置.动作键`。

## 4. S5 路由扩展与兼容边界

1. 顶层业务操作从 8 个入口扩为 13 个入口，新增冻结任务执行、确认进入排队、执行目标状态值变化、收口任务执行请求和处理任务执行结果五类唯一委托。
2. `运行期业务请求` 从 9 个备选项扩为 14 个，`运行期业务结果` 从 9 个备选项扩为 11 个；路由新增索引 9 至 13，既有索引 1 至 8 的类型、顺序、结果索引和八类业务语义保持不变。
3. `自检.运行期组合分层.ixx` 只把请求尺寸断言 9 改为 14、结果尺寸断言 9 改为 11；该文件其它类型、顺序、索引、场景和断言零变化。
4. 顶层公开面未增加仓库、接线、原始令牌、许可、生产会话或执行器依赖。

## 5. S6 阶段 910 与 A01-A20

1. 新建 `自检.运行期兼容请求.ixx`，唯一承载本切片专项正文；生产模块不依赖自检。
2. `入口.cpp` 只增加 Debug import、结果槽、最终回归阶段 910 一次登记、通过值和一个具名总结果门禁；Release 中专项登记和文本均为零。
3. A01-A20 共 20 项具名验收全部通过，覆盖租约 / 上下文正式签名、唯一装配、三参数稳定动作键、无租约和空句柄拒绝、四根活动限定、待命名 / 已命名 / 版本漂移、查询零写、旧名称写路由守恒、回执协议和当前性拒绝、五类新路由强类型结果、二 / 三参数构造以及顶层路由有效性。
4. A14 使用仓库内真实活动概念根作为当前但非任务的目标句柄，避免用不存在节点误触精确版本漂移门禁；返回状态为当前性漂移且完整保留回执副本。

## 6. S7 最终验证

```text
Debug x64 Rebuild：退出 0，0 警告 / 0 错误
Release x64 Rebuild：退出 0，0 警告 / 0 错误
Debug 完整自检：退出 0，P0-B0 A01-A20 = 20 / 20
Release 完整自检：退出 0，P0-B0 专项登记 / 文本 = 0
Debug 最终连续完整自检：20 / 20，每轮退出 0、专项 20 项、失败 0，382 秒
python tools/check_specs.py --strict：通过
git diff --check：通过
冻结消费者：3；实施后总消费者：4，其中新增项只为本计划自检
生产三参数构造精确命中：通过
只读查询组合器 / 租约门面禁止依赖扫描：零命中
组合分层自检差异：仅两条尺寸断言
允许范围、阶段 910 唯一性和 Release 文本隔离：通过
```

曾有一条过短超时的 Rebuild 包装命令被外部终止，并留下孤立 `mspdbsrv`；该进程父进程已不存在，清理后从完整 Rebuild 重新取得上述 0 / 0 证据。该中断结果未计入完成证据。

## 7. 交接边界

本任务只证明关键路径 13 类业务操作、概念 / 名称与任务结果值式查询、唯一运行期装配和租约查询门面已在任务分支建立。它不证明任何后继消费者已改接，不证明完整六树 / SQL 投影、九段冻结、默认入口、#214、HYSNAP01、恢复或旧能力迁移完成。

下一步只允许精确暂存本切片十五文件，提交并非强制推送同名远端 `codex/298-p0b0-r3`。任务分支状态仅为“分支完成待集成”；在独立集成验证、主工作树 main 发布和设计同步之前，不得宣称 #298 正式完成。
