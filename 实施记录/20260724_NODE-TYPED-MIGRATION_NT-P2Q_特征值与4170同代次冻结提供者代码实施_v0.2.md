# NODE-TYPED-MIGRATION NT-P2Q 特征值与 4170 同代次冻结提供者代码实施记录 v0.2

## 1. Git 与计划身份

- 计划：`#375 / DQ-267 / NT-P2Q v0.2`
- 计划 blob：`58d00f4f4a8457843447f95ccf7b38e54df38082`
- 计划索引 blob：`f58b8d4a8c06d931dc1c86e655c871ba1497c659`
- 中央设计提交：`origin/main@1ed96fe6b192f2b28aae2bdc23cd1231c08297b9`
- 执行分支：`codex/execution-current-plan-r1`
- 计划段起点：`4018910c74dce59a84893c1b3ab87204483c8d5c`
- 结果提交：本记录与四个允许代码文件由同一个计划段结果提交承载；精确提交号见结构化完成回执。

## 2. S0

S0 结果为 PASS，并已向配对计划支撑任务 `019f9151-0970-7283-8531-e89104a53b9c` 发送 `EXECUTION-SUPPORT-NOTICE`。

已核对：

- worktree / index clean，upstream 与计划段起点一致。
- 累计父链包含 #340 结果 `e19ad70dc9f8ca0f93a91e1a4298eef9c40f6c72` 与 #374 结果 `4018910c74dce59a84893c1b3ab87204483c8d5c`。
- v0.2 精确计划在正式索引中为 `可执行`，旧 v0.1 blob 未继续使用。
- 新域 4170 仍唯一来自 `节点直接特征批次记录仓`；新域数据操作对旧 `特征批次发布记录账` 和访问器零读取。
- 四个允许代码文件存在，未发现同文件活动所有权重叠。

## 3. 实际变更

- `执行器.节点直接身份结构写入.ixx`
  - 增加非零运行期冻结域身份、非零已发布代次和冻结规则版本 1。
  - 增加 move-only、RAII 的 `节点直接统一冻结许可` 与真实见证。
  - 真实 `已提交` 在全部参与者发布后、独占许可释放前推进代次一次；幂等、拒绝和撤销不推进。
- `参与者.特征值类型化记录.ixx`
  - 增加全量读取状态、结果和 `复制全部已发布记录()`。
  - 在一次共享锁内复制并检查未知格式、不完整记录、重复身份版本和多个当前记录。
- `数据操作.节点直接特征体系.ixx`
  - 为私有新域批次仓增加从 1 开始的真实结构版本。
  - 增加 P2A-FREEZE/v2 原子 DTO 与 `复制同代次特征值与批次全量()`。
  - 按统一许可、特征值仓、批次仓顺序完成两次见证、两仓版本 / 数量复核、悬空引用检查和稳定排序。
  - 段 9 批次命名域 ABI 固定为 1；未读取旧默认域 4170。
- `自检.节点直接特征体系.ixx`
  - 增加许可和 DTO 签名 / 宽度 / 状态静态检查。
  - 增加域身份、错域、规则版本、许可移动、冻结写阻断、真实提交代次推进、幂等 / 拒绝 / 撤销不推进、原子空组、部分可选项和结果副本检查点。

只读复核发现默认移动赋值存在旧互斥生命周期风险，并指出首版自检未驱动真实提交与冻结写阻断；两项均已在允许文件内修正。聚合读取与新旧 4170 边界复核未发现其它具名问题。

## 4. 实际验证

已执行且通过：

```text
git diff --check -- <四个允许代码文件与本记录>
rg -n "节点直接统一冻结见证|节点直接统一冻结许可|取得统一冻结许可|冻结许可属于本域" <核心执行器>
rg -n "复制全部已发布记录|特征值类型化记录全量读取结果" <特征值记录参与者>
rg -n "复制同代次特征值与批次全量|节点直接特征批次冻结记录|批次结构版本" <节点直接特征数据操作>
rg -n "特征批次发布记录账|特征批次发布记录账访问器" <节点直接特征数据操作>
git diff --cached --check
```

前三组符号检查有目标命中；旧默认域 4170 检查零命中；最终差异和暂存范围仅包含计划允许的五个路径。

按计划未运行构建、程序或自检二进制，因此没有编译、运行、并发或恢复验收声明。

## 5. 残余风险与完成边界

- 工程登记、真实 #349 适配、Debug / Release、完整自检、并发与恢复验证仍由 #352 固定汇合和集成验证闭合。
- 当前结果只证明 `P2A-FREEZE/v2` 源码候选和候选级静态 / 专项源码边界检查已形成，不证明 #349 已接线、九段快照已形成、旧 4170 已迁移或节点直接域已成为默认生产域。
