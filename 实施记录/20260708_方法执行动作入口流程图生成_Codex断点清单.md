# 方法执行动作入口流程图生成 Codex 断点清单

日期：2026-07-08

状态：流程图已生成 / 设计确认窗口产物 / 未生成详细设计 / 未生成施工计划 / 未登记可执行队列 / 未修改 C++ / 未运行构建 / 未迁移旧函数

## 1. 输入入口

```text
用户指令：生成下一份流程图
当前窗口：设计确认窗口
迁移顺序信息数据第 11 项：方法执行 / 动作入口流程
```

## 2. 读取依据

```text
AGENTS.md
计划/计划索引.md
规范/000_项目规则总纲.md
规范/001_规则迁移清单.md
规范/动作入口规范.md
规范/详细设计/动作入口详细设计.md
实施记录/20260708_应用逻辑流程图迁移顺序信息数据.md
实施记录/20260706_FS07_动作动态因果入口只读扫描记录.md
实施记录/20260707_FS07_动作动态因果入口增强S1-S4代码实施_Codex断点清单.md
流程图/20260708_方法结构代码逻辑流程图_v0.1.md
流程图/20260708_方法候选召回与选择代码逻辑流程图_v0.1.md
海中鱼巣/领域/任务服务.h
海中鱼巣/领域/方法服务.h
海中鱼巣/领域/特征服务.h
海中鱼巣/领域/动态服务.h
```

## 3. 生成文件

```text
流程图/20260708_方法执行动作入口代码逻辑流程图_v0.1.md
流程图/20260708_方法执行动作入口代码逻辑流程图_v0.1.html
实施记录/20260708_方法执行动作入口流程图生成_Codex断点清单.md
```

## 4. 核心处理

```text
按应用逻辑流程图迁移顺序进入第 11 项。
将方法执行 / 动作入口拆成动作入口登记与读取、动作规格与注册材料复核、任务执行桥请求、特征状态材料写入、动作动态证据记录、最小读回验证和排除项七个区域。
把当前代码事实限定为第一轮入口、关系和材料复核，不扩大为完整动作执行系统、任务结果回写、需求结算或稳定因果结论。
明确高级服务不得直接依赖特征值服务；方法执行需要写特征状态材料时必须经特征服务。
明确线程、函数名、动作名文本、日志标签、控制台命令、SQL、控制面板、D455、体素和外设不得作为动作来源或实施入口。
```

## 5. 当前代码事实摘录

```text
任务服务已有 `读取执行桥请求材料`，当前要求任务承接壳完整，并能读取唯一任务选择方法；缺少任务方法关系时返回缺少任务方法关系。
方法服务已有 `登记动作入口`、`读取方法动作入口`、`按稳定动作键读取方法动作入口`、`绑定动作输入规格`、`绑定动作输出规格` 和 `本能动作注册材料是否有效`。
方法服务已有 `执行特征状态写入方法`，当前要求有效任务、有效方法、任务到方法引用关系和主动动作入口成立，然后调用特征服务写 I64 特征状态材料。
方法服务已有 `记录动作动态证据`，当前要求动作入口归属方法首有效且方法具备主动动作入口，然后调用动态服务记录实例动态，并写动态到来源方法首的引用关系。
动态服务已有 `记录实例动态`、`读取动态来源动作` 和 `动态证据材料是否完整`，当前来源动作必须为空或为动作入口角色。
```

## 6. 结构承载

```text
动作入口结构：方法节点角色、动作入口角色状态、动作入口状态、方法首到动作入口引用关系、稳定动作键索引。
任务执行桥请求：任务节点、任务选择方法关系、执行桥请求材料状态。
特征状态材料：经特征服务写入的第一轮 I64 特征状态材料。
动作动态证据：实例动态节点、场景临时关系、主体引用、目标 / 前值 / 后值 / 来源动作因果来源关系、动态到来源方法首引用关系。
```

## 7. 输出变化

```text
新增第 11 项流程图 Markdown 和 HTML 文件。
新增本断点清单。
未修改 C++。
未生成详细设计。
未生成施工计划。
未登记可执行队列。
未运行构建。
未迁移旧函数。
```

## 8. 验证方式

```text
确认 Markdown 文件存在。
确认 HTML 文件存在。
确认 Markdown 包含 Mermaid fenced block。
确认 HTML 包含 Mermaid CDN import。
确认 Markdown 和 HTML 中 Mermaid 图文本一致。
运行 git diff --check 限定本轮 3 个文件。
运行 tools/check_specs.py 轻量规范治理检查。
```

## 9. 禁止扩大声明

```text
不得声明完整方法执行系统完成。
不得声明动作入口系统、动作动态系统、本能动作系统或因果系统完整完成。
不得声明真实线程调度、工作队列、std::async、外部执行器、取消、超时或重试已接入。
不得声明任务结果回写、需求结算、动作验证报告或稳定因果结论已完成。
不得声明 SQL、控制面板、D455、体素或外设已接入。
不得声明旧动作、动态、因果或本能动作函数体已迁移。
```

## 10. 后续建议

```text
下一份流程图按迁移顺序应进入第 12 项：动态记录 / 输出结果场景流程。
若本图后续进入代码实施，必须另建待确认施工计划，明确允许文件、禁止文件、入口拒绝、失败收口、最小读回验证和完成声明边界。
```
