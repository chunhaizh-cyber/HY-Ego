# SINGLE-WINDOW-S1 单窗口串行双角色规则治理 Codex 断点清单

日期：2026-07-16

计划 / 队列：#281 / DQ-173 / SINGLE-WINDOW-S1

决策：JY-366

执行基线：9b2f670

状态：规则已落地 / 文档验证通过 / 技能 frontmatter 人工复核通过 / 待提交和自动非强制推送 / 未改 C++ / 未构建 / 未运行程序

## 1. 执行前复核

1. `main` 与 `origin/main` 位于同一提交 `9b2f670`，工作树和暂存区在本轮开始时干净。
2. #281 / DQ-173 已登记为当前首个待执行项；流程图和详细设计因不改变机器结构、领域流程或代码调用路径而不适用，替代依据完整。
3. 用户已明确指定同一物理窗口承接后续制定、修订和执行；默认双物理窗口仍须保留。
4. 旧执行技能仍有“仅显式授权才推送”的过期措辞，必须与 AGENTS / JY-343 自动推送持续授权统一。
5. 本轮允许范围不含 C++、工程、流程图、详细设计、日志、快照、数据库或任何 stash 操作。

## 2. 实施结果

1. AGENTS、规则总纲、规则迁移清单和设计执行交互规范已统一单窗口串行双角色规则。
2. plan / execute 两个项目技能已增加角色门禁、同窗口切换和自发自收消息免除规则；execute 技能已改为每轮自动非强制推送。
3. 默认双物理窗口模式继续有效；只有用户明确指定时才启用同一物理窗口模式。
4. 同一物理窗口任一时刻只激活设计、执行或只读复核一个角色；裸 `继续` 只沿当前登记角色。
5. 角色切换前先写断点、索引、队列和交互事件并验证，再自动提交和自动非强制推送；推送成功后切换并重新读取正式状态。
6. 单窗口只免除向自己发送 Codex 任务消息，不免除交互记录、依赖门控、允许 / 禁止文件、实际接口复核、单一写入所有权或验证。
7. 模式持续到用户明确撤销或窗口交互记录正式转交所有权。
8. #281 / DQ-173 已归档；本轮提交推送成功后，当前物理窗口从执行角色切换为设计角色，以 JY-367 原地修订 #235 / DQ-127。

## 3. 验收矩阵

| 编号 | 结果 | 证据 |
| --- | --- | --- |
| SINGLE-WINDOW-S1-A01 | 通过 | AGENTS、总纲和交互规范均明确默认双物理窗口保留。 |
| SINGLE-WINDOW-S1-A02 | 通过 | 用户明确指定时可启用同一物理窗口串行双角色模式。 |
| SINGLE-WINDOW-S1-A03 | 通过 | 任一时刻只有一个角色和一个写入所有者。 |
| SINGLE-WINDOW-S1-A04 | 通过 | 设计角色与执行角色沿用既有允许 / 禁止动作，不合并权限。 |
| SINGLE-WINDOW-S1-A05 | 通过 | 退回设计前须停止实施、记录、验证、提交并推送。 |
| SINGLE-WINDOW-S1-A06 | 通过 | 设计修订发布后，执行角色必须重新读取正式状态和实际接口。 |
| SINGLE-WINDOW-S1-A07 | 通过 | 单窗口只免除自发自收任务消息，窗口交互记录继续保留。 |
| SINGLE-WINDOW-S1-A08 | 通过 | main 单工作树、自动提交和自动非强制推送规则保持有效。 |
| SINGLE-WINDOW-S1-A09 | 通过（人工复核） | plan / execute 技能已统一；quick_validate 因本机缺少 PyYAML 未完成自动校验，未临时安装依赖；两个技能 YAML frontmatter 的分隔符、name 和 description 已人工复核。 |
| SINGLE-WINDOW-S1-A10 | 通过 | 计划索引、队列、当前状态、决策、实施、验收和窗口交互已同步。 |
| SINGLE-WINDOW-S1-A11 | 通过 | `git diff HEAD --name-only` 允许范围无越界；C++ / 工程 / 流程图 / 详细设计变更数 0；未读取任何 stash。 |
| SINGLE-WINDOW-S1-A12 | 待本轮收口 | 计划已移动到 `计划/已完成计划/`，文档验证已通过；等待精确暂存、提交、自动非强制推送和 `main...origin/main = 0 0`。 |

## 4. 验证记录

| 命令 / 检查 | 当前结果 |
| --- | --- |
| `git diff --check` | 通过；只有工作区 LF / CRLF 转换提示，无空白错误。 |
| `python .\tools\check_specs.py` | 通过：`海中鱼巣规范治理检查: 未发现问题`。 |
| plan 技能 `quick_validate.py` | 自动校验未完成：`ModuleNotFoundError: No module named 'yaml'`；按计划不安装依赖，YAML frontmatter 人工复核通过。 |
| execute 技能 `quick_validate.py` | 自动校验未完成：`ModuleNotFoundError: No module named 'yaml'`；按计划不安装依赖，YAML frontmatter 人工复核通过。 |
| 允许文件范围 | 通过；受跟踪变更 14 项全部位于计划允许范围，新断点文件也属于允许范围。 |
| C++ / 工程 / 流程图 / 详细设计零变化 | 通过；禁止范围变更数 0。 |
| stash 边界 | 未读取、未恢复、未修改、未删除 #258 / #261 救援 stash |

## 5. 当前断点与角色切换

本文件、计划索引、任务队列和 WIN-0001-E89 随本轮提交推送成功后，#281 才形成正式发布收口。随后当前物理窗口按已发布交互记录切换为设计角色，重新读取 Git、计划索引、任务队列、#235 原计划、正式断点和实际接口；不得直接执行旧 #235。

## 6. 完成声明边界

本轮只治理 Codex 的同一物理窗口串行双角色模式。不得声明设计与执行权限已合并、计划门禁已取消、#235 已修订或执行、任何 C++ / 业务能力已实现、旧能力已迁移或项目迁移已完成。
