# FS-03 特征特征值逻辑迁移包归并表 Codex 断点清单

日期：2026-07-06
状态：已生成 / 待确认 / 未改代码 / 未迁移旧函数

## 1. 输入入口

计划索引当前执行项为 `HR-001-FM-SCAN`，上一闭合点为 FS-X 控制面板 / SQL / D455 / 体素 / 外设排除项汇总。

当前计划下一步为：

```text
生成 FS-03 特征 / 特征值逻辑迁移包归并表。
```

## 2. 本次读取依据

```text
AGENTS.md
计划/计划索引.md
规范/000_项目规则总纲.md
规范/001_规则迁移清单.md
计划/20260706_函数迁移前置专项_v0.1.md
实施记录/20260706_逐函数只读扫描清单.md
实施记录/20260706_FS03_特征与特征值入口只读扫描记录.md
实施记录/20260706_FS02_状态动态二次特征因果逻辑迁移包归并表.md
```

## 3. 当前边界

```text
本轮只把 FS-03 旧函数事实归并为逻辑迁移包。
不运行旧项目。
不修改旧项目文件。
不修改当前项目代码。
不迁移旧特征主信息、旧抽象特征主信息或旧特征值主信息字段。
不接 VecU / VecI64 原始值容器、体素、D455、外设、控制面板或 SQL。
```

## 4. 本次输出

```text
已生成：
实施记录/20260706_FS03_特征特征值逻辑迁移包归并表.md

覆盖旧函数编号：
OF-106 至 OF-117

归并逻辑包：
FS-03-A 特征身份与语义类型读取
FS-03-B 抽象特征、值域与状态语义分流
FS-03-C 实例特征创建、宿主槽位与当前值读取
FS-03-D I64 特征值写入读取与特征服务封口
FS-03-E Vec 原始值容器与特征值身份
FS-03-F 特征值非权威缓存、内容哈希与命中统计
FS-03-G 特征值域匹配、复合比较与候选判断
FS-03-H 稳态、局部区间覆盖与抽象区间升格
FS-03-I 特征值序列化、反序列化与恢复材料
FS-03-X 体素 / 外设值材料排除
```

## 5. 当前判断

1. FS-03-A、FS-03-C、FS-03-D 是可考虑保留 / 改写的特征服务方向，但仍需用户确认和后续独立详细设计 / 计划。
2. FS-03-B、FS-03-E、FS-03-G、FS-03-H 属于特征 / 特征值增强专项候选，不进入当前代码实施。
3. FS-03-F 属于非权威缓存与统计后续专项，缓存不得裁决业务事实。
4. FS-03-I 属于事件日志持久化恢复后续专项，不复用旧序列化格式。
5. FS-03-X 属于体素 / 外设值材料排除边界，本轮不接 D455、体素或外设。

## 6. 下一步

```text
提交 实施记录/20260706_FS03_特征特征值逻辑迁移包归并表.md 给用户确认。
确认前不得进入 FS-03 代码实施、字段落地或旧函数体迁移。
```

## 7. 验证方式

本切片为文档归并切片，未运行构建、未运行当前项目入口、未运行旧项目程序。

收尾验证覆盖：

```text
rg -n "FS-03|OF-106|OF-117|特征特征值逻辑迁移包" 实施记录 计划 项目记忆
git diff --check
```

## 8. 禁止扩大声明

```text
不得声明旧特征类或旧特征值类已迁移。
不得声明旧特征主信息、旧抽象特征主信息或旧特征值主信息字段可直接迁移。
不得声明 VecU / VecI64、内容哈希、稳态、抽象特征、值域、体素或外设能力已完成。
不得把 FS-03 归并表当作代码实施许可。
```
