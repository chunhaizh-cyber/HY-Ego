# 语素服务入口材料准入代码实施切片确认入队 Codex 断点清单

日期：2026-07-08

## 1. 输入入口

```text
用户确认：`计划/待确认计划/20260708_语素服务入口材料准入代码实施切片_v0.1.md` 小修后确认。
小修要求：
1. 验收排除项扫描补充 `控制面板|std::thread|std::async|工作队列`。
2. 补充确认流转说明：本计划确认时允许从 `计划/待确认计划/` 移入 `计划/` 并同步项目记忆和队列；这不是代码实施允许文件范围。
3. 失败收口后必须通过读回和数量快照证明没有可读半结构；无法证明时停止并登记待确认问题。
```

## 2. 核心处理

```text
将计划从 `计划/待确认计划/` 移入 `计划/`。
按用户小修要求修订计划正文。
更新计划索引。
更新项目记忆当前状态、决策记录和待确认问题 QR-099。
登记可执行队列 #130。
```

## 3. 结构承载

```text
计划：计划/20260708_语素服务入口材料准入代码实施切片_v0.1.md
计划索引：计划/计划索引.md
当前状态：项目记忆/当前状态.md
决策记录：项目记忆/决策记录.md
待确认问题：项目记忆/待确认问题.md
可执行队列：项目记忆/Codex任务队列.md
```

## 4. 输出变化

```text
QR-099 状态改为已小修后确认。
新增决策记录 JY-085。
新增可执行队列 #130。
计划状态改为已确认计划 / 已登记可执行队列 #130 / 代码实施许可 / 未执行。
本次只做计划治理和队列登记，未修改 C++，未运行构建。
```

## 5. 调用路径

```text
设计确认窗口
-> 用户确认代码实施切片并提出小修要求
-> 修订计划
-> 移动计划到 `计划/`
-> 同步项目记忆和计划索引
-> 登记执行窗口可消费的队列 #130
```

## 6. 验证方式

```text
git diff --check
python .\tools\check_specs.py
git status --short --branch
```

## 7. 禁止宣称

```text
不得宣称语素服务入口材料准入代码已实现。
不得宣称 Debug x64 构建已运行。
不得宣称入口前置拒绝已通过。
不得宣称结构不变化验收已通过。
不得宣称旧语素能力已迁移。
不得宣称控制面板、SQL、ADO、WebView、D455、体素、外设、真实线程调度、工作队列或 std::async 已接入。
```
