# 任务结果同义并发结算版本漂移现状施工偏差清单

日期：2026-07-19

标准检查字段：现状代码事实、流程图、详细设计、偏差类型、纠偏方向、是否需要代码计划。下表“当前事实”记录现状代码事实，“正式合同”引用本切片流程图与详细设计，“偏差”表达偏差类型，“纠偏”给出纠偏方向；四项均需要 #314 代码计划。

| 编号 | 当前事实 | 正式合同 | 偏差 | 纠偏 |
| --- | --- | --- | --- | --- |
| TRC-01 | `版本漂移` 在当前正式结算读回前返回 | 同义并发只允许一个提交，其余精确幂等 | 同一请求因竞争者发布时点不同返回幂等或版本漂移 | 无写入版本漂移进入权威当前态归并 |
| TRC-02 | `并发占用` 已有读回归并，`版本漂移` 没有 | 两个分支都可能表示竞争者已经发布 | 等价竞争事实使用两套收口 | 合并同义 / 异义 / 未结算三分支 |
| TRC-03 | SERVICE-DATA-S5 同义并发规模为 8 | TASK-RESULT-S1 A14 使用 100 路并发 | 底层回归窗口较窄 | 底层同义结算专项提升到 100 路并要求 1+99+0 |
| TRC-04 | #312 观察 A14/A18 失败，#313 累计 60 样本未复现 | 间歇失败不能用未复现宣告稳定 | 证据只能说明调度敏感 | 以代码分支差异修复并重验，不继续无目标诊断 |

本清单要求修代码和补正式合同，不修改服务公开 ABI、机器结构、阶段号或运行期装配。

## 完成边界

本清单只证明偏差已经定位并形成 #314 代码计划，不证明纠偏已实现。
