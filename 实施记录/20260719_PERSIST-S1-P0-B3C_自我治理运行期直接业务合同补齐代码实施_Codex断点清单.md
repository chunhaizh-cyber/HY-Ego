# PERSIST-S1-P0-B3C 自我治理运行期直接业务合同补齐代码实施 Codex 断点清单

日期：2026-07-19

执行队列：#310

设计队列：DQ-202

执行计划：`计划/20260719_PERSIST-S1-P0-B3C_自我治理运行期直接业务合同补齐代码实施切片_v0.2.md`

设计裁决：JY-445 / WIN-0001-E190

任务 worktree：`C:\Users\zhchh\.codex\worktrees\310-p0b3c-r1\海中鱼巢`

任务分支：`codex/310-p0b3c-r1`

冻结代码父提交：`1d95aa5975057965fcfa179c56521ad69e5b5b6a`

状态：**验证失败待裁决 / 不可集成 WIP**

## 1. 本次允许范围

本次只保留并提交下列四个文件：

```text
海中鱼巣/领域/组合.运行期业务操作.ixx
海中鱼巣/装配.运行期业务.ixx
海中鱼巣/自检.运行期兼容请求.ixx
实施记录/20260719_PERSIST-S1-P0-B3C_自我治理运行期直接业务合同补齐代码实施_Codex断点清单.md
```

未修改中央治理、入口、工程、统一运行器、线程路由、运行期请求 / 结果 variant、B1 桥、B3 路由或其它计划文件。

## 2. 已形成草案

1. `运行期业务操作组合器` 增加需求、任务、方法、轻量因果四个业务服务引用。
2. 增加 7 个写入口和 4 个读入口，全部复用既有强类型请求 / 结果，只做值式入口拒绝和唯一业务服务委托。
3. `运行期业务装配` 作为唯一装配所有者传入四个既有服务。
4. 既有 910 自检保留 P0-B0 A01-A20，并追加 P0-B3C A01-A12；variant 仍为 14 / 11，线程路由不变。

上述草案没有取得完成结论；它们只作为等待独立基线诊断和后续设计裁决的 WIP 保存。

## 3. 已完成验证

在 JY-445 禁止重跑生效前，已经取得以下证据：

```text
Debug x64 Rebuild：通过
Release x64 Rebuild：通过
Debug 单轮完整 self-test：退出 0
Debug 单轮 P0-B3C A01-A12：12 / 12 通过
Debug 单轮 P0-B0 A01-A20：20 / 20 通过
Release 单轮完整 self-test：退出 0
Release P0-B3C / P0-B0 专项文本：0，保持 Release 隔离
严格规范检查：通过
git diff --check：通过
```

## 4. 严格连续验证失败

Debug 严格连续 20 轮从第 1 轮重新起算，每轮进程自然结束后间隔 2 秒：

```text
第 1—18 轮：每轮退出 0；P0-B3C A01-A12 均为 12 / 12；P0-B0 A01-A20 均为 20 / 20。
第 19 轮：退出 1；P0-B3C A01-A12 仍为 12 / 12；P0-B0 A01-A20 仍为 20 / 20。
第 20 轮：未启动。
```

第 19 轮中央最终门禁显示：

```text
任务结果结算通过：失败
概念命名第一切片通过：失败
```

只读源码复核确认：`任务结果结算通过` 直接依赖阶段 610 `TASK-RESULT-S1` 的总结果；`概念命名第一切片通过` 额外依赖中央自检批次退出码，因此后者是中央批次已有失败的派生项。当前唯一确认的上游失败族为 `TASK-RESULT-S1`，不能据此确定具体叶子验收项。

## 5. 叶子证据不可恢复

第 19 轮完整 stdout / stderr 当时只保存在 PowerShell 进程变量中；失败分支只返回最后 200 行。工具回传内容从 `METHOD-RECALL-S5-A03` 开始，已经截掉更早的 `TASK-RESULT-S1-A01`—`A18`。进程结束后变量不存在，也没有写入临时文件或专项日志。

正式结论：

```text
TASK-RESULT-S1 叶子输出未保留，具体失败 A 项和专项通过 / 失败计数不可恢复。
不猜测 A14 或其它叶子项。
不以新运行替代第 19 轮旧证据。
原连续组整体作废，不续算前 18 轮。
```

## 6. 失败收口

发现第 19 轮失败后已立即停止验证并释放 `D:\海中鱼巣\日志\事件段\自检`。进程核对无残留 `海中鱼巣.exe`，Git index 保持为空，未修改计划外文件。

JY-445 / E190 发布后继续保持：

```text
不构建。
不运行完整 self-test。
不运行连续 self-test。
不修改入口、工程、TASK-RESULT-S1 自检或其它计划外文件。
等待 #311 / DQ-203 独立基线诊断与后续设计裁决。
```

## 7. 完成边界

本提交状态只能写为：

```text
验证失败待裁决 / 不可集成 WIP
```

不得写为“分支完成待集成”。本 WIP 不得进入 `INT-WB-P0B-02-R1`，不得解除 #301 / #306，不证明 #310 完成，不证明 11 个直接合同已经进入 main，也不证明自我治理路由迁移、940 登记、默认入口切换、快照恢复或自我循环完成。
