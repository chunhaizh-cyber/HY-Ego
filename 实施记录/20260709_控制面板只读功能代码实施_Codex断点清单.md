# 控制面板只读功能代码实施 Codex 断点清单

日期：2026-07-09

## 1. 输入入口

```text
计划/已完成计划/20260709_控制面板只读功能专项计划_v0.1.md
流程图/20260709_控制面板功能流程图_v0.1.md
规范/详细设计/控制面板功能详细设计.md
海中鱼巣/领域/显示服务.h
项目记忆/Codex任务队列.md #149
```

## 2. 核心处理

```text
新增 海中鱼巣/领域/控制面板服务.h。
在入口.cpp 接入控制面板服务实例。
新增 CONSOLE-PANEL-A1 到 CONSOLE-PANEL-A9 默认入口验收。
将控制面板只读功能第一轮通过并入服务操作函数矩阵第一批通过。
归档计划并同步计划索引、任务队列、当前状态、决策记录、待确认问题和验收记录。
```

## 3. 结构承载

```text
控制面板请求
控制面板材料
控制面板树视图材料
控制面板操作请求材料
控制面板门禁材料
```

本轮材料只做人读只读材料、树视图候选、操作请求壳和后续专项门禁，不写节点、主信息、关系、索引、特征、特征值、需求、任务、方法、状态、动态或因果结构。

## 4. 输出变化

```text
新增控制面板只读服务壳。
入口输出 控制面板只读功能第一轮: 通过。
CONSOLE-PANEL-A1 到 CONSOLE-PANEL-A9 全部通过。
计划文件已移动到 计划/已完成计划/。
```

## 5. 调用路径

```text
main
-> 控制面板服务::生成只读总览材料
-> 显示服务::生成只读展示材料

main
-> 控制面板服务::生成六类树视图候选材料
-> 控制面板服务::生成树视图候选材料

main
-> 控制面板服务::生成操作请求壳

main
-> 控制面板服务::生成数据库审计门禁
-> 控制面板服务::生成渲染通道门禁
-> 控制面板服务::生成外部感知门禁
```

## 6. 验证方式

```powershell
git diff --check -- 海中鱼巣/领域/控制面板服务.h 海中鱼巣/入口.cpp
python .\tools\check_specs.py
msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m
.\x64\Debug\海中鱼巣.exe
rg -n "SQL|ADO|WebView|D455|体素|外设|std::thread|std::async|工作队列" 海中鱼巣/领域/控制面板服务.h 海中鱼巣/入口.cpp
rg -n "写节点|写主信息|写关系|写索引|裁决需求|裁决任务|裁决方法" 海中鱼巣/领域/控制面板服务.h
```

验证结果：

```text
git diff --check 通过。
tools/check_specs.py 通过。
Debug x64 构建通过，0 警告，0 错误。
默认入口返回 0，CONSOLE-PANEL-A1 到 CONSOLE-PANEL-A9 全部通过。
禁用能力扫描未命中新建 控制面板服务.h；入口.cpp 命中既有 XS-A9 人读输出标签，非本切片新增。
控制面板服务.h 写入 / 裁决短语扫描无命中。
```

## 7. 完成声明边界

```text
可以声明：控制面板只读功能服务壳第一轮已实现并验证。
可以声明：只读总览材料、六类树视图候选、操作请求壳和后续专项门禁具备第一轮入口。
不得声明：真实控制面板 UI 已接入。
不得声明：渲染窗口、结构化输出接口、截图、数据库运行期裁决、D455、体素或外设显示已接入。
不得声明：旧控制面板能力已迁移。
```
