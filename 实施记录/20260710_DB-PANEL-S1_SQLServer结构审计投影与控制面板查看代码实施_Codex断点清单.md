# DB-PANEL-S1 SQL Server 结构审计投影与控制面板查看代码实施 Codex 断点清单

日期：2026-07-10

## 输入入口

`计划/已完成计划/20260710_DB-PANEL-S1_SQLServer结构审计投影与控制面板查看代码实施切片_v0.1.md`

## 核心处理

新增 ODBC SQL Server 适配器，使用 Windows 集成认证初始化 `海中鱼巣` 数据库和 `dbo.结构统计审计` 表。程序核心自检完成后追加当前结构统计审计记录并读回；控制面板只读查询最近 50 条记录。

## 结构承载

```text
内存事实：节点仓库 / 主信息仓库 / 关系仓库 / 索引仓库，均未修改。
审计投影：海中鱼巣.dbo.结构统计审计。
适配代码：海中鱼巣/适配/SQL数据库适配.h/.cpp。
显示代码：海中鱼巣/界面/控制面板窗口.h/.cpp。
```

## 输出变化

```text
默认服务器：.\SQLEXPRESS。
默认数据库：海中鱼巣。
新增参数：--database-self-test-exit。
新增环境覆盖：HY_EGO_SQL_SERVER / HY_EGO_SQL_DATABASE。
控制面板新增数据库审计状态和编号、时间、来源、节点、关系、索引列表。
```

## 调用路径

```text
main
-> 统计服务.读取权威结构统计快照
-> SQL数据库适配.写入结构统计审计
-> SQL数据库适配.读取最近结构统计审计
-> 控制面板窗口.更新数据库控件
```

## 验证结果

```text
MSSQL$SQLEXPRESS：Running / Automatic。
sqlcmd Windows 集成认证连接成功，实例为 zch_bjb\SQLEXPRESS。
Debug x64：0 警告，0 错误。
--database-self-test-exit：返回 0，审计写入和读回通过。
数据库不可用负例：不存在实例返回 1，审计编号为 0。
dbo.结构统计审计：8 个字段完整，均为非空；审计记录可由 sqlcmd 读回。
控制面板：数据库审计列表可见，节点 / 关系 / 索引与数据库记录一致。
刷新：状态栏显示“只读材料和数据库审计投影已刷新；权威结构未改变”。
窗口关闭：正常退出。
git diff --check：无错误。
python .\tools\check_specs.py：未发现问题。
```

## 完成边界

本次只证明结构统计审计投影写入、读回和控制面板只读查看完成。不证明节点 / 主信息 / 关系 / 索引明细持久化、数据库恢复、旧表迁移、完整数据库系统或数据库成为运行期事实源。
