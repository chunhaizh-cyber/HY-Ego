# 函数功能说明清单 Codex 断点清单

日期：2026-07-05
状态：已建立 / 待确认 / 未迁移旧函数

## 1. 输入入口

S0 缺口 3 要求补函数功能说明清单或生成机制，函数迁移前必须逐函数说明并确认。

用户补充口径：

```text
高级服务白名单中少了场景，场景服务也可以用。
```

## 2. 本次处理

```text
新增 说明书/函数功能说明文档.md。
新增 计划/20260705_函数功能说明清单专项_v0.1.md。
更新 计划/计划索引.md。
更新 项目记忆/*。
更新主信息迁移映射计划和总计划中的 S0 缺口 3 状态。
```

## 3. 函数说明覆盖范围

```text
核心仓库函数
日志和容错函数
领域服务公开函数
入口程序局部函数
私有辅助函数清单
```

## 4. 高级服务白名单

高级服务只允许直接依赖：

```text
存在服务
场景服务
特征服务
二次特征服务
状态服务
动态服务
因果服务
```

不得直接依赖：

```text
特征值服务
世界服务写入捷径
节点仓库
主信息仓库
关系仓库
索引仓库
```

需要特征状态材料时，必须通过特征服务取得。

## 5. 当前停止点

```text
函数功能说明清单已建立。
说明状态仍为待确认。
确认前不得迁移旧函数体。
下一步应确认 说明书/函数功能说明文档.md。
```

## 6. 禁止扩大声明

```text
不得声明函数说明已全部确认。
不得声明旧函数已迁移。
不得声明旧项目能力已迁移。
不得声明主信息迁移完成。
```
