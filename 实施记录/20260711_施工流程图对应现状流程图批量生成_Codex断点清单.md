# 施工流程图对应现状流程图批量生成 Codex 断点清单

日期：2026-07-11

状态：已完成 / 未改 C++ / 未构建 / 未加入计划索引 / 未登记任务队列

## 当前边界

```text
输入：流程图根目录施工图、当前代码、实施记录和计划状态。
核心处理：提取 Mermaid 和服务函数标记，定位当前代码定义，生成现状图与核查表。
结构承载：流程图/现状流程图 与 实施记录/现状流程图核查。
输出变化：43 份施工图对应现状图、43 份配套核查表，并归集 2 份已有严格现状图。
调用路径：施工图 -> 当前代码证据 -> 现状图 -> 核查表。
验证方式：45 对 MD/HTML 配对与 Mermaid 一致性检查、规范检查、差异检查。
```

## 一、盘点结果

| 类别 | 数量 | 处理 |
| --- | ---: | --- |
| 根目录 Markdown | 48 | 逐份识别围栏、图类型和代码证据 |
| 施工图 / 代码逻辑目标图 | 43 | 生成对应现状图和配套核查表 |
| 已有严格现状图 | 2 | 保持原 MD/HTML 配对并归集到现状目录 |
| 迁移模板 | 2 | 不当作施工图生成 |
| 旧项目只读证据图 | 1 | 不当作施工图生成 |

## 二、现状分类

| 现状类型 | 数量 | 口径 |
| --- | ---: | --- |
| 已实现或已形成第一轮入口 | 34 | 当前存在函数定义或主题相关实现文件 |
| 部分实现 | 6 | 只承载施工图部分范围，未完成节点不视为现状事实 |
| 未实现 | 3 | 当前没有对应正式入口，仅保留缺口图 |
| 已有严格现状图 | 2 | `THREAD-S1` 和 `CONCEPT-S5` 沿用原配对 |

## 三、核查内容

每份新生成现状图都关联一份配套核查表，其中固定包含：

```text
连续代码范围映射
输入契约 / 调用语境
逻辑内返回 / 追根因解决二分
现状与施工目标偏差
```

正式流程已保证有效后再出现同一无效材料，统一列为“追根因解决”，不因结构尚未变化而改画为普通拒绝。

## 四、完成声明

```text
输入入口：流程图根目录 48 份 Markdown、当前 C++/ixx 代码、当前实施状态。
核心处理：识别 43 份施工图，定位函数定义和主题代码文件，生成现状图与核查表。
结构承载：流程图/现状流程图和实施记录/现状流程图核查。
输出变化：45 对现状图、43 份新核查表和 1 份目录索引。
调用路径：施工图 -> 当前代码证据 -> 实现状态分类 -> 现状图 -> 二分与偏差核查。
验证方式：生成器复跑、MD/HTML 数量和 Mermaid 一致性检查、规范检查、暂存差异检查。
```

本切片没有修改 C++，没有运行构建或程序，没有加入计划索引或任务队列。它只证明现状基线文档已生成，不证明施工目标全部完成，也不代替后续人工逐节点纠偏。
