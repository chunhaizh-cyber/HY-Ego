# FEATURE-VALUE-TXN-S1 确认故障注入合同设计修订 Codex 断点清单

日期：2026-07-14

决策：JY-328

解决问题：QR-182

计划 / 队列 / 阶段：#274 / DQ-166 / 735

交互：WIN-0001-E28

状态：固定确认前故障自检合同已形成 / 原编号重新待执行 / 735 未实施 / 未改 C++ / 未构建 / 未运行

## 1. 输入入口

```text
提交 769abb3：JY-327 确认故障注入验收缺口正式退回
实施记录/20260714_FEATURE-VALUE-TXN-S1_确认故障注入验收缺口_Codex断点清单.md
规范/仓库与服务分层事务边界规范.md
流程图/20260713_特征值原始材料事务适配代码逻辑流程图_v0.1.md/.html
规范/详细设计/特征值原始材料事务适配详细设计.md
计划/20260713_FEATURE-VALUE-TXN-S1_特征值原始材料事务适配代码实施切片_v0.1.md
海中鱼巣/核心/执行器.结构写入.ixx
海中鱼巣/核心/会话.结构写入.ixx
```

## 2. 当前事实

```text
JY-326 的提交准备只读视图和通用参与者主路径已由执行窗口草稿证明可编译并完成一次完整自检，但该草稿未提交，不是当前代码事实。
A09、A10 实际复用同一错误类型准备拒绝布尔值；参与者未进入已准备阶段，侧表未插入真实记录。
执行窗口未提交草稿中的拟议参与者路径在准备成功后立即进入会话确认，没有合法故障点；当前正式代码仍未实现该参与者路径。
四仓库、候选、原始令牌和确认能力继续封闭；不得为自检向领域服务或参与者扩权。
阶段 735 未实施，#267 仍等待 #274 实际完成和 735。
```

## 3. 裁决

QR-182 采用固定点、编译期隔离的自检重载：

```text
仅 Debug|x64 定义 HY_EGO_ENABLE_STRUCTURE_COMMIT_FAULT_SELF_TEST
-> 执行正常业务回调并请求提交
-> 构造 JY-326 提交准备只读视图
-> 真实特征值参与者准备成功并插入 I64 版本或 Vec 记录
-> 在第一笔仓库确认前命中固定故障点一次
-> 参与者按精确节点撤销侧表记录并释放内部锁
-> 会话撤销未确认写集
-> 释放结构许可并返回内部不一致自检结果
```

明确不采用：

```text
不修改生产二参执行入口签名或行为。
不增加运行期全局 / 静态故障开关、任意点位编号或跨调用状态。
不向参与者、窄视图、特征值服务、领域服务或外部调用方暴露故障控制、令牌、候选或确认能力。
不修改四仓库，不调用旧无令牌 / 带令牌兼容写，不伪造候选。
不让 Release 编译确认故障重载或登记阶段 735 隔离自检。
```

## 4. A09 / A10 独立证据

```text
A09：使用错误节点类型或主信息锚点，独立执行准备拒绝；参与者未准备，侧表不增长，会话写集完整撤销。
A10-I64：使用真实 I64 参与者，准备成功后命中固定故障一次；返回后版本记录不存在、目标句柄不可读、四仓库数量回前态。
A10-Vec：使用真实 VecI64 或 VecU64 参与者，准备成功后命中固定故障一次；返回后 Vec 记录不存在、目标句柄不可读、四仓库数量回前态。
```

禁止 `验收[8] = 验收[7]`、`验收[9] = 验收[7]` 或任何等价复用。A09、A10-I64、A10-Vec 必须分别形成执行次数、快照和结果证据。

## 5. 编译与范围隔离

故障符号只允许出现在：

```text
海中鱼巣/核心/执行器.结构写入.ixx
海中鱼巣/领域/自检.特征值原始材料事务.ixx
海中鱼巣/入口.cpp（仅宏条件 import / 调用 / 汇总）
海中鱼巣.vcxproj（仅 Debug|x64 宏）
```

自检 `.ixx` 在 Release 下仍保持合法模块声明；只排除故障重载、确认故障验收正文和入口登记。生产参与者、窄视图、特征值服务、其它服务、线程、界面和持久化不得出现故障符号。

## 6. 上游同步

已同步修订：

```text
规范/仓库与服务分层事务边界规范.md
流程图/20260713_特征值原始材料事务适配代码逻辑流程图_v0.1.md/.html
规范/详细设计/特征值原始材料事务适配详细设计.md
计划/20260713_FEATURE-VALUE-TXN-S1_特征值原始材料事务适配代码实施切片_v0.1.md
```

流程图、详细设计和计划范围一致：只增加 Debug|x64 固定确认前故障自检能力和独立验收，不扩大生产事务公开面，不实施 #267 业务入口。

## 7. 输出变化

```text
QR-182：待裁决 -> 已按 JY-328 关闭。
#274 / DQ-166：退回设计 -> JY-328 修订后以原编号重新待执行。
阶段 735：仍未实施、未登记。
#267 / DQ-159：继续等待 #274 实际完成和 735。
#272 / 725、#266 / 730：保持已完成，不重复执行。
#273：独立状态不变。
#214、#257：继续暂停。
#268-#271：继续原门控。
WIN-0001：#274 所有权在本次提交发布后转回执行窗口。
```

## 8. 执行前复核与验证边界

执行窗口必须从正式提交重新读取实际接口和计划，不得直接续写 WIP。只有以下条件全部一致才可恢复代码实施：

```text
固定故障点可位于参与者准备成功后、第一笔确认前。
故障路径可先撤销真实侧表记录，再撤销未确认会话写集并释放许可。
生产二参入口、参与者和窄视图不暴露故障控制。
Debug|x64 与 Release|x64 宏边界可由工程配置和静态扫描证明。
允许文件足以闭合，禁止文件差异为 0。
```

本轮只验证文档治理、流程图同源和代码范围未变化；不运行 C++ 构建、程序、自检或连续回归。不得登记 735，不得声明 A01-A16、特征值事务适配、#267 或生产调用迁移已经完成。
