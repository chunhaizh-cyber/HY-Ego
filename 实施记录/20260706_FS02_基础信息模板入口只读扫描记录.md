# FS-02 基础信息模板入口只读扫描记录

日期：2026-07-06
状态：FS-02 第二批已登记 / 只读扫描 / 未改代码 / 未迁移旧函数
依据清单：`实施记录/20260706_逐函数只读扫描清单.md`
前序记录：

```text
实施记录/20260706_FS02_语素入口与基础信息桥接只读扫描记录.md
```

## 1. 本轮定位

本轮继续 FS-02，登记旧项目 `基础信息类` 中与语素入口模板、基础信息模板节点、信息入口默认根、入口引用检索、模板主信息创建和存在概念根兼容有关的函数族。

本轮不重复登记 FS-01 已覆盖的通用链表 / 节点树操作，例如 `添加节点`、`添加子节点`、`重挂节点`、`删除节点`、`更新主信息` 和枚举节点等；这些仍按前序 FS-01 记录处理。本轮只登记它们在“语素入口模板自动创建基础信息节点”路径中的领域边界。

## 2. 只读扫描范围

```text
D:\鱼巢\基础信息类.h
D:\鱼巢\基础信息类.cpp
```

扫描证据来自：

```text
基础信息类.h:41-58：语素 / 基础信息整合入口、模板查找、确保、默认根选择和引用检索声明。
基础信息类.h:88-94：存在概念根缓存、模板主信息创建、入口引用判断和清理辅助声明。
基础信息类.cpp:187-248：查找语素入口模板、确保语素入口模板。
基础信息类.cpp:252-317：选择信息入口默认根、查找引用语素入口节点的基础信息、类型准入。
基础信息类.cpp:320-331：基础信息节点类型匹配和引用语素入口节点判断。
基础信息类.cpp:334-377：按语素入口和主信息类型创建旧基础信息主信息。
基础信息类.cpp:380-404：旧主信息引用语素入口节点和范围判断辅助。
基础信息类.cpp:413-488：获取名称、取或创建存在概念根。
基础信息类.cpp:491-499：私有清理主信息辅助。
```

## 3. 逐函数登记

### OF-060

```text
旧函数编号：OF-060
扫描批次：FS-02
旧项目路径：D:\鱼巢\基础信息类.cpp:188
旧函数名：基础信息类::查找语素入口模板
旧函数签名：节点类* 查找语素入口模板(const 语素入口节点类* 入口节点, 枚举_主信息类型 类型) const
当前调用方：旧 `确保语素入口模板`、语素入口对应基础信息查找、基础信息候选召回路径。
触发路径：按语素入口和主信息类型扫描基础信息树，寻找已有模板节点。
输入：语素入口节点、目标主信息类型。
输出：基础信息节点或空。
读结构：旧基础信息树、旧基础信息主信息字段中的名称 / 类型 / 代词 / 来源动作名 / 二次特征概念引用等。
写结构：无。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：按语素入口引用反查基础信息模板候选。
删除行为：不得用旧主信息字段扫描结果直接裁决唯一事实；不得静默选第一个多目标候选。
新增约束：新项目应通过语素对应信息关系、概念追溯关系和索引候选读取，而不是扫描旧主信息字段。
目标新结构：语素对应信息关系、语素概念追溯关系、索引候选集合。
目标领域服务入口：语素服务只读读取入口、关系仓库集合读取、索引仓库候选读取。
对应规范 / 详细设计：语素系统详细设计、关系仓库详细设计、索引仓库详细设计。
入口拒绝条件：入口节点无效、目标类型未定义、多目标语义未确认、查询结果未复核节点类型。
验证方式：通过关系集合读回绑定目标；索引命中后由领域服务复判。
初步判定：改写为领域服务入口
待用户确认项：旧主信息字段扫描是否仅保留为旧项目扫描依据，不进入新项目查询实现。
扫描证据：`基础信息类.cpp:187` 至 `基础信息类.cpp:203`。
```

### OF-061

```text
旧函数编号：OF-061
扫描批次：FS-02
旧项目路径：D:\鱼巢\基础信息类.cpp:206
旧函数名：基础信息类::确保语素入口模板
旧函数签名：节点类* 确保语素入口模板(const 语素入口节点类* 入口节点, 枚举_主信息类型 类型, 节点类* 父节点)
当前调用方：旧语素入口到基础信息模板自动创建路径。
触发路径：语素入口没有对应基础信息模板时，按入口类型和默认根自动创建基础信息节点。
输入：语素入口节点、目标主信息类型、可选父节点。
输出：已有或新建基础信息节点。
读结构：旧语素入口信息入口类型、信息入口存储树、旧基础信息树。
写结构：旧基础信息节点、旧基础信息主信息、旧父子关系。
是否写机器事实：是，旧实现会自动创建基础信息模板节点。
是否依赖日志：否；非法入口时抛异常。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：避免重复创建同一入口模板；确认默认根和上下文限定。
删除行为：不得由语素系统越权自动创建基础信息、高级信息或实例状态 / 动态；不得复制旧主信息字段构造路径。
新增约束：目标基础信息节点必须由对应基础信息原子服务或高级服务创建；语素服务只绑定已有节点。
目标新结构：基础信息节点、语素对应信息关系、概念追溯关系。
目标领域服务入口：存在服务、场景服务、特征服务、状态服务、动态服务、二次特征服务、因果服务、语素服务。
对应规范 / 详细设计：基础信息服务分层规范、语素系统详细设计、领域服务授权写入规范。
入口拒绝条件：语素入口无效、类型未定义、信息入口需要上下文但缺上下文、目标服务未授权创建、半结构创建失败。
验证方式：目标服务先创建节点，语素服务再绑定；创建失败不得被读作有效模板节点。
初步判定：拆分
待用户确认项：旧“确保”语义是否完全删除，还是后续改成世界服务编排入口。
扫描证据：`基础信息类.cpp:205` 至 `基础信息类.cpp:248`。
```

### OF-062

```text
旧函数编号：OF-062
扫描批次：FS-02
旧项目路径：D:\鱼巢\基础信息类.cpp:252
旧函数名：选择信息入口默认根 / 查找引用语素入口节点的基础信息 / 主信息类型是否可由语素入口模板创建
旧函数签名：节点类* 选择信息入口默认根(...); std::vector<节点类*> 查找引用语素入口节点的基础信息(...); static bool 主信息类型是否可由语素入口模板创建(...)
当前调用方：旧模板自动归位、入口候选召回和模板创建准入路径。
触发路径：需要根据信息入口类型选择基础信息树根，或枚举引用某语素入口的基础信息节点。
输入：信息入口类型、上下文节点、语素入口节点、范围根、主信息类型。
输出：默认根节点、候选节点集合或准入 bool。
读结构：旧信息入口定义、旧基础信息树、旧主信息字段。
写结构：无。
是否写机器事实：否；但调用方可能据此创建事实。
是否依赖日志：否。
是否依赖显示：默认根名可服务导航显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：默认根选择、上下文限定和候选枚举语义。
删除行为：不得把默认根文本、存储树枚举或候选 bool 当作机器事实；不得自动归位高级信息根树。
新增约束：默认根只能作为领域服务编排或候选定位材料；权威事实由节点、关系和领域服务入口承载。
目标新结构：语素索引、关系候选集合、领域树关系。
目标领域服务入口：语素服务、世界服务聚合入口、对应领域服务。
对应规范 / 详细设计：仓库逻辑空间与领域树结构规范、语素系统详细设计、领域服务授权写入规范。
入口拒绝条件：信息入口未定义、缺上下文、存储树未确认、候选冲突未处理。
验证方式：候选查询只读；领域服务复判后才写结构。
初步判定：拆分
待用户确认项：信息入口默认根是否进入“仓库逻辑空间与领域树图结构”后续增强。
扫描证据：`基础信息类.cpp:252` 至 `基础信息类.cpp:317`。
```

### OF-063

```text
旧函数编号：OF-063
扫描批次：FS-02 / FS-03 / FS-07
旧项目路径：D:\鱼巢\基础信息类.cpp:320
旧函数名：基础信息节点匹配类型 / 基础信息节点引用语素入口节点 / 私有_主信息引用语素入口节点
旧函数签名：static bool 基础信息节点匹配类型(...); static bool 基础信息节点引用语素入口节点(...); static bool 私有_主信息引用语素入口节点(...)
当前调用方：旧模板查找、引用枚举、候选召回和范围检索路径。
触发路径：判断节点类型是否匹配、主信息是否引用目标语素入口。
输入：基础信息节点、主信息类型、语素入口节点。
输出：局部 bool。
读结构：旧基础信息主信息字段，包括名称、类型、指代代词、动态来源动作名、二次特征概念入口和语言词性。
写结构：无。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：节点类型复核和入口引用复核。
删除行为：不得把局部 bool 持久化为业务事实；不得继续依赖主信息字段表达语素引用。
新增约束：语素引用必须由关系仓库承载；类型复核必须用节点类型、状态 / 动态抽象实例边界和领域服务准入。
目标新结构：节点类型、语素对应信息关系、语素概念追溯关系、动作入口关系。
目标领域服务入口：语素服务、关系仓库、动态服务、二次特征服务。
对应规范 / 详细设计：节点类型与关系类型枚举规范、语素系统详细设计、动态服务详细设计、二次特征服务详细设计。
入口拒绝条件：节点无效、节点类型错误、实例状态 / 动态被长期语素绑定、用 bool 裁决事实。
验证方式：关系读回替代主信息字段扫描；局部判断不写结构。
初步判定：改写为领域服务入口
待用户确认项：动态来源动作名旧字段引用语素入口是否转入动作入口 / 动态专项进一步扫描。
扫描证据：`基础信息类.cpp:320` 至 `基础信息类.cpp:331`、`基础信息类.cpp:380` 至 `基础信息类.cpp:404`。
```

### OF-064

```text
旧函数编号：OF-064
扫描批次：FS-02 / FS-03 / FS-07
旧项目路径：D:\鱼巢\基础信息类.cpp:335
旧函数名：基础信息类::私有_创建语素入口模板主信息
旧函数签名：static 基础信息基类* 私有_创建语素入口模板主信息(const 语素入口节点类* 入口节点, 枚举_主信息类型 类型)
当前调用方：旧 `确保语素入口模板`。
触发路径：根据语素入口和目标主信息类型直接 new 对应旧主信息对象。
输入：语素入口节点、目标主信息类型。
输出：旧基础信息主信息对象指针。
读结构：无；只使用入口节点和类型。
写结构：新建旧主信息对象，字段写入名称、类型、来源动作名、语言词性等。
是否写机器事实：是，旧实现为基础信息模板事实构造主信息。
是否依赖日志：否。
是否依赖显示：部分字段可能供显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：按目标类型选择创建路径的分发表意。
删除行为：不得把旧主信息类字段族搬入新项目；不得由一个私有分发函数绕过基础信息原子服务。
新增约束：存在、场景、状态、动态、二次特征、因果分别由对应服务创建；动作入口和动态来源不得由文本动作名承载。
目标新结构：存在节点、场景节点、抽象状态节点、抽象动态节点、二次特征节点、因果引用节点、语素关系。
目标领域服务入口：存在服务、场景服务、状态服务、动态服务、二次特征服务、因果服务、语素服务。
对应规范 / 详细设计：基础信息服务分层规范、状态服务详细设计、动态服务详细设计、二次特征服务详细设计、因果服务详细设计。
入口拒绝条件：目标类型不允许、目标服务缺失、旧字段直接迁移、半结构创建失败。
验证方式：各原子服务分别创建并读回节点；语素绑定关系由语素服务读回。
初步判定：拆分
待用户确认项：状态 / 动态模板入口是否纳入后续“基础信息服务完整增强”。
扫描证据：`基础信息类.cpp:334` 至 `基础信息类.cpp:377`。
```

### OF-065

```text
旧函数编号：OF-065
扫描批次：FS-02
旧项目路径：D:\鱼巢\基础信息类.cpp:420
旧函数名：基础信息类::取或创建存在概念根
旧函数签名：存在节点类* 取或创建存在概念根()
当前调用方：旧信息入口默认根选择、存在概念模板归位和基础信息初始化路径。
触发路径：需要存在类信息最高抽象根，或把旧“存在概念根”兼容为权威存在根时调用。
输入：旧语素入口词 `"存在"`、`"存在概念根"`、`"概念根"`。
输出：存在概念根节点。
读结构：旧基础信息树、旧存在节点主信息名称 / 类型。
写结构：可能改写旧存在节点主信息名称 / 类型，或创建新存在节点。
是否写机器事实：是，旧实现会创建或迁移存在根节点。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：最高抽象存在根的结构需求，以及旧根名兼容识别。
删除行为：不得用文本根名裁决权威存在根；不得直接改写主信息名称 / 类型完成迁移。
新增约束：存在根应由存在服务或世界服务聚合入口创建；旧文本根名仅作为只读迁移线索。
目标新结构：存在节点、存在概念根关系或世界聚合根关系、语素概念入口。
目标领域服务入口：存在服务、世界服务、语素服务。
对应规范 / 详细设计：存在服务详细设计、世界聚合服务详细设计、仓库逻辑空间与领域树结构规范。
入口拒绝条件：语素入口创建失败、根节点类型错误、旧根冲突、多根未确认、直接文本改写主信息。
验证方式：通过存在服务读回根节点；多根冲突返回冲突或拒绝，不静默裁决。
初步判定：改写为领域服务入口
待用户确认项：存在概念根是否需要独立“世界种子 / 根概念”设计。
扫描证据：`基础信息类.cpp:419` 至 `基础信息类.cpp:488`。
```

### OF-066

```text
旧函数编号：OF-066
扫描批次：FS-02
旧项目路径：D:\鱼巢\基础信息类.cpp:413; D:\鱼巢\基础信息类.cpp:492
旧函数名：基础信息类::获取名称 / 私有_清理主信息_已加锁
旧函数签名：std::string 获取名称(const 节点类* 节点) const; void 私有_清理主信息_已加锁() noexcept
当前调用方：旧显示 / 语言命名、析构或资源清理路径。
触发路径：读取基础信息人读名称，或释放基础信息树节点主信息。
输入：基础信息节点或当前基础信息树。
输出：人读名称字符串；清理函数无返回。
读结构：旧基础信息主信息和语素词面。
写结构：清理函数删除旧主信息指针并置空。
是否写机器事实：`获取名称` 否；清理函数改变运行期内存生命周期。
是否依赖日志：否。
是否依赖显示：`获取名称` 是。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：只读名称生成、人读显示和生命周期清理必须可区分。
删除行为：不得把名称文本写为机器事实；不得用清理函数替代仓库生命周期 / 版本失效。
新增约束：显示层只读；节点生命周期由节点仓库和句柄版本管理；清理只作为资源释放。
目标新结构：无权威目标结构；节点仓库生命周期版本、显示层只读视图。
目标领域服务入口：显示层只读视图专项、节点仓库删除 / 失效入口。
对应规范 / 详细设计：节点仓库详细设计、领域服务授权写入规范、UTF-8 与中文标识符工程规范。
入口拒绝条件：用显示名称裁决事实、裸删主信息绕过仓库生命周期、清理后仍读作有效节点。
验证方式：显示读取不改变结构；删除 / 失效后旧句柄不可读。
初步判定：拆分
待用户确认项：显示名称是否纳入显示层只读视图专项，清理逻辑是否只归入节点仓库生命周期。
扫描证据：`基础信息类.cpp:413` 至 `基础信息类.cpp:417`、`基础信息类.cpp:491` 至 `基础信息类.cpp:499`。
```

## 4. 本轮初步结论

```text
1. 旧 `基础信息类` 的语素模板入口把查找、默认根选择、自动创建基础信息节点和主信息字段构造混在一起，新项目必须拆分。
2. 语素服务只应绑定已有信息节点；基础信息节点创建应分别交给存在、场景、状态、动态、二次特征和因果等原子服务或世界聚合服务。
3. 旧主信息字段中的名称、类型、代词、动态来源动作名、语言词性等只可作为旧扫描线索，不得迁移为新项目权威语素引用字段。
4. 旧存在概念根兼容逻辑有价值，但不能用文本根名静默裁决权威根；后续需要世界种子 / 根概念设计或存在服务入口承接。
5. 本轮仍只登记旧函数事实，不形成迁移许可。
```

## 5. 后续待执行

```text
1. 下一旧函数编号从 OF-067 开始。
2. 继续 FS-02，扫描旧存在类、场景类、状态类、动态类、二次特征类和因果类第一批函数。
3. 涉及特征值、动作动态、因果可靠性、体素和外设时，应转 FS-03、FS-07 或 FS-X 分流。
4. OF-060 到 OF-066 均为初步判定，需用户确认后才可进入任何代码实施。
```

## 6. 禁止宣称

```text
不得声明旧函数已扫描完成。
不得声明旧基础信息类已迁移。
不得声明旧基础信息树、旧主信息字段或旧存在概念根兼容逻辑可直接进入新项目。
不得声明基础信息服务分层、语素系统、世界聚合服务或存在根概念系统完整完成。
不得接控制面板 / SQL / D455 / 体素 / 外设。
```
