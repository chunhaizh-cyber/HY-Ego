# 20260708 FLOW-07 任务承接需求代码实施 Codex 断点清单

日期：2026-07-08

状态：已完成 / 已验证 / 已归档计划 / 代码已实现 / 未迁移旧函数

## 1. 输入入口

- 可执行队列：`项目记忆/Codex任务队列.md` #136。
- 施工计划：`计划/已完成计划/20260708_FLOW-07_任务承接需求施工计划_v0.1.md`。
- 上游流程图：`流程图/20260708_任务承接需求代码逻辑流程图_v0.1.md`。
- 上游详细设计：`规范/详细设计/任务承接需求代码逻辑详细设计.md`。

## 2. 核心处理

- 在 `海中鱼巣/入口.cpp` 增加 FLOW-07 独立验收：
  - TASK-A1 有效需求创建任务。
  - TASK-A2 无效需求拒绝。
  - TASK-A3 缺发生时间戳拒绝。
  - TASK-A4 缺承接材料拒绝。
  - TASK-A5 显式场景不一致拒绝。
  - TASK-A6 承接材料读回。
  - TASK-A7 实际结果状态记录。
  - TASK-A8 完成状态前置拒绝。
  - TASK-A9 任务方法选择。
  - TASK-A10 后续请求材料只读。
  - TASK-A11 高级服务封口。
  - TASK-A12 排除项边界。
- 将显式场景不一致、非方法节点选择、无效实际结果状态、缺时间戳完成状态纳入拒绝前后结构数量快照。

## 3. 结构承载

- 任务承接需求仍由任务节点、来源需求关系、任务虚拟存在关系、运行场景关系、目标状态关系和创建状态关系承载。
- 实际结果状态和任务选择方法仍采用任务服务唯一关系入口；重复记录或错误节点类型在入口拒绝。
- 生命周期迁移、筹办回执、执行桥和运行统计仍为只读请求材料，不写权威任务状态机、不派发执行。

## 4. 输出变化

- `海中鱼巣/入口.cpp` 新增 FLOW-07 和 TASK-A1 到 TASK-A12 验收输出。
- 未修改 `海中鱼巣/领域/任务服务.h`、需求服务、方法服务、核心仓库或工程文件。
- 本计划已归档到 `计划/已完成计划/`。
- `项目记忆/Codex任务队列.md` #136 已标记完成，当前下一项为 #137 BASE-D6。

## 5. 验证结果

```powershell
git diff --check -- 海中鱼巣/领域/任务服务.h 海中鱼巣/入口.cpp
python .\tools\check_specs.py
msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m
.\x64\Debug\海中鱼巣.exe
rg -n "控制面板|SQL|ADO|WebView|D455|体素|外设|std::thread|std::async|工作队列" 海中鱼巣/领域/任务服务.h 海中鱼巣/入口.cpp
```

- `git diff --check`：通过；仅提示 LF/CRLF，未发现 whitespace error。
- `python .\tools\check_specs.py`：通过，输出 `海中鱼巣规范治理检查: 未发现问题`。
- Debug x64 构建：通过，0 警告，0 错误。
- 默认入口：返回 0；`FLOW-07 任务承接需求第一轮`、TASK-A1 到 TASK-A12 均输出 `通过`；既有 `MATRIX-05`、`服务操作函数矩阵第一批` 和最小闭环验收也输出通过。
- 排除项扫描：仅命中既有人读输出标签，不是控制面板、SQL、ADO、WebView、D455、体素、外设、线程或工作队列接入代码。

## 6. 完成声明边界

本次可以声明：

```text
FLOW-07 任务承接需求第一轮入口拒绝、承接壳读回、实际结果状态、任务方法关系和请求材料只读验收已通过。
```

不得声明：

```text
完整任务状态机已实现。
任务筹办、真实执行桥、线程派发、工作队列、任务结果回写或旧任务能力已迁移完成。
控制面板、SQL、ADO、WebView、D455、体素或外设已接入。
```
