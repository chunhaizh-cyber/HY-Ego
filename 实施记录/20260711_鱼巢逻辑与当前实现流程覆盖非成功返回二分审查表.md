# 鱼巢逻辑与当前实现流程覆盖非成功返回二分审查表

日期：2026-07-11（2026-07-12 按 HEAD `15032ed` 复核）

| 节点 | 代码位置 | 返回条件 | 输入语境 | 是否上游保证有效 | 结构是否变化 | 设计是否允许 | 二分口径 | 追根因解决 | 逻辑内返回 | 计划动作 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| C | `D:\鱼巢` Git | ahead / dirty，证据未冻结 | 审计材料 | 否 | 否 | 是 | 逻辑内返回 | 否 | 只记录无法完成全量判定 | S0 冻结 HEAD 与 dirty diff |
| O0 | `鱼巢.cpp:1490` 起 | 外部命令选择不同运行模式 | 外部请求 | 否 | 否 | 是 | 逻辑内返回 | 否 | 合法命令分支 | 旧启动图逐项列出 |
| O1 | `自我类.初始化.cpp:1097-1224` | 某初始化阶段进入后结果不完整 | 正式初始化 | 是 | 可能已有多段写入 | 否 | 追根因解决 | 停止依赖并追查首次异常阶段 | 否 | 九阶段图补写入 / 读回 / 收口 |
| O2 | `自我线程模块.impl.cpp` | 收到停止请求 | 运行控制 | 否 | 只改线程生命周期 | 是 | 逻辑内返回 | 否 | 正常停止分支 | 修复旧自我线程图并补契约 |
| O3-O8 | 旧需求任务方法主链 | 上游正式节点有效但内部找不到承接关系、方法、结果或结算证据 | 正式运行链 | 是 | 可能已进入结构承载 | 否 | 追根因解决 | 追查旧主链首次断点 | 否 | 各目标单独审查，不能按“空候选”统一处理 |
| OE | 旧外设入口 | 无帧、过期、无法识别或未接设备 | 外部材料 | 否 | 否 | 是 | 逻辑内返回 | 否 | 外部材料拒绝或等待 | 只画证据图，保持暂缓 |
| N0 | `入口.cpp:365` 起 | 自检汇总失败 | 当前自检 | 自检内部材料部分保证 | 自检可能已创建隔离 / 样例结构 | 是 | 追根因解决 | 找到首个失败验收项，不以总 bool 掩盖 | 否 | 总入口图关联每个自检模块 |
| N1 | 当前自我线程启动 | 零稳定键或两个稳定键相同 | 外部 / 自检错误参数 | 否 | 否 | 是 | 逻辑内返回 | 否 | 参数无效且结构不变 | 初始化现状图保留该分支 |
| N1 | 当前自我线程启动 | 参数通过后初始化读回不完整 | 正式启动 | 是 | 是 | 否 | 追根因解决 | 停止进入常驻或控制面板 | 否 | 补初始化内部首次异常点 |
| N2 | 自我治理领域路由 | 消息准入成功后领域写入或读回矛盾 | 正式治理消息 | 是 | 可能写权威结构 | 否 | 追根因解决 | 追查路由 / 服务首次异常 | 否 | 优先生成领域路由现状图 |
| N4 | SQL 审计段 | 配置缺失、连接失败或数据库不可用 | 外部适配 | 否 | 运行期结构不变 | 是 | 逻辑内返回 | 否 | 记录审计不可用；普通模式可继续 | SQL 图明确普通模式与 DB 自检模式 |
| N4 | SQL 审计段 | 写入成功后读回编号或数量矛盾 | 审计往返 | 是 | 已写审计数据库 | 否 | 追根因解决 | 审计适配内部不一致 | 否 | 增加往返读回节点 |
| N6 | 控制面板窗口 | 正常关闭 | UI 请求 | 否 | 权威结构不变 | 是 | 逻辑内返回 | 否 | 正常运行结束 | 窗口现状图标正常关闭 |
| N6 | 控制面板窗口 | 依赖有效且进入窗口运行后内部失败 | 正式运行 | 是 | 权威结构应不变 | 否 | 追根因解决 | 记录逻辑错误并返回 1 | 否 | 补窗口失败阶段映射 |
| NDA/NDB | 43 张批量现状图 | MD/HTML 与合并核查表存在，但图基线为 `3f08239`，HEAD 已有 17 个源码变化 | 文档覆盖判定 | 否 | 否 | 是 | 逻辑内返回 | 否 | 保留历史基线，但不计为 HEAD 当前完整包 | S2 先做逐图漂移分类，只刷新受影响目标 |
| ND1 | CONCEPT-S5 兼容副本 | 根目录与专门目录 MD/HTML 哈希不一致，目标代码也已漂移 | 文档治理 | 否 | 否 | 否 | 追根因解决 | 追查唯一正式入口和未同步原因，禁止两份图同时充当当前事实 | 否 | S2D 统一入口并按 HEAD 重验 |
| ND2 | THREAD-S1 文档包 | 缺契约和二分表 | 文档治理 | 否 | 否 | 否 | 追根因解决 | 不能继续标为完整现状包 | 否 | 补齐产物后重跑严格检查 |
| ND3 | HEAD 源码覆盖集合 | 现状图只绑定 38/59 个源码文件 | 文档覆盖判定 | 否 | 否 | 是 | 逻辑内返回 | 否 | 输出缺口集合，不宣称全量 | S2 将 21 个文件分类为归入已有流程、新图或无独立运行逻辑 |
| ND4 | 批量基线后的根目录图 | 新增 8 张图且专门目录没有对应更新 | 施工图 / 现状图收口 | 否 | 否 | 是 | 逻辑内返回 | 否 | 未实施计划保持施工身份；已实施范围登记待回绘 | S2 按实现提交分类，S4 等 #236 |

## 审查结论

“没有结构变化”本身不能决定逻辑内返回。正式流程已经保证输入有效、或已经进入创建 / 绑定 / 写关系 / 写状态 / 动态 / 结算 / 读回后出现非预期结果，统一归为追根因解决。
