# TASK-PLANNING-S1 节点 14 仓库准入允许范围合同漂移修订 Codex 断点清单

日期：2026-07-13

治理编号：JY-304

关联队列：#223 / DQ-115

状态：文档治理完成 / #223 前置已满足并恢复为待实际接口复核 / 未改 C++ / 未构建 / 未运行程序

## 1. 输入入口

```text
#223 / JY-303 执行前实际接口复核回传
HEAD 4a8bdd469ab60b0ab45eb904e6c47e7f0e7511c2
海中鱼巣/核心/节点仓库.cpp
海中鱼巣/核心/节点仓库.h
海中鱼巣/核心/句柄.h
规范/节点类型与关系类型枚举规范.md
流程图/20260711_任务生命周期筹办通用执行与结果结算流程图_v0.1.md
流程图/20260711_任务生命周期筹办通用执行与结果结算流程图_v0.1.html
规范/详细设计/任务生命周期筹办通用执行与结果结算详细设计.md
计划/20260711_TASK-PLANNING-S1_筹办召回与任务服务选择提交代码实施切片_v0.1.md
```

## 2. 已验证当前事实

1. JY-302 预期追加 `节点类型::任务方法选择记录 = 14`，用于承载不可变选择记录。
2. 当前 `节点仓库.cpp` 私有 `节点类型已定义` 固定为 `未分类 <= 类型 <= 二次特征`，实际最高只接受 13。
3. `节点仓库::创建节点` 的无令牌和令牌路径都在写入前调用该私有函数；追加枚举 14 而不调整上限时，两条路径都会稳定拒绝记录节点。
4. #223 原允许文件不含节点仓库，禁止文件同时列出 `.h/.cpp`，执行窗口不能自行修正。
5. 执行窗口撤回前的草案已达到 Debug x64 0/0，但阶段 580 首笔选择提交稳定拒绝并定位到记录节点创建；草案、自检和工程改动已完整撤回，不能作为完成证据。

## 3. JY-304 裁决

### 3.1 唯一允许的节点仓库修改

```text
文件：海中鱼巣/核心/节点仓库.cpp
位置：私有 节点类型已定义
修改：准入上限由 节点类型::二次特征 改为 节点类型::任务方法选择记录
```

`海中鱼巣/核心/节点仓库.h` 继续禁止修改。不得借本次修订改变：

```text
公开 ABI
结构事务接线或令牌验证
仓库锁与锁序
节点编号、创建序号或版本推进
创建、读取、删除、审计流程
主信息有效性前置拒绝
其它节点类型准入或业务语义
```

### 3.2 写前拒绝边界

1. `节点类型::任务方法选择记录 = 14` 可沿节点仓库正式创建入口创建并完整读回。
2. `static_cast<节点类型>(15)` 及更大未知值仍必须在第一笔节点写入前拒绝。
3. 未知节点拒绝后，节点、主信息和关系数量不得增长；不得依靠事后扫描或清理恢复。

## 4. 流程与验收同步

任务选择流程 S3 固定为：

```text
枚举追加节点 14
-> 节点仓库私有准入同步到 14
-> 创建未发布记录主信息 / 节点
-> 写完整规范元组
-> 创建方法投影
-> 关系 16 最后发布
```

A15 必须同时验证节点 14 正例、未知节点 15 负例、关系 16 / 未知关系 17、发布前清理、关系 15 和普通引用回归。节点仓库上限调整不能从 A15 中拆成只编译检查。

## 5. 范围一致性

节点枚举规范、任务生命周期流程图 MD/HTML、详细设计 TL-D45 / A15 和 #223 计划现均允许且仅允许 `节点仓库.cpp` 私有上限调整；`节点仓库.h` 仍在禁止文件中。上游默认裁决、流程顺序、允许文件和验收范围一致，#223 可恢复为待实际接口复核。

## 6. 执行前复核

```powershell
rg -n "节点类型已定义|二次特征|任务方法选择记录" .\海中鱼巣\核心\节点仓库.cpp .\海中鱼巣\核心\句柄.h
rg -n "节点仓库.cpp|节点仓库.h|TASK-PLANNING-S1-A15" .\计划\20260711_TASK-PLANNING-S1_筹办召回与任务服务选择提交代码实施切片_v0.1.md
git diff -- .\海中鱼巣\核心\节点仓库.cpp .\海中鱼巣\核心\节点仓库.h
```

若接受节点 14 需要修改头文件、ABI、令牌、锁、编号、版本、创建流程或其它逻辑，必须再次停在代码修改前退回设计。

## 7. 完成声明边界

本治理只证明节点 14 仓库准入的允许范围与 A15 已补齐。它不证明节点 14、关系 16、#223、阶段 580、任务选择、动作桥、通用执行或后续门控计划已经实现。
