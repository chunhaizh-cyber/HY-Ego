# COMPAT-CLOSURE-S1 精确兼容矩阵与施工合同生成 Codex 断点清单

日期：2026-07-15

决策：JY-340

计划 / 队列 / 阶段：#271 / DQ-163 / COMPAT-CLOSURE-S1 / 780

状态：纯设计与治理完成 / 当前待执行 / 未改 C++ / 未构建 / 未运行程序 / 780 未实施

## 1. 输入入口

```text
WIN-0001-E50 与提交 10a10be
JY-339 自动本地提交规则提交 23238dd
#262 精确双路径事实复核
#263-#270、#272、#274-#278、#250、#249 正式产物
当前领域、线程、入口、初始化、界面、SQL 和工程代码事实
原 #271 计划骨架
```

## 2. 核心处理

1. 重新扫描新正式业务服务、组合器和运行期请求路由。
2. 将正式内部事务能力与传统兼容入口分开，避免把数据操作 / 核心 ABI误判为待删除路径。
3. 逐类登记传统服务头、旧线程、历史初始化、生命周期 / 统计 / 安全删除、入口自检和只读显示适配。
4. 裁决第一轮不批量删除，只新增独立边界自检和静态不增长门禁。
5. 固定入口参数写前拒绝、内部非预期追根因、禁止兼容降级和结构不变化验收。
6. 形成 #271 流程图、详细设计和可执行计划，保持原编号与阶段 780。

## 3. 当前事实证据

```text
正式新业务公开面：14 个服务 + 7 个组合器 + 1 个运行期请求路由 = 22 个文件
禁用原始事务能力命中文件：0
传统服务头：17 个
传统服务头直接包含文件：51 个
自检模块：35 个，其中直接仓库自检 32 个
运行期装配内部仓库 / 事务文本命中：16；公开业务出口仍只有只读业务操作组合器
概念命名路由：仍取得独占许可
概念安全删除：仍为独立兼容事务所有者
```

以上数字是当前文本和调用点基线，不等于可删除数量。

## 4. 结构承载

新增：

```text
实施记录/20260715_COMPAT-CLOSURE-S1_精确兼容入口与调用点矩阵.md
流程图/20260715_服务写入兼容路径精确收口代码逻辑流程图_v0.1.md
流程图/20260715_服务写入兼容路径精确收口代码逻辑流程图_v0.1.html
规范/详细设计/服务写入兼容路径精确收口详细设计.md
实施记录/20260715_COMPAT-CLOSURE-S1_精确兼容矩阵与施工合同生成_Codex断点清单.md
```

修订：

```text
计划/20260713_COMPAT-CLOSURE-S1_服务写入兼容路径收口计划_v0.1.md
计划/计划索引.md
项目记忆/Codex任务队列.md
项目记忆/当前状态.md
项目记忆/决策记录.md
项目记忆/验收记录.md
项目记忆/窗口交互记录.md
```

## 5. 输出变化

1. #271 从“等待精确矩阵 / 不可执行”改为 JY-340 当前待执行。
2. 阶段 780 固定为兼容边界自检和静态不增长验收，不承担生产兼容路径删除。
3. 允许代码范围收窄为一个新 Debug 自检真模块、入口最小登记和工程 / filters。
4. 17 个传统服务头、旧线程、四个历史初始化兼容模块、生命周期 / 统计 / 安全删除和历史自检明确禁止批删。
5. #271 完成后只解除 #257 架构前置；#214 继续用户暂停。

## 6. 调用路径

```text
当前代码事实
-> 精确兼容矩阵
-> 公开面 / 内部能力 / 兼容所有者分层
-> 流程图
-> 详细设计
-> #271 完整施工计划
-> 执行窗口重新逐签名复核
-> 一致后实施 780；漂移则原编号退回设计
```

未来运行期自检路径：

```text
完整运行期上下文候选
-> 运行期业务请求路由
-> 运行期业务操作组合器
-> 无令牌服务
-> 数据操作 / 执行器 / 会话
-> 四仓库
-> 强类型值式结果
```

不得切换到传统服务兜底。

## 7. 验证

本轮只执行文档治理验证：

```text
Markdown / HTML Mermaid 同源
tools/check_specs.py --strict
git diff --check
计划 / 索引 / 队列 / 决策一致性
780 与新模块名唯一性
C++、工程和 filters 在设计轮零差异
```

本轮未运行 C++ 构建、程序、自检或连续回归，不能声明 780 已实施。

## 8. 完成边界

本轮完成只证明 #271 的精确矩阵、流程图、详细设计和施工合同已经形成并登记待执行。

不证明传统兼容路径已删除、生产调用已全部迁移、#257 已实施、#214 已恢复、概念生命周期 / 统计 / 安全删除已新分层或项目迁移完成。
