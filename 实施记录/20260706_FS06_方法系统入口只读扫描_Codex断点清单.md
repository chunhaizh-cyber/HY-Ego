# FS-06 方法系统入口只读扫描 Codex 断点清单

日期：2026-07-06
状态：已记录断点 / 文档切片 / 未改代码 / 未迁移旧函数

## 1. 本切片输入

```text
FS-05 第二批任务模块筹办 / 执行 / 管理边界扫描已提交并推送。
计划索引 HR-001-FM-SCAN 要求继续 FS-06，扫描旧项目方法系统函数第一批。
用户明确“不需要确定的步骤应该持续执行下去”。
```

## 2. 已完成动作

```text
1. 回读计划索引、项目规则总纲、规则迁移清单、函数迁移前置专项、逐函数只读扫描清单、方法系统规范和方法系统详细设计。
2. 只读扫描 `D:\鱼巢\方法主信息类.h`、`D:\鱼巢\方法类.h`、`D:\鱼巢\方法类.cpp`、`D:\鱼巢\方法虚拟存在服务类.h`、`D:\鱼巢\方法虚拟存在服务类.cpp`、`D:\鱼巢\方法节点类型.h`、`D:\鱼巢\方法信息类型.h`。
3. 新增 `实施记录/20260706_FS06_方法系统入口只读扫描记录.md`。
4. 登记 OF-151 到 OF-161。
5. 同步逐函数只读扫描清单、计划索引和项目记忆。
```

## 3. 本切片结论

```text
旧方法系统把方法角色、动作句柄、来源任务、父 / 前置 / 后续方法、条件 / 结果场景、成熟度、可查找策略和统计缓存集中在方法主信息字段与方法虚拟存在特征中。
新项目不得直接迁移旧函数体或旧字段：方法角色必须走抽象状态和关系，动作句柄必须转入动作入口边界，方法虚拟存在必须经存在服务和方法关系绑定，统计和候选命中只能进入非权威缓存或后续方法治理。
```

## 4. 下一步

```text
下一旧函数编号从 OF-162 开始。
FS-06 第一批已覆盖旧项目方法系统入口第一轮边界，可转入 FS-07 旧项目动作 / 动态 / 因果函数第一批只读扫描。
若后续发现 FS-06 遗漏，只能追加登记，不得把当前记录解释为迁移许可。
```

## 5. 禁止宣称

```text
不得声明旧方法系统、旧方法主信息、旧方法虚拟存在服务或旧方法查找能力已迁移。
不得声明完整方法查找、候选排序、条件结果对、方法学习或动作执行系统完成。
不得把动作名、函数名、本能函数 ID、旧动作句柄字段或 SQL 显示文本当作动作身份。
不得接控制面板 / SQL / D455 / 体素 / 外设。
不得声明旧函数扫描已完成或迁移判定已确认。
```

## 6. 验证记录

```text
本切片只做文档和只读事实记录验证。
未改代码。
未运行构建。
未运行当前项目入口。
未运行旧项目程序。
```
