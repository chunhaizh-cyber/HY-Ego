# FS-04 需求树入口只读扫描记录

日期：2026-07-06
状态：FS-04 第一批已登记 / 只读扫描 / 未改代码 / 未迁移旧函数
依据清单：`实施记录/20260706_逐函数只读扫描清单.md`

## 1. 本轮定位

本轮进入 FS-04，登记旧项目 `需求类` 和 `需求主信息类` 中与需求身份、需求目标语义、目标状态引用、父子需求角色、需求更新指令、需求结算摘要、对应任务、任务初始化上下文、需求方向和 SQL 投影有关的函数族。

本轮只做只读事实登记，不迁移旧 `需求主信息类` 字段，不把旧累计安全 / 服务结算、最近结算任务、最近结算时间或 SQL 投影作为新项目权威结算事实。当前项目口径仍是：需求目标是目标状态，不是 I64；权威需求结算记录以结算实例状态和关系承载；非权威摘要只能由权威记录重建。

## 2. 只读扫描范围

```text
D:\鱼巢\需求类.h
D:\鱼巢\需求类.cpp
D:\鱼巢\需求主信息类.h
D:\鱼巢\需求节点类型.h
D:\鱼巢\需求信息类型.h
```

扫描证据来自：

```text
需求类.h:3-6、需求类.cpp:3-6：旧需求类文件头部规则，需求树正式变化只能经需求类提交入口，SQL 只投影显示。
需求主信息类.h:10-35：旧需求主信息定位，需求表达当前状态到目标状态关系，父子结构由树指针表达。
需求主信息类.h:17-43：描述、统计、需求主体、场景、目标存在、当前状态、目标状态、满足关系、目标特征缓存、对应任务和任务列表字段。
需求主信息类.h:45-54：需求有效截止、阻塞父任务、安全 / 服务权重、累计结算、最近结算任务和最近结算时间字段。
需求类.h:18-109：需求树更新指令、枚举目标校验和 SQL / 统计 / 任务绑定材料。
需求类.h:111-220：需求结算收束结果、需求目标视图、需求方向签名和任务初始化上下文。
需求类.h:221-360：逻辑组织需求、获取途径、父子需求角色和逻辑组织需求更新输入。
需求类.cpp:1289-1663：逻辑组织类型、目标语义、获取途径和缺口入树准入函数。
需求类.cpp:1656-1822：读取需求目标特征、需求主体、目标宿主、当前 / 目标状态、场景、对应任务、任务列表、满足关系、截止时间。
需求类.cpp:1828-1869、2721-2783：SQL 控制面板需求显示项和需求树 SQL 投影。
需求类.cpp:1904-1983：父子需求关系角色、阻塞父任务、权重读取。
需求类.cpp:1983-2046：提交需求结算收束和刷新父级结算摘要。
需求类.cpp:2059-2202：需求方向签名、方向比较、普通候选方法筹办准入。
需求类.cpp:2212-2559：满足、删除、权重阻塞、目标关系、新增原子需求和逻辑组织需求更新指令构造。
需求类.cpp:2560-2702：枚举目标生产者分级和入树合法性校验。
需求类.cpp:2704-2719：按主键解析需求节点。
需求类.cpp:2784-2862：绑定对应任务和生成任务初始化上下文。
需求类.cpp:2863 起：获取需求方向。
```

## 3. 逐函数登记

### OF-118

```text
旧函数编号：OF-118
扫描批次：FS-04 / FS-05 / FS-06
旧项目路径：D:\鱼巢\需求类.cpp:1289
旧函数名：逻辑组织需求类型文本 / 逻辑组织目标特征类型 / OR组结算令牌目标特征类型 / 逻辑组织需求类型_按目标特征类型 / 需求目标语义_按目标特征类型 / 需求目标语义视图 / 需求获取途径语义视图 / 需求_按缺口类型选择获取途径
旧函数签名：需求目标语义、逻辑组织需求和缺口入树准入函数族。
当前调用方：旧需求树生成、任务筹办缺口、方法路径组、因果子链支撑和 OR 组结算令牌。
触发路径：根据目标特征类型或获取途径推导需求语义，判断是否可进入普通候选方法筹办。
输入：目标特征类型语素入口、获取途径枚举、缺口布尔输入。
输出：目标语义视图、获取途径视图、逻辑组织类型、局部 bool 或文本。
读结构：旧语素入口、旧枚举常量、旧目标特征类型缓存。
写结构：部分函数会创建语素入口词；不直接写需求树。
是否写机器事实：语素入口创建是结构写入；语义判定本身不是权威需求事实。
是否依赖日志：否。
是否依赖显示：文本函数只服务显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：逻辑组织需求、OR 令牌、方法路径组和因果子链支撑需要后续需求树语义专项。
删除行为：不得把旧获取途径枚举或文本直接作为新项目需求事实。
新增约束：需求树后续增强必须基于需求目标状态、任务承接关系和方法 / 因果证据，不得绕过领域服务。
目标新结构：需求节点、需求目标状态关系、任务来源需求关系、后续逻辑组织需求角色关系。
目标领域服务入口：需求服务、任务服务、方法服务和因果服务后续只读候选入口。
对应规范 / 详细设计：需求目标状态承载详细设计、任务系统详细设计、方法系统详细设计、需求结算记录详细设计。
入口拒绝条件：目标特征无效、获取途径未确认、缺口输入不足或冲突、逻辑组织目标被当普通方法候选。
验证方式：只读语义推导不新增需求、任务或方法事实。
初步判定：排除 / 后续专项
待用户确认项：逻辑组织需求和获取途径是否进入需求树 / 任务管理 / 方法系统后续增强专项。
扫描证据：`需求类.cpp:1289` 至 `需求类.cpp:1663`。
```

### OF-119

```text
旧函数编号：OF-119
扫描批次：FS-04
旧项目路径：D:\鱼巢\需求类.cpp:1656
旧函数名：读取需求目标特征类型 / 读取需求主体引用 / 读取需求目标宿主引用 / 解析需求主体 / 解析需求目标宿主 / 读取需求当前状态引用 / 读取需求目标状态引用 / 解析需求当前状态 / 解析需求目标状态 / 读取需求场景引用 / 解析需求场景 / 读取需求满足关系 / 读取需求有效截止 / 需求已截止
旧函数签名：需求目标、主体、场景、状态和生命周期只读函数族。
当前调用方：旧任务初始化、需求方向比较、需求结算、方法候选和 SQL 投影。
触发路径：从旧需求主信息读取需求主体、场景、目标存在、当前状态、目标状态、满足关系和截止时间。
输入：需求节点。
输出：引用、解析节点、满足关系、截止时间或局部 bool。
读结构：旧需求主信息中的需求主体、需求场景、被需求存在、当前状态、目标状态、目标特征缓存、满足关系、需求有效截止。
写结构：无。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：需求目标必须可追溯到目标状态；需求主体、目标宿主、场景和满足关系是任务承接前置材料。
删除行为：不得把旧需求主信息字段直接迁移为当前权威字段。
新增约束：目标状态通过需求到状态节点的模板关系承载；需求结算状态另由结算实例状态承载。
目标新结构：需求节点、需求目标状态关系、需求目标材料关系、需求虚拟存在 / 场景关系。
目标领域服务入口：需求服务 `读取需求目标状态`、任务服务 `按需求创建任务`。
对应规范 / 详细设计：需求目标状态承载详细设计、需求结算记录详细设计、任务系统详细设计。
入口拒绝条件：需求无效、目标状态缺失、目标状态多于一个、目标状态不是状态节点、场景或存在无效。
验证方式：有效需求可读唯一目标状态；缺材料返回空，不创建任务或结算事实。
初步判定：拆分
待用户确认项：需求主体、目标宿主和场景关系是否进入需求服务后续读取增强。
扫描证据：`需求类.cpp:1656` 至 `需求类.cpp:1822`。
```

### OF-120

```text
旧函数编号：OF-120
扫描批次：FS-04 / FS-10 / FS-X
旧项目路径：D:\鱼巢\需求类.cpp:1828
旧函数名：同步SQL控制面板需求显示项 / 同步删除SQL控制面板需求显示项 / 重写需求树SQL投影
旧函数签名：需求显示镜像和需求树 SQL 投影函数族。
当前调用方：旧需求更新、任务绑定、删除和控制面板显示。
触发路径：把需求树当前态写入 SQL 控制面板显示表或投影库。
输入：需求节点、需求根、阶段文本、来源原因、字段恢复比对标记。
输出：无或局部 bool。
读结构：旧需求树、旧需求主信息、旧任务 / 状态 / 场景 / 统计字段。
写结构：SQL 控制面板显示镜像、投影库表和视图；不应写内存需求事实。
是否写机器事实：否，按旧文件规则只应是显示镜像。
是否依赖日志：是，写运行日志和错误日志。
是否依赖显示：是。
是否依赖 SQL：是。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：可作为后续显示层只读视图的字段参考。
删除行为：本轮不得接 SQL、控制面板或投影库；SQL 写入失败不得影响需求树事实。
新增约束：显示层只读结构，不写业务事实。
目标新结构：显示层只读视图后续专项，不进入当前机器事实结构。
目标领域服务入口：无，显示专项后续确认。
对应规范 / 详细设计：领域服务授权写入规范、函数迁移前置专项、显示层只读视图后续专项。
入口拒绝条件：当前本轮直接拒绝 SQL / 控制面板接入。
验证方式：只登记排除边界，不运行 SQL，不连接控制面板。
初步判定：排除 / 后续专项
待用户确认项：哪些字段可进入显示层只读视图专项。
扫描证据：`需求类.cpp:1828` 至 `需求类.cpp:1869`、`需求类.cpp:2721` 至 `需求类.cpp:2783`。
```

### OF-121

```text
旧函数编号：OF-121
扫描批次：FS-04 / FS-05
旧项目路径：D:\鱼巢\需求类.cpp:1904
旧函数名：需求阻塞父任务执行 / 父子需求关系角色文本 / 读取父子关系角色视图 / 读取需求安全权重 / 读取需求服务权重 / 读取需求综合权重
旧函数签名：父子需求角色、阻塞父任务和权重读取函数族。
当前调用方：旧任务管理、父需求执行闸门、候选路径和需求结算摘要。
触发路径：根据父子关系、截止状态、阻塞标记和目标语义推导子需求角色与权重。
输入：父需求、子需求、方法路径目标特征类型。
输出：父子关系角色、权重值或文本。
读结构：旧需求树父子指针、旧需求主信息阻塞标记、安全权重、服务权重、目标特征类型。
写结构：无。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：文本函数只服务显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：父子需求关系角色和阻塞父任务执行应进入后续需求树 / 任务管理设计。
删除行为：不得把旧权重字段和父子指针角色直接迁移为第一轮权威任务执行闸门。
新增约束：任务执行权限来自任务授权、任务承接和任务选择方法，需求树角色不能绕过任务服务。
目标新结构：需求父子关系、任务来源需求关系、任务生命周期状态、后续需求角色状态。
目标领域服务入口：需求服务后续父子读取入口、任务服务承接入口。
对应规范 / 详细设计：任务系统详细设计、领域服务授权写入规范、需求结算记录详细设计。
入口拒绝条件：父子关系无效、需求已截止、目标语义未确认、权重被当作结算事实。
验证方式：只读角色不创建或阻断任务；任务服务复核需求目标状态。
初步判定：排除 / 后续专项
待用户确认项：父子需求角色和阻塞父任务执行是否进入需求树增强专项。
扫描证据：`需求类.cpp:1904` 至 `需求类.cpp:1982`。
```

### OF-122

```text
旧函数编号：OF-122
扫描批次：FS-04 / FS-08
旧项目路径：D:\鱼巢\需求类.cpp:1983
旧函数名：提交需求结算收束 / 来源需求有安全方向 / 来源需求有服务方向
旧函数签名：需求结算摘要、当前状态收束和父级结算摘要刷新函数族。
当前调用方：旧任务完成、需求结算后处理、父需求摘要和安全 / 服务方向判断。
触发路径：在需求主信息中回写当前状态、截止时间、累计安全 / 服务结算、最近结算任务和最近结算时间。
输入：需求节点、最近结算任务主键、安全 / 服务结算增量、是否允许收束当前状态、时间戳。
输出：需求结算收束结果、局部 bool。
读结构：旧需求主信息目标状态、当前状态、权重、累计结算摘要、最近任务摘要。
写结构：旧需求主信息当前状态、截止时间、累计结算、最近结算任务、最近结算时间和父级摘要。
是否写机器事实：是，旧项目写主信息摘要和收束状态。
是否依赖日志：否。
是否依赖显示：摘要可服务显示。
是否依赖 SQL：间接触发显示同步时可能关联。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：需求结算必须有目标状态、实际状态、来源任务、动态证据和发生时间戳。
删除行为：不得把旧累计摘要、最近结算任务和最近结算时间作为权威结算事实迁移。
新增约束：权威结算记录以需求结算实例状态和关系承载；摘要只能非权威重建。
目标新结构：需求结算实例状态、需求结算记录关系、结算来源任务关系、结算目标状态关系、结算实际状态关系、动态证据关系。
目标领域服务入口：需求服务 `记录需求结算`、`读取最近结算记录`。
对应规范 / 详细设计：需求结算记录详细设计、非权威缓存与统计详细设计、状态服务详细设计。
入口拒绝条件：无效需求、无来源任务、无目标状态、无实际状态、缺发生时间戳、缺动态证据、结果与比较不一致。
验证方式：结算记录可枚举，最近记录按发生时间戳读取；清空摘要不改变结算事实。
初步判定：拆分
待用户确认项：旧结算摘要字段只进入非权威缓存还是完全删除。
扫描证据：`需求类.cpp:1983` 至 `需求类.cpp:2058`。
```

### OF-123

```text
旧函数编号：OF-123
扫描批次：FS-04 / FS-06 / FS-07
旧项目路径：D:\鱼巢\需求类.cpp:2059
旧函数名：需求目标语义视图(需求) / 生成需求方向签名 / 比较需求方向签名 / 比较需求方向 / 需求方向同向 / 需求方向反向 / 目标特征类型可进入普通候选方法筹办 / 普通候选方法筹办禁止原因 / 需求结构形态文本
旧函数签名：需求方向、方法候选准入和结构形态只读函数族。
当前调用方：旧方法候选、需求去重、方向冲突判断、任务筹办和显示诊断。
触发路径：由二次特征方向材料生成需求方向签名，比较目标特征、主体和方向。
输入：需求节点、二次特征节点、目标特征类型。
输出：需求目标语义视图、方向签名、方向比较结果、局部 bool 或文本。
读结构：旧需求主信息目标特征缓存、旧二次特征主信息、旧需求树子链。
写结构：无。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：文本函数只服务显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：需求方向比较可作为方法候选或需求去重候选材料。
删除行为：不得把方向比较 bool 直接当作方法可执行或需求满足事实。
新增约束：方法候选和条件判定需进入方法系统后续增强，不能由需求类直接裁决。
目标新结构：需求目标状态、二次特征组成关系、方法条件 / 结果角色节点。
目标领域服务入口：需求服务只读候选入口、方法服务后续候选入口、二次特征服务读取入口。
对应规范 / 详细设计：方法系统详细设计、二次特征服务详细设计、需求目标状态承载详细设计。
入口拒绝条件：二次特征无效、目标特征缺失、方向语义未确认、候选命中被事实化。
验证方式：方向比较只输出候选；方法选择仍由任务方法关系和方法服务复核。
初步判定：排除 / 后续专项
待用户确认项：需求方向签名是否进入方法候选排序专项。
扫描证据：`需求类.cpp:2059` 至 `需求类.cpp:2202`。
```

### OF-124

```text
旧函数编号：OF-124
扫描批次：FS-04
旧项目路径：D:\鱼巢\需求类.cpp:2212
旧函数名：构造满足需求指令 / 构造删除需求指令 / 构造需求权重阻塞更新指令 / 构造需求目标关系更新指令 / 构造新增原子需求指令 / 构造逻辑组织需求更新指令 / 构造获取途径逻辑组织需求更新指令 / 构造AND组需求更新指令 / 构造OR组需求更新指令 / 构造方法路径组需求更新指令 / 构造因果子链支撑需求更新指令
旧函数签名：统一需求树更新指令构造函数族。
当前调用方：旧需求树更新入口、任务筹办缺口、逻辑组织需求、结算令牌和删除需求路径。
触发路径：把父需求、目标需求、主体、场景、目标宿主、当前状态、目标状态、权重和阻塞标记打包为更新指令。
输入：父需求、目标需求、主体、场景、目标宿主、当前状态、目标状态、满足关系、权重、阻塞标记、观测时间。
输出：需求树更新指令。
读结构：旧需求节点、旧状态、旧存在、旧场景、旧语素入口。
写结构：只构造指令；不直接改需求树。
是否写机器事实：否；指令被后续提交入口执行后才会写事实。
是否依赖日志：逻辑组织指令构造会写运行日志。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：需求树多步更新应先形成结构化指令，再由需求服务统一提交和入口拒绝。
删除行为：不得迁移旧指针字段、主键字段和旧主信息字段名作为当前机器事实。
新增约束：新项目如需需求更新指令，必须用稳定句柄和关系，不用裸指针或 SQL 镜像。
目标新结构：需求节点、需求目标状态关系、需求结算记录关系、任务来源需求关系。
目标领域服务入口：需求服务后续统一更新入口。
对应规范 / 详细设计：错误结果与入口拒绝规范、需求目标状态承载详细设计、需求结算记录详细设计。
入口拒绝条件：父需求无效、目标状态无效、当前状态无效、满足关系未确认、权重摘要被当事实。
验证方式：构造指令不写结构；提交入口拒绝后节点 / 关系 / 状态数量不变。
初步判定：排除 / 后续专项
待用户确认项：是否需要新项目需求服务统一更新指令。
扫描证据：`需求类.cpp:2212` 至 `需求类.cpp:2559`。
```

### OF-125

```text
旧函数编号：OF-125
扫描批次：FS-04 / FS-06
旧项目路径：D:\鱼巢\需求类.cpp:2560
旧函数名：枚举目标生产者分级文本 / 枚举目标校验结论文本 / 校验枚举目标合法性 / 校验更新指令枚举目标
旧函数签名：枚举目标生产者、目标状态值域和比较符合法性校验函数族。
当前调用方：旧需求入树校验、方法生产者缺口和枚举目标治理。
触发路径：根据目标特征类型、当前状态、目标状态和满足关系判断枚举目标是否合法。
输入：目标特征类型、当前状态、目标状态、满足关系、需求树更新指令。
输出：枚举目标校验结果或文本。
读结构：旧抽象特征树、旧状态主信息、旧特征值域、旧生产者分级表。
写结构：无；失败时写运行错误日志。
是否写机器事实：否。
是否依赖日志：是，拒绝原因写运行错误日志。
是否依赖显示：文本函数只服务显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：枚举目标需要校验比较关系、目标值域和生产者能力。
删除行为：不得把生产者分级文本或局部 bool 直接迁移为方法可用事实。
新增约束：目标比较规则和生产者能力必须进入需求结算规则或方法系统专项。
目标新结构：目标状态、实际状态、方法动作入口、条件判定索引后续占位。
目标领域服务入口：需求服务后续结算规则、方法服务后续方法候选入口。
对应规范 / 详细设计：需求结算记录详细设计、方法系统详细设计、动作入口详细设计。
入口拒绝条件：缺目标特征或目标状态、比较符非法、目标值不属于值域、生产者未登记或待补齐。
验证方式：校验失败只返回拒绝原因，不写需求树。
初步判定：排除 / 后续专项
待用户确认项：枚举目标生产者分级是否进入方法系统后续增强专项。
扫描证据：`需求类.cpp:2560` 至 `需求类.cpp:2702`。
```

### OF-126

```text
旧函数编号：OF-126
扫描批次：FS-04
旧项目路径：D:\鱼巢\需求类.cpp:2704
旧函数名：按主键解析需求节点
旧函数签名：需求类::节点类* 按主键解析需求节点(节点类* 需求根节点, const std::string& 主键)
当前调用方：旧需求树更新指令、SQL 投影恢复、任务绑定和诊断。
触发路径：在需求树根下按主键定位需求节点。
输入：需求根节点、主键。
输出：需求节点或空。
读结构：旧需求树、旧主键。
写结构：无。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：使用需求树全局互斥保护读取。
是否依赖外设 / D455 / 体素：否。
保留行为：按稳定身份定位需求节点，并复核树根有效。
删除行为：不得迁移旧独立树根指针和裸节点指针作为长期身份。
新增约束：新项目需求身份使用节点句柄，需求读取必须复核节点类型和生命周期。
目标新结构：节点类型::需求、节点句柄、索引主键。
目标领域服务入口：需求服务 `创建需求`、后续 `需求是否有效` / 需求只读入口。
对应规范 / 详细设计：需求目标状态承载详细设计、节点仓库详细设计、索引仓库详细设计。
入口拒绝条件：需求根无效、主键为空、节点类型不是需求、已删除需求。
验证方式：主键命中后复核需求节点；错误输入返回空且结构不变。
初步判定：改写为领域服务入口
待用户确认项：是否补需求服务按主键读取入口。
扫描证据：`需求类.cpp:2704` 至 `需求类.cpp:2719`。
```

### OF-127

```text
旧函数编号：OF-127
扫描批次：FS-04 / FS-05
旧项目路径：D:\鱼巢\需求类.cpp:2784
旧函数名：绑定对应任务 / 生成任务初始化上下文 / 获取需求方向
旧函数签名：需求到任务绑定、任务初始化上下文和需求方向读取函数族。
当前调用方：旧任务创建、任务筹办、需求方向比较和方法候选。
触发路径：把任务引用写入旧需求主信息，对任务列表去重，并从需求状态材料生成任务初始化上下文。
输入：需求节点、任务节点。
输出：局部 bool、任务初始化上下文、二次特征节点。
读结构：旧需求主信息、旧任务节点、旧状态、旧特征、旧二次特征。
写结构：旧需求主信息对应任务和任务列表；同步 SQL 显示镜像。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：绑定后同步 SQL 显示镜像。
是否依赖 SQL：是，间接同步控制面板显示。
是否依赖线程：使用需求树全局互斥。
是否依赖外设 / D455 / 体素：否。
保留行为：需求承接任务需要稳定来源需求、目标状态、场景、虚拟存在和生命周期状态。
删除行为：不得迁移旧需求主信息对应任务字段和任务列表字段为权威关系。
新增约束：任务承接由任务服务写需求拥有任务关系、任务来源需求关系、任务目标状态关系等结构。
目标新结构：需求拥有任务关系、任务来源需求关系、任务目标状态关系、任务虚拟存在关系、任务场景关系。
目标领域服务入口：任务服务 `按需求创建任务`、需求服务只读目标状态入口。
对应规范 / 详细设计：任务系统详细设计、需求目标状态承载详细设计、领域服务授权写入规范。
入口拒绝条件：需求无效、任务无效、需求无目标状态、场景无效、发生时间戳缺失、SQL 显示同步失败被当业务失败。
验证方式：任务节点可回读来源需求、目标状态、虚拟存在和生命周期状态；SQL 不参与事实裁决。
初步判定：改写为领域服务入口
待用户确认项：旧任务列表历史是否进入任务管理后续增强专项或显示只读视图。
扫描证据：`需求类.cpp:2784` 至 `需求类.cpp:2863`。
```

## 4. 本轮初步分流

```text
可保留方向：
- 需求目标必须能回查目标状态。
- 需求承接任务必须形成稳定任务关系。
- 需求结算必须以目标状态、实际状态、来源任务、动态证据和发生时间戳形成权威记录。
- 需求方向、逻辑组织和枚举目标校验可作为后续候选和规则专项线索。

必须删除或改写：
- 旧需求主信息中的目标状态、当前状态、对应任务、任务列表、权重、截止、结算摘要和最近结算字段直接迁移。
- 旧 SQL 控制面板和投影库作为需求事实来源。
- 旧 bool 方向比较、目标语义文本和获取途径枚举直接裁决方法可执行或需求满足。

后续专项：
- 需求树 / 任务管理 / 方法系统后续增强专项：父子需求、逻辑组织需求、获取途径、方法候选和任务绑定历史。
- 需求结算记录专项：权威结算记录、最近记录和动态证据。
- 非权威缓存与统计专项：累计摘要、最近结算任务和最近结算时间。
- 显示层只读视图专项：SQL 投影和需求树显示字段。
```

## 5. 禁止宣称

```text
不得声明旧需求类或旧需求树已迁移。
不得声明旧需求主信息字段可直接迁移。
不得声明需求树、任务管理、方法候选、需求结算或 SQL 显示系统完成。
不得把旧结算摘要、最近结算任务、最近结算时间、SQL 投影或文本语义当作权威事实。
不得接控制面板 / SQL / D455 / 体素 / 外设。
```
