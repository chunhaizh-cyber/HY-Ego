# START-TIME-S1 控制台启动步骤用时显示计划生成 Codex 断点清单

日期：2026-07-10

状态：流程图 / 详细设计 / 计划已生成 / #170 用户明确插队执行 / 未改 C++ / 未构建

## 1. 当前事实

```text
入口已有 <chrono>，但没有启动步骤耗时输出。
启动步骤和模式分支集中在 海中鱼巣/入口.cpp。
控制面板 运行() 阻塞到窗口关闭，不能在入口外层把其返回耗时称为启动耗时。
```

## 2. 计划依据

```text
流程图/20260710_控制台启动步骤用时显示流程图_v0.1.md
流程图/20260710_控制台启动步骤用时显示流程图_v0.1.html
规范/详细设计/控制台启动步骤用时显示详细设计.md
计划/已完成计划/20260710_START-TIME-S1_控制台启动步骤用时显示代码实施切片_v0.1.md
```

三者范围一致，只允许修改 `海中鱼巣/入口.cpp` 的人读计时观察，不改变机器结构或返回裁决。

## 3. 路由

```text
设计计划队列：DQ-61
可执行队列：#170
优先级：用户本轮明确实施请求，先于仍待执行的 #168 和依赖 #168 的 #169 完成本独立切片
完成后：恢复 #168、#169 的既有依赖顺序
```

## 4. 验证边界

计划必须通过 Debug x64 Rebuild、自检模式、数据库自检模式、计时行解析、`git diff --check` 和规范治理检查。计划生成本身不证明 C++ 已修改或启动计时已经实现。
