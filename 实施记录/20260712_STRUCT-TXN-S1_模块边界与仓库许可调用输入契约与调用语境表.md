# STRUCT-TXN-S1 模块边界与仓库许可调用输入契约与调用语境表

| 入口 | 调用方 | 输入字段 | 输入来源 | 上游是否保证有效 | 允许逻辑内返回 | 追根因触发条件 | 结构变化 | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 仓库旧公开入口 | 既有服务 / 自检 | 既有业务参数、默认空接线 | 正式或测试材料 | 沿用既有合同 | 沿用既有拒绝 | 既有写入后异常 | 按入口 | 全量回归 |
| 已接域共享入口 | #217 隔离自检及后继服务 | 域、纪元、共享状态、四函数表 | 协调器工厂 | 是 | 半接线构造拒绝 | 完整接线却无法取得共享许可 | 普通读 / 新增 / 关系索引变化 | A01-A14 |
| 身份删除 / 事务提交 | 节点、主信息及后继事务包 | 完整接线、独占许可 | 协调器工厂 / 上层许可 | 是 | 跨域、过期、共享令牌写前拒绝 | 活动独占许可验证失败 | 身份删除或事务包变化 | 删除与并发端点压力 |
| 令牌感知入口 | 同一同步调用链上层 | 域、运行期纪元、许可序号、许可类型 | 活动移动许可读取 | 是 | 外部伪造 / 过期 / 跨域写前拒绝 | 正式活动许可导出令牌验证失败 | 写前无变化 | 伪造 / 过期 / 跨域 / 跨线程样本 |
| 关系 / 索引到节点 | 仓库内部 | 同一共享令牌、节点句柄 | 已接域公开入口 | 是 | 节点无效按原入口语义 | 许可期端点读回不一致 | 不同时持两个仓库锁 | 交错压力与静态锁序 |
| 同线程无令牌重入 | 仓库包装误调用 | 当前线程已持同状态许可 | 实现调用链 | 上游应避免 | 不允许作为普通业务返回 | 触发诊断并返回无效许可 | 无 | 重入负例 |
| 许可移动 / 释放 | 同步调用栈 | 移动许可、原线程释放 | 数据桥 RAII | 是 | 外部空许可无动作 | 跨线程释放、重复释放、活动表不一致 | 无业务结构 | 移动与析构负例 |
| 结构事务自检 | 最终回归 500 | 无参隔离夹具 | 自检模块内部 | 是 | 登记冲突退回设计 | A 项缺失 / 重复 | 仅测试结构 | 完整自检与 20 轮 |

接线中的 `std::shared_ptr<void>` 只作运行期状态寿命桥，不持久化、不作为机器身份；调用期许可身份由域、运行期纪元、许可序号、许可类型、活动表和线程归属共同验证。
