# 方法候选召回与选择流程图生成 Codex 断点清单

日期：2026-07-08

## 1. 输入入口

```text
用户指令：生成下一份流程图。
当前窗口：设计确认窗口。
承接顺序：第 9 项“方法结构流程”之后，进入第 10 项“方法候选召回与选择流程”。
```

## 2. 核心处理

```text
1. 回读流程图技能、AGENTS.md、计划索引、项目规则总纲、规则迁移清单。
2. 回读应用逻辑流程图迁移顺序信息数据，确认第 10 项为“方法候选召回与选择流程”。
3. 回读 FS-06 方法系统入口只读扫描记录、方法候选召回与条件结果对拆分设计专项、方法系统详细设计、任务系统详细设计和当前 `需求服务.h` / `任务服务.h` / `方法服务.h`。
4. 确认当前已落代码入口为：
   - 需求服务::方法候选准入材料是否可读
   - 任务服务::读取任务承接材料
   - 任务服务::读取筹办回执请求材料
   - 任务服务::选择任务方法
   - 任务服务::读取任务选择方法
   - 任务服务::读取执行桥请求材料
   - 方法服务::方法首是否有效
   - 方法服务::读取方法条件节点集合
   - 方法服务::读取方法结果节点集合
   - 方法服务::读取方法动作入口
   - 方法服务::按稳定动作键读取方法动作入口
5. 生成方法候选召回与选择代码逻辑流程图 MD / HTML。
```

## 3. 结构承载

```text
流程图承载：
- 流程图/20260708_方法候选召回与选择代码逻辑流程图_v0.1.md
- 流程图/20260708_方法候选召回与选择代码逻辑流程图_v0.1.html

实施记录承载：
- 实施记录/20260708_方法候选召回与选择流程图生成_Codex断点清单.md
```

## 4. 输出变化

```text
新增第 10 项“方法候选召回与选择”代码逻辑流程图。
图中区分：
- 当前已落代码：需求候选准入材料、任务选择上下文、方法结构读取、稳定动作键动作入口读取、任务选择方法关系。
- 当前写入边界：只有 `任务服务::选择任务方法` 可写当前任务到方法的选择关系。
- 当前拒绝边界：需求承接材料不可读、任务承接壳不完整、方法首无效、动作入口归属不成立、候选规则未确认、多候选无授权策略、重复任务方法关系时拒绝。
- 当前差距：结果能力索引、条件判定算法、条件结果对归并、候选排序、自动选择策略、方法学习、来源任务和前后方法关系均未完成。
- 排除项：旧方法查找函数体、旧结果能力裸字段、可查找 bool、成熟度 I64、SQL、控制面板、D455、体素和外设。
```

## 5. 调用路径

```text
用户要求生成下一份流程图
-> 流程图迁移顺序信息数据第 10 项
-> FS-06 只读扫描 / FS-06 后续拆分设计专项 / 方法系统详细设计 / 任务系统详细设计 / 当前服务入口复核
-> 方法候选召回与选择代码逻辑流程图
```

## 6. 验证方式

```text
本切片为流程图文档治理切片。
计划验证：
- 确认第 10 项 Markdown 和 HTML 两份流程图文件存在。
- 确认 Markdown 存在 mermaid fenced block。
- 确认 HTML 包含 Mermaid CDN import。
- 确认 MD / HTML Mermaid 图文本一致。
- 运行 git diff --check。
- 运行 tools/check_specs.py。

实际结果：
- 第 10 项 Markdown、HTML 和本断点清单文件均存在。
- Markdown 存在 mermaid fenced block。
- HTML 包含 Mermaid CDN import。
- MD / HTML Mermaid 图文本一致。
- `git diff --check -- 流程图/20260708_方法候选召回与选择代码逻辑流程图_v0.1.md 流程图/20260708_方法候选召回与选择代码逻辑流程图_v0.1.html 实施记录/20260708_方法候选召回与选择流程图生成_Codex断点清单.md` 通过。
- `python tools/check_specs.py` 输出：海中鱼巣规范治理检查: 未发现问题。

未执行：
- 未修改 C++。
- 未运行 msbuild。
- 未运行默认入口。
- 未生成详细设计。
- 未生成施工计划。
- 未登记可执行队列。
- 未将第 9 项流程图自动登记为已确认。
```

## 7. 禁止宣称

```text
不得宣称方法候选召回已实现。
不得宣称候选排序、自动选择、条件结果对归并、方法学习或完整方法查找系统已完成。
不得宣称结果能力索引、来源任务、父方法、前置 / 后续方法关系已完整落地。
不得宣称任务选择方法关系证明方法满足需求目标。
不得宣称旧方法主信息字段、旧方法函数体、旧动作句柄、SQL、控制面板、D455、体素或外设已迁移。
不得宣称本流程图提供代码实施许可。
```
