# FS-01 旧世界树核心边界只读扫描 Codex 断点清单

日期：2026-07-06
状态：已记录断点 / 文档切片 / 未改代码 / 未迁移旧函数

## 1. 本切片输入

```text
用户要求继续执行无需确认的步骤。
计划索引 HR-001-FM-SCAN 要求继续逐函数只读扫描和旧函数说明登记。
逐函数只读扫描清单允许 FS-01 继续登记旧核心结构函数。
```

## 2. 已完成动作

```text
1. 只读扫描 `D:\鱼巢\世界树类.h` 和 `D:\鱼巢\世界树类.cpp`。
2. 新增 `实施记录/20260706_FS01_旧世界树核心边界只读扫描记录.md`。
3. 登记 OF-019 到 OF-031。
4. 将 SQL 投影、控制面板同步、体素和外设相关内容登记为 FS-X 或后续专项排除边界。
5. 同步逐函数只读扫描清单、计划索引和项目记忆。
```

## 3. 本切片结论

```text
旧 `世界树类` 是聚合门面和服务定位器，不适合整体迁移。
世界种子、存在、场景、特征写入、关系重挂和主键解析必须拆到当前项目已确认的领域服务或仓库入口。
SQL / 控制面板只进入后续显示层只读视图或事件日志持久化恢复专项，本轮不得接入。
体素、D455 和外设只登记为排除边界，本轮不得接入。
```

## 4. 下一步

```text
继续 FS-01。
重点补旧核心结构中的主信息字段访问辅助函数和基础数据类型中句柄 / 值容器边界。
下一旧函数编号从 OF-032 开始。
FS-01 明确关闭前，不进入 FS-02。
登记完成后仍需提交用户确认迁移判定。
```

## 5. 禁止宣称

```text
不得声明 FS-01 已完整扫描。
不得声明旧函数已扫描完成。
不得声明旧函数已迁移。
不得复制旧函数体。
不得新增旧主信息字段。
不得声明旧世界树已迁移。
不得声明旧项目能力迁移完成。
不得接控制面板 / SQL / D455 / 体素 / 外设。
```

## 6. 验证记录

```text
本切片只做文档和只读事实记录验证。
未改代码。
未运行构建。
未运行当前项目入口。
未运行旧项目程序。
```
