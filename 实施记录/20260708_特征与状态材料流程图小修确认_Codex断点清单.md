# 特征与状态材料流程图小修确认 Codex 断点清单

日期：2026-07-08

## 1. 输入入口

```text
用户确认：《20260708_特征与状态材料代码逻辑流程图_v0.1.md》小修后确认。
确认范围：仅限第 4 项“特征与状态材料流程”的代码逻辑流程图材料。
确认用途：可作为后续“特征与状态材料详细设计”或“特征值系统第二轮确认”的输入。
禁止扩大：不构成代码实施许可，不生成详细设计，不生成施工计划，不登记可执行队列。
```

## 2. 核心处理

```text
1. 将流程图依据拆为“已确认 / 已验证依据”和“后续候选依据”。
2. 将 Vec 原始值容器、非权威缓存与内容哈希、值域稳态与候选比较三份第二轮设计标记为：已生成详细设计 / 待确认 / 未改代码。
3. 将“目标状态关系”收束为当前代码承载口径：需求节点通过 `关系类型::模板` 指向唯一目标状态，读取时复核目标节点为状态且唯一。
4. 收紧失败收口：只声明不返回有效句柄、半结构不得作为有效事实；不声明失败后节点 / 主信息 / 关系物理数量完全不变。
5. 将最小读回验证标注为后续详细设计 / 施工计划门禁要求，不声明所有当前写入口已经内置统一读回。
6. 补充验证状态边界：本流程图不产生新的构建、运行或依赖扫描验证。
7. 补充第一轮 `读取特征语义类型` 弱口径：当前等同于 `读取特征身份` 复核 / 命名材料入口，不代表完整特征语义类型系统。
```

## 3. 结构承载

```text
流程图承载：
- 流程图/20260708_特征与状态材料代码逻辑流程图_v0.1.md
- 流程图/20260708_特征与状态材料代码逻辑流程图_v0.1.html

项目记忆承载：
- 项目记忆/当前状态.md
- 项目记忆/决策记录.md
- 项目记忆/待确认问题.md
- 计划/计划索引.md

实施记录承载：
- 实施记录/20260708_特征与状态材料流程图小修确认_Codex断点清单.md
```

## 4. 输出变化

```text
新增决策记录 JY-087。
新增待确认问题记录 QR-102，并标记为已小修后确认。
计划索引新增“特征与状态材料流程图候选”状态行。
当前状态新增“特征与状态材料代码逻辑流程图”状态行。
流程图 MD / HTML 同步修订 Mermaid 中的模板关系、唯一目标状态读取复核、最小读回验证门禁和失败收口口径。
```

## 5. 调用路径

```text
用户确认意见
-> AGENTS.md / 计划索引 / 项目规则总纲 / 规则迁移清单
-> 流程图第 4 项材料小修
-> 项目记忆状态与决策同步
-> 本断点清单记录
```

## 6. 验证方式

```text
本切片为文档治理切片。
计划验证：
- 确认 Markdown 和 HTML 两份流程图文件存在。
- 确认 Markdown 存在 mermaid fenced block。
- 确认 HTML 包含 Mermaid CDN import。
- 确认 MD / HTML Mermaid 图中的目标状态承载和失败收口文本一致。
- 确认最小读回验证在流程图中是门禁要求，不被记录为当前函数内部统一实现。
- 运行 git diff --check。
- 运行 tools/check_specs.py。

实际结果：
- Markdown 和 HTML 两份流程图文件存在。
- Markdown 存在 mermaid fenced block。
- HTML 包含 Mermaid CDN import。
- MD / HTML Mermaid 图文本一致。
- `git diff --check -- 流程图/20260708_特征与状态材料代码逻辑流程图_v0.1.md 流程图/20260708_特征与状态材料代码逻辑流程图_v0.1.html 实施记录/20260708_特征与状态材料流程图小修确认_Codex断点清单.md` 通过；仅出现工作区 LF 将被 Git 触碰时替换为 CRLF 的提示。
- `python tools/check_specs.py` 输出：海中鱼巣规范治理检查: 未发现问题。

未执行：
- 未修改 C++。
- 未运行 msbuild。
- 未运行默认入口。
- 未运行依赖扫描。
```

## 7. 禁止宣称

```text
不得宣称特征与状态材料详细设计已生成。
不得宣称特征值系统第二轮设计已确认。
不得宣称 Vec 原始值容器、内容哈希、值域稳态或序列化恢复已实现。
不得宣称失败后节点 / 主信息 / 关系物理数量完全不变，除非后续另有事务回滚或补偿验证。
不得宣称所有当前写入口已经内置统一最小读回验证。
不得宣称完整特征语义类型系统已实现。
不得宣称旧函数已迁移。
不得宣称本切片提供代码实施许可。
```
