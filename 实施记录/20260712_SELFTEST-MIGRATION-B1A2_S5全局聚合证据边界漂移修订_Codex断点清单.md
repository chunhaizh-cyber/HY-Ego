# SELFTEST-MIGRATION-B1A2 S5 全局聚合证据边界漂移修订 Codex 断点清单

日期：2026-07-12
治理编号：JY-266
队列：#245 / DQ-137

## 1. 输入入口

```text
HEAD / main / origin：0cbce53。
执行窗口第二次 #245 草案：S4、S3B 和 S5 三种所有权均已编译到位；仅剩 S5 机械迁移边界错误，草案已完整回退且 worktree / index 干净。
入口当前 S5 逻辑正文 1828-2277、S5 输出 14071-14096、相邻全局输出 14099-14102、程序总汇总 14258-14353。
```

## 2. 当前事实

1. S5 A01-A12 和 `自我治理第五切片多轮接线与停止收口通过` 只读取 S5 隔离夹具内部结果，不读取四个全局聚合布尔值。
2. `MATRIX基础信息后续入口通过` 定义于约 8822 行；`服务操作函数矩阵第一批通过` 定义于约 8833 行；`最小闭环通过` 定义于约 7917 行；`独立验收项通过` 定义于约 12886 行。
3. 四项只因输出顺序紧邻 S5 A12 而被机械迁移误收；它们没有 `SELF-GOV-S5-*` 编号。
4. 最终 `程序通过` 分别合取 S5 总结果、最小闭环、服务矩阵和独立验收，证明四项与 S5 是并列证据。
5. 概念命名 A11 只消费 S5 自身总结果，不消费四项全局证据。

## 3. 裁决

```text
不在 S5 内部重建四项。
不把四项作为 S5 值式输入。
不为四项新增阶段或回调。
S5 迁移边界固定为：场景正文 1828-2277 + 输出 SELF-GOV-S5 总项与 A01-A12（14071-14096）。
14099-14102 的四项输出和各自定义、最终程序汇总继续留在原所有者；不属于 #245 迁移正文。
```

## 4. 输出变化

三件套新增“逻辑编号边界优先于文本相邻边界”规则。S5 模块继续只接收 `方法登记根稳定键` 与 `自我根需求参数`，只输出 12 项报告和 S5 总结果。#245 再次等待执行前逐字段及逐编号复核；#243 / #217 继续门控。

## 5. 验证方式

```text
静态确认 S5 模块中不存在四个全局布尔标识符。
静态确认入口仍保留四项定义、输出和最终程序汇总引用。
确认 S5 A01-A12、S5 总结果、概念命名 A11 和最终程序汇总语义不变。
流程图纠偏严格检查、规范严格检查、Mermaid 同图和 git diff --check。
```

本批只做文档治理，不修改 C++、工程或测试，不运行构建和程序。
