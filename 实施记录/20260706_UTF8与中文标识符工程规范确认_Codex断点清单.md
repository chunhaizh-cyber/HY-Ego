# 20260706 UTF-8 与中文标识符工程规范确认 Codex 断点清单

## 1. 输入入口

用户确认 `规范/UTF8与中文标识符工程规范.md` 可以确认。

建议吸收的非阻塞增强：

```text
类名 / 结构名 / 枚举名 / 命名空间名 / 文件名优先中文。
不得依赖 Windows 本地代码页。
控制台乱码不作为机器逻辑失败或成功证据。
明确 UTF-8 BOM 和工程 XML 口径。
```

## 2. 本切片范围

| 文件 | 处理 |
| --- | --- |
| `规范/UTF8与中文标识符工程规范.md` | 状态改为已确认规范；补中文命名范围、UTF-8 文件口径、系统代码页边界、控制台乱码边界、BOM 和工程 XML 口径、完成声明边界。 |
| `计划/20260705_海中鱼巣全量重构总计划_v0.1.md` | 将 UTF-8 与中文标识符工程规范状态从已建立 / 待确认推进为已确认 / 未改代码。 |
| `计划/计划索引.md` | 将 UTF-8 与中文标识符工程规范加入已确认文档清单，并从设计确认路线待确认项中移除。 |
| `项目记忆/*` | 同步当前状态、决策、待确认问题、任务队列、验收记录和实施记录。 |

## 3. 验证

```text
rg -n "/utf-8|AdditionalOptions|CharacterSet" 海中鱼巣.vcxproj
rg 检查待确认状态残留
git diff --check
git diff --cached --check
```

当前 `海中鱼巣.vcxproj` 的 Debug / Release、Win32 / x64 四个配置均检索到 `/utf-8`。

本切片未改代码，未运行构建和默认入口。

## 4. 禁止扩大声明

```text
不得声明所有历史文件已逐个编码审计完成。
不得声明所有终端环境中文显示都已验证。
不得声明 CI 已验证 UTF-8。
不得声明所有后续代码命名已自动符合规范。
不得声明代码已修改或重新构建。
```
