# 代码文件模块治理与后续计划安全修订 Codex 断点清单

日期：2026-07-11

治理编号：JY-253

状态：文档治理已完成 / #211、#213、#229-#230 三件套已修订 / #236-#239 已登记 / 未改 C++ / 未构建 / 未运行程序

## 1. 输入入口

```text
用户明确裁决：现有目录结构保持；数据结构文件保持 .h/.cpp；其它后续新代码文件采用模块；文件名采用“功能.分类”。
用户提交的四项治理审查：#213 事务前置、控制面板状态、迁移台账、#229 拆分与入口自检迁出。
正式 HEAD 7733d08。
#211 执行窗口未提交草案及只读验证报告。
当前计划、流程图、详细设计、计划索引和项目记忆。
```

## 2. 当前事实

```text
HEAD 的入口.cpp 已超过 1 MB 和 1.5 万行；#211 未提交草案继续增加三百余行 A01-A12 正文。
线程目录已有真实 C++ 模块；领域初始化仍有 ClInclude + include 的历史 .ixx。
当前普通需求承接读取不要求服务根父子或目标概念关系，#213 分步写在发布前可能被其它公开读取看到。
#217 已明确直接仓库和其它服务可见分步写，并计划提供统一共享 / 独占许可。
只读控制面板第一版、SQL 审计投影和六类真实树已经实现验证，但完整操作、数据库恢复和旧能力迁移未完成。
INFO-MIG-S0 只有来源处理矩阵，缺少统一逐能力迁移 / 新建区分字段。
原 #229 同时覆盖任务迁移、屏障、上下文、四初始化、七线程、SQL、控制面板和自检，范围过宽。
```

## 3. 裁决

### 3.1 代码文件

```text
建立 CODE-FILE-01 正式规范和配套流程图 / 详细设计。
现有目录和既有文件原地保留，不批量改名。
新数据结构使用 功能.分类.h/.cpp；其它新代码使用真实 功能.分类.ixx。
真模块必须同时满足 ClCompile、export module 和 import。
生产 / 自检分离，入口只作参数、模式、最小装配、顶层调用和退出码。
```

### 3.2 #211

```text
生产桥：线程/上行桥.概念命名治理.ixx。
自检：线程/自检.概念命名治理.ixx。
旧 概念命名治理上行桥.ixx 不进入工程或提交。
入口只组装最小上下文、调用一次和汇总；A01-A12、并发和逐项输出移入自检模块。
#211 完成治理编号预留 JY-254。
```

### 3.3 #213

```text
直接前置增加 #217。
创建前重读、分步写、服务根发布和失败撤销全程持同域独占许可。
内部仓库 / 服务调用显式复用同域令牌，不递归取得共享许可。
A09 强化为全部参与域公开读取只见完整前态或完整后态。
逆序撤销只处理独占许可内对外不可见候选，不冒充并发隔离或事务回滚。
生产 / 自检文件分别改为 路由.概念命名治理、 自检.概念命名需求 真模块。
```

### 3.4 控制面板

统一三分口径：

```text
已完成：独立重建的只读控制面板第一版和 SQL Server 结构审计投影。
未完成：完整业务操作控制面板、数据库恢复、旧控制面板 / 旧数据库能力迁移。
迁移归属：独立重建结果不计旧能力迁移完成度。
```

### 3.5 统一能力台账

建立 `实施记录/20260711_旧能力迁移与新内核建设逐能力台账.md`，固定来源性质、旧证据、新结构 / 写入方、计划 / 提交、验收和是否计入旧迁移完成度。已按 MIG-00 至 MIG-20 和 NEW-01 至 NEW-05 建立第一版。

### 3.6 RECOVERY-S3 拆分

为保证屏障先于正式发布路径，采用：

```text
#229 RECOVERY-S3A：任务恢复中断迁移、跨段屏障、不可伪造发布许可。
#237 RECOVERY-S3B：运行期上下文、空宿主首次发布、同身份租约。
#238 RECOVERY-S3C：显式启动分支、线程、SQL 审计、只读控制面板后启。
#239 RECOVERY-S3D：恢复领域独立自检真模块、入口最小调用、端到端验收。
#230 RECOVERY-S4：直接前置改为 #239。
```

原 #229 文件被四份范围完整计划取代，不保留宽计划并行执行。

## 4. 输出变化

```text
新增 CODE-FILE-01 规范、流程图 MD+HTML、详细设计。
新增 #236 ENTRY-MOD-S0 入口 / 自检 / 活动计划冲突只读扫描计划。
新增逐能力统一台账。
修订 #211 与概念命名流程图 / 详细设计。
修订 #213 直接前置、事务隔离、A09 和模块承载。
拆分 #229，新增 #237-#239，修订 #230 前置。
统一控制面板项目口径。
更新 AGENTS、000、001、计划 / 执行 / 理论 / 流程图技能和治理记录。
```

## 5. 队列

```text
#211 仍为当前执行项，JY-254 完成。
#236 / DQ-128：#211 后、#217 前执行纯只读盘点。
#213：等待 #212 和 #217。
#229 / DQ-121：等待 #228、#225。
#237 / DQ-129：等待 #229。
#238 / DQ-130：等待 #237。
#239 / DQ-131：等待 #238、#236。
#230 / DQ-122：等待 #239、#220、#225。
```

## 6. 并行工作树保护

本轮明确排除执行窗口的六份 #211 未提交草案：

```text
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
海中鱼巣/入口.cpp
海中鱼巣/线程/自我治理消息协议.ixx
海中鱼巣/线程/概念命名治理上行桥.ixx
规范/详细设计/自我治理循环详细设计.md
```

这些文件由执行窗口在 JY-253 正式提交后按修订计划适配，本轮不得暂存或提交。

## 7. 验证

```text
Markdown / HTML Mermaid 图体一致。
#211、#213、#229/#237/#238/#239/#230 与详细设计依赖和范围一致。
新模块文件名均符合“功能.分类”。
计划索引、任务队列、当前状态、决策、实施和待确认记录一致。
git diff --check 通过。
tools/check_specs.py 通过。
```

本轮为文档治理，不运行 C++ 构建或程序。

## 8. 禁止扩大声明

本轮不证明 #211、#213、#229-#239 已实现，不证明入口已整体拆分，不证明统一自检运行器已实现，不证明完整控制面板、数据库恢复、旧能力迁移或 RECOVERY 链完成。
