# 执行队列滚动循环规则修订 Codex 断点清单

日期：2026-07-07
状态：已完成 / 文档治理 / 未改 C++ / 未迁移旧函数

## 1. 输入入口

用户指出当前“执行队列消化后再生成后续计划”的整批等待方式无法形成循环，要求先按新口径修改规则。

## 2. 本次规则口径

```text
任一可执行队列项完成并形成正式产物后，设计确认窗口即可基于该项产物生成下一步计划。
单项后续计划不得等待整批可执行队列清空。
跨项综合计划必须列明依赖项，并等待这些依赖项全部完成或真实阻塞。
未完成项的预期结果不得作为计划依据。
涉及 C++、旧能力迁移、字段落地、函数迁移或机器结构改变时，仍必须另建实施切片并确认允许文件、禁止文件和验证方式。
```

## 3. 修改文件

```text
AGENTS.md
计划/20260707_执行队列消化后续计划生成门禁_v0.1.md
计划/计划索引.md
项目记忆/Codex任务队列.md
项目记忆/待确认问题.md
项目记忆/当前状态.md
项目记忆/决策记录.md
项目记忆/实施记录.md
```

## 4. 未修改范围

```text
未修改 C++。
未迁移旧函数体。
未新增旧主信息字段。
未生成下一批具体计划。
未接控制面板、数据库、SQL、ADO、D455、体素或外设。
```

## 5. 验证方式

```powershell
git diff --check
if (Test-Path .\tools\check_specs.py) { python .\tools\check_specs.py }
```

## 6. 完成声明边界

本次只能声明执行队列后续计划生成规则已修订为滚动循环，不能声明任何队列项已因此完成，也不能声明下一批代码实施切片已确认。
