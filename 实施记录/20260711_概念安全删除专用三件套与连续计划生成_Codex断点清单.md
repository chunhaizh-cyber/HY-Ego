# 概念安全删除专用三件套与连续计划生成 Codex 断点清单

日期：2026-07-11

治理编号：JY-235

基准提交：`6f8511f`

状态：文档与计划已生成 / #216-#220 已登记 / 未改 C++ / 未构建 / 未运行程序

## 1. 输入入口

```text
用户要求：基于 #198 正式 S0 生成安全删除专用三件套与前置计划链。
正式事实：实施记录/20260711_CONCEPT-S8-S0_安全物理删除当前代码事实复核_Codex断点清单.md
当前代码：节点、主信息、关系、索引、写入事务、概念图服务、固定点算法和四类原子服务。
执行队列：#199 当前执行；#200-#215 已登记；本批从 #216 续接。
```

## 2. 当前事实断点

```text
可复用：四根、退役生命周期、当前支持、节点相关有效关系、活动版本 / 快照、受影响分量和纯值固定点。
缺失：删除专用完整输入、端点冻结、跨仓库写集、统一读取隔离、反向主键、所有者清理、原子提交和后验审计。
既有风险：关系创建 / 重挂和索引绑定与节点删除存在跨仓库 TOCTOU；写入事务只是状态壳；活动快照不隔离直接仓库读取。
结论：不能从 #198 直接跳到物理删除提交。
```

## 3. 默认裁决

```text
第一版只删除当前活动图中的非根退役普通概念，且零当前有效支持、零全部外部有效关系。
名称、需求、任务、方法和其它外部引用不自动解绑，任一存在都阻止候选。
候选和写集是调用期值式 DTO，不新增生命周期阶段。
删除后结构从同一活动版本完整当前输入全量重算，并复用固定点共享函数。
采用单进程粗粒度结构事务域：普通公开读写共享许可，删除提交独占许可。
同一调用链只取得一次许可，下层显式复用不可复制同域令牌，禁止共享许可重入。
锁序为 结构事务许可 -> 活动图锁_ -> 图写锁_ -> 单个仓库锁；不同时持有多个仓库锁。
不做版本倒退回滚；提交前完成全部分配和验证，唯一提交点后只做无分配发布且无普通失败返回。
四类原子服务分别授权本类型概念清理，概念安全删除服务只做聚合编排。
活动快照最后单次发布；删除回执只汇总结构证据。
等价概念只能由新真实实例走现有发布路径形成新节点句柄。
不声明崩溃、断电、跨重启恢复、分布式事务或高并发性能。
```

## 4. 生成产物

```text
流程图/20260711_概念安全删除候选事务清理与后验固定点流程图_v0.1.md
流程图/20260711_概念安全删除候选事务清理与后验固定点流程图_v0.1.html
规范/详细设计/概念安全删除候选事务清理与后验固定点详细设计.md
计划/20260711_CONCEPT-S8-S1_删除候选与完整当前输入重建代码实施切片_v0.1.md
计划/20260711_STRUCT-TXN-S1_端点冻结写集与统一读取隔离代码实施切片_v0.1.md
计划/20260711_CONCEPT-S8-S2_结构所有者原子清理适配代码实施切片_v0.1.md
计划/20260711_CONCEPT-S8-S3_安全删除提交与活动快照发布代码实施切片_v0.1.md
计划/20260711_CONCEPT-S8-S4_删除后固定点审计与等价重建验收切片_v0.1.md
```

## 5. 队列映射

| 队列 | 设计队列 | 切片 | 直接前置 |
| --- | --- | --- | --- |
| #216 | DQ-108 | CONCEPT-S8-S1 删除候选与完整当前输入重建 | #215 串行；#198 业务完成；#199 正式接口 |
| #217 | DQ-109 | STRUCT-TXN-S1 端点冻结写集与统一读取隔离 | #216 |
| #218 | DQ-110 | CONCEPT-S8-S2 结构所有者原子清理适配 | #217 |
| #219 | DQ-111 | CONCEPT-S8-S3 安全删除提交与活动快照发布 | #218 |
| #220 | DQ-112 | CONCEPT-S8-S4 删除后固定点审计与等价重建验收 | #219 |

治理映射：`QR-168 / JY-235`。#199 仍是当前执行项，本批不插队。

## 6. 失败二分

```text
准入不满足、预算不足、候选过期、事务域未接入或新增支持 / 引用：逻辑内返回，不写本次删除结构。
完整输入矛盾、写集缺项、预构造包不完整、提交点后失败或后验残留：追根因解决，停止依赖路径。
重复删除只在全部审计与清零证据完整时幂等返回；普通读取为空不足以证明完成。
```

## 7. 验证状态

```text
Markdown / HTML Mermaid：已机械确认完全一致。
五份计划与详细设计验收矩阵：10 / 14 / 12 / 12 / 12 项逐字一致。
git diff --check：治理登记前通过。
tools/check_specs.py：已通过，未发现问题。
构建与程序：按设计窗口规则未运行。
```

## 8. 禁止扩大声明

本批只证明流程图、详细设计、五份完整依赖门控计划和治理路由已经形成。不得声明任何 C++ 已改、端点冻结已实现、事务已具备回滚、原子清理已实现、安全物理删除已完成、后验固定点已通过或恢复能力已形成。
