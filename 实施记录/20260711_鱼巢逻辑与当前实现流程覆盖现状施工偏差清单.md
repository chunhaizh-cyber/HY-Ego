# 鱼巢逻辑与当前实现流程覆盖现状施工偏差清单

日期：2026-07-11（2026-07-12 按 HEAD `15032ed` 复核）

| 编号 | 现状代码事实 | 流程图 | 详细设计 | 偏差类型 | 纠偏方向 | 是否需要代码计划 |
| --- | --- | --- | --- | --- | --- | --- |
| FC-01 | 旧鱼巢 HEAD 有 121 个受跟踪源码文件、202635 行，其中 3 个第三方 WebView2 头文件占 65399 行；117 个顶层源码共 136962 行。扫描清单只登记至 OF-206 并保留 OF-207 | 现有图只覆盖部分旧源码 | 不适用，属于证据缺口 | 补契约 / 补证据 | 冻结旧仓库快照，以 117 个顶层第一方源码为主追加未扫描函数族，第三方文件单列排除 | 否，文档计划 |
| FC-02 | 旧仓库 HEAD `ef2cbbf` ahead 4，另有 2 个 dirty C++ 文件 | 20260706-10 旧证据未统一写明该差异 | 不适用 | 补契约 | 每次旧逻辑提取记录 HEAD、ahead、dirty 哈希 | 否 |
| FC-03 | 旧自我线程提取图引用的 5 个旧流程图 / 工程图谱文件当前不存在 | 图的依据链不可复核 | 自我治理详细设计继承了该图作为依据 | 修图 / 修详细设计 | 删除失效路径或恢复真实证据，并重新核对结论 | 否，先文档计划 |
| FC-04 | 旧任务管理、筹办、执行、结果和结算代码均存在复杂调用链 | 0-20 图主要按新服务分类，没有旧代码节点映射 | 各服务设计只引用逻辑包或 S0 | 修图 | 分六个旧运行主链目标生成现状图完整包 | 否 |
| FC-05 | HEAD 递归共有 102 份流程图 Markdown；专门目录已有 45 对现状图，其中 43 对为 `3f08239` 批量基线，另 2 对为严格图兼容副本 | 原审计只扫描根目录，错误写成“56 份中仅 2 份现状图” | 图类型规范与批量生成计划均已存在 | 审计统计纠偏 | 撤销非递归统计；保留 45 对历史基线，另做 HEAD 当前性复核 | 否 |
| FC-06 | HEAD 有 59 个源码文件、42907 行；45 张专门目录现状图的 `覆盖文件` 元数据只绑定 38 个 | 21 个未绑定文件：`主信息仓库.h`、`事件日志段服务.*`、`关系仓库.cpp`、`写入事务.*`、`句柄.h`、`容错检查.h`、`索引仓库.h`、`节点仓库.h`、`控制面板窗口.h`、`任务结果回执协议.ixx`、`有界自我治理队列.ixx`、`自我治理消息协议.ixx`、`SQL数据库适配.cpp`、`二次特征服务.h`、三个 `初始化.*.ixx`、`方法召回服务.h`、`方法召回索引.h` | #236 只准备入口分段事实 | 修图 / 补分类 | 将每个文件归入已有流程、新图或“无独立运行逻辑”；显式绑定仍需逐行与契约复核 | 否 |
| FC-07 | THREAD-S1 已有现状图和映射 | 缺输入契约、非成功二分表，偏差表字段不完整 | 无独立修订计划 | 修图 / 补契约 | 补齐三项并严格校验 | 否 |
| FC-08 | HEAD `入口.cpp` 共 15539 行，main 自第 364 行开始，混合生产装配、全量自检、SQL 和 UI | 已有总入口主题批量现状图，但代码基线为 `3f08239`，不是 HEAD 逐段图 | #236 将先形成分段矩阵 | 修图 / 修详细设计 | 本计划入口批次依赖 #236，不把主题匹配误作逐行覆盖，也不抢跑入口拆分 | 否；代码拆分另走 #236 后计划 |
| FC-09 | 43 张批量现状图代码版本统一为 `3f08239`；到 `15032ed` 有 17 个受跟踪源码文件变化 | `generate_current_flowcharts.py --check` 只校验 45 对文件存在，不校验代码当前性 | 批量生成计划只承诺当时证据基线 | 版本漂移 | 先对 17 个变化文件关联的图做差异分类；未受影响图保留，受影响图按 HEAD 刷新 | 否 |
| FC-10 | `63cec55` 批量基线之后根目录新增 8 份流程图、修改 3 份流程图，专门目录没有变化 | 后增施工图中既有未实施协议，也有 METHOD-RECALL、CONCEPT-S6-S8 等已实施范围 | 相关详细设计与实施记录分散 | 回绘缺口 | 逐图按“未实施保留施工身份 / 已实施生成现状图 / 部分实施拆分”分类 | 否 |
| FC-11 | CONCEPT-S5 根目录与专门目录兼容副本 MD/HTML 哈希不一致；`概念图服务.h` 相对图声明基线有大幅变化 | 独立纠偏包结构可通过严格检查，但两份图不能同时代表 HEAD 当前事实 | S6-S8 后续实现继续扩展同一服务 | 副本冲突 / 版本漂移 | 确认唯一正式入口，按 HEAD 刷新图和四表后再计完整包 | 否；若发现代码 / 设计偏差再另建代码计划 |
| FC-12 | #200 事件段服务、METHOD-RECALL-S1-S6、CONCEPT-S6-S8 等在批量基线后完成 | 对应批量图仍标未实现或停在旧主题入口；后增施工图未回绘 | 实施记录存在 | 修图 | 以完成提交和实际接口为基线回绘，不重用计划中的假定接口 | 否 |
| FC-13 | SQL 审计和控制面板属于独立重建 | 旧控制面板迁移证据与新重建图曾混用，且 SQL `.cpp` / 窗口头未进入覆盖元数据 | 逐能力台账已开始区分 | 修图 / 补契约 | 旧证据图与新实现现状图分开，不计旧迁移完成度 | 否 |
| FC-14 | 当前逐能力台账已建立 MIG / NEW 分类 | 现有流程图尚未普遍引用能力编号 | 无代码影响 | 修图 | 后续图包逐项引用能力编号和来源性质 | 否 |
| FC-15 | 施工图实现后没有统一自动回绘门禁 | 计划完成仍可留下过期预期接口，批量目录也不会随提交自动失效 | 现状 / 施工区分规范只定义类型 | 补规范或补契约 | 在补齐计划中固定“实施记录 -> 版本漂移检查 -> 现状回绘 -> 严格检查”收口 | 否 |

## 分类汇总

```text
修图：FC-03 至 FC-14。
修详细设计：FC-03、FC-08。
修代码：本轮无；入口拆分沿用 #236 后续路线。
补规范或补契约：FC-01、FC-02、FC-07、FC-15。
```
