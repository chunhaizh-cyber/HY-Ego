# FS-02 存在与场景入口只读扫描 Codex 断点清单

日期：2026-07-06
状态：已记录断点 / 文档切片 / 未改代码 / 未迁移旧函数

## 1. 本切片输入

```text
FS-02 第二批基础信息模板入口扫描已提交并推送。
计划索引 HR-001-FM-SCAN 要求继续 FS-02 旧项目存在类、场景类、状态类、动态类、二次特征类和因果类第一批函数只读扫描。
用户明确“不需要确定的步骤应该持续执行下去”。
```

## 2. 已完成动作

```text
1. 回读 `计划/计划索引.md`、`规范/000_项目规则总纲.md`、`规范/001_规则迁移清单.md`。
2. 回读函数迁移前置专项、逐函数扫描清单、函数功能说明、领域服务授权写入规范和错误结果与入口拒绝规范。
3. 回读基础信息服务分层规范、存在服务详细设计和场景服务详细设计。
4. 只读扫描 `D:\鱼巢\存在类.h`、`D:\鱼巢\存在类.cpp`、`D:\鱼巢\场景类.h`、`D:\鱼巢\场景类.cpp`。
5. 新增 `实施记录/20260706_FS02_存在与场景入口只读扫描记录.md`。
6. 登记 OF-067 到 OF-079。
7. 同步逐函数只读扫描清单、计划索引和项目记忆。
```

## 3. 本切片结论

```text
旧存在类的存在身份、名称 / 类型、概念 / 实例、内部世界、旧需求 / 任务 / 方法根、观测位置和场景坐标混合在旧存在主信息与子特征路径中。
新项目应拆为存在服务、语素关系、场景关系、特征状态材料、动态证据和非权威缓存；不得迁移旧存在主信息字段。
旧场景类的场景创建、宿主关系和状态 / 动态 / 二次特征索引有可保留语义，但权威收纳必须由场景服务关系承载，索引快照只能作为读取视图或非权威缓存。
深度 / 彩图轮廓和三维体素候选依赖外设 / 体素 / 特征值原始材料，本轮只登记为 FS-X / 后续专项。
```

## 4. 下一步

```text
下一旧函数编号从 OF-080 开始。
继续 FS-02：旧状态类、动态类、二次特征类和因果类第一批函数只读扫描。
涉及特征值、三维体素、外设或显示视图时，转 FS-03、FS-10 或 FS-X 分流。
```

## 5. 禁止宣称

```text
不得声明旧函数已扫描完成。
不得声明旧存在类或旧场景类已迁移。
不得声明旧存在 / 场景主信息字段可直接进入新项目。
不得声明基础信息服务分层、存在服务、场景服务、状态 / 动态系统或二次特征系统完整完成。
不得接控制面板 / SQL / D455 / 体素 / 外设。
```

## 6. 验证记录

```text
本切片只做文档和只读事实记录验证。
未改代码。
未运行构建。
未运行当前项目入口。
未运行旧项目程序。
```
