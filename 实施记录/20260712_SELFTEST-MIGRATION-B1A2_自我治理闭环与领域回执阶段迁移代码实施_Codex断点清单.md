# SELFTEST-MIGRATION-B1A2 自我治理闭环与领域回执阶段迁移代码实施 Codex 断点清单

日期：2026-07-12

治理编号：JY-267

队列：#245 / DQ-137

## 1. 输入入口

```text
基线：3289d6c。
前置：#244 阶段化自检总成、#242 线程协议阶段已完成。
计划：20260712_SELFTEST-MIGRATION-B1A2_自我治理闭环与领域回执阶段迁移代码实施切片_v0.1。
```

## 2. 实际实现

1. 新建 `自检.自我治理闭环.ixx`，以主装配非拥有语境承载 SELF-GOV-S4A-D 32 项。
2. 新建 `自检.自我治理多轮.ixx`，模块内部唯一拥有 S5 隔离夹具，只接收方法登记根稳定键与自我根需求参数两个值式参数，承载 A01-A12。
3. 新建 `自检.自我治理回执.ixx`，借用后段领域材料、负例句柄和主信息观察锚点，模块内部拥有临时线程、桥与邮箱，承载 S3B 9 项。
4. 入口仅保留三段调用期语境、阶段注册、一次阶段运行和封存报告汇总；初始化闭环在控制面板构造前按 100 / 200 执行，领域回执在原后段材料锚点按 100 执行。
5. 四个全局聚合证据 `MATRIX基础信息后续入口通过`、`服务操作函数矩阵第一批通过`、`最小闭环通过`、`独立验收项通过` 的定义、输出和最终汇总继续留在入口原所有者。

## 3. 守恒与验证

```text
SELF-GOV-S4A-D：32 项。
SELF-GOV-S5：12 项。
SELF-GOV-S3B：9 项。
本切片：53 项；与 #242 的 66 项合计：119 项。

Debug x64：0 警告、0 错误。
完整自检：--self-test-exit 退出 0。
严格规范：tools/check_specs.py --strict 通过。
静态 A03B：S5 不包含四个全局聚合标识符；入口仍保留定义、输出和最终汇总引用。
连续自检：20 / 20 轮退出 0。
```

## 4. 完成边界

```text
#245 已完成并归档，#243 解除为执行前实际接口复核。
不证明入口全部存量自检已迁出，不证明结构事务、安全删除、恢复、自我循环、自我苏醒、真实外设或旧能力迁移完成。
```
