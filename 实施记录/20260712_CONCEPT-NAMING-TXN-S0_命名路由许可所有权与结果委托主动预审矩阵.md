# CONCEPT-NAMING-TXN-S0 命名路由许可所有权与结果委托主动预审矩阵

日期：2026-07-12

治理编号：JY-276

状态：文档治理 / 正式基线 `99da751` / #217 未提交代码不作为实现事实 / 未改 C++ / 未构建 / 未运行程序

## 1. 已验证当前事实

| 编号 | 当前事实 | 计划影响 |
| --- | --- | --- |
| F01 | 需求、语素、任务、方法、状态、动态服务均是传统头文件 | 不能 import 协调模块，只能消费数据桥 / 值式令牌 |
| F02 | `路由.概念命名治理.ixx` 是 #213 预期建立并由 #214 / #215 续接的生产真模块 | 可作为 S3 / S4 顶层许可所有者 |
| F03 | JY-272 禁止同状态持许可时无令牌重入，令牌只同步调用链传递 | 下层服务必须验证 / 下传，不能嵌套再取许可 |
| F04 | #213 计划同时使用结构独占许可和目标概念分片锁 | 必须冻结唯一锁序，防止分片锁内无令牌取得许可 |
| F05 | #214 名称关系成功而动态失败时保留名称事实 | 共享许可不等于跨服务原子事务，不设计回滚名称 |
| F06 | #225 已按 JY-275 设计为结果真模块拥有结果 + 完成许可、需求结算独立许可 | #215 只能锁外适配 / 委托，不得再取得外层许可或复制状态机 |
| F07 | #246 已登记运行期主装配接域 S0 | #213 / #214 只证明新路由连接态，不证明现有主装配整体接域 |

## 2. 假定接口与调用语境

| 切片 | 顶层许可所有者 | 传统服务职责 | 完成边界 |
| --- | --- | --- | --- |
| #213 | `路由.概念命名治理` 取得独占许可 | 需求服务验证 / 下传令牌；按固定分片串行 | 连接态唯一创建 / 撤销 / 发布 |
| #214 | 同一路由取得共享许可 | 语素 / 任务 / 方法 / 状态 / 动态服务验证同一令牌 | 连接态名称绑定与动态回执，非原子跨服务事务 |
| #215 | 不取得许可 | 锁外适配后调用 #225；许可归 #225 | 复用通用结果 / 完成 / 结算，不复制状态机 |

## 3. 固定锁序

```text
#213：结构独占许可 -> 目标概念分片锁 -> 传统服务 -> 仓库本地锁。
#214：结构共享许可 -> 传统服务 -> 仓库本地锁；不使用目标概念分片锁。
#215：命名路由无结构许可 -> #225 内部许可边界。
```

移动许可不进入服务成员、消息、候选、回执或结算 DTO。值式令牌不持久化、不排队、不跨线程。

## 4. 执行前复核与漂移

执行 #213-#215 前分别复核 #217、#223、#225 正式提交和实际模块 / 数据桥 / 服务重载 / DTO / 阶段占用。传统服务必须 import 协调模块、令牌不能贯穿、锁序冲突、#225 不能被窄适配复用或需修改本计划禁止文件时，执行窗口不改代码并退回设计。

## 5. 三件套一致性

本矩阵与概念命名流程图 MD / HTML、详细设计第 16 节和 #213-#215 修订范围一致；业务节点、失败二分、阶段 550 / 590 / 620 和生产 / 自检文件归属不变。

## 6. 完成边界

本记录只证明命名路由许可所有权、锁序和 #225 委托已提前设计。未证明 #217、#213-#215、#223、#225 或 #246 已执行，也不证明命名闭环、自我循环、恢复或旧能力迁移完成。
