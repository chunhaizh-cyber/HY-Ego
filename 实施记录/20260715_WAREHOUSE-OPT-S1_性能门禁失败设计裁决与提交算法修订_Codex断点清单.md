# WAREHOUSE-OPT-S1 性能门禁失败设计裁决与提交算法修订 Codex 断点清单

日期：2026-07-15

交互：WIN-0001-E71

计划 / 队列 / 阶段：#258 / DQ-150 / 800

决策：JY-356

状态：设计裁决与三件套修订完成 / 原 #258 重新待执行 / 800 未实施 / 未改 C++ / 未构建 / 未运行程序

## 1. 输入入口

```text
b633aaa 正式治理提交
实施记录/20260715_WAREHOUSE-OPT-S1_正式性能门禁复测失败_Codex断点清单.md
流程图/20260713_数据仓库关系查询二级索引与性能治理流程图_v0.1.md
规范/详细设计/数据仓库关系查询二级索引与性能治理详细设计.md
计划/20260713_WAREHOUSE-OPT-S1_关系仓库二级索引代码实施切片_v0.1.md
D:/海中鱼巣-执行-265 中四个未提交 WIP 文件的只读差异
```

## 2. 当前事实与草案边界

正式 `main` 仍没有关系仓库二级索引，阶段 800 未登记。JY-355 首版草案只保留在执行工作树，不能作为当前实现事实。

两次正式运行共同证明：

```text
三档结构签名保持不变。
查询候选已缩小，全表参考模型一致。
A01、A07-A12 重复通过。
失效 / 逻辑删除 P95 跨规模约 10-19 微秒，多次超过 #257 的 150% 上限。
三档查询轮稳定性没有连续满足 15% 门槛。
```

## 3. 核心处理与裁决

只读草案复核定位到两个独立问题：

1. S1 验收把 `规模报告::完整()` 与三轮稳定性绑定，导致本来已经成立的 A02-A06 查询正确性证据被稳定性失败联动压成失败。
2. 首版通用索引切换在同一状态变更前后各做一轮五项成员复核，并以嵌套关系编号哈希集合维护稀疏桶，形成跨规模接近固定的提交成本。

JY-356 裁决：

```text
保持原 #258 / DQ-150，不新增平行计划。
保留 150% 纯写 P95 上限、15% 三轮稳定门槛、固定签名和阶段 800 全部门禁。
拆分材料完整、查询正确、稳定合格和性能合格；只修正证据归属，不降低总验收。
重做索引提交算法为受影响桶一次定位、转换计划、定向发布 / 移除和逆序回退。
普通热路径不允许提交前后重复通用五项复核；Debug 全量诊断退出正式写入计时区间。
首版四文件 WIP 只能逐段复用和修改，不得原样暂存或提交。
算法重做后至少两次独立正式运行都要满足 D-H；任一次写入超线即再次退回。
本轮算法重做仍失败时，默认形成不实施 / 不发布 S1 收口，不继续修改阈值追逐实现。
```

## 4. 结构承载

本轮只修订：

```text
仓库优化流程图 Markdown / HTML
仓库优化详细设计
原 #258 施工计划
计划索引
Codex 任务队列
当前状态、决策、验收和窗口交互记录
本断点清单
```

执行草案、正式 C++、工程、filters、入口阶段登记、四仓库、事务 ABI、领域、线程、控制面板、SQL、持久化、外设和 #214 均不在本轮修改范围。

## 5. 输出变化

```text
#258 / DQ-150：JY-355 退回设计 -> JY-356 原编号重新待执行。
正式治理基线：b633aaa；执行窗口须重新读取本次设计提交。
800：仍未实施、未登记。
#259-#261：继续等待 #258 正式完成和 800。
#214：继续用户暂停。
```

## 6. 调用路径

```text
JY-355 正式失败证据
-> 设计窗口只读复核 WIP 提交路径与报告判定
-> 区分正确性联动误报和真实写入回归
-> JY-356 保持硬门禁并重做提交算法
-> 修订流程图 / 详细设计 / 原 #258
-> 原 #258 / DQ-150 重新发布
-> 执行窗口从正式提交复核实际 WIP
-> 一致才继续；仍超线则退回并默认不发布 S1
```

## 7. 验证方式与声明边界

本轮只运行文档一致性、Mermaid 同文、严格规范和 Git 差异检查，不运行 C++ 构建、程序、性能基准或自检。

不得据此声明 #258、A01-A14、800、二级索引、性能提升、仓库优化、令牌优化、分片、显示投影、旧能力迁移或项目迁移完成。
