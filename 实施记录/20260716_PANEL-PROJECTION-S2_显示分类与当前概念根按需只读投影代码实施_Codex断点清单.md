# PANEL-PROJECTION-S2 显示分类与当前概念根按需只读投影代码实施 Codex 断点清单

日期：2026-07-16

计划 / 队列 / 阶段：#261 / DQ-153 / PANEL-PROJECTION-S2 / 830

决策 / 问题：JY-361 / QR-185 已关闭

状态：已完成 / A01-A16 通过 / 830 已登记 / 已构建 / 已运行 / 待随本次提交归档

## 1. 输入与边界

```text
正式基线：bb5ab8c8b8d1ea1eaafd741c3674ab5f2f4e8a6c
流程图/20260716_显示分类与当前概念根按需只读投影流程图_v0.1.md/.html
规范/详细设计/显示分类与当前概念根按需只读投影详细设计.md
计划/已完成计划/20260713_PANEL-PROJECTION-S2_显示分类与当前概念根按需只读投影计划_v0.1.md
实施记录/20260716_PANEL-PROJECTION-S2_性能门禁正式接口复核与WIP恢复顺序_Codex断点清单.md
WIN-0001-E78 / JY-361
```

接管前已确认执行工作树位于 `main`，`HEAD = origin/main = bb5ab8c`，工作树和暂存区为空。以对象身份确认并只执行：

```text
git stash apply 12cda61fead9cae3891fff040b2701f4cab0932e
```

恢复后的 dirty 集恰好为计划固定七路径。只有 `海中鱼巣/入口.cpp` 发生内容冲突；工程和 filters 自动合并，业务服务、窗口和自检模块无冲突。#261 stash 未 pop、drop 或删除。#258 四文件 stash 未读取、恢复、修改、提交或删除。

## 2. 实施结果

`控制面板服务` 新增值式按需请求、根页游标和当前页候选：

```text
世界 / 概念 / 语素 / 需求 / 任务 / 方法
-> 每次只构造请求分类
-> 概念分类只读取一个当前活动根
-> 任务根只由需求承接关系发现
-> 方法根只由任务选择关系发现
-> 根分页只在真实根边界切换
-> 游标复核分类、概念根、活动版本、根集签名和偏移
```

旧 `生成六类真实树只读材料` 签名和原义保留，只供兼容与自检。新旧入口复用同一分类构造、根发现和结构复核逻辑；按需入口不通过旧六类入口回退。

`控制面板窗口` 的生产路径改为当前分类页：

```text
启动 / 分类切换 / 概念根切换 / 翻页 / 刷新
-> 生成当前分类真实树只读材料
-> 成功后整体替换当前页
-> TreeView 只重建当前分类 UI 根和当前页真实根
```

六项导航继续保留；概念根下拉只在概念分类显示；上一页 / 下一页只保存会话期游标历史。逻辑拒绝保留上一有效页并标明未刷新；追根因错误不发布新页，旧页恢复失败时停止窗口路径。

新增 Debug 真模块 `海中鱼巣.自检.控制面板按需投影`。入口在阶段 830 最小登记一次；工程和 filters 各唯一登记该模块，同时保留 #280 的性能门禁分离模块。

## 3. 入口合并结果

入口冲突按 JY-361 定向合并为：

```text
默认 Debug --self-test-exit
-> 不登记 790
-> 登记 795
-> 登记 830

显式 Debug --warehouse-performance-self-test-exit
-> 条件登记 790
-> 登记 795
-> 登记 830

Release 显式性能请求
-> 结构装配前退出 2
```

阶段 790、795、830 各唯一登记。790 继续受 `关系仓库性能验收已请求` 条件控制，未恢复无条件性能登记；#261 不修改性能算法、阈值、种子、签名或关系仓库。

## 4. A01-A16

| 验收 | 结果 |
| --- | --- |
| A01 | 非树分类、零节点 / 深度预算、根页 0 / 257 和错误游标逻辑拒绝，无部分页 |
| A02-A05 | 六类分别独立成功；单分类构造计数隔离；四个概念根和单根输出正确；任务 / 方法根发现不构造上游树 |
| A06-A08 | 根排序、签名、分页、预算拒绝、多根、多父、重复投影和有限展开通过 |
| A09-A10 | 按需完整页与旧六类入口逐项一致；生产窗口对旧六类入口调用为 0 |
| A11-A12 | 当前分类快照、单页 TreeView、概念根选项和拒绝 / 追根因收口通过 |
| A13 | 请求前后节点、关系、索引数量不变，结果完整句柄和主信息仍可读 |
| A14 | Debug 真模块和 Release 零专项输出 / 二进制命中通过 |
| A15 | Debug / Release x64 完整 Rebuild 0 警告、0 错误；默认自检退出 0；专项 A01-A16 全通过 |
| A16 | 最终 Debug 默认功能回归连续 20/20；严格规范、diff、模块 / 阶段唯一性和范围扫描通过 |

## 5. 验证证据

```text
Debug x64 完整 Rebuild：0 warning / 0 error，2:10.93
Debug --self-test-exit：退出 0
PANEL-PROJECTION-S2-A01 至 A16：全部通过
默认 Debug WAREHOUSE-OPT-S0 输出命中：0
最终同一 Debug 二进制：连续 20/20，逐轮退出 0、专项零失败、790 输出零命中

Release x64 完整 Rebuild：0 warning / 0 error，1:28.66
Release --self-test-exit：退出 0
Release --warehouse-performance-self-test-exit：结构装配前退出 2
Release 输出和可执行文件 PANEL-PROJECTION-S2 / 控制面板按需投影标识命中：0

生产窗口旧六类入口调用：0
阶段 790 / 795 / 830：各 1
按需投影与性能门禁分离模块在工程 / filters：各 1
tools/check_specs.py --strict：通过
git diff --check / git diff --cached --check：通过
允许 / 禁止文件和冲突标记扫描：通过
```

显式 Debug 性能模式未在本切片重复运行；JY-361 明确其单次结果不裁决 #261。本切片保持 #280 的条件登记和真实退出码合同，不据此宣称性能稳定或性能改善。

## 6. 输出与声明边界

#261 / DQ-153 已完成并归档，A01-A16 通过，830 已登记。控制面板生产窗口现在只生成当前显示分类的只读页；概念分类可切换当前活动根；旧六类入口继续保留兼容；显示请求不改变权威结构。

当前执行队列没有新的依赖就绪代码项，结果返回设计窗口。#214 继续用户暂停。#261 stash 保持存在；#258 stash 状态不变。

不得据此宣称关系仓库二级索引或性能改善已发布、六分类是六棵物理树、跨页形成同版本权威快照、完整业务操作控制面板完成、旧控制面板 / 数据库 / 外设能力迁移完成，或项目迁移全部完成。
