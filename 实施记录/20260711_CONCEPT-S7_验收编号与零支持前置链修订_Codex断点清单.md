# CONCEPT-S7 验收编号与零支持前置链修订 Codex 断点清单

日期：2026-07-11

决策编号：JY-232

状态：上游漂移已复核 / 流程图与详细设计已修订 / #197 仍由执行窗口治理收口 / 本窗口未改 C++ / 未构建 / 未运行程序

## 1. 输入基线

```text
HEAD：256aef3
分支：main
main...origin/main：0 / 0（修订开始时）
#210：已完成并形成正式支持失效入口、活动版本推进和候选过期能力。
#197：执行窗口报告代码、Debug x64、完整自检和连续 20 轮已通过，但因上游验收编号漂移暂停治理、暂存和提交。
```

修订开始时以下 5 个执行窗口文件已有未暂存改动，本窗口不得修改、暂存或提交：

```text
海中鱼巣/入口.cpp
海中鱼巣/领域/概念图服务.h
规范/仓库逻辑空间与领域树结构规范.md
规范/领域服务授权写入规范.md
计划/20260711_CONCEPT-S7_冷却退役与重新激活代码实施切片_v0.1.md
```

## 2. 正式漂移事实

```text
#197 计划的 CONCEPT-S7-A01 至 A08 已按 #210 修订为：
A01 真实普通概念零支持。
A02 旧候选过期、新候选当前。
A03 活跃到冷却。
A04 冷却到退役及非法活跃直退役。
A05 重新激活。
A06 根保护与同状态幂等。
A07 主动召回过滤并保留全图 / 树可读。
A08 并发转换与支持变化。
```

原详细设计仍把 A01-A08 编为活跃到冷却、冷却到退役、两类重新激活、非法跳转、根保护、退役可读、并发转换。行为大体存在，但编号和聚合边界已经与正式计划不同。

原生命周期流程图只从用途候选和权威支持读取进入 S7，没有显式表达：

```text
#210 正式支持失效
-> 真实普通概念零当前有效支持
-> 活动版本推进
-> 失效前 #195 候选过期
-> 当前活动版本候选重算
-> S7 权威复核与转换
```

因此实施记录无法把 A01-A08 同时回指计划、详细设计和流程图。

补充接口事实：生命周期复核候选是调用期值式 DTO，没有服务内候选存储。#210 的真实写入是推进活动版本，使失效前生命周期复核候选因版本约束过期；其显式清理对象是固定点 `上一候选图_`，不得写成“清除生命周期候选”。

## 3. JY-232 裁决

```text
以 #197 正式计划 A01-A08 为唯一编号基线，详细设计验收矩阵逐项同名、同序、同边界修订。
生命周期流程图 MD / HTML 必须补入 #210 到真实零支持和候选版本换代的 S7 前置链。
活跃到冷却验收必须使用当前活动普通概念，不得回退四根、裸删支持或复用旧候选。
#210 写前逻辑拒绝或真实普通概念仍未达到零当前有效支持，归为逻辑内返回：本次拒绝请求不写，已完成合法失效与活动版本推进保持，不使用旧候选，生命周期关系不变。
每条 #210 请求独立原子；本修订不新增跨多关系事务，也不因后续拒绝或并发新增支持回滚已完成的合法失效。
只有已真实达到零当前有效支持并推进活动版本后，旧候选仍可接受或新候选版本 / 零支持 / 观察材料不一致，才归为追根因解决。
当前候选仍有外部引用或正向用途而不进入转换，继续归为逻辑内返回并保持原生命周期。
```

本裁决不改变 #197 计划的允许文件、禁止文件、机器行为或既有 A01-A08，只校正上游设计与图谱的正式映射。

## 4. 修订材料

```text
流程图/20260711_概念命名用途与生命周期治理流程图_v0.1.md
流程图/20260711_概念命名用途与生命周期治理流程图_v0.1.html
规范/详细设计/概念命名用途与生命周期治理详细设计.md
```

范围一致性：流程图补入 #210 支持失效、活动版本推进、旧候选过期和新候选重算，并把写前拒绝 / 未达零支持逻辑返回与零支持后的候选异常追根因拆成不同分支；详细设计补入同一前置链、值式候选边界、失败二分和与计划完全同序的 A01-A08。

## 5. 治理映射

```text
执行队列：#197，编号不变。
设计队列：DQ-89，编号不变。
决策记录：JY-232。
待确认问题：不新增；QR-167 补记验收映射已经统一。
新计划：不生成；#197 正式计划范围未改变，且该文件由执行窗口持有未暂存改动。
```

## 6. 验证状态

已执行：

```text
详细设计 CONCEPT-S7-A01 至 A08 与 HEAD 中 #197 正式计划逐项文本 8 / 8 一致。
流程图 MD / HTML Mermaid 图文本完全一致。
值式生命周期候选、固定点 `上一候选图_`、逻辑内返回 / 追根因二分和逐关系原子无跨多关系回滚语义检查通过。
git diff --check、git diff --cached --check 与 tools/check_specs.py 通过。
精确 staged scope 为 10 份文档；5 个执行窗口脏文件继续未暂存，C++ / ixx / 工程文件 staged 为零。
执行窗口最终只读复核通过，未发现新增漂移。
```

按设计计划窗口规则，本窗口不运行构建或程序；执行窗口已经报告的 #197 构建和自检结果不由本修订扩大声明。

## 7. 完成声明边界

本修订完成后只能声明 #197 的流程图、详细设计和正式计划验收编号及零支持前置链已经一致。

不得声明 #197 已治理收口、A01-A08 已由本窗口运行、计划已归档、#198 已解锁、安全删除已实现或旧概念能力已迁移。
