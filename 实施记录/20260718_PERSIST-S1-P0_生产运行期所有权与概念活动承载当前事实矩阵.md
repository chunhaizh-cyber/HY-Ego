# PERSIST-S1-P0 生产运行期所有权与概念活动承载当前事实矩阵

日期：2026-07-18

执行队列：#295

设计队列：DQ-187

设计裁决：JY-390

计划版本：v0.1

窗口模式：单窗口串行双角色 / 本轮为执行角色

执行接管基线：`4aa62e327062ba74bab331fa9bd9594e4c1e5113`

审计结论：通过 / M0—M6 完成 / A01—A18 全部通过 / 执行完成待设计同步 / 纯文档只读 S0 / 未构建 / 未运行程序 / 未登记新阶段

## 1. 二选一结论

```text
通过。

入口旧对象、生产模块静态消费者、新运行期宿主 / 租约调用点、22 个正式业务公开面、
概念活动私有字段和生产切换面均可从当前代码形成唯一归属。

当前没有同一生产事实的两个无法裁决的正式所有者：
旧入口对象是当前实际启动栈；新运行期宿主 / 租约仍只在自检使用。

目标 P0 接口尚未建立属于待实施缺口，不是本轮接口漂移。
```

本轮只证明生产旧栈调用点、唯一运行期切换面、概念活动字段所有权和后继批次已经形成当前事实证据。不得声明生产入口已迁移、旧局部对象已退出、段 9 已实现、九段同冻结材料已形成、HYSNAP01 已编码 / 发布 / 恢复、O1 已接生产，或旧持久化、数据库、控制面板能力已迁移。

## 2. M0—M6 执行摘要

| 步骤 | 当前事实与证据 | 结果 |
| --- | --- | --- |
| M0 正式状态重读 | 接管时 `main == origin/main == 4aa62e3`，工作树干净；E131、索引、队列和 WIN-0001 均登记 #295 为当前唯一依赖就绪候选；两份允许输出均不存在 | 通过 |
| M1 入口对象与宿主调用图 | `入口.cpp:456-485` 无条件构造旧四仓库和旧服务；`517` 启动旧初始化线程；`11612-11640` 用旧统计 / 仓库做 SQL 审计，`11662-11678` 用旧控制面板对象运行界面；新宿主 / 租约只在 4 个自检模块出现 | 通过 |
| M2 兼容生产消费者 | 17 个传统服务头不变；直接包含文件 51 -> 55，自检模块 35 -> 49，直接提四仓库自检 32 -> 40；全部增量均为自检，生产线程 / 上行桥 4 个文件未增长；22 个正式公开面禁用能力命中仍为 0 | 通过 |
| M3 概念活动所有权 | 旧服务 7 组私有成员逐字段完成权威 / 可重建 / 瞬态分类；新概念图结构数据操作 / 服务无活动侧表，不能接回旧服务 | 通过 |
| M4 生产切换面 | 完整候选、系统角色、首次发布、租约、消费者改接、旧对象退出和快照消费租约形成 7 步串行主链；并行只允许在租约有效后的只读适配或互不重叠消费者批次 | 通过 |
| M5 后继窄切片 | P0-A、P0-B、P0-C 与 PERSIST-S1-A 的目标文件组、前置、退出条件和串行写入所有权已登记；未生成正式代码计划 | 通过 |
| M6 证据与验证 | 只新增本矩阵和专属断点；strict、diff 和允许范围通过；未构建、未运行、未登记阶段、未操作 stash | 通过 |

## 3. M1：入口旧对象与当前调用归属

### 3.1 实际启动主链

```text
入口参数解析
-> 入口.cpp:456-485 构造旧四仓库、传统服务、初始化器、统计 / 快照 / 显示 / SQL
-> 入口.cpp:516-545 无条件启动旧自我初始化线程并做重复 / 拒绝验证
-> 入口.cpp:547-11591 在同一组旧对象上执行初始化、历史夹具和完整自检
-> 入口.cpp:11592-11610 汇总门禁；自检模式才在这里退出
-> 入口.cpp:11612-11640 读取旧节点 / 关系 / 索引并写 SQL 审计投影
-> 入口.cpp:11662-11678 把旧控制面板服务、数据库和旧结构统计快照交给原生窗口
-> 或入口.cpp:11681-11689 仅保持无窗口常驻
```

因此正常模式不是“先构造新上下文，再把旧栈仅作为隔离自检夹具”。它仍先对旧局部对象完成初始化和自检，再把同一对象组的只读投影交给 SQL / 界面。无窗口常驻也只是保留进程，没有生产 `运行期上下文宿主`、有效租约或运行期业务请求路由实例。

### 3.2 `入口.cpp:456-485` 逐对象矩阵

| 入口对象 | 拥有类型 / 构造依赖 | 当前传入调用方与调用分类 | 当前机器事实写入 | 租约替代结论 |
| --- | --- | --- | --- | --- |
| `主信息` | `主信息仓库`；旧栈根对象 | 传给节点及绝大多数传统服务；`500-505` 入口读回；`766-769` 自我治理自检上下文；历史夹具直接读写 | 是；主信息记录和值容器由旧服务 / 夹具写入 | 四仓库所有权可由租约中的 `运行期上下文` 替代；消费者不得继续持该局部引用 |
| `节点` | `节点仓库(主信息)` | 传给关系、索引、全部传统领域服务、控制面板；`493-505` 数量 / 类型读回；SQL 统计 `11620` | 是；旧服务和自检直接创建 / 删除节点 | 可由租约同身份仓库替代；显示 / SQL 只能取得值式只读材料 |
| `关系` | `关系仓库(节点)` | 传给旧服务、初始化器、自检、控制面板；SQL 统计 `11620` | 是；入口历史夹具和旧服务直接创建、重挂、删除关系 | 可由租约同域关系仓库替代，但生产业务只能经新服务 / 数据操作写 |
| `索引` | `索引仓库(节点)` | 传给语素和自检上下文；历史夹具直接绑定 / 查询；SQL 统计 `11620` | 是；旧语素 / 方法和夹具仍有绑定 | 可由租约同域索引仓库替代；必须保留所有者声明，不向业务面暴露仓库 |
| `事务` | `写入事务` 旧事务壳 | 只在入口传统事务夹具约 `1020-1109` 直接开始 / 提交；不进入正常模式后段 | 只写旧自检夹具 | 不由租约公开；应由新数据操作、执行器和不透明会话取代，不能作为生产兼容出口 |
| `存在` | `存在服务(主信息,节点,关系)` | 传给概念初始化、世界、世界树、自我治理上下文；入口大量旧创建 / 查询夹具 | 是 | 新 `服务.存在` / `组合.存在场景` 已存在；须由运行期业务面分批改接 |
| `场景` | `场景服务(主信息,节点,关系)` | 传给世界、世界树、自我治理与任务 / 方法夹具 | 是 | 新 `服务.场景` / `组合.存在场景` 可承接已覆盖能力；旧治理缺口另列 P0-B |
| `状态` | `状态服务(主信息,节点,关系)` | 传给概念初始化、世界、需求初始化、自我治理、控制面板和大量夹具 | 是 | 新 `服务.状态` / `组合.状态动态` 可承接结构状态；概念生命周期角色尚不能直接替代 |
| `动态` | `动态服务(主信息,节点,关系)` | 传给概念初始化、世界、自我治理和动作 / 因果夹具 | 是 | 新 `服务.动态` / `组合.状态动态` 已覆盖部分；旧动作执行证据链仍缺公开组合入口 |
| `二次特征` | `二次特征服务(主信息,节点,关系)` | 传给概念初始化、特征、世界和概念候选夹具 | 是 | 新 `服务.二次特征` 已存在；旧概念候选活动语义由 P0-C 独立承接 |
| `语素` | `语素服务(主信息,节点,关系,索引)` | 传给概念初始化、语素初始化、需求初始化、控制面板与命名夹具 | 是 | 新 `服务.语素` / `组合.语素概念入口` 已存在；概念命名治理仍缺完整新公开面 |
| `特征值` | `特征值服务(主信息,节点)` | 只作为 `特征` 构造依赖，并进入自我治理自检上下文；入口无直接方法调用 | 是；经旧特征服务维护 I64 / Vec 私有侧表 | 新装配已有私有 `特征值_`，只能经特征服务使用；快照全侧表另归 PERSIST-S1-A |
| `因果` | `因果服务(主信息,节点)` | 传给概念初始化、世界和旧因果 / 自我治理夹具 | 是 | 新 `服务.轻量因果` 已存在；旧动作 / 结算链必须通过新业务组合而不是直接传服务 |
| `概念图` | `概念图服务(节点,关系)` | 传给概念初始化、控制面板、概念命名 / 生命周期 / 候选 / 删除夹具；入口 `547-10307` 广泛读写 | 是；同时持私有根、生命周期登记、活动快照和版本 | 不能仅由现有租约替代；P0-C 必须形成新运行期唯一活动所有者或确定性重建参与者 |
| `概念图初始化` | `概念图初始化服务(概念图,存在,动态,二次特征,因果,状态,语素)` | `557、560` 初始化四根；结果进入控制面板材料；其它为自检 | 是；通过旧原子服务和旧概念图登记写旧栈 | 新系统角色已覆盖四根，但不覆盖生命周期角色 / 签名 / 活动版本；不得接入新装配 |
| `特征` | `特征服务(主信息,节点,关系,二次特征,特征值)` | 传给世界、需求初始化、自我治理；入口特征 / 概念候选夹具大量读写 | 是 | 新 `服务.特征` / `组合.特征状态` 可承接正式能力；旧侧表只能由新装配内同一特征值所有者持有 |
| `世界` | `世界服务(...)` 旧聚合壳 | 传给世界树 / 语素初始化和自我治理上下文；入口旧世界夹具 | 是 | 不应在新装配中复建同名聚合壳；按新存在、场景、状态、动态等公开面拆分 |
| `世界树初始化` | `世界树初始化服务(...)` | 传给 `自我线程实例` 和拒绝验证线程；`556` 读取坐标；自我治理自检上下文 | 是；旧启动初始化写旧栈 | 新系统角色初始化是替代基础，但旧世界树结果字段须逐项映射，不能直接复用对象 |
| `语素初始化` | `语素初始化服务(...)` | 只传给两个旧 `自我线程` 对象；结果从线程初始化快照读回 | 是；旧启动初始化写旧栈 | 新系统角色 / 语素业务面需形成等值启动材料；旧初始化模块保持隔离兼容 |
| `需求` | `需求服务(主信息,节点,关系)` | 传给需求初始化、控制面板、自我治理和大量需求 / 任务夹具 | 是 | 新 `服务.需求` / `组合.需求任务方法` 已存在；概念命名和固定根治理缺口归 P0-B |
| `需求初始化` | `需求初始化服务(特征,语素,需求,状态)` | 传给两个旧 `自我线程`；初始化结果进入自我治理夹具 | 是；创建旧根需求 / 状态关系 | 新系统角色包含根需求材料，但旧初始化结果 / 线程协议须值式改接 |
| `自我线程实例` | `自我线程(语素初始化,世界树初始化,需求初始化)` | `517-524` 无条件启动、等待、读快照并重复启动；另有 `526-544` 拒绝验证对象 | 是；启动阶段经三个初始化器写旧栈 | 不由租约直接替代；生产启动必须先发布新上下文，线程只能在有效租约后构造，旧初始化线程退出生产所有者 |
| `任务` | `任务服务(主信息,节点,关系)` | 传给控制面板、自我治理与任务执行 / 结算夹具 | 是 | 新 `服务.任务` / `组合.需求任务方法` 已存在；旧上行桥读取与完整执行链仍需 P0-B |
| `方法` | `方法服务(主信息,节点,关系)` | `481` 启动前初始化旧方法根；传给控制面板、自我治理和方法 / 动作夹具 | 是 | 新 `服务.方法` / `服务.方法召回` 已存在；旧动作执行桥、方法证据和读取查询缺口仍需 P0-B |
| `统计` | `统计服务` 私有缓存所有者 | 传给自我治理自检；入口概念用途 / 结构统计夹具；正常模式 `11612-11620` 生成 SQL 审计快照 | 否于权威结构；写非权威缓存 | 不能成为机器事实；生产 SQL / 观察只允许消费租约值式快照，统计迁移独立于业务写改接 |
| `快照` | `仓库快照服务` 恢复拒绝壳 | 只在入口约 `1002-1014` 评估恢复请求，自检后无生产调用 | 否；仅值式评估 | 现接口不接受租约且不是 HYSNAP01 服务；PERSIST-S1-A 后由新冻结编排消费租约，旧壳不得扩权 |
| `显示` | `显示服务` | 传给自我治理上下文和 `控制面板服务`；入口只读展示夹具 | 否；只生成人读材料 | 可由租约值式只读材料替代；不得取得仓库可写引用 |
| `数据库` | `SQL数据库适配` | 正常模式 `11621-11622` 写 / 读结构统计审计；`11665` 交给原生窗口 | 否于机器事实；只写审计数据库 | 保留只读 / 审计适配，输入改为同一租约的值式快照；数据库不成为事实源 |
| `控制面板` | `控制面板服务`，在 `796-810` 由显示、节点、概念图、语素、需求、任务、方法、状态构造 | Debug 自检上下文 `812-830`；正常模式 `11664-11668` 交给原生窗口 | 否；只读投影 | 必须改为租约值式查询 / 投影适配；旧概念图对象不能因界面需要继续当段 9 所有者 |

### 3.3 新宿主 / 租约全部当前调用点

| 文件 | 当前调用事实 | 分类 |
| --- | --- | --- |
| `海中鱼巣/自检.运行期上下文.ixx` | 构造宿主，验证发布前空租约、无效 / 不完整候选拒绝、系统角色初始化后首次发布和同身份租约 | 自检 |
| `海中鱼巣/自检.系统角色初始化.ixx` | 初始化系统角色，验证发布前拒绝、完整候选发布、重复 / 并发初始化和租约 | 自检 |
| `海中鱼巣/自检.主装配事务.ixx` | 验证空 / 不完整候选、单次发布、重复候选、第二候选和并发单赢家 | 自检 |
| `海中鱼巣/自检.运行期组合分层.ixx` | 读取候选业务操作并验证发布 / 拒绝路径 | 自检 |
| `海中鱼巣/启动.运行期上下文.ixx` | 只定义上下文、宿主、租约和发布 / 读取接口 | 生产类型定义，不是生产调用 |

全仓没有生产模块持有 `运行期上下文宿主`，没有正常入口调用 `尝试首次发布全新上下文` 或 `读取租约`，也没有快照服务消费租约的调用点。

## 4. M2：兼容基线、22 个公开面与生产消费者

### 4.1 #271 基线漂移复核

复核基线为 `23238dd`，当前为 `4aa62e3`。

| 指标 | #271 基线 | 当前 | 精确变化 | 裁决 |
| --- | ---: | ---: | --- | --- |
| 传统服务头 | 17 | 17 | 无新增、无删除 | 保持冻结兼容 |
| 直接包含传统服务头的源码文件 | 51 | 55 | +4、-0 | 4 个增量全是自检，不是生产消费者增长 |
| `自检.*.ixx` 模块 | 35 | 49 | +14、-0 | 后继专项自检增加 |
| 直接提及四仓库的自检模块 | 32 | 40 | +8、-0 | 8 个增量均为自检夹具 |

直接包含传统头的 4 个新增文件：

```text
海中鱼巣/领域/自检.动态模式.ixx
海中鱼巣/线程/自检.任务结果结算.ixx
海中鱼巣/线程/自检.任务执行调度.ixx
海中鱼巣/自检.控制面板按需投影.ixx
```

新增 14 个自检模块：

```text
海中鱼巣/核心/自检.关系仓库性能基线.ixx
海中鱼巣/线程/自检.任务执行调度.ixx
海中鱼巣/线程/自检.任务结果结算.ixx
海中鱼巣/自检.性能门禁分离.ixx
海中鱼巣/自检.控制面板按需投影.ixx
海中鱼巣/自检.服务写入兼容边界.ixx
海中鱼巣/领域/自检.动态模式.ixx
海中鱼巣/领域/自检.动态聚类.ixx
海中鱼巣/领域/自检.因果完整结构键.ixx
海中鱼巣/领域/自检.因果模式.ixx
海中鱼巣/领域/自检.方法动作场景关系.ixx
海中鱼巣/领域/自检.用途事件.ixx
海中鱼巣/领域/自检.用途观察.ixx
海中鱼巣/领域/自检.用途观察结构.ixx
```

其中直接提及四仓库的 8 个新增自检：

```text
海中鱼巣/核心/自检.关系仓库性能基线.ixx
海中鱼巣/线程/自检.任务执行调度.ixx
海中鱼巣/线程/自检.任务结果结算.ixx
海中鱼巣/自检.控制面板按需投影.ixx
海中鱼巣/自检.服务写入兼容边界.ixx
海中鱼巣/领域/自检.动态模式.ixx
海中鱼巣/领域/自检.方法动作场景关系.ixx
海中鱼巣/领域/自检.用途观察结构.ixx
```

当前 55 个直接包含文件的分类仍可由 #271 基线机械扩展为：四个历史初始化兼容模块 4、生产线程 / 上行桥 4、入口或自检 25、其它生产或兼容文件 22。生产线程 / 上行桥没有新增；新增只扩大自检兼容基线，不授予生产新调用。

### 4.2 22 个正式新业务公开面

14 个服务：

```text
领域/服务.存在.ixx
领域/服务.场景.ixx
领域/服务.状态.ixx
领域/服务.动态.ixx
领域/服务.特征.ixx
领域/服务.二次特征.ixx
领域/服务.基础信息.ixx
领域/服务.语素.ixx
领域/服务.轻量因果.ixx
领域/服务.概念图结构.ixx
领域/服务.需求.ixx
领域/服务.任务.ixx
领域/服务.方法.ixx
领域/服务.方法召回.ixx
```

7 个组合器：

```text
领域/组合.存在场景.ixx
领域/组合.状态动态.ixx
领域/组合.特征状态.ixx
领域/组合.语素概念入口.ixx
领域/组合.概念结构发布.ixx
领域/组合.需求任务方法.ixx
领域/组合.运行期业务操作.ixx
```

唯一运行期请求路由：

```text
线程/路由.运行期业务请求.ixx
```

以上 22 个文件全部存在；`结构事务令牌 / 许可 / 取得共享或独占许可 / 四仓库 / 执行器 / 会话` 命中文件数为 0。唯一业务出口链为：

```text
运行期上下文::读取候选业务操作()
-> 运行期业务装配::读取业务操作()
-> const 运行期业务操作组合器*
-> 运行期业务请求路由(运行期业务操作组合器 const&)
-> 8 类强类型值式请求 / 结果
```

上下文公开的接线 / 仓库访问器属于内部结构与后继冻结入口候选，不是线程、界面或普通领域调用的业务出口。

### 4.3 四个兼容生产模块

| 当前生产模块 | 当前读取 | 当前写入 | 可用目标新公开面 | 当前缺口 | 风险 / 建议批次 |
| --- | --- | --- | --- | --- | --- |
| `线程/路由.概念命名治理.ixx` | 旧概念图待命名请求、语素名称入口 | 取得独占许可并经旧需求服务创建 / 读取概念命名需求 | 语素概念入口、概念结构发布、需求任务方法组合能力 | 8 类运行期请求没有“待命名复核 + 命名需求”完整强类型请求；新概念图结构服务没有活动版本 / 待命名读取 | 高；依赖 P0-C 和 #214 恢复后的专用 P0-B 命名批次，不得临时把令牌传入新服务 |
| `线程/上行桥.概念命名治理.ixx` | 旧概念图待命名请求、旧语素名称入口 | 不写四仓库；只提交线程邮箱消息 | 租约值式只读查询 + 有界队列 | 当前正式业务路由只有命令型 8 请求，没有待命名 / 名称入口值式查询面 | 中；先形成纯只读租约适配，再独立改接，不与命名写批次混写 |
| `线程/自我治理领域路由.ixx` | 旧需求 / 任务 / 方法 / 特征 / 状态 / 动态 / 因果权威读回，统计与显示材料 | 创建 / 选择任务方法、执行特征状态写入、记录动作动态 / 因果、任务结果 / 完成、需求结算；另写非权威统计缓存和日志 | 存在场景、状态动态、特征状态、需求任务方法和运行期业务操作 | 新 8 请求覆盖创建 / 选择 / 完成 / 独立结算的一部分，但没有旧固定根治理、动作执行桥、动作证据和结算后观察的等价单一组合入口 | 极高；作为单独 P0-B 治理批次，先冻结输入 / 输出 DTO 和失败语义；不得把全部领域与入口合成一个宽切片 |
| `线程/任务管理上行桥.ixx` | 旧任务承接 / 选择 / 结果 / 完成，旧方法动作归属 / 状态 / 动态证据 | 不写四仓库；只校验并投递治理消息 | 租约值式任务 / 方法 / 动态只读查询 | 当前新业务路由没有这些只读查询 DTO；不能借仓库访问器绕过服务 | 中；纯只读桥批次，先于旧写路径最终退出，禁止兜底调用旧服务 |

当前 `入口.cpp` 正常模式后段没有构造上述长期治理路由；它们是仍可编译、仍有自检覆盖的生产模块和兼容 ABI。这个事实不降低迁移要求：只要生产文件仍直接依赖传统服务，它们就阻止传统头删除；但不得把静态存在夸大成当前常驻治理线程已运行。

## 5. M3：概念活动逐字段所有权

### 5.1 新旧边界

```text
旧 概念图服务：
  节点仓库&、关系仓库&
  根登记组_
  生命周期状态组_
  专用关系登记组_
  生命周期关系登记组_
  活动快照_
  上一候选图_

新 概念图结构数据操作 / 业务服务：
  只经同域接线和四仓库复核四根角色
  只创建 / 读取 实例支持概念、概念上下位、概念定义材料 三类结构关系
  没有根登记、生命周期角色、生命周期关系登记、活动签名、活动版本或上一候选图侧表
```

因此新服务不是旧活动服务的等价替身；P0-C 不得把旧 `概念图服务.h` 接回 `装配.运行期业务.ixx`。

### 5.2 字段、入口、锁与唯一分类

| 概念活动字段 | 旧服务当前写入入口 | 旧服务当前读取入口 | 当前锁 | 唯一分类 | 确定输入、规范顺序与比较规则 | 段 9 / 新所有者候选 |
| --- | --- | --- | --- | --- | --- | --- |
| 四根登记 `根登记组_` | `登记概念根`；旧概念图初始化写 4 类别、稳定键和完整节点句柄 | `读取概念根`、`读取全部概念根`、`节点是否已登记概念根` | 写时 `图写锁_ + 根登记锁_`，读时 `根登记锁_` | 确定性重建 | 从版本化 `系统角色清单` 的概念存在 / 动态 / 关系 / 因果根取得稳定主键与完整句柄；按存在、动态、关系、因果固定顺序；逐项比较类别、稳定键、完整句柄和节点真实类型 | 新运行期系统角色 + 概念活动重建参与者；段 9 保存并互证，不另存对象地址 |
| 三生命周期状态角色 `生命周期状态组_` | `登记生命周期状态`；旧初始化创建值为 1 / 2 / 3 的抽象状态节点后登记 | `读取生命周期状态`、生命周期关系读回 | `生命周期状态锁_` | 私有权威 | 四仓库只证明状态节点和值；当前无稳定键 / 系统角色用途把某个同值抽象状态唯一指定为概念活跃 / 冷却 / 退役角色，不能靠扫描值 1 / 2 / 3 猜测 | P0-C 必须建立版本化三角色身份；段 9 必须保留完整身份和角色顺序 |
| 三类专用关系登记 `专用关系登记组_` | `绑定实例支持概念`、`绑定概念上下位`、`绑定概念定义材料`、候选图发布；失效 / 删除时移除登记 | 原始 / 活动关系正反向读取、候选验证、删除影响计算 | 普通写 `图写锁_`；候选发布还持 `活动图锁_` | 确定性重建 | 从关系仓库全部当前有效记录筛三种专用关系类型；按类型、完整源句柄、完整目标句柄、完整关系句柄排序；登记集合与仓库集合精确相等，重复或漏项追根因 | 新概念图结构数据操作 + 概念活动重建参与者；段 4 保存关系，段 9 保存 / 互证登记视图 |
| 生命周期关系登记 `生命周期关系登记组_` | `初始化活动概念生命周期`、候选发布、`转换概念生命周期`；安全删除移除 | `读取概念生命周期`、生命周期复核 / 转换、活动快照验证 | `活动图锁_ + 图写锁_`；阶段角色读取另持 `生命周期状态锁_` | 确定性重建，前提是三生命周期角色权威已给出 | 从当前有效 `概念生命周期` 关系筛选；每概念恰一，目标必须等于三角色之一；按完整概念句柄排序；关系编号 / 版本 / 类型 / 端点与仓库记录完整比较 | 段 4 保存历史关系；段 9 保存当前登记候选并与角色映射互证 |
| 活动根组 `活动快照_.根组` | `初始化活动图基础版本`，后继候选发布沿用旧根组 | `读取活动概念根组`、树投影 / 验证 | `活动图锁_` | 确定性重建 | 与系统角色四根集合和固定类别顺序精确相等 | 不独立成为权威；段 9 可编码规范视图用于互证 |
| 活动概念节点组 `活动快照_.概念节点组` | 基础版本加入四根；候选发布按映射加入；安全删除移除 | 活动节点 / 主动召回 / 树 / 命名 / 生命周期 / 删除查询 | `活动图锁_` | 确定性重建 | 由规范签名组概念键集合与当前生命周期关系源集合重建；两集合必须相等并包含四根；按完整节点句柄排序 | 新活动重建参与者形成规范集合，段 9 编码并与关系 / 签名互证 |
| 活动关系组 `活动快照_.关系组` | 基础版本纳入根支持；候选发布形成三类当前活动关系；失效 / 安全删除移除 | 活动正反向查询、树 / 候选 / 删除验证 | `活动图锁_`；写仓库时另持 `图写锁_` | 确定性重建 | 从当前有效三类专用关系重建；按关系类型、源完整句柄、目标完整句柄、关系完整句柄排序；逐字段与关系仓库精确比较 | 新概念图结构数据操作提供结构读回，活动重建参与者形成段 9 规范视图 |
| 活动签名组 `活动快照_.签名组` | 基础版写 4 个根签名；候选发布把规范候选签名绑定到概念 | `读取活动概念签名`、候选等价 / 上下位、命名、生命周期、删除和树验证 | `活动图锁_` | 私有权威 | 签名含版本、约束维度 / 角色、定义节点、I64 / VecI64 / VecU64 / 节点类型值；当前关系只保存句柄和类型，不能恢复候选形成时的完整原始值。按概念完整句柄排序，对每个签名调用同版本规范化并做字段级完整比较 | P0-C 必须建立稳定值式签名所有者；段 9 必须持久化完整规范签名，恢复重建结构后逐签名比较 |
| 活动版本 `活动快照_.活动版本` | 基础版本固定 1；候选发布 / 失效 / 删除按合同加一；生命周期转换不必然推进活动版本 | `读取活动图版本`，所有候选、临时指代、命名、生命周期和删除门禁 | `活动图锁_` | 私有权威 | 不能用最大关系版本、关系数量或时间戳推断；必须保存非零 U64，恢复后精确保留并与段 9 摘要 / 候选身份比较 | P0-C 新活动所有者；段 9 必需字段 |
| 上一候选图 `上一候选图_` | 候选发布赋值；直接关系变化、失效和删除时 reset | 无公开读取，只用于重复候选短路 | `活动图锁_` | 瞬态缓存 | 不参与机器事实恢复；恢复后为空，后续候选按当前仓库 + 签名 + 版本重新计算 | 永不进入段 9 |

### 5.3 段 9 最小权威与重建合同

当前事实支持的最小分层是：

```text
必须作为新运行期私有权威保存：
  版本化三生命周期状态角色身份
  每个活动概念的完整规范签名
  非零活动版本

从系统角色和四仓库确定性重建：
  四根登记
  专用关系登记
  生命周期关系登记及每概念当前阶段
  活动根组
  活动概念节点组
  活动关系组

始终丢弃：
  上一候选图
```

段 9 的规范比较必须使用完整句柄和完整记录，不得只比较编号、名称、哈希或容器数量。若 P0-C 设计选择把更多可重建视图也保存到段 9，它们只能作为互证材料，不能形成第二权威。

## 6. M4：生产唯一上下文切换面

| 顺序 | 当前函数 / 模块 | 缺失生产接口 | 串并行边界 | 非成功收口 | 可验证不增长门禁 |
| ---: | --- | --- | --- | --- | --- |
| 1 | `运行期上下文(域编号,仓库编号)`；当前只在自检 `make_shared` 构造 | 生产启动组合模块、域 / 仓库身份来源、候选生命周期持有点 | 必须先于所有生产消费者串行；候选内部构造顺序不可并行 | 参数 / 候选不完整为逻辑内返回；构造后内部引用混域为追根因 | 生产只出现一种上下文类型，不新建第二套协调器 / 仓库组 |
| 2 | `运行期上下文::初始化系统角色` | 正常入口形成版本 1 参数并取得完整结果的调用点 | 同一候选内串行；并发仅保留既有幂等自检，不用于生产启动 | 参数无效 / 幂等冲突为逻辑内返回；前置通过后结构与清单不一致追根因 | 四根和 21 个系统角色来自同一候选；旧初始化器不写新上下文 |
| 3 | `运行期上下文宿主::尝试首次发布全新上下文` | 生产唯一宿主持有点 | 系统角色完整后串行；生产只允许一个赢家 | 空 / 不完整 / 已有上下文为逻辑内返回；成功后宿主身份异常追根因 | 生产宿主和首次发布调用点各恰一；无通用替换 / 热更新入口 |
| 4 | `运行期上下文宿主::读取租约` | 正常入口持续持有并向消费者分发同身份租约的组合点 | 发布成功后可复制租约并行只读；不得缓存候选裸指针代替租约 | 发布前无租约为依赖门控；发布成功后租约无效或身份不一致追根因 | 所有生产租约读取地址等于宿主当前对象；旧对象地址命中为 0 |
| 5 | `运行期上下文::读取候选业务操作` + `线程/路由.运行期业务请求` | 四个兼容生产模块所需的只读查询、概念命名、固定根治理和动作证据强类型面 | 纯只读适配可并行；同一机器事实的旧写 / 新写切换必须逐消费者串行，禁止双写 | 请求 / 前置拒绝为逻辑内返回；内部提交 / 读回异常追根因，不回退旧服务 | 22 文件禁用能力继续 0；传统生产直接包含点只减不增；无旧服务兜底 |
| 6 | 当前无统一退出函数；旧对象是 `main` 局部生命周期 | 逐消费者归零证明、界面 / SQL 值式适配、旧初始化 / 自检隔离承载 | 必须等全部机器事实写路径改接后串行裁决；自检迁移可独立进行但不能影响生产所有权 | 尚有旧生产写入为依赖门控；已宣称退出但仍有调用为设计 / 逻辑异常 | 正常入口旧四仓库 / 传统服务构造为 0；生产线程传统头包含为 0；自检白名单显式隔离 |
| 7 | `仓库快照服务` 当前只有 `评估恢复请求`，无租约入口 | `权威冻结编排::形成九段冻结材料(运行期上下文租约)`、同域独占许可和参与者集合 | 旧对象退出、P0-C 完成后才可串行取得独占冻结；编码可在冻结复制完成后并行于锁外 | 无租约 / 参与者缺失为依赖门控；独占许可内材料矛盾为追根因 | 快照来源租约恰一；参与者集合完整相等；不临建协调器、不用空段 / SQL / 日志 |

允许并行的只有：有效同身份租约上的纯只读投影、互不重叠且已取得独占写入所有权的消费者准备工作，以及冻结完成后的不可变值式编码。首次发布、同一消费者改接、旧所有者退出和独占冻结必须串行。

## 7. M5：后继窄切片建议

以下是设计角色生成计划时的文件所有权建议，不是本执行角色生成的代码许可。新文件名是按当前 CODE-FILE-01 规则形成的候选；设计角色仍须逐文件复核模块名、工程顺序和实际签名。

### 7.1 P0-A：生产首次发布与租约持有

| 项目 | 建议 |
| --- | --- |
| 精确目标文件组 | 新 `海中鱼巣/启动.生产运行期.ixx`、新 `海中鱼巣/自检.生产运行期首发.ixx`、`海中鱼巣/入口.cpp`、`海中鱼巣.vcxproj`、`海中鱼巣.vcxproj.filters` |
| 只读依赖 | `启动.运行期上下文.ixx`、`装配.运行期业务.ixx`、`系统角色清单.数据.h`、`初始化.系统角色.ixx` |
| 前置 | #295 事实矩阵归档；生产域 / 仓库编号和版本 1 系统角色参数由设计固定 |
| 退出条件 | 正常入口真实构造完整候选、初始化角色、首次发布并持有同身份有效租约；不启动线程 / SQL / 控制面板消费者，不实现快照 |
| 写入所有权 | 只由 P0-A 拥有上述文件；若现有上下文签名足够，不修改 `启动.运行期上下文.ixx`；若不足，退回设计另建接口前置，不能临时扩入 |

### 7.2 P0-B：兼容生产消费者分批改接

P0-B 不应是一个“入口 + 全部领域”切片。建议至少拆成以下串行所有权批次：

| 批次 | 精确目标文件组候选 | 前置 / 退出条件 | 不重叠边界 |
| --- | --- | --- | --- |
| P0-B0 新公开合同一次扩展 | `领域/组合.运行期业务操作.ixx`、`线程/路由.运行期业务请求.ixx`、新 `自检.运行期兼容请求.ixx`、工程 / filters | P0-C 若请求依赖活动材料则先完成；一次性固定缺失的查询 / 命名 / 治理 DTO，22 文件禁用能力仍为 0 | 后继消费者批次只消费，不再各自修改共享顶层路由 |
| P0-B1 纯只读上行桥 | 新 `适配.运行期只读查询.ixx`、`线程/上行桥.概念命名治理.ixx`、`线程/任务管理上行桥.ixx`、新 `线程/自检.运行期只读上行桥.ixx`、工程 / filters | 有效租约和 B0 值式查询；退出时两桥不包含传统服务头、不持仓库、不写机器事实 | 不修改命名写路由和自我治理写路由 |
| P0-B2 概念命名写路径 | `线程/路由.概念命名治理.ixx`、新 `线程/自检.概念命名运行期改接.ixx`、工程 / filters | #214 正式恢复、P0-C 和 B0 完成；退出时不取得独占许可、不直接调用旧概念图 / 需求 / 语素服务 | 不修改 B1 两桥、旧概念图服务或新活动所有者内部 |
| P0-B3 自我治理写路径 | `线程/自我治理领域路由.ixx`、新 `线程/自检.自我治理运行期改接.ixx`、工程 / filters | B0 完成且动作执行 / 证据 / 结算强类型合同齐全；退出时全部权威写只走同一租约新业务面，统计 / 日志仍非权威 | 不修改 B1 / B2 文件，不把 O1 接生产 |
| P0-B4 SQL / 控制面板只读投影 | `领域/控制面板服务.h`、`界面/控制面板窗口.h`、`适配/SQL数据库适配.h`、新 `自检.运行期只读投影.ixx`、工程 / filters | P0-A 有效租约和 B1 只读材料；退出时 SQL / 显示只收值式投影，不持旧概念图 / 仓库可写引用 | 不修改业务服务或活动所有者 |
| P0-B5 旧入口所有者退出 | `海中鱼巣/入口.cpp` 以及只用于入口旧装配的最小自检迁移文件 | B1-B4 全部完成；正常模式旧四仓库 / 传统服务构造和旧写调用归零，旧夹具移入独立自检真模块 | 与 P0-A 对 `入口.cpp` 的写入所有权必须按提交串行移交，禁止并行施工；不借本批实现快照 |

`入口.cpp` 是 P0-A 与最终退出批次不可避免的串行接缝，不能由两个计划同时拥有。设计角色必须在索引中明确“P0-A 提交完成并释放写入所有权 -> P0-B5 取得唯一写入所有权”，而不是并行执行。

### 7.3 P0-C：概念活动唯一所有者 / 重建参与者

| 项目 | 建议 |
| --- | --- |
| 精确目标文件组 | 新稳定值类型 `海中鱼巣/领域/概念活动状态.数据.h`；新真模块 `海中鱼巣/领域/数据操作.概念活动.ixx`、`海中鱼巣/领域/服务.概念活动.ixx`；`海中鱼巣/装配.运行期业务.ixx`；新 `海中鱼巣/领域/自检.概念活动所有权.ixx`；工程 / filters |
| 只读依赖 | `数据操作.概念图结构.ixx`、`服务.概念图结构.ixx`、`系统角色清单.数据.h`、四仓库、概念图算法的规范签名规则 |
| 前置 | P0-A 上下文所有权接口稳定；本矩阵的三类字段分类由设计固定为版本化合同 |
| 退出条件 | 新上下文只有一个活动所有者；三生命周期角色、完整签名和活动版本有唯一值式承载；其余字段按确定性规则重建并精确互证；上一候选图不持久化 |
| 禁止 | 不 import / include 旧 `概念图服务.h` 作为新装配成员；不把旧入口侧表复制进新对象；不接 O1、统计、学习或快照编码 |

P0-C 与 P0-B 的概念命名批次存在接口依赖但不共享写入所有权：P0-C 先发布活动服务合同，P0-B2 只消费公开值式面。

### 7.4 PERSIST-S1-A：九段同冻结不可变材料

| 项目 | 建议 |
| --- | --- |
| 精确目标文件组候选 | 四仓库窄导出所有者：`核心/主信息仓库.h`、`核心/节点仓库.h`、`核心/关系仓库.h`、`核心/索引仓库.h`；侧表所有者：`领域/特征值服务.h`；上下文汇总：`启动.运行期上下文.ixx`；新稳定 DTO `核心/权威冻结材料.数据.h`；新真模块 `核心/冻结.权威状态.ixx`；新 `自检.权威状态冻结.ixx`；工程 / filters |
| 只读参与者 | 系统角色清单、P0-C 概念活动状态、全索引所有者声明、领域登记；O1 只显式声明无私有权威侧表，不接生产 |
| 前置 | P0-A/B/C 全部完成；正常生产旧对象已退出机器事实所有者；全部普通写入口已进入同一结构事务域 |
| 退出条件 | 从正式生产租约取得同域独占许可；按固定参与者顺序复制四仓库全状态 / 高水位、特征侧表、系统角色、索引所有者、领域登记和段 9；释放许可后材料不可变且可精确读回 |
| 禁止 | 不编码 / 写 HYSNAP01，不解析外部文件，不实现指定身份恢复，不临建协调器 / 仓库，不修改 O1 生产接线 |

若四仓库导出、特征值导出、概念活动导出或所有者声明不能在同一独占许可内参与，PERSIST-S1-A 保持依赖门控；不得让 #226 在编码器中补齐。

## 8. 非成功返回分流

| 场景 | 唯一分类 | 收口 |
| --- | --- | --- |
| P0-A/B/C 尚未形成、生产租约不存在、旧生产写仍未归零 | 依赖门控 | 不实施冻结 / 编码，不把目标接口写成当前事实 |
| 候选参数、系统角色参数、请求载荷、句柄、类型或业务前置不满足 | 逻辑内返回 | 写前拒绝，宿主 / 四仓库 / 参数不变化，不调用旧服务兜底 |
| 首次发布时已有当前上下文 | 逻辑内返回 | 保留当前上下文和租约，不替换、不热更新 |
| 纯只读桥无有效租约 | 依赖门控 / 启动拒绝 | 不退回旧局部对象，不输出伪造材料 |
| 前置通过后发布身份、事务提交、完整读回或新旧消费者唯一性不一致 | 追根因解决 | 停止依赖路径，不把日志 / SQL / 显示当事实修补 |
| 段 9 根 / 登记重建与仓库不相等，或签名 / 生命周期角色 / 活动版本缺失 | 追根因解决 | P0-C 不发布，PERSIST-S1-A 与 #226 继续门控 |
| 外部 SQL / 控制面板不可用 | 逻辑内适配返回 | 不改变机器事实，不影响租约和仓库状态 |

## 9. O1 边界

```text
O1 当前权威事实仍全部落四仓库，没有私有权威侧表。
用途观察数据操作 / 服务仍未导入运行期业务装配。
本轮只登记它未来复用段 2—5、段 8 显式声明“无私有权威状态”。
P0-A/B/C 和 PERSIST-S1-A 均不得借所有权迁移把 O1 接入生产、统计、学习、晋级或第十段。
```

## 10. A01—A18 汇总

| 验收项 | 结果 | 摘要 |
| --- | --- | --- |
| A01 | 通过 | Git、E131、#294 归档、#226 门控、#295 唯一候选和执行角色一致 |
| A02 | 通过 | 入口旧对象和宿主 / 租约构造及调用点逐项登记 |
| A03 | 通过 | 每个旧对象的启动、生产、自检、线程、界面、适配归属已区分 |
| A04 | 通过 | 17 / 51 / 35 / 32 复核为 17 / 55 / 49 / 40，新增 / 删除明细完整 |
| A05 | 通过 | 14 服务 + 7 组合器 + 1 路由共 22 文件、唯一业务出口和 8 请求事实明确 |
| A06 | 通过 | 两条兼容写路径与两条只读 / 消息桥路径明确区分；自检增量未当生产许可 |
| A07 | 通过 | 四根、生命周期、两类登记、活动根 / 节点 / 关系 / 签名 / 版本和上一候选逐字段登记 |
| A08 | 通过 | 每个段 9 字段有私有权威、确定性重建或瞬态缓存唯一分类 |
| A09 | 通过 | 可重建字段列出输入、规范顺序、完整比较和版本依赖 |
| A10 | 通过 | 旧概念服务与新结构数据操作 / 服务保持所有权分离，未强接旧服务 |
| A11 | 通过 | 生产首发、租约、消费者、旧对象退出和快照消费序列完整 |
| A12 | 通过 | 快照唯一调用点候选是正式生产租约后的权威冻结编排，前置闭合条件明确 |
| A13 | 通过 | P0-A/B/C 与 PERSIST-S1-A 文件组、依赖、退出条件和串行写入所有权明确 |
| A14 | 通过 | 入口、上下文、业务装配、消费者和概念活动未合并成一个宽切片 |
| A15 | 通过 | O1 只登记四仓库 / 未接装配事实，不扩大生产接线或第十段 |
| A16 | 通过 | 依赖门控、逻辑内返回和追根因解决唯一分流 |
| A17 | 通过 | 本矩阵与专属断点状态、风险和下一步一致 |
| A18 | 通过 | 只写两份允许文件；strict 与 diff 通过；未构建、未运行、未登记阶段 |

## 11. 完成声明六项证据

| 维度 | 本轮证据 |
| --- | --- |
| 输入入口 | #295 / DQ-187 / JY-390、E131、#294 两份证据、#271 兼容矩阵、恢复流程图 / 详细设计和当前代码 |
| 核心处理 | M0—M6；入口逐对象调用图、兼容基线漂移、22 公开面、概念活动字段分类、切换顺序和后继批次 |
| 结构承载 | 本事实矩阵和同名专属断点；代码、工程、上游、计划、索引、队列和中央项目记忆零修改 |
| 输出变化 | 形成可供设计角色生成 P0-A/B/C 与 PERSIST-S1-A 的当前事实依据；状态为执行完成待设计同步 |
| 调用路径 | `E131 设计派发 -> 执行角色重读 -> M0—M5 只读扫描 -> 两份记录 -> 验证 / 提交 / 推送 -> 同窗口设计角色复核` |
| 验证方式 | A01—A18、strict、`git diff --check`、允许文件范围、零构建、零运行、零阶段和 stash 零操作 |

## 12. 下一步边界

执行角色只提交并推送本矩阵和专属断点，不归档 #295、不修改索引 / 队列 / 窗口记录、不生成正式 P0 或 PERSIST-S1-A 计划、不修订 #226。

推送成功并重读正式状态后，当前物理窗口才切回设计角色。设计角色应先归档 #295，再以本矩阵为依据生成窄 P0 计划；P0 正式完成后才生成 PERSIST-S1-A，并在 A 正式完成后再次逐签名修订 #226 v0.2。
