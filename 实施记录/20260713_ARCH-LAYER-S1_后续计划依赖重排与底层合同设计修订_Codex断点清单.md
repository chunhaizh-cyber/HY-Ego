# ARCH-LAYER-S1 后续计划依赖重排与底层合同设计修订 Codex 断点清单

日期：2026-07-13

决策：JY-317

状态：设计修订完成 / 未改生产 C++ / 未构建 / 未运行程序

## 1. 输入入口

```text
实施记录/20260713_ARCH-LAYER-S0_仓库服务双路径与调用点事实复核_Codex断点清单.md
规范/仓库与服务分层事务边界规范.md
流程图/20260713_仓库底层与服务数据操作分层纠偏流程图_v0.1.md/.html
规范/详细设计/仓库底层与服务数据操作分层纠偏详细设计.md
计划/20260713_CORE-LAYER-S1_四仓库统一生命周期合同代码实施切片_v0.1.md
计划/20260713_SERVICE-DATA-S1_不透明结构写入会话与执行器代码实施切片_v0.1.md
当前活动计划与任务队列
```

## 2. 核心处理

依据 #262 真实签名裁决三个底层断点，并复核全部后继活动计划是否满足流程图、详细设计、允许文件和新分层边界。

```text
候选可见性
-> 第一轮不新增主信息 / 节点记录级未发布状态
-> 候选身份只封闭在独占写入会话的数据操作层
-> 确认只关闭撤销能力

关系 / 索引撤销
-> 第一轮不新增关系候选或索引候选
-> 会话保存本次精确关系句柄和索引键
-> 最后发布前失败按写集逆序撤销

普通删除许可
-> 新统一普通删除入口要求独占许可
-> 关系 / 索引旧共享删除保持兼容原义
-> #271 前不静默改义、不批量删除
```

## 3. 结构承载

已修订：分层规范、分层流程图 MD+HTML、分层详细设计、#263、#264、#266-#271 计划骨架、受影响旧计划、计划索引、任务队列、当前状态、决策记录和窗口交互记录。

## 4. 后续计划裁决

| 范围 | 裁决 | 当前权力 |
| --- | --- | --- |
| #263 | 按 JY-317 修订并恢复 | 当前唯一可执行 / 700 |
| #264 | 独占写入会话和精确写集 | 依赖 #263 / 710 |
| #265 | 专项流程图已存在 | 依赖 #264 / 720 |
| #266-#269 | 总设计只给服务族顺序 | 计划骨架保留 / 缺专项流程图与详细设计 / 不可执行 |
| #270 | 与 #248-#250 运行期装配重叠 | 等专项三件套和归并裁决 / 不可执行 |
| #271 | 缺精确兼容入口与上游正式产物 | 计划骨架保留 / 不可执行 |
| #214/#215 | 旧同令牌合同与 JY-316 冲突 | #214 用户暂停并需重设计；#215 传递门控 |
| #224/#225 | 生产模块取得许可合同与 JY-316 冲突 | 架构重设计门控 |
| #231-#235 | #232 共享令牌下传合同冲突 | 整链后置到 #271 后重设计 / 复核 |
| #248-#250 | 与 #270 重叠 | 修订、归并或替代门控 |
| #257-#260 | 性能链 | 继续等待 #271 |
| #261 | 只读显示投影 | 改为 #258 后独立分支，不等待条件 #259/#260 |

## 5. 输出变化

当前唯一可执行入口保持 `#263 / DQ-155`。执行窗口不得消费 `#266-#271`，不得恢复 `#214`，不得执行 `#224/#248/#257`。#263 完成后只能解锁 #264；后续服务族代码计划必须由设计计划窗口先补齐专项三件套。

## 6. 调用路径

```text
#262 真实矩阵
-> JY-317 底层默认裁决
-> #263 四仓库统一合同
-> #264 独占不透明写入会话
-> #265 存在场景垂直样例
-> 设计窗口补 #266-#270 专项三件套
-> 逐项重新登记
-> #271 兼容收口
-> #257/#258 性能与索引
-> #261 独立只读投影；#259/#260 继续按证据裁决
```

## 7. 失败收口

若 #263 实际接口无法同时满足“旧共享删除兼容不改义”和“新严格普通删除独占”，停止代码实施并退回设计。若独占写入会话仍不能证明中途候选及即时关系 / 索引对并发读者不可见，停止 #264 及后继，另立仓库级未发布状态或发布隔离专项，不在服务内部补兜底。

## 8. 验证边界

本轮只做规范、流程图、详细设计、计划和项目记忆治理。验证包含 Mermaid MD/HTML 同源、严格规范、`git diff --check` 和本切片文件范围复核；不运行 C++ 构建或程序，不登记 700，不声明任何仓库或服务代码已经实现。
