# CORE-SESSION-S6 完整主信息值式读取与强类型投影失败归因代码实施 Codex 断点清单

日期：2026-07-17

执行队列：#292

设计队列：DQ-184

设计裁决：JY-384

接管提交：`9213d82617b9b7ad609d146021d19bf1c5787aaf`

正式提交：本记录所在 `main` 提交

状态：执行完成待设计同步 / A01—A18 全部通过 / 阶段 761 已登记 / 880 未登记 / #290 未消费

## 1. 执行前复核

```text
main == origin/main == 9213d82617b9b7ad609d146021d19bf1c5787aaf。
工作树在执行接管前干净。
E124、#292 / DQ-184 / JY-384、计划生成断点和实际接口已重新读取。
#292 是唯一依赖就绪候选；#290 v0.3 依赖 #292、761、JY-385。
节点 / 索引批量读取只有结构写入会话调用；主信息仓库已有令牌内完整记录读取；761 未占用。
```

## 2. 实际修改

| 文件 | 修改 |
| --- | --- |
| `海中鱼巣/核心/节点仓库.h` | 增加节点记录组四态值式结果和 Debug 一次性资源失败入口。 |
| `海中鱼巣/核心/节点仓库.cpp` | 真实空只返回“已形成 + 空集合”；入口拒绝、资源失败和非法有效记录分别归因。 |
| `海中鱼巣/核心/索引仓库.h` | 增加主键绑定组四态值式结果和 Debug 一次性资源失败入口。 |
| `海中鱼巣/核心/索引仓库.cpp` | 批量复制失败不再折叠为空集合；正反绑定缺失、重复和额外项归为内部不一致。 |
| `海中鱼巣/核心/会话.结构写入.ixx` | 投影增加资源失败；新增完整主信息值式读取，核对候选槽位并标记候选及槽位写集已读回。 |
| `海中鱼巣/核心/自检.结构写入会话.ixx` | 新增 CORE-SESSION-S6 A01—A18 Debug 自检正文。 |
| `海中鱼巣/入口.cpp` | 只增加自检结果、阶段 761 一次登记、通过值和顶层具名门禁叶子。 |
| 本记录 | 写入执行事实、验证证据和设计回传边界。 |

未修改 `主信息仓库.h/.cpp`、`结果.结构写入.h`、关系仓库、工程、规范、流程图、计划、项目记忆或任何 O1 / 恢复文件；未新建代码模块。

## 3. 结构与失败语义

```text
节点 / 索引仓库：已形成 / 入口拒绝 / 资源失败 / 内部不一致互斥。
只有已形成状态可携带可消费集合；已形成 + 空集合才是真实零记录。
会话投影：底层资源失败保持资源失败并登记首次失败；会话门禁通过后的仓库入口拒绝映射为内部不一致。
完整主信息：返回完整主信息记录和值容器副本，不解释槽位业务合法性。
候选完整读回：逐项核对本会话 I64 写集后，同时标记主信息候选和对应槽位写集已读回。
读取失败：锁死后继写入；已有候选由既有 #291 撤销合同完整撤销。
```

Debug 故障入口全部位于 `HY_EGO_ENABLE_STRUCTURE_COMMIT_FAULT_SELF_TEST` 内；Release 无故障状态存储、无阶段 761 登记、无 CORE-SESSION-S6 输出。

## 4. A01—A18

| 编号 | 结果 | 证据 |
| --- | --- | --- |
| A01 | 通过 | E124、#292、唯一候选、clean 同步基线和 761 / 880 状态已复读。 |
| A02 | 通过 | 三件套和计划范围一致；执行未修改设计三件套。 |
| A03 | 通过 | 节点批量四态互斥，自检 A01—A04。 |
| A04 | 通过 | 索引批量四态互斥，自检 A01、A02、A05。 |
| A05 | 通过 | 仓库与会话真实空均为已形成 + 空集合，自检 A02—A03。 |
| A06 | 通过 | 节点 / 索引一次性资源失败分别保持资源失败，自检 A04—A06。 |
| A07 | 通过 | 反向绑定重复归为内部不一致并恢复，自检 A07；代码显式覆盖非法有效记录和门禁后入口拒绝。 |
| A08 | 通过 | 会话投影四态和失败锁存通过，自检 A04—A06。 |
| A09 | 通过 | 非已形成结果均不进入可消费分支，资源失败结果携带空集合。 |
| A10 | 通过 | 完整主信息读取可见 15 个非空测试槽，自检 A09。 |
| A11 | 通过 | 第 15 槽之后的额外非空值完整可见，自检 A10。 |
| A12 | 通过 | 无效句柄、跨线程、决定后、版本矛盾和资源失败唯一分流，自检 A13、A15。 |
| A13 | 通过 | 候选完整读回后可请求提交；写后读取失败完整撤销，自检 A11、A16。 |
| A14 | 通过 | 投影仍只允许第一笔写入前；完整读取允许候选写后、请求提交前，自检 A12、A14、A17。 |
| A15 | 通过 | 核心静态扫描无 O1、FNV、角色或业务容量常量；公开领域面未改变。 |
| A16 | 通过 | 759、760、770 及既有仓库 / 会话 / 关系 / 特征值事务随完整自检保持通过。 |
| A17 | 通过 | Debug / Release Rebuild、两配置完整自检、Release 隔离、Debug 20 / 20、strict、diff 和范围检查通过。 |
| A18 | 通过 | 只登记阶段 761；本记录状态为执行完成待设计同步，880 未登记，#290 未消费。 |

## 5. 验证

```text
Debug x64 Rebuild：通过，0 warning / 0 error。
Release x64 Rebuild：通过，0 warning / 0 error。
Debug --self-test-exit：CORE-SESSION-S6-A01—A18 全部通过，完整程序退出 0。
Release --self-test-exit：完整程序退出 0，无 CORE-SESSION-S6 输出。
Release 二进制隔离：无 CORE-SESSION-S6、阶段名称或三个 Debug 故障入口命中。
最终 Debug --self-test-exit：20 / 20，失败 0。
python .\tools\check_specs.py --strict：通过。
git diff --check：通过。
允许范围扫描：只命中七个允许代码文件和本专属实施记录。
核心业务隔离扫描：无 O1、FNV、用途观察、角色或计划列明的容量常量命中。
```

## 6. 完成声明六项证据

| 维度 | 证据 |
| --- | --- |
| 输入入口 | 会话内类型投影请求或完整主信息句柄。 |
| 核心处理 | 仓库四态批量复制、会话失败映射、正反互证和完整值容器读回。 |
| 结构承载 | 既有节点 / 主信息 / 索引仓库、会话首次失败锁存与 #291 撤销能力。 |
| 输出变化 | 强类型批量结果、四态会话投影、完整主信息值式结果和 Debug 阶段 761。 |
| 调用路径 | 数据操作 -> 执行器 -> 会话 -> 节点 / 主信息 / 索引仓库。 |
| 验证方式 | A01—A18、两配置 Rebuild / 完整自检、Release 隔离、Debug 20 / 20、strict 与范围扫描。 |

## 7. 回传与边界

本记录随允许代码精确提交并非强制推送后，当前物理窗口从 #292 执行角色切回设计角色。设计角色必须从已推送提交重新读取实际签名并形成 JY-385，才能归档 #292 和恢复 #290 v0.3。

本轮不证明 #290 已通过，不实现 O1-S2，不分配节点 15 / 关系 18，不登记 880，不声明恢复、统计、学习、晋级、代际或生产接线完成。
