# 需求树后续结构流程图确认 Codex 断点清单

日期：2026-07-08

## 1. 输入入口

```text
用户确认：《需求树后续结构代码逻辑流程图_v0.1》可以确认。
确认范围：仅限第 6 项“需求树后续结构流程”的代码逻辑流程图材料。
禁止扩大：不生成详细设计，不生成施工计划，不登记可执行队列，不修改 C++。
```

## 2. 核心处理

```text
1. 将 `流程图/20260708_需求树后续结构代码逻辑流程图_v0.1.md` 和同名 HTML 标记为已确认流程图材料。
2. 保留当前代码事实边界：需求服务当前只提供需求树后续结构材料读取、结构化更新请求拒绝、枚举目标准入材料可读和方法候选准入材料可读等保守入口。
3. 保留后续门禁边界：父子需求写入、阻塞权重、逻辑组织需求、方向签名、枚举目标值域、方法候选算法和结构化更新提交入口仍需后续详细设计或施工计划确认。
4. 保留服务依赖边界：需求服务不得直接依赖特征值服务；需要当前特征状态材料时必须经特征服务读取。
5. 保留失败收口边界：后续若进入写入施工计划，必须补读回验证、数量快照或事务 / 失效隔离证明；本确认不证明当前所有路径已有完整事务回滚。
```

## 3. 结构承载

```text
流程图承载：
- 流程图/20260708_需求树后续结构代码逻辑流程图_v0.1.md
- 流程图/20260708_需求树后续结构代码逻辑流程图_v0.1.html

项目记忆承载：
- 项目记忆/当前状态.md
- 项目记忆/决策记录.md
- 项目记忆/待确认问题.md
- 计划/计划索引.md

实施记录承载：
- 实施记录/20260708_需求树后续结构流程图确认_Codex断点清单.md
```

## 4. 输出变化

```text
第 6 项“需求树后续结构”代码逻辑流程图确认完成。
项目记忆同步为：
- 当前状态新增第 6 项流程图确认态。
- 决策记录新增本流程图确认决策。
- 待确认问题新增并关闭本流程图确认项。
- 计划索引新增本流程图确认状态行。
```

## 5. 调用路径

```text
用户确认
-> AGENTS.md / 计划索引 / 项目规则总纲 / 规则迁移清单
-> 需求树后续结构流程图生成记录和当前项目记忆
-> 本确认断点清单与项目记忆状态同步
```

## 6. 验证方式

```text
本切片为流程图确认和项目记忆同步切片。
计划验证：
- 确认 Markdown 和 HTML 两份流程图文件存在。
- 确认新增确认断点清单存在。
- 运行 git diff --check。
- 运行 tools/check_specs.py。

实际结果：
- Markdown、HTML 和本确认断点清单文件均存在。
- `git diff --check -- 实施记录/20260708_需求树后续结构流程图确认_Codex断点清单.md 项目记忆/当前状态.md 项目记忆/决策记录.md 项目记忆/待确认问题.md 计划/计划索引.md` 通过；仅出现工作区 LF 将被 Git 触碰时替换为 CRLF 的提示。
- `python tools/check_specs.py` 输出：海中鱼巣规范治理检查: 未发现问题。

未执行：
- 未修改 C++。
- 未运行 msbuild。
- 未运行默认入口。
- 未生成详细设计。
- 未生成施工计划。
- 未登记可执行队列。
```

## 7. 禁止宣称

```text
不得宣称完整需求树已实现。
不得宣称父子需求、阻塞权重、逻辑组织需求、方向签名、枚举目标值域、方法候选算法或结构化更新提交入口已实现。
不得宣称旧需求函数、旧需求主信息字段、SQL、控制面板、D455、体素或外设已迁移。
不得宣称本确认提供代码实施许可。
不得宣称已生成需求树后续结构详细设计、施工计划或可执行队列项。
```
