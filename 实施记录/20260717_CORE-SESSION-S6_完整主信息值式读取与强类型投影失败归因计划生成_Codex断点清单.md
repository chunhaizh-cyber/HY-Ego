# CORE-SESSION-S6 完整主信息值式读取与强类型投影失败归因计划生成 Codex 断点清单

日期：2026-07-17

执行队列：#292

设计队列：DQ-184

设计裁决：JY-384

窗口事件：WIN-0001-E123 / WIN-0001-E124

状态：设计完成待发布 / #292 待执行 / #290 v0.3 依赖门控 / 761、880 未登记 / 单窗口串行双角色

## 1. 接管事实

```text
main == origin/main == 87e94b72c171a39a2feb04421994a6353877d013。
工作树在设计接管前干净。
#290 第二次审计已由执行角色正式按失败收口并推送。
A01—A06、A08、A12—A16 通过；A07、A09—A11 不通过。
#290 未归档，880 未登记，O1-S2 代码计划未生成。
```

## 2. 精确缺口

```text
节点仓库指定类型读取和索引仓库全部绑定读取把复制异常折叠为空集合；会话可能误标“已形成”。
会话无完整主信息值式读取，不能证明值容器不存在第 15 槽之后的额外非空槽。
```

## 3. 三件套

```text
流程图/20260717_不透明结构写入会话完整主信息与强类型投影失败归因流程图_v0.1.md
流程图/20260717_不透明结构写入会话完整主信息与强类型投影失败归因流程图_v0.1.html
规范/详细设计/不透明结构写入会话完整主信息与强类型投影失败归因详细设计.md
计划/20260717_CORE-SESSION-S6_完整主信息值式读取与强类型投影失败归因代码实施切片_v0.1.md
```

范围一致：只补通用强类型读取和完整主信息值式副本；不解释 O1，不修改主信息仓库、工程或恢复链。

## 4. 路由

```text
#292 / DQ-184 / JY-384 为唯一依赖就绪候选。
#290 原编号和路径保留，升级 v0.3，依赖 #292、761 和后继 JY-385 逐签名复核。
E124 设计提交推送并重读后，当前物理窗口从设计角色切执行角色，只消费 #292。
执行完成或异常先提交推送，再切回同一物理窗口设计角色；不发送自发自收消息。
```

## 5. 模型路由

本切片首次改变核心批量读取返回类型和失败锁存语义，不属于成熟机械计划；当前单窗口执行角色继续使用高推理。出现禁止文件需求、真实签名冲突、计划外具名失败或逻辑矛盾时正式退回设计，不允许执行角色临时扩权。

## 6. 设计轮验证

```text
两份 Mermaid 源逐字同源。
python .\tools\check_specs.py --strict
git diff --check
C++、工程、阶段 761 / 880 零修改。
```

本设计轮不运行 msbuild，不运行程序，不读取或触碰 stash。

## 7. 完成边界

本记录只证明 #292 三件套和单窗口执行路由已经形成。不得声明 #292 已实施、761 已登记、#290 已通过、O1-S2 已实现、880 已登记或恢复 / 统计 / 学习 / 生产能力完成。
