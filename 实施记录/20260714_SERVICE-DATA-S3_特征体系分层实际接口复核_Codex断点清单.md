# SERVICE-DATA-S3 特征体系分层实际接口复核 Codex 断点清单

日期：2026-07-14

决策：JY-329

计划 / 队列 / 阶段：#267 / DQ-159 / 740

交互：WIN-0001-E30

状态：#274 正式接口逐签名复核通过 / #267 待执行 / 740 未实施 / 未改 C++ / 未构建 / 未运行

## 1. 输入入口

```text
提交 906d971：#274 / DQ-166 / 735 正式完成提交
计划/已完成计划/20260713_FEATURE-VALUE-TXN-S1_特征值原始材料事务适配代码实施切片_v0.1.md
实施记录/20260714_FEATURE-VALUE-TXN-S1_特征值原始材料事务适配代码实施_Codex断点清单.md
流程图/20260713_特征体系服务分层迁移代码逻辑流程图_v0.1.md/.html
规范/详细设计/特征体系服务分层迁移详细设计.md
计划/20260713_SERVICE-DATA-S3_特征体系服务分层迁移代码实施切片_v0.1.md
海中鱼巣/核心/会话.结构写入.ixx
海中鱼巣/核心/执行器.结构写入.ixx
海中鱼巣/领域/参与者.特征值原始材料.ixx
海中鱼巣/领域/特征服务.h
海中鱼巣/领域/特征值服务.h
海中鱼巣/领域/二次特征服务.h
```

## 2. 前置状态

```text
#274 / DQ-166：已完成并归档。
阶段 735：已登记。
A01-A16：已通过。
Debug / Release x64：0 警告、0 错误。
Release：FEATURE-VALUE-TXN-S1 登记 0，执行器对象故障符号 0。
#267 五个计划模块：当前均不存在。
阶段 740：未登记。
```

## 3. 逐签名复核

| #267 需要的能力 | `906d971` 正式接口 | 结论 |
| --- | --- | --- |
| 单参与者独占事务 | `结构写入执行器::执行(业务回调, 参与者类型&)` | 一致；一次独占许可内完成准备、会话确认、参与者提交或撤销 |
| 提交准备候选读取 | `结构提交准备只读视图` 的五个只读入口 | 一致；只读本会话候选，不放宽普通会话决定后读取 |
| 初始 I64 / Vec 参与者 | `特征值原始材料事务参与者(特征值服务&)`、`登记初始材料(...)` | 一致；版本固定 1，节点和主信息必须属于本会话候选 |
| 侧表值式读取 | `特征值原始材料侧表访问器(特征值服务&)`、`读取(节点句柄)` | 一致；`try_lock_shared`，不访问四仓库，不返回内部引用 |
| 通用结构写集 | 会话创建主信息 / 节点候选、创建带顺序号关系、绑定主键、候选 I64 写入与完整读回 | 一致；可承载定义、槽位、当前值和二次特征有序关系 |
| 失败收口 | 执行器先完成参与者撤销，再完成会话撤销并释放许可 | 一致；#267 不需修改核心或参与者模块 |

附加边界复核：

```text
执行器模块不包含特征值领域类型。
参与者模块不包含四仓库、许可、线程、std::async 或工作队列依赖。
侧表访问器对 I64 只返回类型和版本；I64 值仍须由数据操作层在同一共享结构许可内从主信息读取。
参与者和访问器均不可默认构造、复制或移动；数据操作层按一次调用局部构造。
```

## 4. 业务结构冻结

当前兼容 `特征服务` 的特征宿主白名单为：

```text
存在、场景、需求、任务、方法
```

#267 第一轮二次特征组成项白名单保持：

```text
基础信息、存在、场景、特征、二次特征、因果引用
```

状态、动态和未列类型必须入口拒绝。两个白名单用途不同，不得混用、补默认或扩展为任意有效节点。

## 5. 五条路径可承载性

```text
创建特征定义：主信息候选 + 特征节点候选 + 主键。
创建实例槽位：宿主归属 + 定义模板 + 主键，关系由完整句柄和端点读回。
发布初始状态：特征值候选 + 槽位归属 + I64 槽位 0 或 Vec 侧表 + 版本 1 参与者。
同会话创建槽位和初始状态：一个参与者可登记本次特征值材料，全部结构读回后请求提交。
创建组合二次特征：二次特征候选 + 主键 + `0..n-1` 有序引用关系。
```

读取宿主特征状态可按“共享结构许可 -> 权威关系和主信息 -> 侧表 `try_lock_shared` -> 同许可组合值式材料”闭合，不需要修改 #274、既有三个兼容服务或四仓库。

## 6. 上游修订

本次同步修订：

```text
流程图/20260713_特征体系服务分层迁移代码逻辑流程图_v0.1.md/.html
规范/详细设计/特征体系服务分层迁移详细设计.md
计划/20260713_SERVICE-DATA-S3_特征体系服务分层迁移代码实施切片_v0.1.md
```

修订只把 #274 从假定前置改为已验证正式接口，修正正式实施记录日期路径，并冻结宿主 / 二次特征白名单；没有扩大 #267 允许代码文件或业务范围。

## 7. 输出变化

```text
#267 / DQ-159：前置满足待实际接口复核 -> JY-329 复核通过待执行。
阶段 740：仍未实施、未登记。
#274 / DQ-166、#272、#266：保持已完成，不重复执行。
#268-#271：继续原设计资料或归并门控。
#273：独立状态不变，本切片不消费。
#214、#257：继续暂停。
WIN-0001：#267 所有权在本次提交发布后转回执行窗口。
```

## 8. 执行门禁

执行窗口必须从本次正式提交重新读取实际接口。只有 HEAD 包含 `906d971`、五个计划模块仍不存在、阶段 740 未占用且签名未再次漂移时，才可按 #267 修改允许文件。

若需要修改核心、#274 参与者、既有 `特征服务.h`、`特征值服务.h`、`二次特征服务.h` 或其它禁止文件，必须保持代码零修改并以原 `#267 / DQ-159` 退回设计。

## 9. 验证边界

```text
tools/check_specs.py --strict：通过。
git diff --check：通过。
流程图 MD / HTML Mermaid 文本：同源。
#274 正式路径：存在。
#267 五个计划模块：均不存在。
阶段 740 / SERVICE-DATA-S3 代码登记：不存在。
C++、工程和 filters：相对 906d971 差异为 0。
#267 活动状态：JY-329 / 待执行 / 740 未实施。
```

本轮只执行只读代码复核和文档治理验证，不运行 C++ 构建或程序。不得据此声明 #267、A01-A20、740、生产调用迁移、兼容入口收口、完整特征系统或旧能力迁移已经完成。
