# 设计执行双窗口交互规则建立 Codex 断点清单

日期：2026-07-13

治理编号：JY-308

状态：规则已建立 / 当前交互 WIN-0001 已登记 / 未改 C++ / 未构建 / 未运行程序

## 1. 输入入口

```text
用户明确要求实现“执行计划”窗口与当前窗口交互运行。
JY-306：#214 执行窗口发现真实接口漂移并退回设计。
JY-307：设计窗口修订三件套并重新登记 #214。
Codex 当前任务：生成施工计划。
Codex 目标任务：执行计划。
```

## 2. 核心处理

```text
建立仓库权威状态和任务消息通知两层协议。
固定退回、修订发布、执行接管、完成回传状态机。
更新计划技能和执行技能，使后续交接自动执行同一规则。
建立项目记忆窗口交互记录，保证消息失败后仍可恢复。
```

## 3. 范围一致性

本项是纯协作规则治理，不生成业务流程图、业务详细设计或代码实施计划。流程图 / 详细设计关联不适用；替代依据为用户明确指令、AGENTS 窗口类型规则、JY-306 / JY-307 正式交接事实和现有计划 / 队列权力链。

## 4. 当前交接

```text
计划：#214 / DQ-106
设计修订提交：1d21723
状态：重新待执行
所有窗口：执行窗口
执行窗口动作：重读提交、三件套、队列和实际接口后实施或再次退回
设计窗口动作：等待回传，不消费队列
```

## 5. 验证

```powershell
git diff --check
python .\tools\check_specs.py
python C:\Users\zhchh\.codex\skills\.system\skill-creator\scripts\quick_validate.py .\.codex\skills\hai-zhong-yu-chao-plan
python C:\Users\zhchh\.codex\skills\.system\skill-creator\scripts\quick_validate.py .\.codex\skills\hai-zhong-yu-chao-execute
```

实际结果：`git diff --check` 和 `python .\tools\check_specs.py` 通过；两项 `quick_validate.py` 均因当前 Python 环境缺少 `PyYAML` 而未完成，错误为 `ModuleNotFoundError: No module named 'yaml'`。已人工复核两项技能沿用的 YAML frontmatter 分隔符、`name` 和 `description`，本次仅新增正文段落；不得据此宣称自动技能校验通过。

本次不运行 C++ 构建或程序。

## 6. 禁止扩大声明

本次只证明两个 Codex 窗口的治理交接机制已建立并登记当前 #214 交互。不得声明 #214、阶段 590、名称绑定、动作动态、通用执行或任何业务能力已实现。
