# 真实闭环串联优先目标计划生成 Codex 断点清单

日期：2026-07-09
状态：已生成 / 待确认 / 后置计划 / 未入队 / 未改 C++ / 未构建 / 未迁移旧函数

## 1. 输入入口

用户要求：

```text
当 运行宿主与多线程消息队列专项计划 完成后才能继续下一批优先目标应是把已完成的分散能力串成更真实的闭环：
需求 -> 任务承接 -> 方法候选/选择 -> 动作请求 -> 动态记录 -> 任务回执 -> 需求结算 -> 非权威缓存/事件日志/只读显示材料,请生成下一批优先目标计划
```

## 2. 核心处理

按计划技能和流程图技能执行：

```text
读取 AGENTS.md、计划索引、规则总纲、规则迁移清单、任务队列、待确认问题和当前状态。
读取运行宿主与多线程消息队列专项计划和详细设计。
读取闭环链路相关详细设计和验收用例。
生成真实闭环串联优先目标流程图、详细设计和待确认计划。
同步项目记忆和计划索引登记。
```

## 3. 结构承载

新增文件：

```text
流程图/20260709_真实闭环串联优先目标流程图_v0.1.md
流程图/20260709_真实闭环串联优先目标流程图_v0.1.html
规范/详细设计/真实闭环串联优先目标详细设计.md
计划/待确认计划/20260709_真实闭环串联优先目标计划_v0.1.md
实施记录/20260709_真实闭环串联优先目标计划生成_Codex断点清单.md
```

同步文件：

```text
计划/计划索引.md
项目记忆/Codex任务队列.md
项目记忆/待确认问题.md
项目记忆/当前状态.md
项目记忆/决策记录.md
```

## 4. 输出变化

```text
新增真实闭环串联优先目标流程图和 HTML。
新增真实闭环串联优先目标详细设计。
新增后置待确认计划。
登记 QR-130 待确认问题。
登记 DQ-47 设计计划队列项。
登记 JY-153 决策记录。
```

## 5. 调用路径

```text
用户提出后置优先目标
-> 读取规则、计划和项目记忆
-> 读取运行宿主专项和闭环链路依据
-> 生成流程图
-> 生成详细设计
-> 生成待确认计划
-> 同步项目记忆和计划索引
```

## 6. 验证方式

```powershell
git diff --check
if (Test-Path .\tools\check_specs.py) { python .\tools\check_specs.py }
```

流程图验证：

```text
Markdown 包含 mermaid fenced block。
HTML 包含 Mermaid CDN import。
```

## 7. 完成边界

本次只能声明：

```text
真实闭环串联优先目标计划已生成。
它是运行宿主与多线程消息队列专项完成后的后置待确认计划。
未改 C++，未运行构建，未入队，未迁移旧函数。
```

不得声明：

```text
真实闭环已实现。
运行宿主与多线程消息队列专项已完成。
自我循环、自我苏醒、旧能力迁移、控制面板 / SQL / D455 / 体素 / 外设接入已完成。
```
