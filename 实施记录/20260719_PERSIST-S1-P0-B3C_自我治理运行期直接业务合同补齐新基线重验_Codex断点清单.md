# PERSIST-S1-P0-B3C 自我治理运行期直接业务合同补齐新基线重验 Codex 断点清单

## 1. 任务身份

- 计划：`#310 / DQ-202 / PERSIST-S1-P0-B3C v0.3`
- 执行批次：`WT-310-R2`
- 分支：`codex/310-p0b3c-r2`
- 冻结代码父提交：`e1ec3193534e36b399e8d6a86816ee594412a1c5`
- 设计放行：`JY-483` 至 `JY-487`、`E228` 至 `E232`
- 完成状态：分支完成待独立集成

## 2. 实施范围

本轮从失效旧候选 `1d95aa5975057965fcfa179c56521ad69e5b5b6a..0890464414a8ffd5dfc71dcfc80641d9e4f18181` 机械提取三份代码差异，在冻结新基线逐行复核后落地；没有 cherry-pick、merge、rebase，也没有复用旧实施记录。

实际代码文件：

1. `海中鱼巣/领域/组合.运行期业务操作.ixx`
2. `海中鱼巣/装配.运行期业务.ixx`
3. `海中鱼巣/自检.运行期兼容请求.ixx`

代码差异为 314 行新增、4 行删除。三份当前文件 blob 与旧候选对应 blob 逐个一致：

- `组合.运行期业务操作.ixx`：`071acf98d5996cefd1833e2e17d0cd4438972379`
- `装配.运行期业务.ixx`：`2d8a8d2e7358a8f7f98dcc8a2337522a76357916`
- `自检.运行期兼容请求.ixx`：`dd320ee687f64f2bfb9d9b8fc0533abe615ce76c`

## 3. 实现结果

- `运行期业务操作组合器` 只新增需求、任务、方法、轻量因果四个长期服务引用。
- 新增 7 个写入口和 4 个读入口；写入口只做请求准入和现有服务唯一委托，读入口只返回值式材料。
- `运行期业务装配` 按既有成员构造顺序把四个服务传给业务操作组合器。
- `自检.运行期兼容请求` 保留 P0-B0 A01-A20，并新增 P0-B3C A01-A12。
- `运行期业务请求` / `运行期业务结果` 保持 14 / 11；Debug 最终回归阶段 910 保持唯一登记。
- 入口、工程、统一运行器、线程路由、variant 定义、领域服务实现、#305 和中央治理均未修改。

## 4. 构建与静态固定点

在 `JY-485 / E230` 授权下形成以下全新证据：

- Debug x64 Rebuild：退出 0，0 warning，0 error。
- Release x64 Rebuild：退出 0，0 warning，0 error。
- `py -3.14 tools/check_specs.py --strict`：通过。
- `git diff --check`：通过。
- 精确三代码文件范围、禁止依赖、11 个公开 ABI、variant 14 / 11、阶段 910 唯一登记：通过。
- 构建与静态固定点结束后产品进程数为 0，git index 为空。

## 5. E231 判定器异常

`JY-486 / E231` 首个 Debug 产品单轮自然退出 0，五组专项行数和失败数满足合同，四条指定叶子也实际输出 `: 通过`。机械判定器误把分隔符限定为 `=通过`，因此工具自身返回 1。执行窗口按门禁停止、释放共享目录，没有启动 Release 或连续轮次。

`JY-487 / E232` 已裁决该轮只作具名产品观察，不计入本轮 S3 或后续轮次。本次先对旧日志离线预检，修正后的判定器得到：

- SERVICE-DATA-S5：38 行，失败 0。
- TASK-RESULT-S1：18 行，失败 0。
- PERSIST-S1-P0-A：18 行，失败 0。
- PERSIST-S1-P0-B0：20 行，失败 0。
- PERSIST-S1-P0-B3C：12 行，失败 0。
- A33、A34、A14、A18 的 `: 通过` 各唯一命中。
- 产品进程数为 0。

## 6. E232 全新运行验证

证据目录：`C:\Users\zhchh\AppData\Local\Temp\WT-310-R2_E232_evidence`

### 6.1 Debug 单轮

- 程序退出 0，结束后产品进程数为 0。
- SERVICE-DATA-S5：38 行，失败 0，A33 / A34 各唯一 `: 通过`。
- TASK-RESULT-S1：18 行，失败 0，A14 / A18 各唯一 `: 通过`。
- PERSIST-S1-P0-A：18 行，失败 0。
- PERSIST-S1-P0-B0：20 行，失败 0。
- PERSIST-S1-P0-B3C：12 行，失败 0。

### 6.2 Release 单轮

- 程序退出 0，结束后产品进程数为 0。
- SERVICE-DATA-S5、TASK-RESULT-S1、PERSIST-S1-P0-A、PERSIST-S1-P0-B0、PERSIST-S1-P0-B3C 五组专项输出均为 0 行。

### 6.3 Debug 严格连续 20 轮

- 从第 1 轮全新开始，每轮独立自然结束，完整 stdout / stderr 先写入独立日志，轮间等待 2 秒。
- 20 轮全部退出 0，每轮结束后产品进程数均为 0。
- 每轮五组行数均为 `38 / 18 / 18 / 20 / 12`，失败数均为 0。
- 每轮 A33、A34、A14、A18 的 `: 通过` 均各唯一命中。
- 汇总结果：20 / 20 通过。

验证完成后已释放 `D:\海中鱼巣\日志\事件段\自检` 独占时隙，当前产品进程数为 0。

## 7. 完成边界

本任务只形成 #310 任务分支产物，状态为“分支完成待独立集成”。它不等于 #310 已归档，不修改中央计划、索引、队列或项目记忆，不解除 #305、#301 及其它后继门控，也不推送 `main`。
