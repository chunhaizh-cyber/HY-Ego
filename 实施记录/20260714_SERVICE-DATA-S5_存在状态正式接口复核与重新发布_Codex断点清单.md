# SERVICE-DATA-S5 存在状态正式接口复核与重新发布断点清单

日期：2026-07-14

状态：JY-335 已落实 / #269 / DQ-161 当前待执行 / 760 未实施 / 文档治理已完成 / 未改 C++ / 未构建 / 未运行

## 1. 输入入口

```text
正式基线：85db708
#277 / DQ-169 / CORE-SESSION-S4 / 757 正式归档产物
#278 / DQ-170 / SERVICE-DATA-S5-PRE / 758 正式归档产物
流程图/20260714_需求任务方法服务分层迁移代码逻辑流程图_v0.1.md/.html
规范/详细设计/需求任务方法服务分层迁移详细设计.md
计划/20260713_SERVICE-DATA-S5_需求任务方法服务分层迁移代码实施切片_v0.1.md
WIN-0001-E41、WIN-0001-E42
```

## 2. 逐签名复核

### 2.1 #277 关系能力

```text
失效已发布关系(关系句柄)
重挂已发布关系(关系句柄, 新源节点, 新目标节点)
挂载或重挂节点(节点句柄, 新父节点)
读取关系审计(关系句柄)
读取关系审计记录组(源节点, 关系类型)
```

上述入口继续由结构写入会话封闭原始令牌和仓库引用；关系版本、状态、端点、普通父子单父 / 防环和逆序撤销合同与 #269 一致。

### 2.2 #278 存在状态同会话能力

```text
节点是本会话候选(节点句柄)
形成已发布来源虚拟存在写入规格(...)
形成本会话候选来源虚拟存在写入规格(...)
写入虚拟存在_会话(会话, 规格, 来源)
形成抽象状态写入规格(...)
形成实例状态写入规格(...)
形成候选主体实例状态写入规格(...)
写入抽象状态_会话(会话, 规格)
写入实例状态_会话(会话, 规格)
写入候选主体实例状态_会话(会话, 规格, 主体)
```

三类规格构造权保持业务服务私有封闭。四个参与入口复用调用方同一会话，不取得第二许可、不调用执行器、不请求提交，也不向业务服务暴露原始令牌、仓库、候选或会话。

## 3. 复核结论

```text
1. #277、#278 正式接口与 #269 流程图、详细设计和计划既定合同一致。
2. #269 无需扩大七个新增模块及入口 / 工程的既定允许范围，无需修改核心事务 ABI、四仓库或 #278 既有模块。
3. #269 的 A01-A26、入口拒绝、失败收口、并发、Debug / Release 和连续 20/20 合同保持不变。
4. 七个 #269 新模块当前仍不存在，760 未登记；不得把本次文档复核宣称为代码实施。
5. #273 / DQ-165 已独立完成归档，但不属于 #269 允许范围，执行时不得纳入差异。
6. #214、#257 继续暂停；#270、#271 继续门控。
```

## 4. 输出变化

```text
#269 / DQ-161：等待设计复核 -> JY-335 当前待执行。
流程图：把 #278 未完成假定改为 #277/#278 正式接口已复核。
详细设计：固化 #278 实际签名和构造权 / 同会话边界。
实施计划：保持原编号、七个新增模块及入口 / 工程的既定允许范围、禁止范围、A01-A26 和 760，改为当前待执行。
计划索引、任务队列、当前状态、决策、验收和窗口交互记录：同步 JY-335。
```

## 5. 调用路径

```text
#277 / #278 正式产物
-> 设计窗口逐签名复核
-> 三件套范围一致性检查
-> JY-335
-> 原 #269 / DQ-161 重新发布
-> 执行窗口重读实际接口
-> 一致后实施 760；漂移则以原编号退回设计。
```

## 6. 验证方式与边界

```text
流程图 Markdown / HTML Mermaid 同源检查。
python .\tools\check_specs.py --strict。
git diff --check。
C++、工程和 filters 差异为 0。
七个 #269 新模块不存在，760 未登记。
#214、#257 仍暂停；#273 已独立完成；#270、#271 继续门控。
```

本轮不运行构建、程序或自检，不证明需求、任务、方法、召回、实际结果、结算、生产调用迁移、真实方法执行、动作动态、线程回执或旧能力迁移已经完成。
