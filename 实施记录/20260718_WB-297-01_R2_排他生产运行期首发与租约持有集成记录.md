# WB-297-01 R2 排他生产运行期首发与租约持有集成记录

日期：2026-07-18

执行队列：#297

设计队列：DQ-189

计划版本：v0.2

集成批次：INT-WB-297-01-R2

状态：已集成待设计同步

## 1. 身份与输入核对

```text
集成路径：C:\Users\zhchh\.codex\worktrees\integration-wb-297-01-r2\海中鱼巢
集成分支：codex/integration-wb-297-01-r2
集成起点：206e4d30ffaa99f2dadbf0cfd0195cf235ce9d2e
任务分支：origin/codex/297-p0a-r1
任务提交：997308378caad88b91f33915cb2618b0598d413f
任务父提交：ac90efeae0407ab42ee9020ca9659cfdd9c68459
任务集成提交：8c8782131bd6706211db16bf91d0cc1d9ca3f01f
```

R2 创建前，新路径、本地分支和远端分支均不存在。R2 从正式
`origin/main@206e4d30ffaa99f2dadbf0cfd0195cf235ce9d2e` 创建，创建后
HEAD、分支、worktree 和 index 与登记身份一致。旧
`INT-WB-297-01@ae0daa1d4fae8713f80ab14d155a2855dc32a6f0` 保持只读失效，
未 reset、delete、切基线、覆盖或复用。

任务远端精确指向 `9973083`；该提交只有父提交 `ac90efe`，父后提交数为 1，
差异精确为登记的七文件。cherry-pick 无冲突；集成后七份任务文件与原任务提交
的树内容差异为 0，没有由集成角色修改业务语义。

## 2. 集成范围

集成只包含：

1. `8c87821` 汇集的 #297 七文件任务差异。
2. 本专属 R2 集成记录。

未修改计划、计划索引、任务队列、并行工作树登记表、窗口交互记录、流程图、
详细设计或中央治理。未消费 #298。

## 3. 完整验证

在 R2 独立集成 worktree 从头重建并运行：

```text
Debug x64 Rebuild：0 警告 / 0 错误，66.4 秒
Release x64 Rebuild：0 警告 / 0 错误，63.3 秒
Debug 默认完整自检：退出 0
Debug 显式生产首发：退出 0
Release 默认完整自检：退出 0
Release 显式生产首发：退出 0
四条冲突命令：database / warehouse / D455 / headless 均退出 2
Debug 默认完整自检最终连续组：20 / 20，354.486 秒
Debug 显式生产首发最终连续组：20 / 20，0.793 秒
PERSIST-S1-P0-A A01-A18：18 / 18 通过
python tools/check_specs.py --strict：通过
git diff --check：通过
```

具名与静态门禁：

```text
阶段 900 源码登记命中：1
生产运行期早期分流：入口第 463 行
首个旧主信息仓库构造：入口第 480 行
集成起点到任务集成提交差异：7 文件
任务七文件与原任务提交树差异：0
禁止文件差异：0
Release 二进制 PERSIST-S1-P0-A / A01 / A18 专项文本命中：0
```

验证期间一版 PowerShell 包装器因把参数数组命名为保留自动变量而无参数启动 Debug
程序，进入控制面板消息循环。该错误包装器及残留子进程已终止，不作为产品验证结果；
随后全部程序命令改为显式参数直接调用，并重新形成上列单轮和连续验证证据。
另一次具名输出包装器把 18 个 A 项误设为至少 19 项，因此包装器返回 1；程序本身
退出 0，输出精确为 A01-A18 共 18 项且全部通过。本记录只采用正式 18 项口径。

## 4. 发布与完成边界

提交本记录后，集成角色必须再次 fetch，确认 `origin/main` 仍为集成起点且
WT-MAIN clean；随后只允许让 WT-MAIN `main` 非强制快进到 R2 集成提交并推送
`origin/main`。禁止 force、语义性消冲突或修改中央治理。

本批进入主线后状态仅为“已集成待设计同步”。#297 正式完成、计划归档、中央治理
同步和 #298 解锁仍由后继设计角色根据正式提交裁决。本批只证明显式新运行期早期
首发、唯一生产会话和同身份租约已经进入主线；不证明消费者改接、默认入口切换、
常驻新运行期、线程 / SQL / 控制面板、快照或恢复完成。
