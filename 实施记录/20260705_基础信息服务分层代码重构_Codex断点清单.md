# 基础信息服务分层代码重构 Codex断点清单

日期：2026-07-05

## 0. 恢复摘要

当前切片：执行用户确认的 `基础信息服务分层代码重构`。

依据：

```text
AGENTS.md
计划/计划索引.md
规范/000_项目规则总纲.md
规范/001_规则迁移清单.md
规范/基础信息服务分层规范.md
规范/详细设计/存在服务详细设计.md
规范/详细设计/场景服务详细设计.md
规范/详细设计/状态服务详细设计.md
规范/详细设计/动态服务详细设计.md
规范/详细设计/二次特征服务详细设计.md
规范/详细设计/世界聚合服务详细设计.md
```

当前判断：

```text
本轮只做第一轮代码重构：拆出存在、场景、状态、动态、二次特征原子服务，并让世界服务编排原子服务。
当前入口仍需要基础信息通用节点作为语素样例对应信息，服务清单中无独立基础信息通用服务；本轮保留世界服务 创建基础信息 兼容壳并标记为后续缺口。
不进入旧项目能力迁移，不实现完整抽象状态 / 抽象动态升格系统。
```

## 1. 已完成读取

```text
已读取 AGENTS.md、计划索引、项目规则总纲、规则迁移清单。
已读取基础信息服务分层规范和 6 份详细设计。
已读取现有 世界服务、特征服务、因果服务、需求服务、任务服务、方法服务、语素服务、特征值服务、节点仓库、关系仓库、主信息仓库和入口.cpp。
```

## 2. 已执行

```text
新增存在服务、场景服务、状态服务、动态服务、二次特征服务头文件。
改造世界服务为存在 / 场景聚合编排。
让任务 / 需求 / 方法授权状态写入经状态服务，动态写入经动态服务。
因果服务不再直接创建动态，只以来源动态创建因果引用。
语素服务拒绝绑定场景内实例状态和实例动态。
更新入口.cpp 验收输出。
更新计划索引、规范状态和项目记忆。
运行 git diff --check、Debug x64 构建和控制台运行。
```

## 3. 验证证据

```text
git diff --check：通过，仅有 CRLF 提示。
msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m：通过，0 警告，0 错误。
.\x64\Debug\海中鱼巣.exe：退出码 0。
控制台输出：基础信息服务分层: 第一轮通过。
MC-01 到 MC-11 均输出通过。
MC-09 输出：实例状态语素拒绝=是，实例动态语素拒绝=是，拒绝前后节点 / 关系 / 索引数量一致。
```

## 4. 剩余缺口

```text
世界服务::创建基础信息() 仍为基础信息通用节点兼容壳。
抽象状态和抽象动态当前只有最小创建入口，未实现候选、统计、升格、衰减和淘汰。
高级信息服务仍通过函数参数接收基础信息服务能力，尚未形成完整依赖注入治理。
完整日志系统代码入口仍是后续待确认切片。
```

## 5. 禁止宣称

```text
不得宣称基础信息服务分层已完整完成。
不得宣称抽象状态 / 抽象动态系统完成。
不得宣称旧项目基础信息或世界树能力已迁移。
不得宣称自我循环、自我苏醒或外设接入完成。
```
