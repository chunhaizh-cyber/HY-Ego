# 动作入口详细设计小修确认 Codex 断点清单

日期：2026-07-06
状态：已完成 / 文档小修后确认 / 未改代码

## 1. 输入入口

用户确认结论：

```text
动作入口详细设计方向通过。
正式确认前需补齐来源动作指向、动作入口角色身份、稳定动作键承载、入口状态承载方式。
补齐后可作为第一轮动作入口代码实施依据。
```

## 2. 本次范围

允许：

```text
修订动作入口规范和动作入口详细设计。
同步计划索引和项目记忆状态。
补充断点和验证记录。
```

禁止：

```text
修改代码。
新增动作入口节点类型。
迁移旧动作句柄字段。
把函数名或动作文本写入主信息字段。
实现动作动态来源新逻辑。
声明动作入口代码已实现或动作动态系统已完成。
```

## 3. 待修订要点

```text
P0-1：确认第一轮 `来源动作` 指向动作入口角色节点。
P0-2：确认动作入口角色节点使用 `节点类型::方法`，必须带动作入口角色状态，禁止方法节点自引用冒充动作入口。
P0-3：确认稳定动作键第一轮作为动作入口索引键，不写显示文本、函数名或主信息裸字段。
P0-4：确认动作入口状态第一轮使用抽象状态，不携带发生时间戳。
P1-1：补被动状态提交入口和被动整理入口不得进入普通候选召回。
P1-2：补动作动态和轻量因果引用边界。
```

## 4. 验证方式

```powershell
rg -n "来源动作|动作入口角色节点|稳定动作键|动作入口索引键|动作入口状态|被动状态提交|轻量因果" 规范 计划 项目记忆 实施记录
rg -n "动作入口.*待确认|来源动作.*方法节点.*动作入口角色节点.*独立动作入口节点" 规范 计划 项目记忆 实施记录 --glob "!实施记录/20260706_动作入口详细设计小修确认_Codex断点清单.md" --glob "!实施记录/20260705_主信息相关规范候选提取待确认清单.md"
git diff --check
```

本切片不改代码，不运行构建和默认入口。

## 5. 实际修订

```text
已确认第一轮 `来源动作` 指向动作入口角色节点。
已确认动作入口角色节点使用 `节点类型::方法`，必须带动作入口角色抽象状态。
已禁止方法节点自引用冒充动作入口角色节点。
已确认稳定动作键作为 `uint64` 动作入口索引键，不写显示文本、函数名、动作文本、日志标签或主信息裸字段。
已确认动作入口状态使用抽象状态，不携带发生时间戳。
已补被动状态提交入口和被动整理入口不得进入普通候选召回。
已补动作动态负责动作证据，轻量因果引用只以来源动态作为准入证据。
```

## 6. 验证结果

```text
文档一致性检索已执行。
git diff --check 已执行。
本切片未修改代码，未运行构建，未运行默认入口。
```

## 7. 下一步

```text
按计划索引继续进入任务系统第一轮代码实施。
非权威缓存与统计后续已小修确认，需求结算记录仍待确认。
```
