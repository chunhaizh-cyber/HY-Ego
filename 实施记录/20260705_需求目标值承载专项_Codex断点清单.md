# 需求目标值承载专项 Codex 断点清单

日期：2026-07-05

## 1. 当前入口

计划索引当前下一步：

```text
建立需求目标值承载专项：解决需求服务直接使用需求节点主信息值容器承载 I64 目标值的问题。
随后补函数功能说明清单或生成机制。
完成前不得进入 S1 主信息迁移收口。
```

## 2. 本切片范围

允许：

```text
只读扫描需求服务、状态服务、主信息系统详细设计、最小闭环设计和验收项。
建立需求目标值承载专项详细设计。
建立需求目标值承载专项计划。
更新计划索引和项目记忆。
```

禁止：

```text
修改需求服务代码。
迁移旧项目主信息字段或旧函数体。
把需求目标值临时改写到特征值节点或需求节点主信息值容器。
声明需求目标值承载问题已修复。
声明主信息迁移完成或旧项目能力已迁移。
```

## 3. 当前事实

当前 `需求服务` 事实：

```text
创建I64目标需求(std::int64_t 目标值) 直接把目标值写入需求节点主信息值容器。
创建I64目标需求(目标存在, 目标特征, 特征值服务, 当前特征值, 目标值) 检查存在、特征和当前值后仍调用上述函数。
结算I64目标 从需求节点主信息读取目标值，再读取特征值服务当前值。
```

当前规范事实：

```text
MI-V1 值容器语义只保留在特征值服务范围内，不扩展成需求 / 任务 / 方法通用值容器。
特征值节点不得直接保存需求目标值。
需求服务必须通过存在、特征、特征值和状态等基础服务获得能力。
```

## 4. 本切片结论

已完成：

```text
新增 `规范/详细设计/需求目标值承载详细设计.md`。
新增 `计划/20260705_需求目标值承载专项_v0.1.md`。
更新 `规范/详细设计/领域服务最小入口详细设计.md`，将旧需求目标值主信息值容器路径标记为 S0 缺口 2。
更新计划索引和项目记忆。
```

专项草案口径：

```text
第一轮只支持 I64 下限目标：当前值 >= 目标值。
需求目标值由状态服务创建的目标状态节点承载。
需求节点通过 `模板` 关系引用目标状态节点。
需求节点通过 `引用` 关系引用目标存在、目标特征和当前特征值。
特征值节点仍只保存特征系统当前值，不保存需求目标值。
```

## 5. 验证

```text
本切片未改代码，未运行构建。
git diff --check：通过；仅有 LF/CRLF 工作区提示。
文档一致性检索：`需求目标值承载`、`创建I64下限目标需求`、`结算I64下限目标`、`S0 缺口 2` 和确认前禁止修改 `需求服务.h` 已落入规范、计划、项目记忆和实施记录。
```

## 6. 当前停止点

```text
等待确认 `规范/详细设计/需求目标值承载详细设计.md` 和 `计划/20260705_需求目标值承载专项_v0.1.md`。
确认前不得修改 `海中鱼巣/领域/需求服务.h`。
```

## 7. 禁止扩大声明

```text
不能声明需求目标值承载问题已修复。
不能声明主信息迁移完成。
不能声明旧项目需求树或需求系统已迁移。
```
