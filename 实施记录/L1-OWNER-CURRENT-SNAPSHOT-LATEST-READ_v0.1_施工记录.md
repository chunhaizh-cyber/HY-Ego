# L1 所有者一致投影零守卫原子最新读取施工记录

日期：2026-08-14

计划身份：`L1-OWNER-CURRENT-SNAPSHOT-LATEST-READ v0.1`

计划 blob：`b440c97ffa2434df2aa035ca7fdcba1b4bdbbee5`

详细设计 blob：`e4584a5d6abf09c9568a6d042a0566be65fd861a`

## 1. S0 与所有权

- 设计提交 `d574f3646ae1b4db3b5702b65cbb485b5e659afd` 已发布；计划索引提交 `48e712686c3ce9dc3ca707efdb671622d789a356` 把本计划登记为 `可执行`。
- 施工起点为 `main == origin/main == 48e712686c3ce9dc3ca707efdb671622d789a356`，ahead / behind 为 `0 / 0`，Git index 空。
- 目标生产函数仍在 `海中鱼巣/核心/仓库.L1事实基座.ixx` 拒绝零守卫；公开 DTO、服务转发与合同版本已经存在且无需修改。
- 目标生产文件、三条专项路径和两条记录路径均无其它活动写入所有者；开工时无 `msbuild`、`cl`、`link` 或目标程序进程。
- 调用图谱、路线、`.codex-build`、C1 v0.2 文档候选和其它异主 WIP 全程保留，不属于本计划施工或发布范围。

## 2. 实际施工范围

永久生产文件：

1. `海中鱼巣/核心/仓库.L1事实基座.ixx`

生产工程外临时专项：

2. `验证工具/L1所有者一致当前零守卫参数验证.cpp`
3. `验证工具/L1所有者一致当前零守卫参数验证.vcxproj`
4. `验证工具/L1所有者一致当前零守卫参数验证.vcxproj.filters`

永久记录：

5. `实施记录/L1-OWNER-CURRENT-SNAPSHOT-LATEST-READ_v0.1_施工记录.md`
6. `验证记录/L1-OWNER-CURRENT-SNAPSHOT-LATEST-READ_v0.1_验证记录.md`

未修改公共 DTO、服务转发、合同版本、L2、BIZ、普通应用、主工程、主工程 filters、启动、规范、详细设计、计划或计划索引。

## 3. 已实现内容

- 从 owner 一致当前投影的入口校验中只移除 `期望事实代次 == 0` 拒绝条件；错合同、七组全空和非法选择项校验仍保留。
- 取得既有 `std::shared_lock<std::shared_mutex>` 之后，仅当请求守卫非零时比较当前事实代次；非零不匹配仍返回事实代次漂移和当前代次。
- 零守卫在同一共享锁持有期间直接使用 `状态_.事实代次` 形成结果，并在该锁域完成 owner、节点、关系、值、属性值、源关系组和目标关系组投影。
- 结果的 `期望事实代次` 继续原样回显请求；`读取事实代次` 继续回显锁内当前代次。空库可以为 0，非空已发布态为非零。
- 没有新增缓存、第二读取入口、两次调用、循环重试、sleep、yield、锁种类、公开函数、枚举、DTO、friend、故障注入或验证分支。

## 4. 施工中发现并修复的计划内问题

专项首次 Debug 编译在比较 `vector<L1所有者范围一致值读取结果项>` 时触发已删除的默认 `operator==`。原因是值材料分支不具备可用的默认全等，不是生产实现或合同错误。专项改为逐字段校验查询编码、项目状态、数量、截止和关系组形状；没有修改公共 DTO，也没有新增公开 `operator==`。修正后 Debug / Release 专项均通过。

## 5. 完成边界

本记录只证明计划白名单内的 L1 原子最新读取施工事实。结果提交仍须精确暂存、提交和普通推送；提交身份由 Git 与后继索引退出登记记录。

本叶不证明 C1、A1、阶段 18、方法登记根、阶段 19 或完整 DATA-L1—L5 已实现。三份专项只有施工期生命周期；结果被交互核验接受且计划索引退出后，必须由独立清理提交删除并重跑根工程双配置及静态门禁。正式生产代码中没有为了验证增加的额外代码。
