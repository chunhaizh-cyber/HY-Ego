# BASE-D4 需求树后续结构详细设计重审 Codex 断点清单

日期：2026-07-08
状态：已完成 / 文档治理 / 未改代码 / 未构建 / 未迁移旧函数

## 1. 输入入口

- 执行队列：`项目记忆/Codex任务队列.md` #133。
- 计划：`计划/20260708_BASE-D4_需求树后续结构详细设计重审计划_v0.1.md`。
- 重审对象：`规范/详细设计/需求树后续结构详细设计.md`。
- 依据流程图：`流程图/20260708_需求树后续结构代码逻辑流程图_v0.1.md`。
- 代码事实入口：`海中鱼巣/领域/需求服务.h`。

## 2. 核心处理

- 将旧“候选设计 / 需经服务矩阵和流程图迁移包重审”状态修订为“按第 6 项确认流程图重审完成 / 待用户确认”。
- 按当前 `需求服务` 已落入口重写当前代码事实、请求分流、入口拒绝、后续候选门禁、服务边界、代码差距和验收矩阵。
- 明确第 5 项需求创建与目标状态详细设计只作为上游依据，不自动放开需求树后续结构代码实施许可。
- 明确保留旧 FS-04 逻辑迁移包和旧候选设计为证据材料，不把旧函数作为实施单位。

## 3. 结构承载

- 当前只承载只读材料、局部枚举状态和局部 bool 可读性返回。
- 需求目标状态继续由需求节点到状态节点的 `关系类型::模板` 承载。
- 第一轮阻塞权重只作为请求材料中的局部 I64，不持久化为权威权重。
- 父子需求写入、逻辑组织需求、方向签名、枚举目标、方法候选和结构化更新提交均保留为后续候选门禁。

## 4. 输出变化

- 修订 `规范/详细设计/需求树后续结构详细设计.md`。
- 生成本实施记录。
- 同步计划索引、项目记忆和队列时，只声明 #133 文档治理完成，不生成代码施工计划、不登记 C++ 实施队列。

## 5. 调用路径

```text
执行窗口
-> 读取 #133 计划
-> 读取第 6 项确认流程图与当前需求服务事实
-> 重审并修订需求树后续结构详细设计
-> 生成重审断点清单
-> 同步计划索引和项目记忆
```

## 6. 验证方式

- `git diff --check -- 规范/详细设计/需求树后续结构详细设计.md 实施记录/20260708_BASE-D4_需求树后续结构详细设计重审_Codex断点清单.md 计划/计划索引.md 项目记忆/当前状态.md 项目记忆/决策记录.md 项目记忆/待确认问题.md 项目记忆/Codex任务队列.md`：待本切片收尾执行。
- `python .\tools\check_specs.py`：待本切片收尾执行。

## 7. 未执行内容

- 未修改 C++。
- 未运行构建。
- 未运行默认入口。
- 未生成代码施工计划。
- 未登记新的 C++ 实施队列。
- 未迁移旧需求函数体或旧需求主信息字段。
- 未接入控制面板、SQL、ADO、WebView、D455、体素或外设。
