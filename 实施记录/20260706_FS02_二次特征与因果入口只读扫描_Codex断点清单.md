# FS-02 二次特征与因果入口只读扫描 Codex 断点清单

日期：2026-07-06
状态：已记录断点 / 文档切片 / 未改代码 / 未迁移旧函数

## 1. 本切片输入

```text
FS-02 第四批状态与动态入口扫描已提交并推送。
计划索引 HR-001-FM-SCAN 要求继续 FS-02 旧项目二次特征类和因果类第一批函数只读扫描。
用户明确“不需要确定的步骤应该持续执行下去”。
```

## 2. 已完成动作

```text
1. 回读计划索引、逐函数只读扫描清单、上一批状态与动态扫描记录、函数功能说明文档和项目记忆状态。
2. 只读扫描 `D:\鱼巢\二次特征类.h`、`D:\鱼巢\二次特征类.cpp`、`D:\鱼巢\因果类.h`、`D:\鱼巢\因果类.cpp`。
3. 对照当前二次特征服务详细设计、动态服务详细设计、领域服务最小入口详细设计、领域服务授权写入规范和轻量因果口径。
4. 在编辑计划索引前创建 `计划/.计划索引.lock`。
5. 新增 `实施记录/20260706_FS02_二次特征与因果入口只读扫描记录.md`。
6. 登记 OF-094 到 OF-105。
7. 同步逐函数只读扫描清单、计划索引和项目记忆。
```

## 3. 本切片结论

```text
旧二次特征类同时承担二次特征身份、状态迁移来源、方向签名、值材料、概念刻度链、场景索引和观察关系写入。
新项目必须拆为二次特征服务、状态 / 动态证据、关系仓库组成关系、非权威缓存和后续特征 / 二次特征增强专项。

旧因果类同时承担因果身份、因果模板、证据计数、稳定度、被动 / 主动因果治理、任务投影、模板匹配和自然语言显示。
新项目当前只能承接轻量因果引用入口；因果模板、任务化、投影和显示必须进入后续专项，不得直接迁移旧函数体或旧主信息字段。
```

## 4. 下一步

```text
下一旧函数编号从 OF-106 开始。
继续按逐函数只读扫描清单推进，优先进入 FS-03 旧项目特征 / 特征值函数只读扫描。
旧二次特征值材料、标量 / 离散材料和差值材料转 FS-03。
旧主动因果任务化、方法结构缺口和学习缺口转需求树 / 任务管理 / 方法系统后续增强专项。
旧因果证据样本和模板积累转事件日志持久化恢复专项。
旧因果自然语言和候选说明转显示层只读视图专项。
```

## 5. 禁止宣称

```text
不得声明旧函数已扫描完成。
不得声明旧二次特征类或旧因果类已迁移。
不得声明旧二次特征主信息、旧因果主信息或旧因果模板主信息字段可迁移。
不得声明完整二次特征系统、因果系统、因果模板系统、任务投影系统或方法学习系统完成。
不得接控制面板 / SQL / D455 / 体素 / 外设。
```

## 6. 验证记录

```text
本切片只做文档和只读事实记录验证。
未改代码。
未运行构建。
未运行当前项目入口。
未运行旧项目程序。
```
