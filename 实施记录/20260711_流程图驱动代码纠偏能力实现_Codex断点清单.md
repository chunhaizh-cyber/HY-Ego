# 流程图驱动代码纠偏能力实现 Codex 断点清单

日期：2026-07-11

## 1. 输入入口

用户指令：

```text
这个路径用skill还是别的什么工具?要能够实现,根据代码画流程图,检查流程图,制定修订计划,将修订计划加入计划索引
好,请实现
这个能力要能同步到云端
```

## 2. 核心处理

本次把“根据流程图查问题并优化代码”的路径固定为可复用能力：

```text
新增专用 Codex skill，负责执行流程图驱动代码纠偏主流程。
新增机械检查脚本，负责检查现状流程图、逐行映射、输入契约、非成功返回二分、偏差清单、修订计划和计划登记。
修订现状流程图与施工流程图区分规范，把输入契约 / 调用语境表和二分审查表设为必要产物。
修订规则迁移清单，把该闭环登记为正式项目口径。
```

## 3. 结构承载

新增：

```text
.codex/skills/hai-zhong-yu-chao-flowchart-code-correction/SKILL.md
.codex/skills/hai-zhong-yu-chao-flowchart-code-correction/agents/openai.yaml
.codex/skills/hai-zhong-yu-chao-flowchart-code-correction/references/产物模板.md
tools/check_flowchart_correction.py
实施记录/20260711_流程图驱动代码纠偏能力实现_Codex断点清单.md
```

修订：

```text
规范/现状流程图与施工流程图区分规范.md
规范/001_规则迁移清单.md
```

未修改：

```text
海中鱼巣/**
计划/计划索引.md
项目记忆/Codex任务队列.md
```

## 4. 输出变化

```text
以后用户说“根据代码画流程图 / 检查流程图 / 根据流程图优化代码 / 生成修订计划 / 加入计划索引”时，可触发专用 skill。
以后完整纠偏产物必须包含输入契约 / 调用语境表；没有该表，不得把中途非成功返回定性为逻辑内返回。
以后可用 `tools/check_flowchart_correction.py` 对纠偏产物进行机械检查。
```

## 5. 调用路径

```text
用户请求流程图驱动代码纠偏
-> 触发 hai-zhong-yu-chao-flowchart-code-correction
-> 读取项目规则和当前代码事实
-> 生成现状流程图 / 映射表 / 契约表 / 二分审查表 / 偏差清单
-> 生成修订计划
-> 使用 tools/check_flowchart_correction.py 检查
-> 完整计划登记计划索引和 Codex 任务队列
```

## 6. 验证记录

已执行：

```powershell
py -3 .\tools\check_flowchart_correction.py --self-test
py -3 .\tools\check_specs.py
skill frontmatter check
git diff --check -- .codex/skills/hai-zhong-yu-chao-flowchart-code-correction/SKILL.md .codex/skills/hai-zhong-yu-chao-flowchart-code-correction/agents/openai.yaml .codex/skills/hai-zhong-yu-chao-flowchart-code-correction/references/产物模板.md tools/check_flowchart_correction.py 规范/现状流程图与施工流程图区分规范.md 规范/001_规则迁移清单.md 实施记录/20260711_流程图驱动代码纠偏能力实现_Codex断点清单.md
```

结果：

```text
tools/check_flowchart_correction.py --self-test 通过，输出“流程图驱动代码纠偏检查: 未发现问题”。
tools/check_specs.py 通过，输出“海中鱼巣规范治理检查: 未发现问题”。
skill frontmatter 轻量检查通过：name、description、frontmatter 边界和 TODO 清理均通过。
git diff --check 通过。
官方 quick_validate.py 因当前 py -3 环境缺少 yaml 模块无法运行；本次未安装外部依赖，改用轻量 frontmatter 检查覆盖必要结构校验。
```

## 7. 完成声明边界

可以声明：

```text
流程图驱动代码纠偏能力的 skill、检查脚本和规范口径已实现。
```

不得声明：

```text
既有流程图已全部按新口径重画。
既有代码已按新口径纠偏。
任何真实闭环、自我循环、自我苏醒或旧能力迁移已经完成。
```
