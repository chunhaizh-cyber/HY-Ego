# 语素系统第一轮实现记录

日期：2026-07-05

## 1. 输入入口

用户确认：

```text
确认语素系统规范和详细设计按当前清单执行第一轮实现。
```

执行依据：

```text
规范/语素系统规范.md
规范/详细设计/语素系统详细设计.md
实施记录/20260705_语素系统规范详细设计待确认清单.md
```

## 2. 核心处理

本次只实现语素系统第一轮范围：

```text
最小词单元准入
概念入口
入口绑定目标读取
入口概念追溯读取
语素入口同一判断
```

本次不实现：

```text
实例入口
别名关系
名称候选查询
类型候选查询
名称类型交集查询
查询冲突候选包
节点显示标题集合
SQL / 控制面板语素投影
运行节点入口直达查询
短语组合语义解析
完整自然语言理解
完整上位类型推导
```

## 3. 结构承载

新增或更新代码：

```text
海中鱼巣/核心/句柄.h
海中鱼巣/核心/关系仓库.h
海中鱼巣/核心/关系仓库.cpp
海中鱼巣/领域/语素服务.h
海中鱼巣/入口.cpp
```

新增关系类型：

```text
语素对应信息
语素概念追溯
```

新增只读关系入口：

```text
关系仓库::获取目标节点
```

新增语素服务入口：

```text
判断文本是否可作为最小词单元
创建概念入口
读取入口绑定目标
读取入口概念追溯
语素入口同一
```

## 4. 输出变化

当前语素系统可声明：

```text
已有基础信息、需求、任务或方法节点时，可创建语素入口。
可用第一轮准入状态拒绝空文本、含空白、含组合分隔符和超出第一轮长度的文本。
可创建概念入口，并写入语素入口到概念节点的追溯关系。
可读取语素入口绑定的对应信息节点。
可读取语素入口追溯的概念节点。
可判断同一语素入口句柄。
```

当前仍不得声明：

```text
语素系统完整实现
自然语言理解完成
别名 / 查询 / 显示 / 冲突包完成
SQL / 控制面板语素投影接入
旧项目语素类已移植
旧项目能力已迁移
```

## 5. 验证方式

默认构建：

```text
msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m
结果：0 警告，0 错误
```

容错宏构建：

```text
HY_EGO_ENABLE_FAULT_TOLERANCE_CHECK 启用后 Debug x64 Rebuild
结果：0 警告，0 错误
```

默认运行：

```text
.\x64\Debug\海中鱼巣.exe
语素第一轮: 是
领域服务可写: 是
最小闭环通过: 是
```

容错日志边界：

```text
默认运行后未生成 HY_Ego_fault_tolerance.log
```

## 6. 下一步

按计划进入：

```text
主信息相关规范候选提取
```
