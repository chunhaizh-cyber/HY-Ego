# CONCEPT-NAMING-S1 待命名治理协议与上行桥代码实施 Codex 断点清单

日期：2026-07-12

队列：#211

决策：JY-254

状态：已实施 / CONCEPT-NAMING-S1-A01 至 A12 通过 / Debug x64 已构建 / 完整自检与最终连续 20 轮通过 / 已归档计划 / 待随本切片提交推送

## 1. 执行前复核

1. #210、#197、#209 已完成；当前单项入口仍是 `概念图服务::读取待命名请求(概念, const 语素服务&)`，语素服务仍提供 `读取概念名称入口组`。
2. JY-251 已把生产桥固定为只读持有概念图服务、语素服务和治理邮箱；JY-253 已把生产桥、自检固定为两个 `功能.分类` 真模块。
3. 协议新增枚举数值 9 / 7 / 19 / 9 未被其它正式产物占用；治理邮箱提交签名、冻结行为和完整字段幂等规则未漂移。
4. 允许范围足以闭合协议版本 2、单请求上行桥、独立自检模块和入口最小调用；领域文件、队列、自我线程和任务上行桥均无需修改。
5. 旧草案路径未进入工程，计划、详细设计、CODE-FILE-01 与当前接口一致。

## 2. 实施结果

1. `自我治理消息协议.ixx` 升级为版本 2，追加概念待命名消息、来源角色、概念图服务角色和待命名只读材料类型，并增加目标概念与来源材料版本。
2. 消息形状、路由、句柄组相等、消息等价、完整字段幂等冲突和白名单均覆盖新增字段；既有八类消息继续允许来源材料版本为 0。
3. 新增生产真模块 `海中鱼巣.线程.上行桥.概念命名治理`，只读持有概念图服务、语素服务和邮箱，只处理调用方明确交付的单个待命名请求。
4. 当前请求存在时逐项复核概念、根类别和活动版本；不存在时通过语素名称入口反查区分“目标已有名称”和“当前请求不存在”。
5. 正常、漂移、过期、重复、冲突及内部不一致均返回值式结果；桥不遍历全部待命名请求，不创建需求、关系、任务、方法、动作或结算。
6. 新增自检真模块 `海中鱼巣.线程.自检.概念命名治理`，承载 A01-A12 场景、测试邮箱、碰撞材料、并发压力、逐项输出和总报告。
7. `入口.cpp` 只新增自检模块导入、最小上下文装配、一次调用和总结果合并；A01-A12 正文、并发循环和逐项输出均不在入口。
8. 两个模块均以 `ClCompile` 登记并通过匹配 `import` 使用；旧 `概念命名治理上行桥.ixx` 路径与工程项清零。

## 3. 验收矩阵

| 编号 | 结果 | 证据 |
| --- | --- | --- |
| CONCEPT-NAMING-S1-A01 | 通过 | 协议版本为 2，新增枚举数值固定，既有枚举数值不变。 |
| CONCEPT-NAMING-S1-A02 | 通过 | 目标概念和来源材料版本进入形状、相等与完整字段冲突复核。 |
| CONCEPT-NAMING-S1-A03 | 通过 | 概念待命名消息的来源、目标和材料白名单准确拒绝越位槽位。 |
| CONCEPT-NAMING-S1-A04 | 通过 | 既有八类消息在协议版本 2 下全部回归通过。 |
| CONCEPT-NAMING-S1-A05 | 通过 | 单个当前双参请求经概念图、语素和邮箱只读桥无损提交。 |
| CONCEPT-NAMING-S1-A06 | 通过 | 批量待命名读取不会触发桥提交，生产桥只接受单请求。 |
| CONCEPT-NAMING-S1-A07 | 通过 | 请求消失、已有名称、活动版本 / 根类别漂移和过期均不提交。 |
| CONCEPT-NAMING-S1-A08 | 通过 | 同请求重复收口，邮箱只保留一项。 |
| CONCEPT-NAMING-S1-A09 | 通过 | 摘要 / 幂等碰撞由完整字段拒绝，不覆盖既有消息。 |
| CONCEPT-NAMING-S1-A10 | 通过 | 并发同请求只有一次提交，其余为重复；不同概念请求不丢失。 |
| CONCEPT-NAMING-S1-A11 | 通过 | 邮箱冻结、自我线程、任务上行桥及既有消息回归通过。 |
| CONCEPT-NAMING-S1-A12 | 通过 | 两个真模块、旧路径清零、入口最小调用、工程登记和禁止能力扫描通过。 |

## 4. 验证结果

```text
msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m:1
最终结果：成功，0 警告，0 错误；用时 30.65 秒。

.\x64\Debug\海中鱼巣.exe --self-test-exit
最终结果：退出码 0；CONCEPT-NAMING-S1-A01 至 A12、完整程序回归和最小闭环通过。

最终连续复验
结果：20 / 20 轮退出码 0，总用时 288.325 秒；每轮摘要均为 7 个待命名请求、2 个并发提交、7 个重复。

静态边界
结果：领域文件 0 修改；生产模块不依赖自检模块；入口无 A01-A12 正文、并发循环或逐项输出；旧路径和旧模块名为 0；工程 XML 可解析。

git diff --check
结果：通过，仅有既有 LF / CRLF 工作树提示。

py -3.14 .\tools\check_specs.py
结果：未发现问题。
```

## 5. 完成声明六项

| 项 | 内容 |
| --- | --- |
| 输入入口 | 单个值式概念待命名请求、当前时间戳、截止时间戳和批次编号。 |
| 核心处理 | 双服务只读重读、当前性 / 名称状态复核、确定性消息构造、唯一邮箱幂等提交。 |
| 结构承载 | 协议版本 2 消息值、目标概念和来源材料版本；权威概念、语素及活动版本仍由现有领域结构承载。 |
| 输出变化 | 邮箱新增一条非权威概念待命名治理消息，或返回重复、漂移、已有名称、请求消失、过期、冲突等值式结果。 |
| 调用路径 | `单个待命名请求 -> 概念图 + 语素双参重读 -> 名称入口反查 -> 协议消息 -> 有界治理邮箱`。 |
| 验证方式 | Debug x64 0/0、A01-A12、完整自检、连续 20 轮、静态模块 / 入口 / 领域边界扫描、差异和规范检查。 |

## 6. 完成边界

本切片只证明待命名治理协议版本 2 和单请求上行桥已经实现并验证。它不证明命名需求、自动命名、目标状态、目标概念关系、名称绑定、任务结果、需求结算、自我循环或旧能力迁移完成。
