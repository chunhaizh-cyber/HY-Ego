# FS-02 基础信息模板入口只读扫描 Codex 断点清单

日期：2026-07-06
状态：已记录断点 / 文档切片 / 未改代码 / 未迁移旧函数

## 1. 本切片输入

```text
FS-02 第一批语素入口与基础信息桥接扫描已提交并推送。
计划索引 HR-001-FM-SCAN 要求继续 FS-02 旧项目基础信息类、存在 / 场景 / 状态 / 动态 / 二次特征 / 因果相关函数只读扫描。
```

## 2. 已完成动作

```text
1. 只读扫描 `D:\鱼巢\基础信息类.h`。
2. 只读扫描 `D:\鱼巢\基础信息类.cpp`。
3. 新增 `实施记录/20260706_FS02_基础信息模板入口只读扫描记录.md`。
4. 登记 OF-060 到 OF-066。
5. 同步逐函数只读扫描清单、计划索引和项目记忆。
```

## 3. 本切片结论

```text
旧 `基础信息类` 的语素模板入口混合了模板查找、默认根选择、自动创建基础信息节点和旧主信息字段构造。
新项目不得复制旧 `确保语素入口模板` 的自动创建逻辑；基础信息节点创建必须交给对应基础信息原子服务或世界聚合服务。
旧主信息字段扫描只能作为旧项目事实线索；新项目权威绑定应由语素对应信息关系、概念追溯关系和领域服务复判承载。
旧存在概念根兼容逻辑需要后续根概念 / 世界种子设计承接，不能用文本根名直接裁决权威根。
```

## 4. 下一步

```text
下一旧函数编号从 OF-067 开始。
继续 FS-02：旧存在类、场景类、状态类、动态类、二次特征类和因果类第一批函数只读扫描。
涉及特征值、动作动态、因果可靠性、体素和外设时，转 FS-03、FS-07 或 FS-X 分流。
```

## 5. 禁止宣称

```text
不得声明旧函数已扫描完成。
不得声明旧基础信息类已迁移。
不得声明旧基础信息树、旧主信息字段或旧存在概念根兼容逻辑可直接进入新项目。
不得声明基础信息服务分层、语素系统、世界聚合服务或存在根概念系统完整完成。
不得接控制面板 / SQL / D455 / 体素 / 外设。
```

## 6. 验证记录

```text
本切片只做文档和只读事实记录验证。
未改代码。
未运行构建。
未运行当前项目入口。
未运行旧项目程序。
```
