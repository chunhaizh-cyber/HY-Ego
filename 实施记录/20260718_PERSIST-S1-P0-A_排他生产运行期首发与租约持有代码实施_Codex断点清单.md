# PERSIST-S1-P0-A 排他生产运行期首发与租约持有代码实施 Codex 断点清单

日期：2026-07-18

执行队列：#297

设计队列：DQ-189

计划版本：v0.2

批次：WB-297-01 / WT-297-R1

冻结基线：`ac90efeae0407ab42ee9020ca9659cfdd9c68459`

正式派发：`origin/main@3c3db689f5ffa6ad8352ed7cacad9072b6919f7e`

状态：分支完成待集成 / S0-S6 完成

## 1. 接管与范围

1. 任务 worktree 为 `C:\Users\zhchh\.codex\worktrees\297-p0a-r1\海中鱼巢`，分支为 `codex/297-p0a-r1`；接管时 HEAD 等于冻结基线，worktree / index clean，upstream 为空。
2. 已从 `origin/main@3c3db68` 只读复核 E159 / E160、并行工作树登记、计划索引、任务队列和窗口交互；任务代码仍以 `ac90efe` 为冻结基线，没有把中央治理提交混入代码基线。
3. 本窗口按执行角色使用已登记的无提示 Full access，没有人工命令审批；只修改计划允许的三份新代码文件、入口、工程两文件和本记录。
4. 未修改计划、计划索引、任务队列、并行工作树登记、流程图、详细设计、中央项目记忆或其它工作树。

## 2. S0 实际接口复核

| 计划假定接口 | 冻结基线事实 | 结论 |
| --- | --- | --- |
| `运行期上下文(uint64_t 域编号, uint64_t 仓库编号 = 1)` | `启动.运行期上下文.ixx` 正式存在 | 一致 |
| `初始化系统角色(const 系统角色初始化参数&)` | 返回 `系统角色初始化结果` | 一致 |
| `初始化概念活动(const 概念活动初始化参数&)` | 返回 `概念活动初始化结果` | 一致 |
| `完整()` | 同时复核结构核心、服务装配、系统角色和概念活动 | 一致 |
| `尝试首次发布全新上下文(shared_ptr<运行期上下文>)` | 宿主只允许完整候选首次发布 | 一致 |
| `运行期上下文宿主::读取租约()` | 租约 `有效()` 且 `读取()` 返回当前上下文地址 | 一致 |
| 专项阶段登记所有权 | `入口.cpp` 拥有 Debug import、结果槽、登记与最终具名汇总 | 一致 |

S0 未发现实际接口漂移；`海中鱼巣/自检.运行器.ixx` 保持零修改。

## 3. S1-S3 固定配置与排他会话

1. 新建 `生产运行期配置.数据.h`，唯一内联拥有版本 1 固定配置：域 / 仓库均为 1，21 个系统角色键为稳定前缀加 1 至 21，三个概念活动键为稳定前缀加 22 至 24，安全和服务当前 / 目标值均为 0 / 1，动态根材料为 1，动作键为 `0x4859'0002'0000'0001`。
2. 启动请求只含请求版本；固定配置的完整相等、内部有效性和 21 + 3 跨组唯一性均由数据单头校验，不读取命令行、时间戳、SQL、日志或旧对象。
3. 新建生产真模块 `启动.生产运行期.ixx`。不可复制 `生产运行期会话` 唯一拥有启动互斥和 `运行期上下文宿主`，把并发线性化点固定在候选构造之前。
4. 有效启动顺序固定为：请求复核、当前租约复核、读取固定配置、构造候选、初始化系统角色、初始化概念活动、完整门禁、宿主首次发布、同身份租约复核。
5. 首次调用返回 `已启动`；同会话重复或并发后继调用返回 `已有当前上下文`。无效请求写前拒绝，发布成功后租约无效或异身份归为内部错误，不回退旧通道。

## 4. S4-S5 入口与阶段 900

1. `入口.cpp` 常规 import 生产启动模块，只新增 `--runtime-context` 模式位、冲突门禁、一个局部会话、一次启动调用和退出码。
2. 新通道分支位于运行日志和首个旧 `主信息仓库` 对象构造之前；成功首发并持有租约到分支退出，P0-A 尚无消费者，因此直接退出 0。
3. 新通道与数据库自检、仓库性能、D455 真实样本或 `--headless` 组合时退出 2，不构造新旧任一上下文；与 `--self-test-exit` 组合仍只运行真实新通道。
4. 默认路径保持原有旧装配顺序。仅 Debug import `自检.生产运行期首发`，新增一个结果槽、阶段 900 一次登记和一个最终具名门禁；专项正文全部位于独立自检真模块。
5. 工程把生产启动模块排在运行期上下文之后，把专项自检模块排在生产启动之后；配置数据头按 `ClInclude` 登记。

## 5. A01-A18 与模式证据

Debug 阶段 900 的 18 项具名输出全部通过，覆盖正式签名、固定 24 键、请求仅含版本、会话不可复制、初始化 / 发布顺序后验、无效请求、重复启动、八线程同会话唯一首发、同身份租约、隔离会话、完整材料读回和固定配置不变。

模式矩阵最终结果：

```text
Debug --runtime-context --self-test-exit                         -> 0
Debug --runtime-context --database-self-test-exit                -> 2
Debug --runtime-context --warehouse-performance-self-test-exit   -> 2
Debug --runtime-context --d455-real-sample-exit                  -> 2
Debug --runtime-context --headless                               -> 2
Release --self-test-exit                                         -> 0
Release --runtime-context --self-test-exit                       -> 0
```

静态锚点复核：阶段 900 精确命中 1 次；新通道分支位于入口第 463 行，首个旧仓库构造位于第 480 行；运行器差异为 0；Release 二进制对 `PERSIST-S1-P0-A`、A01 和 A18 文本的匹配均为 0。

## 6. S6 最终验证

最终代码在加入会话级启动互斥后重新从构建开始验证：

```text
Debug x64 Rebuild：0 警告 / 0 错误
Release x64 Rebuild：0 警告 / 0 错误
Debug 完整自检：退出 0，P0-A A01-A18 = 18 / 18
Release 完整自检：退出 0
Debug 默认完整自检最终不间断连续组：20 / 20，371.4 秒
Debug 显式生产首发最终不间断连续组：20 / 20
python tools/check_specs.py --strict：通过
git diff --check：通过
允许范围、阶段唯一性、早分流、Debug import 边界和 Release 文本隔离：通过
```

验证过程中曾在手工终止两个低效输出包装器后的一组非最终循环中出现一次无具名退出 1；该轮没有保留输出，不能作为通过证据，也没有据此放宽验收。代码复核随后把同会话启动从“只在宿主发布点排他”收紧为“候选构造前整段排他”，并从最终代码重新构建、执行具名单轮和一组不间断 20 / 20；最终组未再出现失败。

## 7. 交接边界

本任务只证明显式新运行期早期首发、固定配置、唯一生产会话、排他候选构造和同身份租约已经在任务分支实现。默认入口仍是旧通道；消费者、线程、SQL、控制面板、快照、恢复、默认入口切换和旧生产所有者退出均未实现。

下一步只允许精确暂存七份允许文件，提交并非强制推送同名远端 `codex/297-p0a-r1`。任务分支状态只能为“分支完成待集成”；在集成验证、主工作树 main 发布和设计归档之前，不得宣称 #297 正式完成。
