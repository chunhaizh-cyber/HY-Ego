# RUNTIME-TXN-S0 恢复链接口归属裁决

## 裁决

```text
复用并修订，不新增第二套生产上下文。
```

1. #247 建立唯一 `启动.运行期上下文` 模块中的协调状态、接线、四仓库结构核心和宿主 / 租约骨架；缺少领域服务时不得生产发布。
2. #248 建立 `装配.领域服务` 并回改同一上下文模块，使最终上下文拥有服务装配；全新启动与恢复启动是该最终类型的两个互斥工厂分支，每个进程只允许一次发布一个完整实例。
3. #229 只形成绑定隔离候选身份的跨段完整性屏障 / 发布许可，不拥有运行期上下文。
4. #237 改为消费 #229 许可并调用 #247/#248 最终同一上下文类型的恢复工厂；不得再定义平行上下文、服务装配、宿主或租约类型。
5. #238 只消费已发布租约启动线程、SQL 审计和只读控制面板；不得另建仓库或协调器。
6. #239 验证全新 / 恢复两分支共用同一类型、同一发布槽和同一租约合同。
7. #226 从已发布上下文租约取得共享许可，只冻结生产仓库；#217 隔离自检组永不作为快照来源。
8. #230 在上述合同完成后做失败不变式和并发验收，不承担新的上下文所有权。

## 当前事实

当前源码只有仓库快照拒绝矩阵和隔离候选壳，没有 HYSNAP01 编码、恢复发布、运行期上下文、空宿主、发布许可或租约实现。本裁决是后继计划合同，不是实现完成事实。

## JY-290 追加边界

`装配.领域服务` 只构造并返回领域服务装配，接收接线和四仓库引用，不 import `启动.运行期上下文`，也不拥有仓库、宿主或租约。`启动.运行期上下文` 在 #248 单向 import 服务装配模块，并按“协调器、接线、主信息、节点、关系、索引、领域服务装配”持有最终成员。#237 与所有恢复后继只消费 #248 后完整上下文，不能消费 #247 的结构核心。
