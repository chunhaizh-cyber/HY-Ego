# FS-02 语素入口与基础信息桥接只读扫描 Codex 断点清单

日期：2026-07-06
状态：已记录断点 / 文档切片 / 未改代码 / 未迁移旧函数

## 1. 本切片输入

```text
用户要求不需要确定的步骤持续执行。
计划索引 HR-001-FM-SCAN 要求进入 FS-02 旧项目语素 / 基础信息函数只读扫描。
```

## 2. 已完成动作

```text
1. 只读扫描 `D:\鱼巢\语素类.h`。
2. 只读扫描 `D:\鱼巢\语素类.cpp`。
3. 只读扫描 `D:\鱼巢\语素主信息类.h`。
4. 只读扫描 `D:\鱼巢\语素节点类型.h`。
5. 只读扫描 `D:\鱼巢\语素信息类型.h`。
6. 只读扫描 `D:\鱼巢\语言类_基础信息命名.cpp`。
7. 只读扫描 `D:\鱼巢\语言类.h`。
8. 新增 `实施记录/20260706_FS02_语素入口与基础信息桥接只读扫描记录.md`。
9. 登记 OF-046 到 OF-059。
10. 同步逐函数只读扫描清单、计划索引和项目记忆。
```

## 3. 本切片结论

```text
旧语素系统的可保留主线是“词面 -> 语素入口 -> 内部信息节点”的桥接语义。
旧语素树、旧 `语素入口主信息类` 字段和旧 SQL 投影不得整体迁移。
旧 `确保语素入口对应基础信息` 会自动创建或绑定基础信息，必须拆成领域服务创建目标节点和语素服务绑定关系两个步骤。
候选查询、冲突包、别名反查和场景限定查询只形成后续语素增强候选，不形成迁移许可。
显示标题、语言命名、词性文本映射和 SQL 投影只做人读或后续显示 / 持久化适配，不承载机器事实。
```

## 4. 下一步

```text
下一旧函数编号从 OF-060 开始。
继续 FS-02：旧项目基础信息类、存在 / 场景 / 状态 / 动态 / 二次特征 / 因果相关函数只读扫描。
若 FS-02 后续发现语素入口遗漏，只能追加登记，不得反向宣称旧语素类已迁移。
```

## 5. 禁止宣称

```text
不得声明旧函数已扫描完成。
不得声明旧语素类已迁移。
不得声明旧语素树、旧语素主信息字段或旧 SQL 投影可进入新项目。
不得声明语素候选查询、自然语言理解、显示层只读视图或基础信息系统完整完成。
不得接控制面板 / SQL / D455 / 体素 / 外设。
```

## 6. 验证记录

```text
本切片只做文档和只读事实记录验证。
未改代码。
未运行构建。
未运行当前项目入口。
未运行旧项目程序。
```
