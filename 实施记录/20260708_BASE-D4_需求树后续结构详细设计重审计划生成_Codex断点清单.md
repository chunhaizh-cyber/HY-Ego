# BASE-D4 需求树后续结构详细设计重审计划生成 Codex 断点清单

日期：2026-07-08
状态：待确认计划已生成 / 未登记可执行队列 / 未改 C++ / 未构建 / 未迁移旧函数

## 1. 输入入口

```text
用户指令：生成下一个计划。
窗口类型：设计确认窗口。
承接对象：第 6 项“需求树后续结构代码逻辑流程图”已确认产物。
生成文件：计划/待确认计划/20260708_BASE-D4_需求树后续结构详细设计重审计划_v0.1.md。
```

## 2. 核心处理

1. 复核当前计划索引、项目记忆、待确认问题和第 6 项流程图确认记录。
2. 判定 BASE-D3 需求创建与目标状态详细设计仍为待确认，不能直接生成施工计划。
3. 判定既有 `需求树后续结构详细设计.md` 仍标记为“需经服务矩阵和流程图迁移包重审”。
4. 生成 BASE-D4 需求树后续结构详细设计重审计划候选。
5. 同步计划索引、当前状态、决策记录和待确认问题。

## 3. 结构承载

```text
计划/待确认计划/20260708_BASE-D4_需求树后续结构详细设计重审计划_v0.1.md
实施记录/20260708_BASE-D4_需求树后续结构详细设计重审计划生成_Codex断点清单.md
计划/计划索引.md
项目记忆/当前状态.md
项目记忆/决策记录.md
项目记忆/待确认问题.md
```

## 4. 输出变化

```text
新增 BASE-D4 待确认计划。
计划索引登记 BASE-D4 计划候选。
当前状态记录第 6 项流程图已确认且详细设计重审计划候选已生成。
决策记录新增 BASE-D4 计划候选生成决策。
待确认问题新增 BASE-D4 计划确认项。
```

## 5. 调用路径

```text
第 6 项需求树后续结构流程图确认
-> 既有需求树后续结构详细设计需重审
-> BASE-D4 详细设计重审计划候选
-> 等待用户确认
-> 确认后才可执行详细设计重审
```

## 6. 验证方式

本次是文档治理切片，不运行 C++ 构建，不运行默认入口。

计划生成后执行：

```powershell
git diff --check
python .\tools\check_specs.py
```

## 7. 禁止宣称

```text
不得宣称 BASE-D4 计划已确认。
不得宣称已登记可执行队列。
不得宣称已修订需求树后续结构详细设计。
不得宣称已生成代码施工计划。
不得宣称已修改 C++ 或完成构建。
不得宣称完整需求树、父子需求写入、结构化更新提交或方法候选算法已实现。
```
