# 第二轮最小闭环增强 Codex 断点清单

日期：2026-07-05

## 1. 当前切片

执行 `计划/20260705_第二轮最小闭环增强切片.md`。

## 2. 预期改动

```text
方法服务：增加执行前授权结构入口，执行 I64 写入时必须带授权。
因果服务：动态记录改为被改变目标、改变前值、改变后值必填，来源动作可选。
因果服务：因果引用改为只要求来源动态。
核心仓库：增加只读结构数量读取接口，用于入口拒绝前后数量证明。
入口样例：MC-04 到 MC-10 输出第二轮增强证据。
```

## 3. 当前判断

```text
现有第一轮代码仍要求动态来源动作，因果引用仍要求动态和目标。
现有入口拒绝只输出布尔摘要，尚未读取拒绝前后结构数量。
默认构建已经通过第一轮，第二轮需重新构建和运行验证。
```

## 4. 验证计划

```text
git diff --check
msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m
.\x64\Debug\海中鱼巣.exe
必要时执行宏开启编译检查，不运行会触发弹窗的错误路径。
```

## 6. 实施结果

已完成：

```text
方法服务增加 `形成I64写值授权`。
`执行I64写入方法` 改为必须接收执行前授权。
因果服务 `记录动态` 改为被改变目标、改变前值、改变后值必填，来源动作可选。
因果服务 `记录因果引用` 改为只要求来源动态。
节点仓库、关系仓库、索引仓库增加只读结构数量接口。
入口输出 MC-04 到 MC-10 的第二轮增强证据。
```

验证结果：

```text
`git diff --check` 通过。
默认 Debug x64 Rebuild 通过，0 警告，0 错误。
默认 exe 运行退出码 0，MC-01 到 MC-11 均通过。
MC-09 拒绝前后节点 43 -> 43，关系 26 -> 26，索引 10 -> 10。
临时在 Debug x64 项目宏中开启 `HY_EGO_ENABLE_FAULT_TOLERANCE_CHECK` 的隔离编译通过，0 警告，0 错误。
临时项目宏已还原，默认构建保持宏关闭。
宏开启 exe 未运行，避免故意触发弹窗路径。
```

## 5. 禁止扩大声明

```text
不声明完整最小闭环完成。
不声明基础信息服务分层代码完成。
不声明旧项目能力迁移完成。
```
