# FS-07 动作动态因果入口只读扫描 Codex 断点清单

日期：2026-07-06
状态：已完成 / 只读扫描 / 未改代码 / 未迁移旧函数

## 1. 输入入口

用户要求不需要确认的步骤持续执行。

计划索引当前执行项为 `HR-001-FM-SCAN`，上一闭合点为 FS-06 第一批方法系统入口只读扫描，下一旧函数编号从 `OF-162` 开始。

## 2. 本次读取依据

```text
AGENTS.md
计划/计划索引.md
规范/000_项目规则总纲.md
规范/001_规则迁移清单.md
计划/20260706_函数迁移前置专项_v0.1.md
实施记录/20260706_逐函数只读扫描清单.md
规范/动作入口规范.md
规范/详细设计/动作入口详细设计.md
规范/详细设计/动态服务详细设计.md
规范/错误结果与入口拒绝规范.md
规范/领域服务授权写入规范.md
```

当前未发现 `规范/详细设计/因果服务详细设计.md`。旧因果模板生成、证据动态样本和可靠因果治理只能登记为后续因果服务增强或专项设计依据，不得进入代码实施。

## 3. 本次只读扫描输出

已生成：

```text
实施记录/20260706_FS07_动作动态因果入口只读扫描记录.md
```

已登记旧函数编号：

```text
OF-162 至 OF-174
```

覆盖边界：

```text
旧动态证据材料读取和动作来源边界
旧动作动态创建和来源动作边界
旧动态聚合、低层动态来源链和运动候选
旧因果模板身份、证据状态和查询入口
旧动态生成或命中因果模板
旧因果证据动态样本和动作结果验收
旧默认本能方法种子和动作键映射
旧本能动作注册、可调用检查和执行分派
旧本能动作场景模式匹配和条件参数检查
旧动作验证报告和执行结果摘要
旧动作线程调度边界
旧自我动作实现模块集合边界
旧动作闭环特征名常量
```

## 4. 当前判断

1. 旧动作动态创建语义只能重写为动作入口角色节点加动态服务记录，不直接迁移旧函数体。
2. 旧函数指针注册、本能动作 ID 表、动作文本名和日志标签不能作为新项目动作身份。
3. 旧动作线程只是调度边界，线程不得作为动作来源。
4. 旧因果模板生成依赖尚未建立的因果服务详细设计，当前只能形成后续设计输入。
5. 外设、D455、体素、控制面板和 SQL 相关动作继续归入 FS-X 或后续专项。

## 5. 下一编号和下一步

下一旧函数编号：

```text
OF-175
```

下一步：

```text
继续 FS-08，扫描旧项目缓存 / 统计函数第一批。
```

## 6. 验证方式

本切片为文档和只读扫描切片，未运行构建、未运行当前项目入口、未运行旧项目程序。

收尾验证应覆盖：

```text
rg -n "FS-07|OF-162|OF-174|OF-175|继续 FS-08|动作动态因果入口" 实施记录 计划 项目记忆
git diff --check
```

## 7. 禁止扩大声明

```text
不得声明旧动作系统已迁移。
不得声明动作动态系统完整完成。
不得声明因果服务或因果模板系统已完成。
不得声明旧本能动作函数指针注册表可直接迁移。
不得新增旧动作句柄、旧因果模板或旧动作验证报告主信息字段。
不得接控制面板 / SQL / D455 / 体素 / 外设。
```
