# 函数迁移服务级确认规则修订 Codex 断点清单

日期：2026-07-07
状态：已完成 / 文档治理 / 未改代码 / 未迁移旧函数

## 1. 输入入口

用户提出：

```text
感觉这样迁移太慢了,不用再每个函数确认了,按照服务确认即可
```

## 2. 本次处理

```text
读取 AGENTS 要求的计划索引、项目规则总纲、规则迁移清单和函数迁移前置专项。
取得计划索引锁。
将函数迁移门禁从逐函数确认修订为服务级确认。
同步计划索引和项目记忆。
新增本断点清单。
```

## 3. 修订口径

```text
函数 = 证据采集单位。
服务逻辑包 = 迁移确认单位。
领域服务 / 结构闭环 = 代码实施单位。
```

旧函数仍必须登记事实、归属服务包和副作用边界，但不再逐个等待用户确认。

确认对象改为：

```text
服务逻辑包。
服务增强专项计划。
服务代码实施切片。
```

## 4. 当前影响

```text
FS-02、FS-03 已完成的逻辑包确认和代码切片验证仍有效。
FS-04 / FS-05 / FS-06 已完成的 S0 当前代码事实扫描可继续按服务切片生成 S1-Sn。
FS-07 已确认专项计划并登记 S0，不需要逐函数确认 OF-162 到 OF-174。
FS-08 / FS-09 待确认计划确认后，也只登记对应服务 S0，不逐函数确认。
```

## 5. 禁止扩大声明

```text
本次只修订迁移确认粒度。
不代表任何旧函数体已迁移。
不代表旧主信息字段可回流。
不代表可以跳过服务专项计划或代码实施切片。
不代表可以未写允许文件、禁止文件和验证方式就改 C++。
不接控制面板 / SQL / D455 / 体素 / 外设。
```
