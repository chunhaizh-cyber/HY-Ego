# WB-P0B-01 运行期只读桥与索引所有者双任务独立集成记录

日期：2026-07-19

执行队列：#299、#304

设计队列：DQ-191、DQ-196

集成批次：INT-WB-P0B-01-R1

状态：已集成待设计同步

## 1. 身份与输入核对

```text
集成路径：C:\Users\zhchh\.codex\worktrees\integration-wb-p0b-01-r1\海中鱼巢
集成分支：codex/integration-wb-p0b-01-r1
集成起点：a652cb6bdbb52cd524ceceaaaf4f9a03d09d16aa
第一输入：origin/codex/299-p0b1-r1@86e2503ad6e0a1f51f59011c58fc45d9314c6925
第一输入父提交：33ae6bc3772dafa93a259de452bd509c739f10e8
第一输入汇集提交：1561d433db67faedabb4e88ebfea8e085f4d89af
第二输入：origin/codex/304-a0-r1@49157f8869658a3cd15f24a169d060817abc4209
第二输入父提交：33ae6bc3772dafa93a259de452bd509c739f10e8
第二输入汇集提交：bb35ab9dca1c384a43c8ab9a688da89a5ed7d1aa
```

接管前，登记路径、分支、起点、worktree / index、无 upstream 与远端同名分支
状态均和 JY-439 / E184 一致；WT-MAIN clean，`main == origin/main == a652cb6`。
身份核对通过后进入集成中并取得主线发布占用。

两个任务输入均以 `33ae6bc` 为唯一父提交，分别为单提交、远端 0 / 0、任务树
clean；实际文件数为 6 / 16，交集为 0。集成严格按 `86e2503 -> 49157f8`
顺序执行，两次 cherry-pick 均无冲突。汇集后的 22 份任务文件与两个原任务提交
逐 blob 内容差异为 0，没有由集成角色修改任务代码、自检语义或共享 ABI。

## 2. 集成范围

本集成只包含：

1. `1561d43` 汇集的 #299 六文件任务差异。
2. `bb35ab9` 汇集的 #304 十六文件任务差异。
3. 本专属独立集成记录。

相对集成起点的最终范围精确为 23 文件。未修改计划、计划索引、任务队列、并行
工作树登记表、窗口交互记录、流程图、详细设计或其它中央治理；未登记 920，未
消费 #301、#305 或其它后继计划。

## 3. 独立集成验证

在 INT-WB-P0B-01-R1 中从头重建并运行：

```text
Debug x64 Rebuild：退出 0，0 警告 / 0 错误
Release x64 Rebuild：退出 0，0 警告 / 0 错误
Debug 完整自检：退出 0
Release 完整自检：退出 0
SELF-GOV-S3B A01-A09：9 / 9，失败 0
CONCEPT-NAMING-S1 A01-A12：12 / 12，失败 0
PERSIST-S1-A0 A01-A19：19 / 19，失败 0
Debug 严格完整自检最终连续组：20 / 20，每轮退出 0，轮间 2 秒，403.2 秒
py -3.14 tools/check_specs.py --strict：通过
git diff --check：通过
```

范围与静态门禁：

```text
任务输入范围：6 + 16 文件，交集 0
集成起点到两个任务汇集提交范围：22 文件
任务 22 文件与两个原任务提交树内容差异：0
最终范围：22 个任务文件 + 1 份集成记录 = 23 文件
B1 两个生产上行桥禁止传统领域服务 / 原始事务依赖：0 命中
阶段 970 的 PERSIST-S1-A0 Debug 登记：1
阶段 920 登记：0
通用自检运行器差异：0
Release PERSIST-S1-A0 / 970 专项文本：0
```

## 4. 发布与完成边界

提交并推送本集成分支后，集成角色再次 fetch，确认 `origin/main` 仍等于集成起点
且 WT-MAIN clean，才允许让 `D:\海中鱼巢` 的 `main` 对集成结果执行
`merge --ff-only` 并非强制推送 `origin/main`。禁止 force、语义性消冲突或修改
中央治理。

本批进入主线后的状态仅为“已集成待设计同步”。#299 / #304 正式归档、第二批
解锁和后继派发仍由设计角色根据正式集成提交裁决。本批只证明运行期纯只读上行
桥和索引所有者命名域 / 键保留合同已经联合进入主线；不证明 #301 / #305、920、
完整快照、恢复、默认入口或旧能力迁移完成。

