# FS-05 任务模块筹办执行管理边界只读扫描记录

日期：2026-07-06
状态：FS-05 第二批已登记 / 只读扫描 / 未改代码 / 未迁移旧函数
依据清单：`实施记录/20260706_逐函数只读扫描清单.md`

## 1. 本轮定位

本轮继续 FS-05，登记旧项目任务模块中与任务筹办、任务执行、任务管理工作线程、任务运行包和任务治理协议有关的函数族。

本轮只做只读事实登记，不迁移旧任务模块函数体，不恢复旧线程调度为第一轮任务系统主线，不把旧运行包、工作线程快照、外设承接、日志或控制面板材料当作新项目机器事实。当前新项目任务系统口径仍是：任务服务承接需求，任务目标来自需求目标状态，任务选择方法关系是方法执行权限依据，任务筹办 / 执行 / 管理线程属于后续增强专项。

## 2. 只读扫描范围

```text
D:\鱼巢\任务模块.筹办.ixx
D:\鱼巢\任务模块.执行.ixx
D:\鱼巢\任务模块.管理工作线程.ixx
D:\鱼巢\任务模块.运行包.ixx
D:\鱼巢\任务模块.治理协议.ixx
```

本轮同时识别但不进入当前迁移的边界：

```text
旧 std::async / 工作线程调度。
旧外设观察等待项和观察事实承接请求。
旧运行包封口 / 回流结算包。
旧治理协议中高阶排序、恢复快照、控制承接和原语判定协议。
```

扫描证据来自：

```text
任务模块.筹办.ixx:1-4：筹办只承载任务筹办生命周期普通函数，不作为本能方法、候选方法或动作动态来源。
任务模块.筹办.ixx:39-155：任务推进阶段、推进状况、执行结果类型、推进回执、筹办结果和安全因果风险树维护结果结构。
任务模块.筹办.ixx:5759-6274：`筹办任务` 主入口，读取来源需求、当前 / 目标状态、目标宿主、候选方法和方法结构缺口，形成筹办回执。
任务模块.筹办.ixx:6277-6318：安全因果风险树常驻维护和缺口派生消息入口当前只记录跳过，不直接写需求树或风险事实。
任务模块.执行.ixx:3-6：任务执行只承接已筹办任务，不把任务执行本身登记为本能方法，业务状态仍由通用结构承载。
任务模块.执行.ixx:36-78：任务执行记账结果、步骤执行结果、执行任务结果和语义槽同步结构。
任务模块.执行.ixx:87-153：任务执行临时结果场景、长期输出场景截断和方法调用动作动态创建辅助。
任务模块.执行.ixx:971-1336：任务执行桥类，同步等待方法执行，检查方法动作 / 可执行入口 / 条件 / 结果 / 条件结果对和方法可用性。
任务模块.执行.ixx:1338-1518：执行记录同步空实现、取任务执行方法、按方法运行结果推导任务状态和 `执行任务` 主入口。
任务模块.管理工作线程.ixx:3-6：任务管理工作线程只推进任务域结构化状态和回执，本模块只供人读，不参与机器判断。
任务模块.管理工作线程.ixx:47-154：工作线程一步结果、一步响应、实例快照、外设消息承接结果和导出函数。
任务模块.管理工作线程.ixx:555-743：父任务收束角色视图、从筹办回执推导界面任务状态、填充治理结果和工作线程不得写任务虚拟存在的空实现。
任务模块.管理工作线程.ixx:822-1050：筹办派生需求消息主键槽补齐、完整性检查和重复上报防护。
任务模块.管理工作线程.ixx:3542-3993：工作线程壳启动、外设消息承接、提交任务工作项、读取快照和停止线程壳。
任务模块.运行包.ixx:21-218：运行包状态、需求大类、动作类型、一步治理任务包、回流结算包、校验和封口函数声明。
任务模块.治理协议.ixx:20-228：任务管理功能域、缺口类型、下一步去向、门控结果、方法来源、触发来源和缺口反馈消费策略枚举。
任务模块.治理协议.ixx:229-599：原语判定、二次特征证据、高阶排序、任务管理请求 / 单步决策 / 结果结构。
任务模块.治理协议.ixx:601-630：治理恢复事件镜像和恢复快照结构。
```

## 3. 逐函数登记

### OF-140

```text
旧函数编号：OF-140
扫描批次：FS-05
旧项目路径：D:\鱼巢\任务模块.筹办.ixx:39
旧函数名：枚举_任务推进阶段 / 枚举_任务推进状况 / 枚举_任务执行结果类型 / 结构_任务推进回执 / 结构_任务筹办结果
旧函数签名：任务筹办推进阶段、推进状况、执行结果类型、筹办回执和筹办结果结构族。
当前调用方：旧任务筹办入口、任务管理工作线程、任务执行回执和管理线程协议。
触发路径：任务筹办读取需求和候选方法后，生成建议任务状态、阶段、当前方法、候选方法和派生需求消息。
输入：任务节点、来源需求、目标特征、目标状态、候选方法、缺口类型、发生时间戳。
输出：筹办结果、推进回执、建议状态 / 阶段、当前方法和候选方法引用。
读结构：旧任务主信息、需求目标状态、任务执行方法、方法结构和方法虚拟存在。
写结构：结构体回执；本结构自身不直接写任务树。
是否写机器事实：否，作为过程回执；被后续提交入口消费后才可能形成任务事实。
是否依赖日志：否。
是否依赖显示：可间接服务显示。
是否依赖 SQL：否。
是否依赖线程：间接被任务管理工作线程消费。
是否依赖外设 / D455 / 体素：否。
保留行为：任务筹办应输出结构化回执，不直接作为动作来源或任务完成事实。
删除行为：不得把回执 bool、文本状况或线程快照当作权威任务状态。
新增约束：回执进入新项目前必须由任务服务或后续任务管理服务复核任务、方法、目标状态和发生时间戳。
目标新结构：任务生命周期状态、任务选择方法关系、后续任务管理回执结构。
目标领域服务入口：任务服务后续筹办回执提交入口。
对应规范 / 详细设计：任务系统详细设计、方法系统详细设计、动作入口详细设计。
入口拒绝条件：任务无效、需求目标状态缺失、方法无效、候选方法被当选中事实、回执缺发生时间戳。
验证方式：回执只作为过程材料登记；任务状态变化必须通过任务服务结构读回。
初步判定：拆分
待用户确认项：筹办回执是否进入需求树 / 任务管理 / 方法系统后续增强专项。
扫描证据：`任务模块.筹办.ixx:39` 至 `任务模块.筹办.ixx:155`。
```

### OF-141

```text
旧函数编号：OF-141
扫描批次：FS-05 / FS-06 / FS-07
旧项目路径：D:\鱼巢\任务模块.筹办.ixx:5759
旧函数名：筹办任务
旧函数签名：结构_任务筹办结果 筹办任务(::自我类& 自我对象, 任务类::节点类* 输入任务, 时间戳 now) noexcept
当前调用方：旧任务管理工作线程、任务筹办调度和任务管理协议。
触发路径：归一任务头，构造需求生命周期上下文，判定需求满足、活动子需求、枚举目标合法性、因果路径派生需求、候选方法和方法结构缺口，最后生成进入执行的回执。
输入：自我对象、任务节点、发生时间戳。
输出：任务筹办结果、候选方法集、当前方法、派生需求消息和建议任务状态 / 阶段。
读结构：旧任务主信息、需求当前 / 目标状态、需求目标特征、需求父子角色、方法结构、方法动作和方法虚拟存在。
写结构：本入口直接返回回执；内部可能创建执行输入参数场景和方法 / 状态缺口材料。
是否写机器事实：待确认；主入口以回执为主，但内部缺口构造会创建旧结构材料。
是否依赖日志：是，记录筹办日志。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：被工作线程调用。
是否依赖外设 / D455 / 体素：否。
保留行为：筹办阶段只形成候选、缺口或就绪回执；找到方法不等于任务完成或方法可执行。
删除行为：不得直接迁移旧函数体；不得让筹办现场执行提交方法或直接写需求树。
新增约束：新项目需先确认任务管理增强、方法候选、动作入口和派生需求协议；方法执行权限仍来自任务方法关系。
目标新结构：任务选择方法关系、方法动作入口、方法条件 / 结果角色、派生需求消息或后续需求树结构。
目标领域服务入口：后续任务管理服务 `筹办任务` 或拆分后的任务筹办入口。
对应规范 / 详细设计：任务系统详细设计、方法系统详细设计、动作入口详细设计、需求结算记录详细设计。
入口拒绝条件：无效任务、来源需求缺失、目标状态缺失、目标宿主缺失、方法结构缺动作入口、方法条件 / 结果不完整。
验证方式：筹办只产出可读回执；任务状态、方法绑定和派生需求必须由后续确认服务提交。
初步判定：改写为领域服务入口
待用户确认项：任务筹办是否作为需求树 / 任务管理 / 方法系统后续增强专项第一批代码入口。
扫描证据：`任务模块.筹办.ixx:5759` 至 `任务模块.筹办.ixx:6274`。
```

### OF-142

```text
旧函数编号：OF-142
扫描批次：FS-05 / FS-07
旧项目路径：D:\鱼巢\任务模块.筹办.ixx:5949
旧函数名：筹办_按因果反推必要状态 / 生成安全因果风险树缺口派生需求消息集 / 维护安全因果风险树常驻任务
旧函数签名：因果路径派生需求、风险树常驻维护和安全因果缺口消息函数族。
当前调用方：旧任务筹办主入口和安全因果治理任务。
触发路径：普通筹办前尝试从因果路径反推必要状态，形成阻塞派生需求消息；风险树维护入口当前跳过。
输入：自我对象、任务、需求上下文、发生时间戳。
输出：派生需求消息集、风险树维护结果或空消息集。
读结构：旧因果证据、需求目标状态、目标特征、目标宿主、状态材料。
写结构：主入口中只形成消息草案；风险树维护入口记录跳过，不直接写风险树。
是否写机器事实：消息草案本身不应是机器事实；实际需求入树必须由自我线程和需求类裁决。
是否依赖日志：是。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：是，正式需求入树依赖自我线程。
是否依赖外设 / D455 / 体素：否。
保留行为：因果缺口可以作为派生需求候选材料。
删除行为：不得由任务筹办直接写需求树、风险安全层总值、自我安全值或默认满足事实。
新增约束：轻量因果引用不能当稳定因果结论；派生需求必须经需求服务 / 自我线程边界确认。
目标新结构：派生需求候选消息、因果证据集合、后续需求树生长结构。
目标领域服务入口：后续需求树 / 任务管理 / 因果增强服务。
对应规范 / 详细设计：因果轻量口径、任务系统详细设计、需求结算记录详细设计。
入口拒绝条件：无有效来源动态 / 因果证据、目标状态缺失、派生需求与父目标族重复、任务筹办越权写需求树。
验证方式：只登记缺口消息，不新增需求节点；正式需求入树另走确认入口。
初步判定：排除 / 后续专项
待用户确认项：安全因果风险树是否进入后续因果治理专项。
扫描证据：`任务模块.筹办.ixx:5949` 至 `任务模块.筹办.ixx:6318`。
```

### OF-143

```text
旧函数编号：OF-143
扫描批次：FS-05 / FS-07
旧项目路径：D:\鱼巢\任务模块.执行.ixx:36
旧函数名：枚举_任务执行记账结果 / 结构_任务步骤执行结果_最小 / 结构_执行任务结果 / 同步执行桥语义槽
旧函数签名：任务执行记账结果、步骤执行结果、执行任务结果和抽象特征语义槽同步结构族。
当前调用方：旧任务执行桥、任务管理工作线程和任务管理线程协议。
触发路径：方法执行后形成执行回执、建议任务状态 / 阶段、方法运行结果、动作动态和动作验证摘要。
输入：任务、方法、条件场景、结果场景、方法运行结果、动作动态。
输出：执行结果结构、记账结果值、建议状态 / 阶段和语义槽镜像。
读结构：旧任务、方法、场景、方法运行结果特征。
写结构：结构体回执，不直接提交任务账。
是否写机器事实：否，回执本身不是权威任务事实。
是否依赖日志：否。
是否依赖显示：可间接服务显示。
是否依赖 SQL：否。
是否依赖线程：被工作线程消费。
是否依赖外设 / D455 / 体素：否。
保留行为：执行结果应作为结构化回执，不能用普通 bool 直接裁决任务完成。
删除行为：不得把执行成功 bool 持久化为任务完成、需求满足或方法成功事实。
新增约束：执行回执进入新项目必须复核任务方法关系、动作入口角色和实际结果状态。
目标新结构：任务实际结果状态、动作动态、方法执行结果证据、任务生命周期状态。
目标领域服务入口：后续任务执行服务 / 动作动态记录入口。
对应规范 / 详细设计：任务系统详细设计、动作入口详细设计、动态服务详细设计。
入口拒绝条件：任务无效、方法无效、动作入口无效、结果场景为临时容器却被长期外泄。
验证方式：执行回执只作为后续提交材料，任务完成仍由目标状态和实际结果状态比较。
初步判定：拆分
待用户确认项：执行回执是否与动作入口第一轮结构合并设计。
扫描证据：`任务模块.执行.ixx:36` 至 `任务模块.执行.ixx:78`。
```

### OF-144

```text
旧函数编号：OF-144
扫描批次：FS-05 / FS-07
旧项目路径：D:\鱼巢\任务模块.执行.ixx:87
旧函数名：创建任务执行临时结果场景_执行桥_最小_v0 / 任务执行长期输出结果场景_执行桥_最小_v0 / 私有_创建方法调用动作动态_执行桥_最小_v0
旧函数签名：任务执行临时结果场景、长期输出场景截断和方法调用动作动态创建辅助函数族。
当前调用方：旧任务执行桥和任务执行入口。
触发路径：执行方法时创建临时结果场景，执行结束后记录方法调用动作动态并截断临时容器。
输入：任务、方法、输入场景、输出场景、开始 / 结束时间、执行成功、错误码。
输出：场景节点、动态节点或空。
读结构：旧方法动作名、方法节点、输入 / 输出场景。
写结构：旧内部治理场景、方法调用动作动态。
是否写机器事实：是，动作动态属于证据材料；临时结果场景不得长期外泄。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：可被异步执行体调用。
是否依赖外设 / D455 / 体素：否。
保留行为：方法调用应产生动作动态证据，且需要开始 / 结束时间、输入 / 输出场景和错误码。
删除行为：不得用方法动作名文本或函数名裁决动作身份；临时结果场景不得作为长期世界事实容器。
新增约束：动作来源应指向已确认动作入口角色节点；动作动态必须经动态服务或动作入口后续入口记录。
目标新结构：动作入口角色节点、动作动态、输入 / 输出场景、动态证据。
目标领域服务入口：动作入口服务 / 动态服务后续动作动态记录入口。
对应规范 / 详细设计：动作入口详细设计、动态服务详细设计、任务系统详细设计。
入口拒绝条件：动作入口无效、方法无效、场景无效、缺开始 / 结束时间、临时结果场景被长期读取。
验证方式：动作动态可追溯方法和入口；临时结果场景消费后不得作为长期输出。
初步判定：改写为领域服务入口
待用户确认项：旧方法调用动作动态字段如何映射到当前动作入口角色方案。
扫描证据：`任务模块.执行.ixx:87` 至 `任务模块.执行.ixx:153`。
```

### OF-145

```text
旧函数编号：OF-145
扫描批次：FS-05 / FS-06 / FS-07
旧项目路径：D:\鱼巢\任务模块.执行.ixx:971
旧函数名：任务执行桥类::启动 / 请求停止 / 等待停止 / 取消全部 / 执行方法_同步等待 / 执行任务
旧函数签名：任务执行桥和任务执行主入口函数族。
当前调用方：旧任务管理工作线程和任务执行流程。
触发路径：读取任务执行方法或选中自我能力，检查方法动作、可执行入口、条件节点、结果节点、条件结果对和方法可用性，然后执行本能动作。
输入：执行桥、任务节点、方法节点、当前场景、条件场景、结果场景、超时时间。
输出：方法运行结果、任务执行结果、动作动态和建议任务状态。
读结构：旧任务执行方法、任务选中自我能力、任务场景、方法动作句柄、方法条件 / 结果结构、方法状态。
写结构：方法运行结果实例、临时结果场景、方法调用动作动态；任务账写入为空实现或后续提交。
是否写机器事实：是，方法运行结果和动作动态可成为证据；任务状态不由本入口直接提交。
是否依赖日志：是，部分诊断写运行日志。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：是，使用停止 / 取消标记和 `std::async` 超时执行。
是否依赖外设 / D455 / 体素：否。
保留行为：执行前必须检查方法动作入口、条件 / 结果结构和方法可用性；任务账提交应与执行桥分离。
删除行为：不得迁移旧异步执行桥作为第一轮任务服务主线；不得让线程成为动作来源。
新增约束：新项目方法执行必须已有任务方法关系和动作入口角色；完整任务执行需另建代码实施切片。
目标新结构：任务选择方法关系、动作入口角色、动作动态、任务实际结果状态。
目标领域服务入口：后续任务执行服务、方法服务执行入口、动作入口服务。
对应规范 / 详细设计：任务系统详细设计、方法系统详细设计、动作入口详细设计。
入口拒绝条件：任务无效、方法无效、缺任务方法关系、缺动作入口、缺条件 / 结果角色、缺场景或时间戳。
验证方式：缺动作入口 / 条件 / 结果时拒绝或形成缺口回执；不直接写任务完成状态。
初步判定：拆分
待用户确认项：旧任务执行桥是否进入任务管理后续增强，还是拆入动作入口 / 方法执行专项。
扫描证据：`任务模块.执行.ixx:971` 至 `任务模块.执行.ixx:1518`。
```

### OF-146

```text
旧函数编号：OF-146
扫描批次：FS-05 / FS-08
旧项目路径：D:\鱼巢\任务模块.管理工作线程.ixx:47
旧函数名：结构_工作线程一步结果 / 结构_工作线程一步响应 / 结构_工作线程实例快照 / 确保任务管理工作线程壳已启动 / 读取任务管理工作线程快照 / 停止任务管理工作线程壳
旧函数签名：任务管理工作线程壳、一步响应和运行快照函数族。
当前调用方：旧任务管理线程、控制界面线程、运行诊断和任务提交入口。
触发路径：启动工作线程壳，维护最近任务、方法、推进次数、成功失败次数、派生需求数和最近推进状态等快照。
输入：无或输出指针。
输出：启动 bool、快照结构、停止状态。
读结构：内存线程壳状态。
写结构：内存快照计数、最近主键和最近状态文本。
是否写机器事实：否，快照主要是运行期观察 / 非权威统计。
是否依赖日志：否。
是否依赖显示：是，可服务诊断显示。
是否依赖 SQL：否。
是否依赖线程：是。
是否依赖外设 / D455 / 体素：否。
保留行为：运行快照可作为非权威诊断材料。
删除行为：不得把线程快照、累计计数或最近文本当作任务事实、方法成功或需求满足依据。
新增约束：第一版不接多线程；统计进入非权威缓存或运行期诊断对象。
目标新结构：非权威运行期快照、后续统计缓存。
目标领域服务入口：非权威缓存统计服务或后续任务管理诊断入口。
对应规范 / 详细设计：非权威缓存与统计详细设计、任务系统详细设计。
入口拒绝条件：输出指针为空、快照被业务裁决读取、线程状态被当动作来源。
验证方式：清空或缺失快照不影响任务节点、任务关系、状态和动态事实。
初步判定：排除 / 后续专项
待用户确认项：任务管理快照是否进入显示层只读视图或非权威统计专项。
扫描证据：`任务模块.管理工作线程.ixx:47` 至 `任务模块.管理工作线程.ixx:154`、`任务模块.管理工作线程.ixx:3542` 至 `任务模块.管理工作线程.ixx:3993`。
```

### OF-147

```text
旧函数编号：OF-147
扫描批次：FS-05 / FS-06
旧项目路径：D:\鱼巢\任务模块.管理工作线程.ixx:555
旧函数名：私有_记录父任务收束角色视图_工作线程 / 私有_从任务推进回执推导界面任务状态 / 私有_填充任务层回执到治理结果 / 私有_同步任务回执方法到任务虚拟存在
旧函数签名：工作线程消费筹办回执、推导界面任务状态、填充治理结果和同步方法候选的函数族。
当前调用方：旧工作线程一步推进和任务管理结果填充。
触发路径：筹办回执进入工作线程后，推导建议状态，填充治理结果中的旧 / 新状态、当前方法和候选材料。
输入：任务、来源需求、筹办回执、执行数据、方法运行结果、发生时间戳。
输出：治理结果、快照统计、运行日志。
读结构：旧任务状态、需求父子角色、筹办回执、方法运行结果。
写结构：内存治理结果和工作线程快照；同步任务虚拟存在函数当前为空实现。
是否写机器事实：否，工作线程只填写建议和结果；真实写入由管理界面线程或提交入口负责。
是否依赖日志：是。
是否依赖显示：是，推导界面任务状态。
是否依赖 SQL：否。
是否依赖线程：是。
是否依赖外设 / D455 / 体素：否。
保留行为：工作线程不应直接写任务虚拟存在或任务状态，只生成建议结果。
删除行为：不得迁移“界面任务状态”推导为权威任务状态。
新增约束：任务状态必须由任务服务记录生命周期实例状态；候选 / 当前方法由任务服务关系或后续任务管理入口提交。
目标新结构：任务生命周期状态、任务选择方法关系、任务管理治理结果后续结构。
目标领域服务入口：任务服务后续回执提交入口、任务管理服务。
对应规范 / 详细设计：任务系统详细设计、领域服务授权写入规范。
入口拒绝条件：任务无效、需求无效、回执缺方法复核、界面状态被事实化、工作线程越权写任务虚拟存在。
验证方式：工作线程回执不直接改变任务事实；任务服务读回才算结构变化。
初步判定：拆分
待用户确认项：工作线程和管理界面线程边界是否后续统一重设为单线程任务管理服务。
扫描证据：`任务模块.管理工作线程.ixx:555` 至 `任务模块.管理工作线程.ixx:743`。
```

### OF-148

```text
旧函数编号：OF-148
扫描批次：FS-05 / FS-X
旧项目路径：D:\鱼巢\任务模块.管理工作线程.ixx:113
旧函数名：任务管理外设消息承接_处理一次 / 外设消息承接处理状态文本 / 构造任务管理外设消息承接结果摘要
旧函数签名：外设观察等待项匹配、观察事实承接请求和稳定特征承接请求生成函数族。
当前调用方：旧提交任务工作项和外设观察等待流程。
触发路径：任务工作项带外设观察等待项时，匹配外设观察报告，生成观察事实承接请求或稳定特征承接请求，并标记报告消费。
输入：外设观察等待项、外设观察报告队列。
输出：外设消息承接结果、上行消息集、观察事实承接请求。
读结构：旧外设观察报告、等待项、目标观察约束、目标存在、目标特征类型。
写结构：上行消息、外设报告消费标记、运行日志。
是否写机器事实：待确认；当前应视为外部材料承接请求，不是世界事实。
是否依赖日志：是。
是否依赖显示：可服务诊断显示。
是否依赖 SQL：否。
是否依赖线程：是，被任务管理工作线程调用。
是否依赖外设 / D455 / 体素：是，依赖外设观察报告。
保留行为：外部观察只能作为材料或请求。
删除行为：第一版不接外设；不得把外设报告直接变成世界事实、需求满足或任务完成。
新增约束：外设 / D455 / 体素必须另建材料接入规范、详细设计和验收样本。
目标新结构：无当前目标结构；后续外设材料入口。
目标领域服务入口：后续外设材料服务 / 显示层只读视图，不属于当前 FS-05 迁移。
对应规范 / 详细设计：规则迁移清单 FS-X 暂缓项，函数迁移前置专项。
入口拒绝条件：本轮直接拒绝外设接入、D455 接入、体素接入和报告事实化。
验证方式：只登记排除边界，不运行外设队列，不生成新项目结构。
初步判定：排除 / 后续专项
待用户确认项：后续是否单独建立外设材料接入专项。
扫描证据：`任务模块.管理工作线程.ixx:113` 至 `任务模块.管理工作线程.ixx:140`、`任务模块.管理工作线程.ixx:3542` 至 `任务模块.管理工作线程.ixx:3703`。
```

### OF-149

```text
旧函数编号：OF-149
扫描批次：FS-05 / FS-09
旧项目路径：D:\鱼巢\任务模块.运行包.ixx:21
旧函数名：包已封口 / 校验包头 / 校验任务包 / 校验结算包 / 同步运行包语义槽 / 封口任务包 / 封口结算包
旧函数签名：线程运行包、一步治理任务包、回流结算包、语义槽同步、校验和封口函数族。
当前调用方：旧任务管理线程、运行包治理、回流结算和事件恢复路径。
触发路径：构造一步治理任务包，写来源需求 / 任务、动作类型、候选方法、授权约束、状态变化和回流结算结论，随后校验和封口。
输入：任务包、结算包、封口时间、错误输出。
输出：校验 bool、封口 bool、错误文本。
读结构：运行包内主键、时间戳、授权约束、候选方法、状态变化和结算材料。
写结构：运行包状态、封口时间、抽象特征指针和值镜像。
是否写机器事实：旧项目中作为线程运行材料；新项目当前不能作为权威事实。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：是。
是否依赖外设 / D455 / 体素：否。
保留行为：运行包可作为后续事件日志 / 持久化恢复材料参考。
删除行为：不得让线程运行包绕过领域服务裁决任务、方法、动作或结算事实。
新增约束：数据库 / 事件日志 / 恢复材料只作审计和恢复，不直接裁决运行期事实。
目标新结构：事件日志、持久化恢复材料、非权威审计包。
目标领域服务入口：事件日志持久化恢复专项。
对应规范 / 详细设计：日志系统规范、函数迁移前置专项、后续事件日志持久化恢复专项。
入口拒绝条件：包未封口却作为事实读取、缺来源任务 / 方法、授权约束 bool 被事实化、结算包替代需求结算记录。
验证方式：运行包只登记后续专项，不进入当前任务服务代码。
初步判定：排除 / 后续专项
待用户确认项：运行包是否作为事件日志持久化恢复专项的输入参考。
扫描证据：`任务模块.运行包.ixx:21` 至 `任务模块.运行包.ixx:218`。
```

### OF-150

```text
旧函数编号：OF-150
扫描批次：FS-05 / FS-06 / FS-07 / FS-08 / FS-09
旧项目路径：D:\鱼巢\任务模块.治理协议.ixx:20
旧函数名：任务管理治理协议枚举与结构族
旧函数签名：任务管理功能域、缺口类型、下一步去向、执行前门控、方法来源、原语判定、二次特征证据、高阶排序、任务管理请求 / 单步决策 / 结果、治理恢复事件镜像和恢复快照结构族。
当前调用方：旧任务管理工作线程、管理线程协议、运行包和恢复治理流程。
触发路径：任务管理根据触发事件、二次特征证据、原语判定、高阶排序和控制承接形成单步决策与治理结果。
输入：宿主任务、触发事件、二次特征证据、控制承接、高阶排序请求、恢复事件。
输出：任务管理请求、单步决策、治理结果和恢复快照。
读结构：旧任务、需求、方法、状态、动态、因果、控制请求和运行包材料。
写结构：协议结构体；自身不直接落库。
是否写机器事实：协议结构本身不是事实；被提交入口消费后可能形成事实。
是否依赖日志：否。
是否依赖显示：可服务诊断显示。
是否依赖 SQL：否。
是否依赖线程：是，服务旧任务管理线程。
是否依赖外设 / D455 / 体素：间接可承接外部反馈。
保留行为：任务管理协议中“二次特征证据 -> 原语判定 -> 单步决策 -> 治理结果”的分层可作为后续设计参考。
删除行为：不得把旧 bool 证据字段、线程触发来源或控制请求直接迁移为权威事实。
新增约束：后续任务管理增强必须先确认权威结构、非权威缓存、事件日志和显示边界。
目标新结构：任务管理后续增强结构、二次特征证据、事件日志恢复材料、非权威统计。
目标领域服务入口：需求树 / 任务管理 / 方法系统后续增强专项。
对应规范 / 详细设计：任务系统详细设计、二次特征服务详细设计、非权威缓存与统计详细设计、事件日志持久化恢复专项待建。
入口拒绝条件：协议 bool 被事实化、触发来源为线程却被当动作来源、控制请求越权写任务事实、恢复快照绕过领域服务。
验证方式：只登记协议边界，不实现任务管理状态机或恢复系统。
初步判定：排除 / 后续专项
待用户确认项：治理协议是否拆入任务管理增强、二次特征治理、事件恢复三个专项。
扫描证据：`任务模块.治理协议.ixx:20` 至 `任务模块.治理协议.ixx:630`。
```

## 4. 本轮初步分流

```text
可保留方向：
- 任务筹办只形成结构化回执，不直接作为动作来源。
- 任务执行只承接已筹办任务，不直接登记为本能方法。
- 执行桥必须检查方法动作、可执行入口、条件节点、结果节点、条件结果对和方法可用性。
- 方法调用动作动态需要输入 / 输出场景、开始 / 结束时间、执行结果和错误码。
- 工作线程只生成建议和回执，不应直接写任务虚拟存在或任务状态。

必须删除或改写：
- 旧任务筹办 / 执行函数体直接迁移。
- 旧工作线程、std::async、线程快照和界面任务状态作为权威任务事实。
- 旧外设观察报告直接承接为世界事实。
- 旧运行包或治理协议 bool 字段直接裁决任务完成、方法成功、需求满足或动作来源。

后续专项：
- 需求树 / 任务管理 / 方法系统后续增强专项：筹办回执、候选方法、派生需求、任务管理治理协议。
- 动作入口 / 动态后续增强专项：方法调用动作动态和执行结果证据。
- 事件日志持久化恢复专项：运行包、回流结算包、治理恢复事件镜像和恢复快照。
- 非权威缓存与统计专项：工作线程快照、推进次数、失败次数和最近状态文本。
- 显示层只读视图专项：任务管理诊断视图。
- FS-X 排除：外设观察报告、D455、体素和控制面板接入。
```

## 5. 禁止宣称

```text
不得声明旧任务筹办、任务执行、任务管理工作线程或任务治理协议已迁移。
不得声明完整任务状态机、任务筹办、任务执行、方法候选或调度线程完成。
不得把旧运行包、工作线程快照、回执 bool、日志文本或外设报告当作权威机器事实。
不得接控制面板 / SQL / D455 / 体素 / 外设。
不得声明旧函数扫描已完成；后续若发现遗漏只能追加登记。
```
