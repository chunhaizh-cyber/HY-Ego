# TASK-LIFECYCLE-S1 关系枚举负例顺延回归修订 Codex 断点清单

日期：2026-07-12

治理编号：JY-297

对应队列：#221 / DQ-113

状态：文档修订完成 / 执行窗口代码草案保留未暂存 / Debug x64 0 警告 0 错误 / 完整自检退出 1 / 待定向修改后继续验证 / 未归档 #221

## 1. 输入事实

```text
#221 已形成生产链、自检模块和最终回归阶段 560 草案。
Debug x64 构建为 0 警告 0 错误。
完整自检退出 1；当前唯一明确失败为 CONCEPT-NAMING-S2-A2。
既有 `海中鱼巣/领域/自检.概念命名目标.ixx` 把 `static_cast<关系类型>(14)` 固定作为未知关系负例。
#221 已正式追加 `关系类型::任务生命周期 = 14`，因此该负例现在会合法创建。
```

上述构建和运行结果来自执行窗口回传；本设计治理没有修改 C++、没有运行构建或程序。

## 2. 裁决

1. 关系 13 继续是 `需求目标概念` 正式值。
2. 关系 14 是 #221 新增的 `任务生命周期` 正式值。
3. 未知关系负例顺延为 `static_cast<关系类型>(15)`。
4. 只允许修改 `自检.概念命名目标.ixx` 中这一负例及对应 13 / 14 / 15 断言映射。
5. 不得修改 #212 的生产语义、共享抽象状态、关系 13 创建 / 读回、生命周期 / 删除计数分流或其它 CONCEPT-NAMING-S2 验收正文。

## 3. 允许文件增量

```text
海中鱼巣/领域/自检.概念命名目标.ixx
```

该文件的允许范围仅为：

```text
未知关系负例 14 -> 15
保留 / 补齐 13 = 需求目标概念
保留 / 补齐 14 = 任务生命周期
验证 15 创建被拒绝且有效关系数量不变
```

执行窗口当前其它 #221 允许范围代码草案继续按既有计划管理；本修订不授权扩大任何生产文件或测试语义。

## 4. 验收映射

| 验收项 | 修订后要求 |
| --- | --- |
| TASK-LIFECYCLE-S1-A01 | 关系 13 / 14 为正式枚举，15 为未知拒绝；不得用连续区间放过 15 |
| TASK-LIFECYCLE-S1-A12 | CONCEPT-NAMING-S2-A2 与 TASK、DEMAND、METHOD、SELF-GOV、PANEL、结构事务完整回归通过 |
| CONCEPT-NAMING-S2-A2 | 关系 13 创建 / 读回保持；关系 14 正式准入；关系 15 创建失败且有效关系数量不变 |

## 5. 执行与验证顺序

```text
从当前 #221 未暂存草案继续
-> 只改既有概念命名自检的关系负例映射
-> Debug x64
-> 完整 --self-test-exit
-> TASK-LIFECYCLE-S1 A01-A13
-> CONCEPT-NAMING-S2-A2 与关联回归
-> 严格规范 / diff / static
-> 连续 20 轮完整自检
-> 全部通过后再治理、归档、精确暂存、提交和推送
```

任一验证仍失败时继续按真实根因退回，不得改测试绕过生产缺陷、伪造成功或缩减完整回归。

## 6. 三件套一致性

本修订只纠正测试枚举值映射，不改变任务创建、绑定、写关系、状态发布、读回或失败收口流程，因此任务流程图 MD / HTML 不需要改动。详细设计 TL-D34、15.8 与 #221 JY-297 章节已经同步限定同一范围。

## 7. 完成声明边界

当前只能声明 #221 草案已编译，完整自检发现并定位一项关系负例映射漂移，设计窗口已给出定向修订合同。不得声明 #221、最终回归 560、任务唯一当前态、任务完整闭环或旧任务能力迁移完成。
