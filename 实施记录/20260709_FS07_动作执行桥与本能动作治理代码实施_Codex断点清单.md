# FS-07 动作执行桥与本能动作治理代码实施 Codex 断点清单

日期：2026-07-09

## 1. 执行项

```text
项目记忆/Codex任务队列.md #151
计划/已完成计划/20260707_FS07_动作执行桥与本能动作治理代码实施切片_v0.1.md
```

本次执行前已读取计划、动作执行桥与本能动作治理详细设计、动作入口详细设计、方法执行动作入口代码逻辑详细设计、方法执行动作入口流程图和 HTML 流程图。

## 2. 代码改动

```text
海中鱼巣/领域/方法服务.h
海中鱼巣/入口.cpp
```

实现内容：

```text
新增 `本能动作注册材料`、`方法动作执行桥请求材料`、`方法动作动态证据材料`。
新增 `读取本能动作注册材料`，并让 `本能动作注册材料是否有效` 复用该只读入口。
新增 `读取动作执行桥请求材料`，只在任务到方法引用、主动动作入口、稳定动作键和规格材料均成立时返回材料。
新增 `读取动作动态证据材料`，读回动态证据、来源动作、来源方法关系和发生时间戳。
默认入口新增 FS07-NEXT-A1 到 FS07-NEXT-A8 验收，覆盖主动桥请求、注册材料、被动入口拒绝、拒绝只读、动态证据、轻量因果准入、依赖扫描和排除项扫描。
```

未修改：

```text
核心仓库
任务服务
需求服务
特征值服务
工程文件
旧项目源码
SQL / ADO / WebView / D455 / 体素 / 外设文件
```

## 3. 验证

```powershell
git diff --check
C:\Users\zhchh\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe .\tools\check_specs.py
msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m
.\x64\Debug\海中鱼巣.exe
rg -n "函数指针|std::thread|std::async|工作队列|D455|体素|控制面板|SQL|WebView|外设" 海中鱼巣/领域 海中鱼巣/入口.cpp
rg -n -F "特征值服务" 海中鱼巣/领域/方法服务.h 海中鱼巣/领域/任务服务.h
```

验证结果：

```text
git diff --check 通过，仅提示工作副本 LF 后续会被 Git 触碰为 CRLF。
tools/check_specs.py 通过：未发现问题。
Debug x64 构建通过：0 警告，0 错误。
默认入口返回 0。
默认入口输出 `FS-07-NEXT 动作执行桥与本能动作治理第一轮: 通过`。
FS07-NEXT-A1 到 FS07-NEXT-A8 全部输出 `通过`。
`特征值服务` 依赖扫描在方法服务和任务服务无命中。
排除项扫描只命中既有控制面板只读服务和入口人读输出标签；本次未新增真实线程、工作队列、函数指针、SQL、WebView、D455、体素或外设能力。
```

## 4. 完成边界

本次只能声明：

```text
FS-07 动作执行桥与本能动作治理第一轮代码验收入口已实现并验证。
```

不得声明：

```text
真实动作执行桥已实现。
本能动作执行系统已实现。
线程调度、std::thread、std::async、工作队列、函数指针注册表、外部执行器或外设动作已接入。
任务结果回写、需求结算或稳定因果结论已完成。
旧动作能力已迁移。
控制面板 / SQL / ADO / WebView / D455 / 体素 / 外设已接入。
```
