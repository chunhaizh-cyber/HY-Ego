# SELFTEST-PERF-GATE-S1 功能回归与性能验收分离代码实施 Codex 断点清单

日期：2026-07-16

计划 / 队列 / 阶段：#280 / DQ-172 / SELFTEST-PERF-GATE-S1 / 795

决策 / 问题：JY-360 / QR-185 已关闭

状态：已完成 / A01-A14 通过 / 795 已登记 / 已构建 / 已运行 / 待随本次提交归档

## 1. 输入与边界

```text
正式基线：f7cf72196bb6c54ad64e97224c38c8897f82addc
流程图/20260716_默认功能回归与显式性能验收分离流程图_v0.1.md/.html
规范/详细设计/默认功能回归与显式性能验收分离详细设计.md
计划/已完成计划/20260716_SELFTEST-PERF-GATE-S1_功能回归与性能验收分离代码实施切片_v0.1.md
实施记录/20260716_SELFTEST-PERF-GATE-S1_性能门禁分离设计与前置计划生成_Codex断点清单.md
WIN-0001-E76 / JY-360
```

实施前已确认执行工作树 dirty 集恰好为 #261 七路径且暂存区为空，并按固定说明创建包含未跟踪模块的本地可逆 stash。对象身份为：

```text
12cda61fead9cae3891fff040b2701f4cab0932e
```

#280 完成后该 stash 保持未恢复、未删除、未进入提交。#258 四文件救援 stash 未读取、恢复、修改、提交或删除。

## 2. 实施结果

新增 Debug 真模块 `海中鱼巣.自检.性能门禁分离`，只接收值式的性能请求、790 登记、执行、结果和汇总证据。模块验证默认 / 显式模式唯一、条件登记、真实性能失败不被吞掉、退出汇总和既有参数独立性，不访问仓库、不复制性能算法。

`入口.cpp` 新增以下最小接线：

```text
--self-test-exit
-> Debug 不登记 790
-> 登记并执行 795
-> 功能自检决定退出码

--warehouse-performance-self-test-exit
-> Debug 条件登记并执行原 790
-> 790 后执行 795，汇总请求 / 登记 / 执行 / 结果
-> 功能或性能任一失败均退出 1

Release 显式性能请求
-> 结构装配和运行日志前拒绝
-> 退出 2
```

同时出现两个自检参数时，参数顺序无关，均进入显式性能严格超集。工程和 filters 各唯一登记新模块。

## 3. 默认功能回归

Debug x64 默认 `--self-test-exit` 证据固定为：

```text
SELFTEST-PERF-GATE-S1-EVIDENCE requested=0 registered=0 executed=0 passed=0 gate=1
WAREHOUSE-OPT-S0 输出命中：0
关系查询与并发性能基线输出命中：0
退出码：0
```

最终同一 Debug 二进制连续 20/20 通过。每轮均退出 0、正式性能标识命中 0，并具有相同的 795 默认证据。headless 和数据库自检模式也分别退出 0 且不登记性能阶段；非 SDK D455 真实样本请求继续按原义提前退出 2。

## 4. 显式性能验收

三次显式 Debug 运行均执行原 WAREHOUSE-OPT-S0 A01-A12，并保持三档结构签名：

```text
1,000   c4704f13e23e41b9
10,000  f44904a873488f65
100,000 2e595d5cba78a2c1
```

| 参数 | 三档波动 | 原性能结果 | 795 证据 | 退出码 |
| --- | --- | --- | --- | ---: |
| 仅显式性能参数 | 0.7% / 10.8% / 11.0% | A01-A12 全部通过 | `requested=1 registered=1 executed=1 passed=1 gate=1` | 0 |
| 默认参数在前、显式参数在后 | 1.3% / 19.3% / 68.7% | A09 / A10 / A12 失败 | `requested=1 registered=1 executed=1 passed=0 gate=0` | 1 |
| 显式参数在前、默认参数在后 | 10.8% / 3.9% / 42.2% | A09 / A10 / A12 失败 | `requested=1 registered=1 executed=1 passed=0 gate=0` | 1 |

后两次失败按原 15% 稳定门槛真实保留，没有降级为警告或功能通过。本切片不据首轮成功宣称性能稳定通过，也不改变 #257 正式基线。

## 5. A01-A14

| 验收 | 结果 |
| --- | --- |
| A01 | #261 七路径已精确 stash；对象身份已记录；#258 stash 未触碰 |
| A02-A03 | 默认 Debug 不登记 / 不执行 790，完整功能自检退出 0，中央总成自然守恒 |
| A04-A05 | 显式 Debug 恰好登记 / 执行一次原 790；退出码与真实性能结果一致 |
| A06 | 两种双参数顺序均执行 12 项性能断言，登记 / 执行证据一致 |
| A07 | headless、数据库和非 SDK D455 参数原义不变且不隐式执行性能阶段 |
| A08 | Release 默认自检退出 0；显式性能请求退出 2，且在结构装配前拒绝 |
| A09-A10 | 新真模块、795、工程和 filters 唯一；入口只增加模式、登记、一次调用和汇总 |
| A11 | 性能模块、运行器、关系仓库、阈值、签名和 790 实现差异为 0 |
| A12 | Debug / Release x64 完整 Rebuild 均为 0 警告、0 错误；默认 Debug 连续 20/20 |
| A13 | 严格规范、diff、参数 / 阶段 / 模块唯一性、Release 二进制和允许范围扫描通过 |
| A14 | 795 登记；#261 stash 保持未恢复，返回设计窗口重读正式接口 |

## 6. 输出与声明边界

#280 / DQ-172 已完成并归档，795 已登记。#261 的前置已满足，但执行窗口不得自动恢复其 WIP、不得登记 830；下一步只允许设计窗口从本提交重读入口、工程和阶段接口后重新发布 #261。#214 继续用户暂停。

本完成声明只证明默认功能回归与显式关系仓库性能验收已形成独立运行和真实退出码合同。不得据此宣称关系仓库性能稳定通过、性能优化、二级索引、#261、830、完整控制面板、旧能力迁移或项目迁移完成。
