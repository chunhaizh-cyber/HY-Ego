# FS-03 特征与特征值入口只读扫描记录

日期：2026-07-06
状态：FS-03 第一批已登记 / 只读扫描 / 未改代码 / 未迁移旧函数
依据清单：`实施记录/20260706_逐函数只读扫描清单.md`

## 1. 本轮定位

本轮进入 FS-03，登记旧项目 `特征类`、`特征值类` 和 `特征值主信息类` 中与特征身份、抽象特征、实例特征、特征值写入读取、值域匹配、稳态缓存、VecU / VecI64 值容器和三维体素值材料有关的函数族。

本轮只做只读事实登记，不迁移旧 `特征节点主信息类`、`抽象特征主信息类`、`特征值主信息类` 字段，不把 VecU、指针句柄或体素材料提前接入当前项目。当前项目边界仍是：特征值服务只服务特征系统内部，只有特征服务可直接访问；需求、任务、方法等高级服务只能通过特征服务读取或写入特征状态材料。

## 2. 只读扫描范围

```text
D:\鱼巢\特征类.h
D:\鱼巢\特征类.cpp
D:\鱼巢\特征值类.h
D:\鱼巢\特征值主信息类.h
```

扫描证据来自：

```text
特征类.cpp:1-5：旧特征文件头部规则，文本不得承载特征状态、值域归属或机器裁决。
特征类.h:20-57：稳态策略、写入参数、特征写入结果和特征比较结果结构。
特征类.h:63-208：VecU 解释规则、轮廓、三维体素和局部相似度结构。
特征类.h:220-258、特征类.cpp:497-725：特征服务绑定、特征主信息 / 抽象特征主信息读取、主键解析、语义类型读取、抽象特征创建 / 查找 / 校验。
特征类.cpp:729-1896：常用抽象特征初始化和任务 / 方法状态抽象特征兼容壳。
特征类.cpp:2232-2369：实例特征创建、按类型查找、删除、枚举、子特征读取、当前值读取和复制子特征槽位。
特征类.cpp:2374-2588：抽象特征值域命中、实例特征匹配和按抽象特征查找 / 创建实例特征。
特征类.cpp:2591-2936：特征值写入、I64 / VecU / 指针 / 体素写入、读取、清空、稳态列表和局部区间覆盖。
特征类.cpp:2951-3074：组合抽象特征求值和特征值比较。
特征类.cpp:3086-4072：VecU 解释、三维体素占据位、体素链、轮廓融合和相似度相关函数。
特征值类.h:15-180：特征值节点获取或创建、VecU / VecI64 句柄、内容哈希、缓存和只读指针。
特征值类.h:182-260：轮廓金字塔、分层索引和内容哈希辅助。
特征值主信息类.h:7-23：旧特征值主信息权威本体为原始 VecU 值，内容哈希、命中次数和最后命中只是缓存。
特征值主信息类.h:25-79：特征值主信息比较和命中比较条件。
特征值主信息类.h:95-146：特征值主信息序列化和反序列化。
```

## 3. 逐函数登记

### OF-106

```text
旧函数编号：OF-106
扫描批次：FS-03
旧项目路径：D:\鱼巢\特征类.cpp:497
旧函数名：特征类::绑定基础信息 / 设置差异度函数 / 取特征主信息 / 按主键解析特征节点 / 读取特征语义类型
旧函数签名：特征服务绑定、差异度回调、特征主信息读取、按主键解析和语义类型读取函数族。
当前调用方：旧特征创建、实例特征匹配、需求目标比较、动态差异度、方法候选和显示命名。
触发路径：旧基础信息树按主信息类型或主键识别特征节点，并读取语素入口语义类型。
输入：基础信息类指针、特征节点、主键字符串、差异度函数。
输出：特征主信息指针、特征节点指针、语义类型入口或空。
读结构：旧基础信息树、旧特征主信息、旧主键索引、旧语素入口。
写结构：`绑定基础信息` 和 `设置差异度函数` 只写运行期服务指针 / 回调；其它函数只读。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：语义类型可服务显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：特征节点必须按稳定身份读取，并复核节点类型和语义类型。
删除行为：不得迁移旧回调函数或运行期裸指针作为权威结构。
新增约束：新项目特征类型由特征节点承载，语素入口只做名称、查找或初始化种子。
目标新结构：节点类型::特征、特征节点句柄、特征到特征值归属关系、语素对应信息关系。
目标领域服务入口：特征服务 `创建特征类型`、`读取I64特征值`、`特征值属于特征`。
对应规范 / 详细设计：领域服务最小入口详细设计、需求目标状态承载详细设计、语素系统详细设计。
入口拒绝条件：基础服务缺失、主键为空、句柄无效、节点类型不是特征、语义入口无效。
验证方式：索引命中后复核节点类型；错误输入返回空且结构数量不变。
初步判定：改写为领域服务入口
待用户确认项：是否补特征语义类型只读入口。
扫描证据：`特征类.cpp:497` 至 `特征类.cpp:547`。
```

### OF-107

```text
旧函数编号：OF-107
扫描批次：FS-03
旧项目路径：D:\鱼巢\特征类.cpp:548
旧函数名：取抽象特征主信息 / 创建抽象特征 / 创建叶子抽象特征 / 创建复合抽象特征 / 创建抽象特征_按类型 / 查找子抽象特征 / 校验抽象特征子链 / 取或创建子抽象特征_按类型
旧函数签名：抽象特征主信息读取、抽象特征创建、值域 / 复合结构和子链校验函数族。
当前调用方：旧常用抽象特征初始化、实例特征匹配、任务 / 方法状态抽象特征和需求目标比较。
触发路径：根据语素入口类型和值域创建抽象特征树，并校验复合子链名称唯一。
输入：父抽象特征、语素入口名称 / 类型、特征值域、I64 区间。
输出：抽象特征节点或局部 bool。
读结构：旧抽象特征主信息、旧普通父子链、旧语素入口和值域。
写结构：旧抽象特征节点、旧抽象特征主信息、旧普通父子链。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：语素名称可服务显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：抽象特征表达特征类型和值域约束，可用于实例特征匹配。
删除行为：不得直接迁移旧抽象特征主信息字段和值域变体为当前第一轮权威字段。
新增约束：当前第一轮只确认特征节点和 I64 状态材料；完整抽象特征和值域系统进入特征增强专项。
目标新结构：特征节点、二次特征节点、状态目标值、特征值归属关系、后续抽象特征关系。
目标领域服务入口：特征服务后续抽象特征入口、状态服务目标状态入口。
对应规范 / 详细设计：需求目标状态承载详细设计、二次特征服务详细设计、领域服务授权写入规范。
入口拒绝条件：父节点无效、语素入口无效、值域非法、复合子链重复、特征类型未确认。
验证方式：创建后节点有效且类型可读；值域缺失不得伪造成需求目标。
初步判定：排除 / 后续专项
待用户确认项：抽象特征树和值域系统是否进入“特征 / 特征值系统增强专项”。
扫描证据：`特征类.cpp:548` 至 `特征类.cpp:725`。
```

### OF-108

```text
旧函数编号：OF-108
扫描批次：FS-03 / FS-05 / FS-06
旧项目路径：D:\鱼巢\特征类.cpp:729
旧函数名：初始化常用抽象特征 / 常用抽象特征已初始化
旧函数签名：常用抽象特征、任务状态和方法状态抽象特征兼容壳初始化函数族。
当前调用方：旧世界初始化、任务状态识别、方法状态识别和状态匹配。
触发路径：按固定语素名称创建任务 / 方法状态等抽象特征树，并检测初始化壳是否存在。
输入：抽象特征根。
输出：局部 bool。
读结构：旧抽象特征树、旧语素入口、旧枚举状态值。
写结构：旧抽象特征节点、旧主信息和普通父子链。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：固定语素名称可服务显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：任务 / 方法等状态枚举需要有结构化状态或抽象状态承载。
删除行为：不得通过旧固定语素树初始化来迁移任务状态机或方法状态机。
新增约束：任务 / 方法状态已由任务服务、方法服务和状态服务按各自详细设计承载。
目标新结构：抽象状态、实例状态、任务生命周期状态、方法角色抽象状态。
目标领域服务入口：状态服务、任务服务、方法服务。
对应规范 / 详细设计：任务系统详细设计、方法系统详细设计、状态服务详细设计。
入口拒绝条件：抽象特征根无效、语素入口缺失、试图用初始化壳替代任务 / 方法状态事实。
验证方式：只登记固定状态语义；不新增任务或方法结构。
初步判定：排除 / 后续专项
待用户确认项：旧常用抽象特征初始化中哪些状态语义仍需进入状态字典专项。
扫描证据：`特征类.cpp:729` 至 `特征类.cpp:1896`。
```

### OF-109

```text
旧函数编号：OF-109
扫描批次：FS-03
旧项目路径：D:\鱼巢\特征类.cpp:2232
旧函数名：创建特征 / 创建特征_按类型 / 取或创建子特征_按类型 / 删除特征 / 枚举全部特征 / 获取子特征 / 查找子特征_按类型 / 查找子特征_按名称 / 读取子特征值_按类型 / 复制子特征到宿主
旧函数签名：实例特征创建、查找、删除、枚举、子特征槽位和当前值读取函数族。
当前调用方：旧存在特征、需求目标、状态记录、方法执行和特征槽位复制。
触发路径：在宿主下创建或查找按类型命名的实例特征，并读取 / 复制当前值槽位。
输入：宿主基础信息节点、语素入口特征类型 / 名称、当前值。
输出：特征节点、特征值、节点集合、复制数量或局部 bool。
读结构：旧基础信息树、旧特征主信息、旧普通父子链。
写结构：旧特征节点、旧特征主信息、旧普通父子链；删除会写生命周期。
是否写机器事实：创建 / 删除是，读取否。
是否依赖日志：否。
是否依赖显示：名称可服务显示。
是否依赖 SQL：否。
是否依赖线程：否；内部互斥只保护结构和值写入。
是否依赖外设 / D455 / 体素：否。
保留行为：按宿主和特征类型创建特征槽位，并能读取当前特征状态材料。
删除行为：不得把旧宿主子链和旧主信息当前值字段直接迁移为权威字段。
新增约束：特征值节点必须通过特征服务创建并与特征建立归属关系；高级服务不得直接访问特征值服务。
目标新结构：特征节点、特征值节点、特征到特征值归属关系、主信息 I64 状态材料。
目标领域服务入口：特征服务 `创建特征类型`、`创建特征值`、`写入I64特征值`、`读取I64特征值`。
对应规范 / 详细设计：需求目标状态承载详细设计、领域服务最小入口详细设计、领域服务授权写入规范。
入口拒绝条件：宿主无效、特征类型无效、特征值不归属目标特征、删除后仍可读为有效特征。
验证方式：特征值归属关系可读；非归属特征值写入拒绝。
初步判定：改写为领域服务入口
待用户确认项：旧宿主子特征槽位复制是否进入后续特征系统增强专项。
扫描证据：`特征类.cpp:2232` 至 `特征类.cpp:2369`。
```

### OF-110

```text
旧函数编号：OF-110
扫描批次：FS-03 / FS-06
旧项目路径：D:\鱼巢\特征类.cpp:2374
旧函数名：抽象特征是离散点枚举 / 特征值命中抽象特征值域 / 特征值命中值域 / 匹配抽象特征 / 查找实例特征_按抽象特征 / 取或创建实例特征_按抽象特征
旧函数签名：值域命中、抽象 / 实例特征匹配和按抽象特征创建实例特征函数族。
当前调用方：旧状态匹配、需求目标投影、方法条件、二次特征和因果模板匹配。
触发路径：比较实例特征当前值是否落入抽象特征值域或复合子特征结构。
输入：抽象特征、实例特征、特征值、宿主。
输出：局部 bool、实例特征节点。
读结构：旧抽象特征主信息、旧特征主信息、旧值域、旧特征当前值。
写结构：按抽象特征创建实例特征时写旧特征节点和主信息。
是否写机器事实：匹配读取否，创建实例特征是。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：值域和复合结构匹配可作为后续方法条件或需求目标判断候选。
删除行为：不得把局部 bool 匹配结果直接作为需求满足、任务完成或方法可执行事实。
新增约束：需求目标是目标状态；方法条件 / 结果需由方法角色节点和场景关系承载。
目标新结构：目标状态、实例状态、方法条件 / 结果角色节点、二次特征组成关系。
目标领域服务入口：需求服务、方法服务、状态服务、二次特征服务后续增强入口。
对应规范 / 详细设计：需求目标状态承载详细设计、方法系统详细设计、二次特征服务详细设计。
入口拒绝条件：抽象特征无效、实例特征无效、值域非法、复合子结构不完整、匹配结果被事实化。
验证方式：匹配只返回候选；业务事实必须由状态、动态、需求结算或方法结构写入。
初步判定：排除 / 后续专项
待用户确认项：值域匹配是否进入方法条件判定索引和需求结算规则专项。
扫描证据：`特征类.cpp:2374` 至 `特征类.cpp:2588`。
```

### OF-111

```text
旧函数编号：OF-111
扫描批次：FS-03 / FS-08
旧项目路径：D:\鱼巢\特征类.cpp:2591
旧函数名：写入特征值_按参数 / 写入特征值 / 写入特征值_I64 / 写入特征值_VecU句柄 / 写入特征值_指针句柄 / 读取特征值 / 枚举命中抽象状态 / 解析实例特征命中抽象特征 / 设置实例特征局部区间覆盖 / 清除实例特征局部区间覆盖 / 升格当前稳态为抽象区间 / 合并抽象区间
旧函数签名：特征当前值写入、稳态、局部区间覆盖和抽象区间升格函数族。
当前调用方：旧观察入账、方法执行、状态记录、需求结算、稳态学习和抽象特征扩展。
触发路径：把 I64、VecU 句柄或指针句柄写入旧特征主信息当前值，并更新统计、近帧候选和稳态列表。
输入：特征节点、特征值、时间戳、写入参数、稳态策略、区间。
输出：特征写入结果、局部 bool、特征值或抽象特征节点。
读结构：旧特征主信息、旧稳态列表、旧近帧缓存、旧抽象特征值域。
写结构：旧特征主信息当前值、统计缓存、近帧缓存、稳态列表、局部区间覆盖、抽象区间。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否；内部互斥只保护写入。
是否依赖外设 / D455 / 体素：否。
保留行为：I64 当前状态材料写入应保留为特征服务公开入口；写入应带可追溯时间材料。
删除行为：不得直接迁移 VecU / 指针句柄、稳态列表、近帧候选、统计缓存和局部区间覆盖为第一轮权威字段。
新增约束：第一轮只确认 I64 特征状态材料；缓存和稳态属于非权威缓存 / 特征增强专项。
目标新结构：特征值节点、主信息 I64 材料、动态前后值、状态节点、非权威缓存记录。
目标领域服务入口：特征服务 `写入I64特征值`、`读取I64特征值`；动态服务记录变化；非权威缓存统计后续入口。
对应规范 / 详细设计：需求目标状态承载详细设计、动态服务详细设计、非权威缓存与统计详细设计。
入口拒绝条件：特征无效、特征值无效、特征值不归属特征、缺时间戳、试图让缓存或稳态裁决业务事实。
验证方式：I64 写入后经特征服务读回；拒绝路径不改变特征状态材料。
初步判定：拆分
待用户确认项：VecU、指针句柄、稳态列表和局部区间覆盖是否统一进入特征 / 特征值系统增强专项。
扫描证据：`特征类.cpp:2591` 至 `特征类.cpp:2936`。
```

### OF-112

```text
旧函数编号：OF-112
扫描批次：FS-03 / FS-X
旧项目路径：D:\鱼巢\特征类.cpp:2763
旧函数名：写入三维体素特征值 / 读取三维体素特征值 / 清空三维体素特征值
旧函数签名：三维体素特征值写入、读取和清空函数族。
当前调用方：旧观察融合、体素存在模型、轮廓比较和显示诊断。
触发路径：把 VecU 句柄作为三维体素特征当前值写入或读出。
输入：特征节点、VecU 句柄、时间戳。
输出：局部 bool 或 VecU 句柄。
读结构：旧特征主信息当前值、旧特征值 VecU 句柄。
写结构：旧特征主信息当前值、统计缓存和稳态索引。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：体素可服务显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：是，体素材料属于明确排除边界。
保留行为：只登记体素特征材料需要通过特征服务封口。
删除行为：本轮不得接入三维体素值、体素链、D455 或外设观察。
新增约束：体素材料必须等待外设 / 体素专项确认，不能作为当前迁移函数。
目标新结构：后续体素专项特征值材料、只读观察证据或显示视图。
目标领域服务入口：暂缓；后续特征 / 特征值系统增强专项和外设 / 体素专项。
对应规范 / 详细设计：函数迁移前置专项、非权威缓存与统计详细设计、领域服务授权写入规范。
入口拒绝条件：当前本轮直接拒绝接入体素值；不得从旧 VecU 句柄恢复机器事实。
验证方式：只登记排除边界，不新增代码和结构。
初步判定：排除 / 后续专项
待用户确认项：体素值和 D455 观察材料是否单独进入外设 / 体素专项。
扫描证据：`特征类.cpp:2763` 至 `特征类.cpp:2788`。
```

### OF-113

```text
旧函数编号：OF-113
扫描批次：FS-03 / FS-X
旧项目路径：D:\鱼巢\特征类.cpp:2951
旧函数名：求值组合抽象特征 / 特征状态比较 / 存在集合比较 / VecU解释规则_按特征类型 / 校验坐标类VecI64_按特征类型
旧函数签名：复合特征求值、状态比较、存在集合比较和 VecU 解释规则函数族。
当前调用方：旧需求 / 任务判断、因果投影、观察轮廓比较和候选排序。
触发路径：按特征类型和值材料进行局部比较，输出关系、差值、评分或可比较状态。
输入：抽象特征、特征节点、存在集合、特征值、特征类型、VecI64。
输出：局部 bool、比较结果、差异度、VecU 解释规则。
读结构：旧特征主信息、旧抽象特征主信息、旧 VecU / VecI64 材料。
写结构：无。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：比较结果可服务诊断显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：部分 VecU / 体素解释属于后续排除边界。
保留行为：比较函数只可作为候选或诊断，不直接裁决需求满足或任务完成。
删除行为：不得把旧局部比较 bool、评分或差值直接迁移为业务事实。
新增约束：需求结算规则、方法条件判定和候选排序必须另行确认。
目标新结构：需求结算记录、状态 / 动态证据、二次特征候选、非权威缓存。
目标领域服务入口：需求服务后续结算规则、方法服务条件判定、非权威统计专项。
对应规范 / 详细设计：需求结算记录详细设计、方法系统详细设计、非权威缓存与统计详细设计。
入口拒绝条件：特征类型无效、值材料缺失、VecU 解释规则未确认、比较结果被事实化。
验证方式：比较只返回候选或局部结果；权威事实由结算记录或状态动态承载。
初步判定：排除 / 后续专项
待用户确认项：比较规则是否进入需求结算规则和方法条件判定专项。
扫描证据：`特征类.cpp:2951` 至 `特征类.cpp:3074`、`特征类.cpp:4040` 至 `特征类.cpp:4309`。
```

### OF-114

```text
旧函数编号：OF-114
扫描批次：FS-03 / FS-X
旧项目路径：D:\鱼巢\特征类.cpp:3086
旧函数名：三维体素最终边长 / 三维体素包围立方体细分层数 / 三维体素占据位块数量 / 创建三维体素占据位块 / 读写三维体素占据位 / 创建三维体素链节点VecU / 解析三维体素链节点VecU / 写入三维体素二分层链 / 查询三维体素二分层链 / 从多视角轮廓图生成三维体素 / 比较局部三维体素与三维体素链
旧函数签名：三维体素 VecU 编码、占据位、二分层链、轮廓融合和相似度函数族。
当前调用方：旧外设观察、D455 / 轮廓融合、体素存在模型和显示诊断。
触发路径：把多视角轮廓或长方体占据位编码为 VecU 特征值节点，并比较局部体素与存在体素链。
输入：体素尺寸、占据位块、VecU 句柄、轮廓图视角、融合参数、存在空间绑定参数。
输出：VecIU64、VecU 句柄、体素链写入结果、查询结果、融合结果、相似度结果。
读结构：旧特征值 VecU 节点、旧体素链节点、旧轮廓材料。
写结构：旧特征值节点、旧 VecU 子值节点、旧体素链。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：体素和轮廓可服务显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：是。
保留行为：仅登记体素值容器和轮廓融合为后续专项线索。
删除行为：本轮不得接入 D455、体素、外设或旧体素编码函数。
新增约束：外设材料只作为材料或请求，不直接成为世界事实。
目标新结构：后续外设观察材料、体素特征值、动态证据或显示只读视图。
目标领域服务入口：暂缓；外设 / 体素专项和显示层只读视图专项。
对应规范 / 详细设计：函数迁移前置专项、领域服务授权写入规范、非权威缓存与统计详细设计。
入口拒绝条件：当前本轮直接拒绝体素 / 外设接入。
验证方式：只登记排除项，不运行旧体素函数，不生成体素结构。
初步判定：排除 / 后续专项
待用户确认项：是否为体素值容器和 D455 观察另建专项入口。
扫描证据：`特征类.cpp:3086` 至 `特征类.cpp:4072`。
```

### OF-115

```text
旧函数编号：OF-115
扫描批次：FS-03 / FS-08
旧项目路径：D:\鱼巢\特征值类.h:15
旧函数名：特征值类::初始化 / 查找 / 获取或创建 / 获取或创建VecI64特征值节点 / 获取或创建句柄 / 取节点 / 创建值节点 / 创建子值节点 / 取VecU只读指针 / 取节点VecU只读指针
旧函数签名：VecU / VecI64 特征值节点去重、创建、句柄和只读指针函数族。
当前调用方：旧特征值写入、三维体素链、轮廓值容器和特征值去重缓存。
触发路径：按 VecU 内容哈希查找或创建特征值节点，并用指针式句柄指向主信息。
输入：VecIU64、VecI64、VecU 句柄、轮廓维度、父特征值节点。
输出：特征值节点、VecU 句柄、VecU 只读指针。
读结构：旧特征值链表、旧 VecU 原始值、内容哈希缓存。
写结构：旧特征值节点、旧特征值主信息、哈希候选缓存、分层索引节点。
是否写机器事实：是，旧项目把 VecU 原始值作为特征值主信息权威本体。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否；内部锁只保护链表和缓存。
是否依赖外设 / D455 / 体素：VecU 可承载体素，需按具体来源分流。
保留行为：特征值内容去重必须先比内容哈希，再复核原始值相同。
删除行为：不得迁移指针式 VecU 句柄作为长期身份；不得让高级服务直接调用特征值类。
新增约束：新项目特征值身份必须是稳定节点句柄；特征值服务只供特征服务内部访问。
目标新结构：节点类型::特征值、主信息 I64 或后续 Vec 原始值容器、特征到特征值归属关系、非权威内容哈希缓存。
目标领域服务入口：特征值服务内部入口、特征服务公开封口入口。
对应规范 / 详细设计：需求目标状态承载详细设计、非权威缓存与统计详细设计、领域服务授权写入规范。
入口拒绝条件：Vec 原始值容器未确认、句柄无效、内容哈希命中未复核原始值、高级服务直接访问特征值服务。
验证方式：特征服务外部只见特征值节点句柄和归属关系；缓存清空不影响权威值。
初步判定：拆分
待用户确认项：VecIU64 / VecI64 原始值容器是否进入特征 / 特征值系统增强专项。
扫描证据：`特征值类.h:15` 至 `特征值类.h:180`。
```

### OF-116

```text
旧函数编号：OF-116
扫描批次：FS-03 / FS-08
旧项目路径：D:\鱼巢\特征值类.h:182
旧函数名：私有_重建缓存_已加锁 / 哈希VecIU64 / 生成二维轮廓金字塔 / 分层索引辅助函数
旧函数签名：内容哈希、运行期哈希候选缓存、轮廓分层索引和金字塔辅助函数族。
当前调用方：旧特征值查找、轮廓近似查询、特征值去重和诊断。
触发路径：初始化或查找特征值时重建哈希缓存，并按 VecU / 轮廓材料建立近似索引。
输入：VecIU64、轮廓维度、特征值链表节点。
输出：哈希值、候选集合、分层索引节点或局部缓存。
读结构：旧特征值主信息原始 VecU 值。
写结构：运行期哈希缓存、轮廓分层索引缓存。
是否写机器事实：否，属于运行期缓存。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：轮廓材料可能来自外设，当前只登记。
保留行为：内容哈希只能用于缩小候选，不能替代原始值比较。
删除行为：不得把命中次数、最后命中、哈希候选或分层索引当业务事实。
新增约束：缓存命名空间和缓存记录结构需由非权威缓存与统计方案承载。
目标新结构：非权威缓存记录、缓存命名空间、特征值原始值后续容器。
目标领域服务入口：非权威缓存统计入口；特征值服务内部缓存。
对应规范 / 详细设计：非权威缓存与统计详细设计、错误结果与入口拒绝规范。
入口拒绝条件：缓存命名空间无效、原始值容器未确认、缓存命中未复核权威值。
验证方式：清空缓存后仍可由权威原始值重建；业务流程不依赖缓存存在。
初步判定：排除 / 后续专项
待用户确认项：特征值内容哈希是否等待 Vec 原始值容器确认后再实现。
扫描证据：`特征值类.h:182` 至 `特征值类.h:260`。
```

### OF-117

```text
旧函数编号：OF-117
扫描批次：FS-03 / FS-08 / FS-09
旧项目路径：D:\鱼巢\特征值主信息类.h:7
旧函数名：特征值主信息类::绝对相同 / 比较 / 生成比较量 / 命中比较条件 / 序列化 / 反序列化
旧函数签名：特征值主信息原始 VecU 比较、缓存字段比较和序列化函数族。
当前调用方：旧特征值去重、候选查询、持久化快照和诊断。
触发路径：按 VecU 原始值、内容哈希或命中次数比较特征值主信息，并序列化 VecU、内容哈希、命中次数和最后命中。
输入：特征值主信息、比较字段、输入输出流。
输出：比较量、局部 bool、反序列化主信息。
读结构：旧特征值主信息原始 VecU、内容哈希、命中次数、最后命中。
写结构：反序列化会构造旧主信息对象；不直接写仓库。
是否写机器事实：否；输出可能被后续仓库写入。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否；VecU 内容可能承载体素但本函数不解释。
保留行为：原始 VecU 值是旧特征值主信息权威本体；内容哈希和命中统计只是缓存。
删除行为：不得迁移内容哈希、命中次数、最后命中为业务事实；不得把旧序列化格式当当前事件恢复格式。
新增约束：当前第一轮特征值主信息只允许已确认值材料；Vec 原始值和事件恢复需后续专项确认。
目标新结构：特征值节点、后续 Vec 原始值容器、非权威缓存记录、事件日志 / 快照材料。
目标领域服务入口：特征值服务内部入口、非权威缓存统计入口、事件日志持久化恢复专项。
对应规范 / 详细设计：非权威缓存与统计详细设计、主信息系统详细设计、事件日志持久化恢复后续专项。
入口拒绝条件：反序列化元素数超限、缓存字段被事实化、旧序列化格式未经确认直接恢复。
验证方式：缓存字段清空不改变业务事实；反序列化失败不写结构。
初步判定：拆分
待用户确认项：Vec 原始值序列化是否进入事件日志持久化恢复专项。
扫描证据：`特征值主信息类.h:7` 至 `特征值主信息类.h:146`。
```

## 4. 本轮初步分流

```text
可保留方向：
- 特征节点表达目标特征类型。
- 特征值节点只由特征服务封口创建、读取和写入。
- I64 状态材料可继续作为第一轮特征状态材料。
- 内容哈希只能作为非权威缓存，命中后必须复核原始值。

必须删除或改写：
- 旧特征主信息当前值字段直接承载所有值类型。
- 旧 VecU / 指针句柄作为长期身份。
- 旧稳态列表、近帧候选、局部区间覆盖、命中次数、最后命中作为业务事实。
- 旧体素和外设值材料直接接入当前项目。

后续专项：
- 特征 / 特征值系统增强专项：抽象特征、值域、Vec 原始值容器、稳态和复合比较。
- 非权威缓存与统计专项：内容哈希、命中次数、最后命中和轮廓分层索引。
- 事件日志持久化恢复专项：Vec 原始值序列化和恢复。
- 外设 / 体素专项：三维体素、D455 和轮廓融合。
```

## 5. 禁止宣称

```text
不得声明旧特征类或旧特征值类已迁移。
不得声明旧特征主信息、旧抽象特征主信息或旧特征值主信息字段可直接迁移。
不得声明 VecU / VecI64 原始值容器、特征值内容哈希、稳态系统、抽象特征系统或体素系统完成。
不得接控制面板 / SQL / D455 / 体素 / 外设。
```
