# DYNAMIC-PATTERN-S1 规范动态证据签名与有序窗口代码实施 Codex 断点清单

日期：2026-07-16

计划 / 队列 / 阶段：#232 / DQ-124 / DYNAMIC-PATTERN-S1 / 630

决策：JY-363

事实基线：`97d52a09477f145ed996c70f393fdf8a1f77f8a1`

状态：已完成 / A01-A18 通过 / 630 已登记 / 已归档 / 待本轮自动提交与非强制推送

## 1. 执行前复核

1. 起始 `main == origin/main == 97d52a0`，工作树和暂存区为空。
2. 新版任务结果回执保留完整冻结执行请求；冻结材料、动态关系 1 / 2 / 3 / 4 槽、四仓库带令牌只读入口和阶段 630 与 JY-363 三件套一致。
3. 六个目标真模块和阶段 630 均未占用；目标计划列出的九个代码 / 工程文件足以闭合，不需要修改核心、既有领域服务 / 数据操作、线程协议或运行期装配。
4. #258 / #261 救援 stash 未读取、恢复、修改、提交或删除。

## 2. 实施结果

新增六个 `ClCompile + export module` 真模块：

```text
海中鱼巣/领域/材料.动态模式.ixx
海中鱼巣/领域/算法.动态模式.ixx
海中鱼巣/领域/数据操作.动态模式.ixx
海中鱼巣/领域/服务.动态模式.ixx
海中鱼巣/领域/组合.动态观察.ixx
海中鱼巣/领域/自检.动态模式.ixx
```

工程与入口接线：

1. `海中鱼巣.vcxproj` 和 filters 对六个模块各唯一登记一次。
2. `入口.cpp` 仅在 Debug 专项宏下 import 自检模块、保存一个值式结果、登记一次阶段 630 并加入最终汇总。
3. Release 不 import、不登记、不运行阶段 630；未接唯一运行期业务装配或生产线程路由。

## 3. 核心处理与结构边界

调用路径固定为：

```text
动态观察组合器
-> 动态模式业务服务
-> 动态模式数据操作一次共享许可
-> 完整节点 / 主信息 / 主键 / 当前关系 / 单步失效审计批量读回
-> 动态模式纯值算法
-> 非权威规范动态窗口
```

实现合同：

1. 完整冻结请求与新版回执共同形成非权威观察回合；片段序号由调用方按值提供，非零且同回合唯一。
2. 数据操作每次批量读取只取得一次共享许可并在当前调用栈内使用一次令牌；无第二许可、独占许可、写入口或能力导出。
3. 当前关系按完整关系句柄精确读回；冻结任务生命周期只允许原版本加一、端点与顺序号不变且状态为已失效的单步历史审计。
4. 实例动态严格读取因果关系槽 1 / 2 / 3 / 4；版本 1 只接受完整抽象状态目标，实例状态目标按不支持返回。
5. 结构键只含完整目标句柄、前值、后值、来源动作和规则版本；二次特征角色固定为空。完整键裁决相等，稳定哈希只用于分桶。
6. 窗口、批次和候选均为调用期值式非权威材料，不写仓库、索引、侧表、缓存、文件或数据库。

## 4. A01-A18 证据

| 验收 | 结果 | 证据摘要 |
| --- | --- | --- |
| A01 | 通过 | 正式基线、#231 前置、协议版本、规则版本和阶段占用已复核。 |
| A02 | 通过 | 完整冻结请求 + 新版回执形成非权威回合身份。 |
| A03 | 通过 | 零片段、重复序号和重复动态在取得许可前拒绝。 |
| A04 | 通过 | 公开材料、算法、服务和组合器事务能力静态命中 0。 |
| A05 | 通过 | 单次调用共享许可计数精确增加 1。 |
| A06 | 通过 | 同许可读回动态、前后状态、目标、动作、节点、主信息和主键。 |
| A07 | 通过 | 因果关系 1 / 2 / 3 / 4 完整句柄、端点、顺序号和状态均一致。 |
| A08 | 通过 | 冻结生命周期只经单步已失效审计读回。 |
| A09 | 通过 | 抽象目标正例成功；结构合法的实例生命周期目标负例返回目标类型不支持。 |
| A10 | 通过 | 场景、主体、来源动作、实际结果和时间逐字段一致。 |
| A11 | 通过 | 结构键字段固定，二次特征角色为空。 |
| A12 | 通过 | 排序确定、完整键比较独立于哈希碰撞。 |
| A13 | 通过 | 入口拒绝、许可拒绝、来源过期和不支持路径结构数量不变。 |
| A14 | 通过 | 同许可权威矛盾进入内部不一致，不选择首项或缓存兜底。 |
| A15 | 通过 | 持有共享许可时独占写线程不能完成，释放后完成。 |
| A16 | 通过 | 六真模块、工程登记和 Debug 阶段 630 均唯一。 |
| A17 | 通过 | Debug / Release x64 Rebuild、完整自检、Release 隔离和 Debug 20 / 20 均通过。 |
| A18 | 通过 | 禁止文件差异 0，计划归档和治理收口完整；后继仍返回设计窗口。 |

首轮 A09 自检曾把实例目标与前状态设为同一节点，先命中同源同目标关系重复而未形成负例动态；已只修正自检夹具为另一个真实实例生命周期状态。生产五模块合同未放宽、未增加兼容分支。

## 5. 构建、运行与静态验证

```text
Debug x64 Rebuild：退出 0，0 警告，0 错误。
Release x64 Rebuild：退出 0，0 警告，0 错误。
Debug --self-test-exit：退出 0，DYNAMIC-PATTERN-S1 A01-A18 全通过。
Release --self-test-exit：退出 0，阶段 630 / 专项显示名 / 自检模块名输出命中 0。
Release 海中鱼巣.exe：阶段名、专项显示名、自检显示名和自检模块名二进制命中 0。
最终 Debug 连续 20 / 20：每轮退出 0，每轮专项 18 / 18，通过总耗时约 246 秒。
```

静态扫描：

```text
dirty 代码 / 工程路径：精确 9 个允许文件。
公开事务能力命中：0。
数据操作取得共享许可：1；读取令牌：1；取得独占许可：0。
仓库调用：仅读取节点、主信息、I64、主键、当前关系和关系审计。
阶段 630：入口唯一 1 次。
六模块：vcxproj 与 filters 各唯一 1 次。
生产装配 / 线程新增导入：0。
```

治理完成后还执行 `python .\tools\check_specs.py --strict`、`git diff --check`、暂存差异检查和精确文件白名单检查，全部通过后才提交。

## 6. 完成边界与后继

当前只可声明：新版回执 / 冻结请求非权威回合身份、显式片段序号、一次共享许可批量读取、抽象状态目标规范窗口和阶段 630 已实现并验证。

当前不得声明：重复片段聚类、运动基元、因果机会 / 结果 / 概率、用途学习、候选持久化、运行期生产接线、权威任务尝试节点、特征值反向语义、旧动态能力迁移或项目迁移已经完成。

#233 / DQ-125 与 #234 / DQ-126 不在本轮连续消费；必须返回设计窗口，按 #232 正式 DTO 重做原计划。#235 继续后置，#214 继续用户暂停。
