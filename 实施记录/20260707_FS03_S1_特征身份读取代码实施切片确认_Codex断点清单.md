# FS-03 执行收口与双窗口规则确认 Codex 断点清单

日期：2026-07-07
状态：规则确认 / 执行收口边界 / 当前窗口未改 C++ / 未迁移旧函数

说明：本文件沿用早期 S1 命名，实际记录内容以本文件正文为准；它不单独授权 S1，也不撤销当前已确认的 FS-03 S1-S4 实施切片。

## 1. 输入入口

用户重新确认双窗口规则：

```text
一个窗口确认，然后将确认可以执行计划加入计划索引的可执行列表。
另一个窗口持续执行。
执行过程中如果有需要确认的内容，则加入待确认队列。
```

## 2. 本次确认规则

```text
确认 / 设计窗口：
只负责方案确认、计划确认、计划索引登记、可执行队列登记和待确认问题登记。
不得接管 C++ 实施。

执行窗口：
持续消费 项目记忆/Codex任务队列.md 中的可执行队列。
执行已确认计划直到队列清空、遇到真实阻塞或用户暂停。
执行中发现超出已确认计划、允许文件、禁止文件或验证方式的问题，必须登记到 项目记忆/待确认问题.md。
```

## 3. 当前 FS-03 处理口径

当前 worktree 已存在 FS-03 S1-S4 相关实施差异：

```text
海中鱼巣/领域/特征服务.h
海中鱼巣/入口.cpp
计划/已完成计划/20260707_FS03_特征服务封口增强S1-S4代码实施切片_v0.1.md
```

本确认窗口不接管这些 C++ 差异，不运行构建，不运行默认入口。

执行窗口下一步应承接：

```text
FS-03 S1-S4 代码实施差异收口。
补齐实施断点清单。
执行 Debug x64 构建。
运行默认入口。
执行高级服务越权依赖扫描。
执行 git diff --check。
同步项目记忆并提交推送。
```

若执行窗口发现验证失败、越权文件、需要新增结构、需要改变允许 / 禁止文件或需要修改已确认边界，必须登记到：

```text
项目记忆/待确认问题.md
```

## 4. 禁止扩大声明

本记录只证明：

```text
双窗口规则已重新确认。
FS-03 当前实施差异应交给执行窗口收口。
执行中新增问题必须进入待确认队列。
```

不得声明：

```text
本确认窗口完成了 C++ 实施。
本确认窗口完成了 C++ 构建或默认入口验证。
旧特征 / 特征值能力已迁移。
旧函数体已迁移。
旧主信息字段已迁移。
Vec / 哈希 / 稳态 / 值域 / 序列化 / SQL / 控制面板 / D455 / 体素 / 外设已接入。
```
