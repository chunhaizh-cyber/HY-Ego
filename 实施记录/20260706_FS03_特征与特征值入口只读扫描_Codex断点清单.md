# FS-03 特征与特征值入口只读扫描 Codex 断点清单

日期：2026-07-06
状态：已记录断点 / 文档切片 / 未改代码 / 未迁移旧函数

## 1. 本切片输入

```text
FS-02 第五批二次特征与因果入口扫描已提交并推送。
计划索引 HR-001-FM-SCAN 要求继续 FS-03 旧项目特征 / 特征值函数第一批只读扫描。
用户明确“不需要确定的步骤应该持续执行下去”。
```

## 2. 已完成动作

```text
1. 回读计划索引、逐函数只读扫描清单、函数功能说明文档和当前特征 / 特征值服务边界。
2. 只读扫描 `D:\鱼巢\特征类.h`、`D:\鱼巢\特征类.cpp`、`D:\鱼巢\特征值类.h`、`D:\鱼巢\特征值主信息类.h`。
3. 对照需求目标状态承载详细设计、领域服务最小入口详细设计、非权威缓存与统计详细设计和领域服务授权写入规范。
4. 在编辑计划索引前创建 `计划/.计划索引.lock`。
5. 新增 `实施记录/20260706_FS03_特征与特征值入口只读扫描记录.md`。
6. 登记 OF-106 到 OF-117。
7. 同步逐函数只读扫描清单、计划索引和项目记忆。
```

## 3. 本切片结论

```text
旧特征类将特征身份、抽象特征、值域、实例特征、当前值、稳态、VecU / 指针句柄、体素材料和比较规则混在一个服务内。
新项目第一轮只能承接特征节点、特征值节点、特征到特征值归属关系和 I64 状态材料封口。

旧特征值类以 VecU 原始值为权威本体，并维护内容哈希、命中次数、最后命中和轮廓分层索引。
新项目必须先确认 Vec 原始值容器、缓存命名空间和持久化恢复口径，不能直接迁移旧指针句柄和旧缓存字段。
```

## 4. 下一步

```text
下一旧函数编号从 OF-118 开始。
继续 FS-04 旧项目需求树函数第一批只读扫描。
若后续发现 FS-03 第一批遗漏，只能追加登记，不得把当前记录解释为迁移许可。
```

## 5. 禁止宣称

```text
不得声明旧特征类或旧特征值类已迁移。
不得声明旧特征主信息、旧抽象特征主信息或旧特征值主信息字段可直接迁移。
不得声明特征 / 特征值系统增强、Vec 原始值容器、体素系统或缓存统计系统完成。
不得接控制面板 / SQL / D455 / 体素 / 外设。
```

## 6. 验证记录

```text
本切片只做文档和只读事实记录验证。
未改代码。
未运行构建。
未运行当前项目入口。
未运行旧项目程序。
```
