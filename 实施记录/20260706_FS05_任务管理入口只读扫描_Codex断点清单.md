# FS-05 任务管理入口只读扫描 Codex 断点清单

日期：2026-07-06
状态：已记录断点 / 文档切片 / 未改代码 / 未迁移旧函数

## 1. 本切片输入

```text
FS-04 第一批需求树入口扫描已提交并推送。
计划索引 HR-001-FM-SCAN 要求继续 FS-05 旧项目任务管理函数第一批只读扫描。
用户明确“不需要确定的步骤应该持续执行下去”。
```

## 2. 已完成动作

```text
1. 回读计划索引、逐函数只读扫描清单、任务系统规范和任务系统详细设计。
2. 只读扫描 `D:\鱼巢\任务主信息类.h`、`D:\鱼巢\任务类.h`、`D:\鱼巢\任务类.cpp`、`D:\鱼巢\任务节点类型.h`、`D:\鱼巢\任务信息类型.h`。
3. 新增 `实施记录/20260706_FS05_任务管理入口只读扫描记录.md`。
4. 登记 OF-128 到 OF-139。
5. 同步逐函数只读扫描清单、计划索引和项目记忆。
```

## 3. 本切片结论

```text
旧任务类把任务来源需求、虚拟存在、场景、目标状态、实际结果状态、生命周期时间、任务角色、方法候选、运行统计和 SQL 投影集中在旧任务主信息、任务虚拟存在特征和任务树指针中。
新项目必须拆为任务来源需求关系、任务虚拟存在关系、任务场景关系、任务目标状态关系、任务实际结果状态关系、任务生命周期实例状态、任务选择方法关系、非权威统计和显示层只读视图；旧主信息字段不能直接迁移。
```

## 4. 下一步

```text
下一旧函数编号从 OF-140 开始。
继续 FS-05 第二批，扫描旧项目任务模块筹办 / 执行 / 管理线程边界。
若后续发现 FS-05 第一批遗漏，只能追加登记，不得把当前记录解释为迁移许可。
```

## 5. 禁止宣称

```text
不得声明旧任务类、旧任务树或旧任务管理系统已迁移。
不得声明旧任务主信息字段可直接迁移。
不得声明完整任务状态机、任务筹办、任务执行、调度线程或控制面板任务页完成。
不得接控制面板 / SQL / D455 / 体素 / 外设。
```

## 6. 验证记录

```text
本切片只做文档和只读事实记录验证。
未改代码。
未运行构建。
未运行当前项目入口。
未运行旧项目程序。
```
