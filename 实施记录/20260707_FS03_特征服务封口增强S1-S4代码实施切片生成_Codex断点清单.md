# FS-03 特征服务封口增强 S1-S4 代码实施切片生成 Codex 断点清单

日期：2026-07-07
状态：已生成 / 待确认 / 文档治理 / 未改代码 / 未迁移旧函数

## 1. 输入入口

用户确认：

```text
FS-03 S1-S4 后续代码实施切片方向可以确认。
S1-S4 拆分实施。
特征值服务 public 入口第一轮保持现状。
当前特征值唯一性第一轮只做归属复核和读取拒绝。
下一步生成独立的 FS-03 S1-S4 代码实施切片文件。
当前不能跳过该步骤直接修改 C++。
```

## 2. 复核依据

```text
AGENTS.md
计划/计划索引.md
规范/000_项目规则总纲.md
规范/001_规则迁移清单.md
规范/详细设计/特征服务封口增强详细设计.md
计划/20260706_FS03_特征服务封口增强专项_v0.1.md
计划/20260707_FS03_特征服务封口增强S0当前代码事实扫描切片_v0.1.md
实施记录/20260707_FS03_特征服务封口增强S0当前代码事实扫描_Codex断点清单.md
项目记忆/Codex任务队列.md
项目记忆/当前状态.md
```

## 3. 本次处理

```text
生成 FS-03 S1-S4 代码实施切片文件。
明确允许文件。
明确禁止文件。
明确入口函数名。
明确结构变化。
明确验证命令。
明确拒绝后结构不变化检查方式。
明确完成声明边界。
同步计划索引和项目记忆。
不修改 C++。
不运行 C++ 构建。
```

## 4. 生成文件

```text
计划/20260707_FS03_特征服务封口增强S1-S4代码实施切片_v0.1.md
实施记录/20260707_FS03_特征服务封口增强S1-S4代码实施切片生成_Codex断点清单.md
```

## 5. 当前代码实施门禁

```text
计划/20260707_FS03_特征服务封口增强S1-S4代码实施切片_v0.1.md
```

仍待用户确认。确认前不得开始修改：

```text
海中鱼巣/领域/特征服务.h
海中鱼巣/领域/特征值服务.h
海中鱼巣/入口.cpp
```

## 6. 验证方式

本切片生成阶段只做文档验证：

```powershell
git diff --check
if (Test-Path tools\check_specs.py) { python .\tools\check_specs.py }
git status --short --branch
```

本切片未运行 C++ 构建，未运行默认入口。

## 7. 禁止扩大声明

本次只证明：

```text
FS-03 S1-S4 代码实施切片文件已生成。
切片已明确允许文件、禁止文件、入口函数名、结构变化、验证命令、拒绝后结构不变化检查方式和完成声明边界。
当前仍待用户确认。
```

不得声明：

```text
FS-03 S1-S4 代码实施已获许可。
特征服务封口增强代码已实现。
旧特征 / 特征值能力已迁移。
旧函数体已迁移。
旧主信息字段已迁移。
Vec / 哈希 / 稳态 / 值域 / 序列化 / 体素 / 外设 / 控制面板 / SQL 已接入。
```
