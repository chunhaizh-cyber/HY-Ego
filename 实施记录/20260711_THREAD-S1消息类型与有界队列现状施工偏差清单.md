# THREAD-S1 消息类型与有界队列现状施工偏差清单

日期：2026-07-11

## 1. 元数据

```text
图类型：现状流程图配套偏差清单
对应流程图：流程图/20260711_THREAD-S1消息类型与有界队列现状流程图_v0.1.md
逐行映射表：实施记录/20260711_THREAD-S1消息类型与有界队列现状流程图逐行代码映射表.md
比对对象：
  流程图/20260709_运行宿主与多线程消息队列流程图_v0.1.md
  规范/详细设计/运行宿主与多线程消息队列详细设计.md
  计划/已完成计划/20260710_THREAD-S1_消息类型与有界队列基础壳代码实施切片_v0.1.md
  规范/详细设计/每线程独立ixx模块拆分详细设计.md
  实施记录/20260710_THREAD-MOD-S1_每线程独立ixx模块拆分代码实施_Codex断点清单.md
```

## 2. 偏差清单

| 编号 | 现状代码事实 | 施工流程图 / 详细设计描述 | 偏差类型 | 纠偏方向 |
| --- | --- | --- | --- | --- |
| DEV-THREAD-S1-001 | THREAD-S1 当前权威定义位于 `海中鱼巣/线程/运行消息协议.ixx` 和 `海中鱼巣/线程/有界运行消息队列.ixx` | THREAD-S1 原计划允许文件写的是 `海中鱼巣/核心/运行消息队列.h`；后续 THREAD-MOD-S1 才移动到 ixx 模块 | 图落后代码 / 计划历史位置落后现状 | 现状图保留 ixx 为权威位置；不修旧计划历史记录；后续引用 THREAD-S1 时必须同时引用 THREAD-MOD-S1 迁移记录 |
| DEV-THREAD-S1-002 | 当前 `有界运行消息队列` 是普通对象内 `std::deque` + `std::unordered_map`，无锁、无线程类、无等待唤醒 | 运行宿主与多线程消息队列施工图是全局线程与多队列示意，详细设计提及 MPMC / MPSC / 阻塞队列等后续方向 | 现状粒度小于施工图 | 现状图标注为基础壳；完整多线程调度和阻塞唤醒不得从 THREAD-S1 现状图中宣称完成 |
| DEV-THREAD-S1-003 | 停止消息满载时通过 `pop_back` 弹出尾部旧消息给停止消息让位 | 详细设计要求任何队列不得因满载吞掉停止消息，并区分不同队列满载策略 | 现状实现是第一轮通用策略 | 保留现状；后续若任务回执队列要求不得丢弃已生成回执，需要另建队列分型施工计划 |
| DEV-THREAD-S1-004 | 当前幂等键冲突只比较同一幂等键的材料摘要；完全重复消息未显式返回“已有局部回执”，而是继续入队并 `emplace` 不覆盖索引 | 详细设计允许同一幂等键、同一消息类型、同一来源、同一目标和同一材料摘要去重、返回已有局部回执或忽略重复调度 | 代码偏离详细设计的可选去重细化 / 第一轮未实现完整去重 | 保留为第一轮基础壳现状；若要实现重复消息去重，需要代码纠偏施工计划 |
| DEV-THREAD-S1-005 | 当前句柄版本过期判断只比较 `当前句柄版本 != 0 && 句柄版本 != 当前句柄版本` | 详细设计要求消费者重新复核句柄无效、已删除、版本过期、节点类型、关系类型、任务状态和方法关系 | 现状只覆盖版本材料壳 | 标注为 THREAD-S1 范围内材料判定；完整领域入口复核属于后续线程消费和领域服务计划 |
| DEV-THREAD-S1-006 | 当前代码所有显式拒绝均返回 `写业务事实=false`，没有实现追根因解决分支 | 2026-07-09 口径要求内部写入后不及预期必须追根因 | 非成功口径已符合当前显式代码，但缺少写后读回复核场景 | 保留现状；后续新增插入后一致性复核时，必须把失败画成追根因解决并加代码诊断 |
| DEV-THREAD-S1-007 | 本次现状图未映射 `入口.cpp` 中 THREAD-S1 自检，因为当前工作区该文件已有未提交改动 | THREAD-S1 实施记录包含默认入口 THREAD-S1-A1 到 A8 验证输出 | 当前工作区隔离边界 | 当前图只证明共享模块代码路径；入口自检现状图需等 `入口.cpp` 脏改动收口后另画，或单独注明基于脏工作区 |

## 3. 代码纠偏触发项

```text
本清单未直接授权修改代码。
若后续要实现重复消息去重、不同队列分型满载策略、插入后读回复核或入口自检现状图，必须另建施工计划。
当前立即需要追根因的问题：无。
当前属于逻辑内返回的分支：容量为零、消息编号无效、消息承载事实、句柄版本过期、幂等键冲突、同一任务序号倒退、停止后普通消息、队列已满、空队列出队。
```
