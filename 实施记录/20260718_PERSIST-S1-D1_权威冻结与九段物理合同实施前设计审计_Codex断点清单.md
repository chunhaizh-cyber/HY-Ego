# PERSIST-S1-D1 权威冻结与九段物理合同实施前设计审计 Codex 断点清单

日期：2026-07-18

执行队列：#294

设计队列：DQ-186

设计裁决：JY-389

计划版本：v0.1

窗口模式：单窗口串行双角色 / 本轮为执行角色

执行接管基线：`9c6ecfce753129191fb9c7ca4558fae7b041db2c`

状态：执行完成待设计同步 / M0—M6 完成 / A01—A18 全部通过 / 未构建 / 未运行程序 / 未登记新阶段

## 1. 执行前复核

```text
main == origin/main == 9c6ecfce753129191fb9c7ca4558fae7b041db2c。
工作树在执行接管前干净，分支为 main。
WIN-0001-E130 已随 JY-389 设计提交发布。
#294 / DQ-186 v0.1 是当前唯一依赖就绪候选。
当前物理窗口已按正式状态切为执行角色；外部执行任务无写入权。
计划索引、Codex 任务队列、本计划、JY-389、窗口交互、恢复流程图 / 详细设计、旧 #226、#249、#293 和实际接口已重新读取。
本轮两份允许的审计文件在开始前均不存在。
本轮禁止代码、工程、流程图、详细设计、计划、计划索引、队列和中央项目记忆写入；禁止构建、运行、阶段登记和 stash 操作。
```

## 2. M0—M6 与 A01—A18

| 范围 | 结果 |
| --- | --- |
| M0 正式状态重读 | 通过 |
| M1 运行期所有权拓扑 | 通过 |
| M2 四仓库与侧表所有者 | 通过 |
| M3 九段逐字段物理矩阵 | 通过 |
| M4 CODE-FILE-01 与入口边界 | 通过 |
| M5 后继窄切片 | 通过 |
| M6 证据与验证 | 通过 |
| A01—A18 | 全部通过 |
| 正式结论 | 通过 / 执行完成待设计同步 |

独立矩阵：

```text
实施记录/20260718_PERSIST-S1-D1_权威冻结与九段物理合同实施前设计审计矩阵.md
```

## 3. 当前唯一事实结论

1. `运行期上下文` 唯一拥有同域协调器、接线、四仓库、业务装配和系统角色清单；宿主 / 租约只支持完整全新候选首次发布。
2. `入口.cpp` 当前生产路径仍直接构造旧仓库 / 服务 / 旧概念图对象，未持有生产 `运行期上下文宿主`，也没有正式首次发布或快照服务消费租约调用点。
3. 主信息、节点和关系仓库只有逐句柄或局部读取；索引仓库有全部当前绑定读取。四仓库均没有 HYSNAP01 所需的完整状态 / 高水位 / 指定身份恢复合同。
4. 特征值 I64 / Vec 私有侧表属于 `特征值服务`，当前只有逐节点读取；系统角色清单可复制，但它的独立锁没有与结构事务独占许可形成显式同冻结点。
5. 新 `概念图结构数据操作` 没有活动侧表；旧 `概念图服务` 的根 / 生命周期 / 活动侧表属于旧入口对象，不属于新运行期上下文。段 9 当前不成立，禁止伪造空段或接回旧服务。
6. O1 权威事实全部落四仓库、无私有权威侧表，可映射段 2—5；用途观察数据操作 / 服务尚未接入运行期业务装配，不得借本轮扩大生产接线。
7. 索引记录只有主键和节点句柄。除 O1 有局部命名域 / 规则版本外，全局所有者、格式、键保留和探测声明均缺，不能从键或调用点反推。
8. `仓库快照服务.h` 仍只是恢复拒绝与隔离候选评估壳；HYSNAP01 总头、九段编码、严格自读和原子文件发布均未实现。

这些缺口均可被当前事实唯一描述，不是双重所有者或无法审计的接口冲突，因此本轮不退回“接口漂移待设计修订”。

## 4. 后继设计续点

设计角色必须先读取正式矩阵，再修订或替代恢复三件套和旧 #226。推荐顺序：

```text
P0：生产唯一运行期上下文 / 宿主真正接入口，旧入口所有权退出；裁决段 9 新运行期活动所有者。
切片 A：四仓库全状态 / 高水位、特征值与系统角色同冻结导出、全索引所有者声明、领域登记位图和快照参与者汇总。
切片 B：HYSNAP01 总头 / 九段规范编码、严格自读、CREATE_NEW / 写穿 / 同卷替换 / 最终读回。
RECOVERY-S1 以后：外部严格解析、指定身份隔离导入、领域恢复、屏障和同宿主首次恢复发布，继续独立后置。
```

设计角色不得直接把旧 #226 改成一个大代码切片，也不得把以下事项混入切片 A / B：

```text
恢复解析和指定身份导入。
生产 O1 接线、统计、学习、晋级或自动行动。
旧概念图服务接回新装配。
线程、SQL、控制面板、事件重放或在线热替换。
旧链表二进制、ADO 或旧持久化能力迁移完成声明。
```

## 5. 文件分类续点

```text
快照总头 / 段表 / 段 DTO：稳定值类型文件候选；若只声明则说明无 .cpp 原因。
冻结参与者 / 所有者注册 / 规范编码 / 严格自读 / 原子发布：生产真模块 .ixx。
专项与拒绝矩阵：独立自检真模块 .ixx。
入口.cpp：最多最小 import、一次登记 / 调用和总结果。
```

旧 #226 中计划新建的 `核心/仓库快照服务.cpp` 不符合当前新服务真模块规则；旧计划对多个旧领域头和 `入口.cpp` 的宽允许范围必须收窄。现有 `仓库快照服务.h` 壳的兼容保留、迁移或降权由设计角色裁决。

## 6. MIG-18 / NEW-04 边界

```text
MIG-18：仍是 S0 与设计完成、代码未实现、不计入旧能力迁移；本轮只增加物理审计证据。
NEW-04：#293 的运行期 O1 已实现，但恢复、统计、学习和生产接线仍未实现、不计入旧能力迁移。
O1 进入段 2—5 的映射不等于 HYSNAP01 已冻结、已编码、已恢复或已接生产。
```

## 7. 本轮范围与验证

本轮只新增：

```text
实施记录/20260718_PERSIST-S1-D1_权威冻结与九段物理合同实施前设计审计矩阵.md
实施记录/20260718_PERSIST-S1-D1_权威冻结与九段物理合同实施前设计审计_Codex断点清单.md
```

未修改 C++、工程、规范、流程图、详细设计、计划、计划索引、任务队列、项目记忆、既有实施记录或迁移台账；未运行 `msbuild`、程序、自检或压力测试；未登记阶段；未读取、恢复、修改或删除 stash。

验证结果：

```text
python .\tools\check_specs.py --strict：通过。
git diff --check：通过。
git diff --name-only：仅两份允许文件。
允许 / 禁止范围：通过。
```

## 8. 角色切换门禁

本轮精确提交和非强制推送成功前，当前物理窗口仍是 #294 执行角色，不得修改中央路由或上游设计。

推送成功后，当前物理窗口必须重新读取：

```text
Git HEAD 与 origin/main。
计划索引、Codex 任务队列和窗口交互记录。
#294、JY-389、本断点和独立矩阵。
当前实际接口与本轮精确提交范围。
```

全部一致后才切回设计角色。设计角色负责归档 #294、同步中央路由、修订恢复流程图 / 详细设计 / 旧 #226，并生成后继窄计划；因为目标是当前物理窗口，不发送自发自收任务消息。

## 9. 完成声明六项证据

| 维度 | 本轮证据 |
| --- | --- |
| 输入入口 | #294、JY-389、E130、恢复三件套、旧 #226、#249、#293、迁移台账和当前代码 |
| 核心处理 | M0—M6 与 A01—A18，覆盖所有权、导出 / 恢复缺口、九段、O1、索引所有者和文件分类 |
| 结构承载 | 独立矩阵与本断点；源文件和中央路由零修改 |
| 输出变化 | 形成当前物理合同唯一事实矩阵；状态为执行完成待设计同步 |
| 调用路径 | `E130 -> 执行角色重读 -> M0—M5 -> 两份记录 -> 验证 / 提交 / 推送 -> 设计角色复核` |
| 验证方式 | strict、diff、允许范围、零构建、零运行、零阶段和 stash 零操作 |

## 10. 完成边界

本轮只证明当前权威冻结所有者、HYSNAP01 九段物理映射、旧 #226 年代漂移和后继窄切片边界已经形成只读审计证据。不得声明 #294 已由设计角色归档、旧 #226 已修订、HYSNAP01 已实现 / 可发布 / 可恢复、生产运行期上下文已接入口、O1 已接生产 / 学习，或旧数据库、控制面板、持久化和恢复能力已迁移。
