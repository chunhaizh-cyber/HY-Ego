# CORE-SESSION-S5 代码实施执行断点

日期：2026-07-17

执行队列：#291

设计队列：DQ-183

设计裁决：JY-382

计划版本：v0.6

状态：执行完成待设计同步

## v0.5 单窗口执行 M0—M2 与计划范围冲突退回

执行角色已从 E120 正式提交 `7e1d7c0` 重读 Git、计划索引、队列、#291 v0.5、既有断点和实际接口；`main == origin/main`，`36ab9c0`、`a94e38c` 均为祖先，#291 是唯一依赖就绪候选，实际查询、会话、关系变更、完整接域和隔离签名与计划一致。

### M0 / M1 结果

1. 旧 `程序通过` 长链共 141 个叶子；新 `顶层门禁表` 共 141 个值，脚本逐项比较结果为 `MAPPING_IDENTICAL=True`。
2. 宏包围和原值求值顺序保持一致；最终 `程序通过` 只由该表的 `std::all_of` 计算，失败分支输出表内全部假门禁，不保留第二份长链。
3. Debug x64 Rebuild 通过，0 warning / 0 error；`git diff --check` 通过。

### M2 具名结果

```text
命令：.\x64\Debug\海中鱼巣.exe --self-test-exit
退出码：1
顶层假门禁：概念命名第一切片通过
中央自检总成：登记 52 / 执行 52 / 失败 1 / 总成契约完整
中央失败编号：CORE-UNPUBLISHED-S1
```

`CORE-UNPUBLISHED-S1` 的失败来源位于 `海中鱼巣/核心/自检.未发布身份候选.ixx:75`：第 76—77 行仍要求节点 / 主信息候选完成确认后，`撤销未发布候选` 返回 `已拒绝`，第 238 行把该验收组聚合为 `CORE-UNPUBLISHED-S1`；#291 v0.5 的 S2 / A10 则明确要求节点 / 主信息“已确认待发布候选可用原能力精确撤销，已发布后才拒绝”。生产合同已按 #291 形成，旧自检断言与新合同直接冲突。

### 正式退回条件

命中计划第 13 节退回条件：M1 已完成、M2 已实际运行，具名失败追到本计划禁止 / 未允许文件。`海中鱼巣/核心/自检.未发布身份候选.ixx` 不在第 7 节允许清单内，执行角色不得修改；也不得削弱 A10、从中央总成删除该自检或把失败门禁强制改真。

请设计角色原地修订同一 #291 / DQ-183：把该既有自检文件加入最小允许范围，并明确只允许把确认后撤销断言同步为“已确认待发布可精确撤销、已发布后拒绝”，同时保留其它 11 项权限、令牌、顺序和兼容边界验收。修订前 759 / 880 不登记，#290 不消费。

当前执行状态：`接口漂移待设计修订`。已形成且可安全保留的计划内差异只有 `入口.cpp` 单一具名表与本断点；未修改工程、中央路由、计划、其它自检或生产语义。

## 已完成的范围内实现

1. 节点仓库增加许可内指定类型有效节点值式读取；索引仓库增加单主键绑定与全部绑定值式读取；会话增加物理主键四态查询和类型—索引投影入口。
2. 节点、主信息候选和已发布关系变更均开始支持已确认待发布阶段的精确撤销；会话、执行器与特征值原始材料参与者改为确认待发布与最终发布分段。
3. 结构事务接线、协调器增加撤销失败隔离入口和等待许可获锁后的隔离二次复核；运行期装配开始向特征值服务传入正式结构接线。
4. 已更新既有仓库合同、结构写入会话、特征值原始材料事务自检中的相关确认语义。

## 验证事实

1. `msbuild .\海中鱼巣.vcxproj /p:Configuration=Debug /p:Platform=x64 /m` 通过（0 warning / 0 error）。
2. `git diff --check` 通过。
3. `./x64/Debug/海中鱼巣.exe --self-test-exit` 未通过。最小直接失败项为：
   - `CORE-SESSION-S4-A10 确认待发布可逆与跨会话拒绝`
   - 其聚合项 `CORE-SESSION-S4-A14`
4. 因上项失败，最终完整自检失败；未运行 Release、20/20、严格规范和 759 登记，也未修改 `入口.cpp`。

## 退回理由与禁止动作

计划要求确认后的关系变更必须可逆，但现有 S4 自检在该行为改造后仍无法同时证明“恢复写前记录”和“跨会话能力拒绝”。执行窗口不能通过削弱验收、修改计划或扩大关系仓库以外的范围自行裁决。

本提交不登记 759，不修改计划索引、任务队列、窗口交互记录或任何 O1 / #290 文件；#290 仍保持依赖门控，执行窗口不得继续消费。

## 交回设计窗口所需复核

1. 逐签名复核 `关系仓库::结构化确认已发布关系变更` 与 `结构化撤销已发布关系变更` 的确认后能力语义、版本恢复语义和跨会话拒绝合同。
2. 判定会话最终发布与仓库能力失效的唯一可见点，明确 S4 自检应保留或替换的断言。
3. 修订后重新派发 #291；执行窗口从正式设计提交重新读取实际接口后才可恢复。

## v0.2 重新派发执行结果

设计修订提交 `0c633165cfacf7bdd6b79a07458e4ea574c17884` 已重新读取。按 JY-382 v0.2 修订 S4-A10 后，Debug x64 编译通过，`CORE-SESSION-S4-A10` 与聚合 `CORE-SESSION-S4-A14` 均通过：同一原许可撤销按写前句柄读回完整写前记录、写后句柄过期；第二许可序号不同，连续两次跨会话撤销均为许可拒绝且写后记录保持不变。

但 `--self-test-exit` 仍以退出码 1 结束并仅输出“核心启动自检失败”，未输出任何 `: 失败` 或 `未通过` 的具体自检单元。执行窗口不能在无法定位失败来源时登记 759 或继续扩大代码范围。

当前状态仍为：接口漂移待设计修订。请设计窗口裁决完整程序总成失败的未归因来源，或提供不依赖 759 登记的最小定位入口；#290、759、880 均保持未消费 / 未登记。

## v0.3 执行复核与再次退回

已从 `3c41007` / WIN-0001-E118 重读 #291 v0.3，并保持 `fd52113` 已验证的 S4-A10、S4-A14 关系变更语义不变。本轮已恢复 `结构事务接线::已接域()` 对撤销失败隔离回调的完整要求，增加全空未接域与接线形态有效的值式判定；`自检.动态模式.ixx` 的读取门禁包装已原样转发该回调；执行器无参与者路径在会话撤销非成功时会在当前独占许可析构前请求隔离。

Debug x64 编译通过（0 warning / 0 error），`git diff --check` 通过；`DYNAMIC-PATTERN-S1-A15` 至 A18、`CORE-SESSION-S4-A10`、`CORE-SESSION-S4-A14` 均通过。但完整 `--self-test-exit` 仍以退出码 1 收口，仅输出“核心启动自检失败”，入口尚未形成 v0.3 要求的单一具名顶层门禁表，无法得到计划授权的失败名称。继续修改该长链需要逐项等值迁移并证明全部叶子、宏条件和执行顺序不变；当前执行证据不足以安全完成该重构或归因。

状态：`接口漂移待设计修订`。本轮不登记 759 / 880，不消费 #290，不修改计划、索引、队列或窗口交互。请设计窗口以高推理复核入口长链的等值表落点与剩余参与者撤销路径，重新发布明确的最小实施切片。

## v0.6 执行完成回执

已从 E121 正式提交 `9a82046` 重读 Git、计划索引、任务队列、#291 v0.6、本断点和实际接口；`main == origin/main`，`532e3e5`、`7e1d7c0` 与 `a94e38c` 均为祖先，#291 是唯一依赖就绪候选，当前登记角色为同一物理窗口执行角色。执行期间未修改计划、计划索引、任务队列、窗口交互、流程图、详细设计或其它设计治理文件。

### 实施结果

1. 会话保留许可内物理主键四态点查和类型—索引值式投影，查询只允许在当前线程、写入中、未决定、无首次失败且写集为空时发生；连续 1024 次点查只消费当前执行器取得的一份独占许可。
2. 节点、主信息候选和已发布关系变更均支持同一原许可内的已确认待发布精确撤销；关系恢复逐字段回到写前记录和写前版本，写前句柄恢复有效，写后句柄过期；原许可释放后的后继许可撤销拒绝且记录不变。
3. 会话、执行器和参与者按准备、确认待发布、无失败最终发布、完整撤销四段收口；任一参与者或会话撤销失败都先继续尝试其余逆操作，再于结构许可析构前请求事务域隔离并触发逻辑错误诊断。
4. 特征值服务接域创建 / 写入固定先取得结构独占许可，七个公开读取固定先取得结构共享许可；侧表锁内的节点 / 主信息访问使用同一许可令牌。运行期业务装配传入正式结构接线，全空未接域与完整接域是仅有合法形态，部分接线在四仓库、执行器和特征值服务入口拒绝。
5. Debug-only F01—F09、R01—R06、协调器活动请求观测和索引反向破坏入口均受 `HY_EGO_ENABLE_STRUCTURE_COMMIT_FAULT_SELF_TEST` 包围；Release 二进制未保留这些入口或 `CORE-SESSION-S5` 标识。
6. `入口.cpp` 在 758 与 760 之间登记唯一阶段 759 / `CORE-SESSION-S5`，最终 `程序通过` 仍只由单一具名顶层门禁表值式汇总；失败路径输出全部假门禁，不存在第二份最终长布尔链。

### A01—A24 验收

| 编号 | 正式结果 |
| --- | --- |
| A01 | 通过：E121 v0.6、历史提交、#291 / DQ-183 / JY-382、角色、唯一候选和 759 / 880 执行前零登记已复读。 |
| A02 | 通过：流程图、详细设计与 v0.6 计划对查询、事务、故障和排除范围一致。 |
| A03 | 通过：物理主键结果区分空闲、已绑定、入口拒绝和内部不一致。 |
| A04 | 通过：查询阶段、线程、决定、首次失败和空写集门禁已验证。 |
| A05 | 通过：同一会话连续 1024 次点查只消费一个许可。 |
| A06 | 通过：指定类型节点组与全部索引绑定均按值复制，单次只持一个仓库锁。 |
| A07 | 通过：缺失、重复、额外反向和悬空目标均收口为内部不一致。 |
| A08 | 通过：核心查询文件未出现 O1、FNV、2048、用途观察、角色或容量业务常量。 |
| A09 | 通过：节点 / 主信息未确认候选撤销回归通过。 |
| A10 | 通过：`CORE-UNPUBLISHED-S1` 第 3 项按候选甲 / 候选乙证明同许可撤销和跨许可拒绝，其它 11 项、12 项总数与中央登记保持。 |
| A11 | 通过：关系变更确认不改版本；同许可恢复写前记录，跨许可和最终发布后拒绝。 |
| A12 | 通过：会话确认失败保留写集和确认进度，不直接进入已结束。 |
| A13 | 通过：写入中、确认中和已确认待发布均执行固定逆序全量撤销。 |
| A14 | 通过：撤销读回权威记录、计数、关系、索引和侧表前态，不回拨高水位。 |
| A15 | 通过：真实特征值参与者保留准备、确认待发布、`noexcept` 发布和撤销四段。 |
| A16 | 通过：接域特征值读写锁序、正式运行期接线和带令牌仓库访问已验证。 |
| A17 | 通过：全部参与者与会话确认后才进入最终发布，结构许可最后释放。 |
| A18 | 通过：最终发布区无分配、回调、日志、故障点或可失败返回。 |
| A19 | 通过：CORE-SESSION-S5 的 F01—F09 与 FEATURE-VALUE-TXN-S1 真实参与者回归共同证明权威结构、侧表和当前存在的容量占用恢复前态，失败候选不可读。 |
| A20 | 通过：部分接线拒绝；R01—R06 均在许可析构前隔离；隔离前等待者获锁后二次复核、自清理并返回无效许可。 |
| A21 | 通过：接域普通读者在许可释放前不能完成，成功后一次读到四仓库与侧表完整结果。 |
| A22 | 通过：入口单一具名表；Debug / Release x64 Rebuild 和两配置完整自检通过；Release 故障标识零命中。 |
| A23 | 通过：最终 Debug 完整自检连续 20 / 20，strict、diff、静态扫描及允许 / 禁止范围通过。 |
| A24 | 通过：代码只登记 759；本记录进入 `执行完成待设计同步`。未登记 880，未增加节点 15 / 关系 18，未实施 O1、快照恢复或兼容路径批量删除。 |

### 最终验证

```text
Debug x64 Rebuild：通过，0 warning / 0 error。
Debug --self-test-exit：退出 0；CORE-SESSION-S5-A01—A24 为 24 / 24 通过；协调器隔离 A01—A06 为 6 / 6 通过；FEATURE-VALUE-TXN-S1-A15 通过。
最终 Debug 完整自检连续 20 / 20：每轮退出码均为 0。
Release x64 Rebuild：通过，0 warning / 0 error。
Release --self-test-exit：退出 0。
Release 故障符号扫描：CORE-SESSION-S5、故障枚举 / 配置 / 入口和协调器 Debug 观测共 0 命中。
python .\tools\check_specs.py --strict：未发现问题。
git diff --check：通过。
相对 9a82046 的代码变更文件：14 个，越出 #291 允许范围 0 个。
入口单一汇总：顶层门禁表 1、程序通过 all_of 1、失败门禁输出 1。
禁用能力新增扫描：O1、HYSNAP01、节点 15、关系 18、880 共 0 命中。
```

### 执行角色收口与后继边界

本回执只证明 #291 v0.6 的代码实施与计划内验证通过，不等于 #291 已由设计角色归档，也不等于 #290 / O1-S2 已恢复可执行。执行角色在提交并非强制推送本轮允许文件与本记录后停止消费队列；推送成功后，同一物理窗口须重新读取 Git、计划索引、任务队列、#291 和本记录，再切回设计角色形成 JY-383、裁决 #291 归档及 #290 是否重新派发。759 仅为本轮代码阶段登记；880 仍未登记。
