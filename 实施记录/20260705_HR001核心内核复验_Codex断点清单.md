# HR-001 核心内核复验 Codex 断点清单

日期：2026-07-05

## 1. 本轮入口

用户要求：

```text
按照计划索引执行
```

计划索引当前指向：

```text
HR-001
进入核心内核代码对齐、入口前置拒绝测试和最小闭环样例实现。
```

## 2. 已读取依据

```text
AGENTS.md
计划/计划索引.md
计划/20260705_海中鱼巣全量重构总计划_v0.1.md
规范/000_项目规则总纲.md
规范/001_规则迁移清单.md
规范/验收/最小闭环验收用例.md
```

## 3. 当前判断

当前工作区干净，本地 `main` 与 `origin/main` 对齐。

HR-001 当前允许推进范围：

```text
核心内核代码对齐
仓库入口前置拒绝测试
领域服务最小入口壳
最小闭环样例实现
```

当前禁止推进范围：

```text
旧项目主信息字段迁移
旧项目复杂能力代码搬迁
控制面板 / SQL 实时投影
D455 / 体素 / 外设接入
```

## 4. 本轮最小闭合切片

先执行第一轮复验切片：

```text
核对核心内核和领域服务代码是否已经覆盖 MC-00 到 MC-11 的第一轮验收口径。
若缺少入口拒绝或结构变化读回证据，则在现有最小控制台入口中补齐。
然后执行 Debug x64 构建和最小闭环运行。
```

## 5. 验证前预测

预期默认构建不启用：

```text
HY_EGO_ENABLE_FAULT_TOLERANCE_CHECK
```

预期运行入口只做人读输出，不承载机器事实。

预期至少需要确认：

```text
构建通过。
无效关系入口拒绝。
已删除节点旧句柄失效。
语素入口拒绝非信息节点。
领域服务最小入口可写。
最小闭环结构样例通过。
```

## 6. 待补证据

```text
Debug x64 构建结果。
控制台运行结果。
入口拒绝负例是否覆盖验收用例要求。
结构变化是否能从仓库读取，而不是只依赖输出文字。
```

## 7. 已完成证据

代码对齐：

```text
节点仓库创建节点前校验主信息有效性和节点类型边界。
索引仓库绑定和查询时复查节点当前有效性，并拒绝主键冲突。
关系仓库创建关系前拒绝未定义关系类型、无效端点、已删除端点和唯一关系冲突。
写入事务补齐失败状态和失败边界读取。
需求 / 任务服务补齐最小状态节点结构。
```

构建验证：

```text
msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m
已成功生成，0 个警告，0 个错误。
```

运行验证：

```text
.\x64\Debug\海中鱼巣.exe
返回码 0。
入口拒绝: 是
语素入口封口: 是
语素第一轮: 是
领域服务可写: 是
最小闭环通过: 是
```

完成边界：

```text
只声明第一轮核心入口拒绝和最小闭环结构样例通过。
不得声明旧项目能力已迁移、完整自我循环、自我苏醒或外设接入。
```
