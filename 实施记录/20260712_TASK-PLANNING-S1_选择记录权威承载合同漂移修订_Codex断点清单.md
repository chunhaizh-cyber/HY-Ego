# TASK-PLANNING-S1 选择记录权威承载合同漂移修订 Codex 断点清单

日期：2026-07-12

治理编号：JY-302

队列编号：#223

状态：设计修订完成 / 前置已满足 / 待执行窗口从 clean HEAD 重新复核 / 未改 C++ / 未构建 / 未运行程序

## 1. 当前事实

```text
HEAD/main/origin：0ffb8dad1bdecab150bd11df60bee98712675556
#222 / JY-301：已完成并归档，570、完整自检和连续 20/20 已登记
节点类型当前最大正式值：13
关系类型当前最大正式值：15
当前选择：顺序号 30 引用(任务 -> 方法)，没有独立选择记录
当前未知关系负例：16
```

顺序号 30 关系只能保存任务、方法、关系自身版本和角色顺序号。它不能权威保存所属筹办生命周期关系 / 状态 / 版本、幂等材料编号、批次身份、索引发布版本或登记 / 规格 / 条件 / 上下文 / 动作来源完整快照。

## 2. 默认裁决

| 编号 | 裁决 |
| --- | --- |
| JY302-D1 | 追加 `节点类型::任务方法选择记录 = 14`；每次已发布选择有独立不可变记录节点和主信息。 |
| JY302-D2 | 追加 `关系类型::任务方法选择 = 16`；方向任务到选择记录，顺序号固定 30，同任务只允许一条有效关系。 |
| JY302-D3 | 原顺序号 30 `引用(任务 -> 方法)` 保留为当前方法投影；它必须与记录匹配，但不能单独授权。 |
| JY302-D4 | 记录主信息由任务服务用版本化规范元组完整保存请求；uint64 高 / 低 32 位无损保存，变长项使用数量加定长元组。 |
| JY302-D5 | 幂等在同任务全部选择审计中按同键 + 完整元组裁决；摘要、时间戳、关系版本、容器或内存表不裁决同义。 |
| JY302-D6 | 发布顺序为未发布记录 -> 完整元组 -> 方法投影 -> 回写投影身份 -> 关系16最后发布；发布前失败恢复完整前态。 |
| JY302-D7 | `待重筹办 -> 筹办中` 在同一独占许可内失效旧关系16和方法投影；记录节点、主信息和审计保留，读取路径不惰性修补。 |
| JY302-D8 | 方法动作桥、#214 和 #224 只消费记录关系 / 节点、方法投影和生命周期完整身份组合。 |

## 3. 记录字段组

```text
格式版本与幂等编号
任务、方法、来源需求、父需求、主体、目标宿主、场景、目标特征、目标状态和值材料
双向承接授权关系
所属生命周期关系、状态节点、阶段和版本
召回规则、批次身份材料、完整候选数量和索引发布版本
方法登记、生命周期和结果规格
可选条件策略、每项条件规格和运行上下文当前值版本
动作入口、角色 / 状态、归属、输入输出规格及来源关系
顺序号 30 方法投影完整关系身份
```

任何字段被裁剪、槽位缺失 / 多余、半个 uint64、变长数量不符、重复条件身份或未知格式版本都不得进入发布。

## 4. 文件范围

定向新增允许的生产文件：

```text
海中鱼巣/核心/句柄.h
海中鱼巣/核心/关系仓库.h
海中鱼巣/核心/关系仓库.cpp
海中鱼巣/领域/任务服务.h
海中鱼巣/领域/方法召回服务.h
海中鱼巣/领域/方法服务.h
海中鱼巣/领域/自检.概念命名目标.ixx
海中鱼巣/领域/自检.任务筹办选择.ixx
海中鱼巣.vcxproj
海中鱼巣.vcxproj.filters
海中鱼巣/入口.cpp
```

主信息仓库、节点仓库、索引仓库、状态 / 需求 / 动态服务、线程、界面、SQL、日志和外设仍禁止修改。不得新增选择仓库、中立协议头或任务筹办服务。

## 5. 执行前复核

```text
读取 #203-#209、#221/#222 正式实施记录和实际类型 / 函数。
确认节点 / 主信息 / 关系令牌入口可在同一独占许可内创建和精确清理本次未发布记录。
确认关系仓库可只为关系16增加固定端点 / 顺序号 / 单当前规则，不改变普通引用和关系15。
确认任务服务生命周期迁移能在正式待重筹办返回筹办中时同许可失效旧选择。
确认完整规范元组可由现有主信息值容器无损保存，不修改主信息仓库 ABI。
确认未知关系负例可从16定向顺延17，阶段580未占用。
```

任一项不满足时停在代码修改前，退回设计窗口；不得临时缩减字段、改用摘要、内存表或日志，也不得自行扩大禁止文件。

## 6. 验证边界

后续实现必须覆盖：节点14 / 关系16 / 未知17；关系16同任务单当前；完整记录读回；同键同义、同键异义和同键多项；同周期异方法；正式重筹办失效；旧记录审计；孤立投影拒绝；发布前失败完整清理；普通引用和关系15回归；最终阶段580、Debug x64、完整自检、严格规范和连续20轮。

本次只完成设计治理，不证明上述代码或验证已经发生。
