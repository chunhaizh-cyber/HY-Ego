# 事件段、任务生命周期与持久化恢复下一批计划生成 Codex 断点清单

日期：2026-07-11

状态：事件段流程图 MD+HTML、详细设计和三份计划已生成 / 已登记 #200-#202 / 未改 C++ / 未构建 / 未运行程序

## 1. 输入入口

```text
用户要求继续制定下一批计划。
#188-#199 当前队列与正式计划。
迁移大纲 0-20 项最新归并。
任务、事件日志、事件线程、仓库快照、特征值、概念图当前代码事实。
任务状态机、事件日志、仓库快照、特征值序列化和概念生命周期正式设计。
```

## 2. 当前事实结论

```text
1. 当前事件材料最终只进入人读事件.log，没有可严格回读的结构化事件段。
2. 事件日志线程只承载通用消息，不能在本批猜测接线结构化事件。
3. 任务生命周期枚举完整，但任意状态可直接记录，迁移请求当前态固定读取已创建状态。
4. 固定安全根可直接完成任务，不能据此声称通用任务状态机形成。
5. 快照服务只有恢复拒绝壳；特征值侧表、概念活动图 / 根登记等非四仓库状态仍缺统一持久化清单。
6. #191、#197、#198 和 #200 形成后，才适合统一设计权威恢复。
```

## 3. 下一批裁决

```text
#200 EVENT-SEGMENT-S1：先建立结构化事件段版本 1、严格读回和候选发布基础；不接线程、不恢复仓库。
#201 TASK-LIFECYCLE-S0：等 #192 / #193 后扫描任务当前态、合法迁移、通用执行和固定样例差异。
#202 PERSIST-RECOVERY-S0：等 #191、#197、#198、#200 后统一盘点权威 / 可重建 / 必须丢弃状态。
```

依赖链：

```text
#199 -> #200 -> #201 -> #202

#201 业务前置：#192、#193。
#202 业务前置：#191、#197、#198、#200。
```

## 4. 生成产物

```text
流程图/20260711_结构化事件段持久化与只读校验流程图_v0.1.md
流程图/20260711_结构化事件段持久化与只读校验流程图_v0.1.html
规范/详细设计/结构化事件段持久化与只读校验详细设计.md
计划/20260711_EVENT-SEGMENT-S1_结构化事件段持久化与只读校验基础代码实施切片_v0.1.md
计划/20260711_TASK-LIFECYCLE-S0_任务生命周期与通用执行当前代码事实复核计划_v0.1.md
计划/20260711_PERSIST-RECOVERY-S0_权威状态持久化恢复当前代码事实复核计划_v0.1.md
本断点清单
```

## 5. 范围一致性

```text
事件段流程图、详细设计和 #200 均只覆盖基础段服务、规范编码、候选发布与严格只读。
#201、#202 都是纯只读 S0，不新增机器结构；各自计划已说明新流程图 / 详细设计不适用原因和替代依据。
三份计划均区分当前事实、假定前置、执行前复核和漂移退回。
没有把事件日志、线程消息、缓存、显示或 SQL 升格为当前事实。
没有把固定安全根样例扩大为通用任务执行。
```

## 6. 预留治理编号

```text
DQ-92：EVENT-SEGMENT-S1。
DQ-93：TASK-LIFECYCLE-S0。
DQ-94：PERSIST-RECOVERY-S0。
QR-165：ES-Q1 至 ES-Q12 默认裁决。
JY-218：本批排序、范围和依赖裁决。
```

JY-217 已由 #188 执行窗口预留，不得占用。

## 7. 输入、处理、结构、输出、调用与验证

| 项目 | 内容 |
| --- | --- |
| 输入入口 | 用户指令、迁移大纲、最新队列、正式设计与当前代码 |
| 核心处理 | 归并未完成能力，选择事件段实施和两个后置 S0，固定默认裁决与依赖 |
| 结构承载 | 新流程图 MD+HTML、详细设计、三份计划和本断点；未新增机器结构 |
| 输出变化 | #199 后形成 #200-#202 连续路由；任务与恢复缺口均有明确更新后扫描，不设置永久门控 |
| 调用路径 | 设计窗口生成并登记 -> 执行窗口复核实际接口 -> 代码实施或只读扫描 -> 正式记录 -> 设计窗口续接 |
| 验证方式 | `git diff --check`、规范检查、流程图一致性、路径检查、C++ / 工程零差异；本批不运行构建 |

## 8. 完成声明边界

本次只证明下一批设计与计划文件已生成。不得声明事件段代码、任务状态机、通用执行、快照或恢复已经实现。
