# 服务操作函数矩阵第一批代码实施包确认入队 Codex 断点清单

日期：2026-07-07

状态：已确认 / 已登记可执行队列 #129 / 未改代码 / 未迁移旧函数

## 1. 输入入口

```text
用户口令：全部确认
确认对象：计划/待确认计划/20260707_服务操作函数矩阵第一批代码实施包_v0.1.md
```

## 2. 本次处理

```text
1. 将批次级实施包从 计划/待确认计划/ 移动到 计划/。
2. 将计划状态改为已确认 / 已登记可执行队列。
3. 同步确认 MATRIX-IMP-Q1 到 MATRIX-IMP-Q3。
4. 将可执行项登记为 Codex 任务队列 #129。
5. 将原 6 份小切片标记为已被批次包归并，不再单独确认，不单独入队。
6. 同步计划索引、项目记忆、待确认问题和决策记录。
```

## 3. 确认结论

```text
MATRIX-IMP-Q1：
确认 6 份小切片归并为本批次级代码实施包，不再逐个确认。

MATRIX-IMP-Q2：
确认本批次允许一次修改多个领域服务，但仍禁止核心仓库、工程文件和特征值服务 public 入口。

MATRIX-IMP-Q3：
确认删除实例状态 / 动态写入口继续排除，只实现读取阻断和结构不变化验收。
```

## 4. 新增可执行队列

```text
#129 执行服务操作函数矩阵第一批代码实施包。
```

执行窗口必须按以下计划执行：

```text
计划/20260707_服务操作函数矩阵第一批代码实施包_v0.1.md
```

## 5. 仍然禁止

```text
不迁移旧函数体。
不新增旧主信息字段。
不实现删除实例状态 / 动态写入口。
不接控制面板、数据库、SQL / ADO、WebView、D455、体素、外设或线程。
不修改核心仓库、工程文件或特征值服务 public 入口。
```

## 6. 验证

```text
本次为确认入队和文档治理，不运行 C++ 构建。
后续 #129 执行时必须按计划运行 msbuild、默认入口、git diff --check、tools/check_specs.py 和排除项扫描。
```
