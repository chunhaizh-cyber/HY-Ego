# FS-06 方法系统入口只读扫描记录

日期：2026-07-06
状态：FS-06 第一批已登记 / 只读扫描 / 未改代码 / 未迁移旧函数
依据清单：`实施记录/20260706_逐函数只读扫描清单.md`

## 1. 本轮定位

本轮进入 FS-06，登记旧项目方法系统中与方法身份、方法首 / 条件 / 结果角色、方法虚拟存在、方法能力索引、来源任务、父 / 前置 / 后续方法、条件结果对和 SQL 显示投影有关的函数族。

本轮只做只读事实登记，不迁移旧 `方法主信息类` variant 结构，不恢复旧方法节点种类字段为新项目主信息字段，不把动作名、函数名、SQL 投影、控制面板显示项或本能方法兼容入口当作新项目动作身份。当前新项目方法系统口径仍是：方法角色通过抽象状态和关系承载，方法虚拟存在经存在服务创建并绑定到方法节点，条件 / 结果节点创建时自动绑定方法首，MI-M2 动作入口由动作入口规范承接。

## 2. 只读扫描范围

```text
D:\鱼巢\方法主信息类.h
D:\鱼巢\方法类.h
D:\鱼巢\方法类.cpp
D:\鱼巢\方法虚拟存在服务类.h
D:\鱼巢\方法虚拟存在服务类.cpp
D:\鱼巢\方法节点类型.h
D:\鱼巢\方法信息类型.h
```

本轮同时识别但不进入当前迁移的边界：

```text
旧方法主信息裸字段。
旧动作句柄字段和动作名文本。
旧 SQL 控制面板方法树投影。
旧条件结果对归并和练习样本统计。
旧本能方法首节点兼容入口。
旧方法虚拟存在特征镜像和指针特征写入。
```

扫描证据来自：

```text
方法主信息类.h:3-6：方法主信息只定义方法首、条件、结果结构边界；结果节点事实由结果场景承载；结果能力索引只用于召回。
方法主信息类.h:25-82：方法节点种类、动作句柄类型、方法类型、方法来源、方法状态和结果变化方向枚举。
方法主信息类.h:84-126：结果变化方向允许方向和动作句柄有效性辅助。
方法主信息类.h:128-226：结果能力模板、方法首能力面、公共信息、首 / 条件 / 结果角色字段和方法角色 variant。
方法主信息类.h:244-270：构造首节点、条件节点和结果节点。
方法主信息类.h:272-382：方法角色判断、角色信息读取、有动作、有结果能力和首个结果能力特征读取。
方法类.h:3-6：方法类声明方法树结构和方法能力入口，不承载任务筹办生命周期；SQL 控制面板只作显示镜像。
方法类.h:38-75：按主键解析、按动作句柄 / 动作名查找、注册方法首、注册需求位 / 需求方向方法首。
方法类.h:82-103：取或创建方法条件 / 结果场景、创建条件节点、创建结果节点。
方法类.h:115-219：方法角色、动作、来源、成熟度、结果能力、方法虚拟存在、来源任务、父 / 前置 / 后续方法、结构计数和最低状态读取。
方法类.h:226-314：声明条件特征、治理记录场景、方法虚拟存在特征读写、方法动作动态、条件结果对归并和练习样本统计。
方法类.h:317-355：SQL 控制面板方法显示项、本能方法兼容入口和方法虚拟存在私有入口。
方法类.cpp:2548-2617：注册方法首节点以旧主信息字段保存动作名、动作句柄和结果能力，并同步方法虚拟存在和 SQL 显示。
方法类.cpp:2850-2874：创建方法虚拟存在并写回旧方法首节点主信息引用。
方法类.cpp:2878-2916、2940-2967：创建方法条件场景和结果场景，并绑定方法虚拟存在为场景宿主。
方法类.cpp:2970-3000：通过旧世界树写方法虚拟存在 I64 / 指针 / VecU 特征。
方法类.cpp:3331-3465：条件结果对归并创建条件 / 结果节点，并写入统计和方法虚拟存在节点引用。
方法类.cpp:3546-3738：创建条件节点和结果节点，旧实现以方法树子节点和主信息字段承载场景、索引、时间和主键。
方法虚拟存在服务类.h:3-6：方法虚拟存在服务只封装方法类内部治理入口，不创建世界树成员关系，任务运行参数和回执不得写入方法虚拟存在。
方法虚拟存在服务类.h:12-93：确保 / 创建方法虚拟存在、补齐概念、同步结构特征、读写方法特征、记录统计和兼容初始化入口。
方法虚拟存在服务类.cpp:279-353：同步方法身份、来源、动作句柄类型、成熟度、允许自动召回 / 默认执行和本能动作 ID 等特征。
方法虚拟存在服务类.cpp:356-360、440-495：方法状态特征变化可生成方法域动作动态，但不写任务状态、不修改需求树、不提交世界事实。
方法虚拟存在服务类.cpp:558-767：确保方法虚拟存在、补齐概念、初始化基础信息、同步父 / 前置 / 后续方法、条件 / 结果场景和统计槽位。
方法虚拟存在服务类.cpp:770-867：记录方法虚拟存在特征、读写 I64 / 指针特征、记录运行统计和初始化兼容入口。
方法节点类型.h:1-5、方法信息类型.h:1-5：旧别名只把方法节点映射为链表节点，不提供新项目节点类型依据。
```

## 3. 逐函数登记

### OF-151

```text
旧函数编号：OF-151
扫描批次：FS-06 / FS-07 / FS-08
旧项目路径：D:\鱼巢\方法主信息类.h:25
旧函数名：枚举_方法节点种类 / 枚举_动作句柄类型 / 枚举_方法类型 / 枚举_方法来源 / 枚举_方法状态 / 枚举_结果变化方向 / 方法结果变化方向允许方向 / 结构体_动作句柄::有效
旧函数签名：旧方法主信息枚举、结果方向辅助和动作句柄有效性结构族。
当前调用方：旧方法类、方法虚拟存在服务、任务筹办 / 执行桥和本能方法兼容入口。
触发路径：注册方法首、创建条件 / 结果节点、判断方法状态、查找动作入口和同步方法虚拟存在特征。
输入：方法主信息字段、动作句柄类型、动作句柄值、结果变化方向。
输出：方法节点种类、动作句柄有效性、方法状态值、方向掩码。
读结构：旧方法主信息字段。
写结构：无，本结构族本身只提供字段和判断。
是否写机器事实：否；但旧字段会被其它函数写为机器事实。
是否依赖日志：否。
是否依赖显示：可被 SQL 投影读取。
是否依赖 SQL：间接依赖。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：方法角色、方法状态和结果方向需要结构化表达。
删除行为：动作句柄不得作为旧主信息裸字段迁移；方法节点种类不得作为 I64 / 文本主信息字段迁移。
新增约束：方法角色由抽象状态和关系读取；MI-M2 动作身份走动作入口角色和稳定动作键。
目标新结构：方法角色抽象状态、方法状态、结果方向二次特征或结果初始状态关系、动作入口角色节点。
目标领域服务入口：方法服务、动作入口服务。
对应规范 / 详细设计：方法系统规范、方法系统详细设计、动作入口规范、动作入口详细设计。
入口拒绝条件：方法角色状态缺失或不唯一、动作句柄文本 / 函数名被当动作身份、状态值非法。
验证方式：新项目只读方法角色和动作入口角色节点；不得读取旧裸字段作为事实。
初步判定：拆分
待用户确认项：结果变化方向是否进入方法结果节点后续增强或二次特征专项。
扫描证据：`方法主信息类.h:25` 至 `方法主信息类.h:126`。
```

### OF-152

```text
旧函数编号：OF-152
扫描批次：FS-06
旧项目路径：D:\鱼巢\方法主信息类.h:128
旧函数名：结构_方法结果关系模板 / 结构_方法结果项 / 结构_方法首节点能力面 / 结构_方法节点公共信息 / 结构_方法首节点信息 / 结构_方法条件节点信息 / 结构_方法结果节点信息 / 方法角色信息
旧函数签名：旧方法首、条件、结果角色字段和结果能力索引结构族。
当前调用方：旧方法类注册、查找、条件 / 结果创建、虚拟存在同步和 SQL 投影。
触发路径：方法首节点保存动作、来源、来源任务、父 / 前置 / 后续方法、能力索引、成熟度、自动查找；条件节点保存条件场景、条件判定索引和时间；结果节点保存结果场景和初始状态。
输入：动作名、动作句柄、场景、二次特征索引、状态引用、任务 / 方法引用。
输出：旧方法主信息 role variant。
读结构：语素入口、场景、二次特征、状态、任务、方法和存在引用。
写结构：旧方法主信息字段。
是否写机器事实：是，旧项目以主信息字段承载方法事实。
是否依赖日志：否。
是否依赖显示：是，SQL 投影读取这些字段。
是否依赖 SQL：间接依赖。
是否依赖线程：被任务筹办 / 执行间接读取。
是否依赖外设 / D455 / 体素：否。
保留行为：方法首、条件、结果三类角色及条件场景、结果场景、来源任务、父 / 前置 / 后续方法需要可追溯结构。
删除行为：不得恢复旧 variant 字段；成熟度、自动查找、根写入原语等 bool / I64 不作为一阶事实。
新增约束：角色用抽象状态和关系承载；场景、来源任务和父 / 前置 / 后续方法走方法关系角色；统计和策略进入状态、特征、二次特征或非权威缓存。
目标新结构：节点类型::方法、方法角色状态、方法虚拟存在关系、条件 / 结果场景关系、来源任务关系、父 / 前置 / 后续方法关系。
目标领域服务入口：方法服务。
对应规范 / 详细设计：方法系统规范、方法系统详细设计。
入口拒绝条件：方法节点无角色状态、关系目标类型错误、条件 / 结果场景无效、多个唯一关系、策略 bool 被事实化。
验证方式：通过方法服务读取角色、虚拟存在、条件场景和结果场景。
初步判定：改写为领域服务入口
待用户确认项：来源任务、父方法、前置 / 后续方法第一轮是否只保留占位，仍按详细设计暂缓。
扫描证据：`方法主信息类.h:128` 至 `方法主信息类.h:226`。
```

### OF-153

```text
旧函数编号：OF-153
扫描批次：FS-06
旧项目路径：D:\鱼巢\方法主信息类.h:244
旧函数名：方法主信息类::构造首节点 / 构造条件节点 / 构造结果节点 / 节点种类 / 是否方法首节点 / 是否方法条件节点 / 是否方法结果节点 / 取首节点信息 / 取条件节点信息 / 取结果节点信息 / 有方法动作 / 有结果能力 / 首个结果能力特征类型
旧函数签名：旧方法角色构造、判断和读取辅助函数族。
当前调用方：旧方法类创建和读取方法角色。
触发路径：创建方法首 / 条件 / 结果节点时写旧主信息 variant，读取时按节点种类返回对应角色信息。
输入：动作名、动作句柄或旧方法主信息。
输出：旧方法主信息、角色判断、角色信息指针、动作 / 结果能力判断。
读结构：旧方法主信息 variant。
写结构：旧方法主信息字段。
是否写机器事实：是，构造函数直接形成旧角色事实。
是否依赖日志：否。
是否依赖显示：间接服务显示。
是否依赖 SQL：间接服务 SQL 投影。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：新项目仍需要创建方法首、条件、结果角色节点，并提供专用读取入口。
删除行为：不得用主信息 variant、裸节点种类字段或 abort 式读取作为新项目角色裁决。
新增约束：创建角色节点必须同时写角色抽象状态和必要关系；读取时复核角色状态唯一且目标类型正确。
目标新结构：方法角色抽象状态、方法节点到角色状态关系、方法首到条件 / 结果模板关系。
目标领域服务入口：方法服务 `创建方法首节点`、`创建方法条件节点`、`创建方法结果节点`、`读取方法角色状态`。
对应规范 / 详细设计：方法系统详细设计。
入口拒绝条件：角色状态创建失败、角色关系写入失败、无效方法首、角色状态不唯一。
验证方式：有效方法首 / 条件 / 结果判定同时满足节点、角色状态、场景或虚拟存在关系。
初步判定：改写为领域服务入口
待用户确认项：无。
扫描证据：`方法主信息类.h:244` 至 `方法主信息类.h:382`。
```

### OF-154

```text
旧函数编号：OF-154
扫描批次：FS-06 / FS-07 / FS-10 / FS-X
旧项目路径：D:\鱼巢\方法类.h:38
旧函数名：按主键解析方法节点 / 查找方法首节点_按动作句柄 / 查找方法首节点_按动作名 / 注册方法首节点 / 注册需求位方法首节点 / 注册需求方向方法首节点 / 重写方法树SQL投影
旧函数签名：方法节点解析、方法首查找注册和 SQL 投影函数族。
当前调用方：旧任务筹办、任务执行桥、本能方法初始化、SQL 控制面板同步。
触发路径：按动作或结果能力查找 / 注册方法首；注册后同步方法虚拟存在并写 SQL 控制面板镜像。
输入：方法根节点、动作名、动作句柄、主结果特征类型、结果方向、来源、时间戳、SQL 投影参数。
输出：方法首节点、SQL 投影成功 / 失败。
读结构：旧方法树、旧方法主信息、语素入口、动作句柄和结果能力索引。
写结构：旧方法树节点、旧方法主信息、方法虚拟存在特征、SQL 投影表。
是否写机器事实：是，注册方法首写旧方法树；SQL 投影不是机器事实。
是否依赖日志：是，SQL 投影失败写日志。
是否依赖显示：是。
是否依赖 SQL：是。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：按结构身份查找方法首、注册方法首和需求位 / 方向候选可作为后续方法候选入口参考。
删除行为：不得按动作名文本、函数名或 SQL 投影裁决方法身份；不得接控制面板 / SQL 实时投影。
新增约束：方法首创建走方法服务；动作查找走动作入口稳定键；结果能力召回必须先有结构索引且复核方法角色和结果能力。
目标新结构：方法首角色节点、动作入口角色节点、稳定动作键索引、结果能力索引后续结构、显示层只读视图后续结构。
目标领域服务入口：方法服务、动作入口服务、后续显示层只读视图。
对应规范 / 详细设计：方法系统详细设计、动作入口详细设计、显示层只读视图专项待建。
入口拒绝条件：方法根无效、动作入口无效、结果能力缺结构依据、SQL / 控制面板路径写业务事实。
验证方式：方法首由方法服务读回；SQL 投影缺失不影响方法事实。
初步判定：拆分
待用户确认项：方法候选召回是否进入需求树 / 任务管理 / 方法系统后续增强专项。
扫描证据：`方法类.h:38` 至 `方法类.h:80`、`方法类.cpp:2548` 至 `方法类.cpp:2617`、`方法类.cpp:2402` 至 `方法类.cpp:2497`。
```

### OF-155

```text
旧函数编号：OF-155
扫描批次：FS-06
旧项目路径：D:\鱼巢\方法类.h:82
旧函数名：取或创建_方法条件场景 / 取或创建_方法结果场景 / 创建条件节点 / 创建结果节点
旧函数签名：旧方法条件 / 结果规格场景创建和角色节点创建函数族。
当前调用方：旧方法注册、任务执行桥、条件结果对归并和方法虚拟存在同步。
触发路径：为方法首创建条件场景、结果场景，随后创建方法条件节点或方法结果节点，并复用旧方法树子节点。
输入：方法首节点、宿主存在、条件 / 结果场景模板、二次特征索引、时间起止。
输出：条件场景、结果场景、方法条件节点、方法结果节点。
读结构：旧方法树、旧方法主信息、场景状态语义、二次特征索引。
写结构：旧方法树节点、旧方法主信息条件 / 结果场景字段、条件判定索引和 SQL 显示镜像。
是否写机器事实：是，创建条件 / 结果节点和场景关系。
是否依赖日志：否。
是否依赖显示：间接同步 SQL 显示。
是否依赖 SQL：间接依赖。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：方法条件场景和结果场景是方法规格容器；创建条件 / 结果节点时需要绑定方法首。
删除行为：不得把旧方法树父子关系或主信息字段直接当新项目关系事实；不得让临时执行场景直接变成长期规格。
新增约束：新项目创建条件 / 结果节点时必须自动绑定有效方法首，写角色抽象状态和条件 / 结果场景关系。
目标新结构：方法条件角色节点、方法结果角色节点、方法首到条件 / 结果模板关系、条件 / 结果场景引用关系。
目标领域服务入口：方法服务 `创建方法条件节点`、`创建方法结果节点`、`读取方法条件节点集合`、`读取方法结果节点集合`。
对应规范 / 详细设计：方法系统详细设计。
入口拒绝条件：无效方法首、条件 / 结果场景创建失败、角色状态关系失败、条件 / 结果节点未绑定有效方法首。
验证方式：有效条件 / 结果节点必须可读角色状态、场景和方法首模板关系。
初步判定：改写为领域服务入口
待用户确认项：条件判定索引和结果初始状态仍按详细设计第一轮暂缓。
扫描证据：`方法类.h:82` 至 `方法类.h:103`、`方法类.cpp:2878` 至 `方法类.cpp:2967`、`方法类.cpp:3546` 至 `方法类.cpp:3738`。
```

### OF-156

```text
旧函数编号：OF-156
扫描批次：FS-06 / FS-08
旧项目路径：D:\鱼巢\方法类.h:105
旧函数名：方法首节点生产目标特征 / 方法树存在生产目标特征 / 方法是首节点 / 方法节点种类 / 方法有动作 / 方法来源 / 方法成熟度阶段 / 方法结果项集 / 方法首个结果能力特征类型 / 方法结果能力可被查找命中 / 方法结果能力是根写入原语 / 设置方法结果能力查找属性 / 声明方法结果能力
旧函数签名：方法角色、动作、来源、成熟度、结果能力和候选召回属性读取 / 写入函数族。
当前调用方：旧需求筹办、方法候选查找、方法虚拟存在同步和任务执行桥。
触发路径：判断方法是否能生产目标特征，读写结果能力项，决定是否参与普通查找或标记根写入原语。
输入：方法节点、目标特征类型、结果能力项、可查找 bool、根写入原语 bool。
输出：布尔判断、成熟度、结果能力项、写入成功 / 失败。
读结构：旧方法主信息能力面。
写结构：旧方法主信息能力面。
是否写机器事实：是，结果能力和策略属性在旧项目中是方法事实。
是否依赖日志：否。
是否依赖显示：间接服务 SQL 显示。
是否依赖 SQL：间接依赖。
是否依赖线程：被任务筹办间接读取。
是否依赖外设 / D455 / 体素：否。
保留行为：方法结果能力召回、根写入原语和可查找策略需要保留语义。
删除行为：不得把可查找、根写入原语和成熟度 bool / I64 裸字段迁移为权威主信息。
新增约束：策略落为方法状态、特征、二次特征、策略关系或非权威缓存；候选召回后必须复核方法角色和结果结构。
目标新结构：结果能力关系 / 索引后续结构、方法策略状态、二次特征判断、非权威缓存。
目标领域服务入口：方法服务后续结果能力登记和候选召回入口。
对应规范 / 详细设计：方法系统规范、非权威缓存与统计规范。
入口拒绝条件：目标特征无效、方法非方法首、根写入原语进入普通候选、策略 bool 被事实化。
验证方式：召回候选不等于可执行；结果能力需由方法服务复核并可读结构依据。
初步判定：拆分
待用户确认项：方法结果能力索引是否进入 FS-06 后续增强或特征 / 特征值系统增强专项。
扫描证据：`方法类.h:105` 至 `方法类.h:153`、`方法虚拟存在服务类.cpp:689` 至 `方法虚拟存在服务类.cpp:710`。
```

### OF-157

```text
旧函数编号：OF-157
扫描批次：FS-06 / FS-07
旧项目路径：D:\鱼巢\方法类.h:155
旧函数名：设置方法本能动作入口 / 方法动作名 / 方法动作句柄 / 方法本能ID / 方法指定本能方法 / 查找或创建_本能方法首节点 / 是有效本能方法能力值 / 取或确保本能方法首节点_按能力值 / 补齐本能方法首节点
旧函数签名：旧本能动作入口和方法首节点兼容函数族。
当前调用方：旧本能方法类、任务执行桥、方法初始化和方法虚拟存在同步。
触发路径：给方法首节点写动作名、本能动作 ID 和动作句柄，按能力值查找或补齐本能方法首节点。
输入：方法首节点、动作名、本能方法 ID、自我能力值、宿主存在、时间戳。
输出：动作句柄、本能 ID、方法首节点、同步结果。
读结构：旧方法主信息动作字段、本能方法枚举和能力值。
写结构：旧方法主信息动作名 / 动作句柄、方法虚拟存在特征、SQL 显示镜像。
是否写机器事实：是，旧项目把动作入口写为方法首字段。
是否依赖日志：否。
是否依赖显示：间接同步 SQL。
是否依赖 SQL：间接依赖。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：方法需要可调用入口和稳定动作身份。
删除行为：不得把本能函数 ID、动作名文本或旧动作句柄字段直接作为新项目动作身份。
新增约束：MI-M2 走动作入口角色节点、稳定动作键和动作入口抽象状态；方法节点不得自引用冒充动作入口角色。
目标新结构：动作入口角色节点、稳定动作键索引、动作入口状态、方法动作入口关系。
目标领域服务入口：动作入口服务。
对应规范 / 详细设计：动作入口规范、动作入口详细设计。
入口拒绝条件：动作键无效、动作入口角色缺失、动作入口状态失效、方法节点自引用作为入口。
验证方式：动态来源指向动作入口角色节点或经确认边界；动作入口状态可读。
初步判定：改写为领域服务入口
待用户确认项：旧本能方法枚举是否进入后续函数迁移逐项说明，不在本轮迁移。
扫描证据：`方法类.h:155` 至 `方法类.h:169`、`方法类.h:322` 至 `方法类.h:338`、`方法类.cpp:2095` 至 `方法类.cpp:2148`、`方法类.cpp:2711` 至 `方法类.cpp:2846`。
```

### OF-158

```text
旧函数编号：OF-158
扫描批次：FS-06 / FS-08
旧项目路径：D:\鱼巢\方法类.h:171
旧函数名：方法虚拟存在引用 / 方法虚拟存在只读 / 取或创建_方法虚拟存在 / 记录方法虚拟存在特征 / 读取方法虚拟存在特征_I64 / 读取方法虚拟存在特征_I64或默认 / 读取方法虚拟存在特征_指针 / 记录方法虚拟存在节点引用 / 初始化方法虚拟存在信息
旧函数签名：方法虚拟存在引用、创建、读写特征和兼容初始化函数族。
当前调用方：旧方法类、方法虚拟存在服务、条件结果对归并、动作动态记录和本能方法补齐。
触发路径：方法首创建或同步时创建内部治理存在，写回旧方法主信息引用，并向虚拟存在写方法类型、状态、统计、节点引用等特征。
输入：方法首节点、特征类型、特征值、宿主存在、节点引用、时间戳。
输出：存在节点、特征写入成功 / 失败、读取值、特征节点。
读结构：旧方法主信息方法虚拟存在引用、存在、特征。
写结构：旧方法主信息引用、存在实例概念、特征 I64 / 指针 / VecU。
是否写机器事实：是，方法虚拟存在是旧方法长期能力基底。
是否依赖日志：否。
是否依赖显示：间接服务显示。
是否依赖 SQL：间接依赖。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：方法长期能力基底需要虚拟存在承载；任务运行参数、回执和临时缺口不得写入方法虚拟存在。
删除行为：不得把方法虚拟存在作为世界树成员关系或任务运行账容器；不得把指针特征作为长期身份。
新增约束：方法虚拟存在必须经存在服务创建，并通过方法虚拟存在关系绑定到方法节点；读取必须唯一。
目标新结构：方法虚拟存在节点、方法虚拟存在关系、方法状态、方法统计非权威缓存或特征。
目标领域服务入口：方法服务经存在服务 / 特征服务 / 状态服务。
对应规范 / 详细设计：方法系统详细设计、领域服务授权写入规范。
入口拒绝条件：无效方法首、虚拟存在创建失败、多虚拟存在、目标非存在、指针特征被当长期身份。
验证方式：读取唯一方法虚拟存在，来源可追溯到方法节点。
初步判定：改写为领域服务入口
待用户确认项：旧方法虚拟存在统计槽位是否进入非权威缓存与统计专项。
扫描证据：`方法类.h:171` 至 `方法类.h:267`、`方法类.h:340` 至 `方法类.h:355`、`方法类.cpp:2850` 至 `方法类.cpp:3000`、`方法虚拟存在服务类.h:12` 至 `方法虚拟存在服务类.h:93`。
```

### OF-159

```text
旧函数编号：OF-159
扫描批次：FS-06
旧项目路径：D:\鱼巢\方法类.h:177
旧函数名：方法来源任务引用 / 方法父方法引用 / 方法前置方法引用 / 方法后续方法引用 / 同步方法首节点结构特征
旧函数签名：来源任务、父方法、前置方法、后续方法读取和同步到方法虚拟存在函数族。
当前调用方：旧任务筹办、方法虚拟存在服务和方法结构同步。
触发路径：从方法首主信息读取来源任务 / 父 / 前置 / 后续方法引用，解析为方法虚拟存在后写入方法结构特征。
输入：方法首节点、宿主存在、时间戳。
输出：任务 / 方法引用或虚拟存在指针特征写入结果。
读结构：旧方法主信息引用字段、方法虚拟存在。
写结构：方法虚拟存在指针特征。
是否写机器事实：是，旧来源和序列关系由主信息字段承载。
是否依赖日志：否。
是否依赖显示：间接服务 SQL 显示。
是否依赖 SQL：间接依赖。
是否依赖线程：被任务筹办间接读取。
是否依赖外设 / D455 / 体素：否。
保留行为：来源任务、父方法、前置方法和后续方法需要可追溯关系。
删除行为：不得迁移为主信息字段或指针特征；不得把前置 / 后续方法序列直接当可执行计划。
新增约束：按方法系统详细设计，这些关系第一轮只登记函数占位，代码实施暂缓。
目标新结构：方法来源任务关系、父方法关系、前置方法关系、后续方法关系。
目标领域服务入口：方法服务后续绑定 / 读取入口。
对应规范 / 详细设计：方法系统详细设计。
入口拒绝条件：无效方法、目标非任务 / 非方法、多来源任务、循环父方法、序列规则未确认。
验证方式：后续通过方法服务枚举或读取关系；第一轮不得实现为裸引用混读。
初步判定：改写为领域服务入口 / 后续暂缓
待用户确认项：父 / 前置 / 后续方法是否归入需求树 / 任务管理 / 方法系统后续增强专项。
扫描证据：`方法类.h:177` 至 `方法类.h:187`、`方法类.cpp:2181` 至 `方法类.cpp:2206`、`方法虚拟存在服务类.cpp:651` 至 `方法虚拟存在服务类.cpp:687`。
```

### OF-160

```text
旧函数编号：OF-160
扫描批次：FS-06 / FS-08
旧项目路径：D:\鱼巢\方法类.h:195
旧函数名：方法动作数量 / 方法可执行入口数量 / 方法条件节点数量 / 方法结果节点数量 / 方法条件结果对数量 / 方法首个条件场景 / 方法最低结构状态值 / 方法满足可用最低判据 / 初始化方法统计特征 / 记录方法运行统计
旧函数签名：方法结构计数、最低结构状态、可用性判据和运行统计函数族。
当前调用方：旧方法虚拟存在服务、任务筹办、任务执行桥和方法诊断显示。
触发路径：统计动作入口、条件节点、结果节点、条件结果对，推导最低方法状态；初始化和递增运行统计槽位。
输入：方法首节点、统计名、增量、宿主存在、时间戳。
输出：数量、最低状态、可用 bool、统计写入结果。
读结构：旧方法树、旧方法主信息、方法虚拟存在特征。
写结构：方法虚拟存在统计特征。
是否写机器事实：部分是；结构计数可由结构重建，运行统计更接近非权威缓存。
是否依赖日志：否。
是否依赖显示：可服务诊断显示。
是否依赖 SQL：间接依赖。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：方法结构缺口可以推导待动作、待入口、待条件、待结果、待条件结果对、可用状态。
删除行为：不得把可用 bool 或统计计数当作方法成功事实；不得用统计缓存裁决任务选择。
新增约束：方法状态通过状态结构表达；运行次数、成功次数、候选命中等进入非权威缓存或诊断。
目标新结构：方法状态、结构统计运行期快照、非权威方法统计缓存。
目标领域服务入口：方法服务、非权威缓存统计服务后续入口。
对应规范 / 详细设计：方法系统详细设计、非权威缓存与统计详细设计。
入口拒绝条件：方法非方法首、条件结果对缺结构证据、统计名空、统计值被业务事实化。
验证方式：清空统计不影响方法角色、条件 / 结果节点和动作入口事实。
初步判定：拆分
待用户确认项：方法统计是否进入非权威缓存 FS-08 统一登记。
扫描证据：`方法类.h:195` 至 `方法类.h:219`、`方法虚拟存在服务类.cpp:714` 至 `方法虚拟存在服务类.cpp:740`、`方法虚拟存在服务类.cpp:843` 至 `方法虚拟存在服务类.cpp:858`。
```

### OF-161

```text
旧函数编号：OF-161
扫描批次：FS-06 / FS-07 / FS-08
旧项目路径：D:\鱼巢\方法类.h:269
旧函数名：记录方法调用动作动态 / 判定结果场景复现 / 查找等价条件节点 / 条件节点已有结果子节点 / 条件结果对统计宿主 / 查找复现结果节点判定 / 归并方法条件结果对 / 记录既有条件结果对练习样本
旧函数签名：方法调用动作动态、结果复现、条件结果对归并和练习样本统计函数族。
当前调用方：旧任务执行桥、方法学习归并和方法虚拟存在统计。
触发路径：方法执行后记录方法动作动态，把执行前条件场景和执行后结果场景归并为条件节点 / 结果节点，统计样本、符合 / 不符合次数和最近样本时间。
输入：方法首节点、输入 / 输出场景、动作名、开始 / 结束时间、执行成功、错误码、实际结果场景。
输出：动作动态、条件节点、结果节点、复现判定、统计写入。
读结构：旧方法树、条件 / 结果场景、场景状态、方法虚拟存在。
写结构：动作动态、条件 / 结果节点、方法虚拟存在节点引用、统计特征、运行日志。
是否写机器事实：是，动作动态和条件 / 结果归并可形成证据；统计属于非权威或后续学习材料。
是否依赖日志：是，异常路径写项目运行日志。
是否依赖显示：间接服务显示。
是否依赖 SQL：间接同步。
是否依赖线程：被任务执行桥间接调用。
是否依赖外设 / D455 / 体素：否。
保留行为：方法执行后应形成动作动态证据；经验证的条件 / 结果对可以作为后续学习材料。
删除行为：不得直接迁移旧归并函数体；不得用日志文本或动作名裁决动作来源；不得把单次归并统计当可靠方法成功。
新增约束：动作动态来源先走动作入口；条件结果对第一轮暂缓；轻量因果不等于稳定因果结论。
目标新结构：动作入口角色、动作动态、方法条件 / 结果角色节点、条件结果对后续证据结构、非权威统计缓存。
目标领域服务入口：动作入口服务、动态服务、方法系统后续增强服务。
对应规范 / 详细设计：动作入口详细设计、动态服务详细设计、方法系统详细设计、非权威缓存与统计详细设计。
入口拒绝条件：动作入口缺失、方法无效、场景无效、缺时间戳、条件结果对规则未确认、统计被事实化。
验证方式：动作动态可追溯动作入口；条件结果对未确认前只登记为后续专项。
初步判定：排除 / 后续专项
待用户确认项：条件结果对归并是否作为需求树 / 任务管理 / 方法系统后续增强专项第一批。
扫描证据：`方法类.h:269` 至 `方法类.h:314`、`方法类.cpp:3089` 至 `方法类.cpp:3179`、`方法类.cpp:3234` 至 `方法类.cpp:3465`。
```

## 4. 本轮初步分流

```text
可保留方向：
- 方法首、方法条件、方法结果三类角色需要保留，但由方法角色抽象状态和方法服务关系承载。
- 方法虚拟存在作为方法长期能力基底需要保留，但必须经存在服务创建并由关系绑定，不保存任务运行账。
- 方法条件场景和结果场景作为方法规格容器需要保留，但创建条件 / 结果节点时必须自动绑定有效方法首。
- 来源任务、父方法、前置方法和后续方法需要保留为关系占位，但第一轮代码仍按详细设计暂缓。

必须删除或改写：
- 旧方法主信息 variant 字段和方法节点种类裸字段。
- 旧动作句柄字段、动作名文本、本能函数 ID 直接作为动作身份。
- 成熟度、允许自动查找、可被查找命中、根写入原语等 bool / I64 裸字段作为权威事实。
- 指针特征、SQL 控制面板投影、日志文本作为方法事实或动作来源。

后续专项：
- 动作入口 / 动态后续增强专项：旧动作句柄、本能动作入口和方法调用动作动态。
- 需求树 / 任务管理 / 方法系统后续增强专项：方法候选召回、结果能力索引、来源任务、父 / 前置 / 后续方法和条件结果对。
- 非权威缓存与统计专项：方法运行次数、成功次数、候选命中、样本次数和最近样本时间。
- 显示层只读视图专项：方法树 SQL 投影和控制面板显示镜像。
```

## 5. 禁止宣称

```text
不得声明旧方法主信息类、旧方法类或旧方法虚拟存在服务已迁移。
不得声明完整方法查找、候选排序、条件结果对、方法学习或动作执行系统完成。
不得把动作名、函数名、本能函数 ID、旧动作句柄字段或 SQL 显示文本当作动作身份。
不得迁移旧方法主信息字段、旧方法函数体或旧指针特征。
不得接控制面板 / SQL / D455 / 体素 / 外设。
不得声明旧函数扫描已完成；后续若发现遗漏只能追加登记。
```
