# 非权威缓存与统计详细设计小修确认 Codex 断点清单

日期：2026-07-06
状态：已完成 / 文档小修后确认 / 未改代码

## 1. 输入入口

用户确认非权威缓存与统计详细设计方向通过，但要求小修后确认：

```text
补缓存承载位置。
补缓存命名空间。
收窄第一轮可实施范围。
明确需求结算摘要依赖权威结算记录，未实现前只能占位。
补命中次数 / 最后命中边界、孤立关系诊断边界和验收项。
```

## 2. 本次处理

已修改：

```text
规范/非权威缓存与统计规范.md
规范/详细设计/非权威缓存与统计详细设计.md
计划/计划索引.md
计划/20260705_海中鱼巣全量重构总计划_v0.1.md
计划/20260705_主信息迁移实施映射计划_v0.1.md
项目记忆/当前状态.md
项目记忆/待确认问题.md
项目记忆/决策记录.md
项目记忆/Codex任务队列.md
项目记忆/实施记录.md
项目记忆/验收记录.md
```

未修改代码，未迁移旧缓存字段。

## 3. 已确认口径

```text
非权威缓存不进入主信息权威字段。
缓存承载位置必须显式登记。
第一轮允许承载位置包括索引仓库缓存项、统计服务运行期快照、需求结算摘要缓存和主信息非权威缓存区，但第一轮代码优先只做统计服务运行期快照。
缓存命名空间必须包含缓存类型、来源结构类型、来源句柄或来源仓库、来源版本、生成规则版本、缓存值类型、是否可持久化和失效条件。
第一轮可实施范围收窄到缓存命名空间、缓存记录结构、结构统计运行期快照、缓存清空 / 重建接口壳。
特征值内容哈希、VecI64 / VecIU64 原始值相关缓存、特征值命中次数和最后命中持久缓存暂缓到特征值原始 Vec 能力后。
需求结算摘要缓存暂缓到需求结算记录规范和详细设计确认，并且权威结算记录实现后。
```

## 4. 禁止扩大声明

```text
不声明缓存统计代码已实现。
不声明特征值内容哈希已实现。
不声明命中次数或最后命中缓存已实现。
不声明需求结算摘要已实现。
不声明缓存系统完成。
不声明旧缓存字段已迁移。
不把缓存值作为需求满足、任务完成、方法成功或世界事实依据。
```

## 5. 验证方式

本切片只做文档验证：

```text
文档状态一致性检索。
git diff --check。
git diff --cached --check。
```

未运行 Debug x64 构建，未运行默认入口。

## 6. 下一步

按计划索引继续：

```text
任务系统第一轮代码实施。
需求结算记录规范和详细设计仍待用户确认。
```
