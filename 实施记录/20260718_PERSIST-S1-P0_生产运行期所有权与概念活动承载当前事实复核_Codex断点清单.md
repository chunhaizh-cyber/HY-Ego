# PERSIST-S1-P0 生产运行期所有权与概念活动承载当前事实复核 Codex 断点清单

日期：2026-07-18

执行队列：#295

设计队列：DQ-187

设计裁决：JY-390

计划版本：v0.1

窗口模式：单窗口串行双角色 / 本轮为执行角色

执行接管基线：`4aa62e327062ba74bab331fa9bd9594e4c1e5113`

状态：执行完成待设计同步 / M0—M6 完成 / A01—A18 全部通过 / 未构建 / 未运行程序 / 未登记新阶段

## 1. 执行前复核

```text
main == origin/main == 4aa62e327062ba74bab331fa9bd9594e4c1e5113。
工作树在执行接管前干净，分支为 main。
E131 / JY-390 已随本基线提交发布。
#294 已完成归档；#226 v0.2 是设计复核门控。
#295 / DQ-187 v0.1 是当前唯一依赖就绪候选。
当前物理窗口已按 WIN-0001-E131 正式状态切为执行角色；外部执行任务无写入权。
本轮两份允许输出在执行前均不存在。
本轮禁止修改代码、工程、规范、流程图、详细设计、计划、索引、队列、项目记忆和既有记录；禁止构建、运行、自检、阶段登记和 stash 操作。
```

## 2. M0—M6 与 A01—A18

| 范围 | 结果 |
| --- | --- |
| M0 正式状态重读 | 通过 |
| M1 入口对象与宿主调用图 | 通过 |
| M2 兼容生产消费者复核 | 通过 |
| M3 概念活动逐字段所有权 | 通过 |
| M4 生产唯一上下文切换面 | 通过 |
| M5 后继窄切片建议 | 通过 |
| M6 证据与验证 | 通过 |
| A01—A18 | 全部通过 |
| 正式结论 | 通过 / 执行完成待设计同步 |

独立事实矩阵：

```text
实施记录/20260718_PERSIST-S1-P0_生产运行期所有权与概念活动承载当前事实矩阵.md
```

## 3. 当前唯一事实结论

1. `入口.cpp:456-485` 无条件构造旧主信息、节点、关系、索引和传统服务栈；`517` 无条件启动旧初始化线程。正常模式通过完整自检后仍使用同一旧对象组生成 SQL 结构审计并构造原生控制面板；无窗口常驻没有新运行期业务消费者。
2. `运行期上下文`、唯一宿主、首次发布和同身份租约类型已经存在，但全仓所有宿主 / 租约调用均在 `自检.运行期上下文`、`自检.系统角色初始化`、`自检.主装配事务` 和 `自检.运行期组合分层`。生产入口、线程、界面、SQL 和快照服务没有正式租约调用点。
3. #271 的 17 个传统服务头不变。直接包含传统头的文件从 51 增至 55；自检模块从 35 增至 49；直接提四仓库的自检从 32 增至 40。精确增量分别是 4 个自检包含点、14 个自检模块和其中 8 个直接四仓库自检，均无删除；四个生产线程 / 上行桥消费者没有增长。
4. 14 个服务、7 个组合器和 1 个运行期请求路由共 22 个正式公开面全部存在，原始令牌 / 许可 / 四仓库 / 执行器 / 会话禁用能力命中文件数仍为 0。唯一业务出口是 `运行期上下文::读取候选业务操作 -> 运行期业务装配::读取业务操作 -> const 运行期业务操作组合器* -> 运行期业务请求路由`。
5. 两条兼容写路径是概念命名治理路由和自我治理领域路由；两条只读 / 消息桥是概念命名上行桥和任务管理上行桥。当前 8 类请求不足以等价覆盖待命名查询、概念命名需求、固定根治理、动作执行证据和全部只读查询，不能把“已有新服务”写成“消费者可直接机械替换”。
6. 旧概念图服务私有拥有 `根登记组_`、`生命周期状态组_`、`专用关系登记组_`、`生命周期关系登记组_`、`活动快照_` 和 `上一候选图_`；新概念图结构数据操作 / 服务没有活动侧表，只承载四根角色复核与三类结构关系。
7. 概念四根、专用 / 生命周期关系登记、活动根 / 节点 / 关系可以从版本化系统角色和四仓库按规范顺序确定性重建；三生命周期状态角色、完整概念签名和活动版本仍是私有权威；上一候选图是瞬态缓存，恢复时丢弃。
8. 当前没有两个无法裁决的同域正式所有者：旧入口栈是当前实际启动所有者，新运行期只是类型与自检事实。因此本轮通过，不以目标 P0 代码缺失为接口漂移。

## 4. 精确兼容增量

直接包含传统服务头的 4 个新增自检文件：

```text
海中鱼巣/领域/自检.动态模式.ixx
海中鱼巣/线程/自检.任务结果结算.ixx
海中鱼巣/线程/自检.任务执行调度.ixx
海中鱼巣/自检.控制面板按需投影.ixx
```

14 个新增自检模块中，直接提四仓库的 8 个为：

```text
海中鱼巣/核心/自检.关系仓库性能基线.ixx
海中鱼巣/线程/自检.任务执行调度.ixx
海中鱼巣/线程/自检.任务结果结算.ixx
海中鱼巣/自检.控制面板按需投影.ixx
海中鱼巣/自检.服务写入兼容边界.ixx
海中鱼巣/领域/自检.动态模式.ixx
海中鱼巣/领域/自检.方法动作场景关系.ixx
海中鱼巣/领域/自检.用途观察结构.ixx
```

其它 6 个新增自检不直接提四仓库：

```text
海中鱼巣/自检.性能门禁分离.ixx
海中鱼巣/领域/自检.动态聚类.ixx
海中鱼巣/领域/自检.因果完整结构键.ixx
海中鱼巣/领域/自检.因果模式.ixx
海中鱼巣/领域/自检.用途事件.ixx
海中鱼巣/领域/自检.用途观察.ixx
```

这些增量只更新当前兼容事实，不构成传统头删除资格，也不构成生产新增调用许可。

## 5. 概念活动设计续点

设计角色应把段 9 拆成：

```text
新运行期私有权威：
  版本化三生命周期状态角色完整身份
  概念完整规范签名
  非零活动版本

确定性重建参与者：
  系统角色四根
  当前有效三类专用关系登记
  当前有效生命周期关系登记与概念阶段
  活动根、概念节点和活动关系规范集合

瞬态：
  上一候选图，恢复时为空
```

规范顺序和比较必须至少固定：

```text
根：存在、动态、关系、因果。
专用关系：类型、完整源句柄、完整目标句柄、完整关系句柄。
生命周期：完整概念句柄；每概念恰一，目标属于三角色。
活动节点 / 签名：完整概念句柄。
签名：版本、约束维度、角色、定义节点、值类型和完整值字段逐项相等。
活动版本：精确保留，不从关系版本、数量、时间戳或日志推断。
```

P0-C 不得 import / include 旧 `概念图服务.h` 作为新装配成员，不得用对象内存、空段、名称、SQL、显示或日志补段 9。

## 6. 后继设计续点

建议顺序：

```text
P0-A：生产启动组合点构造完整候选、初始化系统角色、首次发布并持有有效租约。
P0-C：形成概念活动新运行期唯一所有者 / 确定性重建参与者。
P0-B0：一次性补齐兼容消费者所需的新值式公开合同。
P0-B1：两个纯只读 / 消息桥改接租约。
P0-B2：概念命名写路径在 #214 恢复且 P0-C 完成后改接。
P0-B3：自我治理权威写路径按完整动作 / 证据 / 结算合同改接。
P0-B4：SQL / 控制面板只读投影改接。
P0-B5：旧入口局部仓库 / 服务退出生产所有者，历史夹具保持隔离自检。
PERSIST-S1-A：从生产正式租约取得同域独占许可，形成九段不可变冻结材料。
#226 v0.2：只在 A 正式完成后再次逐签名修订，承接编码 / 严格自读 / 原子发布。
```

`入口.cpp` 是 P0-A 与 P0-B5 的串行接缝。设计计划必须明确前一提交释放写入所有权后后一计划才取得，不能并行施工。其它批次应按独立目标文件组分配，避免用一个大计划同时修改入口、全部领域、线程、概念活动和快照。

## 7. 生产切换硬门禁

```text
完整候选
-> 系统角色初始化
-> 唯一宿主首次发布
-> 同身份租约
-> 纯只读消费者先改接
-> 权威写消费者逐项串行改接，禁止旧 / 新双写和旧服务兜底
-> SQL / 控制面板只读投影改接
-> 正常入口旧仓库 / 传统服务构造及生产包含点归零
-> 快照冻结编排才可消费租约和取得同域独占许可
```

以下任一情况均保持 PERSIST-S1-A 与 #226 门控：

```text
生产宿主 / 租约仍只在自检出现。
任一机器事实写入口仍持旧局部仓库或传统服务。
段 9 生命周期角色、签名或活动版本无唯一所有者。
快照参与者不能在同一独占许可内复制。
索引所有者声明或领域登记不完整。
```

## 8. O1 与完成边界

O1 仍只证明四仓库权威结构和无私有权威侧表；尚未接运行期业务装配。P0 与 PERSIST-S1-A 只能在段 8 显式声明 O1 无私有权威状态，并让既有结构复用段 2—5；不得接生产、统计、学习、晋级或新增第十段。

本轮不得声明：

```text
生产入口已切换到运行期上下文。
旧入口对象或传统服务生产调用已归零。
概念活动新所有者、段 9 或九段冻结材料已实现。
HYSNAP01 已编码、发布、解析或恢复。
O1 已接生产、统计或学习。
旧持久化、数据库、控制面板或旧能力迁移已完成。
```

## 9. 本轮文件范围与验证

本轮只新增：

```text
实施记录/20260718_PERSIST-S1-P0_生产运行期所有权与概念活动承载当前事实矩阵.md
实施记录/20260718_PERSIST-S1-P0_生产运行期所有权与概念活动承载当前事实复核_Codex断点清单.md
```

未修改 C++、工程、AGENTS、规范、流程图、详细设计、计划、计划索引、任务队列、项目记忆、既有实施记录或迁移台账；未运行 `msbuild`、程序、自检或压力测试；未登记阶段；未读取、恢复、修改或删除 stash。

验证结果：

```text
python .\tools\check_specs.py --strict：通过。
git diff --check：通过。
git diff --cached --name-only：仅两份允许文件。
允许 / 禁止范围：通过。
```

## 10. 角色切换门禁

本轮精确提交和非强制推送成功前，当前物理窗口仍是 #295 执行角色，不得修改中央路由、归档 #295、生成 P0 / PERSIST-S1-A 或修订 #226。

推送成功后，当前物理窗口必须重新读取：

```text
Git HEAD 与 origin/main。
计划索引、Codex 任务队列和窗口交互记录。
#295、JY-390、#294 正式证据、本断点和独立矩阵。
当前实际接口与本轮精确提交范围。
```

全部一致后才切回设计角色。因为目标是当前物理窗口，不发送自发自收任务消息。

## 11. 完成声明六项证据

| 维度 | 本轮证据 |
| --- | --- |
| 输入入口 | #295、JY-390、E131、#294 证据、#271 矩阵、恢复三件套和当前代码 |
| 核心处理 | M0—M6 与 A01—A18，覆盖入口对象、宿主 / 租约、兼容消费者、22 公开面、段 9 字段和后继批次 |
| 结构承载 | 独立事实矩阵与本断点；源文件和中央路由零修改 |
| 输出变化 | 形成 P0-A/B/C 与 PERSIST-S1-A 的当前事实依据；状态为执行完成待设计同步 |
| 调用路径 | `E131 -> 执行角色重读 -> M0—M5 -> 两份记录 -> 验证 / 提交 / 推送 -> 设计角色复核` |
| 验证方式 | strict、diff、允许范围、零构建、零运行、零阶段和 stash 零操作 |
