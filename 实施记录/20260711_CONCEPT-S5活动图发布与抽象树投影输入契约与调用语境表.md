# CONCEPT-S5 活动图发布与抽象树投影输入契约与调用语境表

日期：2026-07-11

状态：#199 纠偏后代码事实

| 入口 | 调用方 | 输入字段 | 输入来源 | 上游是否保证有效 | 允许逻辑内返回 | 追根因触发条件 | 结构变化 | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 初始化活动图基础版本 | 任意探测 / 测试调用 | 当前已登记根组 | 请求或测试语境 | 否 | 根尚未齐全时允许返回空 | 已有快照但完整性复核失败 | 首次成功时写版本 1 快照 | 根数量、版本和快照读回 |
| 发布候选图版本并返回结果 | S4 固定点正式调用 | 基准版本、计算状态、候选项、三类边、待重算材料 | 正式流程材料 | 已收敛候选内部完整性是 | 基准过期、未收敛、空候选 | 基础图异常；已收敛候选内部错误；创建、生命周期、映射、关系、读回或清理异常 | 通过门禁后写候选结构；提交点替换活动快照 | `候选图发布状态`、版本、结构数量、异常句柄 |
| 发布候选图版本 | 兼容调用方 | 同上 | 候选材料 | 否 | 投影为空只表示没有可兼容返回的版本 | 失败原因不得由该 `optional` 投影裁决 | 与详细入口一致 | 正式测试同时读取详细结果 |
| 发布候选图版本并返回结果 | 负例测试或外部候选适配层 | 同上 | 测试材料 / 候选材料 | 否 | 过期、未收敛和空候选可写前拒绝 | 入口接受后首次创建或写关系异常；正式已收敛候选内部不合法 | 写前拒绝不变；写后失败清理 | CONCEPT-S5-CORR-A01 至 A06 |
| 发布候选图版本并返回结果 | 提交点复核调用 | 新活动快照和待失效旧关系 | 服务内部结构 | 是 | 无 | 快照已替换后旧关系失效异常 | 新活动版本已提交；旧关系收口可能异常 | 状态 `已提交但旧关系失效异常`、新版本、异常关系句柄 |
| 活动关系和签名读取 | 任意查询方 | 源 / 目标 / 概念句柄 | 请求材料 | 否 | 无快照或未命中可返回空 | 无 | 否 | 返回空组 / 空 optional |
| 活动关系和签名读取 | 概念图正式内部读回 | 已由活动快照给出的句柄 | 正式流程材料 | 是 | 不允许静默未命中 | 活动快照存在但签名、关系或生命周期读回缺失 | 否 | 公共追根因检查、快照完整性自检 |
| 读取抽象树视图并返回结果 | 任意 UI 请求 / 测试 | 根句柄、节点预算、深度预算 | 请求材料 | 否 | 无快照、非活动根、零预算、预算耗尽 | 已验证活动图出现空路径、成环或最终视图不完整 | 否 | 结构化读取状态和结构数量 |
| 读取抽象树视图 | 兼容单树读取方 | 同上 | 请求材料 | 否 | 详细结果非成功时返回空 | 原因不得由空 optional 裁决 | 否 | 与详细结果对照 |
| 读取全部抽象树视图并返回结果 | PANEL 正式四树契约 / 正式验收 | 默认预算、当前活动快照 | 正式流程材料 | 是 | 显式零预算或预算耗尽 | 缺快照、缺四根、任一根投影内部错误、最终四树不完整 | 否 | CONCEPT-S5-CORR-A09、四树完整性 |

## 结论

同一空 `optional` 不再承担失败分类。正式发布使用 `候选图发布结果`，任意单树和正式四树分别使用结构化读取入口。活动快照替换是提交点；提交后旧边异常必须返回实际已生效版本。
