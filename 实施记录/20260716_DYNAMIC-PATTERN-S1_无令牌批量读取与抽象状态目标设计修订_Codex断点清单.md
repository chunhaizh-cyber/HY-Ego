# DYNAMIC-PATTERN-S1 无令牌批量读取与抽象状态目标设计修订 Codex 断点清单

日期：2026-07-16

交互：WIN-0001-E82

计划 / 队列 / 阶段：#232 / DQ-124 / DYNAMIC-PATTERN-S1 / 630

决策：JY-363

状态：设计三件套已重做 / 当前待执行 / 630 未实施 / 未改 C++ / 未构建 / 未运行程序

## 1. 输入

```text
#231 正式提交 3c2c324 及权威事实断点。
#224 / #225 / #271 正式协议、组合器、业务服务和兼容边界。
旧动态证据窗口流程图、详细设计和原 #232 计划。
当前计划索引、任务队列、状态、决策、问题、窗口交互和迁移台账。
```

## 2. 核心裁决

1. 删除 #217 和“公开服务取得共享许可并下传原始令牌”的当前合同。
2. 完整冻结请求 + 新版回执形成非权威观察回合身份；调用方只补非零、同回合唯一片段序号，不新增权威任务尝试节点。
3. 版本 1 只支持正式任务执行链的抽象状态目标；特征值反向语义后置，二次特征角色固定为空。
4. 新增 `数据操作.动态模式`，内部一次共享许可按完整节点 / 主信息 / 关系句柄、端点、顺序号、版本和有效 / 历史阶段形成权威批次。
5. 公开组合器、服务、材料和算法零原始令牌、许可、仓库、接线、会话和锁能力。
6. 新建材料、算法、数据操作、服务、组合、自检六个真模块，阶段仍为 630。
7. #232 不修改既有核心、服务、数据操作、线程协议或运行期装配，不持久化候选。
8. #233/#234 不自动消费旧计划，等待 #232 正式 DTO 后重做；#235 后置，#214 继续暂停。

## 3. 输入、处理、结构、输出、调用、验证

| 项 | 内容 |
| --- | --- |
| 输入入口 | 新版回执、内嵌完整冻结请求、片段序号和规则版本。 |
| 核心处理 | 协议前置 -> 数据操作一次共享许可权威批量读取 -> 服务正式链复核 -> 纯值排序 / 结构键 / 哈希。 |
| 结构承载 | 权威事实仍由四仓库承载；本切片只返回不可变非权威 DTO，不新增节点、关系、索引、缓存或持久化段。 |
| 输出变化 | #232 / DQ-124 以原编号转当前待执行；#233/#234 继续设计门控。 |
| 调用路径 | `组合.动态观察 -> 服务.动态模式 -> 数据操作.动态模式 / 算法.动态模式 -> 材料.动态模式`。 |
| 验证方式 | Mermaid 同源、三件套 / 索引 / 队列 / JY-363 一致、严格规范、Git 差异和禁止范围检查；本设计轮不构建不运行。 |

## 4. 允许与禁止

执行允许范围以原 #232 修订计划第 5 节为准。核心、既有数据操作 / 服务、线程协议、运行期装配、持久化、界面、SQL 和救援 stash 全部禁止修改。

## 5. 完成声明边界

本记录只证明 #232 流程图、详细设计、计划和治理路由已按 #231 正式事实重做并重新发布。不得宣称 630、六个模块、规范动态窗口、重复聚类、运动基元、因果概率、用途学习、运行期接线、旧能力迁移或项目迁移已经实现。
