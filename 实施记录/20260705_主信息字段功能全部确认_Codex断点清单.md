# 主信息字段功能全部确认 Codex 断点清单

日期：2026-07-05

## 1. 当前入口

用户输入：

```text
全部确认
```

本次入口承接：

```text
实施记录/20260705_主信息字段功能确认待确认清单.md
计划/计划索引.md
规范/主信息系统规范.md
规范/详细设计/主信息系统详细设计.md
```

## 2. 当前判断

`全部确认` 表示主信息字段功能确认清单中的建议处理和第 6 节默认口径全部确认。

本次确认只打开以下动作：

```text
把待确认清单改为已确认记录。
把确认结论更新到主信息系统规范。
把确认结论更新到主信息系统详细设计。
更新计划索引和项目记忆的门禁状态。
登记下一步主信息迁移实施计划。
```

本次确认不打开以下动作：

```text
不直接迁移旧项目主信息字段。
不新增通用主信息字段。
不复制旧项目主信息类代码。
不声明旧项目能力已迁移。
```

## 3. 已确认核心口径

```text
旧项目主信息字段不直接迁移。
MI-YS5 语素对应信息继续由关系承载，不恢复为主信息字段。
MI-V1 值容器语义只保留在特征值服务范围内，不扩展成需求 / 任务 / 方法通用值容器。
名称、类型、任务名称、方法名称走语素入口关系或候选查询，不走裸字段。
状态、动态、二次特征、因果字段族拆到对应领域系统，不作为主信息第一轮字段。
外设、体素、SQL、控制面板、语言理解和指代系统相关字段全部暂缓。
MI-T2、MI-T3、MI-M1、MI-M2 登记为后续任务 / 方法专项和动作入口规范，不进入本轮主信息迁移。
```

## 4. 下一步

```text
更新确认清单、规范、详细设计、计划索引和项目记忆。
执行文档差异检查。
本切片提交并推送到云端。
后续建立主信息迁移实施计划或字段迁移映射计划，确认后才允许进入结构或代码迁移。
```

## 5. 禁止扩大声明

```text
不能声明主信息字段已迁移。
不能声明主信息代码已迁移。
不能声明旧项目能力已迁移。
不能声明需求 / 任务 / 方法 / 状态 / 动态 / 因果专项已完成。
```
