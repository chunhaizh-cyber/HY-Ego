# FS-03 特征服务封口增强专项生成 Codex 断点清单

日期：2026-07-06
状态：已生成 / 待确认 / 未改代码 / 未迁移旧函数

## 1. 输入入口

```text
用户给出当前状态：FS-03 特征特征值逻辑迁移包归并表已确认。
下一步要求：生成 FS-03 特征服务封口增强专项。
计划索引 HR-001-FM-SCAN：FS-03 已确认逻辑包需补规范 / 详细设计 / 计划。
项目记忆/Codex任务队列.md：任务 66 已登记为 FS-03 已确认逻辑迁移包文档治理项。
```

## 2. 核心处理

```text
复核 AGENTS.md、计划/计划索引.md、规范/000_项目规则总纲.md、规范/001_规则迁移清单.md。
复核 FS-03 逻辑迁移包归并表，确认只纳入 FS-03-A、FS-03-C、FS-03-D。
复核基础信息服务分层规范、领域服务授权写入规范、错误结果与入口拒绝规范、领域服务最小入口详细设计。
只读核对当前 特征服务.h、特征值服务.h、句柄.h 的现有入口和节点 / 关系类型。
生成特征服务封口增强详细设计和专项计划。
同步计划索引、项目记忆和本断点清单。
```

## 3. 结构承载

```text
规范/详细设计/特征服务封口增强详细设计.md
计划/20260706_FS03_特征服务封口增强专项_v0.1.md
实施记录/20260706_FS03_特征服务封口增强专项生成_Codex断点清单.md
计划/计划索引.md
项目记忆/当前状态.md
项目记忆/决策记录.md
项目记忆/实施记录.md
项目记忆/待确认问题.md
项目记忆/Codex任务队列.md
项目记忆/验收记录.md
```

## 4. 输出变化

```text
FS-03-A 特征身份与语义类型读取已形成特征服务只读增强设计口径。
FS-03-C 实例特征创建、宿主槽位与当前值读取已形成特征服务宿主槽位设计口径。
FS-03-D I64 特征值写入读取与特征服务封口已形成对外封口设计口径。
本专项状态为已生成 / 待确认 / 未改代码 / 未迁移旧函数。
```

## 5. 调用路径

```text
用户下一步指令
-> 计划索引 HR-001-FM-SCAN
-> FS-03 已确认逻辑迁移包
-> 特征服务封口增强详细设计
-> FS-03 特征服务封口增强专项计划
-> 项目记忆和计划索引同步
```

## 6. 验证方式

```text
文档生成检查。
文档一致性检索。
git diff --check。
tools/check_specs.py 如存在则运行。
git status 确认未纳入无关修改。
```

本切片不运行 C++ 构建，不运行当前项目入口。

## 7. 下一步

```text
由用户确认 规范/详细设计/特征服务封口增强详细设计.md 和 计划/20260706_FS03_特征服务封口增强专项_v0.1.md。
确认后再把代码实施项登记到可执行队列。
代码实施切片必须另行确认允许文件、禁止文件和验证方式。
FS-02 已确认逻辑包补规范 / 详细设计 / 计划仍需继续承接。
```

## 8. 禁止扩大声明

```text
不得声明 FS-03 特征服务封口增强代码已实现。
不得声明旧特征类或旧特征值类已迁移。
不得声明旧特征主信息、旧抽象特征主信息或旧特征值主信息字段可直接迁移。
不得声明 VecU / VecI64 原始值容器、内容哈希、稳态系统、抽象特征系统、值域系统、需求结算规则或体素系统完成。
不得接控制面板 / SQL / D455 / 体素 / 外设。
不得把文档生成说成构建、入口拒绝或最小闭环验证通过。
```
