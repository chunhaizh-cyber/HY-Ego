# 鱼巢逻辑与当前实现流程覆盖输入契约与调用语境表

日期：2026-07-11（2026-07-12 按 HEAD `15032ed` 复核）

| 入口 | 调用方 | 输入字段 | 输入来源 | 上游是否保证有效 | 允许逻辑内返回 | 追根因触发条件 | 结构变化 | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 旧代码证据冻结 | 覆盖审计 | HEAD、远端差异、dirty 文件与哈希 | `D:\鱼巢` Git 和文件系统 | 否，当前 ahead 4 且 dirty | 基线未冻结时只输出缺口，不宣称完成 | 已宣称使用某提交却读取到另一份内容 | 无 | `git status`、`git log`、`git diff --name-status` |
| 旧函数证据扫描 | 迁移治理 | OF 编号、文件、函数族、调用点 | 旧源码 | 否，现有清单明确允许追加 | 未扫描项登记为下一编号 | 已登记事实与当前代码矛盾却继续计入迁移完成 | 无 | 复核 `OF-207` 空槽与变更文件 |
| 旧运行主链提取 | 文档治理 | 启动、初始化、线程、需求、任务、方法、结果、结算入口 | 旧代码冻结快照 | 仅冻结后保证 | 排除 / 暂缓分支可停止在证据层 | 正式代码调用链被流程图改序、删节点或混入新实现事实 | 无 | 每个目标完整纠偏包与代码行映射 |
| 当前代码入口盘点 | #236 与本计划 | 当前 HEAD、dirty diff、工程登记、入口分段 | `D:\海中鱼巢` | 本轮固定保证为 HEAD `15032ed`；工作树并行变化不进入事实集 | HEAD 外变化只登记为排除材料 | 把 #211 / JY-254 未提交文件或预期模块写成 HEAD 已实现 | 无 | `git ls-tree` / `git show` 与 worktree 分栏记录 |
| 批量现状图当前性复核 | #240 S2 | 45 对现状图、代码版本、覆盖文件、根目录对应图 | `流程图/现状流程图/`、HEAD 源码 | 否；43 张固定为 `3f08239`，专门目录自 `63cec55` 后未变化 | 未受代码 / 施工图变化影响的图可保留，但必须留下比对证据 | 只因 MD/HTML 配对存在就宣称语义仍当前 | 无 | `git diff 3f08239..15032ed`、覆盖元数据集合差、根目录 / 专门目录哈希比对 |
| `main` 参数解析 | 命令行 | `--self-test-exit`、`--database-self-test-exit`、`--headless` | 外部请求 | 否 | 未命中选项采用默认窗口运行 | 参数解析后内部装配失败 | 无 | 入口现状图与参数矩阵 |
| 自我线程初始化 | `main` | 两个稳定键、当前值、目标值 | 固定启动配置 | 是，正式启动路径应提供有效配置；自检另有无效样本 | 外部 / 自检无效参数拒绝 | 参数通过后语素、世界树、根需求写入或读回不完整 | 写节点、主信息、关系、索引、状态 | 初始化现状图、结构数量和读回 |
| 领域服务调用 | 初始化、治理路由、任务方法流程 | 稳定句柄、类型、关系、时间戳、版本 | 上游正式流程或外部请求 | 按调用语境不同 | 外部请求材料无效可拒绝 | 正式流程承诺有效后仍写入 / 读回失败 | 可能写权威结构 | 契约表按外部 / 正式语境拆行 |
| 自我治理消息准入 | 上行桥、任务线程、外部材料桥 | 类型、来源、优先级、句柄、版本、幂等键 | 只读材料 | 否 | 无效、过期、重复、队列满可逻辑返回 | 准入后路由到领域服务出现内部矛盾 | 写非权威队列；后续可能触发领域写入 | 消息和领域路由分别画图 |
| 方法召回索引 | 方法服务 / 召回服务 | 登记、生命周期、规格、能力键、来源版本 | 权威方法结构的派生材料 | 正式重建时保证来源可读 | 过期候选过滤、空桶返回 | 同一来源出现互相矛盾的登记或规格 | 写非权威派生索引 | S3 现状图和来源复核表 |
| SQL 审计适配 | `main`、控制面板 | 环境配置、结构统计快照、读取上限 | 外部数据库配置与非权威快照 | 否 | 配置缺失、数据库不可用可报告并按模式继续 | 写入成功后自读记录矛盾，或审计数据试图裁决运行事实 | 只写审计数据库 | SQL 现状图和 DB 自检模式 |
| 控制面板运行 | `main` | 只读服务、数据库适配、结构快照、停止信号 | 当前进程 | 正式路径保证依赖已构造 | 正常关闭 | 窗口创建 / 运行内部失败 | 不写权威结构 | 窗口现状图和结构不变验证 |
| 流程图覆盖判定 | 文档治理 | MD、HTML、映射、契约、二分、偏差、计划登记、代码版本一致性 | 流程图目录、实施记录和 Git | 否 | 产物缺失或代码漂移时输出“未覆盖 / 待刷新” | 缺产物、基线漂移或副本不一致仍标为 HEAD 当前完整现状包 | 无 | `check_flowchart_correction.py --strict` 加 Git 基线 / 哈希复核；配对检查不能替代当前性检查 |

## 语境规则

同一个领域入口如果既接外部候选又接正式上游材料，必须在目标流程图中拆成两条：

```text
外部 / 自检 / 候选语境：前置拒绝属于逻辑内返回。
正式上游契约语境：上游已保证有效却出现同一拒绝，属于追根因解决。
```
