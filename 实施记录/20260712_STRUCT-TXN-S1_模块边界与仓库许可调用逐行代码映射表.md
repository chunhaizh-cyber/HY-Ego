# STRUCT-TXN-S1 模块边界与仓库许可调用逐行代码映射表

基线：`9823507`。执行窗口三个未提交骨架只证明 ABI 缺口，不作为正式实现。逐入口合同见 `实施记录/20260712_STRUCT-TXN-S1_数据桥ABI与四仓库逐公开入口许可矩阵.md`；行号在执行前重取。

| 文件 | 行号 / 范围 | 代码行为 | 流程图节点 | 行为类型 | 结构变化 | 权威性 | 非成功口径 | 纠偏建议 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `主信息仓库.h/.cpp` | 9 个公开声明入口 | 删除身份与节点创建检查存在并发边界 | A-B、J-K | 读写 | 主信息 | 权威事实 | 既有入口拒绝 | 删除取独占；其余取共享；8 个令牌路径 |
| `节点仓库.h/.cpp` | 7 个公开声明入口 | 创建先读主信息；删除可使关系 / 索引端点失效 | B-D、J-M | 读写 | 节点 | 权威事实 | 既有入口拒绝 | 删除取独占；创建 / 读取取共享；5 个令牌路径，编号读取不取许可 |
| `关系仓库.h/.cpp` | 25 个公开声明入口 | 创建、重挂、审计、查询、统计、父链多处跨节点仓库 | B-D、M-N | 读写 | 关系 | 权威事实 | 既有入口拒绝；内部不一致追根因 | 24 个令牌路径；锁内只复制，锁外同令牌复核节点 |
| `索引仓库.h/.cpp` | 5 个公开声明入口 | 绑定写前读节点；读取 / 计数持索引锁读节点 | B-D、M-N | 读写 | 索引 | 非权威查找结构 | 既有入口拒绝 | 4 个令牌路径；锁内复制，锁外同令牌复核节点 |
| `结构事务接线.数据.h` | 未提交骨架 | 当前只有域、裸 `void*` 和三个令牌字段，无函数表 / RAII / 生命周期 | H-M | 运行期桥 | 无机器事实 | 非权威控制材料 | 不完整骨架不可执行 | 按 JY-272 固定 ABI 重写；不保留裸协调器指针 |
| `协调.结构事务.ixx` | 未提交骨架 | 当前只返回 `{域,this}`，无锁取得、活动表、线程归属或重入判断 | H-M | 许可行为 | 无机器事实 | 非权威控制材料 | 不完整骨架不可执行 | 以共享状态实现四个函数入口、纪元和活动验证 |
| `写入事务.h/.cpp` | 全文件 | 只保存状态与失败文本 | F | 状态壳 | 无仓库隔离 | 非权威控制材料 | 状态机逻辑返回 | #217 禁止修改或冒充事务许可 |
| `入口.cpp` | 主装配构造区及隔离夹具构造点 | 按旧构造器建立主装配和大量隔离仓库 | E、O-P | 装配 | 构造对象 | 运行期引用 | 不适用 | #217 自检内部构造已接域隔离组；主装配不接域 |
| `海中鱼巣.vcxproj/.filters` | 核心与模块登记区 | 四仓库为普通 `.cpp`，尚无事务数据头 / 真模块 | G-I | 工程 | 无 | 编译事实 | 不适用 | 数据头 ClInclude、两个模块 ClCompile |
