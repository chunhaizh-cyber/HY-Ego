# 任务系统详细设计小修确认 Codex 断点清单

日期：2026-07-06
状态：已完成 / 文档小修确认 / 未改代码

## 1. 输入入口

用户确认结论：

```text
任务系统详细设计方向可作为第一轮任务系统方案基础。
补齐 `按需求创建任务` 的任务目标状态关系写入和拒绝规则后，可确认为第一轮任务系统详细设计。
类似“任务节点创建成功但需求关系失败”属于逻辑错误，必须追根因，记录逻辑错误日志并弹窗提醒，不得抛弃错误或直接返回不管。
```

## 2. 本次范围

允许：

```text
修订任务系统规范和任务系统详细设计。
补齐任务目标状态关系写入、拒绝规则、任务专用读取边界和第一轮无半结构策略。
同步计划索引和项目记忆状态。
```

禁止：

```text
修改代码。
进入任务系统第一轮代码实施。
迁移旧任务主信息字段。
声明任务系统代码已实现或任务状态机已完成。
```

## 3. 待修订要点

```text
P0：`按需求创建任务` 写入结构补任务目标状态关系。
P0：入口拒绝补需求无目标状态、需求目标状态不唯一、需求目标状态不是状态节点、任务目标状态关系写入失败。
P1：外部调用方不得直接用裸 `引用` 关系读取任务事实。
P1：任务生命周期状态值改为第一轮任务生命周期状态枚举草案，并声明状态值不是 bool 事实，不能单独证明任务完成。
P1：补第一轮先预读、后写入、末尾统一可读的无半结构策略。
P1：内部写入结果不及预期属于逻辑错误，必须按容错宏诊断、逻辑错误日志和弹窗规则追根因。
```

## 4. 验证方式

```powershell
rg -n "任务目标状态关系|需求无目标状态|裸 `引用`|第一轮任务生命周期状态枚举草案|先预读|逻辑错误" 规范 项目记忆 计划 实施记录
git diff --check
```

本切片不改代码，不运行构建和默认入口。

验证结果：

```text
任务系统待确认旧状态矛盾检索无命中。
任务目标状态关系、需求无目标状态拒绝、第一轮任务生命周期状态枚举草案、先预读策略和逻辑错误追根因规则均可在规范、详细设计和项目记忆中追溯。
git diff --check 通过，仅有 CRLF 换行提示。
```

## 5. 输出变化

```text
任务系统规范和详细设计已按审查意见小修后确认。
`按需求创建任务` 第一轮代码实施必须写入任务目标状态关系。
入口通过后的内部不及预期已明确为逻辑错误追根因路径。
下一步转为任务系统第一轮代码实施。
```

## 6. 禁止扩大声明

```text
本次只声明任务系统规范和详细设计已确认。
不能声明任务系统代码已实现。
不能声明任务状态机、任务筹办或任务执行完成。
不能声明旧任务字段已迁移。
```
