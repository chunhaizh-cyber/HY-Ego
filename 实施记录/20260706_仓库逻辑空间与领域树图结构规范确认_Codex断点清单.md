# 仓库逻辑空间与领域树图结构规范确认 Codex 断点清单

日期：2026-07-06
状态：已完成 / 规范已确认 / 未改代码

## 1. 输入入口

用户附加审查意见结论：

```text
规范/仓库逻辑空间与领域树结构规范.md 方向正确，基本可以作为项目级规范进入确认流程。
建议小修后确认，不建议直接不改就标记为已确认。
```

## 2. 本次范围

允许：

```text
修订仓库逻辑空间规范正文。
补齐逻辑空间定位方式、跨空间关系准入表、索引命名空间和当前状态说明。
修正重复关系拒绝口径，避免误伤语素多对应 / 多路追溯。
更新计划索引和项目记忆。
```

禁止：

```text
修改代码。
新增物理仓库。
新增旧项目主信息字段。
将规范确认扩大为代码实现、最小闭环、旧能力迁移或任务 / 方法系统完成。
```

## 3. 修订内容

```text
标题改为“仓库逻辑空间与领域树图结构规范”。
状态改为“已确认项目规范 / 第一轮仓库逻辑空间与领域树图口径”。
新增逻辑空间定位方式，明确节点类型、创建服务、根 / 入口关系、关系类型、索引命名空间和非权威空间标签的边界。
索引边界表新增当前状态列，并补索引命名空间规则。
关系重复拒绝改为按关系类型唯一性约束拒绝，保留语素多对应 / 多路追溯集合读取口径。
新增跨空间关系准入表。
逻辑空间表新增当前状态列。
风险清单新增“逻辑空间误解为物理拆仓”和“根节点 / 空间入口缺失”两项。
```

## 4. 验证方式

```text
git diff --check
rg -n "仓库逻辑空间与领域树图结构规范|已确认项目规范|逻辑空间定位方式|跨空间关系准入|索引命名空间|多目标|当前状态" 规范 计划 项目记忆 实施记录
```

本切片不改代码，不运行构建和默认入口。

## 5. 输出变化

```text
仓库逻辑空间与领域树图结构规范已成为确认项目规范。
计划索引下一步从确认该规范推进到建立任务系统规范和详细设计。
项目记忆 QR-019 关闭。
```

## 6. 禁止扩大声明

```text
本次只能声明仓库逻辑空间与领域树图结构规范已确认。
不能声明逻辑树图代码已经完整实现。
不能声明任务系统、方法系统、动作入口或缓存统计系统已完成。
不能声明主信息迁移完成或旧项目能力已迁移。
```
