# FS-08 缓存统计结构快照增强 S0 当前代码事实扫描 Codex 断点清单

日期：2026-07-07
状态：已完成 / 只读扫描 / 未改代码 / 未迁移旧函数

## 1. 输入入口

```text
用户要求执行当前可执行队列。
计划/20260707_FS08_缓存统计结构快照增强专项_v0.1.md 已确认。
项目记忆/Codex任务队列.md 已登记：执行 FS-08 S0 当前代码事实扫描。
```

本轮只执行 S0 当前代码事实扫描，不进入 S1-S4，不新建或修改缓存统计 C++ 文件。

## 2. 扫描范围

```text
海中鱼巣/领域/非权威缓存统计服务.h
海中鱼巣/领域/统计服务.h
海中鱼巣/核心/节点仓库.h
海中鱼巣/核心/关系仓库.h
海中鱼巣/核心/索引仓库.h
海中鱼巣/入口.cpp
```

实际扫描命令以 `rg` 和缺失文件检查为主，未运行构建，未运行默认入口。

## 3. 当前代码事实

### 3.1 独立非权威缓存统计服务文件

```text
海中鱼巣/领域/非权威缓存统计服务.h 当前不存在。
```

当前缓存统计能力落在 `海中鱼巣/领域/统计服务.h`。

### 3.2 统计服务当前入口

当前 `统计服务.h` 已有以下结构和入口：

```text
缓存类型：结构统计、特征值内容哈希、特征值命中观察、需求结算摘要。
来源结构类型。
缓存值类型。
缓存失效条件。
缓存命名空间。
结构统计快照。
统计服务。
定义缓存命名空间(...)
创建统计快照结构(...)
重建结构统计缓存(const 节点仓库&, const 关系仓库&, const 索引仓库&)
读取结构统计缓存(...)
清空结构统计缓存(...)
清空全部缓存壳()
缓存可裁决业务事实(...)
```

其中 `缓存可裁决业务事实(...)` 对有效命名空间仍返回 false，符合非权威缓存不裁决业务事实的第一轮边界。

### 3.3 权威结构数量读取事实

当前核心仓库已有结构数量只读入口：

```text
节点仓库::有效节点数量()
关系仓库::有效关系数量()
索引仓库::有效主键数量()
```

`统计服务::重建结构统计缓存` 当前从上述权威仓库数量读数生成结构统计快照，不从旧缓存互相推导。

### 3.4 默认入口样例事实

当前 `入口.cpp` 已覆盖：

```text
定义结构统计缓存命名空间。
无效缓存命名空间拒绝。
空缓存读取返回空。
重建结构统计缓存。
读取结构统计缓存。
缓存业务裁决拒绝。
无效缓存清空拒绝。
清空结构统计缓存。
清空全部缓存壳。
结构统计缓存清空后权威节点 / 关系 / 索引数量不变。
```

控制台输出中已有 `非权威缓存统计第一轮` 和 `缓存统计: 节点=...` 相关验收文字，但本轮未重新运行入口。

## 4. 当前缺口

```text
1. 当前没有独立 `非权威缓存统计服务.h`，第一轮缓存统计能力仍在 `统计服务.h`。
2. 当前只有结构统计运行期快照第一轮入口，没有特征值内容哈希实际计算。
3. 当前没有特征值命中观察缓存、哈希候选索引缓存、需求结算摘要缓存、存在观测缓存和二次特征签名缓存。
4. 当前没有孤立关系诊断报告结构，只能说明本轮不自动修复关系。
5. 当前没有 SQL 快照、控制面板摘要、线程统计、D455、体素或外设统计接入。
6. 当前 S1-S4 仍需另建独立代码实施切片并确认允许文件、禁止文件和验证方式。
```

## 5. 写回问题

```text
FS08-Q1：
后续 S1-S4 是否保留 `统计服务.h` 名称，或改建 / 包裹 `非权威缓存统计服务.h`，需要设计窗口确认。

FS08-Q2：
第一轮结构统计快照已存在，但特征值哈希、命中观察、需求结算摘要、存在观测和二次特征签名缓存仍不得借本轮 S0 直接实施。

FS08-Q3：
缓存缺失、清空和重建不得改变权威节点、关系、索引、状态、动态、需求、任务或方法事实。
```

## 6. 完成声明边界

| 项 | 结论 |
| --- | --- |
| 输入入口 | 已确认 FS-08 专项计划和可执行队列中的 S0 扫描项。 |
| 核心处理 | 只读扫描当前缓存统计入口、核心结构数量入口和默认入口调用点。 |
| 结构承载 | 本断点清单、计划索引、项目记忆。 |
| 输出变化 | FS-08 S0 当前代码事实已记录，后续缺口已写入待确认问题。 |
| 调用路径 | 执行队列 -> FS-08 专项计划 -> 当前代码事实扫描 -> 断点清单 -> 项目记忆同步。 |
| 验证方式 | `rg` 扫描、缺失文件检查、`git diff --check`；未运行构建，未运行默认入口。 |

## 7. 禁止扩大声明

```text
不得宣称缓存统计增强代码已实施。
不得宣称完整非权威缓存系统完成。
不得宣称特征值哈希、命中缓存、需求结算摘要、存在观测缓存或二次特征签名缓存已实现。
不得宣称旧缓存 / 统计函数体或旧主信息缓存字段已迁移。
不得宣称 SQL / 控制面板 / D455 / 体素 / 外设已接入。
```
