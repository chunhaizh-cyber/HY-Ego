# PANEL-RUN-S1 控制面板原生窗口代码实施 Codex 断点清单

日期：2026-07-10

## 输入入口

`计划/已完成计划/20260710_PANEL-RUN-S1_控制面板原生窗口可运行代码实施切片_v0.1.md`

## 核心处理

新增原生 Win32 控制面板窗口，默认无参数运行进入窗口消息循环；`--self-test-exit` 保持自检后退出，`--headless` 保留无窗口常驻入口。

## 结构承载

```text
海中鱼巣/界面/控制面板窗口.h
海中鱼巣/界面/控制面板窗口.cpp
海中鱼巣/入口.cpp
海中鱼巣.vcxproj
```

窗口只承载控制面板服务材料和结构统计快照，不持有仓库可写引用。

## 输出变化

```text
默认运行显示“海中鱼巣控制面板”。
显示六类树根、节点 / 关系 / 索引数量和只读详情。
刷新只重读材料。
操作请求只生成请求壳，不执行领域动作。
WM_CLOSE 正常结束消息循环并退出进程。
```

## 调用路径

`main -> 控制面板窗口::运行 -> Win32 消息循环 -> 控制面板服务只读入口`

## 验证结果

```text
Debug x64：0 警告，0 错误。
--self-test-exit：返回 0，不创建窗口。
默认运行：可见顶层窗口标题为“海中鱼巣控制面板”。
WM_CLOSE：进程正常退出，退出码 0。
用户已确认控制面板可以运行。
```

## 完成边界

只证明第一轮原生只读控制面板窗口可运行；数据库查看由后续 DB-PANEL-S1 独立切片承接。
