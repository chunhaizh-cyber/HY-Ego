# FS-02 动态聚合因果模板删除实例状态后续专项生成 Codex 断点清单

日期：2026-07-07
状态：已生成 / 已确认计划 / 未改代码 / 未迁移旧函数

## 1. 输入入口

用户要求按照新规则持续生成计划，直到遇到问题。

## 2. 核心处理

基于已确认的 FS-02 逻辑迁移包归并表、FS-02 服务增强详细设计、已完成第一轮代码实施切片和第一轮明确暂缓项，生成：

```text
计划/20260707_FS02_动态聚合因果模板删除实例状态后续专项_v0.1.md
```

同步登记该计划为已确认计划。未进入 C++ 实施，未登记可执行 S0。

## 3. 结构承载

```text
计划/20260707_FS02_动态聚合因果模板删除实例状态后续专项_v0.1.md
实施记录/20260707_FS02_动态聚合因果模板删除实例状态后续专项生成_Codex断点清单.md
计划/计划索引.md
项目记忆/Codex任务队列.md
项目记忆/当前状态.md
项目记忆/待确认问题.md
项目记忆/实施记录.md
```

## 4. 输出变化

FS-02 后续专项已确认，范围只包括：

```text
FS-02-G 动态聚合和运动基元候选。
FS-02-J 因果模板、任务化和查询候选。
删除实例状态生命周期治理。
```

确认前不得登记 FS-02 后续专项 S0，不得修改 C++。

## 5. 调用路径

```text
设计计划队列 DQ-09
-> FS-02 已确认逻辑迁移包和第一轮切片边界
-> 生成后续专项已确认计划
-> 登记 QR-053 待确认问题
-> 同步计划索引和项目记忆
```

## 6. 验证方式

本断点清单生成阶段应执行：

```powershell
git diff --check
if (Test-Path .\tools\check_specs.py) { python .\tools\check_specs.py }
```

本次未修改 C++，不运行 MSBuild，不运行默认入口。

## 7. 禁止扩大声明

```text
不声明动态聚合、运动基元候选、因果模板、因果任务化或查询候选已实现。
不声明删除实例状态写入口已实现。
不迁移旧函数体。
不新增旧主信息字段。
不接控制面板 / SQL / D455 / 体素 / 外设。
```
