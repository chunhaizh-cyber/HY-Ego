# 任务系统规范和详细设计建立 Codex 断点清单

日期：2026-07-06
状态：已完成 / 文档已生成 / 待确认 / 未改代码

## 1. 输入入口

计划索引 HR-001 当前下一步：

```text
建立任务系统规范和详细设计；优先覆盖 MI-T2、MI-T3、MI-T4，不得迁移旧任务主信息字段。
```

执行门禁口径：

```text
建立规范和详细设计属于可直接执行文档治理动作。
设计完成后进入代码实施前才需要用户确认。
```

## 2. 本次范围

允许：

```text
建立任务系统规范。
建立任务系统详细设计。
参照旧项目任务规范、任务主信息类和任务类说明提取可迁移口径。
更新计划索引、项目记忆和相关结构规范状态。
```

禁止：

```text
修改代码。
迁移旧任务主信息字段。
恢复旧步骤 / 结果任务树主线。
声明任务系统完成。
```

## 3. 修订内容

```text
新增 `规范/任务系统规范.md`。
新增 `规范/详细设计/任务系统详细设计.md`。
明确 MI-T2 由任务节点、任务角色状态和任务关系角色承载，不迁移为主信息字段。
明确 MI-T3 由任务生命周期实例状态发生时间戳承载，事件日志只做人读观察。
明确 MI-T4 由需求-任务关系、任务虚拟存在、任务场景、任务目标状态和实际结果状态关系承载。
更新计划索引下一步为确认任务系统规范和详细设计。
```

## 4. 验证方式

```powershell
rg -n "任务系统规范|任务系统详细设计|MI-T2|MI-T3|MI-T4|任务虚拟存在|实际结果状态|执行确认门禁" 规范 计划 项目记忆 实施记录
git diff --check
```

本切片不改代码，不运行构建和默认入口。

## 5. 输出变化

```text
任务系统规范和详细设计已建立。
方案进入待确认状态。
确认前不得修改 `任务服务.h` 或迁移旧任务主信息字段。
```

## 6. 禁止扩大声明

```text
本次只能声明任务系统规范和详细设计已建立。
不能声明任务系统规范已确认。
不能声明任务系统代码已实现。
不能声明任务状态机、任务筹办或任务执行完成。
不能声明旧任务字段已迁移。
```
