# CONCEPT-NAMING-S2 抽象状态读取合同漂移修订 Codex 断点清单

日期：2026-07-12

治理编号：JY-280

状态：设计修订完成 / #212 草案已由执行窗口回退 / 未改 C++ / 未构建 / 未运行程序

## 1. 漂移事实

执行窗口按旧计划调用 `状态服务::读取状态材料(共享抽象状态)`，实际 `状态服务.h:149` 对非实例状态返回空，导致 `初始化两个根需求` 返回空并使 SELF-INIT / SELF-GOV 链失败。草案已完整回退，回退后 Debug x64 0/0、完整自检退出 0、工作树干净。

## 2. 正式裁决

```text
创建抽象状态(请求值)
-> 读取状态值(状态) == 请求值
-> !状态是否实例状态(状态)
-> 成功返回共享抽象状态句柄
```

`读取状态材料` 只服务实例状态，不用于抽象状态复核。本切片不新增状态服务入口，不修改 `状态服务.h`，也不把抽象状态伪装为实例状态。

## 3. 三件套一致性

流程图：`流程图/20260711_概念命名需求协议目标创建绑定结算流程图_v0.1.md/.html`。

详细设计：`规范/详细设计/概念命名需求协议目标创建绑定结算详细设计.md`。

计划：`计划/20260711_CONCEPT-NAMING-S2_信息目标状态与目标概念关系基线代码实施切片_v0.1.md`。

三者均固定“读取状态值 + 非实例状态判定”，范围仍只覆盖共享目标状态、关系 13、计数分流和独立自检 510。

## 4. 执行前复核

```powershell
rg -n "创建抽象状态|读取状态值|状态是否实例状态|读取状态材料" .\海中鱼巣\领域\状态服务.h .\海中鱼巣\领域\初始化.需求.ixx
rg -n "CONCEPT-NAMING-S2|顺序.*510|自检\.结构事务|顺序.*500" .\海中鱼巣\入口.cpp .\海中鱼巣\核心
```

若三个状态入口签名、初始化返回类型或 510 阶段占用漂移，不改代码并再次退回设计。

## 5. 完成边界

本记录只证明 #212 读取合同已修订，不证明 #212、关系 13、命名需求或命名闭环实现。
