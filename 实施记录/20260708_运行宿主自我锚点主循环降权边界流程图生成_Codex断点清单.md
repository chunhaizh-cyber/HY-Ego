# 运行宿主 / 自我锚点 / 主循环降权边界流程图生成 Codex 断点清单

日期：2026-07-08

## 1. 本次输入入口

```text
用户指令：继续
当前窗口口径：设计和确认窗口
流程图迁移顺序第 1 项：运行宿主 / 自我锚点 / 主循环降权边界流程
```

## 2. 核心处理

```text
读取流程图技能规则、计划索引、项目规则总纲、规则迁移清单、流程图迁移模板 v0.2、总入口边界流程图和项目记忆当前状态。
按第 1 项生成正式流程图 Markdown / HTML 成对文件。
把旧运行宿主、自我线程、Tick、控制台命令和面板刷新全部降权为材料、请求、调度说明或人读观察。
把入口参数检测、非法数据来源追溯、动作来源拒绝、机器事实写入方归属、服务矩阵匹配、服务逻辑包确认和实施包确认入队纳入同一边界。
```

## 3. 结构承载

```text
流程图/20260708_运行宿主自我锚点主循环降权边界流程图_v0.1.md
流程图/20260708_运行宿主自我锚点主循环降权边界流程图_v0.1.html
项目记忆/当前状态.md
```

## 4. 输出变化

```text
新增第 1 项正式流程图文件。
新增本断点清单。
同步项目记忆中的策略调整状态。
未修改 C++ 代码。
未生成计划。
未登记 Codex 任务队列。
未迁移旧自我线程、旧 Tick、旧主循环、旧函数体、旧字段、控制面板、数据库、SQL、D455、体素或外设。
```

## 5. 调用路径

```text
应用逻辑流程图迁移顺序信息数据
-> 应用逻辑流程图草稿提取信息数据
-> 应用逻辑流程图迁移模板 v0.2
-> 总入口边界 / 权力链流程图 v0.1
-> 运行宿主 / 自我锚点 / 主循环降权边界流程图 v0.1
```

## 6. 验证方式

```text
确认 Markdown 和 HTML 文件存在。
确认 Markdown 包含 Mermaid fenced block。
确认 HTML 包含 Mermaid CDN import。
确认 Markdown 与 HTML 使用同一份 Mermaid 图文本。
运行 git diff --check。
运行 tools/check_specs.py。
```

## 7. 完成声明边界

```text
可以声明：第 1 项运行宿主 / 自我锚点 / 主循环降权边界正式流程图已生成。
不能声明：流程图迁移全部完成。
不能声明：批次级代码实施包已生成。
不能声明：代码已实现、已构建或已运行。
不能声明：旧项目能力已迁移。
不能声明：旧自我线程、真实线程调度、工作队列、std::async 或并发压力验收已完成。
不能声明：自我循环、自我苏醒或初步成熟已完成。
```
