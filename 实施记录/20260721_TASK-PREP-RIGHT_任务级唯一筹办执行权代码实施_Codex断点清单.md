# TASK-PREP-RIGHT 任务级唯一筹办执行权代码实施 Codex 断点清单

日期：2026-07-21
任务：#328 / DQ-220 / TASK-PREP-RIGHT
计划：`计划/20260721_TASK-PREP-RIGHT_任务级唯一筹办执行权代码实施切片_v0.1.md`
状态：接口漂移待设计修订

## 1. 执行身份

```text
任务 worktree：C:\Users\zhchh\.codex\worktrees\328-task-prep-right-r1\海中鱼巢
任务分支：codex/328-task-prep-right-r1
冻结基线 / 接管 HEAD：80da244e10e1071f4f82561e249905599ac210f0
中央派发：E302 / JY-558
派发时中央：main == origin/main == 32340118865c6d3739e14932b049a32cc68ba75b
登记执行角色：/root/pb_spec_code_01_task_prep
```

本窗口只在登记的 WT-328-R1 中执行 #328。未修改中央计划、计划索引、任务队列、工作树登记、窗口交互、规范、流程图或详细设计。

## 2. S0 已验证事实

接管后按计划完成身份、范围和调用点复核：

1. 实际路径、分支、冻结基线和登记身份一致，HEAD 非 detached，接管时 worktree / index clean。
2. 分支无 upstream；S0 时远端同名分支不存在。
3. 计划第 6 节列出的十五份允许代码文件全部存在。
4. #328 与并行任务 #324 的允许文件所有权交集为 0；工程、filters 和入口继续由 #324 独占。
5. 已读取现行规范、详细设计、流程图、函数结构清单与知识图谱，五件套范围一致。
6. `python .\tools\check_specs.py --strict` 通过，结果为 85 / 85。

上述事实只证明 S0 和执行身份成立，不证明接口清单覆盖全部编译调用点，也不证明任务筹办执行权代码已经实现。

## 3. 首次实施尝试与 Debug Rebuild 结果

本轮曾在计划允许范围内开始建立任务筹办执行权值式结构、原子取得 / 复核、召回 / 选择 / 冻结同权传递。首次运行下列计划内验证：

```powershell
msbuild .\海中鱼巣.vcxproj /t:Rebuild /p:Configuration=Debug /p:Platform=x64 /m:1
```

结果：失败，0 warning，2 errors。精确错误为：

```text
海中鱼巣/自检.运行期组合分层.ixx(222,29): error C2440
海中鱼巣/自检.运行期组合分层.ixx(222,36): error C2078
```

失败调用点位于该文件第 218—223 行，当前直接聚合构造：

```cpp
召回并提交唯一选择请求 选择请求{
    {
        句柄有效(需求任务结果.任务.材料.身份.节点)
            ? 需求任务结果.任务.材料.身份.节点 : 新假句柄(),
        新假句柄(), 1, 1},
    新主键(), 新主键()};
```

新增“先取得任务筹办执行权”的正式请求合同后，该聚合构造必须同步新签名；否则现有调用方无法编译。

## 4. 计划外接口漂移裁决

精确计划外文件为：

```text
海中鱼巣/自检.运行期组合分层.ixx
```

该文件不在计划第 6 节允许修改清单内。计划又明确规定：全引用扫描发现其它既有编译调用点需要纯签名同步时，必须先由设计窗口把精确文件加入允许清单并重新发布计划，执行窗口不得自行扩表。

因此，本次结果不是待修代码验证失败，而是允许文件清单未覆盖实际编译调用点，状态固定为：

```text
接口漂移待设计修订
```

执行窗口不得越权修改该自检文件，也不得继续 S1—S7、TPR-A01—A12、Release Rebuild 或完整自检。

## 5. 安全撤回结果

首次 Debug Rebuild 后，已完整撤回七份计划内生产代码草案，并恢复编辑过程中产生的行尾机械变化。撤回后复核：

```text
C++ 差异：0
工程 / filters / 入口差异：0
中央治理材料差异：0
剩余任务差异：仅本专属断点记录
```

未暂存、提交或推送任何未验证代码；未修改计划外调用点；未把失败构建解释为能力实现证据。

## 6. 设计窗口所需动作

1. 把 `海中鱼巣/自检.运行期组合分层.ixx` 精确加入 #328 允许文件清单，只授权同步任务筹办执行权请求签名和同一权值传递。
2. 重新执行全引用扫描，确认是否还存在其它不在允许清单中的聚合构造、直接召回、选择或冻结调用点。
3. 依据扫描结果修订计划、允许 / 禁止矩阵和验证矩阵，发布新的计划版本并重新登记派发。
4. 修订发布前，#328 保持 `接口漂移待设计修订`；不得以本纯断点提交进入实现集成或解除 #327 的前置依赖。

## 7. 本轮输出与声明边界

| 项目 | 结果 |
| --- | --- |
| 输入入口 | #328 / DQ-220 v0.1、E302 / JY-558 正式派发、WT-328-R1 冻结基线 |
| 核心处理 | S0 复核；计划内代码尝试；首次 Debug Rebuild；计划外调用点定位；代码草案完整撤回 |
| 结构承载 | 仅本执行专属断点文档，不形成任何新的机器结构 |
| 输出变化 | 新增一份接口漂移断点；C++、工程和中央治理材料零差异 |
| 调用路径 | `召回并提交唯一选择请求` 新合同 -> `自检.运行期组合分层.ixx` 旧聚合构造 -> 编译失败 |
| 验证方式 | Git 差异核对、`git diff --check`、strict 规范检查、staged 差异检查 |

本轮最多声明：#328 的首次计划内实施尝试识别出一个必须由设计窗口纳入范围的编译调用点，未验证代码已安全撤回，纯接口漂移断点已形成。

不得声明任务级唯一筹办执行权基础、召回前门禁、同权选择 / 冻结、同任务排他、不同任务并发、TPR-A01—A12 或 #328 分支实现已经完成。
