# 应用逻辑流程图迁移模板 v0.2 生成 Codex 断点清单

日期：2026-07-08

## 1. 输入入口

```text
用户提供的《应用逻辑流程图迁移模板 v0.1》只读分析材料。
用户补充的流程图优化口径：入口参数检测用于发现非法数据来源，不是在函数内部兜底。
```

## 2. 核心处理

```text
基于 v0.1 模板生成 v0.2 模板。
保留旧函数事实不作为迁移单位、服务调用编排为主轴的路线。
新增证据包、服务逻辑包确认闸门、矩阵缺口路径、结构写入计划、入口参数检测与非法数据来源追溯、实施包生成前检查、批次级实施包候选生成条件和禁止宣称。
```

## 3. 结构承载

```text
流程图/20260708_应用逻辑流程图迁移模板_v0.2.md
流程图/20260708_应用逻辑流程图迁移模板_v0.2.html
项目记忆/当前状态.md
项目记忆/决策记录.md
```

## 4. 输出变化

```text
新增 v0.2 流程图模板文件。
新增匹配 HTML 渲染文件。
同步项目记忆，记录 v0.2 模板已生成且仅为文档治理，不是代码实施许可。
```

## 5. 调用路径

```text
附件只读分析
-> v0.1 模板
-> v0.2 模板 Markdown / HTML
-> 项目记忆同步
```

## 6. 验证方式

```text
确认 Markdown 文件存在。
确认 HTML 文件存在。
确认 Markdown 包含 mermaid fenced block。
确认 HTML 包含 Mermaid CDN import。
git diff --check
python .\tools\check_specs.py
```

## 7. 完成声明边界

本次只能证明：

```text
应用逻辑流程图迁移模板 v0.2 已生成。
模板已显式加入确认闸门、矩阵缺口、结构写入计划和非法数据来源追溯口径。
```

不能证明：

```text
具体应用逻辑流程图已迁移。
批次级代码实施包已生成。
C++ 代码已修改。
旧项目能力已迁移。
服务调用编排已落地。
动态服务、因果服务、SQL、控制面板、D455、体素或外设已接入。
```
