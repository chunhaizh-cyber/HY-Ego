# PERSIST-S1-A1 四仓库全状态与精确高水位导出代码实施 Codex 断点清单

日期：2026-07-19

计划：#305 / DQ-197 / PERSIST-S1-A1 v0.4

任务工作树：WT-305-R1

冻结父提交：`1d95aa5975057965fcfa179c56521ad69e5b5b6a`

任务分支：`codex/305-a1-r1`

状态：执行完成待设计同步 / 分支完成待集成

## 1. 执行前复核

1. E188 / JY-443 已在 `origin/main@cc703366c7a835049d5fc92d479a9dcaed4d5b7c` 正式登记本 worktree、任务分支、冻结父提交、十四文件所有权和已派发状态；任务代码分支未合并中央治理。
2. 主信息仓库的真实权威状态由 `仓库编号_`、`下个主信息编号_` 和 `主信息表_` 承载；节点仓库另有精确 `下个节点编号_` 与 `下个创建序号_`。
3. 关系仓库的真实权威状态由 `仓库编号_`、`下个关系编号_` 和包含有效、失效、删除记录的 `关系表_` 承载。
4. 索引仓库只有当前 `主键索引_`、反向 `节点主键组_` 和 `永久保留主键组_`；没有独立自增高水位，索引导出不得发明计数器。
5. #304 的索引所有者、命名域、版本、探测序号和保留策略已经形成正式值式记录；本切片逐字段消费该实际合同。
6. 四仓库均可在同域共享事务许可下复制本仓库状态；导出只读路径无需新增无令牌入口或向领域服务暴露原始令牌。

## 2. 实施结果

1. 新增 `核心/权威冻结材料.数据.h`，统一声明四仓库稳定快照记录、仓库级导出材料、具名导出状态和聚合 `权威冻结材料`。全部字段为值、句柄或值式容器，不含指针、锁和对象地址。
2. 主信息仓库导出全部有效、失效、删除记录及材料值、版本、仓库编号和精确下一主信息编号；节点仓库导出全部状态、主信息身份、仓库编号、精确下一节点编号和创建序号。
3. 关系仓库导出全部状态、完整源目标端点、关系类型、角色、版本、仓库编号和精确下一关系编号；失效、删除关系不因端点当前状态被截断。
4. 索引仓库导出全部当前绑定、反向组和永久保留记录，并逐字段保留 #304 所有者声明、命名域、格式版本、探测规则、探测序号和键保留策略；仓库身份沿正式节点仓库编号锚定，不新增索引计数器。
5. 四个导出入口只接受同域有效共享事务令牌，在本仓库共享锁内复制并校验真实私有状态。入口拒绝、资源失败和内部不一致均不返回部分材料；正常导出不排序、不修复、不写仓库。
6. 新增 Debug 专项真模块 `核心/自检.仓库权威导出.ixx`，覆盖空仓、混合状态、墓碑、历史关系端点、当前与永久索引、精确高水位、后继分配不复用以及反向索引不一致的具名收口。
7. 入口只在既有 Debug 宏内增加专项模块 import、一个结果槽、阶段 980 一次登记、一个通过值和最终具名汇总；`自检.运行器.ixx` 保持零差异，Release 不含专项登记或文本。

## 3. 验收结果

| 验收 | 结果 | 证据 |
| --- | --- | --- |
| A01—A05 | 通过 | #304 实际合同、统一稳定 DTO、主信息 / 节点 / 关系全状态和精确高水位专项通过 |
| A06—A10 | 通过 | 索引当前 / 保留 / 所有者合同、墓碑与历史端点、下一可分配值、不复用身份、同域令牌门禁专项通过 |
| A11—A14 | 通过 | 只复制不修复、内部不一致无部分材料、CODE-FILE-01、980 仅 Debug 通过 |
| A15—A19 | 通过 | 四仓库回归、两配置、20 / 20、中央治理与运行器零修改、入口最小 Debug 接线通过 |

专项 Debug 输出：`PERSIST-S1-A1-A01`—`PERSIST-S1-A1-A19` 每轮均为 19 / 19，完整自检退出 0。

## 4. 验证证据

```text
Debug x64 Rebuild: exit 0, 0 warning, 0 error
Debug complete self-test: exit 0, PERSIST-S1-A1 A01-A19 = 19 / 19
Release x64 Rebuild: exit 0, 0 warning, 0 error
Release complete self-test: exit 0
Release PERSIST-S1-A1 / 980 专项文本匹配: 0
Debug strict continuous complete self-test: 20 / 20, each exit 0, each A01-A19 = 19 / 19, elapsed 407.4 s
python tools/check_specs.py --strict: pass
git diff --check: pass
```

首次 Debug Rebuild 仅因新自检模块把匿名辅助命名空间放在 `export namespace` 内，MSVC 报 C2291 / C2294。该模块骨架问题在允许的新自检文件内修正为非导出辅助命名空间后，Debug 与 Release 均重新 Rebuild 通过；未改变生产接口、验收语义或允许范围。

共享完整自检目录由设计窗口明确授予 WT-305 独占，WT-310 在运行期间保持暂停。Debug、Release 和连续二十轮均串行运行，每轮自然结束后保留 2 秒间隔；验证结束后已立即明确释放 `D:\海中鱼巣\日志\事件段\自检`。

## 5. 输入、处理与输出

输入入口：四仓库各自的 `导出权威状态(结构事务令牌)`，只接受同域有效共享许可。

核心处理：令牌准入后，在各仓库本地共享锁内复制全部权威记录、身份与真实下一编号；索引同时交叉核对正向、反向和永久保留合同。发现资源失败或内部不一致时返回具名状态和空材料。

结构承载：`权威冻结材料.数据.h` 的稳定值式 DTO 承载四仓库快照；原仓库仍是当前机器事实唯一所有者，DTO 不取得仓库、锁或运行期对象所有权。

输出变化：成功时返回完整、不可部分发布的值式材料；导出过程不修改仓库、不排序修复、不复活墓碑。失败时不产生可消费的部分材料。

调用路径：Debug 阶段 980 自检取得同域共享许可后分别调用四仓库导出；生产入口、运行期组合器、SQL、控制面板、快照与恢复路径均未接线。

验证方式：两配置 Rebuild、两配置完整自检、Debug A01—A19、Debug 连续 20 / 20、strict、diff、精确文件范围、运行器零差异和 Release 隔离共同裁决。

## 6. 精确范围

本轮只修改计划第 3 节十四文件：一个新数据头、四仓库头 / 实现、一个新专项自检模块、入口 Debug 最小接线、工程 / filters 和本记录。

以下范围保持零修改：中央计划与项目记忆、`自检.运行器.ixx`、生产入口分支、#310、#301、运行期业务组合器 / 装配 / 兼容请求自检、SQL、控制面板、快照、恢复和 #214。

## 7. 完成边界

本轮只证明四仓库全部权威状态与真实精确高水位可以在同域共享许可下稳定导出，并通过当前回归；不证明九段冻结、HYSNAP01 编码、SQL 持久化、恢复重建、运行期生产接线、#310 / #301 或旧能力迁移已经完成。任务分支仍须由独立集成角色按固定顺序验证并进入 main 后，计划才能正式完成。
