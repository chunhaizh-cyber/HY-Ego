# FS-02 二次特征与因果入口只读扫描记录

日期：2026-07-06
状态：FS-02 第五批已登记 / 只读扫描 / 未改代码 / 未迁移旧函数
依据清单：`实施记录/20260706_逐函数只读扫描清单.md`
前序记录：

```text
实施记录/20260706_FS02_语素入口与基础信息桥接只读扫描记录.md
实施记录/20260706_FS02_基础信息模板入口只读扫描记录.md
实施记录/20260706_FS02_存在与场景入口只读扫描记录.md
实施记录/20260706_FS02_状态与动态入口只读扫描记录.md
```

## 1. 本轮定位

本轮继续 FS-02，登记旧项目 `二次特征类` 和 `因果类` 中与二次特征身份、状态迁移来源、概念刻度链、生成语义、来源绑定、值材料写入、因果身份、因果证据、被动 / 主动因果治理、因果模板生成、证据动态样本和因果查询有关的函数族。

本轮只做只读事实登记和初步分流，不复制旧二次特征或因果函数体，不新增旧 `二次特征主信息类`、`因果主信息类`、`因果模板主信息类` 字段。旧因果模板聚合、投影查询、自然语言显示、证据计数和稳定度治理只登记为后续因果 / 方法 / 需求树增强专项线索；当前项目第一轮因果口径仍是轻量因果引用，单个因果引用不得解释为稳定因果结论。

## 2. 只读扫描范围

```text
D:\鱼巢\二次特征类.h
D:\鱼巢\二次特征类.cpp
D:\鱼巢\因果类.h
D:\鱼巢\因果类.cpp
```

扫描证据来自：

```text
二次特征类.cpp:1-5：旧二次特征文件头部规则，声明概念链不替代状态、动态或因果证据链。
二次特征类.h:29-35、二次特征类.cpp:322-348：二次特征服务绑定、主信息读取和按主键解析声明与实现。
二次特征类.h:38-91、二次特征类.cpp:352-590：状态迁移来源、可融合标量区间、方向签名、方向编码、状态引用统计、差值 / 标量 / 离散值读取和语素概念模板主信息声明与实现。
二次特征类.h:93-123、二次特征类.cpp:636-1009：二次特征模板匹配、创建二次特征、按度量创建、签名复用、概念树根、度量链根、概念小区间登记和反查。
二次特征类.h:125-150、二次特征类.cpp:1013-1233：删除 / 枚举 / 子二次特征读取、绑定场景、生成语义和来源绑定。
二次特征类.h:151-180、二次特征类.cpp:1236-1645：主体 / 客体、左右对象、差值、标量、观察关系、离散值、度量签名和概念模板刷新。
二次特征类.cpp:1840：默认概念刻度内部辅助。
因果类.h:12-15：旧因果文件头部规则，被动因果和状态变化线索不得进入普通方法候选。
因果类.cpp:26-31：旧因果实现头部规则，只接收已落账状态迁移、动作动态和证据动态样本。
因果类.h:80-97、因果类.cpp:1411-1477：因果服务绑定、因果主信息 / 模板主信息读取、因果节点判断、解析和按主键解析。
因果类.h:99-149、因果类.cpp:1479-1879：因果证据状态、证据计数、动作结果验收、主果方向命中、条件模板、条件目标状态和动作来源读取。
因果类.h:151-243、因果类.cpp:1883-2538：被动 / 主动因果来源完整性、上游来源、线索边界、根闭环可追溯、主动验证状态和任务化判定。
因果类.h:245-271、因果类.cpp:2541-2699、3107-3446、3483-3489：创建因果模板、被动触发初始模板、生成因果信息和追加证据动态样本。
因果类.h:273-350、因果类.cpp:2709-3099、3456-3600：因果枚举、条件 / 结果 / 动作查询、因果链、任务投影、自检投影、模板匹配和自然语言显示。
```

## 3. 逐函数登记

### OF-094

```text
旧函数编号：OF-094
扫描批次：FS-02
旧项目路径：D:\鱼巢\二次特征类.cpp:322
旧函数名：二次特征类::绑定基础信息 / 取二次特征主信息 / 按主键解析二次特征节点
旧函数签名：void 绑定基础信息(...); 二次特征主信息类* 取二次特征主信息(...); 二次特征节点类* 按主键解析二次特征节点(...)
当前调用方：旧二次特征创建、概念刻度链、模板匹配、因果主果比较和场景二次特征索引。
触发路径：旧基础信息树按主信息类型或主键定位二次特征节点。
输入：基础信息类指针、二次特征节点、主键字符串。
输出：二次特征主信息指针、二次特征节点指针或空。
读结构：旧基础信息树、旧二次特征主信息、旧主键索引。
写结构：`绑定基础信息` 只写运行期服务指针；其它函数只读。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：按稳定身份定位二次特征候选并复核主信息类型。
删除行为：不得用旧运行期裸指针或旧主信息类型直接裁决新项目二次特征有效。
新增约束：新项目必须通过二次特征服务复核节点类型、句柄有效性和组成关系边界。
目标新结构：节点类型::二次特征、节点句柄、索引主键、二次特征服务只读入口。
目标领域服务入口：二次特征服务 `创建二次特征`、`创建组合二次特征`、后续 `读取组成项` / `二次特征是否有效`。
对应规范 / 详细设计：二次特征服务详细设计、索引仓库详细设计、领域服务授权写入规范。
入口拒绝条件：基础服务缺失、主键为空、句柄无效、节点类型不是二次特征、已删除节点。
验证方式：索引命中后读回节点类型和组成关系；错误类型返回空且结构数量不变。
初步判定：改写为领域服务入口
待用户确认项：是否补专用 `二次特征是否有效` 和 `读取组成项` 只读入口。
扫描证据：`二次特征类.cpp:322` 至 `二次特征类.cpp:348`。
```

### OF-095

```text
旧函数编号：OF-095
扫描批次：FS-02 / FS-07
旧项目路径：D:\鱼巢\二次特征类.cpp:352
旧函数名：读取状态迁移来源 / 是状态迁移二次特征 / 读取可融合标量区间 / 融合同向状态迁移可融合标量区间 / 读取方向签名材料 / 读取方向比较结果编码 / 统计状态引用数量 / 读取差值 / 读取标量值 / 读取离散值
旧函数签名：状态迁移来源、方向签名、方向编码、状态引用统计和值材料读取函数族。
当前调用方：旧因果生成、状态迁移比较、概念刻度命中、动态影响统计和模板匹配。
触发路径：从旧二次特征主信息读取来源起始状态、结果状态、来源动态、方向材料、差值、标量值和离散值。
输入：二次特征节点或二次特征主信息、状态节点。
输出：来源状态 / 动态、区间、方向签名、编码、引用计数、差值、标量值、离散编码和语义键。
读结构：旧二次特征主信息中的来源引用、方向材料、差值、标量、离散值、可融合区间。
写结构：`融合同向状态迁移可融合标量区间` 会写旧二次特征主信息区间；其它函数只读。
是否写机器事实：部分写旧二次特征主信息派生材料。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：状态迁移二次特征应可追溯到来源起始状态、结果状态和来源动态；方向、差值和离散材料可作为后续复合特征候选。
删除行为：不得把旧主信息里的方向编码、差值、标量、离散语义键或融合区间直接作为新项目权威结论。
新增约束：二次特征只表达复合判断前置结构；判断结论应由状态、特征值、动态或因果引用承载。
目标新结构：二次特征组成关系、状态节点、动态节点、后续二次特征候选评分或缓存。
目标领域服务入口：二次特征服务后续 `读取组成项` / `创建组合二次特征`；动态服务和因果服务只读证据入口。
对应规范 / 详细设计：二次特征服务详细设计、动态服务详细设计、领域服务最小入口详细设计。
入口拒绝条件：二次特征无效、来源状态无效、来源动态无效、组成项缺失、方向语义未确认、试图用派生值替代状态或动态证据。
验证方式：有效二次特征可回查组成状态和动态；缺来源时只返回缺证据，不写结论。
初步判定：拆分
待用户确认项：方向签名、融合区间和离散语义键是否进入后续二次特征增强专项。
扫描证据：`二次特征类.cpp:352` 至 `二次特征类.cpp:590`。
```

### OF-096

```text
旧函数编号：OF-096
扫描批次：FS-02 / FS-03
旧项目路径：D:\鱼巢\二次特征类.cpp:566
旧函数名：创建语素入口概念模板主信息_直接 / 主信息引用概念入口_直接 / 二次特征模板匹配
旧函数签名：二次特征概念模板主信息创建和模板匹配函数族。
当前调用方：旧语素概念入口、概念刻度链、因果模板匹配和自然语言命名。
触发路径：把语素入口引用写入旧二次特征模板主信息，或按模板主信息匹配二次特征。
输入：语素入口节点、基础信息主信息、二次特征节点、模板二次特征。
输出：二次特征主信息、局部 bool。
读结构：旧语素入口、旧二次特征主信息、旧模板字段。
写结构：创建旧模板主信息对象；不直接写仓库。
是否写机器事实：否；输出可能被后续创建函数写入事实。
是否依赖日志：否。
是否依赖显示：概念名称可服务显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：语素概念入口可作为二次特征命名和模板线索；模板匹配必须复判结构材料。
删除行为：不得让语素入口文本或显示名称直接裁决二次特征事实。
新增约束：语素服务只绑定已存在信息节点；二次特征模板应通过关系、组成项和候选复判表达。
目标新结构：语素对应信息关系、语素概念追溯关系、二次特征组成关系、索引候选。
目标领域服务入口：语素服务多对应 / 多路追溯入口、二次特征服务后续模板读取入口。
对应规范 / 详细设计：语素系统详细设计、二次特征服务详细设计、索引仓库详细设计。
入口拒绝条件：语素入口无效、二次特征无效、模板组成项缺失、只凭文本匹配。
验证方式：模板候选必须能读回二次特征结构；文本或显示标题缺失不影响机器事实。
初步判定：拆分
待用户确认项：二次特征模板匹配是否纳入后续方法候选和因果模板专项。
扫描证据：`二次特征类.cpp:566` 至 `二次特征类.cpp:646`。
```

### OF-097

```text
旧函数编号：OF-097
扫描批次：FS-02 / FS-03 / FS-08
旧项目路径：D:\鱼巢\二次特征类.cpp:646
旧函数名：创建二次特征 / 创建二次特征_按度量 / 获取或创建子二次特征_按签名 / 获取或创建概念树根 / 获取或创建度量链根 / 注册概念小区间 / 按差值命中概念节点 / 用概念名称反查节点 / 私有_确保默认概念刻度
旧函数签名：二次特征创建、按签名复用、概念树根、度量链根和概念刻度函数族。
当前调用方：旧场景二次特征、状态差值概念化、因果模板、需求 / 方法候选和显示命名。
触发路径：按父节点、主信息、度量签名或差值创建 / 命中二次特征节点，并维护旧概念刻度链。
输入：父基础信息节点、二次特征主信息、度量签名、区间、语素概念名称、差值。
输出：二次特征节点或空。
读结构：旧基础信息树、旧二次特征主信息、签名缓存、概念刻度链。
写结构：旧二次特征节点、旧主信息、普通父子链、签名缓存、概念区间主信息。
是否写机器事实：是。
是否依赖日志：是，内部可能写运行日志和阶段耗时。
是否依赖显示：概念名称可服务显示。
是否依赖 SQL：否。
是否依赖线程：否；内部存在静态计数和互斥辅助但不表达动作来源。
是否依赖外设 / D455 / 体素：否。
保留行为：创建二次特征身份、按结构签名复用、概念区间作为候选分类线索。
删除行为：不得迁移旧普通父子概念树、签名缓存、默认刻度和概念名称反查为权威事实。
新增约束：第一轮二次特征创建只建立节点和组成关系；概念刻度、签名缓存和命中次数属于后续非权威缓存 / 二次特征增强。
目标新结构：二次特征节点、组成关系、语素对应信息关系、后续缓存命名空间。
目标领域服务入口：二次特征服务 `创建二次特征`、`创建组合二次特征`、`追加组成项`；后续概念刻度专项。
对应规范 / 详细设计：二次特征服务详细设计、非权威缓存与统计详细设计、领域服务授权写入规范。
入口拒绝条件：父节点无效、主信息缺失、组成项为空、区间非法、概念名称无效、签名命名空间未确认。
验证方式：创建后节点类型为二次特征，组成关系可读；缓存清空不影响二次特征事实。
初步判定：拆分
待用户确认项：概念树根、度量链根和默认概念刻度是否进入特征 / 二次特征系统增强专项。
扫描证据：`二次特征类.cpp:646` 至 `二次特征类.cpp:1009`、`二次特征类.cpp:1840`。
```

### OF-098

```text
旧函数编号：OF-098
扫描批次：FS-02
旧项目路径：D:\鱼巢\二次特征类.cpp:1013
旧函数名：删除二次特征 / 枚举全部二次特征 / 获取子二次特征
旧函数签名：bool 删除二次特征(...); std::vector<二次特征节点类*> 枚举全部二次特征(); std::vector<二次特征节点类*> 获取子二次特征(...)
当前调用方：旧二次特征治理、场景二次特征索引、概念刻度维护和诊断路径。
触发路径：删除二次特征或从旧基础信息树 / 子链枚举二次特征。
输入：二次特征节点、父基础信息节点。
输出：局部 bool 或二次特征节点集合。
读结构：旧基础信息树、旧普通父子链、旧二次特征主信息。
写结构：删除路径写节点生命周期和旧树链。
是否写机器事实：删除是；枚举和读取为否。
是否依赖日志：否。
是否依赖显示：枚举可服务显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：二次特征生命周期管理和按父结构读取子二次特征集合。
删除行为：不得用全局枚举替代领域准入；不得迁移旧父子链作为唯一权威组织方式。
新增约束：删除走节点仓库生命周期；读取必须复核节点类型和关系有效。
目标新结构：二次特征节点生命周期、关系仓库组成关系或普通父子关系。
目标领域服务入口：二次特征服务后续删除 / 读取增强。
对应规范 / 详细设计：二次特征服务详细设计、节点仓库详细设计、关系仓库详细设计。
入口拒绝条件：二次特征无效、父节点无效、删除后仍可被读成有效二次特征。
验证方式：删除后旧句柄失效；读取只返回有效二次特征节点。
初步判定：拆分
待用户确认项：二次特征删除入口是否暂缓到生命周期治理专项。
扫描证据：`二次特征类.cpp:1013` 至 `二次特征类.cpp:1052`。
```

### OF-099

```text
旧函数编号：OF-099
扫描批次：FS-02 / FS-07
旧项目路径：D:\鱼巢\二次特征类.cpp:1054
旧函数名：绑定场景 / 设置生成语义 / 绑定来源 / 设置主体客体 / 设置左右对象
旧函数签名：二次特征场景归属、生成语义、来源状态 / 动态和主体客体写入函数族。
当前调用方：旧观察关系二次特征、状态迁移二次特征、因果模板生成和场景索引。
触发路径：把二次特征归入场景索引，并把来源状态、来源动态、时间段、主体客体等写入旧主信息。
输入：二次特征节点、场景、状态、动态、主体、客体、左右对象、来源类型、时间段和粒度枚举。
输出：局部 bool。
读结构：旧二次特征主信息、旧场景主信息、旧状态 / 动态节点。
写结构：旧二次特征主信息、旧场景二次特征索引。
是否写机器事实：是。
是否依赖日志：否。
是否依赖显示：生成语义可服务命名和显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：二次特征可追溯来源状态、来源动态、主体客体和适用场景。
删除行为：不得把旧场景二次特征索引字段、枚举语义或时间段缓存直接迁移为权威字段。
新增约束：来源关系必须由关系仓库表达；场景归属与实例状态 / 动态边界必须清楚。
目标新结构：二次特征组成关系、场景引用关系、状态 / 动态引用关系、主体 / 客体引用关系。
目标领域服务入口：二次特征服务后续 `追加组成项` / `绑定来源` 增强；场景服务只读收纳关系。
对应规范 / 详细设计：二次特征服务详细设计、场景服务详细设计、动态服务详细设计、关系仓库详细设计。
入口拒绝条件：二次特征无效、场景无效、来源状态无效、来源动态无效、主体客体无效、来源类型未确认。
验证方式：来源关系可读且不依赖旧主信息裸字段；缺来源时不返回有效状态迁移二次特征。
初步判定：拆分
待用户确认项：二次特征来源绑定关系角色是否进入后续二次特征增强专项。
扫描证据：`二次特征类.cpp:1054` 至 `二次特征类.cpp:1258`。
```

### OF-100

```text
旧函数编号：OF-100
扫描批次：FS-02 / FS-03 / FS-07 / FS-08
旧项目路径：D:\鱼巢\二次特征类.cpp:1260
旧函数名：写入差值结果 / 写入标量值 / 写入观察关系二次特征 / 写入离散值 / 刷新度量签名_链键 / 刷新概念模板
旧函数签名：二次特征值材料写入、观察关系写入和派生签名刷新函数族。
当前调用方：旧观察、状态比较、运动基元候选、因果模板和概念刻度链。
触发路径：把差值、标量、满足标记、置信度、观察关系名、离散编码、语义键和签名刷新写入旧二次特征主信息。
输入：二次特征节点、左值、右值、标量值、是否满足、置信度、场景、主体、客体、来源动态、关系名、离散编码和语义键。
输出：局部 bool。
读结构：旧二次特征主信息、旧场景 / 主体 / 客体 / 动态节点。
写结构：旧二次特征主信息、旧场景二次特征索引、内部采样缓存、度量签名和概念模板材料。
是否写机器事实：是。
是否依赖日志：是，内部写入阶段可能记录耗时日志。
是否依赖显示：关系名、语义键和概念模板可服务显示。
是否依赖 SQL：否。
是否依赖线程：否；内部互斥和采样仅为运行期控制。
是否依赖外设 / D455 / 体素：否。
保留行为：观察关系、差值、标量和离散材料可作为二次特征候选输入。
删除行为：不得把旧 `是否满足` bool、置信度、关系名或离散语义键直接作为新项目业务事实。
新增约束：判断结论应由状态、特征值、动态或因果引用承载；缓存 / 签名只作非权威定位和诊断。
目标新结构：状态值材料、动态证据、二次特征组成关系、非权威缓存记录。
目标领域服务入口：二次特征服务后续值材料增强、动态服务证据入口、非权威缓存统计专项。
对应规范 / 详细设计：二次特征服务详细设计、动态服务详细设计、非权威缓存与统计详细设计、错误结果与入口拒绝规范。
入口拒绝条件：二次特征无效、场景无效、来源动态无效、关系名仅作显示却被当事实、试图用 bool 满足标记裁决业务状态。
验证方式：写入拒绝后节点 / 关系 / 状态 / 动态数量不变；有效写入必须能追溯状态或动态证据。
初步判定：拆分
待用户确认项：二次特征值材料是进入特征 / 特征值增强专项，还是先作为非权威观察缓存暂缓。
扫描证据：`二次特征类.cpp:1260` 至 `二次特征类.cpp:1645`。
```

### OF-101

```text
旧函数编号：OF-101
扫描批次：FS-02 / FS-07
旧项目路径：D:\鱼巢\因果类.cpp:1411
旧函数名：因果类::绑定基础信息 / 取因果主信息 / 取模板主信息 / 是因果节点 / 解析因果节点 / 按主键解析因果节点
旧函数签名：因果服务绑定、主信息读取、因果节点判断和主键解析函数族。
当前调用方：旧因果生成、模板查询、证据治理、任务投影和自然语言显示。
触发路径：旧基础信息树按因果主信息、因果模板主信息或主键定位因果候选。
输入：基础信息类指针、因果节点、因果模板节点、基础信息节点、主键字符串。
输出：因果主信息、因果模板主信息、因果节点或空。
读结构：旧基础信息树、旧因果主信息、旧因果模板主信息、旧主键索引。
写结构：`绑定基础信息` 只写运行期服务指针；其它函数只读。
是否写机器事实：否。
是否依赖日志：否。
是否依赖显示：否。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：因果候选必须有稳定节点身份并复核因果主信息或模板主信息。
删除行为：不得迁移旧因果模板节点类型和主信息裸字段作为当前权威因果结构。
新增约束：当前新项目第一轮只确认轻量因果引用；因果模板和稳定因果结论必须后续设计。
目标新结构：节点类型::因果引用、动态准入证据、后续因果候选关系或评分材料。
目标领域服务入口：因果服务 `记录因果引用`、`记录因果引用(来源动态)`；后续因果模板专项。
对应规范 / 详细设计：领域服务最小入口详细设计、动态服务详细设计、领域服务授权写入规范。
入口拒绝条件：基础服务缺失、主键为空、节点无效、节点类型不是因果引用、来源动态缺失。
验证方式：轻量因果引用只以来源动态准入；不持久化来源动态关系；错误输入不新增因果节点。
初步判定：改写为领域服务入口
待用户确认项：旧因果模板节点是否进入后续独立因果系统详细设计。
扫描证据：`因果类.cpp:1411` 至 `因果类.cpp:1477`。
```

### OF-102

```text
旧函数编号：OF-102
扫描批次：FS-02 / FS-07 / FS-08
旧项目路径：D:\鱼巢\因果类.cpp:1479
旧函数名：读取因果证据状态 / 更新因果证据计数 / 验收动作结果因果证据 / 查询主果方向命中因果 / 存在因果条件模板 / 枚举因果条件目标状态 / 查询因果动作来源
旧函数签名：因果证据状态、证据计数、动作结果验收和条件 / 动作来源读取函数族。
当前调用方：旧动作动态验收、方法候选、因果模板治理、需求 / 任务投影。
触发路径：读取或更新旧因果模板里的证据动态样本数、稳定度、条件 / 因 / 果命中计数，并根据动作结果更新模板。
输入：因果节点、因果模板节点、动作动态、预测结果状态、实际结果状态、目标二次特征。
输出：证据状态、计数、命中模板集合、缺失证据集合、条件状态集合、来源方法和动作语义键。
读结构：旧因果主信息、旧因果模板主信息、旧动态、旧状态、旧二次特征。
写结构：更新旧因果模板证据计数和评价时间。
是否写机器事实：是，旧项目将证据计数和稳定度写入因果模板主信息。
是否依赖日志：否。
是否依赖显示：动作语义键可服务显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：因果判断需要来源动态、预测 / 实际结果状态和缺失证据登记；动作来源不能由文本冒充。
删除行为：不得把旧证据计数、稳定度或动作语义文本直接迁移为新项目稳定因果事实。
新增约束：动作动态负责保存具体动作证据；轻量因果引用只做来源动态准入，不替代动作动态或稳定因果结论。
目标新结构：实例动态、目标状态、实际状态、因果引用、后续因果证据评分或非权威统计。
目标领域服务入口：动态服务读取材料、因果服务轻量引用入口、动作入口服务后续读取入口、需求结算记录动态证据入口。
对应规范 / 详细设计：动作入口详细设计、动态服务详细设计、需求结算记录详细设计、非权威缓存与统计详细设计。
入口拒绝条件：动作动态无效、预测结果状态无效、实际结果状态无效、来源动作入口无效、缺动态证据却声明因果。
验证方式：有效动作动态和状态证据可读；缺证据只返回缺失证据，不写稳定因果结论。
初步判定：拆分
待用户确认项：因果证据计数和稳定度是否进入后续因果治理专项或非权威统计专项。
扫描证据：`因果类.cpp:1479` 至 `因果类.cpp:1879`。
```

### OF-103

```text
旧函数编号：OF-103
扫描批次：FS-02 / FS-05 / FS-06 / FS-07
旧项目路径：D:\鱼巢\因果类.cpp:1883
旧函数名：验证被动因果来源动态完整 / 读取被动因果上游来源 / 检查状态变化因果是否只可作为线索 / 检查根闭环因果来源是否可追溯 / 读取被动因果闭环状态 / 验证主动因果来源动态完整 / 读取主动因果动作来源 / 读取主动因果验证状态 / 检查主动因果同源变化证据可回解 / 判定主动因果可任务化
旧函数签名：被动 / 主动因果治理、闭环状态和任务化判定只读函数族。
当前调用方：旧需求树、任务树、方法学习、因果闭环治理和候选任务生成。
触发路径：读取旧因果主信息和证据动态，判断被动因果是否只可作为线索，主动因果是否可任务化。
输入：因果节点。
输出：来源动态数量、完整动态数量、主动 / 被动标记、证据足够标记、可生成原子任务标记、缺口状态值、方法节点、动作语义键和缺失证据集合。
读结构：旧因果主信息、旧证据动态样本、旧方法模板、旧状态迁移二次特征、旧任务 / 需求线索。
写结构：无。
是否写机器事实：否；但输出在旧项目可驱动任务或方法候选。
是否依赖日志：否。
是否依赖显示：缺失证据和动作语义键可服务显示。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：被动因果不得进入普通方法候选；主动因果任务化必须有可回解动态证据、方法来源和状态变化端点。
删除行为：不得把旧 bool 判定直接变成新项目任务、方法或动作入口事实。
新增约束：任务化、方法结构缺口和学习缺口必须进入需求树 / 任务管理 / 方法系统后续增强专项确认。
目标新结构：因果引用、动态证据、任务目标状态、方法动作入口、缺口状态或需求候选。
目标领域服务入口：因果服务后续只读治理入口、任务服务后续候选入口、方法服务后续学习入口。
对应规范 / 详细设计：任务系统详细设计、方法系统详细设计、动作入口详细设计、领域服务授权写入规范。
入口拒绝条件：因果无效、来源动态不完整、方法来源缺失、动作语义只为文本、状态端点不可回解、被动因果被召回为普通方法候选。
验证方式：只读判定不得新增任务 / 方法 / 因果事实；缺证据返回缺失项。
初步判定：排除 / 后续专项
待用户确认项：主动因果任务化规则是否进入“需求树 / 任务管理 / 方法系统后续增强专项”。
扫描证据：`因果类.cpp:1883` 至 `因果类.cpp:2538`。
```

### OF-104

```text
旧函数编号：OF-104
扫描批次：FS-02 / FS-07 / FS-09
旧项目路径：D:\鱼巢\因果类.cpp:2541
旧函数名：创建因果模板 / 确保被动触发初始因果模板 / 生成因果信息 / 追加证据动态样本
旧函数签名：因果模板创建、被动触发模板命中、按动态生成因果信息和追加证据样本函数族。
当前调用方：旧动态记录后因果生成、被动触发线索、动作动态验收和因果模板积累。
触发路径：按状态迁移 / 动作动态 / 二次特征比较创建或命中因果模板，并给模板追加证据动态样本。
输入：父基础信息节点、场景、动态、动作主体、动作语义、动作语义键、条件比较、主果比较、证据动态、层级。
输出：因果模板节点或空。
读结构：旧场景、旧动态、旧二次特征、旧因果模板集合、旧证据动态样本。
写结构：旧因果模板节点、旧因果主信息、旧普通父子链、旧证据动态样本列表。
是否写机器事实：是。
是否依赖日志：是，生成过程记录阶段耗时和缺失证据。
是否依赖显示：动作语义键和自然语言命名可服务显示。
是否依赖 SQL：否。
是否依赖线程：否；证据样本追加使用互斥但不表达动作来源。
是否依赖外设 / D455 / 体素：否。
保留行为：因果信息生成必须基于已落账动态和可回解状态迁移；证据动态样本可作为后续因果聚合材料。
删除行为：不得直接迁移旧因果模板创建算法、旧普通父子挂载、旧动作语义文本或旧证据样本 Vec 字段。
新增约束：当前第一轮只允许轻量因果引用；因果模板生成、样本持久化和事件恢复必须后续设计确认。
目标新结构：因果引用节点、动态证据、后续因果模板节点或事件日志证据样本。
目标领域服务入口：因果服务 `记录因果引用(来源动态)`；后续事件日志持久化恢复专项和因果模板专项。
对应规范 / 详细设计：领域服务最小入口详细设计、动态服务详细设计、事件日志持久化恢复后续专项、错误结果与入口拒绝规范。
入口拒绝条件：动态无效、来源状态端点不可回解、条件比较无效、主果比较无效、动作语义仅为文本、缺证据动态。
验证方式：第一轮只验证来源动态准入和因果引用节点新增；不得验证或宣称模板稳定性。
初步判定：排除 / 后续专项
待用户确认项：因果模板生成是否单独建立因果系统详细设计，不在本轮函数迁移中落地。
扫描证据：`因果类.cpp:2541` 至 `因果类.cpp:2699`、`因果类.cpp:3107` 至 `因果类.cpp:3446`、`因果类.cpp:3483` 至 `因果类.cpp:3489`。
```

### OF-105

```text
旧函数编号：OF-105
扫描批次：FS-02 / FS-04 / FS-05 / FS-07 / FS-10
旧项目路径：D:\鱼巢\因果类.cpp:2709
旧函数名：枚举全部因果 / 枚举因果模板 / 查询条件相关因果 / 查询结果相关因果 / 查询动作相关因果 / 查询因果链 / 查询叶子任务目标投影层级 / 查询自检原子目标投影 / 二次特征匹配模板 / 因果模板匹配二次特征 / 查找匹配因果模板 / 生成抽象因果自然语言
旧函数签名：因果枚举、因果链查询、任务投影、自检投影、模板匹配和自然语言显示函数族。
当前调用方：旧需求目标投影、叶子任务生成、自检、方法候选、显示命名和诊断。
触发路径：按条件、结果、动作、目标状态或二次特征候选查询旧因果模板，并生成显示文本或投影层级。
输入：条件特征、结果特征、结果状态、方法节点、动作语义键、叶子任务、来源需求、目标宿主、目标特征、二次特征候选。
输出：因果节点集合、因果模板集合、边数量、候选投影数量、贡献层级、来源因果主键、缺失证据、自然语言文本。
读结构：旧因果模板、旧二次特征、旧状态、旧特征、旧任务、旧需求、旧语素入口。
写结构：无。
是否写机器事实：否；但输出可驱动旧需求 / 任务 / 方法候选。
是否依赖日志：否。
是否依赖显示：自然语言显示文本是显示层材料。
是否依赖 SQL：否。
是否依赖线程：否。
是否依赖外设 / D455 / 体素：否。
保留行为：因果查询可作为后续需求树、任务管理、方法候选和显示只读视图的候选来源。
删除行为：不得把查询命中、自然语言文本、主键列表或候选层级直接当作需求满足、任务生成或方法可执行事实。
新增约束：候选召回必须由对应领域服务复判；显示文本只读，不写机器事实。
目标新结构：因果引用候选、动态证据、需求目标状态、任务候选、方法候选、显示层只读视图。
目标领域服务入口：因果服务后续查询入口、需求服务 / 任务服务 / 方法服务候选入口、显示层只读视图专项。
对应规范 / 详细设计：需求结算记录详细设计、任务系统详细设计、方法系统详细设计、显示层只读视图后续专项。
入口拒绝条件：查询目标无效、候选关系不唯一、未确认多目标语义、显示文本被当事实、查询命中绕过领域复判。
验证方式：查询只返回候选和缺失证据；不能新增需求、任务、方法或结算事实。
初步判定：排除 / 后续专项
待用户确认项：因果链查询和任务投影是否分别进入需求树 / 任务管理 / 方法系统后续增强专项与显示层只读视图专项。
扫描证据：`因果类.cpp:2709` 至 `因果类.cpp:3099`、`因果类.cpp:3456` 至 `因果类.cpp:3600`。
```

## 4. 本轮初步分流

```text
可保留方向：
- 二次特征节点作为长期基础信息节点。
- 二次特征通过组成关系、来源状态 / 动态和主体客体关系表达复合前置结构。
- 因果入口必须以动态证据作为准入材料。
- 因果查询、模板匹配和投影只能作为候选来源，必须由领域服务复判。

必须删除或改写：
- 旧二次特征主信息字段直接承载方向、差值、标量、离散语义、满足标记和置信度的事实口径。
- 旧因果主信息 / 因果模板主信息字段直接承载证据计数、稳定度、动作语义文本和样本 Vec 的事实口径。
- 旧普通父子概念树、签名缓存、默认刻度和自然语言文本作为权威结构。

后续专项：
- 特征 / 特征值系统增强专项：二次特征值材料、标量 / 离散材料和差值材料。
- 需求树 / 任务管理 / 方法系统后续增强专项：主动因果任务化、方法结构缺口、学习缺口和任务投影。
- 事件日志持久化恢复专项：证据动态样本、因果模板积累和恢复材料。
- 显示层只读视图专项：因果自然语言、候选说明和诊断输出。
```

## 5. 禁止宣称

```text
不得声明旧二次特征类或旧因果类已迁移。
不得声明旧二次特征主信息、旧因果主信息或旧因果模板主信息字段可直接迁移。
不得声明二次特征系统、因果系统、因果模板系统、方法学习系统或任务投影系统完成。
不得把单个因果引用、证据计数、稳定度、候选命中或自然语言文本解释为稳定因果结论。
不得接控制面板 / SQL / D455 / 体素 / 外设。
```
