# 任务生命周期通用执行新三件套与连续计划生成 Codex 断点清单

日期：2026-07-11

治理编号：JY-239

下一可用治理编号：JY-240

计划编号：#221-#225

设计队列：DQ-113-DQ-117

待确认问题：QR-169 已按默认裁决收口，无用户确认门禁

基准提交：`abb293c`

状态：流程图 MD+HTML、详细设计、五份依赖门控计划与交叉依赖修订已生成 / 未改 C++ / 未运行构建 / 未运行程序

## 1. 输入依据

```text
实施记录/20260711_TASK-LIFECYCLE-S0_任务生命周期与通用执行当前代码事实复核_Codex断点清单.md
规范/任务系统规范.md
规范/详细设计/任务状态机筹办执行桥详细设计.md
规范/详细设计/运行宿主与多线程消息队列详细设计.md
规范/详细设计/自我治理循环详细设计.md
规范/详细设计/通用方法召回登记规格索引复判排序详细设计.md
海中鱼巣/领域/任务服务.h
海中鱼巣/领域/方法服务.h
海中鱼巣/线程/任务管理线程.ixx
海中鱼巣/线程/任务工作线程.ixx
海中鱼巣/线程/任务结果回执协议.ixx
海中鱼巣/线程/任务管理上行桥.ixx
```

## 2. 当前事实基线

```text
任务生命周期当前只以多个实例状态和普通引用保存历史，没有唯一当前态关系或版本。
任意状态辅助可写非零 I64；迁移材料把创建状态误作当前状态候选。
任务方法、实际结果和完成入口没有通用召回、合法当前态、目标比较和幂等完成契约。
任务管理 / 工作线程只有请求 / 回执壳；普通回执不生成强类型领域证据。
强类型回执和上行桥已有只读复核基础，但上行桥不正式落账。
固定安全根是入口隔离样例，不能替代通用任务执行路线。
关系枚举当前止于 `概念生命周期 = 12`；#212 已预留 `需求目标概念 = 13`。
```

## 3. JY-239 裁决

```text
1. 追加 `关系类型::任务生命周期 = 14`，不得占用 #212 的关系 13 或重排既有值。
2. 同一任务恰有一条有效任务生命周期关系；关系顺序号是生命周期版本，创建为 1，换代严格加一。
3. 旧关系转已失效并保留审计；时间戳、枚举大小、节点编号和状态数量不裁决当前态。
4. 合法矩阵固定为已创建 -> 已承接 -> 筹办中 -> 排队中 -> 执行中，并含等待 / 待重筹办回路；完成、失败、取消为终态。
5. 通用迁移入口不写完成；失败 / 取消使用专用证据，执行中取消另需动作停止证据。
6. #221-#225 复用 #217 单进程结构事务域和同域令牌，不声明崩溃 / 跨重启恢复。
7. 第一版授权复用需求到任务归属与任务到需求引用配对；#209 实际结论不同则 #223 退回设计。
8. 方法召回、复判、排序和建议全部只读；只有任务服务提交唯一当前选择，语义并列不自动授权。
9. 完整执行请求使用版本化强类型协议和管理线程私有有锁有界队列；运行消息只作信封。
10. 首版只支持正式登记且已有领域执行入口的适配器；禁止任意函数表、外部执行器和真实外设。
11. 线程不是动作来源；状态 / 动态由方法执行或领域服务写，强类型回执仍为非权威材料。
12. 状态服务结构化比较目标 / 实际；未达成只保留证据并等待 / 重筹办，不写最终任务结果。
13. 任务结果与完成在同域独占许可内提交；需求结算是后续独立边界，结算重试不回滚任务完成。
14. #223 是 #209 明确后继并解锁 #214；#225 是 #215 的通用结果精确前置。
15. #216 与 #214 / #215 无业务依赖；执行可跳过两项门控继续 #216-#225，再回扫已解锁项。
```

## 4. 生成产物

### 4.1 流程图与详细设计

```text
流程图/20260711_任务生命周期筹办通用执行与结果结算流程图_v0.1.md
流程图/20260711_任务生命周期筹办通用执行与结果结算流程图_v0.1.html
规范/详细设计/任务生命周期筹办通用执行与结果结算详细设计.md
```

### 4.2 连续实施计划

```text
#221 / DQ-113 TASK-LIFECYCLE-S1 唯一当前态投影与历史读回。
#222 / DQ-114 TASK-LIFECYCLE-S2 合法迁移提交与终态封口。
#223 / DQ-115 TASK-PLANNING-S1 筹办召回与任务服务选择提交。
#224 / DQ-116 TASK-EXECUTION-S1 通用执行调度与强类型回执。
#225 / DQ-117 TASK-RESULT-S1 任务结果完成与需求独立结算。
```

五份计划均包含当前事实、假定接口、前置编号、预期产物、允许 / 禁止文件、入口拒绝、失败二分、实际接口复核、漂移退回、完整验收和完成边界。

## 5. 交叉依赖修订

```text
#214：动态“#209 后继”改为精确 #223；A01 固定记录 #223 提交号和实际接口。
#215：任务结果前置改为精确 #225，并继续要求 #214 真实绑定回执。
#216：删除错误的“队列串行前置 #215”；只依赖已完成 #198 / #199，和 #200-#215 无业务依赖。
概念命名详细设计、#214 / #215 计划、安全删除流程图 MD / HTML、#216 计划和共享治理同步同一口径。
```

这项修订只修复计划路由，不改变任何已实现 C++ 行为，也不把未来 #221-#225 写成当前事实。

## 6. 依赖扫描结果

```text
#202 -> #203 -> #204 -> #205 -> #206 -> #207 -> #208 -> #209
#211 -> #212 -> #213
#216 -> #217 -> #218 -> #219 -> #220
#217 -> #221 -> #222 -> #223 -> #224 -> #225
#209 + #222 -> #223 -> #214
#223 -> #224 -> #225
#214 + #225 -> #215
```

不存在 #215 -> #216 -> #217 -> #221-#225 -> #215 的闭环。编号顺序不等同业务依赖；执行窗口应跳过未满足项并在前置完成后回扫。

## 7. 验证口径

```text
流程图 Markdown 与 HTML Mermaid 主体必须完全一致。
详细设计五组验收矩阵必须与 #221-#225 各计划逐字一致。
计划索引、主执行队列、设计队列、当前状态、QR-169 和 JY-239 必须使用相同依赖编号。
所有五份计划必须同时引用流程图 MD、HTML 和详细设计。
git diff --check 必须通过。
py -3.14 tools/check_specs.py 必须通过。
差异范围不得包含 C++、ixx、vcxproj、filters、日志、SQL、界面或构建产物。
```

本窗口按设计规则不运行构建、不运行程序，不能宣称任何 TASK-LIFECYCLE-S1 至 TASK-RESULT-S1 验收已经通过。

## 8. 完成声明六项

| 项 | 内容 |
| --- | --- |
| 输入入口 | #201 / JY-238 正式事实记录、QR-169 默认裁决、当前任务 / 方法 / 线程 / 回执代码和既有 #203-#220 计划。 |
| 核心处理 | 固定任务当前态、迁移、筹办选择、执行回执、结果结算五段结构契约，并消除 #214-#216 的动态前置与错误串行依赖。 |
| 结构承载 | 本轮仅由流程图、详细设计、计划、索引和项目记忆承载设计 / 路由事实；没有新增机器结构。 |
| 输出变化 | 生成 #221-#225、DQ-113-DQ-117、JY-239，更新 QR-169，并把 #223 / #225 接为 #214 / #215 精确前置。 |
| 调用路径 | `#201 S0 -> 新流程图 -> 详细设计 -> #221 当前态 -> #222 迁移 -> #223 筹办选择 -> #224 执行回执 -> #225 结果结算 -> 回扫 #214 / #215`。 |
| 验证方式 | 文档逐项映射、Mermaid 同图、依赖无环、规范检查、差异和精确文件范围检查；不构建、不运行。 |

## 9. 禁止扩大声明

本断点只证明任务生命周期与通用执行的新三件套、连续计划和治理路由已经生成。不得据此宣称唯一当前态、合法迁移、方法召回、任务授权 / 选择、线程真实执行、强类型回执自动生成、任务结果、需求结算、恢复、持续自我循环、自我苏醒或旧能力迁移已经实现。
