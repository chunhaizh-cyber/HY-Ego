# 需求树后续结构代码逻辑详细设计生成 Codex 断点清单

日期：2026-07-08

## 1. 输入入口

```text
用户指令：生成下一份详细设计。
当前窗口：设计确认窗口。
承接顺序：第 6 项“需求树后续结构流程”。
```

## 2. 核心处理

```text
1. 按开工规则回读计划索引、规则总纲、规则迁移清单和项目记忆入口。
2. 回读应用逻辑流程图迁移顺序信息数据，确认第 6 项为“需求树后续结构流程”。
3. 回读第 6 项代码逻辑流程图、旧需求树候选详细设计、FS-04 只读扫描和逻辑迁移包归并表。
4. 回读当前 `海中鱼巣/领域/需求服务.h`，确认当前已落入口为材料读取、结构化更新请求拒绝、枚举目标准入材料可读和方法候选准入材料可读。
5. 新增 BASE-D4 需求树后续结构代码逻辑详细设计。
6. 同步计划索引、当前状态、决策记录和待确认问题。
```

## 3. 结构承载

```text
详细设计：
- 规范/详细设计/需求树后续结构代码逻辑详细设计.md

项目治理记录：
- 计划/计划索引.md
- 项目记忆/当前状态.md
- 项目记忆/决策记录.md
- 项目记忆/待确认问题.md

实施记录：
- 实施记录/20260708_需求树后续结构代码逻辑详细设计生成_Codex断点清单.md
```

## 4. 输出变化

```text
新增第 6 项 BASE-D4 详细设计候选。
该设计明确：
- 当前已落代码只提供需求树后续结构材料读取、结构化更新请求拒绝、枚举目标准入材料可读和方法候选准入材料可读。
- 阻塞权重只是第一轮请求材料，不是权威权重系统。
- 结构化更新请求判定不提交父子关系。
- 枚举目标和方法候选入口只返回材料可读性，不写事实、不召回方法。
- 父子写入、逻辑组织、方向签名、枚举值域、方法候选算法和结构化提交仍需后续施工计划确认。
```

## 5. 调用路径

```text
生成下一份详细设计
-> 应用逻辑流程图迁移顺序第 6 项
-> 需求树后续结构代码逻辑流程图
-> 当前需求服务入口事实复核
-> BASE-D4 需求树后续结构代码逻辑详细设计
-> 计划索引和项目记忆状态同步
```

## 6. 验证方式

计划验证：

```text
git diff --check
python .\tools\check_specs.py
```

未执行：

```text
未修改 C++。
未运行 msbuild。
未运行默认入口。
未生成施工计划。
未登记可执行队列。
```

## 7. 禁止宣称

```text
不得宣称本设计已确认。
不得宣称父子需求运行时写入已实现。
不得宣称完整需求树、逻辑组织需求、方向签名、枚举目标值域、方法候选算法或结构化更新提交入口已完成。
不得宣称旧需求类、旧需求主信息字段、SQL、控制面板、D455、体素或外设已迁移。
不得宣称本详细设计提供代码实施许可。
```
