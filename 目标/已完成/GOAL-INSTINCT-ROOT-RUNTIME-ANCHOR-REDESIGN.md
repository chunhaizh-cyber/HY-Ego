# GOAL-INSTINCT-ROOT-RUNTIME-ANCHOR-REDESIGN

## 1. 目标身份

- 目标编号：`GOAL-INSTINCT-ROOT-RUNTIME-ANCHOR-REDESIGN`
- 名称：阶段21本能根运行锚点重新设计与计划闭合
- 类型：设计目标
- 记录版本：v0.6
- 创建时间：2026-09-21
- 更新时间：2026-09-23
- 当前状态：完成
- 直接上位目标：`GOAL-SELF-GOVERNANCE-CLOSURE`
- 直接下位目标：`GOAL-INSTINCT-ROOT-RUNTIME-ANCHOR-PROVIDER-IMPLEMENTATION`
- 目标账所有者：交互智能体

## 1.1 目标问题、优先级与设计承载

根材料 provider 已由`f57e3513a`及保留的Git/验证证据发布，安全值/服务值双FT、双完整域概念与实际F provider 已由`576189d3c`发布；阶段21的两个直接上游已经满足各自完成条件。旧阶段21详细设计仍绑定旧事实，旧计划文件已按历史治理删除；二者都不能恢复为当前执行入口。

本目标优先于阶段21代码：先按当前8121、已发布provider接口、普通应用装配和恢复事实重新S0，冻结阶段21只消费两类正式交付形成/恢复运行锚点的最终合同、调用顺序、失败、读回和验证，再登记新的可执行计划。不得通过复活旧计划、临时补建FT/概念、用R代替准确F或直接写需求owner内部结构绕过设计闭合。

## 2. 目标结果、完成条件与证据下限

目标结果是形成当前事实下无DRIFT的阶段21设计包和一份新身份或正式替代身份的可执行代码计划，至少闭合：

- 普通应用中根材料交付与安全/服务实际F交付的唯一消费入口及顺序；
- 双根角色、目标`I64_MAX`、初值实际F、独立D/L/成员和共同截止运行锚点的值式绑定与fresh读回；
- 首次、精确重复、部分恢复、退出/缺项、当前性漂移、发布未知和资源失败边界；
- 允许/禁止文件、owner、幂等身份、生产接线、验证矩阵和完成声明边界。

完成条件：现行规范和当前代码事实复核通过；旧锚点详细设计/计划的每项漂移均被替代设计显式处置；新的完整计划通过创建侧审查并登记为`可执行`。本设计目标不证明阶段21代码、构建、运行、恢复、线程或集成能力完成。

## 3. 范围、依赖与下一动作

- 关联计划：`INSTINCT-ROOT-RUNTIME-ANCHOR-PROVIDER` v0.1 / blob `d2f2ba1c3d6a53c0a7d9e095ad9a462fa06b9b22`，已登记为`可执行`；旧`INSTINCT-ROOT-RUNTIME-ANCHOR` v0.1已删除且禁止恢复。
- 设计承载方式：按复杂代码目标形成有效详细设计、必要业务/函数流程与新计划；不把目标记录当作机器合同。
- 依赖：8121、8120、6130、4115、7150；根材料结果`f57e3513a`与特征组合结果`576189d3c`的Git及仍保留验证证据。
- 明确排除：阶段21代码施工、线程/运行门、需求产生、任务与方法循环、根值后续换代、8300第7级验收和正式集成验收。
- 阻断：无；设计内容、计划正文和索引登记均已发布。
- 剩余风险：旧计划文件名与身份可能包含仍有效片段，但必须逐项按当前接口复核；普通应用当前装配顺序、共同截止与跨进程恢复不能从旧聊天或旧计划直接继承。
- 下一动作：由`GOAL-INSTINCT-ROOT-RUNTIME-ANCHOR-PROVIDER-IMPLEMENTATION`按已登记计划从当前HEAD重新S0并施工四个新生产文件和隔离专项；普通应用、根工程和启动接线仍留给代码结果发布后的直接后继。
- 转入条件：已满足；详细设计和计划正文由`505b2a0ba`发布，计划由`d3b1b17e1`按精确blob登记为`可执行`。

## 4. 状态证据上下文

- 状态事实截止：`d3b1b17e1978f1f35b8203fe7998cffc7850d9e4`
- 关联计划身份：`INSTINCT-ROOT-RUNTIME-ANCHOR-PROVIDER`
- 关联计划观察版本 / blob：v0.1 / `d2f2ba1c3d6a53c0a7d9e095ad9a462fa06b9b22`
- 最后实质进展：`505b2a0ba`发布阶段21组合provider详细设计与代码计划，`d3b1b17e1`按精确blob登记为`可执行`；设计冻结现有`.h/.cpp` provider复用、公开真实自我成功谓词、唯一特征位置导出、同`Gfinal`四项重读、实际F保留载荷映射、内部同内核脚本端口和self值式映射
- 已验证证据：新基线独立复核确认`89495da35`未改变正式规范和三个provider签名；设计治理`git diff --check`、精确计划blob和索引读回通过并推送`origin/main`
- 未验证范围：阶段21代码、构建、运行、恢复、线程、生产接线和集成验收
- 阻断证据来源：无；RC-P1任务已暂停并释放index，其未提交WIP继续原地保护且不与本计划四个新生产文件重叠
- 阻断解除条件：不适用
- 自动复核触发：本目标已终态；后继代码目标、8121及关联规范、provider接口或计划精确版本变化时只读引用本记录
