# GOAL-IFR-INSTANCE-STRUCTURE-PERSISTENT-REPLAY-REPAIR

## 1. 目标身份

- 目标编号：`GOAL-IFR-INSTANCE-STRUCTURE-PERSISTENT-REPLAY-REPAIR`
- 名称：实例特征结构持久重放收敛修复
- 类型：代码实施目标
- 记录版本：v0.2
- 创建与更新时间：2026-09-20
- 当前状态：完成
- 直接上位目标：`GOAL-INSTINCT-ROOT-DEMAND-PROVIDER-IMPLEMENTATION`
- 直接下位目标：无
- 优先级理由：这是根材料 provider 重启读回之前唯一已复现的直接阻断；不修复它，不能把进程内重放冒充为持久恢复。
- 目标账所有者：交互智能体；代码切片所有者为当前交互智能体

## 2. 问题、结果与完成条件

已发布的`存在类数据服务::登记实例特征结构`在持久恢复后能读到首次规范化写集和首次写入结果，但重放时重新提交旧写集，并假定新返回的精确重复回执仍带完整新编码映射。两阶段真实L1持久探针证明该假定不成立：重复回执返回旧事实代次且映射不足，函数在已进入提交后误报`已可能发布`。这使其既有重放成功合同无法跨进程收敛，并在根 provider 前阻断普通应用依赖重建。

目标结果：仅在既有精确重放路径以已持久的首次写入结果恢复映射与首次事实信息，并以当前事实截止完成无新写入的读回；成功仍严格满足既有请求、版本、交付和`Gread`谓词。首次写入、DTO、写集布局、幂等身份、owner、失败枚举、事务和公开合同均不改变。

完成条件：冻结范围内的代码和专属施工/验证记录已发布；仓外两进程真实持久L1探针完成“形成→进程退出→恢复→精确重放”且成功谓词、交付和当前截止成立；指定头/根工程构建按实际风险通过。不得据此声明根材料 provider、阶段21、启动、资源故障、发布未知、连续运行或集成能力完成。

## 3. 切片、依据与下一动作

- 依据：`海中鱼巣/领域/数据服务.存在类.h:4894-4948`；已冻结结果谓词要求`Gread >= r.G0`，L1首次材料提供持久的首次规范化写集与首次写入结果。两进程探针：seed成功`G=10`，recover时实例登记返回`状态=已可能发布(7), 版本=1, G0=10, Gread=7, 首次H=0`。
- 直接代码切片：起点`5d79014412e16cbfab3c6e2d2299a44ec4d4e6e3`；允许`海中鱼巣/领域/数据服务.存在类.h`和本目标专属施工/验证记录；禁止修改DTO、公开合同、L1、需求类、普通应用、计划、规范、启动、线程及持久存储格式。
- 设计承载：既有实例特征结构请求/结果成功谓词、L1首次材料合同和本目标的直接切片声明已唯一裁决；不新增机器语义。
- 验证：仓外持久两进程探针，以及受影响根工程隔离构建；普通应用与根材料恢复仅在修复后重新S0验证。
- 下一动作：无；本目标已于`14953b8703a953eb6c13a087eb0cd64e188b515f`完成并进入终态。直接上位目标须从该基线重新S0。

## 4. 状态证据上下文

- 状态事实截止：`14953b8703a953eb6c13a087eb0cd64e188b515f`
- 关联计划：无；这是不改变公开合同、事务、owner、生命周期或持久格式的直接代码切片。
- 完成证据：提交`14953b8703a953eb6c13a087eb0cd64e188b515f`已推送；两进程真实持久L1探针输出`recover-instance pass G=10 version=2 firstH=7`并退出0；根工程Debug/Release x64隔离重建通过，Release为0警告、0错误。证据见专属施工/验证记录。
- 未验证范围：L1资源失败、发布未知、故障注入、根材料 provider 持久重放、阶段21、启动、跨服务普通应用恢复及集成验收；本目标完成不覆盖这些范围。
- 剩余风险：后续需求结构登记在同一两进程探针中仍返回`事实代次漂移`；它在上位根材料计划白名单内，须由该目标重新S0后处理。
- 自动复核触发：本目标代码、持久探针、相关构建记录、Git发布或用户“继续/同步”。
