# GOAL-SELF-THREAD-G1-ISOLATED-SHELL-IMPLEMENTATION

## 1. 目标身份

- 目标编号：`GOAL-SELF-THREAD-G1-ISOLATED-SHELL-IMPLEMENTATION`
- 名称：自我线程G1隔离上层骨架代码实施
- 类型：代码实施目标
- 记录版本：v0.2
- 创建与更新时间：2026-09-23
- 当前状态：完成（代码实施目标）
- 直接上位目标：`GOAL-SELF-GOVERNANCE-CLOSURE`
- 来源设计目标：`GOAL-SELF-THREAD-M1A-TOP-DOWN-DESIGN`（已完成）
- 直接下位目标：`GOAL-INSTINCT-ROOT-RUNTIME-ANCHOR-REDESIGN`（当前）；不自动进入生产接线
- 目标账所有者：交互智能体

## 1.1 目标问题与优先级

本目标已在`线程_自我.h/.cpp`实现最终self对象可复用的单对象物理生命周期、关闭治理门、强类型有界邮箱、独立停止控制和`join`，并声明但不注入/调用两个上层窄消费端口。阶段21锚点、fresh上下文、双根裁决、根治理意图sink和普通应用生产接线仍未就绪，首叶继续保持治理门关闭。

本目标与暂停的RC-P1代码切片文件、ABI和验证工程不重叠，只修改两个self源码及新增专属专项/记录；不得触碰RC-P1占用的根工程、filters和普通应用实现。

## 2. 目标结果、完成条件与证据下限

目标结果是发布计划`SELF-THREAD-G1-ISOLATED-SHELL`冻结的隔离上层骨架，并以双配置独立专项证明其物理生命周期和停止守恒。

完成条件：

1. `线程_自我.h/.cpp`实现计划冻结的值式DTO、两个窄端口接口、不可复制移动self对象和全部公开函数；对象不注入、不调用端口。
2. 每个对象生命周期至多创建一个物理线程，停止前同选择精确重复、异选择冲突；停止锁存后禁止重启。
3. 创建成功真实形成入口/停门见证，治理门保持关闭，冻结批次为零；开门入口带`待实现`标注并固定返回`依赖未就绪`。
4. 强类型邮箱线程安全、有界；关闭门只排队不消费；停止使用独立控制信号，不受队满影响；所有完成路径最终`join`，禁止`detach`和忙等。
5. self源码零DATA-L1/DATA-L2、仓库、写端口、事务端口、普通应用getter、世界树和需求数据服务调用。
6. 新建专项Debug/Release x64构建与运行通过；施工/验证记录如实登记PASS/FAIL/NOT_RUN和函数级知识变更清单。
7. 允许范围代码和记录以精确提交发布，计划索引退出本计划；交互智能体据结果建立下一最小提供者目标。

最低证据是计划允许范围的已发布代码、施工/验证记录、静态越层检查及双配置专项实际结果。局部代码提交或编译不能证明进程级唯一self、生产接线、治理开门、M1-A或内部治理闭环完成。

## 3. 范围、设计承载与排除

- 关联计划：`SELF-THREAD-G1-ISOLATED-SHELL` v0.1 / blob `13d8dca640645c67ad8a26c0228dd0506ebd3638`，已完成并移入`计划/已完成计划/`。
- 设计承载：`规范/详细设计/20260922_SELF-THREAD-M1A-UPPER-SHELL_自我线程上层生命周期与根需求复核详细设计_v0.1.md`与计划正文组合承载；执行者不得临场改变ABI、并发或生命周期。
- 允许/禁止文件、函数、验证和完成声明：全部以计划第2—9节为准，本记录不复制施工合同。
- 明确排除：根工程/filters、普通应用、启动入口、阶段21provider、真实上下文/双根provider、根治理意图sink、生产开门、任务/方法/安全治理、跨进程恢复和正式集成验收。
- 主要风险：错误复用无锁通用队列或忙等宿主线程；把隔离夹具身份当正式锚点；把“每对象至多一个线程”夸大为进程唯一self；验证工程误读RC-P1 WIP。

## 4. 状态证据上下文

- 状态事实截止：`1445f4250324b3dad4b0524c9be9850c10630267`
- 关联计划身份：`SELF-THREAD-G1-ISOLATED-SHELL`
- 关联计划观察版本 / blob：v0.1 / `13d8dca640645c67ad8a26c0228dd0506ebd3638`
- 最后实质进展：`1445f4250`发布两个self源码、独立专项工程及施工/验证记录；单对象隔离生命周期、关闭门、有界邮箱、停止唤醒和`join`已形成
- 已验证证据：静态越层检查零命中；Debug/Release x64隔离Rebuild均为0 warning / 0 error，专项运行均为`30 PASS / 0 FAIL`；`main == origin/main`
- 未验证范围：进程或普通应用唯一self、治理门开放、阶段21锚点、真实上下文/双根复核、根治理意图、生产接线、跨进程恢复、M1-A和内部治理闭环
- 当前阻断：无
- 阻断证据来源：无
- 阻断解除条件：不适用；S0若发现self源码、正式规范或计划漂移，只停止本切片并具名处理
- 自动复核触发：阶段21提供者、self源码、正式规范、生产接线或验收证据变化

## 5. 下一动作与转入条件

- 下一动作：由当前目标`GOAL-INSTINCT-ROOT-RUNTIME-ANCHOR-REDESIGN`从`1445f4250`重新S0，按self真实消费合同闭合阶段21最小提供者设计；不得直接进入普通应用生产接线。
- 转入结果：本目标已移入`目标/已完成/`。后继设计与代码结果不得反向扩大本目标完成声明。
