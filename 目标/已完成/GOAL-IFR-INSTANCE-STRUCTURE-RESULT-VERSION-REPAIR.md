# GOAL-IFR-INSTANCE-STRUCTURE-RESULT-VERSION-REPAIR

## 1. 目标身份

- 目标编号：`GOAL-IFR-INSTANCE-STRUCTURE-RESULT-VERSION-REPAIR`
- 名称：实例特征结构登记结果与重放一致性修复
- 类型：代码实施目标
- 记录版本：v0.3
- 创建与更新时间：2026-09-20
- 当前状态：完成
- 直接上位目标：`GOAL-INSTINCT-ROOT-DEMAND-PROVIDER-IMPLEMENTATION`
- 直接下位目标：无
- 目标账所有者：交互智能体；代码切片所有者为当前交互智能体

## 2. 目标问题、结果与完成条件

现行`存在类数据服务::登记实例特征结构`有两个同一合同内的实现缺陷：成功形成或精确重复时交付v2结构，却保留`实例特征结构登记结果`默认`版本=1`；以及首次材料保存首次`G0`后，按当前截止重放时把本次并发守卫误作持久写集语义，返回`幂等冲突`。前者使既有成功谓词为假并阻断普通应用装配，后者使精确重放合同不可达。

目标结果：首次和精确重复均返回与既有v2请求、交付和成功谓词一致的结果版本；重放仅忽略本次守卫的`G0`，其余写集字段精确一致。非成功路径、写集布局、幂等身份、owner、事务、读取与交付合同不变。

完成条件：只在冻结文件范围内修正成功结果版本并发布专属施工/验证记录；独立真实L1登记验证成功谓词，普通应用装配能越过实例特征结构检查并到达需求服务装配，根工程指定构建通过。不得据此声明根材料 provider、阶段21、启动或集成能力完成。

## 3. 范围、依据、切片与下一动作

- 依据：`海中鱼巣/领域/数据服务.存在类.h:210-218,4822-4945`；成功谓词已冻结请求v2与结果`版本==2`，当前成功分支只写交付和状态而未写结果版本；`4894-4900`对首次规范化写集的完整比较还将重放守卫`G0`误纳入幂等语义。
- 直接代码切片：起点`f9febe7d78481fecf60472387d9341dc7ad67dfe`；允许`海中鱼巣/领域/数据服务.存在类.h`和本目标专属施工/验证记录；禁止修改DTO定义、请求/结果合同、写集、所有权、普通应用、需求服务、计划、规范、启动和线程。
- 验证：仓外真实L1驱动分别验证首次与精确重复的成功谓词和交付；根工程Debug/Release隔离重建；普通应用仅验证越过本检查和需求服务已持有，不调用根provider。
- 下一动作：无；本目标已于`8ea62e5ed91697f164ca132b80e87720544433d3`完成并进入终态。直接上位目标须从该基线重新S0。

## 4. 状态证据上下文

- 状态事实截止：`8ea62e5ed91697f164ca132b80e87720544433d3`
- 关联计划：无；这是不改变公开合同、事务、owner或生命周期的直接代码切片。
- 完成证据：提交`8ea62e5ed91697f164ca132b80e87720544433d3`已推送；仓外真实L1探针通过首次及精确重复成功谓词和交付，普通应用装配读回取得需求服务且不调用根provider，根工程Debug/Release x64隔离Rebuild均为0警告、0错误；证据见专属施工/验证记录。
- 未验证范围：首次/重复以外的错误路径、资源失败、发布未知、恢复与所有上位业务能力；本目标完成不覆盖这些范围。
- 剩余风险：重放比较仅排除本次截止守卫`G0`，其余写集语义仍精确比较；上位根材料计划须以新基线重新S0确认其专项矩阵的剩余范围。
