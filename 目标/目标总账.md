# 目标总账

本文件是目标模式的当前目标导航入口，不是计划索引，不替代计划正文、施工记录或集成验收记录。

## 当前目标

| 目标编号 | 目标名称 | 类型 | 状态 | 当前关联 | 阻断 |
| --- | --- | --- | --- | --- | --- |
| `GOAL-IF-R-FOUNDATION` | 实例特征 IF/R 基础闭合 | 当前小目标 | 阻断 | `INSTANCE-FEATURE-IF-R-CONCEPT-FOUNDATION` v0.8 | `数据服务.存在类.h`、`应用服务.特征概念类.h/.cpp` 有同范围未提交 WIP，需先确认所有权和 S0 |

## 上位目标

| 目标编号 | 目标名称 | 类型 | 状态 |
| --- | --- | --- | --- |
| `GOAL-INSTINCT-RUNTIME` | 建立基于世界事实和实例特征的本能运行系统基础 | 阶段目标 | 当前路线 |

终态目标分别记录在`已完成/`、`已作废/`或`无法完成/`目录；移动后不在本文件保留现行副本。
