# GOAL-IFR-ACTUAL-F-PROVIDER-DESIGN

## 1. 目标身份

- 目标编号：`GOAL-IFR-ACTUAL-F-PROVIDER-DESIGN`
- 名称：按存在持有特征变更并返回实际F提供者设计闭合
- 类型：设计目标
- 记录版本：v0.7
- 创建与更新时间：2026-09-21
- 当前状态：当前
- 直接上位目标：`GOAL-INSTINCT-RUNTIME`
- 直接下位目标：无；设计闭合后按计划规则形成代码实施目标
- 目标账所有者：交互智能体

## 2. 目标结果与完成条件

为8121阶段21形成唯一、可实施的上游 provider 合同：它仅在存在 owner 已冻结的边界内处理“按存在持有特征变更并返回实际F”，并明确输入、输出、实际F来源、当前采用、IF/R、FCv、幂等、读回、失败、所有权与验证边界。

完成条件：现行规范、当前代码与必要设计材料足以唯一裁决该 provider 的机器语义、物理位置、公开合同、事务、生命周期、失败与验证矩阵，并形成可执行计划。若发现现行材料不能唯一裁决，会改变真实观察或当前采用事实的缺口，必须保持本目标为阻断并记录具名阻断；不得以当前 IF/R 观察或R代表值冒充实际F。

- 目标问题：8121阶段21只能消费实际 F；当前实现只具备 IF/R 观察、I64 F 出生和当前采用的分散能力，尚无该组合 provider 的冻结合同。
- 优先级理由：它是阶段21、本能运行锚点、后续线程与集成验收的唯一直接上游；在它闭合前推进阶段21会绕过存在 owner 或伪造实际 F。
- 设计承载方式：按世界树根的“固定原请求形成/恢复→正式读回→按同一底座受保护发布非权威内存投影”模式，先在本目标中闭合根特征定义 provider；随后在目标计划中直接承载实际 F provider 的完整机器合同，不重复建立独立详细设计文件。

## 3. 范围、依赖与下一动作

- 依据：4115 v0.9、6120 §2.1、6130 §2—§3/§7/§13、7150 §2.1、8121 §2—§5、4110、7160以及当前`数据服务.特征类.*`、`数据服务.存在类.h`、`应用服务.特征概念类.*`。
- 已核对事实：4115已冻结IF/R、R归组与按需概念抽象；现有`特征概念应用服务::处理实例特征R观察`只在`形成新R`时调用 I64 准确 F 出生并提交 IF/R，唯一命中只更新 R 材料。它不调用`存在类数据服务::变更当前采用`、不返回当前采用后的 F，且非 I64 直接返回`比较未启用`。
- 已核对根值：6120 §2.1与6130 §3已经冻结安全/服务根的首次当前值分别为`I64_MAX / 2 = 4611686018427387903`，根目标均为`I64_MAX`；8121“阶段21不得预设首值”仅限制阶段21本身，不否定上游 provider 消费这些已冻结输入。
- 明确排除：阶段21、根材料、线程、启动、普通应用接线、FCr自动汇聚、任意代码修改和把R代表值替代准确F。
- 依赖：双根抽象特征定义的形成/恢复合同是本 provider 所需正式 FT、定义身份及比较规则的唯一来源。当前采用、IF/R 与 F 出生能力均已存在，但只能在该上游输入可读后组合。
- 已裁决的形成模式：用户确认根特征定义与世界树根同样处理。该口径冻结为：两根各以固定、版本化、互不相同的完整原请求形成或恢复持久定义事实；每次调用必须按原请求与首次写入材料收敛，并正式读回 FT、规格、比较规则和首次 H；仅在读回成功后，普通应用才可按同一 L1 底座发布带互斥保护的值式全局投影。投影只加速同进程导航，不是第二身份、第二当前值、跨进程恢复来源或需求 owner。
- 已解除阻断：`DRIFT-ROOT-I64-FEATURE-SOURCE-CLASS-UNDECLARED`已由1120 v0.9、4120 v0.16和6130 v0.16裁决为受限的“系统根初始化来源”。双根定义 provider 只以固定、版本化、互不相同的原请求和首次写入材料形成/恢复FT及规则关联，并正式读回；它不伪造外设、不放宽后天来源、也不产生实例或需求事实。旧`先天I64特征类型.外设提供者`仅保留外设先天语义，后续必须以带标签来源合同处理根FT。
- 当前下一动作：基于新来源类别完成根I64 FT形成/恢复与按存在返回实际F provider的设计闭合，并形成唯一最早可执行计划；仍不得提前实施阶段21、建立根D，或以R代表值替代实际F。

## 4. 状态证据上下文

- 状态事实截止：`a2769249d9df6cc56aca7d99058c84541e920883`
- 关联计划：无；当前仅是设计闭合目标。
- 已验证证据：8120 §5.1—§5.3 已冻结世界树根的固定原请求、正式读回、候选完成后受保护全局投影和同底座约束；当前`世界树应用服务`实现也将投影与持久身份分离。6130 §3 已定义双根固定、版本化业务幂等身份、值域、首次值、目标和单位/维度/分量角色的定义身份绑定；6120 §2.1同样冻结 A0/V0 与目标。4115 v0.9 §2.1、§3明确 IF/R 不等同于准确 F 或当前采用，且唯一命中不得出生 F；7150 §2.1明确当前采用须显式指定准确 F、可为空且不自动回退；8121 §2—§4把阶段21限定为该上游 provider 的消费者。当前`应用服务.特征概念类.h/.cpp`只提供 R 观察结果，未提供实际 F 或当前采用写入。已排除旧候选作为现行依据：`20260828_INSTINCT-BOOTSTRAP-ROOT-ANCHOR_v0.3`使用旧 L2 且把根 D 物理绑定自我；`20260915_INSTINCT-ROOT-RUNTIME-ANCHOR_v0.1`已列 DRIFT 并等待本 provider；`20260829_INSTINCT-STAGE3-SAFETY-ROOT-DEFINITION-CURRENT-PROVIDER_v0.1`仍走旧 L2。
- 未验证范围：根定义 provider 合同、实际 F provider 的完整计划和实现、普通应用消费、阶段21、运行/恢复/集成验证；本次未发布计划或代码。
- 阻断：无。根FT来源类别已裁决；尚未完成根定义 provider、实际F provider计划或代码，不得把设计未完成表述为能力已可用。
- 自动复核触发：1120、4120、6130、6120、4115、7150、7160、8121、根定义合同、特征定义 API、现有 provider 代码或计划发生变化，或用户给出与本口径冲突的机器语义裁决。
