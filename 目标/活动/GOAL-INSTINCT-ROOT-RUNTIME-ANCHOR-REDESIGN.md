# GOAL-INSTINCT-ROOT-RUNTIME-ANCHOR-REDESIGN

## 1. 目标身份

- 目标编号：`GOAL-INSTINCT-ROOT-RUNTIME-ANCHOR-REDESIGN`
- 名称：阶段21本能根运行锚点重新设计与计划闭合
- 类型：设计目标
- 记录版本：v0.3
- 创建时间：2026-09-21
- 更新时间：2026-09-22
- 当前状态：暂停；作为自顶向下施工中上层调用遇到的已知候选提供者目标
- 直接上位目标：`GOAL-SELF-GOVERNANCE-CLOSURE`
- 直接下位目标：无；设计闭合后形成阶段21代码实施目标
- 目标账所有者：交互智能体

## 1.1 目标问题、优先级与设计承载

根材料 provider 已由`f57e3513a`及其施工/验证证据发布，安全值/服务值双FT、双完整域概念与实际F provider 已由`576189d3c`发布；阶段21的两个直接上游已经满足各自完成条件。现有`规范/详细设计/20260915_INSTINCT-ROOT-RUNTIME-ANCHOR_本能根运行锚点详细设计_v0.1.md`及`计划/过期计划/20260915_INSTINCT-ROOT-RUNTIME-ANCHOR_本能根运行锚点代码实施计划_v0.1.md`仍绑定旧事实并已具名 DRIFT，不能直接执行。

本目标优先于阶段21代码：先按当前8121、已发布provider接口、普通应用装配和恢复事实重新S0，冻结阶段21只消费两类正式交付形成/恢复运行锚点的最终合同、调用顺序、失败、读回和验证，再登记新的可执行计划。不得通过复活旧计划、临时补建FT/概念、用R代替准确F或直接写需求owner内部结构绕过设计闭合。

## 2. 目标结果、完成条件与证据下限

目标结果是形成当前事实下无DRIFT的阶段21设计包和一份新身份或正式替代身份的可执行代码计划，至少闭合：

- 普通应用中根材料交付与安全/服务实际F交付的唯一消费入口及顺序；
- 双根角色、目标`I64_MAX`、初值实际F、独立D/L/成员和共同截止运行锚点的值式绑定与fresh读回；
- 首次、精确重复、部分恢复、退出/缺项、当前性漂移、发布未知和资源失败边界；
- 允许/禁止文件、owner、幂等身份、生产接线、验证矩阵和完成声明边界。

完成条件：现行规范和当前代码事实复核通过；旧锚点详细设计/计划的每项漂移均被替代设计显式处置；新的完整计划通过创建侧审查并登记为`可执行`。本设计目标不证明阶段21代码、构建、运行、恢复、线程或集成能力完成。

## 3. 范围、依赖与下一动作

- 关联计划：旧`INSTINCT-ROOT-RUNTIME-ANCHOR` v0.1仅作待重建设计输入，不是当前执行候选；当前尚无可执行阶段21计划。
- 设计承载方式：按复杂代码目标形成有效详细设计、必要业务/函数流程与新计划；不把目标记录当作机器合同。
- 依赖：8121、8120、6130、4115、7150；根材料结果`f57e3513a`及其证据；特征组合结果`576189d3c`及其施工/验证记录。
- 明确排除：阶段21代码施工、线程/运行门、需求产生、任务与方法循环、根值后续换代、8300第7级验收和正式集成验收。
- 阻断：无；两个直接上游已发布，旧设计DRIFT正是本目标要闭合的设计输入。
- 剩余风险：旧计划文件名与身份可能包含仍有效片段，但必须逐项按当前接口复核；普通应用当前装配顺序、共同截止与跨进程恢复不能从旧聊天或旧计划直接继承。
- 下一动作：等待M1-A上层施工固定实际锚点调用合同后恢复；恢复时从届时HEAD重新执行阶段21设计S0，按8121、两个已发布provider、普通应用装配和恢复事实形成无DRIFT设计包与新计划，不得恢复旧v0.1计划。
- 转入条件：新计划完成创建侧审查并登记`可执行`后，本目标移入`目标/已完成/`并建立阶段21代码实施目标；若出现无法由现行规范裁决的新机器语义，转为具名阻断并请求用户裁决。

## 4. 状态证据上下文

- 状态事实截止：`a8aaf58d413e8d5b3da97f8df3078291d8cb9584`
- 关联计划身份：`INSTINCT-ROOT-RUNTIME-ANCHOR`旧v0.1仅为设计输入；新计划待形成
- 关联计划观察版本 / blob：旧v0.1 / `02d9c64185d45abebc42a50a49775db277071c37`，不可执行
- 最后实质进展：`576189d3c`发布安全值/服务值先天FT、完整域概念、实际F provider及首次/跨进程恢复证据；2026-09-22自我治理路线采用先写上层、按真实调用补下层的策略，本目标保留为候选提供者设计目标
- 已验证证据：根材料和特征组合provider均已按各自代码实施目标发布；后者Debug/Release构建、全新数据库首次形成与第二进程恢复通过
- 未验证范围：阶段21最终设计、计划创建侧审查、代码、构建、运行、恢复、线程和集成验收
- 阻断证据来源：无
- 阻断解除条件：不适用
- 自动复核触发：用户恢复目标、8121及关联规范、旧/新详细设计或计划、两个provider接口、普通应用装配、Git与验证证据变化
