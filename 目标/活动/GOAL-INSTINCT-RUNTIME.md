# GOAL-INSTINCT-RUNTIME

## 1. 目标身份

- 目标编号：`GOAL-INSTINCT-RUNTIME`
- 名称：构建本能系统基于世界事实、实例特征和现实锚点持续运行的基础架构
- 类型：集成能力目标
- 记录版本：v0.22
- 创建时间：2026-09-21
- 更新时间：2026-09-25
- 当前状态：候选；当前直接推进位置为`GOAL-SELF-GOVERNANCE-CLOSURE`的4242目标合同值来源适配与G4-A双根复核provider代码实施
- 直接上位目标：无
- 直接下位目标：`GOAL-IF-R-FOUNDATION`（已完成）、`GOAL-INSTINCT-ROOT-PHYSICAL-CONTRACT`（已完成）、`GOAL-INSTINCT-ROOT-DEMAND-PROVIDER-IMPLEMENTATION`（完成）、`GOAL-IFR-ACTUAL-F-PROVIDER-DESIGN`（完成）、`GOAL-SYSTEM-ROOT-I64-ACTUAL-F-PROVIDER-IMPLEMENTATION`（完成）、`GOAL-INSTINCT-ROOT-RUNTIME-ANCHOR-REDESIGN`（完成）、`GOAL-INSTINCT-ROOT-RUNTIME-ANCHOR-PROVIDER-IMPLEMENTATION`（完成）、`GOAL-SELF-GOVERNANCE-CLOSURE`（候选，当前路线）
- 目标账所有者：交互智能体

## 1.1 目标问题与优先级

目标问题是现行系统尚未具备经正式世界事实、实例特征和现实锚点驱动的多轮本能运行闭环。它是当前已确认的最高路线目标；优先级由用户确认、8120的启动顺序和8300的集成验收门槛共同决定。

## 2. 目标结果、范围与排除

目标结果是在正式启动与恢复后，系统能够基于权威世界事实、实例特征和本能根运行锚点，进入受治理的自我运行线程；经需求、任务、方法、实际作用、实际读回、结算与后继决议形成连续多轮运行基础。

本目标消费而不重新定义世界事实、IF/R、根特征值域、特征当前采用、根需求或线程机器合同；这些分别受现行正式规范及后续合法设计包约束。观察候选、日志、缓存、代码提交、构建成功或启动成功不得被当作世界事实、持续运行或集成能力完成。

## 3. 完成条件与证据下限

1. 正式启动 / 恢复能够读回世界根、真实自我、方法登记根、IF/R 所需事实和完整本能根运行锚点；
2. 安全与服务根的实际 F、独立根需求 D、根列表项 L 和当前成员关系均按其 owner 合同建立、恢复和同截止读回；
3. 运行门开放后，需求到任务、方法选择、实际作用、实际读回、结算和下一轮决议可按正式合同连续运行；
4. 按`规范/8300_子规范_自我内部循环最小生命闭环验收_20260720.md`达到第 7 级工程验收，证明没有重复任务风暴、无限派生、空转或主链断裂；
5. 具名正式集成验收在精确提交基线上通过。

最低证据为每个下位目标的正式设计、代码、构建 / 运行 / 恢复记录及最终具名集成验收；任何较低层证据只证明其自身范围。

## 4. 当前路线与设计承载

- 已完成前置：`GOAL-IF-R-FOUNDATION` 已以代码实施目标完成，详见`目标/已完成/GOAL-IF-R-FOUNDATION.md`；其不证明构建、运行、恢复或本目标能力。
- 已完成设计：`GOAL-INSTINCT-ROOT-PHYSICAL-CONTRACT`已冻结根目标合同、独立根 D、根列表 L 与成员关系的 ARCH-L4 物理合同，并登记`ARCH-L4-INSTINCT-ROOT-DEMAND-PROVIDER`为可执行计划。
- 已完成下位目标：根材料 provider 与安全值/服务值先天FT、完整域概念和实际F组合provider均已按各自完成条件发布。
- 后续方向：用户已确认以完整自我线程治理闭环反推数据服务需求，并采用先写上层、在真实调用点补最小下层能力的策略；阶段21组合provider、生产消费、G2-A正式上下文纯读provider、双根RC初始化和4242求值器均已发布，根目标合同值的合法求值来源也已由现行规范冻结。`G4-A-INSTINCT-ROOT-DEMAND-REVIEW` v0.2现已可执行，当前实施该最早代码叶。
- 设计承载方式：机器语义、DTO、所有权、事务、恢复和验收边界由现行规范、具名详细设计和后续计划组合承载；本目标记录不替代这些材料。

## 5. 依赖、风险与下一动作

- 依赖：4115 IF/R、8120 启动顺序、8121 阶段21物理合同、8100 / 8200 运行边界、8300 验收，以及已发布的 ARCH-L4 根材料与特征组合provider。
- 当前阻断：没有当前设计阻断；当前尚缺代码层第三来源分支、G4-A根复核结果DTO与真实provider，已由v0.2计划冻结施工。真实sink/manager和任务管理/工作线程入口继续作为后继缺口。
- 当前风险：把裸`I64_MAX`、伪造目标F/B或专用复制算法当成已登记RC求值；把技术非成功压成未满足；用空sink冒充任务承接；在8120要求的生产者/消费者未就绪前开放治理门。
- 下一动作：执行v0.2计划并发布代码与分层验证结果；代码结果形成后再沿真实消费者闭合根治理意图sink，G2-B开门另行设计。

## 6. 状态证据上下文

- 状态事实截止：`232087355fdc504a1861d8468ff65129d786e157`
- 关联计划身份：`G4-A-INSTINCT-ROOT-DEMAND-REVIEW` v0.2 / blob `0d8f86e6a2f736d00b5c1179a13ae1a515ecb780` / `可执行`
- 最后实质进展：`73243a85f`和`232087355`已发布并激活经过独立复审的v0.2设计/计划，闭合双根完整复核、逐根失败定位、方向分账、验证分层和唯一实施记录路径
- 阻断证据来源：`规范/详细设计/20260924_SELF-ROOT-DEMAND-REVIEW_G4-A双根复核与根治理意图设计阻断_v0.1.md` v0.5
- 阻断解除条件：当前设计阻断已解除；按v0.2实施并验证G4-A结果、状态及provider合同。sink/manager作为其正差距载荷的直接后继另行闭合。
- 自动复核触发：上述合同、任务管理/工作线程入口、关联规范、计划索引、生产代码或验收变化；用户改变目标
