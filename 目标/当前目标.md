# 当前目标

## 1. 目标身份

- 目标编号：`GOAL-IF-R-FOUNDATION`
- 名称：实例特征 IF/R 基础闭合
- 类型：当前小目标
- 状态：阻断
- 记录版本：v0.1
- 更新日期：2026-09-20

## 2. 目标关系

- 上位阶段目标：`GOAL-INSTINCT-RUNTIME`，建立基于世界事实和实例特征的本能运行系统基础
- 后继路线目标：本能根运行时锚定 `INSTINCT-ROOT-RUNTIME-ANCHOR`
- 当前计划：`INSTANCE-FEATURE-IF-R-CONCEPT-FOUNDATION` v0.8
- 设计承载方式：计划 v0.8 及其关联有效详细设计共同承载；本目标记录只承载目标、范围、状态和证据边界，不替代机器合同

## 3. 目标结果

建立实例特征容器`IF(E,FT)`、R集合版本和I64精确归组的可读写基础，使后续本能根运行时能够取得稳定的实例特征事实锚点。

## 4. 完成条件

- 存在类形成并读回 IFR v2 结构；
- 形成 IFR 读取和目标提交入口；
- R项材料按冻结合同承载并受预算、版本和幂等约束；
- 特征概念观察入口按计划接入；
- 不发生 R 到 FCr 的自动或同步概念抽象；
- 满足计划 v0.8 自身规定的文件、差异检查和记录要求。

## 5. 排除范围

- 不修改特征类；
- 不提前实施本能根运行时锚定；
- 不把代码提交、构建或局部读回扩大解释为本能系统完成；
- 不在当前目标内改写公开机器语义或计划合同。

## 6. 当前阻断与下一动作

当前`海中鱼巣/领域/数据服务.存在类.h`、`海中鱼巣/业务/应用服务.特征概念类.h`和`海中鱼巣/业务/应用服务.特征概念类.cpp`存在同一计划允许范围内的未提交修改。下一动作是确认这些 WIP 的唯一所有者、起点和计划归属，并完成 S0；在此之前不得接管、覆盖、清理、暂存或发布这些文件。

## 7. 证据边界

- 已确认：计划索引中该计划为当前唯一`可执行`计划；`main`与`origin/main`当前一致。
- 未确认：该 WIP 的所有者、完整实现正确性、编译、运行、恢复、集成验收和业务闭环。
