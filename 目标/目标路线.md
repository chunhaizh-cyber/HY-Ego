# 目标路线

```text
GOAL-INSTINCT-RUNTIME
构建本能系统基于世界事实、实例特征和现实锚点持续运行的基础架构
    ├── GOAL-IF-R-FOUNDATION（已完成：代码实施目标）
    │
    └── GOAL-INSTINCT-ROOT-PHYSICAL-CONTRACT（已完成：设计目标）
            ↓
        GOAL-INSTINCT-ROOT-TARGET-CONTRACT-REPAIR（已完成：设计修订目标）
            ↓
        GOAL-INSTINCT-ROOT-DEMAND-PROVIDER-IMPLEMENTATION（当前：代码实施目标；先重新S0）
            ↓
        阶段21本能根运行锚点代码实施（仍等待根材料与特征组合 provider 发布）
            ↓
        线程、运行门、连续多轮运行与具名集成验收
```

## 路线说明

- 上位集成能力目标的完成门槛为8300第7级工程验收和具名正式集成验收，不等同于代码已完成。
- IF/R 已完成的范围仅是代码实施，不包含构建、运行、恢复或本能循环能力。
- ARCH-L4 独立根材料 provider 合同已由`ARCH-L4-INSTINCT-ROOT-DEMAND-PROVIDER` v0.3 / blob `cabdcc23353c0d05ac1dff691744674b181ca0da`冻结：需求 owner 登记3个关系类型和1个I64属性类型，每根形成`T/D/L`、`D->F/D->T/L->D`及`T=I64_MAX`属性槽。当前仅允许执行者重新S0后实施；阶段21仍等待其与特征组合 provider 的已发布结果；旧锚点详细设计已具名 DRIFT，不得恢复施工。
- 更远目标不提前登记为计划索引中的`可执行`计划。
