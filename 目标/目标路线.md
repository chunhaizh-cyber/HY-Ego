# 目标路线

```text
GOAL-INSTINCT-RUNTIME
构建本能系统基于世界事实、实例特征和现实锚点持续运行的基础架构
    ├── GOAL-IF-R-FOUNDATION（已完成：代码实施目标）
    │       ↓
    │   GOAL-EXISTENCE-IDENTITY-CONTRACT-REPAIR（已完成）
    │       ↓
    │   GOAL-IFR-FEATURE-IDENTITY-CONSUMER-REPAIR（已完成）
    │
    ├── GOAL-INSTINCT-ROOT-PHYSICAL-CONTRACT（已完成：设计目标）
    │       ↓
    │   GOAL-INSTINCT-ROOT-TARGET-CONTRACT-REPAIR（已完成）
    │       ↓
    │   GOAL-INSTINCT-ROOT-DEMAND-PROVIDER-IMPLEMENTATION（已完成）
    │       ↓
    │   GOAL-SYSTEM-ROOT-I64-ACTUAL-F-PROVIDER-IMPLEMENTATION（已完成）
    │
    └── GOAL-SELF-GOVERNANCE-CLOSURE（当前长期路线）
            ↓
        GOAL-SELF-THREAD-M1A-TOP-DOWN-DESIGN（已完成）
        已冻结上层线程控制流、窄依赖端口和首个代码计划
            ↓
        GOAL-SELF-THREAD-G1-ISOLATED-SHELL-IMPLEMENTATION（已完成）
        已实现单对象关闭门生命周期、强类型邮箱与停止回收
            ├── 现有上层公开服务可用 → 继续原调用链
            └── 真实调用遇到缺口 → 最小下层提供者目标 → 回到原调用点
                    ├── GOAL-INSTINCT-ROOT-RUNTIME-ANCHOR-REDESIGN（已完成）
                    │       ↓
                    │   GOAL-INSTINCT-ROOT-RUNTIME-ANCHOR-PROVIDER-IMPLEMENTATION（已完成）
                    │       ↓
                    │   GOAL-METHOD-REGISTRY-ROOT-EMPTY-DOMAIN-BUDGET-REPAIR-IMPLEMENTATION（已完成）
                    │       └── 已回到阶段21原调用点并从修复提交重新S0
                    │       ↓
                    │   GOAL-INSTINCT-ROOT-RUNTIME-PRODUCTION-CONSUMER-DESIGN（已完成）
                    │       ↓
                    │   GOAL-INSTINCT-ROOT-RUNTIME-PRODUCTION-CONSUMER-IMPLEMENTATION（已完成）
                    │       └── 阶段21生产消费、self停门与宿主分流已由f11d94000发布
                    └── 其它提供者仅按实际调用建立
            ↓
        G1 线程运行骨架
            ↓
        G2 自我上下文正式读回
            └── GOAL-SELF-CONTEXT-FRESH-READ-PROVIDER-IMPLEMENTATION（已完成）
                    └── e04887112已发布纯读双根材料 + 同截止上下文provider + 普通应用所有权；尚未注入self
            ↓
        G3 治理消息循环
            ↓
        G4-A 根需求复核（provider已由90d9b747b发布）
            ├── 已闭合目标合同值来源、结果DTO、逐根技术状态和普通应用唯一provider
            └── 当前后继阻断：根任务目标投影 + 三项初始化幂等身份签发 + task owner任务核心精确合同
                    ├── 先形成最早task owner provider计划
                    └── provider发布后再回到self消费 + 异步根治理意图sink；治理门仍关闭
            ↓
        M1-A 运行、恢复与真实消费验收
            ↓
        G4-B 后继需求正式发布 + G5最小父任务材料
            ↓
        M1-B 验收
            ↓
        G5完整任务管理交接 → G6执行前判断 → G7结果结算 → G8安全/服务持续治理
            ↓
        M2 连续多轮运行与具名正式集成验收
```

```text
GOAL-DYNAMIC-CAPABILITY（候选；当前暂停直接推进）
    ↓
GOAL-DYNAMIC-CAPABILITY-DESIGN（已完成）
    ↓
GOAL-SECONDARY-RELATION-CONCEPT-STRUCTURE-P1-IMPLEMENTATION（已完成）
    ↓
RC求值与形成 / 动态实例符合性 / 动态概念结构与算法的逐叶代码目标
    ↓
7196分层验证、恢复、实际消费者与具名集成验收
```

## 路线说明

- `GOAL-SELF-GOVERNANCE-CLOSURE`是`GOAL-INSTINCT-RUNTIME`的当前集成路线，不另造与本能运行重复的顶层项目目标。
- 采用自顶向下实现：先写自我线程上层当前步骤，由真实调用点反推最小下层能力；下层完成后必须回到原消费者完成接线和独立读回。
- “先写上层”不授权编造底层机器语义。确需占位时必须标注`待实现`、返回结构化未实现且零机器事实写入；需要真实成功的运行门不得由占位解除。
- 已完成G1首叶只形成单对象隔离生命周期。阶段21组合provider已由`4c6cd9277`发布，生产消费闭环已由`f11d94000`发布，G2-A已由`e04887112`发布纯读上下文provider及生产所有权，G4-A双根只读复核provider已由`90d9b747b`发布。self仍未实际消费后两项provider，根治理意图sink尚不存在；现行任务版本3不能承接双根D且不满足5260任务核心合同，治理门仍未开放。
- G0只闭合当前上层步骤必需的合同，不建立底层能力库存；G1—G8是八个能力组，现在不是八份活动目标或计划。
- 方案文件`20260922_SELF-GOVERNANCE_自我线程内部治理闭环实现方案_v0.1.md`是路线设计输入，不是活动目标权威记录、正式机器合同或可执行计划。
- M1-A只证明合法启动前置、线程骨架、上下文、消息循环和根需求复核；M1-B再加入正式后继需求发布及最小父任务材料；M2才覆盖G5—G8和最终具名验收。
- 自我线程只消费具名上层公开服务，不直接或借getter下钻DATA-L1/DATA-L2；缺提供者时形成所属服务的最小设计/计划，不在线程中补底层逻辑。
- 动态路线的RC-P1代码与证据已由`873637e25`、`b3004e01e`发布并归档；它只证明RC结构owner及具名验证范围，不证明RC求值、RC形成或动态概念完成。
- 上位集成目标的完成门槛为8300规定的工程验收和具名正式集成验收，不等同于方案、设计、计划、代码或单次构建完成。
